# Syntax documentation
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → reversed_list = ...  # stored in memory
#   → print(reversed_list)
#   → numbers = ...  # stored in memory
#   → reversed_list = ...  # stored in memory
#   → print(reversed_list)
#   → numbers = ...  # stored in memory
#   → reversed_list = ...  # stored in memory
#   → print(reversed_list)
"""
Problème 1001 :
Écrivez un programme Python à reverse une liste en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
"""
Problème 1002 :
Écrivez un programme Python à extraire chaque troisième élément de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1003 :
Écrivez un programme Python à extraire chaque deuxième caractère de une chaîne en ordre inverse order.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → middle_index = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → middle_index = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → middle_index = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1004 :
Écrivez un programme Python à slice le middle trois caractères de une chaîne avec an impair longueur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1005 :
Écrivez un programme Python à remove le premier et dernier caractères de une chaîne en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1006 :
Écrivez un programme Python à get le premier half de une chaîne en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1007 :
Écrivez un programme Python à get le deuxième half de une chaîne en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → middle_index = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → middle_index = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → middle_index = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1008 :
Écrivez un programme Python à extraire le middle deux caractères de an même-longueur chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
"""
Problème 1009 :
Écrivez un programme Python à remove le premier trois éléments de une liste en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
"""
Problème 1010 :
Écrivez un programme Python à remove le dernier deux éléments de un tuple en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
"""
Problème 1011 :
Écrivez un programme Python à slice chaque alternate élément de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1012 :
Écrivez un programme Python à extraire un substring de index 3 à 7 en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
"""
Problème 1013 :
Écrivez un programme Python à extraire un tuple slice de index 2 à 5.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return s == s[::-1]
#   → return s == s[::-1]
#   → return s == s[::-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_palindrome is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
"""
Problème 1014 :
Écrivez un programme Python à vérifier si une chaîne est un palindrome en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1015 :
Écrivez un programme Python à extraire le dernier trois caractères de une chaîne en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
#   → numbers = ...  # stored in memory
#   → sliced_list = ...  # stored in memory
#   → print(sliced_list)
"""
Problème 1016 :
Écrivez un programme Python à extraire le premier cinq éléments de une liste en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
#   → numbers = ...  # stored in memory
#   → sliced_tuple = ...  # stored in memory
#   → print(sliced_tuple)
"""
Problème 1017 :
Écrivez un programme Python à extraire tous but le dernier trois éléments de un tuple en utilisant slicing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
#   → text = ...  # stored in memory
#   → sliced_text = ...  # stored in memory
#   → print(sliced_text)
"""
Problème 1018 :
Écrivez un programme Python à extraire une chaîne sans ses premier et dernier deux caractères.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → squares = ...  # stored in memory
#   → print(squares)
#   → squares = ...  # stored in memory
#   → print(squares)
#   → squares = ...  # stored in memory
#   → print(squares)
"""
Problème 1019 :
Écrivez un programme Python à créer une liste de squares de nombres de 1 à 10 en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → evens = ...  # stored in memory
#   → print(evens)
#   → evens = ...  # stored in memory
#   → print(evens)
#   → evens = ...  # stored in memory
#   → print(evens)
"""
Problème 1020 :
Écrivez un programme Python à créer une liste de les nombres pairs de 1 à 20 en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → div_by_5 = ...  # stored in memory
#   → print(div_by_5)
#   → div_by_5 = ...  # stored in memory
#   → print(div_by_5)
#   → div_by_5 = ...  # stored in memory
#   → print(div_by_5)
"""
Problème 1021 :
Écrivez un programme Python à créer une liste de nombres de 1 à 50 qui sont divisible by 5 en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → sentence = ...  # stored in memory
#   → first_letters = ...  # stored in memory
#   → print(first_letters)
#   → sentence = ...  # stored in memory
#   → first_letters = ...  # stored in memory
#   → print(first_letters)
#   → sentence = ...  # stored in memory
#   → first_letters = ...  # stored in memory
#   → print(first_letters)
"""
Problème 1022 :
Écrivez un programme Python à créer une liste de le premier lettre de chaque mot dans une donnée phrase en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → sentence = ...  # stored in memory
#   → long_words = ...  # stored in memory
#   → print(long_words)
#   → sentence = ...  # stored in memory
#   → long_words = ...  # stored in memory
#   → print(long_words)
#   → sentence = ...  # stored in memory
#   → long_words = ...  # stored in memory
#   → print(long_words)
"""
Problème 1023 :
Écrivez un programme Python à créer une liste de mots avec plus que trois lettre de une phrase en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → squares_tuples = ...  # stored in memory
#   → print(squares_tuples)
#   → squares_tuples = ...  # stored in memory
#   → print(squares_tuples)
#   → squares_tuples = ...  # stored in memory
#   → print(squares_tuples)
"""
Problème 1024 :
Écrivez un programme Python à générer une liste de tuples contenant un nombre et son carré pour nombres de 1 à 10. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → doubled = ...  # stored in memory
#   → print(doubled)
#   → numbers = ...  # stored in memory
#   → doubled = ...  # stored in memory
#   → print(doubled)
#   → numbers = ...  # stored in memory
#   → doubled = ...  # stored in memory
#   → print(doubled)
"""
Problème 1025 :
Écrivez un programme Python à créer une nouvelle liste où chaque élément de an existing list est doubled. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → div_by_3_or_7 = ...  # stored in memory
#   → print(div_by_3_or_7)
#   → div_by_3_or_7 = ...  # stored in memory
#   → print(div_by_3_or_7)
#   → div_by_3_or_7 = ...  # stored in memory
#   → print(div_by_3_or_7)
"""
Problème 1026 :
Écrivez un programme Python à créer une liste de nombres de 1 à 100 qui sont either divisible by 3 ou 7. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → upper_words = ...  # stored in memory
#   → print(upper_words)
#   → words = ...  # stored in memory
#   → upper_words = ...  # stored in memory
#   → print(upper_words)
#   → words = ...  # stored in memory
#   → upper_words = ...  # stored in memory
#   → print(upper_words)
"""
Problème 1027 :
Écrivez un programme Python à convert une liste de chaînes à uppercase en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → nested_list = ...  # stored in memory
#   → flattened = ...  # stored in memory
#   → print(flattened)
#   → nested_list = ...  # stored in memory
#   → flattened = ...  # stored in memory
#   → print(flattened)
#   → nested_list = ...  # stored in memory
#   → flattened = ...  # stored in memory
#   → print(flattened)
"""
Problème 1028 :
Écrivez un programme Python à flatten une liste imbriquée en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → vowels = ...  # stored in memory
#   → print(vowels)
#   → text = ...  # stored in memory
#   → vowels = ...  # stored in memory
#   → print(vowels)
#   → text = ...  # stored in memory
#   → vowels = ...  # stored in memory
#   → print(vowels)
"""
Problème 1029 :
Écrivez un programme Python à créer une liste de voyelles de une donnée chaîne en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → mixed_list = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → print(numbers)
#   → result = ...  # stored in memory
#   → for x in Ellipsis  # runs body once per item
#   → print(result)
#   → mixed_list = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → print(numbers)
#   → mixed_list = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → print(numbers)
#
# Loop: takes one item from Ellipsis, runs body, repeats until done
"""
Problème 1030 :
Écrivez un programme Python à extraire seulement numeric valeurs de un mixte list. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# evaluates expression
# evaluates expression
# evaluates expression
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → fib = ...  # stored in memory
#   → print(fib)
#   → fib = ...  # stored in memory
#   → print(fib)
#   → fib = ...  # stored in memory
#   → print(fib)
"""
Problème 1031 :
Écrivez un programme Python à générer une liste de premier 10 Fibonacci nombres en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + != **
# Fonctions intégrées : all, int, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → primes = ...  # stored in memory
#   → print(primes)
#   → primes = ...  # stored in memory
#   → print(primes)
#   → primes = ...  # stored in memory
#   → print(primes)
"""
Problème 1032 :
Écrivez un programme Python à créer une liste de prime nombres up à 50 en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → sentence = ...  # stored in memory
#   → vowel_ending_words = ...  # stored in memory
#   → print(vowel_ending_words)
#   → sentence = ...  # stored in memory
#   → vowel_ending_words = ...  # stored in memory
#   → print(vowel_ending_words)
#   → sentence = ...  # stored in memory
#   → vowel_ending_words = ...  # stored in memory
#   → print(vowel_ending_words)
"""
Problème 1033 :
Écrivez un programme Python à générer une liste de tous words dans une phrase qui end avec une voyelle. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → lower_chars = ...  # stored in memory
#   → print(lower_chars)
#   → text = ...  # stored in memory
#   → lower_chars = ...  # stored in memory
#   → print(lower_chars)
#   → text = ...  # stored in memory
#   → lower_chars = ...  # stored in memory
#   → print(lower_chars)
"""
Problème 1034 :
Écrivez un programme Python à créer une liste de les caractères minuscules de une donnée chaîne. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → sentence = ...  # stored in memory
#   → word_lengths = ...  # stored in memory
#   → print(word_lengths)
#   → sentence = ...  # stored in memory
#   → word_lengths = ...  # stored in memory
#   → print(word_lengths)
#   → sentence = ...  # stored in memory
#   → word_lengths = ...  # stored in memory
#   → print(word_lengths)
"""
Problème 1035 :
Écrivez un programme Python à créer une liste de mots avec leur lengths de une donnée phrase. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → unique_numbers = ...  # stored in memory
#   → print(unique_numbers)
#   → numbers = ...  # stored in memory
#   → unique_numbers = ...  # stored in memory
#   → print(unique_numbers)
#   → numbers = ...  # stored in memory
#   → unique_numbers = ...  # stored in memory
#   → print(unique_numbers)
"""
Problème 1036 :
Écrivez un programme Python à remove les doublons de une liste en utilisant List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → filtered_words = ...  # stored in memory
#   → print(filtered_words)
#   → words = ...  # stored in memory
#   → filtered_words = ...  # stored in memory
#   → print(filtered_words)
#   → words = ...  # stored in memory
#   → filtered_words = ...  # stored in memory
#   → print(filtered_words)
"""
Problème 1037 :
Écrivez un programme Python à filtrer words contenant la lettre'p'de une donnée list. List comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 1 if n == 0 else n * factorial(n - 1)
#   → return 1 if n == 0 else n * factorial(n - 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only factorial is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(factorial(...))  # factorial() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(factorial(...))  # factorial() runs first → value goes to print()
"""
Problème 1038 :
Write un Python recursive fonction à calculate la factorielle de une donnée nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n if n == 1 else n + sum_n(n - 1)
#   → return n if n == 1 else n + sum_n(n - 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_n is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(sum_n(...))  # sum_n() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_n(...))  # sum_n() runs first → value goes to print()
"""
Problème 1039 :
Write un Python recursive fonction à calculate le somme de tous nombres de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n if n <= 1 else fibonacci(n - 1) + fibonacci(n - 2)
#   → return n if n <= 1 else fibonacci(n - 1) + fibonacci(n - 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only fibonacci is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(fibonacci(...))  # fibonacci() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(fibonacci(...))  # fibonacci() runs first → value goes to print()
"""
Problème 1040 :
Write un Python recursive fonction à compute le nth Fibonacci nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + ==
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return s if len(s) == 0 else s[-1] + reverse_string(s[:-1])
#   → return s if len(s) == 0 else s[-1] + reverse_string(s[:-1])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(reverse_string(...))  # reverse_string() runs first → value goes to print()
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_string(...))  # reverse_string() runs first → value goes to print()
"""
Problème 1041 :
Write un Python recursive fonction à reverse une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + < //
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 1 if n < 10 else 1 + count_digits(n // 10)
#   → return 1 if n < 10 else 1 + count_digits(n // 10)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(count_digits(...))  # count_digits() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_digits(...))  # count_digits() runs first → value goes to print()
"""
Problème 1042 :
Write un Python recursive fonction à compter le nombre de les chiffres dans une donnée nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return a if b == 0 else gcd(b, a % b)
#   → return a if b == 0 else gcd(b, a % b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only gcd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(gcd(...))  # gcd() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(gcd(...))  # gcd() runs first → value goes to print()
"""
Problème 1043 :
Write un Python recursive fonction à trouver le greatest commun divisor (GCD) des deux nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 0 if not lst else lst[0] + list_sum(lst[1:])
#   → return 0 if not lst else lst[0] + list_sum(lst[1:])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only list_sum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(list_sum(...))  # list_sum() runs first → value goes to print()
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(list_sum(...))  # list_sum() runs first → value goes to print()
"""
Problème 1044 :
Write un Python recursive fonction à trouver le somme d'éléments dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: <= == is and
# Fonctions intégrées : len, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True if len(temp) <= 1 else temp[0] == temp[-1] and is_palindrome(n, temp[1:-1])
#   → return True if len(temp) <= 1 else temp[0] == temp[-1] and is_palindrome(n, temp[1:-1])
#   → return True if len(temp) <= 1 else temp[0] == temp[-1] and is_palindrome(n, temp[1:-1])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_palindrome is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → temp = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1045 :
Write un Python recursive fonction à vérifier si un nombre est un palindrome.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 1 if n == 0 else x * power(x, n - 1)
#   → return 1 if n == 0 else x * power(x, n - 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only power is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(power(...))  # power() runs first → value goes to print()
#   → x = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(power(...))  # power() runs first → value goes to print()
"""
Problème 1046 :
Write un Python recursive fonction à trouver le power de un nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 0 if b == 0 else a + multiply(a, b - 1)
#   → return 0 if b == 0 else a + multiply(a, b - 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(multiply(...))  # multiply() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply(...))  # multiply() runs first → value goes to print()
"""
Problème 1047 :
Write un Python recursive fonction à compute le produit des deux nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: - >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → if n > 0:
#   →     print_numbers(n - 1)
#   → result = n
#   → return result
#   → if n > 0:
#   →     print_numbers(n - 1)
#   → result = n
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_numbers(5)  # jumps into print_numbers(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_numbers(...))  # print_numbers() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1048 :
Write un Python recursive fonction à print nombres de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: - >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → if n > 0:
#   →     print(n)
#   →     print_reverse(n - 1)
#   → if n > 0:
#   →     print(n)
#   →     print_reverse(n - 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_reverse, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_reverse(5)  # jumps into print_reverse(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_reverse(...))  # print_reverse() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1049 :
Write un Python recursive fonction à print nombres de n à 1.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <= and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True if len(lst) <= 1 else lst[0] <= lst[1] and is_sorted(lst[1:])
#   → return True if len(lst) <= 1 else lst[0] <= lst[1] and is_sorted(lst[1:])
#   → return True if len(lst) <= 1 else lst[0] <= lst[1] and is_sorted(lst[1:])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_sorted is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_sorted(...))  # is_sorted() runs first → value goes to print()
#   → print(is_sorted(...))  # is_sorted() runs first → value goes to print()
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_sorted(...))  # is_sorted() runs first → value goes to print()
"""
Problème 1050 :
Write un Python recursive fonction à vérifier si une liste est trié dans l'ordre croissant.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 0 if not s else (1 if s[0] == c else 0) + char_count(s[1:], c)
#   → return 0 if not s else (1 if s[0] == c else 0) + char_count(s[1:], c)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only char_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(char_count(...))  # char_count() runs first → value goes to print()
#   → s = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(char_count(...))  # char_count() runs first → value goes to print()
"""
Problème 1051 :
Write un Python recursive fonction à compter le occurrences de un caractère dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: + - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → if n == 0:
#   →     print(s)
#   → else:
#   →     binary_strings(n - 1, s + '0')
#   →     binary_strings(n - 1, s + '1')
#   → if n == 0:
#   →     print(s)
#   → else:
#   →     binary_strings(n - 1, s + '0')
#   →     binary_strings(n - 1, s + '1')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — binary_strings, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → binary_strings(3)  # jumps into binary_strings(), runs body, returns
#   → n = ...  # stored in memory
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(binary_strings(...))  # binary_strings() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1052 :
Write un Python recursive fonction à générer tous binary strings de longueur n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % + < // ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 0
#   → return 0
#   → return 0
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_of_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → string = ...  # stored in memory
#   → print(sum_of_digits(...))  # sum_of_digits() runs first → value goes to print()
#   → print(sum_of_digits(...))  # sum_of_digits() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_of_digits(...))  # sum_of_digits() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1053 :
Write un Python recursive fonction à compute le somme de les chiffres de un nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 0 if n == 0 else n + triangular_number(n - 1)
#   → return 0 if n == 0 else n + triangular_number(n - 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only triangular_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(triangular_number(...))  # triangular_number() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(triangular_number(...))  # triangular_number() runs first → value goes to print()
"""
Problème 1054 :
Write un Python recursive fonction à calculate le nth triangular nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '' if not s else remove_duplicates(s[1:], seen) if s[0] in seen else s[0] + remove_duplicates(s[1:], seen + s[0])
#   → return '' if not s else remove_duplicates(s[1:], seen) if s[0] in seen else s[0] + remove_duplicates(s[1:], seen + s[0])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_duplicates is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(remove_duplicates(...))  # remove_duplicates() runs first → value goes to print()
#   → s = ...  # stored in memory
#   → seen = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_duplicates(...))  # remove_duplicates() runs first → value goes to print()
"""
Problème 1055 :
Write un Python recursive fonction à remove les doublons de une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: +
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → if not s:
#   →     print(prefix)
#   → else:
#   →     for i in range(len(s)):
#   →         permutations(s[:i] + s[i + 1:], prefix + s[i])
#   → if not s:
#   →     print(prefix)
#   → else:
#   →     for i in range(len(s)):
#   →         permutations(s[:i] + s[i + 1:], prefix + s[i])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — permutations, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → permutations('abc')  # jumps into permutations(), runs body, returns
#   → s = ...  # stored in memory
#   → prefix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(permutations(...))  # permutations() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1056 :
Write un Python recursive fonction à générer tous permutations de une donnée chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len, max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(words, key=lambda word: len(word))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only longest_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → words = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(longest_string(...))  # longest_string() runs first → value goes to print()
"""
Problème 1057 :
Écrivez un programme Python à trouver le plus long chaîne dans une liste en utilisant le `max()` fonction avec un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : is_even()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_even = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_even = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_even = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1058 :
Écrivez un programme Python à vérifier si un nombre est pair en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: math.sqrt(x), numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only square_roots is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(square_roots(...))  # square_roots() runs first → value goes to print()
"""
Problème 1059 :
Écrivez un programme Python à calculate le carré root de tous éléments dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : any, contains_digits, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → contains_digits = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → contains_digits = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → contains_digits = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1060 :
Écrivez un programme Python à vérifier si une chaîne contient un les chiffres en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(tuples, key=lambda x: x[0], reverse=True)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_by_first_element_desc is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → tuples = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_by_first_element_desc(...))  # sort_by_first_element_desc() runs first → value goes to print()
"""
Problème 1061 :
Écrivez un programme Python à trier une liste de tuples based on le premier élément dans l'ordre décroissant en utilisant un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >=
# Fonctions intégrées : filter, len, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(filter(lambda word: len(word) >= length, words))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_short_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → words = ...  # stored in memory
#   → length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(filter_short_words(...))  # filter_short_words() runs first → value goes to print()
"""
Problème 1062 :
Écrivez un programme Python à filtrer out words qui sont shorter que une donnée longueur en utilisant le `filtrer()` fonction avec un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: x * 2, numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only double_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(double_elements(...))  # double_elements() runs first → value goes to print()
"""
Problème 1063 :
Écrivez un programme Python à double tous éléments dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : is_divisible_by_3()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_divisible_by_3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_divisible_by_3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_divisible_by_3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1064 :
Écrivez un programme Python à vérifier si un nombre est divisible by 3 en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : second_largest, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → second_largest = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → second_largest = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → second_largest = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1065 :
Écrivez un programme Python à trouver le deuxième plus grand nombre dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: x ** 3, numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only cube_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(cube_elements(...))  # cube_elements() runs first → value goes to print()
"""
Problème 1066 :
Écrivez un programme Python à map une liste de nombres dans leur cubes en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(words, key=lambda word: len(word))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → words = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_by_length(...))  # sort_by_length() runs first → value goes to print()
"""
Problème 1067 :
Écrivez un programme Python à trier une liste de mots by leur lengths en utilisant le `sorted()` fonction avec un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % !=
# Fonctions intégrées : is_odd()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_odd = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_odd = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_odd = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1068 :
Écrivez un programme Python à vérifier si un nombre est impair en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return reduce(lambda x, y: x + y, numbers)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_of_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_of_elements(...))  # sum_of_elements() runs first → value goes to print()
"""
Problème 1069 :
Écrivez un programme Python à trouver le somme de tous éléments dans une liste en utilisant le `reduce()` fonction avec un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
# Fonctions intégrées : ends_with_vowel()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → ends_with_vowel = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → ends_with_vowel = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → ends_with_vowel = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1070 :
Écrivez un programme Python à vérifier si une chaîne termine avec une voyelle en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → smallest_number = ...  # stored in memory
#   → print('The smallest number in the list is:', smallest_number)
#   → numbers = ...  # stored in memory
#   → smallest_number = ...  # stored in memory
#   → print('The smallest number in the list is:', smallest_number)
#   → numbers = ...  # stored in memory
#   → smallest_number = ...  # stored in memory
#   → print('The smallest number in the list is:', smallest_number)
"""
Problème 1071 :
Écrivez un programme Python à trouver le plus petit nombre dans une liste en utilisant le `min()` fonction avec un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : is_alpha()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_alpha = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_alpha = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_alpha = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1072 :
Écrivez un programme Python à vérifier si une chaîne contient seulement alphabetic caractères en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, map, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: str(x), numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_strings is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_strings(...))  # convert_to_strings() runs first → value goes to print()
"""
Problème 1073 :
Écrivez un programme Python à convert tous integers dans une liste à strings en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : filter, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum(filter(lambda x: x % 2 == 0, numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_of_even is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_of_even(...))  # sum_of_even() runs first → value goes to print()
"""
Problème 1074 :
Écrivez un programme Python à trouver le somme de tous les nombres pairs dans une liste en utilisant un lambda expression.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : count_even, filter, len, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → count_even = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → count_even = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → count_even = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1076 :
Écrivez un programme Python à compter le nombre de les nombres pairs dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % * !=
# Fonctions intégrées : filter, product_of_odds, reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → product_of_odds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → product_of_odds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → product_of_odds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1077 :
Écrivez un programme Python à trouver le produit de tous les nombres impairs dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + > != **
# Fonctions intégrées : all, int, is_prime, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_prime = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_prime = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_prime = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1078 :
Écrivez un programme Python à vérifier si un nombre est prime en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: (x, math.sqrt(x)), numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_square_root_tuples is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_square_root_tuples(...))  # create_square_root_tuples() runs first → value goes to print()
"""
Problème 1079 :
Écrivez un programme Python à créer une liste de tuples (nombre, carré root) pour chaque nombre dans une donnée list en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : any, contains_uppercase, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → contains_uppercase = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → contains_uppercase = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → contains_uppercase = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1080 :
Écrivez un programme Python à vérifier si une chaîne contient un uppercase lettre en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % * / !=
# Fonctions intégrées : Fraction, filter, float, len, list, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → from fractions import Fraction
#   → return float(Fraction(total, count))
#   → from fractions import Fraction
#   → return float(Fraction(total, count))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only average_of_odds is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → average_of_odds(lst)  # jumps into average_of_odds(), runs body, returns
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(average_of_odds(...))  # average_of_odds() runs first → value goes to print()
"""
Problème 1081 :
Écrivez un programme Python à trouver le moyenne de les nombres impairs dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % != **
# Fonctions intégrées : filter, list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: x ** 2, filter(lambda x: x % 2 != 0, numbers)))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only square_odd_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(square_odd_numbers(...))  # square_odd_numbers() runs first → value goes to print()
"""
Problème 1082 :
Écrivez un programme Python à créer une liste de squares pour tous les nombres impairs dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
# Fonctions intégrées : any, contains_negative, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → contains_negative = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → contains_negative = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → contains_negative = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1083 :
Écrivez un programme Python à vérifier si une liste contient un les nombres négatifs en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: (x, x ** 3), numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_cube_tuples is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_cube_tuples(...))  # create_cube_tuples() runs first → value goes to print()
"""
Problème 1084 :
Écrivez un programme Python à créer une liste de tuples où le premier élément est le original nombre et le deuxième élément est ses cube en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
# Fonctions intégrées : filter, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(filter(lambda x: x <= value, numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_greater_than is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(filter_greater_than(...))  # filter_greater_than() runs first → value goes to print()
"""
Problème 1085 :
Écrivez un programme Python à filtrer out nombres greater que une donnée valeur en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len, list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda x: len(x), strings))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_lengths is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_lengths(...))  # convert_to_lengths() runs first → value goes to print()
"""
Problème 1086 :
Écrivez un programme Python à convert une liste de chaînes à leur lengths en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ** ==
# Fonctions intégrées : int, is_perfect_square()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_perfect_square = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_perfect_square = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → is_perfect_square = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1087 :
Écrivez un programme Python à vérifier si un nombre est un perfect carré en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : map, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum(map(lambda x: x ** 3, numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_of_cubes is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_of_cubes(...))  # sum_of_cubes() runs first → value goes to print()
"""
Problème 1088 :
Écrivez un programme Python à trouver le somme de le cubes de tous nombres dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
# Fonctions intégrées : filter, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(filter(lambda x: x <= value, numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_elements_greater_than is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_elements_greater_than(...))  # remove_elements_greater_than() runs first → value goes to print()
"""
Problème 1089 :
Écrivez un programme Python à remove tous éléments greater que un spécifique valeur dans une liste en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : all, all_positive, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → all_positive = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → all_positive = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → all_positive = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1090 :
Écrivez un programme Python à vérifier si tous éléments dans une liste sont positif en utilisant une fonction lambda.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: **
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(n):
#   →     yield (i ** 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, square_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(square_generator(...))  # square_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1091 :
Écrivez un programme Python à créer un generator qui yields le carré de chaque nombre dans une donnée range.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: + <=
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → a, b = (0, 1)
#   → while a <= limit:
#   →     yield a
#   →     a, b = (b, a + b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — fibonacci_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → limit = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(fibonacci_generator(...))  # fibonacci_generator() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
"""
Problème 1092 :
Écrivez un programme Python à créer un generator qui yields Fibonacci nombres up à une donnée nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for char in s:
#   →     yield char
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — char_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(char_generator(...))  # char_generator() runs first → value goes to print()
#
# Loop: takes one item from s, runs body, repeats until done
"""
Problème 1093 :
Écrivez un programme Python à créer un generator qui yields le caractères de une chaîne l'une by l'une.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n, 2):
#   →     yield i
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, step_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(step_generator(...))  # step_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1094 :
Écrivez un programme Python à créer un generator qui yields nombres de 1 à n avec un step de 2.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(2, n + 1, 2):
#   →     yield i
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — even_number_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(even_number_generator(...))  # even_number_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1095 :
Écrivez un programme Python à créer un generator qui yields les nombres pairs de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: + **
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(2, n + 1, 2):
#   →     yield (i ** 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, square_even_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(square_even_generator(...))  # square_even_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1096 :
Écrivez un programme Python à créer un generator qui yields squares de les nombres pairs de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else | while
# Operators: % < !=
# Fonctions intégrées : all, len, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → primes = []
#   → num = 2
#   → while len(primes) < n:
#   →     if all((num % p != 0 for p in primes)):
#   →         primes.append(num)
#   →     num += 1
#   → yield from primes
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — prime_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(prime_generator(...))  # prime_generator() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1097 :
Écrivez un programme Python à créer un generator qui yields les n premiers prime nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: + -
# Fonctions intégrées : len, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(len(lst) - 1):
#   →     yield (lst[i] + lst[i + 1])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — consecutive_pair_sum, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(consecutive_pair_sum(...))  # consecutive_pair_sum() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1098 :
Écrivez un programme Python à créer un generator qui yields le somme de consécutifs pairs de une donnée list.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : factorial, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only factorial_generator is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(factorial_generator(...))  # factorial_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1099 :
Écrivez un programme Python à créer un generator qui yields la factorielle de chaque nombre de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % + == and
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n + 1):
#   →     if i % 3 == 0 and i % 5 == 0:
#   →         yield i
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — divisible_by_3_and_5_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_3_and_5_generator(...))  # divisible_by_3_and_5_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1100 :
Écrivez un programme Python à créer un generator qui yields nombres divisible by 3 et 5 de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for num in lst:
#   →     if num % 2 == 0:
#   →         yield num
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — odd_filter_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(odd_filter_generator(...))  # odd_filter_generator() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1101 :
Écrivez un programme Python à créer un generator qui filtre out les nombres impairs de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → total = 0
#   → for num in lst:
#   →     total += num
#   →     yield total
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — cumulative_sum_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(cumulative_sum_generator(...))  # cumulative_sum_generator() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
"""
Problème 1102 :
Écrivez un programme Python à créer un generator qui yields le cumulative somme de une donnée list de nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: + **
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n + 1):
#   →     yield (i ** power)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — power_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → power = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(power_generator(...))  # power_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1103 :
Écrivez un programme Python à créer un generator qui yields le n-th power de chaque nombre de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → words = sentence.split()
#   → for word in words:
#   →     yield word
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, word_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → sentence = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(word_generator(...))  # word_generator() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
"""
Problème 1104 :
Écrivez un programme Python à créer un generator qui yields chaque mot dans une phrase.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: + **
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n + 1):
#   →     yield (i ** 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, square_number_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(square_number_generator(...))  # square_number_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1105 :
Écrivez un programme Python à créer un generator qui yields nombres qui sont squares de integers up à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % + != **
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for num in range(1, n + 1):
#   →     if num % 2 != 0:
#   →         yield (num ** 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve, square_odd_number_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(main(...))  # main() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1106 :
Écrivez un programme Python à créer un generator qui yields le squares de les nombres impairs de 1 à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: -
# Fonctions intégrées : len, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(len(lst) - 1, -1, -1):
#   →     yield lst[i]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — reverse_list_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_list_generator(...))  # reverse_list_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1107 :
Écrivez un programme Python à créer un generator qui yields le éléments de une donnée list en ordre inverse order.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(multiple, n + 1, multiple):
#   →     yield i
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — multiple_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → multiple = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiple_generator(...))  # multiple_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1108 :
Écrivez un programme Python à créer un generator qui yields multiples de une donnée nombre up à n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for char in s:
#   →     if char.isupper():
#   →         yield char
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, uppercase_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(uppercase_generator(...))  # uppercase_generator() runs first → value goes to print()
#
# Loop: takes one item from s, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1109 :
Écrivez un programme Python à créer un generator qui yields tous le uppercase lettre dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: + -
# Fonctions intégrées : len, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(len(lst) - 1):
#   →     yield (lst[i + 1] - lst[i])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — consecutive_diff_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(consecutive_diff_generator(...))  # consecutive_diff_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1110 :
Écrivez un programme Python à créer un generator qui yields le differences entre consécutifs éléments de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: >
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for num in lst:
#   →     if num > value:
#   →         yield num
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — greater_than_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greater_than_generator(...))  # greater_than_generator() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1111 :
Écrivez un programme Python à créer un generator qui yields nombres greater que une donnée valeur dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : enumerate, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for index, num in enumerate(lst):
#   →     yield (num + index)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, sum_with_index_generator are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_with_index_generator(...))  # sum_with_index_generator() runs first → value goes to print()
#
# Loop: takes one item from enumerate(...), runs body, repeats until done
"""
Problème 1112 :
Écrivez un programme Python à créer un generator qui yields le somme de chaque élément avec ses index de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ==
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for word in lst:
#   →     if word == word[::-1]:
#   →         yield word
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — palindrome_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(palindrome_generator(...))  # palindrome_generator() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1113 :
Écrivez un programme Python à créer un generator qui yields tous palindromes de une liste de chaînes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: * + -
# Fonctions intégrées : len, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(len(lst) - 1):
#   →     yield (lst[i] * lst[i + 1])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — pair_product_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(pair_product_generator(...))  # pair_product_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1114 :
Écrivez un programme Python à créer un generator qui yields le produit d'éléments dans pairs de une donnée list.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: - >
# Fonctions intégrées : len, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, len(lst)):
#   →     if lst[i] > lst[i - 1]:
#   →         yield lst[i]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — greater_than_previous_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greater_than_previous_generator(...))  # greater_than_previous_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1115 :
Écrivez un programme Python à créer un generator qui yields éléments de une liste qui sont greater que le précédent élément.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for num in lst:
#   →     if num % divisor == 0:
#   →         yield num
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — divisible_by_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → divisor = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_generator(...))  # divisible_by_generator() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1116 :
Écrivez un programme Python à créer un generator qui yields tous nombres dans une liste qui sont divisible by une donnée nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: * + <
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → a, b = (0, 2)
#   → count = 0
#   → while count < n:
#   →     yield a
#   →     a, b = (b, 4 * b + a)
#   →     count += 1
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — even_fibonacci_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(even_fibonacci_generator(...))  # even_fibonacci_generator() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
"""
Problème 1117 :
Écrivez un programme Python à créer un generator qui yields les n premiers même Fibonacci nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : len, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(0, len(lst), 2):
#   →     yield lst[i]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — even_index_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(even_index_generator(...))  # even_index_generator() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1118 :
Écrivez un programme Python à créer un generator qui yields tous le même-indexed éléments de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
# Fonctions intégrées : len, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → words = sentence.split()
#   → for word in words:
#   →     if len(word) % 2 == 0:
#   →         yield word
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — even_length_word_generator, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → sentence = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(even_length_word_generator(...))  # even_length_word_generator() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1119 :
Écrivez un programme Python à créer un generator qui yields chaque mot dans une phrase, but seulement ceux avec an même nombre de caractères.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: < >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Positive'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_number(...))  # check_number() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1120 :
Écrivez un programme Python à vérifier si un nombre est positif, négatif, ou zero. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: >= and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return a
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only largest_of_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(largest_of_three(...))  # largest_of_three() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1121 :
Écrivez un programme Python à trouver le plus grand de trois nombres. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % != == or and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_leap_year is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → year = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_leap_year(...))  # is_leap_year() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1122 :
Écrivez un programme Python à vérifier si une donnée year est une année bissextile. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Even'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_even_odd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_even_odd(...))  # check_even_odd() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1123 :
Écrivez un programme Python à vérifier si un nombre est pair ou impair. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divisible_by_3_and_5 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_3_and_5(...))  # divisible_by_3_and_5() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1124 :
Écrivez un programme Python à vérifier si un nombre est divisible by les deux 3 et 5. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % + ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n + 1):
#   →     if i % 7 == 0:
#   →         print(i)
#   → for i in range(1, n + 1):
#   →     if i % 7 == 0:
#   →         print(i)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — divisible_by_7, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → divisible_by_7(50)  # jumps into divisible_by_7(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_7(...))  # divisible_by_7() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1125 :
Écrivez un programme Python à print tous nombres de 1 à n qui sont divisible by 7. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % + < ** ==
# Fonctions intégrées : int, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return False
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_prime is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_prime(...))  # is_prime() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1126 :
Écrivez un programme Python à vérifier si une donnée nombre est un nombre premier. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: * - == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 1
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only factorial is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(factorial(...))  # factorial() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1127 :
Écrivez un programme Python à trouver la factorielle de un nombre en utilisant recursion. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_palindrome is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_palindrome(...))  # is_palindrome() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1128 :
Écrivez un programme Python à vérifier si une chaîne est un palindrome. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: in
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return len([char for char in s if char.lower() in vowels])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_vowels is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → s = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_vowels(...))  # count_vowels() runs first → value goes to print()
#
# Loop: takes one item from s, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1129 :
Écrivez un programme Python à compter le nombre de voyelles dans une donnée chaîne. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % ==
# Fonctions intégrées : range, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_perfect_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_perfect_number(...))  # is_perfect_number() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1130 :
Écrivez un programme Python à vérifier si un nombre est un nombre parfait. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ** ==
# Fonctions intégrées : int, len, str, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_armstrong is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → total = ...  # stored in memory
#   → for digit in num_str  # runs body once per item
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_armstrong(...))  # is_armstrong() runs first → value goes to print()
#
# Loop: takes one item from num_str, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1131 :
Écrivez un programme Python à vérifier si un nombre est un nombre d'Armstrong. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % + != **
# Fonctions intégrées : all, int, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return primes
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only prime_numbers_up_to_n is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(prime_numbers_up_to_n(...))  # prime_numbers_up_to_n() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1132 :
Écrivez un programme Python à print tous le prime nombres less que ou equal à n. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % > == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only even_and_greater_than_10 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(even_and_greater_than_10(...))  # even_and_greater_than_10() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1133 :
Écrivez un programme Python à vérifier si un nombre est an même nombre et greater que 10. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % + ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return divisors
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_divisors is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → n = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_divisors(...))  # find_divisors() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1134 :
Écrivez un programme Python à trouver tous le divisors de un nombre. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: *
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, 11):
#   →     print(f'{n} x {i} = {n * i}')
#   → for i in range(1, 11):
#   →     print(f'{n} x {i} = {n * i}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — multiplication_table, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → multiplication_table(7)  # jumps into multiplication_table(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiplication_table(...))  # multiplication_table() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1135 :
Écrivez un programme Python à print le multiplication table de un nombre up à 10. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ** ==
# Fonctions intégrées : int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return int(n ** 0.5) ** 2 == n
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_perfect_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_perfect_square(...))  # is_perfect_square() runs first → value goes to print()
"""
Problème 1136 :
Écrivez un programme Python à vérifier si un nombre est un perfect carré. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: & - > == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n > 0 and n & n - 1 == 0
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_power_of_2 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_power_of_2(...))  # is_power_of_2() runs first → value goes to print()
"""
Problème 1137 :
Écrivez un programme Python à vérifier si un nombre est un power de 2. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : len, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return lst[1] if len(lst) > 1 else None
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only second_largest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(second_largest(...))  # second_largest() runs first → value goes to print()
"""
Problème 1138 :
Écrivez un programme Python à trouver le deuxième plus grand nombre dans une liste. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else | while
# Operators: % > == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n == 1
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_power_of_3 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_power_of_3(...))  # is_power_of_3() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1139 :
Écrivez un programme Python à vérifier si une donnée nombre est un power de 3. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: + <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → a, b = (0, 1)
#   → while a <= n:
#   →     print(a, end=' ')
#   →     a, b = (b, a + b)
#   → a, b = (0, 1)
#   → while a <= n:
#   →     print(a, end=' ')
#   →     a, b = (b, a + b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only fibonacci_up_to_n is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → fibonacci_up_to_n(20)  # jumps into fibonacci_up_to_n(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(fibonacci_up_to_n(...))  # fibonacci_up_to_n() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
"""
Problème 1140 :
Écrivez un programme Python à print Fibonacci nombres up à n. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ==
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → count = 0
#   → for c in lst:
#   →     if c == element:
#   →         count += 1
#   → return count
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_occurrences is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_occurrences(...))  # count_occurrences() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1141 :
Écrivez un programme Python à compter le occurrences de un spécifique élément dans une liste. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: and
# Fonctions intégrées : is_armstrong, is_perfect_square()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_perfect_square_and_armstrong is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_perfect_square_and_armstrong(...))  # is_perfect_square_and_armstrong() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1142 :
Écrivez un programme Python à vérifier si une donnée nombre est un perfect carré et an un nombre d'Armstrong. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + == and
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [num for num in range(start, end + 1) if num % 7 == 0 and num % 11 == 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divisible_by_7_and_11 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_7_and_11(...))  # divisible_by_7_and_11() runs first → value goes to print()
"""
Problème 1143 :
Écrivez un programme Python à trouver tous nombres dans une donnée range qui sont divisible by 7 et 11. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only factorial_while is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(factorial_while(...))  # factorial_while() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
"""
Problème 1144 :
Écrivez un programme Python à print la factorielle de un nombre en utilisant un tandis que loop. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : reversed()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = ''
#   → for char in lst:
#   →     result = char + result
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_list(...))  # reverse_list() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
"""
Problème 1145 :
Écrivez un programme Python à print une liste de nombres en ordre inverse order. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divisible_by_5_or_10 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_5_or_10(...))  # divisible_by_5_or_10() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1146 :
Écrivez un programme Python à vérifier si un nombre est divisible by 5 ou 10. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divisible_by_any_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divisible_by_any_number(...))  # divisible_by_any_number() runs first → value goes to print()
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1147 :
Écrivez un programme Python à vérifier si un nombre est divisible by un nombre dans une donnée list. Control flow.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 10
#   → def inner_function():
#   →     print(x)
#   → inner_function()
#   → print(x)
#   → print(x)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — inner_function, outer_function, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → outer_function()  # jumps into outer_function(), runs body, returns
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
"""
Problème 1148 :
Écrivez un programme Python à demonstrate variable scope dans une fonction. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 10
#   → result = f'Local variable: {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only test_scope is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → test_scope()  # jumps into test_scope(), runs body, returns
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1149 :
Écrivez un programme Python à show le différence entre global et local variables. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → global x
#   → x = 30
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — modify_global_variable, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → modify_global_variable()  # jumps into modify_global_variable(), runs body, returns
#   → print(x)
#   → x = ...  # stored in memory
#   → x = ...  # stored in memory
"""
Problème 1150 :
Écrivez un programme Python à demonstrate le Utilisez de le `global` keyword. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 10
#   → def inner_function():
#   →     nonlocal x
#   →     x = 20
#   → inner_function()
#   → result = x
#   → return result
#   → nonlocal x
#   → x = 20
#   → nonlocal x
#   → x = 20
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — inner_function, outer_function are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → outer_function()  # jumps into outer_function(), runs body, returns
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → print(x)
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → print(x)
"""
Problème 1151 :
Écrivez un programme Python à demonstrate le Utilisez de `nonlocal` keyword. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 50
#   → result = f'Local x: {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only shadowing_example is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → shadowing_example()  # jumps into shadowing_example(), runs body, returns
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1152 :
Écrivez un programme Python à demonstrate variable shadowing. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → c = a + b
#   → result = f'The sum of {a} and {b} is {c}'
#   → return result
#   → c = a + b
#   → result = f'The sum of {a} and {b} is {c}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only example_function is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → example_function(5, 7)  # jumps into example_function(), runs body, returns
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(example_function(...))  # example_function() runs first → value goes to print()
"""
Problème 1153 :
Écrivez un programme Python à demonstrate le Utilisez de fonction arguments as local variables. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = f'Hello, {name}!'
#   → return result
#   → result = f'Hello, {name}!'
#   → return result
#   → result = f'Hello, {name}!'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only greet is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → greet()  # jumps into greet(), runs body, returns
#   → greet('Alice')  # jumps into greet(), runs body, returns
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greet(...))  # greet() runs first → value goes to print()
"""
Problème 1154 :
Écrivez un programme Python à demonstrate comment à pass une variable as un default argument. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → lst.append(4)
#   → result = f'Modified list: {lst}'
#   → return result
#   → n += 1
#   → print(f'Modified integer: {n}')
#   → lst.append(4)
#   → result = f'Modified list: {lst}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — modify_integer, modify_list are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → modify_list(my_list)  # jumps into modify_list(), runs body, returns
#   → print(?)
#   → my_int = ...  # stored in memory
#   → modify_integer(my_int)  # jumps into modify_integer(), runs body, returns
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(modify_list(...))  # modify_list() runs first → value goes to print()
"""
Problème 1155 :
Écrivez un programme Python à show le Utilisez de mutable et immutable tapez dans fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(3):
#   →     x = i
#   → return f'Outside loop: {x}'
#   → result = f'Outside loop: {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only loop_scope is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → loop_scope()  # jumps into loop_scope(), runs body, returns
#   → for i in range(...)  # runs body once per item
#   → print(?)
#   → for i in range(...)  # runs body once per item
#   → print(?)
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1156 :
Écrivez un programme Python à demonstrate le scope de variables dans une boucle. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → try:
#   →     print(x)
#   → except NameError:
#   →     print('x is not defined')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — check_variable, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → check_variable()  # jumps into check_variable(), runs body, returns
"""
Problème 1157 :
Écrivez un programme Python à vérifier si une variable est defined. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 10
#   → def inner_function():
#   →     y = 20
#   → return f'Outer function: x = {x}'
#   → inner_function()
#   → result = f'Outer function: x = {x}'
#   → return result
#   → x = 10
#   → def inner_function():
#   →     y = 20
#   → return f'Outer function: x = {x}'
#   → inner_function()
#   → result = f'Outer function: x = {x}'
#   → return result
#   → y = 20
#   → print(f'Inner function: x = {x}, y = {y}')
#   → y = 20
#   → print(f'Inner function: x = {x}, y = {y}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — inner_function, outer_function are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → outer_function()  # jumps into outer_function(), runs body, returns
#   → print(outer_function(...))  # outer_function() runs first → value goes to print()
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → print(?)
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → print(?)
"""
Problème 1158 :
Écrivez un programme Python à demonstrate le scope de variables dans imbriqué fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.add(a, b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_numbers(...))  # add_numbers() runs first → value goes to print()
"""
Problème 1159 :
Écrivez un programme Python à demonstrate comment à retourner une variable de une fonction. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return default_list
#   → return default_list
#   → return default_list
#   → return default_list
#   → return default_list
#   → return default_list
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only append_to_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(append_to_list(...))  # append_to_list() runs first → value goes to print()
#   → print(append_to_list(...))  # append_to_list() runs first → value goes to print()
#   → print(append_to_list(...))  # append_to_list() runs first → value goes to print()
#   → print(append_to_list(...))  # append_to_list() runs first → value goes to print()
#   → print(append_to_list(...))  # append_to_list() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → default_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(append_to_list(...))  # append_to_list() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1160 :
Écrivez un programme Python à explain comment default mutable arguments work dans fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x ** 2 for x in lst]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only square_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(square_numbers(...))  # square_numbers() runs first → value goes to print()
"""
Problème 1161 :
Écrivez un programme Python à demonstrate le Utilisez de variables dans list comprehensions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 5
#   → x = x * 2
#   → result = f'New value of x: {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reuse_variable is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → reuse_variable()  # jumps into reuse_variable(), runs body, returns
#   → x = ...  # stored in memory
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1162 :
Écrivez un programme Python à show le effect de reusing le même variable dans une fonction. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = f'Function One: {x}'
#   → return result
#   → x = 40
#   → print(f'Function Two: {x}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — function_one, function_two are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → function_one()  # jumps into function_one(), runs body, returns
#   → function_two()  # jumps into function_two(), runs body, returns
#   → print(?)
#   → print(?)
"""
Problème 1163 :
Écrivez un programme Python à demonstrate variable scope dans different fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → global x
#   → x += 5
#   → global x
#   → x -= 3
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — function_one, function_two, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → function_one()  # jumps into function_one(), runs body, returns
#   → function_two()  # jumps into function_two(), runs body, returns
#   → print(?)
"""
Problème 1164 :
Écrivez un programme Python à demonstrate le Utilisez de `global` pour modifying variables dans different fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 15
#   → def inner_function():
#   →     nonlocal x
#   →     x += 10
#   → return f'Outer function: x = {x}'
#   → inner_function()
#   → result = f'Outer function: x = {x}'
#   → return result
#   → nonlocal x
#   → x += 10
#   → print(f'Inner function: x = {x}')
#   → nonlocal x
#   → x += 10
#   → print(f'Inner function: x = {x}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — inner_function, outer_function are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → outer_function()  # jumps into outer_function(), runs body, returns
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → print(?)
#   → x = ...  # stored in memory
#   → inner_function()  # jumps into inner_function(), runs body, returns
#   → print(?)
"""
Problème 1165 :
Écrivez un programme Python à show comment variable valeurs sont changed dans imbriqué fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for arg in args:
#   →     print(arg)
#   → for arg in args:
#   →     print(arg)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_arguments, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_arguments(1, 2, 3, 'hello', ?)  # jumps into print_arguments(), runs body, returns
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_arguments(...))  # print_arguments() runs first → value goes to print()
#
# Loop: takes one item from args, runs body, repeats until done
"""
Problème 1166 :
Écrivez un programme Python à demonstrate le Utilisez de variable arguments dans fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(3):
#   →     y = i
#   → return f'After the loop: y = {y}'
#   → result = f'After the loop: y = {y}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only loop_variable_scope is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → loop_variable_scope()  # jumps into loop_variable_scope(), runs body, returns
#   → for i in range(...)  # runs body once per item
#   → print(?)
#   → for i in range(...)  # runs body once per item
#   → print(?)
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1167 :
Écrivez un programme Python à show comment le scope de une variable changes dans un pour loop. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = f'Global variable x inside function: {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only global_inside_function is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → global_inside_function()  # jumps into global_inside_function(), runs body, returns
#   → print(?)
#   → print(?)
"""
Problème 1168 :
Écrivez un programme Python à show le behavior de variables dans global scope à l'intérieur de fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → mutable = [1, 2, 3]
#   → immutable = 10
#   → mutable[0] = 100
#   → immutable = 20
#   → return f'Immutable variable: {immutable}'
#   → result = f'Immutable variable: {immutable}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only test_mutability is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → test_mutability()  # jumps into test_mutability(), runs body, returns
#   → mutable = ...  # stored in memory
#   → immutable = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → immutable = ...  # stored in memory
#   → print(?)
#   → print(?)
#   → mutable = ...  # stored in memory
#   → immutable = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → immutable = ...  # stored in memory
#   → print(?)
#   → print(?)
"""
Problème 1169 :
Écrivez un programme Python à explain le différence entre mutable et immutable variables dans terms de scope. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → lst = [x ** 2 for x in range(5)]
#   → result = f'List comprehension result: {lst}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only list_comprehension_variable_scope is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → list_comprehension_variable_scope()  # jumps into list_comprehension_variable_scope(), runs body, returns
#   → lst = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → print(?)
"""
Problème 1170 :
Écrivez un programme Python à show le behavior de variables declared dans une liste comprehension. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → y = x * 2
#   → result = f'Function variable y: {y}'
#   → return result
#   → y = x * 2
#   → result = f'Function variable y: {y}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_variable_in_function is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → z = ...  # stored in memory
#   → print_variable_in_function(z)  # jumps into print_variable_in_function(), runs body, returns
#   → x = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_variable_in_function(...))  # print_variable_in_function() runs first → value goes to print()
"""
Problème 1171 :
Écrivez un programme Python à show qui fonction arguments peut be variables avec different scopes. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(3):
#   →     x = i
#   → return f'Outside the loop: x = {x}'
#   → result = f'Outside the loop: x = {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only for_loop_variable_scope is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for_loop_variable_scope()  # jumps into for_loop_variable_scope(), runs body, returns
#   → for i in range(...)  # runs body once per item
#   → print(?)
#   → for i in range(...)  # runs body once per item
#   → print(?)
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1172 :
Écrivez un programme Python à show qui une variable dans un pour loop peut be accessed à l'extérieur de le loop. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → global x
#   → x = 50
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — change_global_variable, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → change_global_variable()  # jumps into change_global_variable(), runs body, returns
#   → print(?)
#   → x = ...  # stored in memory
#   → x = ...  # stored in memory
"""
Problème 1173 :
Écrivez un programme Python à demonstrate le impact de changing un global variable à l'intérieur de une fonction. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : inner_function()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → x = 5
#   → def middle_function():
#   →     y = 10
#   →
#   →     def inner_function():
#   →         z = 15
#   →         print(f'Inner function: x = {x}, y = {y}, z = {z}')
#   →     inner_function()
#   → middle_function()
#   → y = 10
#   → def inner_function():
#   →     z = 15
#   →     print(f'Inner function: x = {x}, y = {y}, z = {z}')
#   → inner_function()
#   → y = 10
#   → def inner_function():
#   →     z = 15
#   →     print(f'Inner function: x = {x}, y = {y}, z = {z}')
#   → inner_function()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — middle_function, outer_function, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → outer_function()  # jumps into outer_function(), runs body, returns
#   → x = ...  # stored in memory
#   → middle_function()  # jumps into middle_function(), runs body, returns
#   → x = ...  # stored in memory
#   → middle_function()  # jumps into middle_function(), runs body, returns
"""
Problème 1174 :
Écrivez un programme Python à demonstrate variable accessibility dans different levels de imbriqué fonctions. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = f'Global x inside function: {x}'
#   → return result
#   → result = f'Global x inside function: {x}'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_global_variable is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print_global_variable(x)  # jumps into print_global_variable(), runs body, returns
#   → x = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_global_variable(...))  # print_global_variable() runs first → value goes to print()
"""
Problème 1175 :
Écrivez un programme Python à demonstrate comment fonction arguments peut access global variables. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: * - ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 1
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only factorial_recursive is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(factorial_recursive(...))  # factorial_recursive() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1176 :
Écrivez un programme Python à demonstrate le scope de variables dans un recursive fonction. Scope.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return math.sqrt(number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_square_root is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_square_root(...))  # calculate_square_root() runs first → value goes to print()
"""
Problème 1177 :
Écrivez un programme Python à import le `math` module et calculate le carré root de un nombre. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only generate_random_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → random_number = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1178 :
Écrivez un programme Python à Utilisez le `random` module à générer un nombre aléatoire entre 1 et 100. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only current_datetime is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → now = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1179 :
Écrivez un programme Python à Utilisez le `datetime` module à get le actuel date et time. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only current_working_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → cwd = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1180 :
Écrivez un programme Python à Utilisez le `os` module à get le répertoire de travail actuel. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only python_version is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → version = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1181 :
Écrivez un programme Python à Utilisez le `sys` module à get le Python version. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1182 :
Écrivez un programme Python à Utilisez le `json` module à convert un Python dictionnaire à un JSON chaîne. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return bool(re.search('\\d', string))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only contains_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → string = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(contains_number(...))  # contains_number() runs first → value goes to print()
"""
Problème 1183 :
Écrivez un programme Python à Utilisez le `re` module à vérifier si une chaîne contient un nombre. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return os.listdir(directory)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only list_files is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → files = ...  # stored in memory
#   → print(?)
#   → directory = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(list_files(...))  # list_files() runs first → value goes to print()
"""
Problème 1184 :
Écrivez un programme Python à Utilisez le `os` module à list tous files dans un répertoire. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return math.factorial(number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_factorial is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → factorial_result = ...  # stored in memory
#   → print(?)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_factorial(...))  # calculate_factorial() runs first → value goes to print()
"""
Problème 1185 :
Écrivez un programme Python à Utilisez le `math` module à calculate la factorielle de un nombre. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + -
# Fonctions intégrées : range, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only measure_time is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → execution_time = ...  # stored in memory
#   → print(?)
#   → start_time = ...  # stored in memory
#   → sum(range(...))
#   → end_time = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → start_time = ...  # stored in memory
#   → sum(range(...))
#   → end_time = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1186 :
Écrivez un programme Python à Utilisez le `time` module à measure le execution fois de un block de code. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return calendar.isleap(year)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_leap_year is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → year = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_leap_year(...))  # is_leap_year() runs first → value goes to print()
"""
Problème 1187 :
Écrivez un programme Python à Utilisez le `calendar` module à vérifier si un year est une année bissextile. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_pi_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → pi_value = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1188 :
Écrivez un programme Python à Utilisez le `math` module à trouver le valeur de pi. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return lst
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only shuffle_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → shuffled_numbers = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(shuffle_list(...))  # shuffle_list() runs first → value goes to print()
"""
Problème 1189 :
Écrivez un programme Python à Utilisez le `random` module à shuffle une liste de nombres. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return collections.Counter(lst)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_occurrences is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → occurrences = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_occurrences(...))  # count_occurrences() runs first → value goes to print()
"""
Problème 1190 :
Écrivez un programme Python à Utilisez le `collections` module à compter le occurrences d'éléments dans une liste. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return math.gcd(a, b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_gcd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → gcd_result = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_gcd(...))  # calculate_gcd() runs first → value goes to print()
"""
Problem 1191:
def calculate_gcd(a, b):  # defines a function with parameters a, b
    return math.gcd(a, b)  # return sends the result back to the caller
calculate_gcd(48, 180)  # arguments evaluated first, then function body runs
calculate_gcd(a, b)  # arguments evaluated first, then function body runs
print(calculate_gcd(20, 30))  # calculate_gcd() runs first, then print() outputs the result
import math
def calculate_gcd(a, b):  # stored in memory, not executed yet
gcd_result = ...  # assigns a value to the variable
print(?)
def calculate_gcd(a, b):  # stored in memory, not executed yet
def calculate_gcd(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_gcd(...))  # calculate_gcd() runs with a=20, b=30


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → os.rename(old_name, new_name)
#   → os.rename(old_name, new_name)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — rename_file, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → rename_file('old_file.txt', 'new_file.txt')  # jumps into rename_file(), runs body, returns
#   → old_name = ...  # stored in memory
#   → new_name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(rename_file(...))  # rename_file() runs first → value goes to print()
"""
Problem 1192:
def rename_file(old_name, new_name):  # defines a function with parameters old_name, new_name
    os.rename(old_name, new_name)
rename_file('old_file.txt', 'new_file.txt')  # arguments evaluated first, then function body runs
rename_file(old_name, new_name)  # arguments evaluated first, then function body runs
print(rename_file('world', 'world'))  # rename_file() runs first, then print() outputs the result
import os
def rename_file(old_name, new_name):  # stored in memory, not executed yet
rename_file('old_file.txt', 'new_file.txt')  # calls rename_file with old_name='old_file.txt', new_name='new_file.txt'
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
old_name = ...  # assigns a value to the variable
new_name = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(rename_file(...))  # rename_file() runs with old_name='world', new_name='world'


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_current_date is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → formatted_date = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1193 :
Écrivez un programme Python à Utilisez le `datetime` module à format le actuel date. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1194 :
Écrivez un programme Python à Utilisez le `json` module à parse un JSON chaîne dans un Python dictionnaire. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return random.choice(lst)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only select_random_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → elements = ...  # stored in memory
#   → random_element = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(select_random_element(...))  # select_random_element() runs first → value goes to print()
"""
Problème 1195 :
Écrivez un programme Python à Utilisez le `random` module à select un aléatoire élément de une liste. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return round(number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only round_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → rounded_value = ...  # stored in memory
#   → print(?)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(round_number(...))  # round_number() runs first → value goes to print()
"""
Problème 1196 :
Écrivez un programme Python à Utilisez le `math` module à round un floating-point nombre à le nearest integer. module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only access_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1197 :
Écrivez un programme Python à créer un dictionnaire et access ses valeur en utilisant une clé. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_key_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → updated_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1198 :
Écrivez un programme Python à add une paire clé-valeur à an existing dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_key is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → updated_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1199 :
Écrivez un programme Python à remove une paire clé-valeur de un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_keys is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → keys = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1200 :
Écrivez un programme Python à get tous le clés de un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → values = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1201 :
Écrivez un programme Python à get tous le valeurs de un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_key is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1202 :
Écrivez un programme Python à vérifier si une clé existe dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return dictionary.get(key, default)
#   → return dictionary.get(key, default)
#   → return dictionary.get(key, default)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — get_value, get_value_or_default are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_dict = ...  # stored in memory
#   → print(get_value(...))  # get_value() runs first → value goes to print()
#   → print(get_value(...))  # get_value() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(?)
#   → dictionary = ...  # stored in memory
#   → key = ...  # stored in memory
#   → default = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_value(...))  # get_value() runs first → value goes to print()
"""
Problème 1203 :
Écrivez un programme Python à get le valeur associated avec une clé, ou retourner une valeur par défaut si la clé doesn't exist. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only clear_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → updated_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → sample_dict.clear()
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → sample_dict.clear()
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1204 :
Écrivez un programme Python à clear tous éléments de un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only copy_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → copied_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1205 :
Écrivez un programme Python à copy un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_dicts is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → merged_dict = ...  # stored in memory
#   → print(?)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → result = ...  # stored in memory
#   → print(result)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1206 :
Écrivez un programme Python à merge deux dictionnaires. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → sample_dict = {'name': 'Alice', 'age': 30, 'city': 'New York'}
#   → for key, value in sample_dict.items():
#   →     print(f'{key}: {value}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — iterate_dict, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → iterate_dict()  # jumps into iterate_dict(), runs body, returns
#   → sample_dict = ...  # stored in memory
#   → for (key, value) in sample_dict.items(...)  # runs body once per item
#   → sample_dict = ...  # stored in memory
#   → for (key, value) in sample_dict.items(...)  # runs body once per item
#
# Loop: takes one item from sample_dict.items(...), runs body, repeats until done
"""
Problème 1207 :
Écrivez un programme Python à iterate à travers tous les paires clé-valeur dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only dict_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → length = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#
# Loop: takes one item from sample_dict, runs body, repeats until done
"""
Problème 1208 :
Écrivez un programme Python à trouver la longueur de un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → reversed_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1209 :
Écrivez un programme Python à reverse le clés et valeurs dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : dict, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_dict_from_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → created_dict = ...  # stored in memory
#   → print(?)
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1210 :
Écrivez un programme Python à créer un dictionnaire des deux listes, l'une pour clés et l'une pour valeurs. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → total = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1211 :
Écrivez un programme Python à get le somme de tous valeurs dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → filtered_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1212 :
Écrivez un programme Python à filtrer un dictionnaire based on une condition. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only max_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → max_val = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1213 :
Écrivez un programme Python à trouver le maximum valeur dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_dict_empty is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1214 :
Écrivez un programme Python à vérifier si un dictionnaire est vide. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: !=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → updated_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1215 :
Écrivez un programme Python à remove tous occurrences de une valeur dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_key_by_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → key = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1216 :
Écrivez un programme Python à trouver la clé associated avec une donnée valeur dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_defaultdict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → default_dict = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1217 :
Écrivez un programme Python à créer un dictionnaire avec default valeurs en utilisant `defaultdict`. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only first_key_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → first_item = ...  # stored in memory
#   → print(?)
#   → sample_dict = ...  # stored in memory
#   → first_item = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_dict = ...  # stored in memory
#   → first_item = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1218 :
Écrivez un programme Python à get le premier la paire clé-valeur de un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_multiple_dicts is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → merged_dict = ...  # stored in memory
#   → print(?)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict3 = ...  # stored in memory
#   → merged_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict3 = ...  # stored in memory
#   → merged_dict = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1219 :
Écrivez un programme Python à merge multiple dictionnaires dans l'une. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only list_to_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → converted_dict = ...  # stored in memory
#   → print(?)
#   → sample_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → sample_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1220 :
Écrivez un programme Python à convert une liste de tuples dans un dictionnaire. dictionnaire operations.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_path is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → path = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1221 :
Écrivez un programme Python à créer un chemin objet en utilisant le `pathlib` module.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_path_exists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → exists = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1222 :
Écrivez un programme Python à vérifier si un chemin existe en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_if_file is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_file = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1223 :
Écrivez un programme Python à vérifier si un chemin est un fichier en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_if_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_directory = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1224 :
Écrivez un programme Python à vérifier si un chemin est un répertoire en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_file_name is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → file_name = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1225 :
Écrivez un programme Python à get le file nom de un chemin en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_parent_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → parent_directory = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1226 :
Écrivez un programme Python à get le parent directory de un chemin en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * /
# Fonctions intégrées : Fraction, Path, float()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only join_paths is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → joined_path = ...  # stored in memory
#   → print(?)
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1227 :
Écrivez un programme Python à join deux paths en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_file_extension is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → file_extension = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1228 :
Écrivez un programme Python à get le file extension de un chemin en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_file_size is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → file_size = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1229 :
Écrivez un programme Python à get le size de un fichier en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only rename_file is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → renamed_file = ...  # stored in memory
#   → print(?)
#   → old_path = ...  # stored in memory
#   → new_path = ...  # stored in memory
#   → old_path.rename(new_path)
#   → result = ...  # stored in memory
#   → print(result)
#   → old_path = ...  # stored in memory
#   → new_path = ...  # stored in memory
#   → old_path.rename(new_path)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1230 :
Écrivez un programme Python à rename un fichier en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → created_directory = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → p.mkdir()
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → p.mkdir()
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1231 :
Écrivez un programme Python à créer une nouvelle directory en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → removed_directory = ...  # stored in memory
#   → print(removed_directory)
#   → p = ...  # stored in memory
#   → p.rmdir()
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → p.rmdir()
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1232 :
Écrivez un programme Python à remove un répertoire en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_file is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → removed_file = ...  # stored in memory
#   → print(removed_file)
#   → p = ...  # stored in memory
#   → p.unlink()
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → p.unlink()
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1233 :
Écrivez un programme Python à remove un fichier en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_absolute_path is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → absolute_path = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1234 :
Écrivez un programme Python à get le absolute path de un fichier en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_if_symlink is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_symlink = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1235 :
Écrivez un programme Python à vérifier si un chemin est un symbolic link en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_symlink is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → symlink = ...  # stored in memory
#   → print(?)
#   → target = ...  # stored in memory
#   → symlink = ...  # stored in memory
#   → symlink.symlink_to(target)
#   → result = ...  # stored in memory
#   → print(result)
#   → target = ...  # stored in memory
#   → symlink = ...  # stored in memory
#   → symlink.symlink_to(target)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1236 :
Écrivez un programme Python à créer un symbolic link en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_directory_and_files is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → directory_files = ...  # stored in memory
#   → print(?)
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1237 :
Écrivez un programme Python à vérifier si un chemin est un répertoire et si it contient files en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_modified_time is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → last_modified_time = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → timestamp = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → timestamp = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1238 :
Écrivez un programme Python à get le dernier modified fois de un fichier en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_directory_name is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → directory_name = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1239 :
Écrivez un programme Python à get le directory nom de un chemin en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : Path, len, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_if_empty is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → is_empty = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1240 :
Écrivez un programme Python à vérifier si un chemin est vide (no files) en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only iterate_files is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → files_in_directory = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1241 :
Écrivez un programme Python à iterate over tous files dans un répertoire en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_parent_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → parent_directory = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1242 :
Écrivez un programme Python à trouver le path à le parent directory en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_file_extension is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → has_extension = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1243 :
Écrivez un programme Python à vérifier si un fichier un certain extension en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: /
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_human_readable_size is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → size = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → size_in_bytes = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → size_in_bytes = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1244 :
Écrivez un programme Python à get le file size en utilisant `pathlib` dans human-readable format.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only change_working_directory is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → changed_directory = ...  # stored in memory
#   → print(changed_directory)
#   → p = ...  # stored in memory
#   → os.chdir(p)
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → os.chdir(p)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1245 :
Écrivez un programme Python à change le répertoire de travail actuel en utilisant `pathlib`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < > and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1246 :
Écrivez un programme Python à vérifier si deux conditions sont vrai en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <= >= and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1247 :
Écrivez un programme Python à vérifier si un nombre est entre 10 et 20 (les deux inclusive) en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return True if number % 3 == 0 and number % 5 == 0 else False
#   → return True if number % 3 == 0 and number % 5 == 0 else False
#   → return True if number % 3 == 0 and number % 5 == 0 else False
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — check_divisibility, main are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → number = ...  # stored in memory
#   → main(number)  # jumps into main(), runs body, returns
#   → result = ...  # stored in memory
#   → print(?)
#   → number = ...  # stored in memory
#   → main(number)  # jumps into main(), runs body, returns
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(main(...))  # main() runs first → value goes to print()
"""
Problème 1249 :
Écrivez un programme Python à vérifier si un nombre est divisible by les deux 3 et 5 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1250 :
Écrivez un programme Python à vérifier si une liste est non vide et le premier élément est greater que 5 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % != == or and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_leap_year is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → year = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → year = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1251 :
Écrivez un programme Python à vérifier si un year est une année bissextile en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % > == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1252 :
Écrivez un programme Python à vérifier si un nombre est positif, même, et divisible by 4 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > == and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1253 :
Écrivez un programme Python à vérifier si une liste contient exactly trois éléments, et le premier élément est greater que 5 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_string_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1254 :
Écrivez un programme Python à vérifier si une chaîne est lowercase et ses longueur est greater que 3 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < > and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_range_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1255 :
Écrivez un programme Python à vérifier si un nombre est greater que 10 et less que 50 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_multiples is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1256 :
Écrivez un programme Python à vérifier si un nombre est les deux un multiple de 7 et 11 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_voting_eligibility is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → age = ...  # stored in memory
#   → is_citizen = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → age = ...  # stored in memory
#   → is_citizen = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1257 :
Écrivez un programme Python à vérifier si un personne est eligible à vote, meaning le age est greater que 18 et they sont un citizen, en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisibility_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1258 :
Écrivez un programme Python à vérifier si un nombre est divisible by 2, 3, et 5 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % > == and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_string_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1260 :
Écrivez un programme Python à vérifier si une chaîne est pas vide et ses longueur est an même nombre en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <= >= and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_range_inclusive is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1261 :
Écrivez un programme Python à vérifier si un nombre est dans le range de 20 à 100 (inclusive) en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisible_by_2_and_7 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1262 :
Écrivez un programme Python à vérifier si un nombre est divisible by les deux 2 et 7 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % < == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_negative_and_divisible is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1263 :
Écrivez un programme Python à vérifier si un nombre est négatif et divisible by 5 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_digit_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1264 :
Écrivez un programme Python à vérifier si une chaîne contient seulement les chiffres et ses longueur est greater que 5 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % > != and
# Fonctions intégrées : all, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_prime_and_greater_than_10 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1265 :
Écrivez un programme Python à vérifier si un nombre est un nombre premier et greater que 10 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % > != and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_odd_and_positive is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1266 :
Écrivez un programme Python à vérifier si un nombre est impair et greater que 0 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * > and
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_file_exists_and_size is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1267 :
Écrivez un programme Python à vérifier si un fichier existe et ses size est greater que 100 KB en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > == and
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list_length_and_last_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1268 :
Écrivez un programme Python à vérifier si une liste contient exactly 5 éléments et le dernier élément est greater que 10 en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % != <= >= and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_range_and_odd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1269 :
Écrivez un programme Python à vérifier si un nombre est entre 1 et 10 (inclusive) et est impair en utilisant le `and` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < > or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1270 :
Écrivez un programme Python à vérifier si at moins l'une des deux conditions est vrai en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < > or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1271 :
Écrivez un programme Python à vérifier si un nombre est either less que 10 ou greater que 20 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisibility is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1273 :
Écrivez un programme Python à vérifier si un nombre est divisible by 3 ou 5 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > == or
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1274 :
Écrivez un programme Python à vérifier si une liste est vide ou ses premier élément est greater que 5 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % != == or and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_leap_year is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → year = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → year = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1275 :
Écrivez un programme Python à vérifier si un year est une année bissextile ou divisible by 100 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % > == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1276 :
Écrivez un programme Python à vérifier si un nombre est positif ou divisible by 7 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > or
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_string_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1277 :
Écrivez un programme Python à vérifier si une chaîne est lowercase ou ses longueur est greater que 3 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % <= == >= or and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1278 :
Écrivez un programme Python à vérifier si un nombre est entre 10 et 50 ou divisible by 7 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_multiples is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1279 :
Écrivez un programme Python à vérifier si un nombre est un multiple de 7 ou 11 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_voting_eligibility is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → age = ...  # stored in memory
#   → is_citizen = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → age = ...  # stored in memory
#   → is_citizen = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1280 :
Écrivez un programme Python à vérifier si un personne est eligible à vote, meaning le age est greater que 18 ou they sont un citizen, en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisibility_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1281 :
Écrivez un programme Python à vérifier si un nombre est divisible by 2 ou 3 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: == or
# Fonctions intégrées : any, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_string_empty_or_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1283 :
Écrivez un programme Python à vérifier si une chaîne est vide ou contient un nombre en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % < == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_negative_or_divisible_by_3 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1284 :
Écrivez un programme Python à vérifier si un nombre est either négatif ou divisible by 3 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > == or
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1285 :
Écrivez un programme Python à vérifier si une liste est vide ou contient plus que 5 éléments en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: / ** == or
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_perfect_square_or_cube is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1286 :
Écrivez un programme Python à vérifier si un nombre est un perfect carré ou un perfect cube en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_string_type is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1287 :
Écrivez un programme Python à vérifier si une chaîne contient seulement lettre ou seulement les chiffres en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > == or
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1288 :
Écrivez un programme Python à vérifier si une liste contient exactly 3 éléments ou ses somme est greater que 50 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisible_by_2_or_5 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1289 :
Écrivez un programme Python à vérifier si un nombre est divisible by 2 ou 5 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: or
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_file_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → p = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1290 :
Écrivez un programme Python à vérifier si un fichier existe ou si c'est writable en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < > or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1291 :
Écrivez un programme Python à vérifier si un nombre est greater que 100 ou less que 10 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_string_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1292 :
Écrivez un programme Python à vérifier si une chaîne est uppercase ou ses longueur est pair en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: > or
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_list_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1293 :
Écrivez un programme Python à vérifier si une liste est non vide ou ses dernier élément est greater que 10 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_even_or_divisible_by_3 is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(?)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → num = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1294 :
Écrivez un programme Python à vérifier si un nombre est pair ou divisible by 3 en utilisant le `or` operator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(words)
#   → s = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(words)
#   → s = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(words)
"""
Problème 1296 :
Écrivez un programme Python à extraire tous words starting avec un capital lettre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → pass
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)
#   → main()  # jumps into main(), runs body, returns
"""
Problème 1297 :
Écrivez un programme Python à split une chaîne by commas et optional les espaces.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: and
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → password = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → password = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → password = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1298 :
Écrivez un programme Python à vérifier si un password contient at moins l'une chiffre, l'une uppercase, et l'une lowercase lettre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → emails = ...  # stored in memory
#   → print(emails)
#   → text = ...  # stored in memory
#   → emails = ...  # stored in memory
#   → print(emails)
#   → text = ...  # stored in memory
#   → emails = ...  # stored in memory
#   → print(emails)
"""
Problème 1299 :
Écrivez un programme Python à trouver tous email addresses dans un text.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → tweet = ...  # stored in memory
#   → hashtags = ...  # stored in memory
#   → print(hashtags)
#   → tweet = ...  # stored in memory
#   → hashtags = ...  # stored in memory
#   → print(hashtags)
#   → tweet = ...  # stored in memory
#   → hashtags = ...  # stored in memory
#   → print(hashtags)
"""
Problème 1300 :
Écrivez un programme Python à extraire hashtags de un tweet.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → domains = ...  # stored in memory
#   → print(domains)
#   → text = ...  # stored in memory
#   → domains = ...  # stored in memory
#   → print(domains)
#   → text = ...  # stored in memory
#   → domains = ...  # stored in memory
#   → print(domains)
"""
Problème 1301 :
Écrivez un programme Python à extraire domain noms de URLs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → ip = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → ip = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → ip = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1302 :
Écrivez un programme Python à validate si une chaîne est un valid IPv4 address.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → clean = ...  # stored in memory
#   → print(clean)
#   → text = ...  # stored in memory
#   → clean = ...  # stored in memory
#   → print(clean)
#   → text = ...  # stored in memory
#   → clean = ...  # stored in memory
#   → print(clean)
"""
Problème 1303 :
Écrivez un programme Python à remove tous punctuation de une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → s = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → s = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1304 :
Écrivez un programme Python à match une chaîne qui commence par une voyelle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → dates = ...  # stored in memory
#   → print(dates)
#   → text = ...  # stored in memory
#   → dates = ...  # stored in memory
#   → print(dates)
#   → text = ...  # stored in memory
#   → dates = ...  # stored in memory
#   → print(dates)
"""
Problème 1305 :
Écrivez un programme Python à extraire tous valid dates dans le format YYYY-MM-DD.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → color = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → color = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → color = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1306 :
Écrivez un programme Python à validate un hexadecimal color code.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(words)
#   → text = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(words)
#   → text = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(words)
"""
Problème 1307 :
Écrivez un programme Python à extraire tous words qui end avec'ing'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → filename = ...  # stored in memory
#   → ext = ...  # stored in memory
#   → print(ext)
#   → filename = ...  # stored in memory
#   → ext = ...  # stored in memory
#   → print(ext)
#   → filename = ...  # stored in memory
#   → ext = ...  # stored in memory
#   → print(ext)
"""
Problème 1308 :
Écrivez un programme Python à extraire le file extension de un filename.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → duplicates = ...  # stored in memory
#   → print(duplicates)
#   → text = ...  # stored in memory
#   → duplicates = ...  # stored in memory
#   → print(duplicates)
#   → text = ...  # stored in memory
#   → duplicates = ...  # stored in memory
#   → print(duplicates)
"""
Problème 1309 :
Écrivez un programme Python à trouver duplicated words dans une phrase.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → capitalized = ...  # stored in memory
#   → print(capitalized)
#   → text = ...  # stored in memory
#   → capitalized = ...  # stored in memory
#   → print(capitalized)
#   → text = ...  # stored in memory
#   → capitalized = ...  # stored in memory
#   → print(capitalized)
"""
Problème 1310 :
Écrivez un programme Python à extraire tous capitalized words de un paragraphe.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → time = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → time = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → time = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1311 :
Écrivez un programme Python à validate un fois dans 24-hour format (HH:MM).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → print(numbers)
#   → text = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → print(numbers)
#   → text = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → print(numbers)
"""
Problème 1312 :
Écrivez un programme Python à extraire tous integers et floats de une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → pass
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → ip = ...  # stored in memory
#   → clean_ip = ...  # stored in memory
#   → print(clean_ip)
#   → main()  # jumps into main(), runs body, returns
"""
Problème 1313 :
Écrivez un programme Python à remove leading zeros de an IP address.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → slug = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → slug = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → slug = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1314 :
Écrivez un programme Python à vérifier si une chaîne est un valid slug (seulement lowercase lettre, nombres, hyphens).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → content = ...  # stored in memory
#   → print(content)
#   → text = ...  # stored in memory
#   → content = ...  # stored in memory
#   → print(content)
#   → text = ...  # stored in memory
#   → content = ...  # stored in memory
#   → print(content)
"""
Problème 1315 :
Écrivez un programme Python à extraire content à l'intérieur de parentheses.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → html = ...  # stored in memory
#   → tags = ...  # stored in memory
#   → print(tags)
#   → html = ...  # stored in memory
#   → tags = ...  # stored in memory
#   → print(tags)
#   → html = ...  # stored in memory
#   → tags = ...  # stored in memory
#   → print(tags)
"""
Problème 1316 :
Écrivez un programme Python à match tous valid HTML tags dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → zip_code = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → zip_code = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → zip_code = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1317 :
Écrivez un programme Python à validate si une chaîne est un US ZIP code (5 les chiffres).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → abbreviations = ...  # stored in memory
#   → print(abbreviations)
#   → text = ...  # stored in memory
#   → abbreviations = ...  # stored in memory
#   → print(abbreviations)
#   → text = ...  # stored in memory
#   → abbreviations = ...  # stored in memory
#   → print(abbreviations)
"""
Problème 1318 :
Écrivez un programme Python à trouver tous abbreviations comme "U.S.A." dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → count = ...  # stored in memory
#   → print(count)
#   → text = ...  # stored in memory
#   → count = ...  # stored in memory
#   → print(count)
#   → text = ...  # stored in memory
#   → count = ...  # stored in memory
#   → print(count)
"""
Problème 1319 :
Écrivez un programme Python à compter combien de fois un mot apparaît dans un cas-insensitive façon.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → clean = ...  # stored in memory
#   → print(clean)
#   → text = ...  # stored in memory
#   → clean = ...  # stored in memory
#   → print(clean)
#   → text = ...  # stored in memory
#   → clean = ...  # stored in memory
#   → print(clean)
"""
Problème 1320 :
Écrivez un programme Python à replace en double spaces avec un seul space.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → mac = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → mac = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → mac = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1321 :
Écrivez un programme Python à validate un MAC address (e.g., 00:1A:2B:3C:4D:5E).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → email = ...  # stored in memory
#   → username = ...  # stored in memory
#   → print(username)
#   → email = ...  # stored in memory
#   → username = ...  # stored in memory
#   → print(username)
#   → email = ...  # stored in memory
#   → username = ...  # stored in memory
#   → print(username)
"""
Problème 1322 :
Écrivez un programme Python à extraire le username de an email address.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → num = ...  # stored in memory
#   → formatted = ...  # stored in memory
#   → print(formatted)
#   → num = ...  # stored in memory
#   → formatted = ...  # stored in memory
#   → print(formatted)
#   → num = ...  # stored in memory
#   → formatted = ...  # stored in memory
#   → print(formatted)
"""
Problème 1323 :
Écrivez un programme Python à insert commas dans un numeric chaîne chaque trois les chiffres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → handles = ...  # stored in memory
#   → print(handles)
#   → text = ...  # stored in memory
#   → handles = ...  # stored in memory
#   → print(handles)
#   → text = ...  # stored in memory
#   → handles = ...  # stored in memory
#   → print(handles)
"""
Problème 1324 :
Écrivez un programme Python à extraire tous valid Twitter gère de une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → emoji_text = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → emoji_text = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → emoji_text = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1325 :
Écrivez un programme Python à vérifier si une chaîne contient un emoji.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → times = ...  # stored in memory
#   → print(times)
#   → text = ...  # stored in memory
#   → times = ...  # stored in memory
#   → print(times)
#   → text = ...  # stored in memory
#   → times = ...  # stored in memory
#   → print(times)
"""
Problème 1326 :
Écrivez un programme Python à extraire fois valeurs dans 12-hour format avec AM/PM.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → prices = ...  # stored in memory
#   → print(prices)
#   → text = ...  # stored in memory
#   → prices = ...  # stored in memory
#   → print(prices)
#   → text = ...  # stored in memory
#   → prices = ...  # stored in memory
#   → print(prices)
"""
Problème 1327 :
Écrivez un programme Python à extraire prices avec un dollar sign.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → duplicates = ...  # stored in memory
#   → print(set(...))
"""
Problème 1328 :
Écrivez un programme Python à detect en double caractères dans une chaîne en utilisant regex.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → hex_num = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → hex_num = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
#   → hex_num = ...  # stored in memory
#   → match = ...  # stored in memory
#   → print(match)
"""
Problème 1329 :
Écrivez un programme Python à validate un hexadecimal nombre (e.g., 0x1A3F).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1330 :
Écrivez un programme Python à extraire quoted strings (simples ou doubles guillemets).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1331 :
Écrivez un programme Python à vérifier si une variable est un entier en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1332 :
Écrivez un programme Python à vérifier si une variable est une chaîne en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1333 :
Écrivez un programme Python à vérifier si une variable est un flottant en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1334 :
Écrivez un programme Python à vérifier si une variable est une liste en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1335 :
Écrivez un programme Python à vérifier si une variable est un dictionnaire en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1336 :
Écrivez un programme Python à vérifier si une variable est either int ou float en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1337 :
Écrivez un programme Python à vérifier si chaque élément dans une liste est une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → ints = ...  # stored in memory
#   → print(ints)
#   → items = ...  # stored in memory
#   → ints = ...  # stored in memory
#   → print(ints)
#   → items = ...  # stored in memory
#   → ints = ...  # stored in memory
#   → print(ints)
"""
Problème 1338 :
Écrivez un programme Python à filtrer seulement integers de un mixte list en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → strings = ...  # stored in memory
#   → print(strings)
#   → items = ...  # stored in memory
#   → strings = ...  # stored in memory
#   → print(strings)
#   → items = ...  # stored in memory
#   → strings = ...  # stored in memory
#   → print(strings)
"""
Problème 1339 :
Écrivez un programme Python à filtrer seulement strings de un mixte list en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1340 :
Écrivez un programme Python à vérifier si un objet est un booléen en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1341 :
Écrivez un programme Python à vérifier si une variable est un tuple en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1342 :
Écrivez un programme Python à vérifier si un objet est pas une chaîne en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Invalid input'
#   → return 'Invalid input'
#   → return 'Invalid input'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only greet is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(greet(...))  # greet() runs first → value goes to print()
#   → print(greet(...))  # greet() runs first → value goes to print()
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greet(...))  # greet() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1343 :
Écrivez un programme Python à define une fonction qui accepts seulement strings en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → nested = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → nested = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → nested = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1344 :
Écrivez un programme Python à vérifier si tous éléments dans une liste imbriquée sont listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → mixed = ...  # stored in memory
#   → nums = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(nums)
#   → print(words)
#   → mixed = ...  # stored in memory
#   → nums = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(nums)
#   → print(words)
#   → mixed = ...  # stored in memory
#   → nums = ...  # stored in memory
#   → words = ...  # stored in memory
#   → print(nums)
#   → print(words)
"""
Problème 1345 :
Écrivez un programme Python à separate strings et nombres de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: *
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Invalid input'
#   → return 'Invalid input'
#   → return 'Invalid input'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(multiply(...))  # multiply() runs first → value goes to print()
#   → print(multiply(...))  # multiply() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply(...))  # multiply() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1346 :
Écrivez un programme Python à validate argument tapez dans une fonction.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(isinstance(...))
"""
Problème 1347 :
Écrivez un programme Python à vérifier si une variable est un ensemble en utilisant isinstance().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1348 :
Écrivez un programme Python à validate qui dictionnaire clés sont tous strings.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1349 :
Écrivez un programme Python à validate qui dictionnaire valeurs sont tous integers.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: and
# Fonctions intégrées : FakeFile, callable, hasattr()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes FakeFile are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → f = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(FakeFile(...))
"""
Problème 1350 :
Écrivez un programme Python à vérifier si un objet est un fichier-comme objet (un read méthode).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → d.pop('b')
#   → print(d)
#   → d = ...  # stored in memory
#   → d.pop('b')
#   → print(d)
#   → d = ...  # stored in memory
#   → d.pop('b')
#   → print(d)
"""
Problème 1351 :
Écrivez un programme Python à remove une clé de un dictionnaire en utilisant pop() et print le updated dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → value = ...  # stored in memory
#   → print(value)
#   → d = ...  # stored in memory
#   → value = ...  # stored in memory
#   → print(value)
#   → d = ...  # stored in memory
#   → value = ...  # stored in memory
#   → print(value)
"""
Problème 1352 :
Écrivez un programme Python à Utilisez pop() à retrieve le valeur de une clé de un dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → lst.pop()
#   → print(lst)
#   → lst = ...  # stored in memory
#   → lst.pop()
#   → print(lst)
#   → lst = ...  # stored in memory
#   → lst.pop()
#   → print(lst)
"""
Problème 1353 :
Écrivez un programme Python à Utilisez pop() on une liste à remove le dernier élément.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → lst.pop(1)
#   → print(lst)
#   → lst = ...  # stored in memory
#   → lst.pop(1)
#   → print(lst)
#   → lst = ...  # stored in memory
#   → lst.pop(1)
#   → print(lst)
"""
Problème 1354 :
Écrivez un programme Python à remove un spécifique index de une liste en utilisant pop().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → removed = ...  # stored in memory
#   → print(removed)
#   → lst = ...  # stored in memory
#   → removed = ...  # stored in memory
#   → print(removed)
#   → lst = ...  # stored in memory
#   → removed = ...  # stored in memory
#   → print(removed)
"""
Problème 1355 :
Écrivez un programme Python à capture et print le élément removed en utilisant pop() de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: while
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#
# Loop: check condition → if True run body → check again
"""
Problème 1356 :
Écrivez un programme Python à remove tous éléments de une liste en utilisant pop() dans une boucle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → lst = ...  # stored in memory
#   → lst = ...  # stored in memory
"""
Problème 1357 :
Écrivez un programme Python à handle IndexError lorsque popping de an liste vide.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → val = ...  # stored in memory
#   → print(val)
#   → d = ...  # stored in memory
#   → val = ...  # stored in memory
#   → print(val)
#   → d = ...  # stored in memory
#   → val = ...  # stored in memory
#   → print(val)
"""
Problème 1358 :
Écrivez un programme Python à Utilisez pop() avec dictionnaires et provide un default retourner valeur si la clé doesn't exist.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → d = ...  # stored in memory
#   → d = ...  # stored in memory
"""
Problème 1359 :
Écrivez un programme Python à safely remove une clé de un dictionnaire en utilisant pop() à l'intérieur de un try block.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: while
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#
# Loop: check condition → if True run body → check again
"""
Problème 1360 :
Écrivez un programme Python à remove clés l'une by l'une en utilisant pop() jusqu'à le dictionnaire est vide.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → stack = ...  # stored in memory
#   → print(stack.pop(...))
"""
Problème 1361 :
Écrivez un programme Python à pop éléments de un stack represented by une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → queue = ...  # stored in memory
#   → print(queue.pop(...))
"""
Problème 1362 :
Écrivez un programme Python à simulate queue dequeue en utilisant pop(0).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → last = ...  # stored in memory
#   → print(last)
#   → numbers = ...  # stored in memory
#   → last = ...  # stored in memory
#   → print(last)
#   → numbers = ...  # stored in memory
#   → last = ...  # stored in memory
#   → print(last)
"""
Problème 1363 :
Écrivez un programme Python à remove le dernier élément de une liste en utilisant pop() et store it.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → print(d.pop(...))
"""
Problème 1364 :
Écrivez un programme Python à demonstrate le Utilisez de pop() on une chaîne clé dans un dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → for _ in range(...)  # runs body once per item
#   → print(lst)
#   → lst = ...  # stored in memory
#   → for _ in range(...)  # runs body once per item
#   → print(lst)
#   → lst = ...  # stored in memory
#   → for _ in range(...)  # runs body once per item
#   → print(lst)
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1365 :
Écrivez un programme Python à pop multiple éléments de le end de une liste en utilisant une boucle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → sum_last_two = ...  # stored in memory
#   → print(sum_last_two)
#   → lst = ...  # stored in memory
#   → sum_last_two = ...  # stored in memory
#   → print(sum_last_two)
#   → lst = ...  # stored in memory
#   → sum_last_two = ...  # stored in memory
#   → print(sum_last_two)
"""
Problème 1366 :
Écrivez un programme Python à pop et somme le dernier deux nombres de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return d.pop(key, None)
#   → return d.pop(key, None)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_key is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(remove_key(...))  # remove_key() runs first → value goes to print()
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_key(...))  # remove_key() runs first → value goes to print()
"""
Problème 1367 :
Écrivez un programme Python à Utilisez pop() dans une fonction qui supprime une clé de une donnée dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → src = ...  # stored in memory
#   → dst = ...  # stored in memory
#   → dst.append(src.pop(...))
#   → print(dst)
#   → src = ...  # stored in memory
#   → dst = ...  # stored in memory
#   → dst.append(src.pop(...))
#   → print(dst)
#   → src = ...  # stored in memory
#   → dst = ...  # stored in memory
#   → dst.append(src.pop(...))
#   → print(dst)
"""
Problème 1368 :
Écrivez un programme Python à pop un élément de une liste et append it à un autre list.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → actions = ...  # stored in memory
#   → undo = ...  # stored in memory
#   → print(undo)
#   → actions = ...  # stored in memory
#   → undo = ...  # stored in memory
#   → print(undo)
#   → actions = ...  # stored in memory
#   → undo = ...  # stored in memory
#   → print(undo)
"""
Problème 1369 :
Écrivez un programme Python à implement un basic undo feature en utilisant pop() on une liste de actions.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → nested = ...  # stored in memory
#   → last_sublist = ...  # stored in memory
#   → print(last_sublist)
#   → nested = ...  # stored in memory
#   → last_sublist = ...  # stored in memory
#   → print(last_sublist)
#   → nested = ...  # stored in memory
#   → last_sublist = ...  # stored in memory
#   → print(last_sublist)
"""
Problème 1370 :
Écrivez un programme Python à pop éléments de une liste imbriquée.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = kwargs
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_kwargs()  # jumps into print_kwargs(), runs body, returns
#   → print(kwargs)
#   → print(kwargs)
"""
Problème 1371 :
Écrivez un programme Python avec une fonction qui accepts arbitrary keyword arguments et affiche them.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for key, value in kwargs.items():
#   →     print(f'{key}: {value}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — display_kwargs, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → display_kwargs()  # jumps into display_kwargs(), runs body, returns
#   → for (key, value) in kwargs.items(...)  # runs body once per item
#   → for (key, value) in kwargs.items(...)  # runs body once per item
#
# Loop: takes one item from kwargs.items(...), runs body, repeats until done
"""
Problème 1372 :
Write un Python fonction qui affiche chaque la paire clé-valeur dans **kwargs on un separate line.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = len(kwargs)
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → count_kwargs()  # jumps into count_kwargs(), runs body, returns
#   → print(len(...))
#   → print(len(...))
"""
Problème 1373 :
Écrivez un programme Python à compter le nombre de keyword arguments passed à une fonction.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(kwargs.keys())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only kwargs_keys is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(kwargs_keys(...))  # kwargs_keys() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1374 :
Write un Python fonction qui retourne le clés de **kwargs as une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(kwargs.values())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only kwargs_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(kwargs_values(...))  # kwargs_values() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1375 :
Write un Python fonction qui retourne le valeurs de **kwargs as une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'id' in kwargs
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only has_id is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(has_id(...))  # has_id() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1376 :
Write un Python fonction qui retourne vrai si'id'est dans **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return kwargs.get('name', 'Unknown')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_name is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_name(...))  # get_name() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1377 :
Write un Python fonction qui retourne le valeur de un argument nommé'nom'si it existe, else retourner'Unknown'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((v for v in kwargs.values() if isinstance(v, (int, float))))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(sum_kwargs(...))  # sum_kwargs() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1378 :
Write un Python fonction qui sums tous numeric valeurs dans **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {k: v for k, v in kwargs.items() if isinstance(v, str)}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only string_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(string_kwargs(...))  # string_kwargs() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1379 :
Write un Python fonction qui filtre et retourne keyword arguments où valeurs sont strings.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = f"Hello {kwargs.get('name', 'Guest')}, age {kwargs.get('age', 'N/A')}"
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only greet is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → greet()  # jumps into greet(), runs body, returns
#   → print(?)
#   → print(?)
"""
Problème 1380 :
Write un Python fonction qui affiche un formatted chaîne de **kwargs en utilisant nom et age clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return base
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only update_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(update_dict(...))  # update_dict() runs first → value goes to print()
#   → base.update(kwargs)
#   → result = ...  # stored in memory
#   → print(result)
#   → base.update(kwargs)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1381 :
Write un Python fonction qui met à jour un dictionnaire avec keyword arguments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return merged
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_dicts is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(merge_dicts(...))  # merge_dicts() runs first → value goes to print()
#   → merged = ...  # stored in memory
#   → merged.update(kwargs)
#   → result = ...  # stored in memory
#   → print(result)
#   → merged = ...  # stored in memory
#   → merged.update(kwargs)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1382 :
Write un Python fonction qui fusionne deux dictionnaires en utilisant **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % == and
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for k, v in kwargs.items():
#   →     if isinstance(v, int) and v % 2 == 0:
#   →         print(f'{k}: {v}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — even_kwargs, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → even_kwargs()  # jumps into even_kwargs(), runs body, returns
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#
# Loop: takes one item from kwargs.items(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1383 :
Write un Python fonction qui affiche seulement le keyword arguments avec même integer valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for k in sorted(kwargs.keys()):
#   →     print(f'{k}: {kwargs[k]}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, sorted_kwargs are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → sorted_kwargs()  # jumps into sorted_kwargs(), runs body, returns
#   → for k in sorted(...)  # runs body once per item
#   → for k in sorted(...)  # runs body once per item
#
# Loop: takes one item from sorted(...), runs body, repeats until done
"""
Problème 1384 :
Write un Python fonction qui accepts un nombre de keyword arguments et affiche them trié by clé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = sum((1 for v in kwargs.values() if isinstance(v, str)))
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_strings is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → count_strings()  # jumps into count_strings(), runs body, returns
#   → print(sum(...))
#   → print(sum(...))
"""
Problème 1385 :
Write un Python fonction qui affiche le nombre de chaîne valeurs dans **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for k, v in kwargs.items():
#   →     print(f"{k}: {('Truthy' if v else 'Falsy')}")
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — check_truthy, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → check_truthy()  # jumps into check_truthy(), runs body, returns
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#
# Loop: takes one item from kwargs.items(...), runs body, repeats until done
"""
Problème 1386 :
Write un Python fonction qui affiche si chaque valeur dans **kwargs est truthy ou falsy.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for k, v in kwargs.items():
#   →     if k.startswith('a'):
#   →         print(f'{k}: {v}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, starts_with_a are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → starts_with_a()  # jumps into starts_with_a(), runs body, returns
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#
# Loop: takes one item from kwargs.items(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1387 :
Write un Python fonction qui affiche tous les paires clé-valeur dans **kwargs où la clé commence par'a'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {v: k for k, v in kwargs.items()}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(reverse_kwargs(...))  # reverse_kwargs() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1388 :
Write un Python fonction qui retourne un inversé dictionnaire construit de **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(kwargs, key=len)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only longest_key is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(longest_key(...))  # longest_key() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1389 :
Write un Python fonction qui retourne le plus long clé dans **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ', '.join((f'{k}={v}' for k, v in kwargs.items()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only stringify_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(stringify_kwargs(...))  # stringify_kwargs() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1390 :
Write un Python fonction qui crée une chaîne de **kwargs as clé=valeur pairs joined by commas.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return all((isinstance(v, int) for v in kwargs.values()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only all_ints is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(all_ints(...))  # all_ints() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1391 :
Write un Python fonction qui vérifie si tous valeurs dans **kwargs sont integers.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance, max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(nums) if nums else None
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only max_numeric is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(max_numeric(...))  # max_numeric() runs first → value goes to print()
#   → nums = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → nums = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1392 :
Write un Python fonction qui trouve et retourne le maximum numeric valeur dans **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((1 for v in kwargs.values() if v is None))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_none is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(count_none(...))  # count_none() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1393 :
Write un Python fonction qui compte le keyword arguments whose valeur est None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for k, v in kwargs.items():
#   →     print(f'{k}: {type(v).__name__}')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_types is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_types()  # jumps into print_types(), runs body, returns
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#   → for (k, v) in kwargs.items(...)  # runs body once per item
#
# Loop: takes one item from kwargs.items(...), runs body, repeats until done
"""
Problème 1394 :
Write un Python fonction qui affiche le tapez de valeurs dans **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(kwargs.items())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only kv_pairs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(kv_pairs(...))  # kv_pairs() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1395 :
Write un Python fonction qui crée une liste de les paires clé-valeur (tuples) de **kwargs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {k: v for k, v in kwargs.items() if v}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_falsy is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(remove_falsy(...))  # remove_falsy() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1396 :
Write un Python fonction qui filtre out keyword arguments avec falsy valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : any, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return any((isinstance(v, list) for v in kwargs.values()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only has_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(has_list(...))  # has_list() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1397 :
Write un Python fonction qui retourne vrai si at moins l'une valeur dans **kwargs est une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = 'Even' if len(kwargs) % 2 == 0 else 'Odd'
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only even_or_odd_kwargs is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → even_or_odd_kwargs()  # jumps into even_or_odd_kwargs(), runs body, returns
#   → print(?)
#   → print(?)
"""
Problème 1398 :
Write un Python fonction qui affiche si le nombre de keyword arguments est pair ou impair.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {k[5:] if k.startswith('temp_') else k: v for k, v in kwargs.items()}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only clean_temp_keys is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(clean_temp_keys(...))  # clean_temp_keys() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1399 :
Write un Python fonction qui accepts **kwargs et renames un clés qui start avec'temp_'by removing le prefix.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {k: v for k, v in kwargs.items() if k.isupper()}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only uppercase_keys_only is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(uppercase_keys_only(...))  # uppercase_keys_only() runs first → value goes to print()
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1400 :
Write un Python fonction qui accepts **kwargs et retourne seulement ceux les paires clé-valeur où la clé est uppercase.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : enumerate()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → fruits = ...  # stored in memory
#   → for (index, value) in enumerate(...)  # runs body once per item
#   → fruits = ...  # stored in memory
#   → for (index, value) in enumerate(...)  # runs body once per item
#   → fruits = ...  # stored in memory
#   → for (index, value) in enumerate(...)  # runs body once per item
#
# Loop: takes one item from enumerate(...), runs body, repeats until done
"""
Problème 1402 :
Write un Pythonic façon à Utilisez `enumerate()` à get les deux index et valeur de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → names = ...  # stored in memory
#   → ages = ...  # stored in memory
#   → combined = ...  # stored in memory
#   → print(combined)
#   → names = ...  # stored in memory
#   → ages = ...  # stored in memory
#   → combined = ...  # stored in memory
#   → print(combined)
#   → names = ...  # stored in memory
#   → ages = ...  # stored in memory
#   → combined = ...  # stored in memory
#   → print(combined)
"""
Problème 1403 :
Write un Pythonic façon à Utilisez `zip()` à combiner deux listes dans une liste de tuples.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → original = ...  # stored in memory
#   → swapped = ...  # stored in memory
#   → print(swapped)
#   → original = ...  # stored in memory
#   → swapped = ...  # stored in memory
#   → print(swapped)
#   → original = ...  # stored in memory
#   → swapped = ...  # stored in memory
#   → print(swapped)
"""
Problème 1404 :
Write un Pythonic façon à Utilisez un dictionnaire comprehension à swap clés et valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → all_even = ...  # stored in memory
#   → print(all_even)
#   → numbers = ...  # stored in memory
#   → all_even = ...  # stored in memory
#   → print(all_even)
#   → numbers = ...  # stored in memory
#   → all_even = ...  # stored in memory
#   → print(all_even)
"""
Problème 1405 :
Write un Pythonic façon à Utilisez `tous()` à vérifier si tous éléments dans une liste satisfy une condition.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → nested = ...  # stored in memory
#   → flattened = ...  # stored in memory
#   → print(flattened)
#   → nested = ...  # stored in memory
#   → flattened = ...  # stored in memory
#   → print(flattened)
#   → nested = ...  # stored in memory
#   → flattened = ...  # stored in memory
#   → print(flattened)
"""
Problème 1406 :
Write un Pythonic façon à flatten une liste de listes en utilisant une liste comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → uppercase_words = ...  # stored in memory
#   → print(uppercase_words)
#   → words = ...  # stored in memory
#   → uppercase_words = ...  # stored in memory
#   → print(uppercase_words)
#   → words = ...  # stored in memory
#   → uppercase_words = ...  # stored in memory
#   → print(uppercase_words)
"""
Problème 1407 :
Write un Pythonic façon à Utilisez `map()` à convert une liste de chaînes à uppercase.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : filter, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → even_numbers = ...  # stored in memory
#   → print(even_numbers)
#   → numbers = ...  # stored in memory
#   → even_numbers = ...  # stored in memory
#   → print(even_numbers)
#   → numbers = ...  # stored in memory
#   → even_numbers = ...  # stored in memory
#   → print(even_numbers)
"""
Problème 1408 :
Write un Pythonic façon à Utilisez `filtrer()` avec un lambda à get les nombres pairs de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → unique_ordered = ...  # stored in memory
#   → print(unique_ordered)
#   → items = ...  # stored in memory
#   → unique_ordered = ...  # stored in memory
#   → print(unique_ordered)
#   → items = ...  # stored in memory
#   → unique_ordered = ...  # stored in memory
#   → print(unique_ordered)
"""
Problème 1409 :
Write un Pythonic façon à Utilisez `set()` à remove les doublons de une liste tandis que preserving order en utilisant dict.fromkeys().
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → sentence = ...  # stored in memory
#   → print(sentence)
#   → words = ...  # stored in memory
#   → sentence = ...  # stored in memory
#   → print(sentence)
#   → words = ...  # stored in memory
#   → sentence = ...  # stored in memory
#   → print(sentence)
"""
Problème 1410 :
Write un Pythonic façon à Utilisez `str.join()` à concatenate une liste de chaînes avec un separator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → reversed_text = ...  # stored in memory
#   → print(reversed_text)
#   → text = ...  # stored in memory
#   → reversed_text = ...  # stored in memory
#   → print(reversed_text)
#   → text = ...  # stored in memory
#   → reversed_text = ...  # stored in memory
#   → print(reversed_text)
"""
Problème 1411 :
Write un Pythonic façon à Utilisez list slicing à reverse une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Counter()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → char_counts = ...  # stored in memory
#   → print(char_counts)
#   → text = ...  # stored in memory
#   → char_counts = ...  # stored in memory
#   → print(char_counts)
#   → text = ...  # stored in memory
#   → char_counts = ...  # stored in memory
#   → print(char_counts)
"""
Problème 1412 :
Write un Pythonic façon à Utilisez `collections.Counter` à count caractère fréquences dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : chain, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → combined = ...  # stored in memory
#   → print(combined)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → combined = ...  # stored in memory
#   → print(combined)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → combined = ...  # stored in memory
#   → print(combined)
"""
Problème 1413 :
Write un Pythonic façon à Utilisez `itertools.chain()` à combiner multiple listes dans l'une.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → capitalized = ...  # stored in memory
#   → print(capitalized)
#   → text = ...  # stored in memory
#   → capitalized = ...  # stored in memory
#   → print(capitalized)
#   → text = ...  # stored in memory
#   → capitalized = ...  # stored in memory
#   → print(capitalized)
"""
Problème 1414 :
Write un Pythonic façon à Utilisez `str.split()` et List comprehension à capitalize chaque mot.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → matrix = ...  # stored in memory
#   → transposed = ...  # stored in memory
#   → print(transposed)
#   → matrix = ...  # stored in memory
#   → transposed = ...  # stored in memory
#   → print(transposed)
#   → matrix = ...  # stored in memory
#   → transposed = ...  # stored in memory
#   → print(transposed)
"""
Problème 1415 :
Write un Pythonic façon à Utilisez `zip()` avec `*` à transpose une liste de listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
"""
Problème 1416 :
Write un Pythonic façon à Utilisez `dict.get()` avec une valeur par défaut à safely access dictionnaire clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for (category, item) in items  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from items, runs body, repeats until done
"""
Problème 1417 :
Write un Pythonic façon à Utilisez `collections.defaultdict` à group éléments by une clé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : groupby, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → print(grouped)
#   → data = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → print(grouped)
#   → data = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → print(grouped)
"""
Problème 1418 :
Write un Pythonic façon à Utilisez `itertools.groupby()` à group consécutifs identical éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → translation = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → translation = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → translation = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1419 :
Write un Pythonic façon à Utilisez `str.maketrans()` et `str.translate()` à replace multiple caractères.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : frozenset()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → fs1 = ...  # stored in memory
#   → fs2 = ...  # stored in memory
#   → mapping = ...  # stored in memory
#   → print(?)
#   → fs1 = ...  # stored in memory
#   → fs2 = ...  # stored in memory
#   → mapping = ...  # stored in memory
#   → print(?)
#   → fs1 = ...  # stored in memory
#   → fs2 = ...  # stored in memory
#   → mapping = ...  # stored in memory
#   → print(?)
"""
Problème 1420 :
Write un Pythonic façon à Utilisez `frozenset()` à créer an immutable set pour Utilisez as un dictionnaire clé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : combinations, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → pairs = ...  # stored in memory
#   → print(pairs)
#   → items = ...  # stored in memory
#   → pairs = ...  # stored in memory
#   → print(pairs)
#   → items = ...  # stored in memory
#   → pairs = ...  # stored in memory
#   → print(pairs)
"""
Problème 1421 :
Write un Pythonic façon à Utilisez `itertools.combinations()` à générer tous pairs de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : deque, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → queue = ...  # stored in memory
#   → queue.append(1)
#   → queue.append(2)
#   → queue.append(3)
#   → first = ...  # stored in memory
#   → print(first, list(...))
"""
Problème 1422 :
Write un Pythonic façon à Utilisez `collections.deque` à implement un queue avec append et popleft.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(before, after)
#   → text = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(before, after)
#   → text = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(before, after)
"""
Problème 1423 :
Write un Pythonic façon à Utilisez `str.partition()` à split une chaîne at le premier occurrence de un delimiter.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : itemgetter, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → sorted_people = ...  # stored in memory
#   → print(sorted_people)
#   → people = ...  # stored in memory
#   → sorted_people = ...  # stored in memory
#   → print(sorted_people)
#   → people = ...  # stored in memory
#   → sorted_people = ...  # stored in memory
#   → print(sorted_people)
"""
Problème 1424 :
Write un Pythonic façon à Utilisez `operator.itemgetter()` à trier une liste de dictionnaires by une clé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1425 :
Write un Pythonic façon à Utilisez `contextlib.suppress()` à ignore spécifique exceptions.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(any(...))
"""
Problème 1426 :
Write un Pythonic façon à vérifier si un élément dans une liste est vrai.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → lst = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → lst = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
"""
Problème 1427 :
Write un Pythonic façon à Utilisez un generator expression à calculate le somme de squares.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: &
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(a & b)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(a & b)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(a & b)
"""
Problème 1428 :
Write un Pythonic program à get le l'intersection des deux sets.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: |
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(a | b)
#   → print(a | b)
#   → print(a | b)
"""
Problème 1429 :
Write un Pythonic program à get le union des deux sets.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → pass
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only main is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → print(s.isdigit(...))
#   → main()  # jumps into main(), runs body, returns
"""
Problème 1430 :
Write un Pythonic façon à vérifier si une chaîne contient seulement les chiffres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → print(?)
"""
Problème 1431 :
Write un Pythonic l'une-liner à reverse une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → pass
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → zipped = ...  # stored in memory
#   → print(zipped)
#   → main()  # jumps into main(), runs body, returns
"""
Problème 1432 :
Write un Pythonic façon à zip dictionnaire clés et valeurs dans une liste de tuples.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : defaultdict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → print(?)
#   → d = ...  # stored in memory
#   → print(?)
#   → d = ...  # stored in memory
#   → print(?)
"""
Problème 1433 :
Write un Pythonic façon à initialize un dictionnaire avec default integer valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Operators: +
# Fonctions intégrées : defaultdict, dict, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(dict(...))
#   → nested = ...  # stored in memory
#   → for (key1, key2) in data  # runs body once per item
#
# Loop: takes one item from words, runs body, repeats until done
"""
Problème 1434 :
Write un Pythonic program à group éléments by longueur de une liste de mots.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → sorted_lst = ...  # stored in memory
#   → print(sorted_lst)
#   → lst = ...  # stored in memory
#   → sorted_lst = ...  # stored in memory
#   → print(sorted_lst)
#   → lst = ...  # stored in memory
#   → sorted_lst = ...  # stored in memory
#   → print(sorted_lst)
"""
Problème 1435 :
Write un Pythonic program à trier une liste de tuples by le deuxième élément.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
"""
Problème 1436 :
Write un Pythonic façon à Utilisez un context manager à read un fichier.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(max(...))
"""
Problème 1437 :
Write un Pythonic l'une-liner à trouver le maximum élément dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(a, b, c)
#   → lst = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(a, b, c)
#   → lst = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(a, b, c)
"""
Problème 1438 :
Write un Pythonic façon à assign multiple variables de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : dict, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
"""
Problème 1439 :
Write un Pythonic façon à créer un dictionnaire des deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → d.pop('b', None)
#   → print(d)
#   → d = ...  # stored in memory
#   → d.pop('b', None)
#   → print(d)
#   → d = ...  # stored in memory
#   → d.pop('b', None)
#   → print(d)
"""
Problème 1440 :
Write un Pythonic façon à remove une clé de un dictionnaire si it existe.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1441 :
Write un Pythonic façon à vérifier si une variable est None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → d = ...  # stored in memory
#   → d = ...  # stored in memory
"""
Problème 1442 :
Write un Pythonic façon à handle un manquant dictionnaire clé en utilisant try-except.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → t = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(x, y)
#   → t = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(x, y)
#   → t = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(x, y)
"""
Problème 1443 :
Write un Pythonic façon à décompresser un tuple dans variables.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: !=
# Fonctions intégrées : len, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(?)
"""
Problème 1444 :
Write un Pythonic l'une-liner à vérifier si une liste contient les doublons.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Counter()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → freq = ...  # stored in memory
#   → print(freq)
#   → s = ...  # stored in memory
#   → freq = ...  # stored in memory
#   → print(freq)
#   → s = ...  # stored in memory
#   → freq = ...  # stored in memory
#   → print(freq)
"""
Problème 1445 :
Write un Pythonic program à count caractère fréquences dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → print(?)
#   → lst = ...  # stored in memory
#   → print(?)
"""
Problème 1446 :
Write un Pythonic l'une-liner à filtrer out les nombres négatifs de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: % ==
# Fonctions intégrées : any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(any(...))
"""
Problème 1447 :
Write un Pythonic façon à test si une liste un at moins l'une même nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(lst.index(...))
"""
Problème 1448 :
Write un Pythonic façon à get le index de an élément dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
#   → d = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
#   → d = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
"""
Problème 1449 :
Write un Pythonic façon à filtrer dictionnaire éléments avec valeurs > 10.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → inverted = ...  # stored in memory
#   → print(inverted)
#   → d = ...  # stored in memory
#   → inverted = ...  # stored in memory
#   → print(inverted)
#   → d = ...  # stored in memory
#   → inverted = ...  # stored in memory
#   → print(inverted)
"""
Problème 1450 :
Write un Pythonic program à invert un dictionnaire (clés become valeurs, valeurs become clés).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: >
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(all(...))
"""
Problème 1451 :
Écrivez un programme Python en utilisant `tous()` à vérifier si tous nombres dans une liste sont positif.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → print(all(...))
"""
Problème 1452 :
Utilisez `tous()` à determine si tous caractères dans une chaîne sont lowercase.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: % ==
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(all(...))
"""
Problème 1453 :
Write un Pythonic façon à Utilisez `tous()` à vérifier si tous éléments dans une liste sont même.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : all, bool()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → print(all(...))
#   → result = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(result)
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1454 :
vérifier si tous éléments dans un dictionnaire ont non vide valeurs en utilisant `tous()`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: >
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → print(all(...))
"""
Problème 1455 :
Utilisez `tous()` à vérifier si tous éléments dans un ensemble sont greater que 10.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lines = ...  # stored in memory
#   → print(all(...))
#   → result = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(result)
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1456 :
Utilisez `tous()` à validate qui tous lines dans une liste start avec un capital lettre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → row = ...  # stored in memory
#   → print(all(...))
"""
Problème 1457 :
Utilisez `tous()` à vérifier si tous éléments dans un matrix row sont equal.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → lst = ['a', 'b', 'c']
#   → result = True
#   → for x in lst:
#   →     if not isinstance(x, str):
#   →         result = False
#   →         break
#   → result = result
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only main is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(all(...))
#   → main()  # jumps into main(), runs body, returns
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → for x in lst  # runs body once per item
#   → print(result)
#   → lst = ...  # stored in memory
#   → result = ...  # stored in memory
#   → for x in lst  # runs body once per item
#   → print(result)
#
# Loop: takes one item from lst, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1458 :
Utilisez `tous()` à vérifier si tous valeurs dans une liste sont strings.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → print(all(...))
"""
Problème 1459 :
Écrivez un programme Python qui utilise `tous()` à vérifier si tous clés dans un dictionnaire sont strings.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → files = ...  # stored in memory
#   → print(all(...))
"""
Problème 1460 :
Utilisez `tous()` à vérifier qui tous files ont un ".txt" extension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: <=
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(all(...))
"""
Problème 1461 :
Utilisez `tous()` à vérifier si tous nombres dans une liste sont dans un certain range.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: >
# Fonctions intégrées : all, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → matrix = ...  # stored in memory
#   → print(all(...))
"""
Problème 1462 :
vérifier si tous sublists dans un 2D list sont non vide en utilisant `tous()`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → flags = ...  # stored in memory
#   → print(all(...))
"""
Problème 1463 :
Utilisez `tous()` à confirm tous booleans dans une liste sont vrai.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for | if/else
# Operators: in not in
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → print(all(...))
#   → result = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(result)
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1464 :
Utilisez `tous()` à vérifier tous words dans une liste contien la lettre "a".
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: <
# Fonctions intégrées : all, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(all(...))
"""
Problème 1465 :
Écrivez un programme Python en utilisant `tous()` à vérifier si une liste de nombres est strictly increasing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: and
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → envs = ...  # stored in memory
#   → print(all(...))
"""
Problème 1466 :
vérifier si tous les variables d'environnement dans une liste sont defined (non vide strings).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Fonctions intégrées : all, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → t = ...  # stored in memory
#   → print(all(...))
"""
Problème 1467 :
Utilisez `tous()` à ensure tous éléments dans un tuple sont de le même tapez.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + > != ** and
# Fonctions intégrées : all, int, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n > 1 and all((n % i != 0 for i in range(2, int(n ** 0.5) + 1)))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_prime is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(all(...))
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_prime(...))  # is_prime() runs first → value goes to print()
"""
Problème 1468 :
vérifier si tous nombres dans une liste sont prime en utilisant `tous()` et un helper fonction.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: is not
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(all(...))
"""
Problème 1469 :
Utilisez `tous()` à confirm tous valeurs dans une liste sont pas None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for | if/else
# Operators: >= not in
# Fonctions intégrées : all, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → passwords = ...  # stored in memory
#   → print(all(...))
#   → result = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(result)
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1470 :
Utilisez `tous()` à vérifier qui tous passwords dans une liste sont at moins 8 caractères long.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1471 :
Écrivez un programme Python à vérifier si une variable est `None` en utilisant `is`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
"""
Problème 1472 :
vérifier si deux variables point à le même list objet en utilisant `is`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is not
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
"""
Problème 1473 :
vérifier si deux separately created listes sont pas le même objet en utilisant `is`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → flag = ...  # stored in memory
#   → print(?)
#   → flag = ...  # stored in memory
#   → print(?)
#   → flag = ...  # stored in memory
#   → print(?)
"""
Problème 1474 :
Utilisez `is` à vérifier si une variable est exactly `True`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: == is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → s1 = ...  # stored in memory
#   → s2 = ...  # stored in memory
#   → print(?)
#   → print(?)
#   → s1 = ...  # stored in memory
#   → s2 = ...  # stored in memory
#   → print(?)
#   → s1 = ...  # stored in memory
#   → s2 = ...  # stored in memory
#   → print(?)
"""
Problème 1475 :
Utilisez `is` à check identity des deux strings (interned strings).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
"""
Problème 1476 :
Utilisez `is` à check identity de small integers.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only do_nothing is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(?)
"""
Problème 1477 :
vérifier si une fonction retourner valeur est `None` en utilisant `is`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Operators: == is
# Fonctions intégrées : A()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes A are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a1 = ...  # stored in memory
#   → a2 = ...  # stored in memory
#   → print(?)
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(A(...))
"""
Problème 1478 :
Utilisez `is` à vérifier si une classe attribute est shared.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return x
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only identity is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(identity(...))  # identity() runs first → value goes to print()
"""
Problème 1479 :
Utilisez `is` à vérifier si un objet est le même avant et après un l'appel de fonction.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1480 :
Utilisez `is` à compare avec construit-dans singleton `Ellipsis`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Operators: is not
# Fonctions intégrées : X()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes X are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x1 = ...  # stored in memory
#   → x2 = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(X(...))
"""
Problème 1481 :
vérifier si deux instances de une classe sont pas le même objet.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d1 = ...  # stored in memory
#   → d2 = ...  # stored in memory
#   → print(?)
#   → d1 = ...  # stored in memory
#   → d2 = ...  # stored in memory
#   → print(?)
#   → d1 = ...  # stored in memory
#   → d2 = ...  # stored in memory
#   → print(?)
"""
Problème 1482 :
Check identity des deux variables referencing le même dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
# Fonctions intégrées : object()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a, b = ...  # stored in memory
#   → print('Assertion passed')
#   → a, b = ...  # stored in memory
#   → print('Assertion passed')
#   → a, b = ...  # stored in memory
#   → print('Assertion passed')
"""
Problème 1483 :
Utilisez `is` dans an assertion à vérifier objet identity.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → print(?)
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → print(?)
"""
Problème 1484 :
Compare identity des deux Boolean valeurs en utilisant `is`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: is not
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → if name is not None:
#   →     print(f'Hello, {name}!')
#   → if name is not None:
#   →     print(f'Hello, {name}!')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — greet, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → greet('Alice')  # jumps into greet(), runs body, returns
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greet(...))  # greet() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1485 :
Utilisez `is not` à vérifier une fonction argument est pas None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
# Fonctions intégrées : object()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return x is SENTINEL
#   → return x is SENTINEL
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → SENTINEL = ...  # stored in memory
#   → value = ...  # stored in memory
#   → print(check(...))  # check() runs first → value goes to print()
#   → x = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check(...))  # check() runs first → value goes to print()
"""
Problème 1486 :
Utilisez `is` à test si un global constant est unchanged.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → a = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → a = ...  # stored in memory
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → a = ...  # stored in memory
#   → print(?)
"""
Problème 1487 :
Check qui changing une variable makes it no longer le même objet.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → NONE_CONST = ...  # stored in memory
#   → x = ...  # stored in memory
#   → print(?)
#   → NONE_CONST = ...  # stored in memory
#   → x = ...  # stored in memory
#   → print(?)
#   → NONE_CONST = ...  # stored in memory
#   → x = ...  # stored in memory
#   → print(?)
"""
Problème 1488 :
Utilisez `is` à compare avec un constant declared elsewhere.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: is not
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → value = ...  # stored in memory
#   → print(?)
#   → value = ...  # stored in memory
#   → print(?)
#   → value = ...  # stored in memory
#   → print(?)
"""
Problème 1489 :
Utilisez `is` avec `not` à check qui une valeur est pas `False`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: == is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → print(?)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → print(?)
#   → print(?)
"""
Problème 1490 :
Show qui identity et equality sont pas always le même.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(max(...))
"""
Problème 1491 :
Utilisez `default=` avec le `max()` fonction à retourner un default lorsque la liste est vide.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → values = ...  # stored in memory
#   → print(min(...))
"""
Problème 1492 :
Utilisez `default=` avec le `min()` fonction à avoid une erreur on une liste vide sequence.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → print(max(...))
"""
Problème 1493 :
Utilisez `default=` dans `max()` à safely get le plus long mot de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: % ==
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → lst = ...  # stored in memory
#   → print(next(...))
"""
Problème 1494 :
Utilisez `default=` avec `next()` à get le premier même nombre ou -1.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: >
# Fonctions intégrées : next, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → gen = ...  # stored in memory
#   → print(next(...))
"""
Problème 1495 :
Utilisez `default=` dans `next()` à safely access an élément dans un generator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: % ==
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(max(...))
"""
Problème 1496 :
Utilisez `default=` avec `max()` on un filtered list à handle no correspond.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: % ==
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(min(...))
"""
Problème 1497 :
Utilisez `default=` dans `min()` à get un fallback valeur lorsque no condition correspond.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → names = ...  # stored in memory
#   → print(next(...))
"""
Problème 1498 :
Utilisez `default=` avec `next()` à trouver le premier nom starting avec "Z".
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → print(max(...))
"""
Problème 1499 :
Utilisez `default=` avec `max()` à trouver le max dans un sublist ou retourner un placeholder.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → print(next(...))
"""
Problème 1500 :
Utilisez `default=` dans `next()` à handle une liste vide comprehension.
"""

