# Syntax documentation
"""
Problem 1501:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: min, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
s = ...  # assigns a value to the variable
print(min(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → print(min(...))

"""

"""
Problem 1502:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Built-in functions: next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
x == 4  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
nums = ...  # assigns a value to the variable
print(next(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(next(...))

"""

"""
Problem 1503:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
scores = ...  # assigns a value to the variable
print(max(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → scores = ...  # stored in memory
#   → print(max(...))

"""

"""
Problem 1504:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Built-in functions: next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
ch == 'z'  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
letters = ...  # assigns a value to the variable
print(next(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → letters = ...  # stored in memory
#   → print(next(...))

"""

"""
Problem 1505:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
strings = ...  # assigns a value to the variable
print(max(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → strings = ...  # stored in memory
#   → print(max(...))

"""

"""
Problem 1506:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Built-in functions: next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? == 'Zoe'  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
users = ...  # assigns a value to the variable
print(next(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → users = ...  # stored in memory
#   → print(next(...))

"""

"""
Problem 1507:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
print(min(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → print(min(...))

"""

"""
Problem 1508:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
scores = ...  # assigns a value to the variable
print(max(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → scores = ...  # stored in memory
#   → print(max(...))

"""

"""
Problem 1509:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: iter, next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
print(next(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → print(next(...))

"""

"""
Problem 1510:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
words = ...  # assigns a value to the variable
print(max(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → print(max(...))

"""

"""
Problem 1511:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
items = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1512:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    people = [{'age': 25}, {'age': 30}, {'age': 20}]
    min_age_dict = min(people, key=lambda x: x.get('age', 0), default={'age': 0})
    print(min_age_dict)
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
people = ...  # assigns a value to the variable
min_age_dict = ...  # assigns a value to the variable
print(min_age_dict)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
people = ...  # assigns a value to the variable
min_age_dict = ...  # assigns a value to the variable
print(min_age_dict)
people = ...  # assigns a value to the variable
min_age_dict = ...  # assigns a value to the variable
print(min_age_dict)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → min_age_dict = ...  # stored in memory
#   → print(min_age_dict)
#   → people = ...  # stored in memory
#   → min_age_dict = ...  # stored in memory
#   → print(min_age_dict)
#   → people = ...  # stored in memory
#   → min_age_dict = ...  # stored in memory
#   → print(min_age_dict)

"""

"""
Problem 1513:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    words = ['hello', 'world', 'python']
    max_word = max(words, key=len, default='No items')
    print(max_word)
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
words = ...  # assigns a value to the variable
max_word = ...  # assigns a value to the variable
print(max_word)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
words = ...  # assigns a value to the variable
max_word = ...  # assigns a value to the variable
print(max_word)
words = ...  # assigns a value to the variable
max_word = ...  # assigns a value to the variable
print(max_word)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → max_word = ...  # stored in memory
#   → print(max_word)
#   → words = ...  # stored in memory
#   → max_word = ...  # stored in memory
#   → print(max_word)
#   → words = ...  # stored in memory
#   → max_word = ...  # stored in memory
#   → print(max_word)

"""

"""
Problem 1514:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {'user': {'name': 'Alice', 'age': 30}}
    name = data.get('user', {}).get('name', 'Unknown')
    city = data.get('user', {}).get('city', 'Unknown')
    print(name, city)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
name = ...  # assigns a value to the variable
city = ...  # assigns a value to the variable
print(name, city)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
name = ...  # assigns a value to the variable
city = ...  # assigns a value to the variable
print(name, city)
data = ...  # assigns a value to the variable
name = ...  # assigns a value to the variable
city = ...  # assigns a value to the variable
print(name, city)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)

"""

"""
Problem 1515:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
items = ...  # assigns a value to the variable
counter = ...  # assigns a value to the variable
for item in items:  # loops once per item
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → counter = ...  # stored in memory
#   → for item in items  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from items, runs body, repeats until done

"""

"""
Problem 1516:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {}
    data.setdefault('items', []).append('apple')
    data.setdefault('items', []).append('banana')
    print(data)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
print(data)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
print(data)
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
print(data)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(data)

"""

"""
Problem 1517:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    numbers = [1, 3, 5, 8, 10]
    first_even = next((x for x in numbers if x % 2 == 0), None)
    print(first_even)
#
# Operators: % ==
# Built-in functions: next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
numbers = ...  # assigns a value to the variable
first_even = ...  # assigns a value to the variable
print(first_even)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
first_even = ...  # assigns a value to the variable
print(first_even)
numbers = ...  # assigns a value to the variable
first_even = ...  # assigns a value to the variable
print(first_even)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)

"""

"""
Problem 1518:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
items = ...  # assigns a value to the variable
grouped = ...  # assigns a value to the variable
for (category, item) in items:  # loops once per item
  ?.append(item)
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for (category, item) in items  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from items, runs body, repeats until done

"""

"""
Problem 1519:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {'user': {'name': 'Alice'}}
    user_info = data.get('user', {})
    name = user_info.get('name', 'Unknown')
    print(name)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
user_info = ...  # assigns a value to the variable
name = ...  # assigns a value to the variable
print(name)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
user_info = ...  # assigns a value to the variable
name = ...  # assigns a value to the variable
print(name)
data = ...  # assigns a value to the variable
user_info = ...  # assigns a value to the variable
name = ...  # assigns a value to the variable
print(name)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → user_info = ...  # stored in memory
#   → name = ...  # stored in memory
#   → print(name)
#   → data = ...  # stored in memory
#   → user_info = ...  # stored in memory
#   → name = ...  # stored in memory
#   → print(name)
#   → data = ...  # stored in memory
#   → user_info = ...  # stored in memory
#   → name = ...  # stored in memory
#   → print(name)

"""

"""
Problem 1520:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    words = ['hello', 'world', 'python', 'hi']
    shortest = min(words, key=len, default='')
    print(shortest)
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
words = ...  # assigns a value to the variable
shortest = ...  # assigns a value to the variable
print(shortest)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
words = ...  # assigns a value to the variable
shortest = ...  # assigns a value to the variable
print(shortest)
words = ...  # assigns a value to the variable
shortest = ...  # assigns a value to the variable
print(shortest)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → shortest = ...  # stored in memory
#   → print(shortest)
#   → words = ...  # stored in memory
#   → shortest = ...  # stored in memory
#   → print(shortest)
#   → words = ...  # stored in memory
#   → shortest = ...  # stored in memory
#   → print(shortest)

"""

"""
Problem 1521:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {}
    data.setdefault('user', {}).setdefault('profile', {})['name'] = 'Alice'
    print(data)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(data)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(data)
data = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(data)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)

"""

"""
Problem 1522:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    tuples = [(1, 5), (3, 2), (2, 8)]
    max_tuple = max(tuples, key=lambda x: x[1], default=(0, 0))
    print(max_tuple)
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
tuples = ...  # assigns a value to the variable
max_tuple = ...  # assigns a value to the variable
print(max_tuple)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
tuples = ...  # assigns a value to the variable
max_tuple = ...  # assigns a value to the variable
print(max_tuple)
tuples = ...  # assigns a value to the variable
max_tuple = ...  # assigns a value to the variable
print(max_tuple)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → tuples = ...  # stored in memory
#   → max_tuple = ...  # stored in memory
#   → print(max_tuple)
#   → tuples = ...  # stored in memory
#   → max_tuple = ...  # stored in memory
#   → print(max_tuple)
#   → tuples = ...  # stored in memory
#   → max_tuple = ...  # stored in memory
#   → print(max_tuple)

"""

"""
Problem 1523:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Built-in functions: defaultdict, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
items = ...  # assigns a value to the variable
grouped = ...  # assigns a value to the variable
for (key, value) in items:  # loops once per item
  ?.add(value)
print(?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for (key, value) in items  # runs body once per item
#   → print(?)
#
# Loop: takes one item from items, runs body, repeats until done

"""

"""
Problem 1524:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    dict1 = {'a': 10, 'b': 20}
    dict2 = {'b': 30, 'c': 40}
    total = sum((dict1.get(k, 0) + dict2.get(k, 0) for k in set(dict1) | set(dict2)))
    print(total)
#
# Operators: + |
# Built-in functions: set, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
dict1.get(...) + dict2.get(...)  # dict1.get(...) → then dict2.get(...) → then +
set(...) | set(...)  # set(...) → then set(...) → then |
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
total = ...  # assigns a value to the variable
print(total)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
total = ...  # assigns a value to the variable
print(total)
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
total = ...  # assigns a value to the variable
print(total)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)

"""

"""
Problem 1525:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    numbers = [1, 3, 5, 7]
    first_even = next((x for x in numbers if x % 2 == 0), -1)
    print(first_even)
#
# Operators: % ==
# Built-in functions: next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
numbers = ...  # assigns a value to the variable
first_even = ...  # assigns a value to the variable
print(first_even)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
first_even = ...  # assigns a value to the variable
print(first_even)
numbers = ...  # assigns a value to the variable
first_even = ...  # assigns a value to the variable
print(first_even)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)

"""

"""
Problem 1526:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {}
    data.setdefault('items', []).append('apple')
    data.setdefault('items', []).append('banana')
    print(data['items'])
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
print(?)
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
print(?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(?)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(?)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(?)

"""

"""
Problem 1527:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    people = [{'name': 'Alice', 'age': 30}, {'name': 'Bob', 'age': 25}]
    min_age = min((p.get('age', 0) for p in people), default=0)
    print(min_age)
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
people = ...  # assigns a value to the variable
min_age = ...  # assigns a value to the variable
print(min_age)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
people = ...  # assigns a value to the variable
min_age = ...  # assigns a value to the variable
print(min_age)
people = ...  # assigns a value to the variable
min_age = ...  # assigns a value to the variable
print(min_age)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → min_age = ...  # stored in memory
#   → print(min_age)
#   → people = ...  # stored in memory
#   → min_age = ...  # stored in memory
#   → print(min_age)
#   → people = ...  # stored in memory
#   → min_age = ...  # stored in memory
#   → print(min_age)

"""

"""
Problem 1528:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
words = ...  # assigns a value to the variable
grouped = ...  # assigns a value to the variable
for word in words:  # loops once per item
  ?.append(word)
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from words, runs body, repeats until done

"""

"""
Problem 1529:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {'items': ['apple', 'banana']}
    items = data.get('items', [])
    missing = data.get('missing', [])
    print(items, missing)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
items = ...  # assigns a value to the variable
missing = ...  # assigns a value to the variable
print(items, missing)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
items = ...  # assigns a value to the variable
missing = ...  # assigns a value to the variable
print(items, missing)
data = ...  # assigns a value to the variable
items = ...  # assigns a value to the variable
missing = ...  # assigns a value to the variable
print(items, missing)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → items = ...  # stored in memory
#   → missing = ...  # stored in memory
#   → print(items, missing)
#   → data = ...  # stored in memory
#   → items = ...  # stored in memory
#   → missing = ...  # stored in memory
#   → print(items, missing)
#   → data = ...  # stored in memory
#   → items = ...  # stored in memory
#   → missing = ...  # stored in memory
#   → print(items, missing)

"""

"""
Problem 1530:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    words = ['hello', 'world', 'python']
    longest = max(words, key=len, default='N/A')
    print(longest)
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
words = ...  # assigns a value to the variable
longest = ...  # assigns a value to the variable
print(longest)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
words = ...  # assigns a value to the variable
longest = ...  # assigns a value to the variable
print(longest)
words = ...  # assigns a value to the variable
longest = ...  # assigns a value to the variable
print(longest)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → longest = ...  # stored in memory
#   → print(longest)
#   → words = ...  # stored in memory
#   → longest = ...  # stored in memory
#   → print(longest)
#   → words = ...  # stored in memory
#   → longest = ...  # stored in memory
#   → print(longest)

"""

"""
Problem 1531:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    counter = {}
    counter.setdefault('count', 0)
    counter['count'] += 1
    print(counter)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
counter = ...  # assigns a value to the variable
counter.setdefault('count', 0)
print(counter)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
counter = ...  # assigns a value to the variable
counter.setdefault('count', 0)
print(counter)
counter = ...  # assigns a value to the variable
counter.setdefault('count', 0)
print(counter)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → counter = ...  # stored in memory
#   → counter.setdefault('count', 0)
#   → print(counter)
#   → counter = ...  # stored in memory
#   → counter.setdefault('count', 0)
#   → print(counter)
#   → counter = ...  # stored in memory
#   → counter.setdefault('count', 0)
#   → print(counter)

"""

"""
Problem 1532:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def is_prime(n):  # defines a function with parameters n
    return n > 1 and all((n % i != 0 for i in range(2, int(n ** 0.5) + 1)))  # return sends the result back to the caller
#
# Operators: % + > != ** and
# Built-in functions: all, int, next, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
is_prime(n)  # arguments evaluated first, then function body runs
print(is_prime(15))  # is_prime() runs first, then print() outputs the result
n > 1  # evaluated as True or False → picks which branch
is_prime(x)  # arguments evaluated first, then function body runs
? != 0  # evaluated as True or False → picks which branch
n % i  # n → then i → then %
int(...) + 1  # int(...) → then 1 → then +
n ** 0.5  # n → then 0.5 → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def is_prime(n):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
first_prime = ...  # assigns a value to the variable
print(first_prime)
def is_prime(n):  # stored in memory, not executed yet
def is_prime(n):  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(is_prime(...))  # is_prime() runs with n=15
#   → return n > 1 and all((n % i != 0 for i in range(2, int(n ** 0.5) + 1)))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_prime is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_prime = ...  # stored in memory
#   → print(first_prime)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_prime(...))  # is_prime() runs first → value goes to print()

"""

"""
Problem 1533:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
text = ...  # assigns a value to the variable
char_count = ...  # assigns a value to the variable
for char in text:  # loops once per item
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → char_count = ...  # stored in memory
#   → for char in text  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from text, runs body, repeats until done

"""

"""
Problem 1534:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    dict1 = {'a': 1, 'b': 2}
    dict2 = {'b': 3, 'c': 4}
    merged = {k: dict1.get(k, 0) + dict2.get(k, 0) for k in set(dict1) | set(dict2)}
    print(merged)
#
# Operators: + |
# Built-in functions: set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
dict1.get(...) + dict2.get(...)  # dict1.get(...) → then dict2.get(...) → then +
set(...) | set(...)  # set(...) → then set(...) → then |
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
merged = ...  # assigns a value to the variable
print(merged)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
merged = ...  # assigns a value to the variable
print(merged)
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
merged = ...  # assigns a value to the variable
print(merged)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → merged = ...  # stored in memory
#   → print(merged)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → merged = ...  # stored in memory
#   → print(merged)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → merged = ...  # stored in memory
#   → print(merged)

"""

"""
Problem 1535:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    students = [{'name': 'Alice', 'score': 85.5}, {'name': 'Bob', 'score': 92.0}]
    min_score = min((s.get('score', 0.0) for s in students), default=0.0)
    print(min_score)
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
students = ...  # assigns a value to the variable
min_score = ...  # assigns a value to the variable
print(min_score)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
students = ...  # assigns a value to the variable
min_score = ...  # assigns a value to the variable
print(min_score)
students = ...  # assigns a value to the variable
min_score = ...  # assigns a value to the variable
print(min_score)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → students = ...  # stored in memory
#   → min_score = ...  # stored in memory
#   → print(min_score)
#   → students = ...  # stored in memory
#   → min_score = ...  # stored in memory
#   → print(min_score)
#   → students = ...  # stored in memory
#   → min_score = ...  # stored in memory
#   → print(min_score)

"""

"""
Problem 1536:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {}
    data.setdefault('fruits', []).append('apple')
    data.setdefault('fruits', []).append('banana')
    data.setdefault('vegetables', []).append('carrot')
    print(data)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
data.setdefault(...).append('carrot')
print(data)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
data.setdefault(...).append('carrot')
print(data)
data = ...  # assigns a value to the variable
data.setdefault(...).append('apple')
data.setdefault(...).append('banana')
data.setdefault(...).append('carrot')
print(data)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → data.setdefault(...).append('carrot')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → data.setdefault(...).append('carrot')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → data.setdefault(...).append('carrot')
#   → print(data)

"""

"""
Problem 1537:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    products = [{'name': 'laptop', 'price': 999}, {'name': 'phone', 'price': 699}]
    max_price = max((p.get('price', 0) for p in products), default='No products')
    print(max_price)
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
products = ...  # assigns a value to the variable
max_price = ...  # assigns a value to the variable
print(max_price)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
products = ...  # assigns a value to the variable
max_price = ...  # assigns a value to the variable
print(max_price)
products = ...  # assigns a value to the variable
max_price = ...  # assigns a value to the variable
print(max_price)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → products = ...  # stored in memory
#   → max_price = ...  # stored in memory
#   → print(max_price)
#   → products = ...  # stored in memory
#   → max_price = ...  # stored in memory
#   → print(max_price)
#   → products = ...  # stored in memory
#   → max_price = ...  # stored in memory
#   → print(max_price)

"""

"""
Problem 1538:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
data = ...  # assigns a value to the variable
nested = ...  # assigns a value to the variable
for (key1, key2) in data:  # loops once per item
print(?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → nested = ...  # stored in memory
#   → for (key1, key2) in data  # runs body once per item
#   → print(?)
#
# Loop: takes one item from data, runs body, repeats until done

"""

"""
Problem 1539:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    dict1 = {'name': 'Alice'}
    dict2 = {'surname': 'Smith'}
    full_name = dict1.get('name', '') + ' ' + dict2.get('surname', '')
    print(full_name)
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? + dict2.get(...)  # ? → then dict2.get(...) → then +
dict1.get(...) + ' '  # dict1.get(...) → then ' ' → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
full_name = ...  # assigns a value to the variable
print(full_name)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
full_name = ...  # assigns a value to the variable
print(full_name)
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
full_name = ...  # assigns a value to the variable
print(full_name)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → full_name = ...  # stored in memory
#   → print(full_name)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → full_name = ...  # stored in memory
#   → print(full_name)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → full_name = ...  # stored in memory
#   → print(full_name)

"""

"""
Problem 1540:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    numbers = [1, 3, 5, 7, 9]
    first_greater_than_5 = next((x for x in numbers if x > 5), None)
    print(first_greater_than_5)
#
# Operators: >
# Built-in functions: next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
x > 5  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
numbers = ...  # assigns a value to the variable
first_greater_than_5 = ...  # assigns a value to the variable
print(first_greater_than_5)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
first_greater_than_5 = ...  # assigns a value to the variable
print(first_greater_than_5)
numbers = ...  # assigns a value to the variable
first_greater_than_5 = ...  # assigns a value to the variable
print(first_greater_than_5)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_greater_than_5 = ...  # stored in memory
#   → print(first_greater_than_5)
#   → numbers = ...  # stored in memory
#   → first_greater_than_5 = ...  # stored in memory
#   → print(first_greater_than_5)
#   → numbers = ...  # stored in memory
#   → first_greater_than_5 = ...  # stored in memory
#   → print(first_greater_than_5)

"""

"""
Problem 1541:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {}
    data.setdefault('tags', set()).add('python')
    data.setdefault('tags', set()).add('programming')
    print(data)
#
# Built-in functions: set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
data.setdefault(...).add('python')
data.setdefault(...).add('programming')
print(data)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
data.setdefault(...).add('python')
data.setdefault(...).add('programming')
print(data)
data = ...  # assigns a value to the variable
data.setdefault(...).add('python')
data.setdefault(...).add('programming')
print(data)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).add('python')
#   → data.setdefault(...).add('programming')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).add('python')
#   → data.setdefault(...).add('programming')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).add('python')
#   → data.setdefault(...).add('programming')
#   → print(data)

"""

"""
Problem 1542:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    people = [{'name': 'Alice', 'age': 30}, {'name': 'Bob', 'age': 25}]
    oldest = min(people, key=lambda x: x.get('age', 0), default={'name': 'Unknown', 'age': 0})
    print(oldest)
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
people = ...  # assigns a value to the variable
oldest = ...  # assigns a value to the variable
print(oldest)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
people = ...  # assigns a value to the variable
oldest = ...  # assigns a value to the variable
print(oldest)
people = ...  # assigns a value to the variable
oldest = ...  # assigns a value to the variable
print(oldest)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → oldest = ...  # stored in memory
#   → print(oldest)
#   → people = ...  # stored in memory
#   → oldest = ...  # stored in memory
#   → print(oldest)
#   → people = ...  # stored in memory
#   → oldest = ...  # stored in memory
#   → print(oldest)

"""

"""
Problem 1543:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Operators: +
# Built-in functions: defaultdict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
value + ' '  # value → then ' ' → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
words = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
for (key, value) in words:  # loops once per item
print(?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → result = ...  # stored in memory
#   → for (key, value) in words  # runs body once per item
#   → print(?)
#
# Loop: takes one item from words, runs body, repeats until done

"""

"""
Problem 1544:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {'items': [1, 2, 3]}
    existing = data.get('items', [])
    existing.extend([4, 5])
    print(existing)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
existing = ...  # assigns a value to the variable
existing.extend(?)
print(existing)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
existing = ...  # assigns a value to the variable
existing.extend(?)
print(existing)
data = ...  # assigns a value to the variable
existing = ...  # assigns a value to the variable
existing.extend(?)
print(existing)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → existing = ...  # stored in memory
#   → existing.extend(?)
#   → print(existing)
#   → data = ...  # stored in memory
#   → existing = ...  # stored in memory
#   → existing.extend(?)
#   → print(existing)
#   → data = ...  # stored in memory
#   → existing = ...  # stored in memory
#   → existing.extend(?)
#   → print(existing)

"""

"""
Problem 1545:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from datetime import datetime
    events = [datetime(2023, 1, 1), datetime(2023, 2, 1), datetime(2023, 3, 1)]
    max_date = max(events, default=datetime.min)
    print(max_date)
#
# Built-in functions: datetime, max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from datetime import datetime
events = ...  # assigns a value to the variable
max_date = ...  # assigns a value to the variable
print(max_date)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from datetime import datetime
events = ...  # assigns a value to the variable
max_date = ...  # assigns a value to the variable
print(max_date)
from datetime import datetime
events = ...  # assigns a value to the variable
max_date = ...  # assigns a value to the variable
print(max_date)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → events = ...  # stored in memory
#   → max_date = ...  # stored in memory
#   → print(max_date)
#   → events = ...  # stored in memory
#   → max_date = ...  # stored in memory
#   → print(max_date)
#   → events = ...  # stored in memory
#   → max_date = ...  # stored in memory
#   → print(max_date)

"""

"""
Problem 1546:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    data = {}
    data.setdefault('users', {}).setdefault('alice', {})['age'] = 30
    data.setdefault('users', {}).setdefault('bob', {})['age'] = 25
    print(data)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
data = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(data)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(data)
data = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(data)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)

"""

"""
Problem 1547:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def number_gen():  # defines a function with no parameters
    yield 1
    yield 2
    yield 3
#
# Built-in functions: iter, next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
number_gen()  # function body runs with no arguments
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def number_gen():  # stored in memory, not executed yet
gen = ...  # assigns a value to the variable
first = ...  # assigns a value to the variable
empty_gen = ...  # assigns a value to the variable
empty_result = ...  # assigns a value to the variable
print(first, empty_result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
# evaluates expression
# evaluates expression
# evaluates expression
# evaluates expression
# evaluates expression
# evaluates expression
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — number_gen, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → gen = ...  # stored in memory
#   → first = ...  # stored in memory
#   → empty_gen = ...  # stored in memory
#   → empty_result = ...  # stored in memory
#   → print(first, empty_result)

"""

"""
Problem 1548:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import defaultdict
stats = ...  # assigns a value to the variable
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → stats = ...  # stored in memory
#   → print(dict(...))

"""

"""
Problem 1549:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def create_list():  # defines a function with no parameters
    return []  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
'items' not in data  # evaluated as True or False → picks which branch
create_list()  # function body runs with no arguments
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def create_list():  # stored in memory, not executed yet
data = ...  # assigns a value to the variable
# if ... → only one branch executes based on condition
items = ...  # assigns a value to the variable
items.append('apple')
print(data)
def create_list():  # stored in memory, not executed yet
def create_list():  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → if condition  # evaluates → runs the True branch
#   → items = ...  # stored in memory
#   → items.append('apple')
#   → print(data)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1550:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from functools import reduce
    numbers = [2, 3, 4, 5]
    product = reduce(lambda x, y: x * y, numbers)
    print(product)
#
# Operators: *
# Built-in functions: reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
x * y  # x → then y → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import reduce
numbers = ...  # assigns a value to the variable
product = ...  # assigns a value to the variable
print(product)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from functools import reduce
numbers = ...  # assigns a value to the variable
product = ...  # assigns a value to the variable
print(product)
from functools import reduce
numbers = ...  # assigns a value to the variable
product = ...  # assigns a value to the variable
print(product)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)

"""

"""
Problem 1551:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from functools import partial
    multiply_by_5 = partial(lambda x, y: x * y, 5)
    result = multiply_by_5(10)
    print(result)
#
# Operators: *
# Built-in functions: multiply_by_5, partial()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
x * y  # x → then y → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import partial
multiply_by_5 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from functools import partial
multiply_by_5 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
from functools import partial
multiply_by_5 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → multiply_by_5 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → multiply_by_5 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → multiply_by_5 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1552:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def fibonacci(n):  # defines a function with parameters n
    if n < 2:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: + - <
# Built-in functions: lru_cache()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(fibonacci(10))  # fibonacci() runs first, then print() outputs the result
fibonacci(n)  # arguments evaluated first, then function body runs
print(fibonacci(15))  # fibonacci() runs first, then print() outputs the result
n < 2  # evaluated as True or False → picks which branch
fibonacci(...) + fibonacci(...)  # fibonacci(...) → then fibonacci(...) → then +
fibonacci(n - 1)  # arguments evaluated first, then function body runs
fibonacci(n - 2)  # arguments evaluated first, then function body runs
n - 1  # n → then 1 → then -
n - 2  # n → then 2 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import lru_cache
def fibonacci(n):  # stored in memory, not executed yet
print(fibonacci(...))  # fibonacci() runs with n=10
#   → return n
def fibonacci(n):  # stored in memory, not executed yet
def fibonacci(n):  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(fibonacci(...))  # fibonacci() runs with n=15
#   → return n
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only fibonacci is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(fibonacci(...))  # fibonacci() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(fibonacci(...))  # fibonacci() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1553:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import permutations
    items = [1, 2, 3]
    perms = list(permutations(items))
    print(perms)
#
# Built-in functions: list, permutations()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import permutations
items = ...  # assigns a value to the variable
perms = ...  # assigns a value to the variable
print(perms)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import permutations
items = ...  # assigns a value to the variable
perms = ...  # assigns a value to the variable
print(perms)
from itertools import permutations
items = ...  # assigns a value to the variable
perms = ...  # assigns a value to the variable
print(perms)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → perms = ...  # stored in memory
#   → print(perms)
#   → items = ...  # stored in memory
#   → perms = ...  # stored in memory
#   → print(perms)
#   → items = ...  # stored in memory
#   → perms = ...  # stored in memory
#   → print(perms)

"""

"""
Problem 1554:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import product
    list1 = [1, 2]
    list2 = ['a', 'b']
    cartesian = list(product(list1, list2))
    print(cartesian)
#
# Built-in functions: list, product()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import product
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
cartesian = ...  # assigns a value to the variable
print(cartesian)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import product
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
cartesian = ...  # assigns a value to the variable
print(cartesian)
from itertools import product
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
cartesian = ...  # assigns a value to the variable
print(cartesian)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → cartesian = ...  # stored in memory
#   → print(cartesian)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → cartesian = ...  # stored in memory
#   → print(cartesian)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → cartesian = ...  # stored in memory
#   → print(cartesian)

"""

"""
Problem 1555:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import cycle
    colors = ['red', 'green', 'blue']
    color_cycle = cycle(colors)
    first_5 = [next(color_cycle) for _ in range(5)]
    print(first_5)
#
# Built-in functions: cycle, next, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import cycle
colors = ...  # assigns a value to the variable
color_cycle = ...  # assigns a value to the variable
first_5 = ...  # assigns a value to the variable
print(first_5)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import cycle
colors = ...  # assigns a value to the variable
color_cycle = ...  # assigns a value to the variable
first_5 = ...  # assigns a value to the variable
print(first_5)
from itertools import cycle
colors = ...  # assigns a value to the variable
color_cycle = ...  # assigns a value to the variable
first_5 = ...  # assigns a value to the variable
print(first_5)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → colors = ...  # stored in memory
#   → color_cycle = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → colors = ...  # stored in memory
#   → color_cycle = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → colors = ...  # stored in memory
#   → color_cycle = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)

"""

"""
Problem 1556:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def main():  # defines a function with no parameters
    pass
#
# Built-in functions: list, repeat()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
main()  # function body runs with no arguments
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import repeat
repeated = ...  # assigns a value to the variable
print(repeated)
def main():  # stored in memory, not executed yet
main()  # calls main()
#   → pass
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → repeated = ...  # stored in memory
#   → print(repeated)
#   → main()  # jumps into main(), runs body, returns

"""

"""
Problem 1557:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import islice
    numbers = range(10)
    first_5 = list(islice(numbers, 5))
    print(first_5)
#
# Built-in functions: islice, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import islice
numbers = ...  # assigns a value to the variable
first_5 = ...  # assigns a value to the variable
print(first_5)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import islice
numbers = ...  # assigns a value to the variable
first_5 = ...  # assigns a value to the variable
print(first_5)
from itertools import islice
numbers = ...  # assigns a value to the variable
first_5 = ...  # assigns a value to the variable
print(first_5)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → numbers = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → numbers = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)

"""

"""
Problem 1558:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: iter, list, tee()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import tee
numbers = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(list(...), list(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(list(...), list(...))

"""

"""
Problem 1559:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import accumulate
    numbers = [1, 2, 3, 4, 5]
    cumulative = list(accumulate(numbers))
    print(cumulative)
#
# Built-in functions: accumulate, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import accumulate
numbers = ...  # assigns a value to the variable
cumulative = ...  # assigns a value to the variable
print(cumulative)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import accumulate
numbers = ...  # assigns a value to the variable
cumulative = ...  # assigns a value to the variable
print(cumulative)
from itertools import accumulate
numbers = ...  # assigns a value to the variable
cumulative = ...  # assigns a value to the variable
print(cumulative)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → cumulative = ...  # stored in memory
#   → print(cumulative)
#   → numbers = ...  # stored in memory
#   → cumulative = ...  # stored in memory
#   → print(cumulative)
#   → numbers = ...  # stored in memory
#   → cumulative = ...  # stored in memory
#   → print(cumulative)

"""

"""
Problem 1560:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from dataclasses import dataclass
    @dataclass
    class Person:
        name: str
        age: int
    person = Person('Alice', 30)
    print(person)
#
class Person:  # defines a new type
#
# Built-in functions: Person()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from dataclasses import dataclass
class Person:  # class definition stored
person = ...  # assigns a value to the variable
print(person)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Person(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → person = ...  # stored in memory
#   → print(person)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))

"""

"""
Problem 1561:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from dataclasses import dataclass, field
    @dataclass
    class Student:
        name: str
        age: int = field(default=18)
        grades: list = field(default_factory=list)
    student = Student('Bob')
    print(student)
#
class Student:  # defines a new type
#
# Built-in functions: Student, field()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from dataclasses import dataclass, field
class Student:  # class definition stored
student = ...  # assigns a value to the variable
print(student)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Student(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Student are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → student = ...  # stored in memory
#   → print(student)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Student(...))

"""

"""
Problem 1562:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from dataclasses import dataclass
    @dataclass(frozen=True)
    class Point:
        x: int
        y: int
    point = Point(3, 4)
    print(point)
#
class Point:  # defines a new type
#
# Built-in functions: Point, dataclass()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from dataclasses import dataclass
class Point:  # class definition stored
point = ...  # assigns a value to the variable
print(point)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Point(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Point are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → point = ...  # stored in memory
#   → print(point)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Point(...))

"""

"""
Problem 1563:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def sum_numbers(numbers):  # defines a function with parameters numbers
    return sum(numbers)  # return sends the result back to the caller
#
# Keywords: for
# Built-in functions: reduce, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
sum_numbers(?)  # arguments evaluated first, then function body runs
sum_numbers(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from typing import List
def sum_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def sum_numbers(numbers):  # stored in memory, not executed yet
def sum_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(sum_numbers(...))  # sum_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → from functools import reduce
#   → import operator
#   → return reduce(operator.add, numbers, 0)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_numbers(...))  # sum_numbers() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done

"""

"""
Problem 1564:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def greet(name):  # defines a function with parameters name
    if name is None:
        return 'Hello, Guest!'
    return f'Hello, {name}!'  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(greet('Alice'))  # greet() runs first, then print() outputs the result
print(greet())  # greet() runs first, then print() outputs the result
greet(name)  # arguments evaluated first, then function body runs
print(greet('Bob'))  # greet() runs first, then print() outputs the result
name  None  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from typing import Optional
def greet(name):  # stored in memory, not executed yet
print(greet(...), greet(...))  # greet() runs with name='Alice'
#   → return 'Hello, Guest!'
def greet(name):  # stored in memory, not executed yet
def greet(name):  # stored in memory, not executed yet
name = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(greet(...))  # greet() runs with name='Bob'
#   → return 'Hello, Guest!'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only greet is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(greet(...), greet(...))  # greet() runs first → value goes to print()
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greet(...))  # greet() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1565:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def process_value(value):  # defines a function with parameters value
    return str(value)  # return sends the result back to the caller
#
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(process_value(42))  # process_value() runs first, then print() outputs the result
print(process_value('hello'))  # process_value() runs first, then print() outputs the result
process_value(value)  # arguments evaluated first, then function body runs
print(process_value(25))  # process_value() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from typing import Union
def process_value(value):  # stored in memory, not executed yet
print(process_value(...), process_value(...))  # process_value() runs with value=42
#   → return str(value)
def process_value(value):  # stored in memory, not executed yet
def process_value(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(process_value(...))  # process_value() runs with value=25
#   → return str(value)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only process_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(process_value(...), process_value(...))  # process_value() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process_value(...))  # process_value() runs first → value goes to print()

"""

"""
Problem 1566:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    numbers = [1, 2, 3, 4, 5]
    while (n := len(numbers)) > 0:
        numbers.pop()
        print(f'Remaining: {n}')
#
# Keywords: while
# Operators: >
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? > 0  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
numbers = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
numbers = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → numbers = ...  # stored in memory
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1567:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def expensive_function(x):  # defines a function with parameters x
    return x * 2  # return sends the result back to the caller
#
# Keywords: for
# Operators: * >
# Built-in functions: pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
expensive_function(x)  # arguments evaluated first, then function body runs
print(expensive_function(3))  # expensive_function() runs first, then print() outputs the result
x * 2  # x → then 2 → then *
? > 4  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def expensive_function(x):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
def expensive_function(x):  # stored in memory, not executed yet
def expensive_function(x):  # stored in memory, not executed yet
x = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(expensive_function(...))  # expensive_function() runs with x=3
#   → return pow(x, 2)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only expensive_function is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → x = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(expensive_function(...))  # expensive_function() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1568:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def handle_command(cmd):  # defines a function with parameters cmd
    match cmd:
        case 'start':
            return 'Starting...'
        case 'stop':
            return 'Stopping...'
        case 'pause':
            return 'Paused'
        case _:
            return 'Unknown command'
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(handle_command('start'))  # handle_command() runs first, then print() outputs the result
handle_command(cmd)  # arguments evaluated first, then function body runs
print(handle_command(25))  # handle_command() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def handle_command(cmd):  # stored in memory, not executed yet
print(handle_command(...))  # handle_command() runs with cmd='start'
#   → return 'Starting...'
def handle_command(cmd):  # stored in memory, not executed yet
def handle_command(cmd):  # stored in memory, not executed yet
cmd = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(handle_command(...))  # handle_command() runs with cmd=25
#   → return 'Starting...'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only handle_command is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(handle_command(...))  # handle_command() runs first → value goes to print()
#   → cmd = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(handle_command(...))  # handle_command() runs first → value goes to print()

"""

"""
Problem 1569:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def process_point(point):  # defines a function with parameters point
    match point:
        case [0, 0]:
            return 'Origin'
        case [x, 0]:
            return f'On x-axis at {x}'
        case [0, y]:
            return f'On y-axis at {y}'
        case [x, y]:
            return f'Point at ({x}, {y})'
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(process_point(?))  # process_point() runs first, then print() outputs the result
process_point(point)  # arguments evaluated first, then function body runs
print(process_point(25))  # process_point() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def process_point(point):  # stored in memory, not executed yet
print(process_point(...))  # process_point() runs with point=?
#   → return 'Origin'
def process_point(point):  # stored in memory, not executed yet
def process_point(point):  # stored in memory, not executed yet
point = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(process_point(...))  # process_point() runs with point=25
#   → return 'Origin'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only process_point is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(process_point(...))  # process_point() runs first → value goes to print()
#   → point = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process_point(...))  # process_point() runs first → value goes to print()

"""

"""
Problem 1570:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class Circle:
    
        def __init__(self, radius):
            self.radius = radius
    
        @property
        def area(self):
            return 3.14159 * self.radius ** 2
    circle = Circle(5)
    print(circle.area)
#
class Circle:  # defines a new type
    def __init__(self, radius):  # method of Circle
        self.radius = radius  # method of Circle
    @property  # method of Circle
    def area(self):  # method of Circle
        return 3.14159 * self.radius ** 2  # method of Circle
#
# Operators: * **
# Built-in functions: Circle()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
3.14159 * ?  # 3.14159 → then ? → then *
self.radius ** 2  # self.radius → then 2 → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Circle:  # class definition stored
circle = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Circle(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Circle are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → circle = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Circle(...))

"""

"""
Problem 1571:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class Temperature:
    
        def __init__(self):
            self._celsius = 0
    
        @property
        def celsius(self):
            return self._celsius
    
        @celsius.setter
        def celsius(self, value):
            if value < -273.15:
                raise ValueError('Temperature cannot be below absolute zero')
            self._celsius = value
    temp = Temperature()
    temp.celsius = 25
    print(temp.celsius)
#
class Temperature:  # defines a new type
    def __init__(self):  # method of Temperature
        self._celsius = 0  # method of Temperature
    @property  # method of Temperature
    def celsius(self):  # method of Temperature
        return self._celsius  # method of Temperature
    @celsius.setter  # method of Temperature
    def celsius(self, value):  # method of Temperature
        if value < -273.15:  # method of Temperature
            raise ValueError('Temperature cannot be below absolute zero')  # method of Temperature
        self._celsius = value  # method of Temperature
#
# Keywords: if/else
# Operators: <
# Built-in functions: Temperature, ValueError()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
value < ?  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Temperature:  # class definition stored
temp = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Temperature(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Temperature are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → temp = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Temperature(...))
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1572:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
class Book:  # defines a new type
    def __init__(self, title, author):  # method of Book
        self.title = title  # method of Book
        self.author = author  # method of Book
    def __str__(self):  # method of Book
        return f'{self.title} by {self.author}'  # method of Book
#
# Built-in functions: Book, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Book:  # class definition stored
book = ...  # assigns a value to the variable
print(str(...))
obj = ...  # assigns a value to the variable
print(obj)
print(Book(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Book are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → book = ...  # stored in memory
#   → print(str(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Book(...))

"""

"""
Problem 1573:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
class Point:  # defines a new type
    def __init__(self, x, y):  # method of Point
        self.x = x  # method of Point
        self.y = y  # method of Point
    def __repr__(self):  # method of Point
        return f'Point(x={self.x}, y={self.y})'  # method of Point
#
# Built-in functions: Point, repr()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Point:  # class definition stored
point = ...  # assigns a value to the variable
print(repr(...))
obj = ...  # assigns a value to the variable
print(obj)
print(Point(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Point are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → point = ...  # stored in memory
#   → print(repr(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Point(...))

"""

"""
Problem 1574:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class Person:
    
        def __init__(self, name, age):
            self.name = name
            self.age = age
    
        def __eq__(self, other):
            if not isinstance(other, Person):
                return False
            return self.name == other.name and self.age == other.age
    p1 = Person('Alice', 30)
    p2 = Person('Alice', 30)
    print(p1 == p2)
#
class Person:  # defines a new type
    def __init__(self, name, age):  # method of Person
        self.name = name  # method of Person
        self.age = age  # method of Person
    def __eq__(self, other):  # method of Person
        if not isinstance(other, Person):  # method of Person
            return False  # method of Person
        return self.name == other.name and self.age == other.age  # method of Person
#
# Keywords: if/else
# Operators: == and
# Built-in functions: Person, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
p1 == p2  # evaluated as True or False → picks which branch
self.name == other.name  # evaluated as True or False → picks which branch
self.age == other.age  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Person:  # class definition stored
p1 = ...  # assigns a value to the variable
p2 = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Person(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1575:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
class Point:  # defines a new type
    def __init__(self, x, y):  # method of Point
        self.x = x  # method of Point
        self.y = y  # method of Point
    def __hash__(self):  # method of Point
        return hash((self.x, self.y))  # method of Point
    def __eq__(self, other):  # method of Point
        return isinstance(other, Point) and self.x == other.x and (self.y == other.y)  # method of Point
#
# Operators: == and
# Built-in functions: Point, hash, isinstance, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
self.x == other.x  # evaluated as True or False → picks which branch
self.y == other.y  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Point:  # class definition stored
point = ...  # assigns a value to the variable
points_set = ...  # assigns a value to the variable
print(len(...))
obj = ...  # assigns a value to the variable
print(obj)
print(Point(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Point are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → point = ...  # stored in memory
#   → points_set = ...  # stored in memory
#   → print(len(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Point(...))

"""

"""
Problem 1576:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
class Stack:  # defines a new type
    def __init__(self):  # method of Stack
        self.items = []  # method of Stack
    def push(self, item):  # method of Stack
        self.items.append(item)  # method of Stack
    def __len__(self):  # method of Stack
        return len(self.items)  # method of Stack
#
# Built-in functions: Stack, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Stack:  # class definition stored
stack = ...  # assigns a value to the variable
stack.push(1)
stack.push(2)
print(len(...))
obj = ...  # assigns a value to the variable
print(obj)
print(Stack(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Stack are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → stack = ...  # stored in memory
#   → stack.push(1)
#   → stack.push(2)
#   → print(len(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Stack(...))

"""

"""
Problem 1577:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class MyList:
    
        def __init__(self, items):
            self.items = items
    
        def __getitem__(self, index):
            return self.items[index]
    my_list = MyList([10, 20, 30, 40])
    print(my_list[2])
#
class MyList:  # defines a new type
    def __init__(self, items):  # method of MyList
        self.items = items  # method of MyList
    def __getitem__(self, index):  # method of MyList
        return self.items[index]  # method of MyList
#
# Built-in functions: MyList()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class MyList:  # class definition stored
my_list = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(MyList(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyList are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyList(...))

"""

"""
Problem 1578:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class Timer:
    
        def __enter__(self):
            import time
            self.start = time.time()
            return self
    
        def __exit__(self, exc_type, exc_val, exc_tb):
            import time
            self.elapsed = time.time() - self.start
            print(f'Elapsed time: {self.elapsed:.2f} seconds')
    with Timer():
        sum(range(1000000))
#
class Timer:  # defines a new type
    def __enter__(self):  # method of Timer
        import time  # method of Timer
        self.start = time.time()  # method of Timer
        return self  # method of Timer
    def __exit__(self, exc_type, exc_val, exc_tb):  # method of Timer
        import time  # method of Timer
        self.elapsed = time.time() - self.start  # method of Timer
        print(f'Elapsed time: {self.elapsed:.2f} seconds')  # method of Timer
#
# Operators: -
# Built-in functions: Timer, range, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
time.time(...) - self.start  # time.time(...) → then self.start → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Timer:  # class definition stored
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Timer(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Timer are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Timer(...))

"""

"""
Problem 1579:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from contextlib import ExitStack
    class Resource:
    
        def __enter__(self):
            print('Entering resource')
            return self
    
        def __exit__(self, *args):
            print('Exiting resource')
    with ExitStack() as stack:
        r1 = stack.enter_context(Resource())
        r2 = stack.enter_context(Resource())
        print('Using resources')
#
class Resource:  # defines a new type
    def __enter__(self):  # method of Resource
        print('Entering resource')  # method of Resource
        return self  # method of Resource
    def __exit__(self, *args):  # method of Resource
        print('Exiting resource')  # method of Resource
#
# Built-in functions: ExitStack, Resource()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from contextlib import ExitStack
class Resource:  # class definition stored
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Resource(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Resource are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Resource(...))

"""

"""
Problem 1580:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from pathlib import Path
#
# Built-in functions: Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from pathlib import Path
path = ...  # assigns a value to the variable
exists = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from pathlib import Path
from pathlib import Path
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → path = ...  # stored in memory
#   → exists = ...  # stored in memory
#   → print(?)

"""

"""
Problem 1581:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from pathlib import Path
    path = Path('/home/user/documents/file.txt')
    parent = path.parent
    print(parent)
#
# Built-in functions: Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from pathlib import Path
path = ...  # assigns a value to the variable
parent = ...  # assigns a value to the variable
print(parent)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from pathlib import Path
path = ...  # assigns a value to the variable
parent = ...  # assigns a value to the variable
print(parent)
from pathlib import Path
path = ...  # assigns a value to the variable
parent = ...  # assigns a value to the variable
print(parent)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → path = ...  # stored in memory
#   → parent = ...  # stored in memory
#   → print(parent)
#   → path = ...  # stored in memory
#   → parent = ...  # stored in memory
#   → print(parent)
#   → path = ...  # stored in memory
#   → parent = ...  # stored in memory
#   → print(parent)

"""

"""
Problem 1582:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from pathlib import Path
    base = Path('/home/user')
    full_path = base / 'documents' / 'file.txt'
    print(full_path)
#
# Operators: /
# Built-in functions: Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? / 'file.txt'  # ? → then 'file.txt' → then /
base / 'documents'  # base → then 'documents' → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from pathlib import Path
base = ...  # assigns a value to the variable
full_path = ...  # assigns a value to the variable
print(full_path)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from pathlib import Path
base = ...  # assigns a value to the variable
full_path = ...  # assigns a value to the variable
print(full_path)
from pathlib import Path
base = ...  # assigns a value to the variable
full_path = ...  # assigns a value to the variable
print(full_path)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → base = ...  # stored in memory
#   → full_path = ...  # stored in memory
#   → print(full_path)
#   → base = ...  # stored in memory
#   → full_path = ...  # stored in memory
#   → print(full_path)
#   → base = ...  # stored in memory
#   → full_path = ...  # stored in memory
#   → print(full_path)

"""

"""
Problem 1583:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    import json
    data = {'name': 'Alice', 'age': 30, 'city': 'New York'}
    json_string = json.dumps(data)
    print(json_string)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import json
data = ...  # assigns a value to the variable
json_string = ...  # assigns a value to the variable
print(json_string)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
import json
data = ...  # assigns a value to the variable
json_string = ...  # assigns a value to the variable
print(json_string)
import json
data = ...  # assigns a value to the variable
json_string = ...  # assigns a value to the variable
print(json_string)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → json_string = ...  # stored in memory
#   → print(json_string)
#   → data = ...  # stored in memory
#   → json_string = ...  # stored in memory
#   → print(json_string)
#   → data = ...  # stored in memory
#   → json_string = ...  # stored in memory
#   → print(json_string)

"""

"""
Problem 1584:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import json
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1585:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    import re
    text = 'The cat sat on the mat'
    new_text = re.sub('cat', 'dog', text)
    print(new_text)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import re
text = ...  # assigns a value to the variable
new_text = ...  # assigns a value to the variable
print(new_text)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
import re
text = ...  # assigns a value to the variable
new_text = ...  # assigns a value to the variable
print(new_text)
import re
text = ...  # assigns a value to the variable
new_text = ...  # assigns a value to the variable
print(new_text)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → new_text = ...  # stored in memory
#   → print(new_text)
#   → text = ...  # stored in memory
#   → new_text = ...  # stored in memory
#   → print(new_text)
#   → text = ...  # stored in memory
#   → new_text = ...  # stored in memory
#   → print(new_text)

"""

"""
Problem 1586:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    import re
    text = 'The cat and the hat'
    pattern = '\\b\\w{3}\\b'
    matches = [(m.group(), m.start()) for m in re.finditer(pattern, text)]
    print(matches)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import re
text = ...  # assigns a value to the variable
pattern = ...  # assigns a value to the variable
matches = ...  # assigns a value to the variable
print(matches)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
import re
text = ...  # assigns a value to the variable
pattern = ...  # assigns a value to the variable
matches = ...  # assigns a value to the variable
print(matches)
import re
text = ...  # assigns a value to the variable
pattern = ...  # assigns a value to the variable
matches = ...  # assigns a value to the variable
print(matches)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)

"""

"""
Problem 1587:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: if/else
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import re
text = ...  # assigns a value to the variable
pattern = ...  # assigns a value to the variable
match = ...  # assigns a value to the variable
# if ... → only one branch executes based on condition
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → match = ...  # stored in memory
#   → if condition  # evaluates → runs the True branch
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1588:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: OrderedDict, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import OrderedDict
od = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(list(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → od = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(list(...))

"""

"""
Problem 1589:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: ChainMap, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import ChainMap
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
chain = ...  # assigns a value to the variable
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → chain = ...  # stored in memory
#   → print(dict(...))

"""

"""
Problem 1590:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from collections import namedtuple
    Point = namedtuple('Point', ['x', 'y'])
    p = Point(3, 4)
    print(p.x, p.y)
#
# Built-in functions: Point, namedtuple()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import namedtuple
Point = ...  # assigns a value to the variable
p = ...  # assigns a value to the variable
print(?, ?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from collections import namedtuple
Point = ...  # assigns a value to the variable
p = ...  # assigns a value to the variable
print(?, ?)
from collections import namedtuple
Point = ...  # assigns a value to the variable
p = ...  # assigns a value to the variable
print(?, ?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → Point = ...  # stored in memory
#   → p = ...  # stored in memory
#   → print(?, ?)
#   → Point = ...  # stored in memory
#   → p = ...  # stored in memory
#   → print(?, ?)
#   → Point = ...  # stored in memory
#   → p = ...  # stored in memory
#   → print(?, ?)

"""

"""
Problem 1591:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    matrix = [[i * j for j in range(1, 4)] for i in range(1, 4)]
    print(matrix)
#
# Operators: *
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
i * j  # i → then j → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
matrix = ...  # assigns a value to the variable
print(matrix)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
matrix = ...  # assigns a value to the variable
print(matrix)
matrix = ...  # assigns a value to the variable
print(matrix)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → matrix = ...  # stored in memory
#   → print(matrix)
#   → matrix = ...  # stored in memory
#   → print(matrix)
#   → matrix = ...  # stored in memory
#   → print(matrix)

"""

"""
Problem 1592:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    numbers = range(20)
    filtered = [x for x in numbers if x % 2 == 0 and x % 3 == 0]
    print(filtered)
#
# Operators: % == and
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
x % 3  # x → then 3 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
numbers = ...  # assigns a value to the variable
filtered = ...  # assigns a value to the variable
print(filtered)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
filtered = ...  # assigns a value to the variable
print(filtered)
numbers = ...  # assigns a value to the variable
filtered = ...  # assigns a value to the variable
print(filtered)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
#   → numbers = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
#   → numbers = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)

"""

"""
Problem 1593:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from operator import attrgetter
    class Person:
    
        def __init__(self, name, age):
            self.name = name
            self.age = age
    people = [Person('Alice', 30), Person('Bob', 25), Person('Charlie', 35)]
    sorted_people = sorted(people, key=attrgetter('age'))
    print([p.name for p in sorted_people])
#
class Person:  # defines a new type
    def __init__(self, name, age):  # method of Person
        self.name = name  # method of Person
        self.age = age  # method of Person
#
# Built-in functions: Person, attrgetter, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from operator import attrgetter
class Person:  # class definition stored
people = ...  # assigns a value to the variable
sorted_people = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Person(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → sorted_people = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))

"""

"""
Problem 1594:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from operator import methodcaller
    class Person:
    
        def greet(self):
            return 'Hello'
    people = [Person(), Person(), Person()]
    greetings = list(map(methodcaller('greet'), people))
    print(greetings)
#
class Person:  # defines a new type
    def greet(self):  # method of Person
        return 'Hello'  # method of Person
#
# Built-in functions: Person, list, map, methodcaller()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from operator import methodcaller
class Person:  # class definition stored
people = ...  # assigns a value to the variable
greetings = ...  # assigns a value to the variable
print(greetings)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Person(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → greetings = ...  # stored in memory
#   → print(greetings)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))

"""

"""
Problem 1595:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    text = 'HelloWorld'
    result = text.removeprefix('Hello')
    print(result)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1596:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    text = 'HelloWorld'
    result = text.removesuffix('World')
    print(result)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1597:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    text = 'one.two.three.four'
    parts = text.rsplit('.', 2)
    print(parts)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
text = ...  # assigns a value to the variable
parts = ...  # assigns a value to the variable
print(parts)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
parts = ...  # assigns a value to the variable
print(parts)
text = ...  # assigns a value to the variable
parts = ...  # assigns a value to the variable
print(parts)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)

"""

"""
Problem 1598:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    original = [1, 2, [3, 4]]
    copied = original.copy()
    copied[0] = 10
    print(original, copied)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
original = ...  # assigns a value to the variable
copied = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(original, copied)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
original = ...  # assigns a value to the variable
copied = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(original, copied)
original = ...  # assigns a value to the variable
copied = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(original, copied)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → original = ...  # stored in memory
#   → copied = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(original, copied)
#   → original = ...  # stored in memory
#   → copied = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(original, copied)
#   → original = ...  # stored in memory
#   → copied = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(original, copied)

"""

"""
Problem 1599:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    my_list = [1, 2, 3, 4, 5]
    my_list.clear()
    print(my_list)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
my_list = ...  # assigns a value to the variable
my_list.clear()
print(my_list)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
my_list.clear()
print(my_list)
my_list = ...  # assigns a value to the variable
my_list.clear()
print(my_list)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → my_list.clear()
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.clear()
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.clear()
#   → print(my_list)

"""

"""
Problem 1600:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    d = {'a': 1, 'b': 2, 'c': 3}
    item = d.popitem()
    print(item, d)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
d = ...  # assigns a value to the variable
item = ...  # assigns a value to the variable
print(item, d)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
item = ...  # assigns a value to the variable
print(item, d)
d = ...  # assigns a value to the variable
item = ...  # assigns a value to the variable
print(item, d)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → item = ...  # stored in memory
#   → print(item, d)
#   → d = ...  # stored in memory
#   → item = ...  # stored in memory
#   → print(item, d)
#   → d = ...  # stored in memory
#   → item = ...  # stored in memory
#   → print(item, d)

"""

"""
Problem 1601:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    dict1 = {'a': 1, 'b': 2}
    dict2 = {'c': 3, 'd': 4}
    dict1.update(dict2)
    print(dict1)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
dict1.update(dict2)
print(dict1)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
dict1.update(dict2)
print(dict1)
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
dict1.update(dict2)
print(dict1)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → print(dict1)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → print(dict1)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → print(dict1)

"""

"""
Problem 1602:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    d = {'a': 1, 'b': 2, 'c': 3}
    keys = list(d.keys())
    print(keys)
#
# Built-in functions: list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
d = ...  # assigns a value to the variable
keys = ...  # assigns a value to the variable
print(keys)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
keys = ...  # assigns a value to the variable
print(keys)
d = ...  # assigns a value to the variable
keys = ...  # assigns a value to the variable
print(keys)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → keys = ...  # stored in memory
#   → print(keys)
#   → d = ...  # stored in memory
#   → keys = ...  # stored in memory
#   → print(keys)
#   → d = ...  # stored in memory
#   → keys = ...  # stored in memory
#   → print(keys)

"""

"""
Problem 1603:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    d = {'a': 1, 'b': 2, 'c': 3}
    values = list(d.values())
    print(values)
#
# Built-in functions: list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
d = ...  # assigns a value to the variable
values = ...  # assigns a value to the variable
print(values)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
values = ...  # assigns a value to the variable
print(values)
d = ...  # assigns a value to the variable
values = ...  # assigns a value to the variable
print(values)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → values = ...  # stored in memory
#   → print(values)
#   → d = ...  # stored in memory
#   → values = ...  # stored in memory
#   → print(values)
#   → d = ...  # stored in memory
#   → values = ...  # stored in memory
#   → print(values)

"""

"""
Problem 1604:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    d = {'a': 1, 'b': 2, 'c': 3}
    items = list(d.items())
    print(items)
#
# Built-in functions: list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
d = ...  # assigns a value to the variable
items = ...  # assigns a value to the variable
print(items)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
items = ...  # assigns a value to the variable
print(items)
d = ...  # assigns a value to the variable
items = ...  # assigns a value to the variable
print(items)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)
#   → d = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)
#   → d = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)

"""

"""
Problem 1605:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class CustomError(Exception):
        pass
    try:
        raise CustomError('This is a custom error')
    except CustomError as e:
        print(f'Caught: {e}')
#
class CustomError(Exception):  # defines a new type
#
# Keywords: try/except
# Built-in functions: CustomError()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class CustomError(Exception):  # class definition stored
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(CustomError(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes CustomError are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(CustomError(...))

"""

"""
Problem 1606:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    try:
        try:
            int('not a number')
        except ValueError as e:
            raise TypeError('Conversion failed') from e
    except TypeError as e:
        print(f'Error: {e}, caused by: {e.__cause__}')
#
# Keywords: try/except
# Built-in functions: TypeError, int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):

"""

"""
Problem 1607:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from contextlib import redirect_stdout
from io import StringIO
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1608:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import wraps
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1609:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def main():  # defines a function with no parameters
    pass
#
# Built-in functions: accumulate, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
main()  # function body runs with no arguments
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import accumulate
import operator
numbers = ...  # assigns a value to the variable
cumulative_products = ...  # assigns a value to the variable
print(cumulative_products)
def main():  # stored in memory, not executed yet
main()  # calls main()
#   → pass
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → cumulative_products = ...  # stored in memory
#   → print(cumulative_products)
#   → main()  # jumps into main(), runs body, returns

"""

"""
Problem 1610:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import combinations_with_replacement
    items = ['a', 'b', 'c']
    combs = list(combinations_with_replacement(items, 2))
    print(combs)
#
# Built-in functions: combinations_with_replacement, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import combinations_with_replacement
items = ...  # assigns a value to the variable
combs = ...  # assigns a value to the variable
print(combs)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import combinations_with_replacement
items = ...  # assigns a value to the variable
combs = ...  # assigns a value to the variable
print(combs)
from itertools import combinations_with_replacement
items = ...  # assigns a value to the variable
combs = ...  # assigns a value to the variable
print(combs)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → combs = ...  # stored in memory
#   → print(combs)
#   → items = ...  # stored in memory
#   → combs = ...  # stored in memory
#   → print(combs)
#   → items = ...  # stored in memory
#   → combs = ...  # stored in memory
#   → print(combs)

"""

"""
Problem 1611:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import dropwhile
    numbers = [1, 2, 3, 4, 5, 6, 7, 8]
    result = list(dropwhile(lambda x: x < 5, numbers))
    print(result)
#
# Operators: <
# Built-in functions: dropwhile, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
x < 5  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import dropwhile
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import dropwhile
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
from itertools import dropwhile
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1612:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import takewhile
    numbers = [1, 2, 3, 4, 5, 6, 7, 8]
    result = list(takewhile(lambda x: x < 5, numbers))
    print(result)
#
# Operators: <
# Built-in functions: list, takewhile()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
x < 5  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import takewhile
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import takewhile
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
from itertools import takewhile
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1613:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from itertools import starmap
    import operator
    pairs = [(2, 3), (4, 5), (6, 7)]
    products = list(starmap(operator.mul, pairs))
    print(products)
#
# Built-in functions: list, starmap()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from itertools import starmap
import operator
pairs = ...  # assigns a value to the variable
products = ...  # assigns a value to the variable
print(products)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from itertools import starmap
import operator
pairs = ...  # assigns a value to the variable
products = ...  # assigns a value to the variable
print(products)
from itertools import starmap
import operator
pairs = ...  # assigns a value to the variable
products = ...  # assigns a value to the variable
print(products)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → pairs = ...  # stored in memory
#   → products = ...  # stored in memory
#   → print(products)
#   → pairs = ...  # stored in memory
#   → products = ...  # stored in memory
#   → print(products)
#   → pairs = ...  # stored in memory
#   → products = ...  # stored in memory
#   → print(products)

"""

"""
Problem 1614:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def compare(x, y):  # defines a function with parameters x, y
    return y - x  # return sends the result back to the caller
#
# Operators: + -
# Built-in functions: cmp_to_key, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
compare(x, y)  # arguments evaluated first, then function body runs
print(compare(3, 14))  # compare() runs first, then print() outputs the result
y - x  # y → then x → then -
y + ?  # y → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import cmp_to_key
def compare(x, y):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
sorted_nums = ...  # assigns a value to the variable
print(sorted_nums)
def compare(x, y):  # stored in memory, not executed yet
def compare(x, y):  # stored in memory, not executed yet
x = ...  # assigns a value to the variable
y = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(compare(...))  # compare() runs with x=3, y=14
#   → import operator
#   → return operator.sub(y, x)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only compare is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sorted_nums = ...  # stored in memory
#   → print(sorted_nums)
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(compare(...))  # compare() runs first → value goes to print()

"""

"""
Problem 1615:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def process(value):  # defines a function with parameters value
    return f'Unknown type: {type(value).__name__}'  # return sends the result back to the caller
#
# Built-in functions: type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(process(42))  # process() runs first, then print() outputs the result
print(process('hello'))  # process() runs first, then print() outputs the result
process(value)  # arguments evaluated first, then function body runs
print(process(25))  # process() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import singledispatch
def process(value):  # stored in memory, not executed yet
def _(value):  # stored in memory, not executed yet
def _(value):  # stored in memory, not executed yet
print(process(...), process(...))  # process() runs with value=42
#   → return f'Unknown type: {type(value).__name__}'
def process(value):  # stored in memory, not executed yet
def process(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(process(...))  # process() runs with value=25
#   → return f'Unknown type: {type(value).__name__}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — _, process are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(process(...), process(...))  # process() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process(...))  # process() runs first → value goes to print()

"""

"""
Problem 1616:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def process_scores(scores):  # defines a function with parameters scores
    return sum(scores.values())  # return sends the result back to the caller
#
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
process_scores(?)  # arguments evaluated first, then function body runs
process_scores(scores)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from typing import Dict
def process_scores(scores):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def process_scores(scores):  # stored in memory, not executed yet
def process_scores(scores):  # stored in memory, not executed yet
scores = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(process_scores(...))  # process_scores() runs with scores=[4, 1, 8, 6, 3]
#   → return sum(scores.values())
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only process_scores is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → scores = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process_scores(...))  # process_scores() runs first → value goes to print()

"""

"""
Problem 1617:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_name_age():  # defines a function with no parameters
    return ('Alice', 30)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_name_age()  # function body runs with no arguments
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from typing import Tuple
def get_name_age():  # stored in memory, not executed yet
? = ...  # assigns a value to the variable
print(?)
def get_name_age():  # stored in memory, not executed yet
def get_name_age():  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_name_age is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → ? = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 1618:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def apply_operation(x, y, op):  # defines a function with parameters x, y, op
    return op(x, y)  # return sends the result back to the caller
#
# Operators: *
# Built-in functions: op()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
apply_operation(5, 3, ?)  # arguments evaluated first, then function body runs
apply_operation(x, y, op)  # arguments evaluated first, then function body runs
print(apply_operation(3, 14, 25))  # apply_operation() runs first, then print() outputs the result
a * b  # a → then b → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from typing import Callable
def apply_operation(x, y, op):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def apply_operation(x, y, op):  # stored in memory, not executed yet
def apply_operation(x, y, op):  # stored in memory, not executed yet
x = ...  # assigns a value to the variable
y = ...  # assigns a value to the variable
op = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(apply_operation(...))  # apply_operation() runs with x=3, y=14, op=25
#   → return op(x, y)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only apply_operation is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → op = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(apply_operation(...))  # apply_operation() runs first → value goes to print()

"""

"""
Problem 1619:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def categorize(value):  # defines a function with parameters value
    match value:
        case x if x < 0:
            return 'Negative'
        case x if x == 0:
            return 'Zero'
        case x if x > 0:
            return 'Positive'
#
# Operators: < > ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(categorize(?))  # categorize() runs first, then print() outputs the result
print(categorize(0))  # categorize() runs first, then print() outputs the result
print(categorize(5))  # categorize() runs first, then print() outputs the result
categorize(value)  # arguments evaluated first, then function body runs
print(categorize(25))  # categorize() runs first, then print() outputs the result
x < 0  # evaluated as True or False → picks which branch
x == 0  # evaluated as True or False → picks which branch
x > 0  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def categorize(value):  # stored in memory, not executed yet
print(categorize(...), categorize(...), categorize(...))  # categorize() runs with value=?
#   → return 'Negative'
def categorize(value):  # stored in memory, not executed yet
def categorize(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(categorize(...))  # categorize() runs with value=25
#   → return 'Negative'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only categorize is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(categorize(...), categorize(...), categorize(...))  # categorize() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(categorize(...))  # categorize() runs first → value goes to print()

"""

"""
Problem 1620:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def handle_status(code):  # defines a function with parameters code
    match code:
        case 200:
            return 'OK'
        case 404:
            return 'Not Found'
        case 500:
            return 'Server Error'
        case _:
            return 'Unknown Status'
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(handle_status(200))  # handle_status() runs first, then print() outputs the result
print(handle_status(999))  # handle_status() runs first, then print() outputs the result
handle_status(code)  # arguments evaluated first, then function body runs
print(handle_status(25))  # handle_status() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def handle_status(code):  # stored in memory, not executed yet
print(handle_status(...), handle_status(...))  # handle_status() runs with code=200
#   → return 'OK'
def handle_status(code):  # stored in memory, not executed yet
def handle_status(code):  # stored in memory, not executed yet
code = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(handle_status(...))  # handle_status() runs with code=25
#   → return 'OK'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only handle_status is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(handle_status(...), handle_status(...))  # handle_status() runs first → value goes to print()
#   → code = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(handle_status(...))  # handle_status() runs first → value goes to print()

"""

"""
Problem 1621:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class Person:
    
        def __init__(self, name):
            self._name = name
    
        @property
        def name(self):
            return self._name
    
        @name.deleter
        def name(self):
            print('Deleting name')
            self._name = None
    p = Person('Alice')
    del p.name
    print(p.name)
#
class Person:  # defines a new type
    def __init__(self, name):  # method of Person
        self._name = name  # method of Person
    @property  # method of Person
    def name(self):  # method of Person
        return self._name  # method of Person
    @name.deleter  # method of Person
    def name(self):  # method of Person
        print('Deleting name')  # method of Person
        self._name = None  # method of Person
#
# Built-in functions: Person()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class Person:  # class definition stored
p = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(Person(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → p = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))

"""

"""
Problem 1622:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class MyDict:
    
        def __init__(self):
            self._data = {}
    
        def __setitem__(self, key, value):
            self._data[key] = value
    
        def __getitem__(self, key):
            return self._data[key]
    my_dict = MyDict()
    my_dict['key'] = 'value'
    print(my_dict['key'])
#
class MyDict:  # defines a new type
    def __init__(self):  # method of MyDict
        self._data = {}  # method of MyDict
    def __setitem__(self, key, value):  # method of MyDict
        self._data[key] = value  # method of MyDict
    def __getitem__(self, key):  # method of MyDict
        return self._data[key]  # method of MyDict
#
# Built-in functions: MyDict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class MyDict:  # class definition stored
my_dict = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(MyDict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyDict are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_dict = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyDict(...))

"""

"""
Problem 1623:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class MyList:
    
        def __init__(self, items):
            self.items = items
    
        def __delitem__(self, index):
            del self.items[index]
    my_list = MyList([1, 2, 3, 4, 5])
    del my_list[2]
    print(my_list.items)
#
class MyList:  # defines a new type
    def __init__(self, items):  # method of MyList
        self.items = items  # method of MyList
    def __delitem__(self, index):  # method of MyList
        del self.items[index]  # method of MyList
#
# Built-in functions: MyList()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class MyList:  # class definition stored
my_list = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(MyList(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyList are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyList(...))

"""

"""
Problem 1624:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    class MyContainer:
    
        def __init__(self, items):
            self.items = items
    
        def __contains__(self, item):
            return item in self.items
    container = MyContainer([1, 2, 3, 4, 5])
    print(3 in container)
#
class MyContainer:  # defines a new type
    def __init__(self, items):  # method of MyContainer
        self.items = items  # method of MyContainer
    def __contains__(self, item):  # method of MyContainer
        return item in self.items  # method of MyContainer
#
# Operators: in
# Built-in functions: MyContainer()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
3 in container  # evaluated as True or False → picks which branch
item in self.items  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class MyContainer:  # class definition stored
container = ...  # assigns a value to the variable
print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
obj = ...  # assigns a value to the variable
print(obj)
print(MyContainer(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyContainer are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → container = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyContainer(...))

"""

"""
Problem 1625:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
class CountDown:  # defines a new type
    def __init__(self, start):  # method of CountDown
        self.current = start  # method of CountDown
    def __iter__(self):  # method of CountDown
        return self  # method of CountDown
    def __next__(self):  # method of CountDown
        if self.current <= 0:  # method of CountDown
            raise StopIteration  # method of CountDown
        self.current -= 1  # method of CountDown
        return self.current + 1  # method of CountDown
#
# Keywords: if/else
# Operators: + <=
# Built-in functions: CountDown, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
self.current <= 0  # evaluated as True or False → picks which branch
self.current + 1  # self.current → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
class CountDown:  # class definition stored
counter = ...  # assigns a value to the variable
print(list(...))
obj = ...  # assigns a value to the variable
print(obj)
print(CountDown(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes CountDown are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → counter = ...  # stored in memory
#   → print(list(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(CountDown(...))
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1626:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def my_context():  # defines a function with no parameters
    print('Entering context')
    yield 'context value'
    print('Exiting context')
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
my_context()  # function body runs with no arguments
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from contextlib import contextmanager
def my_context():  # stored in memory, not executed yet
def my_context():  # stored in memory, not executed yet
def my_context():  # stored in memory, not executed yet
print('Entering context')
# evaluates expression
print('Exiting context')
print('Entering context')
# evaluates expression
print('Exiting context')
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only my_context is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Entering context')
#   → print('Exiting context')
#   → print('Entering context')
#   → print('Exiting context')

"""

"""
Problem 1627:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from pathlib import Path
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1628:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from pathlib import Path
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1629:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from pathlib import Path
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1630:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import json
from pathlib import Path
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1631:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import json
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1632:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    import re
    pattern = re.compile('\\d+')
    text = 'I have 3 cats and 2 dogs'
    matches = pattern.findall(text)
    print(matches)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import re
pattern = ...  # assigns a value to the variable
text = ...  # assigns a value to the variable
matches = ...  # assigns a value to the variable
print(matches)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
import re
pattern = ...  # assigns a value to the variable
text = ...  # assigns a value to the variable
matches = ...  # assigns a value to the variable
print(matches)
import re
pattern = ...  # assigns a value to the variable
text = ...  # assigns a value to the variable
matches = ...  # assigns a value to the variable
print(matches)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → pattern = ...  # stored in memory
#   → text = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → pattern = ...  # stored in memory
#   → text = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → pattern = ...  # stored in memory
#   → text = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)

"""

"""
Problem 1633:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    import re
    text = 'apple,banana;cherry:date'
    parts = re.split('[,;:]', text)
    print(parts)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import re
text = ...  # assigns a value to the variable
parts = ...  # assigns a value to the variable
print(parts)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
import re
text = ...  # assigns a value to the variable
parts = ...  # assigns a value to the variable
print(parts)
import re
text = ...  # assigns a value to the variable
parts = ...  # assigns a value to the variable
print(parts)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)

"""

"""
Problem 1634:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: OrderedDict, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import OrderedDict
od = ...  # assigns a value to the variable
od.move_to_end('a')
print(list(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → od = ...  # stored in memory
#   → od.move_to_end('a')
#   → print(list(...))

"""

"""
Problem 1635:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: ChainMap, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import ChainMap
dict1 = ...  # assigns a value to the variable
dict2 = ...  # assigns a value to the variable
chain = ...  # assigns a value to the variable
new_chain = ...  # assigns a value to the variable
print(dict(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → chain = ...  # stored in memory
#   → new_chain = ...  # stored in memory
#   → print(dict(...))

"""

"""
Problem 1636:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from collections import namedtuple
    Point = namedtuple('Point', ['x', 'y'])
    p1 = Point(3, 4)
    p2 = p1._replace(x=10)
    print(p1, p2)
#
# Built-in functions: Point, namedtuple()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from collections import namedtuple
Point = ...  # assigns a value to the variable
p1 = ...  # assigns a value to the variable
p2 = ...  # assigns a value to the variable
print(p1, p2)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from collections import namedtuple
Point = ...  # assigns a value to the variable
p1 = ...  # assigns a value to the variable
p2 = ...  # assigns a value to the variable
print(p1, p2)
from collections import namedtuple
Point = ...  # assigns a value to the variable
p1 = ...  # assigns a value to the variable
p2 = ...  # assigns a value to the variable
print(p1, p2)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → Point = ...  # stored in memory
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(p1, p2)
#   → Point = ...  # stored in memory
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(p1, p2)
#   → Point = ...  # stored in memory
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(p1, p2)

"""

"""
Problem 1637:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    table = {f'{i}x{j}': i * j for i in range(1, 4) for j in range(1, 4)}
    print(table)
#
# Operators: *
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
i * j  # i → then j → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
table = ...  # assigns a value to the variable
print(table)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
table = ...  # assigns a value to the variable
print(table)
table = ...  # assigns a value to the variable
print(table)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → table = ...  # stored in memory
#   → print(table)
#   → table = ...  # stored in memory
#   → print(table)
#   → table = ...  # stored in memory
#   → print(table)

"""

"""
Problem 1638:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
strings = ...  # assigns a value to the variable
unique_chars = ...  # assigns a value to the variable
print(sorted(...))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → strings = ...  # stored in memory
#   → unique_chars = ...  # stored in memory
#   → print(sorted(...))

"""

"""
Problem 1639:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from functools import reduce
    import operator
    numbers = [1, 2, 3, 4, 5]
    total = reduce(operator.add, numbers)
    print(total)
#
# Built-in functions: reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import reduce
import operator
numbers = ...  # assigns a value to the variable
total = ...  # assigns a value to the variable
print(total)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from functools import reduce
import operator
numbers = ...  # assigns a value to the variable
total = ...  # assigns a value to the variable
print(total)
from functools import reduce
import operator
numbers = ...  # assigns a value to the variable
total = ...  # assigns a value to the variable
print(total)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → numbers = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → numbers = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)

"""

"""
Problem 1640:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    from functools import reduce
    import operator
    numbers = [2, 3, 4]
    product = reduce(operator.mul, numbers)
    print(product)
#
# Built-in functions: reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from functools import reduce
import operator
numbers = ...  # assigns a value to the variable
product = ...  # assigns a value to the variable
print(product)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
from functools import reduce
import operator
numbers = ...  # assigns a value to the variable
product = ...  # assigns a value to the variable
print(product)
from functools import reduce
import operator
numbers = ...  # assigns a value to the variable
product = ...  # assigns a value to the variable
print(product)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)

"""

"""
Problem 1641:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
str1.casefold(...) == str2.casefold(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
str1 = ...  # assigns a value to the variable
str2 = ...  # assigns a value to the variable
print(?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → print(?)

"""

"""
Problem 1642:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    text = 'Hello, World!'
    encoded = text.encode('utf-8')
    print(encoded)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
text = ...  # assigns a value to the variable
encoded = ...  # assigns a value to the variable
print(encoded)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
encoded = ...  # assigns a value to the variable
print(encoded)
text = ...  # assigns a value to the variable
encoded = ...  # assigns a value to the variable
print(encoded)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → encoded = ...  # stored in memory
#   → print(encoded)
#   → text = ...  # stored in memory
#   → encoded = ...  # stored in memory
#   → print(encoded)
#   → text = ...  # stored in memory
#   → encoded = ...  # stored in memory
#   → print(encoded)

"""

"""
Problem 1643:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    bytes_data = b'Hello, World!'
    decoded = bytes_data.decode('utf-8')
    print(decoded)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
bytes_data = ...  # assigns a value to the variable
decoded = ...  # assigns a value to the variable
print(decoded)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
bytes_data = ...  # assigns a value to the variable
decoded = ...  # assigns a value to the variable
print(decoded)
bytes_data = ...  # assigns a value to the variable
decoded = ...  # assigns a value to the variable
print(decoded)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → bytes_data = ...  # stored in memory
#   → decoded = ...  # stored in memory
#   → print(decoded)
#   → bytes_data = ...  # stored in memory
#   → decoded = ...  # stored in memory
#   → print(decoded)
#   → bytes_data = ...  # stored in memory
#   → decoded = ...  # stored in memory
#   → print(decoded)

"""

"""
Problem 1644:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    my_list = [1, 2, 3]
    my_list.extend([4, 5, 6])
    print(my_list)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
my_list = ...  # assigns a value to the variable
my_list.extend(?)
print(my_list)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
my_list.extend(?)
print(my_list)
my_list = ...  # assigns a value to the variable
my_list.extend(?)
print(my_list)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → my_list.extend(?)
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.extend(?)
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.extend(?)
#   → print(my_list)

"""

"""
Problem 1645:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    my_list = [10, 20, 30, 20, 40]
    index = my_list.index(20)
    print(index)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
my_list = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
print(index)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
print(index)
my_list = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
print(index)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → print(index)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → print(index)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → print(index)

"""

"""
Problem 1646:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    keys = ['a', 'b', 'c']
    d = dict.fromkeys(keys, 0)
    print(d)
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
keys = ...  # assigns a value to the variable
d = ...  # assigns a value to the variable
print(d)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
keys = ...  # assigns a value to the variable
d = ...  # assigns a value to the variable
print(d)
keys = ...  # assigns a value to the variable
d = ...  # assigns a value to the variable
print(d)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → keys = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
#   → keys = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
#   → keys = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)

"""

"""
Problem 1647:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    try:
        result = 10 / 2
    except ZeroDivisionError:
        print('Division by zero')
    else:
        print(f'Result: {result}')
#
# Keywords: try/except
# Operators: /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
10 / 2  # 10 → then 2 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):

"""

"""
Problem 1648:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    try:
        result = 10 / 0
    except ZeroDivisionError:
        print('Caught division by zero')
    finally:
        print('This always executes')
#
# Keywords: try/except
# Operators: /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
10 / 0  # 10 → then 0 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):

"""

"""
Problem 1649:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
from contextlib import suppress
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1650:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def subtract_numbers(a, b):  # defines a function with parameters a, b
    return a - b  # return sends the result back to the caller
#
# Operators: + -
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
subtract_numbers(10, 3)  # arguments evaluated first, then function body runs
subtract_numbers(a, b)  # arguments evaluated first, then function body runs
print(subtract_numbers(20, 30))  # subtract_numbers() runs first, then print() outputs the result
a - b  # a → then b → then -
a + ?  # a → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def subtract_numbers(a, b):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def subtract_numbers(a, b):  # stored in memory, not executed yet
def subtract_numbers(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(subtract_numbers(...))  # subtract_numbers() runs with a=20, b=30
#   → import operator
#   → return operator.sub(a, b)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only subtract_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(subtract_numbers(...))  # subtract_numbers() runs first → value goes to print()

"""

"""
Problem 1651:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def divide_numbers(a, b):  # defines a function with parameters a, b
    return a / b  # return sends the result back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
divide_numbers(10, 2)  # arguments evaluated first, then function body runs
divide_numbers(a, b)  # arguments evaluated first, then function body runs
print(divide_numbers(20, 30))  # divide_numbers() runs first, then print() outputs the result
a / b  # a → then b → then /
a * ?  # a → then ? → then *
1 / b  # 1 → then b → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def divide_numbers(a, b):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def divide_numbers(a, b):  # stored in memory, not executed yet
def divide_numbers(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(divide_numbers(...))  # divide_numbers() runs with a=20, b=30
#   → from fractions import Fraction
#   → return float(Fraction(a, b))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divide_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divide_numbers(...))  # divide_numbers() runs first → value goes to print()

"""

"""
Problem 1652:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_remainder(a, b):  # defines a function with parameters a, b
    return a % b  # return sends the result back to the caller
#
# Operators: % * - //
# Built-in functions: divmod()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_remainder(10, 3)  # arguments evaluated first, then function body runs
calculate_remainder(a, b)  # arguments evaluated first, then function body runs
print(calculate_remainder(20, 30))  # calculate_remainder() runs first, then print() outputs the result
a % b  # a → then b → then %
a - ?  # a → then ? → then -
b * ?  # b → then ? → then *
a // b  # a → then b → then //
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_remainder(a, b):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_remainder(a, b):  # stored in memory, not executed yet
def calculate_remainder(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_remainder(...))  # calculate_remainder() runs with a=20, b=30
#   → return divmod(a, b)[1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_remainder is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_remainder(...))  # calculate_remainder() runs first → value goes to print()

"""

"""
Problem 1653:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_power(base, exponent):  # defines a function with parameters base, exponent
    return base ** exponent  # return sends the result back to the caller
#
# Operators: **
# Built-in functions: pow()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_power(2, 3)  # arguments evaluated first, then function body runs
calculate_power(base, exponent)  # arguments evaluated first, then function body runs
print(calculate_power(10, 25))  # calculate_power() runs first, then print() outputs the result
base ** exponent  # base → then exponent → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_power(base, exponent):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_power(base, exponent):  # stored in memory, not executed yet
def calculate_power(base, exponent):  # stored in memory, not executed yet
base = ...  # assigns a value to the variable
exponent = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_power(...))  # calculate_power() runs with base=10, exponent=25
#   → import math
#   → return math.pow(base, exponent)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_power is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → base = ...  # stored in memory
#   → exponent = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_power(...))  # calculate_power() runs first → value goes to print()

"""

"""
Problem 1654:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_circle_area(radius):  # defines a function with parameters radius
    return 3.14 * radius * radius  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_circle_area(5)  # arguments evaluated first, then function body runs
calculate_circle_area(radius)  # arguments evaluated first, then function body runs
print(calculate_circle_area(14))  # calculate_circle_area() runs first, then print() outputs the result
? * radius  # ? → then radius → then *
3.14 * radius  # 3.14 → then radius → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_circle_area(radius):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_circle_area(radius):  # stored in memory, not executed yet
def calculate_circle_area(radius):  # stored in memory, not executed yet
radius = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_circle_area(...))  # calculate_circle_area() runs with radius=14
#   → return 3.14 * radius * radius
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_circle_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → radius = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_circle_area(...))  # calculate_circle_area() runs first → value goes to print()

"""

"""
Problem 1655:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_rectangle_area(length, width):  # defines a function with parameters length, width
    return length * width  # return sends the result back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_rectangle_area(5, 3)  # arguments evaluated first, then function body runs
calculate_rectangle_area(length, width)  # arguments evaluated first, then function body runs
print(calculate_rectangle_area(25, 25))  # calculate_rectangle_area() runs first, then print() outputs the result
length * width  # length → then width → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_rectangle_area(length, width):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_rectangle_area(length, width):  # stored in memory, not executed yet
def calculate_rectangle_area(length, width):  # stored in memory, not executed yet
length = ...  # assigns a value to the variable
width = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_rectangle_area(...))  # calculate_rectangle_area() runs with length=25, width=25
#   → return pow(length, width)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_rectangle_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_rectangle_area(...))  # calculate_rectangle_area() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1656:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_triangle_area(base, height):  # defines a function with parameters base, height
    return 0.5 * base * height  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_triangle_area(4, 6)  # arguments evaluated first, then function body runs
calculate_triangle_area(base, height)  # arguments evaluated first, then function body runs
print(calculate_triangle_area(10, 20))  # calculate_triangle_area() runs first, then print() outputs the result
? * height  # ? → then height → then *
0.5 * base  # 0.5 → then base → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_triangle_area(base, height):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_triangle_area(base, height):  # stored in memory, not executed yet
def calculate_triangle_area(base, height):  # stored in memory, not executed yet
base = ...  # assigns a value to the variable
height = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_triangle_area(...))  # calculate_triangle_area() runs with base=10, height=20
#   → return 0.5 * base * height
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_triangle_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → base = ...  # stored in memory
#   → height = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_triangle_area(...))  # calculate_triangle_area() runs first → value goes to print()

"""

"""
Problem 1657:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_celsius_to_fahrenheit(celsius):  # defines a function with parameters celsius
    return celsius * 9 / 5 + 32  # return sends the result back to the caller
#
# Operators: * + /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_celsius_to_fahrenheit(25)  # arguments evaluated first, then function body runs
convert_celsius_to_fahrenheit(celsius)  # arguments evaluated first, then function body runs
? + 32  # ? → then 32 → then +
? / 5  # ? → then 5 → then /
celsius * 9  # celsius → then 9 → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_celsius_to_fahrenheit(celsius):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def convert_celsius_to_fahrenheit(celsius):  # stored in memory, not executed yet
def convert_celsius_to_fahrenheit(celsius):  # stored in memory, not executed yet
celsius = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_celsius_to_fahrenheit(...))  # convert_celsius_to_fahrenheit() runs with celsius=25
#   → return celsius * 9 / 5 + 32
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_celsius_to_fahrenheit is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → celsius = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_celsius_to_fahrenheit(...))  # convert_celsius_to_fahrenheit() runs first → value goes to print()

"""

"""
Problem 1658:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_fahrenheit_to_celsius(fahrenheit):  # defines a function with parameters fahrenheit
    return (fahrenheit - 32) * 5 / 9  # return sends the result back to the caller
#
# Operators: * - /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_fahrenheit_to_celsius(77)  # arguments evaluated first, then function body runs
convert_fahrenheit_to_celsius(fahrenheit)  # arguments evaluated first, then function body runs
print(convert_fahrenheit_to_celsius(25))  # convert_fahrenheit_to_celsius() runs first, then print() outputs the result
? / 9  # ? → then 9 → then /
? * 5  # ? → then 5 → then *
fahrenheit - 32  # fahrenheit → then 32 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_fahrenheit_to_celsius(fahrenheit):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def convert_fahrenheit_to_celsius(fahrenheit):  # stored in memory, not executed yet
def convert_fahrenheit_to_celsius(fahrenheit):  # stored in memory, not executed yet
fahrenheit = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_fahrenheit_to_celsius(...))  # convert_fahrenheit_to_celsius() runs with fahrenheit=25
#   → return (fahrenheit - 32) * 5 / 9
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_fahrenheit_to_celsius is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → fahrenheit = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_fahrenheit_to_celsius(...))  # convert_fahrenheit_to_celsius() runs first → value goes to print()

"""

"""
Problem 1659:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def is_positive(number):  # defines a function with parameters number
    return number > 0  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: > <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(is_positive(5))  # is_positive() runs first, then print() outputs the result
print(is_positive(?))  # is_positive() runs first, then print() outputs the result
is_positive(number)  # arguments evaluated first, then function body runs
print(is_positive(25))  # is_positive() runs first, then print() outputs the result
number > 0  # evaluated as True or False → picks which branch
number <= 0  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def is_positive(number):  # stored in memory, not executed yet
print(is_positive(...), is_positive(...))  # is_positive() runs with number=5
#   → return not number <= 0
def is_positive(number):  # stored in memory, not executed yet
def is_positive(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(is_positive(...))  # is_positive() runs with number=25
#   → return not number <= 0
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_positive is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_positive(...), is_positive(...))  # is_positive() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_positive(...))  # is_positive() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1660:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def is_negative(number):  # defines a function with parameters number
    return number < 0  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: < >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(is_negative(?))  # is_negative() runs first, then print() outputs the result
print(is_negative(3))  # is_negative() runs first, then print() outputs the result
is_negative(number)  # arguments evaluated first, then function body runs
print(is_negative(25))  # is_negative() runs first, then print() outputs the result
number < 0  # evaluated as True or False → picks which branch
number >= 0  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def is_negative(number):  # stored in memory, not executed yet
print(is_negative(...), is_negative(...))  # is_negative() runs with number=?
#   → return not number >= 0
def is_negative(number):  # stored in memory, not executed yet
def is_negative(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(is_negative(...))  # is_negative() runs with number=25
#   → return not number >= 0
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_negative is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_negative(...), is_negative(...))  # is_negative() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_negative(...))  # is_negative() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1661:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_first_char(text):  # defines a function with parameters text
    return text[0]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_first_char('Hello')  # arguments evaluated first, then function body runs
get_first_char(text)  # arguments evaluated first, then function body runs
print(get_first_char('world'))  # get_first_char() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_first_char(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_first_char(text):  # stored in memory, not executed yet
def get_first_char(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_first_char(...))  # get_first_char() runs with text='world'
#   → return text[0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_char(...))  # get_first_char() runs first → value goes to print()

"""

"""
Problem 1662:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_last_char(text):  # defines a function with parameters text
    return text[-1]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_last_char('Hello')  # arguments evaluated first, then function body runs
get_last_char(text)  # arguments evaluated first, then function body runs
print(get_last_char('world'))  # get_last_char() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_last_char(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_last_char(text):  # stored in memory, not executed yet
def get_last_char(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_last_char(...))  # get_last_char() runs with text='world'
#   → return text[-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_char(...))  # get_last_char() runs first → value goes to print()

"""

"""
Problem 1663:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_middle_char(text):  # defines a function with parameters text
    return text[len(text) // 2]  # return sends the result back to the caller
#
# Operators: //
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_middle_char('Hello')  # arguments evaluated first, then function body runs
get_middle_char(text)  # arguments evaluated first, then function body runs
print(get_middle_char('world'))  # get_middle_char() runs first, then print() outputs the result
len(...) // 2  # len(...) → then 2 → then //
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_middle_char(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_middle_char(text):  # stored in memory, not executed yet
def get_middle_char(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_middle_char(...))  # get_middle_char() runs with text='world'
#   → return text[len(text) // 2]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_char(...))  # get_middle_char() runs first → value goes to print()

"""

"""
Problem 1664:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def repeat_string(text, n):  # defines a function with parameters text, n
    return text * n  # return sends the result back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
repeat_string('Hi', 3)  # arguments evaluated first, then function body runs
repeat_string(text, n)  # arguments evaluated first, then function body runs
print(repeat_string('world', 15))  # repeat_string() runs first, then print() outputs the result
text * n  # text → then n → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def repeat_string(text, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def repeat_string(text, n):  # stored in memory, not executed yet
def repeat_string(text, n):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(repeat_string(...))  # repeat_string() runs with text='world', n=15
#   → return pow(text, n)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only repeat_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(repeat_string(...))  # repeat_string() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1665:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def swap_first_last(text):  # defines a function with parameters text
    if len(text) <= 1:
        return text
    return text[-1] + text[1:-1] + text[0]  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: + <=
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
swap_first_last('Hello')  # arguments evaluated first, then function body runs
swap_first_last(text)  # arguments evaluated first, then function body runs
print(swap_first_last('world'))  # swap_first_last() runs first, then print() outputs the result
len(...) <= 1  # evaluated as True or False → picks which branch
? + ?  # ? → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def swap_first_last(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def swap_first_last(text):  # stored in memory, not executed yet
def swap_first_last(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(swap_first_last(...))  # swap_first_last() runs with text='world'
#   → return text
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_first_last is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_first_last(...))  # swap_first_last() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1666:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_first_char(text):  # defines a function with parameters text
    return text[1:]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_first_char('Hello')  # arguments evaluated first, then function body runs
remove_first_char(text)  # arguments evaluated first, then function body runs
print(remove_first_char('world'))  # remove_first_char() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_first_char(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_first_char(text):  # stored in memory, not executed yet
def remove_first_char(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_first_char(...))  # remove_first_char() runs with text='world'
#   → return text[1:]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_first_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_first_char(...))  # remove_first_char() runs first → value goes to print()

"""

"""
Problem 1667:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_last_char(text):  # defines a function with parameters text
    return text[:-1]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_last_char('Hello')  # arguments evaluated first, then function body runs
remove_last_char(text)  # arguments evaluated first, then function body runs
print(remove_last_char('world'))  # remove_last_char() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_last_char(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_last_char(text):  # stored in memory, not executed yet
def remove_last_char(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_last_char(...))  # remove_last_char() runs with text='world'
#   → return text[:-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_last_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_last_char(...))  # remove_last_char() runs first → value goes to print()

"""

"""
Problem 1668:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_first_word(text):  # defines a function with parameters text
    return text.split()[0]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_first_word('Hello World Python')  # arguments evaluated first, then function body runs
get_first_word(text)  # arguments evaluated first, then function body runs
print(get_first_word('world'))  # get_first_word() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_first_word(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_first_word(text):  # stored in memory, not executed yet
def get_first_word(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_first_word(...))  # get_first_word() runs with text='world'
#   → return text.split()[0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_word(...))  # get_first_word() runs first → value goes to print()

"""

"""
Problem 1669:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_last_word(text):  # defines a function with parameters text
    return text.split()[-1]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_last_word('Hello World Python')  # arguments evaluated first, then function body runs
get_last_word(text)  # arguments evaluated first, then function body runs
print(get_last_word('world'))  # get_last_word() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_last_word(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_last_word(text):  # stored in memory, not executed yet
def get_last_word(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_last_word(...))  # get_last_word() runs with text='world'
#   → return text.split()[-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_word(...))  # get_last_word() runs first → value goes to print()

"""

"""
Problem 1670:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_characters(text):  # defines a function with parameters text
    return len(text.replace(' ', ''))  # return sends the result back to the caller
#
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_characters('Hello World')  # arguments evaluated first, then function body runs
count_characters(text)  # arguments evaluated first, then function body runs
print(count_characters('world'))  # count_characters() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_characters(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_characters(text):  # stored in memory, not executed yet
def count_characters(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_characters(...))  # count_characters() runs with text='world'
#   → return len(text.replace(' ', ''))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_characters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_characters(...))  # count_characters() runs first → value goes to print()

"""

"""
Problem 1671:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def add_to_list(my_list, element):  # defines a function with parameters my_list, element
    new_list = my_list.copy()
    new_list.append(element)
    return new_list  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
add_to_list(?, 4)  # arguments evaluated first, then function body runs
add_to_list(my_list, element)  # arguments evaluated first, then function body runs
print(add_to_list(?, 25))  # add_to_list() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def add_to_list(my_list, element):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def add_to_list(my_list, element):  # stored in memory, not executed yet
def add_to_list(my_list, element):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(add_to_list(...))  # add_to_list() runs with my_list=[4, 1, 8, 6, 3], element=25
#   → return new_list
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_to_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_to_list(...))  # add_to_list() runs first → value goes to print()

"""

"""
Problem 1672:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_first_element(my_list):  # defines a function with parameters my_list
    return my_list[0]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_first_element(?)  # arguments evaluated first, then function body runs
get_first_element(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_first_element(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_first_element(my_list):  # stored in memory, not executed yet
def get_first_element(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_first_element(...))  # get_first_element() runs with my_list=[4, 1, 8, 6, 3]
#   → return my_list[0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_element(...))  # get_first_element() runs first → value goes to print()

"""

"""
Problem 1673:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_last_element(my_list):  # defines a function with parameters my_list
    return my_list[-1]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_last_element(?)  # arguments evaluated first, then function body runs
get_last_element(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_last_element(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_last_element(my_list):  # stored in memory, not executed yet
def get_last_element(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_last_element(...))  # get_last_element() runs with my_list=[4, 1, 8, 6, 3]
#   → return my_list[-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_element(...))  # get_last_element() runs first → value goes to print()

"""

"""
Problem 1674:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_middle_element(my_list):  # defines a function with parameters my_list
    return my_list[len(my_list) // 2]  # return sends the result back to the caller
#
# Operators: //
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_middle_element(?)  # arguments evaluated first, then function body runs
get_middle_element(my_list)  # arguments evaluated first, then function body runs
len(...) // 2  # len(...) → then 2 → then //
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_middle_element(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_middle_element(my_list):  # stored in memory, not executed yet
def get_middle_element(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_middle_element(...))  # get_middle_element() runs with my_list=[4, 1, 8, 6, 3]
#   → return my_list[len(my_list) // 2]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_element(...))  # get_middle_element() runs first → value goes to print()

"""

"""
Problem 1675:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_first_element(my_list):  # defines a function with parameters my_list
    return my_list[1:]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_first_element(?)  # arguments evaluated first, then function body runs
remove_first_element(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_first_element(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_first_element(my_list):  # stored in memory, not executed yet
def remove_first_element(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_first_element(...))  # remove_first_element() runs with my_list=[4, 1, 8, 6, 3]
#   → return my_list[1:]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_first_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_first_element(...))  # remove_first_element() runs first → value goes to print()

"""

"""
Problem 1676:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_last_element(my_list):  # defines a function with parameters my_list
    return my_list[:-1]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_last_element(?)  # arguments evaluated first, then function body runs
remove_last_element(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_last_element(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_last_element(my_list):  # stored in memory, not executed yet
def remove_last_element(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_last_element(...))  # remove_last_element() runs with my_list=[4, 1, 8, 6, 3]
#   → return my_list[:-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_last_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_last_element(...))  # remove_last_element() runs first → value goes to print()

"""

"""
Problem 1677:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_list_slice(my_list, start, end):  # defines a function with parameters my_list, start, end
    return my_list[start:end]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_list_slice(?, 1, 4)  # arguments evaluated first, then function body runs
get_list_slice(my_list, start, end)  # arguments evaluated first, then function body runs
print(get_list_slice(?, 3, 20))  # get_list_slice() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_list_slice(my_list, start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_list_slice(my_list, start, end):  # stored in memory, not executed yet
def get_list_slice(my_list, start, end):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_list_slice(...))  # get_list_slice() runs with my_list=[4, 1, 8, 6, 3], start=3, end=20
#   → return my_list[start:end]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_list_slice is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_list_slice(...))  # get_list_slice() runs first → value goes to print()

"""

"""
Problem 1678:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def combine_lists(list1, list2):  # defines a function with parameters list1, list2
    return list1 + list2  # return sends the result back to the caller
#
# Operators: +
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
combine_lists(?, ?)  # arguments evaluated first, then function body runs
combine_lists(list1, list2)  # arguments evaluated first, then function body runs
print(combine_lists(25, ?))  # combine_lists() runs first, then print() outputs the result
list1 + list2  # list1 → then list2 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def combine_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def combine_lists(list1, list2):  # stored in memory, not executed yet
def combine_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(combine_lists(...))  # combine_lists() runs with list1=25, list2=[4, 1, 8, 6, 3]
#   → import operator
#   → return operator.add(list1, list2)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only combine_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(combine_lists(...))  # combine_lists() runs first → value goes to print()

"""

"""
Problem 1679:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def multiply_list(numbers):  # defines a function with parameters numbers
    return [x * 2 for x in numbers]  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
multiply_list(?)  # arguments evaluated first, then function body runs
multiply_list(numbers)  # arguments evaluated first, then function body runs
x * 2  # x → then 2 → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def multiply_list(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def multiply_list(numbers):  # stored in memory, not executed yet
def multiply_list(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(multiply_list(...))  # multiply_list() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x * 2 for x in numbers]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply_list(...))  # multiply_list() runs first → value goes to print()

"""

"""
Problem 1680:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def add_one_to_all(numbers):  # defines a function with parameters numbers
    return [x + 1 for x in numbers]  # return sends the result back to the caller
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
add_one_to_all(?)  # arguments evaluated first, then function body runs
add_one_to_all(numbers)  # arguments evaluated first, then function body runs
x + 1  # x → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def add_one_to_all(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def add_one_to_all(numbers):  # stored in memory, not executed yet
def add_one_to_all(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(add_one_to_all(...))  # add_one_to_all() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x + 1 for x in numbers]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_one_to_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_one_to_all(...))  # add_one_to_all() runs first → value goes to print()

"""

"""
Problem 1681:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    for i in range(1, 11):
        print(i)
#
# Keywords: for
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
for i in range(...):  # loops once per item
  print(i)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
for i in range(...):  # loops once per item
  print(i)
for i in range(...):  # loops once per item
  print(i)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1682:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    for i in range(1, 21):
        if i % 2 == 0:
            print(i)
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? == 0  # evaluated as True or False → picks which branch
i % 2  # i → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
for i in range(...):  # loops once per item
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
for i in range(...):  # loops once per item
for i in range(...):  # loops once per item
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1683:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    for i in range(1, 21):
        if i % 2 != 0:
            print(i)
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? != 0  # evaluated as True or False → picks which branch
i % 2  # i → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
for i in range(...):  # loops once per item
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
for i in range(...):  # loops once per item
for i in range(...):  # loops once per item
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1684:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    for i in range(1, 11):
        print(f'5 x {i} = {5 * i}')
#
# Keywords: for
# Operators: *
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
5 * i  # 5 → then i → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
for i in range(...):  # loops once per item
  print(?)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
for i in range(...):  # loops once per item
  print(?)
for i in range(...):  # loops once per item
  print(?)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1685:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    text = 'Hello'
    for char in text:
        print(char)
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
text = ...  # assigns a value to the variable
for char in text:  # loops once per item
  print(char)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
for char in text:  # loops once per item
  print(char)
text = ...  # assigns a value to the variable
for char in text:  # loops once per item
  print(char)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → for char in text  # runs body once per item
#   → text = ...  # stored in memory
#   → for char in text  # runs body once per item
#   → text = ...  # stored in memory
#   → for char in text  # runs body once per item
#
# Loop: takes one item from text, runs body, repeats until done

"""

"""
Problem 1686:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    my_list = [10, 20, 30]
    for element in my_list:
        print(element)
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
my_list = ...  # assigns a value to the variable
for element in my_list:  # loops once per item
  print(element)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
for element in my_list:  # loops once per item
  print(element)
my_list = ...  # assigns a value to the variable
for element in my_list:  # loops once per item
  print(element)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → for element in my_list  # runs body once per item
#   → my_list = ...  # stored in memory
#   → for element in my_list  # runs body once per item
#   → my_list = ...  # stored in memory
#   → for element in my_list  # runs body once per item
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1687:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    total = 0
    for i in range(1, 11):
        total += i
    print(total)
#
# Keywords: for
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
total = ...  # assigns a value to the variable
for i in range(...):  # loops once per item
print(total)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
total = ...  # assigns a value to the variable
for i in range(...):  # loops once per item
print(total)
total = ...  # assigns a value to the variable
for i in range(...):  # loops once per item
print(total)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → total = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(total)
#   → total = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(total)
#   → total = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(total)
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1688:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    product = 1
    for i in range(1, 6):
        product *= i
    print(product)
#
# Keywords: for
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
product = ...  # assigns a value to the variable
for i in range(...):  # loops once per item
print(product)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
product = ...  # assigns a value to the variable
for i in range(...):  # loops once per item
print(product)
product = ...  # assigns a value to the variable
for i in range(...):  # loops once per item
print(product)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → product = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(product)
#   → product = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(product)
#   → product = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(product)
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1689:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    for i in range(10, 0, -1):
        print(i)
#
# Keywords: for
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
for i in range(...):  # loops once per item
  print(i)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
for i in range(...):  # loops once per item
  print(i)
for i in range(...):  # loops once per item
  print(i)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1690:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    for i in range(0, 21):
        if i % 3 == 0:
            print(i)
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
? == 0  # evaluated as True or False → picks which branch
i % 3  # i → then 3 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
for i in range(...):  # loops once per item
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
for i in range(...):  # loops once per item
for i in range(...):  # loops once per item
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1691:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    i = 1
    while i <= 5:
        print(i)
        i += 1
#
# Keywords: while
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
i <= 5  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1692:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    total = 0
    i = 1
    while i <= 10:
        total += i
        i += 1
    print(total)
#
# Keywords: while
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
i <= 10  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
total = ...  # assigns a value to the variable
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
print(total)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
total = ...  # assigns a value to the variable
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
print(total)
total = ...  # assigns a value to the variable
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
print(total)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → total = ...  # stored in memory
#   → i = ...  # stored in memory
#   → print(total)
#   → total = ...  # stored in memory
#   → i = ...  # stored in memory
#   → print(total)
#   → total = ...  # stored in memory
#   → i = ...  # stored in memory
#   → print(total)
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1693:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    i = 2
    while i <= 20:
        print(i)
        i += 2
#
# Keywords: while
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
i <= 20  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1694:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def solve():  # defines a function with no parameters
    i = 10
    while i >= 1:
        print(i)
        i -= 1
#
# Keywords: while
# Operators: >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
i >= 1  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
i = ...  # assigns a value to the variable
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1695:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_greater(a, b):  # defines a function with parameters a, b
    return a > b  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: > <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_greater(5, 3))  # check_greater() runs first, then print() outputs the result
print(check_greater(2, 4))  # check_greater() runs first, then print() outputs the result
check_greater(a, b)  # arguments evaluated first, then function body runs
print(check_greater(20, 30))  # check_greater() runs first, then print() outputs the result
a > b  # evaluated as True or False → picks which branch
a <= b  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_greater(a, b):  # stored in memory, not executed yet
print(check_greater(...), check_greater(...))  # check_greater() runs with a=5, b=3
#   → return not a <= b
def check_greater(a, b):  # stored in memory, not executed yet
def check_greater(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_greater(...))  # check_greater() runs with a=20, b=30
#   → return not a <= b
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_greater is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_greater(...), check_greater(...))  # check_greater() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_greater(...))  # check_greater() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1696:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_less(a, b):  # defines a function with parameters a, b
    return a < b  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: < >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_less(3, 5))  # check_less() runs first, then print() outputs the result
print(check_less(5, 3))  # check_less() runs first, then print() outputs the result
check_less(a, b)  # arguments evaluated first, then function body runs
print(check_less(20, 30))  # check_less() runs first, then print() outputs the result
a < b  # evaluated as True or False → picks which branch
a >= b  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_less(a, b):  # stored in memory, not executed yet
print(check_less(...), check_less(...))  # check_less() runs with a=3, b=5
#   → return not a >= b
def check_less(a, b):  # stored in memory, not executed yet
def check_less(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_less(...))  # check_less() runs with a=20, b=30
#   → return not a >= b
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_less is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_less(...), check_less(...))  # check_less() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_less(...))  # check_less() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1697:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_equal(a, b):  # defines a function with parameters a, b
    return a == b  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_equal(5, 5))  # check_equal() runs first, then print() outputs the result
print(check_equal(5, 3))  # check_equal() runs first, then print() outputs the result
check_equal(a, b)  # arguments evaluated first, then function body runs
print(check_equal(20, 30))  # check_equal() runs first, then print() outputs the result
a == b  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_equal(a, b):  # stored in memory, not executed yet
print(check_equal(...), check_equal(...))  # check_equal() runs with a=5, b=5
#   → return a == b
def check_equal(a, b):  # stored in memory, not executed yet
def check_equal(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_equal(...))  # check_equal() runs with a=20, b=30
#   → return a == b
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_equal(...), check_equal(...))  # check_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_equal(...))  # check_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1698:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_not_equal(a, b):  # defines a function with parameters a, b
    return a != b  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: != ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_not_equal(5, 3))  # check_not_equal() runs first, then print() outputs the result
print(check_not_equal(5, 5))  # check_not_equal() runs first, then print() outputs the result
check_not_equal(a, b)  # arguments evaluated first, then function body runs
print(check_not_equal(20, 30))  # check_not_equal() runs first, then print() outputs the result
a != b  # evaluated as True or False → picks which branch
a == b  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_not_equal(a, b):  # stored in memory, not executed yet
print(check_not_equal(...), check_not_equal(...))  # check_not_equal() runs with a=5, b=3
#   → return not a == b
def check_not_equal(a, b):  # stored in memory, not executed yet
def check_not_equal(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_not_equal(...))  # check_not_equal() runs with a=20, b=30
#   → return not a == b
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_not_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_not_equal(...), check_not_equal(...))  # check_not_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_not_equal(...))  # check_not_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1699:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_greater_equal(a, b):  # defines a function with parameters a, b
    return a >= b  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: < >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_greater_equal(5, 5))  # check_greater_equal() runs first, then print() outputs the result
print(check_greater_equal(3, 5))  # check_greater_equal() runs first, then print() outputs the result
check_greater_equal(a, b)  # arguments evaluated first, then function body runs
print(check_greater_equal(20, 30))  # check_greater_equal() runs first, then print() outputs the result
a >= b  # evaluated as True or False → picks which branch
a < b  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_greater_equal(a, b):  # stored in memory, not executed yet
print(check_greater_equal(...), check_greater_equal(...))  # check_greater_equal() runs with a=5, b=5
#   → return not a < b
def check_greater_equal(a, b):  # stored in memory, not executed yet
def check_greater_equal(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_greater_equal(...))  # check_greater_equal() runs with a=20, b=30
#   → return not a < b
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_greater_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_greater_equal(...), check_greater_equal(...))  # check_greater_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_greater_equal(...))  # check_greater_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1700:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_less_equal(a, b):  # defines a function with parameters a, b
    return a <= b  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: > <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_less_equal(3, 5))  # check_less_equal() runs first, then print() outputs the result
print(check_less_equal(5, 3))  # check_less_equal() runs first, then print() outputs the result
check_less_equal(a, b)  # arguments evaluated first, then function body runs
print(check_less_equal(20, 30))  # check_less_equal() runs first, then print() outputs the result
a <= b  # evaluated as True or False → picks which branch
a > b  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_less_equal(a, b):  # stored in memory, not executed yet
print(check_less_equal(...), check_less_equal(...))  # check_less_equal() runs with a=3, b=5
#   → return not a > b
def check_less_equal(a, b):  # stored in memory, not executed yet
def check_less_equal(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_less_equal(...))  # check_less_equal() runs with a=20, b=30
#   → return not a > b
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_less_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_less_equal(...), check_less_equal(...))  # check_less_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_less_equal(...))  # check_less_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1701:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_absolute_value(number):  # defines a function with parameters number
    return abs(number)  # return sends the result back to the caller
#
# Built-in functions: abs()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(get_absolute_value(?))  # get_absolute_value() runs first, then print() outputs the result
print(get_absolute_value(5))  # get_absolute_value() runs first, then print() outputs the result
get_absolute_value(number)  # arguments evaluated first, then function body runs
print(get_absolute_value(25))  # get_absolute_value() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_absolute_value(number):  # stored in memory, not executed yet
print(get_absolute_value(...), get_absolute_value(...))  # get_absolute_value() runs with number=?
#   → return abs(number)
def get_absolute_value(number):  # stored in memory, not executed yet
def get_absolute_value(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_absolute_value(...))  # get_absolute_value() runs with number=25
#   → return abs(number)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_absolute_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_absolute_value(...), get_absolute_value(...))  # get_absolute_value() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_absolute_value(...))  # get_absolute_value() runs first → value goes to print()

"""

"""
Problem 1702:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def round_number(number):  # defines a function with parameters number
    return round(number)  # return sends the result back to the caller
#
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(round_number(3.7))  # round_number() runs first, then print() outputs the result
print(round_number(3.2))  # round_number() runs first, then print() outputs the result
round_number(number)  # arguments evaluated first, then function body runs
print(round_number(25))  # round_number() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def round_number(number):  # stored in memory, not executed yet
print(round_number(...), round_number(...))  # round_number() runs with number=3.7
#   → return round(number)
def round_number(number):  # stored in memory, not executed yet
def round_number(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(round_number(...))  # round_number() runs with number=25
#   → return round(number)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only round_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(round_number(...), round_number(...))  # round_number() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(round_number(...))  # round_number() runs first → value goes to print()

"""

"""
Problem 1703:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_maximum(a, b, c):  # defines a function with parameters a, b, c
    return max(a, b, c)  # return sends the result back to the caller
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_maximum(10, 5, 8)  # arguments evaluated first, then function body runs
get_maximum(a, b, c)  # arguments evaluated first, then function body runs
print(get_maximum(20, 30, ?))  # get_maximum() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_maximum(a, b, c):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_maximum(a, b, c):  # stored in memory, not executed yet
def get_maximum(a, b, c):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
c = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_maximum(...))  # get_maximum() runs with a=20, b=30, c=[4, 1, 8, 6, 3]
#   → return max(a, b, c)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_maximum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_maximum(...))  # get_maximum() runs first → value goes to print()

"""

"""
Problem 1704:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_minimum(a, b, c):  # defines a function with parameters a, b, c
    return min(a, b, c)  # return sends the result back to the caller
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_minimum(10, 5, 8)  # arguments evaluated first, then function body runs
get_minimum(a, b, c)  # arguments evaluated first, then function body runs
print(get_minimum(20, 30, ?))  # get_minimum() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_minimum(a, b, c):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_minimum(a, b, c):  # stored in memory, not executed yet
def get_minimum(a, b, c):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
c = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_minimum(...))  # get_minimum() runs with a=20, b=30, c=[4, 1, 8, 6, 3]
#   → return min(a, b, c)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_minimum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_minimum(...))  # get_minimum() runs first → value goes to print()

"""

"""
Problem 1705:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_divisible(a, b):  # defines a function with parameters a, b
    return a % b == 0  # return sends the result back to the caller
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_divisible(10, 2))  # check_divisible() runs first, then print() outputs the result
print(check_divisible(10, 3))  # check_divisible() runs first, then print() outputs the result
check_divisible(a, b)  # arguments evaluated first, then function body runs
print(check_divisible(20, 30))  # check_divisible() runs first, then print() outputs the result
? == 0  # evaluated as True or False → picks which branch
a % b  # a → then b → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_divisible(a, b):  # stored in memory, not executed yet
print(check_divisible(...), check_divisible(...))  # check_divisible() runs with a=10, b=2
#   → return a % b == 0
def check_divisible(a, b):  # stored in memory, not executed yet
def check_divisible(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_divisible(...))  # check_divisible() runs with a=20, b=30
#   → return a % b == 0
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisible is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_divisible(...), check_divisible(...))  # check_divisible() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_divisible(...))  # check_divisible() runs first → value goes to print()

"""

"""
Problem 1706:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_to_string(number):  # defines a function with parameters number
    return str(number)  # return sends the result back to the caller
#
# Built-in functions: str, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_to_string(123)  # arguments evaluated first, then function body runs
convert_to_string(number)  # arguments evaluated first, then function body runs
print(convert_to_string(25))  # convert_to_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_to_string(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result, type(...))
def convert_to_string(number):  # stored in memory, not executed yet
def convert_to_string(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_to_string(...))  # convert_to_string() runs with number=25
#   → return str(number)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result, type(...))
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_string(...))  # convert_to_string() runs first → value goes to print()

"""

"""
Problem 1707:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_to_integer(text):  # defines a function with parameters text
    return int(text)  # return sends the result back to the caller
#
# Built-in functions: int, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_to_integer('123')  # arguments evaluated first, then function body runs
convert_to_integer(text)  # arguments evaluated first, then function body runs
print(convert_to_integer('world'))  # convert_to_integer() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_to_integer(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result, type(...))
def convert_to_integer(text):  # stored in memory, not executed yet
def convert_to_integer(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_to_integer(...))  # convert_to_integer() runs with text='world'
#   → return int(text)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_integer is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result, type(...))
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_integer(...))  # convert_to_integer() runs first → value goes to print()

"""

"""
Problem 1708:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_to_float(text):  # defines a function with parameters text
    return float(text)  # return sends the result back to the caller
#
# Built-in functions: float, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_to_float('3.14')  # arguments evaluated first, then function body runs
convert_to_float(text)  # arguments evaluated first, then function body runs
print(convert_to_float('world'))  # convert_to_float() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_to_float(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result, type(...))
def convert_to_float(text):  # stored in memory, not executed yet
def convert_to_float(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_to_float(...))  # convert_to_float() runs with text='world'
#   → return float(text)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_float is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result, type(...))
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_float(...))  # convert_to_float() runs first → value goes to print()

"""

"""
Problem 1709:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_type(value):  # defines a function with parameters value
    return str(type(value).__name__)  # return sends the result back to the caller
#
# Built-in functions: str, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(get_type(5))  # get_type() runs first, then print() outputs the result
print(get_type('hello'))  # get_type() runs first, then print() outputs the result
get_type(value)  # arguments evaluated first, then function body runs
print(get_type(25))  # get_type() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_type(value):  # stored in memory, not executed yet
print(get_type(...), get_type(...))  # get_type() runs with value=5
#   → return str(type(value).__name__)
def get_type(value):  # stored in memory, not executed yet
def get_type(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_type(...))  # get_type() runs with value=25
#   → return str(type(value).__name__)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_type is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_type(...), get_type(...))  # get_type() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_type(...))  # get_type() runs first → value goes to print()

"""

"""
Problem 1710:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_is_integer(value):  # defines a function with parameters value
    return isinstance(value, int)  # return sends the result back to the caller
#
# Built-in functions: isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_is_integer(5))  # check_is_integer() runs first, then print() outputs the result
print(check_is_integer('5'))  # check_is_integer() runs first, then print() outputs the result
check_is_integer(value)  # arguments evaluated first, then function body runs
print(check_is_integer(25))  # check_is_integer() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_is_integer(value):  # stored in memory, not executed yet
print(check_is_integer(...), check_is_integer(...))  # check_is_integer() runs with value=5
#   → return isinstance(value, int)
def check_is_integer(value):  # stored in memory, not executed yet
def check_is_integer(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_is_integer(...))  # check_is_integer() runs with value=25
#   → return isinstance(value, int)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_is_integer is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_is_integer(...), check_is_integer(...))  # check_is_integer() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_is_integer(...))  # check_is_integer() runs first → value goes to print()

"""

"""
Problem 1711:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_is_string(value):  # defines a function with parameters value
    return isinstance(value, str)  # return sends the result back to the caller
#
# Built-in functions: isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_is_string('hello'))  # check_is_string() runs first, then print() outputs the result
print(check_is_string(5))  # check_is_string() runs first, then print() outputs the result
check_is_string(value)  # arguments evaluated first, then function body runs
print(check_is_string(25))  # check_is_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_is_string(value):  # stored in memory, not executed yet
print(check_is_string(...), check_is_string(...))  # check_is_string() runs with value='hello'
#   → return isinstance(value, str)
def check_is_string(value):  # stored in memory, not executed yet
def check_is_string(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_is_string(...))  # check_is_string() runs with value=25
#   → return isinstance(value, str)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_is_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_is_string(...), check_is_string(...))  # check_is_string() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_is_string(...))  # check_is_string() runs first → value goes to print()

"""

"""
Problem 1712:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_is_list(value):  # defines a function with parameters value
    return isinstance(value, list)  # return sends the result back to the caller
#
# Built-in functions: isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_is_list(?))  # check_is_list() runs first, then print() outputs the result
print(check_is_list('hello'))  # check_is_list() runs first, then print() outputs the result
check_is_list(value)  # arguments evaluated first, then function body runs
print(check_is_list(25))  # check_is_list() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_is_list(value):  # stored in memory, not executed yet
print(check_is_list(...), check_is_list(...))  # check_is_list() runs with value=[1, 2, 3]
#   → return isinstance(value, list)
def check_is_list(value):  # stored in memory, not executed yet
def check_is_list(value):  # stored in memory, not executed yet
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_is_list(...))  # check_is_list() runs with value=25
#   → return isinstance(value, list)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_is_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_is_list(...), check_is_list(...))  # check_is_list() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_is_list(...))  # check_is_list() runs first → value goes to print()

"""

"""
Problem 1713:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_greeting(name):  # defines a function with parameters name
    return f'Hello, {name}!'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_greeting('Alice')  # arguments evaluated first, then function body runs
format_greeting(name)  # arguments evaluated first, then function body runs
print(format_greeting('Bob'))  # format_greeting() runs first, then print() outputs the result
'Hello, %s!' % name  # 'Hello, %s!' → then name → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_greeting(name):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_greeting(name):  # stored in memory, not executed yet
def format_greeting(name):  # stored in memory, not executed yet
name = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_greeting(...))  # format_greeting() runs with name='Bob'
#   → return 'Hello, %s!' % name
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_greeting is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_greeting(...))  # format_greeting() runs first → value goes to print()

"""

"""
Problem 1714:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_age_message(name, age):  # defines a function with parameters name, age
    return f'{name} is {age} years old.'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_age_message('Bob', 25)  # arguments evaluated first, then function body runs
format_age_message(name, age)  # arguments evaluated first, then function body runs
'%s is %s years old.' % ?  # '%s is %s years old.' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_age_message(name, age):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_age_message(name, age):  # stored in memory, not executed yet
def format_age_message(name, age):  # stored in memory, not executed yet
name = ...  # assigns a value to the variable
age = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_age_message(...))  # format_age_message() runs with name='Bob', age=25
#   → return '%s is %s years old.' % (name, age)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_age_message is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → age = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_age_message(...))  # format_age_message() runs first → value goes to print()

"""

"""
Problem 1715:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_calculation(num1, num2):  # defines a function with parameters num1, num2
    return f'{num1} + {num2} = {num1 + num2}'  # return sends the result back to the caller
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_calculation(5, 3)  # arguments evaluated first, then function body runs
format_calculation(num1, num2)  # arguments evaluated first, then function body runs
print(format_calculation(20, 30))  # format_calculation() runs first, then print() outputs the result
num1 + num2  # num1 → then num2 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_calculation(num1, num2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_calculation(num1, num2):  # stored in memory, not executed yet
def format_calculation(num1, num2):  # stored in memory, not executed yet
num1 = ...  # assigns a value to the variable
num2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_calculation(...))  # format_calculation() runs with num1=20, num2=30
#   → return f'{num1} + {num2} = {num1 + num2}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_calculation is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → num1 = ...  # stored in memory
#   → num2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_calculation(...))  # format_calculation() runs first → value goes to print()

"""

"""
Problem 1716:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_perimeter_rectangle(length, width):  # defines a function with parameters length, width
    return 2 * (length + width)  # return sends the result back to the caller
#
# Operators: * +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_perimeter_rectangle(5, 3)  # arguments evaluated first, then function body runs
calculate_perimeter_rectangle(length, width)  # arguments evaluated first, then function body runs
print(calculate_perimeter_rectangle(25, 25))  # calculate_perimeter_rectangle() runs first, then print() outputs the result
2 * ?  # 2 → then ? → then *
length + width  # length → then width → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_perimeter_rectangle(length, width):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_perimeter_rectangle(length, width):  # stored in memory, not executed yet
def calculate_perimeter_rectangle(length, width):  # stored in memory, not executed yet
length = ...  # assigns a value to the variable
width = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_perimeter_rectangle(...))  # calculate_perimeter_rectangle() runs with length=25, width=25
#   → return 2 * (length + width)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_perimeter_rectangle is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_perimeter_rectangle(...))  # calculate_perimeter_rectangle() runs first → value goes to print()

"""

"""
Problem 1717:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_perimeter_square(side):  # defines a function with parameters side
    return 4 * side  # return sends the result back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_perimeter_square(5)  # arguments evaluated first, then function body runs
calculate_perimeter_square(side)  # arguments evaluated first, then function body runs
print(calculate_perimeter_square(25))  # calculate_perimeter_square() runs first, then print() outputs the result
4 * side  # 4 → then side → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_perimeter_square(side):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_perimeter_square(side):  # stored in memory, not executed yet
def calculate_perimeter_square(side):  # stored in memory, not executed yet
side = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_perimeter_square(...))  # calculate_perimeter_square() runs with side=25
#   → return pow(4, side)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_perimeter_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → side = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_perimeter_square(...))  # calculate_perimeter_square() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1718:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_circle_circumference(radius):  # defines a function with parameters radius
    return 2 * 3.14 * radius  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_circle_circumference(5)  # arguments evaluated first, then function body runs
calculate_circle_circumference(radius)  # arguments evaluated first, then function body runs
print(calculate_circle_circumference(14))  # calculate_circle_circumference() runs first, then print() outputs the result
? * radius  # ? → then radius → then *
2 * 3.14  # 2 → then 3.14 → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_circle_circumference(radius):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_circle_circumference(radius):  # stored in memory, not executed yet
def calculate_circle_circumference(radius):  # stored in memory, not executed yet
radius = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_circle_circumference(...))  # calculate_circle_circumference() runs with radius=14
#   → return 2 * 3.14 * radius
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_circle_circumference is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → radius = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_circle_circumference(...))  # calculate_circle_circumference() runs first → value goes to print()

"""

"""
Problem 1719:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_km_to_miles(km):  # defines a function with parameters km
    return km * 0.621371  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_km_to_miles(10)  # arguments evaluated first, then function body runs
convert_km_to_miles(km)  # arguments evaluated first, then function body runs
print(convert_km_to_miles(25))  # convert_km_to_miles() runs first, then print() outputs the result
km * 0.621371  # km → then 0.621371 → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_km_to_miles(km):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def convert_km_to_miles(km):  # stored in memory, not executed yet
def convert_km_to_miles(km):  # stored in memory, not executed yet
km = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_km_to_miles(...))  # convert_km_to_miles() runs with km=25
#   → return km * 0.621371
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_km_to_miles is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → km = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_km_to_miles(...))  # convert_km_to_miles() runs first → value goes to print()

"""

"""
Problem 1720:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_miles_to_km(miles):  # defines a function with parameters miles
    return miles / 0.621371  # return sends the result back to the caller
#
# Operators: /
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_miles_to_km(6.21371)  # arguments evaluated first, then function body runs
convert_miles_to_km(miles)  # arguments evaluated first, then function body runs
print(convert_miles_to_km(25))  # convert_miles_to_km() runs first, then print() outputs the result
miles / 0.621371  # miles → then 0.621371 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_miles_to_km(miles):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(round(...))
def convert_miles_to_km(miles):  # stored in memory, not executed yet
def convert_miles_to_km(miles):  # stored in memory, not executed yet
miles = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_miles_to_km(...))  # convert_miles_to_km() runs with miles=25
#   → return miles / 0.621371
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_miles_to_km is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → miles = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_miles_to_km(...))  # convert_miles_to_km() runs first → value goes to print()

"""

"""
Problem 1721:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_kg_to_pounds(kg):  # defines a function with parameters kg
    return kg * 2.20462  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_kg_to_pounds(10)  # arguments evaluated first, then function body runs
convert_kg_to_pounds(kg)  # arguments evaluated first, then function body runs
print(convert_kg_to_pounds(25))  # convert_kg_to_pounds() runs first, then print() outputs the result
kg * 2.20462  # kg → then 2.20462 → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_kg_to_pounds(kg):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def convert_kg_to_pounds(kg):  # stored in memory, not executed yet
def convert_kg_to_pounds(kg):  # stored in memory, not executed yet
kg = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_kg_to_pounds(...))  # convert_kg_to_pounds() runs with kg=25
#   → return kg * 2.20462
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_kg_to_pounds is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → kg = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_kg_to_pounds(...))  # convert_kg_to_pounds() runs first → value goes to print()

"""

"""
Problem 1722:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def convert_pounds_to_kg(pounds):  # defines a function with parameters pounds
    return pounds / 2.20462  # return sends the result back to the caller
#
# Operators: /
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
convert_pounds_to_kg(22.0462)  # arguments evaluated first, then function body runs
convert_pounds_to_kg(pounds)  # arguments evaluated first, then function body runs
print(convert_pounds_to_kg(25))  # convert_pounds_to_kg() runs first, then print() outputs the result
pounds / 2.20462  # pounds → then 2.20462 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def convert_pounds_to_kg(pounds):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(round(...))
def convert_pounds_to_kg(pounds):  # stored in memory, not executed yet
def convert_pounds_to_kg(pounds):  # stored in memory, not executed yet
pounds = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(convert_pounds_to_kg(...))  # convert_pounds_to_kg() runs with pounds=25
#   → return pounds / 2.20462
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_pounds_to_kg is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → pounds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_pounds_to_kg(...))  # convert_pounds_to_kg() runs first → value goes to print()

"""

"""
Problem 1723:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_vowel(char):  # defines a function with parameters char
    return char.lower() in 'aeiou'  # return sends the result back to the caller
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_vowel('a'))  # check_vowel() runs first, then print() outputs the result
print(check_vowel('b'))  # check_vowel() runs first, then print() outputs the result
check_vowel(char)  # arguments evaluated first, then function body runs
print(check_vowel('z'))  # check_vowel() runs first, then print() outputs the result
char.lower(...) in 'aeiou'  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_vowel(char):  # stored in memory, not executed yet
print(check_vowel(...), check_vowel(...))  # check_vowel() runs with char='a'
#   → return char.lower() in 'aeiou'
def check_vowel(char):  # stored in memory, not executed yet
def check_vowel(char):  # stored in memory, not executed yet
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_vowel(...))  # check_vowel() runs with char='z'
#   → return char.lower() in 'aeiou'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_vowel is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_vowel(...), check_vowel(...))  # check_vowel() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_vowel(...))  # check_vowel() runs first → value goes to print()

"""

"""
Problem 1724:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_consonant(char):  # defines a function with parameters char
    return char.isalpha() and char.lower() not in 'aeiou'  # return sends the result back to the caller
#
# Operators: and not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_consonant('b'))  # check_consonant() runs first, then print() outputs the result
print(check_consonant('a'))  # check_consonant() runs first, then print() outputs the result
check_consonant(char)  # arguments evaluated first, then function body runs
print(check_consonant('z'))  # check_consonant() runs first, then print() outputs the result
char.lower(...) not in 'aeiou'  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_consonant(char):  # stored in memory, not executed yet
print(check_consonant(...), check_consonant(...))  # check_consonant() runs with char='b'
#   → return char.isalpha() and char.lower() not in 'aeiou'
def check_consonant(char):  # stored in memory, not executed yet
def check_consonant(char):  # stored in memory, not executed yet
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_consonant(...))  # check_consonant() runs with char='z'
#   → return char.isalpha() and char.lower() not in 'aeiou'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_consonant is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_consonant(...), check_consonant(...))  # check_consonant() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_consonant(...))  # check_consonant() runs first → value goes to print()

"""

"""
Problem 1725:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_digit(char):  # defines a function with parameters char
    return char.isdigit()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_digit('5'))  # check_digit() runs first, then print() outputs the result
print(check_digit('a'))  # check_digit() runs first, then print() outputs the result
check_digit(char)  # arguments evaluated first, then function body runs
print(check_digit('z'))  # check_digit() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_digit(char):  # stored in memory, not executed yet
print(check_digit(...), check_digit(...))  # check_digit() runs with char='5'
#   → return char.isdigit()
def check_digit(char):  # stored in memory, not executed yet
def check_digit(char):  # stored in memory, not executed yet
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_digit(...))  # check_digit() runs with char='z'
#   → return char.isdigit()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_digit is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_digit(...), check_digit(...))  # check_digit() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_digit(...))  # check_digit() runs first → value goes to print()

"""

"""
Problem 1726:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_letter(char):  # defines a function with parameters char
    return char.isalpha()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_letter('a'))  # check_letter() runs first, then print() outputs the result
print(check_letter('5'))  # check_letter() runs first, then print() outputs the result
check_letter(char)  # arguments evaluated first, then function body runs
print(check_letter('z'))  # check_letter() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_letter(char):  # stored in memory, not executed yet
print(check_letter(...), check_letter(...))  # check_letter() runs with char='a'
#   → return char.isalpha()
def check_letter(char):  # stored in memory, not executed yet
def check_letter(char):  # stored in memory, not executed yet
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_letter(...))  # check_letter() runs with char='z'
#   → return char.isalpha()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_letter(...), check_letter(...))  # check_letter() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_letter(...))  # check_letter() runs first → value goes to print()

"""

"""
Problem 1727:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_uppercase(char):  # defines a function with parameters char
    return char.isupper()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_uppercase('A'))  # check_uppercase() runs first, then print() outputs the result
print(check_uppercase('a'))  # check_uppercase() runs first, then print() outputs the result
check_uppercase(char)  # arguments evaluated first, then function body runs
print(check_uppercase('z'))  # check_uppercase() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_uppercase(char):  # stored in memory, not executed yet
print(check_uppercase(...), check_uppercase(...))  # check_uppercase() runs with char='A'
#   → return char.isupper()
def check_uppercase(char):  # stored in memory, not executed yet
def check_uppercase(char):  # stored in memory, not executed yet
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_uppercase(...))  # check_uppercase() runs with char='z'
#   → return char.isupper()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_uppercase is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_uppercase(...), check_uppercase(...))  # check_uppercase() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_uppercase(...))  # check_uppercase() runs first → value goes to print()

"""

"""
Problem 1728:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_lowercase(char):  # defines a function with parameters char
    return char.islower()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_lowercase('a'))  # check_lowercase() runs first, then print() outputs the result
print(check_lowercase('A'))  # check_lowercase() runs first, then print() outputs the result
check_lowercase(char)  # arguments evaluated first, then function body runs
print(check_lowercase('z'))  # check_lowercase() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_lowercase(char):  # stored in memory, not executed yet
print(check_lowercase(...), check_lowercase(...))  # check_lowercase() runs with char='a'
#   → return char.islower()
def check_lowercase(char):  # stored in memory, not executed yet
def check_lowercase(char):  # stored in memory, not executed yet
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_lowercase(...))  # check_lowercase() runs with char='z'
#   → return char.islower()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_lowercase is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_lowercase(...), check_lowercase(...))  # check_lowercase() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_lowercase(...))  # check_lowercase() runs first → value goes to print()

"""

"""
Problem 1729:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_char_index(text, char):  # defines a function with parameters text, char
    return text.find(char)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(find_char_index('Hello', 'e'))  # find_char_index() runs first, then print() outputs the result
print(find_char_index('Hello', 'x'))  # find_char_index() runs first, then print() outputs the result
find_char_index(text, char)  # arguments evaluated first, then function body runs
print(find_char_index('world', 'z'))  # find_char_index() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_char_index(text, char):  # stored in memory, not executed yet
print(find_char_index(...), find_char_index(...))  # find_char_index() runs with text='Hello', char='e'
#   → return text.find(char)
def find_char_index(text, char):  # stored in memory, not executed yet
def find_char_index(text, char):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_char_index(...))  # find_char_index() runs with text='world', char='z'
#   → return text.find(char)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_char_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(find_char_index(...), find_char_index(...))  # find_char_index() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_char_index(...))  # find_char_index() runs first → value goes to print()

"""

"""
Problem 1730:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_char_in_string(text, char):  # defines a function with parameters text, char
    return text.count(char)  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_char_in_string('Hello', 'l')  # arguments evaluated first, then function body runs
count_char_in_string(text, char)  # arguments evaluated first, then function body runs
print(count_char_in_string('world', 'z'))  # count_char_in_string() runs first, then print() outputs the result
c == char  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_char_in_string(text, char):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_char_in_string(text, char):  # stored in memory, not executed yet
def count_char_in_string(text, char):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_char_in_string(...))  # count_char_in_string() runs with text='world', char='z'
#   → count = 0
#   → for c in text:
#   →     if c == char:
#   →         count += 1
#   → return count
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_char_in_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_char_in_string(...))  # count_char_in_string() runs first → value goes to print()
#
# Loop: takes one item from text, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1731:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_starts_with(text, prefix):  # defines a function with parameters text, prefix
    return text.startswith(prefix)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_starts_with('Hello', 'He'))  # check_starts_with() runs first, then print() outputs the result
print(check_starts_with('Hello', 'lo'))  # check_starts_with() runs first, then print() outputs the result
check_starts_with(text, prefix)  # arguments evaluated first, then function body runs
print(check_starts_with('world', 'Hi, '))  # check_starts_with() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_starts_with(text, prefix):  # stored in memory, not executed yet
print(check_starts_with(...), check_starts_with(...))  # check_starts_with() runs with text='Hello', prefix='He'
#   → return text.startswith(prefix)
def check_starts_with(text, prefix):  # stored in memory, not executed yet
def check_starts_with(text, prefix):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
prefix = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_starts_with(...))  # check_starts_with() runs with text='world', prefix='Hi, '
#   → return text.startswith(prefix)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_starts_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_starts_with(...), check_starts_with(...))  # check_starts_with() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → prefix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_starts_with(...))  # check_starts_with() runs first → value goes to print()

"""

"""
Problem 1732:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_ends_with(text, suffix):  # defines a function with parameters text, suffix
    return text.endswith(suffix)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_ends_with('Hello', 'lo'))  # check_ends_with() runs first, then print() outputs the result
print(check_ends_with('Hello', 'He'))  # check_ends_with() runs first, then print() outputs the result
check_ends_with(text, suffix)  # arguments evaluated first, then function body runs
print(check_ends_with('world', '?'))  # check_ends_with() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_ends_with(text, suffix):  # stored in memory, not executed yet
print(check_ends_with(...), check_ends_with(...))  # check_ends_with() runs with text='Hello', suffix='lo'
#   → return text.endswith(suffix)
def check_ends_with(text, suffix):  # stored in memory, not executed yet
def check_ends_with(text, suffix):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
suffix = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_ends_with(...))  # check_ends_with() runs with text='world', suffix='?'
#   → return text.endswith(suffix)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_ends_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_ends_with(...), check_ends_with(...))  # check_ends_with() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → suffix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_ends_with(...))  # check_ends_with() runs first → value goes to print()

"""

"""
Problem 1733:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def replace_char(text, old_char, new_char):  # defines a function with parameters text, old_char, new_char
    return text.replace(old_char, new_char)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
replace_char('Hello', 'l', 'x')  # arguments evaluated first, then function body runs
replace_char(text, old_char, new_char)  # arguments evaluated first, then function body runs
print(replace_char('world', 'world', 'world'))  # replace_char() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def replace_char(text, old_char, new_char):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def replace_char(text, old_char, new_char):  # stored in memory, not executed yet
def replace_char(text, old_char, new_char):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
old_char = ...  # assigns a value to the variable
new_char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(replace_char(...))  # replace_char() runs with text='world', old_char='world', new_char='world'
#   → return text.replace(old_char, new_char)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only replace_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → old_char = ...  # stored in memory
#   → new_char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(replace_char(...))  # replace_char() runs first → value goes to print()

"""

"""
Problem 1734:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_char(text, char):  # defines a function with parameters text, char
    return text.replace(char, '')  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_char('Hello', 'l')  # arguments evaluated first, then function body runs
remove_char(text, char)  # arguments evaluated first, then function body runs
print(remove_char('world', 'z'))  # remove_char() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_char(text, char):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_char(text, char):  # stored in memory, not executed yet
def remove_char(text, char):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
char = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_char(...))  # remove_char() runs with text='world', char='z'
#   → return text.replace(char, '')
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_char(...))  # remove_char() runs first → value goes to print()

"""

"""
Problem 1735:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_substring(text, start, end):  # defines a function with parameters text, start, end
    return text[start:end]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_substring('Hello World', 0, 5)  # arguments evaluated first, then function body runs
get_substring(text, start, end)  # arguments evaluated first, then function body runs
print(get_substring('world', 3, 20))  # get_substring() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_substring(text, start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_substring(text, start, end):  # stored in memory, not executed yet
def get_substring(text, start, end):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_substring(...))  # get_substring() runs with text='world', start=3, end=20
#   → return text[start:end]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_substring is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_substring(...))  # get_substring() runs first → value goes to print()

"""

"""
Problem 1736:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def reverse_list(my_list):  # defines a function with parameters my_list
    return my_list[::-1]  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reversed()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
reverse_list(?)  # arguments evaluated first, then function body runs
reverse_list(my_list)  # arguments evaluated first, then function body runs
char + result  # char → then result → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def reverse_list(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def reverse_list(my_list):  # stored in memory, not executed yet
def reverse_list(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(reverse_list(...))  # reverse_list() runs with my_list=[4, 1, 8, 6, 3]
#   → result = ''
#   → for char in my_list:
#   →     result = char + result
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_list(...))  # reverse_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1737:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_list_length(my_list):  # defines a function with parameters my_list
    return len(my_list)  # return sends the result back to the caller
#
# Keywords: for
# Built-in functions: len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_list_length(?)  # arguments evaluated first, then function body runs
get_list_length(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_list_length(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_list_length(my_list):  # stored in memory, not executed yet
def get_list_length(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_list_length(...))  # get_list_length() runs with my_list=[4, 1, 8, 6, 3]
#   → return sum((1 for _ in my_list))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_list_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_list_length(...))  # get_list_length() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1738:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_element_in_list(my_list, element):  # defines a function with parameters my_list, element
    return element in my_list  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_element_in_list(?, 2))  # check_element_in_list() runs first, then print() outputs the result
print(check_element_in_list(?, 5))  # check_element_in_list() runs first, then print() outputs the result
check_element_in_list(my_list, element)  # arguments evaluated first, then function body runs
print(check_element_in_list(?, 25))  # check_element_in_list() runs first, then print() outputs the result
element in my_list  # evaluated as True or False → picks which branch
element == element  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_element_in_list(my_list, element):  # stored in memory, not executed yet
print(check_element_in_list(...), check_element_in_list(...))  # check_element_in_list() runs with my_list=[1, 2, 3], element=2
#   → return any((element == element for element in my_list))
def check_element_in_list(my_list, element):  # stored in memory, not executed yet
def check_element_in_list(my_list, element):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_element_in_list(...))  # check_element_in_list() runs with my_list=[4, 1, 8, 6, 3], element=25
#   → return any((element == element for element in my_list))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_element_in_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_element_in_list(...), check_element_in_list(...))  # check_element_in_list() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_element_in_list(...))  # check_element_in_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1739:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_element_count(my_list, element):  # defines a function with parameters my_list, element
    return my_list.count(element)  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_element_count(?, 2)  # arguments evaluated first, then function body runs
get_element_count(my_list, element)  # arguments evaluated first, then function body runs
print(get_element_count(?, 25))  # get_element_count() runs first, then print() outputs the result
c == element  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_element_count(my_list, element):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_element_count(my_list, element):  # stored in memory, not executed yet
def get_element_count(my_list, element):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_element_count(...))  # get_element_count() runs with my_list=[4, 1, 8, 6, 3], element=25
#   → count = 0
#   → for c in my_list:
#   →     if c == element:
#   →         count += 1
#   → return count
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_element_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_element_count(...))  # get_element_count() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1740:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_element_index(my_list, element):  # defines a function with parameters my_list, element
    if element in my_list:
        return my_list.index(element)
    return -1  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(get_element_index(?, 20))  # get_element_index() runs first, then print() outputs the result
print(get_element_index(?, 40))  # get_element_index() runs first, then print() outputs the result
get_element_index(my_list, element)  # arguments evaluated first, then function body runs
print(get_element_index(?, 25))  # get_element_index() runs first, then print() outputs the result
element in my_list  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_element_index(my_list, element):  # stored in memory, not executed yet
print(get_element_index(...), get_element_index(...))  # get_element_index() runs with my_list=[10, 20, 30], element=20
#   → return my_list.index(element)
def get_element_index(my_list, element):  # stored in memory, not executed yet
def get_element_index(my_list, element):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_element_index(...))  # get_element_index() runs with my_list=[4, 1, 8, 6, 3], element=25
#   → return my_list.index(element)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_element_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_element_index(...), get_element_index(...))  # get_element_index() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_element_index(...))  # get_element_index() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1741:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_even_numbers(numbers):  # defines a function with parameters numbers
    return [x for x in numbers if x % 2 == 0]  # return sends the result back to the caller
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_even_numbers(?)  # arguments evaluated first, then function body runs
get_even_numbers(numbers)  # arguments evaluated first, then function body runs
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_even_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_even_numbers(numbers):  # stored in memory, not executed yet
def get_even_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_even_numbers(...))  # get_even_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x % 2 == 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_numbers(...))  # get_even_numbers() runs first → value goes to print()

"""

"""
Problem 1742:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_odd_numbers(numbers):  # defines a function with parameters numbers
    return [x for x in numbers if x % 2 != 0]  # return sends the result back to the caller
#
# Operators: % !=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_odd_numbers(?)  # arguments evaluated first, then function body runs
get_odd_numbers(numbers)  # arguments evaluated first, then function body runs
? != 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_odd_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_odd_numbers(numbers):  # stored in memory, not executed yet
def get_odd_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_odd_numbers(...))  # get_odd_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x % 2 != 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_odd_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_odd_numbers(...))  # get_odd_numbers() runs first → value goes to print()

"""

"""
Problem 1743:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_positive_numbers(numbers):  # defines a function with parameters numbers
    return [x for x in numbers if x > 0]  # return sends the result back to the caller
#
# Operators: >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_positive_numbers(?)  # arguments evaluated first, then function body runs
get_positive_numbers(numbers)  # arguments evaluated first, then function body runs
x > 0  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_positive_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_positive_numbers(numbers):  # stored in memory, not executed yet
def get_positive_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_positive_numbers(...))  # get_positive_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x > 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_positive_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_positive_numbers(...))  # get_positive_numbers() runs first → value goes to print()

"""

"""
Problem 1744:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_negative_numbers(numbers):  # defines a function with parameters numbers
    return [x for x in numbers if x < 0]  # return sends the result back to the caller
#
# Operators: <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_negative_numbers(?)  # arguments evaluated first, then function body runs
get_negative_numbers(numbers)  # arguments evaluated first, then function body runs
x < 0  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_negative_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_negative_numbers(numbers):  # stored in memory, not executed yet
def get_negative_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_negative_numbers(...))  # get_negative_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x < 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_negative_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_negative_numbers(...))  # get_negative_numbers() runs first → value goes to print()

"""

"""
Problem 1745:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_numbers_greater_than(numbers, threshold):  # defines a function with parameters numbers, threshold
    return [x for x in numbers if x > threshold]  # return sends the result back to the caller
#
# Operators: >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_numbers_greater_than(?, 10)  # arguments evaluated first, then function body runs
get_numbers_greater_than(numbers, threshold)  # arguments evaluated first, then function body runs
print(get_numbers_greater_than(?, 25))  # get_numbers_greater_than() runs first, then print() outputs the result
x > threshold  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_numbers_greater_than(numbers, threshold):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_numbers_greater_than(numbers, threshold):  # stored in memory, not executed yet
def get_numbers_greater_than(numbers, threshold):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
threshold = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_numbers_greater_than(...))  # get_numbers_greater_than() runs with numbers=[4, 1, 8, 6, 3], threshold=25
#   → return [x for x in numbers if x > threshold]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_greater_than is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → threshold = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_greater_than(...))  # get_numbers_greater_than() runs first → value goes to print()

"""

"""
Problem 1746:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_numbers_less_than(numbers, threshold):  # defines a function with parameters numbers, threshold
    return [x for x in numbers if x < threshold]  # return sends the result back to the caller
#
# Operators: <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_numbers_less_than(?, 10)  # arguments evaluated first, then function body runs
get_numbers_less_than(numbers, threshold)  # arguments evaluated first, then function body runs
print(get_numbers_less_than(?, 25))  # get_numbers_less_than() runs first, then print() outputs the result
x < threshold  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_numbers_less_than(numbers, threshold):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_numbers_less_than(numbers, threshold):  # stored in memory, not executed yet
def get_numbers_less_than(numbers, threshold):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
threshold = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_numbers_less_than(...))  # get_numbers_less_than() runs with numbers=[4, 1, 8, 6, 3], threshold=25
#   → return [x for x in numbers if x < threshold]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_less_than is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → threshold = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_less_than(...))  # get_numbers_less_than() runs first → value goes to print()

"""

"""
Problem 1747:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_sum_of_list(numbers):  # defines a function with parameters numbers
    return sum(numbers)  # return sends the result back to the caller
#
# Keywords: for
# Built-in functions: reduce, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_sum_of_list(?)  # arguments evaluated first, then function body runs
calculate_sum_of_list(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_sum_of_list(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_sum_of_list(numbers):  # stored in memory, not executed yet
def calculate_sum_of_list(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_sum_of_list(...))  # calculate_sum_of_list() runs with numbers=[4, 1, 8, 6, 3]
#   → from functools import reduce
#   → import operator
#   → return reduce(operator.add, numbers, 0)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_sum_of_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_sum_of_list(...))  # calculate_sum_of_list() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done

"""

"""
Problem 1748:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_product_of_list(numbers):  # defines a function with parameters numbers
    product = 1
    for num in numbers:
        product *= num
    return product  # return sends the result back to the caller
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_product_of_list(?)  # arguments evaluated first, then function body runs
calculate_product_of_list(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_product_of_list(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_product_of_list(numbers):  # stored in memory, not executed yet
def calculate_product_of_list(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_product_of_list(...))  # calculate_product_of_list() runs with numbers=[4, 1, 8, 6, 3]
#   → return product
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_product_of_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_product_of_list(...))  # calculate_product_of_list() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done

"""

"""
Problem 1749:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_average_of_list(numbers):  # defines a function with parameters numbers
    return sum(numbers) / len(numbers)  # return sends the result back to the caller
#
# Keywords: for
# Operators: /
# Built-in functions: len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_average_of_list(?)  # arguments evaluated first, then function body runs
get_average_of_list(numbers)  # arguments evaluated first, then function body runs
sum(...) / len(...)  # sum(...) → then len(...) → then /
total / count  # total → then count → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_average_of_list(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_average_of_list(numbers):  # stored in memory, not executed yet
def get_average_of_list(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_average_of_list(...))  # get_average_of_list() runs with numbers=[4, 1, 8, 6, 3]
#   → total = 0
#   → count = 0
#   → for item in numbers:
#   →     total += item
#   →     count += 1
#   → return total / count if count else 0
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_average_of_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_average_of_list(...))  # get_average_of_list() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done

"""

"""
Problem 1750:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def create_dictionary(keys, values):  # defines a function with parameters keys, values
    return dict(zip(keys, values))  # return sends the result back to the caller
#
# Built-in functions: dict, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
create_dictionary(?, ?)  # arguments evaluated first, then function body runs
create_dictionary(keys, values)  # arguments evaluated first, then function body runs
print(create_dictionary(25, ?))  # create_dictionary() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def create_dictionary(keys, values):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def create_dictionary(keys, values):  # stored in memory, not executed yet
def create_dictionary(keys, values):  # stored in memory, not executed yet
keys = ...  # assigns a value to the variable
values = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(create_dictionary(...))  # create_dictionary() runs with keys=25, values=[4, 1, 8, 6, 3]
#   → return dict(zip(keys, values))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_dictionary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_dictionary(...))  # create_dictionary() runs first → value goes to print()

"""

"""
Problem 1751:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_dictionary_value(d, key):  # defines a function with parameters d, key
    return d.get(key)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(get_dictionary_value(?, 'a'))  # get_dictionary_value() runs first, then print() outputs the result
print(get_dictionary_value(?, 'c'))  # get_dictionary_value() runs first, then print() outputs the result
get_dictionary_value(d, key)  # arguments evaluated first, then function body runs
print(get_dictionary_value(10, 'value'))  # get_dictionary_value() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_dictionary_value(d, key):  # stored in memory, not executed yet
print(get_dictionary_value(...), get_dictionary_value(...))  # get_dictionary_value() runs with d=?, key='a'
#   → return d.get(key)
def get_dictionary_value(d, key):  # stored in memory, not executed yet
def get_dictionary_value(d, key):  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
key = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_dictionary_value(...))  # get_dictionary_value() runs with d=10, key='value'
#   → return d.get(key)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_dictionary_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_dictionary_value(...), get_dictionary_value(...))  # get_dictionary_value() runs first → value goes to print()
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_dictionary_value(...))  # get_dictionary_value() runs first → value goes to print()

"""

"""
Problem 1752:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_dictionary_keys(d):  # defines a function with parameters d
    return list(d.keys())  # return sends the result back to the caller
#
# Built-in functions: list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_dictionary_keys(?)  # arguments evaluated first, then function body runs
get_dictionary_keys(d)  # arguments evaluated first, then function body runs
print(get_dictionary_keys(10))  # get_dictionary_keys() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_dictionary_keys(d):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_dictionary_keys(d):  # stored in memory, not executed yet
def get_dictionary_keys(d):  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_dictionary_keys(...))  # get_dictionary_keys() runs with d=10
#   → return list(d.keys())
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_dictionary_keys is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_dictionary_keys(...))  # get_dictionary_keys() runs first → value goes to print()

"""

"""
Problem 1753:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_dictionary_values(d):  # defines a function with parameters d
    return list(d.values())  # return sends the result back to the caller
#
# Built-in functions: list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_dictionary_values(?)  # arguments evaluated first, then function body runs
get_dictionary_values(d)  # arguments evaluated first, then function body runs
print(get_dictionary_values(10))  # get_dictionary_values() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_dictionary_values(d):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_dictionary_values(d):  # stored in memory, not executed yet
def get_dictionary_values(d):  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_dictionary_values(...))  # get_dictionary_values() runs with d=10
#   → return list(d.values())
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_dictionary_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_dictionary_values(...))  # get_dictionary_values() runs first → value goes to print()

"""

"""
Problem 1754:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_key_in_dictionary(d, key):  # defines a function with parameters d, key
    return key in d  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_key_in_dictionary(?, 'a'))  # check_key_in_dictionary() runs first, then print() outputs the result
print(check_key_in_dictionary(?, 'b'))  # check_key_in_dictionary() runs first, then print() outputs the result
check_key_in_dictionary(d, key)  # arguments evaluated first, then function body runs
print(check_key_in_dictionary(10, 'value'))  # check_key_in_dictionary() runs first, then print() outputs the result
key in d  # evaluated as True or False → picks which branch
element == key  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_key_in_dictionary(d, key):  # stored in memory, not executed yet
print(check_key_in_dictionary(...), check_key_in_dictionary(...))  # check_key_in_dictionary() runs with d=?, key='a'
#   → return any((element == key for element in d))
def check_key_in_dictionary(d, key):  # stored in memory, not executed yet
def check_key_in_dictionary(d, key):  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
key = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_key_in_dictionary(...))  # check_key_in_dictionary() runs with d=10, key='value'
#   → return any((element == key for element in d))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_key_in_dictionary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_key_in_dictionary(...), check_key_in_dictionary(...))  # check_key_in_dictionary() runs first → value goes to print()
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_key_in_dictionary(...))  # check_key_in_dictionary() runs first → value goes to print()
#
# Loop: takes one item from d, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1755:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def add_to_dictionary(d, key, value):  # defines a function with parameters d, key, value
    new_dict = d.copy()
    new_dict[key] = value
    return new_dict  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
add_to_dictionary(?, 'b', 2)  # arguments evaluated first, then function body runs
add_to_dictionary(d, key, value)  # arguments evaluated first, then function body runs
print(add_to_dictionary(10, 'value', 25))  # add_to_dictionary() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def add_to_dictionary(d, key, value):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def add_to_dictionary(d, key, value):  # stored in memory, not executed yet
def add_to_dictionary(d, key, value):  # stored in memory, not executed yet
d = ...  # assigns a value to the variable
key = ...  # assigns a value to the variable
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(add_to_dictionary(...))  # add_to_dictionary() runs with d=10, key='value', value=25
#   → return new_dict
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_to_dictionary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_to_dictionary(...))  # add_to_dictionary() runs first → value goes to print()

"""

"""
Problem 1756:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def create_tuple():  # defines a function with no parameters
    return tuple(args)  # return sends the result back to the caller
#
# Built-in functions: tuple()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
create_tuple(1, 2, 3, 4)  # arguments evaluated first, then function body runs
create_tuple(?)  # arguments evaluated first, then function body runs
print(create_tuple(25))  # create_tuple() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def create_tuple():  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def create_tuple():  # stored in memory, not executed yet
def create_tuple():  # stored in memory, not executed yet
? = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(create_tuple(...))  # create_tuple() runs with 
#   → return tuple(args)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_tuple is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_tuple(...))  # create_tuple() runs first → value goes to print()

"""

"""
Problem 1757:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_tuple_element(my_tuple, index):  # defines a function with parameters my_tuple, index
    return my_tuple[index]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_tuple_element(?, 1)  # arguments evaluated first, then function body runs
get_tuple_element(my_tuple, index)  # arguments evaluated first, then function body runs
print(get_tuple_element(?, 4))  # get_tuple_element() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_tuple_element(my_tuple, index):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_tuple_element(my_tuple, index):  # stored in memory, not executed yet
def get_tuple_element(my_tuple, index):  # stored in memory, not executed yet
my_tuple = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_tuple_element(...))  # get_tuple_element() runs with my_tuple=[4, 1, 8, 6, 3], index=4
#   → return my_tuple[index]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_tuple_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_tuple = ...  # stored in memory
#   → index = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_tuple_element(...))  # get_tuple_element() runs first → value goes to print()

"""

"""
Problem 1758:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def unpack_tuple(my_tuple):  # defines a function with parameters my_tuple
    a, b = my_tuple
    return (a, b)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
unpack_tuple(?)  # arguments evaluated first, then function body runs
unpack_tuple(my_tuple)  # arguments evaluated first, then function body runs
print(unpack_tuple(25))  # unpack_tuple() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def unpack_tuple(my_tuple):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def unpack_tuple(my_tuple):  # stored in memory, not executed yet
def unpack_tuple(my_tuple):  # stored in memory, not executed yet
my_tuple = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(unpack_tuple(...))  # unpack_tuple() runs with my_tuple=25
#   → return (a, b)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only unpack_tuple is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_tuple = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(unpack_tuple(...))  # unpack_tuple() runs first → value goes to print()

"""

"""
Problem 1759:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def create_set(my_list):  # defines a function with parameters my_list
    return set(my_list)  # return sends the result back to the caller
#
# Built-in functions: set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
create_set(?)  # arguments evaluated first, then function body runs
create_set(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def create_set(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def create_set(my_list):  # stored in memory, not executed yet
def create_set(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(create_set(...))  # create_set() runs with my_list=[4, 1, 8, 6, 3]
#   → return set(my_list)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_set is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_set(...))  # create_set() runs first → value goes to print()

"""

"""
Problem 1760:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_element_in_set(my_set, element):  # defines a function with parameters my_set, element
    return element in my_set  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_element_in_set(?, 2))  # check_element_in_set() runs first, then print() outputs the result
print(check_element_in_set(?, 5))  # check_element_in_set() runs first, then print() outputs the result
check_element_in_set(my_set, element)  # arguments evaluated first, then function body runs
print(check_element_in_set(25, 25))  # check_element_in_set() runs first, then print() outputs the result
element in my_set  # evaluated as True or False → picks which branch
element == element  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_element_in_set(my_set, element):  # stored in memory, not executed yet
print(check_element_in_set(...), check_element_in_set(...))  # check_element_in_set() runs with my_set=?, element=2
#   → return any((element == element for element in my_set))
def check_element_in_set(my_set, element):  # stored in memory, not executed yet
def check_element_in_set(my_set, element):  # stored in memory, not executed yet
my_set = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_element_in_set(...))  # check_element_in_set() runs with my_set=25, element=25
#   → return any((element == element for element in my_set))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_element_in_set is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_element_in_set(...), check_element_in_set(...))  # check_element_in_set() runs first → value goes to print()
#   → my_set = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_element_in_set(...))  # check_element_in_set() runs first → value goes to print()
#
# Loop: takes one item from my_set, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1761:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def union_sets(set1, set2):  # defines a function with parameters set1, set2
    return set1 | set2  # return sends the result back to the caller
#
# Operators: |
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
union_sets(?, ?)  # arguments evaluated first, then function body runs
union_sets(set1, set2)  # arguments evaluated first, then function body runs
print(union_sets(25, 25))  # union_sets() runs first, then print() outputs the result
set1 | set2  # set1 → then set2 → then |
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def union_sets(set1, set2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def union_sets(set1, set2):  # stored in memory, not executed yet
def union_sets(set1, set2):  # stored in memory, not executed yet
set1 = ...  # assigns a value to the variable
set2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(union_sets(...))  # union_sets() runs with set1=25, set2=25
#   → return set1 | set2
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only union_sets is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → set1 = ...  # stored in memory
#   → set2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(union_sets(...))  # union_sets() runs first → value goes to print()

"""

"""
Problem 1762:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def intersection_sets(set1, set2):  # defines a function with parameters set1, set2
    return set1 & set2  # return sends the result back to the caller
#
# Operators: &
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
intersection_sets(?, ?)  # arguments evaluated first, then function body runs
intersection_sets(set1, set2)  # arguments evaluated first, then function body runs
print(intersection_sets(25, 25))  # intersection_sets() runs first, then print() outputs the result
set1 & set2  # set1 → then set2 → then &
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def intersection_sets(set1, set2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def intersection_sets(set1, set2):  # stored in memory, not executed yet
def intersection_sets(set1, set2):  # stored in memory, not executed yet
set1 = ...  # assigns a value to the variable
set2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(intersection_sets(...))  # intersection_sets() runs with set1=25, set2=25
#   → return set1 & set2
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only intersection_sets is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → set1 = ...  # stored in memory
#   → set2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(intersection_sets(...))  # intersection_sets() runs first → value goes to print()

"""

"""
Problem 1763:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def difference_sets(set1, set2):  # defines a function with parameters set1, set2
    return set1 - set2  # return sends the result back to the caller
#
# Operators: + -
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
difference_sets(?, ?)  # arguments evaluated first, then function body runs
difference_sets(set1, set2)  # arguments evaluated first, then function body runs
print(difference_sets(25, 25))  # difference_sets() runs first, then print() outputs the result
set1 - set2  # set1 → then set2 → then -
set1 + ?  # set1 → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def difference_sets(set1, set2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def difference_sets(set1, set2):  # stored in memory, not executed yet
def difference_sets(set1, set2):  # stored in memory, not executed yet
set1 = ...  # assigns a value to the variable
set2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(difference_sets(...))  # difference_sets() runs with set1=25, set2=25
#   → import operator
#   → return operator.sub(set1, set2)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only difference_sets is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → set1 = ...  # stored in memory
#   → set2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(difference_sets(...))  # difference_sets() runs first → value goes to print()

"""

"""
Problem 1764:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def read_file_content(filename):  # defines a function with parameters filename
    with open(filename, 'r') as f:
        return f.read()
#
# Built-in functions: open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
read_file_content(filename)  # arguments evaluated first, then function body runs
print(read_file_content(25))  # read_file_content() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def read_file_content(filename):  # stored in memory, not executed yet
print('Function defined: read_file_content(filename)')
def read_file_content(filename):  # stored in memory, not executed yet
def read_file_content(filename):  # stored in memory, not executed yet
filename = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(read_file_content(...))  # read_file_content() runs with filename=25
#   → return f.read()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only read_file_content is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Function defined: read_file_content(filename)')
#   → filename = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(read_file_content(...))  # read_file_content() runs first → value goes to print()

"""

"""
Problem 1765:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def write_to_file(filename, content):  # defines a function with parameters filename, content
    with open(filename, 'w') as f:
        f.write(content)
#
# Built-in functions: open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
write_to_file(filename, content)  # arguments evaluated first, then function body runs
print(write_to_file(25, 25))  # write_to_file() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def write_to_file(filename, content):  # stored in memory, not executed yet
print('Function defined: write_to_file(filename, content)')
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
filename = ...  # assigns a value to the variable
content = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(write_to_file(...))  # write_to_file() runs with filename=25, content=25
#   → with open(filename, 'w') as f:
#   →     f.write(content)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, write_to_file are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Function defined: write_to_file(filename, content)')
#   → filename = ...  # stored in memory
#   → content = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(write_to_file(...))  # write_to_file() runs first → value goes to print()

"""

"""
Problem 1766:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def append_to_file(filename, content):  # defines a function with parameters filename, content
    with open(filename, 'a') as f:
        f.write(content)
#
# Built-in functions: open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
append_to_file(filename, content)  # arguments evaluated first, then function body runs
print(append_to_file(25, 25))  # append_to_file() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def append_to_file(filename, content):  # stored in memory, not executed yet
print('Function defined: append_to_file(filename, content)')
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
filename = ...  # assigns a value to the variable
content = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(append_to_file(...))  # append_to_file() runs with filename=25, content=25
#   → with open(filename, 'a') as f:
#   →     f.write(content)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — append_to_file, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Function defined: append_to_file(filename, content)')
#   → filename = ...  # stored in memory
#   → content = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(append_to_file(...))  # append_to_file() runs first → value goes to print()

"""

"""
Problem 1767:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def safe_divide(a, b):  # defines a function with parameters a, b
    try:
        return a / b
    except ZeroDivisionError:
        return None
#
# Keywords: try/except
# Operators: * /
# Built-in functions: Fraction, float()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(safe_divide(10, 2))  # safe_divide() runs first, then print() outputs the result
print(safe_divide(10, 0))  # safe_divide() runs first, then print() outputs the result
safe_divide(a, b)  # arguments evaluated first, then function body runs
print(safe_divide(20, 30))  # safe_divide() runs first, then print() outputs the result
a * ?  # a → then ? → then *
a / b  # a → then b → then /
1 / b  # 1 → then b → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def safe_divide(a, b):  # stored in memory, not executed yet
print(safe_divide(...), safe_divide(...))  # safe_divide() runs with a=10, b=2
#   → from fractions import Fraction
#   → return float(Fraction(a, b))
def safe_divide(a, b):  # stored in memory, not executed yet
def safe_divide(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(safe_divide(...))  # safe_divide() runs with a=20, b=30
#   → from fractions import Fraction
#   → return float(Fraction(a, b))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only safe_divide is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(safe_divide(...), safe_divide(...))  # safe_divide() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(safe_divide(...))  # safe_divide() runs first → value goes to print()

"""

"""
Problem 1768:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def safe_convert_to_int(text):  # defines a function with parameters text
    try:
        return int(text)
    except ValueError:
        return None
#
# Keywords: try/except
# Built-in functions: int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(safe_convert_to_int('123'))  # safe_convert_to_int() runs first, then print() outputs the result
print(safe_convert_to_int('abc'))  # safe_convert_to_int() runs first, then print() outputs the result
safe_convert_to_int(text)  # arguments evaluated first, then function body runs
print(safe_convert_to_int('world'))  # safe_convert_to_int() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def safe_convert_to_int(text):  # stored in memory, not executed yet
print(safe_convert_to_int(...), safe_convert_to_int(...))  # safe_convert_to_int() runs with text='123'
#   → return int(text)
def safe_convert_to_int(text):  # stored in memory, not executed yet
def safe_convert_to_int(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(safe_convert_to_int(...))  # safe_convert_to_int() runs with text='world'
#   → return int(text)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only safe_convert_to_int is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(safe_convert_to_int(...), safe_convert_to_int(...))  # safe_convert_to_int() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(safe_convert_to_int(...))  # safe_convert_to_int() runs first → value goes to print()

"""

"""
Problem 1769:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def safe_get_list_element(my_list, index):  # defines a function with parameters my_list, index
    try:
        return my_list[index]
    except IndexError:
        return None
#
# Keywords: try/except
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(safe_get_list_element(?, 1))  # safe_get_list_element() runs first, then print() outputs the result
print(safe_get_list_element(?, 10))  # safe_get_list_element() runs first, then print() outputs the result
safe_get_list_element(my_list, index)  # arguments evaluated first, then function body runs
print(safe_get_list_element(?, 4))  # safe_get_list_element() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def safe_get_list_element(my_list, index):  # stored in memory, not executed yet
print(safe_get_list_element(...), safe_get_list_element(...))  # safe_get_list_element() runs with my_list=[1, 2, 3], index=1
#   → return my_list[index]
def safe_get_list_element(my_list, index):  # stored in memory, not executed yet
def safe_get_list_element(my_list, index):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(safe_get_list_element(...))  # safe_get_list_element() runs with my_list=[4, 1, 8, 6, 3], index=4
#   → return my_list[index]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only safe_get_list_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(safe_get_list_element(...), safe_get_list_element(...))  # safe_get_list_element() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(safe_get_list_element(...))  # safe_get_list_element() runs first → value goes to print()

"""

"""
Problem 1770:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def join_strings(strings, separator):  # defines a function with parameters strings, separator
    return separator.join(strings)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
join_strings(?, '-')  # arguments evaluated first, then function body runs
join_strings(strings, separator)  # arguments evaluated first, then function body runs
print(join_strings('world', 'world'))  # join_strings() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def join_strings(strings, separator):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def join_strings(strings, separator):  # stored in memory, not executed yet
def join_strings(strings, separator):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
separator = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(join_strings(...))  # join_strings() runs with strings='world', separator='world'
#   → return separator.join(strings)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only join_strings is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → separator = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(join_strings(...))  # join_strings() runs first → value goes to print()

"""

"""
Problem 1771:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def split_string(text, delimiter):  # defines a function with parameters text, delimiter
    return text.split(delimiter)  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
split_string('Hello-World-Python', '-')  # arguments evaluated first, then function body runs
split_string(text, delimiter)  # arguments evaluated first, then function body runs
print(split_string('world', 25))  # split_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def split_string(text, delimiter):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def split_string(text, delimiter):  # stored in memory, not executed yet
def split_string(text, delimiter):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
delimiter = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(split_string(...))  # split_string() runs with text='world', delimiter=25
#   → return text.split(delimiter)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only split_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → delimiter = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(split_string(...))  # split_string() runs first → value goes to print()

"""

"""
Problem 1772:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def strip_whitespace(text):  # defines a function with parameters text
    return text.strip()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
strip_whitespace('  Hello World  ')  # arguments evaluated first, then function body runs
strip_whitespace(text)  # arguments evaluated first, then function body runs
print(strip_whitespace('world'))  # strip_whitespace() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def strip_whitespace(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def strip_whitespace(text):  # stored in memory, not executed yet
def strip_whitespace(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(strip_whitespace(...))  # strip_whitespace() runs with text='world'
#   → return text.strip()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only strip_whitespace is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(strip_whitespace(...))  # strip_whitespace() runs first → value goes to print()

"""

"""
Problem 1773:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def capitalize_first_letter(text):  # defines a function with parameters text
    return text.capitalize()  # return sends the result back to the caller
#
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
capitalize_first_letter('hello world')  # arguments evaluated first, then function body runs
capitalize_first_letter(text)  # arguments evaluated first, then function body runs
print(capitalize_first_letter('world'))  # capitalize_first_letter() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def capitalize_first_letter(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def capitalize_first_letter(text):  # stored in memory, not executed yet
def capitalize_first_letter(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(capitalize_first_letter(...))  # capitalize_first_letter() runs with text='world'
#   → return str(text).capitalize()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only capitalize_first_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(capitalize_first_letter(...))  # capitalize_first_letter() runs first → value goes to print()

"""

"""
Problem 1774:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def swap_case(text):  # defines a function with parameters text
    return text.swapcase()  # return sends the result back to the caller
#
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
swap_case('Hello World')  # arguments evaluated first, then function body runs
swap_case(text)  # arguments evaluated first, then function body runs
print(swap_case('world'))  # swap_case() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def swap_case(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def swap_case(text):  # stored in memory, not executed yet
def swap_case(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(swap_case(...))  # swap_case() runs with text='world'
#   → return str(text).swapcase()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_case is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_case(...))  # swap_case() runs first → value goes to print()

"""

"""
Problem 1775:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_digits(text):  # defines a function with parameters text
    return text.isdigit()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_all_digits('12345'))  # check_all_digits() runs first, then print() outputs the result
print(check_all_digits('12a45'))  # check_all_digits() runs first, then print() outputs the result
check_all_digits(text)  # arguments evaluated first, then function body runs
print(check_all_digits('world'))  # check_all_digits() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_digits(text):  # stored in memory, not executed yet
print(check_all_digits(...), check_all_digits(...))  # check_all_digits() runs with text='12345'
#   → return text.isdigit()
def check_all_digits(text):  # stored in memory, not executed yet
def check_all_digits(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_digits(...))  # check_all_digits() runs with text='world'
#   → return text.isdigit()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_digits(...), check_all_digits(...))  # check_all_digits() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_digits(...))  # check_all_digits() runs first → value goes to print()

"""

"""
Problem 1776:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_letters(text):  # defines a function with parameters text
    return text.isalpha()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_all_letters('Hello'))  # check_all_letters() runs first, then print() outputs the result
print(check_all_letters('Hello123'))  # check_all_letters() runs first, then print() outputs the result
check_all_letters(text)  # arguments evaluated first, then function body runs
print(check_all_letters('world'))  # check_all_letters() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_letters(text):  # stored in memory, not executed yet
print(check_all_letters(...), check_all_letters(...))  # check_all_letters() runs with text='Hello'
#   → return text.isalpha()
def check_all_letters(text):  # stored in memory, not executed yet
def check_all_letters(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_letters(...))  # check_all_letters() runs with text='world'
#   → return text.isalpha()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_letters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_letters(...), check_all_letters(...))  # check_all_letters() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_letters(...))  # check_all_letters() runs first → value goes to print()

"""

"""
Problem 1777:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_alphanumeric(text):  # defines a function with parameters text
    return text.isalnum()  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_all_alphanumeric('Hello123'))  # check_all_alphanumeric() runs first, then print() outputs the result
print(check_all_alphanumeric('Hello 123'))  # check_all_alphanumeric() runs first, then print() outputs the result
check_all_alphanumeric(text)  # arguments evaluated first, then function body runs
print(check_all_alphanumeric('world'))  # check_all_alphanumeric() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_alphanumeric(text):  # stored in memory, not executed yet
print(check_all_alphanumeric(...), check_all_alphanumeric(...))  # check_all_alphanumeric() runs with text='Hello123'
#   → return text.isalnum()
def check_all_alphanumeric(text):  # stored in memory, not executed yet
def check_all_alphanumeric(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_alphanumeric(...))  # check_all_alphanumeric() runs with text='world'
#   → return text.isalnum()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_alphanumeric is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_alphanumeric(...), check_all_alphanumeric(...))  # check_all_alphanumeric() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_alphanumeric(...))  # check_all_alphanumeric() runs first → value goes to print()

"""

"""
Problem 1778:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_squared_numbers(numbers):  # defines a function with parameters numbers
    return [x ** 2 for x in numbers]  # return sends the result back to the caller
#
# Operators: **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_squared_numbers(?)  # arguments evaluated first, then function body runs
get_squared_numbers(numbers)  # arguments evaluated first, then function body runs
x ** 2  # x → then 2 → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_squared_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_squared_numbers(numbers):  # stored in memory, not executed yet
def get_squared_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_squared_numbers(...))  # get_squared_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x ** 2 for x in numbers]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_squared_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_squared_numbers(...))  # get_squared_numbers() runs first → value goes to print()

"""

"""
Problem 1779:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_doubled_numbers(numbers):  # defines a function with parameters numbers
    return [x * 2 for x in numbers]  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_doubled_numbers(?)  # arguments evaluated first, then function body runs
get_doubled_numbers(numbers)  # arguments evaluated first, then function body runs
x * 2  # x → then 2 → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_doubled_numbers(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_doubled_numbers(numbers):  # stored in memory, not executed yet
def get_doubled_numbers(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_doubled_numbers(...))  # get_doubled_numbers() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x * 2 for x in numbers]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_doubled_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_doubled_numbers(...))  # get_doubled_numbers() runs first → value goes to print()

"""

"""
Problem 1780:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_even_numbers_comprehension(numbers):  # defines a function with parameters numbers
    return [x for x in numbers if x % 2 == 0]  # return sends the result back to the caller
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_even_numbers_comprehension(?)  # arguments evaluated first, then function body runs
get_even_numbers_comprehension(numbers)  # arguments evaluated first, then function body runs
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_even_numbers_comprehension(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_even_numbers_comprehension(numbers):  # stored in memory, not executed yet
def get_even_numbers_comprehension(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_even_numbers_comprehension(...))  # get_even_numbers_comprehension() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x % 2 == 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_numbers_comprehension is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_numbers_comprehension(...))  # get_even_numbers_comprehension() runs first → value goes to print()

"""

"""
Problem 1781:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_uppercase_strings(strings):  # defines a function with parameters strings
    return [s.upper() for s in strings]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_uppercase_strings(?)  # arguments evaluated first, then function body runs
get_uppercase_strings(strings)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_uppercase_strings(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_uppercase_strings(strings):  # stored in memory, not executed yet
def get_uppercase_strings(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_uppercase_strings(...))  # get_uppercase_strings() runs with strings=[4, 1, 8, 6, 3]
#   → return [s.upper() for s in strings]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_uppercase_strings is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_uppercase_strings(...))  # get_uppercase_strings() runs first → value goes to print()

"""

"""
Problem 1782:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_string_lengths(strings):  # defines a function with parameters strings
    return [len(s) for s in strings]  # return sends the result back to the caller
#
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_string_lengths(?)  # arguments evaluated first, then function body runs
get_string_lengths(strings)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_string_lengths(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_string_lengths(strings):  # stored in memory, not executed yet
def get_string_lengths(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_string_lengths(...))  # get_string_lengths() runs with strings=[4, 1, 8, 6, 3]
#   → return [len(s) for s in strings]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_string_lengths is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_string_lengths(...))  # get_string_lengths() runs first → value goes to print()

"""

"""
Problem 1783:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def print_number_pattern(n):  # defines a function with parameters n
    for i in range(1, n + 1):
        for j in range(1, i + 1):
            print(j, end=' ')
        print()
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print_number_pattern(5)  # arguments evaluated first, then function body runs
print_number_pattern(n)  # arguments evaluated first, then function body runs
print(print_number_pattern(15))  # print_number_pattern() runs first, then print() outputs the result
n + 1  # n → then 1 → then +
i + 1  # i → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def print_number_pattern(n):  # stored in memory, not executed yet
print_number_pattern(5)  # calls print_number_pattern with n=5
#   → for i in range(1, n + 1):
#   →     for j in range(1, i + 1):
#   →         print(j, end=' ')
#   →     print()
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(print_number_pattern(...))  # print_number_pattern() runs with n=15
#   → for i in range(1, n + 1):
#   →     for j in range(1, i + 1):
#   →         print(j, end=' ')
#   →     print()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_number_pattern, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_number_pattern(5)  # jumps into print_number_pattern(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_number_pattern(...))  # print_number_pattern() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1784:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def print_star_pattern(n):  # defines a function with parameters n
    for i in range(1, n + 1):
        print('*' * i)
#
# Keywords: for
# Operators: * +
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print_star_pattern(5)  # arguments evaluated first, then function body runs
print_star_pattern(n)  # arguments evaluated first, then function body runs
print(print_star_pattern(15))  # print_star_pattern() runs first, then print() outputs the result
n + 1  # n → then 1 → then +
'*' * i  # '*' → then i → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def print_star_pattern(n):  # stored in memory, not executed yet
print_star_pattern(5)  # calls print_star_pattern with n=5
#   → for i in range(1, n + 1):
#   →     print('*' * i)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(print_star_pattern(...))  # print_star_pattern() runs with n=15
#   → for i in range(1, n + 1):
#   →     print('*' * i)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_star_pattern, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_star_pattern(5)  # jumps into print_star_pattern(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_star_pattern(...))  # print_star_pattern() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1785:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def print_reverse_star_pattern(n):  # defines a function with parameters n
    for i in range(n, 0, -1):
        print('*' * i)
#
# Keywords: for
# Operators: *
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print_reverse_star_pattern(5)  # arguments evaluated first, then function body runs
print_reverse_star_pattern(n)  # arguments evaluated first, then function body runs
print(print_reverse_star_pattern(15))  # print_reverse_star_pattern() runs first, then print() outputs the result
'*' * i  # '*' → then i → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def print_reverse_star_pattern(n):  # stored in memory, not executed yet
print_reverse_star_pattern(5)  # calls print_reverse_star_pattern with n=5
#   → for i in range(n, 0, -1):
#   →     print('*' * i)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(print_reverse_star_pattern(...))  # print_reverse_star_pattern() runs with n=15
#   → for i in range(n, 0, -1):
#   →     print('*' * i)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_reverse_star_pattern, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_reverse_star_pattern(5)  # jumps into print_reverse_star_pattern(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_reverse_star_pattern(...))  # print_reverse_star_pattern() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1786:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def print_number_square(n):  # defines a function with parameters n
    for i in range(1, n * n + 1, n):
        row = [str(j) for j in range(i, i + n)]
        print(' '.join(row))
#
# Keywords: for
# Operators: * +
# Built-in functions: range, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print_number_square(3)  # arguments evaluated first, then function body runs
print_number_square(n)  # arguments evaluated first, then function body runs
print(print_number_square(15))  # print_number_square() runs first, then print() outputs the result
? + 1  # ? → then 1 → then +
n * n  # n → then n → then *
i + n  # i → then n → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def print_number_square(n):  # stored in memory, not executed yet
print_number_square(3)  # calls print_number_square with n=3
#   → for i in range(1, n * n + 1, n):
#   →     row = [str(j) for j in range(i, i + n)]
#   → result = ' '.join(row)
#   → return result
def print_number_square(n):  # stored in memory, not executed yet
def print_number_square(n):  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(print_number_square(...))  # print_number_square() runs with n=15
#   → for i in range(1, n * n + 1, n):
#   →     row = [str(j) for j in range(i, i + n)]
#   → result = ' '.join(row)
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_number_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_number_square(3)  # jumps into print_number_square(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_number_square(...))  # print_number_square() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1787:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_factorial(n):  # defines a function with parameters n
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_factorial(5)  # arguments evaluated first, then function body runs
calculate_factorial(n)  # arguments evaluated first, then function body runs
print(calculate_factorial(15))  # calculate_factorial() runs first, then print() outputs the result
n + 1  # n → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_factorial(n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_factorial(n):  # stored in memory, not executed yet
def calculate_factorial(n):  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_factorial(...))  # calculate_factorial() runs with n=15
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_factorial is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_factorial(...))  # calculate_factorial() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1788:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_sum_range(start, end):  # defines a function with parameters start, end
    return sum(range(start, end + 1))  # return sends the result back to the caller
#
# Operators: +
# Built-in functions: range, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_sum_range(1, 10)  # arguments evaluated first, then function body runs
calculate_sum_range(start, end)  # arguments evaluated first, then function body runs
print(calculate_sum_range(3, 20))  # calculate_sum_range() runs first, then print() outputs the result
end + 1  # end → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_sum_range(start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_sum_range(start, end):  # stored in memory, not executed yet
def calculate_sum_range(start, end):  # stored in memory, not executed yet
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_sum_range(...))  # calculate_sum_range() runs with start=3, end=20
#   → return sum(range(start, end + 1))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_sum_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_sum_range(...))  # calculate_sum_range() runs first → value goes to print()

"""

"""
Problem 1789:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_product_range(start, end):  # defines a function with parameters start, end
    product = 1
    for i in range(start, end + 1):
        product *= i
    return product  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_product_range(1, 5)  # arguments evaluated first, then function body runs
calculate_product_range(start, end)  # arguments evaluated first, then function body runs
print(calculate_product_range(3, 20))  # calculate_product_range() runs first, then print() outputs the result
end + 1  # end → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_product_range(start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_product_range(start, end):  # stored in memory, not executed yet
def calculate_product_range(start, end):  # stored in memory, not executed yet
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_product_range(...))  # calculate_product_range() runs with start=3, end=20
#   → return product
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_product_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_product_range(...))  # calculate_product_range() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1790:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_fibonacci_sequence(n):  # defines a function with parameters n
    if n <= 0:
        return []
    elif n == 1:
        return [0]
    elif n == 2:
        return [0, 1]
    fib = [0, 1]
    for i in range(2, n):
        fib.append(fib[i - 1] + fib[i - 2])
    return fib  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: + - <= ==
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_fibonacci_sequence(8)  # arguments evaluated first, then function body runs
get_fibonacci_sequence(n)  # arguments evaluated first, then function body runs
print(get_fibonacci_sequence(15))  # get_fibonacci_sequence() runs first, then print() outputs the result
n <= 0  # evaluated as True or False → picks which branch
n == 1  # evaluated as True or False → picks which branch
n == 2  # evaluated as True or False → picks which branch
? + ?  # ? → then ? → then +
i - 1  # i → then 1 → then -
i - 2  # i → then 2 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_fibonacci_sequence(n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_fibonacci_sequence(n):  # stored in memory, not executed yet
def get_fibonacci_sequence(n):  # stored in memory, not executed yet
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_fibonacci_sequence(...))  # get_fibonacci_sequence() runs with n=15
#   → return []
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_fibonacci_sequence is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_fibonacci_sequence(...))  # get_fibonacci_sequence() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1791:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_palindrome_number(number):  # defines a function with parameters number
    return str(number) == str(number)[::-1]  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_palindrome_number(121))  # check_palindrome_number() runs first, then print() outputs the result
print(check_palindrome_number(123))  # check_palindrome_number() runs first, then print() outputs the result
check_palindrome_number(number)  # arguments evaluated first, then function body runs
print(check_palindrome_number(25))  # check_palindrome_number() runs first, then print() outputs the result
str(...) == ?  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_palindrome_number(number):  # stored in memory, not executed yet
print(check_palindrome_number(...), check_palindrome_number(...))  # check_palindrome_number() runs with number=121
#   → return str(number) == str(number)[::-1]
def check_palindrome_number(number):  # stored in memory, not executed yet
def check_palindrome_number(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_palindrome_number(...))  # check_palindrome_number() runs with number=25
#   → return str(number) == str(number)[::-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_palindrome_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_palindrome_number(...), check_palindrome_number(...))  # check_palindrome_number() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_palindrome_number(...))  # check_palindrome_number() runs first → value goes to print()

"""

"""
Problem 1792:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def sum_of_digits(number):  # defines a function with parameters number
    return sum((int(digit) for digit in str(number)))  # return sends the result back to the caller
#
# Built-in functions: int, str, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
sum_of_digits(12345)  # arguments evaluated first, then function body runs
sum_of_digits(number)  # arguments evaluated first, then function body runs
print(sum_of_digits(25))  # sum_of_digits() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def sum_of_digits(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def sum_of_digits(number):  # stored in memory, not executed yet
def sum_of_digits(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(sum_of_digits(...))  # sum_of_digits() runs with number=25
#   → return sum((int(digit) for digit in str(number)))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_of_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_of_digits(...))  # sum_of_digits() runs first → value goes to print()

"""

"""
Problem 1793:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def product_of_digits(number):  # defines a function with parameters number
    product = 1
    for digit in str(number):
        product *= int(digit)
    return product  # return sends the result back to the caller
#
# Keywords: for
# Built-in functions: int, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
product_of_digits(234)  # arguments evaluated first, then function body runs
product_of_digits(number)  # arguments evaluated first, then function body runs
print(product_of_digits(25))  # product_of_digits() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def product_of_digits(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def product_of_digits(number):  # stored in memory, not executed yet
def product_of_digits(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(product_of_digits(...))  # product_of_digits() runs with number=25
#   → return product
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only product_of_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(product_of_digits(...))  # product_of_digits() runs first → value goes to print()
#
# Loop: takes one item from str(...), runs body, repeats until done

"""

"""
Problem 1794:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_digits(number):  # defines a function with parameters number
    return len(str(number))  # return sends the result back to the caller
#
# Built-in functions: len, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_digits(12345)  # arguments evaluated first, then function body runs
count_digits(number)  # arguments evaluated first, then function body runs
print(count_digits(25))  # count_digits() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_digits(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_digits(number):  # stored in memory, not executed yet
def count_digits(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_digits(...))  # count_digits() runs with number=25
#   → return len(str(number))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_digits(...))  # count_digits() runs first → value goes to print()

"""

"""
Problem 1795:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def reverse_number(number):  # defines a function with parameters number
    return int(str(number)[::-1])  # return sends the result back to the caller
#
# Built-in functions: int, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
reverse_number(12345)  # arguments evaluated first, then function body runs
reverse_number(number)  # arguments evaluated first, then function body runs
print(reverse_number(25))  # reverse_number() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def reverse_number(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def reverse_number(number):  # stored in memory, not executed yet
def reverse_number(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(reverse_number(...))  # reverse_number() runs with number=25
#   → return int(str(number)[::-1])
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_number(...))  # reverse_number() runs first → value goes to print()

"""

"""
Problem 1796:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_first_n_elements(my_list, n):  # defines a function with parameters my_list, n
    return my_list[:n]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_first_n_elements(?, 3)  # arguments evaluated first, then function body runs
get_first_n_elements(my_list, n)  # arguments evaluated first, then function body runs
print(get_first_n_elements(?, 15))  # get_first_n_elements() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_first_n_elements(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_first_n_elements(my_list, n):  # stored in memory, not executed yet
def get_first_n_elements(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_first_n_elements(...))  # get_first_n_elements() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return my_list[:n]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_n_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_n_elements(...))  # get_first_n_elements() runs first → value goes to print()

"""

"""
Problem 1797:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_last_n_elements(my_list, n):  # defines a function with parameters my_list, n
    return my_list[-n:]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_last_n_elements(?, 3)  # arguments evaluated first, then function body runs
get_last_n_elements(my_list, n)  # arguments evaluated first, then function body runs
print(get_last_n_elements(?, 15))  # get_last_n_elements() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_last_n_elements(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_last_n_elements(my_list, n):  # stored in memory, not executed yet
def get_last_n_elements(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_last_n_elements(...))  # get_last_n_elements() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return my_list[-n:]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_n_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_n_elements(...))  # get_last_n_elements() runs first → value goes to print()

"""

"""
Problem 1798:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_duplicates_from_list(my_list):  # defines a function with parameters my_list
    seen = []
    result = []
    for item in my_list:
        if item not in seen:
            seen.append(item)
            result.append(item)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_duplicates_from_list(?)  # arguments evaluated first, then function body runs
remove_duplicates_from_list(my_list)  # arguments evaluated first, then function body runs
item not in seen  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_duplicates_from_list(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_duplicates_from_list(my_list):  # stored in memory, not executed yet
def remove_duplicates_from_list(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_duplicates_from_list(...))  # remove_duplicates_from_list() runs with my_list=[4, 1, 8, 6, 3]
#   → return list(map(lambda item: item, my_list))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_duplicates_from_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_duplicates_from_list(...))  # remove_duplicates_from_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1799:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def merge_two_lists(list1, list2):  # defines a function with parameters list1, list2
    result = []
    max_len = max(len(list1), len(list2))
    for i in range(max_len):
        if i < len(list1):
            result.append(list1[i])
        if i < len(list2):
            result.append(list2[i])
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: len, max, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
merge_two_lists(?, ?)  # arguments evaluated first, then function body runs
merge_two_lists(list1, list2)  # arguments evaluated first, then function body runs
i < len(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def merge_two_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def merge_two_lists(list1, list2):  # stored in memory, not executed yet
def merge_two_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(merge_two_lists(...))  # merge_two_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_two_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(merge_two_lists(...))  # merge_two_lists() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1800:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_common_elements(list1, list2):  # defines a function with parameters list1, list2
    return [x for x in list1 if x in list2]  # return sends the result back to the caller
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_common_elements(?, ?)  # arguments evaluated first, then function body runs
get_common_elements(list1, list2)  # arguments evaluated first, then function body runs
x in list2  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_common_elements(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_common_elements(list1, list2):  # stored in memory, not executed yet
def get_common_elements(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_common_elements(...))  # get_common_elements() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return [x for x in list1 if x in list2]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_common_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_common_elements(...))  # get_common_elements() runs first → value goes to print()

"""

"""
Problem 1801:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_unique_elements(list1, list2):  # defines a function with parameters list1, list2
    result = []
    for item in list1:
        if item not in list2:
            result.append(item)
    for item in list2:
        if item not in list1:
            result.append(item)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_unique_elements(?, ?)  # arguments evaluated first, then function body runs
get_unique_elements(list1, list2)  # arguments evaluated first, then function body runs
item not in list2  # evaluated as True or False → picks which branch
item not in list1  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_unique_elements(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_unique_elements(list1, list2):  # stored in memory, not executed yet
def get_unique_elements(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_unique_elements(...))  # get_unique_elements() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return list(map(lambda item: item, list1))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_elements(...))  # get_unique_elements() runs first → value goes to print()
#
# Loop: takes one item from list1, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1802:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def sort_list_ascending(numbers):  # defines a function with parameters numbers
    return sorted(numbers)  # return sends the result back to the caller
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
sort_list_ascending(?)  # arguments evaluated first, then function body runs
sort_list_ascending(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def sort_list_ascending(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def sort_list_ascending(numbers):  # stored in memory, not executed yet
def sort_list_ascending(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(sort_list_ascending(...))  # sort_list_ascending() runs with numbers=[4, 1, 8, 6, 3]
#   → return sorted(numbers, reverse=False)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_list_ascending is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_list_ascending(...))  # sort_list_ascending() runs first → value goes to print()

"""

"""
Problem 1803:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def sort_list_descending(numbers):  # defines a function with parameters numbers
    return sorted(numbers, reverse=True)  # return sends the result back to the caller
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
sort_list_descending(?)  # arguments evaluated first, then function body runs
sort_list_descending(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def sort_list_descending(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def sort_list_descending(numbers):  # stored in memory, not executed yet
def sort_list_descending(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(sort_list_descending(...))  # sort_list_descending() runs with numbers=[4, 1, 8, 6, 3]
#   → return sorted(numbers, reverse=True)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_list_descending is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_list_descending(...))  # sort_list_descending() runs first → value goes to print()

"""

"""
Problem 1804:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_second_largest(numbers):  # defines a function with parameters numbers
    unique_numbers = sorted(set(numbers), reverse=True)
    return unique_numbers[1] if len(unique_numbers) > 1 else None  # return sends the result back to the caller
#
# Operators: >
# Built-in functions: len, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_second_largest(?)  # arguments evaluated first, then function body runs
get_second_largest(numbers)  # arguments evaluated first, then function body runs
len(...) > 1  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_second_largest(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_second_largest(numbers):  # stored in memory, not executed yet
def get_second_largest(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_second_largest(...))  # get_second_largest() runs with numbers=[4, 1, 8, 6, 3]
#   → return unique_numbers[1] if len(unique_numbers) > 1 else None
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_second_largest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_second_largest(...))  # get_second_largest() runs first → value goes to print()

"""

"""
Problem 1805:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_second_smallest(numbers):  # defines a function with parameters numbers
    unique_numbers = sorted(set(numbers))
    return unique_numbers[1] if len(unique_numbers) > 1 else None  # return sends the result back to the caller
#
# Operators: >
# Built-in functions: len, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_second_smallest(?)  # arguments evaluated first, then function body runs
get_second_smallest(numbers)  # arguments evaluated first, then function body runs
len(...) > 1  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_second_smallest(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_second_smallest(numbers):  # stored in memory, not executed yet
def get_second_smallest(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_second_smallest(...))  # get_second_smallest() runs with numbers=[4, 1, 8, 6, 3]
#   → return unique_numbers[1] if len(unique_numbers) > 1 else None
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_second_smallest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_second_smallest(...))  # get_second_smallest() runs first → value goes to print()

"""

"""
Problem 1806:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_sorted(numbers):  # defines a function with parameters numbers
    return numbers == sorted(numbers)  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_sorted(?))  # check_sorted() runs first, then print() outputs the result
check_sorted(numbers)  # arguments evaluated first, then function body runs
numbers == sorted(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_sorted(numbers):  # stored in memory, not executed yet
print(check_sorted(...), check_sorted(...))  # check_sorted() runs with numbers=[1, 2, 3, 4]
#   → return numbers == sorted(numbers)
def check_sorted(numbers):  # stored in memory, not executed yet
def check_sorted(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_sorted(...))  # check_sorted() runs with numbers=[4, 1, 8, 6, 3]
#   → return numbers == sorted(numbers)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_sorted is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_sorted(...), check_sorted(...))  # check_sorted() runs first → value goes to print()
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_sorted(...))  # check_sorted() runs first → value goes to print()

"""

"""
Problem 1807:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_occurrences_in_list(my_list):  # defines a function with parameters my_list
    counts = {}
    for item in my_list:
        counts[item] = counts.get(item, 0) + 1
    return counts  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_occurrences_in_list(?)  # arguments evaluated first, then function body runs
count_occurrences_in_list(my_list)  # arguments evaluated first, then function body runs
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_occurrences_in_list(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_occurrences_in_list(my_list):  # stored in memory, not executed yet
def count_occurrences_in_list(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_occurrences_in_list(...))  # count_occurrences_in_list() runs with my_list=[4, 1, 8, 6, 3]
#   → return counts
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_occurrences_in_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_occurrences_in_list(...))  # count_occurrences_in_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1808:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_most_frequent(my_list):  # defines a function with parameters my_list
    counts = {}
    for item in my_list:
        counts[item] = counts.get(item, 0) + 1
    return max(counts, key=counts.get)  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_most_frequent(?)  # arguments evaluated first, then function body runs
get_most_frequent(my_list)  # arguments evaluated first, then function body runs
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_most_frequent(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_most_frequent(my_list):  # stored in memory, not executed yet
def get_most_frequent(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_most_frequent(...))  # get_most_frequent() runs with my_list=[4, 1, 8, 6, 3]
#   → return max(counts, key=counts.get)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_most_frequent is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_most_frequent(...))  # get_most_frequent() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1809:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_least_frequent(my_list):  # defines a function with parameters my_list
    counts = {}
    for item in my_list:
        counts[item] = counts.get(item, 0) + 1
    return min(counts, key=counts.get)  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_least_frequent(?)  # arguments evaluated first, then function body runs
get_least_frequent(my_list)  # arguments evaluated first, then function body runs
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_least_frequent(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_least_frequent(my_list):  # stored in memory, not executed yet
def get_least_frequent(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_least_frequent(...))  # get_least_frequent() runs with my_list=[4, 1, 8, 6, 3]
#   → return min(counts, key=counts.get)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_least_frequent is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_least_frequent(...))  # get_least_frequent() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1810:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def flatten_nested_list(nested_list):  # defines a function with parameters nested_list
    result = []
    for sublist in nested_list:
        result.extend(sublist)
    return result  # return sends the result back to the caller
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
flatten_nested_list(?)  # arguments evaluated first, then function body runs
flatten_nested_list(nested_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def flatten_nested_list(nested_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def flatten_nested_list(nested_list):  # stored in memory, not executed yet
def flatten_nested_list(nested_list):  # stored in memory, not executed yet
nested_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(flatten_nested_list(...))  # flatten_nested_list() runs with nested_list=[4, 1, 8, 6, 3]
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only flatten_nested_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → nested_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(flatten_nested_list(...))  # flatten_nested_list() runs first → value goes to print()
#
# Loop: takes one item from nested_list, runs body, repeats until done

"""

"""
Problem 1811:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def group_by_length(strings):  # defines a function with parameters strings
    groups = {}
    for s in strings:
        length = len(s)
        if length not in groups:
            groups[length] = []
        groups[length].append(s)
    return groups  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
group_by_length(?)  # arguments evaluated first, then function body runs
group_by_length(strings)  # arguments evaluated first, then function body runs
length not in groups  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def group_by_length(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def group_by_length(strings):  # stored in memory, not executed yet
def group_by_length(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(group_by_length(...))  # group_by_length() runs with strings=[4, 1, 8, 6, 3]
#   → return groups
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only group_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(group_by_length(...))  # group_by_length() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1812:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def filter_by_length(strings, min_length):  # defines a function with parameters strings, min_length
    return [s for s in strings if len(s) >= min_length]  # return sends the result back to the caller
#
# Operators: >=
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
filter_by_length(?, 3)  # arguments evaluated first, then function body runs
filter_by_length(strings, min_length)  # arguments evaluated first, then function body runs
print(filter_by_length(?, 25))  # filter_by_length() runs first, then print() outputs the result
len(...) >= min_length  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def filter_by_length(strings, min_length):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def filter_by_length(strings, min_length):  # stored in memory, not executed yet
def filter_by_length(strings, min_length):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
min_length = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(filter_by_length(...))  # filter_by_length() runs with strings=[4, 1, 8, 6, 3], min_length=25
#   → return [s for s in strings if len(s) >= min_length]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → min_length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(filter_by_length(...))  # filter_by_length() runs first → value goes to print()

"""

"""
Problem 1813:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_longest_string(strings):  # defines a function with parameters strings
    return max(strings, key=len)  # return sends the result back to the caller
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_longest_string(?)  # arguments evaluated first, then function body runs
get_longest_string(strings)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_longest_string(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_longest_string(strings):  # stored in memory, not executed yet
def get_longest_string(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_longest_string(...))  # get_longest_string() runs with strings=[4, 1, 8, 6, 3]
#   → return max(strings, key=len)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_longest_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_longest_string(...))  # get_longest_string() runs first → value goes to print()

"""

"""
Problem 1814:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_shortest_string(strings):  # defines a function with parameters strings
    return min(strings, key=len)  # return sends the result back to the caller
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_shortest_string(?)  # arguments evaluated first, then function body runs
get_shortest_string(strings)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_shortest_string(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_shortest_string(strings):  # stored in memory, not executed yet
def get_shortest_string(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_shortest_string(...))  # get_shortest_string() runs with strings=[4, 1, 8, 6, 3]
#   → return min(strings, key=len)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_shortest_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_shortest_string(...))  # get_shortest_string() runs first → value goes to print()

"""

"""
Problem 1815:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def capitalize_all_words(text):  # defines a function with parameters text
    return ' '.join((word.capitalize() for word in text.split()))  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
capitalize_all_words('hello world python')  # arguments evaluated first, then function body runs
capitalize_all_words(text)  # arguments evaluated first, then function body runs
print(capitalize_all_words('world'))  # capitalize_all_words() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def capitalize_all_words(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def capitalize_all_words(text):  # stored in memory, not executed yet
def capitalize_all_words(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(capitalize_all_words(...))  # capitalize_all_words() runs with text='world'
#   → return ' '.join((word.capitalize() for word in text.split()))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only capitalize_all_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(capitalize_all_words(...))  # capitalize_all_words() runs first → value goes to print()

"""

"""
Problem 1816:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def reverse_words(text):  # defines a function with parameters text
    words = text.split()
    return ' '.join(reversed(words))  # return sends the result back to the caller
#
# Built-in functions: reversed()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
reverse_words('hello world python')  # arguments evaluated first, then function body runs
reverse_words(text)  # arguments evaluated first, then function body runs
print(reverse_words('world'))  # reverse_words() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def reverse_words(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def reverse_words(text):  # stored in memory, not executed yet
def reverse_words(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(reverse_words(...))  # reverse_words() runs with text='world'
#   → return ' '.join(reversed(words))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_words(...))  # reverse_words() runs first → value goes to print()

"""

"""
Problem 1817:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_words(text):  # defines a function with parameters text
    return len(text.split())  # return sends the result back to the caller
#
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_words('Hello World Python')  # arguments evaluated first, then function body runs
count_words(text)  # arguments evaluated first, then function body runs
print(count_words('world'))  # count_words() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_words(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_words(text):  # stored in memory, not executed yet
def count_words(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_words(...))  # count_words() runs with text='world'
#   → return len(text.split())
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words(...))  # count_words() runs first → value goes to print()

"""

"""
Problem 1818:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_longest_word(text):  # defines a function with parameters text
    words = text.split()
    return max(words, key=len)  # return sends the result back to the caller
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_longest_word('hello world python programming')  # arguments evaluated first, then function body runs
get_longest_word(text)  # arguments evaluated first, then function body runs
print(get_longest_word('world'))  # get_longest_word() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_longest_word(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_longest_word(text):  # stored in memory, not executed yet
def get_longest_word(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_longest_word(...))  # get_longest_word() runs with text='world'
#   → return max(words, key=len)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_longest_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_longest_word(...))  # get_longest_word() runs first → value goes to print()

"""

"""
Problem 1819:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_shortest_word(text):  # defines a function with parameters text
    words = text.split()
    return min(words, key=len)  # return sends the result back to the caller
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_shortest_word('hello world python programming')  # arguments evaluated first, then function body runs
get_shortest_word(text)  # arguments evaluated first, then function body runs
print(get_shortest_word('world'))  # get_shortest_word() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_shortest_word(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_shortest_word(text):  # stored in memory, not executed yet
def get_shortest_word(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_shortest_word(...))  # get_shortest_word() runs with text='world'
#   → return min(words, key=len)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_shortest_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_shortest_word(...))  # get_shortest_word() runs first → value goes to print()

"""

"""
Problem 1820:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_vowels_from_string(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    return ''.join((char for char in text if char not in vowels))  # return sends the result back to the caller
#
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_vowels_from_string('Hello World')  # arguments evaluated first, then function body runs
remove_vowels_from_string(text)  # arguments evaluated first, then function body runs
print(remove_vowels_from_string('world'))  # remove_vowels_from_string() runs first, then print() outputs the result
char not in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_vowels_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_vowels_from_string(text):  # stored in memory, not executed yet
def remove_vowels_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_vowels_from_string(...))  # remove_vowels_from_string() runs with text='world'
#   → return ''.join((char for char in text if char not in vowels))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_vowels_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_vowels_from_string(...))  # remove_vowels_from_string() runs first → value goes to print()

"""

"""
Problem 1821:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_consonants_from_string(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    return ''.join((char for char in text if char in vowels or not char.isalpha()))  # return sends the result back to the caller
#
# Operators: in or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_consonants_from_string('Hello World')  # arguments evaluated first, then function body runs
remove_consonants_from_string(text)  # arguments evaluated first, then function body runs
print(remove_consonants_from_string('world'))  # remove_consonants_from_string() runs first, then print() outputs the result
char in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_consonants_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_consonants_from_string(text):  # stored in memory, not executed yet
def remove_consonants_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_consonants_from_string(...))  # remove_consonants_from_string() runs with text='world'
#   → return ''.join((char for char in text if char in vowels or not char.isalpha()))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_consonants_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_consonants_from_string(...))  # remove_consonants_from_string() runs first → value goes to print()

"""

"""
Problem 1822:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_vowels_in_string(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    return sum((1 for char in text if char in vowels))  # return sends the result back to the caller
#
# Operators: in
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_vowels_in_string('Hello World')  # arguments evaluated first, then function body runs
count_vowels_in_string(text)  # arguments evaluated first, then function body runs
print(count_vowels_in_string('world'))  # count_vowels_in_string() runs first, then print() outputs the result
char in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_vowels_in_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_vowels_in_string(text):  # stored in memory, not executed yet
def count_vowels_in_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_vowels_in_string(...))  # count_vowels_in_string() runs with text='world'
#   → return sum((1 for char in text if char in vowels))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_vowels_in_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_vowels_in_string(...))  # count_vowels_in_string() runs first → value goes to print()

"""

"""
Problem 1824:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def swap_first_last_words(text):  # defines a function with parameters text
    words = text.split()
    if len(words) < 2:
        return text
    words[0], words[-1] = (words[-1], words[0])
    return ' '.join(words)  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: <
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
swap_first_last_words('Hello World Python')  # arguments evaluated first, then function body runs
swap_first_last_words(text)  # arguments evaluated first, then function body runs
print(swap_first_last_words('world'))  # swap_first_last_words() runs first, then print() outputs the result
len(...) < 2  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def swap_first_last_words(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def swap_first_last_words(text):  # stored in memory, not executed yet
def swap_first_last_words(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(swap_first_last_words(...))  # swap_first_last_words() runs with text='world'
#   → return text
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_first_last_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_first_last_words(...))  # swap_first_last_words() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1825:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_middle_word(text):  # defines a function with parameters text
    words = text.split()
    return words[len(words) // 2]  # return sends the result back to the caller
#
# Operators: //
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_middle_word('Hello World Python Programming')  # arguments evaluated first, then function body runs
get_middle_word(text)  # arguments evaluated first, then function body runs
print(get_middle_word('world'))  # get_middle_word() runs first, then print() outputs the result
len(...) // 2  # len(...) → then 2 → then //
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_middle_word(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_middle_word(text):  # stored in memory, not executed yet
def get_middle_word(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_middle_word(...))  # get_middle_word() runs with text='world'
#   → return words[len(words) // 2]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_word(...))  # get_middle_word() runs first → value goes to print()

"""

"""
Problem 1826:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_same(my_list):  # defines a function with parameters my_list
    return all((x == my_list[0] for x in my_list)) if my_list else True  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_all_same(?))  # check_all_same() runs first, then print() outputs the result
check_all_same(my_list)  # arguments evaluated first, then function body runs
x == ?  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_same(my_list):  # stored in memory, not executed yet
print(check_all_same(...), check_all_same(...))  # check_all_same() runs with my_list=[1, 1, 1]
#   → return all((x == my_list[0] for x in my_list)) if my_list else True
def check_all_same(my_list):  # stored in memory, not executed yet
def check_all_same(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_same(...))  # check_all_same() runs with my_list=[4, 1, 8, 6, 3]
#   → return all((x == my_list[0] for x in my_list)) if my_list else True
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_same is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_same(...), check_all_same(...))  # check_all_same() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_same(...))  # check_all_same() runs first → value goes to print()

"""

"""
Problem 1827:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_different(my_list):  # defines a function with parameters my_list
    return len(my_list) == len(set(my_list))  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: len, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_all_different(?))  # check_all_different() runs first, then print() outputs the result
check_all_different(my_list)  # arguments evaluated first, then function body runs
len(...) == len(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_different(my_list):  # stored in memory, not executed yet
print(check_all_different(...), check_all_different(...))  # check_all_different() runs with my_list=[1, 2, 3]
#   → return len(my_list) == len(set(my_list))
def check_all_different(my_list):  # stored in memory, not executed yet
def check_all_different(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_different(...))  # check_all_different() runs with my_list=[4, 1, 8, 6, 3]
#   → return len(my_list) == len(set(my_list))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_different is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_different(...), check_all_different(...))  # check_all_different() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_different(...))  # check_all_different() runs first → value goes to print()

"""

"""
Problem 1828:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_range_list(start, end):  # defines a function with parameters start, end
    return list(range(start, end + 1))  # return sends the result back to the caller
#
# Operators: +
# Built-in functions: list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_range_list(1, 5)  # arguments evaluated first, then function body runs
get_range_list(start, end)  # arguments evaluated first, then function body runs
print(get_range_list(3, 20))  # get_range_list() runs first, then print() outputs the result
end + 1  # end → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_range_list(start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_range_list(start, end):  # stored in memory, not executed yet
def get_range_list(start, end):  # stored in memory, not executed yet
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_range_list(...))  # get_range_list() runs with start=3, end=20
#   → return list(range(start, end + 1))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_range_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_range_list(...))  # get_range_list() runs first → value goes to print()

"""

"""
Problem 1829:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_even_range(start, end):  # defines a function with parameters start, end
    return [x for x in range(start, end + 1) if x % 2 == 0]  # return sends the result back to the caller
#
# Operators: % + ==
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_even_range(1, 10)  # arguments evaluated first, then function body runs
get_even_range(start, end)  # arguments evaluated first, then function body runs
print(get_even_range(3, 20))  # get_even_range() runs first, then print() outputs the result
? == 0  # evaluated as True or False → picks which branch
end + 1  # end → then 1 → then +
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_even_range(start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_even_range(start, end):  # stored in memory, not executed yet
def get_even_range(start, end):  # stored in memory, not executed yet
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_even_range(...))  # get_even_range() runs with start=3, end=20
#   → return [x for x in range(start, end + 1) if x % 2 == 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_range(...))  # get_even_range() runs first → value goes to print()

"""

"""
Problem 1830:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_odd_range(start, end):  # defines a function with parameters start, end
    return [x for x in range(start, end + 1) if x % 2 != 0]  # return sends the result back to the caller
#
# Operators: % + !=
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_odd_range(1, 10)  # arguments evaluated first, then function body runs
get_odd_range(start, end)  # arguments evaluated first, then function body runs
print(get_odd_range(3, 20))  # get_odd_range() runs first, then print() outputs the result
? != 0  # evaluated as True or False → picks which branch
end + 1  # end → then 1 → then +
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_odd_range(start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_odd_range(start, end):  # stored in memory, not executed yet
def get_odd_range(start, end):  # stored in memory, not executed yet
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_odd_range(...))  # get_odd_range() runs with start=3, end=20
#   → return [x for x in range(start, end + 1) if x % 2 != 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_odd_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_odd_range(...))  # get_odd_range() runs first → value goes to print()

"""

"""
Problem 1831:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def multiply_lists(list1, list2):  # defines a function with parameters list1, list2
    return [a * b for a, b in zip(list1, list2)]  # return sends the result back to the caller
#
# Operators: *
# Built-in functions: zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
multiply_lists(?, ?)  # arguments evaluated first, then function body runs
multiply_lists(list1, list2)  # arguments evaluated first, then function body runs
a * b  # a → then b → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def multiply_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def multiply_lists(list1, list2):  # stored in memory, not executed yet
def multiply_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(multiply_lists(...))  # multiply_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return [a * b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply_lists(...))  # multiply_lists() runs first → value goes to print()

"""

"""
Problem 1832:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def add_lists(list1, list2):  # defines a function with parameters list1, list2
    return [a + b for a, b in zip(list1, list2)]  # return sends the result back to the caller
#
# Operators: +
# Built-in functions: zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
add_lists(?, ?)  # arguments evaluated first, then function body runs
add_lists(list1, list2)  # arguments evaluated first, then function body runs
a + b  # a → then b → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def add_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def add_lists(list1, list2):  # stored in memory, not executed yet
def add_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(add_lists(...))  # add_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return [a + b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_lists(...))  # add_lists() runs first → value goes to print()

"""

"""
Problem 1833:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def subtract_lists(list1, list2):  # defines a function with parameters list1, list2
    return [a - b for a, b in zip(list1, list2)]  # return sends the result back to the caller
#
# Operators: -
# Built-in functions: zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
subtract_lists(?, ?)  # arguments evaluated first, then function body runs
subtract_lists(list1, list2)  # arguments evaluated first, then function body runs
a - b  # a → then b → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def subtract_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def subtract_lists(list1, list2):  # stored in memory, not executed yet
def subtract_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(subtract_lists(...))  # subtract_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return [a - b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only subtract_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(subtract_lists(...))  # subtract_lists() runs first → value goes to print()

"""

"""
Problem 1834:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_index_of_max(numbers):  # defines a function with parameters numbers
    return numbers.index(max(numbers))  # return sends the result back to the caller
#
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_index_of_max(?)  # arguments evaluated first, then function body runs
get_index_of_max(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_index_of_max(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_index_of_max(numbers):  # stored in memory, not executed yet
def get_index_of_max(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_index_of_max(...))  # get_index_of_max() runs with numbers=[4, 1, 8, 6, 3]
#   → return numbers.index(max(numbers))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_index_of_max is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_index_of_max(...))  # get_index_of_max() runs first → value goes to print()

"""

"""
Problem 1835:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_index_of_min(numbers):  # defines a function with parameters numbers
    return numbers.index(min(numbers))  # return sends the result back to the caller
#
# Built-in functions: min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_index_of_min(?)  # arguments evaluated first, then function body runs
get_index_of_min(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_index_of_min(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_index_of_min(numbers):  # stored in memory, not executed yet
def get_index_of_min(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_index_of_min(...))  # get_index_of_min() runs with numbers=[4, 1, 8, 6, 3]
#   → return numbers.index(min(numbers))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_index_of_min is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_index_of_min(...))  # get_index_of_min() runs first → value goes to print()

"""

"""
Problem 1836:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def rotate_list_left(my_list, n):  # defines a function with parameters my_list, n
    n = n % len(my_list)
    return my_list[n:] + my_list[:n]  # return sends the result back to the caller
#
# Operators: % +
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
rotate_list_left(?, 2)  # arguments evaluated first, then function body runs
rotate_list_left(my_list, n)  # arguments evaluated first, then function body runs
print(rotate_list_left(?, 15))  # rotate_list_left() runs first, then print() outputs the result
n % len(...)  # n → then len(...) → then %
? + ?  # ? → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def rotate_list_left(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def rotate_list_left(my_list, n):  # stored in memory, not executed yet
def rotate_list_left(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(rotate_list_left(...))  # rotate_list_left() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return my_list[n:] + my_list[:n]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only rotate_list_left is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(rotate_list_left(...))  # rotate_list_left() runs first → value goes to print()

"""

"""
Problem 1837:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def rotate_list_right(my_list, n):  # defines a function with parameters my_list, n
    n = n % len(my_list)
    return my_list[-n:] + my_list[:-n]  # return sends the result back to the caller
#
# Operators: % +
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
rotate_list_right(?, 2)  # arguments evaluated first, then function body runs
rotate_list_right(my_list, n)  # arguments evaluated first, then function body runs
print(rotate_list_right(?, 15))  # rotate_list_right() runs first, then print() outputs the result
n % len(...)  # n → then len(...) → then %
? + ?  # ? → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def rotate_list_right(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def rotate_list_right(my_list, n):  # stored in memory, not executed yet
def rotate_list_right(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(rotate_list_right(...))  # rotate_list_right() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return my_list[-n:] + my_list[:-n]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only rotate_list_right is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(rotate_list_right(...))  # rotate_list_right() runs first → value goes to print()

"""

"""
Problem 1838:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def chunk_list(my_list, n):  # defines a function with parameters my_list, n
    return [my_list[i:i + n] for i in range(0, len(my_list), n)]  # return sends the result back to the caller
#
# Operators: +
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
chunk_list(?, 3)  # arguments evaluated first, then function body runs
chunk_list(my_list, n)  # arguments evaluated first, then function body runs
print(chunk_list(?, 15))  # chunk_list() runs first, then print() outputs the result
i + n  # i → then n → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def chunk_list(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def chunk_list(my_list, n):  # stored in memory, not executed yet
def chunk_list(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(chunk_list(...))  # chunk_list() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return [my_list[i:i + n] for i in range(0, len(my_list), n)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only chunk_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(chunk_list(...))  # chunk_list() runs first → value goes to print()

"""

"""
Problem 1839:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def interleave_lists(list1, list2):  # defines a function with parameters list1, list2
    result = []
    max_len = max(len(list1), len(list2))
    for i in range(max_len):
        if i < len(list1):
            result.append(list1[i])
        if i < len(list2):
            result.append(list2[i])
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: len, max, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
interleave_lists(?, ?)  # arguments evaluated first, then function body runs
interleave_lists(list1, list2)  # arguments evaluated first, then function body runs
i < len(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def interleave_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def interleave_lists(list1, list2):  # stored in memory, not executed yet
def interleave_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(interleave_lists(...))  # interleave_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only interleave_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(interleave_lists(...))  # interleave_lists() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1840:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def zip_lists(list1, list2):  # defines a function with parameters list1, list2
    return list(zip(list1, list2))  # return sends the result back to the caller
#
# Built-in functions: list, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
zip_lists(?, ?)  # arguments evaluated first, then function body runs
zip_lists(list1, list2)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def zip_lists(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def zip_lists(list1, list2):  # stored in memory, not executed yet
def zip_lists(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(zip_lists(...))  # zip_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return list(zip(list1, list2))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only zip_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(zip_lists(...))  # zip_lists() runs first → value goes to print()

"""

"""
Problem 1841:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def unzip_list(tuples_list):  # defines a function with parameters tuples_list
    if not tuples_list:
        return ([], [])
    first_elements = [t[0] for t in tuples_list]
    second_elements = [t[1] for t in tuples_list]
    return (first_elements, second_elements)  # return sends the result back to the caller
#
# Keywords: if/else
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
unzip_list(?)  # arguments evaluated first, then function body runs
unzip_list(tuples_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def unzip_list(tuples_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def unzip_list(tuples_list):  # stored in memory, not executed yet
def unzip_list(tuples_list):  # stored in memory, not executed yet
tuples_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(unzip_list(...))  # unzip_list() runs with tuples_list=[4, 1, 8, 6, 3]
#   → return ([], [])
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only unzip_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → tuples_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(unzip_list(...))  # unzip_list() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1842:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_uppercase_letters(text):  # defines a function with parameters text
    return sum((1 for char in text if char.isupper()))  # return sends the result back to the caller
#
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_uppercase_letters('Hello World')  # arguments evaluated first, then function body runs
count_uppercase_letters(text)  # arguments evaluated first, then function body runs
print(count_uppercase_letters('world'))  # count_uppercase_letters() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_uppercase_letters(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_uppercase_letters(text):  # stored in memory, not executed yet
def count_uppercase_letters(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_uppercase_letters(...))  # count_uppercase_letters() runs with text='world'
#   → return sum((1 for char in text if char.isupper()))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_uppercase_letters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_uppercase_letters(...))  # count_uppercase_letters() runs first → value goes to print()

"""

"""
Problem 1843:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_lowercase_letters(text):  # defines a function with parameters text
    return sum((1 for char in text if char.islower()))  # return sends the result back to the caller
#
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_lowercase_letters('Hello World')  # arguments evaluated first, then function body runs
count_lowercase_letters(text)  # arguments evaluated first, then function body runs
print(count_lowercase_letters('world'))  # count_lowercase_letters() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_lowercase_letters(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_lowercase_letters(text):  # stored in memory, not executed yet
def count_lowercase_letters(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_lowercase_letters(...))  # count_lowercase_letters() runs with text='world'
#   → return sum((1 for char in text if char.islower()))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_lowercase_letters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_lowercase_letters(...))  # count_lowercase_letters() runs first → value goes to print()

"""

"""
Problem 1844:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def swap_uppercase_lowercase(text):  # defines a function with parameters text
    return text.swapcase()  # return sends the result back to the caller
#
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
swap_uppercase_lowercase('Hello World')  # arguments evaluated first, then function body runs
swap_uppercase_lowercase(text)  # arguments evaluated first, then function body runs
print(swap_uppercase_lowercase('world'))  # swap_uppercase_lowercase() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def swap_uppercase_lowercase(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def swap_uppercase_lowercase(text):  # stored in memory, not executed yet
def swap_uppercase_lowercase(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(swap_uppercase_lowercase(...))  # swap_uppercase_lowercase() runs with text='world'
#   → return str(text).swapcase()
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_uppercase_lowercase is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_uppercase_lowercase(...))  # swap_uppercase_lowercase() runs first → value goes to print()

"""

"""
Problem 1845:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_numbers_from_string(text):  # defines a function with parameters text
    return ''.join((char for char in text if not char.isdigit()))  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_numbers_from_string('Hello123World456')  # arguments evaluated first, then function body runs
remove_numbers_from_string(text)  # arguments evaluated first, then function body runs
print(remove_numbers_from_string('world'))  # remove_numbers_from_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_numbers_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_numbers_from_string(text):  # stored in memory, not executed yet
def remove_numbers_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_numbers_from_string(...))  # remove_numbers_from_string() runs with text='world'
#   → return ''.join((char for char in text if not char.isdigit()))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_numbers_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_numbers_from_string(...))  # remove_numbers_from_string() runs first → value goes to print()

"""

"""
Problem 1846:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_letters_from_string(text):  # defines a function with parameters text
    return ''.join((char for char in text if not char.isalpha()))  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_letters_from_string('Hello123World456!')  # arguments evaluated first, then function body runs
remove_letters_from_string(text)  # arguments evaluated first, then function body runs
print(remove_letters_from_string('world'))  # remove_letters_from_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_letters_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_letters_from_string(text):  # stored in memory, not executed yet
def remove_letters_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_letters_from_string(...))  # remove_letters_from_string() runs with text='world'
#   → return ''.join((char for char in text if not char.isalpha()))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_letters_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_letters_from_string(...))  # remove_letters_from_string() runs first → value goes to print()

"""

"""
Problem 1847:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def extract_numbers_from_string(text):  # defines a function with parameters text
    return [char for char in text if char.isdigit()]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
extract_numbers_from_string('Hello123World456')  # arguments evaluated first, then function body runs
extract_numbers_from_string(text)  # arguments evaluated first, then function body runs
print(extract_numbers_from_string('world'))  # extract_numbers_from_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def extract_numbers_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def extract_numbers_from_string(text):  # stored in memory, not executed yet
def extract_numbers_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(extract_numbers_from_string(...))  # extract_numbers_from_string() runs with text='world'
#   → return [char for char in text if char.isdigit()]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only extract_numbers_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(extract_numbers_from_string(...))  # extract_numbers_from_string() runs first → value goes to print()

"""

"""
Problem 1848:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def extract_letters_from_string(text):  # defines a function with parameters text
    return [char for char in text if char.isalpha()]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
extract_letters_from_string('Hello123World456')  # arguments evaluated first, then function body runs
extract_letters_from_string(text)  # arguments evaluated first, then function body runs
print(extract_letters_from_string('world'))  # extract_letters_from_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def extract_letters_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def extract_letters_from_string(text):  # stored in memory, not executed yet
def extract_letters_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(extract_letters_from_string(...))  # extract_letters_from_string() runs with text='world'
#   → return [char for char in text if char.isalpha()]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only extract_letters_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(extract_letters_from_string(...))  # extract_letters_from_string() runs first → value goes to print()

"""

"""
Problem 1849:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_phone_number(digits):  # defines a function with parameters digits
    return f'({digits[0:3]}) {digits[3:6]}-{digits[6:10]}'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_phone_number('1234567890')  # arguments evaluated first, then function body runs
format_phone_number(digits)  # arguments evaluated first, then function body runs
print(format_phone_number(?))  # format_phone_number() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_phone_number(digits):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_phone_number(digits):  # stored in memory, not executed yet
def format_phone_number(digits):  # stored in memory, not executed yet
digits = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_phone_number(...))  # format_phone_number() runs with digits=[4, 1, 8, 6, 3]
#   → return f'({digits[0:3]}) {digits[3:6]}-{digits[6:10]}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_phone_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → digits = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_phone_number(...))  # format_phone_number() runs first → value goes to print()

"""

"""
Problem 1850:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_volume_box(length, width, height):  # defines a function with parameters length, width, height
    return length * width * height  # return sends the result back to the caller
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_volume_box(5, 3, 4)  # arguments evaluated first, then function body runs
calculate_volume_box(length, width, height)  # arguments evaluated first, then function body runs
print(calculate_volume_box(25, 25, 20))  # calculate_volume_box() runs first, then print() outputs the result
? * height  # ? → then height → then *
length * width  # length → then width → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_volume_box(length, width, height):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_volume_box(length, width, height):  # stored in memory, not executed yet
def calculate_volume_box(length, width, height):  # stored in memory, not executed yet
length = ...  # assigns a value to the variable
width = ...  # assigns a value to the variable
height = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_volume_box(...))  # calculate_volume_box() runs with length=25, width=25, height=20
#   → return length * width * height
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_volume_box is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → height = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_volume_box(...))  # calculate_volume_box() runs first → value goes to print()

"""

"""
Problem 1851:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_triangle_valid(a, b, c):  # defines a function with parameters a, b, c
    return a + b > c and a + c > b and (b + c > a)  # return sends the result back to the caller
#
# Operators: + > and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_triangle_valid(3, 4, 5))  # check_triangle_valid() runs first, then print() outputs the result
print(check_triangle_valid(1, 2, 5))  # check_triangle_valid() runs first, then print() outputs the result
check_triangle_valid(a, b, c)  # arguments evaluated first, then function body runs
print(check_triangle_valid(20, 30, 10))  # check_triangle_valid() runs first, then print() outputs the result
? > c  # evaluated as True or False → picks which branch
? > b  # evaluated as True or False → picks which branch
? > a  # evaluated as True or False → picks which branch
a + b  # a → then b → then +
a + c  # a → then c → then +
b + c  # b → then c → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_triangle_valid(a, b, c):  # stored in memory, not executed yet
print(check_triangle_valid(...), check_triangle_valid(...))  # check_triangle_valid() runs with a=3, b=4, c=5
#   → return a + b > c and a + c > b and (b + c > a)
def check_triangle_valid(a, b, c):  # stored in memory, not executed yet
def check_triangle_valid(a, b, c):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
c = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_triangle_valid(...))  # check_triangle_valid() runs with a=20, b=30, c=10
#   → return a + b > c and a + c > b and (b + c > a)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_triangle_valid is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_triangle_valid(...), check_triangle_valid(...))  # check_triangle_valid() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_triangle_valid(...))  # check_triangle_valid() runs first → value goes to print()

"""

"""
Problem 1852:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_grade(score):  # defines a function with parameters score
    if score >= 90:
        return 'A'
    elif score >= 80:
        return 'B'
    elif score >= 70:
        return 'C'
    elif score >= 60:
        return 'D'
    else:
        return 'F'
#
# Keywords: if/else
# Operators: >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(find_grade(95))  # find_grade() runs first, then print() outputs the result
print(find_grade(75))  # find_grade() runs first, then print() outputs the result
print(find_grade(55))  # find_grade() runs first, then print() outputs the result
find_grade(score)  # arguments evaluated first, then function body runs
print(find_grade(25))  # find_grade() runs first, then print() outputs the result
score >= 90  # evaluated as True or False → picks which branch
score >= 80  # evaluated as True or False → picks which branch
score >= 70  # evaluated as True or False → picks which branch
score >= 60  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_grade(score):  # stored in memory, not executed yet
print(find_grade(...), find_grade(...), find_grade(...))  # find_grade() runs with score=95
#   → return 'A'
def find_grade(score):  # stored in memory, not executed yet
def find_grade(score):  # stored in memory, not executed yet
score = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_grade(...))  # find_grade() runs with score=25
#   → return 'A'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_grade is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(find_grade(...), find_grade(...), find_grade(...))  # find_grade() runs first → value goes to print()
#   → score = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_grade(...))  # find_grade() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1853:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_season(month):  # defines a function with parameters month
    if month in [12, 1, 2]:
        return 'Winter'
    elif month in [3, 4, 5]:
        return 'Spring'
    elif month in [6, 7, 8]:
        return 'Summer'
    else:
        return 'Fall'
#
# Keywords: if/else
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(get_season(1))  # get_season() runs first, then print() outputs the result
print(get_season(4))  # get_season() runs first, then print() outputs the result
print(get_season(7))  # get_season() runs first, then print() outputs the result
print(get_season(10))  # get_season() runs first, then print() outputs the result
get_season(month)  # arguments evaluated first, then function body runs
print(get_season(25))  # get_season() runs first, then print() outputs the result
month in [12, 1, 2]  # evaluated as True or False → picks which branch
month in [3, 4, 5]  # evaluated as True or False → picks which branch
month in [6, 7, 8]  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_season(month):  # stored in memory, not executed yet
print(get_season(...), get_season(...), get_season(...), get_season(...))  # get_season() runs with month=1
#   → return 'Winter'
def get_season(month):  # stored in memory, not executed yet
def get_season(month):  # stored in memory, not executed yet
month = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_season(...))  # get_season() runs with month=25
#   → return 'Winter'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_season is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_season(...), get_season(...), get_season(...), get_season(...))  # get_season() runs first → value goes to print()
#   → month = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_season(...))  # get_season() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1854:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_discount(price, discount_percent):  # defines a function with parameters price, discount_percent
    return price * (1 - discount_percent / 100)  # return sends the result back to the caller
#
# Operators: * - /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_discount(100, 20)  # arguments evaluated first, then function body runs
calculate_discount(price, discount_percent)  # arguments evaluated first, then function body runs
print(calculate_discount(25, 25))  # calculate_discount() runs first, then print() outputs the result
price * ?  # price → then ? → then *
1 - ?  # 1 → then ? → then -
discount_percent / 100  # discount_percent → then 100 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_discount(price, discount_percent):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_discount(price, discount_percent):  # stored in memory, not executed yet
def calculate_discount(price, discount_percent):  # stored in memory, not executed yet
price = ...  # assigns a value to the variable
discount_percent = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_discount(...))  # calculate_discount() runs with price=25, discount_percent=25
#   → return price * (1 - discount_percent / 100)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_discount is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → price = ...  # stored in memory
#   → discount_percent = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_discount(...))  # calculate_discount() runs first → value goes to print()

"""

"""
Problem 1855:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_tip(bill_amount, tip_percent):  # defines a function with parameters bill_amount, tip_percent
    tip = bill_amount * (tip_percent / 100)
    total = bill_amount + tip
    return (tip, total)  # return sends the result back to the caller
#
# Operators: * + /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_tip(100, 15)  # arguments evaluated first, then function body runs
calculate_tip(bill_amount, tip_percent)  # arguments evaluated first, then function body runs
print(calculate_tip(25, 25))  # calculate_tip() runs first, then print() outputs the result
bill_amount * ?  # bill_amount → then ? → then *
bill_amount + tip  # bill_amount → then tip → then +
tip_percent / 100  # tip_percent → then 100 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_tip(bill_amount, tip_percent):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_tip(bill_amount, tip_percent):  # stored in memory, not executed yet
def calculate_tip(bill_amount, tip_percent):  # stored in memory, not executed yet
bill_amount = ...  # assigns a value to the variable
tip_percent = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_tip(...))  # calculate_tip() runs with bill_amount=25, tip_percent=25
#   → return (tip, total)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_tip is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → bill_amount = ...  # stored in memory
#   → tip_percent = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_tip(...))  # calculate_tip() runs first → value goes to print()

"""

"""
Problem 1856:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def swap_two_elements(my_list, index1, index2):  # defines a function with parameters my_list, index1, index2
    new_list = my_list.copy()
    new_list[index1], new_list[index2] = (new_list[index2], new_list[index1])
    return new_list  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
swap_two_elements(?, 0, 3)  # arguments evaluated first, then function body runs
swap_two_elements(my_list, index1, index2)  # arguments evaluated first, then function body runs
print(swap_two_elements(?, 25, 25))  # swap_two_elements() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def swap_two_elements(my_list, index1, index2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def swap_two_elements(my_list, index1, index2):  # stored in memory, not executed yet
def swap_two_elements(my_list, index1, index2):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
index1 = ...  # assigns a value to the variable
index2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(swap_two_elements(...))  # swap_two_elements() runs with my_list=[4, 1, 8, 6, 3], index1=25, index2=25
#   → return new_list
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_two_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → index1 = ...  # stored in memory
#   → index2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_two_elements(...))  # swap_two_elements() runs first → value goes to print()

"""

"""
Problem 1857:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def insert_at_index(my_list, index, value):  # defines a function with parameters my_list, index, value
    new_list = my_list.copy()
    new_list.insert(index, value)
    return new_list  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
insert_at_index(?, 2, 99)  # arguments evaluated first, then function body runs
insert_at_index(my_list, index, value)  # arguments evaluated first, then function body runs
print(insert_at_index(?, 4, 25))  # insert_at_index() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def insert_at_index(my_list, index, value):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def insert_at_index(my_list, index, value):  # stored in memory, not executed yet
def insert_at_index(my_list, index, value):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
value = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(insert_at_index(...))  # insert_at_index() runs with my_list=[4, 1, 8, 6, 3], index=4, value=25
#   → return new_list
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only insert_at_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(insert_at_index(...))  # insert_at_index() runs first → value goes to print()

"""

"""
Problem 1858:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_at_index(my_list, index):  # defines a function with parameters my_list, index
    new_list = my_list.copy()
    new_list.pop(index)
    return new_list  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_at_index(?, 1)  # arguments evaluated first, then function body runs
remove_at_index(my_list, index)  # arguments evaluated first, then function body runs
print(remove_at_index(?, 4))  # remove_at_index() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_at_index(my_list, index):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_at_index(my_list, index):  # stored in memory, not executed yet
def remove_at_index(my_list, index):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
index = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_at_index(...))  # remove_at_index() runs with my_list=[4, 1, 8, 6, 3], index=4
#   → return new_list
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_at_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_at_index(...))  # remove_at_index() runs first → value goes to print()

"""

"""
Problem 1860:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_min_max(numbers):  # defines a function with parameters numbers
    return (min(numbers), max(numbers))  # return sends the result back to the caller
#
# Built-in functions: max, min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_min_max(?)  # arguments evaluated first, then function body runs
get_min_max(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_min_max(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_min_max(numbers):  # stored in memory, not executed yet
def get_min_max(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_min_max(...))  # get_min_max() runs with numbers=[4, 1, 8, 6, 3]
#   → return (min(numbers), max(numbers))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_min_max is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_min_max(...))  # get_min_max() runs first → value goes to print()

"""

"""
Problem 1861:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_conditions(number):  # defines a function with parameters number
    return number > 0 and number % 2 == 0 and (number % 3 == 0)  # return sends the result back to the caller
#
# Operators: % > == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_all_conditions(6))  # check_all_conditions() runs first, then print() outputs the result
print(check_all_conditions(5))  # check_all_conditions() runs first, then print() outputs the result
check_all_conditions(number)  # arguments evaluated first, then function body runs
print(check_all_conditions(25))  # check_all_conditions() runs first, then print() outputs the result
number > 0  # evaluated as True or False → picks which branch
? == 0  # evaluated as True or False → picks which branch
number % 2  # number → then 2 → then %
number % 3  # number → then 3 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_conditions(number):  # stored in memory, not executed yet
print(check_all_conditions(...), check_all_conditions(...))  # check_all_conditions() runs with number=6
#   → return number > 0 and number % 2 == 0 and (number % 3 == 0)
def check_all_conditions(number):  # stored in memory, not executed yet
def check_all_conditions(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_conditions(...))  # check_all_conditions() runs with number=25
#   → return number > 0 and number % 2 == 0 and (number % 3 == 0)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_conditions(...), check_all_conditions(...))  # check_all_conditions() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_conditions(...))  # check_all_conditions() runs first → value goes to print()

"""

"""
Problem 1862:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_full_name(first, middle, last):  # defines a function with parameters first, middle, last
    return f'{first} {middle} {last}'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_full_name('John', 'Michael', 'Doe')  # arguments evaluated first, then function body runs
format_full_name(first, middle, last)  # arguments evaluated first, then function body runs
print(format_full_name(25, 25, 25))  # format_full_name() runs first, then print() outputs the result
'%s %s %s' % ?  # '%s %s %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_full_name(first, middle, last):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_full_name(first, middle, last):  # stored in memory, not executed yet
def format_full_name(first, middle, last):  # stored in memory, not executed yet
first = ...  # assigns a value to the variable
middle = ...  # assigns a value to the variable
last = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_full_name(...))  # format_full_name() runs with first=25, middle=25, last=25
#   → return '%s %s %s' % (first, middle, last)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_full_name is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → first = ...  # stored in memory
#   → middle = ...  # stored in memory
#   → last = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_full_name(...))  # format_full_name() runs first → value goes to print()

"""

"""
Problem 1863:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_bmi(weight_kg, height_m):  # defines a function with parameters weight_kg, height_m
    bmi = weight_kg / height_m ** 2
    return round(bmi, 1)  # return sends the result back to the caller
#
# Operators: / **
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_bmi(70, 1.75)  # arguments evaluated first, then function body runs
calculate_bmi(weight_kg, height_m)  # arguments evaluated first, then function body runs
print(calculate_bmi(25, 25))  # calculate_bmi() runs first, then print() outputs the result
weight_kg / ?  # weight_kg → then ? → then /
height_m ** 2  # height_m → then 2 → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_bmi(weight_kg, height_m):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_bmi(weight_kg, height_m):  # stored in memory, not executed yet
def calculate_bmi(weight_kg, height_m):  # stored in memory, not executed yet
weight_kg = ...  # assigns a value to the variable
height_m = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_bmi(...))  # calculate_bmi() runs with weight_kg=25, height_m=25
#   → return round(bmi, 1)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_bmi is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → weight_kg = ...  # stored in memory
#   → height_m = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_bmi(...))  # calculate_bmi() runs first → value goes to print()

"""

"""
Problem 1864:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_bmi_category(weight_kg, height_m):  # defines a function with parameters weight_kg, height_m
    bmi = weight_kg / height_m ** 2
    if bmi < 18.5:
        return 'Underweight'
    elif bmi < 25:
        return 'Normal'
    elif bmi < 30:
        return 'Overweight'
    else:
        return 'Obese'
#
# Keywords: if/else
# Operators: / < **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
check_bmi_category(70, 1.75)  # arguments evaluated first, then function body runs
check_bmi_category(weight_kg, height_m)  # arguments evaluated first, then function body runs
print(check_bmi_category(25, 25))  # check_bmi_category() runs first, then print() outputs the result
weight_kg / ?  # weight_kg → then ? → then /
bmi < 18.5  # evaluated as True or False → picks which branch
height_m ** 2  # height_m → then 2 → then **
bmi < 25  # evaluated as True or False → picks which branch
bmi < 30  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_bmi_category(weight_kg, height_m):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def check_bmi_category(weight_kg, height_m):  # stored in memory, not executed yet
def check_bmi_category(weight_kg, height_m):  # stored in memory, not executed yet
weight_kg = ...  # assigns a value to the variable
height_m = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_bmi_category(...))  # check_bmi_category() runs with weight_kg=25, height_m=25
#   → return 'Underweight'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_bmi_category is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → weight_kg = ...  # stored in memory
#   → height_m = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_bmi_category(...))  # check_bmi_category() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1865:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_common_elements_three(list1, list2, list3):  # defines a function with parameters list1, list2, list3
    return [x for x in list1 if x in list2 and x in list3]  # return sends the result back to the caller
#
# Operators: in and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
find_common_elements_three(?, ?, ?)  # arguments evaluated first, then function body runs
find_common_elements_three(list1, list2, list3)  # arguments evaluated first, then function body runs
x in list2  # evaluated as True or False → picks which branch
x in list3  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_common_elements_three(list1, list2, list3):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def find_common_elements_three(list1, list2, list3):  # stored in memory, not executed yet
def find_common_elements_three(list1, list2, list3):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
list3 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_common_elements_three(...))  # find_common_elements_three() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3], list3=[4, 1, 8, 6, 3]
#   → return [x for x in list1 if x in list2 and x in list3]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_common_elements_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_common_elements_three(...))  # find_common_elements_three() runs first → value goes to print()

"""

"""
Problem 1866:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def merge_three_lists(list1, list2, list3):  # defines a function with parameters list1, list2, list3
    return list1 + list2 + list3  # return sends the result back to the caller
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
merge_three_lists(?, ?, ?)  # arguments evaluated first, then function body runs
merge_three_lists(list1, list2, list3)  # arguments evaluated first, then function body runs
print(merge_three_lists(25, 25, ?))  # merge_three_lists() runs first, then print() outputs the result
? + list3  # ? → then list3 → then +
list1 + list2  # list1 → then list2 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def merge_three_lists(list1, list2, list3):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def merge_three_lists(list1, list2, list3):  # stored in memory, not executed yet
def merge_three_lists(list1, list2, list3):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
list3 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(merge_three_lists(...))  # merge_three_lists() runs with list1=25, list2=25, list3=[4, 1, 8, 6, 3]
#   → return list1 + list2 + list3
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_three_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(merge_three_lists(...))  # merge_three_lists() runs first → value goes to print()

"""

"""
Problem 1867:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_at_indices(my_list, indices):  # defines a function with parameters my_list, indices
    return [my_list[i] for i in indices]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_at_indices(?, ?)  # arguments evaluated first, then function body runs
get_elements_at_indices(my_list, indices)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_at_indices(my_list, indices):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_at_indices(my_list, indices):  # stored in memory, not executed yet
def get_elements_at_indices(my_list, indices):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
indices = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_at_indices(...))  # get_elements_at_indices() runs with my_list=[4, 1, 8, 6, 3], indices=[4, 1, 8, 6, 3]
#   → return [my_list[i] for i in indices]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_at_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → indices = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_at_indices(...))  # get_elements_at_indices() runs first → value goes to print()

"""

"""
Problem 1868:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def replace_multiple_chars(text, replacements):  # defines a function with parameters text, replacements
    result = text
    for old_char, new_char in replacements.items():
        result = result.replace(old_char, new_char)
    return result  # return sends the result back to the caller
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
replace_multiple_chars('Hello World', ?)  # arguments evaluated first, then function body runs
replace_multiple_chars(text, replacements)  # arguments evaluated first, then function body runs
print(replace_multiple_chars('world', 25))  # replace_multiple_chars() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def replace_multiple_chars(text, replacements):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def replace_multiple_chars(text, replacements):  # stored in memory, not executed yet
def replace_multiple_chars(text, replacements):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
replacements = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(replace_multiple_chars(...))  # replace_multiple_chars() runs with text='world', replacements=25
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only replace_multiple_chars is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → replacements = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(replace_multiple_chars(...))  # replace_multiple_chars() runs first → value goes to print()
#
# Loop: takes one item from replacements.items(...), runs body, repeats until done

"""

"""
Problem 1869:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_words_by_length(text):  # defines a function with parameters text
    words = text.split()
    counts = {}
    for word in words:
        length = len(word)
        counts[length] = counts.get(length, 0) + 1
    return counts  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_words_by_length('hello world python programming')  # arguments evaluated first, then function body runs
count_words_by_length(text)  # arguments evaluated first, then function body runs
print(count_words_by_length('world'))  # count_words_by_length() runs first, then print() outputs the result
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_words_by_length(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_words_by_length(text):  # stored in memory, not executed yet
def count_words_by_length(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_words_by_length(...))  # count_words_by_length() runs with text='world'
#   → return counts
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words_by_length(...))  # count_words_by_length() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done

"""

"""
Problem 1870:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def filter_and_transform(numbers):  # defines a function with parameters numbers
    return [x * 2 for x in numbers if x % 2 == 0]  # return sends the result back to the caller
#
# Operators: % * ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
filter_and_transform(?)  # arguments evaluated first, then function body runs
filter_and_transform(numbers)  # arguments evaluated first, then function body runs
x * 2  # x → then 2 → then *
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def filter_and_transform(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def filter_and_transform(numbers):  # stored in memory, not executed yet
def filter_and_transform(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(filter_and_transform(...))  # filter_and_transform() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x * 2 for x in numbers if x % 2 == 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_and_transform is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(filter_and_transform(...))  # filter_and_transform() runs first → value goes to print()

"""

"""
Problem 1871:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_starting_with(strings, prefix):  # defines a function with parameters strings, prefix
    return [s for s in strings if s.startswith(prefix)]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_starting_with(?, 'ap')  # arguments evaluated first, then function body runs
get_strings_starting_with(strings, prefix)  # arguments evaluated first, then function body runs
print(get_strings_starting_with(?, 'Hi, '))  # get_strings_starting_with() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_starting_with(strings, prefix):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_starting_with(strings, prefix):  # stored in memory, not executed yet
def get_strings_starting_with(strings, prefix):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
prefix = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_starting_with(...))  # get_strings_starting_with() runs with strings=[4, 1, 8, 6, 3], prefix='Hi, '
#   → return [s for s in strings if s.startswith(prefix)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_starting_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → prefix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_starting_with(...))  # get_strings_starting_with() runs first → value goes to print()

"""

"""
Problem 1872:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_ending_with(strings, suffix):  # defines a function with parameters strings, suffix
    return [s for s in strings if s.endswith(suffix)]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_ending_with(?, 'ing')  # arguments evaluated first, then function body runs
get_strings_ending_with(strings, suffix)  # arguments evaluated first, then function body runs
print(get_strings_ending_with(?, '?'))  # get_strings_ending_with() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_ending_with(strings, suffix):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_ending_with(strings, suffix):  # stored in memory, not executed yet
def get_strings_ending_with(strings, suffix):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
suffix = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_ending_with(...))  # get_strings_ending_with() runs with strings=[4, 1, 8, 6, 3], suffix='?'
#   → return [s for s in strings if s.endswith(suffix)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_ending_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → suffix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_ending_with(...))  # get_strings_ending_with() runs first → value goes to print()

"""

"""
Problem 1873:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_total_cost(prices, tax_rate):  # defines a function with parameters prices, tax_rate
    subtotal = sum(prices)
    tax = subtotal * (tax_rate / 100)
    return subtotal + tax  # return sends the result back to the caller
#
# Operators: * + /
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_total_cost(?, 10)  # arguments evaluated first, then function body runs
calculate_total_cost(prices, tax_rate)  # arguments evaluated first, then function body runs
print(calculate_total_cost(?, 25))  # calculate_total_cost() runs first, then print() outputs the result
subtotal * ?  # subtotal → then ? → then *
subtotal + tax  # subtotal → then tax → then +
tax_rate / 100  # tax_rate → then 100 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_total_cost(prices, tax_rate):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_total_cost(prices, tax_rate):  # stored in memory, not executed yet
def calculate_total_cost(prices, tax_rate):  # stored in memory, not executed yet
prices = ...  # assigns a value to the variable
tax_rate = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_total_cost(...))  # calculate_total_cost() runs with prices=[4, 1, 8, 6, 3], tax_rate=25
#   → import operator
#   → return operator.add(subtotal, tax)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_total_cost is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → prices = ...  # stored in memory
#   → tax_rate = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_total_cost(...))  # calculate_total_cost() runs first → value goes to print()

"""

"""
Problem 1874:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_all_indices(my_list, element):  # defines a function with parameters my_list, element
    return [i for i, x in enumerate(my_list) if x == element]  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: enumerate()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
find_all_indices(?, 10)  # arguments evaluated first, then function body runs
find_all_indices(my_list, element)  # arguments evaluated first, then function body runs
print(find_all_indices(?, 25))  # find_all_indices() runs first, then print() outputs the result
x == element  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_all_indices(my_list, element):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def find_all_indices(my_list, element):  # stored in memory, not executed yet
def find_all_indices(my_list, element):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_all_indices(...))  # find_all_indices() runs with my_list=[4, 1, 8, 6, 3], element=25
#   → return [i for i, x in enumerate(my_list) if x == element]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_all_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_all_indices(...))  # find_all_indices() runs first → value goes to print()

"""

"""
Problem 1875:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def split_into_chunks(my_list, chunk_size):  # defines a function with parameters my_list, chunk_size
    return [my_list[i:i + chunk_size] for i in range(0, len(my_list), chunk_size)]  # return sends the result back to the caller
#
# Operators: +
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
split_into_chunks(?, 3)  # arguments evaluated first, then function body runs
split_into_chunks(my_list, chunk_size)  # arguments evaluated first, then function body runs
print(split_into_chunks(?, 25))  # split_into_chunks() runs first, then print() outputs the result
i + chunk_size  # i → then chunk_size → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def split_into_chunks(my_list, chunk_size):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def split_into_chunks(my_list, chunk_size):  # stored in memory, not executed yet
def split_into_chunks(my_list, chunk_size):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
chunk_size = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(split_into_chunks(...))  # split_into_chunks() runs with my_list=[4, 1, 8, 6, 3], chunk_size=25
#   → return [my_list[i:i + chunk_size] for i in range(0, len(my_list), chunk_size)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only split_into_chunks is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → chunk_size = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(split_into_chunks(...))  # split_into_chunks() runs first → value goes to print()

"""

"""
Problem 1876:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_multiple_elements(my_list, elements_to_remove):  # defines a function with parameters my_list, elements_to_remove
    return [x for x in my_list if x not in elements_to_remove]  # return sends the result back to the caller
#
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_multiple_elements(?, ?)  # arguments evaluated first, then function body runs
remove_multiple_elements(my_list, elements_to_remove)  # arguments evaluated first, then function body runs
x not in elements_to_remove  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_multiple_elements(my_list, elements_to_remove):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_multiple_elements(my_list, elements_to_remove):  # stored in memory, not executed yet
def remove_multiple_elements(my_list, elements_to_remove):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
elements_to_remove = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_multiple_elements(...))  # remove_multiple_elements() runs with my_list=[4, 1, 8, 6, 3], elements_to_remove=[4, 1, 8, 6, 3]
#   → return [x for x in my_list if x not in elements_to_remove]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_multiple_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → elements_to_remove = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_multiple_elements(...))  # remove_multiple_elements() runs first → value goes to print()

"""

"""
Problem 1877:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_occurrences_multiple(my_list, elements):  # defines a function with parameters my_list, elements
    return {elem: my_list.count(elem) for elem in elements}  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_occurrences_multiple(?, ?)  # arguments evaluated first, then function body runs
count_occurrences_multiple(my_list, elements)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_occurrences_multiple(my_list, elements):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_occurrences_multiple(my_list, elements):  # stored in memory, not executed yet
def count_occurrences_multiple(my_list, elements):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
elements = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_occurrences_multiple(...))  # count_occurrences_multiple() runs with my_list=[4, 1, 8, 6, 3], elements=[5, 3, 8, 4]
#   → return {elem: my_list.count(elem) for elem in elements}
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_occurrences_multiple is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → elements = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_occurrences_multiple(...))  # count_occurrences_multiple() runs first → value goes to print()

"""

"""
Problem 1878:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_unique_elements_three(list1, list2, list3):  # defines a function with parameters list1, list2, list3
    all_elements = list1 + list2 + list3
    result = []
    for elem in all_elements:
        count = list1.count(elem) + list2.count(elem) + list3.count(elem)
        if count == 1:
            result.append(elem)
    return list(set(result))  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: list, map, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_unique_elements_three(?, ?, ?)  # arguments evaluated first, then function body runs
get_unique_elements_three(list1, list2, list3)  # arguments evaluated first, then function body runs
print(get_unique_elements_three(25, 25, ?))  # get_unique_elements_three() runs first, then print() outputs the result
? + list3  # ? → then list3 → then +
list1 + list2  # list1 → then list2 → then +
? + list3.count(...)  # ? → then list3.count(...) → then +
count == 1  # evaluated as True or False → picks which branch
list1.count(...) + list2.count(...)  # list1.count(...) → then list2.count(...) → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_unique_elements_three(list1, list2, list3):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(sorted(...))
def get_unique_elements_three(list1, list2, list3):  # stored in memory, not executed yet
def get_unique_elements_three(list1, list2, list3):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
list3 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_unique_elements_three(...))  # get_unique_elements_three() runs with list1=25, list2=25, list3=[4, 1, 8, 6, 3]
#   → return list(map(lambda elem: elem, all_elements))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_elements_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_elements_three(...))  # get_unique_elements_three() runs first → value goes to print()
#
# Loop: takes one item from all_elements, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1879:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def sort_by_multiple_criteria(people):  # defines a function with parameters people
    return sorted(people, key=lambda x: (x[1], x[2]))  # return sends the result back to the caller
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
sort_by_multiple_criteria(?)  # arguments evaluated first, then function body runs
sort_by_multiple_criteria(people)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def sort_by_multiple_criteria(people):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def sort_by_multiple_criteria(people):  # stored in memory, not executed yet
def sort_by_multiple_criteria(people):  # stored in memory, not executed yet
people = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(sort_by_multiple_criteria(...))  # sort_by_multiple_criteria() runs with people=[4, 1, 8, 6, 3]
#   → return sorted(people, key=lambda x: (x[1], x[2]))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_by_multiple_criteria is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → people = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_by_multiple_criteria(...))  # sort_by_multiple_criteria() runs first → value goes to print()

"""

"""
Problem 1880:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_statistics(numbers):  # defines a function with parameters numbers
    return {'sum': sum(numbers), 'average': sum(numbers) / len(numbers), 'min': min(numbers), 'max': max(numbers)}  # return sends the result back to the caller
#
# Operators: /
# Built-in functions: len, max, min, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_statistics(?)  # arguments evaluated first, then function body runs
calculate_statistics(numbers)  # arguments evaluated first, then function body runs
sum(...) / len(...)  # sum(...) → then len(...) → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_statistics(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_statistics(...))  # calculate_statistics() runs with numbers=[4, 1, 8, 6, 3]
#   → return {'sum': sum(numbers), 'average': sum(numbers) / len(numbers), 'min': min(numbers), 'max': max(numbers)}
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — calculate_statistics, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_statistics(...))  # calculate_statistics() runs first → value goes to print()

"""

"""
Problem 1881:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def extract_numbers_from_string(text):  # defines a function with parameters text
    import re
    return [int(match) for match in re.findall('\\d+', text)]  # return sends the result back to the caller
#
# Built-in functions: int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
extract_numbers_from_string('I have 3 cats and 2 dogs')  # arguments evaluated first, then function body runs
extract_numbers_from_string(text)  # arguments evaluated first, then function body runs
print(extract_numbers_from_string('world'))  # extract_numbers_from_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def extract_numbers_from_string(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def extract_numbers_from_string(text):  # stored in memory, not executed yet
def extract_numbers_from_string(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(extract_numbers_from_string(...))  # extract_numbers_from_string() runs with text='world'
#   → return [int(match) for match in re.findall('\\d+', text)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only extract_numbers_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(extract_numbers_from_string(...))  # extract_numbers_from_string() runs first → value goes to print()

"""

"""
Problem 1883:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_password_strength(password):  # defines a function with parameters password
    return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))  # return sends the result back to the caller
#
# Operators: >= and
# Built-in functions: any, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_password_strength('Password123'))  # check_password_strength() runs first, then print() outputs the result
print(check_password_strength('weak'))  # check_password_strength() runs first, then print() outputs the result
check_password_strength(password)  # arguments evaluated first, then function body runs
print(check_password_strength(?))  # check_password_strength() runs first, then print() outputs the result
len(...) >= 8  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_password_strength(password):  # stored in memory, not executed yet
print(check_password_strength(...), check_password_strength(...))  # check_password_strength() runs with password='Password123'
#   → return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))
def check_password_strength(password):  # stored in memory, not executed yet
def check_password_strength(password):  # stored in memory, not executed yet
password = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_password_strength(...))  # check_password_strength() runs with password=[4, 1, 8, 6, 3]
#   → return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_password_strength is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_password_strength(...), check_password_strength(...))  # check_password_strength() runs first → value goes to print()
#   → password = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_password_strength(...))  # check_password_strength() runs first → value goes to print()

"""

"""
Problem 1884:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_by_length_range(text, min_length, max_length):  # defines a function with parameters text, min_length, max_length
    words = text.split()
    return [word for word in words if min_length <= len(word) <= max_length]  # return sends the result back to the caller
#
# Operators: <=
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_by_length_range('hello world python programming', 5, 6)  # arguments evaluated first, then function body runs
get_words_by_length_range(text, min_length, max_length)  # arguments evaluated first, then function body runs
print(get_words_by_length_range('world', 25, 25))  # get_words_by_length_range() runs first, then print() outputs the result
min_length <= len(...)  # evaluated as True or False → picks which branch
min_length <= max_length  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_by_length_range(text, min_length, max_length):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_by_length_range(text, min_length, max_length):  # stored in memory, not executed yet
def get_words_by_length_range(text, min_length, max_length):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
min_length = ...  # assigns a value to the variable
max_length = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_by_length_range(...))  # get_words_by_length_range() runs with text='world', min_length=25, max_length=25
#   → return [word for word in words if min_length <= len(word) <= max_length]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_by_length_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → min_length = ...  # stored in memory
#   → max_length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_by_length_range(...))  # get_words_by_length_range() runs first → value goes to print()

"""

"""
Problem 1885:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_compound_interest(principal, rate, time, frequency):  # defines a function with parameters principal, rate, time, frequency
    return principal * (1 + rate / (100 * frequency)) ** (frequency * time)  # return sends the result back to the caller
#
# Operators: * + / **
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_compound_interest(1000, 5, 2, 12)  # arguments evaluated first, then function body runs
calculate_compound_interest(principal, rate, time, frequency)  # arguments evaluated first, then function body runs
print(calculate_compound_interest(25, 25, 25, 25))  # calculate_compound_interest() runs first, then print() outputs the result
principal * ?  # principal → then ? → then *
? ** ?  # ? → then ? → then **
1 + ?  # 1 → then ? → then +
frequency * time  # frequency → then time → then *
rate / ?  # rate → then ? → then /
100 * frequency  # 100 → then frequency → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_compound_interest(principal, rate, time, frequency):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(round(...))
def calculate_compound_interest(principal, rate, time, frequency):  # stored in memory, not executed yet
def calculate_compound_interest(principal, rate, time, frequency):  # stored in memory, not executed yet
principal = ...  # assigns a value to the variable
rate = ...  # assigns a value to the variable
time = ...  # assigns a value to the variable
frequency = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_compound_interest(...))  # calculate_compound_interest() runs with principal=25, rate=25, time=25, frequency=25
#   → return principal * (1 + rate / (100 * frequency)) ** (frequency * time)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_compound_interest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → principal = ...  # stored in memory
#   → rate = ...  # stored in memory
#   → time = ...  # stored in memory
#   → frequency = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_compound_interest(...))  # calculate_compound_interest() runs first → value goes to print()

"""

"""
Problem 1886:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_duplicate_elements(my_list):  # defines a function with parameters my_list
    seen = set()
    duplicates = set()
    for item in my_list:
        if item in seen:
            duplicates.add(item)
        seen.add(item)
    return list(duplicates)  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: list, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
find_duplicate_elements(?)  # arguments evaluated first, then function body runs
find_duplicate_elements(my_list)  # arguments evaluated first, then function body runs
item in seen  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_duplicate_elements(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(sorted(...))
def find_duplicate_elements(my_list):  # stored in memory, not executed yet
def find_duplicate_elements(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_duplicate_elements(...))  # find_duplicate_elements() runs with my_list=[4, 1, 8, 6, 3]
#   → return list(duplicates)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_duplicate_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_duplicate_elements(...))  # find_duplicate_elements() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1887:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def group_by_first_letter(strings):  # defines a function with parameters strings
    groups = {}
    for s in strings:
        first_letter = s[0].lower()
        if first_letter not in groups:
            groups[first_letter] = []
        groups[first_letter].append(s)
    return groups  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
group_by_first_letter(?)  # arguments evaluated first, then function body runs
group_by_first_letter(strings)  # arguments evaluated first, then function body runs
first_letter not in groups  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def group_by_first_letter(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def group_by_first_letter(strings):  # stored in memory, not executed yet
def group_by_first_letter(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(group_by_first_letter(...))  # group_by_first_letter() runs with strings=[4, 1, 8, 6, 3]
#   → return groups
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only group_by_first_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(group_by_first_letter(...))  # group_by_first_letter() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1888:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_distance(x1, y1, x2, y2):  # defines a function with parameters x1, y1, x2, y2
    return math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2)  # return sends the result back to the caller
#
# Operators: + - **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_distance(0, 0, 3, 4)  # arguments evaluated first, then function body runs
calculate_distance(x1, y1, x2, y2)  # arguments evaluated first, then function body runs
print(calculate_distance(25, 25, 25, 25))  # calculate_distance() runs first, then print() outputs the result
? + ?  # ? → then ? → then +
? ** 2  # ? → then 2 → then **
x2 - x1  # x2 → then x1 → then -
y2 - y1  # y2 → then y1 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import math
def calculate_distance(x1, y1, x2, y2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_distance(x1, y1, x2, y2):  # stored in memory, not executed yet
def calculate_distance(x1, y1, x2, y2):  # stored in memory, not executed yet
x1 = ...  # assigns a value to the variable
y1 = ...  # assigns a value to the variable
x2 = ...  # assigns a value to the variable
y2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_distance(...))  # calculate_distance() runs with x1=25, y1=25, x2=25, y2=25
#   → return math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_distance is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → x1 = ...  # stored in memory
#   → y1 = ...  # stored in memory
#   → x2 = ...  # stored in memory
#   → y2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_distance(...))  # calculate_distance() runs first → value goes to print()

"""

"""
Problem 1889:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_in_range(number, min_val, max_val):  # defines a function with parameters number, min_val, max_val
    return min_val <= number <= max_val  # return sends the result back to the caller
#
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_in_range(5, 1, 10))  # check_in_range() runs first, then print() outputs the result
print(check_in_range(15, 1, 10))  # check_in_range() runs first, then print() outputs the result
check_in_range(number, min_val, max_val)  # arguments evaluated first, then function body runs
print(check_in_range(25, 25, 25))  # check_in_range() runs first, then print() outputs the result
min_val <= number  # evaluated as True or False → picks which branch
min_val <= max_val  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_in_range(number, min_val, max_val):  # stored in memory, not executed yet
print(check_in_range(...), check_in_range(...))  # check_in_range() runs with number=5, min_val=1, max_val=10
#   → return min_val <= number <= max_val
def check_in_range(number, min_val, max_val):  # stored in memory, not executed yet
def check_in_range(number, min_val, max_val):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
min_val = ...  # assigns a value to the variable
max_val = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_in_range(...))  # check_in_range() runs with number=25, min_val=25, max_val=25
#   → return min_val <= number <= max_val
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_in_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_in_range(...), check_in_range(...))  # check_in_range() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_in_range(...))  # check_in_range() runs first → value goes to print()

"""

"""
Problem 1890:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_divisors(number):  # defines a function with parameters number
    return [i for i in range(1, number + 1) if number % i == 0]  # return sends the result back to the caller
#
# Operators: % + ==
# Built-in functions: range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_divisors(12)  # arguments evaluated first, then function body runs
get_divisors(number)  # arguments evaluated first, then function body runs
print(get_divisors(25))  # get_divisors() runs first, then print() outputs the result
? == 0  # evaluated as True or False → picks which branch
number + 1  # number → then 1 → then +
number % i  # number → then i → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_divisors(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_divisors(number):  # stored in memory, not executed yet
def get_divisors(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_divisors(...))  # get_divisors() runs with number=25
#   → return [i for i in range(1, number + 1) if number % i == 0]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_divisors is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_divisors(...))  # get_divisors() runs first → value goes to print()

"""

"""
Problem 1891:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_perfect_square(number):  # defines a function with parameters number
    import math
    root = math.sqrt(number)
    return root == int(root)  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_perfect_square(16))  # check_perfect_square() runs first, then print() outputs the result
print(check_perfect_square(15))  # check_perfect_square() runs first, then print() outputs the result
check_perfect_square(number)  # arguments evaluated first, then function body runs
print(check_perfect_square(25))  # check_perfect_square() runs first, then print() outputs the result
root == int(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_perfect_square(number):  # stored in memory, not executed yet
print(check_perfect_square(...), check_perfect_square(...))  # check_perfect_square() runs with number=16
#   → return root == int(root)
def check_perfect_square(number):  # stored in memory, not executed yet
def check_perfect_square(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_perfect_square(...))  # check_perfect_square() runs with number=25
#   → return root == int(root)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_perfect_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_perfect_square(...), check_perfect_square(...))  # check_perfect_square() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_perfect_square(...))  # check_perfect_square() runs first → value goes to print()

"""

"""
Problem 1892:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_time(hours, minutes, seconds):  # defines a function with parameters hours, minutes, seconds
    return f'{hours:02d}:{minutes:02d}:{seconds:02d}'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_time(9, 5, 3)  # arguments evaluated first, then function body runs
format_time(hours, minutes, seconds)  # arguments evaluated first, then function body runs
print(format_time(25, 25, 25))  # format_time() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_time(hours, minutes, seconds):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_time(hours, minutes, seconds):  # stored in memory, not executed yet
def format_time(hours, minutes, seconds):  # stored in memory, not executed yet
hours = ...  # assigns a value to the variable
minutes = ...  # assigns a value to the variable
seconds = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_time(...))  # format_time() runs with hours=25, minutes=25, seconds=25
#   → return f'{hours:02d}:{minutes:02d}:{seconds:02d}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_time is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → hours = ...  # stored in memory
#   → minutes = ...  # stored in memory
#   → seconds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_time(...))  # format_time() runs first → value goes to print()

"""

"""
Problem 1893:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_age(birth_year, birth_month, birth_day):  # defines a function with parameters birth_year, birth_month, birth_day
    current_year = 2024
    current_month = 1
    current_day = 1
    age = current_year - birth_year
    if (current_month, current_day) < (birth_month, birth_day):
        age -= 1
    return age  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: + - <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_age(2000, 6, 15)  # arguments evaluated first, then function body runs
calculate_age(birth_year, birth_month, birth_day)  # arguments evaluated first, then function body runs
print(calculate_age(25, 25, 25))  # calculate_age() runs first, then print() outputs the result
current_year - birth_year  # current_year → then birth_year → then -
? < ?  # evaluated as True or False → picks which branch
current_year + ?  # current_year → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_age(birth_year, birth_month, birth_day):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_age(birth_year, birth_month, birth_day):  # stored in memory, not executed yet
def calculate_age(birth_year, birth_month, birth_day):  # stored in memory, not executed yet
birth_year = ...  # assigns a value to the variable
birth_month = ...  # assigns a value to the variable
birth_day = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_age(...))  # calculate_age() runs with birth_year=25, birth_month=25, birth_day=25
#   → import operator
#   → return operator.sub(current_year, birth_year)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_age is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → birth_year = ...  # stored in memory
#   → birth_month = ...  # stored in memory
#   → birth_day = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_age(...))  # calculate_age() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1894:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_middle_elements(my_list):  # defines a function with parameters my_list
    return my_list[1:-1]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_middle_elements(?)  # arguments evaluated first, then function body runs
get_middle_elements(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_middle_elements(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_middle_elements(my_list):  # stored in memory, not executed yet
def get_middle_elements(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_middle_elements(...))  # get_middle_elements() runs with my_list=[4, 1, 8, 6, 3]
#   → return my_list[1:-1]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_elements(...))  # get_middle_elements() runs first → value goes to print()

"""

"""
Problem 1895:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def swap_adjacent_elements(my_list):  # defines a function with parameters my_list
    new_list = my_list.copy()
    for i in range(0, len(new_list) - 1, 2):
        new_list[i], new_list[i + 1] = (new_list[i + 1], new_list[i])
    return new_list  # return sends the result back to the caller
#
# Keywords: for
# Operators: + -
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
swap_adjacent_elements(?)  # arguments evaluated first, then function body runs
swap_adjacent_elements(my_list)  # arguments evaluated first, then function body runs
len(...) - 1  # len(...) → then 1 → then -
i + 1  # i → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def swap_adjacent_elements(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def swap_adjacent_elements(my_list):  # stored in memory, not executed yet
def swap_adjacent_elements(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(swap_adjacent_elements(...))  # swap_adjacent_elements() runs with my_list=[4, 1, 8, 6, 3]
#   → return new_list
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_adjacent_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_adjacent_elements(...))  # swap_adjacent_elements() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done

"""

"""
Problem 1896:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_every_nth_element(my_list, n):  # defines a function with parameters my_list, n
    return my_list[::n]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_every_nth_element(?, 3)  # arguments evaluated first, then function body runs
get_every_nth_element(my_list, n)  # arguments evaluated first, then function body runs
print(get_every_nth_element(?, 15))  # get_every_nth_element() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_every_nth_element(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_every_nth_element(my_list, n):  # stored in memory, not executed yet
def get_every_nth_element(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_every_nth_element(...))  # get_every_nth_element() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return my_list[::n]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_every_nth_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_every_nth_element(...))  # get_every_nth_element() runs first → value goes to print()

"""

"""
Problem 1898:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def remove_duplicates_preserve_order(my_list):  # defines a function with parameters my_list
    seen = set()
    result = []
    for item in my_list:
        if item not in seen:
            seen.add(item)
            result.append(item)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
remove_duplicates_preserve_order(?)  # arguments evaluated first, then function body runs
remove_duplicates_preserve_order(my_list)  # arguments evaluated first, then function body runs
item not in seen  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def remove_duplicates_preserve_order(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def remove_duplicates_preserve_order(my_list):  # stored in memory, not executed yet
def remove_duplicates_preserve_order(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(remove_duplicates_preserve_order(...))  # remove_duplicates_preserve_order() runs with my_list=[4, 1, 8, 6, 3]
#   → return list(map(lambda item: item, my_list))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_duplicates_preserve_order is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_duplicates_preserve_order(...))  # remove_duplicates_preserve_order() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1899:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_intersection_three_lists(list1, list2, list3):  # defines a function with parameters list1, list2, list3
    return [x for x in list1 if x in list2 and x in list3]  # return sends the result back to the caller
#
# Operators: in and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_intersection_three_lists(?, ?, ?)  # arguments evaluated first, then function body runs
get_intersection_three_lists(list1, list2, list3)  # arguments evaluated first, then function body runs
x in list2  # evaluated as True or False → picks which branch
x in list3  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_intersection_three_lists(list1, list2, list3):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_intersection_three_lists(list1, list2, list3):  # stored in memory, not executed yet
def get_intersection_three_lists(list1, list2, list3):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
list3 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_intersection_three_lists(...))  # get_intersection_three_lists() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3], list3=[4, 1, 8, 6, 3]
#   → return [x for x in list1 if x in list2 and x in list3]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_intersection_three_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_intersection_three_lists(...))  # get_intersection_three_lists() runs first → value goes to print()

"""

"""
Problem 1900:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_percentage(part, whole):  # defines a function with parameters part, whole
    return round(part / whole * 100, 2)  # return sends the result back to the caller
#
# Operators: * /
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_percentage(25, 100)  # arguments evaluated first, then function body runs
calculate_percentage(part, whole)  # arguments evaluated first, then function body runs
print(calculate_percentage(25, 25))  # calculate_percentage() runs first, then print() outputs the result
? * 100  # ? → then 100 → then *
part / whole  # part → then whole → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_percentage(part, whole):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_percentage(part, whole):  # stored in memory, not executed yet
def calculate_percentage(part, whole):  # stored in memory, not executed yet
part = ...  # assigns a value to the variable
whole = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_percentage(...))  # calculate_percentage() runs with part=25, whole=25
#   → return round(part / whole * 100, 2)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_percentage is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → part = ...  # stored in memory
#   → whole = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_percentage(...))  # calculate_percentage() runs first → value goes to print()

"""

"""
Problem 1901:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_largest_three(numbers):  # defines a function with parameters numbers
    return sorted(numbers, reverse=True)[:3]  # return sends the result back to the caller
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
find_largest_three(?)  # arguments evaluated first, then function body runs
find_largest_three(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_largest_three(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def find_largest_three(numbers):  # stored in memory, not executed yet
def find_largest_three(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_largest_three(...))  # find_largest_three() runs with numbers=[4, 1, 8, 6, 3]
#   → return sorted(numbers, reverse=True)[:3]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_largest_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_largest_three(...))  # find_largest_three() runs first → value goes to print()

"""

"""
Problem 1902:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_smallest_three(numbers):  # defines a function with parameters numbers
    return sorted(numbers)[:3]  # return sends the result back to the caller
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
find_smallest_three(?)  # arguments evaluated first, then function body runs
find_smallest_three(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_smallest_three(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def find_smallest_three(numbers):  # stored in memory, not executed yet
def find_smallest_three(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_smallest_three(...))  # find_smallest_three() runs with numbers=[4, 1, 8, 6, 3]
#   → return sorted(numbers)[:3]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_smallest_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_smallest_three(...))  # find_smallest_three() runs first → value goes to print()

"""

"""
Problem 1903:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_palindrome_and_length(text):  # defines a function with parameters text
    is_palindrome = text == text[::-1]
    length = len(text)
    return (is_palindrome, length)  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
check_palindrome_and_length('racecar')  # arguments evaluated first, then function body runs
check_palindrome_and_length(text)  # arguments evaluated first, then function body runs
print(check_palindrome_and_length('world'))  # check_palindrome_and_length() runs first, then print() outputs the result
text == ?  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_palindrome_and_length(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def check_palindrome_and_length(text):  # stored in memory, not executed yet
def check_palindrome_and_length(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_palindrome_and_length(...))  # check_palindrome_and_length() runs with text='world'
#   → return (is_palindrome, length)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_palindrome_and_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_palindrome_and_length(...))  # check_palindrome_and_length() runs first → value goes to print()

"""

"""
Problem 1904:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_starting_with_vowel(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    words = text.split()
    return [word for word in words if word[0] in vowels]  # return sends the result back to the caller
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_starting_with_vowel('apple orange banana cherry')  # arguments evaluated first, then function body runs
get_words_starting_with_vowel(text)  # arguments evaluated first, then function body runs
print(get_words_starting_with_vowel('world'))  # get_words_starting_with_vowel() runs first, then print() outputs the result
? in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_starting_with_vowel(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_starting_with_vowel(text):  # stored in memory, not executed yet
def get_words_starting_with_vowel(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_starting_with_vowel(...))  # get_words_starting_with_vowel() runs with text='world'
#   → return [word for word in words if word[0] in vowels]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_starting_with_vowel is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_starting_with_vowel(...))  # get_words_starting_with_vowel() runs first → value goes to print()

"""

"""
Problem 1905:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_ending_with_consonant(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    words = text.split()
    return [word for word in words if word[-1] not in vowels and word[-1].isalpha()]  # return sends the result back to the caller
#
# Operators: and not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_ending_with_consonant('hello world python')  # arguments evaluated first, then function body runs
get_words_ending_with_consonant(text)  # arguments evaluated first, then function body runs
print(get_words_ending_with_consonant('world'))  # get_words_ending_with_consonant() runs first, then print() outputs the result
? not in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_ending_with_consonant(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_ending_with_consonant(text):  # stored in memory, not executed yet
def get_words_ending_with_consonant(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_ending_with_consonant(...))  # get_words_ending_with_consonant() runs with text='world'
#   → return [word for word in words if word[-1] not in vowels and word[-1].isalpha()]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_ending_with_consonant is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_ending_with_consonant(...))  # get_words_ending_with_consonant() runs first → value goes to print()

"""

"""
Problem 1906:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_area_perimeter(length, width):  # defines a function with parameters length, width
    area = length * width
    perimeter = 2 * (length + width)
    return (area, perimeter)  # return sends the result back to the caller
#
# Operators: * +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_area_perimeter(5, 3)  # arguments evaluated first, then function body runs
calculate_area_perimeter(length, width)  # arguments evaluated first, then function body runs
print(calculate_area_perimeter(25, 25))  # calculate_area_perimeter() runs first, then print() outputs the result
length * width  # length → then width → then *
2 * ?  # 2 → then ? → then *
length + width  # length → then width → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_area_perimeter(length, width):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_area_perimeter(length, width):  # stored in memory, not executed yet
def calculate_area_perimeter(length, width):  # stored in memory, not executed yet
length = ...  # assigns a value to the variable
width = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_area_perimeter(...))  # calculate_area_perimeter() runs with length=25, width=25
#   → return (area, perimeter)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_area_perimeter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_area_perimeter(...))  # calculate_area_perimeter() runs first → value goes to print()

"""

"""
Problem 1907:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_currency(amount):  # defines a function with parameters amount
    return f'${amount:.2f}'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_currency(1234.5)  # arguments evaluated first, then function body runs
format_currency(amount)  # arguments evaluated first, then function body runs
print(format_currency(25))  # format_currency() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_currency(amount):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_currency(amount):  # stored in memory, not executed yet
def format_currency(amount):  # stored in memory, not executed yet
amount = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_currency(...))  # format_currency() runs with amount=25
#   → return f'${amount:.2f}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_currency is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → amount = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_currency(...))  # format_currency() runs first → value goes to print()

"""

"""
Problem 1908:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_even_indices(my_list):  # defines a function with parameters my_list
    return [my_list[i] for i in range(0, len(my_list), 2)]  # return sends the result back to the caller
#
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_even_indices(?)  # arguments evaluated first, then function body runs
get_even_indices(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_even_indices(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_even_indices(my_list):  # stored in memory, not executed yet
def get_even_indices(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_even_indices(...))  # get_even_indices() runs with my_list=[4, 1, 8, 6, 3]
#   → return [my_list[i] for i in range(0, len(my_list), 2)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_indices(...))  # get_even_indices() runs first → value goes to print()

"""

"""
Problem 1909:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_odd_indices(my_list):  # defines a function with parameters my_list
    return [my_list[i] for i in range(1, len(my_list), 2)]  # return sends the result back to the caller
#
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_odd_indices(?)  # arguments evaluated first, then function body runs
get_odd_indices(my_list)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_odd_indices(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_odd_indices(my_list):  # stored in memory, not executed yet
def get_odd_indices(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_odd_indices(...))  # get_odd_indices() runs with my_list=[4, 1, 8, 6, 3]
#   → return [my_list[i] for i in range(1, len(my_list), 2)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_odd_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_odd_indices(...))  # get_odd_indices() runs first → value goes to print()

"""

"""
Problem 1910:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def multiply_corresponding(list1, list2):  # defines a function with parameters list1, list2
    return [a * b for a, b in zip(list1, list2)]  # return sends the result back to the caller
#
# Operators: *
# Built-in functions: zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
multiply_corresponding(?, ?)  # arguments evaluated first, then function body runs
multiply_corresponding(list1, list2)  # arguments evaluated first, then function body runs
a * b  # a → then b → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def multiply_corresponding(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def multiply_corresponding(list1, list2):  # stored in memory, not executed yet
def multiply_corresponding(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(multiply_corresponding(...))  # multiply_corresponding() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return [a * b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply_corresponding is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply_corresponding(...))  # multiply_corresponding() runs first → value goes to print()

"""

"""
Problem 1911:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def divide_corresponding(list1, list2):  # defines a function with parameters list1, list2
    result = []
    for a, b in zip(list1, list2):
        if b == 0:
            result.append(None)
        else:
            result.append(a / b)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: / ==
# Built-in functions: zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
divide_corresponding(?, ?)  # arguments evaluated first, then function body runs
divide_corresponding(list1, list2)  # arguments evaluated first, then function body runs
b == 0  # evaluated as True or False → picks which branch
a / b  # a → then b → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def divide_corresponding(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def divide_corresponding(list1, list2):  # stored in memory, not executed yet
def divide_corresponding(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(divide_corresponding(...))  # divide_corresponding() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divide_corresponding is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divide_corresponding(...))  # divide_corresponding() runs first → value goes to print()
#
# Loop: takes one item from zip(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1912:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_containing(strings, substring):  # defines a function with parameters strings, substring
    return [s for s in strings if substring in s]  # return sends the result back to the caller
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_containing(?, 'on')  # arguments evaluated first, then function body runs
get_strings_containing(strings, substring)  # arguments evaluated first, then function body runs
print(get_strings_containing(?, 'world'))  # get_strings_containing() runs first, then print() outputs the result
substring in s  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_containing(strings, substring):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_containing(strings, substring):  # stored in memory, not executed yet
def get_strings_containing(strings, substring):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
substring = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_containing(...))  # get_strings_containing() runs with strings=[4, 1, 8, 6, 3], substring='world'
#   → return [s for s in strings if substring in s]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_containing is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → substring = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_containing(...))  # get_strings_containing() runs first → value goes to print()

"""

"""
Problem 1913:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_characters_by_type(text):  # defines a function with parameters text
    return {'letters': sum((1 for c in text if c.isalpha())), 'digits': sum((1 for c in text if c.isdigit())), 'others': sum((1 for c in text if not c.isalnum() and (not c.isspace())))}  # return sends the result back to the caller
#
# Operators: and
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_characters_by_type('Hello123!')  # arguments evaluated first, then function body runs
count_characters_by_type(text)  # arguments evaluated first, then function body runs
print(count_characters_by_type('world'))  # count_characters_by_type() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_characters_by_type(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_characters_by_type(...))  # count_characters_by_type() runs with text='world'
#   → return {'letters': sum((1 for c in text if c.isalpha())), 'digits': sum((1 for c in text if c.isdigit())), 'others': sum((1 for c in text if not c.isalnum() and (not c.isspace())))}
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — count_characters_by_type, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_characters_by_type(...))  # count_characters_by_type() runs first → value goes to print()

"""

"""
Problem 1914:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_range_between(my_list, min_val, max_val):  # defines a function with parameters my_list, min_val, max_val
    return [x for x in my_list if min_val <= x <= max_val]  # return sends the result back to the caller
#
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_range_between(?, 20, 40)  # arguments evaluated first, then function body runs
get_range_between(my_list, min_val, max_val)  # arguments evaluated first, then function body runs
print(get_range_between(?, 25, 25))  # get_range_between() runs first, then print() outputs the result
min_val <= x  # evaluated as True or False → picks which branch
min_val <= max_val  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_range_between(my_list, min_val, max_val):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_range_between(my_list, min_val, max_val):  # stored in memory, not executed yet
def get_range_between(my_list, min_val, max_val):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
min_val = ...  # assigns a value to the variable
max_val = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_range_between(...))  # get_range_between() runs with my_list=[4, 1, 8, 6, 3], min_val=25, max_val=25
#   → return [x for x in my_list if min_val <= x <= max_val]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_range_between is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_range_between(...))  # get_range_between() runs first → value goes to print()

"""

"""
Problem 1915:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_date(year, month, day):  # defines a function with parameters year, month, day
    return f'{year}-{month:02d}-{day:02d}'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_date(2024, 3, 5)  # arguments evaluated first, then function body runs
format_date(year, month, day)  # arguments evaluated first, then function body runs
print(format_date(25, 25, 25))  # format_date() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_date(year, month, day):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_date(year, month, day):  # stored in memory, not executed yet
def format_date(year, month, day):  # stored in memory, not executed yet
year = ...  # assigns a value to the variable
month = ...  # assigns a value to the variable
day = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_date(...))  # format_date() runs with year=25, month=25, day=25
#   → return f'{year}-{month:02d}-{day:02d}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_date is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → year = ...  # stored in memory
#   → month = ...  # stored in memory
#   → day = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_date(...))  # format_date() runs first → value goes to print()

"""

"""
Problem 1916:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_simple_interest(principal, rate, time):  # defines a function with parameters principal, rate, time
    return principal * rate * time / 100  # return sends the result back to the caller
#
# Operators: * /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_simple_interest(1000, 5, 2)  # arguments evaluated first, then function body runs
calculate_simple_interest(principal, rate, time)  # arguments evaluated first, then function body runs
print(calculate_simple_interest(25, 25, 25))  # calculate_simple_interest() runs first, then print() outputs the result
? / 100  # ? → then 100 → then /
? * time  # ? → then time → then *
principal * rate  # principal → then rate → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_simple_interest(principal, rate, time):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_simple_interest(principal, rate, time):  # stored in memory, not executed yet
def calculate_simple_interest(principal, rate, time):  # stored in memory, not executed yet
principal = ...  # assigns a value to the variable
rate = ...  # assigns a value to the variable
time = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_simple_interest(...))  # calculate_simple_interest() runs with principal=25, rate=25, time=25
#   → return principal * rate * time / 100
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_simple_interest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → principal = ...  # stored in memory
#   → rate = ...  # stored in memory
#   → time = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_simple_interest(...))  # calculate_simple_interest() runs first → value goes to print()

"""

"""
Problem 1917:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_unique_characters(text):  # defines a function with parameters text
    seen = set()
    result = []
    for char in text:
        if char not in seen:
            seen.add(char)
            result.append(char)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_unique_characters('hello')  # arguments evaluated first, then function body runs
get_unique_characters(text)  # arguments evaluated first, then function body runs
print(get_unique_characters('world'))  # get_unique_characters() runs first, then print() outputs the result
char not in seen  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_unique_characters(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_unique_characters(text):  # stored in memory, not executed yet
def get_unique_characters(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_unique_characters(...))  # get_unique_characters() runs with text='world'
#   → return list(map(lambda char: char, text))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_characters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_characters(...))  # get_unique_characters() runs first → value goes to print()
#
# Loop: takes one item from text, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1918:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_words_ending_with(text, suffix):  # defines a function with parameters text, suffix
    words = text.split()
    return sum((1 for word in words if word.endswith(suffix)))  # return sends the result back to the caller
#
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_words_ending_with('running walking jumping', 'ing')  # arguments evaluated first, then function body runs
count_words_ending_with(text, suffix)  # arguments evaluated first, then function body runs
print(count_words_ending_with('world', '?'))  # count_words_ending_with() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_words_ending_with(text, suffix):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_words_ending_with(text, suffix):  # stored in memory, not executed yet
def count_words_ending_with(text, suffix):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
suffix = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_words_ending_with(...))  # count_words_ending_with() runs with text='world', suffix='?'
#   → return sum((1 for word in words if word.endswith(suffix)))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words_ending_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → suffix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words_ending_with(...))  # count_words_ending_with() runs first → value goes to print()

"""

"""
Problem 1919:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_numbers_in_range(numbers, min_val, max_val):  # defines a function with parameters numbers, min_val, max_val
    return [x for x in numbers if min_val <= x <= max_val]  # return sends the result back to the caller
#
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_numbers_in_range(?, 25, 45)  # arguments evaluated first, then function body runs
get_numbers_in_range(numbers, min_val, max_val)  # arguments evaluated first, then function body runs
print(get_numbers_in_range(?, 25, 25))  # get_numbers_in_range() runs first, then print() outputs the result
min_val <= x  # evaluated as True or False → picks which branch
min_val <= max_val  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_numbers_in_range(numbers, min_val, max_val):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_numbers_in_range(numbers, min_val, max_val):  # stored in memory, not executed yet
def get_numbers_in_range(numbers, min_val, max_val):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
min_val = ...  # assigns a value to the variable
max_val = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_numbers_in_range(...))  # get_numbers_in_range() runs with numbers=[4, 1, 8, 6, 3], min_val=25, max_val=25
#   → return [x for x in numbers if min_val <= x <= max_val]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_in_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_in_range(...))  # get_numbers_in_range() runs first → value goes to print()

"""

"""
Problem 1920:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_name_age_city(name, age, city):  # defines a function with parameters name, age, city
    return f'Name: {name}, Age: {age}, City: {city}'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_name_age_city('Alice', 30, 'New York')  # arguments evaluated first, then function body runs
format_name_age_city(name, age, city)  # arguments evaluated first, then function body runs
print(format_name_age_city('Bob', 25, 25))  # format_name_age_city() runs first, then print() outputs the result
'Name: %s, Age: %s, City: %s' % ?  # 'Name: %s, Age: %s, City: %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_name_age_city(name, age, city):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_name_age_city(name, age, city):  # stored in memory, not executed yet
def format_name_age_city(name, age, city):  # stored in memory, not executed yet
name = ...  # assigns a value to the variable
age = ...  # assigns a value to the variable
city = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_name_age_city(...))  # format_name_age_city() runs with name='Bob', age=25, city=25
#   → return 'Name: %s, Age: %s, City: %s' % (name, age, city)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_name_age_city is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → age = ...  # stored in memory
#   → city = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_name_age_city(...))  # format_name_age_city() runs first → value goes to print()

"""

"""
Problem 1921:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_multiple_of(number, divisors):  # defines a function with parameters number, divisors
    return all((number % d == 0 for d in divisors))  # return sends the result back to the caller
#
# Operators: % ==
# Built-in functions: all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_multiple_of(12, ?))  # check_multiple_of() runs first, then print() outputs the result
check_multiple_of(number, divisors)  # arguments evaluated first, then function body runs
print(check_multiple_of(25, ?))  # check_multiple_of() runs first, then print() outputs the result
? == 0  # evaluated as True or False → picks which branch
number % d  # number → then d → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_multiple_of(number, divisors):  # stored in memory, not executed yet
print(check_multiple_of(...), check_multiple_of(...))  # check_multiple_of() runs with number=12, divisors=[2, 3, 4]
#   → return all((number % d == 0 for d in divisors))
def check_multiple_of(number, divisors):  # stored in memory, not executed yet
def check_multiple_of(number, divisors):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
divisors = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_multiple_of(...))  # check_multiple_of() runs with number=25, divisors=[4, 1, 8, 6, 3]
#   → return all((number % d == 0 for d in divisors))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_multiple_of is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_multiple_of(...), check_multiple_of(...))  # check_multiple_of() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → divisors = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_multiple_of(...))  # check_multiple_of() runs first → value goes to print()

"""

"""
Problem 1922:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_sum_of_squares(numbers):  # defines a function with parameters numbers
    return sum((x ** 2 for x in numbers))  # return sends the result back to the caller
#
# Operators: **
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_sum_of_squares(?)  # arguments evaluated first, then function body runs
get_sum_of_squares(numbers)  # arguments evaluated first, then function body runs
x ** 2  # x → then 2 → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_sum_of_squares(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_sum_of_squares(numbers):  # stored in memory, not executed yet
def get_sum_of_squares(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_sum_of_squares(...))  # get_sum_of_squares() runs with numbers=[4, 1, 8, 6, 3]
#   → return sum((x ** 2 for x in numbers))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_sum_of_squares is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_sum_of_squares(...))  # get_sum_of_squares() runs first → value goes to print()

"""

"""
Problem 1923:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_product_of_even(numbers):  # defines a function with parameters numbers
    product = 1
    for x in numbers:
        if x % 2 == 0:
            product *= x
    return product  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_product_of_even(?)  # arguments evaluated first, then function body runs
get_product_of_even(numbers)  # arguments evaluated first, then function body runs
? == 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_product_of_even(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_product_of_even(numbers):  # stored in memory, not executed yet
def get_product_of_even(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_product_of_even(...))  # get_product_of_even() runs with numbers=[4, 1, 8, 6, 3]
#   → return product
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_product_of_even is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_product_of_even(...))  # get_product_of_even() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1924:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_sum_of_odd(numbers):  # defines a function with parameters numbers
    return sum((x for x in numbers if x % 2 != 0))  # return sends the result back to the caller
#
# Operators: % !=
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_sum_of_odd(?)  # arguments evaluated first, then function body runs
get_sum_of_odd(numbers)  # arguments evaluated first, then function body runs
? != 0  # evaluated as True or False → picks which branch
x % 2  # x → then 2 → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_sum_of_odd(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_sum_of_odd(numbers):  # stored in memory, not executed yet
def get_sum_of_odd(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_sum_of_odd(...))  # get_sum_of_odd() runs with numbers=[4, 1, 8, 6, 3]
#   → return sum((x for x in numbers if x % 2 != 0))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_sum_of_odd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_sum_of_odd(...))  # get_sum_of_odd() runs first → value goes to print()

"""

"""
Problem 1925:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def find_second_occurrence(my_list, element):  # defines a function with parameters my_list, element
    count = 0
    for i, item in enumerate(my_list):
        if item == element:
            count += 1
            if count == 2:
                return i
    return -1  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: enumerate()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
find_second_occurrence(?, 10)  # arguments evaluated first, then function body runs
find_second_occurrence(my_list, element)  # arguments evaluated first, then function body runs
print(find_second_occurrence(?, 25))  # find_second_occurrence() runs first, then print() outputs the result
item == element  # evaluated as True or False → picks which branch
count == 2  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def find_second_occurrence(my_list, element):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def find_second_occurrence(my_list, element):  # stored in memory, not executed yet
def find_second_occurrence(my_list, element):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
element = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(find_second_occurrence(...))  # find_second_occurrence() runs with my_list=[4, 1, 8, 6, 3], element=25
#   → return i
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_second_occurrence is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_second_occurrence(...))  # find_second_occurrence() runs first → value goes to print()
#
# Loop: takes one item from enumerate(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1926:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_greater_than_average(numbers):  # defines a function with parameters numbers
    avg = sum(numbers) / len(numbers)
    return [x for x in numbers if x > avg]  # return sends the result back to the caller
#
# Operators: / >
# Built-in functions: len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_greater_than_average(?)  # arguments evaluated first, then function body runs
get_elements_greater_than_average(numbers)  # arguments evaluated first, then function body runs
sum(...) / len(...)  # sum(...) → then len(...) → then /
x > avg  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_greater_than_average(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_greater_than_average(numbers):  # stored in memory, not executed yet
def get_elements_greater_than_average(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_greater_than_average(...))  # get_elements_greater_than_average() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x > avg]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_greater_than_average is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_greater_than_average(...))  # get_elements_greater_than_average() runs first → value goes to print()

"""

"""
Problem 1927:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_less_than_average(numbers):  # defines a function with parameters numbers
    avg = sum(numbers) / len(numbers)
    return [x for x in numbers if x < avg]  # return sends the result back to the caller
#
# Operators: / <
# Built-in functions: len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_less_than_average(?)  # arguments evaluated first, then function body runs
get_elements_less_than_average(numbers)  # arguments evaluated first, then function body runs
sum(...) / len(...)  # sum(...) → then len(...) → then /
x < avg  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_less_than_average(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_less_than_average(numbers):  # stored in memory, not executed yet
def get_elements_less_than_average(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_less_than_average(...))  # get_elements_less_than_average() runs with numbers=[4, 1, 8, 6, 3]
#   → return [x for x in numbers if x < avg]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_less_than_average is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_less_than_average(...))  # get_elements_less_than_average() runs first → value goes to print()

"""

"""
Problem 1929:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_with_length(text, length):  # defines a function with parameters text, length
    words = text.split()
    return [word for word in words if len(word) == length]  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_with_length('hello world python programming', 5)  # arguments evaluated first, then function body runs
get_words_with_length(text, length)  # arguments evaluated first, then function body runs
print(get_words_with_length('world', 25))  # get_words_with_length() runs first, then print() outputs the result
len(...) == length  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_with_length(text, length):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_with_length(text, length):  # stored in memory, not executed yet
def get_words_with_length(text, length):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
length = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_with_length(...))  # get_words_with_length() runs with text='world', length=25
#   → return [word for word in words if len(word) == length]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_length(...))  # get_words_with_length() runs first → value goes to print()

"""

"""
Problem 1930:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_full_address(street_num, street_name, city, state, zipcode):  # defines a function with parameters street_num, street_name, city, state, zipcode
    return f'{street_num} {street_name}, {city}, {state} {zipcode}'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_full_address('123', 'Main St', 'New York', 'NY', '10001')  # arguments evaluated first, then function body runs
format_full_address(street_num, street_name, city, state, zipcode)  # arguments evaluated first, then function body runs
print(format_full_address(25, 'world', 25, 25, 25))  # format_full_address() runs first, then print() outputs the result
'%s %s, %s, %s %s' % ?  # '%s %s, %s, %s %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_full_address(street_num, street_name, city, state, zipcode):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_full_address(street_num, street_name, city, state, zipcode):  # stored in memory, not executed yet
def format_full_address(street_num, street_name, city, state, zipcode):  # stored in memory, not executed yet
street_num = ...  # assigns a value to the variable
street_name = ...  # assigns a value to the variable
city = ...  # assigns a value to the variable
state = ...  # assigns a value to the variable
zipcode = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_full_address(...))  # format_full_address() runs with street_num=25, street_name='world', city=25, state=25, zipcode=25
#   → return '%s %s, %s, %s %s' % (street_num, street_name, city, state, zipcode)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_full_address is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → street_num = ...  # stored in memory
#   → street_name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → state = ...  # stored in memory
#   → zipcode = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_full_address(...))  # format_full_address() runs first → value goes to print()

"""

"""
Problem 1931:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_total_with_discount(prices, discount_percent, tax_rate):  # defines a function with parameters prices, discount_percent, tax_rate
    subtotal = sum(prices)
    discounted = subtotal * (1 - discount_percent / 100)
    total = discounted * (1 + tax_rate / 100)
    return total  # return sends the result back to the caller
#
# Operators: * + - /
# Built-in functions: round, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_total_with_discount(?, 10, 8)  # arguments evaluated first, then function body runs
calculate_total_with_discount(prices, discount_percent, tax_rate)  # arguments evaluated first, then function body runs
print(calculate_total_with_discount(?, 25, 25))  # calculate_total_with_discount() runs first, then print() outputs the result
subtotal * ?  # subtotal → then ? → then *
discounted * ?  # discounted → then ? → then *
1 - ?  # 1 → then ? → then -
1 + ?  # 1 → then ? → then +
discount_percent / 100  # discount_percent → then 100 → then /
tax_rate / 100  # tax_rate → then 100 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_total_with_discount(prices, discount_percent, tax_rate):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(round(...))
def calculate_total_with_discount(prices, discount_percent, tax_rate):  # stored in memory, not executed yet
def calculate_total_with_discount(prices, discount_percent, tax_rate):  # stored in memory, not executed yet
prices = ...  # assigns a value to the variable
discount_percent = ...  # assigns a value to the variable
tax_rate = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_total_with_discount(...))  # calculate_total_with_discount() runs with prices=[4, 1, 8, 6, 3], discount_percent=25, tax_rate=25
#   → return total
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_total_with_discount is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → prices = ...  # stored in memory
#   → discount_percent = ...  # stored in memory
#   → tax_rate = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_total_with_discount(...))  # calculate_total_with_discount() runs first → value goes to print()

"""

"""
Problem 1932:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_common_prefix(str1, str2):  # defines a function with parameters str1, str2
    prefix = ''
    for i in range(min(len(str1), len(str2))):
        if str1[i] == str2[i]:
            prefix += str1[i]
        else:
            break
    return prefix  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: len, min, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_common_prefix('hello', 'help')  # arguments evaluated first, then function body runs
get_common_prefix(str1, str2)  # arguments evaluated first, then function body runs
print(get_common_prefix('world', 'python'))  # get_common_prefix() runs first, then print() outputs the result
? == ?  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_common_prefix(str1, str2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_common_prefix(str1, str2):  # stored in memory, not executed yet
def get_common_prefix(str1, str2):  # stored in memory, not executed yet
str1 = ...  # assigns a value to the variable
str2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_common_prefix(...))  # get_common_prefix() runs with str1='world', str2='python'
#   → return prefix
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_common_prefix is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_common_prefix(...))  # get_common_prefix() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1933:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_common_suffix(str1, str2):  # defines a function with parameters str1, str2
    suffix = ''
    for i in range(1, min(len(str1), len(str2)) + 1):
        if str1[-i] == str2[-i]:
            suffix = str1[-i] + suffix
        else:
            break
    return suffix  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: len, min, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_common_suffix('running', 'walking')  # arguments evaluated first, then function body runs
get_common_suffix(str1, str2)  # arguments evaluated first, then function body runs
print(get_common_suffix('world', 'python'))  # get_common_suffix() runs first, then print() outputs the result
min(...) + 1  # min(...) → then 1 → then +
? == ?  # evaluated as True or False → picks which branch
? + suffix  # ? → then suffix → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_common_suffix(str1, str2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_common_suffix(str1, str2):  # stored in memory, not executed yet
def get_common_suffix(str1, str2):  # stored in memory, not executed yet
str1 = ...  # assigns a value to the variable
str2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_common_suffix(...))  # get_common_suffix() runs with str1='world', str2='python'
#   → return suffix
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_common_suffix is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_common_suffix(...))  # get_common_suffix() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1934:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_all_uppercase_words(text):  # defines a function with parameters text
    words = text.split()
    return all((word[0].isupper() for word in words if word))  # return sends the result back to the caller
#
# Built-in functions: all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
check_all_uppercase_words('Hello World Python')  # arguments evaluated first, then function body runs
check_all_uppercase_words(text)  # arguments evaluated first, then function body runs
print(check_all_uppercase_words('world'))  # check_all_uppercase_words() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_all_uppercase_words(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def check_all_uppercase_words(text):  # stored in memory, not executed yet
def check_all_uppercase_words(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_all_uppercase_words(...))  # check_all_uppercase_words() runs with text='world'
#   → return all((word[0].isupper() for word in words if word))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_uppercase_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_uppercase_words(...))  # check_all_uppercase_words() runs first → value goes to print()

"""

"""
Problem 1935:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_with_length_range(strings, min_length, max_length):  # defines a function with parameters strings, min_length, max_length
    return [s for s in strings if min_length <= len(s) <= max_length]  # return sends the result back to the caller
#
# Operators: <=
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_with_length_range(?, 3, 4)  # arguments evaluated first, then function body runs
get_strings_with_length_range(strings, min_length, max_length)  # arguments evaluated first, then function body runs
print(get_strings_with_length_range(?, 25, 25))  # get_strings_with_length_range() runs first, then print() outputs the result
min_length <= len(...)  # evaluated as True or False → picks which branch
min_length <= max_length  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_with_length_range(strings, min_length, max_length):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_with_length_range(strings, min_length, max_length):  # stored in memory, not executed yet
def get_strings_with_length_range(strings, min_length, max_length):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
min_length = ...  # assigns a value to the variable
max_length = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_with_length_range(...))  # get_strings_with_length_range() runs with strings=[4, 1, 8, 6, 3], min_length=25, max_length=25
#   → return [s for s in strings if min_length <= len(s) <= max_length]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_with_length_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → min_length = ...  # stored in memory
#   → max_length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_with_length_range(...))  # get_strings_with_length_range() runs first → value goes to print()

"""

"""
Problem 1936:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_median(numbers):  # defines a function with parameters numbers
    sorted_nums = sorted(numbers)
    n = len(sorted_nums)
    if n % 2 == 0:
        return (sorted_nums[n // 2 - 1] + sorted_nums[n // 2]) / 2
    else:
        return sorted_nums[n // 2]
#
# Keywords: if/else
# Operators: % + - / // ==
# Built-in functions: len, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_median(?)  # arguments evaluated first, then function body runs
calculate_median(numbers)  # arguments evaluated first, then function body runs
? == 0  # evaluated as True or False → picks which branch
? / 2  # ? → then 2 → then /
n % 2  # n → then 2 → then %
? + ?  # ? → then ? → then +
n // 2  # n → then 2 → then //
? - 1  # ? → then 1 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_median(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_median(numbers):  # stored in memory, not executed yet
def calculate_median(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_median(...))  # calculate_median() runs with numbers=[4, 1, 8, 6, 3]
#   → return (sorted_nums[n // 2 - 1] + sorted_nums[n // 2]) / 2
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_median is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_median(...))  # calculate_median() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1937:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_mode(my_list):  # defines a function with parameters my_list
    counts = {}
    for item in my_list:
        counts[item] = counts.get(item, 0) + 1
    return max(counts, key=counts.get)  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_mode(?)  # arguments evaluated first, then function body runs
get_mode(my_list)  # arguments evaluated first, then function body runs
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_mode(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_mode(my_list):  # stored in memory, not executed yet
def get_mode(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_mode(...))  # get_mode() runs with my_list=[4, 1, 8, 6, 3]
#   → return max(counts, key=counts.get)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_mode is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_mode(...))  # get_mode() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1939:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_numbers_divisible_by_all(numbers, divisors):  # defines a function with parameters numbers, divisors
    return [n for n in numbers if all((n % d == 0 for d in divisors))]  # return sends the result back to the caller
#
# Operators: % ==
# Built-in functions: all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_numbers_divisible_by_all(?, ?)  # arguments evaluated first, then function body runs
get_numbers_divisible_by_all(numbers, divisors)  # arguments evaluated first, then function body runs
? == 0  # evaluated as True or False → picks which branch
n % d  # n → then d → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_numbers_divisible_by_all(numbers, divisors):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_numbers_divisible_by_all(numbers, divisors):  # stored in memory, not executed yet
def get_numbers_divisible_by_all(numbers, divisors):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
divisors = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_numbers_divisible_by_all(...))  # get_numbers_divisible_by_all() runs with numbers=[4, 1, 8, 6, 3], divisors=[4, 1, 8, 6, 3]
#   → return [n for n in numbers if all((n % d == 0 for d in divisors))]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_divisible_by_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → divisors = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_divisible_by_all(...))  # get_numbers_divisible_by_all() runs first → value goes to print()

"""

"""
Problem 1941:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_with_vowel_count(text, n):  # defines a function with parameters text, n
    vowels = 'aeiouAEIOU'
    words = text.split()
    return [word for word in words if sum((1 for c in word if c in vowels)) == n]  # return sends the result back to the caller
#
# Operators: == in
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_with_vowel_count('hello world python programming', 2)  # arguments evaluated first, then function body runs
get_words_with_vowel_count(text, n)  # arguments evaluated first, then function body runs
print(get_words_with_vowel_count('world', 15))  # get_words_with_vowel_count() runs first, then print() outputs the result
sum(...) == n  # evaluated as True or False → picks which branch
c in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_with_vowel_count(text, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_with_vowel_count(text, n):  # stored in memory, not executed yet
def get_words_with_vowel_count(text, n):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_with_vowel_count(...))  # get_words_with_vowel_count() runs with text='world', n=15
#   → return [word for word in words if sum((1 for c in word if c in vowels)) == n]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_vowel_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_vowel_count(...))  # get_words_with_vowel_count() runs first → value goes to print()

"""

"""
Problem 1942:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_table_row(value1, value2, value3):  # defines a function with parameters value1, value2, value3
    return f'{value1} | {value2} | {value3}'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_table_row('Alice', 30, 'Engineer')  # arguments evaluated first, then function body runs
format_table_row(value1, value2, value3)  # arguments evaluated first, then function body runs
print(format_table_row(25, 25, 25))  # format_table_row() runs first, then print() outputs the result
'%s | %s | %s' % ?  # '%s | %s | %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_table_row(value1, value2, value3):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_table_row(value1, value2, value3):  # stored in memory, not executed yet
def format_table_row(value1, value2, value3):  # stored in memory, not executed yet
value1 = ...  # assigns a value to the variable
value2 = ...  # assigns a value to the variable
value3 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_table_row(...))  # format_table_row() runs with value1=25, value2=25, value3=25
#   → return '%s | %s | %s' % (value1, value2, value3)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_table_row is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → value1 = ...  # stored in memory
#   → value2 = ...  # stored in memory
#   → value3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_table_row(...))  # format_table_row() runs first → value goes to print()

"""

"""
Problem 1943:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_not_in_both(list1, list2):  # defines a function with parameters list1, list2
    result = []
    for item in list1:
        if item not in list2:
            result.append(item)
    for item in list2:
        if item not in list1:
            result.append(item)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_not_in_both(?, ?)  # arguments evaluated first, then function body runs
get_elements_not_in_both(list1, list2)  # arguments evaluated first, then function body runs
item not in list2  # evaluated as True or False → picks which branch
item not in list1  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_not_in_both(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(sorted(...))
def get_elements_not_in_both(list1, list2):  # stored in memory, not executed yet
def get_elements_not_in_both(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_not_in_both(...))  # get_elements_not_in_both() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return list(map(lambda item: item, list1))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_not_in_both is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_not_in_both(...))  # get_elements_not_in_both() runs first → value goes to print()
#
# Loop: takes one item from list1, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1944:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_weighted_average(values, weights):  # defines a function with parameters values, weights
    weighted_sum = sum((v * w for v, w in zip(values, weights)))
    total_weight = sum(weights)
    return weighted_sum / total_weight  # return sends the result back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float, sum, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_weighted_average(?, ?)  # arguments evaluated first, then function body runs
calculate_weighted_average(values, weights)  # arguments evaluated first, then function body runs
weighted_sum / total_weight  # weighted_sum → then total_weight → then /
weighted_sum * ?  # weighted_sum → then ? → then *
1 / total_weight  # 1 → then total_weight → then /
v * w  # v → then w → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_weighted_average(values, weights):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_weighted_average(values, weights):  # stored in memory, not executed yet
def calculate_weighted_average(values, weights):  # stored in memory, not executed yet
values = ...  # assigns a value to the variable
weights = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_weighted_average(...))  # calculate_weighted_average() runs with values=[4, 1, 8, 6, 3], weights=[4, 1, 8, 6, 3]
#   → from fractions import Fraction
#   → return float(Fraction(weighted_sum, total_weight))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_weighted_average is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → values = ...  # stored in memory
#   → weights = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_weighted_average(...))  # calculate_weighted_average() runs first → value goes to print()

"""

"""
Problem 1945:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_sorted_by_length(strings):  # defines a function with parameters strings
    return sorted(strings, key=len)  # return sends the result back to the caller
#
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_sorted_by_length(?)  # arguments evaluated first, then function body runs
get_strings_sorted_by_length(strings)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_sorted_by_length(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_sorted_by_length(strings):  # stored in memory, not executed yet
def get_strings_sorted_by_length(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_sorted_by_length(...))  # get_strings_sorted_by_length() runs with strings=[4, 1, 8, 6, 3]
#   → return sorted(strings, key=len)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_sorted_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_sorted_by_length(...))  # get_strings_sorted_by_length() runs first → value goes to print()

"""

"""
Problem 1946:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_percentage_list(numbers):  # defines a function with parameters numbers
    return [f'{n}%' for n in numbers]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_percentage_list(?)  # arguments evaluated first, then function body runs
format_percentage_list(numbers)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_percentage_list(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_percentage_list(numbers):  # stored in memory, not executed yet
def format_percentage_list(numbers):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_percentage_list(...))  # format_percentage_list() runs with numbers=[4, 1, 8, 6, 3]
#   → return [f'{n}%' for n in numbers]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_percentage_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_percentage_list(...))  # format_percentage_list() runs first → value goes to print()

"""

"""
Problem 1947:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_pairs_summing_to(numbers, target):  # defines a function with parameters numbers, target
    pairs = []
    for i in range(len(numbers)):
        for j in range(i + 1, len(numbers)):
            if numbers[i] + numbers[j] == target:
                pairs.append((numbers[i], numbers[j]))
    return pairs  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_pairs_summing_to(?, 7)  # arguments evaluated first, then function body runs
get_pairs_summing_to(numbers, target)  # arguments evaluated first, then function body runs
print(get_pairs_summing_to(?, 'goal'))  # get_pairs_summing_to() runs first, then print() outputs the result
i + 1  # i → then 1 → then +
? == target  # evaluated as True or False → picks which branch
? + ?  # ? → then ? → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_pairs_summing_to(numbers, target):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_pairs_summing_to(numbers, target):  # stored in memory, not executed yet
def get_pairs_summing_to(numbers, target):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
target = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_pairs_summing_to(...))  # get_pairs_summing_to() runs with numbers=[4, 1, 8, 6, 3], target='goal'
#   → return pairs
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_pairs_summing_to is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → target = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_pairs_summing_to(...))  # get_pairs_summing_to() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1948:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_character_types(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    return {'vowels': sum((1 for c in text if c in vowels)), 'consonants': sum((1 for c in text if c.isalpha() and c not in vowels)), 'digits': sum((1 for c in text if c.isdigit())), 'spaces': sum((1 for c in text if c.isspace()))}  # return sends the result back to the caller
#
# Operators: in and not in
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_character_types('Hello World 123')  # arguments evaluated first, then function body runs
count_character_types(text)  # arguments evaluated first, then function body runs
print(count_character_types('world'))  # count_character_types() runs first, then print() outputs the result
c in vowels  # evaluated as True or False → picks which branch
c not in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_character_types(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_character_types(...))  # count_character_types() runs with text='world'
#   → vowels = 'aeiouAEIOU'
#   → return {'vowels': sum((1 for c in text if c in vowels)), 'consonants': sum((1 for c in text if c.isalpha() and c not in vowels)), 'digits': sum((1 for c in text if c.isdigit())), 'spaces': sum((1 for c in text if c.isspace()))}
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — count_character_types, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_character_types(...))  # count_character_types() runs first → value goes to print()

"""

"""
Problem 1949:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_by_vowel_count(text):  # defines a function with parameters text
    vowels = 'aeiouAEIOU'
    words = text.split()
    result = {}
    for word in words:
        count = sum((1 for c in word if c in vowels))
        if count not in result:
            result[count] = []
        result[count].append(word)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: in not in
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_by_vowel_count('hello world python')  # arguments evaluated first, then function body runs
get_words_by_vowel_count(text)  # arguments evaluated first, then function body runs
print(get_words_by_vowel_count('world'))  # get_words_by_vowel_count() runs first, then print() outputs the result
count not in result  # evaluated as True or False → picks which branch
c in vowels  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_by_vowel_count(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_by_vowel_count(text):  # stored in memory, not executed yet
def get_words_by_vowel_count(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_by_vowel_count(...))  # get_words_by_vowel_count() runs with text='world'
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_by_vowel_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_by_vowel_count(...))  # get_words_by_vowel_count() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1950:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_name_with_title(first, last, title):  # defines a function with parameters first, last, title
    return f'{title}. {first} {last}'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_name_with_title('John', 'Doe', 'Dr')  # arguments evaluated first, then function body runs
format_name_with_title(first, last, title)  # arguments evaluated first, then function body runs
print(format_name_with_title(25, 25, 25))  # format_name_with_title() runs first, then print() outputs the result
'%s. %s %s' % ?  # '%s. %s %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_name_with_title(first, last, title):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_name_with_title(first, last, title):  # stored in memory, not executed yet
def format_name_with_title(first, last, title):  # stored in memory, not executed yet
first = ...  # assigns a value to the variable
last = ...  # assigns a value to the variable
title = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_name_with_title(...))  # format_name_with_title() runs with first=25, last=25, title=25
#   → return '%s. %s %s' % (title, first, last)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_name_with_title is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → first = ...  # stored in memory
#   → last = ...  # stored in memory
#   → title = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_name_with_title(...))  # format_name_with_title() runs first → value goes to print()

"""

"""
Problem 1951:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_area_circle_sector(radius, angle_degrees):  # defines a function with parameters radius, angle_degrees
    return angle_degrees / 360 * math.pi * radius ** 2  # return sends the result back to the caller
#
# Operators: * / **
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_area_circle_sector(5, 90)  # arguments evaluated first, then function body runs
calculate_area_circle_sector(radius, angle_degrees)  # arguments evaluated first, then function body runs
print(calculate_area_circle_sector(14, 25))  # calculate_area_circle_sector() runs first, then print() outputs the result
? * ?  # ? → then ? → then *
? * math.pi  # ? → then math.pi → then *
radius ** 2  # radius → then 2 → then **
angle_degrees / 360  # angle_degrees → then 360 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import math
def calculate_area_circle_sector(radius, angle_degrees):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(round(...))
def calculate_area_circle_sector(radius, angle_degrees):  # stored in memory, not executed yet
def calculate_area_circle_sector(radius, angle_degrees):  # stored in memory, not executed yet
radius = ...  # assigns a value to the variable
angle_degrees = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_area_circle_sector(...))  # calculate_area_circle_sector() runs with radius=14, angle_degrees=25
#   → return angle_degrees / 360 * math.pi * radius ** 2
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_area_circle_sector is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → radius = ...  # stored in memory
#   → angle_degrees = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_area_circle_sector(...))  # calculate_area_circle_sector() runs first → value goes to print()

"""

"""
Problem 1952:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_at_multiple_indices(my_list, indices):  # defines a function with parameters my_list, indices
    return {i: my_list[i] for i in indices if i < len(my_list)}  # return sends the result back to the caller
#
# Operators: <
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_at_multiple_indices(?, ?)  # arguments evaluated first, then function body runs
get_elements_at_multiple_indices(my_list, indices)  # arguments evaluated first, then function body runs
i < len(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_at_multiple_indices(my_list, indices):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_at_multiple_indices(my_list, indices):  # stored in memory, not executed yet
def get_elements_at_multiple_indices(my_list, indices):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
indices = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_at_multiple_indices(...))  # get_elements_at_multiple_indices() runs with my_list=[4, 1, 8, 6, 3], indices=[4, 1, 8, 6, 3]
#   → return {i: my_list[i] for i in indices if i < len(my_list)}
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_at_multiple_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → indices = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_at_multiple_indices(...))  # get_elements_at_multiple_indices() runs first → value goes to print()

"""

"""
Problem 1953:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_string_properties(text):  # defines a function with parameters text
    return {'has_uppercase': any((c.isupper() for c in text)), 'has_lowercase': any((c.islower() for c in text)), 'has_digit': any((c.isdigit() for c in text)), 'has_special': any((not c.isalnum() and (not c.isspace()) for c in text))}  # return sends the result back to the caller
#
# Operators: and
# Built-in functions: any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
check_string_properties('Hello123!')  # arguments evaluated first, then function body runs
check_string_properties(text)  # arguments evaluated first, then function body runs
print(check_string_properties('world'))  # check_string_properties() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_string_properties(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def solve():  # stored in memory, not executed yet
def solve():  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_string_properties(...))  # check_string_properties() runs with text='world'
#   → return {'has_uppercase': any((c.isupper() for c in text)), 'has_lowercase': any((c.islower() for c in text)), 'has_digit': any((c.isdigit() for c in text)), 'has_special': any((not c.isalnum() and (not c.isspace()) for c in text))}
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — check_string_properties, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_string_properties(...))  # check_string_properties() runs first → value goes to print()

"""

"""
Problem 1954:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_numbers_summing_to_range(numbers, min_sum, max_sum):  # defines a function with parameters numbers, min_sum, max_sum
    pairs = []
    for i in range(len(numbers)):
        for j in range(i + 1, len(numbers)):
            total = numbers[i] + numbers[j]
            if min_sum <= total <= max_sum:
                pairs.append((numbers[i], numbers[j]))
    return pairs  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: + <=
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_numbers_summing_to_range(?, 5, 7)  # arguments evaluated first, then function body runs
get_numbers_summing_to_range(numbers, min_sum, max_sum)  # arguments evaluated first, then function body runs
print(get_numbers_summing_to_range(?, 25, 25))  # get_numbers_summing_to_range() runs first, then print() outputs the result
i + 1  # i → then 1 → then +
? + ?  # ? → then ? → then +
min_sum <= total  # evaluated as True or False → picks which branch
min_sum <= max_sum  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_numbers_summing_to_range(numbers, min_sum, max_sum):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_numbers_summing_to_range(numbers, min_sum, max_sum):  # stored in memory, not executed yet
def get_numbers_summing_to_range(numbers, min_sum, max_sum):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
min_sum = ...  # assigns a value to the variable
max_sum = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_numbers_summing_to_range(...))  # get_numbers_summing_to_range() runs with numbers=[4, 1, 8, 6, 3], min_sum=25, max_sum=25
#   → return pairs
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_summing_to_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → min_sum = ...  # stored in memory
#   → max_sum = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_summing_to_range(...))  # get_numbers_summing_to_range() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1955:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_phone_with_country(country_code, area_code, number):  # defines a function with parameters country_code, area_code, number
    return f'+{country_code} ({area_code}) {number}'  # return sends the result back to the caller
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_phone_with_country('1', '555', '1234567')  # arguments evaluated first, then function body runs
format_phone_with_country(country_code, area_code, number)  # arguments evaluated first, then function body runs
print(format_phone_with_country(25, 25, 25))  # format_phone_with_country() runs first, then print() outputs the result
'+%s (%s) %s' % ?  # '+%s (%s) %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_phone_with_country(country_code, area_code, number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_phone_with_country(country_code, area_code, number):  # stored in memory, not executed yet
def format_phone_with_country(country_code, area_code, number):  # stored in memory, not executed yet
country_code = ...  # assigns a value to the variable
area_code = ...  # assigns a value to the variable
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_phone_with_country(...))  # format_phone_with_country() runs with country_code=25, area_code=25, number=25
#   → return '+%s (%s) %s' % (country_code, area_code, number)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_phone_with_country is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → country_code = ...  # stored in memory
#   → area_code = ...  # stored in memory
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_phone_with_country(...))  # format_phone_with_country() runs first → value goes to print()

"""

"""
Problem 1956:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_consecutive_elements(my_list, n):  # defines a function with parameters my_list, n
    return [my_list[i:i + n] for i in range(len(my_list) - n + 1)]  # return sends the result back to the caller
#
# Operators: + -
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_consecutive_elements(?, 3)  # arguments evaluated first, then function body runs
get_consecutive_elements(my_list, n)  # arguments evaluated first, then function body runs
print(get_consecutive_elements(?, 15))  # get_consecutive_elements() runs first, then print() outputs the result
i + n  # i → then n → then +
? + 1  # ? → then 1 → then +
len(...) - n  # len(...) → then n → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_consecutive_elements(my_list, n):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_consecutive_elements(my_list, n):  # stored in memory, not executed yet
def get_consecutive_elements(my_list, n):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
n = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_consecutive_elements(...))  # get_consecutive_elements() runs with my_list=[4, 1, 8, 6, 3], n=15
#   → return [my_list[i:i + n] for i in range(len(my_list) - n + 1)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_consecutive_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_consecutive_elements(...))  # get_consecutive_elements() runs first → value goes to print()

"""

"""
Problem 1957:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_gcd(a, b):  # defines a function with parameters a, b
    while b:
        a, b = (b, a % b)
    return a  # return sends the result back to the caller
#
# Keywords: while
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_gcd(48, 18)  # arguments evaluated first, then function body runs
calculate_gcd(a, b)  # arguments evaluated first, then function body runs
print(calculate_gcd(20, 30))  # calculate_gcd() runs first, then print() outputs the result
a % b  # a → then b → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_gcd(a, b):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_gcd(a, b):  # stored in memory, not executed yet
def calculate_gcd(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_gcd(...))  # calculate_gcd() runs with a=20, b=30
#   → return a
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_gcd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_gcd(...))  # calculate_gcd() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1958:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_lcm(a, b):  # defines a function with parameters a, b
    def gcd(x, y):
        while y:
            x, y = (y, x % y)
        return x
    return abs(a * b) // gcd(a, b)  # return sends the result back to the caller
#
# Keywords: while
# Operators: % * //
# Built-in functions: abs, gcd()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_lcm(12, 18)  # arguments evaluated first, then function body runs
calculate_lcm(a, b)  # arguments evaluated first, then function body runs
print(calculate_lcm(20, 30))  # calculate_lcm() runs first, then print() outputs the result
abs(...) // gcd(...)  # abs(...) → then gcd(...) → then //
a * b  # a → then b → then *
x % y  # x → then y → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_lcm(a, b):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_lcm(a, b):  # stored in memory, not executed yet
def calculate_lcm(a, b):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_lcm(...))  # calculate_lcm() runs with a=20, b=30
#   → return x
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_lcm is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_lcm(...))  # calculate_lcm() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again

"""

"""
Problem 1959:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_frequency_dict(my_list):  # defines a function with parameters my_list
    counts = {}
    for item in my_list:
        counts[item] = counts.get(item, 0) + 1
    return dict(sorted(counts.items(), key=lambda x: x[1], reverse=True))  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: dict, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_frequency_dict(?)  # arguments evaluated first, then function body runs
get_frequency_dict(my_list)  # arguments evaluated first, then function body runs
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_frequency_dict(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_frequency_dict(my_list):  # stored in memory, not executed yet
def get_frequency_dict(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_frequency_dict(...))  # get_frequency_dict() runs with my_list=[4, 1, 8, 6, 3]
#   → return dict(sorted(counts.items(), key=lambda x: x[1], reverse=True))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_frequency_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_frequency_dict(...))  # get_frequency_dict() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done

"""

"""
Problem 1960:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_number_with_commas(number):  # defines a function with parameters number
    return f'{number:,}'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_number_with_commas(1234567)  # arguments evaluated first, then function body runs
format_number_with_commas(number)  # arguments evaluated first, then function body runs
print(format_number_with_commas(25))  # format_number_with_commas() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_number_with_commas(number):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_number_with_commas(number):  # stored in memory, not executed yet
def format_number_with_commas(number):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_number_with_commas(...))  # format_number_with_commas() runs with number=25
#   → return f'{number:,}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_number_with_commas is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_number_with_commas(...))  # format_number_with_commas() runs first → value goes to print()

"""

"""
Problem 1961:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_with_same_length(strings):  # defines a function with parameters strings
    result = {}
    for s in strings:
        length = len(s)
        if length not in result:
            result[length] = []
        result[length].append(s)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_with_same_length(?)  # arguments evaluated first, then function body runs
get_words_with_same_length(strings)  # arguments evaluated first, then function body runs
length not in result  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_with_same_length(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_with_same_length(strings):  # stored in memory, not executed yet
def get_words_with_same_length(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_with_same_length(...))  # get_words_with_same_length() runs with strings=[4, 1, 8, 6, 3]
#   → return result
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_same_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_same_length(...))  # get_words_with_same_length() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1962:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def check_anagram_pair(str1, str2):  # defines a function with parameters str1, str2
    return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))  # return sends the result back to the caller
#
# Operators: ==
# Built-in functions: sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(check_anagram_pair('listen', 'silent'))  # check_anagram_pair() runs first, then print() outputs the result
print(check_anagram_pair('hello', 'world'))  # check_anagram_pair() runs first, then print() outputs the result
check_anagram_pair(str1, str2)  # arguments evaluated first, then function body runs
print(check_anagram_pair('world', 'python'))  # check_anagram_pair() runs first, then print() outputs the result
sorted(...) == sorted(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def check_anagram_pair(str1, str2):  # stored in memory, not executed yet
print(check_anagram_pair(...), check_anagram_pair(...))  # check_anagram_pair() runs with str1='listen', str2='silent'
#   → return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))
def check_anagram_pair(str1, str2):  # stored in memory, not executed yet
def check_anagram_pair(str1, str2):  # stored in memory, not executed yet
str1 = ...  # assigns a value to the variable
str2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(check_anagram_pair(...))  # check_anagram_pair() runs with str1='world', str2='python'
#   → return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_anagram_pair is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_anagram_pair(...), check_anagram_pair(...))  # check_anagram_pair() runs first → value goes to print()
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_anagram_pair(...))  # check_anagram_pair() runs first → value goes to print()

"""

"""
Problem 1963:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_sublist_sum(my_list, start, end):  # defines a function with parameters my_list, start, end
    return sum(my_list[start:end])  # return sends the result back to the caller
#
# Built-in functions: sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_sublist_sum(?, 1, 4)  # arguments evaluated first, then function body runs
get_sublist_sum(my_list, start, end)  # arguments evaluated first, then function body runs
print(get_sublist_sum(?, 3, 20))  # get_sublist_sum() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_sublist_sum(my_list, start, end):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_sublist_sum(my_list, start, end):  # stored in memory, not executed yet
def get_sublist_sum(my_list, start, end):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
start = ...  # assigns a value to the variable
end = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_sublist_sum(...))  # get_sublist_sum() runs with my_list=[4, 1, 8, 6, 3], start=3, end=20
#   → return sum(my_list[start:end])
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_sublist_sum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_sublist_sum(...))  # get_sublist_sum() runs first → value goes to print()

"""

"""
Problem 1964:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_time_12hour(hours_24):  # defines a function with parameters hours_24
    if hours_24 == 0:
        return '12 AM'
    elif hours_24 < 12:
        return f'{hours_24} AM'
    elif hours_24 == 12:
        return '12 PM'
    else:
        return f'{hours_24 - 12} PM'
#
# Keywords: if/else
# Operators: - < ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
print(format_time_12hour(0))  # format_time_12hour() runs first, then print() outputs the result
print(format_time_12hour(14))  # format_time_12hour() runs first, then print() outputs the result
print(format_time_12hour(23))  # format_time_12hour() runs first, then print() outputs the result
format_time_12hour(hours_24)  # arguments evaluated first, then function body runs
print(format_time_12hour(25))  # format_time_12hour() runs first, then print() outputs the result
hours_24 == 0  # evaluated as True or False → picks which branch
hours_24 < 12  # evaluated as True or False → picks which branch
hours_24 == 12  # evaluated as True or False → picks which branch
hours_24 - 12  # hours_24 → then 12 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_time_12hour(hours_24):  # stored in memory, not executed yet
print(format_time_12hour(...), format_time_12hour(...), format_time_12hour(...))  # format_time_12hour() runs with hours_24=0
#   → return '12 AM'
def format_time_12hour(hours_24):  # stored in memory, not executed yet
def format_time_12hour(hours_24):  # stored in memory, not executed yet
hours_24 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_time_12hour(...))  # format_time_12hour() runs with hours_24=25
#   → return '12 AM'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_time_12hour is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(format_time_12hour(...), format_time_12hour(...), format_time_12hour(...))  # format_time_12hour() runs first → value goes to print()
#   → hours_24 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_time_12hour(...))  # format_time_12hour() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1965:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_between_values(my_list, min_val, max_val):  # defines a function with parameters my_list, min_val, max_val
    return [x for x in my_list if min_val < x < max_val]  # return sends the result back to the caller
#
# Operators: <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_between_values(?, 15, 45)  # arguments evaluated first, then function body runs
get_elements_between_values(my_list, min_val, max_val)  # arguments evaluated first, then function body runs
print(get_elements_between_values(?, 25, 25))  # get_elements_between_values() runs first, then print() outputs the result
min_val < x  # evaluated as True or False → picks which branch
min_val < max_val  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_between_values(my_list, min_val, max_val):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_between_values(my_list, min_val, max_val):  # stored in memory, not executed yet
def get_elements_between_values(my_list, min_val, max_val):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
min_val = ...  # assigns a value to the variable
max_val = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_between_values(...))  # get_elements_between_values() runs with my_list=[4, 1, 8, 6, 3], min_val=25, max_val=25
#   → return [x for x in my_list if min_val < x < max_val]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_between_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_between_values(...))  # get_elements_between_values() runs first → value goes to print()

"""

"""
Problem 1966:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_substring_occurrences(text, substring):  # defines a function with parameters text, substring
    count = 0
    for i in range(len(text) - len(substring) + 1):
        if text[i:i + len(substring)] == substring:
            count += 1
    return count  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: + - ==
# Built-in functions: len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_substring_occurrences('ababab', 'aba')  # arguments evaluated first, then function body runs
count_substring_occurrences(text, substring)  # arguments evaluated first, then function body runs
print(count_substring_occurrences('world', 'world'))  # count_substring_occurrences() runs first, then print() outputs the result
? + 1  # ? → then 1 → then +
? == substring  # evaluated as True or False → picks which branch
len(...) - len(...)  # len(...) → then len(...) → then -
i + len(...)  # i → then len(...) → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_substring_occurrences(text, substring):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_substring_occurrences(text, substring):  # stored in memory, not executed yet
def count_substring_occurrences(text, substring):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
substring = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_substring_occurrences(...))  # count_substring_occurrences() runs with text='world', substring='world'
#   → return count
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_substring_occurrences is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → substring = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_substring_occurrences(...))  # count_substring_occurrences() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1967:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_list_as_string(my_list, separator):  # defines a function with parameters my_list, separator
    return f'[{separator.join((str(x) for x in my_list))}]'  # return sends the result back to the caller
#
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_list_as_string(?, ', ')  # arguments evaluated first, then function body runs
format_list_as_string(my_list, separator)  # arguments evaluated first, then function body runs
print(format_list_as_string(?, 'world'))  # format_list_as_string() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_list_as_string(my_list, separator):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_list_as_string(my_list, separator):  # stored in memory, not executed yet
def format_list_as_string(my_list, separator):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
separator = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_list_as_string(...))  # format_list_as_string() runs with my_list=[4, 1, 8, 6, 3], separator='world'
#   → return f'[{separator.join((str(x) for x in my_list))}]'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_list_as_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → separator = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_list_as_string(...))  # format_list_as_string() runs first → value goes to print()

"""

"""
Problem 1968:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_unique_combinations(list1, list2):  # defines a function with parameters list1, list2
    return [(a, b) for a in list1 for b in list2]  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_unique_combinations(?, ?)  # arguments evaluated first, then function body runs
get_unique_combinations(list1, list2)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_unique_combinations(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_unique_combinations(list1, list2):  # stored in memory, not executed yet
def get_unique_combinations(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_unique_combinations(...))  # get_unique_combinations() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return [(a, b) for a in list1 for b in list2]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_combinations is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_combinations(...))  # get_unique_combinations() runs first → value goes to print()

"""

"""
Problem 1969:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_quadratic_roots(a, b, c):  # defines a function with parameters a, b, c
    discriminant = b ** 2 - 4 * a * c
    if discriminant < 0:
        return None
    root1 = (-b + math.sqrt(discriminant)) / (2 * a)
    root2 = (-b - math.sqrt(discriminant)) / (2 * a)
    return (root1, root2)  # return sends the result back to the caller
#
# Keywords: if/else
# Operators: * + - / < **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_quadratic_roots(1, ?, 6)  # arguments evaluated first, then function body runs
calculate_quadratic_roots(a, b, c)  # arguments evaluated first, then function body runs
print(calculate_quadratic_roots(20, 30, 10))  # calculate_quadratic_roots() runs first, then print() outputs the result
? - ?  # ? → then ? → then -
discriminant < 0  # evaluated as True or False → picks which branch
? / ?  # ? → then ? → then /
b ** 2  # b → then 2 → then **
? * c  # ? → then c → then *
? + math.sqrt(...)  # ? → then math.sqrt(...) → then +
2 * a  # 2 → then a → then *
? - math.sqrt(...)  # ? → then math.sqrt(...) → then -
4 * a  # 4 → then a → then *
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import math
def calculate_quadratic_roots(a, b, c):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_quadratic_roots(a, b, c):  # stored in memory, not executed yet
def calculate_quadratic_roots(a, b, c):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
c = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_quadratic_roots(...))  # calculate_quadratic_roots() runs with a=20, b=30, c=10
#   → return None
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_quadratic_roots is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_quadratic_roots(...))  # calculate_quadratic_roots() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1970:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_matching_pattern(strings, pattern):  # defines a function with parameters strings, pattern
    result = []
    for s in strings:
        if s.count(pattern) >= 2:
            result.append(s)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: >=
# Built-in functions: list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_matching_pattern(?, 'l')  # arguments evaluated first, then function body runs
get_strings_matching_pattern(strings, pattern)  # arguments evaluated first, then function body runs
print(get_strings_matching_pattern(?, 25))  # get_strings_matching_pattern() runs first, then print() outputs the result
s.count(...) >= 2  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_matching_pattern(strings, pattern):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_matching_pattern(strings, pattern):  # stored in memory, not executed yet
def get_strings_matching_pattern(strings, pattern):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
pattern = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_matching_pattern(...))  # get_strings_matching_pattern() runs with strings=[4, 1, 8, 6, 3], pattern=25
#   → return list(map(lambda s: s, strings))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_matching_pattern is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_matching_pattern(...))  # get_strings_matching_pattern() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1971:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_name_initials(first, middle, last):  # defines a function with parameters first, middle, last
    return f'{first[0]}.{middle[0]}.{last[0]}.'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_name_initials('John', 'Michael', 'Doe')  # arguments evaluated first, then function body runs
format_name_initials(first, middle, last)  # arguments evaluated first, then function body runs
print(format_name_initials(?, ?, ?))  # format_name_initials() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_name_initials(first, middle, last):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_name_initials(first, middle, last):  # stored in memory, not executed yet
def format_name_initials(first, middle, last):  # stored in memory, not executed yet
first = ...  # assigns a value to the variable
middle = ...  # assigns a value to the variable
last = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_name_initials(...))  # format_name_initials() runs with first=[4, 1, 8, 6, 3], middle=[4, 1, 8, 6, 3], last=[4, 1, 8, 6, 3]
#   → return f'{first[0]}.{middle[0]}.{last[0]}.'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_name_initials is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → first = ...  # stored in memory
#   → middle = ...  # stored in memory
#   → last = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_name_initials(...))  # format_name_initials() runs first → value goes to print()

"""

"""
Problem 1972:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_with_condition(my_list, condition_func):  # defines a function with parameters my_list, condition_func
    return [x for x in my_list if condition_func(x)]  # return sends the result back to the caller
#
# Operators: >
# Built-in functions: condition_func()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_with_condition(?, ?)  # arguments evaluated first, then function body runs
get_elements_with_condition(my_list, condition_func)  # arguments evaluated first, then function body runs
print(get_elements_with_condition(?, 25))  # get_elements_with_condition() runs first, then print() outputs the result
x > 3  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_with_condition(my_list, condition_func):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_with_condition(my_list, condition_func):  # stored in memory, not executed yet
def get_elements_with_condition(my_list, condition_func):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
condition_func = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_with_condition(...))  # get_elements_with_condition() runs with my_list=[4, 1, 8, 6, 3], condition_func=25
#   → return [x for x in my_list if condition_func(x)]
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_with_condition is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → condition_func = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_with_condition(...))  # get_elements_with_condition() runs first → value goes to print()

"""

"""
Problem 1973:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_total_area(shapes):  # defines a function with parameters shapes
    total = 0
    for shape_type, dimensions in shapes:
        if shape_type == 'rectangle':
            total += dimensions[0] * dimensions[1]
        elif shape_type == 'circle':
            total += math.pi * dimensions[0] ** 2
    return total  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: * ** ==
# Built-in functions: round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_total_area(?)  # arguments evaluated first, then function body runs
calculate_total_area(shapes)  # arguments evaluated first, then function body runs
print(calculate_total_area(25))  # calculate_total_area() runs first, then print() outputs the result
shape_type == 'rectangle'  # evaluated as True or False → picks which branch
? * ?  # ? → then ? → then *
shape_type == 'circle'  # evaluated as True or False → picks which branch
math.pi * ?  # math.pi → then ? → then *
? ** 2  # ? → then 2 → then **
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import math
def calculate_total_area(shapes):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(round(...))
def calculate_total_area(shapes):  # stored in memory, not executed yet
def calculate_total_area(shapes):  # stored in memory, not executed yet
shapes = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_total_area(...))  # calculate_total_area() runs with shapes=25
#   → return total
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_total_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → shapes = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_total_area(...))  # calculate_total_area() runs first → value goes to print()
#
# Loop: takes one item from shapes, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1974:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_words_with_repeated_chars(text):  # defines a function with parameters text
    words = text.split()
    result = []
    for word in words:
        if len(word) != len(set(word)):
            result.append(word)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: !=
# Built-in functions: len, list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_words_with_repeated_chars('hello world python programming')  # arguments evaluated first, then function body runs
get_words_with_repeated_chars(text)  # arguments evaluated first, then function body runs
print(get_words_with_repeated_chars('world'))  # get_words_with_repeated_chars() runs first, then print() outputs the result
len(...) != len(...)  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_words_with_repeated_chars(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_words_with_repeated_chars(text):  # stored in memory, not executed yet
def get_words_with_repeated_chars(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_words_with_repeated_chars(...))  # get_words_with_repeated_chars() runs with text='world'
#   → return list(map(lambda word: word, words))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_repeated_chars is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_repeated_chars(...))  # get_words_with_repeated_chars() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1975:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_multi_value_string(name, scores):  # defines a function with parameters name, scores
    scores_str = ', '.join((str(s) for s in scores))
    return f'Name: {name}, Scores: {scores_str}'  # return sends the result back to the caller
#
# Operators: %
# Built-in functions: str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_multi_value_string('Alice', ?)  # arguments evaluated first, then function body runs
format_multi_value_string(name, scores)  # arguments evaluated first, then function body runs
print(format_multi_value_string('Bob', ?))  # format_multi_value_string() runs first, then print() outputs the result
'Name: %s, Scores: %s' % ?  # 'Name: %s, Scores: %s' → then ? → then %
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_multi_value_string(name, scores):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_multi_value_string(name, scores):  # stored in memory, not executed yet
def format_multi_value_string(name, scores):  # stored in memory, not executed yet
name = ...  # assigns a value to the variable
scores = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_multi_value_string(...))  # format_multi_value_string() runs with name='Bob', scores=[4, 1, 8, 6, 3]
#   → return 'Name: %s, Scores: %s' % (name, scores_str)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_multi_value_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → scores = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_multi_value_string(...))  # format_multi_value_string() runs first → value goes to print()

"""

"""
Problem 1976:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_symmetric_difference(list1, list2):  # defines a function with parameters list1, list2
    set1 = set(list1)
    set2 = set(list2)
    return list(set1.symmetric_difference(set2))  # return sends the result back to the caller
#
# Built-in functions: list, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_symmetric_difference(?, ?)  # arguments evaluated first, then function body runs
get_symmetric_difference(list1, list2)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_symmetric_difference(list1, list2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(sorted(...))
def get_symmetric_difference(list1, list2):  # stored in memory, not executed yet
def get_symmetric_difference(list1, list2):  # stored in memory, not executed yet
list1 = ...  # assigns a value to the variable
list2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_symmetric_difference(...))  # get_symmetric_difference() runs with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#   → return list(set1.symmetric_difference(set2))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_symmetric_difference is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_symmetric_difference(...))  # get_symmetric_difference() runs first → value goes to print()

"""

"""
Problem 1977:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def count_words_by_first_letter(text):  # defines a function with parameters text
    words = text.split()
    counts = {}
    for word in words:
        first_letter = word[0].lower()
        counts[first_letter] = counts.get(first_letter, 0) + 1
    return counts  # return sends the result back to the caller
#
# Keywords: for
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
count_words_by_first_letter('apple apricot banana cherry')  # arguments evaluated first, then function body runs
count_words_by_first_letter(text)  # arguments evaluated first, then function body runs
print(count_words_by_first_letter('world'))  # count_words_by_first_letter() runs first, then print() outputs the result
counts.get(...) + 1  # counts.get(...) → then 1 → then +
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def count_words_by_first_letter(text):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def count_words_by_first_letter(text):  # stored in memory, not executed yet
def count_words_by_first_letter(text):  # stored in memory, not executed yet
text = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(count_words_by_first_letter(...))  # count_words_by_first_letter() runs with text='world'
#   → return counts
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words_by_first_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words_by_first_letter(...))  # count_words_by_first_letter() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done

"""

"""
Problem 1978:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def format_decimal_places(number, decimal_places):  # defines a function with parameters number, decimal_places
    return f'{number:.{decimal_places}f}'  # return sends the result back to the caller
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
format_decimal_places(3.14159, 2)  # arguments evaluated first, then function body runs
format_decimal_places(number, decimal_places)  # arguments evaluated first, then function body runs
print(format_decimal_places(25, 25))  # format_decimal_places() runs first, then print() outputs the result
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def format_decimal_places(number, decimal_places):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def format_decimal_places(number, decimal_places):  # stored in memory, not executed yet
def format_decimal_places(number, decimal_places):  # stored in memory, not executed yet
number = ...  # assigns a value to the variable
decimal_places = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(format_decimal_places(...))  # format_decimal_places() runs with number=25, decimal_places=25
#   → return f'{number:.{decimal_places}f}'
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_decimal_places is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → decimal_places = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_decimal_places(...))  # format_decimal_places() runs first → value goes to print()

"""

"""
Problem 1979:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_satisfying_all(my_list):  # defines a function with parameters my_list
    result = []
    for item in my_list:
        if all((cond(item) for cond in conditions)):
            result.append(item)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: all, cond, list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_satisfying_all(?, ?, ?)  # arguments evaluated first, then function body runs
get_elements_satisfying_all(my_list, ?)  # arguments evaluated first, then function body runs
print(get_elements_satisfying_all(?, 25))  # get_elements_satisfying_all() runs first, then print() outputs the result
x > 2  # evaluated as True or False → picks which branch
x < 6  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_satisfying_all(my_list):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_elements_satisfying_all(my_list):  # stored in memory, not executed yet
def get_elements_satisfying_all(my_list):  # stored in memory, not executed yet
my_list = ...  # assigns a value to the variable
? = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_elements_satisfying_all(...))  # get_elements_satisfying_all() runs with my_list=[4, 1, 8, 6, 3]
#   → return list(map(lambda item: item, my_list))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_satisfying_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_satisfying_all(...))  # get_elements_satisfying_all() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1980:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_net_price(original_price, discount_percent, tax_rate):  # defines a function with parameters original_price, discount_percent, tax_rate
    discounted = original_price * (1 - discount_percent / 100)
    final = discounted * (1 + tax_rate / 100)
    return final  # return sends the result back to the caller
#
# Operators: * + - /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_net_price(100, 20, 10)  # arguments evaluated first, then function body runs
calculate_net_price(original_price, discount_percent, tax_rate)  # arguments evaluated first, then function body runs
print(calculate_net_price(25, 25, 25))  # calculate_net_price() runs first, then print() outputs the result
original_price * ?  # original_price → then ? → then *
discounted * ?  # discounted → then ? → then *
1 - ?  # 1 → then ? → then -
1 + ?  # 1 → then ? → then +
discount_percent / 100  # discount_percent → then 100 → then /
tax_rate / 100  # tax_rate → then 100 → then /
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_net_price(original_price, discount_percent, tax_rate):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_net_price(original_price, discount_percent, tax_rate):  # stored in memory, not executed yet
def calculate_net_price(original_price, discount_percent, tax_rate):  # stored in memory, not executed yet
original_price = ...  # assigns a value to the variable
discount_percent = ...  # assigns a value to the variable
tax_rate = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_net_price(...))  # calculate_net_price() runs with original_price=25, discount_percent=25, tax_rate=25
#   → return final
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_net_price is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → original_price = ...  # stored in memory
#   → discount_percent = ...  # stored in memory
#   → tax_rate = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_net_price(...))  # calculate_net_price() runs first → value goes to print()

"""

"""
Problem 1981:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_strings_with_all_vowels(strings):  # defines a function with parameters strings
    vowels = set('aeiou')
    result = []
    for s in strings:
        if vowels.issubset(set(s.lower())):
            result.append(s)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Built-in functions: list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_strings_with_all_vowels(?)  # arguments evaluated first, then function body runs
get_strings_with_all_vowels(strings)  # arguments evaluated first, then function body runs
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_strings_with_all_vowels(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def get_strings_with_all_vowels(strings):  # stored in memory, not executed yet
def get_strings_with_all_vowels(strings):  # stored in memory, not executed yet
strings = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(get_strings_with_all_vowels(...))  # get_strings_with_all_vowels() runs with strings=[4, 1, 8, 6, 3]
#   → return list(map(lambda s: s, strings))
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_with_all_vowels is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_with_all_vowels(...))  # get_strings_with_all_vowels() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1984:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import math
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

"""
Problem 1997:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def get_elements_matching_multiple_patterns(strings):  # defines a function with parameters strings
    result = []
    for s in strings:
        if all((pattern in s for pattern in patterns)):
            result.append(s)
    return result  # return sends the result back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
get_elements_matching_multiple_patterns(?, 'o', 'r')  # arguments evaluated first, then function body runs
main()  # function body runs with no arguments
pattern in s  # evaluated as True or False → picks which branch
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def get_elements_matching_multiple_patterns(strings):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def main():  # stored in memory, not executed yet
main()  # calls main()
#   → lst = ['a', 'b', 'c']
#   → result = True
#   → for x in lst:
#   →     if not isinstance(x, str):
#   →         result = False
#   →         break
#   → print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — get_elements_matching_multiple_patterns, main are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → main()  # jumps into main(), runs body, returns
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped

"""

"""
Problem 1998:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
def calculate_statistical_summary(numbers):  # defines a function with parameters numbers
    sorted_nums = sorted(numbers)
    n = len(numbers)
    mean = sum(numbers) / n
    median = sorted_nums[n // 2] if n % 2 else (sorted_nums[n // 2 - 1] + sorted_nums[n // 2]) / 2
    min_val = min(numbers)
    max_val = max(numbers)
    return {'mean': mean, 'median': median, 'min': min_val, 'max': max_val, 'range': max_val - min_val}  # return sends the result back to the caller
#
# Operators: % + - / //
# Built-in functions: len, max, min, sorted, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
calculate_statistical_summary(?)  # arguments evaluated first, then function body runs
sum(...) / n  # sum(...) → then n → then /
n % 2  # n → then 2 → then %
? / 2  # ? → then 2 → then /
max_val - min_val  # max_val → then min_val → then -
n // 2  # n → then 2 → then //
? + ?  # ? → then ? → then +
? - 1  # ? → then 1 → then -
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
def calculate_statistical_summary(numbers):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_statistical_summary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)

"""

"""
Problem 2000:
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
import math
#
# EXECUTION FLOW:
# Execution Flow tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally

"""

