# Syntax documentation for level 1 (problems 1-2000)
# Auto-generated by gen_syntax_files.py

"""
Problem 1501:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(min(s)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Run print(min(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      print(min(...))

"""

"""
Problem 1502:
#
# ===== SYNTAX =====
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(next(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to nums
#   2. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      nums = ... (stored in memory)
#      print(next(...))

"""

"""
Problem 1503:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(scores)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to scores
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      scores = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1504:
#
# ===== SYNTAX =====
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(next(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to letters
#   2. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      letters = ... (stored in memory)
#      print(next(...))

"""

"""
Problem 1505:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(strings)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to strings
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1506:
#
# ===== SYNTAX =====
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(next(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to users
#   2. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      users = ... (stored in memory)
#      print(next(...))

"""

"""
Problem 1507:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(min(data)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Run print(min(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      print(min(...))

"""

"""
Problem 1508:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(scores)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to scores
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      scores = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1509:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(next(iter(...))): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      print(next(...))

"""

"""
Problem 1510:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(words)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1511:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to items
#   2. Assign to result
#   3. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1512:
#
# ===== SYNTAX =====
#   def solve()
#   min() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to people
#   2. Assign to min_age_dict
#   3. Run print(min_age_dict)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to people
#   7. Assign to min_age_dict
#   8. Run print(min_age_dict)
#   9. Assign to people
#   10. Assign to min_age_dict
#   11. Run print(min_age_dict)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      min_age_dict = ... (stored in memory)
#      print(min_age_dict)
#      people = ... (stored in memory)
#      min_age_dict = ... (stored in memory)
#      print(min_age_dict)
#      people = ... (stored in memory)
#      min_age_dict = ... (stored in memory)
#      print(min_age_dict)

"""

"""
Problem 1513:
#
# ===== SYNTAX =====
#   def solve()
#   max() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to max_word
#   3. Run print(max_word)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to max_word
#   8. Run print(max_word)
#   9. Assign to words
#   10. Assign to max_word
#   11. Run print(max_word)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      max_word = ... (stored in memory)
#      print(max_word)
#      words = ... (stored in memory)
#      max_word = ... (stored in memory)
#      print(max_word)
#      words = ... (stored in memory)
#      max_word = ... (stored in memory)
#      print(max_word)

"""

"""
Problem 1514:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to name
#   3. Assign to city
#   4. Run print(name, city)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to name
#   9. Assign to city
#   10. Run print(name, city)
#   11. Assign to data
#   12. Assign to name
#   13. Assign to city
#   14. Run print(name, city)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      name = ... (stored in memory)
#      city = ... (stored in memory)
#      print(name, city)
#      data = ... (stored in memory)
#      name = ... (stored in memory)
#      city = ... (stored in memory)
#      print(name, city)
#      data = ... (stored in memory)
#      name = ... (stored in memory)
#      city = ... (stored in memory)
#      print(name, city)

"""

"""
Problem 1515:
#
# ===== SYNTAX =====
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(dict(counter)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to items
#   3. Assign to counter
#   4. For loop over items
#       4.1 Update ? += ...
#   5. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      counter = ... (stored in memory)
#      for loop runs its body once per item
#      print(dict(...))
#
#   Loop: grabs one item from items each round

"""

"""
Problem 1516:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Run data.setdefault(...).append('apple')
#   3. Run data.setdefault(...).append('banana')
#   4. Run print(data)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Run data.setdefault(...).append('apple')
#   9. Run data.setdefault(...).append('banana')
#   10. Run print(data)
#   11. Assign to data
#   12. Run data.setdefault(...).append('apple')
#   13. Run data.setdefault(...).append('banana')
#   14. Run print(data)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      print(data)
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      print(data)
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      print(data)

"""

"""
Problem 1517:
#
# ===== SYNTAX =====
#   def solve()
#   next() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to first_even
#   3. Run print(first_even)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to first_even
#   8. Run print(first_even)
#   9. Assign to numbers
#   10. Assign to first_even
#   11. Run print(first_even)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      first_even = ... (stored in memory)
#      print(first_even)
#      numbers = ... (stored in memory)
#      first_even = ... (stored in memory)
#      print(first_even)
#      numbers = ... (stored in memory)
#      first_even = ... (stored in memory)
#      print(first_even)

"""

"""
Problem 1518:
#
# ===== SYNTAX =====
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(dict(grouped)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to items
#   3. Assign to grouped
#   4. For loop over items
#       4.1 Run ?.append(item)
#   5. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      grouped = ... (stored in memory)
#      for loop runs its body once per item
#      print(dict(...))
#
#   Loop: grabs one item from items each round

"""

"""
Problem 1519:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to user_info
#   3. Assign to name
#   4. Run print(name)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to user_info
#   9. Assign to name
#   10. Run print(name)
#   11. Assign to data
#   12. Assign to user_info
#   13. Assign to name
#   14. Run print(name)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      user_info = ... (stored in memory)
#      name = ... (stored in memory)
#      print(name)
#      data = ... (stored in memory)
#      user_info = ... (stored in memory)
#      name = ... (stored in memory)
#      print(name)
#      data = ... (stored in memory)
#      user_info = ... (stored in memory)
#      name = ... (stored in memory)
#      print(name)

"""

"""
Problem 1520:
#
# ===== SYNTAX =====
#   def solve()
#   min() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to shortest
#   3. Run print(shortest)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to shortest
#   8. Run print(shortest)
#   9. Assign to words
#   10. Assign to shortest
#   11. Run print(shortest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      shortest = ... (stored in memory)
#      print(shortest)
#      words = ... (stored in memory)
#      shortest = ... (stored in memory)
#      print(shortest)
#      words = ... (stored in memory)
#      shortest = ... (stored in memory)
#      print(shortest)

"""

"""
Problem 1521:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to ?
#   3. Run print(data)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to data
#   7. Assign to ?
#   8. Run print(data)
#   9. Assign to data
#   10. Assign to ?
#   11. Run print(data)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(data)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(data)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(data)

"""

"""
Problem 1522:
#
# ===== SYNTAX =====
#   def solve()
#   max() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to tuples
#   2. Assign to max_tuple
#   3. Run print(max_tuple)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to tuples
#   7. Assign to max_tuple
#   8. Run print(max_tuple)
#   9. Assign to tuples
#   10. Assign to max_tuple
#   11. Run print(max_tuple)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      tuples = ... (stored in memory)
#      max_tuple = ... (stored in memory)
#      print(max_tuple)
#      tuples = ... (stored in memory)
#      max_tuple = ... (stored in memory)
#      print(max_tuple)
#      tuples = ... (stored in memory)
#      max_tuple = ... (stored in memory)
#      print(max_tuple)

"""

"""
Problem 1523:
#
# ===== SYNTAX =====
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to items
#   3. Assign to grouped
#   4. For loop over items
#       4.1 Run ?.add(value)
#   5. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      grouped = ... (stored in memory)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from items each round

"""

"""
Problem 1524:
#
# ===== SYNTAX =====
#   def solve()
#   sum() — built-in function call
#   set() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#
# ===== EXECUTION ORDER =====
#   1. Assign to dict1
#   2. Assign to dict2
#   3. Assign to total
#   4. Run print(total)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Assign to total
#   10. Run print(total)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Assign to total
#   14. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)

"""

"""
Problem 1525:
#
# ===== SYNTAX =====
#   def solve()
#   next() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to first_even
#   3. Run print(first_even)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to first_even
#   8. Run print(first_even)
#   9. Assign to numbers
#   10. Assign to first_even
#   11. Run print(first_even)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      first_even = ... (stored in memory)
#      print(first_even)
#      numbers = ... (stored in memory)
#      first_even = ... (stored in memory)
#      print(first_even)
#      numbers = ... (stored in memory)
#      first_even = ... (stored in memory)
#      print(first_even)

"""

"""
Problem 1526:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Run data.setdefault(...).append('apple')
#   3. Run data.setdefault(...).append('banana')
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Run data.setdefault(...).append('apple')
#   9. Run data.setdefault(...).append('banana')
#   10. Run print(?)
#   11. Assign to data
#   12. Run data.setdefault(...).append('apple')
#   13. Run data.setdefault(...).append('banana')
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      print(?)
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      print(?)
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      print(?)

"""

"""
Problem 1527:
#
# ===== SYNTAX =====
#   def solve()
#   min() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to people
#   2. Assign to min_age
#   3. Run print(min_age)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to people
#   7. Assign to min_age
#   8. Run print(min_age)
#   9. Assign to people
#   10. Assign to min_age
#   11. Run print(min_age)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      min_age = ... (stored in memory)
#      print(min_age)
#      people = ... (stored in memory)
#      min_age = ... (stored in memory)
#      print(min_age)
#      people = ... (stored in memory)
#      min_age = ... (stored in memory)
#      print(min_age)

"""

"""
Problem 1528:
#
# ===== SYNTAX =====
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(dict(grouped)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to words
#   3. Assign to grouped
#   4. For loop over words
#       4.1 Run ?.append(word)
#   5. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      grouped = ... (stored in memory)
#      for loop runs its body once per item
#      print(dict(...))
#
#   Loop: grabs one item from words each round

"""

"""
Problem 1529:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to items
#   3. Assign to missing
#   4. Run print(items, missing)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to items
#   9. Assign to missing
#   10. Run print(items, missing)
#   11. Assign to data
#   12. Assign to items
#   13. Assign to missing
#   14. Run print(items, missing)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      items = ... (stored in memory)
#      missing = ... (stored in memory)
#      print(items, missing)
#      data = ... (stored in memory)
#      items = ... (stored in memory)
#      missing = ... (stored in memory)
#      print(items, missing)
#      data = ... (stored in memory)
#      items = ... (stored in memory)
#      missing = ... (stored in memory)
#      print(items, missing)

"""

"""
Problem 1530:
#
# ===== SYNTAX =====
#   def solve()
#   max() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to longest
#   3. Run print(longest)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to longest
#   8. Run print(longest)
#   9. Assign to words
#   10. Assign to longest
#   11. Run print(longest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      longest = ... (stored in memory)
#      print(longest)
#      words = ... (stored in memory)
#      longest = ... (stored in memory)
#      print(longest)
#      words = ... (stored in memory)
#      longest = ... (stored in memory)
#      print(longest)

"""

"""
Problem 1531:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to counter
#   2. Run counter.setdefault('count', 0)
#   3. Update ? += ...
#   4. Run print(counter)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to counter
#   8. Run counter.setdefault('count', 0)
#   9. Update ? += ...
#   10. Run print(counter)
#   11. Assign to counter
#   12. Run counter.setdefault('count', 0)
#   13. Update ? += ...
#   14. Run print(counter)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      counter = ... (stored in memory)
#      counter.setdefault('count', 0)
#      top-level statement runs
#      print(counter)
#      counter = ... (stored in memory)
#      counter.setdefault('count', 0)
#      top-level statement runs
#      print(counter)
#      counter = ... (stored in memory)
#      counter.setdefault('count', 0)
#      top-level statement runs
#      print(counter)

"""

"""
Problem 1532:
#
# ===== SYNTAX =====
#   def is_prime(n)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   comprehension
#   operators: + % ** != > and
#
# ===== EVALUATION ORDER =====
#   is_prime(n): looks up variable 'n', then runs the function body
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(15): gets the literal 15, then runs the function body
#   is_prime(x): looks up variable 'x', then runs the function body
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(n) — stored for later
#   2. Assign to numbers
#   3. Assign to first_prime
#   4. Run print(first_prime)
#   5. Define def is_prime(n) — stored for later
#   6. Define def is_prime(n) — stored for later
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(is_prime(...))
#       calls is_prime with: n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      first_prime = ... (stored in memory)
#      print(first_prime)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(15)) → function runs first, then print shows the result

"""

"""
Problem 1533:
#
# ===== SYNTAX =====
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(dict(char_count)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to text
#   3. Assign to char_count
#   4. For loop over text
#       4.1 Update ? += ...
#   5. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      char_count = ... (stored in memory)
#      for loop runs its body once per item
#      print(dict(...))
#
#   Loop: grabs one item from text each round

"""

"""
Problem 1534:
#
# ===== SYNTAX =====
#   def solve()
#   set() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   dict1.get(...) + dict2.get(...): first get dict1.get(...), then get dict2.get(...), then compute dict1.get(...) + dict2.get(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#   set(...) | set(...): first get set(...), then get set(...), then compute set(...) | set(...)
#
# ===== EXECUTION ORDER =====
#   1. Assign to dict1
#   2. Assign to dict2
#   3. Assign to merged
#   4. Run print(merged)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Assign to merged
#   10. Run print(merged)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Assign to merged
#   14. Run print(merged)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      merged = ... (stored in memory)
#      print(merged)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      merged = ... (stored in memory)
#      print(merged)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      merged = ... (stored in memory)
#      print(merged)

"""

"""
Problem 1535:
#
# ===== SYNTAX =====
#   def solve()
#   min() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to students
#   2. Assign to min_score
#   3. Run print(min_score)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to students
#   7. Assign to min_score
#   8. Run print(min_score)
#   9. Assign to students
#   10. Assign to min_score
#   11. Run print(min_score)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      students = ... (stored in memory)
#      min_score = ... (stored in memory)
#      print(min_score)
#      students = ... (stored in memory)
#      min_score = ... (stored in memory)
#      print(min_score)
#      students = ... (stored in memory)
#      min_score = ... (stored in memory)
#      print(min_score)

"""

"""
Problem 1536:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Run data.setdefault(...).append('apple')
#   3. Run data.setdefault(...).append('banana')
#   4. Run data.setdefault(...).append('carrot')
#   5. Run print(data)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to data
#   9. Run data.setdefault(...).append('apple')
#   10. Run data.setdefault(...).append('banana')
#   11. Run data.setdefault(...).append('carrot')
#   12. Run print(data)
#   13. Assign to data
#   14. Run data.setdefault(...).append('apple')
#   15. Run data.setdefault(...).append('banana')
#   16. Run data.setdefault(...).append('carrot')
#   17. Run print(data)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      data.setdefault(...).append('carrot')
#      print(data)
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      data.setdefault(...).append('carrot')
#      print(data)
#      data = ... (stored in memory)
#      data.setdefault(...).append('apple')
#      data.setdefault(...).append('banana')
#      data.setdefault(...).append('carrot')
#      print(data)

"""

"""
Problem 1537:
#
# ===== SYNTAX =====
#   def solve()
#   max() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to products
#   2. Assign to max_price
#   3. Run print(max_price)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to products
#   7. Assign to max_price
#   8. Run print(max_price)
#   9. Assign to products
#   10. Assign to max_price
#   11. Run print(max_price)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      products = ... (stored in memory)
#      max_price = ... (stored in memory)
#      print(max_price)
#      products = ... (stored in memory)
#      max_price = ... (stored in memory)
#      print(max_price)
#      products = ... (stored in memory)
#      max_price = ... (stored in memory)
#      print(max_price)

"""

"""
Problem 1538:
#
# ===== SYNTAX =====
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to data
#   3. Assign to nested
#   4. For loop over data
#       4.1 Update ? += ...
#   5. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      nested = ... (stored in memory)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from data each round

"""

"""
Problem 1539:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   ? + dict2.get(...): first get ?, then get dict2.get(...), then compute ? + dict2.get(...)
#   ? + dict2.get(...): first get ?, then get dict2.get(...), then compute ? + dict2.get(...)
#   ? + dict2.get(...): first get ?, then get dict2.get(...), then compute ? + dict2.get(...)
#   dict1.get(...) + ' ': first get dict1.get(...), then get ' ', then compute dict1.get(...) + ' '
#   ? + dict2.get(...): first get ?, then get dict2.get(...), then compute ? + dict2.get(...)
#   ? + dict2.get(...): first get ?, then get dict2.get(...), then compute ? + dict2.get(...)
#   dict1.get(...) + ' ': first get dict1.get(...), then get ' ', then compute dict1.get(...) + ' '
#   dict1.get(...) + ' ': first get dict1.get(...), then get ' ', then compute dict1.get(...) + ' '
#   dict1.get(...) + ' ': first get dict1.get(...), then get ' ', then compute dict1.get(...) + ' '
#   dict1.get(...) + ' ': first get dict1.get(...), then get ' ', then compute dict1.get(...) + ' '
#
# ===== EXECUTION ORDER =====
#   1. Assign to dict1
#   2. Assign to dict2
#   3. Assign to full_name
#   4. Run print(full_name)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Assign to full_name
#   10. Run print(full_name)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Assign to full_name
#   14. Run print(full_name)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      full_name = ... (stored in memory)
#      print(full_name)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      full_name = ... (stored in memory)
#      print(full_name)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      full_name = ... (stored in memory)
#      print(full_name)

"""

"""
Problem 1540:
#
# ===== SYNTAX =====
#   def solve()
#   next() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to first_greater_than_5
#   3. Run print(first_greater_than_5)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to first_greater_than_5
#   8. Run print(first_greater_than_5)
#   9. Assign to numbers
#   10. Assign to first_greater_than_5
#   11. Run print(first_greater_than_5)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      first_greater_than_5 = ... (stored in memory)
#      print(first_greater_than_5)
#      numbers = ... (stored in memory)
#      first_greater_than_5 = ... (stored in memory)
#      print(first_greater_than_5)
#      numbers = ... (stored in memory)
#      first_greater_than_5 = ... (stored in memory)
#      print(first_greater_than_5)

"""

"""
Problem 1541:
#
# ===== SYNTAX =====
#   def solve()
#   set() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Run data.setdefault(...).add('python')
#   3. Run data.setdefault(...).add('programming')
#   4. Run print(data)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Run data.setdefault(...).add('python')
#   9. Run data.setdefault(...).add('programming')
#   10. Run print(data)
#   11. Assign to data
#   12. Run data.setdefault(...).add('python')
#   13. Run data.setdefault(...).add('programming')
#   14. Run print(data)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      data.setdefault(...).add('python')
#      data.setdefault(...).add('programming')
#      print(data)
#      data = ... (stored in memory)
#      data.setdefault(...).add('python')
#      data.setdefault(...).add('programming')
#      print(data)
#      data = ... (stored in memory)
#      data.setdefault(...).add('python')
#      data.setdefault(...).add('programming')
#      print(data)

"""

"""
Problem 1542:
#
# ===== SYNTAX =====
#   def solve()
#   min() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to people
#   2. Assign to oldest
#   3. Run print(oldest)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to people
#   7. Assign to oldest
#   8. Run print(oldest)
#   9. Assign to people
#   10. Assign to oldest
#   11. Run print(oldest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      oldest = ... (stored in memory)
#      print(oldest)
#      people = ... (stored in memory)
#      oldest = ... (stored in memory)
#      print(oldest)
#      people = ... (stored in memory)
#      oldest = ... (stored in memory)
#      print(oldest)

"""

"""
Problem 1543:
#
# ===== SYNTAX =====
#   comprehension
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   value + ' ': first get value, then get ' ', then compute value + ' '
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to words
#   3. Assign to result
#   4. For loop over words
#       4.1 Update ? += ...
#   5. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from words each round

"""

"""
Problem 1544:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to existing
#   3. Run existing.extend(?)
#   4. Run print(existing)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to existing
#   9. Run existing.extend(?)
#   10. Run print(existing)
#   11. Assign to data
#   12. Assign to existing
#   13. Run existing.extend(?)
#   14. Run print(existing)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      existing = ... (stored in memory)
#      existing.extend(?)
#      print(existing)
#      data = ... (stored in memory)
#      existing = ... (stored in memory)
#      existing.extend(?)
#      print(existing)
#      data = ... (stored in memory)
#      existing = ... (stored in memory)
#      existing.extend(?)
#      print(existing)

"""

"""
Problem 1545:
#
# ===== SYNTAX =====
#   def solve()
#   max() — built-in function call
#   datetime() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import datetime from datetime
#   2. Assign to events
#   3. Assign to max_date
#   4. Run print(max_date)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import datetime from datetime
#   8. Assign to events
#   9. Assign to max_date
#   10. Run print(max_date)
#   11. Import datetime from datetime
#   12. Assign to events
#   13. Assign to max_date
#   14. Run print(max_date)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      events = ... (stored in memory)
#      max_date = ... (stored in memory)
#      print(max_date)
#      events = ... (stored in memory)
#      max_date = ... (stored in memory)
#      print(max_date)
#      events = ... (stored in memory)
#      max_date = ... (stored in memory)
#      print(max_date)

"""

"""
Problem 1546:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to ?
#   3. Assign to ?
#   4. Run print(data)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to ?
#   9. Assign to ?
#   10. Run print(data)
#   11. Assign to data
#   12. Assign to ?
#   13. Assign to ?
#   14. Run print(data)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(data)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(data)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(data)

"""

"""
Problem 1547:
#
# ===== SYNTAX =====
#   def number_gen()
#   (no return — the function prints directly instead of returning)
#   number_gen() — built-in function call
#   next() — built-in function call
#   iter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   number_gen(): — no arguments needed
#   number_gen(): — no arguments needed
#   number_gen(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def number_gen() — stored for later
#   2. Assign to gen
#   3. Assign to first
#   4. Assign to empty_gen
#   5. Assign to empty_result
#   6. Run print(first, empty_result)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Evaluate expression
#   10. Evaluate expression
#   11. Evaluate expression
#   12. Evaluate expression
#   13. Evaluate expression
#   14. Evaluate expression
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions number_gen, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      gen = ... (stored in memory)
#      first = ... (stored in memory)
#      empty_gen = ... (stored in memory)
#      empty_result = ... (stored in memory)
#      print(first, empty_result)
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1548:
#
# ===== SYNTAX =====
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(dict(stats)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to stats
#   3. Update ? += ...
#   4. Update ? += ...
#   5. Update ? += ...
#   6. Update ? += ...
#   7. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      stats = ... (stored in memory)
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs
#      print(dict(...))

"""

"""
Problem 1549:
#
# ===== SYNTAX =====
#   def create_list()
#   return — exits the function and sends back a value
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   create_list(): — no arguments needed
#   create_list(): — no arguments needed
#   'items' not in data: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def create_list() — stored for later
#   2. Assign to data
#   3. If statement (one branch runs)
#   4. Assign to items
#   5. Run items.append('apple')
#   6. Run print(data)
#   7. Define def create_list() — stored for later
#   8. Define def create_list() — stored for later
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_list, create_list, create_list are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      if checks a condition and runs ONE branch
#      items = ... (stored in memory)
#      items.append('apple')
#      print(data)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1550:
#
# ===== SYNTAX =====
#   def solve()
#   reduce() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Assign to numbers
#   3. Assign to product
#   4. Run print(product)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import reduce from functools
#   8. Assign to numbers
#   9. Assign to product
#   10. Run print(product)
#   11. Import reduce from functools
#   12. Assign to numbers
#   13. Assign to product
#   14. Run print(product)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      product = ... (stored in memory)
#      print(product)
#      numbers = ... (stored in memory)
#      product = ... (stored in memory)
#      print(product)
#      numbers = ... (stored in memory)
#      product = ... (stored in memory)
#      print(product)

"""

"""
Problem 1551:
#
# ===== SYNTAX =====
#   def solve()
#   partial() — built-in function call
#   multiply_by_5() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#
# ===== EXECUTION ORDER =====
#   1. Import partial from functools
#   2. Assign to multiply_by_5
#   3. Assign to result
#   4. Run print(result)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import partial from functools
#   8. Assign to multiply_by_5
#   9. Assign to result
#   10. Run print(result)
#   11. Import partial from functools
#   12. Assign to multiply_by_5
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      multiply_by_5 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      multiply_by_5 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      multiply_by_5 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1552:
#
# ===== SYNTAX =====
#   def fibonacci(n)
#   return — exits the function and sends back a value
#   lru_cache() — built-in function call
#   fibonacci() — built-in function call
#   if/elif/else
#   operators: + - <
#
# ===== EVALUATION ORDER =====
#   print(fibonacci(10)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(n): looks up variable 'n', then runs the function body
#   print(fibonacci(15)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(...) + fibonacci(...): first get fibonacci(...), then get fibonacci(...), then compute fibonacci(...) + fibonacci(...)
#   fibonacci(10): gets the literal 10, then runs the function body
#   fibonacci(15): gets the literal 15, then runs the function body
#   fibonacci(n - 1): computes the expression first, then runs the function body
#   fibonacci(n - 2): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 2: first get n, then get 2, then compute n - 2
#   n < 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import lru_cache from functools
#   2. Define def fibonacci(n) — stored for later
#   3. Run print(fibonacci(...))
#       calls fibonacci with: n=10
#         3.1 Return from function
#   4. Define def fibonacci(n) — stored for later
#   5. Define def fibonacci(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(fibonacci(...))
#       calls fibonacci with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions fibonacci, fibonacci, fibonacci are just stored
#   Execution starts at the non-def lines below:
#      print(fibonacci(10)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1553:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   permutations() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import permutations from itertools
#   2. Assign to items
#   3. Assign to perms
#   4. Run print(perms)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import permutations from itertools
#   8. Assign to items
#   9. Assign to perms
#   10. Run print(perms)
#   11. Import permutations from itertools
#   12. Assign to items
#   13. Assign to perms
#   14. Run print(perms)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      perms = ... (stored in memory)
#      print(perms)
#      items = ... (stored in memory)
#      perms = ... (stored in memory)
#      print(perms)
#      items = ... (stored in memory)
#      perms = ... (stored in memory)
#      print(perms)

"""

"""
Problem 1554:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   product() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import product from itertools
#   2. Assign to list1
#   3. Assign to list2
#   4. Assign to cartesian
#   5. Run print(cartesian)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import product from itertools
#   9. Assign to list1
#   10. Assign to list2
#   11. Assign to cartesian
#   12. Run print(cartesian)
#   13. Import product from itertools
#   14. Assign to list1
#   15. Assign to list2
#   16. Assign to cartesian
#   17. Run print(cartesian)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      cartesian = ... (stored in memory)
#      print(cartesian)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      cartesian = ... (stored in memory)
#      print(cartesian)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      cartesian = ... (stored in memory)
#      print(cartesian)

"""

"""
Problem 1555:
#
# ===== SYNTAX =====
#   def solve()
#   cycle() — built-in function call
#   next() — built-in function call
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import cycle from itertools
#   2. Assign to colors
#   3. Assign to color_cycle
#   4. Assign to first_5
#   5. Run print(first_5)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import cycle from itertools
#   9. Assign to colors
#   10. Assign to color_cycle
#   11. Assign to first_5
#   12. Run print(first_5)
#   13. Import cycle from itertools
#   14. Assign to colors
#   15. Assign to color_cycle
#   16. Assign to first_5
#   17. Run print(first_5)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      colors = ... (stored in memory)
#      color_cycle = ... (stored in memory)
#      first_5 = ... (stored in memory)
#      print(first_5)
#      colors = ... (stored in memory)
#      color_cycle = ... (stored in memory)
#      first_5 = ... (stored in memory)
#      print(first_5)
#      colors = ... (stored in memory)
#      color_cycle = ... (stored in memory)
#      first_5 = ... (stored in memory)
#      print(first_5)

"""

"""
Problem 1556:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   repeat() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import repeat from itertools
#   2. Assign to repeated
#   3. Run print(repeated)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      repeated = ... (stored in memory)
#      print(repeated)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1557:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   list() — built-in function call
#   islice() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import islice from itertools
#   2. Assign to numbers
#   3. Assign to first_5
#   4. Run print(first_5)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import islice from itertools
#   8. Assign to numbers
#   9. Assign to first_5
#   10. Run print(first_5)
#   11. Import islice from itertools
#   12. Assign to numbers
#   13. Assign to first_5
#   14. Run print(first_5)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      first_5 = ... (stored in memory)
#      print(first_5)
#      numbers = ... (stored in memory)
#      first_5 = ... (stored in memory)
#      print(first_5)
#      numbers = ... (stored in memory)
#      first_5 = ... (stored in memory)
#      print(first_5)

"""

"""
Problem 1558:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(list(iter1)): inner expression resolves first, then print() outputs
#   print(list(iter2)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import tee from itertools
#   2. Assign to numbers
#   3. Assign to ?
#   4. Run print(list(...), list(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(list(...), list(...))

"""

"""
Problem 1559:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   accumulate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import accumulate from itertools
#   2. Assign to numbers
#   3. Assign to cumulative
#   4. Run print(cumulative)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import accumulate from itertools
#   8. Assign to numbers
#   9. Assign to cumulative
#   10. Run print(cumulative)
#   11. Import accumulate from itertools
#   12. Assign to numbers
#   13. Assign to cumulative
#   14. Run print(cumulative)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      cumulative = ... (stored in memory)
#      print(cumulative)
#      numbers = ... (stored in memory)
#      cumulative = ... (stored in memory)
#      print(cumulative)
#      numbers = ... (stored in memory)
#      cumulative = ... (stored in memory)
#      print(cumulative)

"""

"""
Problem 1560:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#   def solve()
#   Person() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Person()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import dataclass from dataclasses
#   2. Define class Person
#   3. Assign to person
#   4. Run print(person)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      print(person)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))

"""

"""
Problem 1561:
#
# ===== SYNTAX =====
#   class Student — defines a new type
#   def solve()
#   Student() — built-in function call
#   field() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Student()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import dataclass, field from dataclasses
#   2. Define class Student
#   3. Assign to student
#   4. Run print(student)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Student(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      student = ... (stored in memory)
#      print(student)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Student(...))

"""

"""
Problem 1562:
#
# ===== SYNTAX =====
#   class Point — defines a new type
#   def solve()
#   dataclass() — built-in function call
#   Point() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Point()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import dataclass from dataclasses
#   2. Define class Point
#   3. Assign to point
#   4. Run print(point)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Point(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      point = ... (stored in memory)
#      print(point)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Point(...))

"""

"""
Problem 1563:
#
# ===== SYNTAX =====
#   def sum_numbers(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   sum_numbers(?): resolves the value, then runs the function body
#   sum_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(sum_numbers(?)): the inner sum_numbers() executes FIRST, then print() outputs the result
#   sum_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import List from typing
#   2. Define def sum_numbers(numbers) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def sum_numbers(numbers) — stored for later
#   6. Define def sum_numbers(numbers) — stored for later
#   7. Assign to numbers
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sum_numbers(...))
#       calls sum_numbers with: numbers=[4, 1, 8, 6, 3]
#         10.1 Import reduce from functools
#         10.2 Import operator
#         10.3 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_numbers, sum_numbers, sum_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 1564:
#
# ===== SYNTAX =====
#   def greet(name)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: is
#
# ===== EVALUATION ORDER =====
#   print(greet('Alice')): the inner greet() executes FIRST, then print() outputs the result
#   print(greet()): the inner greet() executes FIRST, then print() outputs the result
#   greet(name): looks up variable 'name', then runs the function body
#   print(greet('Bob')): the inner greet() executes FIRST, then print() outputs the result
#   greet('Alice'): gets the literal 'Alice', then runs the function body
#   greet(): — no arguments needed
#   greet('Bob'): gets the literal 'Bob', then runs the function body
#   name  None: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import Optional from typing
#   2. Define def greet(name) — stored for later
#   3. Run print(greet(...), greet(...))
#       calls greet with: name='Alice'
#         3.1 Return from function
#   4. Define def greet(name) — stored for later
#   5. Define def greet(name) — stored for later
#   6. Assign to name
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(greet(...))
#       calls greet with: name='Bob'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions greet, greet, greet are just stored
#   Execution starts at the non-def lines below:
#      print(greet('Alice')) → function runs first, then print shows the result
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet('Bob')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1565:
#
# ===== SYNTAX =====
#   def process_value(value)
#   return — exits the function and sends back a value
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(process_value(42)): the inner process_value() executes FIRST, then print() outputs the result
#   print(process_value('hello')): the inner process_value() executes FIRST, then print() outputs the result
#   process_value(value): looks up variable 'value', then runs the function body
#   print(process_value(25)): the inner process_value() executes FIRST, then print() outputs the result
#   process_value(42): gets the literal 42, then runs the function body
#   process_value('hello'): gets the literal 'hello', then runs the function body
#   process_value(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import Union from typing
#   2. Define def process_value(value) — stored for later
#   3. Run print(process_value(...), process_value(...))
#       calls process_value with: value=42
#         3.1 Return from function
#   4. Define def process_value(value) — stored for later
#   5. Define def process_value(value) — stored for later
#   6. Assign to value
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(process_value(...))
#       calls process_value with: value=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions process_value, process_value, process_value are just stored
#   Execution starts at the non-def lines below:
#      print(process_value(42)) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(process_value(25)) → function runs first, then print shows the result

"""

"""
Problem 1566:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to numbers
#   6. While loop
#   7. Assign to numbers
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      while loop repeats until condition is False
#      numbers = ... (stored in memory)
#      while loop repeats until condition is False
#      numbers = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1567:
#
# ===== SYNTAX =====
#   def expensive_function(x)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   pow() — built-in function call
#   comprehension
#   for
#   in
#   operators: * >
#
# ===== EVALUATION ORDER =====
#   expensive_function(x): looks up variable 'x', then runs the function body
#   print(expensive_function(3)): the inner expensive_function() executes FIRST, then print() outputs the result
#   x * 2: first get x, then get 2, then compute x * 2
#   expensive_function(3): gets the literal 3, then runs the function body
#   expensive_function(x): looks up variable 'x', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def expensive_function(x) — stored for later
#   2. Assign to numbers
#   3. Assign to result
#   4. Run print(result)
#   5. Define def expensive_function(x) — stored for later
#   6. Define def expensive_function(x) — stored for later
#   7. Assign to x
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(expensive_function(...))
#       calls expensive_function with: x=3
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions expensive_function, expensive_function, expensive_function are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(expensive_function(3)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1568:
#
# ===== SYNTAX =====
#   def handle_command(cmd)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(handle_command('start')): the inner handle_command() executes FIRST, then print() outputs the result
#   handle_command(cmd): looks up variable 'cmd', then runs the function body
#   print(handle_command(25)): the inner handle_command() executes FIRST, then print() outputs the result
#   handle_command('start'): gets the literal 'start', then runs the function body
#   handle_command(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def handle_command(cmd) — stored for later
#   2. Run print(handle_command(...))
#       calls handle_command with: cmd='start'
#         2.1 Return from function
#   3. Define def handle_command(cmd) — stored for later
#   4. Define def handle_command(cmd) — stored for later
#   5. Assign to cmd
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(handle_command(...))
#       calls handle_command with: cmd=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions handle_command, handle_command, handle_command are just stored
#   Execution starts at the non-def lines below:
#      print(handle_command('start')) → function runs first, then print shows the result
#      cmd = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(handle_command(25)) → function runs first, then print shows the result

"""

"""
Problem 1569:
#
# ===== SYNTAX =====
#   def process_point(point)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(process_point(?)): the inner process_point() executes FIRST, then print() outputs the result
#   process_point(point): looks up variable 'point', then runs the function body
#   print(process_point(25)): the inner process_point() executes FIRST, then print() outputs the result
#   process_point(?): resolves the value, then runs the function body
#   process_point(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def process_point(point) — stored for later
#   2. Run print(process_point(...))
#       calls process_point with: point=(3, 4)
#         2.1 Return from function
#   3. Define def process_point(point) — stored for later
#   4. Define def process_point(point) — stored for later
#   5. Assign to point
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(process_point(...))
#       calls process_point with: point=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions process_point, process_point, process_point are just stored
#   Execution starts at the non-def lines below:
#      print(process_point(?)) → function runs first, then print shows the result
#      point = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(process_point(25)) → function runs first, then print shows the result

"""

"""
Problem 1570:
#
# ===== SYNTAX =====
#   class Circle — defines a new type
#     def __init__ — method of Circle
#     def area — method of Circle
#   def solve()
#   return — exits the function and sends back a value
#   Circle() — built-in function call
#   operators: * **
#
# ===== EVALUATION ORDER =====
#   print(Circle(14)): inner expression resolves first, then print() outputs
#   3.14159 * ?: first get 3.14159, then get ?, then compute 3.14159 * ?
#   self.radius ** 2: first get self.radius, then get 2, then compute self.radius ** 2
#   3.14159 * ?: first get 3.14159, then get ?, then compute 3.14159 * ?
#   3.14159 * ?: first get 3.14159, then get ?, then compute 3.14159 * ?
#   self.radius ** 2: first get self.radius, then get 2, then compute self.radius ** 2
#   self.radius ** 2: first get self.radius, then get 2, then compute self.radius ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define class Circle
#   2. Assign to circle
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to obj
#   7. Run print(obj)
#   8. Run print(Circle(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Circle.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      circle = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Circle(...))

"""

"""
Problem 1571:
#
# ===== SYNTAX =====
#   class Temperature — defines a new type
#     def __init__ — method of Temperature
#     def celsius — method of Temperature
#     def celsius — method of Temperature
#   def solve()
#   return — exits the function and sends back a value
#   Temperature() — built-in function call
#   ValueError() — built-in function call
#   if/elif/else
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(Temperature()): inner expression resolves first, then print() outputs
#   value < -273.15: is this True or False? The answer picks which branch
#   value < -273.15: is this True or False? The answer picks which branch
#   value < -273.15: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define class Temperature
#   2. Assign to temp
#   3. Assign to ?
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Temperature(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Temperature.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      temp = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Temperature(...))
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1572:
#
# ===== SYNTAX =====
#   class Book — defines a new type
#     def __init__ — method of Book
#     def __str__ — method of Book
#
# ===== EVALUATION ORDER =====
#   print(str(book)): inner expression resolves first, then print() outputs
#   print(Book(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Book
#   2. Assign to book
#   3. Run print(str(...))
#   4. Assign to obj
#   5. Run print(obj)
#   6. Run print(Book(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Book.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      book = ... (stored in memory)
#      print(str(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Book(...))

"""

"""
Problem 1573:
#
# ===== SYNTAX =====
#   class Point — defines a new type
#     def __init__ — method of Point
#     def __repr__ — method of Point
#
# ===== EVALUATION ORDER =====
#   print(repr(point)): inner expression resolves first, then print() outputs
#   print(Point(3, 14)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Point
#   2. Assign to point
#   3. Run print(repr(...))
#   4. Assign to obj
#   5. Run print(obj)
#   6. Run print(Point(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Point.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      point = ... (stored in memory)
#      print(repr(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Point(...))

"""

"""
Problem 1574:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#     def __init__ — method of Person
#     def __eq__ — method of Person
#   def solve()
#   return — exits the function and sends back a value
#   Person() — built-in function call
#   isinstance() — built-in function call
#   if/elif/else
#   operators: == and
#
# ===== EVALUATION ORDER =====
#   print(Person('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Person
#   2. Assign to p1
#   3. Assign to p2
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Person.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      p1 = ... (stored in memory)
#      p2 = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1575:
#
# ===== SYNTAX =====
#   class Point — defines a new type
#     def __init__ — method of Point
#     def __hash__ — method of Point
#     def __eq__ — method of Point
#   operators: == and
#
# ===== EVALUATION ORDER =====
#   print(len(points_set)): inner expression resolves first, then print() outputs
#   print(Point(3, 14)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Point
#   2. Assign to point
#   3. Assign to points_set
#   4. Run print(len(...))
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(Point(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Point.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      point = ... (stored in memory)
#      points_set = ... (stored in memory)
#      print(len(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Point(...))

"""

"""
Problem 1576:
#
# ===== SYNTAX =====
#   class Stack — defines a new type
#     def __init__ — method of Stack
#     def push — method of Stack
#     def __len__ — method of Stack
#
# ===== EVALUATION ORDER =====
#   print(len(stack)): inner expression resolves first, then print() outputs
#   print(Stack()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Stack
#   2. Assign to stack
#   3. Run stack.push(1)
#   4. Run stack.push(2)
#   5. Run print(len(...))
#   6. Assign to obj
#   7. Run print(obj)
#   8. Run print(Stack(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Stack.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      stack = ... (stored in memory)
#      stack.push(1)
#      stack.push(2)
#      print(len(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Stack(...))

"""

"""
Problem 1577:
#
# ===== SYNTAX =====
#   class MyList — defines a new type
#     def __init__ — method of MyList
#     def __getitem__ — method of MyList
#   def solve()
#   return — exits the function and sends back a value
#   MyList() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(MyList(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class MyList
#   2. Assign to my_list
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to obj
#   7. Run print(obj)
#   8. Run print(MyList(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   MyList.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(MyList(...))

"""

"""
Problem 1578:
#
# ===== SYNTAX =====
#   class Timer — defines a new type
#     def __enter__ — method of Timer
#     def __exit__ — method of Timer
#   def solve()
#   return — exits the function and sends back a value
#   Timer() — built-in function call
#   sum() — built-in function call
#   range() — built-in function call
#   with
#   operators: -
#
# ===== EVALUATION ORDER =====
#   print(Timer()): inner expression resolves first, then print() outputs
#   time.time(...) - self.start: first get time.time(...), then get self.start, then compute time.time(...) - self.start
#   time.time(...) - self.start: first get time.time(...), then get self.start, then compute time.time(...) - self.start
#   time.time(...) - self.start: first get time.time(...), then get self.start, then compute time.time(...) - self.start
#
# ===== EXECUTION ORDER =====
#   1. Define class Timer
#   2. With
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(Timer(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      obj = ... (stored in memory)
#      print(obj)
#      print(Timer(...))

"""

"""
Problem 1579:
#
# ===== SYNTAX =====
#   class Resource — defines a new type
#     def __enter__ — method of Resource
#     def __exit__ — method of Resource
#   def solve()
#   return — exits the function and sends back a value
#   ExitStack() — built-in function call
#   Resource() — built-in function call
#   with
#
# ===== EVALUATION ORDER =====
#   print(Resource()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import ExitStack from contextlib
#   2. Define class Resource
#   3. With
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to obj
#   7. Run print(obj)
#   8. Run print(Resource(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      obj = ... (stored in memory)
#      print(obj)
#      print(Resource(...))

"""

"""
Problem 1580:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Assign to path
#   3. Assign to exists
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import Path from pathlib
#   8. Import Path from pathlib
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      path = ... (stored in memory)
#      exists = ... (stored in memory)
#      print(?)

"""

"""
Problem 1581:
#
# ===== SYNTAX =====
#   def solve()
#   Path() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Assign to path
#   3. Assign to parent
#   4. Run print(parent)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import Path from pathlib
#   8. Assign to path
#   9. Assign to parent
#   10. Run print(parent)
#   11. Import Path from pathlib
#   12. Assign to path
#   13. Assign to parent
#   14. Run print(parent)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      path = ... (stored in memory)
#      parent = ... (stored in memory)
#      print(parent)
#      path = ... (stored in memory)
#      parent = ... (stored in memory)
#      print(parent)
#      path = ... (stored in memory)
#      parent = ... (stored in memory)
#      print(parent)

"""

"""
Problem 1582:
#
# ===== SYNTAX =====
#   def solve()
#   Path() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: /
#
# ===== EVALUATION ORDER =====
#   ? / 'file.txt': first get ?, then get 'file.txt', then compute ? / 'file.txt'
#   ? / 'file.txt': first get ?, then get 'file.txt', then compute ? / 'file.txt'
#   ? / 'file.txt': first get ?, then get 'file.txt', then compute ? / 'file.txt'
#   base / 'documents': first get base, then get 'documents', then compute base / 'documents'
#   ? / 'file.txt': first get ?, then get 'file.txt', then compute ? / 'file.txt'
#   ? / 'file.txt': first get ?, then get 'file.txt', then compute ? / 'file.txt'
#   base / 'documents': first get base, then get 'documents', then compute base / 'documents'
#   base / 'documents': first get base, then get 'documents', then compute base / 'documents'
#   base / 'documents': first get base, then get 'documents', then compute base / 'documents'
#   base / 'documents': first get base, then get 'documents', then compute base / 'documents'
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Assign to base
#   3. Assign to full_path
#   4. Run print(full_path)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import Path from pathlib
#   8. Assign to base
#   9. Assign to full_path
#   10. Run print(full_path)
#   11. Import Path from pathlib
#   12. Assign to base
#   13. Assign to full_path
#   14. Run print(full_path)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      base = ... (stored in memory)
#      full_path = ... (stored in memory)
#      print(full_path)
#      base = ... (stored in memory)
#      full_path = ... (stored in memory)
#      print(full_path)
#      base = ... (stored in memory)
#      full_path = ... (stored in memory)
#      print(full_path)

"""

"""
Problem 1583:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#   2. Assign to data
#   3. Assign to json_string
#   4. Run print(json_string)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import json
#   8. Assign to data
#   9. Assign to json_string
#   10. Run print(json_string)
#   11. Import json
#   12. Assign to data
#   13. Assign to json_string
#   14. Run print(json_string)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      json_string = ... (stored in memory)
#      print(json_string)
#      data = ... (stored in memory)
#      json_string = ... (stored in memory)
#      print(json_string)
#      data = ... (stored in memory)
#      json_string = ... (stored in memory)
#      print(json_string)

"""

"""
Problem 1584:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1585:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to text
#   3. Assign to new_text
#   4. Run print(new_text)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to text
#   9. Assign to new_text
#   10. Run print(new_text)
#   11. Import re
#   12. Assign to text
#   13. Assign to new_text
#   14. Run print(new_text)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      new_text = ... (stored in memory)
#      print(new_text)
#      text = ... (stored in memory)
#      new_text = ... (stored in memory)
#      print(new_text)
#      text = ... (stored in memory)
#      new_text = ... (stored in memory)
#      print(new_text)

"""

"""
Problem 1586:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to text
#   3. Assign to pattern
#   4. Assign to matches
#   5. Run print(matches)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import re
#   9. Assign to text
#   10. Assign to pattern
#   11. Assign to matches
#   12. Run print(matches)
#   13. Import re
#   14. Assign to text
#   15. Assign to pattern
#   16. Assign to matches
#   17. Run print(matches)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      pattern = ... (stored in memory)
#      matches = ... (stored in memory)
#      print(matches)
#      text = ... (stored in memory)
#      pattern = ... (stored in memory)
#      matches = ... (stored in memory)
#      print(matches)
#      text = ... (stored in memory)
#      pattern = ... (stored in memory)
#      matches = ... (stored in memory)
#      print(matches)

"""

"""
Problem 1587:
#
# ===== SYNTAX =====
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   print(match.group('name')): inner expression resolves first, then print() outputs
#   print(match.group('age')): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to text
#   3. Assign to pattern
#   4. Assign to match
#   5. If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      pattern = ... (stored in memory)
#      match = ... (stored in memory)
#      if checks a condition and runs ONE branch
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1588:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(list(od.keys(...))): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import OrderedDict from collections
#   2. Assign to od
#   3. Assign to ?
#   4. Assign to ?
#   5. Assign to ?
#   6. Run print(list(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      od = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(list(...))

"""

"""
Problem 1589:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(dict(chain)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import ChainMap from collections
#   2. Assign to dict1
#   3. Assign to dict2
#   4. Assign to chain
#   5. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      chain = ... (stored in memory)
#      print(dict(...))

"""

"""
Problem 1590:
#
# ===== SYNTAX =====
#   def solve()
#   namedtuple() — built-in function call
#   Point() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import namedtuple from collections
#   2. Assign to Point
#   3. Assign to p
#   4. Run print(?, ?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import namedtuple from collections
#   8. Assign to Point
#   9. Assign to p
#   10. Run print(?, ?)
#   11. Import namedtuple from collections
#   12. Assign to Point
#   13. Assign to p
#   14. Run print(?, ?)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      Point = ... (stored in memory)
#      p = ... (stored in memory)
#      print(?, ?)
#      Point = ... (stored in memory)
#      p = ... (stored in memory)
#      print(?, ?)
#      Point = ... (stored in memory)
#      p = ... (stored in memory)
#      print(?, ?)

"""

"""
Problem 1591:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#
# ===== EXECUTION ORDER =====
#   1. Assign to matrix
#   2. Run print(matrix)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to matrix
#   6. Run print(matrix)
#   7. Assign to matrix
#   8. Run print(matrix)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      matrix = ... (stored in memory)
#      print(matrix)
#      matrix = ... (stored in memory)
#      print(matrix)
#      matrix = ... (stored in memory)
#      print(matrix)

"""

"""
Problem 1592:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 3: first get x, then get 3, then compute x % 3
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to filtered
#   3. Run print(filtered)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to filtered
#   8. Run print(filtered)
#   9. Assign to numbers
#   10. Assign to filtered
#   11. Run print(filtered)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      filtered = ... (stored in memory)
#      print(filtered)
#      numbers = ... (stored in memory)
#      filtered = ... (stored in memory)
#      print(filtered)
#      numbers = ... (stored in memory)
#      filtered = ... (stored in memory)
#      print(filtered)

"""

"""
Problem 1593:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#     def __init__ — method of Person
#   def solve()
#   sorted() — built-in function call
#   Person() — built-in function call
#   attrgetter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(Person('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import attrgetter from operator
#   2. Define class Person
#   3. Assign to people
#   4. Assign to sorted_people
#   5. Run print(?)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to obj
#   9. Run print(obj)
#   10. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   Person.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      sorted_people = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))

"""

"""
Problem 1594:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#     def greet — method of Person
#   def solve()
#   return — exits the function and sends back a value
#   list() — built-in function call
#   Person() — built-in function call
#   map() — built-in function call
#   methodcaller() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Person()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import methodcaller from operator
#   2. Define class Person
#   3. Assign to people
#   4. Assign to greetings
#   5. Run print(greetings)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to obj
#   9. Run print(obj)
#   10. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      greetings = ... (stored in memory)
#      print(greetings)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))

"""

"""
Problem 1595:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1596:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1597:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to parts
#   3. Run print(parts)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to parts
#   8. Run print(parts)
#   9. Assign to text
#   10. Assign to parts
#   11. Run print(parts)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      parts = ... (stored in memory)
#      print(parts)
#      text = ... (stored in memory)
#      parts = ... (stored in memory)
#      print(parts)
#      text = ... (stored in memory)
#      parts = ... (stored in memory)
#      print(parts)

"""

"""
Problem 1598:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to original
#   2. Assign to copied
#   3. Assign to ?
#   4. Run print(original, copied)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to original
#   8. Assign to copied
#   9. Assign to ?
#   10. Run print(original, copied)
#   11. Assign to original
#   12. Assign to copied
#   13. Assign to ?
#   14. Run print(original, copied)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      original = ... (stored in memory)
#      copied = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(original, copied)
#      original = ... (stored in memory)
#      copied = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(original, copied)
#      original = ... (stored in memory)
#      copied = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(original, copied)

"""

"""
Problem 1599:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to my_list
#   2. Run my_list.clear()
#   3. Run print(my_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to my_list
#   7. Run my_list.clear()
#   8. Run print(my_list)
#   9. Assign to my_list
#   10. Run my_list.clear()
#   11. Run print(my_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      my_list.clear()
#      print(my_list)
#      my_list = ... (stored in memory)
#      my_list.clear()
#      print(my_list)
#      my_list = ... (stored in memory)
#      my_list.clear()
#      print(my_list)

"""

"""
Problem 1600:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to item
#   3. Run print(item, d)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to item
#   8. Run print(item, d)
#   9. Assign to d
#   10. Assign to item
#   11. Run print(item, d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      item = ... (stored in memory)
#      print(item, d)
#      d = ... (stored in memory)
#      item = ... (stored in memory)
#      print(item, d)
#      d = ... (stored in memory)
#      item = ... (stored in memory)
#      print(item, d)

"""

"""
Problem 1601:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to dict1
#   2. Assign to dict2
#   3. Run dict1.update(dict2)
#   4. Run print(dict1)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Run dict1.update(dict2)
#   10. Run print(dict1)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Run dict1.update(dict2)
#   14. Run print(dict1)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      print(dict1)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      print(dict1)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      print(dict1)

"""

"""
Problem 1602:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to keys
#   3. Run print(keys)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to keys
#   8. Run print(keys)
#   9. Assign to d
#   10. Assign to keys
#   11. Run print(keys)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      keys = ... (stored in memory)
#      print(keys)
#      d = ... (stored in memory)
#      keys = ... (stored in memory)
#      print(keys)
#      d = ... (stored in memory)
#      keys = ... (stored in memory)
#      print(keys)

"""

"""
Problem 1603:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to values
#   3. Run print(values)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to values
#   8. Run print(values)
#   9. Assign to d
#   10. Assign to values
#   11. Run print(values)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      values = ... (stored in memory)
#      print(values)
#      d = ... (stored in memory)
#      values = ... (stored in memory)
#      print(values)
#      d = ... (stored in memory)
#      values = ... (stored in memory)
#      print(values)

"""

"""
Problem 1604:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to items
#   3. Run print(items)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to items
#   8. Run print(items)
#   9. Assign to d
#   10. Assign to items
#   11. Run print(items)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      items = ... (stored in memory)
#      print(items)
#      d = ... (stored in memory)
#      items = ... (stored in memory)
#      print(items)
#      d = ... (stored in memory)
#      items = ... (stored in memory)
#      print(items)

"""

"""
Problem 1605:
#
# ===== SYNTAX =====
#   class CustomError(Exception) — inherits from parent
#   def solve()
#   CustomError() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   print(CustomError()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class CustomError(Exception)
#   2. Try block (error handling)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(CustomError(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      obj = ... (stored in memory)
#      print(obj)
#      print(CustomError(...))

"""

"""
Problem 1606:
#
# ===== SYNTAX =====
#   def solve()
#   int() — built-in function call
#   TypeError() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1607:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import redirect_stdout from contextlib
#   2. Import StringIO from io
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1608:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import wraps from functools
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1609:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   accumulate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import accumulate from itertools
#   2. Import operator
#   3. Assign to numbers
#   4. Assign to cumulative_products
#   5. Run print(cumulative_products)
#   6. Define def main() — stored for later
#   7. Run main()
#         7.1 Pass (placeholder)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      cumulative_products = ... (stored in memory)
#      print(cumulative_products)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1610:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   combinations_with_replacement() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import combinations_with_replacement from itertools
#   2. Assign to items
#   3. Assign to combs
#   4. Run print(combs)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import combinations_with_replacement from itertools
#   8. Assign to items
#   9. Assign to combs
#   10. Run print(combs)
#   11. Import combinations_with_replacement from itertools
#   12. Assign to items
#   13. Assign to combs
#   14. Run print(combs)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      combs = ... (stored in memory)
#      print(combs)
#      items = ... (stored in memory)
#      combs = ... (stored in memory)
#      print(combs)
#      items = ... (stored in memory)
#      combs = ... (stored in memory)
#      print(combs)

"""

"""
Problem 1611:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   dropwhile() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: <
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import dropwhile from itertools
#   2. Assign to numbers
#   3. Assign to result
#   4. Run print(result)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import dropwhile from itertools
#   8. Assign to numbers
#   9. Assign to result
#   10. Run print(result)
#   11. Import dropwhile from itertools
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1612:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   takewhile() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: <
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import takewhile from itertools
#   2. Assign to numbers
#   3. Assign to result
#   4. Run print(result)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import takewhile from itertools
#   8. Assign to numbers
#   9. Assign to result
#   10. Run print(result)
#   11. Import takewhile from itertools
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1613:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   starmap() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import starmap from itertools
#   2. Import operator
#   3. Assign to pairs
#   4. Assign to products
#   5. Run print(products)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import starmap from itertools
#   9. Import operator
#   10. Assign to pairs
#   11. Assign to products
#   12. Run print(products)
#   13. Import starmap from itertools
#   14. Import operator
#   15. Assign to pairs
#   16. Assign to products
#   17. Run print(products)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      pairs = ... (stored in memory)
#      products = ... (stored in memory)
#      print(products)
#      pairs = ... (stored in memory)
#      products = ... (stored in memory)
#      print(products)
#      pairs = ... (stored in memory)
#      products = ... (stored in memory)
#      print(products)

"""

"""
Problem 1614:
#
# ===== SYNTAX =====
#   def compare(x, y)
#   return — exits the function and sends back a value
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   compare(x, y): looks up variable 'x', looks up variable 'y', then runs the function body
#   print(compare(3, 14)): the inner compare() executes FIRST, then print() outputs the result
#   y - x: first get y, then get x, then compute y - x
#   y + -x: first get y, then get -x, then compute y + -x
#   compare(3, 14): gets the literal 3, gets the literal 14, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import cmp_to_key from functools
#   2. Define def compare(x, y) — stored for later
#   3. Assign to numbers
#   4. Assign to sorted_nums
#   5. Run print(sorted_nums)
#   6. Define def compare(x, y) — stored for later
#   7. Define def compare(x, y) — stored for later
#   8. Assign to x
#   9. Assign to y
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(compare(...))
#       calls compare with: x=3, y=14
#         12.1 Import operator
#         12.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions compare, compare, compare are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sorted_nums = ... (stored in memory)
#      print(sorted_nums)
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(compare(3, 14)) → function runs first, then print shows the result

"""

"""
Problem 1615:
#
# ===== SYNTAX =====
#   def process(value)
#   return — exits the function and sends back a value
#   type() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(process(42)): the inner process() executes FIRST, then print() outputs the result
#   print(process('hello')): the inner process() executes FIRST, then print() outputs the result
#   process(value): looks up variable 'value', then runs the function body
#   print(process(25)): the inner process() executes FIRST, then print() outputs the result
#   process(42): gets the literal 42, then runs the function body
#   process('hello'): gets the literal 'hello', then runs the function body
#   process(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import singledispatch from functools
#   2. Define def process(value) — stored for later
#   3. Define def _(value) — stored for later
#   4. Define def _(value) — stored for later
#   5. Run print(process(...), process(...))
#       calls process with: value=42
#         5.1 Return from function
#   6. Define def process(value) — stored for later
#   7. Define def process(value) — stored for later
#   8. Assign to value
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(process(...))
#       calls process with: value=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions process, _, _, process, process are just stored
#   Execution starts at the non-def lines below:
#      print(process(42)) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(process(25)) → function runs first, then print shows the result

"""

"""
Problem 1616:
#
# ===== SYNTAX =====
#   def process_scores(scores)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#
# ===== EVALUATION ORDER =====
#   process_scores(?): resolves the value, then runs the function body
#   process_scores(scores): looks up variable 'scores', then runs the function body
#   print(process_scores(?)): the inner process_scores() executes FIRST, then print() outputs the result
#   process_scores(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import Dict from typing
#   2. Define def process_scores(scores) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def process_scores(scores) — stored for later
#   6. Define def process_scores(scores) — stored for later
#   7. Assign to scores
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(process_scores(...))
#       calls process_scores with: scores=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions process_scores, process_scores, process_scores are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      scores = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(process_scores(?)) → function runs first, then print shows the result

"""

"""
Problem 1617:
#
# ===== SYNTAX =====
#   def get_name_age()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_name_age(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Tuple from typing
#   2. Define def get_name_age() — stored for later
#   3. Assign to ?
#   4. Run print(?)
#   5. Define def get_name_age() — stored for later
#   6. Define def get_name_age() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_name_age, get_name_age, get_name_age are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1618:
#
# ===== SYNTAX =====
#   def apply_operation(x, y, op)
#   return — exits the function and sends back a value
#   op() — built-in function call
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   apply_operation(5, 3, ?): gets the literal 5, gets the literal 3, resolves the value, then runs the function body
#   apply_operation(x, y, op): looks up variable 'x', looks up variable 'y', looks up variable 'op', then runs the function body
#   print(apply_operation(3, 14, 25)): the inner apply_operation() executes FIRST, then print() outputs the result
#   apply_operation(3, 14, 25): gets the literal 3, gets the literal 14, gets the literal 25, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#
# ===== EXECUTION ORDER =====
#   1. Import Callable from typing
#   2. Define def apply_operation(x, y, op) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def apply_operation(x, y, op) — stored for later
#   6. Define def apply_operation(x, y, op) — stored for later
#   7. Assign to x
#   8. Assign to y
#   9. Assign to op
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(apply_operation(...))
#       calls apply_operation with: x=3, y=14, op=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions apply_operation, apply_operation, apply_operation are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      op = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(apply_operation(3, 14, 25)) → function runs first, then print shows the result

"""

"""
Problem 1619:
#
# ===== SYNTAX =====
#   def categorize(value)
#   return — exits the function and sends back a value
#   operators: == < >
#
# ===== EVALUATION ORDER =====
#   print(categorize(?)): the inner categorize() executes FIRST, then print() outputs the result
#   print(categorize(0)): the inner categorize() executes FIRST, then print() outputs the result
#   print(categorize(5)): the inner categorize() executes FIRST, then print() outputs the result
#   categorize(value): looks up variable 'value', then runs the function body
#   print(categorize(25)): the inner categorize() executes FIRST, then print() outputs the result
#   categorize(?): resolves the value, then runs the function body
#   categorize(0): gets the literal 0, then runs the function body
#   categorize(5): gets the literal 5, then runs the function body
#   categorize(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def categorize(value) — stored for later
#   2. Run print(categorize(...), categorize(...), categorize(...))
#       calls categorize with: value=-5
#         2.1 Return from function
#   3. Define def categorize(value) — stored for later
#   4. Define def categorize(value) — stored for later
#   5. Assign to value
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(categorize(...))
#       calls categorize with: value=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions categorize, categorize, categorize are just stored
#   Execution starts at the non-def lines below:
#      print(categorize(?)) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(categorize(25)) → function runs first, then print shows the result

"""

"""
Problem 1620:
#
# ===== SYNTAX =====
#   def handle_status(code)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(handle_status(200)): the inner handle_status() executes FIRST, then print() outputs the result
#   print(handle_status(999)): the inner handle_status() executes FIRST, then print() outputs the result
#   handle_status(code): looks up variable 'code', then runs the function body
#   print(handle_status(25)): the inner handle_status() executes FIRST, then print() outputs the result
#   handle_status(200): gets the literal 200, then runs the function body
#   handle_status(999): gets the literal 999, then runs the function body
#   handle_status(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def handle_status(code) — stored for later
#   2. Run print(handle_status(...), handle_status(...))
#       calls handle_status with: code=200
#         2.1 Return from function
#   3. Define def handle_status(code) — stored for later
#   4. Define def handle_status(code) — stored for later
#   5. Assign to code
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(handle_status(...))
#       calls handle_status with: code=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions handle_status, handle_status, handle_status are just stored
#   Execution starts at the non-def lines below:
#      print(handle_status(200)) → function runs first, then print shows the result
#      code = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(handle_status(25)) → function runs first, then print shows the result

"""

"""
Problem 1621:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#     def __init__ — method of Person
#     def name — method of Person
#     def name — method of Person
#   def solve()
#   return — exits the function and sends back a value
#   Person() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Person('Bob')): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Person
#   2. Assign to p
#   3. Delete
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Person.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      p = ... (stored in memory)
#      top-level statement runs
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))

"""

"""
Problem 1622:
#
# ===== SYNTAX =====
#   class MyDict — defines a new type
#     def __init__ — method of MyDict
#     def __setitem__ — method of MyDict
#     def __getitem__ — method of MyDict
#   def solve()
#   return — exits the function and sends back a value
#   MyDict() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(MyDict()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class MyDict
#   2. Assign to my_dict
#   3. Assign to ?
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(MyDict(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   MyDict.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_dict = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(MyDict(...))

"""

"""
Problem 1623:
#
# ===== SYNTAX =====
#   class MyList — defines a new type
#     def __init__ — method of MyList
#     def __delitem__ — method of MyList
#   def solve()
#   MyList() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(MyList(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class MyList
#   2. Assign to my_list
#   3. Delete
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(MyList(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   MyList.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      top-level statement runs
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(MyList(...))

"""

"""
Problem 1624:
#
# ===== SYNTAX =====
#   class MyContainer — defines a new type
#     def __init__ — method of MyContainer
#     def __contains__ — method of MyContainer
#   def solve()
#   return — exits the function and sends back a value
#   MyContainer() — built-in function call
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(MyContainer(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class MyContainer
#   2. Assign to container
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to obj
#   7. Run print(obj)
#   8. Run print(MyContainer(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   MyContainer.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      container = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(MyContainer(...))

"""

"""
Problem 1625:
#
# ===== SYNTAX =====
#   class CountDown — defines a new type
#     def __init__ — method of CountDown
#     def __iter__ — method of CountDown
#     def __next__ — method of CountDown
#   if/elif/else
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   print(list(counter)): inner expression resolves first, then print() outputs
#   print(CountDown(3)): inner expression resolves first, then print() outputs
#   self.current + 1: first get self.current, then get 1, then compute self.current + 1
#   self.current <= 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define class CountDown
#   2. Assign to counter
#   3. Run print(list(...))
#   4. Assign to obj
#   5. Run print(obj)
#   6. Run print(CountDown(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   CountDown.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      counter = ... (stored in memory)
#      print(list(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(CountDown(...))
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1626:
#
# ===== SYNTAX =====
#   def my_context()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   with
#
# ===== EVALUATION ORDER =====
#   my_context(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import contextmanager from contextlib
#   2. Define def my_context() — stored for later
#   3. With
#   4. Define def my_context() — stored for later
#   5. Define def my_context() — stored for later
#   6. Run print('Entering context')
#   7. Evaluate expression
#   8. Run print('Exiting context')
#   9. Run print('Entering context')
#   10. Evaluate expression
#   11. Run print('Exiting context')
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions my_context, my_context, my_context are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print('Entering context')
#      top-level statement runs
#      print('Exiting context')
#      print('Entering context')
#      top-level statement runs
#      print('Exiting context')

"""

"""
Problem 1627:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1628:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1629:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1630:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#   2. Import Path from pathlib
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1631:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1632:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to pattern
#   3. Assign to text
#   4. Assign to matches
#   5. Run print(matches)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import re
#   9. Assign to pattern
#   10. Assign to text
#   11. Assign to matches
#   12. Run print(matches)
#   13. Import re
#   14. Assign to pattern
#   15. Assign to text
#   16. Assign to matches
#   17. Run print(matches)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      pattern = ... (stored in memory)
#      text = ... (stored in memory)
#      matches = ... (stored in memory)
#      print(matches)
#      pattern = ... (stored in memory)
#      text = ... (stored in memory)
#      matches = ... (stored in memory)
#      print(matches)
#      pattern = ... (stored in memory)
#      text = ... (stored in memory)
#      matches = ... (stored in memory)
#      print(matches)

"""

"""
Problem 1633:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to text
#   3. Assign to parts
#   4. Run print(parts)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to text
#   9. Assign to parts
#   10. Run print(parts)
#   11. Import re
#   12. Assign to text
#   13. Assign to parts
#   14. Run print(parts)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      parts = ... (stored in memory)
#      print(parts)
#      text = ... (stored in memory)
#      parts = ... (stored in memory)
#      print(parts)
#      text = ... (stored in memory)
#      parts = ... (stored in memory)
#      print(parts)

"""

"""
Problem 1634:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(list(od.keys(...))): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import OrderedDict from collections
#   2. Assign to od
#   3. Run od.move_to_end('a')
#   4. Run print(list(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      od = ... (stored in memory)
#      od.move_to_end('a')
#      print(list(...))

"""

"""
Problem 1635:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(dict(new_chain)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import ChainMap from collections
#   2. Assign to dict1
#   3. Assign to dict2
#   4. Assign to chain
#   5. Assign to new_chain
#   6. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      chain = ... (stored in memory)
#      new_chain = ... (stored in memory)
#      print(dict(...))

"""

"""
Problem 1636:
#
# ===== SYNTAX =====
#   def solve()
#   namedtuple() — built-in function call
#   Point() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import namedtuple from collections
#   2. Assign to Point
#   3. Assign to p1
#   4. Assign to p2
#   5. Run print(p1, p2)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import namedtuple from collections
#   9. Assign to Point
#   10. Assign to p1
#   11. Assign to p2
#   12. Run print(p1, p2)
#   13. Import namedtuple from collections
#   14. Assign to Point
#   15. Assign to p1
#   16. Assign to p2
#   17. Run print(p1, p2)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      Point = ... (stored in memory)
#      p1 = ... (stored in memory)
#      p2 = ... (stored in memory)
#      print(p1, p2)
#      Point = ... (stored in memory)
#      p1 = ... (stored in memory)
#      p2 = ... (stored in memory)
#      print(p1, p2)
#      Point = ... (stored in memory)
#      p1 = ... (stored in memory)
#      p2 = ... (stored in memory)
#      print(p1, p2)

"""

"""
Problem 1637:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#   i * j: first get i, then get j, then compute i * j
#
# ===== EXECUTION ORDER =====
#   1. Assign to table
#   2. Run print(table)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to table
#   6. Run print(table)
#   7. Assign to table
#   8. Run print(table)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      table = ... (stored in memory)
#      print(table)
#      table = ... (stored in memory)
#      print(table)
#      table = ... (stored in memory)
#      print(table)

"""

"""
Problem 1638:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(sorted(unique_chars)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to strings
#   2. Assign to unique_chars
#   3. Run print(sorted(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      unique_chars = ... (stored in memory)
#      print(sorted(...))

"""

"""
Problem 1639:
#
# ===== SYNTAX =====
#   def solve()
#   reduce() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Import operator
#   3. Assign to numbers
#   4. Assign to total
#   5. Run print(total)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import reduce from functools
#   9. Import operator
#   10. Assign to numbers
#   11. Assign to total
#   12. Run print(total)
#   13. Import reduce from functools
#   14. Import operator
#   15. Assign to numbers
#   16. Assign to total
#   17. Run print(total)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)
#      numbers = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)
#      numbers = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)

"""

"""
Problem 1640:
#
# ===== SYNTAX =====
#   def solve()
#   reduce() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Import operator
#   3. Assign to numbers
#   4. Assign to product
#   5. Run print(product)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import reduce from functools
#   9. Import operator
#   10. Assign to numbers
#   11. Assign to product
#   12. Run print(product)
#   13. Import reduce from functools
#   14. Import operator
#   15. Assign to numbers
#   16. Assign to product
#   17. Run print(product)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      product = ... (stored in memory)
#      print(product)
#      numbers = ... (stored in memory)
#      product = ... (stored in memory)
#      print(product)
#      numbers = ... (stored in memory)
#      product = ... (stored in memory)
#      print(product)

"""

"""
Problem 1641:
#
# ===== SYNTAX =====
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to str1
#   2. Assign to str2
#   3. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      print(?)

"""

"""
Problem 1642:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to encoded
#   3. Run print(encoded)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to encoded
#   8. Run print(encoded)
#   9. Assign to text
#   10. Assign to encoded
#   11. Run print(encoded)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      encoded = ... (stored in memory)
#      print(encoded)
#      text = ... (stored in memory)
#      encoded = ... (stored in memory)
#      print(encoded)
#      text = ... (stored in memory)
#      encoded = ... (stored in memory)
#      print(encoded)

"""

"""
Problem 1643:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to bytes_data
#   2. Assign to decoded
#   3. Run print(decoded)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to bytes_data
#   7. Assign to decoded
#   8. Run print(decoded)
#   9. Assign to bytes_data
#   10. Assign to decoded
#   11. Run print(decoded)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      bytes_data = ... (stored in memory)
#      decoded = ... (stored in memory)
#      print(decoded)
#      bytes_data = ... (stored in memory)
#      decoded = ... (stored in memory)
#      print(decoded)
#      bytes_data = ... (stored in memory)
#      decoded = ... (stored in memory)
#      print(decoded)

"""

"""
Problem 1644:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to my_list
#   2. Run my_list.extend(?)
#   3. Run print(my_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to my_list
#   7. Run my_list.extend(?)
#   8. Run print(my_list)
#   9. Assign to my_list
#   10. Run my_list.extend(?)
#   11. Run print(my_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      my_list.extend(?)
#      print(my_list)
#      my_list = ... (stored in memory)
#      my_list.extend(?)
#      print(my_list)
#      my_list = ... (stored in memory)
#      my_list.extend(?)
#      print(my_list)

"""

"""
Problem 1645:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to my_list
#   2. Assign to index
#   3. Run print(index)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to my_list
#   7. Assign to index
#   8. Run print(index)
#   9. Assign to my_list
#   10. Assign to index
#   11. Run print(index)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      index = ... (stored in memory)
#      print(index)
#      my_list = ... (stored in memory)
#      index = ... (stored in memory)
#      print(index)
#      my_list = ... (stored in memory)
#      index = ... (stored in memory)
#      print(index)

"""

"""
Problem 1646:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to keys
#   2. Assign to d
#   3. Run print(d)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to keys
#   7. Assign to d
#   8. Run print(d)
#   9. Assign to keys
#   10. Assign to d
#   11. Run print(d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      keys = ... (stored in memory)
#      d = ... (stored in memory)
#      print(d)
#      keys = ... (stored in memory)
#      d = ... (stored in memory)
#      print(d)
#      keys = ... (stored in memory)
#      d = ... (stored in memory)
#      print(d)

"""

"""
Problem 1647:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   try/except
#   operators: /
#
# ===== EVALUATION ORDER =====
#   10 / 2: first get 10, then get 2, then compute 10 / 2
#   10 / 2: first get 10, then get 2, then compute 10 / 2
#   10 / 2: first get 10, then get 2, then compute 10 / 2
#   10 / 2: first get 10, then get 2, then compute 10 / 2
#   10 / 2: first get 10, then get 2, then compute 10 / 2
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1648:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   try/except
#   operators: /
#
# ===== EVALUATION ORDER =====
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1649:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import suppress from contextlib
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1650:
#
# ===== SYNTAX =====
#   def subtract_numbers(a, b)
#   return — exits the function and sends back a value
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   subtract_numbers(10, 3): gets the literal 10, gets the literal 3, then runs the function body
#   subtract_numbers(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(subtract_numbers(20, 30)): the inner subtract_numbers() executes FIRST, then print() outputs the result
#   a - b: first get a, then get b, then compute a - b
#   a + -b: first get a, then get -b, then compute a + -b
#   subtract_numbers(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def subtract_numbers(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def subtract_numbers(a, b) — stored for later
#   5. Define def subtract_numbers(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(subtract_numbers(...))
#       calls subtract_numbers with: a=20, b=30
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions subtract_numbers, subtract_numbers, subtract_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(subtract_numbers(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1651:
#
# ===== SYNTAX =====
#   def divide_numbers(a, b)
#   return — exits the function and sends back a value
#   float() — built-in function call
#   Fraction() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   divide_numbers(10, 2): gets the literal 10, gets the literal 2, then runs the function body
#   divide_numbers(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(divide_numbers(20, 30)): the inner divide_numbers() executes FIRST, then print() outputs the result
#   a / b: first get a, then get b, then compute a / b
#   a * ?: first get a, then get ?, then compute a * ?
#   divide_numbers(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   1 / b: first get 1, then get b, then compute 1 / b
#
# ===== EXECUTION ORDER =====
#   1. Define def divide_numbers(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def divide_numbers(a, b) — stored for later
#   5. Define def divide_numbers(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(divide_numbers(...))
#       calls divide_numbers with: a=20, b=30
#         10.1 Import Fraction from fractions
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divide_numbers, divide_numbers, divide_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divide_numbers(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1652:
#
# ===== SYNTAX =====
#   def calculate_remainder(a, b)
#   return — exits the function and sends back a value
#   divmod() — built-in function call
#   operators: - * // %
#
# ===== EVALUATION ORDER =====
#   calculate_remainder(10, 3): gets the literal 10, gets the literal 3, then runs the function body
#   calculate_remainder(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_remainder(20, 30)): the inner calculate_remainder() executes FIRST, then print() outputs the result
#   a % b: first get a, then get b, then compute a % b
#   a - ?: first get a, then get ?, then compute a - ?
#   calculate_remainder(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   b * ?: first get b, then get ?, then compute b * ?
#   a // b: first get a, then get b, then compute a // b
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_remainder(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_remainder(a, b) — stored for later
#   5. Define def calculate_remainder(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_remainder(...))
#       calls calculate_remainder with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_remainder, calculate_remainder, calculate_remainder are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_remainder(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1653:
#
# ===== SYNTAX =====
#   def calculate_power(base, exponent)
#   return — exits the function and sends back a value
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   calculate_power(2, 3): gets the literal 2, gets the literal 3, then runs the function body
#   calculate_power(base, exponent): looks up variable 'base', looks up variable 'exponent', then runs the function body
#   print(calculate_power(10, 25)): the inner calculate_power() executes FIRST, then print() outputs the result
#   base ** exponent: first get base, then get exponent, then compute base ** exponent
#   calculate_power(10, 25): gets the literal 10, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_power(base, exponent) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_power(base, exponent) — stored for later
#   5. Define def calculate_power(base, exponent) — stored for later
#   6. Assign to base
#   7. Assign to exponent
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_power(...))
#       calls calculate_power with: base=10, exponent=25
#         10.1 Import math
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_power, calculate_power, calculate_power are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      base = ... (stored in memory)
#      exponent = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_power(10, 25)) → function runs first, then print shows the result

"""

"""
Problem 1654:
#
# ===== SYNTAX =====
#   def calculate_circle_area(radius)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_circle_area(5): gets the literal 5, then runs the function body
#   calculate_circle_area(radius): looks up variable 'radius', then runs the function body
#   print(calculate_circle_area(14)): the inner calculate_circle_area() executes FIRST, then print() outputs the result
#   ? * radius: first get ?, then get radius, then compute ? * radius
#   ? * radius: first get ?, then get radius, then compute ? * radius
#   ? * radius: first get ?, then get radius, then compute ? * radius
#   calculate_circle_area(14): gets the literal 14, then runs the function body
#   3.14 * radius: first get 3.14, then get radius, then compute 3.14 * radius
#   3.14 * radius: first get 3.14, then get radius, then compute 3.14 * radius
#   3.14 * radius: first get 3.14, then get radius, then compute 3.14 * radius
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_circle_area(radius) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_circle_area(radius) — stored for later
#   5. Define def calculate_circle_area(radius) — stored for later
#   6. Assign to radius
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_circle_area(...))
#       calls calculate_circle_area with: radius=14
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_circle_area, calculate_circle_area, calculate_circle_area are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      radius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_circle_area(14)) → function runs first, then print shows the result

"""

"""
Problem 1655:
#
# ===== SYNTAX =====
#   def calculate_rectangle_area(length, width)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_rectangle_area(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   calculate_rectangle_area(length, width): looks up variable 'length', looks up variable 'width', then runs the function body
#   print(calculate_rectangle_area(25, 25)): the inner calculate_rectangle_area() executes FIRST, then print() outputs the result
#   length * width: first get length, then get width, then compute length * width
#   calculate_rectangle_area(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_rectangle_area(length, width) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_rectangle_area(length, width) — stored for later
#   5. Define def calculate_rectangle_area(length, width) — stored for later
#   6. Assign to length
#   7. Assign to width
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_rectangle_area(...))
#       calls calculate_rectangle_area with: length=25, width=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_rectangle_area, calculate_rectangle_area, calculate_rectangle_area are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_rectangle_area(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1656:
#
# ===== SYNTAX =====
#   def calculate_triangle_area(base, height)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_triangle_area(4, 6): gets the literal 4, gets the literal 6, then runs the function body
#   calculate_triangle_area(base, height): looks up variable 'base', looks up variable 'height', then runs the function body
#   print(calculate_triangle_area(10, 20)): the inner calculate_triangle_area() executes FIRST, then print() outputs the result
#   ? * height: first get ?, then get height, then compute ? * height
#   ? * height: first get ?, then get height, then compute ? * height
#   ? * height: first get ?, then get height, then compute ? * height
#   calculate_triangle_area(10, 20): gets the literal 10, gets the literal 20, then runs the function body
#   0.5 * base: first get 0.5, then get base, then compute 0.5 * base
#   0.5 * base: first get 0.5, then get base, then compute 0.5 * base
#   0.5 * base: first get 0.5, then get base, then compute 0.5 * base
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_triangle_area(base, height) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_triangle_area(base, height) — stored for later
#   5. Define def calculate_triangle_area(base, height) — stored for later
#   6. Assign to base
#   7. Assign to height
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_triangle_area(...))
#       calls calculate_triangle_area with: base=10, height=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_triangle_area, calculate_triangle_area, calculate_triangle_area are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      base = ... (stored in memory)
#      height = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_triangle_area(10, 20)) → function runs first, then print shows the result

"""

"""
Problem 1657:
#
# ===== SYNTAX =====
#   def convert_celsius_to_fahrenheit(celsius)
#   return — exits the function and sends back a value
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   convert_celsius_to_fahrenheit(25): gets the literal 25, then runs the function body
#   convert_celsius_to_fahrenheit(celsius): looks up variable 'celsius', then runs the function body
#   print(convert_celsius_to_fahrenheit(25)): the inner convert_celsius_to_fahrenheit() executes FIRST, then print() outputs the result
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   convert_celsius_to_fahrenheit(25): gets the literal 25, then runs the function body
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_celsius_to_fahrenheit(celsius) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_celsius_to_fahrenheit(celsius) — stored for later
#   5. Define def convert_celsius_to_fahrenheit(celsius) — stored for later
#   6. Assign to celsius
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_celsius_to_fahrenheit(...))
#       calls convert_celsius_to_fahrenheit with: celsius=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_celsius_to_fahrenheit, convert_celsius_to_fahrenheit, convert_celsius_to_fahrenheit are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      celsius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_celsius_to_fahrenheit(25)) → function runs first, then print shows the result

"""

"""
Problem 1658:
#
# ===== SYNTAX =====
#   def convert_fahrenheit_to_celsius(fahrenheit)
#   return — exits the function and sends back a value
#   operators: - * /
#
# ===== EVALUATION ORDER =====
#   convert_fahrenheit_to_celsius(77): gets the literal 77, then runs the function body
#   convert_fahrenheit_to_celsius(fahrenheit): looks up variable 'fahrenheit', then runs the function body
#   print(convert_fahrenheit_to_celsius(25)): the inner convert_fahrenheit_to_celsius() executes FIRST, then print() outputs the result
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   convert_fahrenheit_to_celsius(25): gets the literal 25, then runs the function body
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_fahrenheit_to_celsius(fahrenheit) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_fahrenheit_to_celsius(fahrenheit) — stored for later
#   5. Define def convert_fahrenheit_to_celsius(fahrenheit) — stored for later
#   6. Assign to fahrenheit
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_fahrenheit_to_celsius(...))
#       calls convert_fahrenheit_to_celsius with: fahrenheit=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_fahrenheit_to_celsius, convert_fahrenheit_to_celsius, convert_fahrenheit_to_celsius are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      fahrenheit = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_fahrenheit_to_celsius(25)) → function runs first, then print shows the result

"""

"""
Problem 1659:
#
# ===== SYNTAX =====
#   def is_positive(number)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: > <=
#
# ===== EVALUATION ORDER =====
#   print(is_positive(5)): the inner is_positive() executes FIRST, then print() outputs the result
#   print(is_positive(?)): the inner is_positive() executes FIRST, then print() outputs the result
#   is_positive(number): looks up variable 'number', then runs the function body
#   print(is_positive(25)): the inner is_positive() executes FIRST, then print() outputs the result
#   is_positive(5): gets the literal 5, then runs the function body
#   is_positive(?): resolves the value, then runs the function body
#   is_positive(25): gets the literal 25, then runs the function body
#   number > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_positive(number) — stored for later
#   2. Run print(is_positive(...), is_positive(...))
#       calls is_positive with: number=5
#         2.1 Return from function
#   3. Define def is_positive(number) — stored for later
#   4. Define def is_positive(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(is_positive(...))
#       calls is_positive with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_positive, is_positive, is_positive are just stored
#   Execution starts at the non-def lines below:
#      print(is_positive(5)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_positive(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1660:
#
# ===== SYNTAX =====
#   def is_negative(number)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: < >=
#
# ===== EVALUATION ORDER =====
#   print(is_negative(?)): the inner is_negative() executes FIRST, then print() outputs the result
#   print(is_negative(3)): the inner is_negative() executes FIRST, then print() outputs the result
#   is_negative(number): looks up variable 'number', then runs the function body
#   print(is_negative(25)): the inner is_negative() executes FIRST, then print() outputs the result
#   is_negative(?): resolves the value, then runs the function body
#   is_negative(3): gets the literal 3, then runs the function body
#   is_negative(25): gets the literal 25, then runs the function body
#   number < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_negative(number) — stored for later
#   2. Run print(is_negative(...), is_negative(...))
#       calls is_negative with: number=-5
#         2.1 Return from function
#   3. Define def is_negative(number) — stored for later
#   4. Define def is_negative(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(is_negative(...))
#       calls is_negative with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_negative, is_negative, is_negative are just stored
#   Execution starts at the non-def lines below:
#      print(is_negative(?)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_negative(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1661:
#
# ===== SYNTAX =====
#   def get_first_char(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_first_char('Hello'): gets the literal 'Hello', then runs the function body
#   get_first_char(text): looks up variable 'text', then runs the function body
#   print(get_first_char('world')): the inner get_first_char() executes FIRST, then print() outputs the result
#   get_first_char('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_first_char(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_first_char(text) — stored for later
#   5. Define def get_first_char(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_first_char(...))
#       calls get_first_char with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_first_char, get_first_char, get_first_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_first_char('world')) → function runs first, then print shows the result

"""

"""
Problem 1662:
#
# ===== SYNTAX =====
#   def get_last_char(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_last_char('Hello'): gets the literal 'Hello', then runs the function body
#   get_last_char(text): looks up variable 'text', then runs the function body
#   print(get_last_char('world')): the inner get_last_char() executes FIRST, then print() outputs the result
#   get_last_char('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_last_char(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_last_char(text) — stored for later
#   5. Define def get_last_char(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_last_char(...))
#       calls get_last_char with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_last_char, get_last_char, get_last_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_last_char('world')) → function runs first, then print shows the result

"""

"""
Problem 1663:
#
# ===== SYNTAX =====
#   def get_middle_char(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: //
#
# ===== EVALUATION ORDER =====
#   get_middle_char('Hello'): gets the literal 'Hello', then runs the function body
#   get_middle_char(text): looks up variable 'text', then runs the function body
#   print(get_middle_char('world')): the inner get_middle_char() executes FIRST, then print() outputs the result
#   get_middle_char('world'): gets the literal 'world', then runs the function body
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_middle_char(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_middle_char(text) — stored for later
#   5. Define def get_middle_char(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_middle_char(...))
#       calls get_middle_char with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_middle_char, get_middle_char, get_middle_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_middle_char('world')) → function runs first, then print shows the result

"""

"""
Problem 1664:
#
# ===== SYNTAX =====
#   def repeat_string(text, n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   repeat_string('Hi', 3): gets the literal 'Hi', gets the literal 3, then runs the function body
#   repeat_string(text, n): looks up variable 'text', looks up variable 'n', then runs the function body
#   print(repeat_string('world', 15)): the inner repeat_string() executes FIRST, then print() outputs the result
#   text * n: first get text, then get n, then compute text * n
#   repeat_string('world', 15): gets the literal 'world', gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def repeat_string(text, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def repeat_string(text, n) — stored for later
#   5. Define def repeat_string(text, n) — stored for later
#   6. Assign to text
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(repeat_string(...))
#       calls repeat_string with: text='world', n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions repeat_string, repeat_string, repeat_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(repeat_string('world', 15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1665:
#
# ===== SYNTAX =====
#   def swap_first_last(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   if/elif/else
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   swap_first_last('Hello'): gets the literal 'Hello', then runs the function body
#   swap_first_last(text): looks up variable 'text', then runs the function body
#   print(swap_first_last('world')): the inner swap_first_last() executes FIRST, then print() outputs the result
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   swap_first_last('world'): gets the literal 'world', then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   len(...) <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def swap_first_last(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def swap_first_last(text) — stored for later
#   5. Define def swap_first_last(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(swap_first_last(...))
#       calls swap_first_last with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions swap_first_last, swap_first_last, swap_first_last are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(swap_first_last('world')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1666:
#
# ===== SYNTAX =====
#   def remove_first_char(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_first_char('Hello'): gets the literal 'Hello', then runs the function body
#   remove_first_char(text): looks up variable 'text', then runs the function body
#   print(remove_first_char('world')): the inner remove_first_char() executes FIRST, then print() outputs the result
#   remove_first_char('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_first_char(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_first_char(text) — stored for later
#   5. Define def remove_first_char(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_first_char(...))
#       calls remove_first_char with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_first_char, remove_first_char, remove_first_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_first_char('world')) → function runs first, then print shows the result

"""

"""
Problem 1667:
#
# ===== SYNTAX =====
#   def remove_last_char(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_last_char('Hello'): gets the literal 'Hello', then runs the function body
#   remove_last_char(text): looks up variable 'text', then runs the function body
#   print(remove_last_char('world')): the inner remove_last_char() executes FIRST, then print() outputs the result
#   remove_last_char('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_last_char(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_last_char(text) — stored for later
#   5. Define def remove_last_char(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_last_char(...))
#       calls remove_last_char with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_last_char, remove_last_char, remove_last_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_last_char('world')) → function runs first, then print shows the result

"""

"""
Problem 1668:
#
# ===== SYNTAX =====
#   def get_first_word(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_first_word('Hello World Python'): gets the literal 'Hello World Python', then runs the function body
#   get_first_word(text): looks up variable 'text', then runs the function body
#   print(get_first_word('world')): the inner get_first_word() executes FIRST, then print() outputs the result
#   get_first_word('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_first_word(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_first_word(text) — stored for later
#   5. Define def get_first_word(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_first_word(...))
#       calls get_first_word with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_first_word, get_first_word, get_first_word are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_first_word('world')) → function runs first, then print shows the result

"""

"""
Problem 1669:
#
# ===== SYNTAX =====
#   def get_last_word(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_last_word('Hello World Python'): gets the literal 'Hello World Python', then runs the function body
#   get_last_word(text): looks up variable 'text', then runs the function body
#   print(get_last_word('world')): the inner get_last_word() executes FIRST, then print() outputs the result
#   get_last_word('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_last_word(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_last_word(text) — stored for later
#   5. Define def get_last_word(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_last_word(...))
#       calls get_last_word with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_last_word, get_last_word, get_last_word are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_last_word('world')) → function runs first, then print shows the result

"""

"""
Problem 1670:
#
# ===== SYNTAX =====
#   def count_characters(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#
# ===== EVALUATION ORDER =====
#   count_characters('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_characters(text): looks up variable 'text', then runs the function body
#   print(count_characters('world')): the inner count_characters() executes FIRST, then print() outputs the result
#   count_characters('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_characters(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_characters(text) — stored for later
#   5. Define def count_characters(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_characters(...))
#       calls count_characters with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_characters, count_characters, count_characters are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_characters('world')) → function runs first, then print shows the result

"""

"""
Problem 1671:
#
# ===== SYNTAX =====
#   def add_to_list(my_list, element)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   add_to_list(?, 4): resolves the value, gets the literal 4, then runs the function body
#   add_to_list(my_list, element): looks up variable 'my_list', looks up variable 'element', then runs the function body
#   print(add_to_list(?, 25)): the inner add_to_list() executes FIRST, then print() outputs the result
#   add_to_list(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add_to_list(my_list, element) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def add_to_list(my_list, element) — stored for later
#   5. Define def add_to_list(my_list, element) — stored for later
#   6. Assign to my_list
#   7. Assign to element
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(add_to_list(...))
#       calls add_to_list with: my_list=[4, 1, 8, 6, 3], element=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_to_list, add_to_list, add_to_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_to_list(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1672:
#
# ===== SYNTAX =====
#   def get_first_element(my_list)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_first_element(?): resolves the value, then runs the function body
#   get_first_element(my_list): looks up variable 'my_list', then runs the function body
#   print(get_first_element(?)): the inner get_first_element() executes FIRST, then print() outputs the result
#   get_first_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_first_element(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_first_element(my_list) — stored for later
#   5. Define def get_first_element(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_first_element(...))
#       calls get_first_element with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_first_element, get_first_element, get_first_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_first_element(?)) → function runs first, then print shows the result

"""

"""
Problem 1673:
#
# ===== SYNTAX =====
#   def get_last_element(my_list)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_last_element(?): resolves the value, then runs the function body
#   get_last_element(my_list): looks up variable 'my_list', then runs the function body
#   print(get_last_element(?)): the inner get_last_element() executes FIRST, then print() outputs the result
#   get_last_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_last_element(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_last_element(my_list) — stored for later
#   5. Define def get_last_element(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_last_element(...))
#       calls get_last_element with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_last_element, get_last_element, get_last_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_last_element(?)) → function runs first, then print shows the result

"""

"""
Problem 1674:
#
# ===== SYNTAX =====
#   def get_middle_element(my_list)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: //
#
# ===== EVALUATION ORDER =====
#   get_middle_element(?): resolves the value, then runs the function body
#   get_middle_element(my_list): looks up variable 'my_list', then runs the function body
#   print(get_middle_element(?)): the inner get_middle_element() executes FIRST, then print() outputs the result
#   get_middle_element(?): resolves the value, then runs the function body
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_middle_element(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_middle_element(my_list) — stored for later
#   5. Define def get_middle_element(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_middle_element(...))
#       calls get_middle_element with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_middle_element, get_middle_element, get_middle_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_middle_element(?)) → function runs first, then print shows the result

"""

"""
Problem 1675:
#
# ===== SYNTAX =====
#   def remove_first_element(my_list)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_first_element(?): resolves the value, then runs the function body
#   remove_first_element(my_list): looks up variable 'my_list', then runs the function body
#   print(remove_first_element(?)): the inner remove_first_element() executes FIRST, then print() outputs the result
#   remove_first_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_first_element(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_first_element(my_list) — stored for later
#   5. Define def remove_first_element(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_first_element(...))
#       calls remove_first_element with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_first_element, remove_first_element, remove_first_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_first_element(?)) → function runs first, then print shows the result

"""

"""
Problem 1676:
#
# ===== SYNTAX =====
#   def remove_last_element(my_list)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_last_element(?): resolves the value, then runs the function body
#   remove_last_element(my_list): looks up variable 'my_list', then runs the function body
#   print(remove_last_element(?)): the inner remove_last_element() executes FIRST, then print() outputs the result
#   remove_last_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_last_element(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_last_element(my_list) — stored for later
#   5. Define def remove_last_element(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_last_element(...))
#       calls remove_last_element with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_last_element, remove_last_element, remove_last_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_last_element(?)) → function runs first, then print shows the result

"""

"""
Problem 1677:
#
# ===== SYNTAX =====
#   def get_list_slice(my_list, start, end)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_list_slice(?, 1, 4): resolves the value, gets the literal 1, gets the literal 4, then runs the function body
#   get_list_slice(my_list, start, end): looks up variable 'my_list', looks up variable 'start', looks up variable 'end', then runs the function body
#   print(get_list_slice(?, 3, 20)): the inner get_list_slice() executes FIRST, then print() outputs the result
#   get_list_slice(?, 3, 20): resolves the value, gets the literal 3, gets the literal 20, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_list_slice(my_list, start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_list_slice(my_list, start, end) — stored for later
#   5. Define def get_list_slice(my_list, start, end) — stored for later
#   6. Assign to my_list
#   7. Assign to start
#   8. Assign to end
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_list_slice(...))
#       calls get_list_slice with: my_list=[4, 1, 8, 6, 3], start=3, end=20
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_list_slice, get_list_slice, get_list_slice are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_list_slice(?, 3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1678:
#
# ===== SYNTAX =====
#   def combine_lists(list1, list2)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   combine_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(combine_lists(25, ?)): the inner combine_lists() executes FIRST, then print() outputs the result
#   list1 + list2: first get list1, then get list2, then compute list1 + list2
#   combine_lists(25, ?): gets the literal 25, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def combine_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def combine_lists(list1, list2) — stored for later
#   5. Define def combine_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(combine_lists(...))
#       calls combine_lists with: list1=25, list2=[4, 1, 8, 6, 3]
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions combine_lists, combine_lists, combine_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(combine_lists(25, ?)) → function runs first, then print shows the result

"""

"""
Problem 1679:
#
# ===== SYNTAX =====
#   def multiply_list(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   multiply_list(?): resolves the value, then runs the function body
#   multiply_list(numbers): looks up variable 'numbers', then runs the function body
#   print(multiply_list(?)): the inner multiply_list() executes FIRST, then print() outputs the result
#   multiply_list(?): resolves the value, then runs the function body
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_list(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def multiply_list(numbers) — stored for later
#   5. Define def multiply_list(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(multiply_list(...))
#       calls multiply_list with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_list, multiply_list, multiply_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_list(?)) → function runs first, then print shows the result

"""

"""
Problem 1680:
#
# ===== SYNTAX =====
#   def add_one_to_all(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   add_one_to_all(?): resolves the value, then runs the function body
#   add_one_to_all(numbers): looks up variable 'numbers', then runs the function body
#   print(add_one_to_all(?)): the inner add_one_to_all() executes FIRST, then print() outputs the result
#   add_one_to_all(?): resolves the value, then runs the function body
#   x + 1: first get x, then get 1, then compute x + 1
#   x + 1: first get x, then get 1, then compute x + 1
#   x + 1: first get x, then get 1, then compute x + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def add_one_to_all(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def add_one_to_all(numbers) — stored for later
#   5. Define def add_one_to_all(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(add_one_to_all(...))
#       calls add_one_to_all with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_one_to_all, add_one_to_all, add_one_to_all are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_one_to_all(?)) → function runs first, then print shows the result

"""

"""
Problem 1681:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 Run print(i)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. For loop over range(...)
#       4.1 Run print(i)
#   5. For loop over range(...)
#       5.1 Run print(i)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1682:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 If statement (one branch runs)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. For loop over range(...)
#       4.1 If statement (one branch runs)
#   5. For loop over range(...)
#       5.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1683:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 If statement (one branch runs)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. For loop over range(...)
#       4.1 If statement (one branch runs)
#   5. For loop over range(...)
#       5.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1684:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 Run print(?)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. For loop over range(...)
#       4.1 Run print(?)
#   5. For loop over range(...)
#       5.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1685:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. For loop over text
#       2.1 Run print(char)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to text
#   6. For loop over text
#       6.1 Run print(char)
#   7. Assign to text
#   8. For loop over text
#       8.1 Run print(char)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      for loop runs its body once per item
#      text = ... (stored in memory)
#      for loop runs its body once per item
#      text = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from text each round

"""

"""
Problem 1686:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to my_list
#   2. For loop over my_list
#       2.1 Run print(element)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to my_list
#   6. For loop over my_list
#       6.1 Run print(element)
#   7. Assign to my_list
#   8. For loop over my_list
#       8.1 Run print(element)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      for loop runs its body once per item
#      my_list = ... (stored in memory)
#      for loop runs its body once per item
#      my_list = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1687:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to total
#   2. For loop over range(...)
#       2.1 Update total += ...
#   3. Run print(total)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to total
#   7. For loop over range(...)
#       7.1 Update total += ...
#   8. Run print(total)
#   9. Assign to total
#   10. For loop over range(...)
#       10.1 Update total += ...
#   11. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1688:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to product
#   2. For loop over range(...)
#       2.1 Update product *= ...
#   3. Run print(product)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to product
#   7. For loop over range(...)
#       7.1 Update product *= ...
#   8. Run print(product)
#   9. Assign to product
#   10. For loop over range(...)
#       10.1 Update product *= ...
#   11. Run print(product)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      product = ... (stored in memory)
#      for loop runs its body once per item
#      print(product)
#      product = ... (stored in memory)
#      for loop runs its body once per item
#      print(product)
#      product = ... (stored in memory)
#      for loop runs its body once per item
#      print(product)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1689:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 Run print(i)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. For loop over range(...)
#       4.1 Run print(i)
#   5. For loop over range(...)
#       5.1 Run print(i)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1690:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 3: first get i, then get 3, then compute i % 3
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 If statement (one branch runs)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. For loop over range(...)
#       4.1 If statement (one branch runs)
#   5. For loop over range(...)
#       5.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1691:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1692:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to total
#   2. Assign to i
#   3. While loop
#   4. Run print(total)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to total
#   8. Assign to i
#   9. While loop
#   10. Run print(total)
#   11. Assign to total
#   12. Assign to i
#   13. While loop
#   14. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      total = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#      total = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#      total = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1693:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1694:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1695:
#
# ===== SYNTAX =====
#   def check_greater(a, b)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: > <=
#
# ===== EVALUATION ORDER =====
#   print(check_greater(5, 3)): the inner check_greater() executes FIRST, then print() outputs the result
#   print(check_greater(2, 4)): the inner check_greater() executes FIRST, then print() outputs the result
#   check_greater(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_greater(20, 30)): the inner check_greater() executes FIRST, then print() outputs the result
#   check_greater(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   check_greater(2, 4): gets the literal 2, gets the literal 4, then runs the function body
#   check_greater(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a > b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_greater(a, b) — stored for later
#   2. Run print(check_greater(...), check_greater(...))
#       calls check_greater with: a=5, b=3
#         2.1 Return from function
#   3. Define def check_greater(a, b) — stored for later
#   4. Define def check_greater(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_greater(...))
#       calls check_greater with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_greater, check_greater, check_greater are just stored
#   Execution starts at the non-def lines below:
#      print(check_greater(5, 3)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_greater(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1696:
#
# ===== SYNTAX =====
#   def check_less(a, b)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: < >=
#
# ===== EVALUATION ORDER =====
#   print(check_less(3, 5)): the inner check_less() executes FIRST, then print() outputs the result
#   print(check_less(5, 3)): the inner check_less() executes FIRST, then print() outputs the result
#   check_less(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_less(20, 30)): the inner check_less() executes FIRST, then print() outputs the result
#   check_less(3, 5): gets the literal 3, gets the literal 5, then runs the function body
#   check_less(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   check_less(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a < b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_less(a, b) — stored for later
#   2. Run print(check_less(...), check_less(...))
#       calls check_less with: a=3, b=5
#         2.1 Return from function
#   3. Define def check_less(a, b) — stored for later
#   4. Define def check_less(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_less(...))
#       calls check_less with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_less, check_less, check_less are just stored
#   Execution starts at the non-def lines below:
#      print(check_less(3, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_less(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1697:
#
# ===== SYNTAX =====
#   def check_equal(a, b)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_equal(5, 5)): the inner check_equal() executes FIRST, then print() outputs the result
#   print(check_equal(5, 3)): the inner check_equal() executes FIRST, then print() outputs the result
#   check_equal(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_equal(20, 30)): the inner check_equal() executes FIRST, then print() outputs the result
#   check_equal(5, 5): gets the literal 5, gets the literal 5, then runs the function body
#   check_equal(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   check_equal(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a == b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_equal(a, b) — stored for later
#   2. Run print(check_equal(...), check_equal(...))
#       calls check_equal with: a=5, b=5
#         2.1 Return from function
#   3. Define def check_equal(a, b) — stored for later
#   4. Define def check_equal(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_equal(...))
#       calls check_equal with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_equal, check_equal, check_equal are just stored
#   Execution starts at the non-def lines below:
#      print(check_equal(5, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_equal(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1698:
#
# ===== SYNTAX =====
#   def check_not_equal(a, b)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: == !=
#
# ===== EVALUATION ORDER =====
#   print(check_not_equal(5, 3)): the inner check_not_equal() executes FIRST, then print() outputs the result
#   print(check_not_equal(5, 5)): the inner check_not_equal() executes FIRST, then print() outputs the result
#   check_not_equal(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_not_equal(20, 30)): the inner check_not_equal() executes FIRST, then print() outputs the result
#   check_not_equal(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   check_not_equal(5, 5): gets the literal 5, gets the literal 5, then runs the function body
#   check_not_equal(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a != b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_not_equal(a, b) — stored for later
#   2. Run print(check_not_equal(...), check_not_equal(...))
#       calls check_not_equal with: a=5, b=3
#         2.1 Return from function
#   3. Define def check_not_equal(a, b) — stored for later
#   4. Define def check_not_equal(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_not_equal(...))
#       calls check_not_equal with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_not_equal, check_not_equal, check_not_equal are just stored
#   Execution starts at the non-def lines below:
#      print(check_not_equal(5, 3)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_not_equal(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1699:
#
# ===== SYNTAX =====
#   def check_greater_equal(a, b)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: < >=
#
# ===== EVALUATION ORDER =====
#   print(check_greater_equal(5, 5)): the inner check_greater_equal() executes FIRST, then print() outputs the result
#   print(check_greater_equal(3, 5)): the inner check_greater_equal() executes FIRST, then print() outputs the result
#   check_greater_equal(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_greater_equal(20, 30)): the inner check_greater_equal() executes FIRST, then print() outputs the result
#   check_greater_equal(5, 5): gets the literal 5, gets the literal 5, then runs the function body
#   check_greater_equal(3, 5): gets the literal 3, gets the literal 5, then runs the function body
#   check_greater_equal(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a >= b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_greater_equal(a, b) — stored for later
#   2. Run print(check_greater_equal(...), check_greater_equal(...))
#       calls check_greater_equal with: a=5, b=5
#         2.1 Return from function
#   3. Define def check_greater_equal(a, b) — stored for later
#   4. Define def check_greater_equal(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_greater_equal(...))
#       calls check_greater_equal with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_greater_equal, check_greater_equal, check_greater_equal are just stored
#   Execution starts at the non-def lines below:
#      print(check_greater_equal(5, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_greater_equal(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1700:
#
# ===== SYNTAX =====
#   def check_less_equal(a, b)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: > <=
#
# ===== EVALUATION ORDER =====
#   print(check_less_equal(3, 5)): the inner check_less_equal() executes FIRST, then print() outputs the result
#   print(check_less_equal(5, 3)): the inner check_less_equal() executes FIRST, then print() outputs the result
#   check_less_equal(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_less_equal(20, 30)): the inner check_less_equal() executes FIRST, then print() outputs the result
#   check_less_equal(3, 5): gets the literal 3, gets the literal 5, then runs the function body
#   check_less_equal(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   check_less_equal(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a <= b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_less_equal(a, b) — stored for later
#   2. Run print(check_less_equal(...), check_less_equal(...))
#       calls check_less_equal with: a=3, b=5
#         2.1 Return from function
#   3. Define def check_less_equal(a, b) — stored for later
#   4. Define def check_less_equal(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_less_equal(...))
#       calls check_less_equal with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_less_equal, check_less_equal, check_less_equal are just stored
#   Execution starts at the non-def lines below:
#      print(check_less_equal(3, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_less_equal(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1701:
#
# ===== SYNTAX =====
#   def get_absolute_value(number)
#   return — exits the function and sends back a value
#   abs() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(get_absolute_value(?)): the inner get_absolute_value() executes FIRST, then print() outputs the result
#   print(get_absolute_value(5)): the inner get_absolute_value() executes FIRST, then print() outputs the result
#   get_absolute_value(number): looks up variable 'number', then runs the function body
#   print(get_absolute_value(25)): the inner get_absolute_value() executes FIRST, then print() outputs the result
#   get_absolute_value(?): resolves the value, then runs the function body
#   get_absolute_value(5): gets the literal 5, then runs the function body
#   get_absolute_value(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_absolute_value(number) — stored for later
#   2. Run print(get_absolute_value(...), get_absolute_value(...))
#       calls get_absolute_value with: number=-5
#         2.1 Return from function
#   3. Define def get_absolute_value(number) — stored for later
#   4. Define def get_absolute_value(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(get_absolute_value(...))
#       calls get_absolute_value with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_absolute_value, get_absolute_value, get_absolute_value are just stored
#   Execution starts at the non-def lines below:
#      print(get_absolute_value(?)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_absolute_value(25)) → function runs first, then print shows the result

"""

"""
Problem 1702:
#
# ===== SYNTAX =====
#   def round_number(number)
#   return — exits the function and sends back a value
#   round() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(round_number(3.7)): the inner round_number() executes FIRST, then print() outputs the result
#   print(round_number(3.2)): the inner round_number() executes FIRST, then print() outputs the result
#   round_number(number): looks up variable 'number', then runs the function body
#   print(round_number(25)): the inner round_number() executes FIRST, then print() outputs the result
#   round_number(3.7): gets the literal 3.7, then runs the function body
#   round_number(3.2): gets the literal 3.2, then runs the function body
#   round_number(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def round_number(number) — stored for later
#   2. Run print(round_number(...), round_number(...))
#       calls round_number with: number=3.7
#         2.1 Return from function
#   3. Define def round_number(number) — stored for later
#   4. Define def round_number(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(round_number(...))
#       calls round_number with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions round_number, round_number, round_number are just stored
#   Execution starts at the non-def lines below:
#      print(round_number(3.7)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(round_number(25)) → function runs first, then print shows the result

"""

"""
Problem 1703:
#
# ===== SYNTAX =====
#   def get_maximum(a, b, c)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_maximum(10, 5, 8): gets the literal 10, gets the literal 5, gets the literal 8, then runs the function body
#   get_maximum(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(get_maximum(20, 30, ?)): the inner get_maximum() executes FIRST, then print() outputs the result
#   get_maximum(20, 30, ?): gets the literal 20, gets the literal 30, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_maximum(a, b, c) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_maximum(a, b, c) — stored for later
#   5. Define def get_maximum(a, b, c) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to c
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_maximum(...))
#       calls get_maximum with: a=20, b=30, c=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_maximum, get_maximum, get_maximum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_maximum(20, 30, ?)) → function runs first, then print shows the result

"""

"""
Problem 1704:
#
# ===== SYNTAX =====
#   def get_minimum(a, b, c)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_minimum(10, 5, 8): gets the literal 10, gets the literal 5, gets the literal 8, then runs the function body
#   get_minimum(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(get_minimum(20, 30, ?)): the inner get_minimum() executes FIRST, then print() outputs the result
#   get_minimum(20, 30, ?): gets the literal 20, gets the literal 30, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_minimum(a, b, c) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_minimum(a, b, c) — stored for later
#   5. Define def get_minimum(a, b, c) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to c
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_minimum(...))
#       calls get_minimum with: a=20, b=30, c=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_minimum, get_minimum, get_minimum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_minimum(20, 30, ?)) → function runs first, then print shows the result

"""

"""
Problem 1705:
#
# ===== SYNTAX =====
#   def check_divisible(a, b)
#   return — exits the function and sends back a value
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(check_divisible(10, 2)): the inner check_divisible() executes FIRST, then print() outputs the result
#   print(check_divisible(10, 3)): the inner check_divisible() executes FIRST, then print() outputs the result
#   check_divisible(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(check_divisible(20, 30)): the inner check_divisible() executes FIRST, then print() outputs the result
#   check_divisible(10, 2): gets the literal 10, gets the literal 2, then runs the function body
#   check_divisible(10, 3): gets the literal 10, gets the literal 3, then runs the function body
#   check_divisible(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#
# ===== EXECUTION ORDER =====
#   1. Define def check_divisible(a, b) — stored for later
#   2. Run print(check_divisible(...), check_divisible(...))
#       calls check_divisible with: a=10, b=2
#         2.1 Return from function
#   3. Define def check_divisible(a, b) — stored for later
#   4. Define def check_divisible(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_divisible(...))
#       calls check_divisible with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_divisible, check_divisible, check_divisible are just stored
#   Execution starts at the non-def lines below:
#      print(check_divisible(10, 2)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_divisible(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1706:
#
# ===== SYNTAX =====
#   def convert_to_string(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   convert_to_string(123): gets the literal 123, then runs the function body
#   print(type(result)): inner expression resolves first, then print() outputs
#   convert_to_string(number): looks up variable 'number', then runs the function body
#   print(convert_to_string(25)): the inner convert_to_string() executes FIRST, then print() outputs the result
#   convert_to_string(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_string(number) — stored for later
#   2. Assign to result
#   3. Run print(result, type(...))
#   4. Define def convert_to_string(number) — stored for later
#   5. Define def convert_to_string(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_string(...))
#       calls convert_to_string with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_string, convert_to_string, convert_to_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result, type(...))
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_string(25)) → function runs first, then print shows the result

"""

"""
Problem 1707:
#
# ===== SYNTAX =====
#   def convert_to_integer(text)
#   return — exits the function and sends back a value
#   int() — built-in function call
#
# ===== EVALUATION ORDER =====
#   convert_to_integer('123'): gets the literal '123', then runs the function body
#   print(type(result)): inner expression resolves first, then print() outputs
#   convert_to_integer(text): looks up variable 'text', then runs the function body
#   print(convert_to_integer('world')): the inner convert_to_integer() executes FIRST, then print() outputs the result
#   convert_to_integer('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_integer(text) — stored for later
#   2. Assign to result
#   3. Run print(result, type(...))
#   4. Define def convert_to_integer(text) — stored for later
#   5. Define def convert_to_integer(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_integer(...))
#       calls convert_to_integer with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_integer, convert_to_integer, convert_to_integer are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result, type(...))
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_integer('world')) → function runs first, then print shows the result

"""

"""
Problem 1708:
#
# ===== SYNTAX =====
#   def convert_to_float(text)
#   return — exits the function and sends back a value
#   float() — built-in function call
#
# ===== EVALUATION ORDER =====
#   convert_to_float('3.14'): gets the literal '3.14', then runs the function body
#   print(type(result)): inner expression resolves first, then print() outputs
#   convert_to_float(text): looks up variable 'text', then runs the function body
#   print(convert_to_float('world')): the inner convert_to_float() executes FIRST, then print() outputs the result
#   convert_to_float('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_float(text) — stored for later
#   2. Assign to result
#   3. Run print(result, type(...))
#   4. Define def convert_to_float(text) — stored for later
#   5. Define def convert_to_float(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_float(...))
#       calls convert_to_float with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_float, convert_to_float, convert_to_float are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result, type(...))
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_float('world')) → function runs first, then print shows the result

"""

"""
Problem 1709:
#
# ===== SYNTAX =====
#   def get_type(value)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   type() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(get_type(5)): the inner get_type() executes FIRST, then print() outputs the result
#   print(get_type('hello')): the inner get_type() executes FIRST, then print() outputs the result
#   get_type(value): looks up variable 'value', then runs the function body
#   print(get_type(25)): the inner get_type() executes FIRST, then print() outputs the result
#   get_type(5): gets the literal 5, then runs the function body
#   get_type('hello'): gets the literal 'hello', then runs the function body
#   get_type(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_type(value) — stored for later
#   2. Run print(get_type(...), get_type(...))
#       calls get_type with: value=5
#         2.1 Return from function
#   3. Define def get_type(value) — stored for later
#   4. Define def get_type(value) — stored for later
#   5. Assign to value
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(get_type(...))
#       calls get_type with: value=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_type, get_type, get_type are just stored
#   Execution starts at the non-def lines below:
#      print(get_type(5)) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_type(25)) → function runs first, then print shows the result

"""

"""
Problem 1710:
#
# ===== SYNTAX =====
#   def check_is_integer(value)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(check_is_integer(5)): the inner check_is_integer() executes FIRST, then print() outputs the result
#   print(check_is_integer('5')): the inner check_is_integer() executes FIRST, then print() outputs the result
#   check_is_integer(value): looks up variable 'value', then runs the function body
#   print(check_is_integer(25)): the inner check_is_integer() executes FIRST, then print() outputs the result
#   check_is_integer(5): gets the literal 5, then runs the function body
#   check_is_integer('5'): gets the literal '5', then runs the function body
#   check_is_integer(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_is_integer(value) — stored for later
#   2. Run print(check_is_integer(...), check_is_integer(...))
#       calls check_is_integer with: value=5
#         2.1 Return from function
#   3. Define def check_is_integer(value) — stored for later
#   4. Define def check_is_integer(value) — stored for later
#   5. Assign to value
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_is_integer(...))
#       calls check_is_integer with: value=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_is_integer, check_is_integer, check_is_integer are just stored
#   Execution starts at the non-def lines below:
#      print(check_is_integer(5)) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_is_integer(25)) → function runs first, then print shows the result

"""

"""
Problem 1711:
#
# ===== SYNTAX =====
#   def check_is_string(value)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(check_is_string('hello')): the inner check_is_string() executes FIRST, then print() outputs the result
#   print(check_is_string(5)): the inner check_is_string() executes FIRST, then print() outputs the result
#   check_is_string(value): looks up variable 'value', then runs the function body
#   print(check_is_string(25)): the inner check_is_string() executes FIRST, then print() outputs the result
#   check_is_string('hello'): gets the literal 'hello', then runs the function body
#   check_is_string(5): gets the literal 5, then runs the function body
#   check_is_string(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_is_string(value) — stored for later
#   2. Run print(check_is_string(...), check_is_string(...))
#       calls check_is_string with: value='hello'
#         2.1 Return from function
#   3. Define def check_is_string(value) — stored for later
#   4. Define def check_is_string(value) — stored for later
#   5. Assign to value
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_is_string(...))
#       calls check_is_string with: value=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_is_string, check_is_string, check_is_string are just stored
#   Execution starts at the non-def lines below:
#      print(check_is_string('hello')) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_is_string(25)) → function runs first, then print shows the result

"""

"""
Problem 1712:
#
# ===== SYNTAX =====
#   def check_is_list(value)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(check_is_list(?)): the inner check_is_list() executes FIRST, then print() outputs the result
#   print(check_is_list('hello')): the inner check_is_list() executes FIRST, then print() outputs the result
#   check_is_list(value): looks up variable 'value', then runs the function body
#   print(check_is_list(25)): the inner check_is_list() executes FIRST, then print() outputs the result
#   check_is_list(?): resolves the value, then runs the function body
#   check_is_list('hello'): gets the literal 'hello', then runs the function body
#   check_is_list(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_is_list(value) — stored for later
#   2. Run print(check_is_list(...), check_is_list(...))
#       calls check_is_list with: value=[1, 2, 3]
#         2.1 Return from function
#   3. Define def check_is_list(value) — stored for later
#   4. Define def check_is_list(value) — stored for later
#   5. Assign to value
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_is_list(...))
#       calls check_is_list with: value=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_is_list, check_is_list, check_is_list are just stored
#   Execution starts at the non-def lines below:
#      print(check_is_list(?)) → function runs first, then print shows the result
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_is_list(25)) → function runs first, then print shows the result

"""

"""
Problem 1713:
#
# ===== SYNTAX =====
#   def format_greeting(name)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_greeting('Alice'): gets the literal 'Alice', then runs the function body
#   format_greeting(name): looks up variable 'name', then runs the function body
#   print(format_greeting('Bob')): the inner format_greeting() executes FIRST, then print() outputs the result
#   'Hello, %s!' % name: first get 'Hello, %s!', then get name, then compute 'Hello, %s!' % name
#   format_greeting('Bob'): gets the literal 'Bob', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_greeting(name) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_greeting(name) — stored for later
#   5. Define def format_greeting(name) — stored for later
#   6. Assign to name
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(format_greeting(...))
#       calls format_greeting with: name='Bob'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_greeting, format_greeting, format_greeting are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_greeting('Bob')) → function runs first, then print shows the result

"""

"""
Problem 1714:
#
# ===== SYNTAX =====
#   def format_age_message(name, age)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_age_message('Bob', 25): gets the literal 'Bob', gets the literal 25, then runs the function body
#   format_age_message(name, age): looks up variable 'name', looks up variable 'age', then runs the function body
#   print(format_age_message('Bob', 25)): the inner format_age_message() executes FIRST, then print() outputs the result
#   '%s is %s years old.' % (name, age): first get '%s is %s years old.', then get (name, age), then compute '%s is %s years old.' % (name, age)
#   format_age_message('Bob', 25): gets the literal 'Bob', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_age_message(name, age) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_age_message(name, age) — stored for later
#   5. Define def format_age_message(name, age) — stored for later
#   6. Assign to name
#   7. Assign to age
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(format_age_message(...))
#       calls format_age_message with: name='Bob', age=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_age_message, format_age_message, format_age_message are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      name = ... (stored in memory)
#      age = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_age_message('Bob', 25)) → function runs first, then print shows the result

"""

"""
Problem 1715:
#
# ===== SYNTAX =====
#   def format_calculation(num1, num2)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   format_calculation(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   format_calculation(num1, num2): looks up variable 'num1', looks up variable 'num2', then runs the function body
#   print(format_calculation(20, 30)): the inner format_calculation() executes FIRST, then print() outputs the result
#   format_calculation(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   num1 + num2: first get num1, then get num2, then compute num1 + num2
#   num1 + num2: first get num1, then get num2, then compute num1 + num2
#   num1 + num2: first get num1, then get num2, then compute num1 + num2
#
# ===== EXECUTION ORDER =====
#   1. Define def format_calculation(num1, num2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_calculation(num1, num2) — stored for later
#   5. Define def format_calculation(num1, num2) — stored for later
#   6. Assign to num1
#   7. Assign to num2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(format_calculation(...))
#       calls format_calculation with: num1=20, num2=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_calculation, format_calculation, format_calculation are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      num1 = ... (stored in memory)
#      num2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_calculation(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1716:
#
# ===== SYNTAX =====
#   def calculate_perimeter_rectangle(length, width)
#   return — exits the function and sends back a value
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   calculate_perimeter_rectangle(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   calculate_perimeter_rectangle(length, width): looks up variable 'length', looks up variable 'width', then runs the function body
#   print(calculate_perimeter_rectangle(25, 25)): the inner calculate_perimeter_rectangle() executes FIRST, then print() outputs the result
#   2 * ?: first get 2, then get ?, then compute 2 * ?
#   2 * ?: first get 2, then get ?, then compute 2 * ?
#   2 * ?: first get 2, then get ?, then compute 2 * ?
#   calculate_perimeter_rectangle(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   length + width: first get length, then get width, then compute length + width
#   length + width: first get length, then get width, then compute length + width
#   length + width: first get length, then get width, then compute length + width
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_perimeter_rectangle(length, width) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_perimeter_rectangle(length, width) — stored for later
#   5. Define def calculate_perimeter_rectangle(length, width) — stored for later
#   6. Assign to length
#   7. Assign to width
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_perimeter_rectangle(...))
#       calls calculate_perimeter_rectangle with: length=25, width=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_perimeter_rectangle, calculate_perimeter_rectangle, calculate_perimeter_rectangle are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_perimeter_rectangle(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1717:
#
# ===== SYNTAX =====
#   def calculate_perimeter_square(side)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_perimeter_square(5): gets the literal 5, then runs the function body
#   calculate_perimeter_square(side): looks up variable 'side', then runs the function body
#   print(calculate_perimeter_square(25)): the inner calculate_perimeter_square() executes FIRST, then print() outputs the result
#   4 * side: first get 4, then get side, then compute 4 * side
#   calculate_perimeter_square(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_perimeter_square(side) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_perimeter_square(side) — stored for later
#   5. Define def calculate_perimeter_square(side) — stored for later
#   6. Assign to side
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_perimeter_square(...))
#       calls calculate_perimeter_square with: side=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_perimeter_square, calculate_perimeter_square, calculate_perimeter_square are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      side = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_perimeter_square(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1718:
#
# ===== SYNTAX =====
#   def calculate_circle_circumference(radius)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_circle_circumference(5): gets the literal 5, then runs the function body
#   calculate_circle_circumference(radius): looks up variable 'radius', then runs the function body
#   print(calculate_circle_circumference(14)): the inner calculate_circle_circumference() executes FIRST, then print() outputs the result
#   ? * radius: first get ?, then get radius, then compute ? * radius
#   ? * radius: first get ?, then get radius, then compute ? * radius
#   ? * radius: first get ?, then get radius, then compute ? * radius
#   calculate_circle_circumference(14): gets the literal 14, then runs the function body
#   2 * 3.14: first get 2, then get 3.14, then compute 2 * 3.14
#   2 * 3.14: first get 2, then get 3.14, then compute 2 * 3.14
#   2 * 3.14: first get 2, then get 3.14, then compute 2 * 3.14
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_circle_circumference(radius) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_circle_circumference(radius) — stored for later
#   5. Define def calculate_circle_circumference(radius) — stored for later
#   6. Assign to radius
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_circle_circumference(...))
#       calls calculate_circle_circumference with: radius=14
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_circle_circumference, calculate_circle_circumference, calculate_circle_circumference are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      radius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_circle_circumference(14)) → function runs first, then print shows the result

"""

"""
Problem 1719:
#
# ===== SYNTAX =====
#   def convert_km_to_miles(km)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   convert_km_to_miles(10): gets the literal 10, then runs the function body
#   convert_km_to_miles(km): looks up variable 'km', then runs the function body
#   print(convert_km_to_miles(25)): the inner convert_km_to_miles() executes FIRST, then print() outputs the result
#   km * 0.621371: first get km, then get 0.621371, then compute km * 0.621371
#   km * 0.621371: first get km, then get 0.621371, then compute km * 0.621371
#   km * 0.621371: first get km, then get 0.621371, then compute km * 0.621371
#   convert_km_to_miles(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_km_to_miles(km) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_km_to_miles(km) — stored for later
#   5. Define def convert_km_to_miles(km) — stored for later
#   6. Assign to km
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_km_to_miles(...))
#       calls convert_km_to_miles with: km=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_km_to_miles, convert_km_to_miles, convert_km_to_miles are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      km = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_km_to_miles(25)) → function runs first, then print shows the result

"""

"""
Problem 1720:
#
# ===== SYNTAX =====
#   def convert_miles_to_km(miles)
#   return — exits the function and sends back a value
#   operators: /
#
# ===== EVALUATION ORDER =====
#   convert_miles_to_km(6.21371): gets the literal 6.21371, then runs the function body
#   print(round(result, 2)): inner expression resolves first, then print() outputs
#   convert_miles_to_km(miles): looks up variable 'miles', then runs the function body
#   print(convert_miles_to_km(25)): the inner convert_miles_to_km() executes FIRST, then print() outputs the result
#   miles / 0.621371: first get miles, then get 0.621371, then compute miles / 0.621371
#   miles / 0.621371: first get miles, then get 0.621371, then compute miles / 0.621371
#   miles / 0.621371: first get miles, then get 0.621371, then compute miles / 0.621371
#   convert_miles_to_km(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_miles_to_km(miles) — stored for later
#   2. Assign to result
#   3. Run print(round(...))
#   4. Define def convert_miles_to_km(miles) — stored for later
#   5. Define def convert_miles_to_km(miles) — stored for later
#   6. Assign to miles
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_miles_to_km(...))
#       calls convert_miles_to_km with: miles=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_miles_to_km, convert_miles_to_km, convert_miles_to_km are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(round(...))
#      miles = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_miles_to_km(25)) → function runs first, then print shows the result

"""

"""
Problem 1721:
#
# ===== SYNTAX =====
#   def convert_kg_to_pounds(kg)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   convert_kg_to_pounds(10): gets the literal 10, then runs the function body
#   convert_kg_to_pounds(kg): looks up variable 'kg', then runs the function body
#   print(convert_kg_to_pounds(25)): the inner convert_kg_to_pounds() executes FIRST, then print() outputs the result
#   kg * 2.20462: first get kg, then get 2.20462, then compute kg * 2.20462
#   kg * 2.20462: first get kg, then get 2.20462, then compute kg * 2.20462
#   kg * 2.20462: first get kg, then get 2.20462, then compute kg * 2.20462
#   convert_kg_to_pounds(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_kg_to_pounds(kg) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_kg_to_pounds(kg) — stored for later
#   5. Define def convert_kg_to_pounds(kg) — stored for later
#   6. Assign to kg
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_kg_to_pounds(...))
#       calls convert_kg_to_pounds with: kg=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_kg_to_pounds, convert_kg_to_pounds, convert_kg_to_pounds are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      kg = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_kg_to_pounds(25)) → function runs first, then print shows the result

"""

"""
Problem 1722:
#
# ===== SYNTAX =====
#   def convert_pounds_to_kg(pounds)
#   return — exits the function and sends back a value
#   operators: /
#
# ===== EVALUATION ORDER =====
#   convert_pounds_to_kg(22.0462): gets the literal 22.0462, then runs the function body
#   print(round(result, 2)): inner expression resolves first, then print() outputs
#   convert_pounds_to_kg(pounds): looks up variable 'pounds', then runs the function body
#   print(convert_pounds_to_kg(25)): the inner convert_pounds_to_kg() executes FIRST, then print() outputs the result
#   pounds / 2.20462: first get pounds, then get 2.20462, then compute pounds / 2.20462
#   pounds / 2.20462: first get pounds, then get 2.20462, then compute pounds / 2.20462
#   pounds / 2.20462: first get pounds, then get 2.20462, then compute pounds / 2.20462
#   convert_pounds_to_kg(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_pounds_to_kg(pounds) — stored for later
#   2. Assign to result
#   3. Run print(round(...))
#   4. Define def convert_pounds_to_kg(pounds) — stored for later
#   5. Define def convert_pounds_to_kg(pounds) — stored for later
#   6. Assign to pounds
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_pounds_to_kg(...))
#       calls convert_pounds_to_kg with: pounds=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_pounds_to_kg, convert_pounds_to_kg, convert_pounds_to_kg are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(round(...))
#      pounds = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_pounds_to_kg(25)) → function runs first, then print shows the result

"""

"""
Problem 1723:
#
# ===== SYNTAX =====
#   def check_vowel(char)
#   return — exits the function and sends back a value
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(check_vowel('a')): the inner check_vowel() executes FIRST, then print() outputs the result
#   print(check_vowel('b')): the inner check_vowel() executes FIRST, then print() outputs the result
#   check_vowel(char): looks up variable 'char', then runs the function body
#   print(check_vowel('z')): the inner check_vowel() executes FIRST, then print() outputs the result
#   check_vowel('a'): gets the literal 'a', then runs the function body
#   check_vowel('b'): gets the literal 'b', then runs the function body
#   check_vowel('z'): gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_vowel(char) — stored for later
#   2. Run print(check_vowel(...), check_vowel(...))
#       calls check_vowel with: char='a'
#         2.1 Return from function
#   3. Define def check_vowel(char) — stored for later
#   4. Define def check_vowel(char) — stored for later
#   5. Assign to char
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_vowel(...))
#       calls check_vowel with: char='z'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_vowel, check_vowel, check_vowel are just stored
#   Execution starts at the non-def lines below:
#      print(check_vowel('a')) → function runs first, then print shows the result
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_vowel('z')) → function runs first, then print shows the result

"""

"""
Problem 1724:
#
# ===== SYNTAX =====
#   def check_consonant(char)
#   return — exits the function and sends back a value
#   operators: and
#
# ===== EVALUATION ORDER =====
#   print(check_consonant('b')): the inner check_consonant() executes FIRST, then print() outputs the result
#   print(check_consonant('a')): the inner check_consonant() executes FIRST, then print() outputs the result
#   check_consonant(char): looks up variable 'char', then runs the function body
#   print(check_consonant('z')): the inner check_consonant() executes FIRST, then print() outputs the result
#   check_consonant('b'): gets the literal 'b', then runs the function body
#   check_consonant('a'): gets the literal 'a', then runs the function body
#   check_consonant('z'): gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_consonant(char) — stored for later
#   2. Run print(check_consonant(...), check_consonant(...))
#       calls check_consonant with: char='b'
#         2.1 Return from function
#   3. Define def check_consonant(char) — stored for later
#   4. Define def check_consonant(char) — stored for later
#   5. Assign to char
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_consonant(...))
#       calls check_consonant with: char='z'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_consonant, check_consonant, check_consonant are just stored
#   Execution starts at the non-def lines below:
#      print(check_consonant('b')) → function runs first, then print shows the result
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_consonant('z')) → function runs first, then print shows the result

"""

"""
Problem 1725:
#
# ===== SYNTAX =====
#   def check_digit(char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_digit('5')): the inner check_digit() executes FIRST, then print() outputs the result
#   print(check_digit('a')): the inner check_digit() executes FIRST, then print() outputs the result
#   check_digit(char): looks up variable 'char', then runs the function body
#   print(check_digit('z')): the inner check_digit() executes FIRST, then print() outputs the result
#   check_digit('5'): gets the literal '5', then runs the function body
#   check_digit('a'): gets the literal 'a', then runs the function body
#   check_digit('z'): gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_digit(char) — stored for later
#   2. Run print(check_digit(...), check_digit(...))
#       calls check_digit with: char='5'
#         2.1 Return from function
#   3. Define def check_digit(char) — stored for later
#   4. Define def check_digit(char) — stored for later
#   5. Assign to char
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_digit(...))
#       calls check_digit with: char='z'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_digit, check_digit, check_digit are just stored
#   Execution starts at the non-def lines below:
#      print(check_digit('5')) → function runs first, then print shows the result
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_digit('z')) → function runs first, then print shows the result

"""

"""
Problem 1726:
#
# ===== SYNTAX =====
#   def check_letter(char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_letter('a')): the inner check_letter() executes FIRST, then print() outputs the result
#   print(check_letter('5')): the inner check_letter() executes FIRST, then print() outputs the result
#   check_letter(char): looks up variable 'char', then runs the function body
#   print(check_letter('z')): the inner check_letter() executes FIRST, then print() outputs the result
#   check_letter('a'): gets the literal 'a', then runs the function body
#   check_letter('5'): gets the literal '5', then runs the function body
#   check_letter('z'): gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_letter(char) — stored for later
#   2. Run print(check_letter(...), check_letter(...))
#       calls check_letter with: char='a'
#         2.1 Return from function
#   3. Define def check_letter(char) — stored for later
#   4. Define def check_letter(char) — stored for later
#   5. Assign to char
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_letter(...))
#       calls check_letter with: char='z'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_letter, check_letter, check_letter are just stored
#   Execution starts at the non-def lines below:
#      print(check_letter('a')) → function runs first, then print shows the result
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_letter('z')) → function runs first, then print shows the result

"""

"""
Problem 1727:
#
# ===== SYNTAX =====
#   def check_uppercase(char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_uppercase('A')): the inner check_uppercase() executes FIRST, then print() outputs the result
#   print(check_uppercase('a')): the inner check_uppercase() executes FIRST, then print() outputs the result
#   check_uppercase(char): looks up variable 'char', then runs the function body
#   print(check_uppercase('z')): the inner check_uppercase() executes FIRST, then print() outputs the result
#   check_uppercase('A'): gets the literal 'A', then runs the function body
#   check_uppercase('a'): gets the literal 'a', then runs the function body
#   check_uppercase('z'): gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_uppercase(char) — stored for later
#   2. Run print(check_uppercase(...), check_uppercase(...))
#       calls check_uppercase with: char='A'
#         2.1 Return from function
#   3. Define def check_uppercase(char) — stored for later
#   4. Define def check_uppercase(char) — stored for later
#   5. Assign to char
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_uppercase(...))
#       calls check_uppercase with: char='z'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_uppercase, check_uppercase, check_uppercase are just stored
#   Execution starts at the non-def lines below:
#      print(check_uppercase('A')) → function runs first, then print shows the result
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_uppercase('z')) → function runs first, then print shows the result

"""

"""
Problem 1728:
#
# ===== SYNTAX =====
#   def check_lowercase(char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_lowercase('a')): the inner check_lowercase() executes FIRST, then print() outputs the result
#   print(check_lowercase('A')): the inner check_lowercase() executes FIRST, then print() outputs the result
#   check_lowercase(char): looks up variable 'char', then runs the function body
#   print(check_lowercase('z')): the inner check_lowercase() executes FIRST, then print() outputs the result
#   check_lowercase('a'): gets the literal 'a', then runs the function body
#   check_lowercase('A'): gets the literal 'A', then runs the function body
#   check_lowercase('z'): gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_lowercase(char) — stored for later
#   2. Run print(check_lowercase(...), check_lowercase(...))
#       calls check_lowercase with: char='a'
#         2.1 Return from function
#   3. Define def check_lowercase(char) — stored for later
#   4. Define def check_lowercase(char) — stored for later
#   5. Assign to char
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_lowercase(...))
#       calls check_lowercase with: char='z'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_lowercase, check_lowercase, check_lowercase are just stored
#   Execution starts at the non-def lines below:
#      print(check_lowercase('a')) → function runs first, then print shows the result
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_lowercase('z')) → function runs first, then print shows the result

"""

"""
Problem 1729:
#
# ===== SYNTAX =====
#   def find_char_index(text, char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(find_char_index('Hello', 'e')): the inner find_char_index() executes FIRST, then print() outputs the result
#   print(find_char_index('Hello', 'x')): the inner find_char_index() executes FIRST, then print() outputs the result
#   find_char_index(text, char): looks up variable 'text', looks up variable 'char', then runs the function body
#   print(find_char_index('world', 'z')): the inner find_char_index() executes FIRST, then print() outputs the result
#   find_char_index('Hello', 'e'): gets the literal 'Hello', gets the literal 'e', then runs the function body
#   find_char_index('Hello', 'x'): gets the literal 'Hello', gets the literal 'x', then runs the function body
#   find_char_index('world', 'z'): gets the literal 'world', gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_char_index(text, char) — stored for later
#   2. Run print(find_char_index(...), find_char_index(...))
#       calls find_char_index with: text='Hello', char='e'
#         2.1 Return from function
#   3. Define def find_char_index(text, char) — stored for later
#   4. Define def find_char_index(text, char) — stored for later
#   5. Assign to text
#   6. Assign to char
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(find_char_index(...))
#       calls find_char_index with: text='world', char='z'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_char_index, find_char_index, find_char_index are just stored
#   Execution starts at the non-def lines below:
#      print(find_char_index('Hello', 'e')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_char_index('world', 'z')) → function runs first, then print shows the result

"""

"""
Problem 1730:
#
# ===== SYNTAX =====
#   def count_char_in_string(text, char)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   count_char_in_string('Hello', 'l'): gets the literal 'Hello', gets the literal 'l', then runs the function body
#   count_char_in_string(text, char): looks up variable 'text', looks up variable 'char', then runs the function body
#   print(count_char_in_string('world', 'z')): the inner count_char_in_string() executes FIRST, then print() outputs the result
#   count_char_in_string('world', 'z'): gets the literal 'world', gets the literal 'z', then runs the function body
#   c == char: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_char_in_string(text, char) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_char_in_string(text, char) — stored for later
#   5. Define def count_char_in_string(text, char) — stored for later
#   6. Assign to text
#   7. Assign to char
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_char_in_string(...))
#       calls count_char_in_string with: text='world', char='z'
#         10.1 Assign to count
#         10.2 For loop over text
#         10.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_char_in_string, count_char_in_string, count_char_in_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_char_in_string('world', 'z')) → function runs first, then print shows the result
#
#   Loop: grabs one item from text each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1731:
#
# ===== SYNTAX =====
#   def check_starts_with(text, prefix)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_starts_with('Hello', 'He')): the inner check_starts_with() executes FIRST, then print() outputs the result
#   print(check_starts_with('Hello', 'lo')): the inner check_starts_with() executes FIRST, then print() outputs the result
#   check_starts_with(text, prefix): looks up variable 'text', looks up variable 'prefix', then runs the function body
#   print(check_starts_with('world', 'Hi, ')): the inner check_starts_with() executes FIRST, then print() outputs the result
#   check_starts_with('Hello', 'He'): gets the literal 'Hello', gets the literal 'He', then runs the function body
#   check_starts_with('Hello', 'lo'): gets the literal 'Hello', gets the literal 'lo', then runs the function body
#   check_starts_with('world', 'Hi, '): gets the literal 'world', gets the literal 'Hi, ', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_starts_with(text, prefix) — stored for later
#   2. Run print(check_starts_with(...), check_starts_with(...))
#       calls check_starts_with with: text='Hello', prefix='He'
#         2.1 Return from function
#   3. Define def check_starts_with(text, prefix) — stored for later
#   4. Define def check_starts_with(text, prefix) — stored for later
#   5. Assign to text
#   6. Assign to prefix
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_starts_with(...))
#       calls check_starts_with with: text='world', prefix='Hi, '
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_starts_with, check_starts_with, check_starts_with are just stored
#   Execution starts at the non-def lines below:
#      print(check_starts_with('Hello', 'He')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      prefix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_starts_with('world', 'Hi, ')) → function runs first, then print shows the result

"""

"""
Problem 1732:
#
# ===== SYNTAX =====
#   def check_ends_with(text, suffix)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_ends_with('Hello', 'lo')): the inner check_ends_with() executes FIRST, then print() outputs the result
#   print(check_ends_with('Hello', 'He')): the inner check_ends_with() executes FIRST, then print() outputs the result
#   check_ends_with(text, suffix): looks up variable 'text', looks up variable 'suffix', then runs the function body
#   print(check_ends_with('world', '?')): the inner check_ends_with() executes FIRST, then print() outputs the result
#   check_ends_with('Hello', 'lo'): gets the literal 'Hello', gets the literal 'lo', then runs the function body
#   check_ends_with('Hello', 'He'): gets the literal 'Hello', gets the literal 'He', then runs the function body
#   check_ends_with('world', '?'): gets the literal 'world', gets the literal '?', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_ends_with(text, suffix) — stored for later
#   2. Run print(check_ends_with(...), check_ends_with(...))
#       calls check_ends_with with: text='Hello', suffix='lo'
#         2.1 Return from function
#   3. Define def check_ends_with(text, suffix) — stored for later
#   4. Define def check_ends_with(text, suffix) — stored for later
#   5. Assign to text
#   6. Assign to suffix
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_ends_with(...))
#       calls check_ends_with with: text='world', suffix='?'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_ends_with, check_ends_with, check_ends_with are just stored
#   Execution starts at the non-def lines below:
#      print(check_ends_with('Hello', 'lo')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      suffix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_ends_with('world', '?')) → function runs first, then print shows the result

"""

"""
Problem 1733:
#
# ===== SYNTAX =====
#   def replace_char(text, old_char, new_char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   replace_char('Hello', 'l', 'x'): gets the literal 'Hello', gets the literal 'l', gets the literal 'x', then runs the function body
#   replace_char(text, old_char, new_char): looks up variable 'text', looks up variable 'old_char', looks up variable 'new_char', then runs the function body
#   print(replace_char('world', 'world', 'world')): the inner replace_char() executes FIRST, then print() outputs the result
#   replace_char('world', 'world', 'world'): gets the literal 'world', gets the literal 'world', gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def replace_char(text, old_char, new_char) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def replace_char(text, old_char, new_char) — stored for later
#   5. Define def replace_char(text, old_char, new_char) — stored for later
#   6. Assign to text
#   7. Assign to old_char
#   8. Assign to new_char
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(replace_char(...))
#       calls replace_char with: text='world', old_char='world', new_char='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions replace_char, replace_char, replace_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      old_char = ... (stored in memory)
#      new_char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(replace_char('world', 'world', 'world')) → function runs first, then print shows the result

"""

"""
Problem 1734:
#
# ===== SYNTAX =====
#   def remove_char(text, char)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_char('Hello', 'l'): gets the literal 'Hello', gets the literal 'l', then runs the function body
#   remove_char(text, char): looks up variable 'text', looks up variable 'char', then runs the function body
#   print(remove_char('world', 'z')): the inner remove_char() executes FIRST, then print() outputs the result
#   remove_char('world', 'z'): gets the literal 'world', gets the literal 'z', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_char(text, char) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_char(text, char) — stored for later
#   5. Define def remove_char(text, char) — stored for later
#   6. Assign to text
#   7. Assign to char
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(remove_char(...))
#       calls remove_char with: text='world', char='z'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_char, remove_char, remove_char are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      char = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_char('world', 'z')) → function runs first, then print shows the result

"""

"""
Problem 1735:
#
# ===== SYNTAX =====
#   def get_substring(text, start, end)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_substring('Hello World', 0, 5): gets the literal 'Hello World', gets the literal 0, gets the literal 5, then runs the function body
#   get_substring(text, start, end): looks up variable 'text', looks up variable 'start', looks up variable 'end', then runs the function body
#   print(get_substring('world', 3, 20)): the inner get_substring() executes FIRST, then print() outputs the result
#   get_substring('world', 3, 20): gets the literal 'world', gets the literal 3, gets the literal 20, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_substring(text, start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_substring(text, start, end) — stored for later
#   5. Define def get_substring(text, start, end) — stored for later
#   6. Assign to text
#   7. Assign to start
#   8. Assign to end
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_substring(...))
#       calls get_substring with: text='world', start=3, end=20
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_substring, get_substring, get_substring are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_substring('world', 3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1736:
#
# ===== SYNTAX =====
#   def reverse_list(my_list)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_list(?): resolves the value, then runs the function body
#   reverse_list(my_list): looks up variable 'my_list', then runs the function body
#   print(reverse_list(?)): the inner reverse_list() executes FIRST, then print() outputs the result
#   reverse_list(?): resolves the value, then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_list(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def reverse_list(my_list) — stored for later
#   5. Define def reverse_list(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_list(...))
#       calls reverse_list with: my_list=[4, 1, 8, 6, 3]
#         9.1 Assign to result
#         9.2 For loop over my_list
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_list, reverse_list, reverse_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1737:
#
# ===== SYNTAX =====
#   def get_list_length(my_list)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   get_list_length(?): resolves the value, then runs the function body
#   get_list_length(my_list): looks up variable 'my_list', then runs the function body
#   print(get_list_length(?)): the inner get_list_length() executes FIRST, then print() outputs the result
#   get_list_length(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_list_length(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_list_length(my_list) — stored for later
#   5. Define def get_list_length(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_list_length(...))
#       calls get_list_length with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_list_length, get_list_length, get_list_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_list_length(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1738:
#
# ===== SYNTAX =====
#   def check_element_in_list(my_list, element)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   print(check_element_in_list(?, 2)): the inner check_element_in_list() executes FIRST, then print() outputs the result
#   print(check_element_in_list(?, 5)): the inner check_element_in_list() executes FIRST, then print() outputs the result
#   check_element_in_list(my_list, element): looks up variable 'my_list', looks up variable 'element', then runs the function body
#   print(check_element_in_list(?, 25)): the inner check_element_in_list() executes FIRST, then print() outputs the result
#   check_element_in_list(?, 2): resolves the value, gets the literal 2, then runs the function body
#   check_element_in_list(?, 5): resolves the value, gets the literal 5, then runs the function body
#   check_element_in_list(?, 25): resolves the value, gets the literal 25, then runs the function body
#   element == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_element_in_list(my_list, element) — stored for later
#   2. Run print(check_element_in_list(...), check_element_in_list(...))
#       calls check_element_in_list with: my_list=[1, 2, 3], element=2
#         2.1 Return from function
#   3. Define def check_element_in_list(my_list, element) — stored for later
#   4. Define def check_element_in_list(my_list, element) — stored for later
#   5. Assign to my_list
#   6. Assign to element
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_element_in_list(...))
#       calls check_element_in_list with: my_list=[4, 1, 8, 6, 3], element=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_element_in_list, check_element_in_list, check_element_in_list are just stored
#   Execution starts at the non-def lines below:
#      print(check_element_in_list(?, 2)) → function runs first, then print shows the result
#      my_list = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_element_in_list(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1739:
#
# ===== SYNTAX =====
#   def get_element_count(my_list, element)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   get_element_count(?, 2): resolves the value, gets the literal 2, then runs the function body
#   get_element_count(my_list, element): looks up variable 'my_list', looks up variable 'element', then runs the function body
#   print(get_element_count(?, 25)): the inner get_element_count() executes FIRST, then print() outputs the result
#   get_element_count(?, 25): resolves the value, gets the literal 25, then runs the function body
#   c == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_element_count(my_list, element) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_element_count(my_list, element) — stored for later
#   5. Define def get_element_count(my_list, element) — stored for later
#   6. Assign to my_list
#   7. Assign to element
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_element_count(...))
#       calls get_element_count with: my_list=[4, 1, 8, 6, 3], element=25
#         10.1 Assign to count
#         10.2 For loop over my_list
#         10.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_element_count, get_element_count, get_element_count are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_element_count(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1740:
#
# ===== SYNTAX =====
#   def get_element_index(my_list, element)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(get_element_index(?, 20)): the inner get_element_index() executes FIRST, then print() outputs the result
#   print(get_element_index(?, 40)): the inner get_element_index() executes FIRST, then print() outputs the result
#   get_element_index(my_list, element): looks up variable 'my_list', looks up variable 'element', then runs the function body
#   print(get_element_index(?, 25)): the inner get_element_index() executes FIRST, then print() outputs the result
#   get_element_index(?, 20): resolves the value, gets the literal 20, then runs the function body
#   get_element_index(?, 40): resolves the value, gets the literal 40, then runs the function body
#   get_element_index(?, 25): resolves the value, gets the literal 25, then runs the function body
#   element in my_list: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_element_index(my_list, element) — stored for later
#   2. Run print(get_element_index(...), get_element_index(...))
#       calls get_element_index with: my_list=[10, 20, 30], element=20
#         2.1 Return from function
#   3. Define def get_element_index(my_list, element) — stored for later
#   4. Define def get_element_index(my_list, element) — stored for later
#   5. Assign to my_list
#   6. Assign to element
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_element_index(...))
#       calls get_element_index with: my_list=[4, 1, 8, 6, 3], element=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_element_index, get_element_index, get_element_index are just stored
#   Execution starts at the non-def lines below:
#      print(get_element_index(?, 20)) → function runs first, then print shows the result
#      my_list = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_element_index(?, 25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1741:
#
# ===== SYNTAX =====
#   def get_even_numbers(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   get_even_numbers(?): resolves the value, then runs the function body
#   get_even_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(get_even_numbers(?)): the inner get_even_numbers() executes FIRST, then print() outputs the result
#   get_even_numbers(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_even_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_even_numbers(numbers) — stored for later
#   5. Define def get_even_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_even_numbers(...))
#       calls get_even_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_even_numbers, get_even_numbers, get_even_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_even_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1742:
#
# ===== SYNTAX =====
#   def get_odd_numbers(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   get_odd_numbers(?): resolves the value, then runs the function body
#   get_odd_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(get_odd_numbers(?)): the inner get_odd_numbers() executes FIRST, then print() outputs the result
#   get_odd_numbers(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_odd_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_odd_numbers(numbers) — stored for later
#   5. Define def get_odd_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_odd_numbers(...))
#       calls get_odd_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_odd_numbers, get_odd_numbers, get_odd_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_odd_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1743:
#
# ===== SYNTAX =====
#   def get_positive_numbers(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   get_positive_numbers(?): resolves the value, then runs the function body
#   get_positive_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(get_positive_numbers(?)): the inner get_positive_numbers() executes FIRST, then print() outputs the result
#   get_positive_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_positive_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_positive_numbers(numbers) — stored for later
#   5. Define def get_positive_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_positive_numbers(...))
#       calls get_positive_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_positive_numbers, get_positive_numbers, get_positive_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_positive_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1744:
#
# ===== SYNTAX =====
#   def get_negative_numbers(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: <
#
# ===== EVALUATION ORDER =====
#   get_negative_numbers(?): resolves the value, then runs the function body
#   get_negative_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(get_negative_numbers(?)): the inner get_negative_numbers() executes FIRST, then print() outputs the result
#   get_negative_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_negative_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_negative_numbers(numbers) — stored for later
#   5. Define def get_negative_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_negative_numbers(...))
#       calls get_negative_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_negative_numbers, get_negative_numbers, get_negative_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_negative_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1745:
#
# ===== SYNTAX =====
#   def get_numbers_greater_than(numbers, threshold)
#   return — exits the function and sends back a value
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   get_numbers_greater_than(?, 10): resolves the value, gets the literal 10, then runs the function body
#   get_numbers_greater_than(numbers, threshold): looks up variable 'numbers', looks up variable 'threshold', then runs the function body
#   print(get_numbers_greater_than(?, 25)): the inner get_numbers_greater_than() executes FIRST, then print() outputs the result
#   get_numbers_greater_than(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_numbers_greater_than(numbers, threshold) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_numbers_greater_than(numbers, threshold) — stored for later
#   5. Define def get_numbers_greater_than(numbers, threshold) — stored for later
#   6. Assign to numbers
#   7. Assign to threshold
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_numbers_greater_than(...))
#       calls get_numbers_greater_than with: numbers=[4, 1, 8, 6, 3], threshold=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_numbers_greater_than, get_numbers_greater_than, get_numbers_greater_than are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      threshold = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_numbers_greater_than(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1746:
#
# ===== SYNTAX =====
#   def get_numbers_less_than(numbers, threshold)
#   return — exits the function and sends back a value
#   comprehension
#   operators: <
#
# ===== EVALUATION ORDER =====
#   get_numbers_less_than(?, 10): resolves the value, gets the literal 10, then runs the function body
#   get_numbers_less_than(numbers, threshold): looks up variable 'numbers', looks up variable 'threshold', then runs the function body
#   print(get_numbers_less_than(?, 25)): the inner get_numbers_less_than() executes FIRST, then print() outputs the result
#   get_numbers_less_than(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_numbers_less_than(numbers, threshold) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_numbers_less_than(numbers, threshold) — stored for later
#   5. Define def get_numbers_less_than(numbers, threshold) — stored for later
#   6. Assign to numbers
#   7. Assign to threshold
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_numbers_less_than(...))
#       calls get_numbers_less_than with: numbers=[4, 1, 8, 6, 3], threshold=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_numbers_less_than, get_numbers_less_than, get_numbers_less_than are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      threshold = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_numbers_less_than(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1747:
#
# ===== SYNTAX =====
#   def calculate_sum_of_list(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   calculate_sum_of_list(?): resolves the value, then runs the function body
#   calculate_sum_of_list(numbers): looks up variable 'numbers', then runs the function body
#   print(calculate_sum_of_list(?)): the inner calculate_sum_of_list() executes FIRST, then print() outputs the result
#   calculate_sum_of_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_sum_of_list(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_sum_of_list(numbers) — stored for later
#   5. Define def calculate_sum_of_list(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_sum_of_list(...))
#       calls calculate_sum_of_list with: numbers=[4, 1, 8, 6, 3]
#         9.1 Import reduce from functools
#         9.2 Import operator
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_sum_of_list, calculate_sum_of_list, calculate_sum_of_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_sum_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 1748:
#
# ===== SYNTAX =====
#   def calculate_product_of_list(numbers)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   calculate_product_of_list(?): resolves the value, then runs the function body
#   calculate_product_of_list(numbers): looks up variable 'numbers', then runs the function body
#   print(calculate_product_of_list(?)): the inner calculate_product_of_list() executes FIRST, then print() outputs the result
#   calculate_product_of_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_product_of_list(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_product_of_list(numbers) — stored for later
#   5. Define def calculate_product_of_list(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_product_of_list(...))
#       calls calculate_product_of_list with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_product_of_list, calculate_product_of_list, calculate_product_of_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_product_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 1749:
#
# ===== SYNTAX =====
#   def get_average_of_list(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   for
#   in
#   operators: /
#
# ===== EVALUATION ORDER =====
#   get_average_of_list(?): resolves the value, then runs the function body
#   get_average_of_list(numbers): looks up variable 'numbers', then runs the function body
#   print(get_average_of_list(?)): the inner get_average_of_list() executes FIRST, then print() outputs the result
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   get_average_of_list(?): resolves the value, then runs the function body
#   total / count: first get total, then get count, then compute total / count
#
# ===== EXECUTION ORDER =====
#   1. Define def get_average_of_list(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_average_of_list(numbers) — stored for later
#   5. Define def get_average_of_list(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_average_of_list(...))
#       calls get_average_of_list with: numbers=[4, 1, 8, 6, 3]
#         9.1 Assign to total
#         9.2 Assign to count
#         9.3 For loop over numbers
#         9.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_average_of_list, get_average_of_list, get_average_of_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_average_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 1750:
#
# ===== SYNTAX =====
#   def create_dictionary(keys, values)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   zip() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_dictionary(?, ?): resolves the value, resolves the value, then runs the function body
#   create_dictionary(keys, values): looks up variable 'keys', looks up variable 'values', then runs the function body
#   print(create_dictionary(25, ?)): the inner create_dictionary() executes FIRST, then print() outputs the result
#   create_dictionary(25, ?): gets the literal 25, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_dictionary(keys, values) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def create_dictionary(keys, values) — stored for later
#   5. Define def create_dictionary(keys, values) — stored for later
#   6. Assign to keys
#   7. Assign to values
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(create_dictionary(...))
#       calls create_dictionary with: keys=25, values=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_dictionary, create_dictionary, create_dictionary are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_dictionary(25, ?)) → function runs first, then print shows the result

"""

"""
Problem 1751:
#
# ===== SYNTAX =====
#   def get_dictionary_value(d, key)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(get_dictionary_value(?, 'a')): the inner get_dictionary_value() executes FIRST, then print() outputs the result
#   print(get_dictionary_value(?, 'c')): the inner get_dictionary_value() executes FIRST, then print() outputs the result
#   get_dictionary_value(d, key): looks up variable 'd', looks up variable 'key', then runs the function body
#   print(get_dictionary_value(10, 'value')): the inner get_dictionary_value() executes FIRST, then print() outputs the result
#   get_dictionary_value(?, 'a'): resolves the value, gets the literal 'a', then runs the function body
#   get_dictionary_value(?, 'c'): resolves the value, gets the literal 'c', then runs the function body
#   get_dictionary_value(10, 'value'): gets the literal 10, gets the literal 'value', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_dictionary_value(d, key) — stored for later
#   2. Run print(get_dictionary_value(...), get_dictionary_value(...))
#       calls get_dictionary_value with: d={...}, key='a'
#         2.1 Return from function
#   3. Define def get_dictionary_value(d, key) — stored for later
#   4. Define def get_dictionary_value(d, key) — stored for later
#   5. Assign to d
#   6. Assign to key
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_dictionary_value(...))
#       calls get_dictionary_value with: d=10, key='value'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_dictionary_value, get_dictionary_value, get_dictionary_value are just stored
#   Execution starts at the non-def lines below:
#      print(get_dictionary_value(?, 'a')) → function runs first, then print shows the result
#      d = ... (stored in memory)
#      key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_dictionary_value(10, 'value')) → function runs first, then print shows the result

"""

"""
Problem 1752:
#
# ===== SYNTAX =====
#   def get_dictionary_keys(d)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_dictionary_keys(?): resolves the value, then runs the function body
#   get_dictionary_keys(d): looks up variable 'd', then runs the function body
#   print(get_dictionary_keys(10)): the inner get_dictionary_keys() executes FIRST, then print() outputs the result
#   get_dictionary_keys(10): gets the literal 10, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_dictionary_keys(d) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_dictionary_keys(d) — stored for later
#   5. Define def get_dictionary_keys(d) — stored for later
#   6. Assign to d
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_dictionary_keys(...))
#       calls get_dictionary_keys with: d=10
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_dictionary_keys, get_dictionary_keys, get_dictionary_keys are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_dictionary_keys(10)) → function runs first, then print shows the result

"""

"""
Problem 1753:
#
# ===== SYNTAX =====
#   def get_dictionary_values(d)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_dictionary_values(?): resolves the value, then runs the function body
#   get_dictionary_values(d): looks up variable 'd', then runs the function body
#   print(get_dictionary_values(10)): the inner get_dictionary_values() executes FIRST, then print() outputs the result
#   get_dictionary_values(10): gets the literal 10, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_dictionary_values(d) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_dictionary_values(d) — stored for later
#   5. Define def get_dictionary_values(d) — stored for later
#   6. Assign to d
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_dictionary_values(...))
#       calls get_dictionary_values with: d=10
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_dictionary_values, get_dictionary_values, get_dictionary_values are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_dictionary_values(10)) → function runs first, then print shows the result

"""

"""
Problem 1754:
#
# ===== SYNTAX =====
#   def check_key_in_dictionary(d, key)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   print(check_key_in_dictionary(?, 'a')): the inner check_key_in_dictionary() executes FIRST, then print() outputs the result
#   print(check_key_in_dictionary(?, 'b')): the inner check_key_in_dictionary() executes FIRST, then print() outputs the result
#   check_key_in_dictionary(d, key): looks up variable 'd', looks up variable 'key', then runs the function body
#   print(check_key_in_dictionary(10, 'value')): the inner check_key_in_dictionary() executes FIRST, then print() outputs the result
#   check_key_in_dictionary(?, 'a'): resolves the value, gets the literal 'a', then runs the function body
#   check_key_in_dictionary(?, 'b'): resolves the value, gets the literal 'b', then runs the function body
#   check_key_in_dictionary(10, 'value'): gets the literal 10, gets the literal 'value', then runs the function body
#   element == key: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_key_in_dictionary(d, key) — stored for later
#   2. Run print(check_key_in_dictionary(...), check_key_in_dictionary(...))
#       calls check_key_in_dictionary with: d={...}, key='a'
#         2.1 Return from function
#   3. Define def check_key_in_dictionary(d, key) — stored for later
#   4. Define def check_key_in_dictionary(d, key) — stored for later
#   5. Assign to d
#   6. Assign to key
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_key_in_dictionary(...))
#       calls check_key_in_dictionary with: d=10, key='value'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_key_in_dictionary, check_key_in_dictionary, check_key_in_dictionary are just stored
#   Execution starts at the non-def lines below:
#      print(check_key_in_dictionary(?, 'a')) → function runs first, then print shows the result
#      d = ... (stored in memory)
#      key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_key_in_dictionary(10, 'value')) → function runs first, then print shows the result
#
#   Loop: grabs one item from d each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1755:
#
# ===== SYNTAX =====
#   def add_to_dictionary(d, key, value)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   add_to_dictionary(?, 'b', 2): resolves the value, gets the literal 'b', gets the literal 2, then runs the function body
#   add_to_dictionary(d, key, value): looks up variable 'd', looks up variable 'key', looks up variable 'value', then runs the function body
#   print(add_to_dictionary(10, 'value', 25)): the inner add_to_dictionary() executes FIRST, then print() outputs the result
#   add_to_dictionary(10, 'value', 25): gets the literal 10, gets the literal 'value', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add_to_dictionary(d, key, value) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def add_to_dictionary(d, key, value) — stored for later
#   5. Define def add_to_dictionary(d, key, value) — stored for later
#   6. Assign to d
#   7. Assign to key
#   8. Assign to value
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(add_to_dictionary(...))
#       calls add_to_dictionary with: d=10, key='value', value=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_to_dictionary, add_to_dictionary, add_to_dictionary are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      d = ... (stored in memory)
#      key = ... (stored in memory)
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_to_dictionary(10, 'value', 25)) → function runs first, then print shows the result

"""

"""
Problem 1756:
#
# ===== SYNTAX =====
#   def create_tuple()
#   return — exits the function and sends back a value
#   tuple() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_tuple(1, 2, 3, 4): gets the literal 1, gets the literal 2, gets the literal 3, gets the literal 4, then runs the function body
#   create_tuple(?): resolves the value, then runs the function body
#   print(create_tuple(25)): the inner create_tuple() executes FIRST, then print() outputs the result
#   create_tuple(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_tuple() — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def create_tuple() — stored for later
#   5. Define def create_tuple() — stored for later
#   6. Assign to ?
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(create_tuple(...))
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_tuple, create_tuple, create_tuple are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      ? = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_tuple(25)) → function runs first, then print shows the result

"""

"""
Problem 1757:
#
# ===== SYNTAX =====
#   def get_tuple_element(my_tuple, index)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_tuple_element(?, 1): resolves the value, gets the literal 1, then runs the function body
#   get_tuple_element(my_tuple, index): looks up variable 'my_tuple', looks up variable 'index', then runs the function body
#   print(get_tuple_element(?, 4)): the inner get_tuple_element() executes FIRST, then print() outputs the result
#   get_tuple_element(?, 4): resolves the value, gets the literal 4, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_tuple_element(my_tuple, index) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_tuple_element(my_tuple, index) — stored for later
#   5. Define def get_tuple_element(my_tuple, index) — stored for later
#   6. Assign to my_tuple
#   7. Assign to index
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_tuple_element(...))
#       calls get_tuple_element with: my_tuple=[4, 1, 8, 6, 3], index=4
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_tuple_element, get_tuple_element, get_tuple_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_tuple = ... (stored in memory)
#      index = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_tuple_element(?, 4)) → function runs first, then print shows the result

"""

"""
Problem 1758:
#
# ===== SYNTAX =====
#   def unpack_tuple(my_tuple)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   unpack_tuple(?): resolves the value, then runs the function body
#   unpack_tuple(my_tuple): looks up variable 'my_tuple', then runs the function body
#   print(unpack_tuple(25)): the inner unpack_tuple() executes FIRST, then print() outputs the result
#   unpack_tuple(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def unpack_tuple(my_tuple) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def unpack_tuple(my_tuple) — stored for later
#   5. Define def unpack_tuple(my_tuple) — stored for later
#   6. Assign to my_tuple
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(unpack_tuple(...))
#       calls unpack_tuple with: my_tuple=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions unpack_tuple, unpack_tuple, unpack_tuple are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_tuple = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(unpack_tuple(25)) → function runs first, then print shows the result

"""

"""
Problem 1759:
#
# ===== SYNTAX =====
#   def create_set(my_list)
#   return — exits the function and sends back a value
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_set(?): resolves the value, then runs the function body
#   create_set(my_list): looks up variable 'my_list', then runs the function body
#   print(create_set(?)): the inner create_set() executes FIRST, then print() outputs the result
#   create_set(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_set(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def create_set(my_list) — stored for later
#   5. Define def create_set(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(create_set(...))
#       calls create_set with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_set, create_set, create_set are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_set(?)) → function runs first, then print shows the result

"""

"""
Problem 1760:
#
# ===== SYNTAX =====
#   def check_element_in_set(my_set, element)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   print(check_element_in_set(?, 2)): the inner check_element_in_set() executes FIRST, then print() outputs the result
#   print(check_element_in_set(?, 5)): the inner check_element_in_set() executes FIRST, then print() outputs the result
#   check_element_in_set(my_set, element): looks up variable 'my_set', looks up variable 'element', then runs the function body
#   print(check_element_in_set(25, 25)): the inner check_element_in_set() executes FIRST, then print() outputs the result
#   check_element_in_set(?, 2): resolves the value, gets the literal 2, then runs the function body
#   check_element_in_set(?, 5): resolves the value, gets the literal 5, then runs the function body
#   check_element_in_set(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   element == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_element_in_set(my_set, element) — stored for later
#   2. Run print(check_element_in_set(...), check_element_in_set(...))
#       calls check_element_in_set with: my_set=?, element=2
#         2.1 Return from function
#   3. Define def check_element_in_set(my_set, element) — stored for later
#   4. Define def check_element_in_set(my_set, element) — stored for later
#   5. Assign to my_set
#   6. Assign to element
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_element_in_set(...))
#       calls check_element_in_set with: my_set=25, element=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_element_in_set, check_element_in_set, check_element_in_set are just stored
#   Execution starts at the non-def lines below:
#      print(check_element_in_set(?, 2)) → function runs first, then print shows the result
#      my_set = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_element_in_set(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_set each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1761:
#
# ===== SYNTAX =====
#   def union_sets(set1, set2)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   union_sets(?, ?): resolves the value, resolves the value, then runs the function body
#   union_sets(set1, set2): looks up variable 'set1', looks up variable 'set2', then runs the function body
#   print(union_sets(25, 25)): the inner union_sets() executes FIRST, then print() outputs the result
#   set1 | set2: first get set1, then get set2, then compute set1 | set2
#   set1 | set2: first get set1, then get set2, then compute set1 | set2
#   set1 | set2: first get set1, then get set2, then compute set1 | set2
#   union_sets(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def union_sets(set1, set2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def union_sets(set1, set2) — stored for later
#   5. Define def union_sets(set1, set2) — stored for later
#   6. Assign to set1
#   7. Assign to set2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(union_sets(...))
#       calls union_sets with: set1=25, set2=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions union_sets, union_sets, union_sets are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      set1 = ... (stored in memory)
#      set2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(union_sets(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1762:
#
# ===== SYNTAX =====
#   def intersection_sets(set1, set2)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   intersection_sets(?, ?): resolves the value, resolves the value, then runs the function body
#   intersection_sets(set1, set2): looks up variable 'set1', looks up variable 'set2', then runs the function body
#   print(intersection_sets(25, 25)): the inner intersection_sets() executes FIRST, then print() outputs the result
#   set1 & set2: first get set1, then get set2, then compute set1 & set2
#   set1 & set2: first get set1, then get set2, then compute set1 & set2
#   set1 & set2: first get set1, then get set2, then compute set1 & set2
#   intersection_sets(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def intersection_sets(set1, set2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def intersection_sets(set1, set2) — stored for later
#   5. Define def intersection_sets(set1, set2) — stored for later
#   6. Assign to set1
#   7. Assign to set2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(intersection_sets(...))
#       calls intersection_sets with: set1=25, set2=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions intersection_sets, intersection_sets, intersection_sets are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      set1 = ... (stored in memory)
#      set2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(intersection_sets(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1763:
#
# ===== SYNTAX =====
#   def difference_sets(set1, set2)
#   return — exits the function and sends back a value
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   difference_sets(?, ?): resolves the value, resolves the value, then runs the function body
#   difference_sets(set1, set2): looks up variable 'set1', looks up variable 'set2', then runs the function body
#   print(difference_sets(25, 25)): the inner difference_sets() executes FIRST, then print() outputs the result
#   set1 - set2: first get set1, then get set2, then compute set1 - set2
#   set1 + -set2: first get set1, then get -set2, then compute set1 + -set2
#   difference_sets(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def difference_sets(set1, set2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def difference_sets(set1, set2) — stored for later
#   5. Define def difference_sets(set1, set2) — stored for later
#   6. Assign to set1
#   7. Assign to set2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(difference_sets(...))
#       calls difference_sets with: set1=25, set2=25
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions difference_sets, difference_sets, difference_sets are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      set1 = ... (stored in memory)
#      set2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(difference_sets(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1764:
#
# ===== SYNTAX =====
#   def read_file_content(filename)
#   return — exits the function and sends back a value
#   open() — built-in function call
#   with
#
# ===== EVALUATION ORDER =====
#   read_file_content(filename): looks up variable 'filename', then runs the function body
#   print(read_file_content(25)): the inner read_file_content() executes FIRST, then print() outputs the result
#   read_file_content(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def read_file_content(filename) — stored for later
#   2. Run print('Function defined: read_file_content(filename)')
#   3. Define def read_file_content(filename) — stored for later
#   4. Define def read_file_content(filename) — stored for later
#   5. Assign to filename
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(read_file_content(...))
#       calls read_file_content with: filename=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions read_file_content, read_file_content, read_file_content are just stored
#   Execution starts at the non-def lines below:
#      print('Function defined: read_file_content(filename)')
#      filename = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(read_file_content(25)) → function runs first, then print shows the result

"""

"""
Problem 1765:
#
# ===== SYNTAX =====
#   def write_to_file(filename, content)
#   open() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   with
#
# ===== EVALUATION ORDER =====
#   write_to_file(filename, content): looks up variable 'filename', looks up variable 'content', then runs the function body
#   print(write_to_file(25, 25)): the inner write_to_file() executes FIRST, then print() outputs the result
#   write_to_file(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def write_to_file(filename, content) — stored for later
#   2. Run print('Function defined: write_to_file(filename, content)')
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to filename
#   6. Assign to content
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(write_to_file(...))
#       calls write_to_file with: filename=25, content=25
#         9.1 With
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions write_to_file, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print('Function defined: write_to_file(filename, content)')
#      filename = ... (stored in memory)
#      content = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(write_to_file(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1766:
#
# ===== SYNTAX =====
#   def append_to_file(filename, content)
#   open() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   with
#
# ===== EVALUATION ORDER =====
#   append_to_file(filename, content): looks up variable 'filename', looks up variable 'content', then runs the function body
#   print(append_to_file(25, 25)): the inner append_to_file() executes FIRST, then print() outputs the result
#   append_to_file(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def append_to_file(filename, content) — stored for later
#   2. Run print('Function defined: append_to_file(filename, content)')
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to filename
#   6. Assign to content
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(append_to_file(...))
#       calls append_to_file with: filename=25, content=25
#         9.1 With
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions append_to_file, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print('Function defined: append_to_file(filename, content)')
#      filename = ... (stored in memory)
#      content = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(append_to_file(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1767:
#
# ===== SYNTAX =====
#   def safe_divide(a, b)
#   return — exits the function and sends back a value
#   float() — built-in function call
#   Fraction() — built-in function call
#   try/except
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(safe_divide(10, 2)): the inner safe_divide() executes FIRST, then print() outputs the result
#   print(safe_divide(10, 0)): the inner safe_divide() executes FIRST, then print() outputs the result
#   safe_divide(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(safe_divide(20, 30)): the inner safe_divide() executes FIRST, then print() outputs the result
#   safe_divide(10, 2): gets the literal 10, gets the literal 2, then runs the function body
#   safe_divide(10, 0): gets the literal 10, gets the literal 0, then runs the function body
#   a * ?: first get a, then get ?, then compute a * ?
#   safe_divide(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a / b: first get a, then get b, then compute a / b
#   1 / b: first get 1, then get b, then compute 1 / b
#
# ===== EXECUTION ORDER =====
#   1. Define def safe_divide(a, b) — stored for later
#   2. Run print(safe_divide(...), safe_divide(...))
#       calls safe_divide with: a=10, b=2
#         2.1 Import Fraction from fractions
#         2.2 Return from function
#   3. Define def safe_divide(a, b) — stored for later
#   4. Define def safe_divide(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(safe_divide(...))
#       calls safe_divide with: a=20, b=30
#         9.1 Import Fraction from fractions
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions safe_divide, safe_divide, safe_divide are just stored
#   Execution starts at the non-def lines below:
#      print(safe_divide(10, 2)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(safe_divide(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1768:
#
# ===== SYNTAX =====
#   def safe_convert_to_int(text)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   try/except
#
# ===== EVALUATION ORDER =====
#   print(safe_convert_to_int('123')): the inner safe_convert_to_int() executes FIRST, then print() outputs the result
#   print(safe_convert_to_int('abc')): the inner safe_convert_to_int() executes FIRST, then print() outputs the result
#   safe_convert_to_int(text): looks up variable 'text', then runs the function body
#   print(safe_convert_to_int('world')): the inner safe_convert_to_int() executes FIRST, then print() outputs the result
#   safe_convert_to_int('123'): gets the literal '123', then runs the function body
#   safe_convert_to_int('abc'): gets the literal 'abc', then runs the function body
#   safe_convert_to_int('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def safe_convert_to_int(text) — stored for later
#   2. Run print(safe_convert_to_int(...), safe_convert_to_int(...))
#       calls safe_convert_to_int with: text='123'
#         2.1 Return from function
#   3. Define def safe_convert_to_int(text) — stored for later
#   4. Define def safe_convert_to_int(text) — stored for later
#   5. Assign to text
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(safe_convert_to_int(...))
#       calls safe_convert_to_int with: text='world'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions safe_convert_to_int, safe_convert_to_int, safe_convert_to_int are just stored
#   Execution starts at the non-def lines below:
#      print(safe_convert_to_int('123')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(safe_convert_to_int('world')) → function runs first, then print shows the result

"""

"""
Problem 1769:
#
# ===== SYNTAX =====
#   def safe_get_list_element(my_list, index)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   print(safe_get_list_element(?, 1)): the inner safe_get_list_element() executes FIRST, then print() outputs the result
#   print(safe_get_list_element(?, 10)): the inner safe_get_list_element() executes FIRST, then print() outputs the result
#   safe_get_list_element(my_list, index): looks up variable 'my_list', looks up variable 'index', then runs the function body
#   print(safe_get_list_element(?, 4)): the inner safe_get_list_element() executes FIRST, then print() outputs the result
#   safe_get_list_element(?, 1): resolves the value, gets the literal 1, then runs the function body
#   safe_get_list_element(?, 10): resolves the value, gets the literal 10, then runs the function body
#   safe_get_list_element(?, 4): resolves the value, gets the literal 4, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def safe_get_list_element(my_list, index) — stored for later
#   2. Run print(safe_get_list_element(...), safe_get_list_element(...))
#       calls safe_get_list_element with: my_list=[1, 2, 3], index=1
#         2.1 Return from function
#   3. Define def safe_get_list_element(my_list, index) — stored for later
#   4. Define def safe_get_list_element(my_list, index) — stored for later
#   5. Assign to my_list
#   6. Assign to index
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(safe_get_list_element(...))
#       calls safe_get_list_element with: my_list=[4, 1, 8, 6, 3], index=4
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions safe_get_list_element, safe_get_list_element, safe_get_list_element are just stored
#   Execution starts at the non-def lines below:
#      print(safe_get_list_element(?, 1)) → function runs first, then print shows the result
#      my_list = ... (stored in memory)
#      index = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(safe_get_list_element(?, 4)) → function runs first, then print shows the result

"""

"""
Problem 1770:
#
# ===== SYNTAX =====
#   def join_strings(strings, separator)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   join_strings(?, '-'): resolves the value, gets the literal '-', then runs the function body
#   join_strings(strings, separator): looks up variable 'strings', looks up variable 'separator', then runs the function body
#   print(join_strings('world', 'world')): the inner join_strings() executes FIRST, then print() outputs the result
#   join_strings('world', 'world'): gets the literal 'world', gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def join_strings(strings, separator) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def join_strings(strings, separator) — stored for later
#   5. Define def join_strings(strings, separator) — stored for later
#   6. Assign to strings
#   7. Assign to separator
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(join_strings(...))
#       calls join_strings with: strings='world', separator='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions join_strings, join_strings, join_strings are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      separator = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(join_strings('world', 'world')) → function runs first, then print shows the result

"""

"""
Problem 1771:
#
# ===== SYNTAX =====
#   def split_string(text, delimiter)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   split_string('Hello-World-Python', '-'): gets the literal 'Hello-World-Python', gets the literal '-', then runs the function body
#   split_string(text, delimiter): looks up variable 'text', looks up variable 'delimiter', then runs the function body
#   print(split_string('world', 25)): the inner split_string() executes FIRST, then print() outputs the result
#   split_string('world', 25): gets the literal 'world', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def split_string(text, delimiter) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def split_string(text, delimiter) — stored for later
#   5. Define def split_string(text, delimiter) — stored for later
#   6. Assign to text
#   7. Assign to delimiter
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(split_string(...))
#       calls split_string with: text='world', delimiter=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions split_string, split_string, split_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      delimiter = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(split_string('world', 25)) → function runs first, then print shows the result

"""

"""
Problem 1772:
#
# ===== SYNTAX =====
#   def strip_whitespace(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   strip_whitespace('  Hello World  '): gets the literal '  Hello World  ', then runs the function body
#   strip_whitespace(text): looks up variable 'text', then runs the function body
#   print(strip_whitespace('world')): the inner strip_whitespace() executes FIRST, then print() outputs the result
#   strip_whitespace('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def strip_whitespace(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def strip_whitespace(text) — stored for later
#   5. Define def strip_whitespace(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(strip_whitespace(...))
#       calls strip_whitespace with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions strip_whitespace, strip_whitespace, strip_whitespace are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(strip_whitespace('world')) → function runs first, then print shows the result

"""

"""
Problem 1773:
#
# ===== SYNTAX =====
#   def capitalize_first_letter(text)
#   return — exits the function and sends back a value
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   capitalize_first_letter('hello world'): gets the literal 'hello world', then runs the function body
#   capitalize_first_letter(text): looks up variable 'text', then runs the function body
#   print(capitalize_first_letter('world')): the inner capitalize_first_letter() executes FIRST, then print() outputs the result
#   capitalize_first_letter('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_first_letter(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def capitalize_first_letter(text) — stored for later
#   5. Define def capitalize_first_letter(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(capitalize_first_letter(...))
#       calls capitalize_first_letter with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_first_letter, capitalize_first_letter, capitalize_first_letter are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_first_letter('world')) → function runs first, then print shows the result

"""

"""
Problem 1774:
#
# ===== SYNTAX =====
#   def swap_case(text)
#   return — exits the function and sends back a value
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   swap_case('Hello World'): gets the literal 'Hello World', then runs the function body
#   swap_case(text): looks up variable 'text', then runs the function body
#   print(swap_case('world')): the inner swap_case() executes FIRST, then print() outputs the result
#   swap_case('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def swap_case(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def swap_case(text) — stored for later
#   5. Define def swap_case(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(swap_case(...))
#       calls swap_case with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions swap_case, swap_case, swap_case are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(swap_case('world')) → function runs first, then print shows the result

"""

"""
Problem 1775:
#
# ===== SYNTAX =====
#   def check_all_digits(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_all_digits('12345')): the inner check_all_digits() executes FIRST, then print() outputs the result
#   print(check_all_digits('12a45')): the inner check_all_digits() executes FIRST, then print() outputs the result
#   check_all_digits(text): looks up variable 'text', then runs the function body
#   print(check_all_digits('world')): the inner check_all_digits() executes FIRST, then print() outputs the result
#   check_all_digits('12345'): gets the literal '12345', then runs the function body
#   check_all_digits('12a45'): gets the literal '12a45', then runs the function body
#   check_all_digits('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_digits(text) — stored for later
#   2. Run print(check_all_digits(...), check_all_digits(...))
#       calls check_all_digits with: text='12345'
#         2.1 Return from function
#   3. Define def check_all_digits(text) — stored for later
#   4. Define def check_all_digits(text) — stored for later
#   5. Assign to text
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_all_digits(...))
#       calls check_all_digits with: text='world'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_digits, check_all_digits, check_all_digits are just stored
#   Execution starts at the non-def lines below:
#      print(check_all_digits('12345')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_digits('world')) → function runs first, then print shows the result

"""

"""
Problem 1776:
#
# ===== SYNTAX =====
#   def check_all_letters(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_all_letters('Hello')): the inner check_all_letters() executes FIRST, then print() outputs the result
#   print(check_all_letters('Hello123')): the inner check_all_letters() executes FIRST, then print() outputs the result
#   check_all_letters(text): looks up variable 'text', then runs the function body
#   print(check_all_letters('world')): the inner check_all_letters() executes FIRST, then print() outputs the result
#   check_all_letters('Hello'): gets the literal 'Hello', then runs the function body
#   check_all_letters('Hello123'): gets the literal 'Hello123', then runs the function body
#   check_all_letters('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_letters(text) — stored for later
#   2. Run print(check_all_letters(...), check_all_letters(...))
#       calls check_all_letters with: text='Hello'
#         2.1 Return from function
#   3. Define def check_all_letters(text) — stored for later
#   4. Define def check_all_letters(text) — stored for later
#   5. Assign to text
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_all_letters(...))
#       calls check_all_letters with: text='world'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_letters, check_all_letters, check_all_letters are just stored
#   Execution starts at the non-def lines below:
#      print(check_all_letters('Hello')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_letters('world')) → function runs first, then print shows the result

"""

"""
Problem 1777:
#
# ===== SYNTAX =====
#   def check_all_alphanumeric(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(check_all_alphanumeric('Hello123')): the inner check_all_alphanumeric() executes FIRST, then print() outputs the result
#   print(check_all_alphanumeric('Hello 123')): the inner check_all_alphanumeric() executes FIRST, then print() outputs the result
#   check_all_alphanumeric(text): looks up variable 'text', then runs the function body
#   print(check_all_alphanumeric('world')): the inner check_all_alphanumeric() executes FIRST, then print() outputs the result
#   check_all_alphanumeric('Hello123'): gets the literal 'Hello123', then runs the function body
#   check_all_alphanumeric('Hello 123'): gets the literal 'Hello 123', then runs the function body
#   check_all_alphanumeric('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_alphanumeric(text) — stored for later
#   2. Run print(check_all_alphanumeric(...), check_all_alphanumeric(...))
#       calls check_all_alphanumeric with: text='Hello123'
#         2.1 Return from function
#   3. Define def check_all_alphanumeric(text) — stored for later
#   4. Define def check_all_alphanumeric(text) — stored for later
#   5. Assign to text
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_all_alphanumeric(...))
#       calls check_all_alphanumeric with: text='world'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_alphanumeric, check_all_alphanumeric, check_all_alphanumeric are just stored
#   Execution starts at the non-def lines below:
#      print(check_all_alphanumeric('Hello123')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_alphanumeric('world')) → function runs first, then print shows the result

"""

"""
Problem 1778:
#
# ===== SYNTAX =====
#   def get_squared_numbers(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   get_squared_numbers(?): resolves the value, then runs the function body
#   get_squared_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(get_squared_numbers(?)): the inner get_squared_numbers() executes FIRST, then print() outputs the result
#   get_squared_numbers(?): resolves the value, then runs the function body
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_squared_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_squared_numbers(numbers) — stored for later
#   5. Define def get_squared_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_squared_numbers(...))
#       calls get_squared_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_squared_numbers, get_squared_numbers, get_squared_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_squared_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1779:
#
# ===== SYNTAX =====
#   def get_doubled_numbers(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   get_doubled_numbers(?): resolves the value, then runs the function body
#   get_doubled_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(get_doubled_numbers(?)): the inner get_doubled_numbers() executes FIRST, then print() outputs the result
#   get_doubled_numbers(?): resolves the value, then runs the function body
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_doubled_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_doubled_numbers(numbers) — stored for later
#   5. Define def get_doubled_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_doubled_numbers(...))
#       calls get_doubled_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_doubled_numbers, get_doubled_numbers, get_doubled_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_doubled_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1780:
#
# ===== SYNTAX =====
#   def get_even_numbers_comprehension(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   get_even_numbers_comprehension(?): resolves the value, then runs the function body
#   get_even_numbers_comprehension(numbers): looks up variable 'numbers', then runs the function body
#   print(get_even_numbers_comprehension(?)): the inner get_even_numbers_comprehension() executes FIRST, then print() outputs the result
#   get_even_numbers_comprehension(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_even_numbers_comprehension(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_even_numbers_comprehension(numbers) — stored for later
#   5. Define def get_even_numbers_comprehension(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_even_numbers_comprehension(...))
#       calls get_even_numbers_comprehension with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_even_numbers_comprehension, get_even_numbers_comprehension, get_even_numbers_comprehension are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_even_numbers_comprehension(?)) → function runs first, then print shows the result

"""

"""
Problem 1781:
#
# ===== SYNTAX =====
#   def get_uppercase_strings(strings)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_uppercase_strings(?): resolves the value, then runs the function body
#   get_uppercase_strings(strings): looks up variable 'strings', then runs the function body
#   print(get_uppercase_strings(?)): the inner get_uppercase_strings() executes FIRST, then print() outputs the result
#   get_uppercase_strings(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_uppercase_strings(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_uppercase_strings(strings) — stored for later
#   5. Define def get_uppercase_strings(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_uppercase_strings(...))
#       calls get_uppercase_strings with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_uppercase_strings, get_uppercase_strings, get_uppercase_strings are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_uppercase_strings(?)) → function runs first, then print shows the result

"""

"""
Problem 1782:
#
# ===== SYNTAX =====
#   def get_string_lengths(strings)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_string_lengths(?): resolves the value, then runs the function body
#   get_string_lengths(strings): looks up variable 'strings', then runs the function body
#   print(get_string_lengths(?)): the inner get_string_lengths() executes FIRST, then print() outputs the result
#   get_string_lengths(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_string_lengths(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_string_lengths(strings) — stored for later
#   5. Define def get_string_lengths(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_string_lengths(...))
#       calls get_string_lengths with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_string_lengths, get_string_lengths, get_string_lengths are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_string_lengths(?)) → function runs first, then print shows the result

"""

"""
Problem 1783:
#
# ===== SYNTAX =====
#   def print_number_pattern(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_number_pattern() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print_number_pattern(5): gets the literal 5, then runs the function body
#   print_number_pattern(n): looks up variable 'n', then runs the function body
#   print(print_number_pattern(15)): the inner print_number_pattern() executes FIRST, then print() outputs the result
#   print_number_pattern(5): gets the literal 5, then runs the function body
#   print_number_pattern(5): gets the literal 5, then runs the function body
#   print_number_pattern(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def print_number_pattern(n) — stored for later
#   2. Run print_number_pattern(5)
#       with: n=5
#         2.1 For loop over range(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_number_pattern(...))
#       calls print_number_pattern with: n=15
#         8.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_number_pattern, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print_number_pattern(5) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_number_pattern(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1784:
#
# ===== SYNTAX =====
#   def print_star_pattern(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_star_pattern() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print_star_pattern(5): gets the literal 5, then runs the function body
#   print_star_pattern(n): looks up variable 'n', then runs the function body
#   print(print_star_pattern(15)): the inner print_star_pattern() executes FIRST, then print() outputs the result
#   print_star_pattern(5): gets the literal 5, then runs the function body
#   print_star_pattern(5): gets the literal 5, then runs the function body
#   print_star_pattern(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   '*' * i: first get '*', then get i, then compute '*' * i
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   '*' * i: first get '*', then get i, then compute '*' * i
#   '*' * i: first get '*', then get i, then compute '*' * i
#
# ===== EXECUTION ORDER =====
#   1. Define def print_star_pattern(n) — stored for later
#   2. Run print_star_pattern(5)
#       with: n=5
#         2.1 For loop over range(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_star_pattern(...))
#       calls print_star_pattern with: n=15
#         8.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_star_pattern, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print_star_pattern(5) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_star_pattern(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1785:
#
# ===== SYNTAX =====
#   def print_reverse_star_pattern(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_reverse_star_pattern() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print_reverse_star_pattern(5): gets the literal 5, then runs the function body
#   print_reverse_star_pattern(n): looks up variable 'n', then runs the function body
#   print(print_reverse_star_pattern(15)): the inner print_reverse_star_pattern() executes FIRST, then print() outputs the result
#   print_reverse_star_pattern(5): gets the literal 5, then runs the function body
#   print_reverse_star_pattern(5): gets the literal 5, then runs the function body
#   print_reverse_star_pattern(15): gets the literal 15, then runs the function body
#   '*' * i: first get '*', then get i, then compute '*' * i
#   '*' * i: first get '*', then get i, then compute '*' * i
#   '*' * i: first get '*', then get i, then compute '*' * i
#
# ===== EXECUTION ORDER =====
#   1. Define def print_reverse_star_pattern(n) — stored for later
#   2. Run print_reverse_star_pattern(5)
#       with: n=5
#         2.1 For loop over range(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_reverse_star_pattern(...))
#       calls print_reverse_star_pattern with: n=15
#         8.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_reverse_star_pattern, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print_reverse_star_pattern(5) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_reverse_star_pattern(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1786:
#
# ===== SYNTAX =====
#   def print_number_square(n)
#   range() — built-in function call
#   str() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print_number_square(3): gets the literal 3, then runs the function body
#   print_number_square(n): looks up variable 'n', then runs the function body
#   print(print_number_square(15)): the inner print_number_square() executes FIRST, then print() outputs the result
#   print_number_square(15): gets the literal 15, then runs the function body
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   print(' '.join(row)): inner expression resolves first, then print() outputs
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   n * n: first get n, then get n, then compute n * n
#   n * n: first get n, then get n, then compute n * n
#   n * n: first get n, then get n, then compute n * n
#   i + n: first get i, then get n, then compute i + n
#   i + n: first get i, then get n, then compute i + n
#   i + n: first get i, then get n, then compute i + n
#
# ===== EXECUTION ORDER =====
#   1. Define def print_number_square(n) — stored for later
#   2. Run print_number_square(3)
#       with: n=3
#         2.1 For loop over range(...)
#         2.2 Assign to result
#         2.3 Return from function
#   3. Define def print_number_square(n) — stored for later
#   4. Define def print_number_square(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_number_square(...))
#       calls print_number_square with: n=15
#         8.1 For loop over range(...)
#         8.2 Assign to result
#         8.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_number_square, print_number_square, print_number_square are just stored
#   Execution starts at the non-def lines below:
#      print_number_square(3) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_number_square(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1787:
#
# ===== SYNTAX =====
#   def calculate_factorial(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   calculate_factorial(5): gets the literal 5, then runs the function body
#   calculate_factorial(n): looks up variable 'n', then runs the function body
#   print(calculate_factorial(15)): the inner calculate_factorial() executes FIRST, then print() outputs the result
#   calculate_factorial(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_factorial(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_factorial(n) — stored for later
#   5. Define def calculate_factorial(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_factorial(...))
#       calls calculate_factorial with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_factorial, calculate_factorial, calculate_factorial are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_factorial(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1788:
#
# ===== SYNTAX =====
#   def calculate_sum_range(start, end)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   range() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   calculate_sum_range(1, 10): gets the literal 1, gets the literal 10, then runs the function body
#   calculate_sum_range(start, end): looks up variable 'start', looks up variable 'end', then runs the function body
#   print(calculate_sum_range(3, 20)): the inner calculate_sum_range() executes FIRST, then print() outputs the result
#   calculate_sum_range(3, 20): gets the literal 3, gets the literal 20, then runs the function body
#   end + 1: first get end, then get 1, then compute end + 1
#   end + 1: first get end, then get 1, then compute end + 1
#   end + 1: first get end, then get 1, then compute end + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_sum_range(start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_sum_range(start, end) — stored for later
#   5. Define def calculate_sum_range(start, end) — stored for later
#   6. Assign to start
#   7. Assign to end
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_sum_range(...))
#       calls calculate_sum_range with: start=3, end=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_sum_range, calculate_sum_range, calculate_sum_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_sum_range(3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1789:
#
# ===== SYNTAX =====
#   def calculate_product_range(start, end)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   calculate_product_range(1, 5): gets the literal 1, gets the literal 5, then runs the function body
#   calculate_product_range(start, end): looks up variable 'start', looks up variable 'end', then runs the function body
#   print(calculate_product_range(3, 20)): the inner calculate_product_range() executes FIRST, then print() outputs the result
#   calculate_product_range(3, 20): gets the literal 3, gets the literal 20, then runs the function body
#   end + 1: first get end, then get 1, then compute end + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_product_range(start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_product_range(start, end) — stored for later
#   5. Define def calculate_product_range(start, end) — stored for later
#   6. Assign to start
#   7. Assign to end
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_product_range(...))
#       calls calculate_product_range with: start=3, end=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_product_range, calculate_product_range, calculate_product_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_product_range(3, 20)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1790:
#
# ===== SYNTAX =====
#   def get_fibonacci_sequence(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + - == <=
#
# ===== EVALUATION ORDER =====
#   get_fibonacci_sequence(8): gets the literal 8, then runs the function body
#   get_fibonacci_sequence(n): looks up variable 'n', then runs the function body
#   print(get_fibonacci_sequence(15)): the inner get_fibonacci_sequence() executes FIRST, then print() outputs the result
#   get_fibonacci_sequence(15): gets the literal 15, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   i - 1: first get i, then get 1, then compute i - 1
#   i - 2: first get i, then get 2, then compute i - 2
#   n <= 0: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_fibonacci_sequence(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_fibonacci_sequence(n) — stored for later
#   5. Define def get_fibonacci_sequence(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_fibonacci_sequence(...))
#       calls get_fibonacci_sequence with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_fibonacci_sequence, get_fibonacci_sequence, get_fibonacci_sequence are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_fibonacci_sequence(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1791:
#
# ===== SYNTAX =====
#   def check_palindrome_number(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_palindrome_number(121)): the inner check_palindrome_number() executes FIRST, then print() outputs the result
#   print(check_palindrome_number(123)): the inner check_palindrome_number() executes FIRST, then print() outputs the result
#   check_palindrome_number(number): looks up variable 'number', then runs the function body
#   print(check_palindrome_number(25)): the inner check_palindrome_number() executes FIRST, then print() outputs the result
#   check_palindrome_number(121): gets the literal 121, then runs the function body
#   check_palindrome_number(123): gets the literal 123, then runs the function body
#   check_palindrome_number(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_palindrome_number(number) — stored for later
#   2. Run print(check_palindrome_number(...), check_palindrome_number(...))
#       calls check_palindrome_number with: number=121
#         2.1 Return from function
#   3. Define def check_palindrome_number(number) — stored for later
#   4. Define def check_palindrome_number(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_palindrome_number(...))
#       calls check_palindrome_number with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_palindrome_number, check_palindrome_number, check_palindrome_number are just stored
#   Execution starts at the non-def lines below:
#      print(check_palindrome_number(121)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_palindrome_number(25)) → function runs first, then print shows the result

"""

"""
Problem 1792:
#
# ===== SYNTAX =====
#   def sum_of_digits(number)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   int() — built-in function call
#   str() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   sum_of_digits(12345): gets the literal 12345, then runs the function body
#   sum_of_digits(number): looks up variable 'number', then runs the function body
#   print(sum_of_digits(25)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   sum_of_digits(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_digits(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sum_of_digits(number) — stored for later
#   5. Define def sum_of_digits(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sum_of_digits(...))
#       calls sum_of_digits with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_digits, sum_of_digits, sum_of_digits are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_digits(25)) → function runs first, then print shows the result

"""

"""
Problem 1793:
#
# ===== SYNTAX =====
#   def product_of_digits(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   int() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   product_of_digits(234): gets the literal 234, then runs the function body
#   product_of_digits(number): looks up variable 'number', then runs the function body
#   print(product_of_digits(25)): the inner product_of_digits() executes FIRST, then print() outputs the result
#   product_of_digits(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def product_of_digits(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def product_of_digits(number) — stored for later
#   5. Define def product_of_digits(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(product_of_digits(...))
#       calls product_of_digits with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions product_of_digits, product_of_digits, product_of_digits are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(product_of_digits(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from str(...) each round

"""

"""
Problem 1794:
#
# ===== SYNTAX =====
#   def count_digits(number)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   count_digits(12345): gets the literal 12345, then runs the function body
#   count_digits(number): looks up variable 'number', then runs the function body
#   print(count_digits(25)): the inner count_digits() executes FIRST, then print() outputs the result
#   count_digits(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_digits(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_digits(number) — stored for later
#   5. Define def count_digits(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_digits(...))
#       calls count_digits with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_digits, count_digits, count_digits are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_digits(25)) → function runs first, then print shows the result

"""

"""
Problem 1795:
#
# ===== SYNTAX =====
#   def reverse_number(number)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   reverse_number(12345): gets the literal 12345, then runs the function body
#   reverse_number(number): looks up variable 'number', then runs the function body
#   print(reverse_number(25)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_number(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def reverse_number(number) — stored for later
#   5. Define def reverse_number(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_number(...))
#       calls reverse_number with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_number, reverse_number, reverse_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_number(25)) → function runs first, then print shows the result

"""

"""
Problem 1796:
#
# ===== SYNTAX =====
#   def get_first_n_elements(my_list, n)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_first_n_elements(?, 3): resolves the value, gets the literal 3, then runs the function body
#   get_first_n_elements(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(get_first_n_elements(?, 15)): the inner get_first_n_elements() executes FIRST, then print() outputs the result
#   get_first_n_elements(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_first_n_elements(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_first_n_elements(my_list, n) — stored for later
#   5. Define def get_first_n_elements(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_first_n_elements(...))
#       calls get_first_n_elements with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_first_n_elements, get_first_n_elements, get_first_n_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_first_n_elements(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1797:
#
# ===== SYNTAX =====
#   def get_last_n_elements(my_list, n)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_last_n_elements(?, 3): resolves the value, gets the literal 3, then runs the function body
#   get_last_n_elements(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(get_last_n_elements(?, 15)): the inner get_last_n_elements() executes FIRST, then print() outputs the result
#   get_last_n_elements(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_last_n_elements(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_last_n_elements(my_list, n) — stored for later
#   5. Define def get_last_n_elements(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_last_n_elements(...))
#       calls get_last_n_elements with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_last_n_elements, get_last_n_elements, get_last_n_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_last_n_elements(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1798:
#
# ===== SYNTAX =====
#   def remove_duplicates_from_list(my_list)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   remove_duplicates_from_list(?): resolves the value, then runs the function body
#   remove_duplicates_from_list(my_list): looks up variable 'my_list', then runs the function body
#   print(remove_duplicates_from_list(?)): the inner remove_duplicates_from_list() executes FIRST, then print() outputs the result
#   remove_duplicates_from_list(?): resolves the value, then runs the function body
#   item not in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates_from_list(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_duplicates_from_list(my_list) — stored for later
#   5. Define def remove_duplicates_from_list(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_duplicates_from_list(...))
#       calls remove_duplicates_from_list with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates_from_list, remove_duplicates_from_list, remove_duplicates_from_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates_from_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1799:
#
# ===== SYNTAX =====
#   def merge_two_lists(list1, list2)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   merge_two_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_two_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(merge_two_lists(?, ?)): the inner merge_two_lists() executes FIRST, then print() outputs the result
#   merge_two_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   i < len(...): is this True or False? The answer picks which branch
#   i < len(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_two_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def merge_two_lists(list1, list2) — stored for later
#   5. Define def merge_two_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(merge_two_lists(...))
#       calls merge_two_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_two_lists, merge_two_lists, merge_two_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_two_lists(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1800:
#
# ===== SYNTAX =====
#   def get_common_elements(list1, list2)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   get_common_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   get_common_elements(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(get_common_elements(?, ?)): the inner get_common_elements() executes FIRST, then print() outputs the result
#   get_common_elements(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_common_elements(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_common_elements(list1, list2) — stored for later
#   5. Define def get_common_elements(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_common_elements(...))
#       calls get_common_elements with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_common_elements, get_common_elements, get_common_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_common_elements(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1801:
#
# ===== SYNTAX =====
#   def get_unique_elements(list1, list2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   get_unique_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   get_unique_elements(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(get_unique_elements(?, ?)): the inner get_unique_elements() executes FIRST, then print() outputs the result
#   get_unique_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   item not in list2: is this True or False? The answer picks which branch
#   item not in list1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_unique_elements(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_unique_elements(list1, list2) — stored for later
#   5. Define def get_unique_elements(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_unique_elements(...))
#       calls get_unique_elements with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_unique_elements, get_unique_elements, get_unique_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_unique_elements(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from list1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1802:
#
# ===== SYNTAX =====
#   def sort_list_ascending(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_list_ascending(?): resolves the value, then runs the function body
#   sort_list_ascending(numbers): looks up variable 'numbers', then runs the function body
#   print(sort_list_ascending(?)): the inner sort_list_ascending() executes FIRST, then print() outputs the result
#   sort_list_ascending(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_list_ascending(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_list_ascending(numbers) — stored for later
#   5. Define def sort_list_ascending(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_list_ascending(...))
#       calls sort_list_ascending with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_list_ascending, sort_list_ascending, sort_list_ascending are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_list_ascending(?)) → function runs first, then print shows the result

"""

"""
Problem 1803:
#
# ===== SYNTAX =====
#   def sort_list_descending(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_list_descending(?): resolves the value, then runs the function body
#   sort_list_descending(numbers): looks up variable 'numbers', then runs the function body
#   print(sort_list_descending(?)): the inner sort_list_descending() executes FIRST, then print() outputs the result
#   sort_list_descending(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_list_descending(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_list_descending(numbers) — stored for later
#   5. Define def sort_list_descending(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_list_descending(...))
#       calls sort_list_descending with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_list_descending, sort_list_descending, sort_list_descending are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_list_descending(?)) → function runs first, then print shows the result

"""

"""
Problem 1804:
#
# ===== SYNTAX =====
#   def get_second_largest(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#   len() — built-in function call
#   operators: >
#
# ===== EVALUATION ORDER =====
#   get_second_largest(?): resolves the value, then runs the function body
#   get_second_largest(numbers): looks up variable 'numbers', then runs the function body
#   print(get_second_largest(?)): the inner get_second_largest() executes FIRST, then print() outputs the result
#   get_second_largest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_second_largest(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_second_largest(numbers) — stored for later
#   5. Define def get_second_largest(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_second_largest(...))
#       calls get_second_largest with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_second_largest, get_second_largest, get_second_largest are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_second_largest(?)) → function runs first, then print shows the result

"""

"""
Problem 1805:
#
# ===== SYNTAX =====
#   def get_second_smallest(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#   len() — built-in function call
#   operators: >
#
# ===== EVALUATION ORDER =====
#   get_second_smallest(?): resolves the value, then runs the function body
#   get_second_smallest(numbers): looks up variable 'numbers', then runs the function body
#   print(get_second_smallest(?)): the inner get_second_smallest() executes FIRST, then print() outputs the result
#   get_second_smallest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_second_smallest(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_second_smallest(numbers) — stored for later
#   5. Define def get_second_smallest(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_second_smallest(...))
#       calls get_second_smallest with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_second_smallest, get_second_smallest, get_second_smallest are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_second_smallest(?)) → function runs first, then print shows the result

"""

"""
Problem 1806:
#
# ===== SYNTAX =====
#   def check_sorted(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_sorted(?)): the inner check_sorted() executes FIRST, then print() outputs the result
#   print(check_sorted(?)): the inner check_sorted() executes FIRST, then print() outputs the result
#   check_sorted(numbers): looks up variable 'numbers', then runs the function body
#   print(check_sorted(?)): the inner check_sorted() executes FIRST, then print() outputs the result
#   check_sorted(?): resolves the value, then runs the function body
#   check_sorted(?): resolves the value, then runs the function body
#   check_sorted(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_sorted(numbers) — stored for later
#   2. Run print(check_sorted(...), check_sorted(...))
#       calls check_sorted with: numbers=[1, 2, 3, 4]
#         2.1 Return from function
#   3. Define def check_sorted(numbers) — stored for later
#   4. Define def check_sorted(numbers) — stored for later
#   5. Assign to numbers
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_sorted(...))
#       calls check_sorted with: numbers=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_sorted, check_sorted, check_sorted are just stored
#   Execution starts at the non-def lines below:
#      print(check_sorted(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_sorted(?)) → function runs first, then print shows the result

"""

"""
Problem 1807:
#
# ===== SYNTAX =====
#   def count_occurrences_in_list(my_list)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   count_occurrences_in_list(?): resolves the value, then runs the function body
#   count_occurrences_in_list(my_list): looks up variable 'my_list', then runs the function body
#   print(count_occurrences_in_list(?)): the inner count_occurrences_in_list() executes FIRST, then print() outputs the result
#   count_occurrences_in_list(?): resolves the value, then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences_in_list(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_occurrences_in_list(my_list) — stored for later
#   5. Define def count_occurrences_in_list(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_occurrences_in_list(...))
#       calls count_occurrences_in_list with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences_in_list, count_occurrences_in_list, count_occurrences_in_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences_in_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1808:
#
# ===== SYNTAX =====
#   def get_most_frequent(my_list)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   get_most_frequent(?): resolves the value, then runs the function body
#   get_most_frequent(my_list): looks up variable 'my_list', then runs the function body
#   print(get_most_frequent(?)): the inner get_most_frequent() executes FIRST, then print() outputs the result
#   get_most_frequent(?): resolves the value, then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def get_most_frequent(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_most_frequent(my_list) — stored for later
#   5. Define def get_most_frequent(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_most_frequent(...))
#       calls get_most_frequent with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_most_frequent, get_most_frequent, get_most_frequent are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_most_frequent(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1809:
#
# ===== SYNTAX =====
#   def get_least_frequent(my_list)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   get_least_frequent(?): resolves the value, then runs the function body
#   get_least_frequent(my_list): looks up variable 'my_list', then runs the function body
#   print(get_least_frequent(?)): the inner get_least_frequent() executes FIRST, then print() outputs the result
#   get_least_frequent(?): resolves the value, then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def get_least_frequent(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_least_frequent(my_list) — stored for later
#   5. Define def get_least_frequent(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_least_frequent(...))
#       calls get_least_frequent with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_least_frequent, get_least_frequent, get_least_frequent are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_least_frequent(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1810:
#
# ===== SYNTAX =====
#   def flatten_nested_list(nested_list)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   flatten_nested_list(?): resolves the value, then runs the function body
#   flatten_nested_list(nested_list): looks up variable 'nested_list', then runs the function body
#   print(flatten_nested_list(?)): the inner flatten_nested_list() executes FIRST, then print() outputs the result
#   flatten_nested_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def flatten_nested_list(nested_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def flatten_nested_list(nested_list) — stored for later
#   5. Define def flatten_nested_list(nested_list) — stored for later
#   6. Assign to nested_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(flatten_nested_list(...))
#       calls flatten_nested_list with: nested_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions flatten_nested_list, flatten_nested_list, flatten_nested_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      nested_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(flatten_nested_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from nested_list each round

"""

"""
Problem 1811:
#
# ===== SYNTAX =====
#   def group_by_length(strings)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   group_by_length(?): resolves the value, then runs the function body
#   group_by_length(strings): looks up variable 'strings', then runs the function body
#   print(group_by_length(?)): the inner group_by_length() executes FIRST, then print() outputs the result
#   group_by_length(?): resolves the value, then runs the function body
#   length not in groups: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def group_by_length(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def group_by_length(strings) — stored for later
#   5. Define def group_by_length(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(group_by_length(...))
#       calls group_by_length with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions group_by_length, group_by_length, group_by_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(group_by_length(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1812:
#
# ===== SYNTAX =====
#   def filter_by_length(strings, min_length)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   comprehension
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   filter_by_length(?, 3): resolves the value, gets the literal 3, then runs the function body
#   filter_by_length(strings, min_length): looks up variable 'strings', looks up variable 'min_length', then runs the function body
#   print(filter_by_length(?, 25)): the inner filter_by_length() executes FIRST, then print() outputs the result
#   filter_by_length(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_by_length(strings, min_length) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def filter_by_length(strings, min_length) — stored for later
#   5. Define def filter_by_length(strings, min_length) — stored for later
#   6. Assign to strings
#   7. Assign to min_length
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(filter_by_length(...))
#       calls filter_by_length with: strings=[4, 1, 8, 6, 3], min_length=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_by_length, filter_by_length, filter_by_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      min_length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_by_length(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1813:
#
# ===== SYNTAX =====
#   def get_longest_string(strings)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_longest_string(?): resolves the value, then runs the function body
#   get_longest_string(strings): looks up variable 'strings', then runs the function body
#   print(get_longest_string(?)): the inner get_longest_string() executes FIRST, then print() outputs the result
#   get_longest_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_longest_string(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_longest_string(strings) — stored for later
#   5. Define def get_longest_string(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_longest_string(...))
#       calls get_longest_string with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_longest_string, get_longest_string, get_longest_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_longest_string(?)) → function runs first, then print shows the result

"""

"""
Problem 1814:
#
# ===== SYNTAX =====
#   def get_shortest_string(strings)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_shortest_string(?): resolves the value, then runs the function body
#   get_shortest_string(strings): looks up variable 'strings', then runs the function body
#   print(get_shortest_string(?)): the inner get_shortest_string() executes FIRST, then print() outputs the result
#   get_shortest_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_shortest_string(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_shortest_string(strings) — stored for later
#   5. Define def get_shortest_string(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_shortest_string(...))
#       calls get_shortest_string with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_shortest_string, get_shortest_string, get_shortest_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_shortest_string(?)) → function runs first, then print shows the result

"""

"""
Problem 1815:
#
# ===== SYNTAX =====
#   def capitalize_all_words(text)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   capitalize_all_words('hello world python'): gets the literal 'hello world python', then runs the function body
#   capitalize_all_words(text): looks up variable 'text', then runs the function body
#   print(capitalize_all_words('world')): the inner capitalize_all_words() executes FIRST, then print() outputs the result
#   capitalize_all_words('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_all_words(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def capitalize_all_words(text) — stored for later
#   5. Define def capitalize_all_words(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(capitalize_all_words(...))
#       calls capitalize_all_words with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_all_words, capitalize_all_words, capitalize_all_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_all_words('world')) → function runs first, then print shows the result

"""

"""
Problem 1816:
#
# ===== SYNTAX =====
#   def reverse_words(text)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#
# ===== EVALUATION ORDER =====
#   reverse_words('hello world python'): gets the literal 'hello world python', then runs the function body
#   reverse_words(text): looks up variable 'text', then runs the function body
#   print(reverse_words('world')): the inner reverse_words() executes FIRST, then print() outputs the result
#   reverse_words('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_words(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def reverse_words(text) — stored for later
#   5. Define def reverse_words(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_words(...))
#       calls reverse_words with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_words, reverse_words, reverse_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_words('world')) → function runs first, then print shows the result

"""

"""
Problem 1817:
#
# ===== SYNTAX =====
#   def count_words(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#
# ===== EVALUATION ORDER =====
#   count_words('Hello World Python'): gets the literal 'Hello World Python', then runs the function body
#   count_words(text): looks up variable 'text', then runs the function body
#   print(count_words('world')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_words(text) — stored for later
#   5. Define def count_words(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_words(...))
#       calls count_words with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words, count_words, count_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words('world')) → function runs first, then print shows the result

"""

"""
Problem 1818:
#
# ===== SYNTAX =====
#   def get_longest_word(text)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_longest_word('hello world python programming'): gets the literal 'hello world python programming', then runs the function body
#   get_longest_word(text): looks up variable 'text', then runs the function body
#   print(get_longest_word('world')): the inner get_longest_word() executes FIRST, then print() outputs the result
#   get_longest_word('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_longest_word(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_longest_word(text) — stored for later
#   5. Define def get_longest_word(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_longest_word(...))
#       calls get_longest_word with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_longest_word, get_longest_word, get_longest_word are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_longest_word('world')) → function runs first, then print shows the result

"""

"""
Problem 1819:
#
# ===== SYNTAX =====
#   def get_shortest_word(text)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_shortest_word('hello world python programming'): gets the literal 'hello world python programming', then runs the function body
#   get_shortest_word(text): looks up variable 'text', then runs the function body
#   print(get_shortest_word('world')): the inner get_shortest_word() executes FIRST, then print() outputs the result
#   get_shortest_word('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_shortest_word(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_shortest_word(text) — stored for later
#   5. Define def get_shortest_word(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_shortest_word(...))
#       calls get_shortest_word with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_shortest_word, get_shortest_word, get_shortest_word are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_shortest_word('world')) → function runs first, then print shows the result

"""

"""
Problem 1820:
#
# ===== SYNTAX =====
#   def remove_vowels_from_string(text)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   remove_vowels_from_string('Hello World'): gets the literal 'Hello World', then runs the function body
#   remove_vowels_from_string(text): looks up variable 'text', then runs the function body
#   print(remove_vowels_from_string('world')): the inner remove_vowels_from_string() executes FIRST, then print() outputs the result
#   remove_vowels_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_vowels_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_vowels_from_string(text) — stored for later
#   5. Define def remove_vowels_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_vowels_from_string(...))
#       calls remove_vowels_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_vowels_from_string, remove_vowels_from_string, remove_vowels_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_vowels_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1821:
#
# ===== SYNTAX =====
#   def remove_consonants_from_string(text)
#   return — exits the function and sends back a value
#   comprehension
#   operators: or in
#
# ===== EVALUATION ORDER =====
#   remove_consonants_from_string('Hello World'): gets the literal 'Hello World', then runs the function body
#   remove_consonants_from_string(text): looks up variable 'text', then runs the function body
#   print(remove_consonants_from_string('world')): the inner remove_consonants_from_string() executes FIRST, then print() outputs the result
#   remove_consonants_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_consonants_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_consonants_from_string(text) — stored for later
#   5. Define def remove_consonants_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_consonants_from_string(...))
#       calls remove_consonants_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_consonants_from_string, remove_consonants_from_string, remove_consonants_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_consonants_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1822:
#
# ===== SYNTAX =====
#   def count_vowels_in_string(text)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   count_vowels_in_string('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_vowels_in_string(text): looks up variable 'text', then runs the function body
#   print(count_vowels_in_string('world')): the inner count_vowels_in_string() executes FIRST, then print() outputs the result
#   count_vowels_in_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels_in_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_vowels_in_string(text) — stored for later
#   5. Define def count_vowels_in_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_vowels_in_string(...))
#       calls count_vowels_in_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels_in_string, count_vowels_in_string, count_vowels_in_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels_in_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1824:
#
# ===== SYNTAX =====
#   def swap_first_last_words(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   if/elif/else
#   operators: <
#
# ===== EVALUATION ORDER =====
#   swap_first_last_words('Hello World Python'): gets the literal 'Hello World Python', then runs the function body
#   swap_first_last_words(text): looks up variable 'text', then runs the function body
#   print(swap_first_last_words('world')): the inner swap_first_last_words() executes FIRST, then print() outputs the result
#   swap_first_last_words('world'): gets the literal 'world', then runs the function body
#   len(...) < 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def swap_first_last_words(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def swap_first_last_words(text) — stored for later
#   5. Define def swap_first_last_words(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(swap_first_last_words(...))
#       calls swap_first_last_words with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions swap_first_last_words, swap_first_last_words, swap_first_last_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(swap_first_last_words('world')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1825:
#
# ===== SYNTAX =====
#   def get_middle_word(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: //
#
# ===== EVALUATION ORDER =====
#   get_middle_word('Hello World Python Programming'): gets the literal 'Hello World Python Programming', then runs the function body
#   get_middle_word(text): looks up variable 'text', then runs the function body
#   print(get_middle_word('world')): the inner get_middle_word() executes FIRST, then print() outputs the result
#   get_middle_word('world'): gets the literal 'world', then runs the function body
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_middle_word(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_middle_word(text) — stored for later
#   5. Define def get_middle_word(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_middle_word(...))
#       calls get_middle_word with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_middle_word, get_middle_word, get_middle_word are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_middle_word('world')) → function runs first, then print shows the result

"""

"""
Problem 1826:
#
# ===== SYNTAX =====
#   def check_all_same(my_list)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_all_same(?)): the inner check_all_same() executes FIRST, then print() outputs the result
#   print(check_all_same(?)): the inner check_all_same() executes FIRST, then print() outputs the result
#   check_all_same(my_list): looks up variable 'my_list', then runs the function body
#   print(check_all_same(?)): the inner check_all_same() executes FIRST, then print() outputs the result
#   check_all_same(?): resolves the value, then runs the function body
#   check_all_same(?): resolves the value, then runs the function body
#   check_all_same(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_same(my_list) — stored for later
#   2. Run print(check_all_same(...), check_all_same(...))
#       calls check_all_same with: my_list=[1, 1, 1]
#         2.1 Return from function
#   3. Define def check_all_same(my_list) — stored for later
#   4. Define def check_all_same(my_list) — stored for later
#   5. Assign to my_list
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_all_same(...))
#       calls check_all_same with: my_list=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_same, check_all_same, check_all_same are just stored
#   Execution starts at the non-def lines below:
#      print(check_all_same(?)) → function runs first, then print shows the result
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_same(?)) → function runs first, then print shows the result

"""

"""
Problem 1827:
#
# ===== SYNTAX =====
#   def check_all_different(my_list)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   set() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_all_different(?)): the inner check_all_different() executes FIRST, then print() outputs the result
#   print(check_all_different(?)): the inner check_all_different() executes FIRST, then print() outputs the result
#   check_all_different(my_list): looks up variable 'my_list', then runs the function body
#   print(check_all_different(?)): the inner check_all_different() executes FIRST, then print() outputs the result
#   check_all_different(?): resolves the value, then runs the function body
#   check_all_different(?): resolves the value, then runs the function body
#   check_all_different(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_different(my_list) — stored for later
#   2. Run print(check_all_different(...), check_all_different(...))
#       calls check_all_different with: my_list=[1, 2, 3]
#         2.1 Return from function
#   3. Define def check_all_different(my_list) — stored for later
#   4. Define def check_all_different(my_list) — stored for later
#   5. Assign to my_list
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_all_different(...))
#       calls check_all_different with: my_list=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_different, check_all_different, check_all_different are just stored
#   Execution starts at the non-def lines below:
#      print(check_all_different(?)) → function runs first, then print shows the result
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_different(?)) → function runs first, then print shows the result

"""

"""
Problem 1828:
#
# ===== SYNTAX =====
#   def get_range_list(start, end)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   range() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   get_range_list(1, 5): gets the literal 1, gets the literal 5, then runs the function body
#   get_range_list(start, end): looks up variable 'start', looks up variable 'end', then runs the function body
#   print(get_range_list(3, 20)): the inner get_range_list() executes FIRST, then print() outputs the result
#   get_range_list(3, 20): gets the literal 3, gets the literal 20, then runs the function body
#   end + 1: first get end, then get 1, then compute end + 1
#   end + 1: first get end, then get 1, then compute end + 1
#   end + 1: first get end, then get 1, then compute end + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def get_range_list(start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_range_list(start, end) — stored for later
#   5. Define def get_range_list(start, end) — stored for later
#   6. Assign to start
#   7. Assign to end
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_range_list(...))
#       calls get_range_list with: start=3, end=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_range_list, get_range_list, get_range_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_range_list(3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1829:
#
# ===== SYNTAX =====
#   def get_even_range(start, end)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   operators: + % ==
#
# ===== EVALUATION ORDER =====
#   get_even_range(1, 10): gets the literal 1, gets the literal 10, then runs the function body
#   get_even_range(start, end): looks up variable 'start', looks up variable 'end', then runs the function body
#   print(get_even_range(3, 20)): the inner get_even_range() executes FIRST, then print() outputs the result
#   get_even_range(3, 20): gets the literal 3, gets the literal 20, then runs the function body
#   end + 1: first get end, then get 1, then compute end + 1
#   x % 2: first get x, then get 2, then compute x % 2
#   end + 1: first get end, then get 1, then compute end + 1
#   x % 2: first get x, then get 2, then compute x % 2
#   end + 1: first get end, then get 1, then compute end + 1
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_even_range(start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_even_range(start, end) — stored for later
#   5. Define def get_even_range(start, end) — stored for later
#   6. Assign to start
#   7. Assign to end
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_even_range(...))
#       calls get_even_range with: start=3, end=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_even_range, get_even_range, get_even_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_even_range(3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1830:
#
# ===== SYNTAX =====
#   def get_odd_range(start, end)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   operators: + % !=
#
# ===== EVALUATION ORDER =====
#   get_odd_range(1, 10): gets the literal 1, gets the literal 10, then runs the function body
#   get_odd_range(start, end): looks up variable 'start', looks up variable 'end', then runs the function body
#   print(get_odd_range(3, 20)): the inner get_odd_range() executes FIRST, then print() outputs the result
#   get_odd_range(3, 20): gets the literal 3, gets the literal 20, then runs the function body
#   end + 1: first get end, then get 1, then compute end + 1
#   x % 2: first get x, then get 2, then compute x % 2
#   end + 1: first get end, then get 1, then compute end + 1
#   x % 2: first get x, then get 2, then compute x % 2
#   end + 1: first get end, then get 1, then compute end + 1
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_odd_range(start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_odd_range(start, end) — stored for later
#   5. Define def get_odd_range(start, end) — stored for later
#   6. Assign to start
#   7. Assign to end
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_odd_range(...))
#       calls get_odd_range with: start=3, end=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_odd_range, get_odd_range, get_odd_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_odd_range(3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1831:
#
# ===== SYNTAX =====
#   def multiply_lists(list1, list2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   multiply_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   multiply_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(multiply_lists(?, ?)): the inner multiply_lists() executes FIRST, then print() outputs the result
#   multiply_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#   a * b: first get a, then get b, then compute a * b
#   a * b: first get a, then get b, then compute a * b
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def multiply_lists(list1, list2) — stored for later
#   5. Define def multiply_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(multiply_lists(...))
#       calls multiply_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_lists, multiply_lists, multiply_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_lists(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1832:
#
# ===== SYNTAX =====
#   def add_lists(list1, list2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   add_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   add_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(add_lists(?, ?)): the inner add_lists() executes FIRST, then print() outputs the result
#   add_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Define def add_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def add_lists(list1, list2) — stored for later
#   5. Define def add_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(add_lists(...))
#       calls add_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_lists, add_lists, add_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_lists(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1833:
#
# ===== SYNTAX =====
#   def subtract_lists(list1, list2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   comprehension
#   operators: -
#
# ===== EVALUATION ORDER =====
#   subtract_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   subtract_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(subtract_lists(?, ?)): the inner subtract_lists() executes FIRST, then print() outputs the result
#   subtract_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   a - b: first get a, then get b, then compute a - b
#   a - b: first get a, then get b, then compute a - b
#   a - b: first get a, then get b, then compute a - b
#
# ===== EXECUTION ORDER =====
#   1. Define def subtract_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def subtract_lists(list1, list2) — stored for later
#   5. Define def subtract_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(subtract_lists(...))
#       calls subtract_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions subtract_lists, subtract_lists, subtract_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(subtract_lists(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1834:
#
# ===== SYNTAX =====
#   def get_index_of_max(numbers)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_index_of_max(?): resolves the value, then runs the function body
#   get_index_of_max(numbers): looks up variable 'numbers', then runs the function body
#   print(get_index_of_max(?)): the inner get_index_of_max() executes FIRST, then print() outputs the result
#   get_index_of_max(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_index_of_max(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_index_of_max(numbers) — stored for later
#   5. Define def get_index_of_max(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_index_of_max(...))
#       calls get_index_of_max with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_index_of_max, get_index_of_max, get_index_of_max are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_index_of_max(?)) → function runs first, then print shows the result

"""

"""
Problem 1835:
#
# ===== SYNTAX =====
#   def get_index_of_min(numbers)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_index_of_min(?): resolves the value, then runs the function body
#   get_index_of_min(numbers): looks up variable 'numbers', then runs the function body
#   print(get_index_of_min(?)): the inner get_index_of_min() executes FIRST, then print() outputs the result
#   get_index_of_min(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_index_of_min(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_index_of_min(numbers) — stored for later
#   5. Define def get_index_of_min(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_index_of_min(...))
#       calls get_index_of_min with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_index_of_min, get_index_of_min, get_index_of_min are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_index_of_min(?)) → function runs first, then print shows the result

"""

"""
Problem 1836:
#
# ===== SYNTAX =====
#   def rotate_list_left(my_list, n)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   rotate_list_left(?, 2): resolves the value, gets the literal 2, then runs the function body
#   rotate_list_left(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(rotate_list_left(?, 15)): the inner rotate_list_left() executes FIRST, then print() outputs the result
#   n % len(...): first get n, then get len(...), then compute n % len(...)
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   rotate_list_left(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def rotate_list_left(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def rotate_list_left(my_list, n) — stored for later
#   5. Define def rotate_list_left(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(rotate_list_left(...))
#       calls rotate_list_left with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions rotate_list_left, rotate_list_left, rotate_list_left are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(rotate_list_left(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1837:
#
# ===== SYNTAX =====
#   def rotate_list_right(my_list, n)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   rotate_list_right(?, 2): resolves the value, gets the literal 2, then runs the function body
#   rotate_list_right(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(rotate_list_right(?, 15)): the inner rotate_list_right() executes FIRST, then print() outputs the result
#   n % len(...): first get n, then get len(...), then compute n % len(...)
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   rotate_list_right(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def rotate_list_right(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def rotate_list_right(my_list, n) — stored for later
#   5. Define def rotate_list_right(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(rotate_list_right(...))
#       calls rotate_list_right with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions rotate_list_right, rotate_list_right, rotate_list_right are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(rotate_list_right(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1838:
#
# ===== SYNTAX =====
#   def chunk_list(my_list, n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   chunk_list(?, 3): resolves the value, gets the literal 3, then runs the function body
#   chunk_list(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(chunk_list(?, 15)): the inner chunk_list() executes FIRST, then print() outputs the result
#   chunk_list(?, 15): resolves the value, gets the literal 15, then runs the function body
#   i + n: first get i, then get n, then compute i + n
#   i + n: first get i, then get n, then compute i + n
#   i + n: first get i, then get n, then compute i + n
#
# ===== EXECUTION ORDER =====
#   1. Define def chunk_list(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def chunk_list(my_list, n) — stored for later
#   5. Define def chunk_list(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(chunk_list(...))
#       calls chunk_list with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions chunk_list, chunk_list, chunk_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(chunk_list(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1839:
#
# ===== SYNTAX =====
#   def interleave_lists(list1, list2)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   interleave_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   interleave_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(interleave_lists(?, ?)): the inner interleave_lists() executes FIRST, then print() outputs the result
#   interleave_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   i < len(...): is this True or False? The answer picks which branch
#   i < len(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def interleave_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def interleave_lists(list1, list2) — stored for later
#   5. Define def interleave_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(interleave_lists(...))
#       calls interleave_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions interleave_lists, interleave_lists, interleave_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(interleave_lists(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1840:
#
# ===== SYNTAX =====
#   def zip_lists(list1, list2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   zip() — built-in function call
#
# ===== EVALUATION ORDER =====
#   zip_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   zip_lists(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(zip_lists(?, ?)): the inner zip_lists() executes FIRST, then print() outputs the result
#   zip_lists(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def zip_lists(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def zip_lists(list1, list2) — stored for later
#   5. Define def zip_lists(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(zip_lists(...))
#       calls zip_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions zip_lists, zip_lists, zip_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(zip_lists(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1841:
#
# ===== SYNTAX =====
#   def unzip_list(tuples_list)
#   return — exits the function and sends back a value
#   comprehension
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   unzip_list(?): resolves the value, then runs the function body
#   unzip_list(tuples_list): looks up variable 'tuples_list', then runs the function body
#   print(unzip_list(?)): the inner unzip_list() executes FIRST, then print() outputs the result
#   unzip_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def unzip_list(tuples_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def unzip_list(tuples_list) — stored for later
#   5. Define def unzip_list(tuples_list) — stored for later
#   6. Assign to tuples_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(unzip_list(...))
#       calls unzip_list with: tuples_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions unzip_list, unzip_list, unzip_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      tuples_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(unzip_list(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1842:
#
# ===== SYNTAX =====
#   def count_uppercase_letters(text)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   count_uppercase_letters('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_uppercase_letters(text): looks up variable 'text', then runs the function body
#   print(count_uppercase_letters('world')): the inner count_uppercase_letters() executes FIRST, then print() outputs the result
#   count_uppercase_letters('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_uppercase_letters(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_uppercase_letters(text) — stored for later
#   5. Define def count_uppercase_letters(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_uppercase_letters(...))
#       calls count_uppercase_letters with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_uppercase_letters, count_uppercase_letters, count_uppercase_letters are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_uppercase_letters('world')) → function runs first, then print shows the result

"""

"""
Problem 1843:
#
# ===== SYNTAX =====
#   def count_lowercase_letters(text)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   count_lowercase_letters('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_lowercase_letters(text): looks up variable 'text', then runs the function body
#   print(count_lowercase_letters('world')): the inner count_lowercase_letters() executes FIRST, then print() outputs the result
#   count_lowercase_letters('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_lowercase_letters(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_lowercase_letters(text) — stored for later
#   5. Define def count_lowercase_letters(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_lowercase_letters(...))
#       calls count_lowercase_letters with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_lowercase_letters, count_lowercase_letters, count_lowercase_letters are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_lowercase_letters('world')) → function runs first, then print shows the result

"""

"""
Problem 1844:
#
# ===== SYNTAX =====
#   def swap_uppercase_lowercase(text)
#   return — exits the function and sends back a value
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   swap_uppercase_lowercase('Hello World'): gets the literal 'Hello World', then runs the function body
#   swap_uppercase_lowercase(text): looks up variable 'text', then runs the function body
#   print(swap_uppercase_lowercase('world')): the inner swap_uppercase_lowercase() executes FIRST, then print() outputs the result
#   swap_uppercase_lowercase('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def swap_uppercase_lowercase(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def swap_uppercase_lowercase(text) — stored for later
#   5. Define def swap_uppercase_lowercase(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(swap_uppercase_lowercase(...))
#       calls swap_uppercase_lowercase with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions swap_uppercase_lowercase, swap_uppercase_lowercase, swap_uppercase_lowercase are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(swap_uppercase_lowercase('world')) → function runs first, then print shows the result

"""

"""
Problem 1845:
#
# ===== SYNTAX =====
#   def remove_numbers_from_string(text)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   remove_numbers_from_string('Hello123World456'): gets the literal 'Hello123World456', then runs the function body
#   remove_numbers_from_string(text): looks up variable 'text', then runs the function body
#   print(remove_numbers_from_string('world')): the inner remove_numbers_from_string() executes FIRST, then print() outputs the result
#   remove_numbers_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_numbers_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_numbers_from_string(text) — stored for later
#   5. Define def remove_numbers_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_numbers_from_string(...))
#       calls remove_numbers_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_numbers_from_string, remove_numbers_from_string, remove_numbers_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_numbers_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1846:
#
# ===== SYNTAX =====
#   def remove_letters_from_string(text)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   remove_letters_from_string('Hello123World456!'): gets the literal 'Hello123World456!', then runs the function body
#   remove_letters_from_string(text): looks up variable 'text', then runs the function body
#   print(remove_letters_from_string('world')): the inner remove_letters_from_string() executes FIRST, then print() outputs the result
#   remove_letters_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_letters_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_letters_from_string(text) — stored for later
#   5. Define def remove_letters_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_letters_from_string(...))
#       calls remove_letters_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_letters_from_string, remove_letters_from_string, remove_letters_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_letters_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1847:
#
# ===== SYNTAX =====
#   def extract_numbers_from_string(text)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   extract_numbers_from_string('Hello123World456'): gets the literal 'Hello123World456', then runs the function body
#   extract_numbers_from_string(text): looks up variable 'text', then runs the function body
#   print(extract_numbers_from_string('world')): the inner extract_numbers_from_string() executes FIRST, then print() outputs the result
#   extract_numbers_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def extract_numbers_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def extract_numbers_from_string(text) — stored for later
#   5. Define def extract_numbers_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(extract_numbers_from_string(...))
#       calls extract_numbers_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions extract_numbers_from_string, extract_numbers_from_string, extract_numbers_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(extract_numbers_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1848:
#
# ===== SYNTAX =====
#   def extract_letters_from_string(text)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   extract_letters_from_string('Hello123World456'): gets the literal 'Hello123World456', then runs the function body
#   extract_letters_from_string(text): looks up variable 'text', then runs the function body
#   print(extract_letters_from_string('world')): the inner extract_letters_from_string() executes FIRST, then print() outputs the result
#   extract_letters_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def extract_letters_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def extract_letters_from_string(text) — stored for later
#   5. Define def extract_letters_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(extract_letters_from_string(...))
#       calls extract_letters_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions extract_letters_from_string, extract_letters_from_string, extract_letters_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(extract_letters_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1849:
#
# ===== SYNTAX =====
#   def format_phone_number(digits)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_phone_number('1234567890'): gets the literal '1234567890', then runs the function body
#   format_phone_number(digits): looks up variable 'digits', then runs the function body
#   print(format_phone_number(?)): the inner format_phone_number() executes FIRST, then print() outputs the result
#   format_phone_number(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_phone_number(digits) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_phone_number(digits) — stored for later
#   5. Define def format_phone_number(digits) — stored for later
#   6. Assign to digits
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(format_phone_number(...))
#       calls format_phone_number with: digits=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_phone_number, format_phone_number, format_phone_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      digits = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_phone_number(?)) → function runs first, then print shows the result

"""

"""
Problem 1850:
#
# ===== SYNTAX =====
#   def calculate_volume_box(length, width, height)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_volume_box(5, 3, 4): gets the literal 5, gets the literal 3, gets the literal 4, then runs the function body
#   calculate_volume_box(length, width, height): looks up variable 'length', looks up variable 'width', looks up variable 'height', then runs the function body
#   print(calculate_volume_box(25, 25, 20)): the inner calculate_volume_box() executes FIRST, then print() outputs the result
#   ? * height: first get ?, then get height, then compute ? * height
#   ? * height: first get ?, then get height, then compute ? * height
#   ? * height: first get ?, then get height, then compute ? * height
#   calculate_volume_box(25, 25, 20): gets the literal 25, gets the literal 25, gets the literal 20, then runs the function body
#   length * width: first get length, then get width, then compute length * width
#   length * width: first get length, then get width, then compute length * width
#   length * width: first get length, then get width, then compute length * width
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_volume_box(length, width, height) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_volume_box(length, width, height) — stored for later
#   5. Define def calculate_volume_box(length, width, height) — stored for later
#   6. Assign to length
#   7. Assign to width
#   8. Assign to height
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_volume_box(...))
#       calls calculate_volume_box with: length=25, width=25, height=20
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_volume_box, calculate_volume_box, calculate_volume_box are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      height = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_volume_box(25, 25, 20)) → function runs first, then print shows the result

"""

"""
Problem 1851:
#
# ===== SYNTAX =====
#   def check_triangle_valid(a, b, c)
#   return — exits the function and sends back a value
#   operators: + > and
#
# ===== EVALUATION ORDER =====
#   print(check_triangle_valid(3, 4, 5)): the inner check_triangle_valid() executes FIRST, then print() outputs the result
#   print(check_triangle_valid(1, 2, 5)): the inner check_triangle_valid() executes FIRST, then print() outputs the result
#   check_triangle_valid(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(check_triangle_valid(20, 30, 10)): the inner check_triangle_valid() executes FIRST, then print() outputs the result
#   check_triangle_valid(3, 4, 5): gets the literal 3, gets the literal 4, gets the literal 5, then runs the function body
#   check_triangle_valid(1, 2, 5): gets the literal 1, gets the literal 2, gets the literal 5, then runs the function body
#   check_triangle_valid(20, 30, 10): gets the literal 20, gets the literal 30, gets the literal 10, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   a + c: first get a, then get c, then compute a + c
#   b + c: first get b, then get c, then compute b + c
#   a + b: first get a, then get b, then compute a + b
#   a + c: first get a, then get c, then compute a + c
#   b + c: first get b, then get c, then compute b + c
#   a + b: first get a, then get b, then compute a + b
#   a + c: first get a, then get c, then compute a + c
#   b + c: first get b, then get c, then compute b + c
#
# ===== EXECUTION ORDER =====
#   1. Define def check_triangle_valid(a, b, c) — stored for later
#   2. Run print(check_triangle_valid(...), check_triangle_valid(...))
#       calls check_triangle_valid with: a=3, b=4, c=5
#         2.1 Return from function
#   3. Define def check_triangle_valid(a, b, c) — stored for later
#   4. Define def check_triangle_valid(a, b, c) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to c
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(check_triangle_valid(...))
#       calls check_triangle_valid with: a=20, b=30, c=10
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_triangle_valid, check_triangle_valid, check_triangle_valid are just stored
#   Execution starts at the non-def lines below:
#      print(check_triangle_valid(3, 4, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_triangle_valid(20, 30, 10)) → function runs first, then print shows the result

"""

"""
Problem 1852:
#
# ===== SYNTAX =====
#   def find_grade(score)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   print(find_grade(95)): the inner find_grade() executes FIRST, then print() outputs the result
#   print(find_grade(75)): the inner find_grade() executes FIRST, then print() outputs the result
#   print(find_grade(55)): the inner find_grade() executes FIRST, then print() outputs the result
#   find_grade(score): looks up variable 'score', then runs the function body
#   print(find_grade(25)): the inner find_grade() executes FIRST, then print() outputs the result
#   find_grade(95): gets the literal 95, then runs the function body
#   find_grade(75): gets the literal 75, then runs the function body
#   find_grade(55): gets the literal 55, then runs the function body
#   find_grade(25): gets the literal 25, then runs the function body
#   score >= 90: is this True or False? The answer picks which branch
#   score >= 80: is this True or False? The answer picks which branch
#   score >= 70: is this True or False? The answer picks which branch
#   score >= 60: is this True or False? The answer picks which branch
#   score >= 80: is this True or False? The answer picks which branch
#   score >= 70: is this True or False? The answer picks which branch
#   score >= 60: is this True or False? The answer picks which branch
#   score >= 70: is this True or False? The answer picks which branch
#   score >= 60: is this True or False? The answer picks which branch
#   score >= 60: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_grade(score) — stored for later
#   2. Run print(find_grade(...), find_grade(...), find_grade(...))
#       calls find_grade with: score=95
#         2.1 Return from function
#   3. Define def find_grade(score) — stored for later
#   4. Define def find_grade(score) — stored for later
#   5. Assign to score
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(find_grade(...))
#       calls find_grade with: score=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_grade, find_grade, find_grade are just stored
#   Execution starts at the non-def lines below:
#      print(find_grade(95)) → function runs first, then print shows the result
#      score = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_grade(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1853:
#
# ===== SYNTAX =====
#   def get_season(month)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(get_season(1)): the inner get_season() executes FIRST, then print() outputs the result
#   print(get_season(4)): the inner get_season() executes FIRST, then print() outputs the result
#   print(get_season(7)): the inner get_season() executes FIRST, then print() outputs the result
#   print(get_season(10)): the inner get_season() executes FIRST, then print() outputs the result
#   get_season(month): looks up variable 'month', then runs the function body
#   print(get_season(25)): the inner get_season() executes FIRST, then print() outputs the result
#   get_season(1): gets the literal 1, then runs the function body
#   get_season(4): gets the literal 4, then runs the function body
#   get_season(7): gets the literal 7, then runs the function body
#   get_season(10): gets the literal 10, then runs the function body
#   get_season(25): gets the literal 25, then runs the function body
#   month in [12, 1, 2]: is this True or False? The answer picks which branch
#   month in [3, 4, 5]: is this True or False? The answer picks which branch
#   month in [6, 7, 8]: is this True or False? The answer picks which branch
#   month in [3, 4, 5]: is this True or False? The answer picks which branch
#   month in [6, 7, 8]: is this True or False? The answer picks which branch
#   month in [6, 7, 8]: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_season(month) — stored for later
#   2. Run print(get_season(...), get_season(...), get_season(...), get_season(...))
#       calls get_season with: month=1
#         2.1 Return from function
#   3. Define def get_season(month) — stored for later
#   4. Define def get_season(month) — stored for later
#   5. Assign to month
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(get_season(...))
#       calls get_season with: month=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_season, get_season, get_season are just stored
#   Execution starts at the non-def lines below:
#      print(get_season(1)) → function runs first, then print shows the result
#      month = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_season(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1854:
#
# ===== SYNTAX =====
#   def calculate_discount(price, discount_percent)
#   return — exits the function and sends back a value
#   operators: - * /
#
# ===== EVALUATION ORDER =====
#   calculate_discount(100, 20): gets the literal 100, gets the literal 20, then runs the function body
#   calculate_discount(price, discount_percent): looks up variable 'price', looks up variable 'discount_percent', then runs the function body
#   print(calculate_discount(25, 25)): the inner calculate_discount() executes FIRST, then print() outputs the result
#   price * ?: first get price, then get ?, then compute price * ?
#   price * ?: first get price, then get ?, then compute price * ?
#   price * ?: first get price, then get ?, then compute price * ?
#   calculate_discount(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   1 - ?: first get 1, then get ?, then compute 1 - ?
#   1 - ?: first get 1, then get ?, then compute 1 - ?
#   1 - ?: first get 1, then get ?, then compute 1 - ?
#   discount_percent / 100: first get discount_percent, then get 100, then compute discount_percent / 100
#   discount_percent / 100: first get discount_percent, then get 100, then compute discount_percent / 100
#   discount_percent / 100: first get discount_percent, then get 100, then compute discount_percent / 100
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_discount(price, discount_percent) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_discount(price, discount_percent) — stored for later
#   5. Define def calculate_discount(price, discount_percent) — stored for later
#   6. Assign to price
#   7. Assign to discount_percent
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_discount(...))
#       calls calculate_discount with: price=25, discount_percent=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_discount, calculate_discount, calculate_discount are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      price = ... (stored in memory)
#      discount_percent = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_discount(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1855:
#
# ===== SYNTAX =====
#   def calculate_tip(bill_amount, tip_percent)
#   return — exits the function and sends back a value
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   calculate_tip(100, 15): gets the literal 100, gets the literal 15, then runs the function body
#   calculate_tip(bill_amount, tip_percent): looks up variable 'bill_amount', looks up variable 'tip_percent', then runs the function body
#   print(calculate_tip(25, 25)): the inner calculate_tip() executes FIRST, then print() outputs the result
#   bill_amount * ?: first get bill_amount, then get ?, then compute bill_amount * ?
#   bill_amount + tip: first get bill_amount, then get tip, then compute bill_amount + tip
#   calculate_tip(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   tip_percent / 100: first get tip_percent, then get 100, then compute tip_percent / 100
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_tip(bill_amount, tip_percent) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_tip(bill_amount, tip_percent) — stored for later
#   5. Define def calculate_tip(bill_amount, tip_percent) — stored for later
#   6. Assign to bill_amount
#   7. Assign to tip_percent
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_tip(...))
#       calls calculate_tip with: bill_amount=25, tip_percent=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_tip, calculate_tip, calculate_tip are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      bill_amount = ... (stored in memory)
#      tip_percent = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_tip(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1856:
#
# ===== SYNTAX =====
#   def swap_two_elements(my_list, index1, index2)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   swap_two_elements(?, 0, 3): resolves the value, gets the literal 0, gets the literal 3, then runs the function body
#   swap_two_elements(my_list, index1, index2): looks up variable 'my_list', looks up variable 'index1', looks up variable 'index2', then runs the function body
#   print(swap_two_elements(?, 25, 25)): the inner swap_two_elements() executes FIRST, then print() outputs the result
#   swap_two_elements(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def swap_two_elements(my_list, index1, index2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def swap_two_elements(my_list, index1, index2) — stored for later
#   5. Define def swap_two_elements(my_list, index1, index2) — stored for later
#   6. Assign to my_list
#   7. Assign to index1
#   8. Assign to index2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(swap_two_elements(...))
#       calls swap_two_elements with: my_list=[4, 1, 8, 6, 3], index1=25, index2=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions swap_two_elements, swap_two_elements, swap_two_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      index1 = ... (stored in memory)
#      index2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(swap_two_elements(?, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1857:
#
# ===== SYNTAX =====
#   def insert_at_index(my_list, index, value)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   insert_at_index(?, 2, 99): resolves the value, gets the literal 2, gets the literal 99, then runs the function body
#   insert_at_index(my_list, index, value): looks up variable 'my_list', looks up variable 'index', looks up variable 'value', then runs the function body
#   print(insert_at_index(?, 4, 25)): the inner insert_at_index() executes FIRST, then print() outputs the result
#   insert_at_index(?, 4, 25): resolves the value, gets the literal 4, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def insert_at_index(my_list, index, value) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def insert_at_index(my_list, index, value) — stored for later
#   5. Define def insert_at_index(my_list, index, value) — stored for later
#   6. Assign to my_list
#   7. Assign to index
#   8. Assign to value
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(insert_at_index(...))
#       calls insert_at_index with: my_list=[4, 1, 8, 6, 3], index=4, value=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions insert_at_index, insert_at_index, insert_at_index are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      index = ... (stored in memory)
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(insert_at_index(?, 4, 25)) → function runs first, then print shows the result

"""

"""
Problem 1858:
#
# ===== SYNTAX =====
#   def remove_at_index(my_list, index)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_at_index(?, 1): resolves the value, gets the literal 1, then runs the function body
#   remove_at_index(my_list, index): looks up variable 'my_list', looks up variable 'index', then runs the function body
#   print(remove_at_index(?, 4)): the inner remove_at_index() executes FIRST, then print() outputs the result
#   remove_at_index(?, 4): resolves the value, gets the literal 4, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_at_index(my_list, index) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_at_index(my_list, index) — stored for later
#   5. Define def remove_at_index(my_list, index) — stored for later
#   6. Assign to my_list
#   7. Assign to index
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(remove_at_index(...))
#       calls remove_at_index with: my_list=[4, 1, 8, 6, 3], index=4
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_at_index, remove_at_index, remove_at_index are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      index = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_at_index(?, 4)) → function runs first, then print shows the result

"""

"""
Problem 1860:
#
# ===== SYNTAX =====
#   def get_min_max(numbers)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_min_max(?): resolves the value, then runs the function body
#   get_min_max(numbers): looks up variable 'numbers', then runs the function body
#   print(get_min_max(?)): the inner get_min_max() executes FIRST, then print() outputs the result
#   get_min_max(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_min_max(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_min_max(numbers) — stored for later
#   5. Define def get_min_max(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_min_max(...))
#       calls get_min_max with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_min_max, get_min_max, get_min_max are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_min_max(?)) → function runs first, then print shows the result

"""

"""
Problem 1861:
#
# ===== SYNTAX =====
#   def check_all_conditions(number)
#   return — exits the function and sends back a value
#   operators: % == > and
#
# ===== EVALUATION ORDER =====
#   print(check_all_conditions(6)): the inner check_all_conditions() executes FIRST, then print() outputs the result
#   print(check_all_conditions(5)): the inner check_all_conditions() executes FIRST, then print() outputs the result
#   check_all_conditions(number): looks up variable 'number', then runs the function body
#   print(check_all_conditions(25)): the inner check_all_conditions() executes FIRST, then print() outputs the result
#   check_all_conditions(6): gets the literal 6, then runs the function body
#   check_all_conditions(5): gets the literal 5, then runs the function body
#   check_all_conditions(25): gets the literal 25, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 3: first get number, then get 3, then compute number % 3
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 3: first get number, then get 3, then compute number % 3
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 3: first get number, then get 3, then compute number % 3
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_conditions(number) — stored for later
#   2. Run print(check_all_conditions(...), check_all_conditions(...))
#       calls check_all_conditions with: number=6
#         2.1 Return from function
#   3. Define def check_all_conditions(number) — stored for later
#   4. Define def check_all_conditions(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_all_conditions(...))
#       calls check_all_conditions with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_conditions, check_all_conditions, check_all_conditions are just stored
#   Execution starts at the non-def lines below:
#      print(check_all_conditions(6)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_conditions(25)) → function runs first, then print shows the result

"""

"""
Problem 1862:
#
# ===== SYNTAX =====
#   def format_full_name(first, middle, last)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_full_name('John', 'Michael', 'Doe'): gets the literal 'John', gets the literal 'Michael', gets the literal 'Doe', then runs the function body
#   format_full_name(first, middle, last): looks up variable 'first', looks up variable 'middle', looks up variable 'last', then runs the function body
#   print(format_full_name(25, 25, 25)): the inner format_full_name() executes FIRST, then print() outputs the result
#   '%s %s %s' % (first, middle, last): first get '%s %s %s', then get (first, middle, last), then compute '%s %s %s' % (first, middle, last)
#   format_full_name(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_full_name(first, middle, last) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_full_name(first, middle, last) — stored for later
#   5. Define def format_full_name(first, middle, last) — stored for later
#   6. Assign to first
#   7. Assign to middle
#   8. Assign to last
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_full_name(...))
#       calls format_full_name with: first=25, middle=25, last=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_full_name, format_full_name, format_full_name are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      first = ... (stored in memory)
#      middle = ... (stored in memory)
#      last = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_full_name(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1863:
#
# ===== SYNTAX =====
#   def calculate_bmi(weight_kg, height_m)
#   return — exits the function and sends back a value
#   round() — built-in function call
#   operators: / **
#
# ===== EVALUATION ORDER =====
#   calculate_bmi(70, 1.75): gets the literal 70, gets the literal 1.75, then runs the function body
#   calculate_bmi(weight_kg, height_m): looks up variable 'weight_kg', looks up variable 'height_m', then runs the function body
#   print(calculate_bmi(25, 25)): the inner calculate_bmi() executes FIRST, then print() outputs the result
#   weight_kg / ?: first get weight_kg, then get ?, then compute weight_kg / ?
#   calculate_bmi(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   height_m ** 2: first get height_m, then get 2, then compute height_m ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_bmi(weight_kg, height_m) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_bmi(weight_kg, height_m) — stored for later
#   5. Define def calculate_bmi(weight_kg, height_m) — stored for later
#   6. Assign to weight_kg
#   7. Assign to height_m
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_bmi(...))
#       calls calculate_bmi with: weight_kg=25, height_m=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_bmi, calculate_bmi, calculate_bmi are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      weight_kg = ... (stored in memory)
#      height_m = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_bmi(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1864:
#
# ===== SYNTAX =====
#   def check_bmi_category(weight_kg, height_m)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: / ** <
#
# ===== EVALUATION ORDER =====
#   check_bmi_category(70, 1.75): gets the literal 70, gets the literal 1.75, then runs the function body
#   check_bmi_category(weight_kg, height_m): looks up variable 'weight_kg', looks up variable 'height_m', then runs the function body
#   print(check_bmi_category(25, 25)): the inner check_bmi_category() executes FIRST, then print() outputs the result
#   weight_kg / ?: first get weight_kg, then get ?, then compute weight_kg / ?
#   check_bmi_category(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   height_m ** 2: first get height_m, then get 2, then compute height_m ** 2
#   bmi < 18.5: is this True or False? The answer picks which branch
#   bmi < 25: is this True or False? The answer picks which branch
#   bmi < 30: is this True or False? The answer picks which branch
#   bmi < 25: is this True or False? The answer picks which branch
#   bmi < 30: is this True or False? The answer picks which branch
#   bmi < 30: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_bmi_category(weight_kg, height_m) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_bmi_category(weight_kg, height_m) — stored for later
#   5. Define def check_bmi_category(weight_kg, height_m) — stored for later
#   6. Assign to weight_kg
#   7. Assign to height_m
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(check_bmi_category(...))
#       calls check_bmi_category with: weight_kg=25, height_m=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_bmi_category, check_bmi_category, check_bmi_category are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      weight_kg = ... (stored in memory)
#      height_m = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_bmi_category(25, 25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1865:
#
# ===== SYNTAX =====
#   def find_common_elements_three(list1, list2, list3)
#   return — exits the function and sends back a value
#   comprehension
#   operators: and in
#
# ===== EVALUATION ORDER =====
#   find_common_elements_three(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   find_common_elements_three(list1, list2, list3): looks up variable 'list1', looks up variable 'list2', looks up variable 'list3', then runs the function body
#   print(find_common_elements_three(?, ?, ?)): the inner find_common_elements_three() executes FIRST, then print() outputs the result
#   find_common_elements_three(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_common_elements_three(list1, list2, list3) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_common_elements_three(list1, list2, list3) — stored for later
#   5. Define def find_common_elements_three(list1, list2, list3) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to list3
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(find_common_elements_three(...))
#       calls find_common_elements_three with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3], list3=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_common_elements_three, find_common_elements_three, find_common_elements_three are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_common_elements_three(?, ?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1866:
#
# ===== SYNTAX =====
#   def merge_three_lists(list1, list2, list3)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   merge_three_lists(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   merge_three_lists(list1, list2, list3): looks up variable 'list1', looks up variable 'list2', looks up variable 'list3', then runs the function body
#   print(merge_three_lists(25, 25, ?)): the inner merge_three_lists() executes FIRST, then print() outputs the result
#   ? + list3: first get ?, then get list3, then compute ? + list3
#   ? + list3: first get ?, then get list3, then compute ? + list3
#   ? + list3: first get ?, then get list3, then compute ? + list3
#   merge_three_lists(25, 25, ?): gets the literal 25, gets the literal 25, resolves the value, then runs the function body
#   list1 + list2: first get list1, then get list2, then compute list1 + list2
#   list1 + list2: first get list1, then get list2, then compute list1 + list2
#   list1 + list2: first get list1, then get list2, then compute list1 + list2
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_three_lists(list1, list2, list3) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def merge_three_lists(list1, list2, list3) — stored for later
#   5. Define def merge_three_lists(list1, list2, list3) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to list3
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(merge_three_lists(...))
#       calls merge_three_lists with: list1=25, list2=25, list3=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_three_lists, merge_three_lists, merge_three_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_three_lists(25, 25, ?)) → function runs first, then print shows the result

"""

"""
Problem 1867:
#
# ===== SYNTAX =====
#   def get_elements_at_indices(my_list, indices)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_elements_at_indices(?, ?): resolves the value, resolves the value, then runs the function body
#   get_elements_at_indices(my_list, indices): looks up variable 'my_list', looks up variable 'indices', then runs the function body
#   print(get_elements_at_indices(?, ?)): the inner get_elements_at_indices() executes FIRST, then print() outputs the result
#   get_elements_at_indices(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_at_indices(my_list, indices) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_at_indices(my_list, indices) — stored for later
#   5. Define def get_elements_at_indices(my_list, indices) — stored for later
#   6. Assign to my_list
#   7. Assign to indices
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_elements_at_indices(...))
#       calls get_elements_at_indices with: my_list=[4, 1, 8, 6, 3], indices=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_at_indices, get_elements_at_indices, get_elements_at_indices are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      indices = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_at_indices(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1868:
#
# ===== SYNTAX =====
#   def replace_multiple_chars(text, replacements)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   replace_multiple_chars('Hello World', ?): gets the literal 'Hello World', resolves the value, then runs the function body
#   replace_multiple_chars(text, replacements): looks up variable 'text', looks up variable 'replacements', then runs the function body
#   print(replace_multiple_chars('world', 25)): the inner replace_multiple_chars() executes FIRST, then print() outputs the result
#   replace_multiple_chars('world', 25): gets the literal 'world', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def replace_multiple_chars(text, replacements) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def replace_multiple_chars(text, replacements) — stored for later
#   5. Define def replace_multiple_chars(text, replacements) — stored for later
#   6. Assign to text
#   7. Assign to replacements
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(replace_multiple_chars(...))
#       calls replace_multiple_chars with: text='world', replacements=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions replace_multiple_chars, replace_multiple_chars, replace_multiple_chars are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      replacements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(replace_multiple_chars('world', 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from replacements.items(...) each round

"""

"""
Problem 1869:
#
# ===== SYNTAX =====
#   def count_words_by_length(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   count_words_by_length('hello world python programming'): gets the literal 'hello world python programming', then runs the function body
#   count_words_by_length(text): looks up variable 'text', then runs the function body
#   print(count_words_by_length('world')): the inner count_words_by_length() executes FIRST, then print() outputs the result
#   count_words_by_length('world'): gets the literal 'world', then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words_by_length(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_words_by_length(text) — stored for later
#   5. Define def count_words_by_length(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_words_by_length(...))
#       calls count_words_by_length with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words_by_length, count_words_by_length, count_words_by_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words_by_length('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 1870:
#
# ===== SYNTAX =====
#   def filter_and_transform(numbers)
#   return — exits the function and sends back a value
#   comprehension
#   operators: * % ==
#
# ===== EVALUATION ORDER =====
#   filter_and_transform(?): resolves the value, then runs the function body
#   filter_and_transform(numbers): looks up variable 'numbers', then runs the function body
#   print(filter_and_transform(?)): the inner filter_and_transform() executes FIRST, then print() outputs the result
#   filter_and_transform(?): resolves the value, then runs the function body
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_and_transform(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def filter_and_transform(numbers) — stored for later
#   5. Define def filter_and_transform(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(filter_and_transform(...))
#       calls filter_and_transform with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_and_transform, filter_and_transform, filter_and_transform are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_and_transform(?)) → function runs first, then print shows the result

"""

"""
Problem 1871:
#
# ===== SYNTAX =====
#   def get_strings_starting_with(strings, prefix)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_strings_starting_with(?, 'ap'): resolves the value, gets the literal 'ap', then runs the function body
#   get_strings_starting_with(strings, prefix): looks up variable 'strings', looks up variable 'prefix', then runs the function body
#   print(get_strings_starting_with(?, 'Hi, ')): the inner get_strings_starting_with() executes FIRST, then print() outputs the result
#   get_strings_starting_with(?, 'Hi, '): resolves the value, gets the literal 'Hi, ', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_starting_with(strings, prefix) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_starting_with(strings, prefix) — stored for later
#   5. Define def get_strings_starting_with(strings, prefix) — stored for later
#   6. Assign to strings
#   7. Assign to prefix
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_strings_starting_with(...))
#       calls get_strings_starting_with with: strings=[4, 1, 8, 6, 3], prefix='Hi, '
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_starting_with, get_strings_starting_with, get_strings_starting_with are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      prefix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_starting_with(?, 'Hi, ')) → function runs first, then print shows the result

"""

"""
Problem 1872:
#
# ===== SYNTAX =====
#   def get_strings_ending_with(strings, suffix)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_strings_ending_with(?, 'ing'): resolves the value, gets the literal 'ing', then runs the function body
#   get_strings_ending_with(strings, suffix): looks up variable 'strings', looks up variable 'suffix', then runs the function body
#   print(get_strings_ending_with(?, '?')): the inner get_strings_ending_with() executes FIRST, then print() outputs the result
#   get_strings_ending_with(?, '?'): resolves the value, gets the literal '?', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_ending_with(strings, suffix) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_ending_with(strings, suffix) — stored for later
#   5. Define def get_strings_ending_with(strings, suffix) — stored for later
#   6. Assign to strings
#   7. Assign to suffix
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_strings_ending_with(...))
#       calls get_strings_ending_with with: strings=[4, 1, 8, 6, 3], suffix='?'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_ending_with, get_strings_ending_with, get_strings_ending_with are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      suffix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_ending_with(?, '?')) → function runs first, then print shows the result

"""

"""
Problem 1873:
#
# ===== SYNTAX =====
#   def calculate_total_cost(prices, tax_rate)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   calculate_total_cost(?, 10): resolves the value, gets the literal 10, then runs the function body
#   calculate_total_cost(prices, tax_rate): looks up variable 'prices', looks up variable 'tax_rate', then runs the function body
#   print(calculate_total_cost(?, 25)): the inner calculate_total_cost() executes FIRST, then print() outputs the result
#   subtotal * ?: first get subtotal, then get ?, then compute subtotal * ?
#   subtotal + tax: first get subtotal, then get tax, then compute subtotal + tax
#   calculate_total_cost(?, 25): resolves the value, gets the literal 25, then runs the function body
#   tax_rate / 100: first get tax_rate, then get 100, then compute tax_rate / 100
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_total_cost(prices, tax_rate) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_total_cost(prices, tax_rate) — stored for later
#   5. Define def calculate_total_cost(prices, tax_rate) — stored for later
#   6. Assign to prices
#   7. Assign to tax_rate
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_total_cost(...))
#       calls calculate_total_cost with: prices=[4, 1, 8, 6, 3], tax_rate=25
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_total_cost, calculate_total_cost, calculate_total_cost are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      prices = ... (stored in memory)
#      tax_rate = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_total_cost(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1874:
#
# ===== SYNTAX =====
#   def find_all_indices(my_list, element)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   find_all_indices(?, 10): resolves the value, gets the literal 10, then runs the function body
#   find_all_indices(my_list, element): looks up variable 'my_list', looks up variable 'element', then runs the function body
#   print(find_all_indices(?, 25)): the inner find_all_indices() executes FIRST, then print() outputs the result
#   find_all_indices(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_all_indices(my_list, element) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_all_indices(my_list, element) — stored for later
#   5. Define def find_all_indices(my_list, element) — stored for later
#   6. Assign to my_list
#   7. Assign to element
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(find_all_indices(...))
#       calls find_all_indices with: my_list=[4, 1, 8, 6, 3], element=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_all_indices, find_all_indices, find_all_indices are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_all_indices(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1875:
#
# ===== SYNTAX =====
#   def split_into_chunks(my_list, chunk_size)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   split_into_chunks(?, 3): resolves the value, gets the literal 3, then runs the function body
#   split_into_chunks(my_list, chunk_size): looks up variable 'my_list', looks up variable 'chunk_size', then runs the function body
#   print(split_into_chunks(?, 25)): the inner split_into_chunks() executes FIRST, then print() outputs the result
#   split_into_chunks(?, 25): resolves the value, gets the literal 25, then runs the function body
#   i + chunk_size: first get i, then get chunk_size, then compute i + chunk_size
#   i + chunk_size: first get i, then get chunk_size, then compute i + chunk_size
#   i + chunk_size: first get i, then get chunk_size, then compute i + chunk_size
#
# ===== EXECUTION ORDER =====
#   1. Define def split_into_chunks(my_list, chunk_size) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def split_into_chunks(my_list, chunk_size) — stored for later
#   5. Define def split_into_chunks(my_list, chunk_size) — stored for later
#   6. Assign to my_list
#   7. Assign to chunk_size
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(split_into_chunks(...))
#       calls split_into_chunks with: my_list=[4, 1, 8, 6, 3], chunk_size=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions split_into_chunks, split_into_chunks, split_into_chunks are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      chunk_size = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(split_into_chunks(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1876:
#
# ===== SYNTAX =====
#   def remove_multiple_elements(my_list, elements_to_remove)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   remove_multiple_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   remove_multiple_elements(my_list, elements_to_remove): looks up variable 'my_list', looks up variable 'elements_to_remove', then runs the function body
#   print(remove_multiple_elements(?, ?)): the inner remove_multiple_elements() executes FIRST, then print() outputs the result
#   remove_multiple_elements(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_multiple_elements(my_list, elements_to_remove) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_multiple_elements(my_list, elements_to_remove) — stored for later
#   5. Define def remove_multiple_elements(my_list, elements_to_remove) — stored for later
#   6. Assign to my_list
#   7. Assign to elements_to_remove
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(remove_multiple_elements(...))
#       calls remove_multiple_elements with: my_list=[4, 1, 8, 6, 3], elements_to_remove=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_multiple_elements, remove_multiple_elements, remove_multiple_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      elements_to_remove = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_multiple_elements(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1877:
#
# ===== SYNTAX =====
#   def count_occurrences_multiple(my_list, elements)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   count_occurrences_multiple(?, ?): resolves the value, resolves the value, then runs the function body
#   count_occurrences_multiple(my_list, elements): looks up variable 'my_list', looks up variable 'elements', then runs the function body
#   print(count_occurrences_multiple(?, ?)): the inner count_occurrences_multiple() executes FIRST, then print() outputs the result
#   count_occurrences_multiple(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences_multiple(my_list, elements) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_occurrences_multiple(my_list, elements) — stored for later
#   5. Define def count_occurrences_multiple(my_list, elements) — stored for later
#   6. Assign to my_list
#   7. Assign to elements
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_occurrences_multiple(...))
#       calls count_occurrences_multiple with: my_list=[4, 1, 8, 6, 3], elements=[5, 3, 8, 4]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences_multiple, count_occurrences_multiple, count_occurrences_multiple are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      elements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences_multiple(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1878:
#
# ===== SYNTAX =====
#   def get_unique_elements_three(list1, list2, list3)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   set() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   get_unique_elements_three(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   print(sorted(result)): inner expression resolves first, then print() outputs
#   get_unique_elements_three(list1, list2, list3): looks up variable 'list1', looks up variable 'list2', looks up variable 'list3', then runs the function body
#   print(get_unique_elements_three(25, 25, ?)): the inner get_unique_elements_three() executes FIRST, then print() outputs the result
#   ? + list3: first get ?, then get list3, then compute ? + list3
#   get_unique_elements_three(25, 25, ?): gets the literal 25, gets the literal 25, resolves the value, then runs the function body
#   list1 + list2: first get list1, then get list2, then compute list1 + list2
#   ? + list3.count(...): first get ?, then get list3.count(...), then compute ? + list3.count(...)
#   list1.count(...) + list2.count(...): first get list1.count(...), then get list2.count(...), then compute list1.count(...) + list2.count(...)
#   count == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_unique_elements_three(list1, list2, list3) — stored for later
#   2. Assign to result
#   3. Run print(sorted(...))
#   4. Define def get_unique_elements_three(list1, list2, list3) — stored for later
#   5. Define def get_unique_elements_three(list1, list2, list3) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to list3
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_unique_elements_three(...))
#       calls get_unique_elements_three with: list1=25, list2=25, list3=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_unique_elements_three, get_unique_elements_three, get_unique_elements_three are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(sorted(...))
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_unique_elements_three(25, 25, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from all_elements each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1879:
#
# ===== SYNTAX =====
#   def sort_by_multiple_criteria(people)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   sort_by_multiple_criteria(?): resolves the value, then runs the function body
#   sort_by_multiple_criteria(people): looks up variable 'people', then runs the function body
#   print(sort_by_multiple_criteria(?)): the inner sort_by_multiple_criteria() executes FIRST, then print() outputs the result
#   sort_by_multiple_criteria(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_multiple_criteria(people) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_by_multiple_criteria(people) — stored for later
#   5. Define def sort_by_multiple_criteria(people) — stored for later
#   6. Assign to people
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_by_multiple_criteria(...))
#       calls sort_by_multiple_criteria with: people=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_multiple_criteria, sort_by_multiple_criteria, sort_by_multiple_criteria are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      people = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_multiple_criteria(?)) → function runs first, then print shows the result

"""

"""
Problem 1880:
#
# ===== SYNTAX =====
#   def calculate_statistics(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   min() — built-in function call
#   max() — built-in function call
#   len() — built-in function call
#   calculate_statistics() — built-in function call
#   operators: /
#
# ===== EVALUATION ORDER =====
#   calculate_statistics(?): resolves the value, then runs the function body
#   calculate_statistics(numbers): looks up variable 'numbers', then runs the function body
#   print(calculate_statistics(?)): the inner calculate_statistics() executes FIRST, then print() outputs the result
#   calculate_statistics(?): resolves the value, then runs the function body
#   calculate_statistics(?): resolves the value, then runs the function body
#   calculate_statistics(?): resolves the value, then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_statistics(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_statistics(...))
#       calls calculate_statistics with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_statistics, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_statistics(?)) → function runs first, then print shows the result

"""

"""
Problem 1881:
#
# ===== SYNTAX =====
#   def extract_numbers_from_string(text)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   extract_numbers_from_string('I have 3 cats and 2 dogs'): gets the literal 'I have 3 cats and 2 dogs', then runs the function body
#   extract_numbers_from_string(text): looks up variable 'text', then runs the function body
#   print(extract_numbers_from_string('world')): the inner extract_numbers_from_string() executes FIRST, then print() outputs the result
#   extract_numbers_from_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def extract_numbers_from_string(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def extract_numbers_from_string(text) — stored for later
#   5. Define def extract_numbers_from_string(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(extract_numbers_from_string(...))
#       calls extract_numbers_from_string with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions extract_numbers_from_string, extract_numbers_from_string, extract_numbers_from_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(extract_numbers_from_string('world')) → function runs first, then print shows the result

"""

"""
Problem 1883:
#
# ===== SYNTAX =====
#   def check_password_strength(password)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: >= and
#
# ===== EVALUATION ORDER =====
#   print(check_password_strength('Password123')): the inner check_password_strength() executes FIRST, then print() outputs the result
#   print(check_password_strength('weak')): the inner check_password_strength() executes FIRST, then print() outputs the result
#   check_password_strength(password): looks up variable 'password', then runs the function body
#   print(check_password_strength(?)): the inner check_password_strength() executes FIRST, then print() outputs the result
#   check_password_strength('Password123'): gets the literal 'Password123', then runs the function body
#   check_password_strength('weak'): gets the literal 'weak', then runs the function body
#   check_password_strength(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_password_strength(password) — stored for later
#   2. Run print(check_password_strength(...), check_password_strength(...))
#       calls check_password_strength with: password='Password123'
#         2.1 Return from function
#   3. Define def check_password_strength(password) — stored for later
#   4. Define def check_password_strength(password) — stored for later
#   5. Assign to password
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_password_strength(...))
#       calls check_password_strength with: password=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_password_strength, check_password_strength, check_password_strength are just stored
#   Execution starts at the non-def lines below:
#      print(check_password_strength('Password123')) → function runs first, then print shows the result
#      password = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_password_strength(?)) → function runs first, then print shows the result

"""

"""
Problem 1884:
#
# ===== SYNTAX =====
#   def get_words_by_length_range(text, min_length, max_length)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   comprehension
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   get_words_by_length_range('hello world python programming', 5, 6): gets the literal 'hello world python programming', gets the literal 5, gets the literal 6, then runs the function body
#   get_words_by_length_range(text, min_length, max_length): looks up variable 'text', looks up variable 'min_length', looks up variable 'max_length', then runs the function body
#   print(get_words_by_length_range('world', 25, 25)): the inner get_words_by_length_range() executes FIRST, then print() outputs the result
#   get_words_by_length_range('world', 25, 25): gets the literal 'world', gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_by_length_range(text, min_length, max_length) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_by_length_range(text, min_length, max_length) — stored for later
#   5. Define def get_words_by_length_range(text, min_length, max_length) — stored for later
#   6. Assign to text
#   7. Assign to min_length
#   8. Assign to max_length
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_words_by_length_range(...))
#       calls get_words_by_length_range with: text='world', min_length=25, max_length=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_by_length_range, get_words_by_length_range, get_words_by_length_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      min_length = ... (stored in memory)
#      max_length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_by_length_range('world', 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1885:
#
# ===== SYNTAX =====
#   def calculate_compound_interest(principal, rate, time, frequency)
#   return — exits the function and sends back a value
#   operators: + * / **
#
# ===== EVALUATION ORDER =====
#   calculate_compound_interest(1000, 5, 2, 12): gets the literal 1000, gets the literal 5, gets the literal 2, gets the literal 12, then runs the function body
#   print(round(result, 2)): inner expression resolves first, then print() outputs
#   calculate_compound_interest(principal, rate, time, frequency): looks up variable 'principal', looks up variable 'rate', looks up variable 'time', looks up variable 'frequency', then runs the function body
#   print(calculate_compound_interest(25, 25, 25, 25)): the inner calculate_compound_interest() executes FIRST, then print() outputs the result
#   principal * ?: first get principal, then get ?, then compute principal * ?
#   principal * ?: first get principal, then get ?, then compute principal * ?
#   principal * ?: first get principal, then get ?, then compute principal * ?
#   calculate_compound_interest(25, 25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#   ? ** ?: first get ?, then get ?, then compute ? ** ?
#   ? ** ?: first get ?, then get ?, then compute ? ** ?
#   ? ** ?: first get ?, then get ?, then compute ? ** ?
#   1 + ?: first get 1, then get ?, then compute 1 + ?
#   frequency * time: first get frequency, then get time, then compute frequency * time
#   1 + ?: first get 1, then get ?, then compute 1 + ?
#   frequency * time: first get frequency, then get time, then compute frequency * time
#   1 + ?: first get 1, then get ?, then compute 1 + ?
#   frequency * time: first get frequency, then get time, then compute frequency * time
#   rate / ?: first get rate, then get ?, then compute rate / ?
#   rate / ?: first get rate, then get ?, then compute rate / ?
#   rate / ?: first get rate, then get ?, then compute rate / ?
#   100 * frequency: first get 100, then get frequency, then compute 100 * frequency
#   100 * frequency: first get 100, then get frequency, then compute 100 * frequency
#   100 * frequency: first get 100, then get frequency, then compute 100 * frequency
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_compound_interest(principal, rate, time, frequency) — stored for later
#   2. Assign to result
#   3. Run print(round(...))
#   4. Define def calculate_compound_interest(principal, rate, time, frequency) — stored for later
#   5. Define def calculate_compound_interest(principal, rate, time, frequency) — stored for later
#   6. Assign to principal
#   7. Assign to rate
#   8. Assign to time
#   9. Assign to frequency
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(calculate_compound_interest(...))
#       calls calculate_compound_interest with: principal=25, rate=25, time=25, frequency=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_compound_interest, calculate_compound_interest, calculate_compound_interest are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(round(...))
#      principal = ... (stored in memory)
#      rate = ... (stored in memory)
#      time = ... (stored in memory)
#      frequency = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_compound_interest(25, 25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1886:
#
# ===== SYNTAX =====
#   def find_duplicate_elements(my_list)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   list() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   find_duplicate_elements(?): resolves the value, then runs the function body
#   print(sorted(result)): inner expression resolves first, then print() outputs
#   find_duplicate_elements(my_list): looks up variable 'my_list', then runs the function body
#   print(find_duplicate_elements(?)): the inner find_duplicate_elements() executes FIRST, then print() outputs the result
#   find_duplicate_elements(?): resolves the value, then runs the function body
#   item in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_duplicate_elements(my_list) — stored for later
#   2. Assign to result
#   3. Run print(sorted(...))
#   4. Define def find_duplicate_elements(my_list) — stored for later
#   5. Define def find_duplicate_elements(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(find_duplicate_elements(...))
#       calls find_duplicate_elements with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_duplicate_elements, find_duplicate_elements, find_duplicate_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(sorted(...))
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_duplicate_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1887:
#
# ===== SYNTAX =====
#   def group_by_first_letter(strings)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   group_by_first_letter(?): resolves the value, then runs the function body
#   group_by_first_letter(strings): looks up variable 'strings', then runs the function body
#   print(group_by_first_letter(?)): the inner group_by_first_letter() executes FIRST, then print() outputs the result
#   group_by_first_letter(?): resolves the value, then runs the function body
#   first_letter not in groups: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def group_by_first_letter(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def group_by_first_letter(strings) — stored for later
#   5. Define def group_by_first_letter(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(group_by_first_letter(...))
#       calls group_by_first_letter with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions group_by_first_letter, group_by_first_letter, group_by_first_letter are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(group_by_first_letter(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1888:
#
# ===== SYNTAX =====
#   def calculate_distance(x1, y1, x2, y2)
#   return — exits the function and sends back a value
#   operators: + - **
#
# ===== EVALUATION ORDER =====
#   calculate_distance(0, 0, 3, 4): gets the literal 0, gets the literal 0, gets the literal 3, gets the literal 4, then runs the function body
#   calculate_distance(x1, y1, x2, y2): looks up variable 'x1', looks up variable 'y1', looks up variable 'x2', looks up variable 'y2', then runs the function body
#   print(calculate_distance(25, 25, 25, 25)): the inner calculate_distance() executes FIRST, then print() outputs the result
#   calculate_distance(25, 25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   x2 - x1: first get x2, then get x1, then compute x2 - x1
#   y2 - y1: first get y2, then get y1, then compute y2 - y1
#   x2 - x1: first get x2, then get x1, then compute x2 - x1
#   y2 - y1: first get y2, then get y1, then compute y2 - y1
#   x2 - x1: first get x2, then get x1, then compute x2 - x1
#   y2 - y1: first get y2, then get y1, then compute y2 - y1
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_distance(x1, y1, x2, y2) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def calculate_distance(x1, y1, x2, y2) — stored for later
#   6. Define def calculate_distance(x1, y1, x2, y2) — stored for later
#   7. Assign to x1
#   8. Assign to y1
#   9. Assign to x2
#   10. Assign to y2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(calculate_distance(...))
#       calls calculate_distance with: x1=25, y1=25, x2=25, y2=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_distance, calculate_distance, calculate_distance are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      x1 = ... (stored in memory)
#      y1 = ... (stored in memory)
#      x2 = ... (stored in memory)
#      y2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_distance(25, 25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1889:
#
# ===== SYNTAX =====
#   def check_in_range(number, min_val, max_val)
#   return — exits the function and sends back a value
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   print(check_in_range(5, 1, 10)): the inner check_in_range() executes FIRST, then print() outputs the result
#   print(check_in_range(15, 1, 10)): the inner check_in_range() executes FIRST, then print() outputs the result
#   check_in_range(number, min_val, max_val): looks up variable 'number', looks up variable 'min_val', looks up variable 'max_val', then runs the function body
#   print(check_in_range(25, 25, 25)): the inner check_in_range() executes FIRST, then print() outputs the result
#   check_in_range(5, 1, 10): gets the literal 5, gets the literal 1, gets the literal 10, then runs the function body
#   check_in_range(15, 1, 10): gets the literal 15, gets the literal 1, gets the literal 10, then runs the function body
#   check_in_range(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_in_range(number, min_val, max_val) — stored for later
#   2. Run print(check_in_range(...), check_in_range(...))
#       calls check_in_range with: number=5, min_val=1, max_val=10
#         2.1 Return from function
#   3. Define def check_in_range(number, min_val, max_val) — stored for later
#   4. Define def check_in_range(number, min_val, max_val) — stored for later
#   5. Assign to number
#   6. Assign to min_val
#   7. Assign to max_val
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(check_in_range(...))
#       calls check_in_range with: number=25, min_val=25, max_val=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_in_range, check_in_range, check_in_range are just stored
#   Execution starts at the non-def lines below:
#      print(check_in_range(5, 1, 10)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      min_val = ... (stored in memory)
#      max_val = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_in_range(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1890:
#
# ===== SYNTAX =====
#   def get_divisors(number)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   operators: + % ==
#
# ===== EVALUATION ORDER =====
#   get_divisors(12): gets the literal 12, then runs the function body
#   get_divisors(number): looks up variable 'number', then runs the function body
#   print(get_divisors(25)): the inner get_divisors() executes FIRST, then print() outputs the result
#   get_divisors(25): gets the literal 25, then runs the function body
#   number + 1: first get number, then get 1, then compute number + 1
#   number % i: first get number, then get i, then compute number % i
#   number + 1: first get number, then get 1, then compute number + 1
#   number % i: first get number, then get i, then compute number % i
#   number + 1: first get number, then get 1, then compute number + 1
#   number % i: first get number, then get i, then compute number % i
#
# ===== EXECUTION ORDER =====
#   1. Define def get_divisors(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_divisors(number) — stored for later
#   5. Define def get_divisors(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_divisors(...))
#       calls get_divisors with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_divisors, get_divisors, get_divisors are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_divisors(25)) → function runs first, then print shows the result

"""

"""
Problem 1891:
#
# ===== SYNTAX =====
#   def check_perfect_square(number)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_perfect_square(16)): the inner check_perfect_square() executes FIRST, then print() outputs the result
#   print(check_perfect_square(15)): the inner check_perfect_square() executes FIRST, then print() outputs the result
#   check_perfect_square(number): looks up variable 'number', then runs the function body
#   print(check_perfect_square(25)): the inner check_perfect_square() executes FIRST, then print() outputs the result
#   check_perfect_square(16): gets the literal 16, then runs the function body
#   check_perfect_square(15): gets the literal 15, then runs the function body
#   check_perfect_square(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_perfect_square(number) — stored for later
#   2. Run print(check_perfect_square(...), check_perfect_square(...))
#       calls check_perfect_square with: number=16
#         2.1 Return from function
#   3. Define def check_perfect_square(number) — stored for later
#   4. Define def check_perfect_square(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(check_perfect_square(...))
#       calls check_perfect_square with: number=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_perfect_square, check_perfect_square, check_perfect_square are just stored
#   Execution starts at the non-def lines below:
#      print(check_perfect_square(16)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_perfect_square(25)) → function runs first, then print shows the result

"""

"""
Problem 1892:
#
# ===== SYNTAX =====
#   def format_time(hours, minutes, seconds)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_time(9, 5, 3): gets the literal 9, gets the literal 5, gets the literal 3, then runs the function body
#   format_time(hours, minutes, seconds): looks up variable 'hours', looks up variable 'minutes', looks up variable 'seconds', then runs the function body
#   print(format_time(25, 25, 25)): the inner format_time() executes FIRST, then print() outputs the result
#   format_time(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_time(hours, minutes, seconds) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_time(hours, minutes, seconds) — stored for later
#   5. Define def format_time(hours, minutes, seconds) — stored for later
#   6. Assign to hours
#   7. Assign to minutes
#   8. Assign to seconds
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_time(...))
#       calls format_time with: hours=25, minutes=25, seconds=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_time, format_time, format_time are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      hours = ... (stored in memory)
#      minutes = ... (stored in memory)
#      seconds = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_time(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1893:
#
# ===== SYNTAX =====
#   def calculate_age(birth_year, birth_month, birth_day)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: + - <
#
# ===== EVALUATION ORDER =====
#   calculate_age(2000, 6, 15): gets the literal 2000, gets the literal 6, gets the literal 15, then runs the function body
#   calculate_age(birth_year, birth_month, birth_day): looks up variable 'birth_year', looks up variable 'birth_month', looks up variable 'birth_day', then runs the function body
#   print(calculate_age(25, 25, 25)): the inner calculate_age() executes FIRST, then print() outputs the result
#   current_year - birth_year: first get current_year, then get birth_year, then compute current_year - birth_year
#   current_year + -birth_year: first get current_year, then get -birth_year, then compute current_year + -birth_year
#   calculate_age(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#   (current_month, current_day) < (birth_month, birth_day): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_age(birth_year, birth_month, birth_day) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_age(birth_year, birth_month, birth_day) — stored for later
#   5. Define def calculate_age(birth_year, birth_month, birth_day) — stored for later
#   6. Assign to birth_year
#   7. Assign to birth_month
#   8. Assign to birth_day
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_age(...))
#       calls calculate_age with: birth_year=25, birth_month=25, birth_day=25
#         11.1 Import operator
#         11.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_age, calculate_age, calculate_age are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      birth_year = ... (stored in memory)
#      birth_month = ... (stored in memory)
#      birth_day = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_age(25, 25, 25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1894:
#
# ===== SYNTAX =====
#   def get_middle_elements(my_list)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_middle_elements(?): resolves the value, then runs the function body
#   get_middle_elements(my_list): looks up variable 'my_list', then runs the function body
#   print(get_middle_elements(?)): the inner get_middle_elements() executes FIRST, then print() outputs the result
#   get_middle_elements(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_middle_elements(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_middle_elements(my_list) — stored for later
#   5. Define def get_middle_elements(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_middle_elements(...))
#       calls get_middle_elements with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_middle_elements, get_middle_elements, get_middle_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_middle_elements(?)) → function runs first, then print shows the result

"""

"""
Problem 1895:
#
# ===== SYNTAX =====
#   def swap_adjacent_elements(my_list)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   in
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   swap_adjacent_elements(?): resolves the value, then runs the function body
#   swap_adjacent_elements(my_list): looks up variable 'my_list', then runs the function body
#   print(swap_adjacent_elements(?)): the inner swap_adjacent_elements() executes FIRST, then print() outputs the result
#   swap_adjacent_elements(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def swap_adjacent_elements(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def swap_adjacent_elements(my_list) — stored for later
#   5. Define def swap_adjacent_elements(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(swap_adjacent_elements(...))
#       calls swap_adjacent_elements with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions swap_adjacent_elements, swap_adjacent_elements, swap_adjacent_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(swap_adjacent_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1896:
#
# ===== SYNTAX =====
#   def get_every_nth_element(my_list, n)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_every_nth_element(?, 3): resolves the value, gets the literal 3, then runs the function body
#   get_every_nth_element(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(get_every_nth_element(?, 15)): the inner get_every_nth_element() executes FIRST, then print() outputs the result
#   get_every_nth_element(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_every_nth_element(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_every_nth_element(my_list, n) — stored for later
#   5. Define def get_every_nth_element(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_every_nth_element(...))
#       calls get_every_nth_element with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_every_nth_element, get_every_nth_element, get_every_nth_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_every_nth_element(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1898:
#
# ===== SYNTAX =====
#   def remove_duplicates_preserve_order(my_list)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   remove_duplicates_preserve_order(?): resolves the value, then runs the function body
#   remove_duplicates_preserve_order(my_list): looks up variable 'my_list', then runs the function body
#   print(remove_duplicates_preserve_order(?)): the inner remove_duplicates_preserve_order() executes FIRST, then print() outputs the result
#   remove_duplicates_preserve_order(?): resolves the value, then runs the function body
#   item not in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates_preserve_order(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_duplicates_preserve_order(my_list) — stored for later
#   5. Define def remove_duplicates_preserve_order(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_duplicates_preserve_order(...))
#       calls remove_duplicates_preserve_order with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates_preserve_order, remove_duplicates_preserve_order, remove_duplicates_preserve_order are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates_preserve_order(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1899:
#
# ===== SYNTAX =====
#   def get_intersection_three_lists(list1, list2, list3)
#   return — exits the function and sends back a value
#   comprehension
#   operators: and in
#
# ===== EVALUATION ORDER =====
#   get_intersection_three_lists(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   get_intersection_three_lists(list1, list2, list3): looks up variable 'list1', looks up variable 'list2', looks up variable 'list3', then runs the function body
#   print(get_intersection_three_lists(?, ?, ?)): the inner get_intersection_three_lists() executes FIRST, then print() outputs the result
#   get_intersection_three_lists(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_intersection_three_lists(list1, list2, list3) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_intersection_three_lists(list1, list2, list3) — stored for later
#   5. Define def get_intersection_three_lists(list1, list2, list3) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to list3
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_intersection_three_lists(...))
#       calls get_intersection_three_lists with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3], list3=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_intersection_three_lists, get_intersection_three_lists, get_intersection_three_lists are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_intersection_three_lists(?, ?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1900:
#
# ===== SYNTAX =====
#   def calculate_percentage(part, whole)
#   return — exits the function and sends back a value
#   round() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   calculate_percentage(25, 100): gets the literal 25, gets the literal 100, then runs the function body
#   calculate_percentage(part, whole): looks up variable 'part', looks up variable 'whole', then runs the function body
#   print(calculate_percentage(25, 25)): the inner calculate_percentage() executes FIRST, then print() outputs the result
#   calculate_percentage(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   ? * 100: first get ?, then get 100, then compute ? * 100
#   ? * 100: first get ?, then get 100, then compute ? * 100
#   ? * 100: first get ?, then get 100, then compute ? * 100
#   part / whole: first get part, then get whole, then compute part / whole
#   part / whole: first get part, then get whole, then compute part / whole
#   part / whole: first get part, then get whole, then compute part / whole
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_percentage(part, whole) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_percentage(part, whole) — stored for later
#   5. Define def calculate_percentage(part, whole) — stored for later
#   6. Assign to part
#   7. Assign to whole
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_percentage(...))
#       calls calculate_percentage with: part=25, whole=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_percentage, calculate_percentage, calculate_percentage are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      part = ... (stored in memory)
#      whole = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_percentage(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1901:
#
# ===== SYNTAX =====
#   def find_largest_three(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   find_largest_three(?): resolves the value, then runs the function body
#   find_largest_three(numbers): looks up variable 'numbers', then runs the function body
#   print(find_largest_three(?)): the inner find_largest_three() executes FIRST, then print() outputs the result
#   find_largest_three(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_largest_three(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_largest_three(numbers) — stored for later
#   5. Define def find_largest_three(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(find_largest_three(...))
#       calls find_largest_three with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_largest_three, find_largest_three, find_largest_three are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_largest_three(?)) → function runs first, then print shows the result

"""

"""
Problem 1902:
#
# ===== SYNTAX =====
#   def find_smallest_three(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   find_smallest_three(?): resolves the value, then runs the function body
#   find_smallest_three(numbers): looks up variable 'numbers', then runs the function body
#   print(find_smallest_three(?)): the inner find_smallest_three() executes FIRST, then print() outputs the result
#   find_smallest_three(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_smallest_three(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_smallest_three(numbers) — stored for later
#   5. Define def find_smallest_three(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(find_smallest_three(...))
#       calls find_smallest_three with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_smallest_three, find_smallest_three, find_smallest_three are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_smallest_three(?)) → function runs first, then print shows the result

"""

"""
Problem 1903:
#
# ===== SYNTAX =====
#   def check_palindrome_and_length(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   check_palindrome_and_length('racecar'): gets the literal 'racecar', then runs the function body
#   check_palindrome_and_length(text): looks up variable 'text', then runs the function body
#   print(check_palindrome_and_length('world')): the inner check_palindrome_and_length() executes FIRST, then print() outputs the result
#   check_palindrome_and_length('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_palindrome_and_length(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_palindrome_and_length(text) — stored for later
#   5. Define def check_palindrome_and_length(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_palindrome_and_length(...))
#       calls check_palindrome_and_length with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_palindrome_and_length, check_palindrome_and_length, check_palindrome_and_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_palindrome_and_length('world')) → function runs first, then print shows the result

"""

"""
Problem 1904:
#
# ===== SYNTAX =====
#   def get_words_starting_with_vowel(text)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   get_words_starting_with_vowel('apple orange banana cherry'): gets the literal 'apple orange banana cherry', then runs the function body
#   get_words_starting_with_vowel(text): looks up variable 'text', then runs the function body
#   print(get_words_starting_with_vowel('world')): the inner get_words_starting_with_vowel() executes FIRST, then print() outputs the result
#   get_words_starting_with_vowel('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_starting_with_vowel(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_starting_with_vowel(text) — stored for later
#   5. Define def get_words_starting_with_vowel(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_words_starting_with_vowel(...))
#       calls get_words_starting_with_vowel with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_starting_with_vowel, get_words_starting_with_vowel, get_words_starting_with_vowel are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_starting_with_vowel('world')) → function runs first, then print shows the result

"""

"""
Problem 1905:
#
# ===== SYNTAX =====
#   def get_words_ending_with_consonant(text)
#   return — exits the function and sends back a value
#   comprehension
#   operators: and
#
# ===== EVALUATION ORDER =====
#   get_words_ending_with_consonant('hello world python'): gets the literal 'hello world python', then runs the function body
#   get_words_ending_with_consonant(text): looks up variable 'text', then runs the function body
#   print(get_words_ending_with_consonant('world')): the inner get_words_ending_with_consonant() executes FIRST, then print() outputs the result
#   get_words_ending_with_consonant('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_ending_with_consonant(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_ending_with_consonant(text) — stored for later
#   5. Define def get_words_ending_with_consonant(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_words_ending_with_consonant(...))
#       calls get_words_ending_with_consonant with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_ending_with_consonant, get_words_ending_with_consonant, get_words_ending_with_consonant are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_ending_with_consonant('world')) → function runs first, then print shows the result

"""

"""
Problem 1906:
#
# ===== SYNTAX =====
#   def calculate_area_perimeter(length, width)
#   return — exits the function and sends back a value
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   calculate_area_perimeter(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   calculate_area_perimeter(length, width): looks up variable 'length', looks up variable 'width', then runs the function body
#   print(calculate_area_perimeter(25, 25)): the inner calculate_area_perimeter() executes FIRST, then print() outputs the result
#   length * width: first get length, then get width, then compute length * width
#   2 * ?: first get 2, then get ?, then compute 2 * ?
#   calculate_area_perimeter(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   length + width: first get length, then get width, then compute length + width
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_area_perimeter(length, width) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_area_perimeter(length, width) — stored for later
#   5. Define def calculate_area_perimeter(length, width) — stored for later
#   6. Assign to length
#   7. Assign to width
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_area_perimeter(...))
#       calls calculate_area_perimeter with: length=25, width=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_area_perimeter, calculate_area_perimeter, calculate_area_perimeter are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_area_perimeter(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1907:
#
# ===== SYNTAX =====
#   def format_currency(amount)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_currency(1234.5): gets the literal 1234.5, then runs the function body
#   format_currency(amount): looks up variable 'amount', then runs the function body
#   print(format_currency(25)): the inner format_currency() executes FIRST, then print() outputs the result
#   format_currency(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_currency(amount) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_currency(amount) — stored for later
#   5. Define def format_currency(amount) — stored for later
#   6. Assign to amount
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(format_currency(...))
#       calls format_currency with: amount=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_currency, format_currency, format_currency are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      amount = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_currency(25)) → function runs first, then print shows the result

"""

"""
Problem 1908:
#
# ===== SYNTAX =====
#   def get_even_indices(my_list)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_even_indices(?): resolves the value, then runs the function body
#   get_even_indices(my_list): looks up variable 'my_list', then runs the function body
#   print(get_even_indices(?)): the inner get_even_indices() executes FIRST, then print() outputs the result
#   get_even_indices(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_even_indices(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_even_indices(my_list) — stored for later
#   5. Define def get_even_indices(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_even_indices(...))
#       calls get_even_indices with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_even_indices, get_even_indices, get_even_indices are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_even_indices(?)) → function runs first, then print shows the result

"""

"""
Problem 1909:
#
# ===== SYNTAX =====
#   def get_odd_indices(my_list)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_odd_indices(?): resolves the value, then runs the function body
#   get_odd_indices(my_list): looks up variable 'my_list', then runs the function body
#   print(get_odd_indices(?)): the inner get_odd_indices() executes FIRST, then print() outputs the result
#   get_odd_indices(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_odd_indices(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_odd_indices(my_list) — stored for later
#   5. Define def get_odd_indices(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_odd_indices(...))
#       calls get_odd_indices with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_odd_indices, get_odd_indices, get_odd_indices are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_odd_indices(?)) → function runs first, then print shows the result

"""

"""
Problem 1910:
#
# ===== SYNTAX =====
#   def multiply_corresponding(list1, list2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   multiply_corresponding(?, ?): resolves the value, resolves the value, then runs the function body
#   multiply_corresponding(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(multiply_corresponding(?, ?)): the inner multiply_corresponding() executes FIRST, then print() outputs the result
#   multiply_corresponding(?, ?): resolves the value, resolves the value, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#   a * b: first get a, then get b, then compute a * b
#   a * b: first get a, then get b, then compute a * b
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_corresponding(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def multiply_corresponding(list1, list2) — stored for later
#   5. Define def multiply_corresponding(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(multiply_corresponding(...))
#       calls multiply_corresponding with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_corresponding, multiply_corresponding, multiply_corresponding are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_corresponding(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1911:
#
# ===== SYNTAX =====
#   def divide_corresponding(list1, list2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: / ==
#
# ===== EVALUATION ORDER =====
#   divide_corresponding(?, ?): resolves the value, resolves the value, then runs the function body
#   divide_corresponding(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(divide_corresponding(?, ?)): the inner divide_corresponding() executes FIRST, then print() outputs the result
#   divide_corresponding(?, ?): resolves the value, resolves the value, then runs the function body
#   a / b: first get a, then get b, then compute a / b
#   b == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divide_corresponding(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def divide_corresponding(list1, list2) — stored for later
#   5. Define def divide_corresponding(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(divide_corresponding(...))
#       calls divide_corresponding with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divide_corresponding, divide_corresponding, divide_corresponding are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divide_corresponding(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1912:
#
# ===== SYNTAX =====
#   def get_strings_containing(strings, substring)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   get_strings_containing(?, 'on'): resolves the value, gets the literal 'on', then runs the function body
#   get_strings_containing(strings, substring): looks up variable 'strings', looks up variable 'substring', then runs the function body
#   print(get_strings_containing(?, 'world')): the inner get_strings_containing() executes FIRST, then print() outputs the result
#   get_strings_containing(?, 'world'): resolves the value, gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_containing(strings, substring) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_containing(strings, substring) — stored for later
#   5. Define def get_strings_containing(strings, substring) — stored for later
#   6. Assign to strings
#   7. Assign to substring
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_strings_containing(...))
#       calls get_strings_containing with: strings=[4, 1, 8, 6, 3], substring='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_containing, get_strings_containing, get_strings_containing are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      substring = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_containing(?, 'world')) → function runs first, then print shows the result

"""

"""
Problem 1913:
#
# ===== SYNTAX =====
#   def count_characters_by_type(text)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   count_characters_by_type() — built-in function call
#   comprehension
#   operators: and
#
# ===== EVALUATION ORDER =====
#   count_characters_by_type('Hello123!'): gets the literal 'Hello123!', then runs the function body
#   count_characters_by_type(text): looks up variable 'text', then runs the function body
#   print(count_characters_by_type('world')): the inner count_characters_by_type() executes FIRST, then print() outputs the result
#   count_characters_by_type('Hello123!'): gets the literal 'Hello123!', then runs the function body
#   count_characters_by_type('Hello123!'): gets the literal 'Hello123!', then runs the function body
#   count_characters_by_type('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_characters_by_type(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_characters_by_type(...))
#       calls count_characters_by_type with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_characters_by_type, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_characters_by_type('world')) → function runs first, then print shows the result

"""

"""
Problem 1914:
#
# ===== SYNTAX =====
#   def get_range_between(my_list, min_val, max_val)
#   return — exits the function and sends back a value
#   comprehension
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   get_range_between(?, 20, 40): resolves the value, gets the literal 20, gets the literal 40, then runs the function body
#   get_range_between(my_list, min_val, max_val): looks up variable 'my_list', looks up variable 'min_val', looks up variable 'max_val', then runs the function body
#   print(get_range_between(?, 25, 25)): the inner get_range_between() executes FIRST, then print() outputs the result
#   get_range_between(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_range_between(my_list, min_val, max_val) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_range_between(my_list, min_val, max_val) — stored for later
#   5. Define def get_range_between(my_list, min_val, max_val) — stored for later
#   6. Assign to my_list
#   7. Assign to min_val
#   8. Assign to max_val
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_range_between(...))
#       calls get_range_between with: my_list=[4, 1, 8, 6, 3], min_val=25, max_val=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_range_between, get_range_between, get_range_between are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      min_val = ... (stored in memory)
#      max_val = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_range_between(?, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1915:
#
# ===== SYNTAX =====
#   def format_date(year, month, day)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_date(2024, 3, 5): gets the literal 2024, gets the literal 3, gets the literal 5, then runs the function body
#   format_date(year, month, day): looks up variable 'year', looks up variable 'month', looks up variable 'day', then runs the function body
#   print(format_date(25, 25, 25)): the inner format_date() executes FIRST, then print() outputs the result
#   format_date(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_date(year, month, day) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_date(year, month, day) — stored for later
#   5. Define def format_date(year, month, day) — stored for later
#   6. Assign to year
#   7. Assign to month
#   8. Assign to day
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_date(...))
#       calls format_date with: year=25, month=25, day=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_date, format_date, format_date are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      year = ... (stored in memory)
#      month = ... (stored in memory)
#      day = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_date(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1916:
#
# ===== SYNTAX =====
#   def calculate_simple_interest(principal, rate, time)
#   return — exits the function and sends back a value
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   calculate_simple_interest(1000, 5, 2): gets the literal 1000, gets the literal 5, gets the literal 2, then runs the function body
#   calculate_simple_interest(principal, rate, time): looks up variable 'principal', looks up variable 'rate', looks up variable 'time', then runs the function body
#   print(calculate_simple_interest(25, 25, 25)): the inner calculate_simple_interest() executes FIRST, then print() outputs the result
#   ? / 100: first get ?, then get 100, then compute ? / 100
#   ? / 100: first get ?, then get 100, then compute ? / 100
#   ? / 100: first get ?, then get 100, then compute ? / 100
#   calculate_simple_interest(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#   ? * time: first get ?, then get time, then compute ? * time
#   ? * time: first get ?, then get time, then compute ? * time
#   ? * time: first get ?, then get time, then compute ? * time
#   principal * rate: first get principal, then get rate, then compute principal * rate
#   principal * rate: first get principal, then get rate, then compute principal * rate
#   principal * rate: first get principal, then get rate, then compute principal * rate
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_simple_interest(principal, rate, time) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_simple_interest(principal, rate, time) — stored for later
#   5. Define def calculate_simple_interest(principal, rate, time) — stored for later
#   6. Assign to principal
#   7. Assign to rate
#   8. Assign to time
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_simple_interest(...))
#       calls calculate_simple_interest with: principal=25, rate=25, time=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_simple_interest, calculate_simple_interest, calculate_simple_interest are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      principal = ... (stored in memory)
#      rate = ... (stored in memory)
#      time = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_simple_interest(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1917:
#
# ===== SYNTAX =====
#   def get_unique_characters(text)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   get_unique_characters('hello'): gets the literal 'hello', then runs the function body
#   get_unique_characters(text): looks up variable 'text', then runs the function body
#   print(get_unique_characters('world')): the inner get_unique_characters() executes FIRST, then print() outputs the result
#   get_unique_characters('world'): gets the literal 'world', then runs the function body
#   char not in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_unique_characters(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_unique_characters(text) — stored for later
#   5. Define def get_unique_characters(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_unique_characters(...))
#       calls get_unique_characters with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_unique_characters, get_unique_characters, get_unique_characters are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_unique_characters('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from text each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1918:
#
# ===== SYNTAX =====
#   def count_words_ending_with(text, suffix)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   count_words_ending_with('running walking jumping', 'ing'): gets the literal 'running walking jumping', gets the literal 'ing', then runs the function body
#   count_words_ending_with(text, suffix): looks up variable 'text', looks up variable 'suffix', then runs the function body
#   print(count_words_ending_with('world', '?')): the inner count_words_ending_with() executes FIRST, then print() outputs the result
#   count_words_ending_with('world', '?'): gets the literal 'world', gets the literal '?', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words_ending_with(text, suffix) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_words_ending_with(text, suffix) — stored for later
#   5. Define def count_words_ending_with(text, suffix) — stored for later
#   6. Assign to text
#   7. Assign to suffix
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_words_ending_with(...))
#       calls count_words_ending_with with: text='world', suffix='?'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words_ending_with, count_words_ending_with, count_words_ending_with are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      suffix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words_ending_with('world', '?')) → function runs first, then print shows the result

"""

"""
Problem 1919:
#
# ===== SYNTAX =====
#   def get_numbers_in_range(numbers, min_val, max_val)
#   return — exits the function and sends back a value
#   comprehension
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   get_numbers_in_range(?, 25, 45): resolves the value, gets the literal 25, gets the literal 45, then runs the function body
#   get_numbers_in_range(numbers, min_val, max_val): looks up variable 'numbers', looks up variable 'min_val', looks up variable 'max_val', then runs the function body
#   print(get_numbers_in_range(?, 25, 25)): the inner get_numbers_in_range() executes FIRST, then print() outputs the result
#   get_numbers_in_range(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_numbers_in_range(numbers, min_val, max_val) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_numbers_in_range(numbers, min_val, max_val) — stored for later
#   5. Define def get_numbers_in_range(numbers, min_val, max_val) — stored for later
#   6. Assign to numbers
#   7. Assign to min_val
#   8. Assign to max_val
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_numbers_in_range(...))
#       calls get_numbers_in_range with: numbers=[4, 1, 8, 6, 3], min_val=25, max_val=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_numbers_in_range, get_numbers_in_range, get_numbers_in_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      min_val = ... (stored in memory)
#      max_val = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_numbers_in_range(?, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1920:
#
# ===== SYNTAX =====
#   def format_name_age_city(name, age, city)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_name_age_city('Alice', 30, 'New York'): gets the literal 'Alice', gets the literal 30, gets the literal 'New York', then runs the function body
#   format_name_age_city(name, age, city): looks up variable 'name', looks up variable 'age', looks up variable 'city', then runs the function body
#   print(format_name_age_city('Bob', 25, 25)): the inner format_name_age_city() executes FIRST, then print() outputs the result
#   'Name: %s, Age: %s, City: %s' % (name, age, city): first get 'Name: %s, Age: %s, City: %s', then get (name, age, city), then compute 'Name: %s, Age: %s, City: %s' % (name, age, city)
#   format_name_age_city('Bob', 25, 25): gets the literal 'Bob', gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_name_age_city(name, age, city) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_name_age_city(name, age, city) — stored for later
#   5. Define def format_name_age_city(name, age, city) — stored for later
#   6. Assign to name
#   7. Assign to age
#   8. Assign to city
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_name_age_city(...))
#       calls format_name_age_city with: name='Bob', age=25, city=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_name_age_city, format_name_age_city, format_name_age_city are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      name = ... (stored in memory)
#      age = ... (stored in memory)
#      city = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_name_age_city('Bob', 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1921:
#
# ===== SYNTAX =====
#   def check_multiple_of(number, divisors)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(check_multiple_of(12, ?)): the inner check_multiple_of() executes FIRST, then print() outputs the result
#   print(check_multiple_of(12, ?)): the inner check_multiple_of() executes FIRST, then print() outputs the result
#   check_multiple_of(number, divisors): looks up variable 'number', looks up variable 'divisors', then runs the function body
#   print(check_multiple_of(25, ?)): the inner check_multiple_of() executes FIRST, then print() outputs the result
#   check_multiple_of(12, ?): gets the literal 12, resolves the value, then runs the function body
#   check_multiple_of(12, ?): gets the literal 12, resolves the value, then runs the function body
#   check_multiple_of(25, ?): gets the literal 25, resolves the value, then runs the function body
#   number % d: first get number, then get d, then compute number % d
#   number % d: first get number, then get d, then compute number % d
#   number % d: first get number, then get d, then compute number % d
#
# ===== EXECUTION ORDER =====
#   1. Define def check_multiple_of(number, divisors) — stored for later
#   2. Run print(check_multiple_of(...), check_multiple_of(...))
#       calls check_multiple_of with: number=12, divisors=[2, 3, 4]
#         2.1 Return from function
#   3. Define def check_multiple_of(number, divisors) — stored for later
#   4. Define def check_multiple_of(number, divisors) — stored for later
#   5. Assign to number
#   6. Assign to divisors
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_multiple_of(...))
#       calls check_multiple_of with: number=25, divisors=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_multiple_of, check_multiple_of, check_multiple_of are just stored
#   Execution starts at the non-def lines below:
#      print(check_multiple_of(12, ?)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      divisors = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_multiple_of(25, ?)) → function runs first, then print shows the result

"""

"""
Problem 1922:
#
# ===== SYNTAX =====
#   def get_sum_of_squares(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   get_sum_of_squares(?): resolves the value, then runs the function body
#   get_sum_of_squares(numbers): looks up variable 'numbers', then runs the function body
#   print(get_sum_of_squares(?)): the inner get_sum_of_squares() executes FIRST, then print() outputs the result
#   get_sum_of_squares(?): resolves the value, then runs the function body
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_sum_of_squares(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_sum_of_squares(numbers) — stored for later
#   5. Define def get_sum_of_squares(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_sum_of_squares(...))
#       calls get_sum_of_squares with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_sum_of_squares, get_sum_of_squares, get_sum_of_squares are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_sum_of_squares(?)) → function runs first, then print shows the result

"""

"""
Problem 1923:
#
# ===== SYNTAX =====
#   def get_product_of_even(numbers)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   get_product_of_even(?): resolves the value, then runs the function body
#   get_product_of_even(numbers): looks up variable 'numbers', then runs the function body
#   print(get_product_of_even(?)): the inner get_product_of_even() executes FIRST, then print() outputs the result
#   get_product_of_even(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_product_of_even(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_product_of_even(numbers) — stored for later
#   5. Define def get_product_of_even(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_product_of_even(...))
#       calls get_product_of_even with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_product_of_even, get_product_of_even, get_product_of_even are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_product_of_even(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1924:
#
# ===== SYNTAX =====
#   def get_sum_of_odd(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   get_sum_of_odd(?): resolves the value, then runs the function body
#   get_sum_of_odd(numbers): looks up variable 'numbers', then runs the function body
#   print(get_sum_of_odd(?)): the inner get_sum_of_odd() executes FIRST, then print() outputs the result
#   get_sum_of_odd(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def get_sum_of_odd(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_sum_of_odd(numbers) — stored for later
#   5. Define def get_sum_of_odd(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_sum_of_odd(...))
#       calls get_sum_of_odd with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_sum_of_odd, get_sum_of_odd, get_sum_of_odd are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_sum_of_odd(?)) → function runs first, then print shows the result

"""

"""
Problem 1925:
#
# ===== SYNTAX =====
#   def find_second_occurrence(my_list, element)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   find_second_occurrence(?, 10): resolves the value, gets the literal 10, then runs the function body
#   find_second_occurrence(my_list, element): looks up variable 'my_list', looks up variable 'element', then runs the function body
#   print(find_second_occurrence(?, 25)): the inner find_second_occurrence() executes FIRST, then print() outputs the result
#   find_second_occurrence(?, 25): resolves the value, gets the literal 25, then runs the function body
#   item == element: is this True or False? The answer picks which branch
#   count == 2: is this True or False? The answer picks which branch
#   count == 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_second_occurrence(my_list, element) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_second_occurrence(my_list, element) — stored for later
#   5. Define def find_second_occurrence(my_list, element) — stored for later
#   6. Assign to my_list
#   7. Assign to element
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(find_second_occurrence(...))
#       calls find_second_occurrence with: my_list=[4, 1, 8, 6, 3], element=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_second_occurrence, find_second_occurrence, find_second_occurrence are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_second_occurrence(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1926:
#
# ===== SYNTAX =====
#   def get_elements_greater_than_average(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: / >
#
# ===== EVALUATION ORDER =====
#   get_elements_greater_than_average(?): resolves the value, then runs the function body
#   get_elements_greater_than_average(numbers): looks up variable 'numbers', then runs the function body
#   print(get_elements_greater_than_average(?)): the inner get_elements_greater_than_average() executes FIRST, then print() outputs the result
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   get_elements_greater_than_average(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_greater_than_average(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_greater_than_average(numbers) — stored for later
#   5. Define def get_elements_greater_than_average(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_elements_greater_than_average(...))
#       calls get_elements_greater_than_average with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_greater_than_average, get_elements_greater_than_average, get_elements_greater_than_average are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_greater_than_average(?)) → function runs first, then print shows the result

"""

"""
Problem 1927:
#
# ===== SYNTAX =====
#   def get_elements_less_than_average(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: / <
#
# ===== EVALUATION ORDER =====
#   get_elements_less_than_average(?): resolves the value, then runs the function body
#   get_elements_less_than_average(numbers): looks up variable 'numbers', then runs the function body
#   print(get_elements_less_than_average(?)): the inner get_elements_less_than_average() executes FIRST, then print() outputs the result
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   get_elements_less_than_average(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_less_than_average(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_less_than_average(numbers) — stored for later
#   5. Define def get_elements_less_than_average(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_elements_less_than_average(...))
#       calls get_elements_less_than_average with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_less_than_average, get_elements_less_than_average, get_elements_less_than_average are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_less_than_average(?)) → function runs first, then print shows the result

"""

"""
Problem 1929:
#
# ===== SYNTAX =====
#   def get_words_with_length(text, length)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   get_words_with_length('hello world python programming', 5): gets the literal 'hello world python programming', gets the literal 5, then runs the function body
#   get_words_with_length(text, length): looks up variable 'text', looks up variable 'length', then runs the function body
#   print(get_words_with_length('world', 25)): the inner get_words_with_length() executes FIRST, then print() outputs the result
#   get_words_with_length('world', 25): gets the literal 'world', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_with_length(text, length) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_with_length(text, length) — stored for later
#   5. Define def get_words_with_length(text, length) — stored for later
#   6. Assign to text
#   7. Assign to length
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_words_with_length(...))
#       calls get_words_with_length with: text='world', length=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_with_length, get_words_with_length, get_words_with_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_with_length('world', 25)) → function runs first, then print shows the result

"""

"""
Problem 1930:
#
# ===== SYNTAX =====
#   def format_full_address(street_num, street_name, city, state, zipcode)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_full_address('123', 'Main St', 'New York', 'NY', '10001'): gets the literal '123', gets the literal 'Main St', gets the literal 'New York', gets the literal 'NY', gets the literal '10001', then runs the function body
#   format_full_address(street_num, street_name, city, state, zipcode): looks up variable 'street_num', looks up variable 'street_name', looks up variable 'city', looks up variable 'state', looks up variable 'zipcode', then runs the function body
#   print(format_full_address(25, 'world', 25, 25, 25)): the inner format_full_address() executes FIRST, then print() outputs the result
#   '%s %s, %s, %s %s' % (street_num, street_name, city, state, zipcode): first get '%s %s, %s, %s %s', then get (street_num, street_name, city, state, zipcode), then compute '%s %s, %s, %s %s' % (street_num, street_name, city, state, zipcode)
#   format_full_address(25, 'world', 25, 25, 25): gets the literal 25, gets the literal 'world', gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_full_address(street_num, street_name, city, state, zipcode) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_full_address(street_num, street_name, city, state, zipcode) — stored for later
#   5. Define def format_full_address(street_num, street_name, city, state, zipcode) — stored for later
#   6. Assign to street_num
#   7. Assign to street_name
#   8. Assign to city
#   9. Assign to state
#   10. Assign to zipcode
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(format_full_address(...))
#       calls format_full_address with: street_num=25, street_name='world', city=25, state=25, zipcode=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_full_address, format_full_address, format_full_address are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      street_num = ... (stored in memory)
#      street_name = ... (stored in memory)
#      city = ... (stored in memory)
#      state = ... (stored in memory)
#      zipcode = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_full_address(25, 'world', 25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1931:
#
# ===== SYNTAX =====
#   def calculate_total_with_discount(prices, discount_percent, tax_rate)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   operators: + - * /
#
# ===== EVALUATION ORDER =====
#   calculate_total_with_discount(?, 10, 8): resolves the value, gets the literal 10, gets the literal 8, then runs the function body
#   print(round(result, 2)): inner expression resolves first, then print() outputs
#   calculate_total_with_discount(prices, discount_percent, tax_rate): looks up variable 'prices', looks up variable 'discount_percent', looks up variable 'tax_rate', then runs the function body
#   print(calculate_total_with_discount(?, 25, 25)): the inner calculate_total_with_discount() executes FIRST, then print() outputs the result
#   subtotal * ?: first get subtotal, then get ?, then compute subtotal * ?
#   discounted * ?: first get discounted, then get ?, then compute discounted * ?
#   calculate_total_with_discount(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#   1 - ?: first get 1, then get ?, then compute 1 - ?
#   1 + ?: first get 1, then get ?, then compute 1 + ?
#   discount_percent / 100: first get discount_percent, then get 100, then compute discount_percent / 100
#   tax_rate / 100: first get tax_rate, then get 100, then compute tax_rate / 100
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_total_with_discount(prices, discount_percent, tax_rate) — stored for later
#   2. Assign to result
#   3. Run print(round(...))
#   4. Define def calculate_total_with_discount(prices, discount_percent, tax_rate) — stored for later
#   5. Define def calculate_total_with_discount(prices, discount_percent, tax_rate) — stored for later
#   6. Assign to prices
#   7. Assign to discount_percent
#   8. Assign to tax_rate
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_total_with_discount(...))
#       calls calculate_total_with_discount with: prices=[4, 1, 8, 6, 3], discount_percent=25, tax_rate=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_total_with_discount, calculate_total_with_discount, calculate_total_with_discount are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(round(...))
#      prices = ... (stored in memory)
#      discount_percent = ... (stored in memory)
#      tax_rate = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_total_with_discount(?, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1932:
#
# ===== SYNTAX =====
#   def get_common_prefix(str1, str2)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   min() — built-in function call
#   len() — built-in function call
#   break
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   get_common_prefix('hello', 'help'): gets the literal 'hello', gets the literal 'help', then runs the function body
#   get_common_prefix(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   print(get_common_prefix('world', 'python')): the inner get_common_prefix() executes FIRST, then print() outputs the result
#   get_common_prefix('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   ? == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_common_prefix(str1, str2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_common_prefix(str1, str2) — stored for later
#   5. Define def get_common_prefix(str1, str2) — stored for later
#   6. Assign to str1
#   7. Assign to str2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_common_prefix(...))
#       calls get_common_prefix with: str1='world', str2='python'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_common_prefix, get_common_prefix, get_common_prefix are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_common_prefix('world', 'python')) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1933:
#
# ===== SYNTAX =====
#   def get_common_suffix(str1, str2)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   min() — built-in function call
#   len() — built-in function call
#   break
#   for
#   if/elif/else
#   in
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   get_common_suffix('running', 'walking'): gets the literal 'running', gets the literal 'walking', then runs the function body
#   get_common_suffix(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   print(get_common_suffix('world', 'python')): the inner get_common_suffix() executes FIRST, then print() outputs the result
#   get_common_suffix('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   min(...) + 1: first get min(...), then get 1, then compute min(...) + 1
#   ? + suffix: first get ?, then get suffix, then compute ? + suffix
#   ? == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_common_suffix(str1, str2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_common_suffix(str1, str2) — stored for later
#   5. Define def get_common_suffix(str1, str2) — stored for later
#   6. Assign to str1
#   7. Assign to str2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_common_suffix(...))
#       calls get_common_suffix with: str1='world', str2='python'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_common_suffix, get_common_suffix, get_common_suffix are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_common_suffix('world', 'python')) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1934:
#
# ===== SYNTAX =====
#   def check_all_uppercase_words(text)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   check_all_uppercase_words('Hello World Python'): gets the literal 'Hello World Python', then runs the function body
#   check_all_uppercase_words(text): looks up variable 'text', then runs the function body
#   print(check_all_uppercase_words('world')): the inner check_all_uppercase_words() executes FIRST, then print() outputs the result
#   check_all_uppercase_words('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_all_uppercase_words(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_all_uppercase_words(text) — stored for later
#   5. Define def check_all_uppercase_words(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_all_uppercase_words(...))
#       calls check_all_uppercase_words with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_all_uppercase_words, check_all_uppercase_words, check_all_uppercase_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_all_uppercase_words('world')) → function runs first, then print shows the result

"""

"""
Problem 1935:
#
# ===== SYNTAX =====
#   def get_strings_with_length_range(strings, min_length, max_length)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   comprehension
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   get_strings_with_length_range(?, 3, 4): resolves the value, gets the literal 3, gets the literal 4, then runs the function body
#   get_strings_with_length_range(strings, min_length, max_length): looks up variable 'strings', looks up variable 'min_length', looks up variable 'max_length', then runs the function body
#   print(get_strings_with_length_range(?, 25, 25)): the inner get_strings_with_length_range() executes FIRST, then print() outputs the result
#   get_strings_with_length_range(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_with_length_range(strings, min_length, max_length) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_with_length_range(strings, min_length, max_length) — stored for later
#   5. Define def get_strings_with_length_range(strings, min_length, max_length) — stored for later
#   6. Assign to strings
#   7. Assign to min_length
#   8. Assign to max_length
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_strings_with_length_range(...))
#       calls get_strings_with_length_range with: strings=[4, 1, 8, 6, 3], min_length=25, max_length=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_with_length_range, get_strings_with_length_range, get_strings_with_length_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      min_length = ... (stored in memory)
#      max_length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_with_length_range(?, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1936:
#
# ===== SYNTAX =====
#   def calculate_median(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   if/elif/else
#   operators: + - / // % ==
#
# ===== EVALUATION ORDER =====
#   calculate_median(?): resolves the value, then runs the function body
#   calculate_median(numbers): looks up variable 'numbers', then runs the function body
#   print(calculate_median(?)): the inner calculate_median() executes FIRST, then print() outputs the result
#   ? / 2: first get ?, then get 2, then compute ? / 2
#   ? / 2: first get ?, then get 2, then compute ? / 2
#   calculate_median(?): resolves the value, then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#   ? / 2: first get ?, then get 2, then compute ? / 2
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   n // 2: first get n, then get 2, then compute n // 2
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   n // 2: first get n, then get 2, then compute n // 2
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   n // 2: first get n, then get 2, then compute n // 2
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   n // 2: first get n, then get 2, then compute n // 2
#   n // 2: first get n, then get 2, then compute n // 2
#   n // 2: first get n, then get 2, then compute n // 2
#   n // 2: first get n, then get 2, then compute n // 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_median(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_median(numbers) — stored for later
#   5. Define def calculate_median(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_median(...))
#       calls calculate_median with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_median, calculate_median, calculate_median are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_median(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1937:
#
# ===== SYNTAX =====
#   def get_mode(my_list)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   get_mode(?): resolves the value, then runs the function body
#   get_mode(my_list): looks up variable 'my_list', then runs the function body
#   print(get_mode(?)): the inner get_mode() executes FIRST, then print() outputs the result
#   get_mode(?): resolves the value, then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def get_mode(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_mode(my_list) — stored for later
#   5. Define def get_mode(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_mode(...))
#       calls get_mode with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_mode, get_mode, get_mode are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_mode(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1939:
#
# ===== SYNTAX =====
#   def get_numbers_divisible_by_all(numbers, divisors)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   get_numbers_divisible_by_all(?, ?): resolves the value, resolves the value, then runs the function body
#   get_numbers_divisible_by_all(numbers, divisors): looks up variable 'numbers', looks up variable 'divisors', then runs the function body
#   print(get_numbers_divisible_by_all(?, ?)): the inner get_numbers_divisible_by_all() executes FIRST, then print() outputs the result
#   get_numbers_divisible_by_all(?, ?): resolves the value, resolves the value, then runs the function body
#   n % d: first get n, then get d, then compute n % d
#   n % d: first get n, then get d, then compute n % d
#   n % d: first get n, then get d, then compute n % d
#
# ===== EXECUTION ORDER =====
#   1. Define def get_numbers_divisible_by_all(numbers, divisors) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_numbers_divisible_by_all(numbers, divisors) — stored for later
#   5. Define def get_numbers_divisible_by_all(numbers, divisors) — stored for later
#   6. Assign to numbers
#   7. Assign to divisors
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_numbers_divisible_by_all(...))
#       calls get_numbers_divisible_by_all with: numbers=[4, 1, 8, 6, 3], divisors=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_numbers_divisible_by_all, get_numbers_divisible_by_all, get_numbers_divisible_by_all are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      divisors = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_numbers_divisible_by_all(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1941:
#
# ===== SYNTAX =====
#   def get_words_with_vowel_count(text, n)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   get_words_with_vowel_count('hello world python programming', 2): gets the literal 'hello world python programming', gets the literal 2, then runs the function body
#   get_words_with_vowel_count(text, n): looks up variable 'text', looks up variable 'n', then runs the function body
#   print(get_words_with_vowel_count('world', 15)): the inner get_words_with_vowel_count() executes FIRST, then print() outputs the result
#   get_words_with_vowel_count('world', 15): gets the literal 'world', gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_with_vowel_count(text, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_with_vowel_count(text, n) — stored for later
#   5. Define def get_words_with_vowel_count(text, n) — stored for later
#   6. Assign to text
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_words_with_vowel_count(...))
#       calls get_words_with_vowel_count with: text='world', n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_with_vowel_count, get_words_with_vowel_count, get_words_with_vowel_count are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_with_vowel_count('world', 15)) → function runs first, then print shows the result

"""

"""
Problem 1942:
#
# ===== SYNTAX =====
#   def format_table_row(value1, value2, value3)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_table_row('Alice', 30, 'Engineer'): gets the literal 'Alice', gets the literal 30, gets the literal 'Engineer', then runs the function body
#   format_table_row(value1, value2, value3): looks up variable 'value1', looks up variable 'value2', looks up variable 'value3', then runs the function body
#   print(format_table_row(25, 25, 25)): the inner format_table_row() executes FIRST, then print() outputs the result
#   '%s | %s | %s' % (value1, value2, value3): first get '%s | %s | %s', then get (value1, value2, value3), then compute '%s | %s | %s' % (value1, value2, value3)
#   format_table_row(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_table_row(value1, value2, value3) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_table_row(value1, value2, value3) — stored for later
#   5. Define def format_table_row(value1, value2, value3) — stored for later
#   6. Assign to value1
#   7. Assign to value2
#   8. Assign to value3
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_table_row(...))
#       calls format_table_row with: value1=25, value2=25, value3=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_table_row, format_table_row, format_table_row are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      value1 = ... (stored in memory)
#      value2 = ... (stored in memory)
#      value3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_table_row(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1943:
#
# ===== SYNTAX =====
#   def get_elements_not_in_both(list1, list2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   get_elements_not_in_both(?, ?): resolves the value, resolves the value, then runs the function body
#   print(sorted(result)): inner expression resolves first, then print() outputs
#   get_elements_not_in_both(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(get_elements_not_in_both(?, ?)): the inner get_elements_not_in_both() executes FIRST, then print() outputs the result
#   get_elements_not_in_both(?, ?): resolves the value, resolves the value, then runs the function body
#   item not in list2: is this True or False? The answer picks which branch
#   item not in list1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_not_in_both(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(sorted(...))
#   4. Define def get_elements_not_in_both(list1, list2) — stored for later
#   5. Define def get_elements_not_in_both(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_elements_not_in_both(...))
#       calls get_elements_not_in_both with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_not_in_both, get_elements_not_in_both, get_elements_not_in_both are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(sorted(...))
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_not_in_both(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from list1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1944:
#
# ===== SYNTAX =====
#   def calculate_weighted_average(values, weights)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   zip() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   comprehension
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   calculate_weighted_average(?, ?): resolves the value, resolves the value, then runs the function body
#   calculate_weighted_average(values, weights): looks up variable 'values', looks up variable 'weights', then runs the function body
#   print(calculate_weighted_average(?, ?)): the inner calculate_weighted_average() executes FIRST, then print() outputs the result
#   weighted_sum / total_weight: first get weighted_sum, then get total_weight, then compute weighted_sum / total_weight
#   weighted_sum * ?: first get weighted_sum, then get ?, then compute weighted_sum * ?
#   calculate_weighted_average(?, ?): resolves the value, resolves the value, then runs the function body
#   1 / total_weight: first get 1, then get total_weight, then compute 1 / total_weight
#   v * w: first get v, then get w, then compute v * w
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_weighted_average(values, weights) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_weighted_average(values, weights) — stored for later
#   5. Define def calculate_weighted_average(values, weights) — stored for later
#   6. Assign to values
#   7. Assign to weights
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_weighted_average(...))
#       calls calculate_weighted_average with: values=[4, 1, 8, 6, 3], weights=[4, 1, 8, 6, 3]
#         10.1 Import Fraction from fractions
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_weighted_average, calculate_weighted_average, calculate_weighted_average are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      values = ... (stored in memory)
#      weights = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_weighted_average(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1945:
#
# ===== SYNTAX =====
#   def get_strings_sorted_by_length(strings)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_strings_sorted_by_length(?): resolves the value, then runs the function body
#   get_strings_sorted_by_length(strings): looks up variable 'strings', then runs the function body
#   print(get_strings_sorted_by_length(?)): the inner get_strings_sorted_by_length() executes FIRST, then print() outputs the result
#   get_strings_sorted_by_length(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_sorted_by_length(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_sorted_by_length(strings) — stored for later
#   5. Define def get_strings_sorted_by_length(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_strings_sorted_by_length(...))
#       calls get_strings_sorted_by_length with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_sorted_by_length, get_strings_sorted_by_length, get_strings_sorted_by_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_sorted_by_length(?)) → function runs first, then print shows the result

"""

"""
Problem 1946:
#
# ===== SYNTAX =====
#   def format_percentage_list(numbers)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   format_percentage_list(?): resolves the value, then runs the function body
#   format_percentage_list(numbers): looks up variable 'numbers', then runs the function body
#   print(format_percentage_list(?)): the inner format_percentage_list() executes FIRST, then print() outputs the result
#   format_percentage_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_percentage_list(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_percentage_list(numbers) — stored for later
#   5. Define def format_percentage_list(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(format_percentage_list(...))
#       calls format_percentage_list with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_percentage_list, format_percentage_list, format_percentage_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_percentage_list(?)) → function runs first, then print shows the result

"""

"""
Problem 1947:
#
# ===== SYNTAX =====
#   def get_pairs_summing_to(numbers, target)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   get_pairs_summing_to(?, 7): resolves the value, gets the literal 7, then runs the function body
#   get_pairs_summing_to(numbers, target): looks up variable 'numbers', looks up variable 'target', then runs the function body
#   print(get_pairs_summing_to(?, 'goal')): the inner get_pairs_summing_to() executes FIRST, then print() outputs the result
#   get_pairs_summing_to(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   i + 1: first get i, then get 1, then compute i + 1
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_pairs_summing_to(numbers, target) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_pairs_summing_to(numbers, target) — stored for later
#   5. Define def get_pairs_summing_to(numbers, target) — stored for later
#   6. Assign to numbers
#   7. Assign to target
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_pairs_summing_to(...))
#       calls get_pairs_summing_to with: numbers=[4, 1, 8, 6, 3], target='goal'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_pairs_summing_to, get_pairs_summing_to, get_pairs_summing_to are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_pairs_summing_to(?, 'goal')) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1948:
#
# ===== SYNTAX =====
#   def count_character_types(text)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   count_character_types() — built-in function call
#   comprehension
#   operators: and in
#
# ===== EVALUATION ORDER =====
#   count_character_types('Hello World 123'): gets the literal 'Hello World 123', then runs the function body
#   count_character_types(text): looks up variable 'text', then runs the function body
#   print(count_character_types('world')): the inner count_character_types() executes FIRST, then print() outputs the result
#   count_character_types('Hello World 123'): gets the literal 'Hello World 123', then runs the function body
#   count_character_types('Hello World 123'): gets the literal 'Hello World 123', then runs the function body
#   count_character_types('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_character_types(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_character_types(...))
#       calls count_character_types with: text='world'
#         9.1 Assign to vowels
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_character_types, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_character_types('world')) → function runs first, then print shows the result

"""

"""
Problem 1949:
#
# ===== SYNTAX =====
#   def get_words_by_vowel_count(text)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   get_words_by_vowel_count('hello world python'): gets the literal 'hello world python', then runs the function body
#   get_words_by_vowel_count(text): looks up variable 'text', then runs the function body
#   print(get_words_by_vowel_count('world')): the inner get_words_by_vowel_count() executes FIRST, then print() outputs the result
#   get_words_by_vowel_count('world'): gets the literal 'world', then runs the function body
#   count not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_by_vowel_count(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_by_vowel_count(text) — stored for later
#   5. Define def get_words_by_vowel_count(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_words_by_vowel_count(...))
#       calls get_words_by_vowel_count with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_by_vowel_count, get_words_by_vowel_count, get_words_by_vowel_count are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_by_vowel_count('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1950:
#
# ===== SYNTAX =====
#   def format_name_with_title(first, last, title)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_name_with_title('John', 'Doe', 'Dr'): gets the literal 'John', gets the literal 'Doe', gets the literal 'Dr', then runs the function body
#   format_name_with_title(first, last, title): looks up variable 'first', looks up variable 'last', looks up variable 'title', then runs the function body
#   print(format_name_with_title(25, 25, 25)): the inner format_name_with_title() executes FIRST, then print() outputs the result
#   '%s. %s %s' % (title, first, last): first get '%s. %s %s', then get (title, first, last), then compute '%s. %s %s' % (title, first, last)
#   format_name_with_title(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_name_with_title(first, last, title) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_name_with_title(first, last, title) — stored for later
#   5. Define def format_name_with_title(first, last, title) — stored for later
#   6. Assign to first
#   7. Assign to last
#   8. Assign to title
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_name_with_title(...))
#       calls format_name_with_title with: first=25, last=25, title=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_name_with_title, format_name_with_title, format_name_with_title are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      first = ... (stored in memory)
#      last = ... (stored in memory)
#      title = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_name_with_title(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1951:
#
# ===== SYNTAX =====
#   def calculate_area_circle_sector(radius, angle_degrees)
#   return — exits the function and sends back a value
#   operators: * / **
#
# ===== EVALUATION ORDER =====
#   calculate_area_circle_sector(5, 90): gets the literal 5, gets the literal 90, then runs the function body
#   print(round(result, 2)): inner expression resolves first, then print() outputs
#   calculate_area_circle_sector(radius, angle_degrees): looks up variable 'radius', looks up variable 'angle_degrees', then runs the function body
#   print(calculate_area_circle_sector(14, 25)): the inner calculate_area_circle_sector() executes FIRST, then print() outputs the result
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   calculate_area_circle_sector(14, 25): gets the literal 14, gets the literal 25, then runs the function body
#   ? * math.pi: first get ?, then get math.pi, then compute ? * math.pi
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   ? * math.pi: first get ?, then get math.pi, then compute ? * math.pi
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   ? * math.pi: first get ?, then get math.pi, then compute ? * math.pi
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   angle_degrees / 360: first get angle_degrees, then get 360, then compute angle_degrees / 360
#   angle_degrees / 360: first get angle_degrees, then get 360, then compute angle_degrees / 360
#   angle_degrees / 360: first get angle_degrees, then get 360, then compute angle_degrees / 360
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_area_circle_sector(radius, angle_degrees) — stored for later
#   3. Assign to result
#   4. Run print(round(...))
#   5. Define def calculate_area_circle_sector(radius, angle_degrees) — stored for later
#   6. Define def calculate_area_circle_sector(radius, angle_degrees) — stored for later
#   7. Assign to radius
#   8. Assign to angle_degrees
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_area_circle_sector(...))
#       calls calculate_area_circle_sector with: radius=14, angle_degrees=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_area_circle_sector, calculate_area_circle_sector, calculate_area_circle_sector are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(round(...))
#      radius = ... (stored in memory)
#      angle_degrees = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_area_circle_sector(14, 25)) → function runs first, then print shows the result

"""

"""
Problem 1952:
#
# ===== SYNTAX =====
#   def get_elements_at_multiple_indices(my_list, indices)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   comprehension
#   operators: <
#
# ===== EVALUATION ORDER =====
#   get_elements_at_multiple_indices(?, ?): resolves the value, resolves the value, then runs the function body
#   get_elements_at_multiple_indices(my_list, indices): looks up variable 'my_list', looks up variable 'indices', then runs the function body
#   print(get_elements_at_multiple_indices(?, ?)): the inner get_elements_at_multiple_indices() executes FIRST, then print() outputs the result
#   get_elements_at_multiple_indices(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_at_multiple_indices(my_list, indices) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_at_multiple_indices(my_list, indices) — stored for later
#   5. Define def get_elements_at_multiple_indices(my_list, indices) — stored for later
#   6. Assign to my_list
#   7. Assign to indices
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_elements_at_multiple_indices(...))
#       calls get_elements_at_multiple_indices with: my_list=[4, 1, 8, 6, 3], indices=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_at_multiple_indices, get_elements_at_multiple_indices, get_elements_at_multiple_indices are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      indices = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_at_multiple_indices(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1953:
#
# ===== SYNTAX =====
#   def check_string_properties(text)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   check_string_properties() — built-in function call
#   comprehension
#   operators: and
#
# ===== EVALUATION ORDER =====
#   check_string_properties('Hello123!'): gets the literal 'Hello123!', then runs the function body
#   check_string_properties(text): looks up variable 'text', then runs the function body
#   print(check_string_properties('world')): the inner check_string_properties() executes FIRST, then print() outputs the result
#   check_string_properties('Hello123!'): gets the literal 'Hello123!', then runs the function body
#   check_string_properties('Hello123!'): gets the literal 'Hello123!', then runs the function body
#   check_string_properties('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_properties(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_string_properties(...))
#       calls check_string_properties with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_properties, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_string_properties('world')) → function runs first, then print shows the result

"""

"""
Problem 1954:
#
# ===== SYNTAX =====
#   def get_numbers_summing_to_range(numbers, min_sum, max_sum)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   get_numbers_summing_to_range(?, 5, 7): resolves the value, gets the literal 5, gets the literal 7, then runs the function body
#   get_numbers_summing_to_range(numbers, min_sum, max_sum): looks up variable 'numbers', looks up variable 'min_sum', looks up variable 'max_sum', then runs the function body
#   print(get_numbers_summing_to_range(?, 25, 25)): the inner get_numbers_summing_to_range() executes FIRST, then print() outputs the result
#   get_numbers_summing_to_range(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#   i + 1: first get i, then get 1, then compute i + 1
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   min_sum <= total: is this True or False? The answer picks which branch
#   min_sum <= max_sum: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_numbers_summing_to_range(numbers, min_sum, max_sum) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_numbers_summing_to_range(numbers, min_sum, max_sum) — stored for later
#   5. Define def get_numbers_summing_to_range(numbers, min_sum, max_sum) — stored for later
#   6. Assign to numbers
#   7. Assign to min_sum
#   8. Assign to max_sum
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_numbers_summing_to_range(...))
#       calls get_numbers_summing_to_range with: numbers=[4, 1, 8, 6, 3], min_sum=25, max_sum=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_numbers_summing_to_range, get_numbers_summing_to_range, get_numbers_summing_to_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      min_sum = ... (stored in memory)
#      max_sum = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_numbers_summing_to_range(?, 25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1955:
#
# ===== SYNTAX =====
#   def format_phone_with_country(country_code, area_code, number)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_phone_with_country('1', '555', '1234567'): gets the literal '1', gets the literal '555', gets the literal '1234567', then runs the function body
#   format_phone_with_country(country_code, area_code, number): looks up variable 'country_code', looks up variable 'area_code', looks up variable 'number', then runs the function body
#   print(format_phone_with_country(25, 25, 25)): the inner format_phone_with_country() executes FIRST, then print() outputs the result
#   '+%s (%s) %s' % (country_code, area_code, number): first get '+%s (%s) %s', then get (country_code, area_code, number), then compute '+%s (%s) %s' % (country_code, area_code, number)
#   format_phone_with_country(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_phone_with_country(country_code, area_code, number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_phone_with_country(country_code, area_code, number) — stored for later
#   5. Define def format_phone_with_country(country_code, area_code, number) — stored for later
#   6. Assign to country_code
#   7. Assign to area_code
#   8. Assign to number
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_phone_with_country(...))
#       calls format_phone_with_country with: country_code=25, area_code=25, number=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_phone_with_country, format_phone_with_country, format_phone_with_country are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      country_code = ... (stored in memory)
#      area_code = ... (stored in memory)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_phone_with_country(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1956:
#
# ===== SYNTAX =====
#   def get_consecutive_elements(my_list, n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   get_consecutive_elements(?, 3): resolves the value, gets the literal 3, then runs the function body
#   get_consecutive_elements(my_list, n): looks up variable 'my_list', looks up variable 'n', then runs the function body
#   print(get_consecutive_elements(?, 15)): the inner get_consecutive_elements() executes FIRST, then print() outputs the result
#   get_consecutive_elements(?, 15): resolves the value, gets the literal 15, then runs the function body
#   i + n: first get i, then get n, then compute i + n
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   i + n: first get i, then get n, then compute i + n
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   i + n: first get i, then get n, then compute i + n
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   len(...) - n: first get len(...), then get n, then compute len(...) - n
#   len(...) - n: first get len(...), then get n, then compute len(...) - n
#   len(...) - n: first get len(...), then get n, then compute len(...) - n
#
# ===== EXECUTION ORDER =====
#   1. Define def get_consecutive_elements(my_list, n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_consecutive_elements(my_list, n) — stored for later
#   5. Define def get_consecutive_elements(my_list, n) — stored for later
#   6. Assign to my_list
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_consecutive_elements(...))
#       calls get_consecutive_elements with: my_list=[4, 1, 8, 6, 3], n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_consecutive_elements, get_consecutive_elements, get_consecutive_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_consecutive_elements(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 1957:
#
# ===== SYNTAX =====
#   def calculate_gcd(a, b)
#   return — exits the function and sends back a value
#   while
#   operators: %
#
# ===== EVALUATION ORDER =====
#   calculate_gcd(48, 18): gets the literal 48, gets the literal 18, then runs the function body
#   calculate_gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_gcd(20, 30)): the inner calculate_gcd() executes FIRST, then print() outputs the result
#   calculate_gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a % b: first get a, then get b, then compute a % b
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_gcd(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_gcd(a, b) — stored for later
#   5. Define def calculate_gcd(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_gcd(...))
#       calls calculate_gcd with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_gcd, calculate_gcd, calculate_gcd are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_gcd(20, 30)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1958:
#
# ===== SYNTAX =====
#   def calculate_lcm(a, b)
#   return — exits the function and sends back a value
#   abs() — built-in function call
#   gcd() — built-in function call
#   while
#   operators: * // %
#
# ===== EVALUATION ORDER =====
#   calculate_lcm(12, 18): gets the literal 12, gets the literal 18, then runs the function body
#   calculate_lcm(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_lcm(20, 30)): the inner calculate_lcm() executes FIRST, then print() outputs the result
#   abs(...) // gcd(...): first get abs(...), then get gcd(...), then compute abs(...) // gcd(...)
#   calculate_lcm(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#   x % y: first get x, then get y, then compute x % y
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_lcm(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_lcm(a, b) — stored for later
#   5. Define def calculate_lcm(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_lcm(...))
#       calls calculate_lcm with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_lcm, calculate_lcm, calculate_lcm are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_lcm(20, 30)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1959:
#
# ===== SYNTAX =====
#   def get_frequency_dict(my_list)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   sorted() — built-in function call
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   get_frequency_dict(?): resolves the value, then runs the function body
#   get_frequency_dict(my_list): looks up variable 'my_list', then runs the function body
#   print(get_frequency_dict(?)): the inner get_frequency_dict() executes FIRST, then print() outputs the result
#   get_frequency_dict(?): resolves the value, then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def get_frequency_dict(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_frequency_dict(my_list) — stored for later
#   5. Define def get_frequency_dict(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_frequency_dict(...))
#       calls get_frequency_dict with: my_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_frequency_dict, get_frequency_dict, get_frequency_dict are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_frequency_dict(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round

"""

"""
Problem 1960:
#
# ===== SYNTAX =====
#   def format_number_with_commas(number)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_number_with_commas(1234567): gets the literal 1234567, then runs the function body
#   format_number_with_commas(number): looks up variable 'number', then runs the function body
#   print(format_number_with_commas(25)): the inner format_number_with_commas() executes FIRST, then print() outputs the result
#   format_number_with_commas(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_number_with_commas(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_number_with_commas(number) — stored for later
#   5. Define def format_number_with_commas(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(format_number_with_commas(...))
#       calls format_number_with_commas with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_number_with_commas, format_number_with_commas, format_number_with_commas are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_number_with_commas(25)) → function runs first, then print shows the result

"""

"""
Problem 1961:
#
# ===== SYNTAX =====
#   def get_words_with_same_length(strings)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   get_words_with_same_length(?): resolves the value, then runs the function body
#   get_words_with_same_length(strings): looks up variable 'strings', then runs the function body
#   print(get_words_with_same_length(?)): the inner get_words_with_same_length() executes FIRST, then print() outputs the result
#   get_words_with_same_length(?): resolves the value, then runs the function body
#   length not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_with_same_length(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_with_same_length(strings) — stored for later
#   5. Define def get_words_with_same_length(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_words_with_same_length(...))
#       calls get_words_with_same_length with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_with_same_length, get_words_with_same_length, get_words_with_same_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_with_same_length(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1962:
#
# ===== SYNTAX =====
#   def check_anagram_pair(str1, str2)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(check_anagram_pair('listen', 'silent')): the inner check_anagram_pair() executes FIRST, then print() outputs the result
#   print(check_anagram_pair('hello', 'world')): the inner check_anagram_pair() executes FIRST, then print() outputs the result
#   check_anagram_pair(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   print(check_anagram_pair('world', 'python')): the inner check_anagram_pair() executes FIRST, then print() outputs the result
#   check_anagram_pair('listen', 'silent'): gets the literal 'listen', gets the literal 'silent', then runs the function body
#   check_anagram_pair('hello', 'world'): gets the literal 'hello', gets the literal 'world', then runs the function body
#   check_anagram_pair('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_anagram_pair(str1, str2) — stored for later
#   2. Run print(check_anagram_pair(...), check_anagram_pair(...))
#       calls check_anagram_pair with: str1='listen', str2='silent'
#         2.1 Return from function
#   3. Define def check_anagram_pair(str1, str2) — stored for later
#   4. Define def check_anagram_pair(str1, str2) — stored for later
#   5. Assign to str1
#   6. Assign to str2
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_anagram_pair(...))
#       calls check_anagram_pair with: str1='world', str2='python'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_anagram_pair, check_anagram_pair, check_anagram_pair are just stored
#   Execution starts at the non-def lines below:
#      print(check_anagram_pair('listen', 'silent')) → function runs first, then print shows the result
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_anagram_pair('world', 'python')) → function runs first, then print shows the result

"""

"""
Problem 1963:
#
# ===== SYNTAX =====
#   def get_sublist_sum(my_list, start, end)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_sublist_sum(?, 1, 4): resolves the value, gets the literal 1, gets the literal 4, then runs the function body
#   get_sublist_sum(my_list, start, end): looks up variable 'my_list', looks up variable 'start', looks up variable 'end', then runs the function body
#   print(get_sublist_sum(?, 3, 20)): the inner get_sublist_sum() executes FIRST, then print() outputs the result
#   get_sublist_sum(?, 3, 20): resolves the value, gets the literal 3, gets the literal 20, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_sublist_sum(my_list, start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_sublist_sum(my_list, start, end) — stored for later
#   5. Define def get_sublist_sum(my_list, start, end) — stored for later
#   6. Assign to my_list
#   7. Assign to start
#   8. Assign to end
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_sublist_sum(...))
#       calls get_sublist_sum with: my_list=[4, 1, 8, 6, 3], start=3, end=20
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_sublist_sum, get_sublist_sum, get_sublist_sum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_sublist_sum(?, 3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1964:
#
# ===== SYNTAX =====
#   def format_time_12hour(hours_24)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: - == <
#
# ===== EVALUATION ORDER =====
#   print(format_time_12hour(0)): the inner format_time_12hour() executes FIRST, then print() outputs the result
#   print(format_time_12hour(14)): the inner format_time_12hour() executes FIRST, then print() outputs the result
#   print(format_time_12hour(23)): the inner format_time_12hour() executes FIRST, then print() outputs the result
#   format_time_12hour(hours_24): looks up variable 'hours_24', then runs the function body
#   print(format_time_12hour(25)): the inner format_time_12hour() executes FIRST, then print() outputs the result
#   format_time_12hour(0): gets the literal 0, then runs the function body
#   format_time_12hour(14): gets the literal 14, then runs the function body
#   format_time_12hour(23): gets the literal 23, then runs the function body
#   format_time_12hour(25): gets the literal 25, then runs the function body
#   hours_24 - 12: first get hours_24, then get 12, then compute hours_24 - 12
#   hours_24 == 0: is this True or False? The answer picks which branch
#   hours_24 < 12: is this True or False? The answer picks which branch
#   hours_24 == 12: is this True or False? The answer picks which branch
#   hours_24 < 12: is this True or False? The answer picks which branch
#   hours_24 == 12: is this True or False? The answer picks which branch
#   hours_24 == 12: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def format_time_12hour(hours_24) — stored for later
#   2. Run print(format_time_12hour(...), format_time_12hour(...), format_time_12hour(...))
#       calls format_time_12hour with: hours_24=0
#         2.1 Return from function
#   3. Define def format_time_12hour(hours_24) — stored for later
#   4. Define def format_time_12hour(hours_24) — stored for later
#   5. Assign to hours_24
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(format_time_12hour(...))
#       calls format_time_12hour with: hours_24=25
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_time_12hour, format_time_12hour, format_time_12hour are just stored
#   Execution starts at the non-def lines below:
#      print(format_time_12hour(0)) → function runs first, then print shows the result
#      hours_24 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_time_12hour(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1965:
#
# ===== SYNTAX =====
#   def get_elements_between_values(my_list, min_val, max_val)
#   return — exits the function and sends back a value
#   comprehension
#   operators: <
#
# ===== EVALUATION ORDER =====
#   get_elements_between_values(?, 15, 45): resolves the value, gets the literal 15, gets the literal 45, then runs the function body
#   get_elements_between_values(my_list, min_val, max_val): looks up variable 'my_list', looks up variable 'min_val', looks up variable 'max_val', then runs the function body
#   print(get_elements_between_values(?, 25, 25)): the inner get_elements_between_values() executes FIRST, then print() outputs the result
#   get_elements_between_values(?, 25, 25): resolves the value, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_between_values(my_list, min_val, max_val) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_between_values(my_list, min_val, max_val) — stored for later
#   5. Define def get_elements_between_values(my_list, min_val, max_val) — stored for later
#   6. Assign to my_list
#   7. Assign to min_val
#   8. Assign to max_val
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(get_elements_between_values(...))
#       calls get_elements_between_values with: my_list=[4, 1, 8, 6, 3], min_val=25, max_val=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_between_values, get_elements_between_values, get_elements_between_values are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      min_val = ... (stored in memory)
#      max_val = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_between_values(?, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1966:
#
# ===== SYNTAX =====
#   def count_substring_occurrences(text, substring)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + - ==
#
# ===== EVALUATION ORDER =====
#   count_substring_occurrences('ababab', 'aba'): gets the literal 'ababab', gets the literal 'aba', then runs the function body
#   count_substring_occurrences(text, substring): looks up variable 'text', looks up variable 'substring', then runs the function body
#   print(count_substring_occurrences('world', 'world')): the inner count_substring_occurrences() executes FIRST, then print() outputs the result
#   count_substring_occurrences('world', 'world'): gets the literal 'world', gets the literal 'world', then runs the function body
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   len(...) - len(...): first get len(...), then get len(...), then compute len(...) - len(...)
#   i + len(...): first get i, then get len(...), then compute i + len(...)
#   ? == substring: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_substring_occurrences(text, substring) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_substring_occurrences(text, substring) — stored for later
#   5. Define def count_substring_occurrences(text, substring) — stored for later
#   6. Assign to text
#   7. Assign to substring
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_substring_occurrences(...))
#       calls count_substring_occurrences with: text='world', substring='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_substring_occurrences, count_substring_occurrences, count_substring_occurrences are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      substring = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_substring_occurrences('world', 'world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1967:
#
# ===== SYNTAX =====
#   def format_list_as_string(my_list, separator)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   format_list_as_string(?, ', '): resolves the value, gets the literal ', ', then runs the function body
#   format_list_as_string(my_list, separator): looks up variable 'my_list', looks up variable 'separator', then runs the function body
#   print(format_list_as_string(?, 'world')): the inner format_list_as_string() executes FIRST, then print() outputs the result
#   format_list_as_string(?, 'world'): resolves the value, gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_list_as_string(my_list, separator) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_list_as_string(my_list, separator) — stored for later
#   5. Define def format_list_as_string(my_list, separator) — stored for later
#   6. Assign to my_list
#   7. Assign to separator
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(format_list_as_string(...))
#       calls format_list_as_string with: my_list=[4, 1, 8, 6, 3], separator='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_list_as_string, format_list_as_string, format_list_as_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      separator = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_list_as_string(?, 'world')) → function runs first, then print shows the result

"""

"""
Problem 1968:
#
# ===== SYNTAX =====
#   def get_unique_combinations(list1, list2)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   get_unique_combinations(?, ?): resolves the value, resolves the value, then runs the function body
#   get_unique_combinations(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(get_unique_combinations(?, ?)): the inner get_unique_combinations() executes FIRST, then print() outputs the result
#   get_unique_combinations(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_unique_combinations(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_unique_combinations(list1, list2) — stored for later
#   5. Define def get_unique_combinations(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_unique_combinations(...))
#       calls get_unique_combinations with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_unique_combinations, get_unique_combinations, get_unique_combinations are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_unique_combinations(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1969:
#
# ===== SYNTAX =====
#   def calculate_quadratic_roots(a, b, c)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: + - * / ** <
#
# ===== EVALUATION ORDER =====
#   calculate_quadratic_roots(1, ?, 6): gets the literal 1, resolves the value, gets the literal 6, then runs the function body
#   calculate_quadratic_roots(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(calculate_quadratic_roots(20, 30, 10)): the inner calculate_quadratic_roots() executes FIRST, then print() outputs the result
#   ? - ?: first get ?, then get ?, then compute ? - ?
#   ? / ?: first get ?, then get ?, then compute ? / ?
#   ? / ?: first get ?, then get ?, then compute ? / ?
#   calculate_quadratic_roots(20, 30, 10): gets the literal 20, gets the literal 30, gets the literal 10, then runs the function body
#   b ** 2: first get b, then get 2, then compute b ** 2
#   ? * c: first get ?, then get c, then compute ? * c
#   -b + math.sqrt(...): first get -b, then get math.sqrt(...), then compute -b + math.sqrt(...)
#   2 * a: first get 2, then get a, then compute 2 * a
#   -b - math.sqrt(...): first get -b, then get math.sqrt(...), then compute -b - math.sqrt(...)
#   2 * a: first get 2, then get a, then compute 2 * a
#   4 * a: first get 4, then get a, then compute 4 * a
#   discriminant < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_quadratic_roots(a, b, c) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def calculate_quadratic_roots(a, b, c) — stored for later
#   6. Define def calculate_quadratic_roots(a, b, c) — stored for later
#   7. Assign to a
#   8. Assign to b
#   9. Assign to c
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(calculate_quadratic_roots(...))
#       calls calculate_quadratic_roots with: a=20, b=30, c=10
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_quadratic_roots, calculate_quadratic_roots, calculate_quadratic_roots are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_quadratic_roots(20, 30, 10)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1970:
#
# ===== SYNTAX =====
#   def get_strings_matching_pattern(strings, pattern)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   get_strings_matching_pattern(?, 'l'): resolves the value, gets the literal 'l', then runs the function body
#   get_strings_matching_pattern(strings, pattern): looks up variable 'strings', looks up variable 'pattern', then runs the function body
#   print(get_strings_matching_pattern(?, 25)): the inner get_strings_matching_pattern() executes FIRST, then print() outputs the result
#   get_strings_matching_pattern(?, 25): resolves the value, gets the literal 25, then runs the function body
#   s.count(...) >= 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_matching_pattern(strings, pattern) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_matching_pattern(strings, pattern) — stored for later
#   5. Define def get_strings_matching_pattern(strings, pattern) — stored for later
#   6. Assign to strings
#   7. Assign to pattern
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_strings_matching_pattern(...))
#       calls get_strings_matching_pattern with: strings=[4, 1, 8, 6, 3], pattern=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_matching_pattern, get_strings_matching_pattern, get_strings_matching_pattern are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      pattern = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_matching_pattern(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1971:
#
# ===== SYNTAX =====
#   def format_name_initials(first, middle, last)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_name_initials('John', 'Michael', 'Doe'): gets the literal 'John', gets the literal 'Michael', gets the literal 'Doe', then runs the function body
#   format_name_initials(first, middle, last): looks up variable 'first', looks up variable 'middle', looks up variable 'last', then runs the function body
#   print(format_name_initials(?, ?, ?)): the inner format_name_initials() executes FIRST, then print() outputs the result
#   format_name_initials(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_name_initials(first, middle, last) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_name_initials(first, middle, last) — stored for later
#   5. Define def format_name_initials(first, middle, last) — stored for later
#   6. Assign to first
#   7. Assign to middle
#   8. Assign to last
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(format_name_initials(...))
#       calls format_name_initials with: first=[4, 1, 8, 6, 3], middle=[4, 1, 8, 6, 3], last=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_name_initials, format_name_initials, format_name_initials are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      first = ... (stored in memory)
#      middle = ... (stored in memory)
#      last = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_name_initials(?, ?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1972:
#
# ===== SYNTAX =====
#   def get_elements_with_condition(my_list, condition_func)
#   return — exits the function and sends back a value
#   condition_func() — built-in function call
#   comprehension
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   get_elements_with_condition(?, ?): resolves the value, resolves the value, then runs the function body
#   get_elements_with_condition(my_list, condition_func): looks up variable 'my_list', looks up variable 'condition_func', then runs the function body
#   print(get_elements_with_condition(?, 25)): the inner get_elements_with_condition() executes FIRST, then print() outputs the result
#   get_elements_with_condition(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_with_condition(my_list, condition_func) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_with_condition(my_list, condition_func) — stored for later
#   5. Define def get_elements_with_condition(my_list, condition_func) — stored for later
#   6. Assign to my_list
#   7. Assign to condition_func
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_elements_with_condition(...))
#       calls get_elements_with_condition with: my_list=[4, 1, 8, 6, 3], condition_func=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_with_condition, get_elements_with_condition, get_elements_with_condition are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      condition_func = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_with_condition(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1973:
#
# ===== SYNTAX =====
#   def calculate_total_area(shapes)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: * ** ==
#
# ===== EVALUATION ORDER =====
#   calculate_total_area(?): resolves the value, then runs the function body
#   print(round(result, 2)): inner expression resolves first, then print() outputs
#   calculate_total_area(shapes): looks up variable 'shapes', then runs the function body
#   print(calculate_total_area(25)): the inner calculate_total_area() executes FIRST, then print() outputs the result
#   calculate_total_area(25): gets the literal 25, then runs the function body
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   shape_type == 'rectangle': is this True or False? The answer picks which branch
#   shape_type == 'circle': is this True or False? The answer picks which branch
#   shape_type == 'circle': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_total_area(shapes) — stored for later
#   3. Assign to result
#   4. Run print(round(...))
#   5. Define def calculate_total_area(shapes) — stored for later
#   6. Define def calculate_total_area(shapes) — stored for later
#   7. Assign to shapes
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_total_area(...))
#       calls calculate_total_area with: shapes=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_total_area, calculate_total_area, calculate_total_area are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(round(...))
#      shapes = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_total_area(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from shapes each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1974:
#
# ===== SYNTAX =====
#   def get_words_with_repeated_chars(text)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   set() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   get_words_with_repeated_chars('hello world python programming'): gets the literal 'hello world python programming', then runs the function body
#   get_words_with_repeated_chars(text): looks up variable 'text', then runs the function body
#   print(get_words_with_repeated_chars('world')): the inner get_words_with_repeated_chars() executes FIRST, then print() outputs the result
#   get_words_with_repeated_chars('world'): gets the literal 'world', then runs the function body
#   len(...) != len(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_words_with_repeated_chars(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_words_with_repeated_chars(text) — stored for later
#   5. Define def get_words_with_repeated_chars(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_words_with_repeated_chars(...))
#       calls get_words_with_repeated_chars with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_words_with_repeated_chars, get_words_with_repeated_chars, get_words_with_repeated_chars are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_words_with_repeated_chars('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1975:
#
# ===== SYNTAX =====
#   def format_multi_value_string(name, scores)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   comprehension
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_multi_value_string('Alice', ?): gets the literal 'Alice', resolves the value, then runs the function body
#   format_multi_value_string(name, scores): looks up variable 'name', looks up variable 'scores', then runs the function body
#   print(format_multi_value_string('Bob', ?)): the inner format_multi_value_string() executes FIRST, then print() outputs the result
#   'Name: %s, Scores: %s' % (name, scores_str): first get 'Name: %s, Scores: %s', then get (name, scores_str), then compute 'Name: %s, Scores: %s' % (name, scores_str)
#   format_multi_value_string('Bob', ?): gets the literal 'Bob', resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_multi_value_string(name, scores) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_multi_value_string(name, scores) — stored for later
#   5. Define def format_multi_value_string(name, scores) — stored for later
#   6. Assign to name
#   7. Assign to scores
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(format_multi_value_string(...))
#       calls format_multi_value_string with: name='Bob', scores=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_multi_value_string, format_multi_value_string, format_multi_value_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      name = ... (stored in memory)
#      scores = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_multi_value_string('Bob', ?)) → function runs first, then print shows the result

"""

"""
Problem 1976:
#
# ===== SYNTAX =====
#   def get_symmetric_difference(list1, list2)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_symmetric_difference(?, ?): resolves the value, resolves the value, then runs the function body
#   print(sorted(result)): inner expression resolves first, then print() outputs
#   get_symmetric_difference(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(get_symmetric_difference(?, ?)): the inner get_symmetric_difference() executes FIRST, then print() outputs the result
#   get_symmetric_difference(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_symmetric_difference(list1, list2) — stored for later
#   2. Assign to result
#   3. Run print(sorted(...))
#   4. Define def get_symmetric_difference(list1, list2) — stored for later
#   5. Define def get_symmetric_difference(list1, list2) — stored for later
#   6. Assign to list1
#   7. Assign to list2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_symmetric_difference(...))
#       calls get_symmetric_difference with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_symmetric_difference, get_symmetric_difference, get_symmetric_difference are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(sorted(...))
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_symmetric_difference(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 1977:
#
# ===== SYNTAX =====
#   def count_words_by_first_letter(text)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   count_words_by_first_letter('apple apricot banana cherry'): gets the literal 'apple apricot banana cherry', then runs the function body
#   count_words_by_first_letter(text): looks up variable 'text', then runs the function body
#   print(count_words_by_first_letter('world')): the inner count_words_by_first_letter() executes FIRST, then print() outputs the result
#   count_words_by_first_letter('world'): gets the literal 'world', then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words_by_first_letter(text) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_words_by_first_letter(text) — stored for later
#   5. Define def count_words_by_first_letter(text) — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_words_by_first_letter(...))
#       calls count_words_by_first_letter with: text='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words_by_first_letter, count_words_by_first_letter, count_words_by_first_letter are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words_by_first_letter('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 1978:
#
# ===== SYNTAX =====
#   def format_decimal_places(number, decimal_places)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_decimal_places(3.14159, 2): gets the literal 3.14159, gets the literal 2, then runs the function body
#   format_decimal_places(number, decimal_places): looks up variable 'number', looks up variable 'decimal_places', then runs the function body
#   print(format_decimal_places(25, 25)): the inner format_decimal_places() executes FIRST, then print() outputs the result
#   format_decimal_places(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_decimal_places(number, decimal_places) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def format_decimal_places(number, decimal_places) — stored for later
#   5. Define def format_decimal_places(number, decimal_places) — stored for later
#   6. Assign to number
#   7. Assign to decimal_places
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(format_decimal_places(...))
#       calls format_decimal_places with: number=25, decimal_places=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_decimal_places, format_decimal_places, format_decimal_places are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      decimal_places = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_decimal_places(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1979:
#
# ===== SYNTAX =====
#   def get_elements_satisfying_all(my_list)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   cond() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   get_elements_satisfying_all(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   get_elements_satisfying_all(my_list, ?): looks up variable 'my_list', resolves the value, then runs the function body
#   print(get_elements_satisfying_all(?, 25)): the inner get_elements_satisfying_all() executes FIRST, then print() outputs the result
#   get_elements_satisfying_all(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_satisfying_all(my_list) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_elements_satisfying_all(my_list) — stored for later
#   5. Define def get_elements_satisfying_all(my_list) — stored for later
#   6. Assign to my_list
#   7. Assign to ?
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_elements_satisfying_all(...))
#       calls get_elements_satisfying_all with: my_list=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_satisfying_all, get_elements_satisfying_all, get_elements_satisfying_all are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      my_list = ... (stored in memory)
#      ? = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_elements_satisfying_all(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from my_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1980:
#
# ===== SYNTAX =====
#   def calculate_net_price(original_price, discount_percent, tax_rate)
#   return — exits the function and sends back a value
#   operators: + - * /
#
# ===== EVALUATION ORDER =====
#   calculate_net_price(100, 20, 10): gets the literal 100, gets the literal 20, gets the literal 10, then runs the function body
#   calculate_net_price(original_price, discount_percent, tax_rate): looks up variable 'original_price', looks up variable 'discount_percent', looks up variable 'tax_rate', then runs the function body
#   print(calculate_net_price(25, 25, 25)): the inner calculate_net_price() executes FIRST, then print() outputs the result
#   original_price * ?: first get original_price, then get ?, then compute original_price * ?
#   discounted * ?: first get discounted, then get ?, then compute discounted * ?
#   calculate_net_price(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#   1 - ?: first get 1, then get ?, then compute 1 - ?
#   1 + ?: first get 1, then get ?, then compute 1 + ?
#   discount_percent / 100: first get discount_percent, then get 100, then compute discount_percent / 100
#   tax_rate / 100: first get tax_rate, then get 100, then compute tax_rate / 100
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_net_price(original_price, discount_percent, tax_rate) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_net_price(original_price, discount_percent, tax_rate) — stored for later
#   5. Define def calculate_net_price(original_price, discount_percent, tax_rate) — stored for later
#   6. Assign to original_price
#   7. Assign to discount_percent
#   8. Assign to tax_rate
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_net_price(...))
#       calls calculate_net_price with: original_price=25, discount_percent=25, tax_rate=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_net_price, calculate_net_price, calculate_net_price are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      original_price = ... (stored in memory)
#      discount_percent = ... (stored in memory)
#      tax_rate = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_net_price(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 1981:
#
# ===== SYNTAX =====
#   def get_strings_with_all_vowels(strings)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   get_strings_with_all_vowels(?): resolves the value, then runs the function body
#   get_strings_with_all_vowels(strings): looks up variable 'strings', then runs the function body
#   print(get_strings_with_all_vowels(?)): the inner get_strings_with_all_vowels() executes FIRST, then print() outputs the result
#   get_strings_with_all_vowels(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_strings_with_all_vowels(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def get_strings_with_all_vowels(strings) — stored for later
#   5. Define def get_strings_with_all_vowels(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_strings_with_all_vowels(...))
#       calls get_strings_with_all_vowels with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_strings_with_all_vowels, get_strings_with_all_vowels, get_strings_with_all_vowels are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_strings_with_all_vowels(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1984:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import math
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1997:
#
# ===== SYNTAX =====
#   def get_elements_matching_multiple_patterns(strings)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   get_elements_matching_multiple_patterns(?, 'o', 'r'): resolves the value, gets the literal 'o', gets the literal 'r', then runs the function body
#   main(): — no arguments needed
#   pattern in s: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_elements_matching_multiple_patterns(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Assign to lst
#         5.2 Assign to result
#         5.3 For loop over lst
#         5.4 Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_elements_matching_multiple_patterns, main are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      main() → jumps into function, runs body, returns result → printed
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1998:
#
# ===== SYNTAX =====
#   def calculate_statistical_summary(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   min() — built-in function call
#   max() — built-in function call
#   sum() — built-in function call
#   operators: + - / // %
#
# ===== EVALUATION ORDER =====
#   calculate_statistical_summary(?): resolves the value, then runs the function body
#   sum(...) / n: first get sum(...), then get n, then compute sum(...) / n
#   n % 2: first get n, then get 2, then compute n % 2
#   ? / 2: first get ?, then get 2, then compute ? / 2
#   max_val - min_val: first get max_val, then get min_val, then compute max_val - min_val
#   n // 2: first get n, then get 2, then compute n // 2
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   n // 2: first get n, then get 2, then compute n // 2
#   n // 2: first get n, then get 2, then compute n // 2
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_statistical_summary(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_statistical_summary are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 2000:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import math
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

