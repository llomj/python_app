# Syntax documentation for level 1 (problems 1-2000)
# Auto-generated by gen_syntax_files.py

"""
Problem 1501:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: min, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. s = ... → assigns value to variable
#   2. print(min(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     s = ... → stored in memory
#     print(min(...))

"""

"""
Problem 1502:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Operators: ==
# Built-in functions: next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x == 4 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. nums = ... → assigns value to variable
#   2. print(next(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     nums = ... → stored in memory
#     print(next(...))

"""

"""
Problem 1503:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. scores = ... → assigns value to variable
#   2. print(max(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     scores = ... → stored in memory
#     print(max(...))

"""

"""
Problem 1504:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Operators: ==
# Built-in functions: next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ch == 'z' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. letters = ... → assigns value to variable
#   2. print(next(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     letters = ... → stored in memory
#     print(next(...))

"""

"""
Problem 1505:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. strings = ... → assigns value to variable
#   2. print(max(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     print(max(...))

"""

"""
Problem 1506:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Operators: ==
# Built-in functions: next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 'Zoe' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. users = ... → assigns value to variable
#   2. print(next(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     users = ... → stored in memory
#     print(next(...))

"""

"""
Problem 1507:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. print(min(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     print(min(...))

"""

"""
Problem 1508:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. scores = ... → assigns value to variable
#   2. print(max(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     scores = ... → stored in memory
#     print(max(...))

"""

"""
Problem 1509:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: iter, next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. print(next(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(next(...))

"""

"""
Problem 1510:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. words = ... → assigns value to variable
#   2. print(max(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     print(max(...))

"""

"""
Problem 1511:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. items = ... → assigns value to variable
#   2. result = ... → assigns value to variable
#   3. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1512:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. people = ... → assigns value to variable
#   2. min_age_dict = ... → assigns value to variable
#   3. print(min_age_dict)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. people = ... → assigns value to variable
#   7. min_age_dict = ... → assigns value to variable
#   8. print(min_age_dict)
#   9. people = ... → assigns value to variable
#   10. min_age_dict = ... → assigns value to variable
#   11. print(min_age_dict)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     min_age_dict = ... → stored in memory
#     print(min_age_dict)
#     people = ... → stored in memory
#     min_age_dict = ... → stored in memory
#     print(min_age_dict)
#     people = ... → stored in memory
#     min_age_dict = ... → stored in memory
#     print(min_age_dict)

"""

"""
Problem 1513:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. words = ... → assigns value to variable
#   2. max_word = ... → assigns value to variable
#   3. print(max_word)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. max_word = ... → assigns value to variable
#   8. print(max_word)
#   9. words = ... → assigns value to variable
#   10. max_word = ... → assigns value to variable
#   11. print(max_word)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     max_word = ... → stored in memory
#     print(max_word)
#     words = ... → stored in memory
#     max_word = ... → stored in memory
#     print(max_word)
#     words = ... → stored in memory
#     max_word = ... → stored in memory
#     print(max_word)

"""

"""
Problem 1514:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. name = ... → assigns value to variable
#   3. city = ... → assigns value to variable
#   4. print(name, city)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. name = ... → assigns value to variable
#   9. city = ... → assigns value to variable
#   10. print(name, city)
#   11. data = ... → assigns value to variable
#   12. name = ... → assigns value to variable
#   13. city = ... → assigns value to variable
#   14. print(name, city)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     name = ... → stored in memory
#     city = ... → stored in memory
#     print(name, city)
#     data = ... → stored in memory
#     name = ... → stored in memory
#     city = ... → stored in memory
#     print(name, city)
#     data = ... → stored in memory
#     name = ... → stored in memory
#     city = ... → stored in memory
#     print(name, city)

"""

"""
Problem 1515:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. items = ... → assigns value to variable
#   3. counter = ... → assigns value to variable
#   4. for item in items → loops over each item
#   5. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     counter = ... → stored in memory
#     for item in items → runs body once per item
#     print(dict(...))
#
#   Loop: takes one item from items each round, runs body, repeats until done

"""

"""
Problem 1516:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. data.setdefault(...).append('apple')
#   3. data.setdefault(...).append('banana')
#   4. print(data)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. data.setdefault(...).append('apple')
#   9. data.setdefault(...).append('banana')
#   10. print(data)
#   11. data = ... → assigns value to variable
#   12. data.setdefault(...).append('apple')
#   13. data.setdefault(...).append('banana')
#   14. print(data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     print(data)
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     print(data)
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     print(data)

"""

"""
Problem 1517:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: % ==
# Built-in functions: next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. first_even = ... → assigns value to variable
#   3. print(first_even)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. first_even = ... → assigns value to variable
#   8. print(first_even)
#   9. numbers = ... → assigns value to variable
#   10. first_even = ... → assigns value to variable
#   11. print(first_even)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     first_even = ... → stored in memory
#     print(first_even)
#     numbers = ... → stored in memory
#     first_even = ... → stored in memory
#     print(first_even)
#     numbers = ... → stored in memory
#     first_even = ... → stored in memory
#     print(first_even)

"""

"""
Problem 1518:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. items = ... → assigns value to variable
#   3. grouped = ... → assigns value to variable
#   4. for (category, item) in items → loops over each item
#      → 4.1 ?.append(item)
#   5. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     grouped = ... → stored in memory
#     for (category, item) in items → runs body once per item
#     print(dict(...))
#
#   Loop: takes one item from items each round, runs body, repeats until done

"""

"""
Problem 1519:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. user_info = ... → assigns value to variable
#   3. name = ... → assigns value to variable
#   4. print(name)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. user_info = ... → assigns value to variable
#   9. name = ... → assigns value to variable
#   10. print(name)
#   11. data = ... → assigns value to variable
#   12. user_info = ... → assigns value to variable
#   13. name = ... → assigns value to variable
#   14. print(name)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     user_info = ... → stored in memory
#     name = ... → stored in memory
#     print(name)
#     data = ... → stored in memory
#     user_info = ... → stored in memory
#     name = ... → stored in memory
#     print(name)
#     data = ... → stored in memory
#     user_info = ... → stored in memory
#     name = ... → stored in memory
#     print(name)

"""

"""
Problem 1520:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. words = ... → assigns value to variable
#   2. shortest = ... → assigns value to variable
#   3. print(shortest)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. shortest = ... → assigns value to variable
#   8. print(shortest)
#   9. words = ... → assigns value to variable
#   10. shortest = ... → assigns value to variable
#   11. print(shortest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     shortest = ... → stored in memory
#     print(shortest)
#     words = ... → stored in memory
#     shortest = ... → stored in memory
#     print(shortest)
#     words = ... → stored in memory
#     shortest = ... → stored in memory
#     print(shortest)

"""

"""
Problem 1521:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(data)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. data = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(data)
#   9. data = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(data)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(data)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(data)

"""

"""
Problem 1522:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. tuples = ... → assigns value to variable
#   2. max_tuple = ... → assigns value to variable
#   3. print(max_tuple)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. tuples = ... → assigns value to variable
#   7. max_tuple = ... → assigns value to variable
#   8. print(max_tuple)
#   9. tuples = ... → assigns value to variable
#   10. max_tuple = ... → assigns value to variable
#   11. print(max_tuple)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     tuples = ... → stored in memory
#     max_tuple = ... → stored in memory
#     print(max_tuple)
#     tuples = ... → stored in memory
#     max_tuple = ... → stored in memory
#     print(max_tuple)
#     tuples = ... → stored in memory
#     max_tuple = ... → stored in memory
#     print(max_tuple)

"""

"""
Problem 1523:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Built-in functions: defaultdict, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. items = ... → assigns value to variable
#   3. grouped = ... → assigns value to variable
#   4. for (key, value) in items → loops over each item
#      → 4.1 ?.add(value)
#   5. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     grouped = ... → stored in memory
#     for (key, value) in items → runs body once per item
#     print(?)
#
#   Loop: takes one item from items each round, runs body, repeats until done

"""

"""
Problem 1524:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: + |
# Built-in functions: set, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dict1.get(...) + dict2.get(...) → Python looks up dict1.get(...), then dict2.get(...), then applies +
#   set(...) | set(...) → Python looks up set(...), then set(...), then applies |
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. dict1 = ... → assigns value to variable
#   2. dict2 = ... → assigns value to variable
#   3. total = ... → assigns value to variable
#   4. print(total)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. total = ... → assigns value to variable
#   10. print(total)
#   11. dict1 = ... → assigns value to variable
#   12. dict2 = ... → assigns value to variable
#   13. total = ... → assigns value to variable
#   14. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     total = ... → stored in memory
#     print(total)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     total = ... → stored in memory
#     print(total)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     total = ... → stored in memory
#     print(total)

"""

"""
Problem 1525:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: % ==
# Built-in functions: next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. first_even = ... → assigns value to variable
#   3. print(first_even)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. first_even = ... → assigns value to variable
#   8. print(first_even)
#   9. numbers = ... → assigns value to variable
#   10. first_even = ... → assigns value to variable
#   11. print(first_even)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     first_even = ... → stored in memory
#     print(first_even)
#     numbers = ... → stored in memory
#     first_even = ... → stored in memory
#     print(first_even)
#     numbers = ... → stored in memory
#     first_even = ... → stored in memory
#     print(first_even)

"""

"""
Problem 1526:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. data.setdefault(...).append('apple')
#   3. data.setdefault(...).append('banana')
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. data.setdefault(...).append('apple')
#   9. data.setdefault(...).append('banana')
#   10. print(?)
#   11. data = ... → assigns value to variable
#   12. data.setdefault(...).append('apple')
#   13. data.setdefault(...).append('banana')
#   14. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     print(?)
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     print(?)
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     print(?)

"""

"""
Problem 1527:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. people = ... → assigns value to variable
#   2. min_age = ... → assigns value to variable
#   3. print(min_age)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. people = ... → assigns value to variable
#   7. min_age = ... → assigns value to variable
#   8. print(min_age)
#   9. people = ... → assigns value to variable
#   10. min_age = ... → assigns value to variable
#   11. print(min_age)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     min_age = ... → stored in memory
#     print(min_age)
#     people = ... → stored in memory
#     min_age = ... → stored in memory
#     print(min_age)
#     people = ... → stored in memory
#     min_age = ... → stored in memory
#     print(min_age)

"""

"""
Problem 1528:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. words = ... → assigns value to variable
#   3. grouped = ... → assigns value to variable
#   4. for word in words → loops over each item
#      → 4.1 ?.append(word)
#   5. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     grouped = ... → stored in memory
#     for word in words → runs body once per item
#     print(dict(...))
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 1529:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. items = ... → assigns value to variable
#   3. missing = ... → assigns value to variable
#   4. print(items, missing)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. items = ... → assigns value to variable
#   9. missing = ... → assigns value to variable
#   10. print(items, missing)
#   11. data = ... → assigns value to variable
#   12. items = ... → assigns value to variable
#   13. missing = ... → assigns value to variable
#   14. print(items, missing)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     items = ... → stored in memory
#     missing = ... → stored in memory
#     print(items, missing)
#     data = ... → stored in memory
#     items = ... → stored in memory
#     missing = ... → stored in memory
#     print(items, missing)
#     data = ... → stored in memory
#     items = ... → stored in memory
#     missing = ... → stored in memory
#     print(items, missing)

"""

"""
Problem 1530:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. words = ... → assigns value to variable
#   2. longest = ... → assigns value to variable
#   3. print(longest)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. longest = ... → assigns value to variable
#   8. print(longest)
#   9. words = ... → assigns value to variable
#   10. longest = ... → assigns value to variable
#   11. print(longest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     longest = ... → stored in memory
#     print(longest)
#     words = ... → stored in memory
#     longest = ... → stored in memory
#     print(longest)
#     words = ... → stored in memory
#     longest = ... → stored in memory
#     print(longest)

"""

"""
Problem 1531:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. counter = ... → assigns value to variable
#   2. counter.setdefault('count', 0)
#   3. statement
#   4. print(counter)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. counter = ... → assigns value to variable
#   8. counter.setdefault('count', 0)
#   9. statement
#   10. print(counter)
#   11. counter = ... → assigns value to variable
#   12. counter.setdefault('count', 0)
#   13. statement
#   14. print(counter)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     counter = ... → stored in memory
#     counter.setdefault('count', 0)
#     (statement runs)
#     print(counter)
#     counter = ... → stored in memory
#     counter.setdefault('count', 0)
#     (statement runs)
#     print(counter)
#     counter = ... → stored in memory
#     counter.setdefault('count', 0)
#     (statement runs)
#     print(counter)

"""

"""
Problem 1532:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % + > != ** and
# Built-in functions: all, int, next, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_prime(n) → each argument is evaluated first, then the function body runs
#   is_prime(15) runs first → then print() outputs the result
#   is_prime(15) → each argument is evaluated first, then the function body runs
#   n > 1 → evaluated as True or False (determines which branch runs)
#   is_prime(x) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   n % i → Python looks up n, then i, then applies %
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(n) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. first_prime = ... → assigns value to variable
#   4. print(first_prime)
#   5. def is_prime(n) → stored in memory, not executed
#   6. def is_prime(n) → stored in memory, not executed
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(is_prime(...))
#      → calls is_prime(n) with n=15
#      → 10.1 return n > 1 and all((n % i != 0 for i in range(2, int(n ** 0.5) + 1)))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     first_prime = ... → stored in memory
#     print(first_prime)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()

"""

"""
Problem 1533:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. text = ... → assigns value to variable
#   3. char_count = ... → assigns value to variable
#   4. for char in text → loops over each item
#   5. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     char_count = ... → stored in memory
#     for char in text → runs body once per item
#     print(dict(...))
#
#   Loop: takes one item from text each round, runs body, repeats until done

"""

"""
Problem 1534:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: + |
# Built-in functions: set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dict1.get(...) + dict2.get(...) → Python looks up dict1.get(...), then dict2.get(...), then applies +
#   set(...) | set(...) → Python looks up set(...), then set(...), then applies |
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. dict1 = ... → assigns value to variable
#   2. dict2 = ... → assigns value to variable
#   3. merged = ... → assigns value to variable
#   4. print(merged)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. merged = ... → assigns value to variable
#   10. print(merged)
#   11. dict1 = ... → assigns value to variable
#   12. dict2 = ... → assigns value to variable
#   13. merged = ... → assigns value to variable
#   14. print(merged)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     merged = ... → stored in memory
#     print(merged)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     merged = ... → stored in memory
#     print(merged)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     merged = ... → stored in memory
#     print(merged)

"""

"""
Problem 1535:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. students = ... → assigns value to variable
#   2. min_score = ... → assigns value to variable
#   3. print(min_score)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. students = ... → assigns value to variable
#   7. min_score = ... → assigns value to variable
#   8. print(min_score)
#   9. students = ... → assigns value to variable
#   10. min_score = ... → assigns value to variable
#   11. print(min_score)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     students = ... → stored in memory
#     min_score = ... → stored in memory
#     print(min_score)
#     students = ... → stored in memory
#     min_score = ... → stored in memory
#     print(min_score)
#     students = ... → stored in memory
#     min_score = ... → stored in memory
#     print(min_score)

"""

"""
Problem 1536:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. data.setdefault(...).append('apple')
#   3. data.setdefault(...).append('banana')
#   4. data.setdefault(...).append('carrot')
#   5. print(data)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. data = ... → assigns value to variable
#   9. data.setdefault(...).append('apple')
#   10. data.setdefault(...).append('banana')
#   11. data.setdefault(...).append('carrot')
#   12. print(data)
#   13. data = ... → assigns value to variable
#   14. data.setdefault(...).append('apple')
#   15. data.setdefault(...).append('banana')
#   16. data.setdefault(...).append('carrot')
#   17. print(data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     data.setdefault(...).append('carrot')
#     print(data)
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     data.setdefault(...).append('carrot')
#     print(data)
#     data = ... → stored in memory
#     data.setdefault(...).append('apple')
#     data.setdefault(...).append('banana')
#     data.setdefault(...).append('carrot')
#     print(data)

"""

"""
Problem 1537:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. products = ... → assigns value to variable
#   2. max_price = ... → assigns value to variable
#   3. print(max_price)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. products = ... → assigns value to variable
#   7. max_price = ... → assigns value to variable
#   8. print(max_price)
#   9. products = ... → assigns value to variable
#   10. max_price = ... → assigns value to variable
#   11. print(max_price)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     products = ... → stored in memory
#     max_price = ... → stored in memory
#     print(max_price)
#     products = ... → stored in memory
#     max_price = ... → stored in memory
#     print(max_price)
#     products = ... → stored in memory
#     max_price = ... → stored in memory
#     print(max_price)

"""

"""
Problem 1538:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. data = ... → assigns value to variable
#   3. nested = ... → assigns value to variable
#   4. for (key1, key2) in data → loops over each item
#   5. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     nested = ... → stored in memory
#     for (key1, key2) in data → runs body once per item
#     print(?)
#
#   Loop: takes one item from data each round, runs body, repeats until done

"""

"""
Problem 1539:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? + dict2.get(...) → Python looks up ?, then dict2.get(...), then applies +
#   dict1.get(...) + ' ' → Python looks up dict1.get(...), then ' ', then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. dict1 = ... → assigns value to variable
#   2. dict2 = ... → assigns value to variable
#   3. full_name = ... → assigns value to variable
#   4. print(full_name)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. full_name = ... → assigns value to variable
#   10. print(full_name)
#   11. dict1 = ... → assigns value to variable
#   12. dict2 = ... → assigns value to variable
#   13. full_name = ... → assigns value to variable
#   14. print(full_name)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     full_name = ... → stored in memory
#     print(full_name)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     full_name = ... → stored in memory
#     print(full_name)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     full_name = ... → stored in memory
#     print(full_name)

"""

"""
Problem 1540:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: >
# Built-in functions: next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x > 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. first_greater_than_5 = ... → assigns value to variable
#   3. print(first_greater_than_5)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. first_greater_than_5 = ... → assigns value to variable
#   8. print(first_greater_than_5)
#   9. numbers = ... → assigns value to variable
#   10. first_greater_than_5 = ... → assigns value to variable
#   11. print(first_greater_than_5)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     first_greater_than_5 = ... → stored in memory
#     print(first_greater_than_5)
#     numbers = ... → stored in memory
#     first_greater_than_5 = ... → stored in memory
#     print(first_greater_than_5)
#     numbers = ... → stored in memory
#     first_greater_than_5 = ... → stored in memory
#     print(first_greater_than_5)

"""

"""
Problem 1541:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. data.setdefault(...).add('python')
#   3. data.setdefault(...).add('programming')
#   4. print(data)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. data.setdefault(...).add('python')
#   9. data.setdefault(...).add('programming')
#   10. print(data)
#   11. data = ... → assigns value to variable
#   12. data.setdefault(...).add('python')
#   13. data.setdefault(...).add('programming')
#   14. print(data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     data.setdefault(...).add('python')
#     data.setdefault(...).add('programming')
#     print(data)
#     data = ... → stored in memory
#     data.setdefault(...).add('python')
#     data.setdefault(...).add('programming')
#     print(data)
#     data = ... → stored in memory
#     data.setdefault(...).add('python')
#     data.setdefault(...).add('programming')
#     print(data)

"""

"""
Problem 1542:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. people = ... → assigns value to variable
#   2. oldest = ... → assigns value to variable
#   3. print(oldest)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. people = ... → assigns value to variable
#   7. oldest = ... → assigns value to variable
#   8. print(oldest)
#   9. people = ... → assigns value to variable
#   10. oldest = ... → assigns value to variable
#   11. print(oldest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     oldest = ... → stored in memory
#     print(oldest)
#     people = ... → stored in memory
#     oldest = ... → stored in memory
#     print(oldest)
#     people = ... → stored in memory
#     oldest = ... → stored in memory
#     print(oldest)

"""

"""
Problem 1543:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Operators: +
# Built-in functions: defaultdict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   value + ' ' → Python looks up value, then ' ', then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. words = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. for (key, value) in words → loops over each item
#   5. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     result = ... → stored in memory
#     for (key, value) in words → runs body once per item
#     print(?)
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 1544:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. existing = ... → assigns value to variable
#   3. existing.extend(?)
#   4. print(existing)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. existing = ... → assigns value to variable
#   9. existing.extend(?)
#   10. print(existing)
#   11. data = ... → assigns value to variable
#   12. existing = ... → assigns value to variable
#   13. existing.extend(?)
#   14. print(existing)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     existing = ... → stored in memory
#     existing.extend(?)
#     print(existing)
#     data = ... → stored in memory
#     existing = ... → stored in memory
#     existing.extend(?)
#     print(existing)
#     data = ... → stored in memory
#     existing = ... → stored in memory
#     existing.extend(?)
#     print(existing)

"""

"""
Problem 1545:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: datetime, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from datetime import datetime
#   2. events = ... → assigns value to variable
#   3. max_date = ... → assigns value to variable
#   4. print(max_date)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from datetime import datetime
#   8. events = ... → assigns value to variable
#   9. max_date = ... → assigns value to variable
#   10. print(max_date)
#   11. from datetime import datetime
#   12. events = ... → assigns value to variable
#   13. max_date = ... → assigns value to variable
#   14. print(max_date)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     events = ... → stored in memory
#     max_date = ... → stored in memory
#     print(max_date)
#     events = ... → stored in memory
#     max_date = ... → stored in memory
#     print(max_date)
#     events = ... → stored in memory
#     max_date = ... → stored in memory
#     print(max_date)

"""

"""
Problem 1546:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(data)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. ? = ... → assigns value to variable
#   9. ? = ... → assigns value to variable
#   10. print(data)
#   11. data = ... → assigns value to variable
#   12. ? = ... → assigns value to variable
#   13. ? = ... → assigns value to variable
#   14. print(data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(data)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(data)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(data)

"""

"""
Problem 1547:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def number_gen() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: iter, next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number_gen() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def number_gen() → stored in memory, not executed
#   2. gen = ... → assigns value to variable
#   3. first = ... → assigns value to variable
#   4. empty_gen = ... → assigns value to variable
#   5. empty_result = ... → assigns value to variable
#   6. print(first, empty_result)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. evaluates expression
#   10. evaluates expression
#   11. evaluates expression
#   12. evaluates expression
#   13. evaluates expression
#   14. evaluates expression
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, number_gen) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     gen = ... → stored in memory
#     first = ... → stored in memory
#     empty_gen = ... → stored in memory
#     empty_result = ... → stored in memory
#     print(first, empty_result)
#     (statement runs)
#     (statement runs)
#     (statement runs)
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 1548:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import defaultdict
#   2. stats = ... → assigns value to variable
#   3. statement
#   4. statement
#   5. statement
#   6. statement
#   7. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     stats = ... → stored in memory
#     (statement runs)
#     (statement runs)
#     (statement runs)
#     (statement runs)
#     print(dict(...))

"""

"""
Problem 1549:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_list() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   'items' not in data → evaluated as True or False (determines which branch runs)
#   create_list() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_list() → stored in memory, not executed
#   2. data = ... → assigns value to variable
#   3. if ... → only one branch executes
#   4. items = ... → assigns value to variable
#   5. items.append('apple')
#   6. print(data)
#   7. def create_list() → stored in memory, not executed
#   8. def create_list() → stored in memory, not executed
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. result = ... → assigns value to variable
#   12. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     if → evaluates condition, runs matching branch
#     items = ... → stored in memory
#     items.append('apple')
#     print(data)
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     print(result)
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1550:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x * y → Python looks up x, then y, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. numbers = ... → assigns value to variable
#   3. product = ... → assigns value to variable
#   4. print(product)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from functools import reduce
#   8. numbers = ... → assigns value to variable
#   9. product = ... → assigns value to variable
#   10. print(product)
#   11. from functools import reduce
#   12. numbers = ... → assigns value to variable
#   13. product = ... → assigns value to variable
#   14. print(product)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     product = ... → stored in memory
#     print(product)
#     numbers = ... → stored in memory
#     product = ... → stored in memory
#     print(product)
#     numbers = ... → stored in memory
#     product = ... → stored in memory
#     print(product)

"""

"""
Problem 1551:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
# Built-in functions: multiply_by_5, partial()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x * y → Python looks up x, then y, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import partial
#   2. multiply_by_5 = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from functools import partial
#   8. multiply_by_5 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. from functools import partial
#   12. multiply_by_5 = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     multiply_by_5 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     multiply_by_5 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     multiply_by_5 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1552:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fibonacci(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: + - <
# Built-in functions: lru_cache()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fibonacci(10) runs first → then print() outputs the result
#   fibonacci(n) → each argument is evaluated first, then the function body runs
#   fibonacci(15) runs first → then print() outputs the result
#   n < 2 → evaluated as True or False (determines which branch runs)
#   fibonacci(...) + fibonacci(...) → Python looks up fibonacci(...), then fibonacci(...), then applies +
#   fibonacci(10) → each argument is evaluated first, then the function body runs
#   fibonacci(15) → each argument is evaluated first, then the function body runs
#   fibonacci(n - 1) → each argument is evaluated first, then the function body runs
#   fibonacci(n - 2) → each argument is evaluated first, then the function body runs
#   n - 1 → Python looks up n, then 1, then applies -
#   n - 2 → Python looks up n, then 2, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import lru_cache
#   2. def fibonacci(n) → stored in memory, not executed
#   3. print(fibonacci(...))
#      → calls fibonacci(n) with n=10
#      → 3.1 return n
#   4. def fibonacci(n) → stored in memory, not executed
#   5. def fibonacci(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(fibonacci(...))
#      → calls fibonacci(n) with n=15
#      → 9.1 return n
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fibonacci) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1553:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, permutations()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import permutations
#   2. items = ... → assigns value to variable
#   3. perms = ... → assigns value to variable
#   4. print(perms)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from itertools import permutations
#   8. items = ... → assigns value to variable
#   9. perms = ... → assigns value to variable
#   10. print(perms)
#   11. from itertools import permutations
#   12. items = ... → assigns value to variable
#   13. perms = ... → assigns value to variable
#   14. print(perms)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     perms = ... → stored in memory
#     print(perms)
#     items = ... → stored in memory
#     perms = ... → stored in memory
#     print(perms)
#     items = ... → stored in memory
#     perms = ... → stored in memory
#     print(perms)

"""

"""
Problem 1554:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, product()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import product
#   2. list1 = ... → assigns value to variable
#   3. list2 = ... → assigns value to variable
#   4. cartesian = ... → assigns value to variable
#   5. print(cartesian)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. from itertools import product
#   9. list1 = ... → assigns value to variable
#   10. list2 = ... → assigns value to variable
#   11. cartesian = ... → assigns value to variable
#   12. print(cartesian)
#   13. from itertools import product
#   14. list1 = ... → assigns value to variable
#   15. list2 = ... → assigns value to variable
#   16. cartesian = ... → assigns value to variable
#   17. print(cartesian)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     cartesian = ... → stored in memory
#     print(cartesian)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     cartesian = ... → stored in memory
#     print(cartesian)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     cartesian = ... → stored in memory
#     print(cartesian)

"""

"""
Problem 1555:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: cycle, next, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import cycle
#   2. colors = ... → assigns value to variable
#   3. color_cycle = ... → assigns value to variable
#   4. first_5 = ... → assigns value to variable
#   5. print(first_5)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. from itertools import cycle
#   9. colors = ... → assigns value to variable
#   10. color_cycle = ... → assigns value to variable
#   11. first_5 = ... → assigns value to variable
#   12. print(first_5)
#   13. from itertools import cycle
#   14. colors = ... → assigns value to variable
#   15. color_cycle = ... → assigns value to variable
#   16. first_5 = ... → assigns value to variable
#   17. print(first_5)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     colors = ... → stored in memory
#     color_cycle = ... → stored in memory
#     first_5 = ... → stored in memory
#     print(first_5)
#     colors = ... → stored in memory
#     color_cycle = ... → stored in memory
#     first_5 = ... → stored in memory
#     print(first_5)
#     colors = ... → stored in memory
#     color_cycle = ... → stored in memory
#     first_5 = ... → stored in memory
#     print(first_5)

"""

"""
Problem 1556:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, repeat()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import repeat
#   2. repeated = ... → assigns value to variable
#   3. print(repeated)
#   4. def main() → stored in memory, not executed
#   5. main() → calls main() with 
#      → 5.1 pass
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     repeated = ... → stored in memory
#     print(repeated)
#     main() → jumps into main(), runs body, returns

"""

"""
Problem 1557:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: islice, list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import islice
#   2. numbers = ... → assigns value to variable
#   3. first_5 = ... → assigns value to variable
#   4. print(first_5)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from itertools import islice
#   8. numbers = ... → assigns value to variable
#   9. first_5 = ... → assigns value to variable
#   10. print(first_5)
#   11. from itertools import islice
#   12. numbers = ... → assigns value to variable
#   13. first_5 = ... → assigns value to variable
#   14. print(first_5)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     first_5 = ... → stored in memory
#     print(first_5)
#     numbers = ... → stored in memory
#     first_5 = ... → stored in memory
#     print(first_5)
#     numbers = ... → stored in memory
#     first_5 = ... → stored in memory
#     print(first_5)

"""

"""
Problem 1558:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: iter, list, tee()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import tee
#   2. numbers = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(list(...), list(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(list(...), list(...))

"""

"""
Problem 1559:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: accumulate, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import accumulate
#   2. numbers = ... → assigns value to variable
#   3. cumulative = ... → assigns value to variable
#   4. print(cumulative)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from itertools import accumulate
#   8. numbers = ... → assigns value to variable
#   9. cumulative = ... → assigns value to variable
#   10. print(cumulative)
#   11. from itertools import accumulate
#   12. numbers = ... → assigns value to variable
#   13. cumulative = ... → assigns value to variable
#   14. print(cumulative)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     cumulative = ... → stored in memory
#     print(cumulative)
#     numbers = ... → stored in memory
#     cumulative = ... → stored in memory
#     print(cumulative)
#     numbers = ... → stored in memory
#     cumulative = ... → stored in memory
#     print(cumulative)

"""

"""
Problem 1560:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Person()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from dataclasses import dataclass
#   2. class Person → class definition stored
#   3. person = ... → assigns value to variable
#   4. print(person)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     print(person)
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))

"""

"""
Problem 1561:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Student → defines a new type
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Student, field()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from dataclasses import dataclass, field
#   2. class Student → class definition stored
#   3. student = ... → assigns value to variable
#   4. print(student)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Student(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Student) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     student = ... → stored in memory
#     print(student)
#     obj = ... → stored in memory
#     print(obj)
#     print(Student(...))

"""

"""
Problem 1562:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Point → defines a new type
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Point, dataclass()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from dataclasses import dataclass
#   2. class Point → class definition stored
#   3. point = ... → assigns value to variable
#   4. print(point)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Point(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Point) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     point = ... → stored in memory
#     print(point)
#     obj = ... → stored in memory
#     print(obj)
#     print(Point(...))

"""

"""
Problem 1563:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_numbers(?) → each argument is evaluated first, then the function body runs
#   sum_numbers(numbers) → each argument is evaluated first, then the function body runs
#   sum_numbers(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from typing import List
#   2. def sum_numbers(numbers) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def sum_numbers(numbers) → stored in memory, not executed
#   6. def sum_numbers(numbers) → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sum_numbers(...))
#      → calls sum_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 10.1 from functools import reduce
#      → 10.2 import operator
#      → 10.3 return reduce(operator.add, numbers, 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_numbers(...)) → sum_numbers() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 1564:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def greet(name) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: is
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   greet('Alice') runs first → then print() outputs the result
#   greet() runs first → then print() outputs the result
#   greet(name) → each argument is evaluated first, then the function body runs
#   greet('Bob') runs first → then print() outputs the result
#   name  None → evaluated as True or False (determines which branch runs)
#   greet('Alice') → each argument is evaluated first, then the function body runs
#   greet() → each argument is evaluated first, then the function body runs
#   greet('Bob') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from typing import Optional
#   2. def greet(name) → stored in memory, not executed
#   3. print(greet(...), greet(...))
#      → calls greet(name) with name='Alice'
#      → 3.1 return 'Hello, Guest!'
#   4. def greet(name) → stored in memory, not executed
#   5. def greet(name) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(greet(...))
#      → calls greet(name) with name='Bob'
#      → 9.1 return 'Hello, Guest!'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (greet) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(greet(...), greet(...)) → greet() runs first → value passed to print()
#     name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(greet(...)) → greet() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1565:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def process_value(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   process_value(42) runs first → then print() outputs the result
#   process_value('hello') runs first → then print() outputs the result
#   process_value(value) → each argument is evaluated first, then the function body runs
#   process_value(25) runs first → then print() outputs the result
#   process_value(42) → each argument is evaluated first, then the function body runs
#   process_value('hello') → each argument is evaluated first, then the function body runs
#   process_value(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from typing import Union
#   2. def process_value(value) → stored in memory, not executed
#   3. print(process_value(...), process_value(...))
#      → calls process_value(value) with value=42
#      → 3.1 return str(value)
#   4. def process_value(value) → stored in memory, not executed
#   5. def process_value(value) → stored in memory, not executed
#   6. value = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(process_value(...))
#      → calls process_value(value) with value=25
#      → 9.1 return str(value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (process_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(process_value(...), process_value(...)) → process_value() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(process_value(...)) → process_value() runs first → value passed to print()

"""

"""
Problem 1566:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. numbers = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. numbers = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     while → checks condition before each iteration
#     numbers = ... → stored in memory
#     while → checks condition before each iteration
#     numbers = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1567:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def expensive_function(x) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * >
# Built-in functions: pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   expensive_function(x) → each argument is evaluated first, then the function body runs
#   expensive_function(3) runs first → then print() outputs the result
#   x * 2 → Python looks up x, then 2, then applies *
#   expensive_function(3) → each argument is evaluated first, then the function body runs
#   ? > 4 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def expensive_function(x) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def expensive_function(x) → stored in memory, not executed
#   6. def expensive_function(x) → stored in memory, not executed
#   7. x = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(expensive_function(...))
#      → calls expensive_function(x) with x=3
#      → 10.1 return pow(x, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (expensive_function) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     x = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(expensive_function(...)) → expensive_function() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1568:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def handle_command(cmd) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   handle_command('start') runs first → then print() outputs the result
#   handle_command(cmd) → each argument is evaluated first, then the function body runs
#   handle_command(25) runs first → then print() outputs the result
#   handle_command('start') → each argument is evaluated first, then the function body runs
#   handle_command(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def handle_command(cmd) → stored in memory, not executed
#   2. print(handle_command(...))
#      → calls handle_command(cmd) with cmd='start'
#      → 2.1 return 'Starting...'
#   3. def handle_command(cmd) → stored in memory, not executed
#   4. def handle_command(cmd) → stored in memory, not executed
#   5. cmd = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(handle_command(...))
#      → calls handle_command(cmd) with cmd=25
#      → 8.1 return 'Starting...'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (handle_command) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(handle_command(...)) → handle_command() runs first → value passed to print()
#     cmd = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(handle_command(...)) → handle_command() runs first → value passed to print()

"""

"""
Problem 1569:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def process_point(point) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   process_point(?) runs first → then print() outputs the result
#   process_point(point) → each argument is evaluated first, then the function body runs
#   process_point(25) runs first → then print() outputs the result
#   process_point(?) → each argument is evaluated first, then the function body runs
#   process_point(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def process_point(point) → stored in memory, not executed
#   2. print(process_point(...))
#      → calls process_point(point) with point=?
#      → 2.1 return 'Origin'
#   3. def process_point(point) → stored in memory, not executed
#   4. def process_point(point) → stored in memory, not executed
#   5. point = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(process_point(...))
#      → calls process_point(point) with point=25
#      → 8.1 return 'Origin'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (process_point) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(process_point(...)) → process_point() runs first → value passed to print()
#     point = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(process_point(...)) → process_point() runs first → value passed to print()

"""

"""
Problem 1570:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Circle → defines a new type
#     def __init__(...) → method of Circle
#     def area(...) → method of Circle
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * **
# Built-in functions: Circle()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   3.14159 * ? → Python looks up 3.14159, then ?, then applies *
#   self.radius ** 2 → Python looks up self.radius, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Circle → class definition stored
#   2. circle = ... → assigns value to variable
#   3. print(?)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. obj = ... → assigns value to variable
#   7. print(obj)
#   8. print(Circle(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Circle) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     circle = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Circle(...))

"""

"""
Problem 1571:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Temperature → defines a new type
#     def __init__(...) → method of Temperature
#     def celsius(...) → method of Temperature
#     def celsius(...) → method of Temperature
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: <
# Built-in functions: Temperature, ValueError()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   value < ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Temperature → class definition stored
#   2. temp = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Temperature(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Temperature) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     temp = ... → stored in memory
#     ? = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Temperature(...))
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1572:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Book → defines a new type
#     def __init__(...) → method of Book
#     def __str__(...) → method of Book
#
# Built-in functions: Book, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Book → class definition stored
#   2. book = ... → assigns value to variable
#   3. print(str(...))
#   4. obj = ... → assigns value to variable
#   5. print(obj)
#   6. print(Book(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Book) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     book = ... → stored in memory
#     print(str(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Book(...))

"""

"""
Problem 1573:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Point → defines a new type
#     def __init__(...) → method of Point
#     def __repr__(...) → method of Point
#
# Built-in functions: Point, repr()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Point → class definition stored
#   2. point = ... → assigns value to variable
#   3. print(repr(...))
#   4. obj = ... → assigns value to variable
#   5. print(obj)
#   6. print(Point(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Point) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     point = ... → stored in memory
#     print(repr(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Point(...))

"""

"""
Problem 1574:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#     def __init__(...) → method of Person
#     def __eq__(...) → method of Person
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: == and
# Built-in functions: Person, isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   p1 == p2 → evaluated as True or False (determines which branch runs)
#   self.name == other.name → evaluated as True or False (determines which branch runs)
#   self.age == other.age → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Person → class definition stored
#   2. p1 = ... → assigns value to variable
#   3. p2 = ... → assigns value to variable
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     p1 = ... → stored in memory
#     p2 = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1575:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Point → defines a new type
#     def __init__(...) → method of Point
#     def __hash__(...) → method of Point
#     def __eq__(...) → method of Point
#
# Operators: == and
# Built-in functions: Point, hash, isinstance, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   self.x == other.x → evaluated as True or False (determines which branch runs)
#   self.y == other.y → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Point → class definition stored
#   2. point = ... → assigns value to variable
#   3. points_set = ... → assigns value to variable
#   4. print(len(...))
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(Point(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Point) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     point = ... → stored in memory
#     points_set = ... → stored in memory
#     print(len(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Point(...))

"""

"""
Problem 1576:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Stack → defines a new type
#     def __init__(...) → method of Stack
#     def push(...) → method of Stack
#     def __len__(...) → method of Stack
#
# Built-in functions: Stack, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Stack → class definition stored
#   2. stack = ... → assigns value to variable
#   3. stack.push(1)
#   4. stack.push(2)
#   5. print(len(...))
#   6. obj = ... → assigns value to variable
#   7. print(obj)
#   8. print(Stack(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Stack) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     stack = ... → stored in memory
#     stack.push(1)
#     stack.push(2)
#     print(len(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Stack(...))

"""

"""
Problem 1577:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class MyList → defines a new type
#     def __init__(...) → method of MyList
#     def __getitem__(...) → method of MyList
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: MyList()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class MyList → class definition stored
#   2. my_list = ... → assigns value to variable
#   3. print(?)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. obj = ... → assigns value to variable
#   7. print(obj)
#   8. print(MyList(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (MyList) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_list = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(MyList(...))

"""

"""
Problem 1578:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Timer → defines a new type
#     def __enter__(...) → method of Timer
#     def __exit__(...) → method of Timer
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: -
# Built-in functions: Timer, range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   time.time(...) - self.start → Python looks up time.time(...), then self.start, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Timer → class definition stored
#   2. statement
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(Timer(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Timer) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     obj = ... → stored in memory
#     print(obj)
#     print(Timer(...))

"""

"""
Problem 1579:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Resource → defines a new type
#     def __enter__(...) → method of Resource
#     def __exit__(...) → method of Resource
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: ExitStack, Resource()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from contextlib import ExitStack
#   2. class Resource → class definition stored
#   3. statement
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. obj = ... → assigns value to variable
#   7. print(obj)
#   8. print(Resource(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Resource) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     obj = ... → stored in memory
#     print(obj)
#     print(Resource(...))

"""

"""
Problem 1580:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Path()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from pathlib import Path
#   2. path = ... → assigns value to variable
#   3. exists = ... → assigns value to variable
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from pathlib import Path
#   8. from pathlib import Path
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     path = ... → stored in memory
#     exists = ... → stored in memory
#     print(?)

"""

"""
Problem 1581:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Path()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from pathlib import Path
#   2. path = ... → assigns value to variable
#   3. parent = ... → assigns value to variable
#   4. print(parent)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from pathlib import Path
#   8. path = ... → assigns value to variable
#   9. parent = ... → assigns value to variable
#   10. print(parent)
#   11. from pathlib import Path
#   12. path = ... → assigns value to variable
#   13. parent = ... → assigns value to variable
#   14. print(parent)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     path = ... → stored in memory
#     parent = ... → stored in memory
#     print(parent)
#     path = ... → stored in memory
#     parent = ... → stored in memory
#     print(parent)
#     path = ... → stored in memory
#     parent = ... → stored in memory
#     print(parent)

"""

"""
Problem 1582:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: /
# Built-in functions: Path()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? / 'file.txt' → Python looks up ?, then 'file.txt', then applies /
#   base / 'documents' → Python looks up base, then 'documents', then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from pathlib import Path
#   2. base = ... → assigns value to variable
#   3. full_path = ... → assigns value to variable
#   4. print(full_path)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from pathlib import Path
#   8. base = ... → assigns value to variable
#   9. full_path = ... → assigns value to variable
#   10. print(full_path)
#   11. from pathlib import Path
#   12. base = ... → assigns value to variable
#   13. full_path = ... → assigns value to variable
#   14. print(full_path)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     base = ... → stored in memory
#     full_path = ... → stored in memory
#     print(full_path)
#     base = ... → stored in memory
#     full_path = ... → stored in memory
#     print(full_path)
#     base = ... → stored in memory
#     full_path = ... → stored in memory
#     print(full_path)

"""

"""
Problem 1583:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#   2. data = ... → assigns value to variable
#   3. json_string = ... → assigns value to variable
#   4. print(json_string)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import json
#   8. data = ... → assigns value to variable
#   9. json_string = ... → assigns value to variable
#   10. print(json_string)
#   11. import json
#   12. data = ... → assigns value to variable
#   13. json_string = ... → assigns value to variable
#   14. print(json_string)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     json_string = ... → stored in memory
#     print(json_string)
#     data = ... → stored in memory
#     json_string = ... → stored in memory
#     print(json_string)
#     data = ... → stored in memory
#     json_string = ... → stored in memory
#     print(json_string)

"""

"""
Problem 1584:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1585:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. text = ... → assigns value to variable
#   3. new_text = ... → assigns value to variable
#   4. print(new_text)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import re
#   8. text = ... → assigns value to variable
#   9. new_text = ... → assigns value to variable
#   10. print(new_text)
#   11. import re
#   12. text = ... → assigns value to variable
#   13. new_text = ... → assigns value to variable
#   14. print(new_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     new_text = ... → stored in memory
#     print(new_text)
#     text = ... → stored in memory
#     new_text = ... → stored in memory
#     print(new_text)
#     text = ... → stored in memory
#     new_text = ... → stored in memory
#     print(new_text)

"""

"""
Problem 1586:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. text = ... → assigns value to variable
#   3. pattern = ... → assigns value to variable
#   4. matches = ... → assigns value to variable
#   5. print(matches)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import re
#   9. text = ... → assigns value to variable
#   10. pattern = ... → assigns value to variable
#   11. matches = ... → assigns value to variable
#   12. print(matches)
#   13. import re
#   14. text = ... → assigns value to variable
#   15. pattern = ... → assigns value to variable
#   16. matches = ... → assigns value to variable
#   17. print(matches)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     pattern = ... → stored in memory
#     matches = ... → stored in memory
#     print(matches)
#     text = ... → stored in memory
#     pattern = ... → stored in memory
#     matches = ... → stored in memory
#     print(matches)
#     text = ... → stored in memory
#     pattern = ... → stored in memory
#     matches = ... → stored in memory
#     print(matches)

"""

"""
Problem 1587:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: if/else
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. text = ... → assigns value to variable
#   3. pattern = ... → assigns value to variable
#   4. match = ... → assigns value to variable
#   5. if ... → only one branch executes
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     pattern = ... → stored in memory
#     match = ... → stored in memory
#     if → evaluates condition, runs matching branch
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1588:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: OrderedDict, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import OrderedDict
#   2. od = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. ? = ... → assigns value to variable
#   5. ? = ... → assigns value to variable
#   6. print(list(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     od = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(list(...))

"""

"""
Problem 1589:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: ChainMap, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import ChainMap
#   2. dict1 = ... → assigns value to variable
#   3. dict2 = ... → assigns value to variable
#   4. chain = ... → assigns value to variable
#   5. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     chain = ... → stored in memory
#     print(dict(...))

"""

"""
Problem 1590:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Point, namedtuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import namedtuple
#   2. Point = ... → assigns value to variable
#   3. p = ... → assigns value to variable
#   4. print(?, ?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from collections import namedtuple
#   8. Point = ... → assigns value to variable
#   9. p = ... → assigns value to variable
#   10. print(?, ?)
#   11. from collections import namedtuple
#   12. Point = ... → assigns value to variable
#   13. p = ... → assigns value to variable
#   14. print(?, ?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     Point = ... → stored in memory
#     p = ... → stored in memory
#     print(?, ?)
#     Point = ... → stored in memory
#     p = ... → stored in memory
#     print(?, ?)
#     Point = ... → stored in memory
#     p = ... → stored in memory
#     print(?, ?)

"""

"""
Problem 1591:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i * j → Python looks up i, then j, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. matrix = ... → assigns value to variable
#   2. print(matrix)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. matrix = ... → assigns value to variable
#   6. print(matrix)
#   7. matrix = ... → assigns value to variable
#   8. print(matrix)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     matrix = ... → stored in memory
#     print(matrix)
#     matrix = ... → stored in memory
#     print(matrix)
#     matrix = ... → stored in memory
#     print(matrix)

"""

"""
Problem 1592:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: % == and
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#   x % 3 → Python looks up x, then 3, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. filtered = ... → assigns value to variable
#   3. print(filtered)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. filtered = ... → assigns value to variable
#   8. print(filtered)
#   9. numbers = ... → assigns value to variable
#   10. filtered = ... → assigns value to variable
#   11. print(filtered)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     filtered = ... → stored in memory
#     print(filtered)
#     numbers = ... → stored in memory
#     filtered = ... → stored in memory
#     print(filtered)
#     numbers = ... → stored in memory
#     filtered = ... → stored in memory
#     print(filtered)

"""

"""
Problem 1593:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#     def __init__(...) → method of Person
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Person, attrgetter, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from operator import attrgetter
#   2. class Person → class definition stored
#   3. people = ... → assigns value to variable
#   4. sorted_people = ... → assigns value to variable
#   5. print(?)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. obj = ... → assigns value to variable
#   9. print(obj)
#   10. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     sorted_people = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))

"""

"""
Problem 1594:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#     def greet(...) → method of Person
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Person, list, map, methodcaller()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from operator import methodcaller
#   2. class Person → class definition stored
#   3. people = ... → assigns value to variable
#   4. greetings = ... → assigns value to variable
#   5. print(greetings)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. obj = ... → assigns value to variable
#   9. print(obj)
#   10. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     greetings = ... → stored in memory
#     print(greetings)
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))

"""

"""
Problem 1595:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. text = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1596:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. text = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1597:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. parts = ... → assigns value to variable
#   3. print(parts)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. parts = ... → assigns value to variable
#   8. print(parts)
#   9. text = ... → assigns value to variable
#   10. parts = ... → assigns value to variable
#   11. print(parts)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     parts = ... → stored in memory
#     print(parts)
#     text = ... → stored in memory
#     parts = ... → stored in memory
#     print(parts)
#     text = ... → stored in memory
#     parts = ... → stored in memory
#     print(parts)

"""

"""
Problem 1598:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. original = ... → assigns value to variable
#   2. copied = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(original, copied)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. original = ... → assigns value to variable
#   8. copied = ... → assigns value to variable
#   9. ? = ... → assigns value to variable
#   10. print(original, copied)
#   11. original = ... → assigns value to variable
#   12. copied = ... → assigns value to variable
#   13. ? = ... → assigns value to variable
#   14. print(original, copied)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     original = ... → stored in memory
#     copied = ... → stored in memory
#     ? = ... → stored in memory
#     print(original, copied)
#     original = ... → stored in memory
#     copied = ... → stored in memory
#     ? = ... → stored in memory
#     print(original, copied)
#     original = ... → stored in memory
#     copied = ... → stored in memory
#     ? = ... → stored in memory
#     print(original, copied)

"""

"""
Problem 1599:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. my_list = ... → assigns value to variable
#   2. my_list.clear()
#   3. print(my_list)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. my_list.clear()
#   8. print(my_list)
#   9. my_list = ... → assigns value to variable
#   10. my_list.clear()
#   11. print(my_list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_list = ... → stored in memory
#     my_list.clear()
#     print(my_list)
#     my_list = ... → stored in memory
#     my_list.clear()
#     print(my_list)
#     my_list = ... → stored in memory
#     my_list.clear()
#     print(my_list)

"""

"""
Problem 1600:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. d = ... → assigns value to variable
#   2. item = ... → assigns value to variable
#   3. print(item, d)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. item = ... → assigns value to variable
#   8. print(item, d)
#   9. d = ... → assigns value to variable
#   10. item = ... → assigns value to variable
#   11. print(item, d)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     d = ... → stored in memory
#     item = ... → stored in memory
#     print(item, d)
#     d = ... → stored in memory
#     item = ... → stored in memory
#     print(item, d)
#     d = ... → stored in memory
#     item = ... → stored in memory
#     print(item, d)

"""

"""
Problem 1601:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. dict1 = ... → assigns value to variable
#   2. dict2 = ... → assigns value to variable
#   3. dict1.update(dict2)
#   4. print(dict1)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. dict1.update(dict2)
#   10. print(dict1)
#   11. dict1 = ... → assigns value to variable
#   12. dict2 = ... → assigns value to variable
#   13. dict1.update(dict2)
#   14. print(dict1)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     dict1.update(dict2)
#     print(dict1)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     dict1.update(dict2)
#     print(dict1)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     dict1.update(dict2)
#     print(dict1)

"""

"""
Problem 1602:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. d = ... → assigns value to variable
#   2. keys = ... → assigns value to variable
#   3. print(keys)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. keys = ... → assigns value to variable
#   8. print(keys)
#   9. d = ... → assigns value to variable
#   10. keys = ... → assigns value to variable
#   11. print(keys)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     d = ... → stored in memory
#     keys = ... → stored in memory
#     print(keys)
#     d = ... → stored in memory
#     keys = ... → stored in memory
#     print(keys)
#     d = ... → stored in memory
#     keys = ... → stored in memory
#     print(keys)

"""

"""
Problem 1603:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. d = ... → assigns value to variable
#   2. values = ... → assigns value to variable
#   3. print(values)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. values = ... → assigns value to variable
#   8. print(values)
#   9. d = ... → assigns value to variable
#   10. values = ... → assigns value to variable
#   11. print(values)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     d = ... → stored in memory
#     values = ... → stored in memory
#     print(values)
#     d = ... → stored in memory
#     values = ... → stored in memory
#     print(values)
#     d = ... → stored in memory
#     values = ... → stored in memory
#     print(values)

"""

"""
Problem 1604:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. d = ... → assigns value to variable
#   2. items = ... → assigns value to variable
#   3. print(items)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. items = ... → assigns value to variable
#   8. print(items)
#   9. d = ... → assigns value to variable
#   10. items = ... → assigns value to variable
#   11. print(items)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     d = ... → stored in memory
#     items = ... → stored in memory
#     print(items)
#     d = ... → stored in memory
#     items = ... → stored in memory
#     print(items)
#     d = ... → stored in memory
#     items = ... → stored in memory
#     print(items)

"""

"""
Problem 1605:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class CustomError(Exception) → inherits from parent class
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: CustomError()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class CustomError(Exception) → class definition stored
#   2. statement
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(CustomError(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (CustomError) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     obj = ... → stored in memory
#     print(obj)
#     print(CustomError(...))

"""

"""
Problem 1606:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: TypeError, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 1607:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from contextlib import redirect_stdout
#   2. from io import StringIO
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1608:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import wraps
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1609:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: accumulate, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import accumulate
#   2. import operator
#   3. numbers = ... → assigns value to variable
#   4. cumulative_products = ... → assigns value to variable
#   5. print(cumulative_products)
#   6. def main() → stored in memory, not executed
#   7. main() → calls main() with 
#      → 7.1 pass
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     cumulative_products = ... → stored in memory
#     print(cumulative_products)
#     main() → jumps into main(), runs body, returns

"""

"""
Problem 1610:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: combinations_with_replacement, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import combinations_with_replacement
#   2. items = ... → assigns value to variable
#   3. combs = ... → assigns value to variable
#   4. print(combs)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from itertools import combinations_with_replacement
#   8. items = ... → assigns value to variable
#   9. combs = ... → assigns value to variable
#   10. print(combs)
#   11. from itertools import combinations_with_replacement
#   12. items = ... → assigns value to variable
#   13. combs = ... → assigns value to variable
#   14. print(combs)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     combs = ... → stored in memory
#     print(combs)
#     items = ... → stored in memory
#     combs = ... → stored in memory
#     print(combs)
#     items = ... → stored in memory
#     combs = ... → stored in memory
#     print(combs)

"""

"""
Problem 1611:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: <
# Built-in functions: dropwhile, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x < 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import dropwhile
#   2. numbers = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from itertools import dropwhile
#   8. numbers = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. from itertools import dropwhile
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1612:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: <
# Built-in functions: list, takewhile()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x < 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import takewhile
#   2. numbers = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from itertools import takewhile
#   8. numbers = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. from itertools import takewhile
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1613:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, starmap()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import starmap
#   2. import operator
#   3. pairs = ... → assigns value to variable
#   4. products = ... → assigns value to variable
#   5. print(products)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. from itertools import starmap
#   9. import operator
#   10. pairs = ... → assigns value to variable
#   11. products = ... → assigns value to variable
#   12. print(products)
#   13. from itertools import starmap
#   14. import operator
#   15. pairs = ... → assigns value to variable
#   16. products = ... → assigns value to variable
#   17. print(products)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pairs = ... → stored in memory
#     products = ... → stored in memory
#     print(products)
#     pairs = ... → stored in memory
#     products = ... → stored in memory
#     print(products)
#     pairs = ... → stored in memory
#     products = ... → stored in memory
#     print(products)

"""

"""
Problem 1614:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def compare(x, y) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + -
# Built-in functions: cmp_to_key, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   compare(x, y) → each argument is evaluated first, then the function body runs
#   compare(3, 14) runs first → then print() outputs the result
#   y - x → Python looks up y, then x, then applies -
#   y + ? → Python looks up y, then ?, then applies +
#   compare(3, 14) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import cmp_to_key
#   2. def compare(x, y) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. sorted_nums = ... → assigns value to variable
#   5. print(sorted_nums)
#   6. def compare(x, y) → stored in memory, not executed
#   7. def compare(x, y) → stored in memory, not executed
#   8. x = ... → assigns value to variable
#   9. y = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(compare(...))
#      → calls compare(x, y) with x=3, y=14
#      → 12.1 import operator
#      → 12.2 return operator.sub(y, x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (compare) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     sorted_nums = ... → stored in memory
#     print(sorted_nums)
#     x = ... → stored in memory
#     y = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(compare(...)) → compare() runs first → value passed to print()

"""

"""
Problem 1615:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def process(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: type()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   process(42) runs first → then print() outputs the result
#   process('hello') runs first → then print() outputs the result
#   process(value) → each argument is evaluated first, then the function body runs
#   process(25) runs first → then print() outputs the result
#   process(42) → each argument is evaluated first, then the function body runs
#   process('hello') → each argument is evaluated first, then the function body runs
#   process(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import singledispatch
#   2. def process(value) → stored in memory, not executed
#   3. def _(value) → stored in memory, not executed
#   4. def _(value) → stored in memory, not executed
#   5. print(process(...), process(...))
#      → calls process(value) with value=42
#      → 5.1 return f'Unknown type: {type(value).__name__}'
#   6. def process(value) → stored in memory, not executed
#   7. def process(value) → stored in memory, not executed
#   8. value = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(process(...))
#      → calls process(value) with value=25
#      → 11.1 return f'Unknown type: {type(value).__name__}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (process, _) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(process(...), process(...)) → process() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(process(...)) → process() runs first → value passed to print()

"""

"""
Problem 1616:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def process_scores(scores) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   process_scores(?) → each argument is evaluated first, then the function body runs
#   process_scores(scores) → each argument is evaluated first, then the function body runs
#   process_scores(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from typing import Dict
#   2. def process_scores(scores) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def process_scores(scores) → stored in memory, not executed
#   6. def process_scores(scores) → stored in memory, not executed
#   7. scores = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(process_scores(...))
#      → calls process_scores(scores) with scores=[4, 1, 8, 6, 3]
#      → 10.1 return sum(scores.values())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (process_scores) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     scores = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(process_scores(...)) → process_scores() runs first → value passed to print()

"""

"""
Problem 1617:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_name_age() → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_name_age() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from typing import Tuple
#   2. def get_name_age() → stored in memory, not executed
#   3. ? = ... → assigns value to variable
#   4. print(?)
#   5. def get_name_age() → stored in memory, not executed
#   6. def get_name_age() → stored in memory, not executed
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. result = ... → assigns value to variable
#   10. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_name_age) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     print(?)
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 1618:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def apply_operation(x, y, op) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: op()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   apply_operation(5, 3, ?) → each argument is evaluated first, then the function body runs
#   apply_operation(x, y, op) → each argument is evaluated first, then the function body runs
#   apply_operation(3, 14, 25) runs first → then print() outputs the result
#   apply_operation(3, 14, 25) → each argument is evaluated first, then the function body runs
#   a * b → Python looks up a, then b, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from typing import Callable
#   2. def apply_operation(x, y, op) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def apply_operation(x, y, op) → stored in memory, not executed
#   6. def apply_operation(x, y, op) → stored in memory, not executed
#   7. x = ... → assigns value to variable
#   8. y = ... → assigns value to variable
#   9. op = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(apply_operation(...))
#      → calls apply_operation(x, y, op) with x=3, y=14, op=25
#      → 12.1 return op(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (apply_operation) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     x = ... → stored in memory
#     y = ... → stored in memory
#     op = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(apply_operation(...)) → apply_operation() runs first → value passed to print()

"""

"""
Problem 1619:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def categorize(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: < > ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   categorize(?) runs first → then print() outputs the result
#   categorize(0) runs first → then print() outputs the result
#   categorize(5) runs first → then print() outputs the result
#   categorize(value) → each argument is evaluated first, then the function body runs
#   categorize(25) runs first → then print() outputs the result
#   categorize(?) → each argument is evaluated first, then the function body runs
#   categorize(0) → each argument is evaluated first, then the function body runs
#   categorize(5) → each argument is evaluated first, then the function body runs
#   categorize(25) → each argument is evaluated first, then the function body runs
#   x < 0 → evaluated as True or False (determines which branch runs)
#   x == 0 → evaluated as True or False (determines which branch runs)
#   x > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def categorize(value) → stored in memory, not executed
#   2. print(categorize(...), categorize(...), categorize(...))
#      → calls categorize(value) with value=?
#      → 2.1 return 'Negative'
#   3. def categorize(value) → stored in memory, not executed
#   4. def categorize(value) → stored in memory, not executed
#   5. value = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(categorize(...))
#      → calls categorize(value) with value=25
#      → 8.1 return 'Negative'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (categorize) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(categorize(...), categorize(...), categorize(...)) → categorize() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(categorize(...)) → categorize() runs first → value passed to print()

"""

"""
Problem 1620:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def handle_status(code) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   handle_status(200) runs first → then print() outputs the result
#   handle_status(999) runs first → then print() outputs the result
#   handle_status(code) → each argument is evaluated first, then the function body runs
#   handle_status(25) runs first → then print() outputs the result
#   handle_status(200) → each argument is evaluated first, then the function body runs
#   handle_status(999) → each argument is evaluated first, then the function body runs
#   handle_status(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def handle_status(code) → stored in memory, not executed
#   2. print(handle_status(...), handle_status(...))
#      → calls handle_status(code) with code=200
#      → 2.1 return 'OK'
#   3. def handle_status(code) → stored in memory, not executed
#   4. def handle_status(code) → stored in memory, not executed
#   5. code = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(handle_status(...))
#      → calls handle_status(code) with code=25
#      → 8.1 return 'OK'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (handle_status) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(handle_status(...), handle_status(...)) → handle_status() runs first → value passed to print()
#     code = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(handle_status(...)) → handle_status() runs first → value passed to print()

"""

"""
Problem 1621:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#     def __init__(...) → method of Person
#     def name(...) → method of Person
#     def name(...) → method of Person
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Person()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Person → class definition stored
#   2. p = ... → assigns value to variable
#   3. statement
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     p = ... → stored in memory
#     (statement runs)
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))

"""

"""
Problem 1622:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class MyDict → defines a new type
#     def __init__(...) → method of MyDict
#     def __setitem__(...) → method of MyDict
#     def __getitem__(...) → method of MyDict
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: MyDict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class MyDict → class definition stored
#   2. my_dict = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(MyDict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (MyDict) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_dict = ... → stored in memory
#     ? = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(MyDict(...))

"""

"""
Problem 1623:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class MyList → defines a new type
#     def __init__(...) → method of MyList
#     def __delitem__(...) → method of MyList
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: MyList()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class MyList → class definition stored
#   2. my_list = ... → assigns value to variable
#   3. statement
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(MyList(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (MyList) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_list = ... → stored in memory
#     (statement runs)
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(MyList(...))

"""

"""
Problem 1624:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class MyContainer → defines a new type
#     def __init__(...) → method of MyContainer
#     def __contains__(...) → method of MyContainer
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in
# Built-in functions: MyContainer()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   3 in container → evaluated as True or False (determines which branch runs)
#   item in self.items → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class MyContainer → class definition stored
#   2. container = ... → assigns value to variable
#   3. print(?)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. obj = ... → assigns value to variable
#   7. print(obj)
#   8. print(MyContainer(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (MyContainer) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     container = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(MyContainer(...))

"""

"""
Problem 1625:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class CountDown → defines a new type
#     def __init__(...) → method of CountDown
#     def __iter__(...) → method of CountDown
#     def __next__(...) → method of CountDown
#
# Keywords: if/else
# Operators: + <=
# Built-in functions: CountDown, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   self.current <= 0 → evaluated as True or False (determines which branch runs)
#   self.current + 1 → Python looks up self.current, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class CountDown → class definition stored
#   2. counter = ... → assigns value to variable
#   3. print(list(...))
#   4. obj = ... → assigns value to variable
#   5. print(obj)
#   6. print(CountDown(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (CountDown) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     counter = ... → stored in memory
#     print(list(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(CountDown(...))
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1626:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def my_context() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   my_context() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from contextlib import contextmanager
#   2. def my_context() → stored in memory, not executed
#   3. statement
#   4. def my_context() → stored in memory, not executed
#   5. def my_context() → stored in memory, not executed
#   6. print('Entering context')
#   7. evaluates expression
#   8. print('Exiting context')
#   9. print('Entering context')
#   10. evaluates expression
#   11. print('Exiting context')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (my_context) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print('Entering context')
#     (statement runs)
#     print('Exiting context')
#     print('Entering context')
#     (statement runs)
#     print('Exiting context')

"""

"""
Problem 1627:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from pathlib import Path
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1628:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from pathlib import Path
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1629:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from pathlib import Path
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1630:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#   2. from pathlib import Path
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1631:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1632:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. pattern = ... → assigns value to variable
#   3. text = ... → assigns value to variable
#   4. matches = ... → assigns value to variable
#   5. print(matches)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import re
#   9. pattern = ... → assigns value to variable
#   10. text = ... → assigns value to variable
#   11. matches = ... → assigns value to variable
#   12. print(matches)
#   13. import re
#   14. pattern = ... → assigns value to variable
#   15. text = ... → assigns value to variable
#   16. matches = ... → assigns value to variable
#   17. print(matches)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pattern = ... → stored in memory
#     text = ... → stored in memory
#     matches = ... → stored in memory
#     print(matches)
#     pattern = ... → stored in memory
#     text = ... → stored in memory
#     matches = ... → stored in memory
#     print(matches)
#     pattern = ... → stored in memory
#     text = ... → stored in memory
#     matches = ... → stored in memory
#     print(matches)

"""

"""
Problem 1633:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. text = ... → assigns value to variable
#   3. parts = ... → assigns value to variable
#   4. print(parts)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import re
#   8. text = ... → assigns value to variable
#   9. parts = ... → assigns value to variable
#   10. print(parts)
#   11. import re
#   12. text = ... → assigns value to variable
#   13. parts = ... → assigns value to variable
#   14. print(parts)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     parts = ... → stored in memory
#     print(parts)
#     text = ... → stored in memory
#     parts = ... → stored in memory
#     print(parts)
#     text = ... → stored in memory
#     parts = ... → stored in memory
#     print(parts)

"""

"""
Problem 1634:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: OrderedDict, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import OrderedDict
#   2. od = ... → assigns value to variable
#   3. od.move_to_end('a')
#   4. print(list(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     od = ... → stored in memory
#     od.move_to_end('a')
#     print(list(...))

"""

"""
Problem 1635:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: ChainMap, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import ChainMap
#   2. dict1 = ... → assigns value to variable
#   3. dict2 = ... → assigns value to variable
#   4. chain = ... → assigns value to variable
#   5. new_chain = ... → assigns value to variable
#   6. print(dict(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     chain = ... → stored in memory
#     new_chain = ... → stored in memory
#     print(dict(...))

"""

"""
Problem 1636:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Point, namedtuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from collections import namedtuple
#   2. Point = ... → assigns value to variable
#   3. p1 = ... → assigns value to variable
#   4. p2 = ... → assigns value to variable
#   5. print(p1, p2)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. from collections import namedtuple
#   9. Point = ... → assigns value to variable
#   10. p1 = ... → assigns value to variable
#   11. p2 = ... → assigns value to variable
#   12. print(p1, p2)
#   13. from collections import namedtuple
#   14. Point = ... → assigns value to variable
#   15. p1 = ... → assigns value to variable
#   16. p2 = ... → assigns value to variable
#   17. print(p1, p2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     Point = ... → stored in memory
#     p1 = ... → stored in memory
#     p2 = ... → stored in memory
#     print(p1, p2)
#     Point = ... → stored in memory
#     p1 = ... → stored in memory
#     p2 = ... → stored in memory
#     print(p1, p2)
#     Point = ... → stored in memory
#     p1 = ... → stored in memory
#     p2 = ... → stored in memory
#     print(p1, p2)

"""

"""
Problem 1637:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i * j → Python looks up i, then j, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. table = ... → assigns value to variable
#   2. print(table)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. table = ... → assigns value to variable
#   6. print(table)
#   7. table = ... → assigns value to variable
#   8. print(table)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     table = ... → stored in memory
#     print(table)
#     table = ... → stored in memory
#     print(table)
#     table = ... → stored in memory
#     print(table)

"""

"""
Problem 1638:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. strings = ... → assigns value to variable
#   2. unique_chars = ... → assigns value to variable
#   3. print(sorted(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     unique_chars = ... → stored in memory
#     print(sorted(...))

"""

"""
Problem 1639:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. import operator
#   3. numbers = ... → assigns value to variable
#   4. total = ... → assigns value to variable
#   5. print(total)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. from functools import reduce
#   9. import operator
#   10. numbers = ... → assigns value to variable
#   11. total = ... → assigns value to variable
#   12. print(total)
#   13. from functools import reduce
#   14. import operator
#   15. numbers = ... → assigns value to variable
#   16. total = ... → assigns value to variable
#   17. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     total = ... → stored in memory
#     print(total)
#     numbers = ... → stored in memory
#     total = ... → stored in memory
#     print(total)
#     numbers = ... → stored in memory
#     total = ... → stored in memory
#     print(total)

"""

"""
Problem 1640:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. import operator
#   3. numbers = ... → assigns value to variable
#   4. product = ... → assigns value to variable
#   5. print(product)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. from functools import reduce
#   9. import operator
#   10. numbers = ... → assigns value to variable
#   11. product = ... → assigns value to variable
#   12. print(product)
#   13. from functools import reduce
#   14. import operator
#   15. numbers = ... → assigns value to variable
#   16. product = ... → assigns value to variable
#   17. print(product)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     product = ... → stored in memory
#     print(product)
#     numbers = ... → stored in memory
#     product = ... → stored in memory
#     print(product)
#     numbers = ... → stored in memory
#     product = ... → stored in memory
#     print(product)

"""

"""
Problem 1641:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   str1.casefold(...) == str2.casefold(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. str1 = ... → assigns value to variable
#   2. str2 = ... → assigns value to variable
#   3. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     print(?)

"""

"""
Problem 1642:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. encoded = ... → assigns value to variable
#   3. print(encoded)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. encoded = ... → assigns value to variable
#   8. print(encoded)
#   9. text = ... → assigns value to variable
#   10. encoded = ... → assigns value to variable
#   11. print(encoded)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     encoded = ... → stored in memory
#     print(encoded)
#     text = ... → stored in memory
#     encoded = ... → stored in memory
#     print(encoded)
#     text = ... → stored in memory
#     encoded = ... → stored in memory
#     print(encoded)

"""

"""
Problem 1643:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. bytes_data = ... → assigns value to variable
#   2. decoded = ... → assigns value to variable
#   3. print(decoded)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. bytes_data = ... → assigns value to variable
#   7. decoded = ... → assigns value to variable
#   8. print(decoded)
#   9. bytes_data = ... → assigns value to variable
#   10. decoded = ... → assigns value to variable
#   11. print(decoded)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     bytes_data = ... → stored in memory
#     decoded = ... → stored in memory
#     print(decoded)
#     bytes_data = ... → stored in memory
#     decoded = ... → stored in memory
#     print(decoded)
#     bytes_data = ... → stored in memory
#     decoded = ... → stored in memory
#     print(decoded)

"""

"""
Problem 1644:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. my_list = ... → assigns value to variable
#   2. my_list.extend(?)
#   3. print(my_list)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. my_list.extend(?)
#   8. print(my_list)
#   9. my_list = ... → assigns value to variable
#   10. my_list.extend(?)
#   11. print(my_list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_list = ... → stored in memory
#     my_list.extend(?)
#     print(my_list)
#     my_list = ... → stored in memory
#     my_list.extend(?)
#     print(my_list)
#     my_list = ... → stored in memory
#     my_list.extend(?)
#     print(my_list)

"""

"""
Problem 1645:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. my_list = ... → assigns value to variable
#   2. index = ... → assigns value to variable
#   3. print(index)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. index = ... → assigns value to variable
#   8. print(index)
#   9. my_list = ... → assigns value to variable
#   10. index = ... → assigns value to variable
#   11. print(index)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_list = ... → stored in memory
#     index = ... → stored in memory
#     print(index)
#     my_list = ... → stored in memory
#     index = ... → stored in memory
#     print(index)
#     my_list = ... → stored in memory
#     index = ... → stored in memory
#     print(index)

"""

"""
Problem 1646:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. keys = ... → assigns value to variable
#   2. d = ... → assigns value to variable
#   3. print(d)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. keys = ... → assigns value to variable
#   7. d = ... → assigns value to variable
#   8. print(d)
#   9. keys = ... → assigns value to variable
#   10. d = ... → assigns value to variable
#   11. print(d)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     keys = ... → stored in memory
#     d = ... → stored in memory
#     print(d)
#     keys = ... → stored in memory
#     d = ... → stored in memory
#     print(d)
#     keys = ... → stored in memory
#     d = ... → stored in memory
#     print(d)

"""

"""
Problem 1647:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   10 / 2 → Python looks up 10, then 2, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 1648:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   10 / 0 → Python looks up 10, then 0, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 1649:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from contextlib import suppress
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1650:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def subtract_numbers(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + -
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   subtract_numbers(10, 3) → each argument is evaluated first, then the function body runs
#   subtract_numbers(a, b) → each argument is evaluated first, then the function body runs
#   subtract_numbers(20, 30) runs first → then print() outputs the result
#   a - b → Python looks up a, then b, then applies -
#   a + ? → Python looks up a, then ?, then applies +
#   subtract_numbers(20, 30) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def subtract_numbers(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def subtract_numbers(a, b) → stored in memory, not executed
#   5. def subtract_numbers(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(subtract_numbers(...))
#      → calls subtract_numbers(a, b) with a=20, b=30
#      → 10.1 import operator
#      → 10.2 return operator.sub(a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (subtract_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(subtract_numbers(...)) → subtract_numbers() runs first → value passed to print()

"""

"""
Problem 1651:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def divide_numbers(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   divide_numbers(10, 2) → each argument is evaluated first, then the function body runs
#   divide_numbers(a, b) → each argument is evaluated first, then the function body runs
#   divide_numbers(20, 30) runs first → then print() outputs the result
#   a / b → Python looks up a, then b, then applies /
#   a * ? → Python looks up a, then ?, then applies *
#   divide_numbers(20, 30) → each argument is evaluated first, then the function body runs
#   1 / b → Python looks up 1, then b, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def divide_numbers(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def divide_numbers(a, b) → stored in memory, not executed
#   5. def divide_numbers(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(divide_numbers(...))
#      → calls divide_numbers(a, b) with a=20, b=30
#      → 10.1 from fractions import Fraction
#      → 10.2 return float(Fraction(a, b))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (divide_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(divide_numbers(...)) → divide_numbers() runs first → value passed to print()

"""

"""
Problem 1652:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_remainder(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % * - //
# Built-in functions: divmod()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_remainder(10, 3) → each argument is evaluated first, then the function body runs
#   calculate_remainder(a, b) → each argument is evaluated first, then the function body runs
#   calculate_remainder(20, 30) runs first → then print() outputs the result
#   a % b → Python looks up a, then b, then applies %
#   a - ? → Python looks up a, then ?, then applies -
#   calculate_remainder(20, 30) → each argument is evaluated first, then the function body runs
#   b * ? → Python looks up b, then ?, then applies *
#   a // b → Python looks up a, then b, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_remainder(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_remainder(a, b) → stored in memory, not executed
#   5. def calculate_remainder(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_remainder(...))
#      → calls calculate_remainder(a, b) with a=20, b=30
#      → 10.1 return divmod(a, b)[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_remainder) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_remainder(...)) → calculate_remainder() runs first → value passed to print()

"""

"""
Problem 1653:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_power(base, exponent) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_power(2, 3) → each argument is evaluated first, then the function body runs
#   calculate_power(base, exponent) → each argument is evaluated first, then the function body runs
#   calculate_power(10, 25) runs first → then print() outputs the result
#   base ** exponent → Python looks up base, then exponent, then applies **
#   calculate_power(10, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_power(base, exponent) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_power(base, exponent) → stored in memory, not executed
#   5. def calculate_power(base, exponent) → stored in memory, not executed
#   6. base = ... → assigns value to variable
#   7. exponent = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_power(...))
#      → calls calculate_power(base, exponent) with base=10, exponent=25
#      → 10.1 import math
#      → 10.2 return math.pow(base, exponent)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_power) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     base = ... → stored in memory
#     exponent = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_power(...)) → calculate_power() runs first → value passed to print()

"""

"""
Problem 1654:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_circle_area(radius) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_circle_area(5) → each argument is evaluated first, then the function body runs
#   calculate_circle_area(radius) → each argument is evaluated first, then the function body runs
#   calculate_circle_area(14) runs first → then print() outputs the result
#   ? * radius → Python looks up ?, then radius, then applies *
#   calculate_circle_area(14) → each argument is evaluated first, then the function body runs
#   3.14 * radius → Python looks up 3.14, then radius, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_circle_area(radius) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_circle_area(radius) → stored in memory, not executed
#   5. def calculate_circle_area(radius) → stored in memory, not executed
#   6. radius = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_circle_area(...))
#      → calls calculate_circle_area(radius) with radius=14
#      → 9.1 return 3.14 * radius * radius
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_circle_area) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     radius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_circle_area(...)) → calculate_circle_area() runs first → value passed to print()

"""

"""
Problem 1655:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_rectangle_area(length, width) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_rectangle_area(5, 3) → each argument is evaluated first, then the function body runs
#   calculate_rectangle_area(length, width) → each argument is evaluated first, then the function body runs
#   calculate_rectangle_area(25, 25) runs first → then print() outputs the result
#   length * width → Python looks up length, then width, then applies *
#   calculate_rectangle_area(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_rectangle_area(length, width) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_rectangle_area(length, width) → stored in memory, not executed
#   5. def calculate_rectangle_area(length, width) → stored in memory, not executed
#   6. length = ... → assigns value to variable
#   7. width = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_rectangle_area(...))
#      → calls calculate_rectangle_area(length, width) with length=25, width=25
#      → 10.1 return pow(length, width)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_rectangle_area) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_rectangle_area(...)) → calculate_rectangle_area() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1656:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_triangle_area(base, height) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_triangle_area(4, 6) → each argument is evaluated first, then the function body runs
#   calculate_triangle_area(base, height) → each argument is evaluated first, then the function body runs
#   calculate_triangle_area(10, 20) runs first → then print() outputs the result
#   ? * height → Python looks up ?, then height, then applies *
#   calculate_triangle_area(10, 20) → each argument is evaluated first, then the function body runs
#   0.5 * base → Python looks up 0.5, then base, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_triangle_area(base, height) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_triangle_area(base, height) → stored in memory, not executed
#   5. def calculate_triangle_area(base, height) → stored in memory, not executed
#   6. base = ... → assigns value to variable
#   7. height = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_triangle_area(...))
#      → calls calculate_triangle_area(base, height) with base=10, height=20
#      → 10.1 return 0.5 * base * height
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_triangle_area) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     base = ... → stored in memory
#     height = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_triangle_area(...)) → calculate_triangle_area() runs first → value passed to print()

"""

"""
Problem 1657:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_celsius_to_fahrenheit(celsius) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_celsius_to_fahrenheit(25) → each argument is evaluated first, then the function body runs
#   convert_celsius_to_fahrenheit(celsius) → each argument is evaluated first, then the function body runs
#   convert_celsius_to_fahrenheit(25) runs first → then print() outputs the result
#   ? + 32 → Python looks up ?, then 32, then applies +
#   ? / 5 → Python looks up ?, then 5, then applies /
#   celsius * 9 → Python looks up celsius, then 9, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_celsius_to_fahrenheit(celsius) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def convert_celsius_to_fahrenheit(celsius) → stored in memory, not executed
#   5. def convert_celsius_to_fahrenheit(celsius) → stored in memory, not executed
#   6. celsius = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_celsius_to_fahrenheit(...))
#      → calls convert_celsius_to_fahrenheit(celsius) with celsius=25
#      → 9.1 return celsius * 9 / 5 + 32
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_celsius_to_fahrenheit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     celsius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_celsius_to_fahrenheit(...)) → convert_celsius_to_fahrenheit() runs first → value passed to print()

"""

"""
Problem 1658:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_fahrenheit_to_celsius(fahrenheit) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * - /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_fahrenheit_to_celsius(77) → each argument is evaluated first, then the function body runs
#   convert_fahrenheit_to_celsius(fahrenheit) → each argument is evaluated first, then the function body runs
#   convert_fahrenheit_to_celsius(25) runs first → then print() outputs the result
#   ? / 9 → Python looks up ?, then 9, then applies /
#   convert_fahrenheit_to_celsius(25) → each argument is evaluated first, then the function body runs
#   ? * 5 → Python looks up ?, then 5, then applies *
#   fahrenheit - 32 → Python looks up fahrenheit, then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_fahrenheit_to_celsius(fahrenheit) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def convert_fahrenheit_to_celsius(fahrenheit) → stored in memory, not executed
#   5. def convert_fahrenheit_to_celsius(fahrenheit) → stored in memory, not executed
#   6. fahrenheit = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_fahrenheit_to_celsius(...))
#      → calls convert_fahrenheit_to_celsius(fahrenheit) with fahrenheit=25
#      → 9.1 return (fahrenheit - 32) * 5 / 9
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_fahrenheit_to_celsius) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     fahrenheit = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_fahrenheit_to_celsius(...)) → convert_fahrenheit_to_celsius() runs first → value passed to print()

"""

"""
Problem 1659:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_positive(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: > <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_positive(5) runs first → then print() outputs the result
#   is_positive(?) runs first → then print() outputs the result
#   is_positive(number) → each argument is evaluated first, then the function body runs
#   is_positive(25) runs first → then print() outputs the result
#   number > 0 → evaluated as True or False (determines which branch runs)
#   is_positive(5) → each argument is evaluated first, then the function body runs
#   is_positive(?) → each argument is evaluated first, then the function body runs
#   is_positive(25) → each argument is evaluated first, then the function body runs
#   number <= 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_positive(number) → stored in memory, not executed
#   2. print(is_positive(...), is_positive(...))
#      → calls is_positive(number) with number=5
#      → 2.1 return not number <= 0
#   3. def is_positive(number) → stored in memory, not executed
#   4. def is_positive(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(is_positive(...))
#      → calls is_positive(number) with number=25
#      → 8.1 return not number <= 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_positive) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_positive(...), is_positive(...)) → is_positive() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_positive(...)) → is_positive() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1660:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_negative(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: < >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_negative(?) runs first → then print() outputs the result
#   is_negative(3) runs first → then print() outputs the result
#   is_negative(number) → each argument is evaluated first, then the function body runs
#   is_negative(25) runs first → then print() outputs the result
#   number < 0 → evaluated as True or False (determines which branch runs)
#   is_negative(?) → each argument is evaluated first, then the function body runs
#   is_negative(3) → each argument is evaluated first, then the function body runs
#   is_negative(25) → each argument is evaluated first, then the function body runs
#   number >= 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_negative(number) → stored in memory, not executed
#   2. print(is_negative(...), is_negative(...))
#      → calls is_negative(number) with number=?
#      → 2.1 return not number >= 0
#   3. def is_negative(number) → stored in memory, not executed
#   4. def is_negative(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(is_negative(...))
#      → calls is_negative(number) with number=25
#      → 8.1 return not number >= 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_negative) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_negative(...), is_negative(...)) → is_negative() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_negative(...)) → is_negative() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1661:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_first_char(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_first_char('Hello') → each argument is evaluated first, then the function body runs
#   get_first_char(text) → each argument is evaluated first, then the function body runs
#   get_first_char('world') runs first → then print() outputs the result
#   get_first_char('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_first_char(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_first_char(text) → stored in memory, not executed
#   5. def get_first_char(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_first_char(...))
#      → calls get_first_char(text) with text='world'
#      → 9.1 return text[0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_first_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_first_char(...)) → get_first_char() runs first → value passed to print()

"""

"""
Problem 1662:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_last_char(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_last_char('Hello') → each argument is evaluated first, then the function body runs
#   get_last_char(text) → each argument is evaluated first, then the function body runs
#   get_last_char('world') runs first → then print() outputs the result
#   get_last_char('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_last_char(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_last_char(text) → stored in memory, not executed
#   5. def get_last_char(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_last_char(...))
#      → calls get_last_char(text) with text='world'
#      → 9.1 return text[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_last_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_last_char(...)) → get_last_char() runs first → value passed to print()

"""

"""
Problem 1663:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_middle_char(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: //
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_middle_char('Hello') → each argument is evaluated first, then the function body runs
#   get_middle_char(text) → each argument is evaluated first, then the function body runs
#   get_middle_char('world') runs first → then print() outputs the result
#   get_middle_char('world') → each argument is evaluated first, then the function body runs
#   len(...) // 2 → Python looks up len(...), then 2, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_middle_char(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_middle_char(text) → stored in memory, not executed
#   5. def get_middle_char(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_middle_char(...))
#      → calls get_middle_char(text) with text='world'
#      → 9.1 return text[len(text) // 2]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_middle_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_middle_char(...)) → get_middle_char() runs first → value passed to print()

"""

"""
Problem 1664:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def repeat_string(text, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   repeat_string('Hi', 3) → each argument is evaluated first, then the function body runs
#   repeat_string(text, n) → each argument is evaluated first, then the function body runs
#   repeat_string('world', 15) runs first → then print() outputs the result
#   text * n → Python looks up text, then n, then applies *
#   repeat_string('world', 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def repeat_string(text, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def repeat_string(text, n) → stored in memory, not executed
#   5. def repeat_string(text, n) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(repeat_string(...))
#      → calls repeat_string(text, n) with text='world', n=15
#      → 10.1 return pow(text, n)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (repeat_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(repeat_string(...)) → repeat_string() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1665:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def swap_first_last(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: + <=
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   swap_first_last('Hello') → each argument is evaluated first, then the function body runs
#   swap_first_last(text) → each argument is evaluated first, then the function body runs
#   swap_first_last('world') runs first → then print() outputs the result
#   len(...) <= 1 → evaluated as True or False (determines which branch runs)
#   ? + ? → Python looks up ?, then ?, then applies +
#   swap_first_last('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def swap_first_last(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def swap_first_last(text) → stored in memory, not executed
#   5. def swap_first_last(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(swap_first_last(...))
#      → calls swap_first_last(text) with text='world'
#      → 9.1 return text
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (swap_first_last) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(swap_first_last(...)) → swap_first_last() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1666:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_first_char(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_first_char('Hello') → each argument is evaluated first, then the function body runs
#   remove_first_char(text) → each argument is evaluated first, then the function body runs
#   remove_first_char('world') runs first → then print() outputs the result
#   remove_first_char('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_first_char(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_first_char(text) → stored in memory, not executed
#   5. def remove_first_char(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_first_char(...))
#      → calls remove_first_char(text) with text='world'
#      → 9.1 return text[1:]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_first_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_first_char(...)) → remove_first_char() runs first → value passed to print()

"""

"""
Problem 1667:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_last_char(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_last_char('Hello') → each argument is evaluated first, then the function body runs
#   remove_last_char(text) → each argument is evaluated first, then the function body runs
#   remove_last_char('world') runs first → then print() outputs the result
#   remove_last_char('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_last_char(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_last_char(text) → stored in memory, not executed
#   5. def remove_last_char(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_last_char(...))
#      → calls remove_last_char(text) with text='world'
#      → 9.1 return text[:-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_last_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_last_char(...)) → remove_last_char() runs first → value passed to print()

"""

"""
Problem 1668:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_first_word(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_first_word('Hello World Python') → each argument is evaluated first, then the function body runs
#   get_first_word(text) → each argument is evaluated first, then the function body runs
#   get_first_word('world') runs first → then print() outputs the result
#   get_first_word('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_first_word(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_first_word(text) → stored in memory, not executed
#   5. def get_first_word(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_first_word(...))
#      → calls get_first_word(text) with text='world'
#      → 9.1 return text.split()[0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_first_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_first_word(...)) → get_first_word() runs first → value passed to print()

"""

"""
Problem 1669:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_last_word(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_last_word('Hello World Python') → each argument is evaluated first, then the function body runs
#   get_last_word(text) → each argument is evaluated first, then the function body runs
#   get_last_word('world') runs first → then print() outputs the result
#   get_last_word('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_last_word(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_last_word(text) → stored in memory, not executed
#   5. def get_last_word(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_last_word(...))
#      → calls get_last_word(text) with text='world'
#      → 9.1 return text.split()[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_last_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_last_word(...)) → get_last_word() runs first → value passed to print()

"""

"""
Problem 1670:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_characters(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_characters('Hello World') → each argument is evaluated first, then the function body runs
#   count_characters(text) → each argument is evaluated first, then the function body runs
#   count_characters('world') runs first → then print() outputs the result
#   count_characters('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_characters(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_characters(text) → stored in memory, not executed
#   5. def count_characters(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_characters(...))
#      → calls count_characters(text) with text='world'
#      → 9.1 return len(text.replace(' ', ''))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_characters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_characters(...)) → count_characters() runs first → value passed to print()

"""

"""
Problem 1671:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_to_list(my_list, element) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_to_list(?, 4) → each argument is evaluated first, then the function body runs
#   add_to_list(my_list, element) → each argument is evaluated first, then the function body runs
#   add_to_list(?, 25) runs first → then print() outputs the result
#   add_to_list(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_to_list(my_list, element) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def add_to_list(my_list, element) → stored in memory, not executed
#   5. def add_to_list(my_list, element) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. element = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(add_to_list(...))
#      → calls add_to_list(my_list, element) with my_list=[4, 1, 8, 6, 3], element=25
#      → 10.1 return new_list
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_to_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_to_list(...)) → add_to_list() runs first → value passed to print()

"""

"""
Problem 1672:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_first_element(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_first_element(?) → each argument is evaluated first, then the function body runs
#   get_first_element(my_list) → each argument is evaluated first, then the function body runs
#   get_first_element(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_first_element(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_first_element(my_list) → stored in memory, not executed
#   5. def get_first_element(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_first_element(...))
#      → calls get_first_element(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return my_list[0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_first_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_first_element(...)) → get_first_element() runs first → value passed to print()

"""

"""
Problem 1673:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_last_element(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_last_element(?) → each argument is evaluated first, then the function body runs
#   get_last_element(my_list) → each argument is evaluated first, then the function body runs
#   get_last_element(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_last_element(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_last_element(my_list) → stored in memory, not executed
#   5. def get_last_element(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_last_element(...))
#      → calls get_last_element(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return my_list[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_last_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_last_element(...)) → get_last_element() runs first → value passed to print()

"""

"""
Problem 1674:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_middle_element(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: //
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_middle_element(?) → each argument is evaluated first, then the function body runs
#   get_middle_element(my_list) → each argument is evaluated first, then the function body runs
#   get_middle_element(?) runs first → then print() outputs the result
#   len(...) // 2 → Python looks up len(...), then 2, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_middle_element(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_middle_element(my_list) → stored in memory, not executed
#   5. def get_middle_element(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_middle_element(...))
#      → calls get_middle_element(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return my_list[len(my_list) // 2]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_middle_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_middle_element(...)) → get_middle_element() runs first → value passed to print()

"""

"""
Problem 1675:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_first_element(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_first_element(?) → each argument is evaluated first, then the function body runs
#   remove_first_element(my_list) → each argument is evaluated first, then the function body runs
#   remove_first_element(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_first_element(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_first_element(my_list) → stored in memory, not executed
#   5. def remove_first_element(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_first_element(...))
#      → calls remove_first_element(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return my_list[1:]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_first_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_first_element(...)) → remove_first_element() runs first → value passed to print()

"""

"""
Problem 1676:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_last_element(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_last_element(?) → each argument is evaluated first, then the function body runs
#   remove_last_element(my_list) → each argument is evaluated first, then the function body runs
#   remove_last_element(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_last_element(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_last_element(my_list) → stored in memory, not executed
#   5. def remove_last_element(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_last_element(...))
#      → calls remove_last_element(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return my_list[:-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_last_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_last_element(...)) → remove_last_element() runs first → value passed to print()

"""

"""
Problem 1677:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_list_slice(my_list, start, end) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_list_slice(?, 1, 4) → each argument is evaluated first, then the function body runs
#   get_list_slice(my_list, start, end) → each argument is evaluated first, then the function body runs
#   get_list_slice(?, 3, 20) runs first → then print() outputs the result
#   get_list_slice(?, 3, 20) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_list_slice(my_list, start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_list_slice(my_list, start, end) → stored in memory, not executed
#   5. def get_list_slice(my_list, start, end) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. start = ... → assigns value to variable
#   8. end = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_list_slice(...))
#      → calls get_list_slice(my_list, start, end) with my_list=[4, 1, 8, 6, 3], start=3, end=20
#      → 11.1 return my_list[start:end]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_list_slice) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_list_slice(...)) → get_list_slice() runs first → value passed to print()

"""

"""
Problem 1678:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def combine_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   combine_lists(?, ?) → each argument is evaluated first, then the function body runs
#   combine_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   combine_lists(25, ?) runs first → then print() outputs the result
#   list1 + list2 → Python looks up list1, then list2, then applies +
#   combine_lists(25, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def combine_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def combine_lists(list1, list2) → stored in memory, not executed
#   5. def combine_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(combine_lists(...))
#      → calls combine_lists(list1, list2) with list1=25, list2=[4, 1, 8, 6, 3]
#      → 10.1 import operator
#      → 10.2 return operator.add(list1, list2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (combine_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(combine_lists(...)) → combine_lists() runs first → value passed to print()

"""

"""
Problem 1679:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_list(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_list(?) → each argument is evaluated first, then the function body runs
#   multiply_list(numbers) → each argument is evaluated first, then the function body runs
#   multiply_list(?) runs first → then print() outputs the result
#   x * 2 → Python looks up x, then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_list(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def multiply_list(numbers) → stored in memory, not executed
#   5. def multiply_list(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(multiply_list(...))
#      → calls multiply_list(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x * 2 for x in numbers]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_list(...)) → multiply_list() runs first → value passed to print()

"""

"""
Problem 1680:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_one_to_all(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_one_to_all(?) → each argument is evaluated first, then the function body runs
#   add_one_to_all(numbers) → each argument is evaluated first, then the function body runs
#   add_one_to_all(?) runs first → then print() outputs the result
#   x + 1 → Python looks up x, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_one_to_all(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def add_one_to_all(numbers) → stored in memory, not executed
#   5. def add_one_to_all(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(add_one_to_all(...))
#      → calls add_one_to_all(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x + 1 for x in numbers]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_one_to_all) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_one_to_all(...)) → add_one_to_all() runs first → value passed to print()

"""

"""
Problem 1681:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.1 print(i)
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. for i in range(...) → loops over each item
#      → 4.1 print(i)
#   5. for i in range(...) → loops over each item
#      → 5.1 print(i)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1682:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   i % 2 → Python looks up i, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. for i in range(...) → loops over each item
#   5. for i in range(...) → loops over each item
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1683:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   i % 2 → Python looks up i, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. for i in range(...) → loops over each item
#   5. for i in range(...) → loops over each item
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1684:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: *
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   5 * i → Python looks up 5, then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.1 print(?)
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. for i in range(...) → loops over each item
#      → 4.1 print(?)
#   5. for i in range(...) → loops over each item
#      → 5.1 print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1685:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. for char in text → loops over each item
#      → 2.1 print(char)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. for char in text → loops over each item
#      → 6.1 print(char)
#   7. text = ... → assigns value to variable
#   8. for char in text → loops over each item
#      → 8.1 print(char)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     for char in text → runs body once per item
#     text = ... → stored in memory
#     for char in text → runs body once per item
#     text = ... → stored in memory
#     for char in text → runs body once per item
#
#   Loop: takes one item from text each round, runs body, repeats until done

"""

"""
Problem 1686:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. my_list = ... → assigns value to variable
#   2. for element in my_list → loops over each item
#      → 2.1 print(element)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. my_list = ... → assigns value to variable
#   6. for element in my_list → loops over each item
#      → 6.1 print(element)
#   7. my_list = ... → assigns value to variable
#   8. for element in my_list → loops over each item
#      → 8.1 print(element)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_list = ... → stored in memory
#     for element in my_list → runs body once per item
#     my_list = ... → stored in memory
#     for element in my_list → runs body once per item
#     my_list = ... → stored in memory
#     for element in my_list → runs body once per item
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1687:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. total = ... → assigns value to variable
#   2. for i in range(...) → loops over each item
#   3. print(total)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. total = ... → assigns value to variable
#   7. for i in range(...) → loops over each item
#   8. print(total)
#   9. total = ... → assigns value to variable
#   10. for i in range(...) → loops over each item
#   11. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     total = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(total)
#     total = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(total)
#     total = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(total)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1688:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. product = ... → assigns value to variable
#   2. for i in range(...) → loops over each item
#   3. print(product)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. product = ... → assigns value to variable
#   7. for i in range(...) → loops over each item
#   8. print(product)
#   9. product = ... → assigns value to variable
#   10. for i in range(...) → loops over each item
#   11. print(product)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     product = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(product)
#     product = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(product)
#     product = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(product)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1689:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.1 print(i)
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. for i in range(...) → loops over each item
#      → 4.1 print(i)
#   5. for i in range(...) → loops over each item
#      → 5.1 print(i)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1690:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   i % 3 → Python looks up i, then 3, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. for i in range(...) → loops over each item
#   5. for i in range(...) → loops over each item
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1691:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i <= 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1692:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i <= 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. total = ... → assigns value to variable
#   2. i = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(total)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. total = ... → assigns value to variable
#   8. i = ... → assigns value to variable
#   9. while ... → repeats as long as condition is true
#   10. print(total)
#   11. total = ... → assigns value to variable
#   12. i = ... → assigns value to variable
#   13. while ... → repeats as long as condition is true
#   14. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     total = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#     total = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#     total = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1693:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i <= 20 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1694:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i >= 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1695:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_greater(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: > <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_greater(5, 3) runs first → then print() outputs the result
#   check_greater(2, 4) runs first → then print() outputs the result
#   check_greater(a, b) → each argument is evaluated first, then the function body runs
#   check_greater(20, 30) runs first → then print() outputs the result
#   a > b → evaluated as True or False (determines which branch runs)
#   check_greater(5, 3) → each argument is evaluated first, then the function body runs
#   check_greater(2, 4) → each argument is evaluated first, then the function body runs
#   check_greater(20, 30) → each argument is evaluated first, then the function body runs
#   a <= b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_greater(a, b) → stored in memory, not executed
#   2. print(check_greater(...), check_greater(...))
#      → calls check_greater(a, b) with a=5, b=3
#      → 2.1 return not a <= b
#   3. def check_greater(a, b) → stored in memory, not executed
#   4. def check_greater(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_greater(...))
#      → calls check_greater(a, b) with a=20, b=30
#      → 9.1 return not a <= b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_greater) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_greater(...), check_greater(...)) → check_greater() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_greater(...)) → check_greater() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1696:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_less(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: < >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_less(3, 5) runs first → then print() outputs the result
#   check_less(5, 3) runs first → then print() outputs the result
#   check_less(a, b) → each argument is evaluated first, then the function body runs
#   check_less(20, 30) runs first → then print() outputs the result
#   a < b → evaluated as True or False (determines which branch runs)
#   check_less(3, 5) → each argument is evaluated first, then the function body runs
#   check_less(5, 3) → each argument is evaluated first, then the function body runs
#   check_less(20, 30) → each argument is evaluated first, then the function body runs
#   a >= b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_less(a, b) → stored in memory, not executed
#   2. print(check_less(...), check_less(...))
#      → calls check_less(a, b) with a=3, b=5
#      → 2.1 return not a >= b
#   3. def check_less(a, b) → stored in memory, not executed
#   4. def check_less(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_less(...))
#      → calls check_less(a, b) with a=20, b=30
#      → 9.1 return not a >= b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_less) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_less(...), check_less(...)) → check_less() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_less(...)) → check_less() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1697:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_equal(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_equal(5, 5) runs first → then print() outputs the result
#   check_equal(5, 3) runs first → then print() outputs the result
#   check_equal(a, b) → each argument is evaluated first, then the function body runs
#   check_equal(20, 30) runs first → then print() outputs the result
#   a == b → evaluated as True or False (determines which branch runs)
#   check_equal(5, 5) → each argument is evaluated first, then the function body runs
#   check_equal(5, 3) → each argument is evaluated first, then the function body runs
#   check_equal(20, 30) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_equal(a, b) → stored in memory, not executed
#   2. print(check_equal(...), check_equal(...))
#      → calls check_equal(a, b) with a=5, b=5
#      → 2.1 return a == b
#   3. def check_equal(a, b) → stored in memory, not executed
#   4. def check_equal(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_equal(...))
#      → calls check_equal(a, b) with a=20, b=30
#      → 9.1 return a == b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_equal) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_equal(...), check_equal(...)) → check_equal() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_equal(...)) → check_equal() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1698:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_not_equal(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: != ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_not_equal(5, 3) runs first → then print() outputs the result
#   check_not_equal(5, 5) runs first → then print() outputs the result
#   check_not_equal(a, b) → each argument is evaluated first, then the function body runs
#   check_not_equal(20, 30) runs first → then print() outputs the result
#   a != b → evaluated as True or False (determines which branch runs)
#   check_not_equal(5, 3) → each argument is evaluated first, then the function body runs
#   check_not_equal(5, 5) → each argument is evaluated first, then the function body runs
#   check_not_equal(20, 30) → each argument is evaluated first, then the function body runs
#   a == b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_not_equal(a, b) → stored in memory, not executed
#   2. print(check_not_equal(...), check_not_equal(...))
#      → calls check_not_equal(a, b) with a=5, b=3
#      → 2.1 return not a == b
#   3. def check_not_equal(a, b) → stored in memory, not executed
#   4. def check_not_equal(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_not_equal(...))
#      → calls check_not_equal(a, b) with a=20, b=30
#      → 9.1 return not a == b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_not_equal) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_not_equal(...), check_not_equal(...)) → check_not_equal() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_not_equal(...)) → check_not_equal() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1699:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_greater_equal(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: < >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_greater_equal(5, 5) runs first → then print() outputs the result
#   check_greater_equal(3, 5) runs first → then print() outputs the result
#   check_greater_equal(a, b) → each argument is evaluated first, then the function body runs
#   check_greater_equal(20, 30) runs first → then print() outputs the result
#   a >= b → evaluated as True or False (determines which branch runs)
#   check_greater_equal(5, 5) → each argument is evaluated first, then the function body runs
#   check_greater_equal(3, 5) → each argument is evaluated first, then the function body runs
#   check_greater_equal(20, 30) → each argument is evaluated first, then the function body runs
#   a < b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_greater_equal(a, b) → stored in memory, not executed
#   2. print(check_greater_equal(...), check_greater_equal(...))
#      → calls check_greater_equal(a, b) with a=5, b=5
#      → 2.1 return not a < b
#   3. def check_greater_equal(a, b) → stored in memory, not executed
#   4. def check_greater_equal(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_greater_equal(...))
#      → calls check_greater_equal(a, b) with a=20, b=30
#      → 9.1 return not a < b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_greater_equal) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_greater_equal(...), check_greater_equal(...)) → check_greater_equal() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_greater_equal(...)) → check_greater_equal() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1700:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_less_equal(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: > <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_less_equal(3, 5) runs first → then print() outputs the result
#   check_less_equal(5, 3) runs first → then print() outputs the result
#   check_less_equal(a, b) → each argument is evaluated first, then the function body runs
#   check_less_equal(20, 30) runs first → then print() outputs the result
#   a <= b → evaluated as True or False (determines which branch runs)
#   check_less_equal(3, 5) → each argument is evaluated first, then the function body runs
#   check_less_equal(5, 3) → each argument is evaluated first, then the function body runs
#   check_less_equal(20, 30) → each argument is evaluated first, then the function body runs
#   a > b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_less_equal(a, b) → stored in memory, not executed
#   2. print(check_less_equal(...), check_less_equal(...))
#      → calls check_less_equal(a, b) with a=3, b=5
#      → 2.1 return not a > b
#   3. def check_less_equal(a, b) → stored in memory, not executed
#   4. def check_less_equal(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_less_equal(...))
#      → calls check_less_equal(a, b) with a=20, b=30
#      → 9.1 return not a > b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_less_equal) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_less_equal(...), check_less_equal(...)) → check_less_equal() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_less_equal(...)) → check_less_equal() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1701:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_absolute_value(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: abs()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_absolute_value(?) runs first → then print() outputs the result
#   get_absolute_value(5) runs first → then print() outputs the result
#   get_absolute_value(number) → each argument is evaluated first, then the function body runs
#   get_absolute_value(25) runs first → then print() outputs the result
#   get_absolute_value(?) → each argument is evaluated first, then the function body runs
#   get_absolute_value(5) → each argument is evaluated first, then the function body runs
#   get_absolute_value(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_absolute_value(number) → stored in memory, not executed
#   2. print(get_absolute_value(...), get_absolute_value(...))
#      → calls get_absolute_value(number) with number=?
#      → 2.1 return abs(number)
#   3. def get_absolute_value(number) → stored in memory, not executed
#   4. def get_absolute_value(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(get_absolute_value(...))
#      → calls get_absolute_value(number) with number=25
#      → 8.1 return abs(number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_absolute_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(get_absolute_value(...), get_absolute_value(...)) → get_absolute_value() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_absolute_value(...)) → get_absolute_value() runs first → value passed to print()

"""

"""
Problem 1702:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def round_number(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   round_number(3.7) runs first → then print() outputs the result
#   round_number(3.2) runs first → then print() outputs the result
#   round_number(number) → each argument is evaluated first, then the function body runs
#   round_number(25) runs first → then print() outputs the result
#   round_number(3.7) → each argument is evaluated first, then the function body runs
#   round_number(3.2) → each argument is evaluated first, then the function body runs
#   round_number(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def round_number(number) → stored in memory, not executed
#   2. print(round_number(...), round_number(...))
#      → calls round_number(number) with number=3.7
#      → 2.1 return round(number)
#   3. def round_number(number) → stored in memory, not executed
#   4. def round_number(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(round_number(...))
#      → calls round_number(number) with number=25
#      → 8.1 return round(number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (round_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(round_number(...), round_number(...)) → round_number() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(round_number(...)) → round_number() runs first → value passed to print()

"""

"""
Problem 1703:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_maximum(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_maximum(10, 5, 8) → each argument is evaluated first, then the function body runs
#   get_maximum(a, b, c) → each argument is evaluated first, then the function body runs
#   get_maximum(20, 30, ?) runs first → then print() outputs the result
#   get_maximum(20, 30, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_maximum(a, b, c) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_maximum(a, b, c) → stored in memory, not executed
#   5. def get_maximum(a, b, c) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. c = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_maximum(...))
#      → calls get_maximum(a, b, c) with a=20, b=30, c=[4, 1, 8, 6, 3]
#      → 11.1 return max(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_maximum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_maximum(...)) → get_maximum() runs first → value passed to print()

"""

"""
Problem 1704:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_minimum(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_minimum(10, 5, 8) → each argument is evaluated first, then the function body runs
#   get_minimum(a, b, c) → each argument is evaluated first, then the function body runs
#   get_minimum(20, 30, ?) runs first → then print() outputs the result
#   get_minimum(20, 30, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_minimum(a, b, c) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_minimum(a, b, c) → stored in memory, not executed
#   5. def get_minimum(a, b, c) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. c = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_minimum(...))
#      → calls get_minimum(a, b, c) with a=20, b=30, c=[4, 1, 8, 6, 3]
#      → 11.1 return min(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_minimum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_minimum(...)) → get_minimum() runs first → value passed to print()

"""

"""
Problem 1705:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_divisible(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_divisible(10, 2) runs first → then print() outputs the result
#   check_divisible(10, 3) runs first → then print() outputs the result
#   check_divisible(a, b) → each argument is evaluated first, then the function body runs
#   check_divisible(20, 30) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   check_divisible(10, 2) → each argument is evaluated first, then the function body runs
#   check_divisible(10, 3) → each argument is evaluated first, then the function body runs
#   check_divisible(20, 30) → each argument is evaluated first, then the function body runs
#   a % b → Python looks up a, then b, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_divisible(a, b) → stored in memory, not executed
#   2. print(check_divisible(...), check_divisible(...))
#      → calls check_divisible(a, b) with a=10, b=2
#      → 2.1 return a % b == 0
#   3. def check_divisible(a, b) → stored in memory, not executed
#   4. def check_divisible(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_divisible(...))
#      → calls check_divisible(a, b) with a=20, b=30
#      → 9.1 return a % b == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_divisible) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_divisible(...), check_divisible(...)) → check_divisible() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_divisible(...)) → check_divisible() runs first → value passed to print()

"""

"""
Problem 1706:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_string(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str, type()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_string(123) → each argument is evaluated first, then the function body runs
#   convert_to_string(number) → each argument is evaluated first, then the function body runs
#   convert_to_string(25) runs first → then print() outputs the result
#   convert_to_string(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_string(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result, type(...))
#   4. def convert_to_string(number) → stored in memory, not executed
#   5. def convert_to_string(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_to_string(...))
#      → calls convert_to_string(number) with number=25
#      → 9.1 return str(number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result, type(...))
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_string(...)) → convert_to_string() runs first → value passed to print()

"""

"""
Problem 1707:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_integer(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: int, type()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_integer('123') → each argument is evaluated first, then the function body runs
#   convert_to_integer(text) → each argument is evaluated first, then the function body runs
#   convert_to_integer('world') runs first → then print() outputs the result
#   convert_to_integer('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_integer(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result, type(...))
#   4. def convert_to_integer(text) → stored in memory, not executed
#   5. def convert_to_integer(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_to_integer(...))
#      → calls convert_to_integer(text) with text='world'
#      → 9.1 return int(text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_integer) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result, type(...))
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_integer(...)) → convert_to_integer() runs first → value passed to print()

"""

"""
Problem 1708:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_float(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: float, type()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_float('3.14') → each argument is evaluated first, then the function body runs
#   convert_to_float(text) → each argument is evaluated first, then the function body runs
#   convert_to_float('world') runs first → then print() outputs the result
#   convert_to_float('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_float(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result, type(...))
#   4. def convert_to_float(text) → stored in memory, not executed
#   5. def convert_to_float(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_to_float(...))
#      → calls convert_to_float(text) with text='world'
#      → 9.1 return float(text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_float) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result, type(...))
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_float(...)) → convert_to_float() runs first → value passed to print()

"""

"""
Problem 1709:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_type(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str, type()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_type(5) runs first → then print() outputs the result
#   get_type('hello') runs first → then print() outputs the result
#   get_type(value) → each argument is evaluated first, then the function body runs
#   get_type(25) runs first → then print() outputs the result
#   get_type(5) → each argument is evaluated first, then the function body runs
#   get_type('hello') → each argument is evaluated first, then the function body runs
#   get_type(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_type(value) → stored in memory, not executed
#   2. print(get_type(...), get_type(...))
#      → calls get_type(value) with value=5
#      → 2.1 return str(type(value).__name__)
#   3. def get_type(value) → stored in memory, not executed
#   4. def get_type(value) → stored in memory, not executed
#   5. value = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(get_type(...))
#      → calls get_type(value) with value=25
#      → 8.1 return str(type(value).__name__)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_type) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(get_type(...), get_type(...)) → get_type() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_type(...)) → get_type() runs first → value passed to print()

"""

"""
Problem 1710:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_is_integer(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_is_integer(5) runs first → then print() outputs the result
#   check_is_integer('5') runs first → then print() outputs the result
#   check_is_integer(value) → each argument is evaluated first, then the function body runs
#   check_is_integer(25) runs first → then print() outputs the result
#   check_is_integer(5) → each argument is evaluated first, then the function body runs
#   check_is_integer('5') → each argument is evaluated first, then the function body runs
#   check_is_integer(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_is_integer(value) → stored in memory, not executed
#   2. print(check_is_integer(...), check_is_integer(...))
#      → calls check_is_integer(value) with value=5
#      → 2.1 return isinstance(value, int)
#   3. def check_is_integer(value) → stored in memory, not executed
#   4. def check_is_integer(value) → stored in memory, not executed
#   5. value = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_is_integer(...))
#      → calls check_is_integer(value) with value=25
#      → 8.1 return isinstance(value, int)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_is_integer) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_is_integer(...), check_is_integer(...)) → check_is_integer() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_is_integer(...)) → check_is_integer() runs first → value passed to print()

"""

"""
Problem 1711:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_is_string(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_is_string('hello') runs first → then print() outputs the result
#   check_is_string(5) runs first → then print() outputs the result
#   check_is_string(value) → each argument is evaluated first, then the function body runs
#   check_is_string(25) runs first → then print() outputs the result
#   check_is_string('hello') → each argument is evaluated first, then the function body runs
#   check_is_string(5) → each argument is evaluated first, then the function body runs
#   check_is_string(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_is_string(value) → stored in memory, not executed
#   2. print(check_is_string(...), check_is_string(...))
#      → calls check_is_string(value) with value='hello'
#      → 2.1 return isinstance(value, str)
#   3. def check_is_string(value) → stored in memory, not executed
#   4. def check_is_string(value) → stored in memory, not executed
#   5. value = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_is_string(...))
#      → calls check_is_string(value) with value=25
#      → 8.1 return isinstance(value, str)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_is_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_is_string(...), check_is_string(...)) → check_is_string() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_is_string(...)) → check_is_string() runs first → value passed to print()

"""

"""
Problem 1712:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_is_list(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_is_list(?) runs first → then print() outputs the result
#   check_is_list('hello') runs first → then print() outputs the result
#   check_is_list(value) → each argument is evaluated first, then the function body runs
#   check_is_list(25) runs first → then print() outputs the result
#   check_is_list(?) → each argument is evaluated first, then the function body runs
#   check_is_list('hello') → each argument is evaluated first, then the function body runs
#   check_is_list(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_is_list(value) → stored in memory, not executed
#   2. print(check_is_list(...), check_is_list(...))
#      → calls check_is_list(value) with value=[1, 2, 3]
#      → 2.1 return isinstance(value, list)
#   3. def check_is_list(value) → stored in memory, not executed
#   4. def check_is_list(value) → stored in memory, not executed
#   5. value = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_is_list(...))
#      → calls check_is_list(value) with value=25
#      → 8.1 return isinstance(value, list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_is_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_is_list(...), check_is_list(...)) → check_is_list() runs first → value passed to print()
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_is_list(...)) → check_is_list() runs first → value passed to print()

"""

"""
Problem 1713:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_greeting(name) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_greeting('Alice') → each argument is evaluated first, then the function body runs
#   format_greeting(name) → each argument is evaluated first, then the function body runs
#   format_greeting('Bob') runs first → then print() outputs the result
#   'Hello, %s!' % name → Python looks up 'Hello, %s!', then name, then applies %
#   format_greeting('Bob') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_greeting(name) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_greeting(name) → stored in memory, not executed
#   5. def format_greeting(name) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(format_greeting(...))
#      → calls format_greeting(name) with name='Bob'
#      → 9.1 return 'Hello, %s!' % name
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_greeting) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_greeting(...)) → format_greeting() runs first → value passed to print()

"""

"""
Problem 1714:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_age_message(name, age) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_age_message('Bob', 25) → each argument is evaluated first, then the function body runs
#   format_age_message(name, age) → each argument is evaluated first, then the function body runs
#   format_age_message('Bob', 25) runs first → then print() outputs the result
#   '%s is %s years old.' % ? → Python looks up '%s is %s years old.', then ?, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_age_message(name, age) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_age_message(name, age) → stored in memory, not executed
#   5. def format_age_message(name, age) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. age = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(format_age_message(...))
#      → calls format_age_message(name, age) with name='Bob', age=25
#      → 10.1 return '%s is %s years old.' % (name, age)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_age_message) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     name = ... → stored in memory
#     age = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_age_message(...)) → format_age_message() runs first → value passed to print()

"""

"""
Problem 1715:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_calculation(num1, num2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_calculation(5, 3) → each argument is evaluated first, then the function body runs
#   format_calculation(num1, num2) → each argument is evaluated first, then the function body runs
#   format_calculation(20, 30) runs first → then print() outputs the result
#   format_calculation(20, 30) → each argument is evaluated first, then the function body runs
#   num1 + num2 → Python looks up num1, then num2, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_calculation(num1, num2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_calculation(num1, num2) → stored in memory, not executed
#   5. def format_calculation(num1, num2) → stored in memory, not executed
#   6. num1 = ... → assigns value to variable
#   7. num2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(format_calculation(...))
#      → calls format_calculation(num1, num2) with num1=20, num2=30
#      → 10.1 return f'{num1} + {num2} = {num1 + num2}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_calculation) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     num1 = ... → stored in memory
#     num2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_calculation(...)) → format_calculation() runs first → value passed to print()

"""

"""
Problem 1716:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_perimeter_rectangle(length, width) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_perimeter_rectangle(5, 3) → each argument is evaluated first, then the function body runs
#   calculate_perimeter_rectangle(length, width) → each argument is evaluated first, then the function body runs
#   calculate_perimeter_rectangle(25, 25) runs first → then print() outputs the result
#   2 * ? → Python looks up 2, then ?, then applies *
#   calculate_perimeter_rectangle(25, 25) → each argument is evaluated first, then the function body runs
#   length + width → Python looks up length, then width, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_perimeter_rectangle(length, width) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_perimeter_rectangle(length, width) → stored in memory, not executed
#   5. def calculate_perimeter_rectangle(length, width) → stored in memory, not executed
#   6. length = ... → assigns value to variable
#   7. width = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_perimeter_rectangle(...))
#      → calls calculate_perimeter_rectangle(length, width) with length=25, width=25
#      → 10.1 return 2 * (length + width)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_perimeter_rectangle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_perimeter_rectangle(...)) → calculate_perimeter_rectangle() runs first → value passed to print()

"""

"""
Problem 1717:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_perimeter_square(side) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_perimeter_square(5) → each argument is evaluated first, then the function body runs
#   calculate_perimeter_square(side) → each argument is evaluated first, then the function body runs
#   calculate_perimeter_square(25) runs first → then print() outputs the result
#   4 * side → Python looks up 4, then side, then applies *
#   calculate_perimeter_square(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_perimeter_square(side) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_perimeter_square(side) → stored in memory, not executed
#   5. def calculate_perimeter_square(side) → stored in memory, not executed
#   6. side = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_perimeter_square(...))
#      → calls calculate_perimeter_square(side) with side=25
#      → 9.1 return pow(4, side)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_perimeter_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     side = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_perimeter_square(...)) → calculate_perimeter_square() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1718:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_circle_circumference(radius) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_circle_circumference(5) → each argument is evaluated first, then the function body runs
#   calculate_circle_circumference(radius) → each argument is evaluated first, then the function body runs
#   calculate_circle_circumference(14) runs first → then print() outputs the result
#   ? * radius → Python looks up ?, then radius, then applies *
#   calculate_circle_circumference(14) → each argument is evaluated first, then the function body runs
#   2 * 3.14 → Python looks up 2, then 3.14, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_circle_circumference(radius) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_circle_circumference(radius) → stored in memory, not executed
#   5. def calculate_circle_circumference(radius) → stored in memory, not executed
#   6. radius = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_circle_circumference(...))
#      → calls calculate_circle_circumference(radius) with radius=14
#      → 9.1 return 2 * 3.14 * radius
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_circle_circumference) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     radius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_circle_circumference(...)) → calculate_circle_circumference() runs first → value passed to print()

"""

"""
Problem 1719:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_km_to_miles(km) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_km_to_miles(10) → each argument is evaluated first, then the function body runs
#   convert_km_to_miles(km) → each argument is evaluated first, then the function body runs
#   convert_km_to_miles(25) runs first → then print() outputs the result
#   km * 0.621371 → Python looks up km, then 0.621371, then applies *
#   convert_km_to_miles(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_km_to_miles(km) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def convert_km_to_miles(km) → stored in memory, not executed
#   5. def convert_km_to_miles(km) → stored in memory, not executed
#   6. km = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_km_to_miles(...))
#      → calls convert_km_to_miles(km) with km=25
#      → 9.1 return km * 0.621371
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_km_to_miles) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     km = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_km_to_miles(...)) → convert_km_to_miles() runs first → value passed to print()

"""

"""
Problem 1720:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_miles_to_km(miles) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: /
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_miles_to_km(6.21371) → each argument is evaluated first, then the function body runs
#   convert_miles_to_km(miles) → each argument is evaluated first, then the function body runs
#   convert_miles_to_km(25) runs first → then print() outputs the result
#   miles / 0.621371 → Python looks up miles, then 0.621371, then applies /
#   convert_miles_to_km(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_miles_to_km(miles) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(round(...))
#   4. def convert_miles_to_km(miles) → stored in memory, not executed
#   5. def convert_miles_to_km(miles) → stored in memory, not executed
#   6. miles = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_miles_to_km(...))
#      → calls convert_miles_to_km(miles) with miles=25
#      → 9.1 return miles / 0.621371
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_miles_to_km) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(round(...))
#     miles = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_miles_to_km(...)) → convert_miles_to_km() runs first → value passed to print()

"""

"""
Problem 1721:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_kg_to_pounds(kg) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_kg_to_pounds(10) → each argument is evaluated first, then the function body runs
#   convert_kg_to_pounds(kg) → each argument is evaluated first, then the function body runs
#   convert_kg_to_pounds(25) runs first → then print() outputs the result
#   kg * 2.20462 → Python looks up kg, then 2.20462, then applies *
#   convert_kg_to_pounds(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_kg_to_pounds(kg) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def convert_kg_to_pounds(kg) → stored in memory, not executed
#   5. def convert_kg_to_pounds(kg) → stored in memory, not executed
#   6. kg = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_kg_to_pounds(...))
#      → calls convert_kg_to_pounds(kg) with kg=25
#      → 9.1 return kg * 2.20462
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_kg_to_pounds) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     kg = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_kg_to_pounds(...)) → convert_kg_to_pounds() runs first → value passed to print()

"""

"""
Problem 1722:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_pounds_to_kg(pounds) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: /
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_pounds_to_kg(22.0462) → each argument is evaluated first, then the function body runs
#   convert_pounds_to_kg(pounds) → each argument is evaluated first, then the function body runs
#   convert_pounds_to_kg(25) runs first → then print() outputs the result
#   pounds / 2.20462 → Python looks up pounds, then 2.20462, then applies /
#   convert_pounds_to_kg(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_pounds_to_kg(pounds) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(round(...))
#   4. def convert_pounds_to_kg(pounds) → stored in memory, not executed
#   5. def convert_pounds_to_kg(pounds) → stored in memory, not executed
#   6. pounds = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_pounds_to_kg(...))
#      → calls convert_pounds_to_kg(pounds) with pounds=25
#      → 9.1 return pounds / 2.20462
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_pounds_to_kg) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(round(...))
#     pounds = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_pounds_to_kg(...)) → convert_pounds_to_kg() runs first → value passed to print()

"""

"""
Problem 1723:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_vowel(char) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_vowel('a') runs first → then print() outputs the result
#   check_vowel('b') runs first → then print() outputs the result
#   check_vowel(char) → each argument is evaluated first, then the function body runs
#   check_vowel('z') runs first → then print() outputs the result
#   char.lower(...) in 'aeiou' → evaluated as True or False (determines which branch runs)
#   check_vowel('a') → each argument is evaluated first, then the function body runs
#   check_vowel('b') → each argument is evaluated first, then the function body runs
#   check_vowel('z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_vowel(char) → stored in memory, not executed
#   2. print(check_vowel(...), check_vowel(...))
#      → calls check_vowel(char) with char='a'
#      → 2.1 return char.lower() in 'aeiou'
#   3. def check_vowel(char) → stored in memory, not executed
#   4. def check_vowel(char) → stored in memory, not executed
#   5. char = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_vowel(...))
#      → calls check_vowel(char) with char='z'
#      → 8.1 return char.lower() in 'aeiou'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_vowel) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_vowel(...), check_vowel(...)) → check_vowel() runs first → value passed to print()
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_vowel(...)) → check_vowel() runs first → value passed to print()

"""

"""
Problem 1724:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_consonant(char) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: and not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_consonant('b') runs first → then print() outputs the result
#   check_consonant('a') runs first → then print() outputs the result
#   check_consonant(char) → each argument is evaluated first, then the function body runs
#   check_consonant('z') runs first → then print() outputs the result
#   check_consonant('b') → each argument is evaluated first, then the function body runs
#   check_consonant('a') → each argument is evaluated first, then the function body runs
#   check_consonant('z') → each argument is evaluated first, then the function body runs
#   char.lower(...) not in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_consonant(char) → stored in memory, not executed
#   2. print(check_consonant(...), check_consonant(...))
#      → calls check_consonant(char) with char='b'
#      → 2.1 return char.isalpha() and char.lower() not in 'aeiou'
#   3. def check_consonant(char) → stored in memory, not executed
#   4. def check_consonant(char) → stored in memory, not executed
#   5. char = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_consonant(...))
#      → calls check_consonant(char) with char='z'
#      → 8.1 return char.isalpha() and char.lower() not in 'aeiou'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_consonant) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_consonant(...), check_consonant(...)) → check_consonant() runs first → value passed to print()
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_consonant(...)) → check_consonant() runs first → value passed to print()

"""

"""
Problem 1725:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_digit(char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_digit('5') runs first → then print() outputs the result
#   check_digit('a') runs first → then print() outputs the result
#   check_digit(char) → each argument is evaluated first, then the function body runs
#   check_digit('z') runs first → then print() outputs the result
#   check_digit('5') → each argument is evaluated first, then the function body runs
#   check_digit('a') → each argument is evaluated first, then the function body runs
#   check_digit('z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_digit(char) → stored in memory, not executed
#   2. print(check_digit(...), check_digit(...))
#      → calls check_digit(char) with char='5'
#      → 2.1 return char.isdigit()
#   3. def check_digit(char) → stored in memory, not executed
#   4. def check_digit(char) → stored in memory, not executed
#   5. char = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_digit(...))
#      → calls check_digit(char) with char='z'
#      → 8.1 return char.isdigit()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_digit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_digit(...), check_digit(...)) → check_digit() runs first → value passed to print()
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_digit(...)) → check_digit() runs first → value passed to print()

"""

"""
Problem 1726:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_letter(char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_letter('a') runs first → then print() outputs the result
#   check_letter('5') runs first → then print() outputs the result
#   check_letter(char) → each argument is evaluated first, then the function body runs
#   check_letter('z') runs first → then print() outputs the result
#   check_letter('a') → each argument is evaluated first, then the function body runs
#   check_letter('5') → each argument is evaluated first, then the function body runs
#   check_letter('z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_letter(char) → stored in memory, not executed
#   2. print(check_letter(...), check_letter(...))
#      → calls check_letter(char) with char='a'
#      → 2.1 return char.isalpha()
#   3. def check_letter(char) → stored in memory, not executed
#   4. def check_letter(char) → stored in memory, not executed
#   5. char = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_letter(...))
#      → calls check_letter(char) with char='z'
#      → 8.1 return char.isalpha()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_letter(...), check_letter(...)) → check_letter() runs first → value passed to print()
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_letter(...)) → check_letter() runs first → value passed to print()

"""

"""
Problem 1727:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_uppercase(char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_uppercase('A') runs first → then print() outputs the result
#   check_uppercase('a') runs first → then print() outputs the result
#   check_uppercase(char) → each argument is evaluated first, then the function body runs
#   check_uppercase('z') runs first → then print() outputs the result
#   check_uppercase('A') → each argument is evaluated first, then the function body runs
#   check_uppercase('a') → each argument is evaluated first, then the function body runs
#   check_uppercase('z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_uppercase(char) → stored in memory, not executed
#   2. print(check_uppercase(...), check_uppercase(...))
#      → calls check_uppercase(char) with char='A'
#      → 2.1 return char.isupper()
#   3. def check_uppercase(char) → stored in memory, not executed
#   4. def check_uppercase(char) → stored in memory, not executed
#   5. char = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_uppercase(...))
#      → calls check_uppercase(char) with char='z'
#      → 8.1 return char.isupper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_uppercase(...), check_uppercase(...)) → check_uppercase() runs first → value passed to print()
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_uppercase(...)) → check_uppercase() runs first → value passed to print()

"""

"""
Problem 1728:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_lowercase(char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_lowercase('a') runs first → then print() outputs the result
#   check_lowercase('A') runs first → then print() outputs the result
#   check_lowercase(char) → each argument is evaluated first, then the function body runs
#   check_lowercase('z') runs first → then print() outputs the result
#   check_lowercase('a') → each argument is evaluated first, then the function body runs
#   check_lowercase('A') → each argument is evaluated first, then the function body runs
#   check_lowercase('z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_lowercase(char) → stored in memory, not executed
#   2. print(check_lowercase(...), check_lowercase(...))
#      → calls check_lowercase(char) with char='a'
#      → 2.1 return char.islower()
#   3. def check_lowercase(char) → stored in memory, not executed
#   4. def check_lowercase(char) → stored in memory, not executed
#   5. char = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_lowercase(...))
#      → calls check_lowercase(char) with char='z'
#      → 8.1 return char.islower()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_lowercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_lowercase(...), check_lowercase(...)) → check_lowercase() runs first → value passed to print()
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_lowercase(...)) → check_lowercase() runs first → value passed to print()

"""

"""
Problem 1729:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_char_index(text, char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_char_index('Hello', 'e') runs first → then print() outputs the result
#   find_char_index('Hello', 'x') runs first → then print() outputs the result
#   find_char_index(text, char) → each argument is evaluated first, then the function body runs
#   find_char_index('world', 'z') runs first → then print() outputs the result
#   find_char_index('Hello', 'e') → each argument is evaluated first, then the function body runs
#   find_char_index('Hello', 'x') → each argument is evaluated first, then the function body runs
#   find_char_index('world', 'z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_char_index(text, char) → stored in memory, not executed
#   2. print(find_char_index(...), find_char_index(...))
#      → calls find_char_index(text, char) with text='Hello', char='e'
#      → 2.1 return text.find(char)
#   3. def find_char_index(text, char) → stored in memory, not executed
#   4. def find_char_index(text, char) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. char = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(find_char_index(...))
#      → calls find_char_index(text, char) with text='world', char='z'
#      → 9.1 return text.find(char)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_char_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(find_char_index(...), find_char_index(...)) → find_char_index() runs first → value passed to print()
#     text = ... → stored in memory
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_char_index(...)) → find_char_index() runs first → value passed to print()

"""

"""
Problem 1730:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_char_in_string(text, char) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_char_in_string('Hello', 'l') → each argument is evaluated first, then the function body runs
#   count_char_in_string(text, char) → each argument is evaluated first, then the function body runs
#   count_char_in_string('world', 'z') runs first → then print() outputs the result
#   count_char_in_string('world', 'z') → each argument is evaluated first, then the function body runs
#   c == char → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_char_in_string(text, char) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_char_in_string(text, char) → stored in memory, not executed
#   5. def count_char_in_string(text, char) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. char = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(count_char_in_string(...))
#      → calls count_char_in_string(text, char) with text='world', char='z'
#      → 10.1 count = 0
#      → 10.2 for c in text:
#      → 10.2     if c == char:
#      → 10.2         count += 1
#      → 10.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_char_in_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_char_in_string(...)) → count_char_in_string() runs first → value passed to print()
#
#   Loop: takes one item from text each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1731:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_starts_with(text, prefix) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_starts_with('Hello', 'He') runs first → then print() outputs the result
#   check_starts_with('Hello', 'lo') runs first → then print() outputs the result
#   check_starts_with(text, prefix) → each argument is evaluated first, then the function body runs
#   check_starts_with('world', 'Hi, ') runs first → then print() outputs the result
#   check_starts_with('Hello', 'He') → each argument is evaluated first, then the function body runs
#   check_starts_with('Hello', 'lo') → each argument is evaluated first, then the function body runs
#   check_starts_with('world', 'Hi, ') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_starts_with(text, prefix) → stored in memory, not executed
#   2. print(check_starts_with(...), check_starts_with(...))
#      → calls check_starts_with(text, prefix) with text='Hello', prefix='He'
#      → 2.1 return text.startswith(prefix)
#   3. def check_starts_with(text, prefix) → stored in memory, not executed
#   4. def check_starts_with(text, prefix) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. prefix = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_starts_with(...))
#      → calls check_starts_with(text, prefix) with text='world', prefix='Hi, '
#      → 9.1 return text.startswith(prefix)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_starts_with) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_starts_with(...), check_starts_with(...)) → check_starts_with() runs first → value passed to print()
#     text = ... → stored in memory
#     prefix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_starts_with(...)) → check_starts_with() runs first → value passed to print()

"""

"""
Problem 1732:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_ends_with(text, suffix) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_ends_with('Hello', 'lo') runs first → then print() outputs the result
#   check_ends_with('Hello', 'He') runs first → then print() outputs the result
#   check_ends_with(text, suffix) → each argument is evaluated first, then the function body runs
#   check_ends_with('world', '?') runs first → then print() outputs the result
#   check_ends_with('Hello', 'lo') → each argument is evaluated first, then the function body runs
#   check_ends_with('Hello', 'He') → each argument is evaluated first, then the function body runs
#   check_ends_with('world', '?') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_ends_with(text, suffix) → stored in memory, not executed
#   2. print(check_ends_with(...), check_ends_with(...))
#      → calls check_ends_with(text, suffix) with text='Hello', suffix='lo'
#      → 2.1 return text.endswith(suffix)
#   3. def check_ends_with(text, suffix) → stored in memory, not executed
#   4. def check_ends_with(text, suffix) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. suffix = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_ends_with(...))
#      → calls check_ends_with(text, suffix) with text='world', suffix='?'
#      → 9.1 return text.endswith(suffix)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_ends_with) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_ends_with(...), check_ends_with(...)) → check_ends_with() runs first → value passed to print()
#     text = ... → stored in memory
#     suffix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_ends_with(...)) → check_ends_with() runs first → value passed to print()

"""

"""
Problem 1733:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def replace_char(text, old_char, new_char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   replace_char('Hello', 'l', 'x') → each argument is evaluated first, then the function body runs
#   replace_char(text, old_char, new_char) → each argument is evaluated first, then the function body runs
#   replace_char('world', 'world', 'world') runs first → then print() outputs the result
#   replace_char('world', 'world', 'world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def replace_char(text, old_char, new_char) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def replace_char(text, old_char, new_char) → stored in memory, not executed
#   5. def replace_char(text, old_char, new_char) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. old_char = ... → assigns value to variable
#   8. new_char = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(replace_char(...))
#      → calls replace_char(text, old_char, new_char) with text='world', old_char='world', new_char='world'
#      → 11.1 return text.replace(old_char, new_char)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (replace_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     old_char = ... → stored in memory
#     new_char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(replace_char(...)) → replace_char() runs first → value passed to print()

"""

"""
Problem 1734:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_char(text, char) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_char('Hello', 'l') → each argument is evaluated first, then the function body runs
#   remove_char(text, char) → each argument is evaluated first, then the function body runs
#   remove_char('world', 'z') runs first → then print() outputs the result
#   remove_char('world', 'z') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_char(text, char) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_char(text, char) → stored in memory, not executed
#   5. def remove_char(text, char) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. char = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(remove_char(...))
#      → calls remove_char(text, char) with text='world', char='z'
#      → 10.1 return text.replace(char, '')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     char = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_char(...)) → remove_char() runs first → value passed to print()

"""

"""
Problem 1735:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_substring(text, start, end) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_substring('Hello World', 0, 5) → each argument is evaluated first, then the function body runs
#   get_substring(text, start, end) → each argument is evaluated first, then the function body runs
#   get_substring('world', 3, 20) runs first → then print() outputs the result
#   get_substring('world', 3, 20) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_substring(text, start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_substring(text, start, end) → stored in memory, not executed
#   5. def get_substring(text, start, end) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. start = ... → assigns value to variable
#   8. end = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_substring(...))
#      → calls get_substring(text, start, end) with text='world', start=3, end=20
#      → 11.1 return text[start:end]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_substring) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_substring(...)) → get_substring() runs first → value passed to print()

"""

"""
Problem 1736:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_list(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_list(?) → each argument is evaluated first, then the function body runs
#   reverse_list(my_list) → each argument is evaluated first, then the function body runs
#   reverse_list(?) runs first → then print() outputs the result
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_list(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def reverse_list(my_list) → stored in memory, not executed
#   5. def reverse_list(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_list(...))
#      → calls reverse_list(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 result = ''
#      → 9.2 for char in my_list:
#      → 9.2     result = char + result
#      → 9.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_list(...)) → reverse_list() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1737:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_list_length(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_list_length(?) → each argument is evaluated first, then the function body runs
#   get_list_length(my_list) → each argument is evaluated first, then the function body runs
#   get_list_length(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_list_length(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_list_length(my_list) → stored in memory, not executed
#   5. def get_list_length(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_list_length(...))
#      → calls get_list_length(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return sum((1 for _ in my_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_list_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_list_length(...)) → get_list_length() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1738:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_element_in_list(my_list, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_element_in_list(?, 2) runs first → then print() outputs the result
#   check_element_in_list(?, 5) runs first → then print() outputs the result
#   check_element_in_list(my_list, element) → each argument is evaluated first, then the function body runs
#   check_element_in_list(?, 25) runs first → then print() outputs the result
#   element in my_list → evaluated as True or False (determines which branch runs)
#   check_element_in_list(?, 2) → each argument is evaluated first, then the function body runs
#   check_element_in_list(?, 5) → each argument is evaluated first, then the function body runs
#   check_element_in_list(?, 25) → each argument is evaluated first, then the function body runs
#   element == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_element_in_list(my_list, element) → stored in memory, not executed
#   2. print(check_element_in_list(...), check_element_in_list(...))
#      → calls check_element_in_list(my_list, element) with my_list=[1, 2, 3], element=2
#      → 2.1 return any((element == element for element in my_list))
#   3. def check_element_in_list(my_list, element) → stored in memory, not executed
#   4. def check_element_in_list(my_list, element) → stored in memory, not executed
#   5. my_list = ... → assigns value to variable
#   6. element = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_element_in_list(...))
#      → calls check_element_in_list(my_list, element) with my_list=[4, 1, 8, 6, 3], element=25
#      → 9.1 return any((element == element for element in my_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_element_in_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_element_in_list(...), check_element_in_list(...)) → check_element_in_list() runs first → value passed to print()
#     my_list = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_element_in_list(...)) → check_element_in_list() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1739:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_element_count(my_list, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_element_count(?, 2) → each argument is evaluated first, then the function body runs
#   get_element_count(my_list, element) → each argument is evaluated first, then the function body runs
#   get_element_count(?, 25) runs first → then print() outputs the result
#   get_element_count(?, 25) → each argument is evaluated first, then the function body runs
#   c == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_element_count(my_list, element) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_element_count(my_list, element) → stored in memory, not executed
#   5. def get_element_count(my_list, element) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. element = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_element_count(...))
#      → calls get_element_count(my_list, element) with my_list=[4, 1, 8, 6, 3], element=25
#      → 10.1 count = 0
#      → 10.2 for c in my_list:
#      → 10.2     if c == element:
#      → 10.2         count += 1
#      → 10.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_element_count) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_element_count(...)) → get_element_count() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1740:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_element_index(my_list, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_element_index(?, 20) runs first → then print() outputs the result
#   get_element_index(?, 40) runs first → then print() outputs the result
#   get_element_index(my_list, element) → each argument is evaluated first, then the function body runs
#   get_element_index(?, 25) runs first → then print() outputs the result
#   element in my_list → evaluated as True or False (determines which branch runs)
#   get_element_index(?, 20) → each argument is evaluated first, then the function body runs
#   get_element_index(?, 40) → each argument is evaluated first, then the function body runs
#   get_element_index(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_element_index(my_list, element) → stored in memory, not executed
#   2. print(get_element_index(...), get_element_index(...))
#      → calls get_element_index(my_list, element) with my_list=[10, 20, 30], element=20
#      → 2.1 return my_list.index(element)
#   3. def get_element_index(my_list, element) → stored in memory, not executed
#   4. def get_element_index(my_list, element) → stored in memory, not executed
#   5. my_list = ... → assigns value to variable
#   6. element = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_element_index(...))
#      → calls get_element_index(my_list, element) with my_list=[4, 1, 8, 6, 3], element=25
#      → 9.1 return my_list.index(element)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_element_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(get_element_index(...), get_element_index(...)) → get_element_index() runs first → value passed to print()
#     my_list = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_element_index(...)) → get_element_index() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1741:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_even_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_even_numbers(?) → each argument is evaluated first, then the function body runs
#   get_even_numbers(numbers) → each argument is evaluated first, then the function body runs
#   get_even_numbers(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_even_numbers(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_even_numbers(numbers) → stored in memory, not executed
#   5. def get_even_numbers(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_even_numbers(...))
#      → calls get_even_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x % 2 == 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_even_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_even_numbers(...)) → get_even_numbers() runs first → value passed to print()

"""

"""
Problem 1742:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_odd_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % !=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_odd_numbers(?) → each argument is evaluated first, then the function body runs
#   get_odd_numbers(numbers) → each argument is evaluated first, then the function body runs
#   get_odd_numbers(?) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_odd_numbers(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_odd_numbers(numbers) → stored in memory, not executed
#   5. def get_odd_numbers(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_odd_numbers(...))
#      → calls get_odd_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x % 2 != 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_odd_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_odd_numbers(...)) → get_odd_numbers() runs first → value passed to print()

"""

"""
Problem 1743:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_positive_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_positive_numbers(?) → each argument is evaluated first, then the function body runs
#   get_positive_numbers(numbers) → each argument is evaluated first, then the function body runs
#   get_positive_numbers(?) runs first → then print() outputs the result
#   x > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_positive_numbers(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_positive_numbers(numbers) → stored in memory, not executed
#   5. def get_positive_numbers(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_positive_numbers(...))
#      → calls get_positive_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x > 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_positive_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_positive_numbers(...)) → get_positive_numbers() runs first → value passed to print()

"""

"""
Problem 1744:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_negative_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_negative_numbers(?) → each argument is evaluated first, then the function body runs
#   get_negative_numbers(numbers) → each argument is evaluated first, then the function body runs
#   get_negative_numbers(?) runs first → then print() outputs the result
#   x < 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_negative_numbers(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_negative_numbers(numbers) → stored in memory, not executed
#   5. def get_negative_numbers(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_negative_numbers(...))
#      → calls get_negative_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x < 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_negative_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_negative_numbers(...)) → get_negative_numbers() runs first → value passed to print()

"""

"""
Problem 1745:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_numbers_greater_than(numbers, threshold) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_numbers_greater_than(?, 10) → each argument is evaluated first, then the function body runs
#   get_numbers_greater_than(numbers, threshold) → each argument is evaluated first, then the function body runs
#   get_numbers_greater_than(?, 25) runs first → then print() outputs the result
#   get_numbers_greater_than(?, 25) → each argument is evaluated first, then the function body runs
#   x > threshold → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_numbers_greater_than(numbers, threshold) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_numbers_greater_than(numbers, threshold) → stored in memory, not executed
#   5. def get_numbers_greater_than(numbers, threshold) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. threshold = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_numbers_greater_than(...))
#      → calls get_numbers_greater_than(numbers, threshold) with numbers=[4, 1, 8, 6, 3], threshold=25
#      → 10.1 return [x for x in numbers if x > threshold]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_numbers_greater_than) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     threshold = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_numbers_greater_than(...)) → get_numbers_greater_than() runs first → value passed to print()

"""

"""
Problem 1746:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_numbers_less_than(numbers, threshold) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_numbers_less_than(?, 10) → each argument is evaluated first, then the function body runs
#   get_numbers_less_than(numbers, threshold) → each argument is evaluated first, then the function body runs
#   get_numbers_less_than(?, 25) runs first → then print() outputs the result
#   get_numbers_less_than(?, 25) → each argument is evaluated first, then the function body runs
#   x < threshold → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_numbers_less_than(numbers, threshold) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_numbers_less_than(numbers, threshold) → stored in memory, not executed
#   5. def get_numbers_less_than(numbers, threshold) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. threshold = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_numbers_less_than(...))
#      → calls get_numbers_less_than(numbers, threshold) with numbers=[4, 1, 8, 6, 3], threshold=25
#      → 10.1 return [x for x in numbers if x < threshold]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_numbers_less_than) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     threshold = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_numbers_less_than(...)) → get_numbers_less_than() runs first → value passed to print()

"""

"""
Problem 1747:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_sum_of_list(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_sum_of_list(?) → each argument is evaluated first, then the function body runs
#   calculate_sum_of_list(numbers) → each argument is evaluated first, then the function body runs
#   calculate_sum_of_list(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_sum_of_list(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_sum_of_list(numbers) → stored in memory, not executed
#   5. def calculate_sum_of_list(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_sum_of_list(...))
#      → calls calculate_sum_of_list(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 from functools import reduce
#      → 9.2 import operator
#      → 9.3 return reduce(operator.add, numbers, 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_sum_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_sum_of_list(...)) → calculate_sum_of_list() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 1748:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_product_of_list(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_product_of_list(?) → each argument is evaluated first, then the function body runs
#   calculate_product_of_list(numbers) → each argument is evaluated first, then the function body runs
#   calculate_product_of_list(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_product_of_list(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_product_of_list(numbers) → stored in memory, not executed
#   5. def calculate_product_of_list(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_product_of_list(...))
#      → calls calculate_product_of_list(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return product
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_product_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_product_of_list(...)) → calculate_product_of_list() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 1749:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_average_of_list(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: /
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_average_of_list(?) → each argument is evaluated first, then the function body runs
#   get_average_of_list(numbers) → each argument is evaluated first, then the function body runs
#   get_average_of_list(?) runs first → then print() outputs the result
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   total / count → Python looks up total, then count, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_average_of_list(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_average_of_list(numbers) → stored in memory, not executed
#   5. def get_average_of_list(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_average_of_list(...))
#      → calls get_average_of_list(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 total = 0
#      → 9.2 count = 0
#      → 9.3 for item in numbers:
#      → 9.3     total += item
#      → 9.3     count += 1
#      → 9.4 return total / count if count else 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_average_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_average_of_list(...)) → get_average_of_list() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 1750:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_dictionary(keys, values) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: dict, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_dictionary(?, ?) → each argument is evaluated first, then the function body runs
#   create_dictionary(keys, values) → each argument is evaluated first, then the function body runs
#   create_dictionary(25, ?) runs first → then print() outputs the result
#   create_dictionary(25, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_dictionary(keys, values) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def create_dictionary(keys, values) → stored in memory, not executed
#   5. def create_dictionary(keys, values) → stored in memory, not executed
#   6. keys = ... → assigns value to variable
#   7. values = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(create_dictionary(...))
#      → calls create_dictionary(keys, values) with keys=25, values=[4, 1, 8, 6, 3]
#      → 10.1 return dict(zip(keys, values))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     keys = ... → stored in memory
#     values = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_dictionary(...)) → create_dictionary() runs first → value passed to print()

"""

"""
Problem 1751:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_dictionary_value(d, key) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_dictionary_value(?, 'a') runs first → then print() outputs the result
#   get_dictionary_value(?, 'c') runs first → then print() outputs the result
#   get_dictionary_value(d, key) → each argument is evaluated first, then the function body runs
#   get_dictionary_value(10, 'value') runs first → then print() outputs the result
#   get_dictionary_value(?, 'a') → each argument is evaluated first, then the function body runs
#   get_dictionary_value(?, 'c') → each argument is evaluated first, then the function body runs
#   get_dictionary_value(10, 'value') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_dictionary_value(d, key) → stored in memory, not executed
#   2. print(get_dictionary_value(...), get_dictionary_value(...))
#      → calls get_dictionary_value(d, key) with d=?, key='a'
#      → 2.1 return d.get(key)
#   3. def get_dictionary_value(d, key) → stored in memory, not executed
#   4. def get_dictionary_value(d, key) → stored in memory, not executed
#   5. d = ... → assigns value to variable
#   6. key = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_dictionary_value(...))
#      → calls get_dictionary_value(d, key) with d=10, key='value'
#      → 9.1 return d.get(key)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_dictionary_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(get_dictionary_value(...), get_dictionary_value(...)) → get_dictionary_value() runs first → value passed to print()
#     d = ... → stored in memory
#     key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_dictionary_value(...)) → get_dictionary_value() runs first → value passed to print()

"""

"""
Problem 1752:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_dictionary_keys(d) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_dictionary_keys(?) → each argument is evaluated first, then the function body runs
#   get_dictionary_keys(d) → each argument is evaluated first, then the function body runs
#   get_dictionary_keys(10) runs first → then print() outputs the result
#   get_dictionary_keys(10) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_dictionary_keys(d) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_dictionary_keys(d) → stored in memory, not executed
#   5. def get_dictionary_keys(d) → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_dictionary_keys(...))
#      → calls get_dictionary_keys(d) with d=10
#      → 9.1 return list(d.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_dictionary_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     d = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_dictionary_keys(...)) → get_dictionary_keys() runs first → value passed to print()

"""

"""
Problem 1753:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_dictionary_values(d) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_dictionary_values(?) → each argument is evaluated first, then the function body runs
#   get_dictionary_values(d) → each argument is evaluated first, then the function body runs
#   get_dictionary_values(10) runs first → then print() outputs the result
#   get_dictionary_values(10) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_dictionary_values(d) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_dictionary_values(d) → stored in memory, not executed
#   5. def get_dictionary_values(d) → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_dictionary_values(...))
#      → calls get_dictionary_values(d) with d=10
#      → 9.1 return list(d.values())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_dictionary_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     d = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_dictionary_values(...)) → get_dictionary_values() runs first → value passed to print()

"""

"""
Problem 1754:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_key_in_dictionary(d, key) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_key_in_dictionary(?, 'a') runs first → then print() outputs the result
#   check_key_in_dictionary(?, 'b') runs first → then print() outputs the result
#   check_key_in_dictionary(d, key) → each argument is evaluated first, then the function body runs
#   check_key_in_dictionary(10, 'value') runs first → then print() outputs the result
#   key in d → evaluated as True or False (determines which branch runs)
#   check_key_in_dictionary(?, 'a') → each argument is evaluated first, then the function body runs
#   check_key_in_dictionary(?, 'b') → each argument is evaluated first, then the function body runs
#   check_key_in_dictionary(10, 'value') → each argument is evaluated first, then the function body runs
#   element == key → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_key_in_dictionary(d, key) → stored in memory, not executed
#   2. print(check_key_in_dictionary(...), check_key_in_dictionary(...))
#      → calls check_key_in_dictionary(d, key) with d=?, key='a'
#      → 2.1 return any((element == key for element in d))
#   3. def check_key_in_dictionary(d, key) → stored in memory, not executed
#   4. def check_key_in_dictionary(d, key) → stored in memory, not executed
#   5. d = ... → assigns value to variable
#   6. key = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_key_in_dictionary(...))
#      → calls check_key_in_dictionary(d, key) with d=10, key='value'
#      → 9.1 return any((element == key for element in d))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_key_in_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_key_in_dictionary(...), check_key_in_dictionary(...)) → check_key_in_dictionary() runs first → value passed to print()
#     d = ... → stored in memory
#     key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_key_in_dictionary(...)) → check_key_in_dictionary() runs first → value passed to print()
#
#   Loop: takes one item from d each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1755:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_to_dictionary(d, key, value) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_to_dictionary(?, 'b', 2) → each argument is evaluated first, then the function body runs
#   add_to_dictionary(d, key, value) → each argument is evaluated first, then the function body runs
#   add_to_dictionary(10, 'value', 25) runs first → then print() outputs the result
#   add_to_dictionary(10, 'value', 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_to_dictionary(d, key, value) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def add_to_dictionary(d, key, value) → stored in memory, not executed
#   5. def add_to_dictionary(d, key, value) → stored in memory, not executed
#   6. d = ... → assigns value to variable
#   7. key = ... → assigns value to variable
#   8. value = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(add_to_dictionary(...))
#      → calls add_to_dictionary(d, key, value) with d=10, key='value', value=25
#      → 11.1 return new_dict
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_to_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     d = ... → stored in memory
#     key = ... → stored in memory
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_to_dictionary(...)) → add_to_dictionary() runs first → value passed to print()

"""

"""
Problem 1756:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_tuple() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: tuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_tuple(1, 2, 3, 4) → each argument is evaluated first, then the function body runs
#   create_tuple(?) → each argument is evaluated first, then the function body runs
#   create_tuple(25) runs first → then print() outputs the result
#   create_tuple(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_tuple() → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def create_tuple() → stored in memory, not executed
#   5. def create_tuple() → stored in memory, not executed
#   6. ? = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(create_tuple(...))
#      → calls create_tuple() with 
#      → 9.1 return tuple(args)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_tuple) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     ? = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_tuple(...)) → create_tuple() runs first → value passed to print()

"""

"""
Problem 1757:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_tuple_element(my_tuple, index) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_tuple_element(?, 1) → each argument is evaluated first, then the function body runs
#   get_tuple_element(my_tuple, index) → each argument is evaluated first, then the function body runs
#   get_tuple_element(?, 4) runs first → then print() outputs the result
#   get_tuple_element(?, 4) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_tuple_element(my_tuple, index) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_tuple_element(my_tuple, index) → stored in memory, not executed
#   5. def get_tuple_element(my_tuple, index) → stored in memory, not executed
#   6. my_tuple = ... → assigns value to variable
#   7. index = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_tuple_element(...))
#      → calls get_tuple_element(my_tuple, index) with my_tuple=[4, 1, 8, 6, 3], index=4
#      → 10.1 return my_tuple[index]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_tuple_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_tuple = ... → stored in memory
#     index = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_tuple_element(...)) → get_tuple_element() runs first → value passed to print()

"""

"""
Problem 1758:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def unpack_tuple(my_tuple) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   unpack_tuple(?) → each argument is evaluated first, then the function body runs
#   unpack_tuple(my_tuple) → each argument is evaluated first, then the function body runs
#   unpack_tuple(25) runs first → then print() outputs the result
#   unpack_tuple(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def unpack_tuple(my_tuple) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def unpack_tuple(my_tuple) → stored in memory, not executed
#   5. def unpack_tuple(my_tuple) → stored in memory, not executed
#   6. my_tuple = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(unpack_tuple(...))
#      → calls unpack_tuple(my_tuple) with my_tuple=25
#      → 9.1 return (a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (unpack_tuple) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_tuple = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(unpack_tuple(...)) → unpack_tuple() runs first → value passed to print()

"""

"""
Problem 1759:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_set(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_set(?) → each argument is evaluated first, then the function body runs
#   create_set(my_list) → each argument is evaluated first, then the function body runs
#   create_set(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_set(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def create_set(my_list) → stored in memory, not executed
#   5. def create_set(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(create_set(...))
#      → calls create_set(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return set(my_list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_set) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_set(...)) → create_set() runs first → value passed to print()

"""

"""
Problem 1760:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_element_in_set(my_set, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_element_in_set(?, 2) runs first → then print() outputs the result
#   check_element_in_set(?, 5) runs first → then print() outputs the result
#   check_element_in_set(my_set, element) → each argument is evaluated first, then the function body runs
#   check_element_in_set(25, 25) runs first → then print() outputs the result
#   element in my_set → evaluated as True or False (determines which branch runs)
#   check_element_in_set(?, 2) → each argument is evaluated first, then the function body runs
#   check_element_in_set(?, 5) → each argument is evaluated first, then the function body runs
#   check_element_in_set(25, 25) → each argument is evaluated first, then the function body runs
#   element == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_element_in_set(my_set, element) → stored in memory, not executed
#   2. print(check_element_in_set(...), check_element_in_set(...))
#      → calls check_element_in_set(my_set, element) with my_set=?, element=2
#      → 2.1 return any((element == element for element in my_set))
#   3. def check_element_in_set(my_set, element) → stored in memory, not executed
#   4. def check_element_in_set(my_set, element) → stored in memory, not executed
#   5. my_set = ... → assigns value to variable
#   6. element = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_element_in_set(...))
#      → calls check_element_in_set(my_set, element) with my_set=25, element=25
#      → 9.1 return any((element == element for element in my_set))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_element_in_set) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_element_in_set(...), check_element_in_set(...)) → check_element_in_set() runs first → value passed to print()
#     my_set = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_element_in_set(...)) → check_element_in_set() runs first → value passed to print()
#
#   Loop: takes one item from my_set each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1761:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def union_sets(set1, set2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: |
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   union_sets(?, ?) → each argument is evaluated first, then the function body runs
#   union_sets(set1, set2) → each argument is evaluated first, then the function body runs
#   union_sets(25, 25) runs first → then print() outputs the result
#   set1 | set2 → Python looks up set1, then set2, then applies |
#   union_sets(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def union_sets(set1, set2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def union_sets(set1, set2) → stored in memory, not executed
#   5. def union_sets(set1, set2) → stored in memory, not executed
#   6. set1 = ... → assigns value to variable
#   7. set2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(union_sets(...))
#      → calls union_sets(set1, set2) with set1=25, set2=25
#      → 10.1 return set1 | set2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (union_sets) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     set1 = ... → stored in memory
#     set2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(union_sets(...)) → union_sets() runs first → value passed to print()

"""

"""
Problem 1762:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def intersection_sets(set1, set2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: &
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   intersection_sets(?, ?) → each argument is evaluated first, then the function body runs
#   intersection_sets(set1, set2) → each argument is evaluated first, then the function body runs
#   intersection_sets(25, 25) runs first → then print() outputs the result
#   set1 & set2 → Python looks up set1, then set2, then applies &
#   intersection_sets(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def intersection_sets(set1, set2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def intersection_sets(set1, set2) → stored in memory, not executed
#   5. def intersection_sets(set1, set2) → stored in memory, not executed
#   6. set1 = ... → assigns value to variable
#   7. set2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(intersection_sets(...))
#      → calls intersection_sets(set1, set2) with set1=25, set2=25
#      → 10.1 return set1 & set2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (intersection_sets) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     set1 = ... → stored in memory
#     set2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(intersection_sets(...)) → intersection_sets() runs first → value passed to print()

"""

"""
Problem 1763:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def difference_sets(set1, set2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + -
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   difference_sets(?, ?) → each argument is evaluated first, then the function body runs
#   difference_sets(set1, set2) → each argument is evaluated first, then the function body runs
#   difference_sets(25, 25) runs first → then print() outputs the result
#   set1 - set2 → Python looks up set1, then set2, then applies -
#   set1 + ? → Python looks up set1, then ?, then applies +
#   difference_sets(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def difference_sets(set1, set2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def difference_sets(set1, set2) → stored in memory, not executed
#   5. def difference_sets(set1, set2) → stored in memory, not executed
#   6. set1 = ... → assigns value to variable
#   7. set2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(difference_sets(...))
#      → calls difference_sets(set1, set2) with set1=25, set2=25
#      → 10.1 import operator
#      → 10.2 return operator.sub(set1, set2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (difference_sets) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     set1 = ... → stored in memory
#     set2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(difference_sets(...)) → difference_sets() runs first → value passed to print()

"""

"""
Problem 1764:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def read_file_content(filename) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: open()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   read_file_content(filename) → each argument is evaluated first, then the function body runs
#   read_file_content(25) runs first → then print() outputs the result
#   read_file_content(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def read_file_content(filename) → stored in memory, not executed
#   2. print('Function defined: read_file_content(filename)')
#   3. def read_file_content(filename) → stored in memory, not executed
#   4. def read_file_content(filename) → stored in memory, not executed
#   5. filename = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(read_file_content(...))
#      → calls read_file_content(filename) with filename=25
#      → 8.1 return f.read()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (read_file_content) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print('Function defined: read_file_content(filename)')
#     filename = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(read_file_content(...)) → read_file_content() runs first → value passed to print()

"""

"""
Problem 1765:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def write_to_file(filename, content) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: open()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   write_to_file(filename, content) → each argument is evaluated first, then the function body runs
#   write_to_file(25, 25) runs first → then print() outputs the result
#   write_to_file(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def write_to_file(filename, content) → stored in memory, not executed
#   2. print('Function defined: write_to_file(filename, content)')
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. filename = ... → assigns value to variable
#   6. content = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(write_to_file(...))
#      → calls write_to_file(filename, content) with filename=25, content=25
#      → 9.1 with open(filename, 'w') as f:
#      → 9.1     f.write(content)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, write_to_file) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print('Function defined: write_to_file(filename, content)')
#     filename = ... → stored in memory
#     content = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(write_to_file(...)) → write_to_file() runs first → value passed to print()

"""

"""
Problem 1766:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def append_to_file(filename, content) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: open()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   append_to_file(filename, content) → each argument is evaluated first, then the function body runs
#   append_to_file(25, 25) runs first → then print() outputs the result
#   append_to_file(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def append_to_file(filename, content) → stored in memory, not executed
#   2. print('Function defined: append_to_file(filename, content)')
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. filename = ... → assigns value to variable
#   6. content = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(append_to_file(...))
#      → calls append_to_file(filename, content) with filename=25, content=25
#      → 9.1 with open(filename, 'a') as f:
#      → 9.1     f.write(content)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (append_to_file, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print('Function defined: append_to_file(filename, content)')
#     filename = ... → stored in memory
#     content = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(append_to_file(...)) → append_to_file() runs first → value passed to print()

"""

"""
Problem 1767:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def safe_divide(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: try/except
# Operators: * /
# Built-in functions: Fraction, float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   safe_divide(10, 2) runs first → then print() outputs the result
#   safe_divide(10, 0) runs first → then print() outputs the result
#   safe_divide(a, b) → each argument is evaluated first, then the function body runs
#   safe_divide(20, 30) runs first → then print() outputs the result
#   safe_divide(10, 2) → each argument is evaluated first, then the function body runs
#   safe_divide(10, 0) → each argument is evaluated first, then the function body runs
#   a * ? → Python looks up a, then ?, then applies *
#   safe_divide(20, 30) → each argument is evaluated first, then the function body runs
#   a / b → Python looks up a, then b, then applies /
#   1 / b → Python looks up 1, then b, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def safe_divide(a, b) → stored in memory, not executed
#   2. print(safe_divide(...), safe_divide(...))
#      → calls safe_divide(a, b) with a=10, b=2
#      → 2.1 from fractions import Fraction
#      → 2.2 return float(Fraction(a, b))
#   3. def safe_divide(a, b) → stored in memory, not executed
#   4. def safe_divide(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(safe_divide(...))
#      → calls safe_divide(a, b) with a=20, b=30
#      → 9.1 from fractions import Fraction
#      → 9.2 return float(Fraction(a, b))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (safe_divide) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(safe_divide(...), safe_divide(...)) → safe_divide() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(safe_divide(...)) → safe_divide() runs first → value passed to print()

"""

"""
Problem 1768:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def safe_convert_to_int(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: try/except
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   safe_convert_to_int('123') runs first → then print() outputs the result
#   safe_convert_to_int('abc') runs first → then print() outputs the result
#   safe_convert_to_int(text) → each argument is evaluated first, then the function body runs
#   safe_convert_to_int('world') runs first → then print() outputs the result
#   safe_convert_to_int('123') → each argument is evaluated first, then the function body runs
#   safe_convert_to_int('abc') → each argument is evaluated first, then the function body runs
#   safe_convert_to_int('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def safe_convert_to_int(text) → stored in memory, not executed
#   2. print(safe_convert_to_int(...), safe_convert_to_int(...))
#      → calls safe_convert_to_int(text) with text='123'
#      → 2.1 return int(text)
#   3. def safe_convert_to_int(text) → stored in memory, not executed
#   4. def safe_convert_to_int(text) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(safe_convert_to_int(...))
#      → calls safe_convert_to_int(text) with text='world'
#      → 8.1 return int(text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (safe_convert_to_int) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(safe_convert_to_int(...), safe_convert_to_int(...)) → safe_convert_to_int() runs first → value passed to print()
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(safe_convert_to_int(...)) → safe_convert_to_int() runs first → value passed to print()

"""

"""
Problem 1769:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def safe_get_list_element(my_list, index) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: try/except
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   safe_get_list_element(?, 1) runs first → then print() outputs the result
#   safe_get_list_element(?, 10) runs first → then print() outputs the result
#   safe_get_list_element(my_list, index) → each argument is evaluated first, then the function body runs
#   safe_get_list_element(?, 4) runs first → then print() outputs the result
#   safe_get_list_element(?, 1) → each argument is evaluated first, then the function body runs
#   safe_get_list_element(?, 10) → each argument is evaluated first, then the function body runs
#   safe_get_list_element(?, 4) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def safe_get_list_element(my_list, index) → stored in memory, not executed
#   2. print(safe_get_list_element(...), safe_get_list_element(...))
#      → calls safe_get_list_element(my_list, index) with my_list=[1, 2, 3], index=1
#      → 2.1 return my_list[index]
#   3. def safe_get_list_element(my_list, index) → stored in memory, not executed
#   4. def safe_get_list_element(my_list, index) → stored in memory, not executed
#   5. my_list = ... → assigns value to variable
#   6. index = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(safe_get_list_element(...))
#      → calls safe_get_list_element(my_list, index) with my_list=[4, 1, 8, 6, 3], index=4
#      → 9.1 return my_list[index]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (safe_get_list_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(safe_get_list_element(...), safe_get_list_element(...)) → safe_get_list_element() runs first → value passed to print()
#     my_list = ... → stored in memory
#     index = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(safe_get_list_element(...)) → safe_get_list_element() runs first → value passed to print()

"""

"""
Problem 1770:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def join_strings(strings, separator) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   join_strings(?, '-') → each argument is evaluated first, then the function body runs
#   join_strings(strings, separator) → each argument is evaluated first, then the function body runs
#   join_strings('world', 'world') runs first → then print() outputs the result
#   join_strings('world', 'world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def join_strings(strings, separator) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def join_strings(strings, separator) → stored in memory, not executed
#   5. def join_strings(strings, separator) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. separator = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(join_strings(...))
#      → calls join_strings(strings, separator) with strings='world', separator='world'
#      → 10.1 return separator.join(strings)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (join_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     separator = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(join_strings(...)) → join_strings() runs first → value passed to print()

"""

"""
Problem 1771:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def split_string(text, delimiter) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   split_string('Hello-World-Python', '-') → each argument is evaluated first, then the function body runs
#   split_string(text, delimiter) → each argument is evaluated first, then the function body runs
#   split_string('world', 25) runs first → then print() outputs the result
#   split_string('world', 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def split_string(text, delimiter) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def split_string(text, delimiter) → stored in memory, not executed
#   5. def split_string(text, delimiter) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. delimiter = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(split_string(...))
#      → calls split_string(text, delimiter) with text='world', delimiter=25
#      → 10.1 return text.split(delimiter)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (split_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     delimiter = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(split_string(...)) → split_string() runs first → value passed to print()

"""

"""
Problem 1772:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def strip_whitespace(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   strip_whitespace('  Hello World  ') → each argument is evaluated first, then the function body runs
#   strip_whitespace(text) → each argument is evaluated first, then the function body runs
#   strip_whitespace('world') runs first → then print() outputs the result
#   strip_whitespace('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def strip_whitespace(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def strip_whitespace(text) → stored in memory, not executed
#   5. def strip_whitespace(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(strip_whitespace(...))
#      → calls strip_whitespace(text) with text='world'
#      → 9.1 return text.strip()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (strip_whitespace) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(strip_whitespace(...)) → strip_whitespace() runs first → value passed to print()

"""

"""
Problem 1773:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_first_letter(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_first_letter('hello world') → each argument is evaluated first, then the function body runs
#   capitalize_first_letter(text) → each argument is evaluated first, then the function body runs
#   capitalize_first_letter('world') runs first → then print() outputs the result
#   capitalize_first_letter('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_first_letter(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def capitalize_first_letter(text) → stored in memory, not executed
#   5. def capitalize_first_letter(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(capitalize_first_letter(...))
#      → calls capitalize_first_letter(text) with text='world'
#      → 9.1 return str(text).capitalize()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_first_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_first_letter(...)) → capitalize_first_letter() runs first → value passed to print()

"""

"""
Problem 1774:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def swap_case(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   swap_case('Hello World') → each argument is evaluated first, then the function body runs
#   swap_case(text) → each argument is evaluated first, then the function body runs
#   swap_case('world') runs first → then print() outputs the result
#   swap_case('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def swap_case(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def swap_case(text) → stored in memory, not executed
#   5. def swap_case(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(swap_case(...))
#      → calls swap_case(text) with text='world'
#      → 9.1 return str(text).swapcase()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (swap_case) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(swap_case(...)) → swap_case() runs first → value passed to print()

"""

"""
Problem 1775:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_digits(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_digits('12345') runs first → then print() outputs the result
#   check_all_digits('12a45') runs first → then print() outputs the result
#   check_all_digits(text) → each argument is evaluated first, then the function body runs
#   check_all_digits('world') runs first → then print() outputs the result
#   check_all_digits('12345') → each argument is evaluated first, then the function body runs
#   check_all_digits('12a45') → each argument is evaluated first, then the function body runs
#   check_all_digits('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_digits(text) → stored in memory, not executed
#   2. print(check_all_digits(...), check_all_digits(...))
#      → calls check_all_digits(text) with text='12345'
#      → 2.1 return text.isdigit()
#   3. def check_all_digits(text) → stored in memory, not executed
#   4. def check_all_digits(text) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_all_digits(...))
#      → calls check_all_digits(text) with text='world'
#      → 8.1 return text.isdigit()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_all_digits(...), check_all_digits(...)) → check_all_digits() runs first → value passed to print()
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_digits(...)) → check_all_digits() runs first → value passed to print()

"""

"""
Problem 1776:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_letters(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_letters('Hello') runs first → then print() outputs the result
#   check_all_letters('Hello123') runs first → then print() outputs the result
#   check_all_letters(text) → each argument is evaluated first, then the function body runs
#   check_all_letters('world') runs first → then print() outputs the result
#   check_all_letters('Hello') → each argument is evaluated first, then the function body runs
#   check_all_letters('Hello123') → each argument is evaluated first, then the function body runs
#   check_all_letters('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_letters(text) → stored in memory, not executed
#   2. print(check_all_letters(...), check_all_letters(...))
#      → calls check_all_letters(text) with text='Hello'
#      → 2.1 return text.isalpha()
#   3. def check_all_letters(text) → stored in memory, not executed
#   4. def check_all_letters(text) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_all_letters(...))
#      → calls check_all_letters(text) with text='world'
#      → 8.1 return text.isalpha()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_all_letters(...), check_all_letters(...)) → check_all_letters() runs first → value passed to print()
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_letters(...)) → check_all_letters() runs first → value passed to print()

"""

"""
Problem 1777:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_alphanumeric(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_alphanumeric('Hello123') runs first → then print() outputs the result
#   check_all_alphanumeric('Hello 123') runs first → then print() outputs the result
#   check_all_alphanumeric(text) → each argument is evaluated first, then the function body runs
#   check_all_alphanumeric('world') runs first → then print() outputs the result
#   check_all_alphanumeric('Hello123') → each argument is evaluated first, then the function body runs
#   check_all_alphanumeric('Hello 123') → each argument is evaluated first, then the function body runs
#   check_all_alphanumeric('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_alphanumeric(text) → stored in memory, not executed
#   2. print(check_all_alphanumeric(...), check_all_alphanumeric(...))
#      → calls check_all_alphanumeric(text) with text='Hello123'
#      → 2.1 return text.isalnum()
#   3. def check_all_alphanumeric(text) → stored in memory, not executed
#   4. def check_all_alphanumeric(text) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_all_alphanumeric(...))
#      → calls check_all_alphanumeric(text) with text='world'
#      → 8.1 return text.isalnum()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_alphanumeric) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_all_alphanumeric(...), check_all_alphanumeric(...)) → check_all_alphanumeric() runs first → value passed to print()
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_alphanumeric(...)) → check_all_alphanumeric() runs first → value passed to print()

"""

"""
Problem 1778:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_squared_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_squared_numbers(?) → each argument is evaluated first, then the function body runs
#   get_squared_numbers(numbers) → each argument is evaluated first, then the function body runs
#   get_squared_numbers(?) runs first → then print() outputs the result
#   x ** 2 → Python looks up x, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_squared_numbers(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_squared_numbers(numbers) → stored in memory, not executed
#   5. def get_squared_numbers(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_squared_numbers(...))
#      → calls get_squared_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x ** 2 for x in numbers]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_squared_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_squared_numbers(...)) → get_squared_numbers() runs first → value passed to print()

"""

"""
Problem 1779:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_doubled_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_doubled_numbers(?) → each argument is evaluated first, then the function body runs
#   get_doubled_numbers(numbers) → each argument is evaluated first, then the function body runs
#   get_doubled_numbers(?) runs first → then print() outputs the result
#   x * 2 → Python looks up x, then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_doubled_numbers(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_doubled_numbers(numbers) → stored in memory, not executed
#   5. def get_doubled_numbers(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_doubled_numbers(...))
#      → calls get_doubled_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x * 2 for x in numbers]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_doubled_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_doubled_numbers(...)) → get_doubled_numbers() runs first → value passed to print()

"""

"""
Problem 1780:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_even_numbers_comprehension(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_even_numbers_comprehension(?) → each argument is evaluated first, then the function body runs
#   get_even_numbers_comprehension(numbers) → each argument is evaluated first, then the function body runs
#   get_even_numbers_comprehension(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_even_numbers_comprehension(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_even_numbers_comprehension(numbers) → stored in memory, not executed
#   5. def get_even_numbers_comprehension(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_even_numbers_comprehension(...))
#      → calls get_even_numbers_comprehension(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x % 2 == 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_even_numbers_comprehension) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_even_numbers_comprehension(...)) → get_even_numbers_comprehension() runs first → value passed to print()

"""

"""
Problem 1781:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_uppercase_strings(strings) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_uppercase_strings(?) → each argument is evaluated first, then the function body runs
#   get_uppercase_strings(strings) → each argument is evaluated first, then the function body runs
#   get_uppercase_strings(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_uppercase_strings(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_uppercase_strings(strings) → stored in memory, not executed
#   5. def get_uppercase_strings(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_uppercase_strings(...))
#      → calls get_uppercase_strings(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return [s.upper() for s in strings]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_uppercase_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_uppercase_strings(...)) → get_uppercase_strings() runs first → value passed to print()

"""

"""
Problem 1782:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_string_lengths(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_string_lengths(?) → each argument is evaluated first, then the function body runs
#   get_string_lengths(strings) → each argument is evaluated first, then the function body runs
#   get_string_lengths(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_string_lengths(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_string_lengths(strings) → stored in memory, not executed
#   5. def get_string_lengths(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_string_lengths(...))
#      → calls get_string_lengths(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return [len(s) for s in strings]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_string_lengths) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_string_lengths(...)) → get_string_lengths() runs first → value passed to print()

"""

"""
Problem 1783:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_number_pattern(n) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_number_pattern(5) → each argument is evaluated first, then the function body runs
#   print_number_pattern(n) → each argument is evaluated first, then the function body runs
#   print_number_pattern(15) runs first → then print() outputs the result
#   print_number_pattern(15) → each argument is evaluated first, then the function body runs
#   n + 1 → Python looks up n, then 1, then applies +
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_number_pattern(n) → stored in memory, not executed
#   2. print_number_pattern(5) → calls print_number_pattern(n) with n=5
#      → 2.1 for i in range(1, n + 1):
#      → 2.1     for j in range(1, i + 1):
#      → 2.1         print(j, end=' ')
#      → 2.1     print()
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. n = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(print_number_pattern(...))
#      → calls print_number_pattern(n) with n=15
#      → 8.1 for i in range(1, n + 1):
#      → 8.1     for j in range(1, i + 1):
#      → 8.1         print(j, end=' ')
#      → 8.1     print()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_number_pattern) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print_number_pattern(5) → jumps into print_number_pattern(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_number_pattern(...)) → print_number_pattern() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1784:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_star_pattern(n) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: * +
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_star_pattern(5) → each argument is evaluated first, then the function body runs
#   print_star_pattern(n) → each argument is evaluated first, then the function body runs
#   print_star_pattern(15) runs first → then print() outputs the result
#   print_star_pattern(15) → each argument is evaluated first, then the function body runs
#   n + 1 → Python looks up n, then 1, then applies +
#   '*' * i → Python looks up '*', then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_star_pattern(n) → stored in memory, not executed
#   2. print_star_pattern(5) → calls print_star_pattern(n) with n=5
#      → 2.1 for i in range(1, n + 1):
#      → 2.1     print('*' * i)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. n = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(print_star_pattern(...))
#      → calls print_star_pattern(n) with n=15
#      → 8.1 for i in range(1, n + 1):
#      → 8.1     print('*' * i)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_star_pattern) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print_star_pattern(5) → jumps into print_star_pattern(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_star_pattern(...)) → print_star_pattern() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1785:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_reverse_star_pattern(n) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: *
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_reverse_star_pattern(5) → each argument is evaluated first, then the function body runs
#   print_reverse_star_pattern(n) → each argument is evaluated first, then the function body runs
#   print_reverse_star_pattern(15) runs first → then print() outputs the result
#   print_reverse_star_pattern(15) → each argument is evaluated first, then the function body runs
#   '*' * i → Python looks up '*', then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_reverse_star_pattern(n) → stored in memory, not executed
#   2. print_reverse_star_pattern(5) → calls print_reverse_star_pattern(n) with n=5
#      → 2.1 for i in range(n, 0, -1):
#      → 2.1     print('*' * i)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. n = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(print_reverse_star_pattern(...))
#      → calls print_reverse_star_pattern(n) with n=15
#      → 8.1 for i in range(n, 0, -1):
#      → 8.1     print('*' * i)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_reverse_star_pattern, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print_reverse_star_pattern(5) → jumps into print_reverse_star_pattern(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_reverse_star_pattern(...)) → print_reverse_star_pattern() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1786:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_number_square(n) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: * +
# Built-in functions: range, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_number_square(3) → each argument is evaluated first, then the function body runs
#   print_number_square(n) → each argument is evaluated first, then the function body runs
#   print_number_square(15) runs first → then print() outputs the result
#   print_number_square(15) → each argument is evaluated first, then the function body runs
#   ? + 1 → Python looks up ?, then 1, then applies +
#   n * n → Python looks up n, then n, then applies *
#   i + n → Python looks up i, then n, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_number_square(n) → stored in memory, not executed
#   2. print_number_square(3) → calls print_number_square(n) with n=3
#      → 2.1 for i in range(1, n * n + 1, n):
#      → 2.1     row = [str(j) for j in range(i, i + n)]
#      → 2.2 result = ' '.join(row)
#      → 2.3 return result
#   3. def print_number_square(n) → stored in memory, not executed
#   4. def print_number_square(n) → stored in memory, not executed
#   5. n = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(print_number_square(...))
#      → calls print_number_square(n) with n=15
#      → 8.1 for i in range(1, n * n + 1, n):
#      → 8.1     row = [str(j) for j in range(i, i + n)]
#      → 8.2 result = ' '.join(row)
#      → 8.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_number_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print_number_square(3) → jumps into print_number_square(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_number_square(...)) → print_number_square() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1787:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_factorial(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_factorial(5) → each argument is evaluated first, then the function body runs
#   calculate_factorial(n) → each argument is evaluated first, then the function body runs
#   calculate_factorial(15) runs first → then print() outputs the result
#   calculate_factorial(15) → each argument is evaluated first, then the function body runs
#   n + 1 → Python looks up n, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_factorial(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_factorial(n) → stored in memory, not executed
#   5. def calculate_factorial(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_factorial(...))
#      → calls calculate_factorial(n) with n=15
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_factorial(...)) → calculate_factorial() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1788:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_sum_range(start, end) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_sum_range(1, 10) → each argument is evaluated first, then the function body runs
#   calculate_sum_range(start, end) → each argument is evaluated first, then the function body runs
#   calculate_sum_range(3, 20) runs first → then print() outputs the result
#   calculate_sum_range(3, 20) → each argument is evaluated first, then the function body runs
#   end + 1 → Python looks up end, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_sum_range(start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_sum_range(start, end) → stored in memory, not executed
#   5. def calculate_sum_range(start, end) → stored in memory, not executed
#   6. start = ... → assigns value to variable
#   7. end = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_sum_range(...))
#      → calls calculate_sum_range(start, end) with start=3, end=20
#      → 10.1 return sum(range(start, end + 1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_sum_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_sum_range(...)) → calculate_sum_range() runs first → value passed to print()

"""

"""
Problem 1789:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_product_range(start, end) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_product_range(1, 5) → each argument is evaluated first, then the function body runs
#   calculate_product_range(start, end) → each argument is evaluated first, then the function body runs
#   calculate_product_range(3, 20) runs first → then print() outputs the result
#   calculate_product_range(3, 20) → each argument is evaluated first, then the function body runs
#   end + 1 → Python looks up end, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_product_range(start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_product_range(start, end) → stored in memory, not executed
#   5. def calculate_product_range(start, end) → stored in memory, not executed
#   6. start = ... → assigns value to variable
#   7. end = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_product_range(...))
#      → calls calculate_product_range(start, end) with start=3, end=20
#      → 10.1 return product
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_product_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_product_range(...)) → calculate_product_range() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1790:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_fibonacci_sequence(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - <= ==
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_fibonacci_sequence(8) → each argument is evaluated first, then the function body runs
#   get_fibonacci_sequence(n) → each argument is evaluated first, then the function body runs
#   get_fibonacci_sequence(15) runs first → then print() outputs the result
#   n <= 0 → evaluated as True or False (determines which branch runs)
#   get_fibonacci_sequence(15) → each argument is evaluated first, then the function body runs
#   n == 1 → evaluated as True or False (determines which branch runs)
#   n == 2 → evaluated as True or False (determines which branch runs)
#   ? + ? → Python looks up ?, then ?, then applies +
#   i - 1 → Python looks up i, then 1, then applies -
#   i - 2 → Python looks up i, then 2, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_fibonacci_sequence(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_fibonacci_sequence(n) → stored in memory, not executed
#   5. def get_fibonacci_sequence(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_fibonacci_sequence(...))
#      → calls get_fibonacci_sequence(n) with n=15
#      → 9.1 return []
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_fibonacci_sequence) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_fibonacci_sequence(...)) → get_fibonacci_sequence() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1791:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_palindrome_number(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_palindrome_number(121) runs first → then print() outputs the result
#   check_palindrome_number(123) runs first → then print() outputs the result
#   check_palindrome_number(number) → each argument is evaluated first, then the function body runs
#   check_palindrome_number(25) runs first → then print() outputs the result
#   str(...) == ? → evaluated as True or False (determines which branch runs)
#   check_palindrome_number(121) → each argument is evaluated first, then the function body runs
#   check_palindrome_number(123) → each argument is evaluated first, then the function body runs
#   check_palindrome_number(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_palindrome_number(number) → stored in memory, not executed
#   2. print(check_palindrome_number(...), check_palindrome_number(...))
#      → calls check_palindrome_number(number) with number=121
#      → 2.1 return str(number) == str(number)[::-1]
#   3. def check_palindrome_number(number) → stored in memory, not executed
#   4. def check_palindrome_number(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_palindrome_number(...))
#      → calls check_palindrome_number(number) with number=25
#      → 8.1 return str(number) == str(number)[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_palindrome_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_palindrome_number(...), check_palindrome_number(...)) → check_palindrome_number() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_palindrome_number(...)) → check_palindrome_number() runs first → value passed to print()

"""

"""
Problem 1792:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_digits(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: int, str, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_digits(12345) → each argument is evaluated first, then the function body runs
#   sum_of_digits(number) → each argument is evaluated first, then the function body runs
#   sum_of_digits(25) runs first → then print() outputs the result
#   sum_of_digits(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_digits(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sum_of_digits(number) → stored in memory, not executed
#   5. def sum_of_digits(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sum_of_digits(...))
#      → calls sum_of_digits(number) with number=25
#      → 9.1 return sum((int(digit) for digit in str(number)))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_digits(...)) → sum_of_digits() runs first → value passed to print()

"""

"""
Problem 1793:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def product_of_digits(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: int, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   product_of_digits(234) → each argument is evaluated first, then the function body runs
#   product_of_digits(number) → each argument is evaluated first, then the function body runs
#   product_of_digits(25) runs first → then print() outputs the result
#   product_of_digits(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def product_of_digits(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def product_of_digits(number) → stored in memory, not executed
#   5. def product_of_digits(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(product_of_digits(...))
#      → calls product_of_digits(number) with number=25
#      → 9.1 return product
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (product_of_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(product_of_digits(...)) → product_of_digits() runs first → value passed to print()
#
#   Loop: takes one item from str(...) each round, runs body, repeats until done

"""

"""
Problem 1794:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_digits(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_digits(12345) → each argument is evaluated first, then the function body runs
#   count_digits(number) → each argument is evaluated first, then the function body runs
#   count_digits(25) runs first → then print() outputs the result
#   count_digits(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_digits(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_digits(number) → stored in memory, not executed
#   5. def count_digits(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_digits(...))
#      → calls count_digits(number) with number=25
#      → 9.1 return len(str(number))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_digits(...)) → count_digits() runs first → value passed to print()

"""

"""
Problem 1795:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_number(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: int, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_number(12345) → each argument is evaluated first, then the function body runs
#   reverse_number(number) → each argument is evaluated first, then the function body runs
#   reverse_number(25) runs first → then print() outputs the result
#   reverse_number(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_number(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def reverse_number(number) → stored in memory, not executed
#   5. def reverse_number(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_number(...))
#      → calls reverse_number(number) with number=25
#      → 9.1 return int(str(number)[::-1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()

"""

"""
Problem 1796:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_first_n_elements(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_first_n_elements(?, 3) → each argument is evaluated first, then the function body runs
#   get_first_n_elements(my_list, n) → each argument is evaluated first, then the function body runs
#   get_first_n_elements(?, 15) runs first → then print() outputs the result
#   get_first_n_elements(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_first_n_elements(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_first_n_elements(my_list, n) → stored in memory, not executed
#   5. def get_first_n_elements(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_first_n_elements(...))
#      → calls get_first_n_elements(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return my_list[:n]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_first_n_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_first_n_elements(...)) → get_first_n_elements() runs first → value passed to print()

"""

"""
Problem 1797:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_last_n_elements(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_last_n_elements(?, 3) → each argument is evaluated first, then the function body runs
#   get_last_n_elements(my_list, n) → each argument is evaluated first, then the function body runs
#   get_last_n_elements(?, 15) runs first → then print() outputs the result
#   get_last_n_elements(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_last_n_elements(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_last_n_elements(my_list, n) → stored in memory, not executed
#   5. def get_last_n_elements(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_last_n_elements(...))
#      → calls get_last_n_elements(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return my_list[-n:]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_last_n_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_last_n_elements(...)) → get_last_n_elements() runs first → value passed to print()

"""

"""
Problem 1798:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates_from_list(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates_from_list(?) → each argument is evaluated first, then the function body runs
#   remove_duplicates_from_list(my_list) → each argument is evaluated first, then the function body runs
#   remove_duplicates_from_list(?) runs first → then print() outputs the result
#   item not in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates_from_list(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_duplicates_from_list(my_list) → stored in memory, not executed
#   5. def remove_duplicates_from_list(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_duplicates_from_list(...))
#      → calls remove_duplicates_from_list(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return list(map(lambda item: item, my_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates_from_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates_from_list(...)) → remove_duplicates_from_list() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1799:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_two_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: len, max, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_two_lists(?, ?) → each argument is evaluated first, then the function body runs
#   merge_two_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   merge_two_lists(?, ?) runs first → then print() outputs the result
#   i < len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_two_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def merge_two_lists(list1, list2) → stored in memory, not executed
#   5. def merge_two_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(merge_two_lists(...))
#      → calls merge_two_lists(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_two_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_two_lists(...)) → merge_two_lists() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1800:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_common_elements(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_common_elements(?, ?) → each argument is evaluated first, then the function body runs
#   get_common_elements(list1, list2) → each argument is evaluated first, then the function body runs
#   get_common_elements(?, ?) runs first → then print() outputs the result
#   x in list2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_common_elements(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_common_elements(list1, list2) → stored in memory, not executed
#   5. def get_common_elements(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_common_elements(...))
#      → calls get_common_elements(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return [x for x in list1 if x in list2]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_common_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_common_elements(...)) → get_common_elements() runs first → value passed to print()

"""

"""
Problem 1801:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_unique_elements(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_unique_elements(?, ?) → each argument is evaluated first, then the function body runs
#   get_unique_elements(list1, list2) → each argument is evaluated first, then the function body runs
#   get_unique_elements(?, ?) runs first → then print() outputs the result
#   item not in list2 → evaluated as True or False (determines which branch runs)
#   item not in list1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_unique_elements(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_unique_elements(list1, list2) → stored in memory, not executed
#   5. def get_unique_elements(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_unique_elements(...))
#      → calls get_unique_elements(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return list(map(lambda item: item, list1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_unique_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_unique_elements(...)) → get_unique_elements() runs first → value passed to print()
#
#   Loop: takes one item from list1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1802:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_list_ascending(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_list_ascending(?) → each argument is evaluated first, then the function body runs
#   sort_list_ascending(numbers) → each argument is evaluated first, then the function body runs
#   sort_list_ascending(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_list_ascending(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_list_ascending(numbers) → stored in memory, not executed
#   5. def sort_list_ascending(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_list_ascending(...))
#      → calls sort_list_ascending(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(numbers, reverse=False)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_list_ascending) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_list_ascending(...)) → sort_list_ascending() runs first → value passed to print()

"""

"""
Problem 1803:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_list_descending(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_list_descending(?) → each argument is evaluated first, then the function body runs
#   sort_list_descending(numbers) → each argument is evaluated first, then the function body runs
#   sort_list_descending(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_list_descending(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_list_descending(numbers) → stored in memory, not executed
#   5. def sort_list_descending(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_list_descending(...))
#      → calls sort_list_descending(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(numbers, reverse=True)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_list_descending) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_list_descending(...)) → sort_list_descending() runs first → value passed to print()

"""

"""
Problem 1804:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_second_largest(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
# Built-in functions: len, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_second_largest(?) → each argument is evaluated first, then the function body runs
#   get_second_largest(numbers) → each argument is evaluated first, then the function body runs
#   get_second_largest(?) runs first → then print() outputs the result
#   len(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_second_largest(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_second_largest(numbers) → stored in memory, not executed
#   5. def get_second_largest(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_second_largest(...))
#      → calls get_second_largest(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return unique_numbers[1] if len(unique_numbers) > 1 else None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_second_largest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_second_largest(...)) → get_second_largest() runs first → value passed to print()

"""

"""
Problem 1805:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_second_smallest(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
# Built-in functions: len, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_second_smallest(?) → each argument is evaluated first, then the function body runs
#   get_second_smallest(numbers) → each argument is evaluated first, then the function body runs
#   get_second_smallest(?) runs first → then print() outputs the result
#   len(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_second_smallest(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_second_smallest(numbers) → stored in memory, not executed
#   5. def get_second_smallest(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_second_smallest(...))
#      → calls get_second_smallest(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return unique_numbers[1] if len(unique_numbers) > 1 else None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_second_smallest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_second_smallest(...)) → get_second_smallest() runs first → value passed to print()

"""

"""
Problem 1806:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_sorted(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_sorted(?) runs first → then print() outputs the result
#   check_sorted(numbers) → each argument is evaluated first, then the function body runs
#   numbers == sorted(...) → evaluated as True or False (determines which branch runs)
#   check_sorted(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_sorted(numbers) → stored in memory, not executed
#   2. print(check_sorted(...), check_sorted(...))
#      → calls check_sorted(numbers) with numbers=[1, 2, 3, 4]
#      → 2.1 return numbers == sorted(numbers)
#   3. def check_sorted(numbers) → stored in memory, not executed
#   4. def check_sorted(numbers) → stored in memory, not executed
#   5. numbers = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_sorted(...))
#      → calls check_sorted(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 8.1 return numbers == sorted(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_sorted) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_sorted(...), check_sorted(...)) → check_sorted() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_sorted(...)) → check_sorted() runs first → value passed to print()

"""

"""
Problem 1807:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_occurrences_in_list(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_occurrences_in_list(?) → each argument is evaluated first, then the function body runs
#   count_occurrences_in_list(my_list) → each argument is evaluated first, then the function body runs
#   count_occurrences_in_list(?) runs first → then print() outputs the result
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_occurrences_in_list(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_occurrences_in_list(my_list) → stored in memory, not executed
#   5. def count_occurrences_in_list(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_occurrences_in_list(...))
#      → calls count_occurrences_in_list(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return counts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_occurrences_in_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_occurrences_in_list(...)) → count_occurrences_in_list() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1808:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_most_frequent(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_most_frequent(?) → each argument is evaluated first, then the function body runs
#   get_most_frequent(my_list) → each argument is evaluated first, then the function body runs
#   get_most_frequent(?) runs first → then print() outputs the result
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_most_frequent(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_most_frequent(my_list) → stored in memory, not executed
#   5. def get_most_frequent(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_most_frequent(...))
#      → calls get_most_frequent(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return max(counts, key=counts.get)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_most_frequent) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_most_frequent(...)) → get_most_frequent() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1809:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_least_frequent(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_least_frequent(?) → each argument is evaluated first, then the function body runs
#   get_least_frequent(my_list) → each argument is evaluated first, then the function body runs
#   get_least_frequent(?) runs first → then print() outputs the result
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_least_frequent(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_least_frequent(my_list) → stored in memory, not executed
#   5. def get_least_frequent(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_least_frequent(...))
#      → calls get_least_frequent(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return min(counts, key=counts.get)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_least_frequent) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_least_frequent(...)) → get_least_frequent() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1810:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def flatten_nested_list(nested_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   flatten_nested_list(?) → each argument is evaluated first, then the function body runs
#   flatten_nested_list(nested_list) → each argument is evaluated first, then the function body runs
#   flatten_nested_list(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def flatten_nested_list(nested_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def flatten_nested_list(nested_list) → stored in memory, not executed
#   5. def flatten_nested_list(nested_list) → stored in memory, not executed
#   6. nested_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(flatten_nested_list(...))
#      → calls flatten_nested_list(nested_list) with nested_list=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (flatten_nested_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     nested_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(flatten_nested_list(...)) → flatten_nested_list() runs first → value passed to print()
#
#   Loop: takes one item from nested_list each round, runs body, repeats until done

"""

"""
Problem 1811:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def group_by_length(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   group_by_length(?) → each argument is evaluated first, then the function body runs
#   group_by_length(strings) → each argument is evaluated first, then the function body runs
#   group_by_length(?) runs first → then print() outputs the result
#   length not in groups → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def group_by_length(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def group_by_length(strings) → stored in memory, not executed
#   5. def group_by_length(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(group_by_length(...))
#      → calls group_by_length(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return groups
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (group_by_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(group_by_length(...)) → group_by_length() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1812:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_by_length(strings, min_length) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >=
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_by_length(?, 3) → each argument is evaluated first, then the function body runs
#   filter_by_length(strings, min_length) → each argument is evaluated first, then the function body runs
#   filter_by_length(?, 25) runs first → then print() outputs the result
#   filter_by_length(?, 25) → each argument is evaluated first, then the function body runs
#   len(...) >= min_length → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_by_length(strings, min_length) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def filter_by_length(strings, min_length) → stored in memory, not executed
#   5. def filter_by_length(strings, min_length) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. min_length = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(filter_by_length(...))
#      → calls filter_by_length(strings, min_length) with strings=[4, 1, 8, 6, 3], min_length=25
#      → 10.1 return [s for s in strings if len(s) >= min_length]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_by_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     min_length = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_by_length(...)) → filter_by_length() runs first → value passed to print()

"""

"""
Problem 1813:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_longest_string(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_longest_string(?) → each argument is evaluated first, then the function body runs
#   get_longest_string(strings) → each argument is evaluated first, then the function body runs
#   get_longest_string(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_longest_string(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_longest_string(strings) → stored in memory, not executed
#   5. def get_longest_string(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_longest_string(...))
#      → calls get_longest_string(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return max(strings, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_longest_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_longest_string(...)) → get_longest_string() runs first → value passed to print()

"""

"""
Problem 1814:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_shortest_string(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_shortest_string(?) → each argument is evaluated first, then the function body runs
#   get_shortest_string(strings) → each argument is evaluated first, then the function body runs
#   get_shortest_string(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_shortest_string(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_shortest_string(strings) → stored in memory, not executed
#   5. def get_shortest_string(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_shortest_string(...))
#      → calls get_shortest_string(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return min(strings, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_shortest_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_shortest_string(...)) → get_shortest_string() runs first → value passed to print()

"""

"""
Problem 1815:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_all_words(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_all_words('hello world python') → each argument is evaluated first, then the function body runs
#   capitalize_all_words(text) → each argument is evaluated first, then the function body runs
#   capitalize_all_words('world') runs first → then print() outputs the result
#   capitalize_all_words('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_all_words(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def capitalize_all_words(text) → stored in memory, not executed
#   5. def capitalize_all_words(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(capitalize_all_words(...))
#      → calls capitalize_all_words(text) with text='world'
#      → 9.1 return ' '.join((word.capitalize() for word in text.split()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_all_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_all_words(...)) → capitalize_all_words() runs first → value passed to print()

"""

"""
Problem 1816:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_words(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_words('hello world python') → each argument is evaluated first, then the function body runs
#   reverse_words(text) → each argument is evaluated first, then the function body runs
#   reverse_words('world') runs first → then print() outputs the result
#   reverse_words('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_words(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def reverse_words(text) → stored in memory, not executed
#   5. def reverse_words(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_words(...))
#      → calls reverse_words(text) with text='world'
#      → 9.1 return ' '.join(reversed(words))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_words(...)) → reverse_words() runs first → value passed to print()

"""

"""
Problem 1817:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words('Hello World Python') → each argument is evaluated first, then the function body runs
#   count_words(text) → each argument is evaluated first, then the function body runs
#   count_words('world') runs first → then print() outputs the result
#   count_words('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_words(text) → stored in memory, not executed
#   5. def count_words(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_words(...))
#      → calls count_words(text) with text='world'
#      → 9.1 return len(text.split())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words(...)) → count_words() runs first → value passed to print()

"""

"""
Problem 1818:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_longest_word(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_longest_word('hello world python programming') → each argument is evaluated first, then the function body runs
#   get_longest_word(text) → each argument is evaluated first, then the function body runs
#   get_longest_word('world') runs first → then print() outputs the result
#   get_longest_word('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_longest_word(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_longest_word(text) → stored in memory, not executed
#   5. def get_longest_word(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_longest_word(...))
#      → calls get_longest_word(text) with text='world'
#      → 9.1 return max(words, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_longest_word(...)) → get_longest_word() runs first → value passed to print()

"""

"""
Problem 1819:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_shortest_word(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_shortest_word('hello world python programming') → each argument is evaluated first, then the function body runs
#   get_shortest_word(text) → each argument is evaluated first, then the function body runs
#   get_shortest_word('world') runs first → then print() outputs the result
#   get_shortest_word('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_shortest_word(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_shortest_word(text) → stored in memory, not executed
#   5. def get_shortest_word(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_shortest_word(...))
#      → calls get_shortest_word(text) with text='world'
#      → 9.1 return min(words, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_shortest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_shortest_word(...)) → get_shortest_word() runs first → value passed to print()

"""

"""
Problem 1820:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_vowels_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_vowels_from_string('Hello World') → each argument is evaluated first, then the function body runs
#   remove_vowels_from_string(text) → each argument is evaluated first, then the function body runs
#   remove_vowels_from_string('world') runs first → then print() outputs the result
#   remove_vowels_from_string('world') → each argument is evaluated first, then the function body runs
#   char not in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_vowels_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_vowels_from_string(text) → stored in memory, not executed
#   5. def remove_vowels_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_vowels_from_string(...))
#      → calls remove_vowels_from_string(text) with text='world'
#      → 9.1 return ''.join((char for char in text if char not in vowels))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_vowels_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_vowels_from_string(...)) → remove_vowels_from_string() runs first → value passed to print()

"""

"""
Problem 1821:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_consonants_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in or
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_consonants_from_string('Hello World') → each argument is evaluated first, then the function body runs
#   remove_consonants_from_string(text) → each argument is evaluated first, then the function body runs
#   remove_consonants_from_string('world') runs first → then print() outputs the result
#   remove_consonants_from_string('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_consonants_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_consonants_from_string(text) → stored in memory, not executed
#   5. def remove_consonants_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_consonants_from_string(...))
#      → calls remove_consonants_from_string(text) with text='world'
#      → 9.1 return ''.join((char for char in text if char in vowels or not char.isalpha()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_consonants_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_consonants_from_string(...)) → remove_consonants_from_string() runs first → value passed to print()

"""

"""
Problem 1822:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels_in_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels_in_string('Hello World') → each argument is evaluated first, then the function body runs
#   count_vowels_in_string(text) → each argument is evaluated first, then the function body runs
#   count_vowels_in_string('world') runs first → then print() outputs the result
#   count_vowels_in_string('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels_in_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_vowels_in_string(text) → stored in memory, not executed
#   5. def count_vowels_in_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_vowels_in_string(...))
#      → calls count_vowels_in_string(text) with text='world'
#      → 9.1 return sum((1 for char in text if char in vowels))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels_in_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels_in_string(...)) → count_vowels_in_string() runs first → value passed to print()

"""

"""
Problem 1824:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def swap_first_last_words(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: <
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   swap_first_last_words('Hello World Python') → each argument is evaluated first, then the function body runs
#   swap_first_last_words(text) → each argument is evaluated first, then the function body runs
#   swap_first_last_words('world') runs first → then print() outputs the result
#   len(...) < 2 → evaluated as True or False (determines which branch runs)
#   swap_first_last_words('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def swap_first_last_words(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def swap_first_last_words(text) → stored in memory, not executed
#   5. def swap_first_last_words(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(swap_first_last_words(...))
#      → calls swap_first_last_words(text) with text='world'
#      → 9.1 return text
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (swap_first_last_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(swap_first_last_words(...)) → swap_first_last_words() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1825:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_middle_word(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: //
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_middle_word('Hello World Python Programming') → each argument is evaluated first, then the function body runs
#   get_middle_word(text) → each argument is evaluated first, then the function body runs
#   get_middle_word('world') runs first → then print() outputs the result
#   get_middle_word('world') → each argument is evaluated first, then the function body runs
#   len(...) // 2 → Python looks up len(...), then 2, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_middle_word(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_middle_word(text) → stored in memory, not executed
#   5. def get_middle_word(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_middle_word(...))
#      → calls get_middle_word(text) with text='world'
#      → 9.1 return words[len(words) // 2]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_middle_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_middle_word(...)) → get_middle_word() runs first → value passed to print()

"""

"""
Problem 1826:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_same(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: all()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_same(?) runs first → then print() outputs the result
#   check_all_same(my_list) → each argument is evaluated first, then the function body runs
#   check_all_same(?) → each argument is evaluated first, then the function body runs
#   x == ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_same(my_list) → stored in memory, not executed
#   2. print(check_all_same(...), check_all_same(...))
#      → calls check_all_same(my_list) with my_list=[1, 1, 1]
#      → 2.1 return all((x == my_list[0] for x in my_list)) if my_list else True
#   3. def check_all_same(my_list) → stored in memory, not executed
#   4. def check_all_same(my_list) → stored in memory, not executed
#   5. my_list = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_all_same(...))
#      → calls check_all_same(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 8.1 return all((x == my_list[0] for x in my_list)) if my_list else True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_same) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_all_same(...), check_all_same(...)) → check_all_same() runs first → value passed to print()
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_same(...)) → check_all_same() runs first → value passed to print()

"""

"""
Problem 1827:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_different(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: len, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_different(?) runs first → then print() outputs the result
#   check_all_different(my_list) → each argument is evaluated first, then the function body runs
#   len(...) == len(...) → evaluated as True or False (determines which branch runs)
#   check_all_different(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_different(my_list) → stored in memory, not executed
#   2. print(check_all_different(...), check_all_different(...))
#      → calls check_all_different(my_list) with my_list=[1, 2, 3]
#      → 2.1 return len(my_list) == len(set(my_list))
#   3. def check_all_different(my_list) → stored in memory, not executed
#   4. def check_all_different(my_list) → stored in memory, not executed
#   5. my_list = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_all_different(...))
#      → calls check_all_different(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 8.1 return len(my_list) == len(set(my_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_different) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_all_different(...), check_all_different(...)) → check_all_different() runs first → value passed to print()
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_different(...)) → check_all_different() runs first → value passed to print()

"""

"""
Problem 1828:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_range_list(start, end) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_range_list(1, 5) → each argument is evaluated first, then the function body runs
#   get_range_list(start, end) → each argument is evaluated first, then the function body runs
#   get_range_list(3, 20) runs first → then print() outputs the result
#   get_range_list(3, 20) → each argument is evaluated first, then the function body runs
#   end + 1 → Python looks up end, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_range_list(start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_range_list(start, end) → stored in memory, not executed
#   5. def get_range_list(start, end) → stored in memory, not executed
#   6. start = ... → assigns value to variable
#   7. end = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_range_list(...))
#      → calls get_range_list(start, end) with start=3, end=20
#      → 10.1 return list(range(start, end + 1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_range_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_range_list(...)) → get_range_list() runs first → value passed to print()

"""

"""
Problem 1829:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_even_range(start, end) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % + ==
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_even_range(1, 10) → each argument is evaluated first, then the function body runs
#   get_even_range(start, end) → each argument is evaluated first, then the function body runs
#   get_even_range(3, 20) runs first → then print() outputs the result
#   get_even_range(3, 20) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   end + 1 → Python looks up end, then 1, then applies +
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_even_range(start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_even_range(start, end) → stored in memory, not executed
#   5. def get_even_range(start, end) → stored in memory, not executed
#   6. start = ... → assigns value to variable
#   7. end = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_even_range(...))
#      → calls get_even_range(start, end) with start=3, end=20
#      → 10.1 return [x for x in range(start, end + 1) if x % 2 == 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_even_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_even_range(...)) → get_even_range() runs first → value passed to print()

"""

"""
Problem 1830:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_odd_range(start, end) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % + !=
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_odd_range(1, 10) → each argument is evaluated first, then the function body runs
#   get_odd_range(start, end) → each argument is evaluated first, then the function body runs
#   get_odd_range(3, 20) runs first → then print() outputs the result
#   get_odd_range(3, 20) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   end + 1 → Python looks up end, then 1, then applies +
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_odd_range(start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_odd_range(start, end) → stored in memory, not executed
#   5. def get_odd_range(start, end) → stored in memory, not executed
#   6. start = ... → assigns value to variable
#   7. end = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_odd_range(...))
#      → calls get_odd_range(start, end) with start=3, end=20
#      → 10.1 return [x for x in range(start, end + 1) if x % 2 != 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_odd_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_odd_range(...)) → get_odd_range() runs first → value passed to print()

"""

"""
Problem 1831:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_lists(?, ?) → each argument is evaluated first, then the function body runs
#   multiply_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   multiply_lists(?, ?) runs first → then print() outputs the result
#   a * b → Python looks up a, then b, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def multiply_lists(list1, list2) → stored in memory, not executed
#   5. def multiply_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(multiply_lists(...))
#      → calls multiply_lists(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return [a * b for a, b in zip(list1, list2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_lists(...)) → multiply_lists() runs first → value passed to print()

"""

"""
Problem 1832:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_lists(?, ?) → each argument is evaluated first, then the function body runs
#   add_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   add_lists(?, ?) runs first → then print() outputs the result
#   a + b → Python looks up a, then b, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def add_lists(list1, list2) → stored in memory, not executed
#   5. def add_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(add_lists(...))
#      → calls add_lists(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return [a + b for a, b in zip(list1, list2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_lists(...)) → add_lists() runs first → value passed to print()

"""

"""
Problem 1833:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def subtract_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: -
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   subtract_lists(?, ?) → each argument is evaluated first, then the function body runs
#   subtract_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   subtract_lists(?, ?) runs first → then print() outputs the result
#   a - b → Python looks up a, then b, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def subtract_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def subtract_lists(list1, list2) → stored in memory, not executed
#   5. def subtract_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(subtract_lists(...))
#      → calls subtract_lists(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return [a - b for a, b in zip(list1, list2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (subtract_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(subtract_lists(...)) → subtract_lists() runs first → value passed to print()

"""

"""
Problem 1834:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_index_of_max(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_index_of_max(?) → each argument is evaluated first, then the function body runs
#   get_index_of_max(numbers) → each argument is evaluated first, then the function body runs
#   get_index_of_max(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_index_of_max(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_index_of_max(numbers) → stored in memory, not executed
#   5. def get_index_of_max(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_index_of_max(...))
#      → calls get_index_of_max(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return numbers.index(max(numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_index_of_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_index_of_max(...)) → get_index_of_max() runs first → value passed to print()

"""

"""
Problem 1835:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_index_of_min(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_index_of_min(?) → each argument is evaluated first, then the function body runs
#   get_index_of_min(numbers) → each argument is evaluated first, then the function body runs
#   get_index_of_min(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_index_of_min(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_index_of_min(numbers) → stored in memory, not executed
#   5. def get_index_of_min(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_index_of_min(...))
#      → calls get_index_of_min(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return numbers.index(min(numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_index_of_min) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_index_of_min(...)) → get_index_of_min() runs first → value passed to print()

"""

"""
Problem 1836:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def rotate_list_left(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   rotate_list_left(?, 2) → each argument is evaluated first, then the function body runs
#   rotate_list_left(my_list, n) → each argument is evaluated first, then the function body runs
#   rotate_list_left(?, 15) runs first → then print() outputs the result
#   n % len(...) → Python looks up n, then len(...), then applies %
#   ? + ? → Python looks up ?, then ?, then applies +
#   rotate_list_left(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def rotate_list_left(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def rotate_list_left(my_list, n) → stored in memory, not executed
#   5. def rotate_list_left(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(rotate_list_left(...))
#      → calls rotate_list_left(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return my_list[n:] + my_list[:n]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (rotate_list_left) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(rotate_list_left(...)) → rotate_list_left() runs first → value passed to print()

"""

"""
Problem 1837:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def rotate_list_right(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   rotate_list_right(?, 2) → each argument is evaluated first, then the function body runs
#   rotate_list_right(my_list, n) → each argument is evaluated first, then the function body runs
#   rotate_list_right(?, 15) runs first → then print() outputs the result
#   n % len(...) → Python looks up n, then len(...), then applies %
#   ? + ? → Python looks up ?, then ?, then applies +
#   rotate_list_right(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def rotate_list_right(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def rotate_list_right(my_list, n) → stored in memory, not executed
#   5. def rotate_list_right(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(rotate_list_right(...))
#      → calls rotate_list_right(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return my_list[-n:] + my_list[:-n]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (rotate_list_right) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(rotate_list_right(...)) → rotate_list_right() runs first → value passed to print()

"""

"""
Problem 1838:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def chunk_list(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   chunk_list(?, 3) → each argument is evaluated first, then the function body runs
#   chunk_list(my_list, n) → each argument is evaluated first, then the function body runs
#   chunk_list(?, 15) runs first → then print() outputs the result
#   chunk_list(?, 15) → each argument is evaluated first, then the function body runs
#   i + n → Python looks up i, then n, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def chunk_list(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def chunk_list(my_list, n) → stored in memory, not executed
#   5. def chunk_list(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(chunk_list(...))
#      → calls chunk_list(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return [my_list[i:i + n] for i in range(0, len(my_list), n)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (chunk_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(chunk_list(...)) → chunk_list() runs first → value passed to print()

"""

"""
Problem 1839:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def interleave_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: len, max, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   interleave_lists(?, ?) → each argument is evaluated first, then the function body runs
#   interleave_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   interleave_lists(?, ?) runs first → then print() outputs the result
#   i < len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def interleave_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def interleave_lists(list1, list2) → stored in memory, not executed
#   5. def interleave_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(interleave_lists(...))
#      → calls interleave_lists(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (interleave_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(interleave_lists(...)) → interleave_lists() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1840:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def zip_lists(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   zip_lists(?, ?) → each argument is evaluated first, then the function body runs
#   zip_lists(list1, list2) → each argument is evaluated first, then the function body runs
#   zip_lists(?, ?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def zip_lists(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def zip_lists(list1, list2) → stored in memory, not executed
#   5. def zip_lists(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(zip_lists(...))
#      → calls zip_lists(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return list(zip(list1, list2))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (zip_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(zip_lists(...)) → zip_lists() runs first → value passed to print()

"""

"""
Problem 1841:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def unzip_list(tuples_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   unzip_list(?) → each argument is evaluated first, then the function body runs
#   unzip_list(tuples_list) → each argument is evaluated first, then the function body runs
#   unzip_list(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def unzip_list(tuples_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def unzip_list(tuples_list) → stored in memory, not executed
#   5. def unzip_list(tuples_list) → stored in memory, not executed
#   6. tuples_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(unzip_list(...))
#      → calls unzip_list(tuples_list) with tuples_list=[4, 1, 8, 6, 3]
#      → 9.1 return ([], [])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (unzip_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     tuples_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(unzip_list(...)) → unzip_list() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1842:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_uppercase_letters(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_uppercase_letters('Hello World') → each argument is evaluated first, then the function body runs
#   count_uppercase_letters(text) → each argument is evaluated first, then the function body runs
#   count_uppercase_letters('world') runs first → then print() outputs the result
#   count_uppercase_letters('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_uppercase_letters(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_uppercase_letters(text) → stored in memory, not executed
#   5. def count_uppercase_letters(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_uppercase_letters(...))
#      → calls count_uppercase_letters(text) with text='world'
#      → 9.1 return sum((1 for char in text if char.isupper()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_uppercase_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_uppercase_letters(...)) → count_uppercase_letters() runs first → value passed to print()

"""

"""
Problem 1843:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_lowercase_letters(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_lowercase_letters('Hello World') → each argument is evaluated first, then the function body runs
#   count_lowercase_letters(text) → each argument is evaluated first, then the function body runs
#   count_lowercase_letters('world') runs first → then print() outputs the result
#   count_lowercase_letters('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_lowercase_letters(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_lowercase_letters(text) → stored in memory, not executed
#   5. def count_lowercase_letters(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_lowercase_letters(...))
#      → calls count_lowercase_letters(text) with text='world'
#      → 9.1 return sum((1 for char in text if char.islower()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_lowercase_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_lowercase_letters(...)) → count_lowercase_letters() runs first → value passed to print()

"""

"""
Problem 1844:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def swap_uppercase_lowercase(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   swap_uppercase_lowercase('Hello World') → each argument is evaluated first, then the function body runs
#   swap_uppercase_lowercase(text) → each argument is evaluated first, then the function body runs
#   swap_uppercase_lowercase('world') runs first → then print() outputs the result
#   swap_uppercase_lowercase('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def swap_uppercase_lowercase(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def swap_uppercase_lowercase(text) → stored in memory, not executed
#   5. def swap_uppercase_lowercase(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(swap_uppercase_lowercase(...))
#      → calls swap_uppercase_lowercase(text) with text='world'
#      → 9.1 return str(text).swapcase()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (swap_uppercase_lowercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(swap_uppercase_lowercase(...)) → swap_uppercase_lowercase() runs first → value passed to print()

"""

"""
Problem 1845:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_numbers_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_numbers_from_string('Hello123World456') → each argument is evaluated first, then the function body runs
#   remove_numbers_from_string(text) → each argument is evaluated first, then the function body runs
#   remove_numbers_from_string('world') runs first → then print() outputs the result
#   remove_numbers_from_string('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_numbers_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_numbers_from_string(text) → stored in memory, not executed
#   5. def remove_numbers_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_numbers_from_string(...))
#      → calls remove_numbers_from_string(text) with text='world'
#      → 9.1 return ''.join((char for char in text if not char.isdigit()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_numbers_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_numbers_from_string(...)) → remove_numbers_from_string() runs first → value passed to print()

"""

"""
Problem 1846:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_letters_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_letters_from_string('Hello123World456!') → each argument is evaluated first, then the function body runs
#   remove_letters_from_string(text) → each argument is evaluated first, then the function body runs
#   remove_letters_from_string('world') runs first → then print() outputs the result
#   remove_letters_from_string('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_letters_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_letters_from_string(text) → stored in memory, not executed
#   5. def remove_letters_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_letters_from_string(...))
#      → calls remove_letters_from_string(text) with text='world'
#      → 9.1 return ''.join((char for char in text if not char.isalpha()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_letters_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_letters_from_string(...)) → remove_letters_from_string() runs first → value passed to print()

"""

"""
Problem 1847:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def extract_numbers_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   extract_numbers_from_string('Hello123World456') → each argument is evaluated first, then the function body runs
#   extract_numbers_from_string(text) → each argument is evaluated first, then the function body runs
#   extract_numbers_from_string('world') runs first → then print() outputs the result
#   extract_numbers_from_string('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def extract_numbers_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def extract_numbers_from_string(text) → stored in memory, not executed
#   5. def extract_numbers_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(extract_numbers_from_string(...))
#      → calls extract_numbers_from_string(text) with text='world'
#      → 9.1 return [char for char in text if char.isdigit()]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (extract_numbers_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(extract_numbers_from_string(...)) → extract_numbers_from_string() runs first → value passed to print()

"""

"""
Problem 1848:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def extract_letters_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   extract_letters_from_string('Hello123World456') → each argument is evaluated first, then the function body runs
#   extract_letters_from_string(text) → each argument is evaluated first, then the function body runs
#   extract_letters_from_string('world') runs first → then print() outputs the result
#   extract_letters_from_string('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def extract_letters_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def extract_letters_from_string(text) → stored in memory, not executed
#   5. def extract_letters_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(extract_letters_from_string(...))
#      → calls extract_letters_from_string(text) with text='world'
#      → 9.1 return [char for char in text if char.isalpha()]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (extract_letters_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(extract_letters_from_string(...)) → extract_letters_from_string() runs first → value passed to print()

"""

"""
Problem 1849:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_phone_number(digits) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_phone_number('1234567890') → each argument is evaluated first, then the function body runs
#   format_phone_number(digits) → each argument is evaluated first, then the function body runs
#   format_phone_number(?) runs first → then print() outputs the result
#   format_phone_number(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_phone_number(digits) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_phone_number(digits) → stored in memory, not executed
#   5. def format_phone_number(digits) → stored in memory, not executed
#   6. digits = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(format_phone_number(...))
#      → calls format_phone_number(digits) with digits=[4, 1, 8, 6, 3]
#      → 9.1 return f'({digits[0:3]}) {digits[3:6]}-{digits[6:10]}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_phone_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     digits = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_phone_number(...)) → format_phone_number() runs first → value passed to print()

"""

"""
Problem 1850:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_volume_box(length, width, height) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_volume_box(5, 3, 4) → each argument is evaluated first, then the function body runs
#   calculate_volume_box(length, width, height) → each argument is evaluated first, then the function body runs
#   calculate_volume_box(25, 25, 20) runs first → then print() outputs the result
#   ? * height → Python looks up ?, then height, then applies *
#   calculate_volume_box(25, 25, 20) → each argument is evaluated first, then the function body runs
#   length * width → Python looks up length, then width, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_volume_box(length, width, height) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_volume_box(length, width, height) → stored in memory, not executed
#   5. def calculate_volume_box(length, width, height) → stored in memory, not executed
#   6. length = ... → assigns value to variable
#   7. width = ... → assigns value to variable
#   8. height = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_volume_box(...))
#      → calls calculate_volume_box(length, width, height) with length=25, width=25, height=20
#      → 11.1 return length * width * height
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_volume_box) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     height = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_volume_box(...)) → calculate_volume_box() runs first → value passed to print()

"""

"""
Problem 1851:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_triangle_valid(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + > and
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_triangle_valid(3, 4, 5) runs first → then print() outputs the result
#   check_triangle_valid(1, 2, 5) runs first → then print() outputs the result
#   check_triangle_valid(a, b, c) → each argument is evaluated first, then the function body runs
#   check_triangle_valid(20, 30, 10) runs first → then print() outputs the result
#   check_triangle_valid(3, 4, 5) → each argument is evaluated first, then the function body runs
#   check_triangle_valid(1, 2, 5) → each argument is evaluated first, then the function body runs
#   check_triangle_valid(20, 30, 10) → each argument is evaluated first, then the function body runs
#   ? > c → evaluated as True or False (determines which branch runs)
#   ? > b → evaluated as True or False (determines which branch runs)
#   ? > a → evaluated as True or False (determines which branch runs)
#   a + b → Python looks up a, then b, then applies +
#   a + c → Python looks up a, then c, then applies +
#   b + c → Python looks up b, then c, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_triangle_valid(a, b, c) → stored in memory, not executed
#   2. print(check_triangle_valid(...), check_triangle_valid(...))
#      → calls check_triangle_valid(a, b, c) with a=3, b=4, c=5
#      → 2.1 return a + b > c and a + c > b and (b + c > a)
#   3. def check_triangle_valid(a, b, c) → stored in memory, not executed
#   4. def check_triangle_valid(a, b, c) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. c = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(check_triangle_valid(...))
#      → calls check_triangle_valid(a, b, c) with a=20, b=30, c=10
#      → 10.1 return a + b > c and a + c > b and (b + c > a)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_triangle_valid) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_triangle_valid(...), check_triangle_valid(...)) → check_triangle_valid() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_triangle_valid(...)) → check_triangle_valid() runs first → value passed to print()

"""

"""
Problem 1852:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_grade(score) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_grade(95) runs first → then print() outputs the result
#   find_grade(75) runs first → then print() outputs the result
#   find_grade(55) runs first → then print() outputs the result
#   find_grade(score) → each argument is evaluated first, then the function body runs
#   find_grade(25) runs first → then print() outputs the result
#   score >= 90 → evaluated as True or False (determines which branch runs)
#   find_grade(95) → each argument is evaluated first, then the function body runs
#   find_grade(75) → each argument is evaluated first, then the function body runs
#   find_grade(55) → each argument is evaluated first, then the function body runs
#   find_grade(25) → each argument is evaluated first, then the function body runs
#   score >= 80 → evaluated as True or False (determines which branch runs)
#   score >= 70 → evaluated as True or False (determines which branch runs)
#   score >= 60 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_grade(score) → stored in memory, not executed
#   2. print(find_grade(...), find_grade(...), find_grade(...))
#      → calls find_grade(score) with score=95
#      → 2.1 return 'A'
#   3. def find_grade(score) → stored in memory, not executed
#   4. def find_grade(score) → stored in memory, not executed
#   5. score = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(find_grade(...))
#      → calls find_grade(score) with score=25
#      → 8.1 return 'A'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_grade) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(find_grade(...), find_grade(...), find_grade(...)) → find_grade() runs first → value passed to print()
#     score = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_grade(...)) → find_grade() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1853:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_season(month) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_season(1) runs first → then print() outputs the result
#   get_season(4) runs first → then print() outputs the result
#   get_season(7) runs first → then print() outputs the result
#   get_season(10) runs first → then print() outputs the result
#   get_season(month) → each argument is evaluated first, then the function body runs
#   get_season(25) runs first → then print() outputs the result
#   month in [12, 1, 2] → evaluated as True or False (determines which branch runs)
#   get_season(1) → each argument is evaluated first, then the function body runs
#   get_season(4) → each argument is evaluated first, then the function body runs
#   get_season(7) → each argument is evaluated first, then the function body runs
#   get_season(10) → each argument is evaluated first, then the function body runs
#   get_season(25) → each argument is evaluated first, then the function body runs
#   month in [3, 4, 5] → evaluated as True or False (determines which branch runs)
#   month in [6, 7, 8] → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_season(month) → stored in memory, not executed
#   2. print(get_season(...), get_season(...), get_season(...), get_season(...))
#      → calls get_season(month) with month=1
#      → 2.1 return 'Winter'
#   3. def get_season(month) → stored in memory, not executed
#   4. def get_season(month) → stored in memory, not executed
#   5. month = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(get_season(...))
#      → calls get_season(month) with month=25
#      → 8.1 return 'Winter'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_season) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(get_season(...), get_season(...), get_season(...), get_season(...)) → get_season() runs first → value passed to print()
#     month = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_season(...)) → get_season() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1854:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_discount(price, discount_percent) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * - /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_discount(100, 20) → each argument is evaluated first, then the function body runs
#   calculate_discount(price, discount_percent) → each argument is evaluated first, then the function body runs
#   calculate_discount(25, 25) runs first → then print() outputs the result
#   price * ? → Python looks up price, then ?, then applies *
#   calculate_discount(25, 25) → each argument is evaluated first, then the function body runs
#   1 - ? → Python looks up 1, then ?, then applies -
#   discount_percent / 100 → Python looks up discount_percent, then 100, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_discount(price, discount_percent) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_discount(price, discount_percent) → stored in memory, not executed
#   5. def calculate_discount(price, discount_percent) → stored in memory, not executed
#   6. price = ... → assigns value to variable
#   7. discount_percent = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_discount(...))
#      → calls calculate_discount(price, discount_percent) with price=25, discount_percent=25
#      → 10.1 return price * (1 - discount_percent / 100)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_discount) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     price = ... → stored in memory
#     discount_percent = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_discount(...)) → calculate_discount() runs first → value passed to print()

"""

"""
Problem 1855:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_tip(bill_amount, tip_percent) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_tip(100, 15) → each argument is evaluated first, then the function body runs
#   calculate_tip(bill_amount, tip_percent) → each argument is evaluated first, then the function body runs
#   calculate_tip(25, 25) runs first → then print() outputs the result
#   bill_amount * ? → Python looks up bill_amount, then ?, then applies *
#   bill_amount + tip → Python looks up bill_amount, then tip, then applies +
#   calculate_tip(25, 25) → each argument is evaluated first, then the function body runs
#   tip_percent / 100 → Python looks up tip_percent, then 100, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_tip(bill_amount, tip_percent) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_tip(bill_amount, tip_percent) → stored in memory, not executed
#   5. def calculate_tip(bill_amount, tip_percent) → stored in memory, not executed
#   6. bill_amount = ... → assigns value to variable
#   7. tip_percent = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_tip(...))
#      → calls calculate_tip(bill_amount, tip_percent) with bill_amount=25, tip_percent=25
#      → 10.1 return (tip, total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_tip) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     bill_amount = ... → stored in memory
#     tip_percent = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_tip(...)) → calculate_tip() runs first → value passed to print()

"""

"""
Problem 1856:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def swap_two_elements(my_list, index1, index2) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   swap_two_elements(?, 0, 3) → each argument is evaluated first, then the function body runs
#   swap_two_elements(my_list, index1, index2) → each argument is evaluated first, then the function body runs
#   swap_two_elements(?, 25, 25) runs first → then print() outputs the result
#   swap_two_elements(?, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def swap_two_elements(my_list, index1, index2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def swap_two_elements(my_list, index1, index2) → stored in memory, not executed
#   5. def swap_two_elements(my_list, index1, index2) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. index1 = ... → assigns value to variable
#   8. index2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(swap_two_elements(...))
#      → calls swap_two_elements(my_list, index1, index2) with my_list=[4, 1, 8, 6, 3], index1=25, index2=25
#      → 11.1 return new_list
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (swap_two_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     index1 = ... → stored in memory
#     index2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(swap_two_elements(...)) → swap_two_elements() runs first → value passed to print()

"""

"""
Problem 1857:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def insert_at_index(my_list, index, value) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   insert_at_index(?, 2, 99) → each argument is evaluated first, then the function body runs
#   insert_at_index(my_list, index, value) → each argument is evaluated first, then the function body runs
#   insert_at_index(?, 4, 25) runs first → then print() outputs the result
#   insert_at_index(?, 4, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def insert_at_index(my_list, index, value) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def insert_at_index(my_list, index, value) → stored in memory, not executed
#   5. def insert_at_index(my_list, index, value) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. index = ... → assigns value to variable
#   8. value = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(insert_at_index(...))
#      → calls insert_at_index(my_list, index, value) with my_list=[4, 1, 8, 6, 3], index=4, value=25
#      → 11.1 return new_list
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (insert_at_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     index = ... → stored in memory
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(insert_at_index(...)) → insert_at_index() runs first → value passed to print()

"""

"""
Problem 1858:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_at_index(my_list, index) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_at_index(?, 1) → each argument is evaluated first, then the function body runs
#   remove_at_index(my_list, index) → each argument is evaluated first, then the function body runs
#   remove_at_index(?, 4) runs first → then print() outputs the result
#   remove_at_index(?, 4) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_at_index(my_list, index) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_at_index(my_list, index) → stored in memory, not executed
#   5. def remove_at_index(my_list, index) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. index = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(remove_at_index(...))
#      → calls remove_at_index(my_list, index) with my_list=[4, 1, 8, 6, 3], index=4
#      → 10.1 return new_list
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_at_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     index = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_at_index(...)) → remove_at_index() runs first → value passed to print()

"""

"""
Problem 1860:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_min_max(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max, min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_min_max(?) → each argument is evaluated first, then the function body runs
#   get_min_max(numbers) → each argument is evaluated first, then the function body runs
#   get_min_max(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_min_max(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_min_max(numbers) → stored in memory, not executed
#   5. def get_min_max(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_min_max(...))
#      → calls get_min_max(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return (min(numbers), max(numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_min_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_min_max(...)) → get_min_max() runs first → value passed to print()

"""

"""
Problem 1861:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_conditions(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % > == and
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_conditions(6) runs first → then print() outputs the result
#   check_all_conditions(5) runs first → then print() outputs the result
#   check_all_conditions(number) → each argument is evaluated first, then the function body runs
#   check_all_conditions(25) runs first → then print() outputs the result
#   check_all_conditions(6) → each argument is evaluated first, then the function body runs
#   check_all_conditions(5) → each argument is evaluated first, then the function body runs
#   check_all_conditions(25) → each argument is evaluated first, then the function body runs
#   number > 0 → evaluated as True or False (determines which branch runs)
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#   number % 3 → Python looks up number, then 3, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_conditions(number) → stored in memory, not executed
#   2. print(check_all_conditions(...), check_all_conditions(...))
#      → calls check_all_conditions(number) with number=6
#      → 2.1 return number > 0 and number % 2 == 0 and (number % 3 == 0)
#   3. def check_all_conditions(number) → stored in memory, not executed
#   4. def check_all_conditions(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_all_conditions(...))
#      → calls check_all_conditions(number) with number=25
#      → 8.1 return number > 0 and number % 2 == 0 and (number % 3 == 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_conditions) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_all_conditions(...), check_all_conditions(...)) → check_all_conditions() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_conditions(...)) → check_all_conditions() runs first → value passed to print()

"""

"""
Problem 1862:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_full_name(first, middle, last) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_full_name('John', 'Michael', 'Doe') → each argument is evaluated first, then the function body runs
#   format_full_name(first, middle, last) → each argument is evaluated first, then the function body runs
#   format_full_name(25, 25, 25) runs first → then print() outputs the result
#   '%s %s %s' % ? → Python looks up '%s %s %s', then ?, then applies %
#   format_full_name(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_full_name(first, middle, last) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_full_name(first, middle, last) → stored in memory, not executed
#   5. def format_full_name(first, middle, last) → stored in memory, not executed
#   6. first = ... → assigns value to variable
#   7. middle = ... → assigns value to variable
#   8. last = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_full_name(...))
#      → calls format_full_name(first, middle, last) with first=25, middle=25, last=25
#      → 11.1 return '%s %s %s' % (first, middle, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_full_name) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     first = ... → stored in memory
#     middle = ... → stored in memory
#     last = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_full_name(...)) → format_full_name() runs first → value passed to print()

"""

"""
Problem 1863:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_bmi(weight_kg, height_m) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: / **
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_bmi(70, 1.75) → each argument is evaluated first, then the function body runs
#   calculate_bmi(weight_kg, height_m) → each argument is evaluated first, then the function body runs
#   calculate_bmi(25, 25) runs first → then print() outputs the result
#   weight_kg / ? → Python looks up weight_kg, then ?, then applies /
#   calculate_bmi(25, 25) → each argument is evaluated first, then the function body runs
#   height_m ** 2 → Python looks up height_m, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_bmi(weight_kg, height_m) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_bmi(weight_kg, height_m) → stored in memory, not executed
#   5. def calculate_bmi(weight_kg, height_m) → stored in memory, not executed
#   6. weight_kg = ... → assigns value to variable
#   7. height_m = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_bmi(...))
#      → calls calculate_bmi(weight_kg, height_m) with weight_kg=25, height_m=25
#      → 10.1 return round(bmi, 1)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_bmi) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     weight_kg = ... → stored in memory
#     height_m = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_bmi(...)) → calculate_bmi() runs first → value passed to print()

"""

"""
Problem 1864:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_bmi_category(weight_kg, height_m) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: / < **
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_bmi_category(70, 1.75) → each argument is evaluated first, then the function body runs
#   check_bmi_category(weight_kg, height_m) → each argument is evaluated first, then the function body runs
#   check_bmi_category(25, 25) runs first → then print() outputs the result
#   weight_kg / ? → Python looks up weight_kg, then ?, then applies /
#   bmi < 18.5 → evaluated as True or False (determines which branch runs)
#   check_bmi_category(25, 25) → each argument is evaluated first, then the function body runs
#   height_m ** 2 → Python looks up height_m, then 2, then applies **
#   bmi < 25 → evaluated as True or False (determines which branch runs)
#   bmi < 30 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_bmi_category(weight_kg, height_m) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_bmi_category(weight_kg, height_m) → stored in memory, not executed
#   5. def check_bmi_category(weight_kg, height_m) → stored in memory, not executed
#   6. weight_kg = ... → assigns value to variable
#   7. height_m = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(check_bmi_category(...))
#      → calls check_bmi_category(weight_kg, height_m) with weight_kg=25, height_m=25
#      → 10.1 return 'Underweight'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_bmi_category) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     weight_kg = ... → stored in memory
#     height_m = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_bmi_category(...)) → check_bmi_category() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1865:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_common_elements_three(list1, list2, list3) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in and
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_common_elements_three(?, ?, ?) → each argument is evaluated first, then the function body runs
#   find_common_elements_three(list1, list2, list3) → each argument is evaluated first, then the function body runs
#   find_common_elements_three(?, ?, ?) runs first → then print() outputs the result
#   x in list2 → evaluated as True or False (determines which branch runs)
#   x in list3 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_common_elements_three(list1, list2, list3) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_common_elements_three(list1, list2, list3) → stored in memory, not executed
#   5. def find_common_elements_three(list1, list2, list3) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. list3 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(find_common_elements_three(...))
#      → calls find_common_elements_three(list1, list2, list3) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3], list3=[4, 1, 8, 6, 3]
#      → 11.1 return [x for x in list1 if x in list2 and x in list3]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_common_elements_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     list3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_common_elements_three(...)) → find_common_elements_three() runs first → value passed to print()

"""

"""
Problem 1866:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_three_lists(list1, list2, list3) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_three_lists(?, ?, ?) → each argument is evaluated first, then the function body runs
#   merge_three_lists(list1, list2, list3) → each argument is evaluated first, then the function body runs
#   merge_three_lists(25, 25, ?) runs first → then print() outputs the result
#   ? + list3 → Python looks up ?, then list3, then applies +
#   merge_three_lists(25, 25, ?) → each argument is evaluated first, then the function body runs
#   list1 + list2 → Python looks up list1, then list2, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_three_lists(list1, list2, list3) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def merge_three_lists(list1, list2, list3) → stored in memory, not executed
#   5. def merge_three_lists(list1, list2, list3) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. list3 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(merge_three_lists(...))
#      → calls merge_three_lists(list1, list2, list3) with list1=25, list2=25, list3=[4, 1, 8, 6, 3]
#      → 11.1 return list1 + list2 + list3
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_three_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     list3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_three_lists(...)) → merge_three_lists() runs first → value passed to print()

"""

"""
Problem 1867:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_at_indices(my_list, indices) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_at_indices(?, ?) → each argument is evaluated first, then the function body runs
#   get_elements_at_indices(my_list, indices) → each argument is evaluated first, then the function body runs
#   get_elements_at_indices(?, ?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_at_indices(my_list, indices) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_at_indices(my_list, indices) → stored in memory, not executed
#   5. def get_elements_at_indices(my_list, indices) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. indices = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_elements_at_indices(...))
#      → calls get_elements_at_indices(my_list, indices) with my_list=[4, 1, 8, 6, 3], indices=[4, 1, 8, 6, 3]
#      → 10.1 return [my_list[i] for i in indices]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_at_indices) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     indices = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_at_indices(...)) → get_elements_at_indices() runs first → value passed to print()

"""

"""
Problem 1868:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def replace_multiple_chars(text, replacements) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   replace_multiple_chars('Hello World', ?) → each argument is evaluated first, then the function body runs
#   replace_multiple_chars(text, replacements) → each argument is evaluated first, then the function body runs
#   replace_multiple_chars('world', 25) runs first → then print() outputs the result
#   replace_multiple_chars('world', 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def replace_multiple_chars(text, replacements) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def replace_multiple_chars(text, replacements) → stored in memory, not executed
#   5. def replace_multiple_chars(text, replacements) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. replacements = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(replace_multiple_chars(...))
#      → calls replace_multiple_chars(text, replacements) with text='world', replacements=25
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (replace_multiple_chars) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     replacements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(replace_multiple_chars(...)) → replace_multiple_chars() runs first → value passed to print()
#
#   Loop: takes one item from replacements.items(...) each round, runs body, repeats until done

"""

"""
Problem 1869:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words_by_length(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words_by_length('hello world python programming') → each argument is evaluated first, then the function body runs
#   count_words_by_length(text) → each argument is evaluated first, then the function body runs
#   count_words_by_length('world') runs first → then print() outputs the result
#   count_words_by_length('world') → each argument is evaluated first, then the function body runs
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words_by_length(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_words_by_length(text) → stored in memory, not executed
#   5. def count_words_by_length(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_words_by_length(...))
#      → calls count_words_by_length(text) with text='world'
#      → 9.1 return counts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words_by_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words_by_length(...)) → count_words_by_length() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 1870:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_and_transform(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % * ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_and_transform(?) → each argument is evaluated first, then the function body runs
#   filter_and_transform(numbers) → each argument is evaluated first, then the function body runs
#   filter_and_transform(?) runs first → then print() outputs the result
#   x * 2 → Python looks up x, then 2, then applies *
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_and_transform(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def filter_and_transform(numbers) → stored in memory, not executed
#   5. def filter_and_transform(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(filter_and_transform(...))
#      → calls filter_and_transform(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x * 2 for x in numbers if x % 2 == 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_and_transform) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_and_transform(...)) → filter_and_transform() runs first → value passed to print()

"""

"""
Problem 1871:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_starting_with(strings, prefix) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_starting_with(?, 'ap') → each argument is evaluated first, then the function body runs
#   get_strings_starting_with(strings, prefix) → each argument is evaluated first, then the function body runs
#   get_strings_starting_with(?, 'Hi, ') runs first → then print() outputs the result
#   get_strings_starting_with(?, 'Hi, ') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_starting_with(strings, prefix) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_starting_with(strings, prefix) → stored in memory, not executed
#   5. def get_strings_starting_with(strings, prefix) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. prefix = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_strings_starting_with(...))
#      → calls get_strings_starting_with(strings, prefix) with strings=[4, 1, 8, 6, 3], prefix='Hi, '
#      → 10.1 return [s for s in strings if s.startswith(prefix)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_starting_with) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     prefix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_starting_with(...)) → get_strings_starting_with() runs first → value passed to print()

"""

"""
Problem 1872:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_ending_with(strings, suffix) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_ending_with(?, 'ing') → each argument is evaluated first, then the function body runs
#   get_strings_ending_with(strings, suffix) → each argument is evaluated first, then the function body runs
#   get_strings_ending_with(?, '?') runs first → then print() outputs the result
#   get_strings_ending_with(?, '?') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_ending_with(strings, suffix) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_ending_with(strings, suffix) → stored in memory, not executed
#   5. def get_strings_ending_with(strings, suffix) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. suffix = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_strings_ending_with(...))
#      → calls get_strings_ending_with(strings, suffix) with strings=[4, 1, 8, 6, 3], suffix='?'
#      → 10.1 return [s for s in strings if s.endswith(suffix)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_ending_with) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     suffix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_ending_with(...)) → get_strings_ending_with() runs first → value passed to print()

"""

"""
Problem 1873:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_total_cost(prices, tax_rate) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_total_cost(?, 10) → each argument is evaluated first, then the function body runs
#   calculate_total_cost(prices, tax_rate) → each argument is evaluated first, then the function body runs
#   calculate_total_cost(?, 25) runs first → then print() outputs the result
#   subtotal * ? → Python looks up subtotal, then ?, then applies *
#   subtotal + tax → Python looks up subtotal, then tax, then applies +
#   calculate_total_cost(?, 25) → each argument is evaluated first, then the function body runs
#   tax_rate / 100 → Python looks up tax_rate, then 100, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_total_cost(prices, tax_rate) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_total_cost(prices, tax_rate) → stored in memory, not executed
#   5. def calculate_total_cost(prices, tax_rate) → stored in memory, not executed
#   6. prices = ... → assigns value to variable
#   7. tax_rate = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_total_cost(...))
#      → calls calculate_total_cost(prices, tax_rate) with prices=[4, 1, 8, 6, 3], tax_rate=25
#      → 10.1 import operator
#      → 10.2 return operator.add(subtotal, tax)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_total_cost) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     prices = ... → stored in memory
#     tax_rate = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_total_cost(...)) → calculate_total_cost() runs first → value passed to print()

"""

"""
Problem 1874:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_all_indices(my_list, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_all_indices(?, 10) → each argument is evaluated first, then the function body runs
#   find_all_indices(my_list, element) → each argument is evaluated first, then the function body runs
#   find_all_indices(?, 25) runs first → then print() outputs the result
#   find_all_indices(?, 25) → each argument is evaluated first, then the function body runs
#   x == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_all_indices(my_list, element) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_all_indices(my_list, element) → stored in memory, not executed
#   5. def find_all_indices(my_list, element) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. element = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(find_all_indices(...))
#      → calls find_all_indices(my_list, element) with my_list=[4, 1, 8, 6, 3], element=25
#      → 10.1 return [i for i, x in enumerate(my_list) if x == element]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_all_indices) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_all_indices(...)) → find_all_indices() runs first → value passed to print()

"""

"""
Problem 1875:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def split_into_chunks(my_list, chunk_size) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   split_into_chunks(?, 3) → each argument is evaluated first, then the function body runs
#   split_into_chunks(my_list, chunk_size) → each argument is evaluated first, then the function body runs
#   split_into_chunks(?, 25) runs first → then print() outputs the result
#   split_into_chunks(?, 25) → each argument is evaluated first, then the function body runs
#   i + chunk_size → Python looks up i, then chunk_size, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def split_into_chunks(my_list, chunk_size) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def split_into_chunks(my_list, chunk_size) → stored in memory, not executed
#   5. def split_into_chunks(my_list, chunk_size) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. chunk_size = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(split_into_chunks(...))
#      → calls split_into_chunks(my_list, chunk_size) with my_list=[4, 1, 8, 6, 3], chunk_size=25
#      → 10.1 return [my_list[i:i + chunk_size] for i in range(0, len(my_list), chunk_size)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (split_into_chunks) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     chunk_size = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(split_into_chunks(...)) → split_into_chunks() runs first → value passed to print()

"""

"""
Problem 1876:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_multiple_elements(my_list, elements_to_remove) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_multiple_elements(?, ?) → each argument is evaluated first, then the function body runs
#   remove_multiple_elements(my_list, elements_to_remove) → each argument is evaluated first, then the function body runs
#   remove_multiple_elements(?, ?) runs first → then print() outputs the result
#   x not in elements_to_remove → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_multiple_elements(my_list, elements_to_remove) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_multiple_elements(my_list, elements_to_remove) → stored in memory, not executed
#   5. def remove_multiple_elements(my_list, elements_to_remove) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. elements_to_remove = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(remove_multiple_elements(...))
#      → calls remove_multiple_elements(my_list, elements_to_remove) with my_list=[4, 1, 8, 6, 3], elements_to_remove=[4, 1, 8, 6, 3]
#      → 10.1 return [x for x in my_list if x not in elements_to_remove]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_multiple_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     elements_to_remove = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_multiple_elements(...)) → remove_multiple_elements() runs first → value passed to print()

"""

"""
Problem 1877:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_occurrences_multiple(my_list, elements) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_occurrences_multiple(?, ?) → each argument is evaluated first, then the function body runs
#   count_occurrences_multiple(my_list, elements) → each argument is evaluated first, then the function body runs
#   count_occurrences_multiple(?, ?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_occurrences_multiple(my_list, elements) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_occurrences_multiple(my_list, elements) → stored in memory, not executed
#   5. def count_occurrences_multiple(my_list, elements) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. elements = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(count_occurrences_multiple(...))
#      → calls count_occurrences_multiple(my_list, elements) with my_list=[4, 1, 8, 6, 3], elements=[5, 3, 8, 4]
#      → 10.1 return {elem: my_list.count(elem) for elem in elements}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_occurrences_multiple) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     elements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_occurrences_multiple(...)) → count_occurrences_multiple() runs first → value passed to print()

"""

"""
Problem 1878:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_unique_elements_three(list1, list2, list3) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: list, map, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_unique_elements_three(?, ?, ?) → each argument is evaluated first, then the function body runs
#   get_unique_elements_three(list1, list2, list3) → each argument is evaluated first, then the function body runs
#   get_unique_elements_three(25, 25, ?) runs first → then print() outputs the result
#   ? + list3 → Python looks up ?, then list3, then applies +
#   get_unique_elements_three(25, 25, ?) → each argument is evaluated first, then the function body runs
#   list1 + list2 → Python looks up list1, then list2, then applies +
#   ? + list3.count(...) → Python looks up ?, then list3.count(...), then applies +
#   count == 1 → evaluated as True or False (determines which branch runs)
#   list1.count(...) + list2.count(...) → Python looks up list1.count(...), then list2.count(...), then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_unique_elements_three(list1, list2, list3) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(sorted(...))
#   4. def get_unique_elements_three(list1, list2, list3) → stored in memory, not executed
#   5. def get_unique_elements_three(list1, list2, list3) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. list3 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_unique_elements_three(...))
#      → calls get_unique_elements_three(list1, list2, list3) with list1=25, list2=25, list3=[4, 1, 8, 6, 3]
#      → 11.1 return list(map(lambda elem: elem, all_elements))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_unique_elements_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(sorted(...))
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     list3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_unique_elements_three(...)) → get_unique_elements_three() runs first → value passed to print()
#
#   Loop: takes one item from all_elements each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1879:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_by_multiple_criteria(people) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_multiple_criteria(?) → each argument is evaluated first, then the function body runs
#   sort_by_multiple_criteria(people) → each argument is evaluated first, then the function body runs
#   sort_by_multiple_criteria(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_by_multiple_criteria(people) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_by_multiple_criteria(people) → stored in memory, not executed
#   5. def sort_by_multiple_criteria(people) → stored in memory, not executed
#   6. people = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_by_multiple_criteria(...))
#      → calls sort_by_multiple_criteria(people) with people=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(people, key=lambda x: (x[1], x[2]))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_multiple_criteria) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     people = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_by_multiple_criteria(...)) → sort_by_multiple_criteria() runs first → value passed to print()

"""

"""
Problem 1880:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_statistics(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: /
# Built-in functions: len, max, min, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_statistics(?) → each argument is evaluated first, then the function body runs
#   calculate_statistics(numbers) → each argument is evaluated first, then the function body runs
#   calculate_statistics(?) runs first → then print() outputs the result
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_statistics(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_statistics(...))
#      → calls calculate_statistics(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return {'sum': sum(numbers), 'average': sum(numbers) / len(numbers), 'min': min(numbers), 'max': max(numbers)}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, calculate_statistics) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_statistics(...)) → calculate_statistics() runs first → value passed to print()

"""

"""
Problem 1881:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def extract_numbers_from_string(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   extract_numbers_from_string('I have 3 cats and 2 dogs') → each argument is evaluated first, then the function body runs
#   extract_numbers_from_string(text) → each argument is evaluated first, then the function body runs
#   extract_numbers_from_string('world') runs first → then print() outputs the result
#   extract_numbers_from_string('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def extract_numbers_from_string(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def extract_numbers_from_string(text) → stored in memory, not executed
#   5. def extract_numbers_from_string(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(extract_numbers_from_string(...))
#      → calls extract_numbers_from_string(text) with text='world'
#      → 9.1 return [int(match) for match in re.findall('\\d+', text)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (extract_numbers_from_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(extract_numbers_from_string(...)) → extract_numbers_from_string() runs first → value passed to print()

"""

"""
Problem 1883:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_password_strength(password) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >= and
# Built-in functions: any, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_password_strength('Password123') runs first → then print() outputs the result
#   check_password_strength('weak') runs first → then print() outputs the result
#   check_password_strength(password) → each argument is evaluated first, then the function body runs
#   check_password_strength(?) runs first → then print() outputs the result
#   check_password_strength('Password123') → each argument is evaluated first, then the function body runs
#   check_password_strength('weak') → each argument is evaluated first, then the function body runs
#   check_password_strength(?) → each argument is evaluated first, then the function body runs
#   len(...) >= 8 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_password_strength(password) → stored in memory, not executed
#   2. print(check_password_strength(...), check_password_strength(...))
#      → calls check_password_strength(password) with password='Password123'
#      → 2.1 return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))
#   3. def check_password_strength(password) → stored in memory, not executed
#   4. def check_password_strength(password) → stored in memory, not executed
#   5. password = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_password_strength(...))
#      → calls check_password_strength(password) with password=[4, 1, 8, 6, 3]
#      → 8.1 return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_password_strength) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_password_strength(...), check_password_strength(...)) → check_password_strength() runs first → value passed to print()
#     password = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_password_strength(...)) → check_password_strength() runs first → value passed to print()

"""

"""
Problem 1884:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_by_length_range(text, min_length, max_length) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <=
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_by_length_range('hello world python programming', 5, 6) → each argument is evaluated first, then the function body runs
#   get_words_by_length_range(text, min_length, max_length) → each argument is evaluated first, then the function body runs
#   get_words_by_length_range('world', 25, 25) runs first → then print() outputs the result
#   get_words_by_length_range('world', 25, 25) → each argument is evaluated first, then the function body runs
#   min_length <= len(...) → evaluated as True or False (determines which branch runs)
#   min_length <= max_length → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_by_length_range(text, min_length, max_length) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_by_length_range(text, min_length, max_length) → stored in memory, not executed
#   5. def get_words_by_length_range(text, min_length, max_length) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. min_length = ... → assigns value to variable
#   8. max_length = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_words_by_length_range(...))
#      → calls get_words_by_length_range(text, min_length, max_length) with text='world', min_length=25, max_length=25
#      → 11.1 return [word for word in words if min_length <= len(word) <= max_length]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_by_length_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     min_length = ... → stored in memory
#     max_length = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_by_length_range(...)) → get_words_by_length_range() runs first → value passed to print()

"""

"""
Problem 1885:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_compound_interest(principal, rate, time, frequency) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + / **
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_compound_interest(1000, 5, 2, 12) → each argument is evaluated first, then the function body runs
#   calculate_compound_interest(principal, rate, time, frequency) → each argument is evaluated first, then the function body runs
#   calculate_compound_interest(25, 25, 25, 25) runs first → then print() outputs the result
#   principal * ? → Python looks up principal, then ?, then applies *
#   calculate_compound_interest(25, 25, 25, 25) → each argument is evaluated first, then the function body runs
#   ? ** ? → Python looks up ?, then ?, then applies **
#   1 + ? → Python looks up 1, then ?, then applies +
#   frequency * time → Python looks up frequency, then time, then applies *
#   rate / ? → Python looks up rate, then ?, then applies /
#   100 * frequency → Python looks up 100, then frequency, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_compound_interest(principal, rate, time, frequency) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(round(...))
#   4. def calculate_compound_interest(principal, rate, time, frequency) → stored in memory, not executed
#   5. def calculate_compound_interest(principal, rate, time, frequency) → stored in memory, not executed
#   6. principal = ... → assigns value to variable
#   7. rate = ... → assigns value to variable
#   8. time = ... → assigns value to variable
#   9. frequency = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(calculate_compound_interest(...))
#      → calls calculate_compound_interest(principal, rate, time, frequency) with principal=25, rate=25, time=25, frequency=25
#      → 12.1 return principal * (1 + rate / (100 * frequency)) ** (frequency * time)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_compound_interest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(round(...))
#     principal = ... → stored in memory
#     rate = ... → stored in memory
#     time = ... → stored in memory
#     frequency = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_compound_interest(...)) → calculate_compound_interest() runs first → value passed to print()

"""

"""
Problem 1886:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_duplicate_elements(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: list, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_duplicate_elements(?) → each argument is evaluated first, then the function body runs
#   find_duplicate_elements(my_list) → each argument is evaluated first, then the function body runs
#   find_duplicate_elements(?) runs first → then print() outputs the result
#   item in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_duplicate_elements(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(sorted(...))
#   4. def find_duplicate_elements(my_list) → stored in memory, not executed
#   5. def find_duplicate_elements(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(find_duplicate_elements(...))
#      → calls find_duplicate_elements(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return list(duplicates)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_duplicate_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(sorted(...))
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_duplicate_elements(...)) → find_duplicate_elements() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1887:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def group_by_first_letter(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   group_by_first_letter(?) → each argument is evaluated first, then the function body runs
#   group_by_first_letter(strings) → each argument is evaluated first, then the function body runs
#   group_by_first_letter(?) runs first → then print() outputs the result
#   first_letter not in groups → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def group_by_first_letter(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def group_by_first_letter(strings) → stored in memory, not executed
#   5. def group_by_first_letter(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(group_by_first_letter(...))
#      → calls group_by_first_letter(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return groups
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (group_by_first_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(group_by_first_letter(...)) → group_by_first_letter() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1888:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_distance(x1, y1, x2, y2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + - **
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_distance(0, 0, 3, 4) → each argument is evaluated first, then the function body runs
#   calculate_distance(x1, y1, x2, y2) → each argument is evaluated first, then the function body runs
#   calculate_distance(25, 25, 25, 25) runs first → then print() outputs the result
#   calculate_distance(25, 25, 25, 25) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   ? ** 2 → Python looks up ?, then 2, then applies **
#   x2 - x1 → Python looks up x2, then x1, then applies -
#   y2 - y1 → Python looks up y2, then y1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def calculate_distance(x1, y1, x2, y2) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def calculate_distance(x1, y1, x2, y2) → stored in memory, not executed
#   6. def calculate_distance(x1, y1, x2, y2) → stored in memory, not executed
#   7. x1 = ... → assigns value to variable
#   8. y1 = ... → assigns value to variable
#   9. x2 = ... → assigns value to variable
#   10. y2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(calculate_distance(...))
#      → calls calculate_distance(x1, y1, x2, y2) with x1=25, y1=25, x2=25, y2=25
#      → 13.1 return math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_distance) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     x1 = ... → stored in memory
#     y1 = ... → stored in memory
#     x2 = ... → stored in memory
#     y2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_distance(...)) → calculate_distance() runs first → value passed to print()

"""

"""
Problem 1889:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_in_range(number, min_val, max_val) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_in_range(5, 1, 10) runs first → then print() outputs the result
#   check_in_range(15, 1, 10) runs first → then print() outputs the result
#   check_in_range(number, min_val, max_val) → each argument is evaluated first, then the function body runs
#   check_in_range(25, 25, 25) runs first → then print() outputs the result
#   min_val <= number → evaluated as True or False (determines which branch runs)
#   min_val <= max_val → evaluated as True or False (determines which branch runs)
#   check_in_range(5, 1, 10) → each argument is evaluated first, then the function body runs
#   check_in_range(15, 1, 10) → each argument is evaluated first, then the function body runs
#   check_in_range(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_in_range(number, min_val, max_val) → stored in memory, not executed
#   2. print(check_in_range(...), check_in_range(...))
#      → calls check_in_range(number, min_val, max_val) with number=5, min_val=1, max_val=10
#      → 2.1 return min_val <= number <= max_val
#   3. def check_in_range(number, min_val, max_val) → stored in memory, not executed
#   4. def check_in_range(number, min_val, max_val) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. min_val = ... → assigns value to variable
#   7. max_val = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(check_in_range(...))
#      → calls check_in_range(number, min_val, max_val) with number=25, min_val=25, max_val=25
#      → 10.1 return min_val <= number <= max_val
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_in_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_in_range(...), check_in_range(...)) → check_in_range() runs first → value passed to print()
#     number = ... → stored in memory
#     min_val = ... → stored in memory
#     max_val = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_in_range(...)) → check_in_range() runs first → value passed to print()

"""

"""
Problem 1890:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_divisors(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % + ==
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_divisors(12) → each argument is evaluated first, then the function body runs
#   get_divisors(number) → each argument is evaluated first, then the function body runs
#   get_divisors(25) runs first → then print() outputs the result
#   get_divisors(25) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   number + 1 → Python looks up number, then 1, then applies +
#   number % i → Python looks up number, then i, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_divisors(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_divisors(number) → stored in memory, not executed
#   5. def get_divisors(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_divisors(...))
#      → calls get_divisors(number) with number=25
#      → 9.1 return [i for i in range(1, number + 1) if number % i == 0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_divisors) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_divisors(...)) → get_divisors() runs first → value passed to print()

"""

"""
Problem 1891:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_perfect_square(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_perfect_square(16) runs first → then print() outputs the result
#   check_perfect_square(15) runs first → then print() outputs the result
#   check_perfect_square(number) → each argument is evaluated first, then the function body runs
#   check_perfect_square(25) runs first → then print() outputs the result
#   root == int(...) → evaluated as True or False (determines which branch runs)
#   check_perfect_square(16) → each argument is evaluated first, then the function body runs
#   check_perfect_square(15) → each argument is evaluated first, then the function body runs
#   check_perfect_square(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_perfect_square(number) → stored in memory, not executed
#   2. print(check_perfect_square(...), check_perfect_square(...))
#      → calls check_perfect_square(number) with number=16
#      → 2.1 return root == int(root)
#   3. def check_perfect_square(number) → stored in memory, not executed
#   4. def check_perfect_square(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(check_perfect_square(...))
#      → calls check_perfect_square(number) with number=25
#      → 8.1 return root == int(root)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_perfect_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_perfect_square(...), check_perfect_square(...)) → check_perfect_square() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_perfect_square(...)) → check_perfect_square() runs first → value passed to print()

"""

"""
Problem 1892:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_time(hours, minutes, seconds) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_time(9, 5, 3) → each argument is evaluated first, then the function body runs
#   format_time(hours, minutes, seconds) → each argument is evaluated first, then the function body runs
#   format_time(25, 25, 25) runs first → then print() outputs the result
#   format_time(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_time(hours, minutes, seconds) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_time(hours, minutes, seconds) → stored in memory, not executed
#   5. def format_time(hours, minutes, seconds) → stored in memory, not executed
#   6. hours = ... → assigns value to variable
#   7. minutes = ... → assigns value to variable
#   8. seconds = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_time(...))
#      → calls format_time(hours, minutes, seconds) with hours=25, minutes=25, seconds=25
#      → 11.1 return f'{hours:02d}:{minutes:02d}:{seconds:02d}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_time) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     hours = ... → stored in memory
#     minutes = ... → stored in memory
#     seconds = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_time(...)) → format_time() runs first → value passed to print()

"""

"""
Problem 1893:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_age(birth_year, birth_month, birth_day) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: + - <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_age(2000, 6, 15) → each argument is evaluated first, then the function body runs
#   calculate_age(birth_year, birth_month, birth_day) → each argument is evaluated first, then the function body runs
#   calculate_age(25, 25, 25) runs first → then print() outputs the result
#   current_year - birth_year → Python looks up current_year, then birth_year, then applies -
#   ? < ? → evaluated as True or False (determines which branch runs)
#   current_year + ? → Python looks up current_year, then ?, then applies +
#   calculate_age(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_age(birth_year, birth_month, birth_day) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_age(birth_year, birth_month, birth_day) → stored in memory, not executed
#   5. def calculate_age(birth_year, birth_month, birth_day) → stored in memory, not executed
#   6. birth_year = ... → assigns value to variable
#   7. birth_month = ... → assigns value to variable
#   8. birth_day = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_age(...))
#      → calls calculate_age(birth_year, birth_month, birth_day) with birth_year=25, birth_month=25, birth_day=25
#      → 11.1 import operator
#      → 11.2 return operator.sub(current_year, birth_year)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_age) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     birth_year = ... → stored in memory
#     birth_month = ... → stored in memory
#     birth_day = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_age(...)) → calculate_age() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1894:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_middle_elements(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_middle_elements(?) → each argument is evaluated first, then the function body runs
#   get_middle_elements(my_list) → each argument is evaluated first, then the function body runs
#   get_middle_elements(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_middle_elements(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_middle_elements(my_list) → stored in memory, not executed
#   5. def get_middle_elements(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_middle_elements(...))
#      → calls get_middle_elements(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return my_list[1:-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_middle_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_middle_elements(...)) → get_middle_elements() runs first → value passed to print()

"""

"""
Problem 1895:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def swap_adjacent_elements(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: + -
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   swap_adjacent_elements(?) → each argument is evaluated first, then the function body runs
#   swap_adjacent_elements(my_list) → each argument is evaluated first, then the function body runs
#   swap_adjacent_elements(?) runs first → then print() outputs the result
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def swap_adjacent_elements(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def swap_adjacent_elements(my_list) → stored in memory, not executed
#   5. def swap_adjacent_elements(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(swap_adjacent_elements(...))
#      → calls swap_adjacent_elements(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return new_list
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (swap_adjacent_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(swap_adjacent_elements(...)) → swap_adjacent_elements() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 1896:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_every_nth_element(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_every_nth_element(?, 3) → each argument is evaluated first, then the function body runs
#   get_every_nth_element(my_list, n) → each argument is evaluated first, then the function body runs
#   get_every_nth_element(?, 15) runs first → then print() outputs the result
#   get_every_nth_element(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_every_nth_element(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_every_nth_element(my_list, n) → stored in memory, not executed
#   5. def get_every_nth_element(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_every_nth_element(...))
#      → calls get_every_nth_element(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return my_list[::n]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_every_nth_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_every_nth_element(...)) → get_every_nth_element() runs first → value passed to print()

"""

"""
Problem 1898:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates_preserve_order(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates_preserve_order(?) → each argument is evaluated first, then the function body runs
#   remove_duplicates_preserve_order(my_list) → each argument is evaluated first, then the function body runs
#   remove_duplicates_preserve_order(?) runs first → then print() outputs the result
#   item not in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates_preserve_order(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def remove_duplicates_preserve_order(my_list) → stored in memory, not executed
#   5. def remove_duplicates_preserve_order(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_duplicates_preserve_order(...))
#      → calls remove_duplicates_preserve_order(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return list(map(lambda item: item, my_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates_preserve_order) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates_preserve_order(...)) → remove_duplicates_preserve_order() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1899:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_intersection_three_lists(list1, list2, list3) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in and
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_intersection_three_lists(?, ?, ?) → each argument is evaluated first, then the function body runs
#   get_intersection_three_lists(list1, list2, list3) → each argument is evaluated first, then the function body runs
#   get_intersection_three_lists(?, ?, ?) runs first → then print() outputs the result
#   x in list2 → evaluated as True or False (determines which branch runs)
#   x in list3 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_intersection_three_lists(list1, list2, list3) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_intersection_three_lists(list1, list2, list3) → stored in memory, not executed
#   5. def get_intersection_three_lists(list1, list2, list3) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. list3 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_intersection_three_lists(...))
#      → calls get_intersection_three_lists(list1, list2, list3) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3], list3=[4, 1, 8, 6, 3]
#      → 11.1 return [x for x in list1 if x in list2 and x in list3]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_intersection_three_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     list3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_intersection_three_lists(...)) → get_intersection_three_lists() runs first → value passed to print()

"""

"""
Problem 1900:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_percentage(part, whole) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_percentage(25, 100) → each argument is evaluated first, then the function body runs
#   calculate_percentage(part, whole) → each argument is evaluated first, then the function body runs
#   calculate_percentage(25, 25) runs first → then print() outputs the result
#   calculate_percentage(25, 25) → each argument is evaluated first, then the function body runs
#   ? * 100 → Python looks up ?, then 100, then applies *
#   part / whole → Python looks up part, then whole, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_percentage(part, whole) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_percentage(part, whole) → stored in memory, not executed
#   5. def calculate_percentage(part, whole) → stored in memory, not executed
#   6. part = ... → assigns value to variable
#   7. whole = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_percentage(...))
#      → calls calculate_percentage(part, whole) with part=25, whole=25
#      → 10.1 return round(part / whole * 100, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_percentage) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     part = ... → stored in memory
#     whole = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_percentage(...)) → calculate_percentage() runs first → value passed to print()

"""

"""
Problem 1901:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_largest_three(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_largest_three(?) → each argument is evaluated first, then the function body runs
#   find_largest_three(numbers) → each argument is evaluated first, then the function body runs
#   find_largest_three(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_largest_three(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_largest_three(numbers) → stored in memory, not executed
#   5. def find_largest_three(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(find_largest_three(...))
#      → calls find_largest_three(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(numbers, reverse=True)[:3]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_largest_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_largest_three(...)) → find_largest_three() runs first → value passed to print()

"""

"""
Problem 1902:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_smallest_three(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_smallest_three(?) → each argument is evaluated first, then the function body runs
#   find_smallest_three(numbers) → each argument is evaluated first, then the function body runs
#   find_smallest_three(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_smallest_three(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_smallest_three(numbers) → stored in memory, not executed
#   5. def find_smallest_three(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(find_smallest_three(...))
#      → calls find_smallest_three(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(numbers)[:3]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_smallest_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_smallest_three(...)) → find_smallest_three() runs first → value passed to print()

"""

"""
Problem 1903:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_palindrome_and_length(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_palindrome_and_length('racecar') → each argument is evaluated first, then the function body runs
#   check_palindrome_and_length(text) → each argument is evaluated first, then the function body runs
#   check_palindrome_and_length('world') runs first → then print() outputs the result
#   text == ? → evaluated as True or False (determines which branch runs)
#   check_palindrome_and_length('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_palindrome_and_length(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_palindrome_and_length(text) → stored in memory, not executed
#   5. def check_palindrome_and_length(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_palindrome_and_length(...))
#      → calls check_palindrome_and_length(text) with text='world'
#      → 9.1 return (is_palindrome, length)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_palindrome_and_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_palindrome_and_length(...)) → check_palindrome_and_length() runs first → value passed to print()

"""

"""
Problem 1904:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_starting_with_vowel(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_starting_with_vowel('apple orange banana cherry') → each argument is evaluated first, then the function body runs
#   get_words_starting_with_vowel(text) → each argument is evaluated first, then the function body runs
#   get_words_starting_with_vowel('world') runs first → then print() outputs the result
#   get_words_starting_with_vowel('world') → each argument is evaluated first, then the function body runs
#   ? in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_starting_with_vowel(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_starting_with_vowel(text) → stored in memory, not executed
#   5. def get_words_starting_with_vowel(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_words_starting_with_vowel(...))
#      → calls get_words_starting_with_vowel(text) with text='world'
#      → 9.1 return [word for word in words if word[0] in vowels]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_starting_with_vowel) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_starting_with_vowel(...)) → get_words_starting_with_vowel() runs first → value passed to print()

"""

"""
Problem 1905:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_ending_with_consonant(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: and not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_ending_with_consonant('hello world python') → each argument is evaluated first, then the function body runs
#   get_words_ending_with_consonant(text) → each argument is evaluated first, then the function body runs
#   get_words_ending_with_consonant('world') runs first → then print() outputs the result
#   get_words_ending_with_consonant('world') → each argument is evaluated first, then the function body runs
#   ? not in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_ending_with_consonant(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_ending_with_consonant(text) → stored in memory, not executed
#   5. def get_words_ending_with_consonant(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_words_ending_with_consonant(...))
#      → calls get_words_ending_with_consonant(text) with text='world'
#      → 9.1 return [word for word in words if word[-1] not in vowels and word[-1].isalpha()]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_ending_with_consonant) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_ending_with_consonant(...)) → get_words_ending_with_consonant() runs first → value passed to print()

"""

"""
Problem 1906:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_area_perimeter(length, width) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_area_perimeter(5, 3) → each argument is evaluated first, then the function body runs
#   calculate_area_perimeter(length, width) → each argument is evaluated first, then the function body runs
#   calculate_area_perimeter(25, 25) runs first → then print() outputs the result
#   length * width → Python looks up length, then width, then applies *
#   2 * ? → Python looks up 2, then ?, then applies *
#   calculate_area_perimeter(25, 25) → each argument is evaluated first, then the function body runs
#   length + width → Python looks up length, then width, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_area_perimeter(length, width) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_area_perimeter(length, width) → stored in memory, not executed
#   5. def calculate_area_perimeter(length, width) → stored in memory, not executed
#   6. length = ... → assigns value to variable
#   7. width = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_area_perimeter(...))
#      → calls calculate_area_perimeter(length, width) with length=25, width=25
#      → 10.1 return (area, perimeter)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_area_perimeter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_area_perimeter(...)) → calculate_area_perimeter() runs first → value passed to print()

"""

"""
Problem 1907:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_currency(amount) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_currency(1234.5) → each argument is evaluated first, then the function body runs
#   format_currency(amount) → each argument is evaluated first, then the function body runs
#   format_currency(25) runs first → then print() outputs the result
#   format_currency(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_currency(amount) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_currency(amount) → stored in memory, not executed
#   5. def format_currency(amount) → stored in memory, not executed
#   6. amount = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(format_currency(...))
#      → calls format_currency(amount) with amount=25
#      → 9.1 return f'${amount:.2f}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_currency) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     amount = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_currency(...)) → format_currency() runs first → value passed to print()

"""

"""
Problem 1908:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_even_indices(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_even_indices(?) → each argument is evaluated first, then the function body runs
#   get_even_indices(my_list) → each argument is evaluated first, then the function body runs
#   get_even_indices(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_even_indices(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_even_indices(my_list) → stored in memory, not executed
#   5. def get_even_indices(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_even_indices(...))
#      → calls get_even_indices(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return [my_list[i] for i in range(0, len(my_list), 2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_even_indices) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_even_indices(...)) → get_even_indices() runs first → value passed to print()

"""

"""
Problem 1909:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_odd_indices(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_odd_indices(?) → each argument is evaluated first, then the function body runs
#   get_odd_indices(my_list) → each argument is evaluated first, then the function body runs
#   get_odd_indices(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_odd_indices(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_odd_indices(my_list) → stored in memory, not executed
#   5. def get_odd_indices(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_odd_indices(...))
#      → calls get_odd_indices(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return [my_list[i] for i in range(1, len(my_list), 2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_odd_indices) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_odd_indices(...)) → get_odd_indices() runs first → value passed to print()

"""

"""
Problem 1910:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_corresponding(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_corresponding(?, ?) → each argument is evaluated first, then the function body runs
#   multiply_corresponding(list1, list2) → each argument is evaluated first, then the function body runs
#   multiply_corresponding(?, ?) runs first → then print() outputs the result
#   a * b → Python looks up a, then b, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_corresponding(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def multiply_corresponding(list1, list2) → stored in memory, not executed
#   5. def multiply_corresponding(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(multiply_corresponding(...))
#      → calls multiply_corresponding(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return [a * b for a, b in zip(list1, list2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_corresponding) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_corresponding(...)) → multiply_corresponding() runs first → value passed to print()

"""

"""
Problem 1911:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def divide_corresponding(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: / ==
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   divide_corresponding(?, ?) → each argument is evaluated first, then the function body runs
#   divide_corresponding(list1, list2) → each argument is evaluated first, then the function body runs
#   divide_corresponding(?, ?) runs first → then print() outputs the result
#   b == 0 → evaluated as True or False (determines which branch runs)
#   a / b → Python looks up a, then b, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def divide_corresponding(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def divide_corresponding(list1, list2) → stored in memory, not executed
#   5. def divide_corresponding(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(divide_corresponding(...))
#      → calls divide_corresponding(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (divide_corresponding) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(divide_corresponding(...)) → divide_corresponding() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1912:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_containing(strings, substring) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_containing(?, 'on') → each argument is evaluated first, then the function body runs
#   get_strings_containing(strings, substring) → each argument is evaluated first, then the function body runs
#   get_strings_containing(?, 'world') runs first → then print() outputs the result
#   get_strings_containing(?, 'world') → each argument is evaluated first, then the function body runs
#   substring in s → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_containing(strings, substring) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_containing(strings, substring) → stored in memory, not executed
#   5. def get_strings_containing(strings, substring) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. substring = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_strings_containing(...))
#      → calls get_strings_containing(strings, substring) with strings=[4, 1, 8, 6, 3], substring='world'
#      → 10.1 return [s for s in strings if substring in s]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_containing) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     substring = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_containing(...)) → get_strings_containing() runs first → value passed to print()

"""

"""
Problem 1913:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_characters_by_type(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: and
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_characters_by_type('Hello123!') → each argument is evaluated first, then the function body runs
#   count_characters_by_type(text) → each argument is evaluated first, then the function body runs
#   count_characters_by_type('world') runs first → then print() outputs the result
#   count_characters_by_type('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_characters_by_type(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_characters_by_type(...))
#      → calls count_characters_by_type(text) with text='world'
#      → 9.1 return {'letters': sum((1 for c in text if c.isalpha())), 'digits': sum((1 for c in text if c.isdigit())), 'others': sum((1 for c in text if not c.isalnum() and (not c.isspace())))}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, count_characters_by_type) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_characters_by_type(...)) → count_characters_by_type() runs first → value passed to print()

"""

"""
Problem 1914:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_range_between(my_list, min_val, max_val) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_range_between(?, 20, 40) → each argument is evaluated first, then the function body runs
#   get_range_between(my_list, min_val, max_val) → each argument is evaluated first, then the function body runs
#   get_range_between(?, 25, 25) runs first → then print() outputs the result
#   get_range_between(?, 25, 25) → each argument is evaluated first, then the function body runs
#   min_val <= x → evaluated as True or False (determines which branch runs)
#   min_val <= max_val → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_range_between(my_list, min_val, max_val) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_range_between(my_list, min_val, max_val) → stored in memory, not executed
#   5. def get_range_between(my_list, min_val, max_val) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. min_val = ... → assigns value to variable
#   8. max_val = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_range_between(...))
#      → calls get_range_between(my_list, min_val, max_val) with my_list=[4, 1, 8, 6, 3], min_val=25, max_val=25
#      → 11.1 return [x for x in my_list if min_val <= x <= max_val]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_range_between) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     min_val = ... → stored in memory
#     max_val = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_range_between(...)) → get_range_between() runs first → value passed to print()

"""

"""
Problem 1915:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_date(year, month, day) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_date(2024, 3, 5) → each argument is evaluated first, then the function body runs
#   format_date(year, month, day) → each argument is evaluated first, then the function body runs
#   format_date(25, 25, 25) runs first → then print() outputs the result
#   format_date(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_date(year, month, day) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_date(year, month, day) → stored in memory, not executed
#   5. def format_date(year, month, day) → stored in memory, not executed
#   6. year = ... → assigns value to variable
#   7. month = ... → assigns value to variable
#   8. day = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_date(...))
#      → calls format_date(year, month, day) with year=25, month=25, day=25
#      → 11.1 return f'{year}-{month:02d}-{day:02d}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_date) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     year = ... → stored in memory
#     month = ... → stored in memory
#     day = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_date(...)) → format_date() runs first → value passed to print()

"""

"""
Problem 1916:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_simple_interest(principal, rate, time) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_simple_interest(1000, 5, 2) → each argument is evaluated first, then the function body runs
#   calculate_simple_interest(principal, rate, time) → each argument is evaluated first, then the function body runs
#   calculate_simple_interest(25, 25, 25) runs first → then print() outputs the result
#   ? / 100 → Python looks up ?, then 100, then applies /
#   calculate_simple_interest(25, 25, 25) → each argument is evaluated first, then the function body runs
#   ? * time → Python looks up ?, then time, then applies *
#   principal * rate → Python looks up principal, then rate, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_simple_interest(principal, rate, time) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_simple_interest(principal, rate, time) → stored in memory, not executed
#   5. def calculate_simple_interest(principal, rate, time) → stored in memory, not executed
#   6. principal = ... → assigns value to variable
#   7. rate = ... → assigns value to variable
#   8. time = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_simple_interest(...))
#      → calls calculate_simple_interest(principal, rate, time) with principal=25, rate=25, time=25
#      → 11.1 return principal * rate * time / 100
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_simple_interest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     principal = ... → stored in memory
#     rate = ... → stored in memory
#     time = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_simple_interest(...)) → calculate_simple_interest() runs first → value passed to print()

"""

"""
Problem 1917:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_unique_characters(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_unique_characters('hello') → each argument is evaluated first, then the function body runs
#   get_unique_characters(text) → each argument is evaluated first, then the function body runs
#   get_unique_characters('world') runs first → then print() outputs the result
#   get_unique_characters('world') → each argument is evaluated first, then the function body runs
#   char not in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_unique_characters(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_unique_characters(text) → stored in memory, not executed
#   5. def get_unique_characters(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_unique_characters(...))
#      → calls get_unique_characters(text) with text='world'
#      → 9.1 return list(map(lambda char: char, text))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_unique_characters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_unique_characters(...)) → get_unique_characters() runs first → value passed to print()
#
#   Loop: takes one item from text each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1918:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words_ending_with(text, suffix) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words_ending_with('running walking jumping', 'ing') → each argument is evaluated first, then the function body runs
#   count_words_ending_with(text, suffix) → each argument is evaluated first, then the function body runs
#   count_words_ending_with('world', '?') runs first → then print() outputs the result
#   count_words_ending_with('world', '?') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words_ending_with(text, suffix) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_words_ending_with(text, suffix) → stored in memory, not executed
#   5. def count_words_ending_with(text, suffix) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. suffix = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(count_words_ending_with(...))
#      → calls count_words_ending_with(text, suffix) with text='world', suffix='?'
#      → 10.1 return sum((1 for word in words if word.endswith(suffix)))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words_ending_with) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     suffix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words_ending_with(...)) → count_words_ending_with() runs first → value passed to print()

"""

"""
Problem 1919:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_numbers_in_range(numbers, min_val, max_val) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_numbers_in_range(?, 25, 45) → each argument is evaluated first, then the function body runs
#   get_numbers_in_range(numbers, min_val, max_val) → each argument is evaluated first, then the function body runs
#   get_numbers_in_range(?, 25, 25) runs first → then print() outputs the result
#   get_numbers_in_range(?, 25, 25) → each argument is evaluated first, then the function body runs
#   min_val <= x → evaluated as True or False (determines which branch runs)
#   min_val <= max_val → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_numbers_in_range(numbers, min_val, max_val) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_numbers_in_range(numbers, min_val, max_val) → stored in memory, not executed
#   5. def get_numbers_in_range(numbers, min_val, max_val) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. min_val = ... → assigns value to variable
#   8. max_val = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_numbers_in_range(...))
#      → calls get_numbers_in_range(numbers, min_val, max_val) with numbers=[4, 1, 8, 6, 3], min_val=25, max_val=25
#      → 11.1 return [x for x in numbers if min_val <= x <= max_val]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_numbers_in_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     min_val = ... → stored in memory
#     max_val = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_numbers_in_range(...)) → get_numbers_in_range() runs first → value passed to print()

"""

"""
Problem 1920:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_name_age_city(name, age, city) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_name_age_city('Alice', 30, 'New York') → each argument is evaluated first, then the function body runs
#   format_name_age_city(name, age, city) → each argument is evaluated first, then the function body runs
#   format_name_age_city('Bob', 25, 25) runs first → then print() outputs the result
#   'Name: %s, Age: %s, City: %s' % ? → Python looks up 'Name: %s, Age: %s, City: %s', then ?, then applies %
#   format_name_age_city('Bob', 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_name_age_city(name, age, city) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_name_age_city(name, age, city) → stored in memory, not executed
#   5. def format_name_age_city(name, age, city) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. age = ... → assigns value to variable
#   8. city = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_name_age_city(...))
#      → calls format_name_age_city(name, age, city) with name='Bob', age=25, city=25
#      → 11.1 return 'Name: %s, Age: %s, City: %s' % (name, age, city)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_name_age_city) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     name = ... → stored in memory
#     age = ... → stored in memory
#     city = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_name_age_city(...)) → format_name_age_city() runs first → value passed to print()

"""

"""
Problem 1921:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_multiple_of(number, divisors) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: all()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_multiple_of(12, ?) runs first → then print() outputs the result
#   check_multiple_of(number, divisors) → each argument is evaluated first, then the function body runs
#   check_multiple_of(25, ?) runs first → then print() outputs the result
#   check_multiple_of(12, ?) → each argument is evaluated first, then the function body runs
#   check_multiple_of(25, ?) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   number % d → Python looks up number, then d, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_multiple_of(number, divisors) → stored in memory, not executed
#   2. print(check_multiple_of(...), check_multiple_of(...))
#      → calls check_multiple_of(number, divisors) with number=12, divisors=[2, 3, 4]
#      → 2.1 return all((number % d == 0 for d in divisors))
#   3. def check_multiple_of(number, divisors) → stored in memory, not executed
#   4. def check_multiple_of(number, divisors) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. divisors = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_multiple_of(...))
#      → calls check_multiple_of(number, divisors) with number=25, divisors=[4, 1, 8, 6, 3]
#      → 9.1 return all((number % d == 0 for d in divisors))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_multiple_of) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_multiple_of(...), check_multiple_of(...)) → check_multiple_of() runs first → value passed to print()
#     number = ... → stored in memory
#     divisors = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_multiple_of(...)) → check_multiple_of() runs first → value passed to print()

"""

"""
Problem 1922:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_sum_of_squares(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_sum_of_squares(?) → each argument is evaluated first, then the function body runs
#   get_sum_of_squares(numbers) → each argument is evaluated first, then the function body runs
#   get_sum_of_squares(?) runs first → then print() outputs the result
#   x ** 2 → Python looks up x, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_sum_of_squares(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_sum_of_squares(numbers) → stored in memory, not executed
#   5. def get_sum_of_squares(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_sum_of_squares(...))
#      → calls get_sum_of_squares(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sum((x ** 2 for x in numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_sum_of_squares) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_sum_of_squares(...)) → get_sum_of_squares() runs first → value passed to print()

"""

"""
Problem 1923:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_product_of_even(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_product_of_even(?) → each argument is evaluated first, then the function body runs
#   get_product_of_even(numbers) → each argument is evaluated first, then the function body runs
#   get_product_of_even(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_product_of_even(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_product_of_even(numbers) → stored in memory, not executed
#   5. def get_product_of_even(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_product_of_even(...))
#      → calls get_product_of_even(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return product
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_product_of_even) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_product_of_even(...)) → get_product_of_even() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1924:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_sum_of_odd(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % !=
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_sum_of_odd(?) → each argument is evaluated first, then the function body runs
#   get_sum_of_odd(numbers) → each argument is evaluated first, then the function body runs
#   get_sum_of_odd(?) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_sum_of_odd(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_sum_of_odd(numbers) → stored in memory, not executed
#   5. def get_sum_of_odd(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_sum_of_odd(...))
#      → calls get_sum_of_odd(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sum((x for x in numbers if x % 2 != 0))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_sum_of_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_sum_of_odd(...)) → get_sum_of_odd() runs first → value passed to print()

"""

"""
Problem 1925:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_second_occurrence(my_list, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_second_occurrence(?, 10) → each argument is evaluated first, then the function body runs
#   find_second_occurrence(my_list, element) → each argument is evaluated first, then the function body runs
#   find_second_occurrence(?, 25) runs first → then print() outputs the result
#   find_second_occurrence(?, 25) → each argument is evaluated first, then the function body runs
#   item == element → evaluated as True or False (determines which branch runs)
#   count == 2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_second_occurrence(my_list, element) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_second_occurrence(my_list, element) → stored in memory, not executed
#   5. def find_second_occurrence(my_list, element) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. element = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(find_second_occurrence(...))
#      → calls find_second_occurrence(my_list, element) with my_list=[4, 1, 8, 6, 3], element=25
#      → 10.1 return i
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_second_occurrence) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_second_occurrence(...)) → find_second_occurrence() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1926:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_greater_than_average(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: / >
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_greater_than_average(?) → each argument is evaluated first, then the function body runs
#   get_elements_greater_than_average(numbers) → each argument is evaluated first, then the function body runs
#   get_elements_greater_than_average(?) runs first → then print() outputs the result
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   x > avg → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_greater_than_average(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_greater_than_average(numbers) → stored in memory, not executed
#   5. def get_elements_greater_than_average(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_elements_greater_than_average(...))
#      → calls get_elements_greater_than_average(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x > avg]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_greater_than_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_greater_than_average(...)) → get_elements_greater_than_average() runs first → value passed to print()

"""

"""
Problem 1927:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_less_than_average(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: / <
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_less_than_average(?) → each argument is evaluated first, then the function body runs
#   get_elements_less_than_average(numbers) → each argument is evaluated first, then the function body runs
#   get_elements_less_than_average(?) runs first → then print() outputs the result
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   x < avg → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_less_than_average(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_less_than_average(numbers) → stored in memory, not executed
#   5. def get_elements_less_than_average(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_elements_less_than_average(...))
#      → calls get_elements_less_than_average(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [x for x in numbers if x < avg]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_less_than_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_less_than_average(...)) → get_elements_less_than_average() runs first → value passed to print()

"""

"""
Problem 1929:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_with_length(text, length) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_with_length('hello world python programming', 5) → each argument is evaluated first, then the function body runs
#   get_words_with_length(text, length) → each argument is evaluated first, then the function body runs
#   get_words_with_length('world', 25) runs first → then print() outputs the result
#   get_words_with_length('world', 25) → each argument is evaluated first, then the function body runs
#   len(...) == length → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_with_length(text, length) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_with_length(text, length) → stored in memory, not executed
#   5. def get_words_with_length(text, length) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. length = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_words_with_length(...))
#      → calls get_words_with_length(text, length) with text='world', length=25
#      → 10.1 return [word for word in words if len(word) == length]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_with_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     length = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_with_length(...)) → get_words_with_length() runs first → value passed to print()

"""

"""
Problem 1930:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_full_address(street_num, street_name, city, state, zipcode) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_full_address('123', 'Main St', 'New York', 'NY', '10001') → each argument is evaluated first, then the function body runs
#   format_full_address(street_num, street_name, city, state, zipcode) → each argument is evaluated first, then the function body runs
#   format_full_address(25, 'world', 25, 25, 25) runs first → then print() outputs the result
#   '%s %s, %s, %s %s' % ? → Python looks up '%s %s, %s, %s %s', then ?, then applies %
#   format_full_address(25, 'world', 25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_full_address(street_num, street_name, city, state, zipcode) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_full_address(street_num, street_name, city, state, zipcode) → stored in memory, not executed
#   5. def format_full_address(street_num, street_name, city, state, zipcode) → stored in memory, not executed
#   6. street_num = ... → assigns value to variable
#   7. street_name = ... → assigns value to variable
#   8. city = ... → assigns value to variable
#   9. state = ... → assigns value to variable
#   10. zipcode = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(format_full_address(...))
#      → calls format_full_address(street_num, street_name, city, state, zipcode) with street_num=25, street_name='world', city=25, state=25, zipcode=25
#      → 13.1 return '%s %s, %s, %s %s' % (street_num, street_name, city, state, zipcode)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_full_address) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     street_num = ... → stored in memory
#     street_name = ... → stored in memory
#     city = ... → stored in memory
#     state = ... → stored in memory
#     zipcode = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_full_address(...)) → format_full_address() runs first → value passed to print()

"""

"""
Problem 1931:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_total_with_discount(prices, discount_percent, tax_rate) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + - /
# Built-in functions: round, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_total_with_discount(?, 10, 8) → each argument is evaluated first, then the function body runs
#   calculate_total_with_discount(prices, discount_percent, tax_rate) → each argument is evaluated first, then the function body runs
#   calculate_total_with_discount(?, 25, 25) runs first → then print() outputs the result
#   subtotal * ? → Python looks up subtotal, then ?, then applies *
#   discounted * ? → Python looks up discounted, then ?, then applies *
#   calculate_total_with_discount(?, 25, 25) → each argument is evaluated first, then the function body runs
#   1 - ? → Python looks up 1, then ?, then applies -
#   1 + ? → Python looks up 1, then ?, then applies +
#   discount_percent / 100 → Python looks up discount_percent, then 100, then applies /
#   tax_rate / 100 → Python looks up tax_rate, then 100, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_total_with_discount(prices, discount_percent, tax_rate) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(round(...))
#   4. def calculate_total_with_discount(prices, discount_percent, tax_rate) → stored in memory, not executed
#   5. def calculate_total_with_discount(prices, discount_percent, tax_rate) → stored in memory, not executed
#   6. prices = ... → assigns value to variable
#   7. discount_percent = ... → assigns value to variable
#   8. tax_rate = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_total_with_discount(...))
#      → calls calculate_total_with_discount(prices, discount_percent, tax_rate) with prices=[4, 1, 8, 6, 3], discount_percent=25, tax_rate=25
#      → 11.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_total_with_discount) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(round(...))
#     prices = ... → stored in memory
#     discount_percent = ... → stored in memory
#     tax_rate = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_total_with_discount(...)) → calculate_total_with_discount() runs first → value passed to print()

"""

"""
Problem 1932:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_common_prefix(str1, str2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: len, min, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_common_prefix('hello', 'help') → each argument is evaluated first, then the function body runs
#   get_common_prefix(str1, str2) → each argument is evaluated first, then the function body runs
#   get_common_prefix('world', 'python') runs first → then print() outputs the result
#   get_common_prefix('world', 'python') → each argument is evaluated first, then the function body runs
#   ? == ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_common_prefix(str1, str2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_common_prefix(str1, str2) → stored in memory, not executed
#   5. def get_common_prefix(str1, str2) → stored in memory, not executed
#   6. str1 = ... → assigns value to variable
#   7. str2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_common_prefix(...))
#      → calls get_common_prefix(str1, str2) with str1='world', str2='python'
#      → 10.1 return prefix
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_common_prefix) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_common_prefix(...)) → get_common_prefix() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1933:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_common_suffix(str1, str2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: len, min, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_common_suffix('running', 'walking') → each argument is evaluated first, then the function body runs
#   get_common_suffix(str1, str2) → each argument is evaluated first, then the function body runs
#   get_common_suffix('world', 'python') runs first → then print() outputs the result
#   get_common_suffix('world', 'python') → each argument is evaluated first, then the function body runs
#   min(...) + 1 → Python looks up min(...), then 1, then applies +
#   ? == ? → evaluated as True or False (determines which branch runs)
#   ? + suffix → Python looks up ?, then suffix, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_common_suffix(str1, str2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_common_suffix(str1, str2) → stored in memory, not executed
#   5. def get_common_suffix(str1, str2) → stored in memory, not executed
#   6. str1 = ... → assigns value to variable
#   7. str2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_common_suffix(...))
#      → calls get_common_suffix(str1, str2) with str1='world', str2='python'
#      → 10.1 return suffix
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_common_suffix) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_common_suffix(...)) → get_common_suffix() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1934:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_all_uppercase_words(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: all()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_all_uppercase_words('Hello World Python') → each argument is evaluated first, then the function body runs
#   check_all_uppercase_words(text) → each argument is evaluated first, then the function body runs
#   check_all_uppercase_words('world') runs first → then print() outputs the result
#   check_all_uppercase_words('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_all_uppercase_words(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_all_uppercase_words(text) → stored in memory, not executed
#   5. def check_all_uppercase_words(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_all_uppercase_words(...))
#      → calls check_all_uppercase_words(text) with text='world'
#      → 9.1 return all((word[0].isupper() for word in words if word))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_all_uppercase_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_all_uppercase_words(...)) → check_all_uppercase_words() runs first → value passed to print()

"""

"""
Problem 1935:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_with_length_range(strings, min_length, max_length) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <=
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_with_length_range(?, 3, 4) → each argument is evaluated first, then the function body runs
#   get_strings_with_length_range(strings, min_length, max_length) → each argument is evaluated first, then the function body runs
#   get_strings_with_length_range(?, 25, 25) runs first → then print() outputs the result
#   get_strings_with_length_range(?, 25, 25) → each argument is evaluated first, then the function body runs
#   min_length <= len(...) → evaluated as True or False (determines which branch runs)
#   min_length <= max_length → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_with_length_range(strings, min_length, max_length) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_with_length_range(strings, min_length, max_length) → stored in memory, not executed
#   5. def get_strings_with_length_range(strings, min_length, max_length) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. min_length = ... → assigns value to variable
#   8. max_length = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_strings_with_length_range(...))
#      → calls get_strings_with_length_range(strings, min_length, max_length) with strings=[4, 1, 8, 6, 3], min_length=25, max_length=25
#      → 11.1 return [s for s in strings if min_length <= len(s) <= max_length]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_with_length_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     min_length = ... → stored in memory
#     max_length = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_with_length_range(...)) → get_strings_with_length_range() runs first → value passed to print()

"""

"""
Problem 1936:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_median(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: % + - / // ==
# Built-in functions: len, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_median(?) → each argument is evaluated first, then the function body runs
#   calculate_median(numbers) → each argument is evaluated first, then the function body runs
#   calculate_median(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   ? / 2 → Python looks up ?, then 2, then applies /
#   n % 2 → Python looks up n, then 2, then applies %
#   ? + ? → Python looks up ?, then ?, then applies +
#   n // 2 → Python looks up n, then 2, then applies //
#   ? - 1 → Python looks up ?, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_median(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_median(numbers) → stored in memory, not executed
#   5. def calculate_median(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_median(...))
#      → calls calculate_median(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return (sorted_nums[n // 2 - 1] + sorted_nums[n // 2]) / 2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_median) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_median(...)) → calculate_median() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1937:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_mode(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_mode(?) → each argument is evaluated first, then the function body runs
#   get_mode(my_list) → each argument is evaluated first, then the function body runs
#   get_mode(?) runs first → then print() outputs the result
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_mode(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_mode(my_list) → stored in memory, not executed
#   5. def get_mode(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_mode(...))
#      → calls get_mode(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return max(counts, key=counts.get)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_mode) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_mode(...)) → get_mode() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1939:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_numbers_divisible_by_all(numbers, divisors) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: all()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_numbers_divisible_by_all(?, ?) → each argument is evaluated first, then the function body runs
#   get_numbers_divisible_by_all(numbers, divisors) → each argument is evaluated first, then the function body runs
#   get_numbers_divisible_by_all(?, ?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n % d → Python looks up n, then d, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_numbers_divisible_by_all(numbers, divisors) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_numbers_divisible_by_all(numbers, divisors) → stored in memory, not executed
#   5. def get_numbers_divisible_by_all(numbers, divisors) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. divisors = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_numbers_divisible_by_all(...))
#      → calls get_numbers_divisible_by_all(numbers, divisors) with numbers=[4, 1, 8, 6, 3], divisors=[4, 1, 8, 6, 3]
#      → 10.1 return [n for n in numbers if all((n % d == 0 for d in divisors))]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_numbers_divisible_by_all) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     divisors = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_numbers_divisible_by_all(...)) → get_numbers_divisible_by_all() runs first → value passed to print()

"""

"""
Problem 1941:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_with_vowel_count(text, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: == in
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_with_vowel_count('hello world python programming', 2) → each argument is evaluated first, then the function body runs
#   get_words_with_vowel_count(text, n) → each argument is evaluated first, then the function body runs
#   get_words_with_vowel_count('world', 15) runs first → then print() outputs the result
#   get_words_with_vowel_count('world', 15) → each argument is evaluated first, then the function body runs
#   sum(...) == n → evaluated as True or False (determines which branch runs)
#   c in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_with_vowel_count(text, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_with_vowel_count(text, n) → stored in memory, not executed
#   5. def get_words_with_vowel_count(text, n) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_words_with_vowel_count(...))
#      → calls get_words_with_vowel_count(text, n) with text='world', n=15
#      → 10.1 return [word for word in words if sum((1 for c in word if c in vowels)) == n]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_with_vowel_count) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_with_vowel_count(...)) → get_words_with_vowel_count() runs first → value passed to print()

"""

"""
Problem 1942:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_table_row(value1, value2, value3) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_table_row('Alice', 30, 'Engineer') → each argument is evaluated first, then the function body runs
#   format_table_row(value1, value2, value3) → each argument is evaluated first, then the function body runs
#   format_table_row(25, 25, 25) runs first → then print() outputs the result
#   '%s | %s | %s' % ? → Python looks up '%s | %s | %s', then ?, then applies %
#   format_table_row(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_table_row(value1, value2, value3) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_table_row(value1, value2, value3) → stored in memory, not executed
#   5. def format_table_row(value1, value2, value3) → stored in memory, not executed
#   6. value1 = ... → assigns value to variable
#   7. value2 = ... → assigns value to variable
#   8. value3 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_table_row(...))
#      → calls format_table_row(value1, value2, value3) with value1=25, value2=25, value3=25
#      → 11.1 return '%s | %s | %s' % (value1, value2, value3)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_table_row) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     value1 = ... → stored in memory
#     value2 = ... → stored in memory
#     value3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_table_row(...)) → format_table_row() runs first → value passed to print()

"""

"""
Problem 1943:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_not_in_both(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_not_in_both(?, ?) → each argument is evaluated first, then the function body runs
#   get_elements_not_in_both(list1, list2) → each argument is evaluated first, then the function body runs
#   get_elements_not_in_both(?, ?) runs first → then print() outputs the result
#   item not in list2 → evaluated as True or False (determines which branch runs)
#   item not in list1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_not_in_both(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(sorted(...))
#   4. def get_elements_not_in_both(list1, list2) → stored in memory, not executed
#   5. def get_elements_not_in_both(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_elements_not_in_both(...))
#      → calls get_elements_not_in_both(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return list(map(lambda item: item, list1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_not_in_both) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(sorted(...))
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_not_in_both(...)) → get_elements_not_in_both() runs first → value passed to print()
#
#   Loop: takes one item from list1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1944:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_weighted_average(values, weights) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float, sum, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_weighted_average(?, ?) → each argument is evaluated first, then the function body runs
#   calculate_weighted_average(values, weights) → each argument is evaluated first, then the function body runs
#   calculate_weighted_average(?, ?) runs first → then print() outputs the result
#   weighted_sum / total_weight → Python looks up weighted_sum, then total_weight, then applies /
#   weighted_sum * ? → Python looks up weighted_sum, then ?, then applies *
#   1 / total_weight → Python looks up 1, then total_weight, then applies /
#   v * w → Python looks up v, then w, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_weighted_average(values, weights) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_weighted_average(values, weights) → stored in memory, not executed
#   5. def calculate_weighted_average(values, weights) → stored in memory, not executed
#   6. values = ... → assigns value to variable
#   7. weights = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_weighted_average(...))
#      → calls calculate_weighted_average(values, weights) with values=[4, 1, 8, 6, 3], weights=[4, 1, 8, 6, 3]
#      → 10.1 from fractions import Fraction
#      → 10.2 return float(Fraction(weighted_sum, total_weight))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_weighted_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     values = ... → stored in memory
#     weights = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_weighted_average(...)) → calculate_weighted_average() runs first → value passed to print()

"""

"""
Problem 1945:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_sorted_by_length(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_sorted_by_length(?) → each argument is evaluated first, then the function body runs
#   get_strings_sorted_by_length(strings) → each argument is evaluated first, then the function body runs
#   get_strings_sorted_by_length(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_sorted_by_length(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_sorted_by_length(strings) → stored in memory, not executed
#   5. def get_strings_sorted_by_length(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_strings_sorted_by_length(...))
#      → calls get_strings_sorted_by_length(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(strings, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_sorted_by_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_sorted_by_length(...)) → get_strings_sorted_by_length() runs first → value passed to print()

"""

"""
Problem 1946:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_percentage_list(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_percentage_list(?) → each argument is evaluated first, then the function body runs
#   format_percentage_list(numbers) → each argument is evaluated first, then the function body runs
#   format_percentage_list(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_percentage_list(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_percentage_list(numbers) → stored in memory, not executed
#   5. def format_percentage_list(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(format_percentage_list(...))
#      → calls format_percentage_list(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return [f'{n}%' for n in numbers]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_percentage_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_percentage_list(...)) → format_percentage_list() runs first → value passed to print()

"""

"""
Problem 1947:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_pairs_summing_to(numbers, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_pairs_summing_to(?, 7) → each argument is evaluated first, then the function body runs
#   get_pairs_summing_to(numbers, target) → each argument is evaluated first, then the function body runs
#   get_pairs_summing_to(?, 'goal') runs first → then print() outputs the result
#   get_pairs_summing_to(?, 'goal') → each argument is evaluated first, then the function body runs
#   i + 1 → Python looks up i, then 1, then applies +
#   ? == target → evaluated as True or False (determines which branch runs)
#   ? + ? → Python looks up ?, then ?, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_pairs_summing_to(numbers, target) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_pairs_summing_to(numbers, target) → stored in memory, not executed
#   5. def get_pairs_summing_to(numbers, target) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. target = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_pairs_summing_to(...))
#      → calls get_pairs_summing_to(numbers, target) with numbers=[4, 1, 8, 6, 3], target='goal'
#      → 10.1 return pairs
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_pairs_summing_to) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_pairs_summing_to(...)) → get_pairs_summing_to() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1948:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_character_types(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in and not in
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_character_types('Hello World 123') → each argument is evaluated first, then the function body runs
#   count_character_types(text) → each argument is evaluated first, then the function body runs
#   count_character_types('world') runs first → then print() outputs the result
#   count_character_types('world') → each argument is evaluated first, then the function body runs
#   c in vowels → evaluated as True or False (determines which branch runs)
#   c not in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_character_types(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_character_types(...))
#      → calls count_character_types(text) with text='world'
#      → 9.1 vowels = 'aeiouAEIOU'
#      → 9.2 return {'vowels': sum((1 for c in text if c in vowels)), 'consonants': sum((1 for c in text if c.isalpha() and c not in vowels)), 'digits': sum((1 for c in text if c.isdigit())), 'spaces': sum((1 for c in text if c.isspace()))}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, count_character_types) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_character_types(...)) → count_character_types() runs first → value passed to print()

"""

"""
Problem 1949:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_by_vowel_count(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in not in
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_by_vowel_count('hello world python') → each argument is evaluated first, then the function body runs
#   get_words_by_vowel_count(text) → each argument is evaluated first, then the function body runs
#   get_words_by_vowel_count('world') runs first → then print() outputs the result
#   get_words_by_vowel_count('world') → each argument is evaluated first, then the function body runs
#   count not in result → evaluated as True or False (determines which branch runs)
#   c in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_by_vowel_count(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_by_vowel_count(text) → stored in memory, not executed
#   5. def get_words_by_vowel_count(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_words_by_vowel_count(...))
#      → calls get_words_by_vowel_count(text) with text='world'
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_by_vowel_count) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_by_vowel_count(...)) → get_words_by_vowel_count() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1950:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_name_with_title(first, last, title) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_name_with_title('John', 'Doe', 'Dr') → each argument is evaluated first, then the function body runs
#   format_name_with_title(first, last, title) → each argument is evaluated first, then the function body runs
#   format_name_with_title(25, 25, 25) runs first → then print() outputs the result
#   '%s. %s %s' % ? → Python looks up '%s. %s %s', then ?, then applies %
#   format_name_with_title(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_name_with_title(first, last, title) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_name_with_title(first, last, title) → stored in memory, not executed
#   5. def format_name_with_title(first, last, title) → stored in memory, not executed
#   6. first = ... → assigns value to variable
#   7. last = ... → assigns value to variable
#   8. title = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_name_with_title(...))
#      → calls format_name_with_title(first, last, title) with first=25, last=25, title=25
#      → 11.1 return '%s. %s %s' % (title, first, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_name_with_title) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     first = ... → stored in memory
#     last = ... → stored in memory
#     title = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_name_with_title(...)) → format_name_with_title() runs first → value passed to print()

"""

"""
Problem 1951:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_area_circle_sector(radius, angle_degrees) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * / **
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_area_circle_sector(5, 90) → each argument is evaluated first, then the function body runs
#   calculate_area_circle_sector(radius, angle_degrees) → each argument is evaluated first, then the function body runs
#   calculate_area_circle_sector(14, 25) runs first → then print() outputs the result
#   ? * ? → Python looks up ?, then ?, then applies *
#   calculate_area_circle_sector(14, 25) → each argument is evaluated first, then the function body runs
#   ? * math.pi → Python looks up ?, then math.pi, then applies *
#   radius ** 2 → Python looks up radius, then 2, then applies **
#   angle_degrees / 360 → Python looks up angle_degrees, then 360, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def calculate_area_circle_sector(radius, angle_degrees) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(round(...))
#   5. def calculate_area_circle_sector(radius, angle_degrees) → stored in memory, not executed
#   6. def calculate_area_circle_sector(radius, angle_degrees) → stored in memory, not executed
#   7. radius = ... → assigns value to variable
#   8. angle_degrees = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_area_circle_sector(...))
#      → calls calculate_area_circle_sector(radius, angle_degrees) with radius=14, angle_degrees=25
#      → 11.1 return angle_degrees / 360 * math.pi * radius ** 2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_area_circle_sector) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(round(...))
#     radius = ... → stored in memory
#     angle_degrees = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_area_circle_sector(...)) → calculate_area_circle_sector() runs first → value passed to print()

"""

"""
Problem 1952:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_at_multiple_indices(my_list, indices) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_at_multiple_indices(?, ?) → each argument is evaluated first, then the function body runs
#   get_elements_at_multiple_indices(my_list, indices) → each argument is evaluated first, then the function body runs
#   get_elements_at_multiple_indices(?, ?) runs first → then print() outputs the result
#   i < len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_at_multiple_indices(my_list, indices) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_at_multiple_indices(my_list, indices) → stored in memory, not executed
#   5. def get_elements_at_multiple_indices(my_list, indices) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. indices = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_elements_at_multiple_indices(...))
#      → calls get_elements_at_multiple_indices(my_list, indices) with my_list=[4, 1, 8, 6, 3], indices=[4, 1, 8, 6, 3]
#      → 10.1 return {i: my_list[i] for i in indices if i < len(my_list)}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_at_multiple_indices) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     indices = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_at_multiple_indices(...)) → get_elements_at_multiple_indices() runs first → value passed to print()

"""

"""
Problem 1953:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_string_properties(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: and
# Built-in functions: any()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_string_properties('Hello123!') → each argument is evaluated first, then the function body runs
#   check_string_properties(text) → each argument is evaluated first, then the function body runs
#   check_string_properties('world') runs first → then print() outputs the result
#   check_string_properties('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_string_properties(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_string_properties(...))
#      → calls check_string_properties(text) with text='world'
#      → 9.1 return {'has_uppercase': any((c.isupper() for c in text)), 'has_lowercase': any((c.islower() for c in text)), 'has_digit': any((c.isdigit() for c in text)), 'has_special': any((not c.isalnum() and (not c.isspace()) for c in text))}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, check_string_properties) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_string_properties(...)) → check_string_properties() runs first → value passed to print()

"""

"""
Problem 1954:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_numbers_summing_to_range(numbers, min_sum, max_sum) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + <=
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_numbers_summing_to_range(?, 5, 7) → each argument is evaluated first, then the function body runs
#   get_numbers_summing_to_range(numbers, min_sum, max_sum) → each argument is evaluated first, then the function body runs
#   get_numbers_summing_to_range(?, 25, 25) runs first → then print() outputs the result
#   get_numbers_summing_to_range(?, 25, 25) → each argument is evaluated first, then the function body runs
#   i + 1 → Python looks up i, then 1, then applies +
#   ? + ? → Python looks up ?, then ?, then applies +
#   min_sum <= total → evaluated as True or False (determines which branch runs)
#   min_sum <= max_sum → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_numbers_summing_to_range(numbers, min_sum, max_sum) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_numbers_summing_to_range(numbers, min_sum, max_sum) → stored in memory, not executed
#   5. def get_numbers_summing_to_range(numbers, min_sum, max_sum) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. min_sum = ... → assigns value to variable
#   8. max_sum = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_numbers_summing_to_range(...))
#      → calls get_numbers_summing_to_range(numbers, min_sum, max_sum) with numbers=[4, 1, 8, 6, 3], min_sum=25, max_sum=25
#      → 11.1 return pairs
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_numbers_summing_to_range) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     min_sum = ... → stored in memory
#     max_sum = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_numbers_summing_to_range(...)) → get_numbers_summing_to_range() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1955:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_phone_with_country(country_code, area_code, number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_phone_with_country('1', '555', '1234567') → each argument is evaluated first, then the function body runs
#   format_phone_with_country(country_code, area_code, number) → each argument is evaluated first, then the function body runs
#   format_phone_with_country(25, 25, 25) runs first → then print() outputs the result
#   '+%s (%s) %s' % ? → Python looks up '+%s (%s) %s', then ?, then applies %
#   format_phone_with_country(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_phone_with_country(country_code, area_code, number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_phone_with_country(country_code, area_code, number) → stored in memory, not executed
#   5. def format_phone_with_country(country_code, area_code, number) → stored in memory, not executed
#   6. country_code = ... → assigns value to variable
#   7. area_code = ... → assigns value to variable
#   8. number = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_phone_with_country(...))
#      → calls format_phone_with_country(country_code, area_code, number) with country_code=25, area_code=25, number=25
#      → 11.1 return '+%s (%s) %s' % (country_code, area_code, number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_phone_with_country) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     country_code = ... → stored in memory
#     area_code = ... → stored in memory
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_phone_with_country(...)) → format_phone_with_country() runs first → value passed to print()

"""

"""
Problem 1956:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_consecutive_elements(my_list, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + -
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_consecutive_elements(?, 3) → each argument is evaluated first, then the function body runs
#   get_consecutive_elements(my_list, n) → each argument is evaluated first, then the function body runs
#   get_consecutive_elements(?, 15) runs first → then print() outputs the result
#   get_consecutive_elements(?, 15) → each argument is evaluated first, then the function body runs
#   i + n → Python looks up i, then n, then applies +
#   ? + 1 → Python looks up ?, then 1, then applies +
#   len(...) - n → Python looks up len(...), then n, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_consecutive_elements(my_list, n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_consecutive_elements(my_list, n) → stored in memory, not executed
#   5. def get_consecutive_elements(my_list, n) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_consecutive_elements(...))
#      → calls get_consecutive_elements(my_list, n) with my_list=[4, 1, 8, 6, 3], n=15
#      → 10.1 return [my_list[i:i + n] for i in range(len(my_list) - n + 1)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_consecutive_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_consecutive_elements(...)) → get_consecutive_elements() runs first → value passed to print()

"""

"""
Problem 1957:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_gcd(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_gcd(48, 18) → each argument is evaluated first, then the function body runs
#   calculate_gcd(a, b) → each argument is evaluated first, then the function body runs
#   calculate_gcd(20, 30) runs first → then print() outputs the result
#   calculate_gcd(20, 30) → each argument is evaluated first, then the function body runs
#   a % b → Python looks up a, then b, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_gcd(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_gcd(a, b) → stored in memory, not executed
#   5. def calculate_gcd(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_gcd(...))
#      → calls calculate_gcd(a, b) with a=20, b=30
#      → 10.1 return a
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_gcd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_gcd(...)) → calculate_gcd() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1958:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_lcm(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: % * //
# Built-in functions: abs, gcd()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_lcm(12, 18) → each argument is evaluated first, then the function body runs
#   calculate_lcm(a, b) → each argument is evaluated first, then the function body runs
#   calculate_lcm(20, 30) runs first → then print() outputs the result
#   abs(...) // gcd(...) → Python looks up abs(...), then gcd(...), then applies //
#   calculate_lcm(20, 30) → each argument is evaluated first, then the function body runs
#   a * b → Python looks up a, then b, then applies *
#   x % y → Python looks up x, then y, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_lcm(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_lcm(a, b) → stored in memory, not executed
#   5. def calculate_lcm(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_lcm(...))
#      → calls calculate_lcm(a, b) with a=20, b=30
#      → 10.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_lcm) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_lcm(...)) → calculate_lcm() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 1959:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_frequency_dict(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: dict, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_frequency_dict(?) → each argument is evaluated first, then the function body runs
#   get_frequency_dict(my_list) → each argument is evaluated first, then the function body runs
#   get_frequency_dict(?) runs first → then print() outputs the result
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_frequency_dict(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_frequency_dict(my_list) → stored in memory, not executed
#   5. def get_frequency_dict(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_frequency_dict(...))
#      → calls get_frequency_dict(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 9.1 return dict(sorted(counts.items(), key=lambda x: x[1], reverse=True))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_frequency_dict) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_frequency_dict(...)) → get_frequency_dict() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done

"""

"""
Problem 1960:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_number_with_commas(number) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_number_with_commas(1234567) → each argument is evaluated first, then the function body runs
#   format_number_with_commas(number) → each argument is evaluated first, then the function body runs
#   format_number_with_commas(25) runs first → then print() outputs the result
#   format_number_with_commas(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_number_with_commas(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_number_with_commas(number) → stored in memory, not executed
#   5. def format_number_with_commas(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(format_number_with_commas(...))
#      → calls format_number_with_commas(number) with number=25
#      → 9.1 return f'{number:,}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_number_with_commas) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_number_with_commas(...)) → format_number_with_commas() runs first → value passed to print()

"""

"""
Problem 1961:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_with_same_length(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_with_same_length(?) → each argument is evaluated first, then the function body runs
#   get_words_with_same_length(strings) → each argument is evaluated first, then the function body runs
#   get_words_with_same_length(?) runs first → then print() outputs the result
#   length not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_with_same_length(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_with_same_length(strings) → stored in memory, not executed
#   5. def get_words_with_same_length(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_words_with_same_length(...))
#      → calls get_words_with_same_length(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_with_same_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_with_same_length(...)) → get_words_with_same_length() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1962:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_anagram_pair(str1, str2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_anagram_pair('listen', 'silent') runs first → then print() outputs the result
#   check_anagram_pair('hello', 'world') runs first → then print() outputs the result
#   check_anagram_pair(str1, str2) → each argument is evaluated first, then the function body runs
#   check_anagram_pair('world', 'python') runs first → then print() outputs the result
#   sorted(...) == sorted(...) → evaluated as True or False (determines which branch runs)
#   check_anagram_pair('listen', 'silent') → each argument is evaluated first, then the function body runs
#   check_anagram_pair('hello', 'world') → each argument is evaluated first, then the function body runs
#   check_anagram_pair('world', 'python') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_anagram_pair(str1, str2) → stored in memory, not executed
#   2. print(check_anagram_pair(...), check_anagram_pair(...))
#      → calls check_anagram_pair(str1, str2) with str1='listen', str2='silent'
#      → 2.1 return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))
#   3. def check_anagram_pair(str1, str2) → stored in memory, not executed
#   4. def check_anagram_pair(str1, str2) → stored in memory, not executed
#   5. str1 = ... → assigns value to variable
#   6. str2 = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_anagram_pair(...))
#      → calls check_anagram_pair(str1, str2) with str1='world', str2='python'
#      → 9.1 return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_anagram_pair) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_anagram_pair(...), check_anagram_pair(...)) → check_anagram_pair() runs first → value passed to print()
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_anagram_pair(...)) → check_anagram_pair() runs first → value passed to print()

"""

"""
Problem 1963:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_sublist_sum(my_list, start, end) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_sublist_sum(?, 1, 4) → each argument is evaluated first, then the function body runs
#   get_sublist_sum(my_list, start, end) → each argument is evaluated first, then the function body runs
#   get_sublist_sum(?, 3, 20) runs first → then print() outputs the result
#   get_sublist_sum(?, 3, 20) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_sublist_sum(my_list, start, end) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_sublist_sum(my_list, start, end) → stored in memory, not executed
#   5. def get_sublist_sum(my_list, start, end) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. start = ... → assigns value to variable
#   8. end = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_sublist_sum(...))
#      → calls get_sublist_sum(my_list, start, end) with my_list=[4, 1, 8, 6, 3], start=3, end=20
#      → 11.1 return sum(my_list[start:end])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_sublist_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     start = ... → stored in memory
#     end = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_sublist_sum(...)) → get_sublist_sum() runs first → value passed to print()

"""

"""
Problem 1964:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_time_12hour(hours_24) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: - < ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_time_12hour(0) runs first → then print() outputs the result
#   format_time_12hour(14) runs first → then print() outputs the result
#   format_time_12hour(23) runs first → then print() outputs the result
#   format_time_12hour(hours_24) → each argument is evaluated first, then the function body runs
#   format_time_12hour(25) runs first → then print() outputs the result
#   hours_24 == 0 → evaluated as True or False (determines which branch runs)
#   format_time_12hour(0) → each argument is evaluated first, then the function body runs
#   format_time_12hour(14) → each argument is evaluated first, then the function body runs
#   format_time_12hour(23) → each argument is evaluated first, then the function body runs
#   format_time_12hour(25) → each argument is evaluated first, then the function body runs
#   hours_24 < 12 → evaluated as True or False (determines which branch runs)
#   hours_24 == 12 → evaluated as True or False (determines which branch runs)
#   hours_24 - 12 → Python looks up hours_24, then 12, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_time_12hour(hours_24) → stored in memory, not executed
#   2. print(format_time_12hour(...), format_time_12hour(...), format_time_12hour(...))
#      → calls format_time_12hour(hours_24) with hours_24=0
#      → 2.1 return '12 AM'
#   3. def format_time_12hour(hours_24) → stored in memory, not executed
#   4. def format_time_12hour(hours_24) → stored in memory, not executed
#   5. hours_24 = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(format_time_12hour(...))
#      → calls format_time_12hour(hours_24) with hours_24=25
#      → 8.1 return '12 AM'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_time_12hour) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(format_time_12hour(...), format_time_12hour(...), format_time_12hour(...)) → format_time_12hour() runs first → value passed to print()
#     hours_24 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_time_12hour(...)) → format_time_12hour() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1965:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_between_values(my_list, min_val, max_val) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_between_values(?, 15, 45) → each argument is evaluated first, then the function body runs
#   get_elements_between_values(my_list, min_val, max_val) → each argument is evaluated first, then the function body runs
#   get_elements_between_values(?, 25, 25) runs first → then print() outputs the result
#   get_elements_between_values(?, 25, 25) → each argument is evaluated first, then the function body runs
#   min_val < x → evaluated as True or False (determines which branch runs)
#   min_val < max_val → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_between_values(my_list, min_val, max_val) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_between_values(my_list, min_val, max_val) → stored in memory, not executed
#   5. def get_elements_between_values(my_list, min_val, max_val) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. min_val = ... → assigns value to variable
#   8. max_val = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(get_elements_between_values(...))
#      → calls get_elements_between_values(my_list, min_val, max_val) with my_list=[4, 1, 8, 6, 3], min_val=25, max_val=25
#      → 11.1 return [x for x in my_list if min_val < x < max_val]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_between_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     min_val = ... → stored in memory
#     max_val = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_between_values(...)) → get_elements_between_values() runs first → value passed to print()

"""

"""
Problem 1966:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_substring_occurrences(text, substring) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - ==
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_substring_occurrences('ababab', 'aba') → each argument is evaluated first, then the function body runs
#   count_substring_occurrences(text, substring) → each argument is evaluated first, then the function body runs
#   count_substring_occurrences('world', 'world') runs first → then print() outputs the result
#   count_substring_occurrences('world', 'world') → each argument is evaluated first, then the function body runs
#   ? + 1 → Python looks up ?, then 1, then applies +
#   ? == substring → evaluated as True or False (determines which branch runs)
#   len(...) - len(...) → Python looks up len(...), then len(...), then applies -
#   i + len(...) → Python looks up i, then len(...), then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_substring_occurrences(text, substring) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_substring_occurrences(text, substring) → stored in memory, not executed
#   5. def count_substring_occurrences(text, substring) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. substring = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(count_substring_occurrences(...))
#      → calls count_substring_occurrences(text, substring) with text='world', substring='world'
#      → 10.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_substring_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     substring = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_substring_occurrences(...)) → count_substring_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1967:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_list_as_string(my_list, separator) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_list_as_string(?, ', ') → each argument is evaluated first, then the function body runs
#   format_list_as_string(my_list, separator) → each argument is evaluated first, then the function body runs
#   format_list_as_string(?, 'world') runs first → then print() outputs the result
#   format_list_as_string(?, 'world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_list_as_string(my_list, separator) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_list_as_string(my_list, separator) → stored in memory, not executed
#   5. def format_list_as_string(my_list, separator) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. separator = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(format_list_as_string(...))
#      → calls format_list_as_string(my_list, separator) with my_list=[4, 1, 8, 6, 3], separator='world'
#      → 10.1 return f'[{separator.join((str(x) for x in my_list))}]'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_list_as_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     separator = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_list_as_string(...)) → format_list_as_string() runs first → value passed to print()

"""

"""
Problem 1968:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_unique_combinations(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_unique_combinations(?, ?) → each argument is evaluated first, then the function body runs
#   get_unique_combinations(list1, list2) → each argument is evaluated first, then the function body runs
#   get_unique_combinations(?, ?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_unique_combinations(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_unique_combinations(list1, list2) → stored in memory, not executed
#   5. def get_unique_combinations(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_unique_combinations(...))
#      → calls get_unique_combinations(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return [(a, b) for a in list1 for b in list2]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_unique_combinations) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_unique_combinations(...)) → get_unique_combinations() runs first → value passed to print()

"""

"""
Problem 1969:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_quadratic_roots(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: * + - / < **
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_quadratic_roots(1, ?, 6) → each argument is evaluated first, then the function body runs
#   calculate_quadratic_roots(a, b, c) → each argument is evaluated first, then the function body runs
#   calculate_quadratic_roots(20, 30, 10) runs first → then print() outputs the result
#   ? - ? → Python looks up ?, then ?, then applies -
#   discriminant < 0 → evaluated as True or False (determines which branch runs)
#   ? / ? → Python looks up ?, then ?, then applies /
#   calculate_quadratic_roots(20, 30, 10) → each argument is evaluated first, then the function body runs
#   b ** 2 → Python looks up b, then 2, then applies **
#   ? * c → Python looks up ?, then c, then applies *
#   ? + math.sqrt(...) → Python looks up ?, then math.sqrt(...), then applies +
#   2 * a → Python looks up 2, then a, then applies *
#   ? - math.sqrt(...) → Python looks up ?, then math.sqrt(...), then applies -
#   4 * a → Python looks up 4, then a, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def calculate_quadratic_roots(a, b, c) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def calculate_quadratic_roots(a, b, c) → stored in memory, not executed
#   6. def calculate_quadratic_roots(a, b, c) → stored in memory, not executed
#   7. a = ... → assigns value to variable
#   8. b = ... → assigns value to variable
#   9. c = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(calculate_quadratic_roots(...))
#      → calls calculate_quadratic_roots(a, b, c) with a=20, b=30, c=10
#      → 12.1 return None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_quadratic_roots) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_quadratic_roots(...)) → calculate_quadratic_roots() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1970:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_matching_pattern(strings, pattern) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >=
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_matching_pattern(?, 'l') → each argument is evaluated first, then the function body runs
#   get_strings_matching_pattern(strings, pattern) → each argument is evaluated first, then the function body runs
#   get_strings_matching_pattern(?, 25) runs first → then print() outputs the result
#   get_strings_matching_pattern(?, 25) → each argument is evaluated first, then the function body runs
#   s.count(...) >= 2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_matching_pattern(strings, pattern) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_matching_pattern(strings, pattern) → stored in memory, not executed
#   5. def get_strings_matching_pattern(strings, pattern) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. pattern = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_strings_matching_pattern(...))
#      → calls get_strings_matching_pattern(strings, pattern) with strings=[4, 1, 8, 6, 3], pattern=25
#      → 10.1 return list(map(lambda s: s, strings))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_matching_pattern) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     pattern = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_matching_pattern(...)) → get_strings_matching_pattern() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1971:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_name_initials(first, middle, last) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_name_initials('John', 'Michael', 'Doe') → each argument is evaluated first, then the function body runs
#   format_name_initials(first, middle, last) → each argument is evaluated first, then the function body runs
#   format_name_initials(?, ?, ?) runs first → then print() outputs the result
#   format_name_initials(?, ?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_name_initials(first, middle, last) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_name_initials(first, middle, last) → stored in memory, not executed
#   5. def format_name_initials(first, middle, last) → stored in memory, not executed
#   6. first = ... → assigns value to variable
#   7. middle = ... → assigns value to variable
#   8. last = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(format_name_initials(...))
#      → calls format_name_initials(first, middle, last) with first=[4, 1, 8, 6, 3], middle=[4, 1, 8, 6, 3], last=[4, 1, 8, 6, 3]
#      → 11.1 return f'{first[0]}.{middle[0]}.{last[0]}.'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_name_initials) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     first = ... → stored in memory
#     middle = ... → stored in memory
#     last = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_name_initials(...)) → format_name_initials() runs first → value passed to print()

"""

"""
Problem 1972:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_with_condition(my_list, condition_func) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
# Built-in functions: condition_func()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_with_condition(?, ?) → each argument is evaluated first, then the function body runs
#   get_elements_with_condition(my_list, condition_func) → each argument is evaluated first, then the function body runs
#   get_elements_with_condition(?, 25) runs first → then print() outputs the result
#   get_elements_with_condition(?, 25) → each argument is evaluated first, then the function body runs
#   x > 3 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_with_condition(my_list, condition_func) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_with_condition(my_list, condition_func) → stored in memory, not executed
#   5. def get_elements_with_condition(my_list, condition_func) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. condition_func = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_elements_with_condition(...))
#      → calls get_elements_with_condition(my_list, condition_func) with my_list=[4, 1, 8, 6, 3], condition_func=25
#      → 10.1 return [x for x in my_list if condition_func(x)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_with_condition) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     condition_func = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_with_condition(...)) → get_elements_with_condition() runs first → value passed to print()

"""

"""
Problem 1973:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_total_area(shapes) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: * ** ==
# Built-in functions: round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_total_area(?) → each argument is evaluated first, then the function body runs
#   calculate_total_area(shapes) → each argument is evaluated first, then the function body runs
#   calculate_total_area(25) runs first → then print() outputs the result
#   calculate_total_area(25) → each argument is evaluated first, then the function body runs
#   shape_type == 'rectangle' → evaluated as True or False (determines which branch runs)
#   ? * ? → Python looks up ?, then ?, then applies *
#   shape_type == 'circle' → evaluated as True or False (determines which branch runs)
#   math.pi * ? → Python looks up math.pi, then ?, then applies *
#   ? ** 2 → Python looks up ?, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def calculate_total_area(shapes) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(round(...))
#   5. def calculate_total_area(shapes) → stored in memory, not executed
#   6. def calculate_total_area(shapes) → stored in memory, not executed
#   7. shapes = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_total_area(...))
#      → calls calculate_total_area(shapes) with shapes=25
#      → 10.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_total_area) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(round(...))
#     shapes = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_total_area(...)) → calculate_total_area() runs first → value passed to print()
#
#   Loop: takes one item from shapes each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1974:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_words_with_repeated_chars(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: !=
# Built-in functions: len, list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_words_with_repeated_chars('hello world python programming') → each argument is evaluated first, then the function body runs
#   get_words_with_repeated_chars(text) → each argument is evaluated first, then the function body runs
#   get_words_with_repeated_chars('world') runs first → then print() outputs the result
#   get_words_with_repeated_chars('world') → each argument is evaluated first, then the function body runs
#   len(...) != len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_words_with_repeated_chars(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_words_with_repeated_chars(text) → stored in memory, not executed
#   5. def get_words_with_repeated_chars(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_words_with_repeated_chars(...))
#      → calls get_words_with_repeated_chars(text) with text='world'
#      → 9.1 return list(map(lambda word: word, words))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_words_with_repeated_chars) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_words_with_repeated_chars(...)) → get_words_with_repeated_chars() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1975:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_multi_value_string(name, scores) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_multi_value_string('Alice', ?) → each argument is evaluated first, then the function body runs
#   format_multi_value_string(name, scores) → each argument is evaluated first, then the function body runs
#   format_multi_value_string('Bob', ?) runs first → then print() outputs the result
#   'Name: %s, Scores: %s' % ? → Python looks up 'Name: %s, Scores: %s', then ?, then applies %
#   format_multi_value_string('Bob', ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_multi_value_string(name, scores) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_multi_value_string(name, scores) → stored in memory, not executed
#   5. def format_multi_value_string(name, scores) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. scores = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(format_multi_value_string(...))
#      → calls format_multi_value_string(name, scores) with name='Bob', scores=[4, 1, 8, 6, 3]
#      → 10.1 return 'Name: %s, Scores: %s' % (name, scores_str)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_multi_value_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     name = ... → stored in memory
#     scores = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_multi_value_string(...)) → format_multi_value_string() runs first → value passed to print()

"""

"""
Problem 1976:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_symmetric_difference(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_symmetric_difference(?, ?) → each argument is evaluated first, then the function body runs
#   get_symmetric_difference(list1, list2) → each argument is evaluated first, then the function body runs
#   get_symmetric_difference(?, ?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_symmetric_difference(list1, list2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(sorted(...))
#   4. def get_symmetric_difference(list1, list2) → stored in memory, not executed
#   5. def get_symmetric_difference(list1, list2) → stored in memory, not executed
#   6. list1 = ... → assigns value to variable
#   7. list2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_symmetric_difference(...))
#      → calls get_symmetric_difference(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 10.1 return list(set1.symmetric_difference(set2))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_symmetric_difference) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(sorted(...))
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_symmetric_difference(...)) → get_symmetric_difference() runs first → value passed to print()

"""

"""
Problem 1977:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words_by_first_letter(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words_by_first_letter('apple apricot banana cherry') → each argument is evaluated first, then the function body runs
#   count_words_by_first_letter(text) → each argument is evaluated first, then the function body runs
#   count_words_by_first_letter('world') runs first → then print() outputs the result
#   count_words_by_first_letter('world') → each argument is evaluated first, then the function body runs
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words_by_first_letter(text) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_words_by_first_letter(text) → stored in memory, not executed
#   5. def count_words_by_first_letter(text) → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_words_by_first_letter(...))
#      → calls count_words_by_first_letter(text) with text='world'
#      → 9.1 return counts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words_by_first_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words_by_first_letter(...)) → count_words_by_first_letter() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 1978:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_decimal_places(number, decimal_places) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_decimal_places(3.14159, 2) → each argument is evaluated first, then the function body runs
#   format_decimal_places(number, decimal_places) → each argument is evaluated first, then the function body runs
#   format_decimal_places(25, 25) runs first → then print() outputs the result
#   format_decimal_places(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_decimal_places(number, decimal_places) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def format_decimal_places(number, decimal_places) → stored in memory, not executed
#   5. def format_decimal_places(number, decimal_places) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. decimal_places = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(format_decimal_places(...))
#      → calls format_decimal_places(number, decimal_places) with number=25, decimal_places=25
#      → 10.1 return f'{number:.{decimal_places}f}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_decimal_places) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     decimal_places = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_decimal_places(...)) → format_decimal_places() runs first → value passed to print()

"""

"""
Problem 1979:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_satisfying_all(my_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: all, cond, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_satisfying_all(?, ?, ?) → each argument is evaluated first, then the function body runs
#   get_elements_satisfying_all(my_list, ?) → each argument is evaluated first, then the function body runs
#   get_elements_satisfying_all(?, 25) runs first → then print() outputs the result
#   get_elements_satisfying_all(?, 25) → each argument is evaluated first, then the function body runs
#   x > 2 → evaluated as True or False (determines which branch runs)
#   x < 6 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_satisfying_all(my_list) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_elements_satisfying_all(my_list) → stored in memory, not executed
#   5. def get_elements_satisfying_all(my_list) → stored in memory, not executed
#   6. my_list = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_elements_satisfying_all(...))
#      → calls get_elements_satisfying_all(my_list) with my_list=[4, 1, 8, 6, 3]
#      → 10.1 return list(map(lambda item: item, my_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_elements_satisfying_all) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     my_list = ... → stored in memory
#     ? = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_elements_satisfying_all(...)) → get_elements_satisfying_all() runs first → value passed to print()
#
#   Loop: takes one item from my_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1980:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_net_price(original_price, discount_percent, tax_rate) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + - /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_net_price(100, 20, 10) → each argument is evaluated first, then the function body runs
#   calculate_net_price(original_price, discount_percent, tax_rate) → each argument is evaluated first, then the function body runs
#   calculate_net_price(25, 25, 25) runs first → then print() outputs the result
#   original_price * ? → Python looks up original_price, then ?, then applies *
#   discounted * ? → Python looks up discounted, then ?, then applies *
#   calculate_net_price(25, 25, 25) → each argument is evaluated first, then the function body runs
#   1 - ? → Python looks up 1, then ?, then applies -
#   1 + ? → Python looks up 1, then ?, then applies +
#   discount_percent / 100 → Python looks up discount_percent, then 100, then applies /
#   tax_rate / 100 → Python looks up tax_rate, then 100, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_net_price(original_price, discount_percent, tax_rate) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_net_price(original_price, discount_percent, tax_rate) → stored in memory, not executed
#   5. def calculate_net_price(original_price, discount_percent, tax_rate) → stored in memory, not executed
#   6. original_price = ... → assigns value to variable
#   7. discount_percent = ... → assigns value to variable
#   8. tax_rate = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_net_price(...))
#      → calls calculate_net_price(original_price, discount_percent, tax_rate) with original_price=25, discount_percent=25, tax_rate=25
#      → 11.1 return final
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_net_price) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     original_price = ... → stored in memory
#     discount_percent = ... → stored in memory
#     tax_rate = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_net_price(...)) → calculate_net_price() runs first → value passed to print()

"""

"""
Problem 1981:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_strings_with_all_vowels(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_strings_with_all_vowels(?) → each argument is evaluated first, then the function body runs
#   get_strings_with_all_vowels(strings) → each argument is evaluated first, then the function body runs
#   get_strings_with_all_vowels(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_strings_with_all_vowels(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def get_strings_with_all_vowels(strings) → stored in memory, not executed
#   5. def get_strings_with_all_vowels(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_strings_with_all_vowels(...))
#      → calls get_strings_with_all_vowels(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return list(map(lambda s: s, strings))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_strings_with_all_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_strings_with_all_vowels(...)) → get_strings_with_all_vowels() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1984:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 1997:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_elements_matching_multiple_patterns(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: all, isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_elements_matching_multiple_patterns(?, 'o', 'r') → each argument is evaluated first, then the function body runs
#   main() → each argument is evaluated first, then the function body runs
#   pattern in s → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_elements_matching_multiple_patterns(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def main() → stored in memory, not executed
#   5. main() → calls main() with 
#      → 5.1 lst = ['a', 'b', 'c']
#      → 5.2 result = True
#      → 5.3 for x in lst:
#      → 5.3     if not isinstance(x, str):
#      → 5.3         result = False
#      → 5.3         break
#      → 5.4 print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_elements_matching_multiple_patterns) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     main() → jumps into main(), runs body, returns
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 1998:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_statistical_summary(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % + - / //
# Built-in functions: len, max, min, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_statistical_summary(?) → each argument is evaluated first, then the function body runs
#   sum(...) / n → Python looks up sum(...), then n, then applies /
#   n % 2 → Python looks up n, then 2, then applies %
#   ? / 2 → Python looks up ?, then 2, then applies /
#   max_val - min_val → Python looks up max_val, then min_val, then applies -
#   n // 2 → Python looks up n, then 2, then applies //
#   ? + ? → Python looks up ?, then ?, then applies +
#   ? - 1 → Python looks up ?, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_statistical_summary(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_statistical_summary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 2000:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

