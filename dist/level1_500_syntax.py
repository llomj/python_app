# Syntax documentation for level 1 (problems 1-500)
# Auto-generated by gen_syntax_files.py

"""
Problem 1:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def add_number(a, b)
      Returns a value via 'return' keyword
    def add_num(a, b)
      Returns a value via 'return' keyword
    def add_number(a, b)
      Returns a value via 'return' keyword
    def add_number(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., ()
  Functions called: add_num, add_number, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a + b
  • Function call: print(value)
  • Function call: add_number(5, 5)
  • Binary operation: a + b
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: add_num(5, 5)
  • Function call: sum(value)
  • Function call: operator.add(a, b)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: add_number(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: add_number(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'add_number' (stored in memory, not executed yet)
  2. Execute function call: print(add_number(...))
  3. Define function 'add_num' (stored in memory, not executed yet)
  4. Execute function call: print(add_num(...))
  5. Define function 'add_number' (stored in memory, not executed yet)
  6. Define function 'add_number' (stored in memory, not executed yet)
  7. Assign value to variable: a
  8. Assign value to variable: b
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(add_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'add_number' is defined but NOT executed until called
    - 'add_num' is defined but NOT executed until called
    - 'add_number' is defined but NOT executed until called
    - 'add_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 2:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def multiply_numbers(a, b)
      Returns a value via 'return' keyword
    def multiply_numbers(a, b)
      Returns a value via 'return' keyword
    def multiply_numbers(a, b)
      Returns a value via 'return' keyword
    def multiply_numbers(a, b)
      Returns a value via 'return' keyword
    def multiply_numbers(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, -, (), >=
  Control structures: for loop iteration
  Functions called: abs, multiply_numbers, pow, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a * b
  • Function call: print(value)
  • Function call: multiply_numbers(5, 5)
  • Binary operation: a * b
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: multiply_numbers(3, 7)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: abs(b)
  • Function call: print(value)
  • Function call: multiply_numbers(4, 6)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(b)
  • Function call: pow(a, b)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: multiply_numbers(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: multiply_numbers(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'multiply_numbers' (stored in memory, not executed yet)
  2. Execute function call: print(multiply_numbers(...))
  3. Define function 'multiply_numbers' (stored in memory, not executed yet)
  4. Execute function call: print(multiply_numbers(...))
  5. Define function 'multiply_numbers' (stored in memory, not executed yet)
  6. Execute function call: print(multiply_numbers(...))
  7. Define function 'multiply_numbers' (stored in memory, not executed yet)
  8. Define function 'multiply_numbers' (stored in memory, not executed yet)
  9. Assign value to variable: a
  10. Assign value to variable: b
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(multiply_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'multiply_numbers' is defined but NOT executed until called
    - 'multiply_numbers' is defined but NOT executed until called
    - 'multiply_numbers' is defined but NOT executed until called
    - 'multiply_numbers' is defined but NOT executed until called
    - 'multiply_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 3:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def find_max(number)
      Returns a value via 'return' keyword
    def find_max(numbers)
      Returns a value via 'return' keyword
    def find_max(number)
      Returns a value via 'return' keyword
    def find_max(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, >, (), []
  Control structures: for loop iteration
  Functions called: find_max, max, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: find_max(result)
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Assignment: right side evaluated first, then stored in 'numbers_list'
  • Function call: print('The maximum number in the list is:', value)
  • Function call: find_max(numbers_list)
  • Function call: sorted(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: find_max(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_max(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_max' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(find_max(...))
  4. Define function 'find_max' (stored in memory, not executed yet)
  5. Assign value to variable: numbers_list
  6. Execute function call: print('The maximum number in the list is:', find_max(...))
  7. Define function 'find_max' (stored in memory, not executed yet)
  8. Define function 'find_max' (stored in memory, not executed yet)
  9. Assign value to variable: number
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_max(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_max' is defined but NOT executed until called
    - 'find_max' is defined but NOT executed until called
    - 'find_max' is defined but NOT executed until called
    - 'find_max' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over number, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 4:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def calculate_average(number)
      Returns a value via 'return' keyword
    def cal_average(number)
      Returns a value via 'return' keyword
    def calculate_average(number)
      Returns a value via 'return' keyword
    def calculate_average(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: /, ()
  Control structures: for loop iteration
  Functions called: cal_average, calculate_average, len, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: sum(...) / len(...)
  • Function call: sum(number)
  • Function call: len(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: calculate_average(lst)
  • Binary operation: sum(...) / len(...)
  • Function call: sum(number)
  • Function call: len(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: cal_average(lst)
  • Binary operation: sum(...) / len(...)
  • Function call: sum(number)
  • Function call: len(number)
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: calculate_average(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_average' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(calculate_average(...))
  4. Define function 'cal_average' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(cal_average(...))
  7. Define function 'calculate_average' (stored in memory, not executed yet)
  8. Define function 'calculate_average' (stored in memory, not executed yet)
  9. Assign value to variable: number
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(calculate_average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_average' is defined but NOT executed until called
    - 'cal_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over number, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 5:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: count_vowels, len, print, sum
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: count_vowels(result)
  • Function call: len(value)
  • Function call: string.lower()
  • Function call: print(value)
  • Function call: count_vowels('Jonathan')
  • Function call: sum(value)
  • Function call: string.lower()
  • Function call: print(value)
  • Function call: count_vowels('Python')
  • Function call: sum(value)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_vowels(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(count_vowels(...))
  4. Define function 'count_vowels' (stored in memory, not executed yet)
  5. Execute function call: print(count_vowels(...))
  6. Define function 'count_vowels' (stored in memory, not executed yet)
  7. Execute function call: print(count_vowels(...))
  8. Define function 'count_vowels' (stored in memory, not executed yet)
  9. Define function 'count_vowels' (stored in memory, not executed yet)
  10. Assign value to variable: string
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 6:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def reverse_string(string)
      Returns a value via 'return' keyword
    def reverse_string(string)
      Returns a value via 'return' keyword
    def reverse_string(string)
      Returns a value via 'return' keyword
    def reverse_string(string)
      Returns a value via 'return' keyword
    def reverse_string(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, -, ., (), []
  Control structures: for loop iteration
  Functions called: print, reverse_string, reversed
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'string'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: reverse_string(result)
  • Function call: ''.join(value)
  • Function call: reversed(string)
  • Function call: print(value)
  • Function call: reverse_string('hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: reverse_string('Python')
  • Function call: ''.join(value)
  • Function call: reversed(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: reverse_string(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_string('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_string' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(reverse_string(...))
  4. Define function 'reverse_string' (stored in memory, not executed yet)
  5. Execute function call: print(reverse_string(...))
  6. Define function 'reverse_string' (stored in memory, not executed yet)
  7. Execute function call: print(reverse_string(...))
  8. Define function 'reverse_string' (stored in memory, not executed yet)
  9. Define function 'reverse_string' (stored in memory, not executed yet)
  10. Assign value to variable: string
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(reverse_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 7:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_even(number)
      Returns a value via 'return' keyword
    def is_even(number)
      Returns a value via 'return' keyword
    def is_even(number)
      Returns a value via 'return' keyword
    def is_even(number)
      Returns a value via 'return' keyword
    def is_even(number)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, &, *, (), //, ==
  Functions called: is_even, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: number % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: is_even(result)
  • Binary operation: number & 1
  • Function call: print(value)
  • Function call: is_even(8)
  • Binary operation: expression * 2
  • Binary operation: number // 2
  • Function call: print(value)
  • Function call: is_even(12)
  • Binary operation: number % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: number % 2
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_even(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_even(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_even' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(is_even(...))
  4. Define function 'is_even' (stored in memory, not executed yet)
  5. Execute function call: print(is_even(...))
  6. Define function 'is_even' (stored in memory, not executed yet)
  7. Execute function call: print(is_even(...))
  8. Define function 'is_even' (stored in memory, not executed yet)
  9. Define function 'is_even' (stored in memory, not executed yet)
  10. Assign value to variable: number
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(is_even(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_even' is defined but NOT executed until called
    - 'is_even' is defined but NOT executed until called
    - 'is_even' is defined but NOT executed until called
    - 'is_even' is defined but NOT executed until called
    - 'is_even' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 8:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def cal_square(number)
      Returns a value via 'return' keyword
    def calculate_square(number)
      Returns a value via 'return' keyword
    def cal_square(number)
      Returns a value via 'return' keyword
    def cal_square(number)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), **
  Functions called: cal_square, calculate_square, pow, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: number ** 2
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: cal_square(lst)
  • Binary operation: number ** 2
  • Assignment: right side evaluated first, then stored in 'square'
  • Function call: print(value)
  • Function call: calculate_square(6)
  • Function call: pow(number, 2)
  • Function call: math.pow(number, 2)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: cal_square(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: cal_square(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'cal_square' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(cal_square(...))
  4. Define function 'calculate_square' (stored in memory, not executed yet)
  5. Execute function call: print(calculate_square(...))
  6. Define function 'cal_square' (stored in memory, not executed yet)
  7. Define function 'cal_square' (stored in memory, not executed yet)
  8. Assign value to variable: number
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(cal_square(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'cal_square' is defined but NOT executed until called
    - 'calculate_square' is defined but NOT executed until called
    - 'cal_square' is defined but NOT executed until called
    - 'cal_square' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 9:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def greet_user(string)
      Returns a value via 'return' keyword
    def greet_user(name)
      Returns a value via 'return' keyword
    def greet_user(name)
      Returns a value via 'return' keyword
    def greet_user(string)
      Returns a value via 'return' keyword
    def greet_user(string)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, +, ., ()
  Functions called: greet_user, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: print(value)
  • Function call: greet_user('Jonathan')
  • Binary operation: expression + '!'
  • Binary operation: 'Hello, ' + name
  • Function call: print(value)
  • Function call: greet_user('Alice')
  • Function call: 'Hello, {}!'.format(name)
  • Function call: print(value)
  • Function call: greet_user('Bob')
  • Function call: 'hello {}'.format(string)
  • Binary operation: 'hello %s' % string
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: greet_user(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: greet_user('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'greet_user' (stored in memory, not executed yet)
  2. Execute function call: print(greet_user(...))
  3. Define function 'greet_user' (stored in memory, not executed yet)
  4. Execute function call: print(greet_user(...))
  5. Define function 'greet_user' (stored in memory, not executed yet)
  6. Execute function call: print(greet_user(...))
  7. Define function 'greet_user' (stored in memory, not executed yet)
  8. Define function 'greet_user' (stored in memory, not executed yet)
  9. Assign value to variable: string
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(greet_user(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'greet_user' is defined but NOT executed until called
    - 'greet_user' is defined but NOT executed until called
    - 'greet_user' is defined but NOT executed until called
    - 'greet_user' is defined but NOT executed until called
    - 'greet_user' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 10:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def square_lst(numbers)
      Returns a value via 'return' keyword
    def square_lst(numbers)
      Returns a value via 'return' keyword
    def square_lst(numbers)
      Returns a value via 'return' keyword
    def square_lst(numbers)
      Returns a value via 'return' keyword
    def square_lst(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., (), **
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: list, map, print, square_lst
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'squared_numbers'
  • Binary operation: number ** 2
  • Function call: squared_numbers.append(computed expression)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: square_lst(result)
  • Binary operation: n ** 2
  • Function call: print(value)
  • Function call: square_lst(value)
  • Binary operation: x ** 2
  • Function call: list(value)
  • Function call: map(value, numbers)
  • Function call: print(value)
  • Function call: square_lst(value)
  • Binary operation: number ** 2
  • Binary operation: number ** 2
  • Function call: list(value)
  • Function call: map(value, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: square_lst(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: square_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'square_lst' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(square_lst(...))
  4. Define function 'square_lst' (stored in memory, not executed yet)
  5. Execute function call: print(square_lst(...))
  6. Define function 'square_lst' (stored in memory, not executed yet)
  7. Execute function call: print(square_lst(...))
  8. Define function 'square_lst' (stored in memory, not executed yet)
  9. Define function 'square_lst' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(square_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'square_lst' is defined but NOT executed until called
    - 'square_lst' is defined but NOT executed until called
    - 'square_lst' is defined but NOT executed until called
    - 'square_lst' is defined but NOT executed until called
    - 'square_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 11:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def covert_uppercase(string)
      Returns a value via 'return' keyword
    def covert_uppercase(string)
      Returns a value via 'return' keyword
    def covert_uppercase(string)
      Returns a value via 'return' keyword
    def covert_uppercase(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., (), <=
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: chr, covert_uppercase, ord, print, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.upper()
  • Function call: print(value)
  • Function call: covert_uppercase('Jonathan')
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Binary operation: ord(...) - 32
  • Function call: chr(computed expression)
  • Function call: ord(char)
  • Function call: print(value)
  • Function call: covert_uppercase('hello')
  • Binary operation: ord(...) - 32
  • Function call: ''.join(value)
  • Function call: chr(computed expression)
  • Function call: ord(c)
  • Function call: str(...).upper()
  • Function call: str(string)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: covert_uppercase(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: covert_uppercase('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'covert_uppercase' (stored in memory, not executed yet)
  2. Execute function call: print(covert_uppercase(...))
  3. Define function 'covert_uppercase' (stored in memory, not executed yet)
  4. Execute function call: print(covert_uppercase(...))
  5. Define function 'covert_uppercase' (stored in memory, not executed yet)
  6. Define function 'covert_uppercase' (stored in memory, not executed yet)
  7. Assign value to variable: string
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(covert_uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'covert_uppercase' is defined but NOT executed until called
    - 'covert_uppercase' is defined but NOT executed until called
    - 'covert_uppercase' is defined but NOT executed until called
    - 'covert_uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 12:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def calculate_sum(number)
      Returns a value via 'return' keyword
    def cal_sum(numbers)
      Returns a value via 'return' keyword
    def calculate_sum(number)
      Returns a value via 'return' keyword
    def calculate_sum(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: cal_sum, calculate_sum, print, reduce, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sum(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: calculate_sum(lst)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: cal_sum(lst)
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: reduce(value, number, 0)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: calculate_sum(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_sum(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_sum' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(calculate_sum(...))
  4. Define function 'cal_sum' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(cal_sum(...))
  7. Define function 'calculate_sum' (stored in memory, not executed yet)
  8. Define function 'calculate_sum' (stored in memory, not executed yet)
  9. Assign value to variable: number
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(calculate_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_sum' is defined but NOT executed until called
    - 'cal_sum' is defined but NOT executed until called
    - 'calculate_sum' is defined but NOT executed until called
    - 'calculate_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over number, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 13:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_odd(number)
      Returns a value via 'return' keyword
    def is_odd(number)
      Returns a value via 'return' keyword
    def is_odd(number)
      Returns a value via 'return' keyword
    def is_odd(number)
      Returns a value via 'return' keyword
    def is_odd(number)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, &, !=, (), ==, not
  Functions called: is_odd, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: number % 2
  • Function call: print(value)
  • Function call: is_odd(10)
  • Function call: print(value)
  • Function call: is_odd(7)
  • Binary operation: number & 1
  • Function call: print(value)
  • Function call: is_odd(5)
  • Binary operation: number % 2
  • Function call: print(value)
  • Function call: is_odd(9)
  • Binary operation: number % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: number % 2
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_odd(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_odd(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_odd' (stored in memory, not executed yet)
  2. Execute function call: print(is_odd(...))
  3. Execute function call: print(is_odd(...))
  4. Define function 'is_odd' (stored in memory, not executed yet)
  5. Execute function call: print(is_odd(...))
  6. Define function 'is_odd' (stored in memory, not executed yet)
  7. Execute function call: print(is_odd(...))
  8. Define function 'is_odd' (stored in memory, not executed yet)
  9. Define function 'is_odd' (stored in memory, not executed yet)
  10. Assign value to variable: number
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(is_odd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_odd' is defined but NOT executed until called
    - 'is_odd' is defined but NOT executed until called
    - 'is_odd' is defined but NOT executed until called
    - 'is_odd' is defined but NOT executed until called
    - 'is_odd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 14:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def concatenate_strings(string1, string2)
      Returns a value via 'return' keyword
    def concatenate_string(string1, string2)
      Returns a value via 'return' keyword
    def concatenate_strings(string1, string2)
      Returns a value via 'return' keyword
    def concatenate_strings(string1, string2)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, +, ., ()
  Functions called: concatenate_string, concatenate_strings, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: string1 + string2
  • Assignment: right side evaluated first, then stored in 'concatenate'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: concatenate_strings(lst1, lst2)
  • Binary operation: string1 + string2
  • Function call: print(value)
  • Function call: concatenate_string('Jonathan', 'noll')
  • Function call: ''.join(value)
  • Binary operation: '%s%s' % expression
  • Assignment: right side evaluated first, then stored in 'string1'
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: concatenate_strings(string1, string2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: concatenate_strings('world', 'python')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'concatenate_strings' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(concatenate_strings(...))
  5. Define function 'concatenate_string' (stored in memory, not executed yet)
  6. Execute function call: print(concatenate_string(...))
  7. Define function 'concatenate_strings' (stored in memory, not executed yet)
  8. Define function 'concatenate_strings' (stored in memory, not executed yet)
  9. Assign value to variable: string1
  10. Assign value to variable: string2
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(concatenate_strings(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'concatenate_strings' is defined but NOT executed until called
    - 'concatenate_string' is defined but NOT executed until called
    - 'concatenate_strings' is defined but NOT executed until called
    - 'concatenate_strings' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 15:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def find_length(string)
      Returns a value via 'return' keyword
    def find_len(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration; generator expression
  Functions called: find_len, find_length, len, print, sum
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(string)
  • Function call: print(value)
  • Function call: find_length('Jonathan')
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: print(value)
  • Function call: find_len('Jonathan')
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: sum(value)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: find_length(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_length('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_length' (stored in memory, not executed yet)
  2. Execute function call: print(find_length(...))
  3. Define function 'find_len' (stored in memory, not executed yet)
  4. Execute function call: print(find_len(...))
  5. Define function 'find_length' (stored in memory, not executed yet)
  6. Define function 'find_length' (stored in memory, not executed yet)
  7. Assign value to variable: string
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(find_length(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_length' is defined but NOT executed until called
    - 'find_len' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 16:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, -, ., !=, (), //, ==, []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: is_palindrome, len, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.replace(...).lower()
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: is_palindrome('tom')
  • Function call: print(value)
  • Function call: is_palindrome('racecar')
  • Function call: string.replace(...).lower()
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string'
  • Binary operation: len(...) // 2
  • Function call: range(computed expression)
  • Function call: len(string)
  • Binary operation: i + 1
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_palindrome('madam')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: is_palindrome(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Execute function call: print(is_palindrome(...))
  3. Execute function call: print(is_palindrome(...))
  4. Define function 'is_palindrome' (stored in memory, not executed yet)
  5. Execute function call: print(is_palindrome(...))
  6. Define function 'is_palindrome' (stored in memory, not executed yet)
  7. Define function 'is_palindrome' (stored in memory, not executed yet)
  8. Assign value to variable: string
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 17:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: count_words, len, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'word_count'
  • Assignment: right side evaluated first, then stored in 'example_string'
  • Function call: print(value)
  • Function call: count_words(example_string)
  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: len(words)
  • Assignment: right side evaluated first, then stored in 'example_string'
  • Function call: print(value)
  • Function call: count_words(example_string)
  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: len(words)
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: count_words(arg)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_words(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_words('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_words' (stored in memory, not executed yet)
  2. Assign value to variable: example_string
  3. Execute function call: print(count_words(...))
  4. Define function 'count_words' (stored in memory, not executed yet)
  5. Assign value to variable: example_string
  6. Execute function call: print(count_words(...))
  7. Define function 'count_words' (stored in memory, not executed yet)
  8. Assign value to variable: arg
  9. Execute function call: print(count_words(...))
  10. Define function 'count_words' (stored in memory, not executed yet)
  11. Define function 'count_words' (stored in memory, not executed yet)
  12. Assign value to variable: string
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(count_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 18:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_min()
    def find_min(numbers)
      Returns a value via 'return' keyword
    def find_minimum(numbers)
      Returns a value via 'return' keyword
    def find_min()
      Returns a value via 'return' keyword
    def find_min()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., <, (), []
  Control structures: for loop iteration
  Functions called: find_min, find_minimum, input, int, min, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: print(min_num)
  • Function call: find_min()
  • Function call: min(numbers)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_min(lst)
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Function call: print(value)
  • Function call: find_minimum(min_number)
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: find_min(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min' (stored in memory, not executed yet)
  2. Execute function call: find_min()
  3. Define function 'find_min' (stored in memory, not executed yet)
  4. Assign value to variable: lst
  5. Execute function call: print(find_min(...))
  6. Define function 'find_minimum' (stored in memory, not executed yet)
  7. Assign value to variable: min_number
  8. Execute function call: print(find_minimum(...))
  9. Define function 'find_min' (stored in memory, not executed yet)
  10. Define function 'find_min' (stored in memory, not executed yet)
  11. Assign value to variable: numbers
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(find_min(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_minimum' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
  Runtime execution (top-level code):
    - find_min() is invoked
      → Execution jumps to the function body of 'find_min'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 19:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def is_even_index_sum(numbers)
      Returns a value via 'return' keyword
    def is_even_index_sum(numbers)
      Returns a value via 'return' keyword
    def is_even_index_sum(numbers)
      Returns a value via 'return' keyword
    def is_even_index_sum(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, (), ==, []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: is_even_index_sum, len, print, range, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'even_index_sum'
  • Function call: range(value)
  • Function call: len(numbers)
  • Binary operation: i % 2
  • Condition branch: evaluates boolean expression first
  • Binary operation: even_index_sum % 2
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: is_even_index_sum(lst)
  • Function call: sum(value)
  • Assignment: right side evaluated first, then stored in 'even_index_sum'
  • Binary operation: even_index_sum % 2
  • Function call: print(value)
  • Function call: is_even_index_sum(value)
  • Function call: print(value)
  • Function call: is_even_index_sum(value)
  • Binary operation: even_index_sum % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: even_index_sum % 2
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: is_even_index_sum(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_even_index_sum(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_even_index_sum' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(is_even_index_sum(...))
  4. Define function 'is_even_index_sum' (stored in memory, not executed yet)
  5. Execute function call: print(is_even_index_sum(...))
  6. Execute function call: print(is_even_index_sum(...))
  7. Define function 'is_even_index_sum' (stored in memory, not executed yet)
  8. Define function 'is_even_index_sum' (stored in memory, not executed yet)
  9. Assign value to variable: numbers
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(is_even_index_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_even_index_sum' is defined but NOT executed until called
    - 'is_even_index_sum' is defined but NOT executed until called
    - 'is_even_index_sum' is defined but NOT executed until called
    - 'is_even_index_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 20:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def double_elements(elements)
      Returns a value via 'return' keyword
    def double_elements(numbers)
      Returns a value via 'return' keyword
    def double_elements(elements)
      Returns a value via 'return' keyword
    def double_elements(elements)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, ., ()
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: double_elements, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'doubled'
  • Function call: doubled.append(element)
  • Function call: doubled.append(element)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: double_elements(lst)
  • Assignment: right side evaluated first, then stored in 'doubled_numbers'
  • Binary operation: num * 2
  • Function call: doubled_numbers.append(computed expression)
  • Assignment: right side evaluated first, then stored in 'numbers_list'
  • Function call: print('The list with doubled elements is:', value)
  • Function call: double_elements(numbers_list)
  • Function call: list(value)
  • Function call: map(value, elements)
  • Assignment: right side evaluated first, then stored in 'elements'
  • Function call: double_elements(elements)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: double_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'double_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(double_elements(...))
  4. Define function 'double_elements' (stored in memory, not executed yet)
  5. Assign value to variable: numbers_list
  6. Execute function call: print('The list with doubled elements is:', double_elements(...))
  7. Define function 'double_elements' (stored in memory, not executed yet)
  8. Define function 'double_elements' (stored in memory, not executed yet)
  9. Assign value to variable: elements
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(double_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 21:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_all_positive(numbers)
      Returns a value via 'return' keyword
    def is_all_positive(numbers)
      Returns a value via 'return' keyword
    def is_all_positive(numbers)
      Returns a value via 'return' keyword
    def is_all_positive(numbers)
      Returns a value via 'return' keyword
    def is_all_positive(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, >, (), <=, and
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: all, is_all_positive, len, min, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: is_all_positive(lst)
  • Function call: all(value)
  • Function call: print(value)
  • Function call: is_all_positive(value)
  • Function call: print(value)
  • Function call: is_all_positive(value)
  • Function call: len(numbers)
  • Function call: min(numbers)
  • Function call: print(value)
  • Function call: is_all_positive(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: is_all_positive(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_all_positive(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_all_positive' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(is_all_positive(...))
  4. Define function 'is_all_positive' (stored in memory, not executed yet)
  5. Execute function call: print(is_all_positive(...))
  6. Execute function call: print(is_all_positive(...))
  7. Define function 'is_all_positive' (stored in memory, not executed yet)
  8. Execute function call: print(is_all_positive(...))
  9. Define function 'is_all_positive' (stored in memory, not executed yet)
  10. Define function 'is_all_positive' (stored in memory, not executed yet)
  11. Assign value to variable: numbers
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(is_all_positive(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_all_positive' is defined but NOT executed until called
    - 'is_all_positive' is defined but NOT executed until called
    - 'is_all_positive' is defined but NOT executed until called
    - 'is_all_positive' is defined but NOT executed until called
    - 'is_all_positive' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 22:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_average(numbers)
      Returns a value via 'return' keyword
    def find_average_lst(numbers)
      Returns a value via 'return' keyword
    def find_average_lst(numbers)
      Returns a value via 'return' keyword
    def find_average(numbers)
      Returns a value via 'return' keyword
    def find_average(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: /, >, ()
  Control structures: if/else conditional branching; for loop iteration
  Functions called: find_average, find_average_lst, len, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: sum(...) / len(...)
  • Function call: sum(numbers)
  • Function call: len(numbers)
  • Assignment: right side evaluated first, then stored in 'total_average'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_average(lst)
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'average'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_average_lst(lst)
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'average'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_average_lst(lst)
  • Binary operation: sum(...) / len(...)
  • Function call: sum(numbers)
  • Function call: len(numbers)
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: find_average(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_average' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_average(...))
  4. Define function 'find_average_lst' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(find_average_lst(...))
  7. Define function 'find_average_lst' (stored in memory, not executed yet)
  8. Assign value to variable: lst
  9. Execute function call: print(find_average_lst(...))
  10. Define function 'find_average' (stored in memory, not executed yet)
  11. Define function 'find_average' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(find_average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_average' is defined but NOT executed until called
    - 'find_average_lst' is defined but NOT executed until called
    - 'find_average_lst' is defined but NOT executed until called
    - 'find_average' is defined but NOT executed until called
    - 'find_average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 23:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def contains_negative(numbers)
      Returns a value via 'return' keyword
    def contains_negative(numbers)
      Returns a value via 'return' keyword
    def contains_negative(numbers)
      Returns a value via 'return' keyword
    def contains_negative(numbers)
      Returns a value via 'return' keyword
    def contains_negative(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, <, >, (), and
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: any, contains_negative, len, min, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: contains_negative(lst)
  • Function call: print(value)
  • Function call: contains_negative(value)
  • Function call: any(value)
  • Function call: print(value)
  • Function call: contains_negative(value)
  • Function call: print(value)
  • Function call: contains_negative(value)
  • Function call: len(numbers)
  • Function call: min(numbers)
  • Function call: print(value)
  • Function call: contains_negative(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: contains_negative(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: contains_negative(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'contains_negative' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(contains_negative(...))
  4. Execute function call: print(contains_negative(...))
  5. Define function 'contains_negative' (stored in memory, not executed yet)
  6. Execute function call: print(contains_negative(...))
  7. Execute function call: print(contains_negative(...))
  8. Define function 'contains_negative' (stored in memory, not executed yet)
  9. Execute function call: print(contains_negative(...))
  10. Define function 'contains_negative' (stored in memory, not executed yet)
  11. Define function 'contains_negative' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(contains_negative(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'contains_negative' is defined but NOT executed until called
    - 'contains_negative' is defined but NOT executed until called
    - 'contains_negative' is defined but NOT executed until called
    - 'contains_negative' is defined but NOT executed until called
    - 'contains_negative' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 24:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def find_last_element(string)
      Returns a value via 'return' keyword
    def find_element(elements)
      Returns a value via 'return' keyword
    def find_last_element(string)
      Returns a value via 'return' keyword
    def find_last_element(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, (), ==, []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: find_element, find_last_element, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_last_element(lst)
  • Assignment: right side evaluated first, then stored in 'last'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_element(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: find_last_element(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_last_element('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_last_element' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_last_element(...))
  4. Define function 'find_element' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(find_element(...))
  7. Define function 'find_last_element' (stored in memory, not executed yet)
  8. Define function 'find_last_element' (stored in memory, not executed yet)
  9. Assign value to variable: string
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_last_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_last_element' is defined but NOT executed until called
    - 'find_element' is defined but NOT executed until called
    - 'find_last_element' is defined but NOT executed until called
    - 'find_last_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 25:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def multiply_elements(numbers)
      Returns a value via 'return' keyword
    def multiply_elements(numbers)
      Returns a value via 'return' keyword
    def multiply_elements(numbers)
      Returns a value via 'return' keyword
    def multiply_elements(numbers)
      Returns a value via 'return' keyword
    def multiply_elements(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration
  Functions called: len, multiply_elements, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: range(value)
  • Function call: len(numbers)
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: multiply_elements(arg)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: multiply_elements(lst)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: multiply_elements(arg)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: multiply_elements(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: multiply_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'multiply_elements' (stored in memory, not executed yet)
  2. Assign value to variable: arg
  3. Execute function call: print(multiply_elements(...))
  4. Define function 'multiply_elements' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(multiply_elements(...))
  7. Define function 'multiply_elements' (stored in memory, not executed yet)
  8. Assign value to variable: arg
  9. Execute function call: print(multiply_elements(...))
  10. Define function 'multiply_elements' (stored in memory, not executed yet)
  11. Define function 'multiply_elements' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(multiply_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'multiply_elements' is defined but NOT executed until called
    - 'multiply_elements' is defined but NOT executed until called
    - 'multiply_elements' is defined but NOT executed until called
    - 'multiply_elements' is defined but NOT executed until called
    - 'multiply_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 26:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def has_duplicates(lst)
      Returns a value via 'return' keyword
    def has_duplicates()
      Returns a value via 'return' keyword
    def has_duplicates()
      Returns a value via 'return' keyword
    def has_duplicates(elements)
      Returns a value via 'return' keyword
    def has_duplicates(lst)
      Returns a value via 'return' keyword
    def has_duplicates(lst)
      Returns a value via 'return' keyword
    def has_duplicates(lst)
      Returns a value via 'return' keyword
    def has_duplicates(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., >, !=, (), in
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: any, has_duplicates, input, len, list, map, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Function call: unique.add(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: has_duplicates(lst)
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique'
  • Condition branch: evaluates boolean expression first
  • Function call: print('True')
  • Function call: unique.add(word)
  • Function call: print('False')
  • Function call: has_duplicates()
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique'
  • Condition branch: evaluates boolean expression first
  • Function call: unique.add(word)
  • Function call: print(value)
  • Function call: has_duplicates()
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'duplicate'
  • Condition branch: evaluates boolean expression first
  • Function call: duplicate.add(element)
  • Assignment: right side evaluated first, then stored in 'list_with_duplicates'
  • Assignment: right side evaluated first, then stored in 'list_without_duplicates'
  • Function call: print('Does the list have duplicates?', value)
  • Function call: has_duplicates(list_with_duplicates)
  • Function call: len(lst)
  • Function call: len(value)
  • Function call: set(lst)
  • Function call: print(value)
  • Function call: has_duplicates(value)
  • Function call: print(value)
  • Function call: has_duplicates(value)
  • Function call: any(value)
  • Function call: lst.count(x)
  • Function call: print(value)
  • Function call: has_duplicates(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: has_duplicates(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: has_duplicates(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'has_duplicates' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(has_duplicates(...))
  4. Define function 'has_duplicates' (stored in memory, not executed yet)
  5. Execute function call: has_duplicates()
  6. Define function 'has_duplicates' (stored in memory, not executed yet)
  7. Execute function call: print(has_duplicates(...))
  8. Define function 'has_duplicates' (stored in memory, not executed yet)
  9. Assign value to variable: list_with_duplicates
  10. Assign value to variable: list_without_duplicates
  11. Execute function call: print('Does the list have duplicates?', has_duplicates(...))
  12. Define function 'has_duplicates' (stored in memory, not executed yet)
  13. Execute function call: print(has_duplicates(...))
  14. Execute function call: print(has_duplicates(...))
  15. Define function 'has_duplicates' (stored in memory, not executed yet)
  16. Execute function call: print(has_duplicates(...))
  17. Define function 'has_duplicates' (stored in memory, not executed yet)
  18. Define function 'has_duplicates' (stored in memory, not executed yet)
  19. Assign value to variable: lst
  20. Assign value to variable: result
  21. Execute function call: print(result)
  22. Execute function call: print(has_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
    - 'has_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - has_duplicates() is invoked
      → Execution jumps to the function body of 'has_duplicates'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 27:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def count_occurrences(elements, target)
      Returns a value via 'return' keyword
    def count_occurrences(elements, target)
      Returns a value via 'return' keyword
    def count_occurrences(elements, target)
      Returns a value via 'return' keyword
    def count_occurrences(elements, target)
      Returns a value via 'return' keyword
    def count_occurrences(elements, target)
      Returns a value via 'return' keyword
    def count_occurrences(elements, target)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, ., (), ==
  Control structures: for loop iteration; generator expression
  Functions called: count_occurrences, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'word'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'word'
  • Assignment: right side evaluated first, then stored in 'elements'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: print(value)
  • Function call: count_occurrences(elements, target)
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'list_elements'
  • Assignment: right side evaluated first, then stored in 'target_element'
  • Function call: print(value)
  • Function call: count_occurrences(list_elements, target_element)
  • Function call: elements.count(target)
  • Function call: print(value)
  • Function call: count_occurrences(value, 2)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: count_occurrences(value, 'a')
  • Function call: 'The word {} occures {} times'.format(word, count)
  • Binary operation: 'The word %s occures %s times' % expression
  • Assignment: right side evaluated first, then stored in 'elements'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: count_occurrences(elements, target)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_occurrences(value, 'goal')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_occurrences' (stored in memory, not executed yet)
  2. Assign value to variable: elements
  3. Assign value to variable: target
  4. Execute function call: print(count_occurrences(...))
  5. Define function 'count_occurrences' (stored in memory, not executed yet)
  6. Assign value to variable: list_elements
  7. Assign value to variable: target_element
  8. Execute function call: print(value)
  9. Define function 'count_occurrences' (stored in memory, not executed yet)
  10. Execute function call: print(count_occurrences(...))
  11. Define function 'count_occurrences' (stored in memory, not executed yet)
  12. Execute function call: print(count_occurrences(...))
  13. Define function 'count_occurrences' (stored in memory, not executed yet)
  14. Define function 'count_occurrences' (stored in memory, not executed yet)
  15. Assign value to variable: elements
  16. Assign value to variable: target
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(count_occurrences(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 28:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def remove_vowels(string)
      Returns a value via 'return' keyword
    def remove_vowels(string)
      Returns a value via 'return' keyword
    def remove_vowels(string)
      Returns a value via 'return' keyword
    def remove_vowels(string)
      Returns a value via 'return' keyword
    def remove_vowels(string)
      Returns a value via 'return' keyword
    def remove_vowels(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), not in
  Control structures: for loop iteration; list comprehension
  Functions called: print, remove_vowels
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_vowels(lst)
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'vowels_removed'
  • Condition branch: evaluates boolean expression first
  • Function call: vowels_removed.append(char)
  • Function call: ''.join(vowels_removed)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_vowels(lst)
  • Function call: ''.join(value)
  • Function call: c.lower()
  • Function call: print(value)
  • Function call: remove_vowels('Python')
  • Function call: re.sub('[aeiouAEIOU]', '', string)
  • Function call: print(value)
  • Function call: remove_vowels('Hello World')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: remove_vowels(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_vowels(...))
  4. Define function 'remove_vowels' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(remove_vowels(...))
  7. Define function 'remove_vowels' (stored in memory, not executed yet)
  8. Execute function call: print(remove_vowels(...))
  9. Define function 'remove_vowels' (stored in memory, not executed yet)
  10. Execute function call: print(remove_vowels(...))
  11. Define function 'remove_vowels' (stored in memory, not executed yet)
  12. Define function 'remove_vowels' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(remove_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 29:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; generator expression
  Functions called: capitalize_words, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'capitalized_words'
  • Function call: capitalized_words.append(value)
  • Function call: word.capitalize()
  • Function call: ' '.join(capitalized_words)
  • Assignment: right side evaluated first, then stored in 'input_sentence'
  • Function call: print('Capitalized sentence:', value)
  • Function call: capitalize_words(input_sentence)
  • Function call: sentence.title()
  • Function call: print(value)
  • Function call: capitalize_words('python is awesome')
  • Function call: ' '.join(value)
  • Function call: word.capitalize()
  • Function call: sentence.split()
  • Function call: print(value)
  • Function call: capitalize_words('hello world')
  • Function call: ' '.join(capitalized_words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: ' '.join(capitalized_words)
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: capitalize_words(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: capitalize_words('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'capitalize_words' (stored in memory, not executed yet)
  2. Assign value to variable: input_sentence
  3. Execute function call: print('Capitalized sentence:', capitalize_words(...))
  4. Define function 'capitalize_words' (stored in memory, not executed yet)
  5. Execute function call: print(capitalize_words(...))
  6. Define function 'capitalize_words' (stored in memory, not executed yet)
  7. Execute function call: print(capitalize_words(...))
  8. Define function 'capitalize_words' (stored in memory, not executed yet)
  9. Define function 'capitalize_words' (stored in memory, not executed yet)
  10. Assign value to variable: sentence
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(capitalize_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 30:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def remove_spaces(sentence)
      Returns a value via 'return' keyword
    def remove_space(string)
      Returns a value via 'return' keyword
    def remove_spaces(sentence)
      Returns a value via 'return' keyword
    def remove_spaces(sentence)
      Returns a value via 'return' keyword
    def remove_spaces(sentence)
      Returns a value via 'return' keyword
    def remove_spaces(sentence)
      Returns a value via 'return' keyword
    def remove_spaces(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., !=, ()
  Control structures: for loop iteration; list comprehension
  Functions called: print, remove_space, remove_spaces
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sentence.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: remove_spaces(arg)
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'space_removed'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_space(lst)
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'split_word'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(word)
  • Function call: ''.join(result)
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: remove_spaces(arg)
  • Function call: ''.join(value)
  • Function call: sentence.split()
  • Function call: print(value)
  • Function call: remove_spaces('hello world')
  • Function call: ''.join(value)
  • Function call: print(value)
  • Function call: remove_spaces('a b c')
  • Function call: sentence.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sentence.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: remove_spaces(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_spaces('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_spaces' (stored in memory, not executed yet)
  2. Assign value to variable: arg
  3. Execute function call: print(remove_spaces(...))
  4. Define function 'remove_space' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(remove_space(...))
  7. Define function 'remove_spaces' (stored in memory, not executed yet)
  8. Assign value to variable: arg
  9. Execute function call: print(remove_spaces(...))
  10. Define function 'remove_spaces' (stored in memory, not executed yet)
  11. Execute function call: print(remove_spaces(...))
  12. Define function 'remove_spaces' (stored in memory, not executed yet)
  13. Execute function call: print(remove_spaces(...))
  14. Define function 'remove_spaces' (stored in memory, not executed yet)
  15. Define function 'remove_spaces' (stored in memory, not executed yet)
  16. Assign value to variable: sentence
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(remove_spaces(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_space' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over split_word, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 31:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def get_initials(full_name)
      Returns a value via 'return' keyword
    def get_initials(full_name)
      Returns a value via 'return' keyword
    def get_initials(full_name)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., (), []
  Control structures: generator expression
  Functions called: get_initials, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: full_name.split()
  • Assignment: right side evaluated first, then stored in 'parts'
  • Binary operation: '.'.join(...) + '.'
  • Function call: '.'.join(value)
  • Function call: expression.upper()
  • Function call: print(value)
  • Function call: get_initials('John Doe')
  • Binary operation: '.'.join(...) + '.'
  • Function call: '.'.join(value)
  • Function call: expression.upper()
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: '.'.join(...) + '.'
  • Function call: '.'.join(value)
  • Function call: expression.upper()
  • Assignment: right side evaluated first, then stored in 'full_name'
  • Function call: get_initials(full_name)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: get_initials('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'get_initials' (stored in memory, not executed yet)
  2. Execute function call: print(get_initials(...))
  3. Define function 'get_initials' (stored in memory, not executed yet)
  4. Define function 'get_initials' (stored in memory, not executed yet)
  5. Assign value to variable: full_name
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Execute function call: print(get_initials(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 32:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def reverse_word(string)
      Returns a value via 'return' keyword
    def reverse_words(words)
      Returns a value via 'return' keyword
    def reverse_words(string)
      Returns a value via 'return' keyword
    def reverse_words(string)
      Returns a value via 'return' keyword
    def reverse_word(string)
      Returns a value via 'return' keyword
    def reverse_word(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, -, ., (), []
  Control structures: for loop iteration
  Functions called: print, reverse_word, reverse_words, reversed
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'seperate'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse_word(lst)
  • Function call: words.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'reversed_words'
  • Function call: ' '.join(reversed_words)
  • Assignment: right side evaluated first, then stored in 'reversed_sentence'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse_words(lst)
  • Function call: ' '.join(value)
  • Function call: reversed(value)
  • Function call: string.split()
  • Function call: print(value)
  • Function call: reverse_words('python is fun')
  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: reversed(words)
  • Function call: result.append(word)
  • Function call: ' '.join(result)
  • Function call: print(value)
  • Function call: reverse_words('one two three')
  • Function call: ''.join(value)
  • Function call: reversed(seperate)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: reverse_word(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_word('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_word' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(reverse_word(...))
  4. Define function 'reverse_words' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(reverse_words(...))
  7. Define function 'reverse_words' (stored in memory, not executed yet)
  8. Execute function call: print(reverse_words(...))
  9. Define function 'reverse_words' (stored in memory, not executed yet)
  10. Execute function call: print(reverse_words(...))
  11. Define function 'reverse_word' (stored in memory, not executed yet)
  12. Define function 'reverse_word' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(reverse_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_word' is defined but NOT executed until called
    - 'reverse_words' is defined but NOT executed until called
    - 'reverse_words' is defined but NOT executed until called
    - 'reverse_words' is defined but NOT executed until called
    - 'reverse_word' is defined but NOT executed until called
    - 'reverse_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over reversed(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over seperate, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 33:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def remove_duplicates(input_list)
      Returns a value via 'return' keyword
    def remove_duplicates(input_list)
      Returns a value via 'return' keyword
    def remove_duplicates(input_list)
      Returns a value via 'return' keyword
    def remove_duplicates(input_list)
      Returns a value via 'return' keyword
    def remove_duplicates(input_list)
      Returns a value via 'return' keyword
    def remove_duplicates(input_list)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in, or, not, not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension
  Functions called: list, print, remove_duplicates, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: set(input_list)
  • Assignment: right side evaluated first, then stored in 'input_list'
  • Function call: print('List with duplicates removed:', value)
  • Function call: remove_duplicates(input_list)
  • Assignment: right side evaluated first, then stored in 'unique_elements'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_elements.append(element)
  • Assignment: right side evaluated first, then stored in 'input_list'
  • Function call: print('List with duplicates removed:', value)
  • Function call: remove_duplicates(input_list)
  • Function call: list(value)
  • Function call: dict.fromkeys(input_list)
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: seen.add(x)
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: list(value)
  • Function call: set(input_list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: list(value)
  • Function call: set(input_list)
  • Assignment: right side evaluated first, then stored in 'input_list'
  • Function call: remove_duplicates(input_list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Assign value to variable: input_list
  3. Execute function call: print('List with duplicates removed:', remove_duplicates(...))
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Assign value to variable: input_list
  6. Execute function call: print('List with duplicates removed:', remove_duplicates(...))
  7. Define function 'remove_duplicates' (stored in memory, not executed yet)
  8. Execute function call: print(remove_duplicates(...))
  9. Define function 'remove_duplicates' (stored in memory, not executed yet)
  10. Execute function call: print(remove_duplicates(...))
  11. Define function 'remove_duplicates' (stored in memory, not executed yet)
  12. Define function 'remove_duplicates' (stored in memory, not executed yet)
  13. Assign value to variable: input_list
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(remove_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over input_list, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 34:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def main(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., >, ()
  Control structures: for loop iteration
  Functions called: find_longest_word, len, main, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'largest_len'
  • Assignment: right side evaluated first, then stored in 'largest_word'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'largest_len'
  • Assignment: right side evaluated first, then stored in 'largest_word'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Function call: words.split()
  • Assignment: right side evaluated first, then stored in 'word_list'
  • Function call: max(word_list)
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print('Longest word:', value)
  • Function call: find_longest_word(input_string)
  • Function call: words.split()
  • Assignment: right side evaluated first, then stored in 'split_word'
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print('Longest word:', value)
  • Function call: find_longest_word(input_string)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: main(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: main(lst)
  4. Define function 'main' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: main(lst)
  7. Define function 'find_longest_word' (stored in memory, not executed yet)
  8. Assign value to variable: input_string
  9. Execute function call: print('Longest word:', find_longest_word(...))
  10. Define function 'find_longest_word' (stored in memory, not executed yet)
  11. Assign value to variable: input_string
  12. Execute function call: print('Longest word:', find_longest_word(...))
  13. Define function 'main' (stored in memory, not executed yet)
  14. Define function 'main' (stored in memory, not executed yet)
  15. Assign value to variable: lst
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(main(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_word, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 35:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def find_vowels(string)
      Returns a value via 'return' keyword
    def find_vowels(string)
      Returns a value via 'return' keyword
    def find_vowels(string)
      Returns a value via 'return' keyword
    def find_vowels(string)
      Returns a value via 'return' keyword
    def find_vowels(string)
      Returns a value via 'return' keyword
    def find_vowels(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: filter, find_vowels, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'return_vowels'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_vowels(lst)
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'vowel_list'
  • Condition branch: evaluates boolean expression first
  • Function call: vowel_list.append(char)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_vowels(lst)
  • Function call: char.lower()
  • Function call: print(value)
  • Function call: find_vowels('Python')
  • Function call: list(value)
  • Function call: filter(value, string)
  • Function call: c.lower()
  • Function call: print(value)
  • Function call: find_vowels('Hello World')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: find_vowels(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_vowels(...))
  4. Define function 'find_vowels' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(find_vowels(...))
  7. Define function 'find_vowels' (stored in memory, not executed yet)
  8. Execute function call: print(find_vowels(...))
  9. Define function 'find_vowels' (stored in memory, not executed yet)
  10. Execute function call: print(find_vowels(...))
  11. Define function 'find_vowels' (stored in memory, not executed yet)
  12. Define function 'find_vowels' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(find_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_vowels' is defined but NOT executed until called
    - 'find_vowels' is defined but NOT executed until called
    - 'find_vowels' is defined but NOT executed until called
    - 'find_vowels' is defined but NOT executed until called
    - 'find_vowels' is defined but NOT executed until called
    - 'find_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 36:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
  Keywords used: def, return, while
  Operators used: %, *, +, -, >, (), >=, []
  Control structures: while loop
  Functions called: abs, int, print, reverse_number, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: reverse_number(number)
  • Assignment: right side evaluated first, then stored in 'reversed_num'
  • Function call: abs(number)
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: num % 10
  • Assignment: right side evaluated first, then stored in 'digit'
  • Binary operation: expression + digit
  • Binary operation: reversed_num * 10
  • Assignment: right side evaluated first, then stored in 'reversed_num'
  • Function call: print(value)
  • Function call: reverse_number(value)
  • Function call: print(value)
  • Function call: reverse_number(1000)
  • Function call: int(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: int(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: reverse_number(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_number(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_number' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Execute function call: print(reverse_number(...))
  4. Define function 'reverse_number' (stored in memory, not executed yet)
  5. Execute function call: print(reverse_number(...))
  6. Execute function call: print(reverse_number(...))
  7. Define function 'reverse_number' (stored in memory, not executed yet)
  8. Define function 'reverse_number' (stored in memory, not executed yet)
  9. Assign value to variable: number
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(reverse_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
"""

"""
Problem 37:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def remove_duplicate(strings)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_dupicates(numbers)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicate(strings)
      Returns a value via 'return' keyword
    def remove_duplicate(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in, or, not, not in
  Control structures: for loop iteration; list comprehension
  Functions called: list, print, remove_dupicates, remove_duplicate, remove_duplicates, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: set(strings)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_duplicate(lst)
  • Assignment: right side evaluated first, then stored in 'unique_chars'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_chars.append(char)
  • Function call: ''.join(unique_chars)
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print(value)
  • Function call: remove_duplicates(input_string)
  • Assignment: right side evaluated first, then stored in 'duplicates'
  • Condition branch: evaluates boolean expression first
  • Function call: duplicates.append(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_dupicates(lst)
  • Function call: ''.join(value)
  • Function call: dict.fromkeys(string)
  • Function call: print(value)
  • Function call: remove_duplicates('hello')
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: ''.join(value)
  • Function call: seen.add(c)
  • Function call: print(value)
  • Function call: remove_duplicates('aabbcc')
  • Function call: list(value)
  • Function call: set(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: list(value)
  • Function call: set(strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: remove_duplicate(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicate('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicate' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_duplicate(...))
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Assign value to variable: input_string
  6. Execute function call: print(remove_duplicates(...))
  7. Define function 'remove_dupicates' (stored in memory, not executed yet)
  8. Assign value to variable: lst
  9. Execute function call: print(remove_dupicates(...))
  10. Define function 'remove_duplicates' (stored in memory, not executed yet)
  11. Execute function call: print(remove_duplicates(...))
  12. Define function 'remove_duplicates' (stored in memory, not executed yet)
  13. Execute function call: print(remove_duplicates(...))
  14. Define function 'remove_duplicate' (stored in memory, not executed yet)
  15. Define function 'remove_duplicate' (stored in memory, not executed yet)
  16. Assign value to variable: strings
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(remove_duplicate(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicate' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_dupicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicate' is defined but NOT executed until called
    - 'remove_duplicate' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 38:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def sum_of_list()
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
    def sum_of_list()
      Returns a value via 'return' keyword
    def sum_of_list()
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: +, ., ()
  Control structures: for loop iteration; lambda expression
  Functions called: input, int, print, reduce, sum, sum_of_list
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: print(count)
  • Function call: sum_of_list()
  • Function call: sum(numbers)
  • Function call: print(value)
  • Function call: sum_of_list(value)
  • Binary operation: x + y
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: sum_of_list(value)
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: sum_of_list(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_list' (stored in memory, not executed yet)
  2. Execute function call: sum_of_list()
  3. Define function 'sum_of_list' (stored in memory, not executed yet)
  4. Execute function call: print(sum_of_list(...))
  5. Define function 'sum_of_list' (stored in memory, not executed yet)
  6. Execute function call: print(sum_of_list(...))
  7. Define function 'sum_of_list' (stored in memory, not executed yet)
  8. Define function 'sum_of_list' (stored in memory, not executed yet)
  9. Assign value to variable: numbers
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(sum_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - sum_of_list() is invoked
      → Execution jumps to the function body of 'sum_of_list'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 39:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
    def sum_of_list(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: +, ()
  Control structures: for loop iteration; lambda expression
  Functions called: print, reduce, sum, sum_of_list
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sum_of_list(lst)
  • Function call: sum(numbers)
  • Function call: print(value)
  • Function call: sum_of_list(value)
  • Binary operation: x + y
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: sum_of_list(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: sum_of_list(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_list' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sum_of_list(...))
  4. Define function 'sum_of_list' (stored in memory, not executed yet)
  5. Execute function call: print(sum_of_list(...))
  6. Define function 'sum_of_list' (stored in memory, not executed yet)
  7. Execute function call: print(sum_of_list(...))
  8. Define function 'sum_of_list' (stored in memory, not executed yet)
  9. Define function 'sum_of_list' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(sum_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
    - 'sum_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 40:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_min_max(numbers)
      Returns a value via 'return' keyword
    def find_min_max(numbers)
      Returns a value via 'return' keyword
    def find_min_max(numbers)
      Returns a value via 'return' keyword
    def find_min_max(numbers)
      Returns a value via 'return' keyword
    def find_min_max(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, <, >, (), []
  Control structures: for loop iteration
  Functions called: find_min_max, max, min, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'min_number'
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_min_max(lst)
  • Function call: min(numbers)
  • Function call: max(numbers)
  • Function call: print(value)
  • Function call: find_min_max(value)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sorted_nums'
  • Function call: print(value)
  • Function call: find_min_max(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: find_min_max(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min_max(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min_max' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_min_max(...))
  4. Define function 'find_min_max' (stored in memory, not executed yet)
  5. Execute function call: print(find_min_max(...))
  6. Define function 'find_min_max' (stored in memory, not executed yet)
  7. Execute function call: print(find_min_max(...))
  8. Define function 'find_min_max' (stored in memory, not executed yet)
  9. Define function 'find_min_max' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(find_min_max(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 41:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_occurrences()
      Returns a value via 'return' keyword
    def count_occurrences(lst, target)
      Returns a value via 'return' keyword
    def count_occurrences(lst, target)
      Returns a value via 'return' keyword
    def count_occurrences()
      Returns a value via 'return' keyword
    def count_occurrences()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), ==
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: count_occurrences, input, print, sum
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Function call: count_occurrences()
  • Function call: lst.count(target)
  • Function call: print(value)
  • Function call: count_occurrences(value, 'Tom')
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: count_occurrences(value, 2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: count_occurrences(lst, target)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_occurrences(value, 'goal')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_occurrences' (stored in memory, not executed yet)
  2. Execute function call: count_occurrences()
  3. Define function 'count_occurrences' (stored in memory, not executed yet)
  4. Execute function call: print(count_occurrences(...))
  5. Define function 'count_occurrences' (stored in memory, not executed yet)
  6. Execute function call: print(count_occurrences(...))
  7. Define function 'count_occurrences' (stored in memory, not executed yet)
  8. Define function 'count_occurrences' (stored in memory, not executed yet)
  9. Assign value to variable: lst
  10. Assign value to variable: target
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_occurrences(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
  Runtime execution (top-level code):
    - count_occurrences() is invoked
      → Execution jumps to the function body of 'count_occurrences'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 43:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def square_elements(numbers)
      Returns a value via 'return' keyword
    def square_elements(numbers)
      Returns a value via 'return' keyword
    def square_elements(numbers)
      Returns a value via 'return' keyword
    def square_elements(numbers)
      Returns a value via 'return' keyword
    def square_elements(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., (), **
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: list, map, print, square_elements
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'squared_list'
  • Binary operation: number ** 2
  • Function call: squared_list.append(computed expression)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: square_elements(lst)
  • Binary operation: n ** 2
  • Function call: print(value)
  • Function call: square_elements(value)
  • Binary operation: x ** 2
  • Function call: list(value)
  • Function call: map(value, numbers)
  • Function call: print(value)
  • Function call: square_elements(value)
  • Binary operation: number ** 2
  • Binary operation: number ** 2
  • Function call: list(value)
  • Function call: map(value, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: square_elements(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: square_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'square_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(square_elements(...))
  4. Define function 'square_elements' (stored in memory, not executed yet)
  5. Execute function call: print(square_elements(...))
  6. Define function 'square_elements' (stored in memory, not executed yet)
  7. Execute function call: print(square_elements(...))
  8. Define function 'square_elements' (stored in memory, not executed yet)
  9. Define function 'square_elements' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(square_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'square_elements' is defined but NOT executed until called
    - 'square_elements' is defined but NOT executed until called
    - 'square_elements' is defined but NOT executed until called
    - 'square_elements' is defined but NOT executed until called
    - 'square_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 44:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def count_uppercase(string)
      Returns a value via 'return' keyword
    def count_uppercase(string)
      Returns a value via 'return' keyword
    def count_uppercase(string)
      Returns a value via 'return' keyword
    def count_uppercase(string)
      Returns a value via 'return' keyword
    def count_uppercase(string)
      Returns a value via 'return' keyword
    def count_uppercase(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), ==
  Control structures: for loop iteration; list comprehension; generator expression
  Functions called: count_uppercase, filter, len, list, print, sum
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: char.isupper()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: count_uppercase(lst)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: char.upper()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: count_uppercase(string)
  • Function call: sum(value)
  • Function call: char.isupper()
  • Function call: print(value)
  • Function call: count_uppercase('Hello World')
  • Function call: len(value)
  • Function call: list(value)
  • Function call: filter(value, string)
  • Function call: print(value)
  • Function call: count_uppercase('Python Programming')
  • Function call: sum(value)
  • Function call: char.isupper()
  • Function call: len(value)
  • Function call: char.isupper()
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_uppercase(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_uppercase('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_uppercase' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(count_uppercase(...))
  4. Define function 'count_uppercase' (stored in memory, not executed yet)
  5. Assign value to variable: string
  6. Execute function call: print(count_uppercase(...))
  7. Define function 'count_uppercase' (stored in memory, not executed yet)
  8. Execute function call: print(count_uppercase(...))
  9. Define function 'count_uppercase' (stored in memory, not executed yet)
  10. Execute function call: print(count_uppercase(...))
  11. Define function 'count_uppercase' (stored in memory, not executed yet)
  12. Define function 'count_uppercase' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(count_uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_uppercase' is defined but NOT executed until called
    - 'count_uppercase' is defined but NOT executed until called
    - 'count_uppercase' is defined but NOT executed until called
    - 'count_uppercase' is defined but NOT executed until called
    - 'count_uppercase' is defined but NOT executed until called
    - 'count_uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 45:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def remove_spaces(string)
      Returns a value via 'return' keyword
    def remove_spaces(string)
      Returns a value via 'return' keyword
    def remove_spaces(string)
      Returns a value via 'return' keyword
    def remove_spaces(string)
      Returns a value via 'return' keyword
    def remove_spaces(string)
      Returns a value via 'return' keyword
    def remove_spaces(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., !=, ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension
  Functions called: print, remove_spaces
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_spaces(lst)
  • Assignment: right side evaluated first, then stored in 'string_removed'
  • Condition branch: evaluates boolean expression first
  • Function call: string_removed.append(char)
  • Function call: ''.join(string_removed)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_spaces(lst)
  • Function call: ''.join(value)
  • Function call: string.split()
  • Function call: print(value)
  • Function call: remove_spaces('hello world')
  • Function call: ''.join(value)
  • Function call: print(value)
  • Function call: remove_spaces('a b c')
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: remove_spaces(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_spaces('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_spaces' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_spaces(...))
  4. Define function 'remove_spaces' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(remove_spaces(...))
  7. Define function 'remove_spaces' (stored in memory, not executed yet)
  8. Execute function call: print(remove_spaces(...))
  9. Define function 'remove_spaces' (stored in memory, not executed yet)
  10. Execute function call: print(remove_spaces(...))
  11. Define function 'remove_spaces' (stored in memory, not executed yet)
  12. Define function 'remove_spaces' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(remove_spaces(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
    - 'remove_spaces' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 46:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def merge_lists()
    def merge_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def merge_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def merge_lists()
      Returns a value via 'return' keyword
    def merge_lists()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., ()
  Functions called: input, list, merge_lists, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Type the first sorted list: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Type the second sorted list: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Binary operation: lst1 + lst2
  • Function call: sorted(computed expression)
  • Assignment: right side evaluated first, then stored in 'merged_list'
  • Function call: print(merged_list)
  • Function call: merge_lists()
  • Binary operation: lst1 + lst2
  • Function call: sorted(computed expression)
  • Function call: print(value)
  • Function call: merge_lists(value, value)
  • Function call: list(value)
  • Function call: heapq.merge(lst1, lst2)
  • Function call: print(value)
  • Function call: merge_lists(value, value)
  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Type the first sorted list: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Type the second sorted list: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Binary operation: lst1 + lst2
  • Function call: sorted(computed expression)
  • Assignment: right side evaluated first, then stored in 'merged_list'
  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Type the first sorted list: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Type the second sorted list: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Binary operation: lst1 + lst2
  • Function call: sorted(computed expression)
  • Assignment: right side evaluated first, then stored in 'merged_list'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: merge_lists(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: merge_lists(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'merge_lists' (stored in memory, not executed yet)
  2. Execute function call: merge_lists()
  3. Define function 'merge_lists' (stored in memory, not executed yet)
  4. Execute function call: print(merge_lists(...))
  5. Define function 'merge_lists' (stored in memory, not executed yet)
  6. Execute function call: print(merge_lists(...))
  7. Define function 'merge_lists' (stored in memory, not executed yet)
  8. Define function 'merge_lists' (stored in memory, not executed yet)
  9. Assign value to variable: lst1
  10. Assign value to variable: lst2
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(merge_lists(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'merge_lists' is defined but NOT executed until called
    - 'merge_lists' is defined but NOT executed until called
    - 'merge_lists' is defined but NOT executed until called
    - 'merge_lists' is defined but NOT executed until called
    - 'merge_lists' is defined but NOT executed until called
  Runtime execution (top-level code):
    - merge_lists() is invoked
      → Execution jumps to the function body of 'merge_lists'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 47:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_index(numbers, target)
      Returns a value via 'return' keyword
    def find_index(numbers, target)
      Returns a value via 'return' keyword
    def find_index(numbers, target)
      Returns a value via 'return' keyword
    def find_index(numbers, target)
      Returns a value via 'return' keyword
    def find_index(numbers, target)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, try/except
  Operators used: -, ., (), ==, []
  Control structures: if/else conditional branching; for loop iteration; try/except error handling; list comprehension
  Functions called: enumerate, find_index, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: print('Index of', target, 'in nums:', value)
  • Function call: find_index(numbers, target)
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: print('Index of', target, 'in nums:', value)
  • Function call: find_index(numbers, target)
  • Function call: numbers.index(target)
  • Function call: print(value)
  • Function call: find_index(value, 30)
  • Function call: print(value)
  • Function call: find_index(value, 50)
  • Function call: enumerate(numbers)
  • Assignment: right side evaluated first, then stored in 'indices'
  • Function call: print(value)
  • Function call: find_index(value, 15)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: find_index(numbers, target)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_index(value, 'goal')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_index' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Assign value to variable: target
  4. Execute function call: print('Index of', target, 'in nums:', find_index(...))
  5. Assign value to variable: target
  6. Execute function call: print('Index of', target, 'in nums:', find_index(...))
  7. Define function 'find_index' (stored in memory, not executed yet)
  8. Execute function call: print(find_index(...))
  9. Execute function call: print(find_index(...))
  10. Define function 'find_index' (stored in memory, not executed yet)
  11. Execute function call: print(find_index(...))
  12. Define function 'find_index' (stored in memory, not executed yet)
  13. Define function 'find_index' (stored in memory, not executed yet)
  14. Assign value to variable: numbers
  15. Assign value to variable: target
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(find_index(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_index' is defined but NOT executed until called
    - 'find_index' is defined but NOT executed until called
    - 'find_index' is defined but NOT executed until called
    - 'find_index' is defined but NOT executed until called
    - 'find_index' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 48:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_even_indices(nums)
      Returns a value via 'return' keyword
    def sum_even_indices(nums)
      Returns a value via 'return' keyword
    def sum_even_indices(nums)
      Returns a value via 'return' keyword
    def sum_even_indices(nums)
      Returns a value via 'return' keyword
    def sum_even_indices(nums)
      Returns a value via 'return' keyword
    def sum_even_indices(nums)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, (), ==, []
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: enumerate, len, print, range, sum, sum_even_indices
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: range(value)
  • Function call: len(nums)
  • Binary operation: i % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print('Sum of elements at even indices:', value)
  • Function call: sum_even_indices(numbers)
  • Binary operation: i % 2
  • Function call: sum(value)
  • Function call: range(value)
  • Function call: len(nums)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print('Sum of elements at even indices:', value)
  • Function call: sum_even_indices(numbers)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: sum_even_indices(value)
  • Binary operation: i % 2
  • Function call: sum(value)
  • Function call: enumerate(nums)
  • Function call: print(value)
  • Function call: sum_even_indices(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: sum_even_indices(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_even_indices(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_even_indices' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print('Sum of elements at even indices:', sum_even_indices(...))
  4. Define function 'sum_even_indices' (stored in memory, not executed yet)
  5. Assign value to variable: numbers
  6. Execute function call: print('Sum of elements at even indices:', sum_even_indices(...))
  7. Define function 'sum_even_indices' (stored in memory, not executed yet)
  8. Execute function call: print(sum_even_indices(...))
  9. Define function 'sum_even_indices' (stored in memory, not executed yet)
  10. Execute function call: print(sum_even_indices(...))
  11. Define function 'sum_even_indices' (stored in memory, not executed yet)
  12. Define function 'sum_even_indices' (stored in memory, not executed yet)
  13. Assign value to variable: nums
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(sum_even_indices(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_even_indices' is defined but NOT executed until called
    - 'sum_even_indices' is defined but NOT executed until called
    - 'sum_even_indices' is defined but NOT executed until called
    - 'sum_even_indices' is defined but NOT executed until called
    - 'sum_even_indices' is defined but NOT executed until called
    - 'sum_even_indices' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 49:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(strings)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), [], and, not, not in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: list, map, print, remove_duplicates, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique'
  • Assignment: right side evaluated first, then stored in 'common'
  • Function call: word.lower()
  • Assignment: right side evaluated first, then stored in 'word_lower'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word_lower)
  • Function call: unique.add(word_lower)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_duplicates(lst)
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique'
  • Function call: word.lower()
  • Assignment: right side evaluated first, then stored in 'word_lower'
  • Condition branch: evaluates boolean expression first
  • Function call: unique.add(word_lower)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_duplicates(lst)
  • Assignment: right side evaluated first, then stored in 'unique_strings'
  • Function call: string.lower()
  • Function call: map(value, unique_strings)
  • Condition branch: evaluates boolean expression first
  • Function call: unique_strings.append(string)
  • Assignment: right side evaluated first, then stored in 'input_strings'
  • Function call: print('List with case-insensitive duplicates removed:', value)
  • Function call: remove_duplicates(input_strings)
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: word.lower()
  • Assignment: right side evaluated first, then stored in 'lower_word'
  • Condition branch: evaluates boolean expression first
  • Function call: list(value)
  • Function call: seen.values()
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: x.lower()
  • Function call: seen.add(value)
  • Function call: x.lower()
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: remove_duplicates(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_duplicates(...))
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(remove_duplicates(...))
  7. Define function 'remove_duplicates' (stored in memory, not executed yet)
  8. Assign value to variable: input_strings
  9. Execute function call: print('List with case-insensitive duplicates removed:', remove_duplicates(...))
  10. Define function 'remove_duplicates' (stored in memory, not executed yet)
  11. Execute function call: print(remove_duplicates(...))
  12. Define function 'remove_duplicates' (stored in memory, not executed yet)
  13. Execute function call: print(remove_duplicates(...))
  14. Define function 'remove_duplicates' (stored in memory, not executed yet)
  15. Define function 'remove_duplicates' (stored in memory, not executed yet)
  16. Assign value to variable: lst
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(remove_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over strings, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 50:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: -, ., <, !=, (), ==, []
  Control structures: if/else conditional branching; while loop
  Functions called: is_palindrome, len, print, reversed
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print('Is', input_string, 'a palindrome?', value)
  • Function call: is_palindrome(input_string)
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print('Is', input_string, 'a palindrome?', value)
  • Function call: is_palindrome(input_string)
  • Binary operation: len(...) - 1
  • Function call: len(s)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_palindrome('madam')
  • Function call: print(value)
  • Function call: is_palindrome('python')
  • Function call: ''.join(value)
  • Function call: reversed(s)
  • Function call: print(value)
  • Function call: is_palindrome('level')
  • Function call: print(value)
  • Function call: is_palindrome('world')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_palindrome(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Assign value to variable: input_string
  3. Execute function call: print('Is', input_string, 'a palindrome?', is_palindrome(...))
  4. Assign value to variable: input_string
  5. Execute function call: print('Is', input_string, 'a palindrome?', is_palindrome(...))
  6. Define function 'is_palindrome' (stored in memory, not executed yet)
  7. Execute function call: print(is_palindrome(...))
  8. Execute function call: print(is_palindrome(...))
  9. Define function 'is_palindrome' (stored in memory, not executed yet)
  10. Execute function call: print(is_palindrome(...))
  11. Execute function call: print(is_palindrome(...))
  12. Define function 'is_palindrome' (stored in memory, not executed yet)
  13. Define function 'is_palindrome' (stored in memory, not executed yet)
  14. Assign value to variable: s
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 51:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: *, +, -, ., (), ==
  Control structures: for loop iteration; lambda expression
  Functions called: factorial, print, range, reduce
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: print(value)
  • Function call: factorial(0)
  • Condition branch: evaluates boolean expression first
  • Binary operation: n * factorial(...)
  • Binary operation: n - 1
  • Function call: factorial(computed expression)
  • Function call: print(value)
  • Function call: factorial(5)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(n, 0, value)
  • Function call: print(value)
  • Function call: factorial(5)
  • Function call: math.factorial(n)
  • Function call: print(value)
  • Function call: factorial(6)
  • Binary operation: x * y
  • Binary operation: n + 1
  • Function call: reduce(value, value, 1)
  • Function call: range(1, computed expression)
  • Function call: print(value)
  • Function call: factorial(4)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: factorial(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: factorial(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Execute function call: print(factorial(...))
  3. Define function 'factorial' (stored in memory, not executed yet)
  4. Execute function call: print(factorial(...))
  5. Define function 'factorial' (stored in memory, not executed yet)
  6. Execute function call: print(factorial(...))
  7. Define function 'factorial' (stored in memory, not executed yet)
  8. Execute function call: print(factorial(...))
  9. Define function 'factorial' (stored in memory, not executed yet)
  10. Execute function call: print(factorial(...))
  11. Define function 'factorial' (stored in memory, not executed yet)
  12. Define function 'factorial' (stored in memory, not executed yet)
  13. Assign value to variable: n
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 52:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def main()
      Returns a value via 'return' keyword
    def is_anagram(str1, str2)
      Returns a value via 'return' keyword
    def is_anagram(lst1, lst2)
      Returns a value via 'return' keyword
    def is_anagram(str1, str2)
      Returns a value via 'return' keyword
    def is_anagram(str1, str2)
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., !=, (), ==
  Control structures: for loop iteration
  Functions called: Counter, input, is_anagram, len, main, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).lower()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: input(...).lower()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: sorted(string1)
  • Assignment: right side evaluated first, then stored in 'sorted1'
  • Function call: sorted(string2)
  • Assignment: right side evaluated first, then stored in 'sorted2'
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Function call: str1.lower()
  • Assignment: right side evaluated first, then stored in 'str1'
  • Function call: str2.lower()
  • Assignment: right side evaluated first, then stored in 'str2'
  • Function call: sorted(str1)
  • Function call: sorted(str2)
  • Function call: print(value)
  • Function call: is_anagram('listen', 'silent')
  • Function call: print(value)
  • Function call: is_anagram('hello', 'world')
  • Function call: print(value)
  • Function call: is_anagram('Triangle', 'Integral')
  • Function call: sorted(lst1)
  • Assignment: right side evaluated first, then stored in 'sorted_lst1'
  • Function call: sorted(lst2)
  • Assignment: right side evaluated first, then stored in 'sorted_lst2'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: is_anagram(lst1, lst2)
  • Function call: Counter(value)
  • Function call: Counter(value)
  • Function call: str1.lower()
  • Function call: str2.lower()
  • Function call: print(value)
  • Function call: is_anagram('listen', 'silent')
  • Function call: str1.lower()
  • Function call: str2.lower()
  • Function call: len(str1)
  • Function call: len(str2)
  • Condition branch: evaluates boolean expression first
  • Function call: str1.count(char)
  • Function call: str2.count(char)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_anagram('hello', 'olleh')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'str1'
  • Assignment: right side evaluated first, then stored in 'str2'
  • Function call: is_anagram(str1, str2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_anagram('world', 'python')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Execute function call: main()
  3. Define function 'is_anagram' (stored in memory, not executed yet)
  4. Execute function call: print(is_anagram(...))
  5. Execute function call: print(is_anagram(...))
  6. Execute function call: print(is_anagram(...))
  7. Define function 'is_anagram' (stored in memory, not executed yet)
  8. Assign value to variable: lst1
  9. Assign value to variable: lst2
  10. Execute function call: print(is_anagram(...))
  11. Define function 'is_anagram' (stored in memory, not executed yet)
  12. Execute function call: print(is_anagram(...))
  13. Define function 'is_anagram' (stored in memory, not executed yet)
  14. Execute function call: print(is_anagram(...))
  15. Define function 'main' (stored in memory, not executed yet)
  16. Define function 'main' (stored in memory, not executed yet)
  17. Assign value to variable: str1
  18. Assign value to variable: str2
  19. Assign value to variable: result
  20. Execute function call: print(result)
  21. Execute function call: print(is_anagram(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over str1, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 53:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_words(s)
      Returns a value via 'return' keyword
    def count_words(s)
      Returns a value via 'return' keyword
    def count_words(s)
      Returns a value via 'return' keyword
    def count_words(s)
      Returns a value via 'return' keyword
    def count_words(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: Counter, count_words, dict, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.lower()
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: s.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'word_count'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print(value)
  • Function call: count_words(input_string)
  • Function call: dict(value)
  • Function call: Counter(value)
  • Function call: s.lower(...).split()
  • Function call: s.lower()
  • Function call: print(value)
  • Function call: count_words('hello world hello')
  • Assignment: right side evaluated first, then stored in 'word_count'
  • Function call: s.lower(...).split()
  • Function call: s.lower()
  • Binary operation: word_count.get(...) + 1
  • Function call: word_count.get(word, 0)
  • Function call: print(value)
  • Function call: count_words('one two one')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: count_words(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_words('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_words' (stored in memory, not executed yet)
  2. Assign value to variable: input_string
  3. Execute function call: print(count_words(...))
  4. Define function 'count_words' (stored in memory, not executed yet)
  5. Execute function call: print(count_words(...))
  6. Define function 'count_words' (stored in memory, not executed yet)
  7. Execute function call: print(count_words(...))
  8. Define function 'count_words' (stored in memory, not executed yet)
  9. Define function 'count_words' (stored in memory, not executed yet)
  10. Assign value to variable: s
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over s.lower(...).split(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 54:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, yield
  Operators used: +, -, ., (), <=, ==, []
  Control structures: for loop iteration
  Functions called: fibonacci, list, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'fib_sequence'
  • Function call: range(2, n)
  • Binary operation: expression + expression
  • Assignment: right side evaluated first, then stored in 'next_number'
  • Function call: fib_sequence.append(next_number)
  • Function call: print(value)
  • Function call: fibonacci(10)
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Binary operation: n - 1
  • Function call: fibonacci(computed expression)
  • Assignment: right side evaluated first, then stored in 'seq'
  • Binary operation: expression + expression
  • Function call: seq.append(computed expression)
  • Function call: print(value)
  • Function call: fibonacci(7)
  • Function call: range(n)
  • Binary operation: a + b
  • Function call: print(value)
  • Function call: list(value)
  • Function call: fibonacci(8)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: fibonacci(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fibonacci(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fibonacci' (stored in memory, not executed yet)
  2. Execute function call: print(fibonacci(...))
  3. Define function 'fibonacci' (stored in memory, not executed yet)
  4. Execute function call: print(fibonacci(...))
  5. Define function 'fibonacci' (stored in memory, not executed yet)
  6. Execute function call: print(list(...))
  7. Define function 'fibonacci' (stored in memory, not executed yet)
  8. Define function 'fibonacci' (stored in memory, not executed yet)
  9. Assign value to variable: n
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(fibonacci(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 55:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def area_rectangle()
    def area_rectangle()
      Returns a value via 'return' keyword
    def area_rectangle()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ()
  Functions called: area_rectangle, float, input, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: float(value)
  • Function call: input('Enter length: ')
  • Assignment: right side evaluated first, then stored in 'length'
  • Function call: float(value)
  • Function call: input('Enter width: ')
  • Assignment: right side evaluated first, then stored in 'width'
  • Binary operation: length * width
  • Function call: print(computed expression)
  • Function call: area_rectangle()
  • Function call: float(value)
  • Function call: input('Enter length: ')
  • Assignment: right side evaluated first, then stored in 'length'
  • Function call: float(value)
  • Function call: input('Enter width: ')
  • Assignment: right side evaluated first, then stored in 'width'
  • Binary operation: length * width
  • Function call: float(value)
  • Function call: input('Enter length: ')
  • Assignment: right side evaluated first, then stored in 'length'
  • Function call: float(value)
  • Function call: input('Enter width: ')
  • Assignment: right side evaluated first, then stored in 'width'
  • Binary operation: length * width
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'length'
  • Assignment: right side evaluated first, then stored in 'width'
  • Binary operation: length * width
  • Function call: print(computed expression)
  • Assignment: right side evaluated first, then stored in 'length'
  • Assignment: right side evaluated first, then stored in 'width'
  • Binary operation: length * width
  • Function call: print(computed expression)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'area_rectangle' (stored in memory, not executed yet)
  2. Execute function call: area_rectangle()
  3. Define function 'area_rectangle' (stored in memory, not executed yet)
  4. Define function 'area_rectangle' (stored in memory, not executed yet)
  5. Assign value to variable: length
  6. Assign value to variable: width
  7. Execute function call: print(<expr>)
  8. Assign value to variable: length
  9. Assign value to variable: width
  10. Execute function call: print(<expr>)

▸ EXECUTION FLOW

  Function definition phase:
    - 'area_rectangle' is defined but NOT executed until called
    - 'area_rectangle' is defined but NOT executed until called
    - 'area_rectangle' is defined but NOT executed until called
  Runtime execution (top-level code):
    - area_rectangle() is invoked
      → Execution jumps to the function body of 'area_rectangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 56:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def even_odd()
    def even_odd(n)
      Returns a value via 'return' keyword
    def even_odd(n)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: %, &, (), ==
  Control structures: if/else conditional branching
  Functions called: even_odd, input, int, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Binary operation: user % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print('Number is even')
  • Function call: print('Number is odd')
  • Function call: even_odd()
  • Binary operation: n % 2
  • Function call: print(value)
  • Function call: even_odd(10)
  • Function call: print(value)
  • Function call: even_odd(7)
  • Binary operation: n & 1
  • Function call: print(value)
  • Function call: even_odd(8)
  • Function call: print(value)
  • Function call: even_odd(11)
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: even_odd(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: even_odd(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'even_odd' (stored in memory, not executed yet)
  2. Execute function call: even_odd()
  3. Define function 'even_odd' (stored in memory, not executed yet)
  4. Execute function call: print(even_odd(...))
  5. Execute function call: print(even_odd(...))
  6. Define function 'even_odd' (stored in memory, not executed yet)
  7. Execute function call: print(even_odd(...))
  8. Execute function call: print(even_odd(...))
  9. Assign value to variable: n
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(even_odd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'even_odd' is defined but NOT executed until called
    - 'even_odd' is defined but NOT executed until called
    - 'even_odd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - even_odd() is invoked
      → Execution jumps to the function body of 'even_odd'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 57:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def fibonacci_series()
    def fibonacci_series(limit)
      Returns a value via 'return' keyword
    def fibonacci_series(n)
      Returns a value via 'return' keyword
  Keywords used: def, return, while
  Operators used: +, -, ., (), <=, []
  Control structures: while loop; list comprehension
  Functions called: fibonacci_series, input, int, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('Enter the limit: ')
  • Assignment: right side evaluated first, then stored in 'limit'
  • Function call: print(a)
  • Binary operation: a + b
  • Function call: fibonacci_series()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(a)
  • Binary operation: a + b
  • Function call: print(value)
  • Function call: fibonacci_series(50)
  • Assignment: right side evaluated first, then stored in 'fib'
  • Binary operation: expression + expression
  • Function call: fib.append(computed expression)
  • Function call: range(2, n)
  • Function call: print(value)
  • Function call: fibonacci_series(10)
  • Assignment: right side evaluated first, then stored in 'limit'
  • Function call: fibonacci_series(limit)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fibonacci_series(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fibonacci_series' (stored in memory, not executed yet)
  2. Execute function call: fibonacci_series()
  3. Define function 'fibonacci_series' (stored in memory, not executed yet)
  4. Execute function call: print(fibonacci_series(...))
  5. Define function 'fibonacci_series' (stored in memory, not executed yet)
  6. Execute function call: print(fibonacci_series(...))
  7. Assign value to variable: limit
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(fibonacci_series(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fibonacci_series' is defined but NOT executed until called
    - 'fibonacci_series' is defined but NOT executed until called
    - 'fibonacci_series' is defined but NOT executed until called
  Runtime execution (top-level code):
    - fibonacci_series() is invoked
      → Execution jumps to the function body of 'fibonacci_series'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
"""

"""
Problem 58:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def max_number()
    def max_number(a, b, c)
      Returns a value via 'return' keyword
    def max_number(a, b, c)
      Returns a value via 'return' keyword
    def max_number(numbers)
      Returns a value via 'return' keyword
    def max_number()
      Returns a value via 'return' keyword
    def max_number()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., >, (), >=, [], and
  Control structures: for loop iteration
  Functions called: input, int, max, max_number, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type 3 numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Function call: print(max_num)
  • Function call: max_number()
  • Function call: max(a, b, c)
  • Function call: print(value)
  • Function call: max_number(10, 25, 15)
  • Function call: print(value)
  • Function call: max_number(100, 50, 75)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: max_number(value)
  • Function call: input(...).split()
  • Function call: input('type 3 numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Function call: input(...).split()
  • Function call: input('type 3 numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Assignment: right side evaluated first, then stored in 'c'
  • Function call: max_number(a, b, c)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_number(20, 30, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_number' (stored in memory, not executed yet)
  2. Execute function call: max_number()
  3. Define function 'max_number' (stored in memory, not executed yet)
  4. Execute function call: print(max_number(...))
  5. Define function 'max_number' (stored in memory, not executed yet)
  6. Execute function call: print(max_number(...))
  7. Define function 'max_number' (stored in memory, not executed yet)
  8. Execute function call: print(max_number(...))
  9. Define function 'max_number' (stored in memory, not executed yet)
  10. Define function 'max_number' (stored in memory, not executed yet)
  11. Assign value to variable: a
  12. Assign value to variable: b
  13. Assign value to variable: c
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(max_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_number' is defined but NOT executed until called
    - 'max_number' is defined but NOT executed until called
    - 'max_number' is defined but NOT executed until called
    - 'max_number' is defined but NOT executed until called
    - 'max_number' is defined but NOT executed until called
    - 'max_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - max_number() is invoked
      → Execution jumps to the function body of 'max_number'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 59:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def reverse_string()
    def reverse_string(string)
      Returns a value via 'return' keyword
    def reverse_string(s)
      Returns a value via 'return' keyword
    def reverse_string(s)
      Returns a value via 'return' keyword
    def reverse_string()
      Returns a value via 'return' keyword
    def reverse_string()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, -, ., (), []
  Control structures: for loop iteration
  Functions called: input, print, reverse_string, reversed
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: print(user)
  • Function call: reverse_string()
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse_string(lst)
  • Function call: ''.join(value)
  • Function call: reversed(s)
  • Function call: print(value)
  • Function call: reverse_string('python')
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: reverse_string('hello')
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: reverse_string(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_string('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_string' (stored in memory, not executed yet)
  2. Execute function call: reverse_string()
  3. Define function 'reverse_string' (stored in memory, not executed yet)
  4. Assign value to variable: lst
  5. Execute function call: print(reverse_string(...))
  6. Define function 'reverse_string' (stored in memory, not executed yet)
  7. Execute function call: print(reverse_string(...))
  8. Define function 'reverse_string' (stored in memory, not executed yet)
  9. Execute function call: print(reverse_string(...))
  10. Define function 'reverse_string' (stored in memory, not executed yet)
  11. Define function 'reverse_string' (stored in memory, not executed yet)
  12. Assign value to variable: string
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(reverse_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - reverse_string() is invoked
      → Execution jumps to the function body of 'reverse_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 60:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_palindrome()
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome()
      Returns a value via 'return' keyword
    def is_palindrome()
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: +, -, ., <, !=, (), //, ==, []
  Control structures: if/else conditional branching; while loop; generator expression
  Functions called: all, input, is_palindrome, len, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).lower(...).replace(' ', '')
  • Function call: input(...).lower()
  • Function call: input('type word: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: print(value)
  • Function call: is_palindrome()
  • Function call: s.lower(...).replace(' ', '')
  • Function call: s.lower()
  • Assignment: right side evaluated first, then stored in 's'
  • Binary operation: len(...) - 1
  • Function call: len(s)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_palindrome('racecar')
  • Function call: print(value)
  • Function call: is_palindrome('hello')
  • Function call: s.lower(...).replace(' ', '')
  • Function call: s.lower()
  • Assignment: right side evaluated first, then stored in 's'
  • Binary operation: len(...) // 2
  • Binary operation: i + 1
  • Function call: all(value)
  • Function call: range(computed expression)
  • Function call: len(s)
  • Function call: print(value)
  • Function call: is_palindrome('madam')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_palindrome(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Execute function call: print(is_palindrome(...))
  3. Define function 'is_palindrome' (stored in memory, not executed yet)
  4. Execute function call: print(is_palindrome(...))
  5. Execute function call: print(is_palindrome(...))
  6. Define function 'is_palindrome' (stored in memory, not executed yet)
  7. Execute function call: print(is_palindrome(...))
  8. Define function 'is_palindrome' (stored in memory, not executed yet)
  9. Define function 'is_palindrome' (stored in memory, not executed yet)
  10. Assign value to variable: s
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 61:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_vowels()
    def count_vowels(s)
      Returns a value via 'return' keyword
    def count_vowels(s)
      Returns a value via 'return' keyword
    def count_vowels()
      Returns a value via 'return' keyword
    def count_vowels()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in
  Control structures: for loop iteration; list comprehension; generator expression
  Functions called: count_vowels, input, len, print, sum
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).lower()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Function call: print(count)
  • Function call: count_vowels()
  • Function call: len(value)
  • Function call: s.lower()
  • Function call: print(value)
  • Function call: count_vowels('Python')
  • Function call: print(value)
  • Function call: count_vowels('Hello World')
  • Function call: sum(value)
  • Function call: s.lower()
  • Function call: print(value)
  • Function call: count_vowels('Programming')
  • Function call: input(...).lower()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Function call: input(...).lower()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: count_vowels(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Execute function call: count_vowels()
  3. Define function 'count_vowels' (stored in memory, not executed yet)
  4. Execute function call: print(count_vowels(...))
  5. Execute function call: print(count_vowels(...))
  6. Define function 'count_vowels' (stored in memory, not executed yet)
  7. Execute function call: print(count_vowels(...))
  8. Define function 'count_vowels' (stored in memory, not executed yet)
  9. Define function 'count_vowels' (stored in memory, not executed yet)
  10. Assign value to variable: s
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - count_vowels() is invoked
      → Execution jumps to the function body of 'count_vowels'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 62:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def factorial()
    def factorial()
    def factorial()
      Returns a value via 'return' keyword
    def factorial()
    def factorial()
      Returns a value via 'return' keyword
    def factorial()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: *, +, -, ., (), <=
  Control structures: if/else conditional branching; for loop iteration
  Functions called: fact, factorial, input, int, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(1, 6)
  • Function call: print(value)
  • Function call: factorial()
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: number + 1
  • Function call: range(1, computed expression)
  • Function call: print(value)
  • Function call: factorial()
  • Condition branch: evaluates boolean expression first
  • Binary operation: n * fact(...)
  • Binary operation: n - 1
  • Function call: fact(computed expression)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: fact(number)
  • Function call: factorial()
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: math.factorial(number)
  • Function call: factorial()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(1, 6)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(1, 6)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(1, 6)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(1, 6)
  • Function call: print(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Execute function call: factorial()
  3. Define function 'factorial' (stored in memory, not executed yet)
  4. Execute function call: factorial()
  5. Define function 'factorial' (stored in memory, not executed yet)
  6. Execute function call: factorial()
  7. Define function 'factorial' (stored in memory, not executed yet)
  8. Execute function call: factorial()
  9. Define function 'factorial' (stored in memory, not executed yet)
  10. Define function 'factorial' (stored in memory, not executed yet)
  11. Assign value to variable: result
  12. For loop: iterate over range(...)
  13. Execute function call: print(value)
  14. Assign value to variable: result
  15. For loop: iterate over range(...)
  16. Execute function call: print(value)

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - factorial() is invoked
      → Execution jumps to the function body of 'factorial'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - factorial() is invoked
      → Execution jumps to the function body of 'factorial'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - factorial() is invoked
      → Execution jumps to the function body of 'factorial'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - factorial() is invoked
      → Execution jumps to the function body of 'factorial'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 63:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sum_of_all_numbers()
    def sum_of_all_numbers()
      Returns a value via 'return' keyword
    def sum_of_all_numbers()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ()
  Control structures: for loop iteration
  Functions called: input, int, print, range, sum_of_all_numbers
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'n'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: print(total)
  • Function call: sum_of_all_numbers()
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'n'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'n'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: print(total)
  • Assignment: right side evaluated first, then stored in 'n'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: print(total)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  2. Execute function call: sum_of_all_numbers()
  3. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  4. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  5. Assign value to variable: n
  6. Assign value to variable: total
  7. For loop: iterate over range(...)
  8. Execute function call: print(total)
  9. Assign value to variable: n
  10. Assign value to variable: total
  11. For loop: iterate over range(...)
  12. Execute function call: print(total)

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_all_numbers' is defined but NOT executed until called
    - 'sum_of_all_numbers' is defined but NOT executed until called
    - 'sum_of_all_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - sum_of_all_numbers() is invoked
      → Execution jumps to the function body of 'sum_of_all_numbers'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 64:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def find_largest()
    def find_largest()
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def find_largest()
      Returns a value via 'return' keyword
    def find_largest()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., >, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: find_largest, input, int, largest_element, max, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(largest)
  • Function call: find_largest()
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(largest)
  • Function call: find_largest()
  • Function call: max(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: largest_element(numbers)
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: largest_element(numbers)
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: largest_element(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_largest' (stored in memory, not executed yet)
  2. Execute function call: find_largest()
  3. Define function 'find_largest' (stored in memory, not executed yet)
  4. Execute function call: find_largest()
  5. Define function 'largest_element' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Execute function call: print(largest_element(...))
  8. Define function 'largest_element' (stored in memory, not executed yet)
  9. Assign value to variable: numbers
  10. Execute function call: print(largest_element(...))
  11. Define function 'find_largest' (stored in memory, not executed yet)
  12. Define function 'find_largest' (stored in memory, not executed yet)
  13. Assign value to variable: numbers
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(largest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_largest' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - find_largest() is invoked
      → Execution jumps to the function body of 'find_largest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - find_largest() is invoked
      → Execution jumps to the function body of 'find_largest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 65:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def main(lst)
      Returns a value via 'return' keyword
    def largest_elememt(lst)
      Returns a value via 'return' keyword
    def largest_elememt(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, >, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: largest_elememt, len, main, max, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: largest_elememt(lst)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: largest_elememt(lst)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: main(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: main(lst)
  4. Define function 'largest_elememt' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(largest_elememt(...))
  7. Define function 'largest_elememt' (stored in memory, not executed yet)
  8. Assign value to variable: lst
  9. Execute function call: print(largest_elememt(...))
  10. Define function 'main' (stored in memory, not executed yet)
  11. Define function 'main' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(main(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'largest_elememt' is defined but NOT executed until called
    - 'largest_elememt' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 66:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def remove_duplicates(elements)
      Returns a value via 'return' keyword
    def remove_duplicates(elements)
      Returns a value via 'return' keyword
    def remove_duplicates(elements)
      Returns a value via 'return' keyword
    def remove_duplicates(elements)
      Returns a value via 'return' keyword
    def remove_duplicates(elements)
      Returns a value via 'return' keyword
    def remove_duplicates(elements)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), in, not in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: list, map, print, remove_duplicates, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'duplicates'
  • Condition branch: evaluates boolean expression first
  • Function call: duplicates.append(element)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_duplicates(lst)
  • Function call: list(value)
  • Function call: set(elements)
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: seen.add(item)
  • Function call: result.append(item)
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: list(value)
  • Function call: dict.fromkeys(elements)
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: list(value)
  • Function call: map(value, elements)
  • Assignment: right side evaluated first, then stored in 'elements'
  • Function call: remove_duplicates(elements)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_duplicates(...))
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Execute function call: print(remove_duplicates(...))
  6. Define function 'remove_duplicates' (stored in memory, not executed yet)
  7. Execute function call: print(remove_duplicates(...))
  8. Define function 'remove_duplicates' (stored in memory, not executed yet)
  9. Execute function call: print(remove_duplicates(...))
  10. Define function 'remove_duplicates' (stored in memory, not executed yet)
  11. Define function 'remove_duplicates' (stored in memory, not executed yet)
  12. Assign value to variable: elements
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(remove_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 67:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_leap_year()
      Returns a value via 'return' keyword
    def is_leap_year(year)
      Returns a value via 'return' keyword
    def is_leap_year(year)
      Returns a value via 'return' keyword
    def is_leap_year()
      Returns a value via 'return' keyword
    def is_leap_year()
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: %, !=, (), ==, or, and
  Functions called: input, int, is_leap_year, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('Enter a year: ')
  • Assignment: right side evaluated first, then stored in 'year'
  • Binary operation: year % 400
  • Binary operation: year % 4
  • Binary operation: year % 100
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_leap_year()
  • Binary operation: year % 4
  • Binary operation: year % 100
  • Binary operation: year % 400
  • Function call: print(value)
  • Function call: is_leap_year(2000)
  • Function call: print(value)
  • Function call: is_leap_year(1900)
  • Function call: print(value)
  • Function call: is_leap_year(2024)
  • Binary operation: year % 400
  • Condition branch: evaluates boolean expression first
  • Binary operation: year % 100
  • Condition branch: evaluates boolean expression first
  • Binary operation: year % 4
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_leap_year(2020)
  • Function call: print(value)
  • Function call: is_leap_year(2100)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'year'
  • Function call: is_leap_year(year)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_leap_year(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_leap_year' (stored in memory, not executed yet)
  2. Execute function call: print(is_leap_year(...))
  3. Define function 'is_leap_year' (stored in memory, not executed yet)
  4. Execute function call: print(is_leap_year(...))
  5. Execute function call: print(is_leap_year(...))
  6. Execute function call: print(is_leap_year(...))
  7. Define function 'is_leap_year' (stored in memory, not executed yet)
  8. Execute function call: print(is_leap_year(...))
  9. Execute function call: print(is_leap_year(...))
  10. Define function 'is_leap_year' (stored in memory, not executed yet)
  11. Define function 'is_leap_year' (stored in memory, not executed yet)
  12. Assign value to variable: year
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(is_leap_year(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_leap_year' is defined but NOT executed until called
    - 'is_leap_year' is defined but NOT executed until called
    - 'is_leap_year' is defined but NOT executed until called
    - 'is_leap_year' is defined but NOT executed until called
    - 'is_leap_year' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 68:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def length_string()
    def length_string()
      Returns a value via 'return' keyword
    def length_string()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: input, length_string, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'text'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: print(count)
  • Function call: length_string()
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'text'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'text'
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'text'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: print(count)
  • Assignment: right side evaluated first, then stored in 'text'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: print(count)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'length_string' (stored in memory, not executed yet)
  2. Execute function call: length_string()
  3. Define function 'length_string' (stored in memory, not executed yet)
  4. Define function 'length_string' (stored in memory, not executed yet)
  5. Assign value to variable: text
  6. Assign value to variable: count
  7. For loop: iterate over text
  8. Execute function call: print(count)
  9. Assign value to variable: text
  10. Assign value to variable: count
  11. For loop: iterate over text
  12. Execute function call: print(count)

▸ EXECUTION FLOW

  Function definition phase:
    - 'length_string' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - length_string() is invoked
      → Execution jumps to the function body of 'length_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over text, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over text, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over text, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over text, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over text, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 70:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_each_word(sentence)
      Returns a value via 'return' keyword
    def count_each_word(sentence)
      Returns a value via 'return' keyword
    def count_each_word(sentence)
      Returns a value via 'return' keyword
    def count_each_word(sentence)
      Returns a value via 'return' keyword
    def count_each_word(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ., (), []
  Control structures: for loop iteration
  Functions called: Counter, count_each_word, dict, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'split_word'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: count_each_word(lst)
  • Assignment: right side evaluated first, then stored in 'word_count'
  • Function call: sentence.lower(...).split()
  • Function call: sentence.lower()
  • Binary operation: word_count.get(...) + 1
  • Function call: word_count.get(word, 0)
  • Function call: print(value)
  • Function call: count_each_word('the cat and the dog')
  • Function call: dict(value)
  • Function call: Counter(value)
  • Function call: sentence.lower(...).split()
  • Function call: sentence.lower()
  • Function call: print(value)
  • Function call: count_each_word('one two one two one')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: count_each_word(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_each_word('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_each_word' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(count_each_word(...))
  4. Define function 'count_each_word' (stored in memory, not executed yet)
  5. Execute function call: print(count_each_word(...))
  6. Define function 'count_each_word' (stored in memory, not executed yet)
  7. Execute function call: print(count_each_word(...))
  8. Define function 'count_each_word' (stored in memory, not executed yet)
  9. Define function 'count_each_word' (stored in memory, not executed yet)
  10. Assign value to variable: sentence
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_each_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_each_word' is defined but NOT executed until called
    - 'count_each_word' is defined but NOT executed until called
    - 'count_each_word' is defined but NOT executed until called
    - 'count_each_word' is defined but NOT executed until called
    - 'count_each_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over split_word, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sentence.lower(...).split(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 71:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def second_largest_number(numbers)
      Returns a value via 'return' keyword
    def sec_lar()
    def second_largest_number(numbers)
      Returns a value via 'return' keyword
    def second_largest_number(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: ., <, (), []
  Control structures: if/else conditional branching
  Functions called: input, len, list, map, print, second_largest_number, set, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(value)
  • Function call: set(numbers)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: len(unique_numbers)
  • Condition branch: evaluates boolean expression first
  • Function call: list(value)
  • Function call: map(int, value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: second_largest_number(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: second_largest_number(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: second_largest_number(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'second_largest_number' (stored in memory, not executed yet)
  2. Define function 'sec_lar' (stored in memory, not executed yet)
  3. Define function 'second_largest_number' (stored in memory, not executed yet)
  4. Define function 'second_largest_number' (stored in memory, not executed yet)
  5. Assign value to variable: numbers
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Execute function call: print(second_largest_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'second_largest_number' is defined but NOT executed until called
    - 'sec_lar' is defined but NOT executed until called
    - 'second_largest_number' is defined but NOT executed until called
    - 'second_largest_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 72:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_valid_email(email)
      Returns a value via 'return' keyword
    def is_valid_email(email)
      Returns a value via 'return' keyword
    def is_valid_email(email)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: bool, is_valid_email, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'pattern'
  • Function call: bool(value)
  • Function call: re.fullmatch(pattern, email)
  • Function call: print(value)
  • Function call: is_valid_email('test@example.com')
  • Function call: bool(value)
  • Function call: re.fullmatch(pattern, email)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: bool(value)
  • Function call: re.fullmatch(pattern, email)
  • Assignment: right side evaluated first, then stored in 'email'
  • Function call: is_valid_email(email)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_valid_email(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: re
  2. Define function 'is_valid_email' (stored in memory, not executed yet)
  3. Execute function call: print(is_valid_email(...))
  4. Define function 'is_valid_email' (stored in memory, not executed yet)
  5. Define function 'is_valid_email' (stored in memory, not executed yet)
  6. Assign value to variable: email
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_valid_email(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_valid_email' is defined but NOT executed until called
    - 'is_valid_email' is defined but NOT executed until called
    - 'is_valid_email' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 73:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def factorial(n)
      Returns a value via 'return' keyword
    def main()
    def factorial(n)
      Returns a value via 'return' keyword
    def main()
    def factorial(n)
      Returns a value via 'return' keyword
    def main()
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, try/except
  Operators used: *, +, -, ., <, (), ==, or
  Control structures: for loop iteration; try/except error handling
  Functions called: factorial, input, int, main, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: n * factorial(...)
  • Binary operation: n - 1
  • Function call: factorial(computed expression)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Condition branch: evaluates boolean expression first
  • Function call: print('Factorial is not defined for negative numbers.')
  • Function call: print(value)
  • Function call: factorial(number)
  • Function call: print('Please enter a valid integer.')
  • Function call: main()
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: factorial(number)
  • Function call: main()
  • Function call: math.factorial(n)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: factorial(number)
  • Function call: main()
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: factorial(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: factorial(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Define function 'main' (stored in memory, not executed yet)
  3. Execute function call: main()
  4. Define function 'factorial' (stored in memory, not executed yet)
  5. Define function 'main' (stored in memory, not executed yet)
  6. Execute function call: main()
  7. Define function 'factorial' (stored in memory, not executed yet)
  8. Define function 'main' (stored in memory, not executed yet)
  9. Execute function call: main()
  10. Define function 'factorial' (stored in memory, not executed yet)
  11. Define function 'factorial' (stored in memory, not executed yet)
  12. Assign value to variable: n
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 74:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def are_anagrams()
    def are_anagrams()
      Returns a value via 'return' keyword
    def are_anagrams()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), ==
  Functions called: are_anagrams, input, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).replace(...).lower()
  • Function call: input(...).replace(' ', '')
  • Function call: input('Enter the first string: ')
  • Assignment: right side evaluated first, then stored in 'first'
  • Function call: input(...).replace(...).lower()
  • Function call: input(...).replace(' ', '')
  • Function call: input('Enter the second string: ')
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: print(value)
  • Function call: sorted(first)
  • Function call: sorted(second)
  • Function call: are_anagrams()
  • Function call: input(...).replace(...).lower()
  • Function call: input(...).replace(' ', '')
  • Function call: input('Enter the first string: ')
  • Assignment: right side evaluated first, then stored in 'first'
  • Function call: input(...).replace(...).lower()
  • Function call: input(...).replace(' ', '')
  • Function call: input('Enter the second string: ')
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: sorted(first)
  • Function call: sorted(second)
  • Function call: input(...).replace(...).lower()
  • Function call: input(...).replace(' ', '')
  • Function call: input('Enter the first string: ')
  • Assignment: right side evaluated first, then stored in 'first'
  • Function call: input(...).replace(...).lower()
  • Function call: input(...).replace(' ', '')
  • Function call: input('Enter the second string: ')
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: sorted(first)
  • Function call: sorted(second)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first'
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: print(value)
  • Function call: sorted(first)
  • Function call: sorted(second)
  • Assignment: right side evaluated first, then stored in 'first'
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: print(value)
  • Function call: sorted(first)
  • Function call: sorted(second)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'are_anagrams' (stored in memory, not executed yet)
  2. Execute function call: are_anagrams()
  3. Define function 'are_anagrams' (stored in memory, not executed yet)
  4. Define function 'are_anagrams' (stored in memory, not executed yet)
  5. Assign value to variable: first
  6. Assign value to variable: second
  7. Execute function call: print(value)
  8. Assign value to variable: first
  9. Assign value to variable: second
  10. Execute function call: print(value)

▸ EXECUTION FLOW

  Function definition phase:
    - 'are_anagrams' is defined but NOT executed until called
    - 'are_anagrams' is defined but NOT executed until called
    - 'are_anagrams' is defined but NOT executed until called
  Runtime execution (top-level code):
    - are_anagrams() is invoked
      → Execution jumps to the function body of 'are_anagrams'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 75:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def is_prime(n)
      Returns a value via 'return' keyword
    def main()
    def is_prime(n)
      Returns a value via 'return' keyword
    def is_prime(n)
      Returns a value via 'return' keyword
    def is_prime(n)
      Returns a value via 'return' keyword
    def is_prime(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, try/except, while
  Operators used: %, *, +, <, !=, (), **, <=, ==, or
  Control structures: for loop iteration; while loop; try/except error handling; generator expression
  Functions called: all, input, int, is_prime, main, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Binary operation: n % 2
  • Binary operation: n % 3
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: i * i
  • Binary operation: n % i
  • Binary operation: n % expression
  • Binary operation: i + 2
  • Condition branch: evaluates boolean expression first
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_prime(number)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Function call: print('Please enter a valid integer.')
  • Function call: main()
  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: n % i
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_prime(17)
  • Function call: print(value)
  • Function call: is_prime(10)
  • Function call: print(value)
  • Function call: is_prime(2)
  • Condition branch: evaluates boolean expression first
  • Binary operation: n % i
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: all(value)
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Function call: print(value)
  • Function call: is_prime(23)
  • Function call: print(value)
  • Function call: is_prime(15)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: is_prime(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_prime(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_prime' (stored in memory, not executed yet)
  2. Define function 'main' (stored in memory, not executed yet)
  3. Execute function call: main()
  4. Define function 'is_prime' (stored in memory, not executed yet)
  5. Execute function call: print(is_prime(...))
  6. Execute function call: print(is_prime(...))
  7. Execute function call: print(is_prime(...))
  8. Define function 'is_prime' (stored in memory, not executed yet)
  9. Execute function call: print(is_prime(...))
  10. Execute function call: print(is_prime(...))
  11. Define function 'is_prime' (stored in memory, not executed yet)
  12. Define function 'is_prime' (stored in memory, not executed yet)
  13. Assign value to variable: n
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(is_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_prime' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 76:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def common_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def common_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def find_common_elements()
    def common_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def common_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def common_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def common_elements(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), ==, in, and, not in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: common_elements, filter, find_common_elements, input, list, map, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(i)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: common_elements(lst1, lst2)
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'elements_commmon'
  • Assignment: right side evaluated first, then stored in 'arg1'
  • Assignment: right side evaluated first, then stored in 'arg2'
  • Function call: print(value)
  • Function call: common_elements(arg1, arg2)
  • Function call: input('Enter elements of the first list, separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'input1'
  • Function call: input1.split()
  • Assignment: right side evaluated first, then stored in 'list1'
  • Function call: input('Enter elements of the second list, separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'input2'
  • Function call: input2.split()
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: set(list1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(list2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common_elements'
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: list(common_elements)
  • Function call: print('There are no common elements between the two lists.')
  • Function call: find_common_elements()
  • Function call: list(value)
  • Function call: set(value)
  • Function call: print(value)
  • Function call: common_elements(value, value)
  • Function call: list(value)
  • Function call: filter(value, lst1)
  • Function call: print(value)
  • Function call: common_elements(value, value)
  • Function call: list(value)
  • Function call: map(value, lst1)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: common_elements(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: common_elements(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(common_elements(...))
  5. Define function 'common_elements' (stored in memory, not executed yet)
  6. Assign value to variable: arg1
  7. Assign value to variable: arg2
  8. Execute function call: print(common_elements(...))
  9. Define function 'find_common_elements' (stored in memory, not executed yet)
  10. Execute function call: find_common_elements()
  11. Define function 'common_elements' (stored in memory, not executed yet)
  12. Execute function call: print(common_elements(...))
  13. Define function 'common_elements' (stored in memory, not executed yet)
  14. Execute function call: print(common_elements(...))
  15. Define function 'common_elements' (stored in memory, not executed yet)
  16. Define function 'common_elements' (stored in memory, not executed yet)
  17. Assign value to variable: lst1
  18. Assign value to variable: lst2
  19. Assign value to variable: result
  20. Execute function call: print(result)
  21. Execute function call: print(common_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'common_elements' is defined but NOT executed until called
    - 'common_elements' is defined but NOT executed until called
    - 'find_common_elements' is defined but NOT executed until called
    - 'common_elements' is defined but NOT executed until called
    - 'common_elements' is defined but NOT executed until called
    - 'common_elements' is defined but NOT executed until called
    - 'common_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - find_common_elements() is invoked
      → Execution jumps to the function body of 'find_common_elements'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst2, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 77:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def uppercase(string)
      Returns a value via 'return' keyword
    def uppercase(string)
      Returns a value via 'return' keyword
    def uppercase(string)
      Returns a value via 'return' keyword
    def uppercase(string)
      Returns a value via 'return' keyword
    def uppercase(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), ==
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: filter, len, list, print, sum, uppercase
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: word.upper()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: print(value)
  • Function call: uppercase(sentence)
  • Function call: sum(value)
  • Function call: char.isupper()
  • Function call: print(value)
  • Function call: uppercase('Hello World')
  • Function call: len(value)
  • Function call: list(value)
  • Function call: filter(value, string)
  • Function call: print(value)
  • Function call: uppercase('Python Programming')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: uppercase(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: uppercase('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'uppercase' (stored in memory, not executed yet)
  2. Assign value to variable: sentence
  3. Execute function call: print(uppercase(...))
  4. Define function 'uppercase' (stored in memory, not executed yet)
  5. Execute function call: print(uppercase(...))
  6. Define function 'uppercase' (stored in memory, not executed yet)
  7. Execute function call: print(uppercase(...))
  8. Define function 'uppercase' (stored in memory, not executed yet)
  9. Define function 'uppercase' (stored in memory, not executed yet)
  10. Assign value to variable: string
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'uppercase' is defined but NOT executed until called
    - 'uppercase' is defined but NOT executed until called
    - 'uppercase' is defined but NOT executed until called
    - 'uppercase' is defined but NOT executed until called
    - 'uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 78:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def decimal_to_binary()
    def decimal_to_binary()
      Returns a value via 'return' keyword
    def decimal_to_binary()
    def decimal_to_binary()
      Returns a value via 'return' keyword
    def decimal_to_binary()
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: %, +, >, (), ==, []
  Control structures: if/else conditional branching; while loop
  Functions called: bin, decimal_to_binary, format, input, int, print, str
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('Enter a decimal number: ')
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: bin(decimal_number)
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Function call: print(value)
  • Function call: decimal_to_binary()
  • Function call: int(value)
  • Function call: input('Enter a decimal number: ')
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'binary'
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: str(...) + binary
  • Binary operation: num % 2
  • Function call: str(computed expression)
  • Assignment: right side evaluated first, then stored in 'binary'
  • Function call: print(value)
  • Function call: decimal_to_binary()
  • Function call: int(value)
  • Function call: input('Enter a decimal number: ')
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: format(decimal_number, 'b')
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Function call: print(value)
  • Function call: decimal_to_binary()
  • Function call: int(value)
  • Function call: input('Enter a decimal number: ')
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: bin(decimal_number)
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Function call: int(value)
  • Function call: input('Enter a decimal number: ')
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: bin(decimal_number)
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: bin(decimal_number)
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: bin(decimal_number)
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Function call: print(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'decimal_to_binary' (stored in memory, not executed yet)
  2. Execute function call: decimal_to_binary()
  3. Define function 'decimal_to_binary' (stored in memory, not executed yet)
  4. Execute function call: decimal_to_binary()
  5. Define function 'decimal_to_binary' (stored in memory, not executed yet)
  6. Execute function call: decimal_to_binary()
  7. Define function 'decimal_to_binary' (stored in memory, not executed yet)
  8. Define function 'decimal_to_binary' (stored in memory, not executed yet)
  9. Assign value to variable: decimal_number
  10. Assign value to variable: binary_number
  11. Execute function call: print(value)
  12. Assign value to variable: decimal_number
  13. Assign value to variable: binary_number
  14. Execute function call: print(value)

▸ EXECUTION FLOW

  Function definition phase:
    - 'decimal_to_binary' is defined but NOT executed until called
    - 'decimal_to_binary' is defined but NOT executed until called
    - 'decimal_to_binary' is defined but NOT executed until called
    - 'decimal_to_binary' is defined but NOT executed until called
    - 'decimal_to_binary' is defined but NOT executed until called
  Runtime execution (top-level code):
    - decimal_to_binary() is invoked
      → Execution jumps to the function body of 'decimal_to_binary'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - decimal_to_binary() is invoked
      → Execution jumps to the function body of 'decimal_to_binary'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - decimal_to_binary() is invoked
      → Execution jumps to the function body of 'decimal_to_binary'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 79:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def main()
      Returns a value via 'return' keyword
    def ascending_order_numbers(numbers)
      Returns a value via 'return' keyword
    def ascending_order_numbers(numbers)
      Returns a value via 'return' keyword
    def ascending_order_numbers(numbers)
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, -, ., >, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: ascending_order_numbers, input, int, len, main, print, range, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: input('enter list of numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: result.append(value)
  • Function call: int(num)
  • Function call: sorted(result)
  • Function call: main()
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sort_numbers'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: ascending_order_numbers(numbers)
  • Function call: numbers.copy()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.sort()
  • Function call: print(value)
  • Function call: ascending_order_numbers(value)
  • Function call: numbers.copy()
  • Assignment: right side evaluated first, then stored in 'arr'
  • Function call: len(arr)
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Binary operation: expression - 1
  • Binary operation: n - i
  • Function call: range(0, computed expression)
  • Binary operation: j + 1
  • Condition branch: evaluates boolean expression first
  • Binary operation: j + 1
  • Binary operation: j + 1
  • Function call: print(value)
  • Function call: ascending_order_numbers(value)
  • Function call: result.copy()
  • Assignment: right side evaluated first, then stored in 'copy'
  • Function call: copy.sort()
  • Function call: sorted(result)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: ascending_order_numbers(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: ascending_order_numbers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Execute function call: main()
  3. Define function 'ascending_order_numbers' (stored in memory, not executed yet)
  4. Assign value to variable: numbers
  5. Execute function call: print(ascending_order_numbers(...))
  6. Define function 'ascending_order_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(ascending_order_numbers(...))
  8. Define function 'ascending_order_numbers' (stored in memory, not executed yet)
  9. Execute function call: print(ascending_order_numbers(...))
  10. Define function 'main' (stored in memory, not executed yet)
  11. Define function 'main' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(ascending_order_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'ascending_order_numbers' is defined but NOT executed until called
    - 'ascending_order_numbers' is defined but NOT executed until called
    - 'ascending_order_numbers' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 80:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def generate_random_number()
    def generate_random_number()
    def generate_random_number()
    def generate_random_number()
      Returns a value via 'return' keyword
    def generate_random_number()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., ()
  Functions called: float, generate_random_number, input, int, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('Enter the lower limit: ')
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Function call: int(value)
  • Function call: input('Enter the upper limit: ')
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: random.randint(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Function call: print(value)
  • Function call: generate_random_number()
  • Function call: int(value)
  • Function call: input('Enter the lower limit: ')
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Function call: int(value)
  • Function call: input('Enter the upper limit: ')
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Binary operation: upper_limit + 1
  • Function call: random.randrange(lower_limit, computed expression)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Function call: print(value)
  • Function call: generate_random_number()
  • Function call: float(value)
  • Function call: input('Enter the lower limit: ')
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Function call: float(value)
  • Function call: input('Enter the upper limit: ')
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: random.uniform(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Function call: print(value)
  • Function call: generate_random_number()
  • Function call: int(value)
  • Function call: input('Enter the lower limit: ')
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Function call: int(value)
  • Function call: input('Enter the upper limit: ')
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: random.randint(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Function call: int(value)
  • Function call: input('Enter the lower limit: ')
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Function call: int(value)
  • Function call: input('Enter the upper limit: ')
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: random.randint(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: random.randint(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: random.randint(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'random_number'
  • Function call: print(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: random
  2. Define function 'generate_random_number' (stored in memory, not executed yet)
  3. Execute function call: generate_random_number()
  4. Define function 'generate_random_number' (stored in memory, not executed yet)
  5. Execute function call: generate_random_number()
  6. Define function 'generate_random_number' (stored in memory, not executed yet)
  7. Execute function call: generate_random_number()
  8. Define function 'generate_random_number' (stored in memory, not executed yet)
  9. Define function 'generate_random_number' (stored in memory, not executed yet)
  10. Assign value to variable: lower_limit
  11. Assign value to variable: upper_limit
  12. Assign value to variable: random_number
  13. Execute function call: print(value)
  14. Assign value to variable: lower_limit
  15. Assign value to variable: upper_limit
  16. Assign value to variable: random_number
  17. Execute function call: print(value)

▸ EXECUTION FLOW

  Function definition phase:
    - 'generate_random_number' is defined but NOT executed until called
    - 'generate_random_number' is defined but NOT executed until called
    - 'generate_random_number' is defined but NOT executed until called
    - 'generate_random_number' is defined but NOT executed until called
    - 'generate_random_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - generate_random_number() is invoked
      → Execution jumps to the function body of 'generate_random_number'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - generate_random_number() is invoked
      → Execution jumps to the function body of 'generate_random_number'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - generate_random_number() is invoked
      → Execution jumps to the function body of 'generate_random_number'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 81:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def square_root()
      Returns a value via 'return' keyword
    def square_root()
    def square_root()
      Returns a value via 'return' keyword
    def square_root()
      Returns a value via 'return' keyword
    def square_root()
      Returns a value via 'return' keyword
    def square_root()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., /, <, (), **
  Control structures: if/else conditional branching; for loop iteration
  Functions called: float, input, print, range, square_root
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: float(value)
  • Function call: input('type num: ')
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: num ** 0.5
  • Function call: print(value)
  • Function call: square_root()
  • Function call: float(value)
  • Function call: input('type num: ')
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: num ** 0.5
  • Function call: print(computed expression)
  • Function call: square_root()
  • Function call: float(value)
  • Function call: input('type num: ')
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: math.sqrt(num)
  • Function call: print(value)
  • Function call: square_root()
  • Function call: float(value)
  • Function call: input('type num: ')
  • Assignment: right side evaluated first, then stored in 'num'
  • Condition branch: evaluates boolean expression first
  • Binary operation: num / 2
  • Assignment: right side evaluated first, then stored in 'guess'
  • Function call: range(10)
  • Binary operation: expression / 2
  • Binary operation: guess + expression
  • Binary operation: num / guess
  • Assignment: right side evaluated first, then stored in 'guess'
  • Function call: print(value)
  • Function call: square_root()
  • Binary operation: num ** 0.5
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: num ** 0.5
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: num ** 0.5
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: num ** 0.5
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'square_root' (stored in memory, not executed yet)
  2. Execute function call: print(square_root(...))
  3. Define function 'square_root' (stored in memory, not executed yet)
  4. Execute function call: square_root()
  5. Define function 'square_root' (stored in memory, not executed yet)
  6. Execute function call: print(square_root(...))
  7. Define function 'square_root' (stored in memory, not executed yet)
  8. Execute function call: print(square_root(...))
  9. Define function 'square_root' (stored in memory, not executed yet)
  10. Define function 'square_root' (stored in memory, not executed yet)
  11. Assign value to variable: num
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Assign value to variable: num
  15. Assign value to variable: result
  16. Execute function call: print(result)

▸ EXECUTION FLOW

  Function definition phase:
    - 'square_root' is defined but NOT executed until called
    - 'square_root' is defined but NOT executed until called
    - 'square_root' is defined but NOT executed until called
    - 'square_root' is defined but NOT executed until called
    - 'square_root' is defined but NOT executed until called
    - 'square_root' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - square_root() is invoked
      → Execution jumps to the function body of 'square_root'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 82:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Circle
    Circle methods: __init__, area
  Functions defined: 6
    def circle()
    def calculate_circle_area()
    def calculate_circle_area(radius)
      Returns a value via 'return' keyword
    def main()
    def circle()
      Returns a value via 'return' keyword
    def circle()
      Returns a value via 'return' keyword
  Keywords used: class, def, if/elif/else, lambda, return
  Operators used: *, ., (), **, ==
  Control structures: if/else conditional branching; lambda expression
  Functions called: Circle, calculate_circle_area, circle, float, input, main, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: float(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'radius'
  • Binary operation: math.pi * expression
  • Binary operation: radius ** 2
  • Assignment: right side evaluated first, then stored in 'area'
  • Function call: print(area)
  • Function call: circle()
  • Function call: float(value)
  • Function call: input('Enter the radius of the circle: ')
  • Assignment: right side evaluated first, then stored in 'radius'
  • Binary operation: math.pi * expression
  • Binary operation: radius ** 2
  • Assignment: right side evaluated first, then stored in 'area'
  • Function call: print(value)
  • Function call: calculate_circle_area()
  • Binary operation: math.pi * expression
  • Binary operation: radius ** 2
  • Function call: float(value)
  • Function call: input('Enter the radius of the circle: ')
  • Assignment: right side evaluated first, then stored in 'radius'
  • Function call: calculate_circle_area(radius)
  • Assignment: right side evaluated first, then stored in 'area'
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Binary operation: math.pi * expression
  • Binary operation: radius ** 2
  • Assignment: right side evaluated first, then stored in 'calculate_circle_area'
  • Function call: print(value)
  • Function call: calculate_circle_area(7)
  • Binary operation: math.pi * expression
  • Binary operation: self.radius ** 2
  • Function call: Circle(10)
  • Assignment: right side evaluated first, then stored in 'circle'
  • Function call: print(value)
  • Function call: circle.area()
  • Function call: float(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'radius'
  • Binary operation: math.pi * expression
  • Binary operation: radius ** 2
  • Assignment: right side evaluated first, then stored in 'area'
  • Function call: float(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'radius'
  • Binary operation: math.pi * expression
  • Binary operation: radius ** 2
  • Assignment: right side evaluated first, then stored in 'area'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'radius'
  • Function call: calculate_circle_area(radius)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_circle_area(14)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: math
  2. Define function 'circle' (stored in memory, not executed yet)
  3. Execute function call: circle()
  4. Define function 'calculate_circle_area' (stored in memory, not executed yet)
  5. Execute function call: calculate_circle_area()
  6. Import module: math
  7. Define function 'calculate_circle_area' (stored in memory, not executed yet)
  8. Define function 'main' (stored in memory, not executed yet)
  9. If/else: conditional execution
  10. Assign value to variable: calculate_circle_area
  11. Execute function call: print(value)
  12. Define class 'Circle'
  13. Assign value to variable: circle
  14. Execute function call: print(value)
  15. Define function 'circle' (stored in memory, not executed yet)
  16. Define function 'circle' (stored in memory, not executed yet)
  17. Assign value to variable: radius
  18. Assign value to variable: result
  19. Execute function call: print(result)
  20. Execute function call: print(calculate_circle_area(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Circle' is defined with methods: __init__, area
      • __init__ runs automatically on instantiation to initialize attributes
      • 'area' is a regular method — runs when called on an instance
  Function definition phase:
    - 'circle' is defined but NOT executed until called
    - 'calculate_circle_area' is defined but NOT executed until called
    - 'calculate_circle_area' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'circle' is defined but NOT executed until called
    - 'circle' is defined but NOT executed until called
  Runtime execution (top-level code):
    - circle() is invoked
      → Execution jumps to the function body of 'circle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - calculate_circle_area() is invoked
      → Execution jumps to the function body of 'calculate_circle_area'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 83:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def main()
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: -, ., <, !=, (), <=, ==, []
  Control structures: while loop
  Functions called: input, is_palindrome, len, main, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.replace(...).lower()
  • Function call: s.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: is_palindrome(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Function call: s.replace(...).lower()
  • Function call: s.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 's'
  • Binary operation: len(...) - 1
  • Function call: len(s)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_palindrome('A man a plan a canal Panama')
  • Function call: s.replace(...).lower()
  • Function call: s.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: len(s)
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Function call: is_palindrome(value)
  • Function call: print(value)
  • Function call: is_palindrome('racecar')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_palindrome(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Define function 'main' (stored in memory, not executed yet)
  3. If/else: conditional execution
  4. Define function 'is_palindrome' (stored in memory, not executed yet)
  5. Execute function call: print(is_palindrome(...))
  6. Define function 'is_palindrome' (stored in memory, not executed yet)
  7. Execute function call: print(is_palindrome(...))
  8. Define function 'is_palindrome' (stored in memory, not executed yet)
  9. Define function 'is_palindrome' (stored in memory, not executed yet)
  10. Assign value to variable: s
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 84:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def is_pangram(sentence)
      Returns a value via 'return' keyword
    def pangram(sentence)
      Returns a value via 'return' keyword
    def is_pangram(sentence)
      Returns a value via 'return' keyword
    def is_pangram(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: is_pangram, print, set
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: set('abcdefghijklmnopqrstuvwxyz')
  • Assignment: right side evaluated first, then stored in 'alphabet'
  • Function call: alphabet.issubset(value)
  • Function call: set(value)
  • Function call: sentence.lower()
  • Function call: is_pangram(sentence)
  • Function call: print(value)
  • Function call: is_pangram('The quick brown fox jumps over the lazy dog')
  • Function call: alphabet.issubset(value)
  • Function call: set(value)
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: alphabet.issubset(value)
  • Function call: set(value)
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: is_pangram(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_pangram('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_pangram' (stored in memory, not executed yet)
  2. Define function 'pangram' (stored in memory, not executed yet)
  3. Execute function call: print(is_pangram(...))
  4. Define function 'is_pangram' (stored in memory, not executed yet)
  5. Define function 'is_pangram' (stored in memory, not executed yet)
  6. Assign value to variable: sentence
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_pangram(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_pangram' is defined but NOT executed until called
    - 'pangram' is defined but NOT executed until called
    - 'is_pangram' is defined but NOT executed until called
    - 'is_pangram' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 85:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def gcd(a, b)
      Returns a value via 'return' keyword
    def main()
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: %, ., !=, (), ==
  Control structures: while loop
  Functions called: gcd, input, int, main, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a % b
  • Function call: int(value)
  • Function call: input('Enter the first number: ')
  • Assignment: right side evaluated first, then stored in 'num1'
  • Function call: int(value)
  • Function call: input('Enter the second number: ')
  • Assignment: right side evaluated first, then stored in 'num2'
  • Function call: gcd(num1, num2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Function call: math.gcd(a, b)
  • Function call: print(value)
  • Function call: gcd(48, 18)
  • Function call: print(value)
  • Function call: gcd(100, 75)
  • Condition branch: evaluates boolean expression first
  • Binary operation: a % b
  • Function call: gcd(b, computed expression)
  • Function call: print(value)
  • Function call: gcd(56, 98)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: gcd(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: gcd(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'gcd' (stored in memory, not executed yet)
  2. Define function 'main' (stored in memory, not executed yet)
  3. If/else: conditional execution
  4. Define function 'gcd' (stored in memory, not executed yet)
  5. Execute function call: print(gcd(...))
  6. Execute function call: print(gcd(...))
  7. Define function 'gcd' (stored in memory, not executed yet)
  8. Execute function call: print(gcd(...))
  9. Define function 'gcd' (stored in memory, not executed yet)
  10. Define function 'gcd' (stored in memory, not executed yet)
  11. Assign value to variable: a
  12. Assign value to variable: b
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(gcd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'gcd' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 86:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def gcd(a, b)
      Returns a value via 'return' keyword
    def lcm(a, b)
      Returns a value via 'return' keyword
    def main()
    def lcm(a, b)
      Returns a value via 'return' keyword
    def lcm(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: %, *, +, ., !=, (), //, ==
  Control structures: for loop iteration; while loop
  Functions called: abs, gcd, input, int, lcm, main, max, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a % b
  • Binary operation: abs(...) // gcd(...)
  • Binary operation: a * b
  • Function call: abs(computed expression)
  • Function call: gcd(a, b)
  • Function call: int(value)
  • Function call: input('Enter the first number: ')
  • Assignment: right side evaluated first, then stored in 'num1'
  • Function call: int(value)
  • Function call: input('Enter the second number: ')
  • Assignment: right side evaluated first, then stored in 'num2'
  • Function call: lcm(num1, num2)
  • Assignment: right side evaluated first, then stored in 'result_lcm'
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Function call: math.lcm(a, b)
  • Function call: print(value)
  • Function call: lcm(12, 18)
  • Function call: print(value)
  • Function call: lcm(5, 7)
  • Binary operation: expression + 1
  • Binary operation: a * b
  • Function call: range(value, computed expression, value)
  • Function call: max(a, b)
  • Function call: max(a, b)
  • Binary operation: i % b
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: lcm(4, 6)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: gcd(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: gcd(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'gcd' (stored in memory, not executed yet)
  2. Define function 'lcm' (stored in memory, not executed yet)
  3. Define function 'main' (stored in memory, not executed yet)
  4. If/else: conditional execution
  5. Define function 'lcm' (stored in memory, not executed yet)
  6. Execute function call: print(lcm(...))
  7. Execute function call: print(lcm(...))
  8. Define function 'lcm' (stored in memory, not executed yet)
  9. Execute function call: print(lcm(...))
  10. Define function 'gcd' (stored in memory, not executed yet)
  11. Define function 'gcd' (stored in memory, not executed yet)
  12. Assign value to variable: a
  13. Assign value to variable: b
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(gcd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'gcd' is defined but NOT executed until called
    - 'lcm' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'lcm' is defined but NOT executed until called
    - 'lcm' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 87:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def reverse_lst()
      Returns a value via 'return' keyword
    def reverse_list()
      Returns a value via 'return' keyword
    def reverse_list()
    def reverse_list(items)
      Returns a value via 'return' keyword
    def reverse_list(items)
      Returns a value via 'return' keyword
    def reverse_list(items)
      Returns a value via 'return' keyword
    def reverse_lst()
      Returns a value via 'return' keyword
    def reverse_lst()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: -, ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: deque, input, len, list, print, range, reverse_list, reverse_lst
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('Type a list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'split_lst'
  • Assignment: right side evaluated first, then stored in 'reversed_lst'
  • Function call: reversed_lst.append(item)
  • Function call: ' '.join(reversed_lst)
  • Function call: print(value)
  • Function call: reverse_lst()
  • Function call: input('Enter a list: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'elements'
  • Assignment: right side evaluated first, then stored in 'reversed_elements'
  • Binary operation: len(...) - 1
  • Function call: range(computed expression, value, value)
  • Function call: len(elements)
  • Function call: reversed_elements.append(value)
  • Function call: print(value)
  • Function call: reverse_list()
  • Function call: input('Enter list: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'elements'
  • Assignment: right side evaluated first, then stored in 'reversed_elements'
  • Binary operation: len(...) - 1
  • Function call: range(computed expression, value, value)
  • Function call: len(elements)
  • Function call: reversed_elements.append(value)
  • Function call: print('Reversed list:', reversed_elements)
  • Function call: reverse_list()
  • Assignment: right side evaluated first, then stored in 'reverse_items'
  • Binary operation: len(...) - 1
  • Function call: range(computed expression, value, value)
  • Function call: len(items)
  • Function call: reverse_items.append(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse_list(lst)
  • Binary operation: len(...) - 1
  • Function call: range(computed expression, value, value)
  • Function call: len(items)
  • Function call: print(value)
  • Function call: reverse_list(value)
  • Function call: deque(items)
  • Assignment: right side evaluated first, then stored in 'd'
  • Function call: d.reverse()
  • Function call: list(d)
  • Function call: print(value)
  • Function call: reverse_list(value)
  • Function call: ' '.join(reversed_lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: ' '.join(reversed_lst)
  • Assignment: right side evaluated first, then stored in 'items'
  • Function call: reverse_list(items)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_lst' (stored in memory, not executed yet)
  2. Execute function call: print(reverse_lst(...))
  3. Define function 'reverse_list' (stored in memory, not executed yet)
  4. Execute function call: print(reverse_list(...))
  5. Define function 'reverse_list' (stored in memory, not executed yet)
  6. Execute function call: reverse_list()
  7. Define function 'reverse_list' (stored in memory, not executed yet)
  8. Assign value to variable: lst
  9. Execute function call: print(reverse_list(...))
  10. Define function 'reverse_list' (stored in memory, not executed yet)
  11. Execute function call: print(reverse_list(...))
  12. Define function 'reverse_list' (stored in memory, not executed yet)
  13. Execute function call: print(reverse_list(...))
  14. Define function 'reverse_lst' (stored in memory, not executed yet)
  15. Define function 'reverse_lst' (stored in memory, not executed yet)
  16. Assign value to variable: items
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(reverse_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_lst' is defined but NOT executed until called
    - 'reverse_list' is defined but NOT executed until called
    - 'reverse_list' is defined but NOT executed until called
    - 'reverse_list' is defined but NOT executed until called
    - 'reverse_list' is defined but NOT executed until called
    - 'reverse_list' is defined but NOT executed until called
    - 'reverse_lst' is defined but NOT executed until called
    - 'reverse_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - reverse_list() is invoked
      → Execution jumps to the function body of 'reverse_list'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 88:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def prime_factors(n)
      Returns a value via 'return' keyword
    def main()
    def prime_factors(n)
      Returns a value via 'return' keyword
    def prime_factors(n, divisor)
      (1 parameter(s) with default values)
      Returns a value via 'return' keyword
    def prime_factors(n)
      Returns a value via 'return' keyword
    def prime_factors(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: %, +, ., <, >, (), **, //, ==
  Control structures: for loop iteration; while loop
  Functions called: input, int, main, prime_factors, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'factors'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Binary operation: n % divisor
  • Function call: factors.append(divisor)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Condition branch: evaluates boolean expression first
  • Function call: print('Prime factors are only defined for numbers greater than or equal to 2.')
  • Function call: prime_factors(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Assignment: right side evaluated first, then stored in 'factors'
  • Binary operation: n % 2
  • Function call: factors.append(2)
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: range(3, computed expression, 2)
  • Function call: int(computed expression)
  • Binary operation: n % i
  • Function call: factors.append(i)
  • Condition branch: evaluates boolean expression first
  • Function call: factors.append(n)
  • Function call: print(value)
  • Function call: prime_factors(315)
  • Condition branch: evaluates boolean expression first
  • Binary operation: n % divisor
  • Condition branch: evaluates boolean expression first
  • Binary operation: expression + prime_factors(...)
  • Binary operation: n // divisor
  • Function call: prime_factors(computed expression, divisor)
  • Binary operation: divisor + 1
  • Function call: prime_factors(n, computed expression)
  • Function call: print(value)
  • Function call: prime_factors(60)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: prime_factors(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: prime_factors(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'prime_factors' (stored in memory, not executed yet)
  2. Define function 'main' (stored in memory, not executed yet)
  3. If/else: conditional execution
  4. Define function 'prime_factors' (stored in memory, not executed yet)
  5. Execute function call: print(prime_factors(...))
  6. Define function 'prime_factors' (stored in memory, not executed yet)
  7. Execute function call: print(prime_factors(...))
  8. Define function 'prime_factors' (stored in memory, not executed yet)
  9. Define function 'prime_factors' (stored in memory, not executed yet)
  10. Assign value to variable: n
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(prime_factors(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'prime_factors' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'prime_factors' is defined but NOT executed until called
    - 'prime_factors' is defined but NOT executed until called
    - 'prime_factors' is defined but NOT executed until called
    - 'prime_factors' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 89:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def generate_password(length)
      Returns a value via 'return' keyword
    def generate_password(length)
      Returns a value via 'return' keyword
    def generate_password(length)
      Returns a value via 'return' keyword
    def generate_password(length)
      Returns a value via 'return' keyword
    def generate_password(length)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, +, ., ()
  Control structures: generator expression
  Functions called: generate_password, input, int, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression + string.punctuation
  • Binary operation: string.ascii_letters + string.digits
  • Assignment: right side evaluated first, then stored in 'characters'
  • Function call: ''.join(value)
  • Function call: random.choice(characters)
  • Function call: range(length)
  • Assignment: right side evaluated first, then stored in 'password'
  • Function call: int(value)
  • Function call: input('Enter the length of the password: ')
  • Assignment: right side evaluated first, then stored in 'length'
  • Function call: print('Generated password:', value)
  • Function call: generate_password(length)
  • Binary operation: expression + string.punctuation
  • Binary operation: string.ascii_letters + string.digits
  • Assignment: right side evaluated first, then stored in 'characters'
  • Function call: ''.join(value)
  • Function call: secrets.choice(characters)
  • Function call: range(length)
  • Function call: print('Generated password:', value)
  • Function call: generate_password(value)
  • Function call: int(value)
  • Function call: input('Enter the length: ')
  • Binary operation: string.ascii_letters + string.digits
  • Assignment: right side evaluated first, then stored in 'chars'
  • Binary operation: chars * length
  • Function call: ''.join(value)
  • Function call: random.sample(computed expression, length)
  • Function call: print('Generated password:', value)
  • Function call: generate_password(value)
  • Function call: int(value)
  • Function call: input('Enter the length: ')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'length'
  • Function call: generate_password(length)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: generate_password(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: random
  2. Import module: string
  3. Define function 'generate_password' (stored in memory, not executed yet)
  4. Assign value to variable: length
  5. Execute function call: print('Generated password:', generate_password(...))
  6. Import module: secrets
  7. Import module: string
  8. Define function 'generate_password' (stored in memory, not executed yet)
  9. Execute function call: print('Generated password:', generate_password(...))
  10. Define function 'generate_password' (stored in memory, not executed yet)
  11. Execute function call: print('Generated password:', generate_password(...))
  12. Define function 'generate_password' (stored in memory, not executed yet)
  13. Define function 'generate_password' (stored in memory, not executed yet)
  14. Assign value to variable: length
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(generate_password(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'generate_password' is defined but NOT executed until called
    - 'generate_password' is defined but NOT executed until called
    - 'generate_password' is defined but NOT executed until called
    - 'generate_password' is defined but NOT executed until called
    - 'generate_password' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 90:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_words(sentence)
      Returns a value via 'return' keyword
    def count_words(sentence)
      Returns a value via 'return' keyword
    def count_words(sentence)
      Returns a value via 'return' keyword
    def count_words(sentence)
      Returns a value via 'return' keyword
    def count_words(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; generator expression
  Functions called: count_words, len, print, sum
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'split_words'
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: print(value)
  • Function call: count_words(words)
  • Function call: len(value)
  • Function call: sentence.split()
  • Function call: print(value)
  • Function call: count_words('Python is awesome')
  • Function call: sum(value)
  • Function call: sentence.split()
  • Function call: print(value)
  • Function call: count_words('Hello world from Python')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: count_words(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_words('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_words' (stored in memory, not executed yet)
  2. Assign value to variable: words
  3. Execute function call: print(count_words(...))
  4. Define function 'count_words' (stored in memory, not executed yet)
  5. Execute function call: print(count_words(...))
  6. Define function 'count_words' (stored in memory, not executed yet)
  7. Execute function call: print(count_words(...))
  8. Define function 'count_words' (stored in memory, not executed yet)
  9. Define function 'count_words' (stored in memory, not executed yet)
  10. Assign value to variable: sentence
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over split_words, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 91:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def largest_smallest(elements)
      Returns a value via 'return' keyword
    def largest_smallest(elements)
      Returns a value via 'return' keyword
    def largest_smallest(elements)
      Returns a value via 'return' keyword
    def largest_smallest(elements)
      Returns a value via 'return' keyword
    def largest_smallest(elements)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: <, >, (), []
  Control structures: for loop iteration; lambda expression
  Functions called: largest_smallest, max, min, print, reduce
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'smallest'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: largest_smallest(lst)
  • Function call: min(elements)
  • Function call: max(elements)
  • Function call: print(value)
  • Function call: largest_smallest(value)
  • Function call: reduce(value, elements)
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Function call: reduce(value, elements)
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(value)
  • Function call: largest_smallest(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'elements'
  • Function call: largest_smallest(elements)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_smallest(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'largest_smallest' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(largest_smallest(...))
  4. Define function 'largest_smallest' (stored in memory, not executed yet)
  5. Execute function call: print(largest_smallest(...))
  6. Define function 'largest_smallest' (stored in memory, not executed yet)
  7. Execute function call: print(largest_smallest(...))
  8. Define function 'largest_smallest' (stored in memory, not executed yet)
  9. Define function 'largest_smallest' (stored in memory, not executed yet)
  10. Assign value to variable: elements
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(largest_smallest(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 92:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def is_perfect_square(num)
      Returns a value via 'return' keyword
    def is_perfect_number(n)
      Returns a value via 'return' keyword
    def main()
    def is_perfect_number(n)
      Returns a value via 'return' keyword
    def is_perfect_number(n)
      Returns a value via 'return' keyword
    def is_perfect_square(num)
      Returns a value via 'return' keyword
    def is_perfect_square(num)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, +, ., (), **, //, <=, ==
  Control structures: for loop iteration; generator expression
  Functions called: input, int, is_perfect_number, is_perfect_square, main, print, range, sum
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'sum_divisor'
  • Binary operation: num + 1
  • Function call: range(1, computed expression)
  • Binary operation: num % i
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Function call: is_perfect_square(6)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'sum_of_divisors'
  • Function call: range(1, n)
  • Binary operation: n % i
  • Condition branch: evaluates boolean expression first
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_perfect_number(number)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Condition branch: evaluates boolean expression first
  • Binary operation: n % i
  • Function call: sum(value)
  • Function call: range(1, n)
  • Function call: print(value)
  • Function call: is_perfect_number(28)
  • Function call: print(value)
  • Function call: is_perfect_number(12)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'divisors'
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: n % i
  • Condition branch: evaluates boolean expression first
  • Function call: divisors.add(i)
  • Binary operation: n // i
  • Function call: divisors.add(computed expression)
  • Function call: sum(divisors)
  • Function call: print(value)
  • Function call: is_perfect_number(6)
  • Function call: print(value)
  • Function call: is_perfect_number(496)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: is_perfect_square(num)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_perfect_square(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_perfect_square' (stored in memory, not executed yet)
  2. Execute function call: is_perfect_square(6)
  3. Define function 'is_perfect_number' (stored in memory, not executed yet)
  4. Define function 'main' (stored in memory, not executed yet)
  5. If/else: conditional execution
  6. Define function 'is_perfect_number' (stored in memory, not executed yet)
  7. Execute function call: print(is_perfect_number(...))
  8. Execute function call: print(is_perfect_number(...))
  9. Define function 'is_perfect_number' (stored in memory, not executed yet)
  10. Execute function call: print(is_perfect_number(...))
  11. Execute function call: print(is_perfect_number(...))
  12. Define function 'is_perfect_square' (stored in memory, not executed yet)
  13. Define function 'is_perfect_square' (stored in memory, not executed yet)
  14. Assign value to variable: num
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(is_perfect_square(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_perfect_square' is defined but NOT executed until called
    - 'is_perfect_number' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'is_perfect_number' is defined but NOT executed until called
    - 'is_perfect_number' is defined but NOT executed until called
    - 'is_perfect_square' is defined but NOT executed until called
    - 'is_perfect_square' is defined but NOT executed until called
  Runtime execution (top-level code):
    - is_perfect_square() is invoked
      → Execution jumps to the function body of 'is_perfect_square'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 93:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def factorial()
    def factorial()
      Returns a value via 'return' keyword
    def factorial()
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial()
      Returns a value via 'return' keyword
    def factorial()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: *, +, -, ., (), <=
  Control structures: if/else conditional branching; for loop iteration
  Functions called: factorial, input, int, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: user + 1
  • Function call: range(1, computed expression)
  • Function call: print(result)
  • Function call: factorial()
  • Function call: int(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: user + 1
  • Function call: range(1, computed expression)
  • Function call: print(value)
  • Function call: factorial()
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: print(value)
  • Function call: math.factorial(n)
  • Function call: factorial()
  • Condition branch: evaluates boolean expression first
  • Binary operation: n * factorial(...)
  • Binary operation: n - 1
  • Function call: factorial(computed expression)
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: print(value)
  • Function call: factorial(n)
  • Function call: int(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: user + 1
  • Function call: range(1, computed expression)
  • Function call: int(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: user + 1
  • Function call: range(1, computed expression)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: factorial(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: factorial(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Execute function call: factorial()
  3. Define function 'factorial' (stored in memory, not executed yet)
  4. Execute function call: print(factorial(...))
  5. Import module: math
  6. Define function 'factorial' (stored in memory, not executed yet)
  7. Execute function call: factorial()
  8. Define function 'factorial' (stored in memory, not executed yet)
  9. Assign value to variable: n
  10. Execute function call: print(factorial(...))
  11. Define function 'factorial' (stored in memory, not executed yet)
  12. Define function 'factorial' (stored in memory, not executed yet)
  13. Assign value to variable: n
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - factorial() is invoked
      → Execution jumps to the function body of 'factorial'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - factorial() is invoked
      → Execution jumps to the function body of 'factorial'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 94:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_power_of_two(number)
      Returns a value via 'return' keyword
    def is_power_of_two(number)
      Returns a value via 'return' keyword
    def is_power_of_two(number)
      Returns a value via 'return' keyword
    def is_power_of_two(number)
      Returns a value via 'return' keyword
    def is_power_of_two(number)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: %, &, -, ., >, !=, (), //, <=, ==, and
  Control structures: while loop
  Functions called: input, int, is_power_of_two, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Binary operation: number // 2
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_power_of_two(number)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Binary operation: number & expression
  • Binary operation: number - 1
  • Function call: print(value)
  • Function call: is_power_of_two(16)
  • Function call: print(value)
  • Function call: is_power_of_two(18)
  • Function call: math.log2(...).is_integer()
  • Function call: math.log2(number)
  • Function call: print(value)
  • Function call: is_power_of_two(32)
  • Function call: print(value)
  • Function call: is_power_of_two(33)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_power_of_two(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_power_of_two(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_power_of_two' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. If/else: conditional execution
  4. Define function 'is_power_of_two' (stored in memory, not executed yet)
  5. Execute function call: print(is_power_of_two(...))
  6. Execute function call: print(is_power_of_two(...))
  7. Define function 'is_power_of_two' (stored in memory, not executed yet)
  8. Execute function call: print(is_power_of_two(...))
  9. Execute function call: print(is_power_of_two(...))
  10. Define function 'is_power_of_two' (stored in memory, not executed yet)
  11. Define function 'is_power_of_two' (stored in memory, not executed yet)
  12. Assign value to variable: number
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(is_power_of_two(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_power_of_two' is defined but NOT executed until called
    - 'is_power_of_two' is defined but NOT executed until called
    - 'is_power_of_two' is defined but NOT executed until called
    - 'is_power_of_two' is defined but NOT executed until called
    - 'is_power_of_two' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 95:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def get_list(prompt)
      Returns a value via 'return' keyword
    def find_intersection(list1, list2)
      Returns a value via 'return' keyword
    def main()
    def find_intersection()
    def find_intersection(list1, list2)
      Returns a value via 'return' keyword
    def find_intersection(list1, list2)
      Returns a value via 'return' keyword
    def get_list(prompt)
      Returns a value via 'return' keyword
    def get_list(prompt)
      Returns a value via 'return' keyword
  Keywords used: def, lambda, return
  Operators used: &, ., (), in
  Control structures: list comprehension; lambda expression
  Functions called: filter, find_intersection, get_list, input, list, main, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input(prompt)
  • Binary operation: set(...) & set(...)
  • Function call: list(computed expression)
  • Function call: set(list1)
  • Function call: set(list2)
  • Function call: get_list('Enter the first list of elements, separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'list1'
  • Function call: get_list('Enter the second list of elements, separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: find_intersection(list1, list2)
  • Assignment: right side evaluated first, then stored in 'intersection'
  • Function call: print(value)
  • Function call: main()
  • Function call: input(...).split()
  • Function call: input('Enter lst: ')
  • Assignment: right side evaluated first, then stored in 'list1'
  • Function call: input(...).split()
  • Function call: input('Enter lst: ')
  • Assignment: right side evaluated first, then stored in 'list2'
  • Binary operation: set(...) & set(...)
  • Function call: list(computed expression)
  • Function call: set(list1)
  • Function call: set(list2)
  • Assignment: right side evaluated first, then stored in 'intersection'
  • Function call: print(value)
  • Function call: print(value)
  • Function call: find_intersection()
  • Function call: print(value)
  • Function call: find_intersection(value, value)
  • Function call: list(value)
  • Function call: filter(value, list1)
  • Function call: print(value)
  • Function call: find_intersection(value, value)
  • Function call: input(...).split()
  • Function call: input(prompt)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: input(...).split()
  • Function call: input(prompt)
  • Assignment: right side evaluated first, then stored in 'prompt'
  • Function call: get_list(prompt)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: get_list(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'get_list' (stored in memory, not executed yet)
  2. Define function 'find_intersection' (stored in memory, not executed yet)
  3. Define function 'main' (stored in memory, not executed yet)
  4. Execute function call: main()
  5. Define function 'find_intersection' (stored in memory, not executed yet)
  6. Execute function call: print(find_intersection(...))
  7. Define function 'find_intersection' (stored in memory, not executed yet)
  8. Execute function call: print(find_intersection(...))
  9. Define function 'find_intersection' (stored in memory, not executed yet)
  10. Execute function call: print(find_intersection(...))
  11. Define function 'get_list' (stored in memory, not executed yet)
  12. Define function 'get_list' (stored in memory, not executed yet)
  13. Assign value to variable: prompt
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(get_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'get_list' is defined but NOT executed until called
    - 'find_intersection' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'find_intersection' is defined but NOT executed until called
    - 'find_intersection' is defined but NOT executed until called
    - 'find_intersection' is defined but NOT executed until called
    - 'get_list' is defined but NOT executed until called
    - 'get_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 97:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def binary_to_decimal(binary)
      Returns a value via 'return' keyword
    def main()
    def binary_to_decimal(binary)
      Returns a value via 'return' keyword
    def binary_to_decimal(binary)
      Returns a value via 'return' keyword
    def binary_to_decimal(binary)
      Returns a value via 'return' keyword
    def binary_to_decimal(binary)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: *, -, (), **, ==, in
  Control structures: for loop iteration; generator expression
  Functions called: all, binary_to_decimal, enumerate, input, int, len, main, print, reversed, sum
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'decimal'
  • Binary operation: len(...) - 1
  • Function call: len(binary)
  • Assignment: right side evaluated first, then stored in 'power'
  • Condition branch: evaluates boolean expression first
  • Binary operation: 2 ** power
  • Function call: input('Enter a binary number: ')
  • Assignment: right side evaluated first, then stored in 'binary_number'
  • Function call: all(value)
  • Condition branch: evaluates boolean expression first
  • Function call: binary_to_decimal(binary_number)
  • Assignment: right side evaluated first, then stored in 'decimal_number'
  • Function call: print(value)
  • Function call: print('Invalid binary number. Please enter a valid binary number (consisting of 0s and 1s).')
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Function call: int(binary, 2)
  • Function call: print(value)
  • Function call: binary_to_decimal('1010')
  • Function call: print(value)
  • Function call: binary_to_decimal('1111')
  • Binary operation: int(...) * expression
  • Binary operation: 2 ** i
  • Function call: sum(value)
  • Function call: int(bit)
  • Function call: enumerate(value)
  • Function call: reversed(binary)
  • Function call: print(value)
  • Function call: binary_to_decimal('1101')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'binary'
  • Function call: binary_to_decimal(binary)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: binary_to_decimal(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'binary_to_decimal' (stored in memory, not executed yet)
  2. Define function 'main' (stored in memory, not executed yet)
  3. If/else: conditional execution
  4. Define function 'binary_to_decimal' (stored in memory, not executed yet)
  5. Execute function call: print(binary_to_decimal(...))
  6. Execute function call: print(binary_to_decimal(...))
  7. Define function 'binary_to_decimal' (stored in memory, not executed yet)
  8. Execute function call: print(binary_to_decimal(...))
  9. Define function 'binary_to_decimal' (stored in memory, not executed yet)
  10. Define function 'binary_to_decimal' (stored in memory, not executed yet)
  11. Assign value to variable: binary
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(binary_to_decimal(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'binary_to_decimal' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'binary_to_decimal' is defined but NOT executed until called
    - 'binary_to_decimal' is defined but NOT executed until called
    - 'binary_to_decimal' is defined but NOT executed until called
    - 'binary_to_decimal' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over binary, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 98:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def digits_string()
    def digits_string()
    def digits_string()
    def digits_string()
    def digits_string()
      Returns a value via 'return' keyword
    def digits_string()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; generator expression
  Functions called: digits_string, input, int, map, print, sum
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: int(number)
  • Function call: print(total)
  • Function call: digits_string()
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: print(total)
  • Function call: digits_string()
  • Function call: input(...).split()
  • Function call: input('Enter numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: map(int, numbers)
  • Function call: digits_string()
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: int(x)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Function call: digits_string()
  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: int(number)
  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: int(number)
  • Function call: print(total)
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: int(number)
  • Function call: print(total)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'digits_string' (stored in memory, not executed yet)
  2. Execute function call: digits_string()
  3. Define function 'digits_string' (stored in memory, not executed yet)
  4. Execute function call: digits_string()
  5. Define function 'digits_string' (stored in memory, not executed yet)
  6. Execute function call: digits_string()
  7. Define function 'digits_string' (stored in memory, not executed yet)
  8. Execute function call: digits_string()
  9. Define function 'digits_string' (stored in memory, not executed yet)
  10. Define function 'digits_string' (stored in memory, not executed yet)
  11. Assign value to variable: user
  12. Assign value to variable: total
  13. For loop: iterate over user
  14. Execute function call: print(total)
  15. Assign value to variable: user
  16. Assign value to variable: total
  17. For loop: iterate over user
  18. Execute function call: print(total)

▸ EXECUTION FLOW

  Function definition phase:
    - 'digits_string' is defined but NOT executed until called
    - 'digits_string' is defined but NOT executed until called
    - 'digits_string' is defined but NOT executed until called
    - 'digits_string' is defined but NOT executed until called
    - 'digits_string' is defined but NOT executed until called
    - 'digits_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - digits_string() is invoked
      → Execution jumps to the function body of 'digits_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - digits_string() is invoked
      → Execution jumps to the function body of 'digits_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - digits_string() is invoked
      → Execution jumps to the function body of 'digits_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - digits_string() is invoked
      → Execution jumps to the function body of 'digits_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 99:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def upper_lower(string)
      Returns a value via 'return' keyword
    def upper_lower(string)
      Returns a value via 'return' keyword
    def upper_lower(string)
      Returns a value via 'return' keyword
    def upper_lower(string)
      Returns a value via 'return' keyword
    def upper_lower(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, ., ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: len, print, sum, upper_lower
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count_upper'
  • Assignment: right side evaluated first, then stored in 'count_lower'
  • Function call: char.isupper()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: upper_lower(string)
  • Function call: sum(value)
  • Function call: char.isupper()
  • Assignment: right side evaluated first, then stored in 'upper'
  • Function call: sum(value)
  • Function call: char.islower()
  • Assignment: right side evaluated first, then stored in 'lower'
  • Function call: print(value)
  • Function call: upper_lower('Hello World! 123')
  • Function call: len(value)
  • Function call: c.isupper()
  • Assignment: right side evaluated first, then stored in 'upper'
  • Function call: len(value)
  • Function call: c.islower()
  • Assignment: right side evaluated first, then stored in 'lower'
  • Function call: print(value)
  • Function call: upper_lower('Python Mastery Pro')
  • Function call: 'lower = {} and upper = {}'.format(count_lower, count_upper)
  • Binary operation: 'lower = %s and upper = %s' % expression
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: upper_lower(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: upper_lower('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'upper_lower' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(upper_lower(...))
  4. Define function 'upper_lower' (stored in memory, not executed yet)
  5. Execute function call: print(upper_lower(...))
  6. Define function 'upper_lower' (stored in memory, not executed yet)
  7. Execute function call: print(upper_lower(...))
  8. Define function 'upper_lower' (stored in memory, not executed yet)
  9. Define function 'upper_lower' (stored in memory, not executed yet)
  10. Assign value to variable: string
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(upper_lower(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'upper_lower' is defined but NOT executed until called
    - 'upper_lower' is defined but NOT executed until called
    - 'upper_lower' is defined but NOT executed until called
    - 'upper_lower' is defined but NOT executed until called
    - 'upper_lower' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 100:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def is_prime(num)
      Returns a value via 'return' keyword
    def sum_of_primes(lower, upper)
      Returns a value via 'return' keyword
    def main()
    def sum_of_primes(lower, upper)
      Returns a value via 'return' keyword
    def sum_of_primes(lower, upper)
      Returns a value via 'return' keyword
    def is_prime(num)
      Returns a value via 'return' keyword
    def is_prime(num)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, *, +, >, !=, (), **, <=, ==, [], and
  Control structures: for loop iteration; generator expression
  Functions called: all, input, int, is_prime, main, print, range, sum, sum_of_primes
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: num ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: num % i
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: upper + 1
  • Function call: range(lower, computed expression)
  • Function call: is_prime(num)
  • Condition branch: evaluates boolean expression first
  • Function call: int(value)
  • Function call: input('Enter the lower limit: ')
  • Assignment: right side evaluated first, then stored in 'lower_limit'
  • Function call: int(value)
  • Function call: input('Enter the upper limit: ')
  • Assignment: right side evaluated first, then stored in 'upper_limit'
  • Function call: sum_of_primes(lower_limit, upper_limit)
  • Assignment: right side evaluated first, then stored in 'prime_sum'
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Function call: main()
  • Binary operation: upper + 1
  • Binary operation: num % i
  • Binary operation: int(...) + 1
  • Binary operation: num ** 0.5
  • Function call: sum(value)
  • Function call: range(lower, computed expression)
  • Function call: all(value)
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Function call: print(value)
  • Function call: sum_of_primes(1, 10)
  • Binary operation: expression * expression
  • Binary operation: upper + 1
  • Assignment: right side evaluated first, then stored in 'sieve'
  • Binary operation: int(...) + 1
  • Binary operation: upper ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Condition branch: evaluates boolean expression first
  • Binary operation: i * i
  • Binary operation: upper + 1
  • Function call: range(computed expression, computed expression, i)
  • Binary operation: upper + 1
  • Function call: sum(value)
  • Function call: range(lower, computed expression)
  • Function call: print(value)
  • Function call: sum_of_primes(10, 50)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: is_prime(num)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_prime(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_prime' (stored in memory, not executed yet)
  2. Define function 'sum_of_primes' (stored in memory, not executed yet)
  3. Define function 'main' (stored in memory, not executed yet)
  4. If/else: conditional execution
  5. Define function 'sum_of_primes' (stored in memory, not executed yet)
  6. Execute function call: print(sum_of_primes(...))
  7. Define function 'sum_of_primes' (stored in memory, not executed yet)
  8. Execute function call: print(sum_of_primes(...))
  9. Define function 'is_prime' (stored in memory, not executed yet)
  10. Define function 'is_prime' (stored in memory, not executed yet)
  11. Assign value to variable: num
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(is_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_prime' is defined but NOT executed until called
    - 'sum_of_primes' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'sum_of_primes' is defined but NOT executed until called
    - 'sum_of_primes' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 101:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def power(base, exp)
      Returns a value via 'return' keyword
    def power(base, exp)
      Returns a value via 'return' keyword
    def power(base, exp)
      Returns a value via 'return' keyword
    def power(base, exp)
      Returns a value via 'return' keyword
    def power(base, exp)
      Returns a value via 'return' keyword
    def power(base, exp)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: *, -, ., (), **, ==
  Control structures: if/else conditional branching; for loop iteration
  Functions called: float, input, int, power, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: base * power(...)
  • Binary operation: exp - 1
  • Function call: power(base, computed expression)
  • Function call: float(value)
  • Function call: input('Enter the base number: ')
  • Assignment: right side evaluated first, then stored in 'base'
  • Function call: int(value)
  • Function call: input('Enter the exponent: ')
  • Assignment: right side evaluated first, then stored in 'exp'
  • Function call: power(base, exp)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Binary operation: base ** exp
  • Function call: print(value)
  • Function call: power(2, 5)
  • Function call: math.pow(base, exp)
  • Function call: print(value)
  • Function call: power(3, 4)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(exp)
  • Function call: print(value)
  • Function call: power(2, 8)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'base'
  • Assignment: right side evaluated first, then stored in 'exp'
  • Function call: power(base, exp)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: power(10, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'power' (stored in memory, not executed yet)
  2. Assign value to variable: base
  3. Assign value to variable: exp
  4. Assign value to variable: result
  5. Execute function call: print(value)
  6. Define function 'power' (stored in memory, not executed yet)
  7. Execute function call: print(power(...))
  8. Define function 'power' (stored in memory, not executed yet)
  9. Execute function call: print(power(...))
  10. Define function 'power' (stored in memory, not executed yet)
  11. Execute function call: print(power(...))
  12. Define function 'power' (stored in memory, not executed yet)
  13. Define function 'power' (stored in memory, not executed yet)
  14. Assign value to variable: base
  15. Assign value to variable: exp
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(power(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 102:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def is_anagram()
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., !=, (), ==
  Control structures: for loop iteration
  Functions called: Counter, input, is_anagram, len, print, set, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).replace(' ', '')
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: input(...).replace(' ', '')
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: sorted(string1)
  • Assignment: right side evaluated first, then stored in 'sort1'
  • Function call: sorted(string2)
  • Assignment: right side evaluated first, then stored in 'sort2'
  • Condition branch: evaluates boolean expression first
  • Function call: print('this is anagram')
  • Function call: print('this is not anagram')
  • Function call: is_anagram()
  • Function call: string1.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: string2.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: sorted(string1)
  • Function call: sorted(string2)
  • Assignment: right side evaluated first, then stored in 'string1'
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: print(value)
  • Function call: is_anagram(string1, string2)
  • Function call: Counter(value)
  • Function call: Counter(value)
  • Function call: string1.replace(...).lower()
  • Function call: string2.replace(...).lower()
  • Function call: string1.replace(' ', '')
  • Function call: string2.replace(' ', '')
  • Function call: print(value)
  • Function call: is_anagram('Listen', 'Silent')
  • Function call: string1.replace(...).lower()
  • Function call: string2.replace(...).lower()
  • Function call: string1.replace(' ', '')
  • Function call: string2.replace(' ', '')
  • Function call: len(s1)
  • Function call: len(s2)
  • Condition branch: evaluates boolean expression first
  • Function call: set(s1)
  • Function call: s1.count(char)
  • Function call: s2.count(char)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_anagram('hello', 'olleh')
  • Assignment: right side evaluated first, then stored in 'string1'
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: is_anagram(string1, string2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_anagram('world', 'python')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_anagram' (stored in memory, not executed yet)
  2. Execute function call: is_anagram()
  3. Define function 'is_anagram' (stored in memory, not executed yet)
  4. Assign value to variable: string1
  5. Assign value to variable: string2
  6. Execute function call: print(is_anagram(...))
  7. Define function 'is_anagram' (stored in memory, not executed yet)
  8. Execute function call: print(is_anagram(...))
  9. Define function 'is_anagram' (stored in memory, not executed yet)
  10. Execute function call: print(is_anagram(...))
  11. Assign value to variable: string1
  12. Assign value to variable: string2
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(is_anagram(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
  Runtime execution (top-level code):
    - is_anagram() is invoked
      → Execution jumps to the function body of 'is_anagram'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over set(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 104:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_perfect_square(num)
      Returns a value via 'return' keyword
    def is_perfect_square(num)
      Returns a value via 'return' keyword
    def is_perfect_square(num)
      Returns a value via 'return' keyword
    def is_perfect_square(num)
      Returns a value via 'return' keyword
    def is_perfect_square(num)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: *, ., <, (), **, ==
  Functions called: input, int, is_perfect_square, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('enter number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: math.isqrt(user)
  • Assignment: right side evaluated first, then stored in 'root'
  • Binary operation: root * root
  • Function call: print(value)
  • Function call: math.isqrt(num)
  • Assignment: right side evaluated first, then stored in 'root'
  • Binary operation: root * root
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: is_perfect_square(num)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Binary operation: num ** 0.5
  • Function call: int(computed expression)
  • Assignment: right side evaluated first, then stored in 'root'
  • Binary operation: root * root
  • Function call: print(value)
  • Function call: is_perfect_square(16)
  • Function call: print(value)
  • Function call: is_perfect_square(14)
  • Condition branch: evaluates boolean expression first
  • Function call: int(value)
  • Function call: math.sqrt(num)
  • Assignment: right side evaluated first, then stored in 'root'
  • Binary operation: root * root
  • Function call: print(value)
  • Function call: is_perfect_square(25)
  • Binary operation: root * root
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: root * root
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: is_perfect_square(num)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_perfect_square(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: math
  2. Assign value to variable: user
  3. Assign value to variable: root
  4. Execute function call: print(value)
  5. Define function 'is_perfect_square' (stored in memory, not executed yet)
  6. Assign value to variable: num
  7. If/else: conditional execution
  8. Define function 'is_perfect_square' (stored in memory, not executed yet)
  9. Execute function call: print(is_perfect_square(...))
  10. Execute function call: print(is_perfect_square(...))
  11. Define function 'is_perfect_square' (stored in memory, not executed yet)
  12. Execute function call: print(is_perfect_square(...))
  13. Define function 'is_perfect_square' (stored in memory, not executed yet)
  14. Define function 'is_perfect_square' (stored in memory, not executed yet)
  15. Assign value to variable: num
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(is_perfect_square(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_perfect_square' is defined but NOT executed until called
    - 'is_perfect_square' is defined but NOT executed until called
    - 'is_perfect_square' is defined but NOT executed until called
    - 'is_perfect_square' is defined but NOT executed until called
    - 'is_perfect_square' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 105:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def all_odd_numbers(numbers)
      Returns a value via 'return' keyword
    def all_odd_numbers(numbers)
      Returns a value via 'return' keyword
    def all_odd_numbers(numbers)
      Returns a value via 'return' keyword
    def all_odd_numbers(numbers)
      Returns a value via 'return' keyword
    def all_odd_numbers(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, !=, ()
  Control structures: if/else conditional branching; for loop iteration; generator expression; lambda expression
  Functions called: all_odd_numbers, filter, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: all_odd_numbers(lst)
  • Binary operation: n % 2
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: all_odd_numbers(value)
  • Binary operation: x % 2
  • Function call: sum(value)
  • Function call: filter(value, numbers)
  • Function call: print(value)
  • Function call: all_odd_numbers(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: all_odd_numbers(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: all_odd_numbers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'all_odd_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(all_odd_numbers(...))
  4. Define function 'all_odd_numbers' (stored in memory, not executed yet)
  5. Execute function call: print(all_odd_numbers(...))
  6. Define function 'all_odd_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(all_odd_numbers(...))
  8. Define function 'all_odd_numbers' (stored in memory, not executed yet)
  9. Define function 'all_odd_numbers' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(all_odd_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'all_odd_numbers' is defined but NOT executed until called
    - 'all_odd_numbers' is defined but NOT executed until called
    - 'all_odd_numbers' is defined but NOT executed until called
    - 'all_odd_numbers' is defined but NOT executed until called
    - 'all_odd_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 106:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_of_odd_numbers()
    def sum_of_all_odd()
    def sum_of_odd_numbers()
    def sum_of_all_odd()
    def sum_of_odd_numbers()
      Returns a value via 'return' keyword
    def sum_of_odd_numbers()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, ., !=, ()
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: filter, input, int, list, map, print, sum, sum_of_all_odd, sum_of_odd_numbers
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('Enter a list of numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Assignment: right side evaluated first, then stored in 'odd_sum'
  • Binary operation: num % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print('The sum of all odd numbers is:', odd_sum)
  • Function call: sum_of_odd_numbers()
  • Function call: input(...).split()
  • Function call: input('Enter numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print(total)
  • Function call: sum_of_all_odd()
  • Function call: int(x)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Binary operation: n % 2
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: sum_of_odd_numbers()
  • Function call: list(value)
  • Function call: map(int, value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Binary operation: x % 2
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: filter(value, numbers)
  • Function call: sum_of_all_odd()
  • Function call: input('Enter a list of numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Assignment: right side evaluated first, then stored in 'odd_sum'
  • Binary operation: num % 2
  • Condition branch: evaluates boolean expression first
  • Function call: input('Enter a list of numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Assignment: right side evaluated first, then stored in 'odd_sum'
  • Binary operation: num % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Assignment: right side evaluated first, then stored in 'odd_sum'
  • Binary operation: num % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print('The sum of all odd numbers is:', odd_sum)
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Assignment: right side evaluated first, then stored in 'odd_sum'
  • Binary operation: num % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print('The sum of all odd numbers is:', odd_sum)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_odd_numbers' (stored in memory, not executed yet)
  2. Execute function call: sum_of_odd_numbers()
  3. Define function 'sum_of_all_odd' (stored in memory, not executed yet)
  4. Execute function call: sum_of_all_odd()
  5. Define function 'sum_of_odd_numbers' (stored in memory, not executed yet)
  6. Execute function call: sum_of_odd_numbers()
  7. Define function 'sum_of_all_odd' (stored in memory, not executed yet)
  8. Execute function call: sum_of_all_odd()
  9. Define function 'sum_of_odd_numbers' (stored in memory, not executed yet)
  10. Define function 'sum_of_odd_numbers' (stored in memory, not executed yet)
  11. Assign value to variable: user_input
  12. Assign value to variable: split_numbers
  13. Assign value to variable: int_numbers
  14. For loop: iterate over split_numbers
  15. Assign value to variable: odd_sum
  16. For loop: iterate over int_numbers
  17. Execute function call: print('The sum of all odd numbers is:', odd_sum)
  18. Assign value to variable: user_input
  19. Assign value to variable: split_numbers
  20. Assign value to variable: int_numbers
  21. For loop: iterate over split_numbers
  22. Assign value to variable: odd_sum
  23. For loop: iterate over int_numbers
  24. Execute function call: print('The sum of all odd numbers is:', odd_sum)

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_odd_numbers' is defined but NOT executed until called
    - 'sum_of_all_odd' is defined but NOT executed until called
    - 'sum_of_odd_numbers' is defined but NOT executed until called
    - 'sum_of_all_odd' is defined but NOT executed until called
    - 'sum_of_odd_numbers' is defined but NOT executed until called
    - 'sum_of_odd_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - sum_of_odd_numbers() is invoked
      → Execution jumps to the function body of 'sum_of_odd_numbers'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - sum_of_all_odd() is invoked
      → Execution jumps to the function body of 'sum_of_all_odd'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - sum_of_odd_numbers() is invoked
      → Execution jumps to the function body of 'sum_of_odd_numbers'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - sum_of_all_odd() is invoked
      → Execution jumps to the function body of 'sum_of_all_odd'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 107:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def common_char()
      Returns a value via 'return' keyword
    def common_char(string1, string2)
      Returns a value via 'return' keyword
    def common_char(string1, string2)
      Returns a value via 'return' keyword
    def common_char(string1, string2)
      Returns a value via 'return' keyword
    def common_char()
      Returns a value via 'return' keyword
    def common_char()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: &, ., !=, (), in, and, not in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: common_char, input, len, list, map, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('Type 2 strings separated by a space: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: len(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: print('Please enter exactly two strings.')
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(char)
  • Function call: print(value)
  • Function call: common_char()
  • Assignment: right side evaluated first, then stored in 'characters'
  • Condition branch: evaluates boolean expression first
  • Function call: characters.append(char)
  • Function call: input('Enter the first string: ')
  • Assignment: right side evaluated first, then stored in 'name1'
  • Function call: input('Enter the second string: ')
  • Assignment: right side evaluated first, then stored in 'name2'
  • Function call: print(value)
  • Function call: common_char(name1, name2)
  • Binary operation: set(...) & set(...)
  • Function call: list(computed expression)
  • Function call: set(string1)
  • Function call: set(string2)
  • Function call: print(value)
  • Function call: common_char('hello', 'world')
  • Function call: list(value)
  • Function call: dict.fromkeys(value)
  • Function call: print(value)
  • Function call: common_char('programming', 'python')
  • Function call: list(value)
  • Function call: map(value, str1)
  • Assignment: right side evaluated first, then stored in 'string1'
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: common_char(string1, string2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: common_char('world', 'python')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common_char' (stored in memory, not executed yet)
  2. Execute function call: print(common_char(...))
  3. Define function 'common_char' (stored in memory, not executed yet)
  4. Assign value to variable: name1
  5. Assign value to variable: name2
  6. Execute function call: print(common_char(...))
  7. Define function 'common_char' (stored in memory, not executed yet)
  8. Execute function call: print(common_char(...))
  9. Define function 'common_char' (stored in memory, not executed yet)
  10. Execute function call: print(common_char(...))
  11. Define function 'common_char' (stored in memory, not executed yet)
  12. Define function 'common_char' (stored in memory, not executed yet)
  13. Assign value to variable: string1
  14. Assign value to variable: string2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(common_char(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'common_char' is defined but NOT executed until called
    - 'common_char' is defined but NOT executed until called
    - 'common_char' is defined but NOT executed until called
    - 'common_char' is defined but NOT executed until called
    - 'common_char' is defined but NOT executed until called
    - 'common_char' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over str1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string1, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 108:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def remove_duplicates()
    def remove_duplicates()
    def remove_duplicates()
    def remove_duplicates()
    def remove_duplicates()
      Returns a value via 'return' keyword
    def remove_duplicates()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in, or, not, not in
  Control structures: for loop iteration; list comprehension
  Functions called: input, print, remove_duplicates, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique_user'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_user.add(char)
  • Function call: print(value)
  • Function call: ''.join(unique_user)
  • Function call: remove_duplicates()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique_user'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_user.add(char)
  • Function call: result.append(char)
  • Function call: print(value)
  • Function call: ''.join(result)
  • Function call: remove_duplicates()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: print(value)
  • Function call: ''.join(value)
  • Function call: dict.fromkeys(user)
  • Function call: remove_duplicates()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: ''.join(value)
  • Function call: seen.add(c)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: remove_duplicates()
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique_user'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_user.add(char)
  • Function call: ''.join(unique_user)
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique_user'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_user.add(char)
  • Function call: ''.join(unique_user)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique_user'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_user.add(char)
  • Function call: print(value)
  • Function call: ''.join(unique_user)
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique_user'
  • Condition branch: evaluates boolean expression first
  • Function call: unique_user.add(char)
  • Function call: print(value)
  • Function call: ''.join(unique_user)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Execute function call: remove_duplicates()
  3. Define function 'remove_duplicates' (stored in memory, not executed yet)
  4. Execute function call: remove_duplicates()
  5. Define function 'remove_duplicates' (stored in memory, not executed yet)
  6. Execute function call: remove_duplicates()
  7. Define function 'remove_duplicates' (stored in memory, not executed yet)
  8. Execute function call: remove_duplicates()
  9. Define function 'remove_duplicates' (stored in memory, not executed yet)
  10. Define function 'remove_duplicates' (stored in memory, not executed yet)
  11. Assign value to variable: user
  12. Assign value to variable: unique_user
  13. For loop: iterate over user
  14. Execute function call: print(''.join(...))
  15. Assign value to variable: user
  16. Assign value to variable: unique_user
  17. For loop: iterate over user
  18. Execute function call: print(''.join(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - remove_duplicates() is invoked
      → Execution jumps to the function body of 'remove_duplicates'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - remove_duplicates() is invoked
      → Execution jumps to the function body of 'remove_duplicates'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - remove_duplicates() is invoked
      → Execution jumps to the function body of 'remove_duplicates'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - remove_duplicates() is invoked
      → Execution jumps to the function body of 'remove_duplicates'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 109:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_armstrong(number)
      Returns a value via 'return' keyword
    def is_armstrong(number)
      Returns a value via 'return' keyword
    def is_armstrong(number)
      Returns a value via 'return' keyword
    def is_armstrong(number)
      Returns a value via 'return' keyword
    def is_armstrong(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: %, ., >, (), **, ==
  Control structures: for loop iteration; while loop; list comprehension; generator expression
  Functions called: input, int, is_armstrong, len, print, str, sum
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'digits'
  • Function call: str(number)
  • Function call: digits.append(value)
  • Function call: int(digit)
  • Function call: len(digits)
  • Assignment: right side evaluated first, then stored in 'power'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: digit ** power
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_armstrong(number)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Function call: print(number)
  • Function call: int(d)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'digits'
  • Function call: len(digits)
  • Assignment: right side evaluated first, then stored in 'power'
  • Binary operation: d ** power
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: is_armstrong(153)
  • Function call: print(value)
  • Function call: is_armstrong(9474)
  • Function call: print(value)
  • Function call: is_armstrong(123)
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: len(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'power'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: num % 10
  • Assignment: right side evaluated first, then stored in 'digit'
  • Binary operation: digit ** power
  • Function call: print(value)
  • Function call: is_armstrong(370)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_armstrong(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_armstrong(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_armstrong' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. If/else: conditional execution
  4. Execute function call: print(number)
  5. Define function 'is_armstrong' (stored in memory, not executed yet)
  6. Execute function call: print(is_armstrong(...))
  7. Execute function call: print(is_armstrong(...))
  8. Execute function call: print(is_armstrong(...))
  9. Define function 'is_armstrong' (stored in memory, not executed yet)
  10. Execute function call: print(is_armstrong(...))
  11. Define function 'is_armstrong' (stored in memory, not executed yet)
  12. Define function 'is_armstrong' (stored in memory, not executed yet)
  13. Assign value to variable: number
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(is_armstrong(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_armstrong' is defined but NOT executed until called
    - 'is_armstrong' is defined but NOT executed until called
    - 'is_armstrong' is defined but NOT executed until called
    - 'is_armstrong' is defined but NOT executed until called
    - 'is_armstrong' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over str(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over digits, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 111:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def second_smallest_element()
      Returns a value via 'return' keyword
    def second_smallest_element(elements)
      Returns a value via 'return' keyword
    def second_smallest_element(elements)
      Returns a value via 'return' keyword
    def second_smallest_element(elements)
      Returns a value via 'return' keyword
    def second_smallest_element(elements)
      Returns a value via 'return' keyword
    def second_smallest_element()
      Returns a value via 'return' keyword
    def second_smallest_element()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., <, (), >=, []
  Control structures: for loop iteration
  Functions called: input, int, len, max, min, print, second_smallest_element, set, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('Enter numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: user_input.split()
  • Function call: int_numbers.append(value)
  • Function call: int(num_str)
  • Function call: sorted(int_numbers)
  • Assignment: right side evaluated first, then stored in 'sorted_numbers'
  • Function call: len(sorted_numbers)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: second_smallest_element()
  • Function call: len(elements)
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(elements)
  • Assignment: right side evaluated first, then stored in 'sorted_elements'
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: second_smallest_element(arg)
  • Function call: sorted(value)
  • Function call: set(elements)
  • Assignment: right side evaluated first, then stored in 'unique'
  • Function call: len(unique)
  • Function call: print(value)
  • Function call: second_smallest_element(value)
  • Function call: heapq.nsmallest(2, value)
  • Function call: set(elements)
  • Function call: print(value)
  • Function call: second_smallest_element(value)
  • Function call: min(elements)
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Function call: max(elements)
  • Assignment: right side evaluated first, then stored in 'second'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: print(value)
  • Function call: second_smallest_element(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'elements'
  • Function call: second_smallest_element(elements)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: second_smallest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'second_smallest_element' (stored in memory, not executed yet)
  2. Execute function call: print(second_smallest_element(...))
  3. Define function 'second_smallest_element' (stored in memory, not executed yet)
  4. Assign value to variable: arg
  5. Execute function call: print(second_smallest_element(...))
  6. Define function 'second_smallest_element' (stored in memory, not executed yet)
  7. Execute function call: print(second_smallest_element(...))
  8. Define function 'second_smallest_element' (stored in memory, not executed yet)
  9. Execute function call: print(second_smallest_element(...))
  10. Define function 'second_smallest_element' (stored in memory, not executed yet)
  11. Execute function call: print(second_smallest_element(...))
  12. Define function 'second_smallest_element' (stored in memory, not executed yet)
  13. Define function 'second_smallest_element' (stored in memory, not executed yet)
  14. Assign value to variable: elements
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(second_smallest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user_input.split(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 112:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def generate_permutations(string)
      Returns a value via 'return' keyword
    def generate_permutations(string)
      Returns a value via 'return' keyword
    def generate_permutations(string)
      Returns a value via 'return' keyword
    def generate_permutations(string)
      Returns a value via 'return' keyword
    def generate_permutations(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], not
  Control structures: if/else conditional branching; for loop iteration; list comprehension
  Functions called: backtrack, generate_permutations, input, len, list, permutations, print, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: permutations(string)
  • Function call: result.append(value)
  • Function call: ''.join(p)
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: generate_permutations(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print('Permutations:', result)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: itertools.permutations(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: list(result)
  • Function call: ''.join(p)
  • Function call: permutations(string)
  • Function call: print(value)
  • Function call: generate_permutations('abc')
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(path)
  • Function call: range(value)
  • Function call: len(remaining)
  • Binary operation: path + expression
  • Binary operation: expression + expression
  • Binary operation: i + 1
  • Function call: backtrack(computed expression, computed expression)
  • Function call: backtrack('', string)
  • Function call: print(value)
  • Function call: generate_permutations('ab')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: generate_permutations(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: generate_permutations('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import from itertools: permutations
  2. Define function 'generate_permutations' (stored in memory, not executed yet)
  3. Assign value to variable: string
  4. Assign value to variable: result
  5. Execute function call: print('Permutations:', result)
  6. Import module: itertools
  7. Assign value to variable: string
  8. Assign value to variable: result
  9. Execute function call: print(list(...))
  10. Define function 'generate_permutations' (stored in memory, not executed yet)
  11. Execute function call: print(generate_permutations(...))
  12. Define function 'generate_permutations' (stored in memory, not executed yet)
  13. Execute function call: print(generate_permutations(...))
  14. Define function 'generate_permutations' (stored in memory, not executed yet)
  15. Define function 'generate_permutations' (stored in memory, not executed yet)
  16. Assign value to variable: string
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(generate_permutations(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'generate_permutations' is defined but NOT executed until called
    - 'generate_permutations' is defined but NOT executed until called
    - 'generate_permutations' is defined but NOT executed until called
    - 'generate_permutations' is defined but NOT executed until called
    - 'generate_permutations' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over permutations(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 114:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def max_of_three(lst)
      Returns a value via 'return' keyword
    def main(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(lst)
      Returns a value via 'return' keyword
    def max_of_three(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: -, >, (), >=, [], and
  Control structures: for loop iteration; lambda expression
  Functions called: main, max, max_of_three, print, reduce, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: max_of_three(lst)
  • Function call: max(a, b, c)
  • Function call: main(56, 8, 445)
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Function call: max_of_three()
  • Function call: sorted(value)
  • Function call: print(value)
  • Function call: max_of_three(10, 25, 15)
  • Function call: print(value)
  • Function call: max_of_three(100, 50, 75)
  • Function call: reduce(value, value)
  • Function call: print(value)
  • Function call: max_of_three(7, 3, 11)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: max_of_three(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_of_three(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_three' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: max_of_three(lst)
  4. Define function 'main' (stored in memory, not executed yet)
  5. Execute function call: main(56, 8, 445)
  6. Define function 'max_of_three' (stored in memory, not executed yet)
  7. Execute function call: max_of_three()
  8. Define function 'max_of_three' (stored in memory, not executed yet)
  9. Execute function call: print(max_of_three(...))
  10. Define function 'max_of_three' (stored in memory, not executed yet)
  11. Execute function call: print(max_of_three(...))
  12. Define function 'max_of_three' (stored in memory, not executed yet)
  13. Execute function call: print(max_of_three(...))
  14. Define function 'max_of_three' (stored in memory, not executed yet)
  15. Define function 'max_of_three' (stored in memory, not executed yet)
  16. Assign value to variable: lst
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(max_of_three(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_three' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
  Runtime execution (top-level code):
    - max_of_three() is invoked
      → Execution jumps to the function body of 'max_of_three'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - max_of_three() is invoked
      → Execution jumps to the function body of 'max_of_three'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 115:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def length_string(string)
      Returns a value via 'return' keyword
    def length_list(string)
      Returns a value via 'return' keyword
    def length_string(s)
      Returns a value via 'return' keyword
    def length_string(s)
      Returns a value via 'return' keyword
    def length_string(s)
      Returns a value via 'return' keyword
    def length_string(string)
      Returns a value via 'return' keyword
    def length_string(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, pass, return
  Operators used: +, (), ==, []
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: enumerate, length_list, length_string, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: print(value)
  • Function call: length_string(name)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: length_list(lst)
  • Condition branch: evaluates boolean expression first
  • Binary operation: 1 + length_string(...)
  • Function call: length_string(value)
  • Function call: print(value)
  • Function call: length_string('hello')
  • Function call: enumerate(s)
  • Binary operation: i + 1
  • Function call: print(value)
  • Function call: length_string('python')
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: length_string('world')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: length_string(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: length_string('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'length_string' (stored in memory, not executed yet)
  2. Assign value to variable: name
  3. Execute function call: print(length_string(...))
  4. Define function 'length_list' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(length_list(...))
  7. Define function 'length_string' (stored in memory, not executed yet)
  8. Execute function call: print(length_string(...))
  9. Define function 'length_string' (stored in memory, not executed yet)
  10. Execute function call: print(length_string(...))
  11. Define function 'length_string' (stored in memory, not executed yet)
  12. Execute function call: print(length_string(...))
  13. Define function 'length_string' (stored in memory, not executed yet)
  14. Define function 'length_string' (stored in memory, not executed yet)
  15. Assign value to variable: string
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(length_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'length_string' is defined but NOT executed until called
    - 'length_list' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
    - 'length_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 117:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def translate(text)
      Returns a value via 'return' keyword
    def translate(text)
      Returns a value via 'return' keyword
    def translate(text)
      Returns a value via 'return' keyword
    def translate(text)
      Returns a value via 'return' keyword
    def translate(text)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), and, not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension
  Functions called: input, print, set, translate
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'translated'
  • Function call: char.isalpha()
  • Condition branch: evaluates boolean expression first
  • Binary operation: expression + char
  • Binary operation: char + 'o'
  • Function call: input('Enter a text: ')
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: translate(text)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: set('aeiouAEIOU')
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Binary operation: expression + c
  • Binary operation: c + 'o'
  • Function call: ''.join(value)
  • Function call: c.isalpha()
  • Function call: print(value)
  • Function call: translate('this is fun')
  • Function call: m.group(0)
  • Assignment: right side evaluated first, then stored in 'c'
  • Binary operation: expression + c
  • Binary operation: c + 'o'
  • Function call: re.sub('[b-df-hj-np-tv-zB-DF-HJ-NP-TV-Z]', replace_consonant, text)
  • Function call: print(value)
  • Function call: translate('hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: translate(text)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: translate('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'translate' (stored in memory, not executed yet)
  2. Assign value to variable: text
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'translate' (stored in memory, not executed yet)
  6. Execute function call: print(translate(...))
  7. Define function 'translate' (stored in memory, not executed yet)
  8. Execute function call: print(translate(...))
  9. Define function 'translate' (stored in memory, not executed yet)
  10. Define function 'translate' (stored in memory, not executed yet)
  11. Assign value to variable: text
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(translate(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'translate' is defined but NOT executed until called
    - 'translate' is defined but NOT executed until called
    - 'translate' is defined but NOT executed until called
    - 'translate' is defined but NOT executed until called
    - 'translate' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over text, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 118:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def sum(numbers)
      Returns a value via 'return' keyword
    def multiply(numbers)
      Returns a value via 'return' keyword
    def sum(numbers)
      Returns a value via 'return' keyword
    def multiply(numbers)
      Returns a value via 'return' keyword
    def sum(numbers)
      Returns a value via 'return' keyword
    def multiply(numbers)
      Returns a value via 'return' keyword
    def sum(numbers)
      Returns a value via 'return' keyword
    def sum(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, +, ., ()
  Control structures: for loop iteration; lambda expression
  Functions called: multiply, print, reduce, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print('Sum:', value)
  • Function call: sum(numbers)
  • Function call: print('Multiply:', value)
  • Function call: multiply(numbers)
  • Function call: builtins.sum(numbers)
  • Binary operation: x * y
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: multiply(value)
  • Binary operation: x + y
  • Function call: reduce(value, numbers)
  • Binary operation: x * y
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: multiply(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: sum(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum' (stored in memory, not executed yet)
  2. Define function 'multiply' (stored in memory, not executed yet)
  3. Assign value to variable: numbers
  4. Execute function call: print('Sum:', sum(...))
  5. Execute function call: print('Multiply:', multiply(...))
  6. Define function 'sum' (stored in memory, not executed yet)
  7. Import module: builtins
  8. Define function 'multiply' (stored in memory, not executed yet)
  9. Execute function call: print(sum(...))
  10. Execute function call: print(multiply(...))
  11. Define function 'sum' (stored in memory, not executed yet)
  12. Define function 'multiply' (stored in memory, not executed yet)
  13. Execute function call: print(sum(...))
  14. Execute function call: print(multiply(...))
  15. Define function 'sum' (stored in memory, not executed yet)
  16. Define function 'sum' (stored in memory, not executed yet)
  17. Assign value to variable: numbers
  18. Assign value to variable: result
  19. Execute function call: print(result)
  20. Execute function call: print(sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum' is defined but NOT executed until called
    - 'multiply' is defined but NOT executed until called
    - 'sum' is defined but NOT executed until called
    - 'multiply' is defined but NOT executed until called
    - 'sum' is defined but NOT executed until called
    - 'multiply' is defined but NOT executed until called
    - 'sum' is defined but NOT executed until called
    - 'sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 119:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def reverse(string)
      Returns a value via 'return' keyword
    def reverse(string)
      Returns a value via 'return' keyword
    def reverse(string)
      Returns a value via 'return' keyword
    def reverse(string)
      Returns a value via 'return' keyword
    def reverse(string)
      Returns a value via 'return' keyword
    def reverse(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, -, ., (), <=, []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: input, len, print, reverse, reversed
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'reversed_string'
  • Binary operation: char + reversed_string
  • Assignment: right side evaluated first, then stored in 'reversed_string'
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: reverse(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse('I am testing')
  • Function call: ''.join(value)
  • Function call: reversed(string)
  • Function call: print(value)
  • Function call: reverse('hello')
  • Function call: len(string)
  • Condition branch: evaluates boolean expression first
  • Binary operation: reverse(...) + expression
  • Function call: reverse(value)
  • Function call: print(value)
  • Function call: reverse('python')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: reverse(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'reverse' (stored in memory, not executed yet)
  6. Execute function call: print(reverse(...))
  7. Define function 'reverse' (stored in memory, not executed yet)
  8. Execute function call: print(reverse(...))
  9. Define function 'reverse' (stored in memory, not executed yet)
  10. Execute function call: print(reverse(...))
  11. Define function 'reverse' (stored in memory, not executed yet)
  12. Define function 'reverse' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(reverse(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 120:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def palindromes(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def palindromes(string)
      Returns a value via 'return' keyword
    def palindromes(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: +, -, <, !=, (), //, ==, []
  Control structures: for loop iteration; while loop; generator expression
  Functions called: all, is_palindrome, len, palindromes, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: palindromes(string)
  • Function call: print(value)
  • Function call: is_palindrome('radar')
  • Function call: print(value)
  • Function call: is_palindrome('hello')
  • Binary operation: len(...) - 1
  • Function call: len(string)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_palindrome('level')
  • Binary operation: len(...) // 2
  • Binary operation: i + 1
  • Function call: all(value)
  • Function call: range(computed expression)
  • Function call: len(string)
  • Function call: print(value)
  • Function call: is_palindrome('madam')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: palindromes(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: palindromes('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'palindromes' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(palindromes(...))
  4. Define function 'is_palindrome' (stored in memory, not executed yet)
  5. Execute function call: print(is_palindrome(...))
  6. Execute function call: print(is_palindrome(...))
  7. Define function 'is_palindrome' (stored in memory, not executed yet)
  8. Execute function call: print(is_palindrome(...))
  9. Define function 'is_palindrome' (stored in memory, not executed yet)
  10. Execute function call: print(is_palindrome(...))
  11. Define function 'palindromes' (stored in memory, not executed yet)
  12. Define function 'palindromes' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(palindromes(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'palindromes' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'palindromes' is defined but NOT executed until called
    - 'palindromes' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 121:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
    def is_palindrome(string)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return, while
  Operators used: -, ., <, !=, (), ==, []
  Control structures: while loop
  Functions called: is_palindrome, len, print, reversed
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: print(value)
  • Function call: is_palindrome(name)
  • Binary operation: len(...) - 1
  • Function call: len(string)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_palindrome('racecar')
  • Function call: ''.join(value)
  • Function call: reversed(string)
  • Function call: print(value)
  • Function call: is_palindrome('madam')
  • Function call: print(value)
  • Function call: is_palindrome('hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: is_palindrome(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Assign value to variable: name
  3. Execute function call: print(is_palindrome(...))
  4. Define function 'is_palindrome' (stored in memory, not executed yet)
  5. Execute function call: print(is_palindrome(...))
  6. Define function 'is_palindrome' (stored in memory, not executed yet)
  7. Execute function call: print(is_palindrome(...))
  8. Execute function call: print(is_palindrome(...))
  9. Define function 'is_palindrome' (stored in memory, not executed yet)
  10. Define function 'is_palindrome' (stored in memory, not executed yet)
  11. Assign value to variable: string
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 122:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def is_member(lst, x)
      Returns a value via 'return' keyword
    def is_member(x, a)
      Returns a value via 'return' keyword
    def is_member(x, a)
      Returns a value via 'return' keyword
    def is_member(x, a)
      Returns a value via 'return' keyword
    def is_member(lst, x)
      Returns a value via 'return' keyword
    def is_member(lst, x)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), ==, [], in, or, not
  Control structures: for loop iteration; generator expression
  Functions called: any, input, is_member, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: print(value)
  • Function call: is_member(lst, x)
  • Condition branch: evaluates boolean expression first
  • Function call: input('Enter a value: ')
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: input(...).split(',')
  • Function call: input('Enter a list of values (comma-separated): ')
  • Assignment: right side evaluated first, then stored in 'a'
  • Function call: is_member(x, a)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: any(value)
  • Function call: print(value)
  • Function call: is_member(3, value)
  • Function call: print(value)
  • Function call: is_member(10, value)
  • Condition branch: evaluates boolean expression first
  • Function call: is_member(x, value)
  • Function call: print(value)
  • Function call: is_member('a', value)
  • Function call: print(value)
  • Function call: is_member('z', value)
  • Condition branch: evaluates boolean expression first
  • Function call: any(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: is_member(lst, x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_member(value, 3)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_member' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: x
  4. Execute function call: print(is_member(...))
  5. Define function 'is_member' (stored in memory, not executed yet)
  6. Assign value to variable: x
  7. Assign value to variable: a
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Define function 'is_member' (stored in memory, not executed yet)
  11. Execute function call: print(is_member(...))
  12. Execute function call: print(is_member(...))
  13. Define function 'is_member' (stored in memory, not executed yet)
  14. Execute function call: print(is_member(...))
  15. Execute function call: print(is_member(...))
  16. Define function 'is_member' (stored in memory, not executed yet)
  17. Define function 'is_member' (stored in memory, not executed yet)
  18. Assign value to variable: lst
  19. Assign value to variable: x
  20. Assign value to variable: result
  21. Execute function call: print(result)
  22. Execute function call: print(is_member(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_member' is defined but NOT executed until called
    - 'is_member' is defined but NOT executed until called
    - 'is_member' is defined but NOT executed until called
    - 'is_member' is defined but NOT executed until called
    - 'is_member' is defined but NOT executed until called
    - 'is_member' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over a, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 123:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def overlapping(list1, list2)
      Returns a value via 'return' keyword
    def is_member(element, lst)
      Returns a value via 'return' keyword
    def overlapping(list1, list2)
      Returns a value via 'return' keyword
    def overlapping(list1, list2)
      Returns a value via 'return' keyword
    def overlapping(list1, list2)
      Returns a value via 'return' keyword
    def overlapping(list1, list2)
      Returns a value via 'return' keyword
    def overlapping(list1, list2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: &, (), ==, in
  Control structures: for loop iteration; generator expression
  Functions called: any, bool, is_member, overlapping, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'list1'
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: print(value)
  • Function call: overlapping(list1, list2)
  • Assignment: right side evaluated first, then stored in 'list1'
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: print(value)
  • Function call: overlapping(list1, list2)
  • Condition branch: evaluates boolean expression first
  • Function call: is_member(item1, list2)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'list1'
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: print(value)
  • Function call: overlapping(list1, list2)
  • Assignment: right side evaluated first, then stored in 'list1'
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: print(value)
  • Function call: overlapping(list1, list2)
  • Binary operation: set(...) & set(...)
  • Function call: bool(computed expression)
  • Function call: set(list1)
  • Function call: set(list2)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Function call: any(value)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'list1'
  • Assignment: right side evaluated first, then stored in 'list2'
  • Function call: overlapping(list1, list2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: overlapping(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'overlapping' (stored in memory, not executed yet)
  2. Assign value to variable: list1
  3. Assign value to variable: list2
  4. Execute function call: print(overlapping(...))
  5. Assign value to variable: list1
  6. Assign value to variable: list2
  7. Execute function call: print(overlapping(...))
  8. Define function 'is_member' (stored in memory, not executed yet)
  9. Define function 'overlapping' (stored in memory, not executed yet)
  10. Assign value to variable: list1
  11. Assign value to variable: list2
  12. Execute function call: print(overlapping(...))
  13. Assign value to variable: list1
  14. Assign value to variable: list2
  15. Execute function call: print(overlapping(...))
  16. Define function 'overlapping' (stored in memory, not executed yet)
  17. Execute function call: print(overlapping(...))
  18. Execute function call: print(overlapping(...))
  19. Define function 'overlapping' (stored in memory, not executed yet)
  20. Execute function call: print(overlapping(...))
  21. Execute function call: print(overlapping(...))
  22. Define function 'overlapping' (stored in memory, not executed yet)
  23. Define function 'overlapping' (stored in memory, not executed yet)
  24. Assign value to variable: list1
  25. Assign value to variable: list2
  26. Assign value to variable: result
  27. Execute function call: print(result)
  28. Execute function call: print(overlapping(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'overlapping' is defined but NOT executed until called
    - 'is_member' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over list1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over list1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over list2, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 124:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def overlapping(lst1, lst2)
      Returns a value via 'return' keyword
    def overlapping(lst1, lst2)
      Returns a value via 'return' keyword
    def overlapping(lst1, lst2)
      Returns a value via 'return' keyword
    def overlapping(lst1, lst2)
      Returns a value via 'return' keyword
    def overlapping(lst1, lst2)
      Returns a value via 'return' keyword
    def overlapping(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: &, >, (), ==, in
  Control structures: for loop iteration; generator expression
  Functions called: any, len, overlapping, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: overlapping(lst1, lst2)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: overlapping(lst1, lst2)
  • Binary operation: set(...) & set(...)
  • Function call: len(computed expression)
  • Function call: set(lst1)
  • Function call: set(lst2)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Function call: any(value)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Function call: print(value)
  • Function call: overlapping(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: overlapping(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: overlapping(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'overlapping' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(overlapping(...))
  5. Define function 'overlapping' (stored in memory, not executed yet)
  6. Assign value to variable: lst1
  7. Assign value to variable: lst2
  8. Execute function call: print(overlapping(...))
  9. Define function 'overlapping' (stored in memory, not executed yet)
  10. Execute function call: print(overlapping(...))
  11. Execute function call: print(overlapping(...))
  12. Define function 'overlapping' (stored in memory, not executed yet)
  13. Execute function call: print(overlapping(...))
  14. Execute function call: print(overlapping(...))
  15. Define function 'overlapping' (stored in memory, not executed yet)
  16. Define function 'overlapping' (stored in memory, not executed yet)
  17. Assign value to variable: lst1
  18. Assign value to variable: lst2
  19. Assign value to variable: result
  20. Execute function call: print(result)
  21. Execute function call: print(overlapping(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
    - 'overlapping' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst2, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 126:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def map_words_to_lengths(words)
      Returns a value via 'return' keyword
    def map_words_to_lengths(words)
      Returns a value via 'return' keyword
    def map_words_to_lengths(words)
      Returns a value via 'return' keyword
    def map_words_to_lengths(words)
      Returns a value via 'return' keyword
    def map_words_to_lengths(words)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension
  Functions called: len, list, map, map_words_to_lengths, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'lengths'
  • Function call: lengths.append(value)
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: map_words_to_lengths(words)
  • Assignment: right side evaluated first, then stored in 'lengths'
  • Function call: print(lengths)
  • Function call: len(word)
  • Function call: print(value)
  • Function call: map_words_to_lengths(value)
  • Function call: list(value)
  • Function call: map(len, words)
  • Function call: print(value)
  • Function call: map_words_to_lengths(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: map_words_to_lengths(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: map_words_to_lengths(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'map_words_to_lengths' (stored in memory, not executed yet)
  2. Assign value to variable: words
  3. Assign value to variable: lengths
  4. Execute function call: print(lengths)
  5. Define function 'map_words_to_lengths' (stored in memory, not executed yet)
  6. Execute function call: print(map_words_to_lengths(...))
  7. Define function 'map_words_to_lengths' (stored in memory, not executed yet)
  8. Execute function call: print(map_words_to_lengths(...))
  9. Define function 'map_words_to_lengths' (stored in memory, not executed yet)
  10. Define function 'map_words_to_lengths' (stored in memory, not executed yet)
  11. Assign value to variable: words
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(map_words_to_lengths(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'map_words_to_lengths' is defined but NOT executed until called
    - 'map_words_to_lengths' is defined but NOT executed until called
    - 'map_words_to_lengths' is defined but NOT executed until called
    - 'map_words_to_lengths' is defined but NOT executed until called
    - 'map_words_to_lengths' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 127:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_longest_word(elements)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(elements)
      Returns a value via 'return' keyword
    def find_longest_word(elements)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, (), []
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: find_longest_word, len, max, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'longest_element'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'longest_element'
  • Function call: len(element)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_longest_word(lst)
  • Function call: len(value)
  • Function call: max(words)
  • Function call: print(value)
  • Function call: find_longest_word(value)
  • Function call: max(value)
  • Function call: len(w)
  • Function call: print(value)
  • Function call: find_longest_word(value)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: sum(value)
  • Assignment: right side evaluated first, then stored in 'elements'
  • Function call: find_longest_word(elements)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_longest_word(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_longest_word' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_longest_word(...))
  4. Define function 'find_longest_word' (stored in memory, not executed yet)
  5. Execute function call: print(find_longest_word(...))
  6. Define function 'find_longest_word' (stored in memory, not executed yet)
  7. Execute function call: print(find_longest_word(...))
  8. Define function 'find_longest_word' (stored in memory, not executed yet)
  9. Define function 'find_longest_word' (stored in memory, not executed yet)
  10. Assign value to variable: elements
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(find_longest_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over elements, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over element, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 128:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def filter_long_words(lst, n)
      Returns a value via 'return' keyword
    def filter_long_words(lst, n)
      Returns a value via 'return' keyword
    def filter_long_words(lst, n)
      Returns a value via 'return' keyword
    def filter_long_words(lst, n)
      Returns a value via 'return' keyword
    def filter_long_words(lst, n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., >, ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_long_words, len, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'longest_words'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: longest_words.append(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: print(value)
  • Function call: filter_long_words(lst, n)
  • Function call: len(word)
  • Function call: print(value)
  • Function call: filter_long_words(value, 4)
  • Function call: list(value)
  • Function call: filter(value, lst)
  • Function call: len(w)
  • Function call: print(value)
  • Function call: filter_long_words(value, 5)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: filter_long_words(lst, n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_long_words(value, 15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_long_words' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: n
  4. Execute function call: print(filter_long_words(...))
  5. Define function 'filter_long_words' (stored in memory, not executed yet)
  6. Execute function call: print(filter_long_words(...))
  7. Define function 'filter_long_words' (stored in memory, not executed yet)
  8. Execute function call: print(filter_long_words(...))
  9. Define function 'filter_long_words' (stored in memory, not executed yet)
  10. Define function 'filter_long_words' (stored in memory, not executed yet)
  11. Assign value to variable: lst
  12. Assign value to variable: n
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(filter_long_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_long_words' is defined but NOT executed until called
    - 'filter_long_words' is defined but NOT executed until called
    - 'filter_long_words' is defined but NOT executed until called
    - 'filter_long_words' is defined but NOT executed until called
    - 'filter_long_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 129:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_pangram(sentence)
      Returns a value via 'return' keyword
    def is_pangram(sentence)
      Returns a value via 'return' keyword
    def is_pangram(sentence)
      Returns a value via 'return' keyword
    def is_pangram(sentence)
      Returns a value via 'return' keyword
    def is_pangram(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in, not in
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: all, input, is_pangram, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'alphabet'
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Condition branch: evaluates boolean expression first
  • Function call: input('Enter a sentence: ')
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: is_pangram(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: set(...).issubset(value)
  • Function call: set(value)
  • Function call: set('abcdefghijklmnopqrstuvwxyz')
  • Function call: sentence.lower()
  • Function call: print(value)
  • Function call: is_pangram('The quick brown fox jumps over the lazy dog')
  • Function call: print(value)
  • Function call: is_pangram('Hello World')
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: all(value)
  • Function call: print(value)
  • Function call: is_pangram('Pack my box with five dozen liquor jugs')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: is_pangram(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_pangram('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_pangram' (stored in memory, not executed yet)
  2. Assign value to variable: sentence
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'is_pangram' (stored in memory, not executed yet)
  6. Execute function call: print(is_pangram(...))
  7. Execute function call: print(is_pangram(...))
  8. Define function 'is_pangram' (stored in memory, not executed yet)
  9. Execute function call: print(is_pangram(...))
  10. Define function 'is_pangram' (stored in memory, not executed yet)
  11. Define function 'is_pangram' (stored in memory, not executed yet)
  12. Assign value to variable: sentence
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(is_pangram(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_pangram' is defined but NOT executed until called
    - 'is_pangram' is defined but NOT executed until called
    - 'is_pangram' is defined but NOT executed until called
    - 'is_pangram' is defined but NOT executed until called
    - 'is_pangram' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over alphabet, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 130:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def correct(text)
      Returns a value via 'return' keyword
    def correct(text)
      Returns a value via 'return' keyword
    def correct(text)
      Returns a value via 'return' keyword
    def correct(text)
      Returns a value via 'return' keyword
    def correct(text)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: correct, input, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: re.sub('\\s+', ' ', text)
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: re.sub('\\.(?=[A-Za-z])', '. ', text)
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: correct(text)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: text.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: ' '.join(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.replace('.', '. ')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.replace('.  ', '. ')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: correct('This is very  funny and cool.Indeed!')
  • Function call: re.sub('\\s+', ' ', text)
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: re.sub('\\.(\\w)', '. \\1', text)
  • Function call: print(value)
  • Function call: correct('Hello world.Yes')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'text'
  • Function call: correct(text)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: correct('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: re
  2. Define function 'correct' (stored in memory, not executed yet)
  3. Assign value to variable: text
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'correct' (stored in memory, not executed yet)
  7. Execute function call: print(correct(...))
  8. Define function 'correct' (stored in memory, not executed yet)
  9. Execute function call: print(correct(...))
  10. Define function 'correct' (stored in memory, not executed yet)
  11. Define function 'correct' (stored in memory, not executed yet)
  12. Assign value to variable: text
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(correct(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'correct' is defined but NOT executed until called
    - 'correct' is defined but NOT executed until called
    - 'correct' is defined but NOT executed until called
    - 'correct' is defined but NOT executed until called
    - 'correct' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 131:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def max_func(a, b)
      Returns a value via 'return' keyword
    def max_in_list(numbers)
      Returns a value via 'return' keyword
    def max_in_list(numbers)
      Returns a value via 'return' keyword
    def max_in_list(numbers)
      Returns a value via 'return' keyword
    def max_func(a, b)
      Returns a value via 'return' keyword
    def max_func(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, lambda, return
  Operators used: >, ()
  Control structures: if/else conditional branching; lambda expression
  Functions called: max, max_func, max_in_list, print, reduce
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Function call: reduce(max_func, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: max_in_list(numbers)
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: max_in_list(value)
  • Function call: max(numbers)
  • Function call: print(value)
  • Function call: max_in_list(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: max_func(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_func(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import from functools: reduce
  2. Define function 'max_func' (stored in memory, not executed yet)
  3. Define function 'max_in_list' (stored in memory, not executed yet)
  4. Assign value to variable: numbers
  5. Execute function call: print(max_in_list(...))
  6. Define function 'max_in_list' (stored in memory, not executed yet)
  7. Execute function call: print(max_in_list(...))
  8. Define function 'max_in_list' (stored in memory, not executed yet)
  9. Execute function call: print(max_in_list(...))
  10. Define function 'max_func' (stored in memory, not executed yet)
  11. Define function 'max_func' (stored in memory, not executed yet)
  12. Assign value to variable: a
  13. Assign value to variable: b
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(max_func(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_func' is defined but NOT executed until called
    - 'max_in_list' is defined but NOT executed until called
    - 'max_in_list' is defined but NOT executed until called
    - 'max_in_list' is defined but NOT executed until called
    - 'max_func' is defined but NOT executed until called
    - 'max_func' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 134:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def sum_of_all_numbers()
    def sum_of_all_numbers()
    def sum_of_all_numbers()
    def sum_of_all_numbers()
      Returns a value via 'return' keyword
    def sum_of_all_numbers()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; generator expression
  Functions called: input, int, list, map, print, sum, sum_of_all_numbers
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('Enter a list of numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Function call: sum(int_numbers)
  • Assignment: right side evaluated first, then stored in 'total_sum'
  • Function call: print(total_sum)
  • Function call: sum_of_all_numbers()
  • Function call: list(value)
  • Function call: map(int, value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: sum(numbers)
  • Function call: sum_of_all_numbers()
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: int(x)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Function call: sum_of_all_numbers()
  • Function call: input('Enter a list of numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Function call: sum(int_numbers)
  • Assignment: right side evaluated first, then stored in 'total_sum'
  • Function call: input('Enter a list of numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Function call: sum(int_numbers)
  • Assignment: right side evaluated first, then stored in 'total_sum'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Function call: sum(int_numbers)
  • Assignment: right side evaluated first, then stored in 'total_sum'
  • Function call: print(total_sum)
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: user_input.split()
  • Assignment: right side evaluated first, then stored in 'split_numbers'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(num)
  • Function call: sum(int_numbers)
  • Assignment: right side evaluated first, then stored in 'total_sum'
  • Function call: print(total_sum)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  2. Execute function call: sum_of_all_numbers()
  3. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  4. Execute function call: sum_of_all_numbers()
  5. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  6. Execute function call: sum_of_all_numbers()
  7. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  8. Define function 'sum_of_all_numbers' (stored in memory, not executed yet)
  9. Assign value to variable: user_input
  10. Assign value to variable: split_numbers
  11. Assign value to variable: int_numbers
  12. For loop: iterate over split_numbers
  13. Assign value to variable: total_sum
  14. Execute function call: print(total_sum)
  15. Assign value to variable: user_input
  16. Assign value to variable: split_numbers
  17. Assign value to variable: int_numbers
  18. For loop: iterate over split_numbers
  19. Assign value to variable: total_sum
  20. Execute function call: print(total_sum)

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_all_numbers' is defined but NOT executed until called
    - 'sum_of_all_numbers' is defined but NOT executed until called
    - 'sum_of_all_numbers' is defined but NOT executed until called
    - 'sum_of_all_numbers' is defined but NOT executed until called
    - 'sum_of_all_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - sum_of_all_numbers() is invoked
      → Execution jumps to the function body of 'sum_of_all_numbers'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - sum_of_all_numbers() is invoked
      → Execution jumps to the function body of 'sum_of_all_numbers'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - sum_of_all_numbers() is invoked
      → Execution jumps to the function body of 'sum_of_all_numbers'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 135:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def split_number(numbers)
      Returns a value via 'return' keyword
    def split_number(numbers)
      Returns a value via 'return' keyword
    def split_number(numbers)
      Returns a value via 'return' keyword
    def split_number(numbers)
      Returns a value via 'return' keyword
    def split_number(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: list, map, print, split_number
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: numbers.split()
  • Assignment: right side evaluated first, then stored in 'numbers_split'
  • Assignment: right side evaluated first, then stored in 'number_lst'
  • Function call: number_lst.append(number)
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: split_number(arg)
  • Function call: list(numbers)
  • Function call: print(value)
  • Function call: split_number('123')
  • Function call: print(value)
  • Function call: split_number('456')
  • Function call: list(value)
  • Function call: map(value, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: split_number(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: split_number(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'split_number' (stored in memory, not executed yet)
  2. Assign value to variable: arg
  3. Execute function call: print(split_number(...))
  4. Define function 'split_number' (stored in memory, not executed yet)
  5. Execute function call: print(split_number(...))
  6. Define function 'split_number' (stored in memory, not executed yet)
  7. Execute function call: print(split_number(...))
  8. Define function 'split_number' (stored in memory, not executed yet)
  9. Define function 'split_number' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(split_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'split_number' is defined but NOT executed until called
    - 'split_number' is defined but NOT executed until called
    - 'split_number' is defined but NOT executed until called
    - 'split_number' is defined but NOT executed until called
    - 'split_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 136:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def alphabetically_ordered(sentence)
      Returns a value via 'return' keyword
    def alphabetically_ordered(sentence)
      Returns a value via 'return' keyword
    def alphabetically_ordered(sentence)
      Returns a value via 'return' keyword
    def alphabetically_ordered(sentence)
      Returns a value via 'return' keyword
    def alphabetically_ordered(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: alphabetically_ordered, print, sorted
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: ' '.join(value)
  • Function call: sorted(value)
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: print(value)
  • Function call: alphabetically_ordered(sentence)
  • Function call: ' '.join(value)
  • Function call: sorted(value)
  • Function call: sentence.split()
  • Function call: print(value)
  • Function call: alphabetically_ordered('Banana apple Cherry')
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: words.sort()
  • Function call: ' '.join(words)
  • Function call: print(value)
  • Function call: alphabetically_ordered('zebra cat dog')
  • Function call: ' '.join(value)
  • Function call: sorted(value)
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: ' '.join(value)
  • Function call: sorted(value)
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: alphabetically_ordered(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: alphabetically_ordered('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'alphabetically_ordered' (stored in memory, not executed yet)
  2. Assign value to variable: sentence
  3. Execute function call: print(alphabetically_ordered(...))
  4. Define function 'alphabetically_ordered' (stored in memory, not executed yet)
  5. Execute function call: print(alphabetically_ordered(...))
  6. Define function 'alphabetically_ordered' (stored in memory, not executed yet)
  7. Execute function call: print(alphabetically_ordered(...))
  8. Define function 'alphabetically_ordered' (stored in memory, not executed yet)
  9. Define function 'alphabetically_ordered' (stored in memory, not executed yet)
  10. Assign value to variable: sentence
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(alphabetically_ordered(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'alphabetically_ordered' is defined but NOT executed until called
    - 'alphabetically_ordered' is defined but NOT executed until called
    - 'alphabetically_ordered' is defined but NOT executed until called
    - 'alphabetically_ordered' is defined but NOT executed until called
    - 'alphabetically_ordered' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 137:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def smallest_largest(numbers)
      Returns a value via 'return' keyword
    def smallest_largest(numbers)
      Returns a value via 'return' keyword
    def smallest_largest(numbers)
      Returns a value via 'return' keyword
    def smallest_largest(numbers)
      Returns a value via 'return' keyword
    def smallest_largest(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), []
  Functions called: print, smallest_largest, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: smallest_largest(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.sort()
  • Function call: print(value)
  • Function call: smallest_largest(value)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: smallest_largest(value)
  • Function call: numbers.copy()
  • Assignment: right side evaluated first, then stored in 'copy'
  • Function call: copy.sort()
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: smallest_largest(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: smallest_largest(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'smallest_largest' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(smallest_largest(...))
  4. Define function 'smallest_largest' (stored in memory, not executed yet)
  5. Execute function call: print(smallest_largest(...))
  6. Define function 'smallest_largest' (stored in memory, not executed yet)
  7. Execute function call: print(smallest_largest(...))
  8. Define function 'smallest_largest' (stored in memory, not executed yet)
  9. Define function 'smallest_largest' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(smallest_largest(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'smallest_largest' is defined but NOT executed until called
    - 'smallest_largest' is defined but NOT executed until called
    - 'smallest_largest' is defined but NOT executed until called
    - 'smallest_largest' is defined but NOT executed until called
    - 'smallest_largest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 138:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: largest_smallest, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: largest_smallest(numbers)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: largest_smallest(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.sort()
  • Function call: print(value)
  • Function call: largest_smallest(value)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: largest_smallest(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_smallest(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'largest_smallest' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(largest_smallest(...))
  4. Define function 'largest_smallest' (stored in memory, not executed yet)
  5. Execute function call: print(largest_smallest(...))
  6. Define function 'largest_smallest' (stored in memory, not executed yet)
  7. Execute function call: print(largest_smallest(...))
  8. Define function 'largest_smallest' (stored in memory, not executed yet)
  9. Define function 'largest_smallest' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(largest_smallest(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 139:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def seconded_largest_number(numbers)
      Returns a value via 'return' keyword
    def seconded_largest_number(numbers)
      Returns a value via 'return' keyword
    def seconded_largest_number(numbers)
      Returns a value via 'return' keyword
    def seconded_largest_number(numbers)
      Returns a value via 'return' keyword
    def seconded_largest_number(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: print, seconded_largest_number, set, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: seconded_largest_number(numbers)
  • Function call: heapq.nlargest(2, numbers)
  • Function call: print(value)
  • Function call: seconded_largest_number(value)
  • Function call: sorted(value)
  • Function call: set(numbers)
  • Function call: print(value)
  • Function call: seconded_largest_number(value)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: seconded_largest_number(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: seconded_largest_number(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'seconded_largest_number' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(seconded_largest_number(...))
  4. Define function 'seconded_largest_number' (stored in memory, not executed yet)
  5. Execute function call: print(seconded_largest_number(...))
  6. Define function 'seconded_largest_number' (stored in memory, not executed yet)
  7. Execute function call: print(seconded_largest_number(...))
  8. Define function 'seconded_largest_number' (stored in memory, not executed yet)
  9. Define function 'seconded_largest_number' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(seconded_largest_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'seconded_largest_number' is defined but NOT executed until called
    - 'seconded_largest_number' is defined but NOT executed until called
    - 'seconded_largest_number' is defined but NOT executed until called
    - 'seconded_largest_number' is defined but NOT executed until called
    - 'seconded_largest_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 140:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def fourth_largest_number(numbers)
      Returns a value via 'return' keyword
    def fourth_largest_number(numbers)
      Returns a value via 'return' keyword
    def fourth_largest_number(numbers)
      Returns a value via 'return' keyword
    def fourth_largest_number(numbers)
      Returns a value via 'return' keyword
    def fourth_largest_number(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: fourth_largest_number, print, set, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: fourth_largest_number(numbers)
  • Function call: heapq.nlargest(4, value)
  • Function call: set(numbers)
  • Function call: print(value)
  • Function call: fourth_largest_number(value)
  • Function call: sorted(value)
  • Function call: set(numbers)
  • Function call: print(value)
  • Function call: fourth_largest_number(value)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: fourth_largest_number(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fourth_largest_number(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fourth_largest_number' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(fourth_largest_number(...))
  4. Define function 'fourth_largest_number' (stored in memory, not executed yet)
  5. Execute function call: print(fourth_largest_number(...))
  6. Define function 'fourth_largest_number' (stored in memory, not executed yet)
  7. Execute function call: print(fourth_largest_number(...))
  8. Define function 'fourth_largest_number' (stored in memory, not executed yet)
  9. Define function 'fourth_largest_number' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(fourth_largest_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fourth_largest_number' is defined but NOT executed until called
    - 'fourth_largest_number' is defined but NOT executed until called
    - 'fourth_largest_number' is defined but NOT executed until called
    - 'fourth_largest_number' is defined but NOT executed until called
    - 'fourth_largest_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 141:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def smallest_and_third(numbers)
      Returns a value via 'return' keyword
    def smallest_and_third(numbers)
      Returns a value via 'return' keyword
    def smallest_and_third(numbers)
      Returns a value via 'return' keyword
    def smallest_and_third(numbers)
      Returns a value via 'return' keyword
    def smallest_and_third(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: min, print, set, smallest_and_third, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'third_largest'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: smallest_and_third(numbers)
  • Function call: min(numbers)
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Function call: heapq.nlargest(3, value)
  • Function call: set(numbers)
  • Assignment: right side evaluated first, then stored in 'third_largest'
  • Function call: print(value)
  • Function call: smallest_and_third(value)
  • Function call: sorted(value)
  • Function call: set(numbers)
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: print(value)
  • Function call: smallest_and_third(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: smallest_and_third(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: smallest_and_third(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'smallest_and_third' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(smallest_and_third(...))
  4. Define function 'smallest_and_third' (stored in memory, not executed yet)
  5. Execute function call: print(smallest_and_third(...))
  6. Define function 'smallest_and_third' (stored in memory, not executed yet)
  7. Execute function call: print(smallest_and_third(...))
  8. Define function 'smallest_and_third' (stored in memory, not executed yet)
  9. Define function 'smallest_and_third' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(smallest_and_third(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'smallest_and_third' is defined but NOT executed until called
    - 'smallest_and_third' is defined but NOT executed until called
    - 'smallest_and_third' is defined but NOT executed until called
    - 'smallest_and_third' is defined but NOT executed until called
    - 'smallest_and_third' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 142:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
    def smallest_biggest(numbers)
      Returns a value via 'return' keyword
    def smallest_biggest(numbers)
      Returns a value via 'return' keyword
    def smallest_biggest(numbers)
      Returns a value via 'return' keyword
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
    def largest_smallest(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), []
  Functions called: largest_smallest, print, smallest_biggest, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: largest_smallest(numbers)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: smallest_biggest(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.sort()
  • Function call: print(value)
  • Function call: smallest_biggest(value)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: smallest_biggest(value)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: largest_smallest(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_smallest(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'largest_smallest' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(largest_smallest(...))
  4. Define function 'smallest_biggest' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(smallest_biggest(...))
  7. Define function 'smallest_biggest' (stored in memory, not executed yet)
  8. Execute function call: print(smallest_biggest(...))
  9. Define function 'smallest_biggest' (stored in memory, not executed yet)
  10. Execute function call: print(smallest_biggest(...))
  11. Define function 'largest_smallest' (stored in memory, not executed yet)
  12. Define function 'largest_smallest' (stored in memory, not executed yet)
  13. Assign value to variable: numbers
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(largest_smallest(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'largest_smallest' is defined but NOT executed until called
    - 'smallest_biggest' is defined but NOT executed until called
    - 'smallest_biggest' is defined but NOT executed until called
    - 'smallest_biggest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
    - 'largest_smallest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 143:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def int_str()
    def int_str()
    def int_str()
    def int_str()
      Returns a value via 'return' keyword
    def int_str()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension
  Functions called: input, int_str, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'user_lst'
  • Function call: input('type: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'split_str'
  • Function call: user_lst.append(word)
  • Function call: print(user_lst)
  • Function call: int_str()
  • Function call: print(value)
  • Function call: input(...).split()
  • Function call: input('type: ')
  • Function call: int_str()
  • Function call: print(value)
  • Function call: input(...).split()
  • Function call: input('type: ')
  • Function call: int_str()
  • Assignment: right side evaluated first, then stored in 'user_lst'
  • Function call: input('type: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'split_str'
  • Function call: user_lst.append(word)
  • Assignment: right side evaluated first, then stored in 'user_lst'
  • Function call: input('type: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'split_str'
  • Function call: user_lst.append(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user_lst'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'split_str'
  • Function call: user_lst.append(word)
  • Function call: print(user_lst)
  • Assignment: right side evaluated first, then stored in 'user_lst'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: user.split()
  • Assignment: right side evaluated first, then stored in 'split_str'
  • Function call: user_lst.append(word)
  • Function call: print(user_lst)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'int_str' (stored in memory, not executed yet)
  2. Execute function call: int_str()
  3. Define function 'int_str' (stored in memory, not executed yet)
  4. Execute function call: int_str()
  5. Define function 'int_str' (stored in memory, not executed yet)
  6. Execute function call: int_str()
  7. Define function 'int_str' (stored in memory, not executed yet)
  8. Define function 'int_str' (stored in memory, not executed yet)
  9. Assign value to variable: user_lst
  10. Assign value to variable: user
  11. Assign value to variable: split_str
  12. For loop: iterate over split_str
  13. Execute function call: print(user_lst)
  14. Assign value to variable: user_lst
  15. Assign value to variable: user
  16. Assign value to variable: split_str
  17. For loop: iterate over split_str
  18. Execute function call: print(user_lst)

▸ EXECUTION FLOW

  Function definition phase:
    - 'int_str' is defined but NOT executed until called
    - 'int_str' is defined but NOT executed until called
    - 'int_str' is defined but NOT executed until called
    - 'int_str' is defined but NOT executed until called
    - 'int_str' is defined but NOT executed until called
  Runtime execution (top-level code):
    - int_str() is invoked
      → Execution jumps to the function body of 'int_str'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - int_str() is invoked
      → Execution jumps to the function body of 'int_str'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - int_str() is invoked
      → Execution jumps to the function body of 'int_str'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over split_str, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_str, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_str, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_str, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over split_str, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 144:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def int_to_str_list(number)
      Returns a value via 'return' keyword
    def int_to_str_list(number)
      Returns a value via 'return' keyword
    def int_to_str_list(number)
      Returns a value via 'return' keyword
    def int_to_str_list(number)
      Returns a value via 'return' keyword
    def int_to_str_list(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: int_to_str_list, list, map, print, str
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number_str'
  • Assignment: right side evaluated first, then stored in 'digits_as_strings'
  • Function call: digits_as_strings.append(digit)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: int_to_str_list(number)
  • Function call: list(value)
  • Function call: str(number)
  • Function call: print(value)
  • Function call: int_to_str_list(456)
  • Function call: str(number)
  • Function call: print(value)
  • Function call: int_to_str_list(789)
  • Function call: list(value)
  • Function call: map(value, number_str)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: int_to_str_list(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: int_to_str_list(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'int_to_str_list' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Execute function call: print(int_to_str_list(...))
  4. Define function 'int_to_str_list' (stored in memory, not executed yet)
  5. Execute function call: print(int_to_str_list(...))
  6. Define function 'int_to_str_list' (stored in memory, not executed yet)
  7. Execute function call: print(int_to_str_list(...))
  8. Define function 'int_to_str_list' (stored in memory, not executed yet)
  9. Define function 'int_to_str_list' (stored in memory, not executed yet)
  10. Assign value to variable: number
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(int_to_str_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'int_to_str_list' is defined but NOT executed until called
    - 'int_to_str_list' is defined but NOT executed until called
    - 'int_to_str_list' is defined but NOT executed until called
    - 'int_to_str_list' is defined but NOT executed until called
    - 'int_to_str_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over number_str, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 145:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def common_numbers(lst1, lst2)
      Returns a value via 'return' keyword
    def common_numbers(lst1, lst2)
      Returns a value via 'return' keyword
    def common_numbers(lst1, lst2)
      Returns a value via 'return' keyword
    def common_numbers(lst1, lst2)
      Returns a value via 'return' keyword
    def common_numbers(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), ==, in, and, not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: common_numbers, filter, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(i)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: common_numbers(lst1, lst2)
  • Function call: list(value)
  • Function call: dict.fromkeys(value)
  • Function call: print(value)
  • Function call: common_numbers(value, value)
  • Function call: list(value)
  • Function call: dict.fromkeys(value)
  • Function call: filter(value, lst1)
  • Function call: print(value)
  • Function call: common_numbers(value, value)
  • Function call: list(value)
  • Function call: map(value, lst1)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: common_numbers(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: common_numbers(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(common_numbers(...))
  5. Define function 'common_numbers' (stored in memory, not executed yet)
  6. Execute function call: print(common_numbers(...))
  7. Define function 'common_numbers' (stored in memory, not executed yet)
  8. Execute function call: print(common_numbers(...))
  9. Define function 'common_numbers' (stored in memory, not executed yet)
  10. Define function 'common_numbers' (stored in memory, not executed yet)
  11. Assign value to variable: lst1
  12. Assign value to variable: lst2
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(common_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst2, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 146:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def big_small(numbers)
      Returns a value via 'return' keyword
    def big_small(numbers)
      Returns a value via 'return' keyword
    def big_small(numbers)
      Returns a value via 'return' keyword
    def big_small(numbers)
      Returns a value via 'return' keyword
    def big_small(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: -, ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: big_small, int, print, sorted, str
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'number_lst'
  • Function call: number_lst.append(value)
  • Function call: int(number)
  • Function call: sorted(number_lst)
  • Assignment: right side evaluated first, then stored in 'number_lst'
  • Assignment: right side evaluated first, then stored in 'shorted_str'
  • Function call: shorted_str.append(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: big_small(lst)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: big_small(value)
  • Function call: str(n)
  • Function call: sorted(value)
  • Function call: int(x)
  • Function call: print(value)
  • Function call: big_small(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: big_small(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: big_small(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'big_small' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(big_small(...))
  4. Define function 'big_small' (stored in memory, not executed yet)
  5. Execute function call: print(big_small(...))
  6. Define function 'big_small' (stored in memory, not executed yet)
  7. Execute function call: print(big_small(...))
  8. Define function 'big_small' (stored in memory, not executed yet)
  9. Define function 'big_small' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(big_small(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'big_small' is defined but NOT executed until called
    - 'big_small' is defined but NOT executed until called
    - 'big_small' is defined but NOT executed until called
    - 'big_small' is defined but NOT executed until called
    - 'big_small' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over number_lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 147:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def type_smallest_biggest()
    def small_big()
      Returns a value via 'return' keyword
    def small_big()
      Returns a value via 'return' keyword
    def type_smallest_biggest()
    def type_smallest_biggest()
      Returns a value via 'return' keyword
    def type_smallest_biggest()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: input, int, list, map, print, small_big, sorted, str, type_smallest_biggest
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type number list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort_number'
  • Function call: print(sort_number)
  • Function call: type_smallest_biggest()
  • Function call: input('type number followed by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_string'
  • Function call: user_string.split()
  • Assignment: right side evaluated first, then stored in 'user_split'
  • Assignment: right side evaluated first, then stored in 'int_list'
  • Function call: int_list.append(value)
  • Function call: int(number)
  • Function call: int_list.sort()
  • Assignment: right side evaluated first, then stored in 'sorted_str'
  • Function call: sorted_str.append(value)
  • Function call: str(number)
  • Function call: ' '.join(sorted_str)
  • Function call: print(value)
  • Function call: small_big()
  • Function call: list(value)
  • Function call: map(int, value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: nums.sort()
  • Function call: ' '.join(value)
  • Function call: map(str, nums)
  • Function call: print(value)
  • Function call: small_big()
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: sorted(value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Function call: type_smallest_biggest()
  • Function call: input(...).split()
  • Function call: input('type number list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort_number'
  • Function call: input(...).split()
  • Function call: input('type number list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort_number'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort_number'
  • Function call: print(sort_number)
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort_number'
  • Function call: print(sort_number)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'type_smallest_biggest' (stored in memory, not executed yet)
  2. Execute function call: type_smallest_biggest()
  3. Define function 'small_big' (stored in memory, not executed yet)
  4. Execute function call: print(small_big(...))
  5. Define function 'small_big' (stored in memory, not executed yet)
  6. Execute function call: print(small_big(...))
  7. Define function 'type_smallest_biggest' (stored in memory, not executed yet)
  8. Execute function call: type_smallest_biggest()
  9. Define function 'type_smallest_biggest' (stored in memory, not executed yet)
  10. Define function 'type_smallest_biggest' (stored in memory, not executed yet)
  11. Assign value to variable: user
  12. Assign value to variable: sort_number
  13. Execute function call: print(sort_number)
  14. Assign value to variable: user
  15. Assign value to variable: sort_number
  16. Execute function call: print(sort_number)

▸ EXECUTION FLOW

  Function definition phase:
    - 'type_smallest_biggest' is defined but NOT executed until called
    - 'small_big' is defined but NOT executed until called
    - 'small_big' is defined but NOT executed until called
    - 'type_smallest_biggest' is defined but NOT executed until called
    - 'type_smallest_biggest' is defined but NOT executed until called
    - 'type_smallest_biggest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - type_smallest_biggest() is invoked
      → Execution jumps to the function body of 'type_smallest_biggest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - type_smallest_biggest() is invoked
      → Execution jumps to the function body of 'type_smallest_biggest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user_split, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_list, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 148:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def double_elements(numbers)
      Returns a value via 'return' keyword
    def double_elements(numbers)
      Returns a value via 'return' keyword
    def double_elements(numbers)
      Returns a value via 'return' keyword
    def double_elements(numbers)
      Returns a value via 'return' keyword
    def double_elements(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, +, ., (), []
  Functions called: double_elements, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: numbers + numbers
  • Assignment: right side evaluated first, then stored in 'doubled'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: double_elements(lst)
  • Binary operation: numbers * 2
  • Function call: print(value)
  • Function call: double_elements(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.extend(numbers)
  • Function call: print(value)
  • Function call: double_elements(value)
  • Function call: sum(value)
  • Function call: operator.add(numbers, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: double_elements(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: double_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'double_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(double_elements(...))
  4. Define function 'double_elements' (stored in memory, not executed yet)
  5. Execute function call: print(double_elements(...))
  6. Define function 'double_elements' (stored in memory, not executed yet)
  7. Execute function call: print(double_elements(...))
  8. Define function 'double_elements' (stored in memory, not executed yet)
  9. Define function 'double_elements' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(double_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 149:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def common()
      Returns a value via 'return' keyword
    def common()
      Returns a value via 'return' keyword
    def common()
      Returns a value via 'return' keyword
    def common()
      Returns a value via 'return' keyword
    def common()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: &, ., (), ==, in, and, not in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: common, input, list, map, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(i)
  • Function call: common()
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Binary operation: set(...) & set(...)
  • Function call: list(computed expression)
  • Function call: set(lst1)
  • Function call: set(lst2)
  • Function call: print(value)
  • Function call: common()
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: list(value)
  • Function call: dict.fromkeys(value)
  • Function call: print(value)
  • Function call: common()
  • Function call: list(value)
  • Function call: map(value, lst1)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(i)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(i)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common' (stored in memory, not executed yet)
  2. Execute function call: common()
  3. Define function 'common' (stored in memory, not executed yet)
  4. Execute function call: print(common(...))
  5. Define function 'common' (stored in memory, not executed yet)
  6. Execute function call: print(common(...))
  7. Define function 'common' (stored in memory, not executed yet)
  8. Define function 'common' (stored in memory, not executed yet)
  9. Assign value to variable: lst1
  10. Assign value to variable: lst2
  11. Assign value to variable: result
  12. For loop: iterate over lst1
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Assign value to variable: lst1
  16. Assign value to variable: lst2
  17. Assign value to variable: result
  18. For loop: iterate over lst1
  19. Assign value to variable: result
  20. Execute function call: print(result)

▸ EXECUTION FLOW

  Function definition phase:
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
  Runtime execution (top-level code):
    - common() is invoked
      → Execution jumps to the function body of 'common'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst2, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst2, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst2, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 150:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def common()
    def common()
    def common()
    def common()
      Returns a value via 'return' keyword
    def common()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: &, ., ()
  Functions called: common, input, list, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common_element'
  • Function call: print(common_element)
  • Function call: common()
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Binary operation: set(...) & set(...)
  • Function call: print(computed expression)
  • Function call: set(lst1)
  • Function call: set(lst2)
  • Function call: common()
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: list(value)
  • Function call: set(...).intersection(value)
  • Function call: set(lst2)
  • Function call: set(lst1)
  • Function call: common()
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common_element'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common_element'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common_element'
  • Function call: print(common_element)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common_element'
  • Function call: print(common_element)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common' (stored in memory, not executed yet)
  2. Execute function call: common()
  3. Define function 'common' (stored in memory, not executed yet)
  4. Execute function call: common()
  5. Define function 'common' (stored in memory, not executed yet)
  6. Execute function call: common()
  7. Define function 'common' (stored in memory, not executed yet)
  8. Define function 'common' (stored in memory, not executed yet)
  9. Assign value to variable: lst1
  10. Assign value to variable: lst2
  11. Assign value to variable: set1
  12. Assign value to variable: set2
  13. Assign value to variable: common_element
  14. Execute function call: print(common_element)
  15. Assign value to variable: lst1
  16. Assign value to variable: lst2
  17. Assign value to variable: set1
  18. Assign value to variable: set2
  19. Assign value to variable: common_element
  20. Execute function call: print(common_element)

▸ EXECUTION FLOW

  Function definition phase:
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
    - 'common' is defined but NOT executed until called
  Runtime execution (top-level code):
    - common() is invoked
      → Execution jumps to the function body of 'common'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - common() is invoked
      → Execution jumps to the function body of 'common'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - common() is invoked
      → Execution jumps to the function body of 'common'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 151:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
    def find_longest_word(words)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: >, (), >=, []
  Control structures: if/else conditional branching; for loop iteration; lambda expression
  Functions called: find_longest_word, len, max, print, reduce, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: print(value)
  • Function call: find_longest_word(words)
  • Function call: max(words)
  • Function call: print(value)
  • Function call: find_longest_word(value)
  • Function call: reduce(value, words)
  • Function call: len(a)
  • Function call: len(b)
  • Function call: print(value)
  • Function call: find_longest_word(value)
  • Function call: sorted(words)
  • Function call: print(value)
  • Function call: find_longest_word(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: find_longest_word(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_longest_word(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_longest_word' (stored in memory, not executed yet)
  2. Assign value to variable: words
  3. Execute function call: print(find_longest_word(...))
  4. Define function 'find_longest_word' (stored in memory, not executed yet)
  5. Execute function call: print(find_longest_word(...))
  6. Define function 'find_longest_word' (stored in memory, not executed yet)
  7. Execute function call: print(find_longest_word(...))
  8. Define function 'find_longest_word' (stored in memory, not executed yet)
  9. Execute function call: print(find_longest_word(...))
  10. Define function 'find_longest_word' (stored in memory, not executed yet)
  11. Define function 'find_longest_word' (stored in memory, not executed yet)
  12. Assign value to variable: words
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(find_longest_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 152:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def average(numbers)
      Returns a value via 'return' keyword
    def average(numbers)
      Returns a value via 'return' keyword
    def average(numbers)
      Returns a value via 'return' keyword
    def average(numbers)
      Returns a value via 'return' keyword
    def average(numbers)
      Returns a value via 'return' keyword
    def average(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, +, ., /, ()
  Control structures: for loop iteration; lambda expression
  Functions called: Fraction, average, float, len, print, reduce, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'length'
  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: total / length
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: average(numbers)
  • Binary operation: sum(...) / len(...)
  • Function call: sum(numbers)
  • Function call: len(numbers)
  • Function call: print(value)
  • Function call: average(value)
  • Function call: statistics.mean(numbers)
  • Function call: print(value)
  • Function call: average(value)
  • Binary operation: reduce(...) / len(...)
  • Binary operation: x + y
  • Function call: reduce(value, numbers)
  • Function call: len(numbers)
  • Function call: print(value)
  • Function call: average(value)
  • Binary operation: total * expression
  • Binary operation: 1 / length
  • Function call: float(value)
  • Function call: Fraction(total, length)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: average(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(average(...))
  4. Define function 'average' (stored in memory, not executed yet)
  5. Execute function call: print(average(...))
  6. Define function 'average' (stored in memory, not executed yet)
  7. Execute function call: print(average(...))
  8. Define function 'average' (stored in memory, not executed yet)
  9. Execute function call: print(average(...))
  10. Define function 'average' (stored in memory, not executed yet)
  11. Define function 'average' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 153:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def second_smallest_element(lst)
      Returns a value via 'return' keyword
    def second_smallest_element(lst)
      Returns a value via 'return' keyword
    def second_smallest_element(lst)
      Returns a value via 'return' keyword
    def second_smallest_element(lst)
      Returns a value via 'return' keyword
    def second_smallest_element(lst)
      Returns a value via 'return' keyword
    def second_smallest_element(lst)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: -, ., (), >=, []
  Functions called: len, print, second_smallest_element, set, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'sorted_lst'
  • Function call: len(sorted_lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: second_smallest_element(lst)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'sorted_lst'
  • Function call: len(sorted_lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: second_smallest_element(lst)
  • Function call: heapq.nsmallest(2, value)
  • Function call: set(lst)
  • Function call: print(value)
  • Function call: second_smallest_element(value)
  • Function call: sorted(value)
  • Function call: set(lst)
  • Assignment: right side evaluated first, then stored in 'unique'
  • Function call: len(unique)
  • Function call: print(value)
  • Function call: second_smallest_element(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: second_smallest_element(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: second_smallest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'second_smallest_element' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(second_smallest_element(...))
  4. Define function 'second_smallest_element' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(second_smallest_element(...))
  7. Define function 'second_smallest_element' (stored in memory, not executed yet)
  8. Execute function call: print(second_smallest_element(...))
  9. Define function 'second_smallest_element' (stored in memory, not executed yet)
  10. Execute function call: print(second_smallest_element(...))
  11. Define function 'second_smallest_element' (stored in memory, not executed yet)
  12. Define function 'second_smallest_element' (stored in memory, not executed yet)
  13. Assign value to variable: lst
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(second_smallest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
    - 'second_smallest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 154:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_last_element()
      Returns a value via 'return' keyword
    def find_second_last_element(lst)
      Returns a value via 'return' keyword
    def find_second_last_element(lst)
      Returns a value via 'return' keyword
    def find_last_element()
      Returns a value via 'return' keyword
    def find_last_element()
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: -, ., <, (), >=, [], not
  Functions called: find_last_element, find_second_last_element, input, len, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('Type list (elements separated by spaces): ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: find_last_element()
  • Function call: len(lst)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: find_second_last_element(value)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: find_second_last_element(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_second_last_element(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_second_last_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_last_element' (stored in memory, not executed yet)
  2. Execute function call: print(find_last_element(...))
  3. Define function 'find_second_last_element' (stored in memory, not executed yet)
  4. Execute function call: print(find_second_last_element(...))
  5. Define function 'find_second_last_element' (stored in memory, not executed yet)
  6. Execute function call: print(find_second_last_element(...))
  7. Define function 'find_last_element' (stored in memory, not executed yet)
  8. Define function 'find_last_element' (stored in memory, not executed yet)
  9. Assign value to variable: lst
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_second_last_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_last_element' is defined but NOT executed until called
    - 'find_second_last_element' is defined but NOT executed until called
    - 'find_second_last_element' is defined but NOT executed until called
    - 'find_last_element' is defined but NOT executed until called
    - 'find_last_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 155:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
    def reverse_number(number)
      Returns a value via 'return' keyword
  Keywords used: def, return, while
  Operators used: %, *, +, -, >, (), >=, []
  Control structures: while loop
  Functions called: abs, int, print, reverse_number, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse_number(lst)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'str_number'
  • Assignment: right side evaluated first, then stored in 'reversed_str_number'
  • Function call: int(reversed_str_number)
  • Assignment: right side evaluated first, then stored in 'reversed_number'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: reverse_number(number)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'str_number'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse_number(lst)
  • Assignment: right side evaluated first, then stored in 'reversed_num'
  • Function call: abs(number)
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: expression + expression
  • Binary operation: reversed_num * 10
  • Binary operation: num % 10
  • Assignment: right side evaluated first, then stored in 'reversed_num'
  • Function call: print(value)
  • Function call: reverse_number(123)
  • Function call: print(value)
  • Function call: reverse_number(value)
  • Function call: print(value)
  • Function call: reverse_number(1000)
  • Function call: int(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: int(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: reverse_number(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_number(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_number' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(reverse_number(...))
  4. Define function 'reverse_number' (stored in memory, not executed yet)
  5. Assign value to variable: number
  6. Execute function call: print(reverse_number(...))
  7. Define function 'reverse_number' (stored in memory, not executed yet)
  8. Assign value to variable: lst
  9. Execute function call: print(reverse_number(...))
  10. Define function 'reverse_number' (stored in memory, not executed yet)
  11. Execute function call: print(reverse_number(...))
  12. Execute function call: print(reverse_number(...))
  13. Execute function call: print(reverse_number(...))
  14. Define function 'reverse_number' (stored in memory, not executed yet)
  15. Define function 'reverse_number' (stored in memory, not executed yet)
  16. Assign value to variable: number
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(reverse_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
"""

"""
Problem 156:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
    def remove_duplicates(string)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), in, or, not
  Control structures: generator expression
  Functions called: print, remove_duplicates, set
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'split_word'
  • Function call: set(split_word)
  • Assignment: right side evaluated first, then stored in 'set_word'
  • Function call: ' '.join(set_word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_duplicates(lst)
  • Function call: ''.join(value)
  • Function call: set(string)
  • Function call: print(value)
  • Function call: remove_duplicates('hello')
  • Function call: ''.join(value)
  • Function call: dict.fromkeys(string)
  • Function call: print(value)
  • Function call: remove_duplicates('hello')
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: ''.join(value)
  • Function call: seen.add(c)
  • Function call: print(value)
  • Function call: remove_duplicates('aabbcc')
  • Function call: ' '.join(set_word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: ' '.join(set_word)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: remove_duplicates(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_duplicates(...))
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Execute function call: print(remove_duplicates(...))
  6. Define function 'remove_duplicates' (stored in memory, not executed yet)
  7. Execute function call: print(remove_duplicates(...))
  8. Define function 'remove_duplicates' (stored in memory, not executed yet)
  9. Execute function call: print(remove_duplicates(...))
  10. Define function 'remove_duplicates' (stored in memory, not executed yet)
  11. Define function 'remove_duplicates' (stored in memory, not executed yet)
  12. Assign value to variable: string
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(remove_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 158:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: -, ., >, (), []
  Control structures: for loop iteration; lambda expression
  Functions called: input, int, largest_element, main, max, print, reduce, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sorted_numbers'
  • Assignment: right side evaluated first, then stored in 'largest_number'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: largest_element(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: result.append(value)
  • Function call: int(num)
  • Function call: sorted(result)
  • Assignment: right side evaluated first, then stored in 'largest_num'
  • Function call: main()
  • Function call: max(numbers)
  • Function call: print(value)
  • Function call: largest_element(value)
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: largest_element(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: largest_element(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'largest_element' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(largest_element(...))
  4. Define function 'main' (stored in memory, not executed yet)
  5. Execute function call: main()
  6. Define function 'largest_element' (stored in memory, not executed yet)
  7. Execute function call: print(largest_element(...))
  8. Define function 'largest_element' (stored in memory, not executed yet)
  9. Execute function call: print(largest_element(...))
  10. Define function 'largest_element' (stored in memory, not executed yet)
  11. Define function 'largest_element' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(largest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'largest_element' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 159:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
    def largest_element(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: -, >, (), []
  Control structures: if/else conditional branching; for loop iteration; lambda expression
  Functions called: largest_element, max, print, reduce, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: largest_element(numbers)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: largest_element(value)
  • Function call: reduce(value, numbers)
  • Function call: print(value)
  • Function call: largest_element(value)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: largest_element(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'largest_element' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(largest_element(...))
  4. Define function 'largest_element' (stored in memory, not executed yet)
  5. Execute function call: print(largest_element(...))
  6. Define function 'largest_element' (stored in memory, not executed yet)
  7. Execute function call: print(largest_element(...))
  8. Define function 'largest_element' (stored in memory, not executed yet)
  9. Define function 'largest_element' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(largest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 160:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def find_length(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
    def find_length(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, pass, return
  Operators used: +, (), ==, []
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: enumerate, find_length, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'arg'
  • Function call: print(value)
  • Function call: find_length(arg)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: find_length('Jonathan')
  • Condition branch: evaluates boolean expression first
  • Binary operation: 1 + find_length(...)
  • Function call: find_length(value)
  • Function call: print(value)
  • Function call: find_length('hello')
  • Function call: enumerate(string)
  • Binary operation: i + 1
  • Function call: print(value)
  • Function call: find_length('python')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: find_length(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_length('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_length' (stored in memory, not executed yet)
  2. Assign value to variable: arg
  3. Execute function call: print(find_length(...))
  4. Define function 'find_length' (stored in memory, not executed yet)
  5. Execute function call: print(find_length(...))
  6. Define function 'find_length' (stored in memory, not executed yet)
  7. Execute function call: print(find_length(...))
  8. Define function 'find_length' (stored in memory, not executed yet)
  9. Execute function call: print(find_length(...))
  10. Define function 'find_length' (stored in memory, not executed yet)
  11. Define function 'find_length' (stored in memory, not executed yet)
  12. Assign value to variable: string
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(find_length(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_length' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
    - 'find_length' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 161:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def intersection()
    def intersection()
    def intersection_two_list(lst1, lst2)
      Returns a value via 'return' keyword
    def intersection_two_list(lst1, lst2)
      Returns a value via 'return' keyword
    def intersection_two_list(lst1, lst2)
      Returns a value via 'return' keyword
    def intersection_two_list(lst1, lst2)
      Returns a value via 'return' keyword
    def intersection()
      Returns a value via 'return' keyword
    def intersection()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: &, ., (), in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: filter, input, intersection, intersection_two_list, list, print, set, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: sorted(lst1)
  • Assignment: right side evaluated first, then stored in 'sort1'
  • Function call: sorted(lst2)
  • Assignment: right side evaluated first, then stored in 'sort2'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Function call: print(common)
  • Function call: intersection()
  • Function call: input(...).split()
  • Function call: input('type lst1: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst2: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'common'
  • Function call: print(common)
  • Function call: intersection()
  • Function call: set(lst1)
  • Assignment: right side evaluated first, then stored in 'set1'
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'set2'
  • Function call: set1.intersection(set2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: intersection_two_list(lst1, lst2)
  • Binary operation: set(...) & set(...)
  • Function call: list(computed expression)
  • Function call: set(lst1)
  • Function call: set(lst2)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: intersection_two_list(lst1, lst2)
  • Function call: list(value)
  • Function call: set(value)
  • Function call: print(value)
  • Function call: intersection_two_list(value, value)
  • Function call: list(value)
  • Function call: filter(value, lst1)
  • Function call: print(value)
  • Function call: intersection_two_list(value, value)
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: sorted(lst1)
  • Assignment: right side evaluated first, then stored in 'sort1'
  • Function call: sorted(lst2)
  • Assignment: right side evaluated first, then stored in 'sort2'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: sorted(lst1)
  • Assignment: right side evaluated first, then stored in 'sort1'
  • Function call: sorted(lst2)
  • Assignment: right side evaluated first, then stored in 'sort2'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: intersection_two_list(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: intersection_two_list(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'intersection' (stored in memory, not executed yet)
  2. Execute function call: intersection()
  3. Define function 'intersection' (stored in memory, not executed yet)
  4. Execute function call: intersection()
  5. Define function 'intersection_two_list' (stored in memory, not executed yet)
  6. Assign value to variable: lst1
  7. Assign value to variable: lst2
  8. Execute function call: print(intersection_two_list(...))
  9. Define function 'intersection_two_list' (stored in memory, not executed yet)
  10. Assign value to variable: lst1
  11. Assign value to variable: lst2
  12. Execute function call: print(intersection_two_list(...))
  13. Define function 'intersection_two_list' (stored in memory, not executed yet)
  14. Execute function call: print(intersection_two_list(...))
  15. Define function 'intersection_two_list' (stored in memory, not executed yet)
  16. Execute function call: print(intersection_two_list(...))
  17. Define function 'intersection' (stored in memory, not executed yet)
  18. Define function 'intersection' (stored in memory, not executed yet)
  19. Assign value to variable: lst1
  20. Assign value to variable: lst2
  21. Assign value to variable: result
  22. Execute function call: print(result)
  23. Execute function call: print(intersection_two_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'intersection' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
    - 'intersection_two_list' is defined but NOT executed until called
    - 'intersection_two_list' is defined but NOT executed until called
    - 'intersection_two_list' is defined but NOT executed until called
    - 'intersection_two_list' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
  Runtime execution (top-level code):
    - intersection() is invoked
      → Execution jumps to the function body of 'intersection'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - intersection() is invoked
      → Execution jumps to the function body of 'intersection'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over sort1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sort1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sort1, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 162:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def third_largest()
    def third_largest()
      Returns a value via 'return' keyword
    def third_largest()
      Returns a value via 'return' keyword
    def third_largest(numbers)
      Returns a value via 'return' keyword
    def third_largest()
      Returns a value via 'return' keyword
    def third_largest()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: -, ., (), []
  Control structures: for loop iteration
  Functions called: input, int, list, map, print, sorted, third_largest
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('type number followed by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_string'
  • Function call: user_string.split()
  • Assignment: right side evaluated first, then stored in 'user_split'
  • Assignment: right side evaluated first, then stored in 'int_list'
  • Function call: int_list.append(value)
  • Function call: int(number)
  • Function call: int_list.sort()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: third_largest()
  • Function call: list(value)
  • Function call: map(int, value)
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: third_largest()
  • Function call: list(value)
  • Function call: map(int, value)
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: heapq.nlargest(3, numbers)
  • Function call: print(value)
  • Function call: third_largest()
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: third_largest(value)
  • Function call: input('type number followed by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_string'
  • Function call: user_string.split()
  • Assignment: right side evaluated first, then stored in 'user_split'
  • Assignment: right side evaluated first, then stored in 'int_list'
  • Function call: int_list.append(value)
  • Function call: int(number)
  • Function call: int_list.sort()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: input('type number followed by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_string'
  • Function call: user_string.split()
  • Assignment: right side evaluated first, then stored in 'user_split'
  • Assignment: right side evaluated first, then stored in 'int_list'
  • Function call: int_list.append(value)
  • Function call: int(number)
  • Function call: int_list.sort()
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: third_largest(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: third_largest(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'third_largest' (stored in memory, not executed yet)
  2. Execute function call: third_largest()
  3. Define function 'third_largest' (stored in memory, not executed yet)
  4. Execute function call: print(third_largest(...))
  5. Define function 'third_largest' (stored in memory, not executed yet)
  6. Execute function call: print(third_largest(...))
  7. Define function 'third_largest' (stored in memory, not executed yet)
  8. Execute function call: print(third_largest(...))
  9. Define function 'third_largest' (stored in memory, not executed yet)
  10. Define function 'third_largest' (stored in memory, not executed yet)
  11. Assign value to variable: numbers
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(third_largest(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'third_largest' is defined but NOT executed until called
    - 'third_largest' is defined but NOT executed until called
    - 'third_largest' is defined but NOT executed until called
    - 'third_largest' is defined but NOT executed until called
    - 'third_largest' is defined but NOT executed until called
    - 'third_largest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - third_largest() is invoked
      → Execution jumps to the function body of 'third_largest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user_split, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_split, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_split, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 163:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(sentence)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], in
  Control structures: for loop iteration; dict comprehension; generator expression
  Functions called: Counter, count_vowels, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: string.lower()
  • Assignment: right side evaluated first, then stored in 'string'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: count_vowels(string)
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'vowel_counts'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: count_vowels(string)
  • Function call: Counter(value)
  • Function call: string.lower()
  • Assignment: right side evaluated first, then stored in 'c'
  • Function call: c.get(v, 0)
  • Function call: print(value)
  • Function call: count_vowels('Jonathan is the best')
  • Function call: string.lower()
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: string.count(v)
  • Function call: print(value)
  • Function call: count_vowels('Jonathan is the best')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_vowels(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(count_vowels(...))
  4. Define function 'count_vowels' (stored in memory, not executed yet)
  5. Assign value to variable: string
  6. Execute function call: print(count_vowels(...))
  7. Define function 'count_vowels' (stored in memory, not executed yet)
  8. Execute function call: print(count_vowels(...))
  9. Define function 'count_vowels' (stored in memory, not executed yet)
  10. Execute function call: print(count_vowels(...))
  11. Define function 'count_vowels' (stored in memory, not executed yet)
  12. Define function 'count_vowels' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over vowels, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sentence, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 164:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def count_vowels(sentence)
      Returns a value via 'return' keyword
    def count_vowels(sentence)
      Returns a value via 'return' keyword
    def count_vowels(sentence)
      Returns a value via 'return' keyword
    def count_vowels(sentence)
      Returns a value via 'return' keyword
    def count_vowels(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], in
  Control structures: if/else conditional branching; for loop iteration; dict comprehension; generator expression
  Functions called: Counter, count_vowels, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'vowel_counts'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: count_vowels(string)
  • Function call: Counter(value)
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'c'
  • Function call: c.get(v, 0)
  • Function call: print(value)
  • Function call: count_vowels('Jonathan is the best')
  • Function call: sentence.lower()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: sentence.count(v)
  • Function call: print(value)
  • Function call: count_vowels('Jonathan is the best')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: count_vowels(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(count_vowels(...))
  4. Define function 'count_vowels' (stored in memory, not executed yet)
  5. Execute function call: print(count_vowels(...))
  6. Define function 'count_vowels' (stored in memory, not executed yet)
  7. Execute function call: print(count_vowels(...))
  8. Define function 'count_vowels' (stored in memory, not executed yet)
  9. Define function 'count_vowels' (stored in memory, not executed yet)
  10. Assign value to variable: sentence
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over vowels, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sentence, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 165:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def intiger_string(number)
      Returns a value via 'return' keyword
    def split_number_int(number)
      Returns a value via 'return' keyword
    def split_number_int(number)
      Returns a value via 'return' keyword
    def split_number_int(number)
      Returns a value via 'return' keyword
    def intiger_string(number)
      Returns a value via 'return' keyword
    def intiger_string(number)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Control structures: list comprehension
  Functions called: intiger_string, list, map, print, split_number_int, str, tuple
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: intiger_string(number)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number_str'
  • Function call: tuple(number_str)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: split_number_int(number)
  • Function call: list(value)
  • Function call: map(str, value)
  • Function call: str(number)
  • Function call: print(value)
  • Function call: split_number_int(456)
  • Function call: str(number)
  • Function call: print(value)
  • Function call: split_number_int(789)
  • Function call: list(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: list(value)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: intiger_string(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: intiger_string(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'intiger_string' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Execute function call: print(intiger_string(...))
  4. Define function 'split_number_int' (stored in memory, not executed yet)
  5. Assign value to variable: number
  6. Execute function call: print(split_number_int(...))
  7. Define function 'split_number_int' (stored in memory, not executed yet)
  8. Execute function call: print(split_number_int(...))
  9. Define function 'split_number_int' (stored in memory, not executed yet)
  10. Execute function call: print(split_number_int(...))
  11. Define function 'intiger_string' (stored in memory, not executed yet)
  12. Define function 'intiger_string' (stored in memory, not executed yet)
  13. Assign value to variable: number
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(intiger_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'intiger_string' is defined but NOT executed until called
    - 'split_number_int' is defined but NOT executed until called
    - 'split_number_int' is defined but NOT executed until called
    - 'split_number_int' is defined but NOT executed until called
    - 'intiger_string' is defined but NOT executed until called
    - 'intiger_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 166:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, ., (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: double_elements, list, map, print, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'doubled'
  • Function call: doubled.append(word)
  • Function call: doubled.append(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: double_elements(lst)
  • Binary operation: lst * 2
  • Function call: print(value)
  • Function call: double_elements(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.extend(lst)
  • Function call: print(value)
  • Function call: double_elements(value)
  • Function call: zip(lst, lst)
  • Function call: print(value)
  • Function call: double_elements(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: double_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: double_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'double_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(double_elements(...))
  4. Define function 'double_elements' (stored in memory, not executed yet)
  5. Execute function call: print(double_elements(...))
  6. Define function 'double_elements' (stored in memory, not executed yet)
  7. Execute function call: print(double_elements(...))
  8. Define function 'double_elements' (stored in memory, not executed yet)
  9. Execute function call: print(double_elements(...))
  10. Define function 'double_elements' (stored in memory, not executed yet)
  11. Define function 'double_elements' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(double_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 167:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def main()
      Returns a value via 'return' keyword
    def l_s(numbers)
      Returns a value via 'return' keyword
    def l_s(numbers)
      Returns a value via 'return' keyword
    def l_s(numbers)
      Returns a value via 'return' keyword
    def l_s(numbers)
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
  Keywords used: def, lambda, return
  Operators used: -, ., <, >, (), []
  Control structures: lambda expression
  Functions called: input, l_s, list, main, map, max, min, print, reduce, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: sorted(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'smallest_num'
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'largest_num'
  • Function call: main()
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sort_number_largest'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sort_number_smallest'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: l_s(numbers)
  • Function call: min(numbers)
  • Function call: max(numbers)
  • Function call: print(value)
  • Function call: l_s(value)
  • Function call: reduce(value, numbers)
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Function call: reduce(value, numbers)
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(value)
  • Function call: l_s(value)
  • Function call: sorted(numbers)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: l_s(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: l_s(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: l_s(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Execute function call: main()
  3. Define function 'l_s' (stored in memory, not executed yet)
  4. Assign value to variable: numbers
  5. Execute function call: print(l_s(...))
  6. Define function 'l_s' (stored in memory, not executed yet)
  7. Execute function call: print(l_s(...))
  8. Define function 'l_s' (stored in memory, not executed yet)
  9. Execute function call: print(l_s(...))
  10. Define function 'l_s' (stored in memory, not executed yet)
  11. Execute function call: print(l_s(...))
  12. Define function 'main' (stored in memory, not executed yet)
  13. Define function 'main' (stored in memory, not executed yet)
  14. Assign value to variable: numbers
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(l_s(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'l_s' is defined but NOT executed until called
    - 'l_s' is defined but NOT executed until called
    - 'l_s' is defined but NOT executed until called
    - 'l_s' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 168:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def count_words()
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words(string)
      Returns a value via 'return' keyword
    def count_words()
      Returns a value via 'return' keyword
    def count_words()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], in
  Control structures: for loop iteration
  Functions called: Counter, count_words, dict, input, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type words: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: word.lower()
  • Assignment: right side evaluated first, then stored in 'word'
  • Condition branch: evaluates boolean expression first
  • Function call: print(result)
  • Function call: count_words()
  • Function call: string.lower()
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'word_count'
  • Condition branch: evaluates boolean expression first
  • Function call: dict(value)
  • Function call: sorted(value)
  • Function call: word_count.items()
  • Assignment: right side evaluated first, then stored in 'sorted_word_count'
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Function call: print(value)
  • Function call: count_words(input_string)
  • Function call: dict(value)
  • Function call: sorted(value)
  • Function call: Counter(...).items()
  • Function call: Counter(value)
  • Function call: string.lower(...).split()
  • Function call: string.lower()
  • Function call: print(value)
  • Function call: count_words('This is a test. This test is only a test.')
  • Function call: string.lower(...).split()
  • Function call: string.lower()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: count.get(...) + 1
  • Function call: count.get(word, 0)
  • Function call: dict(value)
  • Function call: sorted(value)
  • Function call: count.items()
  • Function call: print(value)
  • Function call: count_words('one two one two three')
  • Function call: string.lower(...).split()
  • Function call: string.lower()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: count.setdefault(word, 0)
  • Function call: dict(value)
  • Function call: sorted(value)
  • Function call: count.items()
  • Function call: print(value)
  • Function call: count_words('hello hello world')
  • Function call: input(...).split()
  • Function call: input('type words: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: word.lower()
  • Assignment: right side evaluated first, then stored in 'word'
  • Condition branch: evaluates boolean expression first
  • Function call: input(...).split()
  • Function call: input('type words: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: word.lower()
  • Assignment: right side evaluated first, then stored in 'word'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_words(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_words('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_words' (stored in memory, not executed yet)
  2. Execute function call: count_words()
  3. Define function 'count_words' (stored in memory, not executed yet)
  4. Assign value to variable: input_string
  5. Execute function call: print(count_words(...))
  6. Define function 'count_words' (stored in memory, not executed yet)
  7. Execute function call: print(count_words(...))
  8. Define function 'count_words' (stored in memory, not executed yet)
  9. Execute function call: print(count_words(...))
  10. Define function 'count_words' (stored in memory, not executed yet)
  11. Execute function call: print(count_words(...))
  12. Define function 'count_words' (stored in memory, not executed yet)
  13. Define function 'count_words' (stored in memory, not executed yet)
  14. Assign value to variable: string
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(count_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
    - 'count_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - count_words() is invoked
      → Execution jumps to the function body of 'count_words'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over sort, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sort, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over sort, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 169:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def merge_dictionaries(dict1, dict2)
      Returns a value via 'return' keyword
    def merge_dictionaries(dict1, dict2)
      Returns a value via 'return' keyword
    def merge_dictionaries(dict1, dict2)
      Returns a value via 'return' keyword
    def merge_dictionaries(dict1, dict2)
      Returns a value via 'return' keyword
    def merge_dictionaries(dict1, dict2)
      Returns a value via 'return' keyword
    def merge_dictionaries(dict1, dict2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), [], or
  Control structures: for loop iteration
  Functions called: dict, merge_dictionaries, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: dict1.copy()
  • Assignment: right side evaluated first, then stored in 'merged'
  • Function call: dict2.items()
  • Assignment: right side evaluated first, then stored in 'dict1'
  • Assignment: right side evaluated first, then stored in 'dict2'
  • Function call: merge_dictionaries(dict1, dict2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: merge_dictionaries(value, value)
  • Function call: dict(dict1)
  • Function call: print(value)
  • Function call: merge_dictionaries(value, value)
  • Function call: dict1.copy()
  • Assignment: right side evaluated first, then stored in 'merged'
  • Function call: merged.update(dict2)
  • Function call: print(value)
  • Function call: merge_dictionaries(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dict1'
  • Assignment: right side evaluated first, then stored in 'dict2'
  • Function call: merge_dictionaries(dict1, dict2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: merge_dictionaries(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'merge_dictionaries' (stored in memory, not executed yet)
  2. Assign value to variable: dict1
  3. Assign value to variable: dict2
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'merge_dictionaries' (stored in memory, not executed yet)
  7. Execute function call: print(merge_dictionaries(...))
  8. Define function 'merge_dictionaries' (stored in memory, not executed yet)
  9. Execute function call: print(merge_dictionaries(...))
  10. Define function 'merge_dictionaries' (stored in memory, not executed yet)
  11. Execute function call: print(merge_dictionaries(...))
  12. Define function 'merge_dictionaries' (stored in memory, not executed yet)
  13. Define function 'merge_dictionaries' (stored in memory, not executed yet)
  14. Assign value to variable: dict1
  15. Assign value to variable: dict2
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(merge_dictionaries(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'merge_dictionaries' is defined but NOT executed until called
    - 'merge_dictionaries' is defined but NOT executed until called
    - 'merge_dictionaries' is defined but NOT executed until called
    - 'merge_dictionaries' is defined but NOT executed until called
    - 'merge_dictionaries' is defined but NOT executed until called
    - 'merge_dictionaries' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over dict2.items(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 170:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def count_characters(string)
      Returns a value via 'return' keyword
    def count_characters(string)
      Returns a value via 'return' keyword
    def count_characters(string)
      Returns a value via 'return' keyword
    def count_characters(string)
      Returns a value via 'return' keyword
    def count_characters(string)
      Returns a value via 'return' keyword
    def count_characters(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: Counter, count_characters, dict, input, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'counts'
  • Condition branch: evaluates boolean expression first
  • Function call: input('Enter a string: ')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_characters(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: dict(value)
  • Function call: Counter(string)
  • Function call: print(value)
  • Function call: count_characters('hello')
  • Assignment: right side evaluated first, then stored in 'counts'
  • Binary operation: counts.get(...) + 1
  • Function call: counts.get(char, 0)
  • Function call: print(value)
  • Function call: count_characters('aabbcc')
  • Assignment: right side evaluated first, then stored in 'counts'
  • Function call: counts.setdefault(char, 0)
  • Function call: print(value)
  • Function call: count_characters('abcabc')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_characters(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_characters('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_characters' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'count_characters' (stored in memory, not executed yet)
  6. Execute function call: print(count_characters(...))
  7. Define function 'count_characters' (stored in memory, not executed yet)
  8. Execute function call: print(count_characters(...))
  9. Define function 'count_characters' (stored in memory, not executed yet)
  10. Execute function call: print(count_characters(...))
  11. Define function 'count_characters' (stored in memory, not executed yet)
  12. Define function 'count_characters' (stored in memory, not executed yet)
  13. Assign value to variable: string
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(count_characters(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_characters' is defined but NOT executed until called
    - 'count_characters' is defined but NOT executed until called
    - 'count_characters' is defined but NOT executed until called
    - 'count_characters' is defined but NOT executed until called
    - 'count_characters' is defined but NOT executed until called
    - 'count_characters' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 171:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def invert_dictionary(input_dict)
      Returns a value via 'return' keyword
    def invert_dictionary(input_dict)
      Returns a value via 'return' keyword
    def invert_dictionary(input_dict)
      Returns a value via 'return' keyword
    def invert_dictionary(input_dict)
      Returns a value via 'return' keyword
    def invert_dictionary(input_dict)
      Returns a value via 'return' keyword
    def invert_dictionary(input_dict)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), ==, [], not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; dict comprehension
  Functions called: defaultdict, dict, invert_dictionary, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'inverted'
  • Function call: input_dict.items()
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(key)
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Function call: invert_dictionary(input_dict)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: defaultdict(list)
  • Assignment: right side evaluated first, then stored in 'inverted'
  • Function call: input_dict.items()
  • Function call: expression.append(key)
  • Function call: dict(inverted)
  • Function call: print(value)
  • Function call: invert_dictionary(value)
  • Assignment: right side evaluated first, then stored in 'inverted'
  • Function call: input_dict.items()
  • Function call: inverted.setdefault(...).append(key)
  • Function call: inverted.setdefault(value, value)
  • Function call: print(value)
  • Function call: invert_dictionary(value)
  • Function call: set(value)
  • Function call: input_dict.values()
  • Function call: print(value)
  • Function call: invert_dictionary(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Function call: invert_dictionary(input_dict)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: invert_dictionary(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'invert_dictionary' (stored in memory, not executed yet)
  2. Assign value to variable: input_dict
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'invert_dictionary' (stored in memory, not executed yet)
  6. Execute function call: print(invert_dictionary(...))
  7. Define function 'invert_dictionary' (stored in memory, not executed yet)
  8. Execute function call: print(invert_dictionary(...))
  9. Define function 'invert_dictionary' (stored in memory, not executed yet)
  10. Execute function call: print(invert_dictionary(...))
  11. Define function 'invert_dictionary' (stored in memory, not executed yet)
  12. Define function 'invert_dictionary' (stored in memory, not executed yet)
  13. Assign value to variable: input_dict
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(invert_dictionary(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'invert_dictionary' is defined but NOT executed until called
    - 'invert_dictionary' is defined but NOT executed until called
    - 'invert_dictionary' is defined but NOT executed until called
    - 'invert_dictionary' is defined but NOT executed until called
    - 'invert_dictionary' is defined but NOT executed until called
    - 'invert_dictionary' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over input_dict.items(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over input_dict.items(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over input_dict.items(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 172:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_by_value(input_dict, threshold)
      Returns a value via 'return' keyword
    def filter_by_value(input_dict, threshold)
      Returns a value via 'return' keyword
    def filter_by_value(input_dict, threshold)
      Returns a value via 'return' keyword
    def filter_by_value(input_dict, threshold)
      Returns a value via 'return' keyword
    def filter_by_value(input_dict, threshold)
      Returns a value via 'return' keyword
    def filter_by_value(input_dict, threshold)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., >, (), []
  Control structures: if/else conditional branching; for loop iteration; dict comprehension; generator expression; lambda expression
  Functions called: dict, filter, filter_by_value, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'filtered'
  • Function call: input_dict.items()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Assignment: right side evaluated first, then stored in 'threshold'
  • Function call: filter_by_value(input_dict, threshold)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: input_dict.items()
  • Function call: print(value)
  • Function call: filter_by_value(value, 3)
  • Function call: dict(value)
  • Function call: filter(value, value)
  • Function call: input_dict.items()
  • Function call: print(value)
  • Function call: filter_by_value(value, 5)
  • Function call: dict(value)
  • Function call: input_dict.items()
  • Function call: print(value)
  • Function call: filter_by_value(value, 4)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Assignment: right side evaluated first, then stored in 'threshold'
  • Function call: filter_by_value(input_dict, threshold)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_by_value(value, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_by_value' (stored in memory, not executed yet)
  2. Assign value to variable: input_dict
  3. Assign value to variable: threshold
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_by_value' (stored in memory, not executed yet)
  7. Execute function call: print(filter_by_value(...))
  8. Define function 'filter_by_value' (stored in memory, not executed yet)
  9. Execute function call: print(filter_by_value(...))
  10. Define function 'filter_by_value' (stored in memory, not executed yet)
  11. Execute function call: print(filter_by_value(...))
  12. Define function 'filter_by_value' (stored in memory, not executed yet)
  13. Define function 'filter_by_value' (stored in memory, not executed yet)
  14. Assign value to variable: input_dict
  15. Assign value to variable: threshold
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(filter_by_value(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_by_value' is defined but NOT executed until called
    - 'filter_by_value' is defined but NOT executed until called
    - 'filter_by_value' is defined but NOT executed until called
    - 'filter_by_value' is defined but NOT executed until called
    - 'filter_by_value' is defined but NOT executed until called
    - 'filter_by_value' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over input_dict.items(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 173:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def combine_lists_to_dict(keys, values)
      Returns a value via 'return' keyword
    def combine_lists_to_dict(keys, values)
      Returns a value via 'return' keyword
    def combine_lists_to_dict(keys, values)
      Returns a value via 'return' keyword
    def combine_lists_to_dict(keys, values)
      Returns a value via 'return' keyword
    def combine_lists_to_dict(keys, values)
      Returns a value via 'return' keyword
    def combine_lists_to_dict(keys, values)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration; dict comprehension
  Functions called: combine_lists_to_dict, dict, len, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'combined'
  • Function call: range(value)
  • Function call: len(keys)
  • Assignment: right side evaluated first, then stored in 'keys'
  • Assignment: right side evaluated first, then stored in 'values'
  • Function call: combine_lists_to_dict(keys, values)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: dict(value)
  • Function call: zip(keys, values)
  • Function call: print(value)
  • Function call: combine_lists_to_dict(value, value)
  • Function call: range(value)
  • Function call: len(keys)
  • Function call: print(value)
  • Function call: combine_lists_to_dict(value, value)
  • Function call: zip(keys, values)
  • Function call: print(value)
  • Function call: combine_lists_to_dict(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'keys'
  • Assignment: right side evaluated first, then stored in 'values'
  • Function call: combine_lists_to_dict(keys, values)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: combine_lists_to_dict(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'combine_lists_to_dict' (stored in memory, not executed yet)
  2. Assign value to variable: keys
  3. Assign value to variable: values
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'combine_lists_to_dict' (stored in memory, not executed yet)
  7. Execute function call: print(combine_lists_to_dict(...))
  8. Define function 'combine_lists_to_dict' (stored in memory, not executed yet)
  9. Execute function call: print(combine_lists_to_dict(...))
  10. Define function 'combine_lists_to_dict' (stored in memory, not executed yet)
  11. Execute function call: print(combine_lists_to_dict(...))
  12. Define function 'combine_lists_to_dict' (stored in memory, not executed yet)
  13. Define function 'combine_lists_to_dict' (stored in memory, not executed yet)
  14. Assign value to variable: keys
  15. Assign value to variable: values
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(combine_lists_to_dict(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'combine_lists_to_dict' is defined but NOT executed until called
    - 'combine_lists_to_dict' is defined but NOT executed until called
    - 'combine_lists_to_dict' is defined but NOT executed until called
    - 'combine_lists_to_dict' is defined but NOT executed until called
    - 'combine_lists_to_dict' is defined but NOT executed until called
    - 'combine_lists_to_dict' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 174:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def max_value_key(input_dict)
      Returns a value via 'return' keyword
    def max_value_key(input_dict)
      Returns a value via 'return' keyword
    def max_value_key(input_dict)
      Returns a value via 'return' keyword
    def max_value_key(input_dict)
      Returns a value via 'return' keyword
    def max_value_key(input_dict)
      Returns a value via 'return' keyword
    def max_value_key(input_dict)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: -, ., (), ==, >=, [], not
  Control structures: for loop iteration; lambda expression
  Functions called: max, max_value_key, print, reduce, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Function call: max(value)
  • Function call: input_dict.values()
  • Assignment: right side evaluated first, then stored in 'max_value'
  • Function call: input_dict.items()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'my_dict'
  • Function call: print(value)
  • Function call: max_value_key(my_dict)
  • Assignment: right side evaluated first, then stored in 'empty_dict'
  • Function call: print(value)
  • Function call: max_value_key(empty_dict)
  • Condition branch: evaluates boolean expression first
  • Function call: max(input_dict)
  • Function call: print(value)
  • Function call: max_value_key(value)
  • Function call: print(value)
  • Function call: max_value_key(value)
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(value)
  • Function call: input_dict.items()
  • Function call: print(value)
  • Function call: max_value_key(value)
  • Condition branch: evaluates boolean expression first
  • Function call: reduce(value, input_dict)
  • Function call: print(value)
  • Function call: max_value_key(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Function call: max_value_key(input_dict)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_value_key(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_value_key' (stored in memory, not executed yet)
  2. Assign value to variable: my_dict
  3. Execute function call: print(max_value_key(...))
  4. Assign value to variable: empty_dict
  5. Execute function call: print(max_value_key(...))
  6. Define function 'max_value_key' (stored in memory, not executed yet)
  7. Execute function call: print(max_value_key(...))
  8. Execute function call: print(max_value_key(...))
  9. Define function 'max_value_key' (stored in memory, not executed yet)
  10. Execute function call: print(max_value_key(...))
  11. Define function 'max_value_key' (stored in memory, not executed yet)
  12. Execute function call: print(max_value_key(...))
  13. Define function 'max_value_key' (stored in memory, not executed yet)
  14. Define function 'max_value_key' (stored in memory, not executed yet)
  15. Assign value to variable: input_dict
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(max_value_key(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_value_key' is defined but NOT executed until called
    - 'max_value_key' is defined but NOT executed until called
    - 'max_value_key' is defined but NOT executed until called
    - 'max_value_key' is defined but NOT executed until called
    - 'max_value_key' is defined but NOT executed until called
    - 'max_value_key' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over input_dict.items(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 175:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def dict_equal(dict1, dict2)
      Returns a value via 'return' keyword
    def dict_equal(dict1, dict2)
      Returns a value via 'return' keyword
    def dict_equal(dict1, dict2)
      Returns a value via 'return' keyword
    def dict_equal(dict1, dict2)
      Returns a value via 'return' keyword
    def dict_equal(dict1, dict2)
      Returns a value via 'return' keyword
    def dict_equal(dict1, dict2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., ^, !=, (), ==, [], or, not in
  Control structures: for loop iteration; generator expression
  Functions called: all, dict_equal, len, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'dict1'
  • Assignment: right side evaluated first, then stored in 'dict2'
  • Function call: dict_equal(dict1, dict2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: dict1.keys()
  • Function call: dict2.keys()
  • Condition branch: evaluates boolean expression first
  • Function call: all(value)
  • Function call: print(value)
  • Function call: dict_equal(value, value)
  • Function call: print(value)
  • Function call: dict_equal(value, value)
  • Binary operation: set(...) ^ set(...)
  • Function call: len(computed expression)
  • Function call: set(value)
  • Function call: set(value)
  • Function call: dict1.items()
  • Function call: dict2.items()
  • Function call: print(value)
  • Function call: dict_equal(value, value)
  • Function call: print(value)
  • Function call: dict_equal(value, value)
  • Function call: len(dict1)
  • Function call: len(dict2)
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: dict_equal(value, value)
  • Function call: print(value)
  • Function call: dict_equal(value, value)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'dict1'
  • Assignment: right side evaluated first, then stored in 'dict2'
  • Function call: dict_equal(dict1, dict2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: dict_equal(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'dict_equal' (stored in memory, not executed yet)
  2. Assign value to variable: dict1
  3. Assign value to variable: dict2
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'dict_equal' (stored in memory, not executed yet)
  7. Execute function call: print(dict_equal(...))
  8. Execute function call: print(dict_equal(...))
  9. Define function 'dict_equal' (stored in memory, not executed yet)
  10. Execute function call: print(dict_equal(...))
  11. Execute function call: print(dict_equal(...))
  12. Define function 'dict_equal' (stored in memory, not executed yet)
  13. Execute function call: print(dict_equal(...))
  14. Execute function call: print(dict_equal(...))
  15. Define function 'dict_equal' (stored in memory, not executed yet)
  16. Define function 'dict_equal' (stored in memory, not executed yet)
  17. Assign value to variable: dict1
  18. Assign value to variable: dict2
  19. Assign value to variable: result
  20. Execute function call: print(result)
  21. Execute function call: print(dict_equal(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'dict_equal' is defined but NOT executed until called
    - 'dict_equal' is defined but NOT executed until called
    - 'dict_equal' is defined but NOT executed until called
    - 'dict_equal' is defined but NOT executed until called
    - 'dict_equal' is defined but NOT executed until called
    - 'dict_equal' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over dict1, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 176:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: Counter, dict, print, word_frequency
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: word_frequency(string)
  • Function call: dict(value)
  • Function call: Counter(value)
  • Function call: string.split()
  • Function call: print(value)
  • Function call: word_frequency('gore why are you here you are gore')
  • Assignment: right side evaluated first, then stored in 'freq'
  • Function call: string.split()
  • Binary operation: freq.get(...) + 1
  • Function call: freq.get(word, 0)
  • Function call: print(value)
  • Function call: word_frequency('one two one two three')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: word_frequency(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: word_frequency('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'word_frequency' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(word_frequency(...))
  4. Define function 'word_frequency' (stored in memory, not executed yet)
  5. Execute function call: print(word_frequency(...))
  6. Define function 'word_frequency' (stored in memory, not executed yet)
  7. Execute function call: print(word_frequency(...))
  8. Define function 'word_frequency' (stored in memory, not executed yet)
  9. Define function 'word_frequency' (stored in memory, not executed yet)
  10. Assign value to variable: string
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(word_frequency(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string.split(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 177:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def merge_list_of_dicts(dicts)
      Returns a value via 'return' keyword
    def merge_list_of_dicts(dicts)
      Returns a value via 'return' keyword
    def merge_list_of_dicts(dicts)
      Returns a value via 'return' keyword
    def merge_list_of_dicts(dicts)
      Returns a value via 'return' keyword
    def merge_list_of_dicts(dicts)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., ()
  Control structures: for loop iteration; lambda expression
  Functions called: merge_list_of_dicts, print, reduce
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'merged'
  • Function call: merged.update(d)
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Function call: merge_list_of_dicts(dicts)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'merged'
  • Assignment: right side evaluated first, then stored in 'merged'
  • Function call: print(value)
  • Function call: merge_list_of_dicts(value)
  • Function call: reduce(value, dicts, value)
  • Function call: print(value)
  • Function call: merge_list_of_dicts(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Function call: merge_list_of_dicts(dicts)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: merge_list_of_dicts(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'merge_list_of_dicts' (stored in memory, not executed yet)
  2. Assign value to variable: dicts
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'merge_list_of_dicts' (stored in memory, not executed yet)
  6. Execute function call: print(merge_list_of_dicts(...))
  7. Define function 'merge_list_of_dicts' (stored in memory, not executed yet)
  8. Execute function call: print(merge_list_of_dicts(...))
  9. Define function 'merge_list_of_dicts' (stored in memory, not executed yet)
  10. Define function 'merge_list_of_dicts' (stored in memory, not executed yet)
  11. Assign value to variable: dicts
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(merge_list_of_dicts(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'merge_list_of_dicts' is defined but NOT executed until called
    - 'merge_list_of_dicts' is defined but NOT executed until called
    - 'merge_list_of_dicts' is defined but NOT executed until called
    - 'merge_list_of_dicts' is defined but NOT executed until called
    - 'merge_list_of_dicts' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over dicts, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over dicts, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 178:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def remove_duplicates_from_values(input_dict)
      Returns a value via 'return' keyword
    def remove_duplicates_from_values(input_dict)
      Returns a value via 'return' keyword
    def remove_duplicates_from_values(input_dict)
      Returns a value via 'return' keyword
    def remove_duplicates_from_values(input_dict)
      Returns a value via 'return' keyword
    def remove_duplicates_from_values(input_dict)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration; dict comprehension
  Functions called: list, print, remove_duplicates_from_values, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'new_dict'
  • Function call: input_dict.items()
  • Function call: list(value)
  • Function call: set(value)
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Function call: remove_duplicates_from_values(input_dict)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: set(v)
  • Function call: input_dict.items()
  • Function call: print(value)
  • Function call: remove_duplicates_from_values(value)
  • Function call: list(value)
  • Function call: OrderedDict.fromkeys(v)
  • Function call: input_dict.items()
  • Function call: print(value)
  • Function call: remove_duplicates_from_values(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'input_dict'
  • Function call: remove_duplicates_from_values(input_dict)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates_from_values(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates_from_values' (stored in memory, not executed yet)
  2. Assign value to variable: input_dict
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'remove_duplicates_from_values' (stored in memory, not executed yet)
  6. Execute function call: print(remove_duplicates_from_values(...))
  7. Define function 'remove_duplicates_from_values' (stored in memory, not executed yet)
  8. Execute function call: print(remove_duplicates_from_values(...))
  9. Define function 'remove_duplicates_from_values' (stored in memory, not executed yet)
  10. Define function 'remove_duplicates_from_values' (stored in memory, not executed yet)
  11. Assign value to variable: input_dict
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(remove_duplicates_from_values(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates_from_values' is defined but NOT executed until called
    - 'remove_duplicates_from_values' is defined but NOT executed until called
    - 'remove_duplicates_from_values' is defined but NOT executed until called
    - 'remove_duplicates_from_values' is defined but NOT executed until called
    - 'remove_duplicates_from_values' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over input_dict.items(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 179:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def common_keys_max_values(dict1, dict2)
      Returns a value via 'return' keyword
    def common_keys_max_values(dict1, dict2)
      Returns a value via 'return' keyword
    def common_keys_max_values(dict1, dict2)
      Returns a value via 'return' keyword
    def common_keys_max_values(dict1, dict2)
      Returns a value via 'return' keyword
    def common_keys_max_values(dict1, dict2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: &, ., (), []
  Control structures: for loop iteration; dict comprehension
  Functions called: common_keys_max_values, max, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'common_keys'
  • Binary operation: dict1.keys(...) & dict2.keys(...)
  • Function call: dict1.keys()
  • Function call: dict2.keys()
  • Function call: max(value, value)
  • Assignment: right side evaluated first, then stored in 'dict1'
  • Assignment: right side evaluated first, then stored in 'dict2'
  • Function call: common_keys_max_values(dict1, dict2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: dict1.keys(...) & dict2.keys(...)
  • Function call: max(value, value)
  • Function call: dict1.keys()
  • Function call: dict2.keys()
  • Function call: print(value)
  • Function call: common_keys_max_values(value, value)
  • Binary operation: set(...) & set(...)
  • Function call: set(dict1)
  • Function call: set(dict2)
  • Assignment: right side evaluated first, then stored in 'common'
  • Function call: max(value, value)
  • Function call: print(value)
  • Function call: common_keys_max_values(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dict1'
  • Assignment: right side evaluated first, then stored in 'dict2'
  • Function call: common_keys_max_values(dict1, dict2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: common_keys_max_values(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common_keys_max_values' (stored in memory, not executed yet)
  2. Assign value to variable: dict1
  3. Assign value to variable: dict2
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'common_keys_max_values' (stored in memory, not executed yet)
  7. Execute function call: print(common_keys_max_values(...))
  8. Define function 'common_keys_max_values' (stored in memory, not executed yet)
  9. Execute function call: print(common_keys_max_values(...))
  10. Define function 'common_keys_max_values' (stored in memory, not executed yet)
  11. Define function 'common_keys_max_values' (stored in memory, not executed yet)
  12. Assign value to variable: dict1
  13. Assign value to variable: dict2
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(common_keys_max_values(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'common_keys_max_values' is defined but NOT executed until called
    - 'common_keys_max_values' is defined but NOT executed until called
    - 'common_keys_max_values' is defined but NOT executed until called
    - 'common_keys_max_values' is defined but NOT executed until called
    - 'common_keys_max_values' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 180:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def average_values(dicts)
      Returns a value via 'return' keyword
    def average_values(dicts)
      Returns a value via 'return' keyword
    def average_values(dicts)
      Returns a value via 'return' keyword
    def average_values(dicts)
      Returns a value via 'return' keyword
    def average_values(dicts)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., /, (), [], in
  Control structures: if/else conditional branching; for loop iteration; dict comprehension; generator expression
  Functions called: average_values, defaultdict, print, set, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'averages'
  • Assignment: right side evaluated first, then stored in 'counts'
  • Function call: d.items()
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Function call: average_values(dicts)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: defaultdict(float)
  • Assignment: right side evaluated first, then stored in 'sums'
  • Function call: defaultdict(int)
  • Assignment: right side evaluated first, then stored in 'counts'
  • Function call: d.items()
  • Binary operation: expression / expression
  • Function call: print(value)
  • Function call: average_values(value)
  • Function call: set(...).union(value)
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'all_keys'
  • Binary operation: sum(...) / sum(...)
  • Function call: sum(value)
  • Function call: sum(value)
  • Function call: d.get(k, 0)
  • Function call: print(value)
  • Function call: average_values(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Function call: average_values(dicts)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average_values(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average_values' (stored in memory, not executed yet)
  2. Assign value to variable: dicts
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'average_values' (stored in memory, not executed yet)
  6. Execute function call: print(average_values(...))
  7. Define function 'average_values' (stored in memory, not executed yet)
  8. Execute function call: print(average_values(...))
  9. Define function 'average_values' (stored in memory, not executed yet)
  10. Define function 'average_values' (stored in memory, not executed yet)
  11. Assign value to variable: dicts
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(average_values(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average_values' is defined but NOT executed until called
    - 'average_values' is defined but NOT executed until called
    - 'average_values' is defined but NOT executed until called
    - 'average_values' is defined but NOT executed until called
    - 'average_values' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over dicts, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over averages, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over dicts, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over d.items(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over d.items(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 181:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def merge_dicts_with_lists(dicts)
      Returns a value via 'return' keyword
    def merge_dicts_with_lists(dicts)
      Returns a value via 'return' keyword
    def merge_dicts_with_lists(dicts)
      Returns a value via 'return' keyword
    def merge_dicts_with_lists(dicts)
      Returns a value via 'return' keyword
    def merge_dicts_with_lists(dicts)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], in
  Control structures: if/else conditional branching; for loop iteration; dict comprehension; generator expression
  Functions called: defaultdict, list, merge_dicts_with_lists, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'merged'
  • Function call: d.items()
  • Condition branch: evaluates boolean expression first
  • Function call: expression.update(value)
  • Function call: set(value)
  • Function call: list(value)
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Function call: merge_dicts_with_lists(dicts)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: defaultdict(set)
  • Assignment: right side evaluated first, then stored in 'merged'
  • Function call: d.items()
  • Function call: expression.update(v)
  • Function call: list(v)
  • Function call: merged.items()
  • Function call: print(value)
  • Function call: merge_dicts_with_lists(value)
  • Function call: set(value)
  • Assignment: right side evaluated first, then stored in 'all_keys'
  • Function call: list(value)
  • Function call: set(value)
  • Function call: print(value)
  • Function call: merge_dicts_with_lists(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Function call: merge_dicts_with_lists(dicts)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: merge_dicts_with_lists(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'merge_dicts_with_lists' (stored in memory, not executed yet)
  2. Assign value to variable: dicts
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'merge_dicts_with_lists' (stored in memory, not executed yet)
  6. Execute function call: print(merge_dicts_with_lists(...))
  7. Define function 'merge_dicts_with_lists' (stored in memory, not executed yet)
  8. Execute function call: print(merge_dicts_with_lists(...))
  9. Define function 'merge_dicts_with_lists' (stored in memory, not executed yet)
  10. Define function 'merge_dicts_with_lists' (stored in memory, not executed yet)
  11. Assign value to variable: dicts
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(merge_dicts_with_lists(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'merge_dicts_with_lists' is defined but NOT executed until called
    - 'merge_dicts_with_lists' is defined but NOT executed until called
    - 'merge_dicts_with_lists' is defined but NOT executed until called
    - 'merge_dicts_with_lists' is defined but NOT executed until called
    - 'merge_dicts_with_lists' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over dicts, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over merged, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over dicts, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over d.items(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over d.items(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 182:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def group_by_first_letter(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def group_by_first_letter(lst)
      Returns a value via 'return' keyword
    def group_by_first_letter(lst)
      Returns a value via 'return' keyword
    def group_by_first_letter(lst)
      Returns a value via 'return' keyword
    def group_by_first_letter(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], not in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: defaultdict, dict, group_by_first_letter, main, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first_letter'
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: group_by_first_letter(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'key'
  • Assignment: right side evaluated first, then stored in 'value'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.setdefault(...).append(word)
  • Function call: result.setdefault(value, value)
  • Function call: print(value)
  • Function call: group_by_first_letter(value)
  • Function call: defaultdict(list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: expression.append(word)
  • Function call: dict(result)
  • Function call: print(value)
  • Function call: group_by_first_letter(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: group_by_first_letter(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: group_by_first_letter(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(group_by_first_letter(...))
  4. Define function 'main' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: main(lst)
  7. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  8. Execute function call: print(group_by_first_letter(...))
  9. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  10. Execute function call: print(group_by_first_letter(...))
  11. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  12. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  13. Assign value to variable: lst
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(group_by_first_letter(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'group_by_first_letter' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'group_by_first_letter' is defined but NOT executed until called
    - 'group_by_first_letter' is defined but NOT executed until called
    - 'group_by_first_letter' is defined but NOT executed until called
    - 'group_by_first_letter' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 183:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
    def is_anagram(string1, string2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., !=, (), ==, [], in
  Control structures: for loop iteration; generator expression
  Functions called: Counter, all, input, is_anagram, len, print, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string1.replace(...).lower()
  • Function call: string1.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: string2.replace(...).lower()
  • Function call: string2.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: len(string1)
  • Function call: len(string2)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'char_count1'
  • Assignment: right side evaluated first, then stored in 'char_count2'
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Function call: input('Enter the first string: ')
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: input('Enter the second string: ')
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: print('Are the strings anagrams?:', value)
  • Function call: is_anagram(string1, string2)
  • Function call: Counter(value)
  • Function call: Counter(value)
  • Function call: string1.replace(...).lower()
  • Function call: string2.replace(...).lower()
  • Function call: string1.replace(' ', '')
  • Function call: string2.replace(' ', '')
  • Function call: print(value)
  • Function call: is_anagram('listen', 'silent')
  • Function call: string1.replace(...).lower()
  • Function call: string2.replace(...).lower()
  • Function call: string1.replace(' ', '')
  • Function call: string2.replace(' ', '')
  • Function call: len(s1)
  • Function call: len(s2)
  • Condition branch: evaluates boolean expression first
  • Function call: all(value)
  • Function call: s1.count(c)
  • Function call: s2.count(c)
  • Function call: set(s1)
  • Function call: print(value)
  • Function call: is_anagram('hello', 'olleh')
  • Function call: print(value)
  • Function call: is_anagram('test', 'tent')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string1'
  • Assignment: right side evaluated first, then stored in 'string2'
  • Function call: is_anagram(string1, string2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_anagram('world', 'python')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_anagram' (stored in memory, not executed yet)
  2. Assign value to variable: string1
  3. Assign value to variable: string2
  4. Execute function call: print('Are the strings anagrams?:', is_anagram(...))
  5. Define function 'is_anagram' (stored in memory, not executed yet)
  6. Execute function call: print(is_anagram(...))
  7. Define function 'is_anagram' (stored in memory, not executed yet)
  8. Execute function call: print(is_anagram(...))
  9. Execute function call: print(is_anagram(...))
  10. Define function 'is_anagram' (stored in memory, not executed yet)
  11. Define function 'is_anagram' (stored in memory, not executed yet)
  12. Assign value to variable: string1
  13. Assign value to variable: string2
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(is_anagram(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
    - 'is_anagram' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string2, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 184:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def int(number)
      Returns a value via 'return' keyword
    def extract_digit(number)
      Returns a value via 'return' keyword
    def get_digit(n)
      Returns a value via 'return' keyword
    def int(number)
      Returns a value via 'return' keyword
    def int(number)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, (), //, []
  Functions called: extract_digit, get_digit, int, list, print, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: list(number)
  • Assignment: right side evaluated first, then stored in 'list_number'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: int(number)
  • Binary operation: expression % 10
  • Binary operation: number // 10
  • Function call: print(value)
  • Function call: extract_digit(123)
  • Function call: str(n)
  • Function call: print(value)
  • Function call: get_digit(123)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: int(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'int' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Execute function call: print(int(...))
  4. Define function 'extract_digit' (stored in memory, not executed yet)
  5. Execute function call: print(extract_digit(...))
  6. Define function 'get_digit' (stored in memory, not executed yet)
  7. Execute function call: print(get_digit(...))
  8. Define function 'int' (stored in memory, not executed yet)
  9. Define function 'int' (stored in memory, not executed yet)
  10. Assign value to variable: number
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(int(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'int' is defined but NOT executed until called
    - 'extract_digit' is defined but NOT executed until called
    - 'get_digit' is defined but NOT executed until called
    - 'int' is defined but NOT executed until called
    - 'int' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 185:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def int(number)
      Returns a value via 'return' keyword
    def extract_digits(number)
      Returns a value via 'return' keyword
    def get_digits(num)
      Returns a value via 'return' keyword
    def int(number)
      Returns a value via 'return' keyword
    def int(number)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, (), []
  Control structures: generator expression
  Functions called: extract_digits, get_digits, int, print, str, tuple
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Assignment: right side evaluated first, then stored in 'num2'
  • Assignment: right side evaluated first, then stored in 'num4'
  • Assignment: right side evaluated first, then stored in 'num8'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: int(number)
  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: print(value)
  • Function call: extract_digits(12345678)
  • Function call: str(num)
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: tuple(value)
  • Function call: print(value)
  • Function call: get_digits(12345678)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: int(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'int' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Execute function call: print(int(...))
  4. Define function 'extract_digits' (stored in memory, not executed yet)
  5. Execute function call: print(extract_digits(...))
  6. Define function 'get_digits' (stored in memory, not executed yet)
  7. Execute function call: print(get_digits(...))
  8. Define function 'int' (stored in memory, not executed yet)
  9. Define function 'int' (stored in memory, not executed yet)
  10. Assign value to variable: number
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(int(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'int' is defined but NOT executed until called
    - 'extract_digits' is defined but NOT executed until called
    - 'get_digits' is defined but NOT executed until called
    - 'int' is defined but NOT executed until called
    - 'int' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 186:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def get_digits(number, indices)
      Returns a value via 'return' keyword
    def get_digits(number, indices)
      Returns a value via 'return' keyword
    def get_digits(number, indices)
      Returns a value via 'return' keyword
    def get_digits(number, indices)
      Returns a value via 'return' keyword
    def get_digits(number, indices)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., (), []
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: get_digits, itemgetter, list, map, print, str, tuple
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: str(number)
  • Assignment: right side evaluated first, then stored in 'number_str'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(value)
  • Function call: tuple(result)
  • Assignment: right side evaluated first, then stored in 'number'
  • Assignment: right side evaluated first, then stored in 'indices'
  • Function call: print(value)
  • Function call: get_digits(number, indices)
  • Function call: tuple(value)
  • Function call: str(number)
  • Function call: print(value)
  • Function call: get_digits(12345678910, value)
  • Function call: itemgetter(...)(value)
  • Function call: itemgetter(value)
  • Function call: str(number)
  • Function call: print(value)
  • Function call: get_digits(12345678910, value)
  • Function call: list(value)
  • Function call: map(value, indices)
  • Assignment: right side evaluated first, then stored in 'number'
  • Assignment: right side evaluated first, then stored in 'indices'
  • Function call: get_digits(number, indices)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: get_digits(25, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'get_digits' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Assign value to variable: indices
  4. Execute function call: print(get_digits(...))
  5. Define function 'get_digits' (stored in memory, not executed yet)
  6. Execute function call: print(get_digits(...))
  7. Define function 'get_digits' (stored in memory, not executed yet)
  8. Execute function call: print(get_digits(...))
  9. Define function 'get_digits' (stored in memory, not executed yet)
  10. Define function 'get_digits' (stored in memory, not executed yet)
  11. Assign value to variable: number
  12. Assign value to variable: indices
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(get_digits(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'get_digits' is defined but NOT executed until called
    - 'get_digits' is defined but NOT executed until called
    - 'get_digits' is defined but NOT executed until called
    - 'get_digits' is defined but NOT executed until called
    - 'get_digits' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over indices, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 187:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def double_elements(string1)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(lst)
      Returns a value via 'return' keyword
    def double_elements(string1)
      Returns a value via 'return' keyword
    def double_elements(string1)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: double_elements, list, map, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'double'
  • Function call: double.append(word)
  • Function call: double.append(word)
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: print(value)
  • Function call: double_elements(string1)
  • Function call: range(2)
  • Function call: print(value)
  • Function call: double_elements(value)
  • Function call: list(value)
  • Function call: chain.from_iterable(value)
  • Function call: print(value)
  • Function call: double_elements(value)
  • Function call: list(value)
  • Function call: map(value, string1)
  • Assignment: right side evaluated first, then stored in 'string1'
  • Function call: double_elements(string1)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: double_elements('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'double_elements' (stored in memory, not executed yet)
  2. Assign value to variable: string1
  3. Execute function call: print(double_elements(...))
  4. Define function 'double_elements' (stored in memory, not executed yet)
  5. Execute function call: print(double_elements(...))
  6. Define function 'double_elements' (stored in memory, not executed yet)
  7. Execute function call: print(double_elements(...))
  8. Define function 'double_elements' (stored in memory, not executed yet)
  9. Define function 'double_elements' (stored in memory, not executed yet)
  10. Assign value to variable: string1
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(double_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
    - 'double_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string1, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 188:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def get_initials(name)
      Returns a value via 'return' keyword
    def get_initials(name)
      Returns a value via 'return' keyword
    def get_initials(name)
      Returns a value via 'return' keyword
    def get_initials(name)
      Returns a value via 'return' keyword
    def get_initials(name)
      Returns a value via 'return' keyword
    def get_initials(name)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], in
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: get_initials, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(name)
  • Assignment: right side evaluated first, then stored in 'name_list'
  • Function call: expression.upper()
  • Function call: expression.upper()
  • Function call: expression.upper()
  • Function call: expression.upper()
  • Function call: ''.join(name_list)
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: print(value)
  • Function call: get_initials(name)
  • Assignment: right side evaluated first, then stored in 'initials'
  • Condition branch: evaluates boolean expression first
  • Function call: initials.append(value)
  • Function call: char.upper()
  • Function call: initials.append(char)
  • Function call: ''.join(initials)
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: print(value)
  • Function call: get_initials(name)
  • Function call: str.maketrans('nall', 'NALL')
  • Assignment: right side evaluated first, then stored in 'trans'
  • Function call: name.translate(trans)
  • Function call: print(value)
  • Function call: get_initials('jonathan noll')
  • Function call: ''.join(value)
  • Function call: c.upper()
  • Function call: print(value)
  • Function call: get_initials('jonathan noll')
  • Function call: ''.join(name_list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: ''.join(name_list)
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: get_initials(name)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: get_initials('Bob')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'get_initials' (stored in memory, not executed yet)
  2. Assign value to variable: name
  3. Execute function call: print(get_initials(...))
  4. Define function 'get_initials' (stored in memory, not executed yet)
  5. Assign value to variable: name
  6. Execute function call: print(get_initials(...))
  7. Define function 'get_initials' (stored in memory, not executed yet)
  8. Execute function call: print(get_initials(...))
  9. Define function 'get_initials' (stored in memory, not executed yet)
  10. Execute function call: print(get_initials(...))
  11. Define function 'get_initials' (stored in memory, not executed yet)
  12. Define function 'get_initials' (stored in memory, not executed yet)
  13. Assign value to variable: name
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(get_initials(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
    - 'get_initials' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over name, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 189:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_digits()
    def digits_string()
    def sum_digits(s)
      Returns a value via 'return' keyword
    def sum_digits(s)
      Returns a value via 'return' keyword
    def sum_digits()
      Returns a value via 'return' keyword
    def sum_digits()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: +, ., ()
  Control structures: if/else conditional branching; for loop iteration; generator expression; lambda expression
  Functions called: digits_string, filter, input, int, list, print, reduce, sum, sum_digits
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: int(number)
  • Function call: print(count)
  • Function call: sum_digits()
  • Function call: input('Type a string containing numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: char.isdigit()
  • Condition branch: evaluates boolean expression first
  • Function call: int(char)
  • Function call: print(total)
  • Function call: digits_string()
  • Function call: sum(value)
  • Function call: int(c)
  • Function call: c.isdigit()
  • Function call: print(value)
  • Function call: sum_digits('1234')
  • Function call: print(value)
  • Function call: sum_digits('a1b2c3')
  • Binary operation: a + int(...)
  • Function call: reduce(value, value, 0)
  • Function call: filter(value, s)
  • Function call: int(c)
  • Function call: print(value)
  • Function call: sum_digits('45x67')
  • Function call: list(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: int(number)
  • Function call: list(value)
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: sum_digits(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_digits(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_digits' (stored in memory, not executed yet)
  2. Execute function call: sum_digits()
  3. Define function 'digits_string' (stored in memory, not executed yet)
  4. Execute function call: digits_string()
  5. Define function 'sum_digits' (stored in memory, not executed yet)
  6. Execute function call: print(sum_digits(...))
  7. Execute function call: print(sum_digits(...))
  8. Define function 'sum_digits' (stored in memory, not executed yet)
  9. Execute function call: print(sum_digits(...))
  10. Define function 'sum_digits' (stored in memory, not executed yet)
  11. Define function 'sum_digits' (stored in memory, not executed yet)
  12. Assign value to variable: s
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(sum_digits(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_digits' is defined but NOT executed until called
    - 'digits_string' is defined but NOT executed until called
    - 'sum_digits' is defined but NOT executed until called
    - 'sum_digits' is defined but NOT executed until called
    - 'sum_digits' is defined but NOT executed until called
    - 'sum_digits' is defined but NOT executed until called
  Runtime execution (top-level code):
    - sum_digits() is invoked
      → Execution jumps to the function body of 'sum_digits'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - digits_string() is invoked
      → Execution jumps to the function body of 'digits_string'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 190:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def remove_duplicate()
    def remove_duplicate(s)
      Returns a value via 'return' keyword
    def remove_duplicate(s)
      Returns a value via 'return' keyword
    def remove_duplicate()
      Returns a value via 'return' keyword
    def remove_duplicate()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), not in
  Control structures: for loop iteration
  Functions called: input, print, remove_duplicate
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'duplicate'
  • Condition branch: evaluates boolean expression first
  • Function call: duplicate.append(char)
  • Function call: print(duplicate)
  • Function call: remove_duplicate()
  • Function call: ''.join(value)
  • Function call: dict.fromkeys(s)
  • Function call: print(value)
  • Function call: remove_duplicate('hello')
  • Function call: ''.join(value)
  • Function call: OrderedDict.fromkeys(s)
  • Function call: print(value)
  • Function call: remove_duplicate('banana')
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'duplicate'
  • Condition branch: evaluates boolean expression first
  • Function call: duplicate.append(char)
  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'duplicate'
  • Condition branch: evaluates boolean expression first
  • Function call: duplicate.append(char)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: remove_duplicate(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicate(10)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicate' (stored in memory, not executed yet)
  2. Execute function call: remove_duplicate()
  3. Define function 'remove_duplicate' (stored in memory, not executed yet)
  4. Execute function call: print(remove_duplicate(...))
  5. Define function 'remove_duplicate' (stored in memory, not executed yet)
  6. Execute function call: print(remove_duplicate(...))
  7. Define function 'remove_duplicate' (stored in memory, not executed yet)
  8. Define function 'remove_duplicate' (stored in memory, not executed yet)
  9. Assign value to variable: s
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(remove_duplicate(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicate' is defined but NOT executed until called
    - 'remove_duplicate' is defined but NOT executed until called
    - 'remove_duplicate' is defined but NOT executed until called
    - 'remove_duplicate' is defined but NOT executed until called
    - 'remove_duplicate' is defined but NOT executed until called
  Runtime execution (top-level code):
    - remove_duplicate() is invoked
      → Execution jumps to the function body of 'remove_duplicate'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 191:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_min()
    def find_min(nums)
      Returns a value via 'return' keyword
    def find_min(nums)
      Returns a value via 'return' keyword
    def find_min()
      Returns a value via 'return' keyword
    def find_min()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., <, (), []
  Control structures: for loop iteration; lambda expression
  Functions called: find_min, input, int, min, print, reduce
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Function call: print(min_number)
  • Function call: find_min()
  • Function call: min(nums)
  • Function call: print(value)
  • Function call: find_min(value)
  • Function call: reduce(value, nums)
  • Function call: print(value)
  • Function call: find_min(value)
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: find_min(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min' (stored in memory, not executed yet)
  2. Execute function call: find_min()
  3. Define function 'find_min' (stored in memory, not executed yet)
  4. Execute function call: print(find_min(...))
  5. Define function 'find_min' (stored in memory, not executed yet)
  6. Execute function call: print(find_min(...))
  7. Define function 'find_min' (stored in memory, not executed yet)
  8. Define function 'find_min' (stored in memory, not executed yet)
  9. Assign value to variable: nums
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_min(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
  Runtime execution (top-level code):
    - find_min() is invoked
      → Execution jumps to the function body of 'find_min'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_int, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_int, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_int, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 192:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def count_occurrences(lst, element)
      Returns a value via 'return' keyword
    def main()
      Returns a value via 'return' keyword
    def count_occurrences(lst, element)
      Returns a value via 'return' keyword
    def count_occurrences(lst, element)
      Returns a value via 'return' keyword
    def count_occurrences(lst, element)
      Returns a value via 'return' keyword
    def count_occurrences(lst, element)
      Returns a value via 'return' keyword
    def count_occurrences(lst, element)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), ==, []
  Control structures: for loop iteration; lambda expression
  Functions called: Counter, count_occurrences, filter, input, len, list, main, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'test_list'
  • Assignment: right side evaluated first, then stored in 'element_to_count'
  • Function call: print('Number of occurrences of', element_to_count, ':', value)
  • Function call: count_occurrences(test_list, element_to_count)
  • Function call: input(...).lower()
  • Function call: input('type element: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: word.lower()
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: main()
  • Function call: lst.count(element)
  • Function call: print(value)
  • Function call: count_occurrences(value, 1)
  • Function call: Counter(lst)
  • Function call: print(value)
  • Function call: count_occurrences(value, 'a')
  • Function call: len(value)
  • Function call: list(value)
  • Function call: filter(value, lst)
  • Function call: print(value)
  • Function call: count_occurrences(value, 1)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'element'
  • Function call: count_occurrences(lst, element)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_occurrences(value, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_occurrences' (stored in memory, not executed yet)
  2. Assign value to variable: test_list
  3. Assign value to variable: element_to_count
  4. Execute function call: print('Number of occurrences of', element_to_count, ':', count_occurrences(...))
  5. Define function 'main' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Execute function call: print(main(...))
  8. Define function 'count_occurrences' (stored in memory, not executed yet)
  9. Execute function call: print(count_occurrences(...))
  10. Define function 'count_occurrences' (stored in memory, not executed yet)
  11. Execute function call: print(count_occurrences(...))
  12. Define function 'count_occurrences' (stored in memory, not executed yet)
  13. Execute function call: print(count_occurrences(...))
  14. Define function 'count_occurrences' (stored in memory, not executed yet)
  15. Define function 'count_occurrences' (stored in memory, not executed yet)
  16. Assign value to variable: lst
  17. Assign value to variable: element
  18. Assign value to variable: result
  19. Execute function call: print(result)
  20. Execute function call: print(count_occurrences(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_occurrences' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 193:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def find_min_num()
    def find_min_num(nums)
      Returns a value via 'return' keyword
    def find_min_num(nums)
      Returns a value via 'return' keyword
    def find_min_num()
      Returns a value via 'return' keyword
    def find_min_num()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration
  Functions called: find_min_num, input, int, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: print(min_num)
  • Function call: find_min_num()
  • Function call: sorted(nums)
  • Function call: print(value)
  • Function call: find_min_num(value)
  • Function call: sorted(...).pop(0)
  • Function call: sorted(nums)
  • Function call: print(value)
  • Function call: find_min_num(value)
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: input(...).split()
  • Function call: input('type number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Function call: sorted(int_number)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: find_min_num(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min_num(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min_num' (stored in memory, not executed yet)
  2. Execute function call: find_min_num()
  3. Define function 'find_min_num' (stored in memory, not executed yet)
  4. Execute function call: print(find_min_num(...))
  5. Define function 'find_min_num' (stored in memory, not executed yet)
  6. Execute function call: print(find_min_num(...))
  7. Define function 'find_min_num' (stored in memory, not executed yet)
  8. Define function 'find_min_num' (stored in memory, not executed yet)
  9. Assign value to variable: nums
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_min_num(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min_num' is defined but NOT executed until called
    - 'find_min_num' is defined but NOT executed until called
    - 'find_min_num' is defined but NOT executed until called
    - 'find_min_num' is defined but NOT executed until called
    - 'find_min_num' is defined but NOT executed until called
  Runtime execution (top-level code):
    - find_min_num() is invoked
      → Execution jumps to the function body of 'find_min_num'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 194:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def largest_element()
    def largest_element(words)
      Returns a value via 'return' keyword
    def largest_element(words)
      Returns a value via 'return' keyword
    def largest_element()
      Returns a value via 'return' keyword
    def largest_element()
      Returns a value via 'return' keyword
  Keywords used: def, lambda, return
  Operators used: -, ., (), >=, []
  Control structures: lambda expression
  Functions called: input, largest_element, len, max, print, reduce, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: max(user)
  • Assignment: right side evaluated first, then stored in 'largest_word'
  • Function call: print(largest_word)
  • Function call: largest_element()
  • Function call: sorted(words)
  • Function call: print(value)
  • Function call: largest_element(value)
  • Function call: reduce(value, words)
  • Function call: len(a)
  • Function call: len(b)
  • Function call: print(value)
  • Function call: largest_element(value)
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: max(user)
  • Assignment: right side evaluated first, then stored in 'largest_word'
  • Function call: input(...).split()
  • Function call: input('type list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: max(user)
  • Assignment: right side evaluated first, then stored in 'largest_word'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: largest_element(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: largest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'largest_element' (stored in memory, not executed yet)
  2. Execute function call: largest_element()
  3. Define function 'largest_element' (stored in memory, not executed yet)
  4. Execute function call: print(largest_element(...))
  5. Define function 'largest_element' (stored in memory, not executed yet)
  6. Execute function call: print(largest_element(...))
  7. Define function 'largest_element' (stored in memory, not executed yet)
  8. Define function 'largest_element' (stored in memory, not executed yet)
  9. Assign value to variable: words
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(largest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
    - 'largest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - largest_element() is invoked
      → Execution jumps to the function body of 'largest_element'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 195:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def second_largest_element()
      Returns a value via 'return' keyword
    def second_largest_element()
      Returns a value via 'return' keyword
    def second_largest_element()
      Returns a value via 'return' keyword
    def second_largest_element(words)
      Returns a value via 'return' keyword
    def second_largest_element(words)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., <, >, (), []
  Control structures: for loop iteration
  Functions called: input, len, nlargest, print, second_largest_element, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('Enter a list of words: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: len(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: print('The list must contain at least two words.')
  • Function call: sorted(user_input)
  • Assignment: right side evaluated first, then stored in 'sorted_words'
  • Function call: print(value)
  • Function call: second_largest_element()
  • Function call: input(...).split()
  • Function call: input('Enter a list of words: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: len(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: print('The list must contain at least two words.')
  • Function call: sorted(user_input)
  • Assignment: right side evaluated first, then stored in 'sorted_words'
  • Function call: print(value)
  • Function call: second_largest_element()
  • Function call: input(...).split()
  • Function call: input('Enter a list of words: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: len(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: print('less then 2')
  • Function call: sorted(user_input)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)
  • Function call: second_largest_element()
  • Function call: nlargest(2, words)
  • Function call: print(value)
  • Function call: second_largest_element(value)
  • Function call: len(words)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'first'
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: len(w)
  • Function call: len(first)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second'
  • Assignment: right side evaluated first, then stored in 'first'
  • Function call: len(w)
  • Function call: len(second)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second'
  • Function call: print(value)
  • Function call: second_largest_element(value)
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: second_largest_element(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: second_largest_element('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'second_largest_element' (stored in memory, not executed yet)
  2. Execute function call: second_largest_element()
  3. Define function 'second_largest_element' (stored in memory, not executed yet)
  4. Execute function call: second_largest_element()
  5. Define function 'second_largest_element' (stored in memory, not executed yet)
  6. Execute function call: second_largest_element()
  7. Define function 'second_largest_element' (stored in memory, not executed yet)
  8. Execute function call: print(second_largest_element(...))
  9. Define function 'second_largest_element' (stored in memory, not executed yet)
  10. Execute function call: print(second_largest_element(...))
  11. Assign value to variable: words
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(second_largest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'second_largest_element' is defined but NOT executed until called
    - 'second_largest_element' is defined but NOT executed until called
    - 'second_largest_element' is defined but NOT executed until called
    - 'second_largest_element' is defined but NOT executed until called
    - 'second_largest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - second_largest_element() is invoked
      → Execution jumps to the function body of 'second_largest_element'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - second_largest_element() is invoked
      → Execution jumps to the function body of 'second_largest_element'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - second_largest_element() is invoked
      → Execution jumps to the function body of 'second_largest_element'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 196:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, >, (), [], and
  Functions called: list, max, max_of_three, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(value)
  • Function call: list(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'three_numbers'
  • Function call: print(value)
  • Function call: max_of_three(three_numbers)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: max_of_three(value)
  • Function call: print(value)
  • Function call: max_of_three(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: max_of_three(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_of_three(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_three' (stored in memory, not executed yet)
  2. Assign value to variable: three_numbers
  3. Execute function call: print(max_of_three(...))
  4. Define function 'max_of_three' (stored in memory, not executed yet)
  5. Execute function call: print(max_of_three(...))
  6. Define function 'max_of_three' (stored in memory, not executed yet)
  7. Execute function call: print(max_of_three(...))
  8. Define function 'max_of_three' (stored in memory, not executed yet)
  9. Define function 'max_of_three' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(max_of_three(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 197:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
    def max_of_three(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, (), []
  Control structures: for loop iteration
  Functions called: max_of_three, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'max_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Assignment: right side evaluated first, then stored in 'three_numbers'
  • Function call: print(value)
  • Function call: max_of_three(three_numbers)
  • Assignment: right side evaluated first, then stored in 'max_val'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_val'
  • Function call: print(value)
  • Function call: max_of_three(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: max_of_three(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_of_three(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_three' (stored in memory, not executed yet)
  2. Assign value to variable: three_numbers
  3. Execute function call: print(max_of_three(...))
  4. Define function 'max_of_three' (stored in memory, not executed yet)
  5. Execute function call: print(max_of_three(...))
  6. Define function 'max_of_three' (stored in memory, not executed yet)
  7. Define function 'max_of_three' (stored in memory, not executed yet)
  8. Assign value to variable: numbers
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(max_of_three(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 198:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def centimeters_inches()
    def cm_to_inches(cm)
      Returns a value via 'return' keyword
    def centimeters_inches(cm)
      Returns a value via 'return' keyword
    def centimeters_inches()
      Returns a value via 'return' keyword
    def centimeters_inches()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ()
  Functions called: centimeters_inches, cm_to_inches, input, int, print, round
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input('type centimeters: ')
  • Assignment: right side evaluated first, then stored in 'cm'
  • Binary operation: cm * 0.3937
  • Assignment: right side evaluated first, then stored in 'inches'
  • Function call: print(value)
  • Function call: centimeters_inches()
  • Binary operation: cm * 0.3937
  • Function call: print(value)
  • Function call: cm_to_inches(100)
  • Binary operation: cm * 0.3937
  • Function call: round(computed expression, 2)
  • Function call: print(value)
  • Function call: centimeters_inches(50)
  • Function call: int(value)
  • Function call: input('type centimeters: ')
  • Assignment: right side evaluated first, then stored in 'cm'
  • Binary operation: cm * 0.3937
  • Assignment: right side evaluated first, then stored in 'inches'
  • Function call: int(value)
  • Function call: input('type centimeters: ')
  • Assignment: right side evaluated first, then stored in 'cm'
  • Binary operation: cm * 0.3937
  • Assignment: right side evaluated first, then stored in 'inches'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'cm'
  • Function call: cm_to_inches(cm)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: cm_to_inches(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'centimeters_inches' (stored in memory, not executed yet)
  2. Execute function call: centimeters_inches()
  3. Define function 'cm_to_inches' (stored in memory, not executed yet)
  4. Execute function call: print(value)
  5. Define function 'centimeters_inches' (stored in memory, not executed yet)
  6. Execute function call: print(centimeters_inches(...))
  7. Define function 'centimeters_inches' (stored in memory, not executed yet)
  8. Define function 'centimeters_inches' (stored in memory, not executed yet)
  9. Assign value to variable: cm
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(cm_to_inches(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'centimeters_inches' is defined but NOT executed until called
    - 'cm_to_inches' is defined but NOT executed until called
    - 'centimeters_inches' is defined but NOT executed until called
    - 'centimeters_inches' is defined but NOT executed until called
    - 'centimeters_inches' is defined but NOT executed until called
  Runtime execution (top-level code):
    - centimeters_inches() is invoked
      → Execution jumps to the function body of 'centimeters_inches'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 199:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def find_min_max()
    def find_min_max()
      Returns a value via 'return' keyword
    def find_min_max(nums)
      Returns a value via 'return' keyword
    def find_min_max(nums)
      Returns a value via 'return' keyword
    def find_min_max(nums)
      Returns a value via 'return' keyword
    def find_min_max()
      Returns a value via 'return' keyword
    def find_min_max()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: -, ., <, >, (), []
  Control structures: for loop iteration; lambda expression
  Functions called: find_min_max, input, int, max, min, print, reduce, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type list of numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_num'
  • Function call: int_num.append(value)
  • Function call: int(number)
  • Function call: min(int_num)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: max(int_num)
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Function call: print(min_num, max_num)
  • Function call: find_min_max()
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_number'
  • Function call: int_number.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Function call: print(min_number, max_number)
  • Function call: find_min_max()
  • Function call: min(nums)
  • Function call: max(nums)
  • Function call: print(value)
  • Function call: find_min_max(value)
  • Function call: sorted(nums)
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: print(value)
  • Function call: find_min_max(value)
  • Function call: reduce(value, nums, value)
  • Function call: min(value, x)
  • Function call: max(value, x)
  • Function call: print(value)
  • Function call: find_min_max(value)
  • Function call: input(...).split()
  • Function call: input('type list of numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_num'
  • Function call: int_num.append(value)
  • Function call: int(number)
  • Function call: min(int_num)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: max(int_num)
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Function call: input(...).split()
  • Function call: input('type list of numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_num'
  • Function call: int_num.append(value)
  • Function call: int(number)
  • Function call: min(int_num)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: max(int_num)
  • Assignment: right side evaluated first, then stored in 'max_num'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: find_min_max(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min_max(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min_max' (stored in memory, not executed yet)
  2. Execute function call: find_min_max()
  3. Define function 'find_min_max' (stored in memory, not executed yet)
  4. Execute function call: find_min_max()
  5. Define function 'find_min_max' (stored in memory, not executed yet)
  6. Execute function call: print(find_min_max(...))
  7. Define function 'find_min_max' (stored in memory, not executed yet)
  8. Execute function call: print(find_min_max(...))
  9. Define function 'find_min_max' (stored in memory, not executed yet)
  10. Execute function call: print(find_min_max(...))
  11. Define function 'find_min_max' (stored in memory, not executed yet)
  12. Define function 'find_min_max' (stored in memory, not executed yet)
  13. Assign value to variable: nums
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(find_min_max(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
  Runtime execution (top-level code):
    - find_min_max() is invoked
      → Execution jumps to the function body of 'find_min_max'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - find_min_max() is invoked
      → Execution jumps to the function body of 'find_min_max'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_number, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 200:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: +, ., >, (), [], in, and, not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: Counter, common_numbers, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'seen'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(number)
  • Function call: seen.append(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: common_numbers(lst)
  • Function call: Counter(...).items()
  • Function call: Counter(lst)
  • Function call: print(value)
  • Function call: common_numbers(value)
  • Function call: list(value)
  • Function call: lst.count(x)
  • Function call: print(value)
  • Function call: common_numbers(value)
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: count.get(...) + 1
  • Function call: count.get(x, 0)
  • Function call: count.items()
  • Function call: print(value)
  • Function call: common_numbers(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: common_numbers(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: common_numbers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(common_numbers(...))
  4. Define function 'common_numbers' (stored in memory, not executed yet)
  5. Execute function call: print(common_numbers(...))
  6. Define function 'common_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(common_numbers(...))
  8. Define function 'common_numbers' (stored in memory, not executed yet)
  9. Execute function call: print(common_numbers(...))
  10. Define function 'common_numbers' (stored in memory, not executed yet)
  11. Define function 'common_numbers' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(common_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 201:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def common_num(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_numbers(lst)
      Returns a value via 'return' keyword
    def common_num(lst)
      Returns a value via 'return' keyword
    def common_num(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: +, ., >, (), [], in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: Counter, common_num, common_numbers, list, map, print, set
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'unique'
  • Assignment: right side evaluated first, then stored in 'commom'
  • Condition branch: evaluates boolean expression first
  • Function call: commom.append(number)
  • Function call: unique.add(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: common_num(lst)
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'seen'
  • Function call: set()
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.add(number)
  • Function call: seen.add(number)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: common_numbers(lst)
  • Function call: list(value)
  • Function call: lst.count(x)
  • Function call: print(value)
  • Function call: common_numbers(value)
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: count.get(...) + 1
  • Function call: count.get(x, 0)
  • Function call: count.items()
  • Function call: print(value)
  • Function call: common_numbers(value)
  • Function call: Counter(...).items()
  • Function call: Counter(lst)
  • Function call: print(value)
  • Function call: common_numbers(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: common_num(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: common_num(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'common_num' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(common_num(...))
  4. Define function 'common_numbers' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(common_numbers(...))
  7. Define function 'common_numbers' (stored in memory, not executed yet)
  8. Execute function call: print(common_numbers(...))
  9. Define function 'common_numbers' (stored in memory, not executed yet)
  10. Execute function call: print(common_numbers(...))
  11. Define function 'common_numbers' (stored in memory, not executed yet)
  12. Execute function call: print(common_numbers(...))
  13. Define function 'common_num' (stored in memory, not executed yet)
  14. Define function 'common_num' (stored in memory, not executed yet)
  15. Assign value to variable: lst
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(common_num(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'common_num' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_numbers' is defined but NOT executed until called
    - 'common_num' is defined but NOT executed until called
    - 'common_num' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 202:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
    def word_frequency(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: Counter, dict, print, sorted, word_frequency
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: string.replace(' ', '')
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: sorted(string)
  • Assignment: right side evaluated first, then stored in 'sort_char'
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: word_frequency(string)
  • Function call: dict(value)
  • Function call: sorted(value)
  • Function call: Counter(...).items()
  • Function call: Counter(value)
  • Function call: string.replace(' ', '')
  • Function call: print(value)
  • Function call: word_frequency('hello world')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: string.replace(' ', '')
  • Binary operation: result.get(...) + 1
  • Function call: result.get(c, 0)
  • Function call: dict(value)
  • Function call: sorted(value)
  • Function call: result.items()
  • Function call: print(value)
  • Function call: word_frequency('aabbc')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: word_frequency(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: word_frequency('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'word_frequency' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(word_frequency(...))
  4. Define function 'word_frequency' (stored in memory, not executed yet)
  5. Execute function call: print(word_frequency(...))
  6. Define function 'word_frequency' (stored in memory, not executed yet)
  7. Execute function call: print(word_frequency(...))
  8. Define function 'word_frequency' (stored in memory, not executed yet)
  9. Define function 'word_frequency' (stored in memory, not executed yet)
  10. Assign value to variable: string
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(word_frequency(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
    - 'word_frequency' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over sort_char, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over string.replace(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 203:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def total(numbers)
      Returns a value via 'return' keyword
    def total(numbers)
      Returns a value via 'return' keyword
    def total(numbers)
      Returns a value via 'return' keyword
    def total(numbers)
      Returns a value via 'return' keyword
    def total(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, ., ()
  Control structures: for loop iteration; lambda expression
  Functions called: print, reduce, sum, total
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total_numbers'
  • Assignment: right side evaluated first, then stored in 'multiply_numbers'
  • Assignment: right side evaluated first, then stored in 'lst_numbers'
  • Function call: print(value)
  • Function call: total(lst_numbers)
  • Binary operation: a * b
  • Function call: sum(numbers)
  • Function call: reduce(value, numbers, 1)
  • Function call: print(value)
  • Function call: total(value)
  • Function call: sum(numbers)
  • Function call: math.prod(numbers)
  • Function call: print(value)
  • Function call: total(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: total(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: total(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'total' (stored in memory, not executed yet)
  2. Assign value to variable: lst_numbers
  3. Execute function call: print(total(...))
  4. Define function 'total' (stored in memory, not executed yet)
  5. Execute function call: print(total(...))
  6. Define function 'total' (stored in memory, not executed yet)
  7. Execute function call: print(total(...))
  8. Define function 'total' (stored in memory, not executed yet)
  9. Define function 'total' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(total(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'total' is defined but NOT executed until called
    - 'total' is defined but NOT executed until called
    - 'total' is defined but NOT executed until called
    - 'total' is defined but NOT executed until called
    - 'total' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 204:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def list_integers(lst)
      Returns a value via 'return' keyword
    def list_integers(lst)
      Returns a value via 'return' keyword
    def list_integers(lst)
      Returns a value via 'return' keyword
    def list_integers(lst)
      Returns a value via 'return' keyword
    def list_integers(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: len, list, list_integers, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(word)
  • Function call: result.append(value)
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: list_integers(lst)
  • Function call: len(word)
  • Function call: print(value)
  • Function call: list_integers(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(word)
  • Function call: print(value)
  • Function call: list_integers(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_integers(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_integers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_integers' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(list_integers(...))
  4. Define function 'list_integers' (stored in memory, not executed yet)
  5. Execute function call: print(list_integers(...))
  6. Define function 'list_integers' (stored in memory, not executed yet)
  7. Execute function call: print(list_integers(...))
  8. Define function 'list_integers' (stored in memory, not executed yet)
  9. Define function 'list_integers' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(list_integers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_integers' is defined but NOT executed until called
    - 'list_integers' is defined but NOT executed until called
    - 'list_integers' is defined but NOT executed until called
    - 'list_integers' is defined but NOT executed until called
    - 'list_integers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 205:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def average(lst)
      Returns a value via 'return' keyword
    def average(lst)
      Returns a value via 'return' keyword
    def average(lst)
      Returns a value via 'return' keyword
    def average(lst)
      Returns a value via 'return' keyword
    def average(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ., /, ()
  Functions called: Fraction, average, float, len, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sum(lst)
  • Assignment: right side evaluated first, then stored in 'sum_lst'
  • Function call: len(lst)
  • Assignment: right side evaluated first, then stored in 'length'
  • Binary operation: sum_lst / length
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: average(lst)
  • Function call: statistics.mean(lst)
  • Function call: print(value)
  • Function call: average(value)
  • Binary operation: sum(...) / len(...)
  • Function call: sum(lst)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: average(value)
  • Binary operation: sum_lst * expression
  • Binary operation: 1 / length
  • Function call: float(value)
  • Function call: Fraction(sum_lst, length)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: average(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(average(...))
  4. Define function 'average' (stored in memory, not executed yet)
  5. Execute function call: print(average(...))
  6. Define function 'average' (stored in memory, not executed yet)
  7. Execute function call: print(average(...))
  8. Define function 'average' (stored in memory, not executed yet)
  9. Define function 'average' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
    - 'average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 206:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 1
    def max_of_three()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., >, !=, (), []
  Control structures: for loop iteration; list comprehension
  Functions called: input, int, len, max_of_three, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input(...).split()
  • Function call: input('Enter three numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: len(numbers)
  • Condition branch: evaluates boolean expression first
  • Function call: print('not enough numbers')
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(largest)
  • Function call: max_of_three()
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: len(numbers)
  • Condition branch: evaluates boolean expression first
  • Function call: print('not enough numbers')
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(largest)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: len(numbers)
  • Condition branch: evaluates boolean expression first
  • Function call: print('not enough numbers')
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(largest)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_three' (stored in memory, not executed yet)
  2. Execute function call: max_of_three()
  3. Assign value to variable: numbers
  4. If/else: conditional execution
  5. Assign value to variable: largest
  6. For loop: iterate over expression
  7. Execute function call: print(largest)
  8. Assign value to variable: numbers
  9. If/else: conditional execution
  10. Assign value to variable: largest
  11. For loop: iterate over expression
  12. Execute function call: print(largest)

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_three' is defined but NOT executed until called
  Runtime execution (top-level code):
    - max_of_three() is invoked
      → Execution jumps to the function body of 'max_of_three'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 207:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def max_of_list()
      Returns a value via 'return' keyword
    def max_of_list(nums)
      Returns a value via 'return' keyword
    def max_of_list(nums)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: -, ., >, !=, (), []
  Control structures: for loop iteration; lambda expression
  Functions called: input, int, len, max_of_list, print, reduce, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type three numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: len(user)
  • Condition branch: evaluates boolean expression first
  • Function call: print('type more numbers')
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Function call: print(max_number)
  • Function call: max_of_list()
  • Function call: sorted(nums)
  • Function call: print(value)
  • Function call: max_of_list(value)
  • Function call: reduce(value, nums)
  • Function call: print(value)
  • Function call: max_of_list(value)
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: max_of_list(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_list' (stored in memory, not executed yet)
  2. Execute function call: max_of_list()
  3. Define function 'max_of_list' (stored in memory, not executed yet)
  4. Execute function call: print(max_of_list(...))
  5. Define function 'max_of_list' (stored in memory, not executed yet)
  6. Execute function call: print(max_of_list(...))
  7. Assign value to variable: nums
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(max_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_list' is defined but NOT executed until called
    - 'max_of_list' is defined but NOT executed until called
    - 'max_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - max_of_list() is invoked
      → Execution jumps to the function body of 'max_of_list'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 208:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def max_of_list()
    def max_of_list(nums)
      Returns a value via 'return' keyword
    def max_of_list(nums)
      Returns a value via 'return' keyword
    def max_of_list()
      Returns a value via 'return' keyword
    def max_of_list()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration
  Functions called: input, int, max, max_of_list, print, sorted
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type three numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(number)
  • Function call: max(int_numbers)
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Function call: print(max_number)
  • Function call: max_of_list()
  • Function call: max(nums)
  • Function call: print(value)
  • Function call: max_of_list(value)
  • Function call: sorted(nums)
  • Function call: print(value)
  • Function call: max_of_list(value)
  • Function call: input(...).split()
  • Function call: input('type three numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(number)
  • Function call: max(int_numbers)
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Function call: input(...).split()
  • Function call: input('type three numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'int_numbers'
  • Function call: int_numbers.append(value)
  • Function call: int(number)
  • Function call: max(int_numbers)
  • Assignment: right side evaluated first, then stored in 'max_number'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: max_of_list(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_list' (stored in memory, not executed yet)
  2. Execute function call: max_of_list()
  3. Define function 'max_of_list' (stored in memory, not executed yet)
  4. Execute function call: print(max_of_list(...))
  5. Define function 'max_of_list' (stored in memory, not executed yet)
  6. Execute function call: print(max_of_list(...))
  7. Define function 'max_of_list' (stored in memory, not executed yet)
  8. Define function 'max_of_list' (stored in memory, not executed yet)
  9. Assign value to variable: nums
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(max_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_list' is defined but NOT executed until called
    - 'max_of_list' is defined but NOT executed until called
    - 'max_of_list' is defined but NOT executed until called
    - 'max_of_list' is defined but NOT executed until called
    - 'max_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - max_of_list() is invoked
      → Execution jumps to the function body of 'max_of_list'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 209:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: len, list, list_words, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'words_lengths'
  • Function call: words_lengths.append(word)
  • Function call: words_lengths.append(value)
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: list_words(lst)
  • Function call: len(word)
  • Function call: print(value)
  • Function call: list_words(value)
  • Function call: list(value)
  • Function call: chain.from_iterable(value)
  • Function call: len(w)
  • Function call: print(value)
  • Function call: list_words(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_words(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_words(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_words' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(list_words(...))
  4. Define function 'list_words' (stored in memory, not executed yet)
  5. Execute function call: print(list_words(...))
  6. Define function 'list_words' (stored in memory, not executed yet)
  7. Execute function call: print(list_words(...))
  8. Define function 'list_words' (stored in memory, not executed yet)
  9. Define function 'list_words' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(list_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 210:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
    def list_words(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: -, ., ()
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: enumerate, list, list_words, map, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'words_with_lengths'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: words_with_lengths.append(word)
  • Function call: words_with_lengths.append(count)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: list_words(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(word)
  • Function call: result.append(value)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: list_words(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: enumerate(word)
  • Function call: print(value)
  • Function call: list_words(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_words(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_words(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_words' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(list_words(...))
  4. Define function 'list_words' (stored in memory, not executed yet)
  5. Execute function call: print(list_words(...))
  6. Define function 'list_words' (stored in memory, not executed yet)
  7. Execute function call: print(list_words(...))
  8. Define function 'list_words' (stored in memory, not executed yet)
  9. Define function 'list_words' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(list_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
    - 'list_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 211:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def length_words(lst)
      Returns a value via 'return' keyword
    def length_words(lst)
      Returns a value via 'return' keyword
    def length_words(lst)
      Returns a value via 'return' keyword
    def length_words(lst)
      Returns a value via 'return' keyword
    def len_manual(s)
      Returns a value via 'return' keyword
    def length_words(lst)
      Returns a value via 'return' keyword
    def length_words(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, pass, return
  Operators used: +, ., ()
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: enumerate, len_manual, length_words, list, map, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'length'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: length.append(count)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: length_words(lst)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: length_words(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(word)
  • Binary operation: i + 1
  • Function call: result.append(computed expression)
  • Function call: print(value)
  • Function call: length_words(value)
  • Function call: len_manual(w)
  • Assignment: right side evaluated first, then stored in 'c'
  • Function call: print(value)
  • Function call: length_words(value)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: length_words(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: length_words(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'length_words' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(length_words(...))
  4. Define function 'length_words' (stored in memory, not executed yet)
  5. Execute function call: print(length_words(...))
  6. Define function 'length_words' (stored in memory, not executed yet)
  7. Execute function call: print(length_words(...))
  8. Define function 'length_words' (stored in memory, not executed yet)
  9. Define function 'len_manual' (stored in memory, not executed yet)
  10. Execute function call: print(length_words(...))
  11. Define function 'length_words' (stored in memory, not executed yet)
  12. Define function 'length_words' (stored in memory, not executed yet)
  13. Assign value to variable: lst
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(length_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'length_words' is defined but NOT executed until called
    - 'length_words' is defined but NOT executed until called
    - 'length_words' is defined but NOT executed until called
    - 'length_words' is defined but NOT executed until called
    - 'len_manual' is defined but NOT executed until called
    - 'length_words' is defined but NOT executed until called
    - 'length_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 212:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def intersection()
    def intersection(lst1, lst2)
      Returns a value via 'return' keyword
    def intersection(lst1, lst2)
      Returns a value via 'return' keyword
    def intersection()
      Returns a value via 'return' keyword
    def intersection()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), in
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: filter, input, intersection, list, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type lst1: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst2: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Function call: print(common)
  • Function call: intersection()
  • Function call: print(value)
  • Function call: intersection(value, value)
  • Function call: list(value)
  • Function call: filter(value, lst1)
  • Function call: print(value)
  • Function call: intersection(value, value)
  • Function call: input(...).split()
  • Function call: input('type lst1: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst2: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Function call: input(...).split()
  • Function call: input('type lst1: ')
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Function call: input(...).split()
  • Function call: input('type lst2: ')
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'common'
  • Condition branch: evaluates boolean expression first
  • Function call: common.append(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: intersection(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: intersection(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'intersection' (stored in memory, not executed yet)
  2. Execute function call: intersection()
  3. Define function 'intersection' (stored in memory, not executed yet)
  4. Execute function call: print(intersection(...))
  5. Define function 'intersection' (stored in memory, not executed yet)
  6. Execute function call: print(intersection(...))
  7. Define function 'intersection' (stored in memory, not executed yet)
  8. Define function 'intersection' (stored in memory, not executed yet)
  9. Assign value to variable: lst1
  10. Assign value to variable: lst2
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(intersection(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'intersection' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
    - 'intersection' is defined but NOT executed until called
  Runtime execution (top-level code):
    - intersection() is invoked
      → Execution jumps to the function body of 'intersection'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst1, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 213:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 2
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ., ()
  Control structures: for loop iteration; generator expression
  Functions called: print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(1, 6)
  • Binary operation: '* ' * i
  • Function call: print(computed expression)
  • Function call: range(1, 6)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()
  • Function call: range(1, 6)
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: range(i)
  • Function call: range(1, 6)
  • Binary operation: '* ' * i
  • Function call: print(computed expression)
  • Function call: range(1, 6)
  • Binary operation: '* ' * i
  • Function call: range(1, 6)
  • Binary operation: '* ' * i
  • Function call: print(computed expression)
  • Function call: range(1, 6)
  • Binary operation: '* ' * i
  • Function call: print(computed expression)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. For loop: iterate over range(...)
  2. For loop: iterate over range(...)
  3. For loop: iterate over range(...)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. For loop: iterate over range(...)
  7. For loop: iterate over range(...)

▸ EXECUTION FLOW

  Function definition phase:
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 214:
▸ SYNTAX HIGHLIGHTS

  Keywords used: for
  Operators used: *, +, ., ()
  Control structures: for loop iteration; generator expression
  Functions called: print, range, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(1, 6)
  • Binary operation: expression * i
  • Binary operation: str(...) + ' '
  • Function call: print(computed expression)
  • Function call: str(i)
  • Function call: range(1, 6)
  • Function call: range(i)
  • Function call: print(i)
  • Function call: print()
  • Function call: range(1, 6)
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: str(i)
  • Function call: range(i)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. For loop: iterate over range(...)
  2. For loop: iterate over range(...)
  3. For loop: iterate over range(...)

▸ EXECUTION FLOW

  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 216:
▸ SYNTAX HIGHLIGHTS

  Keywords used: for
  Operators used: +, ., ()
  Control structures: for loop iteration; generator expression
  Functions called: map, print, range, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(1, 6)
  • Binary operation: i + 1
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: str(num)
  • Function call: range(1, computed expression)
  • Function call: range(1, 6)
  • Binary operation: i + 1
  • Function call: range(1, computed expression)
  • Function call: print(j)
  • Function call: print()
  • Function call: range(1, 6)
  • Binary operation: i + 1
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: map(str, value)
  • Function call: range(1, computed expression)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. For loop: iterate over range(...)
  2. For loop: iterate over range(...)
  3. For loop: iterate over range(...)

▸ EXECUTION FLOW

  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 217:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 2
    def solve()
    def solve()
  Keywords used: def, for
  Operators used: *, -, ., ()
  Control structures: for loop iteration; generator expression
  Functions called: print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(5, 0, value)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()
  • Function call: range(5, 0, value)
  • Binary operation: '* ' * i
  • Function call: print(computed expression)
  • Function call: range(5, 0, value)
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: range(i)
  • Function call: range(5, 0, value)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()
  • Function call: range(5, 0, value)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()
  • Function call: range(5, 0, value)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()
  • Function call: range(5, 0, value)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. For loop: iterate over range(...)
  2. For loop: iterate over range(...)
  3. For loop: iterate over range(...)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. For loop: iterate over range(...)
  7. For loop: iterate over range(...)

▸ EXECUTION FLOW

  Function definition phase:
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 218:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def print_checkerboard(size)
    def print_checkerboard(size)
    def print_checkerboard(size)
    def solve(enter)
    def solve(enter)
  Keywords used: def, for, if/elif/else
  Operators used: %, *, +, ., (), ==
  Control structures: for loop iteration; generator expression
  Functions called: input, int, print, print_checkerboard, range
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(size)
  • Function call: range(size)
  • Binary operation: expression % 2
  • Binary operation: i + j
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Function call: int(value)
  • Function call: input('Enter the size of the checkerboard: ')
  • Assignment: right side evaluated first, then stored in 'size'
  • Function call: print_checkerboard(size)
  • Function call: range(size)
  • Binary operation: expression % 2
  • Binary operation: i + j
  • Function call: ' '.join(value)
  • Function call: range(size)
  • Assignment: right side evaluated first, then stored in 'row'
  • Function call: print(row)
  • Function call: print_checkerboard(4)
  • Function call: range(size)
  • Binary operation: i % 2
  • Condition branch: evaluates boolean expression first
  • Binary operation: '* ' * size
  • Function call: print(computed expression)
  • Binary operation: ' *' * size
  • Function call: print(computed expression)
  • Function call: print_checkerboard(4)
  • Function call: range(size)
  • Function call: range(size)
  • Binary operation: expression % 2
  • Binary operation: i + j
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'size'
  • Function call: print_checkerboard(size)
  • Function call: range(size)
  • Function call: range(size)
  • Binary operation: expression % 2
  • Binary operation: i + j
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'size_2'
  • Function call: print_checkerboard(size_2)
  • Assignment: right side evaluated first, then stored in 'size'
  • Function call: print_checkerboard(size)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_checkerboard(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_checkerboard' (stored in memory, not executed yet)
  2. Assign value to variable: size
  3. Execute function call: print_checkerboard(size)
  4. Define function 'print_checkerboard' (stored in memory, not executed yet)
  5. Execute function call: print_checkerboard(4)
  6. Define function 'print_checkerboard' (stored in memory, not executed yet)
  7. Execute function call: print_checkerboard(4)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: size
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(print_checkerboard(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_checkerboard' is defined but NOT executed until called
    - 'print_checkerboard' is defined but NOT executed until called
    - 'print_checkerboard' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_checkerboard() is invoked
      → Execution jumps to the function body of 'print_checkerboard'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_checkerboard() is invoked
      → Execution jumps to the function body of 'print_checkerboard'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_checkerboard() is invoked
      → Execution jumps to the function body of 'print_checkerboard'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 219:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def print_floyds_triangle(rows)
    def print_floyds_triangle(rows)
    def print_floyds_triangle(rows)
    def solve()
    def solve()
  Keywords used: def, for, while
  Operators used: +, ., (), <=
  Control structures: for loop iteration; while loop; generator expression
  Functions called: print, print_floyds_triangle, range, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: rows + 1
  • Function call: range(1, computed expression)
  • Function call: range(i)
  • Function call: print(num)
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_floyds_triangle(rows)
  • Assignment: right side evaluated first, then stored in 'num'
  • Assignment: right side evaluated first, then stored in 'r'
  • Binary operation: num + c
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: str(computed expression)
  • Function call: range(r)
  • Function call: print_floyds_triangle(4)
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: rows + 1
  • Function call: range(1, computed expression)
  • Binary operation: num + j
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: str(computed expression)
  • Function call: range(i)
  • Function call: print_floyds_triangle(4)
  • Assignment: right side evaluated first, then stored in 'num'
  • Binary operation: rows + 1
  • Function call: range(1, computed expression)
  • Function call: range(i)
  • Function call: print(num)
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_floyds_triangle(rows)
  • Assignment: right side evaluated first, then stored in 'num_2'
  • Binary operation: rows + 1
  • Function call: range(1, computed expression)
  • Function call: range(i)
  • Function call: print(num_2)
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows_2'
  • Function call: print_floyds_triangle(rows_2)
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_floyds_triangle(rows)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_floyds_triangle(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_floyds_triangle' (stored in memory, not executed yet)
  2. Assign value to variable: rows
  3. Execute function call: print_floyds_triangle(rows)
  4. Define function 'print_floyds_triangle' (stored in memory, not executed yet)
  5. Execute function call: print_floyds_triangle(4)
  6. Define function 'print_floyds_triangle' (stored in memory, not executed yet)
  7. Execute function call: print_floyds_triangle(4)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: rows
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(print_floyds_triangle(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_floyds_triangle' is defined but NOT executed until called
    - 'print_floyds_triangle' is defined but NOT executed until called
    - 'print_floyds_triangle' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_floyds_triangle() is invoked
      → Execution jumps to the function body of 'print_floyds_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_floyds_triangle() is invoked
      → Execution jumps to the function body of 'print_floyds_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_floyds_triangle() is invoked
      → Execution jumps to the function body of 'print_floyds_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 222:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def print_reverse_pyramid(rows)
    def print_reverse_pyramid(rows)
    def print_reverse_pyramid(rows)
    def solve()
    def solve()
  Keywords used: def, for
  Operators used: +, -, ., ()
  Control structures: for loop iteration; generator expression
  Functions called: map, print, print_reverse_pyramid, range, str
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(rows, 0, value)
  • Binary operation: i + 1
  • Function call: range(1, computed expression)
  • Function call: print(j)
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_reverse_pyramid(rows)
  • Function call: range(rows, 0, value)
  • Binary operation: i + 1
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: map(str, value)
  • Function call: range(1, computed expression)
  • Function call: print_reverse_pyramid(5)
  • Function call: range(rows, 0, value)
  • Binary operation: i + 1
  • Function call: print(value)
  • Function call: ' '.join(value)
  • Function call: str(j)
  • Function call: range(1, computed expression)
  • Function call: print_reverse_pyramid(5)
  • Function call: range(rows, 0, value)
  • Binary operation: i + 1
  • Function call: range(1, computed expression)
  • Function call: print(j)
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_reverse_pyramid(rows)
  • Function call: range(rows, 0, value)
  • Binary operation: i + 1
  • Function call: range(1, computed expression)
  • Function call: print(j)
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows_2'
  • Function call: print_reverse_pyramid(rows_2)
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_reverse_pyramid(rows)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_reverse_pyramid(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_reverse_pyramid' (stored in memory, not executed yet)
  2. Assign value to variable: rows
  3. Execute function call: print_reverse_pyramid(rows)
  4. Define function 'print_reverse_pyramid' (stored in memory, not executed yet)
  5. Execute function call: print_reverse_pyramid(5)
  6. Define function 'print_reverse_pyramid' (stored in memory, not executed yet)
  7. Execute function call: print_reverse_pyramid(5)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: rows
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(print_reverse_pyramid(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_reverse_pyramid' is defined but NOT executed until called
    - 'print_reverse_pyramid' is defined but NOT executed until called
    - 'print_reverse_pyramid' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_reverse_pyramid() is invoked
      → Execution jumps to the function body of 'print_reverse_pyramid'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_reverse_pyramid() is invoked
      → Execution jumps to the function body of 'print_reverse_pyramid'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_reverse_pyramid() is invoked
      → Execution jumps to the function body of 'print_reverse_pyramid'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 227:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def print_zigzag(rows)
    def print_zigzag(rows)
    def solve()
    def solve()
  Keywords used: def, for, if/elif/else
  Operators used: %, *, +, -, (), //, ==, or, and
  Control structures: for loop iteration
  Functions called: print, print_zigzag, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(rows)
  • Function call: range(rows)
  • Binary operation: i % 2
  • Binary operation: j % expression
  • Binary operation: i % 2
  • Binary operation: j % expression
  • Binary operation: rows - 1
  • Binary operation: rows - 1
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_zigzag(rows)
  • Function call: range(rows)
  • Binary operation: i % 2
  • Condition branch: evaluates boolean expression first
  • Binary operation: '*   ' * expression
  • Binary operation: expression + 1
  • Binary operation: rows // 2
  • Function call: print(computed expression)
  • Binary operation: ' * *' * expression
  • Binary operation: expression + 1
  • Binary operation: rows // 2
  • Function call: print(computed expression)
  • Function call: print_zigzag(5)
  • Function call: range(rows)
  • Function call: range(rows)
  • Binary operation: i % 2
  • Binary operation: j % expression
  • Binary operation: i % 2
  • Binary operation: j % expression
  • Binary operation: rows - 1
  • Binary operation: rows - 1
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_zigzag(rows)
  • Function call: range(rows)
  • Function call: range(rows)
  • Binary operation: i % 2
  • Binary operation: j % expression
  • Binary operation: i % 2
  • Binary operation: j % expression
  • Binary operation: rows - 1
  • Binary operation: rows - 1
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows_2'
  • Function call: print_zigzag(rows_2)
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_zigzag(rows)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_zigzag(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_zigzag' (stored in memory, not executed yet)
  2. Assign value to variable: rows
  3. Execute function call: print_zigzag(rows)
  4. Define function 'print_zigzag' (stored in memory, not executed yet)
  5. Execute function call: print_zigzag(5)
  6. Define function 'solve' (stored in memory, not executed yet)
  7. Define function 'solve' (stored in memory, not executed yet)
  8. Assign value to variable: rows
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(print_zigzag(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_zigzag' is defined but NOT executed until called
    - 'print_zigzag' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_zigzag() is invoked
      → Execution jumps to the function body of 'print_zigzag'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_zigzag() is invoked
      → Execution jumps to the function body of 'print_zigzag'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 228:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def print_cross(rows)
    def print_cross(rows)
    def print_cross(rows)
    def solve()
    def solve()
  Keywords used: def, for, if/elif/else
  Operators used: *, +, -, ., (), //, ==, or
  Control structures: for loop iteration; generator expression
  Functions called: abs, min, print, print_cross, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: range(rows)
  • Function call: range(rows)
  • Binary operation: expression - i
  • Binary operation: rows - 1
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_cross(rows)
  • Function call: range(rows)
  • Binary operation: expression + expression
  • Binary operation: expression + expression
  • Binary operation: expression + '*'
  • Binary operation: ' ' * expression
  • Binary operation: ' ' * min(...)
  • Binary operation: expression - 1
  • Binary operation: rows // 2
  • Binary operation: abs(...) * 2
  • Binary operation: expression - i
  • Binary operation: rows - 1
  • Binary operation: expression - i
  • Binary operation: rows // 2
  • Function call: print(computed expression)
  • Function call: min(i, computed expression)
  • Function call: abs(computed expression)
  • Function call: print_cross(5)
  • Function call: range(rows)
  • Binary operation: expression - i
  • Binary operation: rows - 1
  • Function call: print(value)
  • Function call: ''.join(value)
  • Function call: range(rows)
  • Function call: print_cross(5)
  • Function call: range(rows)
  • Function call: range(rows)
  • Binary operation: expression - i
  • Binary operation: rows - 1
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_cross(rows)
  • Function call: range(rows)
  • Function call: range(rows)
  • Binary operation: expression - i
  • Binary operation: rows - 1
  • Condition branch: evaluates boolean expression first
  • Function call: print('*')
  • Function call: print(' ')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'rows_2'
  • Function call: print_cross(rows_2)
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: print_cross(rows)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_cross(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_cross' (stored in memory, not executed yet)
  2. Assign value to variable: rows
  3. Execute function call: print_cross(rows)
  4. Define function 'print_cross' (stored in memory, not executed yet)
  5. Execute function call: print_cross(5)
  6. Define function 'print_cross' (stored in memory, not executed yet)
  7. Execute function call: print_cross(5)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: rows
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(print_cross(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_cross' is defined but NOT executed until called
    - 'print_cross' is defined but NOT executed until called
    - 'print_cross' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_cross() is invoked
      → Execution jumps to the function body of 'print_cross'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_cross() is invoked
      → Execution jumps to the function body of 'print_cross'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_cross() is invoked
      → Execution jumps to the function body of 'print_cross'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 230:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def count_occurrences(lst)
      Returns a value via 'return' keyword
    def count_occurrences(lst)
      Returns a value via 'return' keyword
    def count_occurrences(lst)
      Returns a value via 'return' keyword
    def count_occurrences(lst)
      Returns a value via 'return' keyword
    def count_occurrences(lst)
      Returns a value via 'return' keyword
    def count_occurrences(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, ., (), [], in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: Counter, count_occurrences, dict, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: count_occurrences(lst)
  • Function call: dict(value)
  • Function call: Counter(lst)
  • Function call: print(value)
  • Function call: count_occurrences(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: result.get(...) + 1
  • Function call: result.get(item, 0)
  • Function call: print(value)
  • Function call: count_occurrences(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.setdefault(item, 0)
  • Function call: print(value)
  • Function call: count_occurrences(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: count_occurrences(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_occurrences(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_occurrences' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(count_occurrences(...))
  4. Define function 'count_occurrences' (stored in memory, not executed yet)
  5. Execute function call: print(count_occurrences(...))
  6. Define function 'count_occurrences' (stored in memory, not executed yet)
  7. Execute function call: print(count_occurrences(...))
  8. Define function 'count_occurrences' (stored in memory, not executed yet)
  9. Execute function call: print(count_occurrences(...))
  10. Define function 'count_occurrences' (stored in memory, not executed yet)
  11. Define function 'count_occurrences' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(count_occurrences(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
    - 'count_occurrences' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 231:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_of_odd()
    def sum_of_odd(nums)
      Returns a value via 'return' keyword
    def sum_of_odd(nums)
      Returns a value via 'return' keyword
    def sum_of_odd(nums)
      Returns a value via 'return' keyword
    def sum_of_odd()
      Returns a value via 'return' keyword
    def sum_of_odd()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, ., !=, ()
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: filter, input, int, print, sum, sum_of_odd
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print(count)
  • Function call: sum_of_odd()
  • Binary operation: n % 2
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: sum_of_odd(value)
  • Binary operation: x % 2
  • Function call: sum(value)
  • Function call: filter(value, nums)
  • Function call: print(value)
  • Function call: sum_of_odd(value)
  • Binary operation: n % 2
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: sum_of_odd(value)
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: int(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'nums'
  • Function call: sum_of_odd(nums)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_odd(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_odd' (stored in memory, not executed yet)
  2. Execute function call: sum_of_odd()
  3. Define function 'sum_of_odd' (stored in memory, not executed yet)
  4. Execute function call: print(sum_of_odd(...))
  5. Define function 'sum_of_odd' (stored in memory, not executed yet)
  6. Execute function call: print(sum_of_odd(...))
  7. Define function 'sum_of_odd' (stored in memory, not executed yet)
  8. Execute function call: print(sum_of_odd(...))
  9. Define function 'sum_of_odd' (stored in memory, not executed yet)
  10. Define function 'sum_of_odd' (stored in memory, not executed yet)
  11. Assign value to variable: nums
  12. Assign value to variable: result
  13. Execute function call: print(result)
  14. Execute function call: print(sum_of_odd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_odd' is defined but NOT executed until called
    - 'sum_of_odd' is defined but NOT executed until called
    - 'sum_of_odd' is defined but NOT executed until called
    - 'sum_of_odd' is defined but NOT executed until called
    - 'sum_of_odd' is defined but NOT executed until called
    - 'sum_of_odd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - sum_of_odd() is invoked
      → Execution jumps to the function body of 'sum_of_odd'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 232:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def remove_vowels()
    def remove_vowels(s)
      Returns a value via 'return' keyword
    def remove_vowels(s)
      Returns a value via 'return' keyword
    def remove_vowels(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), not in
  Control structures: if/else conditional branching; for loop iteration; lambda expression
  Functions called: filter, input, print, remove_vowels
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('type string: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Condition branch: evaluates boolean expression first
  • Function call: print(char)
  • Function call: remove_vowels()
  • Function call: ''.join(value)
  • Function call: filter(value, s)
  • Function call: print(value)
  • Function call: remove_vowels('hello world')
  • Function call: s.translate(value)
  • Function call: str.maketrans('', '', 'aeiouAEIOU')
  • Function call: print(value)
  • Function call: remove_vowels('banana')
  • Function call: re.sub('[aeiouAEIOU]', '', s)
  • Function call: print(value)
  • Function call: remove_vowels('Hello, World!')
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: remove_vowels(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_vowels(10)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_vowels' (stored in memory, not executed yet)
  2. Execute function call: remove_vowels()
  3. Define function 'remove_vowels' (stored in memory, not executed yet)
  4. Execute function call: print(remove_vowels(...))
  5. Define function 'remove_vowels' (stored in memory, not executed yet)
  6. Execute function call: print(remove_vowels(...))
  7. Define function 'remove_vowels' (stored in memory, not executed yet)
  8. Execute function call: print(remove_vowels(...))
  9. Assign value to variable: s
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(remove_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
    - 'remove_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - remove_vowels() is invoked
      → Execution jumps to the function body of 'remove_vowels'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 233:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def find_min(lst)
      Returns a value via 'return' keyword
    def find_min(lst)
      Returns a value via 'return' keyword
    def find_min(lst)
      Returns a value via 'return' keyword
    def find_min(lst)
      Returns a value via 'return' keyword
    def find_min(lst)
      Returns a value via 'return' keyword
    def find_min(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return, while
  Operators used: <, (), []
  Control structures: for loop iteration; while loop; lambda expression
  Functions called: find_min, len, print, reduce, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'min_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_min(lst)
  • Function call: sorted(lst)
  • Function call: print(value)
  • Function call: find_min(value)
  • Function call: reduce(value, lst)
  • Function call: print(value)
  • Function call: find_min(value)
  • Function call: len(lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_val'
  • Function call: print(value)
  • Function call: find_min(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_min(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_min(...))
  4. Define function 'find_min' (stored in memory, not executed yet)
  5. Execute function call: print(find_min(...))
  6. Define function 'find_min' (stored in memory, not executed yet)
  7. Execute function call: print(find_min(...))
  8. Define function 'find_min' (stored in memory, not executed yet)
  9. Execute function call: print(find_min(...))
  10. Define function 'find_min' (stored in memory, not executed yet)
  11. Define function 'find_min' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(find_min(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 234:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def print_square(n)
    def print_square(n)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, return, while
  Operators used: *, <, ()
  Control structures: while loop
  Functions called: print, print_square
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: '* ' * 5
  • Function call: print(computed expression)
  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: '* ' * n
  • Function call: print(computed expression)
  • Function call: print_square(5)
  • Assignment: right side evaluated first, then stored in 'i'
  • Assignment: right side evaluated first, then stored in 'j'
  • Function call: print('*')
  • Function call: print()
  • Function call: print_square(5)
  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: '* ' * 5
  • Function call: print(computed expression)
  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: '* ' * 5
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: print_square(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_square(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Assign value to variable: i
  2. While loop: repeat while condition is true
  3. Define function 'print_square' (stored in memory, not executed yet)
  4. Execute function call: print_square(5)
  5. Define function 'print_square' (stored in memory, not executed yet)
  6. Execute function call: print_square(5)
  7. Define function 'solve' (stored in memory, not executed yet)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Assign value to variable: n
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(print_square(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_square' is defined but NOT executed until called
    - 'print_square' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_square() is invoked
      → Execution jumps to the function body of 'print_square'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print_square() is invoked
      → Execution jumps to the function body of 'print_square'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
"""

"""
Problem 236:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def decreasing_triangle()
    def decreasing_triangle()
    def decreasing_triangle()
    def solve()
    def solve()
  Keywords used: def, for
  Operators used: *, -, ()
  Control structures: for loop iteration
  Functions called: decreasing_triangle, increasing_triangle, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print('*')
  • Function call: print()
  • Function call: print(value)
  • Function call: increasing_triangle()
  • Function call: range(5, 0, value)
  • Binary operation: '* ' * i
  • Function call: print(computed expression)
  • Function call: decreasing_triangle()
  • Function call: range(5, 0, value)
  • Function call: range(i)
  • Function call: print('*')
  • Function call: print()
  • Function call: decreasing_triangle()
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print('*')
  • Function call: print()
  • Function call: print(value)
  • Function call: increasing_triangle()
  • Assignment: right side evaluated first, then stored in 'n_2'
  • Function call: range(n_2)
  • Function call: range(i, n_2)
  • Function call: print('*')
  • Function call: print()
  • Function call: print(value)
  • Function call: increasing_triangle()
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print('*')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print('*')
  • Function call: print()

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'decreasing_triangle' (stored in memory, not executed yet)
  2. Execute function call: print(increasing_triangle(...))
  3. Define function 'decreasing_triangle' (stored in memory, not executed yet)
  4. Execute function call: decreasing_triangle()
  5. Define function 'decreasing_triangle' (stored in memory, not executed yet)
  6. Execute function call: decreasing_triangle()
  7. Define function 'solve' (stored in memory, not executed yet)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Assign value to variable: n
  10. For loop: iterate over range(...)
  11. Assign value to variable: n
  12. For loop: iterate over range(...)

▸ EXECUTION FLOW

  Function definition phase:
    - 'decreasing_triangle' is defined but NOT executed until called
    - 'decreasing_triangle' is defined but NOT executed until called
    - 'decreasing_triangle' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - decreasing_triangle() is invoked
      → Execution jumps to the function body of 'decreasing_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - decreasing_triangle() is invoked
      → Execution jumps to the function body of 'decreasing_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 238:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def left_triangle()
    def left_triangle()
    def left_triangle()
    def solve()
    def solve()
  Keywords used: def, for
  Operators used: *, +, -, ., ()
  Control structures: for loop iteration
  Functions called: increasing_triangle, left_triangle, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print(' ')
  • Function call: range(n)
  • Binary operation: i + 1
  • Function call: range(computed expression)
  • Function call: print('*')
  • Function call: print()
  • Function call: print(value)
  • Function call: increasing_triangle()
  • Assignment: right side evaluated first, then stored in 'n'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Binary operation: expression + expression
  • Binary operation: '  ' * expression
  • Binary operation: '* ' * i
  • Binary operation: n - i
  • Function call: print(computed expression)
  • Function call: left_triangle()
  • Assignment: right side evaluated first, then stored in 'n'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Binary operation: n * 2
  • Binary operation: '* ' * i
  • Function call: print(value)
  • Function call: expression.rjust(computed expression)
  • Function call: left_triangle()
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print(' ')
  • Function call: range(n)
  • Binary operation: i + 1
  • Function call: range(computed expression)
  • Function call: print('*')
  • Function call: print()
  • Function call: print(value)
  • Function call: increasing_triangle()
  • Assignment: right side evaluated first, then stored in 'n_2'
  • Function call: range(n_2)
  • Function call: range(i, n_2)
  • Function call: print(' ')
  • Function call: range(n_2)
  • Binary operation: i + 1
  • Function call: range(computed expression)
  • Function call: print('*')
  • Function call: print()
  • Function call: print(value)
  • Function call: increasing_triangle()
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print(' ')
  • Function call: range(n)
  • Binary operation: i + 1
  • Function call: range(computed expression)
  • Function call: print('*')
  • Function call: print()
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: range(n)
  • Function call: range(i, n)
  • Function call: print(' ')
  • Function call: range(n)
  • Binary operation: i + 1
  • Function call: range(computed expression)
  • Function call: print('*')
  • Function call: print()

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'left_triangle' (stored in memory, not executed yet)
  2. Execute function call: print(increasing_triangle(...))
  3. Define function 'left_triangle' (stored in memory, not executed yet)
  4. Execute function call: left_triangle()
  5. Define function 'left_triangle' (stored in memory, not executed yet)
  6. Execute function call: left_triangle()
  7. Define function 'solve' (stored in memory, not executed yet)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Assign value to variable: n
  10. For loop: iterate over range(...)
  11. For loop: iterate over range(...)
  12. Assign value to variable: n
  13. For loop: iterate over range(...)
  14. For loop: iterate over range(...)

▸ EXECUTION FLOW

  Function definition phase:
    - 'left_triangle' is defined but NOT executed until called
    - 'left_triangle' is defined but NOT executed until called
    - 'left_triangle' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - left_triangle() is invoked
      → Execution jumps to the function body of 'left_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - left_triangle() is invoked
      → Execution jumps to the function body of 'left_triangle'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 242:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension
  Functions called: enumerate, enumerate_lst, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: enumerate_lst(lst)
  • Function call: list(value)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Function call: enumerate_lst(value)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Function call: enumerate_lst(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: enumerate_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'enumerate_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(enumerate_lst(...))
  4. Define function 'enumerate_lst' (stored in memory, not executed yet)
  5. Execute function call: print(enumerate_lst(...))
  6. Define function 'enumerate_lst' (stored in memory, not executed yet)
  7. Execute function call: print(enumerate_lst(...))
  8. Define function 'enumerate_lst' (stored in memory, not executed yet)
  9. Define function 'enumerate_lst' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(enumerate_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 243:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def enumerate_lst(lst, target)
      Returns a value via 'return' keyword
    def find_first(lst, target)
      Returns a value via 'return' keyword
    def find_first(lst, target)
      Returns a value via 'return' keyword
    def enumerate_lst(lst, target)
      Returns a value via 'return' keyword
    def enumerate_lst(lst, target)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., (), ==
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: enumerate, enumerate_lst, find_first, next, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'target'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: enumerate_lst(lst, target)
  • Function call: lst.index(target)
  • Function call: print(value)
  • Function call: find_first(value, 'Chris')
  • Function call: next(value, value)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Function call: find_first(value, 'Chris')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: enumerate_lst(lst, target)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: enumerate_lst(value, 'goal')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'enumerate_lst' (stored in memory, not executed yet)
  2. Assign value to variable: target
  3. Assign value to variable: lst
  4. Execute function call: print(enumerate_lst(...))
  5. Define function 'find_first' (stored in memory, not executed yet)
  6. Execute function call: print(find_first(...))
  7. Define function 'find_first' (stored in memory, not executed yet)
  8. Execute function call: print(find_first(...))
  9. Define function 'enumerate_lst' (stored in memory, not executed yet)
  10. Define function 'enumerate_lst' (stored in memory, not executed yet)
  11. Assign value to variable: lst
  12. Assign value to variable: target
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(enumerate_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'enumerate_lst' is defined but NOT executed until called
    - 'find_first' is defined but NOT executed until called
    - 'find_first' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 244:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def enumerate_lst(lst)
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ()
  Control structures: for loop iteration; list comprehension
  Functions called: enumerate, enumerate_lst, len, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Function call: enumerate(lst, 1)
  • Function call: enumerate_lst(value)
  • Function call: print(s)
  • Binary operation: len(...) + 1
  • Function call: zip(value, lst)
  • Function call: range(1, computed expression)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: enumerate_lst(value)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: enumerate_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'enumerate_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: enumerate_lst(lst)
  4. Define function 'enumerate_lst' (stored in memory, not executed yet)
  5. For loop: iterate over enumerate_lst(...)
  6. Define function 'enumerate_lst' (stored in memory, not executed yet)
  7. Execute function call: enumerate_lst(value)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(enumerate_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - enumerate_lst() is invoked
      → Execution jumps to the function body of 'enumerate_lst'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - enumerate_lst() is invoked
      → Execution jumps to the function body of 'enumerate_lst'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate_lst(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 245:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def enumerate_lst(lst)
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration; dict comprehension
  Functions called: dict, enumerate, enumerate_lst, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Function call: enumerate_lst(value)
  • Function call: dict(value)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Function call: enumerate_lst(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: enumerate_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'enumerate_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: enumerate_lst(lst)
  4. Define function 'enumerate_lst' (stored in memory, not executed yet)
  5. Execute function call: print(enumerate_lst(...))
  6. Define function 'enumerate_lst' (stored in memory, not executed yet)
  7. Execute function call: print(enumerate_lst(...))
  8. Define function 'enumerate_lst' (stored in memory, not executed yet)
  9. Define function 'enumerate_lst' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(enumerate_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - enumerate_lst() is invoked
      → Execution jumps to the function body of 'enumerate_lst'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 246:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def enumerate_lst(lst)
    def enumerate_lst(lst)
    def enumerate_lst(lst)
      Returns a value via 'return' keyword
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, !=, (), []
  Control structures: for loop iteration; list comprehension
  Functions called: enumerate, enumerate_lst, len, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print(index, word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Function call: range(0, value, 2)
  • Function call: len(lst)
  • Function call: print(i, value)
  • Function call: enumerate_lst(value)
  • Function call: range(0, value, 2)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: enumerate_lst(value)
  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print(index, word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: enumerate_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: enumerate_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'enumerate_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: enumerate_lst(lst)
  4. Define function 'enumerate_lst' (stored in memory, not executed yet)
  5. Execute function call: enumerate_lst(value)
  6. Define function 'enumerate_lst' (stored in memory, not executed yet)
  7. Execute function call: print(enumerate_lst(...))
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(enumerate_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'enumerate_lst' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - enumerate_lst() is invoked
      → Execution jumps to the function body of 'enumerate_lst'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - enumerate_lst() is invoked
      → Execution jumps to the function body of 'enumerate_lst'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 248:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def replace_with_index(lst)
      Returns a value via 'return' keyword
    def replace_with_index(lst)
      Returns a value via 'return' keyword
    def replace_with_index(lst)
      Returns a value via 'return' keyword
    def replace_with_index(lst)
      Returns a value via 'return' keyword
    def replace_with_index(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration; list comprehension
  Functions called: enumerate, len, list, print, range, replace_with_index
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: replace_with_index(lst)
  • Function call: range(value)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: replace_with_index(value)
  • Function call: list(value)
  • Function call: range(value)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: replace_with_index(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: replace_with_index(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: replace_with_index(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'replace_with_index' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(replace_with_index(...))
  4. Define function 'replace_with_index' (stored in memory, not executed yet)
  5. Execute function call: print(replace_with_index(...))
  6. Define function 'replace_with_index' (stored in memory, not executed yet)
  7. Execute function call: print(replace_with_index(...))
  8. Define function 'replace_with_index' (stored in memory, not executed yet)
  9. Define function 'replace_with_index' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(replace_with_index(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'replace_with_index' is defined but NOT executed until called
    - 'replace_with_index' is defined but NOT executed until called
    - 'replace_with_index' is defined but NOT executed until called
    - 'replace_with_index' is defined but NOT executed until called
    - 'replace_with_index' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 250:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_odd_indexed_elements(lst)
      Returns a value via 'return' keyword
    def sum_odd_indexed_elements(lst)
      Returns a value via 'return' keyword
    def sum_odd_indexed_elements(lst)
      Returns a value via 'return' keyword
    def sum_odd_indexed_elements(lst)
      Returns a value via 'return' keyword
    def sum_odd_indexed_elements(lst)
      Returns a value via 'return' keyword
    def sum_odd_indexed_elements(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, !=, (), []
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: enumerate, len, print, range, sum, sum_odd_indexed_elements
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sum_odd_indexed_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: print(value)
  • Function call: sum_odd_indexed_elements(value)
  • Binary operation: i % 2
  • Function call: sum(value)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Function call: sum_odd_indexed_elements(value)
  • Function call: sum(value)
  • Function call: range(1, value, 2)
  • Function call: len(lst)
  • Function call: print(value)
  • Function call: sum_odd_indexed_elements(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sum_odd_indexed_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_odd_indexed_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_odd_indexed_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'sum_odd_indexed_elements' (stored in memory, not executed yet)
  6. Execute function call: print(sum_odd_indexed_elements(...))
  7. Define function 'sum_odd_indexed_elements' (stored in memory, not executed yet)
  8. Execute function call: print(sum_odd_indexed_elements(...))
  9. Define function 'sum_odd_indexed_elements' (stored in memory, not executed yet)
  10. Execute function call: print(sum_odd_indexed_elements(...))
  11. Define function 'sum_odd_indexed_elements' (stored in memory, not executed yet)
  12. Define function 'sum_odd_indexed_elements' (stored in memory, not executed yet)
  13. Assign value to variable: lst
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(sum_odd_indexed_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_odd_indexed_elements' is defined but NOT executed until called
    - 'sum_odd_indexed_elements' is defined but NOT executed until called
    - 'sum_odd_indexed_elements' is defined but NOT executed until called
    - 'sum_odd_indexed_elements' is defined but NOT executed until called
    - 'sum_odd_indexed_elements' is defined but NOT executed until called
    - 'sum_odd_indexed_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 251:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def pair_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def pair_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def pair_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def pair_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def pair_elements(lst1, lst2)
      Returns a value via 'return' keyword
    def pair_elements(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: len, list, pair_elements, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: zip(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'paired'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: pair_elements(lst1, lst2)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: pair_elements(value, value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: pair_elements(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Function call: result.append(value)
  • Function call: print(value)
  • Function call: pair_elements(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: pair_elements(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: pair_elements(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'pair_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(pair_elements(...))
  5. Define function 'pair_elements' (stored in memory, not executed yet)
  6. Execute function call: print(pair_elements(...))
  7. Define function 'pair_elements' (stored in memory, not executed yet)
  8. Execute function call: print(pair_elements(...))
  9. Define function 'pair_elements' (stored in memory, not executed yet)
  10. Execute function call: print(pair_elements(...))
  11. Define function 'pair_elements' (stored in memory, not executed yet)
  12. Define function 'pair_elements' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(pair_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'pair_elements' is defined but NOT executed until called
    - 'pair_elements' is defined but NOT executed until called
    - 'pair_elements' is defined but NOT executed until called
    - 'pair_elements' is defined but NOT executed until called
    - 'pair_elements' is defined but NOT executed until called
    - 'pair_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 252:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_of_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def sum_of_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def sum_of_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def sum_of_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def sum_of_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def sum_of_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: len, list, map, print, range, sum_of_2_lst, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'summed_list'
  • Function call: zip(lst1, lst2)
  • Binary operation: x + y
  • Function call: summed_list.append(computed expression)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: sum_of_2_lst(lst1, lst2)
  • Binary operation: x + y
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: sum_of_2_lst(value, value)
  • Function call: list(value)
  • Function call: map(sum, value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: sum_of_2_lst(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Binary operation: expression + expression
  • Function call: result.append(computed expression)
  • Function call: print(value)
  • Function call: sum_of_2_lst(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: sum_of_2_lst(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_2_lst(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_2_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(sum_of_2_lst(...))
  5. Define function 'sum_of_2_lst' (stored in memory, not executed yet)
  6. Execute function call: print(sum_of_2_lst(...))
  7. Define function 'sum_of_2_lst' (stored in memory, not executed yet)
  8. Execute function call: print(sum_of_2_lst(...))
  9. Define function 'sum_of_2_lst' (stored in memory, not executed yet)
  10. Execute function call: print(sum_of_2_lst(...))
  11. Define function 'sum_of_2_lst' (stored in memory, not executed yet)
  12. Define function 'sum_of_2_lst' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(sum_of_2_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_2_lst' is defined but NOT executed until called
    - 'sum_of_2_lst' is defined but NOT executed until called
    - 'sum_of_2_lst' is defined but NOT executed until called
    - 'sum_of_2_lst' is defined but NOT executed until called
    - 'sum_of_2_lst' is defined but NOT executed until called
    - 'sum_of_2_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 253:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sort_tuples_by_second(tuples_list)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples_list)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples_list)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples_list)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples_list)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples_list)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: +, -, ., >, (), []
  Control structures: if/else conditional branching; for loop iteration; lambda expression
  Functions called: itemgetter, len, list, print, range, sort_tuples_by_second, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(tuples_list)
  • Assignment: right side evaluated first, then stored in 'tuples_list'
  • Function call: sort_tuples_by_second(tuples_list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sorted(tuples_list)
  • Function call: itemgetter(1)
  • Function call: print(value)
  • Function call: sort_tuples_by_second(value)
  • Function call: list(tuples_list)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: range(value)
  • Function call: len(lst)
  • Binary operation: expression - 1
  • Binary operation: len(...) - i
  • Function call: range(0, computed expression)
  • Function call: len(lst)
  • Binary operation: j + 1
  • Condition branch: evaluates boolean expression first
  • Binary operation: j + 1
  • Binary operation: j + 1
  • Function call: print(value)
  • Function call: sort_tuples_by_second(value)
  • Function call: tuples_list.sort()
  • Function call: print(value)
  • Function call: sort_tuples_by_second(value)
  • Function call: sorted(tuples_list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(tuples_list)
  • Assignment: right side evaluated first, then stored in 'tuples_list'
  • Function call: sort_tuples_by_second(tuples_list)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_tuples_by_second(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  2. Assign value to variable: tuples_list
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Import from operator: itemgetter
  6. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  7. Execute function call: print(sort_tuples_by_second(...))
  8. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  9. Execute function call: print(sort_tuples_by_second(...))
  10. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  11. Execute function call: print(sort_tuples_by_second(...))
  12. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  13. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  14. Assign value to variable: tuples_list
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(sort_tuples_by_second(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 254:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def dic_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def dic_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def dic_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def dic_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def dic_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def dic_lst(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration; dict comprehension
  Functions called: dic_lst, dict, len, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: dic_lst(lst1, lst2)
  • Function call: dict(value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: dic_lst(value, value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: dic_lst(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Function call: print(value)
  • Function call: dic_lst(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: dic_lst(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: dic_lst(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'dic_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(dic_lst(...))
  5. Define function 'dic_lst' (stored in memory, not executed yet)
  6. Execute function call: print(dic_lst(...))
  7. Define function 'dic_lst' (stored in memory, not executed yet)
  8. Execute function call: print(dic_lst(...))
  9. Define function 'dic_lst' (stored in memory, not executed yet)
  10. Execute function call: print(dic_lst(...))
  11. Define function 'dic_lst' (stored in memory, not executed yet)
  12. Define function 'dic_lst' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(dic_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'dic_lst' is defined but NOT executed until called
    - 'dic_lst' is defined but NOT executed until called
    - 'dic_lst' is defined but NOT executed until called
    - 'dic_lst' is defined but NOT executed until called
    - 'dic_lst' is defined but NOT executed until called
    - 'dic_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 255:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def combine_3_lst(lst1, lst2, lst3)
      Returns a value via 'return' keyword
    def combine_3_lst(lst1, lst2, lst3)
      Returns a value via 'return' keyword
    def combine_3_lst(lst1, lst2, lst3)
      Returns a value via 'return' keyword
    def combine_3_lst(lst1, lst2, lst3)
      Returns a value via 'return' keyword
    def combine_3_lst(lst1, lst2, lst3)
      Returns a value via 'return' keyword
    def combine_3_lst(lst1, lst2, lst3)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration
  Functions called: combine_3_lst, len, list, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2, lst3)
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'lst3'
  • Function call: print(value)
  • Function call: combine_3_lst(lst1, lst2, lst3)
  • Function call: list(value)
  • Function call: zip(lst1, lst2, lst3)
  • Function call: print(value)
  • Function call: combine_3_lst(value, value, value)
  • Function call: zip(lst1, lst2, lst3)
  • Function call: print(value)
  • Function call: combine_3_lst(value, value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Function call: result.append(value)
  • Function call: print(value)
  • Function call: combine_3_lst(value, value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Assignment: right side evaluated first, then stored in 'lst3'
  • Function call: combine_3_lst(lst1, lst2, lst3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: combine_3_lst(value, value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'combine_3_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Assign value to variable: lst3
  5. Execute function call: print(combine_3_lst(...))
  6. Define function 'combine_3_lst' (stored in memory, not executed yet)
  7. Execute function call: print(combine_3_lst(...))
  8. Define function 'combine_3_lst' (stored in memory, not executed yet)
  9. Execute function call: print(combine_3_lst(...))
  10. Define function 'combine_3_lst' (stored in memory, not executed yet)
  11. Execute function call: print(combine_3_lst(...))
  12. Define function 'combine_3_lst' (stored in memory, not executed yet)
  13. Define function 'combine_3_lst' (stored in memory, not executed yet)
  14. Assign value to variable: lst1
  15. Assign value to variable: lst2
  16. Assign value to variable: lst3
  17. Assign value to variable: result
  18. Execute function call: print(result)
  19. Execute function call: print(combine_3_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'combine_3_lst' is defined but NOT executed until called
    - 'combine_3_lst' is defined but NOT executed until called
    - 'combine_3_lst' is defined but NOT executed until called
    - 'combine_3_lst' is defined but NOT executed until called
    - 'combine_3_lst' is defined but NOT executed until called
    - 'combine_3_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 256:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def transpose_matrix(matrix)
      Returns a value via 'return' keyword
    def transpose_matrix(matrix)
      Returns a value via 'return' keyword
    def transpose_matrix(matrix)
      Returns a value via 'return' keyword
    def transpose_matrix(matrix)
      Returns a value via 'return' keyword
    def transpose_matrix(matrix)
      Returns a value via 'return' keyword
    def transpose_matrix(matrix)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: len, list, print, range, transpose_matrix, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: zip(value)
  • Assignment: right side evaluated first, then stored in 'transposed'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(value)
  • Function call: list(row)
  • Assignment: right side evaluated first, then stored in 'matrix'
  • Function call: print(value)
  • Function call: transpose_matrix(matrix)
  • Function call: list(row)
  • Function call: zip(value)
  • Assignment: right side evaluated first, then stored in 'matrix'
  • Function call: print(value)
  • Function call: transpose_matrix(matrix)
  • Function call: range(value)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'matrix'
  • Function call: print(value)
  • Function call: transpose_matrix(matrix)
  • Function call: len(matrix)
  • Assignment: right side evaluated first, then stored in 'rows'
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'cols'
  • Binary operation: expression * rows
  • Function call: range(cols)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(rows)
  • Function call: range(cols)
  • Assignment: right side evaluated first, then stored in 'matrix'
  • Function call: print(value)
  • Function call: transpose_matrix(matrix)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'matrix'
  • Function call: transpose_matrix(matrix)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: transpose_matrix(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'transpose_matrix' (stored in memory, not executed yet)
  2. Assign value to variable: matrix
  3. Execute function call: print(transpose_matrix(...))
  4. Define function 'transpose_matrix' (stored in memory, not executed yet)
  5. Assign value to variable: matrix
  6. Execute function call: print(transpose_matrix(...))
  7. Define function 'transpose_matrix' (stored in memory, not executed yet)
  8. Assign value to variable: matrix
  9. Execute function call: print(transpose_matrix(...))
  10. Define function 'transpose_matrix' (stored in memory, not executed yet)
  11. Assign value to variable: matrix
  12. Execute function call: print(transpose_matrix(...))
  13. Define function 'transpose_matrix' (stored in memory, not executed yet)
  14. Define function 'transpose_matrix' (stored in memory, not executed yet)
  15. Assign value to variable: matrix
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(transpose_matrix(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'transpose_matrix' is defined but NOT executed until called
    - 'transpose_matrix' is defined but NOT executed until called
    - 'transpose_matrix' is defined but NOT executed until called
    - 'transpose_matrix' is defined but NOT executed until called
    - 'transpose_matrix' is defined but NOT executed until called
    - 'transpose_matrix' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over transposed, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 257:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def list_tuples(lst)
    def list_tuples(lst)
      Returns a value via 'return' keyword
    def list_tuples(lst)
      Returns a value via 'return' keyword
    def list_tuples(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: enumerate, list, list_tuples, map, print, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: zip(value)
  • Assignment: right side evaluated first, then stored in 'unzipped'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: result.append(value)
  • Function call: list(x)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: list_tuples(lst)
  • Function call: list(t)
  • Function call: zip(value)
  • Function call: print(value)
  • Function call: list_tuples(value)
  • Function call: list(value)
  • Function call: map(list, value)
  • Function call: zip(value)
  • Function call: print(value)
  • Function call: list_tuples(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(t)
  • Function call: expression.append(val)
  • Function call: print(value)
  • Function call: list_tuples(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_tuples(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_tuples(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_tuples' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(list_tuples(...))
  4. Define function 'list_tuples' (stored in memory, not executed yet)
  5. Execute function call: print(list_tuples(...))
  6. Define function 'list_tuples' (stored in memory, not executed yet)
  7. Execute function call: print(list_tuples(...))
  8. Define function 'list_tuples' (stored in memory, not executed yet)
  9. Execute function call: print(list_tuples(...))
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(list_tuples(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_tuples' is defined but NOT executed until called
    - 'list_tuples' is defined but NOT executed until called
    - 'list_tuples' is defined but NOT executed until called
    - 'list_tuples' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over unzipped, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 258:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def individualists(lst)
      Returns a value via 'return' keyword
    def individualists(lst)
      Returns a value via 'return' keyword
    def individualists(lst)
      Returns a value via 'return' keyword
    def individualists(lst)
      Returns a value via 'return' keyword
    def individualists(lst)
      Returns a value via 'return' keyword
    def individualists(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., (), []
  Control structures: for loop iteration; list comprehension
  Functions called: enumerate, individualists, len, list, map, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(value)
  • Function call: result.append(value)
  • Function call: list(num)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: individualists(lst)
  • Function call: list(t)
  • Function call: zip(value)
  • Function call: print(value)
  • Function call: individualists(value)
  • Function call: list(value)
  • Function call: map(list, value)
  • Function call: zip(value)
  • Function call: print(value)
  • Function call: individualists(value)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'num_cols'
  • Function call: range(num_cols)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(t)
  • Function call: expression.append(val)
  • Function call: print(value)
  • Function call: individualists(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: individualists(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: individualists(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'individualists' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(individualists(...))
  4. Define function 'individualists' (stored in memory, not executed yet)
  5. Execute function call: print(individualists(...))
  6. Define function 'individualists' (stored in memory, not executed yet)
  7. Execute function call: print(individualists(...))
  8. Define function 'individualists' (stored in memory, not executed yet)
  9. Execute function call: print(individualists(...))
  10. Define function 'individualists' (stored in memory, not executed yet)
  11. Define function 'individualists' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(individualists(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'individualists' is defined but NOT executed until called
    - 'individualists' is defined but NOT executed until called
    - 'individualists' is defined but NOT executed until called
    - 'individualists' is defined but NOT executed until called
    - 'individualists' is defined but NOT executed until called
    - 'individualists' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 259:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def maxium_element_wise(lst1, lst2)
      Returns a value via 'return' keyword
    def maxium_element_wise(lst1, lst2)
      Returns a value via 'return' keyword
    def maxium_element_wise(lst1, lst2)
      Returns a value via 'return' keyword
    def maxium_element_wise(lst1, lst2)
      Returns a value via 'return' keyword
    def maxium_element_wise(lst1, lst2)
      Returns a value via 'return' keyword
    def maxium_element_wise(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., >, (), []
  Control structures: if/else conditional branching; for loop iteration; list comprehension
  Functions called: len, list, map, max, maxium_element_wise, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Function call: result.append(value)
  • Function call: max(x, y)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: maxium_element_wise(lst1, lst2)
  • Function call: max(x, y)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: maxium_element_wise(value, value)
  • Function call: list(value)
  • Function call: map(max, lst1, lst2)
  • Function call: print(value)
  • Function call: maxium_element_wise(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Function call: result.append(value)
  • Function call: print(value)
  • Function call: maxium_element_wise(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: maxium_element_wise(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: maxium_element_wise(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'maxium_element_wise' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(maxium_element_wise(...))
  5. Define function 'maxium_element_wise' (stored in memory, not executed yet)
  6. Execute function call: print(maxium_element_wise(...))
  7. Define function 'maxium_element_wise' (stored in memory, not executed yet)
  8. Execute function call: print(maxium_element_wise(...))
  9. Define function 'maxium_element_wise' (stored in memory, not executed yet)
  10. Execute function call: print(maxium_element_wise(...))
  11. Define function 'maxium_element_wise' (stored in memory, not executed yet)
  12. Define function 'maxium_element_wise' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(maxium_element_wise(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'maxium_element_wise' is defined but NOT executed until called
    - 'maxium_element_wise' is defined but NOT executed until called
    - 'maxium_element_wise' is defined but NOT executed until called
    - 'maxium_element_wise' is defined but NOT executed until called
    - 'maxium_element_wise' is defined but NOT executed until called
    - 'maxium_element_wise' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 260:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def concatenate(lst1, lst2)
      Returns a value via 'return' keyword
    def concatenate(lst1, lst2)
      Returns a value via 'return' keyword
    def concatenate(lst1, lst2)
      Returns a value via 'return' keyword
    def concatenate(lst1, lst2)
      Returns a value via 'return' keyword
    def concatenate(lst1, lst2)
      Returns a value via 'return' keyword
    def concatenate(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: +, ., (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: concatenate, len, list, map, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: concatenate(lst1, lst2)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: concatenate(value, value)
  • Function call: list(value)
  • Function call: map(value, lst1, lst2)
  • Function call: print(value)
  • Function call: concatenate(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Binary operation: expression + expression
  • Binary operation: expression + ' '
  • Function call: result.append(computed expression)
  • Function call: print(value)
  • Function call: concatenate(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: concatenate(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: concatenate(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'concatenate' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(concatenate(...))
  5. Define function 'concatenate' (stored in memory, not executed yet)
  6. Execute function call: print(concatenate(...))
  7. Define function 'concatenate' (stored in memory, not executed yet)
  8. Execute function call: print(concatenate(...))
  9. Define function 'concatenate' (stored in memory, not executed yet)
  10. Execute function call: print(concatenate(...))
  11. Define function 'concatenate' (stored in memory, not executed yet)
  12. Define function 'concatenate' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(concatenate(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'concatenate' is defined but NOT executed until called
    - 'concatenate' is defined but NOT executed until called
    - 'concatenate' is defined but NOT executed until called
    - 'concatenate' is defined but NOT executed until called
    - 'concatenate' is defined but NOT executed until called
    - 'concatenate' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 261:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def combine_to_dict(lst1, lst2)
      Returns a value via 'return' keyword
    def combine_to_dict(lst1, lst2)
      Returns a value via 'return' keyword
    def combine_to_dict(lst1, lst2)
      Returns a value via 'return' keyword
    def combine_to_dict(lst1, lst2)
      Returns a value via 'return' keyword
    def combine_to_dict(lst1, lst2)
      Returns a value via 'return' keyword
    def combine_to_dict(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: ., (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: combine_to_dict, len, list, map, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: combine_to_dict(lst1, lst2)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: combine_to_dict(value, value)
  • Function call: list(value)
  • Function call: map(value, lst1, lst2)
  • Function call: print(value)
  • Function call: combine_to_dict(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Function call: result.append(value)
  • Function call: print(value)
  • Function call: combine_to_dict(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: combine_to_dict(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: combine_to_dict(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'combine_to_dict' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(combine_to_dict(...))
  5. Define function 'combine_to_dict' (stored in memory, not executed yet)
  6. Execute function call: print(combine_to_dict(...))
  7. Define function 'combine_to_dict' (stored in memory, not executed yet)
  8. Execute function call: print(combine_to_dict(...))
  9. Define function 'combine_to_dict' (stored in memory, not executed yet)
  10. Execute function call: print(combine_to_dict(...))
  11. Define function 'combine_to_dict' (stored in memory, not executed yet)
  12. Define function 'combine_to_dict' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(combine_to_dict(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'combine_to_dict' is defined but NOT executed until called
    - 'combine_to_dict' is defined but NOT executed until called
    - 'combine_to_dict' is defined but NOT executed until called
    - 'combine_to_dict' is defined but NOT executed until called
    - 'combine_to_dict' is defined but NOT executed until called
    - 'combine_to_dict' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 262:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., >, (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_pairs, len, list, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: filter_pairs(lst1, lst2)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)
  • Function call: list(value)
  • Function call: filter(value, value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: filter_pairs(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_pairs' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(filter_pairs(...))
  5. Define function 'filter_pairs' (stored in memory, not executed yet)
  6. Execute function call: print(filter_pairs(...))
  7. Define function 'filter_pairs' (stored in memory, not executed yet)
  8. Execute function call: print(filter_pairs(...))
  9. Define function 'filter_pairs' (stored in memory, not executed yet)
  10. Execute function call: print(filter_pairs(...))
  11. Define function 'filter_pairs' (stored in memory, not executed yet)
  12. Define function 'filter_pairs' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(filter_pairs(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 263:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
    def filter_pairs(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., <, (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_pairs, len, list, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: filter_pairs(lst1, lst2)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)
  • Function call: list(value)
  • Function call: filter(value, value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: filter_pairs(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_pairs(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_pairs' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(filter_pairs(...))
  5. Define function 'filter_pairs' (stored in memory, not executed yet)
  6. Execute function call: print(filter_pairs(...))
  7. Define function 'filter_pairs' (stored in memory, not executed yet)
  8. Execute function call: print(filter_pairs(...))
  9. Define function 'filter_pairs' (stored in memory, not executed yet)
  10. Execute function call: print(filter_pairs(...))
  11. Define function 'filter_pairs' (stored in memory, not executed yet)
  12. Define function 'filter_pairs' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(filter_pairs(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
    - 'filter_pairs' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 264:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def add_nested_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def add_nested_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def add_nested_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def add_nested_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def add_nested_lists(lst1, lst2)
      Returns a value via 'return' keyword
    def add_nested_lists(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: +, ., (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: add_nested_lists, len, list, map, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'added_sublist'
  • Function call: zip(sublist1, sublist2)
  • Binary operation: x + y
  • Function call: added_sublist.append(computed expression)
  • Function call: result.append(added_sublist)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: add_nested_lists(lst1, lst2)
  • Binary operation: a + b
  • Function call: zip(lst1, lst2)
  • Function call: zip(s1, s2)
  • Function call: print(value)
  • Function call: add_nested_lists(value, value)
  • Binary operation: a + b
  • Function call: list(value)
  • Function call: map(value, lst1, lst2)
  • Function call: zip(s1, s2)
  • Function call: print(value)
  • Function call: add_nested_lists(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Assignment: right side evaluated first, then stored in 'sub'
  • Function call: range(value)
  • Function call: len(value)
  • Binary operation: expression + expression
  • Function call: sub.append(computed expression)
  • Function call: result.append(sub)
  • Function call: print(value)
  • Function call: add_nested_lists(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: add_nested_lists(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: add_nested_lists(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'add_nested_lists' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(add_nested_lists(...))
  5. Define function 'add_nested_lists' (stored in memory, not executed yet)
  6. Execute function call: print(add_nested_lists(...))
  7. Define function 'add_nested_lists' (stored in memory, not executed yet)
  8. Execute function call: print(add_nested_lists(...))
  9. Define function 'add_nested_lists' (stored in memory, not executed yet)
  10. Execute function call: print(add_nested_lists(...))
  11. Define function 'add_nested_lists' (stored in memory, not executed yet)
  12. Define function 'add_nested_lists' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(add_nested_lists(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'add_nested_lists' is defined but NOT executed until called
    - 'add_nested_lists' is defined but NOT executed until called
    - 'add_nested_lists' is defined but NOT executed until called
    - 'add_nested_lists' is defined but NOT executed until called
    - 'add_nested_lists' is defined but NOT executed until called
    - 'add_nested_lists' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 265:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def euclidean_distance(point1, point2)
      Returns a value via 'return' keyword
    def euclidean_distance(point1, point2)
      Returns a value via 'return' keyword
    def euclidean_distance(point1, point2)
      Returns a value via 'return' keyword
    def euclidean_distance(point1, point2)
      Returns a value via 'return' keyword
    def euclidean_distance(point1, point2)
      Returns a value via 'return' keyword
    def euclidean_distance(point1, point2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: -, ., (), **
  Control structures: for loop iteration; generator expression
  Functions called: euclidean_distance, print, sum, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'distance_squared'
  • Function call: zip(point1, point2)
  • Binary operation: expression ** 2
  • Binary operation: x1 - x2
  • Function call: math.sqrt(distance_squared)
  • Assignment: right side evaluated first, then stored in 'point1'
  • Assignment: right side evaluated first, then stored in 'point2'
  • Function call: print(value)
  • Function call: euclidean_distance(point1, point2)
  • Binary operation: expression ** 2
  • Binary operation: x1 - x2
  • Function call: math.sqrt(value)
  • Function call: sum(value)
  • Function call: zip(point1, point2)
  • Function call: print(value)
  • Function call: euclidean_distance(value, value)
  • Function call: math.dist(point1, point2)
  • Function call: print(value)
  • Function call: euclidean_distance(value, value)
  • Binary operation: sum(...) ** 0.5
  • Binary operation: expression ** 2
  • Binary operation: a - b
  • Function call: sum(value)
  • Function call: zip(point1, point2)
  • Function call: print(value)
  • Function call: euclidean_distance(value, value)
  • Function call: math.sqrt(distance_squared)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: math.sqrt(distance_squared)
  • Assignment: right side evaluated first, then stored in 'point1'
  • Assignment: right side evaluated first, then stored in 'point2'
  • Function call: euclidean_distance(point1, point2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: euclidean_distance(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: math
  2. Define function 'euclidean_distance' (stored in memory, not executed yet)
  3. Assign value to variable: point1
  4. Assign value to variable: point2
  5. Execute function call: print(euclidean_distance(...))
  6. Import module: math
  7. Define function 'euclidean_distance' (stored in memory, not executed yet)
  8. Execute function call: print(euclidean_distance(...))
  9. Import module: math
  10. Define function 'euclidean_distance' (stored in memory, not executed yet)
  11. Execute function call: print(euclidean_distance(...))
  12. Define function 'euclidean_distance' (stored in memory, not executed yet)
  13. Execute function call: print(euclidean_distance(...))
  14. Define function 'euclidean_distance' (stored in memory, not executed yet)
  15. Define function 'euclidean_distance' (stored in memory, not executed yet)
  16. Assign value to variable: point1
  17. Assign value to variable: point2
  18. Assign value to variable: result
  19. Execute function call: print(result)
  20. Execute function call: print(euclidean_distance(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'euclidean_distance' is defined but NOT executed until called
    - 'euclidean_distance' is defined but NOT executed until called
    - 'euclidean_distance' is defined but NOT executed until called
    - 'euclidean_distance' is defined but NOT executed until called
    - 'euclidean_distance' is defined but NOT executed until called
    - 'euclidean_distance' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 266:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def interleave(lst1, lst2)
      Returns a value via 'return' keyword
    def interleave(lst1, lst2)
      Returns a value via 'return' keyword
    def interleave(lst1, lst2)
      Returns a value via 'return' keyword
    def interleave(lst1, lst2)
      Returns a value via 'return' keyword
    def interleave(lst1, lst2)
      Returns a value via 'return' keyword
    def interleave(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration; list comprehension
  Functions called: interleave, list, print, sum, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Function call: result.append(x)
  • Function call: result.append(y)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: interleave(lst1, lst2)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: interleave(value, value)
  • Function call: list(value)
  • Function call: sum(value, value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: interleave(value, value)
  • Function call: list(value)
  • Function call: chain.from_iterable(value)
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: interleave(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: interleave(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: interleave(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'interleave' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(interleave(...))
  5. Define function 'interleave' (stored in memory, not executed yet)
  6. Execute function call: print(interleave(...))
  7. Define function 'interleave' (stored in memory, not executed yet)
  8. Execute function call: print(interleave(...))
  9. Import from itertools: chain
  10. Define function 'interleave' (stored in memory, not executed yet)
  11. Execute function call: print(interleave(...))
  12. Define function 'interleave' (stored in memory, not executed yet)
  13. Define function 'interleave' (stored in memory, not executed yet)
  14. Assign value to variable: lst1
  15. Assign value to variable: lst2
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(interleave(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'interleave' is defined but NOT executed until called
    - 'interleave' is defined but NOT executed until called
    - 'interleave' is defined but NOT executed until called
    - 'interleave' is defined but NOT executed until called
    - 'interleave' is defined but NOT executed until called
    - 'interleave' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 267:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def multiply_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def multiply_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def multiply_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def multiply_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def multiply_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
    def multiply_2_lst(lst1, lst2)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, ., (), []
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: len, list, map, multiply_2_lst, print, range, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(lst1, lst2)
  • Binary operation: x * y
  • Function call: result.append(computed expression)
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: print(value)
  • Function call: multiply_2_lst(lst1, lst2)
  • Binary operation: x * y
  • Function call: zip(lst1, lst2)
  • Function call: print(value)
  • Function call: multiply_2_lst(value, value)
  • Binary operation: a * b
  • Function call: list(value)
  • Function call: map(value, lst1, lst2)
  • Function call: print(value)
  • Function call: multiply_2_lst(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(value)
  • Function call: len(lst1)
  • Binary operation: expression * expression
  • Function call: result.append(computed expression)
  • Function call: print(value)
  • Function call: multiply_2_lst(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst1'
  • Assignment: right side evaluated first, then stored in 'lst2'
  • Function call: multiply_2_lst(lst1, lst2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: multiply_2_lst(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'multiply_2_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst1
  3. Assign value to variable: lst2
  4. Execute function call: print(multiply_2_lst(...))
  5. Define function 'multiply_2_lst' (stored in memory, not executed yet)
  6. Execute function call: print(multiply_2_lst(...))
  7. Define function 'multiply_2_lst' (stored in memory, not executed yet)
  8. Execute function call: print(multiply_2_lst(...))
  9. Define function 'multiply_2_lst' (stored in memory, not executed yet)
  10. Execute function call: print(multiply_2_lst(...))
  11. Define function 'multiply_2_lst' (stored in memory, not executed yet)
  12. Define function 'multiply_2_lst' (stored in memory, not executed yet)
  13. Assign value to variable: lst1
  14. Assign value to variable: lst2
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(multiply_2_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'multiply_2_lst' is defined but NOT executed until called
    - 'multiply_2_lst' is defined but NOT executed until called
    - 'multiply_2_lst' is defined but NOT executed until called
    - 'multiply_2_lst' is defined but NOT executed until called
    - 'multiply_2_lst' is defined but NOT executed until called
    - 'multiply_2_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 268:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def dot_product(vector1, vector2)
      Returns a value via 'return' keyword
    def dot_product(vector1, vector2)
      Returns a value via 'return' keyword
    def dot_product(vector1, vector2)
      Returns a value via 'return' keyword
    def dot_product(vector1, vector2)
      Returns a value via 'return' keyword
    def dot_product(vector1, vector2)
      Returns a value via 'return' keyword
    def dot_product(vector1, vector2)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: *, +, (), []
  Control structures: for loop iteration; generator expression; lambda expression
  Functions called: dot_product, map, print, reduce, sum, zip
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: zip(vector1, vector2)
  • Binary operation: x * y
  • Assignment: right side evaluated first, then stored in 'vector1'
  • Assignment: right side evaluated first, then stored in 'vector2'
  • Function call: print(value)
  • Function call: dot_product(vector1, vector2)
  • Binary operation: x * y
  • Function call: sum(value)
  • Function call: zip(vector1, vector2)
  • Function call: print(value)
  • Function call: dot_product(value, value)
  • Binary operation: a * b
  • Function call: sum(value)
  • Function call: map(value, vector1, vector2)
  • Function call: print(value)
  • Function call: dot_product(value, value)
  • Binary operation: acc + expression
  • Binary operation: expression * expression
  • Function call: reduce(value, value, 0)
  • Function call: zip(vector1, vector2)
  • Function call: print(value)
  • Function call: dot_product(value, value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'vector1'
  • Assignment: right side evaluated first, then stored in 'vector2'
  • Function call: dot_product(vector1, vector2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: dot_product(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'dot_product' (stored in memory, not executed yet)
  2. Assign value to variable: vector1
  3. Assign value to variable: vector2
  4. Execute function call: print(dot_product(...))
  5. Define function 'dot_product' (stored in memory, not executed yet)
  6. Execute function call: print(dot_product(...))
  7. Define function 'dot_product' (stored in memory, not executed yet)
  8. Execute function call: print(dot_product(...))
  9. Import from functools: reduce
  10. Define function 'dot_product' (stored in memory, not executed yet)
  11. Execute function call: print(dot_product(...))
  12. Define function 'dot_product' (stored in memory, not executed yet)
  13. Define function 'dot_product' (stored in memory, not executed yet)
  14. Assign value to variable: vector1
  15. Assign value to variable: vector2
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(dot_product(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'dot_product' is defined but NOT executed until called
    - 'dot_product' is defined but NOT executed until called
    - 'dot_product' is defined but NOT executed until called
    - 'dot_product' is defined but NOT executed until called
    - 'dot_product' is defined but NOT executed until called
    - 'dot_product' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over zip(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 269:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def is_odd(x)
      Returns a value via 'return' keyword
    def filter_even_numbers(lst)
      Returns a value via 'return' keyword
    def filter_even_numbers(lst)
      Returns a value via 'return' keyword
    def filter_even_numbers(lst)
      Returns a value via 'return' keyword
    def filter_even_numbers(lst)
      Returns a value via 'return' keyword
    def is_odd(x)
      Returns a value via 'return' keyword
    def is_odd(x)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, ., !=, ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_even_numbers, is_odd, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x % 2
  • Function call: list(value)
  • Function call: filter(is_odd, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: filter_even_numbers(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: x % 2
  • Function call: list(value)
  • Function call: filter(value, lst)
  • Function call: print(value)
  • Function call: filter_even_numbers(value)
  • Binary operation: x % 2
  • Function call: print(value)
  • Function call: filter_even_numbers(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % 2
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(x)
  • Function call: print(value)
  • Function call: filter_even_numbers(value)
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: is_odd(x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_odd(3)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_odd' (stored in memory, not executed yet)
  2. Define function 'filter_even_numbers' (stored in memory, not executed yet)
  3. Assign value to variable: lst
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_even_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(filter_even_numbers(...))
  8. Define function 'filter_even_numbers' (stored in memory, not executed yet)
  9. Execute function call: print(filter_even_numbers(...))
  10. Define function 'filter_even_numbers' (stored in memory, not executed yet)
  11. Execute function call: print(filter_even_numbers(...))
  12. Define function 'is_odd' (stored in memory, not executed yet)
  13. Define function 'is_odd' (stored in memory, not executed yet)
  14. Assign value to variable: x
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(is_odd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_odd' is defined but NOT executed until called
    - 'filter_even_numbers' is defined but NOT executed until called
    - 'filter_even_numbers' is defined but NOT executed until called
    - 'filter_even_numbers' is defined but NOT executed until called
    - 'filter_even_numbers' is defined but NOT executed until called
    - 'is_odd' is defined but NOT executed until called
    - 'is_odd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 270:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def is_even(x)
      Returns a value via 'return' keyword
    def filter_odd_numbers(lst)
      Returns a value via 'return' keyword
    def filter_odd_numbers(lst)
      Returns a value via 'return' keyword
    def filter_odd_numbers(lst)
      Returns a value via 'return' keyword
    def filter_odd_numbers(lst)
      Returns a value via 'return' keyword
    def is_even(x)
      Returns a value via 'return' keyword
    def is_even(x)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, ., (), ==
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_odd_numbers, is_even, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x % 2
  • Function call: list(value)
  • Function call: filter(is_even, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: filter_odd_numbers(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: x % 2
  • Function call: list(value)
  • Function call: filter(value, lst)
  • Function call: print(value)
  • Function call: filter_odd_numbers(value)
  • Binary operation: x % 2
  • Function call: print(value)
  • Function call: filter_odd_numbers(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % 2
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(x)
  • Function call: print(value)
  • Function call: filter_odd_numbers(value)
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: is_even(x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_even(3)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_even' (stored in memory, not executed yet)
  2. Define function 'filter_odd_numbers' (stored in memory, not executed yet)
  3. Assign value to variable: lst
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_odd_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(filter_odd_numbers(...))
  8. Define function 'filter_odd_numbers' (stored in memory, not executed yet)
  9. Execute function call: print(filter_odd_numbers(...))
  10. Define function 'filter_odd_numbers' (stored in memory, not executed yet)
  11. Execute function call: print(filter_odd_numbers(...))
  12. Define function 'is_even' (stored in memory, not executed yet)
  13. Define function 'is_even' (stored in memory, not executed yet)
  14. Assign value to variable: x
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(is_even(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_even' is defined but NOT executed until called
    - 'filter_odd_numbers' is defined but NOT executed until called
    - 'filter_odd_numbers' is defined but NOT executed until called
    - 'filter_odd_numbers' is defined but NOT executed until called
    - 'filter_odd_numbers' is defined but NOT executed until called
    - 'is_even' is defined but NOT executed until called
    - 'is_even' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 272:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def is_non_negative(x)
      Returns a value via 'return' keyword
    def filter_negative_numbers(lst)
      Returns a value via 'return' keyword
    def filter_negative_numbers(lst)
      Returns a value via 'return' keyword
    def filter_negative_numbers(lst)
      Returns a value via 'return' keyword
    def filter_negative_numbers(lst)
      Returns a value via 'return' keyword
    def is_non_negative(x)
      Returns a value via 'return' keyword
    def is_non_negative(x)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: -, ., <, (), >=, not
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_negative_numbers, is_non_negative, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: filter(is_non_negative, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: filter_negative_numbers(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, lst)
  • Function call: print(value)
  • Function call: filter_negative_numbers(value)
  • Function call: print(value)
  • Function call: filter_negative_numbers(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(x)
  • Function call: print(value)
  • Function call: filter_negative_numbers(value)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: is_non_negative(x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_non_negative(3)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_non_negative' (stored in memory, not executed yet)
  2. Define function 'filter_negative_numbers' (stored in memory, not executed yet)
  3. Assign value to variable: lst
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_negative_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(filter_negative_numbers(...))
  8. Define function 'filter_negative_numbers' (stored in memory, not executed yet)
  9. Execute function call: print(filter_negative_numbers(...))
  10. Define function 'filter_negative_numbers' (stored in memory, not executed yet)
  11. Execute function call: print(filter_negative_numbers(...))
  12. Define function 'is_non_negative' (stored in memory, not executed yet)
  13. Define function 'is_non_negative' (stored in memory, not executed yet)
  14. Assign value to variable: x
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(is_non_negative(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_non_negative' is defined but NOT executed until called
    - 'filter_negative_numbers' is defined but NOT executed until called
    - 'filter_negative_numbers' is defined but NOT executed until called
    - 'filter_negative_numbers' is defined but NOT executed until called
    - 'filter_negative_numbers' is defined but NOT executed until called
    - 'is_non_negative' is defined but NOT executed until called
    - 'is_non_negative' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 274:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def filter_palindromes(lst)
      Returns a value via 'return' keyword
    def filter_palindromes(lst)
      Returns a value via 'return' keyword
    def filter_palindromes(lst)
      Returns a value via 'return' keyword
    def filter_palindromes(lst)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, lambda, return, while
  Operators used: -, <, !=, (), ==, []
  Control structures: if/else conditional branching; while loop; list comprehension; lambda expression
  Functions called: filter, filter_palindromes, is_pal, is_palindrome, len, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: filter(is_palindrome, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: filter_palindromes(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, lst)
  • Function call: print(value)
  • Function call: filter_palindromes(value)
  • Function call: print(value)
  • Function call: filter_palindromes(value)
  • Binary operation: len(...) - 1
  • Function call: len(s)
  • Condition branch: evaluates boolean expression first
  • Function call: is_pal(s)
  • Function call: print(value)
  • Function call: filter_palindromes(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_palindrome(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Define function 'filter_palindromes' (stored in memory, not executed yet)
  3. Assign value to variable: lst
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_palindromes' (stored in memory, not executed yet)
  7. Execute function call: print(filter_palindromes(...))
  8. Define function 'filter_palindromes' (stored in memory, not executed yet)
  9. Execute function call: print(filter_palindromes(...))
  10. Define function 'filter_palindromes' (stored in memory, not executed yet)
  11. Execute function call: print(filter_palindromes(...))
  12. Define function 'is_palindrome' (stored in memory, not executed yet)
  13. Define function 'is_palindrome' (stored in memory, not executed yet)
  14. Assign value to variable: s
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'filter_palindromes' is defined but NOT executed until called
    - 'filter_palindromes' is defined but NOT executed until called
    - 'filter_palindromes' is defined but NOT executed until called
    - 'filter_palindromes' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 276:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_divisible(numbers, divisor)
      Returns a value via 'return' keyword
    def filter_divisible(numbers, divisor)
      Returns a value via 'return' keyword
    def filter_divisible(numbers, divisor)
      Returns a value via 'return' keyword
    def filter_divisible(numbers, divisor)
      Returns a value via 'return' keyword
    def filter_divisible(numbers, divisor)
      Returns a value via 'return' keyword
    def filter_divisible(numbers, divisor)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, ., (), ==
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_divisible, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: n % divisor
  • Function call: list(value)
  • Function call: filter(is_divisible, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: filter_divisible(numbers, divisor)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: n % divisor
  • Function call: list(value)
  • Function call: filter(value, numbers)
  • Function call: print(value)
  • Function call: filter_divisible(value, 5)
  • Binary operation: n % divisor
  • Function call: print(value)
  • Function call: filter_divisible(value, 5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: n % divisor
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(n)
  • Function call: print(value)
  • Function call: filter_divisible(value, 5)
  • Binary operation: n % divisor
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: n % divisor
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: filter_divisible(numbers, divisor)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_divisible(value, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_divisible' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Assign value to variable: divisor
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_divisible' (stored in memory, not executed yet)
  7. Execute function call: print(filter_divisible(...))
  8. Define function 'filter_divisible' (stored in memory, not executed yet)
  9. Execute function call: print(filter_divisible(...))
  10. Define function 'filter_divisible' (stored in memory, not executed yet)
  11. Execute function call: print(filter_divisible(...))
  12. Define function 'filter_divisible' (stored in memory, not executed yet)
  13. Define function 'filter_divisible' (stored in memory, not executed yet)
  14. Assign value to variable: numbers
  15. Assign value to variable: divisor
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(filter_divisible(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_divisible' is defined but NOT executed until called
    - 'filter_divisible' is defined but NOT executed until called
    - 'filter_divisible' is defined but NOT executed until called
    - 'filter_divisible' is defined but NOT executed until called
    - 'filter_divisible' is defined but NOT executed until called
    - 'filter_divisible' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 277:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def is_not_divisible(num, divisor)
      Returns a value via 'return' keyword
    def is_not_divisible(num, divisor)
      Returns a value via 'return' keyword
    def is_not_divisible(num, divisor)
      Returns a value via 'return' keyword
    def is_not_divisible(num, divisor)
      Returns a value via 'return' keyword
    def is_not_divisible(num, divisor)
      Returns a value via 'return' keyword
    def is_not_divisible(num, divisor)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, ., !=, ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, is_not_divisible, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: num % divisor
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: list(value)
  • Function call: filter(is_not_divisible, numbers)
  • Assignment: right side evaluated first, then stored in 'filtered_numbers'
  • Function call: print(filtered_numbers)
  • Binary operation: num % divisor
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Binary operation: n % divisor
  • Function call: list(value)
  • Function call: filter(value, numbers)
  • Assignment: right side evaluated first, then stored in 'filtered_numbers'
  • Function call: print(filtered_numbers)
  • Binary operation: num % divisor
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Binary operation: n % divisor
  • Assignment: right side evaluated first, then stored in 'filtered_numbers'
  • Function call: print(filtered_numbers)
  • Binary operation: num % divisor
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Assignment: right side evaluated first, then stored in 'filtered_numbers'
  • Binary operation: n % divisor
  • Condition branch: evaluates boolean expression first
  • Function call: filtered_numbers.append(n)
  • Function call: print(filtered_numbers)
  • Binary operation: num % divisor
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: num % divisor
  • Assignment: right side evaluated first, then stored in 'num'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: is_not_divisible(num, divisor)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_not_divisible(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_not_divisible' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Assign value to variable: divisor
  4. Assign value to variable: filtered_numbers
  5. Execute function call: print(filtered_numbers)
  6. Define function 'is_not_divisible' (stored in memory, not executed yet)
  7. Assign value to variable: numbers
  8. Assign value to variable: divisor
  9. Assign value to variable: filtered_numbers
  10. Execute function call: print(filtered_numbers)
  11. Define function 'is_not_divisible' (stored in memory, not executed yet)
  12. Assign value to variable: numbers
  13. Assign value to variable: divisor
  14. Assign value to variable: filtered_numbers
  15. Execute function call: print(filtered_numbers)
  16. Define function 'is_not_divisible' (stored in memory, not executed yet)
  17. Assign value to variable: numbers
  18. Assign value to variable: divisor
  19. Assign value to variable: filtered_numbers
  20. For loop: iterate over numbers
  21. Execute function call: print(filtered_numbers)
  22. Define function 'is_not_divisible' (stored in memory, not executed yet)
  23. Define function 'is_not_divisible' (stored in memory, not executed yet)
  24. Assign value to variable: num
  25. Assign value to variable: divisor
  26. Assign value to variable: result
  27. Execute function call: print(result)
  28. Execute function call: print(is_not_divisible(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_not_divisible' is defined but NOT executed until called
    - 'is_not_divisible' is defined but NOT executed until called
    - 'is_not_divisible' is defined but NOT executed until called
    - 'is_not_divisible' is defined but NOT executed until called
    - 'is_not_divisible' is defined but NOT executed until called
    - 'is_not_divisible' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 278:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
    def filter_non_empty(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., !=, ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension
  Functions called: filter, filter_non_empty, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: filter(is_non_empty, strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: filter_non_empty(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(None, strings)
  • Function call: print(value)
  • Function call: filter_non_empty(value)
  • Function call: print(value)
  • Function call: filter_non_empty(value)
  • Function call: list(value)
  • Function call: filter(bool, strings)
  • Function call: print(value)
  • Function call: filter_non_empty(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(s)
  • Function call: print(value)
  • Function call: filter_non_empty(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: filter_non_empty(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_non_empty('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_non_empty' (stored in memory, not executed yet)
  2. Assign value to variable: strings
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'filter_non_empty' (stored in memory, not executed yet)
  6. Execute function call: print(filter_non_empty(...))
  7. Define function 'filter_non_empty' (stored in memory, not executed yet)
  8. Execute function call: print(filter_non_empty(...))
  9. Define function 'filter_non_empty' (stored in memory, not executed yet)
  10. Execute function call: print(filter_non_empty(...))
  11. Define function 'filter_non_empty' (stored in memory, not executed yet)
  12. Execute function call: print(filter_non_empty(...))
  13. Define function 'filter_non_empty' (stored in memory, not executed yet)
  14. Define function 'filter_non_empty' (stored in memory, not executed yet)
  15. Assign value to variable: strings
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(filter_non_empty(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_non_empty' is defined but NOT executed until called
    - 'filter_non_empty' is defined but NOT executed until called
    - 'filter_non_empty' is defined but NOT executed until called
    - 'filter_non_empty' is defined but NOT executed until called
    - 'filter_non_empty' is defined but NOT executed until called
    - 'filter_non_empty' is defined but NOT executed until called
    - 'filter_non_empty' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over strings, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 279:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_prime(numbers)
      Returns a value via 'return' keyword
    def filter_prime(numbers)
      Returns a value via 'return' keyword
    def filter_prime(numbers)
      Returns a value via 'return' keyword
    def filter_prime(numbers)
      Returns a value via 'return' keyword
    def filter_prime(numbers)
      Returns a value via 'return' keyword
    def filter_prime(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: %, +, ., <, >, !=, (), **, ==, >=, and
  Control structures: for loop iteration; list comprehension; generator expression; lambda expression
  Functions called: all, filter, filter_prime, int, is_prime, list, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: n % i
  • Condition branch: evaluates boolean expression first
  • Function call: list(value)
  • Function call: filter(is_prime, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: filter_prime(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: n % i
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: list(value)
  • Function call: filter(value, numbers)
  • Function call: all(value)
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: filter_prime(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: n % i
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: all(value)
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: filter_prime(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: n % i
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: is_prime(n)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(n)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: filter_prime(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: filter_prime(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_prime(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_prime' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'filter_prime' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Define function 'filter_prime' (stored in memory, not executed yet)
  10. Assign value to variable: numbers
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Define function 'filter_prime' (stored in memory, not executed yet)
  14. Assign value to variable: numbers
  15. Assign value to variable: result
  16. Execute function call: print(result)
  17. Define function 'filter_prime' (stored in memory, not executed yet)
  18. Define function 'filter_prime' (stored in memory, not executed yet)
  19. Assign value to variable: numbers
  20. Assign value to variable: result
  21. Execute function call: print(result)
  22. Execute function call: print(filter_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_prime' is defined but NOT executed until called
    - 'filter_prime' is defined but NOT executed until called
    - 'filter_prime' is defined but NOT executed until called
    - 'filter_prime' is defined but NOT executed until called
    - 'filter_prime' is defined but NOT executed until called
    - 'filter_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 280:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_floats(items)
      Returns a value via 'return' keyword
    def filter_floats(items)
      Returns a value via 'return' keyword
    def filter_floats(items)
      Returns a value via 'return' keyword
    def filter_floats(items)
      Returns a value via 'return' keyword
    def filter_floats(items)
      Returns a value via 'return' keyword
    def filter_floats(items)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), not
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_floats, isinstance, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: isinstance(x, float)
  • Function call: list(value)
  • Function call: filter(is_not_float, items)
  • Assignment: right side evaluated first, then stored in 'items'
  • Function call: filter_floats(items)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, items)
  • Function call: isinstance(x, float)
  • Function call: print(value)
  • Function call: filter_floats(value)
  • Function call: isinstance(x, float)
  • Function call: print(value)
  • Function call: filter_floats(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: isinstance(x, float)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(x)
  • Function call: print(value)
  • Function call: filter_floats(value)
  • Function call: isinstance(x, float)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: isinstance(x, float)
  • Assignment: right side evaluated first, then stored in 'items'
  • Function call: filter_floats(items)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_floats(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_floats' (stored in memory, not executed yet)
  2. Assign value to variable: items
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'filter_floats' (stored in memory, not executed yet)
  6. Execute function call: print(filter_floats(...))
  7. Define function 'filter_floats' (stored in memory, not executed yet)
  8. Execute function call: print(filter_floats(...))
  9. Define function 'filter_floats' (stored in memory, not executed yet)
  10. Execute function call: print(filter_floats(...))
  11. Define function 'filter_floats' (stored in memory, not executed yet)
  12. Define function 'filter_floats' (stored in memory, not executed yet)
  13. Assign value to variable: items
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(filter_floats(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_floats' is defined but NOT executed until called
    - 'filter_floats' is defined but NOT executed until called
    - 'filter_floats' is defined but NOT executed until called
    - 'filter_floats' is defined but NOT executed until called
    - 'filter_floats' is defined but NOT executed until called
    - 'filter_floats' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over items, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 281:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_uppercase(strings)
      Returns a value via 'return' keyword
    def filter_uppercase(strings)
      Returns a value via 'return' keyword
    def filter_uppercase(strings)
      Returns a value via 'return' keyword
    def filter_uppercase(strings)
      Returns a value via 'return' keyword
    def filter_uppercase(strings)
      Returns a value via 'return' keyword
    def filter_uppercase(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), not
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_uppercase, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.isupper()
  • Function call: list(value)
  • Function call: filter(is_not_uppercase, strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: filter_uppercase(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, strings)
  • Function call: s.isupper()
  • Function call: print(value)
  • Function call: filter_uppercase(value)
  • Function call: s.isupper()
  • Function call: print(value)
  • Function call: filter_uppercase(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: s.isupper()
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(s)
  • Function call: print(value)
  • Function call: filter_uppercase(value)
  • Function call: s.isupper()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: s.isupper()
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: filter_uppercase(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_uppercase('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_uppercase' (stored in memory, not executed yet)
  2. Assign value to variable: strings
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'filter_uppercase' (stored in memory, not executed yet)
  6. Execute function call: print(filter_uppercase(...))
  7. Define function 'filter_uppercase' (stored in memory, not executed yet)
  8. Execute function call: print(filter_uppercase(...))
  9. Define function 'filter_uppercase' (stored in memory, not executed yet)
  10. Execute function call: print(filter_uppercase(...))
  11. Define function 'filter_uppercase' (stored in memory, not executed yet)
  12. Define function 'filter_uppercase' (stored in memory, not executed yet)
  13. Assign value to variable: strings
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(filter_uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_uppercase' is defined but NOT executed until called
    - 'filter_uppercase' is defined but NOT executed until called
    - 'filter_uppercase' is defined but NOT executed until called
    - 'filter_uppercase' is defined but NOT executed until called
    - 'filter_uppercase' is defined but NOT executed until called
    - 'filter_uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over strings, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 282:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_uppercase_again(strings)
      Returns a value via 'return' keyword
    def filter_uppercase_again(strings)
      Returns a value via 'return' keyword
    def filter_uppercase_again(strings)
      Returns a value via 'return' keyword
    def filter_uppercase_again(strings)
      Returns a value via 'return' keyword
    def filter_uppercase_again(strings)
      Returns a value via 'return' keyword
    def filter_uppercase_again(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), not
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_uppercase_again, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.isupper()
  • Function call: list(value)
  • Function call: filter(is_not_uppercase, strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: filter_uppercase_again(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, strings)
  • Function call: s.isupper()
  • Function call: print(value)
  • Function call: filter_uppercase_again(value)
  • Function call: s.isupper()
  • Function call: print(value)
  • Function call: filter_uppercase_again(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: s.isupper()
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(s)
  • Function call: print(value)
  • Function call: filter_uppercase_again(value)
  • Function call: s.isupper()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: s.isupper()
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: filter_uppercase_again(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_uppercase_again('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_uppercase_again' (stored in memory, not executed yet)
  2. Assign value to variable: strings
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'filter_uppercase_again' (stored in memory, not executed yet)
  6. Execute function call: print(filter_uppercase_again(...))
  7. Define function 'filter_uppercase_again' (stored in memory, not executed yet)
  8. Execute function call: print(filter_uppercase_again(...))
  9. Define function 'filter_uppercase_again' (stored in memory, not executed yet)
  10. Execute function call: print(filter_uppercase_again(...))
  11. Define function 'filter_uppercase_again' (stored in memory, not executed yet)
  12. Define function 'filter_uppercase_again' (stored in memory, not executed yet)
  13. Assign value to variable: strings
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(filter_uppercase_again(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_uppercase_again' is defined but NOT executed until called
    - 'filter_uppercase_again' is defined but NOT executed until called
    - 'filter_uppercase_again' is defined but NOT executed until called
    - 'filter_uppercase_again' is defined but NOT executed until called
    - 'filter_uppercase_again' is defined but NOT executed until called
    - 'filter_uppercase_again' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over strings, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 283:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_non_none(items)
      Returns a value via 'return' keyword
    def filter_non_none(items)
      Returns a value via 'return' keyword
    def filter_non_none(items)
      Returns a value via 'return' keyword
    def filter_non_none(items)
      Returns a value via 'return' keyword
    def filter_non_none(items)
      Returns a value via 'return' keyword
    def filter_non_none(items)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), is not
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_non_none, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: filter(is_not_none, items)
  • Assignment: right side evaluated first, then stored in 'items'
  • Function call: filter_non_none(items)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, items)
  • Function call: print(value)
  • Function call: filter_non_none(value)
  • Function call: print(value)
  • Function call: filter_non_none(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(x)
  • Function call: print(value)
  • Function call: filter_non_none(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'items'
  • Function call: filter_non_none(items)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_non_none(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_non_none' (stored in memory, not executed yet)
  2. Assign value to variable: items
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'filter_non_none' (stored in memory, not executed yet)
  6. Execute function call: print(filter_non_none(...))
  7. Define function 'filter_non_none' (stored in memory, not executed yet)
  8. Execute function call: print(filter_non_none(...))
  9. Define function 'filter_non_none' (stored in memory, not executed yet)
  10. Execute function call: print(filter_non_none(...))
  11. Define function 'filter_non_none' (stored in memory, not executed yet)
  12. Define function 'filter_non_none' (stored in memory, not executed yet)
  13. Assign value to variable: items
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(filter_non_none(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_non_none' is defined but NOT executed until called
    - 'filter_non_none' is defined but NOT executed until called
    - 'filter_non_none' is defined but NOT executed until called
    - 'filter_non_none' is defined but NOT executed until called
    - 'filter_non_none' is defined but NOT executed until called
    - 'filter_non_none' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over items, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 284:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def filter_by_substring(items, substring)
      Returns a value via 'return' keyword
    def filter_by_substring(items, substring)
      Returns a value via 'return' keyword
    def filter_by_substring(items, substring)
      Returns a value via 'return' keyword
    def filter_by_substring(items, substring)
      Returns a value via 'return' keyword
    def filter_by_substring(items, substring)
      Returns a value via 'return' keyword
    def filter_by_substring(items, substring)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter, filter_by_substring, list, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: filter(does_not_contain_substring, items)
  • Assignment: right side evaluated first, then stored in 'items'
  • Assignment: right side evaluated first, then stored in 'substring'
  • Function call: filter_by_substring(items, substring)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: filter(value, items)
  • Function call: print(value)
  • Function call: filter_by_substring(value, 'apple')
  • Function call: print(value)
  • Function call: filter_by_substring(value, 'apple')
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(s)
  • Function call: print(value)
  • Function call: filter_by_substring(value, 'apple')
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'items'
  • Assignment: right side evaluated first, then stored in 'substring'
  • Function call: filter_by_substring(items, substring)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_by_substring(value, 'world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_by_substring' (stored in memory, not executed yet)
  2. Assign value to variable: items
  3. Assign value to variable: substring
  4. Assign value to variable: result
  5. Execute function call: print(result)
  6. Define function 'filter_by_substring' (stored in memory, not executed yet)
  7. Execute function call: print(filter_by_substring(...))
  8. Define function 'filter_by_substring' (stored in memory, not executed yet)
  9. Execute function call: print(filter_by_substring(...))
  10. Define function 'filter_by_substring' (stored in memory, not executed yet)
  11. Execute function call: print(filter_by_substring(...))
  12. Define function 'filter_by_substring' (stored in memory, not executed yet)
  13. Define function 'filter_by_substring' (stored in memory, not executed yet)
  14. Assign value to variable: items
  15. Assign value to variable: substring
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(filter_by_substring(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_by_substring' is defined but NOT executed until called
    - 'filter_by_substring' is defined but NOT executed until called
    - 'filter_by_substring' is defined but NOT executed until called
    - 'filter_by_substring' is defined but NOT executed until called
    - 'filter_by_substring' is defined but NOT executed until called
    - 'filter_by_substring' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over items, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 286:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
    def sum_first_10_natural_numbers()
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return, while
  Operators used: *, +, (), //, <=
  Control structures: for loop iteration; while loop; lambda expression
  Functions called: print, range, reduce, sum, sum_first_10_natural_numbers
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'i'
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: sum_first_10_natural_numbers()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: range(1, 11)
  • Function call: print(value)
  • Function call: sum_first_10_natural_numbers()
  • Binary operation: expression // 2
  • Binary operation: 10 * 11
  • Function call: print(value)
  • Function call: sum_first_10_natural_numbers()
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: range(1, 11)
  • Function call: print(value)
  • Function call: sum_first_10_natural_numbers()
  • Binary operation: acc + x
  • Function call: reduce(value, value, 0)
  • Function call: range(1, 11)
  • Function call: print(value)
  • Function call: sum_first_10_natural_numbers()
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'i'
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'i'
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  5. Execute function call: print(sum_first_10_natural_numbers(...))
  6. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  7. Execute function call: print(sum_first_10_natural_numbers(...))
  8. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  9. Execute function call: print(sum_first_10_natural_numbers(...))
  10. Import from functools: reduce
  11. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  12. Execute function call: print(sum_first_10_natural_numbers(...))
  13. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  14. Define function 'sum_first_10_natural_numbers' (stored in memory, not executed yet)
  15. Assign value to variable: i
  16. Assign value to variable: total
  17. While loop: repeat while condition is true
  18. Assign value to variable: result
  19. Execute function call: print(result)
  20. Assign value to variable: i
  21. Assign value to variable: total
  22. While loop: repeat while condition is true
  23. Assign value to variable: result
  24. Execute function call: print(result)

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
    - 'sum_first_10_natural_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 289:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_of_digits(number)
      Returns a value via 'return' keyword
    def sum_of_digits(number)
      Returns a value via 'return' keyword
    def sum_of_digits(number)
      Returns a value via 'return' keyword
    def sum_of_digits(number)
      Returns a value via 'return' keyword
    def sum_of_digits(number)
      Returns a value via 'return' keyword
    def sum_of_digits(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: %, +, >, (), //, ==
  Control structures: if/else conditional branching; for loop iteration; while loop; generator expression
  Functions called: abs, int, print, str, sum, sum_of_digits
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: number % 10
  • Assignment: right side evaluated first, then stored in 'digit'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: sum_of_digits(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: sum(value)
  • Function call: int(d)
  • Function call: str(value)
  • Function call: abs(number)
  • Function call: print(value)
  • Function call: sum_of_digits(12345)
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: str(value)
  • Function call: abs(number)
  • Function call: int(d)
  • Function call: print(value)
  • Function call: sum_of_digits(12345)
  • Function call: abs(number)
  • Assignment: right side evaluated first, then stored in 'number'
  • Condition branch: evaluates boolean expression first
  • Binary operation: expression + sum_of_digits(...)
  • Binary operation: number % 10
  • Binary operation: number // 10
  • Function call: sum_of_digits(computed expression)
  • Function call: print(value)
  • Function call: sum_of_digits(12345)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: sum_of_digits(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_digits(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_digits' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Assign value to variable: result
  4. Execute function call: print(value)
  5. Define function 'sum_of_digits' (stored in memory, not executed yet)
  6. Execute function call: print(sum_of_digits(...))
  7. Define function 'sum_of_digits' (stored in memory, not executed yet)
  8. Execute function call: print(sum_of_digits(...))
  9. Define function 'sum_of_digits' (stored in memory, not executed yet)
  10. Execute function call: print(sum_of_digits(...))
  11. Define function 'sum_of_digits' (stored in memory, not executed yet)
  12. Define function 'sum_of_digits' (stored in memory, not executed yet)
  13. Assign value to variable: number
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(sum_of_digits(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_digits' is defined but NOT executed until called
    - 'sum_of_digits' is defined but NOT executed until called
    - 'sum_of_digits' is defined but NOT executed until called
    - 'sum_of_digits' is defined but NOT executed until called
    - 'sum_of_digits' is defined but NOT executed until called
    - 'sum_of_digits' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over str(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 290:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def reverse_number(n)
      Returns a value via 'return' keyword
    def reverse_number(n)
      Returns a value via 'return' keyword
    def reverse_number(n)
      Returns a value via 'return' keyword
    def reverse_number(n)
      Returns a value via 'return' keyword
    def reverse_number(n)
      Returns a value via 'return' keyword
    def reverse_number(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, return, while
  Operators used: %, *, +, -, ., <, >, (), //, []
  Control structures: for loop iteration; while loop
  Functions called: abs, input, int, print, reverse_number, reversed, str
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'reversed_num'
  • Binary operation: expression + expression
  • Binary operation: reversed_num * 10
  • Binary operation: n % 10
  • Assignment: right side evaluated first, then stored in 'reversed_num'
  • Binary operation: n // 10
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: int(value)
  • Function call: input('Enter a number: ')
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: print('Reversed number:', value)
  • Function call: reverse_number(num)
  • Assignment: right side evaluated first, then stored in 'sign'
  • Binary operation: sign * int(...)
  • Function call: int(value)
  • Function call: str(value)
  • Function call: abs(n)
  • Function call: print(value)
  • Function call: reverse_number(12345)
  • Assignment: right side evaluated first, then stored in 'sign'
  • Function call: abs(n)
  • Assignment: right side evaluated first, then stored in 'n'
  • Assignment: right side evaluated first, then stored in 'rev'
  • Function call: str(n)
  • Binary operation: expression + int(...)
  • Binary operation: rev * 10
  • Function call: int(d)
  • Assignment: right side evaluated first, then stored in 'rev'
  • Binary operation: rev * sign
  • Function call: print(value)
  • Function call: reverse_number(12345)
  • Assignment: right side evaluated first, then stored in 'sign'
  • Binary operation: sign * int(...)
  • Function call: int(value)
  • Function call: ''.join(value)
  • Function call: reversed(value)
  • Function call: str(value)
  • Function call: abs(n)
  • Function call: print(value)
  • Function call: reverse_number(12345)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: reverse_number(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_number(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_number' (stored in memory, not executed yet)
  2. Assign value to variable: num
  3. Execute function call: print('Reversed number:', reverse_number(...))
  4. Define function 'reverse_number' (stored in memory, not executed yet)
  5. Execute function call: print(reverse_number(...))
  6. Define function 'reverse_number' (stored in memory, not executed yet)
  7. Execute function call: print(reverse_number(...))
  8. Define function 'reverse_number' (stored in memory, not executed yet)
  9. Execute function call: print(reverse_number(...))
  10. Define function 'reverse_number' (stored in memory, not executed yet)
  11. Define function 'reverse_number' (stored in memory, not executed yet)
  12. Assign value to variable: n
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(reverse_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
    - 'reverse_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over str(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 291:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 7
    def factorial(number)
      Returns a value via 'return' keyword
    def factorial(number)
      Returns a value via 'return' keyword
    def factorial(number)
      Returns a value via 'return' keyword
    def factorial(number)
      Returns a value via 'return' keyword
    def factorial(number)
      Returns a value via 'return' keyword
    def factorial(number)
      Returns a value via 'return' keyword
    def factorial(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return, while
  Operators used: *, +, -, ., >, (), <=
  Control structures: for loop iteration; while loop; lambda expression
  Functions called: factorial, print, range, reduce
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number_to_calculate'
  • Function call: factorial(number_to_calculate)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'test_numbers'
  • Function call: print(value)
  • Function call: factorial(num)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: number + 1
  • Function call: range(2, computed expression)
  • Function call: print(value)
  • Function call: factorial(5)
  • Function call: math.factorial(number)
  • Function call: print(value)
  • Function call: factorial(5)
  • Condition branch: evaluates boolean expression first
  • Binary operation: number * factorial(...)
  • Binary operation: number - 1
  • Function call: factorial(computed expression)
  • Function call: print(value)
  • Function call: factorial(5)
  • Condition branch: evaluates boolean expression first
  • Binary operation: x * y
  • Binary operation: number + 1
  • Function call: reduce(value, value)
  • Function call: range(1, computed expression)
  • Function call: print(value)
  • Function call: factorial(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: factorial(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: factorial(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Assign value to variable: number_to_calculate
  3. Assign value to variable: result
  4. Execute function call: print(value)
  5. Assign value to variable: test_numbers
  6. For loop: iterate over test_numbers
  7. Define function 'factorial' (stored in memory, not executed yet)
  8. Execute function call: print(factorial(...))
  9. Import module: math
  10. Define function 'factorial' (stored in memory, not executed yet)
  11. Execute function call: print(factorial(...))
  12. Define function 'factorial' (stored in memory, not executed yet)
  13. Execute function call: print(factorial(...))
  14. Import from functools: reduce
  15. Define function 'factorial' (stored in memory, not executed yet)
  16. Execute function call: print(factorial(...))
  17. Define function 'factorial' (stored in memory, not executed yet)
  18. Define function 'factorial' (stored in memory, not executed yet)
  19. Assign value to variable: number
  20. Assign value to variable: result
  21. Execute function call: print(result)
  22. Execute function call: print(factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over test_numbers, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 293:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: %, *, +, <, !=, (), **, <=, ==
  Control structures: for loop iteration; while loop; generator expression
  Functions called: all, int, is_prime, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: i * i
  • Binary operation: number % i
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'test_numbers'
  • Function call: print('Prime number check:')
  • Function call: is_prime(num)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Condition branch: evaluates boolean expression first
  • Binary operation: number % i
  • Binary operation: int(...) + 1
  • Binary operation: number ** 0.5
  • Function call: all(value)
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Function call: print(value)
  • Function call: is_prime(num)
  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: number ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: number % i
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_prime(num)
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'i'
  • Binary operation: i * i
  • Binary operation: number % i
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: is_prime(num)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_prime(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_prime(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_prime' (stored in memory, not executed yet)
  2. Assign value to variable: test_numbers
  3. Execute function call: print('Prime number check:')
  4. For loop: iterate over test_numbers
  5. Define function 'is_prime' (stored in memory, not executed yet)
  6. For loop: iterate over expression
  7. Define function 'is_prime' (stored in memory, not executed yet)
  8. For loop: iterate over expression
  9. Define function 'is_prime' (stored in memory, not executed yet)
  10. For loop: iterate over expression
  11. Define function 'is_prime' (stored in memory, not executed yet)
  12. Define function 'is_prime' (stored in memory, not executed yet)
  13. Assign value to variable: number
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(is_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over test_numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 294:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def count_digits(number)
      Returns a value via 'return' keyword
    def count_digits(number)
      Returns a value via 'return' keyword
    def count_digits(number)
      Returns a value via 'return' keyword
    def count_digits(number)
      Returns a value via 'return' keyword
    def count_digits(number)
      Returns a value via 'return' keyword
    def count_digits(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: +, -, ., <, >, (), ==
  Control structures: for loop iteration; while loop
  Functions called: abs, count_digits, len, print, str
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'test_numbers'
  • Function call: print('Counting digits:')
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: count_digits(value)
  • Function call: print(value)
  • Function call: count_digits(num)
  • Function call: len(value)
  • Function call: str(value)
  • Function call: abs(number)
  • Function call: print(value)
  • Function call: count_digits(12345)
  • Function call: print(value)
  • Function call: count_digits(value)
  • Condition branch: evaluates boolean expression first
  • Binary operation: math.floor(...) + 1
  • Function call: math.floor(value)
  • Function call: math.log10(value)
  • Function call: abs(number)
  • Function call: print(value)
  • Function call: count_digits(12345)
  • Function call: print(value)
  • Function call: count_digits(value)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: str(value)
  • Function call: abs(number)
  • Function call: print(value)
  • Function call: count_digits(12345)
  • Function call: print(value)
  • Function call: count_digits(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: count_digits(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_digits(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_digits' (stored in memory, not executed yet)
  2. Assign value to variable: test_numbers
  3. Execute function call: print('Counting digits:')
  4. For loop: iterate over test_numbers
  5. Define function 'count_digits' (stored in memory, not executed yet)
  6. Execute function call: print(count_digits(...))
  7. Execute function call: print(count_digits(...))
  8. Import module: math
  9. Define function 'count_digits' (stored in memory, not executed yet)
  10. Execute function call: print(count_digits(...))
  11. Execute function call: print(count_digits(...))
  12. Define function 'count_digits' (stored in memory, not executed yet)
  13. Execute function call: print(count_digits(...))
  14. Execute function call: print(count_digits(...))
  15. Define function 'count_digits' (stored in memory, not executed yet)
  16. Define function 'count_digits' (stored in memory, not executed yet)
  17. Assign value to variable: number
  18. Assign value to variable: result
  19. Execute function call: print(result)
  20. Execute function call: print(count_digits(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_digits' is defined but NOT executed until called
    - 'count_digits' is defined but NOT executed until called
    - 'count_digits' is defined but NOT executed until called
    - 'count_digits' is defined but NOT executed until called
    - 'count_digits' is defined but NOT executed until called
    - 'count_digits' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over test_numbers, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over str(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 295:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: %, ., >, !=, (), ==, and
  Control structures: for loop iteration; while loop
  Functions called: abs, gcd, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a % b
  • Assignment: right side evaluated first, then stored in 'num1'
  • Assignment: right side evaluated first, then stored in 'num2'
  • Function call: gcd(num1, num2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'test_cases'
  • Function call: print('Testing additional cases:')
  • Function call: gcd(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(value)
  • Function call: abs(a)
  • Function call: abs(b)
  • Condition branch: evaluates boolean expression first
  • Binary operation: a % b
  • Assignment: right side evaluated first, then stored in 'a'
  • Binary operation: b % a
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: max(a, b)
  • Function call: print(value)
  • Function call: gcd(56, 98)
  • Function call: math.gcd(a, b)
  • Function call: print(value)
  • Function call: gcd(56, 98)
  • Function call: abs(a)
  • Function call: abs(b)
  • Condition branch: evaluates boolean expression first
  • Binary operation: a % b
  • Function call: gcd(b, computed expression)
  • Function call: print(value)
  • Function call: gcd(56, 98)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: gcd(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: gcd(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'gcd' (stored in memory, not executed yet)
  2. Assign value to variable: num1
  3. Assign value to variable: num2
  4. Assign value to variable: result
  5. Execute function call: print(value)
  6. Assign value to variable: test_cases
  7. Execute function call: print('Testing additional cases:')
  8. For loop: iterate over test_cases
  9. Define function 'gcd' (stored in memory, not executed yet)
  10. Execute function call: print(gcd(...))
  11. Import module: math
  12. Define function 'gcd' (stored in memory, not executed yet)
  13. Execute function call: print(gcd(...))
  14. Define function 'gcd' (stored in memory, not executed yet)
  15. Execute function call: print(gcd(...))
  16. Define function 'gcd' (stored in memory, not executed yet)
  17. Define function 'gcd' (stored in memory, not executed yet)
  18. Assign value to variable: a
  19. Assign value to variable: b
  20. Assign value to variable: result
  21. Execute function call: print(result)
  22. Execute function call: print(gcd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over test_cases, one element at a time
    - Body executes once per element, then loop exits
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 297:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def sum_elements(lst)
      Returns a value via 'return' keyword
    def sum_elements(lst)
      Returns a value via 'return' keyword
    def sum_elements(lst)
      Returns a value via 'return' keyword
    def sum_elements(lst)
      Returns a value via 'return' keyword
    def sum_elements(lst)
      Returns a value via 'return' keyword
    def sum_elements(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return, while
  Operators used: +, <, (), []
  Control structures: for loop iteration; while loop; lambda expression
  Functions called: len, print, reduce, sum, sum_elements
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'index'
  • Function call: len(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sum_elements(lst)
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: print(value)
  • Function call: sum_elements(value)
  • Function call: sum(lst)
  • Function call: print(value)
  • Function call: sum_elements(value)
  • Binary operation: acc + x
  • Function call: reduce(value, lst, 0)
  • Function call: print(value)
  • Function call: sum_elements(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sum_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sum_elements(...))
  4. Define function 'sum_elements' (stored in memory, not executed yet)
  5. Execute function call: print(sum_elements(...))
  6. Define function 'sum_elements' (stored in memory, not executed yet)
  7. Execute function call: print(sum_elements(...))
  8. Import from functools: reduce
  9. Define function 'sum_elements' (stored in memory, not executed yet)
  10. Execute function call: print(sum_elements(...))
  11. Define function 'sum_elements' (stored in memory, not executed yet)
  12. Define function 'sum_elements' (stored in memory, not executed yet)
  13. Assign value to variable: lst
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(sum_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_elements' is defined but NOT executed until called
    - 'sum_elements' is defined but NOT executed until called
    - 'sum_elements' is defined but NOT executed until called
    - 'sum_elements' is defined but NOT executed until called
    - 'sum_elements' is defined but NOT executed until called
    - 'sum_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 298:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 8
    def main(lst)
      Returns a value via 'return' keyword
    def find_largest_element(numbers)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def find_largest_element(numbers)
      Returns a value via 'return' keyword
    def find_largest_element(numbers)
      Returns a value via 'return' keyword
    def find_largest_element(numbers)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: -, <, >, (), []
  Control structures: for loop iteration; while loop
  Functions called: find_largest_element, len, main, max, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'i'
  • Function call: len(lst)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'i'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: len(numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'numbers_list'
  • Function call: find_largest_element(numbers_list)
  • Assignment: right side evaluated first, then stored in 'largest_number'
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'additional_numbers_list'
  • Function call: find_largest_element(additional_numbers_list)
  • Assignment: right side evaluated first, then stored in 'largest_number_in_negative'
  • Function call: print(value)
  • Function call: max(lst)
  • Function call: print(value)
  • Function call: main(value)
  • Function call: max(numbers)
  • Function call: print(value)
  • Function call: find_largest_element(value)
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(value)
  • Function call: find_largest_element(value)
  • Function call: sorted(numbers)
  • Function call: print(value)
  • Function call: find_largest_element(value)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: main(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: main(lst)
  4. Define function 'find_largest_element' (stored in memory, not executed yet)
  5. Assign value to variable: numbers_list
  6. Assign value to variable: largest_number
  7. Execute function call: print(value)
  8. Assign value to variable: additional_numbers_list
  9. Assign value to variable: largest_number_in_negative
  10. Execute function call: print(value)
  11. Define function 'main' (stored in memory, not executed yet)
  12. Execute function call: print(main(...))
  13. Define function 'find_largest_element' (stored in memory, not executed yet)
  14. Execute function call: print(find_largest_element(...))
  15. Define function 'find_largest_element' (stored in memory, not executed yet)
  16. Execute function call: print(find_largest_element(...))
  17. Define function 'find_largest_element' (stored in memory, not executed yet)
  18. Execute function call: print(find_largest_element(...))
  19. Define function 'main' (stored in memory, not executed yet)
  20. Define function 'main' (stored in memory, not executed yet)
  21. Assign value to variable: lst
  22. Assign value to variable: result
  23. Execute function call: print(result)
  24. Execute function call: print(main(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'find_largest_element' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'find_largest_element' is defined but NOT executed until called
    - 'find_largest_element' is defined but NOT executed until called
    - 'find_largest_element' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 299:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def smallest_element(lst)
      Returns a value via 'return' keyword
    def smallest_element(lst)
      Returns a value via 'return' keyword
    def smallest_element(lst)
      Returns a value via 'return' keyword
    def smallest_element(lst)
      Returns a value via 'return' keyword
    def smallest_element(lst)
      Returns a value via 'return' keyword
    def smallest_element(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return, while
  Operators used: <, (), [], not
  Control structures: for loop iteration; while loop
  Functions called: len, min, print, smallest_element, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest_word'
  • Assignment: right side evaluated first, then stored in 'i'
  • Function call: len(lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest_word'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: smallest_element(lst)
  • Condition branch: evaluates boolean expression first
  • Function call: min(lst)
  • Function call: print(value)
  • Function call: smallest_element(value)
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(lst)
  • Function call: print(value)
  • Function call: smallest_element(value)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Function call: print(value)
  • Function call: smallest_element(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: smallest_element(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: smallest_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'smallest_element' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(smallest_element(...))
  4. Define function 'smallest_element' (stored in memory, not executed yet)
  5. Execute function call: print(smallest_element(...))
  6. Define function 'smallest_element' (stored in memory, not executed yet)
  7. Execute function call: print(smallest_element(...))
  8. Define function 'smallest_element' (stored in memory, not executed yet)
  9. Execute function call: print(smallest_element(...))
  10. Define function 'smallest_element' (stored in memory, not executed yet)
  11. Define function 'smallest_element' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(smallest_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'smallest_element' is defined but NOT executed until called
    - 'smallest_element' is defined but NOT executed until called
    - 'smallest_element' is defined but NOT executed until called
    - 'smallest_element' is defined but NOT executed until called
    - 'smallest_element' is defined but NOT executed until called
    - 'smallest_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 304:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def ask_until_condition()
    def ask_until_condition()
    def ask_until_condition()
      Returns a value via 'return' keyword
    def ask_until_condition()
  Keywords used: break, def, if/elif/else, return, while
  Operators used: >, (), <=, not
  Control structures: while loop
  Functions called: ask_until_condition, input, int
  User input: uses input() for interactive data entry

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('Enter a number greater than 10: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: int(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: ask_until_condition()
  • Assignment: right side evaluated first, then stored in 'valid'
  • Function call: input('Enter a number greater than 10: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: int(user_input)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'valid'
  • Function call: ask_until_condition()
  • Function call: input('Enter a number greater than 10: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: int(user_input)
  • Condition branch: evaluates boolean expression first
  • Function call: ask_until_condition()
  • Function call: input('Enter a number greater than 10: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: int(user_input)
  • Function call: input('Enter a number greater than 10: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: ask_until_condition()
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: int(user_input)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Function call: int(user_input)
  • Condition branch: evaluates boolean expression first

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'ask_until_condition' (stored in memory, not executed yet)
  2. Execute function call: ask_until_condition()
  3. Define function 'ask_until_condition' (stored in memory, not executed yet)
  4. Execute function call: ask_until_condition()
  5. Define function 'ask_until_condition' (stored in memory, not executed yet)
  6. Execute function call: ask_until_condition()
  7. Define function 'ask_until_condition' (stored in memory, not executed yet)
  8. Execute function call: ask_until_condition()
  9. While loop: repeat while condition is true
  10. While loop: repeat while condition is true

▸ EXECUTION FLOW

  Function definition phase:
    - 'ask_until_condition' is defined but NOT executed until called
    - 'ask_until_condition' is defined but NOT executed until called
    - 'ask_until_condition' is defined but NOT executed until called
    - 'ask_until_condition' is defined but NOT executed until called
  Runtime execution (top-level code):
    - ask_until_condition() is invoked
      → Execution jumps to the function body of 'ask_until_condition'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - ask_until_condition() is invoked
      → Execution jumps to the function body of 'ask_until_condition'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - ask_until_condition() is invoked
      → Execution jumps to the function body of 'ask_until_condition'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - ask_until_condition() is invoked
      → Execution jumps to the function body of 'ask_until_condition'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 307:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_min_max()
    def find_min_max()
      Returns a value via 'return' keyword
    def find_min_max()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., <, >, (), []
  Control structures: for loop iteration; list comprehension
  Functions called: find_min_max, input, int, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(smallest, largest)
  • Function call: find_min_max()
  • Function call: int(value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: int(value)
  • Function call: input(...).split()
  • Function call: input('Enter numbers: ')
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(smallest, largest)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Assignment: right side evaluated first, then stored in 'largest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'smallest'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(smallest, largest)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min_max' (stored in memory, not executed yet)
  2. Execute function call: find_min_max()
  3. Define function 'find_min_max' (stored in memory, not executed yet)
  4. Define function 'find_min_max' (stored in memory, not executed yet)
  5. Assign value to variable: numbers
  6. Assign value to variable: smallest
  7. Assign value to variable: largest
  8. For loop: iterate over expression
  9. Execute function call: print(smallest, largest)
  10. Assign value to variable: numbers
  11. Assign value to variable: smallest
  12. Assign value to variable: largest
  13. For loop: iterate over expression
  14. Execute function call: print(smallest, largest)

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
    - 'find_min_max' is defined but NOT executed until called
  Runtime execution (top-level code):
    - find_min_max() is invoked
      → Execution jumps to the function body of 'find_min_max'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over expression, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 308:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def group_by_first_letter()
    def group_by_first_letter()
      Returns a value via 'return' keyword
    def group_by_first_letter()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], not in
  Control structures: for loop iteration
  Functions called: group_by_first_letter, input, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type word list: ')
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first_letter'
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(word)
  • Function call: print(result)
  • Function call: group_by_first_letter()
  • Function call: input(...).split()
  • Function call: input('type word list: ')
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first_letter'
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(word)
  • Function call: input(...).split()
  • Function call: input('type word list: ')
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first_letter'
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first_letter'
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(word)
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'first_letter'
  • Condition branch: evaluates boolean expression first
  • Function call: expression.append(word)
  • Function call: print(result)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  2. Execute function call: group_by_first_letter()
  3. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  4. Define function 'group_by_first_letter' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Assign value to variable: result
  7. For loop: iterate over lst
  8. Execute function call: print(result)
  9. Assign value to variable: lst
  10. Assign value to variable: result
  11. For loop: iterate over lst
  12. Execute function call: print(result)

▸ EXECUTION FLOW

  Function definition phase:
    - 'group_by_first_letter' is defined but NOT executed until called
    - 'group_by_first_letter' is defined but NOT executed until called
    - 'group_by_first_letter' is defined but NOT executed until called
  Runtime execution (top-level code):
    - group_by_first_letter() is invoked
      → Execution jumps to the function body of 'group_by_first_letter'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 310:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def find_longest_word(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def find_longest_word(lst)
      Returns a value via 'return' keyword
    def find_longest_word(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, ()
  Control structures: if/else conditional branching; for loop iteration
  Functions called: find_longest_word, len, main, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_longest_word(lst)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_longest_word(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_longest_word(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_longest_word' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_longest_word(...))
  4. Define function 'main' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: main(lst)
  7. Define function 'find_longest_word' (stored in memory, not executed yet)
  8. Define function 'find_longest_word' (stored in memory, not executed yet)
  9. Assign value to variable: lst
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_longest_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_longest_word' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 311:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_longest_word(lst)
      Returns a value via 'return' keyword
    def find_longest_word(lst)
      Returns a value via 'return' keyword
    def find_longest_word(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, ()
  Control structures: if/else conditional branching; for loop iteration
  Functions called: find_longest_word, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Assignment: right side evaluated first, then stored in 'max_length'
  • Assignment: right side evaluated first, then stored in 'current_length'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_length'
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_longest_word(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_longest_word(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_longest_word(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_longest_word' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_longest_word(...))
  4. Define function 'find_longest_word' (stored in memory, not executed yet)
  5. Define function 'find_longest_word' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(find_longest_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
    - 'find_longest_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 312:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def second_largest_number()
    def second_largest_number()
      Returns a value via 'return' keyword
    def second_largest_number()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: input, list, map, print, second_largest_number
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.sort()
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)
  • Function call: second_largest_number()
  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.sort()
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.sort()
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.sort()
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.sort()
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'second_largest_number' (stored in memory, not executed yet)
  2. Execute function call: second_largest_number()
  3. Define function 'second_largest_number' (stored in memory, not executed yet)
  4. Define function 'second_largest_number' (stored in memory, not executed yet)
  5. Assign value to variable: user
  6. Assign value to variable: user_int
  7. Execute function call: user_int.sort()
  8. Assign value to variable: second_largest
  9. Execute function call: print(second_largest)
  10. Assign value to variable: user
  11. Assign value to variable: user_int
  12. Execute function call: user_int.sort()
  13. Assign value to variable: second_largest
  14. Execute function call: print(second_largest)

▸ EXECUTION FLOW

  Function definition phase:
    - 'second_largest_number' is defined but NOT executed until called
    - 'second_largest_number' is defined but NOT executed until called
    - 'second_largest_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - second_largest_number() is invoked
      → Execution jumps to the function body of 'second_largest_number'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - user_int.sort() is invoked
    - print() is invoked
    - user_int.sort() is invoked
    - print() is invoked
"""

"""
Problem 313:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def convert_to_uppercase(strings)
      Returns a value via 'return' keyword
    def convert_to_uppercase(strings)
      Returns a value via 'return' keyword
    def convert_to_uppercase(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), <=
  Control structures: generator expression
  Functions called: chr, convert_to_uppercase, list, map, ord, print, str
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.upper()
  • Function call: list(value)
  • Function call: map(to_uppercase, strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: convert_to_uppercase(strings)
  • Assignment: right side evaluated first, then stored in 'uppercase_strings'
  • Function call: print('Original list:', strings)
  • Function call: print('Uppercase list:', uppercase_strings)
  • Binary operation: ord(...) - 32
  • Function call: ''.join(value)
  • Function call: chr(computed expression)
  • Function call: ord(c)
  • Function call: str(...).upper()
  • Function call: str(s)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: convert_to_uppercase(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: convert_to_uppercase('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'convert_to_uppercase' (stored in memory, not executed yet)
  2. Assign value to variable: strings
  3. Assign value to variable: uppercase_strings
  4. Execute function call: print('Original list:', strings)
  5. Execute function call: print('Uppercase list:', uppercase_strings)
  6. Define function 'convert_to_uppercase' (stored in memory, not executed yet)
  7. Define function 'convert_to_uppercase' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(convert_to_uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'convert_to_uppercase' is defined but NOT executed until called
    - 'convert_to_uppercase' is defined but NOT executed until called
    - 'convert_to_uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 314:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def convert_to_integers(strings)
      Returns a value via 'return' keyword
    def convert_to_integers(strings)
      Returns a value via 'return' keyword
    def convert_to_integers(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: convert_to_integers, int, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: int(s)
  • Function call: list(value)
  • Function call: map(to_integer, strings)
  • Assignment: right side evaluated first, then stored in 'string_numbers'
  • Function call: convert_to_integers(string_numbers)
  • Assignment: right side evaluated first, then stored in 'integer_numbers'
  • Function call: print('Original list of strings:', string_numbers)
  • Function call: print('Converted list of integers:', integer_numbers)
  • Function call: int(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: int(s)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: convert_to_integers(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: convert_to_integers('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'convert_to_integers' (stored in memory, not executed yet)
  2. Assign value to variable: string_numbers
  3. Assign value to variable: integer_numbers
  4. Execute function call: print('Original list of strings:', string_numbers)
  5. Execute function call: print('Converted list of integers:', integer_numbers)
  6. Define function 'convert_to_integers' (stored in memory, not executed yet)
  7. Define function 'convert_to_integers' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(convert_to_integers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'convert_to_integers' is defined but NOT executed until called
    - 'convert_to_integers' is defined but NOT executed until called
    - 'convert_to_integers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 315:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def square_numbers(numbers)
      Returns a value via 'return' keyword
    def square_numbers(numbers)
      Returns a value via 'return' keyword
    def square_numbers(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: list, map, pow, print, range, square_numbers
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * x
  • Function call: list(value)
  • Function call: map(square, numbers)
  • Assignment: right side evaluated first, then stored in 'original_numbers'
  • Function call: square_numbers(original_numbers)
  • Assignment: right side evaluated first, then stored in 'squared_numbers'
  • Function call: print('Original list of numbers:', original_numbers)
  • Function call: print('Squared list of numbers:', squared_numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(x)
  • Function call: pow(x, x)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: square_numbers(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: square_numbers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'square_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: original_numbers
  3. Assign value to variable: squared_numbers
  4. Execute function call: print('Original list of numbers:', original_numbers)
  5. Execute function call: print('Squared list of numbers:', squared_numbers)
  6. Define function 'square_numbers' (stored in memory, not executed yet)
  7. Define function 'square_numbers' (stored in memory, not executed yet)
  8. Assign value to variable: numbers
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(square_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'square_numbers' is defined but NOT executed until called
    - 'square_numbers' is defined but NOT executed until called
    - 'square_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 316:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def string_lengths(strings)
      Returns a value via 'return' keyword
    def string_lengths(strings)
      Returns a value via 'return' keyword
    def string_lengths(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration; generator expression
  Functions called: len, list, map, print, string_lengths, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(s)
  • Function call: list(value)
  • Function call: map(length, strings)
  • Assignment: right side evaluated first, then stored in 'string_list'
  • Function call: string_lengths(string_list)
  • Assignment: right side evaluated first, then stored in 'lengths'
  • Function call: print('Original list of strings:', string_list)
  • Function call: print('Lengths of each string:', lengths)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: sum(value)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: string_lengths(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: string_lengths('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'string_lengths' (stored in memory, not executed yet)
  2. Assign value to variable: string_list
  3. Assign value to variable: lengths
  4. Execute function call: print('Original list of strings:', string_list)
  5. Execute function call: print('Lengths of each string:', lengths)
  6. Define function 'string_lengths' (stored in memory, not executed yet)
  7. Define function 'string_lengths' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(string_lengths(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'string_lengths' is defined but NOT executed until called
    - 'string_lengths' is defined but NOT executed until called
    - 'string_lengths' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 317:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def fahrenheit_to_celsius(temperatures)
      Returns a value via 'return' keyword
    def fahrenheit_to_celsius(temperatures)
      Returns a value via 'return' keyword
    def fahrenheit_to_celsius(temperatures)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, -, /, ()
  Functions called: fahrenheit_to_celsius, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression / 9
  • Binary operation: expression * 5
  • Binary operation: f - 32
  • Function call: list(value)
  • Function call: map(to_celsius, temperatures)
  • Assignment: right side evaluated first, then stored in 'fahrenheit_temps'
  • Function call: fahrenheit_to_celsius(fahrenheit_temps)
  • Assignment: right side evaluated first, then stored in 'celsius_temps'
  • Function call: print('Temperatures in Fahrenheit:', fahrenheit_temps)
  • Function call: print('Temperatures in Celsius:', celsius_temps)
  • Binary operation: expression / 9
  • Binary operation: expression * 5
  • Binary operation: f - 32
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression / 9
  • Binary operation: expression * 5
  • Binary operation: f - 32
  • Assignment: right side evaluated first, then stored in 'temperatures'
  • Function call: fahrenheit_to_celsius(temperatures)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fahrenheit_to_celsius(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fahrenheit_to_celsius' (stored in memory, not executed yet)
  2. Assign value to variable: fahrenheit_temps
  3. Assign value to variable: celsius_temps
  4. Execute function call: print('Temperatures in Fahrenheit:', fahrenheit_temps)
  5. Execute function call: print('Temperatures in Celsius:', celsius_temps)
  6. Define function 'fahrenheit_to_celsius' (stored in memory, not executed yet)
  7. Define function 'fahrenheit_to_celsius' (stored in memory, not executed yet)
  8. Assign value to variable: temperatures
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(fahrenheit_to_celsius(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fahrenheit_to_celsius' is defined but NOT executed until called
    - 'fahrenheit_to_celsius' is defined but NOT executed until called
    - 'fahrenheit_to_celsius' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 319:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def add_five_to_number(x)
      Returns a value via 'return' keyword
    def add_five(lst)
      Returns a value via 'return' keyword
    def add_five_to_number(x)
      Returns a value via 'return' keyword
    def add_five_to_number(x)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., ()
  Functions called: add_five, add_five_to_number, list, map, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x + 5
  • Function call: list(value)
  • Function call: map(add_five_to_number, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: add_five(lst)
  • Function call: sum(value)
  • Function call: operator.add(x, 5)
  • Assignment: right side evaluated first, then stored in 'x'
  • Function call: add_five_to_number(x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: add_five_to_number(3)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'add_five_to_number' (stored in memory, not executed yet)
  2. Define function 'add_five' (stored in memory, not executed yet)
  3. Assign value to variable: lst
  4. Execute function call: print(add_five(...))
  5. Define function 'add_five_to_number' (stored in memory, not executed yet)
  6. Define function 'add_five_to_number' (stored in memory, not executed yet)
  7. Assign value to variable: x
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(add_five_to_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'add_five_to_number' is defined but NOT executed until called
    - 'add_five' is defined but NOT executed until called
    - 'add_five_to_number' is defined but NOT executed until called
    - 'add_five_to_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 320:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def main(lst)
      Returns a value via 'return' keyword
    def square_roots(numbers)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), **
  Functions called: list, main, map, pow, print, square_roots
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: num ** 2
  • Function call: list(value)
  • Function call: map(sqaure_root, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Function call: math.sqrt(x)
  • Function call: list(value)
  • Function call: map(square_root, numbers)
  • Assignment: right side evaluated first, then stored in 'number_list'
  • Function call: square_roots(number_list)
  • Assignment: right side evaluated first, then stored in 'roots'
  • Function call: print('Original numbers:', number_list)
  • Function call: print('Square roots:', roots)
  • Function call: pow(num, 2)
  • Function call: math.pow(num, 2)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: main(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: main(lst)
  4. Import module: math
  5. Define function 'square_roots' (stored in memory, not executed yet)
  6. Assign value to variable: number_list
  7. Assign value to variable: roots
  8. Execute function call: print('Original numbers:', number_list)
  9. Execute function call: print('Square roots:', roots)
  10. Define function 'main' (stored in memory, not executed yet)
  11. Define function 'main' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(main(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'square_roots' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 321:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def lengths_of_strings(strings)
      Returns a value via 'return' keyword
    def lengths_of_strings(strings)
      Returns a value via 'return' keyword
    def lengths_of_strings(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration; generator expression
  Functions called: len, lengths_of_strings, list, map, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(s)
  • Function call: list(value)
  • Function call: map(length, strings)
  • Assignment: right side evaluated first, then stored in 'string_list'
  • Function call: lengths_of_strings(string_list)
  • Assignment: right side evaluated first, then stored in 'lengths'
  • Function call: print('Original strings:', string_list)
  • Function call: print('Lengths of strings:', lengths)
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: sum(value)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: lengths_of_strings(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: lengths_of_strings('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'lengths_of_strings' (stored in memory, not executed yet)
  2. Assign value to variable: string_list
  3. Assign value to variable: lengths
  4. Execute function call: print('Original strings:', string_list)
  5. Execute function call: print('Lengths of strings:', lengths)
  6. Define function 'lengths_of_strings' (stored in memory, not executed yet)
  7. Define function 'lengths_of_strings' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(lengths_of_strings(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'lengths_of_strings' is defined but NOT executed until called
    - 'lengths_of_strings' is defined but NOT executed until called
    - 'lengths_of_strings' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 322:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def normalize_scores(scores)
      Returns a value via 'return' keyword
    def normalize_scores(scores)
      Returns a value via 'return' keyword
    def normalize_scores(scores)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, /, ()
  Functions called: list, map, max, min, normalize_scores, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(scores)
  • Assignment: right side evaluated first, then stored in 'min_score'
  • Function call: max(scores)
  • Assignment: right side evaluated first, then stored in 'max_score'
  • Binary operation: expression / expression
  • Binary operation: score - min_score
  • Binary operation: max_score - min_score
  • Function call: list(value)
  • Function call: map(normalize, scores)
  • Assignment: right side evaluated first, then stored in 'score_list'
  • Function call: normalize_scores(score_list)
  • Assignment: right side evaluated first, then stored in 'normalized_scores'
  • Function call: print('Original scores:', score_list)
  • Function call: print('Normalized scores:', normalized_scores)
  • Binary operation: expression / expression
  • Binary operation: score - min_score
  • Binary operation: max_score - min_score
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression / expression
  • Binary operation: score - min_score
  • Binary operation: max_score - min_score
  • Assignment: right side evaluated first, then stored in 'scores'
  • Function call: normalize_scores(scores)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: normalize_scores(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'normalize_scores' (stored in memory, not executed yet)
  2. Assign value to variable: score_list
  3. Assign value to variable: normalized_scores
  4. Execute function call: print('Original scores:', score_list)
  5. Execute function call: print('Normalized scores:', normalized_scores)
  6. Define function 'normalize_scores' (stored in memory, not executed yet)
  7. Define function 'normalize_scores' (stored in memory, not executed yet)
  8. Assign value to variable: scores
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(normalize_scores(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'normalize_scores' is defined but NOT executed until called
    - 'normalize_scores' is defined but NOT executed until called
    - 'normalize_scores' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 323:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def reverse_strings(strings)
      Returns a value via 'return' keyword
    def reverse_strings(strings)
      Returns a value via 'return' keyword
    def reverse_strings(strings)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, -, ., (), []
  Control structures: for loop iteration
  Functions called: list, map, print, reverse_strings, reversed
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: map(reverse, strings)
  • Assignment: right side evaluated first, then stored in 'string_list'
  • Function call: reverse_strings(string_list)
  • Assignment: right side evaluated first, then stored in 'reversed_strings'
  • Function call: print('Original strings:', string_list)
  • Function call: print('Reversed strings:', reversed_strings)
  • Function call: ''.join(value)
  • Function call: reversed(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: reverse_strings(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_strings('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_strings' (stored in memory, not executed yet)
  2. Assign value to variable: string_list
  3. Assign value to variable: reversed_strings
  4. Execute function call: print('Original strings:', string_list)
  5. Execute function call: print('Reversed strings:', reversed_strings)
  6. Define function 'reverse_strings' (stored in memory, not executed yet)
  7. Define function 'reverse_strings' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(reverse_strings(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_strings' is defined but NOT executed until called
    - 'reverse_strings' is defined but NOT executed until called
    - 'reverse_strings' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 324:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def double_numbers(numbers)
      Returns a value via 'return' keyword
    def double_numbers(numbers)
      Returns a value via 'return' keyword
    def double_numbers(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: double_numbers, list, map, pow, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * 2
  • Function call: list(value)
  • Function call: map(double, numbers)
  • Assignment: right side evaluated first, then stored in 'number_list'
  • Function call: double_numbers(number_list)
  • Assignment: right side evaluated first, then stored in 'doubled_numbers'
  • Function call: print('Original numbers:', number_list)
  • Function call: print('Doubled numbers:', doubled_numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(2)
  • Function call: pow(x, 2)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: double_numbers(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: double_numbers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'double_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: number_list
  3. Assign value to variable: doubled_numbers
  4. Execute function call: print('Original numbers:', number_list)
  5. Execute function call: print('Doubled numbers:', doubled_numbers)
  6. Define function 'double_numbers' (stored in memory, not executed yet)
  7. Define function 'double_numbers' (stored in memory, not executed yet)
  8. Assign value to variable: numbers
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(double_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'double_numbers' is defined but NOT executed until called
    - 'double_numbers' is defined but NOT executed until called
    - 'double_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 325:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def strip_whitespace(strings)
      Returns a value via 'return' keyword
    def strip_whitespace(strings)
      Returns a value via 'return' keyword
    def strip_whitespace(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: list, map, print, strip_whitespace
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.strip()
  • Function call: list(value)
  • Function call: map(strip, strings)
  • Assignment: right side evaluated first, then stored in 'string_list'
  • Function call: strip_whitespace(string_list)
  • Assignment: right side evaluated first, then stored in 'stripped_strings'
  • Function call: print('Original strings:', string_list)
  • Function call: print('Stripped strings:', stripped_strings)
  • Function call: s.strip()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: s.strip()
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: strip_whitespace(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: strip_whitespace('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'strip_whitespace' (stored in memory, not executed yet)
  2. Assign value to variable: string_list
  3. Assign value to variable: stripped_strings
  4. Execute function call: print('Original strings:', string_list)
  5. Execute function call: print('Stripped strings:', stripped_strings)
  6. Define function 'strip_whitespace' (stored in memory, not executed yet)
  7. Define function 'strip_whitespace' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(strip_whitespace(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'strip_whitespace' is defined but NOT executed until called
    - 'strip_whitespace' is defined but NOT executed until called
    - 'strip_whitespace' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 326:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def number_and_square(numbers)
      Returns a value via 'return' keyword
    def number_and_square(numbers)
      Returns a value via 'return' keyword
    def number_and_square(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ()
  Functions called: list, map, number_and_square, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * x
  • Function call: list(value)
  • Function call: map(create_tuple, numbers)
  • Assignment: right side evaluated first, then stored in 'numbers_list'
  • Function call: number_and_square(numbers_list)
  • Assignment: right side evaluated first, then stored in 'squared_tuples'
  • Function call: print('Numbers and their squares:', squared_tuples)
  • Binary operation: x * x
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x * x
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: number_and_square(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: number_and_square(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'number_and_square' (stored in memory, not executed yet)
  2. Assign value to variable: numbers_list
  3. Assign value to variable: squared_tuples
  4. Execute function call: print('Numbers and their squares:', squared_tuples)
  5. Define function 'number_and_square' (stored in memory, not executed yet)
  6. Define function 'number_and_square' (stored in memory, not executed yet)
  7. Assign value to variable: numbers
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(number_and_square(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'number_and_square' is defined but NOT executed until called
    - 'number_and_square' is defined but NOT executed until called
    - 'number_and_square' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 327:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def to_title_case(strings)
      Returns a value via 'return' keyword
    def to_title_case(strings)
      Returns a value via 'return' keyword
    def to_title_case(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: list, map, print, str, to_title_case
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.title()
  • Function call: list(value)
  • Function call: map(title_case, strings)
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: to_title_case(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: s.title()
  • Function call: str(...).title()
  • Function call: str(s)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: to_title_case(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: to_title_case('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'to_title_case' (stored in memory, not executed yet)
  2. Assign value to variable: words
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'to_title_case' (stored in memory, not executed yet)
  6. Define function 'to_title_case' (stored in memory, not executed yet)
  7. Assign value to variable: strings
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(to_title_case(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'to_title_case' is defined but NOT executed until called
    - 'to_title_case' is defined but NOT executed until called
    - 'to_title_case' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 328:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def second_largest_number()
    def second_largest_number()
      Returns a value via 'return' keyword
    def second_largest_number()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., >, (), []
  Control structures: for loop iteration
  Functions called: input, int, len, list, print, second_largest_number, set
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(num)
  • Function call: set(user_int)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: list(unique_numbers)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: unique_numbers.sort()
  • Function call: len(unique_numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)
  • Function call: second_largest_number()
  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(num)
  • Function call: set(user_int)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: list(unique_numbers)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: unique_numbers.sort()
  • Function call: len(unique_numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: input(...).split()
  • Function call: input('Type numbers separated by spaces: ')
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(num)
  • Function call: set(user_int)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: list(unique_numbers)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: unique_numbers.sort()
  • Function call: len(unique_numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(num)
  • Function call: set(user_int)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: list(unique_numbers)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: unique_numbers.sort()
  • Function call: len(unique_numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)
  • Assignment: right side evaluated first, then stored in 'user_input'
  • Assignment: right side evaluated first, then stored in 'user_int'
  • Function call: user_int.append(value)
  • Function call: int(num)
  • Function call: set(user_int)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: list(unique_numbers)
  • Assignment: right side evaluated first, then stored in 'unique_numbers'
  • Function call: unique_numbers.sort()
  • Function call: len(unique_numbers)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Assignment: right side evaluated first, then stored in 'second_largest'
  • Function call: print(second_largest)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'second_largest_number' (stored in memory, not executed yet)
  2. Execute function call: second_largest_number()
  3. Define function 'second_largest_number' (stored in memory, not executed yet)
  4. Define function 'second_largest_number' (stored in memory, not executed yet)
  5. Assign value to variable: user_input
  6. Assign value to variable: user_int
  7. For loop: iterate over user_input
  8. Assign value to variable: unique_numbers
  9. Assign value to variable: unique_numbers
  10. Execute function call: unique_numbers.sort()
  11. If/else: conditional execution
  12. Execute function call: print(second_largest)
  13. Assign value to variable: user_input
  14. Assign value to variable: user_int
  15. For loop: iterate over user_input
  16. Assign value to variable: unique_numbers
  17. Assign value to variable: unique_numbers
  18. Execute function call: unique_numbers.sort()
  19. If/else: conditional execution
  20. Execute function call: print(second_largest)

▸ EXECUTION FLOW

  Function definition phase:
    - 'second_largest_number' is defined but NOT executed until called
    - 'second_largest_number' is defined but NOT executed until called
    - 'second_largest_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - second_largest_number() is invoked
      → Execution jumps to the function body of 'second_largest_number'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - unique_numbers.sort() is invoked
    - print() is invoked
    - unique_numbers.sort() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over user_input, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_input, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_input, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_input, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user_input, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 329:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def list_string(numbers)
      Returns a value via 'return' keyword
    def type_smallest_biggest()
    def list_string(numbers)
      Returns a value via 'return' keyword
    def list_string(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: input, list_string, print, sorted, type_smallest_biggest
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: list_string(lst)
  • Function call: input(...).split()
  • Function call: input('type number list: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: sorted(user)
  • Assignment: right side evaluated first, then stored in 'sort_number'
  • Function call: print(sort_number)
  • Function call: type_smallest_biggest()
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: list_string(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_string' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(list_string(...))
  4. Define function 'type_smallest_biggest' (stored in memory, not executed yet)
  5. Execute function call: type_smallest_biggest()
  6. Define function 'list_string' (stored in memory, not executed yet)
  7. Define function 'list_string' (stored in memory, not executed yet)
  8. Assign value to variable: numbers
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(list_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_string' is defined but NOT executed until called
    - 'type_smallest_biggest' is defined but NOT executed until called
    - 'list_string' is defined but NOT executed until called
    - 'list_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - type_smallest_biggest() is invoked
      → Execution jumps to the function body of 'type_smallest_biggest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 330:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def list_of_string_numbers(n)
      Returns a value via 'return' keyword
    def type_biggest_smallest()
    def list_of_string_numbers(n)
      Returns a value via 'return' keyword
    def list_of_string_numbers(n)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: input, list, list_of_string_numbers, map, print, sorted, type_biggest_smallest
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(n)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: list_of_string_numbers(numbers)
  • Function call: input(...).split()
  • Function call: input('Type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: list(value)
  • Function call: map(int, user)
  • Assignment: right side evaluated first, then stored in 'int_list'
  • Function call: sorted(int_list)
  • Assignment: right side evaluated first, then stored in 'sorted_list'
  • Function call: print(sorted_list)
  • Function call: type_biggest_smallest()
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: list_of_string_numbers(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_of_string_numbers(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_of_string_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(list_of_string_numbers(...))
  4. Define function 'type_biggest_smallest' (stored in memory, not executed yet)
  5. Execute function call: type_biggest_smallest()
  6. Define function 'list_of_string_numbers' (stored in memory, not executed yet)
  7. Define function 'list_of_string_numbers' (stored in memory, not executed yet)
  8. Assign value to variable: n
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(list_of_string_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_of_string_numbers' is defined but NOT executed until called
    - 'type_biggest_smallest' is defined but NOT executed until called
    - 'list_of_string_numbers' is defined but NOT executed until called
    - 'list_of_string_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - type_biggest_smallest() is invoked
      → Execution jumps to the function body of 'type_biggest_smallest'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 331:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def print_index_and_element(lst)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: enumerate, print, print_index_and_element
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element(lst)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element(lst)
  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_index_and_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_index_and_element' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print_index_and_element(lst)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(print_index_and_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_index_and_element' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_index_and_element() is invoked
      → Execution jumps to the function body of 'print_index_and_element'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 332:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def create_index_element_tuples(lst)
      Returns a value via 'return' keyword
    def create_index_element_tuples(lst)
      Returns a value via 'return' keyword
    def create_index_element_tuples(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: create_index_element_tuples, enumerate, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: create_index_element_tuples(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: create_index_element_tuples(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: create_index_element_tuples(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'create_index_element_tuples' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'create_index_element_tuples' (stored in memory, not executed yet)
  6. Define function 'create_index_element_tuples' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(create_index_element_tuples(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'create_index_element_tuples' is defined but NOT executed until called
    - 'create_index_element_tuples' is defined but NOT executed until called
    - 'create_index_element_tuples' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 333:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def print_with_index(strings)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: enumerate, print, print_with_index
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(strings)
  • Function call: print(index, value)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: print_with_index(strings)
  • Function call: enumerate(strings)
  • Function call: print(index, value)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: print_with_index(strings)
  • Function call: enumerate(strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: print_with_index(strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: print_with_index(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_with_index('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_with_index' (stored in memory, not executed yet)
  2. Assign value to variable: strings
  3. Execute function call: print_with_index(strings)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. Assign value to variable: strings
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(print_with_index(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_with_index' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_with_index() is invoked
      → Execution jumps to the function body of 'print_with_index'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 334:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def list_to_dict(lst)
      Returns a value via 'return' keyword
    def list_to_dict(lst)
      Returns a value via 'return' keyword
    def list_to_dict(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration
  Functions called: enumerate, list_to_dict, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_to_dict(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_to_dict(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_to_dict(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_to_dict' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'list_to_dict' (stored in memory, not executed yet)
  6. Define function 'list_to_dict' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(list_to_dict(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_to_dict' is defined but NOT executed until called
    - 'list_to_dict' is defined but NOT executed until called
    - 'list_to_dict' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 335:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_index(lst, element)
      Returns a value via 'return' keyword
    def find_index(lst, element)
      Returns a value via 'return' keyword
    def find_index(lst, element)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, (), ==
  Control structures: if/else conditional branching; for loop iteration
  Functions called: enumerate, find_index, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_index(lst, 'banana')
  • Assignment: right side evaluated first, then stored in 'index'
  • Function call: print(index)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'element'
  • Function call: find_index(lst, element)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_index(value, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_index' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: index
  4. Execute function call: print(index)
  5. Define function 'find_index' (stored in memory, not executed yet)
  6. Define function 'find_index' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: element
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(find_index(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_index' is defined but NOT executed until called
    - 'find_index' is defined but NOT executed until called
    - 'find_index' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 336:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def print_index_and_element_with_colon(lst)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: enumerate, print, print_index_and_element_with_colon
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element_with_colon(lst)
  • Function call: enumerate(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element_with_colon(lst)
  • Function call: enumerate(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element_with_colon(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_and_element_with_colon(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_index_and_element_with_colon(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_index_and_element_with_colon' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print_index_and_element_with_colon(lst)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(print_index_and_element_with_colon(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_index_and_element_with_colon' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_index_and_element_with_colon() is invoked
      → Execution jumps to the function body of 'print_index_and_element_with_colon'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 337:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def create_indexed_strings(lst, prefix)
      Returns a value via 'return' keyword
    def create_indexed_strings(lst, prefix)
      Returns a value via 'return' keyword
    def create_indexed_strings(lst, prefix)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: create_indexed_strings, enumerate, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: create_indexed_strings(lst, 'Item ')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'prefix'
  • Function call: create_indexed_strings(lst, prefix)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: create_indexed_strings(value, 'Hi, ')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'create_indexed_strings' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'create_indexed_strings' (stored in memory, not executed yet)
  6. Define function 'create_indexed_strings' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: prefix
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(create_indexed_strings(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'create_indexed_strings' is defined but NOT executed until called
    - 'create_indexed_strings' is defined but NOT executed until called
    - 'create_indexed_strings' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 338:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def modify_elements(lst)
      Returns a value via 'return' keyword
    def modify_elements(lst)
      Returns a value via 'return' keyword
    def modify_elements(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, ., (), ==, []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: enumerate, modify_elements, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Function call: element.upper()
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: modify_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: modify_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: modify_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'modify_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'modify_elements' (stored in memory, not executed yet)
  6. Define function 'modify_elements' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(modify_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'modify_elements' is defined but NOT executed until called
    - 'modify_elements' is defined but NOT executed until called
    - 'modify_elements' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 340:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def print_odd_index_elements(lst)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, !=, ()
  Control structures: for loop iteration
  Functions called: enumerate, print, print_odd_index_elements
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print(element)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_odd_index_elements(lst)
  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Function call: print(element)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_odd_index_elements(lst)
  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_odd_index_elements(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_odd_index_elements(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_odd_index_elements(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_odd_index_elements' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print_odd_index_elements(lst)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(print_odd_index_elements(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_odd_index_elements' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_odd_index_elements() is invoked
      → Execution jumps to the function body of 'print_odd_index_elements'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 341:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def index_element_if_vowel(lst)
      Returns a value via 'return' keyword
    def index_element_if_vowel(lst)
      Returns a value via 'return' keyword
    def index_element_if_vowel(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), [], in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: enumerate, index_element_if_vowel, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'vowels'
  • Function call: enumerate(lst)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: index_element_if_vowel(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: index_element_if_vowel(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: index_element_if_vowel(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'index_element_if_vowel' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'index_element_if_vowel' (stored in memory, not executed yet)
  6. Define function 'index_element_if_vowel' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(index_element_if_vowel(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'index_element_if_vowel' is defined but NOT executed until called
    - 'index_element_if_vowel' is defined but NOT executed until called
    - 'index_element_if_vowel' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 342:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_max_and_index(lst)
      Returns a value via 'return' keyword
    def find_max_and_index(lst)
      Returns a value via 'return' keyword
    def find_max_and_index(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: enumerate, find_max_and_index, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'max_element'
  • Assignment: right side evaluated first, then stored in 'max_index'
  • Function call: enumerate(lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_element'
  • Assignment: right side evaluated first, then stored in 'max_index'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_max_and_index(lst)
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: find_max_and_index(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_max_and_index(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_max_and_index' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: target
  4. Execute function call: print(value)
  5. Define function 'find_max_and_index' (stored in memory, not executed yet)
  6. Define function 'find_max_and_index' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(find_max_and_index(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_max_and_index' is defined but NOT executed until called
    - 'find_max_and_index' is defined but NOT executed until called
    - 'find_max_and_index' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 343:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def index_and_element_excluding_first_n(lst, n)
      Returns a value via 'return' keyword
    def index_and_element_excluding_first_n(lst, n)
      Returns a value via 'return' keyword
    def index_and_element_excluding_first_n(lst, n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), >=
  Control structures: if/else conditional branching; for loop iteration
  Functions called: enumerate, index_and_element_excluding_first_n, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: enumerate(lst)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: index_and_element_excluding_first_n(lst, 2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: index_and_element_excluding_first_n(lst, n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: index_and_element_excluding_first_n(value, 15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'index_and_element_excluding_first_n' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'index_and_element_excluding_first_n' (stored in memory, not executed yet)
  6. Define function 'index_and_element_excluding_first_n' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: n
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(index_and_element_excluding_first_n(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'index_and_element_excluding_first_n' is defined but NOT executed until called
    - 'index_and_element_excluding_first_n' is defined but NOT executed until called
    - 'index_and_element_excluding_first_n' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 344:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def print_index_squared(numbers)
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), **
  Control structures: for loop iteration
  Functions called: enumerate, print, print_index_squared
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(numbers)
  • Binary operation: number ** 2
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_squared(lst)
  • Function call: enumerate(numbers)
  • Binary operation: number ** 2
  • Function call: print(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_squared(lst)
  • Function call: enumerate(numbers)
  • Binary operation: number ** 2
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print_index_squared(lst)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print_index_squared(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: print_index_squared(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'print_index_squared' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print_index_squared(lst)
  4. Define function 'solve' (stored in memory, not executed yet)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(print_index_squared(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'print_index_squared' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print_index_squared() is invoked
      → Execution jumps to the function body of 'print_index_squared'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 345:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def replace_based_on_index(lst)
      Returns a value via 'return' keyword
    def replace_based_on_index(lst)
      Returns a value via 'return' keyword
    def replace_based_on_index(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, (), ==, []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: enumerate, print, replace_based_on_index
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: enumerate(lst)
  • Binary operation: index % 2
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: replace_based_on_index(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: replace_based_on_index(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: replace_based_on_index(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'replace_based_on_index' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'replace_based_on_index' (stored in memory, not executed yet)
  6. Define function 'replace_based_on_index' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(replace_based_on_index(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'replace_based_on_index' is defined but NOT executed until called
    - 'replace_based_on_index' is defined but NOT executed until called
    - 'replace_based_on_index' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over enumerate(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 346:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sum_of_squares(a, b)
      Returns a value via 'return' keyword
    def sum_of_squares(a, b)
      Returns a value via 'return' keyword
    def sum_of_squares(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, +, ()
  Control structures: for loop iteration
  Functions called: pow, print, range, square, sum_of_squares
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * x
  • Binary operation: square(...) + square(...)
  • Function call: square(a)
  • Function call: square(b)
  • Function call: sum_of_squares(3, 4)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(x)
  • Function call: pow(x, x)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: sum_of_squares(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_squares(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_squares' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sum_of_squares' (stored in memory, not executed yet)
  5. Define function 'sum_of_squares' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sum_of_squares(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_squares' is defined but NOT executed until called
    - 'sum_of_squares' is defined but NOT executed until called
    - 'sum_of_squares' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 347:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def greet()
      Returns a value via 'return' keyword
    def greet()
      Returns a value via 'return' keyword
    def greet()
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: get_name, greet, input, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input('What is your name? ')
  • Function call: get_name()
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: greet()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: input('What is your name? ')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: input('What is your name? ')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'greet' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'greet' (stored in memory, not executed yet)
  5. Define function 'greet' (stored in memory, not executed yet)

▸ EXECUTION FLOW

  Function definition phase:
    - 'greet' is defined but NOT executed until called
    - 'greet' is defined but NOT executed until called
    - 'greet' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
"""

"""
Problem 348:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: *, -, (), ==
  Control structures: if/else conditional branching
  Functions called: factorial, inner_factorial, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: x * inner_factorial(...)
  • Binary operation: x - 1
  • Function call: inner_factorial(computed expression)
  • Function call: inner_factorial(n)
  • Function call: factorial(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: factorial(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: factorial(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'factorial' (stored in memory, not executed yet)
  5. Define function 'factorial' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 349:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def add_prefix(prefix)
      Returns a value via 'return' keyword
    def add_prefix(prefix)
      Returns a value via 'return' keyword
    def add_prefix(prefix)
      Returns a value via 'return' keyword
    def add_prefix(prefix)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, +, ., ()
  Functions called: add_prefix, prefix_func, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: prefix + string
  • Function call: add_prefix('pre-')
  • Assignment: right side evaluated first, then stored in 'prefix_func'
  • Function call: prefix_func('fix')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: prefix + string
  • Function call: add_prefix('pre-')
  • Assignment: right side evaluated first, then stored in 'prefix_func'
  • Function call: prefix_func('fix')
  • Assignment: right side evaluated first, then stored in 'result1'
  • Function call: prefix_func('view')
  • Assignment: right side evaluated first, then stored in 'result2'
  • Function call: prefix_func('dict')
  • Assignment: right side evaluated first, then stored in 'result3'
  • Function call: print(result1)
  • Function call: print(result2)
  • Function call: print(result3)
  • Function call: ''.join(value)
  • Binary operation: '%s%s' % expression
  • Assignment: right side evaluated first, then stored in 'prefix'
  • Function call: add_prefix(prefix)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: add_prefix('Hi, ')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'add_prefix' (stored in memory, not executed yet)
  2. Assign value to variable: prefix_func
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'add_prefix' (stored in memory, not executed yet)
  6. Assign value to variable: prefix_func
  7. Assign value to variable: result1
  8. Assign value to variable: result2
  9. Assign value to variable: result3
  10. Execute function call: print(result1)
  11. Execute function call: print(result2)
  12. Execute function call: print(result3)
  13. Define function 'add_prefix' (stored in memory, not executed yet)
  14. Define function 'add_prefix' (stored in memory, not executed yet)
  15. Assign value to variable: prefix
  16. Assign value to variable: result
  17. Execute function call: print(result)
  18. Execute function call: print(add_prefix(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'add_prefix' is defined but NOT executed until called
    - 'add_prefix' is defined but NOT executed until called
    - 'add_prefix' is defined but NOT executed until called
    - 'add_prefix' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 350:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def check_even(num)
      Returns a value via 'return' keyword
    def check_even(num)
      Returns a value via 'return' keyword
    def check_even(num)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, (), ==
  Functions called: check_even, is_even, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x % 2
  • Function call: is_even(num)
  • Function call: check_even(10)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'num'
  • Function call: check_even(num)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: check_even(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'check_even' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'check_even' (stored in memory, not executed yet)
  5. Define function 'check_even' (stored in memory, not executed yet)
  6. Assign value to variable: num
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(check_even(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'check_even' is defined but NOT executed until called
    - 'check_even' is defined but NOT executed until called
    - 'check_even' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 351:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def power(base, exponent)
      Returns a value via 'return' keyword
    def power(base, exponent)
      Returns a value via 'return' keyword
    def power(base, exponent)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), **
  Functions called: calculate_power, pow, power, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: b ** e
  • Function call: calculate_power(base, exponent)
  • Function call: power(2, 3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: pow(b, e)
  • Function call: math.pow(b, e)
  • Assignment: right side evaluated first, then stored in 'base'
  • Assignment: right side evaluated first, then stored in 'exponent'
  • Function call: power(base, exponent)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: power(10, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'power' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'power' (stored in memory, not executed yet)
  5. Define function 'power' (stored in memory, not executed yet)
  6. Assign value to variable: base
  7. Assign value to variable: exponent
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(power(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
    - 'power' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 352:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def area_circle(radius)
      Returns a value via 'return' keyword
    def area_circle(radius)
      Returns a value via 'return' keyword
    def area_circle(radius)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ., ()
  Functions called: area_circle, calculate_area, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression * r
  • Binary operation: math.pi * r
  • Function call: calculate_area(radius)
  • Function call: print(value)
  • Function call: area_circle(5)
  • Binary operation: expression * r
  • Binary operation: math.pi * r
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression * r
  • Binary operation: math.pi * r
  • Assignment: right side evaluated first, then stored in 'radius'
  • Function call: area_circle(radius)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: area_circle(14)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: math
  2. Define function 'area_circle' (stored in memory, not executed yet)
  3. Execute function call: print(area_circle(...))
  4. Define function 'area_circle' (stored in memory, not executed yet)
  5. Define function 'area_circle' (stored in memory, not executed yet)
  6. Assign value to variable: radius
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(area_circle(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'area_circle' is defined but NOT executed until called
    - 'area_circle' is defined but NOT executed until called
    - 'area_circle' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 353:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: capitalize, capitalize_words, print, str
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: word.capitalize()
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'capitalized_words'
  • Function call: capitalized_words.append(value)
  • Function call: capitalize(word)
  • Function call: ' '.join(capitalized_words)
  • Function call: capitalize_words('hello world from python')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: word.capitalize()
  • Function call: str(...).capitalize()
  • Function call: str(word)
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: capitalize_words(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: capitalize_words('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'capitalize_words' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'capitalize_words' (stored in memory, not executed yet)
  5. Define function 'capitalize_words' (stored in memory, not executed yet)
  6. Assign value to variable: sentence
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(capitalize_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 354:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_palindrome(word)
      Returns a value via 'return' keyword
    def is_palindrome(word)
      Returns a value via 'return' keyword
    def is_palindrome(word)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, -, ., (), ==, []
  Control structures: for loop iteration
  Functions called: is_palindrome, print, reverse_string, reversed
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: reverse_string(word)
  • Function call: is_palindrome('radar')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: ''.join(value)
  • Function call: reversed(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'word'
  • Function call: is_palindrome(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome('coding')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_palindrome' (stored in memory, not executed yet)
  5. Define function 'is_palindrome' (stored in memory, not executed yet)
  6. Assign value to variable: word
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 355:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def concatenate_strings(str1, str2)
      Returns a value via 'return' keyword
    def concatenate_strings(str1, str2)
      Returns a value via 'return' keyword
    def concatenate_strings(str1, str2)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, +, ., ()
  Functions called: concatenate_strings, join_strings, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: s1 + s2
  • Function call: join_strings(str1, str2)
  • Function call: concatenate_strings('Hello, ', 'world!')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: ''.join(value)
  • Binary operation: '%s%s' % expression
  • Assignment: right side evaluated first, then stored in 'str1'
  • Assignment: right side evaluated first, then stored in 'str2'
  • Function call: concatenate_strings(str1, str2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: concatenate_strings('world', 'python')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'concatenate_strings' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'concatenate_strings' (stored in memory, not executed yet)
  5. Define function 'concatenate_strings' (stored in memory, not executed yet)
  6. Assign value to variable: str1
  7. Assign value to variable: str2
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(concatenate_strings(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'concatenate_strings' is defined but NOT executed until called
    - 'concatenate_strings' is defined but NOT executed until called
    - 'concatenate_strings' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 356:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: +, -, (), <=
  Control structures: if/else conditional branching; list comprehension
  Functions called: fib, fibonacci, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: fib(...) + fib(...)
  • Binary operation: x - 1
  • Binary operation: x - 2
  • Function call: fib(computed expression)
  • Function call: fib(computed expression)
  • Function call: fib(i)
  • Function call: range(n)
  • Function call: fibonacci(10)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: fibonacci(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fibonacci(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fibonacci' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'fibonacci' (stored in memory, not executed yet)
  5. Define function 'fibonacci' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(fibonacci(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 357:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def to_uppercase(string)
      Returns a value via 'return' keyword
    def to_uppercase(string)
      Returns a value via 'return' keyword
    def to_uppercase(string)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), <=
  Control structures: generator expression
  Functions called: chr, convert, ord, print, str, to_uppercase
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.upper()
  • Function call: convert(string)
  • Function call: to_uppercase('hello world')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: ord(...) - 32
  • Function call: ''.join(value)
  • Function call: chr(computed expression)
  • Function call: ord(c)
  • Function call: str(...).upper()
  • Function call: str(s)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: to_uppercase(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: to_uppercase('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'to_uppercase' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'to_uppercase' (stored in memory, not executed yet)
  5. Define function 'to_uppercase' (stored in memory, not executed yet)
  6. Assign value to variable: string
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(to_uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'to_uppercase' is defined but NOT executed until called
    - 'to_uppercase' is defined but NOT executed until called
    - 'to_uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 358:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_largest(a, b)
      Returns a value via 'return' keyword
    def find_largest(a, b)
      Returns a value via 'return' keyword
    def find_largest(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: >, ()
  Control structures: if/else conditional branching
  Functions called: compare, find_largest, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Function call: compare(a, b)
  • Function call: find_largest(10, 20)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: find_largest(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_largest(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_largest' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'find_largest' (stored in memory, not executed yet)
  5. Define function 'find_largest' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(find_largest(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_largest' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 359:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculator(operation, a, b)
      Returns a value via 'return' keyword
    def calculator(operation, a, b)
      Returns a value via 'return' keyword
    def calculator(operation, a, b)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: *, +, -, ., /, !=, (), ==
  Functions called: add, calculator, divide, multiply, print, subtract, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x + y
  • Binary operation: x - y
  • Binary operation: x * y
  • Condition branch: evaluates boolean expression first
  • Binary operation: x / y
  • Condition branch: evaluates boolean expression first
  • Function call: add(a, b)
  • Condition branch: evaluates boolean expression first
  • Function call: subtract(a, b)
  • Condition branch: evaluates boolean expression first
  • Function call: multiply(a, b)
  • Condition branch: evaluates boolean expression first
  • Function call: divide(a, b)
  • Function call: calculator('add', 5, 3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: operator.add(x, y)
  • Assignment: right side evaluated first, then stored in 'operation'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculator(operation, a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculator(25, 20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculator' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculator' (stored in memory, not executed yet)
  5. Define function 'calculator' (stored in memory, not executed yet)
  6. Assign value to variable: operation
  7. Assign value to variable: a
  8. Assign value to variable: b
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(calculator(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculator' is defined but NOT executed until called
    - 'calculator' is defined but NOT executed until called
    - 'calculator' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 360:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_prime(n)
      Returns a value via 'return' keyword
    def is_prime(n)
      Returns a value via 'return' keyword
    def is_prime(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, +, <, (), **, ==
  Control structures: for loop iteration
  Functions called: check_divisibility, int, is_prime, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: x ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: x % i
  • Condition branch: evaluates boolean expression first
  • Function call: check_divisibility(n)
  • Function call: is_prime(7)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: is_prime(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_prime(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_prime' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_prime' (stored in memory, not executed yet)
  5. Define function 'is_prime' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 361:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
    def count_vowels(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: count_vowels, is_vowel, len, print, sum
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: is_vowel(char)
  • Condition branch: evaluates boolean expression first
  • Function call: count_vowels('Hello World')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: is_vowel(char)
  • Function call: len(value)
  • Function call: is_vowel(char)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: count_vowels(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'count_vowels' (stored in memory, not executed yet)
  5. Define function 'count_vowels' (stored in memory, not executed yet)
  6. Assign value to variable: string
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over string, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 362:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_average(numbers)
      Returns a value via 'return' keyword
    def calculate_average(numbers)
      Returns a value via 'return' keyword
    def calculate_average(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: /, ()
  Control structures: for loop iteration
  Functions called: calculate_average, len, print, sum_numbers
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: sum_numbers(...) / len(...)
  • Function call: sum_numbers(numbers)
  • Function call: len(numbers)
  • Function call: calculate_average(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: calculate_average(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_average' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_average' (stored in memory, not executed yet)
  5. Define function 'calculate_average' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(calculate_average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over nums, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 363:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def reverse_string()
      Returns a value via 'return' keyword
    def reverse_string(string)
      Returns a value via 'return' keyword
    def reverse_string()
      Returns a value via 'return' keyword
    def reverse_string()
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, -, ., (), []
  Control structures: for loop iteration
  Functions called: print, reverse, reverse_func, reverse_string, reversed
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: reverse_string()
  • Assignment: right side evaluated first, then stored in 'reverse_func'
  • Function call: print(value)
  • Function call: reverse_func('Hello, World!')
  • Assignment: right side evaluated first, then stored in 'reversed_str'
  • Binary operation: char + reversed_str
  • Assignment: right side evaluated first, then stored in 'reversed_str'
  • Function call: reverse(string)
  • Function call: reverse_string('Hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: ''.join(value)
  • Function call: reversed(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: char + result
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: reverse_string(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_string('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_string' (stored in memory, not executed yet)
  2. Assign value to variable: reverse_func
  3. Execute function call: print(reverse_func(...))
  4. Define function 'reverse_string' (stored in memory, not executed yet)
  5. Assign value to variable: result
  6. Execute function call: print(result)
  7. Define function 'reverse_string' (stored in memory, not executed yet)
  8. Define function 'reverse_string' (stored in memory, not executed yet)
  9. Assign value to variable: string
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(reverse_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 364:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def is_sorted(lst)
      Returns a value via 'return' keyword
    def is_sorted()
      Returns a value via 'return' keyword
    def compare_elements(lst)
      Returns a value via 'return' keyword
    def is_sorted(lst)
      Returns a value via 'return' keyword
    def is_sorted(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, -, >, (), ==, []
  Control structures: for loop iteration
  Functions called: compare_elements, is_sorted, len, print, range, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: len(...) - 1
  • Function call: range(computed expression)
  • Function call: len(lst)
  • Binary operation: i + 1
  • Condition branch: evaluates boolean expression first
  • Function call: compare_elements()
  • Function call: is_sorted(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: compare_elements(lst)
  • Function call: sorted(lst)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: is_sorted()
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: is_sorted(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_sorted(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_sorted' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_sorted' (stored in memory, not executed yet)
  5. Define function 'compare_elements' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Execute function call: is_sorted()
  8. Define function 'is_sorted' (stored in memory, not executed yet)
  9. Define function 'is_sorted' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(is_sorted(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_sorted' is defined but NOT executed until called
    - 'is_sorted' is defined but NOT executed until called
    - 'compare_elements' is defined but NOT executed until called
    - 'is_sorted' is defined but NOT executed until called
    - 'is_sorted' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - is_sorted() is invoked
      → Execution jumps to the function body of 'is_sorted'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 365:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def generate_squares(n)
      Returns a value via 'return' keyword
    def generate_squares(n)
      Returns a value via 'return' keyword
    def generate_squares(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, +, ., ()
  Control structures: for loop iteration
  Functions called: generate_squares, pow, print, range, square
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * x
  • Assignment: right side evaluated first, then stored in 'squares'
  • Binary operation: n + 1
  • Function call: range(1, computed expression)
  • Function call: squares.append(value)
  • Function call: square(i)
  • Function call: generate_squares(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(x)
  • Function call: pow(x, x)
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: generate_squares(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: generate_squares(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'generate_squares' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'generate_squares' (stored in memory, not executed yet)
  5. Define function 'generate_squares' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(generate_squares(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'generate_squares' is defined but NOT executed until called
    - 'generate_squares' is defined but NOT executed until called
    - 'generate_squares' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 366:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sum_of_two(a, b)
      Returns a value via 'return' keyword
    def sum_of_two(a, b)
      Returns a value via 'return' keyword
    def sum_of_two(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., ()
  Functions called: add, print, sum, sum_of_two
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x + y
  • Function call: add(a, b)
  • Function call: sum_of_two(3, 5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: operator.add(x, y)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: sum_of_two(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_two(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_two' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sum_of_two' (stored in memory, not executed yet)
  5. Define function 'sum_of_two' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sum_of_two(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_two' is defined but NOT executed until called
    - 'sum_of_two' is defined but NOT executed until called
    - 'sum_of_two' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 367:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def even_or_odd(n)
      Returns a value via 'return' keyword
    def even_or_odd(n)
      Returns a value via 'return' keyword
    def even_or_odd(n)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: %, (), ==
  Control structures: if/else conditional branching
  Functions called: even_or_odd, is_even, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x % 2
  • Function call: is_even(n)
  • Condition branch: evaluates boolean expression first
  • Function call: print(value)
  • Function call: print(value)
  • Function call: even_or_odd(10)
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % 2
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: even_or_odd(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: even_or_odd(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'even_or_odd' (stored in memory, not executed yet)
  2. Execute function call: even_or_odd(10)
  3. Define function 'even_or_odd' (stored in memory, not executed yet)
  4. Define function 'even_or_odd' (stored in memory, not executed yet)
  5. Assign value to variable: n
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Execute function call: print(even_or_odd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'even_or_odd' is defined but NOT executed until called
    - 'even_or_odd' is defined but NOT executed until called
    - 'even_or_odd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - even_or_odd() is invoked
      → Execution jumps to the function body of 'even_or_odd'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 368:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def convert_temperature(celsius)
      Returns a value via 'return' keyword
    def convert_temperature(celsius)
      Returns a value via 'return' keyword
    def convert_temperature(celsius)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, +, /, ()
  Functions called: celsius_to_fahrenheit, convert_temperature, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression + 32
  • Binary operation: expression / 5
  • Binary operation: c * 9
  • Function call: celsius_to_fahrenheit(celsius)
  • Function call: convert_temperature(25)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression + 32
  • Binary operation: expression / 5
  • Binary operation: c * 9
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression + 32
  • Binary operation: expression / 5
  • Binary operation: c * 9
  • Assignment: right side evaluated first, then stored in 'celsius'
  • Function call: convert_temperature(celsius)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: convert_temperature(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'convert_temperature' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'convert_temperature' (stored in memory, not executed yet)
  5. Define function 'convert_temperature' (stored in memory, not executed yet)
  6. Assign value to variable: celsius
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(convert_temperature(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'convert_temperature' is defined but NOT executed until called
    - 'convert_temperature' is defined but NOT executed until called
    - 'convert_temperature' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 369:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def rectangle_area(length, width)
      Returns a value via 'return' keyword
    def rectangle_area(length, width)
      Returns a value via 'return' keyword
    def rectangle_area(length, width)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: calculate_area, pow, print, range, rectangle_area
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: l * w
  • Function call: calculate_area(length, width)
  • Function call: rectangle_area(5, 10)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(w)
  • Function call: pow(l, w)
  • Assignment: right side evaluated first, then stored in 'length'
  • Assignment: right side evaluated first, then stored in 'width'
  • Function call: rectangle_area(length, width)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: rectangle_area(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'rectangle_area' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'rectangle_area' (stored in memory, not executed yet)
  5. Define function 'rectangle_area' (stored in memory, not executed yet)
  6. Assign value to variable: length
  7. Assign value to variable: width
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(rectangle_area(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'rectangle_area' is defined but NOT executed until called
    - 'rectangle_area' is defined but NOT executed until called
    - 'rectangle_area' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 370:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def check_prime(n)
      Returns a value via 'return' keyword
    def check_prime(n)
      Returns a value via 'return' keyword
    def check_prime(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, +, <, (), **, ==
  Control structures: for loop iteration
  Functions called: check_prime, int, is_prime, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: int(...) + 1
  • Binary operation: num ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: num % i
  • Condition branch: evaluates boolean expression first
  • Function call: is_prime(n)
  • Function call: check_prime(11)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: check_prime(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: check_prime(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'check_prime' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'check_prime' (stored in memory, not executed yet)
  5. Define function 'check_prime' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(check_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'check_prime' is defined but NOT executed until called
    - 'check_prime' is defined but NOT executed until called
    - 'check_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 371:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_minimum(a, b, c)
      Returns a value via 'return' keyword
    def find_minimum(a, b, c)
      Returns a value via 'return' keyword
    def find_minimum(a, b, c)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: find_minimum, min, min_of_three, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(x, y, z)
  • Function call: min_of_three(a, b, c)
  • Function call: find_minimum(5, 3, 8)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: min(x, y, z)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: min(x, y, z)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Assignment: right side evaluated first, then stored in 'c'
  • Function call: find_minimum(a, b, c)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_minimum(20, 30, 10)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_minimum' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'find_minimum' (stored in memory, not executed yet)
  5. Define function 'find_minimum' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: c
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(find_minimum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_minimum' is defined but NOT executed until called
    - 'find_minimum' is defined but NOT executed until called
    - 'find_minimum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 373:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_factorial(n)
      Returns a value via 'return' keyword
    def calculate_factorial(n)
      Returns a value via 'return' keyword
    def calculate_factorial(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, (), ==, or
  Control structures: if/else conditional branching; for loop iteration
  Functions called: calculate_factorial, factorial, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: num + 1
  • Function call: range(2, computed expression)
  • Function call: factorial(n)
  • Function call: calculate_factorial(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: calculate_factorial(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_factorial(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_factorial' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_factorial' (stored in memory, not executed yet)
  5. Define function 'calculate_factorial' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(calculate_factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_factorial' is defined but NOT executed until called
    - 'calculate_factorial' is defined but NOT executed until called
    - 'calculate_factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 374:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
    def capitalize_words(sentence)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ., (), []
  Control structures: for loop iteration
  Functions called: capitalize, capitalize_words, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression.upper(...) + expression
  • Function call: expression.upper()
  • Function call: sentence.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Assignment: right side evaluated first, then stored in 'capitalized_words'
  • Function call: capitalized_words.append(value)
  • Function call: capitalize(word)
  • Function call: ' '.join(capitalized_words)
  • Function call: capitalize_words('hello world from python')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression.upper(...) + expression
  • Function call: expression.upper()
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression.upper(...) + expression
  • Function call: expression.upper()
  • Assignment: right side evaluated first, then stored in 'sentence'
  • Function call: capitalize_words(sentence)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: capitalize_words('Python is fun')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'capitalize_words' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'capitalize_words' (stored in memory, not executed yet)
  5. Define function 'capitalize_words' (stored in memory, not executed yet)
  6. Assign value to variable: sentence
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(capitalize_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
    - 'capitalize_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 375:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 6
    def max_in_list(lst)
      Returns a value via 'return' keyword
    def find_largest()
      Returns a value via 'return' keyword
    def max_in_list(lst)
      Returns a value via 'return' keyword
    def find_largest()
      Returns a value via 'return' keyword
    def max_in_list(lst)
      Returns a value via 'return' keyword
    def max_in_list(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, >, (), [], not
  Control structures: for loop iteration
  Functions called: find_largest, max, max_in_list, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(lst)
  • Function call: max_in_list(lst)
  • Assignment: right side evaluated first, then stored in 'largest'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_largest()
  • Condition branch: evaluates boolean expression first
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: max_in_list(numbers)
  • Assignment: right side evaluated first, then stored in 'largest'
  • Function call: print(value)
  • Function call: find_largest()
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: max_in_list(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_in_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_in_list' (stored in memory, not executed yet)
  2. Define function 'find_largest' (stored in memory, not executed yet)
  3. Assign value to variable: lst
  4. Execute function call: print(find_largest(...))
  5. Define function 'max_in_list' (stored in memory, not executed yet)
  6. Define function 'find_largest' (stored in memory, not executed yet)
  7. Execute function call: print(find_largest(...))
  8. Define function 'max_in_list' (stored in memory, not executed yet)
  9. Define function 'max_in_list' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(max_in_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_in_list' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
    - 'max_in_list' is defined but NOT executed until called
    - 'find_largest' is defined but NOT executed until called
    - 'max_in_list' is defined but NOT executed until called
    - 'max_in_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 376:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def reverse_string(s)
      Returns a value via 'return' keyword
    def reverse_string(s)
      Returns a value via 'return' keyword
    def reverse_string(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ()
  Control structures: for loop iteration
  Functions called: print, reverse, reverse_string
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'reversed_str'
  • Binary operation: char + reversed_str
  • Assignment: right side evaluated first, then stored in 'reversed_str'
  • Function call: reverse(s)
  • Function call: reverse_string('hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: reverse_string(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_string' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'reverse_string' (stored in memory, not executed yet)
  5. Define function 'reverse_string' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(reverse_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 377:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_average(lst)
      Returns a value via 'return' keyword
    def calculate_average(lst)
      Returns a value via 'return' keyword
    def calculate_average(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: /, ()
  Control structures: for loop iteration
  Functions called: average, calculate_average, len, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Binary operation: total / len(...)
  • Function call: len(lst)
  • Function call: average(lst)
  • Function call: calculate_average(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: total / len(...)
  • Function call: len(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: total / len(...)
  • Function call: len(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: calculate_average(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_average' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_average' (stored in memory, not executed yet)
  5. Define function 'calculate_average' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(calculate_average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 378:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def check_palindrome(s)
      Returns a value via 'return' keyword
    def check_palindrome(s)
      Returns a value via 'return' keyword
    def check_palindrome(s)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, (), ==, []
  Functions called: check_palindrome, is_palindrome, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: is_palindrome(s)
  • Function call: check_palindrome('racecar')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: check_palindrome(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: check_palindrome(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'check_palindrome' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'check_palindrome' (stored in memory, not executed yet)
  5. Define function 'check_palindrome' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(check_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'check_palindrome' is defined but NOT executed until called
    - 'check_palindrome' is defined but NOT executed until called
    - 'check_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 379:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def closest_to_zero(numbers)
      Returns a value via 'return' keyword
    def closest_to_zero(numbers)
      Returns a value via 'return' keyword
    def closest_to_zero(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, <, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: abs, closest_to_zero, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'closest'
  • Function call: abs(num)
  • Function call: abs(closest)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'closest'
  • Function call: closest_to_zero(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: closest_to_zero(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: closest_to_zero(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'closest_to_zero' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'closest_to_zero' (stored in memory, not executed yet)
  5. Define function 'closest_to_zero' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(closest_to_zero(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'closest_to_zero' is defined but NOT executed until called
    - 'closest_to_zero' is defined but NOT executed until called
    - 'closest_to_zero' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 380:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def check_sorted(lst)
      Returns a value via 'return' keyword
    def check_sorted(lst)
      Returns a value via 'return' keyword
    def check_sorted(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: +, -, >, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: check_sorted, is_sorted, len, print, range
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: len(...) - 1
  • Function call: range(computed expression)
  • Function call: len(lst)
  • Binary operation: i + 1
  • Condition branch: evaluates boolean expression first
  • Function call: is_sorted(lst)
  • Function call: check_sorted(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: check_sorted(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: check_sorted(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'check_sorted' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'check_sorted' (stored in memory, not executed yet)
  5. Define function 'check_sorted' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(check_sorted(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'check_sorted' is defined but NOT executed until called
    - 'check_sorted' is defined but NOT executed until called
    - 'check_sorted' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 381:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def convert_to_integers(strings)
      Returns a value via 'return' keyword
    def convert_to_integers(strings)
      Returns a value via 'return' keyword
    def convert_to_integers(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: convert_to_integers, input, list, map, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: input(...).split()
  • Function call: input('type numbers: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: print(value)
  • Function call: list(value)
  • Function call: map(int, user)
  • Function call: list(value)
  • Function call: map(int, strings)
  • Assignment: right side evaluated first, then stored in 'numbers_in_strings'
  • Function call: print(value)
  • Function call: convert_to_integers(numbers_in_strings)
  • Function call: list(value)
  • Function call: map(int, strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: list(value)
  • Function call: map(int, strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: convert_to_integers(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: convert_to_integers('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Assign value to variable: user
  2. Execute function call: print(list(...))
  3. Define function 'convert_to_integers' (stored in memory, not executed yet)
  4. Assign value to variable: numbers_in_strings
  5. Execute function call: print(convert_to_integers(...))
  6. Define function 'convert_to_integers' (stored in memory, not executed yet)
  7. Define function 'convert_to_integers' (stored in memory, not executed yet)
  8. Assign value to variable: strings
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(convert_to_integers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'convert_to_integers' is defined but NOT executed until called
    - 'convert_to_integers' is defined but NOT executed until called
    - 'convert_to_integers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 382:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_strings_by_length(strings)
      Returns a value via 'return' keyword
    def sort_strings_by_length(strings)
      Returns a value via 'return' keyword
    def sort_strings_by_length(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: print, sort_strings_by_length, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(strings)
  • Assignment: right side evaluated first, then stored in 'sorted_strings'
  • Function call: sort_strings_by_length(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: sort_strings_by_length(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_strings_by_length(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_strings_by_length' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_strings_by_length' (stored in memory, not executed yet)
  5. Define function 'sort_strings_by_length' (stored in memory, not executed yet)
  6. Assign value to variable: strings
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_strings_by_length(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_strings_by_length' is defined but NOT executed until called
    - 'sort_strings_by_length' is defined but NOT executed until called
    - 'sort_strings_by_length' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 384:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_tuples_by_second(tuples)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples)
      Returns a value via 'return' keyword
    def sort_tuples_by_second(tuples)
      Returns a value via 'return' keyword
  Keywords used: def, lambda, return
  Operators used: (), []
  Control structures: lambda expression
  Functions called: print, sort_tuples_by_second, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(tuples)
  • Assignment: right side evaluated first, then stored in 'sorted_tuples'
  • Function call: sort_tuples_by_second(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'tuples'
  • Function call: sort_tuples_by_second(tuples)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_tuples_by_second(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  5. Define function 'sort_tuples_by_second' (stored in memory, not executed yet)
  6. Assign value to variable: tuples
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_tuples_by_second(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
    - 'sort_tuples_by_second' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 385:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_dicts_by_key(dicts, key_name)
      Returns a value via 'return' keyword
    def sort_dicts_by_key(dicts, key_name)
      Returns a value via 'return' keyword
    def sort_dicts_by_key(dicts, key_name)
      Returns a value via 'return' keyword
  Keywords used: def, lambda, return
  Operators used: (), []
  Control structures: lambda expression
  Functions called: print, sort_dicts_by_key, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(dicts)
  • Assignment: right side evaluated first, then stored in 'sorted_dicts'
  • Function call: sort_dicts_by_key(value, 'age')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dicts'
  • Assignment: right side evaluated first, then stored in 'key_name'
  • Function call: sort_dicts_by_key(dicts, key_name)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_dicts_by_key(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_dicts_by_key' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_dicts_by_key' (stored in memory, not executed yet)
  5. Define function 'sort_dicts_by_key' (stored in memory, not executed yet)
  6. Assign value to variable: dicts
  7. Assign value to variable: key_name
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sort_dicts_by_key(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_dicts_by_key' is defined but NOT executed until called
    - 'sort_dicts_by_key' is defined but NOT executed until called
    - 'sort_dicts_by_key' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 386:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def longest_word(words)
      Returns a value via 'return' keyword
    def longest_word(words)
      Returns a value via 'return' keyword
    def longest_word(words)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, ()
  Control structures: if/else conditional branching; for loop iteration
  Functions called: len, longest_word, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'longest'
  • Function call: len(word)
  • Function call: len(longest)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'longest'
  • Function call: longest_word(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: longest_word(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: longest_word(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'longest_word' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'longest_word' (stored in memory, not executed yet)
  5. Define function 'longest_word' (stored in memory, not executed yet)
  6. Assign value to variable: words
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(longest_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'longest_word' is defined but NOT executed until called
    - 'longest_word' is defined but NOT executed until called
    - 'longest_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 387:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_by_absolute_value(numbers)
      Returns a value via 'return' keyword
    def sort_by_absolute_value(numbers)
      Returns a value via 'return' keyword
    def sort_by_absolute_value(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ()
  Functions called: print, sort_by_absolute_value, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'sorted_numbers'
  • Function call: sort_by_absolute_value(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: sort_by_absolute_value(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_by_absolute_value(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_by_absolute_value' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_by_absolute_value' (stored in memory, not executed yet)
  5. Define function 'sort_by_absolute_value' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_by_absolute_value(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_by_absolute_value' is defined but NOT executed until called
    - 'sort_by_absolute_value' is defined but NOT executed until called
    - 'sort_by_absolute_value' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 388:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def sort_strings_by_last_character(strings)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def sort_strings_by_last_character(strings)
      Returns a value via 'return' keyword
    def sort_strings_by_last_character(strings)
      Returns a value via 'return' keyword
  Keywords used: def, lambda, return
  Operators used: -, (), []
  Control structures: lambda expression
  Functions called: list, main, print, sort_strings_by_last_character, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(strings)
  • Assignment: right side evaluated first, then stored in 'sorted_strings'
  • Function call: sort_strings_by_last_character(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: sort_strings_by_last_character(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_strings_by_last_character(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_strings_by_last_character' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'main' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: main(lst)
  7. Define function 'sort_strings_by_last_character' (stored in memory, not executed yet)
  8. Define function 'sort_strings_by_last_character' (stored in memory, not executed yet)
  9. Assign value to variable: strings
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(sort_strings_by_last_character(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_strings_by_last_character' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'sort_strings_by_last_character' is defined but NOT executed until called
    - 'sort_strings_by_last_character' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
"""

"""
Problem 389:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def earliest_date(dates)
      Returns a value via 'return' keyword
    def earliest_date(dates)
      Returns a value via 'return' keyword
    def earliest_date(dates)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: <, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: earliest_date, min, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(dates)
  • Assignment: right side evaluated first, then stored in 'earliest'
  • Function call: earliest_date(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sorted(dates)
  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dates'
  • Function call: earliest_date(dates)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: earliest_date(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'earliest_date' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'earliest_date' (stored in memory, not executed yet)
  5. Define function 'earliest_date' (stored in memory, not executed yet)
  6. Assign value to variable: dates
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(earliest_date(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'earliest_date' is defined but NOT executed until called
    - 'earliest_date' is defined but NOT executed until called
    - 'earliest_date' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over dates, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 390:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def get_surname(name)
      Returns a value via 'return' keyword
    def get_surname(name)
      Returns a value via 'return' keyword
    def get_surname(name)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: get_surname, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: name.split()
  • Assignment: right side evaluated first, then stored in 'names'
  • Function call: sorted(names)
  • Assignment: right side evaluated first, then stored in 'sorted_names'
  • Function call: print(sorted_names)
  • Function call: name.split()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: name.split()
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: get_surname(name)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: get_surname('Bob')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'get_surname' (stored in memory, not executed yet)
  2. Assign value to variable: names
  3. Assign value to variable: sorted_names
  4. Execute function call: print(sorted_names)
  5. Define function 'get_surname' (stored in memory, not executed yet)
  6. Define function 'get_surname' (stored in memory, not executed yet)
  7. Assign value to variable: name
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(get_surname(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'get_surname' is defined but NOT executed until called
    - 'get_surname' is defined but NOT executed until called
    - 'get_surname' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 391:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_strings_alphabetically(strings)
      Returns a value via 'return' keyword
    def sort_strings_alphabetically(strings)
      Returns a value via 'return' keyword
    def sort_strings_alphabetically(strings)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: print, sort_strings_alphabetically, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: sort_strings_alphabetically(strings)
  • Assignment: right side evaluated first, then stored in 'sorted_strings'
  • Function call: print(sorted_strings)
  • Function call: sorted(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: sort_strings_alphabetically(strings)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_strings_alphabetically(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_strings_alphabetically' (stored in memory, not executed yet)
  2. Assign value to variable: strings
  3. Assign value to variable: sorted_strings
  4. Execute function call: print(sorted_strings)
  5. Define function 'sort_strings_alphabetically' (stored in memory, not executed yet)
  6. Define function 'sort_strings_alphabetically' (stored in memory, not executed yet)
  7. Assign value to variable: strings
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sort_strings_alphabetically(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_strings_alphabetically' is defined but NOT executed until called
    - 'sort_strings_alphabetically' is defined but NOT executed until called
    - 'sort_strings_alphabetically' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 392:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def scrabble_score(word)
      Returns a value via 'return' keyword
    def find_highest_scrabble_word(words)
      Returns a value via 'return' keyword
    def scrabble_score(word)
      Returns a value via 'return' keyword
    def scrabble_score(word)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: find_highest_scrabble_word, max, print, scrabble_score
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'score_chart'
  • Assignment: right side evaluated first, then stored in 'total_score'
  • Function call: word.upper()
  • Function call: score_chart.get(letter, 0)
  • Function call: max(words)
  • Assignment: right side evaluated first, then stored in 'highest_word'
  • Function call: find_highest_scrabble_word(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'word'
  • Function call: scrabble_score(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: scrabble_score('coding')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'scrabble_score' (stored in memory, not executed yet)
  2. Define function 'find_highest_scrabble_word' (stored in memory, not executed yet)
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'scrabble_score' (stored in memory, not executed yet)
  6. Define function 'scrabble_score' (stored in memory, not executed yet)
  7. Assign value to variable: word
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(scrabble_score(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'scrabble_score' is defined but NOT executed until called
    - 'find_highest_scrabble_word' is defined but NOT executed until called
    - 'scrabble_score' is defined but NOT executed until called
    - 'scrabble_score' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over word.upper(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 393:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_complex_numbers(complex_numbers)
      Returns a value via 'return' keyword
    def sort_complex_numbers(complex_numbers)
      Returns a value via 'return' keyword
    def sort_complex_numbers(complex_numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ()
  Functions called: abs, print, sort_complex_numbers, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: abs(c)
  • Function call: sorted(complex_numbers)
  • Assignment: right side evaluated first, then stored in 'sorted_numbers'
  • Binary operation: 3 + 4j
  • Binary operation: 1 + 1j
  • Binary operation: 0 + 2j
  • Binary operation: 5 + 0j
  • Function call: sort_complex_numbers(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: abs(c)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: abs(c)
  • Assignment: right side evaluated first, then stored in 'complex_numbers'
  • Function call: sort_complex_numbers(complex_numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_complex_numbers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_complex_numbers' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_complex_numbers' (stored in memory, not executed yet)
  5. Define function 'sort_complex_numbers' (stored in memory, not executed yet)
  6. Assign value to variable: complex_numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_complex_numbers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_complex_numbers' is defined but NOT executed until called
    - 'sort_complex_numbers' is defined but NOT executed until called
    - 'sort_complex_numbers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 394:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_largest_file(file_paths)
      Returns a value via 'return' keyword
    def find_largest_file(file_paths)
      Returns a value via 'return' keyword
    def find_largest_file(file_paths)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: find_largest_file, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: os.path.getsize(file_path)
  • Function call: max(file_paths)
  • Assignment: right side evaluated first, then stored in 'largest_file'
  • Function call: os.path.getsize(file_path)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: os.path.getsize(file_path)
  • Assignment: right side evaluated first, then stored in 'file_paths'
  • Function call: find_largest_file(file_paths)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_largest_file(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import module: os
  2. Define function 'find_largest_file' (stored in memory, not executed yet)
  3. Define function 'find_largest_file' (stored in memory, not executed yet)
  4. Define function 'find_largest_file' (stored in memory, not executed yet)
  5. Assign value to variable: file_paths
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Execute function call: print(find_largest_file(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_largest_file' is defined but NOT executed until called
    - 'find_largest_file' is defined but NOT executed until called
    - 'find_largest_file' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
"""

"""
Problem 395:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_fractions(fractions)
      Returns a value via 'return' keyword
    def sort_fractions(fractions)
      Returns a value via 'return' keyword
    def sort_fractions(fractions)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: Fraction, float, print, sort_fractions, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: float(frac)
  • Function call: sorted(fractions)
  • Assignment: right side evaluated first, then stored in 'sorted_fractions'
  • Function call: sort_fractions(value)
  • Function call: Fraction(1, 2)
  • Function call: Fraction(1, 3)
  • Function call: Fraction(2, 3)
  • Function call: Fraction(1, 4)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: float(frac)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: float(frac)
  • Assignment: right side evaluated first, then stored in 'fractions'
  • Function call: sort_fractions(fractions)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_fractions(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import from fractions: Fraction
  2. Define function 'sort_fractions' (stored in memory, not executed yet)
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'sort_fractions' (stored in memory, not executed yet)
  6. Define function 'sort_fractions' (stored in memory, not executed yet)
  7. Assign value to variable: fractions
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sort_fractions(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_fractions' is defined but NOT executed until called
    - 'sort_fractions' is defined but NOT executed until called
    - 'sort_fractions' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 396:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def sort_tuples_by_sum(tuples)
      Returns a value via 'return' keyword
    def main(lst_tuples)
      Returns a value via 'return' keyword
    def sort_tuples_by_sum(tuples)
      Returns a value via 'return' keyword
    def sort_tuples_by_sum(tuples)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ., (), []
  Control structures: for loop iteration
  Functions called: list, main, print, reduce, sort_tuples_by_sum, sorted, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sum(tup)
  • Function call: sorted(tuples)
  • Assignment: right side evaluated first, then stored in 'sorted_tuples'
  • Function call: sort_tuples_by_sum(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression + expression
  • Function call: list(value)
  • Function call: sorted(lst_tuples)
  • Assignment: right side evaluated first, then stored in 'lst_tuples'
  • Function call: print(value)
  • Function call: main(lst_tuples)
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: reduce(value, tup, 0)
  • Assignment: right side evaluated first, then stored in 'tuples'
  • Function call: sort_tuples_by_sum(tuples)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_tuples_by_sum(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_tuples_by_sum' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'main' (stored in memory, not executed yet)
  5. Assign value to variable: lst_tuples
  6. Execute function call: print(main(...))
  7. Define function 'sort_tuples_by_sum' (stored in memory, not executed yet)
  8. Define function 'sort_tuples_by_sum' (stored in memory, not executed yet)
  9. Assign value to variable: tuples
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(sort_tuples_by_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_tuples_by_sum' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'sort_tuples_by_sum' is defined but NOT executed until called
    - 'sort_tuples_by_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over tup, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 397:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def closest_to_zero(numbers)
      Returns a value via 'return' keyword
    def closest_to_zero(numbers)
      Returns a value via 'return' keyword
    def closest_to_zero(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ()
  Functions called: closest_to_zero, min, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(numbers)
  • Assignment: right side evaluated first, then stored in 'closest'
  • Function call: closest_to_zero(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: closest_to_zero(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: closest_to_zero(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'closest_to_zero' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'closest_to_zero' (stored in memory, not executed yet)
  5. Define function 'closest_to_zero' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(closest_to_zero(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'closest_to_zero' is defined but NOT executed until called
    - 'closest_to_zero' is defined but NOT executed until called
    - 'closest_to_zero' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 398:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_points_by_distance(points)
      Returns a value via 'return' keyword
    def sort_points_by_distance(points)
      Returns a value via 'return' keyword
    def sort_points_by_distance(points)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, (), **, []
  Functions called: print, sort_points_by_distance, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression ** 0.5
  • Binary operation: expression + expression
  • Binary operation: expression ** 2
  • Binary operation: expression ** 2
  • Function call: sorted(points)
  • Assignment: right side evaluated first, then stored in 'sorted_points'
  • Function call: sort_points_by_distance(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression ** 0.5
  • Binary operation: expression + expression
  • Binary operation: expression ** 2
  • Binary operation: expression ** 2
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression ** 0.5
  • Binary operation: expression + expression
  • Binary operation: expression ** 2
  • Binary operation: expression ** 2
  • Assignment: right side evaluated first, then stored in 'points'
  • Function call: sort_points_by_distance(points)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_points_by_distance(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_points_by_distance' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_points_by_distance' (stored in memory, not executed yet)
  5. Define function 'sort_points_by_distance' (stored in memory, not executed yet)
  6. Assign value to variable: points
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_points_by_distance(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_points_by_distance' is defined but NOT executed until called
    - 'sort_points_by_distance' is defined but NOT executed until called
    - 'sort_points_by_distance' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 399:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_words_by_distinct_letters(words)
      Returns a value via 'return' keyword
    def sort_words_by_distinct_letters(words)
      Returns a value via 'return' keyword
    def sort_words_by_distinct_letters(words)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: len, print, set, sort_words_by_distinct_letters, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(value)
  • Function call: set(word)
  • Function call: sorted(words)
  • Assignment: right side evaluated first, then stored in 'sorted_words'
  • Function call: sort_words_by_distinct_letters(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: len(value)
  • Function call: set(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(value)
  • Function call: set(word)
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: sort_words_by_distinct_letters(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_words_by_distinct_letters(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_words_by_distinct_letters' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_words_by_distinct_letters' (stored in memory, not executed yet)
  5. Define function 'sort_words_by_distinct_letters' (stored in memory, not executed yet)
  6. Assign value to variable: words
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_words_by_distinct_letters(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_words_by_distinct_letters' is defined but NOT executed until called
    - 'sort_words_by_distinct_letters' is defined but NOT executed until called
    - 'sort_words_by_distinct_letters' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 400:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def main(lst)
      Returns a value via 'return' keyword
    def ascii_value(word)
      Returns a value via 'return' keyword
    def find_highest_ascii_word(words)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
    def main(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: find_highest_ascii_word, main, max, ord, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: ord(char)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'total_value'
  • Function call: ord(char)
  • Function call: max(words)
  • Assignment: right side evaluated first, then stored in 'highest_word'
  • Function call: find_highest_ascii_word(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: main(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: main(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: main(lst)
  4. Define function 'ascii_value' (stored in memory, not executed yet)
  5. Define function 'find_highest_ascii_word' (stored in memory, not executed yet)
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Define function 'main' (stored in memory, not executed yet)
  9. Define function 'main' (stored in memory, not executed yet)
  10. Assign value to variable: lst
  11. Assign value to variable: result
  12. Execute function call: print(result)
  13. Execute function call: print(main(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'ascii_value' is defined but NOT executed until called
    - 'find_highest_ascii_word' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 401:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def count_vowels(s)
      Returns a value via 'return' keyword
    def sort_by_vowel_count(strings)
      Returns a value via 'return' keyword
    def count_vowels(s)
      Returns a value via 'return' keyword
    def count_vowels(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: count_vowels, len, print, sort_by_vowel_count, sorted, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: s.lower()
  • Assignment: right side evaluated first, then stored in 's'
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(strings)
  • Assignment: right side evaluated first, then stored in 'strings'
  • Function call: sort_by_vowel_count(strings)
  • Assignment: right side evaluated first, then stored in 'sorted_strings'
  • Function call: print(sorted_strings)
  • Function call: sum(value)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: count_vowels(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Define function 'sort_by_vowel_count' (stored in memory, not executed yet)
  3. Assign value to variable: strings
  4. Assign value to variable: sorted_strings
  5. Execute function call: print(sorted_strings)
  6. Define function 'count_vowels' (stored in memory, not executed yet)
  7. Define function 'count_vowels' (stored in memory, not executed yet)
  8. Assign value to variable: s
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'sort_by_vowel_count' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 402:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def convert_to_integers(integers)
      Returns a value via 'return' keyword
    def convert_to_integers(integers)
      Returns a value via 'return' keyword
    def convert_to_integers(integers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: convert_to_integers, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: list(value)
  • Function call: map(str, integers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: convert_to_integers(numbers)
  • Function call: list(value)
  • Function call: map(str, integers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: list(value)
  • Function call: map(str, integers)
  • Assignment: right side evaluated first, then stored in 'integers'
  • Function call: convert_to_integers(integers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: convert_to_integers(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'convert_to_integers' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(convert_to_integers(...))
  4. Define function 'convert_to_integers' (stored in memory, not executed yet)
  5. Define function 'convert_to_integers' (stored in memory, not executed yet)
  6. Assign value to variable: integers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(convert_to_integers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'convert_to_integers' is defined but NOT executed until called
    - 'convert_to_integers' is defined but NOT executed until called
    - 'convert_to_integers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 403:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def add_five(lst)
      Returns a value via 'return' keyword
    def add_five(lst)
      Returns a value via 'return' keyword
    def add_five(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, lambda, return
  Operators used: +, ., ()
  Control structures: for loop iteration; list comprehension; lambda expression
  Functions called: add_five, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: number + 5
  • Function call: result.append(computed expression)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: add_five(lst)
  • Binary operation: number + 5
  • Binary operation: number + 5
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: add_five(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: add_five(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'add_five' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(add_five(...))
  4. Define function 'add_five' (stored in memory, not executed yet)
  5. Define function 'add_five' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(add_five(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'add_five' is defined but NOT executed until called
    - 'add_five' is defined but NOT executed until called
    - 'add_five' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 404:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def find_min(numbers)
      Returns a value via 'return' keyword
    def main(user)
      Returns a value via 'return' keyword
    def find_min(numbers)
      Returns a value via 'return' keyword
    def find_min(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., <, (), []
  Control structures: for loop iteration
  Functions called: find_min, input, int, main, print
  User input: uses input() for interactive data entry
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'min_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: find_min(lst)
  • Assignment: right side evaluated first, then stored in 'int_lst'
  • Function call: int_lst.append(value)
  • Function call: int(num)
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'min_num'
  • Function call: input(...).split()
  • Function call: input('type lst of number: ')
  • Assignment: right side evaluated first, then stored in 'user'
  • Function call: main(user)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: find_min(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_min(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_min' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(find_min(...))
  4. Define function 'main' (stored in memory, not executed yet)
  5. Assign value to variable: user
  6. Execute function call: main(user)
  7. Define function 'find_min' (stored in memory, not executed yet)
  8. Define function 'find_min' (stored in memory, not executed yet)
  9. Assign value to variable: numbers
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(find_min(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_min' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
    - 'find_min' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over user, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over int_lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 405:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def converts(cm)
    def converts(cm)
      Returns a value via 'return' keyword
    def converts(cm)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ()
  Functions called: converts, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: cm * 0.39
  • Assignment: right side evaluated first, then stored in 'inch'
  • Function call: print(inch)
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: print(value)
  • Function call: converts(number)
  • Binary operation: cm * 0.39
  • Assignment: right side evaluated first, then stored in 'inch'
  • Binary operation: cm * 0.39
  • Assignment: right side evaluated first, then stored in 'inch'
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'cm'
  • Function call: converts(cm)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: converts(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'converts' (stored in memory, not executed yet)
  2. Assign value to variable: number
  3. Execute function call: print(converts(...))
  4. Define function 'converts' (stored in memory, not executed yet)
  5. Define function 'converts' (stored in memory, not executed yet)
  6. Assign value to variable: cm
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(converts(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'converts' is defined but NOT executed until called
    - 'converts' is defined but NOT executed until called
    - 'converts' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 406:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def reverse(lst)
      Returns a value via 'return' keyword
    def reverse(lst)
      Returns a value via 'return' keyword
    def reverse(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: print, reverse
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: lst.reverse()
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: reverse(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: reverse(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(reverse(...))
  4. Define function 'reverse' (stored in memory, not executed yet)
  5. Define function 'reverse' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(reverse(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
    - 'reverse' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 407:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_average(lst)
      Returns a value via 'return' keyword
    def calculate_average(lst)
      Returns a value via 'return' keyword
    def calculate_average(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, /, ()
  Control structures: for loop iteration
  Functions called: Fraction, calculate_average, float, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'length_num'
  • Assignment: right side evaluated first, then stored in 'add_num'
  • Binary operation: add_num / length_num
  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: calculate_average(lst)
  • Binary operation: add_num * expression
  • Binary operation: 1 / length_num
  • Function call: float(value)
  • Function call: Fraction(add_num, length_num)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: calculate_average(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_average(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_average' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(calculate_average(...))
  4. Define function 'calculate_average' (stored in memory, not executed yet)
  5. Define function 'calculate_average' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(calculate_average(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
    - 'calculate_average' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 408:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: list, map, print, remove_duplicates
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: lst.split()
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'unique'
  • Assignment: right side evaluated first, then stored in 'commom'
  • Condition branch: evaluates boolean expression first
  • Function call: commom.append(word)
  • Function call: unique.append(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: remove_duplicates(lst)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: remove_duplicates(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(remove_duplicates(...))
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Define function 'remove_duplicates' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(remove_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 409:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def len_words(lst)
      Returns a value via 'return' keyword
    def len_words(lst)
      Returns a value via 'return' keyword
    def len_words(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: (), []
  Control structures: for loop iteration
  Functions called: len_words, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'count'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: len_words(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: len_words(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: len_words(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'len_words' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(len_words(...))
  4. Define function 'len_words' (stored in memory, not executed yet)
  5. Define function 'len_words' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(len_words(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'len_words' is defined but NOT executed until called
    - 'len_words' is defined but NOT executed until called
    - 'len_words' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 410:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def filter_string(lst)
      Returns a value via 'return' keyword
    def filter_string(lst)
      Returns a value via 'return' keyword
    def filter_string(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), ==
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: filter_string, len, list, map, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(word)
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(word)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: filter_string(lst)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: filter_string(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: filter_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'filter_string' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(filter_string(...))
  4. Define function 'filter_string' (stored in memory, not executed yet)
  5. Define function 'filter_string' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(filter_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'filter_string' is defined but NOT executed until called
    - 'filter_string' is defined but NOT executed until called
    - 'filter_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 411:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_sum(a, b)
      Returns a value via 'return' keyword
    def calculate_sum(a, b)
      Returns a value via 'return' keyword
    def calculate_sum(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, ., ()
  Functions called: calculate_sum, inner_sum, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x + y
  • Function call: inner_sum(a, b)
  • Function call: calculate_sum(7, 3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: operator.add(x, y)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculate_sum(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_sum(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_sum' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_sum' (stored in memory, not executed yet)
  5. Define function 'calculate_sum' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(calculate_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_sum' is defined but NOT executed until called
    - 'calculate_sum' is defined but NOT executed until called
    - 'calculate_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 412:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_product(a, b)
      Returns a value via 'return' keyword
    def calculate_product(a, b)
      Returns a value via 'return' keyword
    def calculate_product(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: calculate_product, pow, print, product, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a * b
  • Function call: product()
  • Function call: calculate_product(3, 5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(b)
  • Function call: pow(a, b)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculate_product(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_product(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_product' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_product' (stored in memory, not executed yet)
  5. Define function 'calculate_product' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(calculate_product(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_product' is defined but NOT executed until called
    - 'calculate_product' is defined but NOT executed until called
    - 'calculate_product' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 413:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_difference(a, b)
      Returns a value via 'return' keyword
    def calculate_difference(a, b)
      Returns a value via 'return' keyword
    def calculate_difference(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: +, -, ., ()
  Functions called: calculate_difference, difference, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a - b
  • Function call: difference()
  • Function call: calculate_difference(10, 3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: a + expression
  • Function call: operator.sub(a, b)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculate_difference(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_difference(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_difference' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_difference' (stored in memory, not executed yet)
  5. Define function 'calculate_difference' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(calculate_difference(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_difference' is defined but NOT executed until called
    - 'calculate_difference' is defined but NOT executed until called
    - 'calculate_difference' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 414:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_quotient(a, b)
      Returns a value via 'return' keyword
    def calculate_quotient(a, b)
      Returns a value via 'return' keyword
    def calculate_quotient(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, /, ()
  Functions called: Fraction, calculate_quotient, float, print, quotient
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: a / b
  • Function call: quotient()
  • Function call: calculate_quotient(10, 2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: a * expression
  • Binary operation: 1 / b
  • Function call: float(value)
  • Function call: Fraction(a, b)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculate_quotient(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_quotient(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_quotient' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'calculate_quotient' (stored in memory, not executed yet)
  5. Define function 'calculate_quotient' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(calculate_quotient(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_quotient' is defined but NOT executed until called
    - 'calculate_quotient' is defined but NOT executed until called
    - 'calculate_quotient' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 415:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def area_of_rectangle(length, width)
      Returns a value via 'return' keyword
    def area_of_rectangle(length, width)
      Returns a value via 'return' keyword
    def area_of_rectangle(length, width)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: area, area_of_rectangle, pow, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: length * width
  • Function call: area()
  • Function call: area_of_rectangle(5, 4)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(width)
  • Function call: pow(length, width)
  • Assignment: right side evaluated first, then stored in 'length'
  • Assignment: right side evaluated first, then stored in 'width'
  • Function call: area_of_rectangle(length, width)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: area_of_rectangle(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'area_of_rectangle' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'area_of_rectangle' (stored in memory, not executed yet)
  5. Define function 'area_of_rectangle' (stored in memory, not executed yet)
  6. Assign value to variable: length
  7. Assign value to variable: width
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(area_of_rectangle(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'area_of_rectangle' is defined but NOT executed until called
    - 'area_of_rectangle' is defined but NOT executed until called
    - 'area_of_rectangle' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 416:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def perimeter_of_square(side_length)
      Returns a value via 'return' keyword
    def perimeter_of_square(side_length)
      Returns a value via 'return' keyword
    def perimeter_of_square(side_length)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: perimeter, perimeter_of_square, pow, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: 4 * side_length
  • Function call: perimeter()
  • Function call: perimeter_of_square(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(side_length)
  • Function call: pow(4, side_length)
  • Assignment: right side evaluated first, then stored in 'side_length'
  • Function call: perimeter_of_square(side_length)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: perimeter_of_square(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'perimeter_of_square' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'perimeter_of_square' (stored in memory, not executed yet)
  5. Define function 'perimeter_of_square' (stored in memory, not executed yet)
  6. Assign value to variable: side_length
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(perimeter_of_square(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'perimeter_of_square' is defined but NOT executed until called
    - 'perimeter_of_square' is defined but NOT executed until called
    - 'perimeter_of_square' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 417:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def fahrenheit_to_celsius(fahrenheit)
      Returns a value via 'return' keyword
    def fahrenheit_to_celsius(fahrenheit)
      Returns a value via 'return' keyword
    def fahrenheit_to_celsius(fahrenheit)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, -, /, ()
  Functions called: convert, fahrenheit_to_celsius, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression / 9
  • Binary operation: expression * 5
  • Binary operation: fahrenheit - 32
  • Function call: convert()
  • Function call: fahrenheit_to_celsius(68)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression / 9
  • Binary operation: expression * 5
  • Binary operation: fahrenheit - 32
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression / 9
  • Binary operation: expression * 5
  • Binary operation: fahrenheit - 32
  • Assignment: right side evaluated first, then stored in 'fahrenheit'
  • Function call: fahrenheit_to_celsius(fahrenheit)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fahrenheit_to_celsius(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fahrenheit_to_celsius' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'fahrenheit_to_celsius' (stored in memory, not executed yet)
  5. Define function 'fahrenheit_to_celsius' (stored in memory, not executed yet)
  6. Assign value to variable: fahrenheit
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(fahrenheit_to_celsius(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fahrenheit_to_celsius' is defined but NOT executed until called
    - 'fahrenheit_to_celsius' is defined but NOT executed until called
    - 'fahrenheit_to_celsius' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 418:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def celsius_to_fahrenheit(celsius)
      Returns a value via 'return' keyword
    def celsius_to_fahrenheit(celsius)
      Returns a value via 'return' keyword
    def celsius_to_fahrenheit(celsius)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, +, /, ()
  Functions called: celsius_to_fahrenheit, convert, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression + 32
  • Binary operation: expression / 5
  • Binary operation: celsius * 9
  • Function call: convert()
  • Function call: celsius_to_fahrenheit(20)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression + 32
  • Binary operation: expression / 5
  • Binary operation: celsius * 9
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression + 32
  • Binary operation: expression / 5
  • Binary operation: celsius * 9
  • Assignment: right side evaluated first, then stored in 'celsius'
  • Function call: celsius_to_fahrenheit(celsius)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: celsius_to_fahrenheit(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'celsius_to_fahrenheit' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'celsius_to_fahrenheit' (stored in memory, not executed yet)
  5. Define function 'celsius_to_fahrenheit' (stored in memory, not executed yet)
  6. Assign value to variable: celsius
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(celsius_to_fahrenheit(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'celsius_to_fahrenheit' is defined but NOT executed until called
    - 'celsius_to_fahrenheit' is defined but NOT executed until called
    - 'celsius_to_fahrenheit' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 419:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def even_or_odd(number)
      Returns a value via 'return' keyword
    def even_or_odd(number)
      Returns a value via 'return' keyword
    def even_or_odd(number)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: %, (), ==
  Control structures: if/else conditional branching
  Functions called: check, even_or_odd, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: number % 2
  • Condition branch: evaluates boolean expression first
  • Function call: check()
  • Function call: even_or_odd(7)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: even_or_odd(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: even_or_odd(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'even_or_odd' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'even_or_odd' (stored in memory, not executed yet)
  5. Define function 'even_or_odd' (stored in memory, not executed yet)
  6. Assign value to variable: number
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(even_or_odd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'even_or_odd' is defined but NOT executed until called
    - 'even_or_odd' is defined but NOT executed until called
    - 'even_or_odd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 420:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
    def is_prime(number)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: %, +, <, (), **, ==
  Control structures: for loop iteration
  Functions called: check_divisibility, int, is_prime, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: int(...) + 1
  • Binary operation: n ** 0.5
  • Function call: range(2, computed expression)
  • Function call: int(computed expression)
  • Binary operation: n % i
  • Condition branch: evaluates boolean expression first
  • Condition branch: evaluates boolean expression first
  • Function call: check_divisibility(number)
  • Function call: is_prime(29)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'number'
  • Function call: is_prime(number)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_prime(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_prime' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_prime' (stored in memory, not executed yet)
  5. Define function 'is_prime' (stored in memory, not executed yet)
  6. Assign value to variable: number
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_prime(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
    - 'is_prime' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 421:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def list_sum(numbers)
      Returns a value via 'return' keyword
    def list_sum(numbers)
      Returns a value via 'return' keyword
    def list_sum(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: add_elements, list_sum, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: add_elements()
  • Function call: list_sum(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: list_sum(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_sum(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_sum' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'list_sum' (stored in memory, not executed yet)
  5. Define function 'list_sum' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(list_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_sum' is defined but NOT executed until called
    - 'list_sum' is defined but NOT executed until called
    - 'list_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 422:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def list_product(numbers)
      Returns a value via 'return' keyword
    def list_product(numbers)
      Returns a value via 'return' keyword
    def list_product(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: list_product, multiply_elements, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'product'
  • Function call: multiply_elements()
  • Function call: list_product(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: list_product(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_product(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_product' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'list_product' (stored in memory, not executed yet)
  5. Define function 'list_product' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(list_product(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_product' is defined but NOT executed until called
    - 'list_product' is defined but NOT executed until called
    - 'list_product' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over numbers, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 423:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def greater_than(a, b)
      Returns a value via 'return' keyword
    def greater_than(a, b)
      Returns a value via 'return' keyword
    def greater_than(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: >, (), <=, not
  Control structures: if/else conditional branching
  Functions called: compare, greater_than, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: compare()
  • Function call: greater_than(10, 5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: greater_than(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: greater_than(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'greater_than' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'greater_than' (stored in memory, not executed yet)
  5. Define function 'greater_than' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(greater_than(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'greater_than' is defined but NOT executed until called
    - 'greater_than' is defined but NOT executed until called
    - 'greater_than' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 424:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def string_length(s)
      Returns a value via 'return' keyword
    def string_length(s)
      Returns a value via 'return' keyword
    def string_length(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: calculate_length, print, string_length
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'length'
  • Function call: calculate_length()
  • Function call: string_length('Hello, World!')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: string_length(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: string_length(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'string_length' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'string_length' (stored in memory, not executed yet)
  5. Define function 'string_length' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(string_length(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'string_length' is defined but NOT executed until called
    - 'string_length' is defined but NOT executed until called
    - 'string_length' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 425:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def first_and_last(s)
      Returns a value via 'return' keyword
    def first_and_last(s)
      Returns a value via 'return' keyword
    def first_and_last(s)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: -, >, (), []
  Control structures: if/else conditional branching
  Functions called: first_and_last, get_first_last, len, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(s)
  • Condition branch: evaluates boolean expression first
  • Function call: get_first_last()
  • Function call: first_and_last('Hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: first_and_last(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: first_and_last(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'first_and_last' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'first_and_last' (stored in memory, not executed yet)
  5. Define function 'first_and_last' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(first_and_last(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'first_and_last' is defined but NOT executed until called
    - 'first_and_last' is defined but NOT executed until called
    - 'first_and_last' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 426:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def count_vowels(s)
      Returns a value via 'return' keyword
    def count_vowels(s)
      Returns a value via 'return' keyword
    def count_vowels(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: count_vowels, is_vowel, len, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: char.lower()
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: is_vowel(char)
  • Condition branch: evaluates boolean expression first
  • Function call: count_vowels('Hello, World!')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: sum(value)
  • Function call: is_vowel(char)
  • Function call: len(value)
  • Function call: is_vowel(char)
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: count_vowels(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: count_vowels(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'count_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'count_vowels' (stored in memory, not executed yet)
  5. Define function 'count_vowels' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(count_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
    - 'count_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 427:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def reverse_string(s)
      Returns a value via 'return' keyword
    def reverse_string(s)
      Returns a value via 'return' keyword
    def reverse_string(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: +, ()
  Control structures: for loop iteration
  Functions called: print, reverse, reverse_string
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'reversed_s'
  • Binary operation: char + reversed_s
  • Assignment: right side evaluated first, then stored in 'reversed_s'
  • Function call: reverse()
  • Function call: reverse_string('Hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: reverse_string(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: reverse_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'reverse_string' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'reverse_string' (stored in memory, not executed yet)
  5. Define function 'reverse_string' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(reverse_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
    - 'reverse_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 428:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
    def is_palindrome(s)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, (), ==, []
  Functions called: check, is_palindrome, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: check()
  • Function call: is_palindrome('racecar')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_palindrome(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_palindrome(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_palindrome' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_palindrome' (stored in memory, not executed yet)
  5. Define function 'is_palindrome' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_palindrome(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
    - 'is_palindrome' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 429:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def multiply_by_n(n)
      Returns a value via 'return' keyword
    def multiply_by_n(n)
      Returns a value via 'return' keyword
    def multiply_by_n(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: multiply_by_3, multiply_by_n, pow, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: n * x
  • Function call: multiply_by_n(3)
  • Assignment: right side evaluated first, then stored in 'multiply_by_3'
  • Function call: multiply_by_3(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(x)
  • Function call: pow(n, x)
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: multiply_by_n(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: multiply_by_n(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'multiply_by_n' (stored in memory, not executed yet)
  2. Assign value to variable: multiply_by_3
  3. Assign value to variable: result
  4. Execute function call: print(result)
  5. Define function 'multiply_by_n' (stored in memory, not executed yet)
  6. Define function 'multiply_by_n' (stored in memory, not executed yet)
  7. Assign value to variable: n
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(multiply_by_n(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'multiply_by_n' is defined but NOT executed until called
    - 'multiply_by_n' is defined but NOT executed until called
    - 'multiply_by_n' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 430:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def add_tax(price)
      Returns a value via 'return' keyword
    def add_tax(price)
      Returns a value via 'return' keyword
    def add_tax(price)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, +, ()
  Functions called: add_tax, calculate_tax, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'tax_rate'
  • Binary operation: price + expression
  • Binary operation: price * tax_rate
  • Function call: calculate_tax()
  • Function call: add_tax(100)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: price + expression
  • Binary operation: price * tax_rate
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: price + expression
  • Binary operation: price * tax_rate
  • Assignment: right side evaluated first, then stored in 'price'
  • Function call: add_tax(price)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: add_tax(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'add_tax' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'add_tax' (stored in memory, not executed yet)
  5. Define function 'add_tax' (stored in memory, not executed yet)
  6. Assign value to variable: price
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(add_tax(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'add_tax' is defined but NOT executed until called
    - 'add_tax' is defined but NOT executed until called
    - 'add_tax' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 431:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def greet_person(name)
      Returns a value via 'return' keyword
    def greet_person(name)
      Returns a value via 'return' keyword
    def greet_person(name)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, ., ()
  Functions called: greet_person, greeting, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: greeting()
  • Function call: greet_person('Alice')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: 'Hello, {}!'.format(name)
  • Binary operation: 'Hello, %s!' % name
  • Assignment: right side evaluated first, then stored in 'name'
  • Function call: greet_person(name)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: greet_person('Bob')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'greet_person' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'greet_person' (stored in memory, not executed yet)
  5. Define function 'greet_person' (stored in memory, not executed yet)
  6. Assign value to variable: name
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(greet_person(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'greet_person' is defined but NOT executed until called
    - 'greet_person' is defined but NOT executed until called
    - 'greet_person' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 432:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
    def factorial(n)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: *, -, (), ==, or
  Control structures: if/else conditional branching
  Functions called: calc_factorial, factorial, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: num * calc_factorial(...)
  • Binary operation: num - 1
  • Function call: calc_factorial(computed expression)
  • Function call: calc_factorial(n)
  • Function call: factorial(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: factorial(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: factorial(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'factorial' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'factorial' (stored in memory, not executed yet)
  5. Define function 'factorial' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(factorial(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
    - 'factorial' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 433:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def exponent(base, power)
      Returns a value via 'return' keyword
    def exponent(base, power)
      Returns a value via 'return' keyword
    def exponent(base, power)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: calculate, exponent, print, range
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(power)
  • Function call: calculate()
  • Function call: exponent(2, 3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'base'
  • Assignment: right side evaluated first, then stored in 'power'
  • Function call: exponent(base, power)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: exponent(10, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'exponent' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'exponent' (stored in memory, not executed yet)
  5. Define function 'exponent' (stored in memory, not executed yet)
  6. Assign value to variable: base
  7. Assign value to variable: power
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(exponent(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'exponent' is defined but NOT executed until called
    - 'exponent' is defined but NOT executed until called
    - 'exponent' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 434:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
    def gcd(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return, while
  Operators used: %, !=, ()
  Control structures: while loop
  Functions called: calculate_gcd, gcd, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x % y
  • Function call: calculate_gcd(a, b)
  • Function call: gcd(48, 18)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: gcd(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: gcd(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'gcd' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'gcd' (stored in memory, not executed yet)
  5. Define function 'gcd' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(gcd(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
    - 'gcd' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
"""

"""
Problem 435:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def lcm(a, b)
      Returns a value via 'return' keyword
    def lcm(a, b)
      Returns a value via 'return' keyword
    def lcm(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return, while
  Operators used: %, *, !=, (), //
  Control structures: while loop
  Functions called: abs, calculate_lcm, gcd, lcm, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: abs(...) // gcd(...)
  • Binary operation: x * y
  • Function call: abs(computed expression)
  • Function call: gcd(x, y)
  • Binary operation: x % y
  • Function call: calculate_lcm(a, b)
  • Function call: lcm(4, 5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: abs(...) // gcd(...)
  • Binary operation: x * y
  • Function call: abs(computed expression)
  • Function call: gcd(x, y)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: abs(...) // gcd(...)
  • Binary operation: x * y
  • Function call: abs(computed expression)
  • Function call: gcd(x, y)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: lcm(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: lcm(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'lcm' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'lcm' (stored in memory, not executed yet)
  5. Define function 'lcm' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(lcm(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'lcm' is defined but NOT executed until called
    - 'lcm' is defined but NOT executed until called
    - 'lcm' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
"""

"""
Problem 436:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def square_of_number(n)
      Returns a value via 'return' keyword
    def square_of_number(n)
      Returns a value via 'return' keyword
    def square_of_number(n)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: pow, print, range, square, square_of_number
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: n * n
  • Function call: square()
  • Function call: square_of_number(5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(n)
  • Function call: pow(n, n)
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: square_of_number(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: square_of_number(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'square_of_number' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'square_of_number' (stored in memory, not executed yet)
  5. Define function 'square_of_number' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(square_of_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'square_of_number' is defined but NOT executed until called
    - 'square_of_number' is defined but NOT executed until called
    - 'square_of_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 437:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def cube_of_number(n)
      Returns a value via 'return' keyword
    def cube_of_number(n)
      Returns a value via 'return' keyword
    def cube_of_number(n)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, ()
  Functions called: cube, cube_of_number, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression * n
  • Binary operation: n * n
  • Function call: cube()
  • Function call: cube_of_number(3)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: expression * n
  • Binary operation: n * n
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression * n
  • Binary operation: n * n
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: cube_of_number(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: cube_of_number(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'cube_of_number' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'cube_of_number' (stored in memory, not executed yet)
  5. Define function 'cube_of_number' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(cube_of_number(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'cube_of_number' is defined but NOT executed until called
    - 'cube_of_number' is defined but NOT executed until called
    - 'cube_of_number' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 438:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_list_descending(lst)
      Returns a value via 'return' keyword
    def sort_list_descending(lst)
      Returns a value via 'return' keyword
    def sort_list_descending(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return, while
  Operators used: ., >, (), []
  Control structures: if/else conditional branching; for loop iteration; while loop; list comprehension; lambda expression
  Functions called: list, map, print, sort, sort_list_descending
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'sorted_list'
  • Assignment: right side evaluated first, then stored in 'maximum'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'maximum'
  • Function call: sorted_list.append(maximum)
  • Function call: lst.remove(maximum)
  • Function call: sort()
  • Function call: sort_list_descending(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: list(value)
  • Function call: map(value, lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_list_descending(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_list_descending(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_list_descending' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'sort_list_descending' (stored in memory, not executed yet)
  5. Define function 'sort_list_descending' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_list_descending(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_list_descending' is defined but NOT executed until called
    - 'sort_list_descending' is defined but NOT executed until called
    - 'sort_list_descending' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - While loop checks condition before each iteration
    - Body repeats as long as condition remains True
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 439:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
    def max_of_three(a, b, c)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: >, ()
  Functions called: find_max, max_of_three, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'max_value'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_value'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_value'
  • Function call: find_max()
  • Function call: max_of_three(10, 20, 15)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Assignment: right side evaluated first, then stored in 'c'
  • Function call: max_of_three(a, b, c)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_of_three(20, 30, 10)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_of_three' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'max_of_three' (stored in memory, not executed yet)
  5. Define function 'max_of_three' (stored in memory, not executed yet)
  6. Assign value to variable: a
  7. Assign value to variable: b
  8. Assign value to variable: c
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(max_of_three(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
    - 'max_of_three' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 440:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: /, !=, ()
  Control structures: for loop iteration
  Functions called: average_of_list, calculate_average, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: total / count
  • Function call: calculate_average()
  • Function call: average_of_list(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: average_of_list(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average_of_list' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'average_of_list' (stored in memory, not executed yet)
  5. Define function 'average_of_list' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(average_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 441:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: /, !=, ()
  Control structures: for loop iteration
  Functions called: average_of_list, calculate_average, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'total'
  • Assignment: right side evaluated first, then stored in 'count'
  • Binary operation: total / count
  • Function call: calculate_average()
  • Function call: average_of_list(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: total / count
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: average_of_list(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average_of_list' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'average_of_list' (stored in memory, not executed yet)
  5. Define function 'average_of_list' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(average_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over lst, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 442:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_uppercase(s)
      Returns a value via 'return' keyword
    def is_uppercase(s)
      Returns a value via 'return' keyword
    def is_uppercase(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), not
  Control structures: if/else conditional branching; for loop iteration
  Functions called: check, is_uppercase, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: char.isupper()
  • Condition branch: evaluates boolean expression first
  • Function call: check()
  • Function call: is_uppercase('HELLO')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_uppercase(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_uppercase(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_uppercase' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_uppercase' (stored in memory, not executed yet)
  5. Define function 'is_uppercase' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_uppercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_uppercase' is defined but NOT executed until called
    - 'is_uppercase' is defined but NOT executed until called
    - 'is_uppercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 443:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def is_lowercase(s)
      Returns a value via 'return' keyword
    def is_lowercase(s)
      Returns a value via 'return' keyword
    def is_lowercase(s)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), not
  Control structures: if/else conditional branching; for loop iteration
  Functions called: check, is_lowercase, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: char.islower()
  • Condition branch: evaluates boolean expression first
  • Function call: check()
  • Function call: is_lowercase('hello')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 's'
  • Function call: is_lowercase(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: is_lowercase(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'is_lowercase' (stored in memory, not executed yet)
  2. Assign value to variable: result
  3. Execute function call: print(result)
  4. Define function 'is_lowercase' (stored in memory, not executed yet)
  5. Define function 'is_lowercase' (stored in memory, not executed yet)
  6. Assign value to variable: s
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(is_lowercase(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'is_lowercase' is defined but NOT executed until called
    - 'is_lowercase' is defined but NOT executed until called
    - 'is_lowercase' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over s, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 444:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def string_contains_digit(string)
      Returns a value via 'return' keyword
    def string_contains_digit(string)
      Returns a value via 'return' keyword
    def string_contains_digit(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., ()
  Control structures: if/else conditional branching; for loop iteration
  Functions called: contains_digit, print, string_contains_digit
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: char.isdigit()
  • Condition branch: evaluates boolean expression first
  • Function call: contains_digit(string)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: string_contains_digit(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: string_contains_digit(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: string_contains_digit('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'string_contains_digit' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(string_contains_digit(...))
  4. Define function 'string_contains_digit' (stored in memory, not executed yet)
  5. Define function 'string_contains_digit' (stored in memory, not executed yet)
  6. Assign value to variable: string
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(string_contains_digit(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'string_contains_digit' is defined but NOT executed until called
    - 'string_contains_digit' is defined but NOT executed until called
    - 'string_contains_digit' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 445:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def string_contains_letter(string)
      Returns a value via 'return' keyword
    def string_contains_letter(string)
      Returns a value via 'return' keyword
    def string_contains_letter(string)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., ()
  Control structures: if/else conditional branching; for loop iteration
  Functions called: contains_letter, print, string_contains_letter
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: char.isalpha()
  • Condition branch: evaluates boolean expression first
  • Function call: contains_letter(string)
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: print(value)
  • Function call: string_contains_letter(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'string'
  • Function call: string_contains_letter(string)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: string_contains_letter('world')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'string_contains_letter' (stored in memory, not executed yet)
  2. Assign value to variable: string
  3. Execute function call: print(string_contains_letter(...))
  4. Define function 'string_contains_letter' (stored in memory, not executed yet)
  5. Define function 'string_contains_letter' (stored in memory, not executed yet)
  6. Assign value to variable: string
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(string_contains_letter(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'string_contains_letter' is defined but NOT executed until called
    - 'string_contains_letter' is defined but NOT executed until called
    - 'string_contains_letter' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 446:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, /, ()
  Control structures: for loop iteration
  Functions called: Fraction, average, average_of_list, float, len, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(x)
  • Assignment: right side evaluated first, then stored in 'count_len'
  • Function call: sum(x)
  • Assignment: right side evaluated first, then stored in 'add_num'
  • Binary operation: add_num / count_len
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: average(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: average_of_list(lst)
  • Binary operation: add_num * expression
  • Binary operation: 1 / count_len
  • Function call: float(value)
  • Function call: Fraction(add_num, count_len)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: average_of_list(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average_of_list' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(average_of_list(...))
  4. Define function 'average_of_list' (stored in memory, not executed yet)
  5. Define function 'average_of_list' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(average_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 447:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def check_multiple_of_n(num1, num2)
      Returns a value via 'return' keyword
    def check_multiple_of_n(num1, num2)
      Returns a value via 'return' keyword
    def check_multiple_of_n(num1, num2)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, (), ==
  Functions called: check_multiple_of_n, is_multiple, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x % y
  • Function call: is_multiple(num1, num2)
  • Function call: print(value)
  • Function call: check_multiple_of_n(10, 2)
  • Function call: print(value)
  • Function call: check_multiple_of_n(10, 3)
  • Binary operation: x % y
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x % y
  • Assignment: right side evaluated first, then stored in 'num1'
  • Assignment: right side evaluated first, then stored in 'num2'
  • Function call: check_multiple_of_n(num1, num2)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: check_multiple_of_n(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'check_multiple_of_n' (stored in memory, not executed yet)
  2. Execute function call: print(check_multiple_of_n(...))
  3. Execute function call: print(check_multiple_of_n(...))
  4. Define function 'check_multiple_of_n' (stored in memory, not executed yet)
  5. Define function 'check_multiple_of_n' (stored in memory, not executed yet)
  6. Assign value to variable: num1
  7. Assign value to variable: num2
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(check_multiple_of_n(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'check_multiple_of_n' is defined but NOT executed until called
    - 'check_multiple_of_n' is defined but NOT executed until called
    - 'check_multiple_of_n' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 448:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def longest_string(lst)
      Returns a value via 'return' keyword
    def longest_string(lst)
      Returns a value via 'return' keyword
    def longest_string(lst)
      Returns a value via 'return' keyword
    def longest_string(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: >, (), []
  Control structures: if/else conditional branching; for loop iteration
  Functions called: find_longest, longest, longest_string, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'longest_word'
  • Function call: longest(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: longest_string(lst)
  • Function call: max(strings)
  • Function call: find_longest(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: longest_string(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: longest_string(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: longest_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'longest_string' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(longest_string(...))
  4. Define function 'longest_string' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Execute function call: print(longest_string(...))
  7. Define function 'longest_string' (stored in memory, not executed yet)
  8. Define function 'longest_string' (stored in memory, not executed yet)
  9. Assign value to variable: lst
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(longest_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'longest_string' is defined but NOT executed until called
    - 'longest_string' is defined but NOT executed until called
    - 'longest_string' is defined but NOT executed until called
    - 'longest_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 449:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def shortest_string(lst)
      Returns a value via 'return' keyword
    def shortest_string(lst)
      Returns a value via 'return' keyword
    def shortest_string(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: min, print, shortest, shortest_string
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(x)
  • Function call: shortest(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: shortest_string(lst)
  • Function call: min(x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: min(x)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: shortest_string(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: shortest_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'shortest_string' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(shortest_string(...))
  4. Define function 'shortest_string' (stored in memory, not executed yet)
  5. Define function 'shortest_string' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(shortest_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'shortest_string' is defined but NOT executed until called
    - 'shortest_string' is defined but NOT executed until called
    - 'shortest_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 450:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
    def remove_duplicates(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, lambda, return
  Operators used: ., (), not in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; lambda expression
  Functions called: duplicates_removed, list, map, print, remove_duplicates
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'result'
  • Condition branch: evaluates boolean expression first
  • Function call: result.append(item)
  • Function call: duplicates_removed(lst)
  • Function call: print(value)
  • Function call: remove_duplicates(value)
  • Function call: list(value)
  • Function call: map(value, items)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: remove_duplicates(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: remove_duplicates(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'remove_duplicates' (stored in memory, not executed yet)
  2. Execute function call: print(remove_duplicates(...))
  3. Define function 'remove_duplicates' (stored in memory, not executed yet)
  4. Define function 'remove_duplicates' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Execute function call: print(remove_duplicates(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
    - 'remove_duplicates' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over items, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 451:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
      Returns a value via 'return' keyword
    def fibonacci(n)
      Returns a value via 'return' keyword
  Keywords used: def, if/elif/else, return
  Operators used: +, -, (), <=
  Control structures: if/else conditional branching
  Functions called: fib_recursive, fibonacci, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Condition branch: evaluates boolean expression first
  • Binary operation: fib_recursive(...) + fib_recursive(...)
  • Binary operation: n - 1
  • Binary operation: n - 2
  • Function call: fib_recursive(computed expression)
  • Function call: fib_recursive(computed expression)
  • Function call: fib_recursive(n)
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: print(value)
  • Function call: fibonacci(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'n'
  • Function call: fibonacci(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: fibonacci(15)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'fibonacci' (stored in memory, not executed yet)
  2. Assign value to variable: n
  3. Execute function call: print(fibonacci(...))
  4. Define function 'fibonacci' (stored in memory, not executed yet)
  5. Define function 'fibonacci' (stored in memory, not executed yet)
  6. Assign value to variable: n
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(fibonacci(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
    - 'fibonacci' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 452:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_sum(a, b)
      Returns a value via 'return' keyword
    def calculate_sum(a, b)
      Returns a value via 'return' keyword
    def calculate_sum(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: *, +, ., ()
  Functions called: calculate_sum, inner_mult, inner_sum, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x + y
  • Binary operation: x * y
  • Function call: inner_mult(a, b)
  • Assignment: right side evaluated first, then stored in 'product'
  • Function call: inner_sum(a, b)
  • Function call: print(value)
  • Function call: calculate_sum(7, 3)
  • Function call: sum(value)
  • Function call: operator.add(x, y)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculate_sum(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_sum(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_sum' (stored in memory, not executed yet)
  2. Execute function call: print(calculate_sum(...))
  3. Define function 'calculate_sum' (stored in memory, not executed yet)
  4. Define function 'calculate_sum' (stored in memory, not executed yet)
  5. Assign value to variable: a
  6. Assign value to variable: b
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(calculate_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_sum' is defined but NOT executed until called
    - 'calculate_sum' is defined but NOT executed until called
    - 'calculate_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 453:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def calculate_integers(a, b)
      Returns a value via 'return' keyword
    def calculate_integers(a, b)
      Returns a value via 'return' keyword
    def calculate_integers(a, b)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, +, /, ()
  Control structures: for loop iteration
  Functions called: calculate_integers, division, pow, print, product, range, sum_int
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * y
  • Binary operation: x + y
  • Binary operation: x / y
  • Function call: product(a, b)
  • Function call: sum_int(a, b)
  • Function call: division(a, b)
  • Function call: print(value)
  • Function call: calculate_integers(10, 5)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(y)
  • Function call: pow(x, y)
  • Assignment: right side evaluated first, then stored in 'a'
  • Assignment: right side evaluated first, then stored in 'b'
  • Function call: calculate_integers(a, b)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: calculate_integers(20, 30)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'calculate_integers' (stored in memory, not executed yet)
  2. Execute function call: print(calculate_integers(...))
  3. Define function 'calculate_integers' (stored in memory, not executed yet)
  4. Define function 'calculate_integers' (stored in memory, not executed yet)
  5. Assign value to variable: a
  6. Assign value to variable: b
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(calculate_integers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'calculate_integers' is defined but NOT executed until called
    - 'calculate_integers' is defined but NOT executed until called
    - 'calculate_integers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 455:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
    def average_of_list(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, /, ()
  Control structures: for loop iteration
  Functions called: Fraction, average, average_of_list, float, len, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(x)
  • Assignment: right side evaluated first, then stored in 'count_len'
  • Function call: sum(x)
  • Assignment: right side evaluated first, then stored in 'add_num'
  • Binary operation: add_num / count_len
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: average(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: average_of_list(lst)
  • Binary operation: add_num * expression
  • Binary operation: 1 / count_len
  • Function call: float(value)
  • Function call: Fraction(add_num, count_len)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: average_of_list(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: average_of_list(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'average_of_list' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(average_of_list(...))
  4. Define function 'average_of_list' (stored in memory, not executed yet)
  5. Define function 'average_of_list' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(average_of_list(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
    - 'average_of_list' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 456:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def len_lst(lst)
      Returns a value via 'return' keyword
    def len_lst(lst)
      Returns a value via 'return' keyword
    def len_lst(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: len_lst, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'sort'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: len_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: len_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: len_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'len_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(len_lst(...))
  4. Define function 'len_lst' (stored in memory, not executed yet)
  5. Define function 'len_lst' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(len_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'len_lst' is defined but NOT executed until called
    - 'len_lst' is defined but NOT executed until called
    - 'len_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 457:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def min_len(lst)
      Returns a value via 'return' keyword
    def min_len(lst)
      Returns a value via 'return' keyword
    def min_len(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: min, min_len, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'min_string'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: min_len(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: min_len(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: min_len(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'min_len' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(min_len(...))
  4. Define function 'min_len' (stored in memory, not executed yet)
  5. Define function 'min_len' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(min_len(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'min_len' is defined but NOT executed until called
    - 'min_len' is defined but NOT executed until called
    - 'min_len' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 458:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def max_string_alpha(lst)
      Returns a value via 'return' keyword
    def max_string_alpha(lst)
      Returns a value via 'return' keyword
    def max_string_alpha(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: max, max_string_alpha, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(lst)
  • Function call: print(value)
  • Function call: max_string_alpha(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: max_string_alpha(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: max_string_alpha(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'max_string_alpha' (stored in memory, not executed yet)
  2. Execute function call: print(max_string_alpha(...))
  3. Define function 'max_string_alpha' (stored in memory, not executed yet)
  4. Define function 'max_string_alpha' (stored in memory, not executed yet)
  5. Assign value to variable: lst
  6. Assign value to variable: result
  7. Execute function call: print(result)
  8. Execute function call: print(max_string_alpha(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'max_string_alpha' is defined but NOT executed until called
    - 'max_string_alpha' is defined but NOT executed until called
    - 'max_string_alpha' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 459:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_dicts_by_key(dict_list, sort_key)
      Returns a value via 'return' keyword
    def sort_dicts_by_key(dict_list, sort_key)
      Returns a value via 'return' keyword
    def sort_dicts_by_key(dict_list, sort_key)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: itemgetter, print, sort_dicts_by_key, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(dict_list)
  • Function call: itemgetter(sort_key)
  • Assignment: right side evaluated first, then stored in 'dict_list'
  • Function call: sort_dicts_by_key(dict_list, 'age')
  • Assignment: right side evaluated first, then stored in 'sorted_list'
  • Function call: print(sorted_list)
  • Function call: sorted(dict_list)
  • Function call: itemgetter(sort_key)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(dict_list)
  • Function call: itemgetter(sort_key)
  • Assignment: right side evaluated first, then stored in 'dict_list'
  • Assignment: right side evaluated first, then stored in 'sort_key'
  • Function call: sort_dicts_by_key(dict_list, sort_key)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_dicts_by_key(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import from operator: itemgetter
  2. Define function 'sort_dicts_by_key' (stored in memory, not executed yet)
  3. Assign value to variable: dict_list
  4. Assign value to variable: sorted_list
  5. Execute function call: print(sorted_list)
  6. Define function 'sort_dicts_by_key' (stored in memory, not executed yet)
  7. Define function 'sort_dicts_by_key' (stored in memory, not executed yet)
  8. Assign value to variable: dict_list
  9. Assign value to variable: sort_key
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(sort_dicts_by_key(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_dicts_by_key' is defined but NOT executed until called
    - 'sort_dicts_by_key' is defined but NOT executed until called
    - 'sort_dicts_by_key' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 460:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_by_absolute_value(lst)
      Returns a value via 'return' keyword
    def sort_by_absolute_value(lst)
      Returns a value via 'return' keyword
    def sort_by_absolute_value(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ()
  Functions called: print, sort_by_absolute_value, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_by_absolute_value(lst)
  • Assignment: right side evaluated first, then stored in 'sorted_lst'
  • Function call: print(sorted_lst)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_by_absolute_value(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_by_absolute_value(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_by_absolute_value' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: sorted_lst
  4. Execute function call: print(sorted_lst)
  5. Define function 'sort_by_absolute_value' (stored in memory, not executed yet)
  6. Define function 'sort_by_absolute_value' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sort_by_absolute_value(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_by_absolute_value' is defined but NOT executed until called
    - 'sort_by_absolute_value' is defined but NOT executed until called
    - 'sort_by_absolute_value' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 461:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_words_by_vowel_count(lst)
      Returns a value via 'return' keyword
    def sort_words_by_vowel_count(lst)
      Returns a value via 'return' keyword
    def sort_words_by_vowel_count(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: len, print, sort_words_by_vowel_count, sorted, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_words_by_vowel_count(lst)
  • Function call: sum(value)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_words_by_vowel_count(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_words_by_vowel_count(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_words_by_vowel_count' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_words_by_vowel_count(...))
  4. Define function 'sort_words_by_vowel_count' (stored in memory, not executed yet)
  5. Define function 'sort_words_by_vowel_count' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_words_by_vowel_count(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_words_by_vowel_count' is defined but NOT executed until called
    - 'sort_words_by_vowel_count' is defined but NOT executed until called
    - 'sort_words_by_vowel_count' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 462:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def longest_word(lst)
      Returns a value via 'return' keyword
    def longest_word(lst)
      Returns a value via 'return' keyword
    def longest_word(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: longest_word, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'longest_w'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: longest_word(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: longest_word(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: longest_word(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'longest_word' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(longest_word(...))
  4. Define function 'longest_word' (stored in memory, not executed yet)
  5. Define function 'longest_word' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(longest_word(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'longest_word' is defined but NOT executed until called
    - 'longest_word' is defined but NOT executed until called
    - 'longest_word' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 463:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sum_of_tuple(lst)
      Returns a value via 'return' keyword
    def sum_of_tuple(lst)
      Returns a value via 'return' keyword
    def sum_of_tuple(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: print, sorted, sum_of_tuple
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sum_of_tuple(lst)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sum_of_tuple(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sum_of_tuple(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sum_of_tuple' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sum_of_tuple(...))
  4. Define function 'sum_of_tuple' (stored in memory, not executed yet)
  5. Define function 'sum_of_tuple' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sum_of_tuple(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sum_of_tuple' is defined but NOT executed until called
    - 'sum_of_tuple' is defined but NOT executed until called
    - 'sum_of_tuple' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 464:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def list_of_strings_lst(lst)
      Returns a value via 'return' keyword
    def list_of_strings_lst(lst)
      Returns a value via 'return' keyword
    def list_of_strings_lst(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, (), []
  Functions called: list_of_strings_lst, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: list_of_strings_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: list_of_strings_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: list_of_strings_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'list_of_strings_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(list_of_strings_lst(...))
  4. Define function 'list_of_strings_lst' (stored in memory, not executed yet)
  5. Define function 'list_of_strings_lst' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(list_of_strings_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'list_of_strings_lst' is defined but NOT executed until called
    - 'list_of_strings_lst' is defined but NOT executed until called
    - 'list_of_strings_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 465:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_number_uppercases(lst)
      Returns a value via 'return' keyword
    def sort_number_uppercases(lst)
      Returns a value via 'return' keyword
    def sort_number_uppercases(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., ()
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: len, print, sort_number_uppercases, sorted, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: char.isupper()
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_number_uppercases(lst)
  • Function call: sum(value)
  • Function call: char.isupper()
  • Function call: len(value)
  • Function call: char.isupper()
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_number_uppercases(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_number_uppercases(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_number_uppercases' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_number_uppercases(...))
  4. Define function 'sort_number_uppercases' (stored in memory, not executed yet)
  5. Define function 'sort_number_uppercases' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_number_uppercases(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_number_uppercases' is defined but NOT executed until called
    - 'sort_number_uppercases' is defined but NOT executed until called
    - 'sort_number_uppercases' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 466:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def tuple_largest_product(lst)
      Returns a value via 'return' keyword
    def tuple_largest_product(lst)
      Returns a value via 'return' keyword
    def tuple_largest_product(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ()
  Control structures: for loop iteration
  Functions called: max, print, tuple_largest_product
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: tuple_largest_product(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: tuple_largest_product(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: tuple_largest_product(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'tuple_largest_product' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(tuple_largest_product(...))
  4. Define function 'tuple_largest_product' (stored in memory, not executed yet)
  5. Define function 'tuple_largest_product' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(tuple_largest_product(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'tuple_largest_product' is defined but NOT executed until called
    - 'tuple_largest_product' is defined but NOT executed until called
    - 'tuple_largest_product' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 467:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def floating_point(numbers)
      Returns a value via 'return' keyword
    def floating_point(numbers)
      Returns a value via 'return' keyword
    def floating_point(numbers)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: floating_point, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: print(value)
  • Function call: floating_point(numbers)
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: sorted(numbers)
  • Assignment: right side evaluated first, then stored in 'numbers'
  • Function call: floating_point(numbers)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: floating_point(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'floating_point' (stored in memory, not executed yet)
  2. Assign value to variable: numbers
  3. Execute function call: print(floating_point(...))
  4. Define function 'floating_point' (stored in memory, not executed yet)
  5. Define function 'floating_point' (stored in memory, not executed yet)
  6. Assign value to variable: numbers
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(floating_point(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'floating_point' is defined but NOT executed until called
    - 'floating_point' is defined but NOT executed until called
    - 'floating_point' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 468:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def first_element_decending_order(lst)
      Returns a value via 'return' keyword
    def first_element_decending_order(lst)
      Returns a value via 'return' keyword
    def first_element_decending_order(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: (), []
  Functions called: first_element_decending_order, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: first_element_decending_order(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: first_element_decending_order(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: first_element_decending_order(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'first_element_decending_order' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(first_element_decending_order(...))
  4. Define function 'first_element_decending_order' (stored in memory, not executed yet)
  5. Define function 'first_element_decending_order' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(first_element_decending_order(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'first_element_decending_order' is defined but NOT executed until called
    - 'first_element_decending_order' is defined but NOT executed until called
    - 'first_element_decending_order' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 469:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def number_of_consonants(lst)
      Returns a value via 'return' keyword
    def number_of_consonants(lst)
      Returns a value via 'return' keyword
    def number_of_consonants(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: number_of_consonants, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'consonants_set'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: word.lower()
  • Condition branch: evaluates boolean expression first
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: number_of_consonants(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: number_of_consonants(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: number_of_consonants(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'number_of_consonants' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(number_of_consonants(...))
  4. Define function 'number_of_consonants' (stored in memory, not executed yet)
  5. Define function 'number_of_consonants' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(number_of_consonants(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'number_of_consonants' is defined but NOT executed until called
    - 'number_of_consonants' is defined but NOT executed until called
    - 'number_of_consonants' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over word.lower(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 470:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def min_string(lst)
      Returns a value via 'return' keyword
    def min_string(lst)
      Returns a value via 'return' keyword
    def min_string(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: min, min_string, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: min_string(lst)
  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: min_string(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: min_string(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'min_string' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(min_string(...))
  4. Define function 'min_string' (stored in memory, not executed yet)
  5. Define function 'min_string' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(min_string(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'min_string' is defined but NOT executed until called
    - 'min_string' is defined but NOT executed until called
    - 'min_string' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 471:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def length_of_value(lst, key)
      Returns a value via 'return' keyword
    def length_of_value(lst, key)
      Returns a value via 'return' keyword
    def length_of_value(lst, key)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: len, length_of_value, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(value)
  • Function call: x.get(key, '')
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: length_of_value(lst, 'b')
  • Assignment: right side evaluated first, then stored in 'sorted_lst'
  • Function call: print(sorted_lst)
  • Function call: len(value)
  • Function call: x.get(key, '')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(value)
  • Function call: x.get(key, '')
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'key'
  • Function call: length_of_value(lst, key)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: length_of_value(value, 'value')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'length_of_value' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: sorted_lst
  4. Execute function call: print(sorted_lst)
  5. Define function 'length_of_value' (stored in memory, not executed yet)
  6. Define function 'length_of_value' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: key
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(length_of_value(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'length_of_value' is defined but NOT executed until called
    - 'length_of_value' is defined but NOT executed until called
    - 'length_of_value' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 472:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def find_person(lst_dict, key)
      Returns a value via 'return' keyword
    def find_person(lst_dict, key)
      Returns a value via 'return' keyword
    def find_person(lst_dict, key)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: find_person, len, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(value)
  • Function call: d.get(key, '')
  • Function call: max(lst_dict)
  • Assignment: right side evaluated first, then stored in 'lst_dict'
  • Assignment: right side evaluated first, then stored in 'key'
  • Function call: print(value)
  • Function call: find_person(lst_dict, key)
  • Function call: len(value)
  • Function call: d.get(key, '')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(value)
  • Function call: d.get(key, '')
  • Assignment: right side evaluated first, then stored in 'lst_dict'
  • Assignment: right side evaluated first, then stored in 'key'
  • Function call: find_person(lst_dict, key)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: find_person(value, 'value')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'find_person' (stored in memory, not executed yet)
  2. Assign value to variable: lst_dict
  3. Assign value to variable: key
  4. Execute function call: print(find_person(...))
  5. Define function 'find_person' (stored in memory, not executed yet)
  6. Define function 'find_person' (stored in memory, not executed yet)
  7. Assign value to variable: lst_dict
  8. Assign value to variable: key
  9. Assign value to variable: result
  10. Execute function call: print(result)
  11. Execute function call: print(find_person(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'find_person' is defined but NOT executed until called
    - 'find_person' is defined but NOT executed until called
    - 'find_person' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 473:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 5
    def main(lst, divisor)
      Returns a value via 'return' keyword
    def remainder_key(x, divisor)
      Returns a value via 'return' keyword
    def sort_by_remainder(lst, divisor)
      Returns a value via 'return' keyword
    def main(lst, divisor)
      Returns a value via 'return' keyword
    def main(lst, divisor)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: %, *, -, (), //, []
  Functions called: divmod, main, print, remainder_key, sort_by_remainder, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: num % divisor
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: main(lst, divisor)
  • Binary operation: x % divisor
  • Function call: remainder_key(x, divisor)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: print(value)
  • Function call: sort_by_remainder(lst, divisor)
  • Binary operation: num - expression
  • Binary operation: divisor * expression
  • Binary operation: num // divisor
  • Function call: divmod(num, divisor)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'divisor'
  • Function call: main(lst, divisor)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: main(value, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'main' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: divisor
  4. Execute function call: main(lst, divisor)
  5. Define function 'remainder_key' (stored in memory, not executed yet)
  6. Define function 'sort_by_remainder' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: divisor
  9. Execute function call: print(sort_by_remainder(...))
  10. Define function 'main' (stored in memory, not executed yet)
  11. Define function 'main' (stored in memory, not executed yet)
  12. Assign value to variable: lst
  13. Assign value to variable: divisor
  14. Assign value to variable: result
  15. Execute function call: print(result)
  16. Execute function call: print(main(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'main' is defined but NOT executed until called
    - 'remainder_key' is defined but NOT executed until called
    - 'sort_by_remainder' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
    - 'main' is defined but NOT executed until called
  Runtime execution (top-level code):
    - main() is invoked
      → Execution jumps to the function body of 'main'
      → Function executes its statements sequentially
      → Returns result to the caller
      → Result is printed to console
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 474:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def year_key(date)
      Returns a value via 'return' keyword
    def sort_by_year(dates)
      Returns a value via 'return' keyword
    def year_key(date)
      Returns a value via 'return' keyword
    def year_key(date)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: print, sort_by_year, sorted, year_key
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: datetime.strptime(date, '%Y-%m-%d')
  • Function call: sorted(dates)
  • Function call: print(value)
  • Function call: sort_by_year(value)
  • Function call: datetime.strptime(date, '%Y-%m-%d')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: datetime.strptime(date, '%Y-%m-%d')
  • Assignment: right side evaluated first, then stored in 'date'
  • Function call: year_key(date)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: year_key(25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import from datetime: datetime
  2. Define function 'year_key' (stored in memory, not executed yet)
  3. Define function 'sort_by_year' (stored in memory, not executed yet)
  4. Execute function call: print(sort_by_year(...))
  5. Define function 'year_key' (stored in memory, not executed yet)
  6. Define function 'year_key' (stored in memory, not executed yet)
  7. Assign value to variable: date
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(year_key(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'year_key' is defined but NOT executed until called
    - 'sort_by_year' is defined but NOT executed until called
    - 'year_key' is defined but NOT executed until called
    - 'year_key' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 475:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_by_a_count(names)
      Returns a value via 'return' keyword
    def sort_by_a_count(names)
      Returns a value via 'return' keyword
    def sort_by_a_count(names)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., ()
  Functions called: print, sort_by_a_count, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: name.lower(...).count('a')
  • Function call: name.lower()
  • Function call: sorted(names)
  • Assignment: right side evaluated first, then stored in 'names'
  • Function call: sort_by_a_count(names)
  • Assignment: right side evaluated first, then stored in 'sorted_names'
  • Function call: print(sorted_names)
  • Function call: name.lower(...).count('a')
  • Function call: name.lower()
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: name.lower(...).count('a')
  • Function call: name.lower()
  • Assignment: right side evaluated first, then stored in 'names'
  • Function call: sort_by_a_count(names)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_by_a_count(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_by_a_count' (stored in memory, not executed yet)
  2. Assign value to variable: names
  3. Assign value to variable: sorted_names
  4. Execute function call: print(sorted_names)
  5. Define function 'sort_by_a_count' (stored in memory, not executed yet)
  6. Define function 'sort_by_a_count' (stored in memory, not executed yet)
  7. Assign value to variable: names
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sort_by_a_count(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_by_a_count' is defined but NOT executed until called
    - 'sort_by_a_count' is defined but NOT executed until called
    - 'sort_by_a_count' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 476:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_sqaure(lst)
      Returns a value via 'return' keyword
    def sort_sqaure(lst)
      Returns a value via 'return' keyword
    def sort_sqaure(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: pow, print, range, sort_sqaure, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x * x
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_sqaure(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(x)
  • Function call: pow(x, x)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_sqaure(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_sqaure(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_sqaure' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_sqaure(...))
  4. Define function 'sort_sqaure' (stored in memory, not executed yet)
  5. Define function 'sort_sqaure' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_sqaure(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_sqaure' is defined but NOT executed until called
    - 'sort_sqaure' is defined but NOT executed until called
    - 'sort_sqaure' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 477:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def closest_to_target(lst, target)
      Returns a value via 'return' keyword
    def closest_to_target(lst, target)
      Returns a value via 'return' keyword
    def closest_to_target(lst, target)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ()
  Functions called: abs, closest_to_target, min, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: x - target
  • Function call: abs(computed expression)
  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: closest_to_target(lst, target)
  • Assignment: right side evaluated first, then stored in 'closest_value'
  • Function call: print(closest_value)
  • Binary operation: x - target
  • Function call: abs(computed expression)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: x - target
  • Function call: abs(computed expression)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'target'
  • Function call: closest_to_target(lst, target)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: closest_to_target(value, 'goal')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'closest_to_target' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: target
  4. Assign value to variable: closest_value
  5. Execute function call: print(closest_value)
  6. Define function 'closest_to_target' (stored in memory, not executed yet)
  7. Define function 'closest_to_target' (stored in memory, not executed yet)
  8. Assign value to variable: lst
  9. Assign value to variable: target
  10. Assign value to variable: result
  11. Execute function call: print(result)
  12. Execute function call: print(closest_to_target(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'closest_to_target' is defined but NOT executed until called
    - 'closest_to_target' is defined but NOT executed until called
    - 'closest_to_target' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 478:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def length_of_first_element(lst)
      Returns a value via 'return' keyword
    def length_of_first_element(lst)
      Returns a value via 'return' keyword
    def length_of_first_element(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: (), []
  Functions called: len, length_of_first_element, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(value)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: length_of_first_element(lst)
  • Assignment: right side evaluated first, then stored in 'sorted_list'
  • Function call: print(sorted_list)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: length_of_first_element(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: length_of_first_element(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'length_of_first_element' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Assign value to variable: sorted_list
  4. Execute function call: print(sorted_list)
  5. Define function 'length_of_first_element' (stored in memory, not executed yet)
  6. Define function 'length_of_first_element' (stored in memory, not executed yet)
  7. Assign value to variable: lst
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(length_of_first_element(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'length_of_first_element' is defined but NOT executed until called
    - 'length_of_first_element' is defined but NOT executed until called
    - 'length_of_first_element' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 479:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def number_of_spaces(lst)
      Returns a value via 'return' keyword
    def number_of_spaces(lst)
      Returns a value via 'return' keyword
    def number_of_spaces(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), ==
  Control structures: if/else conditional branching; for loop iteration; generator expression
  Functions called: number_of_spaces, print, sorted, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: x.count(' ')
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: number_of_spaces(lst)
  • Function call: sum(value)
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: number_of_spaces(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: number_of_spaces(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'number_of_spaces' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(number_of_spaces(...))
  4. Define function 'number_of_spaces' (stored in memory, not executed yet)
  5. Define function 'number_of_spaces' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(number_of_spaces(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'number_of_spaces' is defined but NOT executed until called
    - 'number_of_spaces' is defined but NOT executed until called
    - 'number_of_spaces' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over x, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 480:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def most_vowels(lst)
      Returns a value via 'return' keyword
    def most_vowels(lst)
      Returns a value via 'return' keyword
    def most_vowels(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: (), in
  Control structures: if/else conditional branching; for loop iteration; list comprehension; generator expression
  Functions called: len, max, most_vowels, print, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'vowels'
  • Assignment: right side evaluated first, then stored in 'count'
  • Condition branch: evaluates boolean expression first
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: most_vowels(lst)
  • Function call: sum(value)
  • Function call: len(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: most_vowels(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: most_vowels(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'most_vowels' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(most_vowels(...))
  4. Define function 'most_vowels' (stored in memory, not executed yet)
  5. Define function 'most_vowels' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(most_vowels(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'most_vowels' is defined but NOT executed until called
    - 'most_vowels' is defined but NOT executed until called
    - 'most_vowels' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over word, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 481:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_lst_integers(lst)
      Returns a value via 'return' keyword
    def sort_lst_integers(lst)
      Returns a value via 'return' keyword
    def sort_lst_integers(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: abs, len, print, sort_lst_integers, sorted, str
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(value)
  • Function call: str(value)
  • Function call: abs(n)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_lst_integers(lst)
  • Function call: len(value)
  • Function call: str(value)
  • Function call: abs(n)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(value)
  • Function call: str(value)
  • Function call: abs(n)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_lst_integers(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_lst_integers(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_lst_integers' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_lst_integers(...))
  4. Define function 'sort_lst_integers' (stored in memory, not executed yet)
  5. Define function 'sort_lst_integers' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_lst_integers(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_lst_integers' is defined but NOT executed until called
    - 'sort_lst_integers' is defined but NOT executed until called
    - 'sort_lst_integers' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 482:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_tuples_by_difference(lst)
      Returns a value via 'return' keyword
    def sort_tuples_by_difference(lst)
      Returns a value via 'return' keyword
    def sort_tuples_by_difference(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, (), []
  Functions called: abs, print, sort_tuples_by_difference, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: expression - expression
  • Function call: abs(computed expression)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_tuples_by_difference(lst)
  • Binary operation: expression - expression
  • Function call: abs(computed expression)
  • Assignment: right side evaluated first, then stored in 'result'
  • Binary operation: expression - expression
  • Function call: abs(computed expression)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_tuples_by_difference(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_tuples_by_difference(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_tuples_by_difference' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_tuples_by_difference(...))
  4. Define function 'sort_tuples_by_difference' (stored in memory, not executed yet)
  5. Define function 'sort_tuples_by_difference' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_tuples_by_difference(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_tuples_by_difference' is defined but NOT executed until called
    - 'sort_tuples_by_difference' is defined but NOT executed until called
    - 'sort_tuples_by_difference' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 483:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_strings_by_ascii(lst)
      Returns a value via 'return' keyword
    def sort_strings_by_ascii(lst)
      Returns a value via 'return' keyword
    def sort_strings_by_ascii(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: (), []
  Functions called: ord, print, sort_strings_by_ascii, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: ord(value)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_strings_by_ascii(lst)
  • Function call: ord(value)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: ord(value)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_strings_by_ascii(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_strings_by_ascii(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_strings_by_ascii' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_strings_by_ascii(...))
  4. Define function 'sort_strings_by_ascii' (stored in memory, not executed yet)
  5. Define function 'sort_strings_by_ascii' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_strings_by_ascii(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_strings_by_ascii' is defined but NOT executed until called
    - 'sort_strings_by_ascii' is defined but NOT executed until called
    - 'sort_strings_by_ascii' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 484:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_lists_by_sum(lst)
      Returns a value via 'return' keyword
    def sort_lists_by_sum(lst)
      Returns a value via 'return' keyword
    def sort_lists_by_sum(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: ., ()
  Control structures: for loop iteration
  Functions called: print, reduce, sort_lists_by_sum, sorted, sum
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sum(sublist)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_lists_by_sum(lst)
  • Assignment: right side evaluated first, then stored in 'total'
  • Function call: reduce(value, sublist, 0)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_lists_by_sum(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_lists_by_sum(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_lists_by_sum' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_lists_by_sum(...))
  4. Define function 'sort_lists_by_sum' (stored in memory, not executed yet)
  5. Define function 'sort_lists_by_sum' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_lists_by_sum(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_lists_by_sum' is defined but NOT executed until called
    - 'sort_lists_by_sum' is defined but NOT executed until called
    - 'sort_lists_by_sum' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over sublist, one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 485:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def number_of_consonants(lst)
      Returns a value via 'return' keyword
    def number_of_consonants(lst)
      Returns a value via 'return' keyword
    def number_of_consonants(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: ., (), in
  Control structures: if/else conditional branching; for loop iteration
  Functions called: min, number_of_consonants, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Assignment: right side evaluated first, then stored in 'consonants_set'
  • Assignment: right side evaluated first, then stored in 'count'
  • Function call: word.lower()
  • Condition branch: evaluates boolean expression first
  • Function call: min(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: number_of_consonants(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: number_of_consonants(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: number_of_consonants(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'number_of_consonants' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(number_of_consonants(...))
  4. Define function 'number_of_consonants' (stored in memory, not executed yet)
  5. Define function 'number_of_consonants' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(number_of_consonants(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'number_of_consonants' is defined but NOT executed until called
    - 'number_of_consonants' is defined but NOT executed until called
    - 'number_of_consonants' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over word.lower(...), one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 486:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_files_by_extension(lst)
      Returns a value via 'return' keyword
    def sort_files_by_extension(lst)
      Returns a value via 'return' keyword
    def sort_files_by_extension(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: -, ., (), []
  Functions called: print, sort_files_by_extension, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: filename.split('.')
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_files_by_extension(lst)
  • Function call: filename.split('.')
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: filename.split('.')
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_files_by_extension(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_files_by_extension(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_files_by_extension' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_files_by_extension(...))
  4. Define function 'sort_files_by_extension' (stored in memory, not executed yet)
  5. Define function 'sort_files_by_extension' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_files_by_extension(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_files_by_extension' is defined but NOT executed until called
    - 'sort_files_by_extension' is defined but NOT executed until called
    - 'sort_files_by_extension' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 487:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sqaure_numbers_lst(lst)
      Returns a value via 'return' keyword
    def sqaure_numbers_lst(lst)
      Returns a value via 'return' keyword
    def sqaure_numbers_lst(lst)
      Returns a value via 'return' keyword
  Keywords used: def, for, return
  Operators used: *, ()
  Control structures: for loop iteration
  Functions called: pow, print, range, sorted, sqaure_numbers_lst
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: n * n
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sqaure_numbers_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: range(n)
  • Function call: pow(n, n)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sqaure_numbers_lst(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sqaure_numbers_lst(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sqaure_numbers_lst' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sqaure_numbers_lst(...))
  4. Define function 'sqaure_numbers_lst' (stored in memory, not executed yet)
  5. Define function 'sqaure_numbers_lst' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sqaure_numbers_lst(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sqaure_numbers_lst' is defined but NOT executed until called
    - 'sqaure_numbers_lst' is defined but NOT executed until called
    - 'sqaure_numbers_lst' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over range(...), one element at a time
    - Body executes once per element, then loop exits
"""

"""
Problem 488:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_words_by_distinct_letters(words)
      Returns a value via 'return' keyword
    def sort_words_by_distinct_letters(words)
      Returns a value via 'return' keyword
    def sort_words_by_distinct_letters(words)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: len, print, set, sort_words_by_distinct_letters, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: len(value)
  • Function call: set(word)
  • Function call: sorted(words)
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: print(value)
  • Function call: sort_words_by_distinct_letters(words)
  • Function call: len(value)
  • Function call: set(word)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: len(value)
  • Function call: set(word)
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: sort_words_by_distinct_letters(words)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_words_by_distinct_letters(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_words_by_distinct_letters' (stored in memory, not executed yet)
  2. Assign value to variable: words
  3. Execute function call: print(sort_words_by_distinct_letters(...))
  4. Define function 'sort_words_by_distinct_letters' (stored in memory, not executed yet)
  5. Define function 'sort_words_by_distinct_letters' (stored in memory, not executed yet)
  6. Assign value to variable: words
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_words_by_distinct_letters(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_words_by_distinct_letters' is defined but NOT executed until called
    - 'sort_words_by_distinct_letters' is defined but NOT executed until called
    - 'sort_words_by_distinct_letters' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 489:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_second_letter(lst)
      Returns a value via 'return' keyword
    def sort_second_letter(lst)
      Returns a value via 'return' keyword
    def sort_second_letter(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: (), []
  Functions called: print, sort_second_letter, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_second_letter(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_second_letter(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_second_letter(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_second_letter' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_second_letter(...))
  4. Define function 'sort_second_letter' (stored in memory, not executed yet)
  5. Define function 'sort_second_letter' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_second_letter(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_second_letter' is defined but NOT executed until called
    - 'sort_second_letter' is defined but NOT executed until called
    - 'sort_second_letter' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 490:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def people_age(dic)
      Returns a value via 'return' keyword
    def people_age(dic)
      Returns a value via 'return' keyword
    def people_age(dic)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: (), []
  Functions called: people_age, print, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(dic)
  • Assignment: right side evaluated first, then stored in 'people'
  • Function call: print(value)
  • Function call: people_age(people)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dic'
  • Function call: people_age(dic)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: people_age(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'people_age' (stored in memory, not executed yet)
  2. Assign value to variable: people
  3. Execute function call: print(people_age(...))
  4. Define function 'people_age' (stored in memory, not executed yet)
  5. Define function 'people_age' (stored in memory, not executed yet)
  6. Assign value to variable: dic
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(people_age(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'people_age' is defined but NOT executed until called
    - 'people_age' is defined but NOT executed until called
    - 'people_age' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 491:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_by_binary_representation(lst)
      Returns a value via 'return' keyword
    def sort_by_binary_representation(lst)
      Returns a value via 'return' keyword
    def sort_by_binary_representation(lst)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: bin, print, sort_by_binary_representation, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: bin(x)
  • Function call: sorted(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: sort_by_binary_representation(lst)
  • Function call: bin(x)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: bin(x)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: sort_by_binary_representation(lst)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_by_binary_representation(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_by_binary_representation' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(sort_by_binary_representation(...))
  4. Define function 'sort_by_binary_representation' (stored in memory, not executed yet)
  5. Define function 'sort_by_binary_representation' (stored in memory, not executed yet)
  6. Assign value to variable: lst
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_by_binary_representation(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_by_binary_representation' is defined but NOT executed until called
    - 'sort_by_binary_representation' is defined but NOT executed until called
    - 'sort_by_binary_representation' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 492:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_keys_by_value(dic)
      Returns a value via 'return' keyword
    def sort_keys_by_value(dic)
      Returns a value via 'return' keyword
    def sort_keys_by_value(dic)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ., (), []
  Functions called: print, sort_keys_by_value, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: sorted(value)
  • Function call: dic.keys()
  • Assignment: right side evaluated first, then stored in 'dic'
  • Function call: print(value)
  • Function call: sort_keys_by_value(dic)
  • Assignment: right side evaluated first, then stored in 'result'
  • Assignment: right side evaluated first, then stored in 'dic'
  • Function call: sort_keys_by_value(dic)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_keys_by_value(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'sort_keys_by_value' (stored in memory, not executed yet)
  2. Assign value to variable: dic
  3. Execute function call: print(sort_keys_by_value(...))
  4. Define function 'sort_keys_by_value' (stored in memory, not executed yet)
  5. Define function 'sort_keys_by_value' (stored in memory, not executed yet)
  6. Assign value to variable: dic
  7. Assign value to variable: result
  8. Execute function call: print(result)
  9. Execute function call: print(sort_keys_by_value(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_keys_by_value' is defined but NOT executed until called
    - 'sort_keys_by_value' is defined but NOT executed until called
    - 'sort_keys_by_value' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 493:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 3
    def sort_fractions_by_decimal(fractions)
      Returns a value via 'return' keyword
    def sort_fractions_by_decimal(fractions)
      Returns a value via 'return' keyword
    def sort_fractions_by_decimal(fractions)
      Returns a value via 'return' keyword
  Keywords used: def, return
  Operators used: ()
  Functions called: Fraction, float, print, sort_fractions_by_decimal, sorted
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: float(fraction)
  • Function call: sorted(fractions)
  • Function call: Fraction(1, 3)
  • Function call: Fraction(3, 4)
  • Function call: Fraction(2, 5)
  • Function call: Fraction(7, 10)
  • Assignment: right side evaluated first, then stored in 'fractions'
  • Function call: print(value)
  • Function call: sort_fractions_by_decimal(fractions)
  • Function call: float(fraction)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: float(fraction)
  • Assignment: right side evaluated first, then stored in 'fractions'
  • Function call: sort_fractions_by_decimal(fractions)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: sort_fractions_by_decimal(value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Import from fractions: Fraction
  2. Define function 'sort_fractions_by_decimal' (stored in memory, not executed yet)
  3. Assign value to variable: fractions
  4. Execute function call: print(sort_fractions_by_decimal(...))
  5. Define function 'sort_fractions_by_decimal' (stored in memory, not executed yet)
  6. Define function 'sort_fractions_by_decimal' (stored in memory, not executed yet)
  7. Assign value to variable: fractions
  8. Assign value to variable: result
  9. Execute function call: print(result)
  10. Execute function call: print(sort_fractions_by_decimal(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'sort_fractions_by_decimal' is defined but NOT executed until called
    - 'sort_fractions_by_decimal' is defined but NOT executed until called
    - 'sort_fractions_by_decimal' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 494:
▸ SYNTAX HIGHLIGHTS

  Functions defined: 4
    def longest_string_letter(lst, letter)
      Returns a value via 'return' keyword
    def longest_string(string, letter)
      Returns a value via 'return' keyword
    def longest_string_letter(lst, letter)
      Returns a value via 'return' keyword
    def longest_string_letter(lst, letter)
      Returns a value via 'return' keyword
  Keywords used: def, for, if/elif/else, return
  Operators used: -, ., >, ()
  Control structures: for loop iteration
  Functions called: len, letter_ends, longest_string, longest_string_letter, max, print
  Output: uses print() to display results
  Data structures: uses Python collections (list, tuple, dict, set)

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: s.endswith(letter)
  • Function call: len(s)
  • Function call: max(lst)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Function call: print(value)
  • Function call: longest_string_letter(lst, 'n')
  • Function call: print(value)
  • Function call: longest_string_letter(lst, 'a')
  • Function call: string.split()
  • Assignment: right side evaluated first, then stored in 'words'
  • Function call: word.endswith(letter)
  • Condition branch: evaluates boolean expression first
  • Function call: len(word)
  • Assignment: right side evaluated first, then stored in 'longest'
  • Assignment: right side evaluated first, then stored in 'max_length'
  • Function call: letter_ends(word)
  • Assignment: right side evaluated first, then stored in 'length'
  • Condition branch: evaluates boolean expression first
  • Assignment: right side evaluated first, then stored in 'max_length'
  • Assignment: right side evaluated first, then stored in 'longest'
  • Assignment: right side evaluated first, then stored in 'input_string'
  • Assignment: right side evaluated first, then stored in 'letter'
  • Function call: print(value)
  • Function call: longest_string(input_string, letter)
  • Function call: s.endswith(letter)
  • Function call: len(s)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: s.endswith(letter)
  • Function call: len(s)
  • Assignment: right side evaluated first, then stored in 'lst'
  • Assignment: right side evaluated first, then stored in 'letter'
  • Function call: longest_string_letter(lst, letter)
  • Assignment: right side evaluated first, then stored in 'result'
  • Function call: print(result)
  • Function call: print(value)
  • Function call: longest_string_letter(value, value)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define function 'longest_string_letter' (stored in memory, not executed yet)
  2. Assign value to variable: lst
  3. Execute function call: print(longest_string_letter(...))
  4. Execute function call: print(longest_string_letter(...))
  5. Define function 'longest_string' (stored in memory, not executed yet)
  6. Assign value to variable: input_string
  7. Assign value to variable: letter
  8. Execute function call: print(longest_string(...))
  9. Define function 'longest_string_letter' (stored in memory, not executed yet)
  10. Define function 'longest_string_letter' (stored in memory, not executed yet)
  11. Assign value to variable: lst
  12. Assign value to variable: letter
  13. Assign value to variable: result
  14. Execute function call: print(result)
  15. Execute function call: print(longest_string_letter(...))

▸ EXECUTION FLOW

  Function definition phase:
    - 'longest_string_letter' is defined but NOT executed until called
    - 'longest_string' is defined but NOT executed until called
    - 'longest_string_letter' is defined but NOT executed until called
    - 'longest_string_letter' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
  Loop execution flow:
    - For loop iterates over words, one element at a time
    - Body executes once per element, then loop exits
  Conditional flow:
    - If condition is evaluated as True → enters the if-block
    - If condition is False → skips to else/elif or continues
"""

"""
Problem 495:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Animal, Dog, Cat
    Animal methods: speak
    Dog methods: speak
    Cat methods: speak
  Functions defined: 2
    def solve()
      Returns a value via 'return' keyword
    def solve()
      Returns a value via 'return' keyword
  Keywords used: class, def, return
  Operators used: ., ()
  Functions called: Animal, Cat, Dog, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: Dog()
  • Assignment: right side evaluated first, then stored in 'dog'
  • Function call: Cat()
  • Assignment: right side evaluated first, then stored in 'cat'
  • Function call: print(value)
  • Function call: dog.speak()
  • Function call: print(value)
  • Function call: cat.speak()
  • Function call: Dog()
  • Assignment: right side evaluated first, then stored in 'dog'
  • Function call: Cat()
  • Assignment: right side evaluated first, then stored in 'cat'
  • Function call: print(value)
  • Function call: dog.speak()
  • Function call: print(value)
  • Function call: cat.speak()
  • Function call: Dog()
  • Assignment: right side evaluated first, then stored in 'dog_2'
  • Function call: Cat()
  • Assignment: right side evaluated first, then stored in 'cat_2'
  • Function call: print(value)
  • Function call: dog_2.speak()
  • Function call: print(value)
  • Function call: cat_2.speak()
  • Function call: Animal()
  • Assignment: right side evaluated first, then stored in 'obj'
  • Function call: print(obj)
  • Function call: print(value)
  • Function call: Animal()

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define class 'Animal'
  2. Define class 'Dog'
  3. Define class 'Cat'
  4. Assign value to variable: dog
  5. Assign value to variable: cat
  6. Execute function call: print(dog.speak(...))
  7. Execute function call: print(cat.speak(...))
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Define function 'solve' (stored in memory, not executed yet)
  10. Assign value to variable: obj
  11. Execute function call: print(obj)
  12. Execute function call: print(Animal(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Animal' is defined with methods: speak
      • 'speak' is a regular method — runs when called on an instance
    - Class 'Dog' is defined with methods: speak
      • 'speak' is a regular method — runs when called on an instance
    - Class 'Cat' is defined with methods: speak
      • 'speak' is a regular method — runs when called on an instance
  Function definition phase:
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 496:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Vehicle, Car
    Vehicle methods: __init__
    Car methods: __init__
  Functions defined: 2
    def solve()
    def solve()
      Returns a value via 'return' keyword
  Keywords used: class, def, return
  Operators used: ., ()
  Functions called: Car, Vehicle, print, super
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: super(...).__init__(speed, color)
  • Function call: super()
  • Function call: Car(120, 'Red', 'Toyota')
  • Assignment: right side evaluated first, then stored in 'my_car'
  • Function call: print(value)
  • Function call: super(...).__init__(speed, color)
  • Function call: super()
  • Function call: Car(120, 'Red', 'Toyota')
  • Assignment: right side evaluated first, then stored in 'my_car'
  • Function call: print(value)
  • Function call: super(...).__init__(speed, color)
  • Function call: super()
  • Function call: Car(120, 'Red', 'Toyota')
  • Assignment: right side evaluated first, then stored in 'my_car'
  • Function call: Vehicle(15, 15)
  • Assignment: right side evaluated first, then stored in 'obj'
  • Function call: print(obj)
  • Function call: print(value)
  • Function call: Vehicle(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define class 'Vehicle'
  2. Define class 'Car'
  3. Assign value to variable: my_car
  4. Execute function call: print(value)
  5. Define function 'solve' (stored in memory, not executed yet)
  6. Define function 'solve' (stored in memory, not executed yet)
  7. Assign value to variable: obj
  8. Execute function call: print(obj)
  9. Execute function call: print(Vehicle(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Vehicle' is defined with methods: __init__
      • __init__ runs automatically on instantiation to initialize attributes
    - Class 'Car' is defined with methods: __init__
      • __init__ runs automatically on instantiation to initialize attributes
  Function definition phase:
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 497:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Person, Student
    Person methods: __init__, introduce
    Student methods: __init__, introduce
  Functions defined: 2
    def solve()
      Returns a value via 'return' keyword
    def solve()
      Returns a value via 'return' keyword
  Keywords used: class, def, return
  Operators used: ., ()
  Functions called: Person, Student, print, super
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: super(...).__init__(name)
  • Function call: super()
  • Function call: Person('Alice')
  • Assignment: right side evaluated first, then stored in 'person'
  • Function call: print(value)
  • Function call: person.introduce()
  • Function call: Student('Bob', 10)
  • Assignment: right side evaluated first, then stored in 'student'
  • Function call: print(value)
  • Function call: student.introduce()
  • Function call: super(...).__init__(name)
  • Function call: super()
  • Function call: Person('Alice')
  • Assignment: right side evaluated first, then stored in 'person'
  • Function call: print(value)
  • Function call: person.introduce()
  • Function call: Student('Bob', 10)
  • Assignment: right side evaluated first, then stored in 'student'
  • Function call: print(value)
  • Function call: student.introduce()
  • Function call: super(...).__init__(name)
  • Function call: super()
  • Function call: Person('Alice')
  • Assignment: right side evaluated first, then stored in 'person_2'
  • Function call: print(value)
  • Function call: person_2.introduce()
  • Function call: Student('Bob', 10)
  • Assignment: right side evaluated first, then stored in 'student_2'
  • Function call: print(value)
  • Function call: student_2.introduce()
  • Function call: Person('Alice')
  • Assignment: right side evaluated first, then stored in 'obj'
  • Function call: print(obj)
  • Function call: print(value)
  • Function call: Person('Bob')

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define class 'Person'
  2. Define class 'Student'
  3. Assign value to variable: person
  4. Execute function call: print(person.introduce(...))
  5. Assign value to variable: student
  6. Execute function call: print(student.introduce(...))
  7. Define function 'solve' (stored in memory, not executed yet)
  8. Define function 'solve' (stored in memory, not executed yet)
  9. Assign value to variable: obj
  10. Execute function call: print(obj)
  11. Execute function call: print(Person(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Person' is defined with methods: __init__, introduce
      • __init__ runs automatically on instantiation to initialize attributes
      • 'introduce' is a regular method — runs when called on an instance
    - Class 'Student' is defined with methods: __init__, introduce
      • __init__ runs automatically on instantiation to initialize attributes
      • 'introduce' is a regular method — runs when called on an instance
  Function definition phase:
    - 'solve' is defined but NOT executed until called
    - 'solve' is defined but NOT executed until called
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 498:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Shape, Rectangle
    Shape methods: area
    Rectangle methods: __init__, area
  Keywords used: class, def, pass, return
  Operators used: *, ., ()
  Functions called: Rectangle, Shape, print
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Binary operation: self.length * self.width
  • Function call: Rectangle(5, 3)
  • Assignment: right side evaluated first, then stored in 'rectangle'
  • Function call: print(value)
  • Function call: rectangle.area()
  • Function call: Shape(15, 15)
  • Assignment: right side evaluated first, then stored in 'obj'
  • Function call: print(obj)
  • Function call: print(value)
  • Function call: Shape(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define class 'Shape'
  2. Define class 'Rectangle'
  3. Assign value to variable: rectangle
  4. Execute function call: print(value)
  5. Assign value to variable: obj
  6. Execute function call: print(obj)
  7. Execute function call: print(Shape(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Shape' is defined with methods: area
      • 'area' is a regular method — runs when called on an instance
    - Class 'Rectangle' is defined with methods: __init__, area
      • __init__ runs automatically on instantiation to initialize attributes
      • 'area' is a regular method — runs when called on an instance
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 499:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Appliance, WashingMachine
    Appliance methods: __init__
    WashingMachine methods: __init__, wash_clothes
  Keywords used: class, def, return
  Operators used: ., ()
  Functions called: Appliance, WashingMachine, print, super
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: super(...).__init__(brand, power)
  • Function call: super()
  • Function call: WashingMachine('LG', 1500)
  • Assignment: right side evaluated first, then stored in 'washing_machine'
  • Function call: print(value)
  • Function call: washing_machine.wash_clothes()
  • Function call: Appliance(15, 15)
  • Assignment: right side evaluated first, then stored in 'obj'
  • Function call: print(obj)
  • Function call: print(value)
  • Function call: Appliance(25, 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define class 'Appliance'
  2. Define class 'WashingMachine'
  3. Assign value to variable: washing_machine
  4. Execute function call: print(washing_machine.wash_clothes(...))
  5. Assign value to variable: obj
  6. Execute function call: print(obj)
  7. Execute function call: print(Appliance(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Appliance' is defined with methods: __init__
      • __init__ runs automatically on instantiation to initialize attributes
    - Class 'WashingMachine' is defined with methods: __init__, wash_clothes
      • __init__ runs automatically on instantiation to initialize attributes
      • 'wash_clothes' is a regular method — runs when called on an instance
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

"""
Problem 500:
▸ SYNTAX HIGHLIGHTS

  Classes defined: Employee, Manager
    Employee methods: __init__
    Manager methods: __init__, assign_task
  Keywords used: class, def, return
  Operators used: ., ()
  Functions called: Employee, Manager, print, super
  Output: uses print() to display results

▸ EVALUATION ORDER

  Python evaluates expressions using left-to-right, inner-to-outer rules:

  • Function call: super(...).__init__(name, salary)
  • Function call: super()
  • Function call: Manager('John Doe', 75000)
  • Assignment: right side evaluated first, then stored in 'manager'
  • Function call: print(value)
  • Function call: manager.assign_task('Prepare a project report')
  • Function call: Employee('Alice', 15)
  • Assignment: right side evaluated first, then stored in 'obj'
  • Function call: print(obj)
  • Function call: print(value)
  • Function call: Employee('Bob', 25)

▸ EXECUTION ORDER

  Statement execution sequence (top to bottom):
  1. Define class 'Employee'
  2. Define class 'Manager'
  3. Assign value to variable: manager
  4. Execute function call: print(manager.assign_task(...))
  5. Assign value to variable: obj
  6. Execute function call: print(obj)
  7. Execute function call: print(Employee(...))

▸ EXECUTION FLOW

  Class definition phase:
    - Class 'Employee' is defined with methods: __init__
      • __init__ runs automatically on instantiation to initialize attributes
    - Class 'Manager' is defined with methods: __init__, assign_task
      • __init__ runs automatically on instantiation to initialize attributes
      • 'assign_task' is a regular method — runs when called on an instance
  Runtime execution (top-level code):
    - print() is invoked
    - print() is invoked
    - print() is invoked
"""

