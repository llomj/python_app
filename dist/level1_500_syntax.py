# Syntax documentation for level 1 (problems 1-500)
# Auto-generated by gen_syntax_files.py

"""
Problem 1:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_number(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_number(5, 5) runs first → then print() outputs the result
#   add_num(5, 5) runs first → then print() outputs the result
#   add_number(a, b) → each argument is evaluated first, then the function body runs
#   add_number(20, 30) runs first → then print() outputs the result
#   a + b → Python looks up a, then b, then applies +
#   add_number(5, 5) → each argument is evaluated first, then the function body runs
#   add_num(5, 5) → each argument is evaluated first, then the function body runs
#   add_number(20, 30) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_number(a, b) → stored in memory, not executed
#   2. print(add_number(...))
#      → calls add_number(a, b) with a=5, b=5
#      → 2.1 import operator
#      → 2.2 return operator.add(a, b)
#   3. def add_num(a, b) → stored in memory, not executed
#   4. print(add_num(...))
#      → calls add_num(a, b) with a=5, b=5
#      → 4.1 result = a + b
#      → 4.2 return result
#   5. def add_number(a, b) → stored in memory, not executed
#   6. def add_number(a, b) → stored in memory, not executed
#   7. a = ... → assigns value to variable
#   8. b = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(add_number(...))
#      → calls add_number(a, b) with a=20, b=30
#      → 11.1 import operator
#      → 11.2 return operator.add(a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_number, add_num) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(add_number(...)) → add_number() runs first → value passed to print()
#     print(add_num(...)) → add_num() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_number(...)) → add_number() runs first → value passed to print()

"""

"""
Problem 2:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_numbers(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * >=
# Built-in functions: abs, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_numbers(5, 5) runs first → then print() outputs the result
#   multiply_numbers(3, 7) runs first → then print() outputs the result
#   multiply_numbers(4, 6) runs first → then print() outputs the result
#   multiply_numbers(a, b) → each argument is evaluated first, then the function body runs
#   multiply_numbers(20, 30) runs first → then print() outputs the result
#   a * b → Python looks up a, then b, then applies *
#   multiply_numbers(5, 5) → each argument is evaluated first, then the function body runs
#   multiply_numbers(3, 7) → each argument is evaluated first, then the function body runs
#   multiply_numbers(4, 6) → each argument is evaluated first, then the function body runs
#   multiply_numbers(20, 30) → each argument is evaluated first, then the function body runs
#   b >= 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_numbers(a, b) → stored in memory, not executed
#   2. print(multiply_numbers(...))
#      → calls multiply_numbers(a, b) with a=5, b=5
#      → 2.1 return pow(a, b)
#   3. def multiply_numbers(a, b) → stored in memory, not executed
#   4. print(multiply_numbers(...))
#      → calls multiply_numbers(a, b) with a=3, b=7
#      → 4.1 return pow(a, b)
#   5. def multiply_numbers(a, b) → stored in memory, not executed
#   6. print(multiply_numbers(...))
#      → calls multiply_numbers(a, b) with a=4, b=6
#      → 6.1 return pow(a, b)
#   7. def multiply_numbers(a, b) → stored in memory, not executed
#   8. def multiply_numbers(a, b) → stored in memory, not executed
#   9. a = ... → assigns value to variable
#   10. b = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(multiply_numbers(...))
#      → calls multiply_numbers(a, b) with a=20, b=30
#      → 13.1 return pow(a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(multiply_numbers(...)) → multiply_numbers() runs first → value passed to print()
#     print(multiply_numbers(...)) → multiply_numbers() runs first → value passed to print()
#     print(multiply_numbers(...)) → multiply_numbers() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_numbers(...)) → multiply_numbers() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 3:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_max(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_max(result) runs first → then print() outputs the result
#   find_max(numbers_list) runs first → then print() outputs the result
#   find_max(number) → each argument is evaluated first, then the function body runs
#   find_max(?) runs first → then print() outputs the result
#   find_max(result) → each argument is evaluated first, then the function body runs
#   find_max(numbers_list) → each argument is evaluated first, then the function body runs
#   find_max(?) → each argument is evaluated first, then the function body runs
#   num > max_number → evaluated as True or False (determines which branch runs)
#   item > result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_max(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(find_max(...))
#      → calls find_max(number) with number=result
#      → 3.1 result = number[0]
#      → 3.2 for item in number:
#      → 3.2     if item > result:
#      → 3.2         result = item
#      → 3.3 return result
#   4. def find_max(numbers) → stored in memory, not executed
#   5. numbers_list = ... → assigns value to variable
#   6. print('The maximum number in the list is:', find_max(...))
#   7. def find_max(number) → stored in memory, not executed
#   8. def find_max(number) → stored in memory, not executed
#   9. number = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_max(...))
#      → calls find_max(number) with number=[4, 1, 8, 6, 3]
#      → 12.1 result = number[0]
#      → 12.2 for item in number:
#      → 12.2     if item > result:
#      → 12.2         result = item
#      → 12.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(find_max(...)) → find_max() runs first → value passed to print()
#     numbers_list = ... → stored in memory
#     print('The maximum number in the list is:', find_max(...))
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_max(...)) → find_max() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 4:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_average(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: /
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_average(lst) runs first → then print() outputs the result
#   cal_average(lst) runs first → then print() outputs the result
#   calculate_average(number) → each argument is evaluated first, then the function body runs
#   calculate_average(?) runs first → then print() outputs the result
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   calculate_average(lst) → each argument is evaluated first, then the function body runs
#   cal_average(lst) → each argument is evaluated first, then the function body runs
#   calculate_average(?) → each argument is evaluated first, then the function body runs
#   total / count → Python looks up total, then count, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_average(number) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(calculate_average(...))
#      → calls calculate_average(number) with number=lst
#      → 3.1 total = 0
#      → 3.2 count = 0
#      → 3.3 for item in number:
#      → 3.3     total += item
#      → 3.3     count += 1
#      → 3.4 return total / count if count else 0
#   4. def cal_average(number) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(cal_average(...))
#      → calls cal_average(number) with number=lst
#      → 6.1 result = sum(number) / len(number)
#      → 6.2 return result
#   7. def calculate_average(number) → stored in memory, not executed
#   8. def calculate_average(number) → stored in memory, not executed
#   9. number = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(calculate_average(...))
#      → calls calculate_average(number) with number=[4, 1, 8, 6, 3]
#      → 12.1 total = 0
#      → 12.2 count = 0
#      → 12.3 for item in number:
#      → 12.3     total += item
#      → 12.3     count += 1
#      → 12.4 return total / count if count else 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (cal_average, calculate_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(calculate_average(...)) → calculate_average() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(cal_average(...)) → cal_average() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_average(...)) → calculate_average() runs first → value passed to print()
#
#   Loop: takes one item from number each round, runs body, repeats until done

"""

"""
Problem 5:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels(result) runs first → then print() outputs the result
#   count_vowels('Jonathan') runs first → then print() outputs the result
#   count_vowels('Python') runs first → then print() outputs the result
#   count_vowels(string) → each argument is evaluated first, then the function body runs
#   count_vowels('world') runs first → then print() outputs the result
#   count_vowels(result) → each argument is evaluated first, then the function body runs
#   count_vowels('Jonathan') → each argument is evaluated first, then the function body runs
#   count_vowels('Python') → each argument is evaluated first, then the function body runs
#   count_vowels('world') → each argument is evaluated first, then the function body runs
#   char in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(string) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(count_vowels(...))
#      → calls count_vowels(string) with string=result
#      → 3.1 return len([char for char in string if char in 'aeiou'])
#   4. def count_vowels(string) → stored in memory, not executed
#   5. print(count_vowels(...))
#      → calls count_vowels(string) with string='Jonathan'
#      → 5.1 return len([char for char in string if char in 'aeiou'])
#   6. def count_vowels(string) → stored in memory, not executed
#   7. print(count_vowels(...))
#      → calls count_vowels(string) with string='Python'
#      → 7.1 return len([char for char in string if char in 'aeiou'])
#   8. def count_vowels(string) → stored in memory, not executed
#   9. def count_vowels(string) → stored in memory, not executed
#   10. string = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_vowels(...))
#      → calls count_vowels(string) with string='world'
#      → 13.1 return len([char for char in string if char in 'aeiou'])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 6:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_string(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_string(result) runs first → then print() outputs the result
#   reverse_string('hello') runs first → then print() outputs the result
#   reverse_string('Python') runs first → then print() outputs the result
#   reverse_string(string) → each argument is evaluated first, then the function body runs
#   reverse_string('world') runs first → then print() outputs the result
#   reverse_string(result) → each argument is evaluated first, then the function body runs
#   reverse_string('hello') → each argument is evaluated first, then the function body runs
#   reverse_string('Python') → each argument is evaluated first, then the function body runs
#   reverse_string('world') → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_string(string) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(reverse_string(...))
#      → calls reverse_string(string) with string=result
#      → 3.1 result = ''
#      → 3.2 for char in string:
#      → 3.2     result = char + result
#      → 3.3 return result
#   4. def reverse_string(string) → stored in memory, not executed
#   5. print(reverse_string(...))
#      → calls reverse_string(string) with string='hello'
#      → 5.1 result = ''
#      → 5.2 for char in string:
#      → 5.2     result = char + result
#      → 5.3 return result
#   6. def reverse_string(string) → stored in memory, not executed
#   7. print(reverse_string(...))
#      → calls reverse_string(string) with string='Python'
#      → 7.1 result = ''
#      → 7.2 for char in string:
#      → 7.2     result = char + result
#      → 7.3 return result
#   8. def reverse_string(string) → stored in memory, not executed
#   9. def reverse_string(string) → stored in memory, not executed
#   10. string = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(reverse_string(...))
#      → calls reverse_string(string) with string='world'
#      → 13.1 result = ''
#      → 13.2 for char in string:
#      → 13.2     result = char + result
#      → 13.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done

"""

"""
Problem 7:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_even(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % & * // ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_even(result) runs first → then print() outputs the result
#   is_even(8) runs first → then print() outputs the result
#   is_even(12) runs first → then print() outputs the result
#   is_even(number) → each argument is evaluated first, then the function body runs
#   is_even(25) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   is_even(result) → each argument is evaluated first, then the function body runs
#   is_even(8) → each argument is evaluated first, then the function body runs
#   ? == number → evaluated as True or False (determines which branch runs)
#   is_even(12) → each argument is evaluated first, then the function body runs
#   is_even(25) → each argument is evaluated first, then the function body runs
#   number % 2 → Python looks up number, then 2, then applies %
#   number & 1 → Python looks up number, then 1, then applies &
#   ? * 2 → Python looks up ?, then 2, then applies *
#   number // 2 → Python looks up number, then 2, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_even(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(is_even(...))
#      → calls is_even(number) with number=result
#      → 3.1 return number % 2 == 0
#   4. def is_even(number) → stored in memory, not executed
#   5. print(is_even(...))
#      → calls is_even(number) with number=8
#      → 5.1 return number % 2 == 0
#   6. def is_even(number) → stored in memory, not executed
#   7. print(is_even(...))
#      → calls is_even(number) with number=12
#      → 7.1 return number % 2 == 0
#   8. def is_even(number) → stored in memory, not executed
#   9. def is_even(number) → stored in memory, not executed
#   10. number = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(is_even(...))
#      → calls is_even(number) with number=25
#      → 13.1 return number % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_even) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(is_even(...)) → is_even() runs first → value passed to print()
#     print(is_even(...)) → is_even() runs first → value passed to print()
#     print(is_even(...)) → is_even() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_even(...)) → is_even() runs first → value passed to print()

"""

"""
Problem 8:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def cal_square(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   cal_square(lst) runs first → then print() outputs the result
#   calculate_square(6) runs first → then print() outputs the result
#   cal_square(number) → each argument is evaluated first, then the function body runs
#   cal_square(25) runs first → then print() outputs the result
#   number ** 2 → Python looks up number, then 2, then applies **
#   cal_square(lst) → each argument is evaluated first, then the function body runs
#   calculate_square(6) → each argument is evaluated first, then the function body runs
#   cal_square(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def cal_square(number) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(cal_square(...))
#      → calls cal_square(number) with number=lst
#      → 3.1 import math
#      → 3.2 return math.pow(number, 2)
#   4. def calculate_square(number) → stored in memory, not executed
#   5. print(calculate_square(...))
#      → calls calculate_square(number) with number=6
#      → 5.1 square = number ** 2
#      → 5.2 return square
#   6. def cal_square(number) → stored in memory, not executed
#   7. def cal_square(number) → stored in memory, not executed
#   8. number = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(cal_square(...))
#      → calls cal_square(number) with number=25
#      → 11.1 import math
#      → 11.2 return math.pow(number, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_square, cal_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(cal_square(...)) → cal_square() runs first → value passed to print()
#     print(calculate_square(...)) → calculate_square() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(cal_square(...)) → cal_square() runs first → value passed to print()

"""

"""
Problem 9:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def greet_user(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   greet_user('Jonathan') runs first → then print() outputs the result
#   greet_user('Alice') runs first → then print() outputs the result
#   greet_user('Bob') runs first → then print() outputs the result
#   greet_user(string) → each argument is evaluated first, then the function body runs
#   greet_user('world') runs first → then print() outputs the result
#   greet_user('Jonathan') → each argument is evaluated first, then the function body runs
#   ? + '!' → Python looks up ?, then '!', then applies +
#   greet_user('Alice') → each argument is evaluated first, then the function body runs
#   greet_user('Bob') → each argument is evaluated first, then the function body runs
#   'hello %s' % string → Python looks up 'hello %s', then string, then applies %
#   greet_user('world') → each argument is evaluated first, then the function body runs
#   'Hello, ' + name → Python looks up 'Hello, ', then name, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def greet_user(string) → stored in memory, not executed
#   2. print(greet_user(...))
#      → calls greet_user(string) with string='Jonathan'
#      → 2.1 return 'hello %s' % string
#   3. def greet_user(name) → stored in memory, not executed
#   4. print(greet_user(...))
#      → calls greet_user(string) with string='Alice'
#      → 4.1 return 'hello %s' % string
#   5. def greet_user(name) → stored in memory, not executed
#   6. print(greet_user(...))
#      → calls greet_user(string) with string='Bob'
#      → 6.1 return 'hello %s' % string
#   7. def greet_user(string) → stored in memory, not executed
#   8. def greet_user(string) → stored in memory, not executed
#   9. string = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(greet_user(...))
#      → calls greet_user(string) with string='world'
#      → 12.1 return 'hello %s' % string
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (greet_user) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(greet_user(...)) → greet_user() runs first → value passed to print()
#     print(greet_user(...)) → greet_user() runs first → value passed to print()
#     print(greet_user(...)) → greet_user() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(greet_user(...)) → greet_user() runs first → value passed to print()

"""

"""
Problem 10:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_lst(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_lst(result) runs first → then print() outputs the result
#   square_lst(?) runs first → then print() outputs the result
#   square_lst(numbers) → each argument is evaluated first, then the function body runs
#   square_lst(result) → each argument is evaluated first, then the function body runs
#   square_lst(?) → each argument is evaluated first, then the function body runs
#   n ** 2 → Python looks up n, then 2, then applies **
#   number ** 2 → Python looks up number, then 2, then applies **
#   x ** 2 → Python looks up x, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_lst(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(square_lst(...))
#      → calls square_lst(numbers) with numbers=result
#      → 3.1 return list(map(lambda number: number ** 2, numbers))
#   4. def square_lst(numbers) → stored in memory, not executed
#   5. print(square_lst(...))
#      → calls square_lst(numbers) with numbers=[1, 2, 3]
#      → 5.1 return list(map(lambda number: number ** 2, numbers))
#   6. def square_lst(numbers) → stored in memory, not executed
#   7. print(square_lst(...))
#      → calls square_lst(numbers) with numbers=[3, 4, 5]
#      → 7.1 return list(map(lambda number: number ** 2, numbers))
#   8. def square_lst(numbers) → stored in memory, not executed
#   9. def square_lst(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(square_lst(...))
#      → calls square_lst(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return list(map(lambda number: number ** 2, numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(square_lst(...)) → square_lst() runs first → value passed to print()
#     print(square_lst(...)) → square_lst() runs first → value passed to print()
#     print(square_lst(...)) → square_lst() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_lst(...)) → square_lst() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 11:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def covert_uppercase(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: - <=
# Built-in functions: chr, ord, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   covert_uppercase('Jonathan') runs first → then print() outputs the result
#   covert_uppercase('hello') runs first → then print() outputs the result
#   covert_uppercase(string) → each argument is evaluated first, then the function body runs
#   covert_uppercase('world') runs first → then print() outputs the result
#   covert_uppercase('Jonathan') → each argument is evaluated first, then the function body runs
#   covert_uppercase('hello') → each argument is evaluated first, then the function body runs
#   covert_uppercase('world') → each argument is evaluated first, then the function body runs
#   'a' <= char → evaluated as True or False (determines which branch runs)
#   'a' <= 'z' → evaluated as True or False (determines which branch runs)
#   ord(...) - 32 → Python looks up ord(...), then 32, then applies -
#   'a' <= c → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def covert_uppercase(string) → stored in memory, not executed
#   2. print(covert_uppercase(...))
#      → calls covert_uppercase(string) with string='Jonathan'
#      → 2.1 return str(string).upper()
#   3. def covert_uppercase(string) → stored in memory, not executed
#   4. print(covert_uppercase(...))
#      → calls covert_uppercase(string) with string='hello'
#      → 4.1 return str(string).upper()
#   5. def covert_uppercase(string) → stored in memory, not executed
#   6. def covert_uppercase(string) → stored in memory, not executed
#   7. string = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(covert_uppercase(...))
#      → calls covert_uppercase(string) with string='world'
#      → 10.1 return str(string).upper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (covert_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(covert_uppercase(...)) → covert_uppercase() runs first → value passed to print()
#     print(covert_uppercase(...)) → covert_uppercase() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(covert_uppercase(...)) → covert_uppercase() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 12:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_sum(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_sum(lst) runs first → then print() outputs the result
#   cal_sum(lst) runs first → then print() outputs the result
#   calculate_sum(number) → each argument is evaluated first, then the function body runs
#   calculate_sum(?) runs first → then print() outputs the result
#   calculate_sum(lst) → each argument is evaluated first, then the function body runs
#   cal_sum(lst) → each argument is evaluated first, then the function body runs
#   calculate_sum(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_sum(number) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(calculate_sum(...))
#      → calls calculate_sum(number) with number=lst
#      → 3.1 from functools import reduce
#      → 3.2 import operator
#      → 3.3 return reduce(operator.add, number, 0)
#   4. def cal_sum(numbers) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(cal_sum(...))
#      → calls cal_sum(numbers) with numbers=lst
#      → 6.1 count = 0
#      → 6.2 for number in numbers:
#      → 6.2     count += number
#      → 6.3 return count
#   7. def calculate_sum(number) → stored in memory, not executed
#   8. def calculate_sum(number) → stored in memory, not executed
#   9. number = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(calculate_sum(...))
#      → calls calculate_sum(number) with number=[4, 1, 8, 6, 3]
#      → 12.1 from functools import reduce
#      → 12.2 import operator
#      → 12.3 return reduce(operator.add, number, 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_sum, cal_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(calculate_sum(...)) → calculate_sum() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(cal_sum(...)) → cal_sum() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_sum(...)) → calculate_sum() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 13:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_odd(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % & != ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_odd(10) runs first → then print() outputs the result
#   is_odd(7) runs first → then print() outputs the result
#   is_odd(5) runs first → then print() outputs the result
#   is_odd(9) runs first → then print() outputs the result
#   is_odd(number) → each argument is evaluated first, then the function body runs
#   is_odd(25) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   is_odd(10) → each argument is evaluated first, then the function body runs
#   is_odd(7) → each argument is evaluated first, then the function body runs
#   ? == 1 → evaluated as True or False (determines which branch runs)
#   is_odd(5) → each argument is evaluated first, then the function body runs
#   is_odd(9) → each argument is evaluated first, then the function body runs
#   is_odd(25) → each argument is evaluated first, then the function body runs
#   number % 2 → Python looks up number, then 2, then applies %
#   number & 1 → Python looks up number, then 1, then applies &
#   ? == 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_odd(number) → stored in memory, not executed
#   2. print(is_odd(...))
#      → calls is_odd(number) with number=10
#      → 2.1 return number % 2 != 0
#   3. print(is_odd(...))
#      → calls is_odd(number) with number=7
#      → 3.1 return number % 2 != 0
#   4. def is_odd(number) → stored in memory, not executed
#   5. print(is_odd(...))
#      → calls is_odd(number) with number=5
#      → 5.1 return number % 2 != 0
#   6. def is_odd(number) → stored in memory, not executed
#   7. print(is_odd(...))
#      → calls is_odd(number) with number=9
#      → 7.1 return number % 2 != 0
#   8. def is_odd(number) → stored in memory, not executed
#   9. def is_odd(number) → stored in memory, not executed
#   10. number = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(is_odd(...))
#      → calls is_odd(number) with number=25
#      → 13.1 return number % 2 != 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_odd(...)) → is_odd() runs first → value passed to print()
#     print(is_odd(...)) → is_odd() runs first → value passed to print()
#     print(is_odd(...)) → is_odd() runs first → value passed to print()
#     print(is_odd(...)) → is_odd() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_odd(...)) → is_odd() runs first → value passed to print()

"""

"""
Problem 14:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def concatenate_strings(string1, string2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   concatenate_strings(lst1, lst2) runs first → then print() outputs the result
#   concatenate_string('Jonathan', 'noll') runs first → then print() outputs the result
#   concatenate_strings(string1, string2) → each argument is evaluated first, then the function body runs
#   concatenate_strings('world', 'python') runs first → then print() outputs the result
#   string1 + string2 → Python looks up string1, then string2, then applies +
#   concatenate_strings(lst1, lst2) → each argument is evaluated first, then the function body runs
#   concatenate_string('Jonathan', 'noll') → each argument is evaluated first, then the function body runs
#   '%s%s' % ? → Python looks up '%s%s', then ?, then applies %
#   concatenate_strings('world', 'python') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def concatenate_strings(string1, string2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(concatenate_strings(...))
#      → calls concatenate_strings(string1, string2) with string1=lst1, string2=lst2
#      → 4.1 return '%s%s' % (string1, string2)
#   5. def concatenate_string(string1, string2) → stored in memory, not executed
#   6. print(concatenate_string(...))
#      → calls concatenate_string(string1, string2) with string1='Jonathan', string2='noll'
#      → 6.1 return string1 + string2
#   7. def concatenate_strings(string1, string2) → stored in memory, not executed
#   8. def concatenate_strings(string1, string2) → stored in memory, not executed
#   9. string1 = ... → assigns value to variable
#   10. string2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(concatenate_strings(...))
#      → calls concatenate_strings(string1, string2) with string1='world', string2='python'
#      → 13.1 return '%s%s' % (string1, string2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (concatenate_strings, concatenate_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(concatenate_strings(...)) → concatenate_strings() runs first → value passed to print()
#     print(concatenate_string(...)) → concatenate_string() runs first → value passed to print()
#     string1 = ... → stored in memory
#     string2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(concatenate_strings(...)) → concatenate_strings() runs first → value passed to print()

"""

"""
Problem 15:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_length(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_length('Jonathan') runs first → then print() outputs the result
#   find_len('Jonathan') runs first → then print() outputs the result
#   find_length(string) → each argument is evaluated first, then the function body runs
#   find_length('world') runs first → then print() outputs the result
#   find_length('Jonathan') → each argument is evaluated first, then the function body runs
#   find_len('Jonathan') → each argument is evaluated first, then the function body runs
#   find_length('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_length(string) → stored in memory, not executed
#   2. print(find_length(...))
#      → calls find_length(string) with string='Jonathan'
#      → 2.1 return sum((1 for _ in string))
#   3. def find_len(string) → stored in memory, not executed
#   4. print(find_len(...))
#      → calls find_len(string) with string='Jonathan'
#      → 4.1 count = 0
#      → 4.2 for char in string:
#      → 4.2     count += 1
#      → 4.3 return count
#   5. def find_length(string) → stored in memory, not executed
#   6. def find_length(string) → stored in memory, not executed
#   7. string = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(find_length(...))
#      → calls find_length(string) with string='world'
#      → 10.1 return sum((1 for _ in string))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_len, find_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(find_length(...)) → find_length() runs first → value passed to print()
#     print(find_len(...)) → find_len() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_length(...)) → find_length() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done

"""

"""
Problem 16:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + != // ==
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_palindrome('tom') runs first → then print() outputs the result
#   is_palindrome('racecar') runs first → then print() outputs the result
#   is_palindrome('madam') runs first → then print() outputs the result
#   is_palindrome(string) → each argument is evaluated first, then the function body runs
#   is_palindrome('world') runs first → then print() outputs the result
#   string == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome('tom') → each argument is evaluated first, then the function body runs
#   is_palindrome('racecar') → each argument is evaluated first, then the function body runs
#   is_palindrome('madam') → each argument is evaluated first, then the function body runs
#   is_palindrome('world') → each argument is evaluated first, then the function body runs
#   len(...) // 2 → Python looks up len(...), then 2, then applies //
#   ? != ? → evaluated as True or False (determines which branch runs)
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(string) → stored in memory, not executed
#   2. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='tom'
#      → 2.1 return string == string[::-1]
#   3. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='racecar'
#      → 3.1 return string == string[::-1]
#   4. def is_palindrome(string) → stored in memory, not executed
#   5. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='madam'
#      → 5.1 return string == string[::-1]
#   6. def is_palindrome(string) → stored in memory, not executed
#   7. def is_palindrome(string) → stored in memory, not executed
#   8. string = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='world'
#      → 11.1 return string == string[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 17:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words(example_string) runs first → then print() outputs the result
#   count_words(arg) runs first → then print() outputs the result
#   count_words(string) → each argument is evaluated first, then the function body runs
#   count_words('world') runs first → then print() outputs the result
#   count_words(example_string) → each argument is evaluated first, then the function body runs
#   count_words(arg) → each argument is evaluated first, then the function body runs
#   count_words('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words(string) → stored in memory, not executed
#   2. example_string = ... → assigns value to variable
#   3. print(count_words(...))
#      → calls count_words(string) with string=example_string
#      → 3.1 return word_count
#   4. def count_words(string) → stored in memory, not executed
#   5. example_string = ... → assigns value to variable
#   6. print(count_words(...))
#      → calls count_words(string) with string=example_string
#      → 6.1 return word_count
#   7. def count_words(string) → stored in memory, not executed
#   8. arg = ... → assigns value to variable
#   9. print(count_words(...))
#      → calls count_words(string) with string=arg
#      → 9.1 return word_count
#   10. def count_words(string) → stored in memory, not executed
#   11. def count_words(string) → stored in memory, not executed
#   12. string = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(count_words(...))
#      → calls count_words(string) with string='world'
#      → 15.1 return word_count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_string = ... → stored in memory
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     example_string = ... → stored in memory
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     arg = ... → stored in memory
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words(...)) → count_words() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 18:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: input, int, min, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min() → each argument is evaluated first, then the function body runs
#   find_min(lst) runs first → then print() outputs the result
#   find_minimum(min_number) runs first → then print() outputs the result
#   find_min(numbers) → each argument is evaluated first, then the function body runs
#   find_min(?) runs first → then print() outputs the result
#   find_min(lst) → each argument is evaluated first, then the function body runs
#   find_minimum(min_number) → each argument is evaluated first, then the function body runs
#   find_min(?) → each argument is evaluated first, then the function body runs
#   min_num < 0 → evaluated as True or False (determines which branch runs)
#   number < min_number → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min() → stored in memory, not executed
#   2. find_min() → calls find_min() with 
#      → 2.1 user = input('type number: ').split()
#      → 2.2 int_number = []
#      → 2.3 for number in user:
#      → 2.3     int_number.append(int(number))
#      → 2.4 min_num = sorted(int_number)[0]
#      → 2.5 for number in int_number:
#      → 2.5     if min_num < 0:
#      → 2.5         min_num = number
#      → 2.6 result = min_num
#      → 2.7 return result
#   3. def find_min(numbers) → stored in memory, not executed
#   4. lst = ... → assigns value to variable
#   5. print(find_min(...))
#      → calls find_min() with 
#      → 5.1 user = input('type number: ').split()
#      → 5.2 int_number = []
#      → 5.3 for number in user:
#      → 5.3     int_number.append(int(number))
#      → 5.4 min_num = sorted(int_number)[0]
#      → 5.5 for number in int_number:
#      → 5.5     if min_num < 0:
#      → 5.5         min_num = number
#      → 5.6 result = min_num
#      → 5.7 return result
#   6. def find_minimum(numbers) → stored in memory, not executed
#   7. min_number = ... → assigns value to variable
#   8. print(find_minimum(...))
#      → calls find_minimum(numbers) with numbers=min_number
#      → 8.1 min_number = numbers[0]
#      → 8.2 for number in numbers:
#      → 8.2     if number < min_number:
#      → 8.2         min_number = number
#      → 8.3 return min_number
#   9. def find_min() → stored in memory, not executed
#   10. def find_min() → stored in memory, not executed
#   11. numbers = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(find_min(...))
#      → calls find_min() with 
#      → 14.1 user = input('type number: ').split()
#      → 14.2 int_number = []
#      → 14.3 for number in user:
#      → 14.3     int_number.append(int(number))
#      → 14.4 min_num = sorted(int_number)[0]
#      → 14.5 for number in int_number:
#      → 14.5     if min_num < 0:
#      → 14.5         min_num = number
#      → 14.6 result = min_num
#      → 14.7 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_minimum, find_min) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     find_min() → jumps into find_min(), runs body, returns
#     lst = ... → stored in memory
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     min_number = ... → stored in memory
#     print(find_minimum(...)) → find_minimum() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min(...)) → find_min() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 19:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_even_index_sum(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: len, range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_even_index_sum(lst) runs first → then print() outputs the result
#   is_even_index_sum(?) runs first → then print() outputs the result
#   is_even_index_sum(numbers) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   is_even_index_sum(lst) → each argument is evaluated first, then the function body runs
#   is_even_index_sum(?) → each argument is evaluated first, then the function body runs
#   even_index_sum % 2 → Python looks up even_index_sum, then 2, then applies %
#   i % 2 → Python looks up i, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_even_index_sum(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(is_even_index_sum(...))
#      → calls is_even_index_sum(numbers) with numbers=lst
#      → 3.1 return even_index_sum % 2 == 0
#   4. def is_even_index_sum(numbers) → stored in memory, not executed
#   5. print(is_even_index_sum(...))
#      → calls is_even_index_sum(numbers) with numbers=[1, 2, 3, 4, 5]
#      → 5.1 return even_index_sum % 2 == 0
#   6. print(is_even_index_sum(...))
#      → calls is_even_index_sum(numbers) with numbers=[2, 4, 6, 8]
#      → 6.1 return even_index_sum % 2 == 0
#   7. def is_even_index_sum(numbers) → stored in memory, not executed
#   8. def is_even_index_sum(numbers) → stored in memory, not executed
#   9. numbers = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(is_even_index_sum(...))
#      → calls is_even_index_sum(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 12.1 return even_index_sum % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_even_index_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(is_even_index_sum(...)) → is_even_index_sum() runs first → value passed to print()
#     print(is_even_index_sum(...)) → is_even_index_sum() runs first → value passed to print()
#     print(is_even_index_sum(...)) → is_even_index_sum() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_even_index_sum(...)) → is_even_index_sum() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 20:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def double_elements(elements) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   double_elements(lst) runs first → then print() outputs the result
#   double_elements(numbers_list) runs first → then print() outputs the result
#   double_elements(elements) → each argument is evaluated first, then the function body runs
#   double_elements(?) runs first → then print() outputs the result
#   double_elements(lst) → each argument is evaluated first, then the function body runs
#   double_elements(numbers_list) → each argument is evaluated first, then the function body runs
#   double_elements(?) → each argument is evaluated first, then the function body runs
#   num * 2 → Python looks up num, then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def double_elements(elements) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(double_elements(...))
#      → calls double_elements(elements) with elements=lst
#      → 3.1 return list(map(lambda element: element, elements))
#   4. def double_elements(numbers) → stored in memory, not executed
#   5. numbers_list = ... → assigns value to variable
#   6. print('The list with doubled elements is:', double_elements(...))
#   7. def double_elements(elements) → stored in memory, not executed
#   8. def double_elements(elements) → stored in memory, not executed
#   9. elements = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(double_elements(...))
#      → calls double_elements(elements) with elements=[5, 3, 8, 4]
#      → 12.1 return list(map(lambda element: element, elements))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (double_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     numbers_list = ... → stored in memory
#     print('The list with doubled elements is:', double_elements(...))
#     elements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#
#   Loop: takes one item from elements each round, runs body, repeats until done

"""

"""
Problem 21:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_all_positive(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > <= and
# Built-in functions: all, len, min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_all_positive(lst) runs first → then print() outputs the result
#   is_all_positive(?) runs first → then print() outputs the result
#   is_all_positive(numbers) → each argument is evaluated first, then the function body runs
#   is_all_positive(lst) → each argument is evaluated first, then the function body runs
#   is_all_positive(?) → each argument is evaluated first, then the function body runs
#   number <= 0 → evaluated as True or False (determines which branch runs)
#   len(...) > 0 → evaluated as True or False (determines which branch runs)
#   min(...) > 0 → evaluated as True or False (determines which branch runs)
#   n > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_all_positive(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(is_all_positive(...))
#      → calls is_all_positive(numbers) with numbers=lst
#      → 3.1 return False
#   4. def is_all_positive(numbers) → stored in memory, not executed
#   5. print(is_all_positive(...))
#      → calls is_all_positive(numbers) with numbers=[1, 2, 3]
#      → 5.1 return False
#   6. print(is_all_positive(...))
#      → calls is_all_positive(numbers) with numbers=[1, ?, 3]
#      → 6.1 return False
#   7. def is_all_positive(numbers) → stored in memory, not executed
#   8. print(is_all_positive(...))
#      → calls is_all_positive(numbers) with numbers=[5, 10, 15]
#      → 8.1 return False
#   9. def is_all_positive(numbers) → stored in memory, not executed
#   10. def is_all_positive(numbers) → stored in memory, not executed
#   11. numbers = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(is_all_positive(...))
#      → calls is_all_positive(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 14.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_all_positive) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(is_all_positive(...)) → is_all_positive() runs first → value passed to print()
#     print(is_all_positive(...)) → is_all_positive() runs first → value passed to print()
#     print(is_all_positive(...)) → is_all_positive() runs first → value passed to print()
#     print(is_all_positive(...)) → is_all_positive() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_all_positive(...)) → is_all_positive() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 22:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_average(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: / >
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_average(lst) runs first → then print() outputs the result
#   find_average_lst(lst) runs first → then print() outputs the result
#   find_average(numbers) → each argument is evaluated first, then the function body runs
#   find_average(?) runs first → then print() outputs the result
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   find_average(lst) → each argument is evaluated first, then the function body runs
#   count > 0 → evaluated as True or False (determines which branch runs)
#   find_average_lst(lst) → each argument is evaluated first, then the function body runs
#   total / count → Python looks up total, then count, then applies /
#   find_average(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_average(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_average(...))
#      → calls find_average(numbers) with numbers=lst
#      → 3.1 total = 0
#      → 3.2 count = 0
#      → 3.3 for item in numbers:
#      → 3.3     total += item
#      → 3.3     count += 1
#      → 3.4 return total / count if count else 0
#   4. def find_average_lst(numbers) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(find_average_lst(...))
#      → calls find_average_lst(numbers) with numbers=lst
#      → 6.1 total = 0
#      → 6.2 count = 0
#      → 6.3 for number in numbers:
#      → 6.3     total += number
#      → 6.3     count += 1
#      → 6.4 average = total / count
#      → 6.5 return average
#   7. def find_average_lst(numbers) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. print(find_average_lst(...))
#      → calls find_average_lst(numbers) with numbers=lst
#      → 9.1 total = 0
#      → 9.2 count = 0
#      → 9.3 for number in numbers:
#      → 9.3     total += number
#      → 9.3     count += 1
#      → 9.4 average = total / count
#      → 9.5 return average
#   10. def find_average(numbers) → stored in memory, not executed
#   11. def find_average(numbers) → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(find_average(...))
#      → calls find_average(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 15.1 total = 0
#      → 15.2 count = 0
#      → 15.3 for item in numbers:
#      → 15.3     total += item
#      → 15.3     count += 1
#      → 15.4 return total / count if count else 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_average, find_average_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_average(...)) → find_average() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(find_average_lst(...)) → find_average_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(find_average_lst(...)) → find_average_lst() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_average(...)) → find_average() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 23:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def contains_negative(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < > and
# Built-in functions: any, len, min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   contains_negative(lst) runs first → then print() outputs the result
#   contains_negative(?) runs first → then print() outputs the result
#   contains_negative(numbers) → each argument is evaluated first, then the function body runs
#   contains_negative(lst) → each argument is evaluated first, then the function body runs
#   contains_negative(?) → each argument is evaluated first, then the function body runs
#   number < 0 → evaluated as True or False (determines which branch runs)
#   len(...) > 0 → evaluated as True or False (determines which branch runs)
#   min(...) < 0 → evaluated as True or False (determines which branch runs)
#   n < 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def contains_negative(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(contains_negative(...))
#      → calls contains_negative(numbers) with numbers=lst
#      → 3.1 return True
#   4. print(contains_negative(...))
#      → calls contains_negative(numbers) with numbers=[0, 1, 2]
#      → 4.1 return True
#   5. def contains_negative(numbers) → stored in memory, not executed
#   6. print(contains_negative(...))
#      → calls contains_negative(numbers) with numbers=[1, 2, ?]
#      → 6.1 return True
#   7. print(contains_negative(...))
#      → calls contains_negative(numbers) with numbers=[1, 2, 3]
#      → 7.1 return True
#   8. def contains_negative(numbers) → stored in memory, not executed
#   9. print(contains_negative(...))
#      → calls contains_negative(numbers) with numbers=[?, 0, 1]
#      → 9.1 return True
#   10. def contains_negative(numbers) → stored in memory, not executed
#   11. def contains_negative(numbers) → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(contains_negative(...))
#      → calls contains_negative(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 15.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (contains_negative) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(contains_negative(...)) → contains_negative() runs first → value passed to print()
#     print(contains_negative(...)) → contains_negative() runs first → value passed to print()
#     print(contains_negative(...)) → contains_negative() runs first → value passed to print()
#     print(contains_negative(...)) → contains_negative() runs first → value passed to print()
#     print(contains_negative(...)) → contains_negative() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(contains_negative(...)) → contains_negative() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 24:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_last_element(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_last_element(lst) runs first → then print() outputs the result
#   find_element(lst) runs first → then print() outputs the result
#   find_last_element(string) → each argument is evaluated first, then the function body runs
#   find_last_element('world') runs first → then print() outputs the result
#   find_last_element(lst) → each argument is evaluated first, then the function body runs
#   find_element(lst) → each argument is evaluated first, then the function body runs
#   find_last_element('world') → each argument is evaluated first, then the function body runs
#   element == last → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_last_element(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_last_element(...))
#      → calls find_last_element(string) with string=lst
#      → 3.1 return string[-1]
#   4. def find_element(elements) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(find_element(...))
#      → calls find_element(elements) with elements=lst
#      → 6.1 last = elements[-1]
#      → 6.2 for element in elements:
#      → 6.2     if element == last:
#      → 6.2         return last
#   7. def find_last_element(string) → stored in memory, not executed
#   8. def find_last_element(string) → stored in memory, not executed
#   9. string = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_last_element(...))
#      → calls find_last_element(string) with string='world'
#      → 12.1 return string[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_element, find_last_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_last_element(...)) → find_last_element() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(find_element(...)) → find_element() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_last_element(...)) → find_last_element() runs first → value passed to print()
#
#   Loop: takes one item from elements each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 25:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_elements(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_elements(arg) runs first → then print() outputs the result
#   multiply_elements(lst) runs first → then print() outputs the result
#   multiply_elements(numbers) → each argument is evaluated first, then the function body runs
#   multiply_elements(?) runs first → then print() outputs the result
#   multiply_elements(arg) → each argument is evaluated first, then the function body runs
#   multiply_elements(lst) → each argument is evaluated first, then the function body runs
#   multiply_elements(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_elements(numbers) → stored in memory, not executed
#   2. arg = ... → assigns value to variable
#   3. print(multiply_elements(...))
#      → calls multiply_elements(numbers) with numbers=arg
#      → 3.1 return count
#   4. def multiply_elements(numbers) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(multiply_elements(...))
#      → calls multiply_elements(numbers) with numbers=lst
#      → 6.1 return count
#   7. def multiply_elements(numbers) → stored in memory, not executed
#   8. arg = ... → assigns value to variable
#   9. print(multiply_elements(...))
#      → calls multiply_elements(numbers) with numbers=arg
#      → 9.1 return count
#   10. def multiply_elements(numbers) → stored in memory, not executed
#   11. def multiply_elements(numbers) → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(multiply_elements(...))
#      → calls multiply_elements(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 15.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     arg = ... → stored in memory
#     print(multiply_elements(...)) → multiply_elements() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(multiply_elements(...)) → multiply_elements() runs first → value passed to print()
#     arg = ... → stored in memory
#     print(multiply_elements(...)) → multiply_elements() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_elements(...)) → multiply_elements() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 26:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def has_duplicates(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > != in
# Built-in functions: any, input, len, list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   has_duplicates(lst) runs first → then print() outputs the result
#   has_duplicates() → each argument is evaluated first, then the function body runs
#   has_duplicates() runs first → then print() outputs the result
#   has_duplicates(list_with_duplicates) runs first → then print() outputs the result
#   has_duplicates(?) runs first → then print() outputs the result
#   has_duplicates(lst) → each argument is evaluated first, then the function body runs
#   has_duplicates(list_with_duplicates) → each argument is evaluated first, then the function body runs
#   len(...) != len(...) → evaluated as True or False (determines which branch runs)
#   has_duplicates(?) → each argument is evaluated first, then the function body runs
#   word in unique → evaluated as True or False (determines which branch runs)
#   element in duplicate → evaluated as True or False (determines which branch runs)
#   lst.count(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def has_duplicates(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(has_duplicates(...))
#      → calls has_duplicates(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def has_duplicates() → stored in memory, not executed
#   5. has_duplicates() → calls has_duplicates(lst) with 
#      → 5.1 return list(map(lambda word: word, lst))
#   6. def has_duplicates() → stored in memory, not executed
#   7. print(has_duplicates(...))
#      → calls has_duplicates(lst) with 
#      → 7.1 return list(map(lambda word: word, lst))
#   8. def has_duplicates(elements) → stored in memory, not executed
#   9. list_with_duplicates = ... → assigns value to variable
#   10. list_without_duplicates = ... → assigns value to variable
#   11. print('Does the list have duplicates?', has_duplicates(...))
#   12. def has_duplicates(lst) → stored in memory, not executed
#   13. print(has_duplicates(...))
#      → calls has_duplicates(lst) with lst=[1, 2, 3, 2]
#      → 13.1 return list(map(lambda word: word, lst))
#   14. print(has_duplicates(...))
#      → calls has_duplicates(lst) with lst=[1, 2, 3]
#      → 14.1 return list(map(lambda word: word, lst))
#   15. def has_duplicates(lst) → stored in memory, not executed
#   16. print(has_duplicates(...))
#      → calls has_duplicates(lst) with lst=['a', 'b', 'a']
#      → 16.1 return list(map(lambda word: word, lst))
#   17. def has_duplicates(lst) → stored in memory, not executed
#   18. def has_duplicates(lst) → stored in memory, not executed
#   19. lst = ... → assigns value to variable
#   20. result = ... → assigns value to variable
#   21. print(result)
#   22. print(has_duplicates(...))
#      → calls has_duplicates(lst) with lst=[1, 3, 5, 7]
#      → 22.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (has_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(has_duplicates(...)) → has_duplicates() runs first → value passed to print()
#     has_duplicates() → jumps into has_duplicates(), runs body, returns
#     print(has_duplicates(...)) → has_duplicates() runs first → value passed to print()
#     list_with_duplicates = ... → stored in memory
#     list_without_duplicates = ... → stored in memory
#     print('Does the list have duplicates?', has_duplicates(...))
#     print(has_duplicates(...)) → has_duplicates() runs first → value passed to print()
#     print(has_duplicates(...)) → has_duplicates() runs first → value passed to print()
#     print(has_duplicates(...)) → has_duplicates() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(has_duplicates(...)) → has_duplicates() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 27:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_occurrences(elements, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_occurrences(elements, target) runs first → then print() outputs the result
#   count_occurrences(?, 2) runs first → then print() outputs the result
#   count_occurrences(?, 'a') runs first → then print() outputs the result
#   count_occurrences(elements, target) → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 'goal') runs first → then print() outputs the result
#   count_occurrences(?, 2) → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 'a') → each argument is evaluated first, then the function body runs
#   'The word %s occures %s times' % ? → Python looks up 'The word %s occures %s times', then ?, then applies %
#   count_occurrences(?, 'goal') → each argument is evaluated first, then the function body runs
#   element == target → evaluated as True or False (determines which branch runs)
#   count_occurrences(list_elements, target_element) → each argument is evaluated first, then the function body runs
#   x == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_occurrences(elements, target) → stored in memory, not executed
#   2. elements = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. print(count_occurrences(...))
#      → calls count_occurrences(elements, target) with elements=elements, target=target
#      → 4.1 return 'The word %s occures %s times' % (word, count)
#   5. def count_occurrences(elements, target) → stored in memory, not executed
#   6. list_elements = ... → assigns value to variable
#   7. target_element = ... → assigns value to variable
#   8. print(?)
#   9. def count_occurrences(elements, target) → stored in memory, not executed
#   10. print(count_occurrences(...))
#      → calls count_occurrences(elements, target) with elements=[1, 2, 3, 2, 2], target=2
#      → 10.1 return 'The word %s occures %s times' % (word, count)
#   11. def count_occurrences(elements, target) → stored in memory, not executed
#   12. print(count_occurrences(...))
#      → calls count_occurrences(elements, target) with elements=['a', 'b', 'a', 'c'], target='a'
#      → 12.1 return 'The word %s occures %s times' % (word, count)
#   13. def count_occurrences(elements, target) → stored in memory, not executed
#   14. def count_occurrences(elements, target) → stored in memory, not executed
#   15. elements = ... → assigns value to variable
#   16. target = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(count_occurrences(...))
#      → calls count_occurrences(elements, target) with elements=[5, 3, 8, 4], target='goal'
#      → 19.1 return 'The word %s occures %s times' % (word, count)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     elements = ... → stored in memory
#     target = ... → stored in memory
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     list_elements = ... → stored in memory
#     target_element = ... → stored in memory
#     print(?)
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     elements = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from elements each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 28:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_vowels(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_vowels(lst) runs first → then print() outputs the result
#   remove_vowels('Python') runs first → then print() outputs the result
#   remove_vowels('Hello World') runs first → then print() outputs the result
#   remove_vowels(string) → each argument is evaluated first, then the function body runs
#   remove_vowels('world') runs first → then print() outputs the result
#   remove_vowels(lst) → each argument is evaluated first, then the function body runs
#   remove_vowels('Python') → each argument is evaluated first, then the function body runs
#   remove_vowels('Hello World') → each argument is evaluated first, then the function body runs
#   remove_vowels('world') → each argument is evaluated first, then the function body runs
#   char not in vowels → evaluated as True or False (determines which branch runs)
#   c.lower(...) not in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_vowels(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_vowels(...))
#      → calls remove_vowels(string) with string=lst
#      → 3.1 return result
#   4. def remove_vowels(string) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(remove_vowels(...))
#      → calls remove_vowels(string) with string=lst
#      → 6.1 return result
#   7. def remove_vowels(string) → stored in memory, not executed
#   8. print(remove_vowels(...))
#      → calls remove_vowels(string) with string='Python'
#      → 8.1 return result
#   9. def remove_vowels(string) → stored in memory, not executed
#   10. print(remove_vowels(...))
#      → calls remove_vowels(string) with string='Hello World'
#      → 10.1 return result
#   11. def remove_vowels(string) → stored in memory, not executed
#   12. def remove_vowels(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(remove_vowels(...))
#      → calls remove_vowels(string) with string='world'
#      → 16.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 29:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_words(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_words(input_sentence) runs first → then print() outputs the result
#   capitalize_words('python is awesome') runs first → then print() outputs the result
#   capitalize_words('hello world') runs first → then print() outputs the result
#   capitalize_words(sentence) → each argument is evaluated first, then the function body runs
#   capitalize_words('Python is fun') runs first → then print() outputs the result
#   capitalize_words(input_sentence) → each argument is evaluated first, then the function body runs
#   capitalize_words('python is awesome') → each argument is evaluated first, then the function body runs
#   capitalize_words('hello world') → each argument is evaluated first, then the function body runs
#   capitalize_words('Python is fun') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_words(sentence) → stored in memory, not executed
#   2. input_sentence = ... → assigns value to variable
#   3. print('Capitalized sentence:', capitalize_words(...))
#   4. def capitalize_words(sentence) → stored in memory, not executed
#   5. print(capitalize_words(...))
#      → calls capitalize_words(sentence) with sentence='python is awesome'
#      → 5.1 return ' '.join(capitalized_words)
#   6. def capitalize_words(sentence) → stored in memory, not executed
#   7. print(capitalize_words(...))
#      → calls capitalize_words(sentence) with sentence='hello world'
#      → 7.1 return ' '.join(capitalized_words)
#   8. def capitalize_words(sentence) → stored in memory, not executed
#   9. def capitalize_words(sentence) → stored in memory, not executed
#   10. sentence = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(capitalize_words(...))
#      → calls capitalize_words(sentence) with sentence='Python is fun'
#      → 13.1 return ' '.join(capitalized_words)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_sentence = ... → stored in memory
#     print('Capitalized sentence:', capitalize_words(...))
#     print(capitalize_words(...)) → capitalize_words() runs first → value passed to print()
#     print(capitalize_words(...)) → capitalize_words() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_words(...)) → capitalize_words() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 30:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_spaces(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: !=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_spaces(arg) runs first → then print() outputs the result
#   remove_space(lst) runs first → then print() outputs the result
#   remove_spaces('hello world') runs first → then print() outputs the result
#   remove_spaces('a b c') runs first → then print() outputs the result
#   remove_spaces(sentence) → each argument is evaluated first, then the function body runs
#   remove_spaces('Python is fun') runs first → then print() outputs the result
#   remove_spaces(arg) → each argument is evaluated first, then the function body runs
#   remove_space(lst) → each argument is evaluated first, then the function body runs
#   remove_spaces('hello world') → each argument is evaluated first, then the function body runs
#   remove_spaces('a b c') → each argument is evaluated first, then the function body runs
#   remove_spaces('Python is fun') → each argument is evaluated first, then the function body runs
#   c != ' ' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_spaces(sentence) → stored in memory, not executed
#   2. arg = ... → assigns value to variable
#   3. print(remove_spaces(...))
#      → calls remove_spaces(sentence) with sentence=arg
#      → 3.1 return sentence.replace(' ', '')
#   4. def remove_space(string) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(remove_space(...))
#      → calls remove_space(string) with string=lst
#      → 6.1 space_removed = string.replace(' ', '')
#      → 6.2 return space_removed
#   7. def remove_spaces(sentence) → stored in memory, not executed
#   8. arg = ... → assigns value to variable
#   9. print(remove_spaces(...))
#      → calls remove_spaces(sentence) with sentence=arg
#      → 9.1 return sentence.replace(' ', '')
#   10. def remove_spaces(sentence) → stored in memory, not executed
#   11. print(remove_spaces(...))
#      → calls remove_spaces(sentence) with sentence='hello world'
#      → 11.1 return sentence.replace(' ', '')
#   12. def remove_spaces(sentence) → stored in memory, not executed
#   13. print(remove_spaces(...))
#      → calls remove_spaces(sentence) with sentence='a b c'
#      → 13.1 return sentence.replace(' ', '')
#   14. def remove_spaces(sentence) → stored in memory, not executed
#   15. def remove_spaces(sentence) → stored in memory, not executed
#   16. sentence = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(remove_spaces(...))
#      → calls remove_spaces(sentence) with sentence='Python is fun'
#      → 19.1 return sentence.replace(' ', '')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_space, remove_spaces) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     arg = ... → stored in memory
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(remove_space(...)) → remove_space() runs first → value passed to print()
#     arg = ... → stored in memory
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#
#   Loop: takes one item from split_word each round, runs body, repeats until done

"""

"""
Problem 31:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_initials(full_name) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_initials('John Doe') runs first → then print() outputs the result
#   get_initials(full_name) → each argument is evaluated first, then the function body runs
#   get_initials('world') runs first → then print() outputs the result
#   '.'.join(...) + '.' → Python looks up '.'.join(...), then '.', then applies +
#   get_initials('John Doe') → each argument is evaluated first, then the function body runs
#   get_initials('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_initials(full_name) → stored in memory, not executed
#   2. print(get_initials(...))
#      → calls get_initials(full_name) with full_name='John Doe'
#      → 2.1 return '.'.join((part[0].upper() for part in parts)) + '.'
#   3. def get_initials(full_name) → stored in memory, not executed
#   4. def get_initials(full_name) → stored in memory, not executed
#   5. full_name = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(get_initials(...))
#      → calls get_initials(full_name) with full_name='world'
#      → 8.1 return '.'.join((part[0].upper() for part in parts)) + '.'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_initials) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(get_initials(...)) → get_initials() runs first → value passed to print()
#     full_name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_initials(...)) → get_initials() runs first → value passed to print()

"""

"""
Problem 32:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_word(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_word(lst) runs first → then print() outputs the result
#   reverse_words(lst) runs first → then print() outputs the result
#   reverse_words('python is fun') runs first → then print() outputs the result
#   reverse_words('one two three') runs first → then print() outputs the result
#   reverse_word(string) → each argument is evaluated first, then the function body runs
#   reverse_word('world') runs first → then print() outputs the result
#   reverse_word(lst) → each argument is evaluated first, then the function body runs
#   reverse_words(lst) → each argument is evaluated first, then the function body runs
#   reverse_words('python is fun') → each argument is evaluated first, then the function body runs
#   reverse_words('one two three') → each argument is evaluated first, then the function body runs
#   reverse_word('world') → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_word(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(reverse_word(...))
#      → calls reverse_word(string) with string=lst
#      → 3.1 result = ''
#      → 3.2 for char in seperate:
#      → 3.2     result = char + result
#      → 3.3 return result
#   4. def reverse_words(words) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(reverse_words(...))
#      → calls reverse_words(string) with string=lst
#      → 6.1 words = string.split()
#      → 6.2 result = []
#      → 6.3 for word in reversed(words):
#      → 6.3     result.append(word)
#      → 6.4 return ' '.join(result)
#   7. def reverse_words(string) → stored in memory, not executed
#   8. print(reverse_words(...))
#      → calls reverse_words(string) with string='python is fun'
#      → 8.1 words = string.split()
#      → 8.2 result = []
#      → 8.3 for word in reversed(words):
#      → 8.3     result.append(word)
#      → 8.4 return ' '.join(result)
#   9. def reverse_words(string) → stored in memory, not executed
#   10. print(reverse_words(...))
#      → calls reverse_words(string) with string='one two three'
#      → 10.1 words = string.split()
#      → 10.2 result = []
#      → 10.3 for word in reversed(words):
#      → 10.3     result.append(word)
#      → 10.4 return ' '.join(result)
#   11. def reverse_word(string) → stored in memory, not executed
#   12. def reverse_word(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(reverse_word(...))
#      → calls reverse_word(string) with string='world'
#      → 16.1 result = ''
#      → 16.2 for char in seperate:
#      → 16.2     result = char + result
#      → 16.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_words, reverse_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(reverse_word(...)) → reverse_word() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(reverse_words(...)) → reverse_words() runs first → value passed to print()
#     print(reverse_words(...)) → reverse_words() runs first → value passed to print()
#     print(reverse_words(...)) → reverse_words() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_word(...)) → reverse_word() runs first → value passed to print()
#
#   Loop: takes one item from reversed(...) each round, runs body, repeats until done

"""

"""
Problem 33:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates(input_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in or not in
# Built-in functions: list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates(input_list) runs first → then print() outputs the result
#   remove_duplicates(?) runs first → then print() outputs the result
#   remove_duplicates(input_list) → each argument is evaluated first, then the function body runs
#   remove_duplicates(?) → each argument is evaluated first, then the function body runs
#   element not in unique_elements → evaluated as True or False (determines which branch runs)
#   x in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates(input_list) → stored in memory, not executed
#   2. input_list = ... → assigns value to variable
#   3. print('List with duplicates removed:', remove_duplicates(...))
#   4. def remove_duplicates(input_list) → stored in memory, not executed
#   5. input_list = ... → assigns value to variable
#   6. print('List with duplicates removed:', remove_duplicates(...))
#   7. def remove_duplicates(input_list) → stored in memory, not executed
#   8. print(remove_duplicates(...))
#      → calls remove_duplicates(input_list) with input_list=[3, 1, 2, 1, 3]
#      → 8.1 return list(set(input_list))
#   9. def remove_duplicates(input_list) → stored in memory, not executed
#   10. print(remove_duplicates(...))
#      → calls remove_duplicates(input_list) with input_list=['a', 'b', 'a', 'c']
#      → 10.1 return list(set(input_list))
#   11. def remove_duplicates(input_list) → stored in memory, not executed
#   12. def remove_duplicates(input_list) → stored in memory, not executed
#   13. input_list = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(remove_duplicates(...))
#      → calls remove_duplicates(input_list) with input_list=[4, 1, 8, 6, 3]
#      → 16.1 return list(set(input_list))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_list = ... → stored in memory
#     print('List with duplicates removed:', remove_duplicates(...))
#     input_list = ... → stored in memory
#     print('List with duplicates removed:', remove_duplicates(...))
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     input_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#
#   Loop: takes one item from input_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 34:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) → each argument is evaluated first, then the function body runs
#   find_longest_word(input_string) runs first → then print() outputs the result
#   main(?) runs first → then print() outputs the result
#   find_longest_word(input_string) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#   len(...) > largest_len → evaluated as True or False (determines which branch runs)
#   len(...) > count → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. main(lst) → calls main(lst) with lst=lst
#      → 3.1 return max(lst, key=len)
#   4. def main(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. main(lst) → calls main(lst) with lst=lst
#      → 6.1 return max(lst, key=len)
#   7. def find_longest_word(words) → stored in memory, not executed
#   8. input_string = ... → assigns value to variable
#   9. print('Longest word:', find_longest_word(...))
#   10. def find_longest_word(words) → stored in memory, not executed
#   11. input_string = ... → assigns value to variable
#   12. print('Longest word:', find_longest_word(...))
#   13. def main(lst) → stored in memory, not executed
#   14. def main(lst) → stored in memory, not executed
#   15. lst = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 18.1 return max(lst, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, find_longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     input_string = ... → stored in memory
#     print('Longest word:', find_longest_word(...))
#     input_string = ... → stored in memory
#     print('Longest word:', find_longest_word(...))
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 35:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_vowels(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_vowels(lst) runs first → then print() outputs the result
#   find_vowels('Python') runs first → then print() outputs the result
#   find_vowels('Hello World') runs first → then print() outputs the result
#   find_vowels(string) → each argument is evaluated first, then the function body runs
#   find_vowels('world') runs first → then print() outputs the result
#   find_vowels(lst) → each argument is evaluated first, then the function body runs
#   find_vowels('Python') → each argument is evaluated first, then the function body runs
#   find_vowels('Hello World') → each argument is evaluated first, then the function body runs
#   find_vowels('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#   char.lower(...) in 'aeiou' → evaluated as True or False (determines which branch runs)
#   c.lower(...) in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_vowels(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_vowels(...))
#      → calls find_vowels(string) with string=lst
#      → 3.1 return return_vowels
#   4. def find_vowels(string) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(find_vowels(...))
#      → calls find_vowels(string) with string=lst
#      → 6.1 return return_vowels
#   7. def find_vowels(string) → stored in memory, not executed
#   8. print(find_vowels(...))
#      → calls find_vowels(string) with string='Python'
#      → 8.1 return return_vowels
#   9. def find_vowels(string) → stored in memory, not executed
#   10. print(find_vowels(...))
#      → calls find_vowels(string) with string='Hello World'
#      → 10.1 return return_vowels
#   11. def find_vowels(string) → stored in memory, not executed
#   12. def find_vowels(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(find_vowels(...))
#      → calls find_vowels(string) with string='world'
#      → 16.1 return return_vowels
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_vowels(...)) → find_vowels() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(find_vowels(...)) → find_vowels() runs first → value passed to print()
#     print(find_vowels(...)) → find_vowels() runs first → value passed to print()
#     print(find_vowels(...)) → find_vowels() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_vowels(...)) → find_vowels() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 36:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_number(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: % * + > >=
# Built-in functions: abs, int, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_number(number) runs first → then print() outputs the result
#   reverse_number(?) runs first → then print() outputs the result
#   reverse_number(1000) runs first → then print() outputs the result
#   reverse_number(number) → each argument is evaluated first, then the function body runs
#   reverse_number(25) runs first → then print() outputs the result
#   num > 0 → evaluated as True or False (determines which branch runs)
#   reverse_number(?) → each argument is evaluated first, then the function body runs
#   reverse_number(1000) → each argument is evaluated first, then the function body runs
#   reverse_number(25) → each argument is evaluated first, then the function body runs
#   num % 10 → Python looks up num, then 10, then applies %
#   ? + digit → Python looks up ?, then digit, then applies +
#   number >= 0 → evaluated as True or False (determines which branch runs)
#   reversed_num * 10 → Python looks up reversed_num, then 10, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_number(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(reverse_number(...))
#      → calls reverse_number(number) with number=number
#      → 3.1 return int(str(number)[::-1])
#   4. def reverse_number(number) → stored in memory, not executed
#   5. print(reverse_number(...))
#      → calls reverse_number(number) with number=?
#      → 5.1 return int(str(number)[::-1])
#   6. print(reverse_number(...))
#      → calls reverse_number(number) with number=1000
#      → 6.1 return int(str(number)[::-1])
#   7. def reverse_number(number) → stored in memory, not executed
#   8. def reverse_number(number) → stored in memory, not executed
#   9. number = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(reverse_number(...))
#      → calls reverse_number(number) with number=25
#      → 12.1 return int(str(number)[::-1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 37:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicate(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in or not in
# Built-in functions: list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicate(lst) runs first → then print() outputs the result
#   remove_duplicates(input_string) runs first → then print() outputs the result
#   remove_dupicates(lst) runs first → then print() outputs the result
#   remove_duplicates('hello') runs first → then print() outputs the result
#   remove_duplicates('aabbcc') runs first → then print() outputs the result
#   remove_duplicate(strings) → each argument is evaluated first, then the function body runs
#   remove_duplicate('world') runs first → then print() outputs the result
#   remove_duplicate(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates(input_string) → each argument is evaluated first, then the function body runs
#   remove_dupicates(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates('hello') → each argument is evaluated first, then the function body runs
#   remove_duplicates('aabbcc') → each argument is evaluated first, then the function body runs
#   remove_duplicate('world') → each argument is evaluated first, then the function body runs
#   char not in unique_chars → evaluated as True or False (determines which branch runs)
#   number not in duplicates → evaluated as True or False (determines which branch runs)
#   c in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicate(strings) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_duplicate(...))
#      → calls remove_duplicate(strings) with strings=lst
#      → 3.1 return list(set(strings))
#   4. def remove_duplicates(string) → stored in memory, not executed
#   5. input_string = ... → assigns value to variable
#   6. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string=input_string
#      → 6.1 seen = set()
#      → 6.2 return ''.join([c for c in string if not (c in seen or seen.add(c))])
#   7. def remove_dupicates(numbers) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. print(remove_dupicates(...))
#      → calls remove_dupicates(numbers) with numbers=lst
#      → 9.1 duplicates = []
#      → 9.2 for number in numbers:
#      → 9.2     if number not in duplicates:
#      → 9.2         duplicates.append(number)
#      → 9.3 return duplicates
#   10. def remove_duplicates(string) → stored in memory, not executed
#   11. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string='hello'
#      → 11.1 seen = set()
#      → 11.2 return ''.join([c for c in string if not (c in seen or seen.add(c))])
#   12. def remove_duplicates(string) → stored in memory, not executed
#   13. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string='aabbcc'
#      → 13.1 seen = set()
#      → 13.2 return ''.join([c for c in string if not (c in seen or seen.add(c))])
#   14. def remove_duplicate(strings) → stored in memory, not executed
#   15. def remove_duplicate(strings) → stored in memory, not executed
#   16. strings = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(remove_duplicate(...))
#      → calls remove_duplicate(strings) with strings='world'
#      → 19.1 return list(set(strings))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_dupicates, remove_duplicate, remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_duplicate(...)) → remove_duplicate() runs first → value passed to print()
#     input_string = ... → stored in memory
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(remove_dupicates(...)) → remove_dupicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicate(...)) → remove_duplicate() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 38:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_list() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: +
# Built-in functions: input, int, reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_list() → each argument is evaluated first, then the function body runs
#   sum_of_list(?) runs first → then print() outputs the result
#   sum_of_list(numbers) → each argument is evaluated first, then the function body runs
#   sum_of_list(?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_list() → stored in memory, not executed
#   2. sum_of_list() → calls sum_of_list() with 
#      → 2.1 user = input('type numbers: ').split()
#      → 2.2 int_number = []
#      → 2.3 for number in user:
#      → 2.3     int_number.append(int(number))
#      → 2.4 count = 0
#      → 2.5 for number in int_number:
#      → 2.5     count += number
#      → 2.6 result = count
#      → 2.7 return result
#   3. def sum_of_list(numbers) → stored in memory, not executed
#   4. print(sum_of_list(...))
#      → calls sum_of_list() with 
#      → 4.1 user = input('type numbers: ').split()
#      → 4.2 int_number = []
#      → 4.3 for number in user:
#      → 4.3     int_number.append(int(number))
#      → 4.4 count = 0
#      → 4.5 for number in int_number:
#      → 4.5     count += number
#      → 4.6 result = count
#      → 4.7 return result
#   5. def sum_of_list(numbers) → stored in memory, not executed
#   6. print(sum_of_list(...))
#      → calls sum_of_list() with 
#      → 6.1 user = input('type numbers: ').split()
#      → 6.2 int_number = []
#      → 6.3 for number in user:
#      → 6.3     int_number.append(int(number))
#      → 6.4 count = 0
#      → 6.5 for number in int_number:
#      → 6.5     count += number
#      → 6.6 result = count
#      → 6.7 return result
#   7. def sum_of_list() → stored in memory, not executed
#   8. def sum_of_list() → stored in memory, not executed
#   9. numbers = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(sum_of_list(...))
#      → calls sum_of_list() with 
#      → 12.1 user = input('type numbers: ').split()
#      → 12.2 int_number = []
#      → 12.3 for number in user:
#      → 12.3     int_number.append(int(number))
#      → 12.4 count = 0
#      → 12.5 for number in int_number:
#      → 12.5     count += number
#      → 12.6 result = count
#      → 12.7 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sum_of_list() → jumps into sum_of_list(), runs body, returns
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done

"""

"""
Problem 39:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_list(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_list(lst) runs first → then print() outputs the result
#   sum_of_list(?) runs first → then print() outputs the result
#   sum_of_list(numbers) → each argument is evaluated first, then the function body runs
#   sum_of_list(lst) → each argument is evaluated first, then the function body runs
#   sum_of_list(?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_list(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sum_of_list(...))
#      → calls sum_of_list(numbers) with numbers=lst
#      → 3.1 return count
#   4. def sum_of_list(numbers) → stored in memory, not executed
#   5. print(sum_of_list(...))
#      → calls sum_of_list(numbers) with numbers=[1, 2, 3, 4, 5]
#      → 5.1 return count
#   6. def sum_of_list(numbers) → stored in memory, not executed
#   7. print(sum_of_list(...))
#      → calls sum_of_list(numbers) with numbers=[10, 20, 30]
#      → 7.1 return count
#   8. def sum_of_list(numbers) → stored in memory, not executed
#   9. def sum_of_list(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(sum_of_list(...))
#      → calls sum_of_list(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 40:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min_max(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: max, min, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min_max(lst) runs first → then print() outputs the result
#   find_min_max(?) runs first → then print() outputs the result
#   find_min_max(numbers) → each argument is evaluated first, then the function body runs
#   find_min_max(lst) → each argument is evaluated first, then the function body runs
#   find_min_max(?) → each argument is evaluated first, then the function body runs
#   number < min_number → evaluated as True or False (determines which branch runs)
#   number > max_number → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min_max(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_min_max(...))
#      → calls find_min_max(numbers) with numbers=lst
#      → 3.1 return (min_number, max_number)
#   4. def find_min_max(numbers) → stored in memory, not executed
#   5. print(find_min_max(...))
#      → calls find_min_max(numbers) with numbers=[5, 2, 9, 1, 7]
#      → 5.1 return (min_number, max_number)
#   6. def find_min_max(numbers) → stored in memory, not executed
#   7. print(find_min_max(...))
#      → calls find_min_max(numbers) with numbers=[100, 50, 75]
#      → 7.1 return (min_number, max_number)
#   8. def find_min_max(numbers) → stored in memory, not executed
#   9. def find_min_max(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(find_min_max(...))
#      → calls find_min_max(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return (min_number, max_number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_min_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 41:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_occurrences() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: input, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_occurrences() → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 'Tom') runs first → then print() outputs the result
#   count_occurrences(?, 2) runs first → then print() outputs the result
#   count_occurrences(lst, target) → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 'goal') runs first → then print() outputs the result
#   count_occurrences(?, 'Tom') → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 2) → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 'goal') → each argument is evaluated first, then the function body runs
#   word == 'Tom' → evaluated as True or False (determines which branch runs)
#   item == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_occurrences() → stored in memory, not executed
#   2. count_occurrences() → calls count_occurrences() with 
#      → 2.1 return count
#   3. def count_occurrences(lst, target) → stored in memory, not executed
#   4. print(count_occurrences(...))
#      → calls count_occurrences() with 
#      → 4.1 return count
#   5. def count_occurrences(lst, target) → stored in memory, not executed
#   6. print(count_occurrences(...))
#      → calls count_occurrences() with 
#      → 6.1 return count
#   7. def count_occurrences() → stored in memory, not executed
#   8. def count_occurrences() → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. target = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_occurrences(...))
#      → calls count_occurrences() with 
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     count_occurrences() → jumps into count_occurrences(), runs body, returns
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     lst = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 43:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_elements(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_elements(lst) runs first → then print() outputs the result
#   square_elements(?) runs first → then print() outputs the result
#   square_elements(numbers) → each argument is evaluated first, then the function body runs
#   square_elements(lst) → each argument is evaluated first, then the function body runs
#   square_elements(?) → each argument is evaluated first, then the function body runs
#   n ** 2 → Python looks up n, then 2, then applies **
#   number ** 2 → Python looks up number, then 2, then applies **
#   x ** 2 → Python looks up x, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_elements(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(square_elements(...))
#      → calls square_elements(numbers) with numbers=lst
#      → 3.1 return list(map(lambda number: number ** 2, numbers))
#   4. def square_elements(numbers) → stored in memory, not executed
#   5. print(square_elements(...))
#      → calls square_elements(numbers) with numbers=[1, 2, 3, 4]
#      → 5.1 return list(map(lambda number: number ** 2, numbers))
#   6. def square_elements(numbers) → stored in memory, not executed
#   7. print(square_elements(...))
#      → calls square_elements(numbers) with numbers=[5, 6, 7]
#      → 7.1 return list(map(lambda number: number ** 2, numbers))
#   8. def square_elements(numbers) → stored in memory, not executed
#   9. def square_elements(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(square_elements(...))
#      → calls square_elements(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return list(map(lambda number: number ** 2, numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(square_elements(...)) → square_elements() runs first → value passed to print()
#     print(square_elements(...)) → square_elements() runs first → value passed to print()
#     print(square_elements(...)) → square_elements() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_elements(...)) → square_elements() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 44:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_uppercase(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: filter, len, list, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_uppercase(lst) runs first → then print() outputs the result
#   count_uppercase(string) runs first → then print() outputs the result
#   count_uppercase('Hello World') runs first → then print() outputs the result
#   count_uppercase('Python Programming') runs first → then print() outputs the result
#   count_uppercase(string) → each argument is evaluated first, then the function body runs
#   count_uppercase('world') runs first → then print() outputs the result
#   count_uppercase(lst) → each argument is evaluated first, then the function body runs
#   count_uppercase('Hello World') → each argument is evaluated first, then the function body runs
#   count_uppercase('Python Programming') → each argument is evaluated first, then the function body runs
#   count_uppercase('world') → each argument is evaluated first, then the function body runs
#   char == char.upper(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_uppercase(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(count_uppercase(...))
#      → calls count_uppercase(string) with string=lst
#      → 3.1 return len([char for char in string if char.isupper()])
#   4. def count_uppercase(string) → stored in memory, not executed
#   5. string = ... → assigns value to variable
#   6. print(count_uppercase(...))
#      → calls count_uppercase(string) with string=string
#      → 6.1 return len([char for char in string if char.isupper()])
#   7. def count_uppercase(string) → stored in memory, not executed
#   8. print(count_uppercase(...))
#      → calls count_uppercase(string) with string='Hello World'
#      → 8.1 return len([char for char in string if char.isupper()])
#   9. def count_uppercase(string) → stored in memory, not executed
#   10. print(count_uppercase(...))
#      → calls count_uppercase(string) with string='Python Programming'
#      → 10.1 return len([char for char in string if char.isupper()])
#   11. def count_uppercase(string) → stored in memory, not executed
#   12. def count_uppercase(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(count_uppercase(...))
#      → calls count_uppercase(string) with string='world'
#      → 16.1 return len([char for char in string if char.isupper()])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#     string = ... → stored in memory
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 45:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_spaces(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: !=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_spaces(lst) runs first → then print() outputs the result
#   remove_spaces('hello world') runs first → then print() outputs the result
#   remove_spaces('a b c') runs first → then print() outputs the result
#   remove_spaces(string) → each argument is evaluated first, then the function body runs
#   remove_spaces('world') runs first → then print() outputs the result
#   remove_spaces(lst) → each argument is evaluated first, then the function body runs
#   remove_spaces('hello world') → each argument is evaluated first, then the function body runs
#   remove_spaces('a b c') → each argument is evaluated first, then the function body runs
#   remove_spaces('world') → each argument is evaluated first, then the function body runs
#   char != ' ' → evaluated as True or False (determines which branch runs)
#   c != ' ' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_spaces(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_spaces(...))
#      → calls remove_spaces(string) with string=lst
#      → 3.1 return string.replace(' ', '')
#   4. def remove_spaces(string) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(remove_spaces(...))
#      → calls remove_spaces(string) with string=lst
#      → 6.1 return string.replace(' ', '')
#   7. def remove_spaces(string) → stored in memory, not executed
#   8. print(remove_spaces(...))
#      → calls remove_spaces(string) with string='hello world'
#      → 8.1 return string.replace(' ', '')
#   9. def remove_spaces(string) → stored in memory, not executed
#   10. print(remove_spaces(...))
#      → calls remove_spaces(string) with string='a b c'
#      → 10.1 return string.replace(' ', '')
#   11. def remove_spaces(string) → stored in memory, not executed
#   12. def remove_spaces(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(remove_spaces(...))
#      → calls remove_spaces(string) with string='world'
#      → 16.1 return string.replace(' ', '')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_spaces) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_spaces(...)) → remove_spaces() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 46:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_lists() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: +
# Built-in functions: input, list, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_lists() → each argument is evaluated first, then the function body runs
#   merge_lists(?, ?) runs first → then print() outputs the result
#   merge_lists(lst1, lst2) → each argument is evaluated first, then the function body runs
#   merge_lists(?, ?) → each argument is evaluated first, then the function body runs
#   lst1 + lst2 → Python looks up lst1, then lst2, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_lists() → stored in memory, not executed
#   2. merge_lists() → calls merge_lists() with 
#      → 2.1 lst1 = sorted(input('Type the first sorted list: ').split())
#      → 2.2 lst2 = sorted(input('Type the second sorted list: ').split())
#      → 2.3 merged_list = sorted(lst1 + lst2)
#      → 2.4 result = merged_list
#      → 2.5 return result
#   3. def merge_lists(lst1, lst2) → stored in memory, not executed
#   4. print(merge_lists(...))
#      → calls merge_lists() with 
#      → 4.1 lst1 = sorted(input('Type the first sorted list: ').split())
#      → 4.2 lst2 = sorted(input('Type the second sorted list: ').split())
#      → 4.3 merged_list = sorted(lst1 + lst2)
#      → 4.4 result = merged_list
#      → 4.5 return result
#   5. def merge_lists(lst1, lst2) → stored in memory, not executed
#   6. print(merge_lists(...))
#      → calls merge_lists() with 
#      → 6.1 lst1 = sorted(input('Type the first sorted list: ').split())
#      → 6.2 lst2 = sorted(input('Type the second sorted list: ').split())
#      → 6.3 merged_list = sorted(lst1 + lst2)
#      → 6.4 result = merged_list
#      → 6.5 return result
#   7. def merge_lists() → stored in memory, not executed
#   8. def merge_lists() → stored in memory, not executed
#   9. lst1 = ... → assigns value to variable
#   10. lst2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(merge_lists(...))
#      → calls merge_lists() with 
#      → 13.1 lst1 = sorted(input('Type the first sorted list: ').split())
#      → 13.2 lst2 = sorted(input('Type the second sorted list: ').split())
#      → 13.3 merged_list = sorted(lst1 + lst2)
#      → 13.4 result = merged_list
#      → 13.5 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     merge_lists() → jumps into merge_lists(), runs body, returns
#     print(merge_lists(...)) → merge_lists() runs first → value passed to print()
#     print(merge_lists(...)) → merge_lists() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_lists(...)) → merge_lists() runs first → value passed to print()

"""

"""
Problem 47:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_index(numbers, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | try/except
# Operators: ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_index(numbers, target) runs first → then print() outputs the result
#   find_index(?, 30) runs first → then print() outputs the result
#   find_index(?, 50) runs first → then print() outputs the result
#   find_index(?, 15) runs first → then print() outputs the result
#   find_index(numbers, target) → each argument is evaluated first, then the function body runs
#   find_index(?, 'goal') runs first → then print() outputs the result
#   find_index(?, 30) → each argument is evaluated first, then the function body runs
#   find_index(?, 50) → each argument is evaluated first, then the function body runs
#   find_index(?, 15) → each argument is evaluated first, then the function body runs
#   find_index(?, 'goal') → each argument is evaluated first, then the function body runs
#   num == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_index(numbers, target) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. print('Index of', target, 'in nums:', find_index(...))
#   5. target = ... → assigns value to variable
#   6. print('Index of', target, 'in nums:', find_index(...))
#   7. def find_index(numbers, target) → stored in memory, not executed
#   8. print(find_index(...))
#      → calls find_index(numbers, target) with numbers=[10, 20, 30, 40], target=30
#      → 8.1 return index
#   9. print(find_index(...))
#      → calls find_index(numbers, target) with numbers=[10, 20, 30, 40], target=50
#      → 9.1 return index
#   10. def find_index(numbers, target) → stored in memory, not executed
#   11. print(find_index(...))
#      → calls find_index(numbers, target) with numbers=[5, 10, 15, 20], target=15
#      → 11.1 return index
#   12. def find_index(numbers, target) → stored in memory, not executed
#   13. def find_index(numbers, target) → stored in memory, not executed
#   14. numbers = ... → assigns value to variable
#   15. target = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(find_index(...))
#      → calls find_index(numbers, target) with numbers=[4, 1, 8, 6, 3], target='goal'
#      → 18.1 return index
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     target = ... → stored in memory
#     print('Index of', target, 'in nums:', find_index(...))
#     target = ... → stored in memory
#     print('Index of', target, 'in nums:', find_index(...))
#     print(find_index(...)) → find_index() runs first → value passed to print()
#     print(find_index(...)) → find_index() runs first → value passed to print()
#     print(find_index(...)) → find_index() runs first → value passed to print()
#     numbers = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_index(...)) → find_index() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 48:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_even_indices(nums) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: enumerate, len, range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_even_indices(numbers) runs first → then print() outputs the result
#   sum_even_indices(?) runs first → then print() outputs the result
#   sum_even_indices(nums) → each argument is evaluated first, then the function body runs
#   sum_even_indices(numbers) → each argument is evaluated first, then the function body runs
#   sum_even_indices(?) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   i % 2 → Python looks up i, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_even_indices(nums) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print('Sum of elements at even indices:', sum_even_indices(...))
#   4. def sum_even_indices(nums) → stored in memory, not executed
#   5. numbers = ... → assigns value to variable
#   6. print('Sum of elements at even indices:', sum_even_indices(...))
#   7. def sum_even_indices(nums) → stored in memory, not executed
#   8. print(sum_even_indices(...))
#      → calls sum_even_indices(nums) with nums=[10, 20, 30, 40, 50]
#      → 8.1 return total
#   9. def sum_even_indices(nums) → stored in memory, not executed
#   10. print(sum_even_indices(...))
#      → calls sum_even_indices(nums) with nums=[1, 2, 3, 4, 5]
#      → 10.1 return total
#   11. def sum_even_indices(nums) → stored in memory, not executed
#   12. def sum_even_indices(nums) → stored in memory, not executed
#   13. nums = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(sum_even_indices(...))
#      → calls sum_even_indices(nums) with nums=[4, 1, 8, 6, 3]
#      → 16.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_even_indices) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print('Sum of elements at even indices:', sum_even_indices(...))
#     numbers = ... → stored in memory
#     print('Sum of elements at even indices:', sum_even_indices(...))
#     print(sum_even_indices(...)) → sum_even_indices() runs first → value passed to print()
#     print(sum_even_indices(...)) → sum_even_indices() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_even_indices(...)) → sum_even_indices() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 49:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: and not in
# Built-in functions: list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates(lst) runs first → then print() outputs the result
#   remove_duplicates(input_strings) runs first → then print() outputs the result
#   remove_duplicates(?) runs first → then print() outputs the result
#   remove_duplicates(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates(input_strings) → each argument is evaluated first, then the function body runs
#   remove_duplicates(?) → each argument is evaluated first, then the function body runs
#   word_lower not in unique → evaluated as True or False (determines which branch runs)
#   string.lower(...) not in map(...) → evaluated as True or False (determines which branch runs)
#   lower_word not in seen → evaluated as True or False (determines which branch runs)
#   x.lower(...) not in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word_lower, lst))
#   4. def remove_duplicates(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=lst
#      → 6.1 return list(map(lambda word: word_lower, lst))
#   7. def remove_duplicates(strings) → stored in memory, not executed
#   8. input_strings = ... → assigns value to variable
#   9. print('List with case-insensitive duplicates removed:', remove_duplicates(...))
#   10. def remove_duplicates(lst) → stored in memory, not executed
#   11. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=['Apple', 'APPLE', 'banana', 'BANANA']
#      → 11.1 return list(map(lambda word: word_lower, lst))
#   12. def remove_duplicates(lst) → stored in memory, not executed
#   13. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=['Hello', 'HELLO', 'World']
#      → 13.1 return list(map(lambda word: word_lower, lst))
#   14. def remove_duplicates(lst) → stored in memory, not executed
#   15. def remove_duplicates(lst) → stored in memory, not executed
#   16. lst = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=[1, 3, 5, 7]
#      → 19.1 return list(map(lambda word: word_lower, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     input_strings = ... → stored in memory
#     print('List with case-insensitive duplicates removed:', remove_duplicates(...))
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 50:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: - < != ==
# Built-in functions: len, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_palindrome(input_string) runs first → then print() outputs the result
#   is_palindrome('madam') runs first → then print() outputs the result
#   is_palindrome('python') runs first → then print() outputs the result
#   is_palindrome('level') runs first → then print() outputs the result
#   is_palindrome('world') runs first → then print() outputs the result
#   is_palindrome(s) → each argument is evaluated first, then the function body runs
#   is_palindrome(?) runs first → then print() outputs the result
#   s == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome(input_string) → each argument is evaluated first, then the function body runs
#   left < right → evaluated as True or False (determines which branch runs)
#   is_palindrome('madam') → each argument is evaluated first, then the function body runs
#   is_palindrome('python') → each argument is evaluated first, then the function body runs
#   s == ''.join(...) → evaluated as True or False (determines which branch runs)
#   is_palindrome('level') → each argument is evaluated first, then the function body runs
#   is_palindrome('world') → each argument is evaluated first, then the function body runs
#   is_palindrome(?) → each argument is evaluated first, then the function body runs
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? != ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(s) → stored in memory, not executed
#   2. input_string = ... → assigns value to variable
#   3. print('Is', input_string, 'a palindrome?', is_palindrome(...))
#   4. input_string = ... → assigns value to variable
#   5. print('Is', input_string, 'a palindrome?', is_palindrome(...))
#   6. def is_palindrome(s) → stored in memory, not executed
#   7. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='madam'
#      → 7.1 return s == s[::-1]
#   8. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='python'
#      → 8.1 return s == s[::-1]
#   9. def is_palindrome(s) → stored in memory, not executed
#   10. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='level'
#      → 10.1 return s == s[::-1]
#   11. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='world'
#      → 11.1 return s == s[::-1]
#   12. def is_palindrome(s) → stored in memory, not executed
#   13. def is_palindrome(s) → stored in memory, not executed
#   14. s = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(is_palindrome(...))
#      → calls is_palindrome(s) with s=[4, 1, 8, 6, 3]
#      → 17.1 return s == s[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_string = ... → stored in memory
#     print('Is', input_string, 'a palindrome?', is_palindrome(...))
#     input_string = ... → stored in memory
#     print('Is', input_string, 'a palindrome?', is_palindrome(...))
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 51:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: * + - ==
# Built-in functions: range, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial(0) runs first → then print() outputs the result
#   factorial(5) runs first → then print() outputs the result
#   factorial(6) runs first → then print() outputs the result
#   factorial(4) runs first → then print() outputs the result
#   factorial(n) → each argument is evaluated first, then the function body runs
#   factorial(15) runs first → then print() outputs the result
#   n == 0 → evaluated as True or False (determines which branch runs)
#   factorial(0) → each argument is evaluated first, then the function body runs
#   factorial(5) → each argument is evaluated first, then the function body runs
#   factorial(6) → each argument is evaluated first, then the function body runs
#   factorial(4) → each argument is evaluated first, then the function body runs
#   factorial(15) → each argument is evaluated first, then the function body runs
#   n * factorial(...) → Python looks up n, then factorial(...), then applies *
#   n + 1 → Python looks up n, then 1, then applies +
#   factorial(n - 1) → each argument is evaluated first, then the function body runs
#   x * y → Python looks up x, then y, then applies *
#   n - 1 → Python looks up n, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial(n) → stored in memory, not executed
#   2. print(factorial(...))
#      → calls factorial(n) with n=0
#      → 2.1 return 1
#   3. def factorial(n) → stored in memory, not executed
#   4. print(factorial(...))
#      → calls factorial(n) with n=5
#      → 4.1 return 1
#   5. def factorial(n) → stored in memory, not executed
#   6. print(factorial(...))
#      → calls factorial(n) with n=5
#      → 6.1 return 1
#   7. def factorial(n) → stored in memory, not executed
#   8. print(factorial(...))
#      → calls factorial(n) with n=6
#      → 8.1 return 1
#   9. def factorial(n) → stored in memory, not executed
#   10. print(factorial(...))
#      → calls factorial(n) with n=4
#      → 10.1 return 1
#   11. def factorial(n) → stored in memory, not executed
#   12. def factorial(n) → stored in memory, not executed
#   13. n = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(factorial(...))
#      → calls factorial(n) with n=15
#      → 16.1 return 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial(...)) → factorial() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 52:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: != ==
# Built-in functions: Counter, input, len, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   is_anagram('listen', 'silent') runs first → then print() outputs the result
#   is_anagram('hello', 'world') runs first → then print() outputs the result
#   is_anagram('Triangle', 'Integral') runs first → then print() outputs the result
#   is_anagram(lst1, lst2) runs first → then print() outputs the result
#   is_anagram('hello', 'olleh') runs first → then print() outputs the result
#   is_anagram(str1, str2) → each argument is evaluated first, then the function body runs
#   is_anagram('world', 'python') runs first → then print() outputs the result
#   sorted1 == sorted2 → evaluated as True or False (determines which branch runs)
#   sorted(...) == sorted(...) → evaluated as True or False (determines which branch runs)
#   is_anagram('listen', 'silent') → each argument is evaluated first, then the function body runs
#   is_anagram('hello', 'world') → each argument is evaluated first, then the function body runs
#   is_anagram('Triangle', 'Integral') → each argument is evaluated first, then the function body runs
#   sorted_lst1 == sorted_lst2 → evaluated as True or False (determines which branch runs)
#   is_anagram(lst1, lst2) → each argument is evaluated first, then the function body runs
#   Counter(...) == Counter(...) → evaluated as True or False (determines which branch runs)
#   len(...) != len(...) → evaluated as True or False (determines which branch runs)
#   is_anagram('hello', 'olleh') → each argument is evaluated first, then the function body runs
#   is_anagram('world', 'python') → each argument is evaluated first, then the function body runs
#   str1.count(...) != str2.count(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main() → stored in memory, not executed
#   2. main() → calls main() with 
#      → 2.1 return True
#   3. def is_anagram(str1, str2) → stored in memory, not executed
#   4. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1='listen', str2='silent'
#      → 4.1 str1, str2 = (str1.lower(), str2.lower())
#      → 4.2 if len(str1) != len(str2):
#      → 4.2     return False
#      → 4.3 for char in str1:
#      → 4.3     if str1.count(char) != str2.count(char):
#      → 4.3         return False
#      → 4.4 return True
#   5. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1='hello', str2='world'
#      → 5.1 str1, str2 = (str1.lower(), str2.lower())
#      → 5.2 if len(str1) != len(str2):
#      → 5.2     return False
#      → 5.3 for char in str1:
#      → 5.3     if str1.count(char) != str2.count(char):
#      → 5.3         return False
#      → 5.4 return True
#   6. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1='Triangle', str2='Integral'
#      → 6.1 str1, str2 = (str1.lower(), str2.lower())
#      → 6.2 if len(str1) != len(str2):
#      → 6.2     return False
#      → 6.3 for char in str1:
#      → 6.3     if str1.count(char) != str2.count(char):
#      → 6.3         return False
#      → 6.4 return True
#   7. def is_anagram(lst1, lst2) → stored in memory, not executed
#   8. lst1 = ... → assigns value to variable
#   9. lst2 = ... → assigns value to variable
#   10. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1=lst1, str2=lst2
#      → 10.1 str1, str2 = (str1.lower(), str2.lower())
#      → 10.2 if len(str1) != len(str2):
#      → 10.2     return False
#      → 10.3 for char in str1:
#      → 10.3     if str1.count(char) != str2.count(char):
#      → 10.3         return False
#      → 10.4 return True
#   11. def is_anagram(str1, str2) → stored in memory, not executed
#   12. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1='listen', str2='silent'
#      → 12.1 str1, str2 = (str1.lower(), str2.lower())
#      → 12.2 if len(str1) != len(str2):
#      → 12.2     return False
#      → 12.3 for char in str1:
#      → 12.3     if str1.count(char) != str2.count(char):
#      → 12.3         return False
#      → 12.4 return True
#   13. def is_anagram(str1, str2) → stored in memory, not executed
#   14. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1='hello', str2='olleh'
#      → 14.1 str1, str2 = (str1.lower(), str2.lower())
#      → 14.2 if len(str1) != len(str2):
#      → 14.2     return False
#      → 14.3 for char in str1:
#      → 14.3     if str1.count(char) != str2.count(char):
#      → 14.3         return False
#      → 14.4 return True
#   15. def main() → stored in memory, not executed
#   16. def main() → stored in memory, not executed
#   17. str1 = ... → assigns value to variable
#   18. str2 = ... → assigns value to variable
#   19. result = ... → assigns value to variable
#   20. print(result)
#   21. print(is_anagram(...))
#      → calls is_anagram(str1, str2) with str1='world', str2='python'
#      → 21.1 str1, str2 = (str1.lower(), str2.lower())
#      → 21.2 if len(str1) != len(str2):
#      → 21.2     return False
#      → 21.3 for char in str1:
#      → 21.3     if str1.count(char) != str2.count(char):
#      → 21.3         return False
#      → 21.4 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, is_anagram) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     main() → jumps into main(), runs body, returns
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#
#   Loop: takes one item from str1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 53:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + in
# Built-in functions: Counter, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words(input_string) runs first → then print() outputs the result
#   count_words('hello world hello') runs first → then print() outputs the result
#   count_words('one two one') runs first → then print() outputs the result
#   count_words(s) → each argument is evaluated first, then the function body runs
#   count_words('world') runs first → then print() outputs the result
#   count_words(input_string) → each argument is evaluated first, then the function body runs
#   count_words('hello world hello') → each argument is evaluated first, then the function body runs
#   count_words('one two one') → each argument is evaluated first, then the function body runs
#   count_words('world') → each argument is evaluated first, then the function body runs
#   word in word_count → evaluated as True or False (determines which branch runs)
#   word_count.get(...) + 1 → Python looks up word_count.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words(s) → stored in memory, not executed
#   2. input_string = ... → assigns value to variable
#   3. print(count_words(...))
#      → calls count_words(s) with s=input_string
#      → 3.1 return word_count
#   4. def count_words(s) → stored in memory, not executed
#   5. print(count_words(...))
#      → calls count_words(s) with s='hello world hello'
#      → 5.1 return word_count
#   6. def count_words(s) → stored in memory, not executed
#   7. print(count_words(...))
#      → calls count_words(s) with s='one two one'
#      → 7.1 return word_count
#   8. def count_words(s) → stored in memory, not executed
#   9. def count_words(s) → stored in memory, not executed
#   10. s = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_words(...))
#      → calls count_words(s) with s='world'
#      → 13.1 return word_count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_string = ... → stored in memory
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words(...)) → count_words() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 54:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fibonacci(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - <= ==
# Built-in functions: list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fibonacci(10) runs first → then print() outputs the result
#   fibonacci(7) runs first → then print() outputs the result
#   fibonacci(n) → each argument is evaluated first, then the function body runs
#   fibonacci(15) runs first → then print() outputs the result
#   n <= 0 → evaluated as True or False (determines which branch runs)
#   fibonacci(10) → each argument is evaluated first, then the function body runs
#   fibonacci(7) → each argument is evaluated first, then the function body runs
#   fibonacci(15) → each argument is evaluated first, then the function body runs
#   n == 1 → evaluated as True or False (determines which branch runs)
#   ? + ? → Python looks up ?, then ?, then applies +
#   fibonacci(8) → each argument is evaluated first, then the function body runs
#   n == 2 → evaluated as True or False (determines which branch runs)
#   a + b → Python looks up a, then b, then applies +
#   fibonacci(n - 1) → each argument is evaluated first, then the function body runs
#   n - 1 → Python looks up n, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fibonacci(n) → stored in memory, not executed
#   2. print(fibonacci(...))
#      → calls fibonacci(n) with n=10
#      → 2.1 return []
#   3. def fibonacci(n) → stored in memory, not executed
#   4. print(fibonacci(...))
#      → calls fibonacci(n) with n=7
#      → 4.1 return []
#   5. def fibonacci(n) → stored in memory, not executed
#   6. print(list(...))
#   7. def fibonacci(n) → stored in memory, not executed
#   8. def fibonacci(n) → stored in memory, not executed
#   9. n = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(fibonacci(...))
#      → calls fibonacci(n) with n=15
#      → 12.1 return []
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fibonacci) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#     print(list(...))
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 55:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def area_rectangle() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
# Built-in functions: float, input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   area_rectangle() → each argument is evaluated first, then the function body runs
#   length * width → Python looks up length, then width, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def area_rectangle() → stored in memory, not executed
#   2. area_rectangle() → calls area_rectangle() with 
#      → 2.1 length = float(input('Enter length: '))
#      → 2.2 width = float(input('Enter width: '))
#      → 2.3 result = length * width
#      → 2.4 return result
#   3. def area_rectangle() → stored in memory, not executed
#   4. def area_rectangle() → stored in memory, not executed
#   5. length = ... → assigns value to variable
#   6. width = ... → assigns value to variable
#   7. print(length * width)
#   8. length = ... → assigns value to variable
#   9. width = ... → assigns value to variable
#   10. print(length * width)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (area_rectangle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     area_rectangle() → jumps into area_rectangle(), runs body, returns
#     length = ... → stored in memory
#     width = ... → stored in memory
#     print(length * width)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     print(length * width)

"""

"""
Problem 56:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def even_odd() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else
# Operators: % & ==
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   even_odd() → each argument is evaluated first, then the function body runs
#   even_odd(10) runs first → then print() outputs the result
#   even_odd(7) runs first → then print() outputs the result
#   even_odd(8) runs first → then print() outputs the result
#   even_odd(11) runs first → then print() outputs the result
#   even_odd(n) → each argument is evaluated first, then the function body runs
#   even_odd(15) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   even_odd(10) → each argument is evaluated first, then the function body runs
#   even_odd(7) → each argument is evaluated first, then the function body runs
#   even_odd(8) → each argument is evaluated first, then the function body runs
#   even_odd(11) → each argument is evaluated first, then the function body runs
#   even_odd(15) → each argument is evaluated first, then the function body runs
#   user % 2 → Python looks up user, then 2, then applies %
#   n % 2 → Python looks up n, then 2, then applies %
#   n & 1 → Python looks up n, then 1, then applies &
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def even_odd() → stored in memory, not executed
#   2. even_odd() → calls even_odd(n) with 
#      → 2.1 return 'Even' if n & 1 == 0 else 'Odd'
#   3. def even_odd(n) → stored in memory, not executed
#   4. print(even_odd(...))
#      → calls even_odd(n) with n=10
#      → 4.1 return 'Even' if n & 1 == 0 else 'Odd'
#   5. print(even_odd(...))
#      → calls even_odd(n) with n=7
#      → 5.1 return 'Even' if n & 1 == 0 else 'Odd'
#   6. def even_odd(n) → stored in memory, not executed
#   7. print(even_odd(...))
#      → calls even_odd(n) with n=8
#      → 7.1 return 'Even' if n & 1 == 0 else 'Odd'
#   8. print(even_odd(...))
#      → calls even_odd(n) with n=11
#      → 8.1 return 'Even' if n & 1 == 0 else 'Odd'
#   9. n = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(even_odd(...))
#      → calls even_odd(n) with n=15
#      → 12.1 return 'Even' if n & 1 == 0 else 'Odd'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (even_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     even_odd() → jumps into even_odd(), runs body, returns
#     print(even_odd(...)) → even_odd() runs first → value passed to print()
#     print(even_odd(...)) → even_odd() runs first → value passed to print()
#     print(even_odd(...)) → even_odd() runs first → value passed to print()
#     print(even_odd(...)) → even_odd() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(even_odd(...)) → even_odd() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 57:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fibonacci_series() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: + <=
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fibonacci_series() → each argument is evaluated first, then the function body runs
#   fibonacci_series(50) runs first → then print() outputs the result
#   fibonacci_series(10) runs first → then print() outputs the result
#   fibonacci_series(limit) → each argument is evaluated first, then the function body runs
#   fibonacci_series(25) runs first → then print() outputs the result
#   a <= limit → evaluated as True or False (determines which branch runs)
#   fibonacci_series(50) → each argument is evaluated first, then the function body runs
#   fibonacci_series(10) → each argument is evaluated first, then the function body runs
#   fibonacci_series(25) → each argument is evaluated first, then the function body runs
#   a + b → Python looks up a, then b, then applies +
#   ? + ? → Python looks up ?, then ?, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fibonacci_series() → stored in memory, not executed
#   2. fibonacci_series() → calls fibonacci_series(n) with 
#      → 2.1 fib = [0, 1]
#      → 2.2 [fib.append(fib[-1] + fib[-2]) for _ in range(2, n)]
#      → 2.3 return fib[:n]
#   3. def fibonacci_series(limit) → stored in memory, not executed
#   4. print(fibonacci_series(...))
#      → calls fibonacci_series(n) with n=50
#      → 4.1 fib = [0, 1]
#      → 4.2 [fib.append(fib[-1] + fib[-2]) for _ in range(2, n)]
#      → 4.3 return fib[:n]
#   5. def fibonacci_series(n) → stored in memory, not executed
#   6. print(fibonacci_series(...))
#      → calls fibonacci_series(n) with n=10
#      → 6.1 fib = [0, 1]
#      → 6.2 [fib.append(fib[-1] + fib[-2]) for _ in range(2, n)]
#      → 6.3 return fib[:n]
#   7. limit = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(fibonacci_series(...))
#      → calls fibonacci_series(n) with n=25
#      → 10.1 fib = [0, 1]
#      → 10.2 [fib.append(fib[-1] + fib[-2]) for _ in range(2, n)]
#      → 10.3 return fib[:n]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fibonacci_series) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     fibonacci_series() → jumps into fibonacci_series(), runs body, returns
#     print(fibonacci_series(...)) → fibonacci_series() runs first → value passed to print()
#     print(fibonacci_series(...)) → fibonacci_series() runs first → value passed to print()
#     limit = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fibonacci_series(...)) → fibonacci_series() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 58:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_number() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: > >= and
# Built-in functions: input, int, max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_number() → each argument is evaluated first, then the function body runs
#   max_number(10, 25, 15) runs first → then print() outputs the result
#   max_number(100, 50, 75) runs first → then print() outputs the result
#   max_number(?) runs first → then print() outputs the result
#   max_number(a, b, c) → each argument is evaluated first, then the function body runs
#   max_number(20, 30, ?) runs first → then print() outputs the result
#   max_number(10, 25, 15) → each argument is evaluated first, then the function body runs
#   max_number(100, 50, 75) → each argument is evaluated first, then the function body runs
#   max_number(?) → each argument is evaluated first, then the function body runs
#   max_number(20, 30, ?) → each argument is evaluated first, then the function body runs
#   number > max_num → evaluated as True or False (determines which branch runs)
#   a >= b → evaluated as True or False (determines which branch runs)
#   a >= c → evaluated as True or False (determines which branch runs)
#   b >= c → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_number() → stored in memory, not executed
#   2. max_number() → calls max_number() with 
#      → 2.1 user = input('type 3 numbers: ').split()
#      → 2.2 int_number = []
#      → 2.3 for number in user:
#      → 2.3     int_number.append(int(number))
#      → 2.4 max_num = int_number[0]
#      → 2.5 for number in int_number:
#      → 2.5     if number > max_num:
#      → 2.5         max_num = number
#      → 2.6 result = max_num
#      → 2.7 return result
#   3. def max_number(a, b, c) → stored in memory, not executed
#   4. print(max_number(...))
#      → calls max_number() with 
#      → 4.1 user = input('type 3 numbers: ').split()
#      → 4.2 int_number = []
#      → 4.3 for number in user:
#      → 4.3     int_number.append(int(number))
#      → 4.4 max_num = int_number[0]
#      → 4.5 for number in int_number:
#      → 4.5     if number > max_num:
#      → 4.5         max_num = number
#      → 4.6 result = max_num
#      → 4.7 return result
#   5. def max_number(a, b, c) → stored in memory, not executed
#   6. print(max_number(...))
#      → calls max_number() with 
#      → 6.1 user = input('type 3 numbers: ').split()
#      → 6.2 int_number = []
#      → 6.3 for number in user:
#      → 6.3     int_number.append(int(number))
#      → 6.4 max_num = int_number[0]
#      → 6.5 for number in int_number:
#      → 6.5     if number > max_num:
#      → 6.5         max_num = number
#      → 6.6 result = max_num
#      → 6.7 return result
#   7. def max_number(numbers) → stored in memory, not executed
#   8. print(max_number(...))
#      → calls max_number() with 
#      → 8.1 user = input('type 3 numbers: ').split()
#      → 8.2 int_number = []
#      → 8.3 for number in user:
#      → 8.3     int_number.append(int(number))
#      → 8.4 max_num = int_number[0]
#      → 8.5 for number in int_number:
#      → 8.5     if number > max_num:
#      → 8.5         max_num = number
#      → 8.6 result = max_num
#      → 8.7 return result
#   9. def max_number() → stored in memory, not executed
#   10. def max_number() → stored in memory, not executed
#   11. a = ... → assigns value to variable
#   12. b = ... → assigns value to variable
#   13. c = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(max_number(...))
#      → calls max_number() with 
#      → 16.1 user = input('type 3 numbers: ').split()
#      → 16.2 int_number = []
#      → 16.3 for number in user:
#      → 16.3     int_number.append(int(number))
#      → 16.4 max_num = int_number[0]
#      → 16.5 for number in int_number:
#      → 16.5     if number > max_num:
#      → 16.5         max_num = number
#      → 16.6 result = max_num
#      → 16.7 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     max_number() → jumps into max_number(), runs body, returns
#     print(max_number(...)) → max_number() runs first → value passed to print()
#     print(max_number(...)) → max_number() runs first → value passed to print()
#     print(max_number(...)) → max_number() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_number(...)) → max_number() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 59:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_string() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: +
# Built-in functions: input, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_string() → each argument is evaluated first, then the function body runs
#   reverse_string(lst) runs first → then print() outputs the result
#   reverse_string('python') runs first → then print() outputs the result
#   reverse_string('hello') runs first → then print() outputs the result
#   reverse_string(string) → each argument is evaluated first, then the function body runs
#   reverse_string('world') runs first → then print() outputs the result
#   reverse_string(lst) → each argument is evaluated first, then the function body runs
#   reverse_string('python') → each argument is evaluated first, then the function body runs
#   reverse_string('hello') → each argument is evaluated first, then the function body runs
#   reverse_string('world') → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_string() → stored in memory, not executed
#   2. reverse_string() → calls reverse_string() with 
#      → 2.1 user = input('type string: ')
#      → 2.2 user = user[::-1]
#      → 2.3 result = user
#      → 2.4 return result
#   3. def reverse_string(string) → stored in memory, not executed
#   4. lst = ... → assigns value to variable
#   5. print(reverse_string(...))
#      → calls reverse_string() with 
#      → 5.1 user = input('type string: ')
#      → 5.2 user = user[::-1]
#      → 5.3 result = user
#      → 5.4 return result
#   6. def reverse_string(s) → stored in memory, not executed
#   7. print(reverse_string(...))
#      → calls reverse_string() with 
#      → 7.1 user = input('type string: ')
#      → 7.2 user = user[::-1]
#      → 7.3 result = user
#      → 7.4 return result
#   8. def reverse_string(s) → stored in memory, not executed
#   9. print(reverse_string(...))
#      → calls reverse_string() with 
#      → 9.1 user = input('type string: ')
#      → 9.2 user = user[::-1]
#      → 9.3 result = user
#      → 9.4 return result
#   10. def reverse_string() → stored in memory, not executed
#   11. def reverse_string() → stored in memory, not executed
#   12. string = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(reverse_string(...))
#      → calls reverse_string() with 
#      → 15.1 user = input('type string: ')
#      → 15.2 user = user[::-1]
#      → 15.3 result = user
#      → 15.4 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     reverse_string() → jumps into reverse_string(), runs body, returns
#     lst = ... → stored in memory
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 60:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: + - < != // ==
# Built-in functions: all, input, len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_palindrome() runs first → then print() outputs the result
#   is_palindrome('racecar') runs first → then print() outputs the result
#   is_palindrome('hello') runs first → then print() outputs the result
#   is_palindrome('madam') runs first → then print() outputs the result
#   is_palindrome(s) → each argument is evaluated first, then the function body runs
#   is_palindrome('world') runs first → then print() outputs the result
#   user == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome() → each argument is evaluated first, then the function body runs
#   left < right → evaluated as True or False (determines which branch runs)
#   is_palindrome('racecar') → each argument is evaluated first, then the function body runs
#   is_palindrome('hello') → each argument is evaluated first, then the function body runs
#   is_palindrome('madam') → each argument is evaluated first, then the function body runs
#   is_palindrome('world') → each argument is evaluated first, then the function body runs
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? != ? → evaluated as True or False (determines which branch runs)
#   ? == ? → evaluated as True or False (determines which branch runs)
#   len(...) // 2 → Python looks up len(...), then 2, then applies //
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome() → stored in memory, not executed
#   2. print(is_palindrome(...))
#      → calls is_palindrome() with 
#      → 2.1 return user == user[::-1]
#   3. def is_palindrome(s) → stored in memory, not executed
#   4. print(is_palindrome(...))
#      → calls is_palindrome() with 
#      → 4.1 return user == user[::-1]
#   5. print(is_palindrome(...))
#      → calls is_palindrome() with 
#      → 5.1 return user == user[::-1]
#   6. def is_palindrome(s) → stored in memory, not executed
#   7. print(is_palindrome(...))
#      → calls is_palindrome() with 
#      → 7.1 return user == user[::-1]
#   8. def is_palindrome() → stored in memory, not executed
#   9. def is_palindrome() → stored in memory, not executed
#   10. s = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(is_palindrome(...))
#      → calls is_palindrome() with 
#      → 13.1 return user == user[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 61:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: input, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels() → each argument is evaluated first, then the function body runs
#   count_vowels('Python') runs first → then print() outputs the result
#   count_vowels('Hello World') runs first → then print() outputs the result
#   count_vowels('Programming') runs first → then print() outputs the result
#   count_vowels(s) → each argument is evaluated first, then the function body runs
#   count_vowels('world') runs first → then print() outputs the result
#   count_vowels('Python') → each argument is evaluated first, then the function body runs
#   count_vowels('Hello World') → each argument is evaluated first, then the function body runs
#   count_vowels('Programming') → each argument is evaluated first, then the function body runs
#   count_vowels('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#   c in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels() → stored in memory, not executed
#   2. count_vowels() → calls count_vowels() with 
#      → 2.1 user = input('type string: ').lower()
#      → 2.2 vowels = 'aeiou'
#      → 2.3 count = 0
#      → 2.4 for char in user:
#      → 2.4     if char in vowels:
#      → 2.4         count += 1
#      → 2.5 result = count
#      → 2.6 return result
#   3. def count_vowels(s) → stored in memory, not executed
#   4. print(count_vowels(...))
#      → calls count_vowels() with 
#      → 4.1 user = input('type string: ').lower()
#      → 4.2 vowels = 'aeiou'
#      → 4.3 count = 0
#      → 4.4 for char in user:
#      → 4.4     if char in vowels:
#      → 4.4         count += 1
#      → 4.5 result = count
#      → 4.6 return result
#   5. print(count_vowels(...))
#      → calls count_vowels() with 
#      → 5.1 user = input('type string: ').lower()
#      → 5.2 vowels = 'aeiou'
#      → 5.3 count = 0
#      → 5.4 for char in user:
#      → 5.4     if char in vowels:
#      → 5.4         count += 1
#      → 5.5 result = count
#      → 5.6 return result
#   6. def count_vowels(s) → stored in memory, not executed
#   7. print(count_vowels(...))
#      → calls count_vowels() with 
#      → 7.1 user = input('type string: ').lower()
#      → 7.2 vowels = 'aeiou'
#      → 7.3 count = 0
#      → 7.4 for char in user:
#      → 7.4     if char in vowels:
#      → 7.4         count += 1
#      → 7.5 result = count
#      → 7.6 return result
#   8. def count_vowels() → stored in memory, not executed
#   9. def count_vowels() → stored in memory, not executed
#   10. s = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_vowels(...))
#      → calls count_vowels() with 
#      → 13.1 user = input('type string: ').lower()
#      → 13.2 vowels = 'aeiou'
#      → 13.3 count = 0
#      → 13.4 for char in user:
#      → 13.4     if char in vowels:
#      → 13.4         count += 1
#      → 13.5 result = count
#      → 13.6 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     count_vowels() → jumps into count_vowels(), runs body, returns
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 62:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: * + - <=
# Built-in functions: fact, input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial() → each argument is evaluated first, then the function body runs
#   number + 1 → Python looks up number, then 1, then applies +
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   n * fact(...) → Python looks up n, then fact(...), then applies *
#   n - 1 → Python looks up n, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial() → stored in memory, not executed
#   2. factorial() → calls factorial() with 
#      → 2.1 result = 1
#      → 2.2 for i in range(1, 6):
#      → 2.2     result *= i
#      → 2.3 result = f'{result}'
#      → 2.4 return result
#   3. def factorial() → stored in memory, not executed
#   4. factorial() → calls factorial() with 
#      → 4.1 result = 1
#      → 4.2 for i in range(1, 6):
#      → 4.2     result *= i
#      → 4.3 result = f'{result}'
#      → 4.4 return result
#   5. def factorial() → stored in memory, not executed
#   6. factorial() → calls factorial() with 
#      → 6.1 result = 1
#      → 6.2 for i in range(1, 6):
#      → 6.2     result *= i
#      → 6.3 result = f'{result}'
#      → 6.4 return result
#   7. def factorial() → stored in memory, not executed
#   8. factorial() → calls factorial() with 
#      → 8.1 result = 1
#      → 8.2 for i in range(1, 6):
#      → 8.2     result *= i
#      → 8.3 result = f'{result}'
#      → 8.4 return result
#   9. def factorial() → stored in memory, not executed
#   10. def factorial() → stored in memory, not executed
#   11. result = ... → assigns value to variable
#   12. for i in range(...) → loops over each item
#   13. print(?)
#   14. result = ... → assigns value to variable
#   15. for i in range(...) → loops over each item
#   16. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     factorial() → jumps into factorial(), runs body, returns
#     factorial() → jumps into factorial(), runs body, returns
#     factorial() → jumps into factorial(), runs body, returns
#     factorial() → jumps into factorial(), runs body, returns
#     result = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(?)
#     result = ... → stored in memory
#     for i in range(...) → runs body once per item
#     print(?)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 63:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_all_numbers() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: +
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_all_numbers() → each argument is evaluated first, then the function body runs
#   n + 1 → Python looks up n, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_all_numbers() → stored in memory, not executed
#   2. sum_of_all_numbers() → calls sum_of_all_numbers() with 
#      → 2.1 n = int(input('Enter a number: '))
#      → 2.2 total = 0
#      → 2.3 for number in range(1, n + 1):
#      → 2.3     total += number
#      → 2.4 result = total
#      → 2.5 return result
#   3. def sum_of_all_numbers() → stored in memory, not executed
#   4. def sum_of_all_numbers() → stored in memory, not executed
#   5. n = ... → assigns value to variable
#   6. total = ... → assigns value to variable
#   7. for number in range(...) → loops over each item
#   8. print(total)
#   9. n = ... → assigns value to variable
#   10. total = ... → assigns value to variable
#   11. for number in range(...) → loops over each item
#   12. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_all_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sum_of_all_numbers() → jumps into sum_of_all_numbers(), runs body, returns
#     n = ... → stored in memory
#     total = ... → stored in memory
#     for number in range(...) → runs body once per item
#     print(total)
#     n = ... → stored in memory
#     total = ... → stored in memory
#     for number in range(...) → runs body once per item
#     print(total)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 64:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_largest() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: input, int, max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_largest() → each argument is evaluated first, then the function body runs
#   largest_element(numbers) runs first → then print() outputs the result
#   largest_element(numbers) → each argument is evaluated first, then the function body runs
#   largest_element(?) runs first → then print() outputs the result
#   largest_element(?) → each argument is evaluated first, then the function body runs
#   number > largest → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_largest() → stored in memory, not executed
#   2. find_largest() → calls find_largest() with 
#      → 2.1 user = input('type number: ').split()
#      → 2.2 int_number = []
#      → 2.3 for number in user:
#      → 2.3     int_number.append(int(number))
#      → 2.4 sort = sorted(int_number)
#      → 2.5 largest = sort[-1]
#      → 2.6 result = largest
#      → 2.7 return result
#   3. def find_largest() → stored in memory, not executed
#   4. find_largest() → calls find_largest() with 
#      → 4.1 user = input('type number: ').split()
#      → 4.2 int_number = []
#      → 4.3 for number in user:
#      → 4.3     int_number.append(int(number))
#      → 4.4 sort = sorted(int_number)
#      → 4.5 largest = sort[-1]
#      → 4.6 result = largest
#      → 4.7 return result
#   5. def largest_element(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=numbers
#      → 7.1 largest = numbers[0]
#      → 7.2 for number in numbers:
#      → 7.2     if number > largest:
#      → 7.2         largest = number
#      → 7.3 return largest
#   8. def largest_element(numbers) → stored in memory, not executed
#   9. numbers = ... → assigns value to variable
#   10. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=numbers
#      → 10.1 largest = numbers[0]
#      → 10.2 for number in numbers:
#      → 10.2     if number > largest:
#      → 10.2         largest = number
#      → 10.3 return largest
#   11. def find_largest() → stored in memory, not executed
#   12. def find_largest() → stored in memory, not executed
#   13. numbers = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 16.1 largest = numbers[0]
#      → 16.2 for number in numbers:
#      → 16.2     if number > largest:
#      → 16.2         largest = number
#      → 16.3 return largest
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_largest, largest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     find_largest() → jumps into find_largest(), runs body, returns
#     find_largest() → jumps into find_largest(), runs body, returns
#     numbers = ... → stored in memory
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     numbers = ... → stored in memory
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 65:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) → each argument is evaluated first, then the function body runs
#   largest_elememt(lst) runs first → then print() outputs the result
#   main(?) runs first → then print() outputs the result
#   largest_elememt(lst) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#   len(...) > count → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. main(lst) → calls main(lst) with lst=lst
#      → 3.1 return max(lst, key=len)
#   4. def largest_elememt(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(largest_elememt(...))
#      → calls largest_elememt(lst) with lst=lst
#      → 6.1 lst = sorted(lst, key=len)
#      → 6.2 return lst[-1]
#   7. def largest_elememt(lst) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. print(largest_elememt(...))
#      → calls largest_elememt(lst) with lst=lst
#      → 9.1 lst = sorted(lst, key=len)
#      → 9.2 return lst[-1]
#   10. def main(lst) → stored in memory, not executed
#   11. def main(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return max(lst, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, largest_elememt) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     lst = ... → stored in memory
#     print(largest_elememt(...)) → largest_elememt() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(largest_elememt(...)) → largest_elememt() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 66:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates(elements) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in not in
# Built-in functions: list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates(lst) runs first → then print() outputs the result
#   remove_duplicates(?) runs first → then print() outputs the result
#   remove_duplicates(elements) → each argument is evaluated first, then the function body runs
#   remove_duplicates(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates(?) → each argument is evaluated first, then the function body runs
#   element in duplicates → evaluated as True or False (determines which branch runs)
#   item not in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates(elements) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_duplicates(...))
#      → calls remove_duplicates(elements) with elements=lst
#      → 3.1 return list(map(lambda element: element, elements))
#   4. def remove_duplicates(elements) → stored in memory, not executed
#   5. print(remove_duplicates(...))
#      → calls remove_duplicates(elements) with elements=[1, 2, 2, 3, 3, 3]
#      → 5.1 return list(map(lambda element: element, elements))
#   6. def remove_duplicates(elements) → stored in memory, not executed
#   7. print(remove_duplicates(...))
#      → calls remove_duplicates(elements) with elements=[1, 2, 2, 3, 1, 4]
#      → 7.1 return list(map(lambda element: element, elements))
#   8. def remove_duplicates(elements) → stored in memory, not executed
#   9. print(remove_duplicates(...))
#      → calls remove_duplicates(elements) with elements=['a', 'b', 'a', 'c']
#      → 9.1 return list(map(lambda element: element, elements))
#   10. def remove_duplicates(elements) → stored in memory, not executed
#   11. def remove_duplicates(elements) → stored in memory, not executed
#   12. elements = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(remove_duplicates(...))
#      → calls remove_duplicates(elements) with elements=[5, 3, 8, 4]
#      → 15.1 return list(map(lambda element: element, elements))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     elements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#
#   Loop: takes one item from elements each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 67:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_leap_year() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: % != == or and
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_leap_year() runs first → then print() outputs the result
#   is_leap_year(2000) runs first → then print() outputs the result
#   is_leap_year(1900) runs first → then print() outputs the result
#   is_leap_year(2024) runs first → then print() outputs the result
#   is_leap_year(2020) runs first → then print() outputs the result
#   is_leap_year(2100) runs first → then print() outputs the result
#   is_leap_year(year) → each argument is evaluated first, then the function body runs
#   is_leap_year(25) runs first → then print() outputs the result
#   is_leap_year() → each argument is evaluated first, then the function body runs
#   is_leap_year(2000) → each argument is evaluated first, then the function body runs
#   is_leap_year(1900) → each argument is evaluated first, then the function body runs
#   is_leap_year(2024) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   is_leap_year(2020) → each argument is evaluated first, then the function body runs
#   is_leap_year(2100) → each argument is evaluated first, then the function body runs
#   is_leap_year(25) → each argument is evaluated first, then the function body runs
#   year % 400 → Python looks up year, then 400, then applies %
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   year % 4 → Python looks up year, then 4, then applies %
#   year % 100 → Python looks up year, then 100, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_leap_year() → stored in memory, not executed
#   2. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 2.1 return True
#   3. def is_leap_year(year) → stored in memory, not executed
#   4. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 4.1 return True
#   5. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 5.1 return True
#   6. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 6.1 return True
#   7. def is_leap_year(year) → stored in memory, not executed
#   8. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 8.1 return True
#   9. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 9.1 return True
#   10. def is_leap_year() → stored in memory, not executed
#   11. def is_leap_year() → stored in memory, not executed
#   12. year = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(is_leap_year(...))
#      → calls is_leap_year() with 
#      → 15.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_leap_year) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#     year = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_leap_year(...)) → is_leap_year() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 68:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def length_string() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   length_string() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def length_string() → stored in memory, not executed
#   2. length_string() → calls length_string() with 
#      → 2.1 text = input('Enter a string: ')
#      → 2.2 count = 0
#      → 2.3 for _ in text:
#      → 2.3     count += 1
#      → 2.4 result = count
#      → 2.5 return result
#   3. def length_string() → stored in memory, not executed
#   4. def length_string() → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. count = ... → assigns value to variable
#   7. for _ in text → loops over each item
#   8. print(count)
#   9. text = ... → assigns value to variable
#   10. count = ... → assigns value to variable
#   11. for _ in text → loops over each item
#   12. print(count)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (length_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     length_string() → jumps into length_string(), runs body, returns
#     text = ... → stored in memory
#     count = ... → stored in memory
#     for _ in text → runs body once per item
#     print(count)
#     text = ... → stored in memory
#     count = ... → stored in memory
#     for _ in text → runs body once per item
#     print(count)
#
#   Loop: takes one item from text each round, runs body, repeats until done

"""

"""
Problem 70:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_each_word(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: Counter, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_each_word(lst) runs first → then print() outputs the result
#   count_each_word('the cat and the dog') runs first → then print() outputs the result
#   count_each_word('one two one two one') runs first → then print() outputs the result
#   count_each_word(sentence) → each argument is evaluated first, then the function body runs
#   count_each_word('Python is fun') runs first → then print() outputs the result
#   count_each_word(lst) → each argument is evaluated first, then the function body runs
#   count_each_word('the cat and the dog') → each argument is evaluated first, then the function body runs
#   count_each_word('one two one two one') → each argument is evaluated first, then the function body runs
#   count_each_word('Python is fun') → each argument is evaluated first, then the function body runs
#   word_count.get(...) + 1 → Python looks up word_count.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_each_word(sentence) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(count_each_word(...))
#      → calls count_each_word(sentence) with sentence=lst
#      → 3.1 return count
#   4. def count_each_word(sentence) → stored in memory, not executed
#   5. print(count_each_word(...))
#      → calls count_each_word(sentence) with sentence='the cat and the dog'
#      → 5.1 return count
#   6. def count_each_word(sentence) → stored in memory, not executed
#   7. print(count_each_word(...))
#      → calls count_each_word(sentence) with sentence='one two one two one'
#      → 7.1 return count
#   8. def count_each_word(sentence) → stored in memory, not executed
#   9. def count_each_word(sentence) → stored in memory, not executed
#   10. sentence = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_each_word(...))
#      → calls count_each_word(sentence) with sentence='Python is fun'
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_each_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(count_each_word(...)) → count_each_word() runs first → value passed to print()
#     print(count_each_word(...)) → count_each_word() runs first → value passed to print()
#     print(count_each_word(...)) → count_each_word() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_each_word(...)) → count_each_word() runs first → value passed to print()
#
#   Loop: takes one item from split_word each round, runs body, repeats until done

"""

"""
Problem 71:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_largest_number(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: <
# Built-in functions: input, len, list, map, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_largest_number(numbers) → each argument is evaluated first, then the function body runs
#   second_largest_number(?) runs first → then print() outputs the result
#   len(...) < 2 → evaluated as True or False (determines which branch runs)
#   second_largest_number(numbers) runs first → then print() outputs the result
#   second_largest_number(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_largest_number(numbers) → stored in memory, not executed
#   2. def sec_lar() → stored in memory, not executed
#   3. def second_largest_number(numbers) → stored in memory, not executed
#   4. def second_largest_number(numbers) → stored in memory, not executed
#   5. numbers = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(second_largest_number(...))
#      → calls second_largest_number(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 8.1 return None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sec_lar, second_largest_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(second_largest_number(...)) → second_largest_number() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 72:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_valid_email(email) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: bool()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_valid_email('test@example.com') runs first → then print() outputs the result
#   is_valid_email(email) → each argument is evaluated first, then the function body runs
#   is_valid_email(25) runs first → then print() outputs the result
#   is_valid_email('test@example.com') → each argument is evaluated first, then the function body runs
#   is_valid_email(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. def is_valid_email(email) → stored in memory, not executed
#   3. print(is_valid_email(...))
#      → calls is_valid_email(email) with email='test@example.com'
#      → 3.1 return bool(re.fullmatch(pattern, email))
#   4. def is_valid_email(email) → stored in memory, not executed
#   5. def is_valid_email(email) → stored in memory, not executed
#   6. email = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_valid_email(...))
#      → calls is_valid_email(email) with email=25
#      → 9.1 return bool(re.fullmatch(pattern, email))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_valid_email) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_valid_email(...)) → is_valid_email() runs first → value passed to print()
#     email = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_valid_email(...)) → is_valid_email() runs first → value passed to print()

"""

"""
Problem 73:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | try/except
# Operators: * + - < == or
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   factorial(n) → each argument is evaluated first, then the function body runs
#   factorial(15) runs first → then print() outputs the result
#   factorial(15) → each argument is evaluated first, then the function body runs
#   n == 0 → evaluated as True or False (determines which branch runs)
#   n == 1 → evaluated as True or False (determines which branch runs)
#   n * factorial(...) → Python looks up n, then factorial(...), then applies *
#   number < 0 → evaluated as True or False (determines which branch runs)
#   n + 1 → Python looks up n, then 1, then applies +
#   factorial(n - 1) → each argument is evaluated first, then the function body runs
#   n - 1 → Python looks up n, then 1, then applies -
#   factorial(number) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial(n) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. main() → calls main() with 
#      → 3.1 number = int(input('Enter a number: '))
#      → 3.2 print(f'The factorial of {number} is {factorial(number)}')
#   4. def factorial(n) → stored in memory, not executed
#   5. def main() → stored in memory, not executed
#   6. main() → calls main() with 
#      → 6.1 number = int(input('Enter a number: '))
#      → 6.2 print(f'The factorial of {number} is {factorial(number)}')
#   7. def factorial(n) → stored in memory, not executed
#   8. def main() → stored in memory, not executed
#   9. main() → calls main() with 
#      → 9.1 number = int(input('Enter a number: '))
#      → 9.2 print(f'The factorial of {number} is {factorial(number)}')
#   10. def factorial(n) → stored in memory, not executed
#   11. def factorial(n) → stored in memory, not executed
#   12. n = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(factorial(...))
#      → calls factorial(n) with n=15
#      → 15.1 return 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     main() → jumps into main(), runs body, returns
#     main() → jumps into main(), runs body, returns
#     main() → jumps into main(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial(...)) → factorial() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 74:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def are_anagrams() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: ==
# Built-in functions: input, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   are_anagrams() → each argument is evaluated first, then the function body runs
#   sorted(...) == sorted(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def are_anagrams() → stored in memory, not executed
#   2. are_anagrams() → calls are_anagrams() with 
#      → 2.1 first = input('Enter the first string: ').replace(' ', '').lower()
#      → 2.2 second = input('Enter the second string: ').replace(' ', '').lower()
#      → 2.3 result = sorted(first) == sorted(second)
#      → 2.4 return result
#   3. def are_anagrams() → stored in memory, not executed
#   4. def are_anagrams() → stored in memory, not executed
#   5. first = ... → assigns value to variable
#   6. second = ... → assigns value to variable
#   7. print(?)
#   8. first = ... → assigns value to variable
#   9. second = ... → assigns value to variable
#   10. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (are_anagrams) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     are_anagrams() → jumps into are_anagrams(), runs body, returns
#     first = ... → stored in memory
#     second = ... → stored in memory
#     print(?)
#     first = ... → stored in memory
#     second = ... → stored in memory
#     print(?)

"""

"""
Problem 75:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | try/except | while
# Operators: % * + < != ** <= == or
# Built-in functions: all, input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   is_prime(17) runs first → then print() outputs the result
#   is_prime(10) runs first → then print() outputs the result
#   is_prime(2) runs first → then print() outputs the result
#   is_prime(23) runs first → then print() outputs the result
#   is_prime(15) runs first → then print() outputs the result
#   is_prime(n) → each argument is evaluated first, then the function body runs
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   n <= 3 → evaluated as True or False (determines which branch runs)
#   ? <= n → evaluated as True or False (determines which branch runs)
#   n < 2 → evaluated as True or False (determines which branch runs)
#   is_prime(17) → each argument is evaluated first, then the function body runs
#   is_prime(10) → each argument is evaluated first, then the function body runs
#   is_prime(2) → each argument is evaluated first, then the function body runs
#   is_prime(23) → each argument is evaluated first, then the function body runs
#   is_prime(15) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   i * i → Python looks up i, then i, then applies *
#   is_prime(number) → each argument is evaluated first, then the function body runs
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   n % 2 → Python looks up n, then 2, then applies %
#   n % 3 → Python looks up n, then 3, then applies %
#   n % i → Python looks up n, then i, then applies %
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   n % ? → Python looks up n, then ?, then applies %
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#   i + 2 → Python looks up i, then 2, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(n) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. main() → calls main() with 
#      → 3.1 try:
#      → 3.1     number = int(input('Enter a number: '))
#      → 3.1     if is_prime(number):
#      → 3.1         print(f'{number} is a prime number.')
#      → 3.1     else:
#      → 3.1         print(f'{number} is not a prime number.')
#      → 3.1 except ValueError:
#      → 3.1     print('Please enter a valid integer.')
#   4. def is_prime(n) → stored in memory, not executed
#   5. print(is_prime(...))
#      → calls is_prime(n) with n=17
#      → 5.1 return False
#   6. print(is_prime(...))
#      → calls is_prime(n) with n=10
#      → 6.1 return False
#   7. print(is_prime(...))
#      → calls is_prime(n) with n=2
#      → 7.1 return False
#   8. def is_prime(n) → stored in memory, not executed
#   9. print(is_prime(...))
#      → calls is_prime(n) with n=23
#      → 9.1 return False
#   10. print(is_prime(...))
#      → calls is_prime(n) with n=15
#      → 10.1 return False
#   11. def is_prime(n) → stored in memory, not executed
#   12. def is_prime(n) → stored in memory, not executed
#   13. n = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(is_prime(...))
#      → calls is_prime(n) with n=15
#      → 16.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     main() → jumps into main(), runs body, returns
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 76:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common_elements(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in and not in
# Built-in functions: filter, input, list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common_elements(lst1, lst2) runs first → then print() outputs the result
#   common_elements(arg1, arg2) runs first → then print() outputs the result
#   find_common_elements() → each argument is evaluated first, then the function body runs
#   common_elements(?, ?) runs first → then print() outputs the result
#   common_elements(lst1, lst2) → each argument is evaluated first, then the function body runs
#   common_elements(arg1, arg2) → each argument is evaluated first, then the function body runs
#   common_elements(?, ?) → each argument is evaluated first, then the function body runs
#   i == j → evaluated as True or False (determines which branch runs)
#   i not in result → evaluated as True or False (determines which branch runs)
#   x in lst2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common_elements(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(common_elements(...))
#      → calls common_elements(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return list(map(lambda i: i, lst1))
#   5. def common_elements(lst1, lst2) → stored in memory, not executed
#   6. arg1 = ... → assigns value to variable
#   7. arg2 = ... → assigns value to variable
#   8. print(common_elements(...))
#      → calls common_elements(lst1, lst2) with lst1=arg1, lst2=arg2
#      → 8.1 return list(map(lambda i: i, lst1))
#   9. def find_common_elements() → stored in memory, not executed
#   10. find_common_elements() → calls find_common_elements() with 
#      → 10.1 input1 = input('Enter elements of the first list, separated by spaces: ')
#      → 10.2 list1 = input1.split()
#      → 10.3 input2 = input('Enter elements of the second list, separated by spaces: ')
#      → 10.4 list2 = input2.split()
#      → 10.5 set1 = set(list1)
#      → 10.6 set2 = set(list2)
#      → 10.7 common_elements = set1.intersection(set2)
#      → 10.8 if common_elements:
#      → 10.8     print(f'Common elements between the two lists: {list(common_elements)}')
#      → 10.8 else:
#      → 10.8     print('There are no common elements between the two lists.')
#   11. def common_elements(lst1, lst2) → stored in memory, not executed
#   12. print(common_elements(...))
#      → calls common_elements(lst1, lst2) with lst1=[1, 2, 3, 4], lst2=[3, 4, 5, 6]
#      → 12.1 return list(map(lambda i: i, lst1))
#   13. def common_elements(lst1, lst2) → stored in memory, not executed
#   14. print(common_elements(...))
#      → calls common_elements(lst1, lst2) with lst1=['a', 'b', 'c'], lst2=['b', 'c', 'd']
#      → 14.1 return list(map(lambda i: i, lst1))
#   15. def common_elements(lst1, lst2) → stored in memory, not executed
#   16. def common_elements(lst1, lst2) → stored in memory, not executed
#   17. lst1 = ... → assigns value to variable
#   18. lst2 = ... → assigns value to variable
#   19. result = ... → assigns value to variable
#   20. print(result)
#   21. print(common_elements(...))
#      → calls common_elements(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 21.1 return list(map(lambda i: i, lst1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_common_elements, common_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(common_elements(...)) → common_elements() runs first → value passed to print()
#     arg1 = ... → stored in memory
#     arg2 = ... → stored in memory
#     print(common_elements(...)) → common_elements() runs first → value passed to print()
#     find_common_elements() → jumps into find_common_elements(), runs body, returns
#     print(common_elements(...)) → common_elements() runs first → value passed to print()
#     print(common_elements(...)) → common_elements() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_elements(...)) → common_elements() runs first → value passed to print()
#
#   Loop: takes one item from lst1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 77:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def uppercase(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: filter, len, list, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   uppercase(sentence) runs first → then print() outputs the result
#   uppercase('Hello World') runs first → then print() outputs the result
#   uppercase('Python Programming') runs first → then print() outputs the result
#   uppercase(string) → each argument is evaluated first, then the function body runs
#   uppercase('world') runs first → then print() outputs the result
#   uppercase(sentence) → each argument is evaluated first, then the function body runs
#   uppercase('Hello World') → each argument is evaluated first, then the function body runs
#   uppercase('Python Programming') → each argument is evaluated first, then the function body runs
#   uppercase('world') → each argument is evaluated first, then the function body runs
#   word == word.upper(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def uppercase(string) → stored in memory, not executed
#   2. sentence = ... → assigns value to variable
#   3. print(uppercase(...))
#      → calls uppercase(string) with string=sentence
#      → 3.1 return count
#   4. def uppercase(string) → stored in memory, not executed
#   5. print(uppercase(...))
#      → calls uppercase(string) with string='Hello World'
#      → 5.1 return count
#   6. def uppercase(string) → stored in memory, not executed
#   7. print(uppercase(...))
#      → calls uppercase(string) with string='Python Programming'
#      → 7.1 return count
#   8. def uppercase(string) → stored in memory, not executed
#   9. def uppercase(string) → stored in memory, not executed
#   10. string = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(uppercase(...))
#      → calls uppercase(string) with string='world'
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentence = ... → stored in memory
#     print(uppercase(...)) → uppercase() runs first → value passed to print()
#     print(uppercase(...)) → uppercase() runs first → value passed to print()
#     print(uppercase(...)) → uppercase() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(uppercase(...)) → uppercase() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 78:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def decimal_to_binary() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else | while
# Operators: % + > ==
# Built-in functions: bin, format, input, int, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   decimal_to_binary() → each argument is evaluated first, then the function body runs
#   decimal_number == 0 → evaluated as True or False (determines which branch runs)
#   num > 0 → evaluated as True or False (determines which branch runs)
#   str(...) + binary → Python looks up str(...), then binary, then applies +
#   num % 2 → Python looks up num, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def decimal_to_binary() → stored in memory, not executed
#   2. decimal_to_binary() → calls decimal_to_binary() with 
#      → 2.1 decimal_number = int(input('Enter a decimal number: '))
#      → 2.2 binary_number = bin(decimal_number)[2:]
#      → 2.3 result = f'The binary representation of {decimal_number} is {binary_number}'
#      → 2.4 return result
#   3. def decimal_to_binary() → stored in memory, not executed
#   4. decimal_to_binary() → calls decimal_to_binary() with 
#      → 4.1 decimal_number = int(input('Enter a decimal number: '))
#      → 4.2 binary_number = bin(decimal_number)[2:]
#      → 4.3 result = f'The binary representation of {decimal_number} is {binary_number}'
#      → 4.4 return result
#   5. def decimal_to_binary() → stored in memory, not executed
#   6. decimal_to_binary() → calls decimal_to_binary() with 
#      → 6.1 decimal_number = int(input('Enter a decimal number: '))
#      → 6.2 binary_number = bin(decimal_number)[2:]
#      → 6.3 result = f'The binary representation of {decimal_number} is {binary_number}'
#      → 6.4 return result
#   7. def decimal_to_binary() → stored in memory, not executed
#   8. def decimal_to_binary() → stored in memory, not executed
#   9. decimal_number = ... → assigns value to variable
#   10. binary_number = ... → assigns value to variable
#   11. print(?)
#   12. decimal_number = ... → assigns value to variable
#   13. binary_number = ... → assigns value to variable
#   14. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (decimal_to_binary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     decimal_to_binary() → jumps into decimal_to_binary(), runs body, returns
#     decimal_to_binary() → jumps into decimal_to_binary(), runs body, returns
#     decimal_to_binary() → jumps into decimal_to_binary(), runs body, returns
#     decimal_number = ... → stored in memory
#     binary_number = ... → stored in memory
#     print(?)
#     decimal_number = ... → stored in memory
#     binary_number = ... → stored in memory
#     print(?)
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 79:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - >
# Built-in functions: input, int, len, range, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   ascending_order_numbers(numbers) runs first → then print() outputs the result
#   ascending_order_numbers(?) runs first → then print() outputs the result
#   ascending_order_numbers(numbers) → each argument is evaluated first, then the function body runs
#   ascending_order_numbers(?) → each argument is evaluated first, then the function body runs
#   ? - 1 → Python looks up ?, then 1, then applies -
#   ? > ? → evaluated as True or False (determines which branch runs)
#   n - i → Python looks up n, then i, then applies -
#   j + 1 → Python looks up j, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main() → stored in memory, not executed
#   2. main() → calls main() with 
#      → 2.1 return sorted(result, reverse=False)
#   3. def ascending_order_numbers(numbers) → stored in memory, not executed
#   4. numbers = ... → assigns value to variable
#   5. print(ascending_order_numbers(...))
#      → calls ascending_order_numbers(numbers) with numbers=numbers
#      → 5.1 arr = numbers.copy()
#      → 5.2 n = len(arr)
#      → 5.3 for i in range(n):
#      → 5.3     for j in range(0, n - i - 1):
#      → 5.3         if arr[j] > arr[j + 1]:
#      → 5.3             arr[j], arr[j + 1] = (arr[j + 1], arr[j])
#      → 5.4 return arr
#   6. def ascending_order_numbers(numbers) → stored in memory, not executed
#   7. print(ascending_order_numbers(...))
#      → calls ascending_order_numbers(numbers) with numbers=[5, 2, 8, 1, 9]
#      → 7.1 arr = numbers.copy()
#      → 7.2 n = len(arr)
#      → 7.3 for i in range(n):
#      → 7.3     for j in range(0, n - i - 1):
#      → 7.3         if arr[j] > arr[j + 1]:
#      → 7.3             arr[j], arr[j + 1] = (arr[j + 1], arr[j])
#      → 7.4 return arr
#   8. def ascending_order_numbers(numbers) → stored in memory, not executed
#   9. print(ascending_order_numbers(...))
#      → calls ascending_order_numbers(numbers) with numbers=[64, 34, 25, 12, 22]
#      → 9.1 arr = numbers.copy()
#      → 9.2 n = len(arr)
#      → 9.3 for i in range(n):
#      → 9.3     for j in range(0, n - i - 1):
#      → 9.3         if arr[j] > arr[j + 1]:
#      → 9.3             arr[j], arr[j + 1] = (arr[j + 1], arr[j])
#      → 9.4 return arr
#   10. def main() → stored in memory, not executed
#   11. def main() → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(ascending_order_numbers(...))
#      → calls ascending_order_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 15.1 arr = numbers.copy()
#      → 15.2 n = len(arr)
#      → 15.3 for i in range(n):
#      → 15.3     for j in range(0, n - i - 1):
#      → 15.3         if arr[j] > arr[j + 1]:
#      → 15.3             arr[j], arr[j + 1] = (arr[j + 1], arr[j])
#      → 15.4 return arr
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (ascending_order_numbers, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     main() → jumps into main(), runs body, returns
#     numbers = ... → stored in memory
#     print(ascending_order_numbers(...)) → ascending_order_numbers() runs first → value passed to print()
#     print(ascending_order_numbers(...)) → ascending_order_numbers() runs first → value passed to print()
#     print(ascending_order_numbers(...)) → ascending_order_numbers() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(ascending_order_numbers(...)) → ascending_order_numbers() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 80:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def generate_random_number() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: +
# Built-in functions: float, input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   generate_random_number() → each argument is evaluated first, then the function body runs
#   upper_limit + 1 → Python looks up upper_limit, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. def generate_random_number() → stored in memory, not executed
#   3. generate_random_number() → calls generate_random_number() with 
#      → 3.1 lower_limit = int(input('Enter the lower limit: '))
#      → 3.2 upper_limit = int(input('Enter the upper limit: '))
#      → 3.3 random_number = random.randint(lower_limit, upper_limit)
#      → 3.4 result = f'The random number between {lower_limit} and {upper_limit} is: {random_number}'
#      → 3.5 return result
#   4. def generate_random_number() → stored in memory, not executed
#   5. generate_random_number() → calls generate_random_number() with 
#      → 5.1 lower_limit = int(input('Enter the lower limit: '))
#      → 5.2 upper_limit = int(input('Enter the upper limit: '))
#      → 5.3 random_number = random.randint(lower_limit, upper_limit)
#      → 5.4 result = f'The random number between {lower_limit} and {upper_limit} is: {random_number}'
#      → 5.5 return result
#   6. def generate_random_number() → stored in memory, not executed
#   7. generate_random_number() → calls generate_random_number() with 
#      → 7.1 lower_limit = int(input('Enter the lower limit: '))
#      → 7.2 upper_limit = int(input('Enter the upper limit: '))
#      → 7.3 random_number = random.randint(lower_limit, upper_limit)
#      → 7.4 result = f'The random number between {lower_limit} and {upper_limit} is: {random_number}'
#      → 7.5 return result
#   8. def generate_random_number() → stored in memory, not executed
#   9. def generate_random_number() → stored in memory, not executed
#   10. lower_limit = ... → assigns value to variable
#   11. upper_limit = ... → assigns value to variable
#   12. random_number = ... → assigns value to variable
#   13. print(?)
#   14. lower_limit = ... → assigns value to variable
#   15. upper_limit = ... → assigns value to variable
#   16. random_number = ... → assigns value to variable
#   17. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (generate_random_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     generate_random_number() → jumps into generate_random_number(), runs body, returns
#     generate_random_number() → jumps into generate_random_number(), runs body, returns
#     generate_random_number() → jumps into generate_random_number(), runs body, returns
#     lower_limit = ... → stored in memory
#     upper_limit = ... → stored in memory
#     random_number = ... → stored in memory
#     print(?)
#     lower_limit = ... → stored in memory
#     upper_limit = ... → stored in memory
#     random_number = ... → stored in memory
#     print(?)

"""

"""
Problem 81:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_root() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + / < **
# Built-in functions: float, input, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_root() runs first → then print() outputs the result
#   square_root() → each argument is evaluated first, then the function body runs
#   num ** 0.5 → Python looks up num, then 0.5, then applies **
#   num < 0 → evaluated as True or False (determines which branch runs)
#   num / 2 → Python looks up num, then 2, then applies /
#   ? / 2 → Python looks up ?, then 2, then applies /
#   guess + ? → Python looks up guess, then ?, then applies +
#   num / guess → Python looks up num, then guess, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_root() → stored in memory, not executed
#   2. print(square_root(...))
#      → calls square_root() with 
#      → 2.1 return num ** 0.5
#   3. def square_root() → stored in memory, not executed
#   4. square_root() → calls square_root() with 
#      → 4.1 return num ** 0.5
#   5. def square_root() → stored in memory, not executed
#   6. print(square_root(...))
#      → calls square_root() with 
#      → 6.1 return num ** 0.5
#   7. def square_root() → stored in memory, not executed
#   8. print(square_root(...))
#      → calls square_root() with 
#      → 8.1 return num ** 0.5
#   9. def square_root() → stored in memory, not executed
#   10. def square_root() → stored in memory, not executed
#   11. num = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. num = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_root) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(square_root(...)) → square_root() runs first → value passed to print()
#     square_root() → jumps into square_root(), runs body, returns
#     print(square_root(...)) → square_root() runs first → value passed to print()
#     print(square_root(...)) → square_root() runs first → value passed to print()
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 82:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Circle → defines a new type
#     def __init__(...) → method of Circle
#     def area(...) → method of Circle
#
#   def circle() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else
# Operators: * ** ==
# Built-in functions: Circle, float, input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   circle() → each argument is evaluated first, then the function body runs
#   calculate_circle_area() → each argument is evaluated first, then the function body runs
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   calculate_circle_area(radius) → each argument is evaluated first, then the function body runs
#   calculate_circle_area(14) runs first → then print() outputs the result
#   math.pi * ? → Python looks up math.pi, then ?, then applies *
#   main() → each argument is evaluated first, then the function body runs
#   calculate_circle_area(14) → each argument is evaluated first, then the function body runs
#   radius ** 2 → Python looks up radius, then 2, then applies **
#   calculate_circle_area(7) → each argument is evaluated first, then the function body runs
#   self.radius ** 2 → Python looks up self.radius, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def circle() → stored in memory, not executed
#   3. circle() → calls circle() with 
#      → 3.1 radius = float(input('type number: '))
#      → 3.2 area = math.pi * radius ** 2
#      → 3.3 result = area
#      → 3.4 return result
#   4. def calculate_circle_area() → stored in memory, not executed
#   5. calculate_circle_area() → calls calculate_circle_area(radius) with 
#      → 5.1 return math.pi * radius ** 2
#   6. import math
#   7. def calculate_circle_area(radius) → stored in memory, not executed
#   8. def main() → stored in memory, not executed
#   9. if ... → only one branch executes
#   10. calculate_circle_area = ... → assigns value to variable
#   11. print(?)
#   12. class Circle → class definition stored
#   13. circle = ... → assigns value to variable
#   14. print(?)
#   15. def circle() → stored in memory, not executed
#   16. def circle() → stored in memory, not executed
#   17. radius = ... → assigns value to variable
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. print(calculate_circle_area(...))
#      → calls calculate_circle_area(radius) with radius=14
#      → 20.1 return math.pi * radius ** 2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Circle) are defined — methods are stored, not called
#   Functions (circle, calculate_circle_area, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     circle() → jumps into circle(), runs body, returns
#     calculate_circle_area() → jumps into calculate_circle_area(), runs body, returns
#     if → evaluates condition, runs matching branch
#     calculate_circle_area = ... → stored in memory
#     print(?)
#     circle = ... → stored in memory
#     print(?)
#     radius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_circle_area(...)) → calculate_circle_area() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 83:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: - < != <= ==
# Built-in functions: input, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   is_palindrome('A man a plan a canal Panama') runs first → then print() outputs the result
#   is_palindrome('racecar') runs first → then print() outputs the result
#   is_palindrome(s) → each argument is evaluated first, then the function body runs
#   is_palindrome('world') runs first → then print() outputs the result
#   s == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome(user_input) → each argument is evaluated first, then the function body runs
#   main() → each argument is evaluated first, then the function body runs
#   left < right → evaluated as True or False (determines which branch runs)
#   is_palindrome('A man a plan a canal Panama') → each argument is evaluated first, then the function body runs
#   len(...) <= 1 → evaluated as True or False (determines which branch runs)
#   ? != ? → evaluated as True or False (determines which branch runs)
#   is_palindrome(?) → each argument is evaluated first, then the function body runs
#   is_palindrome('racecar') → each argument is evaluated first, then the function body runs
#   is_palindrome('world') → each argument is evaluated first, then the function body runs
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(s) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. if ... → only one branch executes
#   4. def is_palindrome(s) → stored in memory, not executed
#   5. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='A man a plan a canal Panama'
#      → 5.1 return s == s[::-1]
#   6. def is_palindrome(s) → stored in memory, not executed
#   7. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='racecar'
#      → 7.1 return s == s[::-1]
#   8. def is_palindrome(s) → stored in memory, not executed
#   9. def is_palindrome(s) → stored in memory, not executed
#   10. s = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(is_palindrome(...))
#      → calls is_palindrome(s) with s='world'
#      → 13.1 return s == s[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     if → evaluates condition, runs matching branch
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 84:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_pangram(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_pangram('The quick brown fox jumps over the lazy dog') runs first → then print() outputs the result
#   is_pangram(sentence) → each argument is evaluated first, then the function body runs
#   is_pangram('Python is fun') runs first → then print() outputs the result
#   is_pangram('The quick brown fox jumps over the lazy dog') → each argument is evaluated first, then the function body runs
#   is_pangram('Python is fun') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_pangram(sentence) → stored in memory, not executed
#   2. def pangram(sentence) → stored in memory, not executed
#   3. print(is_pangram(...))
#      → calls is_pangram(sentence) with sentence='The quick brown fox jumps over the lazy dog'
#      → 3.1 return alphabet.issubset(set(sentence.lower()))
#   4. def is_pangram(sentence) → stored in memory, not executed
#   5. def is_pangram(sentence) → stored in memory, not executed
#   6. sentence = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_pangram(...))
#      → calls is_pangram(sentence) with sentence='Python is fun'
#      → 9.1 return alphabet.issubset(set(sentence.lower()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_pangram, pangram) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(is_pangram(...)) → is_pangram() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_pangram(...)) → is_pangram() runs first → value passed to print()

"""

"""
Problem 85:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def gcd(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: % != ==
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   gcd(48, 18) runs first → then print() outputs the result
#   gcd(100, 75) runs first → then print() outputs the result
#   gcd(56, 98) runs first → then print() outputs the result
#   gcd(a, b) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) runs first → then print() outputs the result
#   b != 0 → evaluated as True or False (determines which branch runs)
#   gcd(num1, num2) → each argument is evaluated first, then the function body runs
#   main() → each argument is evaluated first, then the function body runs
#   gcd(48, 18) → each argument is evaluated first, then the function body runs
#   gcd(100, 75) → each argument is evaluated first, then the function body runs
#   b == 0 → evaluated as True or False (determines which branch runs)
#   gcd(b, a % b) → each argument is evaluated first, then the function body runs
#   gcd(56, 98) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) → each argument is evaluated first, then the function body runs
#   a % b → Python looks up a, then b, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def gcd(a, b) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. if ... → only one branch executes
#   4. def gcd(a, b) → stored in memory, not executed
#   5. print(gcd(...))
#      → calls gcd(a, b) with a=48, b=18
#      → 5.1 return a
#   6. print(gcd(...))
#      → calls gcd(a, b) with a=100, b=75
#      → 6.1 return a
#   7. def gcd(a, b) → stored in memory, not executed
#   8. print(gcd(...))
#      → calls gcd(a, b) with a=56, b=98
#      → 8.1 return a
#   9. def gcd(a, b) → stored in memory, not executed
#   10. def gcd(a, b) → stored in memory, not executed
#   11. a = ... → assigns value to variable
#   12. b = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(gcd(...))
#      → calls gcd(a, b) with a=20, b=30
#      → 15.1 return a
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, gcd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     if → evaluates condition, runs matching branch
#     print(gcd(...)) → gcd() runs first → value passed to print()
#     print(gcd(...)) → gcd() runs first → value passed to print()
#     print(gcd(...)) → gcd() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(gcd(...)) → gcd() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 86:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def gcd(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: % * + != // ==
# Built-in functions: abs, input, int, max, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   lcm(12, 18) runs first → then print() outputs the result
#   lcm(5, 7) runs first → then print() outputs the result
#   lcm(4, 6) runs first → then print() outputs the result
#   gcd(a, b) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) runs first → then print() outputs the result
#   b != 0 → evaluated as True or False (determines which branch runs)
#   abs(...) // gcd(...) → Python looks up abs(...), then gcd(...), then applies //
#   lcm(num1, num2) → each argument is evaluated first, then the function body runs
#   main() → each argument is evaluated first, then the function body runs
#   lcm(12, 18) → each argument is evaluated first, then the function body runs
#   lcm(5, 7) → each argument is evaluated first, then the function body runs
#   lcm(4, 6) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) → each argument is evaluated first, then the function body runs
#   ? + 1 → Python looks up ?, then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   a % b → Python looks up a, then b, then applies %
#   a * b → Python looks up a, then b, then applies *
#   i % b → Python looks up i, then b, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def gcd(a, b) → stored in memory, not executed
#   2. def lcm(a, b) → stored in memory, not executed
#   3. def main() → stored in memory, not executed
#   4. if ... → only one branch executes
#   5. def lcm(a, b) → stored in memory, not executed
#   6. print(lcm(...))
#      → calls lcm(a, b) with a=12, b=18
#      → 6.1 for i in range(max(a, b), a * b + 1, max(a, b)):
#      → 6.1     if i % b == 0:
#      → 6.1         return i
#   7. print(lcm(...))
#      → calls lcm(a, b) with a=5, b=7
#      → 7.1 for i in range(max(a, b), a * b + 1, max(a, b)):
#      → 7.1     if i % b == 0:
#      → 7.1         return i
#   8. def lcm(a, b) → stored in memory, not executed
#   9. print(lcm(...))
#      → calls lcm(a, b) with a=4, b=6
#      → 9.1 for i in range(max(a, b), a * b + 1, max(a, b)):
#      → 9.1     if i % b == 0:
#      → 9.1         return i
#   10. def gcd(a, b) → stored in memory, not executed
#   11. def gcd(a, b) → stored in memory, not executed
#   12. a = ... → assigns value to variable
#   13. b = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(gcd(...))
#      → calls gcd(a, b) with a=20, b=30
#      → 16.1 return a
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, lcm, gcd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     if → evaluates condition, runs matching branch
#     print(lcm(...)) → lcm() runs first → value passed to print()
#     print(lcm(...)) → lcm() runs first → value passed to print()
#     print(lcm(...)) → lcm() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(gcd(...)) → gcd() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 87:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_lst() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: -
# Built-in functions: deque, input, len, list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_lst() runs first → then print() outputs the result
#   reverse_list() runs first → then print() outputs the result
#   reverse_list() → each argument is evaluated first, then the function body runs
#   reverse_list(lst) runs first → then print() outputs the result
#   reverse_list(?) runs first → then print() outputs the result
#   reverse_list(items) → each argument is evaluated first, then the function body runs
#   reverse_lst() → each argument is evaluated first, then the function body runs
#   reverse_list(lst) → each argument is evaluated first, then the function body runs
#   reverse_list(?) → each argument is evaluated first, then the function body runs
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_lst() → stored in memory, not executed
#   2. print(reverse_lst(...))
#      → calls reverse_lst() with 
#      → 2.1 return ' '.join(reversed_lst)
#   3. def reverse_list() → stored in memory, not executed
#   4. print(reverse_list(...))
#      → calls reverse_list(items) with 
#      → 4.1 from collections import deque
#      → 4.2 d = deque(items)
#      → 4.3 d.reverse()
#      → 4.4 return list(d)
#   5. def reverse_list() → stored in memory, not executed
#   6. reverse_list() → calls reverse_list(items) with 
#      → 6.1 from collections import deque
#      → 6.2 d = deque(items)
#      → 6.3 d.reverse()
#      → 6.4 return list(d)
#   7. def reverse_list(items) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. print(reverse_list(...))
#      → calls reverse_list(items) with items=lst
#      → 9.1 from collections import deque
#      → 9.2 d = deque(items)
#      → 9.3 d.reverse()
#      → 9.4 return list(d)
#   10. def reverse_list(items) → stored in memory, not executed
#   11. print(reverse_list(...))
#      → calls reverse_list(items) with items=[1, 2, 3, 4, 5]
#      → 11.1 from collections import deque
#      → 11.2 d = deque(items)
#      → 11.3 d.reverse()
#      → 11.4 return list(d)
#   12. def reverse_list(items) → stored in memory, not executed
#   13. print(reverse_list(...))
#      → calls reverse_list(items) with items=['a', 'b', 'c']
#      → 13.1 from collections import deque
#      → 13.2 d = deque(items)
#      → 13.3 d.reverse()
#      → 13.4 return list(d)
#   14. def reverse_lst() → stored in memory, not executed
#   15. def reverse_lst() → stored in memory, not executed
#   16. items = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(reverse_list(...))
#      → calls reverse_list(items) with items=[2, 8, 3]
#      → 19.1 from collections import deque
#      → 19.2 d = deque(items)
#      → 19.3 d.reverse()
#      → 19.4 return list(d)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_lst, reverse_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(reverse_lst(...)) → reverse_lst() runs first → value passed to print()
#     print(reverse_list(...)) → reverse_list() runs first → value passed to print()
#     reverse_list() → jumps into reverse_list(), runs body, returns
#     lst = ... → stored in memory
#     print(reverse_list(...)) → reverse_list() runs first → value passed to print()
#     print(reverse_list(...)) → reverse_list() runs first → value passed to print()
#     print(reverse_list(...)) → reverse_list() runs first → value passed to print()
#     items = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_list(...)) → reverse_list() runs first → value passed to print()
#
#   Loop: takes one item from ? each round, runs body, repeats until done

"""

"""
Problem 88:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def prime_factors(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: % + < > ** // ==
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   prime_factors(315) runs first → then print() outputs the result
#   prime_factors(60) runs first → then print() outputs the result
#   prime_factors(n) → each argument is evaluated first, then the function body runs
#   prime_factors(15) runs first → then print() outputs the result
#   n > 1 → evaluated as True or False (determines which branch runs)
#   number < 2 → evaluated as True or False (determines which branch runs)
#   main() → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n > 2 → evaluated as True or False (determines which branch runs)
#   prime_factors(315) → each argument is evaluated first, then the function body runs
#   n == 1 → evaluated as True or False (determines which branch runs)
#   prime_factors(n, divisor + 1) → each argument is evaluated first, then the function body runs
#   prime_factors(60) → each argument is evaluated first, then the function body runs
#   prime_factors(15) → each argument is evaluated first, then the function body runs
#   prime_factors(number) → each argument is evaluated first, then the function body runs
#   n % 2 → Python looks up n, then 2, then applies %
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   n % divisor → Python looks up n, then divisor, then applies %
#   [divisor] + prime_factors(...) → Python looks up [divisor], then prime_factors(...), then applies +
#   divisor + 1 → Python looks up divisor, then 1, then applies +
#   n % i → Python looks up n, then i, then applies %
#   prime_factors(n // divisor, divisor) → each argument is evaluated first, then the function body runs
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#   n // divisor → Python looks up n, then divisor, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def prime_factors(n) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. if ... → only one branch executes
#   4. def prime_factors(n) → stored in memory, not executed
#   5. print(prime_factors(...))
#      → calls prime_factors(n) with n=315
#      → 5.1 return factors
#   6. def prime_factors(n, divisor) → stored in memory, not executed
#   7. print(prime_factors(...))
#      → calls prime_factors(n) with n=60
#      → 7.1 return factors
#   8. def prime_factors(n) → stored in memory, not executed
#   9. def prime_factors(n) → stored in memory, not executed
#   10. n = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(prime_factors(...))
#      → calls prime_factors(n) with n=15
#      → 13.1 return factors
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, prime_factors) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     if → evaluates condition, runs matching branch
#     print(prime_factors(...)) → prime_factors() runs first → value passed to print()
#     print(prime_factors(...)) → prime_factors() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(prime_factors(...)) → prime_factors() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 89:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def generate_password(length) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   generate_password(length) runs first → then print() outputs the result
#   generate_password(int(...)) runs first → then print() outputs the result
#   generate_password(length) → each argument is evaluated first, then the function body runs
#   generate_password(25) runs first → then print() outputs the result
#   ? + string.punctuation → Python looks up ?, then string.punctuation, then applies +
#   generate_password(int(...)) → each argument is evaluated first, then the function body runs
#   string.ascii_letters + string.digits → Python looks up string.ascii_letters, then string.digits, then applies +
#   generate_password(25) → each argument is evaluated first, then the function body runs
#   chars * length → Python looks up chars, then length, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. import string
#   3. def generate_password(length) → stored in memory, not executed
#   4. length = ... → assigns value to variable
#   5. print('Generated password:', generate_password(...))
#   6. import secrets
#   7. import string
#   8. def generate_password(length) → stored in memory, not executed
#   9. print('Generated password:', generate_password(...))
#   10. def generate_password(length) → stored in memory, not executed
#   11. print('Generated password:', generate_password(...))
#   12. def generate_password(length) → stored in memory, not executed
#   13. def generate_password(length) → stored in memory, not executed
#   14. length = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(generate_password(...))
#      → calls generate_password(length) with length=25
#      → 17.1 return password
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (generate_password) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     length = ... → stored in memory
#     print('Generated password:', generate_password(...))
#     print('Generated password:', generate_password(...))
#     print('Generated password:', generate_password(...))
#     length = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(generate_password(...)) → generate_password() runs first → value passed to print()

"""

"""
Problem 90:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words(words) runs first → then print() outputs the result
#   count_words('Python is awesome') runs first → then print() outputs the result
#   count_words('Hello world from Python') runs first → then print() outputs the result
#   count_words(sentence) → each argument is evaluated first, then the function body runs
#   count_words('Python is fun') runs first → then print() outputs the result
#   count_words(words) → each argument is evaluated first, then the function body runs
#   count_words('Python is awesome') → each argument is evaluated first, then the function body runs
#   count_words('Hello world from Python') → each argument is evaluated first, then the function body runs
#   count_words('Python is fun') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words(sentence) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. print(count_words(...))
#      → calls count_words(sentence) with sentence=words
#      → 3.1 return count
#   4. def count_words(sentence) → stored in memory, not executed
#   5. print(count_words(...))
#      → calls count_words(sentence) with sentence='Python is awesome'
#      → 5.1 return count
#   6. def count_words(sentence) → stored in memory, not executed
#   7. print(count_words(...))
#      → calls count_words(sentence) with sentence='Hello world from Python'
#      → 7.1 return count
#   8. def count_words(sentence) → stored in memory, not executed
#   9. def count_words(sentence) → stored in memory, not executed
#   10. sentence = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_words(...))
#      → calls count_words(sentence) with sentence='Python is fun'
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words(...)) → count_words() runs first → value passed to print()
#
#   Loop: takes one item from split_words each round, runs body, repeats until done

"""

"""
Problem 91:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_smallest(elements) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: max, min, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_smallest(lst) runs first → then print() outputs the result
#   largest_smallest(?) runs first → then print() outputs the result
#   largest_smallest(elements) → each argument is evaluated first, then the function body runs
#   largest_smallest(lst) → each argument is evaluated first, then the function body runs
#   largest_smallest(?) → each argument is evaluated first, then the function body runs
#   element < smallest → evaluated as True or False (determines which branch runs)
#   element > largest → evaluated as True or False (determines which branch runs)
#   a < b → evaluated as True or False (determines which branch runs)
#   a > b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_smallest(elements) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(largest_smallest(...))
#      → calls largest_smallest(elements) with elements=lst
#      → 3.1 return (smallest, largest)
#   4. def largest_smallest(elements) → stored in memory, not executed
#   5. print(largest_smallest(...))
#      → calls largest_smallest(elements) with elements=[10, 3, 7, 1, 9]
#      → 5.1 return (smallest, largest)
#   6. def largest_smallest(elements) → stored in memory, not executed
#   7. print(largest_smallest(...))
#      → calls largest_smallest(elements) with elements=[4, 5, 100, 6, 1, 10]
#      → 7.1 return (smallest, largest)
#   8. def largest_smallest(elements) → stored in memory, not executed
#   9. def largest_smallest(elements) → stored in memory, not executed
#   10. elements = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(largest_smallest(...))
#      → calls largest_smallest(elements) with elements=[5, 3, 8, 4]
#      → 13.1 return (smallest, largest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_smallest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     elements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#
#   Loop: takes one item from elements each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 92:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_perfect_square(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + ** // <= ==
# Built-in functions: input, int, range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_perfect_square(6) → each argument is evaluated first, then the function body runs
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   is_perfect_number(28) runs first → then print() outputs the result
#   is_perfect_number(12) runs first → then print() outputs the result
#   is_perfect_number(6) runs first → then print() outputs the result
#   is_perfect_number(496) runs first → then print() outputs the result
#   is_perfect_square(num) → each argument is evaluated first, then the function body runs
#   is_perfect_square(25) runs first → then print() outputs the result
#   num <= 0 → evaluated as True or False (determines which branch runs)
#   n <= 0 → evaluated as True or False (determines which branch runs)
#   sum_of_divisors == n → evaluated as True or False (determines which branch runs)
#   is_perfect_number(number) → each argument is evaluated first, then the function body runs
#   main() → each argument is evaluated first, then the function body runs
#   n == sum(...) → evaluated as True or False (determines which branch runs)
#   is_perfect_number(28) → each argument is evaluated first, then the function body runs
#   is_perfect_number(12) → each argument is evaluated first, then the function body runs
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   is_perfect_number(6) → each argument is evaluated first, then the function body runs
#   is_perfect_number(496) → each argument is evaluated first, then the function body runs
#   is_perfect_square(25) → each argument is evaluated first, then the function body runs
#   num + 1 → Python looks up num, then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   num % i → Python looks up num, then i, then applies %
#   sum_divisor == num → evaluated as True or False (determines which branch runs)
#   n % i → Python looks up n, then i, then applies %
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#   n // i → Python looks up n, then i, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_perfect_square(num) → stored in memory, not executed
#   2. is_perfect_square(6) → calls is_perfect_square(num) with num=6
#      → 2.1 return False
#   3. def is_perfect_number(n) → stored in memory, not executed
#   4. def main() → stored in memory, not executed
#   5. if ... → only one branch executes
#   6. def is_perfect_number(n) → stored in memory, not executed
#   7. print(is_perfect_number(...))
#      → calls is_perfect_number(n) with n=28
#      → 7.1 if n <= 1:
#      → 7.1     return False
#      → 7.2 divisors = {1}
#      → 7.3 for i in range(2, int(n ** 0.5) + 1):
#      → 7.3     if n % i == 0:
#      → 7.3         divisors.add(i)
#      → 7.3         divisors.add(n // i)
#      → 7.4 return n == sum(divisors)
#   8. print(is_perfect_number(...))
#      → calls is_perfect_number(n) with n=12
#      → 8.1 if n <= 1:
#      → 8.1     return False
#      → 8.2 divisors = {1}
#      → 8.3 for i in range(2, int(n ** 0.5) + 1):
#      → 8.3     if n % i == 0:
#      → 8.3         divisors.add(i)
#      → 8.3         divisors.add(n // i)
#      → 8.4 return n == sum(divisors)
#   9. def is_perfect_number(n) → stored in memory, not executed
#   10. print(is_perfect_number(...))
#      → calls is_perfect_number(n) with n=6
#      → 10.1 if n <= 1:
#      → 10.1     return False
#      → 10.2 divisors = {1}
#      → 10.3 for i in range(2, int(n ** 0.5) + 1):
#      → 10.3     if n % i == 0:
#      → 10.3         divisors.add(i)
#      → 10.3         divisors.add(n // i)
#      → 10.4 return n == sum(divisors)
#   11. print(is_perfect_number(...))
#      → calls is_perfect_number(n) with n=496
#      → 11.1 if n <= 1:
#      → 11.1     return False
#      → 11.2 divisors = {1}
#      → 11.3 for i in range(2, int(n ** 0.5) + 1):
#      → 11.3     if n % i == 0:
#      → 11.3         divisors.add(i)
#      → 11.3         divisors.add(n // i)
#      → 11.4 return n == sum(divisors)
#   12. def is_perfect_square(num) → stored in memory, not executed
#   13. def is_perfect_square(num) → stored in memory, not executed
#   14. num = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(is_perfect_square(...))
#      → calls is_perfect_square(num) with num=25
#      → 17.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_perfect_square, is_perfect_number, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     is_perfect_square(6) → jumps into is_perfect_square(), runs body, returns
#     if → evaluates condition, runs matching branch
#     print(is_perfect_number(...)) → is_perfect_number() runs first → value passed to print()
#     print(is_perfect_number(...)) → is_perfect_number() runs first → value passed to print()
#     print(is_perfect_number(...)) → is_perfect_number() runs first → value passed to print()
#     print(is_perfect_number(...)) → is_perfect_number() runs first → value passed to print()
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_perfect_square(...)) → is_perfect_square() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 93:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: * + - <=
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial() → each argument is evaluated first, then the function body runs
#   factorial() runs first → then print() outputs the result
#   factorial(n) runs first → then print() outputs the result
#   factorial(n) → each argument is evaluated first, then the function body runs
#   factorial(15) runs first → then print() outputs the result
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   n * factorial(...) → Python looks up n, then factorial(...), then applies *
#   factorial(15) → each argument is evaluated first, then the function body runs
#   user + 1 → Python looks up user, then 1, then applies +
#   factorial(n - 1) → each argument is evaluated first, then the function body runs
#   n - 1 → Python looks up n, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial() → stored in memory, not executed
#   2. factorial() → calls factorial() with 
#      → 2.1 user = int(input('type number: '))
#      → 2.2 result = 1
#      → 2.3 for number in range(1, user + 1):
#      → 2.3     result *= number
#      → 2.4 result = result
#      → 2.5 return result
#   3. def factorial() → stored in memory, not executed
#   4. print(factorial(...))
#      → calls factorial() with 
#      → 4.1 user = int(input('type number: '))
#      → 4.2 result = 1
#      → 4.3 for number in range(1, user + 1):
#      → 4.3     result *= number
#      → 4.4 result = result
#      → 4.5 return result
#   5. import math
#   6. def factorial() → stored in memory, not executed
#   7. factorial() → calls factorial() with 
#      → 7.1 user = int(input('type number: '))
#      → 7.2 result = 1
#      → 7.3 for number in range(1, user + 1):
#      → 7.3     result *= number
#      → 7.4 result = result
#      → 7.5 return result
#   8. def factorial(n) → stored in memory, not executed
#   9. n = ... → assigns value to variable
#   10. print(factorial(...))
#      → calls factorial() with 
#      → 10.1 user = int(input('type number: '))
#      → 10.2 result = 1
#      → 10.3 for number in range(1, user + 1):
#      → 10.3     result *= number
#      → 10.4 result = result
#      → 10.5 return result
#   11. def factorial() → stored in memory, not executed
#   12. def factorial() → stored in memory, not executed
#   13. n = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(factorial(...))
#      → calls factorial() with 
#      → 16.1 user = int(input('type number: '))
#      → 16.2 result = 1
#      → 16.3 for number in range(1, user + 1):
#      → 16.3     result *= number
#      → 16.4 result = result
#      → 16.5 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     factorial() → jumps into factorial(), runs body, returns
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     factorial() → jumps into factorial(), runs body, returns
#     n = ... → stored in memory
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial(...)) → factorial() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 94:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_power_of_two(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: % & - > != // <= == and
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_power_of_two(number) → each argument is evaluated first, then the function body runs
#   is_power_of_two(16) runs first → then print() outputs the result
#   is_power_of_two(18) runs first → then print() outputs the result
#   is_power_of_two(32) runs first → then print() outputs the result
#   is_power_of_two(33) runs first → then print() outputs the result
#   is_power_of_two(25) runs first → then print() outputs the result
#   number <= 0 → evaluated as True or False (determines which branch runs)
#   number != 1 → evaluated as True or False (determines which branch runs)
#   is_power_of_two(16) → each argument is evaluated first, then the function body runs
#   is_power_of_two(18) → each argument is evaluated first, then the function body runs
#   is_power_of_two(32) → each argument is evaluated first, then the function body runs
#   is_power_of_two(33) → each argument is evaluated first, then the function body runs
#   is_power_of_two(25) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   number // 2 → Python looks up number, then 2, then applies //
#   number > 0 → evaluated as True or False (determines which branch runs)
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#   number & ? → Python looks up number, then ?, then applies &
#   number - 1 → Python looks up number, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_power_of_two(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. if ... → only one branch executes
#   4. def is_power_of_two(number) → stored in memory, not executed
#   5. print(is_power_of_two(...))
#      → calls is_power_of_two(number) with number=16
#      → 5.1 return False
#   6. print(is_power_of_two(...))
#      → calls is_power_of_two(number) with number=18
#      → 6.1 return False
#   7. def is_power_of_two(number) → stored in memory, not executed
#   8. print(is_power_of_two(...))
#      → calls is_power_of_two(number) with number=32
#      → 8.1 return False
#   9. print(is_power_of_two(...))
#      → calls is_power_of_two(number) with number=33
#      → 9.1 return False
#   10. def is_power_of_two(number) → stored in memory, not executed
#   11. def is_power_of_two(number) → stored in memory, not executed
#   12. number = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(is_power_of_two(...))
#      → calls is_power_of_two(number) with number=25
#      → 15.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_power_of_two) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     if → evaluates condition, runs matching branch
#     print(is_power_of_two(...)) → is_power_of_two() runs first → value passed to print()
#     print(is_power_of_two(...)) → is_power_of_two() runs first → value passed to print()
#     print(is_power_of_two(...)) → is_power_of_two() runs first → value passed to print()
#     print(is_power_of_two(...)) → is_power_of_two() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_power_of_two(...)) → is_power_of_two() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 95:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_list(prompt) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: & in
# Built-in functions: filter, input, list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   find_intersection() runs first → then print() outputs the result
#   find_intersection(?, ?) runs first → then print() outputs the result
#   get_list(prompt) → each argument is evaluated first, then the function body runs
#   get_list(25) runs first → then print() outputs the result
#   get_list('Enter the first list of elements, separated by spaces: ') → each argument is evaluated first, then the function body runs
#   get_list('Enter the second list of elements, separated by spaces: ') → each argument is evaluated first, then the function body runs
#   find_intersection(list1, list2) → each argument is evaluated first, then the function body runs
#   find_intersection() → each argument is evaluated first, then the function body runs
#   find_intersection(?, ?) → each argument is evaluated first, then the function body runs
#   get_list(25) → each argument is evaluated first, then the function body runs
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#   x in list2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_list(prompt) → stored in memory, not executed
#   2. def find_intersection(list1, list2) → stored in memory, not executed
#   3. def main() → stored in memory, not executed
#   4. main() → calls main() with 
#      → 4.1 list1 = get_list('Enter the first list of elements, separated by spaces: ')
#      → 4.2 list2 = get_list('Enter the second list of elements, separated by spaces: ')
#      → 4.3 intersection = find_intersection(list1, list2)
#      → 4.4 print(f'The intersection of the two lists is: {intersection}')
#   5. def find_intersection() → stored in memory, not executed
#   6. print(find_intersection(...))
#      → calls find_intersection(list1, list2) with 
#      → 6.1 return list(filter(lambda x: x in list2, list1))
#   7. def find_intersection(list1, list2) → stored in memory, not executed
#   8. print(find_intersection(...))
#      → calls find_intersection(list1, list2) with list1=[1, 2, 3, 4], list2=[3, 4, 5, 6]
#      → 8.1 return list(filter(lambda x: x in list2, list1))
#   9. def find_intersection(list1, list2) → stored in memory, not executed
#   10. print(find_intersection(...))
#      → calls find_intersection(list1, list2) with list1=['a', 'b', 'c'], list2=['b', 'c', 'd']
#      → 10.1 return list(filter(lambda x: x in list2, list1))
#   11. def get_list(prompt) → stored in memory, not executed
#   12. def get_list(prompt) → stored in memory, not executed
#   13. prompt = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(get_list(...))
#      → calls get_list(prompt) with prompt=25
#      → 16.1 return input(prompt).split()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, find_intersection, get_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     main() → jumps into main(), runs body, returns
#     print(find_intersection(...)) → find_intersection() runs first → value passed to print()
#     print(find_intersection(...)) → find_intersection() runs first → value passed to print()
#     print(find_intersection(...)) → find_intersection() runs first → value passed to print()
#     prompt = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_list(...)) → get_list() runs first → value passed to print()

"""

"""
Problem 97:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def binary_to_decimal(binary) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: * - ** == in
# Built-in functions: all, enumerate, input, int, len, reversed, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   binary_to_decimal('1010') runs first → then print() outputs the result
#   binary_to_decimal('1111') runs first → then print() outputs the result
#   binary_to_decimal('1101') runs first → then print() outputs the result
#   binary_to_decimal(binary) → each argument is evaluated first, then the function body runs
#   binary_to_decimal(?) runs first → then print() outputs the result
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   main() → each argument is evaluated first, then the function body runs
#   binary_to_decimal('1010') → each argument is evaluated first, then the function body runs
#   binary_to_decimal('1111') → each argument is evaluated first, then the function body runs
#   binary_to_decimal('1101') → each argument is evaluated first, then the function body runs
#   binary_to_decimal(?) → each argument is evaluated first, then the function body runs
#   digit == '1' → evaluated as True or False (determines which branch runs)
#   binary_to_decimal(binary_number) → each argument is evaluated first, then the function body runs
#   2 ** power → Python looks up 2, then power, then applies **
#   bit in '01' → evaluated as True or False (determines which branch runs)
#   int(...) * ? → Python looks up int(...), then ?, then applies *
#   2 ** i → Python looks up 2, then i, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def binary_to_decimal(binary) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. if ... → only one branch executes
#   4. def binary_to_decimal(binary) → stored in memory, not executed
#   5. print(binary_to_decimal(...))
#      → calls binary_to_decimal(binary) with binary='1010'
#      → 5.1 return decimal
#   6. print(binary_to_decimal(...))
#      → calls binary_to_decimal(binary) with binary='1111'
#      → 6.1 return decimal
#   7. def binary_to_decimal(binary) → stored in memory, not executed
#   8. print(binary_to_decimal(...))
#      → calls binary_to_decimal(binary) with binary='1101'
#      → 8.1 return decimal
#   9. def binary_to_decimal(binary) → stored in memory, not executed
#   10. def binary_to_decimal(binary) → stored in memory, not executed
#   11. binary = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(binary_to_decimal(...))
#      → calls binary_to_decimal(binary) with binary=[4, 1, 8, 6, 3]
#      → 14.1 return decimal
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (binary_to_decimal, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     if → evaluates condition, runs matching branch
#     print(binary_to_decimal(...)) → binary_to_decimal() runs first → value passed to print()
#     print(binary_to_decimal(...)) → binary_to_decimal() runs first → value passed to print()
#     print(binary_to_decimal(...)) → binary_to_decimal() runs first → value passed to print()
#     binary = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(binary_to_decimal(...)) → binary_to_decimal() runs first → value passed to print()
#
#   Loop: takes one item from binary each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 98:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def digits_string() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input, int, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   digits_string() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def digits_string() → stored in memory, not executed
#   2. digits_string() → calls digits_string() with 
#      → 2.1 user = input('Type numbers separated by spaces: ').split()
#      → 2.2 total = 0
#      → 2.3 for number in user:
#      → 2.3     total += int(number)
#      → 2.4 result = total
#      → 2.5 return result
#   3. def digits_string() → stored in memory, not executed
#   4. digits_string() → calls digits_string() with 
#      → 4.1 user = input('Type numbers separated by spaces: ').split()
#      → 4.2 total = 0
#      → 4.3 for number in user:
#      → 4.3     total += int(number)
#      → 4.4 result = total
#      → 4.5 return result
#   5. def digits_string() → stored in memory, not executed
#   6. digits_string() → calls digits_string() with 
#      → 6.1 user = input('Type numbers separated by spaces: ').split()
#      → 6.2 total = 0
#      → 6.3 for number in user:
#      → 6.3     total += int(number)
#      → 6.4 result = total
#      → 6.5 return result
#   7. def digits_string() → stored in memory, not executed
#   8. digits_string() → calls digits_string() with 
#      → 8.1 user = input('Type numbers separated by spaces: ').split()
#      → 8.2 total = 0
#      → 8.3 for number in user:
#      → 8.3     total += int(number)
#      → 8.4 result = total
#      → 8.5 return result
#   9. def digits_string() → stored in memory, not executed
#   10. def digits_string() → stored in memory, not executed
#   11. user = ... → assigns value to variable
#   12. total = ... → assigns value to variable
#   13. for number in user → loops over each item
#   14. print(total)
#   15. user = ... → assigns value to variable
#   16. total = ... → assigns value to variable
#   17. for number in user → loops over each item
#   18. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (digits_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     digits_string() → jumps into digits_string(), runs body, returns
#     digits_string() → jumps into digits_string(), runs body, returns
#     digits_string() → jumps into digits_string(), runs body, returns
#     digits_string() → jumps into digits_string(), runs body, returns
#     user = ... → stored in memory
#     total = ... → stored in memory
#     for number in user → runs body once per item
#     print(total)
#     user = ... → stored in memory
#     total = ... → stored in memory
#     for number in user → runs body once per item
#     print(total)
#
#   Loop: takes one item from user each round, runs body, repeats until done

"""

"""
Problem 99:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def upper_lower(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: %
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   upper_lower(string) runs first → then print() outputs the result
#   upper_lower('Hello World! 123') runs first → then print() outputs the result
#   upper_lower('Python Mastery Pro') runs first → then print() outputs the result
#   upper_lower(string) → each argument is evaluated first, then the function body runs
#   upper_lower('world') runs first → then print() outputs the result
#   upper_lower('Hello World! 123') → each argument is evaluated first, then the function body runs
#   upper_lower('Python Mastery Pro') → each argument is evaluated first, then the function body runs
#   'lower = %s and upper = %s' % ? → Python looks up 'lower = %s and upper = %s', then ?, then applies %
#   upper_lower('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def upper_lower(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(upper_lower(...))
#      → calls upper_lower(string) with string=string
#      → 3.1 return 'lower = %s and upper = %s' % (count_lower, count_upper)
#   4. def upper_lower(string) → stored in memory, not executed
#   5. print(upper_lower(...))
#      → calls upper_lower(string) with string='Hello World! 123'
#      → 5.1 return 'lower = %s and upper = %s' % (count_lower, count_upper)
#   6. def upper_lower(string) → stored in memory, not executed
#   7. print(upper_lower(...))
#      → calls upper_lower(string) with string='Python Mastery Pro'
#      → 7.1 return 'lower = %s and upper = %s' % (count_lower, count_upper)
#   8. def upper_lower(string) → stored in memory, not executed
#   9. def upper_lower(string) → stored in memory, not executed
#   10. string = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(upper_lower(...))
#      → calls upper_lower(string) with string='world'
#      → 13.1 return 'lower = %s and upper = %s' % (count_lower, count_upper)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (upper_lower) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(upper_lower(...)) → upper_lower() runs first → value passed to print()
#     print(upper_lower(...)) → upper_lower() runs first → value passed to print()
#     print(upper_lower(...)) → upper_lower() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(upper_lower(...)) → upper_lower() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 100:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % * + > != ** <= == and
# Built-in functions: all, input, int, range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   __name__ == '__main__' → evaluated as True or False (determines which branch runs)
#   sum_of_primes(1, 10) runs first → then print() outputs the result
#   sum_of_primes(10, 50) runs first → then print() outputs the result
#   is_prime(num) → each argument is evaluated first, then the function body runs
#   is_prime(25) runs first → then print() outputs the result
#   num <= 1 → evaluated as True or False (determines which branch runs)
#   sum_of_primes(lower_limit, upper_limit) → each argument is evaluated first, then the function body runs
#   main() → each argument is evaluated first, then the function body runs
#   sum_of_primes(1, 10) → each argument is evaluated first, then the function body runs
#   [True] * ? → Python looks up [True], then ?, then applies *
#   sum_of_primes(10, 50) → each argument is evaluated first, then the function body runs
#   is_prime(25) → each argument is evaluated first, then the function body runs
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   upper + 1 → Python looks up upper, then 1, then applies +
#   num % i → Python looks up num, then i, then applies %
#   num ** 0.5 → Python looks up num, then 0.5, then applies **
#   upper ** 0.5 → Python looks up upper, then 0.5, then applies **
#   i * i → Python looks up i, then i, then applies *
#   num > 1 → evaluated as True or False (determines which branch runs)
#   ? != 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(num) → stored in memory, not executed
#   2. def sum_of_primes(lower, upper) → stored in memory, not executed
#   3. def main() → stored in memory, not executed
#   4. if ... → only one branch executes
#   5. def sum_of_primes(lower, upper) → stored in memory, not executed
#   6. print(sum_of_primes(...))
#      → calls sum_of_primes(lower, upper) with lower=1, upper=10
#      → 6.1 sieve = [True] * (upper + 1)
#      → 6.2 sieve[0] = sieve[1] = False
#      → 6.3 for i in range(2, int(upper ** 0.5) + 1):
#      → 6.3     if sieve[i]:
#      → 6.3         for j in range(i * i, upper + 1, i):
#      → 6.3             sieve[j] = False
#      → 6.4 return sum((i for i in range(lower, upper + 1) if sieve[i]))
#   7. def sum_of_primes(lower, upper) → stored in memory, not executed
#   8. print(sum_of_primes(...))
#      → calls sum_of_primes(lower, upper) with lower=10, upper=50
#      → 8.1 sieve = [True] * (upper + 1)
#      → 8.2 sieve[0] = sieve[1] = False
#      → 8.3 for i in range(2, int(upper ** 0.5) + 1):
#      → 8.3     if sieve[i]:
#      → 8.3         for j in range(i * i, upper + 1, i):
#      → 8.3             sieve[j] = False
#      → 8.4 return sum((i for i in range(lower, upper + 1) if sieve[i]))
#   9. def is_prime(num) → stored in memory, not executed
#   10. def is_prime(num) → stored in memory, not executed
#   11. num = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(is_prime(...))
#      → calls is_prime(num) with num=25
#      → 14.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_primes, main, is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     if → evaluates condition, runs matching branch
#     print(sum_of_primes(...)) → sum_of_primes() runs first → value passed to print()
#     print(sum_of_primes(...)) → sum_of_primes() runs first → value passed to print()
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 101:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def power(base, exp) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: * - ** ==
# Built-in functions: float, input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   power(base, exp) → each argument is evaluated first, then the function body runs
#   power(2, 5) runs first → then print() outputs the result
#   power(3, 4) runs first → then print() outputs the result
#   power(2, 8) runs first → then print() outputs the result
#   power(10, 25) runs first → then print() outputs the result
#   exp == 0 → evaluated as True or False (determines which branch runs)
#   base * power(...) → Python looks up base, then power(...), then applies *
#   base ** exp → Python looks up base, then exp, then applies **
#   power(2, 5) → each argument is evaluated first, then the function body runs
#   power(3, 4) → each argument is evaluated first, then the function body runs
#   power(2, 8) → each argument is evaluated first, then the function body runs
#   power(10, 25) → each argument is evaluated first, then the function body runs
#   power(base, exp - 1) → each argument is evaluated first, then the function body runs
#   exp - 1 → Python looks up exp, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def power(base, exp) → stored in memory, not executed
#   2. base = ... → assigns value to variable
#   3. exp = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(?)
#   6. def power(base, exp) → stored in memory, not executed
#   7. print(power(...))
#      → calls power(base, exp) with base=2, exp=5
#      → 7.1 return 1
#   8. def power(base, exp) → stored in memory, not executed
#   9. print(power(...))
#      → calls power(base, exp) with base=3, exp=4
#      → 9.1 return 1
#   10. def power(base, exp) → stored in memory, not executed
#   11. print(power(...))
#      → calls power(base, exp) with base=2, exp=8
#      → 11.1 return 1
#   12. def power(base, exp) → stored in memory, not executed
#   13. def power(base, exp) → stored in memory, not executed
#   14. base = ... → assigns value to variable
#   15. exp = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(power(...))
#      → calls power(base, exp) with base=10, exp=25
#      → 18.1 return 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (power) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     base = ... → stored in memory
#     exp = ... → stored in memory
#     result = ... → stored in memory
#     print(?)
#     print(power(...)) → power() runs first → value passed to print()
#     print(power(...)) → power() runs first → value passed to print()
#     print(power(...)) → power() runs first → value passed to print()
#     base = ... → stored in memory
#     exp = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(power(...)) → power() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 102:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_anagram() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: != ==
# Built-in functions: Counter, input, len, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_anagram() → each argument is evaluated first, then the function body runs
#   is_anagram(string1, string2) runs first → then print() outputs the result
#   is_anagram('Listen', 'Silent') runs first → then print() outputs the result
#   is_anagram('hello', 'olleh') runs first → then print() outputs the result
#   is_anagram(string1, string2) → each argument is evaluated first, then the function body runs
#   is_anagram('world', 'python') runs first → then print() outputs the result
#   sort1 == sort2 → evaluated as True or False (determines which branch runs)
#   sorted(...) == sorted(...) → evaluated as True or False (determines which branch runs)
#   Counter(...) == Counter(...) → evaluated as True or False (determines which branch runs)
#   is_anagram('Listen', 'Silent') → each argument is evaluated first, then the function body runs
#   len(...) != len(...) → evaluated as True or False (determines which branch runs)
#   is_anagram('hello', 'olleh') → each argument is evaluated first, then the function body runs
#   is_anagram('world', 'python') → each argument is evaluated first, then the function body runs
#   s1.count(...) != s2.count(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_anagram() → stored in memory, not executed
#   2. is_anagram() → calls is_anagram(string1, string2) with 
#      → 2.1 s1, s2 = (string1.replace(' ', '').lower(), string2.replace(' ', '').lower())
#      → 2.2 if len(s1) != len(s2):
#      → 2.2     return False
#      → 2.3 for char in set(s1):
#      → 2.3     if s1.count(char) != s2.count(char):
#      → 2.3         return False
#      → 2.4 return True
#   3. def is_anagram(string1, string2) → stored in memory, not executed
#   4. string1 = ... → assigns value to variable
#   5. string2 = ... → assigns value to variable
#   6. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1=string1, string2=string2
#      → 6.1 s1, s2 = (string1.replace(' ', '').lower(), string2.replace(' ', '').lower())
#      → 6.2 if len(s1) != len(s2):
#      → 6.2     return False
#      → 6.3 for char in set(s1):
#      → 6.3     if s1.count(char) != s2.count(char):
#      → 6.3         return False
#      → 6.4 return True
#   7. def is_anagram(string1, string2) → stored in memory, not executed
#   8. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='Listen', string2='Silent'
#      → 8.1 s1, s2 = (string1.replace(' ', '').lower(), string2.replace(' ', '').lower())
#      → 8.2 if len(s1) != len(s2):
#      → 8.2     return False
#      → 8.3 for char in set(s1):
#      → 8.3     if s1.count(char) != s2.count(char):
#      → 8.3         return False
#      → 8.4 return True
#   9. def is_anagram(string1, string2) → stored in memory, not executed
#   10. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='hello', string2='olleh'
#      → 10.1 s1, s2 = (string1.replace(' ', '').lower(), string2.replace(' ', '').lower())
#      → 10.2 if len(s1) != len(s2):
#      → 10.2     return False
#      → 10.3 for char in set(s1):
#      → 10.3     if s1.count(char) != s2.count(char):
#      → 10.3         return False
#      → 10.4 return True
#   11. string1 = ... → assigns value to variable
#   12. string2 = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='world', string2='python'
#      → 15.1 s1, s2 = (string1.replace(' ', '').lower(), string2.replace(' ', '').lower())
#      → 15.2 if len(s1) != len(s2):
#      → 15.2     return False
#      → 15.3 for char in set(s1):
#      → 15.3     if s1.count(char) != s2.count(char):
#      → 15.3         return False
#      → 15.4 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_anagram) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     is_anagram() → jumps into is_anagram(), runs body, returns
#     string1 = ... → stored in memory
#     string2 = ... → stored in memory
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     string1 = ... → stored in memory
#     string2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#
#   Loop: takes one item from set(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 104:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_perfect_square(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: * < ** ==
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_perfect_square(num) → each argument is evaluated first, then the function body runs
#   is_perfect_square(16) runs first → then print() outputs the result
#   is_perfect_square(14) runs first → then print() outputs the result
#   is_perfect_square(25) runs first → then print() outputs the result
#   ? == user → evaluated as True or False (determines which branch runs)
#   ? == num → evaluated as True or False (determines which branch runs)
#   num < 0 → evaluated as True or False (determines which branch runs)
#   is_perfect_square(16) → each argument is evaluated first, then the function body runs
#   is_perfect_square(14) → each argument is evaluated first, then the function body runs
#   is_perfect_square(25) → each argument is evaluated first, then the function body runs
#   root * root → Python looks up root, then root, then applies *
#   num ** 0.5 → Python looks up num, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. user = ... → assigns value to variable
#   3. root = ... → assigns value to variable
#   4. print(?)
#   5. def is_perfect_square(num) → stored in memory, not executed
#   6. num = ... → assigns value to variable
#   7. if ... → only one branch executes
#   8. def is_perfect_square(num) → stored in memory, not executed
#   9. print(is_perfect_square(...))
#      → calls is_perfect_square(num) with num=16
#      → 9.1 return root * root == num
#   10. print(is_perfect_square(...))
#      → calls is_perfect_square(num) with num=14
#      → 10.1 return root * root == num
#   11. def is_perfect_square(num) → stored in memory, not executed
#   12. print(is_perfect_square(...))
#      → calls is_perfect_square(num) with num=25
#      → 12.1 return root * root == num
#   13. def is_perfect_square(num) → stored in memory, not executed
#   14. def is_perfect_square(num) → stored in memory, not executed
#   15. num = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(is_perfect_square(...))
#      → calls is_perfect_square(num) with num=25
#      → 18.1 return root * root == num
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_perfect_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     user = ... → stored in memory
#     root = ... → stored in memory
#     print(?)
#     num = ... → stored in memory
#     if → evaluates condition, runs matching branch
#     print(is_perfect_square(...)) → is_perfect_square() runs first → value passed to print()
#     print(is_perfect_square(...)) → is_perfect_square() runs first → value passed to print()
#     print(is_perfect_square(...)) → is_perfect_square() runs first → value passed to print()
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_perfect_square(...)) → is_perfect_square() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 105:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def all_odd_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: filter, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   all_odd_numbers(lst) runs first → then print() outputs the result
#   all_odd_numbers(?) runs first → then print() outputs the result
#   all_odd_numbers(numbers) → each argument is evaluated first, then the function body runs
#   all_odd_numbers(lst) → each argument is evaluated first, then the function body runs
#   all_odd_numbers(?) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#   n % 2 → Python looks up n, then 2, then applies %
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def all_odd_numbers(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(all_odd_numbers(...))
#      → calls all_odd_numbers(numbers) with numbers=lst
#      → 3.1 return count
#   4. def all_odd_numbers(numbers) → stored in memory, not executed
#   5. print(all_odd_numbers(...))
#      → calls all_odd_numbers(numbers) with numbers=[1, 2, 3, 4, 5]
#      → 5.1 return count
#   6. def all_odd_numbers(numbers) → stored in memory, not executed
#   7. print(all_odd_numbers(...))
#      → calls all_odd_numbers(numbers) with numbers=[10, 15, 20, 25]
#      → 7.1 return count
#   8. def all_odd_numbers(numbers) → stored in memory, not executed
#   9. def all_odd_numbers(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(all_odd_numbers(...))
#      → calls all_odd_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (all_odd_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(all_odd_numbers(...)) → all_odd_numbers() runs first → value passed to print()
#     print(all_odd_numbers(...)) → all_odd_numbers() runs first → value passed to print()
#     print(all_odd_numbers(...)) → all_odd_numbers() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(all_odd_numbers(...)) → all_odd_numbers() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 106:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_odd_numbers() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: filter, input, int, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_odd_numbers() → each argument is evaluated first, then the function body runs
#   sum_of_all_odd() → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   num % 2 → Python looks up num, then 2, then applies %
#   number % 2 → Python looks up number, then 2, then applies %
#   n % 2 → Python looks up n, then 2, then applies %
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_odd_numbers() → stored in memory, not executed
#   2. sum_of_odd_numbers() → calls sum_of_odd_numbers() with 
#      → 2.1 user_input = input('Enter a list of numbers separated by spaces: ')
#      → 2.2 split_numbers = user_input.split()
#      → 2.3 int_numbers = []
#      → 2.4 for num in split_numbers:
#      → 2.4     int_numbers.append(int(num))
#      → 2.5 odd_sum = 0
#      → 2.6 for num in int_numbers:
#      → 2.6     if num % 2 != 0:
#      → 2.6         odd_sum += num
#      → 2.7 result = ('The sum of all odd numbers is:', odd_sum)
#      → 2.8 return result
#   3. def sum_of_all_odd() → stored in memory, not executed
#   4. sum_of_all_odd() → calls sum_of_all_odd() with 
#      → 4.1 numbers = list(map(int, input('Enter numbers: ').split()))
#      → 4.2 print(sum(filter(lambda x: x % 2 != 0, numbers)))
#   5. def sum_of_odd_numbers() → stored in memory, not executed
#   6. sum_of_odd_numbers() → calls sum_of_odd_numbers() with 
#      → 6.1 user_input = input('Enter a list of numbers separated by spaces: ')
#      → 6.2 split_numbers = user_input.split()
#      → 6.3 int_numbers = []
#      → 6.4 for num in split_numbers:
#      → 6.4     int_numbers.append(int(num))
#      → 6.5 odd_sum = 0
#      → 6.6 for num in int_numbers:
#      → 6.6     if num % 2 != 0:
#      → 6.6         odd_sum += num
#      → 6.7 result = ('The sum of all odd numbers is:', odd_sum)
#      → 6.8 return result
#   7. def sum_of_all_odd() → stored in memory, not executed
#   8. sum_of_all_odd() → calls sum_of_all_odd() with 
#      → 8.1 numbers = list(map(int, input('Enter numbers: ').split()))
#      → 8.2 print(sum(filter(lambda x: x % 2 != 0, numbers)))
#   9. def sum_of_odd_numbers() → stored in memory, not executed
#   10. def sum_of_odd_numbers() → stored in memory, not executed
#   11. user_input = ... → assigns value to variable
#   12. split_numbers = ... → assigns value to variable
#   13. int_numbers = ... → assigns value to variable
#   14. for num in split_numbers → loops over each item
#      → 14.1 int_numbers.append(int(...))
#   15. odd_sum = ... → assigns value to variable
#   16. for num in int_numbers → loops over each item
#   17. print('The sum of all odd numbers is:', odd_sum)
#   18. user_input = ... → assigns value to variable
#   19. split_numbers = ... → assigns value to variable
#   20. int_numbers = ... → assigns value to variable
#   21. for num in split_numbers → loops over each item
#      → 21.1 int_numbers.append(int(...))
#   22. odd_sum = ... → assigns value to variable
#   23. for num in int_numbers → loops over each item
#   24. print('The sum of all odd numbers is:', odd_sum)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_all_odd, sum_of_odd_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sum_of_odd_numbers() → jumps into sum_of_odd_numbers(), runs body, returns
#     sum_of_all_odd() → jumps into sum_of_all_odd(), runs body, returns
#     sum_of_odd_numbers() → jumps into sum_of_odd_numbers(), runs body, returns
#     sum_of_all_odd() → jumps into sum_of_all_odd(), runs body, returns
#     user_input = ... → stored in memory
#     split_numbers = ... → stored in memory
#     int_numbers = ... → stored in memory
#     for num in split_numbers → runs body once per item
#     odd_sum = ... → stored in memory
#     for num in int_numbers → runs body once per item
#     print('The sum of all odd numbers is:', odd_sum)
#     user_input = ... → stored in memory
#     split_numbers = ... → stored in memory
#     int_numbers = ... → stored in memory
#     for num in split_numbers → runs body once per item
#     odd_sum = ... → stored in memory
#     for num in int_numbers → runs body once per item
#     print('The sum of all odd numbers is:', odd_sum)
#
#   Loop: takes one item from split_numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 107:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common_char() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: & != in and not in
# Built-in functions: input, len, list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common_char() runs first → then print() outputs the result
#   common_char(name1, name2) runs first → then print() outputs the result
#   common_char('hello', 'world') runs first → then print() outputs the result
#   common_char('programming', 'python') runs first → then print() outputs the result
#   common_char(string1, string2) → each argument is evaluated first, then the function body runs
#   common_char('world', 'python') runs first → then print() outputs the result
#   len(...) != 2 → evaluated as True or False (determines which branch runs)
#   common_char() → each argument is evaluated first, then the function body runs
#   common_char(name1, name2) → each argument is evaluated first, then the function body runs
#   common_char('hello', 'world') → each argument is evaluated first, then the function body runs
#   common_char('programming', 'python') → each argument is evaluated first, then the function body runs
#   common_char('world', 'python') → each argument is evaluated first, then the function body runs
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#   char in str2 → evaluated as True or False (determines which branch runs)
#   char not in common → evaluated as True or False (determines which branch runs)
#   char in string2 → evaluated as True or False (determines which branch runs)
#   char not in characters → evaluated as True or False (determines which branch runs)
#   c in string2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common_char() → stored in memory, not executed
#   2. print(common_char(...))
#      → calls common_char() with 
#      → 2.1 return list(map(lambda char: char, str1))
#   3. def common_char(string1, string2) → stored in memory, not executed
#   4. name1 = ... → assigns value to variable
#   5. name2 = ... → assigns value to variable
#   6. print(common_char(...))
#      → calls common_char() with 
#      → 6.1 return list(map(lambda char: char, str1))
#   7. def common_char(string1, string2) → stored in memory, not executed
#   8. print(common_char(...))
#      → calls common_char() with 
#      → 8.1 return list(map(lambda char: char, str1))
#   9. def common_char(string1, string2) → stored in memory, not executed
#   10. print(common_char(...))
#      → calls common_char() with 
#      → 10.1 return list(map(lambda char: char, str1))
#   11. def common_char() → stored in memory, not executed
#   12. def common_char() → stored in memory, not executed
#   13. string1 = ... → assigns value to variable
#   14. string2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(common_char(...))
#      → calls common_char() with 
#      → 17.1 return list(map(lambda char: char, str1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common_char) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(common_char(...)) → common_char() runs first → value passed to print()
#     name1 = ... → stored in memory
#     name2 = ... → stored in memory
#     print(common_char(...)) → common_char() runs first → value passed to print()
#     print(common_char(...)) → common_char() runs first → value passed to print()
#     print(common_char(...)) → common_char() runs first → value passed to print()
#     string1 = ... → stored in memory
#     string2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_char(...)) → common_char() runs first → value passed to print()
#
#   Loop: takes one item from str1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 108:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: in or not in
# Built-in functions: input, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates() → each argument is evaluated first, then the function body runs
#   char not in unique_user → evaluated as True or False (determines which branch runs)
#   c in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates() → stored in memory, not executed
#   2. remove_duplicates() → calls remove_duplicates() with 
#      → 2.1 user = input('type string: ')
#      → 2.2 unique_user = set()
#      → 2.3 for char in user:
#      → 2.3     if char not in unique_user:
#      → 2.3         unique_user.add(char)
#      → 2.4 result = ''.join(unique_user)
#      → 2.5 return result
#   3. def remove_duplicates() → stored in memory, not executed
#   4. remove_duplicates() → calls remove_duplicates() with 
#      → 4.1 user = input('type string: ')
#      → 4.2 unique_user = set()
#      → 4.3 for char in user:
#      → 4.3     if char not in unique_user:
#      → 4.3         unique_user.add(char)
#      → 4.4 result = ''.join(unique_user)
#      → 4.5 return result
#   5. def remove_duplicates() → stored in memory, not executed
#   6. remove_duplicates() → calls remove_duplicates() with 
#      → 6.1 user = input('type string: ')
#      → 6.2 unique_user = set()
#      → 6.3 for char in user:
#      → 6.3     if char not in unique_user:
#      → 6.3         unique_user.add(char)
#      → 6.4 result = ''.join(unique_user)
#      → 6.5 return result
#   7. def remove_duplicates() → stored in memory, not executed
#   8. remove_duplicates() → calls remove_duplicates() with 
#      → 8.1 user = input('type string: ')
#      → 8.2 unique_user = set()
#      → 8.3 for char in user:
#      → 8.3     if char not in unique_user:
#      → 8.3         unique_user.add(char)
#      → 8.4 result = ''.join(unique_user)
#      → 8.5 return result
#   9. def remove_duplicates() → stored in memory, not executed
#   10. def remove_duplicates() → stored in memory, not executed
#   11. user = ... → assigns value to variable
#   12. unique_user = ... → assigns value to variable
#   13. for char in user → loops over each item
#   14. print(''.join(...))
#   15. user = ... → assigns value to variable
#   16. unique_user = ... → assigns value to variable
#   17. for char in user → loops over each item
#   18. print(''.join(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     remove_duplicates() → jumps into remove_duplicates(), runs body, returns
#     remove_duplicates() → jumps into remove_duplicates(), runs body, returns
#     remove_duplicates() → jumps into remove_duplicates(), runs body, returns
#     remove_duplicates() → jumps into remove_duplicates(), runs body, returns
#     user = ... → stored in memory
#     unique_user = ... → stored in memory
#     for char in user → runs body once per item
#     print(''.join(...))
#     user = ... → stored in memory
#     unique_user = ... → stored in memory
#     for char in user → runs body once per item
#     print(''.join(...))
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 109:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_armstrong(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: % > ** ==
# Built-in functions: input, int, len, str, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_armstrong(number) → each argument is evaluated first, then the function body runs
#   is_armstrong(153) runs first → then print() outputs the result
#   is_armstrong(9474) runs first → then print() outputs the result
#   is_armstrong(123) runs first → then print() outputs the result
#   is_armstrong(370) runs first → then print() outputs the result
#   is_armstrong(25) runs first → then print() outputs the result
#   total == number → evaluated as True or False (determines which branch runs)
#   sum(...) == number → evaluated as True or False (determines which branch runs)
#   is_armstrong(153) → each argument is evaluated first, then the function body runs
#   is_armstrong(9474) → each argument is evaluated first, then the function body runs
#   is_armstrong(123) → each argument is evaluated first, then the function body runs
#   num > 0 → evaluated as True or False (determines which branch runs)
#   is_armstrong(370) → each argument is evaluated first, then the function body runs
#   is_armstrong(25) → each argument is evaluated first, then the function body runs
#   digit ** power → Python looks up digit, then power, then applies **
#   num % 10 → Python looks up num, then 10, then applies %
#   d ** power → Python looks up d, then power, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_armstrong(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. if ... → only one branch executes
#   4. print(number)
#   5. def is_armstrong(number) → stored in memory, not executed
#   6. print(is_armstrong(...))
#      → calls is_armstrong(number) with number=153
#      → 6.1 return total == number
#   7. print(is_armstrong(...))
#      → calls is_armstrong(number) with number=9474
#      → 7.1 return total == number
#   8. print(is_armstrong(...))
#      → calls is_armstrong(number) with number=123
#      → 8.1 return total == number
#   9. def is_armstrong(number) → stored in memory, not executed
#   10. print(is_armstrong(...))
#      → calls is_armstrong(number) with number=370
#      → 10.1 return total == number
#   11. def is_armstrong(number) → stored in memory, not executed
#   12. def is_armstrong(number) → stored in memory, not executed
#   13. number = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(is_armstrong(...))
#      → calls is_armstrong(number) with number=25
#      → 16.1 return total == number
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_armstrong) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     if → evaluates condition, runs matching branch
#     print(number)
#     print(is_armstrong(...)) → is_armstrong() runs first → value passed to print()
#     print(is_armstrong(...)) → is_armstrong() runs first → value passed to print()
#     print(is_armstrong(...)) → is_armstrong() runs first → value passed to print()
#     print(is_armstrong(...)) → is_armstrong() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_armstrong(...)) → is_armstrong() runs first → value passed to print()
#
#   Loop: takes one item from str(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 111:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_smallest_element() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < >=
# Built-in functions: input, int, len, max, min, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_smallest_element() runs first → then print() outputs the result
#   second_smallest_element(arg) runs first → then print() outputs the result
#   second_smallest_element(?) runs first → then print() outputs the result
#   second_smallest_element(elements) → each argument is evaluated first, then the function body runs
#   len(...) >= 2 → evaluated as True or False (determines which branch runs)
#   second_smallest_element() → each argument is evaluated first, then the function body runs
#   len(...) < 2 → evaluated as True or False (determines which branch runs)
#   second_smallest_element(arg) → each argument is evaluated first, then the function body runs
#   second_smallest_element(?) → each argument is evaluated first, then the function body runs
#   smallest < x → evaluated as True or False (determines which branch runs)
#   smallest < second → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_smallest_element() → stored in memory, not executed
#   2. print(second_smallest_element(...))
#      → calls second_smallest_element() with 
#      → 2.1 return sorted_numbers[1]
#   3. def second_smallest_element(elements) → stored in memory, not executed
#   4. arg = ... → assigns value to variable
#   5. print(second_smallest_element(...))
#      → calls second_smallest_element() with 
#      → 5.1 return sorted_numbers[1]
#   6. def second_smallest_element(elements) → stored in memory, not executed
#   7. print(second_smallest_element(...))
#      → calls second_smallest_element() with 
#      → 7.1 return sorted_numbers[1]
#   8. def second_smallest_element(elements) → stored in memory, not executed
#   9. print(second_smallest_element(...))
#      → calls second_smallest_element() with 
#      → 9.1 return sorted_numbers[1]
#   10. def second_smallest_element(elements) → stored in memory, not executed
#   11. print(second_smallest_element(...))
#      → calls second_smallest_element() with 
#      → 11.1 return sorted_numbers[1]
#   12. def second_smallest_element() → stored in memory, not executed
#   13. def second_smallest_element() → stored in memory, not executed
#   14. elements = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(second_smallest_element(...))
#      → calls second_smallest_element() with 
#      → 17.1 return sorted_numbers[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_smallest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     arg = ... → stored in memory
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     elements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#
#   Loop: takes one item from user_input.split(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 112:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def generate_permutations(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: +
# Built-in functions: backtrack, input, len, list, permutations, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   generate_permutations(string) → each argument is evaluated first, then the function body runs
#   generate_permutations('abc') runs first → then print() outputs the result
#   generate_permutations('ab') runs first → then print() outputs the result
#   generate_permutations('world') runs first → then print() outputs the result
#   generate_permutations('abc') → each argument is evaluated first, then the function body runs
#   generate_permutations('ab') → each argument is evaluated first, then the function body runs
#   generate_permutations('world') → each argument is evaluated first, then the function body runs
#   path + ? → Python looks up path, then ?, then applies +
#   ? + ? → Python looks up ?, then ?, then applies +
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from itertools import permutations
#   2. def generate_permutations(string) → stored in memory, not executed
#   3. string = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print('Permutations:', result)
#   6. import itertools
#   7. string = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(list(...))
#   10. def generate_permutations(string) → stored in memory, not executed
#   11. print(generate_permutations(...))
#      → calls generate_permutations(string) with string='abc'
#      → 11.1 return result
#   12. def generate_permutations(string) → stored in memory, not executed
#   13. print(generate_permutations(...))
#      → calls generate_permutations(string) with string='ab'
#      → 13.1 return result
#   14. def generate_permutations(string) → stored in memory, not executed
#   15. def generate_permutations(string) → stored in memory, not executed
#   16. string = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(generate_permutations(...))
#      → calls generate_permutations(string) with string='world'
#      → 19.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (generate_permutations) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print('Permutations:', result)
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(list(...))
#     print(generate_permutations(...)) → generate_permutations() runs first → value passed to print()
#     print(generate_permutations(...)) → generate_permutations() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(generate_permutations(...)) → generate_permutations() runs first → value passed to print()
#
#   Loop: takes one item from permutations(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 114:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_three(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > >= and
# Built-in functions: max, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_three(lst) → each argument is evaluated first, then the function body runs
#   main(56, 8, 445) → each argument is evaluated first, then the function body runs
#   max_of_three() → each argument is evaluated first, then the function body runs
#   max_of_three(10, 25, 15) runs first → then print() outputs the result
#   max_of_three(100, 50, 75) runs first → then print() outputs the result
#   max_of_three(7, 3, 11) runs first → then print() outputs the result
#   max_of_three(?) runs first → then print() outputs the result
#   max_of_three(10, 25, 15) → each argument is evaluated first, then the function body runs
#   max_of_three(100, 50, 75) → each argument is evaluated first, then the function body runs
#   max_of_three(7, 3, 11) → each argument is evaluated first, then the function body runs
#   max_of_three(?) → each argument is evaluated first, then the function body runs
#   num > largest → evaluated as True or False (determines which branch runs)
#   a >= b → evaluated as True or False (determines which branch runs)
#   a >= c → evaluated as True or False (determines which branch runs)
#   b >= a → evaluated as True or False (determines which branch runs)
#   b >= c → evaluated as True or False (determines which branch runs)
#   x > y → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_three(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. max_of_three(lst) → calls max_of_three(lst) with lst=lst
#      → 3.1 return largest
#   4. def main(a, b, c) → stored in memory, not executed
#   5. main(56, 8, 445) → calls main(a, b, c) with a=56, b=8, c=445
#      → 5.1 return max(a, b, c)
#   6. def max_of_three(a, b, c) → stored in memory, not executed
#   7. max_of_three() → calls max_of_three(lst) with 
#      → 7.1 return largest
#   8. def max_of_three(a, b, c) → stored in memory, not executed
#   9. print(max_of_three(...))
#      → calls max_of_three(lst) with lst=10
#      → 9.1 return largest
#   10. def max_of_three(a, b, c) → stored in memory, not executed
#   11. print(max_of_three(...))
#      → calls max_of_three(lst) with lst=100
#      → 11.1 return largest
#   12. def max_of_three(a, b, c) → stored in memory, not executed
#   13. print(max_of_three(...))
#      → calls max_of_three(lst) with lst=7
#      → 13.1 return largest
#   14. def max_of_three(lst) → stored in memory, not executed
#   15. def max_of_three(lst) → stored in memory, not executed
#   16. lst = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(max_of_three(...))
#      → calls max_of_three(lst) with lst=[1, 3, 5, 7]
#      → 19.1 return largest
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, max_of_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     max_of_three(lst) → jumps into max_of_three(), runs body, returns
#     main(56, 8, 445) → jumps into main(), runs body, returns
#     max_of_three() → jumps into max_of_three(), runs body, returns
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 115:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def length_string(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: enumerate, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   length_string(name) runs first → then print() outputs the result
#   length_list(lst) runs first → then print() outputs the result
#   length_string('hello') runs first → then print() outputs the result
#   length_string('python') runs first → then print() outputs the result
#   length_string('world') runs first → then print() outputs the result
#   length_string(string) → each argument is evaluated first, then the function body runs
#   length_string(name) → each argument is evaluated first, then the function body runs
#   length_list(lst) → each argument is evaluated first, then the function body runs
#   s == '' → evaluated as True or False (determines which branch runs)
#   1 + length_string(...) → Python looks up 1, then length_string(...), then applies +
#   length_string('hello') → each argument is evaluated first, then the function body runs
#   length_string('python') → each argument is evaluated first, then the function body runs
#   length_string('world') → each argument is evaluated first, then the function body runs
#   length_string(?) → each argument is evaluated first, then the function body runs
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def length_string(string) → stored in memory, not executed
#   2. name = ... → assigns value to variable
#   3. print(length_string(...))
#      → calls length_string(string) with string=name
#      → 3.1 return count
#   4. def length_list(string) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(length_list(...))
#      → calls length_list(string) with string=lst
#      → 6.1 count = 0
#      → 6.2 for char in string:
#      → 6.2     count += 1
#      → 6.3 return count
#   7. def length_string(s) → stored in memory, not executed
#   8. print(length_string(...))
#      → calls length_string(string) with string='hello'
#      → 8.1 return count
#   9. def length_string(s) → stored in memory, not executed
#   10. print(length_string(...))
#      → calls length_string(string) with string='python'
#      → 10.1 return count
#   11. def length_string(s) → stored in memory, not executed
#   12. print(length_string(...))
#      → calls length_string(string) with string='world'
#      → 12.1 return count
#   13. def length_string(string) → stored in memory, not executed
#   14. def length_string(string) → stored in memory, not executed
#   15. string = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(length_string(...))
#      → calls length_string(string) with string='world'
#      → 18.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (length_list, length_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     name = ... → stored in memory
#     print(length_string(...)) → length_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(length_list(...)) → length_list() runs first → value passed to print()
#     print(length_string(...)) → length_string() runs first → value passed to print()
#     print(length_string(...)) → length_string() runs first → value passed to print()
#     print(length_string(...)) → length_string() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(length_string(...)) → length_string() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 117:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def translate(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + and not in
# Built-in functions: input, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   translate(text) → each argument is evaluated first, then the function body runs
#   translate('this is fun') runs first → then print() outputs the result
#   translate('hello') runs first → then print() outputs the result
#   translate('world') runs first → then print() outputs the result
#   translate('this is fun') → each argument is evaluated first, then the function body runs
#   translate('hello') → each argument is evaluated first, then the function body runs
#   translate('world') → each argument is evaluated first, then the function body runs
#   ? + c → Python looks up ?, then c, then applies +
#   char not in vowels → evaluated as True or False (determines which branch runs)
#   ? + char → Python looks up ?, then char, then applies +
#   c + 'o' → Python looks up c, then 'o', then applies +
#   char + 'o' → Python looks up char, then 'o', then applies +
#   c not in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def translate(text) → stored in memory, not executed
#   2. text = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def translate(text) → stored in memory, not executed
#   6. print(translate(...))
#      → calls translate(text) with text='this is fun'
#      → 6.1 return translated
#   7. def translate(text) → stored in memory, not executed
#   8. print(translate(...))
#      → calls translate(text) with text='hello'
#      → 8.1 return translated
#   9. def translate(text) → stored in memory, not executed
#   10. def translate(text) → stored in memory, not executed
#   11. text = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(translate(...))
#      → calls translate(text) with text='world'
#      → 14.1 return translated
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (translate) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(translate(...)) → translate() runs first → value passed to print()
#     print(translate(...)) → translate() runs first → value passed to print()
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(translate(...)) → translate() runs first → value passed to print()
#
#   Loop: takes one item from text each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 118:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * +
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum(numbers) runs first → then print() outputs the result
#   multiply(numbers) runs first → then print() outputs the result
#   sum(?) runs first → then print() outputs the result
#   multiply(?) runs first → then print() outputs the result
#   sum(numbers) → each argument is evaluated first, then the function body runs
#   multiply(numbers) → each argument is evaluated first, then the function body runs
#   sum(?) → each argument is evaluated first, then the function body runs
#   multiply(?) → each argument is evaluated first, then the function body runs
#   x * y → Python looks up x, then y, then applies *
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum(numbers) → stored in memory, not executed
#   2. def multiply(numbers) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. print('Sum:', sum(...))
#   5. print('Multiply:', multiply(...))
#   6. def sum(numbers) → stored in memory, not executed
#   7. import builtins
#   8. def multiply(numbers) → stored in memory, not executed
#   9. print(sum(...))
#      → calls sum(numbers) with numbers=[1, 2, 3, 4]
#      → 9.1 return total
#   10. print(multiply(...))
#      → calls multiply(numbers) with numbers=[1, 2, 3, 4]
#      → 10.1 from functools import reduce
#      → 10.2 return reduce(lambda x, y: x * y, numbers)
#   11. def sum(numbers) → stored in memory, not executed
#   12. def multiply(numbers) → stored in memory, not executed
#   13. print(sum(...))
#      → calls sum(numbers) with numbers=[5, 10, 15]
#      → 13.1 return total
#   14. print(multiply(...))
#      → calls multiply(numbers) with numbers=[2, 3, 4]
#      → 14.1 from functools import reduce
#      → 14.2 return reduce(lambda x, y: x * y, numbers)
#   15. def sum(numbers) → stored in memory, not executed
#   16. def sum(numbers) → stored in memory, not executed
#   17. numbers = ... → assigns value to variable
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. print(sum(...))
#      → calls sum(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 20.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply, sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print('Sum:', sum(...))
#     print('Multiply:', multiply(...))
#     print(sum(...)) → sum() runs first → value passed to print()
#     print(multiply(...)) → multiply() runs first → value passed to print()
#     print(sum(...)) → sum() runs first → value passed to print()
#     print(multiply(...)) → multiply() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum(...)) → sum() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 119:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + <=
# Built-in functions: input, len, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse(string) → each argument is evaluated first, then the function body runs
#   reverse('I am testing') runs first → then print() outputs the result
#   reverse('hello') runs first → then print() outputs the result
#   reverse('python') runs first → then print() outputs the result
#   reverse('world') runs first → then print() outputs the result
#   reverse('I am testing') → each argument is evaluated first, then the function body runs
#   reverse('hello') → each argument is evaluated first, then the function body runs
#   len(...) <= 1 → evaluated as True or False (determines which branch runs)
#   reverse(...) + ? → Python looks up reverse(...), then ?, then applies +
#   reverse('python') → each argument is evaluated first, then the function body runs
#   reverse('world') → each argument is evaluated first, then the function body runs
#   char + reversed_string → Python looks up char, then reversed_string, then applies +
#   reverse(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def reverse(string) → stored in memory, not executed
#   6. print(reverse(...))
#      → calls reverse(string) with string='I am testing'
#      → 6.1 return reversed_string
#   7. def reverse(string) → stored in memory, not executed
#   8. print(reverse(...))
#      → calls reverse(string) with string='hello'
#      → 8.1 return reversed_string
#   9. def reverse(string) → stored in memory, not executed
#   10. print(reverse(...))
#      → calls reverse(string) with string='python'
#      → 10.1 return reversed_string
#   11. def reverse(string) → stored in memory, not executed
#   12. def reverse(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(reverse(...))
#      → calls reverse(string) with string='world'
#      → 16.1 return reversed_string
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse(...)) → reverse() runs first → value passed to print()
#     print(reverse(...)) → reverse() runs first → value passed to print()
#     print(reverse(...)) → reverse() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse(...)) → reverse() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 120:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def palindromes(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: + - < != // ==
# Built-in functions: all, len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   palindromes(string) runs first → then print() outputs the result
#   is_palindrome('radar') runs first → then print() outputs the result
#   is_palindrome('hello') runs first → then print() outputs the result
#   is_palindrome('level') runs first → then print() outputs the result
#   is_palindrome('madam') runs first → then print() outputs the result
#   palindromes(string) → each argument is evaluated first, then the function body runs
#   palindromes('world') runs first → then print() outputs the result
#   string == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome('radar') → each argument is evaluated first, then the function body runs
#   is_palindrome('hello') → each argument is evaluated first, then the function body runs
#   left < right → evaluated as True or False (determines which branch runs)
#   is_palindrome('level') → each argument is evaluated first, then the function body runs
#   is_palindrome('madam') → each argument is evaluated first, then the function body runs
#   palindromes('world') → each argument is evaluated first, then the function body runs
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? != ? → evaluated as True or False (determines which branch runs)
#   ? == ? → evaluated as True or False (determines which branch runs)
#   len(...) // 2 → Python looks up len(...), then 2, then applies //
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def palindromes(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(palindromes(...))
#      → calls palindromes(string) with string=string
#      → 3.1 return True
#   4. def is_palindrome(string) → stored in memory, not executed
#   5. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='radar'
#      → 5.1 return all((string[i] == string[-(i + 1)] for i in range(len(string) // 2)))
#   6. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='hello'
#      → 6.1 return all((string[i] == string[-(i + 1)] for i in range(len(string) // 2)))
#   7. def is_palindrome(string) → stored in memory, not executed
#   8. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='level'
#      → 8.1 return all((string[i] == string[-(i + 1)] for i in range(len(string) // 2)))
#   9. def is_palindrome(string) → stored in memory, not executed
#   10. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='madam'
#      → 10.1 return all((string[i] == string[-(i + 1)] for i in range(len(string) // 2)))
#   11. def palindromes(string) → stored in memory, not executed
#   12. def palindromes(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(palindromes(...))
#      → calls palindromes(string) with string='world'
#      → 16.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (palindromes, is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(palindromes(...)) → palindromes() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(palindromes(...)) → palindromes() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 121:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: - < != ==
# Built-in functions: len, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_palindrome(name) runs first → then print() outputs the result
#   is_palindrome('racecar') runs first → then print() outputs the result
#   is_palindrome('madam') runs first → then print() outputs the result
#   is_palindrome('hello') runs first → then print() outputs the result
#   is_palindrome(string) → each argument is evaluated first, then the function body runs
#   is_palindrome('world') runs first → then print() outputs the result
#   string == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome(name) → each argument is evaluated first, then the function body runs
#   left < right → evaluated as True or False (determines which branch runs)
#   is_palindrome('racecar') → each argument is evaluated first, then the function body runs
#   string == ''.join(...) → evaluated as True or False (determines which branch runs)
#   is_palindrome('madam') → each argument is evaluated first, then the function body runs
#   is_palindrome('hello') → each argument is evaluated first, then the function body runs
#   is_palindrome('world') → each argument is evaluated first, then the function body runs
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? != ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(string) → stored in memory, not executed
#   2. name = ... → assigns value to variable
#   3. print(is_palindrome(...))
#      → calls is_palindrome(string) with string=name
#      → 3.1 return True
#   4. def is_palindrome(string) → stored in memory, not executed
#   5. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='racecar'
#      → 5.1 return True
#   6. def is_palindrome(string) → stored in memory, not executed
#   7. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='madam'
#      → 7.1 return True
#   8. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='hello'
#      → 8.1 return True
#   9. def is_palindrome(string) → stored in memory, not executed
#   10. def is_palindrome(string) → stored in memory, not executed
#   11. string = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(is_palindrome(...))
#      → calls is_palindrome(string) with string='world'
#      → 14.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     name = ... → stored in memory
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 122:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_member(lst, x) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in or
# Built-in functions: any, input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_member(lst, x) runs first → then print() outputs the result
#   is_member(x, a) → each argument is evaluated first, then the function body runs
#   is_member(3, ?) runs first → then print() outputs the result
#   is_member(10, ?) runs first → then print() outputs the result
#   is_member('a', ?) runs first → then print() outputs the result
#   is_member('z', ?) runs first → then print() outputs the result
#   is_member(lst, x) → each argument is evaluated first, then the function body runs
#   is_member(?, 3) runs first → then print() outputs the result
#   x in lst → evaluated as True or False (determines which branch runs)
#   is_member(3, ?) → each argument is evaluated first, then the function body runs
#   is_member(10, ?) → each argument is evaluated first, then the function body runs
#   is_member('a', ?) → each argument is evaluated first, then the function body runs
#   is_member('z', ?) → each argument is evaluated first, then the function body runs
#   is_member(?, 3) → each argument is evaluated first, then the function body runs
#   item == x → evaluated as True or False (determines which branch runs)
#   ? == x → evaluated as True or False (determines which branch runs)
#   is_member(x, ?) → each argument is evaluated first, then the function body runs
#   element == x → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_member(lst, x) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. x = ... → assigns value to variable
#   4. print(is_member(...))
#      → calls is_member(lst, x) with lst=lst, x=x
#      → 4.1 return any((element == x for element in lst))
#   5. def is_member(x, a) → stored in memory, not executed
#   6. x = ... → assigns value to variable
#   7. a = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. def is_member(x, a) → stored in memory, not executed
#   11. print(is_member(...))
#      → calls is_member(lst, x) with lst=3, x=[1, 2, 3, 4]
#      → 11.1 return any((element == x for element in lst))
#   12. print(is_member(...))
#      → calls is_member(lst, x) with lst=10, x=[1, 2, 3]
#      → 12.1 return any((element == x for element in lst))
#   13. def is_member(x, a) → stored in memory, not executed
#   14. print(is_member(...))
#      → calls is_member(lst, x) with lst='a', x=['b', 'c', 'a']
#      → 14.1 return any((element == x for element in lst))
#   15. print(is_member(...))
#      → calls is_member(lst, x) with lst='z', x=['a', 'b', 'c']
#      → 15.1 return any((element == x for element in lst))
#   16. def is_member(lst, x) → stored in memory, not executed
#   17. def is_member(lst, x) → stored in memory, not executed
#   18. lst = ... → assigns value to variable
#   19. x = ... → assigns value to variable
#   20. result = ... → assigns value to variable
#   21. print(result)
#   22. print(is_member(...))
#      → calls is_member(lst, x) with lst=[1, 3, 5, 7], x=3
#      → 22.1 return any((element == x for element in lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_member) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     x = ... → stored in memory
#     print(is_member(...)) → is_member() runs first → value passed to print()
#     x = ... → stored in memory
#     a = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_member(...)) → is_member() runs first → value passed to print()
#     print(is_member(...)) → is_member() runs first → value passed to print()
#     print(is_member(...)) → is_member() runs first → value passed to print()
#     print(is_member(...)) → is_member() runs first → value passed to print()
#     lst = ... → stored in memory
#     x = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_member(...)) → is_member() runs first → value passed to print()
#
#   Loop: takes one item from a each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 123:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def overlapping(list1, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: & == in
# Built-in functions: any, bool, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   overlapping(list1, list2) runs first → then print() outputs the result
#   overlapping(?, ?) runs first → then print() outputs the result
#   overlapping(list1, list2) → each argument is evaluated first, then the function body runs
#   overlapping(?, ?) → each argument is evaluated first, then the function body runs
#   element == item → evaluated as True or False (determines which branch runs)
#   is_member(item1, list2) → each argument is evaluated first, then the function body runs
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#   item1 == item2 → evaluated as True or False (determines which branch runs)
#   x in list2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def overlapping(list1, list2) → stored in memory, not executed
#   2. list1 = ... → assigns value to variable
#   3. list2 = ... → assigns value to variable
#   4. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=list1, list2=list2
#      → 4.1 return True
#   5. list1 = ... → assigns value to variable
#   6. list2 = ... → assigns value to variable
#   7. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=list1, list2=list2
#      → 7.1 return True
#   8. def is_member(element, lst) → stored in memory, not executed
#   9. def overlapping(list1, list2) → stored in memory, not executed
#   10. list1 = ... → assigns value to variable
#   11. list2 = ... → assigns value to variable
#   12. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=list1, list2=list2
#      → 12.1 return True
#   13. list1 = ... → assigns value to variable
#   14. list2 = ... → assigns value to variable
#   15. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=list1, list2=list2
#      → 15.1 return True
#   16. def overlapping(list1, list2) → stored in memory, not executed
#   17. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=[1, 2, 3], list2=[3, 4, 5]
#      → 17.1 return True
#   18. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=[1, 2], list2=[3, 4]
#      → 18.1 return True
#   19. def overlapping(list1, list2) → stored in memory, not executed
#   20. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=['a', 'b', 'c'], list2=['c', 'd']
#      → 20.1 return True
#   21. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=['x', 'y'], list2=['a', 'b']
#      → 21.1 return True
#   22. def overlapping(list1, list2) → stored in memory, not executed
#   23. def overlapping(list1, list2) → stored in memory, not executed
#   24. list1 = ... → assigns value to variable
#   25. list2 = ... → assigns value to variable
#   26. result = ... → assigns value to variable
#   27. print(result)
#   28. print(overlapping(...))
#      → calls overlapping(list1, list2) with list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#      → 28.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_member, overlapping) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#
#   Loop: takes one item from list1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 124:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def overlapping(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: & > == in
# Built-in functions: any, len, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   overlapping(lst1, lst2) runs first → then print() outputs the result
#   overlapping(?, ?) runs first → then print() outputs the result
#   overlapping(lst1, lst2) → each argument is evaluated first, then the function body runs
#   len(...) > 0 → evaluated as True or False (determines which branch runs)
#   overlapping(?, ?) → each argument is evaluated first, then the function body runs
#   element in lst2 → evaluated as True or False (determines which branch runs)
#   i == j → evaluated as True or False (determines which branch runs)
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def overlapping(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return True
#   5. def overlapping(lst1, lst2) → stored in memory, not executed
#   6. lst1 = ... → assigns value to variable
#   7. lst2 = ... → assigns value to variable
#   8. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 8.1 return True
#   9. def overlapping(lst1, lst2) → stored in memory, not executed
#   10. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=[2, 4, 6], lst2=[6, 8, 10]
#      → 10.1 return True
#   11. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=[1, 3, 5], lst2=[2, 4, 6]
#      → 11.1 return True
#   12. def overlapping(lst1, lst2) → stored in memory, not executed
#   13. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=['a', 'b'], lst2=['b', 'c']
#      → 13.1 return True
#   14. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=['x', 'y'], lst2=['p', 'q']
#      → 14.1 return True
#   15. def overlapping(lst1, lst2) → stored in memory, not executed
#   16. def overlapping(lst1, lst2) → stored in memory, not executed
#   17. lst1 = ... → assigns value to variable
#   18. lst2 = ... → assigns value to variable
#   19. result = ... → assigns value to variable
#   20. print(result)
#   21. print(overlapping(...))
#      → calls overlapping(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 21.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (overlapping) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(overlapping(...)) → overlapping() runs first → value passed to print()
#
#   Loop: takes one item from lst1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 126:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def map_words_to_lengths(words) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   map_words_to_lengths(words) → each argument is evaluated first, then the function body runs
#   map_words_to_lengths(?) runs first → then print() outputs the result
#   map_words_to_lengths(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def map_words_to_lengths(words) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. lengths = ... → assigns value to variable
#   4. print(lengths)
#   5. def map_words_to_lengths(words) → stored in memory, not executed
#   6. print(map_words_to_lengths(...))
#      → calls map_words_to_lengths(words) with words=['a', 'ab', 'abc']
#      → 6.1 return lengths
#   7. def map_words_to_lengths(words) → stored in memory, not executed
#   8. print(map_words_to_lengths(...))
#      → calls map_words_to_lengths(words) with words=['hello', 'world']
#      → 8.1 return lengths
#   9. def map_words_to_lengths(words) → stored in memory, not executed
#   10. def map_words_to_lengths(words) → stored in memory, not executed
#   11. words = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(map_words_to_lengths(...))
#      → calls map_words_to_lengths(words) with words=[4, 1, 8, 6, 3]
#      → 14.1 return lengths
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (map_words_to_lengths) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     lengths = ... → stored in memory
#     print(lengths)
#     print(map_words_to_lengths(...)) → map_words_to_lengths() runs first → value passed to print()
#     print(map_words_to_lengths(...)) → map_words_to_lengths() runs first → value passed to print()
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(map_words_to_lengths(...)) → map_words_to_lengths() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 127:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_longest_word(elements) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, max, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_longest_word(lst) runs first → then print() outputs the result
#   find_longest_word(?) runs first → then print() outputs the result
#   find_longest_word(elements) → each argument is evaluated first, then the function body runs
#   find_longest_word(lst) → each argument is evaluated first, then the function body runs
#   find_longest_word(?) → each argument is evaluated first, then the function body runs
#   element > longest_element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_longest_word(elements) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_longest_word(...))
#      → calls find_longest_word(elements) with elements=lst
#      → 3.1 return sum((1 for _ in element))
#   4. def find_longest_word(words) → stored in memory, not executed
#   5. print(find_longest_word(...))
#      → calls find_longest_word(elements) with elements=['hi', 'hello', 'sup']
#      → 5.1 return sum((1 for _ in element))
#   6. def find_longest_word(words) → stored in memory, not executed
#   7. print(find_longest_word(...))
#      → calls find_longest_word(elements) with elements=['a', 'abc', 'ab']
#      → 7.1 return sum((1 for _ in element))
#   8. def find_longest_word(elements) → stored in memory, not executed
#   9. def find_longest_word(elements) → stored in memory, not executed
#   10. elements = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(find_longest_word(...))
#      → calls find_longest_word(elements) with elements=[5, 3, 8, 4]
#      → 13.1 return sum((1 for _ in element))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     elements = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#
#   Loop: takes one item from elements each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 128:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_long_words(lst, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: filter, len, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_long_words(lst, n) runs first → then print() outputs the result
#   filter_long_words(?, 4) runs first → then print() outputs the result
#   filter_long_words(?, 5) runs first → then print() outputs the result
#   filter_long_words(lst, n) → each argument is evaluated first, then the function body runs
#   filter_long_words(?, 15) runs first → then print() outputs the result
#   filter_long_words(?, 4) → each argument is evaluated first, then the function body runs
#   filter_long_words(?, 5) → each argument is evaluated first, then the function body runs
#   filter_long_words(?, 15) → each argument is evaluated first, then the function body runs
#   len(...) > n → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_long_words(lst, n) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. n = ... → assigns value to variable
#   4. print(filter_long_words(...))
#      → calls filter_long_words(lst, n) with lst=lst, n=n
#      → 4.1 return list(map(lambda word: word, lst))
#   5. def filter_long_words(lst, n) → stored in memory, not executed
#   6. print(filter_long_words(...))
#      → calls filter_long_words(lst, n) with lst=['cat', 'elephant', 'dog', 'hippopotamus'], n=4
#      → 6.1 return list(map(lambda word: word, lst))
#   7. def filter_long_words(lst, n) → stored in memory, not executed
#   8. print(filter_long_words(...))
#      → calls filter_long_words(lst, n) with lst=['tiny', 'medium', 'longerword'], n=5
#      → 8.1 return list(map(lambda word: word, lst))
#   9. def filter_long_words(lst, n) → stored in memory, not executed
#   10. def filter_long_words(lst, n) → stored in memory, not executed
#   11. lst = ... → assigns value to variable
#   12. n = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(filter_long_words(...))
#      → calls filter_long_words(lst, n) with lst=[1, 3, 5, 7], n=15
#      → 15.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_long_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     n = ... → stored in memory
#     print(filter_long_words(...)) → filter_long_words() runs first → value passed to print()
#     print(filter_long_words(...)) → filter_long_words() runs first → value passed to print()
#     print(filter_long_words(...)) → filter_long_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_long_words(...)) → filter_long_words() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 129:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_pangram(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in not in
# Built-in functions: all, input, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_pangram(sentence) → each argument is evaluated first, then the function body runs
#   is_pangram('The quick brown fox jumps over the lazy dog') runs first → then print() outputs the result
#   is_pangram('Hello World') runs first → then print() outputs the result
#   is_pangram('Pack my box with five dozen liquor jugs') runs first → then print() outputs the result
#   is_pangram('Python is fun') runs first → then print() outputs the result
#   is_pangram('The quick brown fox jumps over the lazy dog') → each argument is evaluated first, then the function body runs
#   is_pangram('Hello World') → each argument is evaluated first, then the function body runs
#   is_pangram('Pack my box with five dozen liquor jugs') → each argument is evaluated first, then the function body runs
#   is_pangram('Python is fun') → each argument is evaluated first, then the function body runs
#   letter not in sentence → evaluated as True or False (determines which branch runs)
#   letter in sentence → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_pangram(sentence) → stored in memory, not executed
#   2. sentence = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def is_pangram(sentence) → stored in memory, not executed
#   6. print(is_pangram(...))
#      → calls is_pangram(sentence) with sentence='The quick brown fox jumps over the lazy dog'
#      → 6.1 return False
#   7. print(is_pangram(...))
#      → calls is_pangram(sentence) with sentence='Hello World'
#      → 7.1 return False
#   8. def is_pangram(sentence) → stored in memory, not executed
#   9. print(is_pangram(...))
#      → calls is_pangram(sentence) with sentence='Pack my box with five dozen liquor jugs'
#      → 9.1 return False
#   10. def is_pangram(sentence) → stored in memory, not executed
#   11. def is_pangram(sentence) → stored in memory, not executed
#   12. sentence = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(is_pangram(...))
#      → calls is_pangram(sentence) with sentence='Python is fun'
#      → 15.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_pangram) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_pangram(...)) → is_pangram() runs first → value passed to print()
#     print(is_pangram(...)) → is_pangram() runs first → value passed to print()
#     print(is_pangram(...)) → is_pangram() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_pangram(...)) → is_pangram() runs first → value passed to print()
#
#   Loop: takes one item from alphabet each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 130:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def correct(text) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   correct(text) → each argument is evaluated first, then the function body runs
#   correct('This is very  funny and cool.Indeed!') runs first → then print() outputs the result
#   correct('Hello world.Yes') runs first → then print() outputs the result
#   correct('world') runs first → then print() outputs the result
#   correct('This is very  funny and cool.Indeed!') → each argument is evaluated first, then the function body runs
#   correct('Hello world.Yes') → each argument is evaluated first, then the function body runs
#   correct('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. def correct(text) → stored in memory, not executed
#   3. text = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def correct(text) → stored in memory, not executed
#   7. print(correct(...))
#      → calls correct(text) with text='This is very  funny and cool.Indeed!'
#      → 7.1 return text
#   8. def correct(text) → stored in memory, not executed
#   9. print(correct(...))
#      → calls correct(text) with text='Hello world.Yes'
#      → 9.1 return text
#   10. def correct(text) → stored in memory, not executed
#   11. def correct(text) → stored in memory, not executed
#   12. text = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(correct(...))
#      → calls correct(text) with text='world'
#      → 15.1 return text
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (correct) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(correct(...)) → correct() runs first → value passed to print()
#     print(correct(...)) → correct() runs first → value passed to print()
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(correct(...)) → correct() runs first → value passed to print()

"""

"""
Problem 131:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_func(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: max, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_in_list(numbers) runs first → then print() outputs the result
#   max_in_list(?) runs first → then print() outputs the result
#   max_func(a, b) → each argument is evaluated first, then the function body runs
#   max_func(20, 30) runs first → then print() outputs the result
#   a > b → evaluated as True or False (determines which branch runs)
#   max_in_list(numbers) → each argument is evaluated first, then the function body runs
#   max_in_list(?) → each argument is evaluated first, then the function body runs
#   max_func(20, 30) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def max_func(a, b) → stored in memory, not executed
#   3. def max_in_list(numbers) → stored in memory, not executed
#   4. numbers = ... → assigns value to variable
#   5. print(max_in_list(...))
#      → calls max_in_list(numbers) with numbers=numbers
#      → 5.1 return max(numbers)
#   6. def max_in_list(numbers) → stored in memory, not executed
#   7. print(max_in_list(...))
#      → calls max_in_list(numbers) with numbers=[10, 3, 7, 15, 2]
#      → 7.1 return max(numbers)
#   8. def max_in_list(numbers) → stored in memory, not executed
#   9. print(max_in_list(...))
#      → calls max_in_list(numbers) with numbers=[4, 9, 2, 11]
#      → 9.1 return max(numbers)
#   10. def max_func(a, b) → stored in memory, not executed
#   11. def max_func(a, b) → stored in memory, not executed
#   12. a = ... → assigns value to variable
#   13. b = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(max_func(...))
#      → calls max_func(a, b) with a=20, b=30
#      → 16.1 return a
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_func, max_in_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(max_in_list(...)) → max_in_list() runs first → value passed to print()
#     print(max_in_list(...)) → max_in_list() runs first → value passed to print()
#     print(max_in_list(...)) → max_in_list() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_func(...)) → max_func() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 134:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_all_numbers() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input, int, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_all_numbers() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_all_numbers() → stored in memory, not executed
#   2. sum_of_all_numbers() → calls sum_of_all_numbers() with 
#      → 2.1 user_input = input('Enter a list of numbers separated by spaces: ')
#      → 2.2 split_numbers = user_input.split()
#      → 2.3 int_numbers = []
#      → 2.4 for num in split_numbers:
#      → 2.4     int_numbers.append(int(num))
#      → 2.5 total_sum = sum(int_numbers)
#      → 2.6 result = total_sum
#      → 2.7 return result
#   3. def sum_of_all_numbers() → stored in memory, not executed
#   4. sum_of_all_numbers() → calls sum_of_all_numbers() with 
#      → 4.1 user_input = input('Enter a list of numbers separated by spaces: ')
#      → 4.2 split_numbers = user_input.split()
#      → 4.3 int_numbers = []
#      → 4.4 for num in split_numbers:
#      → 4.4     int_numbers.append(int(num))
#      → 4.5 total_sum = sum(int_numbers)
#      → 4.6 result = total_sum
#      → 4.7 return result
#   5. def sum_of_all_numbers() → stored in memory, not executed
#   6. sum_of_all_numbers() → calls sum_of_all_numbers() with 
#      → 6.1 user_input = input('Enter a list of numbers separated by spaces: ')
#      → 6.2 split_numbers = user_input.split()
#      → 6.3 int_numbers = []
#      → 6.4 for num in split_numbers:
#      → 6.4     int_numbers.append(int(num))
#      → 6.5 total_sum = sum(int_numbers)
#      → 6.6 result = total_sum
#      → 6.7 return result
#   7. def sum_of_all_numbers() → stored in memory, not executed
#   8. def sum_of_all_numbers() → stored in memory, not executed
#   9. user_input = ... → assigns value to variable
#   10. split_numbers = ... → assigns value to variable
#   11. int_numbers = ... → assigns value to variable
#   12. for num in split_numbers → loops over each item
#      → 12.1 int_numbers.append(int(...))
#   13. total_sum = ... → assigns value to variable
#   14. print(total_sum)
#   15. user_input = ... → assigns value to variable
#   16. split_numbers = ... → assigns value to variable
#   17. int_numbers = ... → assigns value to variable
#   18. for num in split_numbers → loops over each item
#      → 18.1 int_numbers.append(int(...))
#   19. total_sum = ... → assigns value to variable
#   20. print(total_sum)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_all_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sum_of_all_numbers() → jumps into sum_of_all_numbers(), runs body, returns
#     sum_of_all_numbers() → jumps into sum_of_all_numbers(), runs body, returns
#     sum_of_all_numbers() → jumps into sum_of_all_numbers(), runs body, returns
#     user_input = ... → stored in memory
#     split_numbers = ... → stored in memory
#     int_numbers = ... → stored in memory
#     for num in split_numbers → runs body once per item
#     total_sum = ... → stored in memory
#     print(total_sum)
#     user_input = ... → stored in memory
#     split_numbers = ... → stored in memory
#     int_numbers = ... → stored in memory
#     for num in split_numbers → runs body once per item
#     total_sum = ... → stored in memory
#     print(total_sum)
#
#   Loop: takes one item from split_numbers each round, runs body, repeats until done

"""

"""
Problem 135:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def split_number(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   split_number(arg) runs first → then print() outputs the result
#   split_number('123') runs first → then print() outputs the result
#   split_number('456') runs first → then print() outputs the result
#   split_number(numbers) → each argument is evaluated first, then the function body runs
#   split_number(?) runs first → then print() outputs the result
#   split_number(arg) → each argument is evaluated first, then the function body runs
#   split_number('123') → each argument is evaluated first, then the function body runs
#   split_number('456') → each argument is evaluated first, then the function body runs
#   split_number(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def split_number(numbers) → stored in memory, not executed
#   2. arg = ... → assigns value to variable
#   3. print(split_number(...))
#      → calls split_number(numbers) with numbers=arg
#      → 3.1 return list(map(lambda number: number, numbers))
#   4. def split_number(numbers) → stored in memory, not executed
#   5. print(split_number(...))
#      → calls split_number(numbers) with numbers='123'
#      → 5.1 return list(map(lambda number: number, numbers))
#   6. def split_number(numbers) → stored in memory, not executed
#   7. print(split_number(...))
#      → calls split_number(numbers) with numbers='456'
#      → 7.1 return list(map(lambda number: number, numbers))
#   8. def split_number(numbers) → stored in memory, not executed
#   9. def split_number(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(split_number(...))
#      → calls split_number(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return list(map(lambda number: number, numbers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (split_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     arg = ... → stored in memory
#     print(split_number(...)) → split_number() runs first → value passed to print()
#     print(split_number(...)) → split_number() runs first → value passed to print()
#     print(split_number(...)) → split_number() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(split_number(...)) → split_number() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 136:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def alphabetically_ordered(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   alphabetically_ordered(sentence) runs first → then print() outputs the result
#   alphabetically_ordered('Banana apple Cherry') runs first → then print() outputs the result
#   alphabetically_ordered('zebra cat dog') runs first → then print() outputs the result
#   alphabetically_ordered(sentence) → each argument is evaluated first, then the function body runs
#   alphabetically_ordered('Python is fun') runs first → then print() outputs the result
#   alphabetically_ordered('Banana apple Cherry') → each argument is evaluated first, then the function body runs
#   alphabetically_ordered('zebra cat dog') → each argument is evaluated first, then the function body runs
#   alphabetically_ordered('Python is fun') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def alphabetically_ordered(sentence) → stored in memory, not executed
#   2. sentence = ... → assigns value to variable
#   3. print(alphabetically_ordered(...))
#      → calls alphabetically_ordered(sentence) with sentence=sentence
#      → 3.1 return ' '.join(sorted(sentence.split()))
#   4. def alphabetically_ordered(sentence) → stored in memory, not executed
#   5. print(alphabetically_ordered(...))
#      → calls alphabetically_ordered(sentence) with sentence='Banana apple Cherry'
#      → 5.1 return ' '.join(sorted(sentence.split()))
#   6. def alphabetically_ordered(sentence) → stored in memory, not executed
#   7. print(alphabetically_ordered(...))
#      → calls alphabetically_ordered(sentence) with sentence='zebra cat dog'
#      → 7.1 return ' '.join(sorted(sentence.split()))
#   8. def alphabetically_ordered(sentence) → stored in memory, not executed
#   9. def alphabetically_ordered(sentence) → stored in memory, not executed
#   10. sentence = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(alphabetically_ordered(...))
#      → calls alphabetically_ordered(sentence) with sentence='Python is fun'
#      → 13.1 return ' '.join(sorted(sentence.split()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (alphabetically_ordered) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentence = ... → stored in memory
#     print(alphabetically_ordered(...)) → alphabetically_ordered() runs first → value passed to print()
#     print(alphabetically_ordered(...)) → alphabetically_ordered() runs first → value passed to print()
#     print(alphabetically_ordered(...)) → alphabetically_ordered() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(alphabetically_ordered(...)) → alphabetically_ordered() runs first → value passed to print()

"""

"""
Problem 137:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def smallest_largest(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   smallest_largest(numbers) runs first → then print() outputs the result
#   smallest_largest(?) runs first → then print() outputs the result
#   smallest_largest(numbers) → each argument is evaluated first, then the function body runs
#   smallest_largest(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def smallest_largest(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(smallest_largest(...))
#      → calls smallest_largest(numbers) with numbers=numbers
#      → 3.1 return sorted(numbers, reverse=False)
#   4. def smallest_largest(numbers) → stored in memory, not executed
#   5. print(smallest_largest(...))
#      → calls smallest_largest(numbers) with numbers=[5, 1, 9, 3]
#      → 5.1 return sorted(numbers, reverse=False)
#   6. def smallest_largest(numbers) → stored in memory, not executed
#   7. print(smallest_largest(...))
#      → calls smallest_largest(numbers) with numbers=[10, 2, 8]
#      → 7.1 return sorted(numbers, reverse=False)
#   8. def smallest_largest(numbers) → stored in memory, not executed
#   9. def smallest_largest(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(smallest_largest(...))
#      → calls smallest_largest(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return sorted(numbers, reverse=False)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (smallest_largest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(smallest_largest(...)) → smallest_largest() runs first → value passed to print()
#     print(smallest_largest(...)) → smallest_largest() runs first → value passed to print()
#     print(smallest_largest(...)) → smallest_largest() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(smallest_largest(...)) → smallest_largest() runs first → value passed to print()

"""

"""
Problem 138:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_smallest(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_smallest(numbers) runs first → then print() outputs the result
#   largest_smallest(?) runs first → then print() outputs the result
#   largest_smallest(numbers) → each argument is evaluated first, then the function body runs
#   largest_smallest(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_smallest(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(largest_smallest(...))
#      → calls largest_smallest(numbers) with numbers=numbers
#      → 3.1 return sorted(numbers)[::-1]
#   4. def largest_smallest(numbers) → stored in memory, not executed
#   5. print(largest_smallest(...))
#      → calls largest_smallest(numbers) with numbers=[5, 1, 9, 3]
#      → 5.1 return sorted(numbers)[::-1]
#   6. def largest_smallest(numbers) → stored in memory, not executed
#   7. print(largest_smallest(...))
#      → calls largest_smallest(numbers) with numbers=[10, 2, 8]
#      → 7.1 return sorted(numbers)[::-1]
#   8. def largest_smallest(numbers) → stored in memory, not executed
#   9. def largest_smallest(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(largest_smallest(...))
#      → calls largest_smallest(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return sorted(numbers)[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_smallest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()

"""

"""
Problem 139:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def seconded_largest_number(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   seconded_largest_number(numbers) runs first → then print() outputs the result
#   seconded_largest_number(?) runs first → then print() outputs the result
#   seconded_largest_number(numbers) → each argument is evaluated first, then the function body runs
#   seconded_largest_number(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def seconded_largest_number(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(seconded_largest_number(...))
#      → calls seconded_largest_number(numbers) with numbers=numbers
#      → 3.1 return sorted(numbers)[-2]
#   4. def seconded_largest_number(numbers) → stored in memory, not executed
#   5. print(seconded_largest_number(...))
#      → calls seconded_largest_number(numbers) with numbers=[10, 5, 8, 3, 12]
#      → 5.1 return sorted(numbers)[-2]
#   6. def seconded_largest_number(numbers) → stored in memory, not executed
#   7. print(seconded_largest_number(...))
#      → calls seconded_largest_number(numbers) with numbers=[5, 5, 3, 1, 4]
#      → 7.1 return sorted(numbers)[-2]
#   8. def seconded_largest_number(numbers) → stored in memory, not executed
#   9. def seconded_largest_number(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(seconded_largest_number(...))
#      → calls seconded_largest_number(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return sorted(numbers)[-2]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (seconded_largest_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(seconded_largest_number(...)) → seconded_largest_number() runs first → value passed to print()
#     print(seconded_largest_number(...)) → seconded_largest_number() runs first → value passed to print()
#     print(seconded_largest_number(...)) → seconded_largest_number() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(seconded_largest_number(...)) → seconded_largest_number() runs first → value passed to print()

"""

"""
Problem 140:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fourth_largest_number(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fourth_largest_number(numbers) runs first → then print() outputs the result
#   fourth_largest_number(?) runs first → then print() outputs the result
#   fourth_largest_number(numbers) → each argument is evaluated first, then the function body runs
#   fourth_largest_number(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fourth_largest_number(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(fourth_largest_number(...))
#      → calls fourth_largest_number(numbers) with numbers=numbers
#      → 3.1 return sorted(numbers)[-4]
#   4. def fourth_largest_number(numbers) → stored in memory, not executed
#   5. print(fourth_largest_number(...))
#      → calls fourth_largest_number(numbers) with numbers=[10, 5, 8, 3, 12, 7]
#      → 5.1 return sorted(numbers)[-4]
#   6. def fourth_largest_number(numbers) → stored in memory, not executed
#   7. print(fourth_largest_number(...))
#      → calls fourth_largest_number(numbers) with numbers=[9, 2, 7, 5, 1, 3]
#      → 7.1 return sorted(numbers)[-4]
#   8. def fourth_largest_number(numbers) → stored in memory, not executed
#   9. def fourth_largest_number(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(fourth_largest_number(...))
#      → calls fourth_largest_number(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return sorted(numbers)[-4]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fourth_largest_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(fourth_largest_number(...)) → fourth_largest_number() runs first → value passed to print()
#     print(fourth_largest_number(...)) → fourth_largest_number() runs first → value passed to print()
#     print(fourth_largest_number(...)) → fourth_largest_number() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fourth_largest_number(...)) → fourth_largest_number() runs first → value passed to print()

"""

"""
Problem 141:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def smallest_and_third(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   smallest_and_third(numbers) runs first → then print() outputs the result
#   smallest_and_third(?) runs first → then print() outputs the result
#   smallest_and_third(numbers) → each argument is evaluated first, then the function body runs
#   smallest_and_third(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def smallest_and_third(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(smallest_and_third(...))
#      → calls smallest_and_third(numbers) with numbers=numbers
#      → 3.1 return (smallest, third_largest)
#   4. def smallest_and_third(numbers) → stored in memory, not executed
#   5. print(smallest_and_third(...))
#      → calls smallest_and_third(numbers) with numbers=[5, 9, 2, 7, 11, 3]
#      → 5.1 return (smallest, third_largest)
#   6. def smallest_and_third(numbers) → stored in memory, not executed
#   7. print(smallest_and_third(...))
#      → calls smallest_and_third(numbers) with numbers=[10, 3, 8, 1, 6, 9]
#      → 7.1 return (smallest, third_largest)
#   8. def smallest_and_third(numbers) → stored in memory, not executed
#   9. def smallest_and_third(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(smallest_and_third(...))
#      → calls smallest_and_third(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return (smallest, third_largest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (smallest_and_third) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(smallest_and_third(...)) → smallest_and_third() runs first → value passed to print()
#     print(smallest_and_third(...)) → smallest_and_third() runs first → value passed to print()
#     print(smallest_and_third(...)) → smallest_and_third() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(smallest_and_third(...)) → smallest_and_third() runs first → value passed to print()

"""

"""
Problem 142:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_smallest(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_smallest(numbers) runs first → then print() outputs the result
#   smallest_biggest(lst) runs first → then print() outputs the result
#   smallest_biggest(?) runs first → then print() outputs the result
#   largest_smallest(numbers) → each argument is evaluated first, then the function body runs
#   largest_smallest(?) runs first → then print() outputs the result
#   smallest_biggest(lst) → each argument is evaluated first, then the function body runs
#   smallest_biggest(?) → each argument is evaluated first, then the function body runs
#   largest_smallest(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_smallest(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(largest_smallest(...))
#      → calls largest_smallest(numbers) with numbers=numbers
#      → 3.1 return sorted(numbers)[:]
#   4. def smallest_biggest(numbers) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(smallest_biggest(...))
#      → calls smallest_biggest(numbers) with numbers=lst
#      → 6.1 return sorted(numbers, key=int)
#   7. def smallest_biggest(numbers) → stored in memory, not executed
#   8. print(smallest_biggest(...))
#      → calls smallest_biggest(numbers) with numbers=[3, 1, 4, 2]
#      → 8.1 return sorted(numbers, key=int)
#   9. def smallest_biggest(numbers) → stored in memory, not executed
#   10. print(smallest_biggest(...))
#      → calls smallest_biggest(numbers) with numbers=[9, 5, 7]
#      → 10.1 return sorted(numbers, key=int)
#   11. def largest_smallest(numbers) → stored in memory, not executed
#   12. def largest_smallest(numbers) → stored in memory, not executed
#   13. numbers = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(largest_smallest(...))
#      → calls largest_smallest(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 16.1 return sorted(numbers)[:]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_smallest, smallest_biggest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(smallest_biggest(...)) → smallest_biggest() runs first → value passed to print()
#     print(smallest_biggest(...)) → smallest_biggest() runs first → value passed to print()
#     print(smallest_biggest(...)) → smallest_biggest() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_smallest(...)) → largest_smallest() runs first → value passed to print()

"""

"""
Problem 143:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def int_str() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   int_str() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def int_str() → stored in memory, not executed
#   2. int_str() → calls int_str() with 
#      → 2.1 user_lst = []
#      → 2.2 user = input('type: ')
#      → 2.3 split_str = user.split()
#      → 2.4 for word in split_str:
#      → 2.4     user_lst.append(word)
#      → 2.5 result = user_lst
#      → 2.6 return result
#   3. def int_str() → stored in memory, not executed
#   4. int_str() → calls int_str() with 
#      → 4.1 user_lst = []
#      → 4.2 user = input('type: ')
#      → 4.3 split_str = user.split()
#      → 4.4 for word in split_str:
#      → 4.4     user_lst.append(word)
#      → 4.5 result = user_lst
#      → 4.6 return result
#   5. def int_str() → stored in memory, not executed
#   6. int_str() → calls int_str() with 
#      → 6.1 user_lst = []
#      → 6.2 user = input('type: ')
#      → 6.3 split_str = user.split()
#      → 6.4 for word in split_str:
#      → 6.4     user_lst.append(word)
#      → 6.5 result = user_lst
#      → 6.6 return result
#   7. def int_str() → stored in memory, not executed
#   8. def int_str() → stored in memory, not executed
#   9. user_lst = ... → assigns value to variable
#   10. user = ... → assigns value to variable
#   11. split_str = ... → assigns value to variable
#   12. for word in split_str → loops over each item
#      → 12.1 user_lst.append(word)
#   13. print(user_lst)
#   14. user_lst = ... → assigns value to variable
#   15. user = ... → assigns value to variable
#   16. split_str = ... → assigns value to variable
#   17. for word in split_str → loops over each item
#      → 17.1 user_lst.append(word)
#   18. print(user_lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (int_str) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     int_str() → jumps into int_str(), runs body, returns
#     int_str() → jumps into int_str(), runs body, returns
#     int_str() → jumps into int_str(), runs body, returns
#     user_lst = ... → stored in memory
#     user = ... → stored in memory
#     split_str = ... → stored in memory
#     for word in split_str → runs body once per item
#     print(user_lst)
#     user_lst = ... → stored in memory
#     user = ... → stored in memory
#     split_str = ... → stored in memory
#     for word in split_str → runs body once per item
#     print(user_lst)
#
#   Loop: takes one item from split_str each round, runs body, repeats until done

"""

"""
Problem 144:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def int_to_str_list(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, map, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   int_to_str_list(number) runs first → then print() outputs the result
#   int_to_str_list(456) runs first → then print() outputs the result
#   int_to_str_list(789) runs first → then print() outputs the result
#   int_to_str_list(number) → each argument is evaluated first, then the function body runs
#   int_to_str_list(25) runs first → then print() outputs the result
#   int_to_str_list(456) → each argument is evaluated first, then the function body runs
#   int_to_str_list(789) → each argument is evaluated first, then the function body runs
#   int_to_str_list(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def int_to_str_list(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(int_to_str_list(...))
#      → calls int_to_str_list(number) with number=number
#      → 3.1 return list(map(lambda digit: digit, number_str))
#   4. def int_to_str_list(number) → stored in memory, not executed
#   5. print(int_to_str_list(...))
#      → calls int_to_str_list(number) with number=456
#      → 5.1 return list(map(lambda digit: digit, number_str))
#   6. def int_to_str_list(number) → stored in memory, not executed
#   7. print(int_to_str_list(...))
#      → calls int_to_str_list(number) with number=789
#      → 7.1 return list(map(lambda digit: digit, number_str))
#   8. def int_to_str_list(number) → stored in memory, not executed
#   9. def int_to_str_list(number) → stored in memory, not executed
#   10. number = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(int_to_str_list(...))
#      → calls int_to_str_list(number) with number=25
#      → 13.1 return list(map(lambda digit: digit, number_str))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (int_to_str_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(int_to_str_list(...)) → int_to_str_list() runs first → value passed to print()
#     print(int_to_str_list(...)) → int_to_str_list() runs first → value passed to print()
#     print(int_to_str_list(...)) → int_to_str_list() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(int_to_str_list(...)) → int_to_str_list() runs first → value passed to print()
#
#   Loop: takes one item from number_str each round, runs body, repeats until done

"""

"""
Problem 145:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common_numbers(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in and not in
# Built-in functions: filter, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common_numbers(lst1, lst2) runs first → then print() outputs the result
#   common_numbers(?, ?) runs first → then print() outputs the result
#   common_numbers(lst1, lst2) → each argument is evaluated first, then the function body runs
#   common_numbers(?, ?) → each argument is evaluated first, then the function body runs
#   i == j → evaluated as True or False (determines which branch runs)
#   i not in result → evaluated as True or False (determines which branch runs)
#   x in lst2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common_numbers(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(common_numbers(...))
#      → calls common_numbers(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return list(map(lambda i: i, lst1))
#   5. def common_numbers(lst1, lst2) → stored in memory, not executed
#   6. print(common_numbers(...))
#      → calls common_numbers(lst1, lst2) with lst1=[1, 2, 3], lst2=[3, 4, 5]
#      → 6.1 return list(map(lambda i: i, lst1))
#   7. def common_numbers(lst1, lst2) → stored in memory, not executed
#   8. print(common_numbers(...))
#      → calls common_numbers(lst1, lst2) with lst1=['a', 'b', 'c'], lst2=['b', 'c', 'd']
#      → 8.1 return list(map(lambda i: i, lst1))
#   9. def common_numbers(lst1, lst2) → stored in memory, not executed
#   10. def common_numbers(lst1, lst2) → stored in memory, not executed
#   11. lst1 = ... → assigns value to variable
#   12. lst2 = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(common_numbers(...))
#      → calls common_numbers(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 15.1 return list(map(lambda i: i, lst1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#
#   Loop: takes one item from lst1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 146:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def big_small(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: int, sorted, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   big_small(lst) runs first → then print() outputs the result
#   big_small(?) runs first → then print() outputs the result
#   big_small(numbers) → each argument is evaluated first, then the function body runs
#   big_small(lst) → each argument is evaluated first, then the function body runs
#   big_small(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def big_small(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(big_small(...))
#      → calls big_small(numbers) with numbers=lst
#      → 3.1 return shorted_str
#   4. def big_small(numbers) → stored in memory, not executed
#   5. print(big_small(...))
#      → calls big_small(numbers) with numbers=['4', '67', '6']
#      → 5.1 return shorted_str
#   6. def big_small(numbers) → stored in memory, not executed
#   7. print(big_small(...))
#      → calls big_small(numbers) with numbers=['10', '2', '35']
#      → 7.1 return shorted_str
#   8. def big_small(numbers) → stored in memory, not executed
#   9. def big_small(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(big_small(...))
#      → calls big_small(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return shorted_str
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (big_small) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(big_small(...)) → big_small() runs first → value passed to print()
#     print(big_small(...)) → big_small() runs first → value passed to print()
#     print(big_small(...)) → big_small() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(big_small(...)) → big_small() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 147:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def type_smallest_biggest() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input, int, list, map, sorted, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   type_smallest_biggest() → each argument is evaluated first, then the function body runs
#   small_big() runs first → then print() outputs the result
#   small_big() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def type_smallest_biggest() → stored in memory, not executed
#   2. type_smallest_biggest() → calls type_smallest_biggest() with 
#      → 2.1 user = input('type number list: ').split()
#      → 2.2 sort_number = sorted(user)
#      → 2.3 result = sort_number
#      → 2.4 return result
#   3. def small_big() → stored in memory, not executed
#   4. print(small_big(...))
#      → calls small_big() with 
#      → 4.1 nums = list(map(int, input('Enter numbers: ').split()))
#      → 4.2 nums.sort()
#      → 4.3 return ' '.join(map(str, nums))
#   5. def small_big() → stored in memory, not executed
#   6. print(small_big(...))
#      → calls small_big() with 
#      → 6.1 nums = list(map(int, input('Enter numbers: ').split()))
#      → 6.2 nums.sort()
#      → 6.3 return ' '.join(map(str, nums))
#   7. def type_smallest_biggest() → stored in memory, not executed
#   8. type_smallest_biggest() → calls type_smallest_biggest() with 
#      → 8.1 user = input('type number list: ').split()
#      → 8.2 sort_number = sorted(user)
#      → 8.3 result = sort_number
#      → 8.4 return result
#   9. def type_smallest_biggest() → stored in memory, not executed
#   10. def type_smallest_biggest() → stored in memory, not executed
#   11. user = ... → assigns value to variable
#   12. sort_number = ... → assigns value to variable
#   13. print(sort_number)
#   14. user = ... → assigns value to variable
#   15. sort_number = ... → assigns value to variable
#   16. print(sort_number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (small_big, type_smallest_biggest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     type_smallest_biggest() → jumps into type_smallest_biggest(), runs body, returns
#     print(small_big(...)) → small_big() runs first → value passed to print()
#     print(small_big(...)) → small_big() runs first → value passed to print()
#     type_smallest_biggest() → jumps into type_smallest_biggest(), runs body, returns
#     user = ... → stored in memory
#     sort_number = ... → stored in memory
#     print(sort_number)
#     user = ... → stored in memory
#     sort_number = ... → stored in memory
#     print(sort_number)
#
#   Loop: takes one item from user_split each round, runs body, repeats until done

"""

"""
Problem 148:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def double_elements(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   double_elements(lst) runs first → then print() outputs the result
#   double_elements(?) runs first → then print() outputs the result
#   double_elements(numbers) → each argument is evaluated first, then the function body runs
#   numbers + numbers → Python looks up numbers, then numbers, then applies +
#   double_elements(lst) → each argument is evaluated first, then the function body runs
#   numbers * 2 → Python looks up numbers, then 2, then applies *
#   double_elements(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def double_elements(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(double_elements(...))
#      → calls double_elements(numbers) with numbers=lst
#      → 3.1 import operator
#      → 3.2 return operator.add(numbers, numbers)
#   4. def double_elements(numbers) → stored in memory, not executed
#   5. print(double_elements(...))
#      → calls double_elements(numbers) with numbers=[1, 2, 3]
#      → 5.1 import operator
#      → 5.2 return operator.add(numbers, numbers)
#   6. def double_elements(numbers) → stored in memory, not executed
#   7. print(double_elements(...))
#      → calls double_elements(numbers) with numbers=[5, 6, 7]
#      → 7.1 import operator
#      → 7.2 return operator.add(numbers, numbers)
#   8. def double_elements(numbers) → stored in memory, not executed
#   9. def double_elements(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(double_elements(...))
#      → calls double_elements(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 import operator
#      → 13.2 return operator.add(numbers, numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (double_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(double_elements(...)) → double_elements() runs first → value passed to print()

"""

"""
Problem 149:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: & == in and not in
# Built-in functions: input, list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common() → each argument is evaluated first, then the function body runs
#   common() runs first → then print() outputs the result
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#   i == j → evaluated as True or False (determines which branch runs)
#   i not in result → evaluated as True or False (determines which branch runs)
#   x in lst2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common() → stored in memory, not executed
#   2. common() → calls common() with 
#      → 2.1 return list(map(lambda i: i, lst1))
#   3. def common() → stored in memory, not executed
#   4. print(common(...))
#      → calls common() with 
#      → 4.1 return list(map(lambda i: i, lst1))
#   5. def common() → stored in memory, not executed
#   6. print(common(...))
#      → calls common() with 
#      → 6.1 return list(map(lambda i: i, lst1))
#   7. def common() → stored in memory, not executed
#   8. def common() → stored in memory, not executed
#   9. lst1 = ... → assigns value to variable
#   10. lst2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. for i in lst1 → loops over each item
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. lst1 = ... → assigns value to variable
#   16. lst2 = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. for i in lst1 → loops over each item
#   19. result = ... → assigns value to variable
#   20. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     common() → jumps into common(), runs body, returns
#     print(common(...)) → common() runs first → value passed to print()
#     print(common(...)) → common() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     for i in lst1 → runs body once per item
#     result = ... → stored in memory
#     print(result)
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     for i in lst1 → runs body once per item
#     result = ... → stored in memory
#     print(result)
#
#   Loop: takes one item from lst1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 150:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: &
# Built-in functions: input, list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common() → each argument is evaluated first, then the function body runs
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common() → stored in memory, not executed
#   2. common() → calls common() with 
#      → 2.1 lst1 = input('type lst: ').split()
#      → 2.2 lst2 = input('type lst: ').split()
#      → 2.3 set1 = set(lst1)
#      → 2.4 set2 = set(lst2)
#      → 2.5 common_element = set1.intersection(set2)
#      → 2.6 result = common_element
#      → 2.7 return result
#   3. def common() → stored in memory, not executed
#   4. common() → calls common() with 
#      → 4.1 lst1 = input('type lst: ').split()
#      → 4.2 lst2 = input('type lst: ').split()
#      → 4.3 set1 = set(lst1)
#      → 4.4 set2 = set(lst2)
#      → 4.5 common_element = set1.intersection(set2)
#      → 4.6 result = common_element
#      → 4.7 return result
#   5. def common() → stored in memory, not executed
#   6. common() → calls common() with 
#      → 6.1 lst1 = input('type lst: ').split()
#      → 6.2 lst2 = input('type lst: ').split()
#      → 6.3 set1 = set(lst1)
#      → 6.4 set2 = set(lst2)
#      → 6.5 common_element = set1.intersection(set2)
#      → 6.6 result = common_element
#      → 6.7 return result
#   7. def common() → stored in memory, not executed
#   8. def common() → stored in memory, not executed
#   9. lst1 = ... → assigns value to variable
#   10. lst2 = ... → assigns value to variable
#   11. set1 = ... → assigns value to variable
#   12. set2 = ... → assigns value to variable
#   13. common_element = ... → assigns value to variable
#   14. print(common_element)
#   15. lst1 = ... → assigns value to variable
#   16. lst2 = ... → assigns value to variable
#   17. set1 = ... → assigns value to variable
#   18. set2 = ... → assigns value to variable
#   19. common_element = ... → assigns value to variable
#   20. print(common_element)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     common() → jumps into common(), runs body, returns
#     common() → jumps into common(), runs body, returns
#     common() → jumps into common(), runs body, returns
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     set1 = ... → stored in memory
#     set2 = ... → stored in memory
#     common_element = ... → stored in memory
#     print(common_element)
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     set1 = ... → stored in memory
#     set2 = ... → stored in memory
#     common_element = ... → stored in memory
#     print(common_element)

"""

"""
Problem 151:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_longest_word(words) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > >=
# Built-in functions: len, max, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_longest_word(words) runs first → then print() outputs the result
#   find_longest_word(?) runs first → then print() outputs the result
#   find_longest_word(words) → each argument is evaluated first, then the function body runs
#   find_longest_word(?) → each argument is evaluated first, then the function body runs
#   len(...) > count → evaluated as True or False (determines which branch runs)
#   len(...) >= len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_longest_word(words) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. print(find_longest_word(...))
#      → calls find_longest_word(words) with words=words
#      → 3.1 return longest_word
#   4. def find_longest_word(words) → stored in memory, not executed
#   5. print(find_longest_word(...))
#      → calls find_longest_word(words) with words=['hello', 'world', 'python']
#      → 5.1 return longest_word
#   6. def find_longest_word(words) → stored in memory, not executed
#   7. print(find_longest_word(...))
#      → calls find_longest_word(words) with words=['a', 'ab', 'abc']
#      → 7.1 return longest_word
#   8. def find_longest_word(words) → stored in memory, not executed
#   9. print(find_longest_word(...))
#      → calls find_longest_word(words) with words=['short', 'longer', 'longest']
#      → 9.1 return longest_word
#   10. def find_longest_word(words) → stored in memory, not executed
#   11. def find_longest_word(words) → stored in memory, not executed
#   12. words = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(find_longest_word(...))
#      → calls find_longest_word(words) with words=[4, 1, 8, 6, 3]
#      → 15.1 return longest_word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 152:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * + /
# Built-in functions: Fraction, float, len, reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average(numbers) runs first → then print() outputs the result
#   average(?) runs first → then print() outputs the result
#   average(numbers) → each argument is evaluated first, then the function body runs
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   average(?) → each argument is evaluated first, then the function body runs
#   reduce(...) / len(...) → Python looks up reduce(...), then len(...), then applies /
#   total * ? → Python looks up total, then ?, then applies *
#   total / length → Python looks up total, then length, then applies /
#   1 / length → Python looks up 1, then length, then applies /
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(average(...))
#      → calls average(numbers) with numbers=numbers
#      → 3.1 from fractions import Fraction
#      → 3.2 return float(Fraction(total, length))
#   4. def average(numbers) → stored in memory, not executed
#   5. print(average(...))
#      → calls average(numbers) with numbers=[4, 667, 68, 766]
#      → 5.1 from fractions import Fraction
#      → 5.2 return float(Fraction(total, length))
#   6. def average(numbers) → stored in memory, not executed
#   7. print(average(...))
#      → calls average(numbers) with numbers=[10, 20, 30, 40]
#      → 7.1 from fractions import Fraction
#      → 7.2 return float(Fraction(total, length))
#   8. def average(numbers) → stored in memory, not executed
#   9. print(average(...))
#      → calls average(numbers) with numbers=[1, 2, 3, 4]
#      → 9.1 from fractions import Fraction
#      → 9.2 return float(Fraction(total, length))
#   10. def average(numbers) → stored in memory, not executed
#   11. def average(numbers) → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(average(...))
#      → calls average(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 15.1 from fractions import Fraction
#      → 15.2 return float(Fraction(total, length))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(average(...)) → average() runs first → value passed to print()
#     print(average(...)) → average() runs first → value passed to print()
#     print(average(...)) → average() runs first → value passed to print()
#     print(average(...)) → average() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average(...)) → average() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 153:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_smallest_element(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >=
# Built-in functions: len, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_smallest_element(lst) runs first → then print() outputs the result
#   second_smallest_element(?) runs first → then print() outputs the result
#   second_smallest_element(lst) → each argument is evaluated first, then the function body runs
#   len(...) >= 2 → evaluated as True or False (determines which branch runs)
#   second_smallest_element(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_smallest_element(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(second_smallest_element(...))
#      → calls second_smallest_element(lst) with lst=lst
#      → 3.1 return sorted_lst[1]
#   4. def second_smallest_element(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(second_smallest_element(...))
#      → calls second_smallest_element(lst) with lst=lst
#      → 6.1 return sorted_lst[1]
#   7. def second_smallest_element(lst) → stored in memory, not executed
#   8. print(second_smallest_element(...))
#      → calls second_smallest_element(lst) with lst=[5, 3, 8, 1, 9]
#      → 8.1 return sorted_lst[1]
#   9. def second_smallest_element(lst) → stored in memory, not executed
#   10. print(second_smallest_element(...))
#      → calls second_smallest_element(lst) with lst=[7, 7, 1, 1, 5]
#      → 10.1 return sorted_lst[1]
#   11. def second_smallest_element(lst) → stored in memory, not executed
#   12. def second_smallest_element(lst) → stored in memory, not executed
#   13. lst = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(second_smallest_element(...))
#      → calls second_smallest_element(lst) with lst=[1, 3, 5, 7]
#      → 16.1 return sorted_lst[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_smallest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(second_smallest_element(...)) → second_smallest_element() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 154:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_last_element() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: < >=
# Built-in functions: input, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_last_element() runs first → then print() outputs the result
#   find_second_last_element(?) runs first → then print() outputs the result
#   find_second_last_element(lst) → each argument is evaluated first, then the function body runs
#   find_last_element() → each argument is evaluated first, then the function body runs
#   len(...) < 2 → evaluated as True or False (determines which branch runs)
#   find_second_last_element(?) → each argument is evaluated first, then the function body runs
#   len(...) >= 2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_last_element() → stored in memory, not executed
#   2. print(find_last_element(...))
#      → calls find_last_element() with 
#      → 2.1 return None
#   3. def find_second_last_element(lst) → stored in memory, not executed
#   4. print(find_second_last_element(...))
#      → calls find_second_last_element(lst) with lst=[1, 2, 3, 4, 5]
#      → 4.1 return lst[-2:-1][0] if len(lst) >= 2 else None
#   5. def find_second_last_element(lst) → stored in memory, not executed
#   6. print(find_second_last_element(...))
#      → calls find_second_last_element(lst) with lst=['a', 'b', 'c']
#      → 6.1 return lst[-2:-1][0] if len(lst) >= 2 else None
#   7. def find_last_element() → stored in memory, not executed
#   8. def find_last_element() → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_second_last_element(...))
#      → calls find_second_last_element(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return lst[-2:-1][0] if len(lst) >= 2 else None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_second_last_element, find_last_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(find_last_element(...)) → find_last_element() runs first → value passed to print()
#     print(find_second_last_element(...)) → find_second_last_element() runs first → value passed to print()
#     print(find_second_last_element(...)) → find_second_last_element() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_second_last_element(...)) → find_second_last_element() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 155:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_number(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: % * + > >=
# Built-in functions: abs, int, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_number(lst) runs first → then print() outputs the result
#   reverse_number(number) runs first → then print() outputs the result
#   reverse_number(123) runs first → then print() outputs the result
#   reverse_number(?) runs first → then print() outputs the result
#   reverse_number(1000) runs first → then print() outputs the result
#   reverse_number(number) → each argument is evaluated first, then the function body runs
#   reverse_number(25) runs first → then print() outputs the result
#   reverse_number(lst) → each argument is evaluated first, then the function body runs
#   num > 0 → evaluated as True or False (determines which branch runs)
#   reverse_number(123) → each argument is evaluated first, then the function body runs
#   reverse_number(?) → each argument is evaluated first, then the function body runs
#   reverse_number(1000) → each argument is evaluated first, then the function body runs
#   reverse_number(25) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   number >= 0 → evaluated as True or False (determines which branch runs)
#   reversed_num * 10 → Python looks up reversed_num, then 10, then applies *
#   num % 10 → Python looks up num, then 10, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_number(number) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(reverse_number(...))
#      → calls reverse_number(number) with number=lst
#      → 3.1 return int(str(number)[::-1])
#   4. def reverse_number(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. print(reverse_number(...))
#      → calls reverse_number(number) with number=number
#      → 6.1 return int(str(number)[::-1])
#   7. def reverse_number(number) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. print(reverse_number(...))
#      → calls reverse_number(number) with number=lst
#      → 9.1 return int(str(number)[::-1])
#   10. def reverse_number(number) → stored in memory, not executed
#   11. print(reverse_number(...))
#      → calls reverse_number(number) with number=123
#      → 11.1 return int(str(number)[::-1])
#   12. print(reverse_number(...))
#      → calls reverse_number(number) with number=?
#      → 12.1 return int(str(number)[::-1])
#   13. print(reverse_number(...))
#      → calls reverse_number(number) with number=1000
#      → 13.1 return int(str(number)[::-1])
#   14. def reverse_number(number) → stored in memory, not executed
#   15. def reverse_number(number) → stored in memory, not executed
#   16. number = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(reverse_number(...))
#      → calls reverse_number(number) with number=25
#      → 19.1 return int(str(number)[::-1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     number = ... → stored in memory
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 156:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: in or
# Built-in functions: set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates(lst) runs first → then print() outputs the result
#   remove_duplicates('hello') runs first → then print() outputs the result
#   remove_duplicates('aabbcc') runs first → then print() outputs the result
#   remove_duplicates(string) → each argument is evaluated first, then the function body runs
#   remove_duplicates('world') runs first → then print() outputs the result
#   remove_duplicates(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates('hello') → each argument is evaluated first, then the function body runs
#   remove_duplicates('aabbcc') → each argument is evaluated first, then the function body runs
#   remove_duplicates('world') → each argument is evaluated first, then the function body runs
#   c in seen → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates(string) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string=lst
#      → 3.1 return ' '.join(set_word)
#   4. def remove_duplicates(string) → stored in memory, not executed
#   5. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string='hello'
#      → 5.1 return ' '.join(set_word)
#   6. def remove_duplicates(string) → stored in memory, not executed
#   7. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string='hello'
#      → 7.1 return ' '.join(set_word)
#   8. def remove_duplicates(string) → stored in memory, not executed
#   9. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string='aabbcc'
#      → 9.1 return ' '.join(set_word)
#   10. def remove_duplicates(string) → stored in memory, not executed
#   11. def remove_duplicates(string) → stored in memory, not executed
#   12. string = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(remove_duplicates(...))
#      → calls remove_duplicates(string) with string='world'
#      → 15.1 return ' '.join(set_word)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()

"""

"""
Problem 158:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_element(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: >
# Built-in functions: input, int, max, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_element(numbers) runs first → then print() outputs the result
#   main() → each argument is evaluated first, then the function body runs
#   largest_element(?) runs first → then print() outputs the result
#   largest_element(numbers) → each argument is evaluated first, then the function body runs
#   largest_element(?) → each argument is evaluated first, then the function body runs
#   a > b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_element(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=numbers
#      → 3.1 return largest_number
#   4. def main() → stored in memory, not executed
#   5. main() → calls main() with 
#      → 5.1 result = []
#      → 5.2 user = input('type numbers: ').split()
#      → 5.3 for num in user:
#      → 5.3     result.append(int(num))
#      → 5.3     largest_num = sorted(result)[-1]
#      → 5.4 return largest_num
#   6. def largest_element(numbers) → stored in memory, not executed
#   7. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[2, 56, 75, 4566, 3]
#      → 7.1 return largest_number
#   8. def largest_element(numbers) → stored in memory, not executed
#   9. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[10, 5, 20, 3]
#      → 9.1 return largest_number
#   10. def largest_element(numbers) → stored in memory, not executed
#   11. def largest_element(numbers) → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 15.1 return largest_number
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, largest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     main() → jumps into main(), runs body, returns
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done

"""

"""
Problem 159:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_element(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: max, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_element(numbers) runs first → then print() outputs the result
#   largest_element(?) runs first → then print() outputs the result
#   largest_element(numbers) → each argument is evaluated first, then the function body runs
#   largest_element(?) → each argument is evaluated first, then the function body runs
#   item > result → evaluated as True or False (determines which branch runs)
#   a > b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_element(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=numbers
#      → 3.1 result = numbers[0]
#      → 3.2 for item in numbers:
#      → 3.2     if item > result:
#      → 3.2         result = item
#      → 3.3 return result
#   4. def largest_element(numbers) → stored in memory, not executed
#   5. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[2, 56, 75, 4566, 3]
#      → 5.1 result = numbers[0]
#      → 5.2 for item in numbers:
#      → 5.2     if item > result:
#      → 5.2         result = item
#      → 5.3 return result
#   6. def largest_element(numbers) → stored in memory, not executed
#   7. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[10, 5, 20, 3]
#      → 7.1 result = numbers[0]
#      → 7.2 for item in numbers:
#      → 7.2     if item > result:
#      → 7.2         result = item
#      → 7.3 return result
#   8. def largest_element(numbers) → stored in memory, not executed
#   9. def largest_element(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(largest_element(...))
#      → calls largest_element(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 result = numbers[0]
#      → 13.2 for item in numbers:
#      → 13.2     if item > result:
#      → 13.2         result = item
#      → 13.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 160:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_length(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + ==
# Built-in functions: enumerate, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_length(arg) runs first → then print() outputs the result
#   find_length('Jonathan') runs first → then print() outputs the result
#   find_length('hello') runs first → then print() outputs the result
#   find_length('python') runs first → then print() outputs the result
#   find_length(string) → each argument is evaluated first, then the function body runs
#   find_length('world') runs first → then print() outputs the result
#   find_length(arg) → each argument is evaluated first, then the function body runs
#   find_length('Jonathan') → each argument is evaluated first, then the function body runs
#   string == '' → evaluated as True or False (determines which branch runs)
#   1 + find_length(...) → Python looks up 1, then find_length(...), then applies +
#   find_length('hello') → each argument is evaluated first, then the function body runs
#   find_length('python') → each argument is evaluated first, then the function body runs
#   find_length('world') → each argument is evaluated first, then the function body runs
#   find_length(?) → each argument is evaluated first, then the function body runs
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_length(string) → stored in memory, not executed
#   2. arg = ... → assigns value to variable
#   3. print(find_length(...))
#      → calls find_length(string) with string=arg
#      → 3.1 return count
#   4. def find_length(string) → stored in memory, not executed
#   5. print(find_length(...))
#      → calls find_length(string) with string='Jonathan'
#      → 5.1 return count
#   6. def find_length(string) → stored in memory, not executed
#   7. print(find_length(...))
#      → calls find_length(string) with string='hello'
#      → 7.1 return count
#   8. def find_length(string) → stored in memory, not executed
#   9. print(find_length(...))
#      → calls find_length(string) with string='python'
#      → 9.1 return count
#   10. def find_length(string) → stored in memory, not executed
#   11. def find_length(string) → stored in memory, not executed
#   12. string = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(find_length(...))
#      → calls find_length(string) with string='world'
#      → 15.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     arg = ... → stored in memory
#     print(find_length(...)) → find_length() runs first → value passed to print()
#     print(find_length(...)) → find_length() runs first → value passed to print()
#     print(find_length(...)) → find_length() runs first → value passed to print()
#     print(find_length(...)) → find_length() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_length(...)) → find_length() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 161:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def intersection() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: & in
# Built-in functions: filter, input, list, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   intersection() → each argument is evaluated first, then the function body runs
#   intersection_two_list(lst1, lst2) runs first → then print() outputs the result
#   intersection_two_list(?, ?) runs first → then print() outputs the result
#   intersection_two_list(lst1, lst2) → each argument is evaluated first, then the function body runs
#   intersection_two_list(?, ?) → each argument is evaluated first, then the function body runs
#   word in sort2 → evaluated as True or False (determines which branch runs)
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#   x in lst2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def intersection() → stored in memory, not executed
#   2. intersection() → calls intersection() with 
#      → 2.1 lst1 = input('type list: ').split()
#      → 2.2 lst2 = input('type list: ').split()
#      → 2.3 sort1 = sorted(lst1)
#      → 2.4 sort2 = sorted(lst2)
#      → 2.5 common = []
#      → 2.6 for word in sort1:
#      → 2.6     if word in sort2:
#      → 2.6         common.append(word)
#      → 2.7 result = common
#      → 2.8 return result
#   3. def intersection() → stored in memory, not executed
#   4. intersection() → calls intersection() with 
#      → 4.1 lst1 = input('type list: ').split()
#      → 4.2 lst2 = input('type list: ').split()
#      → 4.3 sort1 = sorted(lst1)
#      → 4.4 sort2 = sorted(lst2)
#      → 4.5 common = []
#      → 4.6 for word in sort1:
#      → 4.6     if word in sort2:
#      → 4.6         common.append(word)
#      → 4.7 result = common
#      → 4.8 return result
#   5. def intersection_two_list(lst1, lst2) → stored in memory, not executed
#   6. lst1 = ... → assigns value to variable
#   7. lst2 = ... → assigns value to variable
#   8. print(intersection_two_list(...))
#      → calls intersection_two_list(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 8.1 return list(filter(lambda x: x in lst2, lst1))
#   9. def intersection_two_list(lst1, lst2) → stored in memory, not executed
#   10. lst1 = ... → assigns value to variable
#   11. lst2 = ... → assigns value to variable
#   12. print(intersection_two_list(...))
#      → calls intersection_two_list(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 12.1 return list(filter(lambda x: x in lst2, lst1))
#   13. def intersection_two_list(lst1, lst2) → stored in memory, not executed
#   14. print(intersection_two_list(...))
#      → calls intersection_two_list(lst1, lst2) with lst1=[1, 2, 3, 4], lst2=[3, 4, 5, 6]
#      → 14.1 return list(filter(lambda x: x in lst2, lst1))
#   15. def intersection_two_list(lst1, lst2) → stored in memory, not executed
#   16. print(intersection_two_list(...))
#      → calls intersection_two_list(lst1, lst2) with lst1=['a', 'b', 'c'], lst2=['b', 'c', 'd']
#      → 16.1 return list(filter(lambda x: x in lst2, lst1))
#   17. def intersection() → stored in memory, not executed
#   18. def intersection() → stored in memory, not executed
#   19. lst1 = ... → assigns value to variable
#   20. lst2 = ... → assigns value to variable
#   21. result = ... → assigns value to variable
#   22. print(result)
#   23. print(intersection_two_list(...))
#      → calls intersection_two_list(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 23.1 return list(filter(lambda x: x in lst2, lst1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (intersection, intersection_two_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     intersection() → jumps into intersection(), runs body, returns
#     intersection() → jumps into intersection(), runs body, returns
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(intersection_two_list(...)) → intersection_two_list() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(intersection_two_list(...)) → intersection_two_list() runs first → value passed to print()
#     print(intersection_two_list(...)) → intersection_two_list() runs first → value passed to print()
#     print(intersection_two_list(...)) → intersection_two_list() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(intersection_two_list(...)) → intersection_two_list() runs first → value passed to print()
#
#   Loop: takes one item from sort1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 162:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def third_largest() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input, int, list, map, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   third_largest() → each argument is evaluated first, then the function body runs
#   third_largest() runs first → then print() outputs the result
#   third_largest(?) runs first → then print() outputs the result
#   third_largest(numbers) → each argument is evaluated first, then the function body runs
#   third_largest(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def third_largest() → stored in memory, not executed
#   2. third_largest() → calls third_largest() with 
#      → 2.1 user_string = input('type number followed by spaces: ')
#      → 2.2 user_split = user_string.split()
#      → 2.3 int_list = []
#      → 2.4 for number in user_split:
#      → 2.4     int_list.append(int(number))
#      → 2.5 int_list.sort()
#      → 2.6 result = int_list[-3]
#      → 2.7 result = result
#      → 2.8 return result
#   3. def third_largest() → stored in memory, not executed
#   4. print(third_largest(...))
#      → calls third_largest() with 
#      → 4.1 user_string = input('type number followed by spaces: ')
#      → 4.2 user_split = user_string.split()
#      → 4.3 int_list = []
#      → 4.4 for number in user_split:
#      → 4.4     int_list.append(int(number))
#      → 4.5 int_list.sort()
#      → 4.6 result = int_list[-3]
#      → 4.7 result = result
#      → 4.8 return result
#   5. def third_largest() → stored in memory, not executed
#   6. print(third_largest(...))
#      → calls third_largest() with 
#      → 6.1 user_string = input('type number followed by spaces: ')
#      → 6.2 user_split = user_string.split()
#      → 6.3 int_list = []
#      → 6.4 for number in user_split:
#      → 6.4     int_list.append(int(number))
#      → 6.5 int_list.sort()
#      → 6.6 result = int_list[-3]
#      → 6.7 result = result
#      → 6.8 return result
#   7. def third_largest(numbers) → stored in memory, not executed
#   8. print(third_largest(...))
#      → calls third_largest() with 
#      → 8.1 user_string = input('type number followed by spaces: ')
#      → 8.2 user_split = user_string.split()
#      → 8.3 int_list = []
#      → 8.4 for number in user_split:
#      → 8.4     int_list.append(int(number))
#      → 8.5 int_list.sort()
#      → 8.6 result = int_list[-3]
#      → 8.7 result = result
#      → 8.8 return result
#   9. def third_largest() → stored in memory, not executed
#   10. def third_largest() → stored in memory, not executed
#   11. numbers = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(third_largest(...))
#      → calls third_largest() with 
#      → 14.1 user_string = input('type number followed by spaces: ')
#      → 14.2 user_split = user_string.split()
#      → 14.3 int_list = []
#      → 14.4 for number in user_split:
#      → 14.4     int_list.append(int(number))
#      → 14.5 int_list.sort()
#      → 14.6 result = int_list[-3]
#      → 14.7 result = result
#      → 14.8 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (third_largest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     third_largest() → jumps into third_largest(), runs body, returns
#     print(third_largest(...)) → third_largest() runs first → value passed to print()
#     print(third_largest(...)) → third_largest() runs first → value passed to print()
#     print(third_largest(...)) → third_largest() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(third_largest(...)) → third_largest() runs first → value passed to print()
#
#   Loop: takes one item from user_split each round, runs body, repeats until done

"""

"""
Problem 163:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: Counter()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels(string) runs first → then print() outputs the result
#   count_vowels('Jonathan is the best') runs first → then print() outputs the result
#   count_vowels(string) → each argument is evaluated first, then the function body runs
#   count_vowels('world') runs first → then print() outputs the result
#   count_vowels('Jonathan is the best') → each argument is evaluated first, then the function body runs
#   count_vowels('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#   c in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_vowels(...))
#      → calls count_vowels(string) with string=string
#      → 3.1 return result
#   4. def count_vowels(sentence) → stored in memory, not executed
#   5. string = ... → assigns value to variable
#   6. print(count_vowels(...))
#      → calls count_vowels(string) with string=string
#      → 6.1 return result
#   7. def count_vowels(string) → stored in memory, not executed
#   8. print(count_vowels(...))
#      → calls count_vowels(string) with string='Jonathan is the best'
#      → 8.1 return result
#   9. def count_vowels(string) → stored in memory, not executed
#   10. print(count_vowels(...))
#      → calls count_vowels(string) with string='Jonathan is the best'
#      → 10.1 return result
#   11. def count_vowels(string) → stored in memory, not executed
#   12. def count_vowels(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(count_vowels(...))
#      → calls count_vowels(string) with string='world'
#      → 16.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     string = ... → stored in memory
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from vowels each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 164:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: Counter()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels(string) runs first → then print() outputs the result
#   count_vowels('Jonathan is the best') runs first → then print() outputs the result
#   count_vowels(sentence) → each argument is evaluated first, then the function body runs
#   count_vowels('Python is fun') runs first → then print() outputs the result
#   count_vowels(string) → each argument is evaluated first, then the function body runs
#   count_vowels('Jonathan is the best') → each argument is evaluated first, then the function body runs
#   count_vowels('Python is fun') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#   c in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(sentence) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_vowels(...))
#      → calls count_vowels(sentence) with sentence=string
#      → 3.1 return vowel_counts
#   4. def count_vowels(sentence) → stored in memory, not executed
#   5. print(count_vowels(...))
#      → calls count_vowels(sentence) with sentence='Jonathan is the best'
#      → 5.1 return vowel_counts
#   6. def count_vowels(sentence) → stored in memory, not executed
#   7. print(count_vowels(...))
#      → calls count_vowels(sentence) with sentence='Jonathan is the best'
#      → 7.1 return vowel_counts
#   8. def count_vowels(sentence) → stored in memory, not executed
#   9. def count_vowels(sentence) → stored in memory, not executed
#   10. sentence = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(count_vowels(...))
#      → calls count_vowels(sentence) with sentence='Python is fun'
#      → 13.1 return vowel_counts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from vowels each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 165:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def intiger_string(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, str, tuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   intiger_string(number) runs first → then print() outputs the result
#   split_number_int(number) runs first → then print() outputs the result
#   split_number_int(456) runs first → then print() outputs the result
#   split_number_int(789) runs first → then print() outputs the result
#   intiger_string(number) → each argument is evaluated first, then the function body runs
#   intiger_string(25) runs first → then print() outputs the result
#   split_number_int(number) → each argument is evaluated first, then the function body runs
#   split_number_int(456) → each argument is evaluated first, then the function body runs
#   split_number_int(789) → each argument is evaluated first, then the function body runs
#   intiger_string(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def intiger_string(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(intiger_string(...))
#      → calls intiger_string(number) with number=number
#      → 3.1 return list(str(number))
#   4. def split_number_int(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. print(split_number_int(...))
#      → calls split_number_int(number) with number=number
#      → 6.1 return [c for c in str(number)]
#   7. def split_number_int(number) → stored in memory, not executed
#   8. print(split_number_int(...))
#      → calls split_number_int(number) with number=456
#      → 8.1 return [c for c in str(number)]
#   9. def split_number_int(number) → stored in memory, not executed
#   10. print(split_number_int(...))
#      → calls split_number_int(number) with number=789
#      → 10.1 return [c for c in str(number)]
#   11. def intiger_string(number) → stored in memory, not executed
#   12. def intiger_string(number) → stored in memory, not executed
#   13. number = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(intiger_string(...))
#      → calls intiger_string(number) with number=25
#      → 16.1 return list(str(number))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (intiger_string, split_number_int) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(intiger_string(...)) → intiger_string() runs first → value passed to print()
#     number = ... → stored in memory
#     print(split_number_int(...)) → split_number_int() runs first → value passed to print()
#     print(split_number_int(...)) → split_number_int() runs first → value passed to print()
#     print(split_number_int(...)) → split_number_int() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(intiger_string(...)) → intiger_string() runs first → value passed to print()

"""

"""
Problem 166:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def double_elements(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   double_elements(lst) runs first → then print() outputs the result
#   double_elements(?) runs first → then print() outputs the result
#   double_elements(lst) → each argument is evaluated first, then the function body runs
#   lst * 2 → Python looks up lst, then 2, then applies *
#   double_elements(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def double_elements(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(double_elements(...))
#      → calls double_elements(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def double_elements(lst) → stored in memory, not executed
#   5. print(double_elements(...))
#      → calls double_elements(lst) with lst=['tree', 'house', 'car']
#      → 5.1 return list(map(lambda word: word, lst))
#   6. def double_elements(lst) → stored in memory, not executed
#   7. print(double_elements(...))
#      → calls double_elements(lst) with lst=['a', 'b', 'c']
#      → 7.1 return list(map(lambda word: word, lst))
#   8. def double_elements(lst) → stored in memory, not executed
#   9. print(double_elements(...))
#      → calls double_elements(lst) with lst=['x', 'y', 'z']
#      → 9.1 return list(map(lambda word: word, lst))
#   10. def double_elements(lst) → stored in memory, not executed
#   11. def double_elements(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(double_elements(...))
#      → calls double_elements(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (double_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 167:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: < >
# Built-in functions: input, list, map, max, min, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   l_s(numbers) runs first → then print() outputs the result
#   l_s(?) runs first → then print() outputs the result
#   l_s(numbers) → each argument is evaluated first, then the function body runs
#   l_s(?) → each argument is evaluated first, then the function body runs
#   a < b → evaluated as True or False (determines which branch runs)
#   a > b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main() → stored in memory, not executed
#   2. main() → calls main() with 
#      → 2.1 return (smallest_num, largest_num)
#   3. def l_s(numbers) → stored in memory, not executed
#   4. numbers = ... → assigns value to variable
#   5. print(l_s(...))
#      → calls l_s(numbers) with numbers=numbers
#      → 5.1 return (sorted(numbers)[0], sorted(numbers)[-1])
#   6. def l_s(numbers) → stored in memory, not executed
#   7. print(l_s(...))
#      → calls l_s(numbers) with numbers=[10, 3, 7, 1, 9]
#      → 7.1 return (sorted(numbers)[0], sorted(numbers)[-1])
#   8. def l_s(numbers) → stored in memory, not executed
#   9. print(l_s(...))
#      → calls l_s(numbers) with numbers=[4, 5, 100, 6, 1, 10]
#      → 9.1 return (sorted(numbers)[0], sorted(numbers)[-1])
#   10. def l_s(numbers) → stored in memory, not executed
#   11. print(l_s(...))
#      → calls l_s(numbers) with numbers=[5, 2, 9, 1, 7]
#      → 11.1 return (sorted(numbers)[0], sorted(numbers)[-1])
#   12. def main() → stored in memory, not executed
#   13. def main() → stored in memory, not executed
#   14. numbers = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(l_s(...))
#      → calls l_s(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 17.1 return (sorted(numbers)[0], sorted(numbers)[-1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, l_s) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     main() → jumps into main(), runs body, returns
#     numbers = ... → stored in memory
#     print(l_s(...)) → l_s() runs first → value passed to print()
#     print(l_s(...)) → l_s() runs first → value passed to print()
#     print(l_s(...)) → l_s() runs first → value passed to print()
#     print(l_s(...)) → l_s() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(l_s(...)) → l_s() runs first → value passed to print()

"""

"""
Problem 168:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_words() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: + in
# Built-in functions: Counter, dict, input, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_words() → each argument is evaluated first, then the function body runs
#   count_words(input_string) runs first → then print() outputs the result
#   count_words('This is a test. This test is only a test.') runs first → then print() outputs the result
#   count_words('one two one two three') runs first → then print() outputs the result
#   count_words('hello hello world') runs first → then print() outputs the result
#   count_words(string) → each argument is evaluated first, then the function body runs
#   count_words('world') runs first → then print() outputs the result
#   count_words(input_string) → each argument is evaluated first, then the function body runs
#   count_words('This is a test. This test is only a test.') → each argument is evaluated first, then the function body runs
#   count_words('one two one two three') → each argument is evaluated first, then the function body runs
#   count_words('hello hello world') → each argument is evaluated first, then the function body runs
#   count_words('world') → each argument is evaluated first, then the function body runs
#   word in result → evaluated as True or False (determines which branch runs)
#   word in word_count → evaluated as True or False (determines which branch runs)
#   count.get(...) + 1 → Python looks up count.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_words() → stored in memory, not executed
#   2. count_words() → calls count_words() with 
#      → 2.1 user = input('type words: ').split()
#      → 2.2 sort = sorted(user)
#      → 2.3 result = {}
#      → 2.4 for word in sort:
#      → 2.4     word = word.lower()
#      → 2.4     if word in result:
#      → 2.4         result[word] += 1
#      → 2.4     else:
#      → 2.4         result[word] = 1
#      → 2.5 result = result
#      → 2.6 return result
#   3. def count_words(string) → stored in memory, not executed
#   4. input_string = ... → assigns value to variable
#   5. print(count_words(...))
#      → calls count_words() with 
#      → 5.1 user = input('type words: ').split()
#      → 5.2 sort = sorted(user)
#      → 5.3 result = {}
#      → 5.4 for word in sort:
#      → 5.4     word = word.lower()
#      → 5.4     if word in result:
#      → 5.4         result[word] += 1
#      → 5.4     else:
#      → 5.4         result[word] = 1
#      → 5.5 result = result
#      → 5.6 return result
#   6. def count_words(string) → stored in memory, not executed
#   7. print(count_words(...))
#      → calls count_words() with 
#      → 7.1 user = input('type words: ').split()
#      → 7.2 sort = sorted(user)
#      → 7.3 result = {}
#      → 7.4 for word in sort:
#      → 7.4     word = word.lower()
#      → 7.4     if word in result:
#      → 7.4         result[word] += 1
#      → 7.4     else:
#      → 7.4         result[word] = 1
#      → 7.5 result = result
#      → 7.6 return result
#   8. def count_words(string) → stored in memory, not executed
#   9. print(count_words(...))
#      → calls count_words() with 
#      → 9.1 user = input('type words: ').split()
#      → 9.2 sort = sorted(user)
#      → 9.3 result = {}
#      → 9.4 for word in sort:
#      → 9.4     word = word.lower()
#      → 9.4     if word in result:
#      → 9.4         result[word] += 1
#      → 9.4     else:
#      → 9.4         result[word] = 1
#      → 9.5 result = result
#      → 9.6 return result
#   10. def count_words(string) → stored in memory, not executed
#   11. print(count_words(...))
#      → calls count_words() with 
#      → 11.1 user = input('type words: ').split()
#      → 11.2 sort = sorted(user)
#      → 11.3 result = {}
#      → 11.4 for word in sort:
#      → 11.4     word = word.lower()
#      → 11.4     if word in result:
#      → 11.4         result[word] += 1
#      → 11.4     else:
#      → 11.4         result[word] = 1
#      → 11.5 result = result
#      → 11.6 return result
#   12. def count_words() → stored in memory, not executed
#   13. def count_words() → stored in memory, not executed
#   14. string = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(count_words(...))
#      → calls count_words() with 
#      → 17.1 user = input('type words: ').split()
#      → 17.2 sort = sorted(user)
#      → 17.3 result = {}
#      → 17.4 for word in sort:
#      → 17.4     word = word.lower()
#      → 17.4     if word in result:
#      → 17.4         result[word] += 1
#      → 17.4     else:
#      → 17.4         result[word] = 1
#      → 17.5 result = result
#      → 17.6 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     count_words() → jumps into count_words(), runs body, returns
#     input_string = ... → stored in memory
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     print(count_words(...)) → count_words() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_words(...)) → count_words() runs first → value passed to print()
#
#   Loop: takes one item from sort each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 169:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_dictionaries(dict1, dict2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: or
# Built-in functions: dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_dictionaries(dict1, dict2) → each argument is evaluated first, then the function body runs
#   merge_dictionaries(?, ?) runs first → then print() outputs the result
#   merge_dictionaries(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_dictionaries(dict1, dict2) → stored in memory, not executed
#   2. dict1 = ... → assigns value to variable
#   3. dict2 = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def merge_dictionaries(dict1, dict2) → stored in memory, not executed
#   7. print(merge_dictionaries(...))
#      → calls merge_dictionaries(dict1, dict2) with dict1=?, dict2=?
#      → 7.1 return merged
#   8. def merge_dictionaries(dict1, dict2) → stored in memory, not executed
#   9. print(merge_dictionaries(...))
#      → calls merge_dictionaries(dict1, dict2) with dict1=?, dict2=?
#      → 9.1 return merged
#   10. def merge_dictionaries(dict1, dict2) → stored in memory, not executed
#   11. print(merge_dictionaries(...))
#      → calls merge_dictionaries(dict1, dict2) with dict1=?, dict2=?
#      → 11.1 return merged
#   12. def merge_dictionaries(dict1, dict2) → stored in memory, not executed
#   13. def merge_dictionaries(dict1, dict2) → stored in memory, not executed
#   14. dict1 = ... → assigns value to variable
#   15. dict2 = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(merge_dictionaries(...))
#      → calls merge_dictionaries(dict1, dict2) with dict1=?, dict2=?
#      → 18.1 return merged
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_dictionaries) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_dictionaries(...)) → merge_dictionaries() runs first → value passed to print()
#     print(merge_dictionaries(...)) → merge_dictionaries() runs first → value passed to print()
#     print(merge_dictionaries(...)) → merge_dictionaries() runs first → value passed to print()
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_dictionaries(...)) → merge_dictionaries() runs first → value passed to print()
#
#   Loop: takes one item from dict2.items(...) each round, runs body, repeats until done

"""

"""
Problem 170:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_characters(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + in
# Built-in functions: Counter, dict, input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_characters(string) → each argument is evaluated first, then the function body runs
#   count_characters('hello') runs first → then print() outputs the result
#   count_characters('aabbcc') runs first → then print() outputs the result
#   count_characters('abcabc') runs first → then print() outputs the result
#   count_characters('world') runs first → then print() outputs the result
#   count_characters('hello') → each argument is evaluated first, then the function body runs
#   count_characters('aabbcc') → each argument is evaluated first, then the function body runs
#   count_characters('abcabc') → each argument is evaluated first, then the function body runs
#   count_characters('world') → each argument is evaluated first, then the function body runs
#   char in counts → evaluated as True or False (determines which branch runs)
#   counts.get(...) + 1 → Python looks up counts.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_characters(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def count_characters(string) → stored in memory, not executed
#   6. print(count_characters(...))
#      → calls count_characters(string) with string='hello'
#      → 6.1 return counts
#   7. def count_characters(string) → stored in memory, not executed
#   8. print(count_characters(...))
#      → calls count_characters(string) with string='aabbcc'
#      → 8.1 return counts
#   9. def count_characters(string) → stored in memory, not executed
#   10. print(count_characters(...))
#      → calls count_characters(string) with string='abcabc'
#      → 10.1 return counts
#   11. def count_characters(string) → stored in memory, not executed
#   12. def count_characters(string) → stored in memory, not executed
#   13. string = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(count_characters(...))
#      → calls count_characters(string) with string='world'
#      → 16.1 return counts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_characters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_characters(...)) → count_characters() runs first → value passed to print()
#     print(count_characters(...)) → count_characters() runs first → value passed to print()
#     print(count_characters(...)) → count_characters() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_characters(...)) → count_characters() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 171:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def invert_dictionary(input_dict) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == not in
# Built-in functions: defaultdict, dict, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   invert_dictionary(input_dict) → each argument is evaluated first, then the function body runs
#   invert_dictionary(?) runs first → then print() outputs the result
#   invert_dictionary(?) → each argument is evaluated first, then the function body runs
#   value not in inverted → evaluated as True or False (determines which branch runs)
#   ? == v → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def invert_dictionary(input_dict) → stored in memory, not executed
#   2. input_dict = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def invert_dictionary(input_dict) → stored in memory, not executed
#   6. print(invert_dictionary(...))
#      → calls invert_dictionary(input_dict) with input_dict=?
#      → 6.1 return inverted
#   7. def invert_dictionary(input_dict) → stored in memory, not executed
#   8. print(invert_dictionary(...))
#      → calls invert_dictionary(input_dict) with input_dict=?
#      → 8.1 return inverted
#   9. def invert_dictionary(input_dict) → stored in memory, not executed
#   10. print(invert_dictionary(...))
#      → calls invert_dictionary(input_dict) with input_dict=?
#      → 10.1 return inverted
#   11. def invert_dictionary(input_dict) → stored in memory, not executed
#   12. def invert_dictionary(input_dict) → stored in memory, not executed
#   13. input_dict = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(invert_dictionary(...))
#      → calls invert_dictionary(input_dict) with input_dict=?
#      → 16.1 return inverted
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (invert_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(invert_dictionary(...)) → invert_dictionary() runs first → value passed to print()
#     print(invert_dictionary(...)) → invert_dictionary() runs first → value passed to print()
#     print(invert_dictionary(...)) → invert_dictionary() runs first → value passed to print()
#     input_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(invert_dictionary(...)) → invert_dictionary() runs first → value passed to print()
#
#   Loop: takes one item from input_dict.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 172:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_by_value(input_dict, threshold) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: dict, filter()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_by_value(input_dict, threshold) → each argument is evaluated first, then the function body runs
#   filter_by_value(?, 3) runs first → then print() outputs the result
#   filter_by_value(?, 5) runs first → then print() outputs the result
#   filter_by_value(?, 4) runs first → then print() outputs the result
#   filter_by_value(?, 25) runs first → then print() outputs the result
#   filter_by_value(?, 3) → each argument is evaluated first, then the function body runs
#   filter_by_value(?, 5) → each argument is evaluated first, then the function body runs
#   filter_by_value(?, 4) → each argument is evaluated first, then the function body runs
#   filter_by_value(?, 25) → each argument is evaluated first, then the function body runs
#   value > threshold → evaluated as True or False (determines which branch runs)
#   v > threshold → evaluated as True or False (determines which branch runs)
#   ? > threshold → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_by_value(input_dict, threshold) → stored in memory, not executed
#   2. input_dict = ... → assigns value to variable
#   3. threshold = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_by_value(input_dict, threshold) → stored in memory, not executed
#   7. print(filter_by_value(...))
#      → calls filter_by_value(input_dict, threshold) with input_dict=?, threshold=3
#      → 7.1 return filtered
#   8. def filter_by_value(input_dict, threshold) → stored in memory, not executed
#   9. print(filter_by_value(...))
#      → calls filter_by_value(input_dict, threshold) with input_dict=?, threshold=5
#      → 9.1 return filtered
#   10. def filter_by_value(input_dict, threshold) → stored in memory, not executed
#   11. print(filter_by_value(...))
#      → calls filter_by_value(input_dict, threshold) with input_dict=?, threshold=4
#      → 11.1 return filtered
#   12. def filter_by_value(input_dict, threshold) → stored in memory, not executed
#   13. def filter_by_value(input_dict, threshold) → stored in memory, not executed
#   14. input_dict = ... → assigns value to variable
#   15. threshold = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(filter_by_value(...))
#      → calls filter_by_value(input_dict, threshold) with input_dict=?, threshold=25
#      → 18.1 return filtered
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_by_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_dict = ... → stored in memory
#     threshold = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_by_value(...)) → filter_by_value() runs first → value passed to print()
#     print(filter_by_value(...)) → filter_by_value() runs first → value passed to print()
#     print(filter_by_value(...)) → filter_by_value() runs first → value passed to print()
#     input_dict = ... → stored in memory
#     threshold = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_by_value(...)) → filter_by_value() runs first → value passed to print()
#
#   Loop: takes one item from input_dict.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 173:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def combine_lists_to_dict(keys, values) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: dict, len, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   combine_lists_to_dict(keys, values) → each argument is evaluated first, then the function body runs
#   combine_lists_to_dict(?, ?) runs first → then print() outputs the result
#   combine_lists_to_dict(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def combine_lists_to_dict(keys, values) → stored in memory, not executed
#   2. keys = ... → assigns value to variable
#   3. values = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def combine_lists_to_dict(keys, values) → stored in memory, not executed
#   7. print(combine_lists_to_dict(...))
#      → calls combine_lists_to_dict(keys, values) with keys=['a', 'b', 'c'], values=[1, 2, 3]
#      → 7.1 return combined
#   8. def combine_lists_to_dict(keys, values) → stored in memory, not executed
#   9. print(combine_lists_to_dict(...))
#      → calls combine_lists_to_dict(keys, values) with keys=['x', 'y'], values=[10, 20]
#      → 9.1 return combined
#   10. def combine_lists_to_dict(keys, values) → stored in memory, not executed
#   11. print(combine_lists_to_dict(...))
#      → calls combine_lists_to_dict(keys, values) with keys=['one', 'two'], values=[1, 2]
#      → 11.1 return combined
#   12. def combine_lists_to_dict(keys, values) → stored in memory, not executed
#   13. def combine_lists_to_dict(keys, values) → stored in memory, not executed
#   14. keys = ... → assigns value to variable
#   15. values = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(combine_lists_to_dict(...))
#      → calls combine_lists_to_dict(keys, values) with keys=[4, 1, 8, 6, 3], values=[4, 1, 8, 6, 3]
#      → 18.1 return combined
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (combine_lists_to_dict) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     keys = ... → stored in memory
#     values = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(combine_lists_to_dict(...)) → combine_lists_to_dict() runs first → value passed to print()
#     print(combine_lists_to_dict(...)) → combine_lists_to_dict() runs first → value passed to print()
#     print(combine_lists_to_dict(...)) → combine_lists_to_dict() runs first → value passed to print()
#     keys = ... → stored in memory
#     values = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(combine_lists_to_dict(...)) → combine_lists_to_dict() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 174:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_value_key(input_dict) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == >=
# Built-in functions: max, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_value_key(my_dict) runs first → then print() outputs the result
#   max_value_key(empty_dict) runs first → then print() outputs the result
#   max_value_key(?) runs first → then print() outputs the result
#   max_value_key(input_dict) → each argument is evaluated first, then the function body runs
#   max_value_key(my_dict) → each argument is evaluated first, then the function body runs
#   max_value_key(empty_dict) → each argument is evaluated first, then the function body runs
#   max_value_key(?) → each argument is evaluated first, then the function body runs
#   value == max_value → evaluated as True or False (determines which branch runs)
#   ? >= ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_value_key(input_dict) → stored in memory, not executed
#   2. my_dict = ... → assigns value to variable
#   3. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=my_dict
#      → 3.1 return None
#   4. empty_dict = ... → assigns value to variable
#   5. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=empty_dict
#      → 5.1 return None
#   6. def max_value_key(input_dict) → stored in memory, not executed
#   7. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=?
#      → 7.1 return None
#   8. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=?
#      → 8.1 return None
#   9. def max_value_key(input_dict) → stored in memory, not executed
#   10. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=?
#      → 10.1 return None
#   11. def max_value_key(input_dict) → stored in memory, not executed
#   12. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=?
#      → 12.1 return None
#   13. def max_value_key(input_dict) → stored in memory, not executed
#   14. def max_value_key(input_dict) → stored in memory, not executed
#   15. input_dict = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(max_value_key(...))
#      → calls max_value_key(input_dict) with input_dict=[4, 1, 8, 6, 3]
#      → 18.1 return None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_value_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_dict = ... → stored in memory
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#     empty_dict = ... → stored in memory
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#     input_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_value_key(...)) → max_value_key() runs first → value passed to print()
#
#   Loop: takes one item from input_dict.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 175:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def dict_equal(dict1, dict2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ^ != == or not in
# Built-in functions: all, len, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dict_equal(dict1, dict2) → each argument is evaluated first, then the function body runs
#   dict_equal(?, ?) runs first → then print() outputs the result
#   dict1 == dict2 → evaluated as True or False (determines which branch runs)
#   dict1.keys(...) != dict2.keys(...) → evaluated as True or False (determines which branch runs)
#   dict_equal(?, ?) → each argument is evaluated first, then the function body runs
#   len(...) == 0 → evaluated as True or False (determines which branch runs)
#   len(...) != len(...) → evaluated as True or False (determines which branch runs)
#   ? == ? → evaluated as True or False (determines which branch runs)
#   set(...) ^ set(...) → Python looks up set(...), then set(...), then applies ^
#   key not in dict2 → evaluated as True or False (determines which branch runs)
#   ? != ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def dict_equal(dict1, dict2) → stored in memory, not executed
#   2. dict1 = ... → assigns value to variable
#   3. dict2 = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def dict_equal(dict1, dict2) → stored in memory, not executed
#   7. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 7.1 return dict1 == dict2
#   8. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 8.1 return dict1 == dict2
#   9. def dict_equal(dict1, dict2) → stored in memory, not executed
#   10. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 10.1 return dict1 == dict2
#   11. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 11.1 return dict1 == dict2
#   12. def dict_equal(dict1, dict2) → stored in memory, not executed
#   13. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 13.1 return dict1 == dict2
#   14. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 14.1 return dict1 == dict2
#   15. def dict_equal(dict1, dict2) → stored in memory, not executed
#   16. def dict_equal(dict1, dict2) → stored in memory, not executed
#   17. dict1 = ... → assigns value to variable
#   18. dict2 = ... → assigns value to variable
#   19. result = ... → assigns value to variable
#   20. print(result)
#   21. print(dict_equal(...))
#      → calls dict_equal(dict1, dict2) with dict1=?, dict2=?
#      → 21.1 return dict1 == dict2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (dict_equal) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(dict_equal(...)) → dict_equal() runs first → value passed to print()
#
#   Loop: takes one item from dict1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 176:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def word_frequency(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + in
# Built-in functions: Counter, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   word_frequency(string) runs first → then print() outputs the result
#   word_frequency('gore why are you here you are gore') runs first → then print() outputs the result
#   word_frequency('one two one two three') runs first → then print() outputs the result
#   word_frequency(string) → each argument is evaluated first, then the function body runs
#   word_frequency('world') runs first → then print() outputs the result
#   word_frequency('gore why are you here you are gore') → each argument is evaluated first, then the function body runs
#   word_frequency('one two one two three') → each argument is evaluated first, then the function body runs
#   word_frequency('world') → each argument is evaluated first, then the function body runs
#   word in result → evaluated as True or False (determines which branch runs)
#   freq.get(...) + 1 → Python looks up freq.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def word_frequency(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(word_frequency(...))
#      → calls word_frequency(string) with string=string
#      → 3.1 return result
#   4. def word_frequency(string) → stored in memory, not executed
#   5. print(word_frequency(...))
#      → calls word_frequency(string) with string='gore why are you here you are gore'
#      → 5.1 return result
#   6. def word_frequency(string) → stored in memory, not executed
#   7. print(word_frequency(...))
#      → calls word_frequency(string) with string='one two one two three'
#      → 7.1 return result
#   8. def word_frequency(string) → stored in memory, not executed
#   9. def word_frequency(string) → stored in memory, not executed
#   10. string = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(word_frequency(...))
#      → calls word_frequency(string) with string='world'
#      → 13.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (word_frequency) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 177:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_list_of_dicts(dicts) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_list_of_dicts(dicts) → each argument is evaluated first, then the function body runs
#   merge_list_of_dicts(?) runs first → then print() outputs the result
#   merge_list_of_dicts(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_list_of_dicts(dicts) → stored in memory, not executed
#   2. dicts = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def merge_list_of_dicts(dicts) → stored in memory, not executed
#   6. print(merge_list_of_dicts(...))
#      → calls merge_list_of_dicts(dicts) with dicts=[?, ?, ?]
#      → 6.1 return merged
#   7. def merge_list_of_dicts(dicts) → stored in memory, not executed
#   8. print(merge_list_of_dicts(...))
#      → calls merge_list_of_dicts(dicts) with dicts=[?, ?, ?]
#      → 8.1 return merged
#   9. def merge_list_of_dicts(dicts) → stored in memory, not executed
#   10. def merge_list_of_dicts(dicts) → stored in memory, not executed
#   11. dicts = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(merge_list_of_dicts(...))
#      → calls merge_list_of_dicts(dicts) with dicts=[4, 1, 8, 6, 3]
#      → 14.1 return merged
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_list_of_dicts) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dicts = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_list_of_dicts(...)) → merge_list_of_dicts() runs first → value passed to print()
#     print(merge_list_of_dicts(...)) → merge_list_of_dicts() runs first → value passed to print()
#     dicts = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_list_of_dicts(...)) → merge_list_of_dicts() runs first → value passed to print()
#
#   Loop: takes one item from dicts each round, runs body, repeats until done

"""

"""
Problem 178:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates_from_values(input_dict) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates_from_values(input_dict) → each argument is evaluated first, then the function body runs
#   remove_duplicates_from_values(?) runs first → then print() outputs the result
#   remove_duplicates_from_values(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates_from_values(input_dict) → stored in memory, not executed
#   2. input_dict = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def remove_duplicates_from_values(input_dict) → stored in memory, not executed
#   6. print(remove_duplicates_from_values(...))
#      → calls remove_duplicates_from_values(input_dict) with input_dict=?
#      → 6.1 return new_dict
#   7. def remove_duplicates_from_values(input_dict) → stored in memory, not executed
#   8. print(remove_duplicates_from_values(...))
#      → calls remove_duplicates_from_values(input_dict) with input_dict=?
#      → 8.1 return new_dict
#   9. def remove_duplicates_from_values(input_dict) → stored in memory, not executed
#   10. def remove_duplicates_from_values(input_dict) → stored in memory, not executed
#   11. input_dict = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(remove_duplicates_from_values(...))
#      → calls remove_duplicates_from_values(input_dict) with input_dict=?
#      → 14.1 return new_dict
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates_from_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     input_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates_from_values(...)) → remove_duplicates_from_values() runs first → value passed to print()
#     print(remove_duplicates_from_values(...)) → remove_duplicates_from_values() runs first → value passed to print()
#     input_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates_from_values(...)) → remove_duplicates_from_values() runs first → value passed to print()
#
#   Loop: takes one item from input_dict.items(...) each round, runs body, repeats until done

"""

"""
Problem 179:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common_keys_max_values(dict1, dict2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: &
# Built-in functions: max, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common_keys_max_values(dict1, dict2) → each argument is evaluated first, then the function body runs
#   common_keys_max_values(?, ?) runs first → then print() outputs the result
#   dict1.keys(...) & dict2.keys(...) → Python looks up dict1.keys(...), then dict2.keys(...), then applies &
#   common_keys_max_values(?, ?) → each argument is evaluated first, then the function body runs
#   set(...) & set(...) → Python looks up set(...), then set(...), then applies &
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common_keys_max_values(dict1, dict2) → stored in memory, not executed
#   2. dict1 = ... → assigns value to variable
#   3. dict2 = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def common_keys_max_values(dict1, dict2) → stored in memory, not executed
#   7. print(common_keys_max_values(...))
#      → calls common_keys_max_values(dict1, dict2) with dict1=?, dict2=?
#      → 7.1 return common_keys
#   8. def common_keys_max_values(dict1, dict2) → stored in memory, not executed
#   9. print(common_keys_max_values(...))
#      → calls common_keys_max_values(dict1, dict2) with dict1=?, dict2=?
#      → 9.1 return common_keys
#   10. def common_keys_max_values(dict1, dict2) → stored in memory, not executed
#   11. def common_keys_max_values(dict1, dict2) → stored in memory, not executed
#   12. dict1 = ... → assigns value to variable
#   13. dict2 = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(common_keys_max_values(...))
#      → calls common_keys_max_values(dict1, dict2) with dict1=[4, 1, 8, 6, 3], dict2=[4, 1, 8, 6, 3]
#      → 16.1 return common_keys
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common_keys_max_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_keys_max_values(...)) → common_keys_max_values() runs first → value passed to print()
#     print(common_keys_max_values(...)) → common_keys_max_values() runs first → value passed to print()
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_keys_max_values(...)) → common_keys_max_values() runs first → value passed to print()
#
#   Loop: takes one item from ? each round, runs body, repeats until done

"""

"""
Problem 180:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average_values(dicts) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: / in
# Built-in functions: defaultdict, set, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average_values(dicts) → each argument is evaluated first, then the function body runs
#   average_values(?) runs first → then print() outputs the result
#   average_values(?) → each argument is evaluated first, then the function body runs
#   ? / ? → Python looks up ?, then ?, then applies /
#   sum(...) / sum(...) → Python looks up sum(...), then sum(...), then applies /
#   key in averages → evaluated as True or False (determines which branch runs)
#   k in d → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average_values(dicts) → stored in memory, not executed
#   2. dicts = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def average_values(dicts) → stored in memory, not executed
#   6. print(average_values(...))
#      → calls average_values(dicts) with dicts=[?, ?, ?]
#      → 6.1 return averages
#   7. def average_values(dicts) → stored in memory, not executed
#   8. print(average_values(...))
#      → calls average_values(dicts) with dicts=[?, ?]
#      → 8.1 return averages
#   9. def average_values(dicts) → stored in memory, not executed
#   10. def average_values(dicts) → stored in memory, not executed
#   11. dicts = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(average_values(...))
#      → calls average_values(dicts) with dicts=[4, 1, 8, 6, 3]
#      → 14.1 return averages
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dicts = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_values(...)) → average_values() runs first → value passed to print()
#     print(average_values(...)) → average_values() runs first → value passed to print()
#     dicts = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_values(...)) → average_values() runs first → value passed to print()
#
#   Loop: takes one item from dicts each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 181:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_dicts_with_lists(dicts) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: defaultdict, list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_dicts_with_lists(dicts) → each argument is evaluated first, then the function body runs
#   merge_dicts_with_lists(?) runs first → then print() outputs the result
#   merge_dicts_with_lists(?) → each argument is evaluated first, then the function body runs
#   key in merged → evaluated as True or False (determines which branch runs)
#   k in d → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_dicts_with_lists(dicts) → stored in memory, not executed
#   2. dicts = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def merge_dicts_with_lists(dicts) → stored in memory, not executed
#   6. print(merge_dicts_with_lists(...))
#      → calls merge_dicts_with_lists(dicts) with dicts=[?, ?]
#      → 6.1 return merged
#   7. def merge_dicts_with_lists(dicts) → stored in memory, not executed
#   8. print(merge_dicts_with_lists(...))
#      → calls merge_dicts_with_lists(dicts) with dicts=[?, ?]
#      → 8.1 return merged
#   9. def merge_dicts_with_lists(dicts) → stored in memory, not executed
#   10. def merge_dicts_with_lists(dicts) → stored in memory, not executed
#   11. dicts = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(merge_dicts_with_lists(...))
#      → calls merge_dicts_with_lists(dicts) with dicts=[4, 1, 8, 6, 3]
#      → 14.1 return merged
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_dicts_with_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dicts = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_dicts_with_lists(...)) → merge_dicts_with_lists() runs first → value passed to print()
#     print(merge_dicts_with_lists(...)) → merge_dicts_with_lists() runs first → value passed to print()
#     dicts = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_dicts_with_lists(...)) → merge_dicts_with_lists() runs first → value passed to print()
#
#   Loop: takes one item from dicts each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 182:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def group_by_first_letter(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: defaultdict, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   group_by_first_letter(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   group_by_first_letter(?) runs first → then print() outputs the result
#   group_by_first_letter(lst) → each argument is evaluated first, then the function body runs
#   group_by_first_letter(?) → each argument is evaluated first, then the function body runs
#   first_letter not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def group_by_first_letter(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(group_by_first_letter(...))
#      → calls group_by_first_letter(lst) with lst=lst
#      → 3.1 return result
#   4. def main(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. main(lst) → calls main(lst) with lst=lst
#      → 6.1 result = {}
#      → 6.2 for word in lst:
#      → 6.2     key = word[0]
#      → 6.2     value = word
#      → 6.2     result[key] = value
#      → 6.3 return result
#   7. def group_by_first_letter(lst) → stored in memory, not executed
#   8. print(group_by_first_letter(...))
#      → calls group_by_first_letter(lst) with lst=['apple', 'banana', 'avocado', 'blueberry']
#      → 8.1 return result
#   9. def group_by_first_letter(lst) → stored in memory, not executed
#   10. print(group_by_first_letter(...))
#      → calls group_by_first_letter(lst) with lst=['cat', 'dog', 'car', 'deer']
#      → 10.1 return result
#   11. def group_by_first_letter(lst) → stored in memory, not executed
#   12. def group_by_first_letter(lst) → stored in memory, not executed
#   13. lst = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(group_by_first_letter(...))
#      → calls group_by_first_letter(lst) with lst=[1, 3, 5, 7]
#      → 16.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (group_by_first_letter, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(group_by_first_letter(...)) → group_by_first_letter() runs first → value passed to print()
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     print(group_by_first_letter(...)) → group_by_first_letter() runs first → value passed to print()
#     print(group_by_first_letter(...)) → group_by_first_letter() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(group_by_first_letter(...)) → group_by_first_letter() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 183:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_anagram(string1, string2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: != == in
# Built-in functions: Counter, all, input, len, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_anagram(string1, string2) runs first → then print() outputs the result
#   is_anagram('listen', 'silent') runs first → then print() outputs the result
#   is_anagram('hello', 'olleh') runs first → then print() outputs the result
#   is_anagram('test', 'tent') runs first → then print() outputs the result
#   is_anagram(string1, string2) → each argument is evaluated first, then the function body runs
#   is_anagram('world', 'python') runs first → then print() outputs the result
#   len(...) != len(...) → evaluated as True or False (determines which branch runs)
#   char_count1 == char_count2 → evaluated as True or False (determines which branch runs)
#   Counter(...) == Counter(...) → evaluated as True or False (determines which branch runs)
#   is_anagram('listen', 'silent') → each argument is evaluated first, then the function body runs
#   is_anagram('hello', 'olleh') → each argument is evaluated first, then the function body runs
#   is_anagram('test', 'tent') → each argument is evaluated first, then the function body runs
#   is_anagram('world', 'python') → each argument is evaluated first, then the function body runs
#   char in char_count1 → evaluated as True or False (determines which branch runs)
#   char in char_count2 → evaluated as True or False (determines which branch runs)
#   s1.count(...) == s2.count(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_anagram(string1, string2) → stored in memory, not executed
#   2. string1 = ... → assigns value to variable
#   3. string2 = ... → assigns value to variable
#   4. print('Are the strings anagrams?:', is_anagram(...))
#   5. def is_anagram(string1, string2) → stored in memory, not executed
#   6. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='listen', string2='silent'
#      → 6.1 return False
#   7. def is_anagram(string1, string2) → stored in memory, not executed
#   8. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='hello', string2='olleh'
#      → 8.1 return False
#   9. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='test', string2='tent'
#      → 9.1 return False
#   10. def is_anagram(string1, string2) → stored in memory, not executed
#   11. def is_anagram(string1, string2) → stored in memory, not executed
#   12. string1 = ... → assigns value to variable
#   13. string2 = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(is_anagram(...))
#      → calls is_anagram(string1, string2) with string1='world', string2='python'
#      → 16.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_anagram) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string1 = ... → stored in memory
#     string2 = ... → stored in memory
#     print('Are the strings anagrams?:', is_anagram(...))
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#     string1 = ... → stored in memory
#     string2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_anagram(...)) → is_anagram() runs first → value passed to print()
#
#   Loop: takes one item from string1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 184:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def int(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % //
# Built-in functions: list, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   int(number) runs first → then print() outputs the result
#   extract_digit(123) runs first → then print() outputs the result
#   get_digit(123) runs first → then print() outputs the result
#   int(number) → each argument is evaluated first, then the function body runs
#   int(25) runs first → then print() outputs the result
#   ? % 10 → Python looks up ?, then 10, then applies %
#   extract_digit(123) → each argument is evaluated first, then the function body runs
#   get_digit(123) → each argument is evaluated first, then the function body runs
#   int(25) → each argument is evaluated first, then the function body runs
#   number // 10 → Python looks up number, then 10, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def int(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(int(...))
#      → calls int(number) with number=number
#      → 3.1 return list_number
#   4. def extract_digit(number) → stored in memory, not executed
#   5. print(extract_digit(...))
#      → calls extract_digit(number) with number=123
#      → 5.1 return number // 10 % 10
#   6. def get_digit(n) → stored in memory, not executed
#   7. print(get_digit(...))
#      → calls get_digit(n) with n=123
#      → 7.1 return str(n)[1]
#   8. def int(number) → stored in memory, not executed
#   9. def int(number) → stored in memory, not executed
#   10. number = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(int(...))
#      → calls int(number) with number=25
#      → 13.1 return list_number
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (int, get_digit, extract_digit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(int(...)) → int() runs first → value passed to print()
#     print(extract_digit(...)) → extract_digit() runs first → value passed to print()
#     print(get_digit(...)) → get_digit() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(int(...)) → int() runs first → value passed to print()

"""

"""
Problem 185:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def int(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str, tuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   int(number) runs first → then print() outputs the result
#   extract_digits(12345678) runs first → then print() outputs the result
#   get_digits(12345678) runs first → then print() outputs the result
#   int(number) → each argument is evaluated first, then the function body runs
#   int(?) runs first → then print() outputs the result
#   extract_digits(12345678) → each argument is evaluated first, then the function body runs
#   get_digits(12345678) → each argument is evaluated first, then the function body runs
#   int(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def int(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(int(...))
#      → calls int(number) with number=number
#      → 3.1 return (num2, num4, num8)
#   4. def extract_digits(number) → stored in memory, not executed
#   5. print(extract_digits(...))
#      → calls extract_digits(number) with number=12345678
#      → 5.1 s = str(number)
#      → 5.2 return (s[1], s[3], s[-1])
#   6. def get_digits(num) → stored in memory, not executed
#   7. print(get_digits(...))
#      → calls get_digits(num) with num=12345678
#      → 7.1 s = str(num)
#      → 7.2 return tuple((s[i] for i in [1, 3, -1]))
#   8. def int(number) → stored in memory, not executed
#   9. def int(number) → stored in memory, not executed
#   10. number = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(int(...))
#      → calls int(number) with number=[4, 1, 8, 6, 3]
#      → 13.1 return (num2, num4, num8)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (int, get_digits, extract_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(int(...)) → int() runs first → value passed to print()
#     print(extract_digits(...)) → extract_digits() runs first → value passed to print()
#     print(get_digits(...)) → get_digits() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(int(...)) → int() runs first → value passed to print()

"""

"""
Problem 186:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_digits(number, indices) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: itemgetter, list, map, str, tuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_digits(number, indices) runs first → then print() outputs the result
#   get_digits(12345678910, ?) runs first → then print() outputs the result
#   get_digits(number, indices) → each argument is evaluated first, then the function body runs
#   get_digits(25, ?) runs first → then print() outputs the result
#   get_digits(12345678910, ?) → each argument is evaluated first, then the function body runs
#   get_digits(25, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_digits(number, indices) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. indices = ... → assigns value to variable
#   4. print(get_digits(...))
#      → calls get_digits(number, indices) with number=number, indices=indices
#      → 4.1 return list(map(lambda i: number_str[i], indices))
#   5. def get_digits(number, indices) → stored in memory, not executed
#   6. print(get_digits(...))
#      → calls get_digits(number, indices) with number=12345678910, indices=[1, 3, 7]
#      → 6.1 return list(map(lambda i: number_str[i], indices))
#   7. def get_digits(number, indices) → stored in memory, not executed
#   8. print(get_digits(...))
#      → calls get_digits(number, indices) with number=12345678910, indices=[1, 3, 7]
#      → 8.1 return list(map(lambda i: number_str[i], indices))
#   9. def get_digits(number, indices) → stored in memory, not executed
#   10. def get_digits(number, indices) → stored in memory, not executed
#   11. number = ... → assigns value to variable
#   12. indices = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(get_digits(...))
#      → calls get_digits(number, indices) with number=25, indices=[4, 1, 8, 6, 3]
#      → 15.1 return list(map(lambda i: number_str[i], indices))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     indices = ... → stored in memory
#     print(get_digits(...)) → get_digits() runs first → value passed to print()
#     print(get_digits(...)) → get_digits() runs first → value passed to print()
#     print(get_digits(...)) → get_digits() runs first → value passed to print()
#     number = ... → stored in memory
#     indices = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_digits(...)) → get_digits() runs first → value passed to print()
#
#   Loop: takes one item from indices each round, runs body, repeats until done

"""

"""
Problem 187:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def double_elements(string1) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, map, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   double_elements(string1) runs first → then print() outputs the result
#   double_elements(?) runs first → then print() outputs the result
#   double_elements(string1) → each argument is evaluated first, then the function body runs
#   double_elements('world') runs first → then print() outputs the result
#   double_elements(?) → each argument is evaluated first, then the function body runs
#   double_elements('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def double_elements(string1) → stored in memory, not executed
#   2. string1 = ... → assigns value to variable
#   3. print(double_elements(...))
#      → calls double_elements(string1) with string1=string1
#      → 3.1 return list(map(lambda word: word, string1))
#   4. def double_elements(lst) → stored in memory, not executed
#   5. print(double_elements(...))
#      → calls double_elements(string1) with string1=['tree', 'road', 'slow']
#      → 5.1 return list(map(lambda word: word, string1))
#   6. def double_elements(lst) → stored in memory, not executed
#   7. print(double_elements(...))
#      → calls double_elements(string1) with string1=['a', 'b', 'c']
#      → 7.1 return list(map(lambda word: word, string1))
#   8. def double_elements(string1) → stored in memory, not executed
#   9. def double_elements(string1) → stored in memory, not executed
#   10. string1 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(double_elements(...))
#      → calls double_elements(string1) with string1='world'
#      → 13.1 return list(map(lambda word: word, string1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (double_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string1 = ... → stored in memory
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#     string1 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(double_elements(...)) → double_elements() runs first → value passed to print()
#
#   Loop: takes one item from string1 each round, runs body, repeats until done

"""

"""
Problem 188:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_initials(name) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_initials(name) runs first → then print() outputs the result
#   get_initials('jonathan noll') runs first → then print() outputs the result
#   get_initials(name) → each argument is evaluated first, then the function body runs
#   get_initials('Bob') runs first → then print() outputs the result
#   get_initials('jonathan noll') → each argument is evaluated first, then the function body runs
#   get_initials('Bob') → each argument is evaluated first, then the function body runs
#   char in 'nall' → evaluated as True or False (determines which branch runs)
#   c in 'nall' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_initials(name) → stored in memory, not executed
#   2. name = ... → assigns value to variable
#   3. print(get_initials(...))
#      → calls get_initials(name) with name=name
#      → 3.1 return ''.join(name_list)
#   4. def get_initials(name) → stored in memory, not executed
#   5. name = ... → assigns value to variable
#   6. print(get_initials(...))
#      → calls get_initials(name) with name=name
#      → 6.1 return ''.join(name_list)
#   7. def get_initials(name) → stored in memory, not executed
#   8. print(get_initials(...))
#      → calls get_initials(name) with name='jonathan noll'
#      → 8.1 return ''.join(name_list)
#   9. def get_initials(name) → stored in memory, not executed
#   10. print(get_initials(...))
#      → calls get_initials(name) with name='jonathan noll'
#      → 10.1 return ''.join(name_list)
#   11. def get_initials(name) → stored in memory, not executed
#   12. def get_initials(name) → stored in memory, not executed
#   13. name = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(get_initials(...))
#      → calls get_initials(name) with name='Bob'
#      → 16.1 return ''.join(name_list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_initials) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     name = ... → stored in memory
#     print(get_initials(...)) → get_initials() runs first → value passed to print()
#     name = ... → stored in memory
#     print(get_initials(...)) → get_initials() runs first → value passed to print()
#     print(get_initials(...)) → get_initials() runs first → value passed to print()
#     print(get_initials(...)) → get_initials() runs first → value passed to print()
#     name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_initials(...)) → get_initials() runs first → value passed to print()
#
#   Loop: takes one item from name each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 189:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_digits() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: +
# Built-in functions: filter, input, int, list, reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_digits() → each argument is evaluated first, then the function body runs
#   digits_string() → each argument is evaluated first, then the function body runs
#   sum_digits('1234') runs first → then print() outputs the result
#   sum_digits('a1b2c3') runs first → then print() outputs the result
#   sum_digits('45x67') runs first → then print() outputs the result
#   sum_digits(s) → each argument is evaluated first, then the function body runs
#   sum_digits(?) runs first → then print() outputs the result
#   sum_digits('1234') → each argument is evaluated first, then the function body runs
#   sum_digits('a1b2c3') → each argument is evaluated first, then the function body runs
#   sum_digits('45x67') → each argument is evaluated first, then the function body runs
#   sum_digits(?) → each argument is evaluated first, then the function body runs
#   a + int(...) → Python looks up a, then int(...), then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_digits() → stored in memory, not executed
#   2. sum_digits() → calls sum_digits() with 
#      → 2.1 user = list(input('type number: '))
#      → 2.2 count = 0
#      → 2.3 for number in user:
#      → 2.3     count += int(number)
#      → 2.4 result = count
#      → 2.5 return result
#   3. def digits_string() → stored in memory, not executed
#   4. digits_string() → calls digits_string() with 
#      → 4.1 user = input('Type a string containing numbers: ')
#      → 4.2 total = 0
#      → 4.3 for char in user:
#      → 4.3     if char.isdigit():
#      → 4.3         total += int(char)
#      → 4.4 print(total)
#   5. def sum_digits(s) → stored in memory, not executed
#   6. print(sum_digits(...))
#      → calls sum_digits() with 
#      → 6.1 user = list(input('type number: '))
#      → 6.2 count = 0
#      → 6.3 for number in user:
#      → 6.3     count += int(number)
#      → 6.4 result = count
#      → 6.5 return result
#   7. print(sum_digits(...))
#      → calls sum_digits() with 
#      → 7.1 user = list(input('type number: '))
#      → 7.2 count = 0
#      → 7.3 for number in user:
#      → 7.3     count += int(number)
#      → 7.4 result = count
#      → 7.5 return result
#   8. def sum_digits(s) → stored in memory, not executed
#   9. print(sum_digits(...))
#      → calls sum_digits() with 
#      → 9.1 user = list(input('type number: '))
#      → 9.2 count = 0
#      → 9.3 for number in user:
#      → 9.3     count += int(number)
#      → 9.4 result = count
#      → 9.5 return result
#   10. def sum_digits() → stored in memory, not executed
#   11. def sum_digits() → stored in memory, not executed
#   12. s = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(sum_digits(...))
#      → calls sum_digits() with 
#      → 15.1 user = list(input('type number: '))
#      → 15.2 count = 0
#      → 15.3 for number in user:
#      → 15.3     count += int(number)
#      → 15.4 result = count
#      → 15.5 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_digits, digits_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sum_digits() → jumps into sum_digits(), runs body, returns
#     digits_string() → jumps into digits_string(), runs body, returns
#     print(sum_digits(...)) → sum_digits() runs first → value passed to print()
#     print(sum_digits(...)) → sum_digits() runs first → value passed to print()
#     print(sum_digits(...)) → sum_digits() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_digits(...)) → sum_digits() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 190:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicate() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicate() → each argument is evaluated first, then the function body runs
#   remove_duplicate('hello') runs first → then print() outputs the result
#   remove_duplicate('banana') runs first → then print() outputs the result
#   remove_duplicate(s) → each argument is evaluated first, then the function body runs
#   remove_duplicate(10) runs first → then print() outputs the result
#   remove_duplicate('hello') → each argument is evaluated first, then the function body runs
#   remove_duplicate('banana') → each argument is evaluated first, then the function body runs
#   remove_duplicate(10) → each argument is evaluated first, then the function body runs
#   char not in duplicate → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicate() → stored in memory, not executed
#   2. remove_duplicate() → calls remove_duplicate() with 
#      → 2.1 user = input('type string: ')
#      → 2.2 duplicate = []
#      → 2.3 for char in user:
#      → 2.3     if char not in duplicate:
#      → 2.3         duplicate.append(char)
#      → 2.4 result = duplicate
#      → 2.5 return result
#   3. def remove_duplicate(s) → stored in memory, not executed
#   4. print(remove_duplicate(...))
#      → calls remove_duplicate() with 
#      → 4.1 user = input('type string: ')
#      → 4.2 duplicate = []
#      → 4.3 for char in user:
#      → 4.3     if char not in duplicate:
#      → 4.3         duplicate.append(char)
#      → 4.4 result = duplicate
#      → 4.5 return result
#   5. def remove_duplicate(s) → stored in memory, not executed
#   6. print(remove_duplicate(...))
#      → calls remove_duplicate() with 
#      → 6.1 user = input('type string: ')
#      → 6.2 duplicate = []
#      → 6.3 for char in user:
#      → 6.3     if char not in duplicate:
#      → 6.3         duplicate.append(char)
#      → 6.4 result = duplicate
#      → 6.5 return result
#   7. def remove_duplicate() → stored in memory, not executed
#   8. def remove_duplicate() → stored in memory, not executed
#   9. s = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(remove_duplicate(...))
#      → calls remove_duplicate() with 
#      → 12.1 user = input('type string: ')
#      → 12.2 duplicate = []
#      → 12.3 for char in user:
#      → 12.3     if char not in duplicate:
#      → 12.3         duplicate.append(char)
#      → 12.4 result = duplicate
#      → 12.5 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicate) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     remove_duplicate() → jumps into remove_duplicate(), runs body, returns
#     print(remove_duplicate(...)) → remove_duplicate() runs first → value passed to print()
#     print(remove_duplicate(...)) → remove_duplicate() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicate(...)) → remove_duplicate() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 191:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: input, int, min, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min() → each argument is evaluated first, then the function body runs
#   find_min(?) runs first → then print() outputs the result
#   find_min(nums) → each argument is evaluated first, then the function body runs
#   find_min(?) → each argument is evaluated first, then the function body runs
#   number < min_number → evaluated as True or False (determines which branch runs)
#   a < b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min() → stored in memory, not executed
#   2. find_min() → calls find_min() with 
#      → 2.1 user = input('type list: ').split()
#      → 2.2 user_int = []
#      → 2.3 for number in user:
#      → 2.3     user_int.append(int(number))
#      → 2.4 min_number = user_int[0]
#      → 2.5 for number in user_int:
#      → 2.5     if number < min_number:
#      → 2.5         min_number = number
#      → 2.6 result = min_number
#      → 2.7 return result
#   3. def find_min(nums) → stored in memory, not executed
#   4. print(find_min(...))
#      → calls find_min() with 
#      → 4.1 user = input('type list: ').split()
#      → 4.2 user_int = []
#      → 4.3 for number in user:
#      → 4.3     user_int.append(int(number))
#      → 4.4 min_number = user_int[0]
#      → 4.5 for number in user_int:
#      → 4.5     if number < min_number:
#      → 4.5         min_number = number
#      → 4.6 result = min_number
#      → 4.7 return result
#   5. def find_min(nums) → stored in memory, not executed
#   6. print(find_min(...))
#      → calls find_min() with 
#      → 6.1 user = input('type list: ').split()
#      → 6.2 user_int = []
#      → 6.3 for number in user:
#      → 6.3     user_int.append(int(number))
#      → 6.4 min_number = user_int[0]
#      → 6.5 for number in user_int:
#      → 6.5     if number < min_number:
#      → 6.5         min_number = number
#      → 6.6 result = min_number
#      → 6.7 return result
#   7. def find_min() → stored in memory, not executed
#   8. def find_min() → stored in memory, not executed
#   9. nums = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_min(...))
#      → calls find_min() with 
#      → 12.1 user = input('type list: ').split()
#      → 12.2 user_int = []
#      → 12.3 for number in user:
#      → 12.3     user_int.append(int(number))
#      → 12.4 min_number = user_int[0]
#      → 12.5 for number in user_int:
#      → 12.5     if number < min_number:
#      → 12.5         min_number = number
#      → 12.6 result = min_number
#      → 12.7 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_min) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     find_min() → jumps into find_min(), runs body, returns
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min(...)) → find_min() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 192:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_occurrences(lst, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: Counter, filter, input, len, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_occurrences(test_list, element_to_count) runs first → then print() outputs the result
#   main() runs first → then print() outputs the result
#   count_occurrences(?, 1) runs first → then print() outputs the result
#   count_occurrences(?, 'a') runs first → then print() outputs the result
#   count_occurrences(lst, element) → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 25) runs first → then print() outputs the result
#   count_occurrences(test_list, element_to_count) → each argument is evaluated first, then the function body runs
#   count == 0 → evaluated as True or False (determines which branch runs)
#   main() → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 1) → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 'a') → each argument is evaluated first, then the function body runs
#   count_occurrences(?, 25) → each argument is evaluated first, then the function body runs
#   item == element → evaluated as True or False (determines which branch runs)
#   word.lower(...) == user → evaluated as True or False (determines which branch runs)
#   x == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_occurrences(lst, element) → stored in memory, not executed
#   2. test_list = ... → assigns value to variable
#   3. element_to_count = ... → assigns value to variable
#   4. print('Number of occurrences of', element_to_count, ':', count_occurrences(...))
#   5. def main() → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. print(main(...))
#      → calls main() with 
#      → 7.1 user = input('type element: ').lower()
#      → 7.2 count = 0
#      → 7.3 for word in lst:
#      → 7.3     if word.lower() == user:
#      → 7.3         count += 1
#      → 7.4 if count == 0:
#      → 7.4     return 'not in lst'
#      → 7.4 else:
#      → 7.4     return count
#   8. def count_occurrences(lst, element) → stored in memory, not executed
#   9. print(count_occurrences(...))
#      → calls count_occurrences(lst, element) with lst=[1, 2, 3, 1, 1, 4], element=1
#      → 9.1 return count
#   10. def count_occurrences(lst, element) → stored in memory, not executed
#   11. print(count_occurrences(...))
#      → calls count_occurrences(lst, element) with lst=['a', 'b', 'a', 'c', 'a'], element='a'
#      → 11.1 return count
#   12. def count_occurrences(lst, element) → stored in memory, not executed
#   13. print(count_occurrences(...))
#      → calls count_occurrences(lst, element) with lst=[1, 2, 1, 3], element=1
#      → 13.1 return count
#   14. def count_occurrences(lst, element) → stored in memory, not executed
#   15. def count_occurrences(lst, element) → stored in memory, not executed
#   16. lst = ... → assigns value to variable
#   17. element = ... → assigns value to variable
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. print(count_occurrences(...))
#      → calls count_occurrences(lst, element) with lst=[1, 3, 5, 7], element=25
#      → 20.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, count_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     test_list = ... → stored in memory
#     element_to_count = ... → stored in memory
#     print('Number of occurrences of', element_to_count, ':', count_occurrences(...))
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     lst = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 193:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min_num() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input, int, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min_num() → each argument is evaluated first, then the function body runs
#   find_min_num(?) runs first → then print() outputs the result
#   find_min_num(nums) → each argument is evaluated first, then the function body runs
#   find_min_num(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min_num() → stored in memory, not executed
#   2. find_min_num() → calls find_min_num() with 
#      → 2.1 user = input('type number: ').split()
#      → 2.2 int_number = []
#      → 2.3 for number in user:
#      → 2.3     int_number.append(int(number))
#      → 2.4 min_num = sorted(int_number)[0]
#      → 2.5 result = min_num
#      → 2.6 return result
#   3. def find_min_num(nums) → stored in memory, not executed
#   4. print(find_min_num(...))
#      → calls find_min_num() with 
#      → 4.1 user = input('type number: ').split()
#      → 4.2 int_number = []
#      → 4.3 for number in user:
#      → 4.3     int_number.append(int(number))
#      → 4.4 min_num = sorted(int_number)[0]
#      → 4.5 result = min_num
#      → 4.6 return result
#   5. def find_min_num(nums) → stored in memory, not executed
#   6. print(find_min_num(...))
#      → calls find_min_num() with 
#      → 6.1 user = input('type number: ').split()
#      → 6.2 int_number = []
#      → 6.3 for number in user:
#      → 6.3     int_number.append(int(number))
#      → 6.4 min_num = sorted(int_number)[0]
#      → 6.5 result = min_num
#      → 6.6 return result
#   7. def find_min_num() → stored in memory, not executed
#   8. def find_min_num() → stored in memory, not executed
#   9. nums = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_min_num(...))
#      → calls find_min_num() with 
#      → 12.1 user = input('type number: ').split()
#      → 12.2 int_number = []
#      → 12.3 for number in user:
#      → 12.3     int_number.append(int(number))
#      → 12.4 min_num = sorted(int_number)[0]
#      → 12.5 result = min_num
#      → 12.6 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_min_num) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     find_min_num() → jumps into find_min_num(), runs body, returns
#     print(find_min_num(...)) → find_min_num() runs first → value passed to print()
#     print(find_min_num(...)) → find_min_num() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min_num(...)) → find_min_num() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done

"""

"""
Problem 194:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_element() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: >=
# Built-in functions: input, len, max, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_element() → each argument is evaluated first, then the function body runs
#   largest_element(?) runs first → then print() outputs the result
#   largest_element(words) → each argument is evaluated first, then the function body runs
#   largest_element(?) → each argument is evaluated first, then the function body runs
#   len(...) >= len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_element() → stored in memory, not executed
#   2. largest_element() → calls largest_element() with 
#      → 2.1 user = input('type list: ').split()
#      → 2.2 largest_word = max(user, key=len)
#      → 2.3 result = largest_word
#      → 2.4 return result
#   3. def largest_element(words) → stored in memory, not executed
#   4. print(largest_element(...))
#      → calls largest_element() with 
#      → 4.1 user = input('type list: ').split()
#      → 4.2 largest_word = max(user, key=len)
#      → 4.3 result = largest_word
#      → 4.4 return result
#   5. def largest_element(words) → stored in memory, not executed
#   6. print(largest_element(...))
#      → calls largest_element() with 
#      → 6.1 user = input('type list: ').split()
#      → 6.2 largest_word = max(user, key=len)
#      → 6.3 result = largest_word
#      → 6.4 return result
#   7. def largest_element() → stored in memory, not executed
#   8. def largest_element() → stored in memory, not executed
#   9. words = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(largest_element(...))
#      → calls largest_element() with 
#      → 12.1 user = input('type list: ').split()
#      → 12.2 largest_word = max(user, key=len)
#      → 12.3 result = largest_word
#      → 12.4 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     largest_element() → jumps into largest_element(), runs body, returns
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     print(largest_element(...)) → largest_element() runs first → value passed to print()
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_element(...)) → largest_element() runs first → value passed to print()

"""

"""
Problem 195:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_largest_element() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: input, len, nlargest, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_largest_element() → each argument is evaluated first, then the function body runs
#   second_largest_element(?) runs first → then print() outputs the result
#   second_largest_element(words) → each argument is evaluated first, then the function body runs
#   second_largest_element('world') runs first → then print() outputs the result
#   len(...) < 2 → evaluated as True or False (determines which branch runs)
#   second_largest_element(?) → each argument is evaluated first, then the function body runs
#   second_largest_element('world') → each argument is evaluated first, then the function body runs
#   len(...) > len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_largest_element() → stored in memory, not executed
#   2. second_largest_element() → calls second_largest_element(words) with 
#      → 2.1 if len(words) < 2:
#      → 2.1     return None
#      → 2.2 first = second = ''
#      → 2.3 for w in words:
#      → 2.3     if len(w) > len(first):
#      → 2.3         second = first
#      → 2.3         first = w
#      → 2.3     elif len(w) > len(second):
#      → 2.3         second = w
#      → 2.4 return second
#   3. def second_largest_element() → stored in memory, not executed
#   4. second_largest_element() → calls second_largest_element(words) with 
#      → 4.1 if len(words) < 2:
#      → 4.1     return None
#      → 4.2 first = second = ''
#      → 4.3 for w in words:
#      → 4.3     if len(w) > len(first):
#      → 4.3         second = first
#      → 4.3         first = w
#      → 4.3     elif len(w) > len(second):
#      → 4.3         second = w
#      → 4.4 return second
#   5. def second_largest_element() → stored in memory, not executed
#   6. second_largest_element() → calls second_largest_element(words) with 
#      → 6.1 if len(words) < 2:
#      → 6.1     return None
#      → 6.2 first = second = ''
#      → 6.3 for w in words:
#      → 6.3     if len(w) > len(first):
#      → 6.3         second = first
#      → 6.3         first = w
#      → 6.3     elif len(w) > len(second):
#      → 6.3         second = w
#      → 6.4 return second
#   7. def second_largest_element(words) → stored in memory, not executed
#   8. print(second_largest_element(...))
#      → calls second_largest_element(words) with words=['apple', 'banana', 'kiwi', 'strawberry']
#      → 8.1 if len(words) < 2:
#      → 8.1     return None
#      → 8.2 first = second = ''
#      → 8.3 for w in words:
#      → 8.3     if len(w) > len(first):
#      → 8.3         second = first
#      → 8.3         first = w
#      → 8.3     elif len(w) > len(second):
#      → 8.3         second = w
#      → 8.4 return second
#   9. def second_largest_element(words) → stored in memory, not executed
#   10. print(second_largest_element(...))
#      → calls second_largest_element(words) with words=['cat', 'dog', 'elephant', 'bee']
#      → 10.1 if len(words) < 2:
#      → 10.1     return None
#      → 10.2 first = second = ''
#      → 10.3 for w in words:
#      → 10.3     if len(w) > len(first):
#      → 10.3         second = first
#      → 10.3         first = w
#      → 10.3     elif len(w) > len(second):
#      → 10.3         second = w
#      → 10.4 return second
#   11. words = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(second_largest_element(...))
#      → calls second_largest_element(words) with words='world'
#      → 14.1 if len(words) < 2:
#      → 14.1     return None
#      → 14.2 first = second = ''
#      → 14.3 for w in words:
#      → 14.3     if len(w) > len(first):
#      → 14.3         second = first
#      → 14.3         first = w
#      → 14.3     elif len(w) > len(second):
#      → 14.3         second = w
#      → 14.4 return second
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_largest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     second_largest_element() → jumps into second_largest_element(), runs body, returns
#     second_largest_element() → jumps into second_largest_element(), runs body, returns
#     second_largest_element() → jumps into second_largest_element(), runs body, returns
#     print(second_largest_element(...)) → second_largest_element() runs first → value passed to print()
#     print(second_largest_element(...)) → second_largest_element() runs first → value passed to print()
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(second_largest_element(...)) → second_largest_element() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 196:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_three(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: > and
# Built-in functions: list, max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_three(three_numbers) runs first → then print() outputs the result
#   max_of_three(?) runs first → then print() outputs the result
#   max_of_three(numbers) → each argument is evaluated first, then the function body runs
#   max_of_three(three_numbers) → each argument is evaluated first, then the function body runs
#   max_of_three(?) → each argument is evaluated first, then the function body runs
#   ? > ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_three(numbers) → stored in memory, not executed
#   2. three_numbers = ... → assigns value to variable
#   3. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=three_numbers
#      → 3.1 return result
#   4. def max_of_three(numbers) → stored in memory, not executed
#   5. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=[567, 56, 6]
#      → 5.1 return result
#   6. def max_of_three(numbers) → stored in memory, not executed
#   7. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=[567, 56, 6]
#      → 7.1 return result
#   8. def max_of_three(numbers) → stored in memory, not executed
#   9. def max_of_three(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_of_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     three_numbers = ... → stored in memory
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()

"""

"""
Problem 197:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_three(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_three(three_numbers) runs first → then print() outputs the result
#   max_of_three(?) runs first → then print() outputs the result
#   max_of_three(numbers) → each argument is evaluated first, then the function body runs
#   ? > max_number → evaluated as True or False (determines which branch runs)
#   max_of_three(three_numbers) → each argument is evaluated first, then the function body runs
#   max_of_three(?) → each argument is evaluated first, then the function body runs
#   n > max_val → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_three(numbers) → stored in memory, not executed
#   2. three_numbers = ... → assigns value to variable
#   3. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=three_numbers
#      → 3.1 return max_number
#   4. def max_of_three(numbers) → stored in memory, not executed
#   5. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=[567, 56, 6]
#      → 5.1 return max_number
#   6. def max_of_three(numbers) → stored in memory, not executed
#   7. def max_of_three(numbers) → stored in memory, not executed
#   8. numbers = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(max_of_three(...))
#      → calls max_of_three(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 11.1 return max_number
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_of_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     three_numbers = ... → stored in memory
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 198:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def centimeters_inches() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
# Built-in functions: input, int, round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   centimeters_inches() → each argument is evaluated first, then the function body runs
#   centimeters_inches(50) runs first → then print() outputs the result
#   cm_to_inches(cm) → each argument is evaluated first, then the function body runs
#   cm_to_inches(25) runs first → then print() outputs the result
#   cm * 0.3937 → Python looks up cm, then 0.3937, then applies *
#   centimeters_inches(50) → each argument is evaluated first, then the function body runs
#   cm_to_inches(25) → each argument is evaluated first, then the function body runs
#   cm_to_inches(100) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def centimeters_inches() → stored in memory, not executed
#   2. centimeters_inches() → calls centimeters_inches() with 
#      → 2.1 cm = int(input('type centimeters: '))
#      → 2.2 inches = cm * 0.3937
#      → 2.3 result = f'{cm} cm = {inches} inches'
#      → 2.4 return result
#   3. def cm_to_inches(cm) → stored in memory, not executed
#   4. print(?)
#   5. def centimeters_inches(cm) → stored in memory, not executed
#   6. print(centimeters_inches(...))
#      → calls centimeters_inches() with 
#      → 6.1 cm = int(input('type centimeters: '))
#      → 6.2 inches = cm * 0.3937
#      → 6.3 result = f'{cm} cm = {inches} inches'
#      → 6.4 return result
#   7. def centimeters_inches() → stored in memory, not executed
#   8. def centimeters_inches() → stored in memory, not executed
#   9. cm = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(cm_to_inches(...))
#      → calls cm_to_inches(cm) with cm=25
#      → 12.1 return cm * 0.3937
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (cm_to_inches, centimeters_inches) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     centimeters_inches() → jumps into centimeters_inches(), runs body, returns
#     print(?)
#     print(centimeters_inches(...)) → centimeters_inches() runs first → value passed to print()
#     cm = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(cm_to_inches(...)) → cm_to_inches() runs first → value passed to print()

"""

"""
Problem 199:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min_max() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: input, int, max, min, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min_max() → each argument is evaluated first, then the function body runs
#   find_min_max(?) runs first → then print() outputs the result
#   find_min_max(nums) → each argument is evaluated first, then the function body runs
#   find_min_max(?) → each argument is evaluated first, then the function body runs
#   number < min_number → evaluated as True or False (determines which branch runs)
#   number > max_number → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min_max() → stored in memory, not executed
#   2. find_min_max() → calls find_min_max() with 
#      → 2.1 user = input('type list of numbers: ').split()
#      → 2.2 int_num = []
#      → 2.3 for number in user:
#      → 2.3     int_num.append(int(number))
#      → 2.4 min_num = min(int_num)
#      → 2.5 max_num = max(int_num)
#      → 2.6 result = (min_num, max_num)
#      → 2.7 return result
#   3. def find_min_max() → stored in memory, not executed
#   4. find_min_max() → calls find_min_max() with 
#      → 4.1 user = input('type list of numbers: ').split()
#      → 4.2 int_num = []
#      → 4.3 for number in user:
#      → 4.3     int_num.append(int(number))
#      → 4.4 min_num = min(int_num)
#      → 4.5 max_num = max(int_num)
#      → 4.6 result = (min_num, max_num)
#      → 4.7 return result
#   5. def find_min_max(nums) → stored in memory, not executed
#   6. print(find_min_max(...))
#      → calls find_min_max() with 
#      → 6.1 user = input('type list of numbers: ').split()
#      → 6.2 int_num = []
#      → 6.3 for number in user:
#      → 6.3     int_num.append(int(number))
#      → 6.4 min_num = min(int_num)
#      → 6.5 max_num = max(int_num)
#      → 6.6 result = (min_num, max_num)
#      → 6.7 return result
#   7. def find_min_max(nums) → stored in memory, not executed
#   8. print(find_min_max(...))
#      → calls find_min_max() with 
#      → 8.1 user = input('type list of numbers: ').split()
#      → 8.2 int_num = []
#      → 8.3 for number in user:
#      → 8.3     int_num.append(int(number))
#      → 8.4 min_num = min(int_num)
#      → 8.5 max_num = max(int_num)
#      → 8.6 result = (min_num, max_num)
#      → 8.7 return result
#   9. def find_min_max(nums) → stored in memory, not executed
#   10. print(find_min_max(...))
#      → calls find_min_max() with 
#      → 10.1 user = input('type list of numbers: ').split()
#      → 10.2 int_num = []
#      → 10.3 for number in user:
#      → 10.3     int_num.append(int(number))
#      → 10.4 min_num = min(int_num)
#      → 10.5 max_num = max(int_num)
#      → 10.6 result = (min_num, max_num)
#      → 10.7 return result
#   11. def find_min_max() → stored in memory, not executed
#   12. def find_min_max() → stored in memory, not executed
#   13. nums = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(find_min_max(...))
#      → calls find_min_max() with 
#      → 16.1 user = input('type list of numbers: ').split()
#      → 16.2 int_num = []
#      → 16.3 for number in user:
#      → 16.3     int_num.append(int(number))
#      → 16.4 min_num = min(int_num)
#      → 16.5 max_num = max(int_num)
#      → 16.6 result = (min_num, max_num)
#      → 16.7 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_min_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     find_min_max() → jumps into find_min_max(), runs body, returns
#     find_min_max() → jumps into find_min_max(), runs body, returns
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min_max(...)) → find_min_max() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 200:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + > in and not in
# Built-in functions: Counter, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common_numbers(lst) runs first → then print() outputs the result
#   common_numbers(?) runs first → then print() outputs the result
#   common_numbers(lst) → each argument is evaluated first, then the function body runs
#   common_numbers(?) → each argument is evaluated first, then the function body runs
#   count.get(...) + 1 → Python looks up count.get(...), then 1, then applies +
#   number in seen → evaluated as True or False (determines which branch runs)
#   number not in common → evaluated as True or False (determines which branch runs)
#   v > 1 → evaluated as True or False (determines which branch runs)
#   lst.count(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common_numbers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=lst
#      → 3.1 return list(map(lambda number: number, lst))
#   4. def common_numbers(lst) → stored in memory, not executed
#   5. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[43, 2, 45, 567, 666, 2, 45]
#      → 5.1 return list(map(lambda number: number, lst))
#   6. def common_numbers(lst) → stored in memory, not executed
#   7. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[43, 2, 45, 567, 666, 2, 45]
#      → 7.1 return list(map(lambda number: number, lst))
#   8. def common_numbers(lst) → stored in memory, not executed
#   9. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[1, 2, 2, 3, 4, 4, 5]
#      → 9.1 return list(map(lambda number: number, lst))
#   10. def common_numbers(lst) → stored in memory, not executed
#   11. def common_numbers(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return list(map(lambda number: number, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 201:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def common_num(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + > in
# Built-in functions: Counter, list, map, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   common_num(lst) runs first → then print() outputs the result
#   common_numbers(lst) runs first → then print() outputs the result
#   common_numbers(?) runs first → then print() outputs the result
#   common_num(lst) → each argument is evaluated first, then the function body runs
#   common_num(?) runs first → then print() outputs the result
#   common_numbers(lst) → each argument is evaluated first, then the function body runs
#   common_numbers(?) → each argument is evaluated first, then the function body runs
#   common_num(?) → each argument is evaluated first, then the function body runs
#   number in unique → evaluated as True or False (determines which branch runs)
#   number in seen → evaluated as True or False (determines which branch runs)
#   count.get(...) + 1 → Python looks up count.get(...), then 1, then applies +
#   v > 1 → evaluated as True or False (determines which branch runs)
#   lst.count(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def common_num(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(common_num(...))
#      → calls common_num(lst) with lst=lst
#      → 3.1 return list(map(lambda number: number, lst))
#   4. def common_numbers(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=lst
#      → 6.1 from collections import Counter
#      → 6.2 return [k for k, v in Counter(lst).items() if v > 1]
#   7. def common_numbers(lst) → stored in memory, not executed
#   8. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[1, 2, 2, 3, 3, 3, 4]
#      → 8.1 from collections import Counter
#      → 8.2 return [k for k, v in Counter(lst).items() if v > 1]
#   9. def common_numbers(lst) → stored in memory, not executed
#   10. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[1, 2, 2, 3, 4, 4]
#      → 10.1 from collections import Counter
#      → 10.2 return [k for k, v in Counter(lst).items() if v > 1]
#   11. def common_numbers(lst) → stored in memory, not executed
#   12. print(common_numbers(...))
#      → calls common_numbers(lst) with lst=[5, 5, 6, 7, 7, 8]
#      → 12.1 from collections import Counter
#      → 12.2 return [k for k, v in Counter(lst).items() if v > 1]
#   13. def common_num(lst) → stored in memory, not executed
#   14. def common_num(lst) → stored in memory, not executed
#   15. lst = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(common_num(...))
#      → calls common_num(lst) with lst=[1, 3, 5, 7]
#      → 18.1 return list(map(lambda number: number, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (common_num, common_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(common_num(...)) → common_num() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     print(common_numbers(...)) → common_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(common_num(...)) → common_num() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 202:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def word_frequency(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + in
# Built-in functions: Counter, dict, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   word_frequency(string) runs first → then print() outputs the result
#   word_frequency('hello world') runs first → then print() outputs the result
#   word_frequency('aabbc') runs first → then print() outputs the result
#   word_frequency(string) → each argument is evaluated first, then the function body runs
#   word_frequency('world') runs first → then print() outputs the result
#   word_frequency('hello world') → each argument is evaluated first, then the function body runs
#   word_frequency('aabbc') → each argument is evaluated first, then the function body runs
#   word_frequency('world') → each argument is evaluated first, then the function body runs
#   word in result → evaluated as True or False (determines which branch runs)
#   result.get(...) + 1 → Python looks up result.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def word_frequency(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(word_frequency(...))
#      → calls word_frequency(string) with string=string
#      → 3.1 return result
#   4. def word_frequency(string) → stored in memory, not executed
#   5. print(word_frequency(...))
#      → calls word_frequency(string) with string='hello world'
#      → 5.1 return result
#   6. def word_frequency(string) → stored in memory, not executed
#   7. print(word_frequency(...))
#      → calls word_frequency(string) with string='aabbc'
#      → 7.1 return result
#   8. def word_frequency(string) → stored in memory, not executed
#   9. def word_frequency(string) → stored in memory, not executed
#   10. string = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(word_frequency(...))
#      → calls word_frequency(string) with string='world'
#      → 13.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (word_frequency) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(word_frequency(...)) → word_frequency() runs first → value passed to print()
#
#   Loop: takes one item from sort_char each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 203:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def total(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   total(lst_numbers) runs first → then print() outputs the result
#   total(?) runs first → then print() outputs the result
#   total(numbers) → each argument is evaluated first, then the function body runs
#   total(lst_numbers) → each argument is evaluated first, then the function body runs
#   total(?) → each argument is evaluated first, then the function body runs
#   a * b → Python looks up a, then b, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def total(numbers) → stored in memory, not executed
#   2. lst_numbers = ... → assigns value to variable
#   3. print(total(...))
#      → calls total(numbers) with numbers=lst_numbers
#      → 3.1 return (total_numbers, multiply_numbers)
#   4. def total(numbers) → stored in memory, not executed
#   5. print(total(...))
#      → calls total(numbers) with numbers=[1, 2, 3, 4]
#      → 5.1 return (total_numbers, multiply_numbers)
#   6. def total(numbers) → stored in memory, not executed
#   7. print(total(...))
#      → calls total(numbers) with numbers=[1, 2, 3, 4]
#      → 7.1 return (total_numbers, multiply_numbers)
#   8. def total(numbers) → stored in memory, not executed
#   9. def total(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(total(...))
#      → calls total(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 13.1 return (total_numbers, multiply_numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (total) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst_numbers = ... → stored in memory
#     print(total(...)) → total() runs first → value passed to print()
#     print(total(...)) → total() runs first → value passed to print()
#     print(total(...)) → total() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(total(...)) → total() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 204:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_integers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_integers(lst) runs first → then print() outputs the result
#   list_integers(?) runs first → then print() outputs the result
#   list_integers(lst) → each argument is evaluated first, then the function body runs
#   list_integers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_integers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_integers(...))
#      → calls list_integers(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def list_integers(lst) → stored in memory, not executed
#   5. print(list_integers(...))
#      → calls list_integers(lst) with lst=['join', 'Jonathan', 'tree']
#      → 5.1 return list(map(lambda word: word, lst))
#   6. def list_integers(lst) → stored in memory, not executed
#   7. print(list_integers(...))
#      → calls list_integers(lst) with lst=['hi', 'there']
#      → 7.1 return list(map(lambda word: word, lst))
#   8. def list_integers(lst) → stored in memory, not executed
#   9. def list_integers(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(list_integers(...))
#      → calls list_integers(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_integers(...)) → list_integers() runs first → value passed to print()
#     print(list_integers(...)) → list_integers() runs first → value passed to print()
#     print(list_integers(...)) → list_integers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_integers(...)) → list_integers() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 205:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average(lst) runs first → then print() outputs the result
#   average(?) runs first → then print() outputs the result
#   average(lst) → each argument is evaluated first, then the function body runs
#   sum_lst / length → Python looks up sum_lst, then length, then applies /
#   average(?) → each argument is evaluated first, then the function body runs
#   sum(...) / len(...) → Python looks up sum(...), then len(...), then applies /
#   sum_lst * ? → Python looks up sum_lst, then ?, then applies *
#   1 / length → Python looks up 1, then length, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(average(...))
#      → calls average(lst) with lst=lst
#      → 3.1 from fractions import Fraction
#      → 3.2 return float(Fraction(sum_lst, length))
#   4. def average(lst) → stored in memory, not executed
#   5. print(average(...))
#      → calls average(lst) with lst=[2666, 566, 6777]
#      → 5.1 from fractions import Fraction
#      → 5.2 return float(Fraction(sum_lst, length))
#   6. def average(lst) → stored in memory, not executed
#   7. print(average(...))
#      → calls average(lst) with lst=[1, 2, 3, 4]
#      → 7.1 from fractions import Fraction
#      → 7.2 return float(Fraction(sum_lst, length))
#   8. def average(lst) → stored in memory, not executed
#   9. def average(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(average(...))
#      → calls average(lst) with lst=[1, 3, 5, 7]
#      → 13.1 from fractions import Fraction
#      → 13.2 return float(Fraction(sum_lst, length))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(average(...)) → average() runs first → value passed to print()
#     print(average(...)) → average() runs first → value passed to print()
#     print(average(...)) → average() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average(...)) → average() runs first → value passed to print()

"""

"""
Problem 206:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_three() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > !=
# Built-in functions: input, int, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_three() → each argument is evaluated first, then the function body runs
#   len(...) != 3 → evaluated as True or False (determines which branch runs)
#   number > largest → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_three() → stored in memory, not executed
#   2. max_of_three() → calls max_of_three() with 
#      → 2.1 numbers = [int(value) for value in input('Enter three numbers: ').split()]
#      → 2.2 if len(numbers) != 3:
#      → 2.2     print('not enough numbers')
#      → 2.2     return
#      → 2.3 largest = numbers[0]
#      → 2.4 for number in numbers[1:]:
#      → 2.4     if number > largest:
#      → 2.4         largest = number
#      → 2.5 print(largest)
#   3. numbers = ... → assigns value to variable
#   4. if ... → only one branch executes
#   5. largest = ... → assigns value to variable
#   6. for number in ? → loops over each item
#   7. print(largest)
#   8. numbers = ... → assigns value to variable
#   9. if ... → only one branch executes
#   10. largest = ... → assigns value to variable
#   11. for number in ? → loops over each item
#   12. print(largest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_of_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     max_of_three() → jumps into max_of_three(), runs body, returns
#     numbers = ... → stored in memory
#     if → evaluates condition, runs matching branch
#     largest = ... → stored in memory
#     for number in ? → runs body once per item
#     print(largest)
#     numbers = ... → stored in memory
#     if → evaluates condition, runs matching branch
#     largest = ... → stored in memory
#     for number in ? → runs body once per item
#     print(largest)
#
#   Loop: takes one item from ? each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 207:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_list() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > !=
# Built-in functions: input, int, len, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_list() → each argument is evaluated first, then the function body runs
#   max_of_list(?) runs first → then print() outputs the result
#   max_of_list(nums) → each argument is evaluated first, then the function body runs
#   len(...) != 3 → evaluated as True or False (determines which branch runs)
#   max_of_list(?) → each argument is evaluated first, then the function body runs
#   number > max_number → evaluated as True or False (determines which branch runs)
#   a > b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_list() → stored in memory, not executed
#   2. max_of_list() → calls max_of_list(nums) with 
#      → 2.1 from functools import reduce
#      → 2.2 return reduce(lambda a, b: a if a > b else b, nums)
#   3. def max_of_list(nums) → stored in memory, not executed
#   4. print(max_of_list(...))
#      → calls max_of_list(nums) with nums=[5, 2, 9, 1, 7]
#      → 4.1 from functools import reduce
#      → 4.2 return reduce(lambda a, b: a if a > b else b, nums)
#   5. def max_of_list(nums) → stored in memory, not executed
#   6. print(max_of_list(...))
#      → calls max_of_list(nums) with nums=[3, 8, 1, 6]
#      → 6.1 from functools import reduce
#      → 6.2 return reduce(lambda a, b: a if a > b else b, nums)
#   7. nums = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(max_of_list(...))
#      → calls max_of_list(nums) with nums=[4, 1, 8, 6, 3]
#      → 10.1 from functools import reduce
#      → 10.2 return reduce(lambda a, b: a if a > b else b, nums)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     max_of_list() → jumps into max_of_list(), runs body, returns
#     print(max_of_list(...)) → max_of_list() runs first → value passed to print()
#     print(max_of_list(...)) → max_of_list() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_of_list(...)) → max_of_list() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 208:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_list() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: input, int, max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_list() → each argument is evaluated first, then the function body runs
#   max_of_list(?) runs first → then print() outputs the result
#   max_of_list(nums) → each argument is evaluated first, then the function body runs
#   max_of_list(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_list() → stored in memory, not executed
#   2. max_of_list() → calls max_of_list() with 
#      → 2.1 user = input('type three numbers: ').split()
#      → 2.2 int_numbers = []
#      → 2.3 for number in user:
#      → 2.3     int_numbers.append(int(number))
#      → 2.4 max_number = max(int_numbers)
#      → 2.5 result = max_number
#      → 2.6 return result
#   3. def max_of_list(nums) → stored in memory, not executed
#   4. print(max_of_list(...))
#      → calls max_of_list() with 
#      → 4.1 user = input('type three numbers: ').split()
#      → 4.2 int_numbers = []
#      → 4.3 for number in user:
#      → 4.3     int_numbers.append(int(number))
#      → 4.4 max_number = max(int_numbers)
#      → 4.5 result = max_number
#      → 4.6 return result
#   5. def max_of_list(nums) → stored in memory, not executed
#   6. print(max_of_list(...))
#      → calls max_of_list() with 
#      → 6.1 user = input('type three numbers: ').split()
#      → 6.2 int_numbers = []
#      → 6.3 for number in user:
#      → 6.3     int_numbers.append(int(number))
#      → 6.4 max_number = max(int_numbers)
#      → 6.5 result = max_number
#      → 6.6 return result
#   7. def max_of_list() → stored in memory, not executed
#   8. def max_of_list() → stored in memory, not executed
#   9. nums = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(max_of_list(...))
#      → calls max_of_list() with 
#      → 12.1 user = input('type three numbers: ').split()
#      → 12.2 int_numbers = []
#      → 12.3 for number in user:
#      → 12.3     int_numbers.append(int(number))
#      → 12.4 max_number = max(int_numbers)
#      → 12.5 result = max_number
#      → 12.6 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     max_of_list() → jumps into max_of_list(), runs body, returns
#     print(max_of_list(...)) → max_of_list() runs first → value passed to print()
#     print(max_of_list(...)) → max_of_list() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_of_list(...)) → max_of_list() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done

"""

"""
Problem 209:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_words(lst) runs first → then print() outputs the result
#   list_words(?) runs first → then print() outputs the result
#   list_words(lst) → each argument is evaluated first, then the function body runs
#   list_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_words(...))
#      → calls list_words(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def list_words(lst) → stored in memory, not executed
#   5. print(list_words(...))
#      → calls list_words(lst) with lst=['Jonathan', 'horse', 'today']
#      → 5.1 return list(map(lambda word: word, lst))
#   6. def list_words(lst) → stored in memory, not executed
#   7. print(list_words(...))
#      → calls list_words(lst) with lst=['cat', 'dog']
#      → 7.1 return list(map(lambda word: word, lst))
#   8. def list_words(lst) → stored in memory, not executed
#   9. def list_words(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(list_words(...))
#      → calls list_words(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_words(...)) → list_words() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 210:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_words(lst) runs first → then print() outputs the result
#   list_words(?) runs first → then print() outputs the result
#   list_words(lst) → each argument is evaluated first, then the function body runs
#   list_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_words(...))
#      → calls list_words(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def list_words(lst) → stored in memory, not executed
#   5. print(list_words(...))
#      → calls list_words(lst) with lst=['Jonathan', 'horse', 'today']
#      → 5.1 return list(map(lambda word: word, lst))
#   6. def list_words(lst) → stored in memory, not executed
#   7. print(list_words(...))
#      → calls list_words(lst) with lst=['hi', 'hello']
#      → 7.1 return list(map(lambda word: word, lst))
#   8. def list_words(lst) → stored in memory, not executed
#   9. def list_words(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(list_words(...))
#      → calls list_words(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_words(...)) → list_words() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 211:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def length_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: enumerate, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   length_words(lst) runs first → then print() outputs the result
#   length_words(?) runs first → then print() outputs the result
#   length_words(lst) → each argument is evaluated first, then the function body runs
#   length_words(?) → each argument is evaluated first, then the function body runs
#   len_manual(w) → each argument is evaluated first, then the function body runs
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def length_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(length_words(...))
#      → calls length_words(lst) with lst=lst
#      → 3.1 return list(map(lambda word: count, lst))
#   4. def length_words(lst) → stored in memory, not executed
#   5. print(length_words(...))
#      → calls length_words(lst) with lst=['Jonathan', 'chris', 'mountain']
#      → 5.1 return list(map(lambda word: count, lst))
#   6. def length_words(lst) → stored in memory, not executed
#   7. print(length_words(...))
#      → calls length_words(lst) with lst=['cat', 'horse']
#      → 7.1 return list(map(lambda word: count, lst))
#   8. def length_words(lst) → stored in memory, not executed
#   9. def len_manual(s) → stored in memory, not executed
#   10. print(length_words(...))
#      → calls length_words(lst) with lst=['apple', 'pear']
#      → 10.1 return list(map(lambda word: count, lst))
#   11. def length_words(lst) → stored in memory, not executed
#   12. def length_words(lst) → stored in memory, not executed
#   13. lst = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(length_words(...))
#      → calls length_words(lst) with lst=[1, 3, 5, 7]
#      → 16.1 return list(map(lambda word: count, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (len_manual, length_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(length_words(...)) → length_words() runs first → value passed to print()
#     print(length_words(...)) → length_words() runs first → value passed to print()
#     print(length_words(...)) → length_words() runs first → value passed to print()
#     print(length_words(...)) → length_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(length_words(...)) → length_words() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 212:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def intersection() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: filter, input, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   intersection() → each argument is evaluated first, then the function body runs
#   intersection(?, ?) runs first → then print() outputs the result
#   intersection(lst1, lst2) → each argument is evaluated first, then the function body runs
#   intersection(?, ?) → each argument is evaluated first, then the function body runs
#   word in lst2 → evaluated as True or False (determines which branch runs)
#   x in lst2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def intersection() → stored in memory, not executed
#   2. intersection() → calls intersection() with 
#      → 2.1 lst1 = input('type lst1: ').split()
#      → 2.2 lst2 = input('type lst2: ').split()
#      → 2.3 common = []
#      → 2.4 for word in lst1:
#      → 2.4     if word in lst2:
#      → 2.4         common.append(word)
#      → 2.5 result = common
#      → 2.6 return result
#   3. def intersection(lst1, lst2) → stored in memory, not executed
#   4. print(intersection(...))
#      → calls intersection() with 
#      → 4.1 lst1 = input('type lst1: ').split()
#      → 4.2 lst2 = input('type lst2: ').split()
#      → 4.3 common = []
#      → 4.4 for word in lst1:
#      → 4.4     if word in lst2:
#      → 4.4         common.append(word)
#      → 4.5 result = common
#      → 4.6 return result
#   5. def intersection(lst1, lst2) → stored in memory, not executed
#   6. print(intersection(...))
#      → calls intersection() with 
#      → 6.1 lst1 = input('type lst1: ').split()
#      → 6.2 lst2 = input('type lst2: ').split()
#      → 6.3 common = []
#      → 6.4 for word in lst1:
#      → 6.4     if word in lst2:
#      → 6.4         common.append(word)
#      → 6.5 result = common
#      → 6.6 return result
#   7. def intersection() → stored in memory, not executed
#   8. def intersection() → stored in memory, not executed
#   9. lst1 = ... → assigns value to variable
#   10. lst2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(intersection(...))
#      → calls intersection() with 
#      → 13.1 lst1 = input('type lst1: ').split()
#      → 13.2 lst2 = input('type lst2: ').split()
#      → 13.3 common = []
#      → 13.4 for word in lst1:
#      → 13.4     if word in lst2:
#      → 13.4         common.append(word)
#      → 13.5 result = common
#      → 13.6 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (intersection) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     intersection() → jumps into intersection(), runs body, returns
#     print(intersection(...)) → intersection() runs first → value passed to print()
#     print(intersection(...)) → intersection() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(intersection(...)) → intersection() runs first → value passed to print()
#
#   Loop: takes one item from lst1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 213:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: *
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   '* ' * i → Python looks up '* ', then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.1 print('* ' * i)
#   2. for i in range(...) → loops over each item
#      → 2.2 print()
#   3. for i in range(...) → loops over each item
#      → 3.1 print(' '.join(...))
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. for i in range(...) → loops over each item
#      → 6.1 print('* ' * i)
#   7. for i in range(...) → loops over each item
#      → 7.1 print('* ' * i)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 214:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Operators: * +
# Built-in functions: range, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? * i → Python looks up ?, then i, then applies *
#   str(...) + ' ' → Python looks up str(...), then ' ', then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.1 print(? * i)
#   2. for i in range(...) → loops over each item
#      → 2.2 print()
#   3. for i in range(...) → loops over each item
#      → 3.1 print(' '.join(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 216:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: for
# Operators: +
# Built-in functions: map, range, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.1 print(' '.join(...))
#   2. for i in range(...) → loops over each item
#      → 2.2 print()
#   3. for i in range(...) → loops over each item
#      → 3.1 print(' '.join(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 217:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: *
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   '* ' * i → Python looks up '* ', then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. for i in range(...) → loops over each item
#      → 1.2 print()
#   2. for i in range(...) → loops over each item
#      → 2.1 print('* ' * i)
#   3. for i in range(...) → loops over each item
#      → 3.1 print(' '.join(...))
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. for i in range(...) → loops over each item
#      → 6.2 print()
#   7. for i in range(...) → loops over each item
#      → 7.2 print()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 218:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_checkerboard(size) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % * + ==
# Built-in functions: input, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_checkerboard(size) → each argument is evaluated first, then the function body runs
#   print_checkerboard(4) → each argument is evaluated first, then the function body runs
#   print_checkerboard(25) runs first → then print() outputs the result
#   print_checkerboard(size_2) → each argument is evaluated first, then the function body runs
#   print_checkerboard(25) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   i % 2 → Python looks up i, then 2, then applies %
#   ? % 2 → Python looks up ?, then 2, then applies %
#   '* ' * size → Python looks up '* ', then size, then applies *
#   ' *' * size → Python looks up ' *', then size, then applies *
#   i + j → Python looks up i, then j, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_checkerboard(size) → stored in memory, not executed
#   2. size = ... → assigns value to variable
#   3. print_checkerboard(size) → calls print_checkerboard(size) with size=size
#      → 3.1 for i in range(size):
#      → 3.1     if i % 2 == 0:
#      → 3.1         print('* ' * size)
#      → 3.1     else:
#      → 3.1         print(' *' * size)
#   4. def print_checkerboard(size) → stored in memory, not executed
#   5. print_checkerboard(4) → calls print_checkerboard(size) with size=4
#      → 5.1 for i in range(size):
#      → 5.1     if i % 2 == 0:
#      → 5.1         print('* ' * size)
#      → 5.1     else:
#      → 5.1         print(' *' * size)
#   6. def print_checkerboard(size) → stored in memory, not executed
#   7. print_checkerboard(4) → calls print_checkerboard(size) with size=4
#      → 7.1 for i in range(size):
#      → 7.1     if i % 2 == 0:
#      → 7.1         print('* ' * size)
#      → 7.1     else:
#      → 7.1         print(' *' * size)
#   8. def solve(enter) → stored in memory, not executed
#   9. def solve(enter) → stored in memory, not executed
#   10. size = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(print_checkerboard(...))
#      → calls print_checkerboard(size) with size=25
#      → 13.1 for i in range(size):
#      → 13.1     if i % 2 == 0:
#      → 13.1         print('* ' * size)
#      → 13.1     else:
#      → 13.1         print(' *' * size)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_checkerboard) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     size = ... → stored in memory
#     print_checkerboard(size) → jumps into print_checkerboard(), runs body, returns
#     print_checkerboard(4) → jumps into print_checkerboard(), runs body, returns
#     print_checkerboard(4) → jumps into print_checkerboard(), runs body, returns
#     size = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_checkerboard(...)) → print_checkerboard() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 219:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_floyds_triangle(rows) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | while
# Operators: + <=
# Built-in functions: range, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_floyds_triangle(rows) → each argument is evaluated first, then the function body runs
#   print_floyds_triangle(4) → each argument is evaluated first, then the function body runs
#   print_floyds_triangle(25) runs first → then print() outputs the result
#   r <= rows → evaluated as True or False (determines which branch runs)
#   print_floyds_triangle(rows_2) → each argument is evaluated first, then the function body runs
#   print_floyds_triangle(25) → each argument is evaluated first, then the function body runs
#   rows + 1 → Python looks up rows, then 1, then applies +
#   num + c → Python looks up num, then c, then applies +
#   num + j → Python looks up num, then j, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_floyds_triangle(rows) → stored in memory, not executed
#   2. rows = ... → assigns value to variable
#   3. print_floyds_triangle(rows) → calls print_floyds_triangle(rows) with rows=rows
#      → 3.1 num = 1
#      → 3.2 for i in range(1, rows + 1):
#      → 3.2     print(' '.join((str(num + j) for j in range(i))))
#      → 3.2     num += i
#   4. def print_floyds_triangle(rows) → stored in memory, not executed
#   5. print_floyds_triangle(4) → calls print_floyds_triangle(rows) with rows=4
#      → 5.1 num = 1
#      → 5.2 for i in range(1, rows + 1):
#      → 5.2     print(' '.join((str(num + j) for j in range(i))))
#      → 5.2     num += i
#   6. def print_floyds_triangle(rows) → stored in memory, not executed
#   7. print_floyds_triangle(4) → calls print_floyds_triangle(rows) with rows=4
#      → 7.1 num = 1
#      → 7.2 for i in range(1, rows + 1):
#      → 7.2     print(' '.join((str(num + j) for j in range(i))))
#      → 7.2     num += i
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. rows = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(print_floyds_triangle(...))
#      → calls print_floyds_triangle(rows) with rows=25
#      → 13.1 num = 1
#      → 13.2 for i in range(1, rows + 1):
#      → 13.2     print(' '.join((str(num + j) for j in range(i))))
#      → 13.2     num += i
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_floyds_triangle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     rows = ... → stored in memory
#     print_floyds_triangle(rows) → jumps into print_floyds_triangle(), runs body, returns
#     print_floyds_triangle(4) → jumps into print_floyds_triangle(), runs body, returns
#     print_floyds_triangle(4) → jumps into print_floyds_triangle(), runs body, returns
#     rows = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_floyds_triangle(...)) → print_floyds_triangle() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 222:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_reverse_pyramid(rows) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: +
# Built-in functions: map, range, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_reverse_pyramid(rows) → each argument is evaluated first, then the function body runs
#   print_reverse_pyramid(5) → each argument is evaluated first, then the function body runs
#   print_reverse_pyramid(25) runs first → then print() outputs the result
#   print_reverse_pyramid(rows_2) → each argument is evaluated first, then the function body runs
#   print_reverse_pyramid(25) → each argument is evaluated first, then the function body runs
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_reverse_pyramid(rows) → stored in memory, not executed
#   2. rows = ... → assigns value to variable
#   3. print_reverse_pyramid(rows) → calls print_reverse_pyramid(rows) with rows=rows
#      → 3.1 for i in range(rows, 0, -1):
#      → 3.1     print(' '.join((str(j) for j in range(1, i + 1))))
#   4. def print_reverse_pyramid(rows) → stored in memory, not executed
#   5. print_reverse_pyramid(5) → calls print_reverse_pyramid(rows) with rows=5
#      → 5.1 for i in range(rows, 0, -1):
#      → 5.1     print(' '.join((str(j) for j in range(1, i + 1))))
#   6. def print_reverse_pyramid(rows) → stored in memory, not executed
#   7. print_reverse_pyramid(5) → calls print_reverse_pyramid(rows) with rows=5
#      → 7.1 for i in range(rows, 0, -1):
#      → 7.1     print(' '.join((str(j) for j in range(1, i + 1))))
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. rows = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(print_reverse_pyramid(...))
#      → calls print_reverse_pyramid(rows) with rows=25
#      → 13.1 for i in range(rows, 0, -1):
#      → 13.1     print(' '.join((str(j) for j in range(1, i + 1))))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_reverse_pyramid) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     rows = ... → stored in memory
#     print_reverse_pyramid(rows) → jumps into print_reverse_pyramid(), runs body, returns
#     print_reverse_pyramid(5) → jumps into print_reverse_pyramid(), runs body, returns
#     print_reverse_pyramid(5) → jumps into print_reverse_pyramid(), runs body, returns
#     rows = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_reverse_pyramid(...)) → print_reverse_pyramid() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 227:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_zigzag(rows) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % * + - // == or and
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_zigzag(rows) → each argument is evaluated first, then the function body runs
#   print_zigzag(5) → each argument is evaluated first, then the function body runs
#   print_zigzag(25) runs first → then print() outputs the result
#   print_zigzag(rows_2) → each argument is evaluated first, then the function body runs
#   print_zigzag(25) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   i % 2 → Python looks up i, then 2, then applies %
#   '*   ' * ? → Python looks up '*   ', then ?, then applies *
#   ' * *' * ? → Python looks up ' * *', then ?, then applies *
#   ? == 1 → evaluated as True or False (determines which branch runs)
#   ? + 1 → Python looks up ?, then 1, then applies +
#   j % ? → Python looks up j, then ?, then applies %
#   rows // 2 → Python looks up rows, then 2, then applies //
#   rows - 1 → Python looks up rows, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_zigzag(rows) → stored in memory, not executed
#   2. rows = ... → assigns value to variable
#   3. print_zigzag(rows) → calls print_zigzag(rows) with rows=rows
#      → 3.1 for i in range(rows):
#      → 3.1     if i % 2 == 0:
#      → 3.1         print('*   ' * (rows // 2 + 1))
#      → 3.1     else:
#      → 3.1         print(' * *' * (rows // 2 + 1))
#   4. def print_zigzag(rows) → stored in memory, not executed
#   5. print_zigzag(5) → calls print_zigzag(rows) with rows=5
#      → 5.1 for i in range(rows):
#      → 5.1     if i % 2 == 0:
#      → 5.1         print('*   ' * (rows // 2 + 1))
#      → 5.1     else:
#      → 5.1         print(' * *' * (rows // 2 + 1))
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. rows = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(print_zigzag(...))
#      → calls print_zigzag(rows) with rows=25
#      → 11.1 for i in range(rows):
#      → 11.1     if i % 2 == 0:
#      → 11.1         print('*   ' * (rows // 2 + 1))
#      → 11.1     else:
#      → 11.1         print(' * *' * (rows // 2 + 1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_zigzag, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     rows = ... → stored in memory
#     print_zigzag(rows) → jumps into print_zigzag(), runs body, returns
#     print_zigzag(5) → jumps into print_zigzag(), runs body, returns
#     rows = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_zigzag(...)) → print_zigzag() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 228:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_cross(rows) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: * + - // == or
# Built-in functions: abs, min, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_cross(rows) → each argument is evaluated first, then the function body runs
#   print_cross(5) → each argument is evaluated first, then the function body runs
#   print_cross(25) runs first → then print() outputs the result
#   print_cross(rows_2) → each argument is evaluated first, then the function body runs
#   print_cross(25) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   j == i → evaluated as True or False (determines which branch runs)
#   j == ? → evaluated as True or False (determines which branch runs)
#   ? - i → Python looks up ?, then i, then applies -
#   ? + '*' → Python looks up ?, then '*', then applies +
#   ' ' * ? → Python looks up ' ', then ?, then applies *
#   i == ? → evaluated as True or False (determines which branch runs)
#   rows - 1 → Python looks up rows, then 1, then applies -
#   ' ' * min(...) → Python looks up ' ', then min(...), then applies *
#   ? - 1 → Python looks up ?, then 1, then applies -
#   rows // 2 → Python looks up rows, then 2, then applies //
#   abs(...) * 2 → Python looks up abs(...), then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_cross(rows) → stored in memory, not executed
#   2. rows = ... → assigns value to variable
#   3. print_cross(rows) → calls print_cross(rows) with rows=rows
#      → 3.1 for i in range(rows):
#      → 3.1     print(''.join(('*' if j == i or j == rows - 1 - i else ' ' for j in range(rows))))
#   4. def print_cross(rows) → stored in memory, not executed
#   5. print_cross(5) → calls print_cross(rows) with rows=5
#      → 5.1 for i in range(rows):
#      → 5.1     print(''.join(('*' if j == i or j == rows - 1 - i else ' ' for j in range(rows))))
#   6. def print_cross(rows) → stored in memory, not executed
#   7. print_cross(5) → calls print_cross(rows) with rows=5
#      → 7.1 for i in range(rows):
#      → 7.1     print(''.join(('*' if j == i or j == rows - 1 - i else ' ' for j in range(rows))))
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. rows = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(print_cross(...))
#      → calls print_cross(rows) with rows=25
#      → 13.1 for i in range(rows):
#      → 13.1     print(''.join(('*' if j == i or j == rows - 1 - i else ' ' for j in range(rows))))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_cross) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     rows = ... → stored in memory
#     print_cross(rows) → jumps into print_cross(), runs body, returns
#     print_cross(5) → jumps into print_cross(), runs body, returns
#     print_cross(5) → jumps into print_cross(), runs body, returns
#     rows = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_cross(...)) → print_cross() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 230:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_occurrences(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + in
# Built-in functions: Counter, dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_occurrences(lst) runs first → then print() outputs the result
#   count_occurrences(?) runs first → then print() outputs the result
#   count_occurrences(lst) → each argument is evaluated first, then the function body runs
#   count_occurrences(?) → each argument is evaluated first, then the function body runs
#   word in result → evaluated as True or False (determines which branch runs)
#   result.get(...) + 1 → Python looks up result.get(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_occurrences(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(count_occurrences(...))
#      → calls count_occurrences(lst) with lst=lst
#      → 3.1 return result
#   4. def count_occurrences(lst) → stored in memory, not executed
#   5. print(count_occurrences(...))
#      → calls count_occurrences(lst) with lst=['tom', 'fred', 'tom', 'chris', 'fred']
#      → 5.1 return result
#   6. def count_occurrences(lst) → stored in memory, not executed
#   7. print(count_occurrences(...))
#      → calls count_occurrences(lst) with lst=['a', 'b', 'a', 'c', 'b']
#      → 7.1 return result
#   8. def count_occurrences(lst) → stored in memory, not executed
#   9. print(count_occurrences(...))
#      → calls count_occurrences(lst) with lst=[1, 2, 1, 3, 1, 2]
#      → 9.1 return result
#   10. def count_occurrences(lst) → stored in memory, not executed
#   11. def count_occurrences(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(count_occurrences(...))
#      → calls count_occurrences(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_occurrences(...)) → count_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 231:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_odd() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: filter, input, int, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_odd() → each argument is evaluated first, then the function body runs
#   sum_of_odd(?) runs first → then print() outputs the result
#   sum_of_odd(nums) → each argument is evaluated first, then the function body runs
#   sum_of_odd(?) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#   n % 2 → Python looks up n, then 2, then applies %
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_odd() → stored in memory, not executed
#   2. sum_of_odd() → calls sum_of_odd() with 
#      → 2.1 user = input('type numbers: ').split()
#      → 2.2 count = 0
#      → 2.3 for number in user:
#      → 2.3     number = int(number)
#      → 2.3     if number % 2 != 0:
#      → 2.3         count += number
#      → 2.4 result = count
#      → 2.5 return result
#   3. def sum_of_odd(nums) → stored in memory, not executed
#   4. print(sum_of_odd(...))
#      → calls sum_of_odd() with 
#      → 4.1 user = input('type numbers: ').split()
#      → 4.2 count = 0
#      → 4.3 for number in user:
#      → 4.3     number = int(number)
#      → 4.3     if number % 2 != 0:
#      → 4.3         count += number
#      → 4.4 result = count
#      → 4.5 return result
#   5. def sum_of_odd(nums) → stored in memory, not executed
#   6. print(sum_of_odd(...))
#      → calls sum_of_odd() with 
#      → 6.1 user = input('type numbers: ').split()
#      → 6.2 count = 0
#      → 6.3 for number in user:
#      → 6.3     number = int(number)
#      → 6.3     if number % 2 != 0:
#      → 6.3         count += number
#      → 6.4 result = count
#      → 6.5 return result
#   7. def sum_of_odd(nums) → stored in memory, not executed
#   8. print(sum_of_odd(...))
#      → calls sum_of_odd() with 
#      → 8.1 user = input('type numbers: ').split()
#      → 8.2 count = 0
#      → 8.3 for number in user:
#      → 8.3     number = int(number)
#      → 8.3     if number % 2 != 0:
#      → 8.3         count += number
#      → 8.4 result = count
#      → 8.5 return result
#   9. def sum_of_odd() → stored in memory, not executed
#   10. def sum_of_odd() → stored in memory, not executed
#   11. nums = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(sum_of_odd(...))
#      → calls sum_of_odd() with 
#      → 14.1 user = input('type numbers: ').split()
#      → 14.2 count = 0
#      → 14.3 for number in user:
#      → 14.3     number = int(number)
#      → 14.3     if number % 2 != 0:
#      → 14.3         count += number
#      → 14.4 result = count
#      → 14.5 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sum_of_odd() → jumps into sum_of_odd(), runs body, returns
#     print(sum_of_odd(...)) → sum_of_odd() runs first → value passed to print()
#     print(sum_of_odd(...)) → sum_of_odd() runs first → value passed to print()
#     print(sum_of_odd(...)) → sum_of_odd() runs first → value passed to print()
#     nums = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_odd(...)) → sum_of_odd() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 232:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_vowels() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: filter, input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_vowels() → each argument is evaluated first, then the function body runs
#   remove_vowels('hello world') runs first → then print() outputs the result
#   remove_vowels('banana') runs first → then print() outputs the result
#   remove_vowels('Hello, World!') runs first → then print() outputs the result
#   remove_vowels(s) → each argument is evaluated first, then the function body runs
#   remove_vowels(10) runs first → then print() outputs the result
#   remove_vowels('hello world') → each argument is evaluated first, then the function body runs
#   remove_vowels('banana') → each argument is evaluated first, then the function body runs
#   remove_vowels('Hello, World!') → each argument is evaluated first, then the function body runs
#   remove_vowels(10) → each argument is evaluated first, then the function body runs
#   char not in 'aeiou' → evaluated as True or False (determines which branch runs)
#   c not in 'aeiouAEIOU' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_vowels() → stored in memory, not executed
#   2. remove_vowels() → calls remove_vowels(s) with 
#      → 2.1 import re
#      → 2.2 return re.sub('[aeiouAEIOU]', '', s)
#   3. def remove_vowels(s) → stored in memory, not executed
#   4. print(remove_vowels(...))
#      → calls remove_vowels(s) with s='hello world'
#      → 4.1 import re
#      → 4.2 return re.sub('[aeiouAEIOU]', '', s)
#   5. def remove_vowels(s) → stored in memory, not executed
#   6. print(remove_vowels(...))
#      → calls remove_vowels(s) with s='banana'
#      → 6.1 import re
#      → 6.2 return re.sub('[aeiouAEIOU]', '', s)
#   7. def remove_vowels(s) → stored in memory, not executed
#   8. print(remove_vowels(...))
#      → calls remove_vowels(s) with s='Hello, World!'
#      → 8.1 import re
#      → 8.2 return re.sub('[aeiouAEIOU]', '', s)
#   9. s = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(remove_vowels(...))
#      → calls remove_vowels(s) with s=10
#      → 12.1 import re
#      → 12.2 return re.sub('[aeiouAEIOU]', '', s)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     remove_vowels() → jumps into remove_vowels(), runs body, returns
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_vowels(...)) → remove_vowels() runs first → value passed to print()
#
#   Loop: takes one item from user each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 233:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: <
# Built-in functions: len, reduce, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min(lst) runs first → then print() outputs the result
#   find_min(?) runs first → then print() outputs the result
#   find_min(lst) → each argument is evaluated first, then the function body runs
#   find_min(?) → each argument is evaluated first, then the function body runs
#   i < len(...) → evaluated as True or False (determines which branch runs)
#   number < min_num → evaluated as True or False (determines which branch runs)
#   ? < min_val → evaluated as True or False (determines which branch runs)
#   a < b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_min(...))
#      → calls find_min(lst) with lst=lst
#      → 3.1 return min_num
#   4. def find_min(lst) → stored in memory, not executed
#   5. print(find_min(...))
#      → calls find_min(lst) with lst=[13, 566, 1]
#      → 5.1 return min_num
#   6. def find_min(lst) → stored in memory, not executed
#   7. print(find_min(...))
#      → calls find_min(lst) with lst=[5, 2, 9, 1, 7]
#      → 7.1 return min_num
#   8. def find_min(lst) → stored in memory, not executed
#   9. print(find_min(...))
#      → calls find_min(lst) with lst=[8, 3, 5, 2]
#      → 9.1 return min_num
#   10. def find_min(lst) → stored in memory, not executed
#   11. def find_min(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(find_min(...))
#      → calls find_min(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return min_num
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_min) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min(...)) → find_min() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 234:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_square(n) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: * <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < 6 → evaluated as True or False (determines which branch runs)
#   print_square(5) → each argument is evaluated first, then the function body runs
#   print_square(n) → each argument is evaluated first, then the function body runs
#   print_square(15) runs first → then print() outputs the result
#   i < n → evaluated as True or False (determines which branch runs)
#   print_square(15) → each argument is evaluated first, then the function body runs
#   '* ' * 5 → Python looks up '* ', then 5, then applies *
#   j < n → evaluated as True or False (determines which branch runs)
#   '* ' * n → Python looks up '* ', then n, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def print_square(n) → stored in memory, not executed
#   4. print_square(5) → calls print_square(n) with n=5
#      → 4.1 i = 0
#      → 4.2 while i < n:
#      → 4.2     j = 0
#      → 4.2     while j < n:
#      → 4.2         print('*', end=' ')
#      → 4.2         j += 1
#      → 4.2     print()
#      → 4.2     i += 1
#   5. def print_square(n) → stored in memory, not executed
#   6. print_square(5) → calls print_square(n) with n=5
#      → 6.1 i = 0
#      → 6.2 while i < n:
#      → 6.2     j = 0
#      → 6.2     while j < n:
#      → 6.2         print('*', end=' ')
#      → 6.2         j += 1
#      → 6.2     print()
#      → 6.2     i += 1
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. n = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(print_square(...))
#      → calls print_square(n) with n=15
#      → 12.1 i = 0
#      → 12.2 while i < n:
#      → 12.2     j = 0
#      → 12.2     while j < n:
#      → 12.2         print('*', end=' ')
#      → 12.2         j += 1
#      → 12.2     print()
#      → 12.2     i += 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_square, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     print_square(5) → jumps into print_square(), runs body, returns
#     print_square(5) → jumps into print_square(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_square(...)) → print_square() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 236:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def decreasing_triangle() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: *
# Built-in functions: increasing_triangle, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   decreasing_triangle() → each argument is evaluated first, then the function body runs
#   '* ' * i → Python looks up '* ', then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def decreasing_triangle() → stored in memory, not executed
#   2. print(increasing_triangle(...))
#   3. def decreasing_triangle() → stored in memory, not executed
#   4. decreasing_triangle() → calls decreasing_triangle() with 
#      → 4.1 for i in range(5, 0, -1):
#      → 4.1     for j in range(i):
#      → 4.1         print('*', end=' ')
#      → 4.1     print()
#   5. def decreasing_triangle() → stored in memory, not executed
#   6. decreasing_triangle() → calls decreasing_triangle() with 
#      → 6.1 for i in range(5, 0, -1):
#      → 6.1     for j in range(i):
#      → 6.1         print('*', end=' ')
#      → 6.1     print()
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. n = ... → assigns value to variable
#   10. for i in range(...) → loops over each item
#      → 10.2 print()
#   11. n = ... → assigns value to variable
#   12. for i in range(...) → loops over each item
#      → 12.2 print()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, decreasing_triangle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(increasing_triangle(...))
#     decreasing_triangle() → jumps into decreasing_triangle(), runs body, returns
#     decreasing_triangle() → jumps into decreasing_triangle(), runs body, returns
#     n = ... → stored in memory
#     for i in range(...) → runs body once per item
#     n = ... → stored in memory
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 238:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def left_triangle() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: * + -
# Built-in functions: increasing_triangle, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   left_triangle() → each argument is evaluated first, then the function body runs
#   n + 1 → Python looks up n, then 1, then applies +
#   i + 1 → Python looks up i, then 1, then applies +
#   ? + ? → Python looks up ?, then ?, then applies +
#   '  ' * ? → Python looks up '  ', then ?, then applies *
#   '* ' * i → Python looks up '* ', then i, then applies *
#   n * 2 → Python looks up n, then 2, then applies *
#   n - i → Python looks up n, then i, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def left_triangle() → stored in memory, not executed
#   2. print(increasing_triangle(...))
#   3. def left_triangle() → stored in memory, not executed
#   4. left_triangle() → calls left_triangle() with 
#      → 4.1 n = 5
#      → 4.2 for i in range(1, n + 1):
#      → 4.2     print(('* ' * i).rjust(n * 2))
#   5. def left_triangle() → stored in memory, not executed
#   6. left_triangle() → calls left_triangle() with 
#      → 6.1 n = 5
#      → 6.2 for i in range(1, n + 1):
#      → 6.2     print(('* ' * i).rjust(n * 2))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. n = ... → assigns value to variable
#   10. for i in range(...) → loops over each item
#   11. for i in range(...) → loops over each item
#      → 11.2 print()
#   12. n = ... → assigns value to variable
#   13. for i in range(...) → loops over each item
#   14. for i in range(...) → loops over each item
#      → 14.2 print()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, left_triangle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(increasing_triangle(...))
#     left_triangle() → jumps into left_triangle(), runs body, returns
#     left_triangle() → jumps into left_triangle(), runs body, returns
#     n = ... → stored in memory
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#     n = ... → stored in memory
#     for i in range(...) → runs body once per item
#     for i in range(...) → runs body once per item
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 242:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def enumerate_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   enumerate_lst(lst) runs first → then print() outputs the result
#   enumerate_lst(?) runs first → then print() outputs the result
#   enumerate_lst(lst) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def enumerate_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=lst
#      → 3.1 return result
#   4. def enumerate_lst(lst) → stored in memory, not executed
#   5. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=['Jon', 'Chris', 'Nathan']
#      → 5.1 return result
#   6. def enumerate_lst(lst) → stored in memory, not executed
#   7. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=['a', 'b', 'c']
#      → 7.1 return result
#   8. def enumerate_lst(lst) → stored in memory, not executed
#   9. def enumerate_lst(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (enumerate_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 243:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def enumerate_lst(lst, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: enumerate, next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   enumerate_lst(lst, target) runs first → then print() outputs the result
#   find_first(?, 'Chris') runs first → then print() outputs the result
#   enumerate_lst(lst, target) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?, 'goal') runs first → then print() outputs the result
#   find_first(?, 'Chris') → each argument is evaluated first, then the function body runs
#   enumerate_lst(?, 'goal') → each argument is evaluated first, then the function body runs
#   word == target → evaluated as True or False (determines which branch runs)
#   v == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def enumerate_lst(lst, target) → stored in memory, not executed
#   2. target = ... → assigns value to variable
#   3. lst = ... → assigns value to variable
#   4. print(enumerate_lst(...))
#      → calls enumerate_lst(lst, target) with lst=lst, target=target
#      → 4.1 return (word, index)
#   5. def find_first(lst, target) → stored in memory, not executed
#   6. print(find_first(...))
#      → calls find_first(lst, target) with lst=['Jon', 'Chris', 'Nathan'], target='Chris'
#      → 6.1 return next(((v, i) for i, v in enumerate(lst) if v == target), -1)
#   7. def find_first(lst, target) → stored in memory, not executed
#   8. print(find_first(...))
#      → calls find_first(lst, target) with lst=['Jon', 'Chris', 'Nathan'], target='Chris'
#      → 8.1 return next(((v, i) for i, v in enumerate(lst) if v == target), -1)
#   9. def enumerate_lst(lst, target) → stored in memory, not executed
#   10. def enumerate_lst(lst, target) → stored in memory, not executed
#   11. lst = ... → assigns value to variable
#   12. target = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(enumerate_lst(...))
#      → calls enumerate_lst(lst, target) with lst=[1, 3, 5, 7], target='goal'
#      → 15.1 return (word, index)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (enumerate_lst, find_first) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     target = ... → stored in memory
#     lst = ... → stored in memory
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     print(find_first(...)) → find_first() runs first → value passed to print()
#     print(find_first(...)) → find_first() runs first → value passed to print()
#     lst = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 244:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def enumerate_lst(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: +
# Built-in functions: enumerate, len, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   enumerate_lst(lst) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?) runs first → then print() outputs the result
#   len(...) + 1 → Python looks up len(...), then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def enumerate_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. enumerate_lst(lst) → calls enumerate_lst(lst) with lst=lst
#      → 3.1 for i, v in zip(range(1, len(lst) + 1), lst):
#      → 3.1     print(f'Index {i}, Name {v}')
#   4. def enumerate_lst(lst) → stored in memory, not executed
#   5. for s in enumerate_lst(...) → loops over each item
#      → 5.1 print(s)
#   6. def enumerate_lst(lst) → stored in memory, not executed
#   7. enumerate_lst(?) → calls enumerate_lst(lst) with lst=['A', 'B', 'C']
#      → 7.1 for i, v in zip(range(1, len(lst) + 1), lst):
#      → 7.1     print(f'Index {i}, Name {v}')
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=[1, 3, 5, 7]
#      → 13.1 for i, v in zip(range(1, len(lst) + 1), lst):
#      → 13.1     print(f'Index {i}, Name {v}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (enumerate_lst, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     enumerate_lst(lst) → jumps into enumerate_lst(), runs body, returns
#     for s in enumerate_lst(...) → runs body once per item
#     enumerate_lst(?) → jumps into enumerate_lst(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#
#   Loop: takes one item from enumerate_lst(...) each round, runs body, repeats until done

"""

"""
Problem 245:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def enumerate_lst(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: dict, enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   enumerate_lst(lst) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?) runs first → then print() outputs the result
#   enumerate_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def enumerate_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. enumerate_lst(lst) → calls enumerate_lst(lst) with lst=lst
#      → 3.1 result = {}
#      → 3.2 for index, word in enumerate(lst):
#      → 3.2     result[index] = word
#      → 3.3 result = result
#      → 3.4 return result
#   4. def enumerate_lst(lst) → stored in memory, not executed
#   5. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=['Jon', 'Chris', 'Nathan']
#      → 5.1 result = {}
#      → 5.2 for index, word in enumerate(lst):
#      → 5.2     result[index] = word
#      → 5.3 result = result
#      → 5.4 return result
#   6. def enumerate_lst(lst) → stored in memory, not executed
#   7. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=['a', 'b', 'c']
#      → 7.1 result = {}
#      → 7.2 for index, word in enumerate(lst):
#      → 7.2     result[index] = word
#      → 7.3 result = result
#      → 7.4 return result
#   8. def enumerate_lst(lst) → stored in memory, not executed
#   9. def enumerate_lst(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=[1, 3, 5, 7]
#      → 13.1 result = {}
#      → 13.2 for index, word in enumerate(lst):
#      → 13.2     result[index] = word
#      → 13.3 result = result
#      → 13.4 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (enumerate_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     enumerate_lst(lst) → jumps into enumerate_lst(), runs body, returns
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 246:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def enumerate_lst(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: enumerate, len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   enumerate_lst(lst) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?) → each argument is evaluated first, then the function body runs
#   enumerate_lst(?) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   index % 2 → Python looks up index, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def enumerate_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. enumerate_lst(lst) → calls enumerate_lst(lst) with lst=lst
#      → 3.1 return [(i, lst[i]) for i in range(0, len(lst), 2)]
#   4. def enumerate_lst(lst) → stored in memory, not executed
#   5. enumerate_lst(?) → calls enumerate_lst(lst) with lst=['Jon', 'Chris', 'Nathan', 'hot', 'class']
#      → 5.1 return [(i, lst[i]) for i in range(0, len(lst), 2)]
#   6. def enumerate_lst(lst) → stored in memory, not executed
#   7. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=['a', 'b', 'c', 'd', 'e']
#      → 7.1 return [(i, lst[i]) for i in range(0, len(lst), 2)]
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(enumerate_lst(...))
#      → calls enumerate_lst(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return [(i, lst[i]) for i in range(0, len(lst), 2)]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (enumerate_lst, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     enumerate_lst(lst) → jumps into enumerate_lst(), runs body, returns
#     enumerate_lst(?) → jumps into enumerate_lst(), runs body, returns
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(enumerate_lst(...)) → enumerate_lst() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 248:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def replace_with_index(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate, len, list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   replace_with_index(lst) runs first → then print() outputs the result
#   replace_with_index(?) runs first → then print() outputs the result
#   replace_with_index(lst) → each argument is evaluated first, then the function body runs
#   replace_with_index(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def replace_with_index(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(replace_with_index(...))
#      → calls replace_with_index(lst) with lst=lst
#      → 3.1 return lst
#   4. def replace_with_index(lst) → stored in memory, not executed
#   5. print(replace_with_index(...))
#      → calls replace_with_index(lst) with lst=['Jon', 'Chris', 'Nathan', 'Sam', 'Rachel']
#      → 5.1 return lst
#   6. def replace_with_index(lst) → stored in memory, not executed
#   7. print(replace_with_index(...))
#      → calls replace_with_index(lst) with lst=[1, 2, 3]
#      → 7.1 return lst
#   8. def replace_with_index(lst) → stored in memory, not executed
#   9. def replace_with_index(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(replace_with_index(...))
#      → calls replace_with_index(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return lst
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (replace_with_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(replace_with_index(...)) → replace_with_index() runs first → value passed to print()
#     print(replace_with_index(...)) → replace_with_index() runs first → value passed to print()
#     print(replace_with_index(...)) → replace_with_index() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(replace_with_index(...)) → replace_with_index() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 250:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_odd_indexed_elements(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: enumerate, len, range, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_odd_indexed_elements(lst) → each argument is evaluated first, then the function body runs
#   sum_odd_indexed_elements(?) runs first → then print() outputs the result
#   sum_odd_indexed_elements(?) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   index % 2 → Python looks up index, then 2, then applies %
#   i % 2 → Python looks up i, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_odd_indexed_elements(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def sum_odd_indexed_elements(lst) → stored in memory, not executed
#   6. print(sum_odd_indexed_elements(...))
#      → calls sum_odd_indexed_elements(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 6.1 return total
#   7. def sum_odd_indexed_elements(lst) → stored in memory, not executed
#   8. print(sum_odd_indexed_elements(...))
#      → calls sum_odd_indexed_elements(lst) with lst=[10, 20, 30, 40]
#      → 8.1 return total
#   9. def sum_odd_indexed_elements(lst) → stored in memory, not executed
#   10. print(sum_odd_indexed_elements(...))
#      → calls sum_odd_indexed_elements(lst) with lst=[2, 4, 6, 8, 10]
#      → 10.1 return total
#   11. def sum_odd_indexed_elements(lst) → stored in memory, not executed
#   12. def sum_odd_indexed_elements(lst) → stored in memory, not executed
#   13. lst = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(sum_odd_indexed_elements(...))
#      → calls sum_odd_indexed_elements(lst) with lst=[1, 3, 5, 7]
#      → 16.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_odd_indexed_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_odd_indexed_elements(...)) → sum_odd_indexed_elements() runs first → value passed to print()
#     print(sum_odd_indexed_elements(...)) → sum_odd_indexed_elements() runs first → value passed to print()
#     print(sum_odd_indexed_elements(...)) → sum_odd_indexed_elements() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_odd_indexed_elements(...)) → sum_odd_indexed_elements() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 251:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def pair_elements(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   pair_elements(lst1, lst2) runs first → then print() outputs the result
#   pair_elements(?, ?) runs first → then print() outputs the result
#   pair_elements(lst1, lst2) → each argument is evaluated first, then the function body runs
#   pair_elements(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def pair_elements(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(pair_elements(...))
#      → calls pair_elements(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return paired
#   5. def pair_elements(lst1, lst2) → stored in memory, not executed
#   6. print(pair_elements(...))
#      → calls pair_elements(lst1, lst2) with lst1=['Jon', 'Chris', 'Nathan', 'Sam'], lst2=['A', 'B', 'C', 'D']
#      → 6.1 return paired
#   7. def pair_elements(lst1, lst2) → stored in memory, not executed
#   8. print(pair_elements(...))
#      → calls pair_elements(lst1, lst2) with lst1=['Jon', 'Chris', 'Nathan', 'Sam'], lst2=['A', 'B', 'C', 'D']
#      → 8.1 return paired
#   9. def pair_elements(lst1, lst2) → stored in memory, not executed
#   10. print(pair_elements(...))
#      → calls pair_elements(lst1, lst2) with lst1=['Jon', 'Chris', 'Nathan', 'Sam'], lst2=['A', 'B', 'C', 'D']
#      → 10.1 return paired
#   11. def pair_elements(lst1, lst2) → stored in memory, not executed
#   12. def pair_elements(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(pair_elements(...))
#      → calls pair_elements(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return paired
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (pair_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 252:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_2_lst(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: len, list, map, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_2_lst(lst1, lst2) runs first → then print() outputs the result
#   sum_of_2_lst(?, ?) runs first → then print() outputs the result
#   sum_of_2_lst(lst1, lst2) → each argument is evaluated first, then the function body runs
#   sum_of_2_lst(?, ?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#   ? + ? → Python looks up ?, then ?, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_2_lst(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(sum_of_2_lst(...))
#      → calls sum_of_2_lst(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return summed_list
#   5. def sum_of_2_lst(lst1, lst2) → stored in memory, not executed
#   6. print(sum_of_2_lst(...))
#      → calls sum_of_2_lst(lst1, lst2) with lst1=[2, 4, 6, 8], lst2=[2, 4, 6, 8]
#      → 6.1 return summed_list
#   7. def sum_of_2_lst(lst1, lst2) → stored in memory, not executed
#   8. print(sum_of_2_lst(...))
#      → calls sum_of_2_lst(lst1, lst2) with lst1=[2, 4, 6, 8], lst2=[2, 4, 6, 8]
#      → 8.1 return summed_list
#   9. def sum_of_2_lst(lst1, lst2) → stored in memory, not executed
#   10. print(sum_of_2_lst(...))
#      → calls sum_of_2_lst(lst1, lst2) with lst1=[2, 4, 6, 8], lst2=[2, 4, 6, 8]
#      → 10.1 return summed_list
#   11. def sum_of_2_lst(lst1, lst2) → stored in memory, not executed
#   12. def sum_of_2_lst(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(sum_of_2_lst(...))
#      → calls sum_of_2_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return summed_list
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_2_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(sum_of_2_lst(...)) → sum_of_2_lst() runs first → value passed to print()
#     print(sum_of_2_lst(...)) → sum_of_2_lst() runs first → value passed to print()
#     print(sum_of_2_lst(...)) → sum_of_2_lst() runs first → value passed to print()
#     print(sum_of_2_lst(...)) → sum_of_2_lst() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_2_lst(...)) → sum_of_2_lst() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 253:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_tuples_by_second(tuples_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - >
# Built-in functions: itemgetter, len, list, range, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_tuples_by_second(tuples_list) → each argument is evaluated first, then the function body runs
#   sort_tuples_by_second(?) runs first → then print() outputs the result
#   sort_tuples_by_second(?) → each argument is evaluated first, then the function body runs
#   ? - 1 → Python looks up ?, then 1, then applies -
#   ? > ? → evaluated as True or False (determines which branch runs)
#   len(...) - i → Python looks up len(...), then i, then applies -
#   j + 1 → Python looks up j, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_tuples_by_second(tuples_list) → stored in memory, not executed
#   2. tuples_list = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. from operator import itemgetter
#   6. def sort_tuples_by_second(tuples_list) → stored in memory, not executed
#   7. print(sort_tuples_by_second(...))
#      → calls sort_tuples_by_second(tuples_list) with tuples_list=[?, ?, ?]
#      → 7.1 return sorted(tuples_list, key=lambda x: x[1])
#   8. def sort_tuples_by_second(tuples_list) → stored in memory, not executed
#   9. print(sort_tuples_by_second(...))
#      → calls sort_tuples_by_second(tuples_list) with tuples_list=[?, ?, ?]
#      → 9.1 return sorted(tuples_list, key=lambda x: x[1])
#   10. def sort_tuples_by_second(tuples_list) → stored in memory, not executed
#   11. print(sort_tuples_by_second(...))
#      → calls sort_tuples_by_second(tuples_list) with tuples_list=[?, ?, ?]
#      → 11.1 return sorted(tuples_list, key=lambda x: x[1])
#   12. def sort_tuples_by_second(tuples_list) → stored in memory, not executed
#   13. def sort_tuples_by_second(tuples_list) → stored in memory, not executed
#   14. tuples_list = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(sort_tuples_by_second(...))
#      → calls sort_tuples_by_second(tuples_list) with tuples_list=[4, 1, 8, 6, 3]
#      → 17.1 return sorted(tuples_list, key=lambda x: x[1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_tuples_by_second) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     tuples_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_tuples_by_second(...)) → sort_tuples_by_second() runs first → value passed to print()
#     print(sort_tuples_by_second(...)) → sort_tuples_by_second() runs first → value passed to print()
#     print(sort_tuples_by_second(...)) → sort_tuples_by_second() runs first → value passed to print()
#     tuples_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_tuples_by_second(...)) → sort_tuples_by_second() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 254:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def dic_lst(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: dict, len, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dic_lst(lst1, lst2) runs first → then print() outputs the result
#   dic_lst(?, ?) runs first → then print() outputs the result
#   dic_lst(lst1, lst2) → each argument is evaluated first, then the function body runs
#   dic_lst(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def dic_lst(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(dic_lst(...))
#      → calls dic_lst(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def dic_lst(lst1, lst2) → stored in memory, not executed
#   6. print(dic_lst(...))
#      → calls dic_lst(lst1, lst2) with lst1=[1, 2, 3, 4], lst2=['Ron', 'break', 'tree', 'son']
#      → 6.1 return result
#   7. def dic_lst(lst1, lst2) → stored in memory, not executed
#   8. print(dic_lst(...))
#      → calls dic_lst(lst1, lst2) with lst1=[1, 2, 3, 4], lst2=['Ron', 'break', 'tree', 'son']
#      → 8.1 return result
#   9. def dic_lst(lst1, lst2) → stored in memory, not executed
#   10. print(dic_lst(...))
#      → calls dic_lst(lst1, lst2) with lst1=[1, 2, 3, 4], lst2=['Ron', 'break', 'tree', 'son']
#      → 10.1 return result
#   11. def dic_lst(lst1, lst2) → stored in memory, not executed
#   12. def dic_lst(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(dic_lst(...))
#      → calls dic_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (dic_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(dic_lst(...)) → dic_lst() runs first → value passed to print()
#     print(dic_lst(...)) → dic_lst() runs first → value passed to print()
#     print(dic_lst(...)) → dic_lst() runs first → value passed to print()
#     print(dic_lst(...)) → dic_lst() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(dic_lst(...)) → dic_lst() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 255:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def combine_3_lst(lst1, lst2, lst3) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   combine_3_lst(lst1, lst2, lst3) runs first → then print() outputs the result
#   combine_3_lst(?, ?, ?) runs first → then print() outputs the result
#   combine_3_lst(lst1, lst2, lst3) → each argument is evaluated first, then the function body runs
#   combine_3_lst(?, ?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def combine_3_lst(lst1, lst2, lst3) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. lst3 = ... → assigns value to variable
#   5. print(combine_3_lst(...))
#      → calls combine_3_lst(lst1, lst2, lst3) with lst1=lst1, lst2=lst2, lst3=lst3
#      → 5.1 return result
#   6. def combine_3_lst(lst1, lst2, lst3) → stored in memory, not executed
#   7. print(combine_3_lst(...))
#      → calls combine_3_lst(lst1, lst2, lst3) with lst1=['Sam', 'Nike', 'system'], lst2=['Sammy', 'Mikey', 'Steven'], lst3=['Son', 'monkey', 'star']
#      → 7.1 return result
#   8. def combine_3_lst(lst1, lst2, lst3) → stored in memory, not executed
#   9. print(combine_3_lst(...))
#      → calls combine_3_lst(lst1, lst2, lst3) with lst1=['Sam', 'Nike', 'system'], lst2=['Sammy', 'Mikey', 'Steven'], lst3=['Son', 'monkey', 'star']
#      → 9.1 return result
#   10. def combine_3_lst(lst1, lst2, lst3) → stored in memory, not executed
#   11. print(combine_3_lst(...))
#      → calls combine_3_lst(lst1, lst2, lst3) with lst1=['Sam', 'Nike', 'system'], lst2=['Sammy', 'Mikey', 'Steven'], lst3=['Son', 'monkey', 'star']
#      → 11.1 return result
#   12. def combine_3_lst(lst1, lst2, lst3) → stored in memory, not executed
#   13. def combine_3_lst(lst1, lst2, lst3) → stored in memory, not executed
#   14. lst1 = ... → assigns value to variable
#   15. lst2 = ... → assigns value to variable
#   16. lst3 = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(combine_3_lst(...))
#      → calls combine_3_lst(lst1, lst2, lst3) with lst1=[2, 4, 6], lst2=[1, 3, 5], lst3=[4, 1, 8, 6, 3]
#      → 19.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (combine_3_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     lst3 = ... → stored in memory
#     print(combine_3_lst(...)) → combine_3_lst() runs first → value passed to print()
#     print(combine_3_lst(...)) → combine_3_lst() runs first → value passed to print()
#     print(combine_3_lst(...)) → combine_3_lst() runs first → value passed to print()
#     print(combine_3_lst(...)) → combine_3_lst() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     lst3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(combine_3_lst(...)) → combine_3_lst() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 256:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def transpose_matrix(matrix) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: len, list, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   transpose_matrix(matrix) runs first → then print() outputs the result
#   transpose_matrix(matrix) → each argument is evaluated first, then the function body runs
#   transpose_matrix(25) runs first → then print() outputs the result
#   transpose_matrix(25) → each argument is evaluated first, then the function body runs
#   [0] * rows → Python looks up [0], then rows, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def transpose_matrix(matrix) → stored in memory, not executed
#   2. matrix = ... → assigns value to variable
#   3. print(transpose_matrix(...))
#      → calls transpose_matrix(matrix) with matrix=matrix
#      → 3.1 return result
#   4. def transpose_matrix(matrix) → stored in memory, not executed
#   5. matrix = ... → assigns value to variable
#   6. print(transpose_matrix(...))
#      → calls transpose_matrix(matrix) with matrix=matrix
#      → 6.1 return result
#   7. def transpose_matrix(matrix) → stored in memory, not executed
#   8. matrix = ... → assigns value to variable
#   9. print(transpose_matrix(...))
#      → calls transpose_matrix(matrix) with matrix=matrix
#      → 9.1 return result
#   10. def transpose_matrix(matrix) → stored in memory, not executed
#   11. matrix = ... → assigns value to variable
#   12. print(transpose_matrix(...))
#      → calls transpose_matrix(matrix) with matrix=matrix
#      → 12.1 return result
#   13. def transpose_matrix(matrix) → stored in memory, not executed
#   14. def transpose_matrix(matrix) → stored in memory, not executed
#   15. matrix = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(transpose_matrix(...))
#      → calls transpose_matrix(matrix) with matrix=25
#      → 18.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (transpose_matrix) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     matrix = ... → stored in memory
#     print(transpose_matrix(...)) → transpose_matrix() runs first → value passed to print()
#     matrix = ... → stored in memory
#     print(transpose_matrix(...)) → transpose_matrix() runs first → value passed to print()
#     matrix = ... → stored in memory
#     print(transpose_matrix(...)) → transpose_matrix() runs first → value passed to print()
#     matrix = ... → stored in memory
#     print(transpose_matrix(...)) → transpose_matrix() runs first → value passed to print()
#     matrix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(transpose_matrix(...)) → transpose_matrix() runs first → value passed to print()
#
#   Loop: takes one item from transposed each round, runs body, repeats until done

"""

"""
Problem 257:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_tuples(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate, list, map, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_tuples(lst) runs first → then print() outputs the result
#   list_tuples(?) runs first → then print() outputs the result
#   list_tuples(lst) → each argument is evaluated first, then the function body runs
#   list_tuples(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_tuples(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_tuples(...))
#      → calls list_tuples(lst) with lst=lst
#      → 3.1 result = [[], [], []]
#      → 3.2 for t in lst:
#      → 3.2     for i, val in enumerate(t):
#      → 3.2         result[i].append(val)
#      → 3.3 return result
#   4. def list_tuples(lst) → stored in memory, not executed
#   5. print(list_tuples(...))
#      → calls list_tuples(lst) with lst=[?, ?, ?]
#      → 5.1 result = [[], [], []]
#      → 5.2 for t in lst:
#      → 5.2     for i, val in enumerate(t):
#      → 5.2         result[i].append(val)
#      → 5.3 return result
#   6. def list_tuples(lst) → stored in memory, not executed
#   7. print(list_tuples(...))
#      → calls list_tuples(lst) with lst=[?, ?, ?]
#      → 7.1 result = [[], [], []]
#      → 7.2 for t in lst:
#      → 7.2     for i, val in enumerate(t):
#      → 7.2         result[i].append(val)
#      → 7.3 return result
#   8. def list_tuples(lst) → stored in memory, not executed
#   9. print(list_tuples(...))
#      → calls list_tuples(lst) with lst=[?, ?, ?]
#      → 9.1 result = [[], [], []]
#      → 9.2 for t in lst:
#      → 9.2     for i, val in enumerate(t):
#      → 9.2         result[i].append(val)
#      → 9.3 return result
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(list_tuples(...))
#      → calls list_tuples(lst) with lst=[1, 3, 5, 7]
#      → 13.1 result = [[], [], []]
#      → 13.2 for t in lst:
#      → 13.2     for i, val in enumerate(t):
#      → 13.2         result[i].append(val)
#      → 13.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_tuples(...)) → list_tuples() runs first → value passed to print()
#     print(list_tuples(...)) → list_tuples() runs first → value passed to print()
#     print(list_tuples(...)) → list_tuples() runs first → value passed to print()
#     print(list_tuples(...)) → list_tuples() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_tuples(...)) → list_tuples() runs first → value passed to print()
#
#   Loop: takes one item from unzipped each round, runs body, repeats until done

"""

"""
Problem 258:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def individualists(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate, len, list, map, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   individualists(lst) runs first → then print() outputs the result
#   individualists(?) runs first → then print() outputs the result
#   individualists(lst) → each argument is evaluated first, then the function body runs
#   individualists(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def individualists(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(individualists(...))
#      → calls individualists(lst) with lst=lst
#      → 3.1 return result
#   4. def individualists(lst) → stored in memory, not executed
#   5. print(individualists(...))
#      → calls individualists(lst) with lst=[?, ?]
#      → 5.1 return result
#   6. def individualists(lst) → stored in memory, not executed
#   7. print(individualists(...))
#      → calls individualists(lst) with lst=[?, ?]
#      → 7.1 return result
#   8. def individualists(lst) → stored in memory, not executed
#   9. print(individualists(...))
#      → calls individualists(lst) with lst=[?, ?]
#      → 9.1 return result
#   10. def individualists(lst) → stored in memory, not executed
#   11. def individualists(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(individualists(...))
#      → calls individualists(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (individualists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(individualists(...)) → individualists() runs first → value passed to print()
#     print(individualists(...)) → individualists() runs first → value passed to print()
#     print(individualists(...)) → individualists() runs first → value passed to print()
#     print(individualists(...)) → individualists() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(individualists(...)) → individualists() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 259:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def maxium_element_wise(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, list, map, max, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   maxium_element_wise(lst1, lst2) runs first → then print() outputs the result
#   maxium_element_wise(?, ?) runs first → then print() outputs the result
#   maxium_element_wise(lst1, lst2) → each argument is evaluated first, then the function body runs
#   maxium_element_wise(?, ?) → each argument is evaluated first, then the function body runs
#   ? > ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def maxium_element_wise(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(maxium_element_wise(...))
#      → calls maxium_element_wise(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def maxium_element_wise(lst1, lst2) → stored in memory, not executed
#   6. print(maxium_element_wise(...))
#      → calls maxium_element_wise(lst1, lst2) with lst1=[2, 4, 6], lst2=[7, 8, 9]
#      → 6.1 return result
#   7. def maxium_element_wise(lst1, lst2) → stored in memory, not executed
#   8. print(maxium_element_wise(...))
#      → calls maxium_element_wise(lst1, lst2) with lst1=[2, 4, 6], lst2=[7, 8, 9]
#      → 8.1 return result
#   9. def maxium_element_wise(lst1, lst2) → stored in memory, not executed
#   10. print(maxium_element_wise(...))
#      → calls maxium_element_wise(lst1, lst2) with lst1=[2, 4, 6], lst2=[7, 8, 9]
#      → 10.1 return result
#   11. def maxium_element_wise(lst1, lst2) → stored in memory, not executed
#   12. def maxium_element_wise(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(maxium_element_wise(...))
#      → calls maxium_element_wise(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (maxium_element_wise) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(maxium_element_wise(...)) → maxium_element_wise() runs first → value passed to print()
#     print(maxium_element_wise(...)) → maxium_element_wise() runs first → value passed to print()
#     print(maxium_element_wise(...)) → maxium_element_wise() runs first → value passed to print()
#     print(maxium_element_wise(...)) → maxium_element_wise() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(maxium_element_wise(...)) → maxium_element_wise() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 260:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def concatenate(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: len, list, map, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   concatenate(lst1, lst2) runs first → then print() outputs the result
#   concatenate(?, ?) runs first → then print() outputs the result
#   concatenate(lst1, lst2) → each argument is evaluated first, then the function body runs
#   concatenate(?, ?) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   ? + ' ' → Python looks up ?, then ' ', then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def concatenate(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(concatenate(...))
#      → calls concatenate(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def concatenate(lst1, lst2) → stored in memory, not executed
#   6. print(concatenate(...))
#      → calls concatenate(lst1, lst2) with lst1=['fruite', 'apple', 'tomato'], lst2=['orange', 'banana', 'carrot']
#      → 6.1 return result
#   7. def concatenate(lst1, lst2) → stored in memory, not executed
#   8. print(concatenate(...))
#      → calls concatenate(lst1, lst2) with lst1=['fruite', 'apple', 'tomato'], lst2=['orange', 'banana', 'carrot']
#      → 8.1 return result
#   9. def concatenate(lst1, lst2) → stored in memory, not executed
#   10. print(concatenate(...))
#      → calls concatenate(lst1, lst2) with lst1=['fruite', 'apple', 'tomato'], lst2=['orange', 'banana', 'carrot']
#      → 10.1 return result
#   11. def concatenate(lst1, lst2) → stored in memory, not executed
#   12. def concatenate(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(concatenate(...))
#      → calls concatenate(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (concatenate) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 261:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def combine_to_dict(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   combine_to_dict(lst1, lst2) runs first → then print() outputs the result
#   combine_to_dict(?, ?) runs first → then print() outputs the result
#   combine_to_dict(lst1, lst2) → each argument is evaluated first, then the function body runs
#   combine_to_dict(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def combine_to_dict(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(combine_to_dict(...))
#      → calls combine_to_dict(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def combine_to_dict(lst1, lst2) → stored in memory, not executed
#   6. print(combine_to_dict(...))
#      → calls combine_to_dict(lst1, lst2) with lst1=[1, 2, 3], lst2=['one', 'two', 'three']
#      → 6.1 return result
#   7. def combine_to_dict(lst1, lst2) → stored in memory, not executed
#   8. print(combine_to_dict(...))
#      → calls combine_to_dict(lst1, lst2) with lst1=[1, 2, 3], lst2=['one', 'two', 'three']
#      → 8.1 return result
#   9. def combine_to_dict(lst1, lst2) → stored in memory, not executed
#   10. print(combine_to_dict(...))
#      → calls combine_to_dict(lst1, lst2) with lst1=[1, 2, 3], lst2=['one', 'two', 'three']
#      → 10.1 return result
#   11. def combine_to_dict(lst1, lst2) → stored in memory, not executed
#   12. def combine_to_dict(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(combine_to_dict(...))
#      → calls combine_to_dict(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (combine_to_dict) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(combine_to_dict(...)) → combine_to_dict() runs first → value passed to print()
#     print(combine_to_dict(...)) → combine_to_dict() runs first → value passed to print()
#     print(combine_to_dict(...)) → combine_to_dict() runs first → value passed to print()
#     print(combine_to_dict(...)) → combine_to_dict() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(combine_to_dict(...)) → combine_to_dict() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 262:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_pairs(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: filter, len, list, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_pairs(lst1, lst2) runs first → then print() outputs the result
#   filter_pairs(?, ?) runs first → then print() outputs the result
#   filter_pairs(lst1, lst2) → each argument is evaluated first, then the function body runs
#   filter_pairs(?, ?) → each argument is evaluated first, then the function body runs
#   x > y → evaluated as True or False (determines which branch runs)
#   ? > ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   6. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#      → 6.1 return result
#   7. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   8. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#      → 8.1 return result
#   9. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   10. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#      → 10.1 return result
#   11. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   12. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_pairs) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 263:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_pairs(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: filter, len, list, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_pairs(lst1, lst2) runs first → then print() outputs the result
#   filter_pairs(?, ?) runs first → then print() outputs the result
#   filter_pairs(lst1, lst2) → each argument is evaluated first, then the function body runs
#   filter_pairs(?, ?) → each argument is evaluated first, then the function body runs
#   x < y → evaluated as True or False (determines which branch runs)
#   ? < ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   6. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#      → 6.1 return result
#   7. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   8. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#      → 8.1 return result
#   9. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   10. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#      → 10.1 return result
#   11. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   12. def filter_pairs(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(filter_pairs(...))
#      → calls filter_pairs(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_pairs) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_pairs(...)) → filter_pairs() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 264:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_nested_lists(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: len, list, map, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_nested_lists(lst1, lst2) runs first → then print() outputs the result
#   add_nested_lists(?, ?) runs first → then print() outputs the result
#   add_nested_lists(lst1, lst2) → each argument is evaluated first, then the function body runs
#   add_nested_lists(?, ?) → each argument is evaluated first, then the function body runs
#   a + b → Python looks up a, then b, then applies +
#   x + y → Python looks up x, then y, then applies +
#   ? + ? → Python looks up ?, then ?, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_nested_lists(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(add_nested_lists(...))
#      → calls add_nested_lists(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def add_nested_lists(lst1, lst2) → stored in memory, not executed
#   6. print(add_nested_lists(...))
#      → calls add_nested_lists(lst1, lst2) with lst1=[[1, 2, 3], [4, 5, 6]], lst2=[[7, 8, 9], [10, 11, 12]]
#      → 6.1 return result
#   7. def add_nested_lists(lst1, lst2) → stored in memory, not executed
#   8. print(add_nested_lists(...))
#      → calls add_nested_lists(lst1, lst2) with lst1=[[1, 2, 3], [4, 5, 6]], lst2=[[7, 8, 9], [10, 11, 12]]
#      → 8.1 return result
#   9. def add_nested_lists(lst1, lst2) → stored in memory, not executed
#   10. print(add_nested_lists(...))
#      → calls add_nested_lists(lst1, lst2) with lst1=[[1, 2, 3], [4, 5, 6]], lst2=[[7, 8, 9], [10, 11, 12]]
#      → 10.1 return result
#   11. def add_nested_lists(lst1, lst2) → stored in memory, not executed
#   12. def add_nested_lists(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(add_nested_lists(...))
#      → calls add_nested_lists(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_nested_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(add_nested_lists(...)) → add_nested_lists() runs first → value passed to print()
#     print(add_nested_lists(...)) → add_nested_lists() runs first → value passed to print()
#     print(add_nested_lists(...)) → add_nested_lists() runs first → value passed to print()
#     print(add_nested_lists(...)) → add_nested_lists() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_nested_lists(...)) → add_nested_lists() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 265:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def euclidean_distance(point1, point2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: - **
# Built-in functions: sum, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   euclidean_distance(point1, point2) runs first → then print() outputs the result
#   euclidean_distance(?, ?) runs first → then print() outputs the result
#   euclidean_distance(point1, point2) → each argument is evaluated first, then the function body runs
#   euclidean_distance(25, 25) runs first → then print() outputs the result
#   euclidean_distance(?, ?) → each argument is evaluated first, then the function body runs
#   sum(...) ** 0.5 → Python looks up sum(...), then 0.5, then applies **
#   euclidean_distance(25, 25) → each argument is evaluated first, then the function body runs
#   ? ** 2 → Python looks up ?, then 2, then applies **
#   x1 - x2 → Python looks up x1, then x2, then applies -
#   a - b → Python looks up a, then b, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def euclidean_distance(point1, point2) → stored in memory, not executed
#   3. point1 = ... → assigns value to variable
#   4. point2 = ... → assigns value to variable
#   5. print(euclidean_distance(...))
#      → calls euclidean_distance(point1, point2) with point1=point1, point2=point2
#      → 5.1 return math.sqrt(distance_squared)
#   6. import math
#   7. def euclidean_distance(point1, point2) → stored in memory, not executed
#   8. print(euclidean_distance(...))
#      → calls euclidean_distance(point1, point2) with point1=?, point2=?
#      → 8.1 return math.sqrt(distance_squared)
#   9. import math
#   10. def euclidean_distance(point1, point2) → stored in memory, not executed
#   11. print(euclidean_distance(...))
#      → calls euclidean_distance(point1, point2) with point1=?, point2=?
#      → 11.1 return math.sqrt(distance_squared)
#   12. def euclidean_distance(point1, point2) → stored in memory, not executed
#   13. print(euclidean_distance(...))
#      → calls euclidean_distance(point1, point2) with point1=?, point2=?
#      → 13.1 return math.sqrt(distance_squared)
#   14. def euclidean_distance(point1, point2) → stored in memory, not executed
#   15. def euclidean_distance(point1, point2) → stored in memory, not executed
#   16. point1 = ... → assigns value to variable
#   17. point2 = ... → assigns value to variable
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. print(euclidean_distance(...))
#      → calls euclidean_distance(point1, point2) with point1=25, point2=25
#      → 20.1 return math.sqrt(distance_squared)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (euclidean_distance) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     point1 = ... → stored in memory
#     point2 = ... → stored in memory
#     print(euclidean_distance(...)) → euclidean_distance() runs first → value passed to print()
#     print(euclidean_distance(...)) → euclidean_distance() runs first → value passed to print()
#     print(euclidean_distance(...)) → euclidean_distance() runs first → value passed to print()
#     print(euclidean_distance(...)) → euclidean_distance() runs first → value passed to print()
#     point1 = ... → stored in memory
#     point2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(euclidean_distance(...)) → euclidean_distance() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 266:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def interleave(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, sum, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   interleave(lst1, lst2) runs first → then print() outputs the result
#   interleave(?, ?) runs first → then print() outputs the result
#   interleave(lst1, lst2) → each argument is evaluated first, then the function body runs
#   interleave(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def interleave(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(interleave(...))
#      → calls interleave(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def interleave(lst1, lst2) → stored in memory, not executed
#   6. print(interleave(...))
#      → calls interleave(lst1, lst2) with lst1=['crissy', 'missy', 'sissy'], lst2=['spotty', 'motty', 'hotty']
#      → 6.1 return result
#   7. def interleave(lst1, lst2) → stored in memory, not executed
#   8. print(interleave(...))
#      → calls interleave(lst1, lst2) with lst1=['crissy', 'missy', 'sissy'], lst2=['spotty', 'motty', 'hotty']
#      → 8.1 return result
#   9. from itertools import chain
#   10. def interleave(lst1, lst2) → stored in memory, not executed
#   11. print(interleave(...))
#      → calls interleave(lst1, lst2) with lst1=['crissy', 'missy', 'sissy'], lst2=['spotty', 'motty', 'hotty']
#      → 11.1 return result
#   12. def interleave(lst1, lst2) → stored in memory, not executed
#   13. def interleave(lst1, lst2) → stored in memory, not executed
#   14. lst1 = ... → assigns value to variable
#   15. lst2 = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(interleave(...))
#      → calls interleave(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 18.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (interleave) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(interleave(...)) → interleave() runs first → value passed to print()
#     print(interleave(...)) → interleave() runs first → value passed to print()
#     print(interleave(...)) → interleave() runs first → value passed to print()
#     print(interleave(...)) → interleave() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(interleave(...)) → interleave() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 267:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_2_lst(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: len, list, map, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_2_lst(lst1, lst2) runs first → then print() outputs the result
#   multiply_2_lst(?, ?) runs first → then print() outputs the result
#   multiply_2_lst(lst1, lst2) → each argument is evaluated first, then the function body runs
#   multiply_2_lst(?, ?) → each argument is evaluated first, then the function body runs
#   x * y → Python looks up x, then y, then applies *
#   ? * ? → Python looks up ?, then ?, then applies *
#   a * b → Python looks up a, then b, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_2_lst(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(multiply_2_lst(...))
#      → calls multiply_2_lst(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def multiply_2_lst(lst1, lst2) → stored in memory, not executed
#   6. print(multiply_2_lst(...))
#      → calls multiply_2_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[3, 6, 9]
#      → 6.1 return result
#   7. def multiply_2_lst(lst1, lst2) → stored in memory, not executed
#   8. print(multiply_2_lst(...))
#      → calls multiply_2_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[3, 6, 9]
#      → 8.1 return result
#   9. def multiply_2_lst(lst1, lst2) → stored in memory, not executed
#   10. print(multiply_2_lst(...))
#      → calls multiply_2_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[3, 6, 9]
#      → 10.1 return result
#   11. def multiply_2_lst(lst1, lst2) → stored in memory, not executed
#   12. def multiply_2_lst(lst1, lst2) → stored in memory, not executed
#   13. lst1 = ... → assigns value to variable
#   14. lst2 = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(multiply_2_lst(...))
#      → calls multiply_2_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 17.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_2_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(multiply_2_lst(...)) → multiply_2_lst() runs first → value passed to print()
#     print(multiply_2_lst(...)) → multiply_2_lst() runs first → value passed to print()
#     print(multiply_2_lst(...)) → multiply_2_lst() runs first → value passed to print()
#     print(multiply_2_lst(...)) → multiply_2_lst() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_2_lst(...)) → multiply_2_lst() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 268:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def dot_product(vector1, vector2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * +
# Built-in functions: map, reduce, sum, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dot_product(vector1, vector2) runs first → then print() outputs the result
#   dot_product(?, ?) runs first → then print() outputs the result
#   dot_product(vector1, vector2) → each argument is evaluated first, then the function body runs
#   dot_product(25, 25) runs first → then print() outputs the result
#   dot_product(?, ?) → each argument is evaluated first, then the function body runs
#   dot_product(25, 25) → each argument is evaluated first, then the function body runs
#   x * y → Python looks up x, then y, then applies *
#   acc + ? → Python looks up acc, then ?, then applies +
#   a * b → Python looks up a, then b, then applies *
#   ? * ? → Python looks up ?, then ?, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def dot_product(vector1, vector2) → stored in memory, not executed
#   2. vector1 = ... → assigns value to variable
#   3. vector2 = ... → assigns value to variable
#   4. print(dot_product(...))
#      → calls dot_product(vector1, vector2) with vector1=vector1, vector2=vector2
#      → 4.1 return result
#   5. def dot_product(vector1, vector2) → stored in memory, not executed
#   6. print(dot_product(...))
#      → calls dot_product(vector1, vector2) with vector1=[1, 2, 3], vector2=[4, 5, 6]
#      → 6.1 return result
#   7. def dot_product(vector1, vector2) → stored in memory, not executed
#   8. print(dot_product(...))
#      → calls dot_product(vector1, vector2) with vector1=[1, 2, 3], vector2=[4, 5, 6]
#      → 8.1 return result
#   9. from functools import reduce
#   10. def dot_product(vector1, vector2) → stored in memory, not executed
#   11. print(dot_product(...))
#      → calls dot_product(vector1, vector2) with vector1=[1, 2, 3], vector2=[4, 5, 6]
#      → 11.1 return result
#   12. def dot_product(vector1, vector2) → stored in memory, not executed
#   13. def dot_product(vector1, vector2) → stored in memory, not executed
#   14. vector1 = ... → assigns value to variable
#   15. vector2 = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(dot_product(...))
#      → calls dot_product(vector1, vector2) with vector1=25, vector2=25
#      → 18.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (dot_product) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     vector1 = ... → stored in memory
#     vector2 = ... → stored in memory
#     print(dot_product(...)) → dot_product() runs first → value passed to print()
#     print(dot_product(...)) → dot_product() runs first → value passed to print()
#     print(dot_product(...)) → dot_product() runs first → value passed to print()
#     print(dot_product(...)) → dot_product() runs first → value passed to print()
#     vector1 = ... → stored in memory
#     vector2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(dot_product(...)) → dot_product() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 269:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_odd(x) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_even_numbers(lst) → each argument is evaluated first, then the function body runs
#   filter_even_numbers(?) runs first → then print() outputs the result
#   is_odd(x) → each argument is evaluated first, then the function body runs
#   is_odd(3) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   filter_even_numbers(?) → each argument is evaluated first, then the function body runs
#   is_odd(3) → each argument is evaluated first, then the function body runs
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_odd(x) → stored in memory, not executed
#   2. def filter_even_numbers(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_even_numbers(lst) → stored in memory, not executed
#   7. print(filter_even_numbers(...))
#      → calls filter_even_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 7.1 result = []
#      → 7.2 for x in lst:
#      → 7.2     if x % 2 != 0:
#      → 7.2         result.append(x)
#      → 7.3 return result
#   8. def filter_even_numbers(lst) → stored in memory, not executed
#   9. print(filter_even_numbers(...))
#      → calls filter_even_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 9.1 result = []
#      → 9.2 for x in lst:
#      → 9.2     if x % 2 != 0:
#      → 9.2         result.append(x)
#      → 9.3 return result
#   10. def filter_even_numbers(lst) → stored in memory, not executed
#   11. print(filter_even_numbers(...))
#      → calls filter_even_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 11.1 result = []
#      → 11.2 for x in lst:
#      → 11.2     if x % 2 != 0:
#      → 11.2         result.append(x)
#      → 11.3 return result
#   12. def is_odd(x) → stored in memory, not executed
#   13. def is_odd(x) → stored in memory, not executed
#   14. x = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(is_odd(...))
#      → calls is_odd(x) with x=3
#      → 17.1 return x % 2 != 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_even_numbers, is_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_even_numbers(...)) → filter_even_numbers() runs first → value passed to print()
#     print(filter_even_numbers(...)) → filter_even_numbers() runs first → value passed to print()
#     print(filter_even_numbers(...)) → filter_even_numbers() runs first → value passed to print()
#     x = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_odd(...)) → is_odd() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 270:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_even(x) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_odd_numbers(lst) → each argument is evaluated first, then the function body runs
#   filter_odd_numbers(?) runs first → then print() outputs the result
#   is_even(x) → each argument is evaluated first, then the function body runs
#   is_even(3) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   filter_odd_numbers(?) → each argument is evaluated first, then the function body runs
#   is_even(3) → each argument is evaluated first, then the function body runs
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_even(x) → stored in memory, not executed
#   2. def filter_odd_numbers(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_odd_numbers(lst) → stored in memory, not executed
#   7. print(filter_odd_numbers(...))
#      → calls filter_odd_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 7.1 result = []
#      → 7.2 for x in lst:
#      → 7.2     if x % 2 == 0:
#      → 7.2         result.append(x)
#      → 7.3 return result
#   8. def filter_odd_numbers(lst) → stored in memory, not executed
#   9. print(filter_odd_numbers(...))
#      → calls filter_odd_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 9.1 result = []
#      → 9.2 for x in lst:
#      → 9.2     if x % 2 == 0:
#      → 9.2         result.append(x)
#      → 9.3 return result
#   10. def filter_odd_numbers(lst) → stored in memory, not executed
#   11. print(filter_odd_numbers(...))
#      → calls filter_odd_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 11.1 result = []
#      → 11.2 for x in lst:
#      → 11.2     if x % 2 == 0:
#      → 11.2         result.append(x)
#      → 11.3 return result
#   12. def is_even(x) → stored in memory, not executed
#   13. def is_even(x) → stored in memory, not executed
#   14. x = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(is_even(...))
#      → calls is_even(x) with x=3
#      → 17.1 return x % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_odd_numbers, is_even) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_odd_numbers(...)) → filter_odd_numbers() runs first → value passed to print()
#     print(filter_odd_numbers(...)) → filter_odd_numbers() runs first → value passed to print()
#     print(filter_odd_numbers(...)) → filter_odd_numbers() runs first → value passed to print()
#     x = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_even(...)) → is_even() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 272:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_non_negative(x) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: < >=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_negative_numbers(lst) → each argument is evaluated first, then the function body runs
#   filter_negative_numbers(?) runs first → then print() outputs the result
#   is_non_negative(x) → each argument is evaluated first, then the function body runs
#   is_non_negative(3) runs first → then print() outputs the result
#   x >= 0 → evaluated as True or False (determines which branch runs)
#   filter_negative_numbers(?) → each argument is evaluated first, then the function body runs
#   is_non_negative(3) → each argument is evaluated first, then the function body runs
#   x < 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_non_negative(x) → stored in memory, not executed
#   2. def filter_negative_numbers(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_negative_numbers(lst) → stored in memory, not executed
#   7. print(filter_negative_numbers(...))
#      → calls filter_negative_numbers(lst) with lst=[?, ?, 0, 1, 2, 3]
#      → 7.1 result = []
#      → 7.2 for x in lst:
#      → 7.2     if x >= 0:
#      → 7.2         result.append(x)
#      → 7.3 return result
#   8. def filter_negative_numbers(lst) → stored in memory, not executed
#   9. print(filter_negative_numbers(...))
#      → calls filter_negative_numbers(lst) with lst=[?, ?, 0, 1, 2, 3]
#      → 9.1 result = []
#      → 9.2 for x in lst:
#      → 9.2     if x >= 0:
#      → 9.2         result.append(x)
#      → 9.3 return result
#   10. def filter_negative_numbers(lst) → stored in memory, not executed
#   11. print(filter_negative_numbers(...))
#      → calls filter_negative_numbers(lst) with lst=[?, ?, 0, 1, 2, 3]
#      → 11.1 result = []
#      → 11.2 for x in lst:
#      → 11.2     if x >= 0:
#      → 11.2         result.append(x)
#      → 11.3 return result
#   12. def is_non_negative(x) → stored in memory, not executed
#   13. def is_non_negative(x) → stored in memory, not executed
#   14. x = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(is_non_negative(...))
#      → calls is_non_negative(x) with x=3
#      → 17.1 return not x < 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_non_negative, filter_negative_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_negative_numbers(...)) → filter_negative_numbers() runs first → value passed to print()
#     print(filter_negative_numbers(...)) → filter_negative_numbers() runs first → value passed to print()
#     print(filter_negative_numbers(...)) → filter_negative_numbers() runs first → value passed to print()
#     x = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_non_negative(...)) → is_non_negative() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 274:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else | while
# Operators: - < != ==
# Built-in functions: filter, is_pal, len, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_palindromes(lst) → each argument is evaluated first, then the function body runs
#   filter_palindromes(?) runs first → then print() outputs the result
#   is_palindrome(s) → each argument is evaluated first, then the function body runs
#   is_palindrome(?) runs first → then print() outputs the result
#   s == ? → evaluated as True or False (determines which branch runs)
#   filter_palindromes(?) → each argument is evaluated first, then the function body runs
#   is_palindrome(?) → each argument is evaluated first, then the function body runs
#   left < right → evaluated as True or False (determines which branch runs)
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? != ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(s) → stored in memory, not executed
#   2. def filter_palindromes(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_palindromes(lst) → stored in memory, not executed
#   7. print(filter_palindromes(...))
#      → calls filter_palindromes(lst) with lst=['madam', 'hello', 'racecar', 'world', 'level']
#      → 7.1 def is_pal(s):
#      → 7.1     left, right = (0, len(s) - 1)
#      → 7.1     while left < right:
#      → 7.1         if s[left] != s[right]:
#      → 7.1             return False
#      → 7.1         left += 1
#      → 7.1         right -= 1
#      → 7.1     return True
#      → 7.2 return [s for s in lst if is_pal(s)]
#   8. def filter_palindromes(lst) → stored in memory, not executed
#   9. print(filter_palindromes(...))
#      → calls filter_palindromes(lst) with lst=['madam', 'hello', 'racecar', 'world', 'level']
#      → 9.1 def is_pal(s):
#      → 9.1     left, right = (0, len(s) - 1)
#      → 9.1     while left < right:
#      → 9.1         if s[left] != s[right]:
#      → 9.1             return False
#      → 9.1         left += 1
#      → 9.1         right -= 1
#      → 9.1     return True
#      → 9.2 return [s for s in lst if is_pal(s)]
#   10. def filter_palindromes(lst) → stored in memory, not executed
#   11. print(filter_palindromes(...))
#      → calls filter_palindromes(lst) with lst=['madam', 'hello', 'racecar', 'world', 'level']
#      → 11.1 def is_pal(s):
#      → 11.1     left, right = (0, len(s) - 1)
#      → 11.1     while left < right:
#      → 11.1         if s[left] != s[right]:
#      → 11.1             return False
#      → 11.1         left += 1
#      → 11.1         right -= 1
#      → 11.1     return True
#      → 11.2 return [s for s in lst if is_pal(s)]
#   12. def is_palindrome(s) → stored in memory, not executed
#   13. def is_palindrome(s) → stored in memory, not executed
#   14. s = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. print(is_palindrome(...))
#      → calls is_palindrome(s) with s=[4, 1, 8, 6, 3]
#      → 17.1 return s == s[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome, filter_palindromes) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_palindromes(...)) → filter_palindromes() runs first → value passed to print()
#     print(filter_palindromes(...)) → filter_palindromes() runs first → value passed to print()
#     print(filter_palindromes(...)) → filter_palindromes() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 276:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_divisible(numbers, divisor) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_divisible(numbers, divisor) → each argument is evaluated first, then the function body runs
#   filter_divisible(?, 5) runs first → then print() outputs the result
#   filter_divisible(?, 25) runs first → then print() outputs the result
#   filter_divisible(?, 5) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   filter_divisible(?, 25) → each argument is evaluated first, then the function body runs
#   n % divisor → Python looks up n, then divisor, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_divisible(numbers, divisor) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. divisor = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_divisible(numbers, divisor) → stored in memory, not executed
#   7. print(filter_divisible(...))
#      → calls filter_divisible(numbers, divisor) with numbers=[10, 15, 20, 25, 30, 35, 40], divisor=5
#      → 7.1 return n % divisor == 0
#   8. def filter_divisible(numbers, divisor) → stored in memory, not executed
#   9. print(filter_divisible(...))
#      → calls filter_divisible(numbers, divisor) with numbers=[10, 15, 20, 25, 30, 35, 40], divisor=5
#      → 9.1 return n % divisor == 0
#   10. def filter_divisible(numbers, divisor) → stored in memory, not executed
#   11. print(filter_divisible(...))
#      → calls filter_divisible(numbers, divisor) with numbers=[10, 15, 20, 25, 30, 35, 40], divisor=5
#      → 11.1 return n % divisor == 0
#   12. def filter_divisible(numbers, divisor) → stored in memory, not executed
#   13. def filter_divisible(numbers, divisor) → stored in memory, not executed
#   14. numbers = ... → assigns value to variable
#   15. divisor = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(filter_divisible(...))
#      → calls filter_divisible(numbers, divisor) with numbers=[4, 1, 8, 6, 3], divisor=25
#      → 18.1 return n % divisor == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_divisible) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     divisor = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_divisible(...)) → filter_divisible() runs first → value passed to print()
#     print(filter_divisible(...)) → filter_divisible() runs first → value passed to print()
#     print(filter_divisible(...)) → filter_divisible() runs first → value passed to print()
#     numbers = ... → stored in memory
#     divisor = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_divisible(...)) → filter_divisible() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 277:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_not_divisible(num, divisor) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_not_divisible(num, divisor) → each argument is evaluated first, then the function body runs
#   is_not_divisible(25, 25) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   is_not_divisible(25, 25) → each argument is evaluated first, then the function body runs
#   num % divisor → Python looks up num, then divisor, then applies %
#   n % divisor → Python looks up n, then divisor, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_not_divisible(num, divisor) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. divisor = ... → assigns value to variable
#   4. filtered_numbers = ... → assigns value to variable
#   5. print(filtered_numbers)
#   6. def is_not_divisible(num, divisor) → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. divisor = ... → assigns value to variable
#   9. filtered_numbers = ... → assigns value to variable
#   10. print(filtered_numbers)
#   11. def is_not_divisible(num, divisor) → stored in memory, not executed
#   12. numbers = ... → assigns value to variable
#   13. divisor = ... → assigns value to variable
#   14. filtered_numbers = ... → assigns value to variable
#   15. print(filtered_numbers)
#   16. def is_not_divisible(num, divisor) → stored in memory, not executed
#   17. numbers = ... → assigns value to variable
#   18. divisor = ... → assigns value to variable
#   19. filtered_numbers = ... → assigns value to variable
#   20. for n in numbers → loops over each item
#   21. print(filtered_numbers)
#   22. def is_not_divisible(num, divisor) → stored in memory, not executed
#   23. def is_not_divisible(num, divisor) → stored in memory, not executed
#   24. num = ... → assigns value to variable
#   25. divisor = ... → assigns value to variable
#   26. result = ... → assigns value to variable
#   27. print(result)
#   28. print(is_not_divisible(...))
#      → calls is_not_divisible(num, divisor) with num=25, divisor=25
#      → 28.1 return num % divisor != 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_not_divisible) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     divisor = ... → stored in memory
#     filtered_numbers = ... → stored in memory
#     print(filtered_numbers)
#     numbers = ... → stored in memory
#     divisor = ... → stored in memory
#     filtered_numbers = ... → stored in memory
#     print(filtered_numbers)
#     numbers = ... → stored in memory
#     divisor = ... → stored in memory
#     filtered_numbers = ... → stored in memory
#     print(filtered_numbers)
#     numbers = ... → stored in memory
#     divisor = ... → stored in memory
#     filtered_numbers = ... → stored in memory
#     for n in numbers → runs body once per item
#     print(filtered_numbers)
#     num = ... → stored in memory
#     divisor = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_not_divisible(...)) → is_not_divisible() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 278:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_non_empty(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: !=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_non_empty(strings) → each argument is evaluated first, then the function body runs
#   filter_non_empty(?) runs first → then print() outputs the result
#   filter_non_empty('world') runs first → then print() outputs the result
#   filter_non_empty(?) → each argument is evaluated first, then the function body runs
#   s != '' → evaluated as True or False (determines which branch runs)
#   filter_non_empty('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_non_empty(strings) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def filter_non_empty(strings) → stored in memory, not executed
#   6. print(filter_non_empty(...))
#      → calls filter_non_empty(strings) with strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#      → 6.1 return s != ''
#   7. def filter_non_empty(strings) → stored in memory, not executed
#   8. print(filter_non_empty(...))
#      → calls filter_non_empty(strings) with strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#      → 8.1 return s != ''
#   9. def filter_non_empty(strings) → stored in memory, not executed
#   10. print(filter_non_empty(...))
#      → calls filter_non_empty(strings) with strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#      → 10.1 return s != ''
#   11. def filter_non_empty(strings) → stored in memory, not executed
#   12. print(filter_non_empty(...))
#      → calls filter_non_empty(strings) with strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#      → 12.1 return s != ''
#   13. def filter_non_empty(strings) → stored in memory, not executed
#   14. def filter_non_empty(strings) → stored in memory, not executed
#   15. strings = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(filter_non_empty(...))
#      → calls filter_non_empty(strings) with strings='world'
#      → 18.1 return s != ''
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_non_empty) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_non_empty(...)) → filter_non_empty() runs first → value passed to print()
#     print(filter_non_empty(...)) → filter_non_empty() runs first → value passed to print()
#     print(filter_non_empty(...)) → filter_non_empty() runs first → value passed to print()
#     print(filter_non_empty(...)) → filter_non_empty() runs first → value passed to print()
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_non_empty(...)) → filter_non_empty() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 279:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_prime(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + < > != ** == >= and
# Built-in functions: all, filter, int, is_prime, list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_prime(numbers) → each argument is evaluated first, then the function body runs
#   filter_prime(?) runs first → then print() outputs the result
#   filter_prime(?) → each argument is evaluated first, then the function body runs
#   n < 2 → evaluated as True or False (determines which branch runs)
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n % i → Python looks up n, then i, then applies %
#   n > 1 → evaluated as True or False (determines which branch runs)
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#   n >= 2 → evaluated as True or False (determines which branch runs)
#   ? != 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_prime(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def filter_prime(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. def filter_prime(numbers) → stored in memory, not executed
#   10. numbers = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. def filter_prime(numbers) → stored in memory, not executed
#   14. numbers = ... → assigns value to variable
#   15. result = ... → assigns value to variable
#   16. print(result)
#   17. def filter_prime(numbers) → stored in memory, not executed
#   18. def filter_prime(numbers) → stored in memory, not executed
#   19. numbers = ... → assigns value to variable
#   20. result = ... → assigns value to variable
#   21. print(result)
#   22. print(filter_prime(...))
#      → calls filter_prime(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 22.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_prime(...)) → filter_prime() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 280:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_floats(items) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: filter, isinstance, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_floats(items) → each argument is evaluated first, then the function body runs
#   filter_floats(?) runs first → then print() outputs the result
#   filter_floats(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_floats(items) → stored in memory, not executed
#   2. items = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def filter_floats(items) → stored in memory, not executed
#   6. print(filter_floats(...))
#      → calls filter_floats(items) with items=[1, 2.5, 'apple', 3.7, 10, 8.9, 7, 4.0]
#      → 6.1 return not isinstance(x, float)
#   7. def filter_floats(items) → stored in memory, not executed
#   8. print(filter_floats(...))
#      → calls filter_floats(items) with items=[1, 2.5, 'apple', 3.7, 10, 8.9, 7, 4.0]
#      → 8.1 return not isinstance(x, float)
#   9. def filter_floats(items) → stored in memory, not executed
#   10. print(filter_floats(...))
#      → calls filter_floats(items) with items=[1, 2.5, 'apple', 3.7, 10, 8.9, 7, 4.0]
#      → 10.1 return not isinstance(x, float)
#   11. def filter_floats(items) → stored in memory, not executed
#   12. def filter_floats(items) → stored in memory, not executed
#   13. items = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(filter_floats(...))
#      → calls filter_floats(items) with items=[2, 8, 3]
#      → 16.1 return not isinstance(x, float)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_floats) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_floats(...)) → filter_floats() runs first → value passed to print()
#     print(filter_floats(...)) → filter_floats() runs first → value passed to print()
#     print(filter_floats(...)) → filter_floats() runs first → value passed to print()
#     items = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_floats(...)) → filter_floats() runs first → value passed to print()
#
#   Loop: takes one item from items each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 281:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_uppercase(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_uppercase(strings) → each argument is evaluated first, then the function body runs
#   filter_uppercase(?) runs first → then print() outputs the result
#   filter_uppercase('world') runs first → then print() outputs the result
#   filter_uppercase(?) → each argument is evaluated first, then the function body runs
#   filter_uppercase('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_uppercase(strings) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def filter_uppercase(strings) → stored in memory, not executed
#   6. print(filter_uppercase(...))
#      → calls filter_uppercase(strings) with strings=['HELLO', 'world', 'PYTHON', 'code', 'CHATGPT', 'example']
#      → 6.1 return not s.isupper()
#   7. def filter_uppercase(strings) → stored in memory, not executed
#   8. print(filter_uppercase(...))
#      → calls filter_uppercase(strings) with strings=['HELLO', 'world', 'PYTHON', 'code', 'CHATGPT', 'example']
#      → 8.1 return not s.isupper()
#   9. def filter_uppercase(strings) → stored in memory, not executed
#   10. print(filter_uppercase(...))
#      → calls filter_uppercase(strings) with strings=['HELLO', 'world', 'PYTHON', 'code', 'CHATGPT', 'example']
#      → 10.1 return not s.isupper()
#   11. def filter_uppercase(strings) → stored in memory, not executed
#   12. def filter_uppercase(strings) → stored in memory, not executed
#   13. strings = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(filter_uppercase(...))
#      → calls filter_uppercase(strings) with strings='world'
#      → 16.1 return not s.isupper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_uppercase(...)) → filter_uppercase() runs first → value passed to print()
#     print(filter_uppercase(...)) → filter_uppercase() runs first → value passed to print()
#     print(filter_uppercase(...)) → filter_uppercase() runs first → value passed to print()
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_uppercase(...)) → filter_uppercase() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 282:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_uppercase_again(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_uppercase_again(strings) → each argument is evaluated first, then the function body runs
#   filter_uppercase_again(?) runs first → then print() outputs the result
#   filter_uppercase_again('world') runs first → then print() outputs the result
#   filter_uppercase_again(?) → each argument is evaluated first, then the function body runs
#   filter_uppercase_again('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_uppercase_again(strings) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def filter_uppercase_again(strings) → stored in memory, not executed
#   6. print(filter_uppercase_again(...))
#      → calls filter_uppercase_again(strings) with strings=['HELLO', 'WORLD', 'python', 'CHATGPT', 'openai', 'example']
#      → 6.1 return not s.isupper()
#   7. def filter_uppercase_again(strings) → stored in memory, not executed
#   8. print(filter_uppercase_again(...))
#      → calls filter_uppercase_again(strings) with strings=['HELLO', 'WORLD', 'python', 'CHATGPT', 'openai', 'example']
#      → 8.1 return not s.isupper()
#   9. def filter_uppercase_again(strings) → stored in memory, not executed
#   10. print(filter_uppercase_again(...))
#      → calls filter_uppercase_again(strings) with strings=['HELLO', 'WORLD', 'python', 'CHATGPT', 'openai', 'example']
#      → 10.1 return not s.isupper()
#   11. def filter_uppercase_again(strings) → stored in memory, not executed
#   12. def filter_uppercase_again(strings) → stored in memory, not executed
#   13. strings = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(filter_uppercase_again(...))
#      → calls filter_uppercase_again(strings) with strings='world'
#      → 16.1 return not s.isupper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_uppercase_again) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_uppercase_again(...)) → filter_uppercase_again() runs first → value passed to print()
#     print(filter_uppercase_again(...)) → filter_uppercase_again() runs first → value passed to print()
#     print(filter_uppercase_again(...)) → filter_uppercase_again() runs first → value passed to print()
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_uppercase_again(...)) → filter_uppercase_again() runs first → value passed to print()
#
#   Loop: takes one item from strings each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 283:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_non_none(items) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: is not
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_non_none(items) → each argument is evaluated first, then the function body runs
#   filter_non_none(?) runs first → then print() outputs the result
#   filter_non_none(?) → each argument is evaluated first, then the function body runs
#   x  None → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_non_none(items) → stored in memory, not executed
#   2. items = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def filter_non_none(items) → stored in memory, not executed
#   6. print(filter_non_none(...))
#      → calls filter_non_none(items) with items=[1, None, 'example', None, 3.14, '', None]
#      → 6.1 return x is not None
#   7. def filter_non_none(items) → stored in memory, not executed
#   8. print(filter_non_none(...))
#      → calls filter_non_none(items) with items=[1, None, 'example', None, 3.14, '', None]
#      → 8.1 return x is not None
#   9. def filter_non_none(items) → stored in memory, not executed
#   10. print(filter_non_none(...))
#      → calls filter_non_none(items) with items=[1, None, 'example', None, 3.14, '', None]
#      → 10.1 return x is not None
#   11. def filter_non_none(items) → stored in memory, not executed
#   12. def filter_non_none(items) → stored in memory, not executed
#   13. items = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(filter_non_none(...))
#      → calls filter_non_none(items) with items=[2, 8, 3]
#      → 16.1 return x is not None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_non_none) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_non_none(...)) → filter_non_none() runs first → value passed to print()
#     print(filter_non_none(...)) → filter_non_none() runs first → value passed to print()
#     print(filter_non_none(...)) → filter_non_none() runs first → value passed to print()
#     items = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_non_none(...)) → filter_non_none() runs first → value passed to print()
#
#   Loop: takes one item from items each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 284:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_by_substring(items, substring) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_by_substring(items, substring) → each argument is evaluated first, then the function body runs
#   filter_by_substring(?, 'apple') runs first → then print() outputs the result
#   filter_by_substring(?, 'world') runs first → then print() outputs the result
#   filter_by_substring(?, 'apple') → each argument is evaluated first, then the function body runs
#   substring not in s → evaluated as True or False (determines which branch runs)
#   filter_by_substring(?, 'world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_by_substring(items, substring) → stored in memory, not executed
#   2. items = ... → assigns value to variable
#   3. substring = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def filter_by_substring(items, substring) → stored in memory, not executed
#   7. print(filter_by_substring(...))
#      → calls filter_by_substring(items, substring) with items=['apple', 'banana', 'cherry', 'pineapple', 'grape'], substring='apple'
#      → 7.1 return substring not in s
#   8. def filter_by_substring(items, substring) → stored in memory, not executed
#   9. print(filter_by_substring(...))
#      → calls filter_by_substring(items, substring) with items=['apple', 'banana', 'cherry', 'pineapple', 'grape'], substring='apple'
#      → 9.1 return substring not in s
#   10. def filter_by_substring(items, substring) → stored in memory, not executed
#   11. print(filter_by_substring(...))
#      → calls filter_by_substring(items, substring) with items=['apple', 'banana', 'cherry', 'pineapple', 'grape'], substring='apple'
#      → 11.1 return substring not in s
#   12. def filter_by_substring(items, substring) → stored in memory, not executed
#   13. def filter_by_substring(items, substring) → stored in memory, not executed
#   14. items = ... → assigns value to variable
#   15. substring = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(filter_by_substring(...))
#      → calls filter_by_substring(items, substring) with items=[2, 8, 3], substring='world'
#      → 18.1 return substring not in s
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_by_substring) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     substring = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_by_substring(...)) → filter_by_substring() runs first → value passed to print()
#     print(filter_by_substring(...)) → filter_by_substring() runs first → value passed to print()
#     print(filter_by_substring(...)) → filter_by_substring() runs first → value passed to print()
#     items = ... → stored in memory
#     substring = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_by_substring(...)) → filter_by_substring() runs first → value passed to print()
#
#   Loop: takes one item from items each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 286:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_first_10_natural_numbers() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | while
# Operators: * + // <=
# Built-in functions: range, reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_first_10_natural_numbers() → each argument is evaluated first, then the function body runs
#   sum_first_10_natural_numbers() runs first → then print() outputs the result
#   i <= 10 → evaluated as True or False (determines which branch runs)
#   ? // 2 → Python looks up ?, then 2, then applies //
#   10 * 11 → Python looks up 10, then 11, then applies *
#   acc + x → Python looks up acc, then x, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_first_10_natural_numbers() → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sum_first_10_natural_numbers() → stored in memory, not executed
#   5. print(sum_first_10_natural_numbers(...))
#      → calls sum_first_10_natural_numbers() with 
#      → 5.1 return total
#   6. def sum_first_10_natural_numbers() → stored in memory, not executed
#   7. print(sum_first_10_natural_numbers(...))
#      → calls sum_first_10_natural_numbers() with 
#      → 7.1 return total
#   8. def sum_first_10_natural_numbers() → stored in memory, not executed
#   9. print(sum_first_10_natural_numbers(...))
#      → calls sum_first_10_natural_numbers() with 
#      → 9.1 return total
#   10. from functools import reduce
#   11. def sum_first_10_natural_numbers() → stored in memory, not executed
#   12. print(sum_first_10_natural_numbers(...))
#      → calls sum_first_10_natural_numbers() with 
#      → 12.1 return total
#   13. def sum_first_10_natural_numbers() → stored in memory, not executed
#   14. def sum_first_10_natural_numbers() → stored in memory, not executed
#   15. i = ... → assigns value to variable
#   16. total = ... → assigns value to variable
#   17. while ... → repeats as long as condition is true
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. i = ... → assigns value to variable
#   21. total = ... → assigns value to variable
#   22. while ... → repeats as long as condition is true
#   23. result = ... → assigns value to variable
#   24. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_first_10_natural_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     print(sum_first_10_natural_numbers(...)) → sum_first_10_natural_numbers() runs first → value passed to print()
#     print(sum_first_10_natural_numbers(...)) → sum_first_10_natural_numbers() runs first → value passed to print()
#     print(sum_first_10_natural_numbers(...)) → sum_first_10_natural_numbers() runs first → value passed to print()
#     print(sum_first_10_natural_numbers(...)) → sum_first_10_natural_numbers() runs first → value passed to print()
#     i = ... → stored in memory
#     total = ... → stored in memory
#     while → checks condition before each iteration
#     result = ... → stored in memory
#     print(result)
#     i = ... → stored in memory
#     total = ... → stored in memory
#     while → checks condition before each iteration
#     result = ... → stored in memory
#     print(result)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 289:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_digits(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: % + > // ==
# Built-in functions: abs, int, str, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_digits(number) → each argument is evaluated first, then the function body runs
#   sum_of_digits(12345) runs first → then print() outputs the result
#   sum_of_digits(25) runs first → then print() outputs the result
#   number > 0 → evaluated as True or False (determines which branch runs)
#   sum_of_digits(12345) → each argument is evaluated first, then the function body runs
#   number == 0 → evaluated as True or False (determines which branch runs)
#   ? + sum_of_digits(...) → Python looks up ?, then sum_of_digits(...), then applies +
#   sum_of_digits(25) → each argument is evaluated first, then the function body runs
#   number % 10 → Python looks up number, then 10, then applies %
#   sum_of_digits(number // 10) → each argument is evaluated first, then the function body runs
#   number // 10 → Python looks up number, then 10, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_digits(number) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(?)
#   5. def sum_of_digits(number) → stored in memory, not executed
#   6. print(sum_of_digits(...))
#      → calls sum_of_digits(number) with number=12345
#      → 6.1 return total
#   7. def sum_of_digits(number) → stored in memory, not executed
#   8. print(sum_of_digits(...))
#      → calls sum_of_digits(number) with number=12345
#      → 8.1 return total
#   9. def sum_of_digits(number) → stored in memory, not executed
#   10. print(sum_of_digits(...))
#      → calls sum_of_digits(number) with number=12345
#      → 10.1 return total
#   11. def sum_of_digits(number) → stored in memory, not executed
#   12. def sum_of_digits(number) → stored in memory, not executed
#   13. number = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(sum_of_digits(...))
#      → calls sum_of_digits(number) with number=25
#      → 16.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(?)
#     print(sum_of_digits(...)) → sum_of_digits() runs first → value passed to print()
#     print(sum_of_digits(...)) → sum_of_digits() runs first → value passed to print()
#     print(sum_of_digits(...)) → sum_of_digits() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_digits(...)) → sum_of_digits() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 290:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_number(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | while
# Operators: % * + < > //
# Built-in functions: abs, input, int, reversed, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_number(num) runs first → then print() outputs the result
#   reverse_number(12345) runs first → then print() outputs the result
#   reverse_number(n) → each argument is evaluated first, then the function body runs
#   reverse_number(15) runs first → then print() outputs the result
#   n > 0 → evaluated as True or False (determines which branch runs)
#   reverse_number(num) → each argument is evaluated first, then the function body runs
#   sign * int(...) → Python looks up sign, then int(...), then applies *
#   reverse_number(12345) → each argument is evaluated first, then the function body runs
#   rev * sign → Python looks up rev, then sign, then applies *
#   reverse_number(15) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   n // 10 → Python looks up n, then 10, then applies //
#   n < 0 → evaluated as True or False (determines which branch runs)
#   ? + int(...) → Python looks up ?, then int(...), then applies +
#   reversed_num * 10 → Python looks up reversed_num, then 10, then applies *
#   n % 10 → Python looks up n, then 10, then applies %
#   rev * 10 → Python looks up rev, then 10, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_number(n) → stored in memory, not executed
#   2. num = ... → assigns value to variable
#   3. print('Reversed number:', reverse_number(...))
#   4. def reverse_number(n) → stored in memory, not executed
#   5. print(reverse_number(...))
#      → calls reverse_number(n) with n=12345
#      → 5.1 return reversed_num
#   6. def reverse_number(n) → stored in memory, not executed
#   7. print(reverse_number(...))
#      → calls reverse_number(n) with n=12345
#      → 7.1 return reversed_num
#   8. def reverse_number(n) → stored in memory, not executed
#   9. print(reverse_number(...))
#      → calls reverse_number(n) with n=12345
#      → 9.1 return reversed_num
#   10. def reverse_number(n) → stored in memory, not executed
#   11. def reverse_number(n) → stored in memory, not executed
#   12. n = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(reverse_number(...))
#      → calls reverse_number(n) with n=15
#      → 15.1 return reversed_num
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     print('Reversed number:', reverse_number(...))
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_number(...)) → reverse_number() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 291:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: * + - > <=
# Built-in functions: range, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial(number_to_calculate) → each argument is evaluated first, then the function body runs
#   factorial(5) runs first → then print() outputs the result
#   factorial(number) → each argument is evaluated first, then the function body runs
#   factorial(25) runs first → then print() outputs the result
#   number > 1 → evaluated as True or False (determines which branch runs)
#   factorial(5) → each argument is evaluated first, then the function body runs
#   number <= 1 → evaluated as True or False (determines which branch runs)
#   number * factorial(...) → Python looks up number, then factorial(...), then applies *
#   factorial(25) → each argument is evaluated first, then the function body runs
#   number + 1 → Python looks up number, then 1, then applies +
#   factorial(number - 1) → each argument is evaluated first, then the function body runs
#   number - 1 → Python looks up number, then 1, then applies -
#   x * y → Python looks up x, then y, then applies *
#   factorial(num) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial(number) → stored in memory, not executed
#   2. number_to_calculate = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(?)
#   5. test_numbers = ... → assigns value to variable
#   6. for num in test_numbers → loops over each item
#      → 6.1 print(?)
#   7. def factorial(number) → stored in memory, not executed
#   8. print(factorial(...))
#      → calls factorial(number) with number=5
#      → 8.1 return result
#   9. import math
#   10. def factorial(number) → stored in memory, not executed
#   11. print(factorial(...))
#      → calls factorial(number) with number=5
#      → 11.1 return result
#   12. def factorial(number) → stored in memory, not executed
#   13. print(factorial(...))
#      → calls factorial(number) with number=5
#      → 13.1 return result
#   14. from functools import reduce
#   15. def factorial(number) → stored in memory, not executed
#   16. print(factorial(...))
#      → calls factorial(number) with number=5
#      → 16.1 return result
#   17. def factorial(number) → stored in memory, not executed
#   18. def factorial(number) → stored in memory, not executed
#   19. number = ... → assigns value to variable
#   20. result = ... → assigns value to variable
#   21. print(result)
#   22. print(factorial(...))
#      → calls factorial(number) with number=25
#      → 22.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number_to_calculate = ... → stored in memory
#     result = ... → stored in memory
#     print(?)
#     test_numbers = ... → stored in memory
#     for num in test_numbers → runs body once per item
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     print(factorial(...)) → factorial() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial(...)) → factorial() runs first → value passed to print()
#
#   Loop: takes one item from test_numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 293:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: % * + < != ** <= ==
# Built-in functions: all, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_prime(number) → each argument is evaluated first, then the function body runs
#   is_prime(25) runs first → then print() outputs the result
#   number < 2 → evaluated as True or False (determines which branch runs)
#   ? <= number → evaluated as True or False (determines which branch runs)
#   is_prime(num) → each argument is evaluated first, then the function body runs
#   number == 2 → evaluated as True or False (determines which branch runs)
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   is_prime(25) → each argument is evaluated first, then the function body runs
#   i * i → Python looks up i, then i, then applies *
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   number % 2 → Python looks up number, then 2, then applies %
#   number % i → Python looks up number, then i, then applies %
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   number ** 0.5 → Python looks up number, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(number) → stored in memory, not executed
#   2. test_numbers = ... → assigns value to variable
#   3. print('Prime number check:')
#   4. for num in test_numbers → loops over each item
#   5. def is_prime(number) → stored in memory, not executed
#   6. for num in [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29] → loops over each item
#      → 6.1 print(?)
#   7. def is_prime(number) → stored in memory, not executed
#   8. for num in [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29] → loops over each item
#      → 8.1 print(?)
#   9. def is_prime(number) → stored in memory, not executed
#   10. for num in [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29] → loops over each item
#      → 10.1 print(?)
#   11. def is_prime(number) → stored in memory, not executed
#   12. def is_prime(number) → stored in memory, not executed
#   13. number = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(is_prime(...))
#      → calls is_prime(number) with number=25
#      → 16.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     test_numbers = ... → stored in memory
#     print('Prime number check:')
#     for num in test_numbers → runs body once per item
#     for num in [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29] → runs body once per item
#     for num in [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29] → runs body once per item
#     for num in [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29] → runs body once per item
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#
#   Loop: takes one item from test_numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 294:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_digits(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: + < > ==
# Built-in functions: abs, len, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_digits(12345) runs first → then print() outputs the result
#   count_digits(?) runs first → then print() outputs the result
#   count_digits(number) → each argument is evaluated first, then the function body runs
#   count_digits(25) runs first → then print() outputs the result
#   number > 0 → evaluated as True or False (determines which branch runs)
#   num < 0 → evaluated as True or False (determines which branch runs)
#   count_digits(12345) → each argument is evaluated first, then the function body runs
#   count_digits(?) → each argument is evaluated first, then the function body runs
#   number == 0 → evaluated as True or False (determines which branch runs)
#   math.floor(...) + 1 → Python looks up math.floor(...), then 1, then applies +
#   count_digits(25) → each argument is evaluated first, then the function body runs
#   count_digits(num) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_digits(number) → stored in memory, not executed
#   2. test_numbers = ... → assigns value to variable
#   3. print('Counting digits:')
#   4. for num in test_numbers → loops over each item
#   5. def count_digits(number) → stored in memory, not executed
#   6. print(count_digits(...))
#      → calls count_digits(number) with number=12345
#      → 6.1 return count
#   7. print(count_digits(...))
#      → calls count_digits(number) with number=?
#      → 7.1 return count
#   8. import math
#   9. def count_digits(number) → stored in memory, not executed
#   10. print(count_digits(...))
#      → calls count_digits(number) with number=12345
#      → 10.1 return count
#   11. print(count_digits(...))
#      → calls count_digits(number) with number=?
#      → 11.1 return count
#   12. def count_digits(number) → stored in memory, not executed
#   13. print(count_digits(...))
#      → calls count_digits(number) with number=12345
#      → 13.1 return count
#   14. print(count_digits(...))
#      → calls count_digits(number) with number=?
#      → 14.1 return count
#   15. def count_digits(number) → stored in memory, not executed
#   16. def count_digits(number) → stored in memory, not executed
#   17. number = ... → assigns value to variable
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. print(count_digits(...))
#      → calls count_digits(number) with number=25
#      → 20.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_digits) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     test_numbers = ... → stored in memory
#     print('Counting digits:')
#     for num in test_numbers → runs body once per item
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_digits(...)) → count_digits() runs first → value passed to print()
#
#   Loop: takes one item from test_numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 295:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def gcd(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: % > != == and
# Built-in functions: abs, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   gcd(num1, num2) → each argument is evaluated first, then the function body runs
#   gcd(56, 98) runs first → then print() outputs the result
#   gcd(a, b) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) runs first → then print() outputs the result
#   gcd(56, 98) → each argument is evaluated first, then the function body runs
#   b == 0 → evaluated as True or False (determines which branch runs)
#   gcd(b, a % b) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) → each argument is evaluated first, then the function body runs
#   a != 0 → evaluated as True or False (determines which branch runs)
#   b != 0 → evaluated as True or False (determines which branch runs)
#   a > b → evaluated as True or False (determines which branch runs)
#   a % b → Python looks up a, then b, then applies %
#   b % a → Python looks up b, then a, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def gcd(a, b) → stored in memory, not executed
#   2. num1 = ... → assigns value to variable
#   3. num2 = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(?)
#   6. test_cases = ... → assigns value to variable
#   7. print('Testing additional cases:')
#   8. for case in test_cases → loops over each item
#      → 8.1 ? = ...
#      → 8.2 result = ...
#      → 8.3 print(?)
#   9. def gcd(a, b) → stored in memory, not executed
#   10. print(gcd(...))
#      → calls gcd(a, b) with a=56, b=98
#      → 10.1 return a
#   11. import math
#   12. def gcd(a, b) → stored in memory, not executed
#   13. print(gcd(...))
#      → calls gcd(a, b) with a=56, b=98
#      → 13.1 return a
#   14. def gcd(a, b) → stored in memory, not executed
#   15. print(gcd(...))
#      → calls gcd(a, b) with a=56, b=98
#      → 15.1 return a
#   16. def gcd(a, b) → stored in memory, not executed
#   17. def gcd(a, b) → stored in memory, not executed
#   18. a = ... → assigns value to variable
#   19. b = ... → assigns value to variable
#   20. result = ... → assigns value to variable
#   21. print(result)
#   22. print(gcd(...))
#      → calls gcd(a, b) with a=20, b=30
#      → 22.1 return a
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (gcd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num1 = ... → stored in memory
#     num2 = ... → stored in memory
#     result = ... → stored in memory
#     print(?)
#     test_cases = ... → stored in memory
#     print('Testing additional cases:')
#     for case in test_cases → runs body once per item
#     print(gcd(...)) → gcd() runs first → value passed to print()
#     print(gcd(...)) → gcd() runs first → value passed to print()
#     print(gcd(...)) → gcd() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(gcd(...)) → gcd() runs first → value passed to print()
#
#   Loop: takes one item from test_cases each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 297:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_elements(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | while
# Operators: + <
# Built-in functions: len, reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_elements(lst) runs first → then print() outputs the result
#   sum_elements(?) runs first → then print() outputs the result
#   sum_elements(lst) → each argument is evaluated first, then the function body runs
#   index < len(...) → evaluated as True or False (determines which branch runs)
#   sum_elements(?) → each argument is evaluated first, then the function body runs
#   acc + x → Python looks up acc, then x, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_elements(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sum_elements(...))
#      → calls sum_elements(lst) with lst=lst
#      → 3.1 return total
#   4. def sum_elements(lst) → stored in memory, not executed
#   5. print(sum_elements(...))
#      → calls sum_elements(lst) with lst=[1, 2, 3, 4, 5]
#      → 5.1 return total
#   6. def sum_elements(lst) → stored in memory, not executed
#   7. print(sum_elements(...))
#      → calls sum_elements(lst) with lst=[1, 2, 3, 4, 5]
#      → 7.1 return total
#   8. from functools import reduce
#   9. def sum_elements(lst) → stored in memory, not executed
#   10. print(sum_elements(...))
#      → calls sum_elements(lst) with lst=[1, 2, 3, 4, 5]
#      → 10.1 return total
#   11. def sum_elements(lst) → stored in memory, not executed
#   12. def sum_elements(lst) → stored in memory, not executed
#   13. lst = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(sum_elements(...))
#      → calls sum_elements(lst) with lst=[1, 3, 5, 7]
#      → 16.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sum_elements(...)) → sum_elements() runs first → value passed to print()
#     print(sum_elements(...)) → sum_elements() runs first → value passed to print()
#     print(sum_elements(...)) → sum_elements() runs first → value passed to print()
#     print(sum_elements(...)) → sum_elements() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_elements(...)) → sum_elements() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 298:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: < >
# Built-in functions: len, max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) → each argument is evaluated first, then the function body runs
#   find_largest_element(numbers_list) → each argument is evaluated first, then the function body runs
#   find_largest_element(additional_numbers_list) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   find_largest_element(?) runs first → then print() outputs the result
#   i < len(...) → evaluated as True or False (determines which branch runs)
#   main(?) → each argument is evaluated first, then the function body runs
#   find_largest_element(?) → each argument is evaluated first, then the function body runs
#   ? > largest → evaluated as True or False (determines which branch runs)
#   n > largest → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. main(lst) → calls main(lst) with lst=lst
#      → 3.1 return max(lst, key=len)
#   4. def find_largest_element(numbers) → stored in memory, not executed
#   5. numbers_list = ... → assigns value to variable
#   6. largest_number = ... → assigns value to variable
#   7. print(?)
#   8. additional_numbers_list = ... → assigns value to variable
#   9. largest_number_in_negative = ... → assigns value to variable
#   10. print(?)
#   11. def main(lst) → stored in memory, not executed
#   12. print(main(...))
#      → calls main(lst) with lst=['tree', 'cliff', 'mountain', 'tree', 'tree', 'grassy']
#      → 12.1 return max(lst, key=len)
#   13. def find_largest_element(numbers) → stored in memory, not executed
#   14. print(find_largest_element(...))
#      → calls find_largest_element(numbers) with numbers=[5, 12, 3, 8, 20, 15]
#      → 14.1 return sorted(numbers)[-1]
#   15. def find_largest_element(numbers) → stored in memory, not executed
#   16. print(find_largest_element(...))
#      → calls find_largest_element(numbers) with numbers=[5, 12, 3, 8, 20, 15]
#      → 16.1 return sorted(numbers)[-1]
#   17. def find_largest_element(numbers) → stored in memory, not executed
#   18. print(find_largest_element(...))
#      → calls find_largest_element(numbers) with numbers=[5, 12, 3, 8, 20, 15]
#      → 18.1 return sorted(numbers)[-1]
#   19. def main(lst) → stored in memory, not executed
#   20. def main(lst) → stored in memory, not executed
#   21. lst = ... → assigns value to variable
#   22. result = ... → assigns value to variable
#   23. print(result)
#   24. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 24.1 return max(lst, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, find_largest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     numbers_list = ... → stored in memory
#     largest_number = ... → stored in memory
#     print(?)
#     additional_numbers_list = ... → stored in memory
#     largest_number_in_negative = ... → stored in memory
#     print(?)
#     print(main(...)) → main() runs first → value passed to print()
#     print(find_largest_element(...)) → find_largest_element() runs first → value passed to print()
#     print(find_largest_element(...)) → find_largest_element() runs first → value passed to print()
#     print(find_largest_element(...)) → find_largest_element() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 299:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def smallest_element(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: <
# Built-in functions: len, min, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   smallest_element(lst) runs first → then print() outputs the result
#   smallest_element(?) runs first → then print() outputs the result
#   smallest_element(lst) → each argument is evaluated first, then the function body runs
#   i < len(...) → evaluated as True or False (determines which branch runs)
#   smallest_element(?) → each argument is evaluated first, then the function body runs
#   ? < smallest_word → evaluated as True or False (determines which branch runs)
#   item < smallest → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def smallest_element(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(smallest_element(...))
#      → calls smallest_element(lst) with lst=lst
#      → 3.1 return None
#   4. def smallest_element(lst) → stored in memory, not executed
#   5. print(smallest_element(...))
#      → calls smallest_element(lst) with lst=['hot', 'hotty', 'hotties']
#      → 5.1 return None
#   6. def smallest_element(lst) → stored in memory, not executed
#   7. print(smallest_element(...))
#      → calls smallest_element(lst) with lst=['hot', 'hotty', 'hotties']
#      → 7.1 return None
#   8. def smallest_element(lst) → stored in memory, not executed
#   9. print(smallest_element(...))
#      → calls smallest_element(lst) with lst=['hot', 'hotty', 'hotties']
#      → 9.1 return None
#   10. def smallest_element(lst) → stored in memory, not executed
#   11. def smallest_element(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(smallest_element(...))
#      → calls smallest_element(lst) with lst=[1, 3, 5, 7]
#      → 15.1 return None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (smallest_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(smallest_element(...)) → smallest_element() runs first → value passed to print()
#     print(smallest_element(...)) → smallest_element() runs first → value passed to print()
#     print(smallest_element(...)) → smallest_element() runs first → value passed to print()
#     print(smallest_element(...)) → smallest_element() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(smallest_element(...)) → smallest_element() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 304:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def ask_until_condition() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else | while
# Operators: > <=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ask_until_condition() → each argument is evaluated first, then the function body runs
#   int(...) <= 10 → evaluated as True or False (determines which branch runs)
#   int(...) > 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def ask_until_condition() → stored in memory, not executed
#   2. ask_until_condition() → calls ask_until_condition() with 
#      → 2.1 user_input = input('Enter a number greater than 10: ')
#      → 2.2 while int(user_input) <= 10:
#      → 2.2     user_input = input('Enter a number greater than 10: ')
#   3. def ask_until_condition() → stored in memory, not executed
#   4. ask_until_condition() → calls ask_until_condition() with 
#      → 4.1 user_input = input('Enter a number greater than 10: ')
#      → 4.2 while int(user_input) <= 10:
#      → 4.2     user_input = input('Enter a number greater than 10: ')
#   5. def ask_until_condition() → stored in memory, not executed
#   6. ask_until_condition() → calls ask_until_condition() with 
#      → 6.1 user_input = input('Enter a number greater than 10: ')
#      → 6.2 while int(user_input) <= 10:
#      → 6.2     user_input = input('Enter a number greater than 10: ')
#   7. def ask_until_condition() → stored in memory, not executed
#   8. ask_until_condition() → calls ask_until_condition() with 
#      → 8.1 user_input = input('Enter a number greater than 10: ')
#      → 8.2 while int(user_input) <= 10:
#      → 8.2     user_input = input('Enter a number greater than 10: ')
#   9. while ... → repeats as long as condition is true
#   10. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (ask_until_condition) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ask_until_condition() → jumps into ask_until_condition(), runs body, returns
#     ask_until_condition() → jumps into ask_until_condition(), runs body, returns
#     ask_until_condition() → jumps into ask_until_condition(), runs body, returns
#     ask_until_condition() → jumps into ask_until_condition(), runs body, returns
#     while → checks condition before each iteration
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 307:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min_max() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: < >
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min_max() → each argument is evaluated first, then the function body runs
#   number < smallest → evaluated as True or False (determines which branch runs)
#   number > largest → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min_max() → stored in memory, not executed
#   2. find_min_max() → calls find_min_max() with 
#      → 2.1 numbers = [int(value) for value in input('Enter numbers: ').split()]
#      → 2.2 smallest = numbers[0]
#      → 2.3 largest = numbers[0]
#      → 2.4 for number in numbers[1:]:
#      → 2.4     if number < smallest:
#      → 2.4         smallest = number
#      → 2.4     if number > largest:
#      → 2.4         largest = number
#      → 2.5 result = (smallest, largest)
#      → 2.6 return result
#   3. def find_min_max() → stored in memory, not executed
#   4. def find_min_max() → stored in memory, not executed
#   5. numbers = ... → assigns value to variable
#   6. smallest = ... → assigns value to variable
#   7. largest = ... → assigns value to variable
#   8. for number in ? → loops over each item
#   9. print(smallest, largest)
#   10. numbers = ... → assigns value to variable
#   11. smallest = ... → assigns value to variable
#   12. largest = ... → assigns value to variable
#   13. for number in ? → loops over each item
#   14. print(smallest, largest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_min_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     find_min_max() → jumps into find_min_max(), runs body, returns
#     numbers = ... → stored in memory
#     smallest = ... → stored in memory
#     largest = ... → stored in memory
#     for number in ? → runs body once per item
#     print(smallest, largest)
#     numbers = ... → stored in memory
#     smallest = ... → stored in memory
#     largest = ... → stored in memory
#     for number in ? → runs body once per item
#     print(smallest, largest)
#
#   Loop: takes one item from ? each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 308:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def group_by_first_letter() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   group_by_first_letter() → each argument is evaluated first, then the function body runs
#   first_letter not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def group_by_first_letter() → stored in memory, not executed
#   2. group_by_first_letter() → calls group_by_first_letter() with 
#      → 2.1 lst = input('type word list: ').split()
#      → 2.2 result = {}
#      → 2.3 for word in lst:
#      → 2.3     first_letter = word[0]
#      → 2.3     if first_letter not in result:
#      → 2.3         result[first_letter] = [word]
#      → 2.3     else:
#      → 2.3         result[first_letter].append(word)
#      → 2.4 result = result
#      → 2.5 return result
#   3. def group_by_first_letter() → stored in memory, not executed
#   4. def group_by_first_letter() → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. for word in lst → loops over each item
#      → 7.1 first_letter = ...
#   8. print(result)
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. for word in lst → loops over each item
#      → 11.1 first_letter = ...
#   12. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (group_by_first_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     group_by_first_letter() → jumps into group_by_first_letter(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     for word in lst → runs body once per item
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     for word in lst → runs body once per item
#     print(result)
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 310:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_longest_word(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_longest_word(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   find_longest_word(lst) → each argument is evaluated first, then the function body runs
#   find_longest_word(?) runs first → then print() outputs the result
#   find_longest_word(?) → each argument is evaluated first, then the function body runs
#   len(...) > count → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_longest_word(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_longest_word(...))
#      → calls find_longest_word(lst) with lst=lst
#      → 3.1 return longest_word
#   4. def main(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. main(lst) → calls main(lst) with lst=lst
#      → 6.1 return max(lst, key=len)
#   7. def find_longest_word(lst) → stored in memory, not executed
#   8. def find_longest_word(lst) → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_longest_word(...))
#      → calls find_longest_word(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return longest_word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, find_longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 311:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_longest_word(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_longest_word(lst) runs first → then print() outputs the result
#   find_longest_word(lst) → each argument is evaluated first, then the function body runs
#   find_longest_word(?) runs first → then print() outputs the result
#   find_longest_word(?) → each argument is evaluated first, then the function body runs
#   current_length > max_length → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_longest_word(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_longest_word(...))
#      → calls find_longest_word(lst) with lst=lst
#      → 3.1 return longest_word
#   4. def find_longest_word(lst) → stored in memory, not executed
#   5. def find_longest_word(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(find_longest_word(...))
#      → calls find_longest_word(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return longest_word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_longest_word(...)) → find_longest_word() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 312:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_largest_number() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: input, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_largest_number() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_largest_number() → stored in memory, not executed
#   2. second_largest_number() → calls second_largest_number() with 
#      → 2.1 user = input('Type numbers separated by spaces: ').split()
#      → 2.2 user_int = list(map(int, user))
#      → 2.3 user_int.sort()
#      → 2.4 second_largest = user_int[-2]
#      → 2.5 result = second_largest
#      → 2.6 return result
#   3. def second_largest_number() → stored in memory, not executed
#   4. def second_largest_number() → stored in memory, not executed
#   5. user = ... → assigns value to variable
#   6. user_int = ... → assigns value to variable
#   7. user_int.sort()
#   8. second_largest = ... → assigns value to variable
#   9. print(second_largest)
#   10. user = ... → assigns value to variable
#   11. user_int = ... → assigns value to variable
#   12. user_int.sort()
#   13. second_largest = ... → assigns value to variable
#   14. print(second_largest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_largest_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     second_largest_number() → jumps into second_largest_number(), runs body, returns
#     user = ... → stored in memory
#     user_int = ... → stored in memory
#     user_int.sort()
#     second_largest = ... → stored in memory
#     print(second_largest)
#     user = ... → stored in memory
#     user_int = ... → stored in memory
#     user_int.sort()
#     second_largest = ... → stored in memory
#     print(second_largest)

"""

"""
Problem 313:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_uppercase(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: - <=
# Built-in functions: chr, list, map, ord, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_uppercase(strings) → each argument is evaluated first, then the function body runs
#   convert_to_uppercase('world') runs first → then print() outputs the result
#   convert_to_uppercase('world') → each argument is evaluated first, then the function body runs
#   'a' <= c → evaluated as True or False (determines which branch runs)
#   'a' <= 'z' → evaluated as True or False (determines which branch runs)
#   ord(...) - 32 → Python looks up ord(...), then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_uppercase(strings) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. uppercase_strings = ... → assigns value to variable
#   4. print('Original list:', strings)
#   5. print('Uppercase list:', uppercase_strings)
#   6. def convert_to_uppercase(strings) → stored in memory, not executed
#   7. def convert_to_uppercase(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(convert_to_uppercase(...))
#      → calls convert_to_uppercase(strings) with strings='world'
#      → 11.1 return str(s).upper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     uppercase_strings = ... → stored in memory
#     print('Original list:', strings)
#     print('Uppercase list:', uppercase_strings)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_uppercase(...)) → convert_to_uppercase() runs first → value passed to print()

"""

"""
Problem 314:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_integers(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: int, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_integers(string_numbers) → each argument is evaluated first, then the function body runs
#   convert_to_integers(strings) → each argument is evaluated first, then the function body runs
#   convert_to_integers('world') runs first → then print() outputs the result
#   convert_to_integers('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_integers(strings) → stored in memory, not executed
#   2. string_numbers = ... → assigns value to variable
#   3. integer_numbers = ... → assigns value to variable
#   4. print('Original list of strings:', string_numbers)
#   5. print('Converted list of integers:', integer_numbers)
#   6. def convert_to_integers(strings) → stored in memory, not executed
#   7. def convert_to_integers(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(convert_to_integers(...))
#      → calls convert_to_integers(strings) with strings='world'
#      → 11.1 return int(s)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string_numbers = ... → stored in memory
#     integer_numbers = ... → stored in memory
#     print('Original list of strings:', string_numbers)
#     print('Converted list of integers:', integer_numbers)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_integers(...)) → convert_to_integers() runs first → value passed to print()

"""

"""
Problem 315:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_numbers(original_numbers) → each argument is evaluated first, then the function body runs
#   square_numbers(numbers) → each argument is evaluated first, then the function body runs
#   square_numbers(?) runs first → then print() outputs the result
#   square_numbers(?) → each argument is evaluated first, then the function body runs
#   x * x → Python looks up x, then x, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_numbers(numbers) → stored in memory, not executed
#   2. original_numbers = ... → assigns value to variable
#   3. squared_numbers = ... → assigns value to variable
#   4. print('Original list of numbers:', original_numbers)
#   5. print('Squared list of numbers:', squared_numbers)
#   6. def square_numbers(numbers) → stored in memory, not executed
#   7. def square_numbers(numbers) → stored in memory, not executed
#   8. numbers = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(square_numbers(...))
#      → calls square_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 11.1 return pow(x, x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     original_numbers = ... → stored in memory
#     squared_numbers = ... → stored in memory
#     print('Original list of numbers:', original_numbers)
#     print('Squared list of numbers:', squared_numbers)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_numbers(...)) → square_numbers() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 316:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_lengths(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_lengths(string_list) → each argument is evaluated first, then the function body runs
#   string_lengths(strings) → each argument is evaluated first, then the function body runs
#   string_lengths('world') runs first → then print() outputs the result
#   string_lengths('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_lengths(strings) → stored in memory, not executed
#   2. string_list = ... → assigns value to variable
#   3. lengths = ... → assigns value to variable
#   4. print('Original list of strings:', string_list)
#   5. print('Lengths of each string:', lengths)
#   6. def string_lengths(strings) → stored in memory, not executed
#   7. def string_lengths(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(string_lengths(...))
#      → calls string_lengths(strings) with strings='world'
#      → 11.1 return sum((1 for _ in s))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (string_lengths) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string_list = ... → stored in memory
#     lengths = ... → stored in memory
#     print('Original list of strings:', string_list)
#     print('Lengths of each string:', lengths)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_lengths(...)) → string_lengths() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 317:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fahrenheit_to_celsius(temperatures) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * - /
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fahrenheit_to_celsius(fahrenheit_temps) → each argument is evaluated first, then the function body runs
#   fahrenheit_to_celsius(temperatures) → each argument is evaluated first, then the function body runs
#   fahrenheit_to_celsius(25) runs first → then print() outputs the result
#   ? / 9 → Python looks up ?, then 9, then applies /
#   fahrenheit_to_celsius(25) → each argument is evaluated first, then the function body runs
#   ? * 5 → Python looks up ?, then 5, then applies *
#   f - 32 → Python looks up f, then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fahrenheit_to_celsius(temperatures) → stored in memory, not executed
#   2. fahrenheit_temps = ... → assigns value to variable
#   3. celsius_temps = ... → assigns value to variable
#   4. print('Temperatures in Fahrenheit:', fahrenheit_temps)
#   5. print('Temperatures in Celsius:', celsius_temps)
#   6. def fahrenheit_to_celsius(temperatures) → stored in memory, not executed
#   7. def fahrenheit_to_celsius(temperatures) → stored in memory, not executed
#   8. temperatures = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(fahrenheit_to_celsius(...))
#      → calls fahrenheit_to_celsius(temperatures) with temperatures=25
#      → 11.1 return (f - 32) * 5 / 9
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fahrenheit_to_celsius) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     fahrenheit_temps = ... → stored in memory
#     celsius_temps = ... → stored in memory
#     print('Temperatures in Fahrenheit:', fahrenheit_temps)
#     print('Temperatures in Celsius:', celsius_temps)
#     temperatures = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fahrenheit_to_celsius(...)) → fahrenheit_to_celsius() runs first → value passed to print()

"""

"""
Problem 319:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_five_to_number(x) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_five(lst) runs first → then print() outputs the result
#   add_five_to_number(x) → each argument is evaluated first, then the function body runs
#   add_five_to_number(3) runs first → then print() outputs the result
#   x + 5 → Python looks up x, then 5, then applies +
#   add_five(lst) → each argument is evaluated first, then the function body runs
#   add_five_to_number(3) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_five_to_number(x) → stored in memory, not executed
#   2. def add_five(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(add_five(...))
#      → calls add_five(lst) with lst=lst
#      → 4.1 return list(map(add_five_to_number, lst))
#   5. def add_five_to_number(x) → stored in memory, not executed
#   6. def add_five_to_number(x) → stored in memory, not executed
#   7. x = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(add_five_to_number(...))
#      → calls add_five_to_number(x) with x=3
#      → 10.1 import operator
#      → 10.2 return operator.add(x, 5)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_five, add_five_to_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(add_five(...)) → add_five() runs first → value passed to print()
#     x = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_five_to_number(...)) → add_five_to_number() runs first → value passed to print()

"""

"""
Problem 320:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: list, map, pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) → each argument is evaluated first, then the function body runs
#   square_roots(number_list) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   main(?) → each argument is evaluated first, then the function body runs
#   num ** 2 → Python looks up num, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. main(lst) → calls main(lst) with lst=lst
#      → 3.1 import math
#      → 3.2 return math.pow(num, 2)
#   4. import math
#   5. def square_roots(numbers) → stored in memory, not executed
#   6. number_list = ... → assigns value to variable
#   7. roots = ... → assigns value to variable
#   8. print('Original numbers:', number_list)
#   9. print('Square roots:', roots)
#   10. def main(lst) → stored in memory, not executed
#   11. def main(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 15.1 import math
#      → 15.2 return math.pow(num, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, square_roots) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     number_list = ... → stored in memory
#     roots = ... → stored in memory
#     print('Original numbers:', number_list)
#     print('Square roots:', roots)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 321:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lengths_of_strings(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lengths_of_strings(string_list) → each argument is evaluated first, then the function body runs
#   lengths_of_strings(strings) → each argument is evaluated first, then the function body runs
#   lengths_of_strings('world') runs first → then print() outputs the result
#   lengths_of_strings('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lengths_of_strings(strings) → stored in memory, not executed
#   2. string_list = ... → assigns value to variable
#   3. lengths = ... → assigns value to variable
#   4. print('Original strings:', string_list)
#   5. print('Lengths of strings:', lengths)
#   6. def lengths_of_strings(strings) → stored in memory, not executed
#   7. def lengths_of_strings(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(lengths_of_strings(...))
#      → calls lengths_of_strings(strings) with strings='world'
#      → 11.1 return sum((1 for _ in s))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lengths_of_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string_list = ... → stored in memory
#     lengths = ... → stored in memory
#     print('Original strings:', string_list)
#     print('Lengths of strings:', lengths)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lengths_of_strings(...)) → lengths_of_strings() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 322:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def normalize_scores(scores) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: - /
# Built-in functions: list, map, max, min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   normalize_scores(score_list) → each argument is evaluated first, then the function body runs
#   normalize_scores(scores) → each argument is evaluated first, then the function body runs
#   normalize_scores(?) runs first → then print() outputs the result
#   ? / ? → Python looks up ?, then ?, then applies /
#   normalize_scores(?) → each argument is evaluated first, then the function body runs
#   score - min_score → Python looks up score, then min_score, then applies -
#   max_score - min_score → Python looks up max_score, then min_score, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def normalize_scores(scores) → stored in memory, not executed
#   2. score_list = ... → assigns value to variable
#   3. normalized_scores = ... → assigns value to variable
#   4. print('Original scores:', score_list)
#   5. print('Normalized scores:', normalized_scores)
#   6. def normalize_scores(scores) → stored in memory, not executed
#   7. def normalize_scores(scores) → stored in memory, not executed
#   8. scores = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(normalize_scores(...))
#      → calls normalize_scores(scores) with scores=[4, 1, 8, 6, 3]
#      → 11.1 return (score - min_score) / (max_score - min_score)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (normalize_scores) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     score_list = ... → stored in memory
#     normalized_scores = ... → stored in memory
#     print('Original scores:', score_list)
#     print('Normalized scores:', normalized_scores)
#     scores = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(normalize_scores(...)) → normalize_scores() runs first → value passed to print()

"""

"""
Problem 323:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_strings(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: list, map, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_strings(string_list) → each argument is evaluated first, then the function body runs
#   reverse_strings(strings) → each argument is evaluated first, then the function body runs
#   reverse_strings('world') runs first → then print() outputs the result
#   reverse_strings('world') → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_strings(strings) → stored in memory, not executed
#   2. string_list = ... → assigns value to variable
#   3. reversed_strings = ... → assigns value to variable
#   4. print('Original strings:', string_list)
#   5. print('Reversed strings:', reversed_strings)
#   6. def reverse_strings(strings) → stored in memory, not executed
#   7. def reverse_strings(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(reverse_strings(...))
#      → calls reverse_strings(strings) with strings='world'
#      → 11.1 result = ''
#      → 11.2 for char in s:
#      → 11.2     result = char + result
#      → 11.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string_list = ... → stored in memory
#     reversed_strings = ... → stored in memory
#     print('Original strings:', string_list)
#     print('Reversed strings:', reversed_strings)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_strings(...)) → reverse_strings() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 324:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def double_numbers(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   double_numbers(number_list) → each argument is evaluated first, then the function body runs
#   double_numbers(numbers) → each argument is evaluated first, then the function body runs
#   double_numbers(?) runs first → then print() outputs the result
#   double_numbers(?) → each argument is evaluated first, then the function body runs
#   x * 2 → Python looks up x, then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def double_numbers(numbers) → stored in memory, not executed
#   2. number_list = ... → assigns value to variable
#   3. doubled_numbers = ... → assigns value to variable
#   4. print('Original numbers:', number_list)
#   5. print('Doubled numbers:', doubled_numbers)
#   6. def double_numbers(numbers) → stored in memory, not executed
#   7. def double_numbers(numbers) → stored in memory, not executed
#   8. numbers = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(double_numbers(...))
#      → calls double_numbers(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 11.1 return pow(x, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (double_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number_list = ... → stored in memory
#     doubled_numbers = ... → stored in memory
#     print('Original numbers:', number_list)
#     print('Doubled numbers:', doubled_numbers)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(double_numbers(...)) → double_numbers() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 325:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def strip_whitespace(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   strip_whitespace(string_list) → each argument is evaluated first, then the function body runs
#   strip_whitespace(strings) → each argument is evaluated first, then the function body runs
#   strip_whitespace('world') runs first → then print() outputs the result
#   strip_whitespace('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def strip_whitespace(strings) → stored in memory, not executed
#   2. string_list = ... → assigns value to variable
#   3. stripped_strings = ... → assigns value to variable
#   4. print('Original strings:', string_list)
#   5. print('Stripped strings:', stripped_strings)
#   6. def strip_whitespace(strings) → stored in memory, not executed
#   7. def strip_whitespace(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(strip_whitespace(...))
#      → calls strip_whitespace(strings) with strings='world'
#      → 11.1 return s.strip()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (strip_whitespace) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string_list = ... → stored in memory
#     stripped_strings = ... → stored in memory
#     print('Original strings:', string_list)
#     print('Stripped strings:', stripped_strings)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(strip_whitespace(...)) → strip_whitespace() runs first → value passed to print()

"""

"""
Problem 326:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def number_and_square(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number_and_square(numbers_list) → each argument is evaluated first, then the function body runs
#   number_and_square(numbers) → each argument is evaluated first, then the function body runs
#   number_and_square(?) runs first → then print() outputs the result
#   number_and_square(?) → each argument is evaluated first, then the function body runs
#   x * x → Python looks up x, then x, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def number_and_square(numbers) → stored in memory, not executed
#   2. numbers_list = ... → assigns value to variable
#   3. squared_tuples = ... → assigns value to variable
#   4. print('Numbers and their squares:', squared_tuples)
#   5. def number_and_square(numbers) → stored in memory, not executed
#   6. def number_and_square(numbers) → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(number_and_square(...))
#      → calls number_and_square(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 10.1 return (x, x * x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (number_and_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers_list = ... → stored in memory
#     squared_tuples = ... → stored in memory
#     print('Numbers and their squares:', squared_tuples)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(number_and_square(...)) → number_and_square() runs first → value passed to print()

"""

"""
Problem 327:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def to_title_case(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   to_title_case(words) → each argument is evaluated first, then the function body runs
#   to_title_case(strings) → each argument is evaluated first, then the function body runs
#   to_title_case('world') runs first → then print() outputs the result
#   to_title_case('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def to_title_case(strings) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def to_title_case(strings) → stored in memory, not executed
#   6. def to_title_case(strings) → stored in memory, not executed
#   7. strings = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(to_title_case(...))
#      → calls to_title_case(strings) with strings='world'
#      → 10.1 return str(s).title()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (to_title_case) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(to_title_case(...)) → to_title_case() runs first → value passed to print()

"""

"""
Problem 328:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_largest_number() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: input, int, len, list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_largest_number() → each argument is evaluated first, then the function body runs
#   len(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_largest_number() → stored in memory, not executed
#   2. second_largest_number() → calls second_largest_number() with 
#      → 2.1 user_input = input('Type numbers separated by spaces: ').split()
#      → 2.2 user_int = []
#      → 2.3 for num in user_input:
#      → 2.3     user_int.append(int(num))
#      → 2.4 unique_numbers = set(user_int)
#      → 2.5 unique_numbers = list(unique_numbers)
#      → 2.6 unique_numbers.sort()
#      → 2.7 if len(unique_numbers) > 1:
#      → 2.7     second_largest = unique_numbers[-2]
#      → 2.7 else:
#      → 2.7     second_largest = 'Not enough unique numbers'
#      → 2.8 result = second_largest
#      → 2.9 return result
#   3. def second_largest_number() → stored in memory, not executed
#   4. def second_largest_number() → stored in memory, not executed
#   5. user_input = ... → assigns value to variable
#   6. user_int = ... → assigns value to variable
#   7. for num in user_input → loops over each item
#      → 7.1 user_int.append(int(...))
#   8. unique_numbers = ... → assigns value to variable
#   9. unique_numbers = ... → assigns value to variable
#   10. unique_numbers.sort()
#   11. if ... → only one branch executes
#   12. print(second_largest)
#   13. user_input = ... → assigns value to variable
#   14. user_int = ... → assigns value to variable
#   15. for num in user_input → loops over each item
#      → 15.1 user_int.append(int(...))
#   16. unique_numbers = ... → assigns value to variable
#   17. unique_numbers = ... → assigns value to variable
#   18. unique_numbers.sort()
#   19. if ... → only one branch executes
#   20. print(second_largest)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_largest_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     second_largest_number() → jumps into second_largest_number(), runs body, returns
#     user_input = ... → stored in memory
#     user_int = ... → stored in memory
#     for num in user_input → runs body once per item
#     unique_numbers = ... → stored in memory
#     unique_numbers = ... → stored in memory
#     unique_numbers.sort()
#     if → evaluates condition, runs matching branch
#     print(second_largest)
#     user_input = ... → stored in memory
#     user_int = ... → stored in memory
#     for num in user_input → runs body once per item
#     unique_numbers = ... → stored in memory
#     unique_numbers = ... → stored in memory
#     unique_numbers.sort()
#     if → evaluates condition, runs matching branch
#     print(second_largest)
#
#   Loop: takes one item from user_input each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 329:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_string(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: input, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_string(lst) runs first → then print() outputs the result
#   type_smallest_biggest() → each argument is evaluated first, then the function body runs
#   list_string(numbers) → each argument is evaluated first, then the function body runs
#   list_string(?) runs first → then print() outputs the result
#   list_string(lst) → each argument is evaluated first, then the function body runs
#   list_string(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_string(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_string(...))
#      → calls list_string(numbers) with numbers=lst
#      → 3.1 return sort
#   4. def type_smallest_biggest() → stored in memory, not executed
#   5. type_smallest_biggest() → calls type_smallest_biggest() with 
#      → 5.1 user = input('type number list: ').split()
#      → 5.2 sort_number = sorted(user, key=int)
#      → 5.3 print(sort_number)
#   6. def list_string(numbers) → stored in memory, not executed
#   7. def list_string(numbers) → stored in memory, not executed
#   8. numbers = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(list_string(...))
#      → calls list_string(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 11.1 return sort
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_string, type_smallest_biggest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_string(...)) → list_string() runs first → value passed to print()
#     type_smallest_biggest() → jumps into type_smallest_biggest(), runs body, returns
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_string(...)) → list_string() runs first → value passed to print()

"""

"""
Problem 330:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_of_string_numbers(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: input, list, map, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_of_string_numbers(numbers) runs first → then print() outputs the result
#   type_biggest_smallest() → each argument is evaluated first, then the function body runs
#   list_of_string_numbers(n) → each argument is evaluated first, then the function body runs
#   list_of_string_numbers(15) runs first → then print() outputs the result
#   list_of_string_numbers(numbers) → each argument is evaluated first, then the function body runs
#   list_of_string_numbers(15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_of_string_numbers(n) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(list_of_string_numbers(...))
#      → calls list_of_string_numbers(n) with n=numbers
#      → 3.1 return sort
#   4. def type_biggest_smallest() → stored in memory, not executed
#   5. type_biggest_smallest() → calls type_biggest_smallest() with 
#      → 5.1 user = input('Type numbers: ').split()
#      → 5.2 int_list = list(map(int, user))
#      → 5.3 sorted_list = sorted(int_list, reverse=True)
#      → 5.4 print(sorted_list)
#   6. def list_of_string_numbers(n) → stored in memory, not executed
#   7. def list_of_string_numbers(n) → stored in memory, not executed
#   8. n = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(list_of_string_numbers(...))
#      → calls list_of_string_numbers(n) with n=15
#      → 11.1 return sort
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_of_string_numbers, type_biggest_smallest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(list_of_string_numbers(...)) → list_of_string_numbers() runs first → value passed to print()
#     type_biggest_smallest() → jumps into type_biggest_smallest(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_of_string_numbers(...)) → list_of_string_numbers() runs first → value passed to print()

"""

"""
Problem 331:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_index_and_element(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_index_and_element(lst) → each argument is evaluated first, then the function body runs
#   print_index_and_element(?) runs first → then print() outputs the result
#   print_index_and_element(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_index_and_element(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print_index_and_element(lst) → calls print_index_and_element(lst) with lst=lst
#      → 3.1 for index, element in enumerate(lst):
#      → 3.1     print(f'Index: {index}, Element: {element}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_index_and_element(...))
#      → calls print_index_and_element(lst) with lst=[1, 3, 5, 7]
#      → 9.1 for index, element in enumerate(lst):
#      → 9.1     print(f'Index: {index}, Element: {element}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_index_and_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print_index_and_element(lst) → jumps into print_index_and_element(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_index_and_element(...)) → print_index_and_element() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 332:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_index_element_tuples(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_index_element_tuples(lst) → each argument is evaluated first, then the function body runs
#   create_index_element_tuples(?) runs first → then print() outputs the result
#   create_index_element_tuples(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_index_element_tuples(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def create_index_element_tuples(lst) → stored in memory, not executed
#   6. def create_index_element_tuples(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(create_index_element_tuples(...))
#      → calls create_index_element_tuples(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_index_element_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_index_element_tuples(...)) → create_index_element_tuples() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 333:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_with_index(strings) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_with_index(strings) → each argument is evaluated first, then the function body runs
#   print_with_index('world') runs first → then print() outputs the result
#   print_with_index('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_with_index(strings) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. print_with_index(strings) → calls print_with_index(strings) with strings=strings
#      → 3.1 for index, value in enumerate(strings):
#      → 3.1     print(index, value)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_with_index(...))
#      → calls print_with_index(strings) with strings='world'
#      → 9.1 for index, value in enumerate(strings):
#      → 9.1     print(index, value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_with_index, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     print_with_index(strings) → jumps into print_with_index(), runs body, returns
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_with_index(...)) → print_with_index() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 334:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_to_dict(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_to_dict(lst) → each argument is evaluated first, then the function body runs
#   list_to_dict(?) runs first → then print() outputs the result
#   list_to_dict(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_to_dict(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def list_to_dict(lst) → stored in memory, not executed
#   6. def list_to_dict(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(list_to_dict(...))
#      → calls list_to_dict(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_to_dict) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_to_dict(...)) → list_to_dict() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 335:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_index(lst, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_index(lst, 'banana') → each argument is evaluated first, then the function body runs
#   find_index(lst, element) → each argument is evaluated first, then the function body runs
#   find_index(?, 25) runs first → then print() outputs the result
#   find_index(?, 25) → each argument is evaluated first, then the function body runs
#   value == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_index(lst, element) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. index = ... → assigns value to variable
#   4. print(index)
#   5. def find_index(lst, element) → stored in memory, not executed
#   6. def find_index(lst, element) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. element = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(find_index(...))
#      → calls find_index(lst, element) with lst=[1, 3, 5, 7], element=25
#      → 11.1 return index
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     index = ... → stored in memory
#     print(index)
#     lst = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_index(...)) → find_index() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 336:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_index_and_element_with_colon(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_index_and_element_with_colon(lst) → each argument is evaluated first, then the function body runs
#   print_index_and_element_with_colon(?) runs first → then print() outputs the result
#   print_index_and_element_with_colon(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_index_and_element_with_colon(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print_index_and_element_with_colon(lst) → calls print_index_and_element_with_colon(lst) with lst=lst
#      → 3.1 for index, element in enumerate(lst):
#      → 3.1     print(f'{index}: {element}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_index_and_element_with_colon(...))
#      → calls print_index_and_element_with_colon(lst) with lst=[1, 3, 5, 7]
#      → 9.1 for index, element in enumerate(lst):
#      → 9.1     print(f'{index}: {element}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_index_and_element_with_colon) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print_index_and_element_with_colon(lst) → jumps into print_index_and_element_with_colon(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_index_and_element_with_colon(...)) → print_index_and_element_with_colon() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 337:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_indexed_strings(lst, prefix) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_indexed_strings(lst, 'Item ') → each argument is evaluated first, then the function body runs
#   create_indexed_strings(lst, prefix) → each argument is evaluated first, then the function body runs
#   create_indexed_strings(?, 'Hi, ') runs first → then print() outputs the result
#   create_indexed_strings(?, 'Hi, ') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_indexed_strings(lst, prefix) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def create_indexed_strings(lst, prefix) → stored in memory, not executed
#   6. def create_indexed_strings(lst, prefix) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. prefix = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(create_indexed_strings(...))
#      → calls create_indexed_strings(lst, prefix) with lst=[1, 3, 5, 7], prefix='Hi, '
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_indexed_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     prefix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_indexed_strings(...)) → create_indexed_strings() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 338:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def modify_elements(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   modify_elements(lst) → each argument is evaluated first, then the function body runs
#   modify_elements(?) runs first → then print() outputs the result
#   modify_elements(?) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   index % 2 → Python looks up index, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def modify_elements(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def modify_elements(lst) → stored in memory, not executed
#   6. def modify_elements(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(modify_elements(...))
#      → calls modify_elements(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return lst
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (modify_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(modify_elements(...)) → modify_elements() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 340:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_odd_index_elements(lst) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: % !=
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_odd_index_elements(lst) → each argument is evaluated first, then the function body runs
#   print_odd_index_elements(?) runs first → then print() outputs the result
#   print_odd_index_elements(?) → each argument is evaluated first, then the function body runs
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   index % 2 → Python looks up index, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_odd_index_elements(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print_odd_index_elements(lst) → calls print_odd_index_elements(lst) with lst=lst
#      → 3.1 for index, element in enumerate(lst):
#      → 3.1     if index % 2 != 0:
#      → 3.1         print(element)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_odd_index_elements(...))
#      → calls print_odd_index_elements(lst) with lst=[1, 3, 5, 7]
#      → 9.1 for index, element in enumerate(lst):
#      → 9.1     if index % 2 != 0:
#      → 9.1         print(element)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_odd_index_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print_odd_index_elements(lst) → jumps into print_odd_index_elements(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_odd_index_elements(...)) → print_odd_index_elements() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 341:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def index_element_if_vowel(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   index_element_if_vowel(lst) → each argument is evaluated first, then the function body runs
#   index_element_if_vowel(?) runs first → then print() outputs the result
#   index_element_if_vowel(?) → each argument is evaluated first, then the function body runs
#   ? in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def index_element_if_vowel(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def index_element_if_vowel(lst) → stored in memory, not executed
#   6. def index_element_if_vowel(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(index_element_if_vowel(...))
#      → calls index_element_if_vowel(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (index_element_if_vowel) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(index_element_if_vowel(...)) → index_element_if_vowel() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 342:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_max_and_index(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_max_and_index(lst) → each argument is evaluated first, then the function body runs
#   find_max_and_index(?) runs first → then print() outputs the result
#   find_max_and_index(?) → each argument is evaluated first, then the function body runs
#   element > max_element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_max_and_index(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(?)
#   5. def find_max_and_index(lst) → stored in memory, not executed
#   6. def find_max_and_index(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(find_max_and_index(...))
#      → calls find_max_and_index(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return (max_index, max_element)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_max_and_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     ? = ... → stored in memory
#     print(?)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_max_and_index(...)) → find_max_and_index() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 343:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def index_and_element_excluding_first_n(lst, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >=
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   index_and_element_excluding_first_n(lst, 2) → each argument is evaluated first, then the function body runs
#   index_and_element_excluding_first_n(lst, n) → each argument is evaluated first, then the function body runs
#   index_and_element_excluding_first_n(?, 15) runs first → then print() outputs the result
#   index_and_element_excluding_first_n(?, 15) → each argument is evaluated first, then the function body runs
#   index >= n → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def index_and_element_excluding_first_n(lst, n) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def index_and_element_excluding_first_n(lst, n) → stored in memory, not executed
#   6. def index_and_element_excluding_first_n(lst, n) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. n = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(index_and_element_excluding_first_n(...))
#      → calls index_and_element_excluding_first_n(lst, n) with lst=[1, 3, 5, 7], n=15
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (index_and_element_excluding_first_n) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(index_and_element_excluding_first_n(...)) → index_and_element_excluding_first_n() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 344:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_index_squared(numbers) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Operators: **
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_index_squared(lst) → each argument is evaluated first, then the function body runs
#   print_index_squared(numbers) → each argument is evaluated first, then the function body runs
#   print_index_squared(?) runs first → then print() outputs the result
#   print_index_squared(?) → each argument is evaluated first, then the function body runs
#   number ** 2 → Python looks up number, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_index_squared(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print_index_squared(lst) → calls print_index_squared(numbers) with numbers=lst
#      → 3.1 for index, number in enumerate(numbers):
#      → 3.1     print(f'Index: {index}, Squared: {number ** 2}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_index_squared(...))
#      → calls print_index_squared(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 for index, number in enumerate(numbers):
#      → 9.1     print(f'Index: {index}, Squared: {number ** 2}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_index_squared) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print_index_squared(lst) → jumps into print_index_squared(), runs body, returns
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_index_squared(...)) → print_index_squared() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 345:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def replace_based_on_index(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   replace_based_on_index(lst) → each argument is evaluated first, then the function body runs
#   replace_based_on_index(?) runs first → then print() outputs the result
#   replace_based_on_index(?) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   index % 2 → Python looks up index, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def replace_based_on_index(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def replace_based_on_index(lst) → stored in memory, not executed
#   6. def replace_based_on_index(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(replace_based_on_index(...))
#      → calls replace_based_on_index(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return lst
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (replace_based_on_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(replace_based_on_index(...)) → replace_based_on_index() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 346:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_squares(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * +
# Built-in functions: pow, range, square()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_squares(3, 4) → each argument is evaluated first, then the function body runs
#   sum_of_squares(a, b) → each argument is evaluated first, then the function body runs
#   sum_of_squares(20, 30) runs first → then print() outputs the result
#   square(...) + square(...) → Python looks up square(...), then square(...), then applies +
#   sum_of_squares(20, 30) → each argument is evaluated first, then the function body runs
#   x * x → Python looks up x, then x, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_squares(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sum_of_squares(a, b) → stored in memory, not executed
#   5. def sum_of_squares(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sum_of_squares(...))
#      → calls sum_of_squares(a, b) with a=20, b=30
#      → 10.1 return pow(x, x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_squares) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_squares(...)) → sum_of_squares() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 347:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def greet() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: get_name, input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   greet() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def greet() → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def greet() → stored in memory, not executed
#   5. def greet() → stored in memory, not executed
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (greet) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 348:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: * - ==
# Built-in functions: inner_factorial()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial(5) → each argument is evaluated first, then the function body runs
#   factorial(n) → each argument is evaluated first, then the function body runs
#   factorial(15) runs first → then print() outputs the result
#   factorial(15) → each argument is evaluated first, then the function body runs
#   x == 0 → evaluated as True or False (determines which branch runs)
#   x * inner_factorial(...) → Python looks up x, then inner_factorial(...), then applies *
#   x - 1 → Python looks up x, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def factorial(n) → stored in memory, not executed
#   5. def factorial(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(factorial(...))
#      → calls factorial(n) with n=15
#      → 9.1 return 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial(...)) → factorial() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 349:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_prefix(prefix) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
# Built-in functions: prefix_func()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_prefix('pre-') → each argument is evaluated first, then the function body runs
#   add_prefix(prefix) → each argument is evaluated first, then the function body runs
#   add_prefix('Hi, ') runs first → then print() outputs the result
#   '%s%s' % ? → Python looks up '%s%s', then ?, then applies %
#   add_prefix('Hi, ') → each argument is evaluated first, then the function body runs
#   prefix + string → Python looks up prefix, then string, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_prefix(prefix) → stored in memory, not executed
#   2. prefix_func = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def add_prefix(prefix) → stored in memory, not executed
#   6. prefix_func = ... → assigns value to variable
#   7. result1 = ... → assigns value to variable
#   8. result2 = ... → assigns value to variable
#   9. result3 = ... → assigns value to variable
#   10. print(result1)
#   11. print(result2)
#   12. print(result3)
#   13. def add_prefix(prefix) → stored in memory, not executed
#   14. def add_prefix(prefix) → stored in memory, not executed
#   15. prefix = ... → assigns value to variable
#   16. result = ... → assigns value to variable
#   17. print(result)
#   18. print(add_prefix(...))
#      → calls add_prefix(prefix) with prefix='Hi, '
#      → 18.1 return '%s%s' % (prefix, string)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_prefix) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     prefix_func = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     prefix_func = ... → stored in memory
#     result1 = ... → stored in memory
#     result2 = ... → stored in memory
#     result3 = ... → stored in memory
#     print(result1)
#     print(result2)
#     print(result3)
#     prefix = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_prefix(...)) → add_prefix() runs first → value passed to print()

"""

"""
Problem 350:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_even(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: is_even()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_even(10) → each argument is evaluated first, then the function body runs
#   check_even(num) → each argument is evaluated first, then the function body runs
#   check_even(25) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   check_even(25) → each argument is evaluated first, then the function body runs
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_even(num) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_even(num) → stored in memory, not executed
#   5. def check_even(num) → stored in memory, not executed
#   6. num = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_even(...))
#      → calls check_even(num) with num=25
#      → 9.1 return x % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_even) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_even(...)) → check_even() runs first → value passed to print()

"""

"""
Problem 351:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def power(base, exponent) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: calculate_power, pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   power(2, 3) → each argument is evaluated first, then the function body runs
#   power(base, exponent) → each argument is evaluated first, then the function body runs
#   power(10, 25) runs first → then print() outputs the result
#   power(10, 25) → each argument is evaluated first, then the function body runs
#   b ** e → Python looks up b, then e, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def power(base, exponent) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def power(base, exponent) → stored in memory, not executed
#   5. def power(base, exponent) → stored in memory, not executed
#   6. base = ... → assigns value to variable
#   7. exponent = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(power(...))
#      → calls power(base, exponent) with base=10, exponent=25
#      → 10.1 import math
#      → 10.2 return math.pow(b, e)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (power) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     base = ... → stored in memory
#     exponent = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(power(...)) → power() runs first → value passed to print()

"""

"""
Problem 352:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def area_circle(radius) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: calculate_area()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   area_circle(5) runs first → then print() outputs the result
#   area_circle(radius) → each argument is evaluated first, then the function body runs
#   area_circle(14) runs first → then print() outputs the result
#   area_circle(5) → each argument is evaluated first, then the function body runs
#   ? * r → Python looks up ?, then r, then applies *
#   area_circle(14) → each argument is evaluated first, then the function body runs
#   math.pi * r → Python looks up math.pi, then r, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def area_circle(radius) → stored in memory, not executed
#   3. print(area_circle(...))
#      → calls area_circle(radius) with radius=5
#      → 3.1 return math.pi * r * r
#   4. def area_circle(radius) → stored in memory, not executed
#   5. def area_circle(radius) → stored in memory, not executed
#   6. radius = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(area_circle(...))
#      → calls area_circle(radius) with radius=14
#      → 9.1 return math.pi * r * r
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (area_circle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(area_circle(...)) → area_circle() runs first → value passed to print()
#     radius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(area_circle(...)) → area_circle() runs first → value passed to print()

"""

"""
Problem 353:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_words(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: capitalize, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_words('hello world from python') → each argument is evaluated first, then the function body runs
#   capitalize_words(sentence) → each argument is evaluated first, then the function body runs
#   capitalize_words('Python is fun') runs first → then print() outputs the result
#   capitalize_words('Python is fun') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_words(sentence) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def capitalize_words(sentence) → stored in memory, not executed
#   5. def capitalize_words(sentence) → stored in memory, not executed
#   6. sentence = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(capitalize_words(...))
#      → calls capitalize_words(sentence) with sentence='Python is fun'
#      → 9.1 return str(word).capitalize()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_words(...)) → capitalize_words() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 354:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(word) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: + ==
# Built-in functions: reverse_string, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_palindrome('radar') → each argument is evaluated first, then the function body runs
#   is_palindrome(word) → each argument is evaluated first, then the function body runs
#   is_palindrome('coding') runs first → then print() outputs the result
#   word == reverse_string(...) → evaluated as True or False (determines which branch runs)
#   is_palindrome('coding') → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(word) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_palindrome(word) → stored in memory, not executed
#   5. def is_palindrome(word) → stored in memory, not executed
#   6. word = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_palindrome(...))
#      → calls is_palindrome(word) with word='coding'
#      → 9.1 result = ''
#      → 9.2 for char in s:
#      → 9.2     result = char + result
#      → 9.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     word = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 355:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def concatenate_strings(str1, str2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
# Built-in functions: join_strings()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   concatenate_strings('Hello, ', 'world!') → each argument is evaluated first, then the function body runs
#   concatenate_strings(str1, str2) → each argument is evaluated first, then the function body runs
#   concatenate_strings('world', 'python') runs first → then print() outputs the result
#   '%s%s' % ? → Python looks up '%s%s', then ?, then applies %
#   concatenate_strings('world', 'python') → each argument is evaluated first, then the function body runs
#   s1 + s2 → Python looks up s1, then s2, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def concatenate_strings(str1, str2) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def concatenate_strings(str1, str2) → stored in memory, not executed
#   5. def concatenate_strings(str1, str2) → stored in memory, not executed
#   6. str1 = ... → assigns value to variable
#   7. str2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(concatenate_strings(...))
#      → calls concatenate_strings(str1, str2) with str1='world', str2='python'
#      → 10.1 return '%s%s' % (s1, s2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (concatenate_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(concatenate_strings(...)) → concatenate_strings() runs first → value passed to print()

"""

"""
Problem 356:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fibonacci(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: + - <=
# Built-in functions: fib, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fibonacci(10) → each argument is evaluated first, then the function body runs
#   fibonacci(n) → each argument is evaluated first, then the function body runs
#   fibonacci(15) runs first → then print() outputs the result
#   fibonacci(15) → each argument is evaluated first, then the function body runs
#   x <= 1 → evaluated as True or False (determines which branch runs)
#   fib(...) + fib(...) → Python looks up fib(...), then fib(...), then applies +
#   x - 1 → Python looks up x, then 1, then applies -
#   x - 2 → Python looks up x, then 2, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fibonacci(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def fibonacci(n) → stored in memory, not executed
#   5. def fibonacci(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(fibonacci(...))
#      → calls fibonacci(n) with n=15
#      → 9.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fibonacci) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 357:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def to_uppercase(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: - <=
# Built-in functions: chr, convert, ord, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   to_uppercase('hello world') → each argument is evaluated first, then the function body runs
#   to_uppercase(string) → each argument is evaluated first, then the function body runs
#   to_uppercase('world') runs first → then print() outputs the result
#   to_uppercase('world') → each argument is evaluated first, then the function body runs
#   'a' <= c → evaluated as True or False (determines which branch runs)
#   'a' <= 'z' → evaluated as True or False (determines which branch runs)
#   ord(...) - 32 → Python looks up ord(...), then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def to_uppercase(string) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def to_uppercase(string) → stored in memory, not executed
#   5. def to_uppercase(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(to_uppercase(...))
#      → calls to_uppercase(string) with string='world'
#      → 9.1 return str(s).upper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (to_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(to_uppercase(...)) → to_uppercase() runs first → value passed to print()

"""

"""
Problem 358:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_largest(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: compare()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_largest(10, 20) → each argument is evaluated first, then the function body runs
#   find_largest(a, b) → each argument is evaluated first, then the function body runs
#   find_largest(20, 30) runs first → then print() outputs the result
#   find_largest(20, 30) → each argument is evaluated first, then the function body runs
#   x > y → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_largest(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_largest(a, b) → stored in memory, not executed
#   5. def find_largest(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(find_largest(...))
#      → calls find_largest(a, b) with a=20, b=30
#      → 10.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_largest) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_largest(...)) → find_largest() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 359:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculator(operation, a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: * + - / != ==
# Built-in functions: add, divide, multiply, subtract, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculator('add', 5, 3) → each argument is evaluated first, then the function body runs
#   calculator(operation, a, b) → each argument is evaluated first, then the function body runs
#   calculator(25, 20, 30) runs first → then print() outputs the result
#   operation == 'add' → evaluated as True or False (determines which branch runs)
#   calculator(25, 20, 30) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#   x - y → Python looks up x, then y, then applies -
#   x * y → Python looks up x, then y, then applies *
#   y != 0 → evaluated as True or False (determines which branch runs)
#   operation == 'subtract' → evaluated as True or False (determines which branch runs)
#   x / y → Python looks up x, then y, then applies /
#   operation == 'multiply' → evaluated as True or False (determines which branch runs)
#   operation == 'divide' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculator(operation, a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculator(operation, a, b) → stored in memory, not executed
#   5. def calculator(operation, a, b) → stored in memory, not executed
#   6. operation = ... → assigns value to variable
#   7. a = ... → assigns value to variable
#   8. b = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculator(...))
#      → calls calculator(operation, a, b) with operation=25, a=20, b=30
#      → 11.1 import operator
#      → 11.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculator) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     operation = ... → stored in memory
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculator(...)) → calculator() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 360:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + < ** ==
# Built-in functions: check_divisibility, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_prime(7) → each argument is evaluated first, then the function body runs
#   is_prime(n) → each argument is evaluated first, then the function body runs
#   is_prime(15) runs first → then print() outputs the result
#   is_prime(15) → each argument is evaluated first, then the function body runs
#   x < 2 → evaluated as True or False (determines which branch runs)
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   x % i → Python looks up x, then i, then applies %
#   x ** 0.5 → Python looks up x, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_prime(n) → stored in memory, not executed
#   5. def is_prime(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_prime(...))
#      → calls is_prime(n) with n=15
#      → 9.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 361:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: is_vowel, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels('Hello World') → each argument is evaluated first, then the function body runs
#   count_vowels(string) → each argument is evaluated first, then the function body runs
#   count_vowels('world') runs first → then print() outputs the result
#   count_vowels('world') → each argument is evaluated first, then the function body runs
#   char in 'aeiouAEIOU' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(string) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_vowels(string) → stored in memory, not executed
#   5. def count_vowels(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_vowels(...))
#      → calls count_vowels(string) with string='world'
#      → 9.1 return len([char for char in string if is_vowel(char)])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 362:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_average(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: /
# Built-in functions: len, sum_numbers()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_average(?) → each argument is evaluated first, then the function body runs
#   calculate_average(numbers) → each argument is evaluated first, then the function body runs
#   calculate_average(?) runs first → then print() outputs the result
#   sum_numbers(...) / len(...) → Python looks up sum_numbers(...), then len(...), then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_average(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_average(numbers) → stored in memory, not executed
#   5. def calculate_average(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_average(...))
#      → calls calculate_average(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_average(...)) → calculate_average() runs first → value passed to print()
#
#   Loop: takes one item from nums each round, runs body, repeats until done

"""

"""
Problem 363:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_string() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reverse, reverse_func, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_string() → each argument is evaluated first, then the function body runs
#   reverse_string('Hello') → each argument is evaluated first, then the function body runs
#   reverse_string(string) → each argument is evaluated first, then the function body runs
#   reverse_string('world') runs first → then print() outputs the result
#   reverse_string('world') → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#   char + reversed_str → Python looks up char, then reversed_str, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_string() → stored in memory, not executed
#   2. reverse_func = ... → assigns value to variable
#   3. print(reverse_func(...))
#   4. def reverse_string(string) → stored in memory, not executed
#   5. result = ... → assigns value to variable
#   6. print(result)
#   7. def reverse_string() → stored in memory, not executed
#   8. def reverse_string() → stored in memory, not executed
#   9. string = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(reverse_string(...))
#      → calls reverse_string() with 
#      → 12.1 result = ''
#      → 12.2 for char in s:
#      → 12.2     result = char + result
#      → 12.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     reverse_func = ... → stored in memory
#     print(reverse_func(...))
#     result = ... → stored in memory
#     print(result)
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 364:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_sorted(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - > ==
# Built-in functions: len, range, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_sorted(?) → each argument is evaluated first, then the function body runs
#   is_sorted() → each argument is evaluated first, then the function body runs
#   is_sorted(lst) → each argument is evaluated first, then the function body runs
#   is_sorted(?) runs first → then print() outputs the result
#   compare_elements() → each argument is evaluated first, then the function body runs
#   compare_elements(lst) → each argument is evaluated first, then the function body runs
#   lst == sorted(...) → evaluated as True or False (determines which branch runs)
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? > ? → evaluated as True or False (determines which branch runs)
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_sorted(lst) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_sorted() → stored in memory, not executed
#   5. def compare_elements(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. is_sorted() → calls is_sorted(lst) with 
#      → 7.1 return False
#   8. def is_sorted(lst) → stored in memory, not executed
#   9. def is_sorted(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(is_sorted(...))
#      → calls is_sorted(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_sorted, compare_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     is_sorted() → jumps into is_sorted(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_sorted(...)) → is_sorted() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 365:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def generate_squares(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * +
# Built-in functions: pow, range, square()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   generate_squares(5) → each argument is evaluated first, then the function body runs
#   generate_squares(n) → each argument is evaluated first, then the function body runs
#   generate_squares(15) runs first → then print() outputs the result
#   generate_squares(15) → each argument is evaluated first, then the function body runs
#   x * x → Python looks up x, then x, then applies *
#   n + 1 → Python looks up n, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def generate_squares(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def generate_squares(n) → stored in memory, not executed
#   5. def generate_squares(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(generate_squares(...))
#      → calls generate_squares(n) with n=15
#      → 9.1 return pow(x, x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (generate_squares) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(generate_squares(...)) → generate_squares() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 366:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_two(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: add, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_two(3, 5) → each argument is evaluated first, then the function body runs
#   sum_of_two(a, b) → each argument is evaluated first, then the function body runs
#   sum_of_two(20, 30) runs first → then print() outputs the result
#   sum_of_two(20, 30) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_two(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sum_of_two(a, b) → stored in memory, not executed
#   5. def sum_of_two(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sum_of_two(...))
#      → calls sum_of_two(a, b) with a=20, b=30
#      → 10.1 import operator
#      → 10.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_two) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_two(...)) → sum_of_two() runs first → value passed to print()

"""

"""
Problem 367:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def even_or_odd(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: % ==
# Built-in functions: is_even()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   even_or_odd(10) → each argument is evaluated first, then the function body runs
#   even_or_odd(n) → each argument is evaluated first, then the function body runs
#   even_or_odd(15) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   even_or_odd(15) → each argument is evaluated first, then the function body runs
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def even_or_odd(n) → stored in memory, not executed
#   2. even_or_odd(10) → calls even_or_odd(n) with n=10
#      → 2.1 return x % 2 == 0
#   3. def even_or_odd(n) → stored in memory, not executed
#   4. def even_or_odd(n) → stored in memory, not executed
#   5. n = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(even_or_odd(...))
#      → calls even_or_odd(n) with n=15
#      → 8.1 return x % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (even_or_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     even_or_odd(10) → jumps into even_or_odd(), runs body, returns
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(even_or_odd(...)) → even_or_odd() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 368:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_temperature(celsius) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
# Built-in functions: celsius_to_fahrenheit()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_temperature(25) → each argument is evaluated first, then the function body runs
#   convert_temperature(celsius) → each argument is evaluated first, then the function body runs
#   convert_temperature(25) runs first → then print() outputs the result
#   ? + 32 → Python looks up ?, then 32, then applies +
#   ? / 5 → Python looks up ?, then 5, then applies /
#   c * 9 → Python looks up c, then 9, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_temperature(celsius) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def convert_temperature(celsius) → stored in memory, not executed
#   5. def convert_temperature(celsius) → stored in memory, not executed
#   6. celsius = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_temperature(...))
#      → calls convert_temperature(celsius) with celsius=25
#      → 9.1 return c * 9 / 5 + 32
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_temperature) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     celsius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_temperature(...)) → convert_temperature() runs first → value passed to print()

"""

"""
Problem 369:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def rectangle_area(length, width) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: calculate_area, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   rectangle_area(5, 10) → each argument is evaluated first, then the function body runs
#   rectangle_area(length, width) → each argument is evaluated first, then the function body runs
#   rectangle_area(25, 25) runs first → then print() outputs the result
#   rectangle_area(25, 25) → each argument is evaluated first, then the function body runs
#   l * w → Python looks up l, then w, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def rectangle_area(length, width) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def rectangle_area(length, width) → stored in memory, not executed
#   5. def rectangle_area(length, width) → stored in memory, not executed
#   6. length = ... → assigns value to variable
#   7. width = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(rectangle_area(...))
#      → calls rectangle_area(length, width) with length=25, width=25
#      → 10.1 return pow(l, w)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (rectangle_area) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(rectangle_area(...)) → rectangle_area() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 370:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_prime(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + < ** ==
# Built-in functions: int, is_prime, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_prime(11) → each argument is evaluated first, then the function body runs
#   check_prime(n) → each argument is evaluated first, then the function body runs
#   check_prime(15) runs first → then print() outputs the result
#   check_prime(15) → each argument is evaluated first, then the function body runs
#   num < 2 → evaluated as True or False (determines which branch runs)
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   num % i → Python looks up num, then i, then applies %
#   num ** 0.5 → Python looks up num, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_prime(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_prime(n) → stored in memory, not executed
#   5. def check_prime(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_prime(...))
#      → calls check_prime(n) with n=15
#      → 9.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_prime(...)) → check_prime() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 371:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_minimum(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min, min_of_three()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_minimum(5, 3, 8) → each argument is evaluated first, then the function body runs
#   find_minimum(a, b, c) → each argument is evaluated first, then the function body runs
#   find_minimum(20, 30, 10) runs first → then print() outputs the result
#   find_minimum(20, 30, 10) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_minimum(a, b, c) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def find_minimum(a, b, c) → stored in memory, not executed
#   5. def find_minimum(a, b, c) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. c = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(find_minimum(...))
#      → calls find_minimum(a, b, c) with a=20, b=30, c=10
#      → 11.1 return min(x, y, z)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_minimum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_minimum(...)) → find_minimum() runs first → value passed to print()

"""

"""
Problem 373:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_factorial(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + == or
# Built-in functions: factorial, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_factorial(5) → each argument is evaluated first, then the function body runs
#   calculate_factorial(n) → each argument is evaluated first, then the function body runs
#   calculate_factorial(15) runs first → then print() outputs the result
#   calculate_factorial(15) → each argument is evaluated first, then the function body runs
#   num == 0 → evaluated as True or False (determines which branch runs)
#   num == 1 → evaluated as True or False (determines which branch runs)
#   num + 1 → Python looks up num, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_factorial(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_factorial(n) → stored in memory, not executed
#   5. def calculate_factorial(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_factorial(...))
#      → calls calculate_factorial(n) with n=15
#      → 9.1 return 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_factorial(...)) → calculate_factorial() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 374:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_words(sentence) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: capitalize()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_words('hello world from python') → each argument is evaluated first, then the function body runs
#   capitalize_words(sentence) → each argument is evaluated first, then the function body runs
#   capitalize_words('Python is fun') runs first → then print() outputs the result
#   ?.upper(...) + ? → Python looks up ?.upper(...), then ?, then applies +
#   capitalize_words('Python is fun') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_words(sentence) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def capitalize_words(sentence) → stored in memory, not executed
#   5. def capitalize_words(sentence) → stored in memory, not executed
#   6. sentence = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(capitalize_words(...))
#      → calls capitalize_words(sentence) with sentence='Python is fun'
#      → 9.1 return word[0].upper() + word[1:]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     sentence = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_words(...)) → capitalize_words() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 375:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_in_list(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_largest() runs first → then print() outputs the result
#   max_in_list(lst) → each argument is evaluated first, then the function body runs
#   max_in_list(?) runs first → then print() outputs the result
#   find_largest() → each argument is evaluated first, then the function body runs
#   max_in_list(numbers) → each argument is evaluated first, then the function body runs
#   max_in_list(?) → each argument is evaluated first, then the function body runs
#   item > result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_in_list(lst) → stored in memory, not executed
#   2. def find_largest() → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(find_largest(...))
#      → calls find_largest() with 
#      → 4.1 numbers = [12, 45, 7, 23, 56, 89, 34]
#      → 4.2 largest = max_in_list(numbers)
#      → 4.3 return largest
#   5. def max_in_list(lst) → stored in memory, not executed
#   6. def find_largest() → stored in memory, not executed
#   7. print(find_largest(...))
#      → calls find_largest() with 
#      → 7.1 numbers = [12, 45, 7, 23, 56, 89, 34]
#      → 7.2 largest = max_in_list(numbers)
#      → 7.3 return largest
#   8. def max_in_list(lst) → stored in memory, not executed
#   9. def max_in_list(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(max_in_list(...))
#      → calls max_in_list(lst) with lst=[1, 3, 5, 7]
#      → 13.1 result = lst[0]
#      → 13.2 for item in lst:
#      → 13.2     if item > result:
#      → 13.2         result = item
#      → 13.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_largest, max_in_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_largest(...)) → find_largest() runs first → value passed to print()
#     print(find_largest(...)) → find_largest() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_in_list(...)) → max_in_list() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 376:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_string(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reverse()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_string('hello') → each argument is evaluated first, then the function body runs
#   reverse_string(s) → each argument is evaluated first, then the function body runs
#   reverse_string(?) runs first → then print() outputs the result
#   reverse_string(?) → each argument is evaluated first, then the function body runs
#   char + reversed_str → Python looks up char, then reversed_str, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_string(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def reverse_string(s) → stored in memory, not executed
#   5. def reverse_string(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_string(...))
#      → calls reverse_string(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return reversed_str
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 377:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_average(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: /
# Built-in functions: average, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_average(?) → each argument is evaluated first, then the function body runs
#   calculate_average(lst) → each argument is evaluated first, then the function body runs
#   calculate_average(?) runs first → then print() outputs the result
#   total / len(...) → Python looks up total, then len(...), then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_average(lst) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_average(lst) → stored in memory, not executed
#   5. def calculate_average(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_average(...))
#      → calls calculate_average(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return total / len(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_average(...)) → calculate_average() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 378:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_palindrome(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: is_palindrome()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_palindrome('racecar') → each argument is evaluated first, then the function body runs
#   check_palindrome(s) → each argument is evaluated first, then the function body runs
#   check_palindrome(?) runs first → then print() outputs the result
#   s == ? → evaluated as True or False (determines which branch runs)
#   check_palindrome(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_palindrome(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_palindrome(s) → stored in memory, not executed
#   5. def check_palindrome(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_palindrome(...))
#      → calls check_palindrome(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return s == s[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_palindrome(...)) → check_palindrome() runs first → value passed to print()

"""

"""
Problem 379:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def closest_to_zero(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: abs()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   closest_to_zero(?) → each argument is evaluated first, then the function body runs
#   closest_to_zero(numbers) → each argument is evaluated first, then the function body runs
#   closest_to_zero(?) runs first → then print() outputs the result
#   abs(...) < abs(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def closest_to_zero(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def closest_to_zero(numbers) → stored in memory, not executed
#   5. def closest_to_zero(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(closest_to_zero(...))
#      → calls closest_to_zero(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return closest
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (closest_to_zero) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(closest_to_zero(...)) → closest_to_zero() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 380:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_sorted(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: + - >
# Built-in functions: is_sorted, len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_sorted(?) → each argument is evaluated first, then the function body runs
#   check_sorted(lst) → each argument is evaluated first, then the function body runs
#   check_sorted(?) runs first → then print() outputs the result
#   len(...) - 1 → Python looks up len(...), then 1, then applies -
#   ? > ? → evaluated as True or False (determines which branch runs)
#   i + 1 → Python looks up i, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_sorted(lst) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def check_sorted(lst) → stored in memory, not executed
#   5. def check_sorted(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_sorted(...))
#      → calls check_sorted(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_sorted) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_sorted(...)) → check_sorted() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 381:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_integers(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: input, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_integers(numbers_in_strings) runs first → then print() outputs the result
#   convert_to_integers(strings) → each argument is evaluated first, then the function body runs
#   convert_to_integers('world') runs first → then print() outputs the result
#   convert_to_integers(numbers_in_strings) → each argument is evaluated first, then the function body runs
#   convert_to_integers('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. user = ... → assigns value to variable
#   2. print(list(...))
#   3. def convert_to_integers(strings) → stored in memory, not executed
#   4. numbers_in_strings = ... → assigns value to variable
#   5. print(convert_to_integers(...))
#      → calls convert_to_integers(strings) with strings=numbers_in_strings
#      → 5.1 return list(map(int, strings))
#   6. def convert_to_integers(strings) → stored in memory, not executed
#   7. def convert_to_integers(strings) → stored in memory, not executed
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(convert_to_integers(...))
#      → calls convert_to_integers(strings) with strings='world'
#      → 11.1 return list(map(int, strings))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     user = ... → stored in memory
#     print(list(...))
#     numbers_in_strings = ... → stored in memory
#     print(convert_to_integers(...)) → convert_to_integers() runs first → value passed to print()
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_integers(...)) → convert_to_integers() runs first → value passed to print()

"""

"""
Problem 382:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_strings_by_length(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_strings_by_length(?) → each argument is evaluated first, then the function body runs
#   sort_strings_by_length(strings) → each argument is evaluated first, then the function body runs
#   sort_strings_by_length(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_strings_by_length(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_strings_by_length(strings) → stored in memory, not executed
#   5. def sort_strings_by_length(strings) → stored in memory, not executed
#   6. strings = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_strings_by_length(...))
#      → calls sort_strings_by_length(strings) with strings=[4, 1, 8, 6, 3]
#      → 9.1 return sorted_strings
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_strings_by_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_strings_by_length(...)) → sort_strings_by_length() runs first → value passed to print()

"""

"""
Problem 384:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_tuples_by_second(tuples) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_tuples_by_second(?) → each argument is evaluated first, then the function body runs
#   sort_tuples_by_second(tuples) → each argument is evaluated first, then the function body runs
#   sort_tuples_by_second(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_tuples_by_second(tuples) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_tuples_by_second(tuples) → stored in memory, not executed
#   5. def sort_tuples_by_second(tuples) → stored in memory, not executed
#   6. tuples = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_tuples_by_second(...))
#      → calls sort_tuples_by_second(tuples) with tuples=[4, 1, 8, 6, 3]
#      → 9.1 return sorted_tuples
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_tuples_by_second) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     tuples = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_tuples_by_second(...)) → sort_tuples_by_second() runs first → value passed to print()

"""

"""
Problem 385:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_dicts_by_key(dicts, key_name) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_dicts_by_key(?, 'age') → each argument is evaluated first, then the function body runs
#   sort_dicts_by_key(dicts, key_name) → each argument is evaluated first, then the function body runs
#   sort_dicts_by_key(?, ?) runs first → then print() outputs the result
#   sort_dicts_by_key(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_dicts_by_key(dicts, key_name) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_dicts_by_key(dicts, key_name) → stored in memory, not executed
#   5. def sort_dicts_by_key(dicts, key_name) → stored in memory, not executed
#   6. dicts = ... → assigns value to variable
#   7. key_name = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_dicts_by_key(...))
#      → calls sort_dicts_by_key(dicts, key_name) with dicts=[4, 1, 8, 6, 3], key_name=[4, 1, 8, 6, 3]
#      → 10.1 return sorted_dicts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_dicts_by_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     dicts = ... → stored in memory
#     key_name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_dicts_by_key(...)) → sort_dicts_by_key() runs first → value passed to print()

"""

"""
Problem 386:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def longest_word(words) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   longest_word(?) → each argument is evaluated first, then the function body runs
#   longest_word(words) → each argument is evaluated first, then the function body runs
#   longest_word(?) runs first → then print() outputs the result
#   len(...) > len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def longest_word(words) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def longest_word(words) → stored in memory, not executed
#   5. def longest_word(words) → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(longest_word(...))
#      → calls longest_word(words) with words=[4, 1, 8, 6, 3]
#      → 9.1 return longest
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(longest_word(...)) → longest_word() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 387:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_by_absolute_value(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_absolute_value(?) → each argument is evaluated first, then the function body runs
#   sort_by_absolute_value(numbers) → each argument is evaluated first, then the function body runs
#   sort_by_absolute_value(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_by_absolute_value(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_by_absolute_value(numbers) → stored in memory, not executed
#   5. def sort_by_absolute_value(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_by_absolute_value(...))
#      → calls sort_by_absolute_value(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sorted_numbers
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_absolute_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_by_absolute_value(...)) → sort_by_absolute_value() runs first → value passed to print()

"""

"""
Problem 388:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_strings_by_last_character(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_strings_by_last_character(?) → each argument is evaluated first, then the function body runs
#   main(lst) → each argument is evaluated first, then the function body runs
#   sort_strings_by_last_character(strings) → each argument is evaluated first, then the function body runs
#   sort_strings_by_last_character(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_strings_by_last_character(strings) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def main(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. main(lst) → calls main(lst) with lst=lst
#      → 6.1 def inner(word):
#      → 6.1     return word[-1]
#      → 6.2 return list(sorted(lst, key=inner))
#   7. def sort_strings_by_last_character(strings) → stored in memory, not executed
#   8. def sort_strings_by_last_character(strings) → stored in memory, not executed
#   9. strings = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(sort_strings_by_last_character(...))
#      → calls sort_strings_by_last_character(strings) with strings=[4, 1, 8, 6, 3]
#      → 12.1 return sorted_strings
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, sort_strings_by_last_character) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_strings_by_last_character(...)) → sort_strings_by_last_character() runs first → value passed to print()

"""

"""
Problem 389:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def earliest_date(dates) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: min, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   earliest_date(?) → each argument is evaluated first, then the function body runs
#   earliest_date(dates) → each argument is evaluated first, then the function body runs
#   earliest_date(?) runs first → then print() outputs the result
#   item < result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def earliest_date(dates) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def earliest_date(dates) → stored in memory, not executed
#   5. def earliest_date(dates) → stored in memory, not executed
#   6. dates = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(earliest_date(...))
#      → calls earliest_date(dates) with dates=[4, 1, 8, 6, 3]
#      → 9.1 result = dates[0]
#      → 9.2 for item in dates:
#      → 9.2     if item < result:
#      → 9.2         result = item
#      → 9.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (earliest_date) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     dates = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(earliest_date(...)) → earliest_date() runs first → value passed to print()
#
#   Loop: takes one item from dates each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 390:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_surname(name) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_surname(name) → each argument is evaluated first, then the function body runs
#   get_surname('Bob') runs first → then print() outputs the result
#   get_surname('Bob') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_surname(name) → stored in memory, not executed
#   2. names = ... → assigns value to variable
#   3. sorted_names = ... → assigns value to variable
#   4. print(sorted_names)
#   5. def get_surname(name) → stored in memory, not executed
#   6. def get_surname(name) → stored in memory, not executed
#   7. name = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_surname(...))
#      → calls get_surname(name) with name='Bob'
#      → 10.1 return name.split()[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_surname) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     names = ... → stored in memory
#     sorted_names = ... → stored in memory
#     print(sorted_names)
#     name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_surname(...)) → get_surname() runs first → value passed to print()

"""

"""
Problem 391:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_strings_alphabetically(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_strings_alphabetically(strings) → each argument is evaluated first, then the function body runs
#   sort_strings_alphabetically(?) runs first → then print() outputs the result
#   sort_strings_alphabetically(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_strings_alphabetically(strings) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. sorted_strings = ... → assigns value to variable
#   4. print(sorted_strings)
#   5. def sort_strings_alphabetically(strings) → stored in memory, not executed
#   6. def sort_strings_alphabetically(strings) → stored in memory, not executed
#   7. strings = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_strings_alphabetically(...))
#      → calls sort_strings_alphabetically(strings) with strings=[4, 1, 8, 6, 3]
#      → 10.1 return sorted(strings, key=str.lower)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_strings_alphabetically) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     sorted_strings = ... → stored in memory
#     print(sorted_strings)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_strings_alphabetically(...)) → sort_strings_alphabetically() runs first → value passed to print()

"""

"""
Problem 392:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def scrabble_score(word) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_highest_scrabble_word(?) → each argument is evaluated first, then the function body runs
#   scrabble_score(word) → each argument is evaluated first, then the function body runs
#   scrabble_score('coding') runs first → then print() outputs the result
#   scrabble_score('coding') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def scrabble_score(word) → stored in memory, not executed
#   2. def find_highest_scrabble_word(words) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def scrabble_score(word) → stored in memory, not executed
#   6. def scrabble_score(word) → stored in memory, not executed
#   7. word = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(scrabble_score(...))
#      → calls scrabble_score(word) with word='coding'
#      → 10.1 return total_score
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (scrabble_score, find_highest_scrabble_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     word = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(scrabble_score(...)) → scrabble_score() runs first → value passed to print()
#
#   Loop: takes one item from word.upper(...) each round, runs body, repeats until done

"""

"""
Problem 393:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_complex_numbers(complex_numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: abs, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_complex_numbers(?) → each argument is evaluated first, then the function body runs
#   sort_complex_numbers(complex_numbers) → each argument is evaluated first, then the function body runs
#   sort_complex_numbers(?) runs first → then print() outputs the result
#   3 + 4j → Python looks up 3, then 4j, then applies +
#   1 + 1j → Python looks up 1, then 1j, then applies +
#   0 + 2j → Python looks up 0, then 2j, then applies +
#   5 + 0j → Python looks up 5, then 0j, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_complex_numbers(complex_numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_complex_numbers(complex_numbers) → stored in memory, not executed
#   5. def sort_complex_numbers(complex_numbers) → stored in memory, not executed
#   6. complex_numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_complex_numbers(...))
#      → calls sort_complex_numbers(complex_numbers) with complex_numbers=[4, 1, 8, 6, 3]
#      → 9.1 return abs(c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_complex_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     complex_numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_complex_numbers(...)) → sort_complex_numbers() runs first → value passed to print()

"""

"""
Problem 394:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_largest_file(file_paths) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_largest_file(file_paths) → each argument is evaluated first, then the function body runs
#   find_largest_file(?) runs first → then print() outputs the result
#   find_largest_file(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import os
#   2. def find_largest_file(file_paths) → stored in memory, not executed
#   3. def find_largest_file(file_paths) → stored in memory, not executed
#   4. def find_largest_file(file_paths) → stored in memory, not executed
#   5. file_paths = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(find_largest_file(...))
#      → calls find_largest_file(file_paths) with file_paths=[4, 1, 8, 6, 3]
#      → 8.1 return os.path.getsize(file_path)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_largest_file) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     file_paths = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_largest_file(...)) → find_largest_file() runs first → value passed to print()

"""

"""
Problem 395:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_fractions(fractions) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Fraction, float, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_fractions(?) → each argument is evaluated first, then the function body runs
#   sort_fractions(fractions) → each argument is evaluated first, then the function body runs
#   sort_fractions(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from fractions import Fraction
#   2. def sort_fractions(fractions) → stored in memory, not executed
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def sort_fractions(fractions) → stored in memory, not executed
#   6. def sort_fractions(fractions) → stored in memory, not executed
#   7. fractions = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_fractions(...))
#      → calls sort_fractions(fractions) with fractions=[4, 1, 8, 6, 3]
#      → 10.1 return float(frac)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_fractions) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     fractions = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_fractions(...)) → sort_fractions() runs first → value passed to print()

"""

"""
Problem 396:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_tuples_by_sum(tuples) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: list, reduce, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_tuples_by_sum(?) → each argument is evaluated first, then the function body runs
#   main(lst_tuples) runs first → then print() outputs the result
#   sort_tuples_by_sum(tuples) → each argument is evaluated first, then the function body runs
#   sort_tuples_by_sum(?) runs first → then print() outputs the result
#   main(lst_tuples) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_tuples_by_sum(tuples) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def main(lst_tuples) → stored in memory, not executed
#   5. lst_tuples = ... → assigns value to variable
#   6. print(main(...))
#      → calls main(lst_tuples) with lst_tuples=lst_tuples
#      → 6.1 def inner(tup):
#      → 6.1     return tup[0] + tup[1]
#      → 6.2 return list(sorted(lst_tuples, key=inner))
#   7. def sort_tuples_by_sum(tuples) → stored in memory, not executed
#   8. def sort_tuples_by_sum(tuples) → stored in memory, not executed
#   9. tuples = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(sort_tuples_by_sum(...))
#      → calls sort_tuples_by_sum(tuples) with tuples=[4, 1, 8, 6, 3]
#      → 12.1 from functools import reduce
#      → 12.2 import operator
#      → 12.3 return reduce(operator.add, tup, 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, sort_tuples_by_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst_tuples = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     tuples = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_tuples_by_sum(...)) → sort_tuples_by_sum() runs first → value passed to print()
#
#   Loop: takes one item from tup each round, runs body, repeats until done

"""

"""
Problem 397:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def closest_to_zero(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   closest_to_zero(?) → each argument is evaluated first, then the function body runs
#   closest_to_zero(numbers) → each argument is evaluated first, then the function body runs
#   closest_to_zero(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def closest_to_zero(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def closest_to_zero(numbers) → stored in memory, not executed
#   5. def closest_to_zero(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(closest_to_zero(...))
#      → calls closest_to_zero(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return closest
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (closest_to_zero) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(closest_to_zero(...)) → closest_to_zero() runs first → value passed to print()

"""

"""
Problem 398:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_points_by_distance(points) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + **
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_points_by_distance(?) → each argument is evaluated first, then the function body runs
#   sort_points_by_distance(points) → each argument is evaluated first, then the function body runs
#   sort_points_by_distance(?) runs first → then print() outputs the result
#   ? ** 0.5 → Python looks up ?, then 0.5, then applies **
#   ? + ? → Python looks up ?, then ?, then applies +
#   ? ** 2 → Python looks up ?, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_points_by_distance(points) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_points_by_distance(points) → stored in memory, not executed
#   5. def sort_points_by_distance(points) → stored in memory, not executed
#   6. points = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_points_by_distance(...))
#      → calls sort_points_by_distance(points) with points=[4, 1, 8, 6, 3]
#      → 9.1 return (point[0] ** 2 + point[1] ** 2) ** 0.5
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_points_by_distance) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     points = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_points_by_distance(...)) → sort_points_by_distance() runs first → value passed to print()

"""

"""
Problem 399:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_words_by_distinct_letters(words) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_words_by_distinct_letters(?) → each argument is evaluated first, then the function body runs
#   sort_words_by_distinct_letters(words) → each argument is evaluated first, then the function body runs
#   sort_words_by_distinct_letters(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_words_by_distinct_letters(words) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_words_by_distinct_letters(words) → stored in memory, not executed
#   5. def sort_words_by_distinct_letters(words) → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_words_by_distinct_letters(...))
#      → calls sort_words_by_distinct_letters(words) with words=[4, 1, 8, 6, 3]
#      → 9.1 return len(set(word))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_words_by_distinct_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_words_by_distinct_letters(...)) → sort_words_by_distinct_letters() runs first → value passed to print()

"""

"""
Problem 400:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: max, ord()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) → each argument is evaluated first, then the function body runs
#   find_highest_ascii_word(?) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. main(lst) → calls main(lst) with lst=lst
#      → 3.1 return count
#   4. def ascii_value(word) → stored in memory, not executed
#   5. def find_highest_ascii_word(words) → stored in memory, not executed
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. def main(lst) → stored in memory, not executed
#   9. def main(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, ascii_value, find_highest_ascii_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done

"""

"""
Problem 401:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: len, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_vowel_count(strings) → each argument is evaluated first, then the function body runs
#   count_vowels(s) → each argument is evaluated first, then the function body runs
#   count_vowels('world') runs first → then print() outputs the result
#   count_vowels('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(s) → stored in memory, not executed
#   2. def sort_by_vowel_count(strings) → stored in memory, not executed
#   3. strings = ... → assigns value to variable
#   4. sorted_strings = ... → assigns value to variable
#   5. print(sorted_strings)
#   6. def count_vowels(s) → stored in memory, not executed
#   7. def count_vowels(s) → stored in memory, not executed
#   8. s = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_vowels(...))
#      → calls count_vowels(s) with s='world'
#      → 11.1 return len([char for char in s if char in vowels])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_vowel_count, count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     sorted_strings = ... → stored in memory
#     print(sorted_strings)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 402:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_integers(integers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_integers(numbers) runs first → then print() outputs the result
#   convert_to_integers(integers) → each argument is evaluated first, then the function body runs
#   convert_to_integers(25) runs first → then print() outputs the result
#   convert_to_integers(numbers) → each argument is evaluated first, then the function body runs
#   convert_to_integers(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_integers(integers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(convert_to_integers(...))
#      → calls convert_to_integers(integers) with integers=numbers
#      → 3.1 return list(map(str, integers))
#   4. def convert_to_integers(integers) → stored in memory, not executed
#   5. def convert_to_integers(integers) → stored in memory, not executed
#   6. integers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_to_integers(...))
#      → calls convert_to_integers(integers) with integers=25
#      → 9.1 return list(map(str, integers))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(convert_to_integers(...)) → convert_to_integers() runs first → value passed to print()
#     integers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_integers(...)) → convert_to_integers() runs first → value passed to print()

"""

"""
Problem 403:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_five(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_five(lst) runs first → then print() outputs the result
#   add_five(lst) → each argument is evaluated first, then the function body runs
#   add_five(?) runs first → then print() outputs the result
#   add_five(?) → each argument is evaluated first, then the function body runs
#   number + 5 → Python looks up number, then 5, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_five(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(add_five(...))
#      → calls add_five(lst) with lst=lst
#      → 3.1 return list(map(lambda number: number + 5, lst))
#   4. def add_five(lst) → stored in memory, not executed
#   5. def add_five(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(add_five(...))
#      → calls add_five(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return list(map(lambda number: number + 5, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_five) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(add_five(...)) → add_five() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_five(...)) → add_five() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 404:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_min(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_min(lst) runs first → then print() outputs the result
#   main(user) → each argument is evaluated first, then the function body runs
#   find_min(numbers) → each argument is evaluated first, then the function body runs
#   find_min(?) runs first → then print() outputs the result
#   find_min(lst) → each argument is evaluated first, then the function body runs
#   find_min(?) → each argument is evaluated first, then the function body runs
#   number < min_num → evaluated as True or False (determines which branch runs)
#   num < min_num → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_min(numbers) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(find_min(...))
#      → calls find_min(numbers) with numbers=lst
#      → 3.1 return min_num
#   4. def main(user) → stored in memory, not executed
#   5. user = ... → assigns value to variable
#   6. main(user) → calls main(user) with user=user
#      → 6.1 int_lst = []
#      → 6.2 for num in user:
#      → 6.2     int_lst.append(int(num))
#      → 6.3 min_num = int_lst[0]
#      → 6.4 for num in int_lst:
#      → 6.4     if num < min_num:
#      → 6.4         min_num = num
#      → 6.5 return min_num
#   7. def find_min(numbers) → stored in memory, not executed
#   8. def find_min(numbers) → stored in memory, not executed
#   9. numbers = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_min(...))
#      → calls find_min(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 12.1 return min_num
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, find_min) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_min(...)) → find_min() runs first → value passed to print()
#     user = ... → stored in memory
#     main(user) → jumps into main(), runs body, returns
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_min(...)) → find_min() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 405:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def converts(cm) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   converts(number) runs first → then print() outputs the result
#   converts(cm) → each argument is evaluated first, then the function body runs
#   converts(25) runs first → then print() outputs the result
#   cm * 0.39 → Python looks up cm, then 0.39, then applies *
#   converts(number) → each argument is evaluated first, then the function body runs
#   converts(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def converts(cm) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(converts(...))
#      → calls converts(cm) with cm=number
#      → 3.1 inch = cm * 0.39
#      → 3.2 result = inch
#      → 3.3 return result
#   4. def converts(cm) → stored in memory, not executed
#   5. def converts(cm) → stored in memory, not executed
#   6. cm = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(converts(...))
#      → calls converts(cm) with cm=25
#      → 9.1 inch = cm * 0.39
#      → 9.2 result = inch
#      → 9.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (converts) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(converts(...)) → converts() runs first → value passed to print()
#     cm = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(converts(...)) → converts() runs first → value passed to print()

"""

"""
Problem 406:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse(lst) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse(lst) runs first → then print() outputs the result
#   reverse(lst) → each argument is evaluated first, then the function body runs
#   reverse(?) runs first → then print() outputs the result
#   reverse(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(reverse(...))
#      → calls reverse(lst) with lst=lst
#      → 3.1 return lst
#   4. def reverse(lst) → stored in memory, not executed
#   5. def reverse(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse(...))
#      → calls reverse(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return lst
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(reverse(...)) → reverse() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse(...)) → reverse() runs first → value passed to print()

"""

"""
Problem 407:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_average(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * /
# Built-in functions: Fraction, float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_average(lst) runs first → then print() outputs the result
#   calculate_average(lst) → each argument is evaluated first, then the function body runs
#   calculate_average(?) runs first → then print() outputs the result
#   add_num / length_num → Python looks up add_num, then length_num, then applies /
#   add_num * ? → Python looks up add_num, then ?, then applies *
#   calculate_average(?) → each argument is evaluated first, then the function body runs
#   1 / length_num → Python looks up 1, then length_num, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_average(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(calculate_average(...))
#      → calls calculate_average(lst) with lst=lst
#      → 3.1 from fractions import Fraction
#      → 3.2 return float(Fraction(add_num, length_num))
#   4. def calculate_average(lst) → stored in memory, not executed
#   5. def calculate_average(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_average(...))
#      → calls calculate_average(lst) with lst=[1, 3, 5, 7]
#      → 9.1 from fractions import Fraction
#      → 9.2 return float(Fraction(add_num, length_num))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(calculate_average(...)) → calculate_average() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_average(...)) → calculate_average() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 408:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates(lst) runs first → then print() outputs the result
#   remove_duplicates(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates(?) runs first → then print() outputs the result
#   remove_duplicates(?) → each argument is evaluated first, then the function body runs
#   word in unique → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def remove_duplicates(lst) → stored in memory, not executed
#   5. def remove_duplicates(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 409:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def len_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   len_words(lst) runs first → then print() outputs the result
#   len_words(lst) → each argument is evaluated first, then the function body runs
#   len_words(?) runs first → then print() outputs the result
#   len_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def len_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(len_words(...))
#      → calls len_words(lst) with lst=lst
#      → 3.1 return result
#   4. def len_words(lst) → stored in memory, not executed
#   5. def len_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(len_words(...))
#      → calls len_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (len_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(len_words(...)) → len_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(len_words(...)) → len_words() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 410:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: len, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_string(lst) runs first → then print() outputs the result
#   filter_string(lst) → each argument is evaluated first, then the function body runs
#   filter_string(?) runs first → then print() outputs the result
#   filter_string(?) → each argument is evaluated first, then the function body runs
#   len(...) == 3 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(filter_string(...))
#      → calls filter_string(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word, lst))
#   4. def filter_string(lst) → stored in memory, not executed
#   5. def filter_string(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(filter_string(...))
#      → calls filter_string(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(filter_string(...)) → filter_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_string(...)) → filter_string() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 411:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_sum(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: inner_sum, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_sum(7, 3) → each argument is evaluated first, then the function body runs
#   calculate_sum(a, b) → each argument is evaluated first, then the function body runs
#   calculate_sum(20, 30) runs first → then print() outputs the result
#   calculate_sum(20, 30) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_sum(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_sum(a, b) → stored in memory, not executed
#   5. def calculate_sum(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_sum(...))
#      → calls calculate_sum(a, b) with a=20, b=30
#      → 10.1 import operator
#      → 10.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_sum(...)) → calculate_sum() runs first → value passed to print()

"""

"""
Problem 412:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_product(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, product, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_product(3, 5) → each argument is evaluated first, then the function body runs
#   calculate_product(a, b) → each argument is evaluated first, then the function body runs
#   calculate_product(20, 30) runs first → then print() outputs the result
#   calculate_product(20, 30) → each argument is evaluated first, then the function body runs
#   a * b → Python looks up a, then b, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_product(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_product(a, b) → stored in memory, not executed
#   5. def calculate_product(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_product(...))
#      → calls calculate_product(a, b) with a=20, b=30
#      → 10.1 return pow(a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_product) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_product(...)) → calculate_product() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 413:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_difference(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + -
# Built-in functions: difference()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_difference(10, 3) → each argument is evaluated first, then the function body runs
#   calculate_difference(a, b) → each argument is evaluated first, then the function body runs
#   calculate_difference(20, 30) runs first → then print() outputs the result
#   a + ? → Python looks up a, then ?, then applies +
#   calculate_difference(20, 30) → each argument is evaluated first, then the function body runs
#   a - b → Python looks up a, then b, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_difference(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_difference(a, b) → stored in memory, not executed
#   5. def calculate_difference(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_difference(...))
#      → calls calculate_difference(a, b) with a=20, b=30
#      → 10.1 import operator
#      → 10.2 return operator.sub(a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_difference) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_difference(...)) → calculate_difference() runs first → value passed to print()

"""

"""
Problem 414:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_quotient(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float, quotient()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_quotient(10, 2) → each argument is evaluated first, then the function body runs
#   calculate_quotient(a, b) → each argument is evaluated first, then the function body runs
#   calculate_quotient(20, 30) runs first → then print() outputs the result
#   a * ? → Python looks up a, then ?, then applies *
#   calculate_quotient(20, 30) → each argument is evaluated first, then the function body runs
#   a / b → Python looks up a, then b, then applies /
#   1 / b → Python looks up 1, then b, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_quotient(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def calculate_quotient(a, b) → stored in memory, not executed
#   5. def calculate_quotient(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_quotient(...))
#      → calls calculate_quotient(a, b) with a=20, b=30
#      → 10.1 from fractions import Fraction
#      → 10.2 return float(Fraction(a, b))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_quotient) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_quotient(...)) → calculate_quotient() runs first → value passed to print()

"""

"""
Problem 415:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def area_of_rectangle(length, width) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: area, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   area_of_rectangle(5, 4) → each argument is evaluated first, then the function body runs
#   area_of_rectangle(length, width) → each argument is evaluated first, then the function body runs
#   area_of_rectangle(25, 25) runs first → then print() outputs the result
#   area_of_rectangle(25, 25) → each argument is evaluated first, then the function body runs
#   length * width → Python looks up length, then width, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def area_of_rectangle(length, width) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def area_of_rectangle(length, width) → stored in memory, not executed
#   5. def area_of_rectangle(length, width) → stored in memory, not executed
#   6. length = ... → assigns value to variable
#   7. width = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(area_of_rectangle(...))
#      → calls area_of_rectangle(length, width) with length=25, width=25
#      → 10.1 return pow(length, width)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (area_of_rectangle) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     length = ... → stored in memory
#     width = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(area_of_rectangle(...)) → area_of_rectangle() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 416:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def perimeter_of_square(side_length) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: perimeter, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   perimeter_of_square(5) → each argument is evaluated first, then the function body runs
#   perimeter_of_square(side_length) → each argument is evaluated first, then the function body runs
#   perimeter_of_square(25) runs first → then print() outputs the result
#   perimeter_of_square(25) → each argument is evaluated first, then the function body runs
#   4 * side_length → Python looks up 4, then side_length, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def perimeter_of_square(side_length) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def perimeter_of_square(side_length) → stored in memory, not executed
#   5. def perimeter_of_square(side_length) → stored in memory, not executed
#   6. side_length = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(perimeter_of_square(...))
#      → calls perimeter_of_square(side_length) with side_length=25
#      → 9.1 return pow(4, side_length)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (perimeter_of_square) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     side_length = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(perimeter_of_square(...)) → perimeter_of_square() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 417:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fahrenheit_to_celsius(fahrenheit) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * - /
# Built-in functions: convert()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fahrenheit_to_celsius(68) → each argument is evaluated first, then the function body runs
#   fahrenheit_to_celsius(fahrenheit) → each argument is evaluated first, then the function body runs
#   fahrenheit_to_celsius(25) runs first → then print() outputs the result
#   ? / 9 → Python looks up ?, then 9, then applies /
#   fahrenheit_to_celsius(25) → each argument is evaluated first, then the function body runs
#   ? * 5 → Python looks up ?, then 5, then applies *
#   fahrenheit - 32 → Python looks up fahrenheit, then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fahrenheit_to_celsius(fahrenheit) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def fahrenheit_to_celsius(fahrenheit) → stored in memory, not executed
#   5. def fahrenheit_to_celsius(fahrenheit) → stored in memory, not executed
#   6. fahrenheit = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(fahrenheit_to_celsius(...))
#      → calls fahrenheit_to_celsius(fahrenheit) with fahrenheit=25
#      → 9.1 return (fahrenheit - 32) * 5 / 9
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fahrenheit_to_celsius) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     fahrenheit = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fahrenheit_to_celsius(...)) → fahrenheit_to_celsius() runs first → value passed to print()

"""

"""
Problem 418:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def celsius_to_fahrenheit(celsius) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
# Built-in functions: convert()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   celsius_to_fahrenheit(20) → each argument is evaluated first, then the function body runs
#   celsius_to_fahrenheit(celsius) → each argument is evaluated first, then the function body runs
#   celsius_to_fahrenheit(25) runs first → then print() outputs the result
#   ? + 32 → Python looks up ?, then 32, then applies +
#   celsius_to_fahrenheit(25) → each argument is evaluated first, then the function body runs
#   ? / 5 → Python looks up ?, then 5, then applies /
#   celsius * 9 → Python looks up celsius, then 9, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def celsius_to_fahrenheit(celsius) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def celsius_to_fahrenheit(celsius) → stored in memory, not executed
#   5. def celsius_to_fahrenheit(celsius) → stored in memory, not executed
#   6. celsius = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(celsius_to_fahrenheit(...))
#      → calls celsius_to_fahrenheit(celsius) with celsius=25
#      → 9.1 return celsius * 9 / 5 + 32
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (celsius_to_fahrenheit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     celsius = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(celsius_to_fahrenheit(...)) → celsius_to_fahrenheit() runs first → value passed to print()

"""

"""
Problem 419:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def even_or_odd(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: % ==
# Built-in functions: check()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   even_or_odd(7) → each argument is evaluated first, then the function body runs
#   even_or_odd(number) → each argument is evaluated first, then the function body runs
#   even_or_odd(25) runs first → then print() outputs the result
#   even_or_odd(25) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def even_or_odd(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def even_or_odd(number) → stored in memory, not executed
#   5. def even_or_odd(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(even_or_odd(...))
#      → calls even_or_odd(number) with number=25
#      → 9.1 return 'Even'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (even_or_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(even_or_odd(...)) → even_or_odd() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 420:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + < ** ==
# Built-in functions: check_divisibility, int, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_prime(29) → each argument is evaluated first, then the function body runs
#   is_prime(number) → each argument is evaluated first, then the function body runs
#   is_prime(25) runs first → then print() outputs the result
#   number < 2 → evaluated as True or False (determines which branch runs)
#   is_prime(25) → each argument is evaluated first, then the function body runs
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n % i → Python looks up n, then i, then applies %
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(number) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_prime(number) → stored in memory, not executed
#   5. def is_prime(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_prime(...))
#      → calls is_prime(number) with number=25
#      → 9.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 421:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_sum(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: add_elements()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_sum(?) → each argument is evaluated first, then the function body runs
#   list_sum(numbers) → each argument is evaluated first, then the function body runs
#   list_sum(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_sum(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def list_sum(numbers) → stored in memory, not executed
#   5. def list_sum(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(list_sum(...))
#      → calls list_sum(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_sum(...)) → list_sum() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 422:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_product(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: multiply_elements()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_product(?) → each argument is evaluated first, then the function body runs
#   list_product(numbers) → each argument is evaluated first, then the function body runs
#   list_product(?) runs first → then print() outputs the result
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_product(numbers) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def list_product(numbers) → stored in memory, not executed
#   5. def list_product(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(list_product(...))
#      → calls list_product(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return product
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_product) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_product(...)) → list_product() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 423:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def greater_than(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: > <=
# Built-in functions: compare()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   greater_than(10, 5) → each argument is evaluated first, then the function body runs
#   greater_than(a, b) → each argument is evaluated first, then the function body runs
#   greater_than(20, 30) runs first → then print() outputs the result
#   a > b → evaluated as True or False (determines which branch runs)
#   greater_than(20, 30) → each argument is evaluated first, then the function body runs
#   a <= b → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def greater_than(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def greater_than(a, b) → stored in memory, not executed
#   5. def greater_than(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(greater_than(...))
#      → calls greater_than(a, b) with a=20, b=30
#      → 10.1 return not a <= b
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (greater_than) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(greater_than(...)) → greater_than() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 424:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_length(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: calculate_length()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_length('Hello, World!') → each argument is evaluated first, then the function body runs
#   string_length(s) → each argument is evaluated first, then the function body runs
#   string_length(?) runs first → then print() outputs the result
#   string_length(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_length(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def string_length(s) → stored in memory, not executed
#   5. def string_length(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(string_length(...))
#      → calls string_length(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return length
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (string_length) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_length(...)) → string_length() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 425:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def first_and_last(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: get_first_last, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   first_and_last('Hello') → each argument is evaluated first, then the function body runs
#   first_and_last(s) → each argument is evaluated first, then the function body runs
#   first_and_last(?) runs first → then print() outputs the result
#   first_and_last(?) → each argument is evaluated first, then the function body runs
#   len(...) > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def first_and_last(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def first_and_last(s) → stored in memory, not executed
#   5. def first_and_last(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(first_and_last(...))
#      → calls first_and_last(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return (s[0], s[-1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (first_and_last) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(first_and_last(...)) → first_and_last() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 426:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: is_vowel, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels('Hello, World!') → each argument is evaluated first, then the function body runs
#   count_vowels(s) → each argument is evaluated first, then the function body runs
#   count_vowels(?) runs first → then print() outputs the result
#   count_vowels(?) → each argument is evaluated first, then the function body runs
#   char.lower(...) in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def count_vowels(s) → stored in memory, not executed
#   5. def count_vowels(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_vowels(...))
#      → calls count_vowels(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return len([char for char in s if is_vowel(char)])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 427:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_string(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: reverse()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_string('Hello') → each argument is evaluated first, then the function body runs
#   reverse_string(s) → each argument is evaluated first, then the function body runs
#   reverse_string(?) runs first → then print() outputs the result
#   reverse_string(?) → each argument is evaluated first, then the function body runs
#   char + reversed_s → Python looks up char, then reversed_s, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_string(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def reverse_string(s) → stored in memory, not executed
#   5. def reverse_string(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_string(...))
#      → calls reverse_string(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return reversed_s
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_string(...)) → reverse_string() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done

"""

"""
Problem 428:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_palindrome(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: check()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_palindrome('racecar') → each argument is evaluated first, then the function body runs
#   is_palindrome(s) → each argument is evaluated first, then the function body runs
#   is_palindrome(?) runs first → then print() outputs the result
#   s == ? → evaluated as True or False (determines which branch runs)
#   is_palindrome(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_palindrome(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_palindrome(s) → stored in memory, not executed
#   5. def is_palindrome(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_palindrome(...))
#      → calls is_palindrome(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return s == s[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_palindrome(...)) → is_palindrome() runs first → value passed to print()

"""

"""
Problem 429:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_by_n(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: multiply_by_3, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_by_n(3) → each argument is evaluated first, then the function body runs
#   multiply_by_n(n) → each argument is evaluated first, then the function body runs
#   multiply_by_n(15) runs first → then print() outputs the result
#   multiply_by_n(15) → each argument is evaluated first, then the function body runs
#   n * x → Python looks up n, then x, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_by_n(n) → stored in memory, not executed
#   2. multiply_by_3 = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def multiply_by_n(n) → stored in memory, not executed
#   6. def multiply_by_n(n) → stored in memory, not executed
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(multiply_by_n(...))
#      → calls multiply_by_n(n) with n=15
#      → 10.1 return pow(n, x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_by_n) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     multiply_by_3 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_by_n(...)) → multiply_by_n() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 430:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_tax(price) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
# Built-in functions: calculate_tax()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_tax(100) → each argument is evaluated first, then the function body runs
#   add_tax(price) → each argument is evaluated first, then the function body runs
#   add_tax(25) runs first → then print() outputs the result
#   price + ? → Python looks up price, then ?, then applies +
#   add_tax(25) → each argument is evaluated first, then the function body runs
#   price * tax_rate → Python looks up price, then tax_rate, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_tax(price) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def add_tax(price) → stored in memory, not executed
#   5. def add_tax(price) → stored in memory, not executed
#   6. price = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(add_tax(...))
#      → calls add_tax(price) with price=25
#      → 9.1 return price + price * tax_rate
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_tax) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     price = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_tax(...)) → add_tax() runs first → value passed to print()

"""

"""
Problem 431:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def greet_person(name) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
# Built-in functions: greeting()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   greet_person('Alice') → each argument is evaluated first, then the function body runs
#   greet_person(name) → each argument is evaluated first, then the function body runs
#   greet_person('Bob') runs first → then print() outputs the result
#   'Hello, %s!' % name → Python looks up 'Hello, %s!', then name, then applies %
#   greet_person('Bob') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def greet_person(name) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def greet_person(name) → stored in memory, not executed
#   5. def greet_person(name) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(greet_person(...))
#      → calls greet_person(name) with name='Bob'
#      → 9.1 return 'Hello, %s!' % name
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (greet_person) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(greet_person(...)) → greet_person() runs first → value passed to print()

"""

"""
Problem 432:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: * - == or
# Built-in functions: calc_factorial()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial(5) → each argument is evaluated first, then the function body runs
#   factorial(n) → each argument is evaluated first, then the function body runs
#   factorial(15) runs first → then print() outputs the result
#   factorial(15) → each argument is evaluated first, then the function body runs
#   num == 0 → evaluated as True or False (determines which branch runs)
#   num == 1 → evaluated as True or False (determines which branch runs)
#   num * calc_factorial(...) → Python looks up num, then calc_factorial(...), then applies *
#   num - 1 → Python looks up num, then 1, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def factorial(n) → stored in memory, not executed
#   5. def factorial(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(factorial(...))
#      → calls factorial(n) with n=15
#      → 9.1 return 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial(...)) → factorial() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 433:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def exponent(base, power) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: calculate, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   exponent(2, 3) → each argument is evaluated first, then the function body runs
#   exponent(base, power) → each argument is evaluated first, then the function body runs
#   exponent(10, 25) runs first → then print() outputs the result
#   exponent(10, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def exponent(base, power) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def exponent(base, power) → stored in memory, not executed
#   5. def exponent(base, power) → stored in memory, not executed
#   6. base = ... → assigns value to variable
#   7. power = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(exponent(...))
#      → calls exponent(base, power) with base=10, power=25
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (exponent) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     base = ... → stored in memory
#     power = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(exponent(...)) → exponent() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 434:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def gcd(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: % !=
# Built-in functions: calculate_gcd()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   gcd(48, 18) → each argument is evaluated first, then the function body runs
#   gcd(a, b) → each argument is evaluated first, then the function body runs
#   gcd(20, 30) runs first → then print() outputs the result
#   gcd(20, 30) → each argument is evaluated first, then the function body runs
#   y != 0 → evaluated as True or False (determines which branch runs)
#   x % y → Python looks up x, then y, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def gcd(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def gcd(a, b) → stored in memory, not executed
#   5. def gcd(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(gcd(...))
#      → calls gcd(a, b) with a=20, b=30
#      → 10.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (gcd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(gcd(...)) → gcd() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 435:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lcm(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: % * != //
# Built-in functions: abs, calculate_lcm, gcd()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lcm(4, 5) → each argument is evaluated first, then the function body runs
#   lcm(a, b) → each argument is evaluated first, then the function body runs
#   lcm(20, 30) runs first → then print() outputs the result
#   abs(...) // gcd(...) → Python looks up abs(...), then gcd(...), then applies //
#   lcm(20, 30) → each argument is evaluated first, then the function body runs
#   y != 0 → evaluated as True or False (determines which branch runs)
#   x * y → Python looks up x, then y, then applies *
#   x % y → Python looks up x, then y, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lcm(a, b) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def lcm(a, b) → stored in memory, not executed
#   5. def lcm(a, b) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lcm(...))
#      → calls lcm(a, b) with a=20, b=30
#      → 10.1 return abs(x * y) // gcd(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lcm) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lcm(...)) → lcm() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 436:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_of_number(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range, square()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_of_number(5) → each argument is evaluated first, then the function body runs
#   square_of_number(n) → each argument is evaluated first, then the function body runs
#   square_of_number(15) runs first → then print() outputs the result
#   square_of_number(15) → each argument is evaluated first, then the function body runs
#   n * n → Python looks up n, then n, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_of_number(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def square_of_number(n) → stored in memory, not executed
#   5. def square_of_number(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(square_of_number(...))
#      → calls square_of_number(n) with n=15
#      → 9.1 return pow(n, n)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_of_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_of_number(...)) → square_of_number() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 437:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def cube_of_number(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: cube()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   cube_of_number(3) → each argument is evaluated first, then the function body runs
#   cube_of_number(n) → each argument is evaluated first, then the function body runs
#   cube_of_number(15) runs first → then print() outputs the result
#   ? * n → Python looks up ?, then n, then applies *
#   cube_of_number(15) → each argument is evaluated first, then the function body runs
#   n * n → Python looks up n, then n, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def cube_of_number(n) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def cube_of_number(n) → stored in memory, not executed
#   5. def cube_of_number(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(cube_of_number(...))
#      → calls cube_of_number(n) with n=15
#      → 9.1 return n * n * n
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (cube_of_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(cube_of_number(...)) → cube_of_number() runs first → value passed to print()

"""

"""
Problem 438:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_list_descending(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else | while
# Operators: >
# Built-in functions: list, map, sort()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_list_descending(?) → each argument is evaluated first, then the function body runs
#   sort_list_descending(lst) → each argument is evaluated first, then the function body runs
#   sort_list_descending(?) runs first → then print() outputs the result
#   item > maximum → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_list_descending(lst) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def sort_list_descending(lst) → stored in memory, not executed
#   5. def sort_list_descending(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_list_descending(...))
#      → calls sort_list_descending(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return list(map(lambda item: maximum, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_list_descending) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_list_descending(...)) → sort_list_descending() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 439:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_of_three(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: find_max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_of_three(10, 20, 15) → each argument is evaluated first, then the function body runs
#   max_of_three(a, b, c) → each argument is evaluated first, then the function body runs
#   max_of_three(20, 30, 10) runs first → then print() outputs the result
#   max_of_three(20, 30, 10) → each argument is evaluated first, then the function body runs
#   b > max_value → evaluated as True or False (determines which branch runs)
#   c > max_value → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_of_three(a, b, c) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def max_of_three(a, b, c) → stored in memory, not executed
#   5. def max_of_three(a, b, c) → stored in memory, not executed
#   6. a = ... → assigns value to variable
#   7. b = ... → assigns value to variable
#   8. c = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(max_of_three(...))
#      → calls max_of_three(a, b, c) with a=20, b=30, c=10
#      → 11.1 return max_value
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_of_three) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_of_three(...)) → max_of_three() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 440:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average_of_list(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: / !=
# Built-in functions: calculate_average()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average_of_list(?) → each argument is evaluated first, then the function body runs
#   average_of_list(lst) → each argument is evaluated first, then the function body runs
#   average_of_list(?) runs first → then print() outputs the result
#   count != 0 → evaluated as True or False (determines which branch runs)
#   total / count → Python looks up total, then count, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average_of_list(lst) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def average_of_list(lst) → stored in memory, not executed
#   5. def average_of_list(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(average_of_list(...))
#      → calls average_of_list(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return total / count if count != 0 else 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_of_list(...)) → average_of_list() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 441:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average_of_list(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: / !=
# Built-in functions: calculate_average()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average_of_list(?) → each argument is evaluated first, then the function body runs
#   average_of_list(lst) → each argument is evaluated first, then the function body runs
#   average_of_list(?) runs first → then print() outputs the result
#   count != 0 → evaluated as True or False (determines which branch runs)
#   total / count → Python looks up total, then count, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average_of_list(lst) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def average_of_list(lst) → stored in memory, not executed
#   5. def average_of_list(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(average_of_list(...))
#      → calls average_of_list(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return total / count if count != 0 else 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_of_list(...)) → average_of_list() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 442:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_uppercase(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: check()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_uppercase('HELLO') → each argument is evaluated first, then the function body runs
#   is_uppercase(s) → each argument is evaluated first, then the function body runs
#   is_uppercase(?) runs first → then print() outputs the result
#   is_uppercase(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_uppercase(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_uppercase(s) → stored in memory, not executed
#   5. def is_uppercase(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_uppercase(...))
#      → calls is_uppercase(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_uppercase(...)) → is_uppercase() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 443:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_lowercase(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: check()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   is_lowercase('hello') → each argument is evaluated first, then the function body runs
#   is_lowercase(s) → each argument is evaluated first, then the function body runs
#   is_lowercase(?) runs first → then print() outputs the result
#   is_lowercase(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_lowercase(s) → stored in memory, not executed
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def is_lowercase(s) → stored in memory, not executed
#   5. def is_lowercase(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(is_lowercase(...))
#      → calls is_lowercase(s) with s=[4, 1, 8, 6, 3]
#      → 9.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_lowercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     result = ... → stored in memory
#     print(result)
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_lowercase(...)) → is_lowercase() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 444:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_contains_digit(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: contains_digit()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_contains_digit(string) runs first → then print() outputs the result
#   string_contains_digit(string) → each argument is evaluated first, then the function body runs
#   string_contains_digit('world') runs first → then print() outputs the result
#   string_contains_digit('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_contains_digit(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(string_contains_digit(...))
#      → calls string_contains_digit(string) with string=string
#      → 3.1 return True
#   4. def string_contains_digit(string) → stored in memory, not executed
#   5. def string_contains_digit(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(string_contains_digit(...))
#      → calls string_contains_digit(string) with string='world'
#      → 9.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (string_contains_digit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(string_contains_digit(...)) → string_contains_digit() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_contains_digit(...)) → string_contains_digit() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 445:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_contains_letter(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: contains_letter()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_contains_letter(string) runs first → then print() outputs the result
#   string_contains_letter(string) → each argument is evaluated first, then the function body runs
#   string_contains_letter('world') runs first → then print() outputs the result
#   string_contains_letter('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_contains_letter(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(string_contains_letter(...))
#      → calls string_contains_letter(string) with string=string
#      → 3.1 return True
#   4. def string_contains_letter(string) → stored in memory, not executed
#   5. def string_contains_letter(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(string_contains_letter(...))
#      → calls string_contains_letter(string) with string='world'
#      → 9.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (string_contains_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(string_contains_letter(...)) → string_contains_letter() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_contains_letter(...)) → string_contains_letter() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 446:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average_of_list(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * /
# Built-in functions: Fraction, average, float, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average_of_list(lst) runs first → then print() outputs the result
#   average_of_list(lst) → each argument is evaluated first, then the function body runs
#   average_of_list(?) runs first → then print() outputs the result
#   add_num * ? → Python looks up add_num, then ?, then applies *
#   average_of_list(?) → each argument is evaluated first, then the function body runs
#   1 / count_len → Python looks up 1, then count_len, then applies /
#   add_num / count_len → Python looks up add_num, then count_len, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average_of_list(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(average_of_list(...))
#      → calls average_of_list(lst) with lst=lst
#      → 3.1 from fractions import Fraction
#      → 3.2 return float(Fraction(add_num, count_len))
#   4. def average_of_list(lst) → stored in memory, not executed
#   5. def average_of_list(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(average_of_list(...))
#      → calls average_of_list(lst) with lst=[1, 3, 5, 7]
#      → 9.1 from fractions import Fraction
#      → 9.2 return float(Fraction(add_num, count_len))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(average_of_list(...)) → average_of_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_of_list(...)) → average_of_list() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done

"""

"""
Problem 447:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_multiple_of_n(num1, num2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: is_multiple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_multiple_of_n(10, 2) runs first → then print() outputs the result
#   check_multiple_of_n(10, 3) runs first → then print() outputs the result
#   check_multiple_of_n(num1, num2) → each argument is evaluated first, then the function body runs
#   check_multiple_of_n(20, 30) runs first → then print() outputs the result
#   check_multiple_of_n(10, 2) → each argument is evaluated first, then the function body runs
#   check_multiple_of_n(10, 3) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   check_multiple_of_n(20, 30) → each argument is evaluated first, then the function body runs
#   x % y → Python looks up x, then y, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_multiple_of_n(num1, num2) → stored in memory, not executed
#   2. print(check_multiple_of_n(...))
#      → calls check_multiple_of_n(num1, num2) with num1=10, num2=2
#      → 2.1 return x % y == 0
#   3. print(check_multiple_of_n(...))
#      → calls check_multiple_of_n(num1, num2) with num1=10, num2=3
#      → 3.1 return x % y == 0
#   4. def check_multiple_of_n(num1, num2) → stored in memory, not executed
#   5. def check_multiple_of_n(num1, num2) → stored in memory, not executed
#   6. num1 = ... → assigns value to variable
#   7. num2 = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(check_multiple_of_n(...))
#      → calls check_multiple_of_n(num1, num2) with num1=20, num2=30
#      → 10.1 return x % y == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_multiple_of_n) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(check_multiple_of_n(...)) → check_multiple_of_n() runs first → value passed to print()
#     print(check_multiple_of_n(...)) → check_multiple_of_n() runs first → value passed to print()
#     num1 = ... → stored in memory
#     num2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_multiple_of_n(...)) → check_multiple_of_n() runs first → value passed to print()

"""

"""
Problem 448:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def longest_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: find_longest, longest, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   longest_string(lst) runs first → then print() outputs the result
#   longest_string(lst) → each argument is evaluated first, then the function body runs
#   longest_string(?) runs first → then print() outputs the result
#   longest_string(?) → each argument is evaluated first, then the function body runs
#   word > longest_word → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def longest_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(longest_string(...))
#      → calls longest_string(lst) with lst=lst
#      → 3.1 return longest_word
#   4. def longest_string(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(longest_string(...))
#      → calls longest_string(lst) with lst=lst
#      → 6.1 return longest_word
#   7. def longest_string(lst) → stored in memory, not executed
#   8. def longest_string(lst) → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(longest_string(...))
#      → calls longest_string(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return longest_word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (longest_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(longest_string(...)) → longest_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(longest_string(...)) → longest_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(longest_string(...)) → longest_string() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 449:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def shortest_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min, shortest()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   shortest_string(lst) runs first → then print() outputs the result
#   shortest_string(lst) → each argument is evaluated first, then the function body runs
#   shortest_string(?) runs first → then print() outputs the result
#   shortest_string(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def shortest_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(shortest_string(...))
#      → calls shortest_string(lst) with lst=lst
#      → 3.1 return min(x, key=len)
#   4. def shortest_string(lst) → stored in memory, not executed
#   5. def shortest_string(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(shortest_string(...))
#      → calls shortest_string(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return min(x, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (shortest_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(shortest_string(...)) → shortest_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(shortest_string(...)) → shortest_string() runs first → value passed to print()

"""

"""
Problem 450:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: duplicates_removed, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates(?) runs first → then print() outputs the result
#   remove_duplicates(lst) → each argument is evaluated first, then the function body runs
#   remove_duplicates(?) → each argument is evaluated first, then the function body runs
#   item not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates(lst) → stored in memory, not executed
#   2. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=['not', 'not', 'today', 'today']
#      → 2.1 return list(map(lambda item: item, items))
#   3. def remove_duplicates(lst) → stored in memory, not executed
#   4. def remove_duplicates(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(remove_duplicates(...))
#      → calls remove_duplicates(lst) with lst=[1, 3, 5, 7]
#      → 8.1 return list(map(lambda item: item, items))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates(...)) → remove_duplicates() runs first → value passed to print()
#
#   Loop: takes one item from items each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 451:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fibonacci(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: + - <=
# Built-in functions: fib_recursive()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fibonacci(n) runs first → then print() outputs the result
#   fibonacci(n) → each argument is evaluated first, then the function body runs
#   fibonacci(15) runs first → then print() outputs the result
#   fibonacci(15) → each argument is evaluated first, then the function body runs
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   fib_recursive(...) + fib_recursive(...) → Python looks up fib_recursive(...), then fib_recursive(...), then applies +
#   n - 1 → Python looks up n, then 1, then applies -
#   n - 2 → Python looks up n, then 2, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def fibonacci(n) → stored in memory, not executed
#   2. n = ... → assigns value to variable
#   3. print(fibonacci(...))
#      → calls fibonacci(n) with n=n
#      → 3.1 return n
#   4. def fibonacci(n) → stored in memory, not executed
#   5. def fibonacci(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(fibonacci(...))
#      → calls fibonacci(n) with n=15
#      → 9.1 return n
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fibonacci) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     n = ... → stored in memory
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fibonacci(...)) → fibonacci() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 452:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_sum(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
# Built-in functions: inner_mult, inner_sum, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_sum(7, 3) runs first → then print() outputs the result
#   calculate_sum(a, b) → each argument is evaluated first, then the function body runs
#   calculate_sum(20, 30) runs first → then print() outputs the result
#   calculate_sum(7, 3) → each argument is evaluated first, then the function body runs
#   calculate_sum(20, 30) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#   x * y → Python looks up x, then y, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_sum(a, b) → stored in memory, not executed
#   2. print(calculate_sum(...))
#      → calls calculate_sum(a, b) with a=7, b=3
#      → 2.1 import operator
#      → 2.2 return operator.add(x, y)
#   3. def calculate_sum(a, b) → stored in memory, not executed
#   4. def calculate_sum(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_sum(...))
#      → calls calculate_sum(a, b) with a=20, b=30
#      → 9.1 import operator
#      → 9.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(calculate_sum(...)) → calculate_sum() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_sum(...)) → calculate_sum() runs first → value passed to print()

"""

"""
Problem 453:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_integers(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * + /
# Built-in functions: division, pow, product, range, sum_int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_integers(10, 5) runs first → then print() outputs the result
#   calculate_integers(a, b) → each argument is evaluated first, then the function body runs
#   calculate_integers(20, 30) runs first → then print() outputs the result
#   calculate_integers(10, 5) → each argument is evaluated first, then the function body runs
#   calculate_integers(20, 30) → each argument is evaluated first, then the function body runs
#   x * y → Python looks up x, then y, then applies *
#   x + y → Python looks up x, then y, then applies +
#   x / y → Python looks up x, then y, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_integers(a, b) → stored in memory, not executed
#   2. print(calculate_integers(...))
#      → calls calculate_integers(a, b) with a=10, b=5
#      → 2.1 return pow(x, y)
#   3. def calculate_integers(a, b) → stored in memory, not executed
#   4. def calculate_integers(a, b) → stored in memory, not executed
#   5. a = ... → assigns value to variable
#   6. b = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(calculate_integers(...))
#      → calls calculate_integers(a, b) with a=20, b=30
#      → 9.1 return pow(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(calculate_integers(...)) → calculate_integers() runs first → value passed to print()
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_integers(...)) → calculate_integers() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 455:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average_of_list(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: * /
# Built-in functions: Fraction, average, float, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average_of_list(lst) runs first → then print() outputs the result
#   average_of_list(lst) → each argument is evaluated first, then the function body runs
#   average_of_list(?) runs first → then print() outputs the result
#   add_num * ? → Python looks up add_num, then ?, then applies *
#   average_of_list(?) → each argument is evaluated first, then the function body runs
#   1 / count_len → Python looks up 1, then count_len, then applies /
#   add_num / count_len → Python looks up add_num, then count_len, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average_of_list(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(average_of_list(...))
#      → calls average_of_list(lst) with lst=lst
#      → 3.1 from fractions import Fraction
#      → 3.2 return float(Fraction(add_num, count_len))
#   4. def average_of_list(lst) → stored in memory, not executed
#   5. def average_of_list(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(average_of_list(...))
#      → calls average_of_list(lst) with lst=[1, 3, 5, 7]
#      → 9.1 from fractions import Fraction
#      → 9.2 return float(Fraction(add_num, count_len))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(average_of_list(...)) → average_of_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_of_list(...)) → average_of_list() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done

"""

"""
Problem 456:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def len_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   len_lst(lst) runs first → then print() outputs the result
#   len_lst(lst) → each argument is evaluated first, then the function body runs
#   len_lst(?) runs first → then print() outputs the result
#   len_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def len_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(len_lst(...))
#      → calls len_lst(lst) with lst=lst
#      → 3.1 return sort
#   4. def len_lst(lst) → stored in memory, not executed
#   5. def len_lst(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(len_lst(...))
#      → calls len_lst(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return sort
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (len_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(len_lst(...)) → len_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(len_lst(...)) → len_lst() runs first → value passed to print()

"""

"""
Problem 457:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def min_len(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   min_len(lst) runs first → then print() outputs the result
#   min_len(lst) → each argument is evaluated first, then the function body runs
#   min_len(?) runs first → then print() outputs the result
#   min_len(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def min_len(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(min_len(...))
#      → calls min_len(lst) with lst=lst
#      → 3.1 return min_string
#   4. def min_len(lst) → stored in memory, not executed
#   5. def min_len(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(min_len(...))
#      → calls min_len(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return min_string
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (min_len) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(min_len(...)) → min_len() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(min_len(...)) → min_len() runs first → value passed to print()

"""

"""
Problem 458:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_string_alpha(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_string_alpha(?) runs first → then print() outputs the result
#   max_string_alpha(lst) → each argument is evaluated first, then the function body runs
#   max_string_alpha(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_string_alpha(lst) → stored in memory, not executed
#   2. print(max_string_alpha(...))
#      → calls max_string_alpha(lst) with lst=['apple', 'banana', 'cherry']
#      → 2.1 return word
#   3. def max_string_alpha(lst) → stored in memory, not executed
#   4. def max_string_alpha(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(max_string_alpha(...))
#      → calls max_string_alpha(lst) with lst=[1, 3, 5, 7]
#      → 8.1 return word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_string_alpha) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(max_string_alpha(...)) → max_string_alpha() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_string_alpha(...)) → max_string_alpha() runs first → value passed to print()

"""

"""
Problem 459:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_dicts_by_key(dict_list, sort_key) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: itemgetter, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_dicts_by_key(dict_list, 'age') → each argument is evaluated first, then the function body runs
#   sort_dicts_by_key(dict_list, sort_key) → each argument is evaluated first, then the function body runs
#   sort_dicts_by_key(?, ?) runs first → then print() outputs the result
#   sort_dicts_by_key(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from operator import itemgetter
#   2. def sort_dicts_by_key(dict_list, sort_key) → stored in memory, not executed
#   3. dict_list = ... → assigns value to variable
#   4. sorted_list = ... → assigns value to variable
#   5. print(sorted_list)
#   6. def sort_dicts_by_key(dict_list, sort_key) → stored in memory, not executed
#   7. def sort_dicts_by_key(dict_list, sort_key) → stored in memory, not executed
#   8. dict_list = ... → assigns value to variable
#   9. sort_key = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(sort_dicts_by_key(...))
#      → calls sort_dicts_by_key(dict_list, sort_key) with dict_list=[4, 1, 8, 6, 3], sort_key=[4, 1, 8, 6, 3]
#      → 12.1 return sorted(dict_list, key=itemgetter(sort_key))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_dicts_by_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict_list = ... → stored in memory
#     sorted_list = ... → stored in memory
#     print(sorted_list)
#     dict_list = ... → stored in memory
#     sort_key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_dicts_by_key(...)) → sort_dicts_by_key() runs first → value passed to print()

"""

"""
Problem 460:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_by_absolute_value(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_absolute_value(lst) → each argument is evaluated first, then the function body runs
#   sort_by_absolute_value(?) runs first → then print() outputs the result
#   sort_by_absolute_value(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_by_absolute_value(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. sorted_lst = ... → assigns value to variable
#   4. print(sorted_lst)
#   5. def sort_by_absolute_value(lst) → stored in memory, not executed
#   6. def sort_by_absolute_value(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_by_absolute_value(...))
#      → calls sort_by_absolute_value(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return sorted(lst, key=abs)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_absolute_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     sorted_lst = ... → stored in memory
#     print(sorted_lst)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_by_absolute_value(...)) → sort_by_absolute_value() runs first → value passed to print()

"""

"""
Problem 461:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_words_by_vowel_count(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: len, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_words_by_vowel_count(lst) runs first → then print() outputs the result
#   sort_words_by_vowel_count(lst) → each argument is evaluated first, then the function body runs
#   sort_words_by_vowel_count(?) runs first → then print() outputs the result
#   sort_words_by_vowel_count(?) → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_words_by_vowel_count(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_words_by_vowel_count(...))
#      → calls sort_words_by_vowel_count(lst) with lst=lst
#      → 3.1 return len([char for char in word if char in vowels])
#   4. def sort_words_by_vowel_count(lst) → stored in memory, not executed
#   5. def sort_words_by_vowel_count(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_words_by_vowel_count(...))
#      → calls sort_words_by_vowel_count(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return len([char for char in word if char in vowels])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_words_by_vowel_count) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_words_by_vowel_count(...)) → sort_words_by_vowel_count() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_words_by_vowel_count(...)) → sort_words_by_vowel_count() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 462:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def longest_word(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   longest_word(lst) runs first → then print() outputs the result
#   longest_word(lst) → each argument is evaluated first, then the function body runs
#   longest_word(?) runs first → then print() outputs the result
#   longest_word(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def longest_word(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(longest_word(...))
#      → calls longest_word(lst) with lst=lst
#      → 3.1 return longest_w
#   4. def longest_word(lst) → stored in memory, not executed
#   5. def longest_word(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(longest_word(...))
#      → calls longest_word(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return longest_w
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (longest_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(longest_word(...)) → longest_word() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(longest_word(...)) → longest_word() runs first → value passed to print()

"""

"""
Problem 463:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_tuple(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_tuple(lst) runs first → then print() outputs the result
#   sum_of_tuple(lst) → each argument is evaluated first, then the function body runs
#   sum_of_tuple(?) runs first → then print() outputs the result
#   sum_of_tuple(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sum_of_tuple(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sum_of_tuple(...))
#      → calls sum_of_tuple(lst) with lst=lst
#      → 3.1 return sorted(lst, key=sum)
#   4. def sum_of_tuple(lst) → stored in memory, not executed
#   5. def sum_of_tuple(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sum_of_tuple(...))
#      → calls sum_of_tuple(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return sorted(lst, key=sum)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_tuple) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sum_of_tuple(...)) → sum_of_tuple() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_tuple(...)) → sum_of_tuple() runs first → value passed to print()

"""

"""
Problem 464:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_of_strings_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_of_strings_lst(lst) runs first → then print() outputs the result
#   list_of_strings_lst(lst) → each argument is evaluated first, then the function body runs
#   list_of_strings_lst(?) runs first → then print() outputs the result
#   list_of_strings_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_of_strings_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_of_strings_lst(...))
#      → calls list_of_strings_lst(lst) with lst=lst
#      → 3.1 return s[-1]
#   4. def list_of_strings_lst(lst) → stored in memory, not executed
#   5. def list_of_strings_lst(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(list_of_strings_lst(...))
#      → calls list_of_strings_lst(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return s[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_of_strings_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_of_strings_lst(...)) → list_of_strings_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_of_strings_lst(...)) → list_of_strings_lst() runs first → value passed to print()

"""

"""
Problem 465:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_number_uppercases(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: len, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_number_uppercases(lst) runs first → then print() outputs the result
#   sort_number_uppercases(lst) → each argument is evaluated first, then the function body runs
#   sort_number_uppercases(?) runs first → then print() outputs the result
#   sort_number_uppercases(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_number_uppercases(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_number_uppercases(...))
#      → calls sort_number_uppercases(lst) with lst=lst
#      → 3.1 return len([char for char in x if char.isupper()])
#   4. def sort_number_uppercases(lst) → stored in memory, not executed
#   5. def sort_number_uppercases(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_number_uppercases(...))
#      → calls sort_number_uppercases(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return len([char for char in x if char.isupper()])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_number_uppercases) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_number_uppercases(...)) → sort_number_uppercases() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_number_uppercases(...)) → sort_number_uppercases() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 466:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def tuple_largest_product(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   tuple_largest_product(lst) runs first → then print() outputs the result
#   tuple_largest_product(lst) → each argument is evaluated first, then the function body runs
#   tuple_largest_product(?) runs first → then print() outputs the result
#   tuple_largest_product(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def tuple_largest_product(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(tuple_largest_product(...))
#      → calls tuple_largest_product(lst) with lst=lst
#      → 3.1 return count
#   4. def tuple_largest_product(lst) → stored in memory, not executed
#   5. def tuple_largest_product(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(tuple_largest_product(...))
#      → calls tuple_largest_product(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (tuple_largest_product) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(tuple_largest_product(...)) → tuple_largest_product() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(tuple_largest_product(...)) → tuple_largest_product() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done

"""

"""
Problem 467:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def floating_point(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   floating_point(numbers) runs first → then print() outputs the result
#   floating_point(numbers) → each argument is evaluated first, then the function body runs
#   floating_point(?) runs first → then print() outputs the result
#   floating_point(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def floating_point(numbers) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(floating_point(...))
#      → calls floating_point(numbers) with numbers=numbers
#      → 3.1 return sorted(numbers, key=round)
#   4. def floating_point(numbers) → stored in memory, not executed
#   5. def floating_point(numbers) → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(floating_point(...))
#      → calls floating_point(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(numbers, key=round)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (floating_point) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(floating_point(...)) → floating_point() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(floating_point(...)) → floating_point() runs first → value passed to print()

"""

"""
Problem 468:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def first_element_decending_order(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   first_element_decending_order(lst) runs first → then print() outputs the result
#   first_element_decending_order(lst) → each argument is evaluated first, then the function body runs
#   first_element_decending_order(?) runs first → then print() outputs the result
#   first_element_decending_order(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def first_element_decending_order(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(first_element_decending_order(...))
#      → calls first_element_decending_order(lst) with lst=lst
#      → 3.1 return x[0]
#   4. def first_element_decending_order(lst) → stored in memory, not executed
#   5. def first_element_decending_order(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(first_element_decending_order(...))
#      → calls first_element_decending_order(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return x[0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (first_element_decending_order) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(first_element_decending_order(...)) → first_element_decending_order() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(first_element_decending_order(...)) → first_element_decending_order() runs first → value passed to print()

"""

"""
Problem 469:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def number_of_consonants(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number_of_consonants(lst) runs first → then print() outputs the result
#   number_of_consonants(lst) → each argument is evaluated first, then the function body runs
#   number_of_consonants(?) runs first → then print() outputs the result
#   number_of_consonants(?) → each argument is evaluated first, then the function body runs
#   char in consonants_set → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def number_of_consonants(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(number_of_consonants(...))
#      → calls number_of_consonants(lst) with lst=lst
#      → 3.1 return count
#   4. def number_of_consonants(lst) → stored in memory, not executed
#   5. def number_of_consonants(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(number_of_consonants(...))
#      → calls number_of_consonants(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (number_of_consonants) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(number_of_consonants(...)) → number_of_consonants() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(number_of_consonants(...)) → number_of_consonants() runs first → value passed to print()
#
#   Loop: takes one item from word.lower(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 470:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def min_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   min_string(lst) runs first → then print() outputs the result
#   min_string(lst) → each argument is evaluated first, then the function body runs
#   min_string(?) runs first → then print() outputs the result
#   min_string(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def min_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(min_string(...))
#      → calls min_string(lst) with lst=lst
#      → 3.1 return min(lst, key=len)
#   4. def min_string(lst) → stored in memory, not executed
#   5. def min_string(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(min_string(...))
#      → calls min_string(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return min(lst, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (min_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(min_string(...)) → min_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(min_string(...)) → min_string() runs first → value passed to print()

"""

"""
Problem 471:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def length_of_value(lst, key) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   length_of_value(lst, 'b') → each argument is evaluated first, then the function body runs
#   length_of_value(lst, key) → each argument is evaluated first, then the function body runs
#   length_of_value(?, 'value') runs first → then print() outputs the result
#   length_of_value(?, 'value') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def length_of_value(lst, key) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. sorted_lst = ... → assigns value to variable
#   4. print(sorted_lst)
#   5. def length_of_value(lst, key) → stored in memory, not executed
#   6. def length_of_value(lst, key) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. key = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(length_of_value(...))
#      → calls length_of_value(lst, key) with lst=[1, 3, 5, 7], key='value'
#      → 11.1 return len(x.get(key, ''))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (length_of_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     sorted_lst = ... → stored in memory
#     print(sorted_lst)
#     lst = ... → stored in memory
#     key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(length_of_value(...)) → length_of_value() runs first → value passed to print()

"""

"""
Problem 472:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_person(lst_dict, key) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_person(lst_dict, key) runs first → then print() outputs the result
#   find_person(lst_dict, key) → each argument is evaluated first, then the function body runs
#   find_person(?, 'value') runs first → then print() outputs the result
#   find_person(?, 'value') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_person(lst_dict, key) → stored in memory, not executed
#   2. lst_dict = ... → assigns value to variable
#   3. key = ... → assigns value to variable
#   4. print(find_person(...))
#      → calls find_person(lst_dict, key) with lst_dict=lst_dict, key=key
#      → 4.1 return len(d.get(key, ''))
#   5. def find_person(lst_dict, key) → stored in memory, not executed
#   6. def find_person(lst_dict, key) → stored in memory, not executed
#   7. lst_dict = ... → assigns value to variable
#   8. key = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(find_person(...))
#      → calls find_person(lst_dict, key) with lst_dict=[4, 1, 8, 6, 3], key='value'
#      → 11.1 return len(d.get(key, ''))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (find_person) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst_dict = ... → stored in memory
#     key = ... → stored in memory
#     print(find_person(...)) → find_person() runs first → value passed to print()
#     lst_dict = ... → stored in memory
#     key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_person(...)) → find_person() runs first → value passed to print()

"""

"""
Problem 473:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst, divisor) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % * - //
# Built-in functions: divmod, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst, divisor) → each argument is evaluated first, then the function body runs
#   sort_by_remainder(lst, divisor) runs first → then print() outputs the result
#   main(?, 25) runs first → then print() outputs the result
#   x % divisor → Python looks up x, then divisor, then applies %
#   sort_by_remainder(lst, divisor) → each argument is evaluated first, then the function body runs
#   num - ? → Python looks up num, then ?, then applies -
#   main(?, 25) → each argument is evaluated first, then the function body runs
#   num % divisor → Python looks up num, then divisor, then applies %
#   remainder_key(x, divisor) → each argument is evaluated first, then the function body runs
#   divisor * ? → Python looks up divisor, then ?, then applies *
#   num // divisor → Python looks up num, then divisor, then applies //
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst, divisor) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. divisor = ... → assigns value to variable
#   4. main(lst, divisor) → calls main(lst, divisor) with lst=lst, divisor=divisor
#      → 4.1 return divmod(num, divisor)[1]
#   5. def remainder_key(x, divisor) → stored in memory, not executed
#   6. def sort_by_remainder(lst, divisor) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. divisor = ... → assigns value to variable
#   9. print(sort_by_remainder(...))
#      → calls sort_by_remainder(lst, divisor) with lst=lst, divisor=divisor
#      → 9.1 def key_func(x):
#      → 9.1     return remainder_key(x, divisor)
#      → 9.2 return sorted(lst, key=key_func)
#   10. def main(lst, divisor) → stored in memory, not executed
#   11. def main(lst, divisor) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. divisor = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(main(...))
#      → calls main(lst, divisor) with lst=[1, 3, 5, 7], divisor=25
#      → 16.1 return divmod(num, divisor)[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, sort_by_remainder, remainder_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     divisor = ... → stored in memory
#     main(lst, divisor) → jumps into main(), runs body, returns
#     lst = ... → stored in memory
#     divisor = ... → stored in memory
#     print(sort_by_remainder(...)) → sort_by_remainder() runs first → value passed to print()
#     lst = ... → stored in memory
#     divisor = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 474:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def year_key(date) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_year(?) runs first → then print() outputs the result
#   year_key(date) → each argument is evaluated first, then the function body runs
#   year_key(25) runs first → then print() outputs the result
#   sort_by_year(?) → each argument is evaluated first, then the function body runs
#   year_key(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from datetime import datetime
#   2. def year_key(date) → stored in memory, not executed
#   3. def sort_by_year(dates) → stored in memory, not executed
#   4. print(sort_by_year(...))
#      → calls sort_by_year(dates) with dates=['2023-10-01', '2020-01-15', '2025-12-31']
#      → 4.1 return sorted(dates, key=year_key)
#   5. def year_key(date) → stored in memory, not executed
#   6. def year_key(date) → stored in memory, not executed
#   7. date = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(year_key(...))
#      → calls year_key(date) with date=25
#      → 10.1 return datetime.strptime(date, '%Y-%m-%d').year
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_year, year_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(sort_by_year(...)) → sort_by_year() runs first → value passed to print()
#     date = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(year_key(...)) → year_key() runs first → value passed to print()

"""

"""
Problem 475:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_by_a_count(names) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_a_count(names) → each argument is evaluated first, then the function body runs
#   sort_by_a_count(?) runs first → then print() outputs the result
#   sort_by_a_count(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_by_a_count(names) → stored in memory, not executed
#   2. names = ... → assigns value to variable
#   3. sorted_names = ... → assigns value to variable
#   4. print(sorted_names)
#   5. def sort_by_a_count(names) → stored in memory, not executed
#   6. def sort_by_a_count(names) → stored in memory, not executed
#   7. names = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_by_a_count(...))
#      → calls sort_by_a_count(names) with names=[4, 1, 8, 6, 3]
#      → 10.1 return name.lower().count('a')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_a_count) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     names = ... → stored in memory
#     sorted_names = ... → stored in memory
#     print(sorted_names)
#     names = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_by_a_count(...)) → sort_by_a_count() runs first → value passed to print()

"""

"""
Problem 476:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_sqaure(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_sqaure(lst) runs first → then print() outputs the result
#   sort_sqaure(lst) → each argument is evaluated first, then the function body runs
#   sort_sqaure(?) runs first → then print() outputs the result
#   sort_sqaure(?) → each argument is evaluated first, then the function body runs
#   x * x → Python looks up x, then x, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_sqaure(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_sqaure(...))
#      → calls sort_sqaure(lst) with lst=lst
#      → 3.1 return pow(x, x)
#   4. def sort_sqaure(lst) → stored in memory, not executed
#   5. def sort_sqaure(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_sqaure(...))
#      → calls sort_sqaure(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(x, x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_sqaure) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_sqaure(...)) → sort_sqaure() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_sqaure(...)) → sort_sqaure() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 477:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def closest_to_target(lst, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: -
# Built-in functions: abs, min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   closest_to_target(lst, target) → each argument is evaluated first, then the function body runs
#   closest_to_target(?, 'goal') runs first → then print() outputs the result
#   closest_to_target(?, 'goal') → each argument is evaluated first, then the function body runs
#   x - target → Python looks up x, then target, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def closest_to_target(lst, target) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. closest_value = ... → assigns value to variable
#   5. print(closest_value)
#   6. def closest_to_target(lst, target) → stored in memory, not executed
#   7. def closest_to_target(lst, target) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. target = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(closest_to_target(...))
#      → calls closest_to_target(lst, target) with lst=[1, 3, 5, 7], target='goal'
#      → 12.1 return abs(x - target)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (closest_to_target) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     target = ... → stored in memory
#     closest_value = ... → stored in memory
#     print(closest_value)
#     lst = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(closest_to_target(...)) → closest_to_target() runs first → value passed to print()

"""

"""
Problem 478:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def length_of_first_element(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   length_of_first_element(lst) → each argument is evaluated first, then the function body runs
#   length_of_first_element(?) runs first → then print() outputs the result
#   length_of_first_element(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def length_of_first_element(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. sorted_list = ... → assigns value to variable
#   4. print(sorted_list)
#   5. def length_of_first_element(lst) → stored in memory, not executed
#   6. def length_of_first_element(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(length_of_first_element(...))
#      → calls length_of_first_element(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return len(x[0])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (length_of_first_element) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     sorted_list = ... → stored in memory
#     print(sorted_list)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(length_of_first_element(...)) → length_of_first_element() runs first → value passed to print()

"""

"""
Problem 479:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def number_of_spaces(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number_of_spaces(lst) runs first → then print() outputs the result
#   number_of_spaces(lst) → each argument is evaluated first, then the function body runs
#   number_of_spaces(?) runs first → then print() outputs the result
#   number_of_spaces(?) → each argument is evaluated first, then the function body runs
#   c == ' ' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def number_of_spaces(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(number_of_spaces(...))
#      → calls number_of_spaces(lst) with lst=lst
#      → 3.1 count = 0
#      → 3.2 for c in x:
#      → 3.2     if c == ' ':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def number_of_spaces(lst) → stored in memory, not executed
#   5. def number_of_spaces(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(number_of_spaces(...))
#      → calls number_of_spaces(lst) with lst=[1, 3, 5, 7]
#      → 9.1 count = 0
#      → 9.2 for c in x:
#      → 9.2     if c == ' ':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (number_of_spaces) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(number_of_spaces(...)) → number_of_spaces() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(number_of_spaces(...)) → number_of_spaces() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 480:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def most_vowels(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: len, max, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   most_vowels(lst) runs first → then print() outputs the result
#   most_vowels(lst) → each argument is evaluated first, then the function body runs
#   most_vowels(?) runs first → then print() outputs the result
#   most_vowels(?) → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def most_vowels(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(most_vowels(...))
#      → calls most_vowels(lst) with lst=lst
#      → 3.1 return len([char for char in word if char in vowels])
#   4. def most_vowels(lst) → stored in memory, not executed
#   5. def most_vowels(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(most_vowels(...))
#      → calls most_vowels(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return len([char for char in word if char in vowels])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (most_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(most_vowels(...)) → most_vowels() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(most_vowels(...)) → most_vowels() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 481:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_lst_integers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: abs, len, sorted, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_lst_integers(lst) runs first → then print() outputs the result
#   sort_lst_integers(lst) → each argument is evaluated first, then the function body runs
#   sort_lst_integers(?) runs first → then print() outputs the result
#   sort_lst_integers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_lst_integers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_lst_integers(...))
#      → calls sort_lst_integers(lst) with lst=lst
#      → 3.1 return len(str(abs(n)))
#   4. def sort_lst_integers(lst) → stored in memory, not executed
#   5. def sort_lst_integers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_lst_integers(...))
#      → calls sort_lst_integers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return len(str(abs(n)))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_lst_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_lst_integers(...)) → sort_lst_integers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_lst_integers(...)) → sort_lst_integers() runs first → value passed to print()

"""

"""
Problem 482:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_tuples_by_difference(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: -
# Built-in functions: abs, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_tuples_by_difference(lst) runs first → then print() outputs the result
#   sort_tuples_by_difference(lst) → each argument is evaluated first, then the function body runs
#   sort_tuples_by_difference(?) runs first → then print() outputs the result
#   sort_tuples_by_difference(?) → each argument is evaluated first, then the function body runs
#   ? - ? → Python looks up ?, then ?, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_tuples_by_difference(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_tuples_by_difference(...))
#      → calls sort_tuples_by_difference(lst) with lst=lst
#      → 3.1 return abs(tup[0] - tup[1])
#   4. def sort_tuples_by_difference(lst) → stored in memory, not executed
#   5. def sort_tuples_by_difference(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_tuples_by_difference(...))
#      → calls sort_tuples_by_difference(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return abs(tup[0] - tup[1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_tuples_by_difference) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_tuples_by_difference(...)) → sort_tuples_by_difference() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_tuples_by_difference(...)) → sort_tuples_by_difference() runs first → value passed to print()

"""

"""
Problem 483:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_strings_by_ascii(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: ord, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_strings_by_ascii(lst) runs first → then print() outputs the result
#   sort_strings_by_ascii(lst) → each argument is evaluated first, then the function body runs
#   sort_strings_by_ascii(?) runs first → then print() outputs the result
#   sort_strings_by_ascii(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_strings_by_ascii(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_strings_by_ascii(...))
#      → calls sort_strings_by_ascii(lst) with lst=lst
#      → 3.1 return ord(s[0])
#   4. def sort_strings_by_ascii(lst) → stored in memory, not executed
#   5. def sort_strings_by_ascii(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_strings_by_ascii(...))
#      → calls sort_strings_by_ascii(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return ord(s[0])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_strings_by_ascii) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_strings_by_ascii(...)) → sort_strings_by_ascii() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_strings_by_ascii(...)) → sort_strings_by_ascii() runs first → value passed to print()

"""

"""
Problem 484:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_lists_by_sum(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: reduce, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_lists_by_sum(lst) runs first → then print() outputs the result
#   sort_lists_by_sum(lst) → each argument is evaluated first, then the function body runs
#   sort_lists_by_sum(?) runs first → then print() outputs the result
#   sort_lists_by_sum(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_lists_by_sum(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_lists_by_sum(...))
#      → calls sort_lists_by_sum(lst) with lst=lst
#      → 3.1 from functools import reduce
#      → 3.2 import operator
#      → 3.3 return reduce(operator.add, sublist, 0)
#   4. def sort_lists_by_sum(lst) → stored in memory, not executed
#   5. def sort_lists_by_sum(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_lists_by_sum(...))
#      → calls sort_lists_by_sum(lst) with lst=[1, 3, 5, 7]
#      → 9.1 from functools import reduce
#      → 9.2 import operator
#      → 9.3 return reduce(operator.add, sublist, 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_lists_by_sum) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_lists_by_sum(...)) → sort_lists_by_sum() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_lists_by_sum(...)) → sort_lists_by_sum() runs first → value passed to print()
#
#   Loop: takes one item from sublist each round, runs body, repeats until done

"""

"""
Problem 485:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def number_of_consonants(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number_of_consonants(lst) runs first → then print() outputs the result
#   number_of_consonants(lst) → each argument is evaluated first, then the function body runs
#   number_of_consonants(?) runs first → then print() outputs the result
#   number_of_consonants(?) → each argument is evaluated first, then the function body runs
#   char in consonants_set → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def number_of_consonants(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(number_of_consonants(...))
#      → calls number_of_consonants(lst) with lst=lst
#      → 3.1 return count
#   4. def number_of_consonants(lst) → stored in memory, not executed
#   5. def number_of_consonants(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(number_of_consonants(...))
#      → calls number_of_consonants(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (number_of_consonants) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(number_of_consonants(...)) → number_of_consonants() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(number_of_consonants(...)) → number_of_consonants() runs first → value passed to print()
#
#   Loop: takes one item from word.lower(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 486:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_files_by_extension(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_files_by_extension(lst) runs first → then print() outputs the result
#   sort_files_by_extension(lst) → each argument is evaluated first, then the function body runs
#   sort_files_by_extension(?) runs first → then print() outputs the result
#   sort_files_by_extension(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_files_by_extension(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_files_by_extension(...))
#      → calls sort_files_by_extension(lst) with lst=lst
#      → 3.1 return filename.split('.')[-1]
#   4. def sort_files_by_extension(lst) → stored in memory, not executed
#   5. def sort_files_by_extension(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_files_by_extension(...))
#      → calls sort_files_by_extension(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return filename.split('.')[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_files_by_extension) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_files_by_extension(...)) → sort_files_by_extension() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_files_by_extension(...)) → sort_files_by_extension() runs first → value passed to print()

"""

"""
Problem 487:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sqaure_numbers_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sqaure_numbers_lst(lst) runs first → then print() outputs the result
#   sqaure_numbers_lst(lst) → each argument is evaluated first, then the function body runs
#   sqaure_numbers_lst(?) runs first → then print() outputs the result
#   sqaure_numbers_lst(?) → each argument is evaluated first, then the function body runs
#   n * n → Python looks up n, then n, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sqaure_numbers_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sqaure_numbers_lst(...))
#      → calls sqaure_numbers_lst(lst) with lst=lst
#      → 3.1 return pow(n, n)
#   4. def sqaure_numbers_lst(lst) → stored in memory, not executed
#   5. def sqaure_numbers_lst(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sqaure_numbers_lst(...))
#      → calls sqaure_numbers_lst(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(n, n)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sqaure_numbers_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sqaure_numbers_lst(...)) → sqaure_numbers_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sqaure_numbers_lst(...)) → sqaure_numbers_lst() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 488:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_words_by_distinct_letters(words) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, set, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_words_by_distinct_letters(words) runs first → then print() outputs the result
#   sort_words_by_distinct_letters(words) → each argument is evaluated first, then the function body runs
#   sort_words_by_distinct_letters(?) runs first → then print() outputs the result
#   sort_words_by_distinct_letters(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_words_by_distinct_letters(words) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. print(sort_words_by_distinct_letters(...))
#      → calls sort_words_by_distinct_letters(words) with words=words
#      → 3.1 return len(set(word))
#   4. def sort_words_by_distinct_letters(words) → stored in memory, not executed
#   5. def sort_words_by_distinct_letters(words) → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_words_by_distinct_letters(...))
#      → calls sort_words_by_distinct_letters(words) with words=[4, 1, 8, 6, 3]
#      → 9.1 return len(set(word))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_words_by_distinct_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     print(sort_words_by_distinct_letters(...)) → sort_words_by_distinct_letters() runs first → value passed to print()
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_words_by_distinct_letters(...)) → sort_words_by_distinct_letters() runs first → value passed to print()

"""

"""
Problem 489:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_second_letter(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_second_letter(lst) runs first → then print() outputs the result
#   sort_second_letter(lst) → each argument is evaluated first, then the function body runs
#   sort_second_letter(?) runs first → then print() outputs the result
#   sort_second_letter(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_second_letter(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_second_letter(...))
#      → calls sort_second_letter(lst) with lst=lst
#      → 3.1 return x[1]
#   4. def sort_second_letter(lst) → stored in memory, not executed
#   5. def sort_second_letter(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_second_letter(...))
#      → calls sort_second_letter(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return x[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_second_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_second_letter(...)) → sort_second_letter() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_second_letter(...)) → sort_second_letter() runs first → value passed to print()

"""

"""
Problem 490:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def people_age(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   people_age(people) runs first → then print() outputs the result
#   people_age(dic) → each argument is evaluated first, then the function body runs
#   people_age(?) runs first → then print() outputs the result
#   people_age(people) → each argument is evaluated first, then the function body runs
#   people_age(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def people_age(dic) → stored in memory, not executed
#   2. people = ... → assigns value to variable
#   3. print(people_age(...))
#      → calls people_age(dic) with dic=people
#      → 3.1 return person['age']
#   4. def people_age(dic) → stored in memory, not executed
#   5. def people_age(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(people_age(...))
#      → calls people_age(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return person['age']
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (people_age) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     print(people_age(...)) → people_age() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(people_age(...)) → people_age() runs first → value passed to print()

"""

"""
Problem 491:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_by_binary_representation(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: bin, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_binary_representation(lst) runs first → then print() outputs the result
#   sort_by_binary_representation(lst) → each argument is evaluated first, then the function body runs
#   sort_by_binary_representation(?) runs first → then print() outputs the result
#   sort_by_binary_representation(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_by_binary_representation(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_by_binary_representation(...))
#      → calls sort_by_binary_representation(lst) with lst=lst
#      → 3.1 return bin(x)
#   4. def sort_by_binary_representation(lst) → stored in memory, not executed
#   5. def sort_by_binary_representation(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_by_binary_representation(...))
#      → calls sort_by_binary_representation(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return bin(x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_binary_representation) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_by_binary_representation(...)) → sort_by_binary_representation() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_by_binary_representation(...)) → sort_by_binary_representation() runs first → value passed to print()

"""

"""
Problem 492:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_keys_by_value(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_keys_by_value(dic) runs first → then print() outputs the result
#   sort_keys_by_value(dic) → each argument is evaluated first, then the function body runs
#   sort_keys_by_value(?) runs first → then print() outputs the result
#   sort_keys_by_value(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_keys_by_value(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(sort_keys_by_value(...))
#      → calls sort_keys_by_value(dic) with dic=dic
#      → 3.1 return dic[key]
#   4. def sort_keys_by_value(dic) → stored in memory, not executed
#   5. def sort_keys_by_value(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_keys_by_value(...))
#      → calls sort_keys_by_value(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return dic[key]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_keys_by_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(sort_keys_by_value(...)) → sort_keys_by_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_keys_by_value(...)) → sort_keys_by_value() runs first → value passed to print()

"""

"""
Problem 493:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_fractions_by_decimal(fractions) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Fraction, float, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_fractions_by_decimal(fractions) runs first → then print() outputs the result
#   sort_fractions_by_decimal(fractions) → each argument is evaluated first, then the function body runs
#   sort_fractions_by_decimal(?) runs first → then print() outputs the result
#   sort_fractions_by_decimal(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from fractions import Fraction
#   2. def sort_fractions_by_decimal(fractions) → stored in memory, not executed
#   3. fractions = ... → assigns value to variable
#   4. print(sort_fractions_by_decimal(...))
#      → calls sort_fractions_by_decimal(fractions) with fractions=fractions
#      → 4.1 return float(fraction)
#   5. def sort_fractions_by_decimal(fractions) → stored in memory, not executed
#   6. def sort_fractions_by_decimal(fractions) → stored in memory, not executed
#   7. fractions = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_fractions_by_decimal(...))
#      → calls sort_fractions_by_decimal(fractions) with fractions=[4, 1, 8, 6, 3]
#      → 10.1 return float(fraction)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_fractions_by_decimal) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     fractions = ... → stored in memory
#     print(sort_fractions_by_decimal(...)) → sort_fractions_by_decimal() runs first → value passed to print()
#     fractions = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_fractions_by_decimal(...)) → sort_fractions_by_decimal() runs first → value passed to print()

"""

"""
Problem 494:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def longest_string_letter(lst, letter) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, letter_ends, max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   longest_string_letter(lst, 'n') runs first → then print() outputs the result
#   longest_string_letter(lst, 'a') runs first → then print() outputs the result
#   longest_string(input_string, letter) runs first → then print() outputs the result
#   longest_string_letter(lst, letter) → each argument is evaluated first, then the function body runs
#   longest_string_letter(?, ?) runs first → then print() outputs the result
#   longest_string_letter(lst, 'n') → each argument is evaluated first, then the function body runs
#   longest_string_letter(lst, 'a') → each argument is evaluated first, then the function body runs
#   longest_string(input_string, letter) → each argument is evaluated first, then the function body runs
#   longest_string_letter(?, ?) → each argument is evaluated first, then the function body runs
#   length > max_length → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def longest_string_letter(lst, letter) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(longest_string_letter(...))
#      → calls longest_string_letter(lst, letter) with lst=lst, letter='n'
#      → 3.1 return len(s) if s.endswith(letter) else -1
#   4. print(longest_string_letter(...))
#      → calls longest_string_letter(lst, letter) with lst=lst, letter='a'
#      → 4.1 return len(s) if s.endswith(letter) else -1
#   5. def longest_string(string, letter) → stored in memory, not executed
#   6. input_string = ... → assigns value to variable
#   7. letter = ... → assigns value to variable
#   8. print(longest_string(...))
#      → calls longest_string(string, letter) with string=input_string, letter=letter
#      → 8.1 words = string.split()
#      → 8.2 def letter_ends(word):
#      → 8.2     if word.endswith(letter):
#      → 8.2         return len(word)
#      → 8.2     else:
#      → 8.2         return 0
#      → 8.3 longest = None
#      → 8.4 max_length = 0
#      → 8.5 for word in words:
#      → 8.5     length = letter_ends(word)
#      → 8.5     if length > max_length:
#      → 8.5         max_length = length
#      → 8.5         longest = word
#      → 8.6 return longest
#   9. def longest_string_letter(lst, letter) → stored in memory, not executed
#   10. def longest_string_letter(lst, letter) → stored in memory, not executed
#   11. lst = ... → assigns value to variable
#   12. letter = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(longest_string_letter(...))
#      → calls longest_string_letter(lst, letter) with lst=[1, 3, 5, 7], letter=[4, 1, 8, 6, 3]
#      → 15.1 return len(s) if s.endswith(letter) else -1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (longest_string, longest_string_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(longest_string_letter(...)) → longest_string_letter() runs first → value passed to print()
#     print(longest_string_letter(...)) → longest_string_letter() runs first → value passed to print()
#     input_string = ... → stored in memory
#     letter = ... → stored in memory
#     print(longest_string(...)) → longest_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     letter = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(longest_string_letter(...)) → longest_string_letter() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 495:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Animal → defines a new type
#     def speak(...) → method of Animal
#   class Dog(Animal) → inherits from parent class
#     def speak(...) → method of Dog
#   class Cat(Animal) → inherits from parent class
#     def speak(...) → method of Cat
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Animal, Cat, Dog()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Animal → class definition stored
#   2. class Dog(Animal) → class definition stored
#   3. class Cat(Animal) → class definition stored
#   4. dog = ... → assigns value to variable
#   5. cat = ... → assigns value to variable
#   6. print(dog.speak(...))
#   7. print(cat.speak(...))
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. obj = ... → assigns value to variable
#   11. print(obj)
#   12. print(Animal(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Dog, Cat, Animal) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dog = ... → stored in memory
#     cat = ... → stored in memory
#     print(dog.speak(...))
#     print(cat.speak(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Animal(...))

"""

"""
Problem 496:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Vehicle → defines a new type
#     def __init__(...) → method of Vehicle
#   class Car(Vehicle) → inherits from parent class
#     def __init__(...) → method of Car
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Car, Vehicle, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Vehicle → class definition stored
#   2. class Car(Vehicle) → class definition stored
#   3. my_car = ... → assigns value to variable
#   4. print(?)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. obj = ... → assigns value to variable
#   8. print(obj)
#   9. print(Vehicle(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Car, Vehicle) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_car = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Vehicle(...))

"""

"""
Problem 497:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#     def __init__(...) → method of Person
#     def introduce(...) → method of Person
#   class Student(Person) → inherits from parent class
#     def __init__(...) → method of Student
#     def introduce(...) → method of Student
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Person, Student, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Person → class definition stored
#   2. class Student(Person) → class definition stored
#   3. person = ... → assigns value to variable
#   4. print(person.introduce(...))
#   5. student = ... → assigns value to variable
#   6. print(student.introduce(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person, Student) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     print(person.introduce(...))
#     student = ... → stored in memory
#     print(student.introduce(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))

"""

"""
Problem 498:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Shape → defines a new type
#     def area(...) → method of Shape
#   class Rectangle(Shape) → inherits from parent class
#     def __init__(...) → method of Rectangle
#     def area(...) → method of Rectangle
#
# Operators: *
# Built-in functions: Rectangle, Shape()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   self.length * self.width → Python looks up self.length, then self.width, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Shape → class definition stored
#   2. class Rectangle(Shape) → class definition stored
#   3. rectangle = ... → assigns value to variable
#   4. print(?)
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(Shape(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Shape, Rectangle) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     rectangle = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Shape(...))

"""

"""
Problem 499:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Appliance → defines a new type
#     def __init__(...) → method of Appliance
#   class WashingMachine(Appliance) → inherits from parent class
#     def __init__(...) → method of WashingMachine
#     def wash_clothes(...) → method of WashingMachine
#
# Built-in functions: Appliance, WashingMachine, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Appliance → class definition stored
#   2. class WashingMachine(Appliance) → class definition stored
#   3. washing_machine = ... → assigns value to variable
#   4. print(washing_machine.wash_clothes(...))
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(Appliance(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Appliance, WashingMachine) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     washing_machine = ... → stored in memory
#     print(washing_machine.wash_clothes(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Appliance(...))

"""

"""
Problem 500:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Employee → defines a new type
#     def __init__(...) → method of Employee
#   class Manager(Employee) → inherits from parent class
#     def __init__(...) → method of Manager
#     def assign_task(...) → method of Manager
#
# Built-in functions: Employee, Manager, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Employee → class definition stored
#   2. class Manager(Employee) → class definition stored
#   3. manager = ... → assigns value to variable
#   4. print(manager.assign_task(...))
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(Employee(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Manager, Employee) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     manager = ... → stored in memory
#     print(manager.assign_task(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Employee(...))

"""

