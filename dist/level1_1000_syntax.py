# Syntax documentation for level 1 (problems 1-1000)
# Auto-generated by gen_syntax_files.py

"""
Problem 501:
#
# ===== SYNTAX =====
#   class Bird — defines a new type
#     def fly — method of Bird
#   class Penguin(Bird) — inherits from parent
#     def fly — method of Penguin
#   def solve()
#   return — exits the function and sends back a value
#   Bird() — built-in function call
#   Penguin() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(generic_bird.fly()): inner expression resolves first, then print() outputs
#   print(penguin.fly()): inner expression resolves first, then print() outputs
#   print(Bird()): inner expression resolves first, then print() outputs
#   print(generic_bird.fly()): inner expression resolves first, then print() outputs
#   print(penguin.fly()): inner expression resolves first, then print() outputs
#   print(generic_bird_2.fly()): inner expression resolves first, then print() outputs
#   print(penguin_2.fly()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Bird
#   2. Define class Penguin(Bird)
#   3. Assign to generic_bird
#   4. Run print(generic_bird.fly(...))
#   5. Assign to penguin
#   6. Run print(penguin.fly(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Bird(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      generic_bird = ... (stored in memory)
#      print(generic_bird.fly(...))
#      penguin = ... (stored in memory)
#      print(penguin.fly(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Bird(...))

"""

"""
Problem 502:
#
# ===== SYNTAX =====
#   class Book — defines a new type
#     def __init__ — method of Book
#   class EBook(Book) — inherits from parent
#     def __init__ — method of EBook
#   class Computer — defines a new type
#   def solve()
#   EBook() — built-in function call
#   super() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Book(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Book
#   2. Define class EBook(Book)
#   3. Assign to ebook
#   4. Run print(?)
#   5. Import dataclass from dataclasses
#   6. Define class Computer
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Book(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   Book.__init__ runs automatically when you create an instance
#   EBook.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ebook = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Book(...))

"""

"""
Problem 503:
#
# ===== SYNTAX =====
#   class Instrument — defines a new type
#     def play — method of Instrument
#   class Guitar(Instrument) — inherits from parent
#     def play — method of Guitar
#   def solve()
#   return — exits the function and sends back a value
#   Instrument() — built-in function call
#   Guitar() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(generic_instrument.play()): inner expression resolves first, then print() outputs
#   print(guitar.play()): inner expression resolves first, then print() outputs
#   print(Instrument()): inner expression resolves first, then print() outputs
#   print(generic_instrument.play()): inner expression resolves first, then print() outputs
#   print(guitar.play()): inner expression resolves first, then print() outputs
#   print(generic_instrument_2.play()): inner expression resolves first, then print() outputs
#   print(guitar_2.play()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Instrument
#   2. Define class Guitar(Instrument)
#   3. Assign to generic_instrument
#   4. Run print(generic_instrument.play(...))
#   5. Assign to guitar
#   6. Run print(guitar.play(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Instrument(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      generic_instrument = ... (stored in memory)
#      print(generic_instrument.play(...))
#      guitar = ... (stored in memory)
#      print(guitar.play(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Instrument(...))

"""

"""
Problem 504:
#
# ===== SYNTAX =====
#   class Game — defines a new type
#     def start — method of Game
#   class Chess(Game) — inherits from parent
#     def start — method of Chess
#   def solve()
#   return — exits the function and sends back a value
#   Game() — built-in function call
#   Chess() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(generic_game.start()): inner expression resolves first, then print() outputs
#   print(chess_game.start()): inner expression resolves first, then print() outputs
#   print(Game()): inner expression resolves first, then print() outputs
#   print(generic_game.start()): inner expression resolves first, then print() outputs
#   print(chess_game.start()): inner expression resolves first, then print() outputs
#   print(generic_game_2.start()): inner expression resolves first, then print() outputs
#   print(chess_game_2.start()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Game
#   2. Define class Chess(Game)
#   3. Assign to generic_game
#   4. Run print(generic_game.start(...))
#   5. Assign to chess_game
#   6. Run print(chess_game.start(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Game(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      generic_game = ... (stored in memory)
#      print(generic_game.start(...))
#      chess_game = ... (stored in memory)
#      print(chess_game.start(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Game(...))

"""

"""
Problem 505:
#
# ===== SYNTAX =====
#   class Vehicle — defines a new type
#     def drive — method of Vehicle
#   class Bicycle(Vehicle) — inherits from parent
#     def drive — method of Bicycle
#
# ===== EVALUATION ORDER =====
#   print(bicycle.drive()): inner expression resolves first, then print() outputs
#   print(Vehicle()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Vehicle
#   2. Define class Bicycle(Vehicle)
#   3. Assign to bicycle
#   4. Run print(bicycle.drive(...))
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(Vehicle(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Execution starts at the non-def lines below:
#      bicycle = ... (stored in memory)
#      print(bicycle.drive(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Vehicle(...))

"""

"""
Problem 506:
#
# ===== SYNTAX =====
#   class Computer — defines a new type
#     def __init__ — method of Computer
#   class Laptop(Computer) — inherits from parent
#     def __init__ — method of Laptop
#   class Computer — defines a new type
#   def solve()
#   Laptop() — built-in function call
#   super() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Computer(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Computer
#   2. Define class Laptop(Computer)
#   3. Assign to laptop
#   4. Run print(?)
#   5. Import dataclass from dataclasses
#   6. Define class Computer
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Computer(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   Computer.__init__ runs automatically when you create an instance
#   Laptop.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      laptop = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Computer(...))

"""

"""
Problem 507:
#
# ===== SYNTAX =====
#   class Device — defines a new type
#     def turn_on — method of Device
#   class Smartphone(Device) — inherits from parent
#     def __init__ — method of Smartphone
#   def solve()
#   return — exits the function and sends back a value
#   Smartphone() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(phone.turn_on()): inner expression resolves first, then print() outputs
#   print(Device(25)): inner expression resolves first, then print() outputs
#   print(phone.turn_on()): inner expression resolves first, then print() outputs
#   print(phone_2.turn_on()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Device
#   2. Define class Smartphone(Device)
#   3. Assign to phone
#   4. Run print(phone.turn_on(...))
#   5. Run print(?)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to obj
#   9. Run print(obj)
#   10. Run print(Device(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Smartphone.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      phone = ... (stored in memory)
#      print(phone.turn_on(...))
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Device(...))

"""

"""
Problem 508:
#
# ===== SYNTAX =====
#   class Parent — defines a new type
#     def display — method of Parent
#   class Child(Parent) — inherits from parent
#     def display — method of Child
#   def solve()
#   return — exits the function and sends back a value
#   Parent() — built-in function call
#   Child() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(parent.display()): inner expression resolves first, then print() outputs
#   print(child.display()): inner expression resolves first, then print() outputs
#   print(Parent()): inner expression resolves first, then print() outputs
#   print(parent.display()): inner expression resolves first, then print() outputs
#   print(child.display()): inner expression resolves first, then print() outputs
#   print(parent_2.display()): inner expression resolves first, then print() outputs
#   print(child_2.display()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Parent
#   2. Define class Child(Parent)
#   3. Assign to parent
#   4. Run print(parent.display(...))
#   5. Assign to child
#   6. Run print(child.display(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Parent(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      parent = ... (stored in memory)
#      print(parent.display(...))
#      child = ... (stored in memory)
#      print(child.display(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Parent(...))

"""

"""
Problem 509:
#
# ===== SYNTAX =====
#   class Building — defines a new type
#     def __init__ — method of Building
#   class Skyscraper(Building) — inherits from parent
#     def __init__ — method of Skyscraper
#   class Computer — defines a new type
#   def solve()
#   Skyscraper() — built-in function call
#   super() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Building(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Building
#   2. Define class Skyscraper(Building)
#   3. Assign to skyscraper
#   4. Run print(?)
#   5. Import dataclass from dataclasses
#   6. Define class Computer
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Building(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Class definitions are processed (methods stored)
#   Building.__init__ runs automatically when you create an instance
#   Skyscraper.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      skyscraper = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Building(...))

"""

"""
Problem 510:
#
# ===== SYNTAX =====
#   class Plant — defines a new type
#     def grow — method of Plant
#   class Flower(Plant) — inherits from parent
#     def grow — method of Flower
#   def solve()
#   return — exits the function and sends back a value
#   Plant() — built-in function call
#   Flower() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(plant.grow()): inner expression resolves first, then print() outputs
#   print(flower.grow()): inner expression resolves first, then print() outputs
#   print(Plant()): inner expression resolves first, then print() outputs
#   print(plant.grow()): inner expression resolves first, then print() outputs
#   print(flower.grow()): inner expression resolves first, then print() outputs
#   print(plant_2.grow()): inner expression resolves first, then print() outputs
#   print(flower_2.grow()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Plant
#   2. Define class Flower(Plant)
#   3. Assign to plant
#   4. Run print(plant.grow(...))
#   5. Assign to flower
#   6. Run print(flower.grow(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Plant(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      plant = ... (stored in memory)
#      print(plant.grow(...))
#      flower = ... (stored in memory)
#      print(flower.grow(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Plant(...))

"""

"""
Problem 511:
#
# ===== SYNTAX =====
#   class Teacher — defines a new type
#     def __init__ — method of Teacher
#     def __str__ — method of Teacher
#   class MathTeacher(Teacher) — inherits from parent
#     def __init__ — method of MathTeacher
#     def teach_math — method of MathTeacher
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(math_teacher.teach_math()): inner expression resolves first, then print() outputs
#   print(Teacher('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Teacher
#   2. Define class MathTeacher(Teacher)
#   3. Assign to teacher
#   4. Run print(teacher)
#   5. Assign to math_teacher
#   6. Run print(math_teacher)
#   7. Run print(math_teacher.teach_math(...))
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to obj
#   11. Run print(obj)
#   12. Run print(Teacher(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Teacher.__init__ runs automatically when you create an instance
#   MathTeacher.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      teacher = ... (stored in memory)
#      print(teacher)
#      math_teacher = ... (stored in memory)
#      print(math_teacher)
#      print(math_teacher.teach_math(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Teacher(...))

"""

"""
Problem 512:
#
# ===== SYNTAX =====
#   class Writer — defines a new type
#     def __init__ — method of Writer
#     def __str__ — method of Writer
#   class Poet(Writer) — inherits from parent
#     def __init__ — method of Poet
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Writer('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Writer
#   2. Define class Poet(Writer)
#   3. Assign to writer
#   4. Run print(writer)
#   5. Assign to poet
#   6. Run print(poet)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Writer(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Writer.__init__ runs automatically when you create an instance
#   Poet.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      writer = ... (stored in memory)
#      print(writer)
#      poet = ... (stored in memory)
#      print(poet)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Writer(...))

"""

"""
Problem 513:
#
# ===== SYNTAX =====
#   class Appliance — defines a new type
#     def use — method of Appliance
#   class Oven(Appliance) — inherits from parent
#     def use — method of Oven
#   def solve()
#   return — exits the function and sends back a value
#   Appliance() — built-in function call
#   Oven() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(appliance.use()): inner expression resolves first, then print() outputs
#   print(oven.use()): inner expression resolves first, then print() outputs
#   print(Appliance()): inner expression resolves first, then print() outputs
#   print(appliance.use()): inner expression resolves first, then print() outputs
#   print(oven.use()): inner expression resolves first, then print() outputs
#   print(appliance_2.use()): inner expression resolves first, then print() outputs
#   print(oven_2.use()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Appliance
#   2. Define class Oven(Appliance)
#   3. Assign to appliance
#   4. Run print(appliance.use(...))
#   5. Assign to oven
#   6. Run print(oven.use(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Appliance(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      appliance = ... (stored in memory)
#      print(appliance.use(...))
#      oven = ... (stored in memory)
#      print(oven.use(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Appliance(...))

"""

"""
Problem 514:
#
# ===== SYNTAX =====
#   class Artist — defines a new type
#     def __init__ — method of Artist
#     def __str__ — method of Artist
#   class Painter(Artist) — inherits from parent
#     def paint — method of Painter
#   def solve()
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(painter.paint()): inner expression resolves first, then print() outputs
#   print(Artist('Bob', 25)): inner expression resolves first, then print() outputs
#   'Painting with ' + self.medium: first get 'Painting with ', then get self.medium, then compute 'Painting with ' + self.medium
#   'Painting with ' + self.medium: first get 'Painting with ', then get self.medium, then compute 'Painting with ' + self.medium
#   'Painting with ' + self.medium: first get 'Painting with ', then get self.medium, then compute 'Painting with ' + self.medium
#
# ===== EXECUTION ORDER =====
#   1. Define class Artist
#   2. Define class Painter(Artist)
#   3. Assign to artist
#   4. Run print(artist)
#   5. Assign to painter
#   6. Run print(painter)
#   7. Run print(painter.paint(...))
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to obj
#   11. Run print(obj)
#   12. Run print(Artist(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Artist.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      artist = ... (stored in memory)
#      print(artist)
#      painter = ... (stored in memory)
#      print(painter)
#      print(painter.paint(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Artist(...))

"""

"""
Problem 515:
#
# ===== SYNTAX =====
#   class Worker — defines a new type
#     def work — method of Worker
#   class Electrician(Worker) — inherits from parent
#     def work — method of Electrician
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(worker.work()): inner expression resolves first, then print() outputs
#   print(electrician.work()): inner expression resolves first, then print() outputs
#   print(Worker()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Worker
#   2. Define class Electrician(Worker)
#   3. Assign to worker
#   4. Run print(worker.work(...))
#   5. Assign to electrician
#   6. Run print(electrician.work(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Worker(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      worker = ... (stored in memory)
#      print(worker.work(...))
#      electrician = ... (stored in memory)
#      print(electrician.work(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Worker(...))

"""

"""
Problem 516:
#
# ===== SYNTAX =====
#   class Animal — defines a new type
#     def eat — method of Animal
#   class Lion(Animal) — inherits from parent
#     def eat — method of Lion
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(animal.eat()): inner expression resolves first, then print() outputs
#   print(lion.eat()): inner expression resolves first, then print() outputs
#   print(Animal()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Animal
#   2. Define class Lion(Animal)
#   3. Assign to animal
#   4. Run print(animal.eat(...))
#   5. Assign to lion
#   6. Run print(lion.eat(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Animal(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      animal = ... (stored in memory)
#      print(animal.eat(...))
#      lion = ... (stored in memory)
#      print(lion.eat(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Animal(...))

"""

"""
Problem 517:
#
# ===== SYNTAX =====
#   class Appliance — defines a new type
#     def run — method of Appliance
#   class Fan(Appliance) — inherits from parent
#     def run — method of Fan
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(appliance.run()): inner expression resolves first, then print() outputs
#   print(fan.run()): inner expression resolves first, then print() outputs
#   print(Appliance()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Appliance
#   2. Define class Fan(Appliance)
#   3. Assign to appliance
#   4. Run print(appliance.run(...))
#   5. Assign to fan
#   6. Run print(fan.run(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Appliance(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      appliance = ... (stored in memory)
#      print(appliance.run(...))
#      fan = ... (stored in memory)
#      print(fan.run(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Appliance(...))

"""

"""
Problem 518:
#
# ===== SYNTAX =====
#   class Employee — defines a new type
#     def get_salary — method of Employee
#   class Intern(Employee) — inherits from parent
#     def get_salary — method of Intern
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Employee()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Employee
#   2. Define class Intern(Employee)
#   3. Assign to employee
#   4. Run print(?)
#   5. Assign to intern
#   6. Run print(?)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Employee(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      employee = ... (stored in memory)
#      print(?)
#      intern = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Employee(...))

"""

"""
Problem 519:
#
# ===== SYNTAX =====
#   class Vehicle — defines a new type
#     def __init__ — method of Vehicle
#     def __str__ — method of Vehicle
#   class Motorcycle(Vehicle) — inherits from parent
#     def __init__ — method of Motorcycle
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Vehicle(25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Vehicle
#   2. Define class Motorcycle(Vehicle)
#   3. Assign to vehicle
#   4. Run print(vehicle)
#   5. Assign to motorcycle
#   6. Run print(motorcycle)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Vehicle(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Vehicle.__init__ runs automatically when you create an instance
#   Motorcycle.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      vehicle = ... (stored in memory)
#      print(vehicle)
#      motorcycle = ... (stored in memory)
#      print(motorcycle)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Vehicle(...))

"""

"""
Problem 520:
#
# ===== SYNTAX =====
#   class Tool — defines a new type
#     def use — method of Tool
#   class Hammer(Tool) — inherits from parent
#     def use — method of Hammer
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(tool.use()): inner expression resolves first, then print() outputs
#   print(hammer.use()): inner expression resolves first, then print() outputs
#   print(Tool()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Tool
#   2. Define class Hammer(Tool)
#   3. Assign to tool
#   4. Run print(tool.use(...))
#   5. Assign to hammer
#   6. Run print(hammer.use(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Tool(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      tool = ... (stored in memory)
#      print(tool.use(...))
#      hammer = ... (stored in memory)
#      print(hammer.use(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Tool(...))

"""

"""
Problem 521:
#
# ===== SYNTAX =====
#   class BankAccount — defines a new type
#     def __init__ — method of BankAccount
#     def __str__ — method of BankAccount
#   class SavingsAccount(BankAccount) — inherits from parent
#     def __init__ — method of SavingsAccount
#     def __str__ — method of SavingsAccount
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(BankAccount(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class BankAccount
#   2. Define class SavingsAccount(BankAccount)
#   3. Assign to account
#   4. Run print(account)
#   5. Assign to savings
#   6. Run print(savings)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(BankAccount(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   BankAccount.__init__ runs automatically when you create an instance
#   SavingsAccount.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      account = ... (stored in memory)
#      print(account)
#      savings = ... (stored in memory)
#      print(savings)
#      obj = ... (stored in memory)
#      print(obj)
#      print(BankAccount(...))

"""

"""
Problem 522:
#
# ===== SYNTAX =====
#   class Weapon — defines a new type
#     def attack — method of Weapon
#   class Sword(Weapon) — inherits from parent
#     def attack — method of Sword
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(weapon.attack()): inner expression resolves first, then print() outputs
#   print(sword.attack()): inner expression resolves first, then print() outputs
#   print(Weapon()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Weapon
#   2. Define class Sword(Weapon)
#   3. Assign to weapon
#   4. Run print(weapon.attack(...))
#   5. Assign to sword
#   6. Run print(sword.attack(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Weapon(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      weapon = ... (stored in memory)
#      print(weapon.attack(...))
#      sword = ... (stored in memory)
#      print(sword.attack(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Weapon(...))

"""

"""
Problem 523:
#
# ===== SYNTAX =====
#   class Appliance — defines a new type
#     def __init__ — method of Appliance
#     def __str__ — method of Appliance
#   class Microwave(Appliance) — inherits from parent
#     def __init__ — method of Microwave
#     def heat_food — method of Microwave
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(microwave.heat_food()): inner expression resolves first, then print() outputs
#   print(Appliance(25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Appliance
#   2. Define class Microwave(Appliance)
#   3. Assign to appliance
#   4. Run print(appliance)
#   5. Assign to microwave
#   6. Run print(microwave)
#   7. Run print(microwave.heat_food(...))
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to obj
#   11. Run print(obj)
#   12. Run print(Appliance(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Appliance.__init__ runs automatically when you create an instance
#   Microwave.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      appliance = ... (stored in memory)
#      print(appliance)
#      microwave = ... (stored in memory)
#      print(microwave)
#      print(microwave.heat_food(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Appliance(...))

"""

"""
Problem 524:
#
# ===== SYNTAX =====
#   class Vehicle — defines a new type
#     def start — method of Vehicle
#   class ElectricCar(Vehicle) — inherits from parent
#     def start — method of ElectricCar
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(vehicle.start()): inner expression resolves first, then print() outputs
#   print(electric_car.start()): inner expression resolves first, then print() outputs
#   print(Vehicle()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Vehicle
#   2. Define class ElectricCar(Vehicle)
#   3. Assign to vehicle
#   4. Run print(vehicle.start(...))
#   5. Assign to electric_car
#   6. Run print(electric_car.start(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Vehicle(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      vehicle = ... (stored in memory)
#      print(vehicle.start(...))
#      electric_car = ... (stored in memory)
#      print(electric_car.start(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Vehicle(...))

"""

"""
Problem 525:
#
# ===== SYNTAX =====
#   class Pet — defines a new type
#     def make_sound — method of Pet
#   class Cat(Pet) — inherits from parent
#     def make_sound — method of Cat
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(pet.make_sound()): inner expression resolves first, then print() outputs
#   print(cat.make_sound()): inner expression resolves first, then print() outputs
#   print(Pet()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Pet
#   2. Define class Cat(Pet)
#   3. Assign to pet
#   4. Run print(pet.make_sound(...))
#   5. Assign to cat
#   6. Run print(cat.make_sound(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Pet(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      pet = ... (stored in memory)
#      print(pet.make_sound(...))
#      cat = ... (stored in memory)
#      print(cat.make_sound(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Pet(...))

"""

"""
Problem 526:
#
# ===== SYNTAX =====
#   class Chef — defines a new type
#     def cook — method of Chef
#   class PastryChef(Chef) — inherits from parent
#     def cook — method of PastryChef
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(chef.cook()): inner expression resolves first, then print() outputs
#   print(pastry_chef.cook()): inner expression resolves first, then print() outputs
#   print(Chef()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Chef
#   2. Define class PastryChef(Chef)
#   3. Assign to chef
#   4. Run print(chef.cook(...))
#   5. Assign to pastry_chef
#   6. Run print(pastry_chef.cook(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Chef(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      chef = ... (stored in memory)
#      print(chef.cook(...))
#      pastry_chef = ... (stored in memory)
#      print(pastry_chef.cook(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Chef(...))

"""

"""
Problem 527:
#
# ===== SYNTAX =====
#   def create_chair(material, color, legs)
#   return — exits the function and sends back a value
#   Chair() — built-in function call
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_chair('wood', 'brown', 4): gets the literal 'wood', gets the literal 'brown', gets the literal 4, then runs the function body
#   create_chair(material, color, legs): looks up variable 'material', looks up variable 'color', looks up variable 'legs', then runs the function body
#   print(create_chair(25, 25, 25)): the inner create_chair() executes FIRST, then print() outputs the result
#   create_chair(25, 25, 25): gets the literal 25, gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_chair(material, color, legs) — stored for later
#   2. Assign to my_chair
#   3. Define def create_chair(material, color, legs) — stored for later
#   4. Define def create_chair(material, color, legs) — stored for later
#   5. Assign to material
#   6. Assign to color
#   7. Assign to legs
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(create_chair(...))
#       calls create_chair with: material=25, color=25, legs=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_chair, create_chair, create_chair are just stored
#   Execution starts at the non-def lines below:
#      my_chair = ... (stored in memory)
#      material = ... (stored in memory)
#      color = ... (stored in memory)
#      legs = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_chair(25, 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 528:
#
# ===== SYNTAX =====
#   class Game — defines a new type
#     def __init__ — method of Game
#     def __str__ — method of Game
#   class Soccer(Game) — inherits from parent
#     def __init__ — method of Soccer
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Game(25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Game
#   2. Define class Soccer(Game)
#   3. Assign to game
#   4. Run print(game)
#   5. Assign to soccer
#   6. Run print(soccer)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Game(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Game.__init__ runs automatically when you create an instance
#   Soccer.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      game = ... (stored in memory)
#      print(game)
#      soccer = ... (stored in memory)
#      print(soccer)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Game(...))

"""

"""
Problem 529:
#
# ===== SYNTAX =====
#   class Musician — defines a new type
#     def play_instrument — method of Musician
#   class Pianist(Musician) — inherits from parent
#     def play_instrument — method of Pianist
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(musician.play_instrument()): inner expression resolves first, then print() outputs
#   print(pianist.play_instrument()): inner expression resolves first, then print() outputs
#   print(Musician()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Musician
#   2. Define class Pianist(Musician)
#   3. Assign to musician
#   4. Run print(musician.play_instrument(...))
#   5. Assign to pianist
#   6. Run print(pianist.play_instrument(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Musician(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      musician = ... (stored in memory)
#      print(musician.play_instrument(...))
#      pianist = ... (stored in memory)
#      print(pianist.play_instrument(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Musician(...))

"""

"""
Problem 530:
#
# ===== SYNTAX =====
#   class Company — defines a new type
#     def __init__ — method of Company
#     def __str__ — method of Company
#   class TechCompany(Company) — inherits from parent
#     def __init__ — method of TechCompany
#     def __str__ — method of TechCompany
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Company('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Company
#   2. Define class TechCompany(Company)
#   3. Assign to company
#   4. Run print(company)
#   5. Assign to tech_company
#   6. Run print(tech_company)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Company(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Company.__init__ runs automatically when you create an instance
#   TechCompany.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      company = ... (stored in memory)
#      print(company)
#      tech_company = ... (stored in memory)
#      print(tech_company)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Company(...))

"""

"""
Problem 531:
#
# ===== SYNTAX =====
#   class Gadget — defines a new type
#     def operate — method of Gadget
#   class Smartwatch(Gadget) — inherits from parent
#     def __init__ — method of Smartwatch
#     def __str__ — method of Smartwatch
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(gadget.operate()): inner expression resolves first, then print() outputs
#   print(smartwatch.operate()): inner expression resolves first, then print() outputs
#   print(Gadget(25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Gadget
#   2. Define class Smartwatch(Gadget)
#   3. Assign to gadget
#   4. Run print(gadget.operate(...))
#   5. Assign to smartwatch
#   6. Run print(smartwatch.operate(...))
#   7. Run print(smartwatch)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to obj
#   11. Run print(obj)
#   12. Run print(Gadget(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Smartwatch.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      gadget = ... (stored in memory)
#      print(gadget.operate(...))
#      smartwatch = ... (stored in memory)
#      print(smartwatch.operate(...))
#      print(smartwatch)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Gadget(...))

"""

"""
Problem 532:
#
# ===== SYNTAX =====
#   class Trainer — defines a new type
#     def train — method of Trainer
#   class FitnessTrainer(Trainer) — inherits from parent
#     def train — method of FitnessTrainer
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(trainer.train()): inner expression resolves first, then print() outputs
#   print(Trainer()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Trainer
#   2. Define class FitnessTrainer(Trainer)
#   3. Assign to trainer
#   4. Run print(trainer.train(...))
#   5. Assign to fitness_trainer
#   6. Run fitness_trainer.train()
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Trainer(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      trainer = ... (stored in memory)
#      print(trainer.train(...))
#      fitness_trainer = ... (stored in memory)
#      fitness_trainer.train()
#      obj = ... (stored in memory)
#      print(obj)
#      print(Trainer(...))

"""

"""
Problem 533:
#
# ===== SYNTAX =====
#   class Book — defines a new type
#     def read — method of Book
#   class ComicBook(Book) — inherits from parent
#     def read — method of ComicBook
#   def solve()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(book.read()): inner expression resolves first, then print() outputs
#   print(Book()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Book
#   2. Define class ComicBook(Book)
#   3. Assign to book
#   4. Run print(book.read(...))
#   5. Assign to comic_book
#   6. Run comic_book.read()
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Book(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      book = ... (stored in memory)
#      print(book.read(...))
#      comic_book = ... (stored in memory)
#      comic_book.read()
#      obj = ... (stored in memory)
#      print(obj)
#      print(Book(...))

"""

"""
Problem 534:
#
# ===== SYNTAX =====
#   class Software — defines a new type
#     def __init__ — method of Software
#     def __str__ — method of Software
#   class GameSoftware(Software) — inherits from parent
#     def __init__ — method of GameSoftware
#     def __str__ — method of GameSoftware
#   def solve()
#   return — exits the function and sends back a value
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(Software('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Software
#   2. Define class GameSoftware(Software)
#   3. Assign to software
#   4. Run print(software)
#   5. Assign to game_software
#   6. Run print(game_software)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Software(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Software.__init__ runs automatically when you create an instance
#   GameSoftware.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      software = ... (stored in memory)
#      print(software)
#      game_software = ... (stored in memory)
#      print(game_software)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Software(...))

"""

"""
Problem 535:
#
# ===== SYNTAX =====
#   def count_letter(s)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_letter(string)): the inner count_letter() executes FIRST, then print() outputs the result
#   count_letter(s): looks up variable 's', then runs the function body
#   print(count_letter(10)): the inner count_letter() executes FIRST, then print() outputs the result
#   count_letter(string): looks up variable 'string', then runs the function body
#   count_letter(10): gets the literal 10, then runs the function body
#   c == 'a': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_letter(s) — stored for later
#   2. Assign to string
#   3. Run print(count_letter(...))
#       calls count_letter with: s=string
#         3.1 Assign to count
#         3.2 For loop over s
#         3.3 Return from function
#   4. Define def count_letter(s) — stored for later
#   5. Define def count_letter(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_letter(...))
#       calls count_letter with: s=10
#         9.1 Assign to count
#         9.2 For loop over s
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_letter, count_letter, count_letter are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_letter(string)) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_letter(10)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 536:
#
# ===== SYNTAX =====
#   def count_number(num)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_number(number)): the inner count_number() executes FIRST, then print() outputs the result
#   count_number(num): looks up variable 'num', then runs the function body
#   print(count_number(25)): the inner count_number() executes FIRST, then print() outputs the result
#   count_number(number): looks up variable 'number', then runs the function body
#   count_number(25): gets the literal 25, then runs the function body
#   c == 5: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_number(num) — stored for later
#   2. Assign to number
#   3. Run print(count_number(...))
#       calls count_number with: num=number
#         3.1 Assign to count
#         3.2 For loop over num
#         3.3 Return from function
#   4. Define def count_number(num) — stored for later
#   5. Define def count_number(num) — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_number(...))
#       calls count_number with: num=25
#         9.1 Assign to count
#         9.2 For loop over num
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_number, count_number, count_number are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(count_number(number)) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_number(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from num each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 537:
#
# ===== SYNTAX =====
#   def count_hello(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_hello(string)): the inner count_hello() executes FIRST, then print() outputs the result
#   count_hello(string): looks up variable 'string', then runs the function body
#   print(count_hello('world')): the inner count_hello() executes FIRST, then print() outputs the result
#   count_hello(string): looks up variable 'string', then runs the function body
#   count_hello('world'): gets the literal 'world', then runs the function body
#   c == 'hello': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_hello(string) — stored for later
#   2. Assign to string
#   3. Run print(count_hello(...))
#       calls count_hello with: string=string
#         3.1 Assign to count
#         3.2 For loop over string
#         3.3 Return from function
#   4. Define def count_hello(string) — stored for later
#   5. Define def count_hello(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_hello(...))
#       calls count_hello with: string='world'
#         9.1 Assign to count
#         9.2 For loop over string
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_hello, count_hello, count_hello are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_hello(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_hello('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 538:
#
# ===== SYNTAX =====
#   def count(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count(string)): the inner count() executes FIRST, then print() outputs the result
#   count(string): looks up variable 'string', then runs the function body
#   print(count('world')): the inner count() executes FIRST, then print() outputs the result
#   count(string): looks up variable 'string', then runs the function body
#   count('world'): gets the literal 'world', then runs the function body
#   c == '#': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count(string) — stored for later
#   2. Assign to string
#   3. Run print(count(...))
#       calls count with: string=string
#         3.1 Assign to count
#         3.2 For loop over string
#         3.3 Return from function
#   4. Define def count(string) — stored for later
#   5. Define def count(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count(...))
#       calls count with: string='world'
#         9.1 Assign to count
#         9.2 For loop over string
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count, count, count are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 540:
#
# ===== SYNTAX =====
#   def count_number(num)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   (no return — the function prints directly instead of returning)
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_number(num)): the inner count_number() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   count_number(num): looks up variable 'num', then runs the function body
#   print(count_number(25)): the inner count_number() executes FIRST, then print() outputs the result
#   count_number(num): looks up variable 'num', then runs the function body
#   count_number(25): gets the literal 25, then runs the function body
#   c == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_number(num) — stored for later
#   2. Assign to num
#   3. Run print(count_number(...))
#       calls count_number with: num=num
#         3.1 Assign to count
#         3.2 For loop over num
#         3.3 Return from function
#   4. Define def main(lst) — stored for later
#   5. Assign to lst
#   6. Run main(lst)
#       with: lst=lst
#         6.1 Assign to total
#         6.2 For loop over lst
#         6.3 Run print(total)
#   7. Define def count_number(num) — stored for later
#   8. Define def count_number(num) — stored for later
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(count_number(...))
#       calls count_number with: num=25
#         12.1 Assign to count
#         12.2 For loop over num
#         12.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_number, main, count_number, count_number are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      print(count_number(num)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_number(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 544:
#
# ===== SYNTAX =====
#   def count_letters(lst)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_letters(lst)): the inner count_letters() executes FIRST, then print() outputs the result
#   count_letters(lst): looks up variable 'lst', then runs the function body
#   print(count_letters(?)): the inner count_letters() executes FIRST, then print() outputs the result
#   count_letters(lst): looks up variable 'lst', then runs the function body
#   count_letters(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_letters(lst) — stored for later
#   2. Assign to lst
#   3. Run print(count_letters(...))
#       calls count_letters with: lst=lst
#         3.1 Return from function
#   4. Define def count_letters(lst) — stored for later
#   5. Define def count_letters(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_letters(...))
#       calls count_letters with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_letters, count_letters, count_letters are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(count_letters(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_letters(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 545:
#
# ===== SYNTAX =====
#   def count_elements_in_lst(lst, element)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_elements_in_lst(lst, element)): the inner count_elements_in_lst() executes FIRST, then print() outputs the result
#   count_elements_in_lst(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   print(count_elements_in_lst(?, 25)): the inner count_elements_in_lst() executes FIRST, then print() outputs the result
#   count_elements_in_lst(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   count_elements_in_lst(?, 25): resolves the value, gets the literal 25, then runs the function body
#   c == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_elements_in_lst(lst, element) — stored for later
#   2. Assign to lst
#   3. Assign to element
#   4. Run print(count_elements_in_lst(...))
#       calls count_elements_in_lst with: lst=lst, element=element
#         4.1 Assign to count
#         4.2 For loop over lst
#         4.3 Return from function
#   5. Define def count_elements_in_lst(lst, element) — stored for later
#   6. Define def count_elements_in_lst(lst, element) — stored for later
#   7. Assign to lst
#   8. Assign to element
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_elements_in_lst(...))
#       calls count_elements_in_lst with: lst=[1, 3, 5, 7], element=25
#         11.1 Assign to count
#         11.2 For loop over lst
#         11.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_elements_in_lst, count_elements_in_lst, count_elements_in_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      element = ... (stored in memory)
#      print(count_elements_in_lst(lst, element)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_elements_in_lst(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 546:
#
# ===== SYNTAX =====
#   def count_test_occurrences(s)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_test_occurrences(s)): the inner count_test_occurrences() executes FIRST, then print() outputs the result
#   count_test_occurrences(s): looks up variable 's', then runs the function body
#   print(count_test_occurrences(10)): the inner count_test_occurrences() executes FIRST, then print() outputs the result
#   count_test_occurrences(s): looks up variable 's', then runs the function body
#   count_test_occurrences(10): gets the literal 10, then runs the function body
#   c == 'test': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_test_occurrences(s) — stored for later
#   2. Assign to s
#   3. Run print(count_test_occurrences(...))
#       calls count_test_occurrences with: s=s
#         3.1 Assign to count
#         3.2 For loop over s
#         3.3 Return from function
#   4. Define def count_test_occurrences(s) — stored for later
#   5. Define def count_test_occurrences(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_test_occurrences(...))
#       calls count_test_occurrences with: s=10
#         9.1 Assign to count
#         9.2 For loop over s
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_test_occurrences, count_test_occurrences, count_test_occurrences are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      print(count_test_occurrences(s)) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_test_occurrences(10)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 547:
#
# ===== SYNTAX =====
#   def count_floating_numbers(lst, number)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_floating_numbers(?, 1.5)): the inner count_floating_numbers() executes FIRST, then print() outputs the result
#   count_floating_numbers(lst, number): looks up variable 'lst', looks up variable 'number', then runs the function body
#   print(count_floating_numbers(?, 25)): the inner count_floating_numbers() executes FIRST, then print() outputs the result
#   count_floating_numbers(?, 1.5): resolves the value, gets the literal 1.5, then runs the function body
#   count_floating_numbers(?, 25): resolves the value, gets the literal 25, then runs the function body
#   c == number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_floating_numbers(lst, number) — stored for later
#   2. Run print(count_floating_numbers(...))
#       calls count_floating_numbers with: lst=[1.5, 2.0, 1.5, 3.5], number=1.5
#         2.1 Assign to count
#         2.2 For loop over lst
#         2.3 Return from function
#   3. Define def count_floating_numbers(lst, number) — stored for later
#   4. Define def count_floating_numbers(lst, number) — stored for later
#   5. Assign to lst
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_floating_numbers(...))
#       calls count_floating_numbers with: lst=[1, 3, 5, 7], number=25
#         9.1 Assign to count
#         9.2 For loop over lst
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_floating_numbers, count_floating_numbers, count_floating_numbers are just stored
#   Execution starts at the non-def lines below:
#      print(count_floating_numbers(?, 1.5)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_floating_numbers(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 548:
#
# ===== SYNTAX =====
#   def count_floating_numbers(lst, number)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_floating_numbers(lst, number)): the inner count_floating_numbers() executes FIRST, then print() outputs the result
#   count_floating_numbers(lst, number): looks up variable 'lst', looks up variable 'number', then runs the function body
#   print(count_floating_numbers(?, 25)): the inner count_floating_numbers() executes FIRST, then print() outputs the result
#   count_floating_numbers(lst, number): looks up variable 'lst', looks up variable 'number', then runs the function body
#   count_floating_numbers(?, 25): resolves the value, gets the literal 25, then runs the function body
#   c == number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_floating_numbers(lst, number) — stored for later
#   2. Assign to lst
#   3. Assign to number
#   4. Run print(count_floating_numbers(...))
#       calls count_floating_numbers with: lst=lst, number=number
#         4.1 Assign to count
#         4.2 For loop over lst
#         4.3 Return from function
#   5. Define def count_floating_numbers(lst, number) — stored for later
#   6. Define def count_floating_numbers(lst, number) — stored for later
#   7. Assign to lst
#   8. Assign to number
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_floating_numbers(...))
#       calls count_floating_numbers with: lst=[1, 3, 5, 7], number=25
#         11.1 Assign to count
#         11.2 For loop over lst
#         11.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_floating_numbers, count_floating_numbers, count_floating_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      number = ... (stored in memory)
#      print(count_floating_numbers(lst, number)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_floating_numbers(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 549:
#
# ===== SYNTAX =====
#   def count_uppercase(string)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_uppercase(string)): the inner count_uppercase() executes FIRST, then print() outputs the result
#   count_uppercase(string): looks up variable 'string', then runs the function body
#   print(count_uppercase('world')): the inner count_uppercase() executes FIRST, then print() outputs the result
#   count_uppercase(string): looks up variable 'string', then runs the function body
#   count_uppercase('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_uppercase(string) — stored for later
#   2. Assign to string
#   3. Run print(count_uppercase(...))
#       calls count_uppercase with: string=string
#         3.1 Return from function
#   4. Define def count_uppercase(string) — stored for later
#   5. Define def count_uppercase(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_uppercase(...))
#       calls count_uppercase with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_uppercase, count_uppercase, count_uppercase are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_uppercase(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_uppercase('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from 'ABCDEFGHIJKLMNOPQRSTUVWXYZ' each round

"""

"""
Problem 550:
#
# ===== SYNTAX =====
#   def count_digit(num, digit)
#   return — exits the function and sends back a value
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(count_digit(12345, 3)): the inner count_digit() executes FIRST, then print() outputs the result
#   count_digit(num, digit): looks up variable 'num', looks up variable 'digit', then runs the function body
#   print(count_digit(25, 25)): the inner count_digit() executes FIRST, then print() outputs the result
#   count_digit(12345, 3): gets the literal 12345, gets the literal 3, then runs the function body
#   count_digit(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_digit(num, digit) — stored for later
#   2. Run print(count_digit(...))
#       calls count_digit with: num=12345, digit=3
#         2.1 Return from function
#   3. Define def count_digit(num, digit) — stored for later
#   4. Define def count_digit(num, digit) — stored for later
#   5. Assign to num
#   6. Assign to digit
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_digit(...))
#       calls count_digit with: num=25, digit=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_digit, count_digit, count_digit are just stored
#   Execution starts at the non-def lines below:
#      print(count_digit(12345, 3)) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      digit = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_digit(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 551:
#
# ===== SYNTAX =====
#   def count_letters(string)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_letters(string)): the inner count_letters() executes FIRST, then print() outputs the result
#   count_letters(string): looks up variable 'string', then runs the function body
#   print(count_letters('world')): the inner count_letters() executes FIRST, then print() outputs the result
#   count_letters(string): looks up variable 'string', then runs the function body
#   count_letters('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_letters(string) — stored for later
#   2. Assign to string
#   3. Run print(count_letters(...))
#       calls count_letters with: string=string
#         3.1 Return from function
#   4. Define def count_letters(string) — stored for later
#   5. Define def count_letters(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_letters(...))
#       calls count_letters with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_letters, count_letters, count_letters are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_letters(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_letters('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string.lower(...) each round

"""

"""
Problem 552:
#
# ===== SYNTAX =====
#   def count_text(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_text(string)): the inner count_text() executes FIRST, then print() outputs the result
#   count_text(string): looks up variable 'string', then runs the function body
#   print(count_text('world')): the inner count_text() executes FIRST, then print() outputs the result
#   count_text(string): looks up variable 'string', then runs the function body
#   count_text('world'): gets the literal 'world', then runs the function body
#   c == 'python': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_text(string) — stored for later
#   2. Assign to string
#   3. Run print(count_text(...))
#       calls count_text with: string=string
#         3.1 Assign to count
#         3.2 For loop over string
#         3.3 Return from function
#   4. Define def count_text(string) — stored for later
#   5. Define def count_text(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_text(...))
#       calls count_text with: string='world'
#         9.1 Assign to count
#         9.2 For loop over string
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_text, count_text, count_text are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_text(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_text('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 553:
#
# ===== SYNTAX =====
#   def count_item_in_dict_list(dict_list, item)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_item_in_dict_list(dict_list, ?)): the inner count_item_in_dict_list() executes FIRST, then print() outputs the result
#   count_item_in_dict_list(dict_list, item): looks up variable 'dict_list', looks up variable 'item', then runs the function body
#   print(count_item_in_dict_list(?, 25)): the inner count_item_in_dict_list() executes FIRST, then print() outputs the result
#   count_item_in_dict_list(dict_list, ?): looks up variable 'dict_list', resolves the value, then runs the function body
#   count_item_in_dict_list(?, 25): resolves the value, gets the literal 25, then runs the function body
#   c == item: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_item_in_dict_list(dict_list, item) — stored for later
#   2. Assign to dict_list
#   3. Run print(count_item_in_dict_list(...))
#       calls count_item_in_dict_list with: dict_list=dict_list, item={...}
#         3.1 Assign to count
#         3.2 For loop over dict_list
#         3.3 Return from function
#   4. Define def count_item_in_dict_list(dict_list, item) — stored for later
#   5. Define def count_item_in_dict_list(dict_list, item) — stored for later
#   6. Assign to dict_list
#   7. Assign to item
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_item_in_dict_list(...))
#       calls count_item_in_dict_list with: dict_list=[4, 1, 8, 6, 3], item=25
#         10.1 Assign to count
#         10.2 For loop over dict_list
#         10.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_item_in_dict_list, count_item_in_dict_list, count_item_in_dict_list are just stored
#   Execution starts at the non-def lines below:
#      dict_list = ... (stored in memory)
#      print(count_item_in_dict_list(dict_list, ?)) → function runs first, then print shows the result
#      dict_list = ... (stored in memory)
#      item = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_item_in_dict_list(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dict_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 554:
#
# ===== SYNTAX =====
#   def count_letters(lst)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_letters(lst)): the inner count_letters() executes FIRST, then print() outputs the result
#   count_letters(lst): looks up variable 'lst', then runs the function body
#   print(count_letters(?)): the inner count_letters() executes FIRST, then print() outputs the result
#   count_letters(lst): looks up variable 'lst', then runs the function body
#   count_letters(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_letters(lst) — stored for later
#   2. Assign to lst
#   3. Run print(count_letters(...))
#       calls count_letters with: lst=lst
#         3.1 Return from function
#   4. Define def count_letters(lst) — stored for later
#   5. Define def count_letters(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_letters(...))
#       calls count_letters with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_letters, count_letters, count_letters are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(count_letters(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_letters(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 555:
#
# ===== SYNTAX =====
#   def count_asterisks(s)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_asterisks(s)): the inner count_asterisks() executes FIRST, then print() outputs the result
#   count_asterisks(s): looks up variable 's', then runs the function body
#   print(count_asterisks(10)): the inner count_asterisks() executes FIRST, then print() outputs the result
#   count_asterisks(s): looks up variable 's', then runs the function body
#   count_asterisks(10): gets the literal 10, then runs the function body
#   c == '*': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_asterisks(s) — stored for later
#   2. Assign to s
#   3. Run print(count_asterisks(...))
#       calls count_asterisks with: s=s
#         3.1 Assign to count
#         3.2 For loop over s
#         3.3 Return from function
#   4. Define def count_asterisks(s) — stored for later
#   5. Define def count_asterisks(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_asterisks(...))
#       calls count_asterisks with: s=10
#         9.1 Assign to count
#         9.2 For loop over s
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_asterisks, count_asterisks, count_asterisks are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      print(count_asterisks(s)) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_asterisks(10)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 556:
#
# ===== SYNTAX =====
#   def count_substring(s, substring)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_substring(s, substring)): the inner count_substring() executes FIRST, then print() outputs the result
#   count_substring(s, substring): looks up variable 's', looks up variable 'substring', then runs the function body
#   print(count_substring(10, 'world')): the inner count_substring() executes FIRST, then print() outputs the result
#   count_substring(s, substring): looks up variable 's', looks up variable 'substring', then runs the function body
#   count_substring(10, 'world'): gets the literal 10, gets the literal 'world', then runs the function body
#   c == substring: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_substring(s, substring) — stored for later
#   2. Assign to s
#   3. Assign to substring
#   4. Run print(count_substring(...))
#       calls count_substring with: s=s, substring=substring
#         4.1 Assign to count
#         4.2 For loop over s
#         4.3 Return from function
#   5. Define def count_substring(s, substring) — stored for later
#   6. Define def count_substring(s, substring) — stored for later
#   7. Assign to s
#   8. Assign to substring
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_substring(...))
#       calls count_substring with: s=10, substring='world'
#         11.1 Assign to count
#         11.2 For loop over s
#         11.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_substring, count_substring, count_substring are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      substring = ... (stored in memory)
#      print(count_substring(s, substring)) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      substring = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_substring(10, 'world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 557:
#
# ===== SYNTAX =====
#   def count_lines_with_word(filename, word)
#   return — exits the function and sends back a value
#   open() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   with
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(count_lines_with_word(filename, word)): the inner count_lines_with_word() executes FIRST, then print() outputs the result
#   count_lines_with_word(filename, word): looks up variable 'filename', looks up variable 'word', then runs the function body
#   print(count_lines_with_word(25, 'coding')): the inner count_lines_with_word() executes FIRST, then print() outputs the result
#   count_lines_with_word(filename, word): looks up variable 'filename', looks up variable 'word', then runs the function body
#   count_lines_with_word(25, 'coding'): gets the literal 25, gets the literal 'coding', then runs the function body
#   line.lower(...).count(...) > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_lines_with_word(filename, word) — stored for later
#   2. Assign to filename
#   3. Assign to word
#   4. Run print(count_lines_with_word(...))
#       calls count_lines_with_word with: filename=filename, word=word
#         4.1 Return from function
#   5. Define def count_lines_with_word(filename, word) — stored for later
#   6. Define def count_lines_with_word(filename, word) — stored for later
#   7. Assign to filename
#   8. Assign to word
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_lines_with_word(...))
#       calls count_lines_with_word with: filename=25, word='coding'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_lines_with_word, count_lines_with_word, count_lines_with_word are just stored
#   Execution starts at the non-def lines below:
#      filename = ... (stored in memory)
#      word = ... (stored in memory)
#      print(count_lines_with_word(filename, word)) → function runs first, then print shows the result
#      filename = ... (stored in memory)
#      word = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_lines_with_word(25, 'coding')) → function runs first, then print shows the result
#
#   Loop: grabs one item from file each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 558:
#
# ===== SYNTAX =====
#   def count_email_domain(email_list, domain)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_email_domain(email_list, domain)): the inner count_email_domain() executes FIRST, then print() outputs the result
#   count_email_domain(email_list, domain): looks up variable 'email_list', looks up variable 'domain', then runs the function body
#   print(count_email_domain(?, 25)): the inner count_email_domain() executes FIRST, then print() outputs the result
#   count_email_domain(email_list, domain): looks up variable 'email_list', looks up variable 'domain', then runs the function body
#   count_email_domain(?, 25): resolves the value, gets the literal 25, then runs the function body
#   email_domain == domain: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_email_domain(email_list, domain) — stored for later
#   2. Assign to email_list
#   3. Assign to domain
#   4. Run print(count_email_domain(...))
#       calls count_email_domain with: email_list=email_list, domain=domain
#         4.1 Return from function
#   5. Define def count_email_domain(email_list, domain) — stored for later
#   6. Define def count_email_domain(email_list, domain) — stored for later
#   7. Assign to email_list
#   8. Assign to domain
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_email_domain(...))
#       calls count_email_domain with: email_list=[4, 1, 8, 6, 3], domain=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_email_domain, count_email_domain, count_email_domain are just stored
#   Execution starts at the non-def lines below:
#      email_list = ... (stored in memory)
#      domain = ... (stored in memory)
#      print(count_email_domain(email_list, domain)) → function runs first, then print shows the result
#      email_list = ... (stored in memory)
#      domain = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_email_domain(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from email_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 559:
#
# ===== SYNTAX =====
#   def count_word(sentences)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_word(sentences)): the inner count_word() executes FIRST, then print() outputs the result
#   count_word(sentences): looks up variable 'sentences', then runs the function body
#   print(count_word(?)): the inner count_word() executes FIRST, then print() outputs the result
#   count_word(sentences): looks up variable 'sentences', then runs the function body
#   count_word(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_word(sentences) — stored for later
#   2. Assign to sentences
#   3. Run print(count_word(...))
#       calls count_word with: sentences=sentences
#         3.1 Return from function
#   4. Define def count_word(sentences) — stored for later
#   5. Define def count_word(sentences) — stored for later
#   6. Assign to sentences
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_word(...))
#       calls count_word with: sentences=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_word, count_word, count_word are just stored
#   Execution starts at the non-def lines below:
#      sentences = ... (stored in memory)
#      print(count_word(sentences)) → function runs first, then print shows the result
#      sentences = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_word(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sentences each round

"""

"""
Problem 560:
#
# ===== SYNTAX =====
#   def count_exclamations(lst)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_exclamations(lst)): the inner count_exclamations() executes FIRST, then print() outputs the result
#   count_exclamations(lst): looks up variable 'lst', then runs the function body
#   print(count_exclamations(?)): the inner count_exclamations() executes FIRST, then print() outputs the result
#   count_exclamations(lst): looks up variable 'lst', then runs the function body
#   count_exclamations(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_exclamations(lst) — stored for later
#   2. Assign to lst
#   3. Run print(count_exclamations(...))
#       calls count_exclamations with: lst=lst
#         3.1 Return from function
#   4. Define def count_exclamations(lst) — stored for later
#   5. Define def count_exclamations(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_exclamations(...))
#       calls count_exclamations with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_exclamations, count_exclamations, count_exclamations are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(count_exclamations(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_exclamations(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 562:
#
# ===== SYNTAX =====
#   def count_word_occurrences(lst, word)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_word_occurrences(words, word_to_count)): the inner count_word_occurrences() executes FIRST, then print() outputs the result
#   count_word_occurrences(lst, word): looks up variable 'lst', looks up variable 'word', then runs the function body
#   print(count_word_occurrences(?, 'coding')): the inner count_word_occurrences() executes FIRST, then print() outputs the result
#   count_word_occurrences(words, word_to_count): looks up variable 'words', looks up variable 'word_to_count', then runs the function body
#   count_word_occurrences(?, 'coding'): resolves the value, gets the literal 'coding', then runs the function body
#   c == word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_word_occurrences(lst, word) — stored for later
#   2. Assign to words
#   3. Assign to word_to_count
#   4. Run print(count_word_occurrences(...))
#       calls count_word_occurrences with: lst=words, word=word_to_count
#         4.1 Assign to count
#         4.2 For loop over lst
#         4.3 Return from function
#   5. Define def count_word_occurrences(lst, word) — stored for later
#   6. Define def count_word_occurrences(lst, word) — stored for later
#   7. Assign to lst
#   8. Assign to word
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_word_occurrences(...))
#       calls count_word_occurrences with: lst=[1, 3, 5, 7], word='coding'
#         11.1 Assign to count
#         11.2 For loop over lst
#         11.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_word_occurrences, count_word_occurrences, count_word_occurrences are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      word_to_count = ... (stored in memory)
#      print(count_word_occurrences(words, word_to_count)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      word = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_word_occurrences(?, 'coding')) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 563:
#
# ===== SYNTAX =====
#   def count_x_in_mixed_list(lst)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_x_in_mixed_list(mixed_list)): the inner count_x_in_mixed_list() executes FIRST, then print() outputs the result
#   count_x_in_mixed_list(lst): looks up variable 'lst', then runs the function body
#   print(count_x_in_mixed_list(?)): the inner count_x_in_mixed_list() executes FIRST, then print() outputs the result
#   count_x_in_mixed_list(mixed_list): looks up variable 'mixed_list', then runs the function body
#   count_x_in_mixed_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_x_in_mixed_list(lst) — stored for later
#   2. Assign to mixed_list
#   3. Run print(count_x_in_mixed_list(...))
#       calls count_x_in_mixed_list with: lst=mixed_list
#         3.1 Return from function
#   4. Define def count_x_in_mixed_list(lst) — stored for later
#   5. Define def count_x_in_mixed_list(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_x_in_mixed_list(...))
#       calls count_x_in_mixed_list with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_x_in_mixed_list, count_x_in_mixed_list, count_x_in_mixed_list are just stored
#   Execution starts at the non-def lines below:
#      mixed_list = ... (stored in memory)
#      print(count_x_in_mixed_list(mixed_list)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_x_in_mixed_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 565:
#
# ===== SYNTAX =====
#   def square_lst(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(square_lst(lst)): the inner square_lst() executes FIRST, then print() outputs the result
#   square_lst(lst): looks up variable 'lst', then runs the function body
#   print(square_lst(?)): the inner square_lst() executes FIRST, then print() outputs the result
#   num ** 2: first get num, then get 2, then compute num ** 2
#   square_lst(lst): looks up variable 'lst', then runs the function body
#   square_lst(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def square_lst(lst) — stored for later
#   2. Define def square(num) — stored for later
#   3. Assign to lst
#   4. Run print(square_lst(...))
#       calls square_lst with: lst=lst
#         4.1 Return from function
#   5. Define def square_lst(lst) — stored for later
#   6. Define def square_lst(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(square_lst(...))
#       calls square_lst with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_lst, square, square_lst, square_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(square_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_lst(?)) → function runs first, then print shows the result

"""

"""
Problem 566:
#
# ===== SYNTAX =====
#   def filter_odd_numbers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   print(filter_odd_numbers(lst)): the inner filter_odd_numbers() executes FIRST, then print() outputs the result
#   filter_odd_numbers(lst): looks up variable 'lst', then runs the function body
#   print(filter_odd_numbers(?)): the inner filter_odd_numbers() executes FIRST, then print() outputs the result
#   filter_odd_numbers(lst): looks up variable 'lst', then runs the function body
#   filter_odd_numbers(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_odd_numbers(lst) — stored for later
#   2. Define def is_odd(x) — stored for later
#   3. Assign to lst
#   4. Run print(filter_odd_numbers(...))
#       calls filter_odd_numbers with: lst=lst
#         4.1 Return from function
#   5. Define def filter_odd_numbers(lst) — stored for later
#   6. Define def filter_odd_numbers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(filter_odd_numbers(...))
#       calls filter_odd_numbers with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_odd_numbers, is_odd, filter_odd_numbers, filter_odd_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(filter_odd_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_odd_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 567:
#
# ===== SYNTAX =====
#   def even_num(num)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(filters_even_numbers(?)): the inner filters_even_numbers() executes FIRST, then print() outputs the result
#   even_num(num): looks up variable 'num', then runs the function body
#   print(even_num(25)): the inner even_num() executes FIRST, then print() outputs the result
#   filters_even_numbers(?): resolves the value, then runs the function body
#   even_num(25): gets the literal 25, then runs the function body
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def even_num(num) — stored for later
#   2. Define def filters_even_numbers(lst) — stored for later
#   3. Run print(filters_even_numbers(...))
#       calls filters_even_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         3.1 Return from function
#   4. Define def even_num(num) — stored for later
#   5. Define def even_num(num) — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_num(...))
#       calls even_num with: num=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_num, filters_even_numbers, even_num, even_num are just stored
#   Execution starts at the non-def lines below:
#      print(filters_even_numbers(?)) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_num(25)) → function runs first, then print shows the result

"""

"""
Problem 568:
#
# ===== SYNTAX =====
#   def convert_to_uppercase(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(convert_to_uppercase(lst)): the inner convert_to_uppercase() executes FIRST, then print() outputs the result
#   convert_to_uppercase(lst): looks up variable 'lst', then runs the function body
#   print(convert_to_uppercase(?)): the inner convert_to_uppercase() executes FIRST, then print() outputs the result
#   convert_to_uppercase(lst): looks up variable 'lst', then runs the function body
#   convert_to_uppercase(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_uppercase(lst) — stored for later
#   2. Assign to lst
#   3. Run print(convert_to_uppercase(...))
#       calls convert_to_uppercase with: lst=lst
#         3.1 Return from function
#   4. Define def convert_to_uppercase(lst) — stored for later
#   5. Define def convert_to_uppercase(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_uppercase(...))
#       calls convert_to_uppercase with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_uppercase, convert_to_uppercase, convert_to_uppercase are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(convert_to_uppercase(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_uppercase(?)) → function runs first, then print shows the result

"""

"""
Problem 569:
#
# ===== SYNTAX =====
#   def empty_strings(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   print(empty_strings(lst)): the inner empty_strings() executes FIRST, then print() outputs the result
#   empty_strings(lst): looks up variable 'lst', then runs the function body
#   print(empty_strings(?)): the inner empty_strings() executes FIRST, then print() outputs the result
#   empty_strings(lst): looks up variable 'lst', then runs the function body
#   empty_strings(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def empty_strings(lst) — stored for later
#   2. Define def call_filter(string) — stored for later
#   3. Assign to lst
#   4. Run print(empty_strings(...))
#       calls empty_strings with: lst=lst
#         4.1 Return from function
#   5. Define def empty_strings(lst) — stored for later
#   6. Define def empty_strings(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(empty_strings(...))
#       calls empty_strings with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions empty_strings, call_filter, empty_strings, empty_strings are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(empty_strings(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(empty_strings(?)) → function runs first, then print shows the result

"""

"""
Problem 570:
#
# ===== SYNTAX =====
#   def multiply_lst(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(multiply_lst(lst)): the inner multiply_lst() executes FIRST, then print() outputs the result
#   multiply_lst(lst): looks up variable 'lst', then runs the function body
#   print(multiply_lst(?)): the inner multiply_lst() executes FIRST, then print() outputs the result
#   multiply_lst(lst): looks up variable 'lst', then runs the function body
#   multiply_lst(?): resolves the value, then runs the function body
#   x * 5: first get x, then get 5, then compute x * 5
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(multiply_lst(...))
#       calls multiply_lst with: lst=lst
#         3.1 Return from function
#   4. Define def multiply_lst(lst) — stored for later
#   5. Define def multiply_lst(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(multiply_lst(...))
#       calls multiply_lst with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_lst, multiply_lst, multiply_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(multiply_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 571:
#
# ===== SYNTAX =====
#   def lst_of_string(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   len() — built-in function call
#   map() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(lst_of_string(lst)): the inner lst_of_string() executes FIRST, then print() outputs the result
#   lst_of_string(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_string(?)): the inner lst_of_string() executes FIRST, then print() outputs the result
#   lst_of_string(lst): looks up variable 'lst', then runs the function body
#   lst_of_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_of_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_of_string(...))
#       calls lst_of_string with: lst=lst
#         3.1 Return from function
#   4. Define def lst_of_string(lst) — stored for later
#   5. Define def lst_of_string(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_of_string(...))
#       calls lst_of_string with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_of_string, lst_of_string, lst_of_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_of_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_of_string(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round

"""

"""
Problem 572:
#
# ===== SYNTAX =====
#   def palindromes(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(palindromes(?)): the inner palindromes() executes FIRST, then print() outputs the result
#   palindromes(lst): looks up variable 'lst', then runs the function body
#   print(palindromes(?)): the inner palindromes() executes FIRST, then print() outputs the result
#   palindromes(?): resolves the value, then runs the function body
#   palindromes(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def palindromes(lst) — stored for later
#   2. Run print(palindromes(...))
#       calls palindromes with: lst=['radar', 'hello', 'level', 'world']
#         2.1 Return from function
#   3. Define def palindromes(lst) — stored for later
#   4. Define def palindromes(lst) — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(palindromes(...))
#       calls palindromes with: lst=[1, 3, 5, 7]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions palindromes, palindromes, palindromes are just stored
#   Execution starts at the non-def lines below:
#      print(palindromes(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(palindromes(?)) → function runs first, then print shows the result

"""

"""
Problem 573:
#
# ===== SYNTAX =====
#   def sum_of_lst(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(sum_of_list(lst)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   print(sum_of_list(lst)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   sum_of_lst(lst): looks up variable 'lst', then runs the function body
#   print(sum_of_lst(?)): the inner sum_of_lst() executes FIRST, then print() outputs the result
#   sum_of_list(lst): looks up variable 'lst', then runs the function body
#   x + y: first get x, then get y, then compute x + y
#   sum_of_list(lst): looks up variable 'lst', then runs the function body
#   sum_of_lst(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def sum_of_lst(lst) — stored for later
#   3. Assign to lst
#   4. Run print(sum_of_list(...))
#       calls sum_of_list with: lst=lst
#         4.1 Return from function
#   5. Define def add(x, y) — stored for later
#   6. Define def sum_of_list(lst) — stored for later
#   7. Assign to lst
#   8. Run print(sum_of_list(...))
#       calls sum_of_list with: lst=lst
#         8.1 Return from function
#   9. Define def sum_of_lst(lst) — stored for later
#   10. Define def sum_of_lst(lst) — stored for later
#   11. Assign to lst
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(sum_of_lst(...))
#       calls sum_of_lst with: lst=[1, 3, 5, 7]
#         14.1 Import operator
#         14.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_of_lst, add, sum_of_list, sum_of_lst, sum_of_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sum_of_list(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(sum_of_list(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_lst(?)) → function runs first, then print shows the result

"""

"""
Problem 574:
#
# ===== SYNTAX =====
#   def sum_of_lst(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(sum_of_list(lst)): inner expression resolves first, then print() outputs
#   print(product_of_list(lst)): the inner product_of_list() executes FIRST, then print() outputs the result
#   sum_of_lst(lst): looks up variable 'lst', then runs the function body
#   print(sum_of_lst(?)): the inner sum_of_lst() executes FIRST, then print() outputs the result
#   x * y: first get x, then get y, then compute x * y
#   product_of_list(lst): looks up variable 'lst', then runs the function body
#   sum_of_lst(?): resolves the value, then runs the function body
#   x * y: first get x, then get y, then compute x * y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def sum_of_lst(lst) — stored for later
#   3. Assign to lst
#   4. Run print(sum_of_list(...))
#   5. Define def multiply(x, y) — stored for later
#   6. Define def product_of_list(lst) — stored for later
#   7. Assign to lst
#   8. Run print(product_of_list(...))
#       calls product_of_list with: lst=lst
#         8.1 Return from function
#   9. Define def sum_of_lst(lst) — stored for later
#   10. Define def sum_of_lst(lst) — stored for later
#   11. Assign to lst
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(sum_of_lst(...))
#       calls sum_of_lst with: lst=[1, 3, 5, 7]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_of_lst, multiply, product_of_list, sum_of_lst, sum_of_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sum_of_list(...))
#      lst = ... (stored in memory)
#      print(product_of_list(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 575:
#
# ===== SYNTAX =====
#   def capitalize_string(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   capitalize_string(lst): looks up variable 'lst', then runs the function body
#   print(lst_strings(lst)): the inner lst_strings() executes FIRST, then print() outputs the result
#   capitalize_string(lst): looks up variable 'lst', then runs the function body
#   print(capitalize_string(?)): the inner capitalize_string() executes FIRST, then print() outputs the result
#   lst_strings(lst): looks up variable 'lst', then runs the function body
#   capitalize_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_string(lst) — stored for later
#   2. Assign to lst
#   3. Run capitalize_string(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def lst_strings(lst) — stored for later
#   5. Assign to lst
#   6. Run print(lst_strings(...))
#       calls lst_strings with: lst=lst
#         6.1 Define def capitalize_lst(word) — stored for later
#         6.2 Return from function
#   7. Define def capitalize_string(lst) — stored for later
#   8. Define def capitalize_string(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(capitalize_string(...))
#       calls capitalize_string with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_string, lst_strings, capitalize_string, capitalize_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      capitalize_string(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(lst_strings(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_string(?)) → function runs first, then print shows the result

"""

"""
Problem 576:
#
# ===== SYNTAX =====
#   def min_number(x, y)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(number_lst(lst)): the inner number_lst() executes FIRST, then print() outputs the result
#   min_number(x, y): looks up variable 'x', looks up variable 'y', then runs the function body
#   print(min_number(3, 14)): the inner min_number() executes FIRST, then print() outputs the result
#   number_lst(lst): looks up variable 'lst', then runs the function body
#   min_number(3, 14): gets the literal 3, gets the literal 14, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def min_number(x, y) — stored for later
#   3. Define def number_lst(lst) — stored for later
#   4. Assign to lst
#   5. Run print(number_lst(...))
#       calls number_lst with: lst=lst
#         5.1 Return from function
#   6. Define def min_number(x, y) — stored for later
#   7. Define def min_number(x, y) — stored for later
#   8. Assign to x
#   9. Assign to y
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(min_number(...))
#       calls min_number with: x=3, y=14
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions min_number, number_lst, min_number, min_number are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(number_lst(lst)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(min_number(3, 14)) → function runs first, then print shows the result

"""

"""
Problem 577:
#
# ===== SYNTAX =====
#   def find_max(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   if/elif/else
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(find_max(lst)): the inner find_max() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(number_lst(lst)): the inner number_lst() executes FIRST, then print() outputs the result
#   find_max(lst): looks up variable 'lst', then runs the function body
#   print(find_max(?)): the inner find_max() executes FIRST, then print() outputs the result
#   find_max(lst): looks up variable 'lst', then runs the function body
#   number_lst(lst): looks up variable 'lst', then runs the function body
#   find_max(?): resolves the value, then runs the function body
#   x > y: is this True or False? The answer picks which branch
#   x > y: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def find_max(lst) — stored for later
#   3. Assign to lst
#   4. Run print(find_max(...))
#       calls find_max with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def max_num(x, y) — stored for later
#   7. Assign to lst
#   8. Run main(lst)
#       with: lst=lst
#         8.1 Return from function
#   9. Import reduce from functools
#   10. Define def min_number(x, y) — stored for later
#   11. Define def number_lst(lst) — stored for later
#   12. Assign to lst
#   13. Run print(number_lst(...))
#       calls number_lst with: lst=lst
#         13.1 Return from function
#   14. Define def find_max(lst) — stored for later
#   15. Define def find_max(lst) — stored for later
#   16. Assign to lst
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(find_max(...))
#       calls find_max with: lst=[1, 3, 5, 7]
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   def lines are SKIPPED during execution — functions find_max, main, max_num, min_number, number_lst, find_max, find_max are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_max(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(number_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_max(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 578:
#
# ===== SYNTAX =====
#   def list_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   len() — built-in function call
#   if/elif/else
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(list_words(lst)): the inner list_words() executes FIRST, then print() outputs the result
#   list_words(lst): looks up variable 'lst', then runs the function body
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   list_words(lst): looks up variable 'lst', then runs the function body
#   list_words(?): resolves the value, then runs the function body
#   len(...) > 3: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def list_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_words(...))
#       calls list_words with: lst=lst
#         3.1 Return from function
#   4. Define def list_words(lst) — stored for later
#   5. Define def list_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(list_words(...))
#       calls list_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_words, list_words, list_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_words(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 579:
#
# ===== SYNTAX =====
#   def lst_strings(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   print(lst_strings(lst)): the inner lst_strings() executes FIRST, then print() outputs the result
#   lst_strings(lst): looks up variable 'lst', then runs the function body
#   print(lst_strings(?)): the inner lst_strings() executes FIRST, then print() outputs the result
#   lst_strings(lst): looks up variable 'lst', then runs the function body
#   '%s%s' % (x, y): first get '%s%s', then get (x, y), then compute '%s%s' % (x, y)
#   lst_strings(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def lst_strings(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_strings(...))
#       calls lst_strings with: lst=lst
#         4.1 Return from function
#   5. Define def lst_strings(lst) — stored for later
#   6. Define def lst_strings(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_strings(...))
#       calls lst_strings with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions lst_strings, lst_strings, lst_strings are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_strings(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_strings(?)) → function runs first, then print shows the result

"""

"""
Problem 580:
#
# ===== SYNTAX =====
#   def num_lst(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   if/elif/else
#   operators: > <=
#
# ===== EVALUATION ORDER =====
#   print(num_lst(?)): the inner num_lst() executes FIRST, then print() outputs the result
#   num_lst(lst): looks up variable 'lst', then runs the function body
#   print(num_lst(?)): the inner num_lst() executes FIRST, then print() outputs the result
#   num_lst(?): resolves the value, then runs the function body
#   num_lst(?): resolves the value, then runs the function body
#   num > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def num_lst(lst) — stored for later
#   2. Run print(num_lst(...))
#       calls num_lst with: lst=[-1, 0, 1, -2, 2]
#         2.1 Return from function
#   3. Define def num_lst(lst) — stored for later
#   4. Define def num_lst(lst) — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(num_lst(...))
#       calls num_lst with: lst=[1, 3, 5, 7]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions num_lst, num_lst, num_lst are just stored
#   Execution starts at the non-def lines below:
#      print(num_lst(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(num_lst(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 581:
#
# ===== SYNTAX =====
#   def num_lst(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(num_lst(lst)): the inner num_lst() executes FIRST, then print() outputs the result
#   num_lst(lst): looks up variable 'lst', then runs the function body
#   print(num_lst(?)): the inner num_lst() executes FIRST, then print() outputs the result
#   num_lst(lst): looks up variable 'lst', then runs the function body
#   num_lst(?): resolves the value, then runs the function body
#   num ** 3: first get num, then get 3, then compute num ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def num_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(num_lst(...))
#       calls num_lst with: lst=lst
#         3.1 Import math
#         3.2 Return from function
#   4. Define def num_lst(lst) — stored for later
#   5. Define def num_lst(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(num_lst(...))
#       calls num_lst with: lst=[1, 3, 5, 7]
#         9.1 Import math
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions num_lst, num_lst, num_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(num_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(num_lst(?)) → function runs first, then print shows the result

"""

"""
Problem 582:
#
# ===== SYNTAX =====
#   def lst_numbers(lst)
#   return — exits the function and sends back a value
#   map() — built-in function call
#   reduce() — built-in function call
#   pow() — built-in function call
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   print(lst_numbers(lst)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   print(lst_numbers(?)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   lst_numbers(?): resolves the value, then runs the function body
#   num ** 2: first get num, then get 2, then compute num ** 2
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def lst_numbers(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=lst
#         4.1 Import math
#         4.2 Return from function
#   5. Define def lst_numbers(lst) — stored for later
#   6. Define def lst_numbers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=[1, 3, 5, 7]
#         10.1 Import math
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions lst_numbers, lst_numbers, lst_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 583:
#
# ===== SYNTAX =====
#   def lst_2(lst1, lst2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   zip() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_2(lst1, lst2)): the inner lst_2() executes FIRST, then print() outputs the result
#   lst_2(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(lst_2(?, ?)): the inner lst_2() executes FIRST, then print() outputs the result
#   lst_2(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   lst_2(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_2(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(lst_2(...))
#       calls lst_2 with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def lst_2(lst1, lst2) — stored for later
#   6. Define def lst_2(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(lst_2(...))
#       calls lst_2 with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_2, lst_2, lst_2 are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(lst_2(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_2(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 584:
#
# ===== SYNTAX =====
#   def non_alphabetical_string(string)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(non_alphabetical_string(?)): the inner non_alphabetical_string() executes FIRST, then print() outputs the result
#   non_alphabetical_string(string): looks up variable 'string', then runs the function body
#   print(non_alphabetical_string('world')): the inner non_alphabetical_string() executes FIRST, then print() outputs the result
#   non_alphabetical_string(?): resolves the value, then runs the function body
#   non_alphabetical_string('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def non_alphabetical_string(string) — stored for later
#   2. Run print(non_alphabetical_string(...))
#       calls non_alphabetical_string with: string=['hello', 'world', 'abc123', 'python']
#         2.1 Return from function
#   3. Define def non_alphabetical_string(string) — stored for later
#   4. Define def non_alphabetical_string(string) — stored for later
#   5. Assign to string
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(non_alphabetical_string(...))
#       calls non_alphabetical_string with: string='world'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions non_alphabetical_string, non_alphabetical_string, non_alphabetical_string are just stored
#   Execution starts at the non-def lines below:
#      print(non_alphabetical_string(?)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(non_alphabetical_string('world')) → function runs first, then print shows the result

"""

"""
Problem 585:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   if/elif/else
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#   word in counts: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def lst_words(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         4.1 Return from function
#   5. Define def lst_words(lst) — stored for later
#   6. Define def lst_words(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 586:
#
# ===== SYNTAX =====
#   def unique_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(unique_words(?)): the inner unique_words() executes FIRST, then print() outputs the result
#   unique_words(lst): looks up variable 'lst', then runs the function body
#   print(unique_words(?)): the inner unique_words() executes FIRST, then print() outputs the result
#   unique_words(?): resolves the value, then runs the function body
#   unique_words(?): resolves the value, then runs the function body
#   word not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def unique_words(lst) — stored for later
#   2. Run print(unique_words(...))
#       calls unique_words with: lst=['a', 'b', 'a', 'c', 'b']
#         2.1 Return from function
#   3. Define def unique_words(lst) — stored for later
#   4. Define def unique_words(lst) — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(unique_words(...))
#       calls unique_words with: lst=[1, 3, 5, 7]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions unique_words, unique_words, unique_words are just stored
#   Execution starts at the non-def lines below:
#      print(unique_words(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(unique_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 587:
#
# ===== SYNTAX =====
#   def word_lst(lst, n)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   len() — built-in function call
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(word_lst(?, 3)): the inner word_lst() executes FIRST, then print() outputs the result
#   word_lst(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   print(word_lst(?, 15)): the inner word_lst() executes FIRST, then print() outputs the result
#   word_lst(?, 3): resolves the value, gets the literal 3, then runs the function body
#   word_lst(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def word_lst(lst, n) — stored for later
#   2. Run print(word_lst(...))
#       calls word_lst with: lst=['a', 'ab', 'abc', 'abcd'], n=3
#         2.1 Return from function
#   3. Define def word_lst(lst, n) — stored for later
#   4. Define def word_lst(lst, n) — stored for later
#   5. Assign to lst
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(word_lst(...))
#       calls word_lst with: lst=[1, 3, 5, 7], n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions word_lst, word_lst, word_lst are just stored
#   Execution starts at the non-def lines below:
#      print(word_lst(?, 3)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(word_lst(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 588:
#
# ===== SYNTAX =====
#   def apply_lsts(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   filter() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: * % !=
#
# ===== EVALUATION ORDER =====
#   print(apply_lsts(lst)): the inner apply_lsts() executes FIRST, then print() outputs the result
#   apply_lsts(lst): looks up variable 'lst', then runs the function body
#   print(apply_lsts(?)): the inner apply_lsts() executes FIRST, then print() outputs the result
#   apply_lsts(lst): looks up variable 'lst', then runs the function body
#   apply_lsts(?): resolves the value, then runs the function body
#   num * num: first get num, then get num, then compute num * num
#   num % 2: first get num, then get 2, then compute num % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def apply_lsts(lst) — stored for later
#   2. Assign to lst
#   3. Run print(apply_lsts(...))
#       calls apply_lsts with: lst=lst
#         3.1 Return from function
#   4. Define def apply_lsts(lst) — stored for later
#   5. Define def apply_lsts(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(apply_lsts(...))
#       calls apply_lsts with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions apply_lsts, apply_lsts, apply_lsts are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(apply_lsts(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(apply_lsts(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 589:
#
# ===== SYNTAX =====
#   def sum_nested_list(nested_list)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(sum_nested_list(?)): the inner sum_nested_list() executes FIRST, then print() outputs the result
#   sum_nested_list(nested_list): looks up variable 'nested_list', then runs the function body
#   print(sum_nested_list(?)): the inner sum_nested_list() executes FIRST, then print() outputs the result
#   sum_nested_list(?): resolves the value, then runs the function body
#   sum_nested_list(?): resolves the value, then runs the function body
#   acc + item: first get acc, then get item, then compute acc + item
#   total + number: first get total, then get number, then compute total + number
#   total + number: first get total, then get number, then compute total + number
#   total + number: first get total, then get number, then compute total + number
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def sum_nested_list(nested_list) — stored for later
#   3. Run print(sum_nested_list(...))
#       calls sum_nested_list with: nested_list=[[1, 2], [3, 4], [5]]
#         3.1 Return from function
#   4. Define def sum_nested_list(nested_list) — stored for later
#   5. Define def sum_nested_list(nested_list) — stored for later
#   6. Assign to nested_list
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sum_nested_list(...))
#       calls sum_nested_list with: nested_list=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_nested_list, sum_nested_list, sum_nested_list are just stored
#   Execution starts at the non-def lines below:
#      print(sum_nested_list(?)) → function runs first, then print shows the result
#      nested_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_nested_list(?)) → function runs first, then print shows the result

"""

"""
Problem 590:
#
# ===== SYNTAX =====
#   def fibonacci_n_numbers(n)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   range() — built-in function call
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   print(fibonacci_n_numbers(10)): the inner fibonacci_n_numbers() executes FIRST, then print() outputs the result
#   fibonacci_n_numbers(n): looks up variable 'n', then runs the function body
#   print(fibonacci_n_numbers(15)): the inner fibonacci_n_numbers() executes FIRST, then print() outputs the result
#   fibonacci_n_numbers(10): gets the literal 10, then runs the function body
#   acc + [?]: first get acc, then get [?], then compute acc + [?]
#   acc + [?]: first get acc, then get [?], then compute acc + [?]
#   fibonacci_n_numbers(15): gets the literal 15, then runs the function body
#   acc + [?]: first get acc, then get [?], then compute acc + [?]
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   n - 2: first get n, then get 2, then compute n - 2
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def fibonacci_n_numbers(n) — stored for later
#   3. Run print(fibonacci_n_numbers(...))
#       calls fibonacci_n_numbers with: n=10
#         3.1 Return from function
#   4. Define def fibonacci_n_numbers(n) — stored for later
#   5. Define def fibonacci_n_numbers(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(fibonacci_n_numbers(...))
#       calls fibonacci_n_numbers with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions fibonacci_n_numbers, fibonacci_n_numbers, fibonacci_n_numbers are just stored
#   Execution starts at the non-def lines below:
#      print(fibonacci_n_numbers(10)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci_n_numbers(15)) → function runs first, then print shows the result

"""

"""
Problem 591:
#
# ===== SYNTAX =====
#   def lst_string(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(lst_string(lst)): the inner lst_string() executes FIRST, then print() outputs the result
#   lst_string(lst): looks up variable 'lst', then runs the function body
#   print(lst_string(?)): the inner lst_string() executes FIRST, then print() outputs the result
#   lst_string(lst): looks up variable 'lst', then runs the function body
#   lst_string(?): resolves the value, then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_string(...))
#       calls lst_string with: lst=lst
#         3.1 Return from function
#   4. Define def lst_string(lst) — stored for later
#   5. Define def lst_string(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_string(...))
#       calls lst_string with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_string, lst_string, lst_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_string(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 592:
#
# ===== SYNTAX =====
#   def sum_even_numbers(lst)
#   return — exits the function and sends back a value
#   filter() — built-in function call
#   reduce() — built-in function call
#   operators: + % ==
#
# ===== EVALUATION ORDER =====
#   print(sum_even_numbers(lst)): the inner sum_even_numbers() executes FIRST, then print() outputs the result
#   sum_even_numbers(lst): looks up variable 'lst', then runs the function body
#   print(sum_even_numbers(?)): the inner sum_even_numbers() executes FIRST, then print() outputs the result
#   sum_even_numbers(lst): looks up variable 'lst', then runs the function body
#   sum_even_numbers(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def sum_even_numbers(lst) — stored for later
#   3. Assign to lst
#   4. Run print(sum_even_numbers(...))
#       calls sum_even_numbers with: lst=lst
#         4.1 Return from function
#   5. Define def sum_even_numbers(lst) — stored for later
#   6. Define def sum_even_numbers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sum_even_numbers(...))
#       calls sum_even_numbers with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_even_numbers, sum_even_numbers, sum_even_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sum_even_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_even_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 593:
#
# ===== SYNTAX =====
#   def lst_string(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   len() — built-in function call
#   if/elif/else
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(lst_string(lst)): the inner lst_string() executes FIRST, then print() outputs the result
#   lst_string(lst): looks up variable 'lst', then runs the function body
#   print(lst_string(?)): the inner lst_string() executes FIRST, then print() outputs the result
#   lst_string(lst): looks up variable 'lst', then runs the function body
#   lst_string(?): resolves the value, then runs the function body
#   len(...) > len(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def lst_string(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_string(...))
#       calls lst_string with: lst=lst
#         4.1 Return from function
#   5. Define def lst_string(lst) — stored for later
#   6. Define def lst_string(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_string(...))
#       calls lst_string with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions lst_string, lst_string, lst_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_string(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 594:
#
# ===== SYNTAX =====
#   def lst_num(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(lst_num(lst)): the inner lst_num() executes FIRST, then print() outputs the result
#   lst_num(lst): looks up variable 'lst', then runs the function body
#   print(lst_num(?)): the inner lst_num() executes FIRST, then print() outputs the result
#   lst_num(lst): looks up variable 'lst', then runs the function body
#   lst_num(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def lst_num(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_num(...))
#       calls lst_num with: lst=lst
#         4.1 Import operator
#         4.2 Return from function
#   5. Define def lst_num(lst) — stored for later
#   6. Define def lst_num(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_num(...))
#       calls lst_num with: lst=[1, 3, 5, 7]
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions lst_num, lst_num, lst_num are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_num(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_num(?)) → function runs first, then print shows the result

"""

"""
Problem 595:
#
# ===== SYNTAX =====
#   def flatten(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   flatten(nested_list): looks up variable 'nested_list', then runs the function body
#   flatten(lst): looks up variable 'lst', then runs the function body
#   print(flatten(?)): the inner flatten() executes FIRST, then print() outputs the result
#   flatten(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def flatten(lst) — stored for later
#   3. Assign to nested_list
#   4. Assign to flattened_list
#   5. Run print(flattened_list)
#   6. Define def flatten(lst) — stored for later
#   7. Define def flatten(lst) — stored for later
#   8. Assign to lst
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(flatten(...))
#       calls flatten with: lst=[1, 3, 5, 7]
#         11.1 Import operator
#         11.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions flatten, flatten, flatten are just stored
#   Execution starts at the non-def lines below:
#      nested_list = ... (stored in memory)
#      flattened_list = ... (stored in memory)
#      print(flattened_list)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(flatten(?)) → function runs first, then print shows the result

"""

"""
Problem 596:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   if/elif/else
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#   word in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def lst_words(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         4.1 Return from function
#   5. Define def lst_words(lst) — stored for later
#   6. Define def lst_words(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 597:
#
# ===== SYNTAX =====
#   def lst_words(lst, target_word)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   sorted() — built-in function call
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   lst_words(lst, target_word): looks up variable 'lst', looks up variable 'target_word', then runs the function body
#   lst_words(lst, target_word): looks up variable 'lst', looks up variable 'target_word', then runs the function body
#   print(lst_words(?, ?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst, target_word) — stored for later
#   2. Assign to lst
#   3. Assign to target_word
#   4. Assign to filtered_words
#   5. Run print(filtered_words)
#   6. Define def lst_words(lst, target_word) — stored for later
#   7. Define def lst_words(lst, target_word) — stored for later
#   8. Assign to lst
#   9. Assign to target_word
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7], target_word=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      target_word = ... (stored in memory)
#      filtered_words = ... (stored in memory)
#      print(filtered_words)
#      lst = ... (stored in memory)
#      target_word = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 598:
#
# ===== SYNTAX =====
#   def compose(func1, func2)
#   return — exits the function and sends back a value
#   func2() — built-in function call
#   func1() — built-in function call
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   compose(add_one, multiply_by_two): looks up variable 'add_one', looks up variable 'multiply_by_two', then runs the function body
#   compose(func1, func2): looks up variable 'func1', looks up variable 'func2', then runs the function body
#   print(compose(25, 25)): the inner compose() executes FIRST, then print() outputs the result
#   x + 1: first get x, then get 1, then compute x + 1
#   x * 2: first get x, then get 2, then compute x * 2
#   compose(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def compose(func1, func2) — stored for later
#   2. Define def add_one(x) — stored for later
#   3. Define def multiply_by_two(x) — stored for later
#   4. Assign to composed_function
#   5. Assign to result
#   6. Run print(result)
#   7. Define def compose(func1, func2) — stored for later
#   8. Define def compose(func1, func2) — stored for later
#   9. Assign to func1
#   10. Assign to func2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(compose(...))
#       calls compose with: func1=25, func2=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions compose, add_one, multiply_by_two, compose, compose are just stored
#   Execution starts at the non-def lines below:
#      composed_function = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      func1 = ... (stored in memory)
#      func2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(compose(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 599:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 600:
#
# ===== SYNTAX =====
#   def is_prime(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   int() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + % ** == <=
#
# ===== EVALUATION ORDER =====
#   filter_primes(numbers): looks up variable 'numbers', then runs the function body
#   is_prime(n): looks up variable 'n', then runs the function body
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(15): gets the literal 15, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % i: first get n, then get i, then compute n % i
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n <= 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(n) — stored for later
#   2. Define def filter_primes(lst) — stored for later
#   3. Assign to numbers
#   4. Assign to prime_numbers
#   5. Run print(prime_numbers)
#   6. Define def is_prime(n) — stored for later
#   7. Define def is_prime(n) — stored for later
#   8. Assign to n
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(is_prime(...))
#       calls is_prime with: n=15
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, filter_primes, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      prime_numbers = ... (stored in memory)
#      print(prime_numbers)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 601:
#
# ===== SYNTAX =====
#   def map_with_index(lst, func)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   func() — built-in function call
#   map() — built-in function call
#   enumerate() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   map_with_index(numbers, example_func): looks up variable 'numbers', looks up variable 'example_func', then runs the function body
#   map_with_index(lst, func): looks up variable 'lst', looks up variable 'func', then runs the function body
#   print(map_with_index(?, 25)): the inner map_with_index() executes FIRST, then print() outputs the result
#   index * value: first get index, then get value, then compute index * value
#   map_with_index(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def map_with_index(lst, func) — stored for later
#   2. Define def example_func(index, value) — stored for later
#   3. Assign to numbers
#   4. Assign to result
#   5. Run print(result)
#   6. Define def map_with_index(lst, func) — stored for later
#   7. Define def map_with_index(lst, func) — stored for later
#   8. Assign to lst
#   9. Assign to func
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(map_with_index(...))
#       calls map_with_index with: lst=[1, 3, 5, 7], func=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions map_with_index, example_func, map_with_index, map_with_index are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      func = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(map_with_index(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 602:
#
# ===== SYNTAX =====
#   def is_in_list(x, list2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   is_in_list() — built-in function call
#   filter() — built-in function call
#   any() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   intersection(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   is_in_list(x, list2): looks up variable 'x', looks up variable 'list2', then runs the function body
#   print(is_in_list(3, ?)): the inner is_in_list() executes FIRST, then print() outputs the result
#   is_in_list(3, ?): gets the literal 3, resolves the value, then runs the function body
#   is_in_list(x, list2): looks up variable 'x', looks up variable 'list2', then runs the function body
#   element == x: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_in_list(x, list2) — stored for later
#   2. Define def intersection(list1, list2) — stored for later
#   3. Assign to list1
#   4. Assign to list2
#   5. Assign to result
#   6. Run print(result)
#   7. Define def is_in_list(x, list2) — stored for later
#   8. Define def is_in_list(x, list2) — stored for later
#   9. Assign to x
#   10. Assign to list2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(is_in_list(...))
#       calls is_in_list with: x=3, list2=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_in_list, intersection, is_in_list, is_in_list are just stored
#   Execution starts at the non-def lines below:
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      x = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_in_list(3, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from list2 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 603:
#
# ===== SYNTAX =====
#   def add(x, y)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   if/elif/else
#   operators: + /
#
# ===== EVALUATION ORDER =====
#   average(numbers): looks up variable 'numbers', then runs the function body
#   add(x, y): looks up variable 'x', looks up variable 'y', then runs the function body
#   print(add(3, 14)): the inner add() executes FIRST, then print() outputs the result
#   x + y: first get x, then get y, then compute x + y
#   total / len(...): first get total, then get len(...), then compute total / len(...)
#   add(3, 14): gets the literal 3, gets the literal 14, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def add(x, y) — stored for later
#   3. Define def average(lst) — stored for later
#   4. Assign to numbers
#   5. Assign to result
#   6. Run print(result)
#   7. Define def add(x, y) — stored for later
#   8. Define def add(x, y) — stored for later
#   9. Assign to x
#   10. Assign to y
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(add(...))
#       calls add with: x=3, y=14
#         13.1 Import operator
#         13.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions add, average, add, add are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add(3, 14)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 604:
#
# ===== SYNTAX =====
#   def compose_functions(functions, initial_value)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   func() — built-in function call
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   compose_functions(functions, initial_value): looks up variable 'functions', looks up variable 'initial_value', then runs the function body
#   compose_functions(functions, initial_value): looks up variable 'functions', looks up variable 'initial_value', then runs the function body
#   print(compose_functions(25, 25)): the inner compose_functions() executes FIRST, then print() outputs the result
#   x + 1: first get x, then get 1, then compute x + 1
#   x * 2: first get x, then get 2, then compute x * 2
#   compose_functions(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def compose_functions(functions, initial_value) — stored for later
#   3. Define def add_one(x) — stored for later
#   4. Define def multiply_by_two(x) — stored for later
#   5. Assign to functions
#   6. Assign to initial_value
#   7. Assign to result
#   8. Run print(result)
#   9. Define def compose_functions(functions, initial_value) — stored for later
#   10. Define def compose_functions(functions, initial_value) — stored for later
#   11. Assign to functions
#   12. Assign to initial_value
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(compose_functions(...))
#       calls compose_functions with: functions=25, initial_value=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions compose_functions, add_one, multiply_by_two, compose_functions, compose_functions are just stored
#   Execution starts at the non-def lines below:
#      functions = ... (stored in memory)
#      initial_value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      functions = ... (stored in memory)
#      initial_value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(compose_functions(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 605:
#
# ===== SYNTAX =====
#   def get_keys(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(get_keys(dic)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_dict_keys(): — no arguments needed
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   print(get_keys(25)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   get_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(get_keys(...))
#       calls get_keys with: dic=dic
#         3.1 Return from function
#   4. Define def get_dict_keys() — stored for later
#   5. Assign to keys_list
#   6. Run print(keys_list)
#   7. Define def get_keys(dic) — stored for later
#   8. Define def get_keys(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(get_keys(...))
#       calls get_keys with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_keys, get_dict_keys, get_keys, get_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(get_keys(dic)) → function runs first, then print shows the result
#      keys_list = ... (stored in memory)
#      print(keys_list)
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_keys(25)) → function runs first, then print shows the result

"""

"""
Problem 606:
#
# ===== SYNTAX =====
#   def sort_string_len(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_string_len(lst)): the inner sort_string_len() executes FIRST, then print() outputs the result
#   sort_string_len(lst): looks up variable 'lst', then runs the function body
#   print(sort_string_len(?)): the inner sort_string_len() executes FIRST, then print() outputs the result
#   sort_string_len(lst): looks up variable 'lst', then runs the function body
#   sort_string_len(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_string_len(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_string_len(...))
#       calls sort_string_len with: lst=lst
#         3.1 Return from function
#   4. Define def sort_string_len(lst) — stored for later
#   5. Define def sort_string_len(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_string_len(...))
#       calls sort_string_len with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_string_len, sort_string_len, sort_string_len are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_string_len(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_string_len(?)) → function runs first, then print shows the result

"""

"""
Problem 607:
#
# ===== SYNTAX =====
#   def lst_tuples_second(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_tuples_second(lst)): the inner lst_tuples_second() executes FIRST, then print() outputs the result
#   lst_tuples_second(lst): looks up variable 'lst', then runs the function body
#   print(lst_tuples_second(?)): the inner lst_tuples_second() executes FIRST, then print() outputs the result
#   lst_tuples_second(lst): looks up variable 'lst', then runs the function body
#   lst_tuples_second(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_tuples_second(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_tuples_second(...))
#       calls lst_tuples_second with: lst=lst
#         3.1 Return from function
#   4. Define def lst_tuples_second(lst) — stored for later
#   5. Define def lst_tuples_second(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_tuples_second(...))
#       calls lst_tuples_second with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_tuples_second, lst_tuples_second, lst_tuples_second are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_tuples_second(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_tuples_second(?)) → function runs first, then print shows the result

"""

"""
Problem 608:
#
# ===== SYNTAX =====
#   def sort_dicts_by_key(lst, target_key)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_dicts_by_key(dicts, target_key): looks up variable 'dicts', looks up variable 'target_key', then runs the function body
#   sort_dicts_by_key(lst, target_key): looks up variable 'lst', looks up variable 'target_key', then runs the function body
#   print(sort_dicts_by_key(?, 25)): the inner sort_dicts_by_key() executes FIRST, then print() outputs the result
#   sort_dicts_by_key(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_dicts_by_key(lst, target_key) — stored for later
#   2. Assign to dicts
#   3. Assign to target_key
#   4. Assign to sorted_dicts
#   5. Run print(sorted_dicts)
#   6. Define def sort_dicts_by_key(lst, target_key) — stored for later
#   7. Define def sort_dicts_by_key(lst, target_key) — stored for later
#   8. Assign to lst
#   9. Assign to target_key
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(sort_dicts_by_key(...))
#       calls sort_dicts_by_key with: lst=[1, 3, 5, 7], target_key=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_dicts_by_key, sort_dicts_by_key, sort_dicts_by_key are just stored
#   Execution starts at the non-def lines below:
#      dicts = ... (stored in memory)
#      target_key = ... (stored in memory)
#      sorted_dicts = ... (stored in memory)
#      print(sorted_dicts)
#      lst = ... (stored in memory)
#      target_key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_dicts_by_key(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 609:
#
# ===== SYNTAX =====
#   def sort_last_names(names)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_last_names(names)): the inner sort_last_names() executes FIRST, then print() outputs the result
#   sort_last_names(names): looks up variable 'names', then runs the function body
#   print(sort_last_names(?)): the inner sort_last_names() executes FIRST, then print() outputs the result
#   sort_last_names(names): looks up variable 'names', then runs the function body
#   sort_last_names(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_last_names(names) — stored for later
#   2. Assign to names
#   3. Run print(sort_last_names(...))
#       calls sort_last_names with: names=names
#         3.1 Return from function
#   4. Define def sort_last_names(names) — stored for later
#   5. Define def sort_last_names(names) — stored for later
#   6. Assign to names
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_last_names(...))
#       calls sort_last_names with: names=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_last_names, sort_last_names, sort_last_names are just stored
#   Execution starts at the non-def lines below:
#      names = ... (stored in memory)
#      print(sort_last_names(names)) → function runs first, then print shows the result
#      names = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_last_names(?)) → function runs first, then print shows the result

"""

"""
Problem 610:
#
# ===== SYNTAX =====
#   def sort_ignore_case(strings)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   sort_ignore_case(mixed_case_strings): looks up variable 'mixed_case_strings', then runs the function body
#   sort_ignore_case(strings): looks up variable 'strings', then runs the function body
#   print(sort_ignore_case(?)): the inner sort_ignore_case() executes FIRST, then print() outputs the result
#   sort_ignore_case(?): resolves the value, then runs the function body
#   ord(...) + 32: first get ord(...), then get 32, then compute ord(...) + 32
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_ignore_case(strings) — stored for later
#   2. Assign to mixed_case_strings
#   3. Assign to sorted_strings
#   4. Run print(sorted_strings)
#   5. Define def sort_ignore_case(strings) — stored for later
#   6. Define def sort_ignore_case(strings) — stored for later
#   7. Assign to strings
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_ignore_case(...))
#       calls sort_ignore_case with: strings=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_ignore_case, sort_ignore_case, sort_ignore_case are just stored
#   Execution starts at the non-def lines below:
#      mixed_case_strings = ... (stored in memory)
#      sorted_strings = ... (stored in memory)
#      print(sorted_strings)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_ignore_case(?)) → function runs first, then print shows the result

"""

"""
Problem 611:
#
# ===== SYNTAX =====
#   def list_floats(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(list_floats(lst)): the inner list_floats() executes FIRST, then print() outputs the result
#   list_floats(lst): looks up variable 'lst', then runs the function body
#   print(list_floats(?)): the inner list_floats() executes FIRST, then print() outputs the result
#   list_floats(lst): looks up variable 'lst', then runs the function body
#   list_floats(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_floats(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_floats(...))
#       calls list_floats with: lst=lst
#         3.1 Return from function
#   4. Define def list_floats(lst) — stored for later
#   5. Define def list_floats(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(list_floats(...))
#       calls list_floats with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_floats, list_floats, list_floats are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_floats(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_floats(?)) → function runs first, then print shows the result

"""

"""
Problem 612:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         3.1 Return from function
#   4. Define def lst_words(lst) — stored for later
#   5. Define def lst_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x.lower(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 613:
#
# ===== SYNTAX =====
#   def sort_people_by_age(people)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_people_by_age(people): looks up variable 'people', then runs the function body
#   sort_people_by_age(people): looks up variable 'people', then runs the function body
#   print(sort_people_by_age(?)): the inner sort_people_by_age() executes FIRST, then print() outputs the result
#   sort_people_by_age(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_people_by_age(people) — stored for later
#   2. Assign to people
#   3. Assign to sorted_people
#   4. Run print(sorted_people)
#   5. Define def sort_people_by_age(people) — stored for later
#   6. Define def sort_people_by_age(people) — stored for later
#   7. Assign to people
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_people_by_age(...))
#       calls sort_people_by_age with: people=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_people_by_age, sort_people_by_age, sort_people_by_age are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      sorted_people = ... (stored in memory)
#      print(sorted_people)
#      people = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_people_by_age(?)) → function runs first, then print shows the result

"""

"""
Problem 614:
#
# ===== SYNTAX =====
#   def lst_integers(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   str() — built-in function call
#   int() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   lst_integers(lst): looks up variable 'lst', then runs the function body
#   lst_integers(lst): looks up variable 'lst', then runs the function body
#   print(lst_integers(?)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_integers(lst) — stored for later
#   2. Assign to lst
#   3. Assign to sorted_list
#   4. Run print(sorted_list)
#   5. Define def lst_integers(lst) — stored for later
#   6. Define def lst_integers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_integers(...))
#       calls lst_integers with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_integers, lst_integers, lst_integers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sorted_list = ... (stored in memory)
#      print(sorted_list)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_integers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from str(...) each round

"""

"""
Problem 615:
#
# ===== SYNTAX =====
#   def lst_products(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   lst_products(products): looks up variable 'products', then runs the function body
#   lst_products(lst): looks up variable 'lst', then runs the function body
#   print(lst_products(?)): the inner lst_products() executes FIRST, then print() outputs the result
#   lst_products(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_products(lst) — stored for later
#   2. Assign to products
#   3. Assign to sorted_products
#   4. Run print(sorted_products)
#   5. Define def lst_products(lst) — stored for later
#   6. Define def lst_products(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_products(...))
#       calls lst_products with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_products, lst_products, lst_products are just stored
#   Execution starts at the non-def lines below:
#      products = ... (stored in memory)
#      sorted_products = ... (stored in memory)
#      print(sorted_products)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_products(?)) → function runs first, then print shows the result

"""

"""
Problem 616:
#
# ===== SYNTAX =====
#   def sort_dates(dates)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_dates(date_list): looks up variable 'date_list', then runs the function body
#   sort_dates(dates): looks up variable 'dates', then runs the function body
#   print(sort_dates(?)): the inner sort_dates() executes FIRST, then print() outputs the result
#   sort_dates(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_dates(dates) — stored for later
#   2. Assign to date_list
#   3. Assign to sorted_dates
#   4. Run print(sorted_dates)
#   5. Define def sort_dates(dates) — stored for later
#   6. Define def sort_dates(dates) — stored for later
#   7. Assign to dates
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_dates(...))
#       calls sort_dates with: dates=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_dates, sort_dates, sort_dates are just stored
#   Execution starts at the non-def lines below:
#      date_list = ... (stored in memory)
#      sorted_dates = ... (stored in memory)
#      print(sorted_dates)
#      dates = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_dates(?)) → function runs first, then print shows the result

"""

"""
Problem 617:
#
# ===== SYNTAX =====
#   def lst_tuples(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   lst_tuples(lst): looks up variable 'lst', then runs the function body
#   lst_tuples(lst): looks up variable 'lst', then runs the function body
#   print(lst_tuples(?)): the inner lst_tuples() executes FIRST, then print() outputs the result
#   lst_tuples(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_tuples(lst) — stored for later
#   2. Assign to lst
#   3. Assign to sorted_tuples
#   4. Run print(sorted_tuples)
#   5. Define def lst_tuples(lst) — stored for later
#   6. Define def lst_tuples(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_tuples(...))
#       calls lst_tuples with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_tuples, lst_tuples, lst_tuples are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sorted_tuples = ... (stored in memory)
#      print(sorted_tuples)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_tuples(?)) → function runs first, then print shows the result

"""

"""
Problem 618:
#
# ===== SYNTAX =====
#   def lst_names(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(lst_names(lst)): the inner lst_names() executes FIRST, then print() outputs the result
#   print(lst_names(lst)): the inner lst_names() executes FIRST, then print() outputs the result
#   lst_names(lst): looks up variable 'lst', then runs the function body
#   print(lst_names(?)): the inner lst_names() executes FIRST, then print() outputs the result
#   lst_names(lst): looks up variable 'lst', then runs the function body
#   lst_names(lst): looks up variable 'lst', then runs the function body
#   lst_names(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_names(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_names(...))
#       calls lst_names with: lst=lst
#         3.1 Return from function
#   4. Define def lst_names(lst) — stored for later
#   5. Assign to lst
#   6. Run print(lst_names(...))
#       calls lst_names with: lst=lst
#         6.1 Return from function
#   7. Define def lst_names(lst) — stored for later
#   8. Define def lst_names(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(lst_names(...))
#       calls lst_names with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_names, lst_names, lst_names, lst_names are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_names(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(lst_names(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_names(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from name each round

"""

"""
Problem 619:
#
# ===== SYNTAX =====
#   def sort_by_hire_date(employees)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_by_hire_date(employees): looks up variable 'employees', then runs the function body
#   sort_by_hire_date(employees): looks up variable 'employees', then runs the function body
#   print(sort_by_hire_date(?)): the inner sort_by_hire_date() executes FIRST, then print() outputs the result
#   sort_by_hire_date(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_hire_date(employees) — stored for later
#   2. Assign to employees
#   3. Assign to sorted_employees
#   4. Run print(sorted_employees)
#   5. Define def sort_by_hire_date(employees) — stored for later
#   6. Define def sort_by_hire_date(employees) — stored for later
#   7. Assign to employees
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_by_hire_date(...))
#       calls sort_by_hire_date with: employees=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_hire_date, sort_by_hire_date, sort_by_hire_date are just stored
#   Execution starts at the non-def lines below:
#      employees = ... (stored in memory)
#      sorted_employees = ... (stored in memory)
#      print(sorted_employees)
#      employees = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_hire_date(?)) → function runs first, then print shows the result

"""

"""
Problem 621:
#
# ===== SYNTAX =====
#   def frequency_first_letter(words)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(frequency_first_letter(words)): the inner frequency_first_letter() executes FIRST, then print() outputs the result
#   print(frequency_first_letter(words)): the inner frequency_first_letter() executes FIRST, then print() outputs the result
#   frequency_first_letter(words): looks up variable 'words', then runs the function body
#   print(frequency_first_letter(?)): the inner frequency_first_letter() executes FIRST, then print() outputs the result
#   frequency_first_letter(words): looks up variable 'words', then runs the function body
#   frequency_first_letter(words): looks up variable 'words', then runs the function body
#   frequency_first_letter(?): resolves the value, then runs the function body
#   first_letter in letter_count: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def frequency_first_letter(words) — stored for later
#   2. Assign to words
#   3. Run print(frequency_first_letter(...))
#       calls frequency_first_letter with: words=words
#         3.1 Return from function
#   4. Import Counter from collections
#   5. Define def frequency_first_letter(words) — stored for later
#   6. Assign to words
#   7. Run print(frequency_first_letter(...))
#       calls frequency_first_letter with: words=words
#         7.1 Return from function
#   8. Define def frequency_first_letter(words) — stored for later
#   9. Define def frequency_first_letter(words) — stored for later
#   10. Assign to words
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(frequency_first_letter(...))
#       calls frequency_first_letter with: words=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions frequency_first_letter, frequency_first_letter, frequency_first_letter, frequency_first_letter are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(frequency_first_letter(words)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      print(frequency_first_letter(words)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(frequency_first_letter(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 622:
#
# ===== SYNTAX =====
#   def lst_of_tuples(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   abs() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_of_tuples(lst)): the inner lst_of_tuples() executes FIRST, then print() outputs the result
#   lst_of_tuples(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_tuples(?)): the inner lst_of_tuples() executes FIRST, then print() outputs the result
#   lst_of_tuples(lst): looks up variable 'lst', then runs the function body
#   lst_of_tuples(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_of_tuples(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_of_tuples(...))
#       calls lst_of_tuples with: lst=lst
#         3.1 Return from function
#   4. Define def lst_of_tuples(lst) — stored for later
#   5. Define def lst_of_tuples(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_of_tuples(...))
#       calls lst_of_tuples with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_of_tuples, lst_of_tuples, lst_of_tuples are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_of_tuples(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_of_tuples(?)) → function runs first, then print shows the result

"""

"""
Problem 623:
#
# ===== SYNTAX =====
#   def reverse_order(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(reverse_order(lst)): the inner reverse_order() executes FIRST, then print() outputs the result
#   print(reverse_order(lst)): the inner reverse_order() executes FIRST, then print() outputs the result
#   reverse_order(lst): looks up variable 'lst', then runs the function body
#   print(reverse_order(?)): the inner reverse_order() executes FIRST, then print() outputs the result
#   reverse_order(lst): looks up variable 'lst', then runs the function body
#   reverse_order(lst): looks up variable 'lst', then runs the function body
#   reverse_order(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_order(lst) — stored for later
#   2. Assign to lst
#   3. Run print(reverse_order(...))
#       calls reverse_order with: lst=lst
#         3.1 Return from function
#   4. Define def reverse_order(lst) — stored for later
#   5. Assign to lst
#   6. Run print(reverse_order(...))
#       calls reverse_order with: lst=lst
#         6.1 Return from function
#   7. Define def reverse_order(lst) — stored for later
#   8. Define def reverse_order(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(reverse_order(...))
#       calls reverse_order with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_order, reverse_order, reverse_order, reverse_order are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(reverse_order(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(reverse_order(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_order(?)) → function runs first, then print shows the result

"""

"""
Problem 624:
#
# ===== SYNTAX =====
#   def temperture(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   print(temperture(lst)): the inner temperture() executes FIRST, then print() outputs the result
#   temperture(lst): looks up variable 'lst', then runs the function body
#   print(temperture(?)): the inner temperture() executes FIRST, then print() outputs the result
#   temperture(lst): looks up variable 'lst', then runs the function body
#   temperture(?): resolves the value, then runs the function body
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? * celsius: first get ?, then get celsius, then compute ? * celsius
#   9 / 5: first get 9, then get 5, then compute 9 / 5
#
# ===== EXECUTION ORDER =====
#   1. Define def temperture(lst) — stored for later
#   2. Assign to lst
#   3. Run print(temperture(...))
#       calls temperture with: lst=lst
#         3.1 Return from function
#   4. Define def temperture(lst) — stored for later
#   5. Define def temperture(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(temperture(...))
#       calls temperture with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions temperture, temperture, temperture are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(temperture(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(temperture(?)) → function runs first, then print shows the result

"""

"""
Problem 625:
#
# ===== SYNTAX =====
#   def sort_scores(scores)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_scores(scores): looks up variable 'scores', then runs the function body
#   sort_scores(scores): looks up variable 'scores', then runs the function body
#   print(sort_scores(?)): the inner sort_scores() executes FIRST, then print() outputs the result
#   sort_scores(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_scores(scores) — stored for later
#   2. Assign to scores
#   3. Assign to sorted_scores
#   4. Run print(sorted_scores)
#   5. Define def sort_scores(scores) — stored for later
#   6. Define def sort_scores(scores) — stored for later
#   7. Assign to scores
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_scores(...))
#       calls sort_scores with: scores=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_scores, sort_scores, sort_scores are just stored
#   Execution starts at the non-def lines below:
#      scores = ... (stored in memory)
#      sorted_scores = ... (stored in memory)
#      print(sorted_scores)
#      scores = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_scores(?)) → function runs first, then print shows the result

"""

"""
Problem 626:
#
# ===== SYNTAX =====
#   def sort_emails(emails)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_emails(emails): looks up variable 'emails', then runs the function body
#   sort_emails(emails): looks up variable 'emails', then runs the function body
#   print(sort_emails(?)): the inner sort_emails() executes FIRST, then print() outputs the result
#   sort_emails(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_emails(emails) — stored for later
#   2. Assign to emails
#   3. Assign to sorted_emails
#   4. Run print(sorted_emails)
#   5. Define def sort_emails(emails) — stored for later
#   6. Define def sort_emails(emails) — stored for later
#   7. Assign to emails
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_emails(...))
#       calls sort_emails with: emails=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_emails, sort_emails, sort_emails are just stored
#   Execution starts at the non-def lines below:
#      emails = ... (stored in memory)
#      sorted_emails = ... (stored in memory)
#      print(sorted_emails)
#      emails = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_emails(?)) → function runs first, then print shows the result

"""

"""
Problem 627:
#
# ===== SYNTAX =====
#   def sort_cities_by_population(cities)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_cities_by_population(cities): looks up variable 'cities', then runs the function body
#   sort_cities_by_population(cities): looks up variable 'cities', then runs the function body
#   print(sort_cities_by_population(?)): the inner sort_cities_by_population() executes FIRST, then print() outputs the result
#   sort_cities_by_population(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_cities_by_population(cities) — stored for later
#   2. Assign to cities
#   3. Assign to sorted_cities
#   4. Run print(sorted_cities)
#   5. Define def sort_cities_by_population(cities) — stored for later
#   6. Define def sort_cities_by_population(cities) — stored for later
#   7. Assign to cities
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_cities_by_population(...))
#       calls sort_cities_by_population with: cities=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_cities_by_population, sort_cities_by_population, sort_cities_by_population are just stored
#   Execution starts at the non-def lines below:
#      cities = ... (stored in memory)
#      sorted_cities = ... (stored in memory)
#      print(sorted_cities)
#      cities = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_cities_by_population(?)) → function runs first, then print shows the result

"""

"""
Problem 628:
#
# ===== SYNTAX =====
#   def last_letter(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(last_letter(lst)): the inner last_letter() executes FIRST, then print() outputs the result
#   last_letter(lst): looks up variable 'lst', then runs the function body
#   print(last_letter(?)): the inner last_letter() executes FIRST, then print() outputs the result
#   last_letter(lst): looks up variable 'lst', then runs the function body
#   last_letter(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def last_letter(lst) — stored for later
#   2. Assign to lst
#   3. Run print(last_letter(...))
#       calls last_letter with: lst=lst
#         3.1 Return from function
#   4. Define def last_letter(lst) — stored for later
#   5. Define def last_letter(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(last_letter(...))
#       calls last_letter with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions last_letter, last_letter, last_letter are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(last_letter(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(last_letter(?)) → function runs first, then print shows the result

"""

"""
Problem 629:
#
# ===== SYNTAX =====
#   def lst_of_numbers(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(lst_of_numbers(lst)): the inner lst_of_numbers() executes FIRST, then print() outputs the result
#   lst_of_numbers(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_numbers(?)): the inner lst_of_numbers() executes FIRST, then print() outputs the result
#   lst_of_numbers(lst): looks up variable 'lst', then runs the function body
#   lst_of_numbers(?): resolves the value, then runs the function body
#   number in frequency: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_of_numbers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_of_numbers(...))
#       calls lst_of_numbers with: lst=lst
#         3.1 Return from function
#   4. Define def lst_of_numbers(lst) — stored for later
#   5. Define def lst_of_numbers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_of_numbers(...))
#       calls lst_of_numbers with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_of_numbers, lst_of_numbers, lst_of_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_of_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_of_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 630:
#
# ===== SYNTAX =====
#   def sort_students_by_grades(students)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_students_by_grades(students)): the inner sort_students_by_grades() executes FIRST, then print() outputs the result
#   sort_students_by_grades(students): looks up variable 'students', then runs the function body
#   print(sort_students_by_grades(?)): the inner sort_students_by_grades() executes FIRST, then print() outputs the result
#   sort_students_by_grades(students): looks up variable 'students', then runs the function body
#   sort_students_by_grades(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_students_by_grades(students) — stored for later
#   2. Assign to students
#   3. Run print(sort_students_by_grades(...))
#       calls sort_students_by_grades with: students=students
#         3.1 Return from function
#   4. Define def sort_students_by_grades(students) — stored for later
#   5. Define def sort_students_by_grades(students) — stored for later
#   6. Assign to students
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_students_by_grades(...))
#       calls sort_students_by_grades with: students=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_students_by_grades, sort_students_by_grades, sort_students_by_grades are just stored
#   Execution starts at the non-def lines below:
#      students = ... (stored in memory)
#      print(sort_students_by_grades(students)) → function runs first, then print shows the result
#      students = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_students_by_grades(?)) → function runs first, then print shows the result

"""

"""
Problem 631:
#
# ===== SYNTAX =====
#   def sort_books_by_year(books)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_books_by_year(books)): the inner sort_books_by_year() executes FIRST, then print() outputs the result
#   sort_books_by_year(books): looks up variable 'books', then runs the function body
#   print(sort_books_by_year(?)): the inner sort_books_by_year() executes FIRST, then print() outputs the result
#   sort_books_by_year(books): looks up variable 'books', then runs the function body
#   sort_books_by_year(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_books_by_year(books) — stored for later
#   2. Assign to books
#   3. Run print(sort_books_by_year(...))
#       calls sort_books_by_year with: books=books
#         3.1 Return from function
#   4. Define def sort_books_by_year(books) — stored for later
#   5. Define def sort_books_by_year(books) — stored for later
#   6. Assign to books
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_books_by_year(...))
#       calls sort_books_by_year with: books=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_books_by_year, sort_books_by_year, sort_books_by_year are just stored
#   Execution starts at the non-def lines below:
#      books = ... (stored in memory)
#      print(sort_books_by_year(books)) → function runs first, then print shows the result
#      books = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_books_by_year(?)) → function runs first, then print shows the result

"""

"""
Problem 632:
#
# ===== SYNTAX =====
#   def sort_files_by_extension(files)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_files_by_extension(files)): the inner sort_files_by_extension() executes FIRST, then print() outputs the result
#   sort_files_by_extension(files): looks up variable 'files', then runs the function body
#   print(sort_files_by_extension(?)): the inner sort_files_by_extension() executes FIRST, then print() outputs the result
#   sort_files_by_extension(files): looks up variable 'files', then runs the function body
#   sort_files_by_extension(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_files_by_extension(files) — stored for later
#   2. Assign to files
#   3. Run print(sort_files_by_extension(...))
#       calls sort_files_by_extension with: files=files
#         3.1 Return from function
#   4. Define def sort_files_by_extension(files) — stored for later
#   5. Define def sort_files_by_extension(files) — stored for later
#   6. Assign to files
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_files_by_extension(...))
#       calls sort_files_by_extension with: files=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_files_by_extension, sort_files_by_extension, sort_files_by_extension are just stored
#   Execution starts at the non-def lines below:
#      files = ... (stored in memory)
#      print(sort_files_by_extension(files)) → function runs first, then print shows the result
#      files = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_files_by_extension(?)) → function runs first, then print shows the result

"""

"""
Problem 633:
#
# ===== SYNTAX =====
#   def sort_movies_by_release_date(movies)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   sort_movies_by_release_date(movies): looks up variable 'movies', then runs the function body
#   sort_movies_by_release_date(movies): looks up variable 'movies', then runs the function body
#   print(sort_movies_by_release_date(?)): the inner sort_movies_by_release_date() executes FIRST, then print() outputs the result
#   sort_movies_by_release_date(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_movies_by_release_date(movies) — stored for later
#   2. Assign to movies
#   3. Assign to sorted_movies
#   4. For loop over sorted_movies
#       4.1 Run print(?)
#   5. Define def sort_movies_by_release_date(movies) — stored for later
#   6. Define def sort_movies_by_release_date(movies) — stored for later
#   7. Assign to movies
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_movies_by_release_date(...))
#       calls sort_movies_by_release_date with: movies=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_movies_by_release_date, sort_movies_by_release_date, sort_movies_by_release_date are just stored
#   Execution starts at the non-def lines below:
#      movies = ... (stored in memory)
#      sorted_movies = ... (stored in memory)
#      for loop runs its body once per item
#      movies = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_movies_by_release_date(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sorted_movies each round

"""

"""
Problem 634:
#
# ===== SYNTAX =====
#   def sort_sentences_by_unique_words(sentences)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   sort_sentences_by_unique_words(sentences): looks up variable 'sentences', then runs the function body
#   sort_sentences_by_unique_words(sentences): looks up variable 'sentences', then runs the function body
#   print(sort_sentences_by_unique_words(?)): the inner sort_sentences_by_unique_words() executes FIRST, then print() outputs the result
#   sort_sentences_by_unique_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_sentences_by_unique_words(sentences) — stored for later
#   2. Assign to sentences
#   3. Assign to sorted_sentences
#   4. For loop over sorted_sentences
#       4.1 Run print(sentence)
#   5. Define def sort_sentences_by_unique_words(sentences) — stored for later
#   6. Define def sort_sentences_by_unique_words(sentences) — stored for later
#   7. Assign to sentences
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_sentences_by_unique_words(...))
#       calls sort_sentences_by_unique_words with: sentences=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_sentences_by_unique_words, sort_sentences_by_unique_words, sort_sentences_by_unique_words are just stored
#   Execution starts at the non-def lines below:
#      sentences = ... (stored in memory)
#      sorted_sentences = ... (stored in memory)
#      for loop runs its body once per item
#      sentences = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_sentences_by_unique_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sorted_sentences each round

"""

"""
Problem 635:
#
# ===== SYNTAX =====
#   def get_keys_from_dictionary(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_keys_from_dictionary(example_dict): looks up variable 'example_dict', then runs the function body
#   get_keys_from_dictionary(dic): looks up variable 'dic', then runs the function body
#   print(get_keys_from_dictionary(25)): the inner get_keys_from_dictionary() executes FIRST, then print() outputs the result
#   get_keys_from_dictionary(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_keys_from_dictionary(dic) — stored for later
#   2. Assign to example_dict
#   3. Assign to keys
#   4. Run print(keys)
#   5. Define def get_keys_from_dictionary(dic) — stored for later
#   6. Define def get_keys_from_dictionary(dic) — stored for later
#   7. Assign to dic
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_keys_from_dictionary(...))
#       calls get_keys_from_dictionary with: dic=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_keys_from_dictionary, get_keys_from_dictionary, get_keys_from_dictionary are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      keys = ... (stored in memory)
#      print(keys)
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_keys_from_dictionary(25)) → function runs first, then print shows the result

"""

"""
Problem 636:
#
# ===== SYNTAX =====
#   def dic_keys(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(dic_keys(dic)): the inner dic_keys() executes FIRST, then print() outputs the result
#   dic_keys(dic): looks up variable 'dic', then runs the function body
#   print(dic_keys(25)): the inner dic_keys() executes FIRST, then print() outputs the result
#   dic_keys(dic): looks up variable 'dic', then runs the function body
#   dic_keys(25): gets the literal 25, then runs the function body
#   value > 10: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def dic_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(dic_keys(...))
#       calls dic_keys with: dic=dic
#         3.1 Return from function
#   4. Define def dic_keys(dic) — stored for later
#   5. Define def dic_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(dic_keys(...))
#       calls dic_keys with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions dic_keys, dic_keys, dic_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(dic_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(dic_keys(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 637:
#
# ===== SYNTAX =====
#   def print_fruit_keys(fruit_prices)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(print_fruit_keys(fruit_prices)): the inner print_fruit_keys() executes FIRST, then print() outputs the result
#   print_fruit_keys(fruit_prices): looks up variable 'fruit_prices', then runs the function body
#   print(print_fruit_keys(25)): the inner print_fruit_keys() executes FIRST, then print() outputs the result
#   print_fruit_keys(fruit_prices): looks up variable 'fruit_prices', then runs the function body
#   print_fruit_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_fruit_keys(fruit_prices) — stored for later
#   2. Assign to fruit_prices
#   3. Run print(print_fruit_keys(...))
#       calls print_fruit_keys with: fruit_prices=fruit_prices
#         3.1 Return from function
#   4. Define def print_fruit_keys(fruit_prices) — stored for later
#   5. Define def print_fruit_keys(fruit_prices) — stored for later
#   6. Assign to fruit_prices
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_fruit_keys(...))
#       calls print_fruit_keys with: fruit_prices=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_fruit_keys, print_fruit_keys, print_fruit_keys are just stored
#   Execution starts at the non-def lines below:
#      fruit_prices = ... (stored in memory)
#      print(print_fruit_keys(fruit_prices)) → function runs first, then print shows the result
#      fruit_prices = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_fruit_keys(25)) → function runs first, then print shows the result

"""

"""
Problem 638:
#
# ===== SYNTAX =====
#   def max_key(dic)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(max_key(example_dict)): the inner max_key() executes FIRST, then print() outputs the result
#   max_key(dic): looks up variable 'dic', then runs the function body
#   print(max_key(?)): the inner max_key() executes FIRST, then print() outputs the result
#   max_key(example_dict): looks up variable 'example_dict', then runs the function body
#   max_key(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def max_key(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(max_key(...))
#       calls max_key with: dic=example_dict
#         3.1 Return from function
#   4. Define def max_key(dic) — stored for later
#   5. Define def max_key(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(max_key(...))
#       calls max_key with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_key, max_key, max_key are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(max_key(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_key(?)) → function runs first, then print shows the result

"""

"""
Problem 639:
#
# ===== SYNTAX =====
#   def name_age(dic)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(name_age(dic)): the inner name_age() executes FIRST, then print() outputs the result
#   name_age(dic): looks up variable 'dic', then runs the function body
#   print(name_age(?)): the inner name_age() executes FIRST, then print() outputs the result
#   name_age(dic): looks up variable 'dic', then runs the function body
#   name_age(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def name_age(dic) — stored for later
#   2. Assign to dic
#   3. Run print(name_age(...))
#       calls name_age with: dic=dic
#         3.1 Return from function
#   4. Define def name_age(dic) — stored for later
#   5. Define def name_age(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(name_age(...))
#       calls name_age with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions name_age, name_age, name_age are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(name_age(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(name_age(?)) → function runs first, then print shows the result

"""

"""
Problem 640:
#
# ===== SYNTAX =====
#   def concatenate_keys(dic)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(concatenate_keys(dic)): the inner concatenate_keys() executes FIRST, then print() outputs the result
#   concatenate_keys(dic): looks up variable 'dic', then runs the function body
#   print(concatenate_keys(?)): the inner concatenate_keys() executes FIRST, then print() outputs the result
#   concatenate_keys(dic): looks up variable 'dic', then runs the function body
#   concatenate_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def concatenate_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(concatenate_keys(...))
#       calls concatenate_keys with: dic=dic
#         3.1 Return from function
#   4. Define def concatenate_keys(dic) — stored for later
#   5. Define def concatenate_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(concatenate_keys(...))
#       calls concatenate_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions concatenate_keys, concatenate_keys, concatenate_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(concatenate_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(concatenate_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round

"""

"""
Problem 641:
#
# ===== SYNTAX =====
#   def print_countries_and_capitals(dic)
#   (no return — the function prints directly instead of returning)
#   print_countries_and_capitals() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_countries_and_capitals(countries_capitals): looks up variable 'countries_capitals', then runs the function body
#   print_countries_and_capitals(dic): looks up variable 'dic', then runs the function body
#   print(print_countries_and_capitals(25)): the inner print_countries_and_capitals() executes FIRST, then print() outputs the result
#   print_countries_and_capitals(countries_capitals): looks up variable 'countries_capitals', then runs the function body
#   print_countries_and_capitals(countries_capitals): looks up variable 'countries_capitals', then runs the function body
#   print_countries_and_capitals(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_countries_and_capitals(dic) — stored for later
#   2. Assign to countries_capitals
#   3. Run print_countries_and_capitals(countries_capitals)
#       with: dic=countries_capitals
#         3.1 For loop over dic.items(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_countries_and_capitals(...))
#       calls print_countries_and_capitals with: dic=25
#         9.1 For loop over dic.items(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_countries_and_capitals, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      countries_capitals = ... (stored in memory)
#      print_countries_and_capitals(countries_capitals) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_countries_and_capitals(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round

"""

"""
Problem 642:
#
# ===== SYNTAX =====
#   def count_key(dic)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_key(dic)): the inner count_key() executes FIRST, then print() outputs the result
#   count_key(dic): looks up variable 'dic', then runs the function body
#   print(count_key(?)): the inner count_key() executes FIRST, then print() outputs the result
#   count_key(dic): looks up variable 'dic', then runs the function body
#   count_key(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_key(dic) — stored for later
#   2. Assign to dic
#   3. Run print(count_key(...))
#       calls count_key with: dic=dic
#         3.1 Return from function
#   4. Define def count_key(dic) — stored for later
#   5. Define def count_key(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_key(...))
#       calls count_key with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_key, count_key, count_key are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(count_key(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_key(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round

"""

"""
Problem 643:
#
# ===== SYNTAX =====
#   def count_key(dic, letter)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_key(dic, 'a')): the inner count_key() executes FIRST, then print() outputs the result
#   count_key(dic, letter): looks up variable 'dic', looks up variable 'letter', then runs the function body
#   print(count_key(?, 25)): the inner count_key() executes FIRST, then print() outputs the result
#   count_key(dic, 'a'): looks up variable 'dic', gets the literal 'a', then runs the function body
#   count_key(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_key(dic, letter) — stored for later
#   2. Assign to dic
#   3. Run print(count_key(...))
#       calls count_key with: dic=dic, letter='a'
#         3.1 Return from function
#   4. Define def count_key(dic, letter) — stored for later
#   5. Define def count_key(dic, letter) — stored for later
#   6. Assign to dic
#   7. Assign to letter
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_key(...))
#       calls count_key with: dic=[4, 1, 8, 6, 3], letter=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_key, count_key, count_key are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(count_key(dic, 'a')) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      letter = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_key(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 644:
#
# ===== SYNTAX =====
#   def sorted_students_grades(dic)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sorted_students_grades(students_grades)): the inner sorted_students_grades() executes FIRST, then print() outputs the result
#   sorted_students_grades(dic): looks up variable 'dic', then runs the function body
#   print(sorted_students_grades(?)): the inner sorted_students_grades() executes FIRST, then print() outputs the result
#   sorted_students_grades(students_grades): looks up variable 'students_grades', then runs the function body
#   sorted_students_grades(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sorted_students_grades(dic) — stored for later
#   2. Assign to students_grades
#   3. Run print(sorted_students_grades(...))
#       calls sorted_students_grades with: dic=students_grades
#         3.1 Return from function
#   4. Define def sorted_students_grades(dic) — stored for later
#   5. Define def sorted_students_grades(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sorted_students_grades(...))
#       calls sorted_students_grades with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sorted_students_grades, sorted_students_grades, sorted_students_grades are just stored
#   Execution starts at the non-def lines below:
#      students_grades = ... (stored in memory)
#      print(sorted_students_grades(students_grades)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sorted_students_grades(?)) → function runs first, then print shows the result

"""

"""
Problem 645:
#
# ===== SYNTAX =====
#   def list_dictionary(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(list_dictionary(lst1, lst2)): the inner list_dictionary() executes FIRST, then print() outputs the result
#   list_dictionary(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(list_dictionary(?, ?)): the inner list_dictionary() executes FIRST, then print() outputs the result
#   list_dictionary(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   list_dictionary(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_dictionary(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(list_dictionary(...))
#       calls list_dictionary with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def list_dictionary(lst1, lst2) — stored for later
#   6. Define def list_dictionary(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(list_dictionary(...))
#       calls list_dictionary with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_dictionary, list_dictionary, list_dictionary are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(list_dictionary(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_dictionary(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 646:
#
# ===== SYNTAX =====
#   def reverse_keys(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(reverse_keys(colors)): the inner reverse_keys() executes FIRST, then print() outputs the result
#   reverse_keys(dic): looks up variable 'dic', then runs the function body
#   print(reverse_keys(25)): the inner reverse_keys() executes FIRST, then print() outputs the result
#   reverse_keys(colors): looks up variable 'colors', then runs the function body
#   reverse_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_keys(dic) — stored for later
#   2. Assign to colors
#   3. Run print(reverse_keys(...))
#       calls reverse_keys with: dic=colors
#         3.1 Return from function
#   4. Define def reverse_keys(dic) — stored for later
#   5. Define def reverse_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_keys(...))
#       calls reverse_keys with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_keys, reverse_keys, reverse_keys are just stored
#   Execution starts at the non-def lines below:
#      colors = ... (stored in memory)
#      print(reverse_keys(colors)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_keys(25)) → function runs first, then print shows the result

"""

"""
Problem 647:
#
# ===== SYNTAX =====
#   def first_key(dic)
#   return — exits the function and sends back a value
#   next() — built-in function call
#   iter() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(first_key(example_dict)): the inner first_key() executes FIRST, then print() outputs the result
#   first_key(dic): looks up variable 'dic', then runs the function body
#   first_key(dic): looks up variable 'dic', then runs the function body
#   print(first_key(25)): the inner first_key() executes FIRST, then print() outputs the result
#   first_key(example_dict): looks up variable 'example_dict', then runs the function body
#   first_key(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def first_key(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(first_key(...))
#       calls first_key with: dic=example_dict
#         3.1 Return from function
#   4. Define def first_key(dic) — stored for later
#   5. Assign to dic
#   6. Run first_key(dic)
#       with: dic=dic
#         6.1 Return from function
#   7. Define def first_key(dic) — stored for later
#   8. Define def first_key(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(first_key(...))
#       calls first_key with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions first_key, first_key, first_key, first_key are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(first_key(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      first_key(dic) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(first_key(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic each round

"""

"""
Problem 648:
#
# ===== SYNTAX =====
#   def shopping_cart_items(cart)
#   (no return — the function prints directly instead of returning)
#   shopping_cart_items() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   shopping_cart_items(cart): looks up variable 'cart', then runs the function body
#   shopping_cart_items(cart): looks up variable 'cart', then runs the function body
#   print(shopping_cart_items(?)): the inner shopping_cart_items() executes FIRST, then print() outputs the result
#   shopping_cart_items(cart): looks up variable 'cart', then runs the function body
#   shopping_cart_items(cart): looks up variable 'cart', then runs the function body
#   shopping_cart_items(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def shopping_cart_items(cart) — stored for later
#   2. Assign to cart
#   3. Run shopping_cart_items(cart)
#       with: cart=cart
#         3.1 For loop over cart.keys(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to cart
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(shopping_cart_items(...))
#       calls shopping_cart_items with: cart=[4, 1, 8, 6, 3]
#         9.1 For loop over cart.keys(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions shopping_cart_items, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      cart = ... (stored in memory)
#      shopping_cart_items(cart) → jumps into function, runs body, returns result → printed
#      cart = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(shopping_cart_items(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from cart.keys(...) each round

"""

"""
Problem 649:
#
# ===== SYNTAX =====
#   def check_key_exists(dic, key)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(check_key_exists(example_dict, key_to_check)): the inner check_key_exists() executes FIRST, then print() outputs the result
#   print(check_key_exists(example_dict, key_to_check)): the inner check_key_exists() executes FIRST, then print() outputs the result
#   check_key_exists(dic, key): looks up variable 'dic', looks up variable 'key', then runs the function body
#   print(check_key_exists(25, 'value')): the inner check_key_exists() executes FIRST, then print() outputs the result
#   check_key_exists(example_dict, key_to_check): looks up variable 'example_dict', looks up variable 'key_to_check', then runs the function body
#   check_key_exists(example_dict, key_to_check): looks up variable 'example_dict', looks up variable 'key_to_check', then runs the function body
#   check_key_exists(25, 'value'): gets the literal 25, gets the literal 'value', then runs the function body
#   key in dic: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_key_exists(dic, key) — stored for later
#   2. Assign to example_dict
#   3. Assign to key_to_check
#   4. Run print(check_key_exists(...))
#       calls check_key_exists with: dic=example_dict, key=key_to_check
#         4.1 Return from function
#   5. Assign to key_to_check
#   6. Run print(check_key_exists(...))
#       calls check_key_exists with: dic=example_dict, key=key_to_check
#         6.1 Return from function
#   7. Define def check_key_exists(dic, key) — stored for later
#   8. Define def check_key_exists(dic, key) — stored for later
#   9. Assign to dic
#   10. Assign to key
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(check_key_exists(...))
#       calls check_key_exists with: dic=25, key='value'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_key_exists, check_key_exists, check_key_exists are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      key_to_check = ... (stored in memory)
#      print(check_key_exists(example_dict, key_to_check)) → function runs first, then print shows the result
#      key_to_check = ... (stored in memory)
#      print(check_key_exists(example_dict, key_to_check)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_key_exists(25, 'value')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 650:
#
# ===== SYNTAX =====
#   def print_keys_greater_than_five(dic)
#   (no return — the function prints directly instead of returning)
#   print_keys_greater_than_five() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print_keys_greater_than_five(items_dict): looks up variable 'items_dict', then runs the function body
#   print_keys_greater_than_five(dic): looks up variable 'dic', then runs the function body
#   print(print_keys_greater_than_five(25)): the inner print_keys_greater_than_five() executes FIRST, then print() outputs the result
#   print_keys_greater_than_five(items_dict): looks up variable 'items_dict', then runs the function body
#   print_keys_greater_than_five(items_dict): looks up variable 'items_dict', then runs the function body
#   print_keys_greater_than_five(25): gets the literal 25, then runs the function body
#   quantity > 5: is this True or False? The answer picks which branch
#   quantity > 5: is this True or False? The answer picks which branch
#   quantity > 5: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_keys_greater_than_five(dic) — stored for later
#   2. Assign to items_dict
#   3. Run print_keys_greater_than_five(items_dict)
#       with: dic=items_dict
#         3.1 For loop over dic.items(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_keys_greater_than_five(...))
#       calls print_keys_greater_than_five with: dic=25
#         9.1 For loop over dic.items(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_keys_greater_than_five, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items_dict = ... (stored in memory)
#      print_keys_greater_than_five(items_dict) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_keys_greater_than_five(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 651:
#
# ===== SYNTAX =====
#   def returns_strings(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(returns_strings(example_dict)): the inner returns_strings() executes FIRST, then print() outputs the result
#   returns_strings(dic): looks up variable 'dic', then runs the function body
#   print(returns_strings(?)): the inner returns_strings() executes FIRST, then print() outputs the result
#   returns_strings(example_dict): looks up variable 'example_dict', then runs the function body
#   returns_strings(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def returns_strings(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(returns_strings(...))
#       calls returns_strings with: dic=example_dict
#         3.1 Return from function
#   4. Define def returns_strings(dic) — stored for later
#   5. Define def returns_strings(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(returns_strings(...))
#       calls returns_strings with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions returns_strings, returns_strings, returns_strings are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(returns_strings(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(returns_strings(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 652:
#
# ===== SYNTAX =====
#   def sports_by_popularity(sports_dict)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sports_by_popularity(sports_dict)): the inner sports_by_popularity() executes FIRST, then print() outputs the result
#   sports_by_popularity(sports_dict): looks up variable 'sports_dict', then runs the function body
#   print(sports_by_popularity(?)): the inner sports_by_popularity() executes FIRST, then print() outputs the result
#   sports_by_popularity(sports_dict): looks up variable 'sports_dict', then runs the function body
#   sports_by_popularity(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sports_by_popularity(sports_dict) — stored for later
#   2. Assign to sports_dict
#   3. Run print(sports_by_popularity(...))
#       calls sports_by_popularity with: sports_dict=sports_dict
#         3.1 Return from function
#   4. Define def sports_by_popularity(sports_dict) — stored for later
#   5. Define def sports_by_popularity(sports_dict) — stored for later
#   6. Assign to sports_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sports_by_popularity(...))
#       calls sports_by_popularity with: sports_dict=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sports_by_popularity, sports_by_popularity, sports_by_popularity are just stored
#   Execution starts at the non-def lines below:
#      sports_dict = ... (stored in memory)
#      print(sports_by_popularity(sports_dict)) → function runs first, then print shows the result
#      sports_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sports_by_popularity(?)) → function runs first, then print shows the result

"""

"""
Problem 653:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   main(dic): looks up variable 'dic', then runs the function body
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(25)): the inner main() executes FIRST, then print() outputs the result
#   main(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Assign to dic
#   3. Run main(dic)
#       with: dic=dic
#         3.1 Return from function
#   4. Assign to data
#   5. Assign to integer_keys
#   6. Run print(integer_keys)
#   7. Define def main(dic) — stored for later
#   8. Define def main(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      main(dic) → jumps into function, runs body, returns result → printed
#      data = ... (stored in memory)
#      integer_keys = ... (stored in memory)
#      print(integer_keys)
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 654:
#
# ===== SYNTAX =====
#   def books_authors(dic)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   books_authors() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   books_authors(dic): looks up variable 'dic', then runs the function body
#   books_authors(dic): looks up variable 'dic', then runs the function body
#   print(books_authors(25)): the inner books_authors() executes FIRST, then print() outputs the result
#   books_authors(dic): looks up variable 'dic', then runs the function body
#   books_authors(dic): looks up variable 'dic', then runs the function body
#   books_authors(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def books_authors(dic) — stored for later
#   2. Assign to dic
#   3. Run books_authors(dic)
#       with: dic=dic
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(books_authors(...))
#       calls books_authors with: dic=25
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions books_authors, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      books_authors(dic) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(books_authors(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 655:
#
# ===== SYNTAX =====
#   def palindromic_keys(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(palindromic_keys(example_dict)): the inner palindromic_keys() executes FIRST, then print() outputs the result
#   palindromic_keys(dic): looks up variable 'dic', then runs the function body
#   print(palindromic_keys(?)): the inner palindromic_keys() executes FIRST, then print() outputs the result
#   palindromic_keys(example_dict): looks up variable 'example_dict', then runs the function body
#   palindromic_keys(?): resolves the value, then runs the function body
#   key == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def palindromic_keys(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(palindromic_keys(...))
#       calls palindromic_keys with: dic=example_dict
#         3.1 Return from function
#   4. Define def palindromic_keys(dic) — stored for later
#   5. Define def palindromic_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(palindromic_keys(...))
#       calls palindromic_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions palindromic_keys, palindromic_keys, palindromic_keys are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(palindromic_keys(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(palindromic_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 656:
#
# ===== SYNTAX =====
#   def pet_owners(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   print(pet_owners(dic)): the inner pet_owners() executes FIRST, then print() outputs the result
#   print(pet_owners(dic)): the inner pet_owners() executes FIRST, then print() outputs the result
#   pet_owners(dic): looks up variable 'dic', then runs the function body
#   print(pet_owners(?)): the inner pet_owners() executes FIRST, then print() outputs the result
#   pet_owners(dic): looks up variable 'dic', then runs the function body
#   pet_owners(dic): looks up variable 'dic', then runs the function body
#   pet_owners(?): resolves the value, then runs the function body
#   'o' in key: is this True or False? The answer picks which branch
#   char == 'o': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def pet_owners(dic) — stored for later
#   2. Assign to dic
#   3. Run print(pet_owners(...))
#       calls pet_owners with: dic=dic
#         3.1 Return from function
#   4. Define def pet_owners(dic) — stored for later
#   5. Assign to dic
#   6. Run print(pet_owners(...))
#       calls pet_owners with: dic=dic
#         6.1 Return from function
#   7. Define def pet_owners(dic) — stored for later
#   8. Define def pet_owners(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(pet_owners(...))
#       calls pet_owners with: dic=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions pet_owners, pet_owners, pet_owners, pet_owners are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(pet_owners(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(pet_owners(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(pet_owners(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 657:
#
# ===== SYNTAX =====
#   def count_tuple_keys(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_tuple_keys(dic)): the inner count_tuple_keys() executes FIRST, then print() outputs the result
#   count_tuple_keys(dic): looks up variable 'dic', then runs the function body
#   print(count_tuple_keys(?)): the inner count_tuple_keys() executes FIRST, then print() outputs the result
#   count_tuple_keys(dic): looks up variable 'dic', then runs the function body
#   count_tuple_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_tuple_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(count_tuple_keys(...))
#       calls count_tuple_keys with: dic=dic
#         3.1 Return from function
#   4. Define def count_tuple_keys(dic) — stored for later
#   5. Define def count_tuple_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_tuple_keys(...))
#       calls count_tuple_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_tuple_keys, count_tuple_keys, count_tuple_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(count_tuple_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_tuple_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 658:
#
# ===== SYNTAX =====
#   def movies(dic)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(movies(dic)): the inner movies() executes FIRST, then print() outputs the result
#   movies(dic): looks up variable 'dic', then runs the function body
#   print(movies(?)): the inner movies() executes FIRST, then print() outputs the result
#   movies(dic): looks up variable 'dic', then runs the function body
#   movies(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def movies(dic) — stored for later
#   2. Assign to dic
#   3. Run print(movies(...))
#       calls movies with: dic=dic
#         3.1 Return from function
#   4. Define def movies(dic) — stored for later
#   5. Define def movies(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(movies(...))
#       calls movies with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions movies, movies, movies are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(movies(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(movies(?)) → function runs first, then print shows the result

"""

"""
Problem 659:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   main(dic): looks up variable 'dic', then runs the function body
#   print(keys_with_list_values(dic)): the inner keys_with_list_values() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(25)): the inner main() executes FIRST, then print() outputs the result
#   keys_with_list_values(dic): looks up variable 'dic', then runs the function body
#   main(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Assign to dic
#   3. Run main(dic)
#       with: dic=dic
#         3.1 Return from function
#   4. Define def keys_with_list_values(dic) — stored for later
#   5. Assign to dic
#   6. Run print(keys_with_list_values(...))
#       calls keys_with_list_values with: dic=dic
#         6.1 Assign to result
#         6.2 For loop over dic.items(...)
#         6.3 Return from function
#   7. Define def main(dic) — stored for later
#   8. Define def main(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, keys_with_list_values, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      main(dic) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      print(keys_with_list_values(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 660:
#
# ===== SYNTAX =====
#   def print_car_brands(dic)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_car_brands() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_car_brands(car_brands): looks up variable 'car_brands', then runs the function body
#   print_car_brands(dic): looks up variable 'dic', then runs the function body
#   print(print_car_brands(25)): the inner print_car_brands() executes FIRST, then print() outputs the result
#   print_car_brands(car_brands): looks up variable 'car_brands', then runs the function body
#   print_car_brands(car_brands_2): looks up variable 'car_brands_2', then runs the function body
#   print_car_brands(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_car_brands(dic) — stored for later
#   2. Assign to car_brands
#   3. Run print_car_brands(car_brands)
#       with: dic=car_brands
#         3.1 Run print('Car Brands:')
#         3.2 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_car_brands(...))
#       calls print_car_brands with: dic=25
#         9.1 Run print('Car Brands:')
#         9.2 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_car_brands, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      car_brands = ... (stored in memory)
#      print_car_brands(car_brands) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_car_brands(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 661:
#
# ===== SYNTAX =====
#   def second_key(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   len() — built-in function call
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(second_key(dic)): the inner second_key() executes FIRST, then print() outputs the result
#   print(second_key(dic)): the inner second_key() executes FIRST, then print() outputs the result
#   second_key(dic): looks up variable 'dic', then runs the function body
#   print(second_key(25)): the inner second_key() executes FIRST, then print() outputs the result
#   second_key(dic): looks up variable 'dic', then runs the function body
#   second_key(dic): looks up variable 'dic', then runs the function body
#   second_key(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def second_key(dic) — stored for later
#   2. Assign to dic
#   3. Run print(second_key(...))
#       calls second_key with: dic=dic
#         3.1 Return from function
#   4. Define def second_key(dic) — stored for later
#   5. Assign to dic
#   6. Run print(second_key(...))
#       calls second_key with: dic=dic
#         6.1 Return from function
#   7. Define def second_key(dic) — stored for later
#   8. Define def second_key(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(second_key(...))
#       calls second_key with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_key, second_key, second_key, second_key are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(second_key(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(second_key(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_key(25)) → function runs first, then print shows the result

"""

"""
Problem 662:
#
# ===== SYNTAX =====
#   def temperature(dic)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(temperature(dic)): the inner temperature() executes FIRST, then print() outputs the result
#   temperature(dic): looks up variable 'dic', then runs the function body
#   print(temperature(?)): the inner temperature() executes FIRST, then print() outputs the result
#   temperature(dic): looks up variable 'dic', then runs the function body
#   temperature(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def temperature(dic) — stored for later
#   2. Assign to dic
#   3. Run print(temperature(...))
#       calls temperature with: dic=dic
#         3.1 Return from function
#   4. Define def temperature(dic) — stored for later
#   5. Define def temperature(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(temperature(...))
#       calls temperature with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions temperature, temperature, temperature are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(temperature(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(temperature(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round

"""

"""
Problem 663:
#
# ===== SYNTAX =====
#   def keys_in_list(dic, given_list)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(keys_in_list(dic, given_list)): the inner keys_in_list() executes FIRST, then print() outputs the result
#   keys_in_list(dic, given_list): looks up variable 'dic', looks up variable 'given_list', then runs the function body
#   print(keys_in_list(?, ?)): the inner keys_in_list() executes FIRST, then print() outputs the result
#   keys_in_list(dic, given_list): looks up variable 'dic', looks up variable 'given_list', then runs the function body
#   keys_in_list(?, ?): resolves the value, resolves the value, then runs the function body
#   key in given_list: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def keys_in_list(dic, given_list) — stored for later
#   2. Assign to dic
#   3. Assign to given_list
#   4. Run print(keys_in_list(...))
#       calls keys_in_list with: dic=dic, given_list=given_list
#         4.1 Return from function
#   5. Define def keys_in_list(dic, given_list) — stored for later
#   6. Define def keys_in_list(dic, given_list) — stored for later
#   7. Assign to dic
#   8. Assign to given_list
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(keys_in_list(...))
#       calls keys_in_list with: dic=[4, 1, 8, 6, 3], given_list=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions keys_in_list, keys_in_list, keys_in_list are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      given_list = ... (stored in memory)
#      print(keys_in_list(dic, given_list)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      given_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(keys_in_list(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 664:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   price < 100: is this True or False? The answer picks which branch
#   price < 100: is this True or False? The answer picks which branch
#   price < 100: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to devices
#   2. For loop over devices.items(...)
#       2.1 If statement (one branch runs)
#   3. Define def main() — stored for later
#   4. Run main()
#         4.1 Assign to devices
#         4.2 Assign to affordable
#         4.3 For loop over affordable
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to devices
#   8. Assign to affordable
#   9. For loop over affordable
#       9.1 Run print(device)
#   10. Assign to devices
#   11. Assign to affordable
#   12. For loop over affordable
#       12.1 Run print(device)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      devices = ... (stored in memory)
#      for loop runs its body once per item
#      main() → jumps into function, runs body, returns result → printed
#      devices = ... (stored in memory)
#      affordable = ... (stored in memory)
#      for loop runs its body once per item
#      devices = ... (stored in memory)
#      affordable = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from devices.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 665:
#
# ===== SYNTAX =====
#   def keys_with_values_greater_than_10(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(keys_with_values_greater_than_10(dic)): the inner keys_with_values_greater_than_10() executes FIRST, then print() outputs the result
#   print(get_dict_keys(my_dict)): the inner get_dict_keys() executes FIRST, then print() outputs the result
#   keys_with_values_greater_than_10(dic): looks up variable 'dic', then runs the function body
#   print(keys_with_values_greater_than_10(?)): the inner keys_with_values_greater_than_10() executes FIRST, then print() outputs the result
#   keys_with_values_greater_than_10(dic): looks up variable 'dic', then runs the function body
#   get_dict_keys(my_dict): looks up variable 'my_dict', then runs the function body
#   keys_with_values_greater_than_10(?): resolves the value, then runs the function body
#   ? > 10: is this True or False? The answer picks which branch
#   value > 10: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def keys_with_values_greater_than_10(dic) — stored for later
#   2. Assign to dic
#   3. Run print(keys_with_values_greater_than_10(...))
#       calls keys_with_values_greater_than_10 with: dic=dic
#         3.1 Return from function
#   4. Define def get_dict_keys(dic) — stored for later
#   5. Assign to my_dict
#   6. Run print(get_dict_keys(...))
#       calls get_dict_keys with: dic=my_dict
#         6.1 Assign to result
#         6.2 For loop over dic.items(...)
#         6.3 Return from function
#   7. Define def keys_with_values_greater_than_10(dic) — stored for later
#   8. Define def keys_with_values_greater_than_10(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(keys_with_values_greater_than_10(...))
#       calls keys_with_values_greater_than_10 with: dic=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions keys_with_values_greater_than_10, get_dict_keys, keys_with_values_greater_than_10, keys_with_values_greater_than_10 are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(keys_with_values_greater_than_10(dic)) → function runs first, then print shows the result
#      my_dict = ... (stored in memory)
#      print(get_dict_keys(my_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(keys_with_values_greater_than_10(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 666:
#
# ===== SYNTAX =====
#   def print_fruit_keys(fruits)
#   (no return — the function prints directly instead of returning)
#   print_fruit_keys() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_fruit_keys(fruits): looks up variable 'fruits', then runs the function body
#   print_fruit_keys(fruits): looks up variable 'fruits', then runs the function body
#   print(print_fruit_keys(?)): the inner print_fruit_keys() executes FIRST, then print() outputs the result
#   print_fruit_keys(fruits): looks up variable 'fruits', then runs the function body
#   print_fruit_keys(fruits): looks up variable 'fruits', then runs the function body
#   print_fruit_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_fruit_keys(fruits) — stored for later
#   2. Assign to fruits
#   3. Run print_fruit_keys(fruits)
#       with: fruits=fruits
#         3.1 For loop over fruits.keys(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to fruits
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_fruit_keys(...))
#       calls print_fruit_keys with: fruits=[4, 1, 8, 6, 3]
#         9.1 For loop over fruits.keys(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_fruit_keys, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      fruits = ... (stored in memory)
#      print_fruit_keys(fruits) → jumps into function, runs body, returns result → printed
#      fruits = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_fruit_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from fruits.keys(...) each round

"""

"""
Problem 667:
#
# ===== SYNTAX =====
#   def name_age(dic)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(name_age(dic)): the inner name_age() executes FIRST, then print() outputs the result
#   name_age(dic): looks up variable 'dic', then runs the function body
#   print(name_age(?)): the inner name_age() executes FIRST, then print() outputs the result
#   name_age(dic): looks up variable 'dic', then runs the function body
#   name_age(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def name_age(dic) — stored for later
#   2. Assign to dic
#   3. Run print(name_age(...))
#       calls name_age with: dic=dic
#         3.1 Return from function
#   4. Define def name_age(dic) — stored for later
#   5. Define def name_age(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(name_age(...))
#       calls name_age with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions name_age, name_age, name_age are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(name_age(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(name_age(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round

"""

"""
Problem 668:
#
# ===== SYNTAX =====
#   def concatenate(dic)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(concatenate(dic)): the inner concatenate() executes FIRST, then print() outputs the result
#   concatenate(dic): looks up variable 'dic', then runs the function body
#   print(concatenate(?)): the inner concatenate() executes FIRST, then print() outputs the result
#   concatenate(dic): looks up variable 'dic', then runs the function body
#   concatenate(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def concatenate(dic) — stored for later
#   2. Assign to dic
#   3. Run print(concatenate(...))
#       calls concatenate with: dic=dic
#         3.1 Return from function
#   4. Define def concatenate(dic) — stored for later
#   5. Define def concatenate(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(concatenate(...))
#       calls concatenate with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions concatenate, concatenate, concatenate are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(concatenate(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(concatenate(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round

"""

"""
Problem 670:
#
# ===== SYNTAX =====
#   def count_keys(dic)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_keys(dic)): the inner count_keys() executes FIRST, then print() outputs the result
#   count_keys(dic): looks up variable 'dic', then runs the function body
#   print(count_keys(25)): the inner count_keys() executes FIRST, then print() outputs the result
#   count_keys(dic): looks up variable 'dic', then runs the function body
#   count_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(count_keys(...))
#       calls count_keys with: dic=dic
#         3.1 Return from function
#   4. Define def count_keys(dic) — stored for later
#   5. Define def count_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_keys(...))
#       calls count_keys with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_keys, count_keys, count_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(count_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_keys(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic each round

"""

"""
Problem 671:
#
# ===== SYNTAX =====
#   def keys_starting_with(dic, letter)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(keys_starting_with(dic, 'a')): the inner keys_starting_with() executes FIRST, then print() outputs the result
#   keys_starting_with(dic, letter): looks up variable 'dic', looks up variable 'letter', then runs the function body
#   print(keys_starting_with(?, 25)): the inner keys_starting_with() executes FIRST, then print() outputs the result
#   keys_starting_with(dic, 'a'): looks up variable 'dic', gets the literal 'a', then runs the function body
#   keys_starting_with(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def keys_starting_with(dic, letter) — stored for later
#   2. Assign to dic
#   3. Run print(keys_starting_with(...))
#       calls keys_starting_with with: dic=dic, letter='a'
#         3.1 Return from function
#   4. Define def keys_starting_with(dic, letter) — stored for later
#   5. Define def keys_starting_with(dic, letter) — stored for later
#   6. Assign to dic
#   7. Assign to letter
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(keys_starting_with(...))
#       calls keys_starting_with with: dic=[4, 1, 8, 6, 3], letter=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions keys_starting_with, keys_starting_with, keys_starting_with are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(keys_starting_with(dic, 'a')) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      letter = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(keys_starting_with(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 672:
#
# ===== SYNTAX =====
#   def students_grades(dic)
#   sorted() — built-in function call
#   (no return — the function prints directly instead of returning)
#   students_grades() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   students_grades(dic): looks up variable 'dic', then runs the function body
#   students_grades(dic): looks up variable 'dic', then runs the function body
#   print(students_grades(?)): the inner students_grades() executes FIRST, then print() outputs the result
#   students_grades(dic): looks up variable 'dic', then runs the function body
#   students_grades(dic): looks up variable 'dic', then runs the function body
#   students_grades(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def students_grades(dic) — stored for later
#   2. Assign to dic
#   3. Run students_grades(dic)
#       with: dic=dic
#         3.1 Assign to sorted_keys
#         3.2 For loop over sorted_keys
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(students_grades(...))
#       calls students_grades with: dic=[4, 1, 8, 6, 3]
#         9.1 Assign to sorted_keys
#         9.2 For loop over sorted_keys
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions students_grades, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      students_grades(dic) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(students_grades(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sorted_keys each round

"""

"""
Problem 673:
#
# ===== SYNTAX =====
#   def two_lst(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(two_lst(lst1, lst2)): the inner two_lst() executes FIRST, then print() outputs the result
#   two_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(two_lst(?, ?)): the inner two_lst() executes FIRST, then print() outputs the result
#   two_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   two_lst(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def two_lst(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(two_lst(...))
#       calls two_lst with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def two_lst(lst1, lst2) — stored for later
#   6. Define def two_lst(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(two_lst(...))
#       calls two_lst with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions two_lst, two_lst, two_lst are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(two_lst(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(two_lst(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 674:
#
# ===== SYNTAX =====
#   def colors_hex(dic)
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   colors_hex() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   colors_hex(colors_dict): looks up variable 'colors_dict', then runs the function body
#   colors_hex(colors_dict): looks up variable 'colors_dict', then runs the function body
#   colors_hex(dic): looks up variable 'dic', then runs the function body
#   print(colors_hex(25)): the inner colors_hex() executes FIRST, then print() outputs the result
#   colors_hex(colors_dict): looks up variable 'colors_dict', then runs the function body
#   colors_hex(colors_dict): looks up variable 'colors_dict', then runs the function body
#   colors_hex(colors_dict): looks up variable 'colors_dict', then runs the function body
#   colors_hex(colors_dict): looks up variable 'colors_dict', then runs the function body
#   colors_hex(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def colors_hex(dic) — stored for later
#   2. Assign to colors_dict
#   3. Run colors_hex(colors_dict)
#       with: dic=colors_dict
#         3.1 Return from function
#   4. Define def colors_hex(dic) — stored for later
#   5. Assign to colors_dict
#   6. Run colors_hex(colors_dict)
#       with: dic=colors_dict
#         6.1 Return from function
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(colors_hex(...))
#       calls colors_hex with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions colors_hex, colors_hex, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      colors_dict = ... (stored in memory)
#      colors_hex(colors_dict) → jumps into function, runs body, returns result → printed
#      colors_dict = ... (stored in memory)
#      colors_hex(colors_dict) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(colors_hex(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from keys_in_reverse each round

"""

"""
Problem 675:
#
# ===== SYNTAX =====
#   def first_key(dic)
#   return — exits the function and sends back a value
#   next() — built-in function call
#   iter() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(first_key(example_dict)): the inner first_key() executes FIRST, then print() outputs the result
#   first_key(dic): looks up variable 'dic', then runs the function body
#   print(first_key(25)): the inner first_key() executes FIRST, then print() outputs the result
#   first_key(example_dict): looks up variable 'example_dict', then runs the function body
#   first_key(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def first_key(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(first_key(...))
#       calls first_key with: dic=example_dict
#         3.1 Return from function
#   4. Define def first_key(dic) — stored for later
#   5. Define def first_key(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(first_key(...))
#       calls first_key with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions first_key, first_key, first_key are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(first_key(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(first_key(25)) → function runs first, then print shows the result

"""

"""
Problem 676:
#
# ===== SYNTAX =====
#   def print_cart_items(cart)
#   (no return — the function prints directly instead of returning)
#   print_cart_items() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_cart_items(shopping_cart): looks up variable 'shopping_cart', then runs the function body
#   print_cart_items(cart): looks up variable 'cart', then runs the function body
#   print(print_cart_items(?)): the inner print_cart_items() executes FIRST, then print() outputs the result
#   print_cart_items(shopping_cart): looks up variable 'shopping_cart', then runs the function body
#   print_cart_items(shopping_cart): looks up variable 'shopping_cart', then runs the function body
#   print_cart_items(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_cart_items(cart) — stored for later
#   2. Assign to shopping_cart
#   3. Run print_cart_items(shopping_cart)
#       with: cart=shopping_cart
#         3.1 For loop over cart.keys(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to cart
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_cart_items(...))
#       calls print_cart_items with: cart=[4, 1, 8, 6, 3]
#         9.1 For loop over cart.keys(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_cart_items, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      shopping_cart = ... (stored in memory)
#      print_cart_items(shopping_cart) → jumps into function, runs body, returns result → printed
#      cart = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_cart_items(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from cart.keys(...) each round

"""

"""
Problem 678:
#
# ===== SYNTAX =====
#   def items_with_high_quantity(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   items_with_high_quantity(inventory): looks up variable 'inventory', then runs the function body
#   items_with_high_quantity(dic): looks up variable 'dic', then runs the function body
#   print(items_with_high_quantity(25)): the inner items_with_high_quantity() executes FIRST, then print() outputs the result
#   items_with_high_quantity(25): gets the literal 25, then runs the function body
#   quantity > 5: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def items_with_high_quantity(dic) — stored for later
#   2. Assign to inventory
#   3. Assign to high_quantity_items
#   4. Run print('Items with quantities greater than 5:')
#   5. For loop over high_quantity_items
#       5.1 Run print(item)
#   6. Define def items_with_high_quantity(dic) — stored for later
#   7. Define def items_with_high_quantity(dic) — stored for later
#   8. Assign to dic
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(items_with_high_quantity(...))
#       calls items_with_high_quantity with: dic=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions items_with_high_quantity, items_with_high_quantity, items_with_high_quantity are just stored
#   Execution starts at the non-def lines below:
#      inventory = ... (stored in memory)
#      high_quantity_items = ... (stored in memory)
#      print('Items with quantities greater than 5:')
#      for loop runs its body once per item
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(items_with_high_quantity(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from high_quantity_items each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 679:
#
# ===== SYNTAX =====
#   def string_keys(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   string_keys(example_dict): looks up variable 'example_dict', then runs the function body
#   string_keys(dic): looks up variable 'dic', then runs the function body
#   print(string_keys(?)): the inner string_keys() executes FIRST, then print() outputs the result
#   string_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_keys(dic) — stored for later
#   2. Assign to example_dict
#   3. Assign to string_keys_list
#   4. Run print('Keys that are strings:', string_keys_list)
#   5. Define def string_keys(dic) — stored for later
#   6. Define def string_keys(dic) — stored for later
#   7. Assign to dic
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(string_keys(...))
#       calls string_keys with: dic=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_keys, string_keys, string_keys are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      string_keys_list = ... (stored in memory)
#      print('Keys that are strings:', string_keys_list)
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 680:
#
# ===== SYNTAX =====
#   def sports_by_popularity(sports_dict)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sports_by_popularity(sports_dict)): the inner sports_by_popularity() executes FIRST, then print() outputs the result
#   sports_by_popularity(sports_dict): looks up variable 'sports_dict', then runs the function body
#   print(sports_by_popularity(?)): the inner sports_by_popularity() executes FIRST, then print() outputs the result
#   sports_by_popularity(sports_dict): looks up variable 'sports_dict', then runs the function body
#   sports_by_popularity(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sports_by_popularity(sports_dict) — stored for later
#   2. Assign to sports_dict
#   3. Run print(sports_by_popularity(...))
#       calls sports_by_popularity with: sports_dict=sports_dict
#         3.1 Return from function
#   4. Define def sports_by_popularity(sports_dict) — stored for later
#   5. Define def sports_by_popularity(sports_dict) — stored for later
#   6. Assign to sports_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sports_by_popularity(...))
#       calls sports_by_popularity with: sports_dict=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sports_by_popularity, sports_by_popularity, sports_by_popularity are just stored
#   Execution starts at the non-def lines below:
#      sports_dict = ... (stored in memory)
#      print(sports_by_popularity(sports_dict)) → function runs first, then print shows the result
#      sports_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sports_by_popularity(?)) → function runs first, then print shows the result

"""

"""
Problem 681:
#
# ===== SYNTAX =====
#   def retrieve_integer_keys(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(retrieve_integer_keys(example_dict)): the inner retrieve_integer_keys() executes FIRST, then print() outputs the result
#   retrieve_integer_keys(dic): looks up variable 'dic', then runs the function body
#   print(retrieve_integer_keys(?)): the inner retrieve_integer_keys() executes FIRST, then print() outputs the result
#   retrieve_integer_keys(example_dict): looks up variable 'example_dict', then runs the function body
#   retrieve_integer_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def retrieve_integer_keys(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(retrieve_integer_keys(...))
#       calls retrieve_integer_keys with: dic=example_dict
#         3.1 Return from function
#   4. Define def retrieve_integer_keys(dic) — stored for later
#   5. Define def retrieve_integer_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(retrieve_integer_keys(...))
#       calls retrieve_integer_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions retrieve_integer_keys, retrieve_integer_keys, retrieve_integer_keys are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(retrieve_integer_keys(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(retrieve_integer_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 682:
#
# ===== SYNTAX =====
#   def print_numbered_books(dic)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_numbered_books() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_numbered_books(books): looks up variable 'books', then runs the function body
#   print_numbered_books(dic): looks up variable 'dic', then runs the function body
#   print(print_numbered_books(25)): the inner print_numbered_books() executes FIRST, then print() outputs the result
#   print_numbered_books(books): looks up variable 'books', then runs the function body
#   print_numbered_books(books): looks up variable 'books', then runs the function body
#   print_numbered_books(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_numbered_books(dic) — stored for later
#   2. Assign to books
#   3. Run print_numbered_books(books)
#       with: dic=books
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_numbered_books(...))
#       calls print_numbered_books with: dic=25
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_numbered_books, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      books = ... (stored in memory)
#      print_numbered_books(books) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_numbered_books(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 683:
#
# ===== SYNTAX =====
#   def palindrome_keys(dic)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(palindrome_keys(example_dict)): the inner palindrome_keys() executes FIRST, then print() outputs the result
#   palindrome_keys(dic): looks up variable 'dic', then runs the function body
#   print(palindrome_keys(?)): the inner palindrome_keys() executes FIRST, then print() outputs the result
#   palindrome_keys(example_dict): looks up variable 'example_dict', then runs the function body
#   palindrome_keys(?): resolves the value, then runs the function body
#   str(...) == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def palindrome_keys(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(palindrome_keys(...))
#       calls palindrome_keys with: dic=example_dict
#         3.1 Return from function
#   4. Define def palindrome_keys(dic) — stored for later
#   5. Define def palindrome_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(palindrome_keys(...))
#       calls palindrome_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions palindrome_keys, palindrome_keys, palindrome_keys are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(palindrome_keys(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(palindrome_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 684:
#
# ===== SYNTAX =====
#   def pets_with_o(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(pets_with_o(pets_dict)): the inner pets_with_o() executes FIRST, then print() outputs the result
#   pets_with_o(dic): looks up variable 'dic', then runs the function body
#   print(pets_with_o(?)): the inner pets_with_o() executes FIRST, then print() outputs the result
#   pets_with_o(pets_dict): looks up variable 'pets_dict', then runs the function body
#   pets_with_o(?): resolves the value, then runs the function body
#   'o' in key: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def pets_with_o(dic) — stored for later
#   2. Assign to pets_dict
#   3. Run print(pets_with_o(...))
#       calls pets_with_o with: dic=pets_dict
#         3.1 Return from function
#   4. Define def pets_with_o(dic) — stored for later
#   5. Define def pets_with_o(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(pets_with_o(...))
#       calls pets_with_o with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions pets_with_o, pets_with_o, pets_with_o are just stored
#   Execution starts at the non-def lines below:
#      pets_dict = ... (stored in memory)
#      print(pets_with_o(pets_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(pets_with_o(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 685:
#
# ===== SYNTAX =====
#   def count_list_keys(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_list_keys(example_dict)): the inner count_list_keys() executes FIRST, then print() outputs the result
#   count_list_keys(dic): looks up variable 'dic', then runs the function body
#   print(count_list_keys(?)): the inner count_list_keys() executes FIRST, then print() outputs the result
#   count_list_keys(example_dict): looks up variable 'example_dict', then runs the function body
#   count_list_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_list_keys(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(count_list_keys(...))
#       calls count_list_keys with: dic=example_dict
#         3.1 Return from function
#   4. Define def count_list_keys(dic) — stored for later
#   5. Define def count_list_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_list_keys(...))
#       calls count_list_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_list_keys, count_list_keys, count_list_keys are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(count_list_keys(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_list_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 686:
#
# ===== SYNTAX =====
#   def sorted_movie_titles(movies)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sorted_movie_titles(movies_dict)): the inner sorted_movie_titles() executes FIRST, then print() outputs the result
#   sorted_movie_titles(movies): looks up variable 'movies', then runs the function body
#   print(sorted_movie_titles(?)): the inner sorted_movie_titles() executes FIRST, then print() outputs the result
#   sorted_movie_titles(movies_dict): looks up variable 'movies_dict', then runs the function body
#   sorted_movie_titles(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sorted_movie_titles(movies) — stored for later
#   2. Assign to movies_dict
#   3. Run print(sorted_movie_titles(...))
#       calls sorted_movie_titles with: movies=movies_dict
#         3.1 Return from function
#   4. Define def sorted_movie_titles(movies) — stored for later
#   5. Define def sorted_movie_titles(movies) — stored for later
#   6. Assign to movies
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sorted_movie_titles(...))
#       calls sorted_movie_titles with: movies=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sorted_movie_titles, sorted_movie_titles, sorted_movie_titles are just stored
#   Execution starts at the non-def lines below:
#      movies_dict = ... (stored in memory)
#      print(sorted_movie_titles(movies_dict)) → function runs first, then print shows the result
#      movies = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sorted_movie_titles(?)) → function runs first, then print shows the result

"""

"""
Problem 687:
#
# ===== SYNTAX =====
#   def keys_with_list_values(dic)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(keys_with_list_values(example_dict)): the inner keys_with_list_values() executes FIRST, then print() outputs the result
#   keys_with_list_values(dic): looks up variable 'dic', then runs the function body
#   print(keys_with_list_values(25)): the inner keys_with_list_values() executes FIRST, then print() outputs the result
#   keys_with_list_values(example_dict): looks up variable 'example_dict', then runs the function body
#   keys_with_list_values(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def keys_with_list_values(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(keys_with_list_values(...))
#       calls keys_with_list_values with: dic=example_dict
#         3.1 Return from function
#   4. Define def keys_with_list_values(dic) — stored for later
#   5. Define def keys_with_list_values(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(keys_with_list_values(...))
#       calls keys_with_list_values with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions keys_with_list_values, keys_with_list_values, keys_with_list_values are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(keys_with_list_values(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(keys_with_list_values(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 688:
#
# ===== SYNTAX =====
#   def print_car_brands(dic)
#   (no return — the function prints directly instead of returning)
#   print_car_brands() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_car_brands(car_brands): looks up variable 'car_brands', then runs the function body
#   print_car_brands(dic): looks up variable 'dic', then runs the function body
#   print(print_car_brands(?)): the inner print_car_brands() executes FIRST, then print() outputs the result
#   print_car_brands(car_brands): looks up variable 'car_brands', then runs the function body
#   print_car_brands(car_brands): looks up variable 'car_brands', then runs the function body
#   print_car_brands(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_car_brands(dic) — stored for later
#   2. Assign to car_brands
#   3. Run print_car_brands(car_brands)
#       with: dic=car_brands
#         3.1 For loop over dic.keys(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_car_brands(...))
#       calls print_car_brands with: dic=[4, 1, 8, 6, 3]
#         9.1 For loop over dic.keys(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_car_brands, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      car_brands = ... (stored in memory)
#      print_car_brands(car_brands) → jumps into function, runs body, returns result → printed
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_car_brands(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round

"""

"""
Problem 689:
#
# ===== SYNTAX =====
#   def second_key(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(second_key(example_dict)): the inner second_key() executes FIRST, then print() outputs the result
#   second_key(dic): looks up variable 'dic', then runs the function body
#   print(second_key(25)): the inner second_key() executes FIRST, then print() outputs the result
#   second_key(example_dict): looks up variable 'example_dict', then runs the function body
#   second_key(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def second_key(dic) — stored for later
#   2. Assign to example_dict
#   3. Run print(second_key(...))
#       calls second_key with: dic=example_dict
#         3.1 Return from function
#   4. Define def second_key(dic) — stored for later
#   5. Define def second_key(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(second_key(...))
#       calls second_key with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_key, second_key, second_key are just stored
#   Execution starts at the non-def lines below:
#      example_dict = ... (stored in memory)
#      print(second_key(example_dict)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_key(25)) → function runs first, then print shows the result

"""

"""
Problem 690:
#
# ===== SYNTAX =====
#   def uppercase_keys(dic)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(uppercase_keys(dic)): the inner uppercase_keys() executes FIRST, then print() outputs the result
#   uppercase_keys(dic): looks up variable 'dic', then runs the function body
#   print(uppercase_keys(?)): the inner uppercase_keys() executes FIRST, then print() outputs the result
#   uppercase_keys(dic): looks up variable 'dic', then runs the function body
#   uppercase_keys(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def uppercase_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(uppercase_keys(...))
#       calls uppercase_keys with: dic=dic
#         3.1 Return from function
#   4. Define def uppercase_keys(dic) — stored for later
#   5. Define def uppercase_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(uppercase_keys(...))
#       calls uppercase_keys with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions uppercase_keys, uppercase_keys, uppercase_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(uppercase_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(uppercase_keys(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round

"""

"""
Problem 691:
#
# ===== SYNTAX =====
#   def key_max(dic)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: > or is
#
# ===== EVALUATION ORDER =====
#   print(key_max(dic)): the inner key_max() executes FIRST, then print() outputs the result
#   print(key_max(dic)): the inner key_max() executes FIRST, then print() outputs the result
#   key_max(dic): looks up variable 'dic', then runs the function body
#   print(key_max(?)): the inner key_max() executes FIRST, then print() outputs the result
#   key_max(dic): looks up variable 'dic', then runs the function body
#   key_max(dic): looks up variable 'dic', then runs the function body
#   key_max(?): resolves the value, then runs the function body
#   max_key_lower  None: is this True or False? The answer picks which branch
#   key_lower > max_key_lower: is this True or False? The answer picks which branch
#   item > result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def key_max(dic) — stored for later
#   2. Assign to dic
#   3. Run print(key_max(...))
#       calls key_max with: dic=dic
#         3.1 Assign to result
#         3.2 For loop over dic
#         3.3 Return from function
#   4. Define def key_max(dic) — stored for later
#   5. Assign to dic
#   6. Run print(key_max(...))
#       calls key_max with: dic=dic
#         6.1 Assign to result
#         6.2 For loop over dic
#         6.3 Return from function
#   7. Define def key_max(dic) — stored for later
#   8. Define def key_max(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(key_max(...))
#       calls key_max with: dic=[4, 1, 8, 6, 3]
#         12.1 Assign to result
#         12.2 For loop over dic
#         12.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions key_max, key_max, key_max, key_max are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(key_max(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(key_max(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(key_max(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 692:
#
# ===== SYNTAX =====
#   def square_numbers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(square_numbers(numbers)): the inner square_numbers() executes FIRST, then print() outputs the result
#   square_numbers(lst): looks up variable 'lst', then runs the function body
#   print(square_numbers(?)): the inner square_numbers() executes FIRST, then print() outputs the result
#   square_numbers(numbers): looks up variable 'numbers', then runs the function body
#   square_numbers(?): resolves the value, then runs the function body
#   num ** 2: first get num, then get 2, then compute num ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_numbers(lst) — stored for later
#   2. Assign to numbers
#   3. Run print(square_numbers(...))
#       calls square_numbers with: lst=numbers
#         3.1 Import math
#         3.2 Return from function
#   4. Define def square_numbers(lst) — stored for later
#   5. Define def square_numbers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_numbers(...))
#       calls square_numbers with: lst=[1, 3, 5, 7]
#         9.1 Import math
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_numbers, square_numbers, square_numbers are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(square_numbers(numbers)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 693:
#
# ===== SYNTAX =====
#   def string_lengths(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   len() — built-in function call
#   map() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(string_lengths(strings)): the inner string_lengths() executes FIRST, then print() outputs the result
#   string_lengths(lst): looks up variable 'lst', then runs the function body
#   print(string_lengths(?)): the inner string_lengths() executes FIRST, then print() outputs the result
#   string_lengths(strings): looks up variable 'strings', then runs the function body
#   string_lengths(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_lengths(lst) — stored for later
#   2. Assign to strings
#   3. Run print(string_lengths(...))
#       calls string_lengths with: lst=strings
#         3.1 Return from function
#   4. Define def string_lengths(lst) — stored for later
#   5. Define def string_lengths(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(string_lengths(...))
#       calls string_lengths with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_lengths, string_lengths, string_lengths are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      print(string_lengths(strings)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_lengths(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round

"""

"""
Problem 694:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   operators: - <=
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#   ord(...) - 32: first get ord(...), then get 32, then compute ord(...) - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         3.1 Return from function
#   4. Define def lst_words(lst) — stored for later
#   5. Define def lst_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result

"""

"""
Problem 695:
#
# ===== SYNTAX =====
#   def lst_numbers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   round() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_numbers(lst)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   print(lst_numbers(?)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   lst_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_numbers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=lst
#         3.1 Return from function
#   4. Define def lst_numbers(lst) — stored for later
#   5. Define def lst_numbers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_numbers, lst_numbers, lst_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 696:
#
# ===== SYNTAX =====
#   def celsius_to_fahrenheit(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   celsius_to_fahrenheit(celsius_temps): looks up variable 'celsius_temps', then runs the function body
#   celsius_to_fahrenheit(lst): looks up variable 'lst', then runs the function body
#   print(celsius_to_fahrenheit(?)): the inner celsius_to_fahrenheit() executes FIRST, then print() outputs the result
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   celsius_to_fahrenheit(?): resolves the value, then runs the function body
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#
# ===== EXECUTION ORDER =====
#   1. Define def celsius_to_fahrenheit(lst) — stored for later
#   2. Assign to celsius_temps
#   3. Assign to fahrenheit_temps
#   4. Run print(fahrenheit_temps)
#   5. Define def celsius_to_fahrenheit(lst) — stored for later
#   6. Define def celsius_to_fahrenheit(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(celsius_to_fahrenheit(...))
#       calls celsius_to_fahrenheit with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions celsius_to_fahrenheit, celsius_to_fahrenheit, celsius_to_fahrenheit are just stored
#   Execution starts at the non-def lines below:
#      celsius_temps = ... (stored in memory)
#      fahrenheit_temps = ... (stored in memory)
#      print(fahrenheit_temps)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(celsius_to_fahrenheit(?)) → function runs first, then print shows the result

"""

"""
Problem 697:
#
# ===== SYNTAX =====
#   def numbers_to_ascii(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   chr() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   numbers_to_ascii(numbers): looks up variable 'numbers', then runs the function body
#   numbers_to_ascii(lst): looks up variable 'lst', then runs the function body
#   print(numbers_to_ascii(?)): the inner numbers_to_ascii() executes FIRST, then print() outputs the result
#   numbers_to_ascii(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def numbers_to_ascii(lst) — stored for later
#   2. Assign to numbers
#   3. Assign to ascii_characters
#   4. Run print(ascii_characters)
#   5. Define def numbers_to_ascii(lst) — stored for later
#   6. Define def numbers_to_ascii(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(numbers_to_ascii(...))
#       calls numbers_to_ascii with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions numbers_to_ascii, numbers_to_ascii, numbers_to_ascii are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      ascii_characters = ... (stored in memory)
#      print(ascii_characters)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(numbers_to_ascii(?)) → function runs first, then print shows the result

"""

"""
Problem 698:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         3.1 Assign to result
#         3.2 For loop over word
#         3.3 Return from function
#   4. Define def lst_words(lst) — stored for later
#   5. Define def lst_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         9.1 Assign to result
#         9.2 For loop over word
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round

"""

"""
Problem 700:
#
# ===== SYNTAX =====
#   def lst_integers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(lst_integers(lst)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(lst): looks up variable 'lst', then runs the function body
#   print(lst_integers(?)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(lst): looks up variable 'lst', then runs the function body
#   lst_integers(?): resolves the value, then runs the function body
#   number * 2: first get number, then get 2, then compute number * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_integers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_integers(...))
#       calls lst_integers with: lst=lst
#         3.1 Return from function
#   4. Define def lst_integers(lst) — stored for later
#   5. Define def lst_integers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_integers(...))
#       calls lst_integers with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_integers, lst_integers, lst_integers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_integers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_integers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 701:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         3.1 Return from function
#   4. Define def lst_words(lst) — stored for later
#   5. Define def lst_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result

"""

"""
Problem 702:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   int() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         3.1 Return from function
#   4. Define def lst_words(lst) — stored for later
#   5. Define def lst_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result

"""

"""
Problem 703:
#
# ===== SYNTAX =====
#   def lst_mixed_case(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   print(lst_mixed_case(lst)): the inner lst_mixed_case() executes FIRST, then print() outputs the result
#   lst_mixed_case(lst): looks up variable 'lst', then runs the function body
#   print(lst_mixed_case(?)): the inner lst_mixed_case() executes FIRST, then print() outputs the result
#   lst_mixed_case(lst): looks up variable 'lst', then runs the function body
#   lst_mixed_case(?): resolves the value, then runs the function body
#   ord(...) + 32: first get ord(...), then get 32, then compute ord(...) + 32
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_mixed_case(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_mixed_case(...))
#       calls lst_mixed_case with: lst=lst
#         3.1 Return from function
#   4. Define def lst_mixed_case(lst) — stored for later
#   5. Define def lst_mixed_case(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_mixed_case(...))
#       calls lst_mixed_case with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_mixed_case, lst_mixed_case, lst_mixed_case are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_mixed_case(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_mixed_case(?)) → function runs first, then print shows the result

"""

"""
Problem 704:
#
# ===== SYNTAX =====
#   def lst_integers(lst1, lst2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(lst_integers(lst1, lst2)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(lst_integers(?, ?)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   lst_integers(?, ?): resolves the value, resolves the value, then runs the function body
#   num1 * num2: first get num1, then get num2, then compute num1 * num2
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_integers(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(lst_integers(...))
#       calls lst_integers with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def lst_integers(lst1, lst2) — stored for later
#   6. Define def lst_integers(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(lst_integers(...))
#       calls lst_integers with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_integers, lst_integers, lst_integers are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(lst_integers(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_integers(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 705:
#
# ===== SYNTAX =====
#   def euclidean_distances(points)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   print(euclidean_distances(points)): the inner euclidean_distances() executes FIRST, then print() outputs the result
#   euclidean_distances(points): looks up variable 'points', then runs the function body
#   print(euclidean_distances(25)): the inner euclidean_distances() executes FIRST, then print() outputs the result
#   euclidean_distances(points): looks up variable 'points', then runs the function body
#   euclidean_distances(25): gets the literal 25, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def euclidean_distances(points) — stored for later
#   3. Assign to points
#   4. Run print(euclidean_distances(...))
#       calls euclidean_distances with: points=points
#         4.1 Return from function
#   5. Define def euclidean_distances(points) — stored for later
#   6. Define def euclidean_distances(points) — stored for later
#   7. Assign to points
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(euclidean_distances(...))
#       calls euclidean_distances with: points=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions euclidean_distances, euclidean_distances, euclidean_distances are just stored
#   Execution starts at the non-def lines below:
#      points = ... (stored in memory)
#      print(euclidean_distances(points)) → function runs first, then print shows the result
#      points = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(euclidean_distances(25)) → function runs first, then print shows the result

"""

"""
Problem 706:
#
# ===== SYNTAX =====
#   def lst_emails(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_emails(lst)): the inner lst_emails() executes FIRST, then print() outputs the result
#   lst_emails(lst): looks up variable 'lst', then runs the function body
#   print(lst_emails(?)): the inner lst_emails() executes FIRST, then print() outputs the result
#   lst_emails(lst): looks up variable 'lst', then runs the function body
#   lst_emails(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_emails(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_emails(...))
#       calls lst_emails with: lst=lst
#         3.1 Return from function
#   4. Define def lst_emails(lst) — stored for later
#   5. Define def lst_emails(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_emails(...))
#       calls lst_emails with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_emails, lst_emails, lst_emails are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_emails(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_emails(?)) → function runs first, then print shows the result

"""

"""
Problem 707:
#
# ===== SYNTAX =====
#   def lst_integers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   if/elif/else
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(lst_integers(lst)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(lst): looks up variable 'lst', then runs the function body
#   print(lst_integers(?)): the inner lst_integers() executes FIRST, then print() outputs the result
#   lst_integers(lst): looks up variable 'lst', then runs the function body
#   lst_integers(?): resolves the value, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_integers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_integers(...))
#       calls lst_integers with: lst=lst
#         3.1 Return from function
#   4. Define def lst_integers(lst) — stored for later
#   5. Define def lst_integers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_integers(...))
#       calls lst_integers with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_integers, lst_integers, lst_integers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_integers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_integers(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 708:
#
# ===== SYNTAX =====
#   def negate_bool(value)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   negate_bool(value): looks up variable 'value', then runs the function body
#   print(negate_bool(25)): the inner negate_bool() executes FIRST, then print() outputs the result
#   negate_bool(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def negate_bool(value) — stored for later
#   2. Assign to bool_list
#   3. Assign to negated_list
#   4. Run print(negated_list)
#   5. Define def negate_bool(value) — stored for later
#   6. Define def negate_bool(value) — stored for later
#   7. Assign to value
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(negate_bool(...))
#       calls negate_bool with: value=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions negate_bool, negate_bool, negate_bool are just stored
#   Execution starts at the non-def lines below:
#      bool_list = ... (stored in memory)
#      negated_list = ... (stored in memory)
#      print(negated_list)
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(negate_bool(25)) → function runs first, then print shows the result

"""

"""
Problem 709:
#
# ===== SYNTAX =====
#   def negate_bool(bool_list)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   negate_bool(bool_list): looks up variable 'bool_list', then runs the function body
#   negate_bool(bool_list): looks up variable 'bool_list', then runs the function body
#   print(negate_bool(?)): the inner negate_bool() executes FIRST, then print() outputs the result
#   negate_bool(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def negate_bool(bool_list) — stored for later
#   2. Assign to bool_list
#   3. Run negate_bool(bool_list)
#       with: bool_list=bool_list
#         3.1 Return from function
#   4. Define def negate_bool(value) — stored for later
#   5. Assign to bool_list
#   6. Assign to negated_list
#   7. Run print(negated_list)
#   8. Define def negate_bool(bool_list) — stored for later
#   9. Define def negate_bool(bool_list) — stored for later
#   10. Assign to bool_list
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(negate_bool(...))
#       calls negate_bool with: bool_list=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions negate_bool, negate_bool, negate_bool, negate_bool are just stored
#   Execution starts at the non-def lines below:
#      bool_list = ... (stored in memory)
#      negate_bool(bool_list) → jumps into function, runs body, returns result → printed
#      bool_list = ... (stored in memory)
#      negated_list = ... (stored in memory)
#      print(negated_list)
#      bool_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(negate_bool(?)) → function runs first, then print shows the result

"""

"""
Problem 710:
#
# ===== SYNTAX =====
#   def negate_booleans(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(negate_booleans(lst)): the inner negate_booleans() executes FIRST, then print() outputs the result
#   negate_booleans(lst): looks up variable 'lst', then runs the function body
#   print(negate_booleans(?)): the inner negate_booleans() executes FIRST, then print() outputs the result
#   negate_booleans(lst): looks up variable 'lst', then runs the function body
#   negate_booleans(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def negate_booleans(lst) — stored for later
#   2. Assign to lst
#   3. Run print(negate_booleans(...))
#       calls negate_booleans with: lst=lst
#         3.1 Return from function
#   4. Define def negate_booleans(lst) — stored for later
#   5. Define def negate_booleans(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(negate_booleans(...))
#       calls negate_booleans with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions negate_booleans, negate_booleans, negate_booleans are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(negate_booleans(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(negate_booleans(?)) → function runs first, then print shows the result

"""

"""
Problem 712:
#
# ===== SYNTAX =====
#   def lst_sentence(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_sentence(lst)): the inner lst_sentence() executes FIRST, then print() outputs the result
#   lst_sentence(lst): looks up variable 'lst', then runs the function body
#   print(lst_sentence(?)): the inner lst_sentence() executes FIRST, then print() outputs the result
#   lst_sentence(lst): looks up variable 'lst', then runs the function body
#   lst_sentence(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_sentence(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_sentence(...))
#       calls lst_sentence with: lst=lst
#         3.1 Return from function
#   4. Define def lst_sentence(lst) — stored for later
#   5. Define def lst_sentence(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_sentence(...))
#       calls lst_sentence with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_sentence, lst_sentence, lst_sentence are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_sentence(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_sentence(?)) → function runs first, then print shows the result

"""

"""
Problem 713:
#
# ===== SYNTAX =====
#   def lst_sentence(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_sentence(lst)): the inner lst_sentence() executes FIRST, then print() outputs the result
#   lst_sentence(lst): looks up variable 'lst', then runs the function body
#   print(lst_sentence(?)): the inner lst_sentence() executes FIRST, then print() outputs the result
#   lst_sentence(lst): looks up variable 'lst', then runs the function body
#   lst_sentence(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_sentence(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_sentence(...))
#       calls lst_sentence with: lst=lst
#         3.1 Return from function
#   4. Define def lst_sentence(lst) — stored for later
#   5. Define def lst_sentence(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_sentence(...))
#       calls lst_sentence with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_sentence, lst_sentence, lst_sentence are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_sentence(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_sentence(?)) → function runs first, then print shows the result

"""

"""
Problem 714:
#
# ===== SYNTAX =====
#   def lst_of_tuples(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(lst_of_tuples(lst)): the inner lst_of_tuples() executes FIRST, then print() outputs the result
#   lst_of_tuples(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_tuples(?)): the inner lst_of_tuples() executes FIRST, then print() outputs the result
#   lst_of_tuples(lst): looks up variable 'lst', then runs the function body
#   lst_of_tuples(?): resolves the value, then runs the function body
#   width * height: first get width, then get height, then compute width * height
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_of_tuples(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_of_tuples(...))
#       calls lst_of_tuples with: lst=lst
#         3.1 Return from function
#   4. Define def lst_of_tuples(lst) — stored for later
#   5. Define def lst_of_tuples(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_of_tuples(...))
#       calls lst_of_tuples with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_of_tuples, lst_of_tuples, lst_of_tuples are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_of_tuples(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_of_tuples(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 715:
#
# ===== SYNTAX =====
#   def extract_domain(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(extract_domain(urls)): the inner extract_domain() executes FIRST, then print() outputs the result
#   extract_domain(lst): looks up variable 'lst', then runs the function body
#   print(extract_domain(?)): the inner extract_domain() executes FIRST, then print() outputs the result
#   extract_domain(urls): looks up variable 'urls', then runs the function body
#   extract_domain(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def extract_domain(lst) — stored for later
#   2. Assign to urls
#   3. Run print(extract_domain(...))
#       calls extract_domain with: lst=urls
#         3.1 Return from function
#   4. Define def extract_domain(lst) — stored for later
#   5. Define def extract_domain(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(extract_domain(...))
#       calls extract_domain with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions extract_domain, extract_domain, extract_domain are just stored
#   Execution starts at the non-def lines below:
#      urls = ... (stored in memory)
#      print(extract_domain(urls)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(extract_domain(?)) → function runs first, then print shows the result

"""

"""
Problem 716:
#
# ===== SYNTAX =====
#   def pair_elements(numbers, strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(pair_elements(numbers, strings)): the inner pair_elements() executes FIRST, then print() outputs the result
#   pair_elements(numbers, strings): looks up variable 'numbers', looks up variable 'strings', then runs the function body
#   print(pair_elements(?, 'world')): the inner pair_elements() executes FIRST, then print() outputs the result
#   pair_elements(numbers, strings): looks up variable 'numbers', looks up variable 'strings', then runs the function body
#   pair_elements(?, 'world'): resolves the value, gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def pair_elements(numbers, strings) — stored for later
#   2. Assign to numbers
#   3. Assign to strings
#   4. Run print(pair_elements(...))
#       calls pair_elements with: numbers=numbers, strings=strings
#         4.1 Return from function
#   5. Define def pair_elements(numbers, strings) — stored for later
#   6. Define def pair_elements(numbers, strings) — stored for later
#   7. Assign to numbers
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(pair_elements(...))
#       calls pair_elements with: numbers=[4, 1, 8, 6, 3], strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions pair_elements, pair_elements, pair_elements are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      strings = ... (stored in memory)
#      print(pair_elements(numbers, strings)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(pair_elements(?, 'world')) → function runs first, then print shows the result

"""

"""
Problem 717:
#
# ===== SYNTAX =====
#   def lowercase_to_uppercase(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   operators: - <=
#
# ===== EVALUATION ORDER =====
#   print(lowercase_to_uppercase(lst)): the inner lowercase_to_uppercase() executes FIRST, then print() outputs the result
#   lowercase_to_uppercase(lst): looks up variable 'lst', then runs the function body
#   print(lowercase_to_uppercase(?)): the inner lowercase_to_uppercase() executes FIRST, then print() outputs the result
#   lowercase_to_uppercase(lst): looks up variable 'lst', then runs the function body
#   lowercase_to_uppercase(?): resolves the value, then runs the function body
#   ord(...) - 32: first get ord(...), then get 32, then compute ord(...) - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def lowercase_to_uppercase(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lowercase_to_uppercase(...))
#       calls lowercase_to_uppercase with: lst=lst
#         3.1 Return from function
#   4. Define def lowercase_to_uppercase(lst) — stored for later
#   5. Define def lowercase_to_uppercase(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lowercase_to_uppercase(...))
#       calls lowercase_to_uppercase with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lowercase_to_uppercase, lowercase_to_uppercase, lowercase_to_uppercase are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lowercase_to_uppercase(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lowercase_to_uppercase(?)) → function runs first, then print shows the result

"""

"""
Problem 718:
#
# ===== SYNTAX =====
#   def factorial_lst(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   range() — built-in function call
#   map() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(factorial_lst(lst)): the inner factorial_lst() executes FIRST, then print() outputs the result
#   print(factorial_lst(lst)): the inner factorial_lst() executes FIRST, then print() outputs the result
#   factorial_lst(lst): looks up variable 'lst', then runs the function body
#   print(factorial_lst(?)): the inner factorial_lst() executes FIRST, then print() outputs the result
#   factorial_lst(lst): looks up variable 'lst', then runs the function body
#   factorial_lst(lst): looks up variable 'lst', then runs the function body
#   factorial_lst(?): resolves the value, then runs the function body
#   num + 1: first get num, then get 1, then compute num + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(factorial_lst(...))
#       calls factorial_lst with: lst=lst
#         3.1 Return from function
#   4. Import math
#   5. Define def factorial_lst(lst) — stored for later
#   6. Assign to lst
#   7. Run print(factorial_lst(...))
#       calls factorial_lst with: lst=lst
#         7.1 Return from function
#   8. Define def factorial_lst(lst) — stored for later
#   9. Define def factorial_lst(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(factorial_lst(...))
#       calls factorial_lst with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions factorial_lst, factorial_lst, factorial_lst, factorial_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(factorial_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(factorial_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 719:
#
# ===== SYNTAX =====
#   def reverse_words_in_sentences(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(reverse_words_in_sentences(lst)): the inner reverse_words_in_sentences() executes FIRST, then print() outputs the result
#   reverse_words_in_sentences(lst): looks up variable 'lst', then runs the function body
#   print(reverse_words_in_sentences(?)): the inner reverse_words_in_sentences() executes FIRST, then print() outputs the result
#   reverse_words_in_sentences(lst): looks up variable 'lst', then runs the function body
#   reverse_words_in_sentences(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_words_in_sentences(lst) — stored for later
#   2. Assign to lst
#   3. Run print(reverse_words_in_sentences(...))
#       calls reverse_words_in_sentences with: lst=lst
#         3.1 Return from function
#   4. Define def reverse_words_in_sentences(lst) — stored for later
#   5. Define def reverse_words_in_sentences(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_words_in_sentences(...))
#       calls reverse_words_in_sentences with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_words_in_sentences, reverse_words_in_sentences, reverse_words_in_sentences are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(reverse_words_in_sentences(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_words_in_sentences(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 720:
#
# ===== SYNTAX =====
#   def lst_strings(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   print(lst_strings(lst)): the inner lst_strings() executes FIRST, then print() outputs the result
#   lst_strings(lst): looks up variable 'lst', then runs the function body
#   print(lst_strings(?)): the inner lst_strings() executes FIRST, then print() outputs the result
#   lst_strings(lst): looks up variable 'lst', then runs the function body
#   lst_strings(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_strings(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_strings(...))
#       calls lst_strings with: lst=lst
#         3.1 Return from function
#   4. Define def lst_strings(lst) — stored for later
#   5. Define def lst_strings(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_strings(...))
#       calls lst_strings with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_strings, lst_strings, lst_strings are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_strings(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_strings(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 721:
#
# ===== SYNTAX =====
#   def square_floats(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(square_floats(lst)): the inner square_floats() executes FIRST, then print() outputs the result
#   square_floats(lst): looks up variable 'lst', then runs the function body
#   print(square_floats(?)): the inner square_floats() executes FIRST, then print() outputs the result
#   square_floats(lst): looks up variable 'lst', then runs the function body
#   square_floats(?): resolves the value, then runs the function body
#   num * num: first get num, then get num, then compute num * num
#
# ===== EXECUTION ORDER =====
#   1. Define def square_floats(lst) — stored for later
#   2. Assign to lst
#   3. Run print(square_floats(...))
#       calls square_floats with: lst=lst
#         3.1 Return from function
#   4. Define def square_floats(lst) — stored for later
#   5. Define def square_floats(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_floats(...))
#       calls square_floats with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_floats, square_floats, square_floats are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(square_floats(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_floats(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 722:
#
# ===== SYNTAX =====
#   def format_phone_numbers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: %
#
# ===== EVALUATION ORDER =====
#   print(format_phone_numbers(lst)): the inner format_phone_numbers() executes FIRST, then print() outputs the result
#   format_phone_numbers(lst): looks up variable 'lst', then runs the function body
#   print(format_phone_numbers(?)): the inner format_phone_numbers() executes FIRST, then print() outputs the result
#   format_phone_numbers(lst): looks up variable 'lst', then runs the function body
#   '+1-%s' % number: first get '+1-%s', then get number, then compute '+1-%s' % number
#   format_phone_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_phone_numbers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(format_phone_numbers(...))
#       calls format_phone_numbers with: lst=lst
#         3.1 Return from function
#   4. Define def format_phone_numbers(lst) — stored for later
#   5. Define def format_phone_numbers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(format_phone_numbers(...))
#       calls format_phone_numbers with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_phone_numbers, format_phone_numbers, format_phone_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(format_phone_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_phone_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 723:
#
# ===== SYNTAX =====
#   def absolute_values(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   abs() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(absolute_values(lst)): the inner absolute_values() executes FIRST, then print() outputs the result
#   absolute_values(lst): looks up variable 'lst', then runs the function body
#   print(absolute_values(?)): the inner absolute_values() executes FIRST, then print() outputs the result
#   absolute_values(lst): looks up variable 'lst', then runs the function body
#   absolute_values(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def absolute_values(lst) — stored for later
#   2. Assign to lst
#   3. Run print(absolute_values(...))
#       calls absolute_values with: lst=lst
#         3.1 Return from function
#   4. Define def absolute_values(lst) — stored for later
#   5. Define def absolute_values(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(absolute_values(...))
#       calls absolute_values with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions absolute_values, absolute_values, absolute_values are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(absolute_values(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(absolute_values(?)) → function runs first, then print shows the result

"""

"""
Problem 724:
#
# ===== SYNTAX =====
#   def pair_lists(lst1, lst2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(pair_lists(lst1, lst2)): the inner pair_lists() executes FIRST, then print() outputs the result
#   pair_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(pair_lists(?, ?)): the inner pair_lists() executes FIRST, then print() outputs the result
#   pair_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   pair_lists(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def pair_lists(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(pair_lists(...))
#       calls pair_lists with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def pair_lists(lst1, lst2) — stored for later
#   6. Define def pair_lists(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(pair_lists(...))
#       calls pair_lists with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions pair_lists, pair_lists, pair_lists are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(pair_lists(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(pair_lists(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 725:
#
# ===== SYNTAX =====
#   def cartesian_to_polar(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   print(cartesian_to_polar(coordinates)): the inner cartesian_to_polar() executes FIRST, then print() outputs the result
#   cartesian_to_polar(lst): looks up variable 'lst', then runs the function body
#   print(cartesian_to_polar(?)): the inner cartesian_to_polar() executes FIRST, then print() outputs the result
#   cartesian_to_polar(coordinates): looks up variable 'coordinates', then runs the function body
#   cartesian_to_polar(?): resolves the value, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   x ** 2: first get x, then get 2, then compute x ** 2
#   y ** 2: first get y, then get 2, then compute y ** 2
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def cartesian_to_polar(lst) — stored for later
#   3. Assign to coordinates
#   4. Run print(cartesian_to_polar(...))
#       calls cartesian_to_polar with: lst=coordinates
#         4.1 Return from function
#   5. Define def cartesian_to_polar(lst) — stored for later
#   6. Define def cartesian_to_polar(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(cartesian_to_polar(...))
#       calls cartesian_to_polar with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions cartesian_to_polar, cartesian_to_polar, cartesian_to_polar are just stored
#   Execution starts at the non-def lines below:
#      coordinates = ... (stored in memory)
#      print(cartesian_to_polar(coordinates)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(cartesian_to_polar(?)) → function runs first, then print shows the result

"""

"""
Problem 726:
#
# ===== SYNTAX =====
#   def lst_cubes(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(lst_cubes(numbers)): the inner lst_cubes() executes FIRST, then print() outputs the result
#   lst_cubes(lst): looks up variable 'lst', then runs the function body
#   print(lst_cubes(?)): the inner lst_cubes() executes FIRST, then print() outputs the result
#   lst_cubes(numbers): looks up variable 'numbers', then runs the function body
#   lst_cubes(?): resolves the value, then runs the function body
#   num ** 3: first get num, then get 3, then compute num ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_cubes(lst) — stored for later
#   2. Assign to numbers
#   3. Run print(lst_cubes(...))
#       calls lst_cubes with: lst=numbers
#         3.1 Import math
#         3.2 Return from function
#   4. Define def lst_cubes(lst) — stored for later
#   5. Define def lst_cubes(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_cubes(...))
#       calls lst_cubes with: lst=[1, 3, 5, 7]
#         9.1 Import math
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_cubes, lst_cubes, lst_cubes are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(lst_cubes(numbers)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_cubes(?)) → function runs first, then print shows the result

"""

"""
Problem 727:
#
# ===== SYNTAX =====
#   def lst_squares_and_cubes(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(lst_squares_and_cubes(lst)): the inner lst_squares_and_cubes() executes FIRST, then print() outputs the result
#   lst_squares_and_cubes(lst): looks up variable 'lst', then runs the function body
#   print(lst_squares_and_cubes(?)): the inner lst_squares_and_cubes() executes FIRST, then print() outputs the result
#   lst_squares_and_cubes(lst): looks up variable 'lst', then runs the function body
#   lst_squares_and_cubes(?): resolves the value, then runs the function body
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 3: first get num, then get 3, then compute num ** 3
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 3: first get num, then get 3, then compute num ** 3
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 3: first get num, then get 3, then compute num ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_squares_and_cubes(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_squares_and_cubes(...))
#       calls lst_squares_and_cubes with: lst=lst
#         3.1 Return from function
#   4. Define def lst_squares_and_cubes(lst) — stored for later
#   5. Define def lst_squares_and_cubes(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_squares_and_cubes(...))
#       calls lst_squares_and_cubes with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_squares_and_cubes, lst_squares_and_cubes, lst_squares_and_cubes are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_squares_and_cubes(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_squares_and_cubes(?)) → function runs first, then print shows the result

"""

"""
Problem 728:
#
# ===== SYNTAX =====
#   def rgb_to_hex(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(rgb_to_hex(lst)): the inner rgb_to_hex() executes FIRST, then print() outputs the result
#   rgb_to_hex(lst): looks up variable 'lst', then runs the function body
#   print(rgb_to_hex(?)): the inner rgb_to_hex() executes FIRST, then print() outputs the result
#   rgb_to_hex(lst): looks up variable 'lst', then runs the function body
#   rgb_to_hex(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def rgb_to_hex(lst) — stored for later
#   2. Assign to lst
#   3. Run print(rgb_to_hex(...))
#       calls rgb_to_hex with: lst=lst
#         3.1 Return from function
#   4. Define def rgb_to_hex(lst) — stored for later
#   5. Define def rgb_to_hex(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(rgb_to_hex(...))
#       calls rgb_to_hex with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions rgb_to_hex, rgb_to_hex, rgb_to_hex are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(rgb_to_hex(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(rgb_to_hex(?)) → function runs first, then print shows the result

"""

"""
Problem 729:
#
# ===== SYNTAX =====
#   def mixed_case_string(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(mixed_case_string(lst)): the inner mixed_case_string() executes FIRST, then print() outputs the result
#   mixed_case_string(lst): looks up variable 'lst', then runs the function body
#   print(mixed_case_string(?)): the inner mixed_case_string() executes FIRST, then print() outputs the result
#   mixed_case_string(lst): looks up variable 'lst', then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   mixed_case_string(?): resolves the value, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?.upper(...): first get ?, then get ?.upper(...), then compute ? + ?.upper(...)
#   ? + ?.upper(...): first get ?, then get ?.upper(...), then compute ? + ?.upper(...)
#   ? + ?.upper(...): first get ?, then get ?.upper(...), then compute ? + ?.upper(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def mixed_case_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(mixed_case_string(...))
#       calls mixed_case_string with: lst=lst
#         3.1 Return from function
#   4. Define def mixed_case_string(lst) — stored for later
#   5. Define def mixed_case_string(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(mixed_case_string(...))
#       calls mixed_case_string with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions mixed_case_string, mixed_case_string, mixed_case_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(mixed_case_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(mixed_case_string(?)) → function runs first, then print shows the result

"""

"""
Problem 730:
#
# ===== SYNTAX =====
#   def convert_dates(date_list)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(convert_dates(date_list)): the inner convert_dates() executes FIRST, then print() outputs the result
#   convert_dates(date_list): looks up variable 'date_list', then runs the function body
#   print(convert_dates(?)): the inner convert_dates() executes FIRST, then print() outputs the result
#   convert_dates(date_list): looks up variable 'date_list', then runs the function body
#   convert_dates(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import datetime from datetime
#   2. Define def convert_dates(date_list) — stored for later
#   3. Assign to date_list
#   4. Run print(convert_dates(...))
#       calls convert_dates with: date_list=date_list
#         4.1 Return from function
#   5. Define def convert_dates(date_list) — stored for later
#   6. Define def convert_dates(date_list) — stored for later
#   7. Assign to date_list
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(convert_dates(...))
#       calls convert_dates with: date_list=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions convert_dates, convert_dates, convert_dates are just stored
#   Execution starts at the non-def lines below:
#      date_list = ... (stored in memory)
#      print(convert_dates(date_list)) → function runs first, then print shows the result
#      date_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_dates(?)) → function runs first, then print shows the result

"""

"""
Problem 731:
#
# ===== SYNTAX =====
#   def calculate_square_roots(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(calculate_square_roots(numbers)): the inner calculate_square_roots() executes FIRST, then print() outputs the result
#   calculate_square_roots(numbers): looks up variable 'numbers', then runs the function body
#   print(calculate_square_roots(?)): the inner calculate_square_roots() executes FIRST, then print() outputs the result
#   calculate_square_roots(numbers): looks up variable 'numbers', then runs the function body
#   calculate_square_roots(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_square_roots(numbers) — stored for later
#   3. Assign to numbers
#   4. Run print(calculate_square_roots(...))
#       calls calculate_square_roots with: numbers=numbers
#         4.1 Return from function
#   5. Define def calculate_square_roots(numbers) — stored for later
#   6. Define def calculate_square_roots(numbers) — stored for later
#   7. Assign to numbers
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_square_roots(...))
#       calls calculate_square_roots with: numbers=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_square_roots, calculate_square_roots, calculate_square_roots are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(calculate_square_roots(numbers)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_square_roots(?)) → function runs first, then print shows the result

"""

"""
Problem 732:
#
# ===== SYNTAX =====
#   def calculate_percentage(scores, max_score)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(calculate_percentage(scores, max_score)): the inner calculate_percentage() executes FIRST, then print() outputs the result
#   calculate_percentage(scores, max_score): looks up variable 'scores', looks up variable 'max_score', then runs the function body
#   print(calculate_percentage(25, 25)): the inner calculate_percentage() executes FIRST, then print() outputs the result
#   calculate_percentage(scores, max_score): looks up variable 'scores', looks up variable 'max_score', then runs the function body
#   ? * 100: first get ?, then get 100, then compute ? * 100
#   ? * 100: first get ?, then get 100, then compute ? * 100
#   calculate_percentage(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   ? * 100: first get ?, then get 100, then compute ? * 100
#   score / max_score: first get score, then get max_score, then compute score / max_score
#   score / max_score: first get score, then get max_score, then compute score / max_score
#   score / max_score: first get score, then get max_score, then compute score / max_score
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_percentage(scores, max_score) — stored for later
#   2. Assign to scores
#   3. Assign to max_score
#   4. Run print(calculate_percentage(...))
#       calls calculate_percentage with: scores=scores, max_score=max_score
#         4.1 Return from function
#   5. Define def calculate_percentage(scores, max_score) — stored for later
#   6. Define def calculate_percentage(scores, max_score) — stored for later
#   7. Assign to scores
#   8. Assign to max_score
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_percentage(...))
#       calls calculate_percentage with: scores=25, max_score=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_percentage, calculate_percentage, calculate_percentage are just stored
#   Execution starts at the non-def lines below:
#      scores = ... (stored in memory)
#      max_score = ... (stored in memory)
#      print(calculate_percentage(scores, max_score)) → function runs first, then print shows the result
#      scores = ... (stored in memory)
#      max_score = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_percentage(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 733:
#
# ===== SYNTAX =====
#   def add(a, b)
#   return — exits the function and sends back a value
#   add() — built-in function call
#   sum() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_list(numbers): looks up variable 'numbers', then runs the function body
#   add(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(add(20, 30)): the inner add() executes FIRST, then print() outputs the result
#   a + b: first get a, then get b, then compute a + b
#   add(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   add(total, number): looks up variable 'total', looks up variable 'number', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add(a, b) — stored for later
#   2. Define def sum_list(numbers) — stored for later
#   3. Assign to numbers
#   4. Assign to result
#   5. Run print(result)
#   6. Define def add(a, b) — stored for later
#   7. Define def add(a, b) — stored for later
#   8. Assign to a
#   9. Assign to b
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(add(...))
#       calls add with: a=20, b=30
#         12.1 Import operator
#         12.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add, sum_list, add, add are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 734:
#
# ===== SYNTAX =====
#   def is_even(n)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   filter_even_numbers(numbers): looks up variable 'numbers', then runs the function body
#   is_even(n): looks up variable 'n', then runs the function body
#   print(is_even(15)): the inner is_even() executes FIRST, then print() outputs the result
#   is_even(15): gets the literal 15, then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def is_even(n) — stored for later
#   2. Define def filter_even_numbers(numbers) — stored for later
#   3. Assign to numbers
#   4. Assign to even_numbers
#   5. Run print(even_numbers)
#   6. Define def is_even(n) — stored for later
#   7. Define def is_even(n) — stored for later
#   8. Assign to n
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(is_even(...))
#       calls is_even with: n=15
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_even, filter_even_numbers, is_even, is_even are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      even_numbers = ... (stored in memory)
#      print(even_numbers)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_even(15)) → function runs first, then print shows the result

"""

"""
Problem 735:
#
# ===== SYNTAX =====
#   def capitalize_first_letter(word)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(word_list(lst)): the inner word_list() executes FIRST, then print() outputs the result
#   print(word_list(lst)): the inner word_list() executes FIRST, then print() outputs the result
#   capitalize_first_letter(word): looks up variable 'word', then runs the function body
#   print(capitalize_first_letter('coding')): the inner capitalize_first_letter() executes FIRST, then print() outputs the result
#   word_list(lst): looks up variable 'lst', then runs the function body
#   word_list(lst): looks up variable 'lst', then runs the function body
#   capitalize_first_letter('coding'): gets the literal 'coding', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_first_letter(word) — stored for later
#   2. Define def word_list(lst) — stored for later
#   3. Assign to lst
#   4. Run print(word_list(...))
#       calls word_list with: lst=lst
#         4.1 Return from function
#   5. Define def word_list(lst) — stored for later
#   6. Define def capitalize_first_letter(word) — stored for later
#   7. Assign to lst
#   8. Run print(word_list(...))
#       calls word_list with: lst=lst
#         8.1 Return from function
#   9. Define def capitalize_first_letter(word) — stored for later
#   10. Define def capitalize_first_letter(word) — stored for later
#   11. Assign to word
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(capitalize_first_letter(...))
#       calls capitalize_first_letter with: word='coding'
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_first_letter, word_list, word_list, capitalize_first_letter, capitalize_first_letter, capitalize_first_letter are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(word_list(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(word_list(lst)) → function runs first, then print shows the result
#      word = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_first_letter('coding')) → function runs first, then print shows the result

"""

"""
Problem 736:
#
# ===== SYNTAX =====
#   def lst_numbers(lst)
#   return — exits the function and sends back a value
#   find_max() — built-in function call
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_numbers(lst)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   print(lst_numbers(?)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   find_max(lst): looks up variable 'lst', then runs the function body
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   find_max(lst): looks up variable 'lst', then runs the function body
#   find_max(lst): looks up variable 'lst', then runs the function body
#   lst_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_numbers(lst) — stored for later
#   2. Define def find_max(numbers) — stored for later
#   3. Assign to lst
#   4. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=lst
#         4.1 Return from function
#   5. Define def lst_numbers(lst) — stored for later
#   6. Define def lst_numbers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_numbers, find_max, lst_numbers, lst_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 737:
#
# ===== SYNTAX =====
#   def lst_tempertures(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: - /
#
# ===== EVALUATION ORDER =====
#   print(lst_tempertures(lst)): the inner lst_tempertures() executes FIRST, then print() outputs the result
#   lst_tempertures(lst): looks up variable 'lst', then runs the function body
#   print(lst_tempertures(?)): the inner lst_tempertures() executes FIRST, then print() outputs the result
#   ? / 1.8: first get ?, then get 1.8, then compute ? / 1.8
#   lst_tempertures(lst): looks up variable 'lst', then runs the function body
#   lst_tempertures(?): resolves the value, then runs the function body
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_tempertures(lst) — stored for later
#   2. Define def convert_to_celsius(fahrenheit) — stored for later
#   3. Assign to lst
#   4. Run print(lst_tempertures(...))
#       calls lst_tempertures with: lst=lst
#         4.1 Return from function
#   5. Define def lst_tempertures(lst) — stored for later
#   6. Define def lst_tempertures(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_tempertures(...))
#       calls lst_tempertures with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_tempertures, convert_to_celsius, lst_tempertures, lst_tempertures are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_tempertures(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_tempertures(?)) → function runs first, then print shows the result

"""

"""
Problem 738:
#
# ===== SYNTAX =====
#   def string_lst(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(string_lst(lst)): the inner string_lst() executes FIRST, then print() outputs the result
#   string_lst(lst): looks up variable 'lst', then runs the function body
#   print(string_lst(?)): the inner string_lst() executes FIRST, then print() outputs the result
#   string_lst(lst): looks up variable 'lst', then runs the function body
#   string_lst(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_lst(lst) — stored for later
#   2. Define def reverse_string(word) — stored for later
#   3. Assign to lst
#   4. Run print(string_lst(...))
#       calls string_lst with: lst=lst
#         4.1 Return from function
#   5. Define def string_lst(lst) — stored for later
#   6. Define def string_lst(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(string_lst(...))
#       calls string_lst with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_lst, reverse_string, string_lst, string_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(string_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_lst(?)) → function runs first, then print shows the result

"""

"""
Problem 739:
#
# ===== SYNTAX =====
#   def lst_words(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(lst_words(lst)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   print(lst_words(?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(lst): looks up variable 'lst', then runs the function body
#   lst_words(?): resolves the value, then runs the function body
#   char.lower(...) in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst) — stored for later
#   2. Define def count_vowels(word) — stored for later
#   3. Assign to lst
#   4. Run print(lst_words(...))
#       calls lst_words with: lst=lst
#         4.1 Return from function
#   5. Define def lst_words(lst) — stored for later
#   6. Define def lst_words(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_words(...))
#       calls lst_words with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, count_vowels, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 740:
#
# ===== SYNTAX =====
#   def lst_of_word(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(lst_of_word(lst)): the inner lst_of_word() executes FIRST, then print() outputs the result
#   lst_of_word(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_word(?)): the inner lst_of_word() executes FIRST, then print() outputs the result
#   lst_of_word(lst): looks up variable 'lst', then runs the function body
#   lst_of_word(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_of_word(lst) — stored for later
#   2. Define def is_palindrome(word) — stored for later
#   3. Assign to lst
#   4. Run print(lst_of_word(...))
#       calls lst_of_word with: lst=lst
#         4.1 Return from function
#   5. Define def lst_of_word(lst) — stored for later
#   6. Define def lst_of_word(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_of_word(...))
#       calls lst_of_word with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_of_word, is_palindrome, lst_of_word, lst_of_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_of_word(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_of_word(?)) → function runs first, then print shows the result

"""

"""
Problem 741:
#
# ===== SYNTAX =====
#   def lst_of_radii(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: * **
#
# ===== EVALUATION ORDER =====
#   print(lst_of_radii(lst)): the inner lst_of_radii() executes FIRST, then print() outputs the result
#   lst_of_radii(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_radii(?)): the inner lst_of_radii() executes FIRST, then print() outputs the result
#   3.14159 * ?: first get 3.14159, then get ?, then compute 3.14159 * ?
#   lst_of_radii(lst): looks up variable 'lst', then runs the function body
#   lst_of_radii(?): resolves the value, then runs the function body
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_of_radii(lst) — stored for later
#   2. Define def calculate_area(radius) — stored for later
#   3. Assign to lst
#   4. Run print(lst_of_radii(...))
#       calls lst_of_radii with: lst=lst
#         4.1 Return from function
#   5. Define def lst_of_radii(lst) — stored for later
#   6. Define def lst_of_radii(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_of_radii(...))
#       calls lst_of_radii with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_of_radii, calculate_area, lst_of_radii, lst_of_radii are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_of_radii(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_of_radii(?)) → function runs first, then print shows the result

"""

"""
Problem 742:
#
# ===== SYNTAX =====
#   def lst_numbers(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(lst_numbers(lst)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   print(lst_numbers(?)): the inner lst_numbers() executes FIRST, then print() outputs the result
#   num * num: first get num, then get num, then compute num * num
#   lst_numbers(lst): looks up variable 'lst', then runs the function body
#   lst_numbers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_numbers(lst) — stored for later
#   2. Define def sqaure_number(num) — stored for later
#   3. Assign to lst
#   4. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=lst
#         4.1 Return from function
#   5. Define def lst_numbers(lst) — stored for later
#   6. Define def lst_numbers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_numbers(...))
#       calls lst_numbers with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_numbers, sqaure_number, lst_numbers, lst_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 743:
#
# ===== SYNTAX =====
#   def get_first_n_elements(lst, num)
#   return — exits the function and sends back a value
#   get_first_n_elements() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(main()): the inner main() executes FIRST, then print() outputs the result
#   get_first_n_elements(lst, num): looks up variable 'lst', looks up variable 'num', then runs the function body
#   print(get_first_n_elements(?, 25)): the inner get_first_n_elements() executes FIRST, then print() outputs the result
#   get_first_n_elements(lst, num): looks up variable 'lst', looks up variable 'num', then runs the function body
#   main(): — no arguments needed
#   get_first_n_elements(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_first_n_elements(lst, num) — stored for later
#   2. Define def main() — stored for later
#   3. Run print(main(...))
#         3.1 Assign to lst
#         3.2 Assign to num
#         3.3 Return from function
#   4. Define def get_first_n_elements(lst, num) — stored for later
#   5. Define def get_first_n_elements(lst, num) — stored for later
#   6. Assign to lst
#   7. Assign to num
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_first_n_elements(...))
#       calls get_first_n_elements with: lst=[1, 3, 5, 7], num=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_first_n_elements, main, get_first_n_elements, get_first_n_elements are just stored
#   Execution starts at the non-def lines below:
#      print(main()) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_first_n_elements(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 744:
#
# ===== SYNTAX =====
#   def merge_dicts(dict1, dict2)
#   return — exits the function and sends back a value
#   merge_dicts() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(combine_dicts(dicts)): the inner combine_dicts() executes FIRST, then print() outputs the result
#   merge_dicts(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(merge_dicts(?, ?)): the inner merge_dicts() executes FIRST, then print() outputs the result
#   combine_dicts(dicts): looks up variable 'dicts', then runs the function body
#   merge_dicts(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_dicts(result, d): looks up variable 'result', looks up variable 'd', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_dicts(dict1, dict2) — stored for later
#   2. Define def combine_dicts(dicts) — stored for later
#   3. Assign to dicts
#   4. Run print(combine_dicts(...))
#       calls combine_dicts with: dicts=dicts
#         4.1 Assign to result
#         4.2 For loop over dicts
#         4.3 Return from function
#   5. Define def merge_dicts(dict1, dict2) — stored for later
#   6. Define def merge_dicts(dict1, dict2) — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(merge_dicts(...))
#       calls merge_dicts with: dict1={...}, dict2={...}
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_dicts, combine_dicts, merge_dicts, merge_dicts are just stored
#   Execution starts at the non-def lines below:
#      dicts = ... (stored in memory)
#      print(combine_dicts(dicts)) → function runs first, then print shows the result
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_dicts(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dicts each round

"""

"""
Problem 745:
#
# ===== SYNTAX =====
#   def format_name(first_name, last_name)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   format_name() — built-in function call
#   map() — built-in function call
#   operators: %
#
# ===== EVALUATION ORDER =====
#   format_names(names): looks up variable 'names', then runs the function body
#   format_name(first_name, last_name): looks up variable 'first_name', looks up variable 'last_name', then runs the function body
#   print(format_name('world', 'world')): the inner format_name() executes FIRST, then print() outputs the result
#   '%s %s' % (first_name, last_name): first get '%s %s', then get (first_name, last_name), then compute '%s %s' % (first_name, last_name)
#   format_name('world', 'world'): gets the literal 'world', gets the literal 'world', then runs the function body
#   format_name(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def format_name(first_name, last_name) — stored for later
#   2. Define def format_names(lst) — stored for later
#   3. Assign to names
#   4. Assign to formatted_names
#   5. Run print(formatted_names)
#   6. Define def format_name(first_name, last_name) — stored for later
#   7. Define def format_name(first_name, last_name) — stored for later
#   8. Assign to first_name
#   9. Assign to last_name
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(format_name(...))
#       calls format_name with: first_name='world', last_name='world'
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions format_name, format_names, format_name, format_name are just stored
#   Execution starts at the non-def lines below:
#      names = ... (stored in memory)
#      formatted_names = ... (stored in memory)
#      print(formatted_names)
#      first_name = ... (stored in memory)
#      last_name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(format_name('world', 'world')) → function runs first, then print shows the result

"""

"""
Problem 746:
#
# ===== SYNTAX =====
#   def find_gcd(a, b)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   find_gcd() — built-in function call
#   map() — built-in function call
#   while
#   operators: %
#
# ===== EVALUATION ORDER =====
#   gcd_of_pairs(pairs): looks up variable 'pairs', then runs the function body
#   find_gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(find_gcd(20, 30)): the inner find_gcd() executes FIRST, then print() outputs the result
#   find_gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   find_gcd(?, ?): resolves the value, resolves the value, then runs the function body
#   a % b: first get a, then get b, then compute a % b
#
# ===== EXECUTION ORDER =====
#   1. Define def find_gcd(a, b) — stored for later
#   2. Define def gcd_of_pairs(lst) — stored for later
#   3. Assign to pairs
#   4. Assign to gcd_results
#   5. Run print(gcd_results)
#   6. Define def find_gcd(a, b) — stored for later
#   7. Define def find_gcd(a, b) — stored for later
#   8. Assign to a
#   9. Assign to b
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_gcd(...))
#       calls find_gcd with: a=20, b=30
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_gcd, gcd_of_pairs, find_gcd, find_gcd are just stored
#   Execution starts at the non-def lines below:
#      pairs = ... (stored in memory)
#      gcd_results = ... (stored in memory)
#      print(gcd_results)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_gcd(20, 30)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 747:
#
# ===== SYNTAX =====
#   def lst_elements(lst)
#   return — exits the function and sends back a value
#   count_occurrences() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(lst_elements(lst)): the inner lst_elements() executes FIRST, then print() outputs the result
#   lst_elements(lst): looks up variable 'lst', then runs the function body
#   print(lst_elements(?)): the inner lst_elements() executes FIRST, then print() outputs the result
#   count_occurrences(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   lst_elements(lst): looks up variable 'lst', then runs the function body
#   count_occurrences(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   count_occurrences(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   lst_elements(?): resolves the value, then runs the function body
#   item == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_elements(lst) — stored for later
#   2. Define def count_occurrences(lst, element) — stored for later
#   3. Assign to lst
#   4. Run print(lst_elements(...))
#       calls lst_elements with: lst=lst
#         4.1 Return from function
#   5. Define def lst_elements(lst) — stored for later
#   6. Define def lst_elements(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_elements(...))
#       calls lst_elements with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_elements, count_occurrences, lst_elements, lst_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_elements(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 748:
#
# ===== SYNTAX =====
#   def lst_words(lst1, lst2)
#   return — exits the function and sends back a value
#   sort_lst() — built-in function call
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   lst_words(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   lst_words(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(lst_words(?, ?)): the inner lst_words() executes FIRST, then print() outputs the result
#   lst_words(?, ?): resolves the value, resolves the value, then runs the function body
#   sort_lst(lst1): looks up variable 'lst1', then runs the function body
#   sort_lst(lst2): looks up variable 'lst2', then runs the function body
#   sort_lst(lst1): looks up variable 'lst1', then runs the function body
#   sort_lst(lst2): looks up variable 'lst2', then runs the function body
#   sort_lst(lst1): looks up variable 'lst1', then runs the function body
#   sort_lst(lst2): looks up variable 'lst2', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_words(lst1, lst2) — stored for later
#   2. Define def sort_lst(lst) — stored for later
#   3. Assign to lst1
#   4. Assign to lst2
#   5. Assign to sorted_lists
#   6. Run print(sorted_lists)
#   7. Define def lst_words(lst1, lst2) — stored for later
#   8. Define def lst_words(lst1, lst2) — stored for later
#   9. Assign to lst1
#   10. Assign to lst2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(lst_words(...))
#       calls lst_words with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_words, sort_lst, lst_words, lst_words are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      sorted_lists = ... (stored in memory)
#      print(sorted_lists)
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_words(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 749:
#
# ===== SYNTAX =====
#   def domain_names(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(domain_names(lst)): the inner domain_names() executes FIRST, then print() outputs the result
#   domain_names(lst): looks up variable 'lst', then runs the function body
#   print(domain_names(?)): the inner domain_names() executes FIRST, then print() outputs the result
#   domain_names(lst): looks up variable 'lst', then runs the function body
#   domain_names(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def domain_names(lst) — stored for later
#   2. Define def extract_URL(url) — stored for later
#   3. Assign to lst
#   4. Run print(domain_names(...))
#       calls domain_names with: lst=lst
#         4.1 Return from function
#   5. Define def domain_names(lst) — stored for later
#   6. Define def domain_names(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(domain_names(...))
#       calls domain_names with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions domain_names, extract_URL, domain_names, domain_names are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(domain_names(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(domain_names(?)) → function runs first, then print shows the result

"""

"""
Problem 750:
#
# ===== SYNTAX =====
#   def lst_sites(lst)
#   return — exits the function and sends back a value
#   remove_duplicates() — built-in function call
#   list() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_sites(lst)): the inner lst_sites() executes FIRST, then print() outputs the result
#   lst_sites(lst): looks up variable 'lst', then runs the function body
#   print(lst_sites(?)): the inner lst_sites() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   lst_sites(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   lst_sites(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_sites(lst) — stored for later
#   2. Define def remove_duplicates(lst) — stored for later
#   3. Assign to lst
#   4. Run print(lst_sites(...))
#       calls lst_sites with: lst=lst
#         4.1 Return from function
#   5. Define def lst_sites(lst) — stored for later
#   6. Define def lst_sites(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lst_sites(...))
#       calls lst_sites with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_sites, remove_duplicates, lst_sites, lst_sites are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_sites(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_sites(?)) → function runs first, then print shows the result

"""

"""
Problem 751:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   flatten_list() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   print(lst_of_lists(nested_list)): the inner lst_of_lists() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   flatten_list(nested_list): looks up variable 'nested_list', then runs the function body
#   lst_of_lists(nested_list): looks up variable 'nested_list', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run print(main(...))
#       calls main with: lst=lst
#         3.1 Return from function
#   4. Define def lst_of_lists(nested_list) — stored for later
#   5. Define def flatten_list(nested_list) — stored for later
#   6. Assign to nested_list
#   7. Run print(lst_of_lists(...))
#       calls lst_of_lists with: nested_list=nested_list
#         7.1 Return from function
#   8. Define def main(lst) — stored for later
#   9. Define def main(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, lst_of_lists, flatten_list, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      nested_list = ... (stored in memory)
#      print(lst_of_lists(nested_list)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 752:
#
# ===== SYNTAX =====
#   def filter_primes(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + % ** == <=
#
# ===== EVALUATION ORDER =====
#   print(filter_primes(numbers)): the inner filter_primes() executes FIRST, then print() outputs the result
#   filter_primes(lst): looks up variable 'lst', then runs the function body
#   print(filter_primes(?)): the inner filter_primes() executes FIRST, then print() outputs the result
#   filter_primes(numbers): looks up variable 'numbers', then runs the function body
#   filter_primes(?): resolves the value, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % i: first get n, then get i, then compute n % i
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n <= 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_primes(lst) — stored for later
#   2. Define def is_prime(n) — stored for later
#   3. Assign to numbers
#   4. Run print(filter_primes(...))
#       calls filter_primes with: lst=numbers
#         4.1 Return from function
#   5. Define def filter_primes(lst) — stored for later
#   6. Define def filter_primes(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(filter_primes(...))
#       calls filter_primes with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_primes, is_prime, filter_primes, filter_primes are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(filter_primes(numbers)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_primes(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 753:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   range() — built-in function call
#   map() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   main(lst): looks up variable 'lst', then runs the function body
#   print(lst_of_numbers(lst)): the inner lst_of_numbers() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   lst_of_numbers(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#   num + 1: first get num, then get 1, then compute num + 1
#   n + 1: first get n, then get 1, then compute n + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run main(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def lst_of_numbers(lst) — stored for later
#   5. Define def calculate_factorial(num) — stored for later
#   6. Assign to lst
#   7. Run print(lst_of_numbers(...))
#       calls lst_of_numbers with: lst=lst
#         7.1 Return from function
#   8. Define def main(lst) — stored for later
#   9. Define def main(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, lst_of_numbers, calculate_factorial, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(lst_of_numbers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 754:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#   sort_dict_by_value() — built-in function call
#   dict() — built-in function call
#   sorted() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(25)): the inner main() executes FIRST, then print() outputs the result
#   sort_dict_by_value(dic): looks up variable 'dic', then runs the function body
#   main(dic): looks up variable 'dic', then runs the function body
#   sort_dict_by_value(dic): looks up variable 'dic', then runs the function body
#   main(dic): looks up variable 'dic', then runs the function body
#   sort_dict_by_value(dic): looks up variable 'dic', then runs the function body
#   sort_dict_by_value(dic): looks up variable 'dic', then runs the function body
#   main(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Define def sort_dict_by_value(d) — stored for later
#   3. Define def get_value(item) — stored for later
#   4. Assign to dic
#   5. Run print(main(...))
#       calls main with: dic=dic
#         5.1 Return from function
#   6. Define def main(dic) — stored for later
#   7. Define def sort_dict_by_value(d) — stored for later
#   8. Assign to dic
#   9. Run print(main(...))
#       calls main with: dic=dic
#         9.1 Return from function
#   10. Define def main(dic) — stored for later
#   11. Define def main(dic) — stored for later
#   12. Assign to dic
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(main(...))
#       calls main with: dic=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, sort_dict_by_value, get_value, main, sort_dict_by_value, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sorted_items each round

"""

"""
Problem 755:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   get_unique_elements() — built-in function call
#   list() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   get_unique_elements(lst): looks up variable 'lst', then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   get_unique_elements(lst): looks up variable 'lst', then runs the function body
#   get_unique_elements(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def get_unique_elements(word) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, get_unique_elements, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 756:
#
# ===== SYNTAX =====
#   def main(str1, str2)
#   return — exits the function and sends back a value
#   check_anagram() — built-in function call
#   sorted() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(main(str1, str2)): the inner main() executes FIRST, then print() outputs the result
#   main(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   print(main('world', 'python')): the inner main() executes FIRST, then print() outputs the result
#   check_anagram(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   main(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   check_anagram(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   check_anagram(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   main('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(str1, str2) — stored for later
#   2. Define def check_anagram(x, y) — stored for later
#   3. Assign to str1
#   4. Assign to str2
#   5. Run print(main(...))
#       calls main with: str1=str1, str2=str2
#         5.1 Return from function
#   6. Define def main(str1, str2) — stored for later
#   7. Define def main(str1, str2) — stored for later
#   8. Assign to str1
#   9. Assign to str2
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: str1='world', str2='python'
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, check_anagram, main, main are just stored
#   Execution starts at the non-def lines below:
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      print(main(str1, str2)) → function runs first, then print shows the result
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main('world', 'python')) → function runs first, then print shows the result

"""

"""
Problem 757:
#
# ===== SYNTAX =====
#   def main(lst1, target1)
#   return — exits the function and sends back a value
#   find_index() — built-in function call
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(main(lst1, target1)): the inner main() executes FIRST, then print() outputs the result
#   main(lst1, target1): looks up variable 'lst1', looks up variable 'target1', then runs the function body
#   print(main(?, 25)): the inner main() executes FIRST, then print() outputs the result
#   find_index(lst, target1): looks up variable 'lst', looks up variable 'target1', then runs the function body
#   main(lst1, target1): looks up variable 'lst1', looks up variable 'target1', then runs the function body
#   find_index(lst, target1): looks up variable 'lst', looks up variable 'target1', then runs the function body
#   find_index(lst, target1): looks up variable 'lst', looks up variable 'target1', then runs the function body
#   main(?, 25): resolves the value, gets the literal 25, then runs the function body
#   word == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst1, target1) — stored for later
#   2. Define def find_index(lst, target) — stored for later
#   3. Assign to lst
#   4. Assign to target1
#   5. Run print(main(...))
#       calls main with: lst1=lst1, target1=target1
#         5.1 Return from function
#   6. Define def main(lst1, target1) — stored for later
#   7. Define def main(lst1, target1) — stored for later
#   8. Assign to lst1
#   9. Assign to target1
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: lst1=[2, 4, 6], target1=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, find_index, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      target1 = ... (stored in memory)
#      print(main(lst1, target1)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      target1 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 758:
#
# ===== SYNTAX =====
#   def main(n)
#   return — exits the function and sends back a value
#   generate_fibonacci() — built-in function call
#   range() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(main(n)): the inner main() executes FIRST, then print() outputs the result
#   main(n): looks up variable 'n', then runs the function body
#   print(main(15)): the inner main() executes FIRST, then print() outputs the result
#   generate_fibonacci(n): looks up variable 'n', then runs the function body
#   main(n): looks up variable 'n', then runs the function body
#   generate_fibonacci(n): looks up variable 'n', then runs the function body
#   generate_fibonacci(n): looks up variable 'n', then runs the function body
#   main(15): gets the literal 15, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Define def main(n) — stored for later
#   2. Define def generate_fibonacci(n) — stored for later
#   3. Assign to n
#   4. Run print(main(...))
#       calls main with: n=n
#         4.1 Return from function
#   5. Define def main(n) — stored for later
#   6. Define def main(n) — stored for later
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, generate_fibonacci, main, main are just stored
#   Execution starts at the non-def lines below:
#      n = ... (stored in memory)
#      print(main(n)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 759:
#
# ===== SYNTAX =====
#   def main(lst1, lst2)
#   return — exits the function and sends back a value
#   calculate_average() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   operators: /
#
# ===== EVALUATION ORDER =====
#   print(main(list1, list2)): the inner main() executes FIRST, then print() outputs the result
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(main(?, ?)): the inner main() executes FIRST, then print() outputs the result
#   calculate_average(lst1): looks up variable 'lst1', then runs the function body
#   calculate_average(lst2): looks up variable 'lst2', then runs the function body
#   total / length_lst: first get total, then get length_lst, then compute total / length_lst
#   main(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   main(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst1, lst2) — stored for later
#   2. Define def calculate_average(lst) — stored for later
#   3. Assign to list1
#   4. Assign to list2
#   5. Run print(main(...))
#       calls main with: lst1=list1, lst2=list2
#         5.1 Return from function
#   6. Define def main(lst1, lst2) — stored for later
#   7. Define def main(lst1, lst2) — stored for later
#   8. Assign to lst1
#   9. Assign to lst2
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, calculate_average, main, main are just stored
#   Execution starts at the non-def lines below:
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      print(main(list1, list2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 760:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   miles * miles_to_km: first get miles, then get miles_to_km, then compute miles * miles_to_km
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def convert_to_kilometers(miles) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, convert_to_kilometers, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 761:
#
# ===== SYNTAX =====
#   def main(lst, n)
#   return — exits the function and sends back a value
#   get_last_n_elements() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(main(lst, n)): the inner main() executes FIRST, then print() outputs the result
#   main(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   print(main(?, 15)): the inner main() executes FIRST, then print() outputs the result
#   get_last_n_elements(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   main(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   get_last_n_elements(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   get_last_n_elements(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   main(?, 15): resolves the value, gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst, n) — stored for later
#   2. Define def get_last_n_elements(lst, n) — stored for later
#   3. Assign to lst
#   4. Assign to n
#   5. Run print(main(...))
#       calls main with: lst=lst, n=n
#         5.1 Return from function
#   6. Define def main(lst, n) — stored for later
#   7. Define def main(lst, n) — stored for later
#   8. Assign to lst
#   9. Assign to n
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7], n=15
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, get_last_n_elements, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      n = ... (stored in memory)
#      print(main(lst, n)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, 15)) → function runs first, then print shows the result

"""

"""
Problem 763:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def get_positive_numbers(number) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, get_positive_numbers, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 764:
#
# ===== SYNTAX =====
#   def get_common_elements(lst1, lst2)
#   return — exits the function and sends back a value
#   get_common_elements() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: and in
#
# ===== EVALUATION ORDER =====
#   print(get_common_elements(lst1, lst2)): the inner get_common_elements() executes FIRST, then print() outputs the result
#   print(main(lst1, lst2)): the inner main() executes FIRST, then print() outputs the result
#   get_common_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(get_common_elements(?, ?)): the inner get_common_elements() executes FIRST, then print() outputs the result
#   get_common_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   get_common_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   get_common_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   word in lst2: is this True or False? The answer picks which branch
#   item in y: is this True or False? The answer picks which branch
#   item not in duplicates: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_common_elements(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(get_common_elements(...))
#       calls get_common_elements with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def main(lst1, lst2) — stored for later
#   6. Define def get_common_elements(x, y) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Run print(main(...))
#       calls main with: lst1=lst1, lst2=lst2
#         9.1 Return from function
#   10. Define def get_common_elements(lst1, lst2) — stored for later
#   11. Define def get_common_elements(lst1, lst2) — stored for later
#   12. Assign to lst1
#   13. Assign to lst2
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(get_common_elements(...))
#       calls get_common_elements with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_common_elements, main, get_common_elements, get_common_elements, get_common_elements are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(get_common_elements(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(main(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_common_elements(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 765:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   check_even_odd() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   check_even_odd(number): looks up variable 'number', then runs the function body
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def check_even_odd(n) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def check_even_odd(n) — stored for later
#   7. Assign to lst
#   8. Run print(main(...))
#       calls main with: lst=lst
#         8.1 Return from function
#   9. Define def main(lst) — stored for later
#   10. Define def main(lst) — stored for later
#   11. Assign to lst
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, check_even_odd, main, check_even_odd, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 766:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def convert_to_uppercase(word) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, convert_to_uppercase, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 767:
#
# ===== SYNTAX =====
#   def main(lst_str)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   len() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(count_characters(lst_str)): the inner count_characters() executes FIRST, then print() outputs the result
#   print(main(lst_str)): the inner main() executes FIRST, then print() outputs the result
#   main(lst_str): looks up variable 'lst_str', then runs the function body
#   print(main('world')): the inner main() executes FIRST, then print() outputs the result
#   count_characters(lst_str): looks up variable 'lst_str', then runs the function body
#   main(lst_str): looks up variable 'lst_str', then runs the function body
#   main('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst_str) — stored for later
#   2. Define def count_characters(s) — stored for later
#   3. Assign to lst_str
#   4. Run print(count_characters(...))
#       calls count_characters with: s=lst_str
#         4.1 Return from function
#   5. Define def main(lst_str) — stored for later
#   6. Define def count_characters(s) — stored for later
#   7. Assign to lst_str
#   8. Run print(main(...))
#       calls main with: lst_str=lst_str
#         8.1 Return from function
#   9. Define def main(lst_str) — stored for later
#   10. Define def main(lst_str) — stored for later
#   11. Assign to lst_str
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(main(...))
#       calls main with: lst_str='world'
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, count_characters, main, count_characters, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst_str = ... (stored in memory)
#      print(count_characters(lst_str)) → function runs first, then print shows the result
#      lst_str = ... (stored in memory)
#      print(main(lst_str)) → function runs first, then print shows the result
#      lst_str = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main('world')) → function runs first, then print shows the result

"""

"""
Problem 768:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def remove_special_characters(s) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, remove_special_characters, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 769:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   is_substring() — built-in function call
#   for
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#   is_substring(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def is_substring(s1, s2) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, is_substring, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 770:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   sum_of_list() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   sum_of_list(lst): looks up variable 'lst', then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   sum_of_list(lst): looks up variable 'lst', then runs the function body
#   sum_of_list(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def sum_of_list(n) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, sum_of_list, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 771:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(main(lst)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Define def get_even_numbers(n) — stored for later
#   3. Assign to lst
#   4. Run print(main(...))
#       calls main with: lst=lst
#         4.1 Return from function
#   5. Define def main(lst) — stored for later
#   6. Define def main(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, get_even_numbers, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(main(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 772:
#
# ===== SYNTAX =====
#   def main(lst1, lst2)
#   return — exits the function and sends back a value
#   merge_sorted_lists() — built-in function call
#   sorted() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(main(lst1, lst2)): the inner main() executes FIRST, then print() outputs the result
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(main(?, ?)): the inner main() executes FIRST, then print() outputs the result
#   merge_sorted_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   merge_sorted_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   merge_sorted_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   main(?, ?): resolves the value, resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst1, lst2) — stored for later
#   2. Define def merge_sorted_lists(x, y) — stored for later
#   3. Assign to lst1
#   4. Assign to lst2
#   5. Run print(main(...))
#       calls main with: lst1=lst1, lst2=lst2
#         5.1 Return from function
#   6. Define def main(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Run main(lst1, lst2)
#       with: lst1=lst1, lst2=lst2
#         9.1 Return from function
#   10. Define def main(lst1, lst2) — stored for later
#   11. Define def main(lst1, lst2) — stored for later
#   12. Assign to lst1
#   13. Assign to lst2
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(main(...))
#       calls main with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, merge_sorted_lists, main, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(main(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      main(lst1, lst2) → jumps into function, runs body, returns result → printed
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 773:
#
# ===== SYNTAX =====
#   def lst_to_dic(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(lst_to_dic(lst1, lst2)): the inner lst_to_dic() executes FIRST, then print() outputs the result
#   print(lst_to_dic(lst1, lst2)): the inner lst_to_dic() executes FIRST, then print() outputs the result
#   lst_to_dic(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(lst_to_dic(?, ?)): the inner lst_to_dic() executes FIRST, then print() outputs the result
#   lst_to_dic(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   lst_to_dic(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   lst_to_dic(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_to_dic(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(lst_to_dic(...))
#       calls lst_to_dic with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def lst_to_dic(lst1, lst2) — stored for later
#   6. Assign to lst1
#   7. Assign to lst2
#   8. Run print(lst_to_dic(...))
#       calls lst_to_dic with: lst1=lst1, lst2=lst2
#         8.1 Return from function
#   9. Define def lst_to_dic(lst1, lst2) — stored for later
#   10. Define def lst_to_dic(lst1, lst2) — stored for later
#   11. Assign to lst1
#   12. Assign to lst2
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(lst_to_dic(...))
#       calls lst_to_dic with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_to_dic, lst_to_dic, lst_to_dic, lst_to_dic are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(lst_to_dic(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(lst_to_dic(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_to_dic(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 774:
#
# ===== SYNTAX =====
#   def main(dic, key_to_find)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(main(dic, key_to_find)): the inner main() executes FIRST, then print() outputs the result
#   print(main(dic, key_to_find)): the inner main() executes FIRST, then print() outputs the result
#   print(value_of_specific_key(dic, target)): the inner value_of_specific_key() executes FIRST, then print() outputs the result
#   print(value_of_specific_key(dic, target)): the inner value_of_specific_key() executes FIRST, then print() outputs the result
#   main(dic, key_to_find): looks up variable 'dic', looks up variable 'key_to_find', then runs the function body
#   print(main(25, 25)): the inner main() executes FIRST, then print() outputs the result
#   main(dic, key_to_find): looks up variable 'dic', looks up variable 'key_to_find', then runs the function body
#   main(dic, key_to_find): looks up variable 'dic', looks up variable 'key_to_find', then runs the function body
#   value_of_specific_key(dic, target): looks up variable 'dic', looks up variable 'target', then runs the function body
#   value_of_specific_key(dic, target): looks up variable 'dic', looks up variable 'target', then runs the function body
#   main(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   key == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic, key_to_find) — stored for later
#   2. Assign to dic
#   3. Assign to key_to_find
#   4. Run print(main(...))
#       calls main with: dic=dic, key_to_find=key_to_find
#         4.1 Return from function
#   5. Assign to key_to_find
#   6. Run print(main(...))
#       calls main with: dic=dic, key_to_find=key_to_find
#         6.1 Return from function
#   7. Define def value_of_specific_key(dic, target) — stored for later
#   8. Assign to dic
#   9. Assign to target
#   10. Run print(value_of_specific_key(...))
#       calls value_of_specific_key with: dic=dic, target=target
#         10.1 For loop over dic.items(...)
#         10.2 Return from function
#   11. Define def value_of_specific_key(dic, target) — stored for later
#   12. Assign to dic
#   13. Assign to target
#   14. Run print(value_of_specific_key(...))
#       calls value_of_specific_key with: dic=dic, target=target
#         14.1 For loop over dic.items(...)
#         14.2 Return from function
#   15. Define def main(dic, key_to_find) — stored for later
#   16. Define def main(dic, key_to_find) — stored for later
#   17. Assign to dic
#   18. Assign to key_to_find
#   19. Assign to result
#   20. Run print(result)
#   21. Run print(main(...))
#       calls main with: dic=25, key_to_find=25
#         21.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, value_of_specific_key, value_of_specific_key, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      key_to_find = ... (stored in memory)
#      print(main(dic, key_to_find)) → function runs first, then print shows the result
#      key_to_find = ... (stored in memory)
#      print(main(dic, key_to_find)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      print(value_of_specific_key(dic, target)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      print(value_of_specific_key(dic, target)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      key_to_find = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 775:
#
# ===== SYNTAX =====
#   def main(dic1, dic2)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(main(dic1, dic2)): the inner main() executes FIRST, then print() outputs the result
#   print(merge_dicts(dic1, dic2)): the inner merge_dicts() executes FIRST, then print() outputs the result
#   main(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   print(main(25, 25)): the inner main() executes FIRST, then print() outputs the result
#   main(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   merge_dicts(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   main(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   key in merge_dic: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic1, dic2) — stored for later
#   2. Assign to dic1
#   3. Assign to dic2
#   4. Run print(main(...))
#       calls main with: dic1=dic1, dic2=dic2
#         4.1 Return from function
#   5. Define def merge_dicts(dic1, dic2) — stored for later
#   6. Assign to dic1
#   7. Assign to dic2
#   8. Run print(merge_dicts(...))
#       calls merge_dicts with: dic1=dic1, dic2=dic2
#         8.1 Assign to merged_dict
#         8.2 Return from function
#   9. Define def main(dic1, dic2) — stored for later
#   10. Define def main(dic1, dic2) — stored for later
#   11. Assign to dic1
#   12. Assign to dic2
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(main(...))
#       calls main with: dic1=25, dic2=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, merge_dicts, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(main(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(merge_dicts(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic2.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 776:
#
# ===== SYNTAX =====
#   def remove_key(dic, key_to_remove)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: != in
#
# ===== EVALUATION ORDER =====
#   print(remove_key(dic, key_to_remove)): the inner remove_key() executes FIRST, then print() outputs the result
#   print(main(dic, target)): the inner main() executes FIRST, then print() outputs the result
#   remove_key(dic, key_to_remove): looks up variable 'dic', looks up variable 'key_to_remove', then runs the function body
#   print(remove_key(?, 25)): the inner remove_key() executes FIRST, then print() outputs the result
#   remove_key(dic, key_to_remove): looks up variable 'dic', looks up variable 'key_to_remove', then runs the function body
#   main(dic, target): looks up variable 'dic', looks up variable 'target', then runs the function body
#   remove_key(?, 25): resolves the value, gets the literal 25, then runs the function body
#   key_to_remove in dic: is this True or False? The answer picks which branch
#   key != target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_key(dic, key_to_remove) — stored for later
#   2. Assign to dic
#   3. Assign to key_to_remove
#   4. Run print(remove_key(...))
#       calls remove_key with: dic=dic, key_to_remove=key_to_remove
#         4.1 Return from function
#   5. Define def main(dic, target) — stored for later
#   6. Assign to dic
#   7. Assign to target
#   8. Run print(main(...))
#       calls main with: dic=dic, target=target
#         8.1 Assign to result
#         8.2 For loop over dic.items(...)
#         8.3 Return from function
#   9. Define def remove_key(dic, key_to_remove) — stored for later
#   10. Define def remove_key(dic, key_to_remove) — stored for later
#   11. Assign to dic
#   12. Assign to key_to_remove
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(remove_key(...))
#       calls remove_key with: dic=[4, 1, 8, 6, 3], key_to_remove=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_key, main, remove_key, remove_key are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      key_to_remove = ... (stored in memory)
#      print(remove_key(dic, key_to_remove)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      print(main(dic, target)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      key_to_remove = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_key(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 778:
#
# ===== SYNTAX =====
#   def main(dic1)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   print(main(dic1)): the inner main() executes FIRST, then print() outputs the result
#   print(does_key_exist(?, 'job')): the inner does_key_exist() executes FIRST, then print() outputs the result
#   print(does_key_exist(?, 'name')): the inner does_key_exist() executes FIRST, then print() outputs the result
#   main(dic1): looks up variable 'dic1', then runs the function body
#   print(main(25)): the inner main() executes FIRST, then print() outputs the result
#   main(dic1): looks up variable 'dic1', then runs the function body
#   does_key_exist(?, 'job'): resolves the value, gets the literal 'job', then runs the function body
#   does_key_exist(?, 'name'): resolves the value, gets the literal 'name', then runs the function body
#   main(25): gets the literal 25, then runs the function body
#   target in dic1.keys(...): is this True or False? The answer picks which branch
#   key == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic1) — stored for later
#   2. Assign to dic1
#   3. Assign to target
#   4. Run print(main(...))
#       calls main with: dic1=dic1
#         4.1 Return from function
#   5. Define def does_key_exist(dic, target) — stored for later
#   6. Run print(does_key_exist(...))
#       calls does_key_exist with: dic={...}, target='job'
#         6.1 For loop over dic.keys(...)
#         6.2 Return from function
#   7. Run print(does_key_exist(...))
#       calls does_key_exist with: dic={...}, target='name'
#         7.1 For loop over dic.keys(...)
#         7.2 Return from function
#   8. Define def main(dic1) — stored for later
#   9. Define def main(dic1) — stored for later
#   10. Assign to dic1
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: dic1=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, does_key_exist, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      target = ... (stored in memory)
#      print(main(dic1)) → function runs first, then print shows the result
#      print(does_key_exist(?, 'job')) → function runs first, then print shows the result
#      print(does_key_exist(?, 'name')) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 779:
#
# ===== SYNTAX =====
#   def sort_dic(dic)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_dic(dic)): the inner sort_dic() executes FIRST, then print() outputs the result
#   sort_dic(dic): looks up variable 'dic', then runs the function body
#   print(sort_dic(?)): the inner sort_dic() executes FIRST, then print() outputs the result
#   sort_dic(dic): looks up variable 'dic', then runs the function body
#   sort_dic(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_dic(dic) — stored for later
#   2. Assign to dic
#   3. Run print(sort_dic(...))
#       calls sort_dic with: dic=dic
#         3.1 Return from function
#   4. Define def sort_dic(dic) — stored for later
#   5. Define def sort_dic(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_dic(...))
#       calls sort_dic with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_dic, sort_dic, sort_dic are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(sort_dic(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_dic(?)) → function runs first, then print shows the result

"""

"""
Problem 780:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   print(sort_values(dic)): the inner sort_values() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   sort_values(dic): looks up variable 'dic', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Assign to dic
#   3. Run print(main(...))
#       calls main with: dic=dic
#         3.1 Return from function
#   4. Define def sort_values(dic) — stored for later
#   5. Define def sort_key(item) — stored for later
#   6. Assign to dic
#   7. Run print(sort_values(...))
#       calls sort_values with: dic=dic
#         7.1 Assign to sorted_items
#         7.2 Return from function
#   8. Define def main(dic) — stored for later
#   9. Define def main(dic) — stored for later
#   10. Assign to dic
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: dic=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, sort_values, sort_key, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(sort_values(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 781:
#
# ===== SYNTAX =====
#   def frequency_elements(lst)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(frequency_elements(lst)): the inner frequency_elements() executes FIRST, then print() outputs the result
#   frequency_elements(lst): looks up variable 'lst', then runs the function body
#   print(frequency_elements(?)): the inner frequency_elements() executes FIRST, then print() outputs the result
#   frequency_elements(lst): looks up variable 'lst', then runs the function body
#   frequency_elements(?): resolves the value, then runs the function body
#   word in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def frequency_elements(lst) — stored for later
#   2. Assign to lst
#   3. Run print(frequency_elements(...))
#       calls frequency_elements with: lst=lst
#         3.1 Return from function
#   4. Define def frequency_elements(lst) — stored for later
#   5. Define def frequency_elements(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(frequency_elements(...))
#       calls frequency_elements with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions frequency_elements, frequency_elements, frequency_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(frequency_elements(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(frequency_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 782:
#
# ===== SYNTAX =====
#   def square_dic()
#   return — exits the function and sends back a value
#   range() — built-in function call
#   for
#   in
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(square_dic()): the inner square_dic() executes FIRST, then print() outputs the result
#   square_dic(): — no arguments needed
#   key ** 2: first get key, then get 2, then compute key ** 2
#   key ** 2: first get key, then get 2, then compute key ** 2
#   key ** 2: first get key, then get 2, then compute key ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_dic() — stored for later
#   2. Run print(square_dic(...))
#         2.1 Return from function
#   3. Define def square_dic() — stored for later
#   4. Define def square_dic() — stored for later
#   5. Assign to result
#   6. For loop over range(...)
#       6.1 Assign to ?
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. For loop over range(...)
#       10.1 Assign to ?
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_dic, square_dic, square_dic are just stored
#   Execution starts at the non-def lines below:
#      print(square_dic()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 783:
#
# ===== SYNTAX =====
#   def main(dic, target)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(main(dic, target)): the inner main() executes FIRST, then print() outputs the result
#   update_value(dic, 'age', 60): looks up variable 'dic', gets the literal 'age', gets the literal 60, then runs the function body
#   main(dic, target): looks up variable 'dic', looks up variable 'target', then runs the function body
#   print(main(?, 'goal')): the inner main() executes FIRST, then print() outputs the result
#   main(dic, target): looks up variable 'dic', looks up variable 'target', then runs the function body
#   main(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   key_to_update in dic: is this True or False? The answer picks which branch
#   key in dic: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic, target) — stored for later
#   2. Assign to dic
#   3. Assign to target
#   4. Run print(main(...))
#       calls main with: dic=dic, target=target
#         4.1 Return from function
#   5. Define def update_value(dic, key, new_value) — stored for later
#   6. Assign to dic
#   7. Run update_value(dic, 'age', 60)
#       with: dic=dic, key='age', new_value=60
#         7.1 If statement (one branch runs)
#         7.2 Return from function
#   8. Run print(dic)
#   9. Define def main(dic, target) — stored for later
#   10. Define def main(dic, target) — stored for later
#   11. Assign to dic
#   12. Assign to target
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(main(...))
#       calls main with: dic=[4, 1, 8, 6, 3], target='goal'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, update_value, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      print(main(dic, target)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      update_value(dic, 'age', 60) → jumps into function, runs body, returns result → printed
#      print(dic)
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, 'goal')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 784:
#
# ===== SYNTAX =====
#   def get_keys(dic)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(get_keys(dic)): the inner get_keys() executes FIRST, then print() outputs the result
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   print(get_keys(25)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   main(dic): looks up variable 'dic', then runs the function body
#   get_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(get_keys(...))
#       calls get_keys with: dic=dic
#         3.1 Return from function
#   4. Define def main(dic) — stored for later
#   5. Assign to dic
#   6. Run print(main(...))
#       calls main with: dic=dic
#         6.1 For loop over dic
#   7. Define def get_keys(dic) — stored for later
#   8. Define def get_keys(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(get_keys(...))
#       calls get_keys with: dic=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_keys, main, get_keys, get_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(get_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_keys(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic each round

"""

"""
Problem 785:
#
# ===== SYNTAX =====
#   def get_keys(dic)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(get_keys(dic)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   print(get_keys(25)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   get_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(get_keys(...))
#       calls get_keys with: dic=dic
#         3.1 Return from function
#   4. Define def get_keys(dic) — stored for later
#   5. Define def get_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_keys(...))
#       calls get_keys with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_keys, get_keys, get_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(get_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_keys(25)) → function runs first, then print shows the result

"""

"""
Problem 786:
#
# ===== SYNTAX =====
#   def get_keys(dic)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(get_keys(dic)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   print(get_keys(25)): the inner get_keys() executes FIRST, then print() outputs the result
#   get_keys(dic): looks up variable 'dic', then runs the function body
#   get_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(get_keys(...))
#       calls get_keys with: dic=dic
#         3.1 Return from function
#   4. Define def get_keys(dic) — stored for later
#   5. Define def get_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(get_keys(...))
#       calls get_keys with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_keys, get_keys, get_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(get_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_keys(25)) → function runs first, then print shows the result

"""

"""
Problem 787:
#
# ===== SYNTAX =====
#   def largest_value(dic)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(largest_value(dic)): the inner largest_value() executes FIRST, then print() outputs the result
#   largest_value(dic): looks up variable 'dic', then runs the function body
#   print(largest_value(?)): the inner largest_value() executes FIRST, then print() outputs the result
#   largest_value(dic): looks up variable 'dic', then runs the function body
#   largest_value(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_value(dic) — stored for later
#   2. Assign to dic
#   3. Run print(largest_value(...))
#       calls largest_value with: dic=dic
#         3.1 Return from function
#   4. Define def largest_value(dic) — stored for later
#   5. Define def largest_value(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(largest_value(...))
#       calls largest_value with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_value, largest_value, largest_value are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(largest_value(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_value(?)) → function runs first, then print shows the result

"""

"""
Problem 788:
#
# ===== SYNTAX =====
#   def largest_value(dic)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(largest_value(dic)): the inner largest_value() executes FIRST, then print() outputs the result
#   largest_value(dic): looks up variable 'dic', then runs the function body
#   print(largest_value(?)): the inner largest_value() executes FIRST, then print() outputs the result
#   largest_value(dic): looks up variable 'dic', then runs the function body
#   largest_value(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_value(dic) — stored for later
#   2. Assign to dic
#   3. Run print(largest_value(...))
#       calls largest_value with: dic=dic
#         3.1 Return from function
#   4. Define def largest_value(dic) — stored for later
#   5. Define def largest_value(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(largest_value(...))
#       calls largest_value with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_value, largest_value, largest_value are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(largest_value(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_value(?)) → function runs first, then print shows the result

"""

"""
Problem 789:
#
# ===== SYNTAX =====
#   def lst_dic(lst1, lst2)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(lst_dic(lst1, lst2)): the inner lst_dic() executes FIRST, then print() outputs the result
#   lst_dic(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(lst_dic(?, ?)): the inner lst_dic() executes FIRST, then print() outputs the result
#   lst_dic(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   lst_dic(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_dic(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(lst_dic(...))
#       calls lst_dic with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def lst_dic(lst1, lst2) — stored for later
#   6. Define def lst_dic(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(lst_dic(...))
#       calls lst_dic with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_dic, lst_dic, lst_dic are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(lst_dic(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_dic(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 790:
#
# ===== SYNTAX =====
#   def combine_dictionaries(dic1, dic2)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(combine_dictionaries(dic1, dic2)): the inner combine_dictionaries() executes FIRST, then print() outputs the result
#   print(combine_dictionaries(dic1, dic2)): the inner combine_dictionaries() executes FIRST, then print() outputs the result
#   combine_dictionaries(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   print(combine_dictionaries(?, 25)): the inner combine_dictionaries() executes FIRST, then print() outputs the result
#   combine_dictionaries(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   combine_dictionaries(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   combine_dictionaries(?, 25): resolves the value, gets the literal 25, then runs the function body
#   key in result: is this True or False? The answer picks which branch
#   key in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def combine_dictionaries(dic1, dic2) — stored for later
#   2. Assign to dic1
#   3. Assign to dic2
#   4. Run print(combine_dictionaries(...))
#       calls combine_dictionaries with: dic1=dic1, dic2=dic2
#         4.1 Return from function
#   5. Define def combine_dictionaries(dic1, dic2) — stored for later
#   6. Assign to dic1
#   7. Assign to dic2
#   8. Run print(combine_dictionaries(...))
#       calls combine_dictionaries with: dic1=dic1, dic2=dic2
#         8.1 Return from function
#   9. Define def combine_dictionaries(dic1, dic2) — stored for later
#   10. Define def combine_dictionaries(dic1, dic2) — stored for later
#   11. Assign to dic1
#   12. Assign to dic2
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(combine_dictionaries(...))
#       calls combine_dictionaries with: dic1=[4, 1, 8, 6, 3], dic2=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions combine_dictionaries, combine_dictionaries, combine_dictionaries, combine_dictionaries are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(combine_dictionaries(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(combine_dictionaries(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(combine_dictionaries(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 791:
#
# ===== SYNTAX =====
#   def invert_dictionary(dic)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(invert_dictionary(dic)): the inner invert_dictionary() executes FIRST, then print() outputs the result
#   invert_dictionary(dic): looks up variable 'dic', then runs the function body
#   print(invert_dictionary(25)): the inner invert_dictionary() executes FIRST, then print() outputs the result
#   invert_dictionary(dic): looks up variable 'dic', then runs the function body
#   invert_dictionary(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def invert_dictionary(dic) — stored for later
#   2. Assign to dic
#   3. Run print(invert_dictionary(...))
#       calls invert_dictionary with: dic=dic
#         3.1 Return from function
#   4. Define def invert_dictionary(dic) — stored for later
#   5. Define def invert_dictionary(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(invert_dictionary(...))
#       calls invert_dictionary with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions invert_dictionary, invert_dictionary, invert_dictionary are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(invert_dictionary(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(invert_dictionary(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round

"""

"""
Problem 792:
#
# ===== SYNTAX =====
#   def remove_duplicates_values(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates_values(dic)): the inner remove_duplicates_values() executes FIRST, then print() outputs the result
#   remove_duplicates_values(dic): looks up variable 'dic', then runs the function body
#   print(remove_duplicates_values(25)): the inner remove_duplicates_values() executes FIRST, then print() outputs the result
#   remove_duplicates_values(dic): looks up variable 'dic', then runs the function body
#   remove_duplicates_values(25): gets the literal 25, then runs the function body
#   value not in unique_values.values(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates_values(dic) — stored for later
#   2. Assign to dic
#   3. Run print(remove_duplicates_values(...))
#       calls remove_duplicates_values with: dic=dic
#         3.1 Return from function
#   4. Define def remove_duplicates_values(dic) — stored for later
#   5. Define def remove_duplicates_values(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_duplicates_values(...))
#       calls remove_duplicates_values with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates_values, remove_duplicates_values, remove_duplicates_values are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(remove_duplicates_values(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates_values(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 793:
#
# ===== SYNTAX =====
#   def same_keys(dic1, dic2)
#   return — exits the function and sends back a value
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(same_keys(dic1, dic2)): the inner same_keys() executes FIRST, then print() outputs the result
#   same_keys(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   print(same_keys(25, 25)): the inner same_keys() executes FIRST, then print() outputs the result
#   same_keys(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   same_keys(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def same_keys(dic1, dic2) — stored for later
#   2. Assign to dic1
#   3. Assign to dic2
#   4. Run print(same_keys(...))
#       calls same_keys with: dic1=dic1, dic2=dic2
#         4.1 Return from function
#   5. Define def same_keys(dic1, dic2) — stored for later
#   6. Define def same_keys(dic1, dic2) — stored for later
#   7. Assign to dic1
#   8. Assign to dic2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(same_keys(...))
#       calls same_keys with: dic1=25, dic2=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions same_keys, same_keys, same_keys are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(same_keys(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(same_keys(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 794:
#
# ===== SYNTAX =====
#   def multiply_by2(dic)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(multiply_by2(dic)): the inner multiply_by2() executes FIRST, then print() outputs the result
#   multiply_by2(dic): looks up variable 'dic', then runs the function body
#   print(multiply_by2(25)): the inner multiply_by2() executes FIRST, then print() outputs the result
#   multiply_by2(dic): looks up variable 'dic', then runs the function body
#   multiply_by2(25): gets the literal 25, then runs the function body
#   value * 2: first get value, then get 2, then compute value * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_by2(dic) — stored for later
#   2. Assign to dic
#   3. Run print(multiply_by2(...))
#       calls multiply_by2 with: dic=dic
#         3.1 Return from function
#   4. Define def multiply_by2(dic) — stored for later
#   5. Define def multiply_by2(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(multiply_by2(...))
#       calls multiply_by2 with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_by2, multiply_by2, multiply_by2 are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(multiply_by2(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_by2(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round

"""

"""
Problem 795:
#
# ===== SYNTAX =====
#   def len_key_value_pair(dic)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(len_key_value_pair(dic)): the inner len_key_value_pair() executes FIRST, then print() outputs the result
#   len_key_value_pair(dic): looks up variable 'dic', then runs the function body
#   print(len_key_value_pair(25)): the inner len_key_value_pair() executes FIRST, then print() outputs the result
#   len_key_value_pair(dic): looks up variable 'dic', then runs the function body
#   len_key_value_pair(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def len_key_value_pair(dic) — stored for later
#   2. Assign to dic
#   3. Run print(len_key_value_pair(...))
#       calls len_key_value_pair with: dic=dic
#         3.1 Return from function
#   4. Define def len_key_value_pair(dic) — stored for later
#   5. Define def len_key_value_pair(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(len_key_value_pair(...))
#       calls len_key_value_pair with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions len_key_value_pair, len_key_value_pair, len_key_value_pair are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(len_key_value_pair(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(len_key_value_pair(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic each round

"""

"""
Problem 796:
#
# ===== SYNTAX =====
#   def map_lst_string_dic(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(map_lst_string_dic(lst)): the inner map_lst_string_dic() executes FIRST, then print() outputs the result
#   print(len_dic(lst)): the inner len_dic() executes FIRST, then print() outputs the result
#   map_lst_string_dic(lst): looks up variable 'lst', then runs the function body
#   print(map_lst_string_dic(?)): the inner map_lst_string_dic() executes FIRST, then print() outputs the result
#   map_lst_string_dic(lst): looks up variable 'lst', then runs the function body
#   len_dic(lst): looks up variable 'lst', then runs the function body
#   map_lst_string_dic(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def map_lst_string_dic(lst) — stored for later
#   2. Assign to lst
#   3. Run print(map_lst_string_dic(...))
#       calls map_lst_string_dic with: lst=lst
#         3.1 Return from function
#   4. Define def len_dic(lst) — stored for later
#   5. Assign to lst
#   6. Run print(len_dic(...))
#       calls len_dic with: lst=lst
#         6.1 Assign to result
#         6.2 For loop over lst
#         6.3 Return from function
#   7. Define def map_lst_string_dic(lst) — stored for later
#   8. Define def map_lst_string_dic(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(map_lst_string_dic(...))
#       calls map_lst_string_dic with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions map_lst_string_dic, len_dic, map_lst_string_dic, map_lst_string_dic are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(map_lst_string_dic(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(len_dic(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(map_lst_string_dic(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 797:
#
# ===== SYNTAX =====
#   def add_key_value_pair(dic)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(add_key_value_pair(dic)): the inner add_key_value_pair() executes FIRST, then print() outputs the result
#   add_key_value_pair(dic): looks up variable 'dic', then runs the function body
#   print(add_key_value_pair(?)): the inner add_key_value_pair() executes FIRST, then print() outputs the result
#   add_key_value_pair(dic): looks up variable 'dic', then runs the function body
#   add_key_value_pair(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add_key_value_pair(dic) — stored for later
#   2. Assign to dic
#   3. Run print(add_key_value_pair(...))
#       calls add_key_value_pair with: dic=dic
#         3.1 Return from function
#   4. Define def add_key_value_pair(dic) — stored for later
#   5. Define def add_key_value_pair(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(add_key_value_pair(...))
#       calls add_key_value_pair with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_key_value_pair, add_key_value_pair, add_key_value_pair are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(add_key_value_pair(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_key_value_pair(?)) → function runs first, then print shows the result

"""

"""
Problem 798:
#
# ===== SYNTAX =====
#   def value_cubes()
#   return — exits the function and sends back a value
#   range() — built-in function call
#   for
#   in
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(value_cubes()): the inner value_cubes() executes FIRST, then print() outputs the result
#   value_cubes(): — no arguments needed
#   num ** 3: first get num, then get 3, then compute num ** 3
#   num ** 3: first get num, then get 3, then compute num ** 3
#   num ** 3: first get num, then get 3, then compute num ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def value_cubes() — stored for later
#   2. Run print(value_cubes(...))
#         2.1 Return from function
#   3. Define def value_cubes() — stored for later
#   4. Define def value_cubes() — stored for later
#   5. Assign to result
#   6. For loop over range(...)
#       6.1 Assign to ?
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. For loop over range(...)
#       10.1 Assign to ?
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions value_cubes, value_cubes, value_cubes are just stored
#   Execution starts at the non-def lines below:
#      print(value_cubes()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 799:
#
# ===== SYNTAX =====
#   def check_if_empty(dic)
#   return — exits the function and sends back a value
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   print(check_if_empty(dic)): the inner check_if_empty() executes FIRST, then print() outputs the result
#   check_if_empty(dic): looks up variable 'dic', then runs the function body
#   print(check_if_empty(25)): the inner check_if_empty() executes FIRST, then print() outputs the result
#   check_if_empty(dic): looks up variable 'dic', then runs the function body
#   check_if_empty(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_if_empty(dic) — stored for later
#   2. Assign to dic
#   3. Run print(check_if_empty(...))
#       calls check_if_empty with: dic=dic
#         3.1 Return from function
#   4. Define def check_if_empty(dic) — stored for later
#   5. Define def check_if_empty(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_if_empty(...))
#       calls check_if_empty with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_if_empty, check_if_empty, check_if_empty are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(check_if_empty(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_if_empty(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 800:
#
# ===== SYNTAX =====
#   def remove_set_keys(dic, keys_to_remove)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(remove_set_keys(dic, keys_to_remove)): the inner remove_set_keys() executes FIRST, then print() outputs the result
#   remove_set_keys(dic, keys_to_remove): looks up variable 'dic', looks up variable 'keys_to_remove', then runs the function body
#   print(remove_set_keys(25, 25)): the inner remove_set_keys() executes FIRST, then print() outputs the result
#   remove_set_keys(dic, keys_to_remove): looks up variable 'dic', looks up variable 'keys_to_remove', then runs the function body
#   remove_set_keys(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   key not in keys_to_remove: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_set_keys(dic, keys_to_remove) — stored for later
#   2. Assign to dic
#   3. Assign to keys_to_remove
#   4. Run print(remove_set_keys(...))
#       calls remove_set_keys with: dic=dic, keys_to_remove=keys_to_remove
#         4.1 Return from function
#   5. Define def remove_set_keys(dic, keys_to_remove) — stored for later
#   6. Define def remove_set_keys(dic, keys_to_remove) — stored for later
#   7. Assign to dic
#   8. Assign to keys_to_remove
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(remove_set_keys(...))
#       calls remove_set_keys with: dic=25, keys_to_remove=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_set_keys, remove_set_keys, remove_set_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      keys_to_remove = ... (stored in memory)
#      print(remove_set_keys(dic, keys_to_remove)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      keys_to_remove = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_set_keys(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 801:
#
# ===== SYNTAX =====
#   def dic_lst_tuples(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(dic_lst_tuples(dic)): the inner dic_lst_tuples() executes FIRST, then print() outputs the result
#   dic_lst_tuples(dic): looks up variable 'dic', then runs the function body
#   print(dic_lst_tuples(25)): the inner dic_lst_tuples() executes FIRST, then print() outputs the result
#   dic_lst_tuples(dic): looks up variable 'dic', then runs the function body
#   dic_lst_tuples(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def dic_lst_tuples(dic) — stored for later
#   2. Assign to dic
#   3. Run print(dic_lst_tuples(...))
#       calls dic_lst_tuples with: dic=dic
#         3.1 Return from function
#   4. Define def dic_lst_tuples(dic) — stored for later
#   5. Define def dic_lst_tuples(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(dic_lst_tuples(...))
#       calls dic_lst_tuples with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions dic_lst_tuples, dic_lst_tuples, dic_lst_tuples are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(dic_lst_tuples(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(dic_lst_tuples(25)) → function runs first, then print shows the result

"""

"""
Problem 802:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#   tuple() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   print(map_value(dic, target_value)): the inner map_value() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(25)): the inner main() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   map_value(dic, target_value): looks up variable 'dic', looks up variable 'target_value', then runs the function body
#   main(25): gets the literal 25, then runs the function body
#   value == 1: is this True or False? The answer picks which branch
#   value == target_value: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Assign to dic
#   3. Run print(main(...))
#       calls main with: dic=dic
#         3.1 Return from function
#   4. Define def map_value(dic, target_value) — stored for later
#   5. Assign to dic
#   6. Assign to target_value
#   7. Run print(map_value(...))
#       calls map_value with: dic=dic, target_value=target_value
#         7.1 Assign to result
#         7.2 For loop over dic.items(...)
#         7.3 Return from function
#   8. Define def main(dic) — stored for later
#   9. Define def main(dic) — stored for later
#   10. Assign to dic
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: dic=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, map_value, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      target_value = ... (stored in memory)
#      print(map_value(dic, target_value)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 803:
#
# ===== SYNTAX =====
#   def replace_value(dic, list_values)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(replace_value(dic, list_values)): the inner replace_value() executes FIRST, then print() outputs the result
#   replace_value(dic, list_values): looks up variable 'dic', looks up variable 'list_values', then runs the function body
#   print(replace_value(?, 25)): the inner replace_value() executes FIRST, then print() outputs the result
#   replace_value(dic, list_values): looks up variable 'dic', looks up variable 'list_values', then runs the function body
#   replace_value(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def replace_value(dic, list_values) — stored for later
#   2. Assign to dic
#   3. Assign to list_values
#   4. Run print(replace_value(...))
#       calls replace_value with: dic=dic, list_values=list_values
#         4.1 Return from function
#   5. Define def replace_value(dic, list_values) — stored for later
#   6. Define def replace_value(dic, list_values) — stored for later
#   7. Assign to dic
#   8. Assign to list_values
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(replace_value(...))
#       calls replace_value with: dic=[4, 1, 8, 6, 3], list_values=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions replace_value, replace_value, replace_value are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      list_values = ... (stored in memory)
#      print(replace_value(dic, list_values)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      list_values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(replace_value(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 804:
#
# ===== SYNTAX =====
#   def nested_dic()
#   return — exits the function and sends back a value
#   range() — built-in function call
#   for
#   in
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(nested_dic()): the inner nested_dic() executes FIRST, then print() outputs the result
#   nested_dic(): — no arguments needed
#   nested_dic(): — no arguments needed
#   key ** 2: first get key, then get 2, then compute key ** 2
#   key ** 3: first get key, then get 3, then compute key ** 3
#   key ** 2: first get key, then get 2, then compute key ** 2
#   key ** 3: first get key, then get 3, then compute key ** 3
#   key ** 2: first get key, then get 2, then compute key ** 2
#   key ** 3: first get key, then get 3, then compute key ** 3
#   key ** 2: first get key, then get 2, then compute key ** 2
#   key ** 3: first get key, then get 3, then compute key ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def nested_dic() — stored for later
#   2. Run print(nested_dic(...))
#         2.1 Return from function
#   3. Define def nested_dic() — stored for later
#   4. Assign to result
#   5. For loop over result.items(...)
#       5.1 Run print(?)
#   6. Define def nested_dic() — stored for later
#   7. Define def nested_dic() — stored for later
#   8. Assign to result
#   9. For loop over range(...)
#       9.1 Assign to ?
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to result
#   13. For loop over range(...)
#       13.1 Assign to ?
#   14. Assign to result
#   15. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions nested_dic, nested_dic, nested_dic, nested_dic are just stored
#   Execution starts at the non-def lines below:
#      print(nested_dic()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from result.items(...) each round

"""

"""
Problem 806:
#
# ===== SYNTAX =====
#   def sort_dic(dic)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_dic(dic)): the inner sort_dic() executes FIRST, then print() outputs the result
#   sort_dic(dic): looks up variable 'dic', then runs the function body
#   print(sort_dic(?)): the inner sort_dic() executes FIRST, then print() outputs the result
#   sort_dic(dic): looks up variable 'dic', then runs the function body
#   sort_dic(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_dic(dic) — stored for later
#   2. Assign to dic
#   3. Run print(sort_dic(...))
#       calls sort_dic with: dic=dic
#         3.1 Return from function
#   4. Define def sort_dic(dic) — stored for later
#   5. Define def sort_dic(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_dic(...))
#       calls sort_dic with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_dic, sort_dic, sort_dic are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(sort_dic(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_dic(?)) → function runs first, then print shows the result

"""

"""
Problem 807:
#
# ===== SYNTAX =====
#   def lst_tuples(lst)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(lst_tuples(lst)): the inner lst_tuples() executes FIRST, then print() outputs the result
#   lst_tuples(lst): looks up variable 'lst', then runs the function body
#   print(lst_tuples(?)): the inner lst_tuples() executes FIRST, then print() outputs the result
#   lst_tuples(lst): looks up variable 'lst', then runs the function body
#   lst_tuples(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lst_tuples(lst) — stored for later
#   2. Assign to lst
#   3. Run print(lst_tuples(...))
#       calls lst_tuples with: lst=lst
#         3.1 Return from function
#   4. Define def lst_tuples(lst) — stored for later
#   5. Define def lst_tuples(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(lst_tuples(...))
#       calls lst_tuples with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lst_tuples, lst_tuples, lst_tuples are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst_tuples(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lst_tuples(?)) → function runs first, then print shows the result

"""

"""
Problem 808:
#
# ===== SYNTAX =====
#   def less_then_10(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   print(less_then_10(dic)): the inner less_then_10() executes FIRST, then print() outputs the result
#   less_then_10(dic): looks up variable 'dic', then runs the function body
#   print(less_then_10(25)): the inner less_then_10() executes FIRST, then print() outputs the result
#   less_then_10(dic): looks up variable 'dic', then runs the function body
#   less_then_10(25): gets the literal 25, then runs the function body
#   value >= 10: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def less_then_10(dic) — stored for later
#   2. Assign to dic
#   3. Run print(less_then_10(...))
#       calls less_then_10 with: dic=dic
#         3.1 Return from function
#   4. Define def less_then_10(dic) — stored for later
#   5. Define def less_then_10(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(less_then_10(...))
#       calls less_then_10 with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions less_then_10, less_then_10, less_then_10 are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(less_then_10(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(less_then_10(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 810:
#
# ===== SYNTAX =====
#   def students_grades(dic)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   print(students_grades(dic)): the inner students_grades() executes FIRST, then print() outputs the result
#   students_grades(dic): looks up variable 'dic', then runs the function body
#   print(students_grades(25)): the inner students_grades() executes FIRST, then print() outputs the result
#   students_grades(dic): looks up variable 'dic', then runs the function body
#   students_grades(25): gets the literal 25, then runs the function body
#   value >= 70: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def students_grades(dic) — stored for later
#   2. Assign to dic
#   3. Run print(students_grades(...))
#       calls students_grades with: dic=dic
#         3.1 Return from function
#   4. Define def students_grades(dic) — stored for later
#   5. Define def students_grades(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(students_grades(...))
#       calls students_grades with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions students_grades, students_grades, students_grades are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(students_grades(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(students_grades(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 811:
#
# ===== SYNTAX =====
#   def word_len(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(word_len(string)): the inner word_len() executes FIRST, then print() outputs the result
#   word_len(string): looks up variable 'string', then runs the function body
#   print(word_len('world')): the inner word_len() executes FIRST, then print() outputs the result
#   word_len(string): looks up variable 'string', then runs the function body
#   word_len('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def word_len(string) — stored for later
#   2. Assign to string
#   3. Run print(word_len(...))
#       calls word_len with: string=string
#         3.1 Return from function
#   4. Define def word_len(string) — stored for later
#   5. Define def word_len(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(word_len(...))
#       calls word_len with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions word_len, word_len, word_len are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(word_len(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(word_len('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 812:
#
# ===== SYNTAX =====
#   def word_len(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(word_len(string)): the inner word_len() executes FIRST, then print() outputs the result
#   word_len(string): looks up variable 'string', then runs the function body
#   print(word_len('world')): the inner word_len() executes FIRST, then print() outputs the result
#   word_len(string): looks up variable 'string', then runs the function body
#   word_len('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def word_len(string) — stored for later
#   2. Assign to string
#   3. Run print(word_len(...))
#       calls word_len with: string=string
#         3.1 Return from function
#   4. Define def word_len(string) — stored for later
#   5. Define def word_len(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(word_len(...))
#       calls word_len with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions word_len, word_len, word_len are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(word_len(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(word_len('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 813:
#
# ===== SYNTAX =====
#   def starting_letter(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(starting_letter(dic)): the inner starting_letter() executes FIRST, then print() outputs the result
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   starting_letter(dic): looks up variable 'dic', then runs the function body
#   print(starting_letter(?)): the inner starting_letter() executes FIRST, then print() outputs the result
#   starting_letter(dic): looks up variable 'dic', then runs the function body
#   main(dic): looks up variable 'dic', then runs the function body
#   starting_letter(?): resolves the value, then runs the function body
#   first_letter not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def starting_letter(dic) — stored for later
#   2. Assign to dic
#   3. Run print(starting_letter(...))
#       calls starting_letter with: dic=dic
#         3.1 Return from function
#   4. Define def main(dic) — stored for later
#   5. Assign to dic
#   6. Run print(main(...))
#       calls main with: dic=dic
#         6.1 Assign to result
#         6.2 For loop over dic.items(...)
#         6.3 Return from function
#   7. Define def starting_letter(dic) — stored for later
#   8. Define def starting_letter(dic) — stored for later
#   9. Assign to dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(starting_letter(...))
#       calls starting_letter with: dic=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions starting_letter, main, starting_letter, starting_letter are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(starting_letter(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(starting_letter(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.keys(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 814:
#
# ===== SYNTAX =====
#   def upper_keys(dic)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(upper_keys(dic)): the inner upper_keys() executes FIRST, then print() outputs the result
#   upper_keys(dic): looks up variable 'dic', then runs the function body
#   print(upper_keys(25)): the inner upper_keys() executes FIRST, then print() outputs the result
#   upper_keys(dic): looks up variable 'dic', then runs the function body
#   upper_keys(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def upper_keys(dic) — stored for later
#   2. Assign to dic
#   3. Run print(upper_keys(...))
#       calls upper_keys with: dic=dic
#         3.1 Return from function
#   4. Define def upper_keys(dic) — stored for later
#   5. Define def upper_keys(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(upper_keys(...))
#       calls upper_keys with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions upper_keys, upper_keys, upper_keys are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(upper_keys(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(upper_keys(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round

"""

"""
Problem 815:
#
# ===== SYNTAX =====
#   def intersection(dic1, dic2)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(intersection(dic1, dic2)): the inner intersection() executes FIRST, then print() outputs the result
#   intersection(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   print(intersection(?, 25)): the inner intersection() executes FIRST, then print() outputs the result
#   intersection(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   intersection(?, 25): resolves the value, gets the literal 25, then runs the function body
#   key in dic2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def intersection(dic1, dic2) — stored for later
#   2. Assign to dic1
#   3. Assign to dic2
#   4. Run print(intersection(...))
#       calls intersection with: dic1=dic1, dic2=dic2
#         4.1 Return from function
#   5. Define def intersection(dic1, dic2) — stored for later
#   6. Define def intersection(dic1, dic2) — stored for later
#   7. Assign to dic1
#   8. Assign to dic2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(intersection(...))
#       calls intersection with: dic1=[4, 1, 8, 6, 3], dic2=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions intersection, intersection, intersection are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(intersection(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(intersection(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 816:
#
# ===== SYNTAX =====
#   def count_vowels(string)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(count_vowels(string)): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(string): looks up variable 'string', then runs the function body
#   print(count_vowels('world')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(string): looks up variable 'string', then runs the function body
#   count_vowels('world'): gets the literal 'world', then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(string) — stored for later
#   2. Assign to string
#   3. Run print(count_vowels(...))
#       calls count_vowels with: string=string
#         3.1 Return from function
#   4. Define def count_vowels(string) — stored for later
#   5. Define def count_vowels(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_vowels(...))
#       calls count_vowels with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_vowels(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from vowels each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 817:
#
# ===== SYNTAX =====
#   def remove_value_is_none(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(remove_value_is_none(dic1)): the inner remove_value_is_none() executes FIRST, then print() outputs the result
#   remove_value_is_none(dic): looks up variable 'dic', then runs the function body
#   print(remove_value_is_none(25)): the inner remove_value_is_none() executes FIRST, then print() outputs the result
#   remove_value_is_none(dic1): looks up variable 'dic1', then runs the function body
#   remove_value_is_none(25): gets the literal 25, then runs the function body
#   value  None: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_value_is_none(dic) — stored for later
#   2. Assign to dic1
#   3. Run print(remove_value_is_none(...))
#       calls remove_value_is_none with: dic=dic1
#         3.1 Return from function
#   4. Define def remove_value_is_none(dic) — stored for later
#   5. Define def remove_value_is_none(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_value_is_none(...))
#       calls remove_value_is_none with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_value_is_none, remove_value_is_none, remove_value_is_none are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      print(remove_value_is_none(dic1)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_value_is_none(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 818:
#
# ===== SYNTAX =====
#   def check_key_value(dic, target)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: == and in
#
# ===== EVALUATION ORDER =====
#   print(check_key_value(dic1, target)): the inner check_key_value() executes FIRST, then print() outputs the result
#   check_key_value(dic, target): looks up variable 'dic', looks up variable 'target', then runs the function body
#   print(check_key_value(?, 'goal')): the inner check_key_value() executes FIRST, then print() outputs the result
#   check_key_value(dic1, target): looks up variable 'dic1', looks up variable 'target', then runs the function body
#   check_key_value(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   key in target: is this True or False? The answer picks which branch
#   ? == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_key_value(dic, target) — stored for later
#   2. Assign to dic1
#   3. Assign to target
#   4. Run print(check_key_value(...))
#       calls check_key_value with: dic=dic1, target=target
#         4.1 Return from function
#   5. Define def check_key_value(dic, target) — stored for later
#   6. Define def check_key_value(dic, target) — stored for later
#   7. Assign to dic
#   8. Assign to target
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(check_key_value(...))
#       calls check_key_value with: dic=[4, 1, 8, 6, 3], target='goal'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_key_value, check_key_value, check_key_value are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      target = ... (stored in memory)
#      print(check_key_value(dic1, target)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_key_value(?, 'goal')) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 819:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 820:
#
# ===== SYNTAX =====
#   def merge_dicts(dic1, dic2)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(merge_dicts(dic1, dic2)): the inner merge_dicts() executes FIRST, then print() outputs the result
#   merge_dicts(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   print(merge_dicts(25, 25)): the inner merge_dicts() executes FIRST, then print() outputs the result
#   merge_dicts(dic1, dic2): looks up variable 'dic1', looks up variable 'dic2', then runs the function body
#   merge_dicts(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_dicts(dic1, dic2) — stored for later
#   2. Assign to dic1
#   3. Assign to dic2
#   4. Run print(merge_dicts(...))
#       calls merge_dicts with: dic1=dic1, dic2=dic2
#         4.1 Return from function
#   5. Define def merge_dicts(dic1, dic2) — stored for later
#   6. Define def merge_dicts(dic1, dic2) — stored for later
#   7. Assign to dic1
#   8. Assign to dic2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(merge_dicts(...))
#       calls merge_dicts with: dic1=25, dic2=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_dicts, merge_dicts, merge_dicts are just stored
#   Execution starts at the non-def lines below:
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      print(merge_dicts(dic1, dic2)) → function runs first, then print shows the result
#      dic1 = ... (stored in memory)
#      dic2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_dicts(25, 25)) → function runs first, then print shows the result

"""

"""
Problem 821:
#
# ===== SYNTAX =====
#   def product_values(dic)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(product_values(dic)): the inner product_values() executes FIRST, then print() outputs the result
#   product_values(dic): looks up variable 'dic', then runs the function body
#   print(product_values(?)): the inner product_values() executes FIRST, then print() outputs the result
#   product_values(dic): looks up variable 'dic', then runs the function body
#   product_values(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def product_values(dic) — stored for later
#   2. Assign to dic
#   3. Run print(product_values(...))
#       calls product_values with: dic=dic
#         3.1 Return from function
#   4. Define def product_values(dic) — stored for later
#   5. Define def product_values(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(product_values(...))
#       calls product_values with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions product_values, product_values, product_values are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(product_values(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(product_values(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.values(...) each round

"""

"""
Problem 822:
#
# ===== SYNTAX =====
#   def average_values(dic)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(average_values(dic)): the inner average_values() executes FIRST, then print() outputs the result
#   average_values(dic): looks up variable 'dic', then runs the function body
#   print(average_values(?)): the inner average_values() executes FIRST, then print() outputs the result
#   sum_values / len_values: first get sum_values, then get len_values, then compute sum_values / len_values
#   average_values(dic): looks up variable 'dic', then runs the function body
#   sum_values * ?: first get sum_values, then get ?, then compute sum_values * ?
#   average_values(?): resolves the value, then runs the function body
#   1 / len_values: first get 1, then get len_values, then compute 1 / len_values
#
# ===== EXECUTION ORDER =====
#   1. Define def average_values(dic) — stored for later
#   2. Assign to dic
#   3. Run print(average_values(...))
#       calls average_values with: dic=dic
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def average_values(dic) — stored for later
#   5. Define def average_values(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(average_values(...))
#       calls average_values with: dic=[4, 1, 8, 6, 3]
#         9.1 Import Fraction from fractions
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_values, average_values, average_values are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(average_values(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_values(?)) → function runs first, then print shows the result

"""

"""
Problem 823:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   print(main(dic, 'name', 'job')): the inner main() executes FIRST, then print() outputs the result
#   print(main(dic, change_keys)): the inner main() executes FIRST, then print() outputs the result
#   print(swap_values(dic, 'a', 'c')): the inner swap_values() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   main(dic, 'name', 'job'): looks up variable 'dic', gets the literal 'name', gets the literal 'job', then runs the function body
#   main(dic, change_keys): looks up variable 'dic', looks up variable 'change_keys', then runs the function body
#   swap_values(dic, 'a', 'c'): looks up variable 'dic', gets the literal 'a', gets the literal 'c', then runs the function body
#   main(?): resolves the value, then runs the function body
#   reverse_values not in result: is this True or False? The answer picks which branch
#   change_keys not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Assign to dic
#   3. Assign to target
#   4. Run print(main(...))
#       calls main with: dic=dic
#         4.1 Return from function
#   5. Define def main(dic, x, y) — stored for later
#   6. Assign to dic
#   7. Run print(main(...))
#       calls main with: dic=dic
#         7.1 Return from function
#   8. Define def main(dic, change_keys) — stored for later
#   9. Assign to dic
#   10. Assign to change_keys
#   11. Run print(main(...))
#       calls main with: dic=dic
#         11.1 Return from function
#   12. Define def swap_values(dic, key1, key2) — stored for later
#   13. Assign to dic
#   14. Run print(swap_values(...))
#       calls swap_values with: dic=dic, key1='a', key2='c'
#         14.1 Assign to ?
#         14.2 Return from function
#   15. Define def main(dic) — stored for later
#   16. Define def main(dic) — stored for later
#   17. Assign to dic
#   18. Assign to result
#   19. Run print(result)
#   20. Run print(main(...))
#       calls main with: dic=[4, 1, 8, 6, 3]
#         20.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, main, main, swap_values, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      target = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(main(dic, 'name', 'job')) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      change_keys = ... (stored in memory)
#      print(main(dic, change_keys)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      print(swap_values(dic, 'a', 'c')) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 824:
#
# ===== SYNTAX =====
#   def largest_value(dic)
#   return — exits the function and sends back a value
#   float() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(largest_value(dic)): the inner largest_value() executes FIRST, then print() outputs the result
#   largest_value(dic): looks up variable 'dic', then runs the function body
#   print(largest_value(25)): the inner largest_value() executes FIRST, then print() outputs the result
#   largest_value(dic): looks up variable 'dic', then runs the function body
#   largest_value(25): gets the literal 25, then runs the function body
#   value > largest_val: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_value(dic) — stored for later
#   2. Assign to dic
#   3. Run print(largest_value(...))
#       calls largest_value with: dic=dic
#         3.1 Return from function
#   4. Define def largest_value(dic) — stored for later
#   5. Define def largest_value(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(largest_value(...))
#       calls largest_value with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_value, largest_value, largest_value are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(largest_value(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_value(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 825:
#
# ===== SYNTAX =====
#   def smallest_value(dic)
#   return — exits the function and sends back a value
#   float() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(smallest_value(dic)): the inner smallest_value() executes FIRST, then print() outputs the result
#   smallest_value(dic): looks up variable 'dic', then runs the function body
#   print(smallest_value(25)): the inner smallest_value() executes FIRST, then print() outputs the result
#   smallest_value(dic): looks up variable 'dic', then runs the function body
#   smallest_value(25): gets the literal 25, then runs the function body
#   value < smallest_val: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def smallest_value(dic) — stored for later
#   2. Assign to dic
#   3. Run print(smallest_value(...))
#       calls smallest_value with: dic=dic
#         3.1 Return from function
#   4. Define def smallest_value(dic) — stored for later
#   5. Define def smallest_value(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(smallest_value(...))
#       calls smallest_value with: dic=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions smallest_value, smallest_value, smallest_value are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(smallest_value(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(smallest_value(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dic.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 826:
#
# ===== SYNTAX =====
#   def main(dic)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(main(dic)): the inner main() executes FIRST, then print() outputs the result
#   print(add_multiple_pairs(dic, new_pairs)): the inner add_multiple_pairs() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(dic): looks up variable 'dic', then runs the function body
#   add_multiple_pairs(dic, new_pairs): looks up variable 'dic', looks up variable 'new_pairs', then runs the function body
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(dic) — stored for later
#   2. Assign to dic
#   3. Run print(main(...))
#       calls main with: dic=dic
#         3.1 Return from function
#   4. Define def add_multiple_pairs(dic, new_pairs) — stored for later
#   5. Assign to dic
#   6. Assign to new_pairs
#   7. Run print(add_multiple_pairs(...))
#       calls add_multiple_pairs with: dic=dic, new_pairs=new_pairs
#         7.1 Run dic.update(new_pairs)
#         7.2 Return from function
#   8. Define def main(dic) — stored for later
#   9. Define def main(dic) — stored for later
#   10. Assign to dic
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: dic=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, add_multiple_pairs, main, main are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(main(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      new_pairs = ... (stored in memory)
#      print(add_multiple_pairs(dic, new_pairs)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 827:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 828:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 829:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 830:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   solve(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to count
#   2. Assign to i
#   3. While loop
#   4. Define def solve(number) — stored for later
#   5. Define def solve(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(solve(...))
#       calls solve with: number=25
#         9.1 Assign to count
#         9.2 Assign to i
#         9.3 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      count = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 832:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to number
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to number
#   6. While loop
#   7. Assign to number
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 833:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: * <=
#
# ===== EVALUATION ORDER =====
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#   5 * i: first get 5, then get i, then compute 5 * i
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to i
#   6. While loop
#   7. Assign to i
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 834:
#
# ===== SYNTAX =====
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to total
#   2. While loop
#   3. Run print(total)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      total = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 835:
#
# ===== SYNTAX =====
#   while
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   number % 2: first get number, then get 2, then compute number % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to number
#   2. While loop
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 836:
#
# ===== SYNTAX =====
#   while
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to total
#   2. Assign to number
#   3. While loop
#   4. Run print(total)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      total = ... (stored in memory)
#      number = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 837:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: + <
#
# ===== EVALUATION ORDER =====
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Assign to ?
#   2. Assign to i
#   3. While loop
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to ?
#   7. Assign to i
#   8. While loop
#   9. Assign to ?
#   10. Assign to i
#   11. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      ? = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      ? = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 838:
#
# ===== SYNTAX =====
#   while
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to secret_number
#   2. Assign to guess
#   3. While loop
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      secret_number = ... (stored in memory)
#      guess = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 839:
#
# ===== SYNTAX =====
#   def solve(text)
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   while
#   operators: < in
#
# ===== EVALUATION ORDER =====
#   solve(text): looks up variable 'text', then runs the function body
#   print(solve('world')): the inner solve() executes FIRST, then print() outputs the result
#   solve('world'): gets the literal 'world', then runs the function body
#   ? in vowels: is this True or False? The answer picks which branch
#   ? in vowels: is this True or False? The answer picks which branch
#   ? in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to string
#   2. Assign to vowels
#   3. Assign to i
#   4. Assign to count
#   5. While loop
#   6. Run print(count)
#   7. Define def solve(text) — stored for later
#   8. Define def solve(text) — stored for later
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(solve(...))
#       calls solve with: text='world'
#         12.1 Assign to string
#         12.2 Assign to vowels
#         12.3 Assign to i
#         12.4 Assign to count
#         12.5 While loop
#         12.6 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      vowels = ... (stored in memory)
#      i = ... (stored in memory)
#      count = ... (stored in memory)
#      while loop repeats until condition is False
#      print(count)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve('world')) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 840:
#
# ===== SYNTAX =====
#   def solve(text)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#
# ===== EVALUATION ORDER =====
#   solve(text): looks up variable 'text', then runs the function body
#   print(solve('world')): the inner solve() executes FIRST, then print() outputs the result
#   solve('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to string
#   2. While loop
#   3. Define def solve(text) — stored for later
#   4. Define def solve(text) — stored for later
#   5. Assign to text
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(solve(...))
#       calls solve with: text='world'
#         8.1 Assign to string
#         8.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      while loop repeats until condition is False
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve('world')) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 841:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   solve(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to number
#   2. While loop
#   3. Define def solve(number) — stored for later
#   4. Define def solve(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(solve(...))
#       calls solve with: number=25
#         8.1 Assign to number
#         8.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 842:
#
# ===== SYNTAX =====
#   def solve(number, number1)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   solve(number, number1): looks up variable 'number', looks up variable 'number1', then runs the function body
#   print(solve(25, 25)): the inner solve() executes FIRST, then print() outputs the result
#   solve(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to total
#   2. Assign to num
#   3. While loop
#   4. Run print(total)
#   5. Define def solve(number, number1) — stored for later
#   6. Define def solve(number, number1) — stored for later
#   7. Assign to number
#   8. Assign to number1
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(solve(...))
#       calls solve with: number=25, number1=25
#         11.1 Assign to total
#         11.2 Assign to num
#         11.3 While loop
#         11.4 Assign to num
#         11.5 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      total = ... (stored in memory)
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#      number = ... (stored in memory)
#      number1 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25, 25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 844:
#
# ===== SYNTAX =====
#   while
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to password
#   2. While loop
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      password = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 845:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to num
#   6. While loop
#   7. Assign to num
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 846:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: + * % >
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   solve(25): gets the literal 25, then runs the function body
#   reversed_num * 10: first get reversed_num, then get 10, then compute reversed_num * 10
#   num % 10: first get num, then get 10, then compute num % 10
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   reversed_num * 10: first get reversed_num, then get 10, then compute reversed_num * 10
#   num % 10: first get num, then get 10, then compute num % 10
#   reversed_num * 10: first get reversed_num, then get 10, then compute reversed_num * 10
#   num % 10: first get num, then get 10, then compute num % 10
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. Assign to reversed_num
#   3. While loop
#   4. Run print(reversed_num)
#   5. Define def solve(number) — stored for later
#   6. Define def solve(number) — stored for later
#   7. Assign to number
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(solve(...))
#       calls solve with: number=25
#         10.1 Assign to num
#         10.2 Assign to reversed_num
#         10.3 While loop
#         10.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      reversed_num = ... (stored in memory)
#      while loop repeats until condition is False
#      print(reversed_num)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 848:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   solve(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Define def solve(number) — stored for later
#   4. Define def solve(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(solve(...))
#       calls solve with: number=25
#         8.1 Assign to num
#         8.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 849:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to num
#   3. While loop
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to num
#   8. While loop
#   9. Import random
#   10. Assign to num
#   11. While loop
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 851:
#
# ===== SYNTAX =====
#   if/elif/else
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   ? not in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to string
#   2. Assign to vowels
#   3. Assign to i
#   4. While loop
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      vowels = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 852:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   solve(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. Assign to count
#   3. While loop
#   4. Run print(count)
#   5. Define def solve(number) — stored for later
#   6. Define def solve(number) — stored for later
#   7. Assign to number
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(solve(...))
#       calls solve with: number=25
#         10.1 Assign to num
#         10.2 Assign to count
#         10.3 While loop
#         10.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      count = ... (stored in memory)
#      while loop repeats until condition is False
#      print(count)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 853:
#
# ===== SYNTAX =====
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. While loop
#   3. Run print(lst)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      while loop repeats until condition is False
#      print(lst)
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 854:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   solve(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Define def solve(number) — stored for later
#   4. Define def solve(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(solve(...))
#       calls solve with: number=25
#         8.1 Assign to num
#         8.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 856:
#
# ===== SYNTAX =====
#   def solve(number)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: ** <=
#
# ===== EVALUATION ORDER =====
#   solve(number): looks up variable 'number', then runs the function body
#   print(solve(25)): the inner solve() executes FIRST, then print() outputs the result
#   num ** 2: first get num, then get 2, then compute num ** 2
#   solve(25): gets the literal 25, then runs the function body
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 2: first get num, then get 2, then compute num ** 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Define def solve(number) — stored for later
#   4. Define def solve(number) — stored for later
#   5. Assign to number
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(solve(...))
#       calls solve with: number=25
#         8.1 Assign to num
#         8.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(solve(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 857:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   while
#   operators: % != <=
#
# ===== EVALUATION ORDER =====
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to num
#   6. While loop
#   7. Assign to num
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 858:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to num
#   6. While loop
#   7. Assign to num
#   8. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 859:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to i
#   3. While loop
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to i
#   8. While loop
#   9. Assign to lst
#   10. Assign to i
#   11. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      lst = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      lst = ... (stored in memory)
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 860:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   while
#   operators: % != <=
#
# ===== EVALUATION ORDER =====
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. Assign to total
#   3. While loop
#   4. Run print(total)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to num
#   8. Assign to total
#   9. While loop
#   10. Run print(total)
#   11. Assign to num
#   12. Assign to total
#   13. While loop
#   14. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      total = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#      num = ... (stored in memory)
#      total = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#      num = ... (stored in memory)
#      total = ... (stored in memory)
#      while loop repeats until condition is False
#      print(total)
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 861:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. Assign to count
#   3. While loop
#   4. Run print(count)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to num
#   8. Assign to count
#   9. While loop
#   10. Run print(count)
#   11. Assign to num
#   12. Assign to count
#   13. While loop
#   14. Run print(count)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      count = ... (stored in memory)
#      while loop repeats until condition is False
#      print(count)
#      num = ... (stored in memory)
#      count = ... (stored in memory)
#      while loop repeats until condition is False
#      print(count)
#      num = ... (stored in memory)
#      count = ... (stored in memory)
#      while loop repeats until condition is False
#      print(count)
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 863:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   while
#   operators: >
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. While loop
#   3. Run print('Lift off!')
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to num
#   7. While loop
#   8. Run print('Lift off!')
#   9. Assign to num
#   10. While loop
#   11. Run print('Lift off!')
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      print('Lift off!')
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      print('Lift off!')
#      num = ... (stored in memory)
#      while loop repeats until condition is False
#      print('Lift off!')
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 865:
#
# ===== SYNTAX =====
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. Assign to product
#   3. While loop
#   4. Run print(product)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      product = ... (stored in memory)
#      while loop repeats until condition is False
#      print(product)
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 866:
#
# ===== SYNTAX =====
#   def main(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   main(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(main(?, ?)): the inner main() executes FIRST, then print() outputs the result
#   main(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run main(lst1, lst2)
#       with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def main(lst1, lst2) — stored for later
#   6. Define def main(lst1, lst2) — stored for later
#   7. Assign to lst1
#   8. Assign to lst2
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(main(...))
#       calls main with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      main(lst1, lst2) → jumps into function, runs body, returns result → printed
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 867:
#
# ===== SYNTAX =====
#   def create_dictionary(keys, values)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   create_dictionary(keys, values): looks up variable 'keys', looks up variable 'values', then runs the function body
#   create_dictionary(keys, values): looks up variable 'keys', looks up variable 'values', then runs the function body
#   print(create_dictionary(?, ?)): the inner create_dictionary() executes FIRST, then print() outputs the result
#   create_dictionary(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_dictionary(keys, values) — stored for later
#   2. Assign to keys
#   3. Assign to values
#   4. Assign to result
#   5. Run print(result)
#   6. Define def create_dictionary(keys, values) — stored for later
#   7. Define def create_dictionary(keys, values) — stored for later
#   8. Assign to keys
#   9. Assign to values
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(create_dictionary(...))
#       calls create_dictionary with: keys=[4, 1, 8, 6, 3], values=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_dictionary, create_dictionary, create_dictionary are just stored
#   Execution starts at the non-def lines below:
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_dictionary(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 868:
#
# ===== SYNTAX =====
#   def capitalize_words(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   capitalize_words(lst): looks up variable 'lst', then runs the function body
#   capitalize_words(lst): looks up variable 'lst', then runs the function body
#   print(capitalize_words(?)): the inner capitalize_words() executes FIRST, then print() outputs the result
#   capitalize_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_words(lst) — stored for later
#   2. Assign to lst
#   3. Run capitalize_words(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def capitalize_words(lst) — stored for later
#   5. Define def capitalize_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(capitalize_words(...))
#       calls capitalize_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_words, capitalize_words, capitalize_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      capitalize_words(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_words(?)) → function runs first, then print shows the result

"""

"""
Problem 869:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to ?
#   3. Run print(a, b, c)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to ?
#   8. Run print(a, b, c)
#   9. Assign to numbers
#   10. Assign to ?
#   11. Run print(a, b, c)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)

"""

"""
Problem 870:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to ?
#   3. Run print(first, middle, last)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to ?
#   8. Run print(first, middle, last)
#   9. Assign to numbers
#   10. Assign to ?
#   11. Run print(first, middle, last)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)

"""

"""
Problem 871:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ?
#   2. Assign to ?
#   3. Run print(x, y)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to ?
#   7. Assign to ?
#   8. Run print(x, y)
#   9. Assign to ?
#   10. Assign to ?
#   11. Run print(x, y)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y)

"""

"""
Problem 872:
#
# ===== SYNTAX =====
#   def solve()
#   zip() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to ?
#   3. Run print(keys, values)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to data
#   7. Assign to ?
#   8. Run print(keys, values)
#   9. Assign to data
#   10. Assign to ?
#   11. Run print(keys, values)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(keys, values)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(keys, values)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(keys, values)

"""

"""
Problem 873:
#
# ===== SYNTAX =====
#   def get_coordinates()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_coordinates(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def get_coordinates() — stored for later
#   2. Assign to ?
#   3. Run print(x, y)
#   4. Define def get_coordinates() — stored for later
#   5. Define def get_coordinates() — stored for later
#   6. Assign to result
#   7. Run print(result)
#   8. Assign to result
#   9. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_coordinates, get_coordinates, get_coordinates are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      print(x, y)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 874:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(''.join(rest)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to word
#   2. Assign to ?
#   3. Run print(first, second, ''.join(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      word = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, second, ''.join(...))

"""

"""
Problem 875:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to values
#   2. Assign to ?
#   3. Run print(first, last)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to values
#   7. Assign to ?
#   8. Run print(first, last)
#   9. Assign to values
#   10. Assign to ?
#   11. Run print(first, last)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      values = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, last)
#      values = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, last)
#      values = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, last)

"""

"""
Problem 876:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to nested_list
#   2. Assign to ?
#   3. Run print(a, b, c, d)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to nested_list
#   7. Assign to ?
#   8. Run print(a, b, c, d)
#   9. Assign to nested_list
#   10. Assign to ?
#   11. Run print(a, b, c, d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      nested_list = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c, d)
#      nested_list = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c, d)
#      nested_list = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c, d)

"""

"""
Problem 877:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ?
#   2. Run print(a, b, c)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to ?
#   6. Run print(a, b, c)
#   7. Assign to ?
#   8. Run print(a, b, c)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      print(a, b, c)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      ? = ... (stored in memory)
#      print(a, b, c)

"""

"""
Problem 878:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to full_name
#   2. Assign to ?
#   3. Run print(first_name, last_name)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to full_name
#   7. Assign to ?
#   8. Run print(first_name, last_name)
#   9. Assign to full_name
#   10. Assign to ?
#   11. Run print(first_name, last_name)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      full_name = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first_name, last_name)
#      full_name = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first_name, last_name)
#      full_name = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first_name, last_name)

"""

"""
Problem 879:
#
# ===== SYNTAX =====
#   def greet(name, age)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   greet(?): resolves the value, then runs the function body
#   greet(name, age): looks up variable 'name', looks up variable 'age', then runs the function body
#   print(greet('Bob', 25)): the inner greet() executes FIRST, then print() outputs the result
#   greet('Bob', 25): gets the literal 'Bob', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def greet(name, age) — stored for later
#   2. Assign to person
#   3. Run greet(?)
#       with: name=?
#         3.1 Assign to result
#         3.2 Return from function
#   4. Define def greet(name, age) — stored for later
#   5. Define def greet(name, age) — stored for later
#   6. Assign to name
#   7. Assign to age
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(greet(...))
#       calls greet with: name='Bob', age=25
#         10.1 Assign to result
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet, greet, greet are just stored
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      greet(?) → jumps into function, runs body, returns result → printed
#      name = ... (stored in memory)
#      age = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet('Bob', 25)) → function runs first, then print shows the result

"""

"""
Problem 880:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to list1
#   2. Assign to list2
#   3. Assign to merged
#   4. Run print(merged)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to list1
#   8. Assign to list2
#   9. Assign to merged
#   10. Run print(merged)
#   11. Assign to list1
#   12. Assign to list2
#   13. Assign to merged
#   14. Run print(merged)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      merged = ... (stored in memory)
#      print(merged)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      merged = ... (stored in memory)
#      print(merged)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      merged = ... (stored in memory)
#      print(merged)

"""

"""
Problem 881:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to dict1
#   2. Assign to dict2
#   3. Assign to merged_dict
#   4. Run print(merged_dict)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Assign to merged_dict
#   10. Run print(merged_dict)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Assign to merged_dict
#   14. Run print(merged_dict)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      merged_dict = ... (stored in memory)
#      print(merged_dict)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      merged_dict = ... (stored in memory)
#      print(merged_dict)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      merged_dict = ... (stored in memory)
#      print(merged_dict)

"""

"""
Problem 882:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to values
#   2. Assign to ?
#   3. Run print(first, third, last)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to values
#   7. Assign to ?
#   8. Run print(first, third, last)
#   9. Assign to values
#   10. Assign to ?
#   11. Run print(first, third, last)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      values = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, third, last)
#      values = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, third, last)
#      values = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, third, last)

"""

"""
Problem 883:
#
# ===== SYNTAX =====
#   def add(a, b, c)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   add(?): resolves the value, then runs the function body
#   add(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(add(20, 30, 10)): the inner add() executes FIRST, then print() outputs the result
#   ? + c: first get ?, then get c, then compute ? + c
#   ? + c: first get ?, then get c, then compute ? + c
#   ? + c: first get ?, then get c, then compute ? + c
#   add(20, 30, 10): gets the literal 20, gets the literal 30, gets the literal 10, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Define def add(a, b, c) — stored for later
#   2. Assign to numbers
#   3. Assign to result
#   4. Run print(result)
#   5. Define def add(a, b, c) — stored for later
#   6. Define def add(a, b, c) — stored for later
#   7. Assign to a
#   8. Assign to b
#   9. Assign to c
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(add(...))
#       calls add with: a=20, b=30, c=10
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add, add, add are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add(20, 30, 10)) → function runs first, then print shows the result

"""

"""
Problem 884:
#
# ===== SYNTAX =====
#   def describe(name, age, city)
#   return — exits the function and sends back a value
#   operators: %
#
# ===== EVALUATION ORDER =====
#   print(describe()): the inner describe() executes FIRST, then print() outputs the result
#   describe(name, age, city): looks up variable 'name', looks up variable 'age', looks up variable 'city', then runs the function body
#   print(describe('Bob', 25, 25)): the inner describe() executes FIRST, then print() outputs the result
#   describe(): — no arguments needed
#   '%s is %s years old and lives in %s.' % (name, age, city): first get '%s is %s years old and lives in %s.', then get (name, age, city), then compute '%s is %s years old and lives in %s.' % (name, age, city)
#   describe('Bob', 25, 25): gets the literal 'Bob', gets the literal 25, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def describe(name, age, city) — stored for later
#   2. Assign to person
#   3. Run print(describe(...))
#       calls describe with: 
#         3.1 Return from function
#   4. Define def describe(name, age, city) — stored for later
#   5. Define def describe(name, age, city) — stored for later
#   6. Assign to name
#   7. Assign to age
#   8. Assign to city
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(describe(...))
#       calls describe with: name='Bob', age=25, city=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions describe, describe, describe are just stored
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      print(describe()) → function runs first, then print shows the result
#      name = ... (stored in memory)
#      age = ... (stored in memory)
#      city = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(describe('Bob', 25, 25)) → function runs first, then print shows the result

"""

"""
Problem 885:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to word
#   2. Assign to ?
#   3. Run print(a, b, c)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to word
#   7. Assign to ?
#   8. Run print(a, b, c)
#   9. Assign to word
#   10. Assign to ?
#   11. Run print(a, b, c)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      word = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      word = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      word = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)

"""

"""
Problem 886:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to ?
#   3. Run print(first, middle, last)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to ?
#   8. Run print(first, middle, last)
#   9. Assign to numbers
#   10. Assign to ?
#   11. Run print(first, middle, last)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)

"""

"""
Problem 887:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to pairs
#   2. For loop over pairs
#       2.1 Run print(number, letter)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to pairs
#   6. For loop over pairs
#       6.1 Run print(number, letter)
#   7. Assign to pairs
#   8. For loop over pairs
#       8.1 Run print(number, letter)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      pairs = ... (stored in memory)
#      for loop runs its body once per item
#      pairs = ... (stored in memory)
#      for loop runs its body once per item
#      pairs = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from pairs each round

"""

"""
Problem 888:
#
# ===== SYNTAX =====
#   def get_user()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_user(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def get_user() — stored for later
#   2. Assign to ?
#   3. Run print(name, age)
#   4. Define def get_user() — stored for later
#   5. Define def get_user() — stored for later
#   6. Assign to result
#   7. Run print(result)
#   8. Assign to result
#   9. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_user, get_user, get_user are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      print(name, age)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 889:
#
# ===== SYNTAX =====
#   def solve()
#   sum() — built-in function call
#   zip() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lists
#   2. Assign to summed
#   3. Run print(summed)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lists
#   7. Assign to summed
#   8. Run print(summed)
#   9. Assign to lists
#   10. Assign to summed
#   11. Run print(summed)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lists = ... (stored in memory)
#      summed = ... (stored in memory)
#      print(summed)
#      lists = ... (stored in memory)
#      summed = ... (stored in memory)
#      print(summed)
#      lists = ... (stored in memory)
#      summed = ... (stored in memory)
#      print(summed)

"""

"""
Problem 890:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to formatted
#   3. Run print(formatted)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to data
#   7. Assign to formatted
#   8. Run print(formatted)
#   9. Assign to data
#   10. Assign to formatted
#   11. Run print(formatted)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      formatted = ... (stored in memory)
#      print(formatted)
#      data = ... (stored in memory)
#      formatted = ... (stored in memory)
#      print(formatted)
#      data = ... (stored in memory)
#      formatted = ... (stored in memory)
#      print(formatted)

"""

"""
Problem 891:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sentence
#   2. Assign to ?
#   3. Run print(first, middle, last)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to ?
#   8. Run print(first, middle, last)
#   9. Assign to sentence
#   10. Assign to ?
#   11. Run print(first, middle, last)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)
#      sentence = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)
#      sentence = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(first, middle, last)

"""

"""
Problem 892:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to word
#   2. Assign to vowels
#   3. Run print(vowels)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to word
#   7. Assign to vowels
#   8. Run print(vowels)
#   9. Assign to word
#   10. Assign to vowels
#   11. Run print(vowels)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      word = ... (stored in memory)
#      vowels = ... (stored in memory)
#      print(vowels)
#      word = ... (stored in memory)
#      vowels = ... (stored in memory)
#      print(vowels)
#      word = ... (stored in memory)
#      vowels = ... (stored in memory)
#      print(vowels)

"""

"""
Problem 893:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to ?
#   3. Run print(name, age, height, is_student)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to data
#   7. Assign to ?
#   8. Run print(name, age, height, is_student)
#   9. Assign to data
#   10. Assign to ?
#   11. Run print(name, age, height, is_student)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(name, age, height, is_student)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(name, age, height, is_student)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(name, age, height, is_student)

"""

"""
Problem 894:
#
# ===== SYNTAX =====
#   def solve()
#   sorted() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to tuples
#   2. Assign to sorted_tuples
#   3. Run print(sorted_tuples)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to tuples
#   7. Assign to sorted_tuples
#   8. Run print(sorted_tuples)
#   9. Assign to tuples
#   10. Assign to sorted_tuples
#   11. Run print(sorted_tuples)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      tuples = ... (stored in memory)
#      sorted_tuples = ... (stored in memory)
#      print(sorted_tuples)
#      tuples = ... (stored in memory)
#      sorted_tuples = ... (stored in memory)
#      print(sorted_tuples)
#      tuples = ... (stored in memory)
#      sorted_tuples = ... (stored in memory)
#      print(sorted_tuples)

"""

"""
Problem 895:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to evens
#   3. Run print(evens)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to evens
#   8. Run print(evens)
#   9. Assign to numbers
#   10. Assign to evens
#   11. Run print(evens)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      evens = ... (stored in memory)
#      print(evens)
#      numbers = ... (stored in memory)
#      evens = ... (stored in memory)
#      print(evens)
#      numbers = ... (stored in memory)
#      evens = ... (stored in memory)
#      print(evens)

"""

"""
Problem 896:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ?
#   2. Assign to ?
#   3. Run print(a, b, c)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to ?
#   7. Assign to ?
#   8. Run print(a, b, c)
#   9. Assign to ?
#   10. Assign to ?
#   11. Run print(a, b, c)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)

"""

"""
Problem 897:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ?
#   2. Run print(first, second, remaining)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to ?
#   6. Run print(first, second, remaining)
#   7. Assign to ?
#   8. Run print(first, second, remaining)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      print(first, second, remaining)
#      ? = ... (stored in memory)
#      print(first, second, remaining)
#      ? = ... (stored in memory)
#      print(first, second, remaining)

"""

"""
Problem 898:
#
# ===== SYNTAX =====
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(add_three(?)): inner expression resolves first, then print() outputs
#   ? + z: first get ?, then get z, then compute ? + z
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Assign to add_three
#   2. Assign to numbers
#   3. Run print(add_three(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      add_three = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(add_three(...))

"""

"""
Problem 899:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   str() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. Assign to digits
#   3. Assign to reversed_digits
#   4. Run print(reversed_digits)
#   5. Assign to num
#   6. Assign to str_num
#   7. Assign to lst_num
#   8. Run print(lst_num)
#   9. Define def solve() — stored for later
#   10. Define def solve() — stored for later
#   11. Assign to num
#   12. Assign to digits
#   13. Assign to reversed_digits
#   14. Run print(reversed_digits)
#   15. Assign to num
#   16. Assign to str_num
#   17. Assign to lst_num
#   18. Run print(lst_num)
#   19. Assign to num
#   20. Assign to digits
#   21. Assign to reversed_digits
#   22. Run print(reversed_digits)
#   23. Assign to num
#   24. Assign to str_num
#   25. Assign to lst_num
#   26. Run print(lst_num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      digits = ... (stored in memory)
#      reversed_digits = ... (stored in memory)
#      print(reversed_digits)
#      num = ... (stored in memory)
#      str_num = ... (stored in memory)
#      lst_num = ... (stored in memory)
#      print(lst_num)
#      num = ... (stored in memory)
#      digits = ... (stored in memory)
#      reversed_digits = ... (stored in memory)
#      print(reversed_digits)
#      num = ... (stored in memory)
#      str_num = ... (stored in memory)
#      lst_num = ... (stored in memory)
#      print(lst_num)
#      num = ... (stored in memory)
#      digits = ... (stored in memory)
#      reversed_digits = ... (stored in memory)
#      print(reversed_digits)
#      num = ... (stored in memory)
#      str_num = ... (stored in memory)
#      lst_num = ... (stored in memory)
#      print(lst_num)

"""

"""
Problem 900:
#
# ===== SYNTAX =====
#   def min_max(lst)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   min_max(numbers): looks up variable 'numbers', then runs the function body
#   min_max(lst): looks up variable 'lst', then runs the function body
#   print(min_max(?)): the inner min_max() executes FIRST, then print() outputs the result
#   min_max(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def min_max(lst) — stored for later
#   2. Assign to numbers
#   3. Assign to ?
#   4. Run print(minimum, maximum)
#   5. Define def min_max(lst) — stored for later
#   6. Define def min_max(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(min_max(...))
#       calls min_max with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions min_max, min_max, min_max are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(minimum, maximum)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(min_max(?)) → function runs first, then print shows the result

"""

"""
Problem 901:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to tuples
#   2. Assign to first_elements
#   3. Run print(first_elements)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      tuples = ... (stored in memory)
#      first_elements = ... (stored in memory)
#      print(first_elements)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 902:
#
# ===== SYNTAX =====
#   def main(lst_dic)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   main(lst_dic): looks up variable 'lst_dic', then runs the function body
#   main(lst_dic): looks up variable 'lst_dic', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst_dic) — stored for later
#   2. Assign to lst_dic
#   3. Run main(lst_dic)
#       with: lst_dic=lst_dic
#         3.1 Return from function
#   4. Assign to people
#   5. Assign to ages
#   6. Run print(ages)
#   7. Define def main(lst_dic) — stored for later
#   8. Define def main(lst_dic) — stored for later
#   9. Assign to lst_dic
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(main(...))
#       calls main with: lst_dic=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst_dic = ... (stored in memory)
#      main(lst_dic) → jumps into function, runs body, returns result → printed
#      people = ... (stored in memory)
#      ages = ... (stored in memory)
#      print(ages)
#      lst_dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 903:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to expression
#   2. Assign to ?
#   3. Run print(a, op1, b, op2, c, op3, d)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to expression
#   7. Assign to ?
#   8. Run print(a, op1, b, op2, c, op3, d)
#   9. Assign to expression
#   10. Assign to ?
#   11. Run print(a, op1, b, op2, c, op3, d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      expression = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, op1, b, op2, c, op3, d)
#      expression = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, op1, b, op2, c, op3, d)
#      expression = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, op1, b, op2, c, op3, d)

"""

"""
Problem 904:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to ?
#   3. Assign to ?
#   4. Run print(x, y, z)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to ?
#   9. Assign to ?
#   10. Run print(x, y, z)
#   11. Assign to data
#   12. Assign to ?
#   13. Assign to ?
#   14. Run print(x, y, z)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y, z)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y, z)
#      data = ... (stored in memory)
#      ? = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y, z)

"""

"""
Problem 905:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to email
#   2. Assign to ?
#   3. Run print(user, domain)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to email
#   7. Assign to ?
#   8. Run print(user, domain)
#   9. Assign to email
#   10. Assign to ?
#   11. Run print(user, domain)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      email = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(user, domain)
#      email = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(user, domain)
#      email = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(user, domain)

"""

"""
Problem 906:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to nested_tuples
#   2. For loop over nested_tuples
#       2.1 Run print(a, b, c)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to nested_tuples
#   6. For loop over nested_tuples
#       6.1 Run print(a, b, c)
#   7. Assign to nested_tuples
#   8. For loop over nested_tuples
#       8.1 Run print(a, b, c)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      nested_tuples = ... (stored in memory)
#      for loop runs its body once per item
#      nested_tuples = ... (stored in memory)
#      for loop runs its body once per item
#      nested_tuples = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from nested_tuples each round

"""

"""
Problem 907:
#
# ===== SYNTAX =====
#   def solve()
#   divmod() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ?
#   2. Run print(quotient, remainder)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to ?
#   6. Run print(quotient, remainder)
#   7. Assign to ?
#   8. Run print(quotient, remainder)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ? = ... (stored in memory)
#      print(quotient, remainder)
#      ? = ... (stored in memory)
#      print(quotient, remainder)
#      ? = ... (stored in memory)
#      print(quotient, remainder)

"""

"""
Problem 908:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to num
#   8. Run print(num)
#   9. Import random
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 909:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to num
#   8. Run print(num)
#   9. Import random
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 910:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to num
#   8. Run print(num)
#   9. Import random
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 911:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to colors
#   3. Assign to selected
#   4. Run print(selected)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to colors
#   9. Assign to selected
#   10. Run print(selected)
#   11. Import random
#   12. Assign to colors
#   13. Assign to selected
#   14. Run print(selected)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      colors = ... (stored in memory)
#      selected = ... (stored in memory)
#      print(selected)
#      colors = ... (stored in memory)
#      selected = ... (stored in memory)
#      print(selected)
#      colors = ... (stored in memory)
#      selected = ... (stored in memory)
#      print(selected)

"""

"""
Problem 912:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to numbers
#   3. Run random.shuffle(numbers)
#   4. Run print(numbers)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to numbers
#   9. Run random.shuffle(numbers)
#   10. Run print(numbers)
#   11. Import random
#   12. Assign to numbers
#   13. Run random.shuffle(numbers)
#   14. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      random.shuffle(numbers)
#      print(numbers)
#      numbers = ... (stored in memory)
#      random.shuffle(numbers)
#      print(numbers)
#      numbers = ... (stored in memory)
#      random.shuffle(numbers)
#      print(numbers)

"""

"""
Problem 913:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to numbers
#   3. Assign to selected
#   4. Run print(selected)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to numbers
#   9. Assign to selected
#   10. Run print(selected)
#   11. Import random
#   12. Assign to numbers
#   13. Assign to selected
#   14. Run print(selected)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      selected = ... (stored in memory)
#      print(selected)
#      numbers = ... (stored in memory)
#      selected = ... (stored in memory)
#      print(selected)
#      numbers = ... (stored in memory)
#      selected = ... (stored in memory)
#      print(selected)

"""

"""
Problem 914:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to num
#   8. Run print(num)
#   9. Import random
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 915:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Import string
#   3. Assign to letter
#   4. Run print(letter)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Import string
#   9. Assign to letter
#   10. Run print(letter)
#   11. Import random
#   12. Import string
#   13. Assign to letter
#   14. Run print(letter)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      letter = ... (stored in memory)
#      print(letter)
#      letter = ... (stored in memory)
#      print(letter)
#      letter = ... (stored in memory)
#      print(letter)

"""

"""
Problem 916:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to roll
#   3. Run print(roll)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to roll
#   8. Run print(roll)
#   9. Import random
#   10. Assign to roll
#   11. Run print(roll)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      roll = ... (stored in memory)
#      print(roll)
#      roll = ... (stored in memory)
#      print(roll)
#      roll = ... (stored in memory)
#      print(roll)

"""

"""
Problem 917:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to flip
#   3. Run print(flip)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to flip
#   8. Run print(flip)
#   9. Import random
#   10. Assign to flip
#   11. Run print(flip)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      flip = ... (stored in memory)
#      print(flip)
#      flip = ... (stored in memory)
#      print(flip)
#      flip = ... (stored in memory)
#      print(flip)

"""

"""
Problem 918:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to first_names
#   3. Assign to last_names
#   4. Assign to full_name
#   5. Run print(full_name)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import random
#   9. Assign to first_names
#   10. Assign to last_names
#   11. Assign to full_name
#   12. Run print(full_name)
#   13. Import random
#   14. Assign to first_names
#   15. Assign to last_names
#   16. Assign to full_name
#   17. Run print(full_name)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      first_names = ... (stored in memory)
#      last_names = ... (stored in memory)
#      full_name = ... (stored in memory)
#      print(full_name)
#      first_names = ... (stored in memory)
#      last_names = ... (stored in memory)
#      full_name = ... (stored in memory)
#      print(full_name)
#      first_names = ... (stored in memory)
#      last_names = ... (stored in memory)
#      full_name = ... (stored in memory)
#      print(full_name)

"""

"""
Problem 919:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Import string
#   3. Assign to characters
#   4. Assign to password
#   5. Run print(password)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import random
#   9. Import string
#   10. Assign to characters
#   11. Assign to password
#   12. Run print(password)
#   13. Import random
#   14. Import string
#   15. Assign to characters
#   16. Assign to password
#   17. Run print(password)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      characters = ... (stored in memory)
#      password = ... (stored in memory)
#      print(password)
#      characters = ... (stored in memory)
#      password = ... (stored in memory)
#      print(password)
#      characters = ... (stored in memory)
#      password = ... (stored in memory)
#      print(password)

"""

"""
Problem 920:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to boolean_value
#   3. Run print(boolean_value)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to boolean_value
#   8. Run print(boolean_value)
#   9. Import random
#   10. Assign to boolean_value
#   11. Run print(boolean_value)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      boolean_value = ... (stored in memory)
#      print(boolean_value)
#      boolean_value = ... (stored in memory)
#      print(boolean_value)
#      boolean_value = ... (stored in memory)
#      print(boolean_value)

"""

"""
Problem 921:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to numbers
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to numbers
#   8. Run print(numbers)
#   9. Import random
#   10. Assign to numbers
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(numbers)
#      numbers = ... (stored in memory)
#      print(numbers)
#      numbers = ... (stored in memory)
#      print(numbers)

"""

"""
Problem 922:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to items
#   3. Assign to subset
#   4. Run print(subset)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to items
#   9. Assign to subset
#   10. Run print(subset)
#   11. Import random
#   12. Assign to items
#   13. Assign to subset
#   14. Run print(subset)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      subset = ... (stored in memory)
#      print(subset)
#      items = ... (stored in memory)
#      subset = ... (stored in memory)
#      print(subset)
#      items = ... (stored in memory)
#      subset = ... (stored in memory)
#      print(subset)

"""

"""
Problem 923:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   start_date + datetime.timedelta(...): first get start_date, then get datetime.timedelta(...), then compute start_date + datetime.timedelta(...)
#   start_date + datetime.timedelta(...): first get start_date, then get datetime.timedelta(...), then compute start_date + datetime.timedelta(...)
#   start_date + datetime.timedelta(...): first get start_date, then get datetime.timedelta(...), then compute start_date + datetime.timedelta(...)
#   start_date + datetime.timedelta(...): first get start_date, then get datetime.timedelta(...), then compute start_date + datetime.timedelta(...)
#   start_date + datetime.timedelta(...): first get start_date, then get datetime.timedelta(...), then compute start_date + datetime.timedelta(...)
#   end_date - start_date: first get end_date, then get start_date, then compute end_date - start_date
#   end_date - start_date: first get end_date, then get start_date, then compute end_date - start_date
#   end_date - start_date: first get end_date, then get start_date, then compute end_date - start_date
#   end_date - start_date: first get end_date, then get start_date, then compute end_date - start_date
#   end_date - start_date: first get end_date, then get start_date, then compute end_date - start_date
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Import datetime
#   3. Assign to start_date
#   4. Assign to end_date
#   5. Assign to random_days
#   6. Assign to random_date
#   7. Run print(random_date)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Import random
#   11. Import datetime
#   12. Assign to start_date
#   13. Assign to end_date
#   14. Assign to random_days
#   15. Assign to random_date
#   16. Run print(random_date)
#   17. Import random
#   18. Import datetime
#   19. Assign to start_date
#   20. Assign to end_date
#   21. Assign to random_days
#   22. Assign to random_date
#   23. Run print(random_date)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      start_date = ... (stored in memory)
#      end_date = ... (stored in memory)
#      random_days = ... (stored in memory)
#      random_date = ... (stored in memory)
#      print(random_date)
#      start_date = ... (stored in memory)
#      end_date = ... (stored in memory)
#      random_days = ... (stored in memory)
#      random_date = ... (stored in memory)
#      print(random_date)
#      start_date = ... (stored in memory)
#      end_date = ... (stored in memory)
#      random_days = ... (stored in memory)
#      random_date = ... (stored in memory)
#      print(random_date)

"""

"""
Problem 924:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to fruits
#   3. Assign to random_fruit
#   4. Run print(random_fruit)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to fruits
#   9. Assign to random_fruit
#   10. Run print(random_fruit)
#   11. Import random
#   12. Assign to fruits
#   13. Assign to random_fruit
#   14. Run print(random_fruit)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      fruits = ... (stored in memory)
#      random_fruit = ... (stored in memory)
#      print(random_fruit)
#      fruits = ... (stored in memory)
#      random_fruit = ... (stored in memory)
#      print(random_fruit)
#      fruits = ... (stored in memory)
#      random_fruit = ... (stored in memory)
#      print(random_fruit)

"""

"""
Problem 925:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to code
#   3. Run print(code)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to code
#   8. Run print(code)
#   9. Import random
#   10. Assign to code
#   11. Run print(code)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      code = ... (stored in memory)
#      print(code)
#      code = ... (stored in memory)
#      print(code)
#      code = ... (stored in memory)
#      print(code)

"""

"""
Problem 926:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to hex_color
#   3. Run print(hex_color)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to hex_color
#   8. Run print(hex_color)
#   9. Import random
#   10. Assign to hex_color
#   11. Run print(hex_color)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      hex_color = ... (stored in memory)
#      print(hex_color)
#      hex_color = ... (stored in memory)
#      print(hex_color)
#      hex_color = ... (stored in memory)
#      print(hex_color)

"""

"""
Problem 927:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to phone_number
#   3. Run print(phone_number)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to phone_number
#   8. Run print(phone_number)
#   9. Import random
#   10. Assign to phone_number
#   11. Run print(phone_number)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      phone_number = ... (stored in memory)
#      print(phone_number)
#      phone_number = ... (stored in memory)
#      print(phone_number)
#      phone_number = ... (stored in memory)
#      print(phone_number)

"""

"""
Problem 928:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to pin
#   3. Run print(pin)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to pin
#   8. Run print(pin)
#   9. Import random
#   10. Assign to pin
#   11. Run print(pin)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      pin = ... (stored in memory)
#      print(pin)
#      pin = ... (stored in memory)
#      print(pin)
#      pin = ... (stored in memory)
#      print(pin)

"""

"""
Problem 929:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to dice1
#   3. Assign to dice2
#   4. Run print(dice1, dice2)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to dice1
#   9. Assign to dice2
#   10. Run print(dice1, dice2)
#   11. Import random
#   12. Assign to dice1
#   13. Assign to dice2
#   14. Run print(dice1, dice2)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dice1 = ... (stored in memory)
#      dice2 = ... (stored in memory)
#      print(dice1, dice2)
#      dice1 = ... (stored in memory)
#      dice2 = ... (stored in memory)
#      print(dice1, dice2)
#      dice1 = ... (stored in memory)
#      dice2 = ... (stored in memory)
#      print(dice1, dice2)

"""

"""
Problem 930:
#
# ===== SYNTAX =====
#   def solve()
#   str() — built-in function call
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to ip_address
#   3. Run print(ip_address)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Import random
#   7. Assign to ip_address
#   8. Run print(ip_address)
#   9. Import random
#   10. Assign to ip_address
#   11. Run print(ip_address)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ip_address = ... (stored in memory)
#      print(ip_address)
#      ip_address = ... (stored in memory)
#      print(ip_address)
#      ip_address = ... (stored in memory)
#      print(ip_address)

"""

"""
Problem 931:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Import string
#   3. Assign to characters
#   4. Assign to password
#   5. Run print(password)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import random
#   9. Import string
#   10. Assign to characters
#   11. Assign to password
#   12. Run print(password)
#   13. Import random
#   14. Import string
#   15. Assign to characters
#   16. Assign to password
#   17. Run print(password)
#
# ===== EXECUTION FLOW =====
#   Python loads 6 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      characters = ... (stored in memory)
#      password = ... (stored in memory)
#      print(password)
#      characters = ... (stored in memory)
#      password = ... (stored in memory)
#      print(password)
#      characters = ... (stored in memory)
#      password = ... (stored in memory)
#      print(password)

"""

"""
Problem 932:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Assign to animals
#   3. Assign to random_animal
#   4. Run print(random_animal)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import random
#   8. Assign to animals
#   9. Assign to random_animal
#   10. Run print(random_animal)
#   11. Import random
#   12. Assign to animals
#   13. Assign to random_animal
#   14. Run print(random_animal)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      animals = ... (stored in memory)
#      random_animal = ... (stored in memory)
#      print(random_animal)
#      animals = ... (stored in memory)
#      random_animal = ... (stored in memory)
#      print(random_animal)
#      animals = ... (stored in memory)
#      random_animal = ... (stored in memory)
#      print(random_animal)

"""

"""
Problem 933:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   operators: /
#
# ===== EVALUATION ORDER =====
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 934:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(value)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(value)
#   7. Try block (error handling)
#   8. Run print(value)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(value)
#      top-level statement runs
#      print(value)
#      top-level statement runs
#      print(value)

"""

"""
Problem 935:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(value)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(value)
#   7. Try block (error handling)
#   8. Run print(value)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(value)
#      top-level statement runs
#      print(value)
#      top-level statement runs
#      print(value)

"""

"""
Problem 936:
#
# ===== SYNTAX =====
#   def solve()
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(num)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(num)
#   7. Try block (error handling)
#   8. Run print(num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(num)
#      top-level statement runs
#      print(num)
#      top-level statement runs
#      print(num)

"""

"""
Problem 937:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   operators: +
#
# ===== EVALUATION ORDER =====
#   'Age: ' + 25: first get 'Age: ', then get 25, then compute 'Age: ' + 25
#   'Age: ' + 25: first get 'Age: ', then get 25, then compute 'Age: ' + 25
#   'Age: ' + 25: first get 'Age: ', then get 25, then compute 'Age: ' + 25
#   'Age: ' + 25: first get 'Age: ', then get 25, then compute 'Age: ' + 25
#   'Age: ' + 25: first get 'Age: ', then get 25, then compute 'Age: ' + 25
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 938:
#
# ===== SYNTAX =====
#   def solve()
#   open() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   with
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(content)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(content)
#   7. Try block (error handling)
#   8. Run print(content)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(content)
#      top-level statement runs
#      print(content)
#      top-level statement runs
#      print(content)

"""

"""
Problem 939:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#     def __init__ — method of Person
#   def solve()
#   Person() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   print(Person('Bob')): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Person
#   2. Try block (error handling)
#   3. Run print(age)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to obj
#   7. Run print(obj)
#   8. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Person.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(age)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))

"""

"""
Problem 940:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   operators: +
#
# ===== EVALUATION ORDER =====
#   x + 10: first get x, then get 10, then compute x + 10
#   x + 10: first get x, then get 10, then compute x + 10
#   x + 10: first get x, then get 10, then compute x + 10
#   x + 10: first get x, then get 10, then compute x + 10
#   x + 10: first get x, then get 10, then compute x + 10
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 941:
#
# ===== SYNTAX =====
#   def solve()
#   open() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   with
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 942:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   operators: /
#
# ===== EVALUATION ORDER =====
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#   a / b: first get a, then get b, then compute a / b
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 943:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   try/except
#   operators: /
#
# ===== EVALUATION ORDER =====
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#   10 / 0: first get 10, then get 0, then compute 10 / 0
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 944:
#
# ===== SYNTAX =====
#   class NegativeNumberError(Exception) — inherits from parent
#   def check_number(n)
#   NegativeNumberError() — built-in function call
#   (no return — the function prints directly instead of returning)
#   check_number() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   try/except
#   operators: <
#
# ===== EVALUATION ORDER =====
#   check_number(n): looks up variable 'n', then runs the function body
#   print(check_number(15)): the inner check_number() executes FIRST, then print() outputs the result
#   check_number(?): resolves the value, then runs the function body
#   check_number(15): gets the literal 15, then runs the function body
#   check_number(?): resolves the value, then runs the function body
#   check_number(?): resolves the value, then runs the function body
#   n < 0: is this True or False? The answer picks which branch
#   n < 0: is this True or False? The answer picks which branch
#   n < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define class NegativeNumberError(Exception)
#   2. Define def check_number(n) — stored for later
#   3. Try block (error handling)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_number(...))
#       calls check_number with: n=15
#         9.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions check_number, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_number(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 945:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   operators: /
#
# ===== EVALUATION ORDER =====
#   '10' / 2: first get '10', then get 2, then compute '10' / 2
#   '10' / 2: first get '10', then get 2, then compute '10' / 2
#   '10' / 2: first get '10', then get 2, then compute '10' / 2
#   '10' / 2: first get '10', then get 2, then compute '10' / 2
#   '10' / 2: first get '10', then get 2, then compute '10' / 2
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Run print(result)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Run print(result)
#   7. Try block (error handling)
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)
#      top-level statement runs
#      print(result)

"""

"""
Problem 946:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 947:
#
# ===== SYNTAX =====
#   def solve()
#   MemoryError() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 948:
#
# ===== SYNTAX =====
#   def infinite_recursion()
#   return — exits the function and sends back a value
#   infinite_recursion() — built-in function call
#   try/except
#
# ===== EVALUATION ORDER =====
#   infinite_recursion(): — no arguments needed
#   infinite_recursion(): — no arguments needed
#   infinite_recursion(): — no arguments needed
#   infinite_recursion(): — no arguments needed
#   infinite_recursion(): — no arguments needed
#   infinite_recursion(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def infinite_recursion() — stored for later
#   2. Try block (error handling)
#   3. Define def infinite_recursion() — stored for later
#   4. Define def infinite_recursion() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions infinite_recursion, infinite_recursion, infinite_recursion are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 949:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 950:
#
# ===== SYNTAX =====
#   def solve()
#   open() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#   with
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 951:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Try block (error handling)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Try block (error handling)
#   5. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 952:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to upper_text
#   3. Run print(upper_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to upper_text
#   8. Run print(upper_text)
#   9. Assign to text
#   10. Assign to upper_text
#   11. Run print(upper_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      upper_text = ... (stored in memory)
#      print(upper_text)
#      text = ... (stored in memory)
#      upper_text = ... (stored in memory)
#      print(upper_text)
#      text = ... (stored in memory)
#      upper_text = ... (stored in memory)
#      print(upper_text)

"""

"""
Problem 953:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to count_a
#   3. Run print(count_a)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to count_a
#   8. Run print(count_a)
#   9. Assign to text
#   10. Assign to count_a
#   11. Run print(count_a)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      count_a = ... (stored in memory)
#      print(count_a)
#      text = ... (stored in memory)
#      count_a = ... (stored in memory)
#      print(count_a)
#      text = ... (stored in memory)
#      count_a = ... (stored in memory)
#      print(count_a)

"""

"""
Problem 954:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 955:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to new_text
#   3. Run print(new_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to new_text
#   8. Run print(new_text)
#   9. Assign to text
#   10. Assign to new_text
#   11. Run print(new_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      new_text = ... (stored in memory)
#      print(new_text)
#      text = ... (stored in memory)
#      new_text = ... (stored in memory)
#      print(new_text)
#      text = ... (stored in memory)
#      new_text = ... (stored in memory)
#      print(new_text)

"""

"""
Problem 956:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sentence
#   2. Assign to words
#   3. Run print(words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to words
#   8. Run print(words)
#   9. Assign to sentence
#   10. Assign to words
#   11. Run print(words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)
#      sentence = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)
#      sentence = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)

"""

"""
Problem 957:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to sentence
#   3. Run print(sentence)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to sentence
#   8. Run print(sentence)
#   9. Assign to words
#   10. Assign to sentence
#   11. Run print(sentence)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      sentence = ... (stored in memory)
#      print(sentence)
#      words = ... (stored in memory)
#      sentence = ... (stored in memory)
#      print(sentence)
#      words = ... (stored in memory)
#      sentence = ... (stored in memory)
#      print(sentence)

"""

"""
Problem 958:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to trimmed_text
#   3. Run print(trimmed_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to trimmed_text
#   8. Run print(trimmed_text)
#   9. Assign to text
#   10. Assign to trimmed_text
#   11. Run print(trimmed_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      trimmed_text = ... (stored in memory)
#      print(trimmed_text)
#      text = ... (stored in memory)
#      trimmed_text = ... (stored in memory)
#      print(trimmed_text)
#      text = ... (stored in memory)
#      trimmed_text = ... (stored in memory)
#      print(trimmed_text)

"""

"""
Problem 959:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to index_30
#   3. Run print(index_30)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to index_30
#   8. Run print(index_30)
#   9. Assign to numbers
#   10. Assign to index_30
#   11. Run print(index_30)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      index_30 = ... (stored in memory)
#      print(index_30)
#      numbers = ... (stored in memory)
#      index_30 = ... (stored in memory)
#      print(index_30)
#      numbers = ... (stored in memory)
#      index_30 = ... (stored in memory)
#      print(index_30)

"""

"""
Problem 960:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Run numbers.sort()
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Run numbers.sort()
#   8. Run print(numbers)
#   9. Assign to numbers
#   10. Run numbers.sort()
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      numbers.sort()
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.sort()
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.sort()
#      print(numbers)

"""

"""
Problem 961:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Run numbers.reverse()
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Run numbers.reverse()
#   8. Run print(numbers)
#   9. Assign to numbers
#   10. Run numbers.reverse()
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      numbers.reverse()
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.reverse()
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.reverse()
#      print(numbers)

"""

"""
Problem 962:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Run numbers.append(4)
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Run numbers.append(4)
#   8. Run print(numbers)
#   9. Assign to numbers
#   10. Run numbers.append(4)
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      numbers.append(4)
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.append(4)
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.append(4)
#      print(numbers)

"""

"""
Problem 963:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to last_item
#   3. Run print(numbers)
#   4. Run print(last_item)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to numbers
#   8. Assign to last_item
#   9. Run print(numbers)
#   10. Run print(last_item)
#   11. Assign to numbers
#   12. Assign to last_item
#   13. Run print(numbers)
#   14. Run print(last_item)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      last_item = ... (stored in memory)
#      print(numbers)
#      print(last_item)
#      numbers = ... (stored in memory)
#      last_item = ... (stored in memory)
#      print(numbers)
#      print(last_item)
#      numbers = ... (stored in memory)
#      last_item = ... (stored in memory)
#      print(numbers)
#      print(last_item)

"""

"""
Problem 964:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Run numbers.remove(10)
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Run numbers.remove(10)
#   8. Run print(numbers)
#   9. Assign to numbers
#   10. Run numbers.remove(10)
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      numbers.remove(10)
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.remove(10)
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.remove(10)
#      print(numbers)

"""

"""
Problem 965:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(list(keys)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to person
#   2. Assign to keys
#   3. Run print(list(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      keys = ... (stored in memory)
#      print(list(...))

"""

"""
Problem 966:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(list(values)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to person
#   2. Assign to values
#   3. Run print(list(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      values = ... (stored in memory)
#      print(list(...))

"""

"""
Problem 967:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to dict1
#   2. Assign to dict2
#   3. Run dict1.update(dict2)
#   4. Run print(dict1)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to dict1
#   8. Assign to dict2
#   9. Run dict1.update(dict2)
#   10. Run print(dict1)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Run dict1.update(dict2)
#   14. Run print(dict1)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      print(dict1)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      print(dict1)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      print(dict1)

"""

"""
Problem 968:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to person
#   2. Run person.pop('age')
#   3. Run print(person)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to person
#   7. Run person.pop('age')
#   8. Run print(person)
#   9. Assign to person
#   10. Run person.pop('age')
#   11. Run print(person)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      person.pop('age')
#      print(person)
#      person = ... (stored in memory)
#      person.pop('age')
#      print(person)
#      person = ... (stored in memory)
#      person.pop('age')
#      print(person)

"""

"""
Problem 969:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to numbers
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 970:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to removed_element
#   3. Run print(removed_element)
#   4. Run print(numbers)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to numbers
#   8. Assign to removed_element
#   9. Run print(removed_element)
#   10. Run print(numbers)
#   11. Assign to numbers
#   12. Assign to removed_element
#   13. Run print(removed_element)
#   14. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      removed_element = ... (stored in memory)
#      print(removed_element)
#      print(numbers)
#      numbers = ... (stored in memory)
#      removed_element = ... (stored in memory)
#      print(removed_element)
#      print(numbers)
#      numbers = ... (stored in memory)
#      removed_element = ... (stored in memory)
#      print(removed_element)
#      print(numbers)

"""

"""
Problem 971:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Run numbers.clear()
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Run numbers.clear()
#   8. Run print(numbers)
#   9. Assign to numbers
#   10. Run numbers.clear()
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      numbers.clear()
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.clear()
#      print(numbers)
#      numbers = ... (stored in memory)
#      numbers.clear()
#      print(numbers)

"""

"""
Problem 972:
#
# ===== SYNTAX =====
#   def solve()
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num_str
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to num_str
#   7. Assign to num
#   8. Run print(num)
#   9. Assign to num_str
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      num_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      num_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 973:
#
# ===== SYNTAX =====
#   def solve()
#   float() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num_str
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to num_str
#   7. Assign to num
#   8. Run print(num)
#   9. Assign to num_str
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      num_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      num_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 974:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to bool_str
#   2. Assign to boolean_value
#   3. Run print(boolean_value)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to bool_str
#   7. Assign to boolean_value
#   8. Run print(boolean_value)
#   9. Assign to bool_str
#   10. Assign to boolean_value
#   11. Run print(boolean_value)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      bool_str = ... (stored in memory)
#      boolean_value = ... (stored in memory)
#      print(boolean_value)
#      bool_str = ... (stored in memory)
#      boolean_value = ... (stored in memory)
#      print(boolean_value)
#      bool_str = ... (stored in memory)
#      boolean_value = ... (stored in memory)
#      print(boolean_value)

"""

"""
Problem 975:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num_str
#   2. Assign to numbers
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to num_str
#   7. Assign to numbers
#   8. Run print(numbers)
#   9. Assign to num_str
#   10. Assign to numbers
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num_str = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      num_str = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      num_str = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)

"""

"""
Problem 976:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import datetime from datetime
#   2. Assign to date_str
#   3. Assign to parsed_date
#   4. Run print(parsed_date)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import datetime from datetime
#   8. Assign to date_str
#   9. Assign to parsed_date
#   10. Run print(parsed_date)
#   11. Import datetime from datetime
#   12. Assign to date_str
#   13. Assign to parsed_date
#   14. Run print(parsed_date)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      date_str = ... (stored in memory)
#      parsed_date = ... (stored in memory)
#      print(parsed_date)
#      date_str = ... (stored in memory)
#      parsed_date = ... (stored in memory)
#      print(parsed_date)
#      date_str = ... (stored in memory)
#      parsed_date = ... (stored in memory)
#      print(parsed_date)

"""

"""
Problem 977:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to text
#   3. Assign to numbers
#   4. Run print(numbers)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to text
#   9. Assign to numbers
#   10. Run print(numbers)
#   11. Import re
#   12. Assign to text
#   13. Assign to numbers
#   14. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      text = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      text = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)

"""

"""
Problem 978:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 979:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#   2. Assign to data
#   3. Assign to json_str
#   4. Run print(json_str)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import json
#   8. Assign to data
#   9. Assign to json_str
#   10. Run print(json_str)
#   11. Import json
#   12. Assign to data
#   13. Assign to json_str
#   14. Run print(json_str)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      json_str = ... (stored in memory)
#      print(json_str)
#      data = ... (stored in memory)
#      json_str = ... (stored in memory)
#      print(json_str)
#      data = ... (stored in memory)
#      json_str = ... (stored in memory)
#      print(json_str)

"""

"""
Problem 980:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import xml.etree.ElementTree
#   2. Assign to xml_str
#   3. Assign to root
#   4. Assign to name
#   5. Run print(name)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import xml.etree.ElementTree
#   9. Assign to xml_str
#   10. Assign to root
#   11. Assign to name
#   12. Run print(name)
#   13. Import xml.etree.ElementTree
#   14. Assign to xml_str
#   15. Assign to root
#   16. Assign to name
#   17. Run print(name)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      xml_str = ... (stored in memory)
#      root = ... (stored in memory)
#      name = ... (stored in memory)
#      print(name)
#      xml_str = ... (stored in memory)
#      root = ... (stored in memory)
#      name = ... (stored in memory)
#      print(name)
#      xml_str = ... (stored in memory)
#      root = ... (stored in memory)
#      name = ... (stored in memory)
#      print(name)

"""

"""
Problem 981:
#
# ===== SYNTAX =====
#   def solve()
#   parse_qs() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import parse_qs from urllib.parse
#   2. Assign to query_str
#   3. Assign to parsed_data
#   4. Run print(parsed_data)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import parse_qs from urllib.parse
#   8. Assign to query_str
#   9. Assign to parsed_data
#   10. Run print(parsed_data)
#   11. Import parse_qs from urllib.parse
#   12. Assign to query_str
#   13. Assign to parsed_data
#   14. Run print(parsed_data)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      query_str = ... (stored in memory)
#      parsed_data = ... (stored in memory)
#      print(parsed_data)
#      query_str = ... (stored in memory)
#      parsed_data = ... (stored in memory)
#      print(parsed_data)
#      query_str = ... (stored in memory)
#      parsed_data = ... (stored in memory)
#      print(parsed_data)

"""

"""
Problem 982:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import csv
#   2. Import StringIO from io
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 983:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import HTMLParser from html.parser
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 984:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import datetime from datetime
#   2. Assign to time_str
#   3. Assign to parsed_time
#   4. Run print(parsed_time)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import datetime from datetime
#   8. Assign to time_str
#   9. Assign to parsed_time
#   10. Run print(parsed_time)
#   11. Import datetime from datetime
#   12. Assign to time_str
#   13. Assign to parsed_time
#   14. Run print(parsed_time)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      time_str = ... (stored in memory)
#      parsed_time = ... (stored in memory)
#      print(parsed_time)
#      time_str = ... (stored in memory)
#      parsed_time = ... (stored in memory)
#      print(parsed_time)
#      time_str = ... (stored in memory)
#      parsed_time = ... (stored in memory)
#      print(parsed_time)

"""

"""
Problem 985:
#
# ===== SYNTAX =====
#   def solve()
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to hex_str
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to hex_str
#   7. Assign to num
#   8. Run print(num)
#   9. Assign to hex_str
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      hex_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      hex_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      hex_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 986:
#
# ===== SYNTAX =====
#   def solve()
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to binary_str
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to binary_str
#   7. Assign to num
#   8. Run print(num)
#   9. Assign to binary_str
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      binary_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      binary_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      binary_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 987:
#
# ===== SYNTAX =====
#   def solve()
#   float() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sci_str
#   2. Assign to num
#   3. Run print(num)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sci_str
#   7. Assign to num
#   8. Run print(num)
#   9. Assign to sci_str
#   10. Assign to num
#   11. Run print(num)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sci_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      sci_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)
#      sci_str = ... (stored in memory)
#      num = ... (stored in memory)
#      print(num)

"""

"""
Problem 988:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import configparser
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 989:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 991:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#   2. Assign to json_str
#   3. Assign to data
#   4. Run print(data)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import json
#   8. Assign to json_str
#   9. Assign to data
#   10. Run print(data)
#   11. Import json
#   12. Assign to json_str
#   13. Assign to data
#   14. Run print(data)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      json_str = ... (stored in memory)
#      data = ... (stored in memory)
#      print(data)
#      json_str = ... (stored in memory)
#      data = ... (stored in memory)
#      print(data)
#      json_str = ... (stored in memory)
#      data = ... (stored in memory)
#      print(data)

"""

"""
Problem 992:
#
# ===== SYNTAX =====
#   def solve()
#   float() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to currency_str
#   3. Assign to value
#   4. Run print(value)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to currency_str
#   9. Assign to value
#   10. Run print(value)
#   11. Import re
#   12. Assign to currency_str
#   13. Assign to value
#   14. Run print(value)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      currency_str = ... (stored in memory)
#      value = ... (stored in memory)
#      print(value)
#      currency_str = ... (stored in memory)
#      value = ... (stored in memory)
#      print(value)
#      currency_str = ... (stored in memory)
#      value = ... (stored in memory)
#      print(value)

"""

"""
Problem 993:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import argparse
#   2. Assign to parser
#   3. Run parser.add_argument('--name')
#   4. Assign to args
#   5. Run print(?)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Import argparse
#   9. Assign to parser
#   10. Run parser.add_argument('--name')
#   11. Assign to args
#   12. Run print(?)
#   13. Import argparse
#   14. Assign to parser
#   15. Run parser.add_argument('--name')
#   16. Assign to args
#   17. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      parser = ... (stored in memory)
#      parser.add_argument('--name')
#      args = ... (stored in memory)
#      print(?)
#      parser = ... (stored in memory)
#      parser.add_argument('--name')
#      args = ... (stored in memory)
#      print(?)
#      parser = ... (stored in memory)
#      parser.add_argument('--name')
#      args = ... (stored in memory)
#      print(?)

"""

"""
Problem 994:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 995:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 996:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_list
#   3. Run print(sliced_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_list
#   8. Run print(sliced_list)
#   9. Assign to numbers
#   10. Assign to sliced_list
#   11. Run print(sliced_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)

"""

"""
Problem 997:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_list
#   3. Run print(sliced_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_list
#   8. Run print(sliced_list)
#   9. Assign to numbers
#   10. Assign to sliced_list
#   11. Run print(sliced_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)

"""

"""
Problem 998:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 999:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1000:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to reversed_text
#   3. Run print(reversed_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to reversed_text
#   8. Run print(reversed_text)
#   9. Assign to text
#   10. Assign to reversed_text
#   11. Run print(reversed_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      reversed_text = ... (stored in memory)
#      print(reversed_text)
#      text = ... (stored in memory)
#      reversed_text = ... (stored in memory)
#      print(reversed_text)
#      text = ... (stored in memory)
#      reversed_text = ... (stored in memory)
#      print(reversed_text)

"""

