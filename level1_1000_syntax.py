# Syntax documentation for level 1 (problems 1-1000)
# Auto-generated by gen_syntax_files.py

"""
Problem 501:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Bird → defines a new type
#     def fly(...) → method of Bird
#   class Penguin(Bird) → inherits from parent class
#     def fly(...) → method of Penguin
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Bird, Penguin()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Bird → class definition stored
#   2. class Penguin(Bird) → class definition stored
#   3. generic_bird = ... → assigns value to variable
#   4. print(generic_bird.fly(...))
#   5. penguin = ... → assigns value to variable
#   6. print(penguin.fly(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Bird(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Penguin, Bird) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     generic_bird = ... → stored in memory
#     print(generic_bird.fly(...))
#     penguin = ... → stored in memory
#     print(penguin.fly(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Bird(...))

"""

"""
Problem 502:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Book → defines a new type
#     def __init__(...) → method of Book
#   class EBook(Book) → inherits from parent class
#     def __init__(...) → method of EBook
#   class Computer → defines a new type
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Book, EBook, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Book → class definition stored
#   2. class EBook(Book) → class definition stored
#   3. ebook = ... → assigns value to variable
#   4. print(?)
#   5. from dataclasses import dataclass
#   6. class Computer → class definition stored
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Book(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (EBook, Computer, Book) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ebook = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Book(...))

"""

"""
Problem 503:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Instrument → defines a new type
#     def play(...) → method of Instrument
#   class Guitar(Instrument) → inherits from parent class
#     def play(...) → method of Guitar
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Guitar, Instrument()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Instrument → class definition stored
#   2. class Guitar(Instrument) → class definition stored
#   3. generic_instrument = ... → assigns value to variable
#   4. print(generic_instrument.play(...))
#   5. guitar = ... → assigns value to variable
#   6. print(guitar.play(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Instrument(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Instrument, Guitar) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     generic_instrument = ... → stored in memory
#     print(generic_instrument.play(...))
#     guitar = ... → stored in memory
#     print(guitar.play(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Instrument(...))

"""

"""
Problem 504:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Game → defines a new type
#     def start(...) → method of Game
#   class Chess(Game) → inherits from parent class
#     def start(...) → method of Chess
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Chess, Game()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Game → class definition stored
#   2. class Chess(Game) → class definition stored
#   3. generic_game = ... → assigns value to variable
#   4. print(generic_game.start(...))
#   5. chess_game = ... → assigns value to variable
#   6. print(chess_game.start(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Game(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Chess, Game) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     generic_game = ... → stored in memory
#     print(generic_game.start(...))
#     chess_game = ... → stored in memory
#     print(chess_game.start(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Game(...))

"""

"""
Problem 505:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Vehicle → defines a new type
#     def drive(...) → method of Vehicle
#   class Bicycle(Vehicle) → inherits from parent class
#     def drive(...) → method of Bicycle
#
# Built-in functions: Bicycle, Vehicle()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Vehicle → class definition stored
#   2. class Bicycle(Vehicle) → class definition stored
#   3. bicycle = ... → assigns value to variable
#   4. print(bicycle.drive(...))
#   5. obj = ... → assigns value to variable
#   6. print(obj)
#   7. print(Vehicle(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Bicycle, Vehicle) are defined — methods are stored, not called
#
#   Execution begins at the top-level code (outside def/class blocks):
#     bicycle = ... → stored in memory
#     print(bicycle.drive(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Vehicle(...))

"""

"""
Problem 506:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Computer → defines a new type
#     def __init__(...) → method of Computer
#   class Laptop(Computer) → inherits from parent class
#     def __init__(...) → method of Laptop
#   class Computer → defines a new type
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Computer, Laptop, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Computer → class definition stored
#   2. class Laptop(Computer) → class definition stored
#   3. laptop = ... → assigns value to variable
#   4. print(?)
#   5. from dataclasses import dataclass
#   6. class Computer → class definition stored
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Computer(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Computer, Laptop) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     laptop = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Computer(...))

"""

"""
Problem 507:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Device → defines a new type
#     def turn_on(...) → method of Device
#   class Smartphone(Device) → inherits from parent class
#     def __init__(...) → method of Smartphone
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Device, Smartphone()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Device → class definition stored
#   2. class Smartphone(Device) → class definition stored
#   3. phone = ... → assigns value to variable
#   4. print(phone.turn_on(...))
#   5. print(?)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. obj = ... → assigns value to variable
#   9. print(obj)
#   10. print(Device(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Device, Smartphone) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     phone = ... → stored in memory
#     print(phone.turn_on(...))
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Device(...))

"""

"""
Problem 508:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Parent → defines a new type
#     def display(...) → method of Parent
#   class Child(Parent) → inherits from parent class
#     def display(...) → method of Child
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Child, Parent()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Parent → class definition stored
#   2. class Child(Parent) → class definition stored
#   3. parent = ... → assigns value to variable
#   4. print(parent.display(...))
#   5. child = ... → assigns value to variable
#   6. print(child.display(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Parent(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Child, Parent) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     parent = ... → stored in memory
#     print(parent.display(...))
#     child = ... → stored in memory
#     print(child.display(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Parent(...))

"""

"""
Problem 509:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Building → defines a new type
#     def __init__(...) → method of Building
#   class Skyscraper(Building) → inherits from parent class
#     def __init__(...) → method of Skyscraper
#   class Computer → defines a new type
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: Building, Skyscraper, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Building → class definition stored
#   2. class Skyscraper(Building) → class definition stored
#   3. skyscraper = ... → assigns value to variable
#   4. print(?)
#   5. from dataclasses import dataclass
#   6. class Computer → class definition stored
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Building(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Computer, Skyscraper, Building) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     skyscraper = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Building(...))

"""

"""
Problem 510:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Plant → defines a new type
#     def grow(...) → method of Plant
#   class Flower(Plant) → inherits from parent class
#     def grow(...) → method of Flower
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Flower, Plant()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Plant → class definition stored
#   2. class Flower(Plant) → class definition stored
#   3. plant = ... → assigns value to variable
#   4. print(plant.grow(...))
#   5. flower = ... → assigns value to variable
#   6. print(flower.grow(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Plant(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Plant, Flower) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     plant = ... → stored in memory
#     print(plant.grow(...))
#     flower = ... → stored in memory
#     print(flower.grow(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Plant(...))

"""

"""
Problem 511:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Teacher → defines a new type
#     def __init__(...) → method of Teacher
#     def __str__(...) → method of Teacher
#   class MathTeacher(Teacher) → inherits from parent class
#     def __init__(...) → method of MathTeacher
#     def teach_math(...) → method of MathTeacher
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: MathTeacher, Teacher, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Teacher → class definition stored
#   2. class MathTeacher(Teacher) → class definition stored
#   3. teacher = ... → assigns value to variable
#   4. print(teacher)
#   5. math_teacher = ... → assigns value to variable
#   6. print(math_teacher)
#   7. print(math_teacher.teach_math(...))
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. obj = ... → assigns value to variable
#   11. print(obj)
#   12. print(Teacher(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Teacher, MathTeacher) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     teacher = ... → stored in memory
#     print(teacher)
#     math_teacher = ... → stored in memory
#     print(math_teacher)
#     print(math_teacher.teach_math(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Teacher(...))

"""

"""
Problem 512:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Writer → defines a new type
#     def __init__(...) → method of Writer
#     def __str__(...) → method of Writer
#   class Poet(Writer) → inherits from parent class
#     def __init__(...) → method of Poet
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Poet, Writer, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Writer → class definition stored
#   2. class Poet(Writer) → class definition stored
#   3. writer = ... → assigns value to variable
#   4. print(writer)
#   5. poet = ... → assigns value to variable
#   6. print(poet)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Writer(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Writer, Poet) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     writer = ... → stored in memory
#     print(writer)
#     poet = ... → stored in memory
#     print(poet)
#     obj = ... → stored in memory
#     print(obj)
#     print(Writer(...))

"""

"""
Problem 513:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Appliance → defines a new type
#     def use(...) → method of Appliance
#   class Oven(Appliance) → inherits from parent class
#     def use(...) → method of Oven
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Appliance, Oven()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Appliance → class definition stored
#   2. class Oven(Appliance) → class definition stored
#   3. appliance = ... → assigns value to variable
#   4. print(appliance.use(...))
#   5. oven = ... → assigns value to variable
#   6. print(oven.use(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Appliance(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Appliance, Oven) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     appliance = ... → stored in memory
#     print(appliance.use(...))
#     oven = ... → stored in memory
#     print(oven.use(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Appliance(...))

"""

"""
Problem 514:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Artist → defines a new type
#     def __init__(...) → method of Artist
#     def __str__(...) → method of Artist
#   class Painter(Artist) → inherits from parent class
#     def paint(...) → method of Painter
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: Artist, Painter()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   'Painting with ' + self.medium → Python looks up 'Painting with ', then self.medium, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Artist → class definition stored
#   2. class Painter(Artist) → class definition stored
#   3. artist = ... → assigns value to variable
#   4. print(artist)
#   5. painter = ... → assigns value to variable
#   6. print(painter)
#   7. print(painter.paint(...))
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. obj = ... → assigns value to variable
#   11. print(obj)
#   12. print(Artist(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Artist, Painter) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     artist = ... → stored in memory
#     print(artist)
#     painter = ... → stored in memory
#     print(painter)
#     print(painter.paint(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Artist(...))

"""

"""
Problem 515:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Worker → defines a new type
#     def work(...) → method of Worker
#   class Electrician(Worker) → inherits from parent class
#     def work(...) → method of Electrician
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Electrician, Worker()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Worker → class definition stored
#   2. class Electrician(Worker) → class definition stored
#   3. worker = ... → assigns value to variable
#   4. print(worker.work(...))
#   5. electrician = ... → assigns value to variable
#   6. print(electrician.work(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Worker(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Electrician, Worker) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     worker = ... → stored in memory
#     print(worker.work(...))
#     electrician = ... → stored in memory
#     print(electrician.work(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Worker(...))

"""

"""
Problem 516:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Animal → defines a new type
#     def eat(...) → method of Animal
#   class Lion(Animal) → inherits from parent class
#     def eat(...) → method of Lion
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Animal, Lion()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Animal → class definition stored
#   2. class Lion(Animal) → class definition stored
#   3. animal = ... → assigns value to variable
#   4. print(animal.eat(...))
#   5. lion = ... → assigns value to variable
#   6. print(lion.eat(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Animal(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Lion, Animal) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     animal = ... → stored in memory
#     print(animal.eat(...))
#     lion = ... → stored in memory
#     print(lion.eat(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Animal(...))

"""

"""
Problem 517:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Appliance → defines a new type
#     def run(...) → method of Appliance
#   class Fan(Appliance) → inherits from parent class
#     def run(...) → method of Fan
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Appliance, Fan()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Appliance → class definition stored
#   2. class Fan(Appliance) → class definition stored
#   3. appliance = ... → assigns value to variable
#   4. print(appliance.run(...))
#   5. fan = ... → assigns value to variable
#   6. print(fan.run(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Appliance(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Fan, Appliance) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     appliance = ... → stored in memory
#     print(appliance.run(...))
#     fan = ... → stored in memory
#     print(fan.run(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Appliance(...))

"""

"""
Problem 518:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Employee → defines a new type
#     def get_salary(...) → method of Employee
#   class Intern(Employee) → inherits from parent class
#     def get_salary(...) → method of Intern
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Employee, Intern()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Employee → class definition stored
#   2. class Intern(Employee) → class definition stored
#   3. employee = ... → assigns value to variable
#   4. print(?)
#   5. intern = ... → assigns value to variable
#   6. print(?)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Employee(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Intern, Employee) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     employee = ... → stored in memory
#     print(?)
#     intern = ... → stored in memory
#     print(?)
#     obj = ... → stored in memory
#     print(obj)
#     print(Employee(...))

"""

"""
Problem 519:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Vehicle → defines a new type
#     def __init__(...) → method of Vehicle
#     def __str__(...) → method of Vehicle
#   class Motorcycle(Vehicle) → inherits from parent class
#     def __init__(...) → method of Motorcycle
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Motorcycle, Vehicle, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Vehicle → class definition stored
#   2. class Motorcycle(Vehicle) → class definition stored
#   3. vehicle = ... → assigns value to variable
#   4. print(vehicle)
#   5. motorcycle = ... → assigns value to variable
#   6. print(motorcycle)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Vehicle(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Motorcycle, Vehicle) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     vehicle = ... → stored in memory
#     print(vehicle)
#     motorcycle = ... → stored in memory
#     print(motorcycle)
#     obj = ... → stored in memory
#     print(obj)
#     print(Vehicle(...))

"""

"""
Problem 520:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Tool → defines a new type
#     def use(...) → method of Tool
#   class Hammer(Tool) → inherits from parent class
#     def use(...) → method of Hammer
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Hammer, Tool()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Tool → class definition stored
#   2. class Hammer(Tool) → class definition stored
#   3. tool = ... → assigns value to variable
#   4. print(tool.use(...))
#   5. hammer = ... → assigns value to variable
#   6. print(hammer.use(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Tool(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Hammer, Tool) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     tool = ... → stored in memory
#     print(tool.use(...))
#     hammer = ... → stored in memory
#     print(hammer.use(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Tool(...))

"""

"""
Problem 521:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class BankAccount → defines a new type
#     def __init__(...) → method of BankAccount
#     def __str__(...) → method of BankAccount
#   class SavingsAccount(BankAccount) → inherits from parent class
#     def __init__(...) → method of SavingsAccount
#     def __str__(...) → method of SavingsAccount
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: BankAccount, SavingsAccount, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class BankAccount → class definition stored
#   2. class SavingsAccount(BankAccount) → class definition stored
#   3. account = ... → assigns value to variable
#   4. print(account)
#   5. savings = ... → assigns value to variable
#   6. print(savings)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(BankAccount(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (BankAccount, SavingsAccount) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     account = ... → stored in memory
#     print(account)
#     savings = ... → stored in memory
#     print(savings)
#     obj = ... → stored in memory
#     print(obj)
#     print(BankAccount(...))

"""

"""
Problem 522:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Weapon → defines a new type
#     def attack(...) → method of Weapon
#   class Sword(Weapon) → inherits from parent class
#     def attack(...) → method of Sword
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Sword, Weapon()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Weapon → class definition stored
#   2. class Sword(Weapon) → class definition stored
#   3. weapon = ... → assigns value to variable
#   4. print(weapon.attack(...))
#   5. sword = ... → assigns value to variable
#   6. print(sword.attack(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Weapon(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Weapon, Sword) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     weapon = ... → stored in memory
#     print(weapon.attack(...))
#     sword = ... → stored in memory
#     print(sword.attack(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Weapon(...))

"""

"""
Problem 523:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Appliance → defines a new type
#     def __init__(...) → method of Appliance
#     def __str__(...) → method of Appliance
#   class Microwave(Appliance) → inherits from parent class
#     def __init__(...) → method of Microwave
#     def heat_food(...) → method of Microwave
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Appliance, Microwave, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Appliance → class definition stored
#   2. class Microwave(Appliance) → class definition stored
#   3. appliance = ... → assigns value to variable
#   4. print(appliance)
#   5. microwave = ... → assigns value to variable
#   6. print(microwave)
#   7. print(microwave.heat_food(...))
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. obj = ... → assigns value to variable
#   11. print(obj)
#   12. print(Appliance(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Appliance, Microwave) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     appliance = ... → stored in memory
#     print(appliance)
#     microwave = ... → stored in memory
#     print(microwave)
#     print(microwave.heat_food(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Appliance(...))

"""

"""
Problem 524:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Vehicle → defines a new type
#     def start(...) → method of Vehicle
#   class ElectricCar(Vehicle) → inherits from parent class
#     def start(...) → method of ElectricCar
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: ElectricCar, Vehicle()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Vehicle → class definition stored
#   2. class ElectricCar(Vehicle) → class definition stored
#   3. vehicle = ... → assigns value to variable
#   4. print(vehicle.start(...))
#   5. electric_car = ... → assigns value to variable
#   6. print(electric_car.start(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Vehicle(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (ElectricCar, Vehicle) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     vehicle = ... → stored in memory
#     print(vehicle.start(...))
#     electric_car = ... → stored in memory
#     print(electric_car.start(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Vehicle(...))

"""

"""
Problem 525:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Pet → defines a new type
#     def make_sound(...) → method of Pet
#   class Cat(Pet) → inherits from parent class
#     def make_sound(...) → method of Cat
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Cat, Pet()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Pet → class definition stored
#   2. class Cat(Pet) → class definition stored
#   3. pet = ... → assigns value to variable
#   4. print(pet.make_sound(...))
#   5. cat = ... → assigns value to variable
#   6. print(cat.make_sound(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Pet(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Cat, Pet) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pet = ... → stored in memory
#     print(pet.make_sound(...))
#     cat = ... → stored in memory
#     print(cat.make_sound(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Pet(...))

"""

"""
Problem 526:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Chef → defines a new type
#     def cook(...) → method of Chef
#   class PastryChef(Chef) → inherits from parent class
#     def cook(...) → method of PastryChef
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Chef, PastryChef()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Chef → class definition stored
#   2. class PastryChef(Chef) → class definition stored
#   3. chef = ... → assigns value to variable
#   4. print(chef.cook(...))
#   5. pastry_chef = ... → assigns value to variable
#   6. print(pastry_chef.cook(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Chef(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (PastryChef, Chef) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     chef = ... → stored in memory
#     print(chef.cook(...))
#     pastry_chef = ... → stored in memory
#     print(pastry_chef.cook(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Chef(...))

"""

"""
Problem 527:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_chair(material, color, legs) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Chair, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_chair('wood', 'brown', 4) → each argument is evaluated first, then the function body runs
#   create_chair(material, color, legs) → each argument is evaluated first, then the function body runs
#   create_chair(25, 25, 25) runs first → then print() outputs the result
#   create_chair(25, 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_chair(material, color, legs) → stored in memory, not executed
#   2. my_chair = ... → assigns value to variable
#   3. def create_chair(material, color, legs) → stored in memory, not executed
#   4. def create_chair(material, color, legs) → stored in memory, not executed
#   5. material = ... → assigns value to variable
#   6. color = ... → assigns value to variable
#   7. legs = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(create_chair(...))
#      → calls create_chair(material, color, legs) with material=25, color=25, legs=25
#      → 10.1 return Chair(material, color, legs)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_chair) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     my_chair = ... → stored in memory
#     material = ... → stored in memory
#     color = ... → stored in memory
#     legs = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_chair(...)) → create_chair() runs first → value passed to print()

"""

"""
Problem 528:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Game → defines a new type
#     def __init__(...) → method of Game
#     def __str__(...) → method of Game
#   class Soccer(Game) → inherits from parent class
#     def __init__(...) → method of Soccer
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Game, Soccer, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Game → class definition stored
#   2. class Soccer(Game) → class definition stored
#   3. game = ... → assigns value to variable
#   4. print(game)
#   5. soccer = ... → assigns value to variable
#   6. print(soccer)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Game(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Game, Soccer) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     game = ... → stored in memory
#     print(game)
#     soccer = ... → stored in memory
#     print(soccer)
#     obj = ... → stored in memory
#     print(obj)
#     print(Game(...))

"""

"""
Problem 529:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Musician → defines a new type
#     def play_instrument(...) → method of Musician
#   class Pianist(Musician) → inherits from parent class
#     def play_instrument(...) → method of Pianist
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Musician, Pianist()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Musician → class definition stored
#   2. class Pianist(Musician) → class definition stored
#   3. musician = ... → assigns value to variable
#   4. print(musician.play_instrument(...))
#   5. pianist = ... → assigns value to variable
#   6. print(pianist.play_instrument(...))
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Musician(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Musician, Pianist) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     musician = ... → stored in memory
#     print(musician.play_instrument(...))
#     pianist = ... → stored in memory
#     print(pianist.play_instrument(...))
#     obj = ... → stored in memory
#     print(obj)
#     print(Musician(...))

"""

"""
Problem 530:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Company → defines a new type
#     def __init__(...) → method of Company
#     def __str__(...) → method of Company
#   class TechCompany(Company) → inherits from parent class
#     def __init__(...) → method of TechCompany
#     def __str__(...) → method of TechCompany
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Company, TechCompany, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Company → class definition stored
#   2. class TechCompany(Company) → class definition stored
#   3. company = ... → assigns value to variable
#   4. print(company)
#   5. tech_company = ... → assigns value to variable
#   6. print(tech_company)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Company(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (TechCompany, Company) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     company = ... → stored in memory
#     print(company)
#     tech_company = ... → stored in memory
#     print(tech_company)
#     obj = ... → stored in memory
#     print(obj)
#     print(Company(...))

"""

"""
Problem 531:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Gadget → defines a new type
#     def operate(...) → method of Gadget
#   class Smartwatch(Gadget) → inherits from parent class
#     def __init__(...) → method of Smartwatch
#     def __str__(...) → method of Smartwatch
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Gadget, Smartwatch, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Gadget → class definition stored
#   2. class Smartwatch(Gadget) → class definition stored
#   3. gadget = ... → assigns value to variable
#   4. print(gadget.operate(...))
#   5. smartwatch = ... → assigns value to variable
#   6. print(smartwatch.operate(...))
#   7. print(smartwatch)
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. obj = ... → assigns value to variable
#   11. print(obj)
#   12. print(Gadget(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Smartwatch, Gadget) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     gadget = ... → stored in memory
#     print(gadget.operate(...))
#     smartwatch = ... → stored in memory
#     print(smartwatch.operate(...))
#     print(smartwatch)
#     obj = ... → stored in memory
#     print(obj)
#     print(Gadget(...))

"""

"""
Problem 532:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Trainer → defines a new type
#     def train(...) → method of Trainer
#   class FitnessTrainer(Trainer) → inherits from parent class
#     def train(...) → method of FitnessTrainer
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: FitnessTrainer, Trainer()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Trainer → class definition stored
#   2. class FitnessTrainer(Trainer) → class definition stored
#   3. trainer = ... → assigns value to variable
#   4. print(trainer.train(...))
#   5. fitness_trainer = ... → assigns value to variable
#   6. fitness_trainer.train()
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Trainer(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Trainer, FitnessTrainer) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     trainer = ... → stored in memory
#     print(trainer.train(...))
#     fitness_trainer = ... → stored in memory
#     fitness_trainer.train()
#     obj = ... → stored in memory
#     print(obj)
#     print(Trainer(...))

"""

"""
Problem 533:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Book → defines a new type
#     def read(...) → method of Book
#   class ComicBook(Book) → inherits from parent class
#     def read(...) → method of ComicBook
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: Book, ComicBook()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Book → class definition stored
#   2. class ComicBook(Book) → class definition stored
#   3. book = ... → assigns value to variable
#   4. print(book.read(...))
#   5. comic_book = ... → assigns value to variable
#   6. comic_book.read()
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Book(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Book, ComicBook) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     book = ... → stored in memory
#     print(book.read(...))
#     comic_book = ... → stored in memory
#     comic_book.read()
#     obj = ... → stored in memory
#     print(obj)
#     print(Book(...))

"""

"""
Problem 534:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Software → defines a new type
#     def __init__(...) → method of Software
#     def __str__(...) → method of Software
#   class GameSoftware(Software) → inherits from parent class
#     def __init__(...) → method of GameSoftware
#     def __str__(...) → method of GameSoftware
#
#   def solve() → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: GameSoftware, Software, super()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Software → class definition stored
#   2. class GameSoftware(Software) → class definition stored
#   3. software = ... → assigns value to variable
#   4. print(software)
#   5. game_software = ... → assigns value to variable
#   6. print(game_software)
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. obj = ... → assigns value to variable
#   10. print(obj)
#   11. print(Software(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (GameSoftware, Software) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     software = ... → stored in memory
#     print(software)
#     game_software = ... → stored in memory
#     print(game_software)
#     obj = ... → stored in memory
#     print(obj)
#     print(Software(...))

"""

"""
Problem 535:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_letter(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_letter(string) runs first → then print() outputs the result
#   count_letter(s) → each argument is evaluated first, then the function body runs
#   count_letter(10) runs first → then print() outputs the result
#   count_letter(string) → each argument is evaluated first, then the function body runs
#   count_letter(10) → each argument is evaluated first, then the function body runs
#   c == 'a' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_letter(s) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_letter(...))
#      → calls count_letter(s) with s=string
#      → 3.1 count = 0
#      → 3.2 for c in s:
#      → 3.2     if c == 'a':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_letter(s) → stored in memory, not executed
#   5. def count_letter(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_letter(...))
#      → calls count_letter(s) with s=10
#      → 9.1 count = 0
#      → 9.2 for c in s:
#      → 9.2     if c == 'a':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_letter(...)) → count_letter() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_letter(...)) → count_letter() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 536:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_number(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_number(number) runs first → then print() outputs the result
#   count_number(num) → each argument is evaluated first, then the function body runs
#   count_number(25) runs first → then print() outputs the result
#   count_number(number) → each argument is evaluated first, then the function body runs
#   count_number(25) → each argument is evaluated first, then the function body runs
#   c == 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_number(num) → stored in memory, not executed
#   2. number = ... → assigns value to variable
#   3. print(count_number(...))
#      → calls count_number(num) with num=number
#      → 3.1 count = 0
#      → 3.2 for c in num:
#      → 3.2     if c == 5:
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_number(num) → stored in memory, not executed
#   5. def count_number(num) → stored in memory, not executed
#   6. num = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_number(...))
#      → calls count_number(num) with num=25
#      → 9.1 count = 0
#      → 9.2 for c in num:
#      → 9.2     if c == 5:
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     print(count_number(...)) → count_number() runs first → value passed to print()
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_number(...)) → count_number() runs first → value passed to print()
#
#   Loop: takes one item from num each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 537:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_hello(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_hello(string) runs first → then print() outputs the result
#   count_hello(string) → each argument is evaluated first, then the function body runs
#   count_hello('world') runs first → then print() outputs the result
#   count_hello('world') → each argument is evaluated first, then the function body runs
#   c == 'hello' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_hello(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_hello(...))
#      → calls count_hello(string) with string=string
#      → 3.1 count = 0
#      → 3.2 for c in string:
#      → 3.2     if c == 'hello':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_hello(string) → stored in memory, not executed
#   5. def count_hello(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_hello(...))
#      → calls count_hello(string) with string='world'
#      → 9.1 count = 0
#      → 9.2 for c in string:
#      → 9.2     if c == 'hello':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_hello) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_hello(...)) → count_hello() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_hello(...)) → count_hello() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 538:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count(string) runs first → then print() outputs the result
#   count(string) → each argument is evaluated first, then the function body runs
#   count('world') runs first → then print() outputs the result
#   count('world') → each argument is evaluated first, then the function body runs
#   c == '#' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count(...))
#      → calls count(string) with string=string
#      → 3.1 count = 0
#      → 3.2 for c in string:
#      → 3.2     if c == '#':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count(string) → stored in memory, not executed
#   5. def count(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count(...))
#      → calls count(string) with string='world'
#      → 9.1 count = 0
#      → 9.2 for c in string:
#      → 9.2     if c == '#':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count(...)) → count() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count(...)) → count() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 540:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_number(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: str, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_number(num) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   count_number(num) → each argument is evaluated first, then the function body runs
#   count_number(25) runs first → then print() outputs the result
#   count_number(25) → each argument is evaluated first, then the function body runs
#   c == 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_number(num) → stored in memory, not executed
#   2. num = ... → assigns value to variable
#   3. print(count_number(...))
#      → calls count_number(num) with num=num
#      → 3.1 count = 0
#      → 3.2 for c in num:
#      → 3.2     if c == 0:
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def main(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. main(lst) → calls main(lst) with lst=lst
#      → 6.1 total = 0
#      → 6.2 for num in lst:
#      → 6.2     total += str(num).count('0')
#      → 6.3 print(total)
#   7. def count_number(num) → stored in memory, not executed
#   8. def count_number(num) → stored in memory, not executed
#   9. num = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(count_number(...))
#      → calls count_number(num) with num=25
#      → 12.1 count = 0
#      → 12.2 for c in num:
#      → 12.2     if c == 0:
#      → 12.2         count += 1
#      → 12.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_number, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     print(count_number(...)) → count_number() runs first → value passed to print()
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_number(...)) → count_number() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 544:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_letters(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_letters(lst) runs first → then print() outputs the result
#   count_letters(lst) → each argument is evaluated first, then the function body runs
#   count_letters(?) runs first → then print() outputs the result
#   count_letters(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_letters(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(count_letters(...))
#      → calls count_letters(lst) with lst=lst
#      → 3.1 return total_count
#   4. def count_letters(lst) → stored in memory, not executed
#   5. def count_letters(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_letters(...))
#      → calls count_letters(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return total_count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(count_letters(...)) → count_letters() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_letters(...)) → count_letters() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 545:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_elements_in_lst(lst, element) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_elements_in_lst(lst, element) runs first → then print() outputs the result
#   count_elements_in_lst(lst, element) → each argument is evaluated first, then the function body runs
#   count_elements_in_lst(?, 25) runs first → then print() outputs the result
#   count_elements_in_lst(?, 25) → each argument is evaluated first, then the function body runs
#   c == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_elements_in_lst(lst, element) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. element = ... → assigns value to variable
#   4. print(count_elements_in_lst(...))
#      → calls count_elements_in_lst(lst, element) with lst=lst, element=element
#      → 4.1 count = 0
#      → 4.2 for c in lst:
#      → 4.2     if c == element:
#      → 4.2         count += 1
#      → 4.3 return count
#   5. def count_elements_in_lst(lst, element) → stored in memory, not executed
#   6. def count_elements_in_lst(lst, element) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. element = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_elements_in_lst(...))
#      → calls count_elements_in_lst(lst, element) with lst=[1, 3, 5, 7], element=25
#      → 11.1 count = 0
#      → 11.2 for c in lst:
#      → 11.2     if c == element:
#      → 11.2         count += 1
#      → 11.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_elements_in_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     element = ... → stored in memory
#     print(count_elements_in_lst(...)) → count_elements_in_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     element = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_elements_in_lst(...)) → count_elements_in_lst() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 546:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_test_occurrences(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_test_occurrences(s) runs first → then print() outputs the result
#   count_test_occurrences(s) → each argument is evaluated first, then the function body runs
#   count_test_occurrences(10) runs first → then print() outputs the result
#   count_test_occurrences(10) → each argument is evaluated first, then the function body runs
#   c == 'test' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_test_occurrences(s) → stored in memory, not executed
#   2. s = ... → assigns value to variable
#   3. print(count_test_occurrences(...))
#      → calls count_test_occurrences(s) with s=s
#      → 3.1 count = 0
#      → 3.2 for c in s:
#      → 3.2     if c == 'test':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_test_occurrences(s) → stored in memory, not executed
#   5. def count_test_occurrences(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_test_occurrences(...))
#      → calls count_test_occurrences(s) with s=10
#      → 9.1 count = 0
#      → 9.2 for c in s:
#      → 9.2     if c == 'test':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_test_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     s = ... → stored in memory
#     print(count_test_occurrences(...)) → count_test_occurrences() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_test_occurrences(...)) → count_test_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 547:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_floating_numbers(lst, number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_floating_numbers(?, 1.5) runs first → then print() outputs the result
#   count_floating_numbers(lst, number) → each argument is evaluated first, then the function body runs
#   count_floating_numbers(?, 25) runs first → then print() outputs the result
#   count_floating_numbers(?, 1.5) → each argument is evaluated first, then the function body runs
#   count_floating_numbers(?, 25) → each argument is evaluated first, then the function body runs
#   c == number → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_floating_numbers(lst, number) → stored in memory, not executed
#   2. print(count_floating_numbers(...))
#      → calls count_floating_numbers(lst, number) with lst=[1.5, 2.0, 1.5, 3.5], number=1.5
#      → 2.1 count = 0
#      → 2.2 for c in lst:
#      → 2.2     if c == number:
#      → 2.2         count += 1
#      → 2.3 return count
#   3. def count_floating_numbers(lst, number) → stored in memory, not executed
#   4. def count_floating_numbers(lst, number) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_floating_numbers(...))
#      → calls count_floating_numbers(lst, number) with lst=[1, 3, 5, 7], number=25
#      → 9.1 count = 0
#      → 9.2 for c in lst:
#      → 9.2     if c == number:
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_floating_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(count_floating_numbers(...)) → count_floating_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_floating_numbers(...)) → count_floating_numbers() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 548:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_floating_numbers(lst, number) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_floating_numbers(lst, number) runs first → then print() outputs the result
#   count_floating_numbers(lst, number) → each argument is evaluated first, then the function body runs
#   count_floating_numbers(?, 25) runs first → then print() outputs the result
#   count_floating_numbers(?, 25) → each argument is evaluated first, then the function body runs
#   c == number → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_floating_numbers(lst, number) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. number = ... → assigns value to variable
#   4. print(count_floating_numbers(...))
#      → calls count_floating_numbers(lst, number) with lst=lst, number=number
#      → 4.1 count = 0
#      → 4.2 for c in lst:
#      → 4.2     if c == number:
#      → 4.2         count += 1
#      → 4.3 return count
#   5. def count_floating_numbers(lst, number) → stored in memory, not executed
#   6. def count_floating_numbers(lst, number) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. number = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_floating_numbers(...))
#      → calls count_floating_numbers(lst, number) with lst=[1, 3, 5, 7], number=25
#      → 11.1 count = 0
#      → 11.2 for c in lst:
#      → 11.2     if c == number:
#      → 11.2         count += 1
#      → 11.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_floating_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     number = ... → stored in memory
#     print(count_floating_numbers(...)) → count_floating_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_floating_numbers(...)) → count_floating_numbers() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 549:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_uppercase(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_uppercase(string) runs first → then print() outputs the result
#   count_uppercase(string) → each argument is evaluated first, then the function body runs
#   count_uppercase('world') runs first → then print() outputs the result
#   count_uppercase('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_uppercase(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_uppercase(...))
#      → calls count_uppercase(string) with string=string
#      → 3.1 return total_uppercase
#   4. def count_uppercase(string) → stored in memory, not executed
#   5. def count_uppercase(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_uppercase(...))
#      → calls count_uppercase(string) with string='world'
#      → 9.1 return total_uppercase
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_uppercase(...)) → count_uppercase() runs first → value passed to print()
#
#   Loop: takes one item from 'ABCDEFGHIJKLMNOPQRSTUVWXYZ' each round, runs body, repeats until done

"""

"""
Problem 550:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_digit(num, digit) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_digit(12345, 3) runs first → then print() outputs the result
#   count_digit(num, digit) → each argument is evaluated first, then the function body runs
#   count_digit(25, 25) runs first → then print() outputs the result
#   count_digit(12345, 3) → each argument is evaluated first, then the function body runs
#   count_digit(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_digit(num, digit) → stored in memory, not executed
#   2. print(count_digit(...))
#      → calls count_digit(num, digit) with num=12345, digit=3
#      → 2.1 return str(num).count(str(digit))
#   3. def count_digit(num, digit) → stored in memory, not executed
#   4. def count_digit(num, digit) → stored in memory, not executed
#   5. num = ... → assigns value to variable
#   6. digit = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_digit(...))
#      → calls count_digit(num, digit) with num=25, digit=25
#      → 9.1 return str(num).count(str(digit))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_digit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(count_digit(...)) → count_digit() runs first → value passed to print()
#     num = ... → stored in memory
#     digit = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_digit(...)) → count_digit() runs first → value passed to print()

"""

"""
Problem 551:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_letters(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_letters(string) runs first → then print() outputs the result
#   count_letters(string) → each argument is evaluated first, then the function body runs
#   count_letters('world') runs first → then print() outputs the result
#   count_letters('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_letters(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_letters(...))
#      → calls count_letters(string) with string=string
#      → 3.1 return letter_count
#   4. def count_letters(string) → stored in memory, not executed
#   5. def count_letters(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_letters(...))
#      → calls count_letters(string) with string='world'
#      → 9.1 return letter_count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_letters(...)) → count_letters() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_letters(...)) → count_letters() runs first → value passed to print()
#
#   Loop: takes one item from string.lower(...) each round, runs body, repeats until done

"""

"""
Problem 552:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_text(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_text(string) runs first → then print() outputs the result
#   count_text(string) → each argument is evaluated first, then the function body runs
#   count_text('world') runs first → then print() outputs the result
#   count_text('world') → each argument is evaluated first, then the function body runs
#   c == 'python' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_text(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_text(...))
#      → calls count_text(string) with string=string
#      → 3.1 count = 0
#      → 3.2 for c in string:
#      → 3.2     if c == 'python':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_text(string) → stored in memory, not executed
#   5. def count_text(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_text(...))
#      → calls count_text(string) with string='world'
#      → 9.1 count = 0
#      → 9.2 for c in string:
#      → 9.2     if c == 'python':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_text) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_text(...)) → count_text() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_text(...)) → count_text() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 553:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_item_in_dict_list(dict_list, item) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_item_in_dict_list(dict_list, ?) runs first → then print() outputs the result
#   count_item_in_dict_list(dict_list, item) → each argument is evaluated first, then the function body runs
#   count_item_in_dict_list(?, 25) runs first → then print() outputs the result
#   count_item_in_dict_list(dict_list, ?) → each argument is evaluated first, then the function body runs
#   count_item_in_dict_list(?, 25) → each argument is evaluated first, then the function body runs
#   c == item → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_item_in_dict_list(dict_list, item) → stored in memory, not executed
#   2. dict_list = ... → assigns value to variable
#   3. print(count_item_in_dict_list(...))
#      → calls count_item_in_dict_list(dict_list, item) with dict_list=dict_list, item=?
#      → 3.1 count = 0
#      → 3.2 for c in dict_list:
#      → 3.2     if c == item:
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_item_in_dict_list(dict_list, item) → stored in memory, not executed
#   5. def count_item_in_dict_list(dict_list, item) → stored in memory, not executed
#   6. dict_list = ... → assigns value to variable
#   7. item = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(count_item_in_dict_list(...))
#      → calls count_item_in_dict_list(dict_list, item) with dict_list=[4, 1, 8, 6, 3], item=25
#      → 10.1 count = 0
#      → 10.2 for c in dict_list:
#      → 10.2     if c == item:
#      → 10.2         count += 1
#      → 10.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_item_in_dict_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict_list = ... → stored in memory
#     print(count_item_in_dict_list(...)) → count_item_in_dict_list() runs first → value passed to print()
#     dict_list = ... → stored in memory
#     item = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_item_in_dict_list(...)) → count_item_in_dict_list() runs first → value passed to print()
#
#   Loop: takes one item from dict_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 554:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_letters(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_letters(lst) runs first → then print() outputs the result
#   count_letters(lst) → each argument is evaluated first, then the function body runs
#   count_letters(?) runs first → then print() outputs the result
#   count_letters(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_letters(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(count_letters(...))
#      → calls count_letters(lst) with lst=lst
#      → 3.1 return total_count
#   4. def count_letters(lst) → stored in memory, not executed
#   5. def count_letters(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_letters(...))
#      → calls count_letters(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return total_count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_letters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(count_letters(...)) → count_letters() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_letters(...)) → count_letters() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 555:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_asterisks(s) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_asterisks(s) runs first → then print() outputs the result
#   count_asterisks(s) → each argument is evaluated first, then the function body runs
#   count_asterisks(10) runs first → then print() outputs the result
#   count_asterisks(10) → each argument is evaluated first, then the function body runs
#   c == '*' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_asterisks(s) → stored in memory, not executed
#   2. s = ... → assigns value to variable
#   3. print(count_asterisks(...))
#      → calls count_asterisks(s) with s=s
#      → 3.1 count = 0
#      → 3.2 for c in s:
#      → 3.2     if c == '*':
#      → 3.2         count += 1
#      → 3.3 return count
#   4. def count_asterisks(s) → stored in memory, not executed
#   5. def count_asterisks(s) → stored in memory, not executed
#   6. s = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_asterisks(...))
#      → calls count_asterisks(s) with s=10
#      → 9.1 count = 0
#      → 9.2 for c in s:
#      → 9.2     if c == '*':
#      → 9.2         count += 1
#      → 9.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_asterisks) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     s = ... → stored in memory
#     print(count_asterisks(...)) → count_asterisks() runs first → value passed to print()
#     s = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_asterisks(...)) → count_asterisks() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 556:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_substring(s, substring) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_substring(s, substring) runs first → then print() outputs the result
#   count_substring(s, substring) → each argument is evaluated first, then the function body runs
#   count_substring(10, 'world') runs first → then print() outputs the result
#   count_substring(10, 'world') → each argument is evaluated first, then the function body runs
#   c == substring → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_substring(s, substring) → stored in memory, not executed
#   2. s = ... → assigns value to variable
#   3. substring = ... → assigns value to variable
#   4. print(count_substring(...))
#      → calls count_substring(s, substring) with s=s, substring=substring
#      → 4.1 count = 0
#      → 4.2 for c in s:
#      → 4.2     if c == substring:
#      → 4.2         count += 1
#      → 4.3 return count
#   5. def count_substring(s, substring) → stored in memory, not executed
#   6. def count_substring(s, substring) → stored in memory, not executed
#   7. s = ... → assigns value to variable
#   8. substring = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_substring(...))
#      → calls count_substring(s, substring) with s=10, substring='world'
#      → 11.1 count = 0
#      → 11.2 for c in s:
#      → 11.2     if c == substring:
#      → 11.2         count += 1
#      → 11.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_substring) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     s = ... → stored in memory
#     substring = ... → stored in memory
#     print(count_substring(...)) → count_substring() runs first → value passed to print()
#     s = ... → stored in memory
#     substring = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_substring(...)) → count_substring() runs first → value passed to print()
#
#   Loop: takes one item from s each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 557:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_lines_with_word(filename, word) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: len, open, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_lines_with_word(filename, word) runs first → then print() outputs the result
#   count_lines_with_word(filename, word) → each argument is evaluated first, then the function body runs
#   count_lines_with_word(25, 'coding') runs first → then print() outputs the result
#   count_lines_with_word(25, 'coding') → each argument is evaluated first, then the function body runs
#   line.lower(...).count(...) > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_lines_with_word(filename, word) → stored in memory, not executed
#   2. filename = ... → assigns value to variable
#   3. word = ... → assigns value to variable
#   4. print(count_lines_with_word(...))
#      → calls count_lines_with_word(filename, word) with filename=filename, word=word
#      → 4.1 return len([line for line in file if line.lower().count(word.lower()) > 0])
#   5. def count_lines_with_word(filename, word) → stored in memory, not executed
#   6. def count_lines_with_word(filename, word) → stored in memory, not executed
#   7. filename = ... → assigns value to variable
#   8. word = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_lines_with_word(...))
#      → calls count_lines_with_word(filename, word) with filename=25, word='coding'
#      → 11.1 return len([line for line in file if line.lower().count(word.lower()) > 0])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_lines_with_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     filename = ... → stored in memory
#     word = ... → stored in memory
#     print(count_lines_with_word(...)) → count_lines_with_word() runs first → value passed to print()
#     filename = ... → stored in memory
#     word = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_lines_with_word(...)) → count_lines_with_word() runs first → value passed to print()
#
#   Loop: takes one item from file each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 558:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_email_domain(email_list, domain) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_email_domain(email_list, domain) runs first → then print() outputs the result
#   count_email_domain(email_list, domain) → each argument is evaluated first, then the function body runs
#   count_email_domain(?, 25) runs first → then print() outputs the result
#   count_email_domain(?, 25) → each argument is evaluated first, then the function body runs
#   email_domain == domain → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_email_domain(email_list, domain) → stored in memory, not executed
#   2. email_list = ... → assigns value to variable
#   3. domain = ... → assigns value to variable
#   4. print(count_email_domain(...))
#      → calls count_email_domain(email_list, domain) with email_list=email_list, domain=domain
#      → 4.1 return len([email for email in email_list if email_domain == domain])
#   5. def count_email_domain(email_list, domain) → stored in memory, not executed
#   6. def count_email_domain(email_list, domain) → stored in memory, not executed
#   7. email_list = ... → assigns value to variable
#   8. domain = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_email_domain(...))
#      → calls count_email_domain(email_list, domain) with email_list=[4, 1, 8, 6, 3], domain=25
#      → 11.1 return len([email for email in email_list if email_domain == domain])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_email_domain) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     email_list = ... → stored in memory
#     domain = ... → stored in memory
#     print(count_email_domain(...)) → count_email_domain() runs first → value passed to print()
#     email_list = ... → stored in memory
#     domain = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_email_domain(...)) → count_email_domain() runs first → value passed to print()
#
#   Loop: takes one item from email_list each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 559:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_word(sentences) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_word(sentences) runs first → then print() outputs the result
#   count_word(sentences) → each argument is evaluated first, then the function body runs
#   count_word(?) runs first → then print() outputs the result
#   count_word(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_word(sentences) → stored in memory, not executed
#   2. sentences = ... → assigns value to variable
#   3. print(count_word(...))
#      → calls count_word(sentences) with sentences=sentences
#      → 3.1 return count
#   4. def count_word(sentences) → stored in memory, not executed
#   5. def count_word(sentences) → stored in memory, not executed
#   6. sentences = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_word(...))
#      → calls count_word(sentences) with sentences=[4, 1, 8, 6, 3]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentences = ... → stored in memory
#     print(count_word(...)) → count_word() runs first → value passed to print()
#     sentences = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_word(...)) → count_word() runs first → value passed to print()
#
#   Loop: takes one item from sentences each round, runs body, repeats until done

"""

"""
Problem 560:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_exclamations(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_exclamations(lst) runs first → then print() outputs the result
#   count_exclamations(lst) → each argument is evaluated first, then the function body runs
#   count_exclamations(?) runs first → then print() outputs the result
#   count_exclamations(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_exclamations(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(count_exclamations(...))
#      → calls count_exclamations(lst) with lst=lst
#      → 3.1 return count
#   4. def count_exclamations(lst) → stored in memory, not executed
#   5. def count_exclamations(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_exclamations(...))
#      → calls count_exclamations(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_exclamations) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(count_exclamations(...)) → count_exclamations() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_exclamations(...)) → count_exclamations() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 562:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_word_occurrences(lst, word) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_word_occurrences(words, word_to_count) runs first → then print() outputs the result
#   count_word_occurrences(lst, word) → each argument is evaluated first, then the function body runs
#   count_word_occurrences(?, 'coding') runs first → then print() outputs the result
#   count_word_occurrences(words, word_to_count) → each argument is evaluated first, then the function body runs
#   count_word_occurrences(?, 'coding') → each argument is evaluated first, then the function body runs
#   c == word → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_word_occurrences(lst, word) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. word_to_count = ... → assigns value to variable
#   4. print(count_word_occurrences(...))
#      → calls count_word_occurrences(lst, word) with lst=words, word=word_to_count
#      → 4.1 count = 0
#      → 4.2 for c in lst:
#      → 4.2     if c == word:
#      → 4.2         count += 1
#      → 4.3 return count
#   5. def count_word_occurrences(lst, word) → stored in memory, not executed
#   6. def count_word_occurrences(lst, word) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. word = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(count_word_occurrences(...))
#      → calls count_word_occurrences(lst, word) with lst=[1, 3, 5, 7], word='coding'
#      → 11.1 count = 0
#      → 11.2 for c in lst:
#      → 11.2     if c == word:
#      → 11.2         count += 1
#      → 11.3 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_word_occurrences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     word_to_count = ... → stored in memory
#     print(count_word_occurrences(...)) → count_word_occurrences() runs first → value passed to print()
#     lst = ... → stored in memory
#     word = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_word_occurrences(...)) → count_word_occurrences() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 563:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_x_in_mixed_list(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_x_in_mixed_list(mixed_list) runs first → then print() outputs the result
#   count_x_in_mixed_list(lst) → each argument is evaluated first, then the function body runs
#   count_x_in_mixed_list(?) runs first → then print() outputs the result
#   count_x_in_mixed_list(mixed_list) → each argument is evaluated first, then the function body runs
#   count_x_in_mixed_list(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_x_in_mixed_list(lst) → stored in memory, not executed
#   2. mixed_list = ... → assigns value to variable
#   3. print(count_x_in_mixed_list(...))
#      → calls count_x_in_mixed_list(lst) with lst=mixed_list
#      → 3.1 return count
#   4. def count_x_in_mixed_list(lst) → stored in memory, not executed
#   5. def count_x_in_mixed_list(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_x_in_mixed_list(...))
#      → calls count_x_in_mixed_list(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_x_in_mixed_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     mixed_list = ... → stored in memory
#     print(count_x_in_mixed_list(...)) → count_x_in_mixed_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_x_in_mixed_list(...)) → count_x_in_mixed_list() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 565:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_lst(lst) runs first → then print() outputs the result
#   square_lst(lst) → each argument is evaluated first, then the function body runs
#   square_lst(?) runs first → then print() outputs the result
#   num ** 2 → Python looks up num, then 2, then applies **
#   square_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_lst(lst) → stored in memory, not executed
#   2. def square(num) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(square_lst(...))
#      → calls square_lst(lst) with lst=lst
#      → 4.1 return list(map(square, lst))
#   5. def square_lst(lst) → stored in memory, not executed
#   6. def square_lst(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(square_lst(...))
#      → calls square_lst(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(square, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square, square_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(square_lst(...)) → square_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_lst(...)) → square_lst() runs first → value passed to print()

"""

"""
Problem 566:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_odd_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % !=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_odd_numbers(lst) runs first → then print() outputs the result
#   filter_odd_numbers(lst) → each argument is evaluated first, then the function body runs
#   filter_odd_numbers(?) runs first → then print() outputs the result
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   filter_odd_numbers(?) → each argument is evaluated first, then the function body runs
#   x % 2 → Python looks up x, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_odd_numbers(lst) → stored in memory, not executed
#   2. def is_odd(x) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(filter_odd_numbers(...))
#      → calls filter_odd_numbers(lst) with lst=lst
#      → 4.1 return list(filter(is_odd, lst))
#   5. def filter_odd_numbers(lst) → stored in memory, not executed
#   6. def filter_odd_numbers(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(filter_odd_numbers(...))
#      → calls filter_odd_numbers(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(filter(is_odd, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_odd_numbers, is_odd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(filter_odd_numbers(...)) → filter_odd_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_odd_numbers(...)) → filter_odd_numbers() runs first → value passed to print()

"""

"""
Problem 567:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def even_num(num) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filters_even_numbers(?) runs first → then print() outputs the result
#   even_num(num) → each argument is evaluated first, then the function body runs
#   even_num(25) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   filters_even_numbers(?) → each argument is evaluated first, then the function body runs
#   even_num(25) → each argument is evaluated first, then the function body runs
#   num % 2 → Python looks up num, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def even_num(num) → stored in memory, not executed
#   2. def filters_even_numbers(lst) → stored in memory, not executed
#   3. print(filters_even_numbers(...))
#      → calls filters_even_numbers(lst) with lst=[1, 2, 3, 4, 5, 6]
#      → 3.1 return list(filter(even_num, lst))
#   4. def even_num(num) → stored in memory, not executed
#   5. def even_num(num) → stored in memory, not executed
#   6. num = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(even_num(...))
#      → calls even_num(num) with num=25
#      → 9.1 return num % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filters_even_numbers, even_num) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(filters_even_numbers(...)) → filters_even_numbers() runs first → value passed to print()
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(even_num(...)) → even_num() runs first → value passed to print()

"""

"""
Problem 568:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_to_uppercase(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_to_uppercase(lst) runs first → then print() outputs the result
#   convert_to_uppercase(lst) → each argument is evaluated first, then the function body runs
#   convert_to_uppercase(?) runs first → then print() outputs the result
#   convert_to_uppercase(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def convert_to_uppercase(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(convert_to_uppercase(...))
#      → calls convert_to_uppercase(lst) with lst=lst
#      → 3.1 return list(map(str.upper, lst))
#   4. def convert_to_uppercase(lst) → stored in memory, not executed
#   5. def convert_to_uppercase(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(convert_to_uppercase(...))
#      → calls convert_to_uppercase(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return list(map(str.upper, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_to_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(convert_to_uppercase(...)) → convert_to_uppercase() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_to_uppercase(...)) → convert_to_uppercase() runs first → value passed to print()

"""

"""
Problem 569:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def empty_strings(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: !=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   empty_strings(lst) runs first → then print() outputs the result
#   empty_strings(lst) → each argument is evaluated first, then the function body runs
#   empty_strings(?) runs first → then print() outputs the result
#   string.strip(...) != '' → evaluated as True or False (determines which branch runs)
#   empty_strings(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def empty_strings(lst) → stored in memory, not executed
#   2. def call_filter(string) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(empty_strings(...))
#      → calls empty_strings(lst) with lst=lst
#      → 4.1 return list(filter(call_filter, lst))
#   5. def empty_strings(lst) → stored in memory, not executed
#   6. def empty_strings(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(empty_strings(...))
#      → calls empty_strings(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(filter(call_filter, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (call_filter, empty_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(empty_strings(...)) → empty_strings() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(empty_strings(...)) → empty_strings() runs first → value passed to print()

"""

"""
Problem 570:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_lst(lst) runs first → then print() outputs the result
#   multiply_lst(lst) → each argument is evaluated first, then the function body runs
#   multiply_lst(?) runs first → then print() outputs the result
#   multiply_lst(?) → each argument is evaluated first, then the function body runs
#   x * 5 → Python looks up x, then 5, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(multiply_lst(...))
#      → calls multiply_lst(lst) with lst=lst
#      → 3.1 return pow(x, 5)
#   4. def multiply_lst(lst) → stored in memory, not executed
#   5. def multiply_lst(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(multiply_lst(...))
#      → calls multiply_lst(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(x, 5)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(multiply_lst(...)) → multiply_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_lst(...)) → multiply_lst() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 571:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_of_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_of_string(lst) runs first → then print() outputs the result
#   lst_of_string(lst) → each argument is evaluated first, then the function body runs
#   lst_of_string(?) runs first → then print() outputs the result
#   lst_of_string(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_of_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_of_string(...))
#      → calls lst_of_string(lst) with lst=lst
#      → 3.1 return sum((1 for _ in word))
#   4. def lst_of_string(lst) → stored in memory, not executed
#   5. def lst_of_string(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_of_string(...))
#      → calls lst_of_string(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return sum((1 for _ in word))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_of_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_of_string(...)) → lst_of_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_of_string(...)) → lst_of_string() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done

"""

"""
Problem 572:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def palindromes(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   palindromes(?) runs first → then print() outputs the result
#   palindromes(lst) → each argument is evaluated first, then the function body runs
#   palindromes(?) → each argument is evaluated first, then the function body runs
#   word == ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def palindromes(lst) → stored in memory, not executed
#   2. print(palindromes(...))
#      → calls palindromes(lst) with lst=['radar', 'hello', 'level', 'world']
#      → 2.1 return word == word[::-1]
#   3. def palindromes(lst) → stored in memory, not executed
#   4. def palindromes(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(palindromes(...))
#      → calls palindromes(lst) with lst=[1, 3, 5, 7]
#      → 8.1 return word == word[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (palindromes) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(palindromes(...)) → palindromes() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(palindromes(...)) → palindromes() runs first → value passed to print()

"""

"""
Problem 573:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_of_list(lst) runs first → then print() outputs the result
#   sum_of_lst(lst) → each argument is evaluated first, then the function body runs
#   sum_of_lst(?) runs first → then print() outputs the result
#   sum_of_list(lst) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#   sum_of_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def sum_of_lst(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(sum_of_list(...))
#      → calls sum_of_list(lst) with lst=lst
#      → 4.1 return reduce(add, lst)
#   5. def add(x, y) → stored in memory, not executed
#   6. def sum_of_list(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. print(sum_of_list(...))
#      → calls sum_of_list(lst) with lst=lst
#      → 8.1 return reduce(add, lst)
#   9. def sum_of_lst(lst) → stored in memory, not executed
#   10. def sum_of_lst(lst) → stored in memory, not executed
#   11. lst = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(sum_of_lst(...))
#      → calls sum_of_lst(lst) with lst=[1, 3, 5, 7]
#      → 14.1 import operator
#      → 14.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_of_list, sum_of_lst, add) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(sum_of_list(...)) → sum_of_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_lst(...)) → sum_of_lst() runs first → value passed to print()

"""

"""
Problem 574:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_of_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: pow, range, reduce, sum_of_list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   product_of_list(lst) runs first → then print() outputs the result
#   sum_of_lst(lst) → each argument is evaluated first, then the function body runs
#   sum_of_lst(?) runs first → then print() outputs the result
#   x * y → Python looks up x, then y, then applies *
#   product_of_list(lst) → each argument is evaluated first, then the function body runs
#   sum_of_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def sum_of_lst(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(sum_of_list(...))
#   5. def multiply(x, y) → stored in memory, not executed
#   6. def product_of_list(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. print(product_of_list(...))
#      → calls product_of_list(lst) with lst=lst
#      → 8.1 return reduce(multiply, lst)
#   9. def sum_of_lst(lst) → stored in memory, not executed
#   10. def sum_of_lst(lst) → stored in memory, not executed
#   11. lst = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(sum_of_lst(...))
#      → calls sum_of_lst(lst) with lst=[1, 3, 5, 7]
#      → 14.1 return pow(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (product_of_list, multiply, sum_of_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sum_of_list(...))
#     lst = ... → stored in memory
#     print(product_of_list(...)) → product_of_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_of_lst(...)) → sum_of_lst() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 575:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_string(lst) → each argument is evaluated first, then the function body runs
#   lst_strings(lst) runs first → then print() outputs the result
#   capitalize_string(?) runs first → then print() outputs the result
#   lst_strings(lst) → each argument is evaluated first, then the function body runs
#   capitalize_string(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. capitalize_string(lst) → calls capitalize_string(lst) with lst=lst
#      → 3.1 return list(map(str.capitalize, lst))
#   4. def lst_strings(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(lst_strings(...))
#      → calls lst_strings(lst) with lst=lst
#      → 6.1 def capitalize_lst(word):
#      → 6.1     word = word.capitalize()
#      → 6.1     return word
#      → 6.2 return list(map(capitalize_lst, lst))
#   7. def capitalize_string(lst) → stored in memory, not executed
#   8. def capitalize_string(lst) → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(capitalize_string(...))
#      → calls capitalize_string(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return list(map(str.capitalize, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_string, lst_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     capitalize_string(lst) → jumps into capitalize_string(), runs body, returns
#     lst = ... → stored in memory
#     print(lst_strings(...)) → lst_strings() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_string(...)) → capitalize_string() runs first → value passed to print()

"""

"""
Problem 576:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def min_number(x, y) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: <
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number_lst(lst) runs first → then print() outputs the result
#   min_number(x, y) → each argument is evaluated first, then the function body runs
#   min_number(3, 14) runs first → then print() outputs the result
#   number_lst(lst) → each argument is evaluated first, then the function body runs
#   min_number(3, 14) → each argument is evaluated first, then the function body runs
#   x < y → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def min_number(x, y) → stored in memory, not executed
#   3. def number_lst(lst) → stored in memory, not executed
#   4. lst = ... → assigns value to variable
#   5. print(number_lst(...))
#      → calls number_lst(lst) with lst=lst
#      → 5.1 return reduce(min_number, lst)
#   6. def min_number(x, y) → stored in memory, not executed
#   7. def min_number(x, y) → stored in memory, not executed
#   8. x = ... → assigns value to variable
#   9. y = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(min_number(...))
#      → calls min_number(x, y) with x=3, y=14
#      → 12.1 return x if x < y else y
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (min_number, number_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(number_lst(...)) → number_lst() runs first → value passed to print()
#     x = ... → stored in memory
#     y = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(min_number(...)) → min_number() runs first → value passed to print()

"""

"""
Problem 577:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_max(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   find_max(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   number_lst(lst) runs first → then print() outputs the result
#   find_max(lst) → each argument is evaluated first, then the function body runs
#   find_max(?) runs first → then print() outputs the result
#   x > y → evaluated as True or False (determines which branch runs)
#   number_lst(lst) → each argument is evaluated first, then the function body runs
#   find_max(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def find_max(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(find_max(...))
#      → calls find_max(lst) with lst=lst
#      → 4.1 return x
#   5. def main(lst) → stored in memory, not executed
#   6. def max_num(x, y) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. main(lst) → calls main(lst) with lst=lst
#      → 8.1 return reduce(max_num, lst)
#   9. from functools import reduce
#   10. def min_number(x, y) → stored in memory, not executed
#   11. def number_lst(lst) → stored in memory, not executed
#   12. lst = ... → assigns value to variable
#   13. print(number_lst(...))
#      → calls number_lst(lst) with lst=lst
#      → 13.1 return reduce(min_number, lst)
#   14. def find_max(lst) → stored in memory, not executed
#   15. def find_max(lst) → stored in memory, not executed
#   16. lst = ... → assigns value to variable
#   17. result = ... → assigns value to variable
#   18. print(result)
#   19. print(find_max(...))
#      → calls find_max(lst) with lst=[1, 3, 5, 7]
#      → 19.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, number_lst, max_num, find_max, min_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(find_max(...)) → find_max() runs first → value passed to print()
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     lst = ... → stored in memory
#     print(number_lst(...)) → number_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_max(...)) → find_max() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 578:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: filter, len, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_words(lst) runs first → then print() outputs the result
#   list_words(lst) → each argument is evaluated first, then the function body runs
#   list_words(?) runs first → then print() outputs the result
#   list_words(?) → each argument is evaluated first, then the function body runs
#   len(...) > 3 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_words(...))
#      → calls list_words(lst) with lst=lst
#      → 3.1 return word
#   4. def list_words(lst) → stored in memory, not executed
#   5. def list_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(list_words(...))
#      → calls list_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_words(...)) → list_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_words(...)) → list_words() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 579:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_strings(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % +
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_strings(lst) runs first → then print() outputs the result
#   lst_strings(lst) → each argument is evaluated first, then the function body runs
#   lst_strings(?) runs first → then print() outputs the result
#   '%s%s' % ? → Python looks up '%s%s', then ?, then applies %
#   lst_strings(?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def lst_strings(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_strings(...))
#      → calls lst_strings(lst) with lst=lst
#      → 4.1 return '%s%s' % (x, y)
#   5. def lst_strings(lst) → stored in memory, not executed
#   6. def lst_strings(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_strings(...))
#      → calls lst_strings(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return '%s%s' % (x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_strings(...)) → lst_strings() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_strings(...)) → lst_strings() runs first → value passed to print()

"""

"""
Problem 580:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def num_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: > <=
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num_lst(?) runs first → then print() outputs the result
#   num_lst(lst) → each argument is evaluated first, then the function body runs
#   num_lst(?) → each argument is evaluated first, then the function body runs
#   num > 0 → evaluated as True or False (determines which branch runs)
#   num <= 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def num_lst(lst) → stored in memory, not executed
#   2. print(num_lst(...))
#      → calls num_lst(lst) with lst=[?, 0, 1, ?, 2]
#      → 2.1 return not num <= 0
#   3. def num_lst(lst) → stored in memory, not executed
#   4. def num_lst(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(num_lst(...))
#      → calls num_lst(lst) with lst=[1, 3, 5, 7]
#      → 8.1 return not num <= 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (num_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(num_lst(...)) → num_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(num_lst(...)) → num_lst() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 581:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def num_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: list, map, pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num_lst(lst) runs first → then print() outputs the result
#   num_lst(lst) → each argument is evaluated first, then the function body runs
#   num_lst(?) runs first → then print() outputs the result
#   num_lst(?) → each argument is evaluated first, then the function body runs
#   num ** 3 → Python looks up num, then 3, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def num_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(num_lst(...))
#      → calls num_lst(lst) with lst=lst
#      → 3.1 import math
#      → 3.2 return math.pow(num, 3)
#   4. def num_lst(lst) → stored in memory, not executed
#   5. def num_lst(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(num_lst(...))
#      → calls num_lst(lst) with lst=[1, 3, 5, 7]
#      → 9.1 import math
#      → 9.2 return math.pow(num, 3)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (num_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(num_lst(...)) → num_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(num_lst(...)) → num_lst() runs first → value passed to print()

"""

"""
Problem 582:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + **
# Built-in functions: map, pow, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_numbers(lst) runs first → then print() outputs the result
#   lst_numbers(lst) → each argument is evaluated first, then the function body runs
#   lst_numbers(?) runs first → then print() outputs the result
#   lst_numbers(?) → each argument is evaluated first, then the function body runs
#   num ** 2 → Python looks up num, then 2, then applies **
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def lst_numbers(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=lst
#      → 4.1 import math
#      → 4.2 return math.pow(num, 2)
#   5. def lst_numbers(lst) → stored in memory, not executed
#   6. def lst_numbers(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=[1, 3, 5, 7]
#      → 10.1 import math
#      → 10.2 return math.pow(num, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()

"""

"""
Problem 583:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_2(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_2(lst1, lst2) runs first → then print() outputs the result
#   lst_2(lst1, lst2) → each argument is evaluated first, then the function body runs
#   lst_2(?, ?) runs first → then print() outputs the result
#   lst_2(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_2(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(lst_2(...))
#      → calls lst_2(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return f'{first_last[0]} {first_last[1]}'
#   5. def lst_2(lst1, lst2) → stored in memory, not executed
#   6. def lst_2(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(lst_2(...))
#      → calls lst_2(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return f'{first_last[0]} {first_last[1]}'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_2) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(lst_2(...)) → lst_2() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_2(...)) → lst_2() runs first → value passed to print()

"""

"""
Problem 584:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def non_alphabetical_string(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   non_alphabetical_string(?) runs first → then print() outputs the result
#   non_alphabetical_string(string) → each argument is evaluated first, then the function body runs
#   non_alphabetical_string('world') runs first → then print() outputs the result
#   non_alphabetical_string(?) → each argument is evaluated first, then the function body runs
#   non_alphabetical_string('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def non_alphabetical_string(string) → stored in memory, not executed
#   2. print(non_alphabetical_string(...))
#      → calls non_alphabetical_string(string) with string=['hello', 'world', 'abc123', 'python']
#      → 2.1 return word.isalpha()
#   3. def non_alphabetical_string(string) → stored in memory, not executed
#   4. def non_alphabetical_string(string) → stored in memory, not executed
#   5. string = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(non_alphabetical_string(...))
#      → calls non_alphabetical_string(string) with string='world'
#      → 8.1 return word.isalpha()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (non_alphabetical_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(non_alphabetical_string(...)) → non_alphabetical_string() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(non_alphabetical_string(...)) → non_alphabetical_string() runs first → value passed to print()

"""

"""
Problem 585:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: in
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#   word in counts → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def lst_words(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 4.1 return counts
#   5. def lst_words(lst) → stored in memory, not executed
#   6. def lst_words(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return counts
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 586:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def unique_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   unique_words(?) runs first → then print() outputs the result
#   unique_words(lst) → each argument is evaluated first, then the function body runs
#   unique_words(?) → each argument is evaluated first, then the function body runs
#   word not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def unique_words(lst) → stored in memory, not executed
#   2. print(unique_words(...))
#      → calls unique_words(lst) with lst=['a', 'b', 'a', 'c', 'b']
#      → 2.1 return list(map(lambda word: word, lst))
#   3. def unique_words(lst) → stored in memory, not executed
#   4. def unique_words(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(unique_words(...))
#      → calls unique_words(lst) with lst=[1, 3, 5, 7]
#      → 8.1 return list(map(lambda word: word, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (unique_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(unique_words(...)) → unique_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(unique_words(...)) → unique_words() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 587:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def word_lst(lst, n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
# Built-in functions: filter, len, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   word_lst(?, 3) runs first → then print() outputs the result
#   word_lst(lst, n) → each argument is evaluated first, then the function body runs
#   word_lst(?, 15) runs first → then print() outputs the result
#   word_lst(?, 3) → each argument is evaluated first, then the function body runs
#   len(...) > n → evaluated as True or False (determines which branch runs)
#   word_lst(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def word_lst(lst, n) → stored in memory, not executed
#   2. print(word_lst(...))
#      → calls word_lst(lst, n) with lst=['a', 'ab', 'abc', 'abcd'], n=3
#      → 2.1 return len(word) > n
#   3. def word_lst(lst, n) → stored in memory, not executed
#   4. def word_lst(lst, n) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(word_lst(...))
#      → calls word_lst(lst, n) with lst=[1, 3, 5, 7], n=15
#      → 9.1 return len(word) > n
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (word_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(word_lst(...)) → word_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(word_lst(...)) → word_lst() runs first → value passed to print()

"""

"""
Problem 588:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def apply_lsts(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: % * !=
# Built-in functions: filter, list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   apply_lsts(lst) runs first → then print() outputs the result
#   apply_lsts(lst) → each argument is evaluated first, then the function body runs
#   apply_lsts(?) runs first → then print() outputs the result
#   apply_lsts(?) → each argument is evaluated first, then the function body runs
#   num * num → Python looks up num, then num, then applies *
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   num % 2 → Python looks up num, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def apply_lsts(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(apply_lsts(...))
#      → calls apply_lsts(lst) with lst=lst
#      → 3.1 return pow(num, num)
#   4. def apply_lsts(lst) → stored in memory, not executed
#   5. def apply_lsts(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(apply_lsts(...))
#      → calls apply_lsts(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(num, num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (apply_lsts) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(apply_lsts(...)) → apply_lsts() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(apply_lsts(...)) → apply_lsts() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 589:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_nested_list(nested_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_nested_list(?) runs first → then print() outputs the result
#   sum_nested_list(nested_list) → each argument is evaluated first, then the function body runs
#   sum_nested_list(?) → each argument is evaluated first, then the function body runs
#   acc + item → Python looks up acc, then item, then applies +
#   total + number → Python looks up total, then number, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def sum_nested_list(nested_list) → stored in memory, not executed
#   3. print(sum_nested_list(...))
#      → calls sum_nested_list(nested_list) with nested_list=[[1, 2], [3, 4], [5]]
#      → 3.1 return reduce(lambda total, number: total + number, flattened, 0)
#   4. def sum_nested_list(nested_list) → stored in memory, not executed
#   5. def sum_nested_list(nested_list) → stored in memory, not executed
#   6. nested_list = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sum_nested_list(...))
#      → calls sum_nested_list(nested_list) with nested_list=[4, 1, 8, 6, 3]
#      → 9.1 return reduce(lambda total, number: total + number, flattened, 0)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_nested_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(sum_nested_list(...)) → sum_nested_list() runs first → value passed to print()
#     nested_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_nested_list(...)) → sum_nested_list() runs first → value passed to print()

"""

"""
Problem 590:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def fibonacci_n_numbers(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + -
# Built-in functions: range, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   fibonacci_n_numbers(10) runs first → then print() outputs the result
#   fibonacci_n_numbers(n) → each argument is evaluated first, then the function body runs
#   fibonacci_n_numbers(15) runs first → then print() outputs the result
#   fibonacci_n_numbers(10) → each argument is evaluated first, then the function body runs
#   acc + [?] → Python looks up acc, then [?], then applies +
#   fibonacci_n_numbers(15) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   n - 2 → Python looks up n, then 2, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def fibonacci_n_numbers(n) → stored in memory, not executed
#   3. print(fibonacci_n_numbers(...))
#      → calls fibonacci_n_numbers(n) with n=10
#      → 3.1 return acc + [acc[-1] + acc[-2]]
#   4. def fibonacci_n_numbers(n) → stored in memory, not executed
#   5. def fibonacci_n_numbers(n) → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(fibonacci_n_numbers(...))
#      → calls fibonacci_n_numbers(n) with n=15
#      → 9.1 return acc + [acc[-1] + acc[-2]]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (fibonacci_n_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(fibonacci_n_numbers(...)) → fibonacci_n_numbers() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(fibonacci_n_numbers(...)) → fibonacci_n_numbers() runs first → value passed to print()

"""

"""
Problem 591:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: len, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_string(lst) runs first → then print() outputs the result
#   lst_string(lst) → each argument is evaluated first, then the function body runs
#   lst_string(?) runs first → then print() outputs the result
#   lst_string(?) → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_string(...))
#      → calls lst_string(lst) with lst=lst
#      → 3.1 return len([char for char in word if char in vowels])
#   4. def lst_string(lst) → stored in memory, not executed
#   5. def lst_string(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_string(...))
#      → calls lst_string(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return len([char for char in word if char in vowels])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_string(...)) → lst_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_string(...)) → lst_string() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 592:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sum_even_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % + ==
# Built-in functions: filter, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_even_numbers(lst) runs first → then print() outputs the result
#   sum_even_numbers(lst) → each argument is evaluated first, then the function body runs
#   sum_even_numbers(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   sum_even_numbers(?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#   num % 2 → Python looks up num, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def sum_even_numbers(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(sum_even_numbers(...))
#      → calls sum_even_numbers(lst) with lst=lst
#      → 4.1 return num % 2 == 0
#   5. def sum_even_numbers(lst) → stored in memory, not executed
#   6. def sum_even_numbers(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sum_even_numbers(...))
#      → calls sum_even_numbers(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return num % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_even_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sum_even_numbers(...)) → sum_even_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sum_even_numbers(...)) → sum_even_numbers() runs first → value passed to print()

"""

"""
Problem 593:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: >
# Built-in functions: len, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_string(lst) runs first → then print() outputs the result
#   lst_string(lst) → each argument is evaluated first, then the function body runs
#   lst_string(?) runs first → then print() outputs the result
#   lst_string(?) → each argument is evaluated first, then the function body runs
#   len(...) > len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def lst_string(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_string(...))
#      → calls lst_string(lst) with lst=lst
#      → 4.1 return x
#   5. def lst_string(lst) → stored in memory, not executed
#   6. def lst_string(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_string(...))
#      → calls lst_string(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_string(...)) → lst_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_string(...)) → lst_string() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 594:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_num(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_num(lst) runs first → then print() outputs the result
#   lst_num(lst) → each argument is evaluated first, then the function body runs
#   lst_num(?) runs first → then print() outputs the result
#   lst_num(?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def lst_num(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_num(...))
#      → calls lst_num(lst) with lst=lst
#      → 4.1 import operator
#      → 4.2 return operator.add(x, y)
#   5. def lst_num(lst) → stored in memory, not executed
#   6. def lst_num(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_num(...))
#      → calls lst_num(lst) with lst=[1, 3, 5, 7]
#      → 10.1 import operator
#      → 10.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_num) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_num(...)) → lst_num() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_num(...)) → lst_num() runs first → value passed to print()

"""

"""
Problem 595:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def flatten(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   flatten(nested_list) → each argument is evaluated first, then the function body runs
#   flatten(lst) → each argument is evaluated first, then the function body runs
#   flatten(?) runs first → then print() outputs the result
#   flatten(?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def flatten(lst) → stored in memory, not executed
#   3. nested_list = ... → assigns value to variable
#   4. flattened_list = ... → assigns value to variable
#   5. print(flattened_list)
#   6. def flatten(lst) → stored in memory, not executed
#   7. def flatten(lst) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(flatten(...))
#      → calls flatten(lst) with lst=[1, 3, 5, 7]
#      → 11.1 import operator
#      → 11.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (flatten) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     nested_list = ... → stored in memory
#     flattened_list = ... → stored in memory
#     print(flattened_list)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(flatten(...)) → flatten() runs first → value passed to print()

"""

"""
Problem 596:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: in
# Built-in functions: reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#   word in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def lst_words(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 4.1 return result
#   5. def lst_words(lst) → stored in memory, not executed
#   6. def lst_words(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 597:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst, target_word) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: !=
# Built-in functions: filter, list, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst, target_word) → each argument is evaluated first, then the function body runs
#   lst_words(?, ?) runs first → then print() outputs the result
#   sorted(...) != sorted(...) → evaluated as True or False (determines which branch runs)
#   lst_words(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst, target_word) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. target_word = ... → assigns value to variable
#   4. filtered_words = ... → assigns value to variable
#   5. print(filtered_words)
#   6. def lst_words(lst, target_word) → stored in memory, not executed
#   7. def lst_words(lst, target_word) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. target_word = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(lst_words(...))
#      → calls lst_words(lst, target_word) with lst=[1, 3, 5, 7], target_word=[4, 1, 8, 6, 3]
#      → 12.1 return sorted(word) != sorted(target_word)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     target_word = ... → stored in memory
#     filtered_words = ... → stored in memory
#     print(filtered_words)
#     lst = ... → stored in memory
#     target_word = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()

"""

"""
Problem 598:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def compose(func1, func2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
# Built-in functions: composed_function, func1, func2()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   compose(add_one, multiply_by_two) → each argument is evaluated first, then the function body runs
#   compose(func1, func2) → each argument is evaluated first, then the function body runs
#   compose(25, 25) runs first → then print() outputs the result
#   x + 1 → Python looks up x, then 1, then applies +
#   x * 2 → Python looks up x, then 2, then applies *
#   compose(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def compose(func1, func2) → stored in memory, not executed
#   2. def add_one(x) → stored in memory, not executed
#   3. def multiply_by_two(x) → stored in memory, not executed
#   4. composed_function = ... → assigns value to variable
#   5. result = ... → assigns value to variable
#   6. print(result)
#   7. def compose(func1, func2) → stored in memory, not executed
#   8. def compose(func1, func2) → stored in memory, not executed
#   9. func1 = ... → assigns value to variable
#   10. func2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(compose(...))
#      → calls compose(func1, func2) with func1=25, func2=25
#      → 13.1 return func2(func1(x))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (compose, add_one, multiply_by_two) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     composed_function = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     func1 = ... → stored in memory
#     func2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(compose(...)) → compose() runs first → value passed to print()

"""

"""
Problem 599:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 600:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_prime(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + ** <= ==
# Built-in functions: filter, int, list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_primes(numbers) → each argument is evaluated first, then the function body runs
#   is_prime(n) → each argument is evaluated first, then the function body runs
#   is_prime(15) runs first → then print() outputs the result
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   is_prime(15) → each argument is evaluated first, then the function body runs
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n % i → Python looks up n, then i, then applies %
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_prime(n) → stored in memory, not executed
#   2. def filter_primes(lst) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. prime_numbers = ... → assigns value to variable
#   5. print(prime_numbers)
#   6. def is_prime(n) → stored in memory, not executed
#   7. def is_prime(n) → stored in memory, not executed
#   8. n = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(is_prime(...))
#      → calls is_prime(n) with n=15
#      → 11.1 return False
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_primes, is_prime) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     prime_numbers = ... → stored in memory
#     print(prime_numbers)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_prime(...)) → is_prime() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 601:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def map_with_index(lst, func) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: enumerate, func, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   map_with_index(numbers, example_func) → each argument is evaluated first, then the function body runs
#   map_with_index(lst, func) → each argument is evaluated first, then the function body runs
#   map_with_index(?, 25) runs first → then print() outputs the result
#   index * value → Python looks up index, then value, then applies *
#   map_with_index(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def map_with_index(lst, func) → stored in memory, not executed
#   2. def example_func(index, value) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def map_with_index(lst, func) → stored in memory, not executed
#   7. def map_with_index(lst, func) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. func = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(map_with_index(...))
#      → calls map_with_index(lst, func) with lst=[1, 3, 5, 7], func=25
#      → 12.1 return func(index, item)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (example_func, map_with_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     lst = ... → stored in memory
#     func = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(map_with_index(...)) → map_with_index() runs first → value passed to print()

"""

"""
Problem 602:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_in_list(x, list2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in
# Built-in functions: any, filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   intersection(list1, list2) → each argument is evaluated first, then the function body runs
#   is_in_list(x, list2) → each argument is evaluated first, then the function body runs
#   is_in_list(3, ?) runs first → then print() outputs the result
#   x in list2 → evaluated as True or False (determines which branch runs)
#   is_in_list(3, ?) → each argument is evaluated first, then the function body runs
#   element == x → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_in_list(x, list2) → stored in memory, not executed
#   2. def intersection(list1, list2) → stored in memory, not executed
#   3. list1 = ... → assigns value to variable
#   4. list2 = ... → assigns value to variable
#   5. result = ... → assigns value to variable
#   6. print(result)
#   7. def is_in_list(x, list2) → stored in memory, not executed
#   8. def is_in_list(x, list2) → stored in memory, not executed
#   9. x = ... → assigns value to variable
#   10. list2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(is_in_list(...))
#      → calls is_in_list(x, list2) with x=3, list2=[4, 1, 8, 6, 3]
#      → 13.1 return any((element == x for element in list2))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_in_list, intersection) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     x = ... → stored in memory
#     list2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_in_list(...)) → is_in_list() runs first → value passed to print()
#
#   Loop: takes one item from list2 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 603:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add(x, y) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: + /
# Built-in functions: len, reduce, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average(numbers) → each argument is evaluated first, then the function body runs
#   add(x, y) → each argument is evaluated first, then the function body runs
#   add(3, 14) runs first → then print() outputs the result
#   x + y → Python looks up x, then y, then applies +
#   total / len(...) → Python looks up total, then len(...), then applies /
#   add(3, 14) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def add(x, y) → stored in memory, not executed
#   3. def average(lst) → stored in memory, not executed
#   4. numbers = ... → assigns value to variable
#   5. result = ... → assigns value to variable
#   6. print(result)
#   7. def add(x, y) → stored in memory, not executed
#   8. def add(x, y) → stored in memory, not executed
#   9. x = ... → assigns value to variable
#   10. y = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(add(...))
#      → calls add(x, y) with x=3, y=14
#      → 13.1 import operator
#      → 13.2 return operator.add(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average, add) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     x = ... → stored in memory
#     y = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add(...)) → add() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 604:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def compose_functions(functions, initial_value) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * +
# Built-in functions: func, reduce()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   compose_functions(functions, initial_value) → each argument is evaluated first, then the function body runs
#   compose_functions(25, 25) runs first → then print() outputs the result
#   x + 1 → Python looks up x, then 1, then applies +
#   x * 2 → Python looks up x, then 2, then applies *
#   compose_functions(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from functools import reduce
#   2. def compose_functions(functions, initial_value) → stored in memory, not executed
#   3. def add_one(x) → stored in memory, not executed
#   4. def multiply_by_two(x) → stored in memory, not executed
#   5. functions = ... → assigns value to variable
#   6. initial_value = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. def compose_functions(functions, initial_value) → stored in memory, not executed
#   10. def compose_functions(functions, initial_value) → stored in memory, not executed
#   11. functions = ... → assigns value to variable
#   12. initial_value = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(compose_functions(...))
#      → calls compose_functions(functions, initial_value) with functions=25, initial_value=25
#      → 15.1 return func(x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_one, compose_functions, multiply_by_two) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     functions = ... → stored in memory
#     initial_value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     functions = ... → stored in memory
#     initial_value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(compose_functions(...)) → compose_functions() runs first → value passed to print()

"""

"""
Problem 605:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_keys(dic) runs first → then print() outputs the result
#   get_dict_keys() → each argument is evaluated first, then the function body runs
#   get_keys(dic) → each argument is evaluated first, then the function body runs
#   get_keys(25) runs first → then print() outputs the result
#   get_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(get_keys(...))
#      → calls get_keys(dic) with dic=dic
#      → 3.1 return list(dic.keys())
#   4. def get_dict_keys() → stored in memory, not executed
#   5. keys_list = ... → assigns value to variable
#   6. print(keys_list)
#   7. def get_keys(dic) → stored in memory, not executed
#   8. def get_keys(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(get_keys(...))
#      → calls get_keys(dic) with dic=25
#      → 12.1 return list(dic.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_dict_keys, get_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(get_keys(...)) → get_keys() runs first → value passed to print()
#     keys_list = ... → stored in memory
#     print(keys_list)
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_keys(...)) → get_keys() runs first → value passed to print()

"""

"""
Problem 606:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_string_len(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_string_len(lst) runs first → then print() outputs the result
#   sort_string_len(lst) → each argument is evaluated first, then the function body runs
#   sort_string_len(?) runs first → then print() outputs the result
#   sort_string_len(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_string_len(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(sort_string_len(...))
#      → calls sort_string_len(lst) with lst=lst
#      → 3.1 return sorted(lst, key=len)
#   4. def sort_string_len(lst) → stored in memory, not executed
#   5. def sort_string_len(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_string_len(...))
#      → calls sort_string_len(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return sorted(lst, key=len)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_string_len) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(sort_string_len(...)) → sort_string_len() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_string_len(...)) → sort_string_len() runs first → value passed to print()

"""

"""
Problem 607:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_tuples_second(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_tuples_second(lst) runs first → then print() outputs the result
#   lst_tuples_second(lst) → each argument is evaluated first, then the function body runs
#   lst_tuples_second(?) runs first → then print() outputs the result
#   lst_tuples_second(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_tuples_second(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_tuples_second(...))
#      → calls lst_tuples_second(lst) with lst=lst
#      → 3.1 return x[1]
#   4. def lst_tuples_second(lst) → stored in memory, not executed
#   5. def lst_tuples_second(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_tuples_second(...))
#      → calls lst_tuples_second(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return x[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_tuples_second) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_tuples_second(...)) → lst_tuples_second() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_tuples_second(...)) → lst_tuples_second() runs first → value passed to print()

"""

"""
Problem 608:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_dicts_by_key(lst, target_key) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_dicts_by_key(dicts, target_key) → each argument is evaluated first, then the function body runs
#   sort_dicts_by_key(lst, target_key) → each argument is evaluated first, then the function body runs
#   sort_dicts_by_key(?, 25) runs first → then print() outputs the result
#   sort_dicts_by_key(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_dicts_by_key(lst, target_key) → stored in memory, not executed
#   2. dicts = ... → assigns value to variable
#   3. target_key = ... → assigns value to variable
#   4. sorted_dicts = ... → assigns value to variable
#   5. print(sorted_dicts)
#   6. def sort_dicts_by_key(lst, target_key) → stored in memory, not executed
#   7. def sort_dicts_by_key(lst, target_key) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. target_key = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(sort_dicts_by_key(...))
#      → calls sort_dicts_by_key(lst, target_key) with lst=[1, 3, 5, 7], target_key=25
#      → 12.1 return d[target_key]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_dicts_by_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dicts = ... → stored in memory
#     target_key = ... → stored in memory
#     sorted_dicts = ... → stored in memory
#     print(sorted_dicts)
#     lst = ... → stored in memory
#     target_key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_dicts_by_key(...)) → sort_dicts_by_key() runs first → value passed to print()

"""

"""
Problem 609:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_last_names(names) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_last_names(names) runs first → then print() outputs the result
#   sort_last_names(names) → each argument is evaluated first, then the function body runs
#   sort_last_names(?) runs first → then print() outputs the result
#   sort_last_names(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_last_names(names) → stored in memory, not executed
#   2. names = ... → assigns value to variable
#   3. print(sort_last_names(...))
#      → calls sort_last_names(names) with names=names
#      → 3.1 return name.split()[-1]
#   4. def sort_last_names(names) → stored in memory, not executed
#   5. def sort_last_names(names) → stored in memory, not executed
#   6. names = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_last_names(...))
#      → calls sort_last_names(names) with names=[4, 1, 8, 6, 3]
#      → 9.1 return name.split()[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_last_names) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     names = ... → stored in memory
#     print(sort_last_names(...)) → sort_last_names() runs first → value passed to print()
#     names = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_last_names(...)) → sort_last_names() runs first → value passed to print()

"""

"""
Problem 610:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_ignore_case(strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + <=
# Built-in functions: chr, ord, sorted, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_ignore_case(mixed_case_strings) → each argument is evaluated first, then the function body runs
#   sort_ignore_case(strings) → each argument is evaluated first, then the function body runs
#   sort_ignore_case(?) runs first → then print() outputs the result
#   sort_ignore_case(?) → each argument is evaluated first, then the function body runs
#   'A' <= c → evaluated as True or False (determines which branch runs)
#   'A' <= 'Z' → evaluated as True or False (determines which branch runs)
#   ord(...) + 32 → Python looks up ord(...), then 32, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_ignore_case(strings) → stored in memory, not executed
#   2. mixed_case_strings = ... → assigns value to variable
#   3. sorted_strings = ... → assigns value to variable
#   4. print(sorted_strings)
#   5. def sort_ignore_case(strings) → stored in memory, not executed
#   6. def sort_ignore_case(strings) → stored in memory, not executed
#   7. strings = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_ignore_case(...))
#      → calls sort_ignore_case(strings) with strings=[4, 1, 8, 6, 3]
#      → 10.1 return str(s).lower()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_ignore_case) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     mixed_case_strings = ... → stored in memory
#     sorted_strings = ... → stored in memory
#     print(sorted_strings)
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_ignore_case(...)) → sort_ignore_case() runs first → value passed to print()

"""

"""
Problem 611:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_floats(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_floats(lst) runs first → then print() outputs the result
#   list_floats(lst) → each argument is evaluated first, then the function body runs
#   list_floats(?) runs first → then print() outputs the result
#   list_floats(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_floats(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(list_floats(...))
#      → calls list_floats(lst) with lst=lst
#      → 3.1 return x
#   4. def list_floats(lst) → stored in memory, not executed
#   5. def list_floats(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(list_floats(...))
#      → calls list_floats(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_floats) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(list_floats(...)) → list_floats() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_floats(...)) → list_floats() runs first → value passed to print()

"""

"""
Problem 612:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 3.1 return count
#   4. def lst_words(lst) → stored in memory, not executed
#   5. def lst_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#
#   Loop: takes one item from x.lower(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 613:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_people_by_age(people) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_people_by_age(people) → each argument is evaluated first, then the function body runs
#   sort_people_by_age(?) runs first → then print() outputs the result
#   sort_people_by_age(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_people_by_age(people) → stored in memory, not executed
#   2. people = ... → assigns value to variable
#   3. sorted_people = ... → assigns value to variable
#   4. print(sorted_people)
#   5. def sort_people_by_age(people) → stored in memory, not executed
#   6. def sort_people_by_age(people) → stored in memory, not executed
#   7. people = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_people_by_age(...))
#      → calls sort_people_by_age(people) with people=[4, 1, 8, 6, 3]
#      → 10.1 return person['age']
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_people_by_age) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     people = ... → stored in memory
#     sorted_people = ... → stored in memory
#     print(sorted_people)
#     people = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_people_by_age(...)) → sort_people_by_age() runs first → value passed to print()

"""

"""
Problem 614:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_integers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: int, sorted, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_integers(lst) → each argument is evaluated first, then the function body runs
#   lst_integers(?) runs first → then print() outputs the result
#   lst_integers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_integers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. sorted_list = ... → assigns value to variable
#   4. print(sorted_list)
#   5. def lst_integers(lst) → stored in memory, not executed
#   6. def lst_integers(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_integers(...))
#      → calls lst_integers(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     sorted_list = ... → stored in memory
#     print(sorted_list)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#
#   Loop: takes one item from str(...) each round, runs body, repeats until done

"""

"""
Problem 615:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_products(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_products(products) → each argument is evaluated first, then the function body runs
#   lst_products(lst) → each argument is evaluated first, then the function body runs
#   lst_products(?) runs first → then print() outputs the result
#   lst_products(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_products(lst) → stored in memory, not executed
#   2. products = ... → assigns value to variable
#   3. sorted_products = ... → assigns value to variable
#   4. print(sorted_products)
#   5. def lst_products(lst) → stored in memory, not executed
#   6. def lst_products(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_products(...))
#      → calls lst_products(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return product['price']
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_products) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     products = ... → stored in memory
#     sorted_products = ... → stored in memory
#     print(sorted_products)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_products(...)) → lst_products() runs first → value passed to print()

"""

"""
Problem 616:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_dates(dates) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: map, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_dates(date_list) → each argument is evaluated first, then the function body runs
#   sort_dates(dates) → each argument is evaluated first, then the function body runs
#   sort_dates(?) runs first → then print() outputs the result
#   sort_dates(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_dates(dates) → stored in memory, not executed
#   2. date_list = ... → assigns value to variable
#   3. sorted_dates = ... → assigns value to variable
#   4. print(sorted_dates)
#   5. def sort_dates(dates) → stored in memory, not executed
#   6. def sort_dates(dates) → stored in memory, not executed
#   7. dates = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_dates(...))
#      → calls sort_dates(dates) with dates=[4, 1, 8, 6, 3]
#      → 10.1 return (year, month, day)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_dates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     date_list = ... → stored in memory
#     sorted_dates = ... → stored in memory
#     print(sorted_dates)
#     dates = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_dates(...)) → sort_dates() runs first → value passed to print()

"""

"""
Problem 617:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_tuples(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_tuples(lst) → each argument is evaluated first, then the function body runs
#   lst_tuples(?) runs first → then print() outputs the result
#   lst_tuples(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_tuples(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. sorted_tuples = ... → assigns value to variable
#   4. print(sorted_tuples)
#   5. def lst_tuples(lst) → stored in memory, not executed
#   6. def lst_tuples(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_tuples(...))
#      → calls lst_tuples(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return (tup[0], tup[1])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     sorted_tuples = ... → stored in memory
#     print(sorted_tuples)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_tuples(...)) → lst_tuples() runs first → value passed to print()

"""

"""
Problem 618:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_names(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_names(lst) runs first → then print() outputs the result
#   lst_names(lst) → each argument is evaluated first, then the function body runs
#   lst_names(?) runs first → then print() outputs the result
#   lst_names(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_names(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_names(...))
#      → calls lst_names(lst) with lst=lst
#      → 3.1 return sum((1 for _ in name))
#   4. def lst_names(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(lst_names(...))
#      → calls lst_names(lst) with lst=lst
#      → 6.1 return sum((1 for _ in name))
#   7. def lst_names(lst) → stored in memory, not executed
#   8. def lst_names(lst) → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(lst_names(...))
#      → calls lst_names(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return sum((1 for _ in name))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_names) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_names(...)) → lst_names() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(lst_names(...)) → lst_names() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_names(...)) → lst_names() runs first → value passed to print()
#
#   Loop: takes one item from name each round, runs body, repeats until done

"""

"""
Problem 619:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_by_hire_date(employees) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_by_hire_date(employees) → each argument is evaluated first, then the function body runs
#   sort_by_hire_date(?) runs first → then print() outputs the result
#   sort_by_hire_date(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_by_hire_date(employees) → stored in memory, not executed
#   2. employees = ... → assigns value to variable
#   3. sorted_employees = ... → assigns value to variable
#   4. print(sorted_employees)
#   5. def sort_by_hire_date(employees) → stored in memory, not executed
#   6. def sort_by_hire_date(employees) → stored in memory, not executed
#   7. employees = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_by_hire_date(...))
#      → calls sort_by_hire_date(employees) with employees=[4, 1, 8, 6, 3]
#      → 10.1 return employee[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_by_hire_date) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     employees = ... → stored in memory
#     sorted_employees = ... → stored in memory
#     print(sorted_employees)
#     employees = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_by_hire_date(...)) → sort_by_hire_date() runs first → value passed to print()

"""

"""
Problem 621:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def frequency_first_letter(words) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: Counter, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   frequency_first_letter(words) runs first → then print() outputs the result
#   frequency_first_letter(words) → each argument is evaluated first, then the function body runs
#   frequency_first_letter(?) runs first → then print() outputs the result
#   frequency_first_letter(?) → each argument is evaluated first, then the function body runs
#   first_letter in letter_count → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def frequency_first_letter(words) → stored in memory, not executed
#   2. words = ... → assigns value to variable
#   3. print(frequency_first_letter(...))
#      → calls frequency_first_letter(words) with words=words
#      → 3.1 return letter_count[word[0]]
#   4. from collections import Counter
#   5. def frequency_first_letter(words) → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. print(frequency_first_letter(...))
#      → calls frequency_first_letter(words) with words=words
#      → 7.1 return letter_count[word[0]]
#   8. def frequency_first_letter(words) → stored in memory, not executed
#   9. def frequency_first_letter(words) → stored in memory, not executed
#   10. words = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(frequency_first_letter(...))
#      → calls frequency_first_letter(words) with words=[4, 1, 8, 6, 3]
#      → 13.1 return letter_count[word[0]]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (frequency_first_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     print(frequency_first_letter(...)) → frequency_first_letter() runs first → value passed to print()
#     words = ... → stored in memory
#     print(frequency_first_letter(...)) → frequency_first_letter() runs first → value passed to print()
#     words = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(frequency_first_letter(...)) → frequency_first_letter() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 622:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_of_tuples(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: abs, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_of_tuples(lst) runs first → then print() outputs the result
#   lst_of_tuples(lst) → each argument is evaluated first, then the function body runs
#   lst_of_tuples(?) runs first → then print() outputs the result
#   lst_of_tuples(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_of_tuples(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_of_tuples(...))
#      → calls lst_of_tuples(lst) with lst=lst
#      → 3.1 return abs(x[0])
#   4. def lst_of_tuples(lst) → stored in memory, not executed
#   5. def lst_of_tuples(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_of_tuples(...))
#      → calls lst_of_tuples(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return abs(x[0])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_of_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_of_tuples(...)) → lst_of_tuples() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_of_tuples(...)) → lst_of_tuples() runs first → value passed to print()

"""

"""
Problem 623:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_order(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_order(lst) runs first → then print() outputs the result
#   reverse_order(lst) → each argument is evaluated first, then the function body runs
#   reverse_order(?) runs first → then print() outputs the result
#   reverse_order(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_order(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(reverse_order(...))
#      → calls reverse_order(lst) with lst=lst
#      → 3.1 return word
#   4. def reverse_order(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(reverse_order(...))
#      → calls reverse_order(lst) with lst=lst
#      → 6.1 return word
#   7. def reverse_order(lst) → stored in memory, not executed
#   8. def reverse_order(lst) → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(reverse_order(...))
#      → calls reverse_order(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return word
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_order) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(reverse_order(...)) → reverse_order() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(reverse_order(...)) → reverse_order() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_order(...)) → reverse_order() runs first → value passed to print()

"""

"""
Problem 624:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def temperture(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   temperture(lst) runs first → then print() outputs the result
#   temperture(lst) → each argument is evaluated first, then the function body runs
#   temperture(?) runs first → then print() outputs the result
#   temperture(?) → each argument is evaluated first, then the function body runs
#   ? + 32 → Python looks up ?, then 32, then applies +
#   ? * celsius → Python looks up ?, then celsius, then applies *
#   9 / 5 → Python looks up 9, then 5, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def temperture(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(temperture(...))
#      → calls temperture(lst) with lst=lst
#      → 3.1 return fahrenheit
#   4. def temperture(lst) → stored in memory, not executed
#   5. def temperture(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(temperture(...))
#      → calls temperture(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return fahrenheit
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (temperture) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(temperture(...)) → temperture() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(temperture(...)) → temperture() runs first → value passed to print()

"""

"""
Problem 625:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_scores(scores) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_scores(scores) → each argument is evaluated first, then the function body runs
#   sort_scores(?) runs first → then print() outputs the result
#   sort_scores(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_scores(scores) → stored in memory, not executed
#   2. scores = ... → assigns value to variable
#   3. sorted_scores = ... → assigns value to variable
#   4. print(sorted_scores)
#   5. def sort_scores(scores) → stored in memory, not executed
#   6. def sort_scores(scores) → stored in memory, not executed
#   7. scores = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_scores(...))
#      → calls sort_scores(scores) with scores=[4, 1, 8, 6, 3]
#      → 10.1 return x
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_scores) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     scores = ... → stored in memory
#     sorted_scores = ... → stored in memory
#     print(sorted_scores)
#     scores = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_scores(...)) → sort_scores() runs first → value passed to print()

"""

"""
Problem 626:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_emails(emails) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_emails(emails) → each argument is evaluated first, then the function body runs
#   sort_emails(?) runs first → then print() outputs the result
#   sort_emails(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_emails(emails) → stored in memory, not executed
#   2. emails = ... → assigns value to variable
#   3. sorted_emails = ... → assigns value to variable
#   4. print(sorted_emails)
#   5. def sort_emails(emails) → stored in memory, not executed
#   6. def sort_emails(emails) → stored in memory, not executed
#   7. emails = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_emails(...))
#      → calls sort_emails(emails) with emails=[4, 1, 8, 6, 3]
#      → 10.1 return email.split('@')[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_emails) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     emails = ... → stored in memory
#     sorted_emails = ... → stored in memory
#     print(sorted_emails)
#     emails = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_emails(...)) → sort_emails() runs first → value passed to print()

"""

"""
Problem 627:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_cities_by_population(cities) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_cities_by_population(cities) → each argument is evaluated first, then the function body runs
#   sort_cities_by_population(?) runs first → then print() outputs the result
#   sort_cities_by_population(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_cities_by_population(cities) → stored in memory, not executed
#   2. cities = ... → assigns value to variable
#   3. sorted_cities = ... → assigns value to variable
#   4. print(sorted_cities)
#   5. def sort_cities_by_population(cities) → stored in memory, not executed
#   6. def sort_cities_by_population(cities) → stored in memory, not executed
#   7. cities = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_cities_by_population(...))
#      → calls sort_cities_by_population(cities) with cities=[4, 1, 8, 6, 3]
#      → 10.1 return city[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_cities_by_population) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     cities = ... → stored in memory
#     sorted_cities = ... → stored in memory
#     print(sorted_cities)
#     cities = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_cities_by_population(...)) → sort_cities_by_population() runs first → value passed to print()

"""

"""
Problem 628:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def last_letter(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   last_letter(lst) runs first → then print() outputs the result
#   last_letter(lst) → each argument is evaluated first, then the function body runs
#   last_letter(?) runs first → then print() outputs the result
#   last_letter(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def last_letter(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(last_letter(...))
#      → calls last_letter(lst) with lst=lst
#      → 3.1 return word[-1]
#   4. def last_letter(lst) → stored in memory, not executed
#   5. def last_letter(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(last_letter(...))
#      → calls last_letter(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return word[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (last_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(last_letter(...)) → last_letter() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(last_letter(...)) → last_letter() runs first → value passed to print()

"""

"""
Problem 629:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_of_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_of_numbers(lst) runs first → then print() outputs the result
#   lst_of_numbers(lst) → each argument is evaluated first, then the function body runs
#   lst_of_numbers(?) runs first → then print() outputs the result
#   lst_of_numbers(?) → each argument is evaluated first, then the function body runs
#   number in frequency → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_of_numbers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_of_numbers(...))
#      → calls lst_of_numbers(lst) with lst=lst
#      → 3.1 return frequency[number]
#   4. def lst_of_numbers(lst) → stored in memory, not executed
#   5. def lst_of_numbers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_of_numbers(...))
#      → calls lst_of_numbers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return frequency[number]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_of_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_of_numbers(...)) → lst_of_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_of_numbers(...)) → lst_of_numbers() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 630:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_students_by_grades(students) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_students_by_grades(students) runs first → then print() outputs the result
#   sort_students_by_grades(students) → each argument is evaluated first, then the function body runs
#   sort_students_by_grades(?) runs first → then print() outputs the result
#   sort_students_by_grades(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_students_by_grades(students) → stored in memory, not executed
#   2. students = ... → assigns value to variable
#   3. print(sort_students_by_grades(...))
#      → calls sort_students_by_grades(students) with students=students
#      → 3.1 return student[1]
#   4. def sort_students_by_grades(students) → stored in memory, not executed
#   5. def sort_students_by_grades(students) → stored in memory, not executed
#   6. students = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_students_by_grades(...))
#      → calls sort_students_by_grades(students) with students=[4, 1, 8, 6, 3]
#      → 9.1 return student[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_students_by_grades) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     students = ... → stored in memory
#     print(sort_students_by_grades(...)) → sort_students_by_grades() runs first → value passed to print()
#     students = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_students_by_grades(...)) → sort_students_by_grades() runs first → value passed to print()

"""

"""
Problem 631:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_books_by_year(books) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_books_by_year(books) runs first → then print() outputs the result
#   sort_books_by_year(books) → each argument is evaluated first, then the function body runs
#   sort_books_by_year(?) runs first → then print() outputs the result
#   sort_books_by_year(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_books_by_year(books) → stored in memory, not executed
#   2. books = ... → assigns value to variable
#   3. print(sort_books_by_year(...))
#      → calls sort_books_by_year(books) with books=books
#      → 3.1 return book[1]
#   4. def sort_books_by_year(books) → stored in memory, not executed
#   5. def sort_books_by_year(books) → stored in memory, not executed
#   6. books = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_books_by_year(...))
#      → calls sort_books_by_year(books) with books=[4, 1, 8, 6, 3]
#      → 9.1 return book[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_books_by_year) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     books = ... → stored in memory
#     print(sort_books_by_year(...)) → sort_books_by_year() runs first → value passed to print()
#     books = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_books_by_year(...)) → sort_books_by_year() runs first → value passed to print()

"""

"""
Problem 632:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_files_by_extension(files) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_files_by_extension(files) runs first → then print() outputs the result
#   sort_files_by_extension(files) → each argument is evaluated first, then the function body runs
#   sort_files_by_extension(?) runs first → then print() outputs the result
#   sort_files_by_extension(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_files_by_extension(files) → stored in memory, not executed
#   2. files = ... → assigns value to variable
#   3. print(sort_files_by_extension(...))
#      → calls sort_files_by_extension(files) with files=files
#      → 3.1 return file.split('.')[-1]
#   4. def sort_files_by_extension(files) → stored in memory, not executed
#   5. def sort_files_by_extension(files) → stored in memory, not executed
#   6. files = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_files_by_extension(...))
#      → calls sort_files_by_extension(files) with files=[4, 1, 8, 6, 3]
#      → 9.1 return file.split('.')[-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_files_by_extension) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     files = ... → stored in memory
#     print(sort_files_by_extension(...)) → sort_files_by_extension() runs first → value passed to print()
#     files = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_files_by_extension(...)) → sort_files_by_extension() runs first → value passed to print()

"""

"""
Problem 633:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_movies_by_release_date(movies) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_movies_by_release_date(movies) → each argument is evaluated first, then the function body runs
#   sort_movies_by_release_date(?) runs first → then print() outputs the result
#   sort_movies_by_release_date(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_movies_by_release_date(movies) → stored in memory, not executed
#   2. movies = ... → assigns value to variable
#   3. sorted_movies = ... → assigns value to variable
#   4. for movie in sorted_movies → loops over each item
#      → 4.1 print(?)
#   5. def sort_movies_by_release_date(movies) → stored in memory, not executed
#   6. def sort_movies_by_release_date(movies) → stored in memory, not executed
#   7. movies = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_movies_by_release_date(...))
#      → calls sort_movies_by_release_date(movies) with movies=[4, 1, 8, 6, 3]
#      → 10.1 return movie['release_date']
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_movies_by_release_date) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     movies = ... → stored in memory
#     sorted_movies = ... → stored in memory
#     for movie in sorted_movies → runs body once per item
#     movies = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_movies_by_release_date(...)) → sort_movies_by_release_date() runs first → value passed to print()
#
#   Loop: takes one item from sorted_movies each round, runs body, repeats until done

"""

"""
Problem 634:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_sentences_by_unique_words(sentences) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, set, sorted, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_sentences_by_unique_words(sentences) → each argument is evaluated first, then the function body runs
#   sort_sentences_by_unique_words(?) runs first → then print() outputs the result
#   sort_sentences_by_unique_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_sentences_by_unique_words(sentences) → stored in memory, not executed
#   2. sentences = ... → assigns value to variable
#   3. sorted_sentences = ... → assigns value to variable
#   4. for sentence in sorted_sentences → loops over each item
#      → 4.1 print(sentence)
#   5. def sort_sentences_by_unique_words(sentences) → stored in memory, not executed
#   6. def sort_sentences_by_unique_words(sentences) → stored in memory, not executed
#   7. sentences = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(sort_sentences_by_unique_words(...))
#      → calls sort_sentences_by_unique_words(sentences) with sentences=[4, 1, 8, 6, 3]
#      → 10.1 return sum((1 for _ in unique_words))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_sentences_by_unique_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentences = ... → stored in memory
#     sorted_sentences = ... → stored in memory
#     for sentence in sorted_sentences → runs body once per item
#     sentences = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_sentences_by_unique_words(...)) → sort_sentences_by_unique_words() runs first → value passed to print()
#
#   Loop: takes one item from sorted_sentences each round, runs body, repeats until done

"""

"""
Problem 635:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_keys_from_dictionary(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_keys_from_dictionary(example_dict) → each argument is evaluated first, then the function body runs
#   get_keys_from_dictionary(dic) → each argument is evaluated first, then the function body runs
#   get_keys_from_dictionary(25) runs first → then print() outputs the result
#   get_keys_from_dictionary(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_keys_from_dictionary(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. keys = ... → assigns value to variable
#   4. print(keys)
#   5. def get_keys_from_dictionary(dic) → stored in memory, not executed
#   6. def get_keys_from_dictionary(dic) → stored in memory, not executed
#   7. dic = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_keys_from_dictionary(...))
#      → calls get_keys_from_dictionary(dic) with dic=25
#      → 10.1 return list(dic.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_keys_from_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     keys = ... → stored in memory
#     print(keys)
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_keys_from_dictionary(...)) → get_keys_from_dictionary() runs first → value passed to print()

"""

"""
Problem 636:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def dic_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dic_keys(dic) runs first → then print() outputs the result
#   dic_keys(dic) → each argument is evaluated first, then the function body runs
#   dic_keys(25) runs first → then print() outputs the result
#   dic_keys(25) → each argument is evaluated first, then the function body runs
#   value > 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def dic_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(dic_keys(...))
#      → calls dic_keys(dic) with dic=dic
#      → 3.1 return result
#   4. def dic_keys(dic) → stored in memory, not executed
#   5. def dic_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(dic_keys(...))
#      → calls dic_keys(dic) with dic=25
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (dic_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(dic_keys(...)) → dic_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(dic_keys(...)) → dic_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 637:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_fruit_keys(fruit_prices) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_fruit_keys(fruit_prices) runs first → then print() outputs the result
#   print_fruit_keys(fruit_prices) → each argument is evaluated first, then the function body runs
#   print_fruit_keys(25) runs first → then print() outputs the result
#   print_fruit_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_fruit_keys(fruit_prices) → stored in memory, not executed
#   2. fruit_prices = ... → assigns value to variable
#   3. print(print_fruit_keys(...))
#      → calls print_fruit_keys(fruit_prices) with fruit_prices=fruit_prices
#      → 3.1 return list(fruit_prices.keys())
#   4. def print_fruit_keys(fruit_prices) → stored in memory, not executed
#   5. def print_fruit_keys(fruit_prices) → stored in memory, not executed
#   6. fruit_prices = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_fruit_keys(...))
#      → calls print_fruit_keys(fruit_prices) with fruit_prices=25
#      → 9.1 return list(fruit_prices.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_fruit_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     fruit_prices = ... → stored in memory
#     print(print_fruit_keys(...)) → print_fruit_keys() runs first → value passed to print()
#     fruit_prices = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_fruit_keys(...)) → print_fruit_keys() runs first → value passed to print()

"""

"""
Problem 638:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def max_key(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   max_key(example_dict) runs first → then print() outputs the result
#   max_key(dic) → each argument is evaluated first, then the function body runs
#   max_key(?) runs first → then print() outputs the result
#   max_key(example_dict) → each argument is evaluated first, then the function body runs
#   max_key(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def max_key(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(max_key(...))
#      → calls max_key(dic) with dic=example_dict
#      → 3.1 return max(dic.keys())
#   4. def max_key(dic) → stored in memory, not executed
#   5. def max_key(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(max_key(...))
#      → calls max_key(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return max(dic.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (max_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(max_key(...)) → max_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(max_key(...)) → max_key() runs first → value passed to print()

"""

"""
Problem 639:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def name_age(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   name_age(dic) runs first → then print() outputs the result
#   name_age(dic) → each argument is evaluated first, then the function body runs
#   name_age(?) runs first → then print() outputs the result
#   name_age(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def name_age(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(name_age(...))
#      → calls name_age(dic) with dic=dic
#      → 3.1 return dic[name]
#   4. def name_age(dic) → stored in memory, not executed
#   5. def name_age(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(name_age(...))
#      → calls name_age(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return dic[name]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (name_age) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(name_age(...)) → name_age() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(name_age(...)) → name_age() runs first → value passed to print()

"""

"""
Problem 640:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def concatenate_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   concatenate_keys(dic) runs first → then print() outputs the result
#   concatenate_keys(dic) → each argument is evaluated first, then the function body runs
#   concatenate_keys(?) runs first → then print() outputs the result
#   concatenate_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def concatenate_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(concatenate_keys(...))
#      → calls concatenate_keys(dic) with dic=dic
#      → 3.1 return result
#   4. def concatenate_keys(dic) → stored in memory, not executed
#   5. def concatenate_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(concatenate_keys(...))
#      → calls concatenate_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (concatenate_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(concatenate_keys(...)) → concatenate_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(concatenate_keys(...)) → concatenate_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done

"""

"""
Problem 641:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_countries_and_capitals(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_countries_and_capitals(countries_capitals) → each argument is evaluated first, then the function body runs
#   print_countries_and_capitals(dic) → each argument is evaluated first, then the function body runs
#   print_countries_and_capitals(25) runs first → then print() outputs the result
#   print_countries_and_capitals(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_countries_and_capitals(dic) → stored in memory, not executed
#   2. countries_capitals = ... → assigns value to variable
#   3. print_countries_and_capitals(countries_capitals) → calls print_countries_and_capitals(dic) with dic=countries_capitals
#      → 3.1 for country, capital in dic.items():
#      → 3.1     print(f'{country}: {capital}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_countries_and_capitals(...))
#      → calls print_countries_and_capitals(dic) with dic=25
#      → 9.1 for country, capital in dic.items():
#      → 9.1     print(f'{country}: {capital}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_countries_and_capitals, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     countries_capitals = ... → stored in memory
#     print_countries_and_capitals(countries_capitals) → jumps into print_countries_and_capitals(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_countries_and_capitals(...)) → print_countries_and_capitals() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done

"""

"""
Problem 642:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_key(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_key(dic) runs first → then print() outputs the result
#   count_key(dic) → each argument is evaluated first, then the function body runs
#   count_key(?) runs first → then print() outputs the result
#   count_key(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_key(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(count_key(...))
#      → calls count_key(dic) with dic=dic
#      → 3.1 return count
#   4. def count_key(dic) → stored in memory, not executed
#   5. def count_key(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_key(...))
#      → calls count_key(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(count_key(...)) → count_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_key(...)) → count_key() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done

"""

"""
Problem 643:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_key(dic, letter) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_key(dic, 'a') runs first → then print() outputs the result
#   count_key(dic, letter) → each argument is evaluated first, then the function body runs
#   count_key(?, 25) runs first → then print() outputs the result
#   count_key(dic, 'a') → each argument is evaluated first, then the function body runs
#   count_key(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_key(dic, letter) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(count_key(...))
#      → calls count_key(dic, letter) with dic=dic, letter='a'
#      → 3.1 return result
#   4. def count_key(dic, letter) → stored in memory, not executed
#   5. def count_key(dic, letter) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. letter = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(count_key(...))
#      → calls count_key(dic, letter) with dic=[4, 1, 8, 6, 3], letter=25
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(count_key(...)) → count_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     letter = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_key(...)) → count_key() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 644:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sorted_students_grades(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sorted_students_grades(students_grades) runs first → then print() outputs the result
#   sorted_students_grades(dic) → each argument is evaluated first, then the function body runs
#   sorted_students_grades(?) runs first → then print() outputs the result
#   sorted_students_grades(students_grades) → each argument is evaluated first, then the function body runs
#   sorted_students_grades(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sorted_students_grades(dic) → stored in memory, not executed
#   2. students_grades = ... → assigns value to variable
#   3. print(sorted_students_grades(...))
#      → calls sorted_students_grades(dic) with dic=students_grades
#      → 3.1 return sorted(dic.keys())
#   4. def sorted_students_grades(dic) → stored in memory, not executed
#   5. def sorted_students_grades(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sorted_students_grades(...))
#      → calls sorted_students_grades(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(dic.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sorted_students_grades) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     students_grades = ... → stored in memory
#     print(sorted_students_grades(...)) → sorted_students_grades() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sorted_students_grades(...)) → sorted_students_grades() runs first → value passed to print()

"""

"""
Problem 645:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def list_dictionary(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   list_dictionary(lst1, lst2) runs first → then print() outputs the result
#   list_dictionary(lst1, lst2) → each argument is evaluated first, then the function body runs
#   list_dictionary(?, ?) runs first → then print() outputs the result
#   list_dictionary(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def list_dictionary(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(list_dictionary(...))
#      → calls list_dictionary(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return dic
#   5. def list_dictionary(lst1, lst2) → stored in memory, not executed
#   6. def list_dictionary(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(list_dictionary(...))
#      → calls list_dictionary(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (list_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(list_dictionary(...)) → list_dictionary() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(list_dictionary(...)) → list_dictionary() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 646:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_keys(colors) runs first → then print() outputs the result
#   reverse_keys(dic) → each argument is evaluated first, then the function body runs
#   reverse_keys(25) runs first → then print() outputs the result
#   reverse_keys(colors) → each argument is evaluated first, then the function body runs
#   reverse_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_keys(dic) → stored in memory, not executed
#   2. colors = ... → assigns value to variable
#   3. print(reverse_keys(...))
#      → calls reverse_keys(dic) with dic=colors
#      → 3.1 return list(dic.keys())[::-1]
#   4. def reverse_keys(dic) → stored in memory, not executed
#   5. def reverse_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_keys(...))
#      → calls reverse_keys(dic) with dic=25
#      → 9.1 return list(dic.keys())[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     colors = ... → stored in memory
#     print(reverse_keys(...)) → reverse_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_keys(...)) → reverse_keys() runs first → value passed to print()

"""

"""
Problem 647:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def first_key(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: iter, next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   first_key(example_dict) runs first → then print() outputs the result
#   first_key(dic) → each argument is evaluated first, then the function body runs
#   first_key(25) runs first → then print() outputs the result
#   first_key(example_dict) → each argument is evaluated first, then the function body runs
#   first_key(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def first_key(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(first_key(...))
#      → calls first_key(dic) with dic=example_dict
#      → 3.1 return next(iter(dic))
#   4. def first_key(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. first_key(dic) → calls first_key(dic) with dic=dic
#      → 6.1 return next(iter(dic))
#   7. def first_key(dic) → stored in memory, not executed
#   8. def first_key(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(first_key(...))
#      → calls first_key(dic) with dic=25
#      → 12.1 return next(iter(dic))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (first_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(first_key(...)) → first_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     first_key(dic) → jumps into first_key(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(first_key(...)) → first_key() runs first → value passed to print()
#
#   Loop: takes one item from dic each round, runs body, repeats until done

"""

"""
Problem 648:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def shopping_cart_items(cart) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   shopping_cart_items(cart) → each argument is evaluated first, then the function body runs
#   shopping_cart_items(?) runs first → then print() outputs the result
#   shopping_cart_items(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def shopping_cart_items(cart) → stored in memory, not executed
#   2. cart = ... → assigns value to variable
#   3. shopping_cart_items(cart) → calls shopping_cart_items(cart) with cart=cart
#      → 3.1 for item in cart.keys():
#      → 3.1     print(item)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. cart = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(shopping_cart_items(...))
#      → calls shopping_cart_items(cart) with cart=[4, 1, 8, 6, 3]
#      → 9.1 for item in cart.keys():
#      → 9.1     print(item)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (shopping_cart_items, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     cart = ... → stored in memory
#     shopping_cart_items(cart) → jumps into shopping_cart_items(), runs body, returns
#     cart = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(shopping_cart_items(...)) → shopping_cart_items() runs first → value passed to print()
#
#   Loop: takes one item from cart.keys(...) each round, runs body, repeats until done

"""

"""
Problem 649:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_key_exists(dic, key) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_key_exists(example_dict, key_to_check) runs first → then print() outputs the result
#   check_key_exists(dic, key) → each argument is evaluated first, then the function body runs
#   check_key_exists(25, 'value') runs first → then print() outputs the result
#   key in dic → evaluated as True or False (determines which branch runs)
#   check_key_exists(example_dict, key_to_check) → each argument is evaluated first, then the function body runs
#   check_key_exists(25, 'value') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_key_exists(dic, key) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. key_to_check = ... → assigns value to variable
#   4. print(check_key_exists(...))
#      → calls check_key_exists(dic, key) with dic=example_dict, key=key_to_check
#      → 4.1 return True
#   5. key_to_check = ... → assigns value to variable
#   6. print(check_key_exists(...))
#      → calls check_key_exists(dic, key) with dic=example_dict, key=key_to_check
#      → 6.1 return True
#   7. def check_key_exists(dic, key) → stored in memory, not executed
#   8. def check_key_exists(dic, key) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. key = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(check_key_exists(...))
#      → calls check_key_exists(dic, key) with dic=25, key='value'
#      → 13.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_key_exists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     key_to_check = ... → stored in memory
#     print(check_key_exists(...)) → check_key_exists() runs first → value passed to print()
#     key_to_check = ... → stored in memory
#     print(check_key_exists(...)) → check_key_exists() runs first → value passed to print()
#     dic = ... → stored in memory
#     key = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_key_exists(...)) → check_key_exists() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 650:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_keys_greater_than_five(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_keys_greater_than_five(items_dict) → each argument is evaluated first, then the function body runs
#   print_keys_greater_than_five(dic) → each argument is evaluated first, then the function body runs
#   print_keys_greater_than_five(25) runs first → then print() outputs the result
#   print_keys_greater_than_five(25) → each argument is evaluated first, then the function body runs
#   quantity > 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_keys_greater_than_five(dic) → stored in memory, not executed
#   2. items_dict = ... → assigns value to variable
#   3. print_keys_greater_than_five(items_dict) → calls print_keys_greater_than_five(dic) with dic=items_dict
#      → 3.1 for item, quantity in dic.items():
#      → 3.1     if quantity > 5:
#      → 3.1         print(item)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_keys_greater_than_five(...))
#      → calls print_keys_greater_than_five(dic) with dic=25
#      → 9.1 for item, quantity in dic.items():
#      → 9.1     if quantity > 5:
#      → 9.1         print(item)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_keys_greater_than_five) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items_dict = ... → stored in memory
#     print_keys_greater_than_five(items_dict) → jumps into print_keys_greater_than_five(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_keys_greater_than_five(...)) → print_keys_greater_than_five() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 651:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def returns_strings(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   returns_strings(example_dict) runs first → then print() outputs the result
#   returns_strings(dic) → each argument is evaluated first, then the function body runs
#   returns_strings(?) runs first → then print() outputs the result
#   returns_strings(example_dict) → each argument is evaluated first, then the function body runs
#   returns_strings(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def returns_strings(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(returns_strings(...))
#      → calls returns_strings(dic) with dic=example_dict
#      → 3.1 return result
#   4. def returns_strings(dic) → stored in memory, not executed
#   5. def returns_strings(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(returns_strings(...))
#      → calls returns_strings(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (returns_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(returns_strings(...)) → returns_strings() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(returns_strings(...)) → returns_strings() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 652:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sports_by_popularity(sports_dict) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sports_by_popularity(sports_dict) runs first → then print() outputs the result
#   sports_by_popularity(sports_dict) → each argument is evaluated first, then the function body runs
#   sports_by_popularity(?) runs first → then print() outputs the result
#   sports_by_popularity(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sports_by_popularity(sports_dict) → stored in memory, not executed
#   2. sports_dict = ... → assigns value to variable
#   3. print(sports_by_popularity(...))
#      → calls sports_by_popularity(sports_dict) with sports_dict=sports_dict
#      → 3.1 return sports_dict[sport]
#   4. def sports_by_popularity(sports_dict) → stored in memory, not executed
#   5. def sports_by_popularity(sports_dict) → stored in memory, not executed
#   6. sports_dict = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sports_by_popularity(...))
#      → calls sports_by_popularity(sports_dict) with sports_dict=[4, 1, 8, 6, 3]
#      → 9.1 return sports_dict[sport]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sports_by_popularity) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sports_dict = ... → stored in memory
#     print(sports_by_popularity(...)) → sports_by_popularity() runs first → value passed to print()
#     sports_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sports_by_popularity(...)) → sports_by_popularity() runs first → value passed to print()

"""

"""
Problem 653:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) → each argument is evaluated first, then the function body runs
#   main(25) runs first → then print() outputs the result
#   main(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. main(dic) → calls main(dic) with dic=dic
#      → 3.1 return result
#   4. data = ... → assigns value to variable
#   5. integer_keys = ... → assigns value to variable
#   6. print(integer_keys)
#   7. def main(dic) → stored in memory, not executed
#   8. def main(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(dic) with dic=25
#      → 12.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     main(dic) → jumps into main(), runs body, returns
#     data = ... → stored in memory
#     integer_keys = ... → stored in memory
#     print(integer_keys)
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 654:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def books_authors(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   books_authors(dic) → each argument is evaluated first, then the function body runs
#   books_authors(25) runs first → then print() outputs the result
#   books_authors(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def books_authors(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. books_authors(dic) → calls books_authors(dic) with dic=dic
#      → 3.1 for index, book in enumerate(dic.keys(), start=1):
#      → 3.1     print(f'{index}. {book}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(books_authors(...))
#      → calls books_authors(dic) with dic=25
#      → 9.1 for index, book in enumerate(dic.keys(), start=1):
#      → 9.1     print(f'{index}. {book}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (books_authors, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     books_authors(dic) → jumps into books_authors(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(books_authors(...)) → books_authors() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 655:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def palindromic_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   palindromic_keys(example_dict) runs first → then print() outputs the result
#   palindromic_keys(dic) → each argument is evaluated first, then the function body runs
#   palindromic_keys(?) runs first → then print() outputs the result
#   palindromic_keys(example_dict) → each argument is evaluated first, then the function body runs
#   palindromic_keys(?) → each argument is evaluated first, then the function body runs
#   key == ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def palindromic_keys(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(palindromic_keys(...))
#      → calls palindromic_keys(dic) with dic=example_dict
#      → 3.1 return result
#   4. def palindromic_keys(dic) → stored in memory, not executed
#   5. def palindromic_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(palindromic_keys(...))
#      → calls palindromic_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (palindromic_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(palindromic_keys(...)) → palindromic_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(palindromic_keys(...)) → palindromic_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 656:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def pet_owners(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   pet_owners(dic) runs first → then print() outputs the result
#   pet_owners(dic) → each argument is evaluated first, then the function body runs
#   pet_owners(?) runs first → then print() outputs the result
#   pet_owners(?) → each argument is evaluated first, then the function body runs
#   'o' in key → evaluated as True or False (determines which branch runs)
#   char == 'o' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def pet_owners(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(pet_owners(...))
#      → calls pet_owners(dic) with dic=dic
#      → 3.1 return result
#   4. def pet_owners(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. print(pet_owners(...))
#      → calls pet_owners(dic) with dic=dic
#      → 6.1 return result
#   7. def pet_owners(dic) → stored in memory, not executed
#   8. def pet_owners(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(pet_owners(...))
#      → calls pet_owners(dic) with dic=[4, 1, 8, 6, 3]
#      → 12.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (pet_owners) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(pet_owners(...)) → pet_owners() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(pet_owners(...)) → pet_owners() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(pet_owners(...)) → pet_owners() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 657:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_tuple_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_tuple_keys(dic) runs first → then print() outputs the result
#   count_tuple_keys(dic) → each argument is evaluated first, then the function body runs
#   count_tuple_keys(?) runs first → then print() outputs the result
#   count_tuple_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_tuple_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(count_tuple_keys(...))
#      → calls count_tuple_keys(dic) with dic=dic
#      → 3.1 return count
#   4. def count_tuple_keys(dic) → stored in memory, not executed
#   5. def count_tuple_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_tuple_keys(...))
#      → calls count_tuple_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_tuple_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(count_tuple_keys(...)) → count_tuple_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_tuple_keys(...)) → count_tuple_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 658:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def movies(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   movies(dic) runs first → then print() outputs the result
#   movies(dic) → each argument is evaluated first, then the function body runs
#   movies(?) runs first → then print() outputs the result
#   movies(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def movies(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(movies(...))
#      → calls movies(dic) with dic=dic
#      → 3.1 return sorted(dic.keys())
#   4. def movies(dic) → stored in memory, not executed
#   5. def movies(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(movies(...))
#      → calls movies(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(dic.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (movies) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(movies(...)) → movies() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(movies(...)) → movies() runs first → value passed to print()

"""

"""
Problem 659:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) → each argument is evaluated first, then the function body runs
#   keys_with_list_values(dic) runs first → then print() outputs the result
#   main(25) runs first → then print() outputs the result
#   keys_with_list_values(dic) → each argument is evaluated first, then the function body runs
#   main(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. main(dic) → calls main(dic) with dic=dic
#      → 3.1 return key
#   4. def keys_with_list_values(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. print(keys_with_list_values(...))
#      → calls keys_with_list_values(dic) with dic=dic
#      → 6.1 result = []
#      → 6.2 for key, value in dic.items():
#      → 6.2     if isinstance(value, list):
#      → 6.2         result.append(key)
#      → 6.3 return result
#   7. def main(dic) → stored in memory, not executed
#   8. def main(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(dic) with dic=25
#      → 12.1 return key
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, keys_with_list_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     main(dic) → jumps into main(), runs body, returns
#     dic = ... → stored in memory
#     print(keys_with_list_values(...)) → keys_with_list_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 660:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_car_brands(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_car_brands(car_brands) → each argument is evaluated first, then the function body runs
#   print_car_brands(dic) → each argument is evaluated first, then the function body runs
#   print_car_brands(25) runs first → then print() outputs the result
#   print_car_brands(car_brands_2) → each argument is evaluated first, then the function body runs
#   print_car_brands(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_car_brands(dic) → stored in memory, not executed
#   2. car_brands = ... → assigns value to variable
#   3. print_car_brands(car_brands) → calls print_car_brands(dic) with dic=car_brands
#      → 3.1 print('Car Brands:')
#      → 3.2 for index, brand in enumerate(dic.keys(), start=1):
#      → 3.2     print(f'{index}. {brand}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_car_brands(...))
#      → calls print_car_brands(dic) with dic=25
#      → 9.1 print('Car Brands:')
#      → 9.2 for index, brand in enumerate(dic.keys(), start=1):
#      → 9.2     print(f'{index}. {brand}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_car_brands) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     car_brands = ... → stored in memory
#     print_car_brands(car_brands) → jumps into print_car_brands(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_car_brands(...)) → print_car_brands() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 661:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_key(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
# Built-in functions: len, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_key(dic) runs first → then print() outputs the result
#   second_key(dic) → each argument is evaluated first, then the function body runs
#   second_key(25) runs first → then print() outputs the result
#   second_key(25) → each argument is evaluated first, then the function body runs
#   len(...) > 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_key(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(second_key(...))
#      → calls second_key(dic) with dic=dic
#      → 3.1 return keys_list[1] if len(keys_list) > 1 else None
#   4. def second_key(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. print(second_key(...))
#      → calls second_key(dic) with dic=dic
#      → 6.1 return keys_list[1] if len(keys_list) > 1 else None
#   7. def second_key(dic) → stored in memory, not executed
#   8. def second_key(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(second_key(...))
#      → calls second_key(dic) with dic=25
#      → 12.1 return keys_list[1] if len(keys_list) > 1 else None
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(second_key(...)) → second_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(second_key(...)) → second_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(second_key(...)) → second_key() runs first → value passed to print()

"""

"""
Problem 662:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def temperature(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   temperature(dic) runs first → then print() outputs the result
#   temperature(dic) → each argument is evaluated first, then the function body runs
#   temperature(?) runs first → then print() outputs the result
#   temperature(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def temperature(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(temperature(...))
#      → calls temperature(dic) with dic=dic
#      → 3.1 return upper_keys
#   4. def temperature(dic) → stored in memory, not executed
#   5. def temperature(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(temperature(...))
#      → calls temperature(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return upper_keys
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (temperature) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(temperature(...)) → temperature() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(temperature(...)) → temperature() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done

"""

"""
Problem 663:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def keys_in_list(dic, given_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   keys_in_list(dic, given_list) runs first → then print() outputs the result
#   keys_in_list(dic, given_list) → each argument is evaluated first, then the function body runs
#   keys_in_list(?, ?) runs first → then print() outputs the result
#   keys_in_list(?, ?) → each argument is evaluated first, then the function body runs
#   key in given_list → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def keys_in_list(dic, given_list) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. given_list = ... → assigns value to variable
#   4. print(keys_in_list(...))
#      → calls keys_in_list(dic, given_list) with dic=dic, given_list=given_list
#      → 4.1 return result
#   5. def keys_in_list(dic, given_list) → stored in memory, not executed
#   6. def keys_in_list(dic, given_list) → stored in memory, not executed
#   7. dic = ... → assigns value to variable
#   8. given_list = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(keys_in_list(...))
#      → calls keys_in_list(dic, given_list) with dic=[4, 1, 8, 6, 3], given_list=[4, 1, 8, 6, 3]
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (keys_in_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     given_list = ... → stored in memory
#     print(keys_in_list(...)) → keys_in_list() runs first → value passed to print()
#     dic = ... → stored in memory
#     given_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(keys_in_list(...)) → keys_in_list() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 664:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for | if/else
# Operators: <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#   price < 100 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. devices = ... → assigns value to variable
#   2. for (device, price) in devices.items(...) → loops over each item
#   3. def main() → stored in memory, not executed
#   4. main() → calls main() with 
#      → 4.1 devices = {'Phone': 999, 'Headphones': 50, 'Keyboard': 80, 'Mouse': 25, 'Charger': 15}
#      → 4.2 affordable = [device for device, price in devices.items() if price < 100]
#      → 4.3 for device in affordable:
#      → 4.3     print(device)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. devices = ... → assigns value to variable
#   8. affordable = ... → assigns value to variable
#   9. for device in affordable → loops over each item
#      → 9.1 print(device)
#   10. devices = ... → assigns value to variable
#   11. affordable = ... → assigns value to variable
#   12. for device in affordable → loops over each item
#      → 12.1 print(device)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     devices = ... → stored in memory
#     for (device, price) in devices.items(...) → runs body once per item
#     main() → jumps into main(), runs body, returns
#     devices = ... → stored in memory
#     affordable = ... → stored in memory
#     for device in affordable → runs body once per item
#     devices = ... → stored in memory
#     affordable = ... → stored in memory
#     for device in affordable → runs body once per item
#
#   Loop: takes one item from devices.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 665:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def keys_with_values_greater_than_10(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   keys_with_values_greater_than_10(dic) runs first → then print() outputs the result
#   get_dict_keys(my_dict) runs first → then print() outputs the result
#   keys_with_values_greater_than_10(dic) → each argument is evaluated first, then the function body runs
#   keys_with_values_greater_than_10(?) runs first → then print() outputs the result
#   get_dict_keys(my_dict) → each argument is evaluated first, then the function body runs
#   keys_with_values_greater_than_10(?) → each argument is evaluated first, then the function body runs
#   ? > 10 → evaluated as True or False (determines which branch runs)
#   value > 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def keys_with_values_greater_than_10(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(keys_with_values_greater_than_10(...))
#      → calls keys_with_values_greater_than_10(dic) with dic=dic
#      → 3.1 return result
#   4. def get_dict_keys(dic) → stored in memory, not executed
#   5. my_dict = ... → assigns value to variable
#   6. print(get_dict_keys(...))
#      → calls get_dict_keys(dic) with dic=my_dict
#      → 6.1 result = []
#      → 6.2 for key, value in dic.items():
#      → 6.2     if value > 10:
#      → 6.2         result.append(key)
#      → 6.3 return result
#   7. def keys_with_values_greater_than_10(dic) → stored in memory, not executed
#   8. def keys_with_values_greater_than_10(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(keys_with_values_greater_than_10(...))
#      → calls keys_with_values_greater_than_10(dic) with dic=[4, 1, 8, 6, 3]
#      → 12.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_dict_keys, keys_with_values_greater_than_10) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(keys_with_values_greater_than_10(...)) → keys_with_values_greater_than_10() runs first → value passed to print()
#     my_dict = ... → stored in memory
#     print(get_dict_keys(...)) → get_dict_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(keys_with_values_greater_than_10(...)) → keys_with_values_greater_than_10() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 666:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_fruit_keys(fruits) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_fruit_keys(fruits) → each argument is evaluated first, then the function body runs
#   print_fruit_keys(?) runs first → then print() outputs the result
#   print_fruit_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_fruit_keys(fruits) → stored in memory, not executed
#   2. fruits = ... → assigns value to variable
#   3. print_fruit_keys(fruits) → calls print_fruit_keys(fruits) with fruits=fruits
#      → 3.1 for key in fruits.keys():
#      → 3.1     print(key)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. fruits = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_fruit_keys(...))
#      → calls print_fruit_keys(fruits) with fruits=[4, 1, 8, 6, 3]
#      → 9.1 for key in fruits.keys():
#      → 9.1     print(key)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_fruit_keys, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     fruits = ... → stored in memory
#     print_fruit_keys(fruits) → jumps into print_fruit_keys(), runs body, returns
#     fruits = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_fruit_keys(...)) → print_fruit_keys() runs first → value passed to print()
#
#   Loop: takes one item from fruits.keys(...) each round, runs body, repeats until done

"""

"""
Problem 667:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def name_age(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   name_age(dic) runs first → then print() outputs the result
#   name_age(dic) → each argument is evaluated first, then the function body runs
#   name_age(?) runs first → then print() outputs the result
#   name_age(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def name_age(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(name_age(...))
#      → calls name_age(dic) with dic=dic
#      → 3.1 return values
#   4. def name_age(dic) → stored in memory, not executed
#   5. def name_age(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(name_age(...))
#      → calls name_age(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return values
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (name_age) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(name_age(...)) → name_age() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(name_age(...)) → name_age() runs first → value passed to print()
#
#   Loop: takes one item from x each round, runs body, repeats until done

"""

"""
Problem 668:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def concatenate(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   concatenate(dic) runs first → then print() outputs the result
#   concatenate(dic) → each argument is evaluated first, then the function body runs
#   concatenate(?) runs first → then print() outputs the result
#   concatenate(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def concatenate(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(concatenate(...))
#      → calls concatenate(dic) with dic=dic
#      → 3.1 return result
#   4. def concatenate(dic) → stored in memory, not executed
#   5. def concatenate(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(concatenate(...))
#      → calls concatenate(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (concatenate) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(concatenate(...)) → concatenate() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done

"""

"""
Problem 670:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_keys(dic) runs first → then print() outputs the result
#   count_keys(dic) → each argument is evaluated first, then the function body runs
#   count_keys(25) runs first → then print() outputs the result
#   count_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(count_keys(...))
#      → calls count_keys(dic) with dic=dic
#      → 3.1 return sum((1 for _ in dic))
#   4. def count_keys(dic) → stored in memory, not executed
#   5. def count_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_keys(...))
#      → calls count_keys(dic) with dic=25
#      → 9.1 return sum((1 for _ in dic))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(count_keys(...)) → count_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_keys(...)) → count_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic each round, runs body, repeats until done

"""

"""
Problem 671:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def keys_starting_with(dic, letter) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   keys_starting_with(dic, 'a') runs first → then print() outputs the result
#   keys_starting_with(dic, letter) → each argument is evaluated first, then the function body runs
#   keys_starting_with(?, 25) runs first → then print() outputs the result
#   keys_starting_with(dic, 'a') → each argument is evaluated first, then the function body runs
#   keys_starting_with(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def keys_starting_with(dic, letter) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(keys_starting_with(...))
#      → calls keys_starting_with(dic, letter) with dic=dic, letter='a'
#      → 3.1 return result
#   4. def keys_starting_with(dic, letter) → stored in memory, not executed
#   5. def keys_starting_with(dic, letter) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. letter = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(keys_starting_with(...))
#      → calls keys_starting_with(dic, letter) with dic=[4, 1, 8, 6, 3], letter=25
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (keys_starting_with) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(keys_starting_with(...)) → keys_starting_with() runs first → value passed to print()
#     dic = ... → stored in memory
#     letter = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(keys_starting_with(...)) → keys_starting_with() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 672:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def students_grades(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   students_grades(dic) → each argument is evaluated first, then the function body runs
#   students_grades(?) runs first → then print() outputs the result
#   students_grades(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def students_grades(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. students_grades(dic) → calls students_grades(dic) with dic=dic
#      → 3.1 sorted_keys = sorted(dic.keys())
#      → 3.2 for key in sorted_keys:
#      → 3.2     print(key)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(students_grades(...))
#      → calls students_grades(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 sorted_keys = sorted(dic.keys())
#      → 9.2 for key in sorted_keys:
#      → 9.2     print(key)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, students_grades) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     students_grades(dic) → jumps into students_grades(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(students_grades(...)) → students_grades() runs first → value passed to print()
#
#   Loop: takes one item from sorted_keys each round, runs body, repeats until done

"""

"""
Problem 673:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def two_lst(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   two_lst(lst1, lst2) runs first → then print() outputs the result
#   two_lst(lst1, lst2) → each argument is evaluated first, then the function body runs
#   two_lst(?, ?) runs first → then print() outputs the result
#   two_lst(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def two_lst(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(two_lst(...))
#      → calls two_lst(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def two_lst(lst1, lst2) → stored in memory, not executed
#   6. def two_lst(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(two_lst(...))
#      → calls two_lst(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (two_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(two_lst(...)) → two_lst() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(two_lst(...)) → two_lst() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 674:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def colors_hex(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   colors_hex(colors_dict) → each argument is evaluated first, then the function body runs
#   colors_hex(dic) → each argument is evaluated first, then the function body runs
#   colors_hex(25) runs first → then print() outputs the result
#   colors_hex(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def colors_hex(dic) → stored in memory, not executed
#   2. colors_dict = ... → assigns value to variable
#   3. colors_hex(colors_dict) → calls colors_hex(dic) with dic=colors_dict
#      → 3.1 return list(dic.keys())[::-1]
#   4. def colors_hex(dic) → stored in memory, not executed
#   5. colors_dict = ... → assigns value to variable
#   6. colors_hex(colors_dict) → calls colors_hex(dic) with dic=colors_dict
#      → 6.1 return list(dic.keys())[::-1]
#   7. def solve() → stored in memory, not executed
#   8. def solve() → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(colors_hex(...))
#      → calls colors_hex(dic) with dic=25
#      → 12.1 return list(dic.keys())[::-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, colors_hex) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     colors_dict = ... → stored in memory
#     colors_hex(colors_dict) → jumps into colors_hex(), runs body, returns
#     colors_dict = ... → stored in memory
#     colors_hex(colors_dict) → jumps into colors_hex(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(colors_hex(...)) → colors_hex() runs first → value passed to print()
#
#   Loop: takes one item from keys_in_reverse each round, runs body, repeats until done

"""

"""
Problem 675:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def first_key(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: iter, next()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   first_key(example_dict) runs first → then print() outputs the result
#   first_key(dic) → each argument is evaluated first, then the function body runs
#   first_key(25) runs first → then print() outputs the result
#   first_key(example_dict) → each argument is evaluated first, then the function body runs
#   first_key(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def first_key(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(first_key(...))
#      → calls first_key(dic) with dic=example_dict
#      → 3.1 return next(iter(dic))
#   4. def first_key(dic) → stored in memory, not executed
#   5. def first_key(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(first_key(...))
#      → calls first_key(dic) with dic=25
#      → 9.1 return next(iter(dic))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (first_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(first_key(...)) → first_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(first_key(...)) → first_key() runs first → value passed to print()

"""

"""
Problem 676:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_cart_items(cart) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_cart_items(shopping_cart) → each argument is evaluated first, then the function body runs
#   print_cart_items(cart) → each argument is evaluated first, then the function body runs
#   print_cart_items(?) runs first → then print() outputs the result
#   print_cart_items(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_cart_items(cart) → stored in memory, not executed
#   2. shopping_cart = ... → assigns value to variable
#   3. print_cart_items(shopping_cart) → calls print_cart_items(cart) with cart=shopping_cart
#      → 3.1 for item in cart.keys():
#      → 3.1     print(item)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. cart = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_cart_items(...))
#      → calls print_cart_items(cart) with cart=[4, 1, 8, 6, 3]
#      → 9.1 for item in cart.keys():
#      → 9.1     print(item)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_cart_items) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     shopping_cart = ... → stored in memory
#     print_cart_items(shopping_cart) → jumps into print_cart_items(), runs body, returns
#     cart = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_cart_items(...)) → print_cart_items() runs first → value passed to print()
#
#   Loop: takes one item from cart.keys(...) each round, runs body, repeats until done

"""

"""
Problem 678:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def items_with_high_quantity(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   items_with_high_quantity(inventory) → each argument is evaluated first, then the function body runs
#   items_with_high_quantity(dic) → each argument is evaluated first, then the function body runs
#   items_with_high_quantity(25) runs first → then print() outputs the result
#   items_with_high_quantity(25) → each argument is evaluated first, then the function body runs
#   quantity > 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def items_with_high_quantity(dic) → stored in memory, not executed
#   2. inventory = ... → assigns value to variable
#   3. high_quantity_items = ... → assigns value to variable
#   4. print('Items with quantities greater than 5:')
#   5. for item in high_quantity_items → loops over each item
#      → 5.1 print(item)
#   6. def items_with_high_quantity(dic) → stored in memory, not executed
#   7. def items_with_high_quantity(dic) → stored in memory, not executed
#   8. dic = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(items_with_high_quantity(...))
#      → calls items_with_high_quantity(dic) with dic=25
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (items_with_high_quantity) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     inventory = ... → stored in memory
#     high_quantity_items = ... → stored in memory
#     print('Items with quantities greater than 5:')
#     for item in high_quantity_items → runs body once per item
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(items_with_high_quantity(...)) → items_with_high_quantity() runs first → value passed to print()
#
#   Loop: takes one item from high_quantity_items each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 679:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_keys(example_dict) → each argument is evaluated first, then the function body runs
#   string_keys(dic) → each argument is evaluated first, then the function body runs
#   string_keys(?) runs first → then print() outputs the result
#   string_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_keys(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. string_keys_list = ... → assigns value to variable
#   4. print('Keys that are strings:', string_keys_list)
#   5. def string_keys(dic) → stored in memory, not executed
#   6. def string_keys(dic) → stored in memory, not executed
#   7. dic = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(string_keys(...))
#      → calls string_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 10.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (string_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     string_keys_list = ... → stored in memory
#     print('Keys that are strings:', string_keys_list)
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_keys(...)) → string_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 680:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sports_by_popularity(sports_dict) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sports_by_popularity(sports_dict) runs first → then print() outputs the result
#   sports_by_popularity(sports_dict) → each argument is evaluated first, then the function body runs
#   sports_by_popularity(?) runs first → then print() outputs the result
#   sports_by_popularity(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sports_by_popularity(sports_dict) → stored in memory, not executed
#   2. sports_dict = ... → assigns value to variable
#   3. print(sports_by_popularity(...))
#      → calls sports_by_popularity(sports_dict) with sports_dict=sports_dict
#      → 3.1 return sports_dict[sport]
#   4. def sports_by_popularity(sports_dict) → stored in memory, not executed
#   5. def sports_by_popularity(sports_dict) → stored in memory, not executed
#   6. sports_dict = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sports_by_popularity(...))
#      → calls sports_by_popularity(sports_dict) with sports_dict=[4, 1, 8, 6, 3]
#      → 9.1 return sports_dict[sport]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sports_by_popularity) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sports_dict = ... → stored in memory
#     print(sports_by_popularity(...)) → sports_by_popularity() runs first → value passed to print()
#     sports_dict = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sports_by_popularity(...)) → sports_by_popularity() runs first → value passed to print()

"""

"""
Problem 681:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def retrieve_integer_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   retrieve_integer_keys(example_dict) runs first → then print() outputs the result
#   retrieve_integer_keys(dic) → each argument is evaluated first, then the function body runs
#   retrieve_integer_keys(?) runs first → then print() outputs the result
#   retrieve_integer_keys(example_dict) → each argument is evaluated first, then the function body runs
#   retrieve_integer_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def retrieve_integer_keys(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(retrieve_integer_keys(...))
#      → calls retrieve_integer_keys(dic) with dic=example_dict
#      → 3.1 return result
#   4. def retrieve_integer_keys(dic) → stored in memory, not executed
#   5. def retrieve_integer_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(retrieve_integer_keys(...))
#      → calls retrieve_integer_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (retrieve_integer_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(retrieve_integer_keys(...)) → retrieve_integer_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(retrieve_integer_keys(...)) → retrieve_integer_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 682:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_numbered_books(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_numbered_books(books) → each argument is evaluated first, then the function body runs
#   print_numbered_books(dic) → each argument is evaluated first, then the function body runs
#   print_numbered_books(25) runs first → then print() outputs the result
#   print_numbered_books(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_numbered_books(dic) → stored in memory, not executed
#   2. books = ... → assigns value to variable
#   3. print_numbered_books(books) → calls print_numbered_books(dic) with dic=books
#      → 3.1 for index, items in enumerate(dic.keys(), start=1):
#      → 3.1     print(f'{index}. {items}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_numbered_books(...))
#      → calls print_numbered_books(dic) with dic=25
#      → 9.1 for index, items in enumerate(dic.keys(), start=1):
#      → 9.1     print(f'{index}. {items}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (print_numbered_books, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     books = ... → stored in memory
#     print_numbered_books(books) → jumps into print_numbered_books(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_numbered_books(...)) → print_numbered_books() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done

"""

"""
Problem 683:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def palindrome_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   palindrome_keys(example_dict) runs first → then print() outputs the result
#   palindrome_keys(dic) → each argument is evaluated first, then the function body runs
#   palindrome_keys(?) runs first → then print() outputs the result
#   palindrome_keys(example_dict) → each argument is evaluated first, then the function body runs
#   palindrome_keys(?) → each argument is evaluated first, then the function body runs
#   str(...) == ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def palindrome_keys(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(palindrome_keys(...))
#      → calls palindrome_keys(dic) with dic=example_dict
#      → 3.1 return result
#   4. def palindrome_keys(dic) → stored in memory, not executed
#   5. def palindrome_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(palindrome_keys(...))
#      → calls palindrome_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (palindrome_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(palindrome_keys(...)) → palindrome_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(palindrome_keys(...)) → palindrome_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 684:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def pets_with_o(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   pets_with_o(pets_dict) runs first → then print() outputs the result
#   pets_with_o(dic) → each argument is evaluated first, then the function body runs
#   pets_with_o(?) runs first → then print() outputs the result
#   pets_with_o(pets_dict) → each argument is evaluated first, then the function body runs
#   pets_with_o(?) → each argument is evaluated first, then the function body runs
#   'o' in key → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def pets_with_o(dic) → stored in memory, not executed
#   2. pets_dict = ... → assigns value to variable
#   3. print(pets_with_o(...))
#      → calls pets_with_o(dic) with dic=pets_dict
#      → 3.1 return result
#   4. def pets_with_o(dic) → stored in memory, not executed
#   5. def pets_with_o(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(pets_with_o(...))
#      → calls pets_with_o(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (pets_with_o) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pets_dict = ... → stored in memory
#     print(pets_with_o(...)) → pets_with_o() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(pets_with_o(...)) → pets_with_o() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 685:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_list_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_list_keys(example_dict) runs first → then print() outputs the result
#   count_list_keys(dic) → each argument is evaluated first, then the function body runs
#   count_list_keys(?) runs first → then print() outputs the result
#   count_list_keys(example_dict) → each argument is evaluated first, then the function body runs
#   count_list_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_list_keys(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(count_list_keys(...))
#      → calls count_list_keys(dic) with dic=example_dict
#      → 3.1 return count
#   4. def count_list_keys(dic) → stored in memory, not executed
#   5. def count_list_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_list_keys(...))
#      → calls count_list_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_list_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(count_list_keys(...)) → count_list_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_list_keys(...)) → count_list_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 686:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sorted_movie_titles(movies) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sorted_movie_titles(movies_dict) runs first → then print() outputs the result
#   sorted_movie_titles(movies) → each argument is evaluated first, then the function body runs
#   sorted_movie_titles(?) runs first → then print() outputs the result
#   sorted_movie_titles(movies_dict) → each argument is evaluated first, then the function body runs
#   sorted_movie_titles(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sorted_movie_titles(movies) → stored in memory, not executed
#   2. movies_dict = ... → assigns value to variable
#   3. print(sorted_movie_titles(...))
#      → calls sorted_movie_titles(movies) with movies=movies_dict
#      → 3.1 return sorted(movies.keys())
#   4. def sorted_movie_titles(movies) → stored in memory, not executed
#   5. def sorted_movie_titles(movies) → stored in memory, not executed
#   6. movies = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sorted_movie_titles(...))
#      → calls sorted_movie_titles(movies) with movies=[4, 1, 8, 6, 3]
#      → 9.1 return sorted(movies.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sorted_movie_titles) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     movies_dict = ... → stored in memory
#     print(sorted_movie_titles(...)) → sorted_movie_titles() runs first → value passed to print()
#     movies = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sorted_movie_titles(...)) → sorted_movie_titles() runs first → value passed to print()

"""

"""
Problem 687:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def keys_with_list_values(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   keys_with_list_values(example_dict) runs first → then print() outputs the result
#   keys_with_list_values(dic) → each argument is evaluated first, then the function body runs
#   keys_with_list_values(25) runs first → then print() outputs the result
#   keys_with_list_values(example_dict) → each argument is evaluated first, then the function body runs
#   keys_with_list_values(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def keys_with_list_values(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(keys_with_list_values(...))
#      → calls keys_with_list_values(dic) with dic=example_dict
#      → 3.1 return result
#   4. def keys_with_list_values(dic) → stored in memory, not executed
#   5. def keys_with_list_values(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(keys_with_list_values(...))
#      → calls keys_with_list_values(dic) with dic=25
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (keys_with_list_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(keys_with_list_values(...)) → keys_with_list_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(keys_with_list_values(...)) → keys_with_list_values() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 688:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def print_car_brands(dic) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   print_car_brands(car_brands) → each argument is evaluated first, then the function body runs
#   print_car_brands(dic) → each argument is evaluated first, then the function body runs
#   print_car_brands(?) runs first → then print() outputs the result
#   print_car_brands(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def print_car_brands(dic) → stored in memory, not executed
#   2. car_brands = ... → assigns value to variable
#   3. print_car_brands(car_brands) → calls print_car_brands(dic) with dic=car_brands
#      → 3.1 for brand in dic.keys():
#      → 3.1     print(f'Brand: {brand}')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(print_car_brands(...))
#      → calls print_car_brands(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 for brand in dic.keys():
#      → 9.1     print(f'Brand: {brand}')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve, print_car_brands) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     car_brands = ... → stored in memory
#     print_car_brands(car_brands) → jumps into print_car_brands(), runs body, returns
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(print_car_brands(...)) → print_car_brands() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done

"""

"""
Problem 689:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def second_key(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   second_key(example_dict) runs first → then print() outputs the result
#   second_key(dic) → each argument is evaluated first, then the function body runs
#   second_key(25) runs first → then print() outputs the result
#   second_key(example_dict) → each argument is evaluated first, then the function body runs
#   second_key(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def second_key(dic) → stored in memory, not executed
#   2. example_dict = ... → assigns value to variable
#   3. print(second_key(...))
#      → calls second_key(dic) with dic=example_dict
#      → 3.1 return list(dic.keys())[1]
#   4. def second_key(dic) → stored in memory, not executed
#   5. def second_key(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(second_key(...))
#      → calls second_key(dic) with dic=25
#      → 9.1 return list(dic.keys())[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (second_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     example_dict = ... → stored in memory
#     print(second_key(...)) → second_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(second_key(...)) → second_key() runs first → value passed to print()

"""

"""
Problem 690:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def uppercase_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   uppercase_keys(dic) runs first → then print() outputs the result
#   uppercase_keys(dic) → each argument is evaluated first, then the function body runs
#   uppercase_keys(?) runs first → then print() outputs the result
#   uppercase_keys(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def uppercase_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(uppercase_keys(...))
#      → calls uppercase_keys(dic) with dic=dic
#      → 3.1 return result
#   4. def uppercase_keys(dic) → stored in memory, not executed
#   5. def uppercase_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(uppercase_keys(...))
#      → calls uppercase_keys(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (uppercase_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(uppercase_keys(...)) → uppercase_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(uppercase_keys(...)) → uppercase_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done

"""

"""
Problem 691:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def key_max(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: > is or
# Built-in functions: max, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   key_max(dic) runs first → then print() outputs the result
#   key_max(dic) → each argument is evaluated first, then the function body runs
#   key_max(?) runs first → then print() outputs the result
#   key_max(?) → each argument is evaluated first, then the function body runs
#   item > result → evaluated as True or False (determines which branch runs)
#   max_key_lower  None → evaluated as True or False (determines which branch runs)
#   key_lower > max_key_lower → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def key_max(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(key_max(...))
#      → calls key_max(dic) with dic=dic
#      → 3.1 result = dic[0]
#      → 3.2 for item in dic:
#      → 3.2     if item > result:
#      → 3.2         result = item
#      → 3.3 return result
#   4. def key_max(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. print(key_max(...))
#      → calls key_max(dic) with dic=dic
#      → 6.1 result = dic[0]
#      → 6.2 for item in dic:
#      → 6.2     if item > result:
#      → 6.2         result = item
#      → 6.3 return result
#   7. def key_max(dic) → stored in memory, not executed
#   8. def key_max(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(key_max(...))
#      → calls key_max(dic) with dic=[4, 1, 8, 6, 3]
#      → 12.1 result = dic[0]
#      → 12.2 for item in dic:
#      → 12.2     if item > result:
#      → 12.2         result = item
#      → 12.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (key_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(key_max(...)) → key_max() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(key_max(...)) → key_max() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(key_max(...)) → key_max() runs first → value passed to print()
#
#   Loop: takes one item from dic each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 692:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: list, map, pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_numbers(numbers) runs first → then print() outputs the result
#   square_numbers(lst) → each argument is evaluated first, then the function body runs
#   square_numbers(?) runs first → then print() outputs the result
#   square_numbers(numbers) → each argument is evaluated first, then the function body runs
#   square_numbers(?) → each argument is evaluated first, then the function body runs
#   num ** 2 → Python looks up num, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_numbers(lst) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(square_numbers(...))
#      → calls square_numbers(lst) with lst=numbers
#      → 3.1 import math
#      → 3.2 return math.pow(num, 2)
#   4. def square_numbers(lst) → stored in memory, not executed
#   5. def square_numbers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(square_numbers(...))
#      → calls square_numbers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 import math
#      → 9.2 return math.pow(num, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(square_numbers(...)) → square_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_numbers(...)) → square_numbers() runs first → value passed to print()

"""

"""
Problem 693:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_lengths(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, list, map, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_lengths(strings) runs first → then print() outputs the result
#   string_lengths(lst) → each argument is evaluated first, then the function body runs
#   string_lengths(?) runs first → then print() outputs the result
#   string_lengths(strings) → each argument is evaluated first, then the function body runs
#   string_lengths(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_lengths(lst) → stored in memory, not executed
#   2. strings = ... → assigns value to variable
#   3. print(string_lengths(...))
#      → calls string_lengths(lst) with lst=strings
#      → 3.1 return sum((1 for _ in string))
#   4. def string_lengths(lst) → stored in memory, not executed
#   5. def string_lengths(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(string_lengths(...))
#      → calls string_lengths(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return sum((1 for _ in string))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (string_lengths) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     strings = ... → stored in memory
#     print(string_lengths(...)) → string_lengths() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_lengths(...)) → string_lengths() runs first → value passed to print()
#
#   Loop: takes one item from string each round, runs body, repeats until done

"""

"""
Problem 694:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: - <=
# Built-in functions: chr, list, map, ord, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#   'a' <= c → evaluated as True or False (determines which branch runs)
#   'a' <= 'z' → evaluated as True or False (determines which branch runs)
#   ord(...) - 32 → Python looks up ord(...), then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 3.1 return str(word).upper()
#   4. def lst_words(lst) → stored in memory, not executed
#   5. def lst_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return str(word).upper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()

"""

"""
Problem 695:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, round()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_numbers(lst) runs first → then print() outputs the result
#   lst_numbers(lst) → each argument is evaluated first, then the function body runs
#   lst_numbers(?) runs first → then print() outputs the result
#   lst_numbers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_numbers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=lst
#      → 3.1 return round(x, 2)
#   4. def lst_numbers(lst) → stored in memory, not executed
#   5. def lst_numbers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return round(x, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()

"""

"""
Problem 696:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def celsius_to_fahrenheit(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * + /
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   celsius_to_fahrenheit(celsius_temps) → each argument is evaluated first, then the function body runs
#   celsius_to_fahrenheit(lst) → each argument is evaluated first, then the function body runs
#   celsius_to_fahrenheit(?) runs first → then print() outputs the result
#   ? + 32 → Python looks up ?, then 32, then applies +
#   celsius_to_fahrenheit(?) → each argument is evaluated first, then the function body runs
#   ? / 5 → Python looks up ?, then 5, then applies /
#   celsius * 9 → Python looks up celsius, then 9, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def celsius_to_fahrenheit(lst) → stored in memory, not executed
#   2. celsius_temps = ... → assigns value to variable
#   3. fahrenheit_temps = ... → assigns value to variable
#   4. print(fahrenheit_temps)
#   5. def celsius_to_fahrenheit(lst) → stored in memory, not executed
#   6. def celsius_to_fahrenheit(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(celsius_to_fahrenheit(...))
#      → calls celsius_to_fahrenheit(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return celsius * 9 / 5 + 32
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (celsius_to_fahrenheit) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     celsius_temps = ... → stored in memory
#     fahrenheit_temps = ... → stored in memory
#     print(fahrenheit_temps)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(celsius_to_fahrenheit(...)) → celsius_to_fahrenheit() runs first → value passed to print()

"""

"""
Problem 697:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def numbers_to_ascii(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: chr, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   numbers_to_ascii(numbers) → each argument is evaluated first, then the function body runs
#   numbers_to_ascii(lst) → each argument is evaluated first, then the function body runs
#   numbers_to_ascii(?) runs first → then print() outputs the result
#   numbers_to_ascii(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def numbers_to_ascii(lst) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. ascii_characters = ... → assigns value to variable
#   4. print(ascii_characters)
#   5. def numbers_to_ascii(lst) → stored in memory, not executed
#   6. def numbers_to_ascii(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(numbers_to_ascii(...))
#      → calls numbers_to_ascii(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return chr(number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (numbers_to_ascii) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     ascii_characters = ... → stored in memory
#     print(ascii_characters)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(numbers_to_ascii(...)) → numbers_to_ascii() runs first → value passed to print()

"""

"""
Problem 698:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: list, map, reversed()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#   char + result → Python looks up char, then result, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 3.1 result = ''
#      → 3.2 for char in word:
#      → 3.2     result = char + result
#      → 3.3 return result
#   4. def lst_words(lst) → stored in memory, not executed
#   5. def lst_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 result = ''
#      → 9.2 for char in word:
#      → 9.2     result = char + result
#      → 9.3 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done

"""

"""
Problem 700:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_integers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_integers(lst) runs first → then print() outputs the result
#   lst_integers(lst) → each argument is evaluated first, then the function body runs
#   lst_integers(?) runs first → then print() outputs the result
#   lst_integers(?) → each argument is evaluated first, then the function body runs
#   number * 2 → Python looks up number, then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_integers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_integers(...))
#      → calls lst_integers(lst) with lst=lst
#      → 3.1 return pow(number, 2)
#   4. def lst_integers(lst) → stored in memory, not executed
#   5. def lst_integers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_integers(...))
#      → calls lst_integers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(number, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 701:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 3.1 return word[0]
#   4. def lst_words(lst) → stored in memory, not executed
#   5. def lst_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return word[0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()

"""

"""
Problem 702:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: int, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 3.1 return int(word, 2)
#   4. def lst_words(lst) → stored in memory, not executed
#   5. def lst_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return int(word, 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()

"""

"""
Problem 703:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_mixed_case(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + <=
# Built-in functions: chr, list, map, ord, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_mixed_case(lst) runs first → then print() outputs the result
#   lst_mixed_case(lst) → each argument is evaluated first, then the function body runs
#   lst_mixed_case(?) runs first → then print() outputs the result
#   lst_mixed_case(?) → each argument is evaluated first, then the function body runs
#   'A' <= c → evaluated as True or False (determines which branch runs)
#   'A' <= 'Z' → evaluated as True or False (determines which branch runs)
#   ord(...) + 32 → Python looks up ord(...), then 32, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_mixed_case(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_mixed_case(...))
#      → calls lst_mixed_case(lst) with lst=lst
#      → 3.1 return str(string).lower()
#   4. def lst_mixed_case(lst) → stored in memory, not executed
#   5. def lst_mixed_case(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_mixed_case(...))
#      → calls lst_mixed_case(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return str(string).lower()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_mixed_case) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_mixed_case(...)) → lst_mixed_case() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_mixed_case(...)) → lst_mixed_case() runs first → value passed to print()

"""

"""
Problem 704:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_integers(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_integers(lst1, lst2) runs first → then print() outputs the result
#   lst_integers(lst1, lst2) → each argument is evaluated first, then the function body runs
#   lst_integers(?, ?) runs first → then print() outputs the result
#   lst_integers(?, ?) → each argument is evaluated first, then the function body runs
#   num1 * num2 → Python looks up num1, then num2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_integers(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(lst_integers(...))
#      → calls lst_integers(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return pow(num1, num2)
#   5. def lst_integers(lst1, lst2) → stored in memory, not executed
#   6. def lst_integers(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(lst_integers(...))
#      → calls lst_integers(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return pow(num1, num2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 705:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def euclidean_distances(points) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   euclidean_distances(points) runs first → then print() outputs the result
#   euclidean_distances(points) → each argument is evaluated first, then the function body runs
#   euclidean_distances(25) runs first → then print() outputs the result
#   euclidean_distances(25) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   ? ** 2 → Python looks up ?, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def euclidean_distances(points) → stored in memory, not executed
#   3. points = ... → assigns value to variable
#   4. print(euclidean_distances(...))
#      → calls euclidean_distances(points) with points=points
#      → 4.1 return math.sqrt(point[0] ** 2 + point[1] ** 2)
#   5. def euclidean_distances(points) → stored in memory, not executed
#   6. def euclidean_distances(points) → stored in memory, not executed
#   7. points = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(euclidean_distances(...))
#      → calls euclidean_distances(points) with points=25
#      → 10.1 return math.sqrt(point[0] ** 2 + point[1] ** 2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (euclidean_distances) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     points = ... → stored in memory
#     print(euclidean_distances(...)) → euclidean_distances() runs first → value passed to print()
#     points = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(euclidean_distances(...)) → euclidean_distances() runs first → value passed to print()

"""

"""
Problem 706:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_emails(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_emails(lst) runs first → then print() outputs the result
#   lst_emails(lst) → each argument is evaluated first, then the function body runs
#   lst_emails(?) runs first → then print() outputs the result
#   lst_emails(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_emails(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_emails(...))
#      → calls lst_emails(lst) with lst=lst
#      → 3.1 return string.split('@')[1]
#   4. def lst_emails(lst) → stored in memory, not executed
#   5. def lst_emails(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_emails(...))
#      → calls lst_emails(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return string.split('@')[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_emails) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_emails(...)) → lst_emails() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_emails(...)) → lst_emails() runs first → value passed to print()

"""

"""
Problem 707:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_integers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: % ==
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_integers(lst) runs first → then print() outputs the result
#   lst_integers(lst) → each argument is evaluated first, then the function body runs
#   lst_integers(?) runs first → then print() outputs the result
#   lst_integers(?) → each argument is evaluated first, then the function body runs
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_integers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_integers(...))
#      → calls lst_integers(lst) with lst=lst
#      → 3.1 return 'Even'
#   4. def lst_integers(lst) → stored in memory, not executed
#   5. def lst_integers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_integers(...))
#      → calls lst_integers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return 'Even'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_integers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_integers(...)) → lst_integers() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 708:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def negate_bool(value) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   negate_bool(value) → each argument is evaluated first, then the function body runs
#   negate_bool(25) runs first → then print() outputs the result
#   negate_bool(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def negate_bool(value) → stored in memory, not executed
#   2. bool_list = ... → assigns value to variable
#   3. negated_list = ... → assigns value to variable
#   4. print(negated_list)
#   5. def negate_bool(value) → stored in memory, not executed
#   6. def negate_bool(value) → stored in memory, not executed
#   7. value = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(negate_bool(...))
#      → calls negate_bool(value) with value=25
#      → 10.1 return not value
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (negate_bool) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     bool_list = ... → stored in memory
#     negated_list = ... → stored in memory
#     print(negated_list)
#     value = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(negate_bool(...)) → negate_bool() runs first → value passed to print()

"""

"""
Problem 709:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def negate_bool(bool_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   negate_bool(bool_list) → each argument is evaluated first, then the function body runs
#   negate_bool(?) runs first → then print() outputs the result
#   negate_bool(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def negate_bool(bool_list) → stored in memory, not executed
#   2. bool_list = ... → assigns value to variable
#   3. negate_bool(bool_list) → calls negate_bool(bool_list) with bool_list=bool_list
#      → 3.1 return not value
#   4. def negate_bool(value) → stored in memory, not executed
#   5. bool_list = ... → assigns value to variable
#   6. negated_list = ... → assigns value to variable
#   7. print(negated_list)
#   8. def negate_bool(bool_list) → stored in memory, not executed
#   9. def negate_bool(bool_list) → stored in memory, not executed
#   10. bool_list = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(negate_bool(...))
#      → calls negate_bool(bool_list) with bool_list=[4, 1, 8, 6, 3]
#      → 13.1 return not value
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (negate_bool) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     bool_list = ... → stored in memory
#     negate_bool(bool_list) → jumps into negate_bool(), runs body, returns
#     bool_list = ... → stored in memory
#     negated_list = ... → stored in memory
#     print(negated_list)
#     bool_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(negate_bool(...)) → negate_bool() runs first → value passed to print()

"""

"""
Problem 710:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def negate_booleans(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   negate_booleans(lst) runs first → then print() outputs the result
#   negate_booleans(lst) → each argument is evaluated first, then the function body runs
#   negate_booleans(?) runs first → then print() outputs the result
#   negate_booleans(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def negate_booleans(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(negate_booleans(...))
#      → calls negate_booleans(lst) with lst=lst
#      → 3.1 return not value
#   4. def negate_booleans(lst) → stored in memory, not executed
#   5. def negate_booleans(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(negate_booleans(...))
#      → calls negate_booleans(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return not value
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (negate_booleans) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(negate_booleans(...)) → negate_booleans() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(negate_booleans(...)) → negate_booleans() runs first → value passed to print()

"""

"""
Problem 712:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_sentence(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_sentence(lst) runs first → then print() outputs the result
#   lst_sentence(lst) → each argument is evaluated first, then the function body runs
#   lst_sentence(?) runs first → then print() outputs the result
#   lst_sentence(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_sentence(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_sentence(...))
#      → calls lst_sentence(lst) with lst=lst
#      → 3.1 return str(word).capitalize()
#   4. def lst_sentence(lst) → stored in memory, not executed
#   5. def lst_sentence(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_sentence(...))
#      → calls lst_sentence(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return str(word).capitalize()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_sentence) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_sentence(...)) → lst_sentence() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_sentence(...)) → lst_sentence() runs first → value passed to print()

"""

"""
Problem 713:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_sentence(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_sentence(lst) runs first → then print() outputs the result
#   lst_sentence(lst) → each argument is evaluated first, then the function body runs
#   lst_sentence(?) runs first → then print() outputs the result
#   lst_sentence(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_sentence(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_sentence(...))
#      → calls lst_sentence(lst) with lst=lst
#      → 3.1 return word.replace(' ', '')
#   4. def lst_sentence(lst) → stored in memory, not executed
#   5. def lst_sentence(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_sentence(...))
#      → calls lst_sentence(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return word.replace(' ', '')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_sentence) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_sentence(...)) → lst_sentence() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_sentence(...)) → lst_sentence() runs first → value passed to print()

"""

"""
Problem 714:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_of_tuples(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_of_tuples(lst) runs first → then print() outputs the result
#   lst_of_tuples(lst) → each argument is evaluated first, then the function body runs
#   lst_of_tuples(?) runs first → then print() outputs the result
#   lst_of_tuples(?) → each argument is evaluated first, then the function body runs
#   width * height → Python looks up width, then height, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_of_tuples(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_of_tuples(...))
#      → calls lst_of_tuples(lst) with lst=lst
#      → 3.1 return pow(width, height)
#   4. def lst_of_tuples(lst) → stored in memory, not executed
#   5. def lst_of_tuples(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_of_tuples(...))
#      → calls lst_of_tuples(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(width, height)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_of_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_of_tuples(...)) → lst_of_tuples() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_of_tuples(...)) → lst_of_tuples() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 715:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def extract_domain(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   extract_domain(urls) runs first → then print() outputs the result
#   extract_domain(lst) → each argument is evaluated first, then the function body runs
#   extract_domain(?) runs first → then print() outputs the result
#   extract_domain(urls) → each argument is evaluated first, then the function body runs
#   extract_domain(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def extract_domain(lst) → stored in memory, not executed
#   2. urls = ... → assigns value to variable
#   3. print(extract_domain(...))
#      → calls extract_domain(lst) with lst=urls
#      → 3.1 return url.split('//')[-1].split('/')[0]
#   4. def extract_domain(lst) → stored in memory, not executed
#   5. def extract_domain(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(extract_domain(...))
#      → calls extract_domain(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return url.split('//')[-1].split('/')[0]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (extract_domain) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     urls = ... → stored in memory
#     print(extract_domain(...)) → extract_domain() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(extract_domain(...)) → extract_domain() runs first → value passed to print()

"""

"""
Problem 716:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def pair_elements(numbers, strings) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   pair_elements(numbers, strings) runs first → then print() outputs the result
#   pair_elements(numbers, strings) → each argument is evaluated first, then the function body runs
#   pair_elements(?, 'world') runs first → then print() outputs the result
#   pair_elements(?, 'world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def pair_elements(numbers, strings) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. strings = ... → assigns value to variable
#   4. print(pair_elements(...))
#      → calls pair_elements(numbers, strings) with numbers=numbers, strings=strings
#      → 4.1 return (num, string)
#   5. def pair_elements(numbers, strings) → stored in memory, not executed
#   6. def pair_elements(numbers, strings) → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. strings = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(pair_elements(...))
#      → calls pair_elements(numbers, strings) with numbers=[4, 1, 8, 6, 3], strings='world'
#      → 11.1 return (num, string)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (pair_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     strings = ... → stored in memory
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()
#     numbers = ... → stored in memory
#     strings = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(pair_elements(...)) → pair_elements() runs first → value passed to print()

"""

"""
Problem 717:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lowercase_to_uppercase(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: - <=
# Built-in functions: chr, list, map, ord, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lowercase_to_uppercase(lst) runs first → then print() outputs the result
#   lowercase_to_uppercase(lst) → each argument is evaluated first, then the function body runs
#   lowercase_to_uppercase(?) runs first → then print() outputs the result
#   lowercase_to_uppercase(?) → each argument is evaluated first, then the function body runs
#   'a' <= c → evaluated as True or False (determines which branch runs)
#   'a' <= 'z' → evaluated as True or False (determines which branch runs)
#   ord(...) - 32 → Python looks up ord(...), then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lowercase_to_uppercase(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lowercase_to_uppercase(...))
#      → calls lowercase_to_uppercase(lst) with lst=lst
#      → 3.1 return str(letter).upper()
#   4. def lowercase_to_uppercase(lst) → stored in memory, not executed
#   5. def lowercase_to_uppercase(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lowercase_to_uppercase(...))
#      → calls lowercase_to_uppercase(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return str(letter).upper()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lowercase_to_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lowercase_to_uppercase(...)) → lowercase_to_uppercase() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lowercase_to_uppercase(...)) → lowercase_to_uppercase() runs first → value passed to print()

"""

"""
Problem 718:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def factorial_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: list, map, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   factorial_lst(lst) runs first → then print() outputs the result
#   factorial_lst(lst) → each argument is evaluated first, then the function body runs
#   factorial_lst(?) runs first → then print() outputs the result
#   factorial_lst(?) → each argument is evaluated first, then the function body runs
#   num + 1 → Python looks up num, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def factorial_lst(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(factorial_lst(...))
#      → calls factorial_lst(lst) with lst=lst
#      → 3.1 return result
#   4. import math
#   5. def factorial_lst(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. print(factorial_lst(...))
#      → calls factorial_lst(lst) with lst=lst
#      → 7.1 return result
#   8. def factorial_lst(lst) → stored in memory, not executed
#   9. def factorial_lst(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(factorial_lst(...))
#      → calls factorial_lst(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (factorial_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(factorial_lst(...)) → factorial_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(factorial_lst(...)) → factorial_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(factorial_lst(...)) → factorial_lst() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 719:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def reverse_words_in_sentences(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   reverse_words_in_sentences(lst) runs first → then print() outputs the result
#   reverse_words_in_sentences(lst) → each argument is evaluated first, then the function body runs
#   reverse_words_in_sentences(?) runs first → then print() outputs the result
#   reverse_words_in_sentences(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def reverse_words_in_sentences(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(reverse_words_in_sentences(...))
#      → calls reverse_words_in_sentences(lst) with lst=lst
#      → 3.1 return list(map(lambda word: word[::-1], words))
#   4. def reverse_words_in_sentences(lst) → stored in memory, not executed
#   5. def reverse_words_in_sentences(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(reverse_words_in_sentences(...))
#      → calls reverse_words_in_sentences(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return list(map(lambda word: word[::-1], words))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_words_in_sentences) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(reverse_words_in_sentences(...)) → reverse_words_in_sentences() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(reverse_words_in_sentences(...)) → reverse_words_in_sentences() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 720:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_strings(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_strings(lst) runs first → then print() outputs the result
#   lst_strings(lst) → each argument is evaluated first, then the function body runs
#   lst_strings(?) runs first → then print() outputs the result
#   lst_strings(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_strings(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_strings(...))
#      → calls lst_strings(lst) with lst=lst
#      → 3.1 return True
#   4. def lst_strings(lst) → stored in memory, not executed
#   5. def lst_strings(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_strings(...))
#      → calls lst_strings(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_strings) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_strings(...)) → lst_strings() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_strings(...)) → lst_strings() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 721:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_floats(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
# Built-in functions: list, map, pow, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_floats(lst) runs first → then print() outputs the result
#   square_floats(lst) → each argument is evaluated first, then the function body runs
#   square_floats(?) runs first → then print() outputs the result
#   square_floats(?) → each argument is evaluated first, then the function body runs
#   num * num → Python looks up num, then num, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_floats(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(square_floats(...))
#      → calls square_floats(lst) with lst=lst
#      → 3.1 return pow(num, num)
#   4. def square_floats(lst) → stored in memory, not executed
#   5. def square_floats(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(square_floats(...))
#      → calls square_floats(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return pow(num, num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_floats) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(square_floats(...)) → square_floats() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(square_floats(...)) → square_floats() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 722:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_phone_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_phone_numbers(lst) runs first → then print() outputs the result
#   format_phone_numbers(lst) → each argument is evaluated first, then the function body runs
#   format_phone_numbers(?) runs first → then print() outputs the result
#   '+1-%s' % number → Python looks up '+1-%s', then number, then applies %
#   format_phone_numbers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_phone_numbers(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(format_phone_numbers(...))
#      → calls format_phone_numbers(lst) with lst=lst
#      → 3.1 return '+1-%s' % number
#   4. def format_phone_numbers(lst) → stored in memory, not executed
#   5. def format_phone_numbers(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(format_phone_numbers(...))
#      → calls format_phone_numbers(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return '+1-%s' % number
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_phone_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(format_phone_numbers(...)) → format_phone_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_phone_numbers(...)) → format_phone_numbers() runs first → value passed to print()

"""

"""
Problem 723:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def absolute_values(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: abs, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   absolute_values(lst) runs first → then print() outputs the result
#   absolute_values(lst) → each argument is evaluated first, then the function body runs
#   absolute_values(?) runs first → then print() outputs the result
#   absolute_values(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def absolute_values(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(absolute_values(...))
#      → calls absolute_values(lst) with lst=lst
#      → 3.1 return abs(num)
#   4. def absolute_values(lst) → stored in memory, not executed
#   5. def absolute_values(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(absolute_values(...))
#      → calls absolute_values(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return abs(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (absolute_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(absolute_values(...)) → absolute_values() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(absolute_values(...)) → absolute_values() runs first → value passed to print()

"""

"""
Problem 724:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def pair_lists(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   pair_lists(lst1, lst2) runs first → then print() outputs the result
#   pair_lists(lst1, lst2) → each argument is evaluated first, then the function body runs
#   pair_lists(?, ?) runs first → then print() outputs the result
#   pair_lists(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def pair_lists(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(pair_lists(...))
#      → calls pair_lists(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return (num1, num2)
#   5. def pair_lists(lst1, lst2) → stored in memory, not executed
#   6. def pair_lists(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(pair_lists(...))
#      → calls pair_lists(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return (num1, num2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (pair_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(pair_lists(...)) → pair_lists() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(pair_lists(...)) → pair_lists() runs first → value passed to print()

"""

"""
Problem 725:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def cartesian_to_polar(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: + **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   cartesian_to_polar(coordinates) runs first → then print() outputs the result
#   cartesian_to_polar(lst) → each argument is evaluated first, then the function body runs
#   cartesian_to_polar(?) runs first → then print() outputs the result
#   cartesian_to_polar(coordinates) → each argument is evaluated first, then the function body runs
#   cartesian_to_polar(?) → each argument is evaluated first, then the function body runs
#   ? + ? → Python looks up ?, then ?, then applies +
#   x ** 2 → Python looks up x, then 2, then applies **
#   y ** 2 → Python looks up y, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def cartesian_to_polar(lst) → stored in memory, not executed
#   3. coordinates = ... → assigns value to variable
#   4. print(cartesian_to_polar(...))
#      → calls cartesian_to_polar(lst) with lst=coordinates
#      → 4.1 return (r, theta)
#   5. def cartesian_to_polar(lst) → stored in memory, not executed
#   6. def cartesian_to_polar(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(cartesian_to_polar(...))
#      → calls cartesian_to_polar(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return (r, theta)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (cartesian_to_polar) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     coordinates = ... → stored in memory
#     print(cartesian_to_polar(...)) → cartesian_to_polar() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(cartesian_to_polar(...)) → cartesian_to_polar() runs first → value passed to print()

"""

"""
Problem 726:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_cubes(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: list, map, pow()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_cubes(numbers) runs first → then print() outputs the result
#   lst_cubes(lst) → each argument is evaluated first, then the function body runs
#   lst_cubes(?) runs first → then print() outputs the result
#   lst_cubes(numbers) → each argument is evaluated first, then the function body runs
#   lst_cubes(?) → each argument is evaluated first, then the function body runs
#   num ** 3 → Python looks up num, then 3, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_cubes(lst) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. print(lst_cubes(...))
#      → calls lst_cubes(lst) with lst=numbers
#      → 3.1 import math
#      → 3.2 return math.pow(num, 3)
#   4. def lst_cubes(lst) → stored in memory, not executed
#   5. def lst_cubes(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_cubes(...))
#      → calls lst_cubes(lst) with lst=[1, 3, 5, 7]
#      → 9.1 import math
#      → 9.2 return math.pow(num, 3)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_cubes) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(lst_cubes(...)) → lst_cubes() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_cubes(...)) → lst_cubes() runs first → value passed to print()

"""

"""
Problem 727:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_squares_and_cubes(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_squares_and_cubes(lst) runs first → then print() outputs the result
#   lst_squares_and_cubes(lst) → each argument is evaluated first, then the function body runs
#   lst_squares_and_cubes(?) runs first → then print() outputs the result
#   lst_squares_and_cubes(?) → each argument is evaluated first, then the function body runs
#   num ** 2 → Python looks up num, then 2, then applies **
#   num ** 3 → Python looks up num, then 3, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_squares_and_cubes(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_squares_and_cubes(...))
#      → calls lst_squares_and_cubes(lst) with lst=lst
#      → 3.1 return (num ** 2, num ** 3)
#   4. def lst_squares_and_cubes(lst) → stored in memory, not executed
#   5. def lst_squares_and_cubes(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_squares_and_cubes(...))
#      → calls lst_squares_and_cubes(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return (num ** 2, num ** 3)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_squares_and_cubes) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_squares_and_cubes(...)) → lst_squares_and_cubes() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_squares_and_cubes(...)) → lst_squares_and_cubes() runs first → value passed to print()

"""

"""
Problem 728:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def rgb_to_hex(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   rgb_to_hex(lst) runs first → then print() outputs the result
#   rgb_to_hex(lst) → each argument is evaluated first, then the function body runs
#   rgb_to_hex(?) runs first → then print() outputs the result
#   rgb_to_hex(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def rgb_to_hex(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(rgb_to_hex(...))
#      → calls rgb_to_hex(lst) with lst=lst
#      → 3.1 return '#{:02x}{:02x}{:02x}'.format(rgb[0], rgb[1], rgb[2])
#   4. def rgb_to_hex(lst) → stored in memory, not executed
#   5. def rgb_to_hex(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(rgb_to_hex(...))
#      → calls rgb_to_hex(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return '#{:02x}{:02x}{:02x}'.format(rgb[0], rgb[1], rgb[2])
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (rgb_to_hex) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(rgb_to_hex(...)) → rgb_to_hex() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(rgb_to_hex(...)) → rgb_to_hex() runs first → value passed to print()

"""

"""
Problem 729:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def mixed_case_string(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   mixed_case_string(lst) runs first → then print() outputs the result
#   mixed_case_string(lst) → each argument is evaluated first, then the function body runs
#   mixed_case_string(?) runs first → then print() outputs the result
#   ? + ? → Python looks up ?, then ?, then applies +
#   mixed_case_string(?) → each argument is evaluated first, then the function body runs
#   ? + ?.upper(...) → Python looks up ?, then ?.upper(...), then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def mixed_case_string(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(mixed_case_string(...))
#      → calls mixed_case_string(lst) with lst=lst
#      → 3.1 return word[0] + word[1].upper() + word[2:]
#   4. def mixed_case_string(lst) → stored in memory, not executed
#   5. def mixed_case_string(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(mixed_case_string(...))
#      → calls mixed_case_string(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return word[0] + word[1].upper() + word[2:]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (mixed_case_string) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(mixed_case_string(...)) → mixed_case_string() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(mixed_case_string(...)) → mixed_case_string() runs first → value passed to print()

"""

"""
Problem 730:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def convert_dates(date_list) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   convert_dates(date_list) runs first → then print() outputs the result
#   convert_dates(date_list) → each argument is evaluated first, then the function body runs
#   convert_dates(?) runs first → then print() outputs the result
#   convert_dates(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from datetime import datetime
#   2. def convert_dates(date_list) → stored in memory, not executed
#   3. date_list = ... → assigns value to variable
#   4. print(convert_dates(...))
#      → calls convert_dates(date_list) with date_list=date_list
#      → 4.1 return datetime.strptime(date_string, '%Y-%m-%d')
#   5. def convert_dates(date_list) → stored in memory, not executed
#   6. def convert_dates(date_list) → stored in memory, not executed
#   7. date_list = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(convert_dates(...))
#      → calls convert_dates(date_list) with date_list=[4, 1, 8, 6, 3]
#      → 10.1 return datetime.strptime(date_string, '%Y-%m-%d')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (convert_dates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     date_list = ... → stored in memory
#     print(convert_dates(...)) → convert_dates() runs first → value passed to print()
#     date_list = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(convert_dates(...)) → convert_dates() runs first → value passed to print()

"""

"""
Problem 731:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_square_roots(numbers) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_square_roots(numbers) runs first → then print() outputs the result
#   calculate_square_roots(numbers) → each argument is evaluated first, then the function body runs
#   calculate_square_roots(?) runs first → then print() outputs the result
#   calculate_square_roots(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import math
#   2. def calculate_square_roots(numbers) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. print(calculate_square_roots(...))
#      → calls calculate_square_roots(numbers) with numbers=numbers
#      → 4.1 return math.sqrt(x)
#   5. def calculate_square_roots(numbers) → stored in memory, not executed
#   6. def calculate_square_roots(numbers) → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(calculate_square_roots(...))
#      → calls calculate_square_roots(numbers) with numbers=[4, 1, 8, 6, 3]
#      → 10.1 return math.sqrt(x)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_square_roots) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(calculate_square_roots(...)) → calculate_square_roots() runs first → value passed to print()
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_square_roots(...)) → calculate_square_roots() runs first → value passed to print()

"""

"""
Problem 732:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def calculate_percentage(scores, max_score) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   calculate_percentage(scores, max_score) runs first → then print() outputs the result
#   calculate_percentage(scores, max_score) → each argument is evaluated first, then the function body runs
#   calculate_percentage(25, 25) runs first → then print() outputs the result
#   ? * 100 → Python looks up ?, then 100, then applies *
#   calculate_percentage(25, 25) → each argument is evaluated first, then the function body runs
#   score / max_score → Python looks up score, then max_score, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def calculate_percentage(scores, max_score) → stored in memory, not executed
#   2. scores = ... → assigns value to variable
#   3. max_score = ... → assigns value to variable
#   4. print(calculate_percentage(...))
#      → calls calculate_percentage(scores, max_score) with scores=scores, max_score=max_score
#      → 4.1 return score / max_score * 100
#   5. def calculate_percentage(scores, max_score) → stored in memory, not executed
#   6. def calculate_percentage(scores, max_score) → stored in memory, not executed
#   7. scores = ... → assigns value to variable
#   8. max_score = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(calculate_percentage(...))
#      → calls calculate_percentage(scores, max_score) with scores=25, max_score=25
#      → 11.1 return score / max_score * 100
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (calculate_percentage) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     scores = ... → stored in memory
#     max_score = ... → stored in memory
#     print(calculate_percentage(...)) → calculate_percentage() runs first → value passed to print()
#     scores = ... → stored in memory
#     max_score = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(calculate_percentage(...)) → calculate_percentage() runs first → value passed to print()

"""

"""
Problem 733:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sum_list(numbers) → each argument is evaluated first, then the function body runs
#   add(a, b) → each argument is evaluated first, then the function body runs
#   add(20, 30) runs first → then print() outputs the result
#   a + b → Python looks up a, then b, then applies +
#   add(20, 30) → each argument is evaluated first, then the function body runs
#   add(total, number) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add(a, b) → stored in memory, not executed
#   2. def sum_list(numbers) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def add(a, b) → stored in memory, not executed
#   7. def add(a, b) → stored in memory, not executed
#   8. a = ... → assigns value to variable
#   9. b = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(add(...))
#      → calls add(a, b) with a=20, b=30
#      → 12.1 import operator
#      → 12.2 return operator.add(a, b)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sum_list, add) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add(...)) → add() runs first → value passed to print()
#
#   Loop: takes one item from numbers each round, runs body, repeats until done

"""

"""
Problem 734:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def is_even(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_even_numbers(numbers) → each argument is evaluated first, then the function body runs
#   is_even(n) → each argument is evaluated first, then the function body runs
#   is_even(15) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   is_even(15) → each argument is evaluated first, then the function body runs
#   n % 2 → Python looks up n, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def is_even(n) → stored in memory, not executed
#   2. def filter_even_numbers(numbers) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. even_numbers = ... → assigns value to variable
#   5. print(even_numbers)
#   6. def is_even(n) → stored in memory, not executed
#   7. def is_even(n) → stored in memory, not executed
#   8. n = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(is_even(...))
#      → calls is_even(n) with n=15
#      → 11.1 return n % 2 == 0
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (filter_even_numbers, is_even) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     even_numbers = ... → stored in memory
#     print(even_numbers)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(is_even(...)) → is_even() runs first → value passed to print()

"""

"""
Problem 735:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_first_letter(word) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   word_list(lst) runs first → then print() outputs the result
#   capitalize_first_letter(word) → each argument is evaluated first, then the function body runs
#   capitalize_first_letter('coding') runs first → then print() outputs the result
#   word_list(lst) → each argument is evaluated first, then the function body runs
#   capitalize_first_letter('coding') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_first_letter(word) → stored in memory, not executed
#   2. def word_list(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(word_list(...))
#      → calls word_list(lst) with lst=lst
#      → 4.1 return list(map(capitalize_first_letter, lst))
#   5. def word_list(lst) → stored in memory, not executed
#   6. def capitalize_first_letter(word) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. print(word_list(...))
#      → calls word_list(lst) with lst=lst
#      → 8.1 return list(map(capitalize_first_letter, lst))
#   9. def capitalize_first_letter(word) → stored in memory, not executed
#   10. def capitalize_first_letter(word) → stored in memory, not executed
#   11. word = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(capitalize_first_letter(...))
#      → calls capitalize_first_letter(word) with word='coding'
#      → 14.1 return str(word).capitalize()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (word_list, capitalize_first_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(word_list(...)) → word_list() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(word_list(...)) → word_list() runs first → value passed to print()
#     word = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_first_letter(...)) → capitalize_first_letter() runs first → value passed to print()

"""

"""
Problem 736:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_numbers(lst) runs first → then print() outputs the result
#   lst_numbers(lst) → each argument is evaluated first, then the function body runs
#   lst_numbers(?) runs first → then print() outputs the result
#   find_max(lst) → each argument is evaluated first, then the function body runs
#   lst_numbers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_numbers(lst) → stored in memory, not executed
#   2. def find_max(numbers) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=lst
#      → 4.1 return find_max(lst)
#   5. def lst_numbers(lst) → stored in memory, not executed
#   6. def lst_numbers(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return find_max(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_numbers, find_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()

"""

"""
Problem 737:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_tempertures(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: - /
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_tempertures(lst) runs first → then print() outputs the result
#   lst_tempertures(lst) → each argument is evaluated first, then the function body runs
#   lst_tempertures(?) runs first → then print() outputs the result
#   ? / 1.8 → Python looks up ?, then 1.8, then applies /
#   lst_tempertures(?) → each argument is evaluated first, then the function body runs
#   fahrenheit - 32 → Python looks up fahrenheit, then 32, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_tempertures(lst) → stored in memory, not executed
#   2. def convert_to_celsius(fahrenheit) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_tempertures(...))
#      → calls lst_tempertures(lst) with lst=lst
#      → 4.1 return list(map(convert_to_celsius, lst))
#   5. def lst_tempertures(lst) → stored in memory, not executed
#   6. def lst_tempertures(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_tempertures(...))
#      → calls lst_tempertures(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(convert_to_celsius, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_tempertures, convert_to_celsius) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_tempertures(...)) → lst_tempertures() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_tempertures(...)) → lst_tempertures() runs first → value passed to print()

"""

"""
Problem 738:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def string_lst(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   string_lst(lst) runs first → then print() outputs the result
#   string_lst(lst) → each argument is evaluated first, then the function body runs
#   string_lst(?) runs first → then print() outputs the result
#   string_lst(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def string_lst(lst) → stored in memory, not executed
#   2. def reverse_string(word) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(string_lst(...))
#      → calls string_lst(lst) with lst=lst
#      → 4.1 return list(map(reverse_string, lst))
#   5. def string_lst(lst) → stored in memory, not executed
#   6. def string_lst(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(string_lst(...))
#      → calls string_lst(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(reverse_string, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (reverse_string, string_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(string_lst(...)) → string_lst() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(string_lst(...)) → string_lst() runs first → value passed to print()

"""

"""
Problem 739:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst) runs first → then print() outputs the result
#   lst_words(lst) → each argument is evaluated first, then the function body runs
#   lst_words(?) runs first → then print() outputs the result
#   lst_words(?) → each argument is evaluated first, then the function body runs
#   char.lower(...) in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst) → stored in memory, not executed
#   2. def count_vowels(word) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_words(...))
#      → calls lst_words(lst) with lst=lst
#      → 4.1 return max(lst, key=count_vowels)
#   5. def lst_words(lst) → stored in memory, not executed
#   6. def lst_words(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_words(...))
#      → calls lst_words(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return max(lst, key=count_vowels)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words, count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()
#
#   Loop: takes one item from word each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 740:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_of_word(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_of_word(lst) runs first → then print() outputs the result
#   lst_of_word(lst) → each argument is evaluated first, then the function body runs
#   lst_of_word(?) runs first → then print() outputs the result
#   word == ? → evaluated as True or False (determines which branch runs)
#   lst_of_word(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_of_word(lst) → stored in memory, not executed
#   2. def is_palindrome(word) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_of_word(...))
#      → calls lst_of_word(lst) with lst=lst
#      → 4.1 return list(filter(is_palindrome, lst))
#   5. def lst_of_word(lst) → stored in memory, not executed
#   6. def lst_of_word(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_of_word(...))
#      → calls lst_of_word(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(filter(is_palindrome, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_palindrome, lst_of_word) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_of_word(...)) → lst_of_word() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_of_word(...)) → lst_of_word() runs first → value passed to print()

"""

"""
Problem 741:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_of_radii(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * **
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_of_radii(lst) runs first → then print() outputs the result
#   lst_of_radii(lst) → each argument is evaluated first, then the function body runs
#   lst_of_radii(?) runs first → then print() outputs the result
#   3.14159 * ? → Python looks up 3.14159, then ?, then applies *
#   lst_of_radii(?) → each argument is evaluated first, then the function body runs
#   radius ** 2 → Python looks up radius, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_of_radii(lst) → stored in memory, not executed
#   2. def calculate_area(radius) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_of_radii(...))
#      → calls lst_of_radii(lst) with lst=lst
#      → 4.1 return list(map(calculate_area, lst))
#   5. def lst_of_radii(lst) → stored in memory, not executed
#   6. def lst_of_radii(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_of_radii(...))
#      → calls lst_of_radii(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(calculate_area, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_of_radii, calculate_area) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_of_radii(...)) → lst_of_radii() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_of_radii(...)) → lst_of_radii() runs first → value passed to print()

"""

"""
Problem 742:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_numbers(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_numbers(lst) runs first → then print() outputs the result
#   lst_numbers(lst) → each argument is evaluated first, then the function body runs
#   lst_numbers(?) runs first → then print() outputs the result
#   num * num → Python looks up num, then num, then applies *
#   lst_numbers(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_numbers(lst) → stored in memory, not executed
#   2. def sqaure_number(num) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=lst
#      → 4.1 return list(map(sqaure_number, lst))
#   5. def lst_numbers(lst) → stored in memory, not executed
#   6. def lst_numbers(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_numbers(...))
#      → calls lst_numbers(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(sqaure_number, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_numbers, sqaure_number) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_numbers(...)) → lst_numbers() runs first → value passed to print()

"""

"""
Problem 743:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_first_n_elements(lst, num) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() runs first → then print() outputs the result
#   get_first_n_elements(lst, num) → each argument is evaluated first, then the function body runs
#   get_first_n_elements(?, 25) runs first → then print() outputs the result
#   main() → each argument is evaluated first, then the function body runs
#   get_first_n_elements(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_first_n_elements(lst, num) → stored in memory, not executed
#   2. def main() → stored in memory, not executed
#   3. print(main(...))
#      → calls main() with 
#      → 3.1 lst = [1, 2, 3, 4, 5, 6]
#      → 3.2 num = 3
#      → 3.3 return get_first_n_elements(lst, num)
#   4. def get_first_n_elements(lst, num) → stored in memory, not executed
#   5. def get_first_n_elements(lst, num) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. num = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(get_first_n_elements(...))
#      → calls get_first_n_elements(lst, num) with lst=[1, 3, 5, 7], num=25
#      → 10.1 return lst[:num]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_first_n_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     num = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_first_n_elements(...)) → get_first_n_elements() runs first → value passed to print()

"""

"""
Problem 744:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_dicts(dict1, dict2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   combine_dicts(dicts) runs first → then print() outputs the result
#   merge_dicts(dict1, dict2) → each argument is evaluated first, then the function body runs
#   merge_dicts(?, ?) runs first → then print() outputs the result
#   combine_dicts(dicts) → each argument is evaluated first, then the function body runs
#   merge_dicts(?, ?) → each argument is evaluated first, then the function body runs
#   merge_dicts(result, d) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_dicts(dict1, dict2) → stored in memory, not executed
#   2. def combine_dicts(dicts) → stored in memory, not executed
#   3. dicts = ... → assigns value to variable
#   4. print(combine_dicts(...))
#      → calls combine_dicts(dicts) with dicts=dicts
#      → 4.1 result = {}
#      → 4.2 for d in dicts:
#      → 4.2     result = merge_dicts(result, d)
#      → 4.3 return result
#   5. def merge_dicts(dict1, dict2) → stored in memory, not executed
#   6. def merge_dicts(dict1, dict2) → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(merge_dicts(...))
#      → calls merge_dicts(dict1, dict2) with dict1=?, dict2=?
#      → 11.1 return {**dict1, **dict2}
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (combine_dicts, merge_dicts) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dicts = ... → stored in memory
#     print(combine_dicts(...)) → combine_dicts() runs first → value passed to print()
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_dicts(...)) → merge_dicts() runs first → value passed to print()
#
#   Loop: takes one item from dicts each round, runs body, repeats until done

"""

"""
Problem 745:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def format_name(first_name, last_name) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   format_names(names) → each argument is evaluated first, then the function body runs
#   format_name(first_name, last_name) → each argument is evaluated first, then the function body runs
#   format_name('world', 'world') runs first → then print() outputs the result
#   '%s %s' % ? → Python looks up '%s %s', then ?, then applies %
#   format_name('world', 'world') → each argument is evaluated first, then the function body runs
#   format_name(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def format_name(first_name, last_name) → stored in memory, not executed
#   2. def format_names(lst) → stored in memory, not executed
#   3. names = ... → assigns value to variable
#   4. formatted_names = ... → assigns value to variable
#   5. print(formatted_names)
#   6. def format_name(first_name, last_name) → stored in memory, not executed
#   7. def format_name(first_name, last_name) → stored in memory, not executed
#   8. first_name = ... → assigns value to variable
#   9. last_name = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(format_name(...))
#      → calls format_name(first_name, last_name) with first_name='world', last_name='world'
#      → 12.1 return '%s %s' % (first_name, last_name)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (format_name, format_names) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     names = ... → stored in memory
#     formatted_names = ... → stored in memory
#     print(formatted_names)
#     first_name = ... → stored in memory
#     last_name = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(format_name(...)) → format_name() runs first → value passed to print()

"""

"""
Problem 746:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def find_gcd(a, b) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: while
# Operators: %
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   gcd_of_pairs(pairs) → each argument is evaluated first, then the function body runs
#   find_gcd(a, b) → each argument is evaluated first, then the function body runs
#   find_gcd(20, 30) runs first → then print() outputs the result
#   find_gcd(20, 30) → each argument is evaluated first, then the function body runs
#   find_gcd(?, ?) → each argument is evaluated first, then the function body runs
#   a % b → Python looks up a, then b, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def find_gcd(a, b) → stored in memory, not executed
#   2. def gcd_of_pairs(lst) → stored in memory, not executed
#   3. pairs = ... → assigns value to variable
#   4. gcd_results = ... → assigns value to variable
#   5. print(gcd_results)
#   6. def find_gcd(a, b) → stored in memory, not executed
#   7. def find_gcd(a, b) → stored in memory, not executed
#   8. a = ... → assigns value to variable
#   9. b = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(find_gcd(...))
#      → calls find_gcd(a, b) with a=20, b=30
#      → 12.1 return a
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (gcd_of_pairs, find_gcd) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pairs = ... → stored in memory
#     gcd_results = ... → stored in memory
#     print(gcd_results)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(find_gcd(...)) → find_gcd() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 747:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_elements(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_elements(lst) runs first → then print() outputs the result
#   lst_elements(lst) → each argument is evaluated first, then the function body runs
#   lst_elements(?) runs first → then print() outputs the result
#   count_occurrences(lst, element) → each argument is evaluated first, then the function body runs
#   lst_elements(?) → each argument is evaluated first, then the function body runs
#   item == element → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_elements(lst) → stored in memory, not executed
#   2. def count_occurrences(lst, element) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_elements(...))
#      → calls lst_elements(lst) with lst=lst
#      → 4.1 return count_occurrences(lst, element)
#   5. def lst_elements(lst) → stored in memory, not executed
#   6. def lst_elements(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_elements(...))
#      → calls lst_elements(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return count_occurrences(lst, element)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_occurrences, lst_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_elements(...)) → lst_elements() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_elements(...)) → lst_elements() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 748:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_words(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_words(lst1, lst2) → each argument is evaluated first, then the function body runs
#   lst_words(?, ?) runs first → then print() outputs the result
#   lst_words(?, ?) → each argument is evaluated first, then the function body runs
#   sort_lst(lst1) → each argument is evaluated first, then the function body runs
#   sort_lst(lst2) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_words(lst1, lst2) → stored in memory, not executed
#   2. def sort_lst(lst) → stored in memory, not executed
#   3. lst1 = ... → assigns value to variable
#   4. lst2 = ... → assigns value to variable
#   5. sorted_lists = ... → assigns value to variable
#   6. print(sorted_lists)
#   7. def lst_words(lst1, lst2) → stored in memory, not executed
#   8. def lst_words(lst1, lst2) → stored in memory, not executed
#   9. lst1 = ... → assigns value to variable
#   10. lst2 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(lst_words(...))
#      → calls lst_words(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 13.1 return (sort_lst(lst1), sort_lst(lst2))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_words, sort_lst) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     sorted_lists = ... → stored in memory
#     print(sorted_lists)
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_words(...)) → lst_words() runs first → value passed to print()

"""

"""
Problem 749:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def domain_names(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   domain_names(lst) runs first → then print() outputs the result
#   domain_names(lst) → each argument is evaluated first, then the function body runs
#   domain_names(?) runs first → then print() outputs the result
#   domain_names(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def domain_names(lst) → stored in memory, not executed
#   2. def extract_URL(url) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(domain_names(...))
#      → calls domain_names(lst) with lst=lst
#      → 4.1 return list(map(extract_URL, lst))
#   5. def domain_names(lst) → stored in memory, not executed
#   6. def domain_names(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(domain_names(...))
#      → calls domain_names(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(extract_URL, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (extract_URL, domain_names) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(domain_names(...)) → domain_names() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(domain_names(...)) → domain_names() runs first → value passed to print()

"""

"""
Problem 750:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_sites(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_sites(lst) runs first → then print() outputs the result
#   lst_sites(lst) → each argument is evaluated first, then the function body runs
#   lst_sites(?) runs first → then print() outputs the result
#   remove_duplicates(lst) → each argument is evaluated first, then the function body runs
#   lst_sites(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_sites(lst) → stored in memory, not executed
#   2. def remove_duplicates(lst) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(lst_sites(...))
#      → calls lst_sites(lst) with lst=lst
#      → 4.1 return remove_duplicates(lst)
#   5. def lst_sites(lst) → stored in memory, not executed
#   6. def lst_sites(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(lst_sites(...))
#      → calls lst_sites(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return remove_duplicates(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates, lst_sites) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_sites(...)) → lst_sites() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_sites(...)) → lst_sites() runs first → value passed to print()

"""

"""
Problem 751:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   lst_of_lists(nested_list) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   flatten_list(nested_list) → each argument is evaluated first, then the function body runs
#   lst_of_lists(nested_list) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(main(...))
#      → calls main(lst) with lst=lst
#      → 3.1 return list(map(lambda i: j, lst))
#   4. def lst_of_lists(nested_list) → stored in memory, not executed
#   5. def flatten_list(nested_list) → stored in memory, not executed
#   6. nested_list = ... → assigns value to variable
#   7. print(lst_of_lists(...))
#      → calls lst_of_lists(nested_list) with nested_list=nested_list
#      → 7.1 return flatten_list(nested_list)
#   8. def main(lst) → stored in memory, not executed
#   9. def main(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return list(map(lambda i: j, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, flatten_list, lst_of_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     nested_list = ... → stored in memory
#     print(lst_of_lists(...)) → lst_of_lists() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 752:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def filter_primes(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % + ** <= ==
# Built-in functions: filter, int, list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   filter_primes(numbers) runs first → then print() outputs the result
#   filter_primes(lst) → each argument is evaluated first, then the function body runs
#   filter_primes(?) runs first → then print() outputs the result
#   n <= 1 → evaluated as True or False (determines which branch runs)
#   filter_primes(numbers) → each argument is evaluated first, then the function body runs
#   filter_primes(?) → each argument is evaluated first, then the function body runs
#   int(...) + 1 → Python looks up int(...), then 1, then applies +
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n % i → Python looks up n, then i, then applies %
#   n ** 0.5 → Python looks up n, then 0.5, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def filter_primes(lst) → stored in memory, not executed
#   2. def is_prime(n) → stored in memory, not executed
#   3. numbers = ... → assigns value to variable
#   4. print(filter_primes(...))
#      → calls filter_primes(lst) with lst=numbers
#      → 4.1 return list(filter(is_prime, lst))
#   5. def filter_primes(lst) → stored in memory, not executed
#   6. def filter_primes(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(filter_primes(...))
#      → calls filter_primes(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(filter(is_prime, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_prime, filter_primes) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(filter_primes(...)) → filter_primes() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(filter_primes(...)) → filter_primes() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 753:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: list, map, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) → each argument is evaluated first, then the function body runs
#   lst_of_numbers(lst) runs first → then print() outputs the result
#   main(?) runs first → then print() outputs the result
#   lst_of_numbers(lst) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#   num + 1 → Python looks up num, then 1, then applies +
#   n + 1 → Python looks up n, then 1, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. main(lst) → calls main(lst) with lst=lst
#      → 3.1 return factorial
#   4. def lst_of_numbers(lst) → stored in memory, not executed
#   5. def calculate_factorial(num) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. print(lst_of_numbers(...))
#      → calls lst_of_numbers(lst) with lst=lst
#      → 7.1 return list(map(calculate_factorial, lst))
#   8. def main(lst) → stored in memory, not executed
#   9. def main(lst) → stored in memory, not executed
#   10. lst = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 13.1 return factorial
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_of_numbers, main, calculate_factorial) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     main(lst) → jumps into main(), runs body, returns
#     lst = ... → stored in memory
#     print(lst_of_numbers(...)) → lst_of_numbers() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 754:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: dict, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   main(25) runs first → then print() outputs the result
#   sort_dict_by_value(dic) → each argument is evaluated first, then the function body runs
#   main(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. def sort_dict_by_value(d) → stored in memory, not executed
#   3. def get_value(item) → stored in memory, not executed
#   4. dic = ... → assigns value to variable
#   5. print(main(...))
#      → calls main(dic) with dic=dic
#      → 5.1 return sort_dict_by_value(dic)
#   6. def main(dic) → stored in memory, not executed
#   7. def sort_dict_by_value(d) → stored in memory, not executed
#   8. dic = ... → assigns value to variable
#   9. print(main(...))
#      → calls main(dic) with dic=dic
#      → 9.1 return sort_dict_by_value(dic)
#   10. def main(dic) → stored in memory, not executed
#   11. def main(dic) → stored in memory, not executed
#   12. dic = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(main(...))
#      → calls main(dic) with dic=25
#      → 15.1 return sort_dict_by_value(dic)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, sort_dict_by_value, get_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from sorted_items each round, runs body, repeats until done

"""

"""
Problem 755:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, set()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   get_unique_elements(lst) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def get_unique_elements(word) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return get_unique_elements(lst)
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return get_unique_elements(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_unique_elements, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 756:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(str1, str2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(str1, str2) runs first → then print() outputs the result
#   main(str1, str2) → each argument is evaluated first, then the function body runs
#   main('world', 'python') runs first → then print() outputs the result
#   check_anagram(str1, str2) → each argument is evaluated first, then the function body runs
#   sorted(...) == sorted(...) → evaluated as True or False (determines which branch runs)
#   main('world', 'python') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(str1, str2) → stored in memory, not executed
#   2. def check_anagram(x, y) → stored in memory, not executed
#   3. str1 = ... → assigns value to variable
#   4. str2 = ... → assigns value to variable
#   5. print(main(...))
#      → calls main(str1, str2) with str1=str1, str2=str2
#      → 5.1 return check_anagram(str1, str2)
#   6. def main(str1, str2) → stored in memory, not executed
#   7. def main(str1, str2) → stored in memory, not executed
#   8. str1 = ... → assigns value to variable
#   9. str2 = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(str1, str2) with str1='world', str2='python'
#      → 12.1 return check_anagram(str1, str2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, check_anagram) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     str1 = ... → stored in memory
#     str2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 757:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst1, target1) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: enumerate()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst1, target1) runs first → then print() outputs the result
#   main(lst1, target1) → each argument is evaluated first, then the function body runs
#   main(?, 25) runs first → then print() outputs the result
#   find_index(lst, target1) → each argument is evaluated first, then the function body runs
#   main(?, 25) → each argument is evaluated first, then the function body runs
#   word == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst1, target1) → stored in memory, not executed
#   2. def find_index(lst, target) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. target1 = ... → assigns value to variable
#   5. print(main(...))
#      → calls main(lst1, target1) with lst1=lst1, target1=target1
#      → 5.1 return find_index(lst, target1)
#   6. def main(lst1, target1) → stored in memory, not executed
#   7. def main(lst1, target1) → stored in memory, not executed
#   8. lst1 = ... → assigns value to variable
#   9. target1 = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(lst1, target1) with lst1=[2, 4, 6], target1=25
#      → 12.1 return find_index(lst, target1)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, find_index) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     target1 = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     target1 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from enumerate(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 758:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(n) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: +
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(n) runs first → then print() outputs the result
#   main(n) → each argument is evaluated first, then the function body runs
#   main(15) runs first → then print() outputs the result
#   generate_fibonacci(n) → each argument is evaluated first, then the function body runs
#   main(15) → each argument is evaluated first, then the function body runs
#   a + b → Python looks up a, then b, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(n) → stored in memory, not executed
#   2. def generate_fibonacci(n) → stored in memory, not executed
#   3. n = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(n) with n=n
#      → 4.1 return generate_fibonacci(n)
#   5. def main(n) → stored in memory, not executed
#   6. def main(n) → stored in memory, not executed
#   7. n = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(n) with n=15
#      → 10.1 return generate_fibonacci(n)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, generate_fibonacci) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     n = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 759:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: /
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(list1, list2) runs first → then print() outputs the result
#   main(lst1, lst2) → each argument is evaluated first, then the function body runs
#   main(?, ?) runs first → then print() outputs the result
#   calculate_average(lst1) → each argument is evaluated first, then the function body runs
#   calculate_average(lst2) → each argument is evaluated first, then the function body runs
#   total / length_lst → Python looks up total, then length_lst, then applies /
#   main(list1, list2) → each argument is evaluated first, then the function body runs
#   main(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst1, lst2) → stored in memory, not executed
#   2. def calculate_average(lst) → stored in memory, not executed
#   3. list1 = ... → assigns value to variable
#   4. list2 = ... → assigns value to variable
#   5. print(main(...))
#      → calls main(lst1, lst2) with lst1=list1, lst2=list2
#      → 5.1 return (avg1, avg2)
#   6. def main(lst1, lst2) → stored in memory, not executed
#   7. def main(lst1, lst2) → stored in memory, not executed
#   8. lst1 = ... → assigns value to variable
#   9. lst2 = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 12.1 return (avg1, avg2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, calculate_average) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 760:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: *
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   miles * miles_to_km → Python looks up miles, then miles_to_km, then applies *
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def convert_to_kilometers(miles) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return list(map(convert_to_kilometers, lst))
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(convert_to_kilometers, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, convert_to_kilometers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 761:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst, n) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst, n) runs first → then print() outputs the result
#   main(lst, n) → each argument is evaluated first, then the function body runs
#   main(?, 15) runs first → then print() outputs the result
#   get_last_n_elements(lst, n) → each argument is evaluated first, then the function body runs
#   main(?, 15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst, n) → stored in memory, not executed
#   2. def get_last_n_elements(lst, n) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. n = ... → assigns value to variable
#   5. print(main(...))
#      → calls main(lst, n) with lst=lst, n=n
#      → 5.1 return get_last_n_elements(lst, n)
#   6. def main(lst, n) → stored in memory, not executed
#   7. def main(lst, n) → stored in memory, not executed
#   8. lst = ... → assigns value to variable
#   9. n = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(lst, n) with lst=[1, 3, 5, 7], n=15
#      → 12.1 return get_last_n_elements(lst, n)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_last_n_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     n = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 763:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: >
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   number > 0 → evaluated as True or False (determines which branch runs)
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def get_positive_numbers(number) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return list(filter(get_positive_numbers, lst))
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(filter(get_positive_numbers, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_positive_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 764:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_common_elements(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in and not in
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_common_elements(lst1, lst2) runs first → then print() outputs the result
#   main(lst1, lst2) runs first → then print() outputs the result
#   get_common_elements(lst1, lst2) → each argument is evaluated first, then the function body runs
#   get_common_elements(?, ?) runs first → then print() outputs the result
#   main(lst1, lst2) → each argument is evaluated first, then the function body runs
#   get_common_elements(?, ?) → each argument is evaluated first, then the function body runs
#   word in lst2 → evaluated as True or False (determines which branch runs)
#   item in y → evaluated as True or False (determines which branch runs)
#   item not in duplicates → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_common_elements(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(get_common_elements(...))
#      → calls get_common_elements(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return list(map(lambda word: word, lst1))
#   5. def main(lst1, lst2) → stored in memory, not executed
#   6. def get_common_elements(x, y) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. print(main(...))
#      → calls main(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 9.1 return get_common_elements(lst1, lst2)
#   10. def get_common_elements(lst1, lst2) → stored in memory, not executed
#   11. def get_common_elements(lst1, lst2) → stored in memory, not executed
#   12. lst1 = ... → assigns value to variable
#   13. lst2 = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(get_common_elements(...))
#      → calls get_common_elements(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 16.1 return list(map(lambda word: word, lst1))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_common_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(get_common_elements(...)) → get_common_elements() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_common_elements(...)) → get_common_elements() runs first → value passed to print()
#
#   Loop: takes one item from lst1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 765:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: % ==
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   main(?) → each argument is evaluated first, then the function body runs
#   n % 2 → Python looks up n, then 2, then applies %
#   check_even_odd(number) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def check_even_odd(n) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return list(map(check_even_odd, lst))
#   5. def main(lst) → stored in memory, not executed
#   6. def check_even_odd(n) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. print(main(...))
#      → calls main(lst) with lst=lst
#      → 8.1 return list(map(check_even_odd, lst))
#   9. def main(lst) → stored in memory, not executed
#   10. def main(lst) → stored in memory, not executed
#   11. lst = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 14.1 return list(map(check_even_odd, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_even_odd, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 766:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def convert_to_uppercase(word) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return list(map(convert_to_uppercase, lst))
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(convert_to_uppercase, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, convert_to_uppercase) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 767:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst_str) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: len, list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_characters(lst_str) runs first → then print() outputs the result
#   main(lst_str) runs first → then print() outputs the result
#   main(lst_str) → each argument is evaluated first, then the function body runs
#   main('world') runs first → then print() outputs the result
#   count_characters(lst_str) → each argument is evaluated first, then the function body runs
#   main('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst_str) → stored in memory, not executed
#   2. def count_characters(s) → stored in memory, not executed
#   3. lst_str = ... → assigns value to variable
#   4. print(count_characters(...))
#      → calls count_characters(s) with s=lst_str
#      → 4.1 return len(s)
#   5. def main(lst_str) → stored in memory, not executed
#   6. def count_characters(s) → stored in memory, not executed
#   7. lst_str = ... → assigns value to variable
#   8. print(main(...))
#      → calls main(lst_str) with lst_str=lst_str
#      → 8.1 return list(map(count_characters, lst_str))
#   9. def main(lst_str) → stored in memory, not executed
#   10. def main(lst_str) → stored in memory, not executed
#   11. lst_str = ... → assigns value to variable
#   12. result = ... → assigns value to variable
#   13. print(result)
#   14. print(main(...))
#      → calls main(lst_str) with lst_str='world'
#      → 14.1 return list(map(count_characters, lst_str))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, count_characters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst_str = ... → stored in memory
#     print(count_characters(...)) → count_characters() runs first → value passed to print()
#     lst_str = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst_str = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 768:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def remove_special_characters(s) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return list(map(remove_special_characters, lst))
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(map(remove_special_characters, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, remove_special_characters) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 769:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   s1 in s2 → evaluated as True or False (determines which branch runs)
#   main(?) → each argument is evaluated first, then the function body runs
#   is_substring(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def is_substring(s1, s2) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return results
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return results
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (is_substring, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 770:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   sum_of_list(lst) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def sum_of_list(n) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return sum_of_list(lst)
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return sum_of_list(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, sum_of_list) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 771:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: % ==
# Built-in functions: filter, list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst) runs first → then print() outputs the result
#   main(lst) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   main(?) → each argument is evaluated first, then the function body runs
#   n % 2 → Python looks up n, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst) → stored in memory, not executed
#   2. def get_even_numbers(n) → stored in memory, not executed
#   3. lst = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(lst) with lst=lst
#      → 4.1 return list(filter(get_even_numbers, lst))
#   5. def main(lst) → stored in memory, not executed
#   6. def main(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(main(...))
#      → calls main(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return list(filter(get_even_numbers, lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_even_numbers) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 772:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst1, lst2) runs first → then print() outputs the result
#   main(lst1, lst2) → each argument is evaluated first, then the function body runs
#   main(?, ?) runs first → then print() outputs the result
#   merge_sorted_lists(lst1, lst2) → each argument is evaluated first, then the function body runs
#   main(?, ?) → each argument is evaluated first, then the function body runs
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst1, lst2) → stored in memory, not executed
#   2. def merge_sorted_lists(x, y) → stored in memory, not executed
#   3. lst1 = ... → assigns value to variable
#   4. lst2 = ... → assigns value to variable
#   5. print(main(...))
#      → calls main(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 5.1 return merge_sorted_lists(lst1, lst2)
#   6. def main(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. main(lst1, lst2) → calls main(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 9.1 return merge_sorted_lists(lst1, lst2)
#   10. def main(lst1, lst2) → stored in memory, not executed
#   11. def main(lst1, lst2) → stored in memory, not executed
#   12. lst1 = ... → assigns value to variable
#   13. lst2 = ... → assigns value to variable
#   14. result = ... → assigns value to variable
#   15. print(result)
#   16. print(main(...))
#      → calls main(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 16.1 return merge_sorted_lists(lst1, lst2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, merge_sorted_lists) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     main(lst1, lst2) → jumps into main(), runs body, returns
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 773:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_to_dic(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, range, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_to_dic(lst1, lst2) runs first → then print() outputs the result
#   lst_to_dic(lst1, lst2) → each argument is evaluated first, then the function body runs
#   lst_to_dic(?, ?) runs first → then print() outputs the result
#   lst_to_dic(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_to_dic(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(lst_to_dic(...))
#      → calls lst_to_dic(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def lst_to_dic(lst1, lst2) → stored in memory, not executed
#   6. lst1 = ... → assigns value to variable
#   7. lst2 = ... → assigns value to variable
#   8. print(lst_to_dic(...))
#      → calls lst_to_dic(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 8.1 return result
#   9. def lst_to_dic(lst1, lst2) → stored in memory, not executed
#   10. def lst_to_dic(lst1, lst2) → stored in memory, not executed
#   11. lst1 = ... → assigns value to variable
#   12. lst2 = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(lst_to_dic(...))
#      → calls lst_to_dic(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 15.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_to_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(lst_to_dic(...)) → lst_to_dic() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(lst_to_dic(...)) → lst_to_dic() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_to_dic(...)) → lst_to_dic() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 774:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic, key_to_find) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic, key_to_find) runs first → then print() outputs the result
#   value_of_specific_key(dic, target) runs first → then print() outputs the result
#   main(dic, key_to_find) → each argument is evaluated first, then the function body runs
#   main(25, 25) runs first → then print() outputs the result
#   value_of_specific_key(dic, target) → each argument is evaluated first, then the function body runs
#   main(25, 25) → each argument is evaluated first, then the function body runs
#   key == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic, key_to_find) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. key_to_find = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(dic, key_to_find) with dic=dic, key_to_find=key_to_find
#      → 4.1 return dic.get(key_to_find, None)
#   5. key_to_find = ... → assigns value to variable
#   6. print(main(...))
#      → calls main(dic, key_to_find) with dic=dic, key_to_find=key_to_find
#      → 6.1 return dic.get(key_to_find, None)
#   7. def value_of_specific_key(dic, target) → stored in memory, not executed
#   8. dic = ... → assigns value to variable
#   9. target = ... → assigns value to variable
#   10. print(value_of_specific_key(...))
#      → calls value_of_specific_key(dic, target) with dic=dic, target=target
#      → 10.1 for key, value in dic.items():
#      → 10.1     if key == target:
#      → 10.1         return value
#      → 10.2 return None
#   11. def value_of_specific_key(dic, target) → stored in memory, not executed
#   12. dic = ... → assigns value to variable
#   13. target = ... → assigns value to variable
#   14. print(value_of_specific_key(...))
#      → calls value_of_specific_key(dic, target) with dic=dic, target=target
#      → 14.1 for key, value in dic.items():
#      → 14.1     if key == target:
#      → 14.1         return value
#      → 14.2 return None
#   15. def main(dic, key_to_find) → stored in memory, not executed
#   16. def main(dic, key_to_find) → stored in memory, not executed
#   17. dic = ... → assigns value to variable
#   18. key_to_find = ... → assigns value to variable
#   19. result = ... → assigns value to variable
#   20. print(result)
#   21. print(main(...))
#      → calls main(dic, key_to_find) with dic=25, key_to_find=25
#      → 21.1 return dic.get(key_to_find, None)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, value_of_specific_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     key_to_find = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     key_to_find = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     print(value_of_specific_key(...)) → value_of_specific_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     print(value_of_specific_key(...)) → value_of_specific_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     key_to_find = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 775:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic1, dic2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic1, dic2) runs first → then print() outputs the result
#   merge_dicts(dic1, dic2) runs first → then print() outputs the result
#   main(dic1, dic2) → each argument is evaluated first, then the function body runs
#   main(25, 25) runs first → then print() outputs the result
#   merge_dicts(dic1, dic2) → each argument is evaluated first, then the function body runs
#   main(25, 25) → each argument is evaluated first, then the function body runs
#   key in merge_dic → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic1, dic2) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. dic2 = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 4.1 return merge_dic
#   5. def merge_dicts(dic1, dic2) → stored in memory, not executed
#   6. dic1 = ... → assigns value to variable
#   7. dic2 = ... → assigns value to variable
#   8. print(merge_dicts(...))
#      → calls merge_dicts(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 8.1 merged_dict = {**dic1, **dic2}
#      → 8.2 return merged_dict
#   9. def main(dic1, dic2) → stored in memory, not executed
#   10. def main(dic1, dic2) → stored in memory, not executed
#   11. dic1 = ... → assigns value to variable
#   12. dic2 = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(main(...))
#      → calls main(dic1, dic2) with dic1=25, dic2=25
#      → 15.1 return merge_dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, merge_dicts) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(merge_dicts(...)) → merge_dicts() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic2.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 776:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_key(dic, key_to_remove) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: != in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_key(dic, key_to_remove) runs first → then print() outputs the result
#   main(dic, target) runs first → then print() outputs the result
#   remove_key(dic, key_to_remove) → each argument is evaluated first, then the function body runs
#   remove_key(?, 25) runs first → then print() outputs the result
#   key_to_remove in dic → evaluated as True or False (determines which branch runs)
#   main(dic, target) → each argument is evaluated first, then the function body runs
#   remove_key(?, 25) → each argument is evaluated first, then the function body runs
#   key != target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_key(dic, key_to_remove) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. key_to_remove = ... → assigns value to variable
#   4. print(remove_key(...))
#      → calls remove_key(dic, key_to_remove) with dic=dic, key_to_remove=key_to_remove
#      → 4.1 return dic
#   5. def main(dic, target) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. target = ... → assigns value to variable
#   8. print(main(...))
#      → calls main(dic, target) with dic=dic, target=target
#      → 8.1 result = {}
#      → 8.2 for key, value in dic.items():
#      → 8.2     if key != target:
#      → 8.2         result[key] = value
#      → 8.3 return result
#   9. def remove_key(dic, key_to_remove) → stored in memory, not executed
#   10. def remove_key(dic, key_to_remove) → stored in memory, not executed
#   11. dic = ... → assigns value to variable
#   12. key_to_remove = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(remove_key(...))
#      → calls remove_key(dic, key_to_remove) with dic=[4, 1, 8, 6, 3], key_to_remove=25
#      → 15.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, remove_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     key_to_remove = ... → stored in memory
#     print(remove_key(...)) → remove_key() runs first → value passed to print()
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     key_to_remove = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_key(...)) → remove_key() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 778:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic1) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic1) runs first → then print() outputs the result
#   does_key_exist(?, 'job') runs first → then print() outputs the result
#   does_key_exist(?, 'name') runs first → then print() outputs the result
#   main(dic1) → each argument is evaluated first, then the function body runs
#   main(25) runs first → then print() outputs the result
#   target in dic1.keys(...) → evaluated as True or False (determines which branch runs)
#   does_key_exist(?, 'job') → each argument is evaluated first, then the function body runs
#   does_key_exist(?, 'name') → each argument is evaluated first, then the function body runs
#   main(25) → each argument is evaluated first, then the function body runs
#   key == target → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic1) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(dic1) with dic1=dic1
#      → 4.1 return True
#   5. def does_key_exist(dic, target) → stored in memory, not executed
#   6. print(does_key_exist(...))
#      → calls does_key_exist(dic, target) with dic=?, target='job'
#      → 6.1 for key in dic.keys():
#      → 6.1     if key == target:
#      → 6.1         return 'key exists'
#      → 6.2 return 'key does not exist'
#   7. print(does_key_exist(...))
#      → calls does_key_exist(dic, target) with dic=?, target='name'
#      → 7.1 for key in dic.keys():
#      → 7.1     if key == target:
#      → 7.1         return 'key exists'
#      → 7.2 return 'key does not exist'
#   8. def main(dic1) → stored in memory, not executed
#   9. def main(dic1) → stored in memory, not executed
#   10. dic1 = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(dic1) with dic1=25
#      → 13.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, does_key_exist) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     target = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     print(does_key_exist(...)) → does_key_exist() runs first → value passed to print()
#     print(does_key_exist(...)) → does_key_exist() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 779:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_dic(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: dict, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_dic(dic) runs first → then print() outputs the result
#   sort_dic(dic) → each argument is evaluated first, then the function body runs
#   sort_dic(?) runs first → then print() outputs the result
#   sort_dic(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_dic(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(sort_dic(...))
#      → calls sort_dic(dic) with dic=dic
#      → 3.1 return dict(sorted(dic.items()))
#   4. def sort_dic(dic) → stored in memory, not executed
#   5. def sort_dic(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_dic(...))
#      → calls sort_dic(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return dict(sorted(dic.items()))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(sort_dic(...)) → sort_dic() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_dic(...)) → sort_dic() runs first → value passed to print()

"""

"""
Problem 780:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: dict, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) runs first → then print() outputs the result
#   sort_values(dic) runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   sort_values(dic) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(main(...))
#      → calls main(dic) with dic=dic
#      → 3.1 return item[1]
#   4. def sort_values(dic) → stored in memory, not executed
#   5. def sort_key(item) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. print(sort_values(...))
#      → calls sort_values(dic) with dic=dic
#      → 7.1 sorted_items = sorted(dic.items(), key=sort_key)
#      → 7.2 return dict(sorted_items)
#   8. def main(dic) → stored in memory, not executed
#   9. def main(dic) → stored in memory, not executed
#   10. dic = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(dic) with dic=[4, 1, 8, 6, 3]
#      → 13.1 return item[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, sort_values, sort_key) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(sort_values(...)) → sort_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 781:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def frequency_elements(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   frequency_elements(lst) runs first → then print() outputs the result
#   frequency_elements(lst) → each argument is evaluated first, then the function body runs
#   frequency_elements(?) runs first → then print() outputs the result
#   frequency_elements(?) → each argument is evaluated first, then the function body runs
#   word in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def frequency_elements(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(frequency_elements(...))
#      → calls frequency_elements(lst) with lst=lst
#      → 3.1 return result
#   4. def frequency_elements(lst) → stored in memory, not executed
#   5. def frequency_elements(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(frequency_elements(...))
#      → calls frequency_elements(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (frequency_elements) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(frequency_elements(...)) → frequency_elements() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(frequency_elements(...)) → frequency_elements() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 782:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def square_dic() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: **
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   square_dic() runs first → then print() outputs the result
#   square_dic() → each argument is evaluated first, then the function body runs
#   key ** 2 → Python looks up key, then 2, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def square_dic() → stored in memory, not executed
#   2. print(square_dic(...))
#      → calls square_dic() with 
#      → 2.1 return result
#   3. def square_dic() → stored in memory, not executed
#   4. def square_dic() → stored in memory, not executed
#   5. result = ... → assigns value to variable
#   6. for key in range(...) → loops over each item
#      → 6.1 ? = ...
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. result = ... → assigns value to variable
#   10. for key in range(...) → loops over each item
#      → 10.1 ? = ...
#   11. result = ... → assigns value to variable
#   12. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (square_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(square_dic(...)) → square_dic() runs first → value passed to print()
#     result = ... → stored in memory
#     for key in range(...) → runs body once per item
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     for key in range(...) → runs body once per item
#     result = ... → stored in memory
#     print(result)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 783:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic, target) runs first → then print() outputs the result
#   update_value(dic, 'age', 60) → each argument is evaluated first, then the function body runs
#   main(dic, target) → each argument is evaluated first, then the function body runs
#   main(?, 'goal') runs first → then print() outputs the result
#   key_to_update in dic → evaluated as True or False (determines which branch runs)
#   key in dic → evaluated as True or False (determines which branch runs)
#   main(?, 'goal') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic, target) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(dic, target) with dic=dic, target=target
#      → 4.1 return dic
#   5. def update_value(dic, key, new_value) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. update_value(dic, 'age', 60) → calls update_value(dic, key, new_value) with dic=dic, key='age', new_value=60
#      → 7.1 if key in dic:
#      → 7.1     dic[key] = new_value
#      → 7.2 return dic
#   8. print(dic)
#   9. def main(dic, target) → stored in memory, not executed
#   10. def main(dic, target) → stored in memory, not executed
#   11. dic = ... → assigns value to variable
#   12. target = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(main(...))
#      → calls main(dic, target) with dic=[4, 1, 8, 6, 3], target='goal'
#      → 15.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, update_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     update_value(dic, 'age', 60) → jumps into update_value(), runs body, returns
#     print(dic)
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 784:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_keys(dic) runs first → then print() outputs the result
#   main(dic) runs first → then print() outputs the result
#   get_keys(dic) → each argument is evaluated first, then the function body runs
#   get_keys(25) runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   get_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(get_keys(...))
#      → calls get_keys(dic) with dic=dic
#      → 3.1 return dic.keys()
#   4. def main(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. print(main(...))
#      → calls main(dic) with dic=dic
#      → 6.1 for key in dic:
#      → 6.1     print(key)
#   7. def get_keys(dic) → stored in memory, not executed
#   8. def get_keys(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(get_keys(...))
#      → calls get_keys(dic) with dic=25
#      → 12.1 return dic.keys()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, get_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(get_keys(...)) → get_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_keys(...)) → get_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic each round, runs body, repeats until done

"""

"""
Problem 785:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_keys(dic) runs first → then print() outputs the result
#   get_keys(dic) → each argument is evaluated first, then the function body runs
#   get_keys(25) runs first → then print() outputs the result
#   get_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(get_keys(...))
#      → calls get_keys(dic) with dic=dic
#      → 3.1 return dic.values()
#   4. def get_keys(dic) → stored in memory, not executed
#   5. def get_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_keys(...))
#      → calls get_keys(dic) with dic=25
#      → 9.1 return dic.values()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(get_keys(...)) → get_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_keys(...)) → get_keys() runs first → value passed to print()

"""

"""
Problem 786:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_keys(dic) runs first → then print() outputs the result
#   get_keys(dic) → each argument is evaluated first, then the function body runs
#   get_keys(25) runs first → then print() outputs the result
#   get_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(get_keys(...))
#      → calls get_keys(dic) with dic=dic
#      → 3.1 return dic.values()
#   4. def get_keys(dic) → stored in memory, not executed
#   5. def get_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(get_keys(...))
#      → calls get_keys(dic) with dic=25
#      → 9.1 return dic.values()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(get_keys(...)) → get_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(get_keys(...)) → get_keys() runs first → value passed to print()

"""

"""
Problem 787:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_value(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_value(dic) runs first → then print() outputs the result
#   largest_value(dic) → each argument is evaluated first, then the function body runs
#   largest_value(?) runs first → then print() outputs the result
#   largest_value(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_value(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(largest_value(...))
#      → calls largest_value(dic) with dic=dic
#      → 3.1 return max(dic.values())
#   4. def largest_value(dic) → stored in memory, not executed
#   5. def largest_value(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(largest_value(...))
#      → calls largest_value(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return max(dic.values())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(largest_value(...)) → largest_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_value(...)) → largest_value() runs first → value passed to print()

"""

"""
Problem 788:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_value(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_value(dic) runs first → then print() outputs the result
#   largest_value(dic) → each argument is evaluated first, then the function body runs
#   largest_value(?) runs first → then print() outputs the result
#   largest_value(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_value(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(largest_value(...))
#      → calls largest_value(dic) with dic=dic
#      → 3.1 return min(dic.values())
#   4. def largest_value(dic) → stored in memory, not executed
#   5. def largest_value(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(largest_value(...))
#      → calls largest_value(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return min(dic.values())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(largest_value(...)) → largest_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_value(...)) → largest_value() runs first → value passed to print()

"""

"""
Problem 789:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_dic(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, min, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_dic(lst1, lst2) runs first → then print() outputs the result
#   lst_dic(lst1, lst2) → each argument is evaluated first, then the function body runs
#   lst_dic(?, ?) runs first → then print() outputs the result
#   lst_dic(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_dic(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. print(lst_dic(...))
#      → calls lst_dic(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def lst_dic(lst1, lst2) → stored in memory, not executed
#   6. def lst_dic(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(lst_dic(...))
#      → calls lst_dic(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     print(lst_dic(...)) → lst_dic() runs first → value passed to print()
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_dic(...)) → lst_dic() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 790:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def combine_dictionaries(dic1, dic2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   combine_dictionaries(dic1, dic2) runs first → then print() outputs the result
#   combine_dictionaries(dic1, dic2) → each argument is evaluated first, then the function body runs
#   combine_dictionaries(?, 25) runs first → then print() outputs the result
#   combine_dictionaries(?, 25) → each argument is evaluated first, then the function body runs
#   key in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def combine_dictionaries(dic1, dic2) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. dic2 = ... → assigns value to variable
#   4. print(combine_dictionaries(...))
#      → calls combine_dictionaries(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 4.1 return result
#   5. def combine_dictionaries(dic1, dic2) → stored in memory, not executed
#   6. dic1 = ... → assigns value to variable
#   7. dic2 = ... → assigns value to variable
#   8. print(combine_dictionaries(...))
#      → calls combine_dictionaries(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 8.1 return result
#   9. def combine_dictionaries(dic1, dic2) → stored in memory, not executed
#   10. def combine_dictionaries(dic1, dic2) → stored in memory, not executed
#   11. dic1 = ... → assigns value to variable
#   12. dic2 = ... → assigns value to variable
#   13. result = ... → assigns value to variable
#   14. print(result)
#   15. print(combine_dictionaries(...))
#      → calls combine_dictionaries(dic1, dic2) with dic1=[4, 1, 8, 6, 3], dic2=25
#      → 15.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (combine_dictionaries) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(combine_dictionaries(...)) → combine_dictionaries() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(combine_dictionaries(...)) → combine_dictionaries() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(combine_dictionaries(...)) → combine_dictionaries() runs first → value passed to print()
#
#   Loop: takes one item from dic1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 791:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def invert_dictionary(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   invert_dictionary(dic) runs first → then print() outputs the result
#   invert_dictionary(dic) → each argument is evaluated first, then the function body runs
#   invert_dictionary(25) runs first → then print() outputs the result
#   invert_dictionary(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def invert_dictionary(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(invert_dictionary(...))
#      → calls invert_dictionary(dic) with dic=dic
#      → 3.1 return inverted
#   4. def invert_dictionary(dic) → stored in memory, not executed
#   5. def invert_dictionary(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(invert_dictionary(...))
#      → calls invert_dictionary(dic) with dic=25
#      → 9.1 return inverted
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (invert_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(invert_dictionary(...)) → invert_dictionary() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(invert_dictionary(...)) → invert_dictionary() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done

"""

"""
Problem 792:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_duplicates_values(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_duplicates_values(dic) runs first → then print() outputs the result
#   remove_duplicates_values(dic) → each argument is evaluated first, then the function body runs
#   remove_duplicates_values(25) runs first → then print() outputs the result
#   remove_duplicates_values(25) → each argument is evaluated first, then the function body runs
#   value not in unique_values.values(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_duplicates_values(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(remove_duplicates_values(...))
#      → calls remove_duplicates_values(dic) with dic=dic
#      → 3.1 return unique_values
#   4. def remove_duplicates_values(dic) → stored in memory, not executed
#   5. def remove_duplicates_values(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_duplicates_values(...))
#      → calls remove_duplicates_values(dic) with dic=25
#      → 9.1 return unique_values
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_duplicates_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(remove_duplicates_values(...)) → remove_duplicates_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_duplicates_values(...)) → remove_duplicates_values() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 793:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def same_keys(dic1, dic2) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   same_keys(dic1, dic2) runs first → then print() outputs the result
#   same_keys(dic1, dic2) → each argument is evaluated first, then the function body runs
#   same_keys(25, 25) runs first → then print() outputs the result
#   dic1.keys(...) == dic2.keys(...) → evaluated as True or False (determines which branch runs)
#   same_keys(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def same_keys(dic1, dic2) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. dic2 = ... → assigns value to variable
#   4. print(same_keys(...))
#      → calls same_keys(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 4.1 return dic1.keys() == dic2.keys()
#   5. def same_keys(dic1, dic2) → stored in memory, not executed
#   6. def same_keys(dic1, dic2) → stored in memory, not executed
#   7. dic1 = ... → assigns value to variable
#   8. dic2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(same_keys(...))
#      → calls same_keys(dic1, dic2) with dic1=25, dic2=25
#      → 11.1 return dic1.keys() == dic2.keys()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (same_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(same_keys(...)) → same_keys() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(same_keys(...)) → same_keys() runs first → value passed to print()

"""

"""
Problem 794:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def multiply_by2(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: *
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   multiply_by2(dic) runs first → then print() outputs the result
#   multiply_by2(dic) → each argument is evaluated first, then the function body runs
#   multiply_by2(25) runs first → then print() outputs the result
#   multiply_by2(25) → each argument is evaluated first, then the function body runs
#   value * 2 → Python looks up value, then 2, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def multiply_by2(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(multiply_by2(...))
#      → calls multiply_by2(dic) with dic=dic
#      → 3.1 return result
#   4. def multiply_by2(dic) → stored in memory, not executed
#   5. def multiply_by2(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(multiply_by2(...))
#      → calls multiply_by2(dic) with dic=25
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (multiply_by2) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(multiply_by2(...)) → multiply_by2() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(multiply_by2(...)) → multiply_by2() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done

"""

"""
Problem 795:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def len_key_value_pair(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   len_key_value_pair(dic) runs first → then print() outputs the result
#   len_key_value_pair(dic) → each argument is evaluated first, then the function body runs
#   len_key_value_pair(25) runs first → then print() outputs the result
#   len_key_value_pair(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def len_key_value_pair(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(len_key_value_pair(...))
#      → calls len_key_value_pair(dic) with dic=dic
#      → 3.1 return sum((1 for _ in dic))
#   4. def len_key_value_pair(dic) → stored in memory, not executed
#   5. def len_key_value_pair(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(len_key_value_pair(...))
#      → calls len_key_value_pair(dic) with dic=25
#      → 9.1 return sum((1 for _ in dic))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (len_key_value_pair) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(len_key_value_pair(...)) → len_key_value_pair() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(len_key_value_pair(...)) → len_key_value_pair() runs first → value passed to print()
#
#   Loop: takes one item from dic each round, runs body, repeats until done

"""

"""
Problem 796:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def map_lst_string_dic(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   map_lst_string_dic(lst) runs first → then print() outputs the result
#   len_dic(lst) runs first → then print() outputs the result
#   map_lst_string_dic(lst) → each argument is evaluated first, then the function body runs
#   map_lst_string_dic(?) runs first → then print() outputs the result
#   len_dic(lst) → each argument is evaluated first, then the function body runs
#   map_lst_string_dic(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def map_lst_string_dic(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(map_lst_string_dic(...))
#      → calls map_lst_string_dic(lst) with lst=lst
#      → 3.1 return result
#   4. def len_dic(lst) → stored in memory, not executed
#   5. lst = ... → assigns value to variable
#   6. print(len_dic(...))
#      → calls len_dic(lst) with lst=lst
#      → 6.1 result = {}
#      → 6.2 for word in lst:
#      → 6.2     count = 0
#      → 6.2     for char in word:
#      → 6.2         count += 1
#      → 6.2     result[word] = count
#      → 6.3 return result
#   7. def map_lst_string_dic(lst) → stored in memory, not executed
#   8. def map_lst_string_dic(lst) → stored in memory, not executed
#   9. lst = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(map_lst_string_dic(...))
#      → calls map_lst_string_dic(lst) with lst=[1, 3, 5, 7]
#      → 12.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (map_lst_string_dic, len_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(map_lst_string_dic(...)) → map_lst_string_dic() runs first → value passed to print()
#     lst = ... → stored in memory
#     print(len_dic(...)) → len_dic() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(map_lst_string_dic(...)) → map_lst_string_dic() runs first → value passed to print()
#
#   Loop: takes one item from lst each round, runs body, repeats until done

"""

"""
Problem 797:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add_key_value_pair(dic) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add_key_value_pair(dic) runs first → then print() outputs the result
#   add_key_value_pair(dic) → each argument is evaluated first, then the function body runs
#   add_key_value_pair(?) runs first → then print() outputs the result
#   add_key_value_pair(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add_key_value_pair(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(add_key_value_pair(...))
#      → calls add_key_value_pair(dic) with dic=dic
#      → 3.1 return dic
#   4. def add_key_value_pair(dic) → stored in memory, not executed
#   5. def add_key_value_pair(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(add_key_value_pair(...))
#      → calls add_key_value_pair(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add_key_value_pair) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(add_key_value_pair(...)) → add_key_value_pair() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add_key_value_pair(...)) → add_key_value_pair() runs first → value passed to print()

"""

"""
Problem 798:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def value_cubes() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: **
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   value_cubes() runs first → then print() outputs the result
#   value_cubes() → each argument is evaluated first, then the function body runs
#   num ** 3 → Python looks up num, then 3, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def value_cubes() → stored in memory, not executed
#   2. print(value_cubes(...))
#      → calls value_cubes() with 
#      → 2.1 return result
#   3. def value_cubes() → stored in memory, not executed
#   4. def value_cubes() → stored in memory, not executed
#   5. result = ... → assigns value to variable
#   6. for num in range(...) → loops over each item
#      → 6.1 ? = ...
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. result = ... → assigns value to variable
#   10. for num in range(...) → loops over each item
#      → 10.1 ? = ...
#   11. result = ... → assigns value to variable
#   12. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (value_cubes) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(value_cubes(...)) → value_cubes() runs first → value passed to print()
#     result = ... → stored in memory
#     for num in range(...) → runs body once per item
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     for num in range(...) → runs body once per item
#     result = ... → stored in memory
#     print(result)
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 799:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_if_empty(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: if/else
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_if_empty(dic) runs first → then print() outputs the result
#   check_if_empty(dic) → each argument is evaluated first, then the function body runs
#   check_if_empty(25) runs first → then print() outputs the result
#   check_if_empty(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_if_empty(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(check_if_empty(...))
#      → calls check_if_empty(dic) with dic=dic
#      → 3.1 return 'Dictionary is empty'
#   4. def check_if_empty(dic) → stored in memory, not executed
#   5. def check_if_empty(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_if_empty(...))
#      → calls check_if_empty(dic) with dic=25
#      → 9.1 return 'Dictionary is empty'
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_if_empty) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(check_if_empty(...)) → check_if_empty() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_if_empty(...)) → check_if_empty() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 800:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_set_keys(dic, keys_to_remove) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_set_keys(dic, keys_to_remove) runs first → then print() outputs the result
#   remove_set_keys(dic, keys_to_remove) → each argument is evaluated first, then the function body runs
#   remove_set_keys(25, 25) runs first → then print() outputs the result
#   remove_set_keys(25, 25) → each argument is evaluated first, then the function body runs
#   key not in keys_to_remove → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_set_keys(dic, keys_to_remove) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. keys_to_remove = ... → assigns value to variable
#   4. print(remove_set_keys(...))
#      → calls remove_set_keys(dic, keys_to_remove) with dic=dic, keys_to_remove=keys_to_remove
#      → 4.1 return result
#   5. def remove_set_keys(dic, keys_to_remove) → stored in memory, not executed
#   6. def remove_set_keys(dic, keys_to_remove) → stored in memory, not executed
#   7. dic = ... → assigns value to variable
#   8. keys_to_remove = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(remove_set_keys(...))
#      → calls remove_set_keys(dic, keys_to_remove) with dic=25, keys_to_remove=25
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_set_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     keys_to_remove = ... → stored in memory
#     print(remove_set_keys(...)) → remove_set_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     keys_to_remove = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_set_keys(...)) → remove_set_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 801:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def dic_lst_tuples(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   dic_lst_tuples(dic) runs first → then print() outputs the result
#   dic_lst_tuples(dic) → each argument is evaluated first, then the function body runs
#   dic_lst_tuples(25) runs first → then print() outputs the result
#   dic_lst_tuples(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def dic_lst_tuples(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(dic_lst_tuples(...))
#      → calls dic_lst_tuples(dic) with dic=dic
#      → 3.1 return list(dic.items())
#   4. def dic_lst_tuples(dic) → stored in memory, not executed
#   5. def dic_lst_tuples(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(dic_lst_tuples(...))
#      → calls dic_lst_tuples(dic) with dic=25
#      → 9.1 return list(dic.items())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (dic_lst_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(dic_lst_tuples(...)) → dic_lst_tuples() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(dic_lst_tuples(...)) → dic_lst_tuples() runs first → value passed to print()

"""

"""
Problem 802:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: ==
# Built-in functions: tuple()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) runs first → then print() outputs the result
#   map_value(dic, target_value) runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   main(25) runs first → then print() outputs the result
#   map_value(dic, target_value) → each argument is evaluated first, then the function body runs
#   main(25) → each argument is evaluated first, then the function body runs
#   value == 1 → evaluated as True or False (determines which branch runs)
#   value == target_value → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(main(...))
#      → calls main(dic) with dic=dic
#      → 3.1 return tuple(result.keys())
#   4. def map_value(dic, target_value) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. target_value = ... → assigns value to variable
#   7. print(map_value(...))
#      → calls map_value(dic, target_value) with dic=dic, target_value=target_value
#      → 7.1 result = {}
#      → 7.2 for key, value in dic.items():
#      → 7.2     if value == target_value:
#      → 7.2         result[key] = value
#      → 7.3 return result
#   8. def main(dic) → stored in memory, not executed
#   9. def main(dic) → stored in memory, not executed
#   10. dic = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(dic) with dic=25
#      → 13.1 return tuple(result.keys())
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (map_value, main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     target_value = ... → stored in memory
#     print(map_value(...)) → map_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 803:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def replace_value(dic, list_values) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Built-in functions: isinstance()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   replace_value(dic, list_values) runs first → then print() outputs the result
#   replace_value(dic, list_values) → each argument is evaluated first, then the function body runs
#   replace_value(?, 25) runs first → then print() outputs the result
#   replace_value(?, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def replace_value(dic, list_values) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. list_values = ... → assigns value to variable
#   4. print(replace_value(...))
#      → calls replace_value(dic, list_values) with dic=dic, list_values=list_values
#      → 4.1 return dic
#   5. def replace_value(dic, list_values) → stored in memory, not executed
#   6. def replace_value(dic, list_values) → stored in memory, not executed
#   7. dic = ... → assigns value to variable
#   8. list_values = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(replace_value(...))
#      → calls replace_value(dic, list_values) with dic=[4, 1, 8, 6, 3], list_values=25
#      → 11.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (replace_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     list_values = ... → stored in memory
#     print(replace_value(...)) → replace_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     list_values = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(replace_value(...)) → replace_value() runs first → value passed to print()
#
#   Loop: takes one item from dic each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 804:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def nested_dic() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Operators: **
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   nested_dic() runs first → then print() outputs the result
#   nested_dic() → each argument is evaluated first, then the function body runs
#   key ** 2 → Python looks up key, then 2, then applies **
#   key ** 3 → Python looks up key, then 3, then applies **
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def nested_dic() → stored in memory, not executed
#   2. print(nested_dic(...))
#      → calls nested_dic() with 
#      → 2.1 return result
#   3. def nested_dic() → stored in memory, not executed
#   4. result = ... → assigns value to variable
#   5. for (key, value) in result.items(...) → loops over each item
#      → 5.1 print(?)
#   6. def nested_dic() → stored in memory, not executed
#   7. def nested_dic() → stored in memory, not executed
#   8. result = ... → assigns value to variable
#   9. for key in range(...) → loops over each item
#      → 9.1 ? = ...
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. result = ... → assigns value to variable
#   13. for key in range(...) → loops over each item
#      → 13.1 ? = ...
#   14. result = ... → assigns value to variable
#   15. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (nested_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     print(nested_dic(...)) → nested_dic() runs first → value passed to print()
#     result = ... → stored in memory
#     for (key, value) in result.items(...) → runs body once per item
#     result = ... → stored in memory
#     for key in range(...) → runs body once per item
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     for key in range(...) → runs body once per item
#     result = ... → stored in memory
#     print(result)
#
#   Loop: takes one item from result.items(...) each round, runs body, repeats until done

"""

"""
Problem 806:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def sort_dic(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: dict, sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   sort_dic(dic) runs first → then print() outputs the result
#   sort_dic(dic) → each argument is evaluated first, then the function body runs
#   sort_dic(?) runs first → then print() outputs the result
#   sort_dic(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def sort_dic(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(sort_dic(...))
#      → calls sort_dic(dic) with dic=dic
#      → 3.1 return item[1]
#   4. def sort_dic(dic) → stored in memory, not executed
#   5. def sort_dic(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(sort_dic(...))
#      → calls sort_dic(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return item[1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (sort_dic) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(sort_dic(...)) → sort_dic() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(sort_dic(...)) → sort_dic() runs first → value passed to print()

"""

"""
Problem 807:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def lst_tuples(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: dict()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   lst_tuples(lst) runs first → then print() outputs the result
#   lst_tuples(lst) → each argument is evaluated first, then the function body runs
#   lst_tuples(?) runs first → then print() outputs the result
#   lst_tuples(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def lst_tuples(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. print(lst_tuples(...))
#      → calls lst_tuples(lst) with lst=lst
#      → 3.1 return dict(lst)
#   4. def lst_tuples(lst) → stored in memory, not executed
#   5. def lst_tuples(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(lst_tuples(...))
#      → calls lst_tuples(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return dict(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (lst_tuples) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     print(lst_tuples(...)) → lst_tuples() runs first → value passed to print()
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(lst_tuples(...)) → lst_tuples() runs first → value passed to print()

"""

"""
Problem 808:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def less_then_10(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   less_then_10(dic) runs first → then print() outputs the result
#   less_then_10(dic) → each argument is evaluated first, then the function body runs
#   less_then_10(25) runs first → then print() outputs the result
#   less_then_10(25) → each argument is evaluated first, then the function body runs
#   value >= 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def less_then_10(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(less_then_10(...))
#      → calls less_then_10(dic) with dic=dic
#      → 3.1 return result
#   4. def less_then_10(dic) → stored in memory, not executed
#   5. def less_then_10(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(less_then_10(...))
#      → calls less_then_10(dic) with dic=25
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (less_then_10) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(less_then_10(...)) → less_then_10() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(less_then_10(...)) → less_then_10() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 810:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def students_grades(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >=
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   students_grades(dic) runs first → then print() outputs the result
#   students_grades(dic) → each argument is evaluated first, then the function body runs
#   students_grades(25) runs first → then print() outputs the result
#   students_grades(25) → each argument is evaluated first, then the function body runs
#   value >= 70 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def students_grades(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(students_grades(...))
#      → calls students_grades(dic) with dic=dic
#      → 3.1 return list(result)
#   4. def students_grades(dic) → stored in memory, not executed
#   5. def students_grades(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(students_grades(...))
#      → calls students_grades(dic) with dic=25
#      → 9.1 return list(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (students_grades) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(students_grades(...)) → students_grades() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(students_grades(...)) → students_grades() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 811:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def word_len(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   word_len(string) runs first → then print() outputs the result
#   word_len(string) → each argument is evaluated first, then the function body runs
#   word_len('world') runs first → then print() outputs the result
#   word_len('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def word_len(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(word_len(...))
#      → calls word_len(string) with string=string
#      → 3.1 return result
#   4. def word_len(string) → stored in memory, not executed
#   5. def word_len(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(word_len(...))
#      → calls word_len(string) with string='world'
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (word_len) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(word_len(...)) → word_len() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(word_len(...)) → word_len() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 812:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def word_len(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   word_len(string) runs first → then print() outputs the result
#   word_len(string) → each argument is evaluated first, then the function body runs
#   word_len('world') runs first → then print() outputs the result
#   word_len('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def word_len(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(word_len(...))
#      → calls word_len(string) with string=string
#      → 3.1 return result
#   4. def word_len(string) → stored in memory, not executed
#   5. def word_len(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(word_len(...))
#      → calls word_len(string) with string='world'
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (word_len) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(word_len(...)) → word_len() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(word_len(...)) → word_len() runs first → value passed to print()
#
#   Loop: takes one item from words each round, runs body, repeats until done

"""

"""
Problem 813:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def starting_letter(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   starting_letter(dic) runs first → then print() outputs the result
#   main(dic) runs first → then print() outputs the result
#   starting_letter(dic) → each argument is evaluated first, then the function body runs
#   starting_letter(?) runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   starting_letter(?) → each argument is evaluated first, then the function body runs
#   first_letter not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def starting_letter(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(starting_letter(...))
#      → calls starting_letter(dic) with dic=dic
#      → 3.1 return result
#   4. def main(dic) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. print(main(...))
#      → calls main(dic) with dic=dic
#      → 6.1 result = {}
#      → 6.2 for key, value in dic.items():
#      → 6.2     first_letter = key[0]
#      → 6.2     result[first_letter] = key
#      → 6.3 return result
#   7. def starting_letter(dic) → stored in memory, not executed
#   8. def starting_letter(dic) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(starting_letter(...))
#      → calls starting_letter(dic) with dic=[4, 1, 8, 6, 3]
#      → 12.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, starting_letter) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(starting_letter(...)) → starting_letter() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(starting_letter(...)) → starting_letter() runs first → value passed to print()
#
#   Loop: takes one item from dic.keys(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 814:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def upper_keys(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   upper_keys(dic) runs first → then print() outputs the result
#   upper_keys(dic) → each argument is evaluated first, then the function body runs
#   upper_keys(25) runs first → then print() outputs the result
#   upper_keys(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def upper_keys(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(upper_keys(...))
#      → calls upper_keys(dic) with dic=dic
#      → 3.1 return result
#   4. def upper_keys(dic) → stored in memory, not executed
#   5. def upper_keys(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(upper_keys(...))
#      → calls upper_keys(dic) with dic=25
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (upper_keys) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(upper_keys(...)) → upper_keys() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(upper_keys(...)) → upper_keys() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done

"""

"""
Problem 815:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def intersection(dic1, dic2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   intersection(dic1, dic2) runs first → then print() outputs the result
#   intersection(dic1, dic2) → each argument is evaluated first, then the function body runs
#   intersection(?, 25) runs first → then print() outputs the result
#   intersection(?, 25) → each argument is evaluated first, then the function body runs
#   key in dic2 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def intersection(dic1, dic2) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. dic2 = ... → assigns value to variable
#   4. print(intersection(...))
#      → calls intersection(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 4.1 return result
#   5. def intersection(dic1, dic2) → stored in memory, not executed
#   6. def intersection(dic1, dic2) → stored in memory, not executed
#   7. dic1 = ... → assigns value to variable
#   8. dic2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(intersection(...))
#      → calls intersection(dic1, dic2) with dic1=[4, 1, 8, 6, 3], dic2=25
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (intersection) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(intersection(...)) → intersection() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(intersection(...)) → intersection() runs first → value passed to print()
#
#   Loop: takes one item from dic1 each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 816:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def count_vowels(string) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   count_vowels(string) runs first → then print() outputs the result
#   count_vowels(string) → each argument is evaluated first, then the function body runs
#   count_vowels('world') runs first → then print() outputs the result
#   count_vowels('world') → each argument is evaluated first, then the function body runs
#   char in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def count_vowels(string) → stored in memory, not executed
#   2. string = ... → assigns value to variable
#   3. print(count_vowels(...))
#      → calls count_vowels(string) with string=string
#      → 3.1 return result
#   4. def count_vowels(string) → stored in memory, not executed
#   5. def count_vowels(string) → stored in memory, not executed
#   6. string = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(count_vowels(...))
#      → calls count_vowels(string) with string='world'
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (count_vowels) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#     string = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(count_vowels(...)) → count_vowels() runs first → value passed to print()
#
#   Loop: takes one item from vowels each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 817:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def remove_value_is_none(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: is not
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   remove_value_is_none(dic1) runs first → then print() outputs the result
#   remove_value_is_none(dic) → each argument is evaluated first, then the function body runs
#   remove_value_is_none(25) runs first → then print() outputs the result
#   remove_value_is_none(dic1) → each argument is evaluated first, then the function body runs
#   remove_value_is_none(25) → each argument is evaluated first, then the function body runs
#   value  None → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def remove_value_is_none(dic) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. print(remove_value_is_none(...))
#      → calls remove_value_is_none(dic) with dic=dic1
#      → 3.1 return result
#   4. def remove_value_is_none(dic) → stored in memory, not executed
#   5. def remove_value_is_none(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(remove_value_is_none(...))
#      → calls remove_value_is_none(dic) with dic=25
#      → 9.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (remove_value_is_none) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     print(remove_value_is_none(...)) → remove_value_is_none() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(remove_value_is_none(...)) → remove_value_is_none() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 818:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def check_key_value(dic, target) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: == in and
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_key_value(dic1, target) runs first → then print() outputs the result
#   check_key_value(dic, target) → each argument is evaluated first, then the function body runs
#   check_key_value(?, 'goal') runs first → then print() outputs the result
#   check_key_value(dic1, target) → each argument is evaluated first, then the function body runs
#   check_key_value(?, 'goal') → each argument is evaluated first, then the function body runs
#   key in target → evaluated as True or False (determines which branch runs)
#   ? == ? → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def check_key_value(dic, target) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. print(check_key_value(...))
#      → calls check_key_value(dic, target) with dic=dic1, target=target
#      → 4.1 return True
#   5. def check_key_value(dic, target) → stored in memory, not executed
#   6. def check_key_value(dic, target) → stored in memory, not executed
#   7. dic = ... → assigns value to variable
#   8. target = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(check_key_value(...))
#      → calls check_key_value(dic, target) with dic=[4, 1, 8, 6, 3], target='goal'
#      → 11.1 return True
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (check_key_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     target = ... → stored in memory
#     print(check_key_value(...)) → check_key_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_key_value(...)) → check_key_value() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 819:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 820:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def merge_dicts(dic1, dic2) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   merge_dicts(dic1, dic2) runs first → then print() outputs the result
#   merge_dicts(dic1, dic2) → each argument is evaluated first, then the function body runs
#   merge_dicts(25, 25) runs first → then print() outputs the result
#   merge_dicts(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def merge_dicts(dic1, dic2) → stored in memory, not executed
#   2. dic1 = ... → assigns value to variable
#   3. dic2 = ... → assigns value to variable
#   4. print(merge_dicts(...))
#      → calls merge_dicts(dic1, dic2) with dic1=dic1, dic2=dic2
#      → 4.1 return merged_dict
#   5. def merge_dicts(dic1, dic2) → stored in memory, not executed
#   6. def merge_dicts(dic1, dic2) → stored in memory, not executed
#   7. dic1 = ... → assigns value to variable
#   8. dic2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(merge_dicts(...))
#      → calls merge_dicts(dic1, dic2) with dic1=25, dic2=25
#      → 11.1 return merged_dict
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (merge_dicts) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     print(merge_dicts(...)) → merge_dicts() runs first → value passed to print()
#     dic1 = ... → stored in memory
#     dic2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(merge_dicts(...)) → merge_dicts() runs first → value passed to print()

"""

"""
Problem 821:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def product_values(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   product_values(dic) runs first → then print() outputs the result
#   product_values(dic) → each argument is evaluated first, then the function body runs
#   product_values(?) runs first → then print() outputs the result
#   product_values(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def product_values(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(product_values(...))
#      → calls product_values(dic) with dic=dic
#      → 3.1 return product
#   4. def product_values(dic) → stored in memory, not executed
#   5. def product_values(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(product_values(...))
#      → calls product_values(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 return product
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (product_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(product_values(...)) → product_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(product_values(...)) → product_values() runs first → value passed to print()
#
#   Loop: takes one item from dic.values(...) each round, runs body, repeats until done

"""

"""
Problem 822:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def average_values(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: * /
# Built-in functions: Fraction, float, len, sum()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   average_values(dic) runs first → then print() outputs the result
#   average_values(dic) → each argument is evaluated first, then the function body runs
#   average_values(?) runs first → then print() outputs the result
#   sum_values / len_values → Python looks up sum_values, then len_values, then applies /
#   sum_values * ? → Python looks up sum_values, then ?, then applies *
#   average_values(?) → each argument is evaluated first, then the function body runs
#   1 / len_values → Python looks up 1, then len_values, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def average_values(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(average_values(...))
#      → calls average_values(dic) with dic=dic
#      → 3.1 from fractions import Fraction
#      → 3.2 return float(Fraction(sum_values, len_values))
#   4. def average_values(dic) → stored in memory, not executed
#   5. def average_values(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(average_values(...))
#      → calls average_values(dic) with dic=[4, 1, 8, 6, 3]
#      → 9.1 from fractions import Fraction
#      → 9.2 return float(Fraction(sum_values, len_values))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (average_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(average_values(...)) → average_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(average_values(...)) → average_values() runs first → value passed to print()

"""

"""
Problem 823:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: not in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) runs first → then print() outputs the result
#   main(dic, 'name', 'job') runs first → then print() outputs the result
#   main(dic, change_keys) runs first → then print() outputs the result
#   swap_values(dic, 'a', 'c') runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   main(dic, 'name', 'job') → each argument is evaluated first, then the function body runs
#   main(dic, change_keys) → each argument is evaluated first, then the function body runs
#   swap_values(dic, 'a', 'c') → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#   reverse_values not in result → evaluated as True or False (determines which branch runs)
#   change_keys not in result → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. target = ... → assigns value to variable
#   4. print(main(...))
#      → calls main(dic) with dic=dic
#      → 4.1 return dic
#   5. def main(dic, x, y) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. print(main(...))
#      → calls main(dic) with dic=dic
#      → 7.1 return dic
#   8. def main(dic, change_keys) → stored in memory, not executed
#   9. dic = ... → assigns value to variable
#   10. change_keys = ... → assigns value to variable
#   11. print(main(...))
#      → calls main(dic) with dic=dic
#      → 11.1 return dic
#   12. def swap_values(dic, key1, key2) → stored in memory, not executed
#   13. dic = ... → assigns value to variable
#   14. print(swap_values(...))
#      → calls swap_values(dic, key1, key2) with dic=dic, key1='a', key2='c'
#      → 14.1 dic[key1], dic[key2] = (dic[key2], dic[key1])
#      → 14.2 return dic
#   15. def main(dic) → stored in memory, not executed
#   16. def main(dic) → stored in memory, not executed
#   17. dic = ... → assigns value to variable
#   18. result = ... → assigns value to variable
#   19. print(result)
#   20. print(main(...))
#      → calls main(dic) with dic=[4, 1, 8, 6, 3]
#      → 20.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, swap_values) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     target = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     change_keys = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     print(swap_values(...)) → swap_values() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 824:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def largest_value(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: >
# Built-in functions: float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   largest_value(dic) runs first → then print() outputs the result
#   largest_value(dic) → each argument is evaluated first, then the function body runs
#   largest_value(25) runs first → then print() outputs the result
#   largest_value(25) → each argument is evaluated first, then the function body runs
#   value > largest_val → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def largest_value(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(largest_value(...))
#      → calls largest_value(dic) with dic=dic
#      → 3.1 return largest_key
#   4. def largest_value(dic) → stored in memory, not executed
#   5. def largest_value(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(largest_value(...))
#      → calls largest_value(dic) with dic=25
#      → 9.1 return largest_key
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (largest_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(largest_value(...)) → largest_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(largest_value(...)) → largest_value() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 825:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def smallest_value(dic) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for | if/else
# Operators: <
# Built-in functions: float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   smallest_value(dic) runs first → then print() outputs the result
#   smallest_value(dic) → each argument is evaluated first, then the function body runs
#   smallest_value(25) runs first → then print() outputs the result
#   smallest_value(25) → each argument is evaluated first, then the function body runs
#   value < smallest_val → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def smallest_value(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(smallest_value(...))
#      → calls smallest_value(dic) with dic=dic
#      → 3.1 return smallest_key
#   4. def smallest_value(dic) → stored in memory, not executed
#   5. def smallest_value(dic) → stored in memory, not executed
#   6. dic = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(smallest_value(...))
#      → calls smallest_value(dic) with dic=25
#      → 9.1 return smallest_key
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (smallest_value) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(smallest_value(...)) → smallest_value() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(smallest_value(...)) → smallest_value() runs first → value passed to print()
#
#   Loop: takes one item from dic.items(...) each round, runs body, repeats until done
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 826:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(dic) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(dic) runs first → then print() outputs the result
#   add_multiple_pairs(dic, new_pairs) runs first → then print() outputs the result
#   main(dic) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   add_multiple_pairs(dic, new_pairs) → each argument is evaluated first, then the function body runs
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(dic) → stored in memory, not executed
#   2. dic = ... → assigns value to variable
#   3. print(main(...))
#      → calls main(dic) with dic=dic
#      → 3.1 return dic
#   4. def add_multiple_pairs(dic, new_pairs) → stored in memory, not executed
#   5. dic = ... → assigns value to variable
#   6. new_pairs = ... → assigns value to variable
#   7. print(add_multiple_pairs(...))
#      → calls add_multiple_pairs(dic, new_pairs) with dic=dic, new_pairs=new_pairs
#      → 7.1 dic.update(new_pairs)
#      → 7.2 return dic
#   8. def main(dic) → stored in memory, not executed
#   9. def main(dic) → stored in memory, not executed
#   10. dic = ... → assigns value to variable
#   11. result = ... → assigns value to variable
#   12. print(result)
#   13. print(main(...))
#      → calls main(dic) with dic=[4, 1, 8, 6, 3]
#      → 13.1 return dic
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, add_multiple_pairs) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dic = ... → stored in memory
#     print(main(...)) → main() runs first → value passed to print()
#     dic = ... → stored in memory
#     new_pairs = ... → stored in memory
#     print(add_multiple_pairs(...)) → add_multiple_pairs() runs first → value passed to print()
#     dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 827:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i <= 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 828:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i <= 20 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 829:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i >= 1 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 830:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < count → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   solve(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. count = ... → assigns value to variable
#   2. i = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. def solve(number) → stored in memory, not executed
#   5. def solve(number) → stored in memory, not executed
#   6. number = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(solve(...))
#      → calls solve(number) with number=25
#      → 9.1 count = number
#      → 9.2 i = 0
#      → 9.3 while i < count:
#      → 9.3     return 'Hello'
#      → 9.3     i += 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     count = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 832:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number <= 1000 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. number = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. number = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 833:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: * <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i <= 10 → evaluated as True or False (determines which branch runs)
#   5 * i → Python looks up 5, then i, then applies *
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. i = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 834:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: <=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   total <= 50 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. total = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     total = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 835:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: % !=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   number % 2 → Python looks up number, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. number = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 836:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: !=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number != 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. total = ... → assigns value to variable
#   2. number = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     total = ... → stored in memory
#     number = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 837:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: + <
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < 10 → evaluated as True or False (determines which branch runs)
#   a + b → Python looks up a, then b, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. ? = ... → assigns value to variable
#   2. i = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. ? = ... → assigns value to variable
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#   9. ? = ... → assigns value to variable
#   10. i = ... → assigns value to variable
#   11. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     ? = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     ? = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 838:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: !=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   guess != secret_number → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. secret_number = ... → assigns value to variable
#   2. guess = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     secret_number = ... → stored in memory
#     guess = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 839:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(text) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else | while
# Operators: < in
# Built-in functions: input, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < len(...) → evaluated as True or False (determines which branch runs)
#   solve(text) → each argument is evaluated first, then the function body runs
#   solve('world') runs first → then print() outputs the result
#   ? in vowels → evaluated as True or False (determines which branch runs)
#   solve('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. string = ... → assigns value to variable
#   2. vowels = ... → assigns value to variable
#   3. i = ... → assigns value to variable
#   4. count = ... → assigns value to variable
#   5. while ... → repeats as long as condition is true
#   6. print(count)
#   7. def solve(text) → stored in memory, not executed
#   8. def solve(text) → stored in memory, not executed
#   9. text = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(solve(...))
#      → calls solve(text) with text='world'
#      → 12.1 string = text
#      → 12.2 vowels = 'aeiouAEIOU'
#      → 12.3 i = 0
#      → 12.4 count = 0
#      → 12.5 while i < len(string):
#      → 12.5     if string[i] in vowels:
#      → 12.5         count += 1
#      → 12.5     i += 1
#      → 12.6 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     vowels = ... → stored in memory
#     i = ... → stored in memory
#     count = ... → stored in memory
#     while → checks condition before each iteration
#     print(count)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 840:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(text) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   solve(text) → each argument is evaluated first, then the function body runs
#   solve('world') runs first → then print() outputs the result
#   solve('world') → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. string = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve(text) → stored in memory, not executed
#   4. def solve(text) → stored in memory, not executed
#   5. text = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(solve(...))
#      → calls solve(text) with text='world'
#      → 8.1 string = text
#      → 8.2 while string:
#      → 8.2     return string
#      → 8.2     string = string[:-1]
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     while → checks condition before each iteration
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 841:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   number >= 0 → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   solve(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. number = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve(number) → stored in memory, not executed
#   4. def solve(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(solve(...))
#      → calls solve(number) with number=25
#      → 8.1 number = number
#      → 8.2 while number >= 0:
#      → 8.2     return number
#      → 8.2     number -= 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     number = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 842:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number, number1) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num >= 0 → evaluated as True or False (determines which branch runs)
#   solve(number, number1) → each argument is evaluated first, then the function body runs
#   solve(25, 25) runs first → then print() outputs the result
#   solve(25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. total = ... → assigns value to variable
#   2. num = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(total)
#   5. def solve(number, number1) → stored in memory, not executed
#   6. def solve(number, number1) → stored in memory, not executed
#   7. number = ... → assigns value to variable
#   8. number1 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(solve(...))
#      → calls solve(number, number1) with number=25, number1=25
#      → 11.1 total = 0
#      → 11.2 num = number
#      → 11.3 while num >= 0:
#      → 11.3     total += num
#      → 11.4 num = number1
#      → 11.5 return total
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     total = ... → stored in memory
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#     number = ... → stored in memory
#     number1 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 844:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: !=
# Built-in functions: input()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   password != 'python' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. password = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     password = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 845:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num <= 10000 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. num = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. num = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 846:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: % * + >
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num > 0 → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   ? + ? → Python looks up ?, then ?, then applies +
#   solve(25) → each argument is evaluated first, then the function body runs
#   reversed_num * 10 → Python looks up reversed_num, then 10, then applies *
#   num % 10 → Python looks up num, then 10, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. reversed_num = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(reversed_num)
#   5. def solve(number) → stored in memory, not executed
#   6. def solve(number) → stored in memory, not executed
#   7. number = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(solve(...))
#      → calls solve(number) with number=25
#      → 10.1 num = number
#      → 10.2 reversed_num = 0
#      → 10.3 while num > 0:
#      → 10.3     reversed_num = reversed_num * 10 + num % 10
#      → 10.3     num //= 10
#      → 10.4 return reversed_num
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     reversed_num = ... → stored in memory
#     while → checks condition before each iteration
#     print(reversed_num)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 848:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num > 0 → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   solve(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve(number) → stored in memory, not executed
#   4. def solve(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(solve(...))
#      → calls solve(number) with number=25
#      → 8.1 num = number
#      → 8.2 while num > 0:
#      → 8.2     return num
#      → 8.2     num -= 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 849:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: % !=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   num % 7 → Python looks up num, then 7, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. num = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. num = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#   9. import random
#   10. num = ... → assigns value to variable
#   11. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 851:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: if/else | while
# Operators: < not in
# Built-in functions: input, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < len(...) → evaluated as True or False (determines which branch runs)
#   ? not in vowels → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. string = ... → assigns value to variable
#   2. vowels = ... → assigns value to variable
#   3. i = ... → assigns value to variable
#   4. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     string = ... → stored in memory
#     vowels = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 852:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num > 0 → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   solve(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. count = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(count)
#   5. def solve(number) → stored in memory, not executed
#   6. def solve(number) → stored in memory, not executed
#   7. number = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(solve(...))
#      → calls solve(number) with number=25
#      → 10.1 num = number
#      → 10.2 count = 0
#      → 10.3 while num > 0:
#      → 10.3     count += 1
#      → 10.3     num //= 10
#      → 10.4 return count
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     count = ... → stored in memory
#     while → checks condition before each iteration
#     print(count)
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 853:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: <
# Built-in functions: input, int, len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   len(...) < 10 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. lst = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. print(lst)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     while → checks condition before each iteration
#     print(lst)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 854:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num >= 1 → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   solve(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve(number) → stored in memory, not executed
#   4. def solve(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(solve(...))
#      → calls solve(number) with number=25
#      → 8.1 num = number
#      → 8.2 while num >= 1:
#      → 8.2     return num
#      → 8.2     num /= 2
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 856:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve(number) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: ** <=
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? <= 1000 → evaluated as True or False (determines which branch runs)
#   solve(number) → each argument is evaluated first, then the function body runs
#   solve(25) runs first → then print() outputs the result
#   num ** 2 → Python looks up num, then 2, then applies **
#   solve(25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve(number) → stored in memory, not executed
#   4. def solve(number) → stored in memory, not executed
#   5. number = ... → assigns value to variable
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. print(solve(...))
#      → calls solve(number) with number=25
#      → 8.1 num = number
#      → 8.2 while num ** 2 <= 1000:
#      → 8.2     return num ** 2
#      → 8.2     num += 1
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     number = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(solve(...)) → solve() runs first → value passed to print()
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 857:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else | while
# Operators: % != <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num <= 50 → evaluated as True or False (determines which branch runs)
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   num % 3 → Python looks up num, then 3, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. num = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. num = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 858:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num <= 100 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. num = ... → assigns value to variable
#   6. while ... → repeats as long as condition is true
#   7. num = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     num = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 859:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <
# Built-in functions: len()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < len(...) → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. lst = ... → assigns value to variable
#   2. i = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. i = ... → assigns value to variable
#   8. while ... → repeats as long as condition is true
#   9. lst = ... → assigns value to variable
#   10. i = ... → assigns value to variable
#   11. while ... → repeats as long as condition is true
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     lst = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#     lst = ... → stored in memory
#     i = ... → stored in memory
#     while → checks condition before each iteration
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 860:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else | while
# Operators: % != <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num <= 100 → evaluated as True or False (determines which branch runs)
#   ? != 0 → evaluated as True or False (determines which branch runs)
#   num % 2 → Python looks up num, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. total = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(total)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. num = ... → assigns value to variable
#   8. total = ... → assigns value to variable
#   9. while ... → repeats as long as condition is true
#   10. print(total)
#   11. num = ... → assigns value to variable
#   12. total = ... → assigns value to variable
#   13. while ... → repeats as long as condition is true
#   14. print(total)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     total = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#     num = ... → stored in memory
#     total = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#     num = ... → stored in memory
#     total = ... → stored in memory
#     while → checks condition before each iteration
#     print(total)
#
#   Loop: check condition → if True, run body → check again
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 861:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: <=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num <= 1000 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. count = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(count)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. num = ... → assigns value to variable
#   8. count = ... → assigns value to variable
#   9. while ... → repeats as long as condition is true
#   10. print(count)
#   11. num = ... → assigns value to variable
#   12. count = ... → assigns value to variable
#   13. while ... → repeats as long as condition is true
#   14. print(count)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     count = ... → stored in memory
#     while → checks condition before each iteration
#     print(count)
#     num = ... → stored in memory
#     count = ... → stored in memory
#     while → checks condition before each iteration
#     print(count)
#     num = ... → stored in memory
#     count = ... → stored in memory
#     while → checks condition before each iteration
#     print(count)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 863:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: while
# Operators: >
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   num > 0 → evaluated as True or False (determines which branch runs)
#   num_2 > 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. while ... → repeats as long as condition is true
#   3. print('Lift off!')
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. num = ... → assigns value to variable
#   7. while ... → repeats as long as condition is true
#   8. print('Lift off!')
#   9. num = ... → assigns value to variable
#   10. while ... → repeats as long as condition is true
#   11. print('Lift off!')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     print('Lift off!')
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     print('Lift off!')
#     num = ... → stored in memory
#     while → checks condition before each iteration
#     print('Lift off!')
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 865:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Keywords: while
# Operators: <
# Built-in functions: input, int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   i < 5 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. i = ... → assigns value to variable
#   2. product = ... → assigns value to variable
#   3. while ... → repeats as long as condition is true
#   4. print(product)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     i = ... → stored in memory
#     product = ... → stored in memory
#     while → checks condition before each iteration
#     print(product)
#
#   Loop: check condition → if True, run body → check again

"""

"""
Problem 866:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst1, lst2) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst1, lst2) → each argument is evaluated first, then the function body runs
#   main(?, ?) runs first → then print() outputs the result
#   main(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst1, lst2) → stored in memory, not executed
#   2. lst1 = ... → assigns value to variable
#   3. lst2 = ... → assigns value to variable
#   4. main(lst1, lst2) → calls main(lst1, lst2) with lst1=lst1, lst2=lst2
#      → 4.1 return result
#   5. def main(lst1, lst2) → stored in memory, not executed
#   6. def main(lst1, lst2) → stored in memory, not executed
#   7. lst1 = ... → assigns value to variable
#   8. lst2 = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(main(...))
#      → calls main(lst1, lst2) with lst1=[2, 4, 6], lst2=[1, 3, 5]
#      → 11.1 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     main(lst1, lst2) → jumps into main(), runs body, returns
#     lst1 = ... → stored in memory
#     lst2 = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()
#
#   Loop: takes one item from zip(...) each round, runs body, repeats until done

"""

"""
Problem 867:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def create_dictionary(keys, values) → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: for
# Built-in functions: len, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   create_dictionary(keys, values) → each argument is evaluated first, then the function body runs
#   create_dictionary(?, ?) runs first → then print() outputs the result
#   create_dictionary(?, ?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def create_dictionary(keys, values) → stored in memory, not executed
#   2. keys = ... → assigns value to variable
#   3. values = ... → assigns value to variable
#   4. result = ... → assigns value to variable
#   5. print(result)
#   6. def create_dictionary(keys, values) → stored in memory, not executed
#   7. def create_dictionary(keys, values) → stored in memory, not executed
#   8. keys = ... → assigns value to variable
#   9. values = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(create_dictionary(...))
#      → calls create_dictionary(keys, values) with keys=[4, 1, 8, 6, 3], values=[4, 1, 8, 6, 3]
#      → 12.1 return dictionary
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (create_dictionary) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     keys = ... → stored in memory
#     values = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     keys = ... → stored in memory
#     values = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(create_dictionary(...)) → create_dictionary() runs first → value passed to print()
#
#   Loop: takes one item from range(...) each round, runs body, repeats until done

"""

"""
Problem 868:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def capitalize_words(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: list, map, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   capitalize_words(lst) → each argument is evaluated first, then the function body runs
#   capitalize_words(?) runs first → then print() outputs the result
#   capitalize_words(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def capitalize_words(lst) → stored in memory, not executed
#   2. lst = ... → assigns value to variable
#   3. capitalize_words(lst) → calls capitalize_words(lst) with lst=lst
#      → 3.1 return str(name).capitalize()
#   4. def capitalize_words(lst) → stored in memory, not executed
#   5. def capitalize_words(lst) → stored in memory, not executed
#   6. lst = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(capitalize_words(...))
#      → calls capitalize_words(lst) with lst=[1, 3, 5, 7]
#      → 9.1 return str(name).capitalize()
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (capitalize_words) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst = ... → stored in memory
#     capitalize_words(lst) → jumps into capitalize_words(), runs body, returns
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(capitalize_words(...)) → capitalize_words() runs first → value passed to print()

"""

"""
Problem 869:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(a, b, c)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(a, b, c)
#   9. numbers = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)

"""

"""
Problem 870:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first, middle, last)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(first, middle, last)
#   9. numbers = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(first, middle, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)

"""

"""
Problem 871:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. ? = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(x, y)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. ? = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(x, y)
#   9. ? = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(x, y)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(x, y)
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(x, y)
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(x, y)

"""

"""
Problem 872:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(keys, values)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. data = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(keys, values)
#   9. data = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(keys, values)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(keys, values)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(keys, values)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(keys, values)

"""

"""
Problem 873:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_coordinates() → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_coordinates() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_coordinates() → stored in memory, not executed
#   2. ? = ... → assigns value to variable
#   3. print(x, y)
#   4. def get_coordinates() → stored in memory, not executed
#   5. def get_coordinates() → stored in memory, not executed
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. result = ... → assigns value to variable
#   9. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_coordinates) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     print(x, y)
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 874:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. word = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first, second, ''.join(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     word = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, second, ''.join(...))

"""

"""
Problem 875:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. values = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first, last)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. values = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(first, last)
#   9. values = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(first, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     values = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, last)
#     values = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, last)
#     values = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, last)

"""

"""
Problem 876:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. nested_list = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(a, b, c, d)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. nested_list = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(a, b, c, d)
#   9. nested_list = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(a, b, c, d)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     nested_list = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c, d)
#     nested_list = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c, d)
#     nested_list = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c, d)

"""

"""
Problem 877:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. ? = ... → assigns value to variable
#   2. print(a, b, c)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. ? = ... → assigns value to variable
#   6. print(a, b, c)
#   7. ? = ... → assigns value to variable
#   8. print(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     print(a, b, c)
#     ? = ... → stored in memory
#     print(a, b, c)
#     ? = ... → stored in memory
#     print(a, b, c)

"""

"""
Problem 878:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. full_name = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first_name, last_name)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. full_name = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(first_name, last_name)
#   9. full_name = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(first_name, last_name)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     full_name = ... → stored in memory
#     ? = ... → stored in memory
#     print(first_name, last_name)
#     full_name = ... → stored in memory
#     ? = ... → stored in memory
#     print(first_name, last_name)
#     full_name = ... → stored in memory
#     ? = ... → stored in memory
#     print(first_name, last_name)

"""

"""
Problem 879:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def greet(name, age) → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   greet(?) → each argument is evaluated first, then the function body runs
#   greet(name, age) → each argument is evaluated first, then the function body runs
#   greet('Bob', 25) runs first → then print() outputs the result
#   greet('Bob', 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def greet(name, age) → stored in memory, not executed
#   2. person = ... → assigns value to variable
#   3. greet(?) → calls greet(name, age) with name=?
#      → 3.1 result = f'Hello, {name}. You are {age} years old.'
#      → 3.2 return result
#   4. def greet(name, age) → stored in memory, not executed
#   5. def greet(name, age) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. age = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(greet(...))
#      → calls greet(name, age) with name='Bob', age=25
#      → 10.1 result = f'Hello, {name}. You are {age} years old.'
#      → 10.2 return result
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (greet) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     greet(?) → jumps into greet(), runs body, returns
#     name = ... → stored in memory
#     age = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(greet(...)) → greet() runs first → value passed to print()

"""

"""
Problem 880:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. list1 = ... → assigns value to variable
#   2. list2 = ... → assigns value to variable
#   3. merged = ... → assigns value to variable
#   4. print(merged)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. list1 = ... → assigns value to variable
#   8. list2 = ... → assigns value to variable
#   9. merged = ... → assigns value to variable
#   10. print(merged)
#   11. list1 = ... → assigns value to variable
#   12. list2 = ... → assigns value to variable
#   13. merged = ... → assigns value to variable
#   14. print(merged)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     merged = ... → stored in memory
#     print(merged)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     merged = ... → stored in memory
#     print(merged)
#     list1 = ... → stored in memory
#     list2 = ... → stored in memory
#     merged = ... → stored in memory
#     print(merged)

"""

"""
Problem 881:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. dict1 = ... → assigns value to variable
#   2. dict2 = ... → assigns value to variable
#   3. merged_dict = ... → assigns value to variable
#   4. print(merged_dict)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. merged_dict = ... → assigns value to variable
#   10. print(merged_dict)
#   11. dict1 = ... → assigns value to variable
#   12. dict2 = ... → assigns value to variable
#   13. merged_dict = ... → assigns value to variable
#   14. print(merged_dict)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     merged_dict = ... → stored in memory
#     print(merged_dict)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     merged_dict = ... → stored in memory
#     print(merged_dict)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     merged_dict = ... → stored in memory
#     print(merged_dict)

"""

"""
Problem 882:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. values = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first, third, last)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. values = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(first, third, last)
#   9. values = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(first, third, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     values = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, third, last)
#     values = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, third, last)
#     values = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, third, last)

"""

"""
Problem 883:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def add(a, b, c) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   add(?) → each argument is evaluated first, then the function body runs
#   add(a, b, c) → each argument is evaluated first, then the function body runs
#   add(20, 30, 10) runs first → then print() outputs the result
#   ? + c → Python looks up ?, then c, then applies +
#   add(20, 30, 10) → each argument is evaluated first, then the function body runs
#   a + b → Python looks up a, then b, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def add(a, b, c) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. result = ... → assigns value to variable
#   4. print(result)
#   5. def add(a, b, c) → stored in memory, not executed
#   6. def add(a, b, c) → stored in memory, not executed
#   7. a = ... → assigns value to variable
#   8. b = ... → assigns value to variable
#   9. c = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(add(...))
#      → calls add(a, b, c) with a=20, b=30, c=10
#      → 12.1 return a + b + c
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (add) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     a = ... → stored in memory
#     b = ... → stored in memory
#     c = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(add(...)) → add() runs first → value passed to print()

"""

"""
Problem 884:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def describe(name, age, city) → defines a reusable function
#   return → sends a value back to the caller
#
# Operators: %
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   describe() runs first → then print() outputs the result
#   describe(name, age, city) → each argument is evaluated first, then the function body runs
#   describe('Bob', 25, 25) runs first → then print() outputs the result
#   describe() → each argument is evaluated first, then the function body runs
#   '%s is %s years old and lives in %s.' % ? → Python looks up '%s is %s years old and lives in %s.', then ?, then applies %
#   describe('Bob', 25, 25) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def describe(name, age, city) → stored in memory, not executed
#   2. person = ... → assigns value to variable
#   3. print(describe(...))
#      → calls describe(name, age, city) with 
#      → 3.1 return '%s is %s years old and lives in %s.' % (name, age, city)
#   4. def describe(name, age, city) → stored in memory, not executed
#   5. def describe(name, age, city) → stored in memory, not executed
#   6. name = ... → assigns value to variable
#   7. age = ... → assigns value to variable
#   8. city = ... → assigns value to variable
#   9. result = ... → assigns value to variable
#   10. print(result)
#   11. print(describe(...))
#      → calls describe(name, age, city) with name='Bob', age=25, city=25
#      → 11.1 return '%s is %s years old and lives in %s.' % (name, age, city)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (describe) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     print(describe(...)) → describe() runs first → value passed to print()
#     name = ... → stored in memory
#     age = ... → stored in memory
#     city = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(describe(...)) → describe() runs first → value passed to print()

"""

"""
Problem 885:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. word = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(a, b, c)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. word = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(a, b, c)
#   9. word = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     word = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)
#     word = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)
#     word = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)

"""

"""
Problem 886:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first, middle, last)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(first, middle, last)
#   9. numbers = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(first, middle, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)

"""

"""
Problem 887:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. pairs = ... → assigns value to variable
#   2. for (number, letter) in pairs → loops over each item
#      → 2.1 print(number, letter)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. pairs = ... → assigns value to variable
#   6. for (number, letter) in pairs → loops over each item
#      → 6.1 print(number, letter)
#   7. pairs = ... → assigns value to variable
#   8. for (number, letter) in pairs → loops over each item
#      → 8.1 print(number, letter)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pairs = ... → stored in memory
#     for (number, letter) in pairs → runs body once per item
#     pairs = ... → stored in memory
#     for (number, letter) in pairs → runs body once per item
#     pairs = ... → stored in memory
#     for (number, letter) in pairs → runs body once per item
#
#   Loop: takes one item from pairs each round, runs body, repeats until done

"""

"""
Problem 888:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def get_user() → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   get_user() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def get_user() → stored in memory, not executed
#   2. ? = ... → assigns value to variable
#   3. print(name, age)
#   4. def get_user() → stored in memory, not executed
#   5. def get_user() → stored in memory, not executed
#   6. result = ... → assigns value to variable
#   7. print(result)
#   8. result = ... → assigns value to variable
#   9. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (get_user) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     print(name, age)
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 889:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: sum, zip()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. lists = ... → assigns value to variable
#   2. summed = ... → assigns value to variable
#   3. print(summed)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. lists = ... → assigns value to variable
#   7. summed = ... → assigns value to variable
#   8. print(summed)
#   9. lists = ... → assigns value to variable
#   10. summed = ... → assigns value to variable
#   11. print(summed)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lists = ... → stored in memory
#     summed = ... → stored in memory
#     print(summed)
#     lists = ... → stored in memory
#     summed = ... → stored in memory
#     print(summed)
#     lists = ... → stored in memory
#     summed = ... → stored in memory
#     print(summed)

"""

"""
Problem 890:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. formatted = ... → assigns value to variable
#   3. print(formatted)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. data = ... → assigns value to variable
#   7. formatted = ... → assigns value to variable
#   8. print(formatted)
#   9. data = ... → assigns value to variable
#   10. formatted = ... → assigns value to variable
#   11. print(formatted)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     formatted = ... → stored in memory
#     print(formatted)
#     data = ... → stored in memory
#     formatted = ... → stored in memory
#     print(formatted)
#     data = ... → stored in memory
#     formatted = ... → stored in memory
#     print(formatted)

"""

"""
Problem 891:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. sentence = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(first, middle, last)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. sentence = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(first, middle, last)
#   9. sentence = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(first, middle, last)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentence = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)
#     sentence = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)
#     sentence = ... → stored in memory
#     ? = ... → stored in memory
#     print(first, middle, last)

"""

"""
Problem 892:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   c in 'aeiou' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. word = ... → assigns value to variable
#   2. vowels = ... → assigns value to variable
#   3. print(vowels)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. word = ... → assigns value to variable
#   7. vowels = ... → assigns value to variable
#   8. print(vowels)
#   9. word = ... → assigns value to variable
#   10. vowels = ... → assigns value to variable
#   11. print(vowels)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     word = ... → stored in memory
#     vowels = ... → stored in memory
#     print(vowels)
#     word = ... → stored in memory
#     vowels = ... → stored in memory
#     print(vowels)
#     word = ... → stored in memory
#     vowels = ... → stored in memory
#     print(vowels)

"""

"""
Problem 893:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(name, age, height, is_student)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. data = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(name, age, height, is_student)
#   9. data = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(name, age, height, is_student)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(name, age, height, is_student)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(name, age, height, is_student)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     print(name, age, height, is_student)

"""

"""
Problem 894:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: sorted()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. tuples = ... → assigns value to variable
#   2. sorted_tuples = ... → assigns value to variable
#   3. print(sorted_tuples)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. tuples = ... → assigns value to variable
#   7. sorted_tuples = ... → assigns value to variable
#   8. print(sorted_tuples)
#   9. tuples = ... → assigns value to variable
#   10. sorted_tuples = ... → assigns value to variable
#   11. print(sorted_tuples)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     tuples = ... → stored in memory
#     sorted_tuples = ... → stored in memory
#     print(sorted_tuples)
#     tuples = ... → stored in memory
#     sorted_tuples = ... → stored in memory
#     print(sorted_tuples)
#     tuples = ... → stored in memory
#     sorted_tuples = ... → stored in memory
#     print(sorted_tuples)

"""

"""
Problem 895:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: % ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? == 0 → evaluated as True or False (determines which branch runs)
#   n % 2 → Python looks up n, then 2, then applies %
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. evens = ... → assigns value to variable
#   3. print(evens)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. evens = ... → assigns value to variable
#   8. print(evens)
#   9. numbers = ... → assigns value to variable
#   10. evens = ... → assigns value to variable
#   11. print(evens)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     evens = ... → stored in memory
#     print(evens)
#     numbers = ... → stored in memory
#     evens = ... → stored in memory
#     print(evens)
#     numbers = ... → stored in memory
#     evens = ... → stored in memory
#     print(evens)

"""

"""
Problem 896:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. ? = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(a, b, c)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. ? = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(a, b, c)
#   9. ? = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, b, c)

"""

"""
Problem 897:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. ? = ... → assigns value to variable
#   2. print(first, second, remaining)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. ? = ... → assigns value to variable
#   6. print(first, second, remaining)
#   7. ? = ... → assigns value to variable
#   8. print(first, second, remaining)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     print(first, second, remaining)
#     ? = ... → stored in memory
#     print(first, second, remaining)
#     ? = ... → stored in memory
#     print(first, second, remaining)

"""

"""
Problem 898:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Operators: +
# Built-in functions: add_three()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? + z → Python looks up ?, then z, then applies +
#   x + y → Python looks up x, then y, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. add_three = ... → assigns value to variable
#   2. numbers = ... → assigns value to variable
#   3. print(add_three(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     add_three = ... → stored in memory
#     numbers = ... → stored in memory
#     print(add_three(...))

"""

"""
Problem 899:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num = ... → assigns value to variable
#   2. digits = ... → assigns value to variable
#   3. reversed_digits = ... → assigns value to variable
#   4. print(reversed_digits)
#   5. num = ... → assigns value to variable
#   6. str_num = ... → assigns value to variable
#   7. lst_num = ... → assigns value to variable
#   8. print(lst_num)
#   9. def solve() → stored in memory, not executed
#   10. def solve() → stored in memory, not executed
#   11. num = ... → assigns value to variable
#   12. digits = ... → assigns value to variable
#   13. reversed_digits = ... → assigns value to variable
#   14. print(reversed_digits)
#   15. num = ... → assigns value to variable
#   16. str_num = ... → assigns value to variable
#   17. lst_num = ... → assigns value to variable
#   18. print(lst_num)
#   19. num = ... → assigns value to variable
#   20. digits = ... → assigns value to variable
#   21. reversed_digits = ... → assigns value to variable
#   22. print(reversed_digits)
#   23. num = ... → assigns value to variable
#   24. str_num = ... → assigns value to variable
#   25. lst_num = ... → assigns value to variable
#   26. print(lst_num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     digits = ... → stored in memory
#     reversed_digits = ... → stored in memory
#     print(reversed_digits)
#     num = ... → stored in memory
#     str_num = ... → stored in memory
#     lst_num = ... → stored in memory
#     print(lst_num)
#     num = ... → stored in memory
#     digits = ... → stored in memory
#     reversed_digits = ... → stored in memory
#     print(reversed_digits)
#     num = ... → stored in memory
#     str_num = ... → stored in memory
#     lst_num = ... → stored in memory
#     print(lst_num)
#     num = ... → stored in memory
#     digits = ... → stored in memory
#     reversed_digits = ... → stored in memory
#     print(reversed_digits)
#     num = ... → stored in memory
#     str_num = ... → stored in memory
#     lst_num = ... → stored in memory
#     print(lst_num)

"""

"""
Problem 900:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def min_max(lst) → defines a reusable function
#   return → sends a value back to the caller
#
# Built-in functions: max, min()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   min_max(numbers) → each argument is evaluated first, then the function body runs
#   min_max(lst) → each argument is evaluated first, then the function body runs
#   min_max(?) runs first → then print() outputs the result
#   min_max(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def min_max(lst) → stored in memory, not executed
#   2. numbers = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(minimum, maximum)
#   5. def min_max(lst) → stored in memory, not executed
#   6. def min_max(lst) → stored in memory, not executed
#   7. lst = ... → assigns value to variable
#   8. result = ... → assigns value to variable
#   9. print(result)
#   10. print(min_max(...))
#      → calls min_max(lst) with lst=[1, 3, 5, 7]
#      → 10.1 return (min(lst), max(lst))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (min_max) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     ? = ... → stored in memory
#     print(minimum, maximum)
#     lst = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(min_max(...)) → min_max() runs first → value passed to print()

"""

"""
Problem 901:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. tuples = ... → assigns value to variable
#   2. first_elements = ... → assigns value to variable
#   3. print(first_elements)
#   4. def main() → stored in memory, not executed
#   5. main() → calls main() with 
#      → 5.1 pass
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     tuples = ... → stored in memory
#     first_elements = ... → stored in memory
#     print(first_elements)
#     main() → jumps into main(), runs body, returns

"""

"""
Problem 902:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main(lst_dic) → defines a reusable function
#   return → sends a value back to the caller
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main(lst_dic) → each argument is evaluated first, then the function body runs
#   main(?) runs first → then print() outputs the result
#   main(?) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def main(lst_dic) → stored in memory, not executed
#   2. lst_dic = ... → assigns value to variable
#   3. main(lst_dic) → calls main(lst_dic) with lst_dic=lst_dic
#      → 3.1 return ages
#   4. people = ... → assigns value to variable
#   5. ages = ... → assigns value to variable
#   6. print(ages)
#   7. def main(lst_dic) → stored in memory, not executed
#   8. def main(lst_dic) → stored in memory, not executed
#   9. lst_dic = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#   12. print(main(...))
#      → calls main(lst_dic) with lst_dic=[4, 1, 8, 6, 3]
#      → 12.1 return ages
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     lst_dic = ... → stored in memory
#     main(lst_dic) → jumps into main(), runs body, returns
#     people = ... → stored in memory
#     ages = ... → stored in memory
#     print(ages)
#     lst_dic = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(main(...)) → main() runs first → value passed to print()

"""

"""
Problem 903:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. expression = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(a, op1, b, op2, c, op3, d)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. expression = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(a, op1, b, op2, c, op3, d)
#   9. expression = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(a, op1, b, op2, c, op3, d)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     expression = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, op1, b, op2, c, op3, d)
#     expression = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, op1, b, op2, c, op3, d)
#     expression = ... → stored in memory
#     ? = ... → stored in memory
#     print(a, op1, b, op2, c, op3, d)

"""

"""
Problem 904:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. data = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. ? = ... → assigns value to variable
#   4. print(x, y, z)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. data = ... → assigns value to variable
#   8. ? = ... → assigns value to variable
#   9. ? = ... → assigns value to variable
#   10. print(x, y, z)
#   11. data = ... → assigns value to variable
#   12. ? = ... → assigns value to variable
#   13. ? = ... → assigns value to variable
#   14. print(x, y, z)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(x, y, z)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(x, y, z)
#     data = ... → stored in memory
#     ? = ... → stored in memory
#     ? = ... → stored in memory
#     print(x, y, z)

"""

"""
Problem 905:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. email = ... → assigns value to variable
#   2. ? = ... → assigns value to variable
#   3. print(user, domain)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. email = ... → assigns value to variable
#   7. ? = ... → assigns value to variable
#   8. print(user, domain)
#   9. email = ... → assigns value to variable
#   10. ? = ... → assigns value to variable
#   11. print(user, domain)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     email = ... → stored in memory
#     ? = ... → stored in memory
#     print(user, domain)
#     email = ... → stored in memory
#     ? = ... → stored in memory
#     print(user, domain)
#     email = ... → stored in memory
#     ? = ... → stored in memory
#     print(user, domain)

"""

"""
Problem 906:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: for
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. nested_tuples = ... → assigns value to variable
#   2. for (a, (b, c)) in nested_tuples → loops over each item
#      → 2.1 print(a, b, c)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. nested_tuples = ... → assigns value to variable
#   6. for (a, (b, c)) in nested_tuples → loops over each item
#      → 6.1 print(a, b, c)
#   7. nested_tuples = ... → assigns value to variable
#   8. for (a, (b, c)) in nested_tuples → loops over each item
#      → 8.1 print(a, b, c)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     nested_tuples = ... → stored in memory
#     for (a, (b, c)) in nested_tuples → runs body once per item
#     nested_tuples = ... → stored in memory
#     for (a, (b, c)) in nested_tuples → runs body once per item
#     nested_tuples = ... → stored in memory
#     for (a, (b, c)) in nested_tuples → runs body once per item
#
#   Loop: takes one item from nested_tuples each round, runs body, repeats until done

"""

"""
Problem 907:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: divmod()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. ? = ... → assigns value to variable
#   2. print(quotient, remainder)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. ? = ... → assigns value to variable
#   6. print(quotient, remainder)
#   7. ? = ... → assigns value to variable
#   8. print(quotient, remainder)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ? = ... → stored in memory
#     print(quotient, remainder)
#     ? = ... → stored in memory
#     print(quotient, remainder)
#     ? = ... → stored in memory
#     print(quotient, remainder)

"""

"""
Problem 908:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. import random
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 909:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. import random
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 910:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. import random
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 911:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. colors = ... → assigns value to variable
#   3. selected = ... → assigns value to variable
#   4. print(selected)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. colors = ... → assigns value to variable
#   9. selected = ... → assigns value to variable
#   10. print(selected)
#   11. import random
#   12. colors = ... → assigns value to variable
#   13. selected = ... → assigns value to variable
#   14. print(selected)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     colors = ... → stored in memory
#     selected = ... → stored in memory
#     print(selected)
#     colors = ... → stored in memory
#     selected = ... → stored in memory
#     print(selected)
#     colors = ... → stored in memory
#     selected = ... → stored in memory
#     print(selected)

"""

"""
Problem 912:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. numbers = ... → assigns value to variable
#   3. random.shuffle(numbers)
#   4. print(numbers)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. numbers = ... → assigns value to variable
#   9. random.shuffle(numbers)
#   10. print(numbers)
#   11. import random
#   12. numbers = ... → assigns value to variable
#   13. random.shuffle(numbers)
#   14. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     random.shuffle(numbers)
#     print(numbers)
#     numbers = ... → stored in memory
#     random.shuffle(numbers)
#     print(numbers)
#     numbers = ... → stored in memory
#     random.shuffle(numbers)
#     print(numbers)

"""

"""
Problem 913:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. numbers = ... → assigns value to variable
#   3. selected = ... → assigns value to variable
#   4. print(selected)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. numbers = ... → assigns value to variable
#   9. selected = ... → assigns value to variable
#   10. print(selected)
#   11. import random
#   12. numbers = ... → assigns value to variable
#   13. selected = ... → assigns value to variable
#   14. print(selected)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     selected = ... → stored in memory
#     print(selected)
#     numbers = ... → stored in memory
#     selected = ... → stored in memory
#     print(selected)
#     numbers = ... → stored in memory
#     selected = ... → stored in memory
#     print(selected)

"""

"""
Problem 914:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. import random
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 915:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. import string
#   3. letter = ... → assigns value to variable
#   4. print(letter)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. import string
#   9. letter = ... → assigns value to variable
#   10. print(letter)
#   11. import random
#   12. import string
#   13. letter = ... → assigns value to variable
#   14. print(letter)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     letter = ... → stored in memory
#     print(letter)
#     letter = ... → stored in memory
#     print(letter)
#     letter = ... → stored in memory
#     print(letter)

"""

"""
Problem 916:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. roll = ... → assigns value to variable
#   3. print(roll)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. roll = ... → assigns value to variable
#   8. print(roll)
#   9. import random
#   10. roll = ... → assigns value to variable
#   11. print(roll)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     roll = ... → stored in memory
#     print(roll)
#     roll = ... → stored in memory
#     print(roll)
#     roll = ... → stored in memory
#     print(roll)

"""

"""
Problem 917:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. flip = ... → assigns value to variable
#   3. print(flip)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. flip = ... → assigns value to variable
#   8. print(flip)
#   9. import random
#   10. flip = ... → assigns value to variable
#   11. print(flip)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     flip = ... → stored in memory
#     print(flip)
#     flip = ... → stored in memory
#     print(flip)
#     flip = ... → stored in memory
#     print(flip)

"""

"""
Problem 918:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. first_names = ... → assigns value to variable
#   3. last_names = ... → assigns value to variable
#   4. full_name = ... → assigns value to variable
#   5. print(full_name)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import random
#   9. first_names = ... → assigns value to variable
#   10. last_names = ... → assigns value to variable
#   11. full_name = ... → assigns value to variable
#   12. print(full_name)
#   13. import random
#   14. first_names = ... → assigns value to variable
#   15. last_names = ... → assigns value to variable
#   16. full_name = ... → assigns value to variable
#   17. print(full_name)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     first_names = ... → stored in memory
#     last_names = ... → stored in memory
#     full_name = ... → stored in memory
#     print(full_name)
#     first_names = ... → stored in memory
#     last_names = ... → stored in memory
#     full_name = ... → stored in memory
#     print(full_name)
#     first_names = ... → stored in memory
#     last_names = ... → stored in memory
#     full_name = ... → stored in memory
#     print(full_name)

"""

"""
Problem 919:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? + string.punctuation → Python looks up ?, then string.punctuation, then applies +
#   string.ascii_letters + string.digits → Python looks up string.ascii_letters, then string.digits, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. import string
#   3. characters = ... → assigns value to variable
#   4. password = ... → assigns value to variable
#   5. print(password)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import random
#   9. import string
#   10. characters = ... → assigns value to variable
#   11. password = ... → assigns value to variable
#   12. print(password)
#   13. import random
#   14. import string
#   15. characters = ... → assigns value to variable
#   16. password = ... → assigns value to variable
#   17. print(password)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     characters = ... → stored in memory
#     password = ... → stored in memory
#     print(password)
#     characters = ... → stored in memory
#     password = ... → stored in memory
#     print(password)
#     characters = ... → stored in memory
#     password = ... → stored in memory
#     print(password)

"""

"""
Problem 920:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. boolean_value = ... → assigns value to variable
#   3. print(boolean_value)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. boolean_value = ... → assigns value to variable
#   8. print(boolean_value)
#   9. import random
#   10. boolean_value = ... → assigns value to variable
#   11. print(boolean_value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     boolean_value = ... → stored in memory
#     print(boolean_value)
#     boolean_value = ... → stored in memory
#     print(boolean_value)
#     boolean_value = ... → stored in memory
#     print(boolean_value)

"""

"""
Problem 921:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. numbers = ... → assigns value to variable
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. numbers = ... → assigns value to variable
#   8. print(numbers)
#   9. import random
#   10. numbers = ... → assigns value to variable
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     print(numbers)
#     numbers = ... → stored in memory
#     print(numbers)
#     numbers = ... → stored in memory
#     print(numbers)

"""

"""
Problem 922:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, range()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. items = ... → assigns value to variable
#   3. subset = ... → assigns value to variable
#   4. print(subset)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. items = ... → assigns value to variable
#   9. subset = ... → assigns value to variable
#   10. print(subset)
#   11. import random
#   12. items = ... → assigns value to variable
#   13. subset = ... → assigns value to variable
#   14. print(subset)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     items = ... → stored in memory
#     subset = ... → stored in memory
#     print(subset)
#     items = ... → stored in memory
#     subset = ... → stored in memory
#     print(subset)
#     items = ... → stored in memory
#     subset = ... → stored in memory
#     print(subset)

"""

"""
Problem 923:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: + -
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   start_date + datetime.timedelta(...) → Python looks up start_date, then datetime.timedelta(...), then applies +
#   end_date - start_date → Python looks up end_date, then start_date, then applies -
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. import datetime
#   3. start_date = ... → assigns value to variable
#   4. end_date = ... → assigns value to variable
#   5. random_days = ... → assigns value to variable
#   6. random_date = ... → assigns value to variable
#   7. print(random_date)
#   8. def solve() → stored in memory, not executed
#   9. def solve() → stored in memory, not executed
#   10. import random
#   11. import datetime
#   12. start_date = ... → assigns value to variable
#   13. end_date = ... → assigns value to variable
#   14. random_days = ... → assigns value to variable
#   15. random_date = ... → assigns value to variable
#   16. print(random_date)
#   17. import random
#   18. import datetime
#   19. start_date = ... → assigns value to variable
#   20. end_date = ... → assigns value to variable
#   21. random_days = ... → assigns value to variable
#   22. random_date = ... → assigns value to variable
#   23. print(random_date)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     start_date = ... → stored in memory
#     end_date = ... → stored in memory
#     random_days = ... → stored in memory
#     random_date = ... → stored in memory
#     print(random_date)
#     start_date = ... → stored in memory
#     end_date = ... → stored in memory
#     random_days = ... → stored in memory
#     random_date = ... → stored in memory
#     print(random_date)
#     start_date = ... → stored in memory
#     end_date = ... → stored in memory
#     random_days = ... → stored in memory
#     random_date = ... → stored in memory
#     print(random_date)

"""

"""
Problem 924:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. fruits = ... → assigns value to variable
#   3. random_fruit = ... → assigns value to variable
#   4. print(random_fruit)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. fruits = ... → assigns value to variable
#   9. random_fruit = ... → assigns value to variable
#   10. print(random_fruit)
#   11. import random
#   12. fruits = ... → assigns value to variable
#   13. random_fruit = ... → assigns value to variable
#   14. print(random_fruit)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     fruits = ... → stored in memory
#     random_fruit = ... → stored in memory
#     print(random_fruit)
#     fruits = ... → stored in memory
#     random_fruit = ... → stored in memory
#     print(random_fruit)
#     fruits = ... → stored in memory
#     random_fruit = ... → stored in memory
#     print(random_fruit)

"""

"""
Problem 925:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. code = ... → assigns value to variable
#   3. print(code)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. code = ... → assigns value to variable
#   8. print(code)
#   9. import random
#   10. code = ... → assigns value to variable
#   11. print(code)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     code = ... → stored in memory
#     print(code)
#     code = ... → stored in memory
#     print(code)
#     code = ... → stored in memory
#     print(code)

"""

"""
Problem 926:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. hex_color = ... → assigns value to variable
#   3. print(hex_color)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. hex_color = ... → assigns value to variable
#   8. print(hex_color)
#   9. import random
#   10. hex_color = ... → assigns value to variable
#   11. print(hex_color)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     hex_color = ... → stored in memory
#     print(hex_color)
#     hex_color = ... → stored in memory
#     print(hex_color)
#     hex_color = ... → stored in memory
#     print(hex_color)

"""

"""
Problem 927:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. phone_number = ... → assigns value to variable
#   3. print(phone_number)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. phone_number = ... → assigns value to variable
#   8. print(phone_number)
#   9. import random
#   10. phone_number = ... → assigns value to variable
#   11. print(phone_number)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     phone_number = ... → stored in memory
#     print(phone_number)
#     phone_number = ... → stored in memory
#     print(phone_number)
#     phone_number = ... → stored in memory
#     print(phone_number)

"""

"""
Problem 928:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. pin = ... → assigns value to variable
#   3. print(pin)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. pin = ... → assigns value to variable
#   8. print(pin)
#   9. import random
#   10. pin = ... → assigns value to variable
#   11. print(pin)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     pin = ... → stored in memory
#     print(pin)
#     pin = ... → stored in memory
#     print(pin)
#     pin = ... → stored in memory
#     print(pin)

"""

"""
Problem 929:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. dice1 = ... → assigns value to variable
#   3. dice2 = ... → assigns value to variable
#   4. print(dice1, dice2)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. dice1 = ... → assigns value to variable
#   9. dice2 = ... → assigns value to variable
#   10. print(dice1, dice2)
#   11. import random
#   12. dice1 = ... → assigns value to variable
#   13. dice2 = ... → assigns value to variable
#   14. print(dice1, dice2)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dice1 = ... → stored in memory
#     dice2 = ... → stored in memory
#     print(dice1, dice2)
#     dice1 = ... → stored in memory
#     dice2 = ... → stored in memory
#     print(dice1, dice2)
#     dice1 = ... → stored in memory
#     dice2 = ... → stored in memory
#     print(dice1, dice2)

"""

"""
Problem 930:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: range, str()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. ip_address = ... → assigns value to variable
#   3. print(ip_address)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. import random
#   7. ip_address = ... → assigns value to variable
#   8. print(ip_address)
#   9. import random
#   10. ip_address = ... → assigns value to variable
#   11. print(ip_address)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     ip_address = ... → stored in memory
#     print(ip_address)
#     ip_address = ... → stored in memory
#     print(ip_address)
#     ip_address = ... → stored in memory
#     print(ip_address)

"""

"""
Problem 931:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   ? + string.punctuation → Python looks up ?, then string.punctuation, then applies +
#   string.ascii_letters + string.digits → Python looks up string.ascii_letters, then string.digits, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. import string
#   3. characters = ... → assigns value to variable
#   4. password = ... → assigns value to variable
#   5. print(password)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import random
#   9. import string
#   10. characters = ... → assigns value to variable
#   11. password = ... → assigns value to variable
#   12. print(password)
#   13. import random
#   14. import string
#   15. characters = ... → assigns value to variable
#   16. password = ... → assigns value to variable
#   17. print(password)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     characters = ... → stored in memory
#     password = ... → stored in memory
#     print(password)
#     characters = ... → stored in memory
#     password = ... → stored in memory
#     print(password)
#     characters = ... → stored in memory
#     password = ... → stored in memory
#     print(password)

"""

"""
Problem 932:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import random
#   2. animals = ... → assigns value to variable
#   3. random_animal = ... → assigns value to variable
#   4. print(random_animal)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import random
#   8. animals = ... → assigns value to variable
#   9. random_animal = ... → assigns value to variable
#   10. print(random_animal)
#   11. import random
#   12. animals = ... → assigns value to variable
#   13. random_animal = ... → assigns value to variable
#   14. print(random_animal)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     animals = ... → stored in memory
#     random_animal = ... → stored in memory
#     print(random_animal)
#     animals = ... → stored in memory
#     random_animal = ... → stored in memory
#     print(random_animal)
#     animals = ... → stored in memory
#     random_animal = ... → stored in memory
#     print(random_animal)

"""

"""
Problem 933:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   a / b → Python looks up a, then b, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 934:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(value)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(value)
#   7. statement
#   8. print(value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(value)
#     (statement runs)
#     print(value)
#     (statement runs)
#     print(value)

"""

"""
Problem 935:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(value)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(value)
#   7. statement
#   8. print(value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(value)
#     (statement runs)
#     print(value)
#     (statement runs)
#     print(value)

"""

"""
Problem 936:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(num)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(num)
#   7. statement
#   8. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(num)
#     (statement runs)
#     print(num)
#     (statement runs)
#     print(num)

"""

"""
Problem 937:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   'Age: ' + 25 → Python looks up 'Age: ', then 25, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 938:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: open()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(content)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(content)
#   7. statement
#   8. print(content)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(content)
#     (statement runs)
#     print(content)
#     (statement runs)
#     print(content)

"""

"""
Problem 939:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class Person → defines a new type
#     def __init__(...) → method of Person
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: Person()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class Person → class definition stored
#   2. statement
#   3. print(age)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. obj = ... → assigns value to variable
#   7. print(obj)
#   8. print(Person(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (Person) are defined — methods are stored, not called
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(age)
#     obj = ... → stored in memory
#     print(obj)
#     print(Person(...))

"""

"""
Problem 940:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: +
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x + 10 → Python looks up x, then 10, then applies +
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 941:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: open()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 942:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   a / b → Python looks up a, then b, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 943:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   10 / 0 → Python looks up 10, then 0, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 944:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Classes:
#   class NegativeNumberError(Exception) → inherits from parent class
#
#   def check_number(n) → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: if/else | try/except
# Operators: <
# Built-in functions: NegativeNumberError()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   check_number(n) → each argument is evaluated first, then the function body runs
#   check_number(15) runs first → then print() outputs the result
#   n < 0 → evaluated as True or False (determines which branch runs)
#   check_number(?) → each argument is evaluated first, then the function body runs
#   check_number(15) → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. class NegativeNumberError(Exception) → class definition stored
#   2. def check_number(n) → stored in memory, not executed
#   3. statement
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. n = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. print(check_number(...))
#      → calls check_number(n) with n=15
#      → 9.1 if n < 0:
#      → 9.1     raise NegativeNumberError('Negative numbers are not allowed')
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Classes (NegativeNumberError) are defined — methods are stored, not called
#   Functions (check_number, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     n = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     print(check_number(...)) → check_number() runs first → value passed to print()
#   Branch: condition picks one path — other branches are skipped

"""

"""
Problem 945:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: /
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   '10' / 2 → Python looks up '10', then 2, then applies /
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. print(result)
#   3. def solve() → stored in memory, not executed
#   4. def solve() → stored in memory, not executed
#   5. statement
#   6. print(result)
#   7. statement
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)
#     (statement runs)
#     print(result)

"""

"""
Problem 946:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Operators: >=
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   x >= 0 → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 947:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: MemoryError()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 948:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def infinite_recursion() → defines a reusable function
#   return → sends a value back to the caller
#
# Keywords: try/except
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   infinite_recursion() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. def infinite_recursion() → stored in memory, not executed
#   2. statement
#   3. def infinite_recursion() → stored in memory, not executed
#   4. def infinite_recursion() → stored in memory, not executed
#   5. result = ... → assigns value to variable
#   6. print(result)
#   7. result = ... → assigns value to variable
#   8. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (infinite_recursion) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     result = ... → stored in memory
#     print(result)
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 949:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 950:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
# Built-in functions: open()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 951:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Keywords: try/except
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. statement
#   2. def solve() → stored in memory, not executed
#   3. def solve() → stored in memory, not executed
#   4. statement
#   5. statement
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     (statement runs)
#     (statement runs)
#     (statement runs)

"""

"""
Problem 952:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. upper_text = ... → assigns value to variable
#   3. print(upper_text)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. upper_text = ... → assigns value to variable
#   8. print(upper_text)
#   9. text = ... → assigns value to variable
#   10. upper_text = ... → assigns value to variable
#   11. print(upper_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     upper_text = ... → stored in memory
#     print(upper_text)
#     text = ... → stored in memory
#     upper_text = ... → stored in memory
#     print(upper_text)
#     text = ... → stored in memory
#     upper_text = ... → stored in memory
#     print(upper_text)

"""

"""
Problem 953:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. count_a = ... → assigns value to variable
#   3. print(count_a)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. count_a = ... → assigns value to variable
#   8. print(count_a)
#   9. text = ... → assigns value to variable
#   10. count_a = ... → assigns value to variable
#   11. print(count_a)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     count_a = ... → stored in memory
#     print(count_a)
#     text = ... → stored in memory
#     count_a = ... → stored in memory
#     print(count_a)
#     text = ... → stored in memory
#     count_a = ... → stored in memory
#     print(count_a)

"""

"""
Problem 954:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. text = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     text = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 955:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. new_text = ... → assigns value to variable
#   3. print(new_text)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. new_text = ... → assigns value to variable
#   8. print(new_text)
#   9. text = ... → assigns value to variable
#   10. new_text = ... → assigns value to variable
#   11. print(new_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     new_text = ... → stored in memory
#     print(new_text)
#     text = ... → stored in memory
#     new_text = ... → stored in memory
#     print(new_text)
#     text = ... → stored in memory
#     new_text = ... → stored in memory
#     print(new_text)

"""

"""
Problem 956:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. sentence = ... → assigns value to variable
#   2. words = ... → assigns value to variable
#   3. print(words)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. sentence = ... → assigns value to variable
#   7. words = ... → assigns value to variable
#   8. print(words)
#   9. sentence = ... → assigns value to variable
#   10. words = ... → assigns value to variable
#   11. print(words)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sentence = ... → stored in memory
#     words = ... → stored in memory
#     print(words)
#     sentence = ... → stored in memory
#     words = ... → stored in memory
#     print(words)
#     sentence = ... → stored in memory
#     words = ... → stored in memory
#     print(words)

"""

"""
Problem 957:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. words = ... → assigns value to variable
#   2. sentence = ... → assigns value to variable
#   3. print(sentence)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. words = ... → assigns value to variable
#   7. sentence = ... → assigns value to variable
#   8. print(sentence)
#   9. words = ... → assigns value to variable
#   10. sentence = ... → assigns value to variable
#   11. print(sentence)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     words = ... → stored in memory
#     sentence = ... → stored in memory
#     print(sentence)
#     words = ... → stored in memory
#     sentence = ... → stored in memory
#     print(sentence)
#     words = ... → stored in memory
#     sentence = ... → stored in memory
#     print(sentence)

"""

"""
Problem 958:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. trimmed_text = ... → assigns value to variable
#   3. print(trimmed_text)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. trimmed_text = ... → assigns value to variable
#   8. print(trimmed_text)
#   9. text = ... → assigns value to variable
#   10. trimmed_text = ... → assigns value to variable
#   11. print(trimmed_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     trimmed_text = ... → stored in memory
#     print(trimmed_text)
#     text = ... → stored in memory
#     trimmed_text = ... → stored in memory
#     print(trimmed_text)
#     text = ... → stored in memory
#     trimmed_text = ... → stored in memory
#     print(trimmed_text)

"""

"""
Problem 959:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. index_30 = ... → assigns value to variable
#   3. print(index_30)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. index_30 = ... → assigns value to variable
#   8. print(index_30)
#   9. numbers = ... → assigns value to variable
#   10. index_30 = ... → assigns value to variable
#   11. print(index_30)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     index_30 = ... → stored in memory
#     print(index_30)
#     numbers = ... → stored in memory
#     index_30 = ... → stored in memory
#     print(index_30)
#     numbers = ... → stored in memory
#     index_30 = ... → stored in memory
#     print(index_30)

"""

"""
Problem 960:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. numbers.sort()
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. numbers.sort()
#   8. print(numbers)
#   9. numbers = ... → assigns value to variable
#   10. numbers.sort()
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     numbers.sort()
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.sort()
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.sort()
#     print(numbers)

"""

"""
Problem 961:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. numbers.reverse()
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. numbers.reverse()
#   8. print(numbers)
#   9. numbers = ... → assigns value to variable
#   10. numbers.reverse()
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     numbers.reverse()
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.reverse()
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.reverse()
#     print(numbers)

"""

"""
Problem 962:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. numbers.append(4)
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. numbers.append(4)
#   8. print(numbers)
#   9. numbers = ... → assigns value to variable
#   10. numbers.append(4)
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     numbers.append(4)
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.append(4)
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.append(4)
#     print(numbers)

"""

"""
Problem 963:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. last_item = ... → assigns value to variable
#   3. print(numbers)
#   4. print(last_item)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. last_item = ... → assigns value to variable
#   9. print(numbers)
#   10. print(last_item)
#   11. numbers = ... → assigns value to variable
#   12. last_item = ... → assigns value to variable
#   13. print(numbers)
#   14. print(last_item)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     last_item = ... → stored in memory
#     print(numbers)
#     print(last_item)
#     numbers = ... → stored in memory
#     last_item = ... → stored in memory
#     print(numbers)
#     print(last_item)
#     numbers = ... → stored in memory
#     last_item = ... → stored in memory
#     print(numbers)
#     print(last_item)

"""

"""
Problem 964:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. numbers.remove(10)
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. numbers.remove(10)
#   8. print(numbers)
#   9. numbers = ... → assigns value to variable
#   10. numbers.remove(10)
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     numbers.remove(10)
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.remove(10)
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.remove(10)
#     print(numbers)

"""

"""
Problem 965:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. person = ... → assigns value to variable
#   2. keys = ... → assigns value to variable
#   3. print(list(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     keys = ... → stored in memory
#     print(list(...))

"""

"""
Problem 966:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
# Built-in functions: list()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. person = ... → assigns value to variable
#   2. values = ... → assigns value to variable
#   3. print(list(...))
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     values = ... → stored in memory
#     print(list(...))

"""

"""
Problem 967:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. dict1 = ... → assigns value to variable
#   2. dict2 = ... → assigns value to variable
#   3. dict1.update(dict2)
#   4. print(dict1)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. dict1 = ... → assigns value to variable
#   8. dict2 = ... → assigns value to variable
#   9. dict1.update(dict2)
#   10. print(dict1)
#   11. dict1 = ... → assigns value to variable
#   12. dict2 = ... → assigns value to variable
#   13. dict1.update(dict2)
#   14. print(dict1)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     dict1.update(dict2)
#     print(dict1)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     dict1.update(dict2)
#     print(dict1)
#     dict1 = ... → stored in memory
#     dict2 = ... → stored in memory
#     dict1.update(dict2)
#     print(dict1)

"""

"""
Problem 968:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. person = ... → assigns value to variable
#   2. person.pop('age')
#   3. print(person)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. person = ... → assigns value to variable
#   7. person.pop('age')
#   8. print(person)
#   9. person = ... → assigns value to variable
#   10. person.pop('age')
#   11. print(person)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     person = ... → stored in memory
#     person.pop('age')
#     print(person)
#     person = ... → stored in memory
#     person.pop('age')
#     print(person)
#     person = ... → stored in memory
#     person.pop('age')
#     print(person)

"""

"""
Problem 969:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: in
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   3 in numbers → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. result = ... → assigns value to variable
#   3. print(result)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. result = ... → assigns value to variable
#   8. print(result)
#   9. numbers = ... → assigns value to variable
#   10. result = ... → assigns value to variable
#   11. print(result)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)
#     numbers = ... → stored in memory
#     result = ... → stored in memory
#     print(result)

"""

"""
Problem 970:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. removed_element = ... → assigns value to variable
#   3. print(removed_element)
#   4. print(numbers)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. numbers = ... → assigns value to variable
#   8. removed_element = ... → assigns value to variable
#   9. print(removed_element)
#   10. print(numbers)
#   11. numbers = ... → assigns value to variable
#   12. removed_element = ... → assigns value to variable
#   13. print(removed_element)
#   14. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     removed_element = ... → stored in memory
#     print(removed_element)
#     print(numbers)
#     numbers = ... → stored in memory
#     removed_element = ... → stored in memory
#     print(removed_element)
#     print(numbers)
#     numbers = ... → stored in memory
#     removed_element = ... → stored in memory
#     print(removed_element)
#     print(numbers)

"""

"""
Problem 971:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. numbers.clear()
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. numbers.clear()
#   8. print(numbers)
#   9. numbers = ... → assigns value to variable
#   10. numbers.clear()
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     numbers.clear()
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.clear()
#     print(numbers)
#     numbers = ... → stored in memory
#     numbers.clear()
#     print(numbers)

"""

"""
Problem 972:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num_str = ... → assigns value to variable
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. num_str = ... → assigns value to variable
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. num_str = ... → assigns value to variable
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     num_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     num_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 973:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num_str = ... → assigns value to variable
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. num_str = ... → assigns value to variable
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. num_str = ... → assigns value to variable
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     num_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     num_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 974:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Operators: ==
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   bool_str.lower(...) == 'true' → evaluated as True or False (determines which branch runs)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. bool_str = ... → assigns value to variable
#   2. boolean_value = ... → assigns value to variable
#   3. print(boolean_value)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. bool_str = ... → assigns value to variable
#   7. boolean_value = ... → assigns value to variable
#   8. print(boolean_value)
#   9. bool_str = ... → assigns value to variable
#   10. boolean_value = ... → assigns value to variable
#   11. print(boolean_value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     bool_str = ... → stored in memory
#     boolean_value = ... → stored in memory
#     print(boolean_value)
#     bool_str = ... → stored in memory
#     boolean_value = ... → stored in memory
#     print(boolean_value)
#     bool_str = ... → stored in memory
#     boolean_value = ... → stored in memory
#     print(boolean_value)

"""

"""
Problem 975:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: list, map()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. num_str = ... → assigns value to variable
#   2. numbers = ... → assigns value to variable
#   3. print(numbers)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. num_str = ... → assigns value to variable
#   7. numbers = ... → assigns value to variable
#   8. print(numbers)
#   9. num_str = ... → assigns value to variable
#   10. numbers = ... → assigns value to variable
#   11. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     num_str = ... → stored in memory
#     numbers = ... → stored in memory
#     print(numbers)
#     num_str = ... → stored in memory
#     numbers = ... → stored in memory
#     print(numbers)
#     num_str = ... → stored in memory
#     numbers = ... → stored in memory
#     print(numbers)

"""

"""
Problem 976:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from datetime import datetime
#   2. date_str = ... → assigns value to variable
#   3. parsed_date = ... → assigns value to variable
#   4. print(parsed_date)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from datetime import datetime
#   8. date_str = ... → assigns value to variable
#   9. parsed_date = ... → assigns value to variable
#   10. print(parsed_date)
#   11. from datetime import datetime
#   12. date_str = ... → assigns value to variable
#   13. parsed_date = ... → assigns value to variable
#   14. print(parsed_date)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     date_str = ... → stored in memory
#     parsed_date = ... → stored in memory
#     print(parsed_date)
#     date_str = ... → stored in memory
#     parsed_date = ... → stored in memory
#     print(parsed_date)
#     date_str = ... → stored in memory
#     parsed_date = ... → stored in memory
#     print(parsed_date)

"""

"""
Problem 977:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. text = ... → assigns value to variable
#   3. numbers = ... → assigns value to variable
#   4. print(numbers)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import re
#   8. text = ... → assigns value to variable
#   9. numbers = ... → assigns value to variable
#   10. print(numbers)
#   11. import re
#   12. text = ... → assigns value to variable
#   13. numbers = ... → assigns value to variable
#   14. print(numbers)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     numbers = ... → stored in memory
#     print(numbers)
#     text = ... → stored in memory
#     numbers = ... → stored in memory
#     print(numbers)
#     text = ... → stored in memory
#     numbers = ... → stored in memory
#     print(numbers)

"""

"""
Problem 978:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 979:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#   2. data = ... → assigns value to variable
#   3. json_str = ... → assigns value to variable
#   4. print(json_str)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import json
#   8. data = ... → assigns value to variable
#   9. json_str = ... → assigns value to variable
#   10. print(json_str)
#   11. import json
#   12. data = ... → assigns value to variable
#   13. json_str = ... → assigns value to variable
#   14. print(json_str)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     data = ... → stored in memory
#     json_str = ... → stored in memory
#     print(json_str)
#     data = ... → stored in memory
#     json_str = ... → stored in memory
#     print(json_str)
#     data = ... → stored in memory
#     json_str = ... → stored in memory
#     print(json_str)

"""

"""
Problem 980:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import xml.etree.ElementTree
#   2. xml_str = ... → assigns value to variable
#   3. root = ... → assigns value to variable
#   4. name = ... → assigns value to variable
#   5. print(name)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import xml.etree.ElementTree
#   9. xml_str = ... → assigns value to variable
#   10. root = ... → assigns value to variable
#   11. name = ... → assigns value to variable
#   12. print(name)
#   13. import xml.etree.ElementTree
#   14. xml_str = ... → assigns value to variable
#   15. root = ... → assigns value to variable
#   16. name = ... → assigns value to variable
#   17. print(name)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     xml_str = ... → stored in memory
#     root = ... → stored in memory
#     name = ... → stored in memory
#     print(name)
#     xml_str = ... → stored in memory
#     root = ... → stored in memory
#     name = ... → stored in memory
#     print(name)
#     xml_str = ... → stored in memory
#     root = ... → stored in memory
#     name = ... → stored in memory
#     print(name)

"""

"""
Problem 981:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: parse_qs()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from urllib.parse import parse_qs
#   2. query_str = ... → assigns value to variable
#   3. parsed_data = ... → assigns value to variable
#   4. print(parsed_data)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from urllib.parse import parse_qs
#   8. query_str = ... → assigns value to variable
#   9. parsed_data = ... → assigns value to variable
#   10. print(parsed_data)
#   11. from urllib.parse import parse_qs
#   12. query_str = ... → assigns value to variable
#   13. parsed_data = ... → assigns value to variable
#   14. print(parsed_data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     query_str = ... → stored in memory
#     parsed_data = ... → stored in memory
#     print(parsed_data)
#     query_str = ... → stored in memory
#     parsed_data = ... → stored in memory
#     print(parsed_data)
#     query_str = ... → stored in memory
#     parsed_data = ... → stored in memory
#     print(parsed_data)

"""

"""
Problem 982:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import csv
#   2. from io import StringIO
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 983:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from html.parser import HTMLParser
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 984:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. from datetime import datetime
#   2. time_str = ... → assigns value to variable
#   3. parsed_time = ... → assigns value to variable
#   4. print(parsed_time)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. from datetime import datetime
#   8. time_str = ... → assigns value to variable
#   9. parsed_time = ... → assigns value to variable
#   10. print(parsed_time)
#   11. from datetime import datetime
#   12. time_str = ... → assigns value to variable
#   13. parsed_time = ... → assigns value to variable
#   14. print(parsed_time)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     time_str = ... → stored in memory
#     parsed_time = ... → stored in memory
#     print(parsed_time)
#     time_str = ... → stored in memory
#     parsed_time = ... → stored in memory
#     print(parsed_time)
#     time_str = ... → stored in memory
#     parsed_time = ... → stored in memory
#     print(parsed_time)

"""

"""
Problem 985:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. hex_str = ... → assigns value to variable
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. hex_str = ... → assigns value to variable
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. hex_str = ... → assigns value to variable
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     hex_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     hex_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     hex_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 986:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: int()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. binary_str = ... → assigns value to variable
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. binary_str = ... → assigns value to variable
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. binary_str = ... → assigns value to variable
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     binary_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     binary_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     binary_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 987:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. sci_str = ... → assigns value to variable
#   2. num = ... → assigns value to variable
#   3. print(num)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. sci_str = ... → assigns value to variable
#   7. num = ... → assigns value to variable
#   8. print(num)
#   9. sci_str = ... → assigns value to variable
#   10. num = ... → assigns value to variable
#   11. print(num)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     sci_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     sci_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)
#     sci_str = ... → stored in memory
#     num = ... → stored in memory
#     print(num)

"""

"""
Problem 988:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import configparser
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 989:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   No code runs at the top level — functions must be called externally

"""

"""
Problem 991:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import json
#   2. json_str = ... → assigns value to variable
#   3. data = ... → assigns value to variable
#   4. print(data)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import json
#   8. json_str = ... → assigns value to variable
#   9. data = ... → assigns value to variable
#   10. print(data)
#   11. import json
#   12. json_str = ... → assigns value to variable
#   13. data = ... → assigns value to variable
#   14. print(data)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     json_str = ... → stored in memory
#     data = ... → stored in memory
#     print(data)
#     json_str = ... → stored in memory
#     data = ... → stored in memory
#     print(data)
#     json_str = ... → stored in memory
#     data = ... → stored in memory
#     print(data)

"""

"""
Problem 992:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
# Built-in functions: float()
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import re
#   2. currency_str = ... → assigns value to variable
#   3. value = ... → assigns value to variable
#   4. print(value)
#   5. def solve() → stored in memory, not executed
#   6. def solve() → stored in memory, not executed
#   7. import re
#   8. currency_str = ... → assigns value to variable
#   9. value = ... → assigns value to variable
#   10. print(value)
#   11. import re
#   12. currency_str = ... → assigns value to variable
#   13. value = ... → assigns value to variable
#   14. print(value)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     currency_str = ... → stored in memory
#     value = ... → stored in memory
#     print(value)
#     currency_str = ... → stored in memory
#     value = ... → stored in memory
#     print(value)
#     currency_str = ... → stored in memory
#     value = ... → stored in memory
#     print(value)

"""

"""
Problem 993:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. import argparse
#   2. parser = ... → assigns value to variable
#   3. parser.add_argument('--name')
#   4. args = ... → assigns value to variable
#   5. print(?)
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#   8. import argparse
#   9. parser = ... → assigns value to variable
#   10. parser.add_argument('--name')
#   11. args = ... → assigns value to variable
#   12. print(?)
#   13. import argparse
#   14. parser = ... → assigns value to variable
#   15. parser.add_argument('--name')
#   16. args = ... → assigns value to variable
#   17. print(?)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     parser = ... → stored in memory
#     parser.add_argument('--name')
#     args = ... → stored in memory
#     print(?)
#     parser = ... → stored in memory
#     parser.add_argument('--name')
#     args = ... → stored in memory
#     print(?)
#     parser = ... → stored in memory
#     parser.add_argument('--name')
#     args = ... → stored in memory
#     print(?)

"""

"""
Problem 994:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. sliced_text = ... → assigns value to variable
#   3. print(sliced_text)
#   4. def main() → stored in memory, not executed
#   5. main() → calls main() with 
#      → 5.1 pass
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)
#     main() → jumps into main(), runs body, returns

"""

"""
Problem 995:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. sliced_text = ... → assigns value to variable
#   3. print(sliced_text)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. sliced_text = ... → assigns value to variable
#   8. print(sliced_text)
#   9. text = ... → assigns value to variable
#   10. sliced_text = ... → assigns value to variable
#   11. print(sliced_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)

"""

"""
Problem 996:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. sliced_list = ... → assigns value to variable
#   3. print(sliced_list)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. sliced_list = ... → assigns value to variable
#   8. print(sliced_list)
#   9. numbers = ... → assigns value to variable
#   10. sliced_list = ... → assigns value to variable
#   11. print(sliced_list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     sliced_list = ... → stored in memory
#     print(sliced_list)
#     numbers = ... → stored in memory
#     sliced_list = ... → stored in memory
#     print(sliced_list)
#     numbers = ... → stored in memory
#     sliced_list = ... → stored in memory
#     print(sliced_list)

"""

"""
Problem 997:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. numbers = ... → assigns value to variable
#   2. sliced_list = ... → assigns value to variable
#   3. print(sliced_list)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. numbers = ... → assigns value to variable
#   7. sliced_list = ... → assigns value to variable
#   8. print(sliced_list)
#   9. numbers = ... → assigns value to variable
#   10. sliced_list = ... → assigns value to variable
#   11. print(sliced_list)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     numbers = ... → stored in memory
#     sliced_list = ... → stored in memory
#     print(sliced_list)
#     numbers = ... → stored in memory
#     sliced_list = ... → stored in memory
#     print(sliced_list)
#     numbers = ... → stored in memory
#     sliced_list = ... → stored in memory
#     print(sliced_list)

"""

"""
Problem 998:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. sliced_text = ... → assigns value to variable
#   3. print(sliced_text)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. sliced_text = ... → assigns value to variable
#   8. print(sliced_text)
#   9. text = ... → assigns value to variable
#   10. sliced_text = ... → assigns value to variable
#   11. print(sliced_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)

"""

"""
Problem 999:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def main() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   main() → each argument is evaluated first, then the function body runs
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. sliced_text = ... → assigns value to variable
#   3. print(sliced_text)
#   4. def main() → stored in memory, not executed
#   5. main() → calls main() with 
#      → 5.1 pass
#   6. def solve() → stored in memory, not executed
#   7. def solve() → stored in memory, not executed
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (main, solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     sliced_text = ... → stored in memory
#     print(sliced_text)
#     main() → jumps into main(), runs body, returns

"""

"""
Problem 1000:
#
# ------
# SYNTAX:
# Syntax shows the Python keywords, operators, and structures used in
# this solution.
#
#   def solve() → defines a reusable function
#   (no return statement — the function prints output directly)
#
#
# ------
# EVALUATION ORDER:
# Evaluation Order shows how Python reads and processes expressions
# step by step. Inner expressions are evaluated first, then outer ones.
#
#   (expressions are evaluated left to right, inner calls first)
#
# ------
# EXECUTION ORDER:
# Execution Order lists every statement in the sequence Python runs them.
# 'def' and 'class' blocks are only stored, not executed yet.
#
#   1. text = ... → assigns value to variable
#   2. reversed_text = ... → assigns value to variable
#   3. print(reversed_text)
#   4. def solve() → stored in memory, not executed
#   5. def solve() → stored in memory, not executed
#   6. text = ... → assigns value to variable
#   7. reversed_text = ... → assigns value to variable
#   8. print(reversed_text)
#   9. text = ... → assigns value to variable
#   10. reversed_text = ... → assigns value to variable
#   11. print(reversed_text)
#
# ------
# EXECUTION FLOW:
# Execution Flow tracks how Python jumps between different parts of the
# code — which blocks are skipped and where execution actually starts.
#
#   Functions (solve) are defined — skipped at runtime, stored in memory
#
#   Execution begins at the top-level code (outside def/class blocks):
#     text = ... → stored in memory
#     reversed_text = ... → stored in memory
#     print(reversed_text)
#     text = ... → stored in memory
#     reversed_text = ... → stored in memory
#     print(reversed_text)
#     text = ... → stored in memory
#     reversed_text = ... → stored in memory
#     print(reversed_text)

"""

