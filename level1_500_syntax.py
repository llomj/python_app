# Syntax documentation for level 1 (problems 1-500)
# Auto-generated by gen_syntax_files.py

"""
Problem 1:
#
# ===== SYNTAX =====
#   def add_number(a, b)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(add_number(5, 5)): the inner add_number() executes FIRST, then print() outputs the result
#   print(add_num(5, 5)): the inner add_num() executes FIRST, then print() outputs the result
#   add_number(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(add_number(20, 30)): the inner add_number() executes FIRST, then print() outputs the result
#   a + b: first get a, then get b, then compute a + b
#   add_number(5, 5): gets the literal 5, gets the literal 5, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   add_num(5, 5): gets the literal 5, gets the literal 5, then runs the function body
#   add_number(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add_number(a, b) — stored for later
#   2. Run print(add_number(...))
#       calls add_number with: a=5, b=5
#         2.1 Import operator
#         2.2 Return from function
#   3. Define def add_num(a, b) — stored for later
#   4. Run print(add_num(...))
#       calls add_num with: a=5, b=5
#         4.1 Assign to result
#         4.2 Return from function
#   5. Define def add_number(a, b) — stored for later
#   6. Define def add_number(a, b) — stored for later
#   7. Assign to a
#   8. Assign to b
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(add_number(...))
#       calls add_number with: a=20, b=30
#         11.1 Import operator
#         11.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_number, add_num, add_number, add_number are just stored
#   Execution starts at the non-def lines below:
#      print(add_number(5, 5)) → function runs first, then print shows the result
#      print(add_num(5, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_number(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 2:
#
# ===== SYNTAX =====
#   def multiply_numbers(a, b)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   abs() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: * >=
#
# ===== EVALUATION ORDER =====
#   print(multiply_numbers(5, 5)): the inner multiply_numbers() executes FIRST, then print() outputs the result
#   print(multiply_numbers(3, 7)): the inner multiply_numbers() executes FIRST, then print() outputs the result
#   print(multiply_numbers(4, 6)): the inner multiply_numbers() executes FIRST, then print() outputs the result
#   multiply_numbers(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(multiply_numbers(20, 30)): the inner multiply_numbers() executes FIRST, then print() outputs the result
#   a * b: first get a, then get b, then compute a * b
#   multiply_numbers(5, 5): gets the literal 5, gets the literal 5, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#   multiply_numbers(3, 7): gets the literal 3, gets the literal 7, then runs the function body
#   multiply_numbers(4, 6): gets the literal 4, gets the literal 6, then runs the function body
#   multiply_numbers(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_numbers(a, b) — stored for later
#   2. Run print(multiply_numbers(...))
#       calls multiply_numbers with: a=5, b=5
#         2.1 Return from function
#   3. Define def multiply_numbers(a, b) — stored for later
#   4. Run print(multiply_numbers(...))
#       calls multiply_numbers with: a=3, b=7
#         4.1 Return from function
#   5. Define def multiply_numbers(a, b) — stored for later
#   6. Run print(multiply_numbers(...))
#       calls multiply_numbers with: a=4, b=6
#         6.1 Return from function
#   7. Define def multiply_numbers(a, b) — stored for later
#   8. Define def multiply_numbers(a, b) — stored for later
#   9. Assign to a
#   10. Assign to b
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(multiply_numbers(...))
#       calls multiply_numbers with: a=20, b=30
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_numbers, multiply_numbers, multiply_numbers, multiply_numbers, multiply_numbers are just stored
#   Execution starts at the non-def lines below:
#      print(multiply_numbers(5, 5)) → function runs first, then print shows the result
#      print(multiply_numbers(3, 7)) → function runs first, then print shows the result
#      print(multiply_numbers(4, 6)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_numbers(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 3:
#
# ===== SYNTAX =====
#   def find_max(number)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(find_max(result)): the inner find_max() executes FIRST, then print() outputs the result
#   print(find_max(numbers_list)): the inner find_max() executes FIRST, then print() outputs the result
#   find_max(number): looks up variable 'number', then runs the function body
#   print(find_max(?)): the inner find_max() executes FIRST, then print() outputs the result
#   find_max(result): looks up variable 'result', then runs the function body
#   find_max(numbers_list): looks up variable 'numbers_list', then runs the function body
#   find_max(?): resolves the value, then runs the function body
#   num > max_number: is this True or False? The answer picks which branch
#   item > result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_max(number) — stored for later
#   2. Assign to result
#   3. Run print(find_max(...))
#       calls find_max with: number=result
#         3.1 Assign to result
#         3.2 For loop over number
#         3.3 Return from function
#   4. Define def find_max(numbers) — stored for later
#   5. Assign to numbers_list
#   6. Run print('The maximum number in the list is:', find_max(...))
#   7. Define def find_max(number) — stored for later
#   8. Define def find_max(number) — stored for later
#   9. Assign to number
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_max(...))
#       calls find_max with: number=[4, 1, 8, 6, 3]
#         12.1 Assign to result
#         12.2 For loop over number
#         12.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_max, find_max, find_max, find_max are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(find_max(result)) → function runs first, then print shows the result
#      numbers_list = ... (stored in memory)
#      print('The maximum number in the list is:', find_max(...))
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_max(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 4:
#
# ===== SYNTAX =====
#   def calculate_average(number)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   for
#   in
#   operators: /
#
# ===== EVALUATION ORDER =====
#   print(calculate_average(lst)): the inner calculate_average() executes FIRST, then print() outputs the result
#   print(cal_average(lst)): the inner cal_average() executes FIRST, then print() outputs the result
#   calculate_average(number): looks up variable 'number', then runs the function body
#   print(calculate_average(?)): the inner calculate_average() executes FIRST, then print() outputs the result
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   calculate_average(lst): looks up variable 'lst', then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   cal_average(lst): looks up variable 'lst', then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   calculate_average(?): resolves the value, then runs the function body
#   total / count: first get total, then get count, then compute total / count
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_average(number) — stored for later
#   2. Assign to lst
#   3. Run print(calculate_average(...))
#       calls calculate_average with: number=lst
#         3.1 Assign to total
#         3.2 Assign to count
#         3.3 For loop over number
#         3.4 Return from function
#   4. Define def cal_average(number) — stored for later
#   5. Assign to lst
#   6. Run print(cal_average(...))
#       calls cal_average with: number=lst
#         6.1 Assign to result
#         6.2 Return from function
#   7. Define def calculate_average(number) — stored for later
#   8. Define def calculate_average(number) — stored for later
#   9. Assign to number
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(calculate_average(...))
#       calls calculate_average with: number=[4, 1, 8, 6, 3]
#         12.1 Assign to total
#         12.2 Assign to count
#         12.3 For loop over number
#         12.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_average, cal_average, calculate_average, calculate_average are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(calculate_average(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(cal_average(lst)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_average(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from number each round

"""

"""
Problem 5:
#
# ===== SYNTAX =====
#   def count_vowels(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(count_vowels(result)): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Jonathan')): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Python')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(string): looks up variable 'string', then runs the function body
#   print(count_vowels('world')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(result): looks up variable 'result', then runs the function body
#   count_vowels('Jonathan'): gets the literal 'Jonathan', then runs the function body
#   count_vowels('Python'): gets the literal 'Python', then runs the function body
#   count_vowels('world'): gets the literal 'world', then runs the function body
#   char in 'aeiou': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(string) — stored for later
#   2. Assign to result
#   3. Run print(count_vowels(...))
#       calls count_vowels with: string=result
#         3.1 Return from function
#   4. Define def count_vowels(string) — stored for later
#   5. Run print(count_vowels(...))
#       calls count_vowels with: string='Jonathan'
#         5.1 Return from function
#   6. Define def count_vowels(string) — stored for later
#   7. Run print(count_vowels(...))
#       calls count_vowels with: string='Python'
#         7.1 Return from function
#   8. Define def count_vowels(string) — stored for later
#   9. Define def count_vowels(string) — stored for later
#   10. Assign to string
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_vowels(...))
#       calls count_vowels with: string='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(count_vowels(result)) → function runs first, then print shows the result
#      print(count_vowels('Jonathan')) → function runs first, then print shows the result
#      print(count_vowels('Python')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 6:
#
# ===== SYNTAX =====
#   def reverse_string(string)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(reverse_string(result)): the inner reverse_string() executes FIRST, then print() outputs the result
#   print(reverse_string('hello')): the inner reverse_string() executes FIRST, then print() outputs the result
#   print(reverse_string('Python')): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(string): looks up variable 'string', then runs the function body
#   print(reverse_string('world')): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(result): looks up variable 'result', then runs the function body
#   reverse_string('hello'): gets the literal 'hello', then runs the function body
#   reverse_string('Python'): gets the literal 'Python', then runs the function body
#   reverse_string('world'): gets the literal 'world', then runs the function body
#   char + result: first get char, then get result, then compute char + result
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_string(string) — stored for later
#   2. Assign to result
#   3. Run print(reverse_string(...))
#       calls reverse_string with: string=result
#         3.1 Assign to result
#         3.2 For loop over string
#         3.3 Return from function
#   4. Define def reverse_string(string) — stored for later
#   5. Run print(reverse_string(...))
#       calls reverse_string with: string='hello'
#         5.1 Assign to result
#         5.2 For loop over string
#         5.3 Return from function
#   6. Define def reverse_string(string) — stored for later
#   7. Run print(reverse_string(...))
#       calls reverse_string with: string='Python'
#         7.1 Assign to result
#         7.2 For loop over string
#         7.3 Return from function
#   8. Define def reverse_string(string) — stored for later
#   9. Define def reverse_string(string) — stored for later
#   10. Assign to string
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(reverse_string(...))
#       calls reverse_string with: string='world'
#         13.1 Assign to result
#         13.2 For loop over string
#         13.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_string, reverse_string, reverse_string, reverse_string, reverse_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(reverse_string(result)) → function runs first, then print shows the result
#      print(reverse_string('hello')) → function runs first, then print shows the result
#      print(reverse_string('Python')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_string('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round

"""

"""
Problem 7:
#
# ===== SYNTAX =====
#   def is_even(number)
#   return — exits the function and sends back a value
#   operators: * // % ==
#
# ===== EVALUATION ORDER =====
#   print(is_even(result)): the inner is_even() executes FIRST, then print() outputs the result
#   print(is_even(8)): the inner is_even() executes FIRST, then print() outputs the result
#   print(is_even(12)): the inner is_even() executes FIRST, then print() outputs the result
#   is_even(number): looks up variable 'number', then runs the function body
#   print(is_even(25)): the inner is_even() executes FIRST, then print() outputs the result
#   is_even(result): looks up variable 'result', then runs the function body
#   is_even(8): gets the literal 8, then runs the function body
#   is_even(12): gets the literal 12, then runs the function body
#   is_even(25): gets the literal 25, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   number & 1: first get number, then get 1, then compute number & 1
#   ? * 2: first get ?, then get 2, then compute ? * 2
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 2: first get number, then get 2, then compute number % 2
#   number // 2: first get number, then get 2, then compute number // 2
#
# ===== EXECUTION ORDER =====
#   1. Define def is_even(number) — stored for later
#   2. Assign to result
#   3. Run print(is_even(...))
#       calls is_even with: number=result
#         3.1 Return from function
#   4. Define def is_even(number) — stored for later
#   5. Run print(is_even(...))
#       calls is_even with: number=8
#         5.1 Return from function
#   6. Define def is_even(number) — stored for later
#   7. Run print(is_even(...))
#       calls is_even with: number=12
#         7.1 Return from function
#   8. Define def is_even(number) — stored for later
#   9. Define def is_even(number) — stored for later
#   10. Assign to number
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(is_even(...))
#       calls is_even with: number=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_even, is_even, is_even, is_even, is_even are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(is_even(result)) → function runs first, then print shows the result
#      print(is_even(8)) → function runs first, then print shows the result
#      print(is_even(12)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_even(25)) → function runs first, then print shows the result

"""

"""
Problem 8:
#
# ===== SYNTAX =====
#   def cal_square(number)
#   return — exits the function and sends back a value
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(cal_square(lst)): the inner cal_square() executes FIRST, then print() outputs the result
#   print(calculate_square(6)): the inner calculate_square() executes FIRST, then print() outputs the result
#   cal_square(number): looks up variable 'number', then runs the function body
#   print(cal_square(25)): the inner cal_square() executes FIRST, then print() outputs the result
#   number ** 2: first get number, then get 2, then compute number ** 2
#   cal_square(lst): looks up variable 'lst', then runs the function body
#   number ** 2: first get number, then get 2, then compute number ** 2
#   calculate_square(6): gets the literal 6, then runs the function body
#   cal_square(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def cal_square(number) — stored for later
#   2. Assign to lst
#   3. Run print(cal_square(...))
#       calls cal_square with: number=lst
#         3.1 Import math
#         3.2 Return from function
#   4. Define def calculate_square(number) — stored for later
#   5. Run print(calculate_square(...))
#       calls calculate_square with: number=6
#         5.1 Assign to square
#         5.2 Return from function
#   6. Define def cal_square(number) — stored for later
#   7. Define def cal_square(number) — stored for later
#   8. Assign to number
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(cal_square(...))
#       calls cal_square with: number=25
#         11.1 Import math
#         11.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions cal_square, calculate_square, cal_square, cal_square are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(cal_square(lst)) → function runs first, then print shows the result
#      print(calculate_square(6)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(cal_square(25)) → function runs first, then print shows the result

"""

"""
Problem 9:
#
# ===== SYNTAX =====
#   def greet_user(string)
#   return — exits the function and sends back a value
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   print(greet_user('Jonathan')): the inner greet_user() executes FIRST, then print() outputs the result
#   print(greet_user('Alice')): the inner greet_user() executes FIRST, then print() outputs the result
#   print(greet_user('Bob')): the inner greet_user() executes FIRST, then print() outputs the result
#   greet_user(string): looks up variable 'string', then runs the function body
#   print(greet_user('world')): the inner greet_user() executes FIRST, then print() outputs the result
#   greet_user('Jonathan'): gets the literal 'Jonathan', then runs the function body
#   ? + '!': first get ?, then get '!', then compute ? + '!'
#   greet_user('Alice'): gets the literal 'Alice', then runs the function body
#   greet_user('Bob'): gets the literal 'Bob', then runs the function body
#   'hello %s' % string: first get 'hello %s', then get string, then compute 'hello %s' % string
#   greet_user('world'): gets the literal 'world', then runs the function body
#   'Hello, ' + name: first get 'Hello, ', then get name, then compute 'Hello, ' + name
#
# ===== EXECUTION ORDER =====
#   1. Define def greet_user(string) — stored for later
#   2. Run print(greet_user(...))
#       calls greet_user with: string='Jonathan'
#         2.1 Return from function
#   3. Define def greet_user(name) — stored for later
#   4. Run print(greet_user(...))
#       calls greet_user with: string='Alice'
#         4.1 Return from function
#   5. Define def greet_user(name) — stored for later
#   6. Run print(greet_user(...))
#       calls greet_user with: string='Bob'
#         6.1 Return from function
#   7. Define def greet_user(string) — stored for later
#   8. Define def greet_user(string) — stored for later
#   9. Assign to string
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(greet_user(...))
#       calls greet_user with: string='world'
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet_user, greet_user, greet_user, greet_user, greet_user are just stored
#   Execution starts at the non-def lines below:
#      print(greet_user('Jonathan')) → function runs first, then print shows the result
#      print(greet_user('Alice')) → function runs first, then print shows the result
#      print(greet_user('Bob')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet_user('world')) → function runs first, then print shows the result

"""

"""
Problem 10:
#
# ===== SYNTAX =====
#   def square_lst(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(square_lst(result)): the inner square_lst() executes FIRST, then print() outputs the result
#   print(square_lst(?)): the inner square_lst() executes FIRST, then print() outputs the result
#   print(square_lst(?)): the inner square_lst() executes FIRST, then print() outputs the result
#   square_lst(numbers): looks up variable 'numbers', then runs the function body
#   print(square_lst(?)): the inner square_lst() executes FIRST, then print() outputs the result
#   square_lst(result): looks up variable 'result', then runs the function body
#   square_lst(?): resolves the value, then runs the function body
#   square_lst(?): resolves the value, then runs the function body
#   square_lst(?): resolves the value, then runs the function body
#   n ** 2: first get n, then get 2, then compute n ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_lst(numbers) — stored for later
#   2. Assign to result
#   3. Run print(square_lst(...))
#       calls square_lst with: numbers=result
#         3.1 Return from function
#   4. Define def square_lst(numbers) — stored for later
#   5. Run print(square_lst(...))
#       calls square_lst with: numbers=[1, 2, 3]
#         5.1 Return from function
#   6. Define def square_lst(numbers) — stored for later
#   7. Run print(square_lst(...))
#       calls square_lst with: numbers=[3, 4, 5]
#         7.1 Return from function
#   8. Define def square_lst(numbers) — stored for later
#   9. Define def square_lst(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(square_lst(...))
#       calls square_lst with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_lst, square_lst, square_lst, square_lst, square_lst are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(square_lst(result)) → function runs first, then print shows the result
#      print(square_lst(?)) → function runs first, then print shows the result
#      print(square_lst(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 11:
#
# ===== SYNTAX =====
#   def covert_uppercase(string)
#   return — exits the function and sends back a value
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: - <=
#
# ===== EVALUATION ORDER =====
#   print(covert_uppercase('Jonathan')): the inner covert_uppercase() executes FIRST, then print() outputs the result
#   print(covert_uppercase('hello')): the inner covert_uppercase() executes FIRST, then print() outputs the result
#   covert_uppercase(string): looks up variable 'string', then runs the function body
#   print(covert_uppercase('world')): the inner covert_uppercase() executes FIRST, then print() outputs the result
#   covert_uppercase('Jonathan'): gets the literal 'Jonathan', then runs the function body
#   covert_uppercase('hello'): gets the literal 'hello', then runs the function body
#   covert_uppercase('world'): gets the literal 'world', then runs the function body
#   ord(...) - 32: first get ord(...), then get 32, then compute ord(...) - 32
#   ord(...) - 32: first get ord(...), then get 32, then compute ord(...) - 32
#   'a' <= char: is this True or False? The answer picks which branch
#   'a' <= 'z': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def covert_uppercase(string) — stored for later
#   2. Run print(covert_uppercase(...))
#       calls covert_uppercase with: string='Jonathan'
#         2.1 Return from function
#   3. Define def covert_uppercase(string) — stored for later
#   4. Run print(covert_uppercase(...))
#       calls covert_uppercase with: string='hello'
#         4.1 Return from function
#   5. Define def covert_uppercase(string) — stored for later
#   6. Define def covert_uppercase(string) — stored for later
#   7. Assign to string
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(covert_uppercase(...))
#       calls covert_uppercase with: string='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions covert_uppercase, covert_uppercase, covert_uppercase, covert_uppercase are just stored
#   Execution starts at the non-def lines below:
#      print(covert_uppercase('Jonathan')) → function runs first, then print shows the result
#      print(covert_uppercase('hello')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(covert_uppercase('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 12:
#
# ===== SYNTAX =====
#   def calculate_sum(number)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(calculate_sum(lst)): the inner calculate_sum() executes FIRST, then print() outputs the result
#   print(cal_sum(lst)): the inner cal_sum() executes FIRST, then print() outputs the result
#   calculate_sum(number): looks up variable 'number', then runs the function body
#   print(calculate_sum(?)): the inner calculate_sum() executes FIRST, then print() outputs the result
#   calculate_sum(lst): looks up variable 'lst', then runs the function body
#   cal_sum(lst): looks up variable 'lst', then runs the function body
#   calculate_sum(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_sum(number) — stored for later
#   2. Assign to lst
#   3. Run print(calculate_sum(...))
#       calls calculate_sum with: number=lst
#         3.1 Import reduce from functools
#         3.2 Import operator
#         3.3 Return from function
#   4. Define def cal_sum(numbers) — stored for later
#   5. Assign to lst
#   6. Run print(cal_sum(...))
#       calls cal_sum with: numbers=lst
#         6.1 Assign to count
#         6.2 For loop over numbers
#         6.3 Return from function
#   7. Define def calculate_sum(number) — stored for later
#   8. Define def calculate_sum(number) — stored for later
#   9. Assign to number
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(calculate_sum(...))
#       calls calculate_sum with: number=[4, 1, 8, 6, 3]
#         12.1 Import reduce from functools
#         12.2 Import operator
#         12.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_sum, cal_sum, calculate_sum, calculate_sum are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(calculate_sum(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(cal_sum(lst)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 13:
#
# ===== SYNTAX =====
#   def is_odd(number)
#   return — exits the function and sends back a value
#   operators: % == !=
#
# ===== EVALUATION ORDER =====
#   print(is_odd(10)): the inner is_odd() executes FIRST, then print() outputs the result
#   print(is_odd(7)): the inner is_odd() executes FIRST, then print() outputs the result
#   print(is_odd(5)): the inner is_odd() executes FIRST, then print() outputs the result
#   print(is_odd(9)): the inner is_odd() executes FIRST, then print() outputs the result
#   is_odd(number): looks up variable 'number', then runs the function body
#   print(is_odd(25)): the inner is_odd() executes FIRST, then print() outputs the result
#   is_odd(10): gets the literal 10, then runs the function body
#   is_odd(7): gets the literal 7, then runs the function body
#   is_odd(5): gets the literal 5, then runs the function body
#   is_odd(9): gets the literal 9, then runs the function body
#   is_odd(25): gets the literal 25, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   number & 1: first get number, then get 1, then compute number & 1
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 2: first get number, then get 2, then compute number % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def is_odd(number) — stored for later
#   2. Run print(is_odd(...))
#       calls is_odd with: number=10
#         2.1 Return from function
#   3. Run print(is_odd(...))
#       calls is_odd with: number=7
#         3.1 Return from function
#   4. Define def is_odd(number) — stored for later
#   5. Run print(is_odd(...))
#       calls is_odd with: number=5
#         5.1 Return from function
#   6. Define def is_odd(number) — stored for later
#   7. Run print(is_odd(...))
#       calls is_odd with: number=9
#         7.1 Return from function
#   8. Define def is_odd(number) — stored for later
#   9. Define def is_odd(number) — stored for later
#   10. Assign to number
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(is_odd(...))
#       calls is_odd with: number=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_odd, is_odd, is_odd, is_odd, is_odd are just stored
#   Execution starts at the non-def lines below:
#      print(is_odd(10)) → function runs first, then print shows the result
#      print(is_odd(7)) → function runs first, then print shows the result
#      print(is_odd(5)) → function runs first, then print shows the result
#      print(is_odd(9)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_odd(25)) → function runs first, then print shows the result

"""

"""
Problem 14:
#
# ===== SYNTAX =====
#   def concatenate_strings(string1, string2)
#   return — exits the function and sends back a value
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   print(concatenate_strings(lst1, lst2)): the inner concatenate_strings() executes FIRST, then print() outputs the result
#   print(concatenate_string('Jonathan', 'noll')): the inner concatenate_string() executes FIRST, then print() outputs the result
#   concatenate_strings(string1, string2): looks up variable 'string1', looks up variable 'string2', then runs the function body
#   print(concatenate_strings('world', 'python')): the inner concatenate_strings() executes FIRST, then print() outputs the result
#   string1 + string2: first get string1, then get string2, then compute string1 + string2
#   concatenate_strings(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   string1 + string2: first get string1, then get string2, then compute string1 + string2
#   concatenate_string('Jonathan', 'noll'): gets the literal 'Jonathan', gets the literal 'noll', then runs the function body
#   '%s%s' % (string1, string2): first get '%s%s', then get (string1, string2), then compute '%s%s' % (string1, string2)
#   concatenate_strings('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def concatenate_strings(string1, string2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(concatenate_strings(...))
#       calls concatenate_strings with: string1=lst1, string2=lst2
#         4.1 Return from function
#   5. Define def concatenate_string(string1, string2) — stored for later
#   6. Run print(concatenate_string(...))
#       calls concatenate_string with: string1='Jonathan', string2='noll'
#         6.1 Return from function
#   7. Define def concatenate_strings(string1, string2) — stored for later
#   8. Define def concatenate_strings(string1, string2) — stored for later
#   9. Assign to string1
#   10. Assign to string2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(concatenate_strings(...))
#       calls concatenate_strings with: string1='world', string2='python'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions concatenate_strings, concatenate_string, concatenate_strings, concatenate_strings are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(concatenate_strings(lst1, lst2)) → function runs first, then print shows the result
#      print(concatenate_string('Jonathan', 'noll')) → function runs first, then print shows the result
#      string1 = ... (stored in memory)
#      string2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(concatenate_strings('world', 'python')) → function runs first, then print shows the result

"""

"""
Problem 15:
#
# ===== SYNTAX =====
#   def find_length(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(find_length('Jonathan')): the inner find_length() executes FIRST, then print() outputs the result
#   print(find_len('Jonathan')): the inner find_len() executes FIRST, then print() outputs the result
#   find_length(string): looks up variable 'string', then runs the function body
#   print(find_length('world')): the inner find_length() executes FIRST, then print() outputs the result
#   find_length('Jonathan'): gets the literal 'Jonathan', then runs the function body
#   find_len('Jonathan'): gets the literal 'Jonathan', then runs the function body
#   find_length('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_length(string) — stored for later
#   2. Run print(find_length(...))
#       calls find_length with: string='Jonathan'
#         2.1 Return from function
#   3. Define def find_len(string) — stored for later
#   4. Run print(find_len(...))
#       calls find_len with: string='Jonathan'
#         4.1 Assign to count
#         4.2 For loop over string
#         4.3 Return from function
#   5. Define def find_length(string) — stored for later
#   6. Define def find_length(string) — stored for later
#   7. Assign to string
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(find_length(...))
#       calls find_length with: string='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_length, find_len, find_length, find_length are just stored
#   Execution starts at the non-def lines below:
#      print(find_length('Jonathan')) → function runs first, then print shows the result
#      print(find_len('Jonathan')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_length('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round

"""

"""
Problem 16:
#
# ===== SYNTAX =====
#   def is_palindrome(string)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + // == !=
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome('tom')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('racecar')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('madam')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(string): looks up variable 'string', then runs the function body
#   print(is_palindrome('world')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome('tom'): gets the literal 'tom', then runs the function body
#   is_palindrome('racecar'): gets the literal 'racecar', then runs the function body
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   is_palindrome('world'): gets the literal 'world', then runs the function body
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   i + 1: first get i, then get 1, then compute i + 1
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(string) — stored for later
#   2. Run print(is_palindrome(...))
#       calls is_palindrome with: string='tom'
#         2.1 Return from function
#   3. Run print(is_palindrome(...))
#       calls is_palindrome with: string='racecar'
#         3.1 Return from function
#   4. Define def is_palindrome(string) — stored for later
#   5. Run print(is_palindrome(...))
#       calls is_palindrome with: string='madam'
#         5.1 Return from function
#   6. Define def is_palindrome(string) — stored for later
#   7. Define def is_palindrome(string) — stored for later
#   8. Assign to string
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(is_palindrome(...))
#       calls is_palindrome with: string='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      print(is_palindrome('tom')) → function runs first, then print shows the result
#      print(is_palindrome('racecar')) → function runs first, then print shows the result
#      print(is_palindrome('madam')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 17:
#
# ===== SYNTAX =====
#   def count_words(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_words(example_string)): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words(example_string)): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words(arg)): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(string): looks up variable 'string', then runs the function body
#   print(count_words('world')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(example_string): looks up variable 'example_string', then runs the function body
#   count_words(example_string): looks up variable 'example_string', then runs the function body
#   count_words(arg): looks up variable 'arg', then runs the function body
#   count_words('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words(string) — stored for later
#   2. Assign to example_string
#   3. Run print(count_words(...))
#       calls count_words with: string=example_string
#         3.1 Return from function
#   4. Define def count_words(string) — stored for later
#   5. Assign to example_string
#   6. Run print(count_words(...))
#       calls count_words with: string=example_string
#         6.1 Return from function
#   7. Define def count_words(string) — stored for later
#   8. Assign to arg
#   9. Run print(count_words(...))
#       calls count_words with: string=arg
#         9.1 Return from function
#   10. Define def count_words(string) — stored for later
#   11. Define def count_words(string) — stored for later
#   12. Assign to string
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(count_words(...))
#       calls count_words with: string='world'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words, count_words, count_words, count_words, count_words are just stored
#   Execution starts at the non-def lines below:
#      example_string = ... (stored in memory)
#      print(count_words(example_string)) → function runs first, then print shows the result
#      example_string = ... (stored in memory)
#      print(count_words(example_string)) → function runs first, then print shows the result
#      arg = ... (stored in memory)
#      print(count_words(arg)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 18:
#
# ===== SYNTAX =====
#   def find_min()
#   sorted() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   find_min(): — no arguments needed
#   print(find_min(lst)): the inner find_min() executes FIRST, then print() outputs the result
#   print(find_minimum(min_number)): the inner find_minimum() executes FIRST, then print() outputs the result
#   find_min(numbers): looks up variable 'numbers', then runs the function body
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   find_min(lst): looks up variable 'lst', then runs the function body
#   find_minimum(min_number): looks up variable 'min_number', then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   min_num < 0: is this True or False? The answer picks which branch
#   number < min_number: is this True or False? The answer picks which branch
#   min_num < 0: is this True or False? The answer picks which branch
#   min_num < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min() — stored for later
#   2. Run find_min()
#         2.1 Assign to user
#         2.2 Assign to int_number
#         2.3 For loop over user
#         2.4 Assign to min_num
#         2.5 For loop over int_number
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def find_min(numbers) — stored for later
#   4. Assign to lst
#   5. Run print(find_min(...))
#         5.1 Assign to user
#         5.2 Assign to int_number
#         5.3 For loop over user
#         5.4 Assign to min_num
#         5.5 For loop over int_number
#         5.6 Assign to result
#         5.7 Return from function
#   6. Define def find_minimum(numbers) — stored for later
#   7. Assign to min_number
#   8. Run print(find_minimum(...))
#       calls find_minimum with: numbers=min_number
#         8.1 Assign to min_number
#         8.2 For loop over numbers
#         8.3 Return from function
#   9. Define def find_min() — stored for later
#   10. Define def find_min() — stored for later
#   11. Assign to numbers
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(find_min(...))
#         14.1 Assign to user
#         14.2 Assign to int_number
#         14.3 For loop over user
#         14.4 Assign to min_num
#         14.5 For loop over int_number
#         14.6 Assign to result
#         14.7 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min, find_min, find_minimum, find_min, find_min are just stored
#   Execution starts at the non-def lines below:
#      find_min() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(find_min(lst)) → function runs first, then print shows the result
#      min_number = ... (stored in memory)
#      print(find_minimum(min_number)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 19:
#
# ===== SYNTAX =====
#   def is_even_index_sum(numbers)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(is_even_index_sum(lst)): the inner is_even_index_sum() executes FIRST, then print() outputs the result
#   print(is_even_index_sum(?)): the inner is_even_index_sum() executes FIRST, then print() outputs the result
#   print(is_even_index_sum(?)): the inner is_even_index_sum() executes FIRST, then print() outputs the result
#   is_even_index_sum(numbers): looks up variable 'numbers', then runs the function body
#   print(is_even_index_sum(?)): the inner is_even_index_sum() executes FIRST, then print() outputs the result
#   is_even_index_sum(lst): looks up variable 'lst', then runs the function body
#   is_even_index_sum(?): resolves the value, then runs the function body
#   is_even_index_sum(?): resolves the value, then runs the function body
#   is_even_index_sum(?): resolves the value, then runs the function body
#   even_index_sum % 2: first get even_index_sum, then get 2, then compute even_index_sum % 2
#   even_index_sum % 2: first get even_index_sum, then get 2, then compute even_index_sum % 2
#   even_index_sum % 2: first get even_index_sum, then get 2, then compute even_index_sum % 2
#   even_index_sum % 2: first get even_index_sum, then get 2, then compute even_index_sum % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_even_index_sum(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(is_even_index_sum(...))
#       calls is_even_index_sum with: numbers=lst
#         3.1 Return from function
#   4. Define def is_even_index_sum(numbers) — stored for later
#   5. Run print(is_even_index_sum(...))
#       calls is_even_index_sum with: numbers=[1, 2, 3, 4, 5]
#         5.1 Return from function
#   6. Run print(is_even_index_sum(...))
#       calls is_even_index_sum with: numbers=[2, 4, 6, 8]
#         6.1 Return from function
#   7. Define def is_even_index_sum(numbers) — stored for later
#   8. Define def is_even_index_sum(numbers) — stored for later
#   9. Assign to numbers
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(is_even_index_sum(...))
#       calls is_even_index_sum with: numbers=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_even_index_sum, is_even_index_sum, is_even_index_sum, is_even_index_sum are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(is_even_index_sum(lst)) → function runs first, then print shows the result
#      print(is_even_index_sum(?)) → function runs first, then print shows the result
#      print(is_even_index_sum(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_even_index_sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 20:
#
# ===== SYNTAX =====
#   def double_elements(elements)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(double_elements(lst)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(numbers_list)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(elements): looks up variable 'elements', then runs the function body
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(lst): looks up variable 'lst', then runs the function body
#   double_elements(numbers_list): looks up variable 'numbers_list', then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#   num * 2: first get num, then get 2, then compute num * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def double_elements(elements) — stored for later
#   2. Assign to lst
#   3. Run print(double_elements(...))
#       calls double_elements with: elements=lst
#         3.1 Return from function
#   4. Define def double_elements(numbers) — stored for later
#   5. Assign to numbers_list
#   6. Run print('The list with doubled elements is:', double_elements(...))
#   7. Define def double_elements(elements) — stored for later
#   8. Define def double_elements(elements) — stored for later
#   9. Assign to elements
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(double_elements(...))
#       calls double_elements with: elements=[5, 3, 8, 4]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions double_elements, double_elements, double_elements, double_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(double_elements(lst)) → function runs first, then print shows the result
#      numbers_list = ... (stored in memory)
#      print('The list with doubled elements is:', double_elements(...))
#      elements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(double_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from elements each round

"""

"""
Problem 21:
#
# ===== SYNTAX =====
#   def is_all_positive(numbers)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   len() — built-in function call
#   min() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: > <= and
#
# ===== EVALUATION ORDER =====
#   print(is_all_positive(lst)): the inner is_all_positive() executes FIRST, then print() outputs the result
#   print(is_all_positive(?)): the inner is_all_positive() executes FIRST, then print() outputs the result
#   print(is_all_positive(?)): the inner is_all_positive() executes FIRST, then print() outputs the result
#   print(is_all_positive(?)): the inner is_all_positive() executes FIRST, then print() outputs the result
#   is_all_positive(numbers): looks up variable 'numbers', then runs the function body
#   print(is_all_positive(?)): the inner is_all_positive() executes FIRST, then print() outputs the result
#   is_all_positive(lst): looks up variable 'lst', then runs the function body
#   is_all_positive(?): resolves the value, then runs the function body
#   is_all_positive(?): resolves the value, then runs the function body
#   is_all_positive(?): resolves the value, then runs the function body
#   is_all_positive(?): resolves the value, then runs the function body
#   number <= 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_all_positive(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(is_all_positive(...))
#       calls is_all_positive with: numbers=lst
#         3.1 Return from function
#   4. Define def is_all_positive(numbers) — stored for later
#   5. Run print(is_all_positive(...))
#       calls is_all_positive with: numbers=[1, 2, 3]
#         5.1 Return from function
#   6. Run print(is_all_positive(...))
#       calls is_all_positive with: numbers=[1, -2, 3]
#         6.1 Return from function
#   7. Define def is_all_positive(numbers) — stored for later
#   8. Run print(is_all_positive(...))
#       calls is_all_positive with: numbers=[5, 10, 15]
#         8.1 Return from function
#   9. Define def is_all_positive(numbers) — stored for later
#   10. Define def is_all_positive(numbers) — stored for later
#   11. Assign to numbers
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(is_all_positive(...))
#       calls is_all_positive with: numbers=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_all_positive, is_all_positive, is_all_positive, is_all_positive, is_all_positive are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(is_all_positive(lst)) → function runs first, then print shows the result
#      print(is_all_positive(?)) → function runs first, then print shows the result
#      print(is_all_positive(?)) → function runs first, then print shows the result
#      print(is_all_positive(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_all_positive(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 22:
#
# ===== SYNTAX =====
#   def find_average(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: / >
#
# ===== EVALUATION ORDER =====
#   print(find_average(lst)): the inner find_average() executes FIRST, then print() outputs the result
#   print(find_average_lst(lst)): the inner find_average_lst() executes FIRST, then print() outputs the result
#   print(find_average_lst(lst)): the inner find_average_lst() executes FIRST, then print() outputs the result
#   find_average(numbers): looks up variable 'numbers', then runs the function body
#   print(find_average(?)): the inner find_average() executes FIRST, then print() outputs the result
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   find_average(lst): looks up variable 'lst', then runs the function body
#   find_average_lst(lst): looks up variable 'lst', then runs the function body
#   total / count: first get total, then get count, then compute total / count
#   find_average_lst(lst): looks up variable 'lst', then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   find_average(?): resolves the value, then runs the function body
#   total / count: first get total, then get count, then compute total / count
#   total / count: first get total, then get count, then compute total / count
#   count > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_average(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(find_average(...))
#       calls find_average with: numbers=lst
#         3.1 Assign to total
#         3.2 Assign to count
#         3.3 For loop over numbers
#         3.4 Return from function
#   4. Define def find_average_lst(numbers) — stored for later
#   5. Assign to lst
#   6. Run print(find_average_lst(...))
#       calls find_average_lst with: numbers=lst
#         6.1 Assign to total
#         6.2 Assign to count
#         6.3 For loop over numbers
#         6.4 Assign to average
#         6.5 Return from function
#   7. Define def find_average_lst(numbers) — stored for later
#   8. Assign to lst
#   9. Run print(find_average_lst(...))
#       calls find_average_lst with: numbers=lst
#         9.1 Assign to total
#         9.2 Assign to count
#         9.3 For loop over numbers
#         9.4 Assign to average
#         9.5 Return from function
#   10. Define def find_average(numbers) — stored for later
#   11. Define def find_average(numbers) — stored for later
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(find_average(...))
#       calls find_average with: numbers=[4, 1, 8, 6, 3]
#         15.1 Assign to total
#         15.2 Assign to count
#         15.3 For loop over numbers
#         15.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_average, find_average_lst, find_average_lst, find_average, find_average are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_average(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(find_average_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(find_average_lst(lst)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_average(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 23:
#
# ===== SYNTAX =====
#   def contains_negative(numbers)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   len() — built-in function call
#   min() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: < > and
#
# ===== EVALUATION ORDER =====
#   print(contains_negative(lst)): the inner contains_negative() executes FIRST, then print() outputs the result
#   print(contains_negative(?)): the inner contains_negative() executes FIRST, then print() outputs the result
#   print(contains_negative(?)): the inner contains_negative() executes FIRST, then print() outputs the result
#   print(contains_negative(?)): the inner contains_negative() executes FIRST, then print() outputs the result
#   print(contains_negative(?)): the inner contains_negative() executes FIRST, then print() outputs the result
#   contains_negative(numbers): looks up variable 'numbers', then runs the function body
#   print(contains_negative(?)): the inner contains_negative() executes FIRST, then print() outputs the result
#   contains_negative(lst): looks up variable 'lst', then runs the function body
#   contains_negative(?): resolves the value, then runs the function body
#   contains_negative(?): resolves the value, then runs the function body
#   contains_negative(?): resolves the value, then runs the function body
#   contains_negative(?): resolves the value, then runs the function body
#   contains_negative(?): resolves the value, then runs the function body
#   number < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def contains_negative(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(contains_negative(...))
#       calls contains_negative with: numbers=lst
#         3.1 Return from function
#   4. Run print(contains_negative(...))
#       calls contains_negative with: numbers=[0, 1, 2]
#         4.1 Return from function
#   5. Define def contains_negative(numbers) — stored for later
#   6. Run print(contains_negative(...))
#       calls contains_negative with: numbers=[1, 2, -3]
#         6.1 Return from function
#   7. Run print(contains_negative(...))
#       calls contains_negative with: numbers=[1, 2, 3]
#         7.1 Return from function
#   8. Define def contains_negative(numbers) — stored for later
#   9. Run print(contains_negative(...))
#       calls contains_negative with: numbers=[-1, 0, 1]
#         9.1 Return from function
#   10. Define def contains_negative(numbers) — stored for later
#   11. Define def contains_negative(numbers) — stored for later
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(contains_negative(...))
#       calls contains_negative with: numbers=[4, 1, 8, 6, 3]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions contains_negative, contains_negative, contains_negative, contains_negative, contains_negative are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(contains_negative(lst)) → function runs first, then print shows the result
#      print(contains_negative(?)) → function runs first, then print shows the result
#      print(contains_negative(?)) → function runs first, then print shows the result
#      print(contains_negative(?)) → function runs first, then print shows the result
#      print(contains_negative(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(contains_negative(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 24:
#
# ===== SYNTAX =====
#   def find_last_element(string)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(find_last_element(lst)): the inner find_last_element() executes FIRST, then print() outputs the result
#   print(find_element(lst)): the inner find_element() executes FIRST, then print() outputs the result
#   find_last_element(string): looks up variable 'string', then runs the function body
#   print(find_last_element('world')): the inner find_last_element() executes FIRST, then print() outputs the result
#   find_last_element(lst): looks up variable 'lst', then runs the function body
#   find_element(lst): looks up variable 'lst', then runs the function body
#   find_last_element('world'): gets the literal 'world', then runs the function body
#   element == last: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_last_element(string) — stored for later
#   2. Assign to lst
#   3. Run print(find_last_element(...))
#       calls find_last_element with: string=lst
#         3.1 Return from function
#   4. Define def find_element(elements) — stored for later
#   5. Assign to lst
#   6. Run print(find_element(...))
#       calls find_element with: elements=lst
#         6.1 Assign to last
#         6.2 For loop over elements
#   7. Define def find_last_element(string) — stored for later
#   8. Define def find_last_element(string) — stored for later
#   9. Assign to string
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_last_element(...))
#       calls find_last_element with: string='world'
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_last_element, find_element, find_last_element, find_last_element are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_last_element(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(find_element(lst)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_last_element('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from elements each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 25:
#
# ===== SYNTAX =====
#   def multiply_elements(numbers)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(multiply_elements(arg)): the inner multiply_elements() executes FIRST, then print() outputs the result
#   print(multiply_elements(lst)): the inner multiply_elements() executes FIRST, then print() outputs the result
#   print(multiply_elements(arg)): the inner multiply_elements() executes FIRST, then print() outputs the result
#   multiply_elements(numbers): looks up variable 'numbers', then runs the function body
#   print(multiply_elements(?)): the inner multiply_elements() executes FIRST, then print() outputs the result
#   multiply_elements(arg): looks up variable 'arg', then runs the function body
#   multiply_elements(lst): looks up variable 'lst', then runs the function body
#   multiply_elements(arg): looks up variable 'arg', then runs the function body
#   multiply_elements(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_elements(numbers) — stored for later
#   2. Assign to arg
#   3. Run print(multiply_elements(...))
#       calls multiply_elements with: numbers=arg
#         3.1 Return from function
#   4. Define def multiply_elements(numbers) — stored for later
#   5. Assign to lst
#   6. Run print(multiply_elements(...))
#       calls multiply_elements with: numbers=lst
#         6.1 Return from function
#   7. Define def multiply_elements(numbers) — stored for later
#   8. Assign to arg
#   9. Run print(multiply_elements(...))
#       calls multiply_elements with: numbers=arg
#         9.1 Return from function
#   10. Define def multiply_elements(numbers) — stored for later
#   11. Define def multiply_elements(numbers) — stored for later
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(multiply_elements(...))
#       calls multiply_elements with: numbers=[4, 1, 8, 6, 3]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_elements, multiply_elements, multiply_elements, multiply_elements, multiply_elements are just stored
#   Execution starts at the non-def lines below:
#      arg = ... (stored in memory)
#      print(multiply_elements(arg)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(multiply_elements(lst)) → function runs first, then print shows the result
#      arg = ... (stored in memory)
#      print(multiply_elements(arg)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 26:
#
# ===== SYNTAX =====
#   def has_duplicates(lst)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   input() — built-in function call
#   len() — built-in function call
#   any() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: != > in
#
# ===== EVALUATION ORDER =====
#   print(has_duplicates(lst)): the inner has_duplicates() executes FIRST, then print() outputs the result
#   has_duplicates(): — no arguments needed
#   print(has_duplicates()): the inner has_duplicates() executes FIRST, then print() outputs the result
#   print(has_duplicates(list_with_duplicates)): the inner has_duplicates() executes FIRST, then print() outputs the result
#   print(has_duplicates(?)): the inner has_duplicates() executes FIRST, then print() outputs the result
#   print(has_duplicates(?)): the inner has_duplicates() executes FIRST, then print() outputs the result
#   print(has_duplicates(?)): the inner has_duplicates() executes FIRST, then print() outputs the result
#   has_duplicates(lst): looks up variable 'lst', then runs the function body
#   print(has_duplicates(?)): the inner has_duplicates() executes FIRST, then print() outputs the result
#   has_duplicates(lst): looks up variable 'lst', then runs the function body
#   has_duplicates(): — no arguments needed
#   has_duplicates(list_with_duplicates): looks up variable 'list_with_duplicates', then runs the function body
#   has_duplicates(?): resolves the value, then runs the function body
#   has_duplicates(?): resolves the value, then runs the function body
#   has_duplicates(?): resolves the value, then runs the function body
#   has_duplicates(?): resolves the value, then runs the function body
#   word in unique: is this True or False? The answer picks which branch
#   word in unique: is this True or False? The answer picks which branch
#   word in unique: is this True or False? The answer picks which branch
#   element in duplicate: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def has_duplicates(lst) — stored for later
#   2. Assign to lst
#   3. Run print(has_duplicates(...))
#       calls has_duplicates with: lst=lst
#         3.1 Return from function
#   4. Define def has_duplicates() — stored for later
#   5. Run has_duplicates()
#       with: 
#         5.1 Return from function
#   6. Define def has_duplicates() — stored for later
#   7. Run print(has_duplicates(...))
#       calls has_duplicates with: 
#         7.1 Return from function
#   8. Define def has_duplicates(elements) — stored for later
#   9. Assign to list_with_duplicates
#   10. Assign to list_without_duplicates
#   11. Run print('Does the list have duplicates?', has_duplicates(...))
#   12. Define def has_duplicates(lst) — stored for later
#   13. Run print(has_duplicates(...))
#       calls has_duplicates with: lst=[1, 2, 3, 2]
#         13.1 Return from function
#   14. Run print(has_duplicates(...))
#       calls has_duplicates with: lst=[1, 2, 3]
#         14.1 Return from function
#   15. Define def has_duplicates(lst) — stored for later
#   16. Run print(has_duplicates(...))
#       calls has_duplicates with: lst=['a', 'b', 'a']
#         16.1 Return from function
#   17. Define def has_duplicates(lst) — stored for later
#   18. Define def has_duplicates(lst) — stored for later
#   19. Assign to lst
#   20. Assign to result
#   21. Run print(result)
#   22. Run print(has_duplicates(...))
#       calls has_duplicates with: lst=[1, 3, 5, 7]
#         22.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions has_duplicates, has_duplicates, has_duplicates, has_duplicates, has_duplicates, has_duplicates, has_duplicates, has_duplicates are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(has_duplicates(lst)) → function runs first, then print shows the result
#      has_duplicates() → jumps into function, runs body, returns result → printed
#      print(has_duplicates()) → function runs first, then print shows the result
#      list_with_duplicates = ... (stored in memory)
#      list_without_duplicates = ... (stored in memory)
#      print('Does the list have duplicates?', has_duplicates(...))
#      print(has_duplicates(?)) → function runs first, then print shows the result
#      print(has_duplicates(?)) → function runs first, then print shows the result
#      print(has_duplicates(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(has_duplicates(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 27:
#
# ===== SYNTAX =====
#   def count_occurrences(elements, target)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(count_occurrences(elements, target)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?, 2)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?, 'a')): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(elements, target): looks up variable 'elements', looks up variable 'target', then runs the function body
#   print(count_occurrences(?, 'goal')): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(elements, target): looks up variable 'elements', looks up variable 'target', then runs the function body
#   count_occurrences(?, 2): resolves the value, gets the literal 2, then runs the function body
#   count_occurrences(?, 'a'): resolves the value, gets the literal 'a', then runs the function body
#   'The word %s occures %s times' % (word, count): first get 'The word %s occures %s times', then get (word, count), then compute 'The word %s occures %s times' % (word, count)
#   count_occurrences(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   count_occurrences(list_elements, target_element): looks up variable 'list_elements', looks up variable 'target_element', then runs the function body
#   element == target: is this True or False? The answer picks which branch
#   element == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences(elements, target) — stored for later
#   2. Assign to elements
#   3. Assign to target
#   4. Run print(count_occurrences(...))
#       calls count_occurrences with: elements=elements, target=target
#         4.1 Return from function
#   5. Define def count_occurrences(elements, target) — stored for later
#   6. Assign to list_elements
#   7. Assign to target_element
#   8. Run print(?)
#   9. Define def count_occurrences(elements, target) — stored for later
#   10. Run print(count_occurrences(...))
#       calls count_occurrences with: elements=[1, 2, 3, 2, 2], target=2
#         10.1 Return from function
#   11. Define def count_occurrences(elements, target) — stored for later
#   12. Run print(count_occurrences(...))
#       calls count_occurrences with: elements=['a', 'b', 'a', 'c'], target='a'
#         12.1 Return from function
#   13. Define def count_occurrences(elements, target) — stored for later
#   14. Define def count_occurrences(elements, target) — stored for later
#   15. Assign to elements
#   16. Assign to target
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(count_occurrences(...))
#       calls count_occurrences with: elements=[5, 3, 8, 4], target='goal'
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences, count_occurrences, count_occurrences, count_occurrences, count_occurrences, count_occurrences are just stored
#   Execution starts at the non-def lines below:
#      elements = ... (stored in memory)
#      target = ... (stored in memory)
#      print(count_occurrences(elements, target)) → function runs first, then print shows the result
#      list_elements = ... (stored in memory)
#      target_element = ... (stored in memory)
#      print(?)
#      print(count_occurrences(?, 2)) → function runs first, then print shows the result
#      print(count_occurrences(?, 'a')) → function runs first, then print shows the result
#      elements = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences(?, 'goal')) → function runs first, then print shows the result
#
#   Loop: grabs one item from elements each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 28:
#
# ===== SYNTAX =====
#   def remove_vowels(string)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(remove_vowels(lst)): the inner remove_vowels() executes FIRST, then print() outputs the result
#   print(remove_vowels(lst)): the inner remove_vowels() executes FIRST, then print() outputs the result
#   print(remove_vowels('Python')): the inner remove_vowels() executes FIRST, then print() outputs the result
#   print(remove_vowels('Hello World')): the inner remove_vowels() executes FIRST, then print() outputs the result
#   remove_vowels(string): looks up variable 'string', then runs the function body
#   print(remove_vowels('world')): the inner remove_vowels() executes FIRST, then print() outputs the result
#   remove_vowels(lst): looks up variable 'lst', then runs the function body
#   remove_vowels(lst): looks up variable 'lst', then runs the function body
#   remove_vowels('Python'): gets the literal 'Python', then runs the function body
#   remove_vowels('Hello World'): gets the literal 'Hello World', then runs the function body
#   remove_vowels('world'): gets the literal 'world', then runs the function body
#   char not in vowels: is this True or False? The answer picks which branch
#   char not in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_vowels(string) — stored for later
#   2. Assign to lst
#   3. Run print(remove_vowels(...))
#       calls remove_vowels with: string=lst
#         3.1 Return from function
#   4. Define def remove_vowels(string) — stored for later
#   5. Assign to lst
#   6. Run print(remove_vowels(...))
#       calls remove_vowels with: string=lst
#         6.1 Return from function
#   7. Define def remove_vowels(string) — stored for later
#   8. Run print(remove_vowels(...))
#       calls remove_vowels with: string='Python'
#         8.1 Return from function
#   9. Define def remove_vowels(string) — stored for later
#   10. Run print(remove_vowels(...))
#       calls remove_vowels with: string='Hello World'
#         10.1 Return from function
#   11. Define def remove_vowels(string) — stored for later
#   12. Define def remove_vowels(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(remove_vowels(...))
#       calls remove_vowels with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_vowels, remove_vowels, remove_vowels, remove_vowels, remove_vowels, remove_vowels are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_vowels(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(remove_vowels(lst)) → function runs first, then print shows the result
#      print(remove_vowels('Python')) → function runs first, then print shows the result
#      print(remove_vowels('Hello World')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 29:
#
# ===== SYNTAX =====
#   def capitalize_words(sentence)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(capitalize_words(input_sentence)): the inner capitalize_words() executes FIRST, then print() outputs the result
#   print(capitalize_words('python is awesome')): the inner capitalize_words() executes FIRST, then print() outputs the result
#   print(capitalize_words('hello world')): the inner capitalize_words() executes FIRST, then print() outputs the result
#   capitalize_words(sentence): looks up variable 'sentence', then runs the function body
#   print(capitalize_words('Python is fun')): the inner capitalize_words() executes FIRST, then print() outputs the result
#   capitalize_words(input_sentence): looks up variable 'input_sentence', then runs the function body
#   capitalize_words('python is awesome'): gets the literal 'python is awesome', then runs the function body
#   capitalize_words('hello world'): gets the literal 'hello world', then runs the function body
#   capitalize_words('Python is fun'): gets the literal 'Python is fun', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_words(sentence) — stored for later
#   2. Assign to input_sentence
#   3. Run print('Capitalized sentence:', capitalize_words(...))
#   4. Define def capitalize_words(sentence) — stored for later
#   5. Run print(capitalize_words(...))
#       calls capitalize_words with: sentence='python is awesome'
#         5.1 Return from function
#   6. Define def capitalize_words(sentence) — stored for later
#   7. Run print(capitalize_words(...))
#       calls capitalize_words with: sentence='hello world'
#         7.1 Return from function
#   8. Define def capitalize_words(sentence) — stored for later
#   9. Define def capitalize_words(sentence) — stored for later
#   10. Assign to sentence
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(capitalize_words(...))
#       calls capitalize_words with: sentence='Python is fun'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_words, capitalize_words, capitalize_words, capitalize_words, capitalize_words are just stored
#   Execution starts at the non-def lines below:
#      input_sentence = ... (stored in memory)
#      print('Capitalized sentence:', capitalize_words(...))
#      print(capitalize_words('python is awesome')) → function runs first, then print shows the result
#      print(capitalize_words('hello world')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_words('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 30:
#
# ===== SYNTAX =====
#   def remove_spaces(sentence)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   print(remove_spaces(arg)): the inner remove_spaces() executes FIRST, then print() outputs the result
#   print(remove_space(lst)): the inner remove_space() executes FIRST, then print() outputs the result
#   print(remove_spaces(arg)): the inner remove_spaces() executes FIRST, then print() outputs the result
#   print(remove_spaces('hello world')): the inner remove_spaces() executes FIRST, then print() outputs the result
#   print(remove_spaces('a b c')): the inner remove_spaces() executes FIRST, then print() outputs the result
#   remove_spaces(sentence): looks up variable 'sentence', then runs the function body
#   print(remove_spaces('Python is fun')): the inner remove_spaces() executes FIRST, then print() outputs the result
#   remove_spaces(arg): looks up variable 'arg', then runs the function body
#   remove_space(lst): looks up variable 'lst', then runs the function body
#   remove_spaces(arg): looks up variable 'arg', then runs the function body
#   remove_spaces('hello world'): gets the literal 'hello world', then runs the function body
#   remove_spaces('a b c'): gets the literal 'a b c', then runs the function body
#   remove_spaces('Python is fun'): gets the literal 'Python is fun', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_spaces(sentence) — stored for later
#   2. Assign to arg
#   3. Run print(remove_spaces(...))
#       calls remove_spaces with: sentence=arg
#         3.1 Return from function
#   4. Define def remove_space(string) — stored for later
#   5. Assign to lst
#   6. Run print(remove_space(...))
#       calls remove_space with: string=lst
#         6.1 Assign to space_removed
#         6.2 Return from function
#   7. Define def remove_spaces(sentence) — stored for later
#   8. Assign to arg
#   9. Run print(remove_spaces(...))
#       calls remove_spaces with: sentence=arg
#         9.1 Return from function
#   10. Define def remove_spaces(sentence) — stored for later
#   11. Run print(remove_spaces(...))
#       calls remove_spaces with: sentence='hello world'
#         11.1 Return from function
#   12. Define def remove_spaces(sentence) — stored for later
#   13. Run print(remove_spaces(...))
#       calls remove_spaces with: sentence='a b c'
#         13.1 Return from function
#   14. Define def remove_spaces(sentence) — stored for later
#   15. Define def remove_spaces(sentence) — stored for later
#   16. Assign to sentence
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(remove_spaces(...))
#       calls remove_spaces with: sentence='Python is fun'
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_spaces, remove_space, remove_spaces, remove_spaces, remove_spaces, remove_spaces, remove_spaces are just stored
#   Execution starts at the non-def lines below:
#      arg = ... (stored in memory)
#      print(remove_spaces(arg)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(remove_space(lst)) → function runs first, then print shows the result
#      arg = ... (stored in memory)
#      print(remove_spaces(arg)) → function runs first, then print shows the result
#      print(remove_spaces('hello world')) → function runs first, then print shows the result
#      print(remove_spaces('a b c')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_spaces('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from split_word each round

"""

"""
Problem 31:
#
# ===== SYNTAX =====
#   def get_initials(full_name)
#   return — exits the function and sends back a value
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(get_initials('John Doe')): the inner get_initials() executes FIRST, then print() outputs the result
#   get_initials(full_name): looks up variable 'full_name', then runs the function body
#   print(get_initials('world')): the inner get_initials() executes FIRST, then print() outputs the result
#   '.'.join(...) + '.': first get '.'.join(...), then get '.', then compute '.'.join(...) + '.'
#   get_initials('John Doe'): gets the literal 'John Doe', then runs the function body
#   '.'.join(...) + '.': first get '.'.join(...), then get '.', then compute '.'.join(...) + '.'
#   '.'.join(...) + '.': first get '.'.join(...), then get '.', then compute '.'.join(...) + '.'
#   get_initials('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_initials(full_name) — stored for later
#   2. Run print(get_initials(...))
#       calls get_initials with: full_name='John Doe'
#         2.1 Return from function
#   3. Define def get_initials(full_name) — stored for later
#   4. Define def get_initials(full_name) — stored for later
#   5. Assign to full_name
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(get_initials(...))
#       calls get_initials with: full_name='world'
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_initials, get_initials, get_initials are just stored
#   Execution starts at the non-def lines below:
#      print(get_initials('John Doe')) → function runs first, then print shows the result
#      full_name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_initials('world')) → function runs first, then print shows the result

"""

"""
Problem 32:
#
# ===== SYNTAX =====
#   def reverse_word(string)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(reverse_word(lst)): the inner reverse_word() executes FIRST, then print() outputs the result
#   print(reverse_words(lst)): the inner reverse_words() executes FIRST, then print() outputs the result
#   print(reverse_words('python is fun')): the inner reverse_words() executes FIRST, then print() outputs the result
#   print(reverse_words('one two three')): the inner reverse_words() executes FIRST, then print() outputs the result
#   reverse_word(string): looks up variable 'string', then runs the function body
#   print(reverse_word('world')): the inner reverse_word() executes FIRST, then print() outputs the result
#   reverse_word(lst): looks up variable 'lst', then runs the function body
#   reverse_words(lst): looks up variable 'lst', then runs the function body
#   reverse_words('python is fun'): gets the literal 'python is fun', then runs the function body
#   reverse_words('one two three'): gets the literal 'one two three', then runs the function body
#   reverse_word('world'): gets the literal 'world', then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_word(string) — stored for later
#   2. Assign to lst
#   3. Run print(reverse_word(...))
#       calls reverse_word with: string=lst
#         3.1 Assign to result
#         3.2 For loop over seperate
#         3.3 Return from function
#   4. Define def reverse_words(words) — stored for later
#   5. Assign to lst
#   6. Run print(reverse_words(...))
#       calls reverse_words with: string=lst
#         6.1 Assign to words
#         6.2 Assign to result
#         6.3 For loop over reversed(...)
#         6.4 Return from function
#   7. Define def reverse_words(string) — stored for later
#   8. Run print(reverse_words(...))
#       calls reverse_words with: string='python is fun'
#         8.1 Assign to words
#         8.2 Assign to result
#         8.3 For loop over reversed(...)
#         8.4 Return from function
#   9. Define def reverse_words(string) — stored for later
#   10. Run print(reverse_words(...))
#       calls reverse_words with: string='one two three'
#         10.1 Assign to words
#         10.2 Assign to result
#         10.3 For loop over reversed(...)
#         10.4 Return from function
#   11. Define def reverse_word(string) — stored for later
#   12. Define def reverse_word(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(reverse_word(...))
#       calls reverse_word with: string='world'
#         16.1 Assign to result
#         16.2 For loop over seperate
#         16.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_word, reverse_words, reverse_words, reverse_words, reverse_word, reverse_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(reverse_word(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(reverse_words(lst)) → function runs first, then print shows the result
#      print(reverse_words('python is fun')) → function runs first, then print shows the result
#      print(reverse_words('one two three')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_word('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from reversed(...) each round

"""

"""
Problem 33:
#
# ===== SYNTAX =====
#   def remove_duplicates(input_list)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: or in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates(input_list)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(input_list)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(input_list): looks up variable 'input_list', then runs the function body
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(input_list): looks up variable 'input_list', then runs the function body
#   remove_duplicates(input_list): looks up variable 'input_list', then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   element not in unique_elements: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(input_list) — stored for later
#   2. Assign to input_list
#   3. Run print('List with duplicates removed:', remove_duplicates(...))
#   4. Define def remove_duplicates(input_list) — stored for later
#   5. Assign to input_list
#   6. Run print('List with duplicates removed:', remove_duplicates(...))
#   7. Define def remove_duplicates(input_list) — stored for later
#   8. Run print(remove_duplicates(...))
#       calls remove_duplicates with: input_list=[3, 1, 2, 1, 3]
#         8.1 Return from function
#   9. Define def remove_duplicates(input_list) — stored for later
#   10. Run print(remove_duplicates(...))
#       calls remove_duplicates with: input_list=['a', 'b', 'a', 'c']
#         10.1 Return from function
#   11. Define def remove_duplicates(input_list) — stored for later
#   12. Define def remove_duplicates(input_list) — stored for later
#   13. Assign to input_list
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(remove_duplicates(...))
#       calls remove_duplicates with: input_list=[4, 1, 8, 6, 3]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      input_list = ... (stored in memory)
#      print('List with duplicates removed:', remove_duplicates(...))
#      input_list = ... (stored in memory)
#      print('List with duplicates removed:', remove_duplicates(...))
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      input_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from input_list each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 34:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   main(lst): looks up variable 'lst', then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   print(find_longest_word(input_string)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   print(find_longest_word(input_string)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   find_longest_word(input_string): looks up variable 'input_string', then runs the function body
#   find_longest_word(input_string): looks up variable 'input_string', then runs the function body
#   main(?): resolves the value, then runs the function body
#   len(...) > largest_len: is this True or False? The answer picks which branch
#   len(...) > count: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run main(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def main(lst) — stored for later
#   5. Assign to lst
#   6. Run main(lst)
#       with: lst=lst
#         6.1 Return from function
#   7. Define def find_longest_word(words) — stored for later
#   8. Assign to input_string
#   9. Run print('Longest word:', find_longest_word(...))
#   10. Define def find_longest_word(words) — stored for later
#   11. Assign to input_string
#   12. Run print('Longest word:', find_longest_word(...))
#   13. Define def main(lst) — stored for later
#   14. Define def main(lst) — stored for later
#   15. Assign to lst
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, main, find_longest_word, find_longest_word, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      input_string = ... (stored in memory)
#      print('Longest word:', find_longest_word(...))
#      input_string = ... (stored in memory)
#      print('Longest word:', find_longest_word(...))
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 35:
#
# ===== SYNTAX =====
#   def find_vowels(string)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(find_vowels(lst)): the inner find_vowels() executes FIRST, then print() outputs the result
#   print(find_vowels(lst)): the inner find_vowels() executes FIRST, then print() outputs the result
#   print(find_vowels('Python')): the inner find_vowels() executes FIRST, then print() outputs the result
#   print(find_vowels('Hello World')): the inner find_vowels() executes FIRST, then print() outputs the result
#   find_vowels(string): looks up variable 'string', then runs the function body
#   print(find_vowels('world')): the inner find_vowels() executes FIRST, then print() outputs the result
#   find_vowels(lst): looks up variable 'lst', then runs the function body
#   find_vowels(lst): looks up variable 'lst', then runs the function body
#   find_vowels('Python'): gets the literal 'Python', then runs the function body
#   find_vowels('Hello World'): gets the literal 'Hello World', then runs the function body
#   find_vowels('world'): gets the literal 'world', then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_vowels(string) — stored for later
#   2. Assign to lst
#   3. Run print(find_vowels(...))
#       calls find_vowels with: string=lst
#         3.1 Return from function
#   4. Define def find_vowels(string) — stored for later
#   5. Assign to lst
#   6. Run print(find_vowels(...))
#       calls find_vowels with: string=lst
#         6.1 Return from function
#   7. Define def find_vowels(string) — stored for later
#   8. Run print(find_vowels(...))
#       calls find_vowels with: string='Python'
#         8.1 Return from function
#   9. Define def find_vowels(string) — stored for later
#   10. Run print(find_vowels(...))
#       calls find_vowels with: string='Hello World'
#         10.1 Return from function
#   11. Define def find_vowels(string) — stored for later
#   12. Define def find_vowels(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(find_vowels(...))
#       calls find_vowels with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_vowels, find_vowels, find_vowels, find_vowels, find_vowels, find_vowels are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_vowels(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(find_vowels(lst)) → function runs first, then print shows the result
#      print(find_vowels('Python')) → function runs first, then print shows the result
#      print(find_vowels('Hello World')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 36:
#
# ===== SYNTAX =====
#   def reverse_number(number)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   str() — built-in function call
#   abs() — built-in function call
#   while
#   operators: + * % > >=
#
# ===== EVALUATION ORDER =====
#   print(reverse_number(number)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(?)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(1000)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(number): looks up variable 'number', then runs the function body
#   print(reverse_number(25)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(number): looks up variable 'number', then runs the function body
#   reverse_number(?): resolves the value, then runs the function body
#   reverse_number(1000): gets the literal 1000, then runs the function body
#   reverse_number(25): gets the literal 25, then runs the function body
#   num % 10: first get num, then get 10, then compute num % 10
#   ? + digit: first get ?, then get digit, then compute ? + digit
#   reversed_num * 10: first get reversed_num, then get 10, then compute reversed_num * 10
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_number(number) — stored for later
#   2. Assign to number
#   3. Run print(reverse_number(...))
#       calls reverse_number with: number=number
#         3.1 Return from function
#   4. Define def reverse_number(number) — stored for later
#   5. Run print(reverse_number(...))
#       calls reverse_number with: number=-456
#         5.1 Return from function
#   6. Run print(reverse_number(...))
#       calls reverse_number with: number=1000
#         6.1 Return from function
#   7. Define def reverse_number(number) — stored for later
#   8. Define def reverse_number(number) — stored for later
#   9. Assign to number
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(reverse_number(...))
#       calls reverse_number with: number=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_number, reverse_number, reverse_number, reverse_number are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(reverse_number(number)) → function runs first, then print shows the result
#      print(reverse_number(?)) → function runs first, then print shows the result
#      print(reverse_number(1000)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_number(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 37:
#
# ===== SYNTAX =====
#   def remove_duplicate(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: or in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicate(lst)): the inner remove_duplicate() executes FIRST, then print() outputs the result
#   print(remove_duplicates(input_string)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_dupicates(lst)): the inner remove_dupicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates('hello')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates('aabbcc')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicate(strings): looks up variable 'strings', then runs the function body
#   print(remove_duplicate('world')): the inner remove_duplicate() executes FIRST, then print() outputs the result
#   remove_duplicate(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(input_string): looks up variable 'input_string', then runs the function body
#   remove_dupicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates('hello'): gets the literal 'hello', then runs the function body
#   remove_duplicates('aabbcc'): gets the literal 'aabbcc', then runs the function body
#   remove_duplicate('world'): gets the literal 'world', then runs the function body
#   char not in unique_chars: is this True or False? The answer picks which branch
#   number not in duplicates: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicate(strings) — stored for later
#   2. Assign to lst
#   3. Run print(remove_duplicate(...))
#       calls remove_duplicate with: strings=lst
#         3.1 Return from function
#   4. Define def remove_duplicates(string) — stored for later
#   5. Assign to input_string
#   6. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string=input_string
#         6.1 Assign to seen
#         6.2 Return from function
#   7. Define def remove_dupicates(numbers) — stored for later
#   8. Assign to lst
#   9. Run print(remove_dupicates(...))
#       calls remove_dupicates with: numbers=lst
#         9.1 Assign to duplicates
#         9.2 For loop over numbers
#         9.3 Return from function
#   10. Define def remove_duplicates(string) — stored for later
#   11. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string='hello'
#         11.1 Assign to seen
#         11.2 Return from function
#   12. Define def remove_duplicates(string) — stored for later
#   13. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string='aabbcc'
#         13.1 Assign to seen
#         13.2 Return from function
#   14. Define def remove_duplicate(strings) — stored for later
#   15. Define def remove_duplicate(strings) — stored for later
#   16. Assign to strings
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(remove_duplicate(...))
#       calls remove_duplicate with: strings='world'
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicate, remove_duplicates, remove_dupicates, remove_duplicates, remove_duplicates, remove_duplicate, remove_duplicate are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_duplicate(lst)) → function runs first, then print shows the result
#      input_string = ... (stored in memory)
#      print(remove_duplicates(input_string)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(remove_dupicates(lst)) → function runs first, then print shows the result
#      print(remove_duplicates('hello')) → function runs first, then print shows the result
#      print(remove_duplicates('aabbcc')) → function runs first, then print shows the result
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicate('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 38:
#
# ===== SYNTAX =====
#   def sum_of_list()
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_of_list(): — no arguments needed
#   print(sum_of_list(?)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   print(sum_of_list(?)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   sum_of_list(numbers): looks up variable 'numbers', then runs the function body
#   print(sum_of_list(?)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   sum_of_list(?): resolves the value, then runs the function body
#   sum_of_list(?): resolves the value, then runs the function body
#   sum_of_list(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_list() — stored for later
#   2. Run sum_of_list()
#         2.1 Assign to user
#         2.2 Assign to int_number
#         2.3 For loop over user
#         2.4 Assign to count
#         2.5 For loop over int_number
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def sum_of_list(numbers) — stored for later
#   4. Run print(sum_of_list(...))
#         4.1 Assign to user
#         4.2 Assign to int_number
#         4.3 For loop over user
#         4.4 Assign to count
#         4.5 For loop over int_number
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def sum_of_list(numbers) — stored for later
#   6. Run print(sum_of_list(...))
#         6.1 Assign to user
#         6.2 Assign to int_number
#         6.3 For loop over user
#         6.4 Assign to count
#         6.5 For loop over int_number
#         6.6 Assign to result
#         6.7 Return from function
#   7. Define def sum_of_list() — stored for later
#   8. Define def sum_of_list() — stored for later
#   9. Assign to numbers
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(sum_of_list(...))
#         12.1 Assign to user
#         12.2 Assign to int_number
#         12.3 For loop over user
#         12.4 Assign to count
#         12.5 For loop over int_number
#         12.6 Assign to result
#         12.7 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_list, sum_of_list, sum_of_list, sum_of_list, sum_of_list are just stored
#   Execution starts at the non-def lines below:
#      sum_of_list() → jumps into function, runs body, returns result → printed
#      print(sum_of_list(?)) → function runs first, then print shows the result
#      print(sum_of_list(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round

"""

"""
Problem 39:
#
# ===== SYNTAX =====
#   def sum_of_list(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(sum_of_list(lst)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   print(sum_of_list(?)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   print(sum_of_list(?)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   sum_of_list(numbers): looks up variable 'numbers', then runs the function body
#   print(sum_of_list(?)): the inner sum_of_list() executes FIRST, then print() outputs the result
#   sum_of_list(lst): looks up variable 'lst', then runs the function body
#   sum_of_list(?): resolves the value, then runs the function body
#   sum_of_list(?): resolves the value, then runs the function body
#   sum_of_list(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_list(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(sum_of_list(...))
#       calls sum_of_list with: numbers=lst
#         3.1 Return from function
#   4. Define def sum_of_list(numbers) — stored for later
#   5. Run print(sum_of_list(...))
#       calls sum_of_list with: numbers=[1, 2, 3, 4, 5]
#         5.1 Return from function
#   6. Define def sum_of_list(numbers) — stored for later
#   7. Run print(sum_of_list(...))
#       calls sum_of_list with: numbers=[10, 20, 30]
#         7.1 Return from function
#   8. Define def sum_of_list(numbers) — stored for later
#   9. Define def sum_of_list(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(sum_of_list(...))
#       calls sum_of_list with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_list, sum_of_list, sum_of_list, sum_of_list, sum_of_list are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sum_of_list(lst)) → function runs first, then print shows the result
#      print(sum_of_list(?)) → function runs first, then print shows the result
#      print(sum_of_list(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 40:
#
# ===== SYNTAX =====
#   def find_min_max(numbers)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   max() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   print(find_min_max(lst)): the inner find_min_max() executes FIRST, then print() outputs the result
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   find_min_max(numbers): looks up variable 'numbers', then runs the function body
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   find_min_max(lst): looks up variable 'lst', then runs the function body
#   find_min_max(?): resolves the value, then runs the function body
#   find_min_max(?): resolves the value, then runs the function body
#   find_min_max(?): resolves the value, then runs the function body
#   number < min_number: is this True or False? The answer picks which branch
#   number > max_number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min_max(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(find_min_max(...))
#       calls find_min_max with: numbers=lst
#         3.1 Return from function
#   4. Define def find_min_max(numbers) — stored for later
#   5. Run print(find_min_max(...))
#       calls find_min_max with: numbers=[5, 2, 9, 1, 7]
#         5.1 Return from function
#   6. Define def find_min_max(numbers) — stored for later
#   7. Run print(find_min_max(...))
#       calls find_min_max with: numbers=[100, 50, 75]
#         7.1 Return from function
#   8. Define def find_min_max(numbers) — stored for later
#   9. Define def find_min_max(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(find_min_max(...))
#       calls find_min_max with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min_max, find_min_max, find_min_max, find_min_max, find_min_max are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_min_max(lst)) → function runs first, then print shows the result
#      print(find_min_max(?)) → function runs first, then print shows the result
#      print(find_min_max(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min_max(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 41:
#
# ===== SYNTAX =====
#   def count_occurrences()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   count_occurrences(): — no arguments needed
#   print(count_occurrences(?, 'Tom')): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?, 2)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(lst, target): looks up variable 'lst', looks up variable 'target', then runs the function body
#   print(count_occurrences(?, 'goal')): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(?, 'Tom'): resolves the value, gets the literal 'Tom', then runs the function body
#   count_occurrences(?, 2): resolves the value, gets the literal 2, then runs the function body
#   count_occurrences(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   word == 'Tom': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences() — stored for later
#   2. Run count_occurrences()
#         2.1 Return from function
#   3. Define def count_occurrences(lst, target) — stored for later
#   4. Run print(count_occurrences(...))
#         4.1 Return from function
#   5. Define def count_occurrences(lst, target) — stored for later
#   6. Run print(count_occurrences(...))
#         6.1 Return from function
#   7. Define def count_occurrences() — stored for later
#   8. Define def count_occurrences() — stored for later
#   9. Assign to lst
#   10. Assign to target
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_occurrences(...))
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences, count_occurrences, count_occurrences, count_occurrences, count_occurrences are just stored
#   Execution starts at the non-def lines below:
#      count_occurrences() → jumps into function, runs body, returns result → printed
#      print(count_occurrences(?, 'Tom')) → function runs first, then print shows the result
#      print(count_occurrences(?, 2)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences(?, 'goal')) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 43:
#
# ===== SYNTAX =====
#   def square_elements(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print(square_elements(lst)): the inner square_elements() executes FIRST, then print() outputs the result
#   print(square_elements(?)): the inner square_elements() executes FIRST, then print() outputs the result
#   print(square_elements(?)): the inner square_elements() executes FIRST, then print() outputs the result
#   square_elements(numbers): looks up variable 'numbers', then runs the function body
#   print(square_elements(?)): the inner square_elements() executes FIRST, then print() outputs the result
#   square_elements(lst): looks up variable 'lst', then runs the function body
#   square_elements(?): resolves the value, then runs the function body
#   square_elements(?): resolves the value, then runs the function body
#   square_elements(?): resolves the value, then runs the function body
#   n ** 2: first get n, then get 2, then compute n ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_elements(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(square_elements(...))
#       calls square_elements with: numbers=lst
#         3.1 Return from function
#   4. Define def square_elements(numbers) — stored for later
#   5. Run print(square_elements(...))
#       calls square_elements with: numbers=[1, 2, 3, 4]
#         5.1 Return from function
#   6. Define def square_elements(numbers) — stored for later
#   7. Run print(square_elements(...))
#       calls square_elements with: numbers=[5, 6, 7]
#         7.1 Return from function
#   8. Define def square_elements(numbers) — stored for later
#   9. Define def square_elements(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(square_elements(...))
#       calls square_elements with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_elements, square_elements, square_elements, square_elements, square_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(square_elements(lst)) → function runs first, then print shows the result
#      print(square_elements(?)) → function runs first, then print shows the result
#      print(square_elements(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 44:
#
# ===== SYNTAX =====
#   def count_uppercase(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_uppercase(lst)): the inner count_uppercase() executes FIRST, then print() outputs the result
#   print(count_uppercase(string)): the inner count_uppercase() executes FIRST, then print() outputs the result
#   print(count_uppercase('Hello World')): the inner count_uppercase() executes FIRST, then print() outputs the result
#   print(count_uppercase('Python Programming')): the inner count_uppercase() executes FIRST, then print() outputs the result
#   count_uppercase(string): looks up variable 'string', then runs the function body
#   print(count_uppercase('world')): the inner count_uppercase() executes FIRST, then print() outputs the result
#   count_uppercase(lst): looks up variable 'lst', then runs the function body
#   count_uppercase(string): looks up variable 'string', then runs the function body
#   count_uppercase('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_uppercase('Python Programming'): gets the literal 'Python Programming', then runs the function body
#   count_uppercase('world'): gets the literal 'world', then runs the function body
#   char == char.upper(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_uppercase(string) — stored for later
#   2. Assign to lst
#   3. Run print(count_uppercase(...))
#       calls count_uppercase with: string=lst
#         3.1 Return from function
#   4. Define def count_uppercase(string) — stored for later
#   5. Assign to string
#   6. Run print(count_uppercase(...))
#       calls count_uppercase with: string=string
#         6.1 Return from function
#   7. Define def count_uppercase(string) — stored for later
#   8. Run print(count_uppercase(...))
#       calls count_uppercase with: string='Hello World'
#         8.1 Return from function
#   9. Define def count_uppercase(string) — stored for later
#   10. Run print(count_uppercase(...))
#       calls count_uppercase with: string='Python Programming'
#         10.1 Return from function
#   11. Define def count_uppercase(string) — stored for later
#   12. Define def count_uppercase(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(count_uppercase(...))
#       calls count_uppercase with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_uppercase, count_uppercase, count_uppercase, count_uppercase, count_uppercase, count_uppercase are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(count_uppercase(lst)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      print(count_uppercase(string)) → function runs first, then print shows the result
#      print(count_uppercase('Hello World')) → function runs first, then print shows the result
#      print(count_uppercase('Python Programming')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_uppercase('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 45:
#
# ===== SYNTAX =====
#   def remove_spaces(string)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   print(remove_spaces(lst)): the inner remove_spaces() executes FIRST, then print() outputs the result
#   print(remove_spaces(lst)): the inner remove_spaces() executes FIRST, then print() outputs the result
#   print(remove_spaces('hello world')): the inner remove_spaces() executes FIRST, then print() outputs the result
#   print(remove_spaces('a b c')): the inner remove_spaces() executes FIRST, then print() outputs the result
#   remove_spaces(string): looks up variable 'string', then runs the function body
#   print(remove_spaces('world')): the inner remove_spaces() executes FIRST, then print() outputs the result
#   remove_spaces(lst): looks up variable 'lst', then runs the function body
#   remove_spaces(lst): looks up variable 'lst', then runs the function body
#   remove_spaces('hello world'): gets the literal 'hello world', then runs the function body
#   remove_spaces('a b c'): gets the literal 'a b c', then runs the function body
#   remove_spaces('world'): gets the literal 'world', then runs the function body
#   char != ' ': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_spaces(string) — stored for later
#   2. Assign to lst
#   3. Run print(remove_spaces(...))
#       calls remove_spaces with: string=lst
#         3.1 Return from function
#   4. Define def remove_spaces(string) — stored for later
#   5. Assign to lst
#   6. Run print(remove_spaces(...))
#       calls remove_spaces with: string=lst
#         6.1 Return from function
#   7. Define def remove_spaces(string) — stored for later
#   8. Run print(remove_spaces(...))
#       calls remove_spaces with: string='hello world'
#         8.1 Return from function
#   9. Define def remove_spaces(string) — stored for later
#   10. Run print(remove_spaces(...))
#       calls remove_spaces with: string='a b c'
#         10.1 Return from function
#   11. Define def remove_spaces(string) — stored for later
#   12. Define def remove_spaces(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(remove_spaces(...))
#       calls remove_spaces with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_spaces, remove_spaces, remove_spaces, remove_spaces, remove_spaces, remove_spaces are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_spaces(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(remove_spaces(lst)) → function runs first, then print shows the result
#      print(remove_spaces('hello world')) → function runs first, then print shows the result
#      print(remove_spaces('a b c')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_spaces('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 46:
#
# ===== SYNTAX =====
#   def merge_lists()
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   merge_lists(): — no arguments needed
#   print(merge_lists(?, ?)): the inner merge_lists() executes FIRST, then print() outputs the result
#   print(merge_lists(?, ?)): the inner merge_lists() executes FIRST, then print() outputs the result
#   merge_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(merge_lists(?, ?)): the inner merge_lists() executes FIRST, then print() outputs the result
#   merge_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   lst1 + lst2: first get lst1, then get lst2, then compute lst1 + lst2
#   lst1 + lst2: first get lst1, then get lst2, then compute lst1 + lst2
#   lst1 + lst2: first get lst1, then get lst2, then compute lst1 + lst2
#   lst1 + lst2: first get lst1, then get lst2, then compute lst1 + lst2
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_lists() — stored for later
#   2. Run merge_lists()
#         2.1 Assign to lst1
#         2.2 Assign to lst2
#         2.3 Assign to merged_list
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def merge_lists(lst1, lst2) — stored for later
#   4. Run print(merge_lists(...))
#         4.1 Assign to lst1
#         4.2 Assign to lst2
#         4.3 Assign to merged_list
#         4.4 Assign to result
#         4.5 Return from function
#   5. Define def merge_lists(lst1, lst2) — stored for later
#   6. Run print(merge_lists(...))
#         6.1 Assign to lst1
#         6.2 Assign to lst2
#         6.3 Assign to merged_list
#         6.4 Assign to result
#         6.5 Return from function
#   7. Define def merge_lists() — stored for later
#   8. Define def merge_lists() — stored for later
#   9. Assign to lst1
#   10. Assign to lst2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(merge_lists(...))
#         13.1 Assign to lst1
#         13.2 Assign to lst2
#         13.3 Assign to merged_list
#         13.4 Assign to result
#         13.5 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_lists, merge_lists, merge_lists, merge_lists, merge_lists are just stored
#   Execution starts at the non-def lines below:
#      merge_lists() → jumps into function, runs body, returns result → printed
#      print(merge_lists(?, ?)) → function runs first, then print shows the result
#      print(merge_lists(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_lists(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 47:
#
# ===== SYNTAX =====
#   def find_index(numbers, target)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   try/except
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(find_index(numbers, target)): the inner find_index() executes FIRST, then print() outputs the result
#   print(find_index(numbers, target)): the inner find_index() executes FIRST, then print() outputs the result
#   print(find_index(?, 30)): the inner find_index() executes FIRST, then print() outputs the result
#   print(find_index(?, 50)): the inner find_index() executes FIRST, then print() outputs the result
#   print(find_index(?, 15)): the inner find_index() executes FIRST, then print() outputs the result
#   find_index(numbers, target): looks up variable 'numbers', looks up variable 'target', then runs the function body
#   print(find_index(?, 'goal')): the inner find_index() executes FIRST, then print() outputs the result
#   find_index(numbers, target): looks up variable 'numbers', looks up variable 'target', then runs the function body
#   find_index(numbers, target): looks up variable 'numbers', looks up variable 'target', then runs the function body
#   find_index(?, 30): resolves the value, gets the literal 30, then runs the function body
#   find_index(?, 50): resolves the value, gets the literal 50, then runs the function body
#   find_index(?, 15): resolves the value, gets the literal 15, then runs the function body
#   find_index(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   num == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_index(numbers, target) — stored for later
#   2. Assign to numbers
#   3. Assign to target
#   4. Run print('Index of', target, 'in nums:', find_index(...))
#   5. Assign to target
#   6. Run print('Index of', target, 'in nums:', find_index(...))
#   7. Define def find_index(numbers, target) — stored for later
#   8. Run print(find_index(...))
#       calls find_index with: numbers=[10, 20, 30, 40], target=30
#         8.1 Return from function
#   9. Run print(find_index(...))
#       calls find_index with: numbers=[10, 20, 30, 40], target=50
#         9.1 Return from function
#   10. Define def find_index(numbers, target) — stored for later
#   11. Run print(find_index(...))
#       calls find_index with: numbers=[5, 10, 15, 20], target=15
#         11.1 Return from function
#   12. Define def find_index(numbers, target) — stored for later
#   13. Define def find_index(numbers, target) — stored for later
#   14. Assign to numbers
#   15. Assign to target
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(find_index(...))
#       calls find_index with: numbers=[4, 1, 8, 6, 3], target='goal'
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_index, find_index, find_index, find_index, find_index are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      target = ... (stored in memory)
#      print('Index of', target, 'in nums:', find_index(...))
#      target = ... (stored in memory)
#      print('Index of', target, 'in nums:', find_index(...))
#      print(find_index(?, 30)) → function runs first, then print shows the result
#      print(find_index(?, 50)) → function runs first, then print shows the result
#      print(find_index(?, 15)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_index(?, 'goal')) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 48:
#
# ===== SYNTAX =====
#   def sum_even_indices(nums)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   enumerate() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(sum_even_indices(numbers)): the inner sum_even_indices() executes FIRST, then print() outputs the result
#   print(sum_even_indices(numbers)): the inner sum_even_indices() executes FIRST, then print() outputs the result
#   print(sum_even_indices(?)): the inner sum_even_indices() executes FIRST, then print() outputs the result
#   print(sum_even_indices(?)): the inner sum_even_indices() executes FIRST, then print() outputs the result
#   sum_even_indices(nums): looks up variable 'nums', then runs the function body
#   print(sum_even_indices(?)): the inner sum_even_indices() executes FIRST, then print() outputs the result
#   sum_even_indices(numbers): looks up variable 'numbers', then runs the function body
#   sum_even_indices(numbers): looks up variable 'numbers', then runs the function body
#   sum_even_indices(?): resolves the value, then runs the function body
#   sum_even_indices(?): resolves the value, then runs the function body
#   sum_even_indices(?): resolves the value, then runs the function body
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_even_indices(nums) — stored for later
#   2. Assign to numbers
#   3. Run print('Sum of elements at even indices:', sum_even_indices(...))
#   4. Define def sum_even_indices(nums) — stored for later
#   5. Assign to numbers
#   6. Run print('Sum of elements at even indices:', sum_even_indices(...))
#   7. Define def sum_even_indices(nums) — stored for later
#   8. Run print(sum_even_indices(...))
#       calls sum_even_indices with: nums=[10, 20, 30, 40, 50]
#         8.1 Return from function
#   9. Define def sum_even_indices(nums) — stored for later
#   10. Run print(sum_even_indices(...))
#       calls sum_even_indices with: nums=[1, 2, 3, 4, 5]
#         10.1 Return from function
#   11. Define def sum_even_indices(nums) — stored for later
#   12. Define def sum_even_indices(nums) — stored for later
#   13. Assign to nums
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(sum_even_indices(...))
#       calls sum_even_indices with: nums=[4, 1, 8, 6, 3]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_even_indices, sum_even_indices, sum_even_indices, sum_even_indices, sum_even_indices, sum_even_indices are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print('Sum of elements at even indices:', sum_even_indices(...))
#      numbers = ... (stored in memory)
#      print('Sum of elements at even indices:', sum_even_indices(...))
#      print(sum_even_indices(?)) → function runs first, then print shows the result
#      print(sum_even_indices(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_even_indices(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 49:
#
# ===== SYNTAX =====
#   def remove_duplicates(lst)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   map() — built-in function call
#   list() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: and
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates(lst)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(lst)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(input_strings)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(input_strings): looks up variable 'input_strings', then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   word_lower not in unique: is this True or False? The answer picks which branch
#   word_lower not in unique: is this True or False? The answer picks which branch
#   string.lower(...) not in map(...): is this True or False? The answer picks which branch
#   lower_word not in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(lst) — stored for later
#   2. Assign to lst
#   3. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=lst
#         3.1 Return from function
#   4. Define def remove_duplicates(lst) — stored for later
#   5. Assign to lst
#   6. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=lst
#         6.1 Return from function
#   7. Define def remove_duplicates(strings) — stored for later
#   8. Assign to input_strings
#   9. Run print('List with case-insensitive duplicates removed:', remove_duplicates(...))
#   10. Define def remove_duplicates(lst) — stored for later
#   11. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=['Apple', 'APPLE', 'banana', 'BANANA']
#         11.1 Return from function
#   12. Define def remove_duplicates(lst) — stored for later
#   13. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=['Hello', 'HELLO', 'World']
#         13.1 Return from function
#   14. Define def remove_duplicates(lst) — stored for later
#   15. Define def remove_duplicates(lst) — stored for later
#   16. Assign to lst
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=[1, 3, 5, 7]
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_duplicates(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(remove_duplicates(lst)) → function runs first, then print shows the result
#      input_strings = ... (stored in memory)
#      print('List with case-insensitive duplicates removed:', remove_duplicates(...))
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 50:
#
# ===== SYNTAX =====
#   def is_palindrome(s)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   reversed() — built-in function call
#   if/elif/else
#   while
#   operators: - == != <
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome(input_string)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome(input_string)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('madam')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('python')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('level')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('world')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome(?)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(input_string): looks up variable 'input_string', then runs the function body
#   is_palindrome(input_string): looks up variable 'input_string', then runs the function body
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   is_palindrome('python'): gets the literal 'python', then runs the function body
#   is_palindrome('level'): gets the literal 'level', then runs the function body
#   is_palindrome('world'): gets the literal 'world', then runs the function body
#   is_palindrome(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(s) — stored for later
#   2. Assign to input_string
#   3. Run print('Is', input_string, 'a palindrome?', is_palindrome(...))
#   4. Assign to input_string
#   5. Run print('Is', input_string, 'a palindrome?', is_palindrome(...))
#   6. Define def is_palindrome(s) — stored for later
#   7. Run print(is_palindrome(...))
#       calls is_palindrome with: s='madam'
#         7.1 Return from function
#   8. Run print(is_palindrome(...))
#       calls is_palindrome with: s='python'
#         8.1 Return from function
#   9. Define def is_palindrome(s) — stored for later
#   10. Run print(is_palindrome(...))
#       calls is_palindrome with: s='level'
#         10.1 Return from function
#   11. Run print(is_palindrome(...))
#       calls is_palindrome with: s='world'
#         11.1 Return from function
#   12. Define def is_palindrome(s) — stored for later
#   13. Define def is_palindrome(s) — stored for later
#   14. Assign to s
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(is_palindrome(...))
#       calls is_palindrome with: s=[4, 1, 8, 6, 3]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      input_string = ... (stored in memory)
#      print('Is', input_string, 'a palindrome?', is_palindrome(...))
#      input_string = ... (stored in memory)
#      print('Is', input_string, 'a palindrome?', is_palindrome(...))
#      print(is_palindrome('madam')) → function runs first, then print shows the result
#      print(is_palindrome('python')) → function runs first, then print shows the result
#      print(is_palindrome('level')) → function runs first, then print shows the result
#      print(is_palindrome('world')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome(?)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 51:
#
# ===== SYNTAX =====
#   def factorial(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   factorial() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: + - * ==
#
# ===== EVALUATION ORDER =====
#   print(factorial(0)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(6)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(4)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(0): gets the literal 0, then runs the function body
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(6): gets the literal 6, then runs the function body
#   factorial(4): gets the literal 4, then runs the function body
#   factorial(15): gets the literal 15, then runs the function body
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   n + 1: first get n, then get 1, then compute n + 1
#   factorial(n - 1): computes the expression first, then runs the function body
#   x * y: first get x, then get y, then compute x * y
#   n + 1: first get n, then get 1, then compute n + 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n == 0: is this True or False? The answer picks which branch
#   n == 0: is this True or False? The answer picks which branch
#   n == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(n) — stored for later
#   2. Run print(factorial(...))
#       calls factorial with: n=0
#         2.1 Return from function
#   3. Define def factorial(n) — stored for later
#   4. Run print(factorial(...))
#       calls factorial with: n=5
#         4.1 Return from function
#   5. Define def factorial(n) — stored for later
#   6. Run print(factorial(...))
#       calls factorial with: n=5
#         6.1 Return from function
#   7. Define def factorial(n) — stored for later
#   8. Run print(factorial(...))
#       calls factorial with: n=6
#         8.1 Return from function
#   9. Define def factorial(n) — stored for later
#   10. Run print(factorial(...))
#       calls factorial with: n=4
#         10.1 Return from function
#   11. Define def factorial(n) — stored for later
#   12. Define def factorial(n) — stored for later
#   13. Assign to n
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(factorial(...))
#       calls factorial with: n=15
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial, factorial, factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      print(factorial(0)) → function runs first, then print shows the result
#      print(factorial(5)) → function runs first, then print shows the result
#      print(factorial(5)) → function runs first, then print shows the result
#      print(factorial(6)) → function runs first, then print shows the result
#      print(factorial(4)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 52:
#
# ===== SYNTAX =====
#   def main()
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   input() — built-in function call
#   Counter() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: == !=
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   print(is_anagram('listen', 'silent')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('hello', 'world')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('Triangle', 'Integral')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram(lst1, lst2)): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('listen', 'silent')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('hello', 'olleh')): the inner is_anagram() executes FIRST, then print() outputs the result
#   is_anagram(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   print(is_anagram('world', 'python')): the inner is_anagram() executes FIRST, then print() outputs the result
#   is_anagram('listen', 'silent'): gets the literal 'listen', gets the literal 'silent', then runs the function body
#   is_anagram('hello', 'world'): gets the literal 'hello', gets the literal 'world', then runs the function body
#   is_anagram('Triangle', 'Integral'): gets the literal 'Triangle', gets the literal 'Integral', then runs the function body
#   is_anagram(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   is_anagram('listen', 'silent'): gets the literal 'listen', gets the literal 'silent', then runs the function body
#   is_anagram('hello', 'olleh'): gets the literal 'hello', gets the literal 'olleh', then runs the function body
#   is_anagram('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   sorted1 == sorted2: is this True or False? The answer picks which branch
#   sorted_lst1 == sorted_lst2: is this True or False? The answer picks which branch
#   len(...) != len(...): is this True or False? The answer picks which branch
#   str1.count(...) != str2.count(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main() — stored for later
#   2. Run main()
#         2.1 Return from function
#   3. Define def is_anagram(str1, str2) — stored for later
#   4. Run print(is_anagram(...))
#       calls is_anagram with: str1='listen', str2='silent'
#         4.1 Assign to ?
#         4.2 If statement (one branch runs)
#         4.3 For loop over str1
#         4.4 Return from function
#   5. Run print(is_anagram(...))
#       calls is_anagram with: str1='hello', str2='world'
#         5.1 Assign to ?
#         5.2 If statement (one branch runs)
#         5.3 For loop over str1
#         5.4 Return from function
#   6. Run print(is_anagram(...))
#       calls is_anagram with: str1='Triangle', str2='Integral'
#         6.1 Assign to ?
#         6.2 If statement (one branch runs)
#         6.3 For loop over str1
#         6.4 Return from function
#   7. Define def is_anagram(lst1, lst2) — stored for later
#   8. Assign to lst1
#   9. Assign to lst2
#   10. Run print(is_anagram(...))
#       calls is_anagram with: str1=lst1, str2=lst2
#         10.1 Assign to ?
#         10.2 If statement (one branch runs)
#         10.3 For loop over str1
#         10.4 Return from function
#   11. Define def is_anagram(str1, str2) — stored for later
#   12. Run print(is_anagram(...))
#       calls is_anagram with: str1='listen', str2='silent'
#         12.1 Assign to ?
#         12.2 If statement (one branch runs)
#         12.3 For loop over str1
#         12.4 Return from function
#   13. Define def is_anagram(str1, str2) — stored for later
#   14. Run print(is_anagram(...))
#       calls is_anagram with: str1='hello', str2='olleh'
#         14.1 Assign to ?
#         14.2 If statement (one branch runs)
#         14.3 For loop over str1
#         14.4 Return from function
#   15. Define def main() — stored for later
#   16. Define def main() — stored for later
#   17. Assign to str1
#   18. Assign to str2
#   19. Assign to result
#   20. Run print(result)
#   21. Run print(is_anagram(...))
#       calls is_anagram with: str1='world', str2='python'
#         21.1 Assign to ?
#         21.2 If statement (one branch runs)
#         21.3 For loop over str1
#         21.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, is_anagram, is_anagram, is_anagram, is_anagram, main, main are just stored
#   Execution starts at the non-def lines below:
#      main() → jumps into function, runs body, returns result → printed
#      print(is_anagram('listen', 'silent')) → function runs first, then print shows the result
#      print(is_anagram('hello', 'world')) → function runs first, then print shows the result
#      print(is_anagram('Triangle', 'Integral')) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(is_anagram(lst1, lst2)) → function runs first, then print shows the result
#      print(is_anagram('listen', 'silent')) → function runs first, then print shows the result
#      print(is_anagram('hello', 'olleh')) → function runs first, then print shows the result
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_anagram('world', 'python')) → function runs first, then print shows the result
#
#   Loop: grabs one item from str1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 53:
#
# ===== SYNTAX =====
#   def count_words(s)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   print(count_words(input_string)): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('hello world hello')): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('one two one')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(s): looks up variable 's', then runs the function body
#   print(count_words('world')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(input_string): looks up variable 'input_string', then runs the function body
#   count_words('hello world hello'): gets the literal 'hello world hello', then runs the function body
#   count_words('one two one'): gets the literal 'one two one', then runs the function body
#   count_words('world'): gets the literal 'world', then runs the function body
#   word_count.get(...) + 1: first get word_count.get(...), then get 1, then compute word_count.get(...) + 1
#   word in word_count: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words(s) — stored for later
#   2. Assign to input_string
#   3. Run print(count_words(...))
#       calls count_words with: s=input_string
#         3.1 Return from function
#   4. Define def count_words(s) — stored for later
#   5. Run print(count_words(...))
#       calls count_words with: s='hello world hello'
#         5.1 Return from function
#   6. Define def count_words(s) — stored for later
#   7. Run print(count_words(...))
#       calls count_words with: s='one two one'
#         7.1 Return from function
#   8. Define def count_words(s) — stored for later
#   9. Define def count_words(s) — stored for later
#   10. Assign to s
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_words(...))
#       calls count_words with: s='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words, count_words, count_words, count_words, count_words are just stored
#   Execution starts at the non-def lines below:
#      input_string = ... (stored in memory)
#      print(count_words(input_string)) → function runs first, then print shows the result
#      print(count_words('hello world hello')) → function runs first, then print shows the result
#      print(count_words('one two one')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 54:
#
# ===== SYNTAX =====
#   def fibonacci(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   fibonacci() — built-in function call
#   (no return — the function prints directly instead of returning)
#   for
#   if/elif/else
#   in
#   operators: + - == <=
#
# ===== EVALUATION ORDER =====
#   print(fibonacci(10)): the inner fibonacci() executes FIRST, then print() outputs the result
#   print(fibonacci(7)): the inner fibonacci() executes FIRST, then print() outputs the result
#   print(list(fibonacci(...))): inner expression resolves first, then print() outputs
#   fibonacci(n): looks up variable 'n', then runs the function body
#   print(fibonacci(15)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(10): gets the literal 10, then runs the function body
#   fibonacci(7): gets the literal 7, then runs the function body
#   fibonacci(15): gets the literal 15, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   fibonacci(8): gets the literal 8, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   fibonacci(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   n <= 0: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n <= 0: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#   n == 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci(n) — stored for later
#   2. Run print(fibonacci(...))
#       calls fibonacci with: n=10
#         2.1 Return from function
#   3. Define def fibonacci(n) — stored for later
#   4. Run print(fibonacci(...))
#       calls fibonacci with: n=7
#         4.1 Return from function
#   5. Define def fibonacci(n) — stored for later
#   6. Run print(list(...))
#   7. Define def fibonacci(n) — stored for later
#   8. Define def fibonacci(n) — stored for later
#   9. Assign to n
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(fibonacci(...))
#       calls fibonacci with: n=15
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci, fibonacci, fibonacci, fibonacci, fibonacci are just stored
#   Execution starts at the non-def lines below:
#      print(fibonacci(10)) → function runs first, then print shows the result
#      print(fibonacci(7)) → function runs first, then print shows the result
#      print(list(...))
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 55:
#
# ===== SYNTAX =====
#   def area_rectangle()
#   float() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   area_rectangle(): — no arguments needed
#   length * width: first get length, then get width, then compute length * width
#   length * width: first get length, then get width, then compute length * width
#   length * width: first get length, then get width, then compute length * width
#   length * width: first get length, then get width, then compute length * width
#   length * width: first get length, then get width, then compute length * width
#
# ===== EXECUTION ORDER =====
#   1. Define def area_rectangle() — stored for later
#   2. Run area_rectangle()
#         2.1 Assign to length
#         2.2 Assign to width
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def area_rectangle() — stored for later
#   4. Define def area_rectangle() — stored for later
#   5. Assign to length
#   6. Assign to width
#   7. Run print(length * width)
#   8. Assign to length
#   9. Assign to width
#   10. Run print(length * width)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions area_rectangle, area_rectangle, area_rectangle are just stored
#   Execution starts at the non-def lines below:
#      area_rectangle() → jumps into function, runs body, returns result → printed
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      print(length * width)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      print(length * width)

"""

"""
Problem 56:
#
# ===== SYNTAX =====
#   def even_odd()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   even_odd(): — no arguments needed
#   print(even_odd(10)): the inner even_odd() executes FIRST, then print() outputs the result
#   print(even_odd(7)): the inner even_odd() executes FIRST, then print() outputs the result
#   print(even_odd(8)): the inner even_odd() executes FIRST, then print() outputs the result
#   print(even_odd(11)): the inner even_odd() executes FIRST, then print() outputs the result
#   even_odd(n): looks up variable 'n', then runs the function body
#   print(even_odd(15)): the inner even_odd() executes FIRST, then print() outputs the result
#   even_odd(10): gets the literal 10, then runs the function body
#   even_odd(7): gets the literal 7, then runs the function body
#   even_odd(8): gets the literal 8, then runs the function body
#   even_odd(11): gets the literal 11, then runs the function body
#   even_odd(15): gets the literal 15, then runs the function body
#   user % 2: first get user, then get 2, then compute user % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   n & 1: first get n, then get 1, then compute n & 1
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def even_odd() — stored for later
#   2. Run even_odd()
#       with: 
#         2.1 Return from function
#   3. Define def even_odd(n) — stored for later
#   4. Run print(even_odd(...))
#       calls even_odd with: n=10
#         4.1 Return from function
#   5. Run print(even_odd(...))
#       calls even_odd with: n=7
#         5.1 Return from function
#   6. Define def even_odd(n) — stored for later
#   7. Run print(even_odd(...))
#       calls even_odd with: n=8
#         7.1 Return from function
#   8. Run print(even_odd(...))
#       calls even_odd with: n=11
#         8.1 Return from function
#   9. Assign to n
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(even_odd(...))
#       calls even_odd with: n=15
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_odd, even_odd, even_odd are just stored
#   Execution starts at the non-def lines below:
#      even_odd() → jumps into function, runs body, returns result → printed
#      print(even_odd(10)) → function runs first, then print shows the result
#      print(even_odd(7)) → function runs first, then print shows the result
#      print(even_odd(8)) → function runs first, then print shows the result
#      print(even_odd(11)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_odd(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 57:
#
# ===== SYNTAX =====
#   def fibonacci_series()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   while
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   fibonacci_series(): — no arguments needed
#   print(fibonacci_series(50)): the inner fibonacci_series() executes FIRST, then print() outputs the result
#   print(fibonacci_series(10)): the inner fibonacci_series() executes FIRST, then print() outputs the result
#   fibonacci_series(limit): looks up variable 'limit', then runs the function body
#   print(fibonacci_series(25)): the inner fibonacci_series() executes FIRST, then print() outputs the result
#   fibonacci_series(50): gets the literal 50, then runs the function body
#   fibonacci_series(10): gets the literal 10, then runs the function body
#   fibonacci_series(25): gets the literal 25, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   ? + ?: first get ?, then get ?, then compute ? + ?
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci_series() — stored for later
#   2. Run fibonacci_series()
#       with: 
#         2.1 Assign to fib
#         2.2 Evaluate expression
#         2.3 Return from function
#   3. Define def fibonacci_series(limit) — stored for later
#   4. Run print(fibonacci_series(...))
#       calls fibonacci_series with: n=50
#         4.1 Assign to fib
#         4.2 Evaluate expression
#         4.3 Return from function
#   5. Define def fibonacci_series(n) — stored for later
#   6. Run print(fibonacci_series(...))
#       calls fibonacci_series with: n=10
#         6.1 Assign to fib
#         6.2 Evaluate expression
#         6.3 Return from function
#   7. Assign to limit
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(fibonacci_series(...))
#       calls fibonacci_series with: n=25
#         10.1 Assign to fib
#         10.2 Evaluate expression
#         10.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci_series, fibonacci_series, fibonacci_series are just stored
#   Execution starts at the non-def lines below:
#      fibonacci_series() → jumps into function, runs body, returns result → printed
#      print(fibonacci_series(50)) → function runs first, then print shows the result
#      print(fibonacci_series(10)) → function runs first, then print shows the result
#      limit = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci_series(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 58:
#
# ===== SYNTAX =====
#   def max_number()
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: > >= and
#
# ===== EVALUATION ORDER =====
#   max_number(): — no arguments needed
#   print(max_number(10, 25, 15)): the inner max_number() executes FIRST, then print() outputs the result
#   print(max_number(100, 50, 75)): the inner max_number() executes FIRST, then print() outputs the result
#   print(max_number(?)): the inner max_number() executes FIRST, then print() outputs the result
#   max_number(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(max_number(20, 30, ?)): the inner max_number() executes FIRST, then print() outputs the result
#   max_number(10, 25, 15): gets the literal 10, gets the literal 25, gets the literal 15, then runs the function body
#   max_number(100, 50, 75): gets the literal 100, gets the literal 50, gets the literal 75, then runs the function body
#   max_number(?): resolves the value, then runs the function body
#   max_number(20, 30, ?): gets the literal 20, gets the literal 30, resolves the value, then runs the function body
#   number > max_num: is this True or False? The answer picks which branch
#   number > max_num: is this True or False? The answer picks which branch
#   number > max_num: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_number() — stored for later
#   2. Run max_number()
#         2.1 Assign to user
#         2.2 Assign to int_number
#         2.3 For loop over user
#         2.4 Assign to max_num
#         2.5 For loop over int_number
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def max_number(a, b, c) — stored for later
#   4. Run print(max_number(...))
#         4.1 Assign to user
#         4.2 Assign to int_number
#         4.3 For loop over user
#         4.4 Assign to max_num
#         4.5 For loop over int_number
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def max_number(a, b, c) — stored for later
#   6. Run print(max_number(...))
#         6.1 Assign to user
#         6.2 Assign to int_number
#         6.3 For loop over user
#         6.4 Assign to max_num
#         6.5 For loop over int_number
#         6.6 Assign to result
#         6.7 Return from function
#   7. Define def max_number(numbers) — stored for later
#   8. Run print(max_number(...))
#         8.1 Assign to user
#         8.2 Assign to int_number
#         8.3 For loop over user
#         8.4 Assign to max_num
#         8.5 For loop over int_number
#         8.6 Assign to result
#         8.7 Return from function
#   9. Define def max_number() — stored for later
#   10. Define def max_number() — stored for later
#   11. Assign to a
#   12. Assign to b
#   13. Assign to c
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(max_number(...))
#         16.1 Assign to user
#         16.2 Assign to int_number
#         16.3 For loop over user
#         16.4 Assign to max_num
#         16.5 For loop over int_number
#         16.6 Assign to result
#         16.7 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_number, max_number, max_number, max_number, max_number, max_number are just stored
#   Execution starts at the non-def lines below:
#      max_number() → jumps into function, runs body, returns result → printed
#      print(max_number(10, 25, 15)) → function runs first, then print shows the result
#      print(max_number(100, 50, 75)) → function runs first, then print shows the result
#      print(max_number(?)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_number(20, 30, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 59:
#
# ===== SYNTAX =====
#   def reverse_string()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_string(): — no arguments needed
#   print(reverse_string(lst)): the inner reverse_string() executes FIRST, then print() outputs the result
#   print(reverse_string('python')): the inner reverse_string() executes FIRST, then print() outputs the result
#   print(reverse_string('hello')): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(string): looks up variable 'string', then runs the function body
#   print(reverse_string('world')): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(lst): looks up variable 'lst', then runs the function body
#   reverse_string('python'): gets the literal 'python', then runs the function body
#   reverse_string('hello'): gets the literal 'hello', then runs the function body
#   reverse_string('world'): gets the literal 'world', then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_string() — stored for later
#   2. Run reverse_string()
#         2.1 Assign to user
#         2.2 Assign to user
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def reverse_string(string) — stored for later
#   4. Assign to lst
#   5. Run print(reverse_string(...))
#         5.1 Assign to user
#         5.2 Assign to user
#         5.3 Assign to result
#         5.4 Return from function
#   6. Define def reverse_string(s) — stored for later
#   7. Run print(reverse_string(...))
#         7.1 Assign to user
#         7.2 Assign to user
#         7.3 Assign to result
#         7.4 Return from function
#   8. Define def reverse_string(s) — stored for later
#   9. Run print(reverse_string(...))
#         9.1 Assign to user
#         9.2 Assign to user
#         9.3 Assign to result
#         9.4 Return from function
#   10. Define def reverse_string() — stored for later
#   11. Define def reverse_string() — stored for later
#   12. Assign to string
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(reverse_string(...))
#         15.1 Assign to user
#         15.2 Assign to user
#         15.3 Assign to result
#         15.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_string, reverse_string, reverse_string, reverse_string, reverse_string, reverse_string are just stored
#   Execution starts at the non-def lines below:
#      reverse_string() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(reverse_string(lst)) → function runs first, then print shows the result
#      print(reverse_string('python')) → function runs first, then print shows the result
#      print(reverse_string('hello')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_string('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 60:
#
# ===== SYNTAX =====
#   def is_palindrome()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   len() — built-in function call
#   all() — built-in function call
#   range() — built-in function call
#   comprehension
#   if/elif/else
#   while
#   operators: + - // == != <
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome()): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('racecar')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('hello')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('madam')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome('world')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(): — no arguments needed
#   is_palindrome('racecar'): gets the literal 'racecar', then runs the function body
#   is_palindrome('hello'): gets the literal 'hello', then runs the function body
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   is_palindrome('world'): gets the literal 'world', then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   i + 1: first get i, then get 1, then compute i + 1
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome() — stored for later
#   2. Run print(is_palindrome(...))
#         2.1 Return from function
#   3. Define def is_palindrome(s) — stored for later
#   4. Run print(is_palindrome(...))
#         4.1 Return from function
#   5. Run print(is_palindrome(...))
#         5.1 Return from function
#   6. Define def is_palindrome(s) — stored for later
#   7. Run print(is_palindrome(...))
#         7.1 Return from function
#   8. Define def is_palindrome() — stored for later
#   9. Define def is_palindrome() — stored for later
#   10. Assign to s
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(is_palindrome(...))
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      print(is_palindrome()) → function runs first, then print shows the result
#      print(is_palindrome('racecar')) → function runs first, then print shows the result
#      print(is_palindrome('hello')) → function runs first, then print shows the result
#      print(is_palindrome('madam')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome('world')) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 61:
#
# ===== SYNTAX =====
#   def count_vowels()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   count_vowels(): — no arguments needed
#   print(count_vowels('Python')): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Hello World')): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Programming')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(s): looks up variable 's', then runs the function body
#   print(count_vowels('world')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels('Python'): gets the literal 'Python', then runs the function body
#   count_vowels('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_vowels('Programming'): gets the literal 'Programming', then runs the function body
#   count_vowels('world'): gets the literal 'world', then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#   char in vowels: is this True or False? The answer picks which branch
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels() — stored for later
#   2. Run count_vowels()
#         2.1 Assign to user
#         2.2 Assign to vowels
#         2.3 Assign to count
#         2.4 For loop over user
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def count_vowels(s) — stored for later
#   4. Run print(count_vowels(...))
#         4.1 Assign to user
#         4.2 Assign to vowels
#         4.3 Assign to count
#         4.4 For loop over user
#         4.5 Assign to result
#         4.6 Return from function
#   5. Run print(count_vowels(...))
#         5.1 Assign to user
#         5.2 Assign to vowels
#         5.3 Assign to count
#         5.4 For loop over user
#         5.5 Assign to result
#         5.6 Return from function
#   6. Define def count_vowels(s) — stored for later
#   7. Run print(count_vowels(...))
#         7.1 Assign to user
#         7.2 Assign to vowels
#         7.3 Assign to count
#         7.4 For loop over user
#         7.5 Assign to result
#         7.6 Return from function
#   8. Define def count_vowels() — stored for later
#   9. Define def count_vowels() — stored for later
#   10. Assign to s
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_vowels(...))
#         13.1 Assign to user
#         13.2 Assign to vowels
#         13.3 Assign to count
#         13.4 For loop over user
#         13.5 Assign to result
#         13.6 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      count_vowels() → jumps into function, runs body, returns result → printed
#      print(count_vowels('Python')) → function runs first, then print shows the result
#      print(count_vowels('Hello World')) → function runs first, then print shows the result
#      print(count_vowels('Programming')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 62:
#
# ===== SYNTAX =====
#   def factorial()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   fact() — built-in function call
#   (no return — the function prints directly instead of returning)
#   for
#   if/elif/else
#   in
#   operators: + - * <=
#
# ===== EVALUATION ORDER =====
#   factorial(): — no arguments needed
#   factorial(): — no arguments needed
#   factorial(): — no arguments needed
#   factorial(): — no arguments needed
#   number + 1: first get number, then get 1, then compute number + 1
#   n * fact(...): first get n, then get fact(...), then compute n * fact(...)
#   n - 1: first get n, then get 1, then compute n - 1
#   n <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial() — stored for later
#   2. Run factorial()
#         2.1 Assign to result
#         2.2 For loop over range(...)
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def factorial() — stored for later
#   4. Run factorial()
#         4.1 Assign to result
#         4.2 For loop over range(...)
#         4.3 Assign to result
#         4.4 Return from function
#   5. Define def factorial() — stored for later
#   6. Run factorial()
#         6.1 Assign to result
#         6.2 For loop over range(...)
#         6.3 Assign to result
#         6.4 Return from function
#   7. Define def factorial() — stored for later
#   8. Run factorial()
#         8.1 Assign to result
#         8.2 For loop over range(...)
#         8.3 Assign to result
#         8.4 Return from function
#   9. Define def factorial() — stored for later
#   10. Define def factorial() — stored for later
#   11. Assign to result
#   12. For loop over range(...)
#       12.1 Update result *= ...
#   13. Run print(?)
#   14. Assign to result
#   15. For loop over range(...)
#       15.1 Update result *= ...
#   16. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial, factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      factorial() → jumps into function, runs body, returns result → printed
#      factorial() → jumps into function, runs body, returns result → printed
#      factorial() → jumps into function, runs body, returns result → printed
#      factorial() → jumps into function, runs body, returns result → printed
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(?)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 63:
#
# ===== SYNTAX =====
#   def sum_of_all_numbers()
#   int() — built-in function call
#   range() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_of_all_numbers(): — no arguments needed
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_all_numbers() — stored for later
#   2. Run sum_of_all_numbers()
#         2.1 Assign to n
#         2.2 Assign to total
#         2.3 For loop over range(...)
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def sum_of_all_numbers() — stored for later
#   4. Define def sum_of_all_numbers() — stored for later
#   5. Assign to n
#   6. Assign to total
#   7. For loop over range(...)
#       7.1 Update total += ...
#   8. Run print(total)
#   9. Assign to n
#   10. Assign to total
#   11. For loop over range(...)
#       11.1 Update total += ...
#   12. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_all_numbers, sum_of_all_numbers, sum_of_all_numbers are just stored
#   Execution starts at the non-def lines below:
#      sum_of_all_numbers() → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#      n = ... (stored in memory)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 64:
#
# ===== SYNTAX =====
#   def find_largest()
#   sorted() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   find_largest(): — no arguments needed
#   find_largest(): — no arguments needed
#   print(largest_element(numbers)): the inner largest_element() executes FIRST, then print() outputs the result
#   print(largest_element(numbers)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   number > largest: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_largest() — stored for later
#   2. Run find_largest()
#         2.1 Assign to user
#         2.2 Assign to int_number
#         2.3 For loop over user
#         2.4 Assign to sort
#         2.5 Assign to largest
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def find_largest() — stored for later
#   4. Run find_largest()
#         4.1 Assign to user
#         4.2 Assign to int_number
#         4.3 For loop over user
#         4.4 Assign to sort
#         4.5 Assign to largest
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def largest_element(numbers) — stored for later
#   6. Assign to numbers
#   7. Run print(largest_element(...))
#       calls largest_element with: numbers=numbers
#         7.1 Assign to largest
#         7.2 For loop over numbers
#         7.3 Return from function
#   8. Define def largest_element(numbers) — stored for later
#   9. Assign to numbers
#   10. Run print(largest_element(...))
#       calls largest_element with: numbers=numbers
#         10.1 Assign to largest
#         10.2 For loop over numbers
#         10.3 Return from function
#   11. Define def find_largest() — stored for later
#   12. Define def find_largest() — stored for later
#   13. Assign to numbers
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(largest_element(...))
#       calls largest_element with: numbers=[4, 1, 8, 6, 3]
#         16.1 Assign to largest
#         16.2 For loop over numbers
#         16.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_largest, find_largest, largest_element, largest_element, find_largest, find_largest are just stored
#   Execution starts at the non-def lines below:
#      find_largest() → jumps into function, runs body, returns result → printed
#      find_largest() → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      print(largest_element(numbers)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      print(largest_element(numbers)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_element(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 65:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   len() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   main(lst): looks up variable 'lst', then runs the function body
#   print(largest_elememt(lst)): the inner largest_elememt() executes FIRST, then print() outputs the result
#   print(largest_elememt(lst)): the inner largest_elememt() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   largest_elememt(lst): looks up variable 'lst', then runs the function body
#   largest_elememt(lst): looks up variable 'lst', then runs the function body
#   main(?): resolves the value, then runs the function body
#   len(...) > count: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run main(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def largest_elememt(lst) — stored for later
#   5. Assign to lst
#   6. Run print(largest_elememt(...))
#       calls largest_elememt with: lst=lst
#         6.1 Assign to lst
#         6.2 Return from function
#   7. Define def largest_elememt(lst) — stored for later
#   8. Assign to lst
#   9. Run print(largest_elememt(...))
#       calls largest_elememt with: lst=lst
#         9.1 Assign to lst
#         9.2 Return from function
#   10. Define def main(lst) — stored for later
#   11. Define def main(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, largest_elememt, largest_elememt, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(largest_elememt(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(largest_elememt(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 66:
#
# ===== SYNTAX =====
#   def remove_duplicates(elements)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   set() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates(lst)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(elements): looks up variable 'elements', then runs the function body
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   element in duplicates: is this True or False? The answer picks which branch
#   item not in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(elements) — stored for later
#   2. Assign to lst
#   3. Run print(remove_duplicates(...))
#       calls remove_duplicates with: elements=lst
#         3.1 Return from function
#   4. Define def remove_duplicates(elements) — stored for later
#   5. Run print(remove_duplicates(...))
#       calls remove_duplicates with: elements=[1, 2, 2, 3, 3, 3]
#         5.1 Return from function
#   6. Define def remove_duplicates(elements) — stored for later
#   7. Run print(remove_duplicates(...))
#       calls remove_duplicates with: elements=[1, 2, 2, 3, 1, 4]
#         7.1 Return from function
#   8. Define def remove_duplicates(elements) — stored for later
#   9. Run print(remove_duplicates(...))
#       calls remove_duplicates with: elements=['a', 'b', 'a', 'c']
#         9.1 Return from function
#   10. Define def remove_duplicates(elements) — stored for later
#   11. Define def remove_duplicates(elements) — stored for later
#   12. Assign to elements
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(remove_duplicates(...))
#       calls remove_duplicates with: elements=[5, 3, 8, 4]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_duplicates(lst)) → function runs first, then print shows the result
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      elements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from elements each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 67:
#
# ===== SYNTAX =====
#   def is_leap_year()
#   return — exits the function and sends back a value
#   int() — built-in function call
#   input() — built-in function call
#   if/elif/else
#   operators: % == != and or
#
# ===== EVALUATION ORDER =====
#   print(is_leap_year()): the inner is_leap_year() executes FIRST, then print() outputs the result
#   print(is_leap_year(2000)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   print(is_leap_year(1900)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   print(is_leap_year(2024)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   print(is_leap_year(2020)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   print(is_leap_year(2100)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   is_leap_year(year): looks up variable 'year', then runs the function body
#   print(is_leap_year(25)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   is_leap_year(): — no arguments needed
#   is_leap_year(2000): gets the literal 2000, then runs the function body
#   is_leap_year(1900): gets the literal 1900, then runs the function body
#   is_leap_year(2024): gets the literal 2024, then runs the function body
#   is_leap_year(2020): gets the literal 2020, then runs the function body
#   is_leap_year(2100): gets the literal 2100, then runs the function body
#   is_leap_year(25): gets the literal 25, then runs the function body
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 4: first get year, then get 4, then compute year % 4
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_leap_year() — stored for later
#   2. Run print(is_leap_year(...))
#         2.1 Return from function
#   3. Define def is_leap_year(year) — stored for later
#   4. Run print(is_leap_year(...))
#         4.1 Return from function
#   5. Run print(is_leap_year(...))
#         5.1 Return from function
#   6. Run print(is_leap_year(...))
#         6.1 Return from function
#   7. Define def is_leap_year(year) — stored for later
#   8. Run print(is_leap_year(...))
#         8.1 Return from function
#   9. Run print(is_leap_year(...))
#         9.1 Return from function
#   10. Define def is_leap_year() — stored for later
#   11. Define def is_leap_year() — stored for later
#   12. Assign to year
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(is_leap_year(...))
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_leap_year, is_leap_year, is_leap_year, is_leap_year, is_leap_year are just stored
#   Execution starts at the non-def lines below:
#      print(is_leap_year()) → function runs first, then print shows the result
#      print(is_leap_year(2000)) → function runs first, then print shows the result
#      print(is_leap_year(1900)) → function runs first, then print shows the result
#      print(is_leap_year(2024)) → function runs first, then print shows the result
#      print(is_leap_year(2020)) → function runs first, then print shows the result
#      print(is_leap_year(2100)) → function runs first, then print shows the result
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_leap_year(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 68:
#
# ===== SYNTAX =====
#   def length_string()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   length_string(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def length_string() — stored for later
#   2. Run length_string()
#         2.1 Assign to text
#         2.2 Assign to count
#         2.3 For loop over text
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def length_string() — stored for later
#   4. Define def length_string() — stored for later
#   5. Assign to text
#   6. Assign to count
#   7. For loop over text
#       7.1 Update count += ...
#   8. Run print(count)
#   9. Assign to text
#   10. Assign to count
#   11. For loop over text
#       11.1 Update count += ...
#   12. Run print(count)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions length_string, length_string, length_string are just stored
#   Execution starts at the non-def lines below:
#      length_string() → jumps into function, runs body, returns result → printed
#      text = ... (stored in memory)
#      count = ... (stored in memory)
#      for loop runs its body once per item
#      print(count)
#      text = ... (stored in memory)
#      count = ... (stored in memory)
#      for loop runs its body once per item
#      print(count)
#
#   Loop: grabs one item from text each round

"""

"""
Problem 70:
#
# ===== SYNTAX =====
#   def count_each_word(sentence)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(count_each_word(lst)): the inner count_each_word() executes FIRST, then print() outputs the result
#   print(count_each_word('the cat and the dog')): the inner count_each_word() executes FIRST, then print() outputs the result
#   print(count_each_word('one two one two one')): the inner count_each_word() executes FIRST, then print() outputs the result
#   count_each_word(sentence): looks up variable 'sentence', then runs the function body
#   print(count_each_word('Python is fun')): the inner count_each_word() executes FIRST, then print() outputs the result
#   count_each_word(lst): looks up variable 'lst', then runs the function body
#   count_each_word('the cat and the dog'): gets the literal 'the cat and the dog', then runs the function body
#   count_each_word('one two one two one'): gets the literal 'one two one two one', then runs the function body
#   count_each_word('Python is fun'): gets the literal 'Python is fun', then runs the function body
#   word_count.get(...) + 1: first get word_count.get(...), then get 1, then compute word_count.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def count_each_word(sentence) — stored for later
#   2. Assign to lst
#   3. Run print(count_each_word(...))
#       calls count_each_word with: sentence=lst
#         3.1 Return from function
#   4. Define def count_each_word(sentence) — stored for later
#   5. Run print(count_each_word(...))
#       calls count_each_word with: sentence='the cat and the dog'
#         5.1 Return from function
#   6. Define def count_each_word(sentence) — stored for later
#   7. Run print(count_each_word(...))
#       calls count_each_word with: sentence='one two one two one'
#         7.1 Return from function
#   8. Define def count_each_word(sentence) — stored for later
#   9. Define def count_each_word(sentence) — stored for later
#   10. Assign to sentence
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_each_word(...))
#       calls count_each_word with: sentence='Python is fun'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_each_word, count_each_word, count_each_word, count_each_word, count_each_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(count_each_word(lst)) → function runs first, then print shows the result
#      print(count_each_word('the cat and the dog')) → function runs first, then print shows the result
#      print(count_each_word('one two one two one')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_each_word('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from split_word each round

"""

"""
Problem 71:
#
# ===== SYNTAX =====
#   def second_largest_number(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   second_largest_number() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   if/elif/else
#   operators: <
#
# ===== EVALUATION ORDER =====
#   second_largest_number(numbers): looks up variable 'numbers', then runs the function body
#   print(second_largest_number(?)): the inner second_largest_number() executes FIRST, then print() outputs the result
#   print(second_largest_number(numbers)): the inner second_largest_number() executes FIRST, then print() outputs the result
#   second_largest_number(?): resolves the value, then runs the function body
#   second_largest_number(numbers): looks up variable 'numbers', then runs the function body
#   len(...) < 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def second_largest_number(numbers) — stored for later
#   2. Define def sec_lar() — stored for later
#   3. Define def second_largest_number(numbers) — stored for later
#   4. Define def second_largest_number(numbers) — stored for later
#   5. Assign to numbers
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(second_largest_number(...))
#       calls second_largest_number with: numbers=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_largest_number, sec_lar, second_largest_number, second_largest_number are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_largest_number(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 72:
#
# ===== SYNTAX =====
#   def is_valid_email(email)
#   return — exits the function and sends back a value
#   bool() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(is_valid_email('test@example.com')): the inner is_valid_email() executes FIRST, then print() outputs the result
#   is_valid_email(email): looks up variable 'email', then runs the function body
#   print(is_valid_email(25)): the inner is_valid_email() executes FIRST, then print() outputs the result
#   is_valid_email('test@example.com'): gets the literal 'test@example.com', then runs the function body
#   is_valid_email(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Define def is_valid_email(email) — stored for later
#   3. Run print(is_valid_email(...))
#       calls is_valid_email with: email='test@example.com'
#         3.1 Return from function
#   4. Define def is_valid_email(email) — stored for later
#   5. Define def is_valid_email(email) — stored for later
#   6. Assign to email
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_valid_email(...))
#       calls is_valid_email with: email=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions is_valid_email, is_valid_email, is_valid_email are just stored
#   Execution starts at the non-def lines below:
#      print(is_valid_email('test@example.com')) → function runs first, then print shows the result
#      email = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_valid_email(25)) → function runs first, then print shows the result

"""

"""
Problem 73:
#
# ===== SYNTAX =====
#   def factorial(n)
#   return — exits the function and sends back a value
#   factorial() — built-in function call
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   if/elif/else
#   in
#   try/except
#   operators: + - * == < or
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   main(): — no arguments needed
#   main(): — no arguments needed
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(15): gets the literal 15, then runs the function body
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   n + 1: first get n, then get 1, then compute n + 1
#   factorial(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   factorial(number): looks up variable 'number', then runs the function body
#   factorial(number): looks up variable 'number', then runs the function body
#   factorial(number): looks up variable 'number', then runs the function body
#   n == 0: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   number < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(n) — stored for later
#   2. Define def main() — stored for later
#   3. Run main()
#         3.1 Assign to number
#         3.2 Run print(?)
#   4. Define def factorial(n) — stored for later
#   5. Define def main() — stored for later
#   6. Run main()
#         6.1 Assign to number
#         6.2 Run print(?)
#   7. Define def factorial(n) — stored for later
#   8. Define def main() — stored for later
#   9. Run main()
#         9.1 Assign to number
#         9.2 Run print(?)
#   10. Define def factorial(n) — stored for later
#   11. Define def factorial(n) — stored for later
#   12. Assign to n
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(factorial(...))
#       calls factorial with: n=15
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, main, factorial, main, factorial, main, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      main() → jumps into function, runs body, returns result → printed
#      main() → jumps into function, runs body, returns result → printed
#      main() → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 74:
#
# ===== SYNTAX =====
#   def are_anagrams()
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   are_anagrams(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def are_anagrams() — stored for later
#   2. Run are_anagrams()
#         2.1 Assign to first
#         2.2 Assign to second
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def are_anagrams() — stored for later
#   4. Define def are_anagrams() — stored for later
#   5. Assign to first
#   6. Assign to second
#   7. Run print(?)
#   8. Assign to first
#   9. Assign to second
#   10. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions are_anagrams, are_anagrams, are_anagrams are just stored
#   Execution starts at the non-def lines below:
#      are_anagrams() → jumps into function, runs body, returns result → printed
#      first = ... (stored in memory)
#      second = ... (stored in memory)
#      print(?)
#      first = ... (stored in memory)
#      second = ... (stored in memory)
#      print(?)

"""

"""
Problem 75:
#
# ===== SYNTAX =====
#   def is_prime(n)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   is_prime() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   range() — built-in function call
#   all() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   try/except
#   while
#   operators: + * % ** == != < <= or
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   print(is_prime(17)): the inner is_prime() executes FIRST, then print() outputs the result
#   print(is_prime(10)): the inner is_prime() executes FIRST, then print() outputs the result
#   print(is_prime(2)): the inner is_prime() executes FIRST, then print() outputs the result
#   print(is_prime(23)): the inner is_prime() executes FIRST, then print() outputs the result
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(n): looks up variable 'n', then runs the function body
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(17): gets the literal 17, then runs the function body
#   is_prime(10): gets the literal 10, then runs the function body
#   is_prime(2): gets the literal 2, then runs the function body
#   is_prime(23): gets the literal 23, then runs the function body
#   is_prime(15): gets the literal 15, then runs the function body
#   is_prime(15): gets the literal 15, then runs the function body
#   i * i: first get i, then get i, then compute i * i
#   is_prime(number): looks up variable 'number', then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 3: first get n, then get 3, then compute n % 3
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   n % ?: first get n, then get ?, then compute n % ?
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n % i: first get n, then get i, then compute n % i
#   i + 2: first get i, then get 2, then compute i + 2
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n <= 1: is this True or False? The answer picks which branch
#   n <= 3: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   n < 2: is this True or False? The answer picks which branch
#   n < 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(n) — stored for later
#   2. Define def main() — stored for later
#   3. Run main()
#         3.1 Try block (error handling)
#   4. Define def is_prime(n) — stored for later
#   5. Run print(is_prime(...))
#       calls is_prime with: n=17
#         5.1 Return from function
#   6. Run print(is_prime(...))
#       calls is_prime with: n=10
#         6.1 Return from function
#   7. Run print(is_prime(...))
#       calls is_prime with: n=2
#         7.1 Return from function
#   8. Define def is_prime(n) — stored for later
#   9. Run print(is_prime(...))
#       calls is_prime with: n=23
#         9.1 Return from function
#   10. Run print(is_prime(...))
#       calls is_prime with: n=15
#         10.1 Return from function
#   11. Define def is_prime(n) — stored for later
#   12. Define def is_prime(n) — stored for later
#   13. Assign to n
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(is_prime(...))
#       calls is_prime with: n=15
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, main, is_prime, is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      main() → jumps into function, runs body, returns result → printed
#      print(is_prime(17)) → function runs first, then print shows the result
#      print(is_prime(10)) → function runs first, then print shows the result
#      print(is_prime(2)) → function runs first, then print shows the result
#      print(is_prime(23)) → function runs first, then print shows the result
#      print(is_prime(15)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 76:
#
# ===== SYNTAX =====
#   def common_elements(lst1, lst2)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   input() — built-in function call
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   filter() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: == and in
#
# ===== EVALUATION ORDER =====
#   print(common_elements(lst1, lst2)): the inner common_elements() executes FIRST, then print() outputs the result
#   print(common_elements(arg1, arg2)): the inner common_elements() executes FIRST, then print() outputs the result
#   find_common_elements(): — no arguments needed
#   print(common_elements(?, ?)): the inner common_elements() executes FIRST, then print() outputs the result
#   print(common_elements(?, ?)): the inner common_elements() executes FIRST, then print() outputs the result
#   common_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(common_elements(?, ?)): the inner common_elements() executes FIRST, then print() outputs the result
#   common_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   common_elements(arg1, arg2): looks up variable 'arg1', looks up variable 'arg2', then runs the function body
#   common_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   common_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   common_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   i == j: is this True or False? The answer picks which branch
#   i not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def common_elements(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(common_elements(...))
#       calls common_elements with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def common_elements(lst1, lst2) — stored for later
#   6. Assign to arg1
#   7. Assign to arg2
#   8. Run print(common_elements(...))
#       calls common_elements with: lst1=arg1, lst2=arg2
#         8.1 Return from function
#   9. Define def find_common_elements() — stored for later
#   10. Run find_common_elements()
#         10.1 Assign to input1
#         10.2 Assign to list1
#         10.3 Assign to input2
#         10.4 Assign to list2
#         10.5 Assign to set1
#         10.6 Assign to set2
#         10.7 Assign to common_elements
#         10.8 If statement (one branch runs)
#   11. Define def common_elements(lst1, lst2) — stored for later
#   12. Run print(common_elements(...))
#       calls common_elements with: lst1=[1, 2, 3, 4], lst2=[3, 4, 5, 6]
#         12.1 Return from function
#   13. Define def common_elements(lst1, lst2) — stored for later
#   14. Run print(common_elements(...))
#       calls common_elements with: lst1=['a', 'b', 'c'], lst2=['b', 'c', 'd']
#         14.1 Return from function
#   15. Define def common_elements(lst1, lst2) — stored for later
#   16. Define def common_elements(lst1, lst2) — stored for later
#   17. Assign to lst1
#   18. Assign to lst2
#   19. Assign to result
#   20. Run print(result)
#   21. Run print(common_elements(...))
#       calls common_elements with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         21.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common_elements, common_elements, find_common_elements, common_elements, common_elements, common_elements, common_elements are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(common_elements(lst1, lst2)) → function runs first, then print shows the result
#      arg1 = ... (stored in memory)
#      arg2 = ... (stored in memory)
#      print(common_elements(arg1, arg2)) → function runs first, then print shows the result
#      find_common_elements() → jumps into function, runs body, returns result → printed
#      print(common_elements(?, ?)) → function runs first, then print shows the result
#      print(common_elements(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_elements(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 77:
#
# ===== SYNTAX =====
#   def uppercase(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(uppercase(sentence)): the inner uppercase() executes FIRST, then print() outputs the result
#   print(uppercase('Hello World')): the inner uppercase() executes FIRST, then print() outputs the result
#   print(uppercase('Python Programming')): the inner uppercase() executes FIRST, then print() outputs the result
#   uppercase(string): looks up variable 'string', then runs the function body
#   print(uppercase('world')): the inner uppercase() executes FIRST, then print() outputs the result
#   uppercase(sentence): looks up variable 'sentence', then runs the function body
#   uppercase('Hello World'): gets the literal 'Hello World', then runs the function body
#   uppercase('Python Programming'): gets the literal 'Python Programming', then runs the function body
#   uppercase('world'): gets the literal 'world', then runs the function body
#   word == word.upper(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def uppercase(string) — stored for later
#   2. Assign to sentence
#   3. Run print(uppercase(...))
#       calls uppercase with: string=sentence
#         3.1 Return from function
#   4. Define def uppercase(string) — stored for later
#   5. Run print(uppercase(...))
#       calls uppercase with: string='Hello World'
#         5.1 Return from function
#   6. Define def uppercase(string) — stored for later
#   7. Run print(uppercase(...))
#       calls uppercase with: string='Python Programming'
#         7.1 Return from function
#   8. Define def uppercase(string) — stored for later
#   9. Define def uppercase(string) — stored for later
#   10. Assign to string
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(uppercase(...))
#       calls uppercase with: string='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions uppercase, uppercase, uppercase, uppercase, uppercase are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      print(uppercase(sentence)) → function runs first, then print shows the result
#      print(uppercase('Hello World')) → function runs first, then print shows the result
#      print(uppercase('Python Programming')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(uppercase('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 78:
#
# ===== SYNTAX =====
#   def decimal_to_binary()
#   int() — built-in function call
#   input() — built-in function call
#   bin() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   format() — built-in function call
#   (no return — the function prints directly instead of returning)
#   if/elif/else
#   while
#   operators: + % == >
#
# ===== EVALUATION ORDER =====
#   decimal_to_binary(): — no arguments needed
#   decimal_to_binary(): — no arguments needed
#   decimal_to_binary(): — no arguments needed
#   str(...) + binary: first get str(...), then get binary, then compute str(...) + binary
#   num % 2: first get num, then get 2, then compute num % 2
#   decimal_number == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def decimal_to_binary() — stored for later
#   2. Run decimal_to_binary()
#         2.1 Assign to decimal_number
#         2.2 Assign to binary_number
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def decimal_to_binary() — stored for later
#   4. Run decimal_to_binary()
#         4.1 Assign to decimal_number
#         4.2 Assign to binary_number
#         4.3 Assign to result
#         4.4 Return from function
#   5. Define def decimal_to_binary() — stored for later
#   6. Run decimal_to_binary()
#         6.1 Assign to decimal_number
#         6.2 Assign to binary_number
#         6.3 Assign to result
#         6.4 Return from function
#   7. Define def decimal_to_binary() — stored for later
#   8. Define def decimal_to_binary() — stored for later
#   9. Assign to decimal_number
#   10. Assign to binary_number
#   11. Run print(?)
#   12. Assign to decimal_number
#   13. Assign to binary_number
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions decimal_to_binary, decimal_to_binary, decimal_to_binary, decimal_to_binary, decimal_to_binary are just stored
#   Execution starts at the non-def lines below:
#      decimal_to_binary() → jumps into function, runs body, returns result → printed
#      decimal_to_binary() → jumps into function, runs body, returns result → printed
#      decimal_to_binary() → jumps into function, runs body, returns result → printed
#      decimal_number = ... (stored in memory)
#      binary_number = ... (stored in memory)
#      print(?)
#      decimal_number = ... (stored in memory)
#      binary_number = ... (stored in memory)
#      print(?)
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 79:
#
# ===== SYNTAX =====
#   def main()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   sorted() — built-in function call
#   int() — built-in function call
#   len() — built-in function call
#   range() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + - >
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   print(ascending_order_numbers(numbers)): the inner ascending_order_numbers() executes FIRST, then print() outputs the result
#   print(ascending_order_numbers(?)): the inner ascending_order_numbers() executes FIRST, then print() outputs the result
#   print(ascending_order_numbers(?)): the inner ascending_order_numbers() executes FIRST, then print() outputs the result
#   ascending_order_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(ascending_order_numbers(?)): the inner ascending_order_numbers() executes FIRST, then print() outputs the result
#   ascending_order_numbers(numbers): looks up variable 'numbers', then runs the function body
#   ascending_order_numbers(?): resolves the value, then runs the function body
#   ascending_order_numbers(?): resolves the value, then runs the function body
#   ascending_order_numbers(?): resolves the value, then runs the function body
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   n - i: first get n, then get i, then compute n - i
#   j + 1: first get j, then get 1, then compute j + 1
#   j + 1: first get j, then get 1, then compute j + 1
#   j + 1: first get j, then get 1, then compute j + 1
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main() — stored for later
#   2. Run main()
#         2.1 Return from function
#   3. Define def ascending_order_numbers(numbers) — stored for later
#   4. Assign to numbers
#   5. Run print(ascending_order_numbers(...))
#       calls ascending_order_numbers with: numbers=numbers
#         5.1 Assign to arr
#         5.2 Assign to n
#         5.3 For loop over range(...)
#         5.4 Return from function
#   6. Define def ascending_order_numbers(numbers) — stored for later
#   7. Run print(ascending_order_numbers(...))
#       calls ascending_order_numbers with: numbers=[5, 2, 8, 1, 9]
#         7.1 Assign to arr
#         7.2 Assign to n
#         7.3 For loop over range(...)
#         7.4 Return from function
#   8. Define def ascending_order_numbers(numbers) — stored for later
#   9. Run print(ascending_order_numbers(...))
#       calls ascending_order_numbers with: numbers=[64, 34, 25, 12, 22]
#         9.1 Assign to arr
#         9.2 Assign to n
#         9.3 For loop over range(...)
#         9.4 Return from function
#   10. Define def main() — stored for later
#   11. Define def main() — stored for later
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(ascending_order_numbers(...))
#       calls ascending_order_numbers with: numbers=[4, 1, 8, 6, 3]
#         15.1 Assign to arr
#         15.2 Assign to n
#         15.3 For loop over range(...)
#         15.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, ascending_order_numbers, ascending_order_numbers, ascending_order_numbers, main, main are just stored
#   Execution starts at the non-def lines below:
#      main() → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      print(ascending_order_numbers(numbers)) → function runs first, then print shows the result
#      print(ascending_order_numbers(?)) → function runs first, then print shows the result
#      print(ascending_order_numbers(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(ascending_order_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 80:
#
# ===== SYNTAX =====
#   def generate_random_number()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   float() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   generate_random_number(): — no arguments needed
#   generate_random_number(): — no arguments needed
#   generate_random_number(): — no arguments needed
#   upper_limit + 1: first get upper_limit, then get 1, then compute upper_limit + 1
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Define def generate_random_number() — stored for later
#   3. Run generate_random_number()
#         3.1 Assign to lower_limit
#         3.2 Assign to upper_limit
#         3.3 Assign to random_number
#         3.4 Assign to result
#         3.5 Return from function
#   4. Define def generate_random_number() — stored for later
#   5. Run generate_random_number()
#         5.1 Assign to lower_limit
#         5.2 Assign to upper_limit
#         5.3 Assign to random_number
#         5.4 Assign to result
#         5.5 Return from function
#   6. Define def generate_random_number() — stored for later
#   7. Run generate_random_number()
#         7.1 Assign to lower_limit
#         7.2 Assign to upper_limit
#         7.3 Assign to random_number
#         7.4 Assign to result
#         7.5 Return from function
#   8. Define def generate_random_number() — stored for later
#   9. Define def generate_random_number() — stored for later
#   10. Assign to lower_limit
#   11. Assign to upper_limit
#   12. Assign to random_number
#   13. Run print(?)
#   14. Assign to lower_limit
#   15. Assign to upper_limit
#   16. Assign to random_number
#   17. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions generate_random_number, generate_random_number, generate_random_number, generate_random_number, generate_random_number are just stored
#   Execution starts at the non-def lines below:
#      generate_random_number() → jumps into function, runs body, returns result → printed
#      generate_random_number() → jumps into function, runs body, returns result → printed
#      generate_random_number() → jumps into function, runs body, returns result → printed
#      lower_limit = ... (stored in memory)
#      upper_limit = ... (stored in memory)
#      random_number = ... (stored in memory)
#      print(?)
#      lower_limit = ... (stored in memory)
#      upper_limit = ... (stored in memory)
#      random_number = ... (stored in memory)
#      print(?)

"""

"""
Problem 81:
#
# ===== SYNTAX =====
#   def square_root()
#   return — exits the function and sends back a value
#   float() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   range() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + / ** <
#
# ===== EVALUATION ORDER =====
#   print(square_root()): the inner square_root() executes FIRST, then print() outputs the result
#   square_root(): — no arguments needed
#   print(square_root()): the inner square_root() executes FIRST, then print() outputs the result
#   print(square_root()): the inner square_root() executes FIRST, then print() outputs the result
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   square_root(): — no arguments needed
#   square_root(): — no arguments needed
#   num / 2: first get num, then get 2, then compute num / 2
#   square_root(): — no arguments needed
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   ? / 2: first get ?, then get 2, then compute ? / 2
#   guess + ?: first get guess, then get ?, then compute guess + ?
#   num / guess: first get num, then get guess, then compute num / guess
#   num < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def square_root() — stored for later
#   2. Run print(square_root(...))
#         2.1 Return from function
#   3. Define def square_root() — stored for later
#   4. Run square_root()
#         4.1 Return from function
#   5. Define def square_root() — stored for later
#   6. Run print(square_root(...))
#         6.1 Return from function
#   7. Define def square_root() — stored for later
#   8. Run print(square_root(...))
#         8.1 Return from function
#   9. Define def square_root() — stored for later
#   10. Define def square_root() — stored for later
#   11. Assign to num
#   12. Assign to result
#   13. Run print(result)
#   14. Assign to num
#   15. Assign to result
#   16. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_root, square_root, square_root, square_root, square_root, square_root are just stored
#   Execution starts at the non-def lines below:
#      print(square_root()) → function runs first, then print shows the result
#      square_root() → jumps into function, runs body, returns result → printed
#      print(square_root()) → function runs first, then print shows the result
#      print(square_root()) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 82:
#
# ===== SYNTAX =====
#   class Circle — defines a new type
#     def __init__ — method of Circle
#     def area — method of Circle
#   def circle()
#   float() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   calculate_circle_area() — built-in function call
#   (no return — the function prints directly instead of returning)
#   if/elif/else
#   lambda
#   operators: * ** ==
#
# ===== EVALUATION ORDER =====
#   circle(): — no arguments needed
#   calculate_circle_area(): — no arguments needed
#   calculate_circle_area(radius): looks up variable 'radius', then runs the function body
#   print(calculate_circle_area(14)): the inner calculate_circle_area() executes FIRST, then print() outputs the result
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   calculate_circle_area(radius): looks up variable 'radius', then runs the function body
#   main(): — no arguments needed
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   calculate_circle_area(14): gets the literal 14, then runs the function body
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   math.pi * ?: first get math.pi, then get ?, then compute math.pi * ?
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   radius ** 2: first get radius, then get 2, then compute radius ** 2
#   calculate_circle_area(7): gets the literal 7, then runs the function body
#   self.radius ** 2: first get self.radius, then get 2, then compute self.radius ** 2
#   __name__ == '__main__': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def circle() — stored for later
#   3. Run circle()
#         3.1 Assign to radius
#         3.2 Assign to area
#         3.3 Assign to result
#         3.4 Return from function
#   4. Define def calculate_circle_area() — stored for later
#   5. Run calculate_circle_area()
#       with: 
#         5.1 Return from function
#   6. Import math
#   7. Define def calculate_circle_area(radius) — stored for later
#   8. Define def main() — stored for later
#   9. If statement (one branch runs)
#   10. Assign to calculate_circle_area
#   11. Run print(?)
#   12. Define class Circle
#   13. Assign to circle
#   14. Run print(?)
#   15. Define def circle() — stored for later
#   16. Define def circle() — stored for later
#   17. Assign to radius
#   18. Assign to result
#   19. Run print(result)
#   20. Run print(calculate_circle_area(...))
#       calls calculate_circle_area with: radius=14
#         20.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   Class definitions are processed (methods stored)
#   Circle.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions circle, calculate_circle_area, calculate_circle_area, main, circle, circle are just stored
#   Execution starts at the non-def lines below:
#      circle() → jumps into function, runs body, returns result → printed
#      calculate_circle_area() → jumps into function, runs body, returns result → printed
#      if checks a condition and runs ONE branch
#      calculate_circle_area = ... (stored in memory)
#      print(?)
#      circle = ... (stored in memory)
#      print(?)
#      radius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_circle_area(14)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 83:
#
# ===== SYNTAX =====
#   def is_palindrome(s)
#   return — exits the function and sends back a value
#   input() — built-in function call
#   is_palindrome() — built-in function call
#   (no return — the function prints directly instead of returning)
#   len() — built-in function call
#   if/elif/else
#   while
#   operators: - == != < <=
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome('A man a plan a canal Panama')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('racecar')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome('world')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(user_input): looks up variable 'user_input', then runs the function body
#   main(): — no arguments needed
#   is_palindrome('A man a plan a canal Panama'): gets the literal 'A man a plan a canal Panama', then runs the function body
#   is_palindrome(?): resolves the value, then runs the function body
#   is_palindrome('racecar'): gets the literal 'racecar', then runs the function body
#   is_palindrome('world'): gets the literal 'world', then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   len(...) <= 1: is this True or False? The answer picks which branch
#   ? != ?: is this True or False? The answer picks which branch
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(s) — stored for later
#   2. Define def main() — stored for later
#   3. If statement (one branch runs)
#   4. Define def is_palindrome(s) — stored for later
#   5. Run print(is_palindrome(...))
#       calls is_palindrome with: s='A man a plan a canal Panama'
#         5.1 Return from function
#   6. Define def is_palindrome(s) — stored for later
#   7. Run print(is_palindrome(...))
#       calls is_palindrome with: s='racecar'
#         7.1 Return from function
#   8. Define def is_palindrome(s) — stored for later
#   9. Define def is_palindrome(s) — stored for later
#   10. Assign to s
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(is_palindrome(...))
#       calls is_palindrome with: s='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, main, is_palindrome, is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      if checks a condition and runs ONE branch
#      print(is_palindrome('A man a plan a canal Panama')) → function runs first, then print shows the result
#      print(is_palindrome('racecar')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome('world')) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 84:
#
# ===== SYNTAX =====
#   def is_pangram(sentence)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   is_pangram() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(is_pangram('The quick brown fox jumps over the lazy dog')): the inner is_pangram() executes FIRST, then print() outputs the result
#   is_pangram(sentence): looks up variable 'sentence', then runs the function body
#   print(is_pangram('Python is fun')): the inner is_pangram() executes FIRST, then print() outputs the result
#   is_pangram(sentence): looks up variable 'sentence', then runs the function body
#   is_pangram('The quick brown fox jumps over the lazy dog'): gets the literal 'The quick brown fox jumps over the lazy dog', then runs the function body
#   is_pangram('Python is fun'): gets the literal 'Python is fun', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_pangram(sentence) — stored for later
#   2. Define def pangram(sentence) — stored for later
#   3. Run print(is_pangram(...))
#       calls is_pangram with: sentence='The quick brown fox jumps over the lazy dog'
#         3.1 Return from function
#   4. Define def is_pangram(sentence) — stored for later
#   5. Define def is_pangram(sentence) — stored for later
#   6. Assign to sentence
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_pangram(...))
#       calls is_pangram with: sentence='Python is fun'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_pangram, pangram, is_pangram, is_pangram are just stored
#   Execution starts at the non-def lines below:
#      print(is_pangram('The quick brown fox jumps over the lazy dog')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_pangram('Python is fun')) → function runs first, then print shows the result

"""

"""
Problem 85:
#
# ===== SYNTAX =====
#   def gcd(a, b)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   gcd() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   if/elif/else
#   while
#   operators: % == !=
#
# ===== EVALUATION ORDER =====
#   print(gcd(48, 18)): the inner gcd() executes FIRST, then print() outputs the result
#   print(gcd(100, 75)): the inner gcd() executes FIRST, then print() outputs the result
#   print(gcd(56, 98)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(gcd(20, 30)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(num1, num2): looks up variable 'num1', looks up variable 'num2', then runs the function body
#   main(): — no arguments needed
#   gcd(48, 18): gets the literal 48, gets the literal 18, then runs the function body
#   gcd(100, 75): gets the literal 100, gets the literal 75, then runs the function body
#   gcd(b, a % b): looks up variable 'b', computes the expression first, then runs the function body
#   gcd(56, 98): gets the literal 56, gets the literal 98, then runs the function body
#   gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   b == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def gcd(a, b) — stored for later
#   2. Define def main() — stored for later
#   3. If statement (one branch runs)
#   4. Define def gcd(a, b) — stored for later
#   5. Run print(gcd(...))
#       calls gcd with: a=48, b=18
#         5.1 Return from function
#   6. Run print(gcd(...))
#       calls gcd with: a=100, b=75
#         6.1 Return from function
#   7. Define def gcd(a, b) — stored for later
#   8. Run print(gcd(...))
#       calls gcd with: a=56, b=98
#         8.1 Return from function
#   9. Define def gcd(a, b) — stored for later
#   10. Define def gcd(a, b) — stored for later
#   11. Assign to a
#   12. Assign to b
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(gcd(...))
#       calls gcd with: a=20, b=30
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions gcd, main, gcd, gcd, gcd, gcd are just stored
#   Execution starts at the non-def lines below:
#      if checks a condition and runs ONE branch
#      print(gcd(48, 18)) → function runs first, then print shows the result
#      print(gcd(100, 75)) → function runs first, then print shows the result
#      print(gcd(56, 98)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(gcd(20, 30)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 86:
#
# ===== SYNTAX =====
#   def gcd(a, b)
#   return — exits the function and sends back a value
#   abs() — built-in function call
#   gcd() — built-in function call
#   int() — built-in function call
#   lcm() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   range() — built-in function call
#   max() — built-in function call
#   for
#   if/elif/else
#   in
#   while
#   operators: + * // % == !=
#
# ===== EVALUATION ORDER =====
#   print(lcm(12, 18)): the inner lcm() executes FIRST, then print() outputs the result
#   print(lcm(5, 7)): the inner lcm() executes FIRST, then print() outputs the result
#   print(lcm(4, 6)): the inner lcm() executes FIRST, then print() outputs the result
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(gcd(20, 30)): the inner gcd() executes FIRST, then print() outputs the result
#   abs(...) // gcd(...): first get abs(...), then get gcd(...), then compute abs(...) // gcd(...)
#   lcm(num1, num2): looks up variable 'num1', looks up variable 'num2', then runs the function body
#   main(): — no arguments needed
#   lcm(12, 18): gets the literal 12, gets the literal 18, then runs the function body
#   lcm(5, 7): gets the literal 5, gets the literal 7, then runs the function body
#   lcm(4, 6): gets the literal 4, gets the literal 6, then runs the function body
#   gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   a % b: first get a, then get b, then compute a % b
#   a * b: first get a, then get b, then compute a * b
#   a * b: first get a, then get b, then compute a * b
#   i % b: first get i, then get b, then compute i % b
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def gcd(a, b) — stored for later
#   2. Define def lcm(a, b) — stored for later
#   3. Define def main() — stored for later
#   4. If statement (one branch runs)
#   5. Define def lcm(a, b) — stored for later
#   6. Run print(lcm(...))
#       calls lcm with: a=12, b=18
#         6.1 For loop over range(...)
#   7. Run print(lcm(...))
#       calls lcm with: a=5, b=7
#         7.1 For loop over range(...)
#   8. Define def lcm(a, b) — stored for later
#   9. Run print(lcm(...))
#       calls lcm with: a=4, b=6
#         9.1 For loop over range(...)
#   10. Define def gcd(a, b) — stored for later
#   11. Define def gcd(a, b) — stored for later
#   12. Assign to a
#   13. Assign to b
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(gcd(...))
#       calls gcd with: a=20, b=30
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions gcd, lcm, main, lcm, lcm, gcd, gcd are just stored
#   Execution starts at the non-def lines below:
#      if checks a condition and runs ONE branch
#      print(lcm(12, 18)) → function runs first, then print shows the result
#      print(lcm(5, 7)) → function runs first, then print shows the result
#      print(lcm(4, 6)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(gcd(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 87:
#
# ===== SYNTAX =====
#   def reverse_lst()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   deque() — built-in function call
#   list() — built-in function call
#   comprehension
#   for
#   in
#   operators: -
#
# ===== EVALUATION ORDER =====
#   print(reverse_lst()): the inner reverse_lst() executes FIRST, then print() outputs the result
#   print(reverse_list()): the inner reverse_list() executes FIRST, then print() outputs the result
#   reverse_list(): — no arguments needed
#   print(reverse_list(lst)): the inner reverse_list() executes FIRST, then print() outputs the result
#   print(reverse_list(?)): the inner reverse_list() executes FIRST, then print() outputs the result
#   print(reverse_list(?)): the inner reverse_list() executes FIRST, then print() outputs the result
#   reverse_list(items): looks up variable 'items', then runs the function body
#   print(reverse_list(?)): the inner reverse_list() executes FIRST, then print() outputs the result
#   reverse_lst(): — no arguments needed
#   reverse_list(): — no arguments needed
#   reverse_list(lst): looks up variable 'lst', then runs the function body
#   reverse_list(?): resolves the value, then runs the function body
#   reverse_list(?): resolves the value, then runs the function body
#   reverse_list(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_lst() — stored for later
#   2. Run print(reverse_lst(...))
#         2.1 Return from function
#   3. Define def reverse_list() — stored for later
#   4. Run print(reverse_list(...))
#       calls reverse_list with: 
#         4.1 Import deque from collections
#         4.2 Assign to d
#         4.3 Run d.reverse()
#         4.4 Return from function
#   5. Define def reverse_list() — stored for later
#   6. Run reverse_list()
#       with: 
#         6.1 Import deque from collections
#         6.2 Assign to d
#         6.3 Run d.reverse()
#         6.4 Return from function
#   7. Define def reverse_list(items) — stored for later
#   8. Assign to lst
#   9. Run print(reverse_list(...))
#       calls reverse_list with: items=lst
#         9.1 Import deque from collections
#         9.2 Assign to d
#         9.3 Run d.reverse()
#         9.4 Return from function
#   10. Define def reverse_list(items) — stored for later
#   11. Run print(reverse_list(...))
#       calls reverse_list with: items=[1, 2, 3, 4, 5]
#         11.1 Import deque from collections
#         11.2 Assign to d
#         11.3 Run d.reverse()
#         11.4 Return from function
#   12. Define def reverse_list(items) — stored for later
#   13. Run print(reverse_list(...))
#       calls reverse_list with: items=['a', 'b', 'c']
#         13.1 Import deque from collections
#         13.2 Assign to d
#         13.3 Run d.reverse()
#         13.4 Return from function
#   14. Define def reverse_lst() — stored for later
#   15. Define def reverse_lst() — stored for later
#   16. Assign to items
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(reverse_list(...))
#       calls reverse_list with: items=[2, 8, 3]
#         19.1 Import deque from collections
#         19.2 Assign to d
#         19.3 Run d.reverse()
#         19.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_lst, reverse_list, reverse_list, reverse_list, reverse_list, reverse_list, reverse_lst, reverse_lst are just stored
#   Execution starts at the non-def lines below:
#      print(reverse_lst()) → function runs first, then print shows the result
#      print(reverse_list()) → function runs first, then print shows the result
#      reverse_list() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(reverse_list(lst)) → function runs first, then print shows the result
#      print(reverse_list(?)) → function runs first, then print shows the result
#      print(reverse_list(?)) → function runs first, then print shows the result
#      items = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from ? each round

"""

"""
Problem 88:
#
# ===== SYNTAX =====
#   def prime_factors(n)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   input() — built-in function call
#   prime_factors() — built-in function call
#   (no return — the function prints directly instead of returning)
#   range() — built-in function call
#   for
#   if/elif/else
#   in
#   while
#   operators: + // % ** == < >
#
# ===== EVALUATION ORDER =====
#   print(prime_factors(315)): the inner prime_factors() executes FIRST, then print() outputs the result
#   print(prime_factors(60)): the inner prime_factors() executes FIRST, then print() outputs the result
#   prime_factors(n): looks up variable 'n', then runs the function body
#   print(prime_factors(15)): the inner prime_factors() executes FIRST, then print() outputs the result
#   main(): — no arguments needed
#   prime_factors(315): gets the literal 315, then runs the function body
#   prime_factors(n, divisor + 1): looks up variable 'n', computes the expression first, then runs the function body
#   prime_factors(60): gets the literal 60, then runs the function body
#   prime_factors(15): gets the literal 15, then runs the function body
#   prime_factors(number): looks up variable 'number', then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   [divisor] + prime_factors(...): first get [divisor], then get prime_factors(...), then compute [divisor] + prime_factors(...)
#   divisor + 1: first get divisor, then get 1, then compute divisor + 1
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % i: first get n, then get i, then compute n % i
#   prime_factors(n // divisor, divisor): computes the expression first, looks up variable 'divisor', then runs the function body
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n // divisor: first get n, then get divisor, then compute n // divisor
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   number < 2: is this True or False? The answer picks which branch
#   n > 2: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def prime_factors(n) — stored for later
#   2. Define def main() — stored for later
#   3. If statement (one branch runs)
#   4. Define def prime_factors(n) — stored for later
#   5. Run print(prime_factors(...))
#       calls prime_factors with: n=315
#         5.1 Return from function
#   6. Define def prime_factors(n, divisor) — stored for later
#   7. Run print(prime_factors(...))
#       calls prime_factors with: n=60
#         7.1 Return from function
#   8. Define def prime_factors(n) — stored for later
#   9. Define def prime_factors(n) — stored for later
#   10. Assign to n
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(prime_factors(...))
#       calls prime_factors with: n=15
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions prime_factors, main, prime_factors, prime_factors, prime_factors, prime_factors are just stored
#   Execution starts at the non-def lines below:
#      if checks a condition and runs ONE branch
#      print(prime_factors(315)) → function runs first, then print shows the result
#      print(prime_factors(60)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(prime_factors(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 89:
#
# ===== SYNTAX =====
#   def generate_password(length)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print(generate_password(length)): the inner generate_password() executes FIRST, then print() outputs the result
#   print(generate_password(int(...))): the inner generate_password() executes FIRST, then print() outputs the result
#   print(generate_password(int(...))): the inner generate_password() executes FIRST, then print() outputs the result
#   generate_password(length): looks up variable 'length', then runs the function body
#   print(generate_password(25)): the inner generate_password() executes FIRST, then print() outputs the result
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   generate_password(length): looks up variable 'length', then runs the function body
#   ? + string.punctuation: first get ?, then get string.punctuation, then compute ? + string.punctuation
#   generate_password(int(...)): resolves the value, then runs the function body
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   generate_password(int(...)): resolves the value, then runs the function body
#   generate_password(25): gets the literal 25, then runs the function body
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   string.ascii_letters + string.digits: first get string.ascii_letters, then get string.digits, then compute string.ascii_letters + string.digits
#   chars * length: first get chars, then get length, then compute chars * length
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Import string
#   3. Define def generate_password(length) — stored for later
#   4. Assign to length
#   5. Run print('Generated password:', generate_password(...))
#   6. Import secrets
#   7. Import string
#   8. Define def generate_password(length) — stored for later
#   9. Run print('Generated password:', generate_password(...))
#   10. Define def generate_password(length) — stored for later
#   11. Run print('Generated password:', generate_password(...))
#   12. Define def generate_password(length) — stored for later
#   13. Define def generate_password(length) — stored for later
#   14. Assign to length
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(generate_password(...))
#       calls generate_password with: length=25
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 4 import(s) first
#   def lines are SKIPPED during execution — functions generate_password, generate_password, generate_password, generate_password, generate_password are just stored
#   Execution starts at the non-def lines below:
#      length = ... (stored in memory)
#      print('Generated password:', generate_password(...))
#      print('Generated password:', generate_password(...))
#      print('Generated password:', generate_password(...))
#      length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(generate_password(25)) → function runs first, then print shows the result

"""

"""
Problem 90:
#
# ===== SYNTAX =====
#   def count_words(sentence)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(count_words(words)): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('Python is awesome')): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('Hello world from Python')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(sentence): looks up variable 'sentence', then runs the function body
#   print(count_words('Python is fun')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(words): looks up variable 'words', then runs the function body
#   count_words('Python is awesome'): gets the literal 'Python is awesome', then runs the function body
#   count_words('Hello world from Python'): gets the literal 'Hello world from Python', then runs the function body
#   count_words('Python is fun'): gets the literal 'Python is fun', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words(sentence) — stored for later
#   2. Assign to words
#   3. Run print(count_words(...))
#       calls count_words with: sentence=words
#         3.1 Return from function
#   4. Define def count_words(sentence) — stored for later
#   5. Run print(count_words(...))
#       calls count_words with: sentence='Python is awesome'
#         5.1 Return from function
#   6. Define def count_words(sentence) — stored for later
#   7. Run print(count_words(...))
#       calls count_words with: sentence='Hello world from Python'
#         7.1 Return from function
#   8. Define def count_words(sentence) — stored for later
#   9. Define def count_words(sentence) — stored for later
#   10. Assign to sentence
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_words(...))
#       calls count_words with: sentence='Python is fun'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words, count_words, count_words, count_words, count_words are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(count_words(words)) → function runs first, then print shows the result
#      print(count_words('Python is awesome')) → function runs first, then print shows the result
#      print(count_words('Hello world from Python')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from split_words each round

"""

"""
Problem 91:
#
# ===== SYNTAX =====
#   def largest_smallest(elements)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   max() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   print(largest_smallest(lst)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   largest_smallest(elements): looks up variable 'elements', then runs the function body
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   largest_smallest(lst): looks up variable 'lst', then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#   element < smallest: is this True or False? The answer picks which branch
#   element > largest: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_smallest(elements) — stored for later
#   2. Assign to lst
#   3. Run print(largest_smallest(...))
#       calls largest_smallest with: elements=lst
#         3.1 Return from function
#   4. Define def largest_smallest(elements) — stored for later
#   5. Run print(largest_smallest(...))
#       calls largest_smallest with: elements=[10, 3, 7, 1, 9]
#         5.1 Return from function
#   6. Define def largest_smallest(elements) — stored for later
#   7. Run print(largest_smallest(...))
#       calls largest_smallest with: elements=[4, 5, 100, 6, 1, 10]
#         7.1 Return from function
#   8. Define def largest_smallest(elements) — stored for later
#   9. Define def largest_smallest(elements) — stored for later
#   10. Assign to elements
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(largest_smallest(...))
#       calls largest_smallest with: elements=[5, 3, 8, 4]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_smallest, largest_smallest, largest_smallest, largest_smallest, largest_smallest are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(largest_smallest(lst)) → function runs first, then print shows the result
#      print(largest_smallest(?)) → function runs first, then print shows the result
#      print(largest_smallest(?)) → function runs first, then print shows the result
#      elements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_smallest(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from elements each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 92:
#
# ===== SYNTAX =====
#   def is_perfect_square(num)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   int() — built-in function call
#   is_perfect_number() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + // % ** == <=
#
# ===== EVALUATION ORDER =====
#   is_perfect_square(6): gets the literal 6, then runs the function body
#   print(is_perfect_number(28)): the inner is_perfect_number() executes FIRST, then print() outputs the result
#   print(is_perfect_number(12)): the inner is_perfect_number() executes FIRST, then print() outputs the result
#   print(is_perfect_number(6)): the inner is_perfect_number() executes FIRST, then print() outputs the result
#   print(is_perfect_number(496)): the inner is_perfect_number() executes FIRST, then print() outputs the result
#   is_perfect_square(num): looks up variable 'num', then runs the function body
#   print(is_perfect_square(25)): the inner is_perfect_square() executes FIRST, then print() outputs the result
#   is_perfect_number(number): looks up variable 'number', then runs the function body
#   main(): — no arguments needed
#   is_perfect_number(28): gets the literal 28, then runs the function body
#   is_perfect_number(12): gets the literal 12, then runs the function body
#   is_perfect_number(6): gets the literal 6, then runs the function body
#   is_perfect_number(496): gets the literal 496, then runs the function body
#   is_perfect_square(25): gets the literal 25, then runs the function body
#   num + 1: first get num, then get 1, then compute num + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   num % i: first get num, then get i, then compute num % i
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n // i: first get n, then get i, then compute n // i
#   n % i: first get n, then get i, then compute n % i
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   num <= 0: is this True or False? The answer picks which branch
#   n <= 0: is this True or False? The answer picks which branch
#   n <= 0: is this True or False? The answer picks which branch
#   n <= 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   sum_divisor == num: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   sum_divisor == num: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_perfect_square(num) — stored for later
#   2. Run is_perfect_square(6)
#       with: num=6
#         2.1 Return from function
#   3. Define def is_perfect_number(n) — stored for later
#   4. Define def main() — stored for later
#   5. If statement (one branch runs)
#   6. Define def is_perfect_number(n) — stored for later
#   7. Run print(is_perfect_number(...))
#       calls is_perfect_number with: n=28
#         7.1 If statement (one branch runs)
#         7.2 Assign to divisors
#         7.3 For loop over range(...)
#         7.4 Return from function
#   8. Run print(is_perfect_number(...))
#       calls is_perfect_number with: n=12
#         8.1 If statement (one branch runs)
#         8.2 Assign to divisors
#         8.3 For loop over range(...)
#         8.4 Return from function
#   9. Define def is_perfect_number(n) — stored for later
#   10. Run print(is_perfect_number(...))
#       calls is_perfect_number with: n=6
#         10.1 If statement (one branch runs)
#         10.2 Assign to divisors
#         10.3 For loop over range(...)
#         10.4 Return from function
#   11. Run print(is_perfect_number(...))
#       calls is_perfect_number with: n=496
#         11.1 If statement (one branch runs)
#         11.2 Assign to divisors
#         11.3 For loop over range(...)
#         11.4 Return from function
#   12. Define def is_perfect_square(num) — stored for later
#   13. Define def is_perfect_square(num) — stored for later
#   14. Assign to num
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(is_perfect_square(...))
#       calls is_perfect_square with: num=25
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_perfect_square, is_perfect_number, main, is_perfect_number, is_perfect_number, is_perfect_square, is_perfect_square are just stored
#   Execution starts at the non-def lines below:
#      is_perfect_square(6) → jumps into function, runs body, returns result → printed
#      if checks a condition and runs ONE branch
#      print(is_perfect_number(28)) → function runs first, then print shows the result
#      print(is_perfect_number(12)) → function runs first, then print shows the result
#      print(is_perfect_number(6)) → function runs first, then print shows the result
#      print(is_perfect_number(496)) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_perfect_square(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 93:
#
# ===== SYNTAX =====
#   def factorial()
#   int() — built-in function call
#   range() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   factorial() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + - * <=
#
# ===== EVALUATION ORDER =====
#   factorial(): — no arguments needed
#   print(factorial()): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(): — no arguments needed
#   print(factorial(n)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(): — no arguments needed
#   print(math.factorial(n)): inner expression resolves first, then print() outputs
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   factorial(n): looks up variable 'n', then runs the function body
#   factorial(15): gets the literal 15, then runs the function body
#   user + 1: first get user, then get 1, then compute user + 1
#   user + 1: first get user, then get 1, then compute user + 1
#   factorial(n - 1): computes the expression first, then runs the function body
#   user + 1: first get user, then get 1, then compute user + 1
#   user + 1: first get user, then get 1, then compute user + 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial() — stored for later
#   2. Run factorial()
#         2.1 Assign to user
#         2.2 Assign to result
#         2.3 For loop over range(...)
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def factorial() — stored for later
#   4. Run print(factorial(...))
#         4.1 Assign to user
#         4.2 Assign to result
#         4.3 For loop over range(...)
#         4.4 Assign to result
#         4.5 Return from function
#   5. Import math
#   6. Define def factorial() — stored for later
#   7. Run factorial()
#         7.1 Assign to user
#         7.2 Assign to result
#         7.3 For loop over range(...)
#         7.4 Assign to result
#         7.5 Return from function
#   8. Define def factorial(n) — stored for later
#   9. Assign to n
#   10. Run print(factorial(...))
#         10.1 Assign to user
#         10.2 Assign to result
#         10.3 For loop over range(...)
#         10.4 Assign to result
#         10.5 Return from function
#   11. Define def factorial() — stored for later
#   12. Define def factorial() — stored for later
#   13. Assign to n
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(factorial(...))
#         16.1 Assign to user
#         16.2 Assign to result
#         16.3 For loop over range(...)
#         16.4 Assign to result
#         16.5 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial, factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      factorial() → jumps into function, runs body, returns result → printed
#      print(factorial()) → function runs first, then print shows the result
#      factorial() → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      print(factorial(n)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 94:
#
# ===== SYNTAX =====
#   def is_power_of_two(number)
#   return — exits the function and sends back a value
#   if/elif/else
#   while
#   operators: - // % == != > <= and
#
# ===== EVALUATION ORDER =====
#   is_power_of_two(number): looks up variable 'number', then runs the function body
#   print(is_power_of_two(16)): the inner is_power_of_two() executes FIRST, then print() outputs the result
#   print(is_power_of_two(18)): the inner is_power_of_two() executes FIRST, then print() outputs the result
#   print(is_power_of_two(32)): the inner is_power_of_two() executes FIRST, then print() outputs the result
#   print(is_power_of_two(33)): the inner is_power_of_two() executes FIRST, then print() outputs the result
#   is_power_of_two(number): looks up variable 'number', then runs the function body
#   print(is_power_of_two(25)): the inner is_power_of_two() executes FIRST, then print() outputs the result
#   is_power_of_two(16): gets the literal 16, then runs the function body
#   is_power_of_two(18): gets the literal 18, then runs the function body
#   is_power_of_two(32): gets the literal 32, then runs the function body
#   is_power_of_two(33): gets the literal 33, then runs the function body
#   is_power_of_two(25): gets the literal 25, then runs the function body
#   number // 2: first get number, then get 2, then compute number // 2
#   number % 2: first get number, then get 2, then compute number % 2
#   number & ?: first get number, then get ?, then compute number & ?
#   number - 1: first get number, then get 1, then compute number - 1
#   number <= 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_power_of_two(number) — stored for later
#   2. Assign to number
#   3. If statement (one branch runs)
#   4. Define def is_power_of_two(number) — stored for later
#   5. Run print(is_power_of_two(...))
#       calls is_power_of_two with: number=16
#         5.1 Return from function
#   6. Run print(is_power_of_two(...))
#       calls is_power_of_two with: number=18
#         6.1 Return from function
#   7. Define def is_power_of_two(number) — stored for later
#   8. Run print(is_power_of_two(...))
#       calls is_power_of_two with: number=32
#         8.1 Return from function
#   9. Run print(is_power_of_two(...))
#       calls is_power_of_two with: number=33
#         9.1 Return from function
#   10. Define def is_power_of_two(number) — stored for later
#   11. Define def is_power_of_two(number) — stored for later
#   12. Assign to number
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(is_power_of_two(...))
#       calls is_power_of_two with: number=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_power_of_two, is_power_of_two, is_power_of_two, is_power_of_two, is_power_of_two are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      if checks a condition and runs ONE branch
#      print(is_power_of_two(16)) → function runs first, then print shows the result
#      print(is_power_of_two(18)) → function runs first, then print shows the result
#      print(is_power_of_two(32)) → function runs first, then print shows the result
#      print(is_power_of_two(33)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_power_of_two(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 95:
#
# ===== SYNTAX =====
#   def get_list(prompt)
#   return — exits the function and sends back a value
#   input() — built-in function call
#   list() — built-in function call
#   set() — built-in function call
#   get_list() — built-in function call
#   find_intersection() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   filter() — built-in function call
#   comprehension
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   print(find_intersection()): the inner find_intersection() executes FIRST, then print() outputs the result
#   print(find_intersection(?, ?)): the inner find_intersection() executes FIRST, then print() outputs the result
#   print(find_intersection(?, ?)): the inner find_intersection() executes FIRST, then print() outputs the result
#   get_list(prompt): looks up variable 'prompt', then runs the function body
#   print(get_list(25)): the inner get_list() executes FIRST, then print() outputs the result
#   get_list('Enter the first list of elements, separated by spaces: '): gets the literal 'Enter the first list of elements, separated by spaces: ', then runs the function body
#   get_list('Enter the second list of elements, separated by spaces: '): gets the literal 'Enter the second list of elements, separated by spaces: ', then runs the function body
#   find_intersection(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   find_intersection(): — no arguments needed
#   find_intersection(?, ?): resolves the value, resolves the value, then runs the function body
#   find_intersection(?, ?): resolves the value, resolves the value, then runs the function body
#   get_list(25): gets the literal 25, then runs the function body
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def get_list(prompt) — stored for later
#   2. Define def find_intersection(list1, list2) — stored for later
#   3. Define def main() — stored for later
#   4. Run main()
#         4.1 Assign to list1
#         4.2 Assign to list2
#         4.3 Assign to intersection
#         4.4 Run print(?)
#   5. Define def find_intersection() — stored for later
#   6. Run print(find_intersection(...))
#       calls find_intersection with: 
#         6.1 Return from function
#   7. Define def find_intersection(list1, list2) — stored for later
#   8. Run print(find_intersection(...))
#       calls find_intersection with: list1=[1, 2, 3, 4], list2=[3, 4, 5, 6]
#         8.1 Return from function
#   9. Define def find_intersection(list1, list2) — stored for later
#   10. Run print(find_intersection(...))
#       calls find_intersection with: list1=['a', 'b', 'c'], list2=['b', 'c', 'd']
#         10.1 Return from function
#   11. Define def get_list(prompt) — stored for later
#   12. Define def get_list(prompt) — stored for later
#   13. Assign to prompt
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(get_list(...))
#       calls get_list with: prompt=25
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_list, find_intersection, main, find_intersection, find_intersection, find_intersection, get_list, get_list are just stored
#   Execution starts at the non-def lines below:
#      main() → jumps into function, runs body, returns result → printed
#      print(find_intersection()) → function runs first, then print shows the result
#      print(find_intersection(?, ?)) → function runs first, then print shows the result
#      print(find_intersection(?, ?)) → function runs first, then print shows the result
#      prompt = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_list(25)) → function runs first, then print shows the result

"""

"""
Problem 97:
#
# ===== SYNTAX =====
#   def binary_to_decimal(binary)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   input() — built-in function call
#   all() — built-in function call
#   binary_to_decimal() — built-in function call
#   (no return — the function prints directly instead of returning)
#   int() — built-in function call
#   sum() — built-in function call
#   enumerate() — built-in function call
#   reversed() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: - * ** == in
#
# ===== EVALUATION ORDER =====
#   print(binary_to_decimal('1010')): the inner binary_to_decimal() executes FIRST, then print() outputs the result
#   print(binary_to_decimal('1111')): the inner binary_to_decimal() executes FIRST, then print() outputs the result
#   print(binary_to_decimal('1101')): the inner binary_to_decimal() executes FIRST, then print() outputs the result
#   binary_to_decimal(binary): looks up variable 'binary', then runs the function body
#   print(binary_to_decimal(?)): the inner binary_to_decimal() executes FIRST, then print() outputs the result
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   main(): — no arguments needed
#   binary_to_decimal('1010'): gets the literal '1010', then runs the function body
#   binary_to_decimal('1111'): gets the literal '1111', then runs the function body
#   binary_to_decimal('1101'): gets the literal '1101', then runs the function body
#   binary_to_decimal(?): resolves the value, then runs the function body
#   binary_to_decimal(binary_number): looks up variable 'binary_number', then runs the function body
#   2 ** power: first get 2, then get power, then compute 2 ** power
#   int(...) * ?: first get int(...), then get ?, then compute int(...) * ?
#   2 ** i: first get 2, then get i, then compute 2 ** i
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   bit in '01': is this True or False? The answer picks which branch
#   digit == '1': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def binary_to_decimal(binary) — stored for later
#   2. Define def main() — stored for later
#   3. If statement (one branch runs)
#   4. Define def binary_to_decimal(binary) — stored for later
#   5. Run print(binary_to_decimal(...))
#       calls binary_to_decimal with: binary='1010'
#         5.1 Return from function
#   6. Run print(binary_to_decimal(...))
#       calls binary_to_decimal with: binary='1111'
#         6.1 Return from function
#   7. Define def binary_to_decimal(binary) — stored for later
#   8. Run print(binary_to_decimal(...))
#       calls binary_to_decimal with: binary='1101'
#         8.1 Return from function
#   9. Define def binary_to_decimal(binary) — stored for later
#   10. Define def binary_to_decimal(binary) — stored for later
#   11. Assign to binary
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(binary_to_decimal(...))
#       calls binary_to_decimal with: binary=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions binary_to_decimal, main, binary_to_decimal, binary_to_decimal, binary_to_decimal, binary_to_decimal are just stored
#   Execution starts at the non-def lines below:
#      if checks a condition and runs ONE branch
#      print(binary_to_decimal('1010')) → function runs first, then print shows the result
#      print(binary_to_decimal('1111')) → function runs first, then print shows the result
#      print(binary_to_decimal('1101')) → function runs first, then print shows the result
#      binary = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(binary_to_decimal(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from binary each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 98:
#
# ===== SYNTAX =====
#   def digits_string()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   sum() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   digits_string(): — no arguments needed
#   digits_string(): — no arguments needed
#   digits_string(): — no arguments needed
#   digits_string(): — no arguments needed
#   print(sum(map(...))): inner expression resolves first, then print() outputs
#   print(sum(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define def digits_string() — stored for later
#   2. Run digits_string()
#         2.1 Assign to user
#         2.2 Assign to total
#         2.3 For loop over user
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def digits_string() — stored for later
#   4. Run digits_string()
#         4.1 Assign to user
#         4.2 Assign to total
#         4.3 For loop over user
#         4.4 Assign to result
#         4.5 Return from function
#   5. Define def digits_string() — stored for later
#   6. Run digits_string()
#         6.1 Assign to user
#         6.2 Assign to total
#         6.3 For loop over user
#         6.4 Assign to result
#         6.5 Return from function
#   7. Define def digits_string() — stored for later
#   8. Run digits_string()
#         8.1 Assign to user
#         8.2 Assign to total
#         8.3 For loop over user
#         8.4 Assign to result
#         8.5 Return from function
#   9. Define def digits_string() — stored for later
#   10. Define def digits_string() — stored for later
#   11. Assign to user
#   12. Assign to total
#   13. For loop over user
#       13.1 Update total += ...
#   14. Run print(total)
#   15. Assign to user
#   16. Assign to total
#   17. For loop over user
#       17.1 Update total += ...
#   18. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions digits_string, digits_string, digits_string, digits_string, digits_string, digits_string are just stored
#   Execution starts at the non-def lines below:
#      digits_string() → jumps into function, runs body, returns result → printed
#      digits_string() → jumps into function, runs body, returns result → printed
#      digits_string() → jumps into function, runs body, returns result → printed
#      digits_string() → jumps into function, runs body, returns result → printed
#      user = ... (stored in memory)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#      user = ... (stored in memory)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      print(total)
#
#   Loop: grabs one item from user each round

"""

"""
Problem 99:
#
# ===== SYNTAX =====
#   def upper_lower(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: %
#
# ===== EVALUATION ORDER =====
#   print(upper_lower(string)): the inner upper_lower() executes FIRST, then print() outputs the result
#   print(upper_lower('Hello World! 123')): the inner upper_lower() executes FIRST, then print() outputs the result
#   print(upper_lower('Python Mastery Pro')): the inner upper_lower() executes FIRST, then print() outputs the result
#   upper_lower(string): looks up variable 'string', then runs the function body
#   print(upper_lower('world')): the inner upper_lower() executes FIRST, then print() outputs the result
#   upper_lower(string): looks up variable 'string', then runs the function body
#   upper_lower('Hello World! 123'): gets the literal 'Hello World! 123', then runs the function body
#   upper_lower('Python Mastery Pro'): gets the literal 'Python Mastery Pro', then runs the function body
#   'lower = %s and upper = %s' % (count_lower, count_upper): first get 'lower = %s and upper = %s', then get (count_lower, count_upper), then compute 'lower = %s and upper = %s' % (count_lower, count_upper)
#   upper_lower('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def upper_lower(string) — stored for later
#   2. Assign to string
#   3. Run print(upper_lower(...))
#       calls upper_lower with: string=string
#         3.1 Return from function
#   4. Define def upper_lower(string) — stored for later
#   5. Run print(upper_lower(...))
#       calls upper_lower with: string='Hello World! 123'
#         5.1 Return from function
#   6. Define def upper_lower(string) — stored for later
#   7. Run print(upper_lower(...))
#       calls upper_lower with: string='Python Mastery Pro'
#         7.1 Return from function
#   8. Define def upper_lower(string) — stored for later
#   9. Define def upper_lower(string) — stored for later
#   10. Assign to string
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(upper_lower(...))
#       calls upper_lower with: string='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions upper_lower, upper_lower, upper_lower, upper_lower, upper_lower are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(upper_lower(string)) → function runs first, then print shows the result
#      print(upper_lower('Hello World! 123')) → function runs first, then print shows the result
#      print(upper_lower('Python Mastery Pro')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(upper_lower('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 100:
#
# ===== SYNTAX =====
#   def is_prime(num)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   int() — built-in function call
#   is_prime() — built-in function call
#   sum_of_primes() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   sum() — built-in function call
#   all() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + * % ** == != > <= and
#
# ===== EVALUATION ORDER =====
#   print(sum_of_primes(1, 10)): the inner sum_of_primes() executes FIRST, then print() outputs the result
#   print(sum_of_primes(10, 50)): the inner sum_of_primes() executes FIRST, then print() outputs the result
#   is_prime(num): looks up variable 'num', then runs the function body
#   print(is_prime(25)): the inner is_prime() executes FIRST, then print() outputs the result
#   sum_of_primes(lower_limit, upper_limit): looks up variable 'lower_limit', looks up variable 'upper_limit', then runs the function body
#   main(): — no arguments needed
#   sum_of_primes(1, 10): gets the literal 1, gets the literal 10, then runs the function body
#   [True] * ?: first get [True], then get ?, then compute [True] * ?
#   sum_of_primes(10, 50): gets the literal 10, gets the literal 50, then runs the function body
#   is_prime(25): gets the literal 25, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   upper + 1: first get upper, then get 1, then compute upper + 1
#   is_prime(num): looks up variable 'num', then runs the function body
#   upper + 1: first get upper, then get 1, then compute upper + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   num % i: first get num, then get i, then compute num % i
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   upper ** 0.5: first get upper, then get 0.5, then compute upper ** 0.5
#   i * i: first get i, then get i, then compute i * i
#   upper + 1: first get upper, then get 1, then compute upper + 1
#   upper + 1: first get upper, then get 1, then compute upper + 1
#   upper + 1: first get upper, then get 1, then compute upper + 1
#   num % i: first get num, then get i, then compute num % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   __name__ == '__main__': is this True or False? The answer picks which branch
#   num <= 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(num) — stored for later
#   2. Define def sum_of_primes(lower, upper) — stored for later
#   3. Define def main() — stored for later
#   4. If statement (one branch runs)
#   5. Define def sum_of_primes(lower, upper) — stored for later
#   6. Run print(sum_of_primes(...))
#       calls sum_of_primes with: lower=1, upper=10
#         6.1 Assign to sieve
#         6.2 Assign to ?, ?
#         6.3 For loop over range(...)
#         6.4 Return from function
#   7. Define def sum_of_primes(lower, upper) — stored for later
#   8. Run print(sum_of_primes(...))
#       calls sum_of_primes with: lower=10, upper=50
#         8.1 Assign to sieve
#         8.2 Assign to ?, ?
#         8.3 For loop over range(...)
#         8.4 Return from function
#   9. Define def is_prime(num) — stored for later
#   10. Define def is_prime(num) — stored for later
#   11. Assign to num
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(is_prime(...))
#       calls is_prime with: num=25
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, sum_of_primes, main, sum_of_primes, sum_of_primes, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      if checks a condition and runs ONE branch
#      print(sum_of_primes(1, 10)) → function runs first, then print shows the result
#      print(sum_of_primes(10, 50)) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 101:
#
# ===== SYNTAX =====
#   def power(base, exp)
#   return — exits the function and sends back a value
#   power() — built-in function call
#   range() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: - * ** ==
#
# ===== EVALUATION ORDER =====
#   power(base, exp): looks up variable 'base', looks up variable 'exp', then runs the function body
#   print(power(2, 5)): the inner power() executes FIRST, then print() outputs the result
#   print(power(3, 4)): the inner power() executes FIRST, then print() outputs the result
#   print(power(2, 8)): the inner power() executes FIRST, then print() outputs the result
#   power(base, exp): looks up variable 'base', looks up variable 'exp', then runs the function body
#   print(power(10, 25)): the inner power() executes FIRST, then print() outputs the result
#   base * power(...): first get base, then get power(...), then compute base * power(...)
#   base ** exp: first get base, then get exp, then compute base ** exp
#   power(2, 5): gets the literal 2, gets the literal 5, then runs the function body
#   power(3, 4): gets the literal 3, gets the literal 4, then runs the function body
#   power(2, 8): gets the literal 2, gets the literal 8, then runs the function body
#   power(10, 25): gets the literal 10, gets the literal 25, then runs the function body
#   power(base, exp - 1): looks up variable 'base', computes the expression first, then runs the function body
#   exp - 1: first get exp, then get 1, then compute exp - 1
#   exp == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def power(base, exp) — stored for later
#   2. Assign to base
#   3. Assign to exp
#   4. Assign to result
#   5. Run print(?)
#   6. Define def power(base, exp) — stored for later
#   7. Run print(power(...))
#       calls power with: base=2, exp=5
#         7.1 Return from function
#   8. Define def power(base, exp) — stored for later
#   9. Run print(power(...))
#       calls power with: base=3, exp=4
#         9.1 Return from function
#   10. Define def power(base, exp) — stored for later
#   11. Run print(power(...))
#       calls power with: base=2, exp=8
#         11.1 Return from function
#   12. Define def power(base, exp) — stored for later
#   13. Define def power(base, exp) — stored for later
#   14. Assign to base
#   15. Assign to exp
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(power(...))
#       calls power with: base=10, exp=25
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions power, power, power, power, power, power are just stored
#   Execution starts at the non-def lines below:
#      base = ... (stored in memory)
#      exp = ... (stored in memory)
#      result = ... (stored in memory)
#      print(?)
#      print(power(2, 5)) → function runs first, then print shows the result
#      print(power(3, 4)) → function runs first, then print shows the result
#      print(power(2, 8)) → function runs first, then print shows the result
#      base = ... (stored in memory)
#      exp = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(power(10, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 102:
#
# ===== SYNTAX =====
#   def is_anagram()
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   Counter() — built-in function call
#   set() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: == !=
#
# ===== EVALUATION ORDER =====
#   is_anagram(): — no arguments needed
#   print(is_anagram(string1, string2)): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('Listen', 'Silent')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('hello', 'olleh')): the inner is_anagram() executes FIRST, then print() outputs the result
#   is_anagram(string1, string2): looks up variable 'string1', looks up variable 'string2', then runs the function body
#   print(is_anagram('world', 'python')): the inner is_anagram() executes FIRST, then print() outputs the result
#   is_anagram(string1, string2): looks up variable 'string1', looks up variable 'string2', then runs the function body
#   is_anagram('Listen', 'Silent'): gets the literal 'Listen', gets the literal 'Silent', then runs the function body
#   is_anagram('hello', 'olleh'): gets the literal 'hello', gets the literal 'olleh', then runs the function body
#   is_anagram('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   sort1 == sort2: is this True or False? The answer picks which branch
#   len(...) != len(...): is this True or False? The answer picks which branch
#   s1.count(...) != s2.count(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_anagram() — stored for later
#   2. Run is_anagram()
#       with: 
#         2.1 Assign to ?
#         2.2 If statement (one branch runs)
#         2.3 For loop over set(...)
#         2.4 Return from function
#   3. Define def is_anagram(string1, string2) — stored for later
#   4. Assign to string1
#   5. Assign to string2
#   6. Run print(is_anagram(...))
#       calls is_anagram with: string1=string1, string2=string2
#         6.1 Assign to ?
#         6.2 If statement (one branch runs)
#         6.3 For loop over set(...)
#         6.4 Return from function
#   7. Define def is_anagram(string1, string2) — stored for later
#   8. Run print(is_anagram(...))
#       calls is_anagram with: string1='Listen', string2='Silent'
#         8.1 Assign to ?
#         8.2 If statement (one branch runs)
#         8.3 For loop over set(...)
#         8.4 Return from function
#   9. Define def is_anagram(string1, string2) — stored for later
#   10. Run print(is_anagram(...))
#       calls is_anagram with: string1='hello', string2='olleh'
#         10.1 Assign to ?
#         10.2 If statement (one branch runs)
#         10.3 For loop over set(...)
#         10.4 Return from function
#   11. Assign to string1
#   12. Assign to string2
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(is_anagram(...))
#       calls is_anagram with: string1='world', string2='python'
#         15.1 Assign to ?
#         15.2 If statement (one branch runs)
#         15.3 For loop over set(...)
#         15.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_anagram, is_anagram, is_anagram, is_anagram are just stored
#   Execution starts at the non-def lines below:
#      is_anagram() → jumps into function, runs body, returns result → printed
#      string1 = ... (stored in memory)
#      string2 = ... (stored in memory)
#      print(is_anagram(string1, string2)) → function runs first, then print shows the result
#      print(is_anagram('Listen', 'Silent')) → function runs first, then print shows the result
#      print(is_anagram('hello', 'olleh')) → function runs first, then print shows the result
#      string1 = ... (stored in memory)
#      string2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_anagram('world', 'python')) → function runs first, then print shows the result
#
#   Loop: grabs one item from set(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 104:
#
# ===== SYNTAX =====
#   def is_perfect_square(num)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   if/elif/else
#   operators: * ** == <
#
# ===== EVALUATION ORDER =====
#   is_perfect_square(num): looks up variable 'num', then runs the function body
#   print(is_perfect_square(16)): the inner is_perfect_square() executes FIRST, then print() outputs the result
#   print(is_perfect_square(14)): the inner is_perfect_square() executes FIRST, then print() outputs the result
#   print(is_perfect_square(25)): the inner is_perfect_square() executes FIRST, then print() outputs the result
#   is_perfect_square(num): looks up variable 'num', then runs the function body
#   print(is_perfect_square(25)): the inner is_perfect_square() executes FIRST, then print() outputs the result
#   is_perfect_square(16): gets the literal 16, then runs the function body
#   is_perfect_square(14): gets the literal 14, then runs the function body
#   is_perfect_square(25): gets the literal 25, then runs the function body
#   is_perfect_square(25): gets the literal 25, then runs the function body
#   root * root: first get root, then get root, then compute root * root
#   root * root: first get root, then get root, then compute root * root
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   root * root: first get root, then get root, then compute root * root
#   root * root: first get root, then get root, then compute root * root
#   root * root: first get root, then get root, then compute root * root
#   root * root: first get root, then get root, then compute root * root
#   num < 0: is this True or False? The answer picks which branch
#   num < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Assign to user
#   3. Assign to root
#   4. Run print(?)
#   5. Define def is_perfect_square(num) — stored for later
#   6. Assign to num
#   7. If statement (one branch runs)
#   8. Define def is_perfect_square(num) — stored for later
#   9. Run print(is_perfect_square(...))
#       calls is_perfect_square with: num=16
#         9.1 Return from function
#   10. Run print(is_perfect_square(...))
#       calls is_perfect_square with: num=14
#         10.1 Return from function
#   11. Define def is_perfect_square(num) — stored for later
#   12. Run print(is_perfect_square(...))
#       calls is_perfect_square with: num=25
#         12.1 Return from function
#   13. Define def is_perfect_square(num) — stored for later
#   14. Define def is_perfect_square(num) — stored for later
#   15. Assign to num
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(is_perfect_square(...))
#       calls is_perfect_square with: num=25
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions is_perfect_square, is_perfect_square, is_perfect_square, is_perfect_square, is_perfect_square are just stored
#   Execution starts at the non-def lines below:
#      user = ... (stored in memory)
#      root = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      if checks a condition and runs ONE branch
#      print(is_perfect_square(16)) → function runs first, then print shows the result
#      print(is_perfect_square(14)) → function runs first, then print shows the result
#      print(is_perfect_square(25)) → function runs first, then print shows the result
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_perfect_square(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 105:
#
# ===== SYNTAX =====
#   def all_odd_numbers(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   print(all_odd_numbers(lst)): the inner all_odd_numbers() executes FIRST, then print() outputs the result
#   print(all_odd_numbers(?)): the inner all_odd_numbers() executes FIRST, then print() outputs the result
#   print(all_odd_numbers(?)): the inner all_odd_numbers() executes FIRST, then print() outputs the result
#   all_odd_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(all_odd_numbers(?)): the inner all_odd_numbers() executes FIRST, then print() outputs the result
#   all_odd_numbers(lst): looks up variable 'lst', then runs the function body
#   all_odd_numbers(?): resolves the value, then runs the function body
#   all_odd_numbers(?): resolves the value, then runs the function body
#   all_odd_numbers(?): resolves the value, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def all_odd_numbers(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(all_odd_numbers(...))
#       calls all_odd_numbers with: numbers=lst
#         3.1 Return from function
#   4. Define def all_odd_numbers(numbers) — stored for later
#   5. Run print(all_odd_numbers(...))
#       calls all_odd_numbers with: numbers=[1, 2, 3, 4, 5]
#         5.1 Return from function
#   6. Define def all_odd_numbers(numbers) — stored for later
#   7. Run print(all_odd_numbers(...))
#       calls all_odd_numbers with: numbers=[10, 15, 20, 25]
#         7.1 Return from function
#   8. Define def all_odd_numbers(numbers) — stored for later
#   9. Define def all_odd_numbers(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(all_odd_numbers(...))
#       calls all_odd_numbers with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions all_odd_numbers, all_odd_numbers, all_odd_numbers, all_odd_numbers, all_odd_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(all_odd_numbers(lst)) → function runs first, then print shows the result
#      print(all_odd_numbers(?)) → function runs first, then print shows the result
#      print(all_odd_numbers(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(all_odd_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 106:
#
# ===== SYNTAX =====
#   def sum_of_odd_numbers()
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   sum() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   map() — built-in function call
#   filter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   sum_of_odd_numbers(): — no arguments needed
#   sum_of_all_odd(): — no arguments needed
#   sum_of_odd_numbers(): — no arguments needed
#   sum_of_all_odd(): — no arguments needed
#   print(sum(?)): inner expression resolves first, then print() outputs
#   print(sum(filter(...))): inner expression resolves first, then print() outputs
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   number % 2: first get number, then get 2, then compute number % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_odd_numbers() — stored for later
#   2. Run sum_of_odd_numbers()
#         2.1 Assign to user_input
#         2.2 Assign to split_numbers
#         2.3 Assign to int_numbers
#         2.4 For loop over split_numbers
#         2.5 Assign to odd_sum
#         2.6 For loop over int_numbers
#         2.7 Assign to result
#         2.8 Return from function
#   3. Define def sum_of_all_odd() — stored for later
#   4. Run sum_of_all_odd()
#         4.1 Assign to numbers
#         4.2 Run print(sum(...))
#   5. Define def sum_of_odd_numbers() — stored for later
#   6. Run sum_of_odd_numbers()
#         6.1 Assign to user_input
#         6.2 Assign to split_numbers
#         6.3 Assign to int_numbers
#         6.4 For loop over split_numbers
#         6.5 Assign to odd_sum
#         6.6 For loop over int_numbers
#         6.7 Assign to result
#         6.8 Return from function
#   7. Define def sum_of_all_odd() — stored for later
#   8. Run sum_of_all_odd()
#         8.1 Assign to numbers
#         8.2 Run print(sum(...))
#   9. Define def sum_of_odd_numbers() — stored for later
#   10. Define def sum_of_odd_numbers() — stored for later
#   11. Assign to user_input
#   12. Assign to split_numbers
#   13. Assign to int_numbers
#   14. For loop over split_numbers
#       14.1 Run int_numbers.append(int(...))
#   15. Assign to odd_sum
#   16. For loop over int_numbers
#       16.1 If statement (one branch runs)
#   17. Run print('The sum of all odd numbers is:', odd_sum)
#   18. Assign to user_input
#   19. Assign to split_numbers
#   20. Assign to int_numbers
#   21. For loop over split_numbers
#       21.1 Run int_numbers.append(int(...))
#   22. Assign to odd_sum
#   23. For loop over int_numbers
#       23.1 If statement (one branch runs)
#   24. Run print('The sum of all odd numbers is:', odd_sum)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_odd_numbers, sum_of_all_odd, sum_of_odd_numbers, sum_of_all_odd, sum_of_odd_numbers, sum_of_odd_numbers are just stored
#   Execution starts at the non-def lines below:
#      sum_of_odd_numbers() → jumps into function, runs body, returns result → printed
#      sum_of_all_odd() → jumps into function, runs body, returns result → printed
#      sum_of_odd_numbers() → jumps into function, runs body, returns result → printed
#      sum_of_all_odd() → jumps into function, runs body, returns result → printed
#      user_input = ... (stored in memory)
#      split_numbers = ... (stored in memory)
#      int_numbers = ... (stored in memory)
#      for loop runs its body once per item
#      odd_sum = ... (stored in memory)
#      for loop runs its body once per item
#      print('The sum of all odd numbers is:', odd_sum)
#      user_input = ... (stored in memory)
#      split_numbers = ... (stored in memory)
#      int_numbers = ... (stored in memory)
#      for loop runs its body once per item
#      odd_sum = ... (stored in memory)
#      for loop runs its body once per item
#      print('The sum of all odd numbers is:', odd_sum)
#
#   Loop: grabs one item from split_numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 107:
#
# ===== SYNTAX =====
#   def common_char()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   input() — built-in function call
#   list() — built-in function call
#   set() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: != and in
#
# ===== EVALUATION ORDER =====
#   print(common_char()): the inner common_char() executes FIRST, then print() outputs the result
#   print(common_char(name1, name2)): the inner common_char() executes FIRST, then print() outputs the result
#   print(common_char('hello', 'world')): the inner common_char() executes FIRST, then print() outputs the result
#   print(common_char('programming', 'python')): the inner common_char() executes FIRST, then print() outputs the result
#   common_char(string1, string2): looks up variable 'string1', looks up variable 'string2', then runs the function body
#   print(common_char('world', 'python')): the inner common_char() executes FIRST, then print() outputs the result
#   common_char(): — no arguments needed
#   common_char(name1, name2): looks up variable 'name1', looks up variable 'name2', then runs the function body
#   common_char('hello', 'world'): gets the literal 'hello', gets the literal 'world', then runs the function body
#   common_char('programming', 'python'): gets the literal 'programming', gets the literal 'python', then runs the function body
#   common_char('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   len(...) != 2: is this True or False? The answer picks which branch
#   char in str2: is this True or False? The answer picks which branch
#   char not in common: is this True or False? The answer picks which branch
#   char in string2: is this True or False? The answer picks which branch
#   char not in characters: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def common_char() — stored for later
#   2. Run print(common_char(...))
#         2.1 Return from function
#   3. Define def common_char(string1, string2) — stored for later
#   4. Assign to name1
#   5. Assign to name2
#   6. Run print(common_char(...))
#         6.1 Return from function
#   7. Define def common_char(string1, string2) — stored for later
#   8. Run print(common_char(...))
#         8.1 Return from function
#   9. Define def common_char(string1, string2) — stored for later
#   10. Run print(common_char(...))
#         10.1 Return from function
#   11. Define def common_char() — stored for later
#   12. Define def common_char() — stored for later
#   13. Assign to string1
#   14. Assign to string2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(common_char(...))
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common_char, common_char, common_char, common_char, common_char, common_char are just stored
#   Execution starts at the non-def lines below:
#      print(common_char()) → function runs first, then print shows the result
#      name1 = ... (stored in memory)
#      name2 = ... (stored in memory)
#      print(common_char(name1, name2)) → function runs first, then print shows the result
#      print(common_char('hello', 'world')) → function runs first, then print shows the result
#      print(common_char('programming', 'python')) → function runs first, then print shows the result
#      string1 = ... (stored in memory)
#      string2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_char('world', 'python')) → function runs first, then print shows the result
#
#   Loop: grabs one item from str1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 108:
#
# ===== SYNTAX =====
#   def remove_duplicates()
#   input() — built-in function call
#   set() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: or in
#
# ===== EVALUATION ORDER =====
#   remove_duplicates(): — no arguments needed
#   remove_duplicates(): — no arguments needed
#   remove_duplicates(): — no arguments needed
#   remove_duplicates(): — no arguments needed
#   print(''.join(unique_user)): inner expression resolves first, then print() outputs
#   print(''.join(unique_user)): inner expression resolves first, then print() outputs
#   print(''.join(unique_user)): inner expression resolves first, then print() outputs
#   print(''.join(result)): inner expression resolves first, then print() outputs
#   print(''.join(dict.fromkeys(...))): inner expression resolves first, then print() outputs
#   char not in unique_user: is this True or False? The answer picks which branch
#   char not in unique_user: is this True or False? The answer picks which branch
#   char not in unique_user: is this True or False? The answer picks which branch
#   char not in unique_user: is this True or False? The answer picks which branch
#   char not in unique_user: is this True or False? The answer picks which branch
#   char not in unique_user: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates() — stored for later
#   2. Run remove_duplicates()
#         2.1 Assign to user
#         2.2 Assign to unique_user
#         2.3 For loop over user
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def remove_duplicates() — stored for later
#   4. Run remove_duplicates()
#         4.1 Assign to user
#         4.2 Assign to unique_user
#         4.3 For loop over user
#         4.4 Assign to result
#         4.5 Return from function
#   5. Define def remove_duplicates() — stored for later
#   6. Run remove_duplicates()
#         6.1 Assign to user
#         6.2 Assign to unique_user
#         6.3 For loop over user
#         6.4 Assign to result
#         6.5 Return from function
#   7. Define def remove_duplicates() — stored for later
#   8. Run remove_duplicates()
#         8.1 Assign to user
#         8.2 Assign to unique_user
#         8.3 For loop over user
#         8.4 Assign to result
#         8.5 Return from function
#   9. Define def remove_duplicates() — stored for later
#   10. Define def remove_duplicates() — stored for later
#   11. Assign to user
#   12. Assign to unique_user
#   13. For loop over user
#       13.1 If statement (one branch runs)
#   14. Run print(''.join(...))
#   15. Assign to user
#   16. Assign to unique_user
#   17. For loop over user
#       17.1 If statement (one branch runs)
#   18. Run print(''.join(...))
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      remove_duplicates() → jumps into function, runs body, returns result → printed
#      remove_duplicates() → jumps into function, runs body, returns result → printed
#      remove_duplicates() → jumps into function, runs body, returns result → printed
#      remove_duplicates() → jumps into function, runs body, returns result → printed
#      user = ... (stored in memory)
#      unique_user = ... (stored in memory)
#      for loop runs its body once per item
#      print(''.join(...))
#      user = ... (stored in memory)
#      unique_user = ... (stored in memory)
#      for loop runs its body once per item
#      print(''.join(...))
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 109:
#
# ===== SYNTAX =====
#   def is_armstrong(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   len() — built-in function call
#   int() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   while
#   operators: % ** == >
#
# ===== EVALUATION ORDER =====
#   is_armstrong(number): looks up variable 'number', then runs the function body
#   print(is_armstrong(153)): the inner is_armstrong() executes FIRST, then print() outputs the result
#   print(is_armstrong(9474)): the inner is_armstrong() executes FIRST, then print() outputs the result
#   print(is_armstrong(123)): the inner is_armstrong() executes FIRST, then print() outputs the result
#   print(is_armstrong(370)): the inner is_armstrong() executes FIRST, then print() outputs the result
#   is_armstrong(number): looks up variable 'number', then runs the function body
#   print(is_armstrong(25)): the inner is_armstrong() executes FIRST, then print() outputs the result
#   is_armstrong(153): gets the literal 153, then runs the function body
#   is_armstrong(9474): gets the literal 9474, then runs the function body
#   is_armstrong(123): gets the literal 123, then runs the function body
#   is_armstrong(370): gets the literal 370, then runs the function body
#   is_armstrong(25): gets the literal 25, then runs the function body
#   digit ** power: first get digit, then get power, then compute digit ** power
#   num % 10: first get num, then get 10, then compute num % 10
#   digit ** power: first get digit, then get power, then compute digit ** power
#   d ** power: first get d, then get power, then compute d ** power
#   total == number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_armstrong(number) — stored for later
#   2. Assign to number
#   3. If statement (one branch runs)
#   4. Run print(number)
#   5. Define def is_armstrong(number) — stored for later
#   6. Run print(is_armstrong(...))
#       calls is_armstrong with: number=153
#         6.1 Return from function
#   7. Run print(is_armstrong(...))
#       calls is_armstrong with: number=9474
#         7.1 Return from function
#   8. Run print(is_armstrong(...))
#       calls is_armstrong with: number=123
#         8.1 Return from function
#   9. Define def is_armstrong(number) — stored for later
#   10. Run print(is_armstrong(...))
#       calls is_armstrong with: number=370
#         10.1 Return from function
#   11. Define def is_armstrong(number) — stored for later
#   12. Define def is_armstrong(number) — stored for later
#   13. Assign to number
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(is_armstrong(...))
#       calls is_armstrong with: number=25
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_armstrong, is_armstrong, is_armstrong, is_armstrong, is_armstrong are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      if checks a condition and runs ONE branch
#      print(number)
#      print(is_armstrong(153)) → function runs first, then print shows the result
#      print(is_armstrong(9474)) → function runs first, then print shows the result
#      print(is_armstrong(123)) → function runs first, then print shows the result
#      print(is_armstrong(370)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_armstrong(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from str(...) each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 111:
#
# ===== SYNTAX =====
#   def second_smallest_element()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   sorted() — built-in function call
#   len() — built-in function call
#   int() — built-in function call
#   set() — built-in function call
#   min() — built-in function call
#   max() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: < >=
#
# ===== EVALUATION ORDER =====
#   print(second_smallest_element()): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(arg)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   second_smallest_element(elements): looks up variable 'elements', then runs the function body
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   second_smallest_element(): — no arguments needed
#   second_smallest_element(arg): looks up variable 'arg', then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   len(...) >= 2: is this True or False? The answer picks which branch
#   len(...) < 2: is this True or False? The answer picks which branch
#   smallest < x: is this True or False? The answer picks which branch
#   smallest < second: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def second_smallest_element() — stored for later
#   2. Run print(second_smallest_element(...))
#         2.1 Return from function
#   3. Define def second_smallest_element(elements) — stored for later
#   4. Assign to arg
#   5. Run print(second_smallest_element(...))
#         5.1 Return from function
#   6. Define def second_smallest_element(elements) — stored for later
#   7. Run print(second_smallest_element(...))
#         7.1 Return from function
#   8. Define def second_smallest_element(elements) — stored for later
#   9. Run print(second_smallest_element(...))
#         9.1 Return from function
#   10. Define def second_smallest_element(elements) — stored for later
#   11. Run print(second_smallest_element(...))
#         11.1 Return from function
#   12. Define def second_smallest_element() — stored for later
#   13. Define def second_smallest_element() — stored for later
#   14. Assign to elements
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(second_smallest_element(...))
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element are just stored
#   Execution starts at the non-def lines below:
#      print(second_smallest_element()) → function runs first, then print shows the result
#      arg = ... (stored in memory)
#      print(second_smallest_element(arg)) → function runs first, then print shows the result
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#      elements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user_input.split(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 112:
#
# ===== SYNTAX =====
#   def generate_permutations(string)
#   return — exits the function and sends back a value
#   permutations() — built-in function call
#   backtrack() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   generate_permutations(string): looks up variable 'string', then runs the function body
#   print(list(result)): inner expression resolves first, then print() outputs
#   print(generate_permutations('abc')): the inner generate_permutations() executes FIRST, then print() outputs the result
#   print(generate_permutations('ab')): the inner generate_permutations() executes FIRST, then print() outputs the result
#   generate_permutations(string): looks up variable 'string', then runs the function body
#   print(generate_permutations('world')): the inner generate_permutations() executes FIRST, then print() outputs the result
#   generate_permutations('abc'): gets the literal 'abc', then runs the function body
#   generate_permutations('ab'): gets the literal 'ab', then runs the function body
#   generate_permutations('world'): gets the literal 'world', then runs the function body
#   path + ?: first get path, then get ?, then compute path + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Import permutations from itertools
#   2. Define def generate_permutations(string) — stored for later
#   3. Assign to string
#   4. Assign to result
#   5. Run print('Permutations:', result)
#   6. Import itertools
#   7. Assign to string
#   8. Assign to result
#   9. Run print(list(...))
#   10. Define def generate_permutations(string) — stored for later
#   11. Run print(generate_permutations(...))
#       calls generate_permutations with: string='abc'
#         11.1 Return from function
#   12. Define def generate_permutations(string) — stored for later
#   13. Run print(generate_permutations(...))
#       calls generate_permutations with: string='ab'
#         13.1 Return from function
#   14. Define def generate_permutations(string) — stored for later
#   15. Define def generate_permutations(string) — stored for later
#   16. Assign to string
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(generate_permutations(...))
#       calls generate_permutations with: string='world'
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   def lines are SKIPPED during execution — functions generate_permutations, generate_permutations, generate_permutations, generate_permutations, generate_permutations are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print('Permutations:', result)
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(list(...))
#      print(generate_permutations('abc')) → function runs first, then print shows the result
#      print(generate_permutations('ab')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(generate_permutations('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from permutations(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 114:
#
# ===== SYNTAX =====
#   def max_of_three(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sorted() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: > >= and
#
# ===== EVALUATION ORDER =====
#   max_of_three(lst): looks up variable 'lst', then runs the function body
#   main(56, 8, 445): gets the literal 56, gets the literal 8, gets the literal 445, then runs the function body
#   max_of_three(): — no arguments needed
#   print(max_of_three(10, 25, 15)): the inner max_of_three() executes FIRST, then print() outputs the result
#   print(max_of_three(100, 50, 75)): the inner max_of_three() executes FIRST, then print() outputs the result
#   print(max_of_three(7, 3, 11)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(lst): looks up variable 'lst', then runs the function body
#   print(max_of_three(?)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(10, 25, 15): gets the literal 10, gets the literal 25, gets the literal 15, then runs the function body
#   max_of_three(100, 50, 75): gets the literal 100, gets the literal 50, gets the literal 75, then runs the function body
#   max_of_three(7, 3, 11): gets the literal 7, gets the literal 3, gets the literal 11, then runs the function body
#   max_of_three(?): resolves the value, then runs the function body
#   a >= b: is this True or False? The answer picks which branch
#   a >= c: is this True or False? The answer picks which branch
#   b >= a: is this True or False? The answer picks which branch
#   b >= c: is this True or False? The answer picks which branch
#   num > largest: is this True or False? The answer picks which branch
#   b >= a: is this True or False? The answer picks which branch
#   b >= c: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_three(lst) — stored for later
#   2. Assign to lst
#   3. Run max_of_three(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def main(a, b, c) — stored for later
#   5. Run main(56, 8, 445)
#       with: a=56, b=8, c=445
#         5.1 Return from function
#   6. Define def max_of_three(a, b, c) — stored for later
#   7. Run max_of_three()
#       with: 
#         7.1 Return from function
#   8. Define def max_of_three(a, b, c) — stored for later
#   9. Run print(max_of_three(...))
#       calls max_of_three with: lst=10
#         9.1 Return from function
#   10. Define def max_of_three(a, b, c) — stored for later
#   11. Run print(max_of_three(...))
#       calls max_of_three with: lst=100
#         11.1 Return from function
#   12. Define def max_of_three(a, b, c) — stored for later
#   13. Run print(max_of_three(...))
#       calls max_of_three with: lst=7
#         13.1 Return from function
#   14. Define def max_of_three(lst) — stored for later
#   15. Define def max_of_three(lst) — stored for later
#   16. Assign to lst
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(max_of_three(...))
#       calls max_of_three with: lst=[1, 3, 5, 7]
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_three, main, max_of_three, max_of_three, max_of_three, max_of_three, max_of_three, max_of_three are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      max_of_three(lst) → jumps into function, runs body, returns result → printed
#      main(56, 8, 445) → jumps into function, runs body, returns result → printed
#      max_of_three() → jumps into function, runs body, returns result → printed
#      print(max_of_three(10, 25, 15)) → function runs first, then print shows the result
#      print(max_of_three(100, 50, 75)) → function runs first, then print shows the result
#      print(max_of_three(7, 3, 11)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_of_three(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 115:
#
# ===== SYNTAX =====
#   def length_string(string)
#   return — exits the function and sends back a value
#   length_string() — built-in function call
#   enumerate() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   print(length_string(name)): the inner length_string() executes FIRST, then print() outputs the result
#   print(length_list(lst)): the inner length_list() executes FIRST, then print() outputs the result
#   print(length_string('hello')): the inner length_string() executes FIRST, then print() outputs the result
#   print(length_string('python')): the inner length_string() executes FIRST, then print() outputs the result
#   print(length_string('world')): the inner length_string() executes FIRST, then print() outputs the result
#   length_string(string): looks up variable 'string', then runs the function body
#   print(length_string('world')): the inner length_string() executes FIRST, then print() outputs the result
#   length_string(name): looks up variable 'name', then runs the function body
#   length_list(lst): looks up variable 'lst', then runs the function body
#   1 + length_string(...): first get 1, then get length_string(...), then compute 1 + length_string(...)
#   length_string('hello'): gets the literal 'hello', then runs the function body
#   length_string('python'): gets the literal 'python', then runs the function body
#   length_string('world'): gets the literal 'world', then runs the function body
#   length_string('world'): gets the literal 'world', then runs the function body
#   length_string(?): resolves the value, then runs the function body
#   i + 1: first get i, then get 1, then compute i + 1
#   s == '': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def length_string(string) — stored for later
#   2. Assign to name
#   3. Run print(length_string(...))
#       calls length_string with: string=name
#         3.1 Return from function
#   4. Define def length_list(string) — stored for later
#   5. Assign to lst
#   6. Run print(length_list(...))
#       calls length_list with: string=lst
#         6.1 Assign to count
#         6.2 For loop over string
#         6.3 Return from function
#   7. Define def length_string(s) — stored for later
#   8. Run print(length_string(...))
#       calls length_string with: string='hello'
#         8.1 Return from function
#   9. Define def length_string(s) — stored for later
#   10. Run print(length_string(...))
#       calls length_string with: string='python'
#         10.1 Return from function
#   11. Define def length_string(s) — stored for later
#   12. Run print(length_string(...))
#       calls length_string with: string='world'
#         12.1 Return from function
#   13. Define def length_string(string) — stored for later
#   14. Define def length_string(string) — stored for later
#   15. Assign to string
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(length_string(...))
#       calls length_string with: string='world'
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions length_string, length_list, length_string, length_string, length_string, length_string, length_string are just stored
#   Execution starts at the non-def lines below:
#      name = ... (stored in memory)
#      print(length_string(name)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(length_list(lst)) → function runs first, then print shows the result
#      print(length_string('hello')) → function runs first, then print shows the result
#      print(length_string('python')) → function runs first, then print shows the result
#      print(length_string('world')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(length_string('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 117:
#
# ===== SYNTAX =====
#   def translate(text)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + and
#
# ===== EVALUATION ORDER =====
#   translate(text): looks up variable 'text', then runs the function body
#   print(translate('this is fun')): the inner translate() executes FIRST, then print() outputs the result
#   print(translate('hello')): the inner translate() executes FIRST, then print() outputs the result
#   translate(text): looks up variable 'text', then runs the function body
#   print(translate('world')): the inner translate() executes FIRST, then print() outputs the result
#   translate('this is fun'): gets the literal 'this is fun', then runs the function body
#   translate('hello'): gets the literal 'hello', then runs the function body
#   translate('world'): gets the literal 'world', then runs the function body
#   ? + c: first get ?, then get c, then compute ? + c
#   ? + char: first get ?, then get char, then compute ? + char
#   c + 'o': first get c, then get 'o', then compute c + 'o'
#   char + 'o': first get char, then get 'o', then compute char + 'o'
#   ? + c: first get ?, then get c, then compute ? + c
#   c + 'o': first get c, then get 'o', then compute c + 'o'
#   char not in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def translate(text) — stored for later
#   2. Assign to text
#   3. Assign to result
#   4. Run print(result)
#   5. Define def translate(text) — stored for later
#   6. Run print(translate(...))
#       calls translate with: text='this is fun'
#         6.1 Return from function
#   7. Define def translate(text) — stored for later
#   8. Run print(translate(...))
#       calls translate with: text='hello'
#         8.1 Return from function
#   9. Define def translate(text) — stored for later
#   10. Define def translate(text) — stored for later
#   11. Assign to text
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(translate(...))
#       calls translate with: text='world'
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions translate, translate, translate, translate, translate are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(translate('this is fun')) → function runs first, then print shows the result
#      print(translate('hello')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(translate('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from text each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 118:
#
# ===== SYNTAX =====
#   def sum(numbers)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print(sum(numbers)): the inner sum() executes FIRST, then print() outputs the result
#   print(multiply(numbers)): the inner multiply() executes FIRST, then print() outputs the result
#   print(sum(?)): the inner sum() executes FIRST, then print() outputs the result
#   print(multiply(?)): the inner multiply() executes FIRST, then print() outputs the result
#   print(sum(?)): the inner sum() executes FIRST, then print() outputs the result
#   print(multiply(?)): the inner multiply() executes FIRST, then print() outputs the result
#   sum(numbers): looks up variable 'numbers', then runs the function body
#   print(sum(?)): the inner sum() executes FIRST, then print() outputs the result
#   sum(numbers): looks up variable 'numbers', then runs the function body
#   multiply(numbers): looks up variable 'numbers', then runs the function body
#   sum(?): resolves the value, then runs the function body
#   multiply(?): resolves the value, then runs the function body
#   sum(?): resolves the value, then runs the function body
#   multiply(?): resolves the value, then runs the function body
#   sum(?): resolves the value, then runs the function body
#   x * y: first get x, then get y, then compute x * y
#   x + y: first get x, then get y, then compute x + y
#   x * y: first get x, then get y, then compute x * y
#
# ===== EXECUTION ORDER =====
#   1. Define def sum(numbers) — stored for later
#   2. Define def multiply(numbers) — stored for later
#   3. Assign to numbers
#   4. Run print('Sum:', sum(...))
#   5. Run print('Multiply:', multiply(...))
#   6. Define def sum(numbers) — stored for later
#   7. Import builtins
#   8. Define def multiply(numbers) — stored for later
#   9. Run print(sum(...))
#       calls sum with: numbers=[1, 2, 3, 4]
#         9.1 Return from function
#   10. Run print(multiply(...))
#       calls multiply with: numbers=[1, 2, 3, 4]
#         10.1 Import reduce from functools
#         10.2 Return from function
#   11. Define def sum(numbers) — stored for later
#   12. Define def multiply(numbers) — stored for later
#   13. Run print(sum(...))
#       calls sum with: numbers=[5, 10, 15]
#         13.1 Return from function
#   14. Run print(multiply(...))
#       calls multiply with: numbers=[2, 3, 4]
#         14.1 Import reduce from functools
#         14.2 Return from function
#   15. Define def sum(numbers) — stored for later
#   16. Define def sum(numbers) — stored for later
#   17. Assign to numbers
#   18. Assign to result
#   19. Run print(result)
#   20. Run print(sum(...))
#       calls sum with: numbers=[4, 1, 8, 6, 3]
#         20.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum, multiply, sum, multiply, sum, multiply, sum, sum are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print('Sum:', sum(...))
#      print('Multiply:', multiply(...))
#      print(sum(?)) → function runs first, then print shows the result
#      print(multiply(?)) → function runs first, then print shows the result
#      print(sum(?)) → function runs first, then print shows the result
#      print(multiply(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 119:
#
# ===== SYNTAX =====
#   def reverse(string)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#   len() — built-in function call
#   reverse() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   reverse(string): looks up variable 'string', then runs the function body
#   print(reverse('I am testing')): the inner reverse() executes FIRST, then print() outputs the result
#   print(reverse('hello')): the inner reverse() executes FIRST, then print() outputs the result
#   print(reverse('python')): the inner reverse() executes FIRST, then print() outputs the result
#   reverse(string): looks up variable 'string', then runs the function body
#   print(reverse('world')): the inner reverse() executes FIRST, then print() outputs the result
#   reverse('I am testing'): gets the literal 'I am testing', then runs the function body
#   reverse('hello'): gets the literal 'hello', then runs the function body
#   reverse(...) + ?: first get reverse(...), then get ?, then compute reverse(...) + ?
#   reverse('python'): gets the literal 'python', then runs the function body
#   reverse('world'): gets the literal 'world', then runs the function body
#   char + reversed_string: first get char, then get reversed_string, then compute char + reversed_string
#   reverse(?): resolves the value, then runs the function body
#   len(...) <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse(string) — stored for later
#   2. Assign to string
#   3. Assign to result
#   4. Run print(result)
#   5. Define def reverse(string) — stored for later
#   6. Run print(reverse(...))
#       calls reverse with: string='I am testing'
#         6.1 Return from function
#   7. Define def reverse(string) — stored for later
#   8. Run print(reverse(...))
#       calls reverse with: string='hello'
#         8.1 Return from function
#   9. Define def reverse(string) — stored for later
#   10. Run print(reverse(...))
#       calls reverse with: string='python'
#         10.1 Return from function
#   11. Define def reverse(string) — stored for later
#   12. Define def reverse(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(reverse(...))
#       calls reverse with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse, reverse, reverse, reverse, reverse, reverse are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse('I am testing')) → function runs first, then print shows the result
#      print(reverse('hello')) → function runs first, then print shows the result
#      print(reverse('python')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 120:
#
# ===== SYNTAX =====
#   def palindromes(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   all() — built-in function call
#   range() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   while
#   operators: + - // == != <
#
# ===== EVALUATION ORDER =====
#   print(palindromes(string)): the inner palindromes() executes FIRST, then print() outputs the result
#   print(is_palindrome('radar')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('hello')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('level')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('madam')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   palindromes(string): looks up variable 'string', then runs the function body
#   print(palindromes('world')): the inner palindromes() executes FIRST, then print() outputs the result
#   palindromes(string): looks up variable 'string', then runs the function body
#   is_palindrome('radar'): gets the literal 'radar', then runs the function body
#   is_palindrome('hello'): gets the literal 'hello', then runs the function body
#   is_palindrome('level'): gets the literal 'level', then runs the function body
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   palindromes('world'): gets the literal 'world', then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   i + 1: first get i, then get 1, then compute i + 1
#   string == ?: is this True or False? The answer picks which branch
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def palindromes(string) — stored for later
#   2. Assign to string
#   3. Run print(palindromes(...))
#       calls palindromes with: string=string
#         3.1 Return from function
#   4. Define def is_palindrome(string) — stored for later
#   5. Run print(is_palindrome(...))
#       calls is_palindrome with: string='radar'
#         5.1 Return from function
#   6. Run print(is_palindrome(...))
#       calls is_palindrome with: string='hello'
#         6.1 Return from function
#   7. Define def is_palindrome(string) — stored for later
#   8. Run print(is_palindrome(...))
#       calls is_palindrome with: string='level'
#         8.1 Return from function
#   9. Define def is_palindrome(string) — stored for later
#   10. Run print(is_palindrome(...))
#       calls is_palindrome with: string='madam'
#         10.1 Return from function
#   11. Define def palindromes(string) — stored for later
#   12. Define def palindromes(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(palindromes(...))
#       calls palindromes with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions palindromes, is_palindrome, is_palindrome, is_palindrome, palindromes, palindromes are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(palindromes(string)) → function runs first, then print shows the result
#      print(is_palindrome('radar')) → function runs first, then print shows the result
#      print(is_palindrome('hello')) → function runs first, then print shows the result
#      print(is_palindrome('level')) → function runs first, then print shows the result
#      print(is_palindrome('madam')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(palindromes('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 121:
#
# ===== SYNTAX =====
#   def is_palindrome(string)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   reversed() — built-in function call
#   if/elif/else
#   while
#   operators: - == != <
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome(name)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('racecar')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('madam')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('hello')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(string): looks up variable 'string', then runs the function body
#   print(is_palindrome('world')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(name): looks up variable 'name', then runs the function body
#   is_palindrome('racecar'): gets the literal 'racecar', then runs the function body
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   is_palindrome('hello'): gets the literal 'hello', then runs the function body
#   is_palindrome('world'): gets the literal 'world', then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   string == ?: is this True or False? The answer picks which branch
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(string) — stored for later
#   2. Assign to name
#   3. Run print(is_palindrome(...))
#       calls is_palindrome with: string=name
#         3.1 Return from function
#   4. Define def is_palindrome(string) — stored for later
#   5. Run print(is_palindrome(...))
#       calls is_palindrome with: string='racecar'
#         5.1 Return from function
#   6. Define def is_palindrome(string) — stored for later
#   7. Run print(is_palindrome(...))
#       calls is_palindrome with: string='madam'
#         7.1 Return from function
#   8. Run print(is_palindrome(...))
#       calls is_palindrome with: string='hello'
#         8.1 Return from function
#   9. Define def is_palindrome(string) — stored for later
#   10. Define def is_palindrome(string) — stored for later
#   11. Assign to string
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(is_palindrome(...))
#       calls is_palindrome with: string='world'
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      name = ... (stored in memory)
#      print(is_palindrome(name)) → function runs first, then print shows the result
#      print(is_palindrome('racecar')) → function runs first, then print shows the result
#      print(is_palindrome('madam')) → function runs first, then print shows the result
#      print(is_palindrome('hello')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome('world')) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 122:
#
# ===== SYNTAX =====
#   def is_member(lst, x)
#   return — exits the function and sends back a value
#   any() — built-in function call
#   is_member() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == or in
#
# ===== EVALUATION ORDER =====
#   print(is_member(lst, x)): the inner is_member() executes FIRST, then print() outputs the result
#   is_member(x, a): looks up variable 'x', looks up variable 'a', then runs the function body
#   print(is_member(3, ?)): the inner is_member() executes FIRST, then print() outputs the result
#   print(is_member(10, ?)): the inner is_member() executes FIRST, then print() outputs the result
#   print(is_member('a', ?)): the inner is_member() executes FIRST, then print() outputs the result
#   print(is_member('z', ?)): the inner is_member() executes FIRST, then print() outputs the result
#   is_member(lst, x): looks up variable 'lst', looks up variable 'x', then runs the function body
#   print(is_member(?, 3)): the inner is_member() executes FIRST, then print() outputs the result
#   is_member(lst, x): looks up variable 'lst', looks up variable 'x', then runs the function body
#   is_member(3, ?): gets the literal 3, resolves the value, then runs the function body
#   is_member(10, ?): gets the literal 10, resolves the value, then runs the function body
#   is_member('a', ?): gets the literal 'a', resolves the value, then runs the function body
#   is_member('z', ?): gets the literal 'z', resolves the value, then runs the function body
#   is_member(?, 3): resolves the value, gets the literal 3, then runs the function body
#   is_member(x, ?): looks up variable 'x', resolves the value, then runs the function body
#   item == x: is this True or False? The answer picks which branch
#   element == x: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_member(lst, x) — stored for later
#   2. Assign to lst
#   3. Assign to x
#   4. Run print(is_member(...))
#       calls is_member with: lst=lst, x=x
#         4.1 Return from function
#   5. Define def is_member(x, a) — stored for later
#   6. Assign to x
#   7. Assign to a
#   8. Assign to result
#   9. Run print(result)
#   10. Define def is_member(x, a) — stored for later
#   11. Run print(is_member(...))
#       calls is_member with: lst=3, x=[1, 2, 3, 4]
#         11.1 Return from function
#   12. Run print(is_member(...))
#       calls is_member with: lst=10, x=[1, 2, 3]
#         12.1 Return from function
#   13. Define def is_member(x, a) — stored for later
#   14. Run print(is_member(...))
#       calls is_member with: lst='a', x=['b', 'c', 'a']
#         14.1 Return from function
#   15. Run print(is_member(...))
#       calls is_member with: lst='z', x=['a', 'b', 'c']
#         15.1 Return from function
#   16. Define def is_member(lst, x) — stored for later
#   17. Define def is_member(lst, x) — stored for later
#   18. Assign to lst
#   19. Assign to x
#   20. Assign to result
#   21. Run print(result)
#   22. Run print(is_member(...))
#       calls is_member with: lst=[1, 3, 5, 7], x=3
#         22.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_member, is_member, is_member, is_member, is_member, is_member are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      x = ... (stored in memory)
#      print(is_member(lst, x)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      a = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_member(3, ?)) → function runs first, then print shows the result
#      print(is_member(10, ?)) → function runs first, then print shows the result
#      print(is_member('a', ?)) → function runs first, then print shows the result
#      print(is_member('z', ?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_member(?, 3)) → function runs first, then print shows the result
#
#   Loop: grabs one item from a each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 123:
#
# ===== SYNTAX =====
#   def overlapping(list1, list2)
#   return — exits the function and sends back a value
#   is_member() — built-in function call
#   bool() — built-in function call
#   set() — built-in function call
#   any() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == in
#
# ===== EVALUATION ORDER =====
#   print(overlapping(list1, list2)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(list1, list2)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(list1, list2)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(list1, list2)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   overlapping(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   overlapping(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   overlapping(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   overlapping(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   overlapping(list1, list2): looks up variable 'list1', looks up variable 'list2', then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   is_member(item1, list2): looks up variable 'item1', looks up variable 'list2', then runs the function body
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   element == item: is this True or False? The answer picks which branch
#   item1 == item2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def overlapping(list1, list2) — stored for later
#   2. Assign to list1
#   3. Assign to list2
#   4. Run print(overlapping(...))
#       calls overlapping with: list1=list1, list2=list2
#         4.1 Return from function
#   5. Assign to list1
#   6. Assign to list2
#   7. Run print(overlapping(...))
#       calls overlapping with: list1=list1, list2=list2
#         7.1 Return from function
#   8. Define def is_member(element, lst) — stored for later
#   9. Define def overlapping(list1, list2) — stored for later
#   10. Assign to list1
#   11. Assign to list2
#   12. Run print(overlapping(...))
#       calls overlapping with: list1=list1, list2=list2
#         12.1 Return from function
#   13. Assign to list1
#   14. Assign to list2
#   15. Run print(overlapping(...))
#       calls overlapping with: list1=list1, list2=list2
#         15.1 Return from function
#   16. Define def overlapping(list1, list2) — stored for later
#   17. Run print(overlapping(...))
#       calls overlapping with: list1=[1, 2, 3], list2=[3, 4, 5]
#         17.1 Return from function
#   18. Run print(overlapping(...))
#       calls overlapping with: list1=[1, 2], list2=[3, 4]
#         18.1 Return from function
#   19. Define def overlapping(list1, list2) — stored for later
#   20. Run print(overlapping(...))
#       calls overlapping with: list1=['a', 'b', 'c'], list2=['c', 'd']
#         20.1 Return from function
#   21. Run print(overlapping(...))
#       calls overlapping with: list1=['x', 'y'], list2=['a', 'b']
#         21.1 Return from function
#   22. Define def overlapping(list1, list2) — stored for later
#   23. Define def overlapping(list1, list2) — stored for later
#   24. Assign to list1
#   25. Assign to list2
#   26. Assign to result
#   27. Run print(result)
#   28. Run print(overlapping(...))
#       calls overlapping with: list1=[4, 1, 8, 6, 3], list2=[4, 1, 8, 6, 3]
#         28.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions overlapping, is_member, overlapping, overlapping, overlapping, overlapping, overlapping are just stored
#   Execution starts at the non-def lines below:
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      print(overlapping(list1, list2)) → function runs first, then print shows the result
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      print(overlapping(list1, list2)) → function runs first, then print shows the result
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      print(overlapping(list1, list2)) → function runs first, then print shows the result
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      print(overlapping(list1, list2)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from list1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 124:
#
# ===== SYNTAX =====
#   def overlapping(lst1, lst2)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   set() — built-in function call
#   any() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == > in
#
# ===== EVALUATION ORDER =====
#   print(overlapping(lst1, lst2)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(lst1, lst2)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   overlapping(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(overlapping(?, ?)): the inner overlapping() executes FIRST, then print() outputs the result
#   overlapping(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   overlapping(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   overlapping(?, ?): resolves the value, resolves the value, then runs the function body
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   element in lst2: is this True or False? The answer picks which branch
#   i == j: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def overlapping(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(overlapping(...))
#       calls overlapping with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def overlapping(lst1, lst2) — stored for later
#   6. Assign to lst1
#   7. Assign to lst2
#   8. Run print(overlapping(...))
#       calls overlapping with: lst1=lst1, lst2=lst2
#         8.1 Return from function
#   9. Define def overlapping(lst1, lst2) — stored for later
#   10. Run print(overlapping(...))
#       calls overlapping with: lst1=[2, 4, 6], lst2=[6, 8, 10]
#         10.1 Return from function
#   11. Run print(overlapping(...))
#       calls overlapping with: lst1=[1, 3, 5], lst2=[2, 4, 6]
#         11.1 Return from function
#   12. Define def overlapping(lst1, lst2) — stored for later
#   13. Run print(overlapping(...))
#       calls overlapping with: lst1=['a', 'b'], lst2=['b', 'c']
#         13.1 Return from function
#   14. Run print(overlapping(...))
#       calls overlapping with: lst1=['x', 'y'], lst2=['p', 'q']
#         14.1 Return from function
#   15. Define def overlapping(lst1, lst2) — stored for later
#   16. Define def overlapping(lst1, lst2) — stored for later
#   17. Assign to lst1
#   18. Assign to lst2
#   19. Assign to result
#   20. Run print(result)
#   21. Run print(overlapping(...))
#       calls overlapping with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         21.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions overlapping, overlapping, overlapping, overlapping, overlapping, overlapping are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(overlapping(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(overlapping(lst1, lst2)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(overlapping(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 126:
#
# ===== SYNTAX =====
#   def map_words_to_lengths(words)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   map_words_to_lengths(words): looks up variable 'words', then runs the function body
#   print(map_words_to_lengths(?)): the inner map_words_to_lengths() executes FIRST, then print() outputs the result
#   print(map_words_to_lengths(?)): the inner map_words_to_lengths() executes FIRST, then print() outputs the result
#   map_words_to_lengths(words): looks up variable 'words', then runs the function body
#   print(map_words_to_lengths(?)): the inner map_words_to_lengths() executes FIRST, then print() outputs the result
#   map_words_to_lengths(?): resolves the value, then runs the function body
#   map_words_to_lengths(?): resolves the value, then runs the function body
#   map_words_to_lengths(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def map_words_to_lengths(words) — stored for later
#   2. Assign to words
#   3. Assign to lengths
#   4. Run print(lengths)
#   5. Define def map_words_to_lengths(words) — stored for later
#   6. Run print(map_words_to_lengths(...))
#       calls map_words_to_lengths with: words=['a', 'ab', 'abc']
#         6.1 Return from function
#   7. Define def map_words_to_lengths(words) — stored for later
#   8. Run print(map_words_to_lengths(...))
#       calls map_words_to_lengths with: words=['hello', 'world']
#         8.1 Return from function
#   9. Define def map_words_to_lengths(words) — stored for later
#   10. Define def map_words_to_lengths(words) — stored for later
#   11. Assign to words
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(map_words_to_lengths(...))
#       calls map_words_to_lengths with: words=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions map_words_to_lengths, map_words_to_lengths, map_words_to_lengths, map_words_to_lengths, map_words_to_lengths are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      lengths = ... (stored in memory)
#      print(lengths)
#      print(map_words_to_lengths(?)) → function runs first, then print shows the result
#      print(map_words_to_lengths(?)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(map_words_to_lengths(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 127:
#
# ===== SYNTAX =====
#   def find_longest_word(elements)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   max() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(find_longest_word(lst)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(elements): looks up variable 'elements', then runs the function body
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(lst): looks up variable 'lst', then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   element > longest_element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_longest_word(elements) — stored for later
#   2. Assign to lst
#   3. Run print(find_longest_word(...))
#       calls find_longest_word with: elements=lst
#         3.1 Return from function
#   4. Define def find_longest_word(words) — stored for later
#   5. Run print(find_longest_word(...))
#       calls find_longest_word with: elements=['hi', 'hello', 'sup']
#         5.1 Return from function
#   6. Define def find_longest_word(words) — stored for later
#   7. Run print(find_longest_word(...))
#       calls find_longest_word with: elements=['a', 'abc', 'ab']
#         7.1 Return from function
#   8. Define def find_longest_word(elements) — stored for later
#   9. Define def find_longest_word(elements) — stored for later
#   10. Assign to elements
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(find_longest_word(...))
#       calls find_longest_word with: elements=[5, 3, 8, 4]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_longest_word, find_longest_word, find_longest_word, find_longest_word, find_longest_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_longest_word(lst)) → function runs first, then print shows the result
#      print(find_longest_word(?)) → function runs first, then print shows the result
#      print(find_longest_word(?)) → function runs first, then print shows the result
#      elements = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_longest_word(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from elements each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 128:
#
# ===== SYNTAX =====
#   def filter_long_words(lst, n)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(filter_long_words(lst, n)): the inner filter_long_words() executes FIRST, then print() outputs the result
#   print(filter_long_words(?, 4)): the inner filter_long_words() executes FIRST, then print() outputs the result
#   print(filter_long_words(?, 5)): the inner filter_long_words() executes FIRST, then print() outputs the result
#   filter_long_words(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   print(filter_long_words(?, 15)): the inner filter_long_words() executes FIRST, then print() outputs the result
#   filter_long_words(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   filter_long_words(?, 4): resolves the value, gets the literal 4, then runs the function body
#   filter_long_words(?, 5): resolves the value, gets the literal 5, then runs the function body
#   filter_long_words(?, 15): resolves the value, gets the literal 15, then runs the function body
#   len(...) > n: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_long_words(lst, n) — stored for later
#   2. Assign to lst
#   3. Assign to n
#   4. Run print(filter_long_words(...))
#       calls filter_long_words with: lst=lst, n=n
#         4.1 Return from function
#   5. Define def filter_long_words(lst, n) — stored for later
#   6. Run print(filter_long_words(...))
#       calls filter_long_words with: lst=['cat', 'elephant', 'dog', 'hippopotamus'], n=4
#         6.1 Return from function
#   7. Define def filter_long_words(lst, n) — stored for later
#   8. Run print(filter_long_words(...))
#       calls filter_long_words with: lst=['tiny', 'medium', 'longerword'], n=5
#         8.1 Return from function
#   9. Define def filter_long_words(lst, n) — stored for later
#   10. Define def filter_long_words(lst, n) — stored for later
#   11. Assign to lst
#   12. Assign to n
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(filter_long_words(...))
#       calls filter_long_words with: lst=[1, 3, 5, 7], n=15
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_long_words, filter_long_words, filter_long_words, filter_long_words, filter_long_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      n = ... (stored in memory)
#      print(filter_long_words(lst, n)) → function runs first, then print shows the result
#      print(filter_long_words(?, 4)) → function runs first, then print shows the result
#      print(filter_long_words(?, 5)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_long_words(?, 15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 129:
#
# ===== SYNTAX =====
#   def is_pangram(sentence)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   all() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   is_pangram(sentence): looks up variable 'sentence', then runs the function body
#   print(is_pangram('The quick brown fox jumps over the lazy dog')): the inner is_pangram() executes FIRST, then print() outputs the result
#   print(is_pangram('Hello World')): the inner is_pangram() executes FIRST, then print() outputs the result
#   print(is_pangram('Pack my box with five dozen liquor jugs')): the inner is_pangram() executes FIRST, then print() outputs the result
#   is_pangram(sentence): looks up variable 'sentence', then runs the function body
#   print(is_pangram('Python is fun')): the inner is_pangram() executes FIRST, then print() outputs the result
#   is_pangram('The quick brown fox jumps over the lazy dog'): gets the literal 'The quick brown fox jumps over the lazy dog', then runs the function body
#   is_pangram('Hello World'): gets the literal 'Hello World', then runs the function body
#   is_pangram('Pack my box with five dozen liquor jugs'): gets the literal 'Pack my box with five dozen liquor jugs', then runs the function body
#   is_pangram('Python is fun'): gets the literal 'Python is fun', then runs the function body
#   letter not in sentence: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_pangram(sentence) — stored for later
#   2. Assign to sentence
#   3. Assign to result
#   4. Run print(result)
#   5. Define def is_pangram(sentence) — stored for later
#   6. Run print(is_pangram(...))
#       calls is_pangram with: sentence='The quick brown fox jumps over the lazy dog'
#         6.1 Return from function
#   7. Run print(is_pangram(...))
#       calls is_pangram with: sentence='Hello World'
#         7.1 Return from function
#   8. Define def is_pangram(sentence) — stored for later
#   9. Run print(is_pangram(...))
#       calls is_pangram with: sentence='Pack my box with five dozen liquor jugs'
#         9.1 Return from function
#   10. Define def is_pangram(sentence) — stored for later
#   11. Define def is_pangram(sentence) — stored for later
#   12. Assign to sentence
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(is_pangram(...))
#       calls is_pangram with: sentence='Python is fun'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_pangram, is_pangram, is_pangram, is_pangram, is_pangram are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_pangram('The quick brown fox jumps over the lazy dog')) → function runs first, then print shows the result
#      print(is_pangram('Hello World')) → function runs first, then print shows the result
#      print(is_pangram('Pack my box with five dozen liquor jugs')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_pangram('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from alphabet each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 130:
#
# ===== SYNTAX =====
#   def correct(text)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   correct(text): looks up variable 'text', then runs the function body
#   print(correct('This is very  funny and cool.Indeed!')): the inner correct() executes FIRST, then print() outputs the result
#   print(correct('Hello world.Yes')): the inner correct() executes FIRST, then print() outputs the result
#   correct(text): looks up variable 'text', then runs the function body
#   print(correct('world')): the inner correct() executes FIRST, then print() outputs the result
#   correct('This is very  funny and cool.Indeed!'): gets the literal 'This is very  funny and cool.Indeed!', then runs the function body
#   correct('Hello world.Yes'): gets the literal 'Hello world.Yes', then runs the function body
#   correct('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Define def correct(text) — stored for later
#   3. Assign to text
#   4. Assign to result
#   5. Run print(result)
#   6. Define def correct(text) — stored for later
#   7. Run print(correct(...))
#       calls correct with: text='This is very  funny and cool.Indeed!'
#         7.1 Return from function
#   8. Define def correct(text) — stored for later
#   9. Run print(correct(...))
#       calls correct with: text='Hello world.Yes'
#         9.1 Return from function
#   10. Define def correct(text) — stored for later
#   11. Define def correct(text) — stored for later
#   12. Assign to text
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(correct(...))
#       calls correct with: text='world'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions correct, correct, correct, correct, correct are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(correct('This is very  funny and cool.Indeed!')) → function runs first, then print shows the result
#      print(correct('Hello world.Yes')) → function runs first, then print shows the result
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(correct('world')) → function runs first, then print shows the result

"""

"""
Problem 131:
#
# ===== SYNTAX =====
#   def max_func(a, b)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   max() — built-in function call
#   if/elif/else
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(max_in_list(numbers)): the inner max_in_list() executes FIRST, then print() outputs the result
#   print(max_in_list(?)): the inner max_in_list() executes FIRST, then print() outputs the result
#   print(max_in_list(?)): the inner max_in_list() executes FIRST, then print() outputs the result
#   max_func(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(max_func(20, 30)): the inner max_func() executes FIRST, then print() outputs the result
#   max_in_list(numbers): looks up variable 'numbers', then runs the function body
#   max_in_list(?): resolves the value, then runs the function body
#   max_in_list(?): resolves the value, then runs the function body
#   max_func(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a > b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def max_func(a, b) — stored for later
#   3. Define def max_in_list(numbers) — stored for later
#   4. Assign to numbers
#   5. Run print(max_in_list(...))
#       calls max_in_list with: numbers=numbers
#         5.1 Return from function
#   6. Define def max_in_list(numbers) — stored for later
#   7. Run print(max_in_list(...))
#       calls max_in_list with: numbers=[10, 3, 7, 15, 2]
#         7.1 Return from function
#   8. Define def max_in_list(numbers) — stored for later
#   9. Run print(max_in_list(...))
#       calls max_in_list with: numbers=[4, 9, 2, 11]
#         9.1 Return from function
#   10. Define def max_func(a, b) — stored for later
#   11. Define def max_func(a, b) — stored for later
#   12. Assign to a
#   13. Assign to b
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(max_func(...))
#       calls max_func with: a=20, b=30
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions max_func, max_in_list, max_in_list, max_in_list, max_func, max_func are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(max_in_list(numbers)) → function runs first, then print shows the result
#      print(max_in_list(?)) → function runs first, then print shows the result
#      print(max_in_list(?)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_func(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 134:
#
# ===== SYNTAX =====
#   def sum_of_all_numbers()
#   input() — built-in function call
#   sum() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   sum_of_all_numbers(): — no arguments needed
#   sum_of_all_numbers(): — no arguments needed
#   sum_of_all_numbers(): — no arguments needed
#   print(sum(numbers)): inner expression resolves first, then print() outputs
#   print(sum(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_all_numbers() — stored for later
#   2. Run sum_of_all_numbers()
#         2.1 Assign to user_input
#         2.2 Assign to split_numbers
#         2.3 Assign to int_numbers
#         2.4 For loop over split_numbers
#         2.5 Assign to total_sum
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def sum_of_all_numbers() — stored for later
#   4. Run sum_of_all_numbers()
#         4.1 Assign to user_input
#         4.2 Assign to split_numbers
#         4.3 Assign to int_numbers
#         4.4 For loop over split_numbers
#         4.5 Assign to total_sum
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def sum_of_all_numbers() — stored for later
#   6. Run sum_of_all_numbers()
#         6.1 Assign to user_input
#         6.2 Assign to split_numbers
#         6.3 Assign to int_numbers
#         6.4 For loop over split_numbers
#         6.5 Assign to total_sum
#         6.6 Assign to result
#         6.7 Return from function
#   7. Define def sum_of_all_numbers() — stored for later
#   8. Define def sum_of_all_numbers() — stored for later
#   9. Assign to user_input
#   10. Assign to split_numbers
#   11. Assign to int_numbers
#   12. For loop over split_numbers
#       12.1 Run int_numbers.append(int(...))
#   13. Assign to total_sum
#   14. Run print(total_sum)
#   15. Assign to user_input
#   16. Assign to split_numbers
#   17. Assign to int_numbers
#   18. For loop over split_numbers
#       18.1 Run int_numbers.append(int(...))
#   19. Assign to total_sum
#   20. Run print(total_sum)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_all_numbers, sum_of_all_numbers, sum_of_all_numbers, sum_of_all_numbers, sum_of_all_numbers are just stored
#   Execution starts at the non-def lines below:
#      sum_of_all_numbers() → jumps into function, runs body, returns result → printed
#      sum_of_all_numbers() → jumps into function, runs body, returns result → printed
#      sum_of_all_numbers() → jumps into function, runs body, returns result → printed
#      user_input = ... (stored in memory)
#      split_numbers = ... (stored in memory)
#      int_numbers = ... (stored in memory)
#      for loop runs its body once per item
#      total_sum = ... (stored in memory)
#      print(total_sum)
#      user_input = ... (stored in memory)
#      split_numbers = ... (stored in memory)
#      int_numbers = ... (stored in memory)
#      for loop runs its body once per item
#      total_sum = ... (stored in memory)
#      print(total_sum)
#
#   Loop: grabs one item from split_numbers each round

"""

"""
Problem 135:
#
# ===== SYNTAX =====
#   def split_number(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(split_number(arg)): the inner split_number() executes FIRST, then print() outputs the result
#   print(split_number('123')): the inner split_number() executes FIRST, then print() outputs the result
#   print(split_number('456')): the inner split_number() executes FIRST, then print() outputs the result
#   split_number(numbers): looks up variable 'numbers', then runs the function body
#   print(split_number(?)): the inner split_number() executes FIRST, then print() outputs the result
#   split_number(arg): looks up variable 'arg', then runs the function body
#   split_number('123'): gets the literal '123', then runs the function body
#   split_number('456'): gets the literal '456', then runs the function body
#   split_number(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def split_number(numbers) — stored for later
#   2. Assign to arg
#   3. Run print(split_number(...))
#       calls split_number with: numbers=arg
#         3.1 Return from function
#   4. Define def split_number(numbers) — stored for later
#   5. Run print(split_number(...))
#       calls split_number with: numbers='123'
#         5.1 Return from function
#   6. Define def split_number(numbers) — stored for later
#   7. Run print(split_number(...))
#       calls split_number with: numbers='456'
#         7.1 Return from function
#   8. Define def split_number(numbers) — stored for later
#   9. Define def split_number(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(split_number(...))
#       calls split_number with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions split_number, split_number, split_number, split_number, split_number are just stored
#   Execution starts at the non-def lines below:
#      arg = ... (stored in memory)
#      print(split_number(arg)) → function runs first, then print shows the result
#      print(split_number('123')) → function runs first, then print shows the result
#      print(split_number('456')) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(split_number(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 136:
#
# ===== SYNTAX =====
#   def alphabetically_ordered(sentence)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(alphabetically_ordered(sentence)): the inner alphabetically_ordered() executes FIRST, then print() outputs the result
#   print(alphabetically_ordered('Banana apple Cherry')): the inner alphabetically_ordered() executes FIRST, then print() outputs the result
#   print(alphabetically_ordered('zebra cat dog')): the inner alphabetically_ordered() executes FIRST, then print() outputs the result
#   alphabetically_ordered(sentence): looks up variable 'sentence', then runs the function body
#   print(alphabetically_ordered('Python is fun')): the inner alphabetically_ordered() executes FIRST, then print() outputs the result
#   alphabetically_ordered(sentence): looks up variable 'sentence', then runs the function body
#   alphabetically_ordered('Banana apple Cherry'): gets the literal 'Banana apple Cherry', then runs the function body
#   alphabetically_ordered('zebra cat dog'): gets the literal 'zebra cat dog', then runs the function body
#   alphabetically_ordered('Python is fun'): gets the literal 'Python is fun', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def alphabetically_ordered(sentence) — stored for later
#   2. Assign to sentence
#   3. Run print(alphabetically_ordered(...))
#       calls alphabetically_ordered with: sentence=sentence
#         3.1 Return from function
#   4. Define def alphabetically_ordered(sentence) — stored for later
#   5. Run print(alphabetically_ordered(...))
#       calls alphabetically_ordered with: sentence='Banana apple Cherry'
#         5.1 Return from function
#   6. Define def alphabetically_ordered(sentence) — stored for later
#   7. Run print(alphabetically_ordered(...))
#       calls alphabetically_ordered with: sentence='zebra cat dog'
#         7.1 Return from function
#   8. Define def alphabetically_ordered(sentence) — stored for later
#   9. Define def alphabetically_ordered(sentence) — stored for later
#   10. Assign to sentence
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(alphabetically_ordered(...))
#       calls alphabetically_ordered with: sentence='Python is fun'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions alphabetically_ordered, alphabetically_ordered, alphabetically_ordered, alphabetically_ordered, alphabetically_ordered are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      print(alphabetically_ordered(sentence)) → function runs first, then print shows the result
#      print(alphabetically_ordered('Banana apple Cherry')) → function runs first, then print shows the result
#      print(alphabetically_ordered('zebra cat dog')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(alphabetically_ordered('Python is fun')) → function runs first, then print shows the result

"""

"""
Problem 137:
#
# ===== SYNTAX =====
#   def smallest_largest(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(smallest_largest(numbers)): the inner smallest_largest() executes FIRST, then print() outputs the result
#   print(smallest_largest(?)): the inner smallest_largest() executes FIRST, then print() outputs the result
#   print(smallest_largest(?)): the inner smallest_largest() executes FIRST, then print() outputs the result
#   smallest_largest(numbers): looks up variable 'numbers', then runs the function body
#   print(smallest_largest(?)): the inner smallest_largest() executes FIRST, then print() outputs the result
#   smallest_largest(numbers): looks up variable 'numbers', then runs the function body
#   smallest_largest(?): resolves the value, then runs the function body
#   smallest_largest(?): resolves the value, then runs the function body
#   smallest_largest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def smallest_largest(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(smallest_largest(...))
#       calls smallest_largest with: numbers=numbers
#         3.1 Return from function
#   4. Define def smallest_largest(numbers) — stored for later
#   5. Run print(smallest_largest(...))
#       calls smallest_largest with: numbers=[5, 1, 9, 3]
#         5.1 Return from function
#   6. Define def smallest_largest(numbers) — stored for later
#   7. Run print(smallest_largest(...))
#       calls smallest_largest with: numbers=[10, 2, 8]
#         7.1 Return from function
#   8. Define def smallest_largest(numbers) — stored for later
#   9. Define def smallest_largest(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(smallest_largest(...))
#       calls smallest_largest with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions smallest_largest, smallest_largest, smallest_largest, smallest_largest, smallest_largest are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(smallest_largest(numbers)) → function runs first, then print shows the result
#      print(smallest_largest(?)) → function runs first, then print shows the result
#      print(smallest_largest(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(smallest_largest(?)) → function runs first, then print shows the result

"""

"""
Problem 138:
#
# ===== SYNTAX =====
#   def largest_smallest(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(largest_smallest(numbers)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   largest_smallest(numbers): looks up variable 'numbers', then runs the function body
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   largest_smallest(numbers): looks up variable 'numbers', then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_smallest(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(largest_smallest(...))
#       calls largest_smallest with: numbers=numbers
#         3.1 Return from function
#   4. Define def largest_smallest(numbers) — stored for later
#   5. Run print(largest_smallest(...))
#       calls largest_smallest with: numbers=[5, 1, 9, 3]
#         5.1 Return from function
#   6. Define def largest_smallest(numbers) — stored for later
#   7. Run print(largest_smallest(...))
#       calls largest_smallest with: numbers=[10, 2, 8]
#         7.1 Return from function
#   8. Define def largest_smallest(numbers) — stored for later
#   9. Define def largest_smallest(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(largest_smallest(...))
#       calls largest_smallest with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_smallest, largest_smallest, largest_smallest, largest_smallest, largest_smallest are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(largest_smallest(numbers)) → function runs first, then print shows the result
#      print(largest_smallest(?)) → function runs first, then print shows the result
#      print(largest_smallest(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_smallest(?)) → function runs first, then print shows the result

"""

"""
Problem 139:
#
# ===== SYNTAX =====
#   def seconded_largest_number(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(seconded_largest_number(numbers)): the inner seconded_largest_number() executes FIRST, then print() outputs the result
#   print(seconded_largest_number(?)): the inner seconded_largest_number() executes FIRST, then print() outputs the result
#   print(seconded_largest_number(?)): the inner seconded_largest_number() executes FIRST, then print() outputs the result
#   seconded_largest_number(numbers): looks up variable 'numbers', then runs the function body
#   print(seconded_largest_number(?)): the inner seconded_largest_number() executes FIRST, then print() outputs the result
#   seconded_largest_number(numbers): looks up variable 'numbers', then runs the function body
#   seconded_largest_number(?): resolves the value, then runs the function body
#   seconded_largest_number(?): resolves the value, then runs the function body
#   seconded_largest_number(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def seconded_largest_number(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(seconded_largest_number(...))
#       calls seconded_largest_number with: numbers=numbers
#         3.1 Return from function
#   4. Define def seconded_largest_number(numbers) — stored for later
#   5. Run print(seconded_largest_number(...))
#       calls seconded_largest_number with: numbers=[10, 5, 8, 3, 12]
#         5.1 Return from function
#   6. Define def seconded_largest_number(numbers) — stored for later
#   7. Run print(seconded_largest_number(...))
#       calls seconded_largest_number with: numbers=[5, 5, 3, 1, 4]
#         7.1 Return from function
#   8. Define def seconded_largest_number(numbers) — stored for later
#   9. Define def seconded_largest_number(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(seconded_largest_number(...))
#       calls seconded_largest_number with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions seconded_largest_number, seconded_largest_number, seconded_largest_number, seconded_largest_number, seconded_largest_number are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(seconded_largest_number(numbers)) → function runs first, then print shows the result
#      print(seconded_largest_number(?)) → function runs first, then print shows the result
#      print(seconded_largest_number(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(seconded_largest_number(?)) → function runs first, then print shows the result

"""

"""
Problem 140:
#
# ===== SYNTAX =====
#   def fourth_largest_number(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(fourth_largest_number(numbers)): the inner fourth_largest_number() executes FIRST, then print() outputs the result
#   print(fourth_largest_number(?)): the inner fourth_largest_number() executes FIRST, then print() outputs the result
#   print(fourth_largest_number(?)): the inner fourth_largest_number() executes FIRST, then print() outputs the result
#   fourth_largest_number(numbers): looks up variable 'numbers', then runs the function body
#   print(fourth_largest_number(?)): the inner fourth_largest_number() executes FIRST, then print() outputs the result
#   fourth_largest_number(numbers): looks up variable 'numbers', then runs the function body
#   fourth_largest_number(?): resolves the value, then runs the function body
#   fourth_largest_number(?): resolves the value, then runs the function body
#   fourth_largest_number(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def fourth_largest_number(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(fourth_largest_number(...))
#       calls fourth_largest_number with: numbers=numbers
#         3.1 Return from function
#   4. Define def fourth_largest_number(numbers) — stored for later
#   5. Run print(fourth_largest_number(...))
#       calls fourth_largest_number with: numbers=[10, 5, 8, 3, 12, 7]
#         5.1 Return from function
#   6. Define def fourth_largest_number(numbers) — stored for later
#   7. Run print(fourth_largest_number(...))
#       calls fourth_largest_number with: numbers=[9, 2, 7, 5, 1, 3]
#         7.1 Return from function
#   8. Define def fourth_largest_number(numbers) — stored for later
#   9. Define def fourth_largest_number(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(fourth_largest_number(...))
#       calls fourth_largest_number with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fourth_largest_number, fourth_largest_number, fourth_largest_number, fourth_largest_number, fourth_largest_number are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(fourth_largest_number(numbers)) → function runs first, then print shows the result
#      print(fourth_largest_number(?)) → function runs first, then print shows the result
#      print(fourth_largest_number(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fourth_largest_number(?)) → function runs first, then print shows the result

"""

"""
Problem 141:
#
# ===== SYNTAX =====
#   def smallest_and_third(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   min() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(smallest_and_third(numbers)): the inner smallest_and_third() executes FIRST, then print() outputs the result
#   print(smallest_and_third(?)): the inner smallest_and_third() executes FIRST, then print() outputs the result
#   print(smallest_and_third(?)): the inner smallest_and_third() executes FIRST, then print() outputs the result
#   smallest_and_third(numbers): looks up variable 'numbers', then runs the function body
#   print(smallest_and_third(?)): the inner smallest_and_third() executes FIRST, then print() outputs the result
#   smallest_and_third(numbers): looks up variable 'numbers', then runs the function body
#   smallest_and_third(?): resolves the value, then runs the function body
#   smallest_and_third(?): resolves the value, then runs the function body
#   smallest_and_third(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def smallest_and_third(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(smallest_and_third(...))
#       calls smallest_and_third with: numbers=numbers
#         3.1 Return from function
#   4. Define def smallest_and_third(numbers) — stored for later
#   5. Run print(smallest_and_third(...))
#       calls smallest_and_third with: numbers=[5, 9, 2, 7, 11, 3]
#         5.1 Return from function
#   6. Define def smallest_and_third(numbers) — stored for later
#   7. Run print(smallest_and_third(...))
#       calls smallest_and_third with: numbers=[10, 3, 8, 1, 6, 9]
#         7.1 Return from function
#   8. Define def smallest_and_third(numbers) — stored for later
#   9. Define def smallest_and_third(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(smallest_and_third(...))
#       calls smallest_and_third with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions smallest_and_third, smallest_and_third, smallest_and_third, smallest_and_third, smallest_and_third are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(smallest_and_third(numbers)) → function runs first, then print shows the result
#      print(smallest_and_third(?)) → function runs first, then print shows the result
#      print(smallest_and_third(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(smallest_and_third(?)) → function runs first, then print shows the result

"""

"""
Problem 142:
#
# ===== SYNTAX =====
#   def largest_smallest(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(largest_smallest(numbers)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   print(smallest_biggest(lst)): the inner smallest_biggest() executes FIRST, then print() outputs the result
#   print(smallest_biggest(?)): the inner smallest_biggest() executes FIRST, then print() outputs the result
#   print(smallest_biggest(?)): the inner smallest_biggest() executes FIRST, then print() outputs the result
#   largest_smallest(numbers): looks up variable 'numbers', then runs the function body
#   print(largest_smallest(?)): the inner largest_smallest() executes FIRST, then print() outputs the result
#   largest_smallest(numbers): looks up variable 'numbers', then runs the function body
#   smallest_biggest(lst): looks up variable 'lst', then runs the function body
#   smallest_biggest(?): resolves the value, then runs the function body
#   smallest_biggest(?): resolves the value, then runs the function body
#   largest_smallest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_smallest(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(largest_smallest(...))
#       calls largest_smallest with: numbers=numbers
#         3.1 Return from function
#   4. Define def smallest_biggest(numbers) — stored for later
#   5. Assign to lst
#   6. Run print(smallest_biggest(...))
#       calls smallest_biggest with: numbers=lst
#         6.1 Return from function
#   7. Define def smallest_biggest(numbers) — stored for later
#   8. Run print(smallest_biggest(...))
#       calls smallest_biggest with: numbers=[3, 1, 4, 2]
#         8.1 Return from function
#   9. Define def smallest_biggest(numbers) — stored for later
#   10. Run print(smallest_biggest(...))
#       calls smallest_biggest with: numbers=[9, 5, 7]
#         10.1 Return from function
#   11. Define def largest_smallest(numbers) — stored for later
#   12. Define def largest_smallest(numbers) — stored for later
#   13. Assign to numbers
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(largest_smallest(...))
#       calls largest_smallest with: numbers=[4, 1, 8, 6, 3]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_smallest, smallest_biggest, smallest_biggest, smallest_biggest, largest_smallest, largest_smallest are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(largest_smallest(numbers)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(smallest_biggest(lst)) → function runs first, then print shows the result
#      print(smallest_biggest(?)) → function runs first, then print shows the result
#      print(smallest_biggest(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_smallest(?)) → function runs first, then print shows the result

"""

"""
Problem 143:
#
# ===== SYNTAX =====
#   def int_str()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   int_str(): — no arguments needed
#   int_str(): — no arguments needed
#   int_str(): — no arguments needed
#   print(input(...).split()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define def int_str() — stored for later
#   2. Run int_str()
#         2.1 Assign to user_lst
#         2.2 Assign to user
#         2.3 Assign to split_str
#         2.4 For loop over split_str
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def int_str() — stored for later
#   4. Run int_str()
#         4.1 Assign to user_lst
#         4.2 Assign to user
#         4.3 Assign to split_str
#         4.4 For loop over split_str
#         4.5 Assign to result
#         4.6 Return from function
#   5. Define def int_str() — stored for later
#   6. Run int_str()
#         6.1 Assign to user_lst
#         6.2 Assign to user
#         6.3 Assign to split_str
#         6.4 For loop over split_str
#         6.5 Assign to result
#         6.6 Return from function
#   7. Define def int_str() — stored for later
#   8. Define def int_str() — stored for later
#   9. Assign to user_lst
#   10. Assign to user
#   11. Assign to split_str
#   12. For loop over split_str
#       12.1 Run user_lst.append(word)
#   13. Run print(user_lst)
#   14. Assign to user_lst
#   15. Assign to user
#   16. Assign to split_str
#   17. For loop over split_str
#       17.1 Run user_lst.append(word)
#   18. Run print(user_lst)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions int_str, int_str, int_str, int_str, int_str are just stored
#   Execution starts at the non-def lines below:
#      int_str() → jumps into function, runs body, returns result → printed
#      int_str() → jumps into function, runs body, returns result → printed
#      int_str() → jumps into function, runs body, returns result → printed
#      user_lst = ... (stored in memory)
#      user = ... (stored in memory)
#      split_str = ... (stored in memory)
#      for loop runs its body once per item
#      print(user_lst)
#      user_lst = ... (stored in memory)
#      user = ... (stored in memory)
#      split_str = ... (stored in memory)
#      for loop runs its body once per item
#      print(user_lst)
#
#   Loop: grabs one item from split_str each round

"""

"""
Problem 144:
#
# ===== SYNTAX =====
#   def int_to_str_list(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(int_to_str_list(number)): the inner int_to_str_list() executes FIRST, then print() outputs the result
#   print(int_to_str_list(456)): the inner int_to_str_list() executes FIRST, then print() outputs the result
#   print(int_to_str_list(789)): the inner int_to_str_list() executes FIRST, then print() outputs the result
#   int_to_str_list(number): looks up variable 'number', then runs the function body
#   print(int_to_str_list(25)): the inner int_to_str_list() executes FIRST, then print() outputs the result
#   int_to_str_list(number): looks up variable 'number', then runs the function body
#   int_to_str_list(456): gets the literal 456, then runs the function body
#   int_to_str_list(789): gets the literal 789, then runs the function body
#   int_to_str_list(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def int_to_str_list(number) — stored for later
#   2. Assign to number
#   3. Run print(int_to_str_list(...))
#       calls int_to_str_list with: number=number
#         3.1 Return from function
#   4. Define def int_to_str_list(number) — stored for later
#   5. Run print(int_to_str_list(...))
#       calls int_to_str_list with: number=456
#         5.1 Return from function
#   6. Define def int_to_str_list(number) — stored for later
#   7. Run print(int_to_str_list(...))
#       calls int_to_str_list with: number=789
#         7.1 Return from function
#   8. Define def int_to_str_list(number) — stored for later
#   9. Define def int_to_str_list(number) — stored for later
#   10. Assign to number
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(int_to_str_list(...))
#       calls int_to_str_list with: number=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions int_to_str_list, int_to_str_list, int_to_str_list, int_to_str_list, int_to_str_list are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(int_to_str_list(number)) → function runs first, then print shows the result
#      print(int_to_str_list(456)) → function runs first, then print shows the result
#      print(int_to_str_list(789)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(int_to_str_list(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from number_str each round

"""

"""
Problem 145:
#
# ===== SYNTAX =====
#   def common_numbers(lst1, lst2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: == and in
#
# ===== EVALUATION ORDER =====
#   print(common_numbers(lst1, lst2)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?, ?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?, ?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   common_numbers(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(common_numbers(?, ?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   common_numbers(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   common_numbers(?, ?): resolves the value, resolves the value, then runs the function body
#   common_numbers(?, ?): resolves the value, resolves the value, then runs the function body
#   common_numbers(?, ?): resolves the value, resolves the value, then runs the function body
#   i == j: is this True or False? The answer picks which branch
#   i not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def common_numbers(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(common_numbers(...))
#       calls common_numbers with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def common_numbers(lst1, lst2) — stored for later
#   6. Run print(common_numbers(...))
#       calls common_numbers with: lst1=[1, 2, 3], lst2=[3, 4, 5]
#         6.1 Return from function
#   7. Define def common_numbers(lst1, lst2) — stored for later
#   8. Run print(common_numbers(...))
#       calls common_numbers with: lst1=['a', 'b', 'c'], lst2=['b', 'c', 'd']
#         8.1 Return from function
#   9. Define def common_numbers(lst1, lst2) — stored for later
#   10. Define def common_numbers(lst1, lst2) — stored for later
#   11. Assign to lst1
#   12. Assign to lst2
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(common_numbers(...))
#       calls common_numbers with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common_numbers, common_numbers, common_numbers, common_numbers, common_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(common_numbers(lst1, lst2)) → function runs first, then print shows the result
#      print(common_numbers(?, ?)) → function runs first, then print shows the result
#      print(common_numbers(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_numbers(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 146:
#
# ===== SYNTAX =====
#   def big_small(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   int() — built-in function call
#   str() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(big_small(lst)): the inner big_small() executes FIRST, then print() outputs the result
#   print(big_small(?)): the inner big_small() executes FIRST, then print() outputs the result
#   print(big_small(?)): the inner big_small() executes FIRST, then print() outputs the result
#   big_small(numbers): looks up variable 'numbers', then runs the function body
#   print(big_small(?)): the inner big_small() executes FIRST, then print() outputs the result
#   big_small(lst): looks up variable 'lst', then runs the function body
#   big_small(?): resolves the value, then runs the function body
#   big_small(?): resolves the value, then runs the function body
#   big_small(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def big_small(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(big_small(...))
#       calls big_small with: numbers=lst
#         3.1 Return from function
#   4. Define def big_small(numbers) — stored for later
#   5. Run print(big_small(...))
#       calls big_small with: numbers=['4', '67', '6']
#         5.1 Return from function
#   6. Define def big_small(numbers) — stored for later
#   7. Run print(big_small(...))
#       calls big_small with: numbers=['10', '2', '35']
#         7.1 Return from function
#   8. Define def big_small(numbers) — stored for later
#   9. Define def big_small(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(big_small(...))
#       calls big_small with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions big_small, big_small, big_small, big_small, big_small are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(big_small(lst)) → function runs first, then print shows the result
#      print(big_small(?)) → function runs first, then print shows the result
#      print(big_small(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(big_small(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 147:
#
# ===== SYNTAX =====
#   def type_smallest_biggest()
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   str() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   type_smallest_biggest(): — no arguments needed
#   print(small_big()): the inner small_big() executes FIRST, then print() outputs the result
#   print(small_big()): the inner small_big() executes FIRST, then print() outputs the result
#   type_smallest_biggest(): — no arguments needed
#   small_big(): — no arguments needed
#   small_big(): — no arguments needed
#   print(' '.join(sorted(...))): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define def type_smallest_biggest() — stored for later
#   2. Run type_smallest_biggest()
#         2.1 Assign to user
#         2.2 Assign to sort_number
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def small_big() — stored for later
#   4. Run print(small_big(...))
#         4.1 Assign to nums
#         4.2 Run nums.sort()
#         4.3 Return from function
#   5. Define def small_big() — stored for later
#   6. Run print(small_big(...))
#         6.1 Assign to nums
#         6.2 Run nums.sort()
#         6.3 Return from function
#   7. Define def type_smallest_biggest() — stored for later
#   8. Run type_smallest_biggest()
#         8.1 Assign to user
#         8.2 Assign to sort_number
#         8.3 Assign to result
#         8.4 Return from function
#   9. Define def type_smallest_biggest() — stored for later
#   10. Define def type_smallest_biggest() — stored for later
#   11. Assign to user
#   12. Assign to sort_number
#   13. Run print(sort_number)
#   14. Assign to user
#   15. Assign to sort_number
#   16. Run print(sort_number)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions type_smallest_biggest, small_big, small_big, type_smallest_biggest, type_smallest_biggest, type_smallest_biggest are just stored
#   Execution starts at the non-def lines below:
#      type_smallest_biggest() → jumps into function, runs body, returns result → printed
#      print(small_big()) → function runs first, then print shows the result
#      print(small_big()) → function runs first, then print shows the result
#      type_smallest_biggest() → jumps into function, runs body, returns result → printed
#      user = ... (stored in memory)
#      sort_number = ... (stored in memory)
#      print(sort_number)
#      user = ... (stored in memory)
#      sort_number = ... (stored in memory)
#      print(sort_number)
#
#   Loop: grabs one item from user_split each round

"""

"""
Problem 148:
#
# ===== SYNTAX =====
#   def double_elements(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print(double_elements(lst)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(numbers): looks up variable 'numbers', then runs the function body
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   numbers + numbers: first get numbers, then get numbers, then compute numbers + numbers
#   double_elements(lst): looks up variable 'lst', then runs the function body
#   numbers * 2: first get numbers, then get 2, then compute numbers * 2
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def double_elements(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(double_elements(...))
#       calls double_elements with: numbers=lst
#         3.1 Import operator
#         3.2 Return from function
#   4. Define def double_elements(numbers) — stored for later
#   5. Run print(double_elements(...))
#       calls double_elements with: numbers=[1, 2, 3]
#         5.1 Import operator
#         5.2 Return from function
#   6. Define def double_elements(numbers) — stored for later
#   7. Run print(double_elements(...))
#       calls double_elements with: numbers=[5, 6, 7]
#         7.1 Import operator
#         7.2 Return from function
#   8. Define def double_elements(numbers) — stored for later
#   9. Define def double_elements(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(double_elements(...))
#       calls double_elements with: numbers=[4, 1, 8, 6, 3]
#         13.1 Import operator
#         13.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions double_elements, double_elements, double_elements, double_elements, double_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(double_elements(lst)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(double_elements(?)) → function runs first, then print shows the result

"""

"""
Problem 149:
#
# ===== SYNTAX =====
#   def common()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   list() — built-in function call
#   set() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: == and in
#
# ===== EVALUATION ORDER =====
#   common(): — no arguments needed
#   print(common()): the inner common() executes FIRST, then print() outputs the result
#   print(common()): the inner common() executes FIRST, then print() outputs the result
#   common(): — no arguments needed
#   common(): — no arguments needed
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   i == j: is this True or False? The answer picks which branch
#   i not in result: is this True or False? The answer picks which branch
#   i == j: is this True or False? The answer picks which branch
#   i not in result: is this True or False? The answer picks which branch
#   i == j: is this True or False? The answer picks which branch
#   i not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def common() — stored for later
#   2. Run common()
#         2.1 Return from function
#   3. Define def common() — stored for later
#   4. Run print(common(...))
#         4.1 Return from function
#   5. Define def common() — stored for later
#   6. Run print(common(...))
#         6.1 Return from function
#   7. Define def common() — stored for later
#   8. Define def common() — stored for later
#   9. Assign to lst1
#   10. Assign to lst2
#   11. Assign to result
#   12. For loop over lst1
#       12.1 For loop over lst2
#   13. Assign to result
#   14. Run print(result)
#   15. Assign to lst1
#   16. Assign to lst2
#   17. Assign to result
#   18. For loop over lst1
#       18.1 For loop over lst2
#   19. Assign to result
#   20. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common, common, common, common, common are just stored
#   Execution starts at the non-def lines below:
#      common() → jumps into function, runs body, returns result → printed
#      print(common()) → function runs first, then print shows the result
#      print(common()) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from lst1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 150:
#
# ===== SYNTAX =====
#   def common()
#   set() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   common(): — no arguments needed
#   common(): — no arguments needed
#   common(): — no arguments needed
#   print(list(set(...).intersection(...))): inner expression resolves first, then print() outputs
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def common() — stored for later
#   2. Run common()
#         2.1 Assign to lst1
#         2.2 Assign to lst2
#         2.3 Assign to set1
#         2.4 Assign to set2
#         2.5 Assign to common_element
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def common() — stored for later
#   4. Run common()
#         4.1 Assign to lst1
#         4.2 Assign to lst2
#         4.3 Assign to set1
#         4.4 Assign to set2
#         4.5 Assign to common_element
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def common() — stored for later
#   6. Run common()
#         6.1 Assign to lst1
#         6.2 Assign to lst2
#         6.3 Assign to set1
#         6.4 Assign to set2
#         6.5 Assign to common_element
#         6.6 Assign to result
#         6.7 Return from function
#   7. Define def common() — stored for later
#   8. Define def common() — stored for later
#   9. Assign to lst1
#   10. Assign to lst2
#   11. Assign to set1
#   12. Assign to set2
#   13. Assign to common_element
#   14. Run print(common_element)
#   15. Assign to lst1
#   16. Assign to lst2
#   17. Assign to set1
#   18. Assign to set2
#   19. Assign to common_element
#   20. Run print(common_element)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common, common, common, common, common are just stored
#   Execution starts at the non-def lines below:
#      common() → jumps into function, runs body, returns result → printed
#      common() → jumps into function, runs body, returns result → printed
#      common() → jumps into function, runs body, returns result → printed
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      set1 = ... (stored in memory)
#      set2 = ... (stored in memory)
#      common_element = ... (stored in memory)
#      print(common_element)
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      set1 = ... (stored in memory)
#      set2 = ... (stored in memory)
#      common_element = ... (stored in memory)
#      print(common_element)

"""

"""
Problem 151:
#
# ===== SYNTAX =====
#   def find_longest_word(words)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   max() — built-in function call
#   reduce() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: > >=
#
# ===== EVALUATION ORDER =====
#   print(find_longest_word(words)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(words): looks up variable 'words', then runs the function body
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(words): looks up variable 'words', then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   len(...) > count: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_longest_word(words) — stored for later
#   2. Assign to words
#   3. Run print(find_longest_word(...))
#       calls find_longest_word with: words=words
#         3.1 Return from function
#   4. Define def find_longest_word(words) — stored for later
#   5. Run print(find_longest_word(...))
#       calls find_longest_word with: words=['hello', 'world', 'python']
#         5.1 Return from function
#   6. Define def find_longest_word(words) — stored for later
#   7. Run print(find_longest_word(...))
#       calls find_longest_word with: words=['a', 'ab', 'abc']
#         7.1 Return from function
#   8. Define def find_longest_word(words) — stored for later
#   9. Run print(find_longest_word(...))
#       calls find_longest_word with: words=['short', 'longer', 'longest']
#         9.1 Return from function
#   10. Define def find_longest_word(words) — stored for later
#   11. Define def find_longest_word(words) — stored for later
#   12. Assign to words
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(find_longest_word(...))
#       calls find_longest_word with: words=[4, 1, 8, 6, 3]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_longest_word, find_longest_word, find_longest_word, find_longest_word, find_longest_word, find_longest_word are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(find_longest_word(words)) → function runs first, then print shows the result
#      print(find_longest_word(?)) → function runs first, then print shows the result
#      print(find_longest_word(?)) → function runs first, then print shows the result
#      print(find_longest_word(?)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_longest_word(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 152:
#
# ===== SYNTAX =====
#   def average(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   reduce() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   for
#   in
#   lambda
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   print(average(numbers)): the inner average() executes FIRST, then print() outputs the result
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   average(numbers): looks up variable 'numbers', then runs the function body
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   average(numbers): looks up variable 'numbers', then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   average(?): resolves the value, then runs the function body
#   average(?): resolves the value, then runs the function body
#   reduce(...) / len(...): first get reduce(...), then get len(...), then compute reduce(...) / len(...)
#   average(?): resolves the value, then runs the function body
#   total * ?: first get total, then get ?, then compute total * ?
#   average(?): resolves the value, then runs the function body
#   total / length: first get total, then get length, then compute total / length
#   1 / length: first get 1, then get length, then compute 1 / length
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Define def average(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(average(...))
#       calls average with: numbers=numbers
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def average(numbers) — stored for later
#   5. Run print(average(...))
#       calls average with: numbers=[4, 667, 68, 766]
#         5.1 Import Fraction from fractions
#         5.2 Return from function
#   6. Define def average(numbers) — stored for later
#   7. Run print(average(...))
#       calls average with: numbers=[10, 20, 30, 40]
#         7.1 Import Fraction from fractions
#         7.2 Return from function
#   8. Define def average(numbers) — stored for later
#   9. Run print(average(...))
#       calls average with: numbers=[1, 2, 3, 4]
#         9.1 Import Fraction from fractions
#         9.2 Return from function
#   10. Define def average(numbers) — stored for later
#   11. Define def average(numbers) — stored for later
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(average(...))
#       calls average with: numbers=[4, 1, 8, 6, 3]
#         15.1 Import Fraction from fractions
#         15.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average, average, average, average, average, average are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(average(numbers)) → function runs first, then print shows the result
#      print(average(?)) → function runs first, then print shows the result
#      print(average(?)) → function runs first, then print shows the result
#      print(average(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 153:
#
# ===== SYNTAX =====
#   def second_smallest_element(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   set() — built-in function call
#   if/elif/else
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   print(second_smallest_element(lst)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(lst)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   second_smallest_element(lst): looks up variable 'lst', then runs the function body
#   print(second_smallest_element(?)): the inner second_smallest_element() executes FIRST, then print() outputs the result
#   second_smallest_element(lst): looks up variable 'lst', then runs the function body
#   second_smallest_element(lst): looks up variable 'lst', then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   second_smallest_element(?): resolves the value, then runs the function body
#   len(...) >= 2: is this True or False? The answer picks which branch
#   len(...) >= 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def second_smallest_element(lst) — stored for later
#   2. Assign to lst
#   3. Run print(second_smallest_element(...))
#       calls second_smallest_element with: lst=lst
#         3.1 Return from function
#   4. Define def second_smallest_element(lst) — stored for later
#   5. Assign to lst
#   6. Run print(second_smallest_element(...))
#       calls second_smallest_element with: lst=lst
#         6.1 Return from function
#   7. Define def second_smallest_element(lst) — stored for later
#   8. Run print(second_smallest_element(...))
#       calls second_smallest_element with: lst=[5, 3, 8, 1, 9]
#         8.1 Return from function
#   9. Define def second_smallest_element(lst) — stored for later
#   10. Run print(second_smallest_element(...))
#       calls second_smallest_element with: lst=[7, 7, 1, 1, 5]
#         10.1 Return from function
#   11. Define def second_smallest_element(lst) — stored for later
#   12. Define def second_smallest_element(lst) — stored for later
#   13. Assign to lst
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(second_smallest_element(...))
#       calls second_smallest_element with: lst=[1, 3, 5, 7]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element, second_smallest_element are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(second_smallest_element(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(second_smallest_element(lst)) → function runs first, then print shows the result
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_smallest_element(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 154:
#
# ===== SYNTAX =====
#   def find_last_element()
#   return — exits the function and sends back a value
#   input() — built-in function call
#   len() — built-in function call
#   if/elif/else
#   operators: < >=
#
# ===== EVALUATION ORDER =====
#   print(find_last_element()): the inner find_last_element() executes FIRST, then print() outputs the result
#   print(find_second_last_element(?)): the inner find_second_last_element() executes FIRST, then print() outputs the result
#   print(find_second_last_element(?)): the inner find_second_last_element() executes FIRST, then print() outputs the result
#   find_second_last_element(lst): looks up variable 'lst', then runs the function body
#   print(find_second_last_element(?)): the inner find_second_last_element() executes FIRST, then print() outputs the result
#   find_last_element(): — no arguments needed
#   find_second_last_element(?): resolves the value, then runs the function body
#   find_second_last_element(?): resolves the value, then runs the function body
#   find_second_last_element(?): resolves the value, then runs the function body
#   len(...) < 2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_last_element() — stored for later
#   2. Run print(find_last_element(...))
#         2.1 Return from function
#   3. Define def find_second_last_element(lst) — stored for later
#   4. Run print(find_second_last_element(...))
#       calls find_second_last_element with: lst=[1, 2, 3, 4, 5]
#         4.1 Return from function
#   5. Define def find_second_last_element(lst) — stored for later
#   6. Run print(find_second_last_element(...))
#       calls find_second_last_element with: lst=['a', 'b', 'c']
#         6.1 Return from function
#   7. Define def find_last_element() — stored for later
#   8. Define def find_last_element() — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_second_last_element(...))
#       calls find_second_last_element with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_last_element, find_second_last_element, find_second_last_element, find_last_element, find_last_element are just stored
#   Execution starts at the non-def lines below:
#      print(find_last_element()) → function runs first, then print shows the result
#      print(find_second_last_element(?)) → function runs first, then print shows the result
#      print(find_second_last_element(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_second_last_element(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 155:
#
# ===== SYNTAX =====
#   def reverse_number(number)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   str() — built-in function call
#   abs() — built-in function call
#   while
#   operators: + * % > >=
#
# ===== EVALUATION ORDER =====
#   print(reverse_number(lst)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(number)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(lst)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(123)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(?)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(1000)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(number): looks up variable 'number', then runs the function body
#   print(reverse_number(25)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(lst): looks up variable 'lst', then runs the function body
#   reverse_number(number): looks up variable 'number', then runs the function body
#   reverse_number(lst): looks up variable 'lst', then runs the function body
#   reverse_number(123): gets the literal 123, then runs the function body
#   reverse_number(?): resolves the value, then runs the function body
#   reverse_number(1000): gets the literal 1000, then runs the function body
#   reverse_number(25): gets the literal 25, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   reversed_num * 10: first get reversed_num, then get 10, then compute reversed_num * 10
#   num % 10: first get num, then get 10, then compute num % 10
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_number(number) — stored for later
#   2. Assign to lst
#   3. Run print(reverse_number(...))
#       calls reverse_number with: number=lst
#         3.1 Return from function
#   4. Define def reverse_number(number) — stored for later
#   5. Assign to number
#   6. Run print(reverse_number(...))
#       calls reverse_number with: number=number
#         6.1 Return from function
#   7. Define def reverse_number(number) — stored for later
#   8. Assign to lst
#   9. Run print(reverse_number(...))
#       calls reverse_number with: number=lst
#         9.1 Return from function
#   10. Define def reverse_number(number) — stored for later
#   11. Run print(reverse_number(...))
#       calls reverse_number with: number=123
#         11.1 Return from function
#   12. Run print(reverse_number(...))
#       calls reverse_number with: number=-456
#         12.1 Return from function
#   13. Run print(reverse_number(...))
#       calls reverse_number with: number=1000
#         13.1 Return from function
#   14. Define def reverse_number(number) — stored for later
#   15. Define def reverse_number(number) — stored for later
#   16. Assign to number
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(reverse_number(...))
#       calls reverse_number with: number=25
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_number, reverse_number, reverse_number, reverse_number, reverse_number, reverse_number are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(reverse_number(lst)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      print(reverse_number(number)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(reverse_number(lst)) → function runs first, then print shows the result
#      print(reverse_number(123)) → function runs first, then print shows the result
#      print(reverse_number(?)) → function runs first, then print shows the result
#      print(reverse_number(1000)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_number(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 156:
#
# ===== SYNTAX =====
#   def remove_duplicates(string)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   comprehension
#   operators: or in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates(lst)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates('hello')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates('hello')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   print(remove_duplicates('aabbcc')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(string): looks up variable 'string', then runs the function body
#   print(remove_duplicates('world')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates('hello'): gets the literal 'hello', then runs the function body
#   remove_duplicates('hello'): gets the literal 'hello', then runs the function body
#   remove_duplicates('aabbcc'): gets the literal 'aabbcc', then runs the function body
#   remove_duplicates('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(string) — stored for later
#   2. Assign to lst
#   3. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string=lst
#         3.1 Return from function
#   4. Define def remove_duplicates(string) — stored for later
#   5. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string='hello'
#         5.1 Return from function
#   6. Define def remove_duplicates(string) — stored for later
#   7. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string='hello'
#         7.1 Return from function
#   8. Define def remove_duplicates(string) — stored for later
#   9. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string='aabbcc'
#         9.1 Return from function
#   10. Define def remove_duplicates(string) — stored for later
#   11. Define def remove_duplicates(string) — stored for later
#   12. Assign to string
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(remove_duplicates(...))
#       calls remove_duplicates with: string='world'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_duplicates(lst)) → function runs first, then print shows the result
#      print(remove_duplicates('hello')) → function runs first, then print shows the result
#      print(remove_duplicates('hello')) → function runs first, then print shows the result
#      print(remove_duplicates('aabbcc')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates('world')) → function runs first, then print shows the result

"""

"""
Problem 158:
#
# ===== SYNTAX =====
#   def largest_element(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   max() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(largest_element(numbers)): the inner largest_element() executes FIRST, then print() outputs the result
#   main(): — no arguments needed
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_element(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(largest_element(...))
#       calls largest_element with: numbers=numbers
#         3.1 Return from function
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Assign to result
#         5.2 Assign to user
#         5.3 For loop over user
#         5.4 Return from function
#   6. Define def largest_element(numbers) — stored for later
#   7. Run print(largest_element(...))
#       calls largest_element with: numbers=[2, 56, 75, 4566, 3]
#         7.1 Return from function
#   8. Define def largest_element(numbers) — stored for later
#   9. Run print(largest_element(...))
#       calls largest_element with: numbers=[10, 5, 20, 3]
#         9.1 Return from function
#   10. Define def largest_element(numbers) — stored for later
#   11. Define def largest_element(numbers) — stored for later
#   12. Assign to numbers
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(largest_element(...))
#       calls largest_element with: numbers=[4, 1, 8, 6, 3]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_element, main, largest_element, largest_element, largest_element, largest_element are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(largest_element(numbers)) → function runs first, then print shows the result
#      main() → jumps into function, runs body, returns result → printed
#      print(largest_element(?)) → function runs first, then print shows the result
#      print(largest_element(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_element(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round

"""

"""
Problem 159:
#
# ===== SYNTAX =====
#   def largest_element(numbers)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sorted() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(largest_element(numbers)): the inner largest_element() executes FIRST, then print() outputs the result
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(numbers): looks up variable 'numbers', then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   item > result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_element(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(largest_element(...))
#       calls largest_element with: numbers=numbers
#         3.1 Assign to result
#         3.2 For loop over numbers
#         3.3 Return from function
#   4. Define def largest_element(numbers) — stored for later
#   5. Run print(largest_element(...))
#       calls largest_element with: numbers=[2, 56, 75, 4566, 3]
#         5.1 Assign to result
#         5.2 For loop over numbers
#         5.3 Return from function
#   6. Define def largest_element(numbers) — stored for later
#   7. Run print(largest_element(...))
#       calls largest_element with: numbers=[10, 5, 20, 3]
#         7.1 Assign to result
#         7.2 For loop over numbers
#         7.3 Return from function
#   8. Define def largest_element(numbers) — stored for later
#   9. Define def largest_element(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(largest_element(...))
#       calls largest_element with: numbers=[4, 1, 8, 6, 3]
#         13.1 Assign to result
#         13.2 For loop over numbers
#         13.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_element, largest_element, largest_element, largest_element, largest_element are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(largest_element(numbers)) → function runs first, then print shows the result
#      print(largest_element(?)) → function runs first, then print shows the result
#      print(largest_element(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_element(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 160:
#
# ===== SYNTAX =====
#   def find_length(string)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   find_length() — built-in function call
#   enumerate() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   print(find_length(arg)): the inner find_length() executes FIRST, then print() outputs the result
#   print(find_length('Jonathan')): the inner find_length() executes FIRST, then print() outputs the result
#   print(find_length('hello')): the inner find_length() executes FIRST, then print() outputs the result
#   print(find_length('python')): the inner find_length() executes FIRST, then print() outputs the result
#   find_length(string): looks up variable 'string', then runs the function body
#   print(find_length('world')): the inner find_length() executes FIRST, then print() outputs the result
#   find_length(arg): looks up variable 'arg', then runs the function body
#   find_length('Jonathan'): gets the literal 'Jonathan', then runs the function body
#   1 + find_length(...): first get 1, then get find_length(...), then compute 1 + find_length(...)
#   find_length('hello'): gets the literal 'hello', then runs the function body
#   find_length('python'): gets the literal 'python', then runs the function body
#   find_length('world'): gets the literal 'world', then runs the function body
#   find_length(?): resolves the value, then runs the function body
#   i + 1: first get i, then get 1, then compute i + 1
#   string == '': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_length(string) — stored for later
#   2. Assign to arg
#   3. Run print(find_length(...))
#       calls find_length with: string=arg
#         3.1 Return from function
#   4. Define def find_length(string) — stored for later
#   5. Run print(find_length(...))
#       calls find_length with: string='Jonathan'
#         5.1 Return from function
#   6. Define def find_length(string) — stored for later
#   7. Run print(find_length(...))
#       calls find_length with: string='hello'
#         7.1 Return from function
#   8. Define def find_length(string) — stored for later
#   9. Run print(find_length(...))
#       calls find_length with: string='python'
#         9.1 Return from function
#   10. Define def find_length(string) — stored for later
#   11. Define def find_length(string) — stored for later
#   12. Assign to string
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(find_length(...))
#       calls find_length with: string='world'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_length, find_length, find_length, find_length, find_length, find_length are just stored
#   Execution starts at the non-def lines below:
#      arg = ... (stored in memory)
#      print(find_length(arg)) → function runs first, then print shows the result
#      print(find_length('Jonathan')) → function runs first, then print shows the result
#      print(find_length('hello')) → function runs first, then print shows the result
#      print(find_length('python')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_length('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 161:
#
# ===== SYNTAX =====
#   def intersection()
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   set() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   intersection(): — no arguments needed
#   intersection(): — no arguments needed
#   print(intersection_two_list(lst1, lst2)): the inner intersection_two_list() executes FIRST, then print() outputs the result
#   print(intersection_two_list(lst1, lst2)): the inner intersection_two_list() executes FIRST, then print() outputs the result
#   print(intersection_two_list(?, ?)): the inner intersection_two_list() executes FIRST, then print() outputs the result
#   print(intersection_two_list(?, ?)): the inner intersection_two_list() executes FIRST, then print() outputs the result
#   intersection_two_list(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(intersection_two_list(?, ?)): the inner intersection_two_list() executes FIRST, then print() outputs the result
#   intersection_two_list(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   intersection_two_list(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   intersection_two_list(?, ?): resolves the value, resolves the value, then runs the function body
#   intersection_two_list(?, ?): resolves the value, resolves the value, then runs the function body
#   intersection_two_list(?, ?): resolves the value, resolves the value, then runs the function body
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   word in sort2: is this True or False? The answer picks which branch
#   word in sort2: is this True or False? The answer picks which branch
#   word in sort2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def intersection() — stored for later
#   2. Run intersection()
#         2.1 Assign to lst1
#         2.2 Assign to lst2
#         2.3 Assign to sort1
#         2.4 Assign to sort2
#         2.5 Assign to common
#         2.6 For loop over sort1
#         2.7 Assign to result
#         2.8 Return from function
#   3. Define def intersection() — stored for later
#   4. Run intersection()
#         4.1 Assign to lst1
#         4.2 Assign to lst2
#         4.3 Assign to sort1
#         4.4 Assign to sort2
#         4.5 Assign to common
#         4.6 For loop over sort1
#         4.7 Assign to result
#         4.8 Return from function
#   5. Define def intersection_two_list(lst1, lst2) — stored for later
#   6. Assign to lst1
#   7. Assign to lst2
#   8. Run print(intersection_two_list(...))
#       calls intersection_two_list with: lst1=lst1, lst2=lst2
#         8.1 Return from function
#   9. Define def intersection_two_list(lst1, lst2) — stored for later
#   10. Assign to lst1
#   11. Assign to lst2
#   12. Run print(intersection_two_list(...))
#       calls intersection_two_list with: lst1=lst1, lst2=lst2
#         12.1 Return from function
#   13. Define def intersection_two_list(lst1, lst2) — stored for later
#   14. Run print(intersection_two_list(...))
#       calls intersection_two_list with: lst1=[1, 2, 3, 4], lst2=[3, 4, 5, 6]
#         14.1 Return from function
#   15. Define def intersection_two_list(lst1, lst2) — stored for later
#   16. Run print(intersection_two_list(...))
#       calls intersection_two_list with: lst1=['a', 'b', 'c'], lst2=['b', 'c', 'd']
#         16.1 Return from function
#   17. Define def intersection() — stored for later
#   18. Define def intersection() — stored for later
#   19. Assign to lst1
#   20. Assign to lst2
#   21. Assign to result
#   22. Run print(result)
#   23. Run print(intersection_two_list(...))
#       calls intersection_two_list with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         23.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions intersection, intersection, intersection_two_list, intersection_two_list, intersection_two_list, intersection_two_list, intersection, intersection are just stored
#   Execution starts at the non-def lines below:
#      intersection() → jumps into function, runs body, returns result → printed
#      intersection() → jumps into function, runs body, returns result → printed
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(intersection_two_list(lst1, lst2)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(intersection_two_list(lst1, lst2)) → function runs first, then print shows the result
#      print(intersection_two_list(?, ?)) → function runs first, then print shows the result
#      print(intersection_two_list(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(intersection_two_list(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sort1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 162:
#
# ===== SYNTAX =====
#   def third_largest()
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   sorted() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   third_largest(): — no arguments needed
#   print(third_largest()): the inner third_largest() executes FIRST, then print() outputs the result
#   print(third_largest()): the inner third_largest() executes FIRST, then print() outputs the result
#   print(third_largest(?)): the inner third_largest() executes FIRST, then print() outputs the result
#   third_largest(numbers): looks up variable 'numbers', then runs the function body
#   print(third_largest(?)): the inner third_largest() executes FIRST, then print() outputs the result
#   third_largest(): — no arguments needed
#   third_largest(): — no arguments needed
#   third_largest(?): resolves the value, then runs the function body
#   third_largest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def third_largest() — stored for later
#   2. Run third_largest()
#         2.1 Assign to user_string
#         2.2 Assign to user_split
#         2.3 Assign to int_list
#         2.4 For loop over user_split
#         2.5 Run int_list.sort()
#         2.6 Assign to result
#         2.7 Assign to result
#         2.8 Return from function
#   3. Define def third_largest() — stored for later
#   4. Run print(third_largest(...))
#         4.1 Assign to user_string
#         4.2 Assign to user_split
#         4.3 Assign to int_list
#         4.4 For loop over user_split
#         4.5 Run int_list.sort()
#         4.6 Assign to result
#         4.7 Assign to result
#         4.8 Return from function
#   5. Define def third_largest() — stored for later
#   6. Run print(third_largest(...))
#         6.1 Assign to user_string
#         6.2 Assign to user_split
#         6.3 Assign to int_list
#         6.4 For loop over user_split
#         6.5 Run int_list.sort()
#         6.6 Assign to result
#         6.7 Assign to result
#         6.8 Return from function
#   7. Define def third_largest(numbers) — stored for later
#   8. Run print(third_largest(...))
#         8.1 Assign to user_string
#         8.2 Assign to user_split
#         8.3 Assign to int_list
#         8.4 For loop over user_split
#         8.5 Run int_list.sort()
#         8.6 Assign to result
#         8.7 Assign to result
#         8.8 Return from function
#   9. Define def third_largest() — stored for later
#   10. Define def third_largest() — stored for later
#   11. Assign to numbers
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(third_largest(...))
#         14.1 Assign to user_string
#         14.2 Assign to user_split
#         14.3 Assign to int_list
#         14.4 For loop over user_split
#         14.5 Run int_list.sort()
#         14.6 Assign to result
#         14.7 Assign to result
#         14.8 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions third_largest, third_largest, third_largest, third_largest, third_largest, third_largest are just stored
#   Execution starts at the non-def lines below:
#      third_largest() → jumps into function, runs body, returns result → printed
#      print(third_largest()) → function runs first, then print shows the result
#      print(third_largest()) → function runs first, then print shows the result
#      print(third_largest(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(third_largest(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user_split each round

"""

"""
Problem 163:
#
# ===== SYNTAX =====
#   def count_vowels(string)
#   return — exits the function and sends back a value
#   Counter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(count_vowels(string)): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels(string)): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Jonathan is the best')): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Jonathan is the best')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(string): looks up variable 'string', then runs the function body
#   print(count_vowels('world')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(string): looks up variable 'string', then runs the function body
#   count_vowels(string): looks up variable 'string', then runs the function body
#   count_vowels('Jonathan is the best'): gets the literal 'Jonathan is the best', then runs the function body
#   count_vowels('Jonathan is the best'): gets the literal 'Jonathan is the best', then runs the function body
#   count_vowels('world'): gets the literal 'world', then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(string) — stored for later
#   2. Assign to string
#   3. Run print(count_vowels(...))
#       calls count_vowels with: string=string
#         3.1 Return from function
#   4. Define def count_vowels(sentence) — stored for later
#   5. Assign to string
#   6. Run print(count_vowels(...))
#       calls count_vowels with: string=string
#         6.1 Return from function
#   7. Define def count_vowels(string) — stored for later
#   8. Run print(count_vowels(...))
#       calls count_vowels with: string='Jonathan is the best'
#         8.1 Return from function
#   9. Define def count_vowels(string) — stored for later
#   10. Run print(count_vowels(...))
#       calls count_vowels with: string='Jonathan is the best'
#         10.1 Return from function
#   11. Define def count_vowels(string) — stored for later
#   12. Define def count_vowels(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(count_vowels(...))
#       calls count_vowels with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels, count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_vowels(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      print(count_vowels(string)) → function runs first, then print shows the result
#      print(count_vowels('Jonathan is the best')) → function runs first, then print shows the result
#      print(count_vowels('Jonathan is the best')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from vowels each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 164:
#
# ===== SYNTAX =====
#   def count_vowels(sentence)
#   return — exits the function and sends back a value
#   Counter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(count_vowels(string)): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Jonathan is the best')): the inner count_vowels() executes FIRST, then print() outputs the result
#   print(count_vowels('Jonathan is the best')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(sentence): looks up variable 'sentence', then runs the function body
#   print(count_vowels('Python is fun')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(string): looks up variable 'string', then runs the function body
#   count_vowels('Jonathan is the best'): gets the literal 'Jonathan is the best', then runs the function body
#   count_vowels('Jonathan is the best'): gets the literal 'Jonathan is the best', then runs the function body
#   count_vowels('Python is fun'): gets the literal 'Python is fun', then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(sentence) — stored for later
#   2. Assign to string
#   3. Run print(count_vowels(...))
#       calls count_vowels with: sentence=string
#         3.1 Return from function
#   4. Define def count_vowels(sentence) — stored for later
#   5. Run print(count_vowels(...))
#       calls count_vowels with: sentence='Jonathan is the best'
#         5.1 Return from function
#   6. Define def count_vowels(sentence) — stored for later
#   7. Run print(count_vowels(...))
#       calls count_vowels with: sentence='Jonathan is the best'
#         7.1 Return from function
#   8. Define def count_vowels(sentence) — stored for later
#   9. Define def count_vowels(sentence) — stored for later
#   10. Assign to sentence
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(count_vowels(...))
#       calls count_vowels with: sentence='Python is fun'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(count_vowels(string)) → function runs first, then print shows the result
#      print(count_vowels('Jonathan is the best')) → function runs first, then print shows the result
#      print(count_vowels('Jonathan is the best')) → function runs first, then print shows the result
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from vowels each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 165:
#
# ===== SYNTAX =====
#   def intiger_string(number)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   str() — built-in function call
#   tuple() — built-in function call
#   map() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(intiger_string(number)): the inner intiger_string() executes FIRST, then print() outputs the result
#   print(split_number_int(number)): the inner split_number_int() executes FIRST, then print() outputs the result
#   print(split_number_int(456)): the inner split_number_int() executes FIRST, then print() outputs the result
#   print(split_number_int(789)): the inner split_number_int() executes FIRST, then print() outputs the result
#   intiger_string(number): looks up variable 'number', then runs the function body
#   print(intiger_string(25)): the inner intiger_string() executes FIRST, then print() outputs the result
#   intiger_string(number): looks up variable 'number', then runs the function body
#   split_number_int(number): looks up variable 'number', then runs the function body
#   split_number_int(456): gets the literal 456, then runs the function body
#   split_number_int(789): gets the literal 789, then runs the function body
#   intiger_string(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def intiger_string(number) — stored for later
#   2. Assign to number
#   3. Run print(intiger_string(...))
#       calls intiger_string with: number=number
#         3.1 Return from function
#   4. Define def split_number_int(number) — stored for later
#   5. Assign to number
#   6. Run print(split_number_int(...))
#       calls split_number_int with: number=number
#         6.1 Return from function
#   7. Define def split_number_int(number) — stored for later
#   8. Run print(split_number_int(...))
#       calls split_number_int with: number=456
#         8.1 Return from function
#   9. Define def split_number_int(number) — stored for later
#   10. Run print(split_number_int(...))
#       calls split_number_int with: number=789
#         10.1 Return from function
#   11. Define def intiger_string(number) — stored for later
#   12. Define def intiger_string(number) — stored for later
#   13. Assign to number
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(intiger_string(...))
#       calls intiger_string with: number=25
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions intiger_string, split_number_int, split_number_int, split_number_int, intiger_string, intiger_string are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(intiger_string(number)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      print(split_number_int(number)) → function runs first, then print shows the result
#      print(split_number_int(456)) → function runs first, then print shows the result
#      print(split_number_int(789)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(intiger_string(25)) → function runs first, then print shows the result

"""

"""
Problem 166:
#
# ===== SYNTAX =====
#   def double_elements(lst)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(double_elements(lst)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(lst): looks up variable 'lst', then runs the function body
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(lst): looks up variable 'lst', then runs the function body
#   lst * 2: first get lst, then get 2, then compute lst * 2
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def double_elements(lst) — stored for later
#   2. Assign to lst
#   3. Run print(double_elements(...))
#       calls double_elements with: lst=lst
#         3.1 Return from function
#   4. Define def double_elements(lst) — stored for later
#   5. Run print(double_elements(...))
#       calls double_elements with: lst=['tree', 'house', 'car']
#         5.1 Return from function
#   6. Define def double_elements(lst) — stored for later
#   7. Run print(double_elements(...))
#       calls double_elements with: lst=['a', 'b', 'c']
#         7.1 Return from function
#   8. Define def double_elements(lst) — stored for later
#   9. Run print(double_elements(...))
#       calls double_elements with: lst=['x', 'y', 'z']
#         9.1 Return from function
#   10. Define def double_elements(lst) — stored for later
#   11. Define def double_elements(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(double_elements(...))
#       calls double_elements with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions double_elements, double_elements, double_elements, double_elements, double_elements, double_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(double_elements(lst)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(double_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 167:
#
# ===== SYNTAX =====
#   def main()
#   return — exits the function and sends back a value
#   list() — built-in function call
#   min() — built-in function call
#   max() — built-in function call
#   sorted() — built-in function call
#   input() — built-in function call
#   map() — built-in function call
#   reduce() — built-in function call
#   lambda
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#   print(l_s(numbers)): the inner l_s() executes FIRST, then print() outputs the result
#   print(l_s(?)): the inner l_s() executes FIRST, then print() outputs the result
#   print(l_s(?)): the inner l_s() executes FIRST, then print() outputs the result
#   print(l_s(?)): the inner l_s() executes FIRST, then print() outputs the result
#   l_s(numbers): looks up variable 'numbers', then runs the function body
#   print(l_s(?)): the inner l_s() executes FIRST, then print() outputs the result
#   l_s(numbers): looks up variable 'numbers', then runs the function body
#   l_s(?): resolves the value, then runs the function body
#   l_s(?): resolves the value, then runs the function body
#   l_s(?): resolves the value, then runs the function body
#   l_s(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main() — stored for later
#   2. Run main()
#         2.1 Return from function
#   3. Define def l_s(numbers) — stored for later
#   4. Assign to numbers
#   5. Run print(l_s(...))
#       calls l_s with: numbers=numbers
#         5.1 Return from function
#   6. Define def l_s(numbers) — stored for later
#   7. Run print(l_s(...))
#       calls l_s with: numbers=[10, 3, 7, 1, 9]
#         7.1 Return from function
#   8. Define def l_s(numbers) — stored for later
#   9. Run print(l_s(...))
#       calls l_s with: numbers=[4, 5, 100, 6, 1, 10]
#         9.1 Return from function
#   10. Define def l_s(numbers) — stored for later
#   11. Run print(l_s(...))
#       calls l_s with: numbers=[5, 2, 9, 1, 7]
#         11.1 Return from function
#   12. Define def main() — stored for later
#   13. Define def main() — stored for later
#   14. Assign to numbers
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(l_s(...))
#       calls l_s with: numbers=[4, 1, 8, 6, 3]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, l_s, l_s, l_s, l_s, main, main are just stored
#   Execution starts at the non-def lines below:
#      main() → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      print(l_s(numbers)) → function runs first, then print shows the result
#      print(l_s(?)) → function runs first, then print shows the result
#      print(l_s(?)) → function runs first, then print shows the result
#      print(l_s(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(l_s(?)) → function runs first, then print shows the result

"""

"""
Problem 168:
#
# ===== SYNTAX =====
#   def count_words()
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   count_words(): — no arguments needed
#   print(count_words(input_string)): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('This is a test. This test is only a test.')): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('one two one two three')): the inner count_words() executes FIRST, then print() outputs the result
#   print(count_words('hello hello world')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(string): looks up variable 'string', then runs the function body
#   print(count_words('world')): the inner count_words() executes FIRST, then print() outputs the result
#   count_words(input_string): looks up variable 'input_string', then runs the function body
#   count_words('This is a test. This test is only a test.'): gets the literal 'This is a test. This test is only a test.', then runs the function body
#   count_words('one two one two three'): gets the literal 'one two one two three', then runs the function body
#   count_words('hello hello world'): gets the literal 'hello hello world', then runs the function body
#   count_words('world'): gets the literal 'world', then runs the function body
#   count.get(...) + 1: first get count.get(...), then get 1, then compute count.get(...) + 1
#   word in result: is this True or False? The answer picks which branch
#   word in word_count: is this True or False? The answer picks which branch
#   word in result: is this True or False? The answer picks which branch
#   word in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_words() — stored for later
#   2. Run count_words()
#         2.1 Assign to user
#         2.2 Assign to sort
#         2.3 Assign to result
#         2.4 For loop over sort
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def count_words(string) — stored for later
#   4. Assign to input_string
#   5. Run print(count_words(...))
#         5.1 Assign to user
#         5.2 Assign to sort
#         5.3 Assign to result
#         5.4 For loop over sort
#         5.5 Assign to result
#         5.6 Return from function
#   6. Define def count_words(string) — stored for later
#   7. Run print(count_words(...))
#         7.1 Assign to user
#         7.2 Assign to sort
#         7.3 Assign to result
#         7.4 For loop over sort
#         7.5 Assign to result
#         7.6 Return from function
#   8. Define def count_words(string) — stored for later
#   9. Run print(count_words(...))
#         9.1 Assign to user
#         9.2 Assign to sort
#         9.3 Assign to result
#         9.4 For loop over sort
#         9.5 Assign to result
#         9.6 Return from function
#   10. Define def count_words(string) — stored for later
#   11. Run print(count_words(...))
#         11.1 Assign to user
#         11.2 Assign to sort
#         11.3 Assign to result
#         11.4 For loop over sort
#         11.5 Assign to result
#         11.6 Return from function
#   12. Define def count_words() — stored for later
#   13. Define def count_words() — stored for later
#   14. Assign to string
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(count_words(...))
#         17.1 Assign to user
#         17.2 Assign to sort
#         17.3 Assign to result
#         17.4 For loop over sort
#         17.5 Assign to result
#         17.6 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_words, count_words, count_words, count_words, count_words, count_words, count_words are just stored
#   Execution starts at the non-def lines below:
#      count_words() → jumps into function, runs body, returns result → printed
#      input_string = ... (stored in memory)
#      print(count_words(input_string)) → function runs first, then print shows the result
#      print(count_words('This is a test. This test is only a test.')) → function runs first, then print shows the result
#      print(count_words('one two one two three')) → function runs first, then print shows the result
#      print(count_words('hello hello world')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_words('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from sort each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 169:
#
# ===== SYNTAX =====
#   def merge_dictionaries(dict1, dict2)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   for
#   in
#   operators: or
#
# ===== EVALUATION ORDER =====
#   merge_dictionaries(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(merge_dictionaries(?, ?)): the inner merge_dictionaries() executes FIRST, then print() outputs the result
#   print(merge_dictionaries(?, ?)): the inner merge_dictionaries() executes FIRST, then print() outputs the result
#   print(merge_dictionaries(?, ?)): the inner merge_dictionaries() executes FIRST, then print() outputs the result
#   merge_dictionaries(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(merge_dictionaries(?, ?)): the inner merge_dictionaries() executes FIRST, then print() outputs the result
#   merge_dictionaries(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_dictionaries(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_dictionaries(?, ?): resolves the value, resolves the value, then runs the function body
#   merge_dictionaries(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_dictionaries(dict1, dict2) — stored for later
#   2. Assign to dict1
#   3. Assign to dict2
#   4. Assign to result
#   5. Run print(result)
#   6. Define def merge_dictionaries(dict1, dict2) — stored for later
#   7. Run print(merge_dictionaries(...))
#       calls merge_dictionaries with: dict1={...}, dict2={...}
#         7.1 Return from function
#   8. Define def merge_dictionaries(dict1, dict2) — stored for later
#   9. Run print(merge_dictionaries(...))
#       calls merge_dictionaries with: dict1={...}, dict2={...}
#         9.1 Return from function
#   10. Define def merge_dictionaries(dict1, dict2) — stored for later
#   11. Run print(merge_dictionaries(...))
#       calls merge_dictionaries with: dict1={...}, dict2={...}
#         11.1 Return from function
#   12. Define def merge_dictionaries(dict1, dict2) — stored for later
#   13. Define def merge_dictionaries(dict1, dict2) — stored for later
#   14. Assign to dict1
#   15. Assign to dict2
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(merge_dictionaries(...))
#       calls merge_dictionaries with: dict1={...}, dict2={...}
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_dictionaries, merge_dictionaries, merge_dictionaries, merge_dictionaries, merge_dictionaries, merge_dictionaries are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_dictionaries(?, ?)) → function runs first, then print shows the result
#      print(merge_dictionaries(?, ?)) → function runs first, then print shows the result
#      print(merge_dictionaries(?, ?)) → function runs first, then print shows the result
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_dictionaries(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dict2.items(...) each round

"""

"""
Problem 170:
#
# ===== SYNTAX =====
#   def count_characters(string)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   count_characters(string): looks up variable 'string', then runs the function body
#   print(count_characters('hello')): the inner count_characters() executes FIRST, then print() outputs the result
#   print(count_characters('aabbcc')): the inner count_characters() executes FIRST, then print() outputs the result
#   print(count_characters('abcabc')): the inner count_characters() executes FIRST, then print() outputs the result
#   count_characters(string): looks up variable 'string', then runs the function body
#   print(count_characters('world')): the inner count_characters() executes FIRST, then print() outputs the result
#   count_characters('hello'): gets the literal 'hello', then runs the function body
#   count_characters('aabbcc'): gets the literal 'aabbcc', then runs the function body
#   count_characters('abcabc'): gets the literal 'abcabc', then runs the function body
#   count_characters('world'): gets the literal 'world', then runs the function body
#   counts.get(...) + 1: first get counts.get(...), then get 1, then compute counts.get(...) + 1
#   char in counts: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_characters(string) — stored for later
#   2. Assign to string
#   3. Assign to result
#   4. Run print(result)
#   5. Define def count_characters(string) — stored for later
#   6. Run print(count_characters(...))
#       calls count_characters with: string='hello'
#         6.1 Return from function
#   7. Define def count_characters(string) — stored for later
#   8. Run print(count_characters(...))
#       calls count_characters with: string='aabbcc'
#         8.1 Return from function
#   9. Define def count_characters(string) — stored for later
#   10. Run print(count_characters(...))
#       calls count_characters with: string='abcabc'
#         10.1 Return from function
#   11. Define def count_characters(string) — stored for later
#   12. Define def count_characters(string) — stored for later
#   13. Assign to string
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(count_characters(...))
#       calls count_characters with: string='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_characters, count_characters, count_characters, count_characters, count_characters, count_characters are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_characters('hello')) → function runs first, then print shows the result
#      print(count_characters('aabbcc')) → function runs first, then print shows the result
#      print(count_characters('abcabc')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_characters('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 171:
#
# ===== SYNTAX =====
#   def invert_dictionary(input_dict)
#   return — exits the function and sends back a value
#   defaultdict() — built-in function call
#   dict() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   invert_dictionary(input_dict): looks up variable 'input_dict', then runs the function body
#   print(invert_dictionary(?)): the inner invert_dictionary() executes FIRST, then print() outputs the result
#   print(invert_dictionary(?)): the inner invert_dictionary() executes FIRST, then print() outputs the result
#   print(invert_dictionary(?)): the inner invert_dictionary() executes FIRST, then print() outputs the result
#   invert_dictionary(input_dict): looks up variable 'input_dict', then runs the function body
#   print(invert_dictionary(?)): the inner invert_dictionary() executes FIRST, then print() outputs the result
#   invert_dictionary(?): resolves the value, then runs the function body
#   invert_dictionary(?): resolves the value, then runs the function body
#   invert_dictionary(?): resolves the value, then runs the function body
#   invert_dictionary(?): resolves the value, then runs the function body
#   value not in inverted: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def invert_dictionary(input_dict) — stored for later
#   2. Assign to input_dict
#   3. Assign to result
#   4. Run print(result)
#   5. Define def invert_dictionary(input_dict) — stored for later
#   6. Run print(invert_dictionary(...))
#       calls invert_dictionary with: input_dict={...}
#         6.1 Return from function
#   7. Define def invert_dictionary(input_dict) — stored for later
#   8. Run print(invert_dictionary(...))
#       calls invert_dictionary with: input_dict={...}
#         8.1 Return from function
#   9. Define def invert_dictionary(input_dict) — stored for later
#   10. Run print(invert_dictionary(...))
#       calls invert_dictionary with: input_dict={...}
#         10.1 Return from function
#   11. Define def invert_dictionary(input_dict) — stored for later
#   12. Define def invert_dictionary(input_dict) — stored for later
#   13. Assign to input_dict
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(invert_dictionary(...))
#       calls invert_dictionary with: input_dict={...}
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions invert_dictionary, invert_dictionary, invert_dictionary, invert_dictionary, invert_dictionary, invert_dictionary are just stored
#   Execution starts at the non-def lines below:
#      input_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(invert_dictionary(?)) → function runs first, then print shows the result
#      print(invert_dictionary(?)) → function runs first, then print shows the result
#      print(invert_dictionary(?)) → function runs first, then print shows the result
#      input_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(invert_dictionary(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from input_dict.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 172:
#
# ===== SYNTAX =====
#   def filter_by_value(input_dict, threshold)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   filter_by_value(input_dict, threshold): looks up variable 'input_dict', looks up variable 'threshold', then runs the function body
#   print(filter_by_value(?, 3)): the inner filter_by_value() executes FIRST, then print() outputs the result
#   print(filter_by_value(?, 5)): the inner filter_by_value() executes FIRST, then print() outputs the result
#   print(filter_by_value(?, 4)): the inner filter_by_value() executes FIRST, then print() outputs the result
#   filter_by_value(input_dict, threshold): looks up variable 'input_dict', looks up variable 'threshold', then runs the function body
#   print(filter_by_value(?, 25)): the inner filter_by_value() executes FIRST, then print() outputs the result
#   filter_by_value(?, 3): resolves the value, gets the literal 3, then runs the function body
#   filter_by_value(?, 5): resolves the value, gets the literal 5, then runs the function body
#   filter_by_value(?, 4): resolves the value, gets the literal 4, then runs the function body
#   filter_by_value(?, 25): resolves the value, gets the literal 25, then runs the function body
#   value > threshold: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_by_value(input_dict, threshold) — stored for later
#   2. Assign to input_dict
#   3. Assign to threshold
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_by_value(input_dict, threshold) — stored for later
#   7. Run print(filter_by_value(...))
#       calls filter_by_value with: input_dict={...}, threshold=3
#         7.1 Return from function
#   8. Define def filter_by_value(input_dict, threshold) — stored for later
#   9. Run print(filter_by_value(...))
#       calls filter_by_value with: input_dict={...}, threshold=5
#         9.1 Return from function
#   10. Define def filter_by_value(input_dict, threshold) — stored for later
#   11. Run print(filter_by_value(...))
#       calls filter_by_value with: input_dict={...}, threshold=4
#         11.1 Return from function
#   12. Define def filter_by_value(input_dict, threshold) — stored for later
#   13. Define def filter_by_value(input_dict, threshold) — stored for later
#   14. Assign to input_dict
#   15. Assign to threshold
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(filter_by_value(...))
#       calls filter_by_value with: input_dict={...}, threshold=25
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_by_value, filter_by_value, filter_by_value, filter_by_value, filter_by_value, filter_by_value are just stored
#   Execution starts at the non-def lines below:
#      input_dict = ... (stored in memory)
#      threshold = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_by_value(?, 3)) → function runs first, then print shows the result
#      print(filter_by_value(?, 5)) → function runs first, then print shows the result
#      print(filter_by_value(?, 4)) → function runs first, then print shows the result
#      input_dict = ... (stored in memory)
#      threshold = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_by_value(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from input_dict.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 173:
#
# ===== SYNTAX =====
#   def combine_lists_to_dict(keys, values)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   len() — built-in function call
#   dict() — built-in function call
#   zip() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   combine_lists_to_dict(keys, values): looks up variable 'keys', looks up variable 'values', then runs the function body
#   print(combine_lists_to_dict(?, ?)): the inner combine_lists_to_dict() executes FIRST, then print() outputs the result
#   print(combine_lists_to_dict(?, ?)): the inner combine_lists_to_dict() executes FIRST, then print() outputs the result
#   print(combine_lists_to_dict(?, ?)): the inner combine_lists_to_dict() executes FIRST, then print() outputs the result
#   combine_lists_to_dict(keys, values): looks up variable 'keys', looks up variable 'values', then runs the function body
#   print(combine_lists_to_dict(?, ?)): the inner combine_lists_to_dict() executes FIRST, then print() outputs the result
#   combine_lists_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_lists_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_lists_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_lists_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def combine_lists_to_dict(keys, values) — stored for later
#   2. Assign to keys
#   3. Assign to values
#   4. Assign to result
#   5. Run print(result)
#   6. Define def combine_lists_to_dict(keys, values) — stored for later
#   7. Run print(combine_lists_to_dict(...))
#       calls combine_lists_to_dict with: keys=['a', 'b', 'c'], values=[1, 2, 3]
#         7.1 Return from function
#   8. Define def combine_lists_to_dict(keys, values) — stored for later
#   9. Run print(combine_lists_to_dict(...))
#       calls combine_lists_to_dict with: keys=['x', 'y'], values=[10, 20]
#         9.1 Return from function
#   10. Define def combine_lists_to_dict(keys, values) — stored for later
#   11. Run print(combine_lists_to_dict(...))
#       calls combine_lists_to_dict with: keys=['one', 'two'], values=[1, 2]
#         11.1 Return from function
#   12. Define def combine_lists_to_dict(keys, values) — stored for later
#   13. Define def combine_lists_to_dict(keys, values) — stored for later
#   14. Assign to keys
#   15. Assign to values
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(combine_lists_to_dict(...))
#       calls combine_lists_to_dict with: keys=[4, 1, 8, 6, 3], values=[4, 1, 8, 6, 3]
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions combine_lists_to_dict, combine_lists_to_dict, combine_lists_to_dict, combine_lists_to_dict, combine_lists_to_dict, combine_lists_to_dict are just stored
#   Execution starts at the non-def lines below:
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(combine_lists_to_dict(?, ?)) → function runs first, then print shows the result
#      print(combine_lists_to_dict(?, ?)) → function runs first, then print shows the result
#      print(combine_lists_to_dict(?, ?)) → function runs first, then print shows the result
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(combine_lists_to_dict(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 174:
#
# ===== SYNTAX =====
#   def max_value_key(input_dict)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sorted() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: == >=
#
# ===== EVALUATION ORDER =====
#   print(max_value_key(my_dict)): the inner max_value_key() executes FIRST, then print() outputs the result
#   print(max_value_key(empty_dict)): the inner max_value_key() executes FIRST, then print() outputs the result
#   print(max_value_key(?)): the inner max_value_key() executes FIRST, then print() outputs the result
#   print(max_value_key(?)): the inner max_value_key() executes FIRST, then print() outputs the result
#   print(max_value_key(?)): the inner max_value_key() executes FIRST, then print() outputs the result
#   print(max_value_key(?)): the inner max_value_key() executes FIRST, then print() outputs the result
#   max_value_key(input_dict): looks up variable 'input_dict', then runs the function body
#   print(max_value_key(?)): the inner max_value_key() executes FIRST, then print() outputs the result
#   max_value_key(my_dict): looks up variable 'my_dict', then runs the function body
#   max_value_key(empty_dict): looks up variable 'empty_dict', then runs the function body
#   max_value_key(?): resolves the value, then runs the function body
#   max_value_key(?): resolves the value, then runs the function body
#   max_value_key(?): resolves the value, then runs the function body
#   max_value_key(?): resolves the value, then runs the function body
#   max_value_key(?): resolves the value, then runs the function body
#   value == max_value: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_value_key(input_dict) — stored for later
#   2. Assign to my_dict
#   3. Run print(max_value_key(...))
#       calls max_value_key with: input_dict=my_dict
#         3.1 Return from function
#   4. Assign to empty_dict
#   5. Run print(max_value_key(...))
#       calls max_value_key with: input_dict=empty_dict
#         5.1 Return from function
#   6. Define def max_value_key(input_dict) — stored for later
#   7. Run print(max_value_key(...))
#       calls max_value_key with: input_dict={...}
#         7.1 Return from function
#   8. Run print(max_value_key(...))
#       calls max_value_key with: input_dict={...}
#         8.1 Return from function
#   9. Define def max_value_key(input_dict) — stored for later
#   10. Run print(max_value_key(...))
#       calls max_value_key with: input_dict={...}
#         10.1 Return from function
#   11. Define def max_value_key(input_dict) — stored for later
#   12. Run print(max_value_key(...))
#       calls max_value_key with: input_dict={...}
#         12.1 Return from function
#   13. Define def max_value_key(input_dict) — stored for later
#   14. Define def max_value_key(input_dict) — stored for later
#   15. Assign to input_dict
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(max_value_key(...))
#       calls max_value_key with: input_dict=[4, 1, 8, 6, 3]
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_value_key, max_value_key, max_value_key, max_value_key, max_value_key, max_value_key are just stored
#   Execution starts at the non-def lines below:
#      my_dict = ... (stored in memory)
#      print(max_value_key(my_dict)) → function runs first, then print shows the result
#      empty_dict = ... (stored in memory)
#      print(max_value_key(empty_dict)) → function runs first, then print shows the result
#      print(max_value_key(?)) → function runs first, then print shows the result
#      print(max_value_key(?)) → function runs first, then print shows the result
#      print(max_value_key(?)) → function runs first, then print shows the result
#      print(max_value_key(?)) → function runs first, then print shows the result
#      input_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_value_key(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from input_dict.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 175:
#
# ===== SYNTAX =====
#   def dict_equal(dict1, dict2)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   len() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == != or
#
# ===== EVALUATION ORDER =====
#   dict_equal(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   dict_equal(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(dict_equal(?, ?)): the inner dict_equal() executes FIRST, then print() outputs the result
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   dict_equal(?, ?): resolves the value, resolves the value, then runs the function body
#   set(...) ^ set(...): first get set(...), then get set(...), then compute set(...) ^ set(...)
#   dict1.keys(...) != dict2.keys(...): is this True or False? The answer picks which branch
#   len(...) != len(...): is this True or False? The answer picks which branch
#   dict1 == dict2: is this True or False? The answer picks which branch
#   key not in dict2: is this True or False? The answer picks which branch
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def dict_equal(dict1, dict2) — stored for later
#   2. Assign to dict1
#   3. Assign to dict2
#   4. Assign to result
#   5. Run print(result)
#   6. Define def dict_equal(dict1, dict2) — stored for later
#   7. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         7.1 Return from function
#   8. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         8.1 Return from function
#   9. Define def dict_equal(dict1, dict2) — stored for later
#   10. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         10.1 Return from function
#   11. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         11.1 Return from function
#   12. Define def dict_equal(dict1, dict2) — stored for later
#   13. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         13.1 Return from function
#   14. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         14.1 Return from function
#   15. Define def dict_equal(dict1, dict2) — stored for later
#   16. Define def dict_equal(dict1, dict2) — stored for later
#   17. Assign to dict1
#   18. Assign to dict2
#   19. Assign to result
#   20. Run print(result)
#   21. Run print(dict_equal(...))
#       calls dict_equal with: dict1={...}, dict2={...}
#         21.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions dict_equal, dict_equal, dict_equal, dict_equal, dict_equal, dict_equal are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(dict_equal(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dict1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 176:
#
# ===== SYNTAX =====
#   def word_frequency(string)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   print(word_frequency(string)): the inner word_frequency() executes FIRST, then print() outputs the result
#   print(word_frequency('gore why are you here you are gore')): the inner word_frequency() executes FIRST, then print() outputs the result
#   print(word_frequency('one two one two three')): the inner word_frequency() executes FIRST, then print() outputs the result
#   word_frequency(string): looks up variable 'string', then runs the function body
#   print(word_frequency('world')): the inner word_frequency() executes FIRST, then print() outputs the result
#   word_frequency(string): looks up variable 'string', then runs the function body
#   word_frequency('gore why are you here you are gore'): gets the literal 'gore why are you here you are gore', then runs the function body
#   word_frequency('one two one two three'): gets the literal 'one two one two three', then runs the function body
#   word_frequency('world'): gets the literal 'world', then runs the function body
#   freq.get(...) + 1: first get freq.get(...), then get 1, then compute freq.get(...) + 1
#   word in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def word_frequency(string) — stored for later
#   2. Assign to string
#   3. Run print(word_frequency(...))
#       calls word_frequency with: string=string
#         3.1 Return from function
#   4. Define def word_frequency(string) — stored for later
#   5. Run print(word_frequency(...))
#       calls word_frequency with: string='gore why are you here you are gore'
#         5.1 Return from function
#   6. Define def word_frequency(string) — stored for later
#   7. Run print(word_frequency(...))
#       calls word_frequency with: string='one two one two three'
#         7.1 Return from function
#   8. Define def word_frequency(string) — stored for later
#   9. Define def word_frequency(string) — stored for later
#   10. Assign to string
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(word_frequency(...))
#       calls word_frequency with: string='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions word_frequency, word_frequency, word_frequency, word_frequency, word_frequency are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(word_frequency(string)) → function runs first, then print shows the result
#      print(word_frequency('gore why are you here you are gore')) → function runs first, then print shows the result
#      print(word_frequency('one two one two three')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(word_frequency('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 177:
#
# ===== SYNTAX =====
#   def merge_list_of_dicts(dicts)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   merge_list_of_dicts(dicts): looks up variable 'dicts', then runs the function body
#   print(merge_list_of_dicts(?)): the inner merge_list_of_dicts() executes FIRST, then print() outputs the result
#   print(merge_list_of_dicts(?)): the inner merge_list_of_dicts() executes FIRST, then print() outputs the result
#   merge_list_of_dicts(dicts): looks up variable 'dicts', then runs the function body
#   print(merge_list_of_dicts(?)): the inner merge_list_of_dicts() executes FIRST, then print() outputs the result
#   merge_list_of_dicts(?): resolves the value, then runs the function body
#   merge_list_of_dicts(?): resolves the value, then runs the function body
#   merge_list_of_dicts(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_list_of_dicts(dicts) — stored for later
#   2. Assign to dicts
#   3. Assign to result
#   4. Run print(result)
#   5. Define def merge_list_of_dicts(dicts) — stored for later
#   6. Run print(merge_list_of_dicts(...))
#       calls merge_list_of_dicts with: dicts=[{...}, {...}, {...}]
#         6.1 Return from function
#   7. Define def merge_list_of_dicts(dicts) — stored for later
#   8. Run print(merge_list_of_dicts(...))
#       calls merge_list_of_dicts with: dicts=[{...}, {...}, {...}]
#         8.1 Return from function
#   9. Define def merge_list_of_dicts(dicts) — stored for later
#   10. Define def merge_list_of_dicts(dicts) — stored for later
#   11. Assign to dicts
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(merge_list_of_dicts(...))
#       calls merge_list_of_dicts with: dicts=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_list_of_dicts, merge_list_of_dicts, merge_list_of_dicts, merge_list_of_dicts, merge_list_of_dicts are just stored
#   Execution starts at the non-def lines below:
#      dicts = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_list_of_dicts(?)) → function runs first, then print shows the result
#      print(merge_list_of_dicts(?)) → function runs first, then print shows the result
#      dicts = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_list_of_dicts(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dicts each round

"""

"""
Problem 178:
#
# ===== SYNTAX =====
#   def remove_duplicates_from_values(input_dict)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   remove_duplicates_from_values(input_dict): looks up variable 'input_dict', then runs the function body
#   print(remove_duplicates_from_values(?)): the inner remove_duplicates_from_values() executes FIRST, then print() outputs the result
#   print(remove_duplicates_from_values(?)): the inner remove_duplicates_from_values() executes FIRST, then print() outputs the result
#   remove_duplicates_from_values(input_dict): looks up variable 'input_dict', then runs the function body
#   print(remove_duplicates_from_values(?)): the inner remove_duplicates_from_values() executes FIRST, then print() outputs the result
#   remove_duplicates_from_values(?): resolves the value, then runs the function body
#   remove_duplicates_from_values(?): resolves the value, then runs the function body
#   remove_duplicates_from_values(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates_from_values(input_dict) — stored for later
#   2. Assign to input_dict
#   3. Assign to result
#   4. Run print(result)
#   5. Define def remove_duplicates_from_values(input_dict) — stored for later
#   6. Run print(remove_duplicates_from_values(...))
#       calls remove_duplicates_from_values with: input_dict={...}
#         6.1 Return from function
#   7. Define def remove_duplicates_from_values(input_dict) — stored for later
#   8. Run print(remove_duplicates_from_values(...))
#       calls remove_duplicates_from_values with: input_dict={...}
#         8.1 Return from function
#   9. Define def remove_duplicates_from_values(input_dict) — stored for later
#   10. Define def remove_duplicates_from_values(input_dict) — stored for later
#   11. Assign to input_dict
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(remove_duplicates_from_values(...))
#       calls remove_duplicates_from_values with: input_dict={...}
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates_from_values, remove_duplicates_from_values, remove_duplicates_from_values, remove_duplicates_from_values, remove_duplicates_from_values are just stored
#   Execution starts at the non-def lines below:
#      input_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates_from_values(?)) → function runs first, then print shows the result
#      print(remove_duplicates_from_values(?)) → function runs first, then print shows the result
#      input_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates_from_values(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from input_dict.items(...) each round

"""

"""
Problem 179:
#
# ===== SYNTAX =====
#   def common_keys_max_values(dict1, dict2)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   common_keys_max_values(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(common_keys_max_values(?, ?)): the inner common_keys_max_values() executes FIRST, then print() outputs the result
#   print(common_keys_max_values(?, ?)): the inner common_keys_max_values() executes FIRST, then print() outputs the result
#   common_keys_max_values(dict1, dict2): looks up variable 'dict1', looks up variable 'dict2', then runs the function body
#   print(common_keys_max_values(?, ?)): the inner common_keys_max_values() executes FIRST, then print() outputs the result
#   dict1.keys(...) & dict2.keys(...): first get dict1.keys(...), then get dict2.keys(...), then compute dict1.keys(...) & dict2.keys(...)
#   common_keys_max_values(?, ?): resolves the value, resolves the value, then runs the function body
#   set(...) & set(...): first get set(...), then get set(...), then compute set(...) & set(...)
#   common_keys_max_values(?, ?): resolves the value, resolves the value, then runs the function body
#   common_keys_max_values(?, ?): resolves the value, resolves the value, then runs the function body
#   dict1.keys(...) & dict2.keys(...): first get dict1.keys(...), then get dict2.keys(...), then compute dict1.keys(...) & dict2.keys(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def common_keys_max_values(dict1, dict2) — stored for later
#   2. Assign to dict1
#   3. Assign to dict2
#   4. Assign to result
#   5. Run print(result)
#   6. Define def common_keys_max_values(dict1, dict2) — stored for later
#   7. Run print(common_keys_max_values(...))
#       calls common_keys_max_values with: dict1={...}, dict2={...}
#         7.1 Return from function
#   8. Define def common_keys_max_values(dict1, dict2) — stored for later
#   9. Run print(common_keys_max_values(...))
#       calls common_keys_max_values with: dict1={...}, dict2={...}
#         9.1 Return from function
#   10. Define def common_keys_max_values(dict1, dict2) — stored for later
#   11. Define def common_keys_max_values(dict1, dict2) — stored for later
#   12. Assign to dict1
#   13. Assign to dict2
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(common_keys_max_values(...))
#       calls common_keys_max_values with: dict1=[4, 1, 8, 6, 3], dict2=[4, 1, 8, 6, 3]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common_keys_max_values, common_keys_max_values, common_keys_max_values, common_keys_max_values, common_keys_max_values are just stored
#   Execution starts at the non-def lines below:
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_keys_max_values(?, ?)) → function runs first, then print shows the result
#      print(common_keys_max_values(?, ?)) → function runs first, then print shows the result
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_keys_max_values(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from ? each round

"""

"""
Problem 180:
#
# ===== SYNTAX =====
#   def average_values(dicts)
#   return — exits the function and sends back a value
#   defaultdict() — built-in function call
#   set() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: / in
#
# ===== EVALUATION ORDER =====
#   average_values(dicts): looks up variable 'dicts', then runs the function body
#   print(average_values(?)): the inner average_values() executes FIRST, then print() outputs the result
#   print(average_values(?)): the inner average_values() executes FIRST, then print() outputs the result
#   average_values(dicts): looks up variable 'dicts', then runs the function body
#   print(average_values(?)): the inner average_values() executes FIRST, then print() outputs the result
#   average_values(?): resolves the value, then runs the function body
#   average_values(?): resolves the value, then runs the function body
#   average_values(?): resolves the value, then runs the function body
#   ? / ?: first get ?, then get ?, then compute ? / ?
#   sum(...) / sum(...): first get sum(...), then get sum(...), then compute sum(...) / sum(...)
#   key in averages: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def average_values(dicts) — stored for later
#   2. Assign to dicts
#   3. Assign to result
#   4. Run print(result)
#   5. Define def average_values(dicts) — stored for later
#   6. Run print(average_values(...))
#       calls average_values with: dicts=[{...}, {...}, {...}]
#         6.1 Return from function
#   7. Define def average_values(dicts) — stored for later
#   8. Run print(average_values(...))
#       calls average_values with: dicts=[{...}, {...}]
#         8.1 Return from function
#   9. Define def average_values(dicts) — stored for later
#   10. Define def average_values(dicts) — stored for later
#   11. Assign to dicts
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(average_values(...))
#       calls average_values with: dicts=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_values, average_values, average_values, average_values, average_values are just stored
#   Execution starts at the non-def lines below:
#      dicts = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_values(?)) → function runs first, then print shows the result
#      print(average_values(?)) → function runs first, then print shows the result
#      dicts = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_values(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dicts each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 181:
#
# ===== SYNTAX =====
#   def merge_dicts_with_lists(dicts)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   set() — built-in function call
#   defaultdict() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   merge_dicts_with_lists(dicts): looks up variable 'dicts', then runs the function body
#   print(merge_dicts_with_lists(?)): the inner merge_dicts_with_lists() executes FIRST, then print() outputs the result
#   print(merge_dicts_with_lists(?)): the inner merge_dicts_with_lists() executes FIRST, then print() outputs the result
#   merge_dicts_with_lists(dicts): looks up variable 'dicts', then runs the function body
#   print(merge_dicts_with_lists(?)): the inner merge_dicts_with_lists() executes FIRST, then print() outputs the result
#   merge_dicts_with_lists(?): resolves the value, then runs the function body
#   merge_dicts_with_lists(?): resolves the value, then runs the function body
#   merge_dicts_with_lists(?): resolves the value, then runs the function body
#   key in merged: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_dicts_with_lists(dicts) — stored for later
#   2. Assign to dicts
#   3. Assign to result
#   4. Run print(result)
#   5. Define def merge_dicts_with_lists(dicts) — stored for later
#   6. Run print(merge_dicts_with_lists(...))
#       calls merge_dicts_with_lists with: dicts=[{...}, {...}]
#         6.1 Return from function
#   7. Define def merge_dicts_with_lists(dicts) — stored for later
#   8. Run print(merge_dicts_with_lists(...))
#       calls merge_dicts_with_lists with: dicts=[{...}, {...}]
#         8.1 Return from function
#   9. Define def merge_dicts_with_lists(dicts) — stored for later
#   10. Define def merge_dicts_with_lists(dicts) — stored for later
#   11. Assign to dicts
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(merge_dicts_with_lists(...))
#       calls merge_dicts_with_lists with: dicts=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_dicts_with_lists, merge_dicts_with_lists, merge_dicts_with_lists, merge_dicts_with_lists, merge_dicts_with_lists are just stored
#   Execution starts at the non-def lines below:
#      dicts = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_dicts_with_lists(?)) → function runs first, then print shows the result
#      print(merge_dicts_with_lists(?)) → function runs first, then print shows the result
#      dicts = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(merge_dicts_with_lists(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dicts each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 182:
#
# ===== SYNTAX =====
#   def group_by_first_letter(lst)
#   return — exits the function and sends back a value
#   defaultdict() — built-in function call
#   dict() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(group_by_first_letter(lst)): the inner group_by_first_letter() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(group_by_first_letter(?)): the inner group_by_first_letter() executes FIRST, then print() outputs the result
#   print(group_by_first_letter(?)): the inner group_by_first_letter() executes FIRST, then print() outputs the result
#   group_by_first_letter(lst): looks up variable 'lst', then runs the function body
#   print(group_by_first_letter(?)): the inner group_by_first_letter() executes FIRST, then print() outputs the result
#   group_by_first_letter(lst): looks up variable 'lst', then runs the function body
#   group_by_first_letter(?): resolves the value, then runs the function body
#   group_by_first_letter(?): resolves the value, then runs the function body
#   group_by_first_letter(?): resolves the value, then runs the function body
#   first_letter not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def group_by_first_letter(lst) — stored for later
#   2. Assign to lst
#   3. Run print(group_by_first_letter(...))
#       calls group_by_first_letter with: lst=lst
#         3.1 Return from function
#   4. Define def main(lst) — stored for later
#   5. Assign to lst
#   6. Run main(lst)
#       with: lst=lst
#         6.1 Assign to result
#         6.2 For loop over lst
#         6.3 Return from function
#   7. Define def group_by_first_letter(lst) — stored for later
#   8. Run print(group_by_first_letter(...))
#       calls group_by_first_letter with: lst=['apple', 'banana', 'avocado', 'blueberry']
#         8.1 Return from function
#   9. Define def group_by_first_letter(lst) — stored for later
#   10. Run print(group_by_first_letter(...))
#       calls group_by_first_letter with: lst=['cat', 'dog', 'car', 'deer']
#         10.1 Return from function
#   11. Define def group_by_first_letter(lst) — stored for later
#   12. Define def group_by_first_letter(lst) — stored for later
#   13. Assign to lst
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(group_by_first_letter(...))
#       calls group_by_first_letter with: lst=[1, 3, 5, 7]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions group_by_first_letter, main, group_by_first_letter, group_by_first_letter, group_by_first_letter, group_by_first_letter are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(group_by_first_letter(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      print(group_by_first_letter(?)) → function runs first, then print shows the result
#      print(group_by_first_letter(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(group_by_first_letter(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 183:
#
# ===== SYNTAX =====
#   def is_anagram(string1, string2)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   Counter() — built-in function call
#   all() — built-in function call
#   set() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: == != in
#
# ===== EVALUATION ORDER =====
#   print(is_anagram(string1, string2)): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('listen', 'silent')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('hello', 'olleh')): the inner is_anagram() executes FIRST, then print() outputs the result
#   print(is_anagram('test', 'tent')): the inner is_anagram() executes FIRST, then print() outputs the result
#   is_anagram(string1, string2): looks up variable 'string1', looks up variable 'string2', then runs the function body
#   print(is_anagram('world', 'python')): the inner is_anagram() executes FIRST, then print() outputs the result
#   is_anagram(string1, string2): looks up variable 'string1', looks up variable 'string2', then runs the function body
#   is_anagram('listen', 'silent'): gets the literal 'listen', gets the literal 'silent', then runs the function body
#   is_anagram('hello', 'olleh'): gets the literal 'hello', gets the literal 'olleh', then runs the function body
#   is_anagram('test', 'tent'): gets the literal 'test', gets the literal 'tent', then runs the function body
#   is_anagram('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   len(...) != len(...): is this True or False? The answer picks which branch
#   len(...) != len(...): is this True or False? The answer picks which branch
#   char in char_count1: is this True or False? The answer picks which branch
#   char in char_count2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_anagram(string1, string2) — stored for later
#   2. Assign to string1
#   3. Assign to string2
#   4. Run print('Are the strings anagrams?:', is_anagram(...))
#   5. Define def is_anagram(string1, string2) — stored for later
#   6. Run print(is_anagram(...))
#       calls is_anagram with: string1='listen', string2='silent'
#         6.1 Return from function
#   7. Define def is_anagram(string1, string2) — stored for later
#   8. Run print(is_anagram(...))
#       calls is_anagram with: string1='hello', string2='olleh'
#         8.1 Return from function
#   9. Run print(is_anagram(...))
#       calls is_anagram with: string1='test', string2='tent'
#         9.1 Return from function
#   10. Define def is_anagram(string1, string2) — stored for later
#   11. Define def is_anagram(string1, string2) — stored for later
#   12. Assign to string1
#   13. Assign to string2
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(is_anagram(...))
#       calls is_anagram with: string1='world', string2='python'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_anagram, is_anagram, is_anagram, is_anagram, is_anagram are just stored
#   Execution starts at the non-def lines below:
#      string1 = ... (stored in memory)
#      string2 = ... (stored in memory)
#      print('Are the strings anagrams?:', is_anagram(...))
#      print(is_anagram('listen', 'silent')) → function runs first, then print shows the result
#      print(is_anagram('hello', 'olleh')) → function runs first, then print shows the result
#      print(is_anagram('test', 'tent')) → function runs first, then print shows the result
#      string1 = ... (stored in memory)
#      string2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_anagram('world', 'python')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 184:
#
# ===== SYNTAX =====
#   def int(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   list() — built-in function call
#   operators: // %
#
# ===== EVALUATION ORDER =====
#   print(int(number)): the inner int() executes FIRST, then print() outputs the result
#   print(extract_digit(123)): the inner extract_digit() executes FIRST, then print() outputs the result
#   print(get_digit(123)): the inner get_digit() executes FIRST, then print() outputs the result
#   int(number): looks up variable 'number', then runs the function body
#   print(int(25)): the inner int() executes FIRST, then print() outputs the result
#   int(number): looks up variable 'number', then runs the function body
#   ? % 10: first get ?, then get 10, then compute ? % 10
#   extract_digit(123): gets the literal 123, then runs the function body
#   get_digit(123): gets the literal 123, then runs the function body
#   int(25): gets the literal 25, then runs the function body
#   number // 10: first get number, then get 10, then compute number // 10
#
# ===== EXECUTION ORDER =====
#   1. Define def int(number) — stored for later
#   2. Assign to number
#   3. Run print(int(...))
#       calls int with: number=number
#         3.1 Return from function
#   4. Define def extract_digit(number) — stored for later
#   5. Run print(extract_digit(...))
#       calls extract_digit with: number=123
#         5.1 Return from function
#   6. Define def get_digit(n) — stored for later
#   7. Run print(get_digit(...))
#       calls get_digit with: n=123
#         7.1 Return from function
#   8. Define def int(number) — stored for later
#   9. Define def int(number) — stored for later
#   10. Assign to number
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(int(...))
#       calls int with: number=25
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions int, extract_digit, get_digit, int, int are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(int(number)) → function runs first, then print shows the result
#      print(extract_digit(123)) → function runs first, then print shows the result
#      print(get_digit(123)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(int(25)) → function runs first, then print shows the result

"""

"""
Problem 185:
#
# ===== SYNTAX =====
#   def int(number)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   tuple() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(int(number)): the inner int() executes FIRST, then print() outputs the result
#   print(extract_digits(12345678)): the inner extract_digits() executes FIRST, then print() outputs the result
#   print(get_digits(12345678)): the inner get_digits() executes FIRST, then print() outputs the result
#   int(number): looks up variable 'number', then runs the function body
#   print(int(?)): the inner int() executes FIRST, then print() outputs the result
#   int(number): looks up variable 'number', then runs the function body
#   extract_digits(12345678): gets the literal 12345678, then runs the function body
#   get_digits(12345678): gets the literal 12345678, then runs the function body
#   int(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def int(number) — stored for later
#   2. Assign to number
#   3. Run print(int(...))
#       calls int with: number=number
#         3.1 Return from function
#   4. Define def extract_digits(number) — stored for later
#   5. Run print(extract_digits(...))
#       calls extract_digits with: number=12345678
#         5.1 Assign to s
#         5.2 Return from function
#   6. Define def get_digits(num) — stored for later
#   7. Run print(get_digits(...))
#       calls get_digits with: num=12345678
#         7.1 Assign to s
#         7.2 Return from function
#   8. Define def int(number) — stored for later
#   9. Define def int(number) — stored for later
#   10. Assign to number
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(int(...))
#       calls int with: number=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions int, extract_digits, get_digits, int, int are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(int(number)) → function runs first, then print shows the result
#      print(extract_digits(12345678)) → function runs first, then print shows the result
#      print(get_digits(12345678)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(int(?)) → function runs first, then print shows the result

"""

"""
Problem 186:
#
# ===== SYNTAX =====
#   def get_digits(number, indices)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   tuple() — built-in function call
#   itemgetter() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(get_digits(number, indices)): the inner get_digits() executes FIRST, then print() outputs the result
#   print(get_digits(12345678910, ?)): the inner get_digits() executes FIRST, then print() outputs the result
#   print(get_digits(12345678910, ?)): the inner get_digits() executes FIRST, then print() outputs the result
#   get_digits(number, indices): looks up variable 'number', looks up variable 'indices', then runs the function body
#   print(get_digits(25, ?)): the inner get_digits() executes FIRST, then print() outputs the result
#   get_digits(number, indices): looks up variable 'number', looks up variable 'indices', then runs the function body
#   get_digits(12345678910, ?): gets the literal 12345678910, resolves the value, then runs the function body
#   get_digits(12345678910, ?): gets the literal 12345678910, resolves the value, then runs the function body
#   get_digits(25, ?): gets the literal 25, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_digits(number, indices) — stored for later
#   2. Assign to number
#   3. Assign to indices
#   4. Run print(get_digits(...))
#       calls get_digits with: number=number, indices=indices
#         4.1 Return from function
#   5. Define def get_digits(number, indices) — stored for later
#   6. Run print(get_digits(...))
#       calls get_digits with: number=12345678910, indices=[1, 3, 7]
#         6.1 Return from function
#   7. Define def get_digits(number, indices) — stored for later
#   8. Run print(get_digits(...))
#       calls get_digits with: number=12345678910, indices=[1, 3, 7]
#         8.1 Return from function
#   9. Define def get_digits(number, indices) — stored for later
#   10. Define def get_digits(number, indices) — stored for later
#   11. Assign to number
#   12. Assign to indices
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(get_digits(...))
#       calls get_digits with: number=25, indices=[4, 1, 8, 6, 3]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_digits, get_digits, get_digits, get_digits, get_digits are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      indices = ... (stored in memory)
#      print(get_digits(number, indices)) → function runs first, then print shows the result
#      print(get_digits(12345678910, ?)) → function runs first, then print shows the result
#      print(get_digits(12345678910, ?)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      indices = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_digits(25, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from indices each round

"""

"""
Problem 187:
#
# ===== SYNTAX =====
#   def double_elements(string1)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(double_elements(string1)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(string1): looks up variable 'string1', then runs the function body
#   print(double_elements('world')): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(string1): looks up variable 'string1', then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(?): resolves the value, then runs the function body
#   double_elements('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def double_elements(string1) — stored for later
#   2. Assign to string1
#   3. Run print(double_elements(...))
#       calls double_elements with: string1=string1
#         3.1 Return from function
#   4. Define def double_elements(lst) — stored for later
#   5. Run print(double_elements(...))
#       calls double_elements with: string1=['tree', 'road', 'slow']
#         5.1 Return from function
#   6. Define def double_elements(lst) — stored for later
#   7. Run print(double_elements(...))
#       calls double_elements with: string1=['a', 'b', 'c']
#         7.1 Return from function
#   8. Define def double_elements(string1) — stored for later
#   9. Define def double_elements(string1) — stored for later
#   10. Assign to string1
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(double_elements(...))
#       calls double_elements with: string1='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions double_elements, double_elements, double_elements, double_elements, double_elements are just stored
#   Execution starts at the non-def lines below:
#      string1 = ... (stored in memory)
#      print(double_elements(string1)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      print(double_elements(?)) → function runs first, then print shows the result
#      string1 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(double_elements('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string1 each round

"""

"""
Problem 188:
#
# ===== SYNTAX =====
#   def get_initials(name)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(get_initials(name)): the inner get_initials() executes FIRST, then print() outputs the result
#   print(get_initials(name)): the inner get_initials() executes FIRST, then print() outputs the result
#   print(get_initials('jonathan noll')): the inner get_initials() executes FIRST, then print() outputs the result
#   print(get_initials('jonathan noll')): the inner get_initials() executes FIRST, then print() outputs the result
#   get_initials(name): looks up variable 'name', then runs the function body
#   print(get_initials('Bob')): the inner get_initials() executes FIRST, then print() outputs the result
#   get_initials(name): looks up variable 'name', then runs the function body
#   get_initials(name): looks up variable 'name', then runs the function body
#   get_initials('jonathan noll'): gets the literal 'jonathan noll', then runs the function body
#   get_initials('jonathan noll'): gets the literal 'jonathan noll', then runs the function body
#   get_initials('Bob'): gets the literal 'Bob', then runs the function body
#   char in 'nall': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def get_initials(name) — stored for later
#   2. Assign to name
#   3. Run print(get_initials(...))
#       calls get_initials with: name=name
#         3.1 Return from function
#   4. Define def get_initials(name) — stored for later
#   5. Assign to name
#   6. Run print(get_initials(...))
#       calls get_initials with: name=name
#         6.1 Return from function
#   7. Define def get_initials(name) — stored for later
#   8. Run print(get_initials(...))
#       calls get_initials with: name='jonathan noll'
#         8.1 Return from function
#   9. Define def get_initials(name) — stored for later
#   10. Run print(get_initials(...))
#       calls get_initials with: name='jonathan noll'
#         10.1 Return from function
#   11. Define def get_initials(name) — stored for later
#   12. Define def get_initials(name) — stored for later
#   13. Assign to name
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(get_initials(...))
#       calls get_initials with: name='Bob'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_initials, get_initials, get_initials, get_initials, get_initials, get_initials are just stored
#   Execution starts at the non-def lines below:
#      name = ... (stored in memory)
#      print(get_initials(name)) → function runs first, then print shows the result
#      name = ... (stored in memory)
#      print(get_initials(name)) → function runs first, then print shows the result
#      print(get_initials('jonathan noll')) → function runs first, then print shows the result
#      print(get_initials('jonathan noll')) → function runs first, then print shows the result
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_initials('Bob')) → function runs first, then print shows the result
#
#   Loop: grabs one item from name each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 189:
#
# ===== SYNTAX =====
#   def sum_digits()
#   list() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_digits(): — no arguments needed
#   digits_string(): — no arguments needed
#   print(sum_digits('1234')): the inner sum_digits() executes FIRST, then print() outputs the result
#   print(sum_digits('a1b2c3')): the inner sum_digits() executes FIRST, then print() outputs the result
#   print(sum_digits('45x67')): the inner sum_digits() executes FIRST, then print() outputs the result
#   sum_digits(s): looks up variable 's', then runs the function body
#   print(sum_digits(?)): the inner sum_digits() executes FIRST, then print() outputs the result
#   sum_digits('1234'): gets the literal '1234', then runs the function body
#   sum_digits('a1b2c3'): gets the literal 'a1b2c3', then runs the function body
#   sum_digits('45x67'): gets the literal '45x67', then runs the function body
#   sum_digits(?): resolves the value, then runs the function body
#   a + int(...): first get a, then get int(...), then compute a + int(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_digits() — stored for later
#   2. Run sum_digits()
#         2.1 Assign to user
#         2.2 Assign to count
#         2.3 For loop over user
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def digits_string() — stored for later
#   4. Run digits_string()
#         4.1 Assign to user
#         4.2 Assign to total
#         4.3 For loop over user
#         4.4 Run print(total)
#   5. Define def sum_digits(s) — stored for later
#   6. Run print(sum_digits(...))
#         6.1 Assign to user
#         6.2 Assign to count
#         6.3 For loop over user
#         6.4 Assign to result
#         6.5 Return from function
#   7. Run print(sum_digits(...))
#         7.1 Assign to user
#         7.2 Assign to count
#         7.3 For loop over user
#         7.4 Assign to result
#         7.5 Return from function
#   8. Define def sum_digits(s) — stored for later
#   9. Run print(sum_digits(...))
#         9.1 Assign to user
#         9.2 Assign to count
#         9.3 For loop over user
#         9.4 Assign to result
#         9.5 Return from function
#   10. Define def sum_digits() — stored for later
#   11. Define def sum_digits() — stored for later
#   12. Assign to s
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(sum_digits(...))
#         15.1 Assign to user
#         15.2 Assign to count
#         15.3 For loop over user
#         15.4 Assign to result
#         15.5 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_digits, digits_string, sum_digits, sum_digits, sum_digits, sum_digits are just stored
#   Execution starts at the non-def lines below:
#      sum_digits() → jumps into function, runs body, returns result → printed
#      digits_string() → jumps into function, runs body, returns result → printed
#      print(sum_digits('1234')) → function runs first, then print shows the result
#      print(sum_digits('a1b2c3')) → function runs first, then print shows the result
#      print(sum_digits('45x67')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_digits(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 190:
#
# ===== SYNTAX =====
#   def remove_duplicate()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   remove_duplicate(): — no arguments needed
#   print(remove_duplicate('hello')): the inner remove_duplicate() executes FIRST, then print() outputs the result
#   print(remove_duplicate('banana')): the inner remove_duplicate() executes FIRST, then print() outputs the result
#   remove_duplicate(s): looks up variable 's', then runs the function body
#   print(remove_duplicate(10)): the inner remove_duplicate() executes FIRST, then print() outputs the result
#   remove_duplicate('hello'): gets the literal 'hello', then runs the function body
#   remove_duplicate('banana'): gets the literal 'banana', then runs the function body
#   remove_duplicate(10): gets the literal 10, then runs the function body
#   char not in duplicate: is this True or False? The answer picks which branch
#   char not in duplicate: is this True or False? The answer picks which branch
#   char not in duplicate: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicate() — stored for later
#   2. Run remove_duplicate()
#         2.1 Assign to user
#         2.2 Assign to duplicate
#         2.3 For loop over user
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def remove_duplicate(s) — stored for later
#   4. Run print(remove_duplicate(...))
#         4.1 Assign to user
#         4.2 Assign to duplicate
#         4.3 For loop over user
#         4.4 Assign to result
#         4.5 Return from function
#   5. Define def remove_duplicate(s) — stored for later
#   6. Run print(remove_duplicate(...))
#         6.1 Assign to user
#         6.2 Assign to duplicate
#         6.3 For loop over user
#         6.4 Assign to result
#         6.5 Return from function
#   7. Define def remove_duplicate() — stored for later
#   8. Define def remove_duplicate() — stored for later
#   9. Assign to s
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(remove_duplicate(...))
#         12.1 Assign to user
#         12.2 Assign to duplicate
#         12.3 For loop over user
#         12.4 Assign to result
#         12.5 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicate, remove_duplicate, remove_duplicate, remove_duplicate, remove_duplicate are just stored
#   Execution starts at the non-def lines below:
#      remove_duplicate() → jumps into function, runs body, returns result → printed
#      print(remove_duplicate('hello')) → function runs first, then print shows the result
#      print(remove_duplicate('banana')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicate(10)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 191:
#
# ===== SYNTAX =====
#   def find_min()
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: <
#
# ===== EVALUATION ORDER =====
#   find_min(): — no arguments needed
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   find_min(nums): looks up variable 'nums', then runs the function body
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   find_min(?): resolves the value, then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   number < min_number: is this True or False? The answer picks which branch
#   number < min_number: is this True or False? The answer picks which branch
#   number < min_number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min() — stored for later
#   2. Run find_min()
#         2.1 Assign to user
#         2.2 Assign to user_int
#         2.3 For loop over user
#         2.4 Assign to min_number
#         2.5 For loop over user_int
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def find_min(nums) — stored for later
#   4. Run print(find_min(...))
#         4.1 Assign to user
#         4.2 Assign to user_int
#         4.3 For loop over user
#         4.4 Assign to min_number
#         4.5 For loop over user_int
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def find_min(nums) — stored for later
#   6. Run print(find_min(...))
#         6.1 Assign to user
#         6.2 Assign to user_int
#         6.3 For loop over user
#         6.4 Assign to min_number
#         6.5 For loop over user_int
#         6.6 Assign to result
#         6.7 Return from function
#   7. Define def find_min() — stored for later
#   8. Define def find_min() — stored for later
#   9. Assign to nums
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_min(...))
#         12.1 Assign to user
#         12.2 Assign to user_int
#         12.3 For loop over user
#         12.4 Assign to min_number
#         12.5 For loop over user_int
#         12.6 Assign to result
#         12.7 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min, find_min, find_min, find_min, find_min are just stored
#   Execution starts at the non-def lines below:
#      find_min() → jumps into function, runs body, returns result → printed
#      print(find_min(?)) → function runs first, then print shows the result
#      print(find_min(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 192:
#
# ===== SYNTAX =====
#   def count_occurrences(lst, element)
#   return — exits the function and sends back a value
#   input() — built-in function call
#   Counter() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(count_occurrences(test_list, element_to_count)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(main()): the inner main() executes FIRST, then print() outputs the result
#   print(count_occurrences(?, 1)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?, 'a')): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?, 1)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   print(count_occurrences(?, 25)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(test_list, element_to_count): looks up variable 'test_list', looks up variable 'element_to_count', then runs the function body
#   main(): — no arguments needed
#   count_occurrences(?, 1): resolves the value, gets the literal 1, then runs the function body
#   count_occurrences(?, 'a'): resolves the value, gets the literal 'a', then runs the function body
#   count_occurrences(?, 1): resolves the value, gets the literal 1, then runs the function body
#   count_occurrences(?, 25): resolves the value, gets the literal 25, then runs the function body
#   count == 0: is this True or False? The answer picks which branch
#   item == element: is this True or False? The answer picks which branch
#   word.lower(...) == user: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences(lst, element) — stored for later
#   2. Assign to test_list
#   3. Assign to element_to_count
#   4. Run print('Number of occurrences of', element_to_count, ':', count_occurrences(...))
#   5. Define def main() — stored for later
#   6. Assign to lst
#   7. Run print(main(...))
#         7.1 Assign to user
#         7.2 Assign to count
#         7.3 For loop over lst
#         7.4 If statement (one branch runs)
#   8. Define def count_occurrences(lst, element) — stored for later
#   9. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 2, 3, 1, 1, 4], element=1
#         9.1 Return from function
#   10. Define def count_occurrences(lst, element) — stored for later
#   11. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=['a', 'b', 'a', 'c', 'a'], element='a'
#         11.1 Return from function
#   12. Define def count_occurrences(lst, element) — stored for later
#   13. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 2, 1, 3], element=1
#         13.1 Return from function
#   14. Define def count_occurrences(lst, element) — stored for later
#   15. Define def count_occurrences(lst, element) — stored for later
#   16. Assign to lst
#   17. Assign to element
#   18. Assign to result
#   19. Run print(result)
#   20. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 3, 5, 7], element=25
#         20.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences, main, count_occurrences, count_occurrences, count_occurrences, count_occurrences, count_occurrences are just stored
#   Execution starts at the non-def lines below:
#      test_list = ... (stored in memory)
#      element_to_count = ... (stored in memory)
#      print('Number of occurrences of', element_to_count, ':', count_occurrences(...))
#      lst = ... (stored in memory)
#      print(main()) → function runs first, then print shows the result
#      print(count_occurrences(?, 1)) → function runs first, then print shows the result
#      print(count_occurrences(?, 'a')) → function runs first, then print shows the result
#      print(count_occurrences(?, 1)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 193:
#
# ===== SYNTAX =====
#   def find_min_num()
#   sorted() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   find_min_num(): — no arguments needed
#   print(find_min_num(?)): the inner find_min_num() executes FIRST, then print() outputs the result
#   print(find_min_num(?)): the inner find_min_num() executes FIRST, then print() outputs the result
#   find_min_num(nums): looks up variable 'nums', then runs the function body
#   print(find_min_num(?)): the inner find_min_num() executes FIRST, then print() outputs the result
#   find_min_num(?): resolves the value, then runs the function body
#   find_min_num(?): resolves the value, then runs the function body
#   find_min_num(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min_num() — stored for later
#   2. Run find_min_num()
#         2.1 Assign to user
#         2.2 Assign to int_number
#         2.3 For loop over user
#         2.4 Assign to min_num
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def find_min_num(nums) — stored for later
#   4. Run print(find_min_num(...))
#         4.1 Assign to user
#         4.2 Assign to int_number
#         4.3 For loop over user
#         4.4 Assign to min_num
#         4.5 Assign to result
#         4.6 Return from function
#   5. Define def find_min_num(nums) — stored for later
#   6. Run print(find_min_num(...))
#         6.1 Assign to user
#         6.2 Assign to int_number
#         6.3 For loop over user
#         6.4 Assign to min_num
#         6.5 Assign to result
#         6.6 Return from function
#   7. Define def find_min_num() — stored for later
#   8. Define def find_min_num() — stored for later
#   9. Assign to nums
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_min_num(...))
#         12.1 Assign to user
#         12.2 Assign to int_number
#         12.3 For loop over user
#         12.4 Assign to min_num
#         12.5 Assign to result
#         12.6 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min_num, find_min_num, find_min_num, find_min_num, find_min_num are just stored
#   Execution starts at the non-def lines below:
#      find_min_num() → jumps into function, runs body, returns result → printed
#      print(find_min_num(?)) → function runs first, then print shows the result
#      print(find_min_num(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min_num(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round

"""

"""
Problem 194:
#
# ===== SYNTAX =====
#   def largest_element()
#   max() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   reduce() — built-in function call
#   len() — built-in function call
#   lambda
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   largest_element(): — no arguments needed
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(words): looks up variable 'words', then runs the function body
#   print(largest_element(?)): the inner largest_element() executes FIRST, then print() outputs the result
#   largest_element(?): resolves the value, then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#   largest_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_element() — stored for later
#   2. Run largest_element()
#         2.1 Assign to user
#         2.2 Assign to largest_word
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def largest_element(words) — stored for later
#   4. Run print(largest_element(...))
#         4.1 Assign to user
#         4.2 Assign to largest_word
#         4.3 Assign to result
#         4.4 Return from function
#   5. Define def largest_element(words) — stored for later
#   6. Run print(largest_element(...))
#         6.1 Assign to user
#         6.2 Assign to largest_word
#         6.3 Assign to result
#         6.4 Return from function
#   7. Define def largest_element() — stored for later
#   8. Define def largest_element() — stored for later
#   9. Assign to words
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(largest_element(...))
#         12.1 Assign to user
#         12.2 Assign to largest_word
#         12.3 Assign to result
#         12.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_element, largest_element, largest_element, largest_element, largest_element are just stored
#   Execution starts at the non-def lines below:
#      largest_element() → jumps into function, runs body, returns result → printed
#      print(largest_element(?)) → function runs first, then print shows the result
#      print(largest_element(?)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_element(?)) → function runs first, then print shows the result

"""

"""
Problem 195:
#
# ===== SYNTAX =====
#   def second_largest_element()
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   input() — built-in function call
#   nlargest() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   second_largest_element(): — no arguments needed
#   second_largest_element(): — no arguments needed
#   second_largest_element(): — no arguments needed
#   print(second_largest_element(?)): the inner second_largest_element() executes FIRST, then print() outputs the result
#   print(second_largest_element(?)): the inner second_largest_element() executes FIRST, then print() outputs the result
#   second_largest_element(words): looks up variable 'words', then runs the function body
#   print(second_largest_element('world')): the inner second_largest_element() executes FIRST, then print() outputs the result
#   second_largest_element(?): resolves the value, then runs the function body
#   second_largest_element(?): resolves the value, then runs the function body
#   second_largest_element('world'): gets the literal 'world', then runs the function body
#   len(...) < 2: is this True or False? The answer picks which branch
#   len(...) < 2: is this True or False? The answer picks which branch
#   len(...) < 2: is this True or False? The answer picks which branch
#   len(...) < 2: is this True or False? The answer picks which branch
#   len(...) > len(...): is this True or False? The answer picks which branch
#   len(...) > len(...): is this True or False? The answer picks which branch
#   len(...) > len(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def second_largest_element() — stored for later
#   2. Run second_largest_element()
#       with: 
#         2.1 If statement (one branch runs)
#         2.2 Assign to first, second
#         2.3 For loop over words
#         2.4 Return from function
#   3. Define def second_largest_element() — stored for later
#   4. Run second_largest_element()
#       with: 
#         4.1 If statement (one branch runs)
#         4.2 Assign to first, second
#         4.3 For loop over words
#         4.4 Return from function
#   5. Define def second_largest_element() — stored for later
#   6. Run second_largest_element()
#       with: 
#         6.1 If statement (one branch runs)
#         6.2 Assign to first, second
#         6.3 For loop over words
#         6.4 Return from function
#   7. Define def second_largest_element(words) — stored for later
#   8. Run print(second_largest_element(...))
#       calls second_largest_element with: words=['apple', 'banana', 'kiwi', 'strawberry']
#         8.1 If statement (one branch runs)
#         8.2 Assign to first, second
#         8.3 For loop over words
#         8.4 Return from function
#   9. Define def second_largest_element(words) — stored for later
#   10. Run print(second_largest_element(...))
#       calls second_largest_element with: words=['cat', 'dog', 'elephant', 'bee']
#         10.1 If statement (one branch runs)
#         10.2 Assign to first, second
#         10.3 For loop over words
#         10.4 Return from function
#   11. Assign to words
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(second_largest_element(...))
#       calls second_largest_element with: words='world'
#         14.1 If statement (one branch runs)
#         14.2 Assign to first, second
#         14.3 For loop over words
#         14.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_largest_element, second_largest_element, second_largest_element, second_largest_element, second_largest_element are just stored
#   Execution starts at the non-def lines below:
#      second_largest_element() → jumps into function, runs body, returns result → printed
#      second_largest_element() → jumps into function, runs body, returns result → printed
#      second_largest_element() → jumps into function, runs body, returns result → printed
#      print(second_largest_element(?)) → function runs first, then print shows the result
#      print(second_largest_element(?)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_largest_element('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 196:
#
# ===== SYNTAX =====
#   def max_of_three(numbers)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   list() — built-in function call
#   sorted() — built-in function call
#   operators: > and
#
# ===== EVALUATION ORDER =====
#   print(max_of_three(three_numbers)): the inner max_of_three() executes FIRST, then print() outputs the result
#   print(max_of_three(?)): the inner max_of_three() executes FIRST, then print() outputs the result
#   print(max_of_three(?)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(numbers): looks up variable 'numbers', then runs the function body
#   print(max_of_three(?)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(three_numbers): looks up variable 'three_numbers', then runs the function body
#   max_of_three(?): resolves the value, then runs the function body
#   max_of_three(?): resolves the value, then runs the function body
#   max_of_three(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_three(numbers) — stored for later
#   2. Assign to three_numbers
#   3. Run print(max_of_three(...))
#       calls max_of_three with: numbers=three_numbers
#         3.1 Return from function
#   4. Define def max_of_three(numbers) — stored for later
#   5. Run print(max_of_three(...))
#       calls max_of_three with: numbers=[567, 56, 6]
#         5.1 Return from function
#   6. Define def max_of_three(numbers) — stored for later
#   7. Run print(max_of_three(...))
#       calls max_of_three with: numbers=[567, 56, 6]
#         7.1 Return from function
#   8. Define def max_of_three(numbers) — stored for later
#   9. Define def max_of_three(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(max_of_three(...))
#       calls max_of_three with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_three, max_of_three, max_of_three, max_of_three, max_of_three are just stored
#   Execution starts at the non-def lines below:
#      three_numbers = ... (stored in memory)
#      print(max_of_three(three_numbers)) → function runs first, then print shows the result
#      print(max_of_three(?)) → function runs first, then print shows the result
#      print(max_of_three(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_of_three(?)) → function runs first, then print shows the result

"""

"""
Problem 197:
#
# ===== SYNTAX =====
#   def max_of_three(numbers)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(max_of_three(three_numbers)): the inner max_of_three() executes FIRST, then print() outputs the result
#   print(max_of_three(?)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(numbers): looks up variable 'numbers', then runs the function body
#   print(max_of_three(?)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(three_numbers): looks up variable 'three_numbers', then runs the function body
#   max_of_three(?): resolves the value, then runs the function body
#   max_of_three(?): resolves the value, then runs the function body
#   ? > max_number: is this True or False? The answer picks which branch
#   ? > max_number: is this True or False? The answer picks which branch
#   n > max_val: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_three(numbers) — stored for later
#   2. Assign to three_numbers
#   3. Run print(max_of_three(...))
#       calls max_of_three with: numbers=three_numbers
#         3.1 Return from function
#   4. Define def max_of_three(numbers) — stored for later
#   5. Run print(max_of_three(...))
#       calls max_of_three with: numbers=[567, 56, 6]
#         5.1 Return from function
#   6. Define def max_of_three(numbers) — stored for later
#   7. Define def max_of_three(numbers) — stored for later
#   8. Assign to numbers
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(max_of_three(...))
#       calls max_of_three with: numbers=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_three, max_of_three, max_of_three, max_of_three are just stored
#   Execution starts at the non-def lines below:
#      three_numbers = ... (stored in memory)
#      print(max_of_three(three_numbers)) → function runs first, then print shows the result
#      print(max_of_three(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_of_three(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 198:
#
# ===== SYNTAX =====
#   def centimeters_inches()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   round() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   centimeters_inches(): — no arguments needed
#   print(centimeters_inches(50)): the inner centimeters_inches() executes FIRST, then print() outputs the result
#   cm_to_inches(cm): looks up variable 'cm', then runs the function body
#   print(cm_to_inches(25)): the inner cm_to_inches() executes FIRST, then print() outputs the result
#   cm * 0.3937: first get cm, then get 0.3937, then compute cm * 0.3937
#   cm * 0.3937: first get cm, then get 0.3937, then compute cm * 0.3937
#   centimeters_inches(50): gets the literal 50, then runs the function body
#   cm * 0.3937: first get cm, then get 0.3937, then compute cm * 0.3937
#   cm * 0.3937: first get cm, then get 0.3937, then compute cm * 0.3937
#   cm_to_inches(25): gets the literal 25, then runs the function body
#   cm * 0.3937: first get cm, then get 0.3937, then compute cm * 0.3937
#   cm_to_inches(100): gets the literal 100, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def centimeters_inches() — stored for later
#   2. Run centimeters_inches()
#         2.1 Assign to cm
#         2.2 Assign to inches
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def cm_to_inches(cm) — stored for later
#   4. Run print(?)
#   5. Define def centimeters_inches(cm) — stored for later
#   6. Run print(centimeters_inches(...))
#         6.1 Assign to cm
#         6.2 Assign to inches
#         6.3 Assign to result
#         6.4 Return from function
#   7. Define def centimeters_inches() — stored for later
#   8. Define def centimeters_inches() — stored for later
#   9. Assign to cm
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(cm_to_inches(...))
#       calls cm_to_inches with: cm=25
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions centimeters_inches, cm_to_inches, centimeters_inches, centimeters_inches, centimeters_inches are just stored
#   Execution starts at the non-def lines below:
#      centimeters_inches() → jumps into function, runs body, returns result → printed
#      print(?)
#      print(centimeters_inches(50)) → function runs first, then print shows the result
#      cm = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(cm_to_inches(25)) → function runs first, then print shows the result

"""

"""
Problem 199:
#
# ===== SYNTAX =====
#   def find_min_max()
#   min() — built-in function call
#   max() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   find_min_max(): — no arguments needed
#   find_min_max(): — no arguments needed
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   find_min_max(nums): looks up variable 'nums', then runs the function body
#   print(find_min_max(?)): the inner find_min_max() executes FIRST, then print() outputs the result
#   find_min_max(?): resolves the value, then runs the function body
#   find_min_max(?): resolves the value, then runs the function body
#   find_min_max(?): resolves the value, then runs the function body
#   find_min_max(?): resolves the value, then runs the function body
#   number < min_number: is this True or False? The answer picks which branch
#   number > max_number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min_max() — stored for later
#   2. Run find_min_max()
#         2.1 Assign to user
#         2.2 Assign to int_num
#         2.3 For loop over user
#         2.4 Assign to min_num
#         2.5 Assign to max_num
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def find_min_max() — stored for later
#   4. Run find_min_max()
#         4.1 Assign to user
#         4.2 Assign to int_num
#         4.3 For loop over user
#         4.4 Assign to min_num
#         4.5 Assign to max_num
#         4.6 Assign to result
#         4.7 Return from function
#   5. Define def find_min_max(nums) — stored for later
#   6. Run print(find_min_max(...))
#         6.1 Assign to user
#         6.2 Assign to int_num
#         6.3 For loop over user
#         6.4 Assign to min_num
#         6.5 Assign to max_num
#         6.6 Assign to result
#         6.7 Return from function
#   7. Define def find_min_max(nums) — stored for later
#   8. Run print(find_min_max(...))
#         8.1 Assign to user
#         8.2 Assign to int_num
#         8.3 For loop over user
#         8.4 Assign to min_num
#         8.5 Assign to max_num
#         8.6 Assign to result
#         8.7 Return from function
#   9. Define def find_min_max(nums) — stored for later
#   10. Run print(find_min_max(...))
#         10.1 Assign to user
#         10.2 Assign to int_num
#         10.3 For loop over user
#         10.4 Assign to min_num
#         10.5 Assign to max_num
#         10.6 Assign to result
#         10.7 Return from function
#   11. Define def find_min_max() — stored for later
#   12. Define def find_min_max() — stored for later
#   13. Assign to nums
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(find_min_max(...))
#         16.1 Assign to user
#         16.2 Assign to int_num
#         16.3 For loop over user
#         16.4 Assign to min_num
#         16.5 Assign to max_num
#         16.6 Assign to result
#         16.7 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min_max, find_min_max, find_min_max, find_min_max, find_min_max, find_min_max, find_min_max are just stored
#   Execution starts at the non-def lines below:
#      find_min_max() → jumps into function, runs body, returns result → printed
#      find_min_max() → jumps into function, runs body, returns result → printed
#      print(find_min_max(?)) → function runs first, then print shows the result
#      print(find_min_max(?)) → function runs first, then print shows the result
#      print(find_min_max(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min_max(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 200:
#
# ===== SYNTAX =====
#   def common_numbers(lst)
#   return — exits the function and sends back a value
#   Counter() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: + > and in
#
# ===== EVALUATION ORDER =====
#   print(common_numbers(lst)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   common_numbers(lst): looks up variable 'lst', then runs the function body
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   common_numbers(lst): looks up variable 'lst', then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   count.get(...) + 1: first get count.get(...), then get 1, then compute count.get(...) + 1
#   number in seen: is this True or False? The answer picks which branch
#   number not in common: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def common_numbers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(common_numbers(...))
#       calls common_numbers with: lst=lst
#         3.1 Return from function
#   4. Define def common_numbers(lst) — stored for later
#   5. Run print(common_numbers(...))
#       calls common_numbers with: lst=[43, 2, 45, 567, 666, 2, 45]
#         5.1 Return from function
#   6. Define def common_numbers(lst) — stored for later
#   7. Run print(common_numbers(...))
#       calls common_numbers with: lst=[43, 2, 45, 567, 666, 2, 45]
#         7.1 Return from function
#   8. Define def common_numbers(lst) — stored for later
#   9. Run print(common_numbers(...))
#       calls common_numbers with: lst=[1, 2, 2, 3, 4, 4, 5]
#         9.1 Return from function
#   10. Define def common_numbers(lst) — stored for later
#   11. Define def common_numbers(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(common_numbers(...))
#       calls common_numbers with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common_numbers, common_numbers, common_numbers, common_numbers, common_numbers, common_numbers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(common_numbers(lst)) → function runs first, then print shows the result
#      print(common_numbers(?)) → function runs first, then print shows the result
#      print(common_numbers(?)) → function runs first, then print shows the result
#      print(common_numbers(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 201:
#
# ===== SYNTAX =====
#   def common_num(lst)
#   return — exits the function and sends back a value
#   set() — built-in function call
#   list() — built-in function call
#   Counter() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: + > in
#
# ===== EVALUATION ORDER =====
#   print(common_num(lst)): the inner common_num() executes FIRST, then print() outputs the result
#   print(common_numbers(lst)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   print(common_numbers(?)): the inner common_numbers() executes FIRST, then print() outputs the result
#   common_num(lst): looks up variable 'lst', then runs the function body
#   print(common_num(?)): the inner common_num() executes FIRST, then print() outputs the result
#   common_num(lst): looks up variable 'lst', then runs the function body
#   common_numbers(lst): looks up variable 'lst', then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   common_numbers(?): resolves the value, then runs the function body
#   common_num(?): resolves the value, then runs the function body
#   count.get(...) + 1: first get count.get(...), then get 1, then compute count.get(...) + 1
#   number in unique: is this True or False? The answer picks which branch
#   number in seen: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def common_num(lst) — stored for later
#   2. Assign to lst
#   3. Run print(common_num(...))
#       calls common_num with: lst=lst
#         3.1 Return from function
#   4. Define def common_numbers(lst) — stored for later
#   5. Assign to lst
#   6. Run print(common_numbers(...))
#       calls common_numbers with: lst=lst
#         6.1 Import Counter from collections
#         6.2 Return from function
#   7. Define def common_numbers(lst) — stored for later
#   8. Run print(common_numbers(...))
#       calls common_numbers with: lst=[1, 2, 2, 3, 3, 3, 4]
#         8.1 Import Counter from collections
#         8.2 Return from function
#   9. Define def common_numbers(lst) — stored for later
#   10. Run print(common_numbers(...))
#       calls common_numbers with: lst=[1, 2, 2, 3, 4, 4]
#         10.1 Import Counter from collections
#         10.2 Return from function
#   11. Define def common_numbers(lst) — stored for later
#   12. Run print(common_numbers(...))
#       calls common_numbers with: lst=[5, 5, 6, 7, 7, 8]
#         12.1 Import Counter from collections
#         12.2 Return from function
#   13. Define def common_num(lst) — stored for later
#   14. Define def common_num(lst) — stored for later
#   15. Assign to lst
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(common_num(...))
#       calls common_num with: lst=[1, 3, 5, 7]
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions common_num, common_numbers, common_numbers, common_numbers, common_numbers, common_num, common_num are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(common_num(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(common_numbers(lst)) → function runs first, then print shows the result
#      print(common_numbers(?)) → function runs first, then print shows the result
#      print(common_numbers(?)) → function runs first, then print shows the result
#      print(common_numbers(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(common_num(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 202:
#
# ===== SYNTAX =====
#   def word_frequency(string)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   print(word_frequency(string)): the inner word_frequency() executes FIRST, then print() outputs the result
#   print(word_frequency('hello world')): the inner word_frequency() executes FIRST, then print() outputs the result
#   print(word_frequency('aabbc')): the inner word_frequency() executes FIRST, then print() outputs the result
#   word_frequency(string): looks up variable 'string', then runs the function body
#   print(word_frequency('world')): the inner word_frequency() executes FIRST, then print() outputs the result
#   word_frequency(string): looks up variable 'string', then runs the function body
#   word_frequency('hello world'): gets the literal 'hello world', then runs the function body
#   word_frequency('aabbc'): gets the literal 'aabbc', then runs the function body
#   word_frequency('world'): gets the literal 'world', then runs the function body
#   result.get(...) + 1: first get result.get(...), then get 1, then compute result.get(...) + 1
#   word in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def word_frequency(string) — stored for later
#   2. Assign to string
#   3. Run print(word_frequency(...))
#       calls word_frequency with: string=string
#         3.1 Return from function
#   4. Define def word_frequency(string) — stored for later
#   5. Run print(word_frequency(...))
#       calls word_frequency with: string='hello world'
#         5.1 Return from function
#   6. Define def word_frequency(string) — stored for later
#   7. Run print(word_frequency(...))
#       calls word_frequency with: string='aabbc'
#         7.1 Return from function
#   8. Define def word_frequency(string) — stored for later
#   9. Define def word_frequency(string) — stored for later
#   10. Assign to string
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(word_frequency(...))
#       calls word_frequency with: string='world'
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions word_frequency, word_frequency, word_frequency, word_frequency, word_frequency are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(word_frequency(string)) → function runs first, then print shows the result
#      print(word_frequency('hello world')) → function runs first, then print shows the result
#      print(word_frequency('aabbc')) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(word_frequency('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from sort_char each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 203:
#
# ===== SYNTAX =====
#   def total(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(total(lst_numbers)): the inner total() executes FIRST, then print() outputs the result
#   print(total(?)): the inner total() executes FIRST, then print() outputs the result
#   print(total(?)): the inner total() executes FIRST, then print() outputs the result
#   total(numbers): looks up variable 'numbers', then runs the function body
#   print(total(?)): the inner total() executes FIRST, then print() outputs the result
#   total(lst_numbers): looks up variable 'lst_numbers', then runs the function body
#   total(?): resolves the value, then runs the function body
#   total(?): resolves the value, then runs the function body
#   total(?): resolves the value, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#
# ===== EXECUTION ORDER =====
#   1. Define def total(numbers) — stored for later
#   2. Assign to lst_numbers
#   3. Run print(total(...))
#       calls total with: numbers=lst_numbers
#         3.1 Return from function
#   4. Define def total(numbers) — stored for later
#   5. Run print(total(...))
#       calls total with: numbers=[1, 2, 3, 4]
#         5.1 Return from function
#   6. Define def total(numbers) — stored for later
#   7. Run print(total(...))
#       calls total with: numbers=[1, 2, 3, 4]
#         7.1 Return from function
#   8. Define def total(numbers) — stored for later
#   9. Define def total(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(total(...))
#       calls total with: numbers=[4, 1, 8, 6, 3]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions total, total, total, total, total are just stored
#   Execution starts at the non-def lines below:
#      lst_numbers = ... (stored in memory)
#      print(total(lst_numbers)) → function runs first, then print shows the result
#      print(total(?)) → function runs first, then print shows the result
#      print(total(?)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(total(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 204:
#
# ===== SYNTAX =====
#   def list_integers(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(list_integers(lst)): the inner list_integers() executes FIRST, then print() outputs the result
#   print(list_integers(?)): the inner list_integers() executes FIRST, then print() outputs the result
#   print(list_integers(?)): the inner list_integers() executes FIRST, then print() outputs the result
#   list_integers(lst): looks up variable 'lst', then runs the function body
#   print(list_integers(?)): the inner list_integers() executes FIRST, then print() outputs the result
#   list_integers(lst): looks up variable 'lst', then runs the function body
#   list_integers(?): resolves the value, then runs the function body
#   list_integers(?): resolves the value, then runs the function body
#   list_integers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_integers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_integers(...))
#       calls list_integers with: lst=lst
#         3.1 Return from function
#   4. Define def list_integers(lst) — stored for later
#   5. Run print(list_integers(...))
#       calls list_integers with: lst=['join', 'Jonathan', 'tree']
#         5.1 Return from function
#   6. Define def list_integers(lst) — stored for later
#   7. Run print(list_integers(...))
#       calls list_integers with: lst=['hi', 'there']
#         7.1 Return from function
#   8. Define def list_integers(lst) — stored for later
#   9. Define def list_integers(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(list_integers(...))
#       calls list_integers with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_integers, list_integers, list_integers, list_integers, list_integers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_integers(lst)) → function runs first, then print shows the result
#      print(list_integers(?)) → function runs first, then print shows the result
#      print(list_integers(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_integers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 205:
#
# ===== SYNTAX =====
#   def average(lst)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(average(lst)): the inner average() executes FIRST, then print() outputs the result
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   average(lst): looks up variable 'lst', then runs the function body
#   print(average(?)): the inner average() executes FIRST, then print() outputs the result
#   sum_lst / length: first get sum_lst, then get length, then compute sum_lst / length
#   average(lst): looks up variable 'lst', then runs the function body
#   average(?): resolves the value, then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   average(?): resolves the value, then runs the function body
#   sum_lst * ?: first get sum_lst, then get ?, then compute sum_lst * ?
#   average(?): resolves the value, then runs the function body
#   1 / length: first get 1, then get length, then compute 1 / length
#
# ===== EXECUTION ORDER =====
#   1. Define def average(lst) — stored for later
#   2. Assign to lst
#   3. Run print(average(...))
#       calls average with: lst=lst
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def average(lst) — stored for later
#   5. Run print(average(...))
#       calls average with: lst=[2666, 566, 6777]
#         5.1 Import Fraction from fractions
#         5.2 Return from function
#   6. Define def average(lst) — stored for later
#   7. Run print(average(...))
#       calls average with: lst=[1, 2, 3, 4]
#         7.1 Import Fraction from fractions
#         7.2 Return from function
#   8. Define def average(lst) — stored for later
#   9. Define def average(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(average(...))
#       calls average with: lst=[1, 3, 5, 7]
#         13.1 Import Fraction from fractions
#         13.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average, average, average, average, average are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(average(lst)) → function runs first, then print shows the result
#      print(average(?)) → function runs first, then print shows the result
#      print(average(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average(?)) → function runs first, then print shows the result

"""

"""
Problem 206:
#
# ===== SYNTAX =====
#   def max_of_three()
#   return — exits the function and sends back a value
#   int() — built-in function call
#   len() — built-in function call
#   input() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: != >
#
# ===== EVALUATION ORDER =====
#   max_of_three(): — no arguments needed
#   len(...) != 3: is this True or False? The answer picks which branch
#   len(...) != 3: is this True or False? The answer picks which branch
#   len(...) != 3: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_three() — stored for later
#   2. Run max_of_three()
#         2.1 Assign to numbers
#         2.2 If statement (one branch runs)
#         2.3 Assign to largest
#         2.4 For loop over ?
#         2.5 Run print(largest)
#   3. Assign to numbers
#   4. If statement (one branch runs)
#   5. Assign to largest
#   6. For loop over ?
#       6.1 If statement (one branch runs)
#   7. Run print(largest)
#   8. Assign to numbers
#   9. If statement (one branch runs)
#   10. Assign to largest
#   11. For loop over ?
#       11.1 If statement (one branch runs)
#   12. Run print(largest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_three are just stored
#   Execution starts at the non-def lines below:
#      max_of_three() → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      if checks a condition and runs ONE branch
#      largest = ... (stored in memory)
#      for loop runs its body once per item
#      print(largest)
#      numbers = ... (stored in memory)
#      if checks a condition and runs ONE branch
#      largest = ... (stored in memory)
#      for loop runs its body once per item
#      print(largest)
#
#   Loop: grabs one item from ? each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 207:
#
# ===== SYNTAX =====
#   def max_of_list()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   sorted() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: != >
#
# ===== EVALUATION ORDER =====
#   max_of_list(): — no arguments needed
#   print(max_of_list(?)): the inner max_of_list() executes FIRST, then print() outputs the result
#   print(max_of_list(?)): the inner max_of_list() executes FIRST, then print() outputs the result
#   max_of_list(nums): looks up variable 'nums', then runs the function body
#   print(max_of_list(?)): the inner max_of_list() executes FIRST, then print() outputs the result
#   max_of_list(?): resolves the value, then runs the function body
#   max_of_list(?): resolves the value, then runs the function body
#   max_of_list(?): resolves the value, then runs the function body
#   len(...) != 3: is this True or False? The answer picks which branch
#   number > max_number: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_list() — stored for later
#   2. Run max_of_list()
#       with: 
#         2.1 Import reduce from functools
#         2.2 Return from function
#   3. Define def max_of_list(nums) — stored for later
#   4. Run print(max_of_list(...))
#       calls max_of_list with: nums=[5, 2, 9, 1, 7]
#         4.1 Import reduce from functools
#         4.2 Return from function
#   5. Define def max_of_list(nums) — stored for later
#   6. Run print(max_of_list(...))
#       calls max_of_list with: nums=[3, 8, 1, 6]
#         6.1 Import reduce from functools
#         6.2 Return from function
#   7. Assign to nums
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(max_of_list(...))
#       calls max_of_list with: nums=[4, 1, 8, 6, 3]
#         10.1 Import reduce from functools
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_list, max_of_list, max_of_list are just stored
#   Execution starts at the non-def lines below:
#      max_of_list() → jumps into function, runs body, returns result → printed
#      print(max_of_list(?)) → function runs first, then print shows the result
#      print(max_of_list(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 208:
#
# ===== SYNTAX =====
#   def max_of_list()
#   max() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   max_of_list(): — no arguments needed
#   print(max_of_list(?)): the inner max_of_list() executes FIRST, then print() outputs the result
#   print(max_of_list(?)): the inner max_of_list() executes FIRST, then print() outputs the result
#   max_of_list(nums): looks up variable 'nums', then runs the function body
#   print(max_of_list(?)): the inner max_of_list() executes FIRST, then print() outputs the result
#   max_of_list(?): resolves the value, then runs the function body
#   max_of_list(?): resolves the value, then runs the function body
#   max_of_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_list() — stored for later
#   2. Run max_of_list()
#         2.1 Assign to user
#         2.2 Assign to int_numbers
#         2.3 For loop over user
#         2.4 Assign to max_number
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def max_of_list(nums) — stored for later
#   4. Run print(max_of_list(...))
#         4.1 Assign to user
#         4.2 Assign to int_numbers
#         4.3 For loop over user
#         4.4 Assign to max_number
#         4.5 Assign to result
#         4.6 Return from function
#   5. Define def max_of_list(nums) — stored for later
#   6. Run print(max_of_list(...))
#         6.1 Assign to user
#         6.2 Assign to int_numbers
#         6.3 For loop over user
#         6.4 Assign to max_number
#         6.5 Assign to result
#         6.6 Return from function
#   7. Define def max_of_list() — stored for later
#   8. Define def max_of_list() — stored for later
#   9. Assign to nums
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(max_of_list(...))
#         12.1 Assign to user
#         12.2 Assign to int_numbers
#         12.3 For loop over user
#         12.4 Assign to max_number
#         12.5 Assign to result
#         12.6 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_list, max_of_list, max_of_list, max_of_list, max_of_list are just stored
#   Execution starts at the non-def lines below:
#      max_of_list() → jumps into function, runs body, returns result → printed
#      print(max_of_list(?)) → function runs first, then print shows the result
#      print(max_of_list(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round

"""

"""
Problem 209:
#
# ===== SYNTAX =====
#   def list_words(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(list_words(lst)): the inner list_words() executes FIRST, then print() outputs the result
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   list_words(lst): looks up variable 'lst', then runs the function body
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   list_words(lst): looks up variable 'lst', then runs the function body
#   list_words(?): resolves the value, then runs the function body
#   list_words(?): resolves the value, then runs the function body
#   list_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_words(...))
#       calls list_words with: lst=lst
#         3.1 Return from function
#   4. Define def list_words(lst) — stored for later
#   5. Run print(list_words(...))
#       calls list_words with: lst=['Jonathan', 'horse', 'today']
#         5.1 Return from function
#   6. Define def list_words(lst) — stored for later
#   7. Run print(list_words(...))
#       calls list_words with: lst=['cat', 'dog']
#         7.1 Return from function
#   8. Define def list_words(lst) — stored for later
#   9. Define def list_words(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(list_words(...))
#       calls list_words with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_words, list_words, list_words, list_words, list_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_words(lst)) → function runs first, then print shows the result
#      print(list_words(?)) → function runs first, then print shows the result
#      print(list_words(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 210:
#
# ===== SYNTAX =====
#   def list_words(lst)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   enumerate() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(list_words(lst)): the inner list_words() executes FIRST, then print() outputs the result
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   list_words(lst): looks up variable 'lst', then runs the function body
#   print(list_words(?)): the inner list_words() executes FIRST, then print() outputs the result
#   list_words(lst): looks up variable 'lst', then runs the function body
#   list_words(?): resolves the value, then runs the function body
#   list_words(?): resolves the value, then runs the function body
#   list_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_words(...))
#       calls list_words with: lst=lst
#         3.1 Return from function
#   4. Define def list_words(lst) — stored for later
#   5. Run print(list_words(...))
#       calls list_words with: lst=['Jonathan', 'horse', 'today']
#         5.1 Return from function
#   6. Define def list_words(lst) — stored for later
#   7. Run print(list_words(...))
#       calls list_words with: lst=['hi', 'hello']
#         7.1 Return from function
#   8. Define def list_words(lst) — stored for later
#   9. Define def list_words(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(list_words(...))
#       calls list_words with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_words, list_words, list_words, list_words, list_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_words(lst)) → function runs first, then print shows the result
#      print(list_words(?)) → function runs first, then print shows the result
#      print(list_words(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 211:
#
# ===== SYNTAX =====
#   def length_words(lst)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   enumerate() — built-in function call
#   len_manual() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(length_words(lst)): the inner length_words() executes FIRST, then print() outputs the result
#   print(length_words(?)): the inner length_words() executes FIRST, then print() outputs the result
#   print(length_words(?)): the inner length_words() executes FIRST, then print() outputs the result
#   print(length_words(?)): the inner length_words() executes FIRST, then print() outputs the result
#   length_words(lst): looks up variable 'lst', then runs the function body
#   print(length_words(?)): the inner length_words() executes FIRST, then print() outputs the result
#   length_words(lst): looks up variable 'lst', then runs the function body
#   length_words(?): resolves the value, then runs the function body
#   length_words(?): resolves the value, then runs the function body
#   length_words(?): resolves the value, then runs the function body
#   length_words(?): resolves the value, then runs the function body
#   len_manual(w): looks up variable 'w', then runs the function body
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def length_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(length_words(...))
#       calls length_words with: lst=lst
#         3.1 Return from function
#   4. Define def length_words(lst) — stored for later
#   5. Run print(length_words(...))
#       calls length_words with: lst=['Jonathan', 'chris', 'mountain']
#         5.1 Return from function
#   6. Define def length_words(lst) — stored for later
#   7. Run print(length_words(...))
#       calls length_words with: lst=['cat', 'horse']
#         7.1 Return from function
#   8. Define def length_words(lst) — stored for later
#   9. Define def len_manual(s) — stored for later
#   10. Run print(length_words(...))
#       calls length_words with: lst=['apple', 'pear']
#         10.1 Return from function
#   11. Define def length_words(lst) — stored for later
#   12. Define def length_words(lst) — stored for later
#   13. Assign to lst
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(length_words(...))
#       calls length_words with: lst=[1, 3, 5, 7]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions length_words, length_words, length_words, length_words, len_manual, length_words, length_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(length_words(lst)) → function runs first, then print shows the result
#      print(length_words(?)) → function runs first, then print shows the result
#      print(length_words(?)) → function runs first, then print shows the result
#      print(length_words(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(length_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 212:
#
# ===== SYNTAX =====
#   def intersection()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   intersection(): — no arguments needed
#   print(intersection(?, ?)): the inner intersection() executes FIRST, then print() outputs the result
#   print(intersection(?, ?)): the inner intersection() executes FIRST, then print() outputs the result
#   intersection(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(intersection(?, ?)): the inner intersection() executes FIRST, then print() outputs the result
#   intersection(?, ?): resolves the value, resolves the value, then runs the function body
#   intersection(?, ?): resolves the value, resolves the value, then runs the function body
#   intersection(?, ?): resolves the value, resolves the value, then runs the function body
#   word in lst2: is this True or False? The answer picks which branch
#   word in lst2: is this True or False? The answer picks which branch
#   word in lst2: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def intersection() — stored for later
#   2. Run intersection()
#         2.1 Assign to lst1
#         2.2 Assign to lst2
#         2.3 Assign to common
#         2.4 For loop over lst1
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def intersection(lst1, lst2) — stored for later
#   4. Run print(intersection(...))
#         4.1 Assign to lst1
#         4.2 Assign to lst2
#         4.3 Assign to common
#         4.4 For loop over lst1
#         4.5 Assign to result
#         4.6 Return from function
#   5. Define def intersection(lst1, lst2) — stored for later
#   6. Run print(intersection(...))
#         6.1 Assign to lst1
#         6.2 Assign to lst2
#         6.3 Assign to common
#         6.4 For loop over lst1
#         6.5 Assign to result
#         6.6 Return from function
#   7. Define def intersection() — stored for later
#   8. Define def intersection() — stored for later
#   9. Assign to lst1
#   10. Assign to lst2
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(intersection(...))
#         13.1 Assign to lst1
#         13.2 Assign to lst2
#         13.3 Assign to common
#         13.4 For loop over lst1
#         13.5 Assign to result
#         13.6 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions intersection, intersection, intersection, intersection, intersection are just stored
#   Execution starts at the non-def lines below:
#      intersection() → jumps into function, runs body, returns result → printed
#      print(intersection(?, ?)) → function runs first, then print shows the result
#      print(intersection(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(intersection(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst1 each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 213:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 Run print('* ' * i)
#   2. For loop over range(...)
#       2.1 For loop over range(...)
#       2.2 Run print()
#   3. For loop over range(...)
#       3.1 Run print(' '.join(...))
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. For loop over range(...)
#       6.1 Run print('* ' * i)
#   7. For loop over range(...)
#       7.1 Run print('* ' * i)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 214:
#
# ===== SYNTAX =====
#   comprehension
#   for
#   in
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   ? * i: first get ?, then get i, then compute ? * i
#   str(...) + ' ': first get str(...), then get ' ', then compute str(...) + ' '
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 Run print(? * i)
#   2. For loop over range(...)
#       2.1 For loop over range(...)
#       2.2 Run print()
#   3. For loop over range(...)
#       3.1 Run print(' '.join(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 216:
#
# ===== SYNTAX =====
#   comprehension
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   print(' '.join(map(...))): inner expression resolves first, then print() outputs
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 Run print(' '.join(...))
#   2. For loop over range(...)
#       2.1 For loop over range(...)
#       2.2 Run print()
#   3. For loop over range(...)
#       3.1 Run print(' '.join(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 217:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#
# ===== EXECUTION ORDER =====
#   1. For loop over range(...)
#       1.1 For loop over range(...)
#       1.2 Run print()
#   2. For loop over range(...)
#       2.1 Run print('* ' * i)
#   3. For loop over range(...)
#       3.1 Run print(' '.join(...))
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. For loop over range(...)
#       6.1 For loop over range(...)
#       6.2 Run print()
#   7. For loop over range(...)
#       7.1 For loop over range(...)
#       7.2 Run print()
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 218:
#
# ===== SYNTAX =====
#   def print_checkerboard(size)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   print_checkerboard() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + * % ==
#
# ===== EVALUATION ORDER =====
#   print_checkerboard(size): looks up variable 'size', then runs the function body
#   print_checkerboard(4): gets the literal 4, then runs the function body
#   print_checkerboard(4): gets the literal 4, then runs the function body
#   print_checkerboard(size): looks up variable 'size', then runs the function body
#   print(print_checkerboard(25)): the inner print_checkerboard() executes FIRST, then print() outputs the result
#   print_checkerboard(size): looks up variable 'size', then runs the function body
#   print_checkerboard(size_2): looks up variable 'size_2', then runs the function body
#   print_checkerboard(25): gets the literal 25, then runs the function body
#   i % 2: first get i, then get 2, then compute i % 2
#   ? % 2: first get ?, then get 2, then compute ? % 2
#   '* ' * size: first get '* ', then get size, then compute '* ' * size
#   ' *' * size: first get ' *', then get size, then compute ' *' * size
#   i + j: first get i, then get j, then compute i + j
#   ? % 2: first get ?, then get 2, then compute ? % 2
#   ? % 2: first get ?, then get 2, then compute ? % 2
#   ? % 2: first get ?, then get 2, then compute ? % 2
#   i + j: first get i, then get j, then compute i + j
#   i + j: first get i, then get j, then compute i + j
#   i + j: first get i, then get j, then compute i + j
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_checkerboard(size) — stored for later
#   2. Assign to size
#   3. Run print_checkerboard(size)
#       with: size=size
#         3.1 For loop over range(...)
#   4. Define def print_checkerboard(size) — stored for later
#   5. Run print_checkerboard(4)
#       with: size=4
#         5.1 For loop over range(...)
#   6. Define def print_checkerboard(size) — stored for later
#   7. Run print_checkerboard(4)
#       with: size=4
#         7.1 For loop over range(...)
#   8. Define def solve(enter) — stored for later
#   9. Define def solve(enter) — stored for later
#   10. Assign to size
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(print_checkerboard(...))
#       calls print_checkerboard with: size=25
#         13.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_checkerboard, print_checkerboard, print_checkerboard, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      size = ... (stored in memory)
#      print_checkerboard(size) → jumps into function, runs body, returns result → printed
#      print_checkerboard(4) → jumps into function, runs body, returns result → printed
#      print_checkerboard(4) → jumps into function, runs body, returns result → printed
#      size = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_checkerboard(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 219:
#
# ===== SYNTAX =====
#   def print_floyds_triangle(rows)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   str() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   print_floyds_triangle() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   in
#   while
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   print_floyds_triangle(rows): looks up variable 'rows', then runs the function body
#   print_floyds_triangle(4): gets the literal 4, then runs the function body
#   print_floyds_triangle(4): gets the literal 4, then runs the function body
#   print_floyds_triangle(rows): looks up variable 'rows', then runs the function body
#   print(print_floyds_triangle(25)): the inner print_floyds_triangle() executes FIRST, then print() outputs the result
#   print_floyds_triangle(rows): looks up variable 'rows', then runs the function body
#   print_floyds_triangle(rows_2): looks up variable 'rows_2', then runs the function body
#   print_floyds_triangle(25): gets the literal 25, then runs the function body
#   rows + 1: first get rows, then get 1, then compute rows + 1
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   rows + 1: first get rows, then get 1, then compute rows + 1
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   rows + 1: first get rows, then get 1, then compute rows + 1
#   rows + 1: first get rows, then get 1, then compute rows + 1
#   num + c: first get num, then get c, then compute num + c
#   num + j: first get num, then get j, then compute num + j
#
# ===== EXECUTION ORDER =====
#   1. Define def print_floyds_triangle(rows) — stored for later
#   2. Assign to rows
#   3. Run print_floyds_triangle(rows)
#       with: rows=rows
#         3.1 Assign to num
#         3.2 For loop over range(...)
#   4. Define def print_floyds_triangle(rows) — stored for later
#   5. Run print_floyds_triangle(4)
#       with: rows=4
#         5.1 Assign to num
#         5.2 For loop over range(...)
#   6. Define def print_floyds_triangle(rows) — stored for later
#   7. Run print_floyds_triangle(4)
#       with: rows=4
#         7.1 Assign to num
#         7.2 For loop over range(...)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to rows
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(print_floyds_triangle(...))
#       calls print_floyds_triangle with: rows=25
#         13.1 Assign to num
#         13.2 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_floyds_triangle, print_floyds_triangle, print_floyds_triangle, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      rows = ... (stored in memory)
#      print_floyds_triangle(rows) → jumps into function, runs body, returns result → printed
#      print_floyds_triangle(4) → jumps into function, runs body, returns result → printed
#      print_floyds_triangle(4) → jumps into function, runs body, returns result → printed
#      rows = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_floyds_triangle(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 222:
#
# ===== SYNTAX =====
#   def print_reverse_pyramid(rows)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   str() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_reverse_pyramid() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print_reverse_pyramid(rows): looks up variable 'rows', then runs the function body
#   print_reverse_pyramid(5): gets the literal 5, then runs the function body
#   print_reverse_pyramid(5): gets the literal 5, then runs the function body
#   print_reverse_pyramid(rows): looks up variable 'rows', then runs the function body
#   print(print_reverse_pyramid(25)): the inner print_reverse_pyramid() executes FIRST, then print() outputs the result
#   print_reverse_pyramid(rows): looks up variable 'rows', then runs the function body
#   print_reverse_pyramid(rows_2): looks up variable 'rows_2', then runs the function body
#   print_reverse_pyramid(25): gets the literal 25, then runs the function body
#   print(' '.join(map(...))): inner expression resolves first, then print() outputs
#   print(' '.join(?)): inner expression resolves first, then print() outputs
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def print_reverse_pyramid(rows) — stored for later
#   2. Assign to rows
#   3. Run print_reverse_pyramid(rows)
#       with: rows=rows
#         3.1 For loop over range(...)
#   4. Define def print_reverse_pyramid(rows) — stored for later
#   5. Run print_reverse_pyramid(5)
#       with: rows=5
#         5.1 For loop over range(...)
#   6. Define def print_reverse_pyramid(rows) — stored for later
#   7. Run print_reverse_pyramid(5)
#       with: rows=5
#         7.1 For loop over range(...)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to rows
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(print_reverse_pyramid(...))
#       calls print_reverse_pyramid with: rows=25
#         13.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_reverse_pyramid, print_reverse_pyramid, print_reverse_pyramid, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      rows = ... (stored in memory)
#      print_reverse_pyramid(rows) → jumps into function, runs body, returns result → printed
#      print_reverse_pyramid(5) → jumps into function, runs body, returns result → printed
#      print_reverse_pyramid(5) → jumps into function, runs body, returns result → printed
#      rows = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_reverse_pyramid(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 227:
#
# ===== SYNTAX =====
#   def print_zigzag(rows)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   print_zigzag() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   if/elif/else
#   in
#   operators: + - * // % == and or
#
# ===== EVALUATION ORDER =====
#   print_zigzag(rows): looks up variable 'rows', then runs the function body
#   print_zigzag(5): gets the literal 5, then runs the function body
#   print_zigzag(rows): looks up variable 'rows', then runs the function body
#   print(print_zigzag(25)): the inner print_zigzag() executes FIRST, then print() outputs the result
#   print_zigzag(rows): looks up variable 'rows', then runs the function body
#   print_zigzag(rows_2): looks up variable 'rows_2', then runs the function body
#   print_zigzag(25): gets the literal 25, then runs the function body
#   i % 2: first get i, then get 2, then compute i % 2
#   '*   ' * ?: first get '*   ', then get ?, then compute '*   ' * ?
#   ' * *' * ?: first get ' * *', then get ?, then compute ' * *' * ?
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   ? + 1: first get ?, then get 1, then compute ? + 1
#   i % 2: first get i, then get 2, then compute i % 2
#   j % ?: first get j, then get ?, then compute j % ?
#   i % 2: first get i, then get 2, then compute i % 2
#   j % ?: first get j, then get ?, then compute j % ?
#   rows // 2: first get rows, then get 2, then compute rows // 2
#   rows // 2: first get rows, then get 2, then compute rows // 2
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   i % 2: first get i, then get 2, then compute i % 2
#   j % ?: first get j, then get ?, then compute j % ?
#   i % 2: first get i, then get 2, then compute i % 2
#   j % ?: first get j, then get ?, then compute j % ?
#   i % 2: first get i, then get 2, then compute i % 2
#   j % ?: first get j, then get ?, then compute j % ?
#   i % 2: first get i, then get 2, then compute i % 2
#   j % ?: first get j, then get ?, then compute j % ?
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 1: is this True or False? The answer picks which branch
#   ? == 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 1: is this True or False? The answer picks which branch
#   ? == 1: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 1: is this True or False? The answer picks which branch
#   ? == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_zigzag(rows) — stored for later
#   2. Assign to rows
#   3. Run print_zigzag(rows)
#       with: rows=rows
#         3.1 For loop over range(...)
#   4. Define def print_zigzag(rows) — stored for later
#   5. Run print_zigzag(5)
#       with: rows=5
#         5.1 For loop over range(...)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to rows
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(print_zigzag(...))
#       calls print_zigzag with: rows=25
#         11.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_zigzag, print_zigzag, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      rows = ... (stored in memory)
#      print_zigzag(rows) → jumps into function, runs body, returns result → printed
#      print_zigzag(5) → jumps into function, runs body, returns result → printed
#      rows = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_zigzag(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 228:
#
# ===== SYNTAX =====
#   def print_cross(rows)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   min() — built-in function call
#   abs() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   print_cross() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + - * // == or
#
# ===== EVALUATION ORDER =====
#   print_cross(rows): looks up variable 'rows', then runs the function body
#   print_cross(5): gets the literal 5, then runs the function body
#   print_cross(5): gets the literal 5, then runs the function body
#   print_cross(rows): looks up variable 'rows', then runs the function body
#   print(print_cross(25)): the inner print_cross() executes FIRST, then print() outputs the result
#   print_cross(rows): looks up variable 'rows', then runs the function body
#   print_cross(rows_2): looks up variable 'rows_2', then runs the function body
#   print_cross(25): gets the literal 25, then runs the function body
#   print(''.join(?)): inner expression resolves first, then print() outputs
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? - i: first get ?, then get i, then compute ? - i
#   ? + '*': first get ?, then get '*', then compute ? + '*'
#   ' ' * ?: first get ' ', then get ?, then compute ' ' * ?
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   ' ' * min(...): first get ' ', then get min(...), then compute ' ' * min(...)
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   rows // 2: first get rows, then get 2, then compute rows // 2
#   ? - i: first get ?, then get i, then compute ? - i
#   ? - i: first get ?, then get i, then compute ? - i
#   abs(...) * 2: first get abs(...), then get 2, then compute abs(...) * 2
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   ? - i: first get ?, then get i, then compute ? - i
#   ? - i: first get ?, then get i, then compute ? - i
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   ? - i: first get ?, then get i, then compute ? - i
#   rows - 1: first get rows, then get 1, then compute rows - 1
#   rows // 2: first get rows, then get 2, then compute rows // 2
#   j == i: is this True or False? The answer picks which branch
#   j == ?: is this True or False? The answer picks which branch
#   j == i: is this True or False? The answer picks which branch
#   j == ?: is this True or False? The answer picks which branch
#   j == i: is this True or False? The answer picks which branch
#   j == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_cross(rows) — stored for later
#   2. Assign to rows
#   3. Run print_cross(rows)
#       with: rows=rows
#         3.1 For loop over range(...)
#   4. Define def print_cross(rows) — stored for later
#   5. Run print_cross(5)
#       with: rows=5
#         5.1 For loop over range(...)
#   6. Define def print_cross(rows) — stored for later
#   7. Run print_cross(5)
#       with: rows=5
#         7.1 For loop over range(...)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to rows
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(print_cross(...))
#       calls print_cross with: rows=25
#         13.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_cross, print_cross, print_cross, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      rows = ... (stored in memory)
#      print_cross(rows) → jumps into function, runs body, returns result → printed
#      print_cross(5) → jumps into function, runs body, returns result → printed
#      print_cross(5) → jumps into function, runs body, returns result → printed
#      rows = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_cross(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 230:
#
# ===== SYNTAX =====
#   def count_occurrences(lst)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   Counter() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   print(count_occurrences(lst)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   print(count_occurrences(?)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(lst): looks up variable 'lst', then runs the function body
#   print(count_occurrences(?)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(lst): looks up variable 'lst', then runs the function body
#   count_occurrences(?): resolves the value, then runs the function body
#   count_occurrences(?): resolves the value, then runs the function body
#   count_occurrences(?): resolves the value, then runs the function body
#   count_occurrences(?): resolves the value, then runs the function body
#   result.get(...) + 1: first get result.get(...), then get 1, then compute result.get(...) + 1
#   word in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences(lst) — stored for later
#   2. Assign to lst
#   3. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=lst
#         3.1 Return from function
#   4. Define def count_occurrences(lst) — stored for later
#   5. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=['tom', 'fred', 'tom', 'chris', 'fred']
#         5.1 Return from function
#   6. Define def count_occurrences(lst) — stored for later
#   7. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=['a', 'b', 'a', 'c', 'b']
#         7.1 Return from function
#   8. Define def count_occurrences(lst) — stored for later
#   9. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 2, 1, 3, 1, 2]
#         9.1 Return from function
#   10. Define def count_occurrences(lst) — stored for later
#   11. Define def count_occurrences(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences, count_occurrences, count_occurrences, count_occurrences, count_occurrences, count_occurrences are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(count_occurrences(lst)) → function runs first, then print shows the result
#      print(count_occurrences(?)) → function runs first, then print shows the result
#      print(count_occurrences(?)) → function runs first, then print shows the result
#      print(count_occurrences(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 231:
#
# ===== SYNTAX =====
#   def sum_of_odd()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   sum_of_odd(): — no arguments needed
#   print(sum_of_odd(?)): the inner sum_of_odd() executes FIRST, then print() outputs the result
#   print(sum_of_odd(?)): the inner sum_of_odd() executes FIRST, then print() outputs the result
#   print(sum_of_odd(?)): the inner sum_of_odd() executes FIRST, then print() outputs the result
#   sum_of_odd(nums): looks up variable 'nums', then runs the function body
#   print(sum_of_odd(?)): the inner sum_of_odd() executes FIRST, then print() outputs the result
#   sum_of_odd(?): resolves the value, then runs the function body
#   sum_of_odd(?): resolves the value, then runs the function body
#   sum_of_odd(?): resolves the value, then runs the function body
#   sum_of_odd(?): resolves the value, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 2: first get number, then get 2, then compute number % 2
#   number % 2: first get number, then get 2, then compute number % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   n % 2: first get n, then get 2, then compute n % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_odd() — stored for later
#   2. Run sum_of_odd()
#         2.1 Assign to user
#         2.2 Assign to count
#         2.3 For loop over user
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def sum_of_odd(nums) — stored for later
#   4. Run print(sum_of_odd(...))
#         4.1 Assign to user
#         4.2 Assign to count
#         4.3 For loop over user
#         4.4 Assign to result
#         4.5 Return from function
#   5. Define def sum_of_odd(nums) — stored for later
#   6. Run print(sum_of_odd(...))
#         6.1 Assign to user
#         6.2 Assign to count
#         6.3 For loop over user
#         6.4 Assign to result
#         6.5 Return from function
#   7. Define def sum_of_odd(nums) — stored for later
#   8. Run print(sum_of_odd(...))
#         8.1 Assign to user
#         8.2 Assign to count
#         8.3 For loop over user
#         8.4 Assign to result
#         8.5 Return from function
#   9. Define def sum_of_odd() — stored for later
#   10. Define def sum_of_odd() — stored for later
#   11. Assign to nums
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(sum_of_odd(...))
#         14.1 Assign to user
#         14.2 Assign to count
#         14.3 For loop over user
#         14.4 Assign to result
#         14.5 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_odd, sum_of_odd, sum_of_odd, sum_of_odd, sum_of_odd, sum_of_odd are just stored
#   Execution starts at the non-def lines below:
#      sum_of_odd() → jumps into function, runs body, returns result → printed
#      print(sum_of_odd(?)) → function runs first, then print shows the result
#      print(sum_of_odd(?)) → function runs first, then print shows the result
#      print(sum_of_odd(?)) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_odd(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 232:
#
# ===== SYNTAX =====
#   def remove_vowels()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   filter() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   remove_vowels(): — no arguments needed
#   print(remove_vowels('hello world')): the inner remove_vowels() executes FIRST, then print() outputs the result
#   print(remove_vowels('banana')): the inner remove_vowels() executes FIRST, then print() outputs the result
#   print(remove_vowels('Hello, World!')): the inner remove_vowels() executes FIRST, then print() outputs the result
#   remove_vowels(s): looks up variable 's', then runs the function body
#   print(remove_vowels(10)): the inner remove_vowels() executes FIRST, then print() outputs the result
#   remove_vowels('hello world'): gets the literal 'hello world', then runs the function body
#   remove_vowels('banana'): gets the literal 'banana', then runs the function body
#   remove_vowels('Hello, World!'): gets the literal 'Hello, World!', then runs the function body
#   remove_vowels(10): gets the literal 10, then runs the function body
#   char not in 'aeiou': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_vowels() — stored for later
#   2. Run remove_vowels()
#       with: 
#         2.1 Import re
#         2.2 Return from function
#   3. Define def remove_vowels(s) — stored for later
#   4. Run print(remove_vowels(...))
#       calls remove_vowels with: s='hello world'
#         4.1 Import re
#         4.2 Return from function
#   5. Define def remove_vowels(s) — stored for later
#   6. Run print(remove_vowels(...))
#       calls remove_vowels with: s='banana'
#         6.1 Import re
#         6.2 Return from function
#   7. Define def remove_vowels(s) — stored for later
#   8. Run print(remove_vowels(...))
#       calls remove_vowels with: s='Hello, World!'
#         8.1 Import re
#         8.2 Return from function
#   9. Assign to s
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(remove_vowels(...))
#       calls remove_vowels with: s=10
#         12.1 Import re
#         12.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_vowels, remove_vowels, remove_vowels, remove_vowels are just stored
#   Execution starts at the non-def lines below:
#      remove_vowels() → jumps into function, runs body, returns result → printed
#      print(remove_vowels('hello world')) → function runs first, then print shows the result
#      print(remove_vowels('banana')) → function runs first, then print shows the result
#      print(remove_vowels('Hello, World!')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_vowels(10)) → function runs first, then print shows the result
#
#   Loop: grabs one item from user each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 233:
#
# ===== SYNTAX =====
#   def find_min(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   reduce() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(find_min(lst)): the inner find_min() executes FIRST, then print() outputs the result
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   find_min(lst): looks up variable 'lst', then runs the function body
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   find_min(lst): looks up variable 'lst', then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   number < min_num: is this True or False? The answer picks which branch
#   ? < min_val: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min(lst) — stored for later
#   2. Assign to lst
#   3. Run print(find_min(...))
#       calls find_min with: lst=lst
#         3.1 Return from function
#   4. Define def find_min(lst) — stored for later
#   5. Run print(find_min(...))
#       calls find_min with: lst=[13, 566, 1]
#         5.1 Return from function
#   6. Define def find_min(lst) — stored for later
#   7. Run print(find_min(...))
#       calls find_min with: lst=[5, 2, 9, 1, 7]
#         7.1 Return from function
#   8. Define def find_min(lst) — stored for later
#   9. Run print(find_min(...))
#       calls find_min with: lst=[8, 3, 5, 2]
#         9.1 Return from function
#   10. Define def find_min(lst) — stored for later
#   11. Define def find_min(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(find_min(...))
#       calls find_min with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min, find_min, find_min, find_min, find_min, find_min are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_min(lst)) → function runs first, then print shows the result
#      print(find_min(?)) → function runs first, then print shows the result
#      print(find_min(?)) → function runs first, then print shows the result
#      print(find_min(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 234:
#
# ===== SYNTAX =====
#   def print_square(n)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: * <
#
# ===== EVALUATION ORDER =====
#   print_square(5): gets the literal 5, then runs the function body
#   print_square(5): gets the literal 5, then runs the function body
#   print_square(n): looks up variable 'n', then runs the function body
#   print(print_square(15)): the inner print_square() executes FIRST, then print() outputs the result
#   print_square(15): gets the literal 15, then runs the function body
#   '* ' * 5: first get '* ', then get 5, then compute '* ' * 5
#   '* ' * 5: first get '* ', then get 5, then compute '* ' * 5
#   '* ' * n: first get '* ', then get n, then compute '* ' * n
#   '* ' * 5: first get '* ', then get 5, then compute '* ' * 5
#
# ===== EXECUTION ORDER =====
#   1. Assign to i
#   2. While loop
#   3. Define def print_square(n) — stored for later
#   4. Run print_square(5)
#       with: n=5
#         4.1 Assign to i
#         4.2 While loop
#   5. Define def print_square(n) — stored for later
#   6. Run print_square(5)
#       with: n=5
#         6.1 Assign to i
#         6.2 While loop
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to n
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(print_square(...))
#       calls print_square with: n=15
#         12.1 Assign to i
#         12.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_square, print_square, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      i = ... (stored in memory)
#      while loop repeats until condition is False
#      print_square(5) → jumps into function, runs body, returns result → printed
#      print_square(5) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_square(15)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 236:
#
# ===== SYNTAX =====
#   def decreasing_triangle()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   increasing_triangle() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(increasing_triangle()): inner expression resolves first, then print() outputs
#   decreasing_triangle(): — no arguments needed
#   decreasing_triangle(): — no arguments needed
#   print(increasing_triangle()): inner expression resolves first, then print() outputs
#   print(increasing_triangle()): inner expression resolves first, then print() outputs
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#
# ===== EXECUTION ORDER =====
#   1. Define def decreasing_triangle() — stored for later
#   2. Run print(increasing_triangle(...))
#   3. Define def decreasing_triangle() — stored for later
#   4. Run decreasing_triangle()
#         4.1 For loop over range(...)
#   5. Define def decreasing_triangle() — stored for later
#   6. Run decreasing_triangle()
#         6.1 For loop over range(...)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to n
#   10. For loop over range(...)
#       10.1 For loop over range(...)
#       10.2 Run print()
#   11. Assign to n
#   12. For loop over range(...)
#       12.1 For loop over range(...)
#       12.2 Run print()
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions decreasing_triangle, decreasing_triangle, decreasing_triangle, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print(increasing_triangle(...))
#      decreasing_triangle() → jumps into function, runs body, returns result → printed
#      decreasing_triangle() → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      for loop runs its body once per item
#      n = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 238:
#
# ===== SYNTAX =====
#   def left_triangle()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   increasing_triangle() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   in
#   operators: + - *
#
# ===== EVALUATION ORDER =====
#   print(increasing_triangle()): inner expression resolves first, then print() outputs
#   left_triangle(): — no arguments needed
#   left_triangle(): — no arguments needed
#   print(increasing_triangle()): inner expression resolves first, then print() outputs
#   print(increasing_triangle()): inner expression resolves first, then print() outputs
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   print(?.rjust(n * 2)): inner expression resolves first, then print() outputs
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   '  ' * ?: first get '  ', then get ?, then compute '  ' * ?
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#   n * 2: first get n, then get 2, then compute n * 2
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   n - i: first get n, then get i, then compute n - i
#   '* ' * i: first get '* ', then get i, then compute '* ' * i
#
# ===== EXECUTION ORDER =====
#   1. Define def left_triangle() — stored for later
#   2. Run print(increasing_triangle(...))
#   3. Define def left_triangle() — stored for later
#   4. Run left_triangle()
#         4.1 Assign to n
#         4.2 For loop over range(...)
#   5. Define def left_triangle() — stored for later
#   6. Run left_triangle()
#         6.1 Assign to n
#         6.2 For loop over range(...)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to n
#   10. For loop over range(...)
#       10.1 For loop over range(...)
#   11. For loop over range(...)
#       11.1 For loop over range(...)
#       11.2 Run print()
#   12. Assign to n
#   13. For loop over range(...)
#       13.1 For loop over range(...)
#   14. For loop over range(...)
#       14.1 For loop over range(...)
#       14.2 Run print()
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions left_triangle, left_triangle, left_triangle, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print(increasing_triangle(...))
#      left_triangle() → jumps into function, runs body, returns result → printed
#      left_triangle() → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      for loop runs its body once per item
#      for loop runs its body once per item
#      n = ... (stored in memory)
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 242:
#
# ===== SYNTAX =====
#   def enumerate_lst(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   list() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(enumerate_lst(lst)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def enumerate_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=lst
#         3.1 Return from function
#   4. Define def enumerate_lst(lst) — stored for later
#   5. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=['Jon', 'Chris', 'Nathan']
#         5.1 Return from function
#   6. Define def enumerate_lst(lst) — stored for later
#   7. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=['a', 'b', 'c']
#         7.1 Return from function
#   8. Define def enumerate_lst(lst) — stored for later
#   9. Define def enumerate_lst(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions enumerate_lst, enumerate_lst, enumerate_lst, enumerate_lst, enumerate_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(enumerate_lst(lst)) → function runs first, then print shows the result
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 243:
#
# ===== SYNTAX =====
#   def enumerate_lst(lst, target)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   next() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(enumerate_lst(lst, target)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   print(find_first(?, 'Chris')): the inner find_first() executes FIRST, then print() outputs the result
#   print(find_first(?, 'Chris')): the inner find_first() executes FIRST, then print() outputs the result
#   enumerate_lst(lst, target): looks up variable 'lst', looks up variable 'target', then runs the function body
#   print(enumerate_lst(?, 'goal')): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(lst, target): looks up variable 'lst', looks up variable 'target', then runs the function body
#   find_first(?, 'Chris'): resolves the value, gets the literal 'Chris', then runs the function body
#   find_first(?, 'Chris'): resolves the value, gets the literal 'Chris', then runs the function body
#   enumerate_lst(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   word == target: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def enumerate_lst(lst, target) — stored for later
#   2. Assign to target
#   3. Assign to lst
#   4. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=lst, target=target
#         4.1 Return from function
#   5. Define def find_first(lst, target) — stored for later
#   6. Run print(find_first(...))
#       calls find_first with: lst=['Jon', 'Chris', 'Nathan'], target='Chris'
#         6.1 Return from function
#   7. Define def find_first(lst, target) — stored for later
#   8. Run print(find_first(...))
#       calls find_first with: lst=['Jon', 'Chris', 'Nathan'], target='Chris'
#         8.1 Return from function
#   9. Define def enumerate_lst(lst, target) — stored for later
#   10. Define def enumerate_lst(lst, target) — stored for later
#   11. Assign to lst
#   12. Assign to target
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=[1, 3, 5, 7], target='goal'
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions enumerate_lst, find_first, find_first, enumerate_lst, enumerate_lst are just stored
#   Execution starts at the non-def lines below:
#      target = ... (stored in memory)
#      lst = ... (stored in memory)
#      print(enumerate_lst(lst, target)) → function runs first, then print shows the result
#      print(find_first(?, 'Chris')) → function runs first, then print shows the result
#      print(find_first(?, 'Chris')) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(enumerate_lst(?, 'goal')) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 244:
#
# ===== SYNTAX =====
#   def enumerate_lst(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   enumerate_lst() — built-in function call
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   len(...) + 1: first get len(...), then get 1, then compute len(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def enumerate_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run enumerate_lst(lst)
#       with: lst=lst
#         3.1 For loop over zip(...)
#   4. Define def enumerate_lst(lst) — stored for later
#   5. For loop over enumerate_lst(...)
#       5.1 Run print(s)
#   6. Define def enumerate_lst(lst) — stored for later
#   7. Run enumerate_lst(?)
#       with: lst=['A', 'B', 'C']
#         7.1 For loop over zip(...)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=[1, 3, 5, 7]
#         13.1 For loop over zip(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions enumerate_lst, enumerate_lst, enumerate_lst, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      enumerate_lst(lst) → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      enumerate_lst(?) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate_lst(...) each round

"""

"""
Problem 245:
#
# ===== SYNTAX =====
#   def enumerate_lst(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def enumerate_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run enumerate_lst(lst)
#       with: lst=lst
#         3.1 Assign to result
#         3.2 For loop over enumerate(...)
#         3.3 Assign to result
#         3.4 Return from function
#   4. Define def enumerate_lst(lst) — stored for later
#   5. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=['Jon', 'Chris', 'Nathan']
#         5.1 Assign to result
#         5.2 For loop over enumerate(...)
#         5.3 Assign to result
#         5.4 Return from function
#   6. Define def enumerate_lst(lst) — stored for later
#   7. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=['a', 'b', 'c']
#         7.1 Assign to result
#         7.2 For loop over enumerate(...)
#         7.3 Assign to result
#         7.4 Return from function
#   8. Define def enumerate_lst(lst) — stored for later
#   9. Define def enumerate_lst(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=[1, 3, 5, 7]
#         13.1 Assign to result
#         13.2 For loop over enumerate(...)
#         13.3 Assign to result
#         13.4 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions enumerate_lst, enumerate_lst, enumerate_lst, enumerate_lst, enumerate_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      enumerate_lst(lst) → jumps into function, runs body, returns result → printed
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 246:
#
# ===== SYNTAX =====
#   def enumerate_lst(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   enumerate_lst() — built-in function call
#   (no return — the function prints directly instead of returning)
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   print(enumerate_lst(?)): the inner enumerate_lst() executes FIRST, then print() outputs the result
#   enumerate_lst(?): resolves the value, then runs the function body
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(lst): looks up variable 'lst', then runs the function body
#   enumerate_lst(?): resolves the value, then runs the function body
#   index % 2: first get index, then get 2, then compute index % 2
#   index % 2: first get index, then get 2, then compute index % 2
#   index % 2: first get index, then get 2, then compute index % 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def enumerate_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run enumerate_lst(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def enumerate_lst(lst) — stored for later
#   5. Run enumerate_lst(?)
#       with: lst=['Jon', 'Chris', 'Nathan', 'hot', 'class']
#         5.1 Return from function
#   6. Define def enumerate_lst(lst) — stored for later
#   7. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=['a', 'b', 'c', 'd', 'e']
#         7.1 Return from function
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(enumerate_lst(...))
#       calls enumerate_lst with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions enumerate_lst, enumerate_lst, enumerate_lst, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      enumerate_lst(lst) → jumps into function, runs body, returns result → printed
#      enumerate_lst(?) → jumps into function, runs body, returns result → printed
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(enumerate_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 248:
#
# ===== SYNTAX =====
#   def replace_with_index(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(replace_with_index(lst)): the inner replace_with_index() executes FIRST, then print() outputs the result
#   print(replace_with_index(?)): the inner replace_with_index() executes FIRST, then print() outputs the result
#   print(replace_with_index(?)): the inner replace_with_index() executes FIRST, then print() outputs the result
#   replace_with_index(lst): looks up variable 'lst', then runs the function body
#   print(replace_with_index(?)): the inner replace_with_index() executes FIRST, then print() outputs the result
#   replace_with_index(lst): looks up variable 'lst', then runs the function body
#   replace_with_index(?): resolves the value, then runs the function body
#   replace_with_index(?): resolves the value, then runs the function body
#   replace_with_index(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def replace_with_index(lst) — stored for later
#   2. Assign to lst
#   3. Run print(replace_with_index(...))
#       calls replace_with_index with: lst=lst
#         3.1 Return from function
#   4. Define def replace_with_index(lst) — stored for later
#   5. Run print(replace_with_index(...))
#       calls replace_with_index with: lst=['Jon', 'Chris', 'Nathan', 'Sam', 'Rachel']
#         5.1 Return from function
#   6. Define def replace_with_index(lst) — stored for later
#   7. Run print(replace_with_index(...))
#       calls replace_with_index with: lst=[1, 2, 3]
#         7.1 Return from function
#   8. Define def replace_with_index(lst) — stored for later
#   9. Define def replace_with_index(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(replace_with_index(...))
#       calls replace_with_index with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions replace_with_index, replace_with_index, replace_with_index, replace_with_index, replace_with_index are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(replace_with_index(lst)) → function runs first, then print shows the result
#      print(replace_with_index(?)) → function runs first, then print shows the result
#      print(replace_with_index(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(replace_with_index(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 250:
#
# ===== SYNTAX =====
#   def sum_odd_indexed_elements(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   sum() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   sum_odd_indexed_elements(lst): looks up variable 'lst', then runs the function body
#   print(sum_odd_indexed_elements(?)): the inner sum_odd_indexed_elements() executes FIRST, then print() outputs the result
#   print(sum_odd_indexed_elements(?)): the inner sum_odd_indexed_elements() executes FIRST, then print() outputs the result
#   print(sum_odd_indexed_elements(?)): the inner sum_odd_indexed_elements() executes FIRST, then print() outputs the result
#   sum_odd_indexed_elements(lst): looks up variable 'lst', then runs the function body
#   print(sum_odd_indexed_elements(?)): the inner sum_odd_indexed_elements() executes FIRST, then print() outputs the result
#   sum_odd_indexed_elements(?): resolves the value, then runs the function body
#   sum_odd_indexed_elements(?): resolves the value, then runs the function body
#   sum_odd_indexed_elements(?): resolves the value, then runs the function body
#   sum_odd_indexed_elements(?): resolves the value, then runs the function body
#   index % 2: first get index, then get 2, then compute index % 2
#   i % 2: first get i, then get 2, then compute i % 2
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_odd_indexed_elements(lst) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def sum_odd_indexed_elements(lst) — stored for later
#   6. Run print(sum_odd_indexed_elements(...))
#       calls sum_odd_indexed_elements with: lst=[1, 2, 3, 4, 5, 6]
#         6.1 Return from function
#   7. Define def sum_odd_indexed_elements(lst) — stored for later
#   8. Run print(sum_odd_indexed_elements(...))
#       calls sum_odd_indexed_elements with: lst=[10, 20, 30, 40]
#         8.1 Return from function
#   9. Define def sum_odd_indexed_elements(lst) — stored for later
#   10. Run print(sum_odd_indexed_elements(...))
#       calls sum_odd_indexed_elements with: lst=[2, 4, 6, 8, 10]
#         10.1 Return from function
#   11. Define def sum_odd_indexed_elements(lst) — stored for later
#   12. Define def sum_odd_indexed_elements(lst) — stored for later
#   13. Assign to lst
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(sum_odd_indexed_elements(...))
#       calls sum_odd_indexed_elements with: lst=[1, 3, 5, 7]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_odd_indexed_elements, sum_odd_indexed_elements, sum_odd_indexed_elements, sum_odd_indexed_elements, sum_odd_indexed_elements, sum_odd_indexed_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_odd_indexed_elements(?)) → function runs first, then print shows the result
#      print(sum_odd_indexed_elements(?)) → function runs first, then print shows the result
#      print(sum_odd_indexed_elements(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_odd_indexed_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 251:
#
# ===== SYNTAX =====
#   def pair_elements(lst1, lst2)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   zip() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(pair_elements(lst1, lst2)): the inner pair_elements() executes FIRST, then print() outputs the result
#   print(pair_elements(?, ?)): the inner pair_elements() executes FIRST, then print() outputs the result
#   print(pair_elements(?, ?)): the inner pair_elements() executes FIRST, then print() outputs the result
#   print(pair_elements(?, ?)): the inner pair_elements() executes FIRST, then print() outputs the result
#   pair_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(pair_elements(?, ?)): the inner pair_elements() executes FIRST, then print() outputs the result
#   pair_elements(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   pair_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   pair_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   pair_elements(?, ?): resolves the value, resolves the value, then runs the function body
#   pair_elements(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def pair_elements(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(pair_elements(...))
#       calls pair_elements with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def pair_elements(lst1, lst2) — stored for later
#   6. Run print(pair_elements(...))
#       calls pair_elements with: lst1=['Jon', 'Chris', 'Nathan', 'Sam'], lst2=['A', 'B', 'C', 'D']
#         6.1 Return from function
#   7. Define def pair_elements(lst1, lst2) — stored for later
#   8. Run print(pair_elements(...))
#       calls pair_elements with: lst1=['Jon', 'Chris', 'Nathan', 'Sam'], lst2=['A', 'B', 'C', 'D']
#         8.1 Return from function
#   9. Define def pair_elements(lst1, lst2) — stored for later
#   10. Run print(pair_elements(...))
#       calls pair_elements with: lst1=['Jon', 'Chris', 'Nathan', 'Sam'], lst2=['A', 'B', 'C', 'D']
#         10.1 Return from function
#   11. Define def pair_elements(lst1, lst2) — stored for later
#   12. Define def pair_elements(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(pair_elements(...))
#       calls pair_elements with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions pair_elements, pair_elements, pair_elements, pair_elements, pair_elements, pair_elements are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(pair_elements(lst1, lst2)) → function runs first, then print shows the result
#      print(pair_elements(?, ?)) → function runs first, then print shows the result
#      print(pair_elements(?, ?)) → function runs first, then print shows the result
#      print(pair_elements(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(pair_elements(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 252:
#
# ===== SYNTAX =====
#   def sum_of_2_lst(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(sum_of_2_lst(lst1, lst2)): the inner sum_of_2_lst() executes FIRST, then print() outputs the result
#   print(sum_of_2_lst(?, ?)): the inner sum_of_2_lst() executes FIRST, then print() outputs the result
#   print(sum_of_2_lst(?, ?)): the inner sum_of_2_lst() executes FIRST, then print() outputs the result
#   print(sum_of_2_lst(?, ?)): the inner sum_of_2_lst() executes FIRST, then print() outputs the result
#   sum_of_2_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(sum_of_2_lst(?, ?)): the inner sum_of_2_lst() executes FIRST, then print() outputs the result
#   sum_of_2_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   sum_of_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   sum_of_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   sum_of_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   sum_of_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#   x + y: first get x, then get y, then compute x + y
#   ? + ?: first get ?, then get ?, then compute ? + ?
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_2_lst(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(sum_of_2_lst(...))
#       calls sum_of_2_lst with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def sum_of_2_lst(lst1, lst2) — stored for later
#   6. Run print(sum_of_2_lst(...))
#       calls sum_of_2_lst with: lst1=[2, 4, 6, 8], lst2=[2, 4, 6, 8]
#         6.1 Return from function
#   7. Define def sum_of_2_lst(lst1, lst2) — stored for later
#   8. Run print(sum_of_2_lst(...))
#       calls sum_of_2_lst with: lst1=[2, 4, 6, 8], lst2=[2, 4, 6, 8]
#         8.1 Return from function
#   9. Define def sum_of_2_lst(lst1, lst2) — stored for later
#   10. Run print(sum_of_2_lst(...))
#       calls sum_of_2_lst with: lst1=[2, 4, 6, 8], lst2=[2, 4, 6, 8]
#         10.1 Return from function
#   11. Define def sum_of_2_lst(lst1, lst2) — stored for later
#   12. Define def sum_of_2_lst(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(sum_of_2_lst(...))
#       calls sum_of_2_lst with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_2_lst, sum_of_2_lst, sum_of_2_lst, sum_of_2_lst, sum_of_2_lst, sum_of_2_lst are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(sum_of_2_lst(lst1, lst2)) → function runs first, then print shows the result
#      print(sum_of_2_lst(?, ?)) → function runs first, then print shows the result
#      print(sum_of_2_lst(?, ?)) → function runs first, then print shows the result
#      print(sum_of_2_lst(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_2_lst(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 253:
#
# ===== SYNTAX =====
#   def sort_tuples_by_second(tuples_list)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   itemgetter() — built-in function call
#   list() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   operators: + - >
#
# ===== EVALUATION ORDER =====
#   sort_tuples_by_second(tuples_list): looks up variable 'tuples_list', then runs the function body
#   print(sort_tuples_by_second(?)): the inner sort_tuples_by_second() executes FIRST, then print() outputs the result
#   print(sort_tuples_by_second(?)): the inner sort_tuples_by_second() executes FIRST, then print() outputs the result
#   print(sort_tuples_by_second(?)): the inner sort_tuples_by_second() executes FIRST, then print() outputs the result
#   sort_tuples_by_second(tuples_list): looks up variable 'tuples_list', then runs the function body
#   print(sort_tuples_by_second(?)): the inner sort_tuples_by_second() executes FIRST, then print() outputs the result
#   sort_tuples_by_second(?): resolves the value, then runs the function body
#   sort_tuples_by_second(?): resolves the value, then runs the function body
#   sort_tuples_by_second(?): resolves the value, then runs the function body
#   sort_tuples_by_second(?): resolves the value, then runs the function body
#   ? - 1: first get ?, then get 1, then compute ? - 1
#   len(...) - i: first get len(...), then get i, then compute len(...) - i
#   j + 1: first get j, then get 1, then compute j + 1
#   j + 1: first get j, then get 1, then compute j + 1
#   j + 1: first get j, then get 1, then compute j + 1
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_tuples_by_second(tuples_list) — stored for later
#   2. Assign to tuples_list
#   3. Assign to result
#   4. Run print(result)
#   5. Import itemgetter from operator
#   6. Define def sort_tuples_by_second(tuples_list) — stored for later
#   7. Run print(sort_tuples_by_second(...))
#       calls sort_tuples_by_second with: tuples_list=[(1, 3), (3, 2), (5, 1)]
#         7.1 Return from function
#   8. Define def sort_tuples_by_second(tuples_list) — stored for later
#   9. Run print(sort_tuples_by_second(...))
#       calls sort_tuples_by_second with: tuples_list=[(1, 3), (3, 2), (5, 1)]
#         9.1 Return from function
#   10. Define def sort_tuples_by_second(tuples_list) — stored for later
#   11. Run print(sort_tuples_by_second(...))
#       calls sort_tuples_by_second with: tuples_list=[(1, 3), (3, 2), (5, 1)]
#         11.1 Return from function
#   12. Define def sort_tuples_by_second(tuples_list) — stored for later
#   13. Define def sort_tuples_by_second(tuples_list) — stored for later
#   14. Assign to tuples_list
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(sort_tuples_by_second(...))
#       calls sort_tuples_by_second with: tuples_list=[4, 1, 8, 6, 3]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sort_tuples_by_second, sort_tuples_by_second, sort_tuples_by_second, sort_tuples_by_second, sort_tuples_by_second, sort_tuples_by_second are just stored
#   Execution starts at the non-def lines below:
#      tuples_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_tuples_by_second(?)) → function runs first, then print shows the result
#      print(sort_tuples_by_second(?)) → function runs first, then print shows the result
#      print(sort_tuples_by_second(?)) → function runs first, then print shows the result
#      tuples_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_tuples_by_second(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 254:
#
# ===== SYNTAX =====
#   def dic_lst(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   dict() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(dic_lst(lst1, lst2)): the inner dic_lst() executes FIRST, then print() outputs the result
#   print(dic_lst(?, ?)): the inner dic_lst() executes FIRST, then print() outputs the result
#   print(dic_lst(?, ?)): the inner dic_lst() executes FIRST, then print() outputs the result
#   print(dic_lst(?, ?)): the inner dic_lst() executes FIRST, then print() outputs the result
#   dic_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(dic_lst(?, ?)): the inner dic_lst() executes FIRST, then print() outputs the result
#   dic_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   dic_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   dic_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   dic_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   dic_lst(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def dic_lst(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(dic_lst(...))
#       calls dic_lst with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def dic_lst(lst1, lst2) — stored for later
#   6. Run print(dic_lst(...))
#       calls dic_lst with: lst1=[1, 2, 3, 4], lst2=['Ron', 'break', 'tree', 'son']
#         6.1 Return from function
#   7. Define def dic_lst(lst1, lst2) — stored for later
#   8. Run print(dic_lst(...))
#       calls dic_lst with: lst1=[1, 2, 3, 4], lst2=['Ron', 'break', 'tree', 'son']
#         8.1 Return from function
#   9. Define def dic_lst(lst1, lst2) — stored for later
#   10. Run print(dic_lst(...))
#       calls dic_lst with: lst1=[1, 2, 3, 4], lst2=['Ron', 'break', 'tree', 'son']
#         10.1 Return from function
#   11. Define def dic_lst(lst1, lst2) — stored for later
#   12. Define def dic_lst(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(dic_lst(...))
#       calls dic_lst with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions dic_lst, dic_lst, dic_lst, dic_lst, dic_lst, dic_lst are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(dic_lst(lst1, lst2)) → function runs first, then print shows the result
#      print(dic_lst(?, ?)) → function runs first, then print shows the result
#      print(dic_lst(?, ?)) → function runs first, then print shows the result
#      print(dic_lst(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(dic_lst(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 255:
#
# ===== SYNTAX =====
#   def combine_3_lst(lst1, lst2, lst3)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(combine_3_lst(lst1, lst2, lst3)): the inner combine_3_lst() executes FIRST, then print() outputs the result
#   print(combine_3_lst(?, ?, ?)): the inner combine_3_lst() executes FIRST, then print() outputs the result
#   print(combine_3_lst(?, ?, ?)): the inner combine_3_lst() executes FIRST, then print() outputs the result
#   print(combine_3_lst(?, ?, ?)): the inner combine_3_lst() executes FIRST, then print() outputs the result
#   combine_3_lst(lst1, lst2, lst3): looks up variable 'lst1', looks up variable 'lst2', looks up variable 'lst3', then runs the function body
#   print(combine_3_lst(?, ?, ?)): the inner combine_3_lst() executes FIRST, then print() outputs the result
#   combine_3_lst(lst1, lst2, lst3): looks up variable 'lst1', looks up variable 'lst2', looks up variable 'lst3', then runs the function body
#   combine_3_lst(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   combine_3_lst(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   combine_3_lst(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#   combine_3_lst(?, ?, ?): resolves the value, resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def combine_3_lst(lst1, lst2, lst3) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Assign to lst3
#   5. Run print(combine_3_lst(...))
#       calls combine_3_lst with: lst1=lst1, lst2=lst2, lst3=lst3
#         5.1 Return from function
#   6. Define def combine_3_lst(lst1, lst2, lst3) — stored for later
#   7. Run print(combine_3_lst(...))
#       calls combine_3_lst with: lst1=['Sam', 'Nike', 'system'], lst2=['Sammy', 'Mikey', 'Steven'], lst3=['Son', 'monkey', 'star']
#         7.1 Return from function
#   8. Define def combine_3_lst(lst1, lst2, lst3) — stored for later
#   9. Run print(combine_3_lst(...))
#       calls combine_3_lst with: lst1=['Sam', 'Nike', 'system'], lst2=['Sammy', 'Mikey', 'Steven'], lst3=['Son', 'monkey', 'star']
#         9.1 Return from function
#   10. Define def combine_3_lst(lst1, lst2, lst3) — stored for later
#   11. Run print(combine_3_lst(...))
#       calls combine_3_lst with: lst1=['Sam', 'Nike', 'system'], lst2=['Sammy', 'Mikey', 'Steven'], lst3=['Son', 'monkey', 'star']
#         11.1 Return from function
#   12. Define def combine_3_lst(lst1, lst2, lst3) — stored for later
#   13. Define def combine_3_lst(lst1, lst2, lst3) — stored for later
#   14. Assign to lst1
#   15. Assign to lst2
#   16. Assign to lst3
#   17. Assign to result
#   18. Run print(result)
#   19. Run print(combine_3_lst(...))
#       calls combine_3_lst with: lst1=[2, 4, 6], lst2=[1, 3, 5], lst3=[4, 1, 8, 6, 3]
#         19.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions combine_3_lst, combine_3_lst, combine_3_lst, combine_3_lst, combine_3_lst, combine_3_lst are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      lst3 = ... (stored in memory)
#      print(combine_3_lst(lst1, lst2, lst3)) → function runs first, then print shows the result
#      print(combine_3_lst(?, ?, ?)) → function runs first, then print shows the result
#      print(combine_3_lst(?, ?, ?)) → function runs first, then print shows the result
#      print(combine_3_lst(?, ?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      lst3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(combine_3_lst(?, ?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 256:
#
# ===== SYNTAX =====
#   def transpose_matrix(matrix)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(transpose_matrix(matrix)): the inner transpose_matrix() executes FIRST, then print() outputs the result
#   print(transpose_matrix(matrix)): the inner transpose_matrix() executes FIRST, then print() outputs the result
#   print(transpose_matrix(matrix)): the inner transpose_matrix() executes FIRST, then print() outputs the result
#   print(transpose_matrix(matrix)): the inner transpose_matrix() executes FIRST, then print() outputs the result
#   transpose_matrix(matrix): looks up variable 'matrix', then runs the function body
#   print(transpose_matrix(25)): the inner transpose_matrix() executes FIRST, then print() outputs the result
#   transpose_matrix(matrix): looks up variable 'matrix', then runs the function body
#   transpose_matrix(matrix): looks up variable 'matrix', then runs the function body
#   transpose_matrix(matrix): looks up variable 'matrix', then runs the function body
#   transpose_matrix(matrix): looks up variable 'matrix', then runs the function body
#   transpose_matrix(25): gets the literal 25, then runs the function body
#   [0] * rows: first get [0], then get rows, then compute [0] * rows
#
# ===== EXECUTION ORDER =====
#   1. Define def transpose_matrix(matrix) — stored for later
#   2. Assign to matrix
#   3. Run print(transpose_matrix(...))
#       calls transpose_matrix with: matrix=matrix
#         3.1 Return from function
#   4. Define def transpose_matrix(matrix) — stored for later
#   5. Assign to matrix
#   6. Run print(transpose_matrix(...))
#       calls transpose_matrix with: matrix=matrix
#         6.1 Return from function
#   7. Define def transpose_matrix(matrix) — stored for later
#   8. Assign to matrix
#   9. Run print(transpose_matrix(...))
#       calls transpose_matrix with: matrix=matrix
#         9.1 Return from function
#   10. Define def transpose_matrix(matrix) — stored for later
#   11. Assign to matrix
#   12. Run print(transpose_matrix(...))
#       calls transpose_matrix with: matrix=matrix
#         12.1 Return from function
#   13. Define def transpose_matrix(matrix) — stored for later
#   14. Define def transpose_matrix(matrix) — stored for later
#   15. Assign to matrix
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(transpose_matrix(...))
#       calls transpose_matrix with: matrix=25
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions transpose_matrix, transpose_matrix, transpose_matrix, transpose_matrix, transpose_matrix, transpose_matrix are just stored
#   Execution starts at the non-def lines below:
#      matrix = ... (stored in memory)
#      print(transpose_matrix(matrix)) → function runs first, then print shows the result
#      matrix = ... (stored in memory)
#      print(transpose_matrix(matrix)) → function runs first, then print shows the result
#      matrix = ... (stored in memory)
#      print(transpose_matrix(matrix)) → function runs first, then print shows the result
#      matrix = ... (stored in memory)
#      print(transpose_matrix(matrix)) → function runs first, then print shows the result
#      matrix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(transpose_matrix(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from transposed each round

"""

"""
Problem 257:
#
# ===== SYNTAX =====
#   def list_tuples(lst)
#   zip() — built-in function call
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   map() — built-in function call
#   enumerate() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(list_tuples(lst)): the inner list_tuples() executes FIRST, then print() outputs the result
#   print(list_tuples(?)): the inner list_tuples() executes FIRST, then print() outputs the result
#   print(list_tuples(?)): the inner list_tuples() executes FIRST, then print() outputs the result
#   print(list_tuples(?)): the inner list_tuples() executes FIRST, then print() outputs the result
#   list_tuples(lst): looks up variable 'lst', then runs the function body
#   print(list_tuples(?)): the inner list_tuples() executes FIRST, then print() outputs the result
#   list_tuples(lst): looks up variable 'lst', then runs the function body
#   list_tuples(?): resolves the value, then runs the function body
#   list_tuples(?): resolves the value, then runs the function body
#   list_tuples(?): resolves the value, then runs the function body
#   list_tuples(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_tuples(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_tuples(...))
#       calls list_tuples with: lst=lst
#         3.1 Assign to result
#         3.2 For loop over lst
#         3.3 Return from function
#   4. Define def list_tuples(lst) — stored for later
#   5. Run print(list_tuples(...))
#       calls list_tuples with: lst=[(1, 2, 3), (4, 5, 6), (7, 8, 9)]
#         5.1 Assign to result
#         5.2 For loop over lst
#         5.3 Return from function
#   6. Define def list_tuples(lst) — stored for later
#   7. Run print(list_tuples(...))
#       calls list_tuples with: lst=[(1, 2, 3), (4, 5, 6), (7, 8, 9)]
#         7.1 Assign to result
#         7.2 For loop over lst
#         7.3 Return from function
#   8. Define def list_tuples(lst) — stored for later
#   9. Run print(list_tuples(...))
#       calls list_tuples with: lst=[(1, 2, 3), (4, 5, 6), (7, 8, 9)]
#         9.1 Assign to result
#         9.2 For loop over lst
#         9.3 Return from function
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(list_tuples(...))
#       calls list_tuples with: lst=[1, 3, 5, 7]
#         13.1 Assign to result
#         13.2 For loop over lst
#         13.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_tuples, list_tuples, list_tuples, list_tuples are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_tuples(lst)) → function runs first, then print shows the result
#      print(list_tuples(?)) → function runs first, then print shows the result
#      print(list_tuples(?)) → function runs first, then print shows the result
#      print(list_tuples(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_tuples(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from unzipped each round

"""

"""
Problem 258:
#
# ===== SYNTAX =====
#   def individualists(lst)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   len() — built-in function call
#   enumerate() — built-in function call
#   range() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(individualists(lst)): the inner individualists() executes FIRST, then print() outputs the result
#   print(individualists(?)): the inner individualists() executes FIRST, then print() outputs the result
#   print(individualists(?)): the inner individualists() executes FIRST, then print() outputs the result
#   print(individualists(?)): the inner individualists() executes FIRST, then print() outputs the result
#   individualists(lst): looks up variable 'lst', then runs the function body
#   print(individualists(?)): the inner individualists() executes FIRST, then print() outputs the result
#   individualists(lst): looks up variable 'lst', then runs the function body
#   individualists(?): resolves the value, then runs the function body
#   individualists(?): resolves the value, then runs the function body
#   individualists(?): resolves the value, then runs the function body
#   individualists(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def individualists(lst) — stored for later
#   2. Assign to lst
#   3. Run print(individualists(...))
#       calls individualists with: lst=lst
#         3.1 Return from function
#   4. Define def individualists(lst) — stored for later
#   5. Run print(individualists(...))
#       calls individualists with: lst=[(1, 2, 3), (4, 5, 6)]
#         5.1 Return from function
#   6. Define def individualists(lst) — stored for later
#   7. Run print(individualists(...))
#       calls individualists with: lst=[(1, 2, 3), (4, 5, 6)]
#         7.1 Return from function
#   8. Define def individualists(lst) — stored for later
#   9. Run print(individualists(...))
#       calls individualists with: lst=[(1, 2, 3), (4, 5, 6)]
#         9.1 Return from function
#   10. Define def individualists(lst) — stored for later
#   11. Define def individualists(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(individualists(...))
#       calls individualists with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions individualists, individualists, individualists, individualists, individualists, individualists are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(individualists(lst)) → function runs first, then print shows the result
#      print(individualists(?)) → function runs first, then print shows the result
#      print(individualists(?)) → function runs first, then print shows the result
#      print(individualists(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(individualists(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 259:
#
# ===== SYNTAX =====
#   def maxium_element_wise(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   max() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(maxium_element_wise(lst1, lst2)): the inner maxium_element_wise() executes FIRST, then print() outputs the result
#   print(maxium_element_wise(?, ?)): the inner maxium_element_wise() executes FIRST, then print() outputs the result
#   print(maxium_element_wise(?, ?)): the inner maxium_element_wise() executes FIRST, then print() outputs the result
#   print(maxium_element_wise(?, ?)): the inner maxium_element_wise() executes FIRST, then print() outputs the result
#   maxium_element_wise(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(maxium_element_wise(?, ?)): the inner maxium_element_wise() executes FIRST, then print() outputs the result
#   maxium_element_wise(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   maxium_element_wise(?, ?): resolves the value, resolves the value, then runs the function body
#   maxium_element_wise(?, ?): resolves the value, resolves the value, then runs the function body
#   maxium_element_wise(?, ?): resolves the value, resolves the value, then runs the function body
#   maxium_element_wise(?, ?): resolves the value, resolves the value, then runs the function body
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def maxium_element_wise(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(maxium_element_wise(...))
#       calls maxium_element_wise with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def maxium_element_wise(lst1, lst2) — stored for later
#   6. Run print(maxium_element_wise(...))
#       calls maxium_element_wise with: lst1=[2, 4, 6], lst2=[7, 8, 9]
#         6.1 Return from function
#   7. Define def maxium_element_wise(lst1, lst2) — stored for later
#   8. Run print(maxium_element_wise(...))
#       calls maxium_element_wise with: lst1=[2, 4, 6], lst2=[7, 8, 9]
#         8.1 Return from function
#   9. Define def maxium_element_wise(lst1, lst2) — stored for later
#   10. Run print(maxium_element_wise(...))
#       calls maxium_element_wise with: lst1=[2, 4, 6], lst2=[7, 8, 9]
#         10.1 Return from function
#   11. Define def maxium_element_wise(lst1, lst2) — stored for later
#   12. Define def maxium_element_wise(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(maxium_element_wise(...))
#       calls maxium_element_wise with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions maxium_element_wise, maxium_element_wise, maxium_element_wise, maxium_element_wise, maxium_element_wise, maxium_element_wise are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(maxium_element_wise(lst1, lst2)) → function runs first, then print shows the result
#      print(maxium_element_wise(?, ?)) → function runs first, then print shows the result
#      print(maxium_element_wise(?, ?)) → function runs first, then print shows the result
#      print(maxium_element_wise(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(maxium_element_wise(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 260:
#
# ===== SYNTAX =====
#   def concatenate(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(concatenate(lst1, lst2)): the inner concatenate() executes FIRST, then print() outputs the result
#   print(concatenate(?, ?)): the inner concatenate() executes FIRST, then print() outputs the result
#   print(concatenate(?, ?)): the inner concatenate() executes FIRST, then print() outputs the result
#   print(concatenate(?, ?)): the inner concatenate() executes FIRST, then print() outputs the result
#   concatenate(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(concatenate(?, ?)): the inner concatenate() executes FIRST, then print() outputs the result
#   concatenate(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   concatenate(?, ?): resolves the value, resolves the value, then runs the function body
#   concatenate(?, ?): resolves the value, resolves the value, then runs the function body
#   concatenate(?, ?): resolves the value, resolves the value, then runs the function body
#   concatenate(?, ?): resolves the value, resolves the value, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ' ': first get ?, then get ' ', then compute ? + ' '
#
# ===== EXECUTION ORDER =====
#   1. Define def concatenate(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(concatenate(...))
#       calls concatenate with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def concatenate(lst1, lst2) — stored for later
#   6. Run print(concatenate(...))
#       calls concatenate with: lst1=['fruite', 'apple', 'tomato'], lst2=['orange', 'banana', 'carrot']
#         6.1 Return from function
#   7. Define def concatenate(lst1, lst2) — stored for later
#   8. Run print(concatenate(...))
#       calls concatenate with: lst1=['fruite', 'apple', 'tomato'], lst2=['orange', 'banana', 'carrot']
#         8.1 Return from function
#   9. Define def concatenate(lst1, lst2) — stored for later
#   10. Run print(concatenate(...))
#       calls concatenate with: lst1=['fruite', 'apple', 'tomato'], lst2=['orange', 'banana', 'carrot']
#         10.1 Return from function
#   11. Define def concatenate(lst1, lst2) — stored for later
#   12. Define def concatenate(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(concatenate(...))
#       calls concatenate with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions concatenate, concatenate, concatenate, concatenate, concatenate, concatenate are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(concatenate(lst1, lst2)) → function runs first, then print shows the result
#      print(concatenate(?, ?)) → function runs first, then print shows the result
#      print(concatenate(?, ?)) → function runs first, then print shows the result
#      print(concatenate(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(concatenate(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 261:
#
# ===== SYNTAX =====
#   def combine_to_dict(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(combine_to_dict(lst1, lst2)): the inner combine_to_dict() executes FIRST, then print() outputs the result
#   print(combine_to_dict(?, ?)): the inner combine_to_dict() executes FIRST, then print() outputs the result
#   print(combine_to_dict(?, ?)): the inner combine_to_dict() executes FIRST, then print() outputs the result
#   print(combine_to_dict(?, ?)): the inner combine_to_dict() executes FIRST, then print() outputs the result
#   combine_to_dict(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(combine_to_dict(?, ?)): the inner combine_to_dict() executes FIRST, then print() outputs the result
#   combine_to_dict(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   combine_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#   combine_to_dict(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def combine_to_dict(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(combine_to_dict(...))
#       calls combine_to_dict with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def combine_to_dict(lst1, lst2) — stored for later
#   6. Run print(combine_to_dict(...))
#       calls combine_to_dict with: lst1=[1, 2, 3], lst2=['one', 'two', 'three']
#         6.1 Return from function
#   7. Define def combine_to_dict(lst1, lst2) — stored for later
#   8. Run print(combine_to_dict(...))
#       calls combine_to_dict with: lst1=[1, 2, 3], lst2=['one', 'two', 'three']
#         8.1 Return from function
#   9. Define def combine_to_dict(lst1, lst2) — stored for later
#   10. Run print(combine_to_dict(...))
#       calls combine_to_dict with: lst1=[1, 2, 3], lst2=['one', 'two', 'three']
#         10.1 Return from function
#   11. Define def combine_to_dict(lst1, lst2) — stored for later
#   12. Define def combine_to_dict(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(combine_to_dict(...))
#       calls combine_to_dict with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions combine_to_dict, combine_to_dict, combine_to_dict, combine_to_dict, combine_to_dict, combine_to_dict are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(combine_to_dict(lst1, lst2)) → function runs first, then print shows the result
#      print(combine_to_dict(?, ?)) → function runs first, then print shows the result
#      print(combine_to_dict(?, ?)) → function runs first, then print shows the result
#      print(combine_to_dict(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(combine_to_dict(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 262:
#
# ===== SYNTAX =====
#   def filter_pairs(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(filter_pairs(lst1, lst2)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   filter_pairs(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   filter_pairs(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   x > y: is this True or False? The answer picks which branch
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_pairs(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def filter_pairs(lst1, lst2) — stored for later
#   6. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#         6.1 Return from function
#   7. Define def filter_pairs(lst1, lst2) — stored for later
#   8. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#         8.1 Return from function
#   9. Define def filter_pairs(lst1, lst2) — stored for later
#   10. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#         10.1 Return from function
#   11. Define def filter_pairs(lst1, lst2) — stored for later
#   12. Define def filter_pairs(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_pairs, filter_pairs, filter_pairs, filter_pairs, filter_pairs, filter_pairs are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(filter_pairs(lst1, lst2)) → function runs first, then print shows the result
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 263:
#
# ===== SYNTAX =====
#   def filter_pairs(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(filter_pairs(lst1, lst2)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   filter_pairs(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(filter_pairs(?, ?)): the inner filter_pairs() executes FIRST, then print() outputs the result
#   filter_pairs(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   filter_pairs(?, ?): resolves the value, resolves the value, then runs the function body
#   x < y: is this True or False? The answer picks which branch
#   ? < ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_pairs(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def filter_pairs(lst1, lst2) — stored for later
#   6. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#         6.1 Return from function
#   7. Define def filter_pairs(lst1, lst2) — stored for later
#   8. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#         8.1 Return from function
#   9. Define def filter_pairs(lst1, lst2) — stored for later
#   10. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[5, 8, 3, 10], lst2=[4, 7, 6, 2]
#         10.1 Return from function
#   11. Define def filter_pairs(lst1, lst2) — stored for later
#   12. Define def filter_pairs(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(filter_pairs(...))
#       calls filter_pairs with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_pairs, filter_pairs, filter_pairs, filter_pairs, filter_pairs, filter_pairs are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(filter_pairs(lst1, lst2)) → function runs first, then print shows the result
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_pairs(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 264:
#
# ===== SYNTAX =====
#   def add_nested_lists(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(add_nested_lists(lst1, lst2)): the inner add_nested_lists() executes FIRST, then print() outputs the result
#   print(add_nested_lists(?, ?)): the inner add_nested_lists() executes FIRST, then print() outputs the result
#   print(add_nested_lists(?, ?)): the inner add_nested_lists() executes FIRST, then print() outputs the result
#   print(add_nested_lists(?, ?)): the inner add_nested_lists() executes FIRST, then print() outputs the result
#   add_nested_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(add_nested_lists(?, ?)): the inner add_nested_lists() executes FIRST, then print() outputs the result
#   add_nested_lists(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   add_nested_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   add_nested_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   add_nested_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   add_nested_lists(?, ?): resolves the value, resolves the value, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   x + y: first get x, then get y, then compute x + y
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Define def add_nested_lists(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(add_nested_lists(...))
#       calls add_nested_lists with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def add_nested_lists(lst1, lst2) — stored for later
#   6. Run print(add_nested_lists(...))
#       calls add_nested_lists with: lst1=[[1, 2, 3], [4, 5, 6]], lst2=[[7, 8, 9], [10, 11, 12]]
#         6.1 Return from function
#   7. Define def add_nested_lists(lst1, lst2) — stored for later
#   8. Run print(add_nested_lists(...))
#       calls add_nested_lists with: lst1=[[1, 2, 3], [4, 5, 6]], lst2=[[7, 8, 9], [10, 11, 12]]
#         8.1 Return from function
#   9. Define def add_nested_lists(lst1, lst2) — stored for later
#   10. Run print(add_nested_lists(...))
#       calls add_nested_lists with: lst1=[[1, 2, 3], [4, 5, 6]], lst2=[[7, 8, 9], [10, 11, 12]]
#         10.1 Return from function
#   11. Define def add_nested_lists(lst1, lst2) — stored for later
#   12. Define def add_nested_lists(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(add_nested_lists(...))
#       calls add_nested_lists with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_nested_lists, add_nested_lists, add_nested_lists, add_nested_lists, add_nested_lists, add_nested_lists are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(add_nested_lists(lst1, lst2)) → function runs first, then print shows the result
#      print(add_nested_lists(?, ?)) → function runs first, then print shows the result
#      print(add_nested_lists(?, ?)) → function runs first, then print shows the result
#      print(add_nested_lists(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_nested_lists(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 265:
#
# ===== SYNTAX =====
#   def euclidean_distance(point1, point2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#   operators: - **
#
# ===== EVALUATION ORDER =====
#   print(euclidean_distance(point1, point2)): the inner euclidean_distance() executes FIRST, then print() outputs the result
#   print(euclidean_distance(?, ?)): the inner euclidean_distance() executes FIRST, then print() outputs the result
#   print(euclidean_distance(?, ?)): the inner euclidean_distance() executes FIRST, then print() outputs the result
#   print(euclidean_distance(?, ?)): the inner euclidean_distance() executes FIRST, then print() outputs the result
#   euclidean_distance(point1, point2): looks up variable 'point1', looks up variable 'point2', then runs the function body
#   print(euclidean_distance(25, 25)): the inner euclidean_distance() executes FIRST, then print() outputs the result
#   euclidean_distance(point1, point2): looks up variable 'point1', looks up variable 'point2', then runs the function body
#   euclidean_distance(?, ?): resolves the value, resolves the value, then runs the function body
#   euclidean_distance(?, ?): resolves the value, resolves the value, then runs the function body
#   sum(...) ** 0.5: first get sum(...), then get 0.5, then compute sum(...) ** 0.5
#   euclidean_distance(?, ?): resolves the value, resolves the value, then runs the function body
#   euclidean_distance(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   x1 - x2: first get x1, then get x2, then compute x1 - x2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   x1 - x2: first get x1, then get x2, then compute x1 - x2
#   a - b: first get a, then get b, then compute a - b
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def euclidean_distance(point1, point2) — stored for later
#   3. Assign to point1
#   4. Assign to point2
#   5. Run print(euclidean_distance(...))
#       calls euclidean_distance with: point1=point1, point2=point2
#         5.1 Return from function
#   6. Import math
#   7. Define def euclidean_distance(point1, point2) — stored for later
#   8. Run print(euclidean_distance(...))
#       calls euclidean_distance with: point1=(3, 4), point2=(6, 8)
#         8.1 Return from function
#   9. Import math
#   10. Define def euclidean_distance(point1, point2) — stored for later
#   11. Run print(euclidean_distance(...))
#       calls euclidean_distance with: point1=(3, 4), point2=(6, 8)
#         11.1 Return from function
#   12. Define def euclidean_distance(point1, point2) — stored for later
#   13. Run print(euclidean_distance(...))
#       calls euclidean_distance with: point1=(3, 4), point2=(6, 8)
#         13.1 Return from function
#   14. Define def euclidean_distance(point1, point2) — stored for later
#   15. Define def euclidean_distance(point1, point2) — stored for later
#   16. Assign to point1
#   17. Assign to point2
#   18. Assign to result
#   19. Run print(result)
#   20. Run print(euclidean_distance(...))
#       calls euclidean_distance with: point1=25, point2=25
#         20.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions euclidean_distance, euclidean_distance, euclidean_distance, euclidean_distance, euclidean_distance, euclidean_distance are just stored
#   Execution starts at the non-def lines below:
#      point1 = ... (stored in memory)
#      point2 = ... (stored in memory)
#      print(euclidean_distance(point1, point2)) → function runs first, then print shows the result
#      print(euclidean_distance(?, ?)) → function runs first, then print shows the result
#      print(euclidean_distance(?, ?)) → function runs first, then print shows the result
#      print(euclidean_distance(?, ?)) → function runs first, then print shows the result
#      point1 = ... (stored in memory)
#      point2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(euclidean_distance(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 266:
#
# ===== SYNTAX =====
#   def interleave(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(interleave(lst1, lst2)): the inner interleave() executes FIRST, then print() outputs the result
#   print(interleave(?, ?)): the inner interleave() executes FIRST, then print() outputs the result
#   print(interleave(?, ?)): the inner interleave() executes FIRST, then print() outputs the result
#   print(interleave(?, ?)): the inner interleave() executes FIRST, then print() outputs the result
#   interleave(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(interleave(?, ?)): the inner interleave() executes FIRST, then print() outputs the result
#   interleave(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   interleave(?, ?): resolves the value, resolves the value, then runs the function body
#   interleave(?, ?): resolves the value, resolves the value, then runs the function body
#   interleave(?, ?): resolves the value, resolves the value, then runs the function body
#   interleave(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def interleave(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(interleave(...))
#       calls interleave with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def interleave(lst1, lst2) — stored for later
#   6. Run print(interleave(...))
#       calls interleave with: lst1=['crissy', 'missy', 'sissy'], lst2=['spotty', 'motty', 'hotty']
#         6.1 Return from function
#   7. Define def interleave(lst1, lst2) — stored for later
#   8. Run print(interleave(...))
#       calls interleave with: lst1=['crissy', 'missy', 'sissy'], lst2=['spotty', 'motty', 'hotty']
#         8.1 Return from function
#   9. Import chain from itertools
#   10. Define def interleave(lst1, lst2) — stored for later
#   11. Run print(interleave(...))
#       calls interleave with: lst1=['crissy', 'missy', 'sissy'], lst2=['spotty', 'motty', 'hotty']
#         11.1 Return from function
#   12. Define def interleave(lst1, lst2) — stored for later
#   13. Define def interleave(lst1, lst2) — stored for later
#   14. Assign to lst1
#   15. Assign to lst2
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(interleave(...))
#       calls interleave with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions interleave, interleave, interleave, interleave, interleave, interleave are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(interleave(lst1, lst2)) → function runs first, then print shows the result
#      print(interleave(?, ?)) → function runs first, then print shows the result
#      print(interleave(?, ?)) → function runs first, then print shows the result
#      print(interleave(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(interleave(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 267:
#
# ===== SYNTAX =====
#   def multiply_2_lst(lst1, lst2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(multiply_2_lst(lst1, lst2)): the inner multiply_2_lst() executes FIRST, then print() outputs the result
#   print(multiply_2_lst(?, ?)): the inner multiply_2_lst() executes FIRST, then print() outputs the result
#   print(multiply_2_lst(?, ?)): the inner multiply_2_lst() executes FIRST, then print() outputs the result
#   print(multiply_2_lst(?, ?)): the inner multiply_2_lst() executes FIRST, then print() outputs the result
#   multiply_2_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   print(multiply_2_lst(?, ?)): the inner multiply_2_lst() executes FIRST, then print() outputs the result
#   multiply_2_lst(lst1, lst2): looks up variable 'lst1', looks up variable 'lst2', then runs the function body
#   multiply_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   multiply_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   multiply_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   multiply_2_lst(?, ?): resolves the value, resolves the value, then runs the function body
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   a * b: first get a, then get b, then compute a * b
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_2_lst(lst1, lst2) — stored for later
#   2. Assign to lst1
#   3. Assign to lst2
#   4. Run print(multiply_2_lst(...))
#       calls multiply_2_lst with: lst1=lst1, lst2=lst2
#         4.1 Return from function
#   5. Define def multiply_2_lst(lst1, lst2) — stored for later
#   6. Run print(multiply_2_lst(...))
#       calls multiply_2_lst with: lst1=[2, 4, 6], lst2=[3, 6, 9]
#         6.1 Return from function
#   7. Define def multiply_2_lst(lst1, lst2) — stored for later
#   8. Run print(multiply_2_lst(...))
#       calls multiply_2_lst with: lst1=[2, 4, 6], lst2=[3, 6, 9]
#         8.1 Return from function
#   9. Define def multiply_2_lst(lst1, lst2) — stored for later
#   10. Run print(multiply_2_lst(...))
#       calls multiply_2_lst with: lst1=[2, 4, 6], lst2=[3, 6, 9]
#         10.1 Return from function
#   11. Define def multiply_2_lst(lst1, lst2) — stored for later
#   12. Define def multiply_2_lst(lst1, lst2) — stored for later
#   13. Assign to lst1
#   14. Assign to lst2
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(multiply_2_lst(...))
#       calls multiply_2_lst with: lst1=[2, 4, 6], lst2=[1, 3, 5]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_2_lst, multiply_2_lst, multiply_2_lst, multiply_2_lst, multiply_2_lst, multiply_2_lst are just stored
#   Execution starts at the non-def lines below:
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      print(multiply_2_lst(lst1, lst2)) → function runs first, then print shows the result
#      print(multiply_2_lst(?, ?)) → function runs first, then print shows the result
#      print(multiply_2_lst(?, ?)) → function runs first, then print shows the result
#      print(multiply_2_lst(?, ?)) → function runs first, then print shows the result
#      lst1 = ... (stored in memory)
#      lst2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_2_lst(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 268:
#
# ===== SYNTAX =====
#   def dot_product(vector1, vector2)
#   return — exits the function and sends back a value
#   zip() — built-in function call
#   sum() — built-in function call
#   map() — built-in function call
#   reduce() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print(dot_product(vector1, vector2)): the inner dot_product() executes FIRST, then print() outputs the result
#   print(dot_product(?, ?)): the inner dot_product() executes FIRST, then print() outputs the result
#   print(dot_product(?, ?)): the inner dot_product() executes FIRST, then print() outputs the result
#   print(dot_product(?, ?)): the inner dot_product() executes FIRST, then print() outputs the result
#   dot_product(vector1, vector2): looks up variable 'vector1', looks up variable 'vector2', then runs the function body
#   print(dot_product(25, 25)): the inner dot_product() executes FIRST, then print() outputs the result
#   dot_product(vector1, vector2): looks up variable 'vector1', looks up variable 'vector2', then runs the function body
#   dot_product(?, ?): resolves the value, resolves the value, then runs the function body
#   dot_product(?, ?): resolves the value, resolves the value, then runs the function body
#   dot_product(?, ?): resolves the value, resolves the value, then runs the function body
#   dot_product(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   acc + ?: first get acc, then get ?, then compute acc + ?
#   a * b: first get a, then get b, then compute a * b
#   ? * ?: first get ?, then get ?, then compute ? * ?
#
# ===== EXECUTION ORDER =====
#   1. Define def dot_product(vector1, vector2) — stored for later
#   2. Assign to vector1
#   3. Assign to vector2
#   4. Run print(dot_product(...))
#       calls dot_product with: vector1=vector1, vector2=vector2
#         4.1 Return from function
#   5. Define def dot_product(vector1, vector2) — stored for later
#   6. Run print(dot_product(...))
#       calls dot_product with: vector1=[1, 2, 3], vector2=[4, 5, 6]
#         6.1 Return from function
#   7. Define def dot_product(vector1, vector2) — stored for later
#   8. Run print(dot_product(...))
#       calls dot_product with: vector1=[1, 2, 3], vector2=[4, 5, 6]
#         8.1 Return from function
#   9. Import reduce from functools
#   10. Define def dot_product(vector1, vector2) — stored for later
#   11. Run print(dot_product(...))
#       calls dot_product with: vector1=[1, 2, 3], vector2=[4, 5, 6]
#         11.1 Return from function
#   12. Define def dot_product(vector1, vector2) — stored for later
#   13. Define def dot_product(vector1, vector2) — stored for later
#   14. Assign to vector1
#   15. Assign to vector2
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(dot_product(...))
#       calls dot_product with: vector1=25, vector2=25
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions dot_product, dot_product, dot_product, dot_product, dot_product, dot_product are just stored
#   Execution starts at the non-def lines below:
#      vector1 = ... (stored in memory)
#      vector2 = ... (stored in memory)
#      print(dot_product(vector1, vector2)) → function runs first, then print shows the result
#      print(dot_product(?, ?)) → function runs first, then print shows the result
#      print(dot_product(?, ?)) → function runs first, then print shows the result
#      print(dot_product(?, ?)) → function runs first, then print shows the result
#      vector1 = ... (stored in memory)
#      vector2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(dot_product(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from zip(...) each round

"""

"""
Problem 269:
#
# ===== SYNTAX =====
#   def is_odd(x)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   filter_even_numbers(lst): looks up variable 'lst', then runs the function body
#   print(filter_even_numbers(?)): the inner filter_even_numbers() executes FIRST, then print() outputs the result
#   print(filter_even_numbers(?)): the inner filter_even_numbers() executes FIRST, then print() outputs the result
#   print(filter_even_numbers(?)): the inner filter_even_numbers() executes FIRST, then print() outputs the result
#   is_odd(x): looks up variable 'x', then runs the function body
#   print(is_odd(3)): the inner is_odd() executes FIRST, then print() outputs the result
#   filter_even_numbers(?): resolves the value, then runs the function body
#   filter_even_numbers(?): resolves the value, then runs the function body
#   filter_even_numbers(?): resolves the value, then runs the function body
#   is_odd(3): gets the literal 3, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_odd(x) — stored for later
#   2. Define def filter_even_numbers(lst) — stored for later
#   3. Assign to lst
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_even_numbers(lst) — stored for later
#   7. Run print(filter_even_numbers(...))
#       calls filter_even_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         7.1 Assign to result
#         7.2 For loop over lst
#         7.3 Return from function
#   8. Define def filter_even_numbers(lst) — stored for later
#   9. Run print(filter_even_numbers(...))
#       calls filter_even_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         9.1 Assign to result
#         9.2 For loop over lst
#         9.3 Return from function
#   10. Define def filter_even_numbers(lst) — stored for later
#   11. Run print(filter_even_numbers(...))
#       calls filter_even_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         11.1 Assign to result
#         11.2 For loop over lst
#         11.3 Return from function
#   12. Define def is_odd(x) — stored for later
#   13. Define def is_odd(x) — stored for later
#   14. Assign to x
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(is_odd(...))
#       calls is_odd with: x=3
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_odd, filter_even_numbers, filter_even_numbers, filter_even_numbers, filter_even_numbers, is_odd, is_odd are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_even_numbers(?)) → function runs first, then print shows the result
#      print(filter_even_numbers(?)) → function runs first, then print shows the result
#      print(filter_even_numbers(?)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_odd(3)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 270:
#
# ===== SYNTAX =====
#   def is_even(x)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   filter_odd_numbers(lst): looks up variable 'lst', then runs the function body
#   print(filter_odd_numbers(?)): the inner filter_odd_numbers() executes FIRST, then print() outputs the result
#   print(filter_odd_numbers(?)): the inner filter_odd_numbers() executes FIRST, then print() outputs the result
#   print(filter_odd_numbers(?)): the inner filter_odd_numbers() executes FIRST, then print() outputs the result
#   is_even(x): looks up variable 'x', then runs the function body
#   print(is_even(3)): the inner is_even() executes FIRST, then print() outputs the result
#   filter_odd_numbers(?): resolves the value, then runs the function body
#   filter_odd_numbers(?): resolves the value, then runs the function body
#   filter_odd_numbers(?): resolves the value, then runs the function body
#   is_even(3): gets the literal 3, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_even(x) — stored for later
#   2. Define def filter_odd_numbers(lst) — stored for later
#   3. Assign to lst
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_odd_numbers(lst) — stored for later
#   7. Run print(filter_odd_numbers(...))
#       calls filter_odd_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         7.1 Assign to result
#         7.2 For loop over lst
#         7.3 Return from function
#   8. Define def filter_odd_numbers(lst) — stored for later
#   9. Run print(filter_odd_numbers(...))
#       calls filter_odd_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         9.1 Assign to result
#         9.2 For loop over lst
#         9.3 Return from function
#   10. Define def filter_odd_numbers(lst) — stored for later
#   11. Run print(filter_odd_numbers(...))
#       calls filter_odd_numbers with: lst=[1, 2, 3, 4, 5, 6]
#         11.1 Assign to result
#         11.2 For loop over lst
#         11.3 Return from function
#   12. Define def is_even(x) — stored for later
#   13. Define def is_even(x) — stored for later
#   14. Assign to x
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(is_even(...))
#       calls is_even with: x=3
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_even, filter_odd_numbers, filter_odd_numbers, filter_odd_numbers, filter_odd_numbers, is_even, is_even are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_odd_numbers(?)) → function runs first, then print shows the result
#      print(filter_odd_numbers(?)) → function runs first, then print shows the result
#      print(filter_odd_numbers(?)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_even(3)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 272:
#
# ===== SYNTAX =====
#   def is_non_negative(x)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: < >=
#
# ===== EVALUATION ORDER =====
#   filter_negative_numbers(lst): looks up variable 'lst', then runs the function body
#   print(filter_negative_numbers(?)): the inner filter_negative_numbers() executes FIRST, then print() outputs the result
#   print(filter_negative_numbers(?)): the inner filter_negative_numbers() executes FIRST, then print() outputs the result
#   print(filter_negative_numbers(?)): the inner filter_negative_numbers() executes FIRST, then print() outputs the result
#   is_non_negative(x): looks up variable 'x', then runs the function body
#   print(is_non_negative(3)): the inner is_non_negative() executes FIRST, then print() outputs the result
#   filter_negative_numbers(?): resolves the value, then runs the function body
#   filter_negative_numbers(?): resolves the value, then runs the function body
#   filter_negative_numbers(?): resolves the value, then runs the function body
#   is_non_negative(3): gets the literal 3, then runs the function body
#   x >= 0: is this True or False? The answer picks which branch
#   x >= 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_non_negative(x) — stored for later
#   2. Define def filter_negative_numbers(lst) — stored for later
#   3. Assign to lst
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_negative_numbers(lst) — stored for later
#   7. Run print(filter_negative_numbers(...))
#       calls filter_negative_numbers with: lst=[-2, -1, 0, 1, 2, 3]
#         7.1 Assign to result
#         7.2 For loop over lst
#         7.3 Return from function
#   8. Define def filter_negative_numbers(lst) — stored for later
#   9. Run print(filter_negative_numbers(...))
#       calls filter_negative_numbers with: lst=[-2, -1, 0, 1, 2, 3]
#         9.1 Assign to result
#         9.2 For loop over lst
#         9.3 Return from function
#   10. Define def filter_negative_numbers(lst) — stored for later
#   11. Run print(filter_negative_numbers(...))
#       calls filter_negative_numbers with: lst=[-2, -1, 0, 1, 2, 3]
#         11.1 Assign to result
#         11.2 For loop over lst
#         11.3 Return from function
#   12. Define def is_non_negative(x) — stored for later
#   13. Define def is_non_negative(x) — stored for later
#   14. Assign to x
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(is_non_negative(...))
#       calls is_non_negative with: x=3
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_non_negative, filter_negative_numbers, filter_negative_numbers, filter_negative_numbers, filter_negative_numbers, is_non_negative, is_non_negative are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_negative_numbers(?)) → function runs first, then print shows the result
#      print(filter_negative_numbers(?)) → function runs first, then print shows the result
#      print(filter_negative_numbers(?)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_non_negative(3)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 274:
#
# ===== SYNTAX =====
#   def is_palindrome(s)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   is_pal() — built-in function call
#   len() — built-in function call
#   comprehension
#   if/elif/else
#   lambda
#   while
#   operators: - == != <
#
# ===== EVALUATION ORDER =====
#   filter_palindromes(lst): looks up variable 'lst', then runs the function body
#   print(filter_palindromes(?)): the inner filter_palindromes() executes FIRST, then print() outputs the result
#   print(filter_palindromes(?)): the inner filter_palindromes() executes FIRST, then print() outputs the result
#   print(filter_palindromes(?)): the inner filter_palindromes() executes FIRST, then print() outputs the result
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome(?)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   filter_palindromes(?): resolves the value, then runs the function body
#   filter_palindromes(?): resolves the value, then runs the function body
#   filter_palindromes(?): resolves the value, then runs the function body
#   is_palindrome(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   ? != ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(s) — stored for later
#   2. Define def filter_palindromes(lst) — stored for later
#   3. Assign to lst
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_palindromes(lst) — stored for later
#   7. Run print(filter_palindromes(...))
#       calls filter_palindromes with: lst=['madam', 'hello', 'racecar', 'world', 'level']
#         7.1 Define def is_pal(s) — stored for later
#         7.2 Return from function
#   8. Define def filter_palindromes(lst) — stored for later
#   9. Run print(filter_palindromes(...))
#       calls filter_palindromes with: lst=['madam', 'hello', 'racecar', 'world', 'level']
#         9.1 Define def is_pal(s) — stored for later
#         9.2 Return from function
#   10. Define def filter_palindromes(lst) — stored for later
#   11. Run print(filter_palindromes(...))
#       calls filter_palindromes with: lst=['madam', 'hello', 'racecar', 'world', 'level']
#         11.1 Define def is_pal(s) — stored for later
#         11.2 Return from function
#   12. Define def is_palindrome(s) — stored for later
#   13. Define def is_palindrome(s) — stored for later
#   14. Assign to s
#   15. Assign to result
#   16. Run print(result)
#   17. Run print(is_palindrome(...))
#       calls is_palindrome with: s=[4, 1, 8, 6, 3]
#         17.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, filter_palindromes, filter_palindromes, filter_palindromes, filter_palindromes, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_palindromes(?)) → function runs first, then print shows the result
#      print(filter_palindromes(?)) → function runs first, then print shows the result
#      print(filter_palindromes(?)) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome(?)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 276:
#
# ===== SYNTAX =====
#   def filter_divisible(numbers, divisor)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   filter_divisible(numbers, divisor): looks up variable 'numbers', looks up variable 'divisor', then runs the function body
#   print(filter_divisible(?, 5)): the inner filter_divisible() executes FIRST, then print() outputs the result
#   print(filter_divisible(?, 5)): the inner filter_divisible() executes FIRST, then print() outputs the result
#   print(filter_divisible(?, 5)): the inner filter_divisible() executes FIRST, then print() outputs the result
#   filter_divisible(numbers, divisor): looks up variable 'numbers', looks up variable 'divisor', then runs the function body
#   print(filter_divisible(?, 25)): the inner filter_divisible() executes FIRST, then print() outputs the result
#   filter_divisible(?, 5): resolves the value, gets the literal 5, then runs the function body
#   filter_divisible(?, 5): resolves the value, gets the literal 5, then runs the function body
#   filter_divisible(?, 5): resolves the value, gets the literal 5, then runs the function body
#   filter_divisible(?, 25): resolves the value, gets the literal 25, then runs the function body
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_divisible(numbers, divisor) — stored for later
#   2. Assign to numbers
#   3. Assign to divisor
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_divisible(numbers, divisor) — stored for later
#   7. Run print(filter_divisible(...))
#       calls filter_divisible with: numbers=[10, 15, 20, 25, 30, 35, 40], divisor=5
#         7.1 Return from function
#   8. Define def filter_divisible(numbers, divisor) — stored for later
#   9. Run print(filter_divisible(...))
#       calls filter_divisible with: numbers=[10, 15, 20, 25, 30, 35, 40], divisor=5
#         9.1 Return from function
#   10. Define def filter_divisible(numbers, divisor) — stored for later
#   11. Run print(filter_divisible(...))
#       calls filter_divisible with: numbers=[10, 15, 20, 25, 30, 35, 40], divisor=5
#         11.1 Return from function
#   12. Define def filter_divisible(numbers, divisor) — stored for later
#   13. Define def filter_divisible(numbers, divisor) — stored for later
#   14. Assign to numbers
#   15. Assign to divisor
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(filter_divisible(...))
#       calls filter_divisible with: numbers=[4, 1, 8, 6, 3], divisor=25
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_divisible, filter_divisible, filter_divisible, filter_divisible, filter_divisible, filter_divisible are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      divisor = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_divisible(?, 5)) → function runs first, then print shows the result
#      print(filter_divisible(?, 5)) → function runs first, then print shows the result
#      print(filter_divisible(?, 5)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      divisor = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_divisible(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 277:
#
# ===== SYNTAX =====
#   def is_not_divisible(num, divisor)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   is_not_divisible(num, divisor): looks up variable 'num', looks up variable 'divisor', then runs the function body
#   print(is_not_divisible(25, 25)): the inner is_not_divisible() executes FIRST, then print() outputs the result
#   is_not_divisible(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   n % divisor: first get n, then get divisor, then compute n % divisor
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_not_divisible(num, divisor) — stored for later
#   2. Assign to numbers
#   3. Assign to divisor
#   4. Assign to filtered_numbers
#   5. Run print(filtered_numbers)
#   6. Define def is_not_divisible(num, divisor) — stored for later
#   7. Assign to numbers
#   8. Assign to divisor
#   9. Assign to filtered_numbers
#   10. Run print(filtered_numbers)
#   11. Define def is_not_divisible(num, divisor) — stored for later
#   12. Assign to numbers
#   13. Assign to divisor
#   14. Assign to filtered_numbers
#   15. Run print(filtered_numbers)
#   16. Define def is_not_divisible(num, divisor) — stored for later
#   17. Assign to numbers
#   18. Assign to divisor
#   19. Assign to filtered_numbers
#   20. For loop over numbers
#       20.1 If statement (one branch runs)
#   21. Run print(filtered_numbers)
#   22. Define def is_not_divisible(num, divisor) — stored for later
#   23. Define def is_not_divisible(num, divisor) — stored for later
#   24. Assign to num
#   25. Assign to divisor
#   26. Assign to result
#   27. Run print(result)
#   28. Run print(is_not_divisible(...))
#       calls is_not_divisible with: num=25, divisor=25
#         28.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_not_divisible, is_not_divisible, is_not_divisible, is_not_divisible, is_not_divisible, is_not_divisible are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      divisor = ... (stored in memory)
#      filtered_numbers = ... (stored in memory)
#      print(filtered_numbers)
#      numbers = ... (stored in memory)
#      divisor = ... (stored in memory)
#      filtered_numbers = ... (stored in memory)
#      print(filtered_numbers)
#      numbers = ... (stored in memory)
#      divisor = ... (stored in memory)
#      filtered_numbers = ... (stored in memory)
#      print(filtered_numbers)
#      numbers = ... (stored in memory)
#      divisor = ... (stored in memory)
#      filtered_numbers = ... (stored in memory)
#      for loop runs its body once per item
#      print(filtered_numbers)
#      num = ... (stored in memory)
#      divisor = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_not_divisible(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 278:
#
# ===== SYNTAX =====
#   def filter_non_empty(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   filter_non_empty(strings): looks up variable 'strings', then runs the function body
#   print(filter_non_empty(?)): the inner filter_non_empty() executes FIRST, then print() outputs the result
#   print(filter_non_empty(?)): the inner filter_non_empty() executes FIRST, then print() outputs the result
#   print(filter_non_empty(?)): the inner filter_non_empty() executes FIRST, then print() outputs the result
#   print(filter_non_empty(?)): the inner filter_non_empty() executes FIRST, then print() outputs the result
#   filter_non_empty(strings): looks up variable 'strings', then runs the function body
#   print(filter_non_empty('world')): the inner filter_non_empty() executes FIRST, then print() outputs the result
#   filter_non_empty(?): resolves the value, then runs the function body
#   filter_non_empty(?): resolves the value, then runs the function body
#   filter_non_empty(?): resolves the value, then runs the function body
#   filter_non_empty(?): resolves the value, then runs the function body
#   filter_non_empty('world'): gets the literal 'world', then runs the function body
#   s != '': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_non_empty(strings) — stored for later
#   2. Assign to strings
#   3. Assign to result
#   4. Run print(result)
#   5. Define def filter_non_empty(strings) — stored for later
#   6. Run print(filter_non_empty(...))
#       calls filter_non_empty with: strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#         6.1 Return from function
#   7. Define def filter_non_empty(strings) — stored for later
#   8. Run print(filter_non_empty(...))
#       calls filter_non_empty with: strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#         8.1 Return from function
#   9. Define def filter_non_empty(strings) — stored for later
#   10. Run print(filter_non_empty(...))
#       calls filter_non_empty with: strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#         10.1 Return from function
#   11. Define def filter_non_empty(strings) — stored for later
#   12. Run print(filter_non_empty(...))
#       calls filter_non_empty with: strings=['apple', '', 'banana', '', 'cherry', 'date', '']
#         12.1 Return from function
#   13. Define def filter_non_empty(strings) — stored for later
#   14. Define def filter_non_empty(strings) — stored for later
#   15. Assign to strings
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(filter_non_empty(...))
#       calls filter_non_empty with: strings='world'
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_non_empty, filter_non_empty, filter_non_empty, filter_non_empty, filter_non_empty, filter_non_empty, filter_non_empty are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_non_empty(?)) → function runs first, then print shows the result
#      print(filter_non_empty(?)) → function runs first, then print shows the result
#      print(filter_non_empty(?)) → function runs first, then print shows the result
#      print(filter_non_empty(?)) → function runs first, then print shows the result
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_non_empty('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 279:
#
# ===== SYNTAX =====
#   def filter_prime(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   range() — built-in function call
#   filter() — built-in function call
#   int() — built-in function call
#   all() — built-in function call
#   is_prime() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: + % ** == != < > >= and
#
# ===== EVALUATION ORDER =====
#   filter_prime(numbers): looks up variable 'numbers', then runs the function body
#   filter_prime(numbers): looks up variable 'numbers', then runs the function body
#   filter_prime(numbers): looks up variable 'numbers', then runs the function body
#   filter_prime(numbers): looks up variable 'numbers', then runs the function body
#   filter_prime(numbers): looks up variable 'numbers', then runs the function body
#   print(filter_prime(?)): the inner filter_prime() executes FIRST, then print() outputs the result
#   filter_prime(?): resolves the value, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n < 2: is this True or False? The answer picks which branch
#   n < 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_prime(numbers) — stored for later
#   2. Assign to numbers
#   3. Assign to result
#   4. Run print(result)
#   5. Define def filter_prime(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Define def filter_prime(numbers) — stored for later
#   10. Assign to numbers
#   11. Assign to result
#   12. Run print(result)
#   13. Define def filter_prime(numbers) — stored for later
#   14. Assign to numbers
#   15. Assign to result
#   16. Run print(result)
#   17. Define def filter_prime(numbers) — stored for later
#   18. Define def filter_prime(numbers) — stored for later
#   19. Assign to numbers
#   20. Assign to result
#   21. Run print(result)
#   22. Run print(filter_prime(...))
#       calls filter_prime with: numbers=[4, 1, 8, 6, 3]
#         22.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_prime, filter_prime, filter_prime, filter_prime, filter_prime, filter_prime are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_prime(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 280:
#
# ===== SYNTAX =====
#   def filter_floats(items)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   isinstance() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   filter_floats(items): looks up variable 'items', then runs the function body
#   print(filter_floats(?)): the inner filter_floats() executes FIRST, then print() outputs the result
#   print(filter_floats(?)): the inner filter_floats() executes FIRST, then print() outputs the result
#   print(filter_floats(?)): the inner filter_floats() executes FIRST, then print() outputs the result
#   filter_floats(items): looks up variable 'items', then runs the function body
#   print(filter_floats(?)): the inner filter_floats() executes FIRST, then print() outputs the result
#   filter_floats(?): resolves the value, then runs the function body
#   filter_floats(?): resolves the value, then runs the function body
#   filter_floats(?): resolves the value, then runs the function body
#   filter_floats(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_floats(items) — stored for later
#   2. Assign to items
#   3. Assign to result
#   4. Run print(result)
#   5. Define def filter_floats(items) — stored for later
#   6. Run print(filter_floats(...))
#       calls filter_floats with: items=[1, 2.5, 'apple', 3.7, 10, 8.9, 7, 4.0]
#         6.1 Return from function
#   7. Define def filter_floats(items) — stored for later
#   8. Run print(filter_floats(...))
#       calls filter_floats with: items=[1, 2.5, 'apple', 3.7, 10, 8.9, 7, 4.0]
#         8.1 Return from function
#   9. Define def filter_floats(items) — stored for later
#   10. Run print(filter_floats(...))
#       calls filter_floats with: items=[1, 2.5, 'apple', 3.7, 10, 8.9, 7, 4.0]
#         10.1 Return from function
#   11. Define def filter_floats(items) — stored for later
#   12. Define def filter_floats(items) — stored for later
#   13. Assign to items
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(filter_floats(...))
#       calls filter_floats with: items=[2, 8, 3]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_floats, filter_floats, filter_floats, filter_floats, filter_floats, filter_floats are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_floats(?)) → function runs first, then print shows the result
#      print(filter_floats(?)) → function runs first, then print shows the result
#      print(filter_floats(?)) → function runs first, then print shows the result
#      items = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_floats(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from items each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 281:
#
# ===== SYNTAX =====
#   def filter_uppercase(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   filter_uppercase(strings): looks up variable 'strings', then runs the function body
#   print(filter_uppercase(?)): the inner filter_uppercase() executes FIRST, then print() outputs the result
#   print(filter_uppercase(?)): the inner filter_uppercase() executes FIRST, then print() outputs the result
#   print(filter_uppercase(?)): the inner filter_uppercase() executes FIRST, then print() outputs the result
#   filter_uppercase(strings): looks up variable 'strings', then runs the function body
#   print(filter_uppercase('world')): the inner filter_uppercase() executes FIRST, then print() outputs the result
#   filter_uppercase(?): resolves the value, then runs the function body
#   filter_uppercase(?): resolves the value, then runs the function body
#   filter_uppercase(?): resolves the value, then runs the function body
#   filter_uppercase('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_uppercase(strings) — stored for later
#   2. Assign to strings
#   3. Assign to result
#   4. Run print(result)
#   5. Define def filter_uppercase(strings) — stored for later
#   6. Run print(filter_uppercase(...))
#       calls filter_uppercase with: strings=['HELLO', 'world', 'PYTHON', 'code', 'CHATGPT', 'example']
#         6.1 Return from function
#   7. Define def filter_uppercase(strings) — stored for later
#   8. Run print(filter_uppercase(...))
#       calls filter_uppercase with: strings=['HELLO', 'world', 'PYTHON', 'code', 'CHATGPT', 'example']
#         8.1 Return from function
#   9. Define def filter_uppercase(strings) — stored for later
#   10. Run print(filter_uppercase(...))
#       calls filter_uppercase with: strings=['HELLO', 'world', 'PYTHON', 'code', 'CHATGPT', 'example']
#         10.1 Return from function
#   11. Define def filter_uppercase(strings) — stored for later
#   12. Define def filter_uppercase(strings) — stored for later
#   13. Assign to strings
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(filter_uppercase(...))
#       calls filter_uppercase with: strings='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_uppercase, filter_uppercase, filter_uppercase, filter_uppercase, filter_uppercase, filter_uppercase are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_uppercase(?)) → function runs first, then print shows the result
#      print(filter_uppercase(?)) → function runs first, then print shows the result
#      print(filter_uppercase(?)) → function runs first, then print shows the result
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_uppercase('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 282:
#
# ===== SYNTAX =====
#   def filter_uppercase_again(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   filter_uppercase_again(strings): looks up variable 'strings', then runs the function body
#   print(filter_uppercase_again(?)): the inner filter_uppercase_again() executes FIRST, then print() outputs the result
#   print(filter_uppercase_again(?)): the inner filter_uppercase_again() executes FIRST, then print() outputs the result
#   print(filter_uppercase_again(?)): the inner filter_uppercase_again() executes FIRST, then print() outputs the result
#   filter_uppercase_again(strings): looks up variable 'strings', then runs the function body
#   print(filter_uppercase_again('world')): the inner filter_uppercase_again() executes FIRST, then print() outputs the result
#   filter_uppercase_again(?): resolves the value, then runs the function body
#   filter_uppercase_again(?): resolves the value, then runs the function body
#   filter_uppercase_again(?): resolves the value, then runs the function body
#   filter_uppercase_again('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_uppercase_again(strings) — stored for later
#   2. Assign to strings
#   3. Assign to result
#   4. Run print(result)
#   5. Define def filter_uppercase_again(strings) — stored for later
#   6. Run print(filter_uppercase_again(...))
#       calls filter_uppercase_again with: strings=['HELLO', 'WORLD', 'python', 'CHATGPT', 'openai', 'example']
#         6.1 Return from function
#   7. Define def filter_uppercase_again(strings) — stored for later
#   8. Run print(filter_uppercase_again(...))
#       calls filter_uppercase_again with: strings=['HELLO', 'WORLD', 'python', 'CHATGPT', 'openai', 'example']
#         8.1 Return from function
#   9. Define def filter_uppercase_again(strings) — stored for later
#   10. Run print(filter_uppercase_again(...))
#       calls filter_uppercase_again with: strings=['HELLO', 'WORLD', 'python', 'CHATGPT', 'openai', 'example']
#         10.1 Return from function
#   11. Define def filter_uppercase_again(strings) — stored for later
#   12. Define def filter_uppercase_again(strings) — stored for later
#   13. Assign to strings
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(filter_uppercase_again(...))
#       calls filter_uppercase_again with: strings='world'
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_uppercase_again, filter_uppercase_again, filter_uppercase_again, filter_uppercase_again, filter_uppercase_again, filter_uppercase_again are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_uppercase_again(?)) → function runs first, then print shows the result
#      print(filter_uppercase_again(?)) → function runs first, then print shows the result
#      print(filter_uppercase_again(?)) → function runs first, then print shows the result
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_uppercase_again('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from strings each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 283:
#
# ===== SYNTAX =====
#   def filter_non_none(items)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   filter_non_none(items): looks up variable 'items', then runs the function body
#   print(filter_non_none(?)): the inner filter_non_none() executes FIRST, then print() outputs the result
#   print(filter_non_none(?)): the inner filter_non_none() executes FIRST, then print() outputs the result
#   print(filter_non_none(?)): the inner filter_non_none() executes FIRST, then print() outputs the result
#   filter_non_none(items): looks up variable 'items', then runs the function body
#   print(filter_non_none(?)): the inner filter_non_none() executes FIRST, then print() outputs the result
#   filter_non_none(?): resolves the value, then runs the function body
#   filter_non_none(?): resolves the value, then runs the function body
#   filter_non_none(?): resolves the value, then runs the function body
#   filter_non_none(?): resolves the value, then runs the function body
#   x  None: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_non_none(items) — stored for later
#   2. Assign to items
#   3. Assign to result
#   4. Run print(result)
#   5. Define def filter_non_none(items) — stored for later
#   6. Run print(filter_non_none(...))
#       calls filter_non_none with: items=[1, None, 'example', None, 3.14, '', None]
#         6.1 Return from function
#   7. Define def filter_non_none(items) — stored for later
#   8. Run print(filter_non_none(...))
#       calls filter_non_none with: items=[1, None, 'example', None, 3.14, '', None]
#         8.1 Return from function
#   9. Define def filter_non_none(items) — stored for later
#   10. Run print(filter_non_none(...))
#       calls filter_non_none with: items=[1, None, 'example', None, 3.14, '', None]
#         10.1 Return from function
#   11. Define def filter_non_none(items) — stored for later
#   12. Define def filter_non_none(items) — stored for later
#   13. Assign to items
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(filter_non_none(...))
#       calls filter_non_none with: items=[2, 8, 3]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_non_none, filter_non_none, filter_non_none, filter_non_none, filter_non_none, filter_non_none are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_non_none(?)) → function runs first, then print shows the result
#      print(filter_non_none(?)) → function runs first, then print shows the result
#      print(filter_non_none(?)) → function runs first, then print shows the result
#      items = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_non_none(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from items each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 284:
#
# ===== SYNTAX =====
#   def filter_by_substring(items, substring)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   filter_by_substring(items, substring): looks up variable 'items', looks up variable 'substring', then runs the function body
#   print(filter_by_substring(?, 'apple')): the inner filter_by_substring() executes FIRST, then print() outputs the result
#   print(filter_by_substring(?, 'apple')): the inner filter_by_substring() executes FIRST, then print() outputs the result
#   print(filter_by_substring(?, 'apple')): the inner filter_by_substring() executes FIRST, then print() outputs the result
#   filter_by_substring(items, substring): looks up variable 'items', looks up variable 'substring', then runs the function body
#   print(filter_by_substring(?, 'world')): the inner filter_by_substring() executes FIRST, then print() outputs the result
#   filter_by_substring(?, 'apple'): resolves the value, gets the literal 'apple', then runs the function body
#   filter_by_substring(?, 'apple'): resolves the value, gets the literal 'apple', then runs the function body
#   filter_by_substring(?, 'apple'): resolves the value, gets the literal 'apple', then runs the function body
#   filter_by_substring(?, 'world'): resolves the value, gets the literal 'world', then runs the function body
#   substring not in s: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_by_substring(items, substring) — stored for later
#   2. Assign to items
#   3. Assign to substring
#   4. Assign to result
#   5. Run print(result)
#   6. Define def filter_by_substring(items, substring) — stored for later
#   7. Run print(filter_by_substring(...))
#       calls filter_by_substring with: items=['apple', 'banana', 'cherry', 'pineapple', 'grape'], substring='apple'
#         7.1 Return from function
#   8. Define def filter_by_substring(items, substring) — stored for later
#   9. Run print(filter_by_substring(...))
#       calls filter_by_substring with: items=['apple', 'banana', 'cherry', 'pineapple', 'grape'], substring='apple'
#         9.1 Return from function
#   10. Define def filter_by_substring(items, substring) — stored for later
#   11. Run print(filter_by_substring(...))
#       calls filter_by_substring with: items=['apple', 'banana', 'cherry', 'pineapple', 'grape'], substring='apple'
#         11.1 Return from function
#   12. Define def filter_by_substring(items, substring) — stored for later
#   13. Define def filter_by_substring(items, substring) — stored for later
#   14. Assign to items
#   15. Assign to substring
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(filter_by_substring(...))
#       calls filter_by_substring with: items=[2, 8, 3], substring='world'
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_by_substring, filter_by_substring, filter_by_substring, filter_by_substring, filter_by_substring, filter_by_substring are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      substring = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_by_substring(?, 'apple')) → function runs first, then print shows the result
#      print(filter_by_substring(?, 'apple')) → function runs first, then print shows the result
#      print(filter_by_substring(?, 'apple')) → function runs first, then print shows the result
#      items = ... (stored in memory)
#      substring = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_by_substring(?, 'world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from items each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 286:
#
# ===== SYNTAX =====
#   def sum_first_10_natural_numbers()
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   range() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   while
#   operators: + * // <=
#
# ===== EVALUATION ORDER =====
#   sum_first_10_natural_numbers(): — no arguments needed
#   print(sum_first_10_natural_numbers()): the inner sum_first_10_natural_numbers() executes FIRST, then print() outputs the result
#   print(sum_first_10_natural_numbers()): the inner sum_first_10_natural_numbers() executes FIRST, then print() outputs the result
#   print(sum_first_10_natural_numbers()): the inner sum_first_10_natural_numbers() executes FIRST, then print() outputs the result
#   print(sum_first_10_natural_numbers()): the inner sum_first_10_natural_numbers() executes FIRST, then print() outputs the result
#   sum_first_10_natural_numbers(): — no arguments needed
#   ? // 2: first get ?, then get 2, then compute ? // 2
#   sum_first_10_natural_numbers(): — no arguments needed
#   sum_first_10_natural_numbers(): — no arguments needed
#   sum_first_10_natural_numbers(): — no arguments needed
#   10 * 11: first get 10, then get 11, then compute 10 * 11
#   acc + x: first get acc, then get x, then compute acc + x
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_first_10_natural_numbers() — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sum_first_10_natural_numbers() — stored for later
#   5. Run print(sum_first_10_natural_numbers(...))
#         5.1 Return from function
#   6. Define def sum_first_10_natural_numbers() — stored for later
#   7. Run print(sum_first_10_natural_numbers(...))
#         7.1 Return from function
#   8. Define def sum_first_10_natural_numbers() — stored for later
#   9. Run print(sum_first_10_natural_numbers(...))
#         9.1 Return from function
#   10. Import reduce from functools
#   11. Define def sum_first_10_natural_numbers() — stored for later
#   12. Run print(sum_first_10_natural_numbers(...))
#         12.1 Return from function
#   13. Define def sum_first_10_natural_numbers() — stored for later
#   14. Define def sum_first_10_natural_numbers() — stored for later
#   15. Assign to i
#   16. Assign to total
#   17. While loop
#   18. Assign to result
#   19. Run print(result)
#   20. Assign to i
#   21. Assign to total
#   22. While loop
#   23. Assign to result
#   24. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_first_10_natural_numbers, sum_first_10_natural_numbers, sum_first_10_natural_numbers, sum_first_10_natural_numbers, sum_first_10_natural_numbers, sum_first_10_natural_numbers, sum_first_10_natural_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      print(sum_first_10_natural_numbers()) → function runs first, then print shows the result
#      print(sum_first_10_natural_numbers()) → function runs first, then print shows the result
#      print(sum_first_10_natural_numbers()) → function runs first, then print shows the result
#      print(sum_first_10_natural_numbers()) → function runs first, then print shows the result
#      i = ... (stored in memory)
#      total = ... (stored in memory)
#      while loop repeats until condition is False
#      result = ... (stored in memory)
#      print(result)
#      i = ... (stored in memory)
#      total = ... (stored in memory)
#      while loop repeats until condition is False
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from range(...) each round
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 289:
#
# ===== SYNTAX =====
#   def sum_of_digits(number)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   int() — built-in function call
#   str() — built-in function call
#   abs() — built-in function call
#   sum_of_digits() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   while
#   operators: + // % == >
#
# ===== EVALUATION ORDER =====
#   sum_of_digits(number): looks up variable 'number', then runs the function body
#   print(sum_of_digits(12345)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   print(sum_of_digits(12345)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   print(sum_of_digits(12345)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   sum_of_digits(number): looks up variable 'number', then runs the function body
#   print(sum_of_digits(25)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   sum_of_digits(12345): gets the literal 12345, then runs the function body
#   sum_of_digits(12345): gets the literal 12345, then runs the function body
#   ? + sum_of_digits(...): first get ?, then get sum_of_digits(...), then compute ? + sum_of_digits(...)
#   sum_of_digits(12345): gets the literal 12345, then runs the function body
#   sum_of_digits(25): gets the literal 25, then runs the function body
#   number % 10: first get number, then get 10, then compute number % 10
#   number % 10: first get number, then get 10, then compute number % 10
#   sum_of_digits(number // 10): computes the expression first, then runs the function body
#   number // 10: first get number, then get 10, then compute number // 10
#   number == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_digits(number) — stored for later
#   2. Assign to number
#   3. Assign to result
#   4. Run print(?)
#   5. Define def sum_of_digits(number) — stored for later
#   6. Run print(sum_of_digits(...))
#       calls sum_of_digits with: number=12345
#         6.1 Return from function
#   7. Define def sum_of_digits(number) — stored for later
#   8. Run print(sum_of_digits(...))
#       calls sum_of_digits with: number=12345
#         8.1 Return from function
#   9. Define def sum_of_digits(number) — stored for later
#   10. Run print(sum_of_digits(...))
#       calls sum_of_digits with: number=12345
#         10.1 Return from function
#   11. Define def sum_of_digits(number) — stored for later
#   12. Define def sum_of_digits(number) — stored for later
#   13. Assign to number
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(sum_of_digits(...))
#       calls sum_of_digits with: number=25
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_digits, sum_of_digits, sum_of_digits, sum_of_digits, sum_of_digits, sum_of_digits are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(?)
#      print(sum_of_digits(12345)) → function runs first, then print shows the result
#      print(sum_of_digits(12345)) → function runs first, then print shows the result
#      print(sum_of_digits(12345)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_digits(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from str(...) each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 290:
#
# ===== SYNTAX =====
#   def reverse_number(n)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   str() — built-in function call
#   abs() — built-in function call
#   reversed() — built-in function call
#   for
#   in
#   while
#   operators: + * // % < >
#
# ===== EVALUATION ORDER =====
#   print(reverse_number(num)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(12345)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(12345)): the inner reverse_number() executes FIRST, then print() outputs the result
#   print(reverse_number(12345)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(n): looks up variable 'n', then runs the function body
#   print(reverse_number(15)): the inner reverse_number() executes FIRST, then print() outputs the result
#   reverse_number(num): looks up variable 'num', then runs the function body
#   sign * int(...): first get sign, then get int(...), then compute sign * int(...)
#   reverse_number(12345): gets the literal 12345, then runs the function body
#   rev * sign: first get rev, then get sign, then compute rev * sign
#   reverse_number(12345): gets the literal 12345, then runs the function body
#   sign * int(...): first get sign, then get int(...), then compute sign * int(...)
#   reverse_number(12345): gets the literal 12345, then runs the function body
#   reverse_number(15): gets the literal 15, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   n // 10: first get n, then get 10, then compute n // 10
#   ? + int(...): first get ?, then get int(...), then compute ? + int(...)
#   reversed_num * 10: first get reversed_num, then get 10, then compute reversed_num * 10
#   n % 10: first get n, then get 10, then compute n % 10
#   rev * 10: first get rev, then get 10, then compute rev * 10
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_number(n) — stored for later
#   2. Assign to num
#   3. Run print('Reversed number:', reverse_number(...))
#   4. Define def reverse_number(n) — stored for later
#   5. Run print(reverse_number(...))
#       calls reverse_number with: n=12345
#         5.1 Return from function
#   6. Define def reverse_number(n) — stored for later
#   7. Run print(reverse_number(...))
#       calls reverse_number with: n=12345
#         7.1 Return from function
#   8. Define def reverse_number(n) — stored for later
#   9. Run print(reverse_number(...))
#       calls reverse_number with: n=12345
#         9.1 Return from function
#   10. Define def reverse_number(n) — stored for later
#   11. Define def reverse_number(n) — stored for later
#   12. Assign to n
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(reverse_number(...))
#       calls reverse_number with: n=15
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_number, reverse_number, reverse_number, reverse_number, reverse_number, reverse_number are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      print('Reversed number:', reverse_number(...))
#      print(reverse_number(12345)) → function runs first, then print shows the result
#      print(reverse_number(12345)) → function runs first, then print shows the result
#      print(reverse_number(12345)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_number(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from str(...) each round
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 291:
#
# ===== SYNTAX =====
#   def factorial(number)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   factorial() — built-in function call
#   reduce() — built-in function call
#   for
#   if/elif/else
#   in
#   lambda
#   while
#   operators: + - * > <=
#
# ===== EVALUATION ORDER =====
#   factorial(number_to_calculate): looks up variable 'number_to_calculate', then runs the function body
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(number): looks up variable 'number', then runs the function body
#   print(factorial(25)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(5): gets the literal 5, then runs the function body
#   number * factorial(...): first get number, then get factorial(...), then compute number * factorial(...)
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(25): gets the literal 25, then runs the function body
#   number + 1: first get number, then get 1, then compute number + 1
#   factorial(number - 1): computes the expression first, then runs the function body
#   number - 1: first get number, then get 1, then compute number - 1
#   x * y: first get x, then get y, then compute x * y
#   number + 1: first get number, then get 1, then compute number + 1
#   factorial(num): looks up variable 'num', then runs the function body
#   number <= 1: is this True or False? The answer picks which branch
#   number <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(number) — stored for later
#   2. Assign to number_to_calculate
#   3. Assign to result
#   4. Run print(?)
#   5. Assign to test_numbers
#   6. For loop over test_numbers
#       6.1 Run print(?)
#   7. Define def factorial(number) — stored for later
#   8. Run print(factorial(...))
#       calls factorial with: number=5
#         8.1 Return from function
#   9. Import math
#   10. Define def factorial(number) — stored for later
#   11. Run print(factorial(...))
#       calls factorial with: number=5
#         11.1 Return from function
#   12. Define def factorial(number) — stored for later
#   13. Run print(factorial(...))
#       calls factorial with: number=5
#         13.1 Return from function
#   14. Import reduce from functools
#   15. Define def factorial(number) — stored for later
#   16. Run print(factorial(...))
#       calls factorial with: number=5
#         16.1 Return from function
#   17. Define def factorial(number) — stored for later
#   18. Define def factorial(number) — stored for later
#   19. Assign to number
#   20. Assign to result
#   21. Run print(result)
#   22. Run print(factorial(...))
#       calls factorial with: number=25
#         22.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial, factorial, factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      number_to_calculate = ... (stored in memory)
#      result = ... (stored in memory)
#      print(?)
#      test_numbers = ... (stored in memory)
#      for loop runs its body once per item
#      print(factorial(5)) → function runs first, then print shows the result
#      print(factorial(5)) → function runs first, then print shows the result
#      print(factorial(5)) → function runs first, then print shows the result
#      print(factorial(5)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from test_numbers each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 293:
#
# ===== SYNTAX =====
#   def is_prime(number)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   while
#   operators: + * % ** == != < <=
#
# ===== EVALUATION ORDER =====
#   is_prime(number): looks up variable 'number', then runs the function body
#   print(is_prime(25)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(num): looks up variable 'num', then runs the function body
#   is_prime(25): gets the literal 25, then runs the function body
#   i * i: first get i, then get i, then compute i * i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   number % 2: first get number, then get 2, then compute number % 2
#   i * i: first get i, then get i, then compute i * i
#   number % i: first get number, then get i, then compute number % i
#   number % i: first get number, then get i, then compute number % i
#   number % i: first get number, then get i, then compute number % i
#   number % i: first get number, then get i, then compute number % i
#   number ** 0.5: first get number, then get 0.5, then compute number ** 0.5
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   is_prime(num): looks up variable 'num', then runs the function body
#   is_prime(num): looks up variable 'num', then runs the function body
#   is_prime(num): looks up variable 'num', then runs the function body
#   number ** 0.5: first get number, then get 0.5, then compute number ** 0.5
#   number < 2: is this True or False? The answer picks which branch
#   number < 2: is this True or False? The answer picks which branch
#   number < 2: is this True or False? The answer picks which branch
#   number < 2: is this True or False? The answer picks which branch
#   number == 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(number) — stored for later
#   2. Assign to test_numbers
#   3. Run print('Prime number check:')
#   4. For loop over test_numbers
#       4.1 If statement (one branch runs)
#   5. Define def is_prime(number) — stored for later
#   6. For loop over [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29]
#       6.1 Run print(?)
#   7. Define def is_prime(number) — stored for later
#   8. For loop over [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29]
#       8.1 Run print(?)
#   9. Define def is_prime(number) — stored for later
#   10. For loop over [1, 2, 3, 4, 5, 10, 13, 17, 20, 23, 24, 29]
#       10.1 Run print(?)
#   11. Define def is_prime(number) — stored for later
#   12. Define def is_prime(number) — stored for later
#   13. Assign to number
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(is_prime(...))
#       calls is_prime with: number=25
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, is_prime, is_prime, is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      test_numbers = ... (stored in memory)
#      print('Prime number check:')
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#      for loop runs its body once per item
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from test_numbers each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 294:
#
# ===== SYNTAX =====
#   def count_digits(number)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   str() — built-in function call
#   abs() — built-in function call
#   for
#   if/elif/else
#   in
#   while
#   operators: + == < >
#
# ===== EVALUATION ORDER =====
#   print(count_digits(12345)): the inner count_digits() executes FIRST, then print() outputs the result
#   print(count_digits(?)): the inner count_digits() executes FIRST, then print() outputs the result
#   print(count_digits(12345)): the inner count_digits() executes FIRST, then print() outputs the result
#   print(count_digits(?)): the inner count_digits() executes FIRST, then print() outputs the result
#   print(count_digits(12345)): the inner count_digits() executes FIRST, then print() outputs the result
#   print(count_digits(?)): the inner count_digits() executes FIRST, then print() outputs the result
#   count_digits(number): looks up variable 'number', then runs the function body
#   print(count_digits(25)): the inner count_digits() executes FIRST, then print() outputs the result
#   count_digits(12345): gets the literal 12345, then runs the function body
#   count_digits(?): resolves the value, then runs the function body
#   math.floor(...) + 1: first get math.floor(...), then get 1, then compute math.floor(...) + 1
#   count_digits(12345): gets the literal 12345, then runs the function body
#   count_digits(?): resolves the value, then runs the function body
#   count_digits(12345): gets the literal 12345, then runs the function body
#   count_digits(?): resolves the value, then runs the function body
#   count_digits(25): gets the literal 25, then runs the function body
#   count_digits(?): resolves the value, then runs the function body
#   count_digits(num): looks up variable 'num', then runs the function body
#   num < 0: is this True or False? The answer picks which branch
#   number == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_digits(number) — stored for later
#   2. Assign to test_numbers
#   3. Run print('Counting digits:')
#   4. For loop over test_numbers
#       4.1 If statement (one branch runs)
#   5. Define def count_digits(number) — stored for later
#   6. Run print(count_digits(...))
#       calls count_digits with: number=12345
#         6.1 Return from function
#   7. Run print(count_digits(...))
#       calls count_digits with: number=-456
#         7.1 Return from function
#   8. Import math
#   9. Define def count_digits(number) — stored for later
#   10. Run print(count_digits(...))
#       calls count_digits with: number=12345
#         10.1 Return from function
#   11. Run print(count_digits(...))
#       calls count_digits with: number=-456
#         11.1 Return from function
#   12. Define def count_digits(number) — stored for later
#   13. Run print(count_digits(...))
#       calls count_digits with: number=12345
#         13.1 Return from function
#   14. Run print(count_digits(...))
#       calls count_digits with: number=-456
#         14.1 Return from function
#   15. Define def count_digits(number) — stored for later
#   16. Define def count_digits(number) — stored for later
#   17. Assign to number
#   18. Assign to result
#   19. Run print(result)
#   20. Run print(count_digits(...))
#       calls count_digits with: number=25
#         20.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions count_digits, count_digits, count_digits, count_digits, count_digits, count_digits are just stored
#   Execution starts at the non-def lines below:
#      test_numbers = ... (stored in memory)
#      print('Counting digits:')
#      for loop runs its body once per item
#      print(count_digits(12345)) → function runs first, then print shows the result
#      print(count_digits(?)) → function runs first, then print shows the result
#      print(count_digits(12345)) → function runs first, then print shows the result
#      print(count_digits(?)) → function runs first, then print shows the result
#      print(count_digits(12345)) → function runs first, then print shows the result
#      print(count_digits(?)) → function runs first, then print shows the result
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_digits(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from test_numbers each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 295:
#
# ===== SYNTAX =====
#   def gcd(a, b)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   abs() — built-in function call
#   gcd() — built-in function call
#   for
#   if/elif/else
#   in
#   while
#   operators: % == != > and
#
# ===== EVALUATION ORDER =====
#   gcd(num1, num2): looks up variable 'num1', looks up variable 'num2', then runs the function body
#   print(gcd(56, 98)): the inner gcd() executes FIRST, then print() outputs the result
#   print(gcd(56, 98)): the inner gcd() executes FIRST, then print() outputs the result
#   print(gcd(56, 98)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(gcd(20, 30)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   gcd(56, 98): gets the literal 56, gets the literal 98, then runs the function body
#   gcd(56, 98): gets the literal 56, gets the literal 98, then runs the function body
#   gcd(b, a % b): looks up variable 'b', computes the expression first, then runs the function body
#   gcd(56, 98): gets the literal 56, gets the literal 98, then runs the function body
#   gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#   b % a: first get b, then get a, then compute b % a
#   b == 0: is this True or False? The answer picks which branch
#   a > b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def gcd(a, b) — stored for later
#   2. Assign to num1
#   3. Assign to num2
#   4. Assign to result
#   5. Run print(?)
#   6. Assign to test_cases
#   7. Run print('Testing additional cases:')
#   8. For loop over test_cases
#       8.1 Assign to ?
#       8.2 Assign to result
#       8.3 Run print(?)
#   9. Define def gcd(a, b) — stored for later
#   10. Run print(gcd(...))
#       calls gcd with: a=56, b=98
#         10.1 Return from function
#   11. Import math
#   12. Define def gcd(a, b) — stored for later
#   13. Run print(gcd(...))
#       calls gcd with: a=56, b=98
#         13.1 Return from function
#   14. Define def gcd(a, b) — stored for later
#   15. Run print(gcd(...))
#       calls gcd with: a=56, b=98
#         15.1 Return from function
#   16. Define def gcd(a, b) — stored for later
#   17. Define def gcd(a, b) — stored for later
#   18. Assign to a
#   19. Assign to b
#   20. Assign to result
#   21. Run print(result)
#   22. Run print(gcd(...))
#       calls gcd with: a=20, b=30
#         22.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions gcd, gcd, gcd, gcd, gcd, gcd are just stored
#   Execution starts at the non-def lines below:
#      num1 = ... (stored in memory)
#      num2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(?)
#      test_cases = ... (stored in memory)
#      print('Testing additional cases:')
#      for loop runs its body once per item
#      print(gcd(56, 98)) → function runs first, then print shows the result
#      print(gcd(56, 98)) → function runs first, then print shows the result
#      print(gcd(56, 98)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(gcd(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from test_cases each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 297:
#
# ===== SYNTAX =====
#   def sum_elements(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   lambda
#   while
#   operators: + <
#
# ===== EVALUATION ORDER =====
#   print(sum_elements(lst)): the inner sum_elements() executes FIRST, then print() outputs the result
#   print(sum_elements(?)): the inner sum_elements() executes FIRST, then print() outputs the result
#   print(sum_elements(?)): the inner sum_elements() executes FIRST, then print() outputs the result
#   print(sum_elements(?)): the inner sum_elements() executes FIRST, then print() outputs the result
#   sum_elements(lst): looks up variable 'lst', then runs the function body
#   print(sum_elements(?)): the inner sum_elements() executes FIRST, then print() outputs the result
#   sum_elements(lst): looks up variable 'lst', then runs the function body
#   sum_elements(?): resolves the value, then runs the function body
#   sum_elements(?): resolves the value, then runs the function body
#   sum_elements(?): resolves the value, then runs the function body
#   sum_elements(?): resolves the value, then runs the function body
#   acc + x: first get acc, then get x, then compute acc + x
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_elements(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sum_elements(...))
#       calls sum_elements with: lst=lst
#         3.1 Return from function
#   4. Define def sum_elements(lst) — stored for later
#   5. Run print(sum_elements(...))
#       calls sum_elements with: lst=[1, 2, 3, 4, 5]
#         5.1 Return from function
#   6. Define def sum_elements(lst) — stored for later
#   7. Run print(sum_elements(...))
#       calls sum_elements with: lst=[1, 2, 3, 4, 5]
#         7.1 Return from function
#   8. Import reduce from functools
#   9. Define def sum_elements(lst) — stored for later
#   10. Run print(sum_elements(...))
#       calls sum_elements with: lst=[1, 2, 3, 4, 5]
#         10.1 Return from function
#   11. Define def sum_elements(lst) — stored for later
#   12. Define def sum_elements(lst) — stored for later
#   13. Assign to lst
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(sum_elements(...))
#       calls sum_elements with: lst=[1, 3, 5, 7]
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_elements, sum_elements, sum_elements, sum_elements, sum_elements, sum_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sum_elements(lst)) → function runs first, then print shows the result
#      print(sum_elements(?)) → function runs first, then print shows the result
#      print(sum_elements(?)) → function runs first, then print shows the result
#      print(sum_elements(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 298:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   max() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   while
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   main(lst): looks up variable 'lst', then runs the function body
#   find_largest_element(numbers_list): looks up variable 'numbers_list', then runs the function body
#   find_largest_element(additional_numbers_list): looks up variable 'additional_numbers_list', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   print(find_largest_element(?)): the inner find_largest_element() executes FIRST, then print() outputs the result
#   print(find_largest_element(?)): the inner find_largest_element() executes FIRST, then print() outputs the result
#   print(find_largest_element(?)): the inner find_largest_element() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(?): resolves the value, then runs the function body
#   find_largest_element(?): resolves the value, then runs the function body
#   find_largest_element(?): resolves the value, then runs the function body
#   find_largest_element(?): resolves the value, then runs the function body
#   main(?): resolves the value, then runs the function body
#   ? > largest: is this True or False? The answer picks which branch
#   n > largest: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run main(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def find_largest_element(numbers) — stored for later
#   5. Assign to numbers_list
#   6. Assign to largest_number
#   7. Run print(?)
#   8. Assign to additional_numbers_list
#   9. Assign to largest_number_in_negative
#   10. Run print(?)
#   11. Define def main(lst) — stored for later
#   12. Run print(main(...))
#       calls main with: lst=['tree', 'cliff', 'mountain', 'tree', 'tree', 'grassy']
#         12.1 Return from function
#   13. Define def find_largest_element(numbers) — stored for later
#   14. Run print(find_largest_element(...))
#       calls find_largest_element with: numbers=[5, 12, 3, 8, 20, 15]
#         14.1 Return from function
#   15. Define def find_largest_element(numbers) — stored for later
#   16. Run print(find_largest_element(...))
#       calls find_largest_element with: numbers=[5, 12, 3, 8, 20, 15]
#         16.1 Return from function
#   17. Define def find_largest_element(numbers) — stored for later
#   18. Run print(find_largest_element(...))
#       calls find_largest_element with: numbers=[5, 12, 3, 8, 20, 15]
#         18.1 Return from function
#   19. Define def main(lst) — stored for later
#   20. Define def main(lst) — stored for later
#   21. Assign to lst
#   22. Assign to result
#   23. Run print(result)
#   24. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         24.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, find_largest_element, main, find_largest_element, find_largest_element, find_largest_element, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      numbers_list = ... (stored in memory)
#      largest_number = ... (stored in memory)
#      print(?)
#      additional_numbers_list = ... (stored in memory)
#      largest_number_in_negative = ... (stored in memory)
#      print(?)
#      print(main(?)) → function runs first, then print shows the result
#      print(find_largest_element(?)) → function runs first, then print shows the result
#      print(find_largest_element(?)) → function runs first, then print shows the result
#      print(find_largest_element(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from ? each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 299:
#
# ===== SYNTAX =====
#   def smallest_element(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   min() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   while
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(smallest_element(lst)): the inner smallest_element() executes FIRST, then print() outputs the result
#   print(smallest_element(?)): the inner smallest_element() executes FIRST, then print() outputs the result
#   print(smallest_element(?)): the inner smallest_element() executes FIRST, then print() outputs the result
#   print(smallest_element(?)): the inner smallest_element() executes FIRST, then print() outputs the result
#   smallest_element(lst): looks up variable 'lst', then runs the function body
#   print(smallest_element(?)): the inner smallest_element() executes FIRST, then print() outputs the result
#   smallest_element(lst): looks up variable 'lst', then runs the function body
#   smallest_element(?): resolves the value, then runs the function body
#   smallest_element(?): resolves the value, then runs the function body
#   smallest_element(?): resolves the value, then runs the function body
#   smallest_element(?): resolves the value, then runs the function body
#   ? < smallest_word: is this True or False? The answer picks which branch
#   item < smallest: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def smallest_element(lst) — stored for later
#   2. Assign to lst
#   3. Run print(smallest_element(...))
#       calls smallest_element with: lst=lst
#         3.1 Return from function
#   4. Define def smallest_element(lst) — stored for later
#   5. Run print(smallest_element(...))
#       calls smallest_element with: lst=['hot', 'hotty', 'hotties']
#         5.1 Return from function
#   6. Define def smallest_element(lst) — stored for later
#   7. Run print(smallest_element(...))
#       calls smallest_element with: lst=['hot', 'hotty', 'hotties']
#         7.1 Return from function
#   8. Define def smallest_element(lst) — stored for later
#   9. Run print(smallest_element(...))
#       calls smallest_element with: lst=['hot', 'hotty', 'hotties']
#         9.1 Return from function
#   10. Define def smallest_element(lst) — stored for later
#   11. Define def smallest_element(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(smallest_element(...))
#       calls smallest_element with: lst=[1, 3, 5, 7]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions smallest_element, smallest_element, smallest_element, smallest_element, smallest_element, smallest_element are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(smallest_element(lst)) → function runs first, then print shows the result
#      print(smallest_element(?)) → function runs first, then print shows the result
#      print(smallest_element(?)) → function runs first, then print shows the result
#      print(smallest_element(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(smallest_element(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 304:
#
# ===== SYNTAX =====
#   def ask_until_condition()
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   break
#   if/elif/else
#   while
#   operators: > <=
#
# ===== EVALUATION ORDER =====
#   ask_until_condition(): — no arguments needed
#   ask_until_condition(): — no arguments needed
#   ask_until_condition(): — no arguments needed
#   ask_until_condition(): — no arguments needed
#   int(...) > 10: is this True or False? The answer picks which branch
#   int(...) > 10: is this True or False? The answer picks which branch
#   int(...) > 10: is this True or False? The answer picks which branch
#   int(...) > 10: is this True or False? The answer picks which branch
#   int(...) > 10: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def ask_until_condition() — stored for later
#   2. Run ask_until_condition()
#         2.1 Assign to user_input
#         2.2 While loop
#   3. Define def ask_until_condition() — stored for later
#   4. Run ask_until_condition()
#         4.1 Assign to user_input
#         4.2 While loop
#   5. Define def ask_until_condition() — stored for later
#   6. Run ask_until_condition()
#         6.1 Assign to user_input
#         6.2 While loop
#   7. Define def ask_until_condition() — stored for later
#   8. Run ask_until_condition()
#         8.1 Assign to user_input
#         8.2 While loop
#   9. While loop
#   10. While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions ask_until_condition, ask_until_condition, ask_until_condition, ask_until_condition are just stored
#   Execution starts at the non-def lines below:
#      ask_until_condition() → jumps into function, runs body, returns result → printed
#      ask_until_condition() → jumps into function, runs body, returns result → printed
#      ask_until_condition() → jumps into function, runs body, returns result → printed
#      ask_until_condition() → jumps into function, runs body, returns result → printed
#      while loop repeats until condition is False
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 307:
#
# ===== SYNTAX =====
#   def find_min_max()
#   int() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   find_min_max(): — no arguments needed
#   number < smallest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#   number < smallest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#   number < smallest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#   number < smallest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#   number < smallest: is this True or False? The answer picks which branch
#   number > largest: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min_max() — stored for later
#   2. Run find_min_max()
#         2.1 Assign to numbers
#         2.2 Assign to smallest
#         2.3 Assign to largest
#         2.4 For loop over ?
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def find_min_max() — stored for later
#   4. Define def find_min_max() — stored for later
#   5. Assign to numbers
#   6. Assign to smallest
#   7. Assign to largest
#   8. For loop over ?
#       8.1 If statement (one branch runs)
#       8.2 If statement (one branch runs)
#   9. Run print(smallest, largest)
#   10. Assign to numbers
#   11. Assign to smallest
#   12. Assign to largest
#   13. For loop over ?
#       13.1 If statement (one branch runs)
#       13.2 If statement (one branch runs)
#   14. Run print(smallest, largest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min_max, find_min_max, find_min_max are just stored
#   Execution starts at the non-def lines below:
#      find_min_max() → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      smallest = ... (stored in memory)
#      largest = ... (stored in memory)
#      for loop runs its body once per item
#      print(smallest, largest)
#      numbers = ... (stored in memory)
#      smallest = ... (stored in memory)
#      largest = ... (stored in memory)
#      for loop runs its body once per item
#      print(smallest, largest)
#
#   Loop: grabs one item from ? each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 308:
#
# ===== SYNTAX =====
#   def group_by_first_letter()
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   group_by_first_letter(): — no arguments needed
#   first_letter not in result: is this True or False? The answer picks which branch
#   first_letter not in result: is this True or False? The answer picks which branch
#   first_letter not in result: is this True or False? The answer picks which branch
#   first_letter not in result: is this True or False? The answer picks which branch
#   first_letter not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def group_by_first_letter() — stored for later
#   2. Run group_by_first_letter()
#         2.1 Assign to lst
#         2.2 Assign to result
#         2.3 For loop over lst
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def group_by_first_letter() — stored for later
#   4. Define def group_by_first_letter() — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. For loop over lst
#       7.1 Assign to first_letter
#       7.2 If statement (one branch runs)
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. For loop over lst
#       11.1 Assign to first_letter
#       11.2 If statement (one branch runs)
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions group_by_first_letter, group_by_first_letter, group_by_first_letter are just stored
#   Execution starts at the non-def lines below:
#      group_by_first_letter() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 310:
#
# ===== SYNTAX =====
#   def find_longest_word(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   max() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(find_longest_word(lst)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   main(lst): looks up variable 'lst', then runs the function body
#   find_longest_word(lst): looks up variable 'lst', then runs the function body
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(lst): looks up variable 'lst', then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   len(...) > count: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_longest_word(lst) — stored for later
#   2. Assign to lst
#   3. Run print(find_longest_word(...))
#       calls find_longest_word with: lst=lst
#         3.1 Return from function
#   4. Define def main(lst) — stored for later
#   5. Assign to lst
#   6. Run main(lst)
#       with: lst=lst
#         6.1 Return from function
#   7. Define def find_longest_word(lst) — stored for later
#   8. Define def find_longest_word(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_longest_word(...))
#       calls find_longest_word with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_longest_word, main, find_longest_word, find_longest_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_longest_word(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_longest_word(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 311:
#
# ===== SYNTAX =====
#   def find_longest_word(lst)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(find_longest_word(lst)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(lst): looks up variable 'lst', then runs the function body
#   print(find_longest_word(?)): the inner find_longest_word() executes FIRST, then print() outputs the result
#   find_longest_word(lst): looks up variable 'lst', then runs the function body
#   find_longest_word(?): resolves the value, then runs the function body
#   current_length > max_length: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_longest_word(lst) — stored for later
#   2. Assign to lst
#   3. Run print(find_longest_word(...))
#       calls find_longest_word with: lst=lst
#         3.1 Return from function
#   4. Define def find_longest_word(lst) — stored for later
#   5. Define def find_longest_word(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(find_longest_word(...))
#       calls find_longest_word with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_longest_word, find_longest_word, find_longest_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_longest_word(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_longest_word(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 312:
#
# ===== SYNTAX =====
#   def second_largest_number()
#   list() — built-in function call
#   map() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   second_largest_number(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def second_largest_number() — stored for later
#   2. Run second_largest_number()
#         2.1 Assign to user
#         2.2 Assign to user_int
#         2.3 Run user_int.sort()
#         2.4 Assign to second_largest
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def second_largest_number() — stored for later
#   4. Define def second_largest_number() — stored for later
#   5. Assign to user
#   6. Assign to user_int
#   7. Run user_int.sort()
#   8. Assign to second_largest
#   9. Run print(second_largest)
#   10. Assign to user
#   11. Assign to user_int
#   12. Run user_int.sort()
#   13. Assign to second_largest
#   14. Run print(second_largest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_largest_number, second_largest_number, second_largest_number are just stored
#   Execution starts at the non-def lines below:
#      second_largest_number() → jumps into function, runs body, returns result → printed
#      user = ... (stored in memory)
#      user_int = ... (stored in memory)
#      user_int.sort()
#      second_largest = ... (stored in memory)
#      print(second_largest)
#      user = ... (stored in memory)
#      user_int = ... (stored in memory)
#      user_int.sort()
#      second_largest = ... (stored in memory)
#      print(second_largest)

"""

"""
Problem 313:
#
# ===== SYNTAX =====
#   def convert_to_uppercase(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   operators: - <=
#
# ===== EVALUATION ORDER =====
#   convert_to_uppercase(strings): looks up variable 'strings', then runs the function body
#   convert_to_uppercase(strings): looks up variable 'strings', then runs the function body
#   print(convert_to_uppercase('world')): the inner convert_to_uppercase() executes FIRST, then print() outputs the result
#   convert_to_uppercase('world'): gets the literal 'world', then runs the function body
#   ord(...) - 32: first get ord(...), then get 32, then compute ord(...) - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_uppercase(strings) — stored for later
#   2. Assign to strings
#   3. Assign to uppercase_strings
#   4. Run print('Original list:', strings)
#   5. Run print('Uppercase list:', uppercase_strings)
#   6. Define def convert_to_uppercase(strings) — stored for later
#   7. Define def convert_to_uppercase(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(convert_to_uppercase(...))
#       calls convert_to_uppercase with: strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_uppercase, convert_to_uppercase, convert_to_uppercase are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      uppercase_strings = ... (stored in memory)
#      print('Original list:', strings)
#      print('Uppercase list:', uppercase_strings)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_uppercase('world')) → function runs first, then print shows the result

"""

"""
Problem 314:
#
# ===== SYNTAX =====
#   def convert_to_integers(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   int() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   convert_to_integers(string_numbers): looks up variable 'string_numbers', then runs the function body
#   convert_to_integers(strings): looks up variable 'strings', then runs the function body
#   print(convert_to_integers('world')): the inner convert_to_integers() executes FIRST, then print() outputs the result
#   convert_to_integers('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_integers(strings) — stored for later
#   2. Assign to string_numbers
#   3. Assign to integer_numbers
#   4. Run print('Original list of strings:', string_numbers)
#   5. Run print('Converted list of integers:', integer_numbers)
#   6. Define def convert_to_integers(strings) — stored for later
#   7. Define def convert_to_integers(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(convert_to_integers(...))
#       calls convert_to_integers with: strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_integers, convert_to_integers, convert_to_integers are just stored
#   Execution starts at the non-def lines below:
#      string_numbers = ... (stored in memory)
#      integer_numbers = ... (stored in memory)
#      print('Original list of strings:', string_numbers)
#      print('Converted list of integers:', integer_numbers)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_integers('world')) → function runs first, then print shows the result

"""

"""
Problem 315:
#
# ===== SYNTAX =====
#   def square_numbers(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   square_numbers(original_numbers): looks up variable 'original_numbers', then runs the function body
#   square_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(square_numbers(?)): the inner square_numbers() executes FIRST, then print() outputs the result
#   square_numbers(?): resolves the value, then runs the function body
#   x * x: first get x, then get x, then compute x * x
#
# ===== EXECUTION ORDER =====
#   1. Define def square_numbers(numbers) — stored for later
#   2. Assign to original_numbers
#   3. Assign to squared_numbers
#   4. Run print('Original list of numbers:', original_numbers)
#   5. Run print('Squared list of numbers:', squared_numbers)
#   6. Define def square_numbers(numbers) — stored for later
#   7. Define def square_numbers(numbers) — stored for later
#   8. Assign to numbers
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(square_numbers(...))
#       calls square_numbers with: numbers=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_numbers, square_numbers, square_numbers are just stored
#   Execution starts at the non-def lines below:
#      original_numbers = ... (stored in memory)
#      squared_numbers = ... (stored in memory)
#      print('Original list of numbers:', original_numbers)
#      print('Squared list of numbers:', squared_numbers)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 316:
#
# ===== SYNTAX =====
#   def string_lengths(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   len() — built-in function call
#   map() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   string_lengths(string_list): looks up variable 'string_list', then runs the function body
#   string_lengths(strings): looks up variable 'strings', then runs the function body
#   print(string_lengths('world')): the inner string_lengths() executes FIRST, then print() outputs the result
#   string_lengths('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_lengths(strings) — stored for later
#   2. Assign to string_list
#   3. Assign to lengths
#   4. Run print('Original list of strings:', string_list)
#   5. Run print('Lengths of each string:', lengths)
#   6. Define def string_lengths(strings) — stored for later
#   7. Define def string_lengths(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(string_lengths(...))
#       calls string_lengths with: strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_lengths, string_lengths, string_lengths are just stored
#   Execution starts at the non-def lines below:
#      string_list = ... (stored in memory)
#      lengths = ... (stored in memory)
#      print('Original list of strings:', string_list)
#      print('Lengths of each string:', lengths)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_lengths('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 317:
#
# ===== SYNTAX =====
#   def fahrenheit_to_celsius(temperatures)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: - * /
#
# ===== EVALUATION ORDER =====
#   fahrenheit_to_celsius(fahrenheit_temps): looks up variable 'fahrenheit_temps', then runs the function body
#   fahrenheit_to_celsius(temperatures): looks up variable 'temperatures', then runs the function body
#   print(fahrenheit_to_celsius(25)): the inner fahrenheit_to_celsius() executes FIRST, then print() outputs the result
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   fahrenheit_to_celsius(25): gets the literal 25, then runs the function body
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   f - 32: first get f, then get 32, then compute f - 32
#   f - 32: first get f, then get 32, then compute f - 32
#   f - 32: first get f, then get 32, then compute f - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def fahrenheit_to_celsius(temperatures) — stored for later
#   2. Assign to fahrenheit_temps
#   3. Assign to celsius_temps
#   4. Run print('Temperatures in Fahrenheit:', fahrenheit_temps)
#   5. Run print('Temperatures in Celsius:', celsius_temps)
#   6. Define def fahrenheit_to_celsius(temperatures) — stored for later
#   7. Define def fahrenheit_to_celsius(temperatures) — stored for later
#   8. Assign to temperatures
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(fahrenheit_to_celsius(...))
#       calls fahrenheit_to_celsius with: temperatures=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fahrenheit_to_celsius, fahrenheit_to_celsius, fahrenheit_to_celsius are just stored
#   Execution starts at the non-def lines below:
#      fahrenheit_temps = ... (stored in memory)
#      celsius_temps = ... (stored in memory)
#      print('Temperatures in Fahrenheit:', fahrenheit_temps)
#      print('Temperatures in Celsius:', celsius_temps)
#      temperatures = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fahrenheit_to_celsius(25)) → function runs first, then print shows the result

"""

"""
Problem 319:
#
# ===== SYNTAX =====
#   def add_five_to_number(x)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(add_five(lst)): the inner add_five() executes FIRST, then print() outputs the result
#   add_five_to_number(x): looks up variable 'x', then runs the function body
#   print(add_five_to_number(3)): the inner add_five_to_number() executes FIRST, then print() outputs the result
#   x + 5: first get x, then get 5, then compute x + 5
#   add_five(lst): looks up variable 'lst', then runs the function body
#   add_five_to_number(3): gets the literal 3, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add_five_to_number(x) — stored for later
#   2. Define def add_five(lst) — stored for later
#   3. Assign to lst
#   4. Run print(add_five(...))
#       calls add_five with: lst=lst
#         4.1 Return from function
#   5. Define def add_five_to_number(x) — stored for later
#   6. Define def add_five_to_number(x) — stored for later
#   7. Assign to x
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(add_five_to_number(...))
#       calls add_five_to_number with: x=3
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_five_to_number, add_five, add_five_to_number, add_five_to_number are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(add_five(lst)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_five_to_number(3)) → function runs first, then print shows the result

"""

"""
Problem 320:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   main(lst): looks up variable 'lst', then runs the function body
#   square_roots(number_list): looks up variable 'number_list', then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(?): resolves the value, then runs the function body
#   num ** 2: first get num, then get 2, then compute num ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run main(lst)
#       with: lst=lst
#         3.1 Import math
#         3.2 Return from function
#   4. Import math
#   5. Define def square_roots(numbers) — stored for later
#   6. Assign to number_list
#   7. Assign to roots
#   8. Run print('Original numbers:', number_list)
#   9. Run print('Square roots:', roots)
#   10. Define def main(lst) — stored for later
#   11. Define def main(lst) — stored for later
#   12. Assign to lst
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         15.1 Import math
#         15.2 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions main, square_roots, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      number_list = ... (stored in memory)
#      roots = ... (stored in memory)
#      print('Original numbers:', number_list)
#      print('Square roots:', roots)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result

"""

"""
Problem 321:
#
# ===== SYNTAX =====
#   def lengths_of_strings(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   len() — built-in function call
#   map() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   lengths_of_strings(string_list): looks up variable 'string_list', then runs the function body
#   lengths_of_strings(strings): looks up variable 'strings', then runs the function body
#   print(lengths_of_strings('world')): the inner lengths_of_strings() executes FIRST, then print() outputs the result
#   lengths_of_strings('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def lengths_of_strings(strings) — stored for later
#   2. Assign to string_list
#   3. Assign to lengths
#   4. Run print('Original strings:', string_list)
#   5. Run print('Lengths of strings:', lengths)
#   6. Define def lengths_of_strings(strings) — stored for later
#   7. Define def lengths_of_strings(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(lengths_of_strings(...))
#       calls lengths_of_strings with: strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lengths_of_strings, lengths_of_strings, lengths_of_strings are just stored
#   Execution starts at the non-def lines below:
#      string_list = ... (stored in memory)
#      lengths = ... (stored in memory)
#      print('Original strings:', string_list)
#      print('Lengths of strings:', lengths)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lengths_of_strings('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 322:
#
# ===== SYNTAX =====
#   def normalize_scores(scores)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   min() — built-in function call
#   max() — built-in function call
#   map() — built-in function call
#   operators: - /
#
# ===== EVALUATION ORDER =====
#   normalize_scores(score_list): looks up variable 'score_list', then runs the function body
#   normalize_scores(scores): looks up variable 'scores', then runs the function body
#   print(normalize_scores(?)): the inner normalize_scores() executes FIRST, then print() outputs the result
#   ? / ?: first get ?, then get ?, then compute ? / ?
#   ? / ?: first get ?, then get ?, then compute ? / ?
#   normalize_scores(?): resolves the value, then runs the function body
#   ? / ?: first get ?, then get ?, then compute ? / ?
#   score - min_score: first get score, then get min_score, then compute score - min_score
#   max_score - min_score: first get max_score, then get min_score, then compute max_score - min_score
#   score - min_score: first get score, then get min_score, then compute score - min_score
#   max_score - min_score: first get max_score, then get min_score, then compute max_score - min_score
#   score - min_score: first get score, then get min_score, then compute score - min_score
#   max_score - min_score: first get max_score, then get min_score, then compute max_score - min_score
#
# ===== EXECUTION ORDER =====
#   1. Define def normalize_scores(scores) — stored for later
#   2. Assign to score_list
#   3. Assign to normalized_scores
#   4. Run print('Original scores:', score_list)
#   5. Run print('Normalized scores:', normalized_scores)
#   6. Define def normalize_scores(scores) — stored for later
#   7. Define def normalize_scores(scores) — stored for later
#   8. Assign to scores
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(normalize_scores(...))
#       calls normalize_scores with: scores=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions normalize_scores, normalize_scores, normalize_scores are just stored
#   Execution starts at the non-def lines below:
#      score_list = ... (stored in memory)
#      normalized_scores = ... (stored in memory)
#      print('Original scores:', score_list)
#      print('Normalized scores:', normalized_scores)
#      scores = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(normalize_scores(?)) → function runs first, then print shows the result

"""

"""
Problem 323:
#
# ===== SYNTAX =====
#   def reverse_strings(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_strings(string_list): looks up variable 'string_list', then runs the function body
#   reverse_strings(strings): looks up variable 'strings', then runs the function body
#   print(reverse_strings('world')): the inner reverse_strings() executes FIRST, then print() outputs the result
#   reverse_strings('world'): gets the literal 'world', then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_strings(strings) — stored for later
#   2. Assign to string_list
#   3. Assign to reversed_strings
#   4. Run print('Original strings:', string_list)
#   5. Run print('Reversed strings:', reversed_strings)
#   6. Define def reverse_strings(strings) — stored for later
#   7. Define def reverse_strings(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(reverse_strings(...))
#       calls reverse_strings with: strings='world'
#         11.1 Assign to result
#         11.2 For loop over s
#         11.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_strings, reverse_strings, reverse_strings are just stored
#   Execution starts at the non-def lines below:
#      string_list = ... (stored in memory)
#      reversed_strings = ... (stored in memory)
#      print('Original strings:', string_list)
#      print('Reversed strings:', reversed_strings)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_strings('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 324:
#
# ===== SYNTAX =====
#   def double_numbers(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   double_numbers(number_list): looks up variable 'number_list', then runs the function body
#   double_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(double_numbers(?)): the inner double_numbers() executes FIRST, then print() outputs the result
#   double_numbers(?): resolves the value, then runs the function body
#   x * 2: first get x, then get 2, then compute x * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def double_numbers(numbers) — stored for later
#   2. Assign to number_list
#   3. Assign to doubled_numbers
#   4. Run print('Original numbers:', number_list)
#   5. Run print('Doubled numbers:', doubled_numbers)
#   6. Define def double_numbers(numbers) — stored for later
#   7. Define def double_numbers(numbers) — stored for later
#   8. Assign to numbers
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(double_numbers(...))
#       calls double_numbers with: numbers=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions double_numbers, double_numbers, double_numbers are just stored
#   Execution starts at the non-def lines below:
#      number_list = ... (stored in memory)
#      doubled_numbers = ... (stored in memory)
#      print('Original numbers:', number_list)
#      print('Doubled numbers:', doubled_numbers)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(double_numbers(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 325:
#
# ===== SYNTAX =====
#   def strip_whitespace(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   strip_whitespace(string_list): looks up variable 'string_list', then runs the function body
#   strip_whitespace(strings): looks up variable 'strings', then runs the function body
#   print(strip_whitespace('world')): the inner strip_whitespace() executes FIRST, then print() outputs the result
#   strip_whitespace('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def strip_whitespace(strings) — stored for later
#   2. Assign to string_list
#   3. Assign to stripped_strings
#   4. Run print('Original strings:', string_list)
#   5. Run print('Stripped strings:', stripped_strings)
#   6. Define def strip_whitespace(strings) — stored for later
#   7. Define def strip_whitespace(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(strip_whitespace(...))
#       calls strip_whitespace with: strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions strip_whitespace, strip_whitespace, strip_whitespace are just stored
#   Execution starts at the non-def lines below:
#      string_list = ... (stored in memory)
#      stripped_strings = ... (stored in memory)
#      print('Original strings:', string_list)
#      print('Stripped strings:', stripped_strings)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(strip_whitespace('world')) → function runs first, then print shows the result

"""

"""
Problem 326:
#
# ===== SYNTAX =====
#   def number_and_square(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   number_and_square(numbers_list): looks up variable 'numbers_list', then runs the function body
#   number_and_square(numbers): looks up variable 'numbers', then runs the function body
#   print(number_and_square(?)): the inner number_and_square() executes FIRST, then print() outputs the result
#   number_and_square(?): resolves the value, then runs the function body
#   x * x: first get x, then get x, then compute x * x
#   x * x: first get x, then get x, then compute x * x
#   x * x: first get x, then get x, then compute x * x
#
# ===== EXECUTION ORDER =====
#   1. Define def number_and_square(numbers) — stored for later
#   2. Assign to numbers_list
#   3. Assign to squared_tuples
#   4. Run print('Numbers and their squares:', squared_tuples)
#   5. Define def number_and_square(numbers) — stored for later
#   6. Define def number_and_square(numbers) — stored for later
#   7. Assign to numbers
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(number_and_square(...))
#       calls number_and_square with: numbers=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions number_and_square, number_and_square, number_and_square are just stored
#   Execution starts at the non-def lines below:
#      numbers_list = ... (stored in memory)
#      squared_tuples = ... (stored in memory)
#      print('Numbers and their squares:', squared_tuples)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(number_and_square(?)) → function runs first, then print shows the result

"""

"""
Problem 327:
#
# ===== SYNTAX =====
#   def to_title_case(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   str() — built-in function call
#
# ===== EVALUATION ORDER =====
#   to_title_case(words): looks up variable 'words', then runs the function body
#   to_title_case(strings): looks up variable 'strings', then runs the function body
#   print(to_title_case('world')): the inner to_title_case() executes FIRST, then print() outputs the result
#   to_title_case('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def to_title_case(strings) — stored for later
#   2. Assign to words
#   3. Assign to result
#   4. Run print(result)
#   5. Define def to_title_case(strings) — stored for later
#   6. Define def to_title_case(strings) — stored for later
#   7. Assign to strings
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(to_title_case(...))
#       calls to_title_case with: strings='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions to_title_case, to_title_case, to_title_case are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(to_title_case('world')) → function runs first, then print shows the result

"""

"""
Problem 328:
#
# ===== SYNTAX =====
#   def second_largest_number()
#   set() — built-in function call
#   list() — built-in function call
#   len() — built-in function call
#   input() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   second_largest_number(): — no arguments needed
#   len(...) > 1: is this True or False? The answer picks which branch
#   len(...) > 1: is this True or False? The answer picks which branch
#   len(...) > 1: is this True or False? The answer picks which branch
#   len(...) > 1: is this True or False? The answer picks which branch
#   len(...) > 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def second_largest_number() — stored for later
#   2. Run second_largest_number()
#         2.1 Assign to user_input
#         2.2 Assign to user_int
#         2.3 For loop over user_input
#         2.4 Assign to unique_numbers
#         2.5 Assign to unique_numbers
#         2.6 Run unique_numbers.sort()
#         2.7 If statement (one branch runs)
#         2.8 Assign to result
#         2.9 Return from function
#   3. Define def second_largest_number() — stored for later
#   4. Define def second_largest_number() — stored for later
#   5. Assign to user_input
#   6. Assign to user_int
#   7. For loop over user_input
#       7.1 Run user_int.append(int(...))
#   8. Assign to unique_numbers
#   9. Assign to unique_numbers
#   10. Run unique_numbers.sort()
#   11. If statement (one branch runs)
#   12. Run print(second_largest)
#   13. Assign to user_input
#   14. Assign to user_int
#   15. For loop over user_input
#       15.1 Run user_int.append(int(...))
#   16. Assign to unique_numbers
#   17. Assign to unique_numbers
#   18. Run unique_numbers.sort()
#   19. If statement (one branch runs)
#   20. Run print(second_largest)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_largest_number, second_largest_number, second_largest_number are just stored
#   Execution starts at the non-def lines below:
#      second_largest_number() → jumps into function, runs body, returns result → printed
#      user_input = ... (stored in memory)
#      user_int = ... (stored in memory)
#      for loop runs its body once per item
#      unique_numbers = ... (stored in memory)
#      unique_numbers = ... (stored in memory)
#      unique_numbers.sort()
#      if checks a condition and runs ONE branch
#      print(second_largest)
#      user_input = ... (stored in memory)
#      user_int = ... (stored in memory)
#      for loop runs its body once per item
#      unique_numbers = ... (stored in memory)
#      unique_numbers = ... (stored in memory)
#      unique_numbers.sort()
#      if checks a condition and runs ONE branch
#      print(second_largest)
#
#   Loop: grabs one item from user_input each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 329:
#
# ===== SYNTAX =====
#   def list_string(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   print(list_string(lst)): the inner list_string() executes FIRST, then print() outputs the result
#   type_smallest_biggest(): — no arguments needed
#   list_string(numbers): looks up variable 'numbers', then runs the function body
#   print(list_string(?)): the inner list_string() executes FIRST, then print() outputs the result
#   list_string(lst): looks up variable 'lst', then runs the function body
#   list_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_string(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(list_string(...))
#       calls list_string with: numbers=lst
#         3.1 Return from function
#   4. Define def type_smallest_biggest() — stored for later
#   5. Run type_smallest_biggest()
#         5.1 Assign to user
#         5.2 Assign to sort_number
#         5.3 Run print(sort_number)
#   6. Define def list_string(numbers) — stored for later
#   7. Define def list_string(numbers) — stored for later
#   8. Assign to numbers
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(list_string(...))
#       calls list_string with: numbers=[4, 1, 8, 6, 3]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_string, type_smallest_biggest, list_string, list_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_string(lst)) → function runs first, then print shows the result
#      type_smallest_biggest() → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_string(?)) → function runs first, then print shows the result

"""

"""
Problem 330:
#
# ===== SYNTAX =====
#   def list_of_string_numbers(n)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   input() — built-in function call
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   print(list_of_string_numbers(numbers)): the inner list_of_string_numbers() executes FIRST, then print() outputs the result
#   type_biggest_smallest(): — no arguments needed
#   list_of_string_numbers(n): looks up variable 'n', then runs the function body
#   print(list_of_string_numbers(15)): the inner list_of_string_numbers() executes FIRST, then print() outputs the result
#   list_of_string_numbers(numbers): looks up variable 'numbers', then runs the function body
#   list_of_string_numbers(15): gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_of_string_numbers(n) — stored for later
#   2. Assign to numbers
#   3. Run print(list_of_string_numbers(...))
#       calls list_of_string_numbers with: n=numbers
#         3.1 Return from function
#   4. Define def type_biggest_smallest() — stored for later
#   5. Run type_biggest_smallest()
#         5.1 Assign to user
#         5.2 Assign to int_list
#         5.3 Assign to sorted_list
#         5.4 Run print(sorted_list)
#   6. Define def list_of_string_numbers(n) — stored for later
#   7. Define def list_of_string_numbers(n) — stored for later
#   8. Assign to n
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(list_of_string_numbers(...))
#       calls list_of_string_numbers with: n=15
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_of_string_numbers, type_biggest_smallest, list_of_string_numbers, list_of_string_numbers are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(list_of_string_numbers(numbers)) → function runs first, then print shows the result
#      type_biggest_smallest() → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_of_string_numbers(15)) → function runs first, then print shows the result

"""

"""
Problem 331:
#
# ===== SYNTAX =====
#   def print_index_and_element(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_index_and_element() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_index_and_element(lst): looks up variable 'lst', then runs the function body
#   print_index_and_element(lst): looks up variable 'lst', then runs the function body
#   print(print_index_and_element(?)): the inner print_index_and_element() executes FIRST, then print() outputs the result
#   print_index_and_element(lst): looks up variable 'lst', then runs the function body
#   print_index_and_element(lst): looks up variable 'lst', then runs the function body
#   print_index_and_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_index_and_element(lst) — stored for later
#   2. Assign to lst
#   3. Run print_index_and_element(lst)
#       with: lst=lst
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_index_and_element(...))
#       calls print_index_and_element with: lst=[1, 3, 5, 7]
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_index_and_element, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print_index_and_element(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_index_and_element(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 332:
#
# ===== SYNTAX =====
#   def create_index_element_tuples(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   create_index_element_tuples(lst): looks up variable 'lst', then runs the function body
#   create_index_element_tuples(lst): looks up variable 'lst', then runs the function body
#   print(create_index_element_tuples(?)): the inner create_index_element_tuples() executes FIRST, then print() outputs the result
#   create_index_element_tuples(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_index_element_tuples(lst) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def create_index_element_tuples(lst) — stored for later
#   6. Define def create_index_element_tuples(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(create_index_element_tuples(...))
#       calls create_index_element_tuples with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_index_element_tuples, create_index_element_tuples, create_index_element_tuples are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_index_element_tuples(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 333:
#
# ===== SYNTAX =====
#   def print_with_index(strings)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_with_index() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_with_index(strings): looks up variable 'strings', then runs the function body
#   print_with_index(strings): looks up variable 'strings', then runs the function body
#   print(print_with_index('world')): the inner print_with_index() executes FIRST, then print() outputs the result
#   print_with_index(strings): looks up variable 'strings', then runs the function body
#   print_with_index(strings): looks up variable 'strings', then runs the function body
#   print_with_index('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_with_index(strings) — stored for later
#   2. Assign to strings
#   3. Run print_with_index(strings)
#       with: strings=strings
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_with_index(...))
#       calls print_with_index with: strings='world'
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_with_index, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      print_with_index(strings) → jumps into function, runs body, returns result → printed
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_with_index('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 334:
#
# ===== SYNTAX =====
#   def list_to_dict(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   list_to_dict(lst): looks up variable 'lst', then runs the function body
#   list_to_dict(lst): looks up variable 'lst', then runs the function body
#   print(list_to_dict(?)): the inner list_to_dict() executes FIRST, then print() outputs the result
#   list_to_dict(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_to_dict(lst) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def list_to_dict(lst) — stored for later
#   6. Define def list_to_dict(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(list_to_dict(...))
#       calls list_to_dict with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_to_dict, list_to_dict, list_to_dict are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_to_dict(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 335:
#
# ===== SYNTAX =====
#   def find_index(lst, element)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   find_index(lst, 'banana'): looks up variable 'lst', gets the literal 'banana', then runs the function body
#   find_index(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   print(find_index(?, 25)): the inner find_index() executes FIRST, then print() outputs the result
#   find_index(?, 25): resolves the value, gets the literal 25, then runs the function body
#   value == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_index(lst, element) — stored for later
#   2. Assign to lst
#   3. Assign to index
#   4. Run print(index)
#   5. Define def find_index(lst, element) — stored for later
#   6. Define def find_index(lst, element) — stored for later
#   7. Assign to lst
#   8. Assign to element
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(find_index(...))
#       calls find_index with: lst=[1, 3, 5, 7], element=25
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_index, find_index, find_index are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      index = ... (stored in memory)
#      print(index)
#      lst = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_index(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 336:
#
# ===== SYNTAX =====
#   def print_index_and_element_with_colon(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_index_and_element_with_colon() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_index_and_element_with_colon(lst): looks up variable 'lst', then runs the function body
#   print_index_and_element_with_colon(lst): looks up variable 'lst', then runs the function body
#   print(print_index_and_element_with_colon(?)): the inner print_index_and_element_with_colon() executes FIRST, then print() outputs the result
#   print_index_and_element_with_colon(lst): looks up variable 'lst', then runs the function body
#   print_index_and_element_with_colon(lst): looks up variable 'lst', then runs the function body
#   print_index_and_element_with_colon(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_index_and_element_with_colon(lst) — stored for later
#   2. Assign to lst
#   3. Run print_index_and_element_with_colon(lst)
#       with: lst=lst
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_index_and_element_with_colon(...))
#       calls print_index_and_element_with_colon with: lst=[1, 3, 5, 7]
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_index_and_element_with_colon, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print_index_and_element_with_colon(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_index_and_element_with_colon(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 337:
#
# ===== SYNTAX =====
#   def create_indexed_strings(lst, prefix)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   create_indexed_strings(lst, 'Item '): looks up variable 'lst', gets the literal 'Item ', then runs the function body
#   create_indexed_strings(lst, prefix): looks up variable 'lst', looks up variable 'prefix', then runs the function body
#   print(create_indexed_strings(?, 'Hi, ')): the inner create_indexed_strings() executes FIRST, then print() outputs the result
#   create_indexed_strings(?, 'Hi, '): resolves the value, gets the literal 'Hi, ', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def create_indexed_strings(lst, prefix) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def create_indexed_strings(lst, prefix) — stored for later
#   6. Define def create_indexed_strings(lst, prefix) — stored for later
#   7. Assign to lst
#   8. Assign to prefix
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(create_indexed_strings(...))
#       calls create_indexed_strings with: lst=[1, 3, 5, 7], prefix='Hi, '
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_indexed_strings, create_indexed_strings, create_indexed_strings are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      prefix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_indexed_strings(?, 'Hi, ')) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 338:
#
# ===== SYNTAX =====
#   def modify_elements(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   modify_elements(lst): looks up variable 'lst', then runs the function body
#   modify_elements(lst): looks up variable 'lst', then runs the function body
#   print(modify_elements(?)): the inner modify_elements() executes FIRST, then print() outputs the result
#   modify_elements(?): resolves the value, then runs the function body
#   index % 2: first get index, then get 2, then compute index % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def modify_elements(lst) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def modify_elements(lst) — stored for later
#   6. Define def modify_elements(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(modify_elements(...))
#       calls modify_elements with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions modify_elements, modify_elements, modify_elements are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(modify_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 340:
#
# ===== SYNTAX =====
#   def print_odd_index_elements(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_odd_index_elements() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   print_odd_index_elements(lst): looks up variable 'lst', then runs the function body
#   print_odd_index_elements(lst): looks up variable 'lst', then runs the function body
#   print(print_odd_index_elements(?)): the inner print_odd_index_elements() executes FIRST, then print() outputs the result
#   print_odd_index_elements(lst): looks up variable 'lst', then runs the function body
#   print_odd_index_elements(lst): looks up variable 'lst', then runs the function body
#   print_odd_index_elements(?): resolves the value, then runs the function body
#   index % 2: first get index, then get 2, then compute index % 2
#   index % 2: first get index, then get 2, then compute index % 2
#   index % 2: first get index, then get 2, then compute index % 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_odd_index_elements(lst) — stored for later
#   2. Assign to lst
#   3. Run print_odd_index_elements(lst)
#       with: lst=lst
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_odd_index_elements(...))
#       calls print_odd_index_elements with: lst=[1, 3, 5, 7]
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_odd_index_elements, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print_odd_index_elements(lst) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_odd_index_elements(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 341:
#
# ===== SYNTAX =====
#   def index_element_if_vowel(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   index_element_if_vowel(lst): looks up variable 'lst', then runs the function body
#   index_element_if_vowel(lst): looks up variable 'lst', then runs the function body
#   print(index_element_if_vowel(?)): the inner index_element_if_vowel() executes FIRST, then print() outputs the result
#   index_element_if_vowel(?): resolves the value, then runs the function body
#   ? in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def index_element_if_vowel(lst) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def index_element_if_vowel(lst) — stored for later
#   6. Define def index_element_if_vowel(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(index_element_if_vowel(...))
#       calls index_element_if_vowel with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions index_element_if_vowel, index_element_if_vowel, index_element_if_vowel are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(index_element_if_vowel(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 342:
#
# ===== SYNTAX =====
#   def find_max_and_index(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   find_max_and_index(lst): looks up variable 'lst', then runs the function body
#   find_max_and_index(lst): looks up variable 'lst', then runs the function body
#   print(find_max_and_index(?)): the inner find_max_and_index() executes FIRST, then print() outputs the result
#   find_max_and_index(?): resolves the value, then runs the function body
#   element > max_element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_max_and_index(lst) — stored for later
#   2. Assign to lst
#   3. Assign to ?
#   4. Run print(?)
#   5. Define def find_max_and_index(lst) — stored for later
#   6. Define def find_max_and_index(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(find_max_and_index(...))
#       calls find_max_and_index with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_max_and_index, find_max_and_index, find_max_and_index are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_max_and_index(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 343:
#
# ===== SYNTAX =====
#   def index_and_element_excluding_first_n(lst, n)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   index_and_element_excluding_first_n(lst, 2): looks up variable 'lst', gets the literal 2, then runs the function body
#   index_and_element_excluding_first_n(lst, n): looks up variable 'lst', looks up variable 'n', then runs the function body
#   print(index_and_element_excluding_first_n(?, 15)): the inner index_and_element_excluding_first_n() executes FIRST, then print() outputs the result
#   index_and_element_excluding_first_n(?, 15): resolves the value, gets the literal 15, then runs the function body
#   index >= n: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def index_and_element_excluding_first_n(lst, n) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def index_and_element_excluding_first_n(lst, n) — stored for later
#   6. Define def index_and_element_excluding_first_n(lst, n) — stored for later
#   7. Assign to lst
#   8. Assign to n
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(index_and_element_excluding_first_n(...))
#       calls index_and_element_excluding_first_n with: lst=[1, 3, 5, 7], n=15
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions index_and_element_excluding_first_n, index_and_element_excluding_first_n, index_and_element_excluding_first_n are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(index_and_element_excluding_first_n(?, 15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 344:
#
# ===== SYNTAX =====
#   def print_index_squared(numbers)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   print_index_squared() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: **
#
# ===== EVALUATION ORDER =====
#   print_index_squared(lst): looks up variable 'lst', then runs the function body
#   print_index_squared(numbers): looks up variable 'numbers', then runs the function body
#   print(print_index_squared(?)): the inner print_index_squared() executes FIRST, then print() outputs the result
#   print_index_squared(lst): looks up variable 'lst', then runs the function body
#   print_index_squared(lst): looks up variable 'lst', then runs the function body
#   print_index_squared(?): resolves the value, then runs the function body
#   number ** 2: first get number, then get 2, then compute number ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#   number ** 2: first get number, then get 2, then compute number ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def print_index_squared(numbers) — stored for later
#   2. Assign to lst
#   3. Run print_index_squared(lst)
#       with: numbers=lst
#         3.1 For loop over enumerate(...)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_index_squared(...))
#       calls print_index_squared with: numbers=[4, 1, 8, 6, 3]
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_index_squared, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print_index_squared(lst) → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_index_squared(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 345:
#
# ===== SYNTAX =====
#   def replace_based_on_index(lst)
#   return — exits the function and sends back a value
#   enumerate() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   replace_based_on_index(lst): looks up variable 'lst', then runs the function body
#   replace_based_on_index(lst): looks up variable 'lst', then runs the function body
#   print(replace_based_on_index(?)): the inner replace_based_on_index() executes FIRST, then print() outputs the result
#   replace_based_on_index(?): resolves the value, then runs the function body
#   index % 2: first get index, then get 2, then compute index % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def replace_based_on_index(lst) — stored for later
#   2. Assign to lst
#   3. Assign to result
#   4. Run print(result)
#   5. Define def replace_based_on_index(lst) — stored for later
#   6. Define def replace_based_on_index(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(replace_based_on_index(...))
#       calls replace_based_on_index with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions replace_based_on_index, replace_based_on_index, replace_based_on_index are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(replace_based_on_index(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 346:
#
# ===== SYNTAX =====
#   def sum_of_squares(a, b)
#   return — exits the function and sends back a value
#   square() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   sum_of_squares(3, 4): gets the literal 3, gets the literal 4, then runs the function body
#   sum_of_squares(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(sum_of_squares(20, 30)): the inner sum_of_squares() executes FIRST, then print() outputs the result
#   square(...) + square(...): first get square(...), then get square(...), then compute square(...) + square(...)
#   sum_of_squares(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x * x: first get x, then get x, then compute x * x
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_squares(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sum_of_squares(a, b) — stored for later
#   5. Define def sum_of_squares(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sum_of_squares(...))
#       calls sum_of_squares with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_squares, sum_of_squares, sum_of_squares are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_squares(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 347:
#
# ===== SYNTAX =====
#   def greet()
#   return — exits the function and sends back a value
#   get_name() — built-in function call
#   input() — built-in function call
#
# ===== EVALUATION ORDER =====
#   greet(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def greet() — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def greet() — stored for later
#   5. Define def greet() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet, greet, greet are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 348:
#
# ===== SYNTAX =====
#   def factorial(n)
#   return — exits the function and sends back a value
#   inner_factorial() — built-in function call
#   if/elif/else
#   operators: - * ==
#
# ===== EVALUATION ORDER =====
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(15): gets the literal 15, then runs the function body
#   x * inner_factorial(...): first get x, then get inner_factorial(...), then compute x * inner_factorial(...)
#   x - 1: first get x, then get 1, then compute x - 1
#   x == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def factorial(n) — stored for later
#   5. Define def factorial(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(factorial(...))
#       calls factorial with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 349:
#
# ===== SYNTAX =====
#   def add_prefix(prefix)
#   return — exits the function and sends back a value
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   add_prefix('pre-'): gets the literal 'pre-', then runs the function body
#   add_prefix('pre-'): gets the literal 'pre-', then runs the function body
#   add_prefix(prefix): looks up variable 'prefix', then runs the function body
#   print(add_prefix('Hi, ')): the inner add_prefix() executes FIRST, then print() outputs the result
#   '%s%s' % (prefix, string): first get '%s%s', then get (prefix, string), then compute '%s%s' % (prefix, string)
#   add_prefix('Hi, '): gets the literal 'Hi, ', then runs the function body
#   prefix + string: first get prefix, then get string, then compute prefix + string
#   prefix + string: first get prefix, then get string, then compute prefix + string
#
# ===== EXECUTION ORDER =====
#   1. Define def add_prefix(prefix) — stored for later
#   2. Assign to prefix_func
#   3. Assign to result
#   4. Run print(result)
#   5. Define def add_prefix(prefix) — stored for later
#   6. Assign to prefix_func
#   7. Assign to result1
#   8. Assign to result2
#   9. Assign to result3
#   10. Run print(result1)
#   11. Run print(result2)
#   12. Run print(result3)
#   13. Define def add_prefix(prefix) — stored for later
#   14. Define def add_prefix(prefix) — stored for later
#   15. Assign to prefix
#   16. Assign to result
#   17. Run print(result)
#   18. Run print(add_prefix(...))
#       calls add_prefix with: prefix='Hi, '
#         18.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_prefix, add_prefix, add_prefix, add_prefix are just stored
#   Execution starts at the non-def lines below:
#      prefix_func = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      prefix_func = ... (stored in memory)
#      result1 = ... (stored in memory)
#      result2 = ... (stored in memory)
#      result3 = ... (stored in memory)
#      print(result1)
#      print(result2)
#      print(result3)
#      prefix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_prefix('Hi, ')) → function runs first, then print shows the result

"""

"""
Problem 350:
#
# ===== SYNTAX =====
#   def check_even(num)
#   return — exits the function and sends back a value
#   is_even() — built-in function call
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   check_even(10): gets the literal 10, then runs the function body
#   check_even(num): looks up variable 'num', then runs the function body
#   print(check_even(25)): the inner check_even() executes FIRST, then print() outputs the result
#   check_even(25): gets the literal 25, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def check_even(num) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_even(num) — stored for later
#   5. Define def check_even(num) — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_even(...))
#       calls check_even with: num=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_even, check_even, check_even are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_even(25)) → function runs first, then print shows the result

"""

"""
Problem 351:
#
# ===== SYNTAX =====
#   def power(base, exponent)
#   return — exits the function and sends back a value
#   calculate_power() — built-in function call
#   pow() — built-in function call
#   operators: **
#
# ===== EVALUATION ORDER =====
#   power(2, 3): gets the literal 2, gets the literal 3, then runs the function body
#   power(base, exponent): looks up variable 'base', looks up variable 'exponent', then runs the function body
#   print(power(10, 25)): the inner power() executes FIRST, then print() outputs the result
#   power(10, 25): gets the literal 10, gets the literal 25, then runs the function body
#   b ** e: first get b, then get e, then compute b ** e
#
# ===== EXECUTION ORDER =====
#   1. Define def power(base, exponent) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def power(base, exponent) — stored for later
#   5. Define def power(base, exponent) — stored for later
#   6. Assign to base
#   7. Assign to exponent
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(power(...))
#       calls power with: base=10, exponent=25
#         10.1 Import math
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions power, power, power are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      base = ... (stored in memory)
#      exponent = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(power(10, 25)) → function runs first, then print shows the result

"""

"""
Problem 352:
#
# ===== SYNTAX =====
#   def area_circle(radius)
#   return — exits the function and sends back a value
#   calculate_area() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(area_circle(5)): the inner area_circle() executes FIRST, then print() outputs the result
#   area_circle(radius): looks up variable 'radius', then runs the function body
#   print(area_circle(14)): the inner area_circle() executes FIRST, then print() outputs the result
#   area_circle(5): gets the literal 5, then runs the function body
#   ? * r: first get ?, then get r, then compute ? * r
#   ? * r: first get ?, then get r, then compute ? * r
#   area_circle(14): gets the literal 14, then runs the function body
#   ? * r: first get ?, then get r, then compute ? * r
#   math.pi * r: first get math.pi, then get r, then compute math.pi * r
#   math.pi * r: first get math.pi, then get r, then compute math.pi * r
#   math.pi * r: first get math.pi, then get r, then compute math.pi * r
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def area_circle(radius) — stored for later
#   3. Run print(area_circle(...))
#       calls area_circle with: radius=5
#         3.1 Return from function
#   4. Define def area_circle(radius) — stored for later
#   5. Define def area_circle(radius) — stored for later
#   6. Assign to radius
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(area_circle(...))
#       calls area_circle with: radius=14
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions area_circle, area_circle, area_circle are just stored
#   Execution starts at the non-def lines below:
#      print(area_circle(5)) → function runs first, then print shows the result
#      radius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(area_circle(14)) → function runs first, then print shows the result

"""

"""
Problem 353:
#
# ===== SYNTAX =====
#   def capitalize_words(sentence)
#   return — exits the function and sends back a value
#   capitalize() — built-in function call
#   str() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   capitalize_words('hello world from python'): gets the literal 'hello world from python', then runs the function body
#   capitalize_words(sentence): looks up variable 'sentence', then runs the function body
#   print(capitalize_words('Python is fun')): the inner capitalize_words() executes FIRST, then print() outputs the result
#   capitalize_words('Python is fun'): gets the literal 'Python is fun', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_words(sentence) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def capitalize_words(sentence) — stored for later
#   5. Define def capitalize_words(sentence) — stored for later
#   6. Assign to sentence
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(capitalize_words(...))
#       calls capitalize_words with: sentence='Python is fun'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_words, capitalize_words, capitalize_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_words('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 354:
#
# ===== SYNTAX =====
#   def is_palindrome(word)
#   return — exits the function and sends back a value
#   reverse_string() — built-in function call
#   reversed() — built-in function call
#   for
#   in
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   is_palindrome('radar'): gets the literal 'radar', then runs the function body
#   is_palindrome(word): looks up variable 'word', then runs the function body
#   print(is_palindrome('coding')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome('coding'): gets the literal 'coding', then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(word) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_palindrome(word) — stored for later
#   5. Define def is_palindrome(word) — stored for later
#   6. Assign to word
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_palindrome(...))
#       calls is_palindrome with: word='coding'
#         9.1 Assign to result
#         9.2 For loop over s
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      word = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome('coding')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 355:
#
# ===== SYNTAX =====
#   def concatenate_strings(str1, str2)
#   return — exits the function and sends back a value
#   join_strings() — built-in function call
#   operators: + %
#
# ===== EVALUATION ORDER =====
#   concatenate_strings('Hello, ', 'world!'): gets the literal 'Hello, ', gets the literal 'world!', then runs the function body
#   concatenate_strings(str1, str2): looks up variable 'str1', looks up variable 'str2', then runs the function body
#   print(concatenate_strings('world', 'python')): the inner concatenate_strings() executes FIRST, then print() outputs the result
#   '%s%s' % (s1, s2): first get '%s%s', then get (s1, s2), then compute '%s%s' % (s1, s2)
#   concatenate_strings('world', 'python'): gets the literal 'world', gets the literal 'python', then runs the function body
#   s1 + s2: first get s1, then get s2, then compute s1 + s2
#
# ===== EXECUTION ORDER =====
#   1. Define def concatenate_strings(str1, str2) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def concatenate_strings(str1, str2) — stored for later
#   5. Define def concatenate_strings(str1, str2) — stored for later
#   6. Assign to str1
#   7. Assign to str2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(concatenate_strings(...))
#       calls concatenate_strings with: str1='world', str2='python'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions concatenate_strings, concatenate_strings, concatenate_strings are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      str1 = ... (stored in memory)
#      str2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(concatenate_strings('world', 'python')) → function runs first, then print shows the result

"""

"""
Problem 356:
#
# ===== SYNTAX =====
#   def fibonacci(n)
#   return — exits the function and sends back a value
#   fib() — built-in function call
#   range() — built-in function call
#   comprehension
#   if/elif/else
#   operators: + - <=
#
# ===== EVALUATION ORDER =====
#   fibonacci(10): gets the literal 10, then runs the function body
#   fibonacci(n): looks up variable 'n', then runs the function body
#   print(fibonacci(15)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(15): gets the literal 15, then runs the function body
#   fib(...) + fib(...): first get fib(...), then get fib(...), then compute fib(...) + fib(...)
#   x - 1: first get x, then get 1, then compute x - 1
#   x - 2: first get x, then get 2, then compute x - 2
#   x <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def fibonacci(n) — stored for later
#   5. Define def fibonacci(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(fibonacci(...))
#       calls fibonacci with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci, fibonacci, fibonacci are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 357:
#
# ===== SYNTAX =====
#   def to_uppercase(string)
#   return — exits the function and sends back a value
#   convert() — built-in function call
#   chr() — built-in function call
#   ord() — built-in function call
#   str() — built-in function call
#   comprehension
#   operators: - <=
#
# ===== EVALUATION ORDER =====
#   to_uppercase('hello world'): gets the literal 'hello world', then runs the function body
#   to_uppercase(string): looks up variable 'string', then runs the function body
#   print(to_uppercase('world')): the inner to_uppercase() executes FIRST, then print() outputs the result
#   to_uppercase('world'): gets the literal 'world', then runs the function body
#   ord(...) - 32: first get ord(...), then get 32, then compute ord(...) - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def to_uppercase(string) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def to_uppercase(string) — stored for later
#   5. Define def to_uppercase(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(to_uppercase(...))
#       calls to_uppercase with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions to_uppercase, to_uppercase, to_uppercase are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(to_uppercase('world')) → function runs first, then print shows the result

"""

"""
Problem 358:
#
# ===== SYNTAX =====
#   def find_largest(a, b)
#   return — exits the function and sends back a value
#   compare() — built-in function call
#   if/elif/else
#   operators: >
#
# ===== EVALUATION ORDER =====
#   find_largest(10, 20): gets the literal 10, gets the literal 20, then runs the function body
#   find_largest(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(find_largest(20, 30)): the inner find_largest() executes FIRST, then print() outputs the result
#   find_largest(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x > y: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_largest(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_largest(a, b) — stored for later
#   5. Define def find_largest(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(find_largest(...))
#       calls find_largest with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_largest, find_largest, find_largest are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_largest(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 359:
#
# ===== SYNTAX =====
#   def calculator(operation, a, b)
#   return — exits the function and sends back a value
#   add() — built-in function call
#   subtract() — built-in function call
#   multiply() — built-in function call
#   divide() — built-in function call
#   sum() — built-in function call
#   if/elif/else
#   operators: + - * / == !=
#
# ===== EVALUATION ORDER =====
#   calculator('add', 5, 3): gets the literal 'add', gets the literal 5, gets the literal 3, then runs the function body
#   calculator(operation, a, b): looks up variable 'operation', looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculator(25, 20, 30)): the inner calculator() executes FIRST, then print() outputs the result
#   calculator(25, 20, 30): gets the literal 25, gets the literal 20, gets the literal 30, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#   x - y: first get x, then get y, then compute x - y
#   x * y: first get x, then get y, then compute x * y
#   x / y: first get x, then get y, then compute x / y
#   operation == 'add': is this True or False? The answer picks which branch
#   operation == 'subtract': is this True or False? The answer picks which branch
#   operation == 'multiply': is this True or False? The answer picks which branch
#   operation == 'divide': is this True or False? The answer picks which branch
#   y != 0: is this True or False? The answer picks which branch
#   operation == 'subtract': is this True or False? The answer picks which branch
#   operation == 'multiply': is this True or False? The answer picks which branch
#   operation == 'divide': is this True or False? The answer picks which branch
#   operation == 'multiply': is this True or False? The answer picks which branch
#   operation == 'divide': is this True or False? The answer picks which branch
#   operation == 'divide': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def calculator(operation, a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculator(operation, a, b) — stored for later
#   5. Define def calculator(operation, a, b) — stored for later
#   6. Assign to operation
#   7. Assign to a
#   8. Assign to b
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculator(...))
#       calls calculator with: operation=25, a=20, b=30
#         11.1 Import operator
#         11.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculator, calculator, calculator are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      operation = ... (stored in memory)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculator(25, 20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 360:
#
# ===== SYNTAX =====
#   def is_prime(n)
#   return — exits the function and sends back a value
#   check_divisibility() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + % ** == <
#
# ===== EVALUATION ORDER =====
#   is_prime(7): gets the literal 7, then runs the function body
#   is_prime(n): looks up variable 'n', then runs the function body
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(15): gets the literal 15, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   x % i: first get x, then get i, then compute x % i
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x < 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_prime(n) — stored for later
#   5. Define def is_prime(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_prime(...))
#       calls is_prime with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 361:
#
# ===== SYNTAX =====
#   def count_vowels(string)
#   return — exits the function and sends back a value
#   is_vowel() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   count_vowels('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_vowels(string): looks up variable 'string', then runs the function body
#   print(count_vowels('world')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(string) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_vowels(string) — stored for later
#   5. Define def count_vowels(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_vowels(...))
#       calls count_vowels with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from string each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 362:
#
# ===== SYNTAX =====
#   def calculate_average(numbers)
#   return — exits the function and sends back a value
#   sum_numbers() — built-in function call
#   len() — built-in function call
#   for
#   in
#   operators: /
#
# ===== EVALUATION ORDER =====
#   calculate_average(?): resolves the value, then runs the function body
#   calculate_average(numbers): looks up variable 'numbers', then runs the function body
#   print(calculate_average(?)): the inner calculate_average() executes FIRST, then print() outputs the result
#   sum_numbers(...) / len(...): first get sum_numbers(...), then get len(...), then compute sum_numbers(...) / len(...)
#   calculate_average(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_average(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_average(numbers) — stored for later
#   5. Define def calculate_average(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_average(...))
#       calls calculate_average with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_average, calculate_average, calculate_average are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_average(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from nums each round

"""

"""
Problem 363:
#
# ===== SYNTAX =====
#   def reverse_string()
#   return — exits the function and sends back a value
#   reverse() — built-in function call
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_string(): — no arguments needed
#   print(reverse_func('Hello, World!')): inner expression resolves first, then print() outputs
#   reverse_string('Hello'): gets the literal 'Hello', then runs the function body
#   reverse_string(string): looks up variable 'string', then runs the function body
#   print(reverse_string('world')): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string('world'): gets the literal 'world', then runs the function body
#   char + result: first get char, then get result, then compute char + result
#   char + reversed_str: first get char, then get reversed_str, then compute char + reversed_str
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_string() — stored for later
#   2. Assign to reverse_func
#   3. Run print(reverse_func(...))
#   4. Define def reverse_string(string) — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Define def reverse_string() — stored for later
#   8. Define def reverse_string() — stored for later
#   9. Assign to string
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(reverse_string(...))
#         12.1 Assign to result
#         12.2 For loop over s
#         12.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_string, reverse_string, reverse_string, reverse_string are just stored
#   Execution starts at the non-def lines below:
#      reverse_func = ... (stored in memory)
#      print(reverse_func(...))
#      result = ... (stored in memory)
#      print(result)
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_string('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 364:
#
# ===== SYNTAX =====
#   def is_sorted(lst)
#   return — exits the function and sends back a value
#   compare_elements() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + - == >
#
# ===== EVALUATION ORDER =====
#   is_sorted(?): resolves the value, then runs the function body
#   is_sorted(): — no arguments needed
#   is_sorted(lst): looks up variable 'lst', then runs the function body
#   print(is_sorted(?)): the inner is_sorted() executes FIRST, then print() outputs the result
#   compare_elements(): — no arguments needed
#   compare_elements(lst): looks up variable 'lst', then runs the function body
#   is_sorted(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   i + 1: first get i, then get 1, then compute i + 1
#   lst == sorted(...): is this True or False? The answer picks which branch
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_sorted(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_sorted() — stored for later
#   5. Define def compare_elements(lst) — stored for later
#   6. Assign to lst
#   7. Run is_sorted()
#       with: 
#         7.1 Return from function
#   8. Define def is_sorted(lst) — stored for later
#   9. Define def is_sorted(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(is_sorted(...))
#       calls is_sorted with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_sorted, is_sorted, compare_elements, is_sorted, is_sorted are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      is_sorted() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_sorted(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 365:
#
# ===== SYNTAX =====
#   def generate_squares(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   square() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   generate_squares(5): gets the literal 5, then runs the function body
#   generate_squares(n): looks up variable 'n', then runs the function body
#   print(generate_squares(15)): the inner generate_squares() executes FIRST, then print() outputs the result
#   generate_squares(15): gets the literal 15, then runs the function body
#   x * x: first get x, then get x, then compute x * x
#   n + 1: first get n, then get 1, then compute n + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def generate_squares(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def generate_squares(n) — stored for later
#   5. Define def generate_squares(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(generate_squares(...))
#       calls generate_squares with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions generate_squares, generate_squares, generate_squares are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(generate_squares(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 366:
#
# ===== SYNTAX =====
#   def sum_of_two(a, b)
#   return — exits the function and sends back a value
#   add() — built-in function call
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_of_two(3, 5): gets the literal 3, gets the literal 5, then runs the function body
#   sum_of_two(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(sum_of_two(20, 30)): the inner sum_of_two() executes FIRST, then print() outputs the result
#   sum_of_two(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_two(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sum_of_two(a, b) — stored for later
#   5. Define def sum_of_two(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sum_of_two(...))
#       calls sum_of_two with: a=20, b=30
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_two, sum_of_two, sum_of_two are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_two(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 367:
#
# ===== SYNTAX =====
#   def even_or_odd(n)
#   return — exits the function and sends back a value
#   is_even() — built-in function call
#   if/elif/else
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   even_or_odd(10): gets the literal 10, then runs the function body
#   even_or_odd(n): looks up variable 'n', then runs the function body
#   print(even_or_odd(15)): the inner even_or_odd() executes FIRST, then print() outputs the result
#   even_or_odd(15): gets the literal 15, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def even_or_odd(n) — stored for later
#   2. Run even_or_odd(10)
#       with: n=10
#         2.1 Return from function
#   3. Define def even_or_odd(n) — stored for later
#   4. Define def even_or_odd(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(even_or_odd(...))
#       calls even_or_odd with: n=15
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_or_odd, even_or_odd, even_or_odd are just stored
#   Execution starts at the non-def lines below:
#      even_or_odd(10) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_or_odd(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 368:
#
# ===== SYNTAX =====
#   def convert_temperature(celsius)
#   return — exits the function and sends back a value
#   celsius_to_fahrenheit() — built-in function call
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   convert_temperature(25): gets the literal 25, then runs the function body
#   convert_temperature(celsius): looks up variable 'celsius', then runs the function body
#   print(convert_temperature(25)): the inner convert_temperature() executes FIRST, then print() outputs the result
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   convert_temperature(25): gets the literal 25, then runs the function body
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   c * 9: first get c, then get 9, then compute c * 9
#   c * 9: first get c, then get 9, then compute c * 9
#   c * 9: first get c, then get 9, then compute c * 9
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_temperature(celsius) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_temperature(celsius) — stored for later
#   5. Define def convert_temperature(celsius) — stored for later
#   6. Assign to celsius
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_temperature(...))
#       calls convert_temperature with: celsius=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_temperature, convert_temperature, convert_temperature are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      celsius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_temperature(25)) → function runs first, then print shows the result

"""

"""
Problem 369:
#
# ===== SYNTAX =====
#   def rectangle_area(length, width)
#   return — exits the function and sends back a value
#   calculate_area() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   rectangle_area(5, 10): gets the literal 5, gets the literal 10, then runs the function body
#   rectangle_area(length, width): looks up variable 'length', looks up variable 'width', then runs the function body
#   print(rectangle_area(25, 25)): the inner rectangle_area() executes FIRST, then print() outputs the result
#   rectangle_area(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   l * w: first get l, then get w, then compute l * w
#
# ===== EXECUTION ORDER =====
#   1. Define def rectangle_area(length, width) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def rectangle_area(length, width) — stored for later
#   5. Define def rectangle_area(length, width) — stored for later
#   6. Assign to length
#   7. Assign to width
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(rectangle_area(...))
#       calls rectangle_area with: length=25, width=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions rectangle_area, rectangle_area, rectangle_area are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(rectangle_area(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 370:
#
# ===== SYNTAX =====
#   def check_prime(n)
#   return — exits the function and sends back a value
#   is_prime() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + % ** == <
#
# ===== EVALUATION ORDER =====
#   check_prime(11): gets the literal 11, then runs the function body
#   check_prime(n): looks up variable 'n', then runs the function body
#   print(check_prime(15)): the inner check_prime() executes FIRST, then print() outputs the result
#   check_prime(15): gets the literal 15, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   num % i: first get num, then get i, then compute num % i
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   num < 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_prime(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_prime(n) — stored for later
#   5. Define def check_prime(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_prime(...))
#       calls check_prime with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_prime, check_prime, check_prime are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_prime(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 371:
#
# ===== SYNTAX =====
#   def find_minimum(a, b, c)
#   return — exits the function and sends back a value
#   min_of_three() — built-in function call
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   find_minimum(5, 3, 8): gets the literal 5, gets the literal 3, gets the literal 8, then runs the function body
#   find_minimum(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(find_minimum(20, 30, 10)): the inner find_minimum() executes FIRST, then print() outputs the result
#   find_minimum(20, 30, 10): gets the literal 20, gets the literal 30, gets the literal 10, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_minimum(a, b, c) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def find_minimum(a, b, c) — stored for later
#   5. Define def find_minimum(a, b, c) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to c
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(find_minimum(...))
#       calls find_minimum with: a=20, b=30, c=10
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_minimum, find_minimum, find_minimum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_minimum(20, 30, 10)) → function runs first, then print shows the result

"""

"""
Problem 373:
#
# ===== SYNTAX =====
#   def calculate_factorial(n)
#   return — exits the function and sends back a value
#   factorial() — built-in function call
#   range() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + == or
#
# ===== EVALUATION ORDER =====
#   calculate_factorial(5): gets the literal 5, then runs the function body
#   calculate_factorial(n): looks up variable 'n', then runs the function body
#   print(calculate_factorial(15)): the inner calculate_factorial() executes FIRST, then print() outputs the result
#   calculate_factorial(15): gets the literal 15, then runs the function body
#   num + 1: first get num, then get 1, then compute num + 1
#   num == 0: is this True or False? The answer picks which branch
#   num == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_factorial(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_factorial(n) — stored for later
#   5. Define def calculate_factorial(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_factorial(...))
#       calls calculate_factorial with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_factorial, calculate_factorial, calculate_factorial are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_factorial(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 374:
#
# ===== SYNTAX =====
#   def capitalize_words(sentence)
#   return — exits the function and sends back a value
#   capitalize() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   capitalize_words('hello world from python'): gets the literal 'hello world from python', then runs the function body
#   capitalize_words(sentence): looks up variable 'sentence', then runs the function body
#   print(capitalize_words('Python is fun')): the inner capitalize_words() executes FIRST, then print() outputs the result
#   ?.upper(...) + ?: first get ?.upper(...), then get ?, then compute ?.upper(...) + ?
#   ?.upper(...) + ?: first get ?.upper(...), then get ?, then compute ?.upper(...) + ?
#   capitalize_words('Python is fun'): gets the literal 'Python is fun', then runs the function body
#   ?.upper(...) + ?: first get ?.upper(...), then get ?, then compute ?.upper(...) + ?
#
# ===== EXECUTION ORDER =====
#   1. Define def capitalize_words(sentence) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def capitalize_words(sentence) — stored for later
#   5. Define def capitalize_words(sentence) — stored for later
#   6. Assign to sentence
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(capitalize_words(...))
#       calls capitalize_words with: sentence='Python is fun'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions capitalize_words, capitalize_words, capitalize_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(capitalize_words('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 375:
#
# ===== SYNTAX =====
#   def max_in_list(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   max_in_list() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(find_largest()): the inner find_largest() executes FIRST, then print() outputs the result
#   print(find_largest()): the inner find_largest() executes FIRST, then print() outputs the result
#   max_in_list(lst): looks up variable 'lst', then runs the function body
#   print(max_in_list(?)): the inner max_in_list() executes FIRST, then print() outputs the result
#   max_in_list(lst): looks up variable 'lst', then runs the function body
#   find_largest(): — no arguments needed
#   max_in_list(numbers): looks up variable 'numbers', then runs the function body
#   find_largest(): — no arguments needed
#   max_in_list(?): resolves the value, then runs the function body
#   item > result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_in_list(lst) — stored for later
#   2. Define def find_largest() — stored for later
#   3. Assign to lst
#   4. Run print(find_largest(...))
#         4.1 Assign to numbers
#         4.2 Assign to largest
#         4.3 Return from function
#   5. Define def max_in_list(lst) — stored for later
#   6. Define def find_largest() — stored for later
#   7. Run print(find_largest(...))
#         7.1 Assign to numbers
#         7.2 Assign to largest
#         7.3 Return from function
#   8. Define def max_in_list(lst) — stored for later
#   9. Define def max_in_list(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(max_in_list(...))
#       calls max_in_list with: lst=[1, 3, 5, 7]
#         13.1 Assign to result
#         13.2 For loop over lst
#         13.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_in_list, find_largest, max_in_list, find_largest, max_in_list, max_in_list are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_largest()) → function runs first, then print shows the result
#      print(find_largest()) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_in_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 376:
#
# ===== SYNTAX =====
#   def reverse_string(s)
#   return — exits the function and sends back a value
#   reverse() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_string('hello'): gets the literal 'hello', then runs the function body
#   reverse_string(s): looks up variable 's', then runs the function body
#   print(reverse_string(?)): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(?): resolves the value, then runs the function body
#   char + reversed_str: first get char, then get reversed_str, then compute char + reversed_str
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_string(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def reverse_string(s) — stored for later
#   5. Define def reverse_string(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_string(...))
#       calls reverse_string with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_string, reverse_string, reverse_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_string(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 377:
#
# ===== SYNTAX =====
#   def calculate_average(lst)
#   return — exits the function and sends back a value
#   average() — built-in function call
#   len() — built-in function call
#   for
#   in
#   operators: /
#
# ===== EVALUATION ORDER =====
#   calculate_average(?): resolves the value, then runs the function body
#   calculate_average(lst): looks up variable 'lst', then runs the function body
#   print(calculate_average(?)): the inner calculate_average() executes FIRST, then print() outputs the result
#   total / len(...): first get total, then get len(...), then compute total / len(...)
#   total / len(...): first get total, then get len(...), then compute total / len(...)
#   calculate_average(?): resolves the value, then runs the function body
#   total / len(...): first get total, then get len(...), then compute total / len(...)
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_average(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_average(lst) — stored for later
#   5. Define def calculate_average(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_average(...))
#       calls calculate_average with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_average, calculate_average, calculate_average are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_average(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 378:
#
# ===== SYNTAX =====
#   def check_palindrome(s)
#   return — exits the function and sends back a value
#   is_palindrome() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   check_palindrome('racecar'): gets the literal 'racecar', then runs the function body
#   check_palindrome(s): looks up variable 's', then runs the function body
#   print(check_palindrome(?)): the inner check_palindrome() executes FIRST, then print() outputs the result
#   check_palindrome(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def check_palindrome(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_palindrome(s) — stored for later
#   5. Define def check_palindrome(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_palindrome(...))
#       calls check_palindrome with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_palindrome, check_palindrome, check_palindrome are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_palindrome(?)) → function runs first, then print shows the result

"""

"""
Problem 379:
#
# ===== SYNTAX =====
#   def closest_to_zero(numbers)
#   return — exits the function and sends back a value
#   abs() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   closest_to_zero(?): resolves the value, then runs the function body
#   closest_to_zero(numbers): looks up variable 'numbers', then runs the function body
#   print(closest_to_zero(?)): the inner closest_to_zero() executes FIRST, then print() outputs the result
#   closest_to_zero(?): resolves the value, then runs the function body
#   abs(...) < abs(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def closest_to_zero(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def closest_to_zero(numbers) — stored for later
#   5. Define def closest_to_zero(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(closest_to_zero(...))
#       calls closest_to_zero with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions closest_to_zero, closest_to_zero, closest_to_zero are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(closest_to_zero(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 380:
#
# ===== SYNTAX =====
#   def check_sorted(lst)
#   return — exits the function and sends back a value
#   is_sorted() — built-in function call
#   range() — built-in function call
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + - >
#
# ===== EVALUATION ORDER =====
#   check_sorted(?): resolves the value, then runs the function body
#   check_sorted(lst): looks up variable 'lst', then runs the function body
#   print(check_sorted(?)): the inner check_sorted() executes FIRST, then print() outputs the result
#   check_sorted(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   i + 1: first get i, then get 1, then compute i + 1
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_sorted(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_sorted(lst) — stored for later
#   5. Define def check_sorted(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_sorted(...))
#       calls check_sorted with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_sorted, check_sorted, check_sorted are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_sorted(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 381:
#
# ===== SYNTAX =====
#   def convert_to_integers(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(list(map(...))): inner expression resolves first, then print() outputs
#   print(convert_to_integers(numbers_in_strings)): the inner convert_to_integers() executes FIRST, then print() outputs the result
#   convert_to_integers(strings): looks up variable 'strings', then runs the function body
#   print(convert_to_integers('world')): the inner convert_to_integers() executes FIRST, then print() outputs the result
#   convert_to_integers(numbers_in_strings): looks up variable 'numbers_in_strings', then runs the function body
#   convert_to_integers('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to user
#   2. Run print(list(...))
#   3. Define def convert_to_integers(strings) — stored for later
#   4. Assign to numbers_in_strings
#   5. Run print(convert_to_integers(...))
#       calls convert_to_integers with: strings=numbers_in_strings
#         5.1 Return from function
#   6. Define def convert_to_integers(strings) — stored for later
#   7. Define def convert_to_integers(strings) — stored for later
#   8. Assign to strings
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(convert_to_integers(...))
#       calls convert_to_integers with: strings='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_integers, convert_to_integers, convert_to_integers are just stored
#   Execution starts at the non-def lines below:
#      user = ... (stored in memory)
#      print(list(...))
#      numbers_in_strings = ... (stored in memory)
#      print(convert_to_integers(numbers_in_strings)) → function runs first, then print shows the result
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_integers('world')) → function runs first, then print shows the result

"""

"""
Problem 382:
#
# ===== SYNTAX =====
#   def sort_strings_by_length(strings)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_strings_by_length(?): resolves the value, then runs the function body
#   sort_strings_by_length(strings): looks up variable 'strings', then runs the function body
#   print(sort_strings_by_length(?)): the inner sort_strings_by_length() executes FIRST, then print() outputs the result
#   sort_strings_by_length(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_strings_by_length(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_strings_by_length(strings) — stored for later
#   5. Define def sort_strings_by_length(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_strings_by_length(...))
#       calls sort_strings_by_length with: strings=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_strings_by_length, sort_strings_by_length, sort_strings_by_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_strings_by_length(?)) → function runs first, then print shows the result

"""

"""
Problem 384:
#
# ===== SYNTAX =====
#   def sort_tuples_by_second(tuples)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   sort_tuples_by_second(?): resolves the value, then runs the function body
#   sort_tuples_by_second(tuples): looks up variable 'tuples', then runs the function body
#   print(sort_tuples_by_second(?)): the inner sort_tuples_by_second() executes FIRST, then print() outputs the result
#   sort_tuples_by_second(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_tuples_by_second(tuples) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_tuples_by_second(tuples) — stored for later
#   5. Define def sort_tuples_by_second(tuples) — stored for later
#   6. Assign to tuples
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_tuples_by_second(...))
#       calls sort_tuples_by_second with: tuples=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_tuples_by_second, sort_tuples_by_second, sort_tuples_by_second are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      tuples = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_tuples_by_second(?)) → function runs first, then print shows the result

"""

"""
Problem 385:
#
# ===== SYNTAX =====
#   def sort_dicts_by_key(dicts, key_name)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   sort_dicts_by_key(?, 'age'): resolves the value, gets the literal 'age', then runs the function body
#   sort_dicts_by_key(dicts, key_name): looks up variable 'dicts', looks up variable 'key_name', then runs the function body
#   print(sort_dicts_by_key(?, ?)): the inner sort_dicts_by_key() executes FIRST, then print() outputs the result
#   sort_dicts_by_key(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_dicts_by_key(dicts, key_name) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_dicts_by_key(dicts, key_name) — stored for later
#   5. Define def sort_dicts_by_key(dicts, key_name) — stored for later
#   6. Assign to dicts
#   7. Assign to key_name
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_dicts_by_key(...))
#       calls sort_dicts_by_key with: dicts=[4, 1, 8, 6, 3], key_name=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_dicts_by_key, sort_dicts_by_key, sort_dicts_by_key are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      dicts = ... (stored in memory)
#      key_name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_dicts_by_key(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 386:
#
# ===== SYNTAX =====
#   def longest_word(words)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   longest_word(?): resolves the value, then runs the function body
#   longest_word(words): looks up variable 'words', then runs the function body
#   print(longest_word(?)): the inner longest_word() executes FIRST, then print() outputs the result
#   longest_word(?): resolves the value, then runs the function body
#   len(...) > len(...): is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def longest_word(words) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def longest_word(words) — stored for later
#   5. Define def longest_word(words) — stored for later
#   6. Assign to words
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(longest_word(...))
#       calls longest_word with: words=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions longest_word, longest_word, longest_word are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(longest_word(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 387:
#
# ===== SYNTAX =====
#   def sort_by_absolute_value(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_by_absolute_value(?): resolves the value, then runs the function body
#   sort_by_absolute_value(numbers): looks up variable 'numbers', then runs the function body
#   print(sort_by_absolute_value(?)): the inner sort_by_absolute_value() executes FIRST, then print() outputs the result
#   sort_by_absolute_value(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_absolute_value(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_by_absolute_value(numbers) — stored for later
#   5. Define def sort_by_absolute_value(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_by_absolute_value(...))
#       calls sort_by_absolute_value with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_absolute_value, sort_by_absolute_value, sort_by_absolute_value are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_absolute_value(?)) → function runs first, then print shows the result

"""

"""
Problem 388:
#
# ===== SYNTAX =====
#   def sort_strings_by_last_character(strings)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   list() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   sort_strings_by_last_character(?): resolves the value, then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   sort_strings_by_last_character(strings): looks up variable 'strings', then runs the function body
#   print(sort_strings_by_last_character(?)): the inner sort_strings_by_last_character() executes FIRST, then print() outputs the result
#   sort_strings_by_last_character(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_strings_by_last_character(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def main(lst) — stored for later
#   5. Assign to lst
#   6. Run main(lst)
#       with: lst=lst
#         6.1 Define def inner(word) — stored for later
#         6.2 Return from function
#   7. Define def sort_strings_by_last_character(strings) — stored for later
#   8. Define def sort_strings_by_last_character(strings) — stored for later
#   9. Assign to strings
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(sort_strings_by_last_character(...))
#       calls sort_strings_by_last_character with: strings=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_strings_by_last_character, main, sort_strings_by_last_character, sort_strings_by_last_character are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_strings_by_last_character(?)) → function runs first, then print shows the result

"""

"""
Problem 389:
#
# ===== SYNTAX =====
#   def earliest_date(dates)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   earliest_date(?): resolves the value, then runs the function body
#   earliest_date(dates): looks up variable 'dates', then runs the function body
#   print(earliest_date(?)): the inner earliest_date() executes FIRST, then print() outputs the result
#   earliest_date(?): resolves the value, then runs the function body
#   item < result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def earliest_date(dates) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def earliest_date(dates) — stored for later
#   5. Define def earliest_date(dates) — stored for later
#   6. Assign to dates
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(earliest_date(...))
#       calls earliest_date with: dates=[4, 1, 8, 6, 3]
#         9.1 Assign to result
#         9.2 For loop over dates
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions earliest_date, earliest_date, earliest_date are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      dates = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(earliest_date(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from dates each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 390:
#
# ===== SYNTAX =====
#   def get_surname(name)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_surname(name): looks up variable 'name', then runs the function body
#   print(get_surname('Bob')): the inner get_surname() executes FIRST, then print() outputs the result
#   get_surname('Bob'): gets the literal 'Bob', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_surname(name) — stored for later
#   2. Assign to names
#   3. Assign to sorted_names
#   4. Run print(sorted_names)
#   5. Define def get_surname(name) — stored for later
#   6. Define def get_surname(name) — stored for later
#   7. Assign to name
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(get_surname(...))
#       calls get_surname with: name='Bob'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_surname, get_surname, get_surname are just stored
#   Execution starts at the non-def lines below:
#      names = ... (stored in memory)
#      sorted_names = ... (stored in memory)
#      print(sorted_names)
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_surname('Bob')) → function runs first, then print shows the result

"""

"""
Problem 391:
#
# ===== SYNTAX =====
#   def sort_strings_alphabetically(strings)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_strings_alphabetically(strings): looks up variable 'strings', then runs the function body
#   sort_strings_alphabetically(strings): looks up variable 'strings', then runs the function body
#   print(sort_strings_alphabetically(?)): the inner sort_strings_alphabetically() executes FIRST, then print() outputs the result
#   sort_strings_alphabetically(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_strings_alphabetically(strings) — stored for later
#   2. Assign to strings
#   3. Assign to sorted_strings
#   4. Run print(sorted_strings)
#   5. Define def sort_strings_alphabetically(strings) — stored for later
#   6. Define def sort_strings_alphabetically(strings) — stored for later
#   7. Assign to strings
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_strings_alphabetically(...))
#       calls sort_strings_alphabetically with: strings=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_strings_alphabetically, sort_strings_alphabetically, sort_strings_alphabetically are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      sorted_strings = ... (stored in memory)
#      print(sorted_strings)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_strings_alphabetically(?)) → function runs first, then print shows the result

"""

"""
Problem 392:
#
# ===== SYNTAX =====
#   def scrabble_score(word)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   find_highest_scrabble_word(?): resolves the value, then runs the function body
#   scrabble_score(word): looks up variable 'word', then runs the function body
#   print(scrabble_score('coding')): the inner scrabble_score() executes FIRST, then print() outputs the result
#   scrabble_score('coding'): gets the literal 'coding', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def scrabble_score(word) — stored for later
#   2. Define def find_highest_scrabble_word(words) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def scrabble_score(word) — stored for later
#   6. Define def scrabble_score(word) — stored for later
#   7. Assign to word
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(scrabble_score(...))
#       calls scrabble_score with: word='coding'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions scrabble_score, find_highest_scrabble_word, scrabble_score, scrabble_score are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      word = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(scrabble_score('coding')) → function runs first, then print shows the result
#
#   Loop: grabs one item from word.upper(...) each round

"""

"""
Problem 393:
#
# ===== SYNTAX =====
#   def sort_complex_numbers(complex_numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   abs() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sort_complex_numbers(?): resolves the value, then runs the function body
#   sort_complex_numbers(complex_numbers): looks up variable 'complex_numbers', then runs the function body
#   print(sort_complex_numbers(?)): the inner sort_complex_numbers() executes FIRST, then print() outputs the result
#   sort_complex_numbers(?): resolves the value, then runs the function body
#   3 + 4j: first get 3, then get 4j, then compute 3 + 4j
#   1 + 1j: first get 1, then get 1j, then compute 1 + 1j
#   0 + 2j: first get 0, then get 2j, then compute 0 + 2j
#   5 + 0j: first get 5, then get 0j, then compute 5 + 0j
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_complex_numbers(complex_numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_complex_numbers(complex_numbers) — stored for later
#   5. Define def sort_complex_numbers(complex_numbers) — stored for later
#   6. Assign to complex_numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_complex_numbers(...))
#       calls sort_complex_numbers with: complex_numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_complex_numbers, sort_complex_numbers, sort_complex_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      complex_numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_complex_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 394:
#
# ===== SYNTAX =====
#   def find_largest_file(file_paths)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   find_largest_file(file_paths): looks up variable 'file_paths', then runs the function body
#   print(find_largest_file(?)): the inner find_largest_file() executes FIRST, then print() outputs the result
#   find_largest_file(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import os
#   2. Define def find_largest_file(file_paths) — stored for later
#   3. Define def find_largest_file(file_paths) — stored for later
#   4. Define def find_largest_file(file_paths) — stored for later
#   5. Assign to file_paths
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(find_largest_file(...))
#       calls find_largest_file with: file_paths=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions find_largest_file, find_largest_file, find_largest_file are just stored
#   Execution starts at the non-def lines below:
#      file_paths = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_largest_file(?)) → function runs first, then print shows the result

"""

"""
Problem 395:
#
# ===== SYNTAX =====
#   def sort_fractions(fractions)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   float() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_fractions(?): resolves the value, then runs the function body
#   sort_fractions(fractions): looks up variable 'fractions', then runs the function body
#   print(sort_fractions(?)): the inner sort_fractions() executes FIRST, then print() outputs the result
#   sort_fractions(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import Fraction from fractions
#   2. Define def sort_fractions(fractions) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def sort_fractions(fractions) — stored for later
#   6. Define def sort_fractions(fractions) — stored for later
#   7. Assign to fractions
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_fractions(...))
#       calls sort_fractions with: fractions=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sort_fractions, sort_fractions, sort_fractions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      fractions = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_fractions(?)) → function runs first, then print shows the result

"""

"""
Problem 396:
#
# ===== SYNTAX =====
#   def sort_tuples_by_sum(tuples)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   sum() — built-in function call
#   list() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sort_tuples_by_sum(?): resolves the value, then runs the function body
#   print(main(lst_tuples)): the inner main() executes FIRST, then print() outputs the result
#   sort_tuples_by_sum(tuples): looks up variable 'tuples', then runs the function body
#   print(sort_tuples_by_sum(?)): the inner sort_tuples_by_sum() executes FIRST, then print() outputs the result
#   main(lst_tuples): looks up variable 'lst_tuples', then runs the function body
#   sort_tuples_by_sum(?): resolves the value, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_tuples_by_sum(tuples) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def main(lst_tuples) — stored for later
#   5. Assign to lst_tuples
#   6. Run print(main(...))
#       calls main with: lst_tuples=lst_tuples
#         6.1 Define def inner(tup) — stored for later
#         6.2 Return from function
#   7. Define def sort_tuples_by_sum(tuples) — stored for later
#   8. Define def sort_tuples_by_sum(tuples) — stored for later
#   9. Assign to tuples
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(sort_tuples_by_sum(...))
#       calls sort_tuples_by_sum with: tuples=[4, 1, 8, 6, 3]
#         12.1 Import reduce from functools
#         12.2 Import operator
#         12.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_tuples_by_sum, main, sort_tuples_by_sum, sort_tuples_by_sum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst_tuples = ... (stored in memory)
#      print(main(lst_tuples)) → function runs first, then print shows the result
#      tuples = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_tuples_by_sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from tup each round

"""

"""
Problem 397:
#
# ===== SYNTAX =====
#   def closest_to_zero(numbers)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   closest_to_zero(?): resolves the value, then runs the function body
#   closest_to_zero(numbers): looks up variable 'numbers', then runs the function body
#   print(closest_to_zero(?)): the inner closest_to_zero() executes FIRST, then print() outputs the result
#   closest_to_zero(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def closest_to_zero(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def closest_to_zero(numbers) — stored for later
#   5. Define def closest_to_zero(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(closest_to_zero(...))
#       calls closest_to_zero with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions closest_to_zero, closest_to_zero, closest_to_zero are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(closest_to_zero(?)) → function runs first, then print shows the result

"""

"""
Problem 398:
#
# ===== SYNTAX =====
#   def sort_points_by_distance(points)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   sort_points_by_distance(?): resolves the value, then runs the function body
#   sort_points_by_distance(points): looks up variable 'points', then runs the function body
#   print(sort_points_by_distance(?)): the inner sort_points_by_distance() executes FIRST, then print() outputs the result
#   ? ** 0.5: first get ?, then get 0.5, then compute ? ** 0.5
#   ? ** 0.5: first get ?, then get 0.5, then compute ? ** 0.5
#   sort_points_by_distance(?): resolves the value, then runs the function body
#   ? ** 0.5: first get ?, then get 0.5, then compute ? ** 0.5
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#   ? ** 2: first get ?, then get 2, then compute ? ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_points_by_distance(points) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_points_by_distance(points) — stored for later
#   5. Define def sort_points_by_distance(points) — stored for later
#   6. Assign to points
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_points_by_distance(...))
#       calls sort_points_by_distance with: points=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_points_by_distance, sort_points_by_distance, sort_points_by_distance are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      points = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_points_by_distance(?)) → function runs first, then print shows the result

"""

"""
Problem 399:
#
# ===== SYNTAX =====
#   def sort_words_by_distinct_letters(words)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_words_by_distinct_letters(?): resolves the value, then runs the function body
#   sort_words_by_distinct_letters(words): looks up variable 'words', then runs the function body
#   print(sort_words_by_distinct_letters(?)): the inner sort_words_by_distinct_letters() executes FIRST, then print() outputs the result
#   sort_words_by_distinct_letters(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_words_by_distinct_letters(words) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_words_by_distinct_letters(words) — stored for later
#   5. Define def sort_words_by_distinct_letters(words) — stored for later
#   6. Assign to words
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_words_by_distinct_letters(...))
#       calls sort_words_by_distinct_letters with: words=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_words_by_distinct_letters, sort_words_by_distinct_letters, sort_words_by_distinct_letters are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_words_by_distinct_letters(?)) → function runs first, then print shows the result

"""

"""
Problem 400:
#
# ===== SYNTAX =====
#   def main(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   ord() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   main(lst): looks up variable 'lst', then runs the function body
#   find_highest_ascii_word(?): resolves the value, then runs the function body
#   main(lst): looks up variable 'lst', then runs the function body
#   print(main(?)): the inner main() executes FIRST, then print() outputs the result
#   main(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst) — stored for later
#   2. Assign to lst
#   3. Run main(lst)
#       with: lst=lst
#         3.1 Return from function
#   4. Define def ascii_value(word) — stored for later
#   5. Define def find_highest_ascii_word(words) — stored for later
#   6. Assign to result
#   7. Run print(result)
#   8. Define def main(lst) — stored for later
#   9. Define def main(lst) — stored for later
#   10. Assign to lst
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7]
#         13.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, ascii_value, find_highest_ascii_word, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      main(lst) → jumps into function, runs body, returns result → printed
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round

"""

"""
Problem 401:
#
# ===== SYNTAX =====
#   def count_vowels(s)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   sort_by_vowel_count(strings): looks up variable 'strings', then runs the function body
#   count_vowels(s): looks up variable 's', then runs the function body
#   print(count_vowels('world')): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels('world'): gets the literal 'world', then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(s) — stored for later
#   2. Define def sort_by_vowel_count(strings) — stored for later
#   3. Assign to strings
#   4. Assign to sorted_strings
#   5. Run print(sorted_strings)
#   6. Define def count_vowels(s) — stored for later
#   7. Define def count_vowels(s) — stored for later
#   8. Assign to s
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_vowels(...))
#       calls count_vowels with: s='world'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, sort_by_vowel_count, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      strings = ... (stored in memory)
#      sorted_strings = ... (stored in memory)
#      print(sorted_strings)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 402:
#
# ===== SYNTAX =====
#   def convert_to_integers(integers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(convert_to_integers(numbers)): the inner convert_to_integers() executes FIRST, then print() outputs the result
#   convert_to_integers(integers): looks up variable 'integers', then runs the function body
#   print(convert_to_integers(25)): the inner convert_to_integers() executes FIRST, then print() outputs the result
#   convert_to_integers(numbers): looks up variable 'numbers', then runs the function body
#   convert_to_integers(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_integers(integers) — stored for later
#   2. Assign to numbers
#   3. Run print(convert_to_integers(...))
#       calls convert_to_integers with: integers=numbers
#         3.1 Return from function
#   4. Define def convert_to_integers(integers) — stored for later
#   5. Define def convert_to_integers(integers) — stored for later
#   6. Assign to integers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_integers(...))
#       calls convert_to_integers with: integers=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_integers, convert_to_integers, convert_to_integers are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(convert_to_integers(numbers)) → function runs first, then print shows the result
#      integers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_integers(25)) → function runs first, then print shows the result

"""

"""
Problem 403:
#
# ===== SYNTAX =====
#   def add_five(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   in
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(add_five(lst)): the inner add_five() executes FIRST, then print() outputs the result
#   add_five(lst): looks up variable 'lst', then runs the function body
#   print(add_five(?)): the inner add_five() executes FIRST, then print() outputs the result
#   add_five(lst): looks up variable 'lst', then runs the function body
#   add_five(?): resolves the value, then runs the function body
#   number + 5: first get number, then get 5, then compute number + 5
#   number + 5: first get number, then get 5, then compute number + 5
#   number + 5: first get number, then get 5, then compute number + 5
#
# ===== EXECUTION ORDER =====
#   1. Define def add_five(lst) — stored for later
#   2. Assign to lst
#   3. Run print(add_five(...))
#       calls add_five with: lst=lst
#         3.1 Return from function
#   4. Define def add_five(lst) — stored for later
#   5. Define def add_five(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(add_five(...))
#       calls add_five with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_five, add_five, add_five are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(add_five(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_five(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 404:
#
# ===== SYNTAX =====
#   def find_min(numbers)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(find_min(lst)): the inner find_min() executes FIRST, then print() outputs the result
#   main(user): looks up variable 'user', then runs the function body
#   find_min(numbers): looks up variable 'numbers', then runs the function body
#   print(find_min(?)): the inner find_min() executes FIRST, then print() outputs the result
#   find_min(lst): looks up variable 'lst', then runs the function body
#   find_min(?): resolves the value, then runs the function body
#   number < min_num: is this True or False? The answer picks which branch
#   num < min_num: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def find_min(numbers) — stored for later
#   2. Assign to lst
#   3. Run print(find_min(...))
#       calls find_min with: numbers=lst
#         3.1 Return from function
#   4. Define def main(user) — stored for later
#   5. Assign to user
#   6. Run main(user)
#       with: user=user
#         6.1 Assign to int_lst
#         6.2 For loop over user
#         6.3 Assign to min_num
#         6.4 For loop over int_lst
#         6.5 Return from function
#   7. Define def find_min(numbers) — stored for later
#   8. Define def find_min(numbers) — stored for later
#   9. Assign to numbers
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(find_min(...))
#       calls find_min with: numbers=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_min, main, find_min, find_min are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(find_min(lst)) → function runs first, then print shows the result
#      user = ... (stored in memory)
#      main(user) → jumps into function, runs body, returns result → printed
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_min(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 405:
#
# ===== SYNTAX =====
#   def converts(cm)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(converts(number)): the inner converts() executes FIRST, then print() outputs the result
#   converts(cm): looks up variable 'cm', then runs the function body
#   print(converts(25)): the inner converts() executes FIRST, then print() outputs the result
#   cm * 0.39: first get cm, then get 0.39, then compute cm * 0.39
#   converts(number): looks up variable 'number', then runs the function body
#   cm * 0.39: first get cm, then get 0.39, then compute cm * 0.39
#   cm * 0.39: first get cm, then get 0.39, then compute cm * 0.39
#   converts(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def converts(cm) — stored for later
#   2. Assign to number
#   3. Run print(converts(...))
#       calls converts with: cm=number
#         3.1 Assign to inch
#         3.2 Assign to result
#         3.3 Return from function
#   4. Define def converts(cm) — stored for later
#   5. Define def converts(cm) — stored for later
#   6. Assign to cm
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(converts(...))
#       calls converts with: cm=25
#         9.1 Assign to inch
#         9.2 Assign to result
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions converts, converts, converts are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      print(converts(number)) → function runs first, then print shows the result
#      cm = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(converts(25)) → function runs first, then print shows the result

"""

"""
Problem 406:
#
# ===== SYNTAX =====
#   def reverse(lst)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(reverse(lst)): the inner reverse() executes FIRST, then print() outputs the result
#   reverse(lst): looks up variable 'lst', then runs the function body
#   print(reverse(?)): the inner reverse() executes FIRST, then print() outputs the result
#   reverse(lst): looks up variable 'lst', then runs the function body
#   reverse(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse(lst) — stored for later
#   2. Assign to lst
#   3. Run print(reverse(...))
#       calls reverse with: lst=lst
#         3.1 Return from function
#   4. Define def reverse(lst) — stored for later
#   5. Define def reverse(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse(...))
#       calls reverse with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse, reverse, reverse are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(reverse(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse(?)) → function runs first, then print shows the result

"""

"""
Problem 407:
#
# ===== SYNTAX =====
#   def calculate_average(lst)
#   return — exits the function and sends back a value
#   float() — built-in function call
#   Fraction() — built-in function call
#   for
#   in
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(calculate_average(lst)): the inner calculate_average() executes FIRST, then print() outputs the result
#   calculate_average(lst): looks up variable 'lst', then runs the function body
#   print(calculate_average(?)): the inner calculate_average() executes FIRST, then print() outputs the result
#   add_num / length_num: first get add_num, then get length_num, then compute add_num / length_num
#   calculate_average(lst): looks up variable 'lst', then runs the function body
#   add_num * ?: first get add_num, then get ?, then compute add_num * ?
#   calculate_average(?): resolves the value, then runs the function body
#   1 / length_num: first get 1, then get length_num, then compute 1 / length_num
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_average(lst) — stored for later
#   2. Assign to lst
#   3. Run print(calculate_average(...))
#       calls calculate_average with: lst=lst
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def calculate_average(lst) — stored for later
#   5. Define def calculate_average(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_average(...))
#       calls calculate_average with: lst=[1, 3, 5, 7]
#         9.1 Import Fraction from fractions
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_average, calculate_average, calculate_average are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(calculate_average(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_average(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 408:
#
# ===== SYNTAX =====
#   def remove_duplicates(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates(lst)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   word in unique: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(lst) — stored for later
#   2. Assign to lst
#   3. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=lst
#         3.1 Return from function
#   4. Define def remove_duplicates(lst) — stored for later
#   5. Define def remove_duplicates(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(remove_duplicates(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 409:
#
# ===== SYNTAX =====
#   def len_words(lst)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(len_words(lst)): the inner len_words() executes FIRST, then print() outputs the result
#   len_words(lst): looks up variable 'lst', then runs the function body
#   print(len_words(?)): the inner len_words() executes FIRST, then print() outputs the result
#   len_words(lst): looks up variable 'lst', then runs the function body
#   len_words(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def len_words(lst) — stored for later
#   2. Assign to lst
#   3. Run print(len_words(...))
#       calls len_words with: lst=lst
#         3.1 Return from function
#   4. Define def len_words(lst) — stored for later
#   5. Define def len_words(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(len_words(...))
#       calls len_words with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions len_words, len_words, len_words are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(len_words(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(len_words(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 410:
#
# ===== SYNTAX =====
#   def filter_string(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(filter_string(lst)): the inner filter_string() executes FIRST, then print() outputs the result
#   filter_string(lst): looks up variable 'lst', then runs the function body
#   print(filter_string(?)): the inner filter_string() executes FIRST, then print() outputs the result
#   filter_string(lst): looks up variable 'lst', then runs the function body
#   filter_string(?): resolves the value, then runs the function body
#   len(...) == 3: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(filter_string(...))
#       calls filter_string with: lst=lst
#         3.1 Return from function
#   4. Define def filter_string(lst) — stored for later
#   5. Define def filter_string(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(filter_string(...))
#       calls filter_string with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_string, filter_string, filter_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(filter_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_string(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 411:
#
# ===== SYNTAX =====
#   def calculate_sum(a, b)
#   return — exits the function and sends back a value
#   inner_sum() — built-in function call
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   calculate_sum(7, 3): gets the literal 7, gets the literal 3, then runs the function body
#   calculate_sum(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_sum(20, 30)): the inner calculate_sum() executes FIRST, then print() outputs the result
#   calculate_sum(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_sum(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_sum(a, b) — stored for later
#   5. Define def calculate_sum(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_sum(...))
#       calls calculate_sum with: a=20, b=30
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_sum, calculate_sum, calculate_sum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_sum(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 412:
#
# ===== SYNTAX =====
#   def calculate_product(a, b)
#   return — exits the function and sends back a value
#   product() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   calculate_product(3, 5): gets the literal 3, gets the literal 5, then runs the function body
#   calculate_product(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_product(20, 30)): the inner calculate_product() executes FIRST, then print() outputs the result
#   calculate_product(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a * b: first get a, then get b, then compute a * b
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_product(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_product(a, b) — stored for later
#   5. Define def calculate_product(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_product(...))
#       calls calculate_product with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_product, calculate_product, calculate_product are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_product(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 413:
#
# ===== SYNTAX =====
#   def calculate_difference(a, b)
#   return — exits the function and sends back a value
#   difference() — built-in function call
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   calculate_difference(10, 3): gets the literal 10, gets the literal 3, then runs the function body
#   calculate_difference(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_difference(20, 30)): the inner calculate_difference() executes FIRST, then print() outputs the result
#   a + -b: first get a, then get -b, then compute a + -b
#   calculate_difference(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a - b: first get a, then get b, then compute a - b
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_difference(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_difference(a, b) — stored for later
#   5. Define def calculate_difference(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_difference(...))
#       calls calculate_difference with: a=20, b=30
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_difference, calculate_difference, calculate_difference are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_difference(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 414:
#
# ===== SYNTAX =====
#   def calculate_quotient(a, b)
#   return — exits the function and sends back a value
#   quotient() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   calculate_quotient(10, 2): gets the literal 10, gets the literal 2, then runs the function body
#   calculate_quotient(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_quotient(20, 30)): the inner calculate_quotient() executes FIRST, then print() outputs the result
#   a * ?: first get a, then get ?, then compute a * ?
#   calculate_quotient(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a / b: first get a, then get b, then compute a / b
#   1 / b: first get 1, then get b, then compute 1 / b
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_quotient(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def calculate_quotient(a, b) — stored for later
#   5. Define def calculate_quotient(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_quotient(...))
#       calls calculate_quotient with: a=20, b=30
#         10.1 Import Fraction from fractions
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_quotient, calculate_quotient, calculate_quotient are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_quotient(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 415:
#
# ===== SYNTAX =====
#   def area_of_rectangle(length, width)
#   return — exits the function and sends back a value
#   area() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   area_of_rectangle(5, 4): gets the literal 5, gets the literal 4, then runs the function body
#   area_of_rectangle(length, width): looks up variable 'length', looks up variable 'width', then runs the function body
#   print(area_of_rectangle(25, 25)): the inner area_of_rectangle() executes FIRST, then print() outputs the result
#   area_of_rectangle(25, 25): gets the literal 25, gets the literal 25, then runs the function body
#   length * width: first get length, then get width, then compute length * width
#
# ===== EXECUTION ORDER =====
#   1. Define def area_of_rectangle(length, width) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def area_of_rectangle(length, width) — stored for later
#   5. Define def area_of_rectangle(length, width) — stored for later
#   6. Assign to length
#   7. Assign to width
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(area_of_rectangle(...))
#       calls area_of_rectangle with: length=25, width=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions area_of_rectangle, area_of_rectangle, area_of_rectangle are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      length = ... (stored in memory)
#      width = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(area_of_rectangle(25, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 416:
#
# ===== SYNTAX =====
#   def perimeter_of_square(side_length)
#   return — exits the function and sends back a value
#   perimeter() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   perimeter_of_square(5): gets the literal 5, then runs the function body
#   perimeter_of_square(side_length): looks up variable 'side_length', then runs the function body
#   print(perimeter_of_square(25)): the inner perimeter_of_square() executes FIRST, then print() outputs the result
#   perimeter_of_square(25): gets the literal 25, then runs the function body
#   4 * side_length: first get 4, then get side_length, then compute 4 * side_length
#
# ===== EXECUTION ORDER =====
#   1. Define def perimeter_of_square(side_length) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def perimeter_of_square(side_length) — stored for later
#   5. Define def perimeter_of_square(side_length) — stored for later
#   6. Assign to side_length
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(perimeter_of_square(...))
#       calls perimeter_of_square with: side_length=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions perimeter_of_square, perimeter_of_square, perimeter_of_square are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      side_length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(perimeter_of_square(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 417:
#
# ===== SYNTAX =====
#   def fahrenheit_to_celsius(fahrenheit)
#   return — exits the function and sends back a value
#   convert() — built-in function call
#   operators: - * /
#
# ===== EVALUATION ORDER =====
#   fahrenheit_to_celsius(68): gets the literal 68, then runs the function body
#   fahrenheit_to_celsius(fahrenheit): looks up variable 'fahrenheit', then runs the function body
#   print(fahrenheit_to_celsius(25)): the inner fahrenheit_to_celsius() executes FIRST, then print() outputs the result
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   fahrenheit_to_celsius(25): gets the literal 25, then runs the function body
#   ? / 9: first get ?, then get 9, then compute ? / 9
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   ? * 5: first get ?, then get 5, then compute ? * 5
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#   fahrenheit - 32: first get fahrenheit, then get 32, then compute fahrenheit - 32
#
# ===== EXECUTION ORDER =====
#   1. Define def fahrenheit_to_celsius(fahrenheit) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def fahrenheit_to_celsius(fahrenheit) — stored for later
#   5. Define def fahrenheit_to_celsius(fahrenheit) — stored for later
#   6. Assign to fahrenheit
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(fahrenheit_to_celsius(...))
#       calls fahrenheit_to_celsius with: fahrenheit=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fahrenheit_to_celsius, fahrenheit_to_celsius, fahrenheit_to_celsius are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      fahrenheit = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fahrenheit_to_celsius(25)) → function runs first, then print shows the result

"""

"""
Problem 418:
#
# ===== SYNTAX =====
#   def celsius_to_fahrenheit(celsius)
#   return — exits the function and sends back a value
#   convert() — built-in function call
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   celsius_to_fahrenheit(20): gets the literal 20, then runs the function body
#   celsius_to_fahrenheit(celsius): looks up variable 'celsius', then runs the function body
#   print(celsius_to_fahrenheit(25)): the inner celsius_to_fahrenheit() executes FIRST, then print() outputs the result
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   celsius_to_fahrenheit(25): gets the literal 25, then runs the function body
#   ? + 32: first get ?, then get 32, then compute ? + 32
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   ? / 5: first get ?, then get 5, then compute ? / 5
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#   celsius * 9: first get celsius, then get 9, then compute celsius * 9
#
# ===== EXECUTION ORDER =====
#   1. Define def celsius_to_fahrenheit(celsius) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def celsius_to_fahrenheit(celsius) — stored for later
#   5. Define def celsius_to_fahrenheit(celsius) — stored for later
#   6. Assign to celsius
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(celsius_to_fahrenheit(...))
#       calls celsius_to_fahrenheit with: celsius=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions celsius_to_fahrenheit, celsius_to_fahrenheit, celsius_to_fahrenheit are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      celsius = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(celsius_to_fahrenheit(25)) → function runs first, then print shows the result

"""

"""
Problem 419:
#
# ===== SYNTAX =====
#   def even_or_odd(number)
#   return — exits the function and sends back a value
#   check() — built-in function call
#   if/elif/else
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   even_or_odd(7): gets the literal 7, then runs the function body
#   even_or_odd(number): looks up variable 'number', then runs the function body
#   print(even_or_odd(25)): the inner even_or_odd() executes FIRST, then print() outputs the result
#   even_or_odd(25): gets the literal 25, then runs the function body
#   number % 2: first get number, then get 2, then compute number % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def even_or_odd(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def even_or_odd(number) — stored for later
#   5. Define def even_or_odd(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_or_odd(...))
#       calls even_or_odd with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_or_odd, even_or_odd, even_or_odd are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_or_odd(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 420:
#
# ===== SYNTAX =====
#   def is_prime(number)
#   return — exits the function and sends back a value
#   check_divisibility() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + % ** == <
#
# ===== EVALUATION ORDER =====
#   is_prime(29): gets the literal 29, then runs the function body
#   is_prime(number): looks up variable 'number', then runs the function body
#   print(is_prime(25)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(25): gets the literal 25, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % i: first get n, then get i, then compute n % i
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   number < 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(number) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_prime(number) — stored for later
#   5. Define def is_prime(number) — stored for later
#   6. Assign to number
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_prime(...))
#       calls is_prime with: number=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 421:
#
# ===== SYNTAX =====
#   def list_sum(numbers)
#   return — exits the function and sends back a value
#   add_elements() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   list_sum(?): resolves the value, then runs the function body
#   list_sum(numbers): looks up variable 'numbers', then runs the function body
#   print(list_sum(?)): the inner list_sum() executes FIRST, then print() outputs the result
#   list_sum(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_sum(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def list_sum(numbers) — stored for later
#   5. Define def list_sum(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(list_sum(...))
#       calls list_sum with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_sum, list_sum, list_sum are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 422:
#
# ===== SYNTAX =====
#   def list_product(numbers)
#   return — exits the function and sends back a value
#   multiply_elements() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   list_product(?): resolves the value, then runs the function body
#   list_product(numbers): looks up variable 'numbers', then runs the function body
#   print(list_product(?)): the inner list_product() executes FIRST, then print() outputs the result
#   list_product(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_product(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def list_product(numbers) — stored for later
#   5. Define def list_product(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(list_product(...))
#       calls list_product with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_product, list_product, list_product are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_product(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from numbers each round

"""

"""
Problem 423:
#
# ===== SYNTAX =====
#   def greater_than(a, b)
#   return — exits the function and sends back a value
#   compare() — built-in function call
#   if/elif/else
#   operators: > <=
#
# ===== EVALUATION ORDER =====
#   greater_than(10, 5): gets the literal 10, gets the literal 5, then runs the function body
#   greater_than(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(greater_than(20, 30)): the inner greater_than() executes FIRST, then print() outputs the result
#   greater_than(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a > b: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def greater_than(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def greater_than(a, b) — stored for later
#   5. Define def greater_than(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(greater_than(...))
#       calls greater_than with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greater_than, greater_than, greater_than are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greater_than(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 424:
#
# ===== SYNTAX =====
#   def string_length(s)
#   return — exits the function and sends back a value
#   calculate_length() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   string_length('Hello, World!'): gets the literal 'Hello, World!', then runs the function body
#   string_length(s): looks up variable 's', then runs the function body
#   print(string_length(?)): the inner string_length() executes FIRST, then print() outputs the result
#   string_length(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_length(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def string_length(s) — stored for later
#   5. Define def string_length(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(string_length(...))
#       calls string_length with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_length, string_length, string_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_length(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 425:
#
# ===== SYNTAX =====
#   def first_and_last(s)
#   return — exits the function and sends back a value
#   get_first_last() — built-in function call
#   len() — built-in function call
#   if/elif/else
#   operators: >
#
# ===== EVALUATION ORDER =====
#   first_and_last('Hello'): gets the literal 'Hello', then runs the function body
#   first_and_last(s): looks up variable 's', then runs the function body
#   print(first_and_last(?)): the inner first_and_last() executes FIRST, then print() outputs the result
#   first_and_last(?): resolves the value, then runs the function body
#   len(...) > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def first_and_last(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def first_and_last(s) — stored for later
#   5. Define def first_and_last(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(first_and_last(...))
#       calls first_and_last with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions first_and_last, first_and_last, first_and_last are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(first_and_last(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 426:
#
# ===== SYNTAX =====
#   def count_vowels(s)
#   return — exits the function and sends back a value
#   is_vowel() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   count_vowels('Hello, World!'): gets the literal 'Hello, World!', then runs the function body
#   count_vowels(s): looks up variable 's', then runs the function body
#   print(count_vowels(?)): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_vowels(s) — stored for later
#   5. Define def count_vowels(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_vowels(...))
#       calls count_vowels with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 427:
#
# ===== SYNTAX =====
#   def reverse_string(s)
#   return — exits the function and sends back a value
#   reverse() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_string('Hello'): gets the literal 'Hello', then runs the function body
#   reverse_string(s): looks up variable 's', then runs the function body
#   print(reverse_string(?)): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(?): resolves the value, then runs the function body
#   char + reversed_s: first get char, then get reversed_s, then compute char + reversed_s
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_string(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def reverse_string(s) — stored for later
#   5. Define def reverse_string(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_string(...))
#       calls reverse_string with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_string, reverse_string, reverse_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_string(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 428:
#
# ===== SYNTAX =====
#   def is_palindrome(s)
#   return — exits the function and sends back a value
#   check() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   is_palindrome('racecar'): gets the literal 'racecar', then runs the function body
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome(?)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_palindrome(s) — stored for later
#   5. Define def is_palindrome(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_palindrome(...))
#       calls is_palindrome with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome(?)) → function runs first, then print shows the result

"""

"""
Problem 429:
#
# ===== SYNTAX =====
#   def multiply_by_n(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   multiply_by_n(3): gets the literal 3, then runs the function body
#   multiply_by_n(n): looks up variable 'n', then runs the function body
#   print(multiply_by_n(15)): the inner multiply_by_n() executes FIRST, then print() outputs the result
#   multiply_by_n(15): gets the literal 15, then runs the function body
#   n * x: first get n, then get x, then compute n * x
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply_by_n(n) — stored for later
#   2. Assign to multiply_by_3
#   3. Assign to result
#   4. Run print(result)
#   5. Define def multiply_by_n(n) — stored for later
#   6. Define def multiply_by_n(n) — stored for later
#   7. Assign to n
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(multiply_by_n(...))
#       calls multiply_by_n with: n=15
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply_by_n, multiply_by_n, multiply_by_n are just stored
#   Execution starts at the non-def lines below:
#      multiply_by_3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply_by_n(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 430:
#
# ===== SYNTAX =====
#   def add_tax(price)
#   return — exits the function and sends back a value
#   calculate_tax() — built-in function call
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   add_tax(100): gets the literal 100, then runs the function body
#   add_tax(price): looks up variable 'price', then runs the function body
#   print(add_tax(25)): the inner add_tax() executes FIRST, then print() outputs the result
#   price + ?: first get price, then get ?, then compute price + ?
#   price + ?: first get price, then get ?, then compute price + ?
#   add_tax(25): gets the literal 25, then runs the function body
#   price + ?: first get price, then get ?, then compute price + ?
#   price * tax_rate: first get price, then get tax_rate, then compute price * tax_rate
#   price * tax_rate: first get price, then get tax_rate, then compute price * tax_rate
#   price * tax_rate: first get price, then get tax_rate, then compute price * tax_rate
#
# ===== EXECUTION ORDER =====
#   1. Define def add_tax(price) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def add_tax(price) — stored for later
#   5. Define def add_tax(price) — stored for later
#   6. Assign to price
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(add_tax(...))
#       calls add_tax with: price=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_tax, add_tax, add_tax are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      price = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_tax(25)) → function runs first, then print shows the result

"""

"""
Problem 431:
#
# ===== SYNTAX =====
#   def greet_person(name)
#   return — exits the function and sends back a value
#   greeting() — built-in function call
#   operators: %
#
# ===== EVALUATION ORDER =====
#   greet_person('Alice'): gets the literal 'Alice', then runs the function body
#   greet_person(name): looks up variable 'name', then runs the function body
#   print(greet_person('Bob')): the inner greet_person() executes FIRST, then print() outputs the result
#   'Hello, %s!' % name: first get 'Hello, %s!', then get name, then compute 'Hello, %s!' % name
#   greet_person('Bob'): gets the literal 'Bob', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def greet_person(name) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def greet_person(name) — stored for later
#   5. Define def greet_person(name) — stored for later
#   6. Assign to name
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(greet_person(...))
#       calls greet_person with: name='Bob'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet_person, greet_person, greet_person are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet_person('Bob')) → function runs first, then print shows the result

"""

"""
Problem 432:
#
# ===== SYNTAX =====
#   def factorial(n)
#   return — exits the function and sends back a value
#   calc_factorial() — built-in function call
#   if/elif/else
#   operators: - * == or
#
# ===== EVALUATION ORDER =====
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(15): gets the literal 15, then runs the function body
#   num * calc_factorial(...): first get num, then get calc_factorial(...), then compute num * calc_factorial(...)
#   num - 1: first get num, then get 1, then compute num - 1
#   num == 0: is this True or False? The answer picks which branch
#   num == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def factorial(n) — stored for later
#   5. Define def factorial(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(factorial(...))
#       calls factorial with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 433:
#
# ===== SYNTAX =====
#   def exponent(base, power)
#   return — exits the function and sends back a value
#   calculate() — built-in function call
#   range() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   exponent(2, 3): gets the literal 2, gets the literal 3, then runs the function body
#   exponent(base, power): looks up variable 'base', looks up variable 'power', then runs the function body
#   print(exponent(10, 25)): the inner exponent() executes FIRST, then print() outputs the result
#   exponent(10, 25): gets the literal 10, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def exponent(base, power) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def exponent(base, power) — stored for later
#   5. Define def exponent(base, power) — stored for later
#   6. Assign to base
#   7. Assign to power
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(exponent(...))
#       calls exponent with: base=10, power=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions exponent, exponent, exponent are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      base = ... (stored in memory)
#      power = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(exponent(10, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 434:
#
# ===== SYNTAX =====
#   def gcd(a, b)
#   return — exits the function and sends back a value
#   calculate_gcd() — built-in function call
#   while
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   gcd(48, 18): gets the literal 48, gets the literal 18, then runs the function body
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(gcd(20, 30)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x % y: first get x, then get y, then compute x % y
#
# ===== EXECUTION ORDER =====
#   1. Define def gcd(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def gcd(a, b) — stored for later
#   5. Define def gcd(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(gcd(...))
#       calls gcd with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions gcd, gcd, gcd are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(gcd(20, 30)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 435:
#
# ===== SYNTAX =====
#   def lcm(a, b)
#   return — exits the function and sends back a value
#   calculate_lcm() — built-in function call
#   abs() — built-in function call
#   gcd() — built-in function call
#   while
#   operators: * // % !=
#
# ===== EVALUATION ORDER =====
#   lcm(4, 5): gets the literal 4, gets the literal 5, then runs the function body
#   lcm(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(lcm(20, 30)): the inner lcm() executes FIRST, then print() outputs the result
#   abs(...) // gcd(...): first get abs(...), then get gcd(...), then compute abs(...) // gcd(...)
#   abs(...) // gcd(...): first get abs(...), then get gcd(...), then compute abs(...) // gcd(...)
#   lcm(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   abs(...) // gcd(...): first get abs(...), then get gcd(...), then compute abs(...) // gcd(...)
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x % y: first get x, then get y, then compute x % y
#
# ===== EXECUTION ORDER =====
#   1. Define def lcm(a, b) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def lcm(a, b) — stored for later
#   5. Define def lcm(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(lcm(...))
#       calls lcm with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions lcm, lcm, lcm are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(lcm(20, 30)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 436:
#
# ===== SYNTAX =====
#   def square_of_number(n)
#   return — exits the function and sends back a value
#   square() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   square_of_number(5): gets the literal 5, then runs the function body
#   square_of_number(n): looks up variable 'n', then runs the function body
#   print(square_of_number(15)): the inner square_of_number() executes FIRST, then print() outputs the result
#   square_of_number(15): gets the literal 15, then runs the function body
#   n * n: first get n, then get n, then compute n * n
#
# ===== EXECUTION ORDER =====
#   1. Define def square_of_number(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def square_of_number(n) — stored for later
#   5. Define def square_of_number(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_of_number(...))
#       calls square_of_number with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_of_number, square_of_number, square_of_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_of_number(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 437:
#
# ===== SYNTAX =====
#   def cube_of_number(n)
#   return — exits the function and sends back a value
#   cube() — built-in function call
#   operators: *
#
# ===== EVALUATION ORDER =====
#   cube_of_number(3): gets the literal 3, then runs the function body
#   cube_of_number(n): looks up variable 'n', then runs the function body
#   print(cube_of_number(15)): the inner cube_of_number() executes FIRST, then print() outputs the result
#   ? * n: first get ?, then get n, then compute ? * n
#   ? * n: first get ?, then get n, then compute ? * n
#   cube_of_number(15): gets the literal 15, then runs the function body
#   ? * n: first get ?, then get n, then compute ? * n
#   n * n: first get n, then get n, then compute n * n
#   n * n: first get n, then get n, then compute n * n
#   n * n: first get n, then get n, then compute n * n
#
# ===== EXECUTION ORDER =====
#   1. Define def cube_of_number(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def cube_of_number(n) — stored for later
#   5. Define def cube_of_number(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(cube_of_number(...))
#       calls cube_of_number with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions cube_of_number, cube_of_number, cube_of_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(cube_of_number(15)) → function runs first, then print shows the result

"""

"""
Problem 438:
#
# ===== SYNTAX =====
#   def sort_list_descending(lst)
#   return — exits the function and sends back a value
#   sort() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#   while
#   operators: >
#
# ===== EVALUATION ORDER =====
#   sort_list_descending(?): resolves the value, then runs the function body
#   sort_list_descending(lst): looks up variable 'lst', then runs the function body
#   print(sort_list_descending(?)): the inner sort_list_descending() executes FIRST, then print() outputs the result
#   sort_list_descending(?): resolves the value, then runs the function body
#   item > maximum: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_list_descending(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_list_descending(lst) — stored for later
#   5. Define def sort_list_descending(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_list_descending(...))
#       calls sort_list_descending with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_list_descending, sort_list_descending, sort_list_descending are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_list_descending(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 439:
#
# ===== SYNTAX =====
#   def max_of_three(a, b, c)
#   return — exits the function and sends back a value
#   find_max() — built-in function call
#   if/elif/else
#   operators: >
#
# ===== EVALUATION ORDER =====
#   max_of_three(10, 20, 15): gets the literal 10, gets the literal 20, gets the literal 15, then runs the function body
#   max_of_three(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(max_of_three(20, 30, 10)): the inner max_of_three() executes FIRST, then print() outputs the result
#   max_of_three(20, 30, 10): gets the literal 20, gets the literal 30, gets the literal 10, then runs the function body
#   b > max_value: is this True or False? The answer picks which branch
#   c > max_value: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def max_of_three(a, b, c) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def max_of_three(a, b, c) — stored for later
#   5. Define def max_of_three(a, b, c) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to c
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(max_of_three(...))
#       calls max_of_three with: a=20, b=30, c=10
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_of_three, max_of_three, max_of_three are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_of_three(20, 30, 10)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 440:
#
# ===== SYNTAX =====
#   def average_of_list(lst)
#   return — exits the function and sends back a value
#   calculate_average() — built-in function call
#   for
#   in
#   operators: / !=
#
# ===== EVALUATION ORDER =====
#   average_of_list(?): resolves the value, then runs the function body
#   average_of_list(lst): looks up variable 'lst', then runs the function body
#   print(average_of_list(?)): the inner average_of_list() executes FIRST, then print() outputs the result
#   average_of_list(?): resolves the value, then runs the function body
#   total / count: first get total, then get count, then compute total / count
#   total / count: first get total, then get count, then compute total / count
#   total / count: first get total, then get count, then compute total / count
#
# ===== EXECUTION ORDER =====
#   1. Define def average_of_list(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def average_of_list(lst) — stored for later
#   5. Define def average_of_list(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(average_of_list(...))
#       calls average_of_list with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_of_list, average_of_list, average_of_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 441:
#
# ===== SYNTAX =====
#   def average_of_list(lst)
#   return — exits the function and sends back a value
#   calculate_average() — built-in function call
#   for
#   in
#   operators: / !=
#
# ===== EVALUATION ORDER =====
#   average_of_list(?): resolves the value, then runs the function body
#   average_of_list(lst): looks up variable 'lst', then runs the function body
#   print(average_of_list(?)): the inner average_of_list() executes FIRST, then print() outputs the result
#   average_of_list(?): resolves the value, then runs the function body
#   total / count: first get total, then get count, then compute total / count
#   total / count: first get total, then get count, then compute total / count
#   total / count: first get total, then get count, then compute total / count
#
# ===== EXECUTION ORDER =====
#   1. Define def average_of_list(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def average_of_list(lst) — stored for later
#   5. Define def average_of_list(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(average_of_list(...))
#       calls average_of_list with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_of_list, average_of_list, average_of_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 442:
#
# ===== SYNTAX =====
#   def is_uppercase(s)
#   return — exits the function and sends back a value
#   check() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   is_uppercase('HELLO'): gets the literal 'HELLO', then runs the function body
#   is_uppercase(s): looks up variable 's', then runs the function body
#   print(is_uppercase(?)): the inner is_uppercase() executes FIRST, then print() outputs the result
#   is_uppercase(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_uppercase(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_uppercase(s) — stored for later
#   5. Define def is_uppercase(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_uppercase(...))
#       calls is_uppercase with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_uppercase, is_uppercase, is_uppercase are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_uppercase(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 443:
#
# ===== SYNTAX =====
#   def is_lowercase(s)
#   return — exits the function and sends back a value
#   check() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   is_lowercase('hello'): gets the literal 'hello', then runs the function body
#   is_lowercase(s): looks up variable 's', then runs the function body
#   print(is_lowercase(?)): the inner is_lowercase() executes FIRST, then print() outputs the result
#   is_lowercase(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_lowercase(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_lowercase(s) — stored for later
#   5. Define def is_lowercase(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_lowercase(...))
#       calls is_lowercase with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_lowercase, is_lowercase, is_lowercase are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_lowercase(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 444:
#
# ===== SYNTAX =====
#   def string_contains_digit(string)
#   return — exits the function and sends back a value
#   contains_digit() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(string_contains_digit(string)): the inner string_contains_digit() executes FIRST, then print() outputs the result
#   string_contains_digit(string): looks up variable 'string', then runs the function body
#   print(string_contains_digit('world')): the inner string_contains_digit() executes FIRST, then print() outputs the result
#   string_contains_digit(string): looks up variable 'string', then runs the function body
#   string_contains_digit('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_contains_digit(string) — stored for later
#   2. Assign to string
#   3. Run print(string_contains_digit(...))
#       calls string_contains_digit with: string=string
#         3.1 Return from function
#   4. Define def string_contains_digit(string) — stored for later
#   5. Define def string_contains_digit(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(string_contains_digit(...))
#       calls string_contains_digit with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_contains_digit, string_contains_digit, string_contains_digit are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(string_contains_digit(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_contains_digit('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 445:
#
# ===== SYNTAX =====
#   def string_contains_letter(string)
#   return — exits the function and sends back a value
#   contains_letter() — built-in function call
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(string_contains_letter(string)): the inner string_contains_letter() executes FIRST, then print() outputs the result
#   string_contains_letter(string): looks up variable 'string', then runs the function body
#   print(string_contains_letter('world')): the inner string_contains_letter() executes FIRST, then print() outputs the result
#   string_contains_letter(string): looks up variable 'string', then runs the function body
#   string_contains_letter('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def string_contains_letter(string) — stored for later
#   2. Assign to string
#   3. Run print(string_contains_letter(...))
#       calls string_contains_letter with: string=string
#         3.1 Return from function
#   4. Define def string_contains_letter(string) — stored for later
#   5. Define def string_contains_letter(string) — stored for later
#   6. Assign to string
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(string_contains_letter(...))
#       calls string_contains_letter with: string='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_contains_letter, string_contains_letter, string_contains_letter are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(string_contains_letter(string)) → function runs first, then print shows the result
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(string_contains_letter('world')) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 446:
#
# ===== SYNTAX =====
#   def average_of_list(lst)
#   return — exits the function and sends back a value
#   average() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   for
#   in
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(average_of_list(lst)): the inner average_of_list() executes FIRST, then print() outputs the result
#   average_of_list(lst): looks up variable 'lst', then runs the function body
#   print(average_of_list(?)): the inner average_of_list() executes FIRST, then print() outputs the result
#   average_of_list(lst): looks up variable 'lst', then runs the function body
#   add_num * ?: first get add_num, then get ?, then compute add_num * ?
#   average_of_list(?): resolves the value, then runs the function body
#   1 / count_len: first get 1, then get count_len, then compute 1 / count_len
#   add_num / count_len: first get add_num, then get count_len, then compute add_num / count_len
#
# ===== EXECUTION ORDER =====
#   1. Define def average_of_list(lst) — stored for later
#   2. Assign to lst
#   3. Run print(average_of_list(...))
#       calls average_of_list with: lst=lst
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def average_of_list(lst) — stored for later
#   5. Define def average_of_list(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(average_of_list(...))
#       calls average_of_list with: lst=[1, 3, 5, 7]
#         9.1 Import Fraction from fractions
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_of_list, average_of_list, average_of_list are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(average_of_list(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round

"""

"""
Problem 447:
#
# ===== SYNTAX =====
#   def check_multiple_of_n(num1, num2)
#   return — exits the function and sends back a value
#   is_multiple() — built-in function call
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(check_multiple_of_n(10, 2)): the inner check_multiple_of_n() executes FIRST, then print() outputs the result
#   print(check_multiple_of_n(10, 3)): the inner check_multiple_of_n() executes FIRST, then print() outputs the result
#   check_multiple_of_n(num1, num2): looks up variable 'num1', looks up variable 'num2', then runs the function body
#   print(check_multiple_of_n(20, 30)): the inner check_multiple_of_n() executes FIRST, then print() outputs the result
#   check_multiple_of_n(10, 2): gets the literal 10, gets the literal 2, then runs the function body
#   check_multiple_of_n(10, 3): gets the literal 10, gets the literal 3, then runs the function body
#   check_multiple_of_n(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x % y: first get x, then get y, then compute x % y
#   x % y: first get x, then get y, then compute x % y
#   x % y: first get x, then get y, then compute x % y
#
# ===== EXECUTION ORDER =====
#   1. Define def check_multiple_of_n(num1, num2) — stored for later
#   2. Run print(check_multiple_of_n(...))
#       calls check_multiple_of_n with: num1=10, num2=2
#         2.1 Return from function
#   3. Run print(check_multiple_of_n(...))
#       calls check_multiple_of_n with: num1=10, num2=3
#         3.1 Return from function
#   4. Define def check_multiple_of_n(num1, num2) — stored for later
#   5. Define def check_multiple_of_n(num1, num2) — stored for later
#   6. Assign to num1
#   7. Assign to num2
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(check_multiple_of_n(...))
#       calls check_multiple_of_n with: num1=20, num2=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_multiple_of_n, check_multiple_of_n, check_multiple_of_n are just stored
#   Execution starts at the non-def lines below:
#      print(check_multiple_of_n(10, 2)) → function runs first, then print shows the result
#      print(check_multiple_of_n(10, 3)) → function runs first, then print shows the result
#      num1 = ... (stored in memory)
#      num2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_multiple_of_n(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 448:
#
# ===== SYNTAX =====
#   def longest_string(lst)
#   return — exits the function and sends back a value
#   longest() — built-in function call
#   find_longest() — built-in function call
#   max() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(longest_string(lst)): the inner longest_string() executes FIRST, then print() outputs the result
#   print(longest_string(lst)): the inner longest_string() executes FIRST, then print() outputs the result
#   longest_string(lst): looks up variable 'lst', then runs the function body
#   print(longest_string(?)): the inner longest_string() executes FIRST, then print() outputs the result
#   longest_string(lst): looks up variable 'lst', then runs the function body
#   longest_string(lst): looks up variable 'lst', then runs the function body
#   longest_string(?): resolves the value, then runs the function body
#   word > longest_word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def longest_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(longest_string(...))
#       calls longest_string with: lst=lst
#         3.1 Return from function
#   4. Define def longest_string(lst) — stored for later
#   5. Assign to lst
#   6. Run print(longest_string(...))
#       calls longest_string with: lst=lst
#         6.1 Return from function
#   7. Define def longest_string(lst) — stored for later
#   8. Define def longest_string(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(longest_string(...))
#       calls longest_string with: lst=[1, 3, 5, 7]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions longest_string, longest_string, longest_string, longest_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(longest_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      print(longest_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(longest_string(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 449:
#
# ===== SYNTAX =====
#   def shortest_string(lst)
#   return — exits the function and sends back a value
#   shortest() — built-in function call
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(shortest_string(lst)): the inner shortest_string() executes FIRST, then print() outputs the result
#   shortest_string(lst): looks up variable 'lst', then runs the function body
#   print(shortest_string(?)): the inner shortest_string() executes FIRST, then print() outputs the result
#   shortest_string(lst): looks up variable 'lst', then runs the function body
#   shortest_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def shortest_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(shortest_string(...))
#       calls shortest_string with: lst=lst
#         3.1 Return from function
#   4. Define def shortest_string(lst) — stored for later
#   5. Define def shortest_string(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(shortest_string(...))
#       calls shortest_string with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions shortest_string, shortest_string, shortest_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(shortest_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(shortest_string(?)) → function runs first, then print shows the result

"""

"""
Problem 450:
#
# ===== SYNTAX =====
#   def remove_duplicates(lst)
#   return — exits the function and sends back a value
#   duplicates_removed() — built-in function call
#   list() — built-in function call
#   map() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   lambda
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(lst): looks up variable 'lst', then runs the function body
#   print(remove_duplicates(?)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(?): resolves the value, then runs the function body
#   remove_duplicates(?): resolves the value, then runs the function body
#   item not in result: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(lst) — stored for later
#   2. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=['not', 'not', 'today', 'today']
#         2.1 Return from function
#   3. Define def remove_duplicates(lst) — stored for later
#   4. Define def remove_duplicates(lst) — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(remove_duplicates(...))
#       calls remove_duplicates with: lst=[1, 3, 5, 7]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from items each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 451:
#
# ===== SYNTAX =====
#   def fibonacci(n)
#   return — exits the function and sends back a value
#   fib_recursive() — built-in function call
#   if/elif/else
#   operators: + - <=
#
# ===== EVALUATION ORDER =====
#   print(fibonacci(n)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(n): looks up variable 'n', then runs the function body
#   print(fibonacci(15)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(n): looks up variable 'n', then runs the function body
#   fibonacci(15): gets the literal 15, then runs the function body
#   fib_recursive(...) + fib_recursive(...): first get fib_recursive(...), then get fib_recursive(...), then compute fib_recursive(...) + fib_recursive(...)
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 2: first get n, then get 2, then compute n - 2
#   n <= 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci(n) — stored for later
#   2. Assign to n
#   3. Run print(fibonacci(...))
#       calls fibonacci with: n=n
#         3.1 Return from function
#   4. Define def fibonacci(n) — stored for later
#   5. Define def fibonacci(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(fibonacci(...))
#       calls fibonacci with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci, fibonacci, fibonacci are just stored
#   Execution starts at the non-def lines below:
#      n = ... (stored in memory)
#      print(fibonacci(n)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 452:
#
# ===== SYNTAX =====
#   def calculate_sum(a, b)
#   return — exits the function and sends back a value
#   inner_mult() — built-in function call
#   inner_sum() — built-in function call
#   sum() — built-in function call
#   operators: + *
#
# ===== EVALUATION ORDER =====
#   print(calculate_sum(7, 3)): the inner calculate_sum() executes FIRST, then print() outputs the result
#   calculate_sum(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_sum(20, 30)): the inner calculate_sum() executes FIRST, then print() outputs the result
#   calculate_sum(7, 3): gets the literal 7, gets the literal 3, then runs the function body
#   calculate_sum(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#   x * y: first get x, then get y, then compute x * y
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_sum(a, b) — stored for later
#   2. Run print(calculate_sum(...))
#       calls calculate_sum with: a=7, b=3
#         2.1 Import operator
#         2.2 Return from function
#   3. Define def calculate_sum(a, b) — stored for later
#   4. Define def calculate_sum(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_sum(...))
#       calls calculate_sum with: a=20, b=30
#         9.1 Import operator
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_sum, calculate_sum, calculate_sum are just stored
#   Execution starts at the non-def lines below:
#      print(calculate_sum(7, 3)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_sum(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 453:
#
# ===== SYNTAX =====
#   def calculate_integers(a, b)
#   return — exits the function and sends back a value
#   product() — built-in function call
#   sum_int() — built-in function call
#   division() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: + * /
#
# ===== EVALUATION ORDER =====
#   print(calculate_integers(10, 5)): the inner calculate_integers() executes FIRST, then print() outputs the result
#   calculate_integers(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_integers(20, 30)): the inner calculate_integers() executes FIRST, then print() outputs the result
#   calculate_integers(10, 5): gets the literal 10, gets the literal 5, then runs the function body
#   calculate_integers(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   x * y: first get x, then get y, then compute x * y
#   x + y: first get x, then get y, then compute x + y
#   x / y: first get x, then get y, then compute x / y
#
# ===== EXECUTION ORDER =====
#   1. Define def calculate_integers(a, b) — stored for later
#   2. Run print(calculate_integers(...))
#       calls calculate_integers with: a=10, b=5
#         2.1 Return from function
#   3. Define def calculate_integers(a, b) — stored for later
#   4. Define def calculate_integers(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(calculate_integers(...))
#       calls calculate_integers with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions calculate_integers, calculate_integers, calculate_integers are just stored
#   Execution starts at the non-def lines below:
#      print(calculate_integers(10, 5)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_integers(20, 30)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 455:
#
# ===== SYNTAX =====
#   def average_of_list(lst)
#   return — exits the function and sends back a value
#   average() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   for
#   in
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   print(average_of_list(lst)): the inner average_of_list() executes FIRST, then print() outputs the result
#   average_of_list(lst): looks up variable 'lst', then runs the function body
#   print(average_of_list(?)): the inner average_of_list() executes FIRST, then print() outputs the result
#   average_of_list(lst): looks up variable 'lst', then runs the function body
#   add_num * ?: first get add_num, then get ?, then compute add_num * ?
#   average_of_list(?): resolves the value, then runs the function body
#   1 / count_len: first get 1, then get count_len, then compute 1 / count_len
#   add_num / count_len: first get add_num, then get count_len, then compute add_num / count_len
#
# ===== EXECUTION ORDER =====
#   1. Define def average_of_list(lst) — stored for later
#   2. Assign to lst
#   3. Run print(average_of_list(...))
#       calls average_of_list with: lst=lst
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def average_of_list(lst) — stored for later
#   5. Define def average_of_list(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(average_of_list(...))
#       calls average_of_list with: lst=[1, 3, 5, 7]
#         9.1 Import Fraction from fractions
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_of_list, average_of_list, average_of_list are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(average_of_list(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_of_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round

"""

"""
Problem 456:
#
# ===== SYNTAX =====
#   def len_lst(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(len_lst(lst)): the inner len_lst() executes FIRST, then print() outputs the result
#   len_lst(lst): looks up variable 'lst', then runs the function body
#   print(len_lst(?)): the inner len_lst() executes FIRST, then print() outputs the result
#   len_lst(lst): looks up variable 'lst', then runs the function body
#   len_lst(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def len_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(len_lst(...))
#       calls len_lst with: lst=lst
#         3.1 Return from function
#   4. Define def len_lst(lst) — stored for later
#   5. Define def len_lst(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(len_lst(...))
#       calls len_lst with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions len_lst, len_lst, len_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(len_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(len_lst(?)) → function runs first, then print shows the result

"""

"""
Problem 457:
#
# ===== SYNTAX =====
#   def min_len(lst)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(min_len(lst)): the inner min_len() executes FIRST, then print() outputs the result
#   min_len(lst): looks up variable 'lst', then runs the function body
#   print(min_len(?)): the inner min_len() executes FIRST, then print() outputs the result
#   min_len(lst): looks up variable 'lst', then runs the function body
#   min_len(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def min_len(lst) — stored for later
#   2. Assign to lst
#   3. Run print(min_len(...))
#       calls min_len with: lst=lst
#         3.1 Return from function
#   4. Define def min_len(lst) — stored for later
#   5. Define def min_len(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(min_len(...))
#       calls min_len with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions min_len, min_len, min_len are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(min_len(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(min_len(?)) → function runs first, then print shows the result

"""

"""
Problem 458:
#
# ===== SYNTAX =====
#   def max_string_alpha(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(max_string_alpha(?)): the inner max_string_alpha() executes FIRST, then print() outputs the result
#   max_string_alpha(lst): looks up variable 'lst', then runs the function body
#   print(max_string_alpha(?)): the inner max_string_alpha() executes FIRST, then print() outputs the result
#   max_string_alpha(?): resolves the value, then runs the function body
#   max_string_alpha(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def max_string_alpha(lst) — stored for later
#   2. Run print(max_string_alpha(...))
#       calls max_string_alpha with: lst=['apple', 'banana', 'cherry']
#         2.1 Return from function
#   3. Define def max_string_alpha(lst) — stored for later
#   4. Define def max_string_alpha(lst) — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(max_string_alpha(...))
#       calls max_string_alpha with: lst=[1, 3, 5, 7]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_string_alpha, max_string_alpha, max_string_alpha are just stored
#   Execution starts at the non-def lines below:
#      print(max_string_alpha(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(max_string_alpha(?)) → function runs first, then print shows the result

"""

"""
Problem 459:
#
# ===== SYNTAX =====
#   def sort_dicts_by_key(dict_list, sort_key)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   itemgetter() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_dicts_by_key(dict_list, 'age'): looks up variable 'dict_list', gets the literal 'age', then runs the function body
#   sort_dicts_by_key(dict_list, sort_key): looks up variable 'dict_list', looks up variable 'sort_key', then runs the function body
#   print(sort_dicts_by_key(?, ?)): the inner sort_dicts_by_key() executes FIRST, then print() outputs the result
#   sort_dicts_by_key(?, ?): resolves the value, resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import itemgetter from operator
#   2. Define def sort_dicts_by_key(dict_list, sort_key) — stored for later
#   3. Assign to dict_list
#   4. Assign to sorted_list
#   5. Run print(sorted_list)
#   6. Define def sort_dicts_by_key(dict_list, sort_key) — stored for later
#   7. Define def sort_dicts_by_key(dict_list, sort_key) — stored for later
#   8. Assign to dict_list
#   9. Assign to sort_key
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(sort_dicts_by_key(...))
#       calls sort_dicts_by_key with: dict_list=[4, 1, 8, 6, 3], sort_key=[4, 1, 8, 6, 3]
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sort_dicts_by_key, sort_dicts_by_key, sort_dicts_by_key are just stored
#   Execution starts at the non-def lines below:
#      dict_list = ... (stored in memory)
#      sorted_list = ... (stored in memory)
#      print(sorted_list)
#      dict_list = ... (stored in memory)
#      sort_key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_dicts_by_key(?, ?)) → function runs first, then print shows the result

"""

"""
Problem 460:
#
# ===== SYNTAX =====
#   def sort_by_absolute_value(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_by_absolute_value(lst): looks up variable 'lst', then runs the function body
#   sort_by_absolute_value(lst): looks up variable 'lst', then runs the function body
#   print(sort_by_absolute_value(?)): the inner sort_by_absolute_value() executes FIRST, then print() outputs the result
#   sort_by_absolute_value(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_absolute_value(lst) — stored for later
#   2. Assign to lst
#   3. Assign to sorted_lst
#   4. Run print(sorted_lst)
#   5. Define def sort_by_absolute_value(lst) — stored for later
#   6. Define def sort_by_absolute_value(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_by_absolute_value(...))
#       calls sort_by_absolute_value with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_absolute_value, sort_by_absolute_value, sort_by_absolute_value are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sorted_lst = ... (stored in memory)
#      print(sorted_lst)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_absolute_value(?)) → function runs first, then print shows the result

"""

"""
Problem 461:
#
# ===== SYNTAX =====
#   def sort_words_by_vowel_count(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(sort_words_by_vowel_count(lst)): the inner sort_words_by_vowel_count() executes FIRST, then print() outputs the result
#   sort_words_by_vowel_count(lst): looks up variable 'lst', then runs the function body
#   print(sort_words_by_vowel_count(?)): the inner sort_words_by_vowel_count() executes FIRST, then print() outputs the result
#   sort_words_by_vowel_count(lst): looks up variable 'lst', then runs the function body
#   sort_words_by_vowel_count(?): resolves the value, then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_words_by_vowel_count(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_words_by_vowel_count(...))
#       calls sort_words_by_vowel_count with: lst=lst
#         3.1 Return from function
#   4. Define def sort_words_by_vowel_count(lst) — stored for later
#   5. Define def sort_words_by_vowel_count(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_words_by_vowel_count(...))
#       calls sort_words_by_vowel_count with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_words_by_vowel_count, sort_words_by_vowel_count, sort_words_by_vowel_count are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_words_by_vowel_count(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_words_by_vowel_count(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 462:
#
# ===== SYNTAX =====
#   def longest_word(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(longest_word(lst)): the inner longest_word() executes FIRST, then print() outputs the result
#   longest_word(lst): looks up variable 'lst', then runs the function body
#   print(longest_word(?)): the inner longest_word() executes FIRST, then print() outputs the result
#   longest_word(lst): looks up variable 'lst', then runs the function body
#   longest_word(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def longest_word(lst) — stored for later
#   2. Assign to lst
#   3. Run print(longest_word(...))
#       calls longest_word with: lst=lst
#         3.1 Return from function
#   4. Define def longest_word(lst) — stored for later
#   5. Define def longest_word(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(longest_word(...))
#       calls longest_word with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions longest_word, longest_word, longest_word are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(longest_word(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(longest_word(?)) → function runs first, then print shows the result

"""

"""
Problem 463:
#
# ===== SYNTAX =====
#   def sum_of_tuple(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sum_of_tuple(lst)): the inner sum_of_tuple() executes FIRST, then print() outputs the result
#   sum_of_tuple(lst): looks up variable 'lst', then runs the function body
#   print(sum_of_tuple(?)): the inner sum_of_tuple() executes FIRST, then print() outputs the result
#   sum_of_tuple(lst): looks up variable 'lst', then runs the function body
#   sum_of_tuple(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_tuple(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sum_of_tuple(...))
#       calls sum_of_tuple with: lst=lst
#         3.1 Return from function
#   4. Define def sum_of_tuple(lst) — stored for later
#   5. Define def sum_of_tuple(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sum_of_tuple(...))
#       calls sum_of_tuple with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_tuple, sum_of_tuple, sum_of_tuple are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sum_of_tuple(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_tuple(?)) → function runs first, then print shows the result

"""

"""
Problem 464:
#
# ===== SYNTAX =====
#   def list_of_strings_lst(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(list_of_strings_lst(lst)): the inner list_of_strings_lst() executes FIRST, then print() outputs the result
#   list_of_strings_lst(lst): looks up variable 'lst', then runs the function body
#   print(list_of_strings_lst(?)): the inner list_of_strings_lst() executes FIRST, then print() outputs the result
#   list_of_strings_lst(lst): looks up variable 'lst', then runs the function body
#   list_of_strings_lst(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_of_strings_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(list_of_strings_lst(...))
#       calls list_of_strings_lst with: lst=lst
#         3.1 Return from function
#   4. Define def list_of_strings_lst(lst) — stored for later
#   5. Define def list_of_strings_lst(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(list_of_strings_lst(...))
#       calls list_of_strings_lst with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_of_strings_lst, list_of_strings_lst, list_of_strings_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(list_of_strings_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_of_strings_lst(?)) → function runs first, then print shows the result

"""

"""
Problem 465:
#
# ===== SYNTAX =====
#   def sort_number_uppercases(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(sort_number_uppercases(lst)): the inner sort_number_uppercases() executes FIRST, then print() outputs the result
#   sort_number_uppercases(lst): looks up variable 'lst', then runs the function body
#   print(sort_number_uppercases(?)): the inner sort_number_uppercases() executes FIRST, then print() outputs the result
#   sort_number_uppercases(lst): looks up variable 'lst', then runs the function body
#   sort_number_uppercases(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_number_uppercases(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_number_uppercases(...))
#       calls sort_number_uppercases with: lst=lst
#         3.1 Return from function
#   4. Define def sort_number_uppercases(lst) — stored for later
#   5. Define def sort_number_uppercases(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_number_uppercases(...))
#       calls sort_number_uppercases with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_number_uppercases, sort_number_uppercases, sort_number_uppercases are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_number_uppercases(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_number_uppercases(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 466:
#
# ===== SYNTAX =====
#   def tuple_largest_product(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(tuple_largest_product(lst)): the inner tuple_largest_product() executes FIRST, then print() outputs the result
#   tuple_largest_product(lst): looks up variable 'lst', then runs the function body
#   print(tuple_largest_product(?)): the inner tuple_largest_product() executes FIRST, then print() outputs the result
#   tuple_largest_product(lst): looks up variable 'lst', then runs the function body
#   tuple_largest_product(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def tuple_largest_product(lst) — stored for later
#   2. Assign to lst
#   3. Run print(tuple_largest_product(...))
#       calls tuple_largest_product with: lst=lst
#         3.1 Return from function
#   4. Define def tuple_largest_product(lst) — stored for later
#   5. Define def tuple_largest_product(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(tuple_largest_product(...))
#       calls tuple_largest_product with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions tuple_largest_product, tuple_largest_product, tuple_largest_product are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(tuple_largest_product(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(tuple_largest_product(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round

"""

"""
Problem 467:
#
# ===== SYNTAX =====
#   def floating_point(numbers)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(floating_point(numbers)): the inner floating_point() executes FIRST, then print() outputs the result
#   floating_point(numbers): looks up variable 'numbers', then runs the function body
#   print(floating_point(?)): the inner floating_point() executes FIRST, then print() outputs the result
#   floating_point(numbers): looks up variable 'numbers', then runs the function body
#   floating_point(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def floating_point(numbers) — stored for later
#   2. Assign to numbers
#   3. Run print(floating_point(...))
#       calls floating_point with: numbers=numbers
#         3.1 Return from function
#   4. Define def floating_point(numbers) — stored for later
#   5. Define def floating_point(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(floating_point(...))
#       calls floating_point with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions floating_point, floating_point, floating_point are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      print(floating_point(numbers)) → function runs first, then print shows the result
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(floating_point(?)) → function runs first, then print shows the result

"""

"""
Problem 468:
#
# ===== SYNTAX =====
#   def first_element_decending_order(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(first_element_decending_order(lst)): the inner first_element_decending_order() executes FIRST, then print() outputs the result
#   first_element_decending_order(lst): looks up variable 'lst', then runs the function body
#   print(first_element_decending_order(?)): the inner first_element_decending_order() executes FIRST, then print() outputs the result
#   first_element_decending_order(lst): looks up variable 'lst', then runs the function body
#   first_element_decending_order(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def first_element_decending_order(lst) — stored for later
#   2. Assign to lst
#   3. Run print(first_element_decending_order(...))
#       calls first_element_decending_order with: lst=lst
#         3.1 Return from function
#   4. Define def first_element_decending_order(lst) — stored for later
#   5. Define def first_element_decending_order(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(first_element_decending_order(...))
#       calls first_element_decending_order with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions first_element_decending_order, first_element_decending_order, first_element_decending_order are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(first_element_decending_order(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(first_element_decending_order(?)) → function runs first, then print shows the result

"""

"""
Problem 469:
#
# ===== SYNTAX =====
#   def number_of_consonants(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(number_of_consonants(lst)): the inner number_of_consonants() executes FIRST, then print() outputs the result
#   number_of_consonants(lst): looks up variable 'lst', then runs the function body
#   print(number_of_consonants(?)): the inner number_of_consonants() executes FIRST, then print() outputs the result
#   number_of_consonants(lst): looks up variable 'lst', then runs the function body
#   number_of_consonants(?): resolves the value, then runs the function body
#   char in consonants_set: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def number_of_consonants(lst) — stored for later
#   2. Assign to lst
#   3. Run print(number_of_consonants(...))
#       calls number_of_consonants with: lst=lst
#         3.1 Return from function
#   4. Define def number_of_consonants(lst) — stored for later
#   5. Define def number_of_consonants(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(number_of_consonants(...))
#       calls number_of_consonants with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions number_of_consonants, number_of_consonants, number_of_consonants are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(number_of_consonants(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(number_of_consonants(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word.lower(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 470:
#
# ===== SYNTAX =====
#   def min_string(lst)
#   return — exits the function and sends back a value
#   min() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(min_string(lst)): the inner min_string() executes FIRST, then print() outputs the result
#   min_string(lst): looks up variable 'lst', then runs the function body
#   print(min_string(?)): the inner min_string() executes FIRST, then print() outputs the result
#   min_string(lst): looks up variable 'lst', then runs the function body
#   min_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def min_string(lst) — stored for later
#   2. Assign to lst
#   3. Run print(min_string(...))
#       calls min_string with: lst=lst
#         3.1 Return from function
#   4. Define def min_string(lst) — stored for later
#   5. Define def min_string(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(min_string(...))
#       calls min_string with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions min_string, min_string, min_string are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(min_string(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(min_string(?)) → function runs first, then print shows the result

"""

"""
Problem 471:
#
# ===== SYNTAX =====
#   def length_of_value(lst, key)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#
# ===== EVALUATION ORDER =====
#   length_of_value(lst, 'b'): looks up variable 'lst', gets the literal 'b', then runs the function body
#   length_of_value(lst, key): looks up variable 'lst', looks up variable 'key', then runs the function body
#   print(length_of_value(?, 'value')): the inner length_of_value() executes FIRST, then print() outputs the result
#   length_of_value(?, 'value'): resolves the value, gets the literal 'value', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def length_of_value(lst, key) — stored for later
#   2. Assign to lst
#   3. Assign to sorted_lst
#   4. Run print(sorted_lst)
#   5. Define def length_of_value(lst, key) — stored for later
#   6. Define def length_of_value(lst, key) — stored for later
#   7. Assign to lst
#   8. Assign to key
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(length_of_value(...))
#       calls length_of_value with: lst=[1, 3, 5, 7], key='value'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions length_of_value, length_of_value, length_of_value are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sorted_lst = ... (stored in memory)
#      print(sorted_lst)
#      lst = ... (stored in memory)
#      key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(length_of_value(?, 'value')) → function runs first, then print shows the result

"""

"""
Problem 472:
#
# ===== SYNTAX =====
#   def find_person(lst_dict, key)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   len() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(find_person(lst_dict, key)): the inner find_person() executes FIRST, then print() outputs the result
#   find_person(lst_dict, key): looks up variable 'lst_dict', looks up variable 'key', then runs the function body
#   print(find_person(?, 'value')): the inner find_person() executes FIRST, then print() outputs the result
#   find_person(lst_dict, key): looks up variable 'lst_dict', looks up variable 'key', then runs the function body
#   find_person(?, 'value'): resolves the value, gets the literal 'value', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def find_person(lst_dict, key) — stored for later
#   2. Assign to lst_dict
#   3. Assign to key
#   4. Run print(find_person(...))
#       calls find_person with: lst_dict=lst_dict, key=key
#         4.1 Return from function
#   5. Define def find_person(lst_dict, key) — stored for later
#   6. Define def find_person(lst_dict, key) — stored for later
#   7. Assign to lst_dict
#   8. Assign to key
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(find_person(...))
#       calls find_person with: lst_dict=[4, 1, 8, 6, 3], key='value'
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_person, find_person, find_person are just stored
#   Execution starts at the non-def lines below:
#      lst_dict = ... (stored in memory)
#      key = ... (stored in memory)
#      print(find_person(lst_dict, key)) → function runs first, then print shows the result
#      lst_dict = ... (stored in memory)
#      key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_person(?, 'value')) → function runs first, then print shows the result

"""

"""
Problem 473:
#
# ===== SYNTAX =====
#   def main(lst, divisor)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   remainder_key() — built-in function call
#   divmod() — built-in function call
#   operators: - * // %
#
# ===== EVALUATION ORDER =====
#   main(lst, divisor): looks up variable 'lst', looks up variable 'divisor', then runs the function body
#   print(sort_by_remainder(lst, divisor)): the inner sort_by_remainder() executes FIRST, then print() outputs the result
#   main(lst, divisor): looks up variable 'lst', looks up variable 'divisor', then runs the function body
#   print(main(?, 25)): the inner main() executes FIRST, then print() outputs the result
#   x % divisor: first get x, then get divisor, then compute x % divisor
#   sort_by_remainder(lst, divisor): looks up variable 'lst', looks up variable 'divisor', then runs the function body
#   num - ?: first get num, then get ?, then compute num - ?
#   main(?, 25): resolves the value, gets the literal 25, then runs the function body
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   remainder_key(x, divisor): looks up variable 'x', looks up variable 'divisor', then runs the function body
#   divisor * ?: first get divisor, then get ?, then compute divisor * ?
#   num // divisor: first get num, then get divisor, then compute num // divisor
#
# ===== EXECUTION ORDER =====
#   1. Define def main(lst, divisor) — stored for later
#   2. Assign to lst
#   3. Assign to divisor
#   4. Run main(lst, divisor)
#       with: lst=lst, divisor=divisor
#         4.1 Return from function
#   5. Define def remainder_key(x, divisor) — stored for later
#   6. Define def sort_by_remainder(lst, divisor) — stored for later
#   7. Assign to lst
#   8. Assign to divisor
#   9. Run print(sort_by_remainder(...))
#       calls sort_by_remainder with: lst=lst, divisor=divisor
#         9.1 Define def key_func(x) — stored for later
#         9.2 Return from function
#   10. Define def main(lst, divisor) — stored for later
#   11. Define def main(lst, divisor) — stored for later
#   12. Assign to lst
#   13. Assign to divisor
#   14. Assign to result
#   15. Run print(result)
#   16. Run print(main(...))
#       calls main with: lst=[1, 3, 5, 7], divisor=25
#         16.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, remainder_key, sort_by_remainder, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      divisor = ... (stored in memory)
#      main(lst, divisor) → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      divisor = ... (stored in memory)
#      print(sort_by_remainder(lst, divisor)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      divisor = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 474:
#
# ===== SYNTAX =====
#   def year_key(date)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_by_year(?)): the inner sort_by_year() executes FIRST, then print() outputs the result
#   year_key(date): looks up variable 'date', then runs the function body
#   print(year_key(25)): the inner year_key() executes FIRST, then print() outputs the result
#   sort_by_year(?): resolves the value, then runs the function body
#   year_key(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import datetime from datetime
#   2. Define def year_key(date) — stored for later
#   3. Define def sort_by_year(dates) — stored for later
#   4. Run print(sort_by_year(...))
#       calls sort_by_year with: dates=['2023-10-01', '2020-01-15', '2025-12-31']
#         4.1 Return from function
#   5. Define def year_key(date) — stored for later
#   6. Define def year_key(date) — stored for later
#   7. Assign to date
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(year_key(...))
#       calls year_key with: date=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions year_key, sort_by_year, year_key, year_key are just stored
#   Execution starts at the non-def lines below:
#      print(sort_by_year(?)) → function runs first, then print shows the result
#      date = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(year_key(25)) → function runs first, then print shows the result

"""

"""
Problem 475:
#
# ===== SYNTAX =====
#   def sort_by_a_count(names)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sort_by_a_count(names): looks up variable 'names', then runs the function body
#   sort_by_a_count(names): looks up variable 'names', then runs the function body
#   print(sort_by_a_count(?)): the inner sort_by_a_count() executes FIRST, then print() outputs the result
#   sort_by_a_count(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_a_count(names) — stored for later
#   2. Assign to names
#   3. Assign to sorted_names
#   4. Run print(sorted_names)
#   5. Define def sort_by_a_count(names) — stored for later
#   6. Define def sort_by_a_count(names) — stored for later
#   7. Assign to names
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_by_a_count(...))
#       calls sort_by_a_count with: names=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_a_count, sort_by_a_count, sort_by_a_count are just stored
#   Execution starts at the non-def lines below:
#      names = ... (stored in memory)
#      sorted_names = ... (stored in memory)
#      print(sorted_names)
#      names = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_a_count(?)) → function runs first, then print shows the result

"""

"""
Problem 476:
#
# ===== SYNTAX =====
#   def sort_sqaure(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(sort_sqaure(lst)): the inner sort_sqaure() executes FIRST, then print() outputs the result
#   sort_sqaure(lst): looks up variable 'lst', then runs the function body
#   print(sort_sqaure(?)): the inner sort_sqaure() executes FIRST, then print() outputs the result
#   sort_sqaure(lst): looks up variable 'lst', then runs the function body
#   sort_sqaure(?): resolves the value, then runs the function body
#   x * x: first get x, then get x, then compute x * x
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_sqaure(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_sqaure(...))
#       calls sort_sqaure with: lst=lst
#         3.1 Return from function
#   4. Define def sort_sqaure(lst) — stored for later
#   5. Define def sort_sqaure(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_sqaure(...))
#       calls sort_sqaure with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_sqaure, sort_sqaure, sort_sqaure are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_sqaure(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_sqaure(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 477:
#
# ===== SYNTAX =====
#   def closest_to_target(lst, target)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   abs() — built-in function call
#   operators: -
#
# ===== EVALUATION ORDER =====
#   closest_to_target(lst, target): looks up variable 'lst', looks up variable 'target', then runs the function body
#   closest_to_target(lst, target): looks up variable 'lst', looks up variable 'target', then runs the function body
#   print(closest_to_target(?, 'goal')): the inner closest_to_target() executes FIRST, then print() outputs the result
#   closest_to_target(?, 'goal'): resolves the value, gets the literal 'goal', then runs the function body
#   x - target: first get x, then get target, then compute x - target
#   x - target: first get x, then get target, then compute x - target
#   x - target: first get x, then get target, then compute x - target
#
# ===== EXECUTION ORDER =====
#   1. Define def closest_to_target(lst, target) — stored for later
#   2. Assign to lst
#   3. Assign to target
#   4. Assign to closest_value
#   5. Run print(closest_value)
#   6. Define def closest_to_target(lst, target) — stored for later
#   7. Define def closest_to_target(lst, target) — stored for later
#   8. Assign to lst
#   9. Assign to target
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(closest_to_target(...))
#       calls closest_to_target with: lst=[1, 3, 5, 7], target='goal'
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions closest_to_target, closest_to_target, closest_to_target are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      target = ... (stored in memory)
#      closest_value = ... (stored in memory)
#      print(closest_value)
#      lst = ... (stored in memory)
#      target = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(closest_to_target(?, 'goal')) → function runs first, then print shows the result

"""

"""
Problem 478:
#
# ===== SYNTAX =====
#   def length_of_first_element(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#
# ===== EVALUATION ORDER =====
#   length_of_first_element(lst): looks up variable 'lst', then runs the function body
#   length_of_first_element(lst): looks up variable 'lst', then runs the function body
#   print(length_of_first_element(?)): the inner length_of_first_element() executes FIRST, then print() outputs the result
#   length_of_first_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def length_of_first_element(lst) — stored for later
#   2. Assign to lst
#   3. Assign to sorted_list
#   4. Run print(sorted_list)
#   5. Define def length_of_first_element(lst) — stored for later
#   6. Define def length_of_first_element(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(length_of_first_element(...))
#       calls length_of_first_element with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions length_of_first_element, length_of_first_element, length_of_first_element are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sorted_list = ... (stored in memory)
#      print(sorted_list)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(length_of_first_element(?)) → function runs first, then print shows the result

"""

"""
Problem 479:
#
# ===== SYNTAX =====
#   def number_of_spaces(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(number_of_spaces(lst)): the inner number_of_spaces() executes FIRST, then print() outputs the result
#   number_of_spaces(lst): looks up variable 'lst', then runs the function body
#   print(number_of_spaces(?)): the inner number_of_spaces() executes FIRST, then print() outputs the result
#   number_of_spaces(lst): looks up variable 'lst', then runs the function body
#   number_of_spaces(?): resolves the value, then runs the function body
#   c == ' ': is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def number_of_spaces(lst) — stored for later
#   2. Assign to lst
#   3. Run print(number_of_spaces(...))
#       calls number_of_spaces with: lst=lst
#         3.1 Assign to count
#         3.2 For loop over x
#         3.3 Return from function
#   4. Define def number_of_spaces(lst) — stored for later
#   5. Define def number_of_spaces(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(number_of_spaces(...))
#       calls number_of_spaces with: lst=[1, 3, 5, 7]
#         9.1 Assign to count
#         9.2 For loop over x
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions number_of_spaces, number_of_spaces, number_of_spaces are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(number_of_spaces(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(number_of_spaces(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from x each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 480:
#
# ===== SYNTAX =====
#   def most_vowels(lst)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(most_vowels(lst)): the inner most_vowels() executes FIRST, then print() outputs the result
#   most_vowels(lst): looks up variable 'lst', then runs the function body
#   print(most_vowels(?)): the inner most_vowels() executes FIRST, then print() outputs the result
#   most_vowels(lst): looks up variable 'lst', then runs the function body
#   most_vowels(?): resolves the value, then runs the function body
#   char in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def most_vowels(lst) — stored for later
#   2. Assign to lst
#   3. Run print(most_vowels(...))
#       calls most_vowels with: lst=lst
#         3.1 Return from function
#   4. Define def most_vowels(lst) — stored for later
#   5. Define def most_vowels(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(most_vowels(...))
#       calls most_vowels with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions most_vowels, most_vowels, most_vowels are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(most_vowels(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(most_vowels(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 481:
#
# ===== SYNTAX =====
#   def sort_lst_integers(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   str() — built-in function call
#   abs() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_lst_integers(lst)): the inner sort_lst_integers() executes FIRST, then print() outputs the result
#   sort_lst_integers(lst): looks up variable 'lst', then runs the function body
#   print(sort_lst_integers(?)): the inner sort_lst_integers() executes FIRST, then print() outputs the result
#   sort_lst_integers(lst): looks up variable 'lst', then runs the function body
#   sort_lst_integers(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_lst_integers(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_lst_integers(...))
#       calls sort_lst_integers with: lst=lst
#         3.1 Return from function
#   4. Define def sort_lst_integers(lst) — stored for later
#   5. Define def sort_lst_integers(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_lst_integers(...))
#       calls sort_lst_integers with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_lst_integers, sort_lst_integers, sort_lst_integers are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_lst_integers(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_lst_integers(?)) → function runs first, then print shows the result

"""

"""
Problem 482:
#
# ===== SYNTAX =====
#   def sort_tuples_by_difference(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   abs() — built-in function call
#   operators: -
#
# ===== EVALUATION ORDER =====
#   print(sort_tuples_by_difference(lst)): the inner sort_tuples_by_difference() executes FIRST, then print() outputs the result
#   sort_tuples_by_difference(lst): looks up variable 'lst', then runs the function body
#   print(sort_tuples_by_difference(?)): the inner sort_tuples_by_difference() executes FIRST, then print() outputs the result
#   sort_tuples_by_difference(lst): looks up variable 'lst', then runs the function body
#   sort_tuples_by_difference(?): resolves the value, then runs the function body
#   ? - ?: first get ?, then get ?, then compute ? - ?
#   ? - ?: first get ?, then get ?, then compute ? - ?
#   ? - ?: first get ?, then get ?, then compute ? - ?
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_tuples_by_difference(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_tuples_by_difference(...))
#       calls sort_tuples_by_difference with: lst=lst
#         3.1 Return from function
#   4. Define def sort_tuples_by_difference(lst) — stored for later
#   5. Define def sort_tuples_by_difference(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_tuples_by_difference(...))
#       calls sort_tuples_by_difference with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_tuples_by_difference, sort_tuples_by_difference, sort_tuples_by_difference are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_tuples_by_difference(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_tuples_by_difference(?)) → function runs first, then print shows the result

"""

"""
Problem 483:
#
# ===== SYNTAX =====
#   def sort_strings_by_ascii(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   ord() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_strings_by_ascii(lst)): the inner sort_strings_by_ascii() executes FIRST, then print() outputs the result
#   sort_strings_by_ascii(lst): looks up variable 'lst', then runs the function body
#   print(sort_strings_by_ascii(?)): the inner sort_strings_by_ascii() executes FIRST, then print() outputs the result
#   sort_strings_by_ascii(lst): looks up variable 'lst', then runs the function body
#   sort_strings_by_ascii(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_strings_by_ascii(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_strings_by_ascii(...))
#       calls sort_strings_by_ascii with: lst=lst
#         3.1 Return from function
#   4. Define def sort_strings_by_ascii(lst) — stored for later
#   5. Define def sort_strings_by_ascii(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_strings_by_ascii(...))
#       calls sort_strings_by_ascii with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_strings_by_ascii, sort_strings_by_ascii, sort_strings_by_ascii are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_strings_by_ascii(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_strings_by_ascii(?)) → function runs first, then print shows the result

"""

"""
Problem 484:
#
# ===== SYNTAX =====
#   def sort_lists_by_sum(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   sum() — built-in function call
#   reduce() — built-in function call
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(sort_lists_by_sum(lst)): the inner sort_lists_by_sum() executes FIRST, then print() outputs the result
#   sort_lists_by_sum(lst): looks up variable 'lst', then runs the function body
#   print(sort_lists_by_sum(?)): the inner sort_lists_by_sum() executes FIRST, then print() outputs the result
#   sort_lists_by_sum(lst): looks up variable 'lst', then runs the function body
#   sort_lists_by_sum(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_lists_by_sum(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_lists_by_sum(...))
#       calls sort_lists_by_sum with: lst=lst
#         3.1 Import reduce from functools
#         3.2 Import operator
#         3.3 Return from function
#   4. Define def sort_lists_by_sum(lst) — stored for later
#   5. Define def sort_lists_by_sum(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_lists_by_sum(...))
#       calls sort_lists_by_sum with: lst=[1, 3, 5, 7]
#         9.1 Import reduce from functools
#         9.2 Import operator
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_lists_by_sum, sort_lists_by_sum, sort_lists_by_sum are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_lists_by_sum(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_lists_by_sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from sublist each round

"""

"""
Problem 485:
#
# ===== SYNTAX =====
#   def number_of_consonants(lst)
#   return — exits the function and sends back a value
#   min() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(number_of_consonants(lst)): the inner number_of_consonants() executes FIRST, then print() outputs the result
#   number_of_consonants(lst): looks up variable 'lst', then runs the function body
#   print(number_of_consonants(?)): the inner number_of_consonants() executes FIRST, then print() outputs the result
#   number_of_consonants(lst): looks up variable 'lst', then runs the function body
#   number_of_consonants(?): resolves the value, then runs the function body
#   char in consonants_set: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def number_of_consonants(lst) — stored for later
#   2. Assign to lst
#   3. Run print(number_of_consonants(...))
#       calls number_of_consonants with: lst=lst
#         3.1 Return from function
#   4. Define def number_of_consonants(lst) — stored for later
#   5. Define def number_of_consonants(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(number_of_consonants(...))
#       calls number_of_consonants with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions number_of_consonants, number_of_consonants, number_of_consonants are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(number_of_consonants(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(number_of_consonants(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from word.lower(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 486:
#
# ===== SYNTAX =====
#   def sort_files_by_extension(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_files_by_extension(lst)): the inner sort_files_by_extension() executes FIRST, then print() outputs the result
#   sort_files_by_extension(lst): looks up variable 'lst', then runs the function body
#   print(sort_files_by_extension(?)): the inner sort_files_by_extension() executes FIRST, then print() outputs the result
#   sort_files_by_extension(lst): looks up variable 'lst', then runs the function body
#   sort_files_by_extension(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_files_by_extension(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_files_by_extension(...))
#       calls sort_files_by_extension with: lst=lst
#         3.1 Return from function
#   4. Define def sort_files_by_extension(lst) — stored for later
#   5. Define def sort_files_by_extension(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_files_by_extension(...))
#       calls sort_files_by_extension with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_files_by_extension, sort_files_by_extension, sort_files_by_extension are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_files_by_extension(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_files_by_extension(?)) → function runs first, then print shows the result

"""

"""
Problem 487:
#
# ===== SYNTAX =====
#   def sqaure_numbers_lst(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   range() — built-in function call
#   pow() — built-in function call
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(sqaure_numbers_lst(lst)): the inner sqaure_numbers_lst() executes FIRST, then print() outputs the result
#   sqaure_numbers_lst(lst): looks up variable 'lst', then runs the function body
#   print(sqaure_numbers_lst(?)): the inner sqaure_numbers_lst() executes FIRST, then print() outputs the result
#   sqaure_numbers_lst(lst): looks up variable 'lst', then runs the function body
#   sqaure_numbers_lst(?): resolves the value, then runs the function body
#   n * n: first get n, then get n, then compute n * n
#
# ===== EXECUTION ORDER =====
#   1. Define def sqaure_numbers_lst(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sqaure_numbers_lst(...))
#       calls sqaure_numbers_lst with: lst=lst
#         3.1 Return from function
#   4. Define def sqaure_numbers_lst(lst) — stored for later
#   5. Define def sqaure_numbers_lst(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sqaure_numbers_lst(...))
#       calls sqaure_numbers_lst with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sqaure_numbers_lst, sqaure_numbers_lst, sqaure_numbers_lst are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sqaure_numbers_lst(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sqaure_numbers_lst(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 488:
#
# ===== SYNTAX =====
#   def sort_words_by_distinct_letters(words)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   set() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_words_by_distinct_letters(words)): the inner sort_words_by_distinct_letters() executes FIRST, then print() outputs the result
#   sort_words_by_distinct_letters(words): looks up variable 'words', then runs the function body
#   print(sort_words_by_distinct_letters(?)): the inner sort_words_by_distinct_letters() executes FIRST, then print() outputs the result
#   sort_words_by_distinct_letters(words): looks up variable 'words', then runs the function body
#   sort_words_by_distinct_letters(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_words_by_distinct_letters(words) — stored for later
#   2. Assign to words
#   3. Run print(sort_words_by_distinct_letters(...))
#       calls sort_words_by_distinct_letters with: words=words
#         3.1 Return from function
#   4. Define def sort_words_by_distinct_letters(words) — stored for later
#   5. Define def sort_words_by_distinct_letters(words) — stored for later
#   6. Assign to words
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_words_by_distinct_letters(...))
#       calls sort_words_by_distinct_letters with: words=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_words_by_distinct_letters, sort_words_by_distinct_letters, sort_words_by_distinct_letters are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(sort_words_by_distinct_letters(words)) → function runs first, then print shows the result
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_words_by_distinct_letters(?)) → function runs first, then print shows the result

"""

"""
Problem 489:
#
# ===== SYNTAX =====
#   def sort_second_letter(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_second_letter(lst)): the inner sort_second_letter() executes FIRST, then print() outputs the result
#   sort_second_letter(lst): looks up variable 'lst', then runs the function body
#   print(sort_second_letter(?)): the inner sort_second_letter() executes FIRST, then print() outputs the result
#   sort_second_letter(lst): looks up variable 'lst', then runs the function body
#   sort_second_letter(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_second_letter(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_second_letter(...))
#       calls sort_second_letter with: lst=lst
#         3.1 Return from function
#   4. Define def sort_second_letter(lst) — stored for later
#   5. Define def sort_second_letter(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_second_letter(...))
#       calls sort_second_letter with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_second_letter, sort_second_letter, sort_second_letter are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_second_letter(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_second_letter(?)) → function runs first, then print shows the result

"""

"""
Problem 490:
#
# ===== SYNTAX =====
#   def people_age(dic)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(people_age(people)): the inner people_age() executes FIRST, then print() outputs the result
#   people_age(dic): looks up variable 'dic', then runs the function body
#   print(people_age(?)): the inner people_age() executes FIRST, then print() outputs the result
#   people_age(people): looks up variable 'people', then runs the function body
#   people_age(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def people_age(dic) — stored for later
#   2. Assign to people
#   3. Run print(people_age(...))
#       calls people_age with: dic=people
#         3.1 Return from function
#   4. Define def people_age(dic) — stored for later
#   5. Define def people_age(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(people_age(...))
#       calls people_age with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions people_age, people_age, people_age are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      print(people_age(people)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(people_age(?)) → function runs first, then print shows the result

"""

"""
Problem 491:
#
# ===== SYNTAX =====
#   def sort_by_binary_representation(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   bin() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_by_binary_representation(lst)): the inner sort_by_binary_representation() executes FIRST, then print() outputs the result
#   sort_by_binary_representation(lst): looks up variable 'lst', then runs the function body
#   print(sort_by_binary_representation(?)): the inner sort_by_binary_representation() executes FIRST, then print() outputs the result
#   sort_by_binary_representation(lst): looks up variable 'lst', then runs the function body
#   sort_by_binary_representation(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_binary_representation(lst) — stored for later
#   2. Assign to lst
#   3. Run print(sort_by_binary_representation(...))
#       calls sort_by_binary_representation with: lst=lst
#         3.1 Return from function
#   4. Define def sort_by_binary_representation(lst) — stored for later
#   5. Define def sort_by_binary_representation(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_by_binary_representation(...))
#       calls sort_by_binary_representation with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_binary_representation, sort_by_binary_representation, sort_by_binary_representation are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(sort_by_binary_representation(lst)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_binary_representation(?)) → function runs first, then print shows the result

"""

"""
Problem 492:
#
# ===== SYNTAX =====
#   def sort_keys_by_value(dic)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_keys_by_value(dic)): the inner sort_keys_by_value() executes FIRST, then print() outputs the result
#   sort_keys_by_value(dic): looks up variable 'dic', then runs the function body
#   print(sort_keys_by_value(?)): the inner sort_keys_by_value() executes FIRST, then print() outputs the result
#   sort_keys_by_value(dic): looks up variable 'dic', then runs the function body
#   sort_keys_by_value(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_keys_by_value(dic) — stored for later
#   2. Assign to dic
#   3. Run print(sort_keys_by_value(...))
#       calls sort_keys_by_value with: dic=dic
#         3.1 Return from function
#   4. Define def sort_keys_by_value(dic) — stored for later
#   5. Define def sort_keys_by_value(dic) — stored for later
#   6. Assign to dic
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_keys_by_value(...))
#       calls sort_keys_by_value with: dic=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_keys_by_value, sort_keys_by_value, sort_keys_by_value are just stored
#   Execution starts at the non-def lines below:
#      dic = ... (stored in memory)
#      print(sort_keys_by_value(dic)) → function runs first, then print shows the result
#      dic = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_keys_by_value(?)) → function runs first, then print shows the result

"""

"""
Problem 493:
#
# ===== SYNTAX =====
#   def sort_fractions_by_decimal(fractions)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   float() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(sort_fractions_by_decimal(fractions)): the inner sort_fractions_by_decimal() executes FIRST, then print() outputs the result
#   sort_fractions_by_decimal(fractions): looks up variable 'fractions', then runs the function body
#   print(sort_fractions_by_decimal(?)): the inner sort_fractions_by_decimal() executes FIRST, then print() outputs the result
#   sort_fractions_by_decimal(fractions): looks up variable 'fractions', then runs the function body
#   sort_fractions_by_decimal(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import Fraction from fractions
#   2. Define def sort_fractions_by_decimal(fractions) — stored for later
#   3. Assign to fractions
#   4. Run print(sort_fractions_by_decimal(...))
#       calls sort_fractions_by_decimal with: fractions=fractions
#         4.1 Return from function
#   5. Define def sort_fractions_by_decimal(fractions) — stored for later
#   6. Define def sort_fractions_by_decimal(fractions) — stored for later
#   7. Assign to fractions
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sort_fractions_by_decimal(...))
#       calls sort_fractions_by_decimal with: fractions=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sort_fractions_by_decimal, sort_fractions_by_decimal, sort_fractions_by_decimal are just stored
#   Execution starts at the non-def lines below:
#      fractions = ... (stored in memory)
#      print(sort_fractions_by_decimal(fractions)) → function runs first, then print shows the result
#      fractions = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_fractions_by_decimal(?)) → function runs first, then print shows the result

"""

"""
Problem 494:
#
# ===== SYNTAX =====
#   def longest_string_letter(lst, letter)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   len() — built-in function call
#   letter_ends() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(longest_string_letter(lst, 'n')): the inner longest_string_letter() executes FIRST, then print() outputs the result
#   print(longest_string_letter(lst, 'a')): the inner longest_string_letter() executes FIRST, then print() outputs the result
#   print(longest_string(input_string, letter)): the inner longest_string() executes FIRST, then print() outputs the result
#   longest_string_letter(lst, letter): looks up variable 'lst', looks up variable 'letter', then runs the function body
#   print(longest_string_letter(?, ?)): the inner longest_string_letter() executes FIRST, then print() outputs the result
#   longest_string_letter(lst, 'n'): looks up variable 'lst', gets the literal 'n', then runs the function body
#   longest_string_letter(lst, 'a'): looks up variable 'lst', gets the literal 'a', then runs the function body
#   longest_string(input_string, letter): looks up variable 'input_string', looks up variable 'letter', then runs the function body
#   longest_string_letter(?, ?): resolves the value, resolves the value, then runs the function body
#   length > max_length: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def longest_string_letter(lst, letter) — stored for later
#   2. Assign to lst
#   3. Run print(longest_string_letter(...))
#       calls longest_string_letter with: lst=lst, letter='n'
#         3.1 Return from function
#   4. Run print(longest_string_letter(...))
#       calls longest_string_letter with: lst=lst, letter='a'
#         4.1 Return from function
#   5. Define def longest_string(string, letter) — stored for later
#   6. Assign to input_string
#   7. Assign to letter
#   8. Run print(longest_string(...))
#       calls longest_string with: string=input_string, letter=letter
#         8.1 Assign to words
#         8.2 Define def letter_ends(word) — stored for later
#         8.3 Assign to longest
#         8.4 Assign to max_length
#         8.5 For loop over words
#         8.6 Return from function
#   9. Define def longest_string_letter(lst, letter) — stored for later
#   10. Define def longest_string_letter(lst, letter) — stored for later
#   11. Assign to lst
#   12. Assign to letter
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(longest_string_letter(...))
#       calls longest_string_letter with: lst=[1, 3, 5, 7], letter=[4, 1, 8, 6, 3]
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions longest_string_letter, longest_string, longest_string_letter, longest_string_letter are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(longest_string_letter(lst, 'n')) → function runs first, then print shows the result
#      print(longest_string_letter(lst, 'a')) → function runs first, then print shows the result
#      input_string = ... (stored in memory)
#      letter = ... (stored in memory)
#      print(longest_string(input_string, letter)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      letter = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(longest_string_letter(?, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 495:
#
# ===== SYNTAX =====
#   class Animal — defines a new type
#     def speak — method of Animal
#   class Dog(Animal) — inherits from parent
#     def speak — method of Dog
#   class Cat(Animal) — inherits from parent
#     def speak — method of Cat
#   def solve()
#   return — exits the function and sends back a value
#   Dog() — built-in function call
#   Cat() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(dog.speak()): inner expression resolves first, then print() outputs
#   print(cat.speak()): inner expression resolves first, then print() outputs
#   print(Animal()): inner expression resolves first, then print() outputs
#   print(dog.speak()): inner expression resolves first, then print() outputs
#   print(cat.speak()): inner expression resolves first, then print() outputs
#   print(dog_2.speak()): inner expression resolves first, then print() outputs
#   print(cat_2.speak()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Animal
#   2. Define class Dog(Animal)
#   3. Define class Cat(Animal)
#   4. Assign to dog
#   5. Assign to cat
#   6. Run print(dog.speak(...))
#   7. Run print(cat.speak(...))
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to obj
#   11. Run print(obj)
#   12. Run print(Animal(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      dog = ... (stored in memory)
#      cat = ... (stored in memory)
#      print(dog.speak(...))
#      print(cat.speak(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Animal(...))

"""

"""
Problem 496:
#
# ===== SYNTAX =====
#   class Vehicle — defines a new type
#     def __init__ — method of Vehicle
#   class Car(Vehicle) — inherits from parent
#     def __init__ — method of Car
#   def solve()
#   Car() — built-in function call
#   super() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(Vehicle(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Vehicle
#   2. Define class Car(Vehicle)
#   3. Assign to my_car
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(Vehicle(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Vehicle.__init__ runs automatically when you create an instance
#   Car.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      my_car = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Vehicle(...))

"""

"""
Problem 497:
#
# ===== SYNTAX =====
#   class Person — defines a new type
#     def __init__ — method of Person
#     def introduce — method of Person
#   class Student(Person) — inherits from parent
#     def __init__ — method of Student
#     def introduce — method of Student
#   def solve()
#   return — exits the function and sends back a value
#   Person() — built-in function call
#   Student() — built-in function call
#   super() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(person.introduce()): inner expression resolves first, then print() outputs
#   print(student.introduce()): inner expression resolves first, then print() outputs
#   print(Person('Bob')): inner expression resolves first, then print() outputs
#   print(person.introduce()): inner expression resolves first, then print() outputs
#   print(student.introduce()): inner expression resolves first, then print() outputs
#   print(person_2.introduce()): inner expression resolves first, then print() outputs
#   print(student_2.introduce()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Person
#   2. Define class Student(Person)
#   3. Assign to person
#   4. Run print(person.introduce(...))
#   5. Assign to student
#   6. Run print(student.introduce(...))
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to obj
#   10. Run print(obj)
#   11. Run print(Person(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Person.__init__ runs automatically when you create an instance
#   Student.__init__ runs automatically when you create an instance
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      person = ... (stored in memory)
#      print(person.introduce(...))
#      student = ... (stored in memory)
#      print(student.introduce(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Person(...))

"""

"""
Problem 498:
#
# ===== SYNTAX =====
#   class Shape — defines a new type
#     def area — method of Shape
#   class Rectangle(Shape) — inherits from parent
#     def __init__ — method of Rectangle
#     def area — method of Rectangle
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(Shape(25, 25)): inner expression resolves first, then print() outputs
#   self.length * self.width: first get self.length, then get self.width, then compute self.length * self.width
#
# ===== EXECUTION ORDER =====
#   1. Define class Shape
#   2. Define class Rectangle(Shape)
#   3. Assign to rectangle
#   4. Run print(?)
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(Shape(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Rectangle.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      rectangle = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(Shape(...))

"""

"""
Problem 499:
#
# ===== SYNTAX =====
#   class Appliance — defines a new type
#     def __init__ — method of Appliance
#   class WashingMachine(Appliance) — inherits from parent
#     def __init__ — method of WashingMachine
#     def wash_clothes — method of WashingMachine
#
# ===== EVALUATION ORDER =====
#   print(washing_machine.wash_clothes()): inner expression resolves first, then print() outputs
#   print(Appliance(25, 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Appliance
#   2. Define class WashingMachine(Appliance)
#   3. Assign to washing_machine
#   4. Run print(washing_machine.wash_clothes(...))
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(Appliance(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Appliance.__init__ runs automatically when you create an instance
#   WashingMachine.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      washing_machine = ... (stored in memory)
#      print(washing_machine.wash_clothes(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Appliance(...))

"""

"""
Problem 500:
#
# ===== SYNTAX =====
#   class Employee — defines a new type
#     def __init__ — method of Employee
#   class Manager(Employee) — inherits from parent
#     def __init__ — method of Manager
#     def assign_task — method of Manager
#
# ===== EVALUATION ORDER =====
#   print(manager.assign_task('Prepare a project report')): inner expression resolves first, then print() outputs
#   print(Employee('Bob', 25)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class Employee
#   2. Define class Manager(Employee)
#   3. Assign to manager
#   4. Run print(manager.assign_task(...))
#   5. Assign to obj
#   6. Run print(obj)
#   7. Run print(Employee(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Employee.__init__ runs automatically when you create an instance
#   Manager.__init__ runs automatically when you create an instance
#   Execution starts at the non-def lines below:
#      manager = ... (stored in memory)
#      print(manager.assign_task(...))
#      obj = ... (stored in memory)
#      print(obj)
#      print(Employee(...))

"""

