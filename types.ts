export interface Exercise { id: number; title: string; description: string; initialCode: string; solution: string; hint: string; category: string; }
export interface Stats { shots: number; success: number; failed: number; }
